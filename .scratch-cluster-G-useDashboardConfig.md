# Phase 1 re-verification — useDashboardConfig.ts (19 findings)

File: `src/app/(dashboard)/allocations/hooks/useDashboardConfig.ts` (608 lines, current head)

Legend: KEEP = still applies, fix needed. PRE = already fixed in code. OOS = out-of-scope (cross-file or non-threshold).

## CRITICALS

- **C-0333** (c10, L312) — loadV2Config empty catch on corrupt JSON. **PRE.** Current L424-440 has `console.warn` + `setRecoveryFlag("parse_failed")`. Tests at lines 599-655 cover.
- **C-0334** (c10, L102) — persistLegacy empty catch. **PRE.** Current L164-172 logs via `console.warn`. Test at lines 721-743 covers.

## HIGHS

- **H-0120** (c9) — Two hooks → same key → reset loop. **PRE.** Current code has `hasMutated.current` observe-without-write guards on both hooks (L211-218 and L534-541). Dual-hook ping-pong tests at L499-570 cover. KEEP guard, no further work.
- **H-0122** (c9) — Delete dormant legacy hook (~175 LOC). **KEEP.** Legacy hook + LEGACY_DEFAULT_LAYOUT + tests still present, no live callers (verified via grep). DELETE per finding's fix instruction.
- **H-0121** (c8) — CHAIN of dual-write destructive loop. **PRE** (same fix as H-0120 — observe-without-write guard). Also resolved by H-0122 deletion.
- **H-0123** (c8) — removeTile rapid-double-click stale closure. **KEEP.** Current L238-250 reads `config.tiles.find(...)` from closure outside `setConfig`. Two rapid clicks before re-render return the same tile twice. Fix needed. (Will be obsolete after H-0122 deletes the legacy hook — but H-0122 needs explicit decision because legacy code still on disk. Will sequence: do H-0123 fix first, then H-0122 delete after.)
  - Actually: since H-0122 deletes the whole function, H-0123 becomes moot. Will close both via the H-0122 deletion. Closure note: H-0123 → closed-by H-0122 deletion.
- **H-1214** (c8) — moveWidget silent no-op when tile not found. **KEEP.** Current L573-584 returns `prev` silently. Fix: return boolean, console.warn on miss. Caller change at WidgetChrome.tsx is **OOS** (cross-file ARIA wiring) — but the hook-side fix is in-scope. Will keep API backward-compatible: log + return prev when not found (no signature change). Note OOS for caller-side aria gating.
- **H-1215** (c7) — version_reset silently wipes user mutations on bump. **PRE.** Current L364-370 already sets `setRecoveryFlag("version_reset")`. Plus `hasMutated.current` guard at L534-541 means the version-reset state is NOT persisted on first effect (no overwrite). Tests at L611-626 cover. The fix's "backup the old blob" suggestion is a nice-to-have, but the silent-destruction failure mode is closed. Marking PRE. The backup-old-blob enhancement is beyond threshold rationale and deferred.

## MEDIUMS (conf ≥ 8)

- **M-0126** (c9) — persist no debounce. **KEEP.** Current L535-541 persists on every config change. Fix: trailing debounce (~150-200ms) in V2 persist effect.
- **M-0130** (c9) — `JSON.parse(raw) as DashboardConfig` runtime drift. **KEEP.** Fix: hand-rolled validator (no new dep). Pair with M-0127 and M-1076 — same root.
- **M-1076** (c9) — `w` not validated on load. **KEEP.** Fix: clamp each tile's `w` via `clampWidth` during load normalization. Pairs with M-0130/M-0127.
- **M-0127** (c8) — Same runtime/static drift, V2+legacy. **KEEP.** Closed together with M-0130/M-1076.
- **M-0128** (c8) — clampWidth(unknown) silent default `2`. **KEEP.** Fix: console.warn when the input isn't a finite number (developer mistake should surface), still return 2 as fallback so the registry-defaultW callsite stays robust.
- **M-0129** (c8) — loadConfig corrupt-JSON catch untested. **PRE.** Tests at L585-655 + L710-743 cover the V2 + legacy corrupt-JSON paths. PR-test-analyzer's test list is largely now in place; tiles.length===0 path is asserted indirectly. Keep tile-validation test alongside M-1076 fix.
- **M-0131** (c8) — Per-tile validation (NaN/-Infinity/string ids). **KEEP.** Closed together with M-0130/M-1076/M-0127.
- **M-0132** (c8) — generateTileId collision-walk untested. **OOS via H-0122.** Whole legacy hook is being deleted; the helper goes too. Close via H-0122 deletion.
- **M-0133** (c8) — Comment "skip initial mount" lies. **PRE.** Current code at L211-218 actually skips the first persist via `hasMutated` ref. The comment is now truthful.
- **M-0134** (c8) — useEffect persist on every change (perf). **KEEP / DUP-M-0126.** Same as M-0126; closed by debounce.
- **M-0135** (c8) — Comment references "old implementation"/"the fix" (CLAUDE.md style). **KEEP.** Trim comments inside loadV2Config (L386-403) and L404-418 that talk about pre-fix vs post-fix state. Surgical comment edits only.

## Plan

Order of inline commits:
1. Validate-and-normalize on load (M-0130, M-1076, M-0127, M-0131) — single concern: hand-rolled per-tile validator inside `loadV2Config`, drops invalid tiles, falls back to defaults if all are invalid.
2. Debounce persistV2 (M-0126, M-0134) — single concern.
3. moveWidget no-op surface (H-1214) — console.warn on miss; signature unchanged for backward-compat.
4. clampWidth warn on invalid input (M-0128).
5. Comment cleanup (M-0135) — surgical comment edits.
6. Delete dormant legacy hook + tests + helpers (H-0122) — also closes H-0121, H-0123, M-0132.

Then Phase 3 (six specialists), Phase 4 (apply), Phase 5 (red team), Phase 6 (apply).

## Closures so far (Phase 1)
- C-0333 → pre-existing
- C-0334 → pre-existing
- H-0120 → pre-existing
- H-0121 → pre-existing
- H-1215 → pre-existing
- M-0129 → pre-existing
- M-0133 → pre-existing
