# Contracts registry (B25 lint-consolidation capstone)

This directory is the **discoverable home + fail-loud pin** for the cross-cutting
refactor program's by-construction invariant guards. The guards themselves live
where they're most local (next to the code they protect); `contracts-registry.test.ts`
pins the *set* of them so a guard can't be deleted/renamed and leave CI green —
the silent-reintroduction gap B25 closes.

Two enforcement layers ship here:

1. **`tools/eslint-plugin-quantalyze/`** — edit-time AST rules (set to `"error"`
   in `eslint.config.mjs`, so a future raw offender fails `frontend-lint` CI).
2. **`contracts-registry.test.ts`** — the registry guard (existence + plugin/config
   wiring integrity) + **`.github/workflows/contracts.yml`** — the named CI surface.

## Honesty gate — what does NOT get a lint rule

The capstone's first job was an inventory pass: skip anything already enforced.
Most classes are closed by a **type brand / discriminated union / SECDEF RPC**,
which is strictly stronger than a bypassable lint rule — so they get NO rule:

| Batch | Class | Enforced by | Rule? |
|---|---|---|---|
| B1 | money-unit mixing | nominal `Usd`/`Ratio`/`Fraction` brands + `safe*` validators | type-enforced — no rule |
| B6 | factsheet no-invented-data | `FactsheetApiPayload\|FactsheetCsvPayload` union | type-enforced — no rule |
| B8 | closed-sets | `SUPPORTED_EXCHANGES` + `satisfies` + `closed-sets.test.ts` | type-enforced — no rule |
| B4c | audit action↔entity_type | `AUDIT_ACTION_ENTITY_TYPE_MAP` union + `@ts-expect-error` test | type-enforced — no rule |
| B19 | chunked IN-query | `analytics-service/services/db.py` (Python) | out of ESLint scope |
| B14 | freshness-signal-consumption | `freshness.ts` shipped; lint half not | **deferred → lands after B25** |
| B17 | labeled-metric-consumption | (runtime half not shipped) | **deferred → lands after B25** |

## eslint-plugin-quantalyze rules (the genuine AST delta)

| Rule | Batch | Bans | Canonical helper | Exemptions |
|---|---|---|---|---|
| `no-raw-localstorage` | B7 | `localStorage` / `window.localStorage` member access | `useCrossTabStorage` (`@/lib/storage`) | `src/lib/storage/**`; files with `B7 sanctioned-exception:`; test files |
| `no-raw-published-predicate` | B10 | `.eq("status","published")` | `withPublishedOnly` (`@/lib/visibility`) | files with `B10 sanctioned-exception:` / `B10 visibility:`; test files |
| `no-raw-retry-after-parse` | B20 | `Number()/parseInt()` of a Retry-After header | `parseRetryAfterSeconds` (`@/lib/retry`) | `src/lib/retry/**`; files with `B20 sanctioned-exception:`; test files |
| `no-passthrough-on-ipc` | B9 | Zod `.passthrough()` / `.catchall()` / `.loose()` / `z.looseObject()` (all four Zod-v4 "keep unknown keys" forms) on a boundary parser (NEW-C40-01 leak class) | `.strict()` (fail loud) or default `.strip()` | per-site inline `// eslint-disable-line quantalyze/no-passthrough-on-ipc -- B9 sanctioned-exception:` (~22 forward-compat read-only envelopes: HTTP responses + widget render contracts); test files. **Enforced repo-wide, not file-scoped** — a file allowlist could go stale when a new boundary module is added. Matched by method NAME (not Zod-type resolution): a future unrelated non-Zod `.passthrough()`/`.loose()` would also flag and the same inline escape is the sanctioned mechanism. |
| `no-rem-less-clamp` | DS-04 (v1.4 ph49) | a CSS `clamp()` whose preferred (middle) term is viewport-only (no `rem`/`em`) — the WCAG 1.4.4 / W3C F94 zoom-unsafe shape, e.g. `clamp(2rem, 3vw, 4rem)` | a `rem`-anchored middle term, e.g. `clamp(1rem, 0.95rem + 0.25vw, 1.125rem)` | numeric `Math.clamp(...)` (no viewport unit) is invisible to it; `src/components/charts/**`; test/fixture files; files with `DS-04 sanctioned-exception:`. **Enforced repo-wide at `"error"`** (the baseline has zero rem-less clamp strings). |
| `no-raw-analytics-fetch` | SEAM-01 (v1.16 ph140) | a raw `fetch()` of the analytics service base URL — any identifier INITIALIZED from `process.env.ANALYTICS_SERVICE_URL` (through `??`/`||` chains, destructuring or computed access) that is then fetched as a bare arg, a template slot, or a `+` concatenation | `resilientFetch(budgetKey, path, init)` (`@/lib/resilient-fetch`), which owns the `SEAM_BUDGETS` timeout and the shared `breaker:railway` circuit breaker | a CLOSED four-path allowlist in `eslint.config.mjs`: the core itself plus the three documented `SEAM_EXCLUSIONS` (`debug-key-flow/**` bespoke SSE, `cron/warm-analytics/**` and `warmup-analytics.ts` health warmers). **No in-file escape hatch, by design** — a violation means a new seam call site, which belongs in the core, so widening the allowlist is always the wrong fix (T-140-26). `contracts-registry.test.ts` asserts both the repo-wide error and all four exemptions, frozen at four. |
| `no-raw-font-px` | DS-04 (v1.4 ph49) | a raw px font-size — `text-[NNpx]` arbitrary class or `fontSize: "NNpx"` inline style (decimal + uppercase variants too) | a fluid `--text-*` token / `text-*` utility | `src/components/charts/**`; test files; files with `DS-04 sanctioned-exception:`. **SCOPED, not repo-wide:** `"error"` only on the clean `src/lib/design-tokens/**` surface, `"warn"` on the 558-site dirty baseline — a strangler that ratchets to error per-surface in phases 52/53 (a repo-wide error would red-CI the existing app). |

Most rules are `"error"` repo-wide (not `"warn"`): the recon proved a clean baseline, so they fail
CI by construction on a future raw offender — the literal goal of the capstone.
The escape hatch is a greppable, batch-tagged `B<n> sanctioned-exception:` comment.
The one deliberate exception is `no-raw-font-px` (DS-04), which is `"error"` only on the
clean `src/lib/design-tokens/**` surface and `"warn"` on the dirty `text-[NNpx]` baseline
pending the per-surface strangler in phases 52/53 — `contracts-registry.test.ts` asserts both
its scoped error and its intentional warn so neither can silently flip.

## Registered invariant guards

The authoritative list is `CONTRACT_GUARDS` in `contracts-registry.test.ts`
(grep sweeps, parity matrices, compile-time pairings, registry-completeness checks,
and the three `check-*` CI-gate scripts). Edit that array + this file together when
adding or removing a guard.

⚠️ **This prose table is a SUBSET of the array, and always has been** — 16 rows
against 44 before plan 140.4-10, 23 against 51 after it (that plan added one prose
row per array row it added, and did not backfill the pre-existing gap). Nothing
asserts the two are in step, so the drift is invisible to CI. Read the array as
authoritative and this table as the annotated part of it.

⚠️ **Two phase-140.4 guards are NOT registerable here and are residuals, not
omissions:** `analytics-service/tests/test_raw_5xx_census.py` (the raw-5xx census,
plan 140.4-04) runs under pytest, and `supabase/tests/test_csv_finalize_double_submit.sql`
(plan 140.4-03) runs in the `psql` lane. Every path in `CONTRACT_GUARDS` is TS-side
and the runner is vitest, so adding either would make `existsSync` pass while no
registered run ever executes it. A cross-runner registry is unbuilt.

| Guard | Batch | Invariant |
|---|---|---|
| `src/lib/sample-floor.test.ts` | Phase22 | `SAMPLE_FLOOR_OVERLAPPING_DAYS=60` value pin + every `evaluateSampleFloor` gate branch (ok / below-floor / no-usable-n incl. null/NaN/Infinity/negative / per-call override). The HONEST-02 single-source floor — fails loud if a future feature (Phases 26/27) forks its own floor instead of importing this one. |
| `src/lib/seam-constants.pin.test.ts` | 140.2 (v1.16) | The seam core's tuning values, every expectation a hand-typed literal: 13 `SEAM_BUDGETS` `timeoutMs` values, the `SeamBudgetKey` SET as a **sorted equality rather than a length** (a length check passes a RENAME), all six breaker constants, the A-14 ordering invariant (`BREAKER_COOLDOWN_S` ≥ `BREAKER_WINDOW`, or the counter has not decayed when the lock expires and the breaker flaps forever), `SEAM_RETRIES === 0` as a NEGATIVE pin (Phase 141 owns raising it), and the two fake↔production literal pins. **Exists because Phase 140 certified "5/5 mutation-tested" while ten simultaneous semantic mutations to the core produced a byte-identical `8859 passed`** — 0 of 13 budgets and 5 of 6 breaker constants were pinned to anything, every assertion having read its expected value out of the table under test. Zero module mocking, grep-asserted. |
| `src/lib/seam-budgets.invariant.test.ts` | 140.2 (v1.16) | The seam budget invariant. Reads each of the 15 route files' `export const maxDuration` **from disk** (anchored `^…/m`, so a comment cannot satisfy the guard) and checks it against `SEAM_ROUTE_BUDGETS`, then checks the summed `timeoutMs × calls × (1 + SEAM_RETRIES)` fits inside it. Plus (140.2) a deep compare of every route row's CONTENTS against a hand-typed 15-row map — the only assertion that can see a **leg dropped from a multi-leg row**, which silently halves that route's worst case while SC-4a and SC-4b both get *more* comfortable — and a sorted-set pin of the three `SEAM_EXCLUSIONS` paths. SC-4b charges the breaker's own store round trips in the closed / open / failing states (140.2-07), and — 140.2-10 / SEAMCORE-10 — sums within a **`branch`** and takes the **MAXIMUM across branches**: `finalize-wizard`'s composite path (one cache-bypassing probe **per member**, then `runLegacyFinalize`, whose enqueue is a Supabase RPC and therefore spends **zero** `process-key-enqueue`) and its single-key path are mutually exclusive, so the pre-140.2-10 single summed row described a path no request takes *and* modelled the fan-out branch — the one that can actually reach the ceiling — not at all. **SC-4e** then binds the declared composite `calls` to `MAX_COMPOSITE_MEMBERS` read from the **route file on disk** (a Next.js route module cannot `export` an arbitrary constant, so the disk read is the only genuine cross-file link available): raising the cap without raising the declaration reddens here, where every other assertion in the file would stay green while SC-4b certified headroom the route no longer has. Ledger rows **M24 / M32 / M41 / M47**. |
| `src/lib/seam-errors.purity.test.ts` | 140.2 (v1.16) | The seam error leaf stays a dependency-free leaf. Comment-stripped, `^…/m`-anchored source scan of `src/lib/seam-errors.ts` asserting **no import statement, no `export … from` re-export** (a dependency edge in everything but name), **no `require()`** and **no `process.env` read**, plus a hand-typed SET of its exported class names and a byte-for-byte pin of `CircuitOpenError`'s static message asserted across two different `retryAfterS` values (so an interpolated message cannot pass by coincidence — T-140-05: the unauthenticated `verify-strategy` teaser reaches the seam). The purity is load-bearing twice over: `wizardErrors.ts` value-imports the class and is value-imported by ten `"use client"` components (so a dependency here ships a Redis client to the **browser bundle**), and sixteen route tests replace the seam clients wholesale (so `instanceof` through a re-export would evaluate against `undefined` and throw a `TypeError` from inside a catch block, turning a clean 503 into a crash). **Exists because prepending an `@upstash/redis` import to the leaf was GREEN across the full suite — there was no `seam-errors.test.ts` at all.** |
| `src/lib/seam-discriminator.purity.test.ts` | 140.2 / SEAMCORE-01 (v1.16) | The seam **discriminator** leaf stays a dependency-free leaf. Same comment-stripped, `^…/m`-anchored source scan as the error leaf's guard — **no import statement, no `export … from`, no `require()`, no `process.env`** — plus a hand-typed SET of its exported names. The set is exactly **three predicates**: the closed service-dependency vocabulary and the residual global-key literal are deliberately **not** exported, because everything exported from a leaf is browser-reachable and survives every wholesale seam mock, so an exported vocabulary would let a consumer build its own breaker key — the one thing threat T-140-01 exists to prevent. The purity is load-bearing because plan `140.3-01` applies this predicate at a `"use client"` component; the guard's failure message names the specific wrong "tidy-up" (importing the vocabulary from `resilient-fetch.ts` to de-duplicate it), which would both ship a Redis client to the browser and destroy the two-independent-vocabularies falsifier. |
| `src/lib/tenant-claim.test.ts` | 140.2 / TS-04 (v1.16) | The **ONE `X-Tenant-Claim` mint** (ROADMAP SC7), extracted from `process-key-client.ts` so that a wire format which must agree with a **third implementation in another language** (`analytics-service/services/rate_limit.py:verify_tenant_claim`) has exactly one TS source. Every expected MAC is recomputed **in the test** with `node:crypto` or read from the committed fixture — none is imported from the module under test, because an oracle that asks the code what it expects cannot fail (140.1's mutation #3, `default_platform_key` returning `""`, shipped GREEN against an identity assertion). The **clock is stubbed and the crypto is real**, which is the right way round: the MAC is the half that has to agree with the other language. Pins the 300s TTL as a **hand-typed** literal (the constant is deliberately NOT exported), an exact claim string under a fixed clock, two payloads ⇒ two different MACs, and the same payload under two different secrets ⇒ two different MACs — that last one being the shape that matters most, because keying the MAC on `ANALYTICS_SERVICE_KEY` instead of `INTERNAL_API_TOKEN` still produces a **syntactically perfect** claim that simply fails `compare_digest`, leaving every route on `platform:<path>` with **no error anywhere**. Plus the four fail-loud refusals — empty payload (a claim over `""` buckets every caller together while looking exactly like working isolation), whitespace-only, dot-containing (the verifier's `rsplit(".", 2)` is dot-tolerant and the payload reaches the bucket key verbatim), and absent/empty secret — each a named `TenantClaimError` whose message names the env var and never its value. The committed case table `tests/fixtures/tenant-claim-parity.json` is read **both ways**: round-tripped through the shipped mint, and self-checked against an independently computed HMAC so a hand-edited `claim` cannot turn the round-trip into a pin on a typo — with a non-empty guard, because a silently emptied fixture makes every `it.each` vacuously pass. ⚠️ **The Python half of this parity gate is OWED**, not done: no pytest reads that fixture yet (`140.1-TS-OBLIGATIONS.md` **TS-36**, owner Phase 146). Ledger rows **M28 / M44 / M45 / M46**. |
| `src/lib/seam-discriminator.test.ts` | 140.2 / SEAMCORE-01 (v1.16) | The **status → breaker-verdict table** (ROADMAP SC2), every expectation hand-typed and zero module mocking. CALLER (`400/401/403/404/422`), CALLER-THROTTLED (`429`, in **all three wire shapes that coexist at HEAD** — the flat app-global handler body, the nested `service_error` envelope, and the bare scalar carrying no `code` at all) and CALLER'S EXCHANGE (`424`) each record **zero**; SERVICE-PERMANENT (`500`) records **zero including the bodyless `text/plain` shape** Starlette's `ServerErrorMiddleware` emits, which is the single most common 5xx (TRAP-2 / O-4); SERVICE-TRANSIENT (`503`) records **one, keyed on its named dependency** (O-2). **Every row is driven twice — with its body and with no body argument** — which is what makes "counts/does-not-count is decidable from the status line alone" an assertion rather than a claim. Plus the T-140-01 half: a `424`'s venue slug never becomes a key **for any venue**, an unrecognised or non-string `dependency` on a `503` falls back to the global key with a loud diagnostic, and two different service dependencies produce two **different** keys (the leaf-level precondition for OB-8). Code and human-string extraction branch on `typeof body.detail` per `STATUS_CONTRACT.md` §2.1 — reading `body.detail.code` unconditionally regresses **both** flat shapes to `UNKNOWN`, the exact dead end PYAPIFIX2-01 exists to kill, reintroduced at the layer that was supposed to consume the fix (ledger row M25). |
| `src/lib/seam-redaction.test.ts` | 140.2 / SEAMCORE-06 (v1.16) | The seam's **redaction leaf**, asserted in **BOTH directions on the SAME input**. One realistic undici transport message carrying `INTERNAL_API_TOKEN`, `X-Service-Key` and a live end-user Supabase JWT alongside an `ECONNREFUSED 10.0.0.1:8002` fragment: every secret is gone **and** the syscall token, `errno` and `syscall` survive. A one-sided test is exactly how over-redaction ships green — TRAP-1 pulls in two directions, and a redactor that eats `ECONNREFUSED` has replaced one incident with two. Also pins the **short-secret refusal** (a candidate below `MIN_REDACTABLE_SECRET_LENGTH` = 12 is refused rather than substring-replaced, and the refusal is signalled in the returned line, naming the env var but never its value — ledger row **M33** lowers that constant so a 4-character secret eats `ECONNREFUSED`, and nothing else in the repo can see it), the preserve-collision signal, the error-**NAME** shape undici produces in a retry wrapper, the `cause` chain `SeamBodyReadError` has attached since 140.2-05, and the `undefined` / `null` / circular / hostile-`toString` inputs — a scrubber that throws runs inside a `catch` block and would replace the caller's real error with a bookkeeping one. Plus the leaf-purity half (no `import`, no `export … from`, no `require()`) and the one purity rule that DIFFERS from the other two leaves: `process.env` is read at **call** time and must never be captured at module scope, because a captured value is stale for a rotated secret and for every test that stubs the env after import. Every secret value, preserve token and list member is hand-typed in the test rather than read back out of the module. |
| `src/lib/seam-log-coverage.test.ts` | 140.2 / SEAMCORE-06 (v1.16) | **No `console.error` / `console.warn` in the six seam files passes a raw caught error.** A source scan rather than a behavioural test, because the failure mode is a **new** log site appearing that nobody wrote a test for — a behavioural test can only cover sites it names. Two halves: bare **catch-bound** identifiers, and bare **error-shaped bindings** (`error`, `*Err`, `*Error`), which is how the Supabase / PostgREST errors arrive — five of those were scrubbed on `finalize-wizard` and three were not, the instance-not-class shape this programme exists to close. The scanner is comment-stripped, **string-literal-masked** (a message containing the word *error* is prose; `${err}` inside a template **interpolation** is a real reference, so interpolation depth is tracked rather than the literal skipped) and uses a **quote-aware** balanced-paren argument extraction, because one real site logs `(CT-7)` inside a template. The allowlist is by reason, not convenience: wrapping in `scrubSeamError` / `scrubSeamString`, plus `.retryAfterS`, `.deadlineExceeded` and `.code` — non-string facts from types we define, none of which can carry an undici header. Carries its own **positive control, negative control and a fail-loud lower bound** on sites scanned, because a guard that asserts an ABSENCE reads as protection while proving nothing if its scanner silently stops matching. The failure message names the reason (undici embeds outgoing headers, and on this seam that is a live user JWT) and names the wrong fix (dropping `err` instead of scrubbing it — the A-10 defect). Ledger row **M42**. |
| `src/lib/resilient-fetch.wiring.test.ts` | 140 / 140.2 (v1.16) | Both seam clients demonstrably invoke the ONE core (SC-1c), the two security-critical headers forwarded byte-for-byte — and, added in 140.2, **the 13 budget-key BINDINGS**. The class is bindings, not the 5 lexical call sites: two of the five take their key from a **variable** (nine `analytics-client` wrappers fan out through one; `budgetKeyFor(flow_type)` computes another), so a call-graph enumeration reports 5 and is wrong. Each binding's key AND upstream path are pinned to hand-typed literals through a partial mock of the transport only, so the genuine `SEAM_BUDGETS` table stays in play. The load-bearing half is the **completeness mechanism**: the three binding families are DISCOVERED by reading source from disk and compared **as a SET** against a hand-typed 13-member roster (`EXPECTED_BINDINGS`), in the `limiter-ordering.test.ts` walk-classify-fail-on-unclassified idiom. A 14th binding that REUSES an existing budget key breaks no individual pin and fails only here (ledger row M22b); a hand-typed six-file seam-call set additionally catches a new call site that passes its key as a variable and would therefore belong to no family. |
| `tools/eslint-plugin-quantalyze/tests/no-raw-analytics-fetch.test.ts` | 140.2 / SEAM-01 (v1.16) | The `RuleTester` fixtures pinning **what the rule actually catches**, as opposed to that it exists and resolves to `"error"`. One invalid fixture per documented URL shape — the env member read inline in a template; one binding fetched through a template slot; an **aliased** template (`const url = \`${base}/x\`; fetch(url)`); an aliased `new URL(path, base)` — plus the inline-`new URL()` regression (caught all along, contrary to CONTEXT's claim, and pinned so a refactor of the subtree recursion cannot lose it), a **two-hop declaration-order** case pinning that the fixed point is resolved at `Program:exit`, and the two `/health` warmers' exact `${url.replace(/\/+$/, "")}/health` shape — pinned here because the four-path allowlist makes the rule's verdict on that shape invisible in the real files. Two discriminators, one per detection property: *"renamed binding"* (init-tracking, not name-matching) and an **untainted two-hop alias** structurally identical to the aliased-template case, differing only in which env var seeded it (so widening the taint cannot be confused with the rule starting to match bindings called `url`). **Registered in 140.2-04 because it was silently deletable:** moving the file away left the whole contracts suite and `npm run lint` green — the three wiring assertions pin the rule's registration, severity and allowlist, and none of them notices that its behavioural fixtures are gone. The other eight rules' fixture files are in the same state; only SEAM-01's is claimed here. |
| `src/lib/seam-copy.pin.test.ts` | 140.3-G1 (v1.16) | The seam's **user-facing copy**, pinned to hand-typed strings — what a person actually reads when the analytics service is down, degraded or throttled. This is the one class a passing route test cannot cover: a route can return exactly the right status with exactly the wrong sentence, and during an outage the sentence *is* the product. **Registered at 140.3 gap closure because it was silently deletable** — moving the file away left CI green, the same state 140.2-04 found the eslint fixture in. |
| `src/lib/seam-poll-disjointness.pin.test.ts` | 140.3-G1 (v1.16) | **SOURCE BACKSTOP** for the wizard sync-poll, and the smaller half of a re-tiering. An **inverted allow-list** of every `/api/` URL the three poll modules may name (an unlisted URL is a hard failure, in the `check-admin-route-manifest.ts` "no known mechanism matched" idiom), with each entry's seam-ness **hand-typed and independently re-checked** against the seam-route set derived from `src/app/api` by import edge — so the tick's target acquiring a seam import reddens *even though no URL literal changed*. Plus a fail-loud `CALL_EDGE` yield bound and a six-slot needle self-test whose discriminator is the `wizardFetch(` spelling. ⚠️ **Why it was re-tiered:** this file used to assert *"ZERO fetch calls exist on the poll path"*. That was **false at HEAD** — `SyncPreviewStep.tsx` makes three real network calls, two of them to the seam route `/api/keys/sync` — and the assertion passed anyway because its needle was `/\bfetch\s*\(/` while every call on that path is spelled `wizardFetch(`. **The guard was green under the exact mutation it existed to prevent.** Widening the needle does not repair it: the file-scoped claim is simply untrue, so a wider needle reddens on a clean tree. The property that IS true is tick-scoped and now lives at runtime (row below). This file is **kept, not deleted**, because it covers the two roster modules the runtime guard never renders. Ledger rows **M86 / M87**. |
| `src/lib/seam-ssr-exposure.pin.test.ts` | 140.3-G1 (v1.16) | **Zero server directives under `src/`**, and no server-rendered module outside `src/app/api/` imports a seam client. That pair is the single reason a breaker trip can never 500 a page: every seam call happens in a route handler or in a browser component's fetch, so when the circuit opens the shell still renders and the failure arrives as a sentence with a retry rather than as a locked-out user. The needles are **assembled at runtime from fragments**, so the file never contains the directive it forbids — a guard that plants the string it bans makes the repo-wide acceptance grep return a hit forever and trains the next reader to ignore it. **Registered at 140.3 gap closure because it was silently deletable.** |
| `src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.poll-disjointness.runtime.test.tsx` | 140.3-G1 (v1.16) | **RUNTIME, TICK-SCOPED retry-storm fence** — the load-bearing half of the re-tiering described two rows up. Spies on `globalThis.fetch`, which **every** wrapper ultimately reaches (`wizardFetch`'s body calls the *unqualified* global, and so would a future `apiFetch`, a wrapper of a wrapper, or an inline `const f = fetch`), so it is indirection-proof **by construction** in a way no source needle can be. It splits the render at a runtime **mark**: the user-initiated kickoff *may* call the seam and does (`POST /api/keys/sync`); the repeating tick *may not*. Tick-ness is a fact about **when a callback fires**, not about which file a call site sits in — which is why this could never have been a regex. The seam-route set is **derived from disk** and matched **segment-wise**, so `/api/keys/syncing` is not `/api/keys/sync` and the live tick target `/api/strategies/[id]/sync-progress` stays a near-miss negative rather than a false alarm. Carries a **positive control** (a tick call must actually fire), because a dead poll and a safe poll both produce "no seam call" — the M77c shape, where a deleted carry and a correct absence were indistinguishable to 3,163 tests. Ledger rows **M86 / M86b**. |
| `src/lib/seam-ratelimit-posture.invariant.test.ts` | 140.4-13 / SEAMRIM-05 (v1.16) | **A limiter MISCONFIGURATION answers 503 on every seam route — never a 429 that reads as the caller being throttled.** The limiter runs FIRST in every seam route, so when Upstash is unreachable the breaker beside it never even trips: the deny was the only signal there was, and on 11 of the 15 routes it was a 429. On the key-connect path the wizard renders that as `KEY_RATE_LIMIT`, whose copy calls the throttle *"exchange-side and not a problem with your key"* — false for every user on their first click, for as long as our own store is down, and invisible to a canary that watches 5xx. **Two halves with different reach, and the file states its own boundary rather than implying coverage it lacks.** *Structural (all 15 routes, population derived from `src/app/api` by import edge on every run):* partitions the seam into limiter / no-limiter, pins the limiter set against a **hand-typed** roster so a new seam route fails **by name**, and asserts chokepoint-routed deny arms **≥ `checkLimit` sites per route** — **per ARM, not per file**, because `keys/sync` has two and a file-level *"does it mention `rateLimitDenyJson`"* check stays green with the second reverted to an inlined 429 (ledger row **M105** runs exactly that mutation). The no-limiter set is an **equality** against `{admin/match/eval}`, in `test_limiter_identity.py:462-479`'s idiom and for its reason: a future repair must **shrink** the quarantine in the same commit rather than leave a stale exemption. Sources are **comment-stripped before counting**, which is load-bearing and not cosmetic — `admin/match/eval` and `admin/match/recompute` both name `rateLimitDenyJson` in **prose**, so a line-based grep (DEF-16-2) reads the *no-limiter* route as having adopted the chokepoint and reports the class closed while it is open. Self-tested in **both polarities**, including that exact comment shape and the two-arm/one-routed-arm shape, behind a **vacuity fence** (≥ 15 routes, ≥ 12 `checkLimit` sites) because a scanner that matches nothing reports agreement forever. *Behavioural (3 of 15, spanning the three auth shapes — public `verify-strategy`, authenticated `keys/sync` at **both** arms, admin `match/recompute`):* drives the real handlers with `vi.spyOn` on the **real** `ratelimit` module, so the deny builder under test is production's and cannot drift from a double, and proves the 503 reaches **the wire** rather than merely appearing in source. A full 15-route behavioural table is **deliberately not attempted** — each route needs its own auth / body / downstream fixture, so fifteen bespoke stacks would each be free to drift; the remaining twelve are covered structurally here and behaviourally in their own `route.test.ts` files. |
| `scripts/check-route-contract.ts` | NAV-03 (Phase 51) | `ROUTE_CONTRACT_MANIFEST` ↔ `proxy.ts` `PUBLIC_ROUTES` + `next.config.ts` `redirects()` lockstep (the #512 class). A CI-gate script run in `npm run lint`: walks the `src/app/**` page tree and fails if a page route is unclassified, a manifest-`public` route is not covered by `PUBLIC_ROUTES` (anon would 307→login), a `redirectFrom` has no `redirects()` source (old link 404s), or a non-`exception` manifest entry maps to no page. Closes the route-drift class by construction at lint time. |
| `src/lib/seam-copy.purity.test.ts` | 140.4-10 / SEAMUX-01 (v1.16) | `src/lib/seam-copy.ts` stays a **dependency-free leaf** *and* stays a table of plain string **constants**. Comment-stripped, `^…/m`-anchored source scan — no `import`, no `export … from`, no `require()`, no `process.env` — plus a hand-typed **SET equality** over its exported names and an assertion that every export is a **string, never a function**. The purity is load-bearing because the copy is value-imported by `"use client"` components, so a dependency edge here ships a server module to the **browser bundle**. The string-not-function half is the subtler one: an outage sentence computed at render time is no longer visible to the copy pin that exists to stop the wrong sentence shipping. **Registered by 140.4-10 — it was silently deletable with green CI.** |
| `src/lib/sentry-capture.test.ts` | 140.4-10 / SEAMCORE-06 + SEAMRIM-04 (v1.16) | **`captureToSentry` scrubs BEFORE it dispatches**, asserted on every channel a secret can leave the process on: the error **message**, the error **name and stack**, an explicitly-passed **per-request** secret, a **short** per-request secret (HI-03 — the redaction leaf *refuses* to substring-replace below `MIN_REDACTABLE_SECRET_LENGTH`, so without a separate case a 4-character secret leaves for a third party), the **`cause` chain**, string values inside **`extra`**, and a **non-Error** thrown value. Plus the 140.4-06 half: the function **returns its import chain**, so `after(captureToSentry(...))` observes the capture instead of detaching it (NEW-C10-03), and it **resolves rather than rejects** when the SDK throws, when the dynamic import rejects, and when a hostile getter in `extra` explodes — a capture helper that throws runs inside a `catch` block and would replace the caller's real error with a bookkeeping one. **Registered by 140.4-10; it matches no naming convention** and was found only by knowing it was a property's sole asserter. |
| `tests/redis/seam-breaker.redis.test.ts` | 140.4-10 / SEAMCORE-09 (v1.16) | The breaker's behaviour against a **real Upstash instance** rather than a fake: counter identity and namespace (`breaker:<key>:failures:<epoch window>`, one counter **per key**), the trip at **exactly** the threshold (a literal 4 failures leave no lock; the 5th creates it), trip **idempotency** (a later trip does not ratchet the first writer's expiry), the encoded cooldown being the lock's life with `Retry-After` agreeing, sliding-window decay with weighted carry-over, and **no increment on a denied `limit()`**. ⚠️ **Registered with its limit stated:** this file runs only under `npm run test:redis`, whose `include` is `tests/redis/**` and which needs a live store — it is **not** in the default vitest run, so registration pins its **existence, not its execution**. **D-01, the live-Redis lane, is a KNOWN BLIND SPOT** carried by phase 140.4 and no plan may claim it closed. |
| `tests/lib/validate-key-venue-transient-parity.test.ts` | 140.4-10 / TS-35 (v1.16) | The wizard's key-validation classifier agrees with the **committed cross-language venue-transient contract fixture**, which reserved this file as its consumer — the assertion that a Python-side classification and a TS-side one cannot drift apart silently. Carries the canonical case roster as a **set** (no silent shrink), keeps its **negatives** (`recoverable` is pinned false somewhere, so it is not a field that is always true — the polarity failure that makes a boolean contract vacuous), asserts every case answers **exactly one** status without asserting *which*, pins the single permitted divergence as **the one that is actually there** rather than as a general tolerance, and re-checks that the surviving substring cascade classifies the same bytes when no code rides along. **Registered by 140.4-10; it matches no naming convention.** |
| `src/components/ui/LiveRegion.test.tsx` | 140.4-05 / SEAMRIM-10 (v1.16) | The **announcement primitive** phase 140.4 created, pinned in **both polarities** and for **visual inertness**. `assertive` ⇒ `role="alert"` + `aria-live="assertive"`; non-assertive ⇒ `role="status"` + `aria-live="polite"`; the message is carried in both; and the region **renders even when the message is null**, because a live region mounted only when it has something to say is announced by nothing — the AT has no region to watch. The **inertness** assertion is the load-bearing one: `className` is **exactly `sr-only`** in both polarities, because the entire reason this primitive exists rather than a `DESIGN.md` change is that it closes the a11y class **without touching a founder-owned visual decision**. Ledger row **M107** adds one utility class to it, under which the naive `grep -c 'className="sr-only"' → 1` receipt still **passes**. |
| `src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.readfailure.runtime.test.tsx` | 140.4-02 / SEAMRIM-02 (v1.16) | **RUNTIME proof that a read FAILURE is never rendered as a MEASUREMENT** — the C-3 defect, re-graded **up** because it was demonstrated by rendering. supabase-js **resolves rather than throws**, so a read-failed account and a genuinely-empty one produced **byte-identical DOM** (*"We found only 0 filled trade(s) on this key."*), and the under-pled half was worse: a 2-day strategy correctly blocked as `INSUFFICIENT_DAYS` became **`passed: true`** when the timestamp reads failed — **publishing an under-history track record as verified**, on a product whose core value is a trustworthy verified track record. Drives each read independently **including the second member** (`earliest`), so an instance-fix cannot masquerade as a class-fix; carries the **positive counterpart** (a genuinely empty account must still render the measured-zero sentence — without it, deleting the sentence outright would pass); asserts the **property, not an instance** (read-failed and empty render *different* DOM); and pins the three-failure escalation to the recoverable `SYNC_FAILED` surface, that a keyless draft still reaches a verdict, and that the failed read is **logged** so the escalation is diagnosable. |
| `tools/eslint-plugin-quantalyze/tests/no-unchecked-supabase-read.test.ts` | 140.4-14 / SEAMRIM-11 (v1.16) | The `RuleTester` fixtures pinning **what `no-unchecked-supabase-read` actually catches**, as opposed to that it exists and resolves to a level — the same silent deletability 140.2-04 found in the sibling fixture file, where moving it away left the whole contracts suite and `npm run lint` green. Covers **both destructure forms**: the object form (`const { data } = await q()`) and the `Promise.all` **array** form. The array half is load-bearing — **an object-only predicate returns zero on the array form, which is exactly how C-3 survived every prior census** — and it is the shape ledger row **M106** mutates. |

## Adding a new by-construction guard

1. Write the guard where it's most local (or add a rule to `eslint-plugin-quantalyze`).
2. Register it in `CONTRACT_GUARDS` + this file.
3. If it's an eslint rule, wire it in `eslint.config.mjs` and add a RuleTester fixture
   under `tools/eslint-plugin-quantalyze/tests/`.
