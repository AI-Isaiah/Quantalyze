# Fix briefing — `analytics-service/routers/portfolio.py`

Scope: fix every CRITICAL + HIGH + MEDIUM≥conf-8 finding listed below in current `main`.

---

## CRITICAL — 14 findings in scope

#### C-0206 · L? · pr-test-analyzer c10 ✅  · current: L53-347
- **Title**: _compute_portfolio_analytics (200+ lines, full pipeline) has zero direct test — TWR/weights/benchmark/alerts/persist all uncovered
- **Summary**: The core async function that orchestrates the entire portfolio analytics pipeline (lines 133-380, ~250 LOC) — strategy fetch, weight normalization, returns alignment, TWR/period-returns, correlation matrix, risk decomp, attribution, benchmark fetch, equity curve, sharpe/vol/drawdown, narrative, persist+alerts — has NO unit or integration test. The only file touching the router (test_portfolio_router_logic.py) only exercises _generate_alerts. With analytics-service/ enforcing --cov-fail-under=80, this single file alone almost certainly drops coverage below the gate. Missing tests it could regress silently: insert-of-status='computing' row, second-strategy weight renormalization correctness, fillna(0) behavior on sparse strategies, benchmark-stale fallback to None (line 295), compute_twr returning None when len<2, _safe_float(vol/mean) division-by-zero guard. Any of these failing in prod would persist computation_status='failed' silently.
- **Evidence**: routers/portfolio.py:133-380 (entire computation pipeline). tests/test_portfolio_router_logic.py:1432-1559 — only TestGenerateAlerts class, the rest of the router untested.
- **Re-verified 2026-05-15**: STILL_LIVE — _compute_portfolio_analytics pipeline still has NO direct unit/integration test. Only TestGenerateAlerts (110 lines) and TestGenerateRebalanceDriftAlert exercise downstream helpers.
  - Anchor: tests/test_portfolio_router_logic.py: only 2 classes (TestGenerateAlerts, TestGenerateRebalanceDriftAlert). No test imports _compute_portfolio_analytics or hits the /portfolio-analytics endpoint.
- **Source**: `batch-S1b.pr-test-analyzer.jsonl` (batches: S1b)

#### C-0207 · L? · pr-test-analyzer c10 ✅  · current: L936-1092
- **Title**: verify_strategy endpoint (POST /api/verify-strategy, 150+ lines) has zero tests — encryption, exchange flow, matching algorithm all uncovered
- **Summary**: The landing-page verification flow (lines 549-703) handles untrusted user input (api_key, api_secret, passphrase) and writes encrypted credentials to verification_requests. Untested branches: (a) get_kek() RuntimeError → 503; (b) create_exchange ValueError → 400; (c) validate_key_permissions raising → 500; (d) validation['error'] populated → 400; (e) <2 trades → 400 _fail_vr; (f) <2 trading days post-aggregation → 400 _fail_vr; (g) the vectorized strategy-matching block (lines 636-664) — including the >0.95 correlation threshold for matched_strategy_id; (h) uncaught exception → _fail_vr + 500. This is a SECURITY-sensitive path (handles raw API secrets) with no test coverage. A regression where the 'finally: await exchange.close()' is skipped, or the second create_exchange fails after credentials persisted, would leak resources or persist orphan rows.
- **Evidence**: routers/portfolio.py:549-703 — entire endpoint. No test file imports verify_strategy or VerifyStrategyRequest.
- **Re-verified 2026-05-15**: STILL_LIVE — verify_strategy endpoint still has no real handler tests. test_verify_strategy_no_legacy_writes.py is two AST/string-literal regression tests for the migration-107 no-INSERT contract — does not exercise encryption, exchange flow, matching, or any bra
  - Anchor: tests/test_verify_strategy_no_legacy_writes.py:53,101 are AST-only checks; no TestClient call to /api/verify-strategy.
- **Source**: `batch-S1b.pr-test-analyzer.jsonl` (batches: S1b)

#### C-0208 · L? · pr-test-analyzer c10 ✅  · current: L651-681
- **Title**: portfolio_analytics endpoint concurrency guard (semaphore + DB in-flight check) has zero tests — TOCTOU window claim is untested
- **Summary**: The endpoint (lines 425-455) has a load-bearing comment: 'Ordering matters: check inside the semaphore prevents a TOCTOU window where two concurrent requests both pass the check before either INSERT runs.' This concurrency contract has no test. Missing: (a) two concurrent calls with same portfolio_id → second returns 409, not 500; (b) non-existent portfolio_id → 404; (c) semaphore _compute_semaphore acquire/release lifecycle when _compute_portfolio_analytics raises; (d) is the semaphore 'asyncio.Semaphore(3)' actually limiting bursts (assert behavior with 4 concurrent calls). Regression where the semaphore is moved OUTSIDE the in-flight check would re-introduce TOCTOU and ship without test failure.
- **Evidence**: routers/portfolio.py:425-455 — 'Ordering matters' comment + semaphore. routers/portfolio.py:126 — '_compute_semaphore = asyncio.Semaphore(3)'. No test exercises concurrent calls, semaphore release, or 409 path.
- **Re-verified 2026-05-15**: STILL_LIVE — TOCTOU/semaphore concurrency guard still untested. No concurrent caller test, no 409 path test, no semaphore-release test exists in tests/.
  - Anchor: portfolio.py:668 'async with _compute_semaphore'; portfolio.py:46 _compute_semaphore. No test references _compute_semaphore or hits /portfolio-analytics.
- **Source**: `batch-S1b.pr-test-analyzer.jsonl` (batches: S1b)

#### C-0209 · L? · pr-test-analyzer c10 ✅  · current: L688-791
- **Title**: portfolio_optimizer endpoint (lines 462-542) has zero tests — strategy filtering, weight override, persist-suggestions all uncovered
- **Summary**: Untested endpoint logic: (a) portfolio not found → 404; (b) no strategies in portfolio → 400; (c) custom req.weights overriding base weights via dict.update (silent — tests don't validate override semantics); (d) candidate selection via .not_.in_('id', strategy_ids) — empty published list path; (e) no candidates returns empty suggestions vs. error; (f) suggestion persistence: only writes if latest analytics row exists with status='complete' — silently no-ops otherwise (no test that we don't break the 'most recent computed_at DESC' ordering). The unit test file test_portfolio_optimizer.py only tests the pure helper find_improvement_candidates, never the endpoint wiring around it.
- **Evidence**: routers/portfolio.py:462-542 (endpoint). tests/test_portfolio_optimizer.py — only 3 tests against find_improvement_candidates and generate_narrative; no router tests.
- **Re-verified 2026-05-15**: STILL_LIVE — portfolio_optimizer endpoint untested. test_portfolio_optimizer.py covers find_improvement_candidates/generate_narrative only. test_routers_audit_emission_structure.py is AST-only.
  - Anchor: tests/test_portfolio_optimizer.py:6,18,28 — three tests, all against services helpers, none hit /portfolio-optimizer.
- **Source**: `batch-S1b.pr-test-analyzer.jsonl` (batches: S1b)

#### C-0210 · L? · red-team c10 ✅  · current: L118-121, L138-144, L189, L204-206
- **Title**: CHAIN: Silent-skip + identity-cov fallback resurrects the v0.17.1 KPI-17 silent-zero pattern across the entire portfolio analytics surface
- **Summary**: Six findings stack into a guaranteed-incorrect-but-looks-correct dashboard the moment ANY upstream data drifts: (a) silent-failure-hunter #3 — strategies missing from strategy_analytics are silently `continue`d with no log; (b) silent-failure-hunter #5 — surviving strategies' weights are renormalized to 100% (the comment explicitly acknowledges the trap, the fix re-introduces it from a different angle); (c) silent-failure-hunter #4 — strategies missing equity_curve are dropped from strategy_twrs and `twrs_list = [strategy_twrs.get(sid, 0.0) ...]` (code-reviewer #4) backfills 0.0 contributions for the survivors that still appear in ordered_sids; (d) silent-failure-hunter #6 + code-reviewer #3 — when len(df) <= 5 days, a literal `np.eye(...)` identity matrix is substituted for cov, so risk_decomposition becomes pure fiction with no flag; (e) silent-failure-hunter #7 — benchmark fetch failure becomes silent `benchmark_comparison: null` indistinguishable from 'no overlap'; (f) silent-failure-hunter #15 + #16 — when ANY of the above raises (KeyError on a renamed column from _records_to_series, NaN somewhere in cov, etc.) the row is marked `computation_status='failed'` with the generic message 'Analytics computation failed. Contact support if this persists.' — the actual exception class/traceback never reaches the DB row operators inspect first. Operational chain: an upstream cron lag, RLS misconfig, or schema drift on `strategy_analytics.returns_series` (the exact pattern from project memory's v0.17.1 KPI-17 saga, PRs #95-#100) causes 4 of 5 portfolio strategies to vanish from analytics_rows. The portfolio computes from one strategy, renormalized to 100% weight; a brand-new strategy with <6 days of history produces a fake identity-covariance risk decomposition; the user sees a clean, plausible dashboard with risk_decomposition, attribution, and narrative — every number is wrong and there is no operator-visible signal. The cron silent-fail (chain #1 above) means even the recompute loop reports green. The data-migration #2 chain ALSO mutates the same row in-place via the optimizer endpoint, so historical replay can't even reconstruct the truth. This is the canonical 'analytics columns drift, errors get logged as warnings, downstream metrics silently go to zero' anti-pattern that project memory explicitly warns about — re-shipped end-to-end.
- **Evidence**: silent-failure-hunter.jsonl:3,4,5,6,7,15,16; code-reviewer.jsonl:3,4; portfolio.py:192-218,263,278-280,292-309,322-324,372-380; project memory v0.17.1 KPI-17 saga
- **Re-verified 2026-05-15**: STILL_LIVE — Silent continue on missing strategy_analytics (L120-121), renormalization-to-100% (L141-144), identity-cov fallback (L189), and twr default 0.0 (L204) all present — the KPI-17 silent-zero pattern is intact.
  - Anchor: portfolio.py:120-121 'if not row: continue'; L189 cov_matrix = ... np.eye(len(ordered_sids)); L204 strategy_twrs.get(sid, 0.0).
- **Source**: `batch-S1b.red-team.jsonl` (batches: S1b)

#### C-0211 · L? · silent-failure-hunter c10 ✅  · current: L118-121
- **Title**: Strategies missing from strategy_analytics are silently skipped — re-creates the v0.17.1 KPI-17 silent-zero pattern
- **Summary**: The loop over strategy_ids does `row = analytics_rows.get(sid); if not row: continue` with NO log line and NO failure indicator. If 4 of 5 portfolio strategies don't have a strategy_analytics row (cron lag, sync failure, RLS misconfig, FK race), the portfolio computes silently from 1 strategy. The renormalization a few lines later then re-weights to 100% across the survivor and the user sees 'real' numbers that are completely wrong. This is the exact `try/except → fills_data=[]` pattern from the v0.17.1 KPI-17 saga (PRs #95-#100): metrics look fine, root cause is upstream missing data. Required: warn-log every dropped sid, count drops, and FAIL the computation (not silently degrade) when more than ~25% of portfolio weight has no analytics data.
- **Evidence**: analytics-service/routers/portfolio.py:192-199 for sid in strategy_ids: row = analytics_rows.get(sid) if not row: continue s = _records_to_series(row.get("returns_series"), name=sid) if s is not None: strategy_returns[sid] = s
- **Re-verified 2026-05-15**: STILL_LIVE — Loop still does `row = analytics_rows.get(sid); if not row: continue` with no log line, no failure indicator.
  - Anchor: portfolio.py:118-121 'for sid in strategy_ids: row = analytics_rows.get(sid); if not row: continue'.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### C-0212 · L? · silent-failure-hunter c10 ✅  · current: L491-495
- **Title**: _generate_alerts swallows ALL exceptions — high-severity drawdown alerts can vanish silently and tests enshrine this
- **Summary**: `except Exception as exc: logger.warning(...)` on the alert insert means a transient Supabase failure causes a -25% drawdown alert (severity='high') to be lost forever. The companion test `test_supabase_insert_failure_does_not_raise` even codifies this: 'If Supabase raises during insert, _generate_alerts swallows it silently' — the test name proves the silent failure is intentional. For a security/risk alerting subsystem this is unacceptable. Required: at minimum upgrade to logger.exception (Sentry capture), retry once, and propagate the failure to the calling _compute_portfolio_analytics so the analytics row can be marked alert_delivery_failed (or the computation itself can be re-queued). Tests should assert the failure SURFACES, not that it is silenced.
- **Evidence**: analytics-service/routers/portfolio.py:414-418 if alerts: try: supabase.table("portfolio_alerts").insert(alerts).execute() except Exception as exc: logger.warning("Failed to insert portfolio alerts for %s: %s", portfolio_id, exc) analytics-service/tests/test_portfolio_router_logic.py:1553-1558 def test_supabase_insert_failure_does_not_raise(self): """If Supabase raises during insert, _generate_alerts swallows it silently."""
- **Re-verified 2026-05-15**: STILL_LIVE — _generate_alerts still wraps each insert in try/except Exception and only logs.warning. Failure of a 'high'-severity drawdown alert is silently lost. The companion test (test_supabase_insert_failure_does_not_raise) still codifies the swallow at L135.
  - Anchor: portfolio.py:480-495 'try: ... supabase.table("portfolio_alerts").insert(alert).execute() except Exception as exc: logger.warning(...)'; tests/test_portfolio_router_logic.py:135 'test_supabase_insert_failure_does_not_raise'.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### C-0213 · L? · red-team c9 ✅  · current: L67-69 (insert) + cron.py:304-309 (swallow)
- **Title**: CHAIN: Cron-induced poison row permanently bricks portfolio analytics + suppresses every future 409
- **Summary**: Three findings combine into a permanent denial-of-service-by-data-poisoning per portfolio: (1) data-migration #1 — _compute_portfolio_analytics INSERTs a `computation_status='computing'` row before doing any work and only clears it via _fail() (caught path) or final UPDATE; SIGKILL/OOM/pod eviction between INSERT and UPDATE leaves an orphaned 'computing' row forever, with no watchdog; (2) code-reviewer #1 + api-contract #2 + data-migration #3 — cron_sync calls _compute_portfolio_analytics directly, BYPASSING both the asyncio semaphore AND the in-flight DB check that the public endpoint uses; (3) silent-failure-hunter #1 — cron swallows the per-portfolio exception with logger.error (no exc_info, no Sentry capture, no metric) and returns the original strategy-sync `synced/failed` payload to the caller, so monitoring sees HTTP 200 + 'failed=0'. Attack/operational chain: an attacker (or unlucky cron tick during a deploy or autoscaler eviction) only needs to kill a single python worker mid-computation — or fire two overlapping recompute paths (cron + user-triggered) so two 'computing' rows exist for the same portfolio_id. The orphan row then permanently triggers the in-flight check at portfolio.py:443-451, returning 409 to every legitimate user request forever; cron will keep adding more 'computing' rows on every tick (no in-flight guard there) so the table grows without bound. Because the cron return payload reflects only the upstream sync counters (silent-failure-hunter #1, #26), neither the cron success rate, the `failed` counter, nor any Sentry event surfaces the brick. Operators only learn when a user opens a support ticket. Severity is critical: data-availability collapse with zero observability and no automatic recovery path.
- **Evidence**: silent-failure-hunter.jsonl:1 (cron swallows exceptions), data-migration.jsonl:1 (orphan computing row, no watchdog), data-migration.jsonl:3 + code-reviewer.jsonl:1 + api-contract.jsonl:2 (cron bypasses semaphore + in-flight check), portfolio.py:142-154,443-451; cron.py:67-72
- **Re-verified 2026-05-15**: STILL_LIVE — Chain still intact: portfolio.py:67-69 INSERTs computing row before work; cron.py:308-309 catches & swallows _compute_portfolio_analytics exceptions; no orphan-computing watchdog/reaper exists.
  - Anchor: portfolio.py:67-69 insert computing; cron.py:308 'except Exception as e: logger.error(...)'; no grep hit for orphan/computing watchdog.
- **Source**: `batch-S1b.red-team.jsonl` (batches: S1b)

#### C-0214 · L? · red-team c9 ✅  · current: L936-1092
- **Title**: CHAIN: Unauthenticated landing-page verify-strategy endpoint enables credential-spray DoS amplifier + log exfiltration
- **Summary**: Five findings stack to turn /api/verify-strategy into a credential-stuffing amplifier and PII leak pipeline. (a) security #3 — rate limit is IP-only (5/hour via slowapi.get_remote_address); X-Forwarded-For trust isn't audited so the limit can be spoofed via headers, and rotating cloud IPs trivially bypass it. (b) api-contract #5 + code-reviewer #10 — every successful call instantiates create_exchange TWICE (lines 559 + 601) using the same plaintext credentials, doubling the live exchange API load per request; the router has no Idempotency-Key, so a flaky-client retry inserts a new verification_requests row each time. (c) security #1 + security #2 + type-design #4 + type-design #6 — `email: str`, `api_key/api_secret/passphrase: str` (no SecretStr, no EmailStr, no length cap, no validator) — encrypted credentials are stored regardless of whether the email is valid. (d) security #4 — the validation `except` block does `logger.error('verify_strategy: key validation error: %s', exc)` and CCXT's auth error messages embed the raw api_key in the signature-mismatch text — the key fragment is shipped to Railway/Sentry breadcrumbs that the PII scrubber doesn't touch. (e) silent-failure-hunter #9 — bare `except Exception: raise HTTPException(400, 'Failed to initialise exchange connection')` discards the exception class entirely; operators can't distinguish 'network down' from 'CCXT signature changed' from a genuine bad-key. Attack chain: attacker rotates 5,000 IPs, scripts 25,000/hour POSTs with leaked credential pairs from any breach corpus, each victim credential triggers two live exchange handshakes (so the victim's exchange-side IP-rate-limit and account-throttle get hit at our cost) AND lands an encrypted blob plus the user's plaintext email on disk. Concurrently, every successful auth-fail logs a key fragment via the unguarded logger.error, building an internal log corpus an attacker with read access (or a leaked log shipper) can mine. Because the credentials are persisted in verification_requests (code-reviewer #14 — no enforced expires_at, no email format check) the attacker never has to ex-fil them — they sit there until manually purged. The `5/hour` limit looks tight but is decorative against any rotated-IP threat model.
- **Evidence**: security.jsonl:1,2,3,4 (rate-limit, schema, log-leak); silent-failure-hunter.jsonl:9 (opaque except), api-contract.jsonl:5 (no idempotency, dual exchange); code-reviewer.jsonl:10,14 (dual exchange, no TTL/email check); type-design.jsonl:3,4,6 (str enums + SecretStr); portfolio.py:550-606
- **Re-verified 2026-05-15**: STILL_LIVE — Verify-strategy endpoint still IP-only rate-limited (5/hour at L937), still double-instantiates create_exchange (L958, L990), still bare except (L961), still verbatim CCXT exc logged (L967). Endpoint sits behind X-Service-Key but is invoked from the 
  - Anchor: portfolio.py:937 '@limiter.limit("5/hour")'; L32 'Limiter(key_func=get_remote_address)'; L961 bare except; L967 logs raw exc.
- **Source**: `batch-S1b.red-team.jsonl` (batches: S1b)

#### C-0215 · L? · red-team c9 ✅  · current: L688-791
- **Title**: CHAIN: Service-key IDOR + unvalidated weights dict lets any service-key holder corrupt any portfolio's persisted analytics
- **Summary**: Three findings combine into a write-side data-corruption attack. (1) security #5 — POST /api/portfolio-optimizer authenticates only via the shared X-Service-Key middleware. The handler at lines 462-542 looks up portfolios by id alone (`SELECT id WHERE id = req.portfolio_id`); there is no caller→portfolio ownership join. Any holder of the service key (frontend SSR worker, a leaked key in a build log, an internal tool, or a compromised dependency reading env vars) can target any portfolio_id. (2) security #1 + api-contract #4 + type-design #5 + pr-test-analyzer #20 — `weights: Optional[dict]` with no value validation; `if req.weights: weights.update(req.weights)` silently merges arbitrary keys/values, including phantom strategy_ids that aren't part of the portfolio, negative weights, NaN, Inf, or string values that NumPy will silently coerce or propagate. (3) data-migration #2 — the handler then does `supabase.table('portfolio_analytics').update({'optimizer_suggestions': suggestions}).eq('id', latest.data[0]['id']).execute()`, which mutates the latest 'complete' analytics row in-place even though the table is documented as append-only ('NO UNIQUE — each recompute creates a new row'). Attack chain: attacker with service key crafts {portfolio_id: <victim_uuid>, weights: {<phantom_sid>: float('nan')}}; numpy matmul propagates NaN through find_improvement_candidates' score formula (silent-failure-hunter #22 confirms None-deltas collapse to 0), producing meaningless top-5 suggestions; those suggestions are PERSISTED by mutating the snapshot row (data-migration #2) so the victim's dashboard, history, and any downstream replay/audit see fabricated 'optimizer recommended XYZ at time T' even though the row's computed_at predates the attack. Audit trail is destroyed (the snapshot's timestamp is wrong by design — the row was complete BEFORE optimizer_suggestions was injected). Combined with silent-failure-hunter #14 (the same handler silently no-ops the persistence when no complete row exists, but the response still returns suggestions) callers can't tell whether they're seeing live data or a poisoned cached row.
- **Evidence**: security.jsonl:1,5; api-contract.jsonl:4; type-design.jsonl:5; data-migration.jsonl:2; silent-failure-hunter.jsonl:14,22; pr-test-analyzer.jsonl:20; portfolio.py:462-542
- **Re-verified 2026-05-15**: STILL_LIVE — portfolio_optimizer still has no ownership check (only id-eq lookup at L695-697), still calls weights.update(req.weights) with no validation at L722-723, still UPDATEs latest analytics in-place at L771-773.
  - Anchor: portfolio.py:695-697 SELECT id WHERE id=req.portfolio_id (no eq on caller user); L722-723 weights.update(req.weights); L771-773 in-place UPDATE optimizer_suggestions.
- **Source**: `batch-S1b.red-team.jsonl` (batches: S1b)

#### C-0216 · L455 · api-contract c9 ✅  · current: L679-681
- **Title**: POST /api/portfolio-analytics 200 body silently discards all computed metrics
- **Summary**: _compute_portfolio_analytics returns {analytics_id, ...update_payload} containing sharpe, twr, max_drawdown, volatility, period returns, correlation matrix, equity curve, attribution, risk decomposition, narrative, and benchmark comparison — but the HTTP handler only surfaces {status, portfolio_id, analytics_id}. The entire update_payload spread is dropped. Any caller expecting inline metrics (as verify-strategy provides) gets no data. There is no 202/polling contract to signal that metrics must be fetched separately. The 200 status misleads callers into thinking the response is complete.
- **Evidence**: portfolio.py:453 `result = await _compute_portfolio_analytics(req.portfolio_id)`; portfolio.py:367 returns {analytics_id, ...update_payload}; portfolio.py:455 response discards the spread. Contrast verify-strategy:684-691 which returns twr/sharpe/period-returns inline on 200.
- **Fix**: Either return the full sanitized metrics inline (add `**{k:v for k,v in result.items() if k != 'analytics_id'}` to the response) or change status to 202 Accepted with a Location header pointing to the polling resource (portfolio_analytics/{analytics_id}). Document which pattern is canonical across all three sibling endpoints.
- **Re-verified 2026-05-15**: STILL_LIVE — Handler discards update_payload spread. _compute_portfolio_analytics returns {analytics_id, **update_payload}, but handler returns only {status, portfolio_id, analytics_id}.
  - Anchor: portfolio.py:335 returns {analytics_id, **update_payload}; portfolio.py:679-681 returns {status, portfolio_id, analytics_id} only.
- **Source**: `batch-S1b.api-contract.jsonl` (batches: S1b)

#### C-0314 · L400 · pr-test-analyzer c10 ⏳
- **Title**: _generate_alerts Sprint 4 alert rules (regime_shift, underperformance, concentration_creep) entirely untested
- **Summary**: The three new alert rules added in this PR — regime_shift (lines 400-431), underperformance (lines 433-456), concentration_creep (lines 458-471) — each compute non-trivial thresholds (rolling correlation delta > 0.15, vol-band trailing distance with second-place gap >= 0.005, weight_pct >= 1.5x equal-weight baseline). These are ALERT GENERATION rules — false positives spam users, false negatives miss intelligence-layer signals which is the entire selling point of Sprint 4. test_portfolio_router_logic.py::TestGenerateAlerts was only updated for the mock plumbing (select-then-insert), not for the 3 new rule branches. A regression that changes the 0.15 threshold to 0.015, swaps the sign on best_recent > best_prior in the direction computation, or drops the second-place gap guard would never be caught — the dedup-mock-fix masquerades as test coverage for the new logic.
- **Evidence**: `grep -rn 'regime_shift\|concentration_creep\|alert_type.*underperformance' analytics-service/tests/` returns zero matches inside test bodies (only file comments). Production code present in analytics-service/routers/portfolio.py:400-471.
- **Fix**: Append to analytics-service/tests/test_portfolio_router_logic.py::TestGenerateAlerts: test_regime_shift_fires_when_rolling_corr_delta_exceeds_015 (build rolling_corr={'s1-s2':[{'value':0.2}]*5+[{'value':0.5}]*5}, assert insert called with alert_type='regime_shift' and direction='tightened'); test_regime_shift_skipped_below_threshold (delta=0.10, assert no insert); test_underperformance_fires_when_worst_trails_by_more_than_band (attribution with worst contribution -0.05 and second -0.04 gap >=0.005, risk_decomp with vol_band=0.01, assert alert_type='underperformance'); test_underperformance_suppressed_when_second_place_too_close (gap < 0.005, assert no insert); test_concentration_creep_fires_when_top_weight_exceeds_15x_equal (risk_decomp 3 strategies with top weight_pct=55, equal_weight=33, assert alert_type='concentration_creep'); test_concentration_creep_skipped_with_only_2_strategies (len<3, assert no insert).
- **Source**: `batch-G13.pr-test-analyzer.jsonl` (batches: G13)

#### C-0315 · L281 · silent-failure-hunter c9 ⏳
- **Title**: Bare `except Exception: pass` silently drops monthly_returns from narrative
- **Summary**: The monthly_returns build block catches ALL exceptions with no logging — `except Exception: pass # Monthly breakdown is best-effort`. Any failure (date parsing bug, missing portfolio_returns_series, KeyError, ZeroDivisionError, schema drift on `d.year`/`d.month`) silently drops the entire monthly breakdown from the persisted narrative. Operators get zero signal: no log line, no Sentry event, no metric. The MorningBriefing UI quietly omits the 'Monthly breakdown' section and users never know whether the monthly data is unavailable because it failed or because they have a new portfolio. This is exactly the debug-nightmare class the project bans.
- **Evidence**: analytics-service/routers/portfolio.py:269-282 — `try: ... monthly_returns[year_str][month_str] *= (1 + float(v)) ... analytics_payload['monthly_returns'] = monthly_returns\n        except Exception:\n            pass  # Monthly breakdown is best-effort`
- **Fix**: Narrow the catch to expected types (e.g. `except (AttributeError, ValueError, TypeError, ZeroDivisionError) as exc:`) AND emit `logger.warning('monthly_returns build failed for %s: %s', portfolio_id, exc, exc_info=True)` so failures surface in logs/Sentry. Re-raise unexpected errors so they're not hidden.
- **Source**: `batch-G13.silent-failure-hunter.jsonl` (batches: G13)

#### C-0316 · L293 · silent-failure-hunter c9 ⏳
- **Title**: Bare `except Exception: pass` silently drops optimizer suggestions from narrative
- **Summary**: The optimizer_suggestions fetch wraps a Supabase SELECT in `try: ... except Exception: pass # Optimizer sentence is best-effort`. Any Supabase failure (transient network, RLS misconfig, schema drift in the `optimizer_suggestions` JSONB column) is swallowed with no log line. The 'If you trim ... expected Sharpe moves from X to Y' recommendation simply vanishes from the narrative with no operator signal. Worse, this catches programming errors like AttributeError on `.data[0].get(...)` if the table shape ever changes — masking real bugs as 'no suggestions available'.
- **Evidence**: analytics-service/routers/portfolio.py:285-294 — `try:\n            prev_analytics = supabase.table('portfolio_analytics').select('optimizer_suggestions')...execute()\n            if prev_analytics.data and prev_analytics.data[0].get('optimizer_suggestions'):\n                analytics_payload['optimizer_suggestions'] = ...\n        except Exception:\n            pass  # Optimizer sentence is best-effort`
- **Fix**: At minimum add `logger.warning('optimizer_suggestions fetch failed for %s: %s', portfolio_id, exc, exc_info=True)`. Catch only the specific Supabase exception types (`PostgrestAPIError`, `httpx.HTTPError`) rather than bare Exception so programming bugs (IndexError, AttributeError) bubble up.
- **Source**: `batch-G13.silent-failure-hunter.jsonl` (batches: G13)

## HIGH — 30 findings in scope

#### H-0570 · L? · code-reviewer c9 ✅  · current: L53-347 under L668
- **Title**: Async endpoint blocks event loop with sync Supabase + heavy pandas under semaphore
- **Summary**: `_compute_portfolio_analytics` is `async def` and is called from inside `async with _compute_semaphore:` in the public endpoint, but every Supabase `.execute()` call (12+ per invocation) and every pandas/numpy aggregation (`df.cov`, `corrwith`, `cumprod`, `compute_rolling_correlation` over potentially 30-day windows × n*(n-1)/2 pairs) is fully synchronous. The semaphore plus the lack of any `await asyncio.to_thread(...)` means a single in-flight portfolio computation blocks the entire FastAPI worker's event loop — including health checks, other endpoints, and the Sentry middleware. This contradicts the file-level docstring claim that 'asyncio.Semaphore is process-local … limits within-process burst' (line 124-125): it can't be a burst limiter when each call holds the loop. Fix: wrap the synchronous body in `await asyncio.to_thread(_sync_compute, ...)` (or run via `loop.run_in_executor`), or convert the endpoint to a background task.
- **Evidence**: analytics-service/routers/portfolio.py:142-363 — `async def _compute_portfolio_analytics(...)` containing only sync calls (`supabase.table(...).execute()`, `df.cov().values`, `compute_rolling_correlation(dict(strategy_returns))`, etc.). Held under `async with _compute_semaphore:` at line 442.
- **Re-verified 2026-05-15**: STILL_LIVE — _compute_portfolio_analytics is still async def with only sync supabase.execute() + pandas calls. Held under async with _compute_semaphore in the public endpoint.
  - Anchor: portfolio.py:53 'async def _compute_portfolio_analytics'; only `await` is for get_benchmark_returns at L220. All supabase.execute() and df.cov/corrwith/cumprod calls are sync. L668 semaphore.
- **Source**: `batch-S1b.code-reviewer.jsonl` (batches: S1b)

#### H-0571 · L? · code-reviewer c9 ✅  · current: L1048-1050
- **Title**: verify_strategy idxmax crashes when all correlations are NaN
- **Summary**: In the strategy-matching block (lines 655-662), once `aligned` has ≥30 rows, `corrs = aligned.drop(columns=['_target']).corrwith(aligned['_target'])` then `best = corrs.idxmax()` and `if corrs[best] > 0.95`. If every column has zero variance over the aligned window (all-flat returns; rare but possible for a brand-new published strategy that has only zeros in returns_series), `corrs` is all-NaN, `idxmax()` returns `nan`, and `corrs[nan]` raises KeyError. The outer `except Exception` swallows it, leaving `matched_strategy_id = None`. The user-facing impact is small (matching simply fails), but the silent KeyError + warning log is not the intended path and Rule 6 'root-cause obsession' applies. Fix: filter `corrs = corrs.dropna()` before `idxmax`, then short-circuit if empty.
- **Evidence**: analytics-service/routers/portfolio.py:658-662 — `if len(aligned) >= 30:\n corrs = aligned.drop(columns=[\"_target\"]).corrwith(aligned[\"_target\"])\n best = corrs.idxmax()\n if corrs[best] > 0.95:`
- **Re-verified 2026-05-15**: STILL_LIVE — corrwith → idxmax → corrs[best] > 0.95 still has no NaN guard. All-flat corrs returns NaN, idxmax returns NaN, comparison evaluates False so the spec match doesn't fire but if all corrs are NaN idxmax can raise/return unexpected.
  - Anchor: portfolio.py:1048-1050 'corrs = aligned.drop(columns=["_target"]).corrwith(aligned["_target"]); best = corrs.idxmax(); if corrs[best] > 0.95'. No corrs.isna().all() guard.
- **Source**: `batch-S1b.code-reviewer.jsonl` (batches: S1b)

#### H-0572 · L? · data-migration c9 ✅  · current: L67-69
- **Title**: Orphaned `computing` row permanently blocks all future portfolio analytics after process kill / pod restart
- **Summary**: _compute_portfolio_analytics INSERTs a portfolio_analytics row with computation_status='computing' at the start of its work and relies on _fail() (a caught-exception handler) or the final UPDATE to clear it. If the Python worker is killed between INSERT and the final UPDATE — OOM kill, pod eviction, SIGKILL during a rolling deploy — the row remains permanently stuck in state 'computing'. The HTTP endpoint's in-flight guard (lines 443-451) checks for ANY 'computing' row and returns HTTP 409 for all subsequent requests, making the portfolio permanently unrecoverable without a manual DB UPDATE. _fail() is never called on process death because it is only reachable from caught exceptions inside the try block.
- **Evidence**: portfolio.py:142 — INSERT computation_status='computing' before any computation begins; portfolio.py:151-154 — _fail() defined as inner sync function, only called from explicit raise paths; portfolio.py:370-380 — outer except block calls _fail() then re-raises, but process death between line 142 and line 361 bypasses both; migration 010 line 165-168 — portfolio_analytics_service_update policy allows UPDATE but no ON CONFLICT or TTL exists to reset stuck rows.
- **Fix**: Add a watchdog (analogous to migration 117's reset_stalled_compute_jobs) that resets portfolio_analytics rows stuck in 'computing' for >N minutes. Or use a claim-token pattern (UUID per claim) so a follow-up CRON pass can detect and reclaim orphaned rows.
- **Re-verified 2026-05-15**: STILL_LIVE — Computing row INSERT still happens before work, with cleanup only via _fail() in caught-exception paths or final UPDATE. No watchdog/reaper for orphaned 'computing' rows. Migration grep shows no stuck-row sweeper.
  - Anchor: portfolio.py:67-69 INSERT computation_status='computing'; portfolio.py:76-80 _fail only on caught exception. No 'reaper'/'stale.computing' grep hit in analytics-service/.
- **Source**: `batch-S1b.data-migration.jsonl` (batches: S1b)

#### H-0573 · L? · data-migration c9 ✅  · current: L771-773
- **Title**: `portfolio_optimizer` mutates an append-only portfolio_analytics snapshot via UPDATE, violating the table's immutable-history contract
- **Summary**: Migration 010 defines portfolio_analytics as append-only: 'NO UNIQUE(portfolio_id) — each recompute creates a new row. Dashboard queries: ORDER BY computed_at DESC LIMIT 1'. The portfolio_optimizer endpoint finds the latest 'complete' row and UPDATEs its optimizer_suggestions column in-place. This violates the immutable-history contract: (1) computed_at no longer reflects when optimizer_suggestions was populated, corrupting audit trail; (2) concurrent dashboard readers caching the row see content change mid-flight; (3) replay/historical analysis sees injected optimizer data that did not exist at computation time.
- **Evidence**: portfolio.py:529-536 — supabase.table('portfolio_analytics').update({'optimizer_suggestions': suggestions}).eq('id', latest.data[0]['id']).execute(); migration 010 lines 24-25 — '-- portfolio_analytics: append-only computed snapshots' / '-- NO UNIQUE(portfolio_id) — each recompute creates a new row'.
- **Fix**: Either INSERT a new portfolio_analytics row with optimizer_suggestions populated, or store optimizer_suggestions in a dedicated portfolio_optimizer_runs table that joins back via portfolio_analytics_id. Add an INSERT/UPDATE trigger on portfolio_analytics to forbid mutating non-null optimizer_suggestions.
- **Re-verified 2026-05-15**: STILL_LIVE — portfolio_optimizer still does in-place UPDATE on latest 'complete' portfolio_analytics row to attach optimizer_suggestions, violating the append-only contract.
  - Anchor: portfolio.py:771-773 'supabase.table("portfolio_analytics").update({"optimizer_suggestions": suggestions}).eq("id", latest.data[0]["id"]).execute()'.
- **Source**: `batch-S1b.data-migration.jsonl` (batches: S1b)

#### H-0574 · L? · pr-test-analyzer c9 ✅  · current: L138-144
- **Title**: Weight renormalization on missing strategies — load-bearing comment, but the regression scenario it prevents has no test
- **Summary**: Lines 213-218 carry a critical comment: 'Without this, a missing high-weight strategy silently suppresses all returns (e.g., 80% weight strategy missing → surviving 20% still gets 0.2× multiplier).' This is a documented bug-fix with NO regression test. A test should: (a) build portfolio with 80%/20% weights, return data only for the 20% strategy, assert the surviving weight is renormalized to 1.0 not left at 0.2; (b) build with all strategies missing → graceful failure path (currently raises 400); (c) total_available_w sum exactly 0 → division-by-zero guard via `or 1.0`. A regression where line 217 reverts to `weights = {sid: w for sid, w in weights.items() if sid in available_sids}` without renormalization would silently 4-5x understate returns and pass all current tests.
- **Evidence**: routers/portfolio.py:213-218 — 'Without this, a missing high-weight strategy silently suppresses all returns'. No test exercises portfolio with missing-strategy data + verifies renormalized weight magnitude.
- **Re-verified 2026-05-15**: STILL_LIVE — Renormalization block still present with load-bearing comment. No test exercises the 80/20 missing-strategy regression scenario (grep of tests/ shows no test feeds analytics_rows missing a high-weight sid and asserts the surviving weight).
  - Anchor: portfolio.py:138-144 renormalization comment + code; no test in tests/test_portfolio_router_logic.py covers _compute_portfolio_analytics paths.
- **Source**: `batch-S1b.pr-test-analyzer.jsonl` (batches: S1b)

#### H-0575 · L? · pr-test-analyzer c9 ✅  · current: L35-41
- **Title**: _records_to_series helper untested — silent None on bad shape would propagate as missing strategy data
- **Summary**: Helper at lines 115-121 converts [{date, value}, ...] to a DatetimeIndex pd.Series, returning None on non-list/empty inputs. Used in 5 callsites across the router. No test covers: (a) None input → None; (b) [] input → None; (c) malformed records missing 'date' or 'value' keys → KeyError raised, NOT swallowed; (d) duplicate dates in records → silently keeps last; (e) un-parseable date strings → TypeError. A regression silently dropping rows would propagate as 'No returns data available' (line 210) — looks like a data problem, not code bug.
- **Evidence**: routers/portfolio.py:115-121. No `_records_to_series` references in any test file (grep tests/ confirms).
- **Re-verified 2026-05-15**: STILL_LIVE — _records_to_series helper unchanged and untested. No grep hit for '_records_to_series' in any tests/ file.
  - Anchor: portfolio.py:35-41 helper; grep tests/ for _records_to_series returns no matches.
- **Source**: `batch-S1b.pr-test-analyzer.jsonl` (batches: S1b)

#### H-0576 · L? · red-team c9 ✅  · current: L53-347 + cron.py:304-309
- **Title**: CHAIN: Async event-loop blocking + cron sequential await = single slow portfolio kills health checks for the whole worker
- **Summary**: Three findings combine to amplify a per-portfolio computation cost into a process-wide availability hit. (1) code-reviewer #5 — _compute_portfolio_analytics is `async def` but every Supabase `.execute()` (12+ calls per invocation) and every pandas/numpy call (`df.cov`, `corrwith`, `cumprod`, `compute_rolling_correlation` over ~30-day windows × n*(n-1)/2 pairs) is fully synchronous; under `async with _compute_semaphore` this blocks the FastAPI worker's event loop including health checks and Sentry middleware. (2) performance #2 — cron_sync awaits portfolio recomputes one-at-a-time in a for-loop. (3) performance #5 — there is no partial index on `portfolio_analytics.computation_status='computing'`, so the 'in-flight' lookup that runs at the start of every public computation does a heap-scan that grows with portfolio_analytics history depth (and the orphan-row chain above ensures it grows unboundedly under failure). Operational chain: 50 portfolios touched by a strategy sync × 2-3s synchronous compute each = 100-150s of event-loop-blocking time inside ONE cron tick. During those minutes the worker fails Railway's health probe → Railway restarts the pod → the in-flight INSERT was already done → we now have an orphan 'computing' row (chain #1) AND every subsequent cron tick makes it worse. An attacker can amplify this trivially: trigger 3 concurrent portfolio recomputes on portfolios with large strategy fan-out (semaphore caps at 3) and the worker is dead for the entire computation window. Performance #1 + #3 (unbounded `published strategies` SELECT + bulk JSONB fetch in optimizer/verify-strategy) provide an even cheaper amplifier — one POST /api/portfolio-optimizer call against a ~500-strategy catalog will pull a multi-MB JSONB payload into memory under the same blocking event loop. No timeout is set on _compute_portfolio_analytics so a single pathological input can monopolize one of three semaphore slots indefinitely. The cron's silent-swallow plus the absence of `portfolios_recomputed/failed` in the response means none of this surfaces in monitoring.
- **Evidence**: code-reviewer.jsonl:5; performance.jsonl:1,2,3,5; silent-failure-hunter.jsonl:1,26; data-migration.jsonl:1; portfolio.py:142-380; cron.py:67-72
- **Re-verified 2026-05-15**: STILL_LIVE — Async event-loop blocking pattern unchanged (see F-014). Cron sequentially awaits each portfolio recompute with no concurrency, so one slow portfolio blocks the loop.
  - Anchor: portfolio.py:53 async + sync internals; cron.py:304-309 sequential `for pid in portfolio_ids: await _compute_portfolio_analytics(pid)`.
- **Source**: `batch-S1b.red-team.jsonl` (batches: S1b)

#### H-0577 · L? · silent-failure-hunter c9 ✅  · current: L127-129, L204-205
- **Title**: Equity curve silently skipped when missing — TWR per strategy and portfolio_twr can come from a partial subset without warning
- **Summary**: `if eq is not None: strategy_equity[sid] = eq` — if a strategy has returns_series but no equity_curve column populated, it is omitted from `strategy_twrs`. Later, `port_twr_for_attr = portfolio_twr or 0.0` and the attribution math then treats the missing TWR as 0.0. No log, no DB column to inspect afterwards. A genuine -50% strategy will silently appear as 0% contribution. Required: warn-log each missing equity_curve and either compute equity from returns_series or mark the analytics row 'partial'.
- **Evidence**: analytics-service/routers/portfolio.py:201-203, 278-280 eq = _records_to_series(row.get("equity_curve"), name=sid) if eq is not None: strategy_equity[sid] = eq ... twrs_list = [strategy_twrs.get(sid, 0.0) for sid in ordered_sids] port_twr_for_attr = portfolio_twr or 0.0
- **Re-verified 2026-05-15**: STILL_LIVE — Equity curve still silently skipped when missing (L127-129 'if eq is not None: strategy_equity[sid] = eq'), and twrs_list defaults to 0.0 for missing TWR.
  - Anchor: portfolio.py:127-129 conditional add; portfolio.py:204-205 'twrs_list = [strategy_twrs.get(sid, 0.0)...]; port_twr_for_attr = portfolio_twr or 0.0'.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### H-0578 · L? · silent-failure-hunter c9 ✅  · current: L138-144
- **Title**: Weight renormalization silently masks missing strategies — comment acknowledges the trap, fix doesn't address it
- **Summary**: The comment correctly identifies that '80% weight strategy missing → surviving 20% still gets 0.2× multiplier' is a silent suppressor — but the chosen fix (renormalize to surviving strategies) ALSO silently hides the same fact. A user who allocates 80% to Strategy A and 20% to Strategy B will see analytics that look correct after the fix, while in reality only Strategy B drove the numbers. There is no log, no DB flag, no narrative warning. Required: emit a warning + persist `partial_data: true` (or list of dropped strategy_ids) on portfolio_analytics so the dashboard can show 'analytics derived from N of M strategies'.
- **Evidence**: analytics-service/routers/portfolio.py:212-218 # Renormalize weights to only the strategies that have data. # Without this, a missing high-weight strategy silently suppresses all returns # (e.g., 80% weight strategy missing → surviving 20% still gets 0.2× multiplier). available_sids = set(strategy_returns.keys()) weights = {sid: w for sid, w in weights.items() if sid in available_sids} total_available_w = sum(weights.values()) or 1.0 weights = {sid: w / total_available_w for sid, w in weights.items()}
- **Re-verified 2026-05-15**: STILL_LIVE — Renormalization block still silently masks missing strategies. No log, no DB column flag distinguishing 'all expected strategies had data' from 'renormalized to subset'.
  - Anchor: portfolio.py:138-144 renormalize-to-available-sids; no log line per missing sid, no missing_strategy_count field on the persisted analytics payload.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### H-0579 · L? · silent-failure-hunter c9 ✅  · current: L189
- **Title**: Identity covariance matrix substituted for short history — risk decomposition becomes meaningless without warning
- **Summary**: `cov_matrix = df.cov().values if len(df) > 5 else np.eye(len(ordered_sids))` silently substitutes a meaningless identity matrix when fewer than 6 days of overlap exist. The downstream `compute_risk_decomposition` then produces numbers that look real but represent the assumption that every strategy has unit variance and zero cross-correlation. Users see a 'risk_decomposition' field on the dashboard with no indication the math is fake. Required: either return None / mark 'insufficient_history' or log a warning AND persist a flag — never silently fall back to a fake covariance.
- **Evidence**: analytics-service/routers/portfolio.py:263 cov_matrix = df.cov().values if len(df) > 5 else np.eye(len(ordered_sids))
- **Re-verified 2026-05-15**: STILL_LIVE — Identity covariance substituted silently for <6 days history — see F-012.
  - Anchor: portfolio.py:189 'cov_matrix = df.cov().values if len(df) > 5 else np.eye(len(ordered_sids))'; no warn log path.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### H-0580 · L? · silent-failure-hunter c9 ✅  · current: L218-235
- **Title**: Benchmark fetch failure becomes silent `null` benchmark_comparison — UI cannot distinguish failure from 'no benchmark'
- **Summary**: `except Exception as exc: logger.warning('Benchmark fetch failed ...')` then proceeds with `benchmark_comparison = None` (the sentinel set before the try). The persisted analytics row will show benchmark_comparison: null, indistinguishable from a stale benchmark or insufficient overlap. Worse: the catch is broad — it will swallow KeyError from a malformed benchmark series, NameError from an import bug, asyncio.CancelledError, etc. Required: narrow the except, persist a `benchmark_error` field, and bubble unexpected types via Sentry (`logger.exception`).
- **Evidence**: analytics-service/routers/portfolio.py:292-309 benchmark_comparison = None try: benchmark_rets, benchmark_stale = await get_benchmark_returns("BTC") if benchmark_rets is not None and not benchmark_stale: ... except Exception as exc: logger.warning("Benchmark fetch failed for portfolio %s: %s", portfolio_id, exc)
- **Re-verified 2026-05-15**: STILL_LIVE — Benchmark fetch failure block still has bare 'except Exception' catching everything and leaving benchmark_comparison = None with a single logger.warning. No structured error field persisted.
  - Anchor: portfolio.py:218 init None; 234-235 except Exception + logger.warning.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### H-0581 · L? · silent-failure-hunter c9 ✅  · current: L957-962
- **Title**: verify_strategy: bare `except Exception` on create_exchange hides the real failure type from logs and Sentry
- **Summary**: `try: exchange = create_exchange(...) except ValueError: raise HTTPException(400, str(exc)) except Exception: raise HTTPException(400, 'Failed to initialise exchange connection')` — the second except swallows the entire exception (no `as exc`, no log call). Network errors, ImportError on a missing ccxt extension, TypeError from a bad signature change, all become an opaque 400. Operators have zero diagnostic info. Required: capture the exception with logger.exception and either include exception class in the response or store it in verification_requests.error_message.
- **Evidence**: analytics-service/routers/portfolio.py:558-563 try: exchange = create_exchange(req.exchange, req.api_key, req.api_secret, req.passphrase) except ValueError as exc: raise HTTPException(status_code=400, detail=str(exc)) except Exception: raise HTTPException(status_code=400, detail="Failed to initialise exchange connection")
- **Re-verified 2026-05-15**: STILL_LIVE — verify_strategy still has 'except Exception:' on create_exchange with no 'as exc', no log, and a generic 400 detail.
  - Anchor: portfolio.py:957-962 'try: exchange = create_exchange(...); except ValueError as exc: raise HTTPException(400, str(exc)); except Exception: raise HTTPException(400, "Failed to initialise exchange connection")'.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### H-0582 · L? · silent-failure-hunter c9 ✅  · current: L1024-1053
- **Title**: Strategy matching failure is logged as warning and silently presents `matched_strategy_id=None` as 'no match'
- **Summary**: The `try: ... published_result = supabase.table('strategies')... except Exception as exc: logger.warning('verify_strategy: strategy matching failed: %s', exc)` block leaves `matched_strategy_id = None`. The response and DB row then look identical to a successful run that found no >0.95 correlation. The user is told 'we couldn't match your strategy' when in fact we never tried (DB outage, RLS misconfig, etc.). Required: surface a distinct `matching_status` field ('matched' / 'no_match' / 'matching_unavailable') and never conflate the three.
- **Evidence**: analytics-service/routers/portfolio.py:635-664 matched_strategy_id = None try: published_result = supabase.table("strategies").select("id").eq(...).execute() ... except Exception as exc: logger.warning("verify_strategy: strategy matching failed: %s", exc)
- **Re-verified 2026-05-15**: STILL_LIVE — Strategy matching failure still logs as warning and presents matched_strategy_id=None as 'no match' — indistinguishable from a successful query that found nothing.
  - Anchor: portfolio.py:1024 init matched_strategy_id=None; L1052-1053 except Exception + logger.warning; response shape never carries 'matching_failed' flag.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### H-0583 · L? · code-reviewer c9 ✅  · current: L695-702, L778-785
- **Title**: portfolio_optimizer emits audit attribution to portfolio OWNER without verifying the JWT/caller, enabling attribution-spoof / log-pollution
- **Summary**: Unlike portfolio_bridge / portfolio_simulator which do `.eq('id', req.portfolio_id).eq('user_id', req.user_id)` for ownership, portfolio_optimizer only checks the portfolio exists and pulls `portfolio_owner_id` from the row — then audits the run AS that owner. A caller (Next-side or any service-role caller with the URL) can supply any portfolio_id and cause a `optimizer.run` audit_log row to be written attributed to the unwitting actual owner. Forensic integrity of audit_log is broken.
- **Evidence**: portfolio_result = supabase.table('portfolios').select('id, user_id').eq('id', req.portfolio_id).single().execute(); ... portfolio_owner_id = portfolio_result.data.get('user_id'); ... log_audit_event(user_id=portfolio_owner_id, action='optimizer.run', ...) — no ownership eq('user_id', req.<jwt>) check, contrast bridge/simulator routes.
- **Re-verified 2026-05-15**: STILL_LIVE — portfolio_optimizer still pulls portfolio_owner_id from the row and audits as that owner without JWT/caller cross-check; contrast portfolio_bridge at L815-817 which does .eq('user_id', req.user_id).
  - Anchor: portfolio.py:695-697 select id where id=req.portfolio_id (no user_id eq); L702 portfolio_owner_id from row; L778-785 log_audit_event(user_id=portfolio_owner_id, ...). Bridge: L815-817 includes .eq('user_id', req.user_id).
- **Source**: `batch-S9c.code-reviewer.jsonl` (batches: S9c)

#### H-0584 · L? · code-reviewer c8 ✅  · current: L189
- **Title**: Identity-matrix fallback in risk decomposition fabricates portfolio variance
- **Summary**: `cov_matrix = df.cov().values if len(df) > 5 else np.eye(len(ordered_sids))` (line 263) substitutes a unit-variance, zero-covariance identity matrix when fewer than 6 days of returns are available. That fake matrix is then passed verbatim to `compute_risk_decomposition`, which will compute marginal_risk_pct, standalone_vol, and component_var as if every strategy had σ=1 (per-day decimal — completely unrealistic). Those values get persisted to `portfolio_analytics.risk_decomposition` and shown to the user / fed into the narrative. This is a silent data-quality bug that violates Rule 12 'fail loud'. Fix: when `len(df) <= 5`, set `risk_decomp = []` (or `None`) and the narrative + UI should treat it as 'insufficient history' instead of rendering bogus numbers.
- **Evidence**: analytics-service/routers/portfolio.py:263 — `cov_matrix = df.cov().values if len(df) > 5 else np.eye(len(ordered_sids))`; the result flows into `compute_risk_decomposition(ordered_weights, cov_matrix)` on line 264 and then into the persisted `risk_decomposition` payload.
- **Re-verified 2026-05-15**: STILL_LIVE — Identity-matrix fallback unchanged: cov_matrix = df.cov().values if len(df) > 5 else np.eye(len(ordered_sids)).
  - Anchor: portfolio.py:189 'cov_matrix = df.cov().values if len(df) > 5 else np.eye(len(ordered_sids))'.
- **Source**: `batch-S1b.code-reviewer.jsonl` (batches: S1b)

#### H-0585 · L? · code-reviewer c8 ✅  · current: L204
- **Title**: Attribution silently treats missing-TWR strategies as 0% return
- **Summary**: `twrs_list = [strategy_twrs.get(sid, 0.0) for sid in ordered_sids]` (line 278) defaults strategies that lack an equity curve / TWR computation to 0.0. The strategy still has weight (it's in `ordered_sids` because it had returns) but its contribution = w*0 = 0 and `allocation_effect = (w - equal_weight) * (0 - portfolio_twr)` becomes a fabricated allocation effect attributed to a strategy whose actual return is unknown. The portfolio's narrative ('driven primarily by X') will pick this up and mislabel a missing-data strategy as the contributor. Fix: drop strategies missing TWR from `ordered_sids` for the attribution call (or surface them with `contribution=null` and skip in the narrative selection).
- **Evidence**: analytics-service/routers/portfolio.py:278 — `twrs_list = [strategy_twrs.get(sid, 0.0) for sid in ordered_sids]`. `strategy_twrs` is only populated when `strategy_equity[sid]` exists AND `compute_twr(eq, []) is not None` (lines 220-224).
- **Re-verified 2026-05-15**: STILL_LIVE — Attribution still defaults missing-TWR strategies to 0.0: twrs_list = [strategy_twrs.get(sid, 0.0) for sid in ordered_sids].
  - Anchor: portfolio.py:204 'twrs_list = [strategy_twrs.get(sid, 0.0) for sid in ordered_sids]'.
- **Source**: `batch-S1b.code-reviewer.jsonl` (batches: S1b)

#### H-0586 · L? · red-team c8 ✅  · current: L651-681, L688-791, L936-1092
- **Title**: CHAIN: portfolio-analytics 200-status-with-empty-body + no response_model + envelope drift = silent client integration failure
- **Summary**: Three contract-level findings combine into 'looks healthy from the outside, broken inside the typed client'. (1) api-contract #1 — POST /api/portfolio-analytics returns {status, portfolio_id, analytics_id} but the underlying _compute_portfolio_analytics returns the full update_payload (sharpe, twr, max_drawdown, etc.); the handler discards the spread. There is no 202/Location/polling contract, so callers expecting inline metrics get HTTP 200 with no metrics and no signal that they should fetch portfolio_analytics separately. (2) api-contract #3 — three sibling POSTs ship incompatible envelopes (`analytics_id` vs `verification_id`, optimizer returns `suggestions`, verify returns inline metrics) with no shared discriminator; a typed SDK can't share a success supertype. (3) type-design #13 — none of the three endpoints declare `response_model=`, so OpenAPI schema is empty and a future code change that drops `analytics_id` from the dict won't fail any contract test. Compound failure: a frontend that polls portfolio_analytics directly (because the API contract doesn't surface the metrics) is at the mercy of read-replica lag plus the orphan-row chain (#1 above) — it can see status='computing' from a stale orphan forever and never realize that recomputes are happening. Worse, the chain #4 silent-failure pattern means when a row IS marked 'failed', the generic message 'Analytics computation failed. Contact support if this persists' is the ONLY signal the client ever sees — there is no error_code field and no shared error envelope. Combined with cron's silent-swallow + no portfolios_recomputed counter (silent-failure-hunter #1, #26), monitoring sees green at every layer while the user-facing dashboard is permanently empty.
- **Evidence**: api-contract.jsonl:1,3,4; type-design.jsonl:13; silent-failure-hunter.jsonl:1,15,26; portfolio.py:425-455,462-542,549-703
- **Re-verified 2026-05-15**: STILL_LIVE — Three endpoints still return divergent envelopes with no response_model. portfolio-analytics returns {status, portfolio_id, analytics_id}; optimizer returns {status, portfolio_id, suggestions}; verify-strategy returns full inline metrics.
  - Anchor: portfolio.py:681 vs 787-791 vs 1072-1080 — three different response shapes. No FastAPI response_model parameter on any of the three decorators.
- **Source**: `batch-S1b.red-team.jsonl` (batches: S1b)

#### H-0587 · L? · red-team c8 ✅  · current: L1026-1051
- **Title**: CHAIN: Strategy-matching limit(100) without ORDER BY + 0.95 threshold + silent failure-mode collapse = false-positive 'we recognized your strategy' + missed-match indistinguishable from system outage
- **Summary**: Five findings combine into a high-stakes false claim shown to landing-page users. (1) code-reviewer #11 + performance #3 — the matching candidate pool is `.limit(100)` with no `.order(...)`, so once the catalog crosses 100 published strategies Postgres returns a non-deterministic 100-row slice; the user's actual strategy may be excluded from the match candidates without any log. (2) code-reviewer #6 — `corrs.idxmax()` raises if every column has zero variance over the aligned window (e.g., a brand-new published strategy with all-zero returns), and the outer `except Exception` swallows it as 'matching failed'. (3) silent-failure-hunter #11 — the broad `except Exception` collapses three distinct outcomes ('matched / no_match / matching_unavailable') into matched_strategy_id=None, so a transient DB outage during matching presents to the user as 'we couldn't match your strategy' even though we never tried. (4) pr-test-analyzer #27 + testing #6 — the >0.95 correlation threshold is the entire 'is this a known strategy?' contract; it has zero test coverage, no boundary test, no test of ties (idxmax picks the first by index ordering — non-deterministic from the user's perspective). (5) silent-failure-hunter #18 — `_parse_date` silently truncates `str(value)[:10]` and a 10-char epoch like '1735689600' becomes Timestamp('1970-01-21'), so a producer regression in returns_series date format silently aligns the wrong dates and produces a corrupted correlation that may cross the 0.95 threshold against the wrong strategy. Attack/error chain: an attacker publishes a strategy with returns_series shaped as a constant series (or with epoch-formatted dates) so its corrwith vs many candidate series spuriously hits 0.95+ for victim queries; victims see 'matched_strategy_id=<attacker_strategy_id>' attached to their verification_requests row, and the platform's downstream emails ('we recognized you as <attacker>') credibly impersonate the attacker as the strategy author. Even without the attack, the silent error-collapse means landing-page metrics ('% of users who matched') become meaningless because system outages, no-data, no-overlap, and genuine no-match all aggregate as `matched_strategy_id IS NULL`.
- **Evidence**: code-reviewer.jsonl:6,11; performance.jsonl:3; silent-failure-hunter.jsonl:11,18; pr-test-analyzer.jsonl:27; testing.jsonl:6; portfolio.py:636-664
- **Re-verified 2026-05-15**: STILL_LIVE — Strategy matching block in verify_strategy still does .limit(100) with no .order(...) at L1028, then idxmax + >0.95 threshold at L1049-1050, and the whole block is wrapped in except Exception that logs warning + matched_strategy_id stays None.
  - Anchor: portfolio.py:1026-1028 '.eq("status", "published").limit(100)' (no order); L1049-1050 idxmax + 0.95; L1052-1053 except Exception warning.
- **Source**: `batch-S1b.red-team.jsonl` (batches: S1b)

#### H-0588 · L? · red-team c8 ✅  · current: L62, L77, L767
- **Title**: CHAIN-18: portfolio.py @audit-skip dead annotations + portfolio_optimizer attribution spoof + bridge req.user_id pass-through = audit-coverage illusion
- **Summary**: Combined coverage-illusion attack. (1) Multiple `# @audit-skip:` annotations in portfolio.py (code-simplifier L7, comment-analyzer L9) — no scanner consumes the token; grep shows zero matches in scripts/ or .github/. Future maintainers see machine-readable suppression markers, believe gaps are CI-tracked. (2) portfolio_optimizer (CHAIN-3) emits as the row's portfolio_owner_id without JWT cross-check (code-reviewer L1). (3) portfolio_bridge / portfolio_simulator pass req.user_id directly to log_audit_event without JWT cross-check (silent-failure-hunter L10). (4) The pr-test-analyzer findings L1 + L2 confirm test_routers_audit_emission.py is a smoke test (hasattr only) and test_routers_audit_emission_structure.py is AST-only — neither catches runtime drop. (5) The audit-coverage.test.ts from S9b only scans the TS audit helper. Attack: a contributor adds a new endpoint that emits 'admin.delete' audit attributed to req.user_id (no JWT check); the @audit-skip annotations elsewhere set the expectation 'audit coverage is enforced'; CI passes because the AST sees a log_audit_event call; production attributes a destructive action to whichever user_id was posted. Defense in depth completely cosmetic.
- **Evidence**: portfolio.py lines 20-24, 32, 59-62 audit-skip markers; portfolio.py 41-77 portfolio_optimizer; pr-test L1 hasattr-only; pr-test L2 AST-only; code-simplifier L7 grep returns zero matches.
- **Re-verified 2026-05-15**: STILL_LIVE — @audit-skip markers still present (3 sites); no scanner under scripts/ or .github/ consumes the token (grep returns empty). Combines with F-029 (attribution spoof) for the full chain.
  - Anchor: portfolio.py:62, 77, 767 '# @audit-skip:'; grep '@audit-skip' scripts/ .github/ returns no matches.
- **Source**: `batch-S9c.red-team.jsonl` (batches: S9c)

#### H-0589 · L494-495 · security c9 ✅  · current: L722-723
- **Title**: Unvalidated user-controlled weights dict merged into weight calculation without bounds or key scoping
- **Summary**: PortfolioOptimizerRequest.weights is typed Optional[dict] with no value-type or key constraint. At line 494-495 of portfolio.py (diff hunk for /api/portfolio-optimizer), the handler calls `weights.update(req.weights)` with no validation of: (a) whether keys are strategy_ids that belong to the portfolio, (b) whether values are numeric (not strings, NaN, Inf, or negative), or (c) renormalization after the merge. An attacker can inject arbitrary strategy_ids (adding phantom entries to the weight vector used by find_improvement_candidates) or Inf/NaN values that propagate through numpy matmul and silently corrupt optimizer_suggestions stored in portfolio_analytics.
- **Evidence**: analytics-service/models/schemas.py:30 — `weights: Optional[dict] = None`; analytics-service/routers/portfolio.py (diff offset 492-495) — `if req.weights: weights.update(req.weights)` with no subsequent validation
- **Fix**: Change the Pydantic type to `Dict[str, float]` and add a @validator that (1) asserts all keys are in strategy_ids fetched from DB, (2) asserts all values are finite floats in (0, 1] or at minimum > 0, and (3) renormalizes the resulting weight vector to sum to 1.
- **Re-verified 2026-05-15**: STILL_LIVE — weights.update(req.weights) still merges user-controlled dict with no key-scoping, value-type, NaN/Inf, or renormalization validation. PortfolioOptimizerRequest.weights remains Optional[dict].
  - Anchor: portfolio.py:722-723 'if req.weights: weights.update(req.weights)'. No subsequent normalize/validation. schemas.py:30 weights: Optional[dict] = None (not re-read since the diff but referenced verbatim).
- **Source**: `batch-S1b.security.jsonl` (batches: S1b)

#### H-0590 · L510-514 · performance c9 ✅  · current: L738-750
- **Title**: portfolio-optimizer fetches ALL published strategies with no LIMIT, then bulk-fetches all their returns_series JSONB
- **Summary**: POST /api/portfolio-optimizer queries strategies WHERE is_published=True with no pagination, returning every published strategy ID. It then fetches all those strategies' returns_series JSONB column in a single .in_() query. As the catalog grows, this is an unbounded full-table scan followed by a potentially multi-MB JSONB payload. A platform with 500 published strategies each holding 500 daily data points fetches ~250k data-point objects in a single HTTP round-trip from PostgREST.
- **Evidence**: analytics-service/routers/portfolio.py lines 510-525: supabase.table('strategies').select('id').eq('is_published', True).not_.in_('id', strategy_ids).execute() — no .limit(). Then supabase.table('strategy_analytics').select('strategy_id, returns_series').in_('strategy_id', candidate_ids).execute() — also no .limit().
- **Fix**: (1) Add .limit(200) or a configurable cap to the published strategies query. (2) Fetch only candidate IDs first, then compute pairwise correlations using a streaming/batched approach. (3) Pre-compute correlation scores and store them in a dedicated table.
- **Re-verified 2026-05-15**: STILL_LIVE — portfolio-optimizer still fetches all published strategies with no LIMIT (L738-740) then bulk-fetches their returns_series JSONB with no LIMIT (L748-750).
  - Anchor: portfolio.py:738-740 .eq('status','published').not_.in_('id', strategy_ids).execute() — no .limit(); L748-750 strategy_analytics.in_('strategy_id', candidate_ids) — no .limit().
- **Source**: `batch-S1b.performance.jsonl` (batches: S1b)

#### H-0591 · L538 · api-contract c9 ✅  · current: L681, L787-791, L1072-1080
- **Title**: Response envelope drift across the three sibling POST endpoints — no shared discriminator or field set
- **Summary**: Three parallel POST endpoints in the same router ship incompatible success envelopes: /api/portfolio-analytics returns {status, portfolio_id, analytics_id}; /api/portfolio-optimizer returns {status, portfolio_id, suggestions}; /api/verify-strategy returns {status, verification_id, matched_strategy_id, twr, sharpe, return_24h, return_mtd, return_ytd}. No shared top-level discriminator (ok:true) exists. Entity-reference field names differ (analytics_id vs verification_id). Only verify-strategy returns computed metrics inline. A typed client SDK cannot declare a single success supertype; callers must maintain three separate decoders.
- **Evidence**: portfolio.py:455 (portfolio-analytics response), portfolio.py:538-542 (portfolio-optimizer response), portfolio.py:684-691 (verify-strategy response).
- **Fix**: Adopt a shared envelope: {ok:true, entity_id:<uuid>, data:{...endpoint-specific payload}} across all three endpoints. Alternatively use a discriminated union documented in models/schemas.py. At minimum ensure analytics_id, verification_id field names follow one naming convention.
- **Re-verified 2026-05-15**: STILL_LIVE — Three sibling endpoints still ship incompatible success envelopes with no shared discriminator or response_model.
  - Anchor: portfolio.py:681 analytics returns {status, portfolio_id, analytics_id}; 787-791 optimizer returns {status, portfolio_id, suggestions}; 1072-1080 verify returns {status, verification_id, matched_strategy_id, results, twr, sharpe, return_24h, ...}.
- **Source**: `batch-S1b.api-contract.jsonl` (batches: S1b)

#### H-0592 · L549 · api-contract c8 ✅  · current: L936-995
- **Title**: POST /api/verify-strategy has no idempotency surface despite causing two live exchange connections and a DB row per call
- **Summary**: VerifyStrategyRequest carries plaintext api_key + api_secret. The handler instantiates create_exchange twice per successful call (line 559 for validate_key_permissions, line 601 for fetch_all_trades) — a network retry after a 500 fires four live exchange connections using the same credentials and inserts a new verification_requests row each time (different verification_id per call). No Idempotency-Key header is accepted. The route has a 5/hour rate limit, so 5 retries from a flaky client consume the full daily budget and leave 5 orphaned processing rows in verification_requests with no cleanup.
- **Evidence**: portfolio.py:559 `exchange = create_exchange(req.exchange, req.api_key, req.api_secret, req.passphrase)` then line 571 `await exchange.close()`. portfolio.py:601 `exchange = create_exchange(...)` again — same credentials, second live connection. No Idempotency-Key header read. Rate limit at line 550 is 5/hour.
- **Fix**: (1) Accept an optional Idempotency-Key header; dedup by (email, exchange, sha256(api_key), Idempotency-Key) against recent verification_requests rows. (2) Collapse the two create_exchange calls. (3) Add `idempotent: false` to the 200 body (true on replay) so callers can detect dedup.
- **Re-verified 2026-05-15**: STILL_LIVE — verify_strategy still has no Idempotency-Key surface and still instantiates create_exchange twice (L958, L990).
  - Anchor: portfolio.py:958 create_exchange(...) first; L990 create_exchange(...) second. No grep for 'Idempotency-Key' header read. Rate limit only at L937.
- **Source**: `batch-S1b.api-contract.jsonl` (batches: S1b)

#### H-0593 · L549-551 · security c8 ✅  · current: L32, L937
- **Title**: verify-strategy rate limit is IP-only — no per-email or per-credential scoping on a live-credential acceptance endpoint
- **Summary**: The @limiter.limit('5/hour') on verify_strategy uses key_func=get_remote_address (IP-only). This endpoint accepts plaintext exchange API credentials (api_key, api_secret, passphrase), makes live outbound exchange API calls, and is accessible from the landing-page flow without session auth. An attacker rotating cloud IPs (trivial with AWS Lambda, GCP Cloud Functions, etc.) bypasses the rate limit completely. Each probe triggers a live exchange API call — multiplying the cost on both the analytics service and potentially rate-counting against victim accounts at the exchange.
- **Evidence**: analytics-service/routers/portfolio.py:550 — `@limiter.limit('5/hour')`; limiter = Limiter(key_func=get_remote_address) at line 111
- **Fix**: Add a per-email composite rate limit: use a custom key_func that combines the remote IP and the normalized email from the request body. Also confirm X-Forwarded-For is correctly trusted/stripped behind the Railway reverse proxy so the IP key cannot be spoofed via a header.
- **Re-verified 2026-05-15**: STILL_LIVE — Rate limit still uses Limiter(key_func=get_remote_address) at L32 and '5/hour' at L937. No per-email/per-credential scoping.
  - Anchor: portfolio.py:32 'limiter = Limiter(key_func=get_remote_address)'; L937 '@limiter.limit("5/hour")'.
- **Source**: `batch-S1b.security.jsonl` (batches: S1b)

#### H-0594 · L637-645 · performance c8 ✅  · current: L1026-1042
- **Title**: verify_strategy bulk-fetches returns_series JSONB for up to 100 published strategies for in-memory correlation matching
- **Summary**: To find a matching published strategy, verify_strategy fetches up to 100 published strategy IDs then immediately fetches all 100 of their returns_series JSONB columns in one query. Each returns_series is a JSON array of daily {date,value} records (potentially 500-1000+ entries). This is up to 100 × 1000 data-point objects materialized into Python memory per call.
- **Evidence**: analytics-service/routers/portfolio.py lines 637-645: published_result fetches up to 100 IDs (.limit(100)), then sa_result = supabase.table('strategy_analytics').select('strategy_id, returns_series').in_('strategy_id', published_ids).execute() fetches all with no limit.
- **Fix**: (1) Pre-compute fingerprints (e.g. weekly-sampled equity curve hash or low-dimensional embedding) per published strategy and store them in strategy_analytics. At verify time, compare only fingerprints to shortlist candidates, then fetch full series for only the top-k. (2) Limit the .in_() fetch to a smaller candidate set (top 20 by recency or AUM).
- **Re-verified 2026-05-15**: STILL_LIVE — verify_strategy still does .limit(100) at L1028 followed by in_-based bulk fetch of returns_series JSONB at L1032-1034 with no limit on payload size.
  - Anchor: portfolio.py:1026-1028 published .limit(100); L1032-1034 strategy_analytics.in_('strategy_id', published_ids).execute() (no limit).
- **Source**: `batch-S1b.performance.jsonl` (batches: S1b)

#### H-1070 · L480 · pr-test-analyzer c9 ⏳
- **Title**: Alert dedup logic (select-then-insert per type) tested only via mock plumbing, never the dedup decision
- **Summary**: The new dedup loop (portfolio.py:480-499) replaces the broken batched upsert with: for each alert, SELECT existing unacknowledged of same type, skip if found, else INSERT. The accompanying test changes in test_portfolio_router_logic.py only updated the mock to return data=[] by default — they NEVER set existing data=[{...}] to verify the skip-when-existing branch fires. A regression that drops the `if existing.data: continue` check (or inverts it to `if not existing.data: continue`) would create infinite duplicate alerts of the same type. This is the entire reason migration 042's partial unique index exists; the application-level dedup is the first line and a regression there would slam the DB constraint and surface as generic insert errors.
- **Evidence**: `grep -n 'existing.data\|skip.*existing' analytics-service/tests/test_portfolio_router_logic.py` returns zero matches. The mock plumbing at test_portfolio_router_logic.py:54-66 sets data=[] (always passes dedup check). No test ever sets the select mock to return existing data and asserts insert was NOT called.
- **Fix**: Append to analytics-service/tests/test_portfolio_router_logic.py::TestGenerateAlerts: def test_skips_insert_when_unacked_alert_of_same_type_exists(self): sb = self._make_supabase_mock(); sb.table.return_value.select.return_value.eq.return_value.eq.return_value.is_.return_value.limit.return_value.execute.return_value = MagicMock(data=[{'id': 'existing-1'}]); _generate_alerts(sb, 'p-1', max_drawdown=-0.25, avg_pairwise_corr=0.85); sb.table.return_value.insert.assert_not_called(). Also: def test_inserts_when_existing_select_returns_empty(self) which is the only path the current mock exercises.
- **Source**: `batch-G13.pr-test-analyzer.jsonl` (batches: G13)

#### H-1071 · L795 · pr-test-analyzer c9 ⏳
- **Title**: portfolio_bridge router endpoint has no integration test for ownership / no-candidate / no-returns gates
- **Summary**: The Python /api/portfolio-bridge endpoint (lines 798-893) is reachable from the Next.js bridge route and is the second layer of defense-in-depth ownership checks (.eq('user_id', req.user_id)). It also gates on (1) underperformer is in the portfolio, (2) returns data exists, (3) at least one published candidate exists. test_bridge_scoring.py covers the SCORING function only (find_replacement_candidates) — it does NOT exercise the router which is where the security-sensitive ownership/membership checks live. Only an AST-based audit-emission structural test (test_routers_audit_emission_structure.py) inspects this endpoint. A regression that drops .eq('user_id', req.user_id) on line 814 silently turns the service into a service-role-bypass tool that any authenticated caller can use to score any portfolio.
- **Evidence**: `grep -rn '/portfolio-bridge\|portfolio_bridge\|underperformer' analytics-service/tests/ --include='*.py'` returns only AST structural emission tests (test_routers_audit_emission_structure.py:114-144) and the unit-level bridge_scoring tests. No FastAPI TestClient invocation against the endpoint. Production code in analytics-service/routers/portfolio.py:795-905.
- **Fix**: Add analytics-service/tests/test_portfolio_bridge_router.py mirroring the pattern used by other router integration tests. class TestPortfolioBridgeEndpoint with FastAPI TestClient and Supabase mock: test_404_when_portfolio_user_id_mismatch (mock .eq('id').eq('user_id').single() returns data=None, assert 404); test_400_when_underperformer_not_in_portfolio (portfolio_strategies returns no row matching underperformer_strategy_id, assert 400); test_400_when_no_returns_data (strategy_analytics.in_ returns empty, assert 400); test_returns_empty_candidates_when_no_published_candidates (strategies.eq('status','published') returns empty, assert 200 with candidates=[]); test_hydrates_strategy_name_from_strategies_table (assert each candidate has strategy_name from candidate_names map); test_excludes_portfolio_members_from_candidates (assert .not_.in_('id', strategy_ids) invoked).
- **Source**: `batch-G13.pr-test-analyzer.jsonl` (batches: G13)

#### H-1072 · L858 · performance c8 ⏳
- **Title**: Bridge endpoint pulls unbounded published strategies + returns_series JSONB
- **Summary**: Two queries fetch all published strategies (no LIMIT, no pagination) and then `strategy_analytics.in_(candidate_ids)` pulls the full `returns_series` JSONB for every one. Each `returns_series` is hundreds of date/value records. As the platform grows (10s -> 100s -> 1000s of published strategies) this becomes a multi-megabyte payload per Bridge call. Rate limit is 10/hour/user but Fluid Compute charges Active CPU + memory: parsing/holding all returns_series in pandas inflates request peak memory linearly with strategy count.
- **Evidence**: analytics-service/routers/portfolio.py:857-870 — `all_published = supabase.table("strategies").select("id, name").eq("status","published").not_.in_("id", strategy_ids).execute()` then `sa_cand_result = supabase.table("strategy_analytics").select("strategy_id, returns_series").in_("strategy_id", candidate_ids).execute()` — no `.limit()`, no chunking.
- **Fix**: Cap candidates with `.limit(50)` after ordering by a quality proxy (sharpe DESC), or split into a two-pass: first fetch only `id, name` + a cheap pre-filter column (e.g., sharpe), keep top-N by sharpe, then fetch returns_series only for the survivors.
- **Source**: `batch-G13.performance.jsonl` (batches: G13)

#### H-1073 · L479 · performance c8 ⏳
- **Title**: O(N) select-then-insert loop in _generate_alerts replaces single batch insert
- **Summary**: `_generate_alerts` runs once per portfolio recompute (cron-driven across every portfolio). For each of up to 5 alert types (drawdown, correlation_spike, regime_shift, underperformance, concentration_creep) it does SELECT then INSERT — 2 PostgREST round trips per type, up to 10 round trips per portfolio compute. PR #55 explicitly traded the prior single batch insert for this pattern. With 100 portfolios on the cron, that is up to 1000 PostgREST round trips per cron sweep just for alert generation.
- **Evidence**: analytics-service/routers/portfolio.py:479-495 — `for alert in alerts: existing = supabase.table("portfolio_alerts").select("id").eq(...).is_("acknowledged_at","null").limit(1).execute(); if existing.data: continue; supabase.table("portfolio_alerts").insert(alert).execute()`
- **Fix**: The partial unique index `portfolio_alerts_dedup_unacked` (migration 042) already gives DB-level dedup. Use a single batch INSERT ... ON CONFLICT DO NOTHING via PostgREST's `prefer: resolution=ignore-duplicates` header (supported on supabase-py's `upsert(..., ignore_duplicates=True, on_conflict="portfolio_id,alert_type")` against the unique index). One round trip instead of 10.
- **Source**: `batch-G13.performance.jsonl` (batches: G13)

#### H-1074 · L413 · red-team c8 ⏳
- **Title**: regime_shift alert crashes _generate_alerts when rolling_corr contains None entries from _safe_float(NaN)
- **Summary**: compute_rolling_correlation (portfolio_risk.py:39) writes `_safe_float(v)` into each rolling-corr entry's `value` field. `_safe_float` returns None when v is NaN — which happens routinely at the leading edge of a rolling window (insufficient data) or when two strategies have non-overlapping dates within the window. The Sprint 4 regime_shift branch then does `recent_vals = [p['value'] for p in series[-window:]]` (line 411) followed by `recent_avg = sum(recent_vals) / len(recent_vals)` (line 413). When any `p['value']` is None, `sum([None, 0.4, 0.5, ...])` raises `TypeError: unsupported operand type(s) for +: 'int' and 'NoneType'`. The exception propagates up through _generate_alerts → _compute_portfolio_analytics's outer try/except → portfolio analytics computation fails with status 'failed' and any sibling alert types (drawdown, correlation_spike, underperformance, concentration_creep) that would have fired in the same _generate_alerts call are silently lost. This is a per-portfolio recompute cascading failure that ALL specialists missed: silent-failure-hunter caught the monthly_returns swallow but not this rolling_corr None-crash, performance focused on O(N) inserts, and pr-test-analyzer flagged missing tests for the rule but did not pin the NaN/None input case.
- **Evidence**: analytics-service/routers/portfolio.py:411-414 `recent_vals = [p['value'] if isinstance(p, dict) else p for p in series[-window:]]\n            prior_vals = [p['value'] if isinstance(p, dict) else p for p in series[-window * 2:-window]]\n            recent_avg = sum(recent_vals) / len(recent_vals)\n            prior_avg = sum(prior_vals) / len(prior_vals)`. Combined with analytics-service/services/portfolio_risk.py:39 `result[key] = [{'date': d.isoformat(), 'value': _safe_float(v)} for d, v in rolling.items()]` where _safe_float returns None on NaN (services/metrics.py:72-80). Reproduces deterministically with any new strategy whose rolling-corr window straddles its first 30 days.
- **Fix**: Filter out None values before averaging: `recent_clean = [x for x in recent_vals if x is not None]; if not recent_clean: continue; recent_avg = sum(recent_clean) / len(recent_clean)`. Apply the same filter to prior_vals. Add a regression test that feeds rolling_corr with a leading-None series and asserts _generate_alerts does not raise.
- **Source**: `batch-G13.red-team.jsonl` (batches: G13) · kind=new

## MEDIUM — 17 findings in scope

#### M-0612 · L? · silent-failure-hunter c9 ✅  · current: L969-973
- **Title**: verify_strategy: `except Exception: pass` on exchange.close() is a textbook silent failure
- **Summary**: In the `finally` cleanup, `try: await exchange.close() except Exception: pass` swallows everything with no logging at all. A leaked TCP connection, an SSL shutdown error, a cancelled task on shutdown — all invisible. While close() failures shouldn't break the response, they MUST be observable. Required: `except Exception as exc: logger.warning('verify_strategy: exchange.close() failed: %s', exc, exc_info=True)`. Empty catch blocks are forbidden by the project's error-handling policy.
- **Evidence**: analytics-service/routers/portfolio.py:570-574 finally: try: await exchange.close() except Exception: pass
- **Re-verified 2026-05-15**: STILL_LIVE — verify_strategy still has 'try: await exchange.close() except Exception: pass' with no log call.
  - Anchor: portfolio.py:969-973 finally block 'try: await exchange.close(); except Exception: pass'.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### M-0613 · L? · comment-analyzer c9 ✅  · current: L62, L77, L767
- **Title**: `@audit-skip` directive comments in routers/portfolio.py imply enforcement, but no scanner consumes them
- **Summary**: Multiple `# @audit-skip: …` comments in routers/portfolio.py read like machine-parseable suppression markers used by a coverage gate (compare to ESLint `eslint-disable-next-line` or type-ignore directives). A grep across scripts/, .github/, and src/ shows no tool that reads `@audit-skip` — they are documentation-only and may give future contributors a false sense that the audit-emission coverage is being CI-enforced by these annotations.
- **Evidence**: grep -rn '@audit-skip' returns hits only in source files (8+ inside routers/portfolio.py, src/app/api/preferences/route.ts, src/app/api/trades/upload/route.ts, etc.). No scanner under scripts/ or .github/workflows/ matches the token. The accompanying gdpr-coverage script enforces a different invariant entirely (USER_EXPORT_TABLES vs migrations).
- **Re-verified 2026-05-15**: STILL_LIVE — Same evidence as F-046: directive comments with no scanner. comment-analyzer's duplicate of code-simplifier finding.
  - Anchor: portfolio.py:62, 77, 767 markers; no consumer in scripts/ or .github/workflows/.
- **Source**: `batch-S9c.comment-analyzer.jsonl` (batches: S9c)

#### M-0614 · L? · code-reviewer c8 ✅  · current: L39-40
- **Title**: _records_to_series KeyError on malformed records
- **Summary**: `dates = [r['date'] for r in raw]` and `vals = [r['value'] for r in raw]` (lines 119-120) raise KeyError if any record is missing the `date`/`value` keys (e.g., legacy `strategy_analytics.returns_series` rows shaped as `{ts, val}` or `{date, return}`). The KeyError bubbles to the outer `try/except Exception` in `_compute_portfolio_analytics`, which writes 'Analytics computation failed. Contact support if this persists.' — losing the actual root cause. Use `r.get('date')` / `r.get('value')` and skip records missing either key, or fail with a specific error like 'returns_series record schema mismatch'.
- **Evidence**: analytics-service/routers/portfolio.py:115-121 — `def _records_to_series(raw: list | None, name: str = '') -> pd.Series | None:\n if not isinstance(raw, list) or not raw:\n return None\n dates = [r['date'] for r in raw]\n vals = [r['value'] for r in raw]`
- **Re-verified 2026-05-15**: STILL_LIVE — _records_to_series still raises KeyError on malformed records: dates = [r['date'] for r in raw]; vals = [r['value'] for r in raw]. No try/except in helper or per-strategy guard at callers.
  - Anchor: portfolio.py:35-41 helper body unchanged. Callers at L123, L127, L731, L753, L850, L873, L1040 do not wrap in per-strategy try/except.
- **Source**: `batch-S1b.code-reviewer.jsonl` (batches: S1b)

#### M-0615 · L? · silent-failure-hunter c8 ✅  · current: L248-250
- **Title**: Sharpe / volatility silently None when len<2 or vol==0 — dashboard shows blank with no explanation
- **Summary**: `vol = ... if len(portfolio_returns_series) > 1 else None` and `sharpe = _safe_float(mean_ret/vol) if vol and vol != 0 and mean_ret is not None else None` silently substitute None for three different reasons (too few days, zero vol, NaN mean) without distinguishing them in the persisted row. A 'flat' strategy with vol==0 looks identical to 'computation broken'. Project memory's bug #4 (`positions.duration_days INTEGER` truncating to 0) is exactly this class of silent zero/None. Required: log + persist a `metric_status` per metric or dedicated reason codes.
- **Evidence**: analytics-service/routers/portfolio.py:322-324 vol = portfolio_returns_series.std() * np.sqrt(252) if len(portfolio_returns_series) > 1 else None mean_ret = portfolio_returns_series.mean() * 252 if len(portfolio_returns_series) > 1 else None sharpe = _safe_float(mean_ret / vol) if vol and vol != 0 and mean_ret is not None else None
- **Re-verified 2026-05-15**: STILL_LIVE — Sharpe/vol still collapse three distinct reasons (len<2, vol==0, mean NaN) into None with no distinguisher.
  - Anchor: portfolio.py:248-250 vol/mean_ret guarded by len>1; sharpe further guarded by 'vol and vol != 0 and mean_ret is not None'.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### M-0616 · L? · silent-failure-hunter c8 ✅  · current: L245
- **Title**: `total_aum = sum(...) or None` collapses three distinct states into one — '$0 AUM', 'all missing', and 'no strategies' all become null
- **Summary**: `total_aum = sum(strategy_aum.get(sid, 0) for sid in strategy_ids) or None` uses `0` as a per-strategy default for missing AUM AND collapses a sum of 0 to None. A portfolio where every strategy has total_aum NULL in DB is indistinguishable from a portfolio where all strategies are flat at $0 — both write None. AUM is a load-bearing display number. Required: track `aum_known_count` separately or only sum when ALL strategies have non-null AUM.
- **Evidence**: analytics-service/routers/portfolio.py:319 total_aum = sum(strategy_aum.get(sid, 0) for sid in strategy_ids) or None
- **Re-verified 2026-05-15**: STILL_LIVE — total_aum still uses 'sum(strategy_aum.get(sid, 0) for sid in strategy_ids) or None' — collapses three distinct states (zero, all missing, no strategies) into one.
  - Anchor: portfolio.py:245 'total_aum = sum(strategy_aum.get(sid, 0) for sid in strategy_ids) or None'.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### M-0617 · L? · silent-failure-hunter c8 ✅  · current: L762-773
- **Title**: portfolio_optimizer endpoint silently no-ops the optimizer_suggestions write when no completed analytics row exists
- **Summary**: `if latest.data: supabase.table('portfolio_analytics').update({'optimizer_suggestions': suggestions})...` — when there is no `complete` analytics row yet, the suggestions are returned in the HTTP response but NEVER persisted, with no log. A user who closes the page loses the suggestions; a future dashboard load shows nothing. The response also doesn't distinguish 'persisted' from 'response-only'. Required: either insert a fresh portfolio_analytics row (matching the 'append-only computed snapshots' contract) or warn-log + return a status flag.
- **Evidence**: analytics-service/routers/portfolio.py:529-536 latest = supabase.table("portfolio_analytics").select("id").eq( "portfolio_id", req.portfolio_id ).eq("computation_status", "complete").order("computed_at", desc=True).limit(1).execute() if latest.data: supabase.table("portfolio_analytics").update( {"optimizer_suggestions": suggestions} ).eq("id", latest.data[0]["id"]).execute()
- **Re-verified 2026-05-15**: STILL_LIVE — portfolio_optimizer still no-ops the optimizer_suggestions write when there is no completed analytics row. No log line, no field in response distinguishing persisted from response-only.
  - Anchor: portfolio.py:762-773 'latest = ...computation_status=complete...limit(1); if latest.data: supabase.table.update({optimizer_suggestions: suggestions})...'. No else branch.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### M-0618 · L? · silent-failure-hunter c8 ✅  · current: L339-347
- **Title**: Generic _fail message overwrites the real exception in DB — operators get 'Contact support' instead of root cause
- **Summary**: On any unhandled exception, `_fail('Analytics computation failed. Contact support if this persists.')` is written to portfolio_analytics.computation_error. The actual exception (KeyError on a renamed column, NaN in correlation, schema drift like the v0.17.1 KPI-17 cases) is only in the Sentry log — it never reaches the DB row that operators inspect first. Required: persist the exception class + first line of the traceback (truncated) to computation_error so the same lookup that surfaces the failed row also surfaces the cause.
- **Evidence**: analytics-service/routers/portfolio.py:372-380 except Exception as exc: logger.error( "Portfolio analytics computation failed for %s: %s", portfolio_id, str(exc), exc_info=True, ) _fail("Analytics computation failed. Contact support if this persists.") raise HTTPException(status_code=500, detail="Portfolio analytics computation failed")
- **Re-verified 2026-05-15**: STILL_LIVE — Generic _fail message 'Analytics computation failed. Contact support if this persists.' still overwrites the real exception in DB. The actual exception only lands in Sentry/logs.
  - Anchor: portfolio.py:339-347 outer except Exception logs.error then _fail('Analytics computation failed. Contact support if this persists.').
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### M-0619 · L? · silent-failure-hunter c8 ✅  · current: L35-41
- **Title**: _records_to_series returns None on empty input but raises KeyError on malformed records — caller treats both as 'no data'
- **Summary**: `if not isinstance(raw, list) or not raw: return None` covers empty/missing, but a record like {'date': '...', 'val': 0.01} (typo / column rename) raises KeyError that propagates out of the for-loop comprehension. In the calling code there is no per-strategy try/except, so one malformed strategy_analytics row fails the whole portfolio computation with a generic 'Analytics computation failed'. Conversely a list of records all with date=None silently produces a Series with NaT index that breaks downstream alignment. Required: explicit validation that logs and skips malformed records (with their sid) instead of either crashing the whole compute or silently corrupting it.
- **Evidence**: analytics-service/routers/portfolio.py:115-121 def _records_to_series(raw: list | None, name: str = "") -> pd.Series | None: if not isinstance(raw, list) or not raw: return None dates = [r["date"] for r in raw] vals = [r["value"] for r in raw] return pd.Series(vals, index=pd.DatetimeIndex(dates), name=name)
- **Re-verified 2026-05-15**: STILL_LIVE — Same helper as F-019/F-037: returns None on empty input but raises KeyError on malformed records; outer caller has no per-strategy guard.
  - Anchor: portfolio.py:35-41 helper body; callers do not wrap in try/except.
- **Source**: `batch-S1b.silent-failure-hunter.jsonl` (batches: S1b)

#### M-0620 · L? · type-design-analyzer c8 ✅  · current: L68, L79, L289, L300, L384, L392, L671, L764
- **Title**: Computation status / alert type / severity strings duplicated across DB CHECK + Python with no shared enum
- **Summary**: `computation_status` literal `'computing'|'complete'|'failed'` is hard-coded across portfolio.py at lines 143, 153, 341, 444, 531; the SQL CHECK constraint declares the canonical set in migration 010 line 1595 (`'pending','computing','complete','failed'`). Same for alert_type (`'drawdown'`, `'correlation_spike'` at portfolio.py:398/406; SQL line 1621 lists more values) and severity (`'high'|'medium'|'low'` at portfolio.py:399/407; SQL line 1622). Drift between these layers is a silent-failure hazard — a typo `'completed'` would CHECK-fail at INSERT but the value is computed at the very end of an analytics run. Define a single `class ComputationStatus(str, Enum)` / `AlertType` / `AlertSeverity` and use `.value` everywhere so the type system flags typos at edit time.
- **Evidence**: analytics-service/routers/portfolio.py:143,153,341,398-407,444,531; supabase/migrations/010_portfolio_intelligence.sql:1595,1621-1622,1653,1672
- **Re-verified 2026-05-15**: STILL_LIVE — computation_status / alert_type / severity literals still hard-coded across the file with no shared Python enum. No imports of an enum module for these tokens.
  - Anchor: portfolio.py:68 'computing'; L79 'failed'; L289 'complete'; L300 'complete'; L384 'drawdown'; L392 'correlation_spike'; L424 'regime_shift'; L448 'underperformance'; L465 'concentration_creep'; L596 'rebalance_drift'; L385 severity 'high'/'medium' li
- **Source**: `batch-S1b.type-design-analyzer.jsonl` (batches: S1b)

#### M-0621 · L? · type-design-analyzer c8 ✅  · current: L240, L600, L1012
- **Title**: Datetime values in API/DB-bound dicts mixed `datetime.now(timezone.utc).isoformat()` with `pd.Timestamp.isoformat()` — no centralized coercion
- **Summary**: Timestamps cross the boundary as raw ISO strings (`datetime.now(timezone.utc).isoformat()` at portfolio.py:681, `d.isoformat()` for `pd.Timestamp` at 314 / 624) but consumers/storage handle them as TIMESTAMPTZ. There is no boundary type asserting tz-awareness. A naive `pd.Timestamp` (no tz) would `.isoformat()` to a tz-naive string; Postgres TIMESTAMPTZ then assumes UTC silently, which can shift wall-clock display by hours. Use a Pydantic `AwareDatetime` (Pydantic v2) on response models, or a single helper `to_utc_iso(value)` that asserts tz-awareness. As-is the invariant ('all dates leave this service in UTC') lives nowhere.
- **Evidence**: analytics-service/routers/portfolio.py:314, 624, 681
- **Re-verified 2026-05-15**: STILL_LIVE — Datetime mixing still present: datetime.now(timezone.utc).isoformat() at L532/586/600 mixes with pd.Timestamp .isoformat() at L240/1012. No centralized tz-aware coercion.
  - Anchor: portfolio.py:240 'd.isoformat()' for pd.Timestamp; L1012 'd.isoformat()' similarly; L532/586 datetime.now(timezone.utc).
- **Source**: `batch-S1b.type-design-analyzer.jsonl` (batches: S1b)

#### M-0622 · L? · code-simplifier c8 ✅  · current: L62, L77, L767
- **Title**: @audit-skip annotations in portfolio.py have no consuming tooling
- **Summary**: Three `# @audit-skip:` comment blocks were added to portfolio.py (lines 62-66, 77, 767-770) with the intent of marking sites that don't need audit emission. Grep across the repo shows no script, lint rule, or test consumes the `@audit-skip` token (`grep -rn 'audit-skip' scripts/ .github/` returns empty). These are dead annotations — they look like a static-analysis hint but no analyzer reads them. Either remove them or wire a lint/CI script that asserts every supabase mutation site is either a `log_audit_event` call OR carries a `@audit-skip` comment.
- **Evidence**: analytics-service/routers/portfolio.py lines 20-24, 32, 59-62. grep `audit-skip` against scripts/ and .github/ returns no results. No referenced tooling reads the annotation.
- **Re-verified 2026-05-15**: STILL_LIVE — @audit-skip annotations still present at three sites, no consuming tooling. Same as F-030/F-047.
  - Anchor: portfolio.py:62, 77, 767 '# @audit-skip:'; grep over scripts/ + .github/ returns empty.
- **Source**: `batch-S9c.code-simplifier.jsonl` (batches: S9c)

#### M-0623 · L? · silent-failure-hunter c8 ✅  · current: L702, L778-785
- **Title**: portfolio_optimizer audit silently drops when portfolio.user_id is NULL — no log line, no metric
- **Summary**: `portfolio_owner_id = portfolio_result.data.get('user_id')` — if the row has NULL user_id (orphan portfolio left over from a partially-anonymized profile, or a future schema change), the subsequent `if portfolio_owner_id:` evaluates False and the audit emission is silently skipped. Compute continues, response is 200, no log line announces that this run was unaudited. The bridge handler at least logs candidate_count=0; the optimizer handler offers no equivalent forensic anchor when audit drops.
- **Evidence**: portfolio.py lines 50, 70-77: `portfolio_owner_id = portfolio_result.data.get('user_id')` ... `if portfolio_owner_id: log_audit_event(...)`. No else-branch. The optimizer NULL-attribution case becomes a forensic blind spot — exactly the kind of attribution gap audit_log was built to close.
- **Re-verified 2026-05-15**: STILL_LIVE — portfolio_optimizer audit silently drops when portfolio_owner_id is NULL: 'if portfolio_owner_id: log_audit_event(...)' with no else / no log of skip.
  - Anchor: portfolio.py:702 portfolio_owner_id = portfolio_result.data.get('user_id'); L778 'if portfolio_owner_id:' guards log_audit_event with no else-branch.
- **Source**: `batch-S9c.silent-failure-hunter.jsonl` (batches: S9c)

#### M-0624 · L95-100,714-719,836-841 · maintainability c9 ✅  · current: L95-100, L714-719, L836-841
- **Title**: Weight normalization block triplicated across three endpoints
- **Summary**: The identical 4-line block that builds raw_weights, computes total_w, and normalizes to weights appears three times verbatim. Any future change to the default-weight logic or column name must be applied in all three places independently.
- **Evidence**: Lines 95-100, 714-719, 836-841: `raw_weights = {row['strategy_id']: float(row['current_weight']) if row.get('current_weight') else 1.0 for row in portfolio_strategies}; total_w = sum(raw_weights.values()) or 1.0; weights = {sid: w / total_w for sid, w in raw_weights.items()}`
- **Fix**: Extract `def _build_normalized_weights(portfolio_strategies: list[dict]) -> dict[str, float]` and call at all three sites.
- **Re-verified 2026-05-15**: STILL_LIVE — Weight normalization 4-line block still triplicated verbatim in _compute_portfolio_analytics, portfolio_optimizer, and portfolio_bridge.
  - Anchor: portfolio.py:95-100, 714-719, 836-841 — identical raw_weights/total_w/weights normalize blocks across three endpoints.
- **Source**: `batch-S1b.maintainability.jsonl` (batches: S1b)

#### M-0625 · L239-242,1011-1014 · maintainability c9 ✅  · current: L239-242, L1011-1014
- **Title**: Equity curve serialization list comprehension duplicated verbatim
- **Summary**: The 3-line list comprehension that converts a cumprod Series into [{date, value}] JSON-serializable dicts appears twice identically: once in _compute_portfolio_analytics and once in verify_strategy.
- **Evidence**: Lines 239-242 and 1011-1014: `[{"date": d.isoformat(), "value": _safe_float(float(v))} for d, v in cumulative.items()]`
- **Fix**: Extract `def _series_to_curve(series: pd.Series) -> list[dict]` and call at both sites.
- **Re-verified 2026-05-15**: STILL_LIVE — Equity curve list comprehension still duplicated verbatim between _compute_portfolio_analytics and verify_strategy.
  - Anchor: portfolio.py:239-242 and 1011-1014 — identical '[{"date": d.isoformat(), "value": _safe_float(float(v))} for d, v in cumulative.items()]'.
- **Source**: `batch-S1b.maintainability.jsonl` (batches: S1b)

#### M-0626 · L248-250,1020-1022 · maintainability c9 ✅  · current: L248-250, L1020-1022
- **Title**: Annualised Sharpe/vol computation duplicated — three independent implementations now exist
- **Summary**: The 3-line block computing annualised vol (std * sqrt(252)), mean_ret (* 252), and Sharpe appears identically in both _compute_portfolio_analytics (lines 248-250) and verify_strategy (lines 1020-1022). A structurally identical `_compute_sharpe` also exists in services/portfolio_optimizer.py (line 139), making three divergent implementations of the same financial formula.
- **Evidence**: Lines 248-250: `vol = portfolio_returns_series.std() * np.sqrt(252) ... mean_ret = portfolio_returns_series.mean() * 252 ... sharpe = _safe_float(mean_ret / vol)`. Lines 1020-1022: identical substituting `returns`.
- **Fix**: Use `_compute_sharpe` from services.portfolio_optimizer or add `compute_sharpe_and_vol(returns: pd.Series) -> tuple[float|None, float|None]` to a shared module.
- **Re-verified 2026-05-15**: STILL_LIVE — Annualised Sharpe/vol formula duplicated in _compute_portfolio_analytics and verify_strategy. Third copy in services/portfolio_optimizer._compute_sharpe.
  - Anchor: portfolio.py:248-250 and 1020-1022 — same `std * sqrt(252)` / `mean * 252` / `mean_ret / vol` pattern.
- **Source**: `batch-S1b.maintainability.jsonl` (batches: S1b)

#### M-0627 · L381,385,389 · maintainability c9 ✅  · current: L381-389
- **Title**: Alert thresholds are bare magic numbers with no named constants
- **Summary**: The drawdown alert threshold (-0.10), high-severity threshold (-0.20), and correlation spike threshold (0.70) are bare float literals in _generate_alerts. These are business-critical thresholds invisible to config review and fragile to change independently.
- **Evidence**: Line 381: `if max_drawdown is not None and max_drawdown < -0.10:` Line 385: `'severity': 'high' if max_drawdown < -0.20 else 'medium'` Line 389: `if avg_pairwise_corr is not None and avg_pairwise_corr > 0.70:`
- **Fix**: Define module-level constants: DRAWDOWN_ALERT_THRESHOLD, DRAWDOWN_HIGH_SEVERITY_THRESHOLD, CORRELATION_SPIKE_THRESHOLD.
- **Re-verified 2026-05-15**: STILL_LIVE — Alert threshold magic numbers still inline: -0.10 / -0.20 / 0.70. No named constants.
  - Anchor: portfolio.py:381 'max_drawdown < -0.10'; L385 'max_drawdown < -0.20'; L389 'avg_pairwise_corr > 0.70'.
- **Source**: `batch-S1b.maintainability.jsonl` (batches: S1b)

#### M-0628 · L567-568 · security c8 ✅  · current: L967
- **Title**: CCXT exception message logged verbatim during key validation — API key fragments can appear in service logs
- **Summary**: At the key validation step, the except block logs the raw CCXT exception string: `logger.error('verify_strategy: key validation error: %s', exc)`. CCXT's auth error messages for Binance and OKX include the API key in the message body for signature-mismatch errors (e.g., 'Invalid signature for key AKID_ABC123...'). This writes the user's raw api_key into the structured log stream shipped to Railway and Sentry. The Sentry PII scrubber in sentry_init.py scrubs request fields but not log breadcrumbs.
- **Evidence**: analytics-service/routers/portfolio.py (diff line 568) — `logger.error('verify_strategy: key validation error: %s', exc)` — exc contains the raw CCXT exception which may embed the api_key from req.api_key
- **Fix**: Replace `logger.error('...%s', exc)` with `logger.error('...%s', type(exc).__name__)` to log only the exception class, or scrub credentials before logging: `safe = str(exc).replace(req.api_key, '[REDACTED]').replace(req.api_secret, '[REDACTED]')`.
- **Re-verified 2026-05-15**: STILL_LIVE — CCXT exception still logged verbatim: logger.error('verify_strategy: key validation error: %s', exc). Exc may embed user-supplied api_key for signature-mismatch errors.
  - Anchor: portfolio.py:967 'logger.error("verify_strategy: key validation error: %s", exc)' — raw exc string interpolated.
- **Source**: `batch-S1b.security.jsonl` (batches: S1b)

---
**TOTAL IN-SCOPE FINDINGS: 61**

