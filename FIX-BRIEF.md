# Fix briefing — `analytics-service/services/job_worker.py`

Scope: fix every CRITICAL + HIGH + MEDIUM≥conf-8 finding listed below in current `main`.

---

## CRITICAL — 1 findings in scope

#### C-0317 · L900 · pr-test-analyzer c10 ⏳
- **Title**: run_sync_funding_job has zero direct tests
- **Summary**: The new analytics-worker entrypoint `run_sync_funding_job` (lines 900-958) is the per-strategy handler invoked by the cron-enqueued compute jobs. It contains the exchange-routing switch (binance/okx/bybit/unsupported→FAILED-permanent), the ccxt.RateLimitExceeded → `_stamp_429` circuit-breaker stamp, the finally-close-exchange path, the empty-rows DONE branch, and the upsert call. None of these branches are exercised. A regression where the unsupported-exchange branch silently returns DONE (or where _stamp_429 is skipped on a 429) would not be caught. The Python suite has `test_funding_fetch.py` for the normalizers and `test_funding_backfill_idempotency.py` for the shared upsert, but nothing for the worker handler itself.
- **Evidence**: `grep -rn run_sync_funding_job analytics-service/tests/` → zero hits. job_worker.py:900-958 is the only definition; dispatch wiring at job_worker.py:1758-1759.
- **Fix**: Create /Users/helios-mammut/claude-projects/quantalyze/analytics-service/tests/test_job_worker_sync_funding.py with describe 'run_sync_funding_job': assertions: (a) returns FAILED+permanent when key_row.exchange='kraken'; (b) calls _stamp_429 + re-raises on ccxt.RateLimitExceeded; (c) returns DONE with no upsert when fetch_funding_* returns []; (d) calls upsert_funding_rows with the rows on happy path; (e) ctx.exchange.close() is awaited in finally even on raise. Patch services.funding_fetch.fetch_funding_binance/okx/bybit and services.funding_fetch.upsert_funding_rows; mock _exchange_preflight to return a populated ctx.
- **Source**: `batch-G14.pr-test-analyzer.jsonl` (batches: G14)

## HIGH — 14 findings in scope

#### H-0682 · L? · red-team c9 ⏳  · merged 2-way
- **Title**: run_rescore_allocator_job classifies EVERY exception as transient — permanent bugs retry forever, exhausting worker capacity (job-queue DoS)
- **Summary**: Handler hardcodes `error_kind='transient'` for any Exception (line 528). KeyError from _LIQUIDITY_TIER_RANK on adversarial preferences, AssertionError from match_engine renormalize, ValidationError from a corrupt scoring_weight_overrides JSON, schema-drift TypeError — all permanent — get re-dispatched on every watchdog tick. compute_jobs.attempts increments but the partial unique index `compute_jobs_one_inflight_per_kind_allocator` only protects against duplicates in (pending, running, done_pending_children). A failed job → re-enqueue cycle saturates _scoring_semaphore for ALL allocators with the universe full-scan (~30k strategies × analytics × returns). One bad mandate edit can DoS daily cron. Comment 'allocator scoring is idempotent' is wrong about COST.
- **Evidence**: job_worker.py:520-530 except Exception error_kind='transient'; code-reviewer #10 conf=7; silent-failure-hunter #5 conf=8; performance #11 conf=6 'reloads full universe per job'. Existing classify_exception helper noted — bypassed.
- **Also flagged**: [silent-failure-hunter] run_rescore_allocator_job classifies every exception as 'transient' — permanent bugs retry forever
- **Source**: `batch-S10c.red-team.jsonl`, `batch-S10c.silent-failure-hunter.jsonl` (batches: S10c)

#### H-0683 · L? · silent-failure-hunter c9 ⏳
- **Title**: reconcile audit emission has unwrapped `db_execute(_load_strategy_owner)` that can throw and abort the reconcile epilogue
- **Summary**: After the reconciliation report is upserted, the new audit block calls `owner_id = await db_execute(_load_strategy_owner)`. `db_execute` does NOT catch exceptions — if the supabase SELECT raises (transient PostgREST 503, network blip, RLS surprise), the exception propagates and skips the downstream `_generate_alerts` fan-out (lines 1335+). The reconcile silently appears to have succeeded (report row written) but no alerts fan out and no audit row is emitted. The fix should either swallow inside `_load_strategy_owner` or call `log_audit_event(user_id=None, ...)` which is rejected upfront, but currently the owner-lookup itself is the unprotected liability.
- **Evidence**: job_worker.py lines 1303-1322: `def _load_strategy_owner(): res = ctx.supabase.table('strategies').select('user_id').eq('id', strategy_id).maybe_single().execute(); return (res.data or {}).get('user_id') if res.data else None` ... `owner_id = await db_execute(_load_strategy_owner)` — no try/except wrapper. Comment says 'best-effort owner lookup' but the code does NOT implement best-effort semantics.
- **Source**: `batch-S9c.silent-failure-hunter.jsonl` (batches: S9c)

#### H-0684 · L? · pr-test-analyzer c8 ⏳
- **Title**: reconcile.compare audit emission has no test that proves owner_id=NULL path is gracefully handled
- **Summary**: Diff lines 312-333 add a best-effort `_load_strategy_owner` lookup and skip emission when `owner_id` is None. The structural AST test at test_routers_audit_emission_structure.py asserts a single emission exists, but does NOT cover the branch where owner_id is None — the emission is conditionally skipped. The `assert len(reconcile_calls) == 1` line (diff lines 926-929) requires exactly ONE call. If a refactor moves the emission inside an `else` branch only, the count check fails — but if the owner lookup ALWAYS returns None due to a bug in maybe_single(), the production code silently skips every reconcile audit and there is no test that fails.
- **Evidence**: diff lines 312-333: `if owner_id: log_audit_event(...)`. test_audit.py only tests the wrapper. test_routers_audit_emission_structure.py walks AST but cannot prove runtime owner lookup returns the right shape. No mocked supabase test for run_reconcile_strategy_job exists with audit verification.
- **Source**: `batch-S9c.pr-test-analyzer.jsonl` (batches: S9c)

#### H-0685 · L? · red-team c7 ⏳
- **Title**: CHAIN-25: reconcile owner-lookup db_execute unwrapped + silent owner-NULL drop + no orphan-attribution fallback = reconcile audit blind spot
- **Summary**: job_worker.py adds reconcile audit emission via `owner_id = await db_execute(_load_strategy_owner)` BEFORE the audit call (code-reviewer L15, silent-failure-hunter L12, performance L10). Compound: (1) db_execute does NOT catch exceptions — a transient PostgREST 503 in the owner lookup propagates and ABORTS the reconcile epilogue, skipping _generate_alerts (silent-failure-hunter L2). The reconcile silently appears to have succeeded (report row written by earlier step) but no alerts fan out. (2) When strategy was deleted between job enqueue and reconcile completion, owner_id is None and audit is silently skipped (silent-failure-hunter L12) — no fallback audit_log_orphan, no metric. (3) Ownership change between enqueue and completion attributes the audit to the CURRENT owner — but the data was reconciled for the OLD owner (code-reviewer L15). Attack: enqueue a reconcile for a strategy, immediately transfer the strategy to a victim user; reconcile completes 5 min later; audit row pins reconcile.compare to the victim. Combined with CHAIN-3 (no FK on user_id), even hard-deleted users get attributed audits. The 'best-effort' contract in the code comment normalizes the silent drop without addressing it.
- **Evidence**: job_worker.py:1303-1322 _load_strategy_owner with maybe_single + no try/except; performance L10 confirms extra round-trip; comment in code at silent-failure-hunter L12 'drop the audit event rather than failing'.
- **Source**: `batch-S9c.red-team.jsonl` (batches: S9c)

#### H-0689 · L554 · test-analyzer c7 ⏳
- **Title**: Multi-account/multi-strategy concurrent sync_trades race not tested
- **Summary**: sync_trades takes pg_advisory_xact_lock(hashtext(strategy_id)) which serializes per-strategy. But Phase 2 raw fill upsert at job_worker.py:579 is OUTSIDE that lock — two concurrent run_sync_trades_job invocations for SAME strategy (cron + manual trigger) could interleave their Phase 2 batches, and one Phase 1 DELETE could wipe the other's Phase 2 fills.
- **Evidence**: job_worker.py:552-588 — Phase 1 RPC takes lock, returns; lock released; Phase 2 (572-588) runs unprotected. No watchdog/pending-lock check between phases. test_job_worker.py has no concurrent test.
- **Fix**: Add real-Supabase test `test_concurrent_sync_trades_no_data_loss` in analytics-service/tests/: run two run_sync_trades_job tasks concurrently for same strategy via asyncio.gather, count is_fill rows after — assert no duplicates and no missing fills. Or add advisory lock around Phase 2.
- **Source**: `batch-G12.partial.jsonl` (batches: G12)

#### H-0691 · L637-650 · silent-failure-hunter c8 ⏳
- **Title**: Phase 2 raw-fill ingestion failure logs `warning` only — sync job marked successful with no fills
- **Summary**: In `run_sync_trades_job`, when `USE_RAW_TRADE_INGESTION=true` and `fetch_raw_trades` raises, the inner except logs `warning` saying 'Phase 1 succeeded' and continues. Job result is whatever Phase 1 (daily PnL) produced, with no propagation of the Phase 2 failure to the job row, no `data_quality_flags`, no admin compute-jobs page indicator. Operators see a healthy sync_trades success while fills are quietly never being ingested for this strategy.
- **Evidence**: job_worker.py:644-650 `except Exception as e: logger.warning('Raw fill ingestion failed for strategy %s (Phase 1 succeeded): %s', ...)`. No persisted error_kind, no last_error column update, no `phase2_failed=true` on strategy_analytics. The 'Strategies Missing Fills' health card on admin page only shows strategies with 0 fill rows total — a strategy whose fills are lagging by days due to repeated Phase 2 failures looks healthy.
- **Fix**: On Phase 2 failure: (a) write `phase2_fill_ingestion_failed=true` + error to strategy_analytics.data_quality_flags so it shows up on Position Metrics Failed health card; (b) bump a Phase 2 attempt counter on api_keys row; (c) after N consecutive Phase 2 failures, mark api_key needs_attention so allocator gets a UI nudge.
- **Source**: `batch-G12.partial.jsonl` (batches: G12)

#### H-0692 · L658-674 · silent-failure-hunter c8 ⏳
- **Title**: `ignore_duplicates=True` on raw-fill upsert silently discards exchange-amended fills (final fee, settlement updates)
- **Summary**: Phase 2 fill persistence uses `.upsert(rows, on_conflict='strategy_id,exchange,exchange_fill_id', ignore_duplicates=True)`. When the exchange amends a previously-reported fill (final settlement fee, post-trade fee adjustment, corrected price), the new payload has the same fill_id but updated values; `ignore_duplicates=True` discards the update entirely. Position ROI computed off old fee/price stays wrong forever. No log, no metric, no flag — 'persisted N raw fills' message is misleading.
- **Evidence**: job_worker.py:665-669 `ctx.supabase.table('trades').upsert([...], on_conflict='strategy_id,exchange,exchange_fill_id', ignore_duplicates=True).execute()`. Logger says 'persisted %d raw fills' counting request rows, not actual changed rows.
- **Fix**: Either (a) compare incoming fill payload against existing DB row and log `fill_amended` when fields like fee/price/cost differ, then DO UPDATE; or (b) explicitly document fill amendments are not supported and add a periodic reconciliation job. Allocators should know whether their displayed fees are 'preliminary' or 'final'.
- **Source**: `batch-G12.partial.jsonl` (batches: G12)

#### H-0693 · L660 · code-reviewer c8 ⏳
- **Title**: Phase 1 RPC and Phase 2 fill upsert run sequentially without transaction — partial-failure inconsistency
- **Summary**: Phase 1 sync_trades RPC commits, then Phase 2 batch upserts run separately via per-batch `await db_execute(_insert_fills)`. If Phase 2 fails mid-loop (network blip), Phase 1 is committed but Phase 2 partial. The cursor advance at end of run still moves last_sync_at forward, meaning the next sync's overlap window may miss the fills that failed to insert.
- **Evidence**: Lines 626-674 in job_worker.py. No transaction wrapping. The cursor advance (`update_cursor`) executes regardless of Phase 2 partial success. ignore_duplicates=True silently swallows fill-level errors.
- **Fix**: Track Phase 2 partial success: set last_sync_at only after Phase 2 batches all succeed. Or move Phase 2 to a separate job kind with its own cursor.
- **Source**: `batch-G12.partial.jsonl` (batches: G12)

#### H-1110 · L132 · type-design-analyzer c9 ⏳
- **Title**: DispatchResult.error_kind: str | None encodes the same 3-value enum as a comment, not a type
- **Summary**: DispatchResult is a frozen-feeling dataclass that carries the classifier's output across module boundaries (job_worker → main_worker → DB RPC). error_kind is typed `str | None` with a comment '# transient | permanent | unknown'. Every `return DispatchResult(error_kind='permanent', ...)` callsite (lines 362, 375, 427, 448, 454, 867, 891, 938, 1483, 1505, 1705, 1723, 1792) is a hardcoded literal — but nothing prevents a callsite (current or future) from passing `error_kind='Permanent'` (capitalization drift), `error_kind='retry'` (wrong vocabulary), or `error_kind=str(some_var)`. Main_worker.py:330 then forwards this directly to the RPC via `kind or 'unknown'`. The DB CHECK catches it, but the failure mode is a postgres exception not a static type error.
- **Evidence**: analytics-service/services/job_worker.py:128-133 DispatchResult dataclass; analytics-service/main_worker.py:322,330 propagates result.error_kind verbatim to the RPC
- **Fix**: Once ErrorKind is defined, change DispatchResult to `error_kind: ErrorKind | None = None`. Tighten the dataclass with `@dataclass(frozen=True, slots=True)` so accidental mutation also fails statically. Add a constructor-side assertion in __post_init__ that error_kind is None or matches the Literal — the DB CHECK becomes defense-in-depth instead of the only guard.
- **Source**: `batch-G21.type-design-analyzer.jsonl` (batches: G21)

#### H-1111 · L1 · type-design-analyzer c9 ⏳
- **Title**: Job status modeled as bare str with no Python Literal mirroring the DB CHECK
- **Summary**: compute_jobs.status is a DB-CHECK enum: ('pending', 'running', 'done', 'done_pending_children', 'failed_retry', 'failed_final'). Migration 089 (the headline change of PR #82) widened the claim filter to `status IN ('pending', 'failed_retry')` — the partition between 'claimable' and 'non-claimable' status values is now a load-bearing invariant. The Python worker reads `job['status']` (untyped dict access), references the literal strings in mark_* RPC params, and has no JobStatus enum/Literal anywhere. A future refactor that splits failed_retry into 'failed_retry_transient' / 'failed_retry_unknown' (a reasonable evolution) would need to grep every str literal across the worker codebase. The audit prompt explicitly flagged 'job status enum encoded as string with no Literal' — this is that.
- **Evidence**: analytics-service/services/job_worker.py uses `job: dict` throughout (lines 505, 857, 878, 900, 961, 1000, etc.); supabase/migrations/20260411144407_compute_jobs_queue.sql:112-120 defines the 6-value CHECK
- **Fix**: Add `JobStatus = Literal['pending','running','done','done_pending_children','failed_retry','failed_final']` plus a `CLAIMABLE_STATUSES: tuple[JobStatus, ...] = ('pending', 'failed_retry')` constant next to TIMEOUT_PER_KIND. The migration's widened filter is then mirrored as a single source of truth in code, and any future status added in SQL forces a Python-side update (via a typed TypedDict ComputeJobRow that exhausts JobStatus in match statements).
- **Source**: `batch-G21.type-design-analyzer.jsonl` (batches: G21)

#### H-1112 · L164 · type-design-analyzer c9 ⏳
- **Title**: classify_exception returns bare tuple[str, str] instead of (Literal['transient','permanent','unknown'], str)
- **Summary**: The error classifier's return type is annotated `tuple[str, str]`. The first element is a 3-value enum ('transient' | 'permanent' | 'unknown') enforced by a DB CHECK constraint in compute_jobs.error_kind and re-validated by mark_compute_job_failed (which raises if not in ('transient','permanent','unknown')). The Python side has no Literal/Enum representation: a typo such as `return ('permamnent', ...)` would type-check fine, slip past unit tests that only assert specific strings, and explode at the DB boundary as a generic plpgsql exception with no static signal. The PR-Y2/PR-Y1 audit prompt explicitly flagged 'classifier return type that's bool instead of an enum (Retry|Permanent|Unknown)' — the actual codebase ships the moral equivalent (bare str with a comment annotation). This is the central type-design weakness of PR #82.
- **Evidence**: analytics-service/services/job_worker.py:164 `def classify_exception(exc: Exception) -> tuple[str, str]:` with returns at lines 173, 182, 192, 198, 205, 215, 217, 220
- **Fix**: Define `ErrorKind = Literal['transient', 'permanent', 'unknown']` at module scope (next to the existing Side/PositionDirection Literals at lines 78-79), retype to `def classify_exception(exc: Exception) -> tuple[ErrorKind, str]:`, and propagate to DispatchResult.error_kind. mypy/pyright then catches the typo class statically. Zero runtime cost, single-file change.
- **Source**: `batch-G21.type-design-analyzer.jsonl` (batches: G21)

#### H-1113 · L212 · silent-failure-hunter c8 ⏳
- **Title**: HTTPException 4xx classifier swallows transient infrastructure 403/404/422 as permanent
- **Summary**: The new classifier marks ALL 4xx HTTPException (except 408/429) as 'permanent', sending the job straight to failed_final with NO retry. But routers/internal.py raises 403 'Internal API not configured' (depends on env var INTERNAL_API_TOKEN that can be temporarily misconfigured during a deploy), 403 'Forbidden' on transient token mismatch, and 404 'API key not found' which can occur during a key rotation race. analytics_runner.py also wraps any internal Exception into HTTPException(500,'Analytics computation failed') at line 1198 — a 500 falls through to 'unknown' so retries OK, but the inner exception detail string is lost (replaced with generic message). The 4xx classifier blanket-permanent rule means a strategy that ENCOUNTERS a transient 403 once (e.g., during deploy) terminates as failed_final, requiring manual admin intervention to re-enqueue.
- **Evidence**: analytics-service/services/job_worker.py:212-217 (HTTPException 4xx → permanent); analytics-service/routers/internal.py:114,118,185 (403 not-configured / Forbidden / 404 transient cases)
- **Fix**: Narrow the permanent classification to status codes that are SEMANTICALLY permanent (400 'Insufficient trade history', 422 validation). Allow caller to opt into permanent-for-this-message via HTTPException.detail prefix, OR keep 403/404 as 'unknown' (retried) because deploy-time 403s should self-heal once env is fixed. At minimum, document and add a regression test that 403 'Internal API not configured' is NOT treated as permanent.
- **Source**: `batch-G21.silent-failure-hunter.jsonl` (batches: G21)

#### H-1114 · L215 · type-design-analyzer c8 ⏳
- **Title**: HTTPException.detail is Any — silently stringifies dicts/lists with no schema guarantee
- **Summary**: FastAPI types `HTTPException.detail: Any` because callers may raise `HTTPException(400, detail={'code': 'INSUFFICIENT_TRADES', 'have': 12, 'need': 30})` for structured client errors. The new classifier branch calls `str(exc.detail)[:480]` which renders a dict as `"{'code': 'INSUFFICIENT_TRADES', 'have': 12, ...}"` — readable for humans, but the admin UI surface loses any structural contract. There is no type at the boundary saying 'compute_jobs.last_error is always a flat string scalar; structured detail must be JSON-serialized'. Worse: a non-stringifiable detail (e.g. an object whose __str__ raises) would crash classify_exception itself, propagate out of the dispatch try/except in main_worker.py:352, and trigger the 'unhandled error' fallback path — turning a permanent 4xx into a fallback 'unknown' kind, defeating the entire point of PR #82.
- **Evidence**: analytics-service/services/job_worker.py:215,217 `f"{status}: {str(exc.detail)[:480]}"`; compare with the rest of classify_exception which uses `str(exc)` (always safe because Exception.__str__ is contractually str)
- **Fix**: Guard the stringification: `try: detail_str = str(exc.detail) except Exception: detail_str = '<unstringifiable detail>'`. Or stronger: introduce a `_format_http_detail(detail: Any) -> str` helper that handles dict/list/str/None explicitly and serializes structured detail via json.dumps with a fallback. Add a regression test mirroring test_http_exception_400_is_permanent but with `detail={'code': 'X', 'data': [...]}` to lock the contract.
- **Source**: `batch-G21.type-design-analyzer.jsonl` (batches: G21)

#### H-1115 · L953 · silent-failure-hunter c8 ⏳
- **Title**: upsert_funding_rows returns errors array but worker never reads it
- **Summary**: upsert_funding_rows (funding_fetch.py:454-495) catches per-batch UPSERT failures into a `errors: list[str]` field of the return dict, then continues processing the next batch. job_worker.run_sync_funding_job reads `result['inserted']` for logging but completely ignores `result['errors']`. If 9 of 10 batches fail with 'connection reset' but the 10th succeeds, the worker reports `inserted=100` (the partial total), logs no warning, and returns DispatchResult(DONE). 900 funding rows silently missing.
- **Evidence**: funding_fetch.py:486-495 — `except Exception as exc: errors.append(str(exc)[:200]); logger.warning(...); ... return {'inserted': total, 'skipped': 0, 'errors': errors}`. job_worker.py:953-958 — `result = await upsert_funding_rows(...); logger.info('sync_funding: upserted %d funding rows for strategy %s', result['inserted'], strategy_id); return DispatchResult(outcome=DispatchOutcome.DONE)` — errors discarded.
- **Fix**: After the upsert call, if `result['errors']` is non-empty, return DispatchResult(outcome=FAILED, error_kind='transient', error_message=f"{len(result['errors'])} upsert batches failed: {result['errors'][:3]}") so the job is retried. Or at minimum, logger.error each entry with strategy_id so on-call has a searchable signature.
- **Source**: `batch-G14.silent-failure-hunter.jsonl` (batches: G14)

## MEDIUM — 15 findings in scope

#### M-0669 · L? · silent-failure-hunter c8 ⏳
- **Title**: reconcile.compare audit silently skipped when strategy was deleted between job enqueue and reconcile completion
- **Summary**: The owner-lookup uses `maybe_single()` and returns None if the strategies row is gone. The downstream `if owner_id:` then no-ops the audit emission. Comment 'Best-effort owner lookup — if the strategy was deleted between the job enqueue and now, drop the audit event rather than failing the whole reconcile path' acknowledges the gap explicitly — but the dropped event is exactly the one a forensic investigation would need ('which reconcile run committed the report rows for a now-deleted strategy?'). No fallback that emits the audit with user_id=NULL into a `audit_log_orphan` table; no metric increment.
- **Evidence**: job_worker.py lines 1307-1322: `def _load_strategy_owner(): res = ctx.supabase.table('strategies').select('user_id').eq('id', strategy_id).maybe_single().execute(); return (res.data or {}).get('user_id') if res.data else None` ... `owner_id = await db_execute(_load_strategy_owner); if owner_id: log_audit_event(...)`. The comment block normalizes the silent drop as acceptable rather than addressing it.
- **Source**: `batch-S9c.silent-failure-hunter.jsonl` (batches: S9c)

#### M-0670 · L? · type-design-analyzer c8 ⏳
- **Title**: _emit_audit in job_worker drops the metadata shape and treats `action` as bare str — same anti-pattern as the wrapper but one nesting deeper
- **Summary**: job_worker._emit_audit (L477-498) wraps log_audit_event with a tighter signature for the 3-action allocator.holdings.* family, but `action: str` (L480) and `metadata: dict | None` (L481) keep the same stringly-typed contract. The function exists specifically because Phase 06 added three new audit actions (allocator.holdings.sync_requested / sync_completed / sync_failed — see audit.ts:139-141), yet the Python wrapper does not narrow `action` to `Literal['allocator.holdings.sync_requested','allocator.holdings.sync_completed','allocator.holdings.sync_failed']` or a Pydantic enum. The entity_type is hard-coded `'api_key'` (L495), which is the only type-design win — that single literal could not have been narrower without a full discriminated union, but the action parameter is the obvious place to start.
- **Evidence**: L478-482: `def _emit_audit(allocator_id: str, api_key_id: str, action: str, metadata: dict | None = None) -> None:`. L495 entity_type='api_key' hard-coded — IF the contract for this wrapper is 'emit allocator-holdings audit only', then make the typed signature explicit: `action: Literal['allocator.holdings.sync_requested', 'allocator.holdings.sync_completed', 'allocator.holdings.sync_failed']`. The function is internal (single-underscore prefix) so no callers outside this module would be affected.
- **Source**: `batch-S9c.type-design-analyzer.jsonl` (batches: S9c)

#### M-0671 · L108 · api-contract c8 ⏳
- **Title**: sync_trades job has no idempotency key — re-dispatch creates duplicate compute_jobs even though fills dedup at DB level
- **Summary**: TIMEOUT_PER_KIND['sync_trades'] was raised from 5 to 15 minutes (L108) to support a 90-day raw fill backfill. The fill upsert at L664 uses ON CONFLICT DO NOTHING via the partial unique index, which makes fill insertion idempotent. But the job dispatcher itself has no idempotency key — re-calling /enqueue with the same (strategy_id, kind) before the first job's watchdog window expires creates a parallel run that races on the same fetch_raw_trades window, doubling exchange API spend and risking RateLimitExceeded.
- **Evidence**: job_worker.py L108 raises sync_trades timeout. L626–675 the run_sync_trades_job has no per-strategy in-progress lock or external idempotency-key parameter; the only protection is the watchdog. Fill DB-level dedup is at L666 on_conflict='strategy_id,exchange,exchange_fill_id', ignore_duplicates=True — protects the table, not the queue.
- **Fix**: Accept an optional idempotency_key (or use a deterministic key = sync_trades:<strategy_id>:<since_ms_bucket>) on the enqueue path. Reject duplicate enqueues that match an in-flight or completed job within a window.
- **Source**: `batch-G12.partial.jsonl` (batches: G12)

#### M-0672 · L514 · simplifier c9 ⏳
- **Title**: Inline `import os` inside run_sync_trades_job — should be top-level
- **Summary**: run_sync_trades_job has `import os` inside the function body to read USE_RAW_TRADE_INGESTION. The other inline imports (`from services.exchange import fetch_raw_trades`) are circular-aware, but `os` is a stdlib module — it belongs at the top of the file with the other imports.
- **Evidence**: L514: ` import os\n if os.environ.get("USE_RAW_TRADE_INGESTION", "false").lower() == "true":`. The module already imports asyncio/json/logging at top (L40–42). No reason to inline-import os.
- **Fix**: Move `import os` to the top-of-file import block. Drop the inline import. Optional: extract the env-flag check to a module-level constant `_RAW_TRADE_INGESTION = os.environ.get(...).lower() == 'true'` so the literal string isn't repeated and tests can monkey-patch the constant.
- **Source**: `batch-G12.partial.jsonl` (batches: G12)

#### M-0673 · L637 · code-reviewer c8 ⏳
- **Title**: Feature flag re-read on every job — env-var changes mid-rollout produce inconsistent worker behavior
- **Summary**: `os.environ.get('USE_RAW_TRADE_INGESTION', 'false').lower() == 'true'` is read inside run_sync_trades_job. Per-job env reads + lazy `from services.exchange import fetch_raw_trades` inside the if-block create rollout-window inconsistency: workers spawned at different env-var values process different jobs differently. Diagnosing 'why did this strategy not pull fills last sync' becomes painful.
- **Evidence**: Lines 636-639. Lazy import is also unnecessary — the module is already imported at top of file via `fetch_all_trades`.
- **Fix**: Read flag once at module import (or via a small config object). Move the import to top-of-file. Lazy import inside if-block adds clutter without benefit.
- **Source**: `batch-G12.partial.jsonl` (batches: G12)

#### M-0946 · L212 · type-design-analyzer c9 ⏳
- **Title**: HTTPException branch uses two non-exclusive `if` statements; static checker cannot see they are exhaustive over 4xx
- **Summary**: The new HTTPException branch is structured as: `if isinstance(exc, HTTPException): status = exc.status_code; if status in (408, 429): return (transient, ...); if 400 <= status < 500: return (permanent, ...)`. Both inner `if`s use `return`, so control flow is correct. But the first `if` is not `elif`, so a future maintainer who wants to add a third 4xx-specific rule (say, 451 'Unavailable for Legal Reasons' as permanent-no-retry-no-alert) must understand the exhaustiveness implicit in 'first if returned, so second if is the catch-all'. A `match status:` block with `case 408 | 429`, `case s if 400 <= s < 500`, `case _` (the unmodified fallthrough) would express the 'these are the three buckets of HTTP status the classifier knows about' invariant structurally. Currently the invariant is 'reader-must-trace-the-returns'.
- **Evidence**: analytics-service/services/job_worker.py:212-217
- **Fix**: Rewrite as `match exc.status_code: case 408 | 429: return ('transient', ...); case s if 400 <= s < 500: return ('permanent', ...); case _: pass` (then fall through to the unknown branch). Documents the trichotomy structurally and gives mypy a hook for exhaustiveness checking via a reveal_type on the trailing fallthrough.
- **Source**: `batch-G21.type-design-analyzer.jsonl` (batches: G21)

#### M-0947 · L212 · silent-failure-hunter c8 ⏳
- **Title**: Classifier ignores ColdStartSymbolDiscoveryError-shaped failures and gives caller no explicit fallback
- **Summary**: The classifier's HTTPException branch comes AFTER the ccxt.BaseError catch-all and BEFORE the final 'unknown' return. That ordering is correct for the common case. However: when analytics_runner.py:1198 wraps an arbitrary inner exception (ValueError, KeyError, DB error) into HTTPException(500,'Analytics computation failed'), the classifier sees status=500, fails the `400 <= status < 500` check, and falls through to 'unknown' — the inner exception's actual message is gone (replaced by 'Analytics computation failed' before the worker ever sees it). Sentry only gets the wrapper's `__cause__` if exc_info=True, which is NOT used by classify_exception. So operators investigating compute_analytics failures see a generic 500 admin-UI row with no actionable signal.
- **Evidence**: analytics-service/services/analytics_runner.py:1184-1198 (catch Exception → re-raise as HTTPException(500,generic)); analytics-service/services/job_worker.py:212-217 (classifier reads only exc.detail, drops __cause__)
- **Fix**: Include `exc.__cause__` repr in the classifier message when present: `f"{status}: {str(exc.detail)[:300]} (cause: {repr(exc.__cause__)[:180]})"`. OR change analytics_runner.py:1198 to include `str(e)[:200]` in the detail. Pick one — silent context loss across the layer boundary is the actual defect.
- **Source**: `batch-G21.silent-failure-hunter.jsonl` (batches: G21)

#### M-0948 · L215 · silent-failure-hunter c8 ⏳
- **Title**: Classifier truncates HTTPException.detail via str() without handling non-string detail types
- **Summary**: FastAPI HTTPException.detail can be any type — dict, list, BaseModel — not just str. The classifier does `str(exc.detail)[:480]` which produces opaque '{...}' representations for dicts and meaningless 'pydantic.BaseModel object at 0x...' for models. Operators reading admin UI rows see garbage instead of the actual failure reason. While today's callsites in analytics-service use plain strings, future code or third-party FastAPI deps raising HTTPException with structured detail will silently lose context.
- **Evidence**: analytics-service/services/job_worker.py:213-217 — `str(exc.detail)[:480]` with no type narrowing
- **Fix**: Explicitly handle non-string detail: `detail_str = exc.detail if isinstance(exc.detail, str) else json.dumps(exc.detail, default=str)`. Add a unit test for HTTPException(detail={'field':'x'}) to lock the contract.
- **Source**: `batch-G21.silent-failure-hunter.jsonl` (batches: G21)

#### M-0949 · L215 · code-reviewer c8 ⏳
- **Title**: HTTPException 4xx classifier swallows custom `detail` types that are not stringifiable to the truncated 480-char message
- **Summary**: `HTTPException(status_code=400, detail=...)` accepts `detail` as any type (str, dict, list, custom Pydantic model). The classifier does `str(exc.detail)[:480]` which works for str/dict (repr-style output), but if a handler raises `HTTPException(400, detail=SomeDataclass(...))` or `detail={"errors": [...nested...]}`, the truncation yields garbled JSON/repr that can mislead the admin UI. More importantly, the f-string `f"{status}: {str(exc.detail)[:480]}"` is then truncated again downstream by the consumer's UI fields (`mark_compute_job_failed` p_error column has its own cap). The double-truncation can produce corrupted unicode if the slice falls inside a multi-byte character — exception sanitization should slice by code point, not byte, but slicing a str is by code point in Python so this is OK. Real risk: dict/list detail renders as a string with Python repr quoting (single quotes), which is JSON-invalid if downstream parsers expect JSON.
- **Evidence**: L215-217: `if status in (408, 429): return ('transient', f'{status}: {str(exc.detail)[:480]}'); if 400 <= status < 500: return ('permanent', f'{status}: {str(exc.detail)[:480]}')`
- **Fix**: Document the contract that handlers MUST raise HTTPException with a string `detail`, or coerce non-string detail via `json.dumps(exc.detail, default=str)` then truncate. Adding a unit test with `HTTPException(400, detail={'foo': 'bar'})` would lock the contract.
- **Source**: `batch-G21.code-reviewer.jsonl` (batches: G21)

#### M-0950 · L204 · pr-test-analyzer c8 ⏳
- **Title**: HTTPException classifier branch order untested vs CCXT BaseError — overlap risk if FastAPI HTTPException ever subclasses Exception via ccxt path
- **Summary**: classify_exception orders branches: ccxt.BaseError catch-all (line 204) comes BEFORE the HTTPException branch (line 212). This works today because HTTPException doesn't inherit from ccxt.BaseError. But if a future change wraps HTTPException via a CCXT-flavored intermediate (or if a developer raises a custom exception that inherits from both), the ccxt branch would catch it first and classify it as 'unknown' — silently bypassing the 4xx → permanent logic. No test pins the ordering.
- **Evidence**: analytics-service/services/job_worker.py:200-216 — branch order matters; ccxt.BaseError before HTTPException. No test pins this specific order via a multi-inheritance class.
- **Fix**: Add to TestClassifyException (defensive, low cost):  def test_http_exception_with_ccxt_baseerror_parent_still_permanent(self) -> None:     """Defensive: even if a future caller raises a class inheriting from both,     HTTPException 4xx must classify as permanent, not unknown."""     # Today HTTPException doesn't inherit from BaseError; this test pins the     # contract that the classifier should prefer the more-specific (HTTP) branch.     # If/when refactor flips branch order, this test fires.     class HybridError(HTTPException, ccxt.BaseError):         def __init__(self):             HTTPException.__init__(self, status_code=400, detail='hybrid')             ccxt.BaseError.__init__(self, 'hybrid')     kind, _ = classify_exception(HybridError())     # If this test fails today, it's a signal to reorder the branches so     # HTTPException comes before the ccxt.BaseError catch-all.     # (Acceptable to mark xfail with a comment if current order is intentional.)
- **Source**: `batch-G21.pr-test-analyzer.jsonl` (batches: G21)

#### M-0951 · L215 · security c8 ⏳
- **Title**: HTTPException detail can be a dict containing internal context; truncated str(dict) may surface as compute_jobs.last_error
- **Summary**: The new classifier branch builds last_error via `f"{status}: {str(exc.detail)[:480]}"`. FastAPI's HTTPException.detail is typed `Any` — analytics-service/routers/csv.py raises HTTPException(detail={'ok': False, 'code': 'CSV_INVALID_FORMAT', 'debug_context': {'fmt_received': fmt}, ...}). If any present-or-future queue handler raises HTTPException with a dict detail (or imports/calls into csv.py / portfolio.py etc. which do), `str(dict)` produces the raw Python repr including all internal keys (`debug_context`, server-side hints, raw upstream fields). Truncation at 480 chars can also split UTF-8 bytes mid-character on non-ASCII detail content. This is a contained risk today (queue handlers in services/ only use string detail), but the contract `Exception → last_error` now has no schema check.
- **Evidence**: analytics-service/services/job_worker.py:212-217 (new branch); analytics-service/routers/csv.py:44-50, 70-76, 84-90, 93-99 (HTTPException(detail=dict) examples already in repo)
- **Fix**: Coerce non-string detail to a fixed, safe representation. Replace `str(exc.detail)[:480]` with: `detail_str = exc.detail if isinstance(exc.detail, str) else getattr(exc.detail, 'code', '<non-string detail>'); detail_str[:480]`. Also slice on characters (`str.encode('utf-8', 'replace').decode('utf-8', 'replace')[:480]`) to avoid mid-codepoint truncation.
- **Source**: `batch-G21.security.jsonl` (batches: G21)

#### M-0952 · L1 · type-design-analyzer c8 ⏳
- **Title**: State machine transitions (pending → running → failed_retry → pending) live only in SQL — no Python-side encoding
- **Summary**: The compute_jobs queue is a state machine: pending → running, running → done | done_pending_children | failed_retry | failed_final, failed_retry → pending (the new edge added by migration 089). Migration 038 line 106 documented the INTENT of failed_retry → pending years ago, but the implementation only caught up in PR #82. The Python worker has no representation of this state machine — no transition table, no `class JobStateMachine:` with `valid_transitions: dict[JobStatus, set[JobStatus]]`, no assertions that a claim transitions only from {pending, failed_retry} to {running}. Reviewers reading the diff to migration 089 have to mentally reconstruct 'is failed_retry already in some Python-side set somewhere that needs updating' — and the answer is no, because there is no Python-side set anywhere. This is the long-tail risk: every new status transition is one migration + zero Python changes, until one day a worker assumption silently breaks.
- **Evidence**: analytics-service/services/job_worker.py has no class/dict/set encoding compute_jobs status transitions; the state machine is documented only in CHANGELOG.md and migration headers
- **Fix**: Add a `VALID_TRANSITIONS: Final[dict[JobStatus, frozenset[JobStatus]]]` mapping at module scope in job_worker.py, with comments cross-linking to mark_compute_job_done / mark_compute_job_failed / claim_compute_jobs / reset_stalled_compute_jobs. Even if no runtime code consults the map today, having it in source means future state machine evolutions are diff-visible and migrations + Python advance together. Tiny pragmatic version: a `frozenset[JobStatus]` named CLAIMABLE_STATUSES that mirrors the new SQL WHERE clause exactly.
- **Source**: `batch-G21.type-design-analyzer.jsonl` (batches: G21)

#### M-0953 · L1 · type-design-analyzer c8 ⏳
- **Title**: compute_jobs.priority modeled as bare str — no Priority Literal
- **Summary**: compute_jobs.priority is a 3-value DB CHECK enum ('low', 'normal', 'high') that drives the priority-aware claim throttle in claim_compute_jobs_with_priority. Migration 090 (the second half of this batch) re-references priority ordering inside the dedupe CTE (`CASE priority WHEN 'high' THEN 0 WHEN 'normal' THEN 1 ELSE 2 END`). The Python worker never types priority — it's a bare `job.get('priority')` (or absent — dispatch_tick doesn't even read it). When the worker eventually grows a priority-aware feature (e.g., longer timeout for 'high' jobs, or per-priority circuit-breaker rules), the lack of a Priority Literal means the SQL → Python boundary is unguarded.
- **Evidence**: supabase/migrations/20260428120836_compute_jobs_priority.sql:54-55 defines the 3-value CHECK; analytics-service/services/job_worker.py has zero references to priority strings
- **Fix**: Add `Priority = Literal['low', 'normal', 'high']` alongside JobStatus. When a future PR adds priority-driven behavior, the boundary type is already in place. Pair with a typed `ComputeJobRow` TypedDict (status: JobStatus, priority: Priority, kind: JobKind, ...) — the dispatch_tick loop then casts the supabase response once and the rest of the worker enjoys static checking.
- **Source**: `batch-G21.type-design-analyzer.jsonl` (batches: G21)

#### M-0954 · L505 · type-design-analyzer c8 ⏳
- **Title**: dispatch and run_* handlers accept untyped `job: dict` — compute_jobs row shape lives only in SQL
- **Summary**: Every handler signature in job_worker.py is `async def run_<kind>_job(job: dict) -> DispatchResult:` (lines 505, 857, 878, 900, 961, 1000, 1455, 1505, 1655, 1700, 1789, etc., plus dispatch_tick in main_worker.py). The job dict is the raw supabase response of a compute_jobs row — 17+ columns including the load-bearing `status`, `kind`, `priority`, `attempts`, `claim_token`, `next_attempt_at`, the 4 partition columns, plus the new failed_retry semantics from migration 089. Code calls `job['kind']`, `job['id']`, `job.get('claim_token')`, `job.get('allocator_id')` etc. with KeyError as the only signal. Migration 089's contract — 'failed_retry rows are now claimable when their backoff has elapsed' — exists nowhere in Python. The PR-Y2 / G21 batch is exactly when this matters: the SQL got a major semantic upgrade, but the Python boundary did not.
- **Evidence**: analytics-service/services/job_worker.py:505 `async def run_sync_trades_job(job: dict) -> DispatchResult:` and ~10 other run_* handlers with identical signature; analytics-service/main_worker.py:290-300 iterates jobs as dicts
- **Fix**: Add a `ComputeJobRow(TypedDict, total=False)` in services/job_worker.py reflecting compute_jobs columns with proper Literal types for status/kind/priority/error_kind, plus the 4 partition column unions. Retype every `job: dict` to `job: ComputeJobRow`. This is a one-line-per-handler change; zero runtime cost; massively reduces the risk of a future schema change drifting silently from worker code. Cast once in dispatch_tick (`for job in cast(list[ComputeJobRow], jobs):`) and the rest of the pipeline gets static checking.
- **Source**: `batch-G21.type-design-analyzer.jsonl` (batches: G21)

#### M-0955 · L940 · pr-test-analyzer c8 ⏳
- **Title**: sync_funding RateLimitExceeded → _stamp_429 path not tested
- **Summary**: run_sync_funding_job's RateLimitExceeded path (lines 940-942) stamps `last_429_at` via `_stamp_429` before re-raising — this drives the per-exchange circuit breaker that prevents the next cron tick from re-enqueuing while the exchange is in cooldown. There is no test that this specific stamp happens for sync_funding (only sync_trades has the equivalent test). A regression that dropped the await on _stamp_429 — or that only stamped on sync_trades — would cause sync_funding to spin into a 429 storm on the next 4-hour tick.
- **Evidence**: `grep -n 'sync_funding.*RateLimit\|stamp_429.*sync_funding\|sync_funding.*429' analytics-service/tests/` → zero hits. Existing job_worker tests cover sync_trades 429 (test_job_worker.py:69) but not sync_funding.
- **Fix**: In the new test_job_worker_sync_funding.py (see critical finding above), add `async def test_rate_limit_exceeded_stamps_last_429_at_and_reraises` — patch the funding_fetch_binance to raise ccxt.RateLimitExceeded; mock supabase.table('api_keys').update().eq().execute; assert (a) supabase.table('api_keys').update was called with a `last_429_at` key, (b) RateLimitExceeded propagates (dispatch loop catches it for retry classification).
- **Source**: `batch-G14.pr-test-analyzer.jsonl` (batches: G14)

---
**TOTAL IN-SCOPE FINDINGS: 30**
