{"specialist":"performance","severity":"HIGH","confidence":8,"title":"CREATE INDEX on portfolio_analytics without CONCURRENTLY blocks all writers","file":"supabase/migrations/20260516122247_portfolio_analytics_stuck_row_reaper.sql","line":"L66-L68","summary":"`CREATE INDEX IF NOT EXISTS idx_portfolio_analytics_computing ON portfolio_analytics (...)` takes a full ACCESS EXCLUSIVE-equivalent lock on the table for the index build. Until the build completes, every INSERT/UPDATE/DELETE on portfolio_analytics blocks — including the live `_compute_portfolio_analytics` INSERTs from the Railway analytics worker and the cron tick.","evidence":"portfolio_analytics is an append-only history table (per comment in same file referencing H-0573 — 'next compute attempt INSERTs a fresh row per the append-only contract') and is written by analytics-service/routers/portfolio.py:360 (insert) and the per-portfolio compute cron in routers/cron.py:552. Migration 20260407075303_portfolio_intelligence.sql line 134 already established a base index `idx_portfolio_analytics_latest ON portfolio_analytics (portfolio_id, computed_at DESC)`; adding the new partial variant without CONCURRENTLY repeats a B-tree build on the same key columns under an exclusive lock instead of using SHARE UPDATE EXCLUSIVE. The migration also does NOT set `lock_timeout`, so the lock acquisition itself can queue indefinitely behind a long-running compute.","fix":"Move the `CREATE INDEX` out of the wrapping migration tx and use `CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_portfolio_analytics_computing ON portfolio_analytics (portfolio_id, computed_at DESC) WHERE computation_status = 'computing';` in a separate migration file with no surrounding BEGIN/COMMIT (CONCURRENTLY cannot run inside a tx block). Supabase migration runner wraps files — split this into its own file. Alternatively, since the partial WHERE predicate only matches rows with status='computing' (likely << 1000 even on a hot system), an EXCLUSIVE-lock build will finish fast on a small partial set — but you must still SET lock_timeout = '5s' so a hung lock acquisition aborts instead of queueing forever."}
{"specialist":"performance","severity":"MEDIUM","confidence":8,"title":"ALTER TABLE portfolio_analytics ADD COLUMN missing lock_timeout","file":"supabase/migrations/20260516121000_portfolio_analytics_data_quality.sql","line":"L31-L32","summary":"`ALTER TABLE portfolio_analytics ADD COLUMN IF NOT EXISTS data_quality JSONB` (no DEFAULT) is a metadata-only operation in PG 11+ (no table rewrite), but it still requires ACCESS EXCLUSIVE lock acquisition. Without `SET lock_timeout`, the migration can queue behind a long-running analytics compute (1-3 min per portfolio per the stuck-row-reaper file's stale_threshold comment) and block every read+write on portfolio_analytics until the long tx releases.","evidence":"Every other migration in this PR (20260516160000 through 20260516160800) follows the convention `BEGIN; SET lock_timeout = '5s';` to bound lock-acquisition latency. This migration deviates with no transaction wrapper at all. Compare to the same PR's 160000_data_deletion_requests_state_check.sql L20-L21 which sets lock_timeout = '5s' before ALTER TABLE.","fix":"Wrap the migration body in `BEGIN; SET lock_timeout = '5s'; ... COMMIT;` to match the codebase convention. Also worth: `SET statement_timeout = '30s';` so the whole migration aborts if anything pathological happens during apply."}
{"specialist":"performance","severity":"MEDIUM","confidence":7,"title":"Stuck-row-reaper migration also missing lock_timeout — second lock-acquisition exposure","file":"supabase/migrations/20260516122247_portfolio_analytics_stuck_row_reaper.sql","line":"L1-L68","summary":"Same lock_timeout omission as 20260516121000. The file has no `BEGIN; SET lock_timeout = '5s';` wrapper, so the `CREATE INDEX` lock-acquisition can queue behind a long-running compute. Coupled with the HIGH finding above (CONCURRENTLY missing), this compounds: not only is the index build taking an exclusive lock, the acquisition can wait arbitrarily long.","evidence":"File has no explicit BEGIN/SET/COMMIT — relies on Supabase's implicit per-file tx wrapper. The audit migrations 160000-160800 explicitly set lock_timeout = '5s' even though they run inside Supabase's wrapper, because the EXPLICIT SET is what bounds the lock-acquisition cost.","fix":"Add `BEGIN; SET lock_timeout = '5s';` at top of file and `COMMIT;` at end — but note this conflicts with CONCURRENTLY (which cannot run inside a tx block). The clean split is: one migration for the FUNCTION + COMMENT (transactional, with lock_timeout), one for the `CREATE INDEX CONCURRENTLY` (non-transactional, separate file)."}
{"specialist":"performance","severity":"LOW","confidence":6,"title":"COUNT(*) in visibility helper should use EXISTS for early exit","file":"supabase/migrations/20260516160700_commit_scenario_batch_strategy_visibility.sql","line":"L78-L84","summary":"`_assert_strategy_visible_to_allocator` runs `SELECT COUNT(*) INTO v_org_member_count FROM organization_members WHERE organization_id = v_org_id` then checks `IF v_org_member_count = 0`. COUNT(*) scans every member row in the index range to compute the total even though we only need to know whether any rows exist (and the very next operation is an EXISTS-style probe for the specific user anyway).","evidence":"The trigger fires BEFORE INSERT on match_decisions per row. While the (organization_id, user_id) PK on organization_members covers the query, COUNT(*) reads the full index range for the org. For organizations with hundreds of members, this is wasted IO compared to `EXISTS (SELECT 1 FROM organization_members WHERE organization_id = v_org_id LIMIT 1)` which stops at the first row. Note: match_decisions write volume is low (2 INSERT sites per grep) so impact is small — keeping as LOW.","fix":"Replace lines 78-84 with `SELECT EXISTS (SELECT 1 FROM organization_members WHERE organization_id = v_org_id) INTO v_org_has_members; IF NOT v_org_has_members THEN RETURN TRUE; END IF;`. Even better: collapse into a single query `RETURN (NOT EXISTS (SELECT 1 FROM organization_members WHERE organization_id = v_org_id)) OR EXISTS (SELECT 1 FROM organization_members WHERE organization_id = v_org_id AND user_id = p_allocator_id);` so only one round-trip happens for the membership branch."}
{"specialist":"performance","severity":"LOW","confidence":7,"title":"Visibility trigger calls SECURITY DEFINER helper per row — measurable overhead in batch INSERTs","file":"supabase/migrations/20260516160700_commit_scenario_batch_strategy_visibility.sql","line":"L114-L149","summary":"`_match_decisions_visibility_check` is a BEFORE INSERT FOR EACH ROW trigger that calls `_assert_strategy_visible_to_allocator` (SECURITY DEFINER, STABLE). Each call switches role context, sets the search_path, runs 2-3 lookups, and unwinds. commit_scenario_batch's loop (20260426131720_commit_scenario_batch_rpc.sql L106) inserts rows one-at-a-time, so the trigger fires N times per batch.","evidence":"BEFORE INSERT trigger + SECDEF helper has overhead vs. a non-SECDEF helper. The helper is correctly marked STABLE but the function-invocation cost is per-row, not amortized. For typical batches (1-50 allocator-scenario diffs), this is sub-ms and not a bottleneck — keeping as LOW. The architectural choice (trigger vs inline RPC check) is reasonable for defense-in-depth: trigger catches future writers, not just commit_scenario_batch.","fix":"No fix required for current scope. If batch sizes grow >>100, consider moving the visibility check inline into commit_scenario_batch's loop where it can be combined with the strategy ownership lookup the RPC already does (`SELECT status FROM strategies WHERE id = (v_diff->>'strategy_id')::uuid` at L153). One extra JOIN to organization_members would handle both checks in a single query per diff."}
{"specialist":"performance","severity":"LOW","confidence":6,"title":"_assert_retention_columns REVOKEd from service_role contradicts canary-cron callability comment","file":"supabase/migrations/20260516160200_retention_crons_schema_drift_probe.sql","line":"L83","summary":"Helper is REVOKEd from PUBLIC, anon, authenticated, AND service_role. The comment block at L13-L15 / L75-L80 says the helper is 'intended to be re-callable from a future canary cron if/when one is built' — but the typical Supabase cron pattern runs the body as service_role. If a future cron tries to invoke as service_role, it will fail with insufficient_privilege. Not a perf issue today; logged because the comment claims re-callability that the GRANT posture forbids.","evidence":"Line 83: `REVOKE ALL ON FUNCTION public._assert_retention_columns() FROM PUBLIC, anon, authenticated, service_role;` — vs the COMMENT at L75-L80 'intended to be re-callable from a future canary cron'.","fix":"Either (a) `GRANT EXECUTE ON FUNCTION public._assert_retention_columns() TO service_role;` to match the documented intent of canary-cron callability, or (b) update the COMMENT to clarify 'callable only from migration apply-time as superuser; canary cron will require a future GRANT'. Pure documentation/posture nit — no current operational impact."}
