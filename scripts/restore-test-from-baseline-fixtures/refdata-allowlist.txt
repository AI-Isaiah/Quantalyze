# Self-test reference-data allowlist — the FIXTURE twin of
# scripts/restore-test-refdata-allowlist.txt, loaded through the ARM_REFDATA_ALLOWLIST
# seam by every self-test arm.
#
# ⛔ WHY A FIXTURE ALLOWLIST AND NOT THE REAL ONE. The real file names eight tables
# (auth.users, public.compute_job_kinds, public.discovery_categories, …) that the
# baseline FIXTURE dump never creates. Pointed at the lane, its in-transaction gate
# would count a relation that does not exist and abort EVERY arm on a table the
# fixture was never supposed to have. The seam is what keeps the mechanism under
# test and the corpus out of it.
#
# The grammar is the real file's, byte for byte:
#
#     <basename>.sql <TAB> <schema.table> <TAB> <n> <TAB> # <anchor + consumer>
#
# ONE entry, on purpose: the fixture proves the MECHANISM (extract → replay under a
# public search_path → gate non-empty → census) end to end, not the corpus.
#
# ⭐ 164.9.2 C5: the `update:1` line below is NOT a second entry. It names the
# fixture's one literal top-level UPDATE and pins no row count, so the restore's
# REFDATA_ENTRY_N (INSERT lines only) still reads 1. It sits ABOVE the INSERT
# line on purpose: arm 33 goes RED if the extractor ever emits in line order.
#
# ⛔ THE PINNED COUNT IS 2 STATEMENTS OVER 3 ROWS, AND THE GAP IS DELIBERATE. The
# gate compares rows against STATEMENTS, so the fixture must be able to lose a row
# and still clear the floor (arm 23 leg (c), which must reach the registry/CHECK
# invariant) AND to lose two and fall under it (leg (d), the SHORT branch's only
# falsifier). See the header of 20260103000000_fixture_c.sql before changing it.
20260103000000_fixture_c.sql	public.fx_keep	update:1	# 20260103000000_fixture_c.sql:53 — 164.9.2 C5: the literal UPDATE that sets row 3 from its DEFAULT to the value arm 33 pins; listed ABOVE the INSERT line on purpose, so line order differs from statement order and only the extractor's (basename, offset) sort replays it after its INSERT (arm 33); arm 34 removes this line and the wrong-state leg aborts
20260103000000_fixture_c.sql	public.fx_keep	2	# 20260103000000_fixture_c.sql:34 and :36 — read by baseline-fixture.sql's `fx_keep_kind_check`, whose admitted labels are exactly these rows; arm 22 counts them, arm 23 proves the gate bites when the replay is deleted (leg a), when a row is lost (leg c) and when a whole statement is (leg d)
