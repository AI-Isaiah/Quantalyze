-- RED: the file carries two statements; the allowlist still pins one. A pinned
-- count moves only when an APPLIED migration is edited, which is the event this
-- refusal exists to name.
INSERT INTO fx_ref (id, label) VALUES (1, 'one');
INSERT INTO fx_ref (id, label) VALUES (2, 'two');
