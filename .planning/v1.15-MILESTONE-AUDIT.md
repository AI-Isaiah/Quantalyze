---
milestone: v1.15
audited: 2026-07-24
status: human_needed
scores:
  requirements: 12/16 satisfied (engineering) + 4/16 human_needed (founder LIVE ops)
  phases: 6/6 executed (buildable scope) — 134/139 verification human_needed, 135/136/137/138 passed
  integration: 6/6 cross-phase connections WIRED
  flows: end-to-end code path connected (dark behind MT5_ENABLED / NEXT_PUBLIC_MT5_ENABLED)
engineering_complete: true
go_live_complete: false
human_needed:
  - id: MT5SPIKE-01
    phase: 134
    reason: "Live broker-demo feasibility proof (Wine auto-login, order_check investor-vs-master, deal viability, server-time offset). Harness (scripts/mt5_spike.py) + go/no-go doc land; the LIVE run needs founder demo creds + a running gateway."
  - id: MT5GW-01
    phase: 139
    reason: "Prod gateway stand-up (gmag11 v2.3, VNC install + investor login, private network). Runbook + configs land (docs/runbooks/mt5-go-live.md, deploy/mt5-gateway/); founder LIVE op."
  - id: MT5GOLIVE-01
    phase: 139
    reason: "Hosting decision (Railway co-locate PRIMARY per research) + credential isolation + broker IP allowlisting off the gateway IP. Runbook decision matrix lands; founder LIVE op."
  - id: MT5GOLIVE-02
    phase: 139
    reason: "Real broker investor account onboard + soak to parity + LIVE flag flip (Railway MT5_ENABLED + Vercel NEXT_PUBLIC_MT5_ENABLED). Soak runner (scripts/mt5_soak.py) + tests land; founder LIVE op — the milestone's finish line."
gaps:
  requirements: []   # no codeable gaps — the 4 above are human_needed founder ops, all buildable deliverables landed + WIRED
  integration: []    # 6/6 WIRED, 0 blockers
  flows: []
tech_debt:
  - phase: 134
    items:
      - "DEAL_TYPE ambiguous middle (CHARGE/INTEREST/CANCELED/DIVIDEND/TAX) fail-loud until founder confirms classification at the live spike (136-05 human-verify checkpoint) — SAFE default (unknown→raise)."
      - "order_check master-rejection retcode 10009 is [ASSUMED] pending the live spike (WR-03) — a Phase-139 go-live gate; validate branch codes defensively."
  - phase: 139
    items:
      - "Worker Railway env dual-stack check (A2) — the single gating unknown for the Railway-co-locate-primary host recommendation; founder confirms at stand-up."
      - "Broker IP-allowlisting policy (A1) + soak-window length (A5, rec 5-10 business days) — founder-env unknowns, [ASSUMED]."
prod_risk:
  - "supabase/migrations/20260723172032_mt5_exchange_boundary_checks.sql AUTO-APPLIES to PROD on merge to main. Verified on TEST (reject-before/accept-after, all 4 constraints admit mt5, bogus rejected, version synced). Verbatim sfox/deribit clone (widen-only, never narrows)."
---

# Milestone v1.15 — MetaTrader 5 live api_verified account sync — AUDIT

## Verdict

**Engineering COMPLETE and green; go-live is founder LIVE-ops pending (`human_needed`).**

The milestone's definition of done is "FULL GO-LIVE this milestone." That finish line is NOT met
because it requires founder LIVE operations (stand up the prod gateway, onboard a real broker
investor account, soak to parity, flip the flags LIVE) that an autonomous agent cannot perform.
All ENGINEERING is done, verified per-phase, and WIRED end-to-end; the feature ships DARK behind
`MT5_ENABLED` / `NEXT_PUBLIC_MT5_ENABLED`.

This is NOT a `gaps_found` state — there are no codeable gaps. Every buildable deliverable for the
4 human_needed requirements landed (spike harness, gateway runbook + configs, soak runner). It is
NOT `passed` either (go-live unmet). It is `human_needed`: the remaining work is a founder go-live
session driven entirely by the committed runbook.

## Requirements (16)

| Requirement | Phase | Status | Evidence |
|-------------|-------|--------|----------|
| MT5GW-02 | 134 | ✅ satisfied | Mt5Client contract + offline suite (37 tests, no mt5linux) |
| MT5SRC-01 | 135 | ✅ satisfied | Source/SUPPORTED_SOURCES/_FACTORIES lockstep + parity test |
| MT5SRC-02 | 135 | ✅ satisfied | is_mt5 validate branch, structural read-only, master rejected |
| MT5SRC-03 | 135 | ✅ satisfied | 3 routes accept mt5; migration TEST-verified (PROD at /ship) |
| MT5RECON-01 | 136 | ✅ satisfied | combine_mt5_deal_ledger → ONE backbone → api_verified |
| MT5RECON-02 | 136 | ✅ satisfied | √252 traditional; mutation test; mt5 ∉ crypto sets |
| MT5RECON-03 | 136 | ✅ satisfied | reconciliation parity + uPnL-wedge DQ (CR-01 money-fix) |
| MT5CONC-01 | 137 | ✅ satisfied | bounded restart-on-timeout; 136 read bound not duplicated |
| MT5CONC-02 | 137 | ✅ satisfied | per-terminal lock + login bracket; mismatch persists nothing |
| MT5UI-01 | 138 | ✅ satisfied | flag-gated card + setup guide; OFF byte-identical |
| MT5UI-02 | 138 | ✅ satisfied | badge reused zero-change; all-roles e2e in blocking CI |
| MT5SPIKE-01 | 134 | ⚠️ human_needed | harness + go/no-go doc land; live proof founder-run |
| MT5GW-01 | 139 | ⚠️ human_needed | runbook + configs land; gateway stand-up founder op |
| MT5GOLIVE-01 | 139 | ⚠️ human_needed | host matrix + isolation runbook land; hosting founder op |
| MT5GOLIVE-02 | 139 | ⚠️ human_needed | soak runner + tests land; soak RUN + flag flip founder op |

## Cross-phase integration — 6/6 WIRED (0 blockers, 0 orphans)

1. Key save/validate: UI slot mapping (login→api_key, investor-pw→api_secret, server→passphrase) byte-consistent across ConnectKeyStep, the 135 validate branch, and the 136 worker `_make_mt5_session`.
2. Source registration: mt5 in Source Literal + SUPPORTED_SOURCES + _FACTORIES + _LEDGER_BACKED_SOURCES + _NATIVE_RETURNS_VENUES → derive routes to the ONE backbone (no ccxt clobber).
3. api_verified → badge: process_key stamps api_verified (non-csv) → factsheet trust_tier projection → provenance-blind VerifiedBadge (all-roles e2e seeded).
4. Hardening wraps the SAME derive read (lock + login bracket + restart on the live venue=="mt5" branch).
5. Flags gate consistently; OFF+OFF = fully dark. Client-on/server-off half-state → honest 400 (not a crash); WR-01 fix removed the dead guide link.
6. Soak composes the real 134 harness + 136 combiner (a green soak exercises production reconstruction).

## Nyquist

VALIDATION.md present for 134/137/139 (+ embedded validation architecture in 135/136/138 RESEARCH.md per project convention). All impl tasks carry automated pytest/vitest verify; no watch-mode; TDD tests-first. Compliant.

## Test health

Full analytics-service suite: 4419 passed / 96 skipped at milestone close. TS: tsc clean; ConnectKeyStep/closed-sets/route/parity vitest green; mt5-badge.spec.ts registered in the blocking e2e-seeded CI list.

## Remaining path to go-live (founder)

Follow `docs/runbooks/mt5-go-live.md`: host decision + A2 dual-stack check → stand-up + VNC install + investor login → credential isolation → onboard broker account → run `scripts/mt5_soak.py` over the soak window → explicit GATE-CHECK (134–138 CI + soak parity green) → flip MT5_ENABLED (Railway) + NEXT_PUBLIC_MT5_ENABLED (Vercel) + redeploy → prod verify across roles. Rollback: both flags to empty + redeploy (138 byte-identity makes it trivial).

**Do NOT archive/complete this milestone until the go-live session is done** — the finish line is FULL GO-LIVE, which is founder-pending.
