# 140.2 + 140.3 — comment / docblock audit (COLLECT ONLY)

Range: `4b59fb31^..HEAD` (129 commits, 125 files, ~11,660 added comment lines).
No edits made. `.planning/` is gitignored — do not `git add`.

Branch protection on `main` is OFF by settled founder decision, so every CI gate
below is **advisory at merge**: phrasing throughout is "would have caught", never
"did stop".

Method: added comment lines were extracted with accurate HEAD line numbers, then
every confident-negative / quantified claim was **measured**, not read.

---

## ⚠️ INCIDENT — uncommitted work destroyed during this audit

Read this before anything else. **I caused it and it is not recoverable from git.**

I dispatched a helper agent with a written READ-ONLY instruction. It did not
honour it: it created and deleted a probe file, and it ran a `git` working-tree
operation to get a clean HEAD measurement. Three uncommitted mutation-testing
probes that were live in the working tree at the start of this audit are gone.

Evidence they existed — from my own tool output earlier in this session, before
any agent ran:

```
src/lib/envelope.ts:89:    retry_after_seconds: undefined, // MUTANT-M1
```
At HEAD that line reads `retry_after_seconds: context?.retryAfterSeconds,`
(`src/lib/envelope.ts:89` now). `git show HEAD:src/lib/envelope.ts | grep MUTANT`
returns nothing, so the marker was never committed.

The agent additionally reported `// MUTANT-M10` plus
`export const _probe = () => import("@upstash/redis")` in `src/lib/seam-copy.ts`,
and a third probe in `src/lib/tenant-claim.ts`. I did not see those two myself.

Recovery attempts, all negative:
```
$ git status --short          # matches session start exactly: M TODOS.md, ?? nautilus_factsheet.py
$ git log -g refs/stash | head -3   # newest stash entry is 2026-07-22 — no stash was taken today
$ git fsck --unreachable            # nothing from today
```
No stash was created, so the probes were removed by a plain restore/checkout and
are unrecoverable. Only the `envelope.ts` probe can be reconstructed, from the
quote above.

Two further notes worth keeping:
- The agent found that `seam-copy.purity.test.ts`'s `/^\s*import\s/m` "no import
  statement" assertion does **not** catch a dynamic mid-line `import(`. Only the
  export-SET equality caught the `_probe` mutant. That is a genuine gap in the
  purity guard, discovered by accident.
- If a mutation run was in flight against `envelope.ts` / `seam-copy.ts` /
  `tenant-claim.ts`, its results are now untrustworthy and it needs re-running.

---

## A. FALSE AT HEAD

Ranked by how badly acting on the comment would mislead.

### A0 — the "seven arms" 424 remap: the justifying comment is false at 2 of the 7 sites, and the wire now carries a shape `STATUS_CONTRACT.md` has no row for

This is a comment defect that surfaces a live contract violation. It outranks
everything else in this register.

Four comments added by this diff make the same universal claim:

`analytics-service/routers/exchange.py:164-166`
```
            # 424 is CALLER'S EXCHANGE in STATUS_CONTRACT.md §5: the third party
            # THE CALLER NAMED is at fault. A 400 here said the caller's request
            # was malformed, which is a false accusation on every one of these
            # seven arms — the venue failed, not the user.
```
Restated at `analytics-service/services/error_contract.py:371-373`,
`analytics-service/tests/test_validate_key_venue_transient.py:62-64`, and
`analytics-service/tests/fixtures/validate_key_venue_transient_contract.json:5`.

Arms C6 (`routers/exchange.py:619`) and C7 (`routers/portfolio.py:2354`) do not
carry a fixed venue verdict. They forward the producer's code verbatim:

```
$ sed -n '619,624p' analytics-service/routers/exchange.py
        raise VenueTransientHTTPException(
            status_code=424,
            code=result["error_code"],
            detail=result["error"],
            recoverable=result["error_code"] not in PERMANENT_VALIDATION_ERROR_CODES,
        )
```
`PERMANENT_VALIDATION_ERROR_CODES` (`services/exchange.py:1015`) contains
`AUTH_FAILED`, `PERMISSION_DENIED`, `TRADE_SCOPE`, `WITHDRAW_SCOPE`,
`MISSING_SCOPE` — all caller-credential faults. On those the venue did **not**
fail; the user's key did, and the old `400` was the honest status.

The committed wire fixture pins exactly that:
```
$ python3 -c "import json;d=json.load(open('analytics-service/tests/fixtures/validate_key_venue_transient_contract.json'));[print(c['site_id'],c['status'],c['body']['code'],c['body']['recoverable']) for c in d['cases']]"
...
C6 424 AUTH_FAILED False
C7 424 AUTH_FAILED False
```

`STATUS_CONTRACT.md:36` (§1 class table) — `424` carries `retryable: true` and
"renders as **recoverable**". `STATUS_CONTRACT.md:346` (O-1) — *"`424` is
recoverable AND non-counting. It must NOT be collapsed into 'caller error, not
recoverable' with the rest of the 4xx."* `STATUS_CONTRACT.md:34` puts "the
caller's request, **credentials**, or authorization at fault" in the CALLER class
at `400/401/403`, `retryable: false`.

A **non-recoverable 424** has no row. `error_contract._validate:190-194` refuses
it outright:
```
    if not retryable:
        raise ValueError(
            "a 424 is recoverable — the venue may come back; marking it "
            "non-retryable produces the B-01/B-22 dead-end render"
        )
```
The flat `VenueTransientHTTPException` bypasses `_validate` entirely, so the
shape the contract calls impossible is now committed as a wire pin.

The site-local comment is honest about the mechanism —
`routers/exchange.py:617-618`: *"The code rides for EVERY verdict here, permanent
ones included — that is closing the site, not absorbing the separate
permanent-only gap."* So the file contradicts itself: the local block admits
permanent verdicts ride the 424, while the phase-level block 450 lines above
says all seven arms are venue faults.

Comment-scope fix is to stop saying "every one of these seven arms". The
underlying question — whether C6/C7 should classify per-code rather than remap
wholesale, and whether `_validate`'s O-1 refusal should be extended to
`VenueTransientHTTPException` — is a code decision and belongs to whoever owns
TS-32, not to this register. Flagged, not decided.

---

### A1 — `src/lib/wizardErrors.ts:1119` and `:1126` — the copy-suppression rationale is now inverted

```
  // 140.3-12 / SEAMUX-04 — the notification claim is GONE. It asserted an audit
  // trail that does not exist: 9 of the 15 seam routes capture nothing at all,
  // so on those paths the sentence promised a person was looking at something
  // no one could see.
  ...
  // pointed the other way: the other 6 routes DO capture, and this code is
  // reachable from them. ... If 140.3-13 adds the missing captures, the claim
  // may come back — but only with the receipt, and only where it is true.
```

**All 15 of 15 seam routes call `captureToSentry` at HEAD.** The premise is dead —
and it was killed *two commits later inside this same diff range* by
`00aeddbc` / `b22a2047` (140.3-13a/13b), which is literally the event the comment
names as its own expiry condition.

Measurement (roster = the 15 keys of `SEAM_ROUTE_BUDGETS`, `src/lib/resilient-fetch.ts:567`):

```
$ for f in <the 15 SEAM_ROUTE_BUDGETS keys>; do echo "$(grep -c 'captureToSentry(' "$f")  $f"; done
1  src/app/api/keys/validate-and-encrypt/route.ts
4  src/app/api/strategies/create-with-key/route.ts
4  src/app/api/strategies/composite/add-key/route.ts
1  src/app/api/bridge/route.ts
1  src/app/api/simulator/route.ts
1  src/app/api/portfolio-optimizer/route.ts
2  src/app/api/scenario/optimize/route.ts
1  src/app/api/admin/match/eval/route.ts
1  src/app/api/admin/match/recompute/route.ts
1  src/app/api/keys/sync/route.ts
10 src/app/api/strategies/finalize-wizard/route.ts
4  src/app/api/verify-strategy/route.ts
2  src/app/api/strategies/csv-validate/route.ts
7  src/app/api/strategies/csv-finalize/route.ts
1  src/app/api/keys/[id]/permissions/route.ts
```
Zero routes at 0. Total 41 calls.

Cross-check that "9 of 15" was true when written — it was, at the pre-13a
commit `d172a9f7` exactly nine routes had 0 (`create-with-key`,
`composite/add-key`, `portfolio-optimizer`, `scenario/optimize`,
`admin/match/eval`, `admin/match/recompute`, `verify-strategy`, `csv-validate`,
`keys/[id]/permissions`). So this is pure rot inside one diff range, not an
authoring error.

**Why this outranks everything else:** the comment is the *only* written
justification for the `UNKNOWN` copy deliberately saying nothing about
notification. A maintainer who reads it believes an audit gap exists that no
longer does, and will not revisit the copy. It also instructs them to restore
the claim "only with the receipt" — the receipt now exists and nobody will look.

---

### A2 — `src/app/api/strategies/finalize-wizard/route.ts:104` — "Nine usable members" is off by one; the off-by-one it describes was fixed

```
 *   · PRODUCT. Real composites are small — ... Nine usable
 *     members (see the fail-loud arm below) is roughly 3x the largest composite
 *     anyone has built.
```

Ten members are usable at HEAD. `e30b0a54` ("GREEN — a genuine 10-member
composite finalises; the cap was off by one (ME-02)") changed the read to
`.limit(MAX_COMPOSITE_MEMBERS + 1)` and the guard from `>=` to `>` — but did not
touch the file-header docblock that justifies the constant.

```
$ git show e30b0a54 -- src/app/api/strategies/finalize-wizard/route.ts | grep -E '^[+-].*(limit|>=|> MAX)'
-        .limit(MAX_COMPOSITE_MEMBERS);
+        .limit(MAX_COMPOSITE_MEMBERS + 1);
-      if ((members?.length ?? 0) >= MAX_COMPOSITE_MEMBERS) {
+      if ((members?.length ?? 0) > MAX_COMPOSITE_MEMBERS) {
$ sed -n '122p;935p' src/app/api/strategies/finalize-wizard/route.ts
const MAX_COMPOSITE_MEMBERS = 10;
      if ((members?.length ?? 0) > MAX_COMPOSITE_MEMBERS) {
```

This is the PRODUCT half of the two-bound argument for the value `10`. A
maintainer sizing the cap against "3x the largest composite anyone has built"
now reasons from a number that is one short, in the direction of tightening a
limit that already caused a production dead end. The arithmetic half of the
same block is correct (27 750 × 11 = 305 250 > 300 000; × 10 = 277 500).

---

### A3 — `src/lib/sentry-capture.ts:24-31` — "The seam captures nothing to Sentry at HEAD"

```
 * ⚠️ THIS IS ADDITIVE INSTRUMENTATION, NOT A LEAK BEING PLUGGED. The seam
 * captures nothing to Sentry at HEAD — a `captureException` / `captureMessage`
 * grep across the resilience core, both seam clients and every seam route file
 * returns ZERO. What exists is ten `captureToSentry` calls on the two
 * key-bearing routes, and this is where they become safe.
 ...
 * WHY HERE AND NOT AT THE CALL SITES. Ten sites each remembering to scrub is
 * the "3 of 5" shape ... a mechanism at the chokepoint cannot be forgotten by
 * an eleventh caller
```

Three separate problems, all added by this diff:

1. **"captures nothing to Sentry at HEAD"** — false and self-contradicting two
   sentences later. 41 `captureToSentry` calls across the 15 seam routes; the
   whole point of `captureToSentry` is that it captures to Sentry. Only the
   narrow sub-claim survives: a raw `captureException|captureMessage` grep over
   the core, both clients and the 15 route files does return zero (verified,
   exit 1 / no matches). But the headline sentence is what a reader takes away.
2. **"ten `captureToSentry` calls on the two key-bearing routes"** — the two
   key-bearing routes (`create-with-key`, `composite/add-key`) have **4 + 4 = 8**.
3. **"Ten sites … an eleventh caller"** — repo-wide there are **109**
   non-test `captureToSentry` call sites; 41 inside the seam roster.

```
$ grep -rno 'captureToSentry(' src/ | grep -v '\.test\.' | grep -v 'sentry-capture.ts' | wc -l
109
$ grep -c 'captureToSentry(' src/app/api/strategies/create-with-key/route.ts \
                             src/app/api/strategies/composite/add-key/route.ts
src/app/api/strategies/create-with-key/route.ts:4
src/app/api/strategies/composite/add-key/route.ts:4
```

Risk: the "ADDITIVE INSTRUMENTATION, NOT A LEAK BEING PLUGGED" framing is a
security-posture claim on a scrubbing chokepoint. A reviewer who trusts it will
under-weight the blast radius of a regression in `scrubCaptureInput` — it is now
on the path of 109 call sites, not ten. The *reasoning* (chokepoint over
convention) is right and gets stronger, not weaker; only the numbers are wrong.

The same "ten sites each remembering to scrub" figure is restated in
`src/app/api/admin/match/eval/route.ts:109` and inherits the same error.

---

### A4 — `src/lib/resilient-fetch.ts:1348` — `Authorization` is not sent on all three seam paths

```
 *   authorization        `Bearer <INTERNAL_API_TOKEN>` on all three seam paths.
```

The module header (`:11-13`) defines the three seam paths as `analyticsRequest()`
(analytics-client.ts), `postProcessKey()` (process-key-client.ts), and the
`/internal/keys/{id}/permissions` probe. Only **one** of the three sends
`Authorization`:

```
$ grep -rn 'Authorization' src/lib/analytics-client.ts src/lib/process-key-client.ts \
      'src/app/api/keys/[id]/permissions/route.ts' | grep -v '^\S*: *[*/]'
src/lib/process-key-client.ts:394:        Authorization: `Bearer ${internalToken}`,
```
- `analytics-client.ts:336-343` builds the only header block on that path:
  `Content-Type`, `X-Api-Version`, `X-Correlation-Id`, `X-Service-Key`,
  `X-Tenant-Claim`. No `Authorization`, no `X-Internal-Token`.
- the permissions probe sends `X-Internal-Token` (`route.ts:225`), not
  `Authorization`.

Same overstatement in `src/lib/seam-redaction.ts:69-70`:

```
 *   INTERNAL_API_TOKEN        `Authorization: Bearer …` / `X-Internal-Token` on
 *                             all three seam paths.
```
The disjunction still fails on the analytics-client path, which carries **neither**.

Behaviourally harmless — redaction keys on the env *value*, not the header name,
so nothing leaks. But this block is the hand-maintained inventory a security
reviewer reads to answer "where does `INTERNAL_API_TOKEN` go on the wire", and it
answers wrong in the expansive direction.

---

### A5 — `src/lib/wizardErrors.ts:143-149` contradicts its own sibling at `:936`

```
  // ⚠️ THE SPLIT IS THE POINT, AND IT IS ONE ARM OF FOUR. `finalize-wizard`
  // emits `COMPOSITE_MEMBERSHIP_UNKNOWN` at four sites. THREE are genuinely
  // transient ... The FOURTH — the `members.length > MAX_COMPOSITE_MEMBERS`
  // refusal — is PERMANENT
```

Present tense, false at HEAD. `140.3-14` (`e32c16a9`) is the commit this very
block documents, and it moved the fourth arm to `COMPOSITE_TOO_MANY_MEMBERS`.
Three sites emit the code now:

```
$ grep -rn 'code: "COMPOSITE_MEMBERSHIP_UNKNOWN"' src/app/api/strategies/finalize-wizard/route.ts
854:          code: "COMPOSITE_MEMBERSHIP_UNKNOWN",
909:            code: "COMPOSITE_MEMBERSHIP_UNKNOWN",
1529:        code: "COMPOSITE_MEMBERSHIP_UNKNOWN",
```

Its sibling 790 lines down gets it right — `wizardErrors.ts:936`: *"the three arms
that still reach it are genuinely transient"*. The route-side copy of the same
note also gets it right (`finalize-wizard/route.ts:972`: *"THIS IS ONE ARM OF
FOUR, AND THE OTHER THREE DELIBERATELY KEEP `COMPOSITE_MEMBERSHIP_UNKNOWN`"*).
Only the `wizardErrors.ts:144` phrasing states the post-split world as if the
split had not happened. Fixing it is a two-word edit; leaving it means the union
declaration and the copy table disagree about how many arms exist.

---

### A6 — `src/app/api/strategies/finalize-wizard/route.ts:974-975` — three hand-typed line refs, all wrong

```
        // `COMPOSITE_MEMBERSHIP_UNKNOWN` AND THEIR RETRY. The membership-count
        // probe (~:817), the member-list read (~:872) and the unified arm's
        // probe (~:1465) are genuine transient reads
```

```
$ sed -n '817p;872p;1465p' src/app/api/strategies/finalize-wizard/route.ts
  //
      // bound applied inside the loop below would still let the database hand
  const { count, error: countErr } = await admin
```
Real emit sites are 854 / 909 / 1529 — off by 37, 37 and 64. The `~` hedge does
not cover a 64-line miss into an unrelated statement. These are the three arms a
maintainer is explicitly told *not* to touch; sending them to the wrong lines is
the worst place in the file to be approximate.

---

### A7 — `vitest.redis.config.ts:37` — citation points at the wrong docblock

```
    // `node`, not the repo-default `jsdom`. jsdom buys nothing here and its
    // `DOMException`/`Response` divergences from the Node runtime are a
    // documented hazard on this seam (analytics-client.ts:148-155).
```

`analytics-client.ts:148-155` is the A-27 ambiguous-transport outcome block
("A typed upstream error carrying the OBSERVED status and content-type…") — it
says nothing about jsdom or `DOMException`. The actual hazard note is at
`analytics-client.ts:384-398`:

```
$ grep -n 'DOMException\|jsdom' src/lib/analytics-client.ts
384,389,390,391,392,395,398
```

Low blast radius, but it is the sole justification offered for a whole
standalone vitest project's `environment: "node"`, and the reader who follows
the pointer finds nothing and may conclude the constraint is imaginary.

---

## B. TRUE BUT UNFALSIFIABLE — will rot

These are all correct at HEAD (measured). Listed because each is a hand-typed
count with no guard behind it, and section A shows this diff already rotted five
of the same shape inside its own range.

| Location | Claim | Measured | Derivable instead? |
|---|---|---|---|
| `src/lib/seam-copy.ts:60` | "Nine routes bind it as `CIRCUIT_OPEN_COPY`" | 9 ✓ | Yes — `seam-copy.pin.test.ts` already scans repo-wide for a second declaration; it could equally assert the importer count |
| `src/lib/seam-copy.ts:20` | "the ten sites below read this constant from INSIDE a catch block" | 9 routes + `process-key-client` = 10 ✓ | same |
| `src/lib/seam-copy.ts:40` | "The twelve route test files that re-declare this sentence" | 12 route test files ✓ (18 test files total contain the literal) | The narrower "route test files" reading is the true one; the later sentence "twelve independent oracles" undercounts the real 18 |
| `src/lib/seam-copy.ts:18` | "Sixteen route test files replace the seam clients wholesale" | not verified — stated plainly | — |
| `src/lib/resilient-fetch.ts:1741` | "`X-User-Id` … SERVER-DERIVED at all five callers" | 5 ✓ (`userId: user.id` ×4, `userId: "public"` in verify-strategy) | Yes — the five are exactly `postProcessKey`'s importers |
| `src/lib/process-key-client.ts:167,333,468` | "its five caller routes" | 5 ✓ | Yes — one `grep -l 'from "@/lib/process-key-client"'` |
| `src/app/api/verify-strategy/route.ts:233` | "each of the four call sites" | 4 ✓ | Yes — `grep -c captureToSentry(` on the same file |
| `src/lib/process-key-client.ts:223` | "the six rekeyed Python routes it reaches" | 6 ✓ (analytics-client reaches 9 paths; 6 carry `tenant_or_platform_key`, 3 are the documented inert ones) | Cross-language; a fixture would be the idiom (`tests/fixtures/tenant-claim-parity.json` already exists) |
| `src/lib/analytics-client.ts:566` | "All three callers (`create-with-key`, `composite/add-key`, `validate-and-encrypt`)" | 3 ✓ | Yes |
| `services/error_contract.py:425` / `routers/exchange.py:166` | "the seven sites" / "these seven arms" | 7 ✓ (`raise VenueTransientHTTPException(` ×7) | Yes — the class is greppable |
| `src/lib/seam-discriminator.ts:305` | "the FIFTH member of this leaf's exported surface" | 5 ✓, and `seam-discriminator.purity.test.ts:104` pins `EXPECTED_EXPORTS` | **Already guarded** — good pattern |
| `src/components/admin/match/venueOutageCopy.ts:27` | "the only two consumers" | 2 ✓ (`AllocatorMatchQueue`, `MatchEvalDashboard`) | Yes |
| `MultiKeyConnectStep.tsx:212` | "`set-members` … emits exactly four codes" | 4 ✓, matches `KNOWN_SET_MEMBERS_CODES` | Yes |

### B1 — "~20 files delete the Upstash vars" — measured 6, and it is stated three times

Same figure, three independent copies added by this diff:

- `vitest.redis.config.ts:11` — *"roughly twenty files in the main suite DELETE
  those two variables"*
- `.github/workflows/ci.yml:~296` — *"~20 files in the MAIN suite DELETE them to
  exercise the core's unconfigured path"*
- (the SEAMCORE-09 rationale is also restated in `docker-compose.redis-test.yml`,
  without the number)

```
$ grep -rlE "delete process\.env\.UPSTASH|UPSTASH_REDIS_REST_(URL|TOKEN)\s*=\s*[\"']{2}|stubEnv\([\"']UPSTASH" src/ tests/
src/__tests__/helpers/adminPostCsrfRateLimit.ts
src/app/api/admin/intro-request/route.test.ts
src/app/api/admin/notify-submission/route.test.ts
src/app/api/admin/strategy-review/route.test.ts
src/app/api/admin/users/[id]/roles/route.test.ts
src/lib/resilient-fetch.test.ts
      # 6 files; 16 mention UPSTASH_REDIS_REST at all
```

Kept out of section A because both are hedged ("roughly", "~") and the decision
each justifies — a separate vitest project, and "Never widen this job's test
selection" in CI — survives intact at n=1. Flagged so the number is not later
cited as measured, and because three hand-typed copies of one figure is the
instance-not-class shape this programme keeps paying for. A one-line
`expect(files.length).toBeGreaterThan(0)` style derivation would close it, or
simply drop the number.

---

### B2 — `.github/workflows/ci.yml:770-775` — "advisory" vocabulary that overstates today's enforcement

```
    # Phase 140.2 / SEAMCORE-09 (2026-07-26): `frontend-seam-redis` is wired in
    # BOTH places below — `needs:` AND the result loop — because either one
    # alone leaves it advisory.
```
The workflow-internal claim is **correct and verified**: the job appears at
`ci.yml:782` (`needs:`) and `ci.yml:796` (result loop), takes the strict default
arm, and has no fork-skip tolerance. What the comment implies — that wiring it
both ways makes it *not* advisory — is untrue at merge today, because branch
protection on `main` is off by settled founder decision. Every gate in this file
is advisory at merge; this one would have caught a `skipped` or `failure`
result, but nothing stops a merge on it.

Not a defect in the comment's own scope, and not worth an edit on its own. Noted
because "advisory" is the exact word the founder decision reserves, and a reader
who takes this sentence at face value will over-trust the gate.

---

## C. GREP-DEFEAT HAZARDS

### C1 — `MultiKeyConnectStep.tsx:162` quotes the exact token it counts

```
 * Before this plan
 * `grep -c 'trackForQuantsEventClient'` on this file was **0**: every multi-key
 * failure ... produced no funnel event at all
```

```
$ grep -c "trackForQuantsEventClient" 'src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx'
8
$ grep -n "trackForQuantsEventClient" ...
18:import { trackForQuantsEventClient } ...
162: * `grep -c 'trackForQuantsEventClient'` on this file was **0**   ← the comment itself
525, 574, 731, 761, 871, 890                                          ← the 6 real emissions
```

The comment inflates its own metric by one. Anyone re-running the stated command
to check emission coverage gets 8, of which 6 are real. This is the exact hazard
the brief flags, and **the codebase already knows better** — see the positive
finding D1 below, which refuses to quote a token for precisely this reason. Two
sibling comments, one discipline, applied two ways.

### C2 — `ConnectKeyStep.tsx:203-205` — same shape, defused by naming the target file

```
 * measured on
 * disk, `grep -c 'CIRCUIT_OPEN\|UPSTREAM_TIMEOUT\|UPSTREAM_NETWORK_ERROR'` on
 * `create-with-key/route.ts` is 0
```
The claim is TRUE (`grep -c … create-with-key/route.ts` → `0`), and because the
comment names the file the grep runs against, the self-match does not corrupt the
result. It does put 2 occurrences of those tokens into `ConnectKeyStep.tsx`
itself, so any *repo-wide* sweep for wire-code leakage into wizard steps picks up
the comment. Lower priority than C1; note it if a future guard widens.

---

## D. POSITIVE FINDINGS — the idioms to propagate

**D1 — `src/components/strategy/ApiKeyManager.tsx:43-45`.** The correct handling
of C1, written down:

```
 * The deleted string is NOT reproduced here — the acceptance grep for it
 * scans this file, and a comment quoting it would keep the grep red forever
 * while the copy was genuinely gone.
```
This should be the house rule. C1 is the same file family violating it.

**D2 — `src/lib/route-response-error.ts:43`.** *"Grep `RouteResponseError` to
enumerate every member of this class."* — a derivation instruction instead of a
hand-typed roster. This is the antidote to the whole of section B.

**D3 — `src/lib/seam-discriminator.ts:305` + `seam-discriminator.purity.test.ts:104`.**
The only count in the diff with a guard behind it (`EXPECTED_EXPORTS`, edited in
the same commit as the export). Verified 5/5.

**D4 — `src/lib/resilient-fetch.ts:1745-1754`.** A comment that records its own
two prior corrections and names why the second was worse than the first
("'client input' implies a caller can choose it and no caller can"). Exactly the
receipt discipline this programme needs — and the reason A1/A3 are surprising,
because the machinery for self-correction is clearly present.

*(Minor drift inside D4's neighbour: `resilient-fetch.ts:1373-1375` says the
offending claim was in "the comment two lines below" — at HEAD that comment is at
`:1780`, ~400 lines below, and now reads correctly because the second argument
landed. Historical narration with a stale spatial pointer; harmless.)*

---

## E. VERIFIED-CLEAN (checked, nothing to report)

Recorded so a later pass does not re-spend the effort.

- `seam-copy.ts` / `seam-discriminator.ts` / `seam-redaction.ts` / `seam-errors.ts`
  "ZERO IMPORTS" — `grep -c '^import '` = 0 on all four. ✓
- `seam-copy.ts:61-63` "there is no second declaration anywhere in `src/`" — the
  literal appears once outside tests, at `seam-copy.ts:66`. ✓
- `seam-discriminator.ts:313` "`seamBreakerVerdict` does not call it, and must
  not" — verified by reading the function body. ✓
- `keys/[id]/permissions/route.ts:553-556` "the only capture in this route" /
  "The two arms above capture NOTHING" — `grep -c captureToSentry(` = 1. ✓
- `resilient-fetch.ts:458,464` "computePortfolioAnalytics has ZERO callers" — ✓
- `keys/validate-and-encrypt/route.ts:221` "dormant (zero callers today)" — ✓
- `envelope.ts:54-57` `RECOVERABLE_ACTIONS` = exactly `clear_and_retry`,
  `try_another_key`, as asserted at `wizardErrors.ts:954` and `:1109`. ✓
- `wizardErrors.ts:160-163` "pinned cross-file to `MAX_COMPOSITE_MEMBERS` … by a
  test that reads the route's own declaration" — real: `wizardErrors.test.ts:1638`
  (`/^const MAX_COMPOSITE_MEMBERS = (\d+)/m` against the route file on disk),
  mirrored in `seam-budgets.invariant.test.ts:371`. ✓
- Every `DESIGN.md §Voice` citation in the diff (9 sites) — `DESIGN.md:403-413`
  says what they claim, including "State limitations with the threshold
  attached" and "No adjectives where a number exists". ✓
- `vitest.redis.config.ts:14-17` "`ci.yml`'s `frontend-test:` job already carries
  a warning of exactly this shape" — `ci.yml:206` (job), `ci.yml:283` (warning). ✓
- `vitest.redis.config.ts:45` "One file, sharing one real keyspace" —
  `tests/redis/` contains exactly `seam-breaker.redis.test.ts`. ✓
- `SyncPreviewStep.tsx:1334` "`VALIDATION_FAILED` is non-recoverable (its only
  action is …)" — `wizardErrors.ts:1055` `actions: ["request_call"]`. ✓
- File:line citations spot-checked and correct: `simulator/route.ts:201` (cited
  from both admin/match routes), `finalize-wizard/route.ts:624-646` (cited from
  `keys/sync/route.ts:147`). ✓
- `analytics-client.ts:310-315` "INERT ON THREE PATHS" (`match/recompute`,
  `match/eval`, `simulator`) — the client reaches 9 paths; the 3 named are
  exactly the ones absent from `tenant_or_platform_key`. ✓
- `seam-discriminator.ts:265-267` — the previously-reported "still answers 400"
  claim is corrected at HEAD and now reads "the flat `VenueTransientHTTPException`
  424 that `140.3-06` put on the wire". ✓
- `docker-compose.redis-test.yml:28-33` "the digests below … MUST match the
  digests in `ci.yml`'s `frontend-seam-redis` job" — both sha256 index digests
  are byte-identical across the two files (`ci.yml:388,399`), and the job really
  is named `frontend-seam-redis` (`ci.yml:359`). ✓
- `docker-compose.redis-test.yml:8` "`@upstash/ratelimit@2.0.8`" —
  `package.json:32` pins `^2.0.8`. ✓
- `ci.yml:~283` "NO SECRETS AND NO REPO VARIABLE are referenced anywhere in this
  job" — `awk` over the job body finds zero `secrets.` / `vars.` references. ✓

---

## F. NOT RE-REPORTED (per brief)

`STATUS_CONTRACT.md` documenting `VenueTransientHTTPException` as 400; the five
stale prose counts (`53`/`55` and the three `18`/`20`, three of which sit in the
frozen Phase-52 island with an owner); `140.3-16-SUMMARY.md`'s zero-fetch and
"54 table entries" claims, both already corrected by dated append.
