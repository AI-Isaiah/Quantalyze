# 140.2 + 140.3 — test falsifiability review

**Scope:** `git diff 4b59fb31^ HEAD` (125 files, 129 commits). Reviewer worked in an
isolated worktree at detached `a77d607e`. Nothing committed; every mutation reverted
before the next.

**Baseline (this worktree, full default vitest suite, no scoping):**

```
Test Files  715 passed | 19 skipped (734)
     Tests  9792 passed | 287 skipped (10079)
  Duration  127.74s
```

**Caveat on "the full suite":** `npx vitest run` with the default config does NOT
reach `tests/redis/**` (its own `vitest.redis.config.ts`), the Python suite, the
eslint gates, or the e2e lane. Every "SURVIVED" below means "survived the 9792-test
default suite"; where I widened the scope I say so.

---

## 0. Headline: the mutation campaign says the seam core lane is genuinely falsifiable

Ten semantic mutations were applied one at a time to production source, each followed
by a **full** 9792-test run and an automatic revert. Contrast this with the 140.1
result the brief cites (ten simultaneous mutations → byte-identical green).

| # | Mutation | File | Result |
|---|----------|------|--------|
| M1 | a `500` now COUNTS against the breaker (`counts: false` → `true`) | `src/lib/seam-discriminator.ts:438` | **CAUGHT** — 4 failed |
| M2 | `seamCorrelationId` loses its FLAT-shape carry (the M77c shape, exactly) | `src/lib/seam-discriminator.ts:321` | **CAUGHT** |
| M3 | `seamDependencyName` drops the `SERVICE_DEPENDENCIES` defence-in-depth check | `src/lib/seam-discriminator.ts:279` | **CAUGHT** |
| M4 | `x-user-access-token` removed from `CREDENTIAL_HEADER_NAMES` (live JWT stops being scrubbed from the core's own log) | `src/lib/resilient-fetch.ts:1360` | **CAUGHT** |
| M5 | `scrubSeamError(message, [args.userAccessToken])` → `scrubSeamError(message)` | `src/lib/process-key-client.ts:542` | **CAUGHT** |
| M6 | tenant-claim TTL 300 → 600 | `src/lib/tenant-claim.ts:56` | **CAUGHT** |
| M7 | 429 attributability `caller-throttled` → `caller` | `src/lib/seam-discriminator.ts:424` | **CAUGHT** — 6 failed |
| M8 | a transport failure stops arming the breaker (`TRANSPORT.counts` → `false`) | `src/lib/seam-discriminator.ts:460` | **CAUGHT** — 12 failed / 4 files |
| M9 | 503 loses per-dependency breaker keying (every trip goes to the global key) | `src/lib/seam-discriminator.ts:449` | **CAUGHT** — 12 failed / 4 files |
| M10 | plain-object renderer stops honouring the diagnostic-key whitelist | `src/lib/seam-redaction.ts:393` | **CAUGHT — but for the wrong reason**, see §4 |

**10 / 10 caught.** Failure counts are tight (1–12 tests, 1–4 files), which is what a
genuine semantic detection looks like; timeout noise under load would scatter across
unrelated files. Individual failing test names were confirmed for M10; for M1–M9 only
the counts were captured.

The pattern in `src/lib/*.ts` (dependency-free leaves + hand-typed oracles + explicit
positive controls + fail-loud scanner bounds) is doing real work, and the answer to the
brief's central question is: **yes, the new seam-core tests can fail.** The vacuity in
this diff is not in the leaves — it is at the **edges**: the user-facing HTTP bodies,
the wizard's code→copy mapping, and two `doMock` registries that leak forward.

---

## 1. CRITICAL — the 502 body of `/api/strategies/csv-validate` returns `INTERNAL_API_TOKEN` to the browser, and this diff's own two-sided TRAP-1 oracle sits next to it looking the other way

**VERIFIED by execution.**

- Leak site: `src/app/api/strategies/csv-validate/route.ts:262` + `:274`
  ```ts
  const message = err instanceof Error ? err.message : "CSV validation failed";
  captureToSentry(err, { ... });                      // ← scrubbed at the chokepoint
  console.error("[strategies/csv-validate] unified path threw:", message);
  return csvErrorEnvelope("CSV_UPSTREAM_FAIL", message, {}, 502);   // ← NOT scrubbed
  ```
  `csvErrorEnvelope`'s second parameter is `human_message` (`route.ts:67-95`), i.e. the
  wire body.

- The new test that stands over this arm — added by **140.3-13b, in this diff** —
  drives it with a token-bearing undici message and asserts the **Sentry** payload in
  both TRAP-1 directions:
  `src/__tests__/csv-validate-route.test.ts:498-514`
  ```ts
  postProcessKeyMock.mockRejectedValue(
    new Error(`connect ECONNREFUSED 10.0.0.5:8002 (X-Internal-Token: ${INTERNAL_TOKEN})`),
  );
  ...
  expect(res.status).toBe(502);      // ← the response is checked for STATUS only
  const { err, options } = await nextCapture();   // ← everything after this is Sentry
  ```
  `res.json()` is never read in that case, nor in the `TRAP-1 BOTH DIRECTIONS` case at
  `:517`.

- Proof (probe inserted at `:506`, run, reverted):
  ```
  PROBE human_message >>> connect ECONNREFUSED 10.0.0.5:8002 (X-Internal-Token: int_9f3a1c7e5b2d84a6f0c1e3d5b7a9f2c48e6d0b1a)
  PROBE token in HTTP body? >>> true
  ```

- And the behaviour is **pinned green** by an older case in the same file:
  `src/__tests__/csv-validate-route.test.ts:382-397`
  ```ts
  // Cross-AI revision 2026-04-30: throw message surfaces verbatim.
  expect(json.human_message).toContain("ANALYTICS_SERVICE_URL not configured");
  ```
  So a fix has to delete an assertion, not just add one.

**Why this is a class defect and not a one-off.** The repo already closed this exact
class twice, by name, and both closures are quoted in source:

- `src/app/api/simulator/route.ts:214-218` — *"genuine 5xx / unexpected exceptions
  return a STATIC message. Echoing `err.message` here leaked the `parseResponse()`
  contract-violation string (Python schema field names) and FastAPI 5xx detail to
  authenticated allocators — the byte-identical defect **F5** closed in the sister
  `/api/bridge` route."*
- `src/app/api/strategies/composite/set-members/route.ts:157` — *"Never forward the raw
  message — it can carry internal detail (**H-0305**)."*
- `src/app/api/keys/[id]/permissions/route.ts:524-543` maps to a stable `userMessage`
  for the same reason.

`csv-validate` is the remaining member, it was edited by this phase, and no guard can
see it: `seam-log-coverage.test.ts`'s `SEAM_FILES` roster (`:55-64`) does not list it,
and the HI-02 completeness assertion (`:298`) only discovers routes calling
`validateKey(` / `encryptKey(` — `csv-validate` calls `postProcessKey`.

**Undetected mutation (already true at HEAD):** replace the whole of `csv-validate`'s
static-copy handling with `human_message: message` — it already is that, and 9792
tests plus a two-sided redaction oracle in the same file certify it green.

**Ledger-ready falsifier:** in the `POSITIVE:` case at `:498`, add
`expect(JSON.stringify(await res.clone().json())).not.toContain(INTERNAL_TOKEN)`.
It reddens today.

---

## 2. HIGH — `SubmitStep.test.tsx:252` names a code in its title and asserts only an affordance; the wrong code ships green

**VERIFIED by mutation.**

`src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx:252`
```ts
it("maps a thrown fetch to KEY_NETWORK_TIMEOUT", async () => {
  ...
  await screen.findByRole("button", { name: "Retry" });
  expect(findWizardError()).toBeUndefined();
});
```
The body never mentions `KEY_NETWORK_TIMEOUT`. `findByRole("button", {name:"Retry"})`
is satisfied by **any** recoverable code, and `findWizardError()).toBeUndefined()` is
the negative half of the same non-assertion.

Mutation applied at `src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx:249`:
`setErrorCode("KEY_NETWORK_TIMEOUT")` → `setErrorCode("KEY_RATE_LIMIT")` — a real union
member, so `tsc` is silent, and a completely different user-facing sentence
(*"We could not reach the exchange."* → the rate-limit copy at `wizardErrors.ts`).

```
# whole wizard steps directory + wizardErrors
Test Files  15 passed (15)      Tests  358 passed (358)
# every remaining file that mentions KEY_NETWORK_TIMEOUT
Test Files  23 passed | 2 skipped (25)   Tests  467 passed | 7 skipped (474)
```

825 tests, including all 11 files that name the constant, **cannot see it**. The user
is told the exchange throttled them when in fact our own fetch threw.

**This is instance-not-class inside this phase's own work.** The sibling wizard step,
edited in the same diff, gets it right:
`src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx:1173`
```ts
it("a thrown add-key fetch emits KEY_NETWORK_TIMEOUT", async () => {
  ...
  expect(payload.code).toBe("KEY_NETWORK_TIMEOUT");
```
and its siblings at `:1135` / `:1158` additionally assert
`toHaveAttribute("data-error-code", …)` on the rendered envelope. `SubmitStep` renders
the same `ErrorEnvelope`, which carries `data-error-code={envelope.code}`
(`src/components/error/ErrorEnvelope.tsx:138`), so the assertion is available and
simply was not made.

**Falsifier:** `expect(await screen.findByTestId("error-envelope")).toHaveAttribute(
"data-error-code", "KEY_NETWORK_TIMEOUT")` — the exact idiom the sibling file already
uses.

---

## 3. HIGH — `postProcessKey` drops the upstream `Retry-After`, and every 429 double in the repo is the *Next-side* limiter's response, so nothing can see it

**VERIFIED by execution.** Probe (created, run, deleted) driving the exact
`main.py:516-530` byte-shape through `postProcessKey` with `Retry-After: 47` on the
response:

```
PROBE status >>> 429
PROBE Retry-After header >>> null          ← dropped
PROBE body.retry_after_seconds >>> 47      ← survives, and nothing under src/ reads it
```

Corroborating measurement: `src/lib/process-key-client.test.ts` (639 new lines in this
diff) contains **zero** occurrences of `429`, and `tests/integration/process-key-thin-adapters.test.ts`
contains zero `429` and zero `Retry-After`. The client's forwarded-429 pass-through is
untested end to end.

- Python emits it: `analytics-service/main.py:516-530` — the app-global
  `RateLimitExceeded` handler returns the flat 429 body **plus**
  `headers={"Retry-After": str(retry_after)}`. `/process-key` is `@limiter.limit`-ed
  (`routers/process_key.py:817-818`), so this is a live shape.
- TS discards it: `src/lib/process-key-client.ts:643-648` —
  `NextResponse.json(body, { status: res.status })`, no `headers`. All five caller
  routes lose it.
- The only consumer reads the **header**:
  `src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx:618` —
  `const advertisedWait = parseRetryAfterSeconds(res.headers)`. The body's
  `retry_after_seconds` is read by nothing under `src/`.
- Every 429 double is `/api/keys/sync`'s **own** Upstash arm shape
  (`SyncPreviewStep.render.test.tsx:1197-1212` supplies `Retry-After: 60` on the
  response, matching `keys/sync/route.ts:118-130`). No test drives a **forwarded
  Python** 429 through `postProcessKey`.

**Consequence:** an analytics-side throttle (the per-tenant limiter PYAPI-08 exists
for) renders in the wizard as a throttle state with **no countdown and an enabled
Retry**, which immediately re-429s. The sibling site closed this (TS-34,
`keys/[id]/permissions/route.ts:495-503` forwards the header); the `postProcessKey`
half was never done. 1-of-2 class closure, and the doubles are why it reads green.

Note this sits directly under the diff's own `ErrorEnvelope` TRAP-3 work
(`ErrorEnvelope.test.tsx:474` — *"renders NO wait line at all when the envelope carries
none"*). That case is correct; the reason it can never fire on this path is that the
wait is thrown away one layer up.

---

## 4. HIGH — the redaction leaf's fallback branch dumps an arbitrary object verbatim, contradicting its own whitelist rationale; the only test of that branch uses a benign payload

**VERIFIED by probe.**

`src/lib/seam-redaction.ts:174-182` states the design:

> *"Nothing else is read. A whitelist rather than a dump is the TRAP-1-safe direction:
> an arbitrary object on this seam **can carry a request body**, and a request body on
> the key-connect path **carries raw exchange credentials**."*

`describeThrown` then falls through to `JSON.stringify(err)` at `:420` whenever the
object has none of `name/code/message/details/hint/errno/syscall` — which is exactly
what a request body looks like.

Probe (created, run, deleted):
```
PROBE A OUTPUT >>> {"api_key":"AKIAmt5login26547876","api_secret":"kX7pQ2mN9vB4tR8sL1wY6hG3jD5fA0cZ","passphrase":"hunter2","exchange":"okx"}
PROBE B OUTPUT >>> {"requestBody":{"api_secret":"kX7pQ2mN9vB4tR8sL1wY6hG3jD5fA0cZ"}}
```

The single test of this branch — `src/lib/seam-redaction.test.ts:453-462` — uses
`{ status: 503, upstream: "railway" }` and asserts only that *something* is rendered.
It is the one **one-sided** case in an otherwise rigorously two-sided file, and it is
one-sided in precisely the direction the file's header forbids.

Reachability is via `captureToSentry(err, …)` for any non-`Error` `err`
(`sentry-capture.ts:77` → `scrubSeamError`), i.e. Sentry, a third party. Nothing in
the diff bounds it.

**M10 is the sharpest evidence here.** Widening `PLAIN_OBJECT_DIAGNOSTIC_KEYS` to
`Object.keys(record)` — i.e. deleting the whitelist entirely, the exact mutation the
docblock warns against — DID redden the full suite, but by exactly one test:

```
FAIL  src/lib/seam-redaction.test.ts > [SEAMCORE-06] scrubSeamError covers the shapes
      undici and the core produce > bounds the serialised fallback so an opaque
      payload cannot flood the log
Tests  1 failed | 27 passed (28)
```

That is the **length-bound** case (`:464`, a 4 000-character blob), not a
credential-safety case. So the whitelist's stated reason for existing is protected only
incidentally, and only above 500 characters. A real
`{api_key, api_secret, passphrase, exchange}` object is ~130 characters — comfortably
under the bound — so deleting the whitelist would ship green against that payload.

**Falsifier:** one case asserting the fallback output does not contain a hand-typed
`api_secret` value that was never declared as a per-request secret.

---

## 5. MEDIUM-HIGH — `captureToSentry`'s `secrets:` argument has no roster guard, and Sentry is a third party

**Reasoned from reading + grep.**

`captureToSentry` scrubs at the chokepoint, but a **per-request** secret is only
removed if the call site names it. Measured at HEAD: **108** production `captureToSentry`
call sites; `secrets:` is passed at **13** of them, in **four** files
(`composite/add-key` ×4, `create-with-key` ×4, `verify-strategy` ×4,
`validate-and-encrypt` ×1).

The mechanism the phase built for the equivalent `console.*` risk —
`src/lib/seam-log-coverage.test.ts`, with a hand-typed `SEAM_FILES` roster **plus** an
HI-02 completeness assertion that discovers new credential-bearing routes from disk —
has **no Sentry counterpart**. `grep captureToSentry src/lib/seam-log-coverage.test.ts`
returns nothing.

So the exact defect HI-02 was filed for (a route that starts carrying credentials and
is invisible to the roster) is closed for `console.error` and open for the sink that
leaves our infrastructure.

**Undetected mutation:** delete `secrets: [api_key, apiSecretNormalized, passphraseOrNull]`
from any one of the eight sites in `create-with-key` / `composite/add-key`. Whether
that reddens depends on whether that specific site has its own behavioural case — the
class has no guard either way.

---

## 6. MEDIUM — `seam-log-coverage.ts`'s needle covers `console.error|warn` only, and identifies subjects by catch-binding or `*Err|*Error` naming

**Reasoned from reading.** This is failure-mode #6 territory, though narrower than the
`wizardFetch(` case already fixed.

`src/lib/seam-log-coverage.test.ts:117` — `const opener = /console\.(error|warn)\s*\(/g`.
A new `console.log(err)` or `console.info(\`${err}\`)` on any of the eight seam files is
invisible. (Measured: zero such sites at HEAD across all eight files, so this is a
future-proofing gap, not a live hole.)

`:276` — `const errorish = /^(error|[\w$]*(Err|Error))$/`. A Supabase result destructured
under a name outside that convention — `const { error: dbFail } = await supabase…`,
then `console.error("…", dbFail)` — is neither catch-bound nor errorish, so both
`it.each` arms skip it. The docblock at `:265-271` explains the naming convention was
chosen precisely because these bindings are not catch-bound; the residual is that the
convention is the guard.

The rest of that file is strong and I want to say so explicitly: it carries a positive
control (`:406`), a negative control (`:421`), a prose false-positive control (`:365`),
a `)`-inside-a-string control (`:396`), and a fail-loud scanner lower bound (`:347`).

---

## 7. MEDIUM — the `424` venue-attribution feature is tested on the two endpoints that cannot emit 424, and absent on the one that does

**Verified by reading the Python routers.**

- `analytics-service/routers/match.py:58` imports only `RETRY_AFTER_SECONDS,
  service_error`; its five `service_error` statuses are `503, 503, 500, 400, 500`
  (`:1655, :1689, :1808, :1873, :1892`). `/api/match/recompute` and `/api/match/eval`
  have **no 424 site**.
  Doubles that assume one: `src/app/api/admin/match/recompute/route.seam.test.ts:293-410`
  (four cases with `dependency:"binance"`/`"bybit"`),
  `.../recompute/route.test.ts:362-380`, `.../eval/route.test.ts:336-353`. Downstream,
  `src/components/admin/match/venueOutageCopy.ts` and both consumers render a named venue
  that can never arrive.
- `analytics-service/routers/internal.py:495-501` DOES emit
  `service_error(424, "EXCHANGE_PROBE_FAILED", dependency=<venue slug>, retryable=True)`
  on `/internal/keys/{id}/permissions`. Its TS consumer has **no 424 arm**:
  `src/app/api/keys/[id]/permissions/route.ts:470` handles 429 and lets everything else
  fall into the substring cascade → `PROBE_FAILED` / 502. Neither `route.test.ts` nor
  `route.seam.test.ts` contains a 424 case.

So the one venue outage the service genuinely attributes is flattened to "our upstream
is broken" on the key-permissions badge, while a "verified" venue-attribution feature
sits on two endpoints where it is dead code. Nothing reddens in either direction.

---

## 8. MEDIUM — `PortfolioImpactPanel`'s "CONTRACT A/B" doubles feed Python wire bytes into a component that fetches a Next route which cannot emit them

**Verified by grep against the route.**

`src/components/portfolio/PortfolioImpactPanel.test.tsx:1478-1505` (nested
`{detail:{code,dependency,retryable,detail}}` at 500) and `:1529-1565` (flat
app-global 429) carry a comment at `:1480` claiming *"/api/simulator is a seam route,
so this is the body this component actually receives."*

The component fetches the **Next** route (`PortfolioImpactPanel.tsx:56`).
`src/app/api/simulator/route.ts` contains `detail` only in comments; all eleven
`NextResponse.json` bodies are `{ error: string }` (+ optional `retryAfter`). A Python
500 lands on `route.ts:223` → the **static** `{error:"Portfolio impact simulation
failed."}`; a 4xx lands on `:202` → `{error: err.message}`.

Consequence: `seamErrorCode(raw)` / `seamHumanMessage(raw)` at
`PortfolioImpactPanel.tsx:95-96` — the TS-05 "Class-5 site 3" fix — can only ever
return `null` in production, and is certified working by a fabricated body. Conversely
the arms that DO decide what the user sees are not exercised: deleting the 4xx message
forward would not redden these cases.

`analytics-service/docs/STATUS_CONTRACT.md:195` and `:205-219` encode the same false
premise, so the doc will not correct a future reader.

---

## 9. MEDIUM-LOW — `analytics-client.test.ts` still builds the venue-transient bodies at `400`; the wire has been `424` since 140.3-06

`src/lib/analytics-client.test.ts:1839` and `:1866` construct the `DDOS_PROTECTION` /
`EXCHANGE_UNAVAILABLE` responses with `{ status: 400 }`, under a comment at `:1826`
saying "plan 140.3-06 remaps 400 → 424 in the next wave". That wave has landed —
verified by running `python3 -m pytest tests/test_validate_key_venue_transient.py -q`
(**23 passed**, asserting `status == 424` at all seven sites through the real `main.app`
stack) and by `analytics-service/tests/fixtures/validate_key_venue_transient_contract.json`
carrying `"status": 424` on all 14 cases.

Inert today because `classifyKeyValidationError` discards the upstream status — but
`seamBreakerVerdict` already branches on 424 (`caller-exchange`), so the moment anything
at this chokepoint does the same, these two cases exercise a status the service no
longer sends.

---

## 10. HIGH — isolation: `vi.doMock` leaks forward in two route test files, and the negative-only oracles beside them go vacuous

**VERIFIED by insertion and by shuffle.** (Caveat: these two runs were done while
production-source mutations were in flight elsewhere in this worktree; the agent
re-ran each finding in isolation against a clean `git status` for the file under test.)

`vi.resetModules()` clears the **module** registry, not the **mock** registry. A
`vi.doMock` registered inside one `describe` survives to end-of-file unless
`vi.doUnmock` is called. Measured at HEAD:

| File | `doMock` | `doUnmock` |
|---|---|---|
| `src/app/api/strategies/create-with-key/route.test.ts` | 6 | **0** |
| `src/app/api/verify-strategy/route.test.ts` | 17 | **2** (both inside one `finally`) |

**(a) `create-with-key/route.test.ts:1147-1189`** — the H-0306 describe registers a
real `withAuth` plus a session-less Supabase client; its `afterEach` (`:1155`) calls
only `vi.resetModules()`. A probe case inserted at `:1190` — where a maintainer would
naturally add a sibling — observed:

```
AssertionError: PROBE reveal: expected 401 to be 999
```

and a companion probe asserting only `expect(validateKeyMock).not.toHaveBeenCalled()`
**passed** — because the route answered 401 and the handler never ran. That is the
incident shape verbatim. Shuffling the file alone
(`--sequence.shuffle.tests=true --sequence.seed=1337`) produced
`Tests 10 failed | 53 passed (63)`, all ten being the sfox/mt5 gate and
envelope-encryption describes receiving 401.

The file survives today on two unenforced accidents: H-0306 sits second-to-last, and
the final describe (`:1213`, the 140.3-13b block **added by this diff**) happens to
re-register both factories — with a comment recording that its first run was
"10 × 401". Both are load-bearing; neither is a mechanism.

**(b) `verify-strategy/route.test.ts`** — `@/lib/process-key-client` (`:233, 308, 404,
456, 485, 573, 734`) and `@/lib/supabase/admin` (`:254, 329, 392, 446, 474, 565, 652`)
all leak to end-of-file. A probe appended at end-of-file with no mocks of its own
inherited the **entire happy path** from `:749` and returned a real 200 verification
body. Two shipped cases already run on inherited state: `:711` and `:719`
(`NEGATIVE: a caller fault … is NEVER captured`) install no mocks at all and survive
only because they also assert `res.status === 400`. And from `:254` onward the
file-top `vi.mock("@/lib/supabase/admin")` at `:70` — the one supplying the
`verification_requests` daily-cap counting path the file's own header says it exists
to pin — is permanently shadowed by doubles that `throw` on that table.

**Fixes:** `vi.doUnmock(...)` for both modules in the H-0306 `afterEach`; a file-level
`afterEach` in `verify-strategy` doing the same; explicit mocks for `:711`/`:719`.

**Structural fix worth considering:** `src/test-setup.ts:47-49` is the only global
`afterEach` and it calls `cleanup()` only — no `vi.unstubAllGlobals()`, no
`process.env` snapshot/restore. That is why "4 of 38 files never unstub" is a standing
repo condition. Relatedly `src/lib/process-key-client.test.ts` sets
`INTERNAL_API_TOKEN` / `ANALYTICS_SERVICE_URL` in six describe-level `beforeEach`
hooks and never deletes them; `process.env` is process-wide, so every file scheduled
after it in the same worker inherits both. No test in this diff demonstrably depends
on that, so it is a smell rather than a proven vacuity — but adding both to
`test-setup.ts` closes the class permanently.

### Isolation lanes that ARE clean — do not re-audit

- **`vi.stubGlobal` without `vi.unstubAllGlobals`: clean.** All 15 changed files that
  call it have a matching `vi.unstubAllGlobals()` in an *enclosing* `afterEach`
  (checked by scope, not by count). The five grep hits with no unstub in the same file
  (`SyncPreviewStep.poll-disjointness.runtime.test.tsx:43`,
  `recompute/route.seam.test.ts:177`, `keys/sync/route.seam.test.ts:242`,
  `verify-strategy/route.seam.test.ts:225`, `PortfolioImpactPanel.test.tsx:60`) are
  **comments warning against the idiom**, not calls.
- **Fake timers: clean.** Every `useFakeTimers` in the diff has a `useRealTimers` in an
  enclosing `afterEach` or a `finally`.
- **Module-scope mutable state: clean.** `WizardClient.test.tsx` (`:29/50/67` reset at
  `:307`), `ApiKeyManager.test.tsx:84` (reset at `:428`/`:670`, plus a
  `not.toBeNull()` guard before use), `permissions/route.test.ts` `STATE`/`RF`
  (`:208-241`).
- **Shared fixtures: clean.** `src/test/helpers/upstash-breaker.ts` holds no
  module-level mutable state; `tests/fixtures/tenant-claim-parity.json` is read-only.
- **`tests/redis/seam-breaker.redis.test.ts`**'s two module-scope `let`s
  (`expectsDenial:312`, `casesExecuted:327`) are deliberate anti-vacuity machinery with
  documented drain semantics.

---

## 11. LOW/INFO — three smaller ones

- **`/process-key`'s middleware gate emits the NESTED envelope; every wizard reader takes
  `code` from the top level.** `analytics-service/main.py:679-684`
  (`500 INTERNAL_TOKEN_UNCONFIGURED`) and `:694-707` (`401 UNAUTHENTICATED`) use
  `service_error_response`. `postProcessKey` forwards verbatim. `SubmitStep.tsx:207`
  reads `data.code`; `SyncPreviewStep.tsx:601` reads `failure?.code` — both top-level,
  both `undefined` → `UNKNOWN`, and `seamCorrelationId` returns `null` (no
  `correlation_id` in that shape at all). No double models it. A stale Vercel-side
  `INTERNAL_API_TOKEN` therefore renders as the generic dead end with no id.
- **`permissions/route.test.ts:655-668` feeds a 429 shape that endpoint cannot produce.**
  `routers/internal.py` carries no `@limiter.limit`, so `RateLimitExceeded` never fires
  there; its only 429 is the **nested** `service_error(429, "RATE_LIMITED",
  retry_after=60)` at `internal.py:246-251`. All six TS-34 429 cases (`:709, :745, :772,
  :802, :820`) use the flat payload, so no case drives 429 *and* the nested envelope
  together. Structurally covered by CONTRACT A's nested read at 500, hence LOW.
- **`seam-ssr-exposure.pin.test.ts:111` exempts every `"use client"` module from the
  seam-import scan** on the stated grounds that it "runs in the browser, not on a
  render" (`:253`). In the App Router a `"use client"` module is still **server-rendered**
  on the first pass, so a seam call in a render body — as opposed to an effect or a
  handler — would throw during SSR exactly as the pin's rationale says it must not. No
  such call exists today. Also note `SEAM_IMPORT_EDGE` allows `./` and `../` but not
  `../../` (no such import exists at HEAD).

---

## Lanes I checked and believe are clean — do not re-audit

- **`src/lib/seam-discriminator.ts` + `seam-discriminator.test.ts`** — the status→verdict
  table matches `error_contract._validate` arm for arm; M1/M2/M3/M7/M8/M9 all target it
  and the first four are caught.
- **`src/lib/tenant-claim.ts` + fixture** — every MAC recomputed in-test with
  `node:crypto`, TTL hand-typed, fixture read **both ways** (round-trip + independent
  HMAC self-check) with a non-empty guard. The missing Python reader is already tracked
  (TS-36, owner 146) and disclosed in the fixture's own `_comment`.
- **`src/lib/seam-copy.pin.test.ts`** — hand-typed literal, hand-typed consumer count
  (10, not `discovered.length`), comment-stripped scan, banned-infrastructure-token
  negative. The deliberate exclusion of the twelve route-test literals is correct
  (TRAP-9) and stated.
- **`SyncPreviewStep.poll-disjointness.runtime.test.tsx`** — carries the positive control
  the M77c lesson demands (`:406`, "the poll tick issues at least one network call"), a
  derived-set non-triviality bound (`:350`), a matcher polarity pin (`:377`) whose
  negative member is the LIVE tick target, and an honesty case proving the kickoff DOES
  hit the seam (`:442`).
- **`tests/redis/seam-breaker.redis.test.ts` + `frontend-seam-redis`** — no `skipIf`
  anywhere; empty-corpus preflight; store-reachability preflight; wired into BOTH
  `needs:` and the aggregator's result loop with no fork-skip tolerance arm; R-5/R-6/R-7
  cover exactly the three sliding-window properties `upstash-breaker.ts`'s fixed-window
  double cannot express.
- **`src/test/helpers/upstash-breaker.ts`** — the de-self-referencing done in 140.2-02 is
  real: `FAKE_THRESHOLD`, `FAKE_WINDOW_MS`, `FAKE_LOCK_TOMBSTONE_MS`,
  `BREAKER_KEY_LITERAL` and the lock encoding are all hand-typed and pinned
  literal-against-literal in `seam-constants.pin.test.ts`.
- **`ErrorEnvelope.test.tsx`** — each "renders no X" negative has a sibling positive in
  the same describe that reddens if the subject (`data-testid="error-envelope-wait"`,
  the Retry label) is deleted. The two false-affordance conditions are tested separately
  so a collapse of the two guards reddens.
- **`venueOutageCopy.ts`** — both branches (named venue, `null` → "An exchange") asserted
  at both consumers with hand-typed sentences.
- **`resilient-fetch.wiring.test.ts`** — 13 bindings pinned individually AND compared as
  a SET against a hand-typed roster, so a 14th binding REUSING an existing key fails.
- **`finalize-wizard/route.test.ts:1516`** (H-0328) — token embedded in both `message`
  and `name`, aggregate over every `console.error` argument, and a positive control that
  the safe formatter still emits something.
- **`RouteResponseError` call sites** — `PortfolioOptimizer`, `ReplacementPanel` and
  `KeyPermissionBadge` each pair the "raw message never reaches the DOM" negative with a
  positive on the component's own fallback copy AND a positive that the raw value still
  reaches `console.error`. The wiring is tested, not just the helper.

## Reconciled fixtures — verified, do not re-audit

- `validate_key_venue_transient_contract.json` ↔ `tests/lib/validate-key-venue-transient-parity.test.ts`
  — green both sides (23 pytest / 48 vitest), independently hand-typed rosters,
  `recoverable` polarity pinned in both directions with the `AUTH_FAILED` negative
  control at both collapse sites.
- `process_key_onboard_contract.json` — two real consumers, positives proven against a
  real TestClient response.
- 204/205/304 and non-JSON 2xx — Python never sends them; both clients model them
  defensively and say so.
