# Comment claims inside TEST files — 140.2 + 140.3

> **PROVENANCE.** Produced by a child agent of the FIRST (invalidated) comment-accuracy run,
> during the shared-tree window in which stray `MUTANT` markers were live in
> `src/lib/envelope.ts`, `src/lib/tenant-claim.ts`, `src/lib/seam-copy.ts`.
> None of those three is a subject below. **Retained.**
>
> ⚠️ The agent labelled its measurements `HEAD (56fb7167)` — that is the PRE-PHASE commit and the
> label is WRONG. Real HEAD is `a77d607e`. Its measurements are nonetheless consistent with
> `a77d607e` (they reflect post-G2 line numbers), so the label — not the work — is the error.
> **Ironic given the subject.** Orchestrator independently re-verified findings 1 and 6; both CONFIRMED.

## TIER 1 — would actively mislead

### 1. `src/lib/sentry-capture.test.ts:7-16` — "the seam captures nothing to Sentry today" — FALSE
> *"…every seam route file returns ZERO at HEAD: the seam captures nothing to Sentry today.
> What DOES exist is ten `captureToSentry` calls on the two key-bearing routes…"*

**ORCHESTRATOR-VERIFIED at `a77d607e`: 41 `captureToSentry` call sites across ALL 15 seam routes.**
(validate-and-encrypt 1 · create-with-key 4 · composite/add-key 4 · bridge 1 · simulator 1 · portfolio-optimizer 1 · scenario/optimize 2 · match/eval 1 · match/recompute 1 · keys/sync 1 · finalize-wizard 10 · verify-strategy 4 · csv-validate 2 · csv-finalize 7 · permissions 1.)
The `captureException`/`captureMessage` half IS true (measured 0). Everything after it is not — and it was **already 20-across-5 at the commit that introduced the file** (`19e904c2`), so it was never accurate. **No pair of routes sums to 10.** The seam is now the densest Sentry emitter in the repo.

### 2. `src/lib/wizardErrors.test.ts:1442-1446` — "9 of the 15 seam routes capture nothing" (present tense)
It is the **reason string** for the `"been notified"` copy ban. True at the diff base `4b59fb31^` (exactly those 9). Closed by `00aeddbc` (4 routes) + `b22a2047` (5 routes) **in this same diff**. At HEAD: **0 of 15**. The comment's own precondition ("until a route has one") is now satisfied everywhere, so the *stated* ground for the ban has evaporated — though the ban remains defensible on other grounds.
✅ Sibling claims at `admin/match/eval/route.test.ts:402-408` and `portfolio-optimizer/route.test.ts:389-393` say the same thing but are explicitly framed as **historical** ("Measured on the untouched tree") — those are CORRECT.

### 3. `src/lib/seam-log-coverage.test.ts:20,32` — "six files" / "a seventh file"; roster is **EIGHT**
`SEAM_FILES` (`:55`) has 8 entries, and the docblock 14 lines lower at `:37` says "The eight seam files", `:46` says "⚠️ EIGHT SINCE HI-02, AND THE LAST TWO WERE THE HOLE". **The file contradicts itself within one screen.**
Leaked into the registry: `src/__tests__/contracts/contracts-registry.test.ts:164` — *"no console.error/warn in the six seam files…"*. That string is the human-readable description of guard coverage, so an auditor **under-counts by two** — exactly the HI-02 hole the file's own docblock says cost the phase.
✅ `:38`'s "fifteen ROUTES … only five carry credential-bearing error logs" is CORRECT (15 keys; 5 of the 8 `SEAM_FILES` are routes).

### 4. `src/lib/seam-constants.pin.test.ts:140-143` — `exchange.py:314,324` now land on prose
Both cited lines are now inside a RED-TEAM comment block. Real sites: **`exchange.py:335` and `:345`**. Broken by `1f8ad052` (`fix(140.3-06): all seven venue sites answer 424`) — **a commit inside this diff** — which shifted them 21 lines.
The **set** claim still holds (`portfolio.py:684`, `match.py:1657,1691` verify exactly); only the exchange line numbers are stale. Matters more than usual: the comment is the stated evidence base for the hand-typed `EXPECTED_DEPENDENCIES` map at `:150`.

### 5. `src/app/api/keys/sync/route.test.ts:873-874` — `SubmitStep.tsx:156` points at nothing
Cited as the place that "already branches on `data.code === "WIZARD_DUPLICATE"`". Line 156 is inside the `COMPOSITE_MEMBERSHIP_UNKNOWN` allow-list comment added by 140.3-14. The real branch is at **`:235`**, 79 lines away.

### 6. Four `SyncPreviewStep.tsx` citations, ALL off by exactly 6 — **broken by OUR OWN gap-closure docs commit**
`src/lib/seam-poll-disjointness.pin.test.ts:145-151` cites `:582`, `:1230`, `:745`; `SyncPreviewStep.poll-disjointness.runtime.test.tsx:246-247` cites `:757`.
Actual at HEAD: **588 · 1236 · 751 · 763.** All four were CORRECT when written (verified against `e93044c1`).
**ORCHESTRATOR-VERIFIED:** `2d58fd45 docs(140.3-G2): drop five unowned prose counts, name the guard instead` touched that file **+7/−1 = net +6**.
⚠️ **The commit whose stated purpose was that "a pointer cannot drift" invalidated four pointers.** This is our own regression, introduced during the gap closure, and it is the strongest available argument that line-number citations are themselves an unowned-count class.

### 7. "Sixteen route test files replace the seam clients wholesale" — measured **15**, and it was 15 when written
Five copies: `seam-errors.purity.test.ts:29,105` · `seam-copy.purity.test.ts:28,86` · `seam-discriminator.purity.test.ts:31,116` · `analytics-client.test.ts:1622,1849`.
Measured: **15** under `src/app/api`; 18 adding the three `src/__tests__` route tests; 21 repo-wide. **No definition yields 16.** Already 15 at the commit that first wrote it (`35c67d7b`).
The number is baked into four `PURITY_RATIONALE` **failure messages** — i.e. it is the text an engineer reads at the exact moment a purity guard reddens.

## TIER 2 — smaller, still wrong

8. `resilient-fetch.test.ts:366,371` — "eight assertions" / "every one of those eight sites"; `expectLockArmed(` call sites measure **9** (731, 912, 929, 1055, 1148, 1159, 1168, 1246, 1535).
9. `seam-constants.pin.test.ts:307` — `describe("breaker constants — all six pinned…")` pins **seven** constants plus two relational assertions; `resilient-fetch.ts` exports **eight** `BREAKER_*` (three `BREAKER_STORE_*` pinned separately at `:405-429`). "All six" reads nothing at HEAD.
10. `admin/match/recompute/route.seam.test.ts:340-342` — "the only oracle in the repo … the only one that runs the REAL client over REAL wire bytes." False as written: `analytics-client.test.ts:1820-1822` says the same of itself. The *venue-name* uniqueness may hold; the stated reason does not.

## Verified TRUE — do not re-flag
`seam-log-coverage.test.ts:38` (15 routes / 5 credential-bearing) · `seam-copy.pin.test.ts:29,50,136` ("twelve independent oracles" — 12 under `src/app/api`, **none** imports the constant; checked all 18 files holding the literal) · `resilient-fetch.wiring.test.ts:651` ("the 13 bindings" — B-01…B-13) · "all seven `VenueTransientHTTPException` sites" (exchange ×6, portfolio ×1) · "FIVE caller routes" calling `postProcessKey` · `seam-redaction.test.ts:421` ("six converted Supabase log sites") · `MultiKeyConnectStep.test.tsx:1201` · `SubmitStep.test.tsx` (`finalize-wizard/route.ts:992` → 503) · citations at `finalize-wizard/route.ts:1551`, `match.py:1657,1691`, `internal.py:303,319,498`, `portfolio.py:684`, `wizard-correlation.ts:58`, `critical-regressions.test.ts:134`, `check-admin-route-manifest.ts:298-322`, `Modal.test.tsx:23-39`, `seam-ssr-exposure.pin.test.ts:186`.

**Citation spot-check: 19 checked, 5 wrong** (`exchange.py:314`, `:324`, `SubmitStep.tsx:156`, and the four-`SyncPreviewStep` cluster counted as one).

## Suspected, NOT verified
`admin/match/recompute/route.seam.test.ts:327` — "the ENTIRE suite stayed green (3 163 passed, 0 failed)". Suite not re-run. Written as a past-tense mutation receipt, so arguably defensible, but it is a hand-typed number in the same class and later phases added thousands of test lines — near-certainly no longer reproducible.

## The class conclusion
Line-number citations into other files are an **unowned count** in exactly the sense GC-4 attacked prose counts: they are correct when written, nothing derives or checks them, and a routine edit elsewhere silently invalidates them. **Three separate commits inside this diff each broke citations** — `1f8ad052` (a fix), `2d58fd45` (a docs commit *about* unowned counts), and 140.3-14's allow-list insert. Any remedy that only removes prose integers leaves this class untouched.
