# 140.2 + 140.3 — comment accuracy review

Range: `4b59fb31^..HEAD` (129 commits, 125 files, ~11 660 added comment lines).
Method: added/modified comment lines extracted from the diff, then every confident
negative / absolute / counted claim **measured** against HEAD rather than read.

Branch protection on `main` is OFF by settled founder decision, so every CI gate here is
advisory at merge — the guards below **would have caught** these; none **did stop** them.

Out of scope by instruction, not re-reported: `STATUS_CONTRACT.md` documenting
`VenueTransientHTTPException` as 400; the five stale prose counts (53/55, three 18/20);
`140.3-16-SUMMARY.md`'s zero-fetch and "54 table entries" claims.

**Headline:** the single largest defect class is *self-inflicted*. `140.2` wrote several
comments describing the seam's Sentry-capture state, its 4xx statuses, and its export
surfaces as facts. `140.3` then changed every one of those facts, in this same range, and
did not go back. Seven of the seventeen findings below are 140.2 comments falsified by
140.3 commits. F-1, F-2 and F-16 are three separate statements of the same now-wrong fact.

---

## 1. FALSE AT HEAD

### F-1 — `src/lib/sentry-capture.ts:24-28` — the capture chokepoint denies what 140.3 built

> ```
>  * ⚠️ THIS IS ADDITIVE INSTRUMENTATION, NOT A LEAK BEING PLUGGED. The seam
>  * captures nothing to Sentry at HEAD — a `captureException` / `captureMessage`
>  * grep across the resilience core, both seam clients and every seam route file
>  * returns ZERO. What exists is ten `captureToSentry` calls on the two
>  * key-bearing routes, and this is where they become safe.
> ```

Added by `55d07323 feat(140.2-08)`. Falsified in-range by `00aeddbc` / `b22a2047`
(140.3-13a/13b, SEAMUX-08).

Measured — `captureToSentry(` call sites in each of the 15 routes named by `SEAM_ROUTE_BUDGETS`:

```
1  keys/validate-and-encrypt      4  strategies/create-with-key     4  strategies/composite/add-key
1  bridge                         1  simulator                      1  portfolio-optimizer
2  scenario/optimize              1  admin/match/eval               1  admin/match/recompute
1  keys/sync                     10  strategies/finalize-wizard      4  verify-strategy
2  strategies/csv-validate        7  strategies/csv-finalize         1  keys/[id]/permissions
```

**41 calls across 15 of 15 seam routes** — not "ten … on the two key-bearing routes".
Only the narrow sub-clause survives: a direct `Sentry.captureException|captureMessage` grep
over the core and both clients still returns zero (everything goes through `captureToSentry`).

This is the security argument for why the scrubbing chokepoint is *additive* rather than a
leak fix. A maintainer sizing the blast radius of a `captureToSentry` change, or deciding
whether scrubbing is on the hot path, is told the seam sends nothing to a third party. It
now sends 41 payloads, several carrying raw exchange credentials via `secrets: [...]`.

Fix: state the post-SEAMUX-08 position — every seam route captures from its terminal /
contract-violation arm under the one policy in `admin/match/eval/route.ts`, and *that* is
why scrubbing must live here rather than at 41 call sites. The "ten sites each remembering
to scrub" line below it now understates its own argument.

---

### F-2 — `src/lib/wizardErrors.ts:1118-1130` — user-facing copy is withheld on a precondition that has since been satisfied

> ```
>   // 140.3-12 / SEAMUX-04 — the notification claim is GONE. It asserted an audit
>   // trail that does not exist: 9 of the 15 seam routes capture nothing at all,
>   // so on those paths the sentence promised a person was looking at something
>   // no one could see.
>   ...
>   // pointed the other way: the other 6 routes DO capture, and this code is
>   // reachable from them. ... If 140.3-13 adds the missing captures, the claim may come back
>   // — but only with the receipt, and only where it is true.
> ```

Same measurement as F-1: **15 of 15 capture; zero capture nothing.** Both numbers in the
paragraph (9 and 6) are wrong. And the comment names its own escape condition — *"If
140.3-13 adds the missing captures"* — which `00aeddbc` ("4 of the 9 zero-capture seam
routes capture") and `b22a2047` ("the OTHER five … same ONE policy") did, inside this range,
after the comment was written.

Worse than F-1 in one respect: this is the recorded justification for `UNKNOWN`'s copy
staying silent about notification. A maintainer revisiting that copy is told the receipt
does not exist when it now does, and will not re-open a decision whose stated precondition
has flipped.

---

### F-3 — `src/app/api/keys/[id]/permissions/route.ts:163-166` — "5 minutes" is 60 seconds, on a security-relevant staleness window

> ```
> /**
>  * Fetch the live permission triple from the Python service. Wrapped in
>  * unstable_cache so concurrent callers + repeat hits inside 5 minutes
>  * collapse to a single upstream request.
> ```

```
$ grep -n "revalidate" "src/app/api/keys/[id]/permissions/route.ts"
344:    { revalidate: 60, tags: [`key-permissions:${keyId}`] },
```

Contradicted by **six** sibling comments in the same file — `:45` *"This Next layer — 60
seconds via unstable_cache"*, `:212` *"caches for 60s while the breaker's cooldown is 30s"*,
`:320` *"an unvalidated scope verdict replayed for 60 seconds"*, plus `:97`, `:299`, `:396` —
and by an **in-range added** comment in another file:

> `src/lib/analytics-schemas.ts:341`: *"`keys/[id]/permissions` proxies the same body to `KeyPermissionBadge`, which renders it as a live security claim about a money-bearing key — and memoizes it for 60 seconds."*

The "5 minutes" sentence predates the range (its enclosing docblock was touched by
`4986584b`); it is reported because 140.2 added the contradicting sentence and did not
reconcile. 5× overstatement, in the unsafe direction, on how long a stale scope verdict for
a money-bearing key can be served. One in-file grep during 140.2 would have caught it.

---

### F-4 — `src/__tests__/contracts/contracts-registry.test.ts:151` and `REGISTRY.md:65` — the discriminator's export set is five, not three

> `contracts-registry.test.ts:151`: *"plus its exported surface pinned as a hand-typed SET of **exactly three predicates**"*
> `REGISTRY.md:65`: *"The set is exactly **three predicates**"*

Both added in range by `8012ea41` (140.2-06); grown in range by `16244027` (140.3-11,
`seamDependencyName`) and `c07b9c39` (140.3-15, `seamCorrelationId`).

```
$ grep -n "^export function" src/lib/seam-discriminator.ts
206: seamErrorCode   219: seamHumanMessage   269: seamDependencyName
321: seamCorrelationId   368: seamBreakerVerdict
```
`EXPECTED_EXPORTS` (`seam-discriminator.purity.test.ts:104-110`) lists all **five**, and
`seam-discriminator.ts:324` says *"it is the FIFTH member of this leaf's exported surface"*.

The guard is correct; only its two registry descriptions rotted — the dangerous direction,
because the registry is what a reviewer reads to decide whether a guard still covers its
class. It now understates coverage of the export surface that IS the T-140-01 control.

---

### F-5 — `src/__tests__/contracts/contracts-registry.test.ts:164` and `REGISTRY.md:69` — "the six seam files" is eight, and the guard says so loudly

> `contracts-registry.test.ts:164`: *"no console.error/warn in **the six seam files** passes a bare caught identifier…"*
> `REGISTRY.md:69`: *"**No `console.error` / `console.warn` in the six seam files** passes a raw caught error."*

The guard it registers contradicts it in its own header:

> `seam-log-coverage.test.ts:37`: *"**The eight seam files.** Hand-typed…"*
> `seam-log-coverage.test.ts:47`: *"⚠️ **EIGHT SINCE HI-02, AND THE LAST TWO WERE THE HOLE.** `create-with-key` and `composite/add-key` … neither was here NOR in `SEAM_EXCLUSIONS`, so this guard could not see them **while the REGISTRY entry claimed class closure**."*

`SEAM_FILES` (`seam-log-coverage.test.ts:55-64`) = 8 entries. The guard's own docblock names
"the REGISTRY entry claimed class closure" as the failure that let two credential-logging
routes go unscanned — and the registry entry still says six. A reviewer cross-checking scope
against the registry re-derives the exact wrong number the guard warns about.

---

### F-6 — `analytics-service/docs/STATUS_CONTRACT.md:245-247` — a "✅ CLOSED" note describing behaviour TS-34 already changed

> ```
> > ✅ **CLOSED 2026-07-27 by plan `140.3-01`** … The reply is still `PROBE_FAILED` / 502 and the
> > `Retry-After` is still discarded — answering **429 not 502** and forwarding the header
> > is **TS-34**, deliberately left to 140.3's response-shape plan, because two changes to
> > one block in one pass is TRAP-8.
> ```

Added in range by 140.3-01. TS-34 landed in the same range at
`4986584b fix(140.3-09): a /internal throttle answers 429 with the upstream wait forwarded (TS-34)`.

At HEAD, `src/app/api/keys/[id]/permissions/route.ts`:
```
471:      if (seamFailure?.status === 429) {
494:            status: 429,
500:                    "Retry-After": String(seamFailure.retryAfterSeconds),
```

This is the document every downstream phase is explicitly told to read instead of
re-deriving, and the note reads as a deliberate deferral. A maintainer would plan TS-34 work
that is done, or "fix" the 429 arm back to 502.

The rest of that section's in-range correction block checks out: `scenario/commit/route.ts`
imports no seam module; `errorSchema.ts:22` is `detail: z.string().optional()`; all three
real Class-5 sites read through `seamHumanMessage`/`seamErrorCode` at HEAD.

---

### F-7 — `src/lib/wizardErrors.ts:1535-1546` — "the complete set" is contradicted four lines later in its own docblock

> ```
>  * The same reasoning admits `process-key-client`'s two transport codes. Those
>  * three wire codes are the complete set of non-`WizardErrorCode` codes that
>  * `finalize-wizard` can put in front of a user today;
> ...
>  * (`draft_state_invalid` and `COMPOSITE_UNSUPPORTED_UNIFIED` also reach
>  * SubmitStep without a wizard member — both are deliberately out of scope here…)
> ```

Measured — non-comment `code: "…"` literals emitted by `finalize-wizard/route.ts`, tested
against the `WizardErrorCode` union:

```
CIRCUIT_OPEN                  @493    NOT a WizardErrorCode
draft_state_invalid           @1190   NOT a WizardErrorCode
COMPOSITE_UNSUPPORTED_UNIFIED @1551   NOT a WizardErrorCode
```

Three non-union codes leave that route; only one (`CIRCUIT_OPEN`) is among the "three wire
codes" the sentence covers. "Complete set" is the load-bearing phrase — it is what tells the
next maintainer that `SEAM_CODE_TO_WIZARD_CODE` needs no further members. It is false by the
docblock's own admission two sentences later. The scoping is defensible; the word "complete"
is not.

---

### F-8 — `src/lib/seam-copy.ts:50-53` — SEAMUX-03's written scope is wrong in both numbers

> ```
>  * OUT OF SCOPE, stated so it is not smuggled in later: the ten emitters wrap
>  * this string in five different envelope shapes and only two of them carry a
>  * `code`.
> ```

Measured over the ten `CIRCUIT_OPEN_COPY` / `CIRCUIT_OPEN_HUMAN_MESSAGE` emission sites:

| shape | n | sites |
|---|---|---|
| `{ error }` | 6 | bridge, match/eval, match/recompute, simulator, scenario/optimize, keys/validate-and-encrypt |
| `{ status, suggestions, error }` | 1 | portfolio-optimizer:195 |
| `{ error, code }` | 2 | finalize-wizard:493, keys/[id]/permissions:426 |
| `{ ok, code, human_message, correlation_id, recoverable }` | 1 | process-key-client:437-447 |

**Four** shapes, **three** carry a `code`. Already wrong when written — `git show 05b86a6f`
(the commit creating `seam-copy.ts`) shows `{ error: CIRCUIT_OPEN_COPY, code: "CIRCUIT_OPEN" }`
already at both route sites alongside process-key-client's coded envelope.

This paragraph *is* the written scope of requirement SEAMUX-03. The later plan told to
size off it starts from a wrong inventory of what it must unify.

---

### F-9 — `src/lib/wizardErrors.ts:143-147` — "at four sites", present tense, describing the pre-split tree

> ```
>   // ⚠️ THE SPLIT IS THE POINT, AND IT IS ONE ARM OF FOUR. `finalize-wizard`
>   // emits `COMPOSITE_MEMBERSHIP_UNKNOWN` at four sites. THREE are genuinely
>   // transient member-list reads … The FOURTH — the `members.length > MAX_COMPOSITE_MEMBERS` refusal —
> ```

```
$ grep -n 'code: "COMPOSITE_' src/app/api/strategies/finalize-wizard/route.ts
854:  COMPOSITE_MEMBERSHIP_UNKNOWN    909:  COMPOSITE_MEMBERSHIP_UNKNOWN
992:  COMPOSITE_TOO_MANY_MEMBERS      1529: COMPOSITE_MEMBERSHIP_UNKNOWN
```

Three sites at HEAD. The fourth now emits `COMPOSITE_TOO_MANY_MEMBERS` — which is precisely
the split this comment documents, written as if it had not happened yet. A maintainer
grepping for four finds three and concludes an emission site was lost. The past tense
("emitted … at four sites") fixes it entirely.

---

### F-10 — `src/lib/tenant-claim.ts:81-83` — "The three wrappers", then five are named

> ```
>  * The three wrappers that already carried an identity before this phase
>  * (`runPortfolioOptimizer`, `findReplacementCandidates`,
>  * `computePortfolioAnalytics`, plus `recomputeMatch` / `simulateAddCandidate`)
>  * keep their existing `actorId` / `userId` string parameters
> ```

All five exist in `analytics-client.ts` and all five take a bare identity string
(`computePortfolioAnalytics(… actorId: string)`, `runPortfolioOptimizer(portfolioId, actorId: string)`,
`findReplacementCandidates(… userId: string)`, `simulateAddCandidate(… userId: string)`,
`recomputeMatch(… actorId: string)`).

The count is the security argument: it defines the exempt set allowed to keep a bare
`string` beside no adjacent secret, against threat T-140.2-09-02. A reader taking "three"
literally treats two of the five as unaudited — and may either "fix" them or, worse, read
the exemption as narrower than what was actually reasoned about.

---

### F-11 — stale `exchange.py` line citations, broken by 140.3-06 inside this same range

`1f8ad052` (140.3-06) inserted ~21 lines of rationale at `exchange.py:160-188`. Seven
citations written by `3d9a766b` (140.2-06, also in range) now point at unrelated code:

| citation | claims | at HEAD | actual |
|---|---|---|---|
| `resilient-fetch.ts:357` — `exchange.py:314,324` | 503 `dependency="mt5-gateway"` in `_validate_mt5_key` | a RED-TEAM note on `Mt5Client.__init__` | 335, 345 |
| `resilient-fetch.ts:394` — `exchange.py:314,324` | same | same | 335, 345 |
| `resilient-fetch.ts:362` — `exchange.py:626` | 500 naming `kek` | `return {"valid": …}` | 647 |
| `resilient-fetch.ts:405` — `exchange.py:626` | same | same | 647 |
| `resilient-fetch.ts:363` — `exchange.py:276,287` | 500 naming `mt5-gateway` | — | 297, 308 |
| `resilient-fetch.ts:364` — `exchange.py:547` | 424 naming a venue | — | 566 |
| `seam-constants.pin.test.ts:142` — `exchange.py:314,324` | the only reachable 503-with-dependency set | — | 335, 345 |

```
$ grep -n 'dependency="mt5-gateway"\|dependency="kek"\|dependency="egress-proxy"' analytics-service/routers/exchange.py
132: egress-proxy   # still correct — above the insertion point
297: mt5-gateway    308: mt5-gateway    335: mt5-gateway    345: mt5-gateway    647: kek
```

The **semantic** claim survives — only a 503 counts, and the reachable set is still
mt5-gateway (exchange.py) + supabase (portfolio.py:682, match.py:1655/1689). The
non-`exchange.py` citations all still land correctly.

These comments are labelled **"The values are EVIDENCE"** and are the falsifier for ledger
row M39. An auditor follows the citation, lands on a comment about RPyC sockets, and cannot
tell whether the map is wrong or the pointer is. Prefer symbol references (`_validate_mt5_key`'s
`mt5-gateway` arms) over line numbers in a file this programme is actively editing.

---

### F-12 — `src/components/admin/match/venueOutageCopy.ts:34-37` — "neither admin route forwards a `Retry-After`"

> ```
>  * … **No wait is named**, deliberately: neither admin route forwards a `Retry-After`
>  * (`AnalyticsUpstreamError` carries no headers), so naming a duration would be
>  * the invented number TRAP-3 is about.
> ```

```
$ grep -n "Retry-After" src/app/api/admin/match/{recompute,eval}/route.ts
recompute:89:  { status: 429, headers: { …, "Retry-After": String(rl.retryAfter) } }
recompute:129:        "Retry-After": String(err.retryAfterS),
eval:178:             "Retry-After": String(err.retryAfterS),
```

Both routes forward `Retry-After` on the `CircuitOpenError` 503 arm; `recompute` also on its
own rate-limit 429. The parenthetical is the true, narrow claim — and both routes state that
narrow version themselves, which is what makes this a contradiction rather than an error:

> `admin/match/recompute/route.ts:160-162` (and `eval:207-209`, identical): *"The status only. No header rides along: `AnalyticsUpstreamError` carries none, so a forwarded upstream 429 reaches the client WITHOUT its `Retry-After`."*

The design conclusion is still right (the 424 arrives via `AnalyticsUpstreamError`), but a
maintainer wiring a countdown into the admin surface reads this as "the wait is never
available here" and could delete working `Retry-After` plumbing as dead code. Rescope to
"the 424 arm carries no header".

---

### F-13 — `src/components/admin/match/venueOutageCopy.ts:21-22` — `WizardErrorCode` is not "all key-connect and sync states"

> ```
>  *   `src/lib/wizardErrors.ts` — keyed on `WizardErrorCode`, whose members are
>  *      all key-connect and sync states.
> ```

**17** union members are `CSV_*` upload states (`CSV_PARSE_FAILED`, `CSV_SCHEMA_VIOLATION`,
`CSV_NAV_ZERO`, `CSV_SHARPE_SUSPICIOUS`, …), plus metadata/lifecycle members
`METADATA_DESCRIPTION_REQUIRED`, `SESSION_EXPIRED`, `SUBMIT_NOTIFY_FAILED`,
`DRAFT_ALREADY_EXISTS`, `GUARD_BLOCKED`.

`ConnectKeyStep.tsx:195` depends on the opposite: *"`CSV_NAV_ZERO` arriving here is drift,
not a key-connect failure, and reading it as one would render CSV copy on the exchange form."*

Low blast radius — the placement decision for `venueOutageCopy.ts` is right regardless — but
it is the *stated reason*, and anyone re-litigating that placement reasons from a false
picture of the table.

---

### F-14 — `src/lib/seam-discriminator.ts:41-43` — the header says a 424's `dependency` is never read here; the file exports a reader for it

> ```
>  * ⚠️ A BREAKER KEY MAY ONLY COME FROM THE CLOSED SET BELOW (threat T-140-01).
>  * … A `424`'s `dependency` is the CALLER'S
>  * VENUE (§4) and is deliberately never read here; …
> ```

Header by `0ad6d3fb` (140.2-06). `seamDependencyName` — whose entire job is reading a 424's
`dependency` — added 200 lines below by `16244027` (140.3-11):
```
269:export function seamDependencyName(body: unknown): string | null {
273:  const dependency = detail.dependency;
```

The paragraph is scoped to breaker keys and defensible in that narrow reading, but "never
read **here**" means this module, and the module now reads it. The correct distinction
(`serviceDependencyOf` = may this become a KEY; `seamDependencyName` = may this be SHOWN) is
already written out at `:236-246` — the header never learned about it. A maintainer trusting
the header deletes `seamDependencyName` as a T-140-01 violation. One clause fixes it:
"never read *for a breaker key*".

---

### F-15 — `src/components/strategy/ApiKeyManager.tsx:73-74` — the status enumeration omits two live arms

> ```
>  *     envelopes from the same route and BOTH are live. The route's own arms emit
>  *     `{ error }` (400 / 404 / 429 / the composite-probe 503); every envelope
>  *     `postProcessKey` builds emits `{ code, human_message }` …
> ```

`keys/sync/route.ts` route-own `{ error }` arms at HEAD:
```
72,84  400  MISSING_STRATEGY_ID / INVALID_STRATEGY_ID
118,128 429 RATE_LIMITED
166    500  DRAFT_LOOKUP_FAILED          ← omitted
189    404  GATE_DRAFT_GONE
229    503  COMPOSITE_MEMBERSHIP_UNKNOWN
297    503  SYNC_KICKOFF_FAILED          ← omitted
```

A maintainer auditing which statuses need the `error` reader (vs. the `human_message` reader)
would conclude a 500 falls to the `postProcessKey` envelope branch. It does not — it is a
route-own `{ error }` body, and `syncFailureMessage` is the only thing that renders it.

---

### F-16 — `src/app/api/admin/match/recompute/route.ts:16` — "all nine seam routes"

> ```
> // That docblock is the source of truth for all nine seam routes and is
> // deliberately NOT duplicated here: two copies of a policy is how two policies start.
> ```

There are **fifteen** seam routes (`SEAM_ROUTE_BUDGETS`), all fifteen capture, and nine cite
this policy. The intended referent is "the nine that 140.3-13a/b converted" — the source
docblock is headed *"ONE RULE, ALL NINE ROUTES"* and opens *"Nine of the fifteen seam
routes captured NOTHING"* — but "all nine seam routes" reads as a population count. Six
routes' captures (finalize-wizard's ten, csv-finalize's seven, keys/sync, bridge, simulator,
validate-and-encrypt) sit outside the policy, and a maintainer adding a capture there would
believe the docblock covers them. "…for the nine routes it converted" removes the ambiguity.

---

### F-17 — `src/components/admin/AllocatorMatchQueue.tsx:248` — "twenty lines below" is fifty-five

> `// The shape is `handleDecision`'s, twenty lines below in this same file:`

`handleDecision` is at `:303` — 55 lines. It was already 35 when the comment was written
(`20fd9619`: comment at 247, target at 282). Minor: a reader scrolling ~20 lines lands
mid-`load()` and may conclude the reference implementation was deleted.

---

## 2. TRUE BUT UNFALSIFIABLE — destined to rot

### R-1 — "Sixteen route test files replace the seam clients wholesale" — now in six places, matches no measurement

Propagated in range into three new production leaves and their three purity tests:
`seam-copy.ts:18`, `seam-discriminator.ts:13`, `seam-copy.purity.test.ts:28`,
`seam-discriminator.purity.test.ts:31`, `seam-errors.purity.test.ts:29` (plus the
out-of-range `seam-errors.ts:22`, which additionally says *"and **seven of eight** use a FULL
factory"* — internally inconsistent with "sixteen" in the same sentence).

```
$ grep -rlE 'vi\.mock\(\s*["'"'"']@/lib/(analytics-client|process-key-client|resilient-fetch)["'"'"']' src/app/api | wc -l
15
$ … same over src + tests | wc -l
18        # 15 under src/app/api + 3 route tests under src/__tests__
$ … over src/app/api at 4b59fb31^ | wc -l
15
```
No reading gives 16, at HEAD or at the base. The **rationale** is correct and load-bearing;
only the integer is decoration. The repo already has the idiom to derive it — both
`seam-ssr-exposure.pin.test.ts` and `seam-copy.pin.test.ts` walk `src/` and scan. Simplest
fix is to drop the number, exactly as `2d58fd45 docs(140.3-G2)` already did for five other
prose counts.

### R-2 — `src/lib/seam-ssr-exposure.pin.test.ts:101` — "Fourteen files … name these modules in prose"

Measured with the file's own `stripComments` + `SEAM_IMPORT_EDGE` logic: **15**. New file,
in range, one out on the day it shipped. The point (a substring grep reports them all as
violations, and the natural "fix" is to weaken the guard) is right.

### R-3 — `src/lib/seam-copy.ts:40` and `seam-copy.pin.test.ts:29` — "The twelve route test files"

Correct at HEAD: exactly 12 files under `src/app/api/**` carry the hand-typed breaker
sentence (19 repo-wide, minus 5 component/lib tests, minus the leaf and its pin). Flagged
only because it is asserted twice in prose while `EXPECTED_LEAF_CONSUMERS = 10` — the *other*
half of the same census — is a pinned literal with a scan behind it. Nothing reddens when a
thirteenth appears.

### R-4 — "~20 files in the MAIN suite DELETE them" (Upstash env), stated in two new files

`vitest.redis.config.ts:12` and `.github/workflows/ci.yml` (frontend-seam-redis env block).
```
$ grep -rl "delete process.env.UPSTASH_REDIS_REST" src tests | wc -l   →  6
$ grep -rl "UPSTASH_REDIS_REST_URL" src tests --include='*.ts*' | wc -l →  14
```
Six under the literal reading, fourteen under the loosest. Hedged with "roughly"/"~", so low
severity — but it is the stated justification for a separate CI job *and* a separate vitest
project, and it overstates by up to 3×. The argument survives at any n ≥ 1.

### R-5 — `vitest.redis.config.ts:36` — stale cross-file line reference in a brand-new file

> *"…a documented hazard on this seam (**analytics-client.ts:148-155**)."*

`analytics-client.ts:145-156` at HEAD is the null-body-status (204/205/304) docblock; the
jsdom `DOMException` hazard now lives at `analytics-client.ts:391`. That file gained 432
lines in this same range, so the reference was stale on arrival.

### R-6 — `STATUS_CONTRACT.md` §2.1 warns against line numbers, then keeps six of them

The in-range correction block adds: *"**The three Class-5 sites, located by code text**
(line numbers are deliberately omitted — two of these three files moved during 140.2)"*.
Fifty lines below, the untouched PYAPIFIX2-03 paragraph still carries `route.ts:147`,
`route.ts:254-268`, `route.ts:275`, `internal.py:246`. `internal.py:246` is still right;
**`route.ts:147`** is now a docblock, not the `err.detail ?? …` read it is cited as. The
correction's own discipline was not applied three inches away, which makes the document read
more audited than it is.

---

## 3. GREP HAZARDS — checked, and clean

Checked specifically because seven executors in this programme were bitten by their own
comments matching their own acceptance greps. **No live instance found.** Both guards at
risk handle it deliberately and say why:

- `seam-constants.pin.test.ts:186-190` — *"Declared once, so the string `SEAM_BUDGETS` followed by a subscript never appears in this file — the acceptance grep for the self-referential shape must stay clean, comments included."*
  Verified: `grep -c "SEAM_BUDGETS\[" src/lib/seam-constants.pin.test.ts` → `0`.
- `seam-ssr-exposure.pin.test.ts:75-79` — the forbidden directive is assembled at runtime
  (`` const DIRECTIVE_BODY = `${"use"} ${"server"}` ``) so the guard never plants the string
  it bans; the registry row states the reason out loud.
- `seam-copy.pin.test.ts:73-83` and `seam-ssr-exposure.pin.test.ts` both comment-strip before
  matching, each with the rationale written down (*"Every one of the ten emitters carries a
  docblock discussing this copy … a bare `grep -c` is self-invalidating here"*).

---

## 4. POSITIVE FINDINGS — the house style to point at when fixing §1

- **`src/lib/seam-discriminator.ts:400-414`** — the previously-false 400 claim is not silently
  deleted but *quoted, dated to the commit that broke it, and corrected*, with the reason it
  was routed here rather than fixed in place. The best comment in the diff.
- **`src/lib/resilient-fetch.ts:1745-1751`** — *"⚠️ TWO CORRECTIONS HAVE NOW LANDED ON THIS ONE COMMENT, so read the next edit carefully"*, then both wrong versions quoted verbatim. A comment that records its own error history gets more trustworthy with age.
- **`src/app/api/verify-strategy/route.ts:368-374`** — the "loud in logs/Sentry" promise made
  true rather than deleted, naming which half was false and how it was measured (*"`grep -c captureToSentry` on this file read 0"*).
- **`src/lib/seam-poll-disjointness.pin.test.ts:121-125`** — *"⚠️ THIS REPLACED A HAND-TYPED `0`. The old constant asserted that the poll path made zero network calls. It makes three. A hand-typed literal that is not the truth is worse than no literal, because it reads as a pinned fact and trains the next reader to believe the scan beneath it."*
- **`src/app/api/admin/match/eval/route.ts:57-115`** — the capture policy written once, each
  exclusion carrying its own falsifiable reason, cited (never restated) from
  `admin/match/recompute:16-18` and `scenario/optimize:16-18` on the explicit ground that
  *"two copies of a policy is how two policies start."* Verified: the siblings cite, they do
  not duplicate.

**The pattern worth institutionalising:** every one of these five is a comment that names
the measurement behind it. Every finding in §1 is a comment that names a *number* without
one. That is the whole difference.

---

## 5. Verified-correct claims (measured; recorded so nobody re-spends it)

| claim | site | measurement |
|---|---|---|
| "all seven venue sites answer 424" | `error_contract.py:425`, `exchange.py:165` | 7 `raise VenueTransientHTTPException`, all `status_code=424` |
| "Nine routes bind it as `CIRCUIT_OPEN_COPY`" / "all ten seam emitters" | `seam-copy.ts:60`, `eval:46` | 9 route importers + `process-key-client` |
| "no second declaration anywhere in `src/`" | `seam-copy.ts:63` | only `seam-copy.ts` among production files |
| "Every one of the nine wrappers" / "eight of nine" | `analytics-client.ts:259, :317` | 9 `analyticsRequest(` call sites |
| "`/api/match/recompute` and `/api/match/eval` have NO Python limiter at all" | `analytics-client.ts:310` | `match.py` carries zero `@limiter.limit` |
| "the six rekeyed Python routes" | `process-key-client.ts:223` | 9 wrappers − 3 inert (recompute, eval, simulator) |
| "at all five caller routes" / "SERVER-DERIVED at all five callers" | `process-key-client.ts:374`, `resilient-fetch.ts:1741` | 5 `postProcessKey` callers, each `user.id` or literal `"public"` |
| "the 5 lexical call sites" / "13 budget-key bindings" | registry, `resilient-fetch.wiring.test.ts` | 5 `resilientFetch(` sites; roster pinned by sorted-set equality |
| "a hand-typed 15-row map" / "the 3-path `SEAM_EXCLUSIONS` set" | registry | 15 keys, 3 exclusions |
| "the FIFTH member of this leaf's exported surface" | `seam-discriminator.ts:324` | 5 exports, pinned by `EXPECTED_EXPORTS` |
| "the only two consumers that render it" | `venueOutageCopy.ts:27` | `AllocatorMatchQueue`, `MatchEvalDashboard` |
| "the other eight rules' fixture files" | `contracts-registry.test.ts:163` | 9 plugin rules, 9 fixtures, 1 registered |
| image digests "MUST stay identical to docker-compose.redis-test.yml" | `ci.yml` | both sha256 digests byte-identical |
| "the shortest preserve token (9 characters)" / floor 12 | `seam-redaction.ts:118` | `ENOTFOUND`/`ETIMEDOUT`/`EAI_AGAIN` = 9; floor = 12 |
| the double "INCREMENTS ON DENIAL" | `ci.yml` frontend-seam-redis | `fakeRatelimitFor` increments before computing `success` |
| "every one of the emitter's six return arms sets it" (`detected_at`) | `analytics-schemas.ts:381` | 6 return arms in `internal.py` (363,379,401,415,427,508) |
| "Three client components shared one shape" | `route-response-error.ts:135` | KeyPermissionBadge, ReplacementPanel, PortfolioOptimizer |
| "seven different facts" / "a machine code on EVERY arm" | `keys/sync:5-12` | 7 distinct codes, every non-2xx arm coded |
| "the same four arms" (`[api_key, apiSecretNormalized, passphraseOrNull]`) | `create-with-key:21`, `composite/add-key:22` | 4/4 in each file |
| "the cascade's six substrings" / "the only capture in this route" | `keys/[id]/permissions:450, :556` | 6; 1 |
| "this handler has zero callers" | `validate-and-encrypt:189` | `_unifiedValidateAndEncryptHandler` → 0 callers |
| "the ONE place `/process-key` reads it from" (`wizard_session_id`) | `finalize-wizard:1500` | every read in `process_key.py` is `body.context.get(...)` |
| "the route has exactly one 2xx exit" | `VerificationForm.tsx:121` | 11 `NextResponse.json` exits, exactly one status-less |
| "emits exactly four codes" / "never calls `classifyKeyValidationError`" | `MultiKeyConnectStep.tsx:212` | 4 distinct codes; `grep -c` = 0 |
| "`ErrorEnvelope` computes `showRetry = envelope.recoverable && Boolean(onRetry)`" | `SyncPreviewStep.tsx:1292` | exact at `ErrorEnvelope.tsx:108` |
| "the ONE parser, which never emits 0/negative" | `ErrorEnvelope.tsx:118` | `parseRetryAfterSeconds` returns `null` for `<= 0` on both branches |
| "`ErrorResponseSchema` has exactly one production consumer" | `PortfolioImpactPanel.tsx:89` | only non-test importer |
| "`/api/simulator` publishes `Retry-After` on its 503 arms too" | `PortfolioImpactPanel.tsx:149` | `simulator/route.ts:117-118`, `:188-192` |
| "the two paths … `void handleDeleteDraft()`" | `WizardClient.tsx:723-726` | exactly 2 call sites (`:876`, `:1188`) |

---

## 6. Not verified — stated plainly rather than implied

- *"four of the seven properties this phase depends on are unobservable through the double"*
  (`ci.yml`, frontend-seam-redis) — the seven properties were not enumerated.
- *"Ten production files declared this sentence independently and none of them exported it"*
  (`seam-copy.ts:34`) — historical claim about the pre-140.3-04 tree; not reconstructed.
- *"FIVE of this route's six real thrown messages fall to `{code:"UNKNOWN", status:500}`"*
  (`keys/[id]/permissions/route.ts:516`) — requires replaying `classifyKeyValidationError`
  over an unenumerated message set.
- *"Phase 140.1.2 made all seven venue-transient sites emit a stable `code`"*
  (`wizardErrors.ts:1288`) — the wizard-side table has six entries; the seven Python emission
  sites' `code` values were not individually enumerated.
- Several "an acceptance grep counts that exact text in this file" claims
  (`WeightOptimizerSection.tsx:89`, `SyncPreviewStep.tsx:612-616`, `ConnectKeyStep.tsx:181`) —
  the greps live in plan artifacts, not in the repo, so neither their existence nor their
  range is confirmable from the tree.
- Every "this used to read X" / "before this plan Y" historical claim was taken at face value
  unless its present-tense half was checkable.

---

## Suggested order of work

1. **F-1, F-2, F-16** — one edit each; all three assert the same now-wrong fact about seam
   Sentry coverage. Fixing one and not the others is this programme's signature failure.
2. **F-3** — one-word fix, but it is a security claim about a money-bearing key and it is 5×
   in the unsafe direction.
3. **F-4, F-5** — the contracts registry is what a reviewer trusts to know what is guarded;
   both entries currently understate their own guards. Add a `REGISTRY.md` ↔ guard-header
   consistency note to whatever closes them, because this is the second time the registry has
   drifted from a guard that grew.
4. **F-6** — `STATUS_CONTRACT.md` is the cross-language contract; a stale "deferred" note
   there costs a future phase real planning time.
5. **F-7 … F-17** — mechanical.
6. **R-1** — delete the number in all six places rather than correcting it to 15/18. There is
   no reading under which 16 is right, and the sentence is stronger without an integer.
