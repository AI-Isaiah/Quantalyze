# CHECKPOINT — 2026-07-30 — v1.16 phase-140 close + ship

Fresh state capture (STATE.md is stale — dated 2026-07-27, predates 140.4/140.5 execution).
HEAD: `2c7bc32c` on `feat/v1.16-production-resilience`. Tree: `M TODOS.md`, `?? nautilus_factsheet.py` + this checkpoint. Suite green (~10,205 vitest / 0 fail, pytest ~4817).

## Where execution actually stands
- **140.4** (seamrim): 14/14 plans executed. Verifier ran (`140.4-VERIFICATION.md`, gaps_found 39/43, 17/17 mutations RED). Code review ran (`140.4-REVIEW.md`, 2 CR + 11 WR). **⚠️ Both artifacts are STALE** — a `140.4-16` fix wave (16 commits: CR-01a/b/c, CR-02, WR-01..11) landed AFTER them and closed the findings. Finding 1 (SEAM_MISCONFIGURED reachability) also fixed (recogniseSeamErrorCode hop now at ConnectKeyStep:496 + MultiKeyConnectStep). **CR-01 durable client-mint** added this session (`453b4291`, 422 tests green) — the last missing piece. **140.4 is effectively closed; ledgers just not re-verified.**
- **140.5** (seamprose): 7/8 plans executed. **140.5-08 NOT run** (dispatched as a background executor at prior session's tail; session resumed before it landed; worktree agent-a660f887 sits at HEAD = nothing merged). **140.5-06 partial** (task 2 = 1 of 5 arrival routes; executor honestly declined to fake breadth). No verifier/review on 140.5 yet.

## The ONE still-open bar-clearing defect
- **`src/app/api/keys/[id]/permissions/route.ts:542`** — `isConfigError = … || rawMessage.includes("not configured")` prose-greps the upstream body → a reached-and-answered KEK/venue fault containing "not configured" is misclassified as OUR-layer PROBE_BACKEND_UNAVAILABLE. User-facing misattribution (the milestone's signature defect). Surfaced+pinned by 140.5-06, deferred "to the phase decision". Fix: scope config-detection to OUR own thrown sentinel, not a substring on arbitrary upstream prose. Tests at route.test.ts:708/764 currently pin the wrong behavior.

## Blast-radius bar (founder, 2026-07-29): reviews block ONLY on user-facing OR data-integrity
Everything else → TODOS.md, never blocking. Already-fixed CR-01 (data-integrity: CSV double-submit merged a different submission into an existing strategy → fabricated verified track record). Log to TODOS: Finding 2 (stale "no guard" claim in VALIDATION.md), Finding 3 (stale line citations = 140.5's own remit), Finding 4 (eslint backstop 1-of-2), 11 WARNING review items, §Voice prose, 140.5-06's 4 remaining arrival routes.

## Ship state — NOTHING shipped incl phase 140
- Branch is **391 commits** ahead of origin/main; origin/main is **+1 ahead** (diverged — merge it first).
- Diff = 634K lines but splits: **72K code (268 files)** vs **562K .planning (2200 files)**. `.planning` is ALREADY TRACKED on origin/main, so it just rides along — not a separate concern.
- **DECISION (user, 2026-07-30): per-phase stacked PRs, code-focused.** Merge origin/main → ship phases in order (140 core → 140.1/.1.1/.1.2 → 140.2 → 140.3 → 140.4/-16 → 140.5), each landed before the next, review scope = the code. Sequencing is load-bearing (STATE.md:182): breaker(140) before retry(141), SEAM before JOB.

## Agreed next steps (trimmed close, then ship)
1. Finish 140.5 execution: land **140.5-08** (registry-roster guard; whole-file matching is a hard req — DEF-16-2 bit 3× via prettier-wrapped skips), decide 140.5-06 remainder (likely land guard + log breadth).
2. Fix **route.542** (the open bar-clearing defect), TDD.
3. Verify 140.5 (one gsd-verifier); light re-verify 140.4 for a clean ledger post-140.4-16.
4. Log the non-blocking items to TODOS.md.
5. C-4/TRAP-4 browser pass (destructive control) — per memory a browser pass beats 10k tests.
6. Ship per-phase (merge origin/main first).

## Founder-owed (STATE.md, none done — auto-approve ≠ confirmation)
Copy vs DESIGN.md §Voice (~12 sentences) · TRAP-4 five clicks in a REAL browser (C-4) · Sentry ingestion in a preview deploy (⚠️tripping the breaker captures NOTHING — force a terminal-arm failure).

## Open task IDs (this session): #16 CR-01 (DONE, 453b4291) · #17 CR-02 (already fixed by 140.4-16, close) · #18 route.542 (OPEN) · #19 Finding 1 (already fixed, close) · #20 land 140.5-08 + verify + log + browser.
