---
phase: 131-smoothed-mtm-options-daily-mtm
plan: 01b
type: execute
wave: 2
depends_on: [131-01a]
files_modified:
  - analytics-service/services/deribit_txn.py
  - analytics-service/services/deribit_ingest.py
  - analytics-service/services/allocated_capital.py
  - analytics-service/tests/test_deribit_txn.py
  - analytics-service/tests/test_deribit_ingest.py
  - analytics-service/tests/test_smoothed_mtm_core.py
  - analytics-service/tests/test_native_nav_sc4_identity.py
autonomous: true
requirements: [SMTM-01, SMTM-02]
must_haves:
  truths:
    - "A Deribit options fixture computed under pnl_basis='smoothed_mtm' yields a daily series whose total equals the cash_settlement total on a flat terminal book (redistribution preserves the sum)"
    - "cash_settlement and mark_to_market outputs are BYTE-IDENTICAL pre/post on every fixture, including options fixtures (SC-4 extended: the new code is a THIRD branch, not a rewrite)"
    - "A perp-only or USD-native ledger under smoothed_mtm makes ZERO option-mark fetches (asserted on the exchange stub) and is byte-identical to cash_settlement"
    - "A missing daily mark inside a listed instrument's life fails loud AT LEDGER BUILD via the 131-01a hole guard (LedgerValuationError naming instrument+day) — no interpolation, no session-lump fallback"
  artifacts:
    - path: "analytics-service/services/deribit_ingest.py"
      provides: "smoothed-gated ΔMTM merge in build_deribit_native_ledger + pre_mark_retention_option_days bucket + native_options_session_upl state read"
      contains: "pre_mark_retention_option_days"
    - path: "analytics-service/services/deribit_txn.py"
      provides: "PNL_BASIS_SMOOTHED_MTM, use_smoothed branch, smoothed identity channels"
      contains: "PNL_BASIS_SMOOTHED_MTM"
    - path: "analytics-service/services/allocated_capital.py"
      provides: "smoothed_mtm member in _VALID_PNL_BASES (config-layer enum sync)"
      contains: "smoothed_mtm"
  key_links:
    - from: "analytics-service/services/allocated_capital.py"
      to: "analytics-service/services/deribit_txn.py"
      via: "both basis enums carry smoothed_mtm (config/compute agreement)"
      pattern: "smoothed_mtm"
    - from: "analytics-service/services/deribit_ingest.py"
      to: "analytics-service/services/deribit_txn.py"
      via: "build_deribit_native_ledger merges option_mtm_daily output into txn_rows_to_native_daily output (smoothed-gated)"
      pattern: "option_mtm_daily"
---

<objective>
Plan 01b of the split 131-01 (seam at the original Task-2/Task-3 boundary, per plan-check
MEDIUM-1) — depends on 131-01a, whose dormant pure core this plan wires live: both basis
enum members + the `use_smoothed` branch in `txn_rows_to_native_daily` + the adapter ΔMTM
merge in `build_deribit_native_ledger` + the smoothed-only identity channels in
`assert_balance_identity` + the SC-4 byte-identity pins and total-preservation acceptance.
ADDITIVE — the existing `cash_settlement` and `mark_to_market` arms stay byte-identical
(SC-4). All new logic is gated on `use_smoothed = pnl_basis == PNL_BASIS_SMOOTHED_MTM` and
classification-gated on option rows.

Purpose: options books currently lump per-SESSION deltas (weeks of accrual) onto single
settlement days (live evidence: 94%/day, +3305%/month, total correct). `smoothed_mtm` spreads
each lump across the days it accrued via `Book[d]−Book[d−1]`, total-preserving (telescoping:
Σ native_pnl = Σchange + Book(last settlement)).

Output: both enum members + the smoothed branch + adapter merge + extended
`assert_balance_identity` — all proven by failing-first tests, no worker/frontend wiring
(that is 131-02/131-03).

CRITICAL REFRAME vs the archived 83-PLAN: 83 REPLACED the native option attribution
(deleted the coverage-gated fee-only reclass, reworked the identity guard unconditionally).
Here NOTHING existing is deleted or reworked unconditionally — the 83 Tasks 4–6 semantics live
ONLY inside the new `use_smoothed` branch / smoothed-only guard channels. Any diff hunk that
changes behavior on the `cash_settlement` or `mark_to_market` path is a defect.

Task numbering continues from 131-01a (Tasks 0–2 live there); the anchors and
cross-references below are preserved verbatim from the pre-split 131-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-smoothed-mtm-options-daily-mtm/131-CONTEXT.md
@.planning/phases/131-smoothed-mtm-options-daily-mtm/131-RESEARCH.md
@.planning/phases/131-smoothed-mtm-options-daily-mtm/131-PATTERNS.md
@.planning/phases/131-smoothed-mtm-options-daily-mtm/131-01a-SUMMARY.md
@.planning/milestones/post-v1.8-standalone-phases/83-daily-option-mtm/83-PLAN.md

Read-first source anchors (all under analytics-service/, line numbers per 131-PATTERNS.md,
re-verify with grep before editing):
- services/deribit_ingest.py:597-691 (`fetch_deribit_perp_daily_index` — 01a cloned it),
  :1759-1836 (`build_deribit_native_ledger`), :723 (`CompletenessReport`), :787/:1820
  (`deribit_raw_rows_have_option_activity`), :1611 (`_build_dense_native_marks`)
- services/deribit_txn.py:144 (`classify_instrument` option arm), :816
  (`_NATIVE_OPTIONS_SUMMARY_TYPES`), :848-853 (basis constants + `_PNL_BASES` +
  `DEFAULT_PNL_BASIS`), :1384 (`_summary_coverage_windows`), :1506/:1652
  (`assert_balance_identity` + branch), :1691 (`_summary_contribution`), :1709
  (`_option_commission`), :1728 (`txn_rows_to_native_daily`), :1798 (`use_mtm`),
  :1847-1870 (summary handling), :1926-1932 (option trade/delivery `-commission` arm)
- services/allocated_capital.py:56 (`_VALID_PNL_BASES`), :158/:230 (validation sites)
- tests/test_deribit_txn.py:980 (AST purity guard), tests/test_cash_basis_series_sc4.py,
  tests/test_native_nav_sc4_identity.py (`_real_adapter_ledger` stubbed-exchange pattern)
</context>

<interfaces>
From 131-01a (consume, do not re-derive — read 131-01a-SUMMARY.md for final signatures):
- `fetch_deribit_option_daily_marks(exchange, instrument, *, oldest_day, newest_day, sleep, max_retries)`
  → dict[utc_day, close]; transient exhaustion → DeribitTransientReadError; structural
  no-data → {} (the caller — Task 4 here — owns the fail-loud/pre-retention decision)
- `replay_option_positions(rows)` → per-instrument
  {currency, first_day, last_day, positions: {day: signed_size}}
- `option_mtm_daily(positions, marks)` → (delta_mtm: dict[ccy, dict[day, float]],
  terminal_book: dict[ccy, float]); raises LedgerValuationError on any hole inside a listed
  life — including retention-STRADDLING instruments (partial marks), pinned in 01a Task 2
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3: smoothed_mtm in BOTH enums + the use_smoothed branch in txn_rows_to_native_daily</name>
  <files>analytics-service/services/deribit_txn.py, analytics-service/services/allocated_capital.py, analytics-service/tests/test_deribit_txn.py, analytics-service/tests/test_smoothed_mtm_core.py</files>
  <behavior>
    - Enum sync pin (new test): PNL_BASIS_SMOOTHED_MTM == "smoothed_mtm" is a member of BOTH
      deribit_txn._PNL_BASES AND allocated_capital._VALID_PNL_BASES; DEFAULT_PNL_BASIS
      UNCHANGED ("cash_settlement"); allocated_capital validation sites (:158, :230) accept
      "smoothed_mtm" and still reject unknowns
    - Under use_smoothed: option trade/delivery rows contribute FULL native `change`
      (the 2025-07-13-class fixture contributes its premium cash on its day — the
      redistribution happens in the adapter-merged ΔMTM channel, Task 4)
    - Under use_smoothed: options_settlement_summary contributes NOTHING (summary row with
      realized_pl=0.03, unrealized_pl=-0.01 creates NO entry) but the change==0 enforcement
      on summary rows still fires (nonzero summary change → fail loud, kept verbatim)
    - SC-4 BYTE-IDENTITY: an OPTIONS fixture run under cash_settlement and under
      mark_to_market produces output byte-identical to pre-change (pin by asserting the new
      branch is never entered — e.g. golden dict equality against the pre-change expected
      values already encoded in existing tests, which must pass UNMODIFIED)
    - Existing test_perp_only_ledger_byte_identical-class pins stay green UNMODIFIED
  </behavior>
  <action>
    Failing tests FIRST (enum-sync pin in the NEW tests/test_smoothed_mtm_core.py; branch
    behavior in test_deribit_txn.py). Then:
    (a) deribit_txn.py:848-853 — add `PNL_BASIS_SMOOTHED_MTM = "smoothed_mtm"` and add it to
    `_PNL_BASES`. DEFAULT_PNL_BASIS untouched.
    (b) allocated_capital.py:56 — add "smoothed_mtm" to `_VALID_PNL_BASES`. The functions
    THREAD the basis string without switching on it (131-PATTERNS), so this member unblocks
    the config layer only — verify the validation sites :158/:230 need no other change.
    (c) txn_rows_to_native_daily (:1728) — add
    `use_smoothed = pnl_basis == PNL_BASIS_SMOOTHED_MTM` next to the existing `use_mtm`
    (:1798). In the aggregation loop: when use_smoothed, option trade/delivery rows take the
    FULL `change` path (bypass the coverage-gated −commission arm at :1926-1932 for THIS
    basis only — the arm itself stays byte-identical for the other bases) and summary rows
    (:1847-1870) enforce change==0 then contribute nothing. The signature of
    txn_rows_to_native_daily is UNCHANGED (no marks parameter — the ΔMTM merge lives in the
    adapter, Task 4, preserving AST purity and the 83-PLAN Q2 split). Every hunk MUST be
    inside an `if use_smoothed` arm or a new helper only that arm calls — per 131-CONTEXT
    the two existing bases are byte-untouched (SC-4). Do NOT delete the Phase-82 fee-only
    reclass, `_summary_contribution`, or any Phase-82 test — the 83-PLAN Task-4 deletions are
    explicitly NOT reused under the third-basis framing.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_smoothed_mtm_core.py tests/test_deribit_txn.py -x -q && python -m mypy --strict --follow-imports=silent services/deribit_txn.py services/allocated_capital.py</automated>
  </verify>
  <done>Enum-sync pin green; smoothed branch behavior green from failing-first; ALL pre-existing deribit_txn tests green UNMODIFIED; mypy --strict 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Adapter ΔMTM merge in build_deribit_native_ledger (smoothed-gated) + pre-retention bucket</name>
  <files>analytics-service/services/deribit_ingest.py, analytics-service/tests/test_deribit_ingest.py, analytics-service/tests/test_smoothed_mtm_core.py</files>
  <behavior>
    - Options fixture under pnl_basis="smoothed_mtm" (stubbed-exchange real-adapter pattern
      from test_native_nav_sc4_identity._real_adapter_ledger): ledger native_pnl == cash
      channel + hand-computed ΔMTM per (day, ccy) — and WIRING-GUARD: neutering the merge
      (monkeypatch option_mtm_daily to return empties) reddens the test
    - One marks request per held instrument, expiry-capped bounds (never past expiry),
      instrument name verbatim
    - Same options fixture under "cash_settlement" and "mark_to_market": marks fetcher NEVER
      called (assert call count == 0 on the stub) and ledger output equals the pre-change
      golden — the merge is DOUBLY gated (basis AND option classification)
    - Perp-only fixture under "smoothed_mtm": fetcher never called, ledger byte-identical to
      its cash_settlement output (empty replay ⇒ no-op merge ⇒ identical float ops)
    - Pre-retention fixture (instrument whose whole life predates the ~2.5yr retention →
      wholly-empty marks response): instrument contributes NO MTM, its days stay cash-basis,
      and CompletenessReport gains pre_mark_retention_option_days day-buckets (the
      complete_with_warnings channel — worker stamping is 131-02's job)
    - In-retention wholly-empty OR holed marks: the 131-01a Task-2 fail-loud fires at ledger
      build (LedgerValuationError naming instrument+day)
    - STRADDLE vs PRE-RETENTION partition (plan-check MEDIUM-3 clarifier): the partition
      keys on WHOLLY-empty marks AND expiry older than the horizon. An instrument whose life
      STRADDLES the horizon returns PARTIAL marks → it is NOT bucketed, NOT excluded — its
      head holes fall through to the 01a hole guard and fail the ledger build loud (the
      pinned D-07 consequence; founder-visible trade-off surfaced in 131-02)
  </behavior>
  <action>
    Failing tests FIRST. In build_deribit_native_ledger (:1759, threads pnl_basis at :1808),
    add a branch entered ONLY when pnl_basis == PNL_BASIS_SMOOTHED_MTM AND
    deribit_raw_rows_have_option_activity (:787) is true:
    (a) replay_option_positions(raw_rows);
    (b) per held instrument, await fetch_deribit_option_daily_marks over
    [first_held_day, min(last_held_day, expiry_day)] — expiry parsed from the instrument name
    via the existing classification machinery, never guessed; existing public-read pacing;
    one request per instrument; per-job memoization at minimum (persistent caching is a named
    follow-up knob, NOT built now);
    (c) partition instruments: wholly-empty response AND expiry older than the retention
    horizon constant (pinned from the 131-01a Task-0 evidence, ~2.5yr) → collect day-buckets
    into a NEW CompletenessReport field `pre_mark_retention_option_days` (add the field at
    :723 with a default that keeps every existing constructor call site valid — grep all
    constructions); wholly-empty INSIDE retention, or holed (including retention-STRADDLING
    instruments, whose partial marks are never bucketed) → let option_mtm_daily fail loud;
    (d) option_mtm_daily(...) → MERGE delta_mtm into the native_daily dict (dict-add per
    (day, ccy)) BEFORE the pd.Series conversion;
    (e) carry terminal_book forward for Task 5's guard threading.
    When the basis is NOT smoothed, the function body is byte-identical to today — assert by
    keeping every existing test_deribit_ingest test green UNMODIFIED. _build_dense_native_marks
    (:1611) needs no change (required days derive from the native_pnl series, which now
    includes ΔMTM days — covered automatically by the dense span; 83-PLAN Q2/Q5).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_deribit_ingest.py tests/test_smoothed_mtm_core.py -x -q && python -m mypy --strict --follow-imports=silent services/deribit_ingest.py</automated>
  </verify>
  <done>Merge proven INVOKED at the call site (wiring-guard reddens when neutered); zero-fetch assertions green for non-smoothed bases and perp-only; pre-retention bucket stamped; straddlers proven NOT bucketed (fail loud); all pre-existing ingest tests green UNMODIFIED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Smoothed-only identity channels in assert_balance_identity + native_options_session_upl</name>
  <files>analytics-service/services/deribit_txn.py, analytics-service/services/deribit_ingest.py, analytics-service/tests/test_deribit_txn.py, analytics-service/tests/test_smoothed_mtm_core.py</files>
  <behavior>
    - Smoothed basis, cash channel (strict, ALL currencies): Σ(cash contributions) == Σchange
      over _NATIVE_CASH_BEARING_TYPES rows — arithmetic identity now option rows carry full
      change; test_dropped_cash_row_fails_loud: deleting one option trade row from the
      fixture breaches it (mutation-honesty)
    - Smoothed basis, book channel (anchor cross-check): computed Book[c](last settlement)
      (replay × marks, the Task-4 terminal_book) reconciles against
      state.native_options_value[c] − state.native_options_session_upl[c] within
      max($1-equiv native floor, 1e-4·throughput_c); perturbed anchor options_value → raises
      LedgerValuationError with currency + magnitude-class only
    - NON-smoothed bases: assert_balance_identity behavior BYTE-IDENTICAL — the existing
      branch at :1652, the CR-01 open_option_ccys exemption, and every existing identity test
      stay green UNMODIFIED (the 83-PLAN Task-6 deletions are NOT performed)
    - DeribitNativeAccountState gains native_options_session_upl read off the SAME
      get_account_summaries response; ABSENT → 0.0 (SC-4 byte-safe; pin with a
      state-construction test)
  </behavior>
  <action>
    Failing tests FIRST. EXTEND (do not rework) assert_balance_identity (:1506): add a
    smoothed-only path — either a new `pnl_basis`/mode parameter defaulting to existing
    behavior, or a sibling entry point the adapter calls only under smoothed (executor's
    call; either way every existing call site compiles unchanged and the non-smoothed path
    is byte-identical). The smoothed path implements the two channels above; signature gains
    terminal_book + anchor mappings threaded from Task 4. Keep the CR-01
    open_option_ccys/_option_activity_after_coverage machinery INTACT for the existing bases
    — under smoothed the strict channel simply does not consult it (the open book is VALUED
    under this basis, so no exemption is needed; under the other bases the exemption remains
    load-bearing). Add native_options_session_upl to DeribitNativeAccountState
    (deribit_ingest.py summaries read; absent → 0.0). The 83-PLAN Q3-3 summary-channel
    cross-check (Σ(rpl+upl) == in-window option change+commission + ΔBook over each coverage
    window, via _summary_coverage_windows:1384/_option_commission:1709) is implemented as a
    smoothed-only cross-check with fail-loud on material breach — the summaries stop driving
    smoothed attribution but keep policing it.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_deribit_txn.py tests/test_smoothed_mtm_core.py -x -q && python -m mypy --strict --follow-imports=silent services/deribit_txn.py services/deribit_ingest.py</automated>
  </verify>
  <done>Both smoothed channels green from failing-first with breach siblings; every pre-existing identity test green UNMODIFIED; state field absent→0.0 pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: SC-4 byte-identity pins + total-preservation acceptance + full gates</name>
  <files>analytics-service/tests/test_smoothed_mtm_core.py, analytics-service/tests/test_native_nav_sc4_identity.py</files>
  <behavior>
    - Acceptance A (redistribution proof, from 83-PLAN §5.2 reframed): synthetic
      options+perps fixture with a MULTI-DAY option position, flat terminal book →
      smoothed_mtm per-ccy total == cash_settlement per-ccy total EXACTLY (float-exact
      fixture); §5-class inception closure green
    - Acceptance B (no session lumps, §5.1 reframed): the smoothed daily series spreads the
      option P&L across held days — assert NO day carries the whole session delta (each
      day ≈ its hand-computed mark delta) while the same fixture's mark_to_market series
      still shows the lump (proves the third basis fixes what the second lumps)
    - Acceptance C (non-flat terminal, §6 risk encoded): open-position-at-anchor fixture →
      smoothed total == Σchange + Book(last settlement), book-channel guard green with the
      settled-book anchor decomposition (equity − session_upl == cash + options_value −
      options_session_upl)
    - Acceptance D (sparse marks, §5-adjacent): the holed-marks fixture fails loud at ledger
      build naming instrument+day; mutation-honesty: perturbing one daily mark materially
      breaches the book-channel guard (marks are load-bearing, not decorative)
    - SC-4 real-adapter tier: (i) existing test_native_nav_sc4_identity matrix passes
      UNMODIFIED; (ii) NEW perp-only real-adapter fixture under smoothed_mtm asserts
      bit-exact (check_exact=True) equality with its cash_settlement run AND zero
      option-mark fetches (stub call-count 0); (iii) USD-native/linear-only fixture likewise;
      (iv) OPTIONS fixture under cash_settlement and mark_to_market asserts bit-exact
      equality with pre-change goldens (the reframe's keystone — existing arms untouched)
  </behavior>
  <action>
    These are the phase's closing pins — write them as tests only (no production code
    expected; if a pin reddens, the defect is upstream in Tasks 1–5 and is fixed THERE).
    Document the SC-4 mechanism in a comment where the smoothed branch begins
    (131-PATTERNS "SC-4 mechanism"): every new arm is classification-gated AND basis-gated —
    perp-only/USD-native ⇒ empty replay ⇒ no fetches ⇒ no-op merge ⇒ identical float ops ⇒
    bit-identical; native_options_session_upl absent→0.0. Then run the full plan gates.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/ -x -q && python -m mypy --strict --follow-imports=silent services/ routers/ models/ && python -m pytest tests/test_deribit_txn.py -k "purity" -q</automated>
  </verify>
  <done>Full analytics pytest green (incl. UNMODIFIED SC-4 suites + AST purity); mypy --strict 0; acceptance pins A–D green; the four byte-identity pins green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → Deribit public API | untrusted chart-data responses cross into money math |
| transaction-log rows → replay | venue-supplied `position`/`change` fields drive P&L |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-02 | Info disclosure | LedgerValuationError messages | mitigate | instrument+day / currency+magnitude-class only — never row payloads or balances (existing leak discipline) |
| T-131-03 | DoS | per-instrument mark fan-out | accept | one public request per held instrument ever, expiry-capped spans, existing pacing/retry, per-job memoization; persistent cache is a named follow-up knob |
| T-131-04 | Tampering | `position` field trust | mitigate | book-channel anchor cross-check is the independent backstop — replay drift surfaces as a guard breach, never a silent wrong MTM |
| T-131-SC | Tampering | package installs | accept | no new packages installed in this phase |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/ -q` fully green
- `python -m mypy --strict --follow-imports=silent services/ routers/ models/` → 0 errors
- AST purity guard (test_deribit_txn.py:980) green — deribit_txn.py stays pandas/async-free
- `git diff` audit: every production hunk in deribit_txn.py/deribit_ingest.py is inside a
  `use_smoothed`/smoothed-gated arm, a new function, or the two enum lines — NO hunk alters
  the cash_settlement or mark_to_market execution path
- No pre-existing test modified or deleted (the 83-PLAN Task-4/6 deletions are explicitly
  NOT performed under the third-basis framing)
</verification>

<success_criteria>
- smoothed_mtm total == cash_settlement total on a flat terminal book (exact); non-flat
  terminal telescopes to Σchange + Book(last settlement) with a green book-channel guard
- No session-lump spikes in the smoothed daily series (each day ≈ its mark delta)
- Perp-only/USD-native byte-identical under smoothed_mtm with ZERO option-mark fetches;
  options fixtures byte-identical under the two EXISTING bases
- Sparse marks inside listed life fail loud naming instrument+day; no interpolation;
  retention-STRADDLING instruments are never bucketed as pre-retention (they fail loud —
  the D-07 consequence pinned in 131-01a)
- Both basis enums carry smoothed_mtm (config/compute agreement pinned by test)
- Live acceptance (Phoenix `95089958` re-onboard, Zav2) stays OUT of this plan — deferred to
  post-131-03 live verification, per 83-PLAN Task 10 (key currently deleted from test DB)
</success_criteria>

<output>
Create `.planning/phases/131-smoothed-mtm-options-daily-mtm/131-01b-SUMMARY.md` when done.
</output>
