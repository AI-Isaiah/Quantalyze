---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 07
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet]
wave: 8
depends_on: ["164.4-06"]
files_modified:
  - supabase/tests/test_strategies_private_owner_isolation.sql
  - supabase/tests/test_api_keys_venue_identity_uniq.sql
  - supabase/tests/test_capital_ownership_column.sql
  - supabase/tests/test_csv_daily_returns_perkey_rls.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 5: four files (8 + 7 + 7 + 7 = 29 sections) fully annotated and proven"
  - "FILES_FLOOR 12 → 16, ARMS_FLOOR → measured (expected 256)"
affects: [164.4-08]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the four files carries one RED-UNDER-SETUP proven GREEN and every section has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok) and all four files restoring GREEN (SC-2)"
    - "Floors equal the run's printed numbers with dated blocks and both separation directions; the scanCorpus pin lists sixteen files (SC-3)"
    - "Waivers carry reasons in the twin and move WAIVED_CEILING by exactly their count; dead arms are TODOS findings (SC-4)"
  artifacts:
    - path: "supabase/tests/test_api_keys_venue_identity_uniq.sql"
      provides: "7 sections twinned (17 identities; 8 stamps named — the longest apply list of the batch)"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_strategies_private_owner_isolation.sql"
      provides: "8 sections twinned"
      contains: "RED-UNDER-SETUP"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 16"
      pattern: "scanCorpus"
---

<objective>
Batch 5, largest-first: `test_strategies_private_owner_isolation.sql` (planner-measured 252 lines,
**8 sections**, 2 stamps), `test_api_keys_venue_identity_uniq.sql` (662 lines, 17 ids → **7
sections**, **8 stamps named** — expect the longest apply list of the batch and record its per-lane
time), `test_capital_ownership_column.sql` (187 lines, 9 ids → **7 sections**, 1 stamp) and
`test_csv_daily_returns_perkey_rls.sql` (202 lines, 8 ids → **7 sections**, 1 stamp). Re-derive
sections with the one-liner.

Purpose: 29 sections; run ~288 lanes.
Output: 29 twins, four setup lines, FILES_FLOOR 16, ARMS_FLOOR → measured (expected 256).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Read before implementing: Plan 00's record, Plan 06's SUMMARY, PATTERNS § P1, § P2, § P3, § P4.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: strategies_private_owner_isolation — setup proven GREEN + ONE section end-to-end</name>
  <files>supabase/tests/test_strategies_private_owner_isolation.sql</files>
  <precondition>164.4-06-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`).</precondition>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-07-PLAN.md` and halt on non-zero.
    Discover the apply list with the lane loop (stand-ins per Plan 00's DECISION, then the real
    last-defining migrations; `--workdir` outside the repo; additive fixture only for relations no
    listed migration creates, recorded in the SUMMARY). Write the setup line at column 0 in the
    header with its 3-line paragraph; re-prove exit 0. Author ONE section (the first identity) in
    the P1.1 shape with a measured `occurrences`; `--parse-only`; diagnostic lane; read the verdict.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_strategies_private_owner_isolation.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>The first file has a proven setup line and one twin whose lane reads RED (identity ok).</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all four files</name>
  <files>supabase/tests/test_strategies_private_owner_isolation.sql, supabase/tests/test_api_keys_venue_identity_uniq.sql, supabase/tests/test_capital_ownership_column.sql, supabase/tests/test_csv_daily_returns_perkey_rls.sql</files>
  <action>
    Discover and prove a setup line for each of the other three files. Run the section-coverage
    one-liner over all four, paste it into the SUMMARY, and twin every untwinned section: real
    identity as `arm`; mutate the code under test in the last-defining listed migration;
    `occurrences` measured; `sql` step for exactly-pinned grant/policy drift; LAYERED `apply` for
    self-verifying migrations; `neuter` only for idiom arms; no fixture targets. Waivers only for
    arms that bite but cannot be mutated mechanically, reason in the twin, +1 ceiling each, >5
    cumulative → STOP. Dead arm = TODOS finding. Never edit an assertion, message or identity;
    diffs are added comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_strategies_private_owner_isolation.sql supabase/tests/test_api_keys_venue_identity_uniq.sql supabase/tests/test_capital_ownership_column.sql supabase/tests/test_csv_daily_returns_perkey_rls.sql</automated>
    Expected 8 / 7 / 7 / 7 sections, 0 untwinned each (a booked dead-arm finding is the only permitted exception).
  </verify>
  <done>All 29 sections twinned (or booked); four setup lines proven; parse-only green.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 12 → 16, ARMS_FLOOR → measured, pins and prose in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, CLAUDE.md</files>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 16/71`, `biting: 256`). In ONE commit with the annotations: Group A `run.mjs`
    floors with compact dated blocks (both separation directions, COVERAGE, RECORD), currency lines,
    `WAIVED_CEILING` +n with reasons only for accepted waivers; Group B registry strings and dates;
    Group C `totalAnchored`, `GREEN_LOG` (coverage 16/71, arms, biting, lane-invocations, mean,
    sixteen per-file lines summing to the aggregate, spawned string), `armsSeen`/`stepsSeen`,
    scanCorpus pin "16 of 71" + names; Group D `CLAUDE.md` currency sentence. `timeout-minutes` →
    30 with the measurement only if the run is ≥ 10 min (~288 lanes; ~6 min projected on ubuntu —
    record the actual). Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + wall clock in the SUMMARY.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 16/71`, `biting:` == ARMS_FLOOR; pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep | medium | mitigate | reason in twin, printed; ceiling +1 per reviewed waiver; >5 → STOP |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline counted as coverage | medium | mitigate | baseline proven first; per-file `judged` read off the run |
| T-164.4-08 | Denial of Service | lane time growth (8-stamp apply list) | low | mitigate | per-lane time recorded; timeout raised only with a measurement |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 8 / 7 / 7 / 7 sections, 0 untwinned; parse-only exit 0.
- Full run: `coverage: files 16/71`, `biting:` == ARMS_FLOOR, tallies agree, exit 0; lockstep vitest green; tree clean; `--pending` anchors green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- Four more files fully proven; floors moved by the measured delta in the same commit.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-07-SUMMARY.md` when done. Record: one-liner output, the four apply lists, run log lines, floors old → new, waivers/findings, PR number, SHA-bound run id and wall clock.
</output>
