---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 07
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet]
wave: 8
depends_on: ["164.4-06"]
files_modified:
  - supabase/tests/test_csv_finalize_atomic_fold.sql
  - supabase/tests/test_funding_fees_rls.sql
  - supabase/tests/test_allocator_equity_derived_rls.sql
  - supabase/tests/test_user_notes_dashboard_scope.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 4: four files (7 + 7 + 6 + 6 = 26 sections) fully annotated and proven — the old batch 6 minus test_derive_allocator_keys_fanout.sql, which is lane-blocked (section 6 sits behind an `IF EXISTS pg_cron` guard) and deferred"
  - "FILES_FLOOR 13 → 17, ARMS_FLOOR → measured (expected 189)"
affects: [164.4-08]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the four files carries one RED-UNDER-SETUP proven GREEN and every section has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok) and all four files restoring GREEN (SC-2)"
    - "Floors equal the run's printed numbers with dated blocks and both separation directions; the scanCorpus pin lists seventeen files; test_derive_allocator_keys_fanout.sql stays on the `lane-blocked:` line, untouched (SC-3, SC-4)"
    - "A waiver candidate is a STOP checkpoint to the founder; dead arms are TODOS findings (SC-4)"
  artifacts:
    - path: "supabase/tests/test_csv_finalize_atomic_fold.sql"
      provides: "7 sections twinned (32 identities Part 1..7 with sub-suffixes; one non-idiom raise)"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_funding_fees_rls.sql"
      provides: "7 sections twinned (0 stamps named — migrations found by object name)"
      contains: "RED-UNDER-SETUP"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 17"
      pattern: "scanCorpus"
---

<objective>
Batch 4, largest-first: `test_csv_finalize_atomic_fold.sql` (1277 lines, 79 idiom raises, 32
identities `Part 1`…`Part 7c` → **7 sections**, 5 stamps, **1 non-idiom raise** — pick mutations
it does not observe), `test_funding_fees_rls.sql` (401 lines, **7 sections**, **0 stamps named**),
`test_allocator_equity_derived_rls.sql` (216 lines, 8 ids → **6 sections**, 1 stamp; its `:54`
`SKIP` fires only when migration `20260717233529` is absent — put it in the apply list and confirm
the baseline prints no SKIP) and `test_user_notes_dashboard_scope.sql` (152 lines, **6 sections**,
1 stamp). Section counts re-confirmed at HEAD 007708dd by `gateSectionCount`. For the atomic-fold
file, a section like `Part 6` spans thirteen identities (`6a`…`6m`); one twin per section is the
requirement, a second on a different bit is allowed. Re-derive with the one-liner.

`test_derive_allocator_keys_fanout.sql` (7 sections) was in this batch before the spike. It is
NOT here: its ASSERTION 6 (`TEST FAILED (6)`) is guarded by `:159 IF EXISTS (SELECT 1 FROM
pg_extension WHERE extname = 'pg_cron')`, so on the lane that section can never bite and the file
cannot land fully proven. It is deferred with the other three pg_cron files and printed by the
runner as `lane-blocked:` (Plan 03) — do not touch it.

Purpose: 26 sections; run ~223 lanes.
Output: 26 twins, four setup lines, FILES_FLOOR 17, ARMS_FLOOR → measured (expected 189 = 163 + 26).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Per-task reading lists are in each task's `<read_first>`.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: csv_finalize_atomic_fold — setup proven GREEN + ONE section end-to-end past its non-idiom raise</name>
  <files>supabase/tests/test_csv_finalize_atomic_fold.sql</files>
  <precondition>164.4-06-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`).</precondition>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (the DECISION on stand-ins, the three authoring rules, the discovery loop), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-06-SUMMARY.md (apply lists and per-lane times of the previous batch), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1 shapes, § P1.1 placement, § P2.1 setup line placement), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-RESEARCH.md (§ Common Pitfalls 3 — the non-idiom raise this file carries), scripts/mutation-runner/GRAMMAR.md (rule 4: no fixture targets), supabase/tests/test_strategy_shares_rls.sql:40-61 (the reference header shape)</read_first>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-07-PLAN.md` and halt on non-zero.
    Discover the apply list with the lane loop (stand-ins per Plan 00's DECISION, then the real
    last-defining migrations; `--workdir` outside the repo; under bash; additive fixture only for
    relations no listed migration creates, recorded in the SUMMARY). Write the setup line at column
    0 in the header with its 3-line paragraph; re-prove exit 0. Locate the file's one non-idiom
    raise and the object it reads; author ONE section (`Part 1`'s first identity) in the P1.1 shape
    with a mutation that raise does not observe and a measured `occurrences`; `--parse-only`;
    diagnostic lane; read the verdict line.

    ⚠️ From this point until Task 2 completes, the parser test's section-inclusion pin (armed in
    Plan 02: twin sections ⊇ identity sections for every annotated file) is EXPECTED RED for this
    file — one twin over 7 sections is exactly the state the pin refuses. Do NOT touch the pin,
    do NOT loosen it, and do not run it as a gate before Task 2; it goes green when the last
    section is twinned and is asserted in Task 3's lockstep vitest.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_csv_finalize_atomic_fold.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>The first file has a proven setup line and one twin whose lane reads RED (identity ok).</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all four files</name>
  <files>supabase/tests/test_csv_finalize_atomic_fold.sql, supabase/tests/test_funding_fees_rls.sql, supabase/tests/test_allocator_equity_derived_rls.sql, supabase/tests/test_user_notes_dashboard_scope.sql</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (discovery loop, additive-fixture rule), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1, § P2.1-P2.3), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md (§ Waivers & un-falsifiable arms), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-RESEARCH.md (§ Common Pitfalls 3), scripts/mutation-runner/GRAMMAR.md (Shape 1b/3 layered apply; Shape 4 — what NOT to author), supabase/tests/test_allocator_equity_derived_rls.sql:54 (the SKIP keyed on migration 20260717233529)</read_first>
  <action>
    Discover and prove a setup line for each of the other three files (object-name lookup for
    `test_funding_fees_rls.sql`; `20260717233529` in the list for allocator_equity_derived_rls and
    the baseline lane's NOTICE output confirmed free of `SKIP`). Run the section-coverage
    one-liner over all four, paste it into the SUMMARY, and twin every untwinned section: real
    identity as `arm`; mutate the code under test in the last-defining listed migration;
    `occurrences` measured; `sql` step for exactly-pinned grant/policy drift; LAYERED `apply` for
    self-verifying migrations; `neuter` only for idiom arms that fire first; no fixture targets.
    Waivers: `WAIVED_CEILING` stays 0 — a biting-but-unmutatable arm is a STOP checkpoint to the
    founder. Dead arm = TODOS finding. Never edit an assertion, message or identity; diffs are
    added comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_csv_finalize_atomic_fold.sql supabase/tests/test_funding_fees_rls.sql supabase/tests/test_allocator_equity_derived_rls.sql supabase/tests/test_user_notes_dashboard_scope.sql</automated>
    Expected 7 / 7 / 6 / 6 sections, 0 untwinned each (a booked dead-arm finding is the only permitted exception).
  </verify>
  <done>All 26 sections twinned (or booked); four setup lines proven; parse-only green.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 13 → 17, ARMS_FLOOR → measured, pins and prose in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, CLAUDE.md</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-06-SUMMARY.md (the pin choreography as executed; the ubuntu per-lane mean and job wall clock), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P3, § P4 — every item), scripts/mutation-runner/run.mjs (FILES_FLOOR, ARMS_FLOOR, WAIVED_CEILING and their dated blocks), src/__tests__/gate-family-meta.test.ts (KNOWN_THRESHOLD_SITES), src/__tests__/mutation-runner-floors.test.ts (GREEN_LOG, totalAnchored), src/__tests__/mutation-annotation-parser.test.ts (armsSeen, stepsSeen, scanCorpus exact pin, section-inclusion pin), CLAUDE.md (§ SQL gate integrity jobs)</read_first>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 17/71`, `biting: 189`). In ONE commit with the annotations: Group A `run.mjs`
    floors with compact dated blocks (both separation directions, COVERAGE, RECORD), currency
    lines, `WAIVED_CEILING` unedited; Group B registry strings and dates; Group C `totalAnchored`,
    `GREEN_LOG` (coverage 17/71, arms, biting, lane-invocations, mean, `  pending: 23 …`, seventeen
    per-file lines summing to the aggregate, spawned string), `armsSeen`/`stepsSeen`, scanCorpus
    pin "17 of 71" + names; Group D `CLAUDE.md` currency sentence. `timeout-minutes` → 30 with the
    measurement only if the projection is ≥ 10 min (~223 lanes × the ubuntu per-lane mean from
    Plan 06's SUMMARY + its measured non-lane job overhead; ≈ 3.8 min of lanes at 1.03 s/lane —
    never a local wall clock scaled by a constant) — record the ubuntu job wall clock AND the ubuntu
    log's `per-arm lane time:` mean in the SUMMARY: Plan 08 reads both for the timeout
    re-justification.
    Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + the job's wall clock in the SUMMARY. Plan 08's precondition reads the wall clock.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 17/71`, `biting:` == ARMS_FLOOR; pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run and its wall clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep | medium | mitigate | no executor-authored waivers; STOP checkpoint; WAIVED_CEILING stays 0 |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline or green-SKIP counted as coverage | medium | mitigate | baseline proven and its NOTICE output read before authoring; per-file `judged` read off the run |
| T-164.4-08 | Denial of Service | run approaching `timeout-minutes` | medium | mitigate | wall clock measured this batch and handed to Plan 08 |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 7 / 7 / 6 / 6 sections, 0 untwinned; parse-only exit 0.
- Full run: `coverage: files 17/71`, `lane-blocked: 4 file(s) …` still naming derive, `biting:` == ARMS_FLOOR, tallies agree, exit 0; lockstep vitest green; tree clean; `--pending` anchors green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- Four more files fully proven; floors moved by the measured delta in the same commit; the timeout decision in Plan 08 is made from this batch's measured wall clock.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-07-SUMMARY.md` when done. Record: one-liner output, the four apply lists, run log lines, floors old → new, waiver candidates / findings, the measured ubuntu wall clock, PR number, SHA-bound run id.
</output>
