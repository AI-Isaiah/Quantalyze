---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 06
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet]
wave: 7
depends_on: ["164.4-05"]
files_modified:
  - supabase/tests/test_wizard_composite_members.sql
  - supabase/tests/test_capital_ownership_allocation_guard.sql
  - supabase/tests/test_create_wizard_strategy_for_key.sql
  - supabase/tests/test_scenario_shares_rls.sql
  - supabase/tests/test_strategy_keys_rls.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 4: five files (11 + 10 + 9 + 9 + 9 = 48 sections) fully annotated and proven"
  - "FILES_FLOOR 7 → 12, ARMS_FLOOR → measured (expected 227)"
affects: [164.4-07]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the five files carries one RED-UNDER-SETUP proven GREEN and every section has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok) and all five files restoring GREEN (SC-2)"
    - "Floors equal the run's printed numbers with dated blocks and both separation directions; the scanCorpus pin lists twelve files (SC-3)"
    - "Waivers carry reasons in the twin and move WAIVED_CEILING by exactly their count; dead arms are TODOS findings (SC-4)"
  artifacts:
    - path: "supabase/tests/test_wizard_composite_members.sql"
      provides: "11 sections twinned"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_capital_ownership_allocation_guard.sql"
      provides: "10 sections twinned (21 identities)"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_strategy_keys_rls.sql"
      provides: "9 sections twinned (13 identities)"
      contains: "RED-UNDER-SETUP"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 12"
      pattern: "scanCorpus"
---

<objective>
Batch 4, largest-first: `test_wizard_composite_members.sql` (planner-measured 409 lines, 11 ids →
**11 sections**, 1 stamp), `test_capital_ownership_allocation_guard.sql` (899 lines, 21 ids →
**10 sections**, **0 stamps named** — its migrations must be found by object name),
`test_create_wizard_strategy_for_key.sql` (231 lines, **9 sections**, 3 stamps; a sentinel file),
`test_scenario_shares_rls.sql` (483 lines, **9 sections**, 1 stamp) and `test_strategy_keys_rls.sql`
(295 lines, 13 ids → **9 sections**, 1 stamp). Five apply lists to discover; the RLS gates will
need the grant/policy `sql`-step shape (Shape 2) wherever a migration pins a privilege set exactly.
Re-derive sections with the one-liner.

Purpose: 48 sections; run ~251 lanes.
Output: 48 twins, five setup lines, FILES_FLOOR 12, ARMS_FLOOR → measured (expected 227).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Read before implementing: Plan 00's record, Plan 05's SUMMARY, PATTERNS § P1 (Shape 2 for grant
drift), § P2, § P3, § P4.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: wizard_composite_members — setup proven GREEN + ONE section end-to-end</name>
  <files>supabase/tests/test_wizard_composite_members.sql</files>
  <precondition>164.4-05-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`).</precondition>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-06-PLAN.md` and halt on non-zero.
    Discover the apply list with the lane loop (stand-ins per Plan 00's DECISION, then the real
    last-defining migrations; `--workdir` outside the repo; additive fixture only for relations no
    listed migration creates, recorded in the SUMMARY). Write the setup line at column 0 in the
    header with its 3-line paragraph; re-prove exit 0. Author ONE section (the first identity) in
    the P1.1 shape with a measured `occurrences`; `--parse-only`; diagnostic lane; read the verdict.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_wizard_composite_members.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>wizard_composite_members has a proven setup line and one twin whose lane reads RED (identity ok).</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all five files</name>
  <files>supabase/tests/test_wizard_composite_members.sql, supabase/tests/test_capital_ownership_allocation_guard.sql, supabase/tests/test_create_wizard_strategy_for_key.sql, supabase/tests/test_scenario_shares_rls.sql, supabase/tests/test_strategy_keys_rls.sql</files>
  <action>
    Discover and prove a setup line for each of the other four files (for
    `test_capital_ownership_allocation_guard.sql`, which names no stamp, find the migrations by
    grepping the function/table names it exercises across `supabase/migrations/` and take the
    last-defining one of each). Run the section-coverage one-liner over all five, paste it into the
    SUMMARY, and twin every untwinned section: real identity as `arm`; mutate the code under test
    in the last-defining listed migration; `occurrences` measured; `sql` step for exactly-pinned
    grant/policy drift; LAYERED `apply` for self-verifying migrations; `neuter` only for idiom
    arms; no fixture targets. Waivers only for arms that bite but cannot be mutated mechanically,
    reason in the twin, +1 ceiling each, >5 cumulative → STOP. Dead arm = TODOS finding. Never edit
    an assertion, message or identity; diffs are added comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_wizard_composite_members.sql supabase/tests/test_capital_ownership_allocation_guard.sql supabase/tests/test_create_wizard_strategy_for_key.sql supabase/tests/test_scenario_shares_rls.sql supabase/tests/test_strategy_keys_rls.sql</automated>
    Expected 11 / 10 / 9 / 9 / 9 sections, 0 untwinned each (a booked dead-arm finding is the only permitted exception).
  </verify>
  <done>All 48 sections twinned (or booked); five setup lines proven; parse-only green.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 7 → 12, ARMS_FLOOR → measured, pins and prose in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, CLAUDE.md</files>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 12/71`, `biting: 227`). In ONE commit with the annotations: Group A `run.mjs`
    floors with compact dated blocks (both separation directions, COVERAGE naming the files, RECORD),
    currency lines, `WAIVED_CEILING` +n with reasons only for accepted waivers; Group B registry
    strings and dates; Group C `totalAnchored`, `GREEN_LOG` (coverage 12/71, arms, biting,
    lane-invocations, mean, twelve per-file lines summing to the aggregate, spawned string),
    `armsSeen`/`stepsSeen`, scanCorpus pin "12 of 71" + names; Group D `CLAUDE.md` currency
    sentence. `timeout-minutes` → 30 with the measurement only if the run is ≥ 10 min (~251
    lanes; ~5.4 min projected on ubuntu). Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + wall clock in the SUMMARY.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 12/71`, `biting:` == ARMS_FLOOR; pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep | medium | mitigate | reason in twin, printed; ceiling +1 per reviewed waiver; >5 → STOP |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline counted as coverage | medium | mitigate | baseline proven first; per-file `judged` read off the run |
| T-164.4-06 | Tampering | RLS arms "proven" by a grant the stand-in granted | medium | mitigate | grant drift is a `sql` step against the REAL migration's grants; stand-ins carry no grants beyond what the gate names |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 11 / 10 / 9 / 9 / 9 sections, 0 untwinned; parse-only exit 0.
- Full run: `coverage: files 12/71`, `biting:` == ARMS_FLOOR, tallies agree, exit 0; lockstep vitest green; tree clean; `--pending` anchors green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- Five more files fully proven; floors moved by the measured delta in the same commit.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-06-SUMMARY.md` when done. Record: one-liner output, the five apply lists, run log lines, floors old → new, waivers/findings, PR number, SHA-bound run id and wall clock.
</output>
