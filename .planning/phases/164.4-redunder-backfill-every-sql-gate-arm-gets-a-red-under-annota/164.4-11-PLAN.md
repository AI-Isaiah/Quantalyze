---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 11
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, mixed-files, waivers, lockstep-pins, floor-ratchet, phase-close]
wave: 12
depends_on: ["164.4-10"]
files_modified:
  - supabase/tests/test_api_keys_exchange_not_user_writable.sql
  - supabase/tests/test_strategy_keys_publish_integrity.sql
  - supabase/tests/test_api_keys_insert_not_client_writable.sql
  - supabase/tests/test_sync_status_marked_refresh_protected.sql
  - supabase/tests/test_guard_wizard_draft_updates_auth_uid.sql
  - supabase/tests/test_profiles_privileged_columns_locked.sql
  - supabase/tests/test_wizard_session_idempotency.sql
  - scripts/mutation-runner/run.mjs
  - scripts/mutation-runner/GRAMMAR.md
  - scripts/mutation-runner/VAC06-DEMOS.md
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - .github/workflows/ci.yml
  - CLAUDE.md
  - TODOS.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 8, the seven mixed files (15 sections, 128 non-idiom raises between them) fully annotated and proven; any waiver a founder-accepted, printed decision"
  - "End state on today's lane: FILES_FLOOR 40, ARMS_FLOOR → measured (expected 265), WAIVED_CEILING at the measured count (expected 0); coverage files 40/71 with the 27 non-idiom files and the 4 lane-blocked files printed by name and the pending line empty"
affects: [phase-close, REDUNDER-NONIDIOM, REDUNDER-PGCRON]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the 40 annotatable idiom files carries a RED-UNDER-SETUP and every section a prose marker and a twin; the runner's `  pending:` line is empty, its `unreachable:` line names the 27 non-idiom files and its `lane-blocked:` line names the 4 deferred files with the TODO id (SC-1 as amended, SC-4)"
    - "The full-corpus run exits 0 with every non-waived twin RED (identity ok) and every file restoring GREEN; biting == executed == lane-invocations (SC-2)"
    - "FILES_FLOOR = 40, ARMS_FLOOR = the run's biting, WAIVED_CEILING = the run's waived, each with a dated block and both separation directions; every prose site quoting the figures is current or explicitly lineage (SC-3)"
    - "The ledger the verifier reads agrees with the gate: ROADMAP criterion 1 and goal line and STATE carry SCOPE AMENDMENT #2 (Plan 03) with no surviving pre-amendment count claim — asserted by grep in Task 3's verify, not by a sentence in the SUMMARY (SC-1 as amended)"
    - "Every waiver, if any, was accepted by the founder at a checkpoint before it was written, is a twin with its reason, printed every run, and the cumulative total is ≤ 5 (SC-4)"
  artifacts:
    - path: "supabase/tests/test_profiles_privileged_columns_locked.sql"
      provides: "1 section twinned beside 7 compound-head SET ROLE lines the R4-C01 tokenizer refuses to neuter"
      contains: "RED-UNDER-SETUP"
    - path: "scripts/mutation-runner/run.mjs"
      provides: "FILES_FLOOR=40 and the end-state ARMS_FLOOR / WAIVED_CEILING with dated blocks"
      contains: "FILES_FLOOR"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 40, unreachableFiles 27, laneBlockedFiles 4, pendingFiles 0"
      pattern: "scanCorpus"
---

<objective>
The seven ⚠️ mixed files, batched LAST by CONTEXT because they are the waiver generator: their
non-idiom raises outnumber their idiom raises, and a non-idiom raise that fires first under a
mutation scores `NO-IDENTITY` and cannot be neutered. Planner-measured, sections re-confirmed at
HEAD 007708dd: `test_api_keys_exchange_not_user_writable.sql` (961 lines, 5 ids → **4 sections**,
**38 non-idiom**, 7 stamps; four `SKIP` notices keyed on migrations `20260810120000`,
`20260811210000`, `20260814120000` — all must be in the apply list so the guarded blocks RUN),
`test_strategy_keys_publish_integrity.sql` (371 lines, 5 ids → **4**, 10 non-idiom, 1 stamp),
`test_api_keys_insert_not_client_writable.sql` (372 lines, **3**, 11 non-idiom, 1 stamp; `:349`
SKIP keyed on the INSERT-revoke marker), `test_sync_status_marked_refresh_protected.sql` (763
lines, 2 ids → **1**, 38 non-idiom, 2 stamps; sentinel file; its applied-ness gate RAISES, never
SKIPs), `test_guard_wizard_draft_updates_auth_uid.sql` (244 lines, **1**, 9 non-idiom, 0 stamps),
`test_profiles_privileged_columns_locked.sql` (140 lines, **1**, 7 non-idiom, 1 stamp — carries
seven `SET ROLE postgres; IF NOT ok THEN` compound-head lines the 164.3.1 tokenizer refuses as a
neuter target, so do not plan on neutering there; `:88` SKIP keyed on `20260529150000`) and
`test_wizard_session_idempotency.sql` (317 lines, **1**, 15 non-idiom, 5 stamps; `:274` SKIP keyed
on `20260814120000`). 15 sections.

⛔ WAIVER GATE, before any twin: read Plan 10's cumulative waiver count (expected 0). For each of
the 15 sections, first look for a mutation the shadowing non-idiom raises do not observe (mutate
the object only the idiom arm reads). `WAIVED_CEILING` is 0 and the executor does not move it: if
no such mutation exists for an arm that DOES bite, STOP, write the SUMMARY with the arm, the
shadowing raise's file:line and the mutations tried, and return a checkpoint to the founder. Only
a founder-accepted waiver becomes a Shape 4 twin, +1 to `WAIVED_CEILING` with the reason beside
the constant; more than 5 cumulative is never ratcheted.

Purpose: criterion 1 holds for every idiom file the lane can reach; the phase ends with the floors
at their measured end state and BOTH exclusions (27 non-idiom, 4 lane-blocked) printed by the gate.
Output: 15 twins, seven setup lines, FILES_FLOOR 40, ARMS_FLOOR → measured (expected 265 = 250 +
15), the final prose sweep, the two successor TODOs cross-referenced.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Per-task reading lists are in each task's `<read_first>`.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs WAIVED_CEILING -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
<!-- verify-symbol: TODOS.md REDUNDER-NONIDIOM -->
<!-- verify-symbol: TODOS.md REDUNDER-PGCRON -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: api_keys_exchange_not_user_writable — setup proven GREEN with no SKIP + ONE section end-to-end past 38 non-idiom raises</name>
  <files>supabase/tests/test_api_keys_exchange_not_user_writable.sql</files>
  <precondition>164.4-10-SUMMARY.md exists, records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`), and states the cumulative waiver count.</precondition>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (the DECISION on stand-ins, the three authoring rules, the discovery loop), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-10-SUMMARY.md (the cumulative waiver tally; apply lists; ubuntu wall clock and per-lane mean), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1 shapes, § P1.1 placement, § P2.1 setup line placement), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-RESEARCH.md (§ Common Pitfalls 3 — 38 non-idiom raises in this file), scripts/mutation-runner/GRAMMAR.md (rule 4: no fixture targets; § Neuter semantics), supabase/tests/test_strategy_shares_rls.sql:40-61 (the reference header shape)</read_first>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-11-PLAN.md` and halt on non-zero.
    Discover the apply list with the lane loop (stand-ins per Plan 00's DECISION, then the real
    last-defining migrations, including `20260810120000`, `20260811210000`, `20260814120000` and
    their prerequisites; `--workdir` outside the repo; under bash; additive fixture only for
    relations no listed migration creates, recorded in the SUMMARY). Read the baseline lane's
    NOTICE output: it must print no `SKIP` — a section behind a still-firing SKIP is un-falsifiable
    and may not be twinned. Write the setup line at column 0 in the header with its 3-line
    paragraph; re-prove exit 0. List the file's 38 non-idiom raises and the objects each reads;
    choose, for the first idiom section, a mutation none of them observes. Author that ONE section
    in the P1.1 shape with a measured `occurrences`; `--parse-only`; diagnostic lane; read the
    verdict — a `NO-IDENTITY` here means a non-idiom raise fired first: change the mutation, not
    the raise.

    ⚠️ From this point until Task 2 completes, the parser test's section-inclusion pin (armed in
    Plan 02: twin sections ⊇ identity sections for every annotated file) is EXPECTED RED for this
    file — one twin over 4 sections is exactly the state the pin refuses. Do NOT touch the pin,
    do NOT loosen it, and do not run it as a gate before Task 2; it goes green when the last
    section is twinned and is asserted in Task 3's lockstep vitest.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_api_keys_exchange_not_user_writable.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>The first mixed file has a proven setup line (baseline prints no SKIP) and one twin whose lane reads RED (identity ok) with no non-idiom raise ahead of it.</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all seven files; a waiver only via a founder checkpoint</name>
  <files>supabase/tests/test_api_keys_exchange_not_user_writable.sql, supabase/tests/test_strategy_keys_publish_integrity.sql, supabase/tests/test_api_keys_insert_not_client_writable.sql, supabase/tests/test_sync_status_marked_refresh_protected.sql, supabase/tests/test_guard_wizard_draft_updates_auth_uid.sql, supabase/tests/test_profiles_privileged_columns_locked.sql, supabase/tests/test_wizard_session_idempotency.sql, TODOS.md</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (discovery loop, additive-fixture rule), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1, § P1.3 Shape 4 — the waiver form; the corpus has none, the first real one is a founder decision not a template fill; § P2.1-P2.3), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md (§ Waivers & un-falsifiable arms — the locked +1-per-accepted-waiver rule and the 5-waiver escalation), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-RESEARCH.md (§ Waivers, § Common Pitfalls 3), scripts/mutation-runner/GRAMMAR.md (Shape 4 waiver grammar; § Neuter semantics; the R4-C01 compound-head refusal), supabase/tests/test_api_keys_insert_not_client_writable.sql:349 (SKIP keyed on the INSERT-revoke marker), supabase/tests/test_profiles_privileged_columns_locked.sql:88 (SKIP keyed on 20260529150000), supabase/tests/test_wizard_session_idempotency.sql:274 (SKIP keyed on 20260814120000), TODOS.md (the [REDUNDER-NONIDIOM] entry, where dead-arm findings go)</read_first>
  <action>
    Discover and prove a setup line for each of the other six files (object-name lookup for
    `test_guard_wizard_draft_updates_auth_uid.sql`, which names no stamp; every SKIP-keyed
    migration in the list; baseline NOTICE output free of `SKIP` for each). Run the
    section-coverage one-liner over all seven, paste it into the SUMMARY, and twin every untwinned
    section per the rules: real identity as `arm`; mutate the code under test in the last-defining
    listed migration; `occurrences` measured; `sql` step for exactly-pinned grant/policy drift
    (these are not-user-writable / not-client-writable gates — the `sql` GRANT shape will be the
    common one); LAYERED `apply` for self-verifying migrations; `neuter` only for idiom arms and
    never in `test_profiles_privileged_columns_locked.sql`'s compound-head branches; no fixture
    targets. Shadowed by a non-idiom raise → a mutation it does not observe; only when none exists
    for an arm that bites → STOP and return a founder checkpoint naming the arm and that raise's
    file:line; a founder-accepted waiver is then a Shape 4 twin whose reason names the line. Keep a
    running tally; more than 5 cumulative is never ratcheted. A dead arm is a FINDING → `TODOS.md`
    beside `[REDUNDER-NONIDIOM]`, no twin, said loudly. Never edit an assertion, message or
    identity; diffs on the gate files are added comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_api_keys_exchange_not_user_writable.sql supabase/tests/test_strategy_keys_publish_integrity.sql supabase/tests/test_api_keys_insert_not_client_writable.sql supabase/tests/test_sync_status_marked_refresh_protected.sql supabase/tests/test_guard_wizard_draft_updates_auth_uid.sql supabase/tests/test_profiles_privileged_columns_locked.sql supabase/tests/test_wizard_session_idempotency.sql</automated>
    Expected 4 / 4 / 3 / 1 / 1 / 1 / 1 sections, 0 untwinned each (a booked dead-arm finding is the only permitted exception). Founder-accepted waivers count as twins here and in parity.
  </verify>
  <done>All 15 sections twinned (or booked); seven setup lines proven with no SKIP; parse-only green; the waiver tally is ≤ 5 cumulative and every waiver was founder-accepted, or the plan stopped.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green at the end state — FILES_FLOOR 40, ARMS_FLOOR and WAIVED_CEILING measured, final pin and prose sweep</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, scripts/mutation-runner/GRAMMAR.md, scripts/mutation-runner/VAC06-DEMOS.md, .github/workflows/ci.yml, CLAUDE.md, TODOS.md</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-03-SUMMARY.md (the lane-blocked and lane-probe contract; the seven ledger sites SCOPE AMENDMENT #2 was written to and the greps that assert them), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-10-SUMMARY.md (the pin choreography as executed; the waiver tally; the ubuntu per-lane mean and job wall clock), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P3, § P4 — every item, § P4 Group D the full prose list for the final sweep), scripts/mutation-runner/run.mjs (FILES_FLOOR, ARMS_FLOOR, WAIVED_CEILING and their dated blocks and currency lines), scripts/mutation-runner/GRAMMAR.md (the header's remaining-files sentence; Shape 4's currency sentence), scripts/mutation-runner/VAC06-DEMOS.md (currency lines), .github/workflows/ci.yml (the sql-mutation TIMEOUT comment as Plan 08 left it), src/__tests__/gate-family-meta.test.ts (KNOWN_THRESHOLD_SITES), src/__tests__/mutation-runner-floors.test.ts (GREEN_LOG, totalAnchored), src/__tests__/mutation-annotation-parser.test.ts (armsSeen, stepsSeen, scanCorpus exact pin, laneBlockedFiles, pendingFiles, section-inclusion pin), CLAUDE.md (§ SQL gate integrity jobs), TODOS.md (the [REDUNDER-NONIDIOM] and [REDUNDER-PGCRON] entries), .planning/ROADMAP.md (the Phase 164.4 goal line and criterion 1 as amended by Plan 03), .planning/STATE.md (the SCOPE AMENDMENT #2 entry and the [REDUNDER-PGCRON] line)</read_first>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 40/71`, `unreachable: 27 file(s) …`, `lane-blocked: 4 file(s) …`, an EMPTY
    `  pending:` (quote whichever the runner prints for an empty list), `biting: 265` minus
    waivers, `arms: E/A/W` with W the waiver count). In ONE commit with the annotations:
    Group A — `run.mjs`: `FILES_FLOOR` → 40 and `ARMS_FLOOR` → measured biting, each with a dated
    block (both separation directions, COVERAGE pointing at the parser test's exact list, RECORD);
    `WAIVED_CEILING` → measured W (only if W > 0), its block listing every waiver's arm and reason;
    currency lines edited to the end state ("Phase 164.4 raises FILES_FLOOR as it backfills…" →
    past tense with the date; the 27 excluded files pointed at `[REDUNDER-NONIDIOM]`, the 4
    deferred at `[REDUNDER-PGCRON]`); lineage blocks untouched.
    Group B — registry strings `FILES_FLOOR=40`, `ARMS_FLOOR=<measured>`, `WAIVED_CEILING=<W>`, dates.
    Group C — `totalAnchored`, `GREEN_LOG` (coverage 40/71, arms E/A/W, biting, lane-invocations,
    mean, the lane-blocked line, forty per-file lines summing to the aggregate, `  waived:` lines
    if W > 0, spawned string), `armsSeen`/`stepsSeen`, scanCorpus pin "40 of 71" + all 40 names,
    `laneBlockedFiles` the 4 names, and an assertion that `pendingFiles` is empty.
    Group D — final prose sweep against PATTERNS § P4 Group D: `CLAUDE.md` § SQL gate integrity
    jobs currency sentence to the end state (files 40/71, arms E/A/W, 27 + 4 printed); `GRAMMAR.md`
    header ("~70 more gate files" → the measured end state) and Shape 4 currency ("the real corpus
    has none" is false once W > 0 — rewrite to the measured count with the date and keep the
    2026-08-29 sentence as lineage); `VAC06-DEMOS.md` currency lines; the `ci.yml` `sql-mutation`
    TIMEOUT comment re-justified with this batch's measured wall clock (~345 lanes; raise to 30 only
    if ≥ 10 min; ≥ 20 min is a STOP per Plan 08's ceiling rule).
    `TODOS.md`: `[REDUNDER-NONIDIOM]` gains one line "164.4 closed <date>: 40/71 files, <E> arms
    biting, <W> waived; the runner prints these 27 by name every run"; `[REDUNDER-PGCRON]` gains
    "164.4 closed <date> with the 4 files lane-blocked and printed every run; 100 sections owed to
    this item". Then grep the repo for the superseded figures (`30/30/0`, `files 1/71`,
    `other 70 files`, `~70 more`, `44/71`, `355 sections`, `WAIVED_CEILING`) with `grep -a -rn` over
    `CLAUDE.md scripts/ src/__tests__/ .github/` and classify every hit as lineage (a dated record
    of a run) or currency (fix it) in the SUMMARY. ⚠️ `WAIVED_CEILING` is in that list because
    `CLAUDE.md` states it as "(0)": if any waiver landed, that parenthetical is a stale-integer
    claim. `44/71` is in that list because the ledger's pre-amendment sites said 44: Plan 03 wrote
    SCOPE AMENDMENT #2 into ROADMAP (goal line, goal sentence, section-unit paragraph, criterion 1,
    phase table row) and STATE (narrowing entry, [REDUNDER-PGCRON] line). Assert the LEDGER, not
    the SUMMARY: re-run Plan 03 Task 3's positive and negative greps (they are in the verify below)
    and read the ROADMAP goal line and criterion 1 by eye. Any surviving 44-file or ~365-twin
    currency claim is a Plan 03 regression — fix the ledger site with a scoped `Edit` and say so in
    the SUMMARY; never explain it away in the SUMMARY while the ledger still reads 44. The
    `timeout-minutes` projection for ~345 lanes is lanes × the ubuntu per-lane mean from Plan 10's
    SUMMARY + its measured non-lane overhead, replaced by the measured ubuntu wall clock once the
    PR runs — never a local wall clock scaled by a constant. Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && node scripts/mutation-runner/run.mjs --parse-only | grep -a -q -E '^coverage: files 40/71$' && node scripts/mutation-runner/run.mjs --parse-only | grep -a -q -E '^lane-blocked: 4 file\(s\) ' && node scripts/mutation-runner/run.mjs 2>&1 | grep -a -q -E '^lane-probe: pg_cron absent' && grep -a -c 'SCOPE AMENDMENT #2' .planning/ROADMAP.md | awk '{exit !($1>=5)}' && grep -a -c 'SCOPE AMENDMENT #2' .planning/STATE.md | awk '{exit !($1>=2)}' && grep -a -q 'a silent exclusion fails this criterion just' .planning/ROADMAP.md && ! grep -a -q '~365 twins' .planning/ROADMAP.md && ! grep -a -q 'in the 44 IDIOM files carries' .planning/ROADMAP.md && ! grep -a -q '3 idiom gate files (71 of 355' .planning/STATE.md && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + wall clock in the SUMMARY. Then run `/gsd-verify-work 164.4`.</human-check>
    The ledger greps re-assert Plan 03's amendment at phase close: the verifier reads ROADMAP
    criterion 1 and the goal line, and they must state the end state this run just printed.
  </verify>
  <done>Full run exit 0 with `coverage: files 40/71`, the 27 non-idiom and 4 lane-blocked files printed, `lane-probe:` measured, `  pending:` empty, `biting:` == ARMS_FLOOR, waived == WAIVED_CEILING; every pin and prose site at the end state in the same commit; ROADMAP and STATE still carry SCOPE AMENDMENT #2 with no surviving pre-amendment count claim; SUMMARY records the SHA-bound ubuntu-green run, the final numbers and the lineage/currency classification of every superseded figure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |
| waiver twin → floors | a waiver satisfies parity and spawns no lane; bounded from above by WAIVED_CEILING in-process and in CI; only the founder may accept one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep — this is the batch where it is first exercised for real | high | mitigate | mutation-first search per section; a waiver is a founder checkpoint, never an executor call; reason names the shadowing raise; >5 cumulative never ratcheted |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline or green-SKIP counted as coverage | high | mitigate | every SKIP-keyed migration in the apply list; baseline NOTICE output read before twinning; per-file `judged` read off the run |
| T-164.4-09 | Tampering | neutering a compound-head branch that leaks `SET ROLE postgres` | high | mitigate | the R4-C01 tokenizer refuses it (`neuter-missed`); this plan does not neuter in that file |
| T-164.4-04 | Repudiation | stale-integer claims after the last floor move, including any surviving pre-amendment "44" in the ledger | medium | mitigate | the final grep sweep classifies every superseded figure as lineage or currency; Plan 03's ledger greps are re-run in this plan's verify so the end state 40 + 4 lane-blocked is asserted in ROADMAP/STATE, not merely stated in the SUMMARY |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 4 / 4 / 3 / 1 / 1 / 1 / 1 sections, 0 untwinned; parse-only exit 0.
- Full run: `coverage: files 40/71`, `unreachable: 27 file(s) …`, `lane-blocked: 4 file(s) …`, `  pending:` empty, `biting:` == ARMS_FLOOR, waived == WAIVED_CEILING ≤ 5, tallies agree, exit 0; lockstep vitest green; ci.yml parses; tree clean; `--pending` anchors green (this is the last pending plan).
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- Criteria 1 (as amended), 2, 3 and 4 hold at once, emitted by the gate itself: 40/71 annotated and proven, 27 non-idiom and 4 lane-blocked files printed by name with their reasons, floors at their measured end state, waivers (if any) founder-accepted, recorded and bounded. The phase is ready for `/gsd-verify-work 164.4`, with the 100 lane-blocked sections owed to `[REDUNDER-PGCRON]` stated plainly.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-11-SUMMARY.md` when done. Record: one-liner output, the seven apply lists, run log lines verbatim (coverage, unreachable, lane-blocked, pending, arms, biting, lane-invocations, every waived line), floors old → end state, every waiver's arm + reason + shadowing line + the founder checkpoint that accepted it, findings, wall clock and timeout decision, PR number, SHA-bound run id, and the lineage/currency classification table.
</output>
