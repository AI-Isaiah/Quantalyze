---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 11
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet]
wave: 12
depends_on: ["164.4-10"]
files_modified:
  - supabase/tests/test_allocator_equity_pre_terminus_flag.sql
  - supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql
  - supabase/tests/test_metrics_by_basis_write.sql
  - supabase/tests/test_set_compute_job_progress.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 9: the four remaining non-mixed files (2 sections each = 8 sections) fully annotated and proven"
  - "FILES_FLOOR 33 → 37, ARMS_FLOOR → measured (expected 350); every non-mixed idiom file is now covered"
affects: [164.4-12]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the four files carries one RED-UNDER-SETUP proven GREEN and every section has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok) and all four files restoring GREEN (SC-2)"
    - "Floors equal the run's printed numbers with dated blocks and both separation directions; the scanCorpus pin lists thirty-seven files; the `pending:` line names exactly the seven mixed files (SC-3)"
    - "Waivers carry reasons in the twin and move WAIVED_CEILING by exactly their count; dead arms are TODOS findings (SC-4)"
  artifacts:
    - path: "supabase/tests/test_set_compute_job_progress.sql"
      provides: "2 sections twinned (5 identities)"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_metrics_by_basis_write.sql"
      provides: "2 sections twinned (2 stamps)"
      contains: "RED-UNDER-SETUP"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 37"
      pattern: "scanCorpus"
---

<objective>
Batch 9, the last non-mixed files: `test_allocator_equity_pre_terminus_flag.sql` (planner-measured
106 lines, **2 sections**, 1 stamp), `test_enqueue_compute_job_dedupe_non_terminal.sql` (207
lines, **2**, 0 stamps), `test_metrics_by_basis_write.sql` (123 lines, **2**, 2 stamps) and
`test_set_compute_job_progress.sql` (187 lines, 5 ids → **2 sections**, 1 stamp). Small files,
four apply lists. After this batch the runner's `pending:` line must name exactly the seven ⚠️
mixed files Plan 12 takes — read it off the run and quote it in the SUMMARY; it is the phase's own
statement of what is left. Re-derive with the one-liner.

Purpose: 8 sections; run ~424 lanes; the waiver tally to date is stated for Plan 12's gate.
Output: 8 twins, four setup lines, FILES_FLOOR 37, ARMS_FLOOR → measured (expected 350).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Read before implementing: Plan 00's record, Plan 10's SUMMARY, PATTERNS § P1, § P2, § P3, § P4.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs WAIVED_CEILING -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: allocator_equity_pre_terminus_flag — setup proven GREEN + ONE section end-to-end</name>
  <files>supabase/tests/test_allocator_equity_pre_terminus_flag.sql</files>
  <precondition>164.4-10-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`).</precondition>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-11-PLAN.md` and halt on non-zero.
    Discover the apply list with the lane loop (stand-ins per Plan 00's DECISION, then the real
    last-defining migrations; `--workdir` outside the repo; additive fixture only for relations no
    listed migration creates, recorded in the SUMMARY). Write the setup line at column 0 in the
    header with its 3-line paragraph; re-prove exit 0. Author ONE section (the first identity) in
    the P1.1 shape with a measured `occurrences`; `--parse-only`; diagnostic lane; read the verdict.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_allocator_equity_pre_terminus_flag.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>The first file has a proven setup line and one twin whose lane reads RED (identity ok).</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all four files</name>
  <files>supabase/tests/test_allocator_equity_pre_terminus_flag.sql, supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql, supabase/tests/test_metrics_by_basis_write.sql, supabase/tests/test_set_compute_job_progress.sql</files>
  <action>
    Discover and prove a setup line for each of the other three files (object-name lookup for
    `test_enqueue_compute_job_dedupe_non_terminal.sql`, which names no stamp). Run the
    section-coverage one-liner over all four, paste it into the SUMMARY, and twin every untwinned
    section: real identity as `arm`; mutate the code under test in the last-defining listed
    migration; `occurrences` measured; `sql` step for exactly-pinned grant/policy drift; LAYERED
    `apply` for self-verifying migrations; `neuter` only for idiom arms; no fixture targets. Waivers
    only for arms that bite but cannot be mutated mechanically, +1 ceiling each, >5 cumulative →
    STOP. Dead arm = TODOS finding. Never edit an assertion, message or identity; diffs are added
    comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_allocator_equity_pre_terminus_flag.sql supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql supabase/tests/test_metrics_by_basis_write.sql supabase/tests/test_set_compute_job_progress.sql</automated>
    Expected 2 sections and 0 untwinned in each of the four (a booked dead-arm finding is the only permitted exception).
  </verify>
  <done>All 8 sections twinned (or booked); four setup lines proven; parse-only green.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 33 → 37, ARMS_FLOOR → measured, pins and prose in the same commit, waiver tally stated</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, CLAUDE.md</files>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 37/71`, `biting: 350`; the `pending:` line must name exactly the seven mixed
    files — if it names anything else, a non-mixed file was missed and this batch is not done). In
    ONE commit with the annotations: Group A `run.mjs` floors with compact dated blocks (both
    separation directions, COVERAGE, RECORD), currency lines, `WAIVED_CEILING` +n with reasons only
    for accepted waivers; Group B registry strings and dates; Group C `totalAnchored`, `GREEN_LOG`
    (coverage 37/71, arms, biting, lane-invocations, mean, thirty-seven per-file lines summing to
    the aggregate, spawned string), `armsSeen`/`stepsSeen`, scanCorpus pin "37 of 71" + names;
    Group D `CLAUDE.md` currency sentence. State in the SUMMARY the cumulative waiver count and
    `WAIVED_CEILING` value: Plan 12 may start only if that count plus the mixed files' forecast stays
    ≤ 5. `timeout-minutes` per the standing rule (~424 lanes). Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + wall clock + the cumulative waiver count in the SUMMARY.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 37/71`, `biting:` == ARMS_FLOOR, `pending:` naming exactly the seven mixed files; pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run and the waiver tally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep | medium | mitigate | reason in twin, printed; ceiling +1 per reviewed waiver; cumulative tally stated for Plan 12's gate |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline counted as coverage | medium | mitigate | baseline proven first; per-file `judged` read off the run |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 2 sections, 0 untwinned, four times; parse-only exit 0.
- Full run: `coverage: files 37/71`, `pending:` == the seven mixed files, `biting:` == ARMS_FLOOR, tallies agree, exit 0; lockstep vitest green; tree clean; `--pending` anchors green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- Every non-mixed idiom file is proven; the runner's own `pending:` line says only the seven mixed files remain; the waiver tally is on the record for Plan 12's gate.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-11-SUMMARY.md` when done. Record: one-liner output, the four apply lists, run log lines including the `pending:` line verbatim, floors old → new, cumulative waivers and WAIVED_CEILING, findings, wall clock, PR number, SHA-bound run id.
</output>
