---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 06
subsystem: mutation-runner
tags: [red-under-m, red-under-setup, rls, skip-guarded-arms, floor-ratchet, lockstep-pins, pg-lane, batch]

requires:
  - phase: 164.4-00
    provides: "the DECIDED stand-in fixture strategy, the discovery loop, and the three authoring rules every twin here obeys"
  - phase: 164.4-02
    provides: "the SECTION-INCLUSION pin that is live for every file in corpus.annotatedFiles, and the lockstep-pin choreography"
  - phase: 164.4-05
    provides: "FILES_FLOOR 9 / ARMS_FLOOR 134, the MEASURED rule that an unwrapped positive control needs a SILENT drift, and the ubuntu-green sql-mutation run at PR #735 head f0d19bf7 that is this plan's precondition"
provides:
  - "Four NEW gate files fully annotated and PROVEN: 8 + 7 + 7 + 7 = 29 sections, every one with a prose RED-UNDER and a machine twin that reddened its OWN arm first on a real throwaway pg-lane cluster"
  - "The phase's third FILE move: FILES_FLOOR 9 -> 13 and ARMS_FLOOR 134 -> 163, both pinned from the run's own printed lines with both separation directions driven on real lanes"
  - "The FIRST batch whose arms sat behind a runtime SKIP: migration 20260814120000 is in test_api_keys_venue_identity_uniq.sql's apply list, the baseline was READ for zero `SKIP (` notices and the `PASS (structural)` variant, and section 4's twin drives exactly the assertion the SKIP would have hidden"
  - "Three new additive stand-in fixtures, including 10-fixture-strategies-rls-baseline.sql — which DROPS 01-fixture-core.sql's narrower stand-in policy so the REAL 20260405061912_rls_policies.sql is the thing under test, and restores the table grants without which GUARD 6, GUARD 7 and RLS 4 all passed vacuously"
affects: [164.4-07, 164.4-08, 164.4-09, 164.4-10, 164.4-11]

actuals:
  tokens: 18852
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "A gate whose arms hide behind a runtime `RAISE NOTICE 'SKIP (…)'` must have the guarding migration IN its apply list, and the BASELINE output must be read for the proof — this file prints a state-aware `PASS (structural)` vs `PASS WITH 3 SKIPS` summary, so the variant it prints IS the machine evidence that the guarded arms ran"
    - "A stand-in policy that is NARROWER than production (01-fixture-core's `strategies_read … TO authenticated`) makes an anon arm unfalsifiable rather than merely imprecise; the fix is to DROP the stand-in in an additive fixture so the REAL migration defines the object, not to widen the stand-in"
    - "Two arms that read the SAME property from two tenants (A sees A's row / B sees B's row) cannot both be reddened by one mutation; the second takes the first as a `neuter`"
    - "An unwrapped positive control whose SQLSTATE the gate's own handler CATCHES (6e's `EXCEPTION WHEN unique_violation`) can take the LITERAL drift — the silent-form rule binds only where the raise escapes every handler"

key-files:
  created:
    - scripts/pg-lane/fixtures/10-fixture-strategies-rls-baseline.sql
    - scripts/pg-lane/fixtures/11-fixture-api-keys-created-at.sql
    - scripts/pg-lane/fixtures/12-fixture-profiles-is-admin.sql
    - .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/deferred-items.md
  modified:
    - supabase/tests/test_strategies_private_owner_isolation.sql
    - supabase/tests/test_api_keys_venue_identity_uniq.sql
    - supabase/tests/test_capital_ownership_column.sql
    - supabase/tests/test_csv_daily_returns_perkey_rls.sql
    - scripts/mutation-runner/run.mjs
    - scripts/mutation-runner/GRAMMAR.md
    - src/__tests__/gate-family-meta.test.ts
    - src/__tests__/mutation-runner-floors.test.ts
    - src/__tests__/mutation-annotation-parser.test.ts
    - .github/workflows/ci.yml
    - CLAUDE.md

key-decisions:
  - "test_strategies_private_owner_isolation.sql takes the REAL 20260405061912_rls_policies.sql rather than 01-fixture-core.sql's stand-in `strategies_read`. MEASURED with the stand-in: the file printed ALL PASS while GUARD 6's UPDATE and GUARD 7's INSERT were refused by the GRANT layer with the same 42501 those arms read as trigger proof, and anon held no SELECT at all — three vacuous passes inside the vacuity detector. 10-fixture-strategies-rls-baseline.sql drops the stand-in policy and restores Supabase's bootstrap table grants so the migration is the object under test."
  - "GUARD 8 (service_role CAN publish) is an unwrapped POSITIVE CONTROL and takes the SILENT form — a BEFORE ROW trigger returning NULL scoped to service_role — carrying plan 164.4-05's measured rule forward rather than re-discovering it. GUARD 7 narrows the trigger to `BEFORE UPDATE` so GUARD 6 still bites and 7 is the first failure."
  - "test_api_keys_venue_identity_uniq.sql's apply list carries migration 20260814120000 and its prerequisites, and the baseline was read for the proof: ZERO `SKIP (` notices and the `PASS (structural)` variant, never `PASS WITH 3 SKIPS`. Section 4's twin re-GRANTs EXECUTE to `authenticated` — the arm reachable only while `v_b_live` — so its RED is itself the evidence that the skip is inert."
  - "Every twin in test_api_keys_venue_identity_uniq.sql is a `sql` step because 20260812083206's own post-verify re-asserts the column, the index (UNIQUE / partial / both predicate conjuncts / column order), the non-blank CHECK, the scrub trigger's attachment and posture and every create_wizard_strategy canary. A migration EDIT aborts the apply before the gate runs; the `sql` steps drift the LIVE object AFTER apply, which is what this gate exists to catch."
  - "test_capital_ownership_column.sql arm 7 neuters arm 6c. 6c and 7 both insert a NULL capital_ownership and are INDISTINGUISHABLE to any trigger — 6c names the column, 7 omits it, and the row reaching the trigger is identical — so the stamping drift reddens 6c first. Same shape for csv_daily_returns Assertion 4a, which neuters Assertion 1."
  - "`ci.yml`'s `timeout-minutes: 15` was NOT raised. A linear fit over THREE ubuntu points (45 arms/119 s, 86/171 s, 134/232 s) gives 1.27 s per arm of JOB cost plus ~62 s fixed, so 163 arms projects to ~4.5 min and the phase's ~265-arm end state to ~6.6 min — both inside 15."

patterns-established:
  - "Every floor move states BOTH separation directions, driven through runCorpus on real lanes, beside the constant"
  - "A gate's SKIP-guarded arms are proven live by reading the BASELINE's own state-aware summary line, and one twin is deliberately chosen to be the arm the SKIP would have hidden"

requirements-completed: [SC-1, SC-2, SC-3, SC-4]

coverage:
  - id: D1
    description: "test_strategies_private_owner_isolation.sql: one RED-UNDER-SETUP discovered over 2 lane runs, and all 8 sections twinned"
    requirement: "SC-1"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_strategies_private_owner_isolation.sql -> baseline exit 0, 8/8 `RED (identity ok)` (RLS 1, RLS 2, RLS 3, GUARD 5, GUARD 6, GUARD 7, GUARD 8, RLS 4), restore exit 0; `sections 8 / judged 8 / annotated 8 / waived 0 / biting 8`"
        status: pass
      - kind: integration
        ref: "3 timed GREEN lane runs at 1.00 / 0.96 / 0.96 s (mean 0.97 s); 2 discovery iterations — iteration 1 stopped on `ERROR: 42703 column \"source\" of relation \"strategies\" does not exist`"
        status: pass
    human_judgment: false
  - id: D2
    description: "test_api_keys_venue_identity_uniq.sql: the batch's longest apply list (7 real migrations + 6 stand-ins), all 7 sections twinned, and the two SKIP-guarded assertions PROVEN to run"
    requirement: "SC-1"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_api_keys_venue_identity_uniq.sql -> baseline exit 0, 7/7 `RED (identity ok)` (1, 2, 3, 4, 5, 6.0, 6e), restore exit 0; `sections 7 / judged 7 / annotated 7 / waived 0 / biting 7`"
        status: pass
      - kind: integration
        ref: "BASELINE NOTICE check (threat T-164.4-05): `grep -ac 'SKIP (' <baseline output>` -> 0, and the file prints its `PASS (structural)` variant — the state-aware summary whose ELSE branch is `PASS WITH 3 SKIPS (structural)`. So the auth.uid()-absence pair (3c) and the `authenticated` negative (4) both RAN. Arm 4's twin drives the v_b_live-armed assertion itself and scores RED."
        status: pass
      - kind: integration
        ref: "3 timed GREEN lane runs at 1.10 / 1.08 / 1.07 s (mean 1.08 s) — the longest apply list of the batch; 3 discovery iterations, stopping on `ERROR: 42883 function public.create_wizard_strategy(...) does not exist` and `ERROR: 42703 column \"created_at\" does not exist`"
        status: pass
    human_judgment: false
  - id: D3
    description: "test_capital_ownership_column.sql: all 7 sections twinned, its apply list reused verbatim from its sibling and GREEN on the first iteration"
    requirement: "SC-1"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_capital_ownership_column.sql -> baseline exit 0, 7/7 `RED (identity ok)` (1, 2, 3, 4, 6a, 7, 5), restore exit 0; `sections 7 / judged 7 / annotated 7 / waived 0 / biting 7`"
        status: pass
      - kind: integration
        ref: "3 timed GREEN lane runs at 0.94 / 0.99 / 0.94 s (mean 0.96 s); 1 discovery iteration — the apply list is byte-identical to test_capital_ownership_allocation_guard.sql's, which plan 164.4-05 discovered over 5 lane runs"
        status: pass
    human_judgment: false
  - id: D4
    description: "test_csv_daily_returns_perkey_rls.sql: all 7 sections twinned, and the anon arm made falsifiable by the bootstrap-grants fixture"
    requirement: "SC-1"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_csv_daily_returns_perkey_rls.sql -> baseline exit 0, 7/7 `RED (identity ok)` (Assertion 1-7, with 4a standing for section `Assertion 4`), restore exit 0; `sections 7 / judged 7 / annotated 7 / waived 0 / biting 7`"
        status: pass
      - kind: integration
        ref: "3 timed GREEN lane runs at 0.93 / 0.99 / 0.93 s (mean 0.95 s); 2 discovery iterations — iteration 1 stopped on `ERROR: 42703 column p.is_admin does not exist` at 20260522111839_csv_daily_returns.sql:90"
        status: pass
    human_judgment: false
  - id: D5
    description: "The full-corpus run exits 0 with judged == annotated == biting on all thirteen files and the two independent tallies agreeing"
    requirement: "SC-2"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs -> exit 0, 191 s wall: `coverage: files 13/71`, `arms: 163/163/0`, `biting: 163`, `lane-invocations: 163`, `per-arm lane time: mean 1.0s over 163 arm run(s)`, `pending: 27`, thirteen per-file rows summing 7+10+7+9+7+15+15+11+9+8+9+45+11 = 163, `✅ No defects`"
        status: pass
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --self-test -> exit 0, `SELF-TEST PASSED: both floor modes, the waiver ceiling, the wrong-identity mode, MEASURE_FAIL, the absurdity floor, the stand-in target refusal, the stale-deferral probe and the 164.3.1-11 regression corpus all fire`"
        status: pass
    human_judgment: false
  - id: D6
    description: "FILES_FLOOR 9 -> 13 and ARMS_FLOOR 134 -> 163, each pinned from the run's own printed line with a dated block and both separation directions"
    requirement: "SC-3"
    verification:
      - kind: integration
        ref: "runCorpus({filesFloor:13,armsFloor:163}) -> filesAnnotated 13, biting 163, laneInvocations 163, floor-defects 0, exit 0, SILENT (191.4 s). runCorpus({filesFloor:14,armsFloor:164}) -> floor-defects 2, exit 1, FIRES `FILES_FLOOR regression: 13 annotated file(s) < floor 14` and `ARMS_FLOOR regression: 163 biting arm(s) < floor 164` (193.2 s). Both on REAL lanes, 163 arm lanes each."
        status: pass
      - kind: unit
        ref: "src/__tests__/gate-family-meta.test.ts#KNOWN_THRESHOLD_SITES exact-set (found ⊆ known AND known ⊆ found) with FILES_FLOOR=13, ARMS_FLOOR=163, WAIVED_CEILING=0; the bare-measurement walker reports `ok scripts/mutation-runner/run.mjs :: FILES_FLOOR=13 (line 266)` and `:: ARMS_FLOOR=163 (line 488)`"
        status: pass
    human_judgment: false
  - id: D7
    description: "Every lockstep pin and prose currency site moved in the SAME commit as the floors; the four lockstep vitest files, the whole suite, tsc, the gate linter and the anchor verifier are green"
    requirement: "SC-3"
    verification:
      - kind: unit
        ref: "./node_modules/.bin/vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts -> 4 files / 211 tests passed"
        status: pass
      - kind: unit
        ref: "./node_modules/.bin/vitest run (FULL suite, serial, nothing else on the box) -> 838 files / 13890 tests passed, 19 files + 280 tests skipped, 0 failed, 108 s"
        status: pass
      - kind: other
        ref: "./node_modules/.bin/tsc --noEmit -p tsconfig.json -> exit 0; node scripts/lint-sql-gates.mjs -> 71 files, 0 findings; node scripts/verify-plan-anchors.mjs --pending -> 84 claims, no stale claims; git status --porcelain empty after the full run (no dirty-checkout)"
        status: pass
    human_judgment: false
  - id: D8
    description: "Anti-vacuity: a NEW control was proven able to fail — one twin removed from a new file reddens seven independent pins, and the byte-backup restore is verified by shasum"
    requirement: "SC-2"
    verification:
      - kind: unit
        ref: "delete the single line `-- RED-UNDER-M: {\"arm\":\"GUARD 8\",…}` from supabase/tests/test_strategies_private_owner_isolation.sql:280; shasum 78c009e3 -> 6942144e PROVES the edit landed and `grep -ac` returns 0; then parse-only exits 1 with `parity … 8 prose RED-UNDER marker(s) vs 7 RED-UNDER-M twin(s)` and 7 vitest arms fire — see the RED messages quoted in ## Anti-Vacuity Proof; restore from the byte backup returns shasum 78c009e3, `grep -ac` 1, `git status --porcelain` empty for the file, parse-only exit 0, 211/211 green"
        status: pass
    human_judgment: false
  - id: D9
    description: "Gate-file additions are COMMENT-ONLY: no assertion, identity, arm or failure message changed"
    requirement: "SC-2"
    verification:
      - kind: other
        ref: "git diff -U0 636bb063..HEAD -- supabase/tests/ parsed line by line: 277 added lines, 0 non-comment added, 0 removed. Asserted after Task 1 (20 added, 0 non-comment) and again after Task 2 (257 added, 0 non-comment)."
        status: pass
    human_judgment: false
  - id: D10
    description: "The batch lands as its own PR with `sql-mutation` GREEN on the ubuntu CI host, SHA-bound to the PR head"
    requirement: "SC-2"
    verification:
      - kind: other
        ref: "DONE by the orchestrator. PR #736, head SHA ba6fe1e25133eb762c698aa731e9cc30e745efc8, squash-merged 2026-09-03T19:28:51Z as 6d6368ef. CI run 33794810067 job 100779820029 — headSha ba6fe1e2…, sql-mutation conclusion=success, wall clock 278 s (19:09:57Z → 19:14:35Z). The UBUNTU job's OWN log lines, read from the run rather than restated: `arms: 163/163/0`, `biting: 163`, `lane-invocations: 163`, `tallies agree`, `coverage: files 13/71`, `per-arm lane time: mean 1.0s over 163 arm run(s)`, `No defects`. Identical to macOS on every figure — the fourth consecutive batch where the two hosts agree, which is what retires the one-host doubt about the psql CONTEXT/LOCATION grammar the identity attribution reads. All 23 SHA-bound checks success. ⭐ Timing: this SUMMARY predicted ~269 s from the 3-point fit; measured 278 s (3% over). FOUR ubuntu points now: 45/119 s, 86/171 s, 134/232 s, 163/278 s → refit 1.35 s per arm of JOB cost + ~58 s fixed, so the ~265-arm end state projects to ~416 s ≈ 6.9 min, inside `timeout-minutes: 15` without a raise. Plan 08 still re-justifies it against ITS OWN measured run."
        status: pass
    human_judgment: true
    rationale: "The executor is forbidden to push, open a PR or merge (execution brief `<human_check_handling>`). Every LOCAL equivalent was run and is recorded under D5/D7, but a local macOS green is ONE host: the psql CONTEXT/LOCATION grammar the identity attribution reads is measured on macOS / PostgreSQL 16.13 only, and the ubuntu runner's PostgreSQL build and per-lane cost are different measurements. Commits produced: d0af9b10, 65c4a13b, 2a19dbe1 (+ this SUMMARY commit). The orchestrator must land them as one PR, confirm `sql-mutation` GREEN on ubuntu SHA-bound to the PR head (`gh run view <id> --json headSha,conclusion`), and write the run id, head SHA and the job's wall clock back into this entry. Plan 07's precondition reads it. PROJECTION to check against: three ubuntu points exist — 45 arms/119 s (run 33751634051), 86/171 s (33774615747), 134/232 s (33785233457) — fitting 1.27 s per arm of JOB cost plus ~62 s of fixed overhead, so 163 arms should land near 269 s ≈ 4.5 min, well inside `timeout-minutes: 15`. That JOB figure is larger than the runner's own `per-arm lane time: mean 1.0s`, which measures the LANE only and excludes per-arm orchestration."

duration: 58 min
completed: 2026-09-03
status: complete
---

# Phase 164.4 Plan 06: The private-by-default / venue-identity batch — the phase's third FILE move Summary

**29 sections across four NEW gate files — private-by-default owner isolation, the api_keys venue-identity dedup backstop, the capital-ownership column shape and the per-key csv_daily_returns RLS axis — annotated and machine-proven on throwaway pg-lane clusters, taking `coverage: files 9/71 -> 13/71` and `biting: 134 -> 163` with zero waivers, zero dead arms, and the first batch whose arms had to be lifted out from behind a runtime SKIP before they could be twinned at all.**

## Performance

- **Duration:** 58 min
- **Started:** 2026-09-03T17:58:16Z
- **Completed:** 2026-09-03T18:57:09Z
- **Tasks:** 3
- **Files modified:** 15 (4 created, 11 modified)

## Accomplishments

- **Four files fully proven.** 8 + 7 + 7 + 7 = 29 sections, one twin each, every one `RED (identity ok)` on its own lane with a GREEN baseline before and a GREEN restore after. `judged == annotated == biting` on all four, and every one bit on the FIRST attempt — no twin had to be re-cut.
- **The two SKIP-guarded assertions were made to RUN, and the proof was read off the lane** rather than assumed: zero `SKIP (` notices and the `PASS (structural)` summary variant. Section 4's twin drives the very assertion the skip would have hidden.
- **Three vacuous passes found and closed** on `test_strategies_private_owner_isolation.sql` — GUARD 6, GUARD 7 and RLS 4 all passed against the stand-in policy for reasons unrelated to the objects they name.
- **The phase's third FILE move**, with both floors moving together and every pin, exact set and prose currency site in the same commit.
- **Zero waivers, zero findings inside the corpus.** `WAIVED_CEILING` stays 0.

## The four apply lists, as discovered

| gate | lane runs | what the iterations bought | timed GREEN runs | mean |
|---|---|---|---|---|
| `test_strategies_private_owner_isolation.sql` | 2 | a NEW `10-fixture-strategies-rls-baseline.sql`. Iteration 1 died on `ERROR: 42703 column "source" of relation "strategies" does not exist`; the deeper find was that the list must carry the REAL `20260405061912_rls_policies.sql` and therefore DROP 01-fixture-core.sql's narrower stand-in `strategies_read` first | 1.00 / 0.96 / 0.96 | **0.97 s** |
| `test_api_keys_venue_identity_uniq.sql` | 3 | `20260602190000_f6_wizard_session_idempotency.sql` (iteration 1 died on `42883 function public.create_wizard_strategy(…) does not exist` — `20260710180000` re-bases a body it does not create); then a NEW `11-fixture-api-keys-created-at.sql` for `20260811210000`'s PROD-census block | 1.10 / 1.08 / 1.07 | **1.08 s** |
| `test_capital_ownership_column.sql` | 1 | nothing — the sibling `test_capital_ownership_allocation_guard.sql`'s list is byte-identical and GREEN here on the first try | 0.94 / 0.99 / 0.94 | **0.96 s** |
| `test_csv_daily_returns_perkey_rls.sql` | 2 | a NEW `12-fixture-profiles-is-admin.sql`. Iteration 1 died on `ERROR: 42703 column p.is_admin does not exist` at `20260522111839_csv_daily_returns.sql:90` — the `csv_daily_returns_admin_select` policy predicate | 0.93 / 0.99 / 0.93 | **0.95 s** |

⭐ **A SUBSYSTEM SIBLING IS FREE, AND PLAN 05 SAID SO.** `test_capital_ownership_column.sql` needed
ONE lane run because its sibling had already paid for the list. The other three needed 7 runs and
three new fixtures between them. The assortment cost is real; batch by subsystem where the
largest-first ordering allows it.

⭐ **THE LONGEST APPLY LIST OF THE BATCH COSTS ~11 % MORE PER LANE.** 13 entries (7 real migrations
+ 6 stand-ins) ran at a mean of 1.08 s against 0.95-0.97 s for the 6-7-entry lists. Cluster boot is
still the dominant term at this scale; the apply list is a visible but small marginal cost.

## The section-coverage one-liner (Task 2 verify), verbatim

```
supabase/tests/test_strategies_private_owner_isolation.sql sections 8 untwinned 0
supabase/tests/test_api_keys_venue_identity_uniq.sql sections 7 untwinned 0
supabase/tests/test_capital_ownership_column.sql sections 7 untwinned 0
supabase/tests/test_csv_daily_returns_perkey_rls.sql sections 7 untwinned 0
ONELINER_EXIT=0
```

## The full run, read OFF THE RUN LOG

```
node scripts/mutation-runner/run.mjs   ->  exit 0, 191 s wall

coverage: files 13/71
unreachable: 27 file(s) raise outside the runner's identity idiom — … (TODOS [REDUNDER-NONIDIOM])
lane-blocked: 4 file(s) probe pg_extension for pg_cron, which the pg-lane cannot host — … (deferred 2026-09-03, TODOS [REDUNDER-PGCRON])
lane-probe: pg_cron absent — lane-blocked class is current
  pending: 27 idiom file(s) without RED-UNDER — …
arms: 163/163/0   (executed/annotated/waived)
biting: 163   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 163   (… plus 13 baseline / 13 restore leg(s))
  file test_api_keys_venue_identity_uniq.sql: sections 7 / judged 7 / annotated 7 / waived 0 / biting 7
  file test_capital_ownership_allocation_guard.sql: sections 10 / judged 10 / annotated 10 / waived 0 / biting 10
  file test_capital_ownership_column.sql: sections 7 / judged 7 / annotated 7 / waived 0 / biting 7
  file test_create_wizard_strategy_for_key.sql: sections 9 / judged 9 / annotated 9 / waived 0 / biting 9
  file test_csv_daily_returns_perkey_rls.sql: sections 7 / judged 7 / annotated 7 / waived 0 / biting 7
  file test_ledger_refresh_composite_arm.sql: sections 15 / judged 15 / annotated 15 / waived 0 / biting 15
  file test_ledger_refresh_fanout.sql: sections 15 / judged 15 / annotated 15 / waived 0 / biting 15
  file test_ledger_refresh_staleness.sql: sections 11 / judged 11 / annotated 11 / waived 0 / biting 11
  file test_scenario_shares_rls.sql: sections 9 / judged 9 / annotated 9 / waived 0 / biting 9
  file test_strategies_private_owner_isolation.sql: sections 8 / judged 8 / annotated 8 / waived 0 / biting 8
  file test_strategy_keys_rls.sql: sections 9 / judged 9 / annotated 9 / waived 0 / biting 9
  file test_strategy_shares_rls.sql: sections 35 / judged 45 / annotated 45 / waived 0 / biting 45
  file test_wizard_composite_members.sql: sections 11 / judged 11 / annotated 11 / waived 0 / biting 11
per-arm lane time: mean 1.0s over 163 arm run(s)

✅ No defects. Every annotated arm bit its own arm first.
```

The two independent tallies AGREE (`arms:` executed 163, `lane-invocations:` 163). `pending:` fell
31 -> 27; the phase's end state on today's lane is 40 of 71.

## Floors, old -> new, with both separation directions

| constant | old | new | SILENT at | FIRES at | the message it fires with |
|---|---|---|---|---|---|
| `FILES_FLOOR` | 9 | **13** | 13 | 14 | `FILES_FLOOR regression: 13 annotated file(s) < floor 14` |
| `ARMS_FLOOR` | 134 | **163** | 163 | 164 | `ARMS_FLOOR regression: 163 biting arm(s) < floor 164` |
| `WAIVED_CEILING` | 0 | **0 — UNCHANGED** | — | — | not edited; this batch added no waiver |

Both directions were driven through the REAL verdict loop on REAL lanes
(`runCorpus({filesFloor, armsFloor})`, 163 arm lanes per configuration, 6 min 25 s
for the pair), not simulated:

```
filesFloor=13  armsFloor=163  filesAnnotated=13 biting=163 laneInvocations=163 floor-defects=0 exit=0 wall=191.4s
filesFloor=14  armsFloor=164  filesAnnotated=13 biting=163 laneInvocations=163 floor-defects=2 exit=1 wall=193.2s
    -> FILES_FLOOR regression: 13 annotated file(s) < floor 14
    -> ARMS_FLOOR regression: 163 biting arm(s) < floor 164
```

## ⭐ THE FINDING THIS BATCH BOUGHT — a stand-in that is NARROWER than production makes an arm UNFALSIFIABLE

`01-fixture-core.sql:58-60` declares

```sql
CREATE POLICY strategies_read ON strategies
  FOR SELECT TO authenticated
  USING (status = 'published' OR user_id = auth.uid());
```

Production's definition (`20260405061912_rls_policies.sql:28-30`) is the same predicate with **no
role restriction**, and `strategies` in production carries Supabase's bootstrap table grants for all
three API roles. Under the stand-in, `test_strategies_private_owner_isolation.sql` printed
**`ALL PASS`** — and three of its eight arms were passing for reasons unrelated to the objects they
name:

| arm | what it claims to prove | why it passed on the stand-in |
|---|---|---|
| `GUARD 6` | authenticated owner cannot UPDATE own private -> published; the `guard_strategies_publish_transition` TRIGGER is the sole source of 42501 | `authenticated` held only `SELECT`, so the UPDATE was refused by the GRANT layer with 42501 — the exact SQLSTATE the arm's `EXCEPTION WHEN insufficient_privilege` reads as trigger proof |
| `GUARD 7` | same, for a fresh published INSERT | identical mechanism |
| `RLS 4` | anon sees 0 rows for the private strategy | anon had no policy AND no grant, so it can never see ANY row — no mutation of `strategies_read` could ever redden it |

The fix is **not** to widen the stand-in. `10-fixture-strategies-rls-baseline.sql` DROPs it so the
REAL migration defines the object the arm asserts on, and restores the grants production has, which
is what makes RLS 4's twin (`OR current_user = 'anon'`) able to bite at all. The WHY is the
fixture's own header.

⭐ **THE GENERAL FORM, for later batches:** a stand-in that is narrower than production does not
merely under-test — it can make an arm structurally unfalsifiable while the file prints PASS. Read
every arm's PRECONDITIONS (grants, role scoping, default privileges) against production before
accepting a green baseline as evidence.

## ⭐ THE SECOND FINDING — a runtime SKIP is an APPLY-LIST question, and the baseline's own summary is the proof

`test_api_keys_venue_identity_uniq.sql` hides two assertions behind
`RAISE NOTICE 'SKIP (3c …)'` (:403) and `'SKIP (4 …)'` (:437) whenever migration `20260814120000`
(Phase 156 Migration B) is not applied. An arm behind a firing SKIP is un-falsifiable and would
report non-biting; annotating it as if it bit is exactly the vacuity this phase exists to remove.

Two things made it honest:

1. **Migration B and its prerequisites are in the apply list** — `20260602190000`, `20260710120000`,
   `20260710180000`, `20260811210000`, `20260812083206`, `20260813150106`, `20260814120000`.
   (Plan 164.4-05 could not take `20260814120000` for `test_create_wizard_strategy_for_key.sql`
   because `20260811210000`'s ACL post-verify aborted on a cluster where those RPCs were created for
   the first time. It applies cleanly HERE because this list creates them through
   `20260602190000` -> `20260710180000` first, so the ACL state the post-verify checks is the one it
   expects.)
2. **The baseline output was READ, not assumed.** `grep -ac 'SKIP ('` -> **0**, and the file's own
   state-aware last statement printed the `PASS (structural)` variant — the one whose ELSE branch is
   `PASS WITH 3 SKIPS (structural)` and whose wording claims "ZERO auth.uid()" and "authenticated
   shut out". The gate says which of its two states it ran in; that is the machine evidence.

Section 4's twin then drives the guarded assertion itself (`GRANT EXECUTE … TO authenticated`,
reachable only while `v_b_live`), so its `RED (identity ok)` is a second, independent proof that
the skip is inert.

## Anti-Vacuity Proof — a new control, neutered and observed RED

The 29 twins are each already a neuter -> observe-RED -> restore cycle performed by the runner
itself. Beyond that, a NEW control was proven able to fail by hand.

**Neuter:** delete the single line `-- RED-UNDER-M: {"arm":"GUARD 8",…}` from
`supabase/tests/test_strategies_private_owner_isolation.sql` (line 280).

**The neuter PROVED to have landed before anything was run:**

```
shasum before: 78c009e3fb76b2b6acf70d4462adc619d4505c47
shasum after:  6942144e808a304fa16a2c38ead8c181233e5a7b      (differs)
grep -ac 'RED-UNDER-M: {"arm":"GUARD 8"' -> 0                (the line is gone)
```

**Observed RED — seven independent controls fired, each naming the defect:**

```
node scripts/mutation-runner/run.mjs --parse-only  -> exit 1
  ❌ 1 static defect(s):
    parity  supabase/tests/test_strategies_private_owner_isolation.sql
        8 prose RED-UNDER marker(s) vs 7 RED-UNDER-M twin(s)

vitest (the four lockstep files) -> 7 failed / 204 passed
  × 164.4-02: every SECTION an annotated file raises for also carries a twin
      SECTION coverage gap. … {"test_strategies_private_owner_isolation.sql":["GUARD 8"]}
  × equals the number of NON-WAIVED twins — the biting count the run measured
      The corpus declares 162 twin(s) of which 0 are waivers, so a green run
      bites 162. ARMS_FLOOR is 163. Update the floor in
      scripts/mutation-runner/run.mjs from a MEASURED run, never from this number.
  × never demands more biting arms than the corpus actually declares
      expected 163 to be less than or equal to 162
  × any file carrying structured twins must have one per prose marker
      test_strategies_private_owner_isolation.sql: 8 prose RED-UNDER marker(s) but
      7 RED-UNDER-M twin(s).
  × REAL CORPUS: no annotation that exists today rewrites an identity
      expected 162 to be 163
  × no `find` or `anchor` in the REAL corpus names the literal — rule 3b's needle half
      expected 142 to be 143
  × `--parse-only` deliberately does NOT print it — so CI's demand for it also catches a mode swap
```

**Restore, verified — never `git checkout --`, always the byte backup:**

```
cp <backup> <file>
shasum: 78c009e3fb76b2b6acf70d4462adc619d4505c47   (matches the pre-neuter hash)
grep -ac 'RED-UNDER-M: {"arm":"GUARD 8"' -> 1      (the newest edit is back)
git status --porcelain -- <file> -> (empty)
run.mjs --parse-only -> exit 0 ; vitest 4 files / 211 tests passed
```

## Task Commits

1. **Task 1 (tracer): strategies_private_owner_isolation setup line + first proven twin (RLS 1)** — `d0af9b10` (test)
2. **Task 2: twin all 29 sections of the batch-3 four** — `65c4a13b` (test)
3. **Task 3: FILES_FLOOR 9 -> 13, ARMS_FLOOR 134 -> 163, every pin in lockstep** — `2a19dbe1` (feat)

**Plan metadata:** this SUMMARY + STATE.md + ROADMAP.md (docs: complete plan)

## Files Created/Modified

- `supabase/tests/test_strategies_private_owner_isolation.sql` — setup line + 8 twins (added comment lines only)
- `supabase/tests/test_api_keys_venue_identity_uniq.sql` — setup line + 7 twins (added comment lines only)
- `supabase/tests/test_capital_ownership_column.sql` — setup line + 7 twins (added comment lines only)
- `supabase/tests/test_csv_daily_returns_perkey_rls.sql` — setup line + 7 twins (added comment lines only)
- `scripts/pg-lane/fixtures/10-fixture-strategies-rls-baseline.sql` — drops 01-fixture-core's narrower stand-in `strategies_read`, restores production's table grants, adds `strategies.source`, `profiles.created_at` and three empty relations `20260405061912` writes policies for
- `scripts/pg-lane/fixtures/11-fixture-api-keys-created-at.sql` — `api_keys.created_at`, read by `20260811210000`'s census block and dated backfill cutoff
- `scripts/pg-lane/fixtures/12-fixture-profiles-is-admin.sql` — `profiles.is_admin`, DEFAULT false on purpose
- `scripts/mutation-runner/run.mjs` — `FILES_FLOOR` 9→13 and `ARMS_FLOOR` 134→163 with dated blocks and both separation directions; `--self-test` and derived-class currency
- `scripts/mutation-runner/GRAMMAR.md`, `CLAUDE.md`, `.github/workflows/ci.yml` — currency; lineage blocks untouched
- `src/__tests__/gate-family-meta.test.ts` — `FILES_FLOOR=13`, `ARMS_FLOOR=163` in `KNOWN_THRESHOLD_SITES`
- `src/__tests__/mutation-runner-floors.test.ts` — `totalAnchored` 134→163; `GREEN_LOG` rebuilt at the measured numbers with thirteen per-file rows; every `GREEN_LOG`-derived RED arm re-cut
- `src/__tests__/mutation-annotation-parser.test.ts` — `armsSeen` 163, `stepsSeen` 143, needles 143, `scanCorpus` exact pin now lists all thirteen basenames (`filesTotal` stays 71)
- `.planning/phases/164.4-…/deferred-items.md` — one out-of-scope finding (see Issues Encountered)

## Decisions Made

See `key-decisions` in the frontmatter. The three most consequential for later batches:

- **Read every arm's PRECONDITIONS against production before accepting a green baseline.** Three arms
  here passed against a stand-in policy narrower than the real one, one of them structurally
  unfalsifiable. `ALL PASS` on the lane is not evidence that an arm CAN fail; only the twin's RED is.
- **A runtime SKIP is answered by the apply list, and the answer is read off the baseline.** Where a
  gate prints a state-aware summary, that line is the machine evidence for which state it ran in —
  better than any grep the executor could invent.
- **The silent-drift rule binds only where the raise ESCAPES a handler.** `6e` in
  `test_api_keys_venue_identity_uniq.sql` is a positive control whose 23505 the gate itself catches
  and re-raises as `TEST FAILED (6e)`, so it takes the LITERAL drift (a second, tenant-blind unique
  index). Compare `GUARD 8`, whose UPDATE has no handler at all and needs the silent form.

## Deviations from Plan

**None - plan executed exactly as written.**

Every twin bit on its first attempt; no waiver was proposed; no arm was found unable to fail; the
plan's expected numbers (`coverage: files 13/71`, `biting: 163`, 8/7/7/7 sections, `pending: 27`)
were all confirmed by measurement rather than adjusted.

## Issues Encountered

- **`.planning/WINDOWS.md` cannot be written to at all.** `gsd-tools windows fixed 28` refuses with
  `Ledger counts disagree with entries: frontmatter open/waived/fixed/total=26/0/2/28 but entries
  yield 29/0/2/31`. This is PRE-EXISTING and unrelated to anything this plan touched, so nothing was
  written to the ledger; the finding is logged to the phase's `deferred-items.md` instead. Its
  consequence is that PATTERNS § P4 item 13b's disagreement stands — `CLAUDE.md` says WINDOWS entry
  28 is closed while row 28 still reads `open` — even though the evidence for closing it is now four
  ubuntu-green `sql-mutation` runs rather than one.
- **zsh does not word-split an unquoted apply list.** The first three timing attempts died on
  `ERROR: apply file not found: <the whole list>` — exactly the trap `164.4-00-FIXTURE-STRATEGY.md`
  records. Every lane in this plan was driven from a small bash wrapper reading the list with
  `mapfile`.
- **A prose marker must begin exactly `-- RED-UNDER:`.** `PROSE_RE` in `parse.mjs:58` is
  `/^[ \t]*--[ \t]*RED-UNDER:/`, so the first draft of `test_capital_ownership_column.sql`'s stacked
  markers (`-- RED-UNDER (1):`) counted as 4 prose against 7 twins and parse-only reported a
  `parity` defect. Restored from the byte backup (shasum confirmed) and re-authored as
  `-- RED-UNDER: (assertion 1) …`.
- **⚠️ EVERY MEASUREMENT IN THIS SUMMARY IS FROM ONE HOST — macOS, PostgreSQL 16.13, this box.**
  The identity attribution reads psql's CONTEXT/LOCATION grammar, which `GRAMMAR.md` states is
  measured on macOS only. A local green is not evidence about the ubuntu runner. See D10 and
  "Next Phase Readiness".

## Known Stubs

None. Every section of all four files carries a twin that was executed and observed RED; no
placeholder, no TODO, no partially-annotated file. The section-inclusion pin armed in plan 02 would
fail the build on any of those, and it is green.

## Threat Flags

None. The diff on the four gate files is **added comment lines only** — measured over
`git diff -U0 636bb063..HEAD -- supabase/tests/`: 277 added lines, **0 non-comment added, 0
removed** — so no assertion, identity, arm or failure message changed and the `ci.yml` sentinel
gate, which counts non-comment `RAISE EXCEPTION` lines, cannot move. The remaining edits are two
ratchet constants, test pins and prose. No network endpoint, auth path, file-access pattern or
schema change at a trust boundary. `WAIVED_CEILING` (T-164.3-21, waiver creep) was not touched;
T-164.4-01 (twin targeting a stand-in) is refused at parse time and no twin targets
`scripts/pg-lane/fixtures/**` — the 22 `sql`-step twins that drift a stand-in-provided object target
the LIVE database object by name, never the fixture file; **T-164.4-05 (a green-SKIP section twinned
as if it bit) is the threat this batch actually exercised**, and it is closed by the baseline NOTICE
check recorded under D2 plus the thirteen per-file `judged` columns; T-164.4-08 (lane time growth on
the 8-stamp apply list) is measured: 1.08 s/lane against 0.95-0.97 s for the shorter lists, 163 arms
at mean 1.0 s, 191 s wall, `timeout-minutes` unchanged with a three-point ubuntu fit recorded beside
it.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**Ready for 164.4-07.** Its precondition — a merged PR whose head SHA has an ubuntu
`sql-mutation` run with conclusion `success` — is **NOT yet satisfiable from this
plan alone**: nothing was pushed and no PR was opened (D10 above). The orchestrator
must land commits `d0af9b10`, `65c4a13b`, `2a19dbe1` and this SUMMARY commit as one
PR, confirm `sql-mutation` GREEN on ubuntu SHA-bound to the PR head
(`gh run view <id> --json headSha,conclusion`), and write the run id, head SHA and
the job's wall clock back into coverage entry **D10**.

**What batch 07 should carry forward:**

- **Check the arm's PRECONDITIONS, not just its baseline.** A stand-in narrower than production
  (a role-scoped policy, a missing grant, absent default privileges) can make an arm
  structurally unfalsifiable while the file prints PASS. Three arms in this batch were in that
  state. Ask, per arm: what would have to be true for a mutation of the named object to be
  VISIBLE to this assertion?
- **A runtime SKIP is an apply-list problem.** Put the guarding migration in the list, then read
  the baseline for the proof — a state-aware summary line, if the gate has one, is better
  evidence than any count.
- **Reuse a sibling's apply list verbatim where one exists.** `test_capital_ownership_column.sql`
  cost ONE lane run for that reason; the other three cost 7 between them.
- **A positive control does not automatically need a silent drift.** Check whether the gate's own
  handler catches the SQLSTATE the literal drift raises; if it does, the literal drift is fine.
- **27 idiom files remain `pending:`**, plus the 4 `lane-blocked:` owed to `[REDUNDER-PGCRON]` and
  the 27 `unreachable:`. End state on today's lane: `coverage: files 40/71`.
- **CI budget:** 163 arms at mean 1.0 s = 191 s locally. The three-point ubuntu fit
  (1.27 s/arm of JOB cost + ~62 s fixed) projects 163 arms at ~4.5 min and the phase's ~265-arm
  end state at ~6.6 min. `ci.yml`'s `timeout-minutes: 15` was NOT raised; raise it in the batch
  whose MEASURED run first approaches ~10 min, and re-justify against that run rather than
  inheriting this arithmetic.

---
*Phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota*
*Completed: 2026-09-03*

## Self-Check: PASSED

All four created files and the SUMMARY exist on disk; all four commit hashes
(`d0af9b10`, `65c4a13b`, `2a19dbe1`, `c5f79c77`) resolve in `git log --all`;
`FILES_FLOOR = 13`, `ARMS_FLOOR = 163` and `WAIVED_CEILING = 0` are the live
values in `scripts/mutation-runner/run.mjs` (:266, :488, :517).
