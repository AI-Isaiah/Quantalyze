---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 08
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet]
wave: 9
depends_on: ["164.4-07"]
files_modified:
  - supabase/tests/test_csv_finalize_atomic_fold.sql
  - supabase/tests/test_derive_allocator_keys_fanout.sql
  - supabase/tests/test_funding_fees_rls.sql
  - supabase/tests/test_allocator_equity_derived_rls.sql
  - supabase/tests/test_user_notes_dashboard_scope.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 6: five files (7 + 7 + 7 + 6 + 6 = 33 sections) fully annotated and proven"
  - "FILES_FLOOR 16 → 21, ARMS_FLOOR → measured (expected 289)"
affects: [164.4-09]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the five files carries one RED-UNDER-SETUP proven GREEN and every section has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok) and all five files restoring GREEN (SC-2)"
    - "Floors equal the run's printed numbers with dated blocks and both separation directions; the scanCorpus pin lists twenty-one files (SC-3)"
    - "Waivers carry reasons in the twin and move WAIVED_CEILING by exactly their count; dead arms are TODOS findings (SC-4)"
  artifacts:
    - path: "supabase/tests/test_csv_finalize_atomic_fold.sql"
      provides: "7 sections twinned (32 identities Part 1..7 with sub-suffixes; one non-idiom raise)"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_funding_fees_rls.sql"
      provides: "7 sections twinned (0 stamps named — migrations found by object name)"
      contains: "RED-UNDER-SETUP"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 21"
      pattern: "scanCorpus"
---

<objective>
Batch 6, largest-first: `test_csv_finalize_atomic_fold.sql` (planner-measured 1277 lines, 79 idiom
raises, 32 identities `Part 1`…`Part 7c` → **7 sections**, 5 stamps, **1 non-idiom raise** — pick
mutations it does not observe), `test_derive_allocator_keys_fanout.sql` (198 lines, **7 sections**,
2 stamps), `test_funding_fees_rls.sql` (401 lines, **7 sections**, **0 stamps named**),
`test_allocator_equity_derived_rls.sql` (216 lines, 8 ids → **6 sections**, 1 stamp) and
`test_user_notes_dashboard_scope.sql` (152 lines, **6 sections**, 1 stamp). For the atomic-fold
file, a section like `Part 6` spans thirteen identities (`6a`…`6m`); one twin per section is the
requirement, a second twin on an identity testing a different bit is allowed. Re-derive with the
one-liner.

Purpose: 33 sections; run ~331 lanes.
Output: 33 twins, five setup lines, FILES_FLOOR 21, ARMS_FLOOR → measured (expected 289).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Read before implementing: Plan 00's record, Plan 07's SUMMARY, PATTERNS § P1, § P2, § P3, § P4;
RESEARCH § Common Pitfalls 3 (non-idiom raise firing first).

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: csv_finalize_atomic_fold — setup proven GREEN + ONE section end-to-end</name>
  <files>supabase/tests/test_csv_finalize_atomic_fold.sql</files>
  <precondition>164.4-07-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`).</precondition>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-08-PLAN.md` and halt on non-zero.
    Discover the apply list with the lane loop (stand-ins per Plan 00's DECISION, then the real
    last-defining migrations; `--workdir` outside the repo; additive fixture only for relations no
    listed migration creates, recorded in the SUMMARY). Write the setup line at column 0 in the
    header with its 3-line paragraph; re-prove exit 0. Locate the file's non-idiom raise first.
    Author ONE section (`Part 1`, arm `Part 1`) in the P1.1 shape with a measured `occurrences`;
    `--parse-only`; diagnostic lane; read the verdict.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_csv_finalize_atomic_fold.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>The atomic-fold file has a proven setup line and one twin whose lane reads RED (identity ok).</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all five files</name>
  <files>supabase/tests/test_csv_finalize_atomic_fold.sql, supabase/tests/test_derive_allocator_keys_fanout.sql, supabase/tests/test_funding_fees_rls.sql, supabase/tests/test_allocator_equity_derived_rls.sql, supabase/tests/test_user_notes_dashboard_scope.sql</files>
  <action>
    Discover and prove a setup line for each of the other four files (for `test_funding_fees_rls.sql`,
    which names no stamp, find migrations by the object names it exercises). Run the
    section-coverage one-liner over all five, paste it into the SUMMARY, and twin every untwinned
    section: real identity as `arm`; mutate the code under test in the last-defining listed
    migration; `occurrences` measured; `sql` step for exactly-pinned grant/policy drift; LAYERED
    `apply` for self-verifying migrations; `neuter` only for idiom arms; no fixture targets; a
    section shadowed by the non-idiom raise gets a mutation that raise does not observe, else a
    waiver naming its line. Waivers only for arms that bite but cannot be mutated mechanically,
    +1 ceiling each, >5 cumulative → STOP. Dead arm = TODOS finding. Never edit an assertion,
    message or identity; diffs are added comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_csv_finalize_atomic_fold.sql supabase/tests/test_derive_allocator_keys_fanout.sql supabase/tests/test_funding_fees_rls.sql supabase/tests/test_allocator_equity_derived_rls.sql supabase/tests/test_user_notes_dashboard_scope.sql</automated>
    Expected 7 / 7 / 7 / 6 / 6 sections, 0 untwinned each (a booked dead-arm finding is the only permitted exception).
  </verify>
  <done>All 33 sections twinned (or booked); five setup lines proven; parse-only green.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 16 → 21, ARMS_FLOOR → measured, pins and prose in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, CLAUDE.md</files>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 21/71`, `biting: 289`). In ONE commit with the annotations: Group A `run.mjs`
    floors with compact dated blocks (both separation directions, COVERAGE, RECORD), currency lines,
    `WAIVED_CEILING` +n with reasons only for accepted waivers; Group B registry strings and dates;
    Group C `totalAnchored`, `GREEN_LOG` (coverage 21/71, arms, biting, lane-invocations, mean,
    twenty-one per-file lines summing to the aggregate, spawned string), `armsSeen`/`stepsSeen`,
    scanCorpus pin "21 of 71" + names; Group D `CLAUDE.md` currency sentence. `timeout-minutes` →
    30 with the measurement only if the run is ≥ 10 min (~331 lanes; ~6.8 min projected on ubuntu
    at 1.03 s/lane, ~11.5 min at 2.0 s/lane — record the actual and decide from it). Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + wall clock in the SUMMARY.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 21/71`, `biting:` == ARMS_FLOOR; pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run and its wall clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep | medium | mitigate | reason in twin, printed; ceiling +1 per reviewed waiver; >5 → STOP |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline counted as coverage | medium | mitigate | baseline proven first; per-file `judged` read off the run |
| T-164.4-08 | Denial of Service | run approaching `timeout-minutes` | medium | mitigate | wall clock measured this batch; raise to 30 with the measurement when ≥ 10 min |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 7 / 7 / 7 / 6 / 6 sections, 0 untwinned; parse-only exit 0.
- Full run: `coverage: files 21/71`, `biting:` == ARMS_FLOOR, tallies agree, exit 0; lockstep vitest green; tree clean; `--pending` anchors green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- Five more files fully proven; floors moved by the measured delta in the same commit; the timeout decision is made from a measured wall clock.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-08-SUMMARY.md` when done. Record: one-liner output, the five apply lists, run log lines, floors old → new, waivers/findings, the measured ubuntu wall clock and the timeout decision, PR number, SHA-bound run id.
</output>
