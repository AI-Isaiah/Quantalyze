---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 08
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet, ci-timeout]
wave: 9
depends_on: ["164.4-07"]
files_modified:
  - supabase/tests/test_csv_finalize_double_submit.sql
  - supabase/tests/test_get_published_trust_signals.sql
  - supabase/tests/test_get_verified_cohort_rank_gate.sql
  - supabase/tests/test_scenario_downgrade_sweep.sql
  - supabase/tests/test_scenarios_rls.sql
  - supabase/tests/test_strategy_analytics_series_completeness.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - .github/workflows/ci.yml
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 5: six five-section files (30 sections) fully annotated and proven"
  - "FILES_FLOOR 17 → 23, ARMS_FLOOR → measured (expected 219); the sql-mutation timeout re-justified from two measured ubuntu wall clocks"
affects: [164.4-09]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the six files carries one RED-UNDER-SETUP proven GREEN and every section has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok) and all six files restoring GREEN (SC-2)"
    - "Floors equal the run's printed numbers with dated blocks and both separation directions; the scanCorpus pin lists twenty-three files; the job timeout's comment cites a dated measurement, not the 30-arm / 64 s claim (SC-3)"
    - "A waiver candidate is a STOP checkpoint to the founder; dead arms are TODOS findings (SC-4)"
  artifacts:
    - path: "supabase/tests/test_get_verified_cohort_rank_gate.sql"
      provides: "5 sections twinned (9 identities; a sentinel file)"
      contains: "RED-UNDER-SETUP"
    - path: ".github/workflows/ci.yml"
      provides: "timeout-minutes on sql-mutation re-justified with the measured wall clock (raised only if the measurement demands it)"
      contains: "timeout-minutes"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 23"
      pattern: "scanCorpus"
---

<objective>
Batch 5, largest-first: six five-section files — `test_csv_finalize_double_submit.sql` (394 lines,
13 ids → **5 sections**, 3 stamps), `test_get_published_trust_signals.sql` (158 lines, **5**, 0
stamps; a SECDEF grant gate), `test_get_verified_cohort_rank_gate.sql` (305 lines, 9 ids → **5**,
2 stamps; sentinel file), `test_scenario_downgrade_sweep.sql` (496 lines, 8 ids → **5**, 0 stamps),
`test_scenarios_rls.sql` (268 lines, **5**, 1 stamp) and
`test_strategy_analytics_series_completeness.sql` (109 lines, **5**, 1 stamp; anti-green-skip
contract in its header). Two files name no stamp — find their migrations by object name. Section
counts re-confirmed at HEAD 007708dd. Re-derive with the one-liner.

⚠️ CI budget checkpoint. After this batch the run is ~265 lanes: ≈ 4.6 min of lanes on ubuntu at
the measured 1.03 s/lane (≈ 9 min at 2.0 s/lane), plus the job's fixed non-lane overhead, against `timeout-minutes: 15` on the `sql-mutation`
job whose comment (`ci.yml:849-852`) justifies it with "30 arms x ~2.0s … 64s wall clock" — a claim
already superseded by the measured 80 s job at 32 lanes (run 33683536312). It is NOT a
`FLOOR|MIN|CEILING|MAX|LIMIT` name, so `gate-family-meta` does not police it — this plan does.
Task 3 reads Plan 07's measured ubuntu wall clock and this batch's, rewrites the comment to cite
both (dated), and raises the timeout to 30 only if either is ≥ 10 min.

Purpose: 30 sections; the timeout decision made from data.
Output: 30 twins, six setup lines, FILES_FLOOR 23, ARMS_FLOOR → measured (expected 219 = 189 + 30), timeout re-justified.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Per-task reading lists are in each task's `<read_first>`.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
<!-- verify-symbol: .github/workflows/ci.yml timeout-minutes -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: csv_finalize_double_submit — setup proven GREEN + ONE section end-to-end</name>
  <files>supabase/tests/test_csv_finalize_double_submit.sql</files>
  <precondition>164.4-07-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success AND its measured wall clock (assert with `gh run view <id> --json headSha,conclusion`).</precondition>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (the DECISION on stand-ins, the three authoring rules, the discovery loop), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-07-SUMMARY.md (apply lists; the measured ubuntu wall clock and per-lane mean this plan's timeout decision reads), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1 shapes, § P1.1 placement, § P2.1 setup line placement), scripts/mutation-runner/GRAMMAR.md (rule 4: no fixture targets), supabase/tests/test_strategy_shares_rls.sql:40-61 (the reference header shape)</read_first>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-08-PLAN.md` and halt on non-zero.
    Discover the apply list with the lane loop (stand-ins per Plan 00's DECISION, then the real
    last-defining migrations; `--workdir` outside the repo; under bash; additive fixture only for
    relations no listed migration creates, recorded in the SUMMARY). Write the setup line at column
    0 in the header with its 3-line paragraph; re-prove exit 0. Author ONE section (the first
    identity) in the P1.1 shape with a measured `occurrences`; `--parse-only`; diagnostic lane;
    read the verdict.

    ⚠️ From this point until Task 2 completes, the parser test's section-inclusion pin (armed in
    Plan 02: twin sections ⊇ identity sections for every annotated file) is EXPECTED RED for this
    file — one twin over 5 sections is exactly the state the pin refuses. Do NOT touch the pin,
    do NOT loosen it, and do not run it as a gate before Task 2; it goes green when the last
    section is twinned and is asserted in Task 3's lockstep vitest.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_csv_finalize_double_submit.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>The first file has a proven setup line and one twin whose lane reads RED (identity ok).</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all six files</name>
  <files>supabase/tests/test_csv_finalize_double_submit.sql, supabase/tests/test_get_published_trust_signals.sql, supabase/tests/test_get_verified_cohort_rank_gate.sql, supabase/tests/test_scenario_downgrade_sweep.sql, supabase/tests/test_scenarios_rls.sql, supabase/tests/test_strategy_analytics_series_completeness.sql</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (discovery loop, additive-fixture rule), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1, § P2.1-P2.3), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md (§ Waivers & un-falsifiable arms), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-RESEARCH.md (§ Common Pitfalls 3), scripts/mutation-runner/GRAMMAR.md (the sql step for SECDEF EXECUTE grant drift; Shape 1b/3 layered apply; Shape 4 — what NOT to author)</read_first>
  <action>
    Discover and prove a setup line for each of the other five files (object-name lookup for the
    two that name no stamp). Run the section-coverage one-liner over all six, paste it into the
    SUMMARY, and twin every untwinned section: real identity as `arm`; mutate the code under test
    in the last-defining listed migration; `occurrences` measured; `sql` step for exactly-pinned
    grant/policy drift (the SECDEF `get_published_trust_signals` gate: revoke/grant EXECUTE as a
    `sql` step, never edit the migration's pinned grant); LAYERED `apply` for self-verifying
    migrations; `neuter` only for idiom arms; no fixture targets. Waivers: `WAIVED_CEILING` stays
    0 — a biting-but-unmutatable arm is a STOP checkpoint to the founder. Dead arm = TODOS finding.
    Never edit an assertion, message or identity; diffs are added comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_csv_finalize_double_submit.sql supabase/tests/test_get_published_trust_signals.sql supabase/tests/test_get_verified_cohort_rank_gate.sql supabase/tests/test_scenario_downgrade_sweep.sql supabase/tests/test_scenarios_rls.sql supabase/tests/test_strategy_analytics_series_completeness.sql</automated>
    Expected 5 sections and 0 untwinned in each of the six (a booked dead-arm finding is the only permitted exception).
  </verify>
  <done>All 30 sections twinned (or booked); six setup lines proven; parse-only green.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 17 → 23, ARMS_FLOOR → measured, timeout re-justified, pins and prose in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, .github/workflows/ci.yml, CLAUDE.md</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-07-SUMMARY.md (the measured ubuntu wall clock, its lane count and per-lane mean), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P3, § P4 — every item, § P4 item 13l the timeout, § P6 the split mode that is NOT this plan's to build), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md (the locked decision on the 20-minute split threshold), .github/workflows/ci.yml:849-852 (the sql-mutation TIMEOUT comment being rewritten), scripts/mutation-runner/run.mjs (FILES_FLOOR, ARMS_FLOOR, WAIVED_CEILING and their dated blocks), src/__tests__/gate-family-meta.test.ts (KNOWN_THRESHOLD_SITES), src/__tests__/mutation-runner-floors.test.ts (GREEN_LOG, totalAnchored), src/__tests__/mutation-annotation-parser.test.ts (armsSeen, stepsSeen, scanCorpus exact pin, section-inclusion pin), CLAUDE.md (§ SQL gate integrity jobs)</read_first>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 23/71`, `biting: 219`). In ONE commit with the annotations: Group A `run.mjs`
    floors with compact dated blocks (both separation directions, COVERAGE, RECORD), currency
    lines, `WAIVED_CEILING` unedited; Group B registry strings and dates; Group C `totalAnchored`,
    `GREEN_LOG` (coverage 23/71, arms, biting, lane-invocations, mean, `  pending: 17 …`,
    twenty-three per-file lines summing to the aggregate, spawned string), `armsSeen`/`stepsSeen`,
    scanCorpus pin "23 of 71" + names; Group D `CLAUDE.md` currency sentence.
    TIMEOUT: rewrite the `sql-mutation` job's TIMEOUT comment (the one justifying the value with
    "30 arms x ~2.0s … 64s wall clock") to cite Plan 07's measured ubuntu wall clock (dated, with
    its lane count) and this batch's PROJECTION — ~265 lanes × the ubuntu per-lane mean from Plan
    07's SUMMARY + Plan 07's measured non-lane job overhead — labelled as a projection; after the
    PR's first `sql-mutation` run, replace the projection with the measured ubuntu wall clock in
    the same PR so the landed comment cites two measurements. Set `timeout-minutes` to 30 if either
    is ≥ 10 min, otherwise leave 15 and say so beside the measurement. Never derive an ubuntu
    figure by dividing a local wall clock by a constant: ubuntu is measured slightly SLOWER per
    lane than local (RESEARCH 1.03 s vs a local mean of ~1.0 s), and the 80 s job at 32 lanes is
    not a per-lane ratio either (it includes checkout, node setup and cluster init). An
    under-projection here is a job timeout that reads as a wedged cluster. Verify SERIALLY.
    ⛔ CEILING — a timeout raise is legitimate only BELOW 20 minutes measured. At or above 20 min
    on ubuntu, STOP: the CONTEXT's locked decision is that PRs then run CHANGED gate files with a
    scheduled full run enforcing the floors, and the split is PRINTED, never silent. That is its
    own plan (new runner subset mode + ci.yml work — `--file` never exits 0 today, PATTERNS § P6);
    it is NOT a further timeout raise.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && ! grep -a -q '30 arms x ~2.0s' .github/workflows/ci.yml && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head, under the timeout; record run id + SHA + wall clock in the SUMMARY. Plan 09's precondition reads it.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 23/71`, `biting:` == ARMS_FLOOR; pins moved in the same commit; the timeout comment cites two dated measurements; SUMMARY records the SHA-bound ubuntu-green run and its wall clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep | medium | mitigate | no executor-authored waivers; STOP checkpoint; WAIVED_CEILING stays 0 |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline counted as coverage | medium | mitigate | baseline proven first; per-file `judged` read off the run |
| T-164.4-08 | Denial of Service | run exceeding `timeout-minutes` and reading as a wedged cluster | medium | mitigate | timeout re-justified from two measured wall clocks in this batch; ≥ 20 min is a STOP, not a raise |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 5 sections, 0 untwinned, six times; parse-only exit 0.
- Full run: `coverage: files 23/71`, `biting:` == ARMS_FLOOR, tallies agree, exit 0; lockstep vitest green; ci.yml parses and no longer carries the 30-arm claim; tree clean; `--pending` anchors green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA, under the timeout.
</verification>

<success_criteria>
- Six more files fully proven; floors moved by the measured delta; the job timeout carries a dated measurement instead of a 30-arm claim.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-08-SUMMARY.md` when done. Record: one-liner output, the six apply lists, run log lines, floors old → new, waiver candidates / findings, both wall clocks and the timeout decision, PR number, SHA-bound run id.
</output>
