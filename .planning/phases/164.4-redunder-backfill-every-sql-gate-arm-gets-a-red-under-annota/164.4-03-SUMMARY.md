---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 03
subsystem: mutation-runner
tags: [runner-output, lane-blocked, pg_cron, deferral, criterion-4, ci-measure-fail, red-under, scope-amendment]

requires:
  - phase: 164.4-02
    provides: "the reference file at 35/35 sections, ARMS_FLOOR 45, and PR #732 merged with sql-mutation GREEN on ubuntu at head b912e4f6 (run 33743816665) — this plan's precondition"
  - phase: 164.4-01
    provides: "the `unreachable:` contract this plan copies: derived class, both print sites, the ci.yml MEASURE_FAIL pair, the exact-set pin, and the identity-carrier-is-read-off-RAW-text decision"
provides:
  - "A DERIVED fifth corpus class `lane-blocked` — the idiom gate files whose EXECUTABLE text probes `pg_extension` for pg_cron — printed BY NAME with its reason and TODO id on every run, in both modes, and MEASURE_FAILed by CI when absent or self-contradicting"
  - "The deferral CAN EXPIRE: every lane-spawning run drives a `probe` lane leg asking `pg_available_extensions` for pg_cron, prints what it MEASURED as `lane-probe:`, and raises `lane-blocked-stale` (exit 1) when pg_cron is available while the class is non-empty — proven by --self-test scenario 17/17"
  - "[REDUNDER-PGCRON] corrected by measurement: 4 files not 3, 100 of 355 sections not 71, and 2 RAISE + 2 green-skip rather than the blanket all-files-RAISE the ledgers claimed"
  - "SCOPE AMENDMENT #2 (founder 2026-09-03) written into all six ROADMAP sites and all three STATE sites, with criterion 1's silent-exclusion sentence kept VERBATIM — counts moved, the standard did not"

affects: [164.4-04, every batch plan after it, 164.4-11 end state]

actuals:
  tokens: 24285
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "A derived exclusion class must be PROBED against the thing its printed reason is about. `lane-blocked` is derived from the corpus; 'the pg-lane cannot host pg_cron' is a fact about the LANE, and a reason nothing measures is a reason that cannot expire"
    - "The expiry arm reddens the gate when the blocker is FIXED. `lane-blocked-stale` exits 1 the day pg_cron arrives, so closing [REDUNDER-PGCRON] forces the four files to be annotated instead of leaving them parked"
    - "A fixture PAIR that is byte-identical except the comment markers, asserted as byte identity by the test, so the classification flip is attributable to the comment and the pair cannot drift into two unrelated fixtures"
    - "Markers a stub must answer are DERIVED from the production constants (`PROBE_ABSENT_OUTPUT = \\`NOTICE:  ${LANE_PROBE_ABSENT}\\``), so a stub cannot answer with a string the real reader would reject"

key-files:
  created:
    - scripts/mutation-runner/fixtures/lane-probe/pg-cron-probe.sql
    - scripts/mutation-runner/fixtures/selftest/lane-blocked/lane-blocked-gate.sql
    - scripts/mutation-runner/fixtures/selftest/lane-blocked/lane-blocked-comment-only-gate.sql
  modified:
    - scripts/mutation-runner/parse.mjs
    - scripts/mutation-runner/run.mjs
    - .github/workflows/ci.yml
    - src/__tests__/mutation-annotation-parser.test.ts
    - src/__tests__/mutation-runner-floors.test.ts
    - src/__tests__/gate-family-meta.test.ts
    - TODOS.md
    - CLAUDE.md
    - .planning/ROADMAP.md
    - .planning/STATE.md

key-decisions:
  - "The probe runs when `targets.length > 0 || corpus.laneBlockedFiles.length > 0` — every run that would spawn a lane anyway, PLUS any run asserting a non-empty lane-blocked class. A run that is neither spawns nothing, so cluster-free modes (the print-contract pins) do not acquire a PostgreSQL dependency."
  - "`pg_available_extensions`, not `pg_extension`. The question is what the BINARY the lane booted could install — its control files on disk — not what this throwaway database happens to have created. `pg_extension` would answer 'no' forever on a fresh cluster even after pg_cron was installed on the runner: a false negative that would keep the deferral alive by construction."
  - "The runner reads the probe's MARKER, never its exit status. The probe RAISEs on AVAILABLE and NOTICEs on absent, so reading the status would conflate 'pg_cron is here' with 'the lane broke'."
  - "`unreachable` is decided BEFORE `lane-blocked`. `test_retention_crons_safe.sql` probes pg_cron AND raises outside the identity idiom; it stays unreachable, because the reason no arm of it can be judged is the idiom, not the lane — a file cannot be deferred out of a class it was never in."
  - "`lane-blocked-stale` is NOT in `NON_BITING_DEFECT_KINDS`. It is a finding about the corpus's SCOPE, not a verdict on any arm, so it must not subtract from `biting` and move a floor."
  - "`lane-invocations:` text is UNCHANGED and the probe leg is never compared to `arms:`. ci.yml parses that line and the absurdity floor's exact equality is asserted on `laneLegs.arm` alone; the probe count rides on the result object as evidence."
  - "CI asserts the lane-probe line's PRESENCE only, never that it says `absent`. The runner already exits 1 on AVAILABLE through `lane-blocked-stale` and the job fails on its exit status; re-deciding it in ci.yml would be a second judge of one question. CI's job is to notice the probe went MISSING."

patterns-established:
  - "Establish a classification's expected counts by MEASUREMENT BEFORE the edit (a standalone node script over the live corpus), never by reading the finished implementation — a count derived from the artifact always agrees with it"
  - "Grep the file for the neuter you just applied BEFORE reading the test result: a substitution that silently did not match turns a green suite into evidence of nothing"

requirements-completed: [SC-1, SC-3, SC-4]

coverage:
  - id: D1
    description: "Every run, full or --parse-only, prints `lane-blocked: 4 file(s) …` naming exactly the four deferred files with the reason and `(deferred 2026-09-03, TODOS [REDUNDER-PGCRON])`, and the `  pending:` line no longer lists them (SC-1, SC-4)"
    requirement: "SC-1"
    verification:
      - kind: other
        ref: "node scripts/mutation-runner/run.mjs --parse-only -> exit 0; the four `.sql` tokens on the lane-blocked line, extracted and compared as one sorted single-spaced string, equal `test_derive_allocator_keys_fanout.sql test_reconcile_dropped_enqueue_sweep.sql test_retention_orphaned_running.sql test_strategy_analytics_stuck_computing_reaper.sql`"
        status: pass
      - kind: other
        ref: "`grep -a -E '^  pending: ' | grep -a -q 'test_reconcile_dropped_enqueue_sweep.sql'` -> exit 1 (the pending line no longer names them); `^  pending: 39 idiom file\\(s\\) ` -> exit 0"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-annotation-parser.test.ts#scanCorpus names the 4 lane-blocked files EXACTLY — the deferral is a measured set, not a hand list"
        status: pass
    human_judgment: false
  - id: D2
    description: "The class is DERIVED from the corpus (a live `pg_extension` probe in EXECUTABLE text), not hard-coded: a fixture carrying the probe only inside a `--` comment classifies pending (SC-4)"
    requirement: "SC-4"
    verification:
      - kind: unit
        ref: "src/__tests__/mutation-annotation-parser.test.ts#a pg_cron probe in a `--` COMMENT does not make a gate lane-blocked — the derivation reads code, not prose"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-annotation-parser.test.ts#uncommenting the same guard flips it from pending to lane-blocked"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-annotation-parser.test.ts#the fixture PAIR differs ONLY in the three `--` markers on the pg_cron guard — asserts byte identity after the substitution"
        status: pass
      - kind: other
        ref: "REAL-CORPUS negative controls, measured: `test_wizard_composite_fence.sql:698` mentions pg_cron only in a comment and stays `pending`; `test_retention_crons_safe.sql` probes it in code but is non-idiom and stays `unreachable`. 6 files mention pg_cron in raw bytes, 5 probe the catalog, 4 are idiom."
        status: pass
    human_judgment: false
  - id: D3
    description: "ci.yml MEASURE_FAILs when the `lane-blocked:` line is absent and when its claimed count disagrees with the `.sql` names beside it; both arms observed RED by neutering the ci.yml condition, then restored from a byte backup (SC-3)"
    requirement: "SC-3"
    verification:
      - kind: unit
        ref: "src/__tests__/mutation-runner-floors.test.ts#RED: the lane-blocked line ABSENT is a MEASURE_FAIL — a silent deferral fails like a missing annotation (runs the LIVE countRecheckScript() extracted from ci.yml)"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-runner-floors.test.ts#RED: a CLAIMED deferred count that disagrees with the NAMES printed beside it fails, quoting both numbers"
        status: pass
      - kind: other
        ref: "PROVEN ABLE TO FAIL — see the anti-vacuity table below, rows CI-1 and CI-2. ci.yml sha256 594f299f… identical after each restore."
        status: pass
    human_judgment: false
  - id: D4
    description: "The parser test pins the exact 4-name set and the five-class partition invariant annotated + pending + unreachable + inert + lane-blocked = 71 (SC-3)"
    requirement: "SC-3"
    verification:
      - kind: unit
        ref: "src/__tests__/mutation-annotation-parser.test.ts#the FIVE classes sum to filesTotal — annotated + pending + unreachable + inert + lane-blocked -> 1 + 39 + 27 + 0 + 4 = 71"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-annotation-parser.test.ts#the five classes PARTITION the corpus, checked against a SECOND derivation (re-derived from readdirSync + the two primitives, compared set for set)"
        status: pass
      - kind: other
        ref: "PROVEN ABLE TO FAIL — anti-vacuity row A: classifyGateIdiom neutered to never return \"lane-blocked\" -> 3 parser arms RED (`expected [] to deeply equal [ …(4) ]`)"
        status: pass
    human_judgment: false
  - id: D5
    description: "FILES_FLOOR, ARMS_FLOOR and WAIVED_CEILING are UNEDITED by this plan (1 / 45 / 0) and the measured run is recorded (SC-3)"
    requirement: "SC-3"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs -> exit 0; coverage files 1/71; arms: 45/45/0; biting: 45; lane-invocations: 45; file test_strategy_shares_rls.sql: sections 35 / judged 45 / annotated 45 / waived 0 / biting 45; per-arm lane time: mean 0.9s over 45 arm run(s); ✅ No defects"
        status: pass
      - kind: other
        ref: "`node -e import(run.mjs)` -> FILES_FLOOR 1 ARMS_FLOOR 45 WAIVED_CEILING 0; `git diff` over run.mjs touches no constant, only the FILES_FLOOR CURRENCY comment"
        status: pass
      - kind: unit
        ref: "src/__tests__/gate-family-meta.test.ts#every threshold carries a measurement token and a date within the window -> `ok scripts/mutation-runner/run.mjs :: ARMS_FLOOR=45`, `FILES_FLOOR=1`, `WAIVED_CEILING=0`"
        status: pass
    human_judgment: false
  - id: D6
    description: "⭐ The lane-blocked reason is NOT a constant that can outlive its cause: every lane-spawning run prints a `lane-probe:` line MEASURED on the lane; pg_cron AVAILABLE with a non-empty class is a `lane-blocked-stale` MEASURE_FAIL that exits 1; an unreadable probe is a MEASURE_FAIL; a --self-test scenario injects an AVAILABLE probe and observes the exit-1 (SC-3, SC-4)"
    requirement: "SC-4"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --self-test -> exit 0, `SELF-TEST PASSED`, 17/17. Scenario 17/17 drives the A/B: AVAILABLE -> exitCode 1 with EXACTLY ONE defect, `lane-blocked-stale` naming lane-blocked-gate.sql and saying `the deferral has expired`; absent -> exitCode 0 with NO defects. Same corpus, same stub, one marker apart."
        status: pass
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_strategy_shares_rls.sql --arm \"NO-DELETE 1\" -> `lane-probe: pg_cron absent — lane-blocked class is current`, measured through a REAL pg-lane leg"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-runner-floors.test.ts#RED: the lane-probe line ABSENT is a MEASURE_FAIL and #RED: the runner's UNREADABLE probe form does not satisfy the lane-probe assertion"
        status: pass
      - kind: other
        ref: "PROVEN ABLE TO FAIL — anti-vacuity rows B (rule disabled -> `SELF-TEST FAIL: AVAILABLE: exit code is 1 (got 0)`, --self-test exits 1), C (marker read disabled -> 3 floors arms RED) and CI-3."
        status: pass
    human_judgment: false
  - id: D7
    description: "The ledger says what is REACHABLE: six ROADMAP sites and three STATE sites carry SCOPE AMENDMENT #2; criterion 1's silent-exclusion sentence survives verbatim; the pre-amendment count claims are gone (SC-1, SC-4)"
    requirement: "SC-1"
    verification:
      - kind: other
        ref: "POSITIVE: `grep -c 'SCOPE AMENDMENT #2' .planning/ROADMAP.md` -> 6 (>= 5); `.planning/STATE.md` -> 2 (>= 2); `grep -q 'a silent exclusion fails this criterion just' ROADMAP` -> exit 0; `grep -q '100 sections' ROADMAP` -> exit 0; `grep -q 'DEFERRED 2026-09-03' TODOS.md` -> exit 0; `lane-blocked` and `lane-probe` in CLAUDE.md -> exit 0"
        status: pass
      - kind: other
        ref: "NEGATIVE (each exit 1 == the pre-amendment claim is gone), ROADMAP body scoped to heading..`**Plans:**` (110 lines, >= 40): `~365 twins`, `1 file to 44, behind a floor`, `**44 idiom files**`, `in the 44 IDIOM files carries`; whole-file: `1/44 idiom files annotated today`. STATE whole-file: `~365 twins`, `3 idiom gate files (71 of 355`, `those files RAISE on its absence`."
        status: pass
      - kind: other
        ref: "node scripts/verify-plan-anchors.mjs --pending -> exit 0, 11 plan files, 152 claims checked, no stale claims — the ledger edits and the run.mjs line shifts broke no downstream plan anchor"
        status: pass
    human_judgment: false
  - id: D8
    description: "The plan's <human-check>: land this batch as its own PR and confirm sql-mutation GREEN on ubuntu SHA-bound to the PR head, with the lane-blocked and lane-probe lines visible in the job log, recording run id + SHA + wall clock"
    requirement: "SC-3"
    verification:
      - kind: other
        ref: "PENDING — the executor was instructed not to push, not to open a PR and not to merge. Commits produced on branch `phase-164.4-redunder-backfill`: 0a9ed7ce (Task 1), 508847e9 (Task 2), 1611ece2 (Task 3), plus the SUMMARY commit. Base at dispatch c8dedfac. The orchestrator lands these and writes the run id, head SHA and wall clock back into this entry."
        status: unknown
    human_judgment: true
    rationale: "Requires a PR head SHA and an ubuntu CI run, neither of which an executor forbidden to push can produce. Every LOCAL equivalent was run and is green — full corpus run, --self-test 17/17, the three lockstep vitest files, the full 838-file vitest suite, tsc --noEmit, npm run lint — but a local macOS green is ONE HOST, not the SHA-bound ubuntu evidence the gate between batches requires. Plan 04's precondition reads this entry."

duration: 35 min
completed: 2026-09-03
status: complete
---

# Phase 164.4 Plan 03: The lane-blocked class, the lane probe, and SCOPE AMENDMENT #2 Summary

**The runner now DERIVES, PRINTS and PROBES the four-file pg_cron deferral — `lane-blocked:` names them with their reason every run, `lane-probe:` says what the lane itself answered, and `lane-blocked-stale` exits 1 the day pg_cron arrives, so 100 deferred sections cannot be parked behind a true-looking line.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-09-03T10:53:01Z
- **Completed:** 2026-09-03T11:28:14Z (code); SUMMARY written immediately after
- **Tasks:** 3 (1 tracer + 2 auto)
- **Files modified:** 13 (3 created, 10 modified)

## Accomplishments

- A fifth corpus class, `lane-blocked`, derived by `gateNeedsPgCron` from the gate's EXECUTABLE text and printed at BOTH print sites with its reason and TODO id.
- A per-run **lane probe** — a fourth lane leg — so the deferral's reason is MEASURED against the lane rather than asserted, and expires the day pg_cron lands.
- `sql-mutation` MEASURE_FAILs on an absent or self-contradicting `lane-blocked:` line and on a missing `lane-probe:` line, each arm driven from vitest through the LIVE ci.yml `run:` block.
- SCOPE AMENDMENT #2 written to nine ledger sites, with criterion 1's teeth kept verbatim, and `[REDUNDER-PGCRON]`'s mechanism corrected by re-measurement: **4 files, 100 sections, 2 RAISE + 2 green-skip** — the ledgers had said three files, 71 sections, all RAISE.

## Task Commits

1. **Task 1 (tracer): the lane-blocked class + the lane probe** — `0a9ed7ce` (feat)
2. **Task 2: CI MEASURE_FAIL arms + GREEN_LOG + four RED arms** — `508847e9` (test)
3. **Task 3: ledgers corrected by measurement, SCOPE AMENDMENT #2** — `1611ece2` (docs)

**Plan metadata:** see the `docs(164.4-03)` commit carrying this SUMMARY.

## The two new lines, verbatim

`--parse-only` (no lane spawned, so no probe line):

```
coverage: files 1/71
unreachable: 27 file(s) raise outside the runner's identity idiom — … (TODOS [REDUNDER-NONIDIOM])
lane-blocked: 4 file(s) probe pg_extension for pg_cron, which the pg-lane cannot host — test_derive_allocator_keys_fanout.sql test_reconcile_dropped_enqueue_sweep.sql test_retention_orphaned_running.sql test_strategy_analytics_stuck_computing_reaper.sql (deferred 2026-09-03, TODOS [REDUNDER-PGCRON])
  pending: 39 idiom file(s) without RED-UNDER — test_allocator_equity_derived_rls.sql … test_wizard_session_idempotency.sql
arms: 0/45/0   (executed/annotated/waived)
```

Full run — the `lane-blocked:` line is byte-identical, and the probe line is added:

```
coverage: files 1/71
lane-blocked: 4 file(s) probe pg_extension for pg_cron, which the pg-lane cannot host — test_derive_allocator_keys_fanout.sql test_reconcile_dropped_enqueue_sweep.sql test_retention_orphaned_running.sql test_strategy_analytics_stuck_computing_reaper.sql (deferred 2026-09-03, TODOS [REDUNDER-PGCRON])
lane-probe: pg_cron absent — lane-blocked class is current
  pending: 39 idiom file(s) without RED-UNDER — …
arms: 45/45/0   (executed/annotated/waived)
biting: 45   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 45   (arm lanes actually spawned — tallied inside runLane, independent of the 45 the verdict loop counted; plus 1 baseline / 1 restore leg(s))
  file test_strategy_shares_rls.sql: sections 35 / judged 45 / annotated 45 / waived 0 / biting 45
per-arm lane time: mean 0.9s over 45 arm run(s)

✅ No defects. Every annotated arm bit its own arm first.
```

Exit 0. `FILES_FLOOR 1 / ARMS_FLOOR 45 / WAIVED_CEILING 0` — **unchanged by this plan**, read back out of the module rather than off the source line. Wall clock 45 s for 48 lanes (1 probe + 1 baseline + 45 arms + 1 restore).

The `lane-blocked-stale` self-test scenario is **17/17**.

## Anti-vacuity: every new control watched to fail

Every neuter used a `cp` byte backup and was restored by `cp` — never `git checkout --`, which restores to HEAD and would have destroyed the uncommitted work in the same file. Each restore was verified by `shasum -a 256` against the pre-neuter hash AND by grepping for the newest edit.

| # | Control | Neuter | Observed RED (verbatim) | Restore |
|---|---|---|---|---|
| A | the lane-blocked class itself (parser pins) | `classifyGateIdiom` → `return "pending";` | `× uncommenting the same guard flips it from pending to lane-blocked` / `× scanCorpus names the 4 lane-blocked files EXACTLY` / `× the FIVE classes sum to filesTotal` — `AssertionError: expected [] to deeply equal [ …(4) ]`, `expected [] to have a length of 4 but got +0`. 3 failed / 97 passed | parse.mjs sha256 `7c1758bb…` identical; `lane-blocked" : "pending"` back |
| B | the `lane-blocked-stale` rule | `if (false && laneProbe.available === true && …)` | `SELF-TEST FAIL: AVAILABLE: exit code is 1 (got 0)` → `=== SELF-TEST FAILED ===`, `--self-test` exit 1 | run.mjs sha256 `64259054…` identical |
| C | the probe's absent-marker read (the MEASURE_FAIL path) | `else if (false) laneProbe = { available: false };` | 3 floors arms RED: `AssertionError: the stub must have ANSWERED the probe leg: expected false to be true` (severed / ENOENT / SIGKILL) | run.mjs sha256 `64259054…` identical |
| CI-1 | ci.yml lane-blocked count/name cross-check | `if [ "$lane_blocked_claimed" -ne "$lane_blocked_named" ]` → `if false` | `× RED: a CLAIMED deferred count that disagrees with the NAMES printed beside it fails, quoting both numbers`. 1 failed / 76 passed | ci.yml sha256 `594f299f…` identical |
| CI-2 | ci.yml lane-blocked line read | `$lane_blocked_line` overwritten with a constant | `× RED: the lane-blocked line ABSENT is a MEASURE_FAIL` **and** `× RED: a CLAIMED deferred count …`. 2 failed / 75 passed | ci.yml sha256 `594f299f…` identical |
| CI-3 | ci.yml lane-probe line read | `$lane_probe_line` overwritten with a constant | `× RED: the lane-probe line ABSENT is a MEASURE_FAIL` **and** `× RED: the runner's UNREADABLE probe form does not satisfy the lane-probe assertion`. 2 failed / 75 passed | ci.yml sha256 `594f299f…` identical |

⚠️ **CI-3's first attempt was a broken instrument, not a green.** A `perl -pi -e` substitution did not match the line (the escaping of the alternation group was wrong), the suite reported `Tests 77 passed` and, read naively, that would have been recorded as "the arm did not fire". It was caught by grepping the file for the neuter *before* reading the result — the line was still the original `grep`. Redone with a Python exact-literal replacement asserting `count == 1`, which then produced the two RED arms above. This is the same class as landmine 3 (`${PIPESTATUS[0]}` under zsh) and it is recorded because the failure mode is *silent green*.

The `lane-blocked-stale` scenario is additionally its own control: the run with the absent marker is the **same corpus, same stub, one marker apart**, and it exits **0 with zero defects** while the AVAILABLE run exits **1 with exactly one defect**. The exit code is therefore attributable to the measurement and to nothing else in the corpus.

## The nine ledger sites amended

| # | File | Site | What it now says |
|---|---|---|---|
| a | ROADMAP.md | Phase 164.4 **Goal** line + its coverage sentence | "Every arm in **the 40 idiom files the pg-lane can reach** (4 more are printed by the runner as lane-blocked every run — SCOPE AMENDMENT #2, founder 2026-09-03, 100 sections owed to `[REDUNDER-PGCRON]`)"; coverage now "moves from 1 file to those 40 reachable files … with the **27 excluded non-idiom files and the 4 deferred lane-blocked files each printed by name on every run**". The old "1 file to 44" destination is gone from the paragraph. |
| b | ROADMAP.md | the 2026-09-02 narrowing's goal sentence | the bold 44-file phrase REPLACED (not appended to) by "the **40 idiom files the pg-lane can reach** (of 44 — 4 lane-blocked, SCOPE AMENDMENT #2, founder 2026-09-03, `[REDUNDER-PGCRON]`)". |
| c | ROADMAP.md | NEW ⚖️ **SCOPE AMENDMENT #2** block | the founder decision, the per-file mechanism as re-measured, the 40/255 vs 4/100 split, and how the deferral expires. Explicitly: "This is criterion 4 applied, not criterion 4 waived." |
| d | ROADMAP.md | the section-unit paragraph | "**255 sections / ~265 twins** across the 40 reachable idiom files; the 100 sections in the 4 lane-blocked files are owed to `[REDUNDER-PGCRON]`". The 516-identity sentence stays as lineage. |
| e | ROADMAP.md | criterion 1 | opens "Every arm in the 40 REACHABLE idiom files carries a `RED-UNDER`"; names BOTH excluded sets; **last sentence kept VERBATIM** — "a silent exclusion fails this criterion just as a missing annotation does". |
| f | ROADMAP.md | phase table row | "40 reachable idiom files + 4 lane-blocked (SCOPE AMENDMENT #2)" replaces "1/44 idiom files annotated today". |
| g | STATE.md | the arm-unit line | "**Arm unit = SECTION** (not per-raise, not per-identity)" kept VERBATIM; only the counts restated: 255 / ~265 over 40 reachable, 100 owed. |
| h | STATE.md | ⚖️ SCOPE AMENDMENT #2 paragraph under the 2026-09-02 narrowing | the decision, the corrected mechanism, and the "recorded in EIGHT places" note with the same v1.17 lesson the first amendment carries. |
| i | STATE.md | the `[REDUNDER-PGCRON]` blocker line | **4** files (not 3), **100** of 355 sections (not 71), "2 RAISE + 2 green-skip" replacing the blanket all-files-RAISE clause; keeps "needs a lane-substrate plan" and the TODOS pointer. |

Plus `TODOS.md` `[REDUNDER-PGCRON]` (mechanism per file with line numbers, the `DEFERRED 2026-09-03` founder line, how the deferral expires — **not closed**), `CLAUDE.md` § SQL gate integrity jobs (one sentence naming both lines and `lane-blocked-stale`), and `run.mjs`'s `FILES_FLOOR` currency comment (reachable denominator 40, not 44; the constant itself unedited).

### The measurement that corrected the record

Established **before** any edit, by a standalone node script over the live corpus (never by reading the finished implementation):

- 71 `.sql` files. **6** mention `pg_cron` in raw bytes; **5** probe `pg_extension` for it in executable code; **4** of those 5 are idiom files.
- `test_wizard_composite_fence.sql:698` — comment only → stays `pending`.
- `test_retention_crons_safe.sql` — probes in code but non-idiom → stays `unreachable`.
- Post-edit: 1 annotated + 39 pending + 27 unreachable + 0 inert + **4 lane-blocked** = 71.

Per-file mechanism, re-read at HEAD — and **two of the four do not RAISE**, which is what the ledgers had wrong:

| File | Sections | Mechanism |
|---|---|---|
| `test_reconcile_dropped_enqueue_sweep.sql` | 39 | `:268-269` `IF NOT EXISTS (… 'pg_cron') THEN RAISE EXCEPTION 'TEST FAILED (1/JOB-04) …'` — baseline never GREEN |
| `test_retention_orphaned_running.sql` | 25 | `:212-213`, same shape, `1/JOB-05` — baseline never GREEN |
| `test_strategy_analytics_stuck_computing_reaper.sql` | 29 | baseline **GREEN**; `RAISE NOTICE 'SKIP …'` at `:282` (Part 1b + Parts 2-3), `:326`, `:483` withholds `1/JOB-02`, `1/JOB-03`, `2…`, `3…` |
| `test_derive_allocator_keys_fanout.sql` | 7 | baseline **GREEN**, does not raise; `:159 IF EXISTS (… 'pg_cron') THEN` guards ASSERTION 6, `:169` prints `pg_cron not present — skipping cron assertion` |

**= 100 of 355 sections.** All four deferred together under CONTEXT's rule that each plan lands its files fully proven — no file left half-annotated.

## Deviations from Plan

### Auto-fixed issues

**1. [Rule 3 — blocking] The fixture pair had to move one directory deeper, and the probe condition had to widen.**
- **Found during:** Task 1, wiring the self-test scenario.
- **Issue:** the plan places both fixtures directly in `fixtures/selftest/` and runs the probe on "every mode that spawns lanes". Two things collide with that. (i) The floors test's PRINT CONTRACT arm runs `runCorpus({ scopeDir: SELFTEST_DIR, onlyFile: "no-such-gate.sql" })` with the REAL `runLane`, relying on "no lane is spawned, so no cluster is needed here"; a lane-blocked file sitting in `SELFTEST_DIR` would have made that cluster-free vitest arm boot a real PostgreSQL cluster. (ii) The scenario corpus is deliberately all-unannotated, so `targets` is empty and a bare `targets.length > 0` condition would never probe it.
- **Fix:** both fixtures moved to `fixtures/selftest/lane-blocked/` (`readdirSync` is non-recursive, so `SELFTEST_DIR` and `FIXTURE_CORPUS` are untouched — the plan's own sanctioned fallback, "move both new fixtures under a subdirectory and scope the scenario there"), and the probe condition widened to `targets.length > 0 || corpus.laneBlockedFiles.length > 0` — every run that would spawn a lane anyway, plus any run asserting a non-empty class. The rationale is written at the call site.
- **Why this is better, not just different:** the scenario now gets an exit-0 control. AVAILABLE → exit 1 with exactly one defect; absent → exit 0 with zero defects. Had the fixtures stayed in `SELFTEST_DIR`, the only reachable scenario shape carried unrelated defects in both arms and the control could assert an absence but never a clean pass.
- **Commit:** `0a9ed7ce`.

**2. [Rule 2 — missing critical] A fourth ci.yml RED arm: the runner's `UNREADABLE` probe form must not satisfy the grep.**
- **Issue:** the plan names three RED arms. But the runner prints a third probe form — `lane-probe: UNREADABLE …` — when the probe lane returns no marker. That IS a printed `lane-probe:` line, so a lazier grep (`^lane-probe:`) would accept it and a missing measurement would pass CI with only the in-process MEASURE_FAIL carrying the burden.
- **Fix:** the ci.yml grep is `^lane-probe: pg_cron (absent|AVAILABLE)`, and a fourth vitest arm pins that the UNREADABLE form still fails — asserting first that the substituted line IS a lane-probe line, so the arm cannot pass by the substitution silently not applying.
- **Commit:** `508847e9`.

**3. [Rule 3 — lockstep] Four pin sites the plan's checklist did not name.**
- **Found during:** Task 1, exactly the class 164.4-02-SUMMARY warned about.
- **Issue and fix:**
  - `src/__tests__/gate-family-meta.test.ts` carries three `armNeedle`s spelling `SELF-TEST 9/16`, `11/16`, `12/16`. Bumping the denominator to `/17` breaks them. Bumped in the SAME commit (a fourth file, not in the plan's `files_modified`).
  - `mutation-runner-floors.test.ts`'s defect-kind coverage pin is an EXACT SET; `lane-blocked-stale` had to be added to `exercised` with its reason. It went RED first (`expected [ 'absurdity', …(9) ] to deeply equal [ …(8) ]`) — which is itself the evidence that scenario 17 really exercises the kind.
  - The three vitest lane stubs (`severedLane`, `dead`, `killed`) needed probe branches, plus a `noProbeMeasureFail` assertion each, so a stub that stops answering the probe fails loudly rather than carrying a spurious second `lane-unrunnable`.
  - `ci.yml`'s self-test denominator comment `16/16` → `17/17` (landed in Task 2's commit, since ci.yml is Task 2's file).
- **Commits:** `0a9ed7ce`, `508847e9`.

**4. [Rule 1 — bug] Criterion 1's teeth sentence had to be re-wrapped.**
- **Issue:** rewriting criterion 1's opening clause pushed "a silent exclusion" onto the end of the previous line, so the plan's line-based verify grep `'a silent exclusion fails this criterion just'` returned exit 1 — the sentence survived semantically but not as a matchable line, which is exactly what that grep exists to catch.
- **Fix:** re-flowed so the phrase sits on one line. Grep now exit 0. The sentence's words are unchanged.
- **Commit:** `1611ece2`.

**5. [Design, recorded not hidden] `logCorpusClassification` gained an optional third parameter.**
- The plan asks for `lane-probe:` "right after the `lane-blocked:` line". Both print sites share this one function, and `--parse-only` must print no probe line at all. Rather than opening a second print seam, the function takes `laneProbe = null` and prints the line between `lane-blocked:` and `  pending:` only when a measurement exists. `null` deliberately means "not measured", never "absent" — the same could-not-measure-is-not-measured-zero rule the NO-DEFAULTS check already applies to the classes.
- **Commit:** `0a9ed7ce`.

---

**Total deviations:** 5 (1 blocking-shape, 1 missing-critical, 1 lockstep, 1 bug, 1 recorded design choice).
**Impact on plan:** no floor, ceiling or pin was weakened; every change adds a control or keeps an existing one from going vacuous. Nothing was descoped.

## Issues Encountered

- **The `<human-check>` is NOT satisfiable by the executor and is OPEN.** It requires the batch landed as its own PR with `sql-mutation` GREEN **on ubuntu**, SHA-bound to the PR head. This executor was instructed not to push, not to open a PR and not to merge. Every local equivalent was run and is green — but **a local macOS green is one host, not the ubuntu SHA-bound evidence** the between-batches gate requires, and the two hosts have diverged before (`reference_ci_node22_vs_local_node25`). Recorded as coverage D8, `status: unknown`, `human_judgment: true`. The orchestrator lands it and writes the run id, head SHA and wall clock back into that entry; **164.4-04's precondition reads it.**
- **A neuter that silently did not apply reported as a pass.** See the ⚠️ under the anti-vacuity table. Caught, redone, recorded.
- **`gsd-tools query state.advance-plan` REFUSES on this repo** (`ambiguous_position_phase` — the Current Position section carries two `Phase:` entries). Pre-existing and already documented in STATE.md § Known Issues; the position was advanced by hand, 3/13 → 4/12 (the phase was replanned to 12 plans on 2026-09-03). `state.update-progress`, `state.record-metric`, `state.add-decision`, `state.record-session` and `roadmap.update-plan-progress` all ran normally.
- **`requirements.ready-ids` returned 0 of 3 ready.** Correct, not a failure: SC-1/SC-3/SC-4 are declared by sibling plans 04–11 that have no SUMMARY yet, so the shared-ID gate holds them. `REQUIREMENTS.md` is untouched by this plan.
- **280 vitest tests are skipped suite-wide.** Pre-existing, unrelated, unchanged by this plan — the same figure 164.4-02 recorded. Stated rather than omitted.
- **No database was contacted.** Every lane — including the new probe leg — ran through `scripts/pg-lane/run.sh` on throwaway clusters under `$TMPDIR`. No `supabase` CLI invocation; this checkout is linked to PRODUCTION. The probe reads `pg_available_extensions` on the throwaway cluster and installs nothing; pg_cron was deliberately NOT installed (threat T-164.4-01).
- **Self-test wall clock is now ~55 s** (17 scenarios, most driving real lanes, plus one probe lane each). Well inside `timeout-minutes: 15`; recorded so a future reader can attribute the increase to the probe rather than to a regression.

## Full local evidence

| Check | Result |
|---|---|
| `node scripts/mutation-runner/run.mjs` | exit 0 · 45 s · `arms: 45/45/0` · `biting: 45` · `lane-invocations: 45` · `✅ No defects` |
| `node scripts/mutation-runner/run.mjs --parse-only` | exit 0 · lane-blocked 4 named sorted · pending 39 · no lane-probe line |
| `node scripts/mutation-runner/run.mjs --self-test` | exit 0 · `SELF-TEST PASSED` · 17/17 |
| `--file … --arm "NO-DELETE 1"` | exit 2 (narrowed never exits 0) · `lane-probe: pg_cron absent` through a real lane |
| `python3 -c "yaml.safe_load(ci.yml)"` | exit 0 · `lane-blocked` ×7, `lane-probe` ×3 in ci.yml |
| vitest — parser + floors + gate-family-meta | 198 passed |
| **vitest — FULL suite, serially** | **838 files passed, 19 skipped · 13882 tests passed, 280 skipped · 101 s** |
| `tsc --noEmit` | exit 0 |
| `npm run lint` | 0 errors, 3 pre-existing warnings; admin-route, route-contract and planning-hygiene checks OK |
| `node scripts/verify-plan-anchors.mjs --pending` | exit 0 · 11 plans · 152 claims · no stale claims |
| `git status --porcelain` | EMPTY after each task commit |

## User Setup Required

None.

## Next Phase Readiness

**164.4-04 is blocked ONLY on the human-check** (coverage D8): the batch must land as its own PR with `sql-mutation` GREEN on ubuntu, SHA-bound to the PR head, with the `lane-blocked:` and `lane-probe:` lines visible in the job log. Everything else it inherits is in place:

- **The `pending:` denominator is now 39, not 43.** Every batch plan from 04 on annotates out of 39 idiom files; the four pg_cron files are OUT of every batch and the runner says so on every run. A batch plan that still names one of them is planning against a stale corpus.
- **The lockstep-pin tax is now measured twice.** 164.4-02 paid four pins its checklist did not name; this plan paid four more of a different kind (`gate-family-meta` armNeedles, the defect-kind exact set, three lane stubs, the ci.yml denominator comment). Any plan that changes runner OUTPUT SHAPE or the self-test scenario COUNT should budget for both classes and grep all of `src/__tests__` and `.github/workflows` for the literal before editing it.
- **`WAIVED_CEILING` is still 0** and no waiver exists in the corpus. The first real waiver remains a founder decision.
- **Per-run cost grew by one lane.** The full run is 48 lanes at ~0.9 s (45 s wall clock locally) and `--self-test` is ~55 s. The ubuntu measurement for the previous shape was 102 s; expect roughly +1 lane's worth. The trigger to revisit the CI split is still a measured run approaching ~10 minutes.
- **`ARMS_FLOOR` stays 45 and `FILES_FLOOR` stays 1.** This plan annotated nothing; it changed what the runner SAYS about the corpus it does not annotate.

## Self-Check: PASSED

- `scripts/mutation-runner/parse.mjs` — FOUND (`gateNeedsPgCron`, `laneBlockedFiles`)
- `scripts/mutation-runner/run.mjs` — FOUND (`lane-blocked:`, `lane-probe:`, `lane-blocked-stale`, `LANE_LEGS` with `probe`)
- `scripts/mutation-runner/fixtures/lane-probe/pg-cron-probe.sql` — FOUND (`pg_available_extensions`)
- `scripts/mutation-runner/fixtures/selftest/lane-blocked/lane-blocked-gate.sql` — FOUND
- `scripts/mutation-runner/fixtures/selftest/lane-blocked/lane-blocked-comment-only-gate.sql` — FOUND
- `.github/workflows/ci.yml` — FOUND (`lane-blocked` ×7, `lane-probe` ×3)
- `src/__tests__/mutation-annotation-parser.test.ts` — FOUND (exact-set pin, five-class sum, the pair)
- `src/__tests__/mutation-runner-floors.test.ts` — FOUND (GREEN_LOG + 4 RED arms)
- `src/__tests__/gate-family-meta.test.ts` — FOUND (`/17` armNeedles)
- `TODOS.md` — FOUND (`DEFERRED 2026-09-03`)
- `CLAUDE.md` — FOUND (`lane-blocked`, `lane-probe`)
- `.planning/ROADMAP.md` — FOUND (`SCOPE AMENDMENT #2` ×6)
- `.planning/STATE.md` — FOUND (`SCOPE AMENDMENT #2` ×2)
- Commit `0a9ed7ce` — FOUND
- Commit `508847e9` — FOUND
- Commit `1611ece2` — FOUND

---
*Phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota*
*Completed: 2026-09-03*
