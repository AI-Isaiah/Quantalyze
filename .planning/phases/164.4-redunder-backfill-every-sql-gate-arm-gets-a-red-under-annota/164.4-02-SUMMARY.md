---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 02
subsystem: mutation-runner
tags: [red-under-m, batch, reference-file, lockstep-pins, floor-ratchet, section-inclusion]

requires:
  - phase: 164.4-01
    provides: "the SECTION unit (sectionOfIdentity / gateSectionCount), the per-file breakdown, the unreachable print, and PR #731 merged with sql-mutation GREEN on ubuntu at f44a517c"
  - phase: 164.3
    provides: "scripts/mutation-runner/ (parser, runner, floors, self-test) and the sql-mutation CI job"
provides:
  - "The reference file meets criterion 1 under the SECTION unit: all 35 sections carry a twin (15 new), every one proven RED under its own mutation with the file GREEN on restore"
  - "ARMS_FLOOR ratcheted 30 -> 45, pinned at the measured biting count, with both separation directions driven through the real verdict loop on real lanes"
  - "A DURABLE half-annotation control: the section-inclusion pin in mutation-annotation-parser.test.ts asserts, per annotated file, that the SET of twin sections CONTAINS the SET of identity sections — not a count"
  - "The whole lockstep-pin choreography (PATTERNS § P4 groups A-D) rehearsed once at scale 15 before it runs at scale 41"
  - "Currency prose made count-free at seven sites so later batches touch fewer of them"
affects: [164.4-03, every batch plan after it]

actuals:
  tokens: 13500
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "SET INCLUSION, never a count, for a coverage pin: `annotated >= sections` is satisfied by two twins on half the sections, which is exactly the half-annotated file the pin exists to refuse"
    - "A section reached only with an earlier layer neutered says so in its own prose, naming the arms neutered and why — the discipline REACTIVATE 1g already used"
    - "A twin whose mutation would abort the migration's own apply-time verification disables that verification IN THE SAME `apply`, the Shape 3 layering SHAPE 4d/4e established"
    - "A dated floor block carries a SEPARATION line — the value measured SILENT and value+1 measured FIRING — so the pin is known to sit exactly at the separation point rather than below it"

key-files:
  created: []
  modified:
    - supabase/tests/test_strategy_shares_rls.sql
    - scripts/mutation-runner/run.mjs
    - scripts/mutation-runner/GRAMMAR.md
    - scripts/mutation-runner/VAC06-DEMOS.md
    - scripts/pg-lane/README.md
    - src/__tests__/gate-family-meta.test.ts
    - src/__tests__/mutation-runner-floors.test.ts
    - src/__tests__/mutation-annotation-parser.test.ts
    - .github/workflows/ci.yml
    - CLAUDE.md

key-decisions:
  - "The arm chosen for a section is the identity that ACTUALLY speaks under the mutation, not the section's first identity. TENANT 3b (not 3a), SERVICE-ROLE 1-grant (not SERVICE-ROLE 1) and REVOKE 1b (not 1c) were each selected because the file's own prose predicts that arm as the first failure under the natural regression — and the lane confirmed it."
  - "Grant/policy/ACL drift is always a `sql` step. Migration STEP 2b pins the authenticated ACL exactly and STEP 5/6 pin the function shapes, so an edit aborts the apply and no arm of the gate can be a first failure. Post-apply is also the faithful shape: the drift these arms catch is a later ALTER, not a rewritten migration."
  - "TRIGGER 1a weakens rule (2) from `<=` to `<` rather than deleting the block. `<` is already refused by rule (1), so the rule survives as text (satisfying STEP 6 arm (v-b)'s regex probe) and enforces nothing — one honest step plus one layered probe-disable instead of a fragile whole-block deletion."
  - "MONOTONIC 1a reuses TRIGGER 1a's mutation with TRIGGER 1a/1b neutered, rather than removing the revoke's `generation = generation + 1`. Measured reason: with the bump gone the trigger's rule (2) refuses revoke_strategy_share outright, so the RPC raises a non-TEST-FAILED error and NO arm of the file is attributable."
  - "ARMS_FLOOR moved but FILES_FLOOR did not, and that asymmetry is stated at both constants. No new FILE was annotated; SECTION coverage moved WITHIN the one annotated file, 20/35 -> 35/35."
  - "No waiver was authored and WAIVED_CEILING stays 0. All 15 sections took a mechanical mutation, so there was no waiver candidate to escalate."

patterns-established:
  - "Author every twin from the file's live bytes via a generator script that MEASURES each `occurrences` at write time — a needle transcribed by hand into a plan or a commit message is a needle that can be stale"
  - "Restore a neutered pin from a `cp` byte backup and re-verify by sha256 AND by grepping for the newest change; `git checkout --` restores to HEAD and would silently destroy uncommitted work"

requirements-completed: [SC-1, SC-2, SC-3, SC-4]

coverage:
  - id: D1
    description: "Every section of test_strategy_shares_rls.sql has a RED-UNDER prose marker and an adjacent RED-UNDER-M twin; the parity gate passes; the section-coverage one-liner reports 0 untwinned (SC-1)"
    requirement: "SC-1"
    verification:
      - kind: other
        ref: "the plan's Task 2 section-coverage one-liner -> `supabase/tests/test_strategy_shares_rls.sql sections 35 untwinned 0`, exit 0 (baseline before this plan: `sections 35 untwinned 15`, exit 1)"
        status: pass
      - kind: other
        ref: "node scripts/mutation-runner/run.mjs --parse-only -> exit 0, `test_strategy_shares_rls.sql: 45 prose / 45 twin(s) / 0 waiver(s)`"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-runner-floors.test.ts#any file carrying structured twins must have one per prose marker"
        status: pass
    human_judgment: false
  - id: D2
    description: "The full-corpus run exits 0 with every new twin RED (identity ok) and the file GREEN on restore; biting == executed == lane-invocations (SC-2)"
    requirement: "SC-2"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs -> exit 0; coverage files 1/71; arms: 45/45/0; biting: 45; lane-invocations: 45; file test_strategy_shares_rls.sql: sections 35 / judged 45 / annotated 45 / waived 0 / biting 45; per-arm lane time: mean 0.9s over 45 arm run(s); baseline exit 0 and restore exit 0"
        status: pass
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --self-test -> `SELF-TEST PASSED`, 16/16 scenarios"
        status: pass
    human_judgment: false
  - id: D3
    description: "ARMS_FLOOR equals the biting count read off THAT run, with a dated MEASURED block and both separation directions beside the constant; FILES_FLOOR stays 1 because no new FILE was annotated (SC-3)"
    requirement: "SC-3"
    verification:
      - kind: other
        ref: "scripts/mutation-runner/run.mjs `export const ARMS_FLOOR = 45;` with the `RE-DERIVED 2026-09-03 (plan 164.4-02)` block above it; `export const FILES_FLOOR = 1;` unchanged with a dated CURRENCY note explaining why"
        status: pass
      - kind: other
        ref: "SEPARATION via runCorpus({armsFloor}) on real lanes: `armsFloor=45  biting=45  floor-defects=0  SILENT` and `armsFloor=46  biting=45  floor-defects=1  FIRES -> ARMS_FLOOR regression: 45 biting arm(s) < floor 46`"
        status: pass
      - kind: unit
        ref: "src/__tests__/gate-family-meta.test.ts#every threshold carries a measurement token and a date within the window, and the found set is exactly KNOWN_THRESHOLD_SITES -> `ok scripts/mutation-runner/run.mjs :: ARMS_FLOOR=45 (line 237)`"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-runner-floors.test.ts#GREEN: a log whose two tallies agree passes and says so — runs the LIVE ci.yml count-recheck step against the moved GREEN_LOG"
        status: pass
    human_judgment: false
  - id: D4
    description: "Any waiver candidate is a STOP-and-escalate checkpoint, not an executor decision: WAIVED_CEILING stays 0 unless the founder accepts a named waiver on the record; a dead arm is a TODOS finding, not a waiver (SC-4)"
    requirement: "SC-4"
    verification:
      - kind: other
        ref: "`export const WAIVED_CEILING = 0;` unchanged; run prints `arms: 45/45/0` — W field 0; 0 waiver candidates arose and 0 dead arms were found, so nothing was escalated and nothing was booked to TODOS.md"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-runner-floors.test.ts#RED: a waived count above WAIVED_CEILING fails naming both numbers — the W field is read, not ignored"
        status: pass
    human_judgment: false
  - id: D5
    description: "⭐ The SECTION-INCLUSION pin is ARMED for every file in corpus.annotatedFiles, asserted as an empty `missing` array naming the file and every untwinned section — NOT a count comparison"
    requirement: "SC-1"
    verification:
      - kind: unit
        ref: "src/__tests__/mutation-annotation-parser.test.ts#164.4-02: every SECTION an annotated file raises for also carries a twin — set inclusion, not a count"
        status: pass
      - kind: other
        ref: "PROVEN ABLE TO FAIL: dropped the single NO-DELETE 1 twin line (its section carries exactly one twin, so `have` shrinks) -> RED with `{\"test_strategy_shares_rls.sql\":[\"NO-DELETE 1\"]}`; restored from a byte backup, sha256 e83808f3… identical, `git status --porcelain` empty for the file"
        status: pass
    human_judgment: false
  - id: D6
    description: "The plan's <human-check>: land this batch as its own PR and confirm sql-mutation GREEN on ubuntu SHA-bound to the PR head, recording run id + SHA + wall clock"
    requirement: "SC-2"
    verification:
      - kind: other
        ref: "NOT DONE BY THIS EXECUTOR — no PR opened, nothing pushed, no CI run exists yet for these three commits (b5fa08c2 / c850a790 / 9cffb584). The orchestrator lands the batch and must record the run id, head SHA and wall clock here before plan 03 starts."
        status: pending
    human_judgment: true
    rationale: "The executor was instructed not to open a PR and not to push. The local equivalents were run instead and are all green (full run, --self-test, four lockstep vitest files, the full 838-file vitest suite, tsc, eslint), but a local macOS green is a measurement of one host and is NOT the ubuntu SHA-bound evidence plan 03's precondition reads."

duration: 46 min
completed: 2026-09-03
status: complete
---

# Phase 164.4 Plan 02: Close the reference file Summary

**`test_strategy_shares_rls.sql` finally is what it was described as — the fully annotated reference: all 35 of its sections now carry a `RED-UNDER-M` twin (15 new), every one of the 45 arms bit its own arm first on the very first full run, `ARMS_FLOOR` is ratcheted to that measured 45 with both separation directions driven on real lanes, and the half-annotation hole is closed durably by a SET-INCLUSION pin that a count comparison could never have closed.**

## Performance

- **Duration:** 46 min
- **Started:** 2026-09-03T09:26:00Z
- **Completed:** 2026-09-03T10:12:00Z
- **Tasks:** 3
- **Files modified:** 10 (0 created, 10 modified)

## Accomplishments

- **Criterion 1 holds for the one file that already claimed it.** The reference file was described as "1 file fully annotated" and measured at 30 twins over 20 of its 35 sections. The 15 gaps — `SHAPE 2, TENANT 1, TENANT 2, TENANT 3, TENANT 4, NO-DELETE 1, REVOKE 1, REVOKE 2, TRIGGER 1, TRIGGER 2, MONOTONIC 1, ANON 1, ANON 2, SERVICE-ROLE 0, SERVICE-ROLE 1` — are closed.
- **All 45 arms bit on the FIRST full run.** No `wrong-first-failure`, no `no-red`, no `occurrence-mismatch`, no iteration. That is a consequence of measuring every needle before writing it and of reading each section's own prose for which identity the regression makes speak.
- **The ratchet moved in the same commit as the annotations,** with every Group A–D checklist item, so the batch cannot land half-pinned.
- **The durable half-annotation control is armed and was watched to fail.** Per annotated file: `sections(twins) ⊇ sections(identities)`, asserted as an empty `missing` array. A file that later grows a 39th section without a twin for it now fails the build — which the batch plans' own one-liner cannot do, because that runs only while a batch plan is executing.
- **Seven currency-prose sites made count-free,** so the remaining nine batches re-touch fewer of them.

## Task Commits

1. **Task 1 (tracer): `NO-DELETE 1` end to end — twin, parse, lane RED, restore GREEN** — `b5fa08c2`
2. **Task 2: the remaining 14 sections** — `c850a790`
3. **Task 3: ARMS_FLOOR 30 → 45, the section-inclusion pin, and every lockstep pin and prose site** — `9cffb584`

**Plan metadata:** this commit (`docs(164.4-02)`).

## The measured run — quoted from the log, never from a grep of the file

```
$ node scripts/mutation-runner/run.mjs          # exit 0, at HEAD c850a790
  baseline  supabase/tests/test_strategy_shares_rls.sql — exit 0 (0.9s)
  ... 45 arms, every one `exit   3  RED (identity ok)` ...
  restore   supabase/tests/test_strategy_shares_rls.sql — exit 0 (0.9s)

coverage: files 1/71
arms: 45/45/0   (executed/annotated/waived)
biting: 45   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 45   (arm lanes actually spawned — tallied inside runLane, independent of the 45 the verdict loop counted; plus 1 baseline / 1 restore leg(s))
  file test_strategy_shares_rls.sql: sections 35 / judged 45 / annotated 45 / waived 0 / biting 45
per-arm lane time: mean 0.9s over 45 arm run(s)

✅ No defects. Every annotated arm bit its own arm first.
```

**The two independent tallies AGREE:** `arms:` executed **45** (counted in `runCorpus`'s verdict loop) and `lane-invocations:` **45** (counted inside `runLane` at the spawn). That is the v0.77.1.0 control, and it is the reason `judged 45 / biting 45` is a measurement rather than a restatement.

**Section-coverage one-liner**, before and after:

```
before:  supabase/tests/test_strategy_shares_rls.sql sections 35 untwinned 15 SHAPE 2 | TENANT 1 | TENANT 2 | TENANT 3 | NO-DELETE 1 | REVOKE 1 | REVOKE 2 | TRIGGER 1 | MONOTONIC 1 | TRIGGER 2 | TENANT 4 | ANON 1 | ANON 2 | SERVICE-ROLE 0 | SERVICE-ROLE 1      (exit 1)
after:   supabase/tests/test_strategy_shares_rls.sql sections 35 untwinned 0                                                                                                                                                                                        (exit 0)
```

## ARMS_FLOOR: 30 → 45, and why it is exactly 45

`biting: 45` was read off the run log above, never off the file. Both separation directions were then driven through the REAL verdict loop on REAL lanes (`runCorpus({ scopeDir, armsFloor })` — the same function the CLI calls):

| Floor tried | biting | floor defects | verdict |
|---|---|---|---|
| **45** (the pinned value) | 45 | 0 | **SILENT** |
| 46 (one above) | 45 | 1 | **FIRES** — `ARMS_FLOOR regression: 45 biting arm(s) < floor 46` |

So 45 is the separation point itself, not a comfortable value below it.

`FILES_FLOOR` stays **1** and `WAIVED_CEILING` stays **0**, both with a dated note saying why: no new FILE was annotated (SECTION coverage moved *within* the one annotated file, 20/35 → 35/35), and no waiver was authored.

## What the 15 new twins actually mutate

| Section | Arm | Mutation | Layering |
|---|---|---|---|
| SHAPE 2 | `SHAPE 2a` | `ALTER FUNCTION create_strategy_share(uuid) SECURITY DEFINER` | — |
| TENANT 1 | `TENANT 1a` | `ALTER POLICY … WITH CHECK (created_by = auth.uid())` (drops the CR-01 EXISTS half) | — |
| TENANT 2 | `TENANT 2a` | same, + the TENANT 1 mint NULLed in the gate | neuter `TENANT 1a`, `TENANT 1b` |
| TENANT 3 | `TENANT 3b` | `ALTER POLICY … WITH CHECK (EXISTS (…))` (drops the `created_by` half) | — |
| TENANT 4 | `TENANT 4a` | `ALTER POLICY … USING (true)` | — |
| NO-DELETE 1 | `NO-DELETE 1` | `GRANT DELETE ON strategy_shares TO authenticated` | neuter `SHAPE 3` |
| REVOKE 1 | `REVOKE 1b` | drop `revoked_at = now(),` from STEP 4's UPDATE | — |
| REVOKE 2 | `REVOKE 2a` | drop `AND revoked_at IS NULL` from STEP 4's UPDATE | + disable STEP 6 (i-b) |
| TRIGGER 1 | `TRIGGER 1a` | trigger rule (2) `<=` → `<` (already refused by rule (1)) | + disable STEP 6 (v-b) |
| MONOTONIC 1 | `MONOTONIC 1a` | same two steps | neuter `TRIGGER 1a`, `TRIGGER 1b` |
| TRIGGER 2 | `TRIGGER 2a` | delete trigger rule (1) entirely | + disable STEP 6 (v-a) |
| ANON 1 | `ANON 1a` | `GRANT SELECT ON strategy_shares TO anon` | — |
| ANON 2 | `ANON 2` | `CREATE POLICY zz_anon_read … FOR SELECT TO anon USING (true)` | — |
| SERVICE-ROLE 0 | `SERVICE-ROLE 0-acl` | `GRANT EXECUTE ON FUNCTION revoke_strategy_share(uuid) TO service_role` | — |
| SERVICE-ROLE 1 | `SERVICE-ROLE 1-grant` | same GRANT | neuter `SERVICE-ROLE 0-acl` |

Three arms speak only with an earlier layer neutered, and each says so in its own prose rather than in a SUMMARY: that is defence in depth working, not a weakness in the twin. `git diff` for Tasks 1+2: **113 added lines, 0 removed, 0 of them non-comment.**

## Anti-vacuity proofs (neuter → observe RED → restore)

Every pin this plan ADDED or CHANGED was watched to fail. Restores are from `cp` byte backups, re-verified by `shasum -a 256` **and** by grepping for the newest change. **`git checkout --` was never used** — it restores to HEAD and would have destroyed uncommitted work.

### 1. The new SECTION-INCLUSION pin

```
neuter:   dropped line 1135, the `{"arm":"NO-DELETE 1",…}` twin — a section carrying
          exactly ONE twin, so the `have` SET actually shrinks
command:  ./node_modules/.bin/vitest run src/__tests__/mutation-annotation-parser.test.ts \
            -t "set inclusion, not a count"
observed: × 164.4-02: every SECTION an annotated file raises for also carries a twin — set inclusion, not a count
          AssertionError: SECTION coverage gap. … {"test_strategy_shares_rls.sql":["NO-DELETE 1"]}: expected { …(1) } to deeply equal {}
restore:  cp <byte backup> → sha256 e83808f35455eff8aef58aecf916d702cd843dcf558f5b41dc460b3859ef7320 (identical),
          `"arm":"NO-DELETE 1"` present, `git status --porcelain -- <gate>` empty
re-green: 1 passed
```

### 2. `KNOWN_THRESHOLD_SITES :: ARMS_FLOOR=45` (Group B lockstep)

```
neuter:   the registry string back to `ARMS_FLOOR=30`
observed: × every threshold carries a measurement token and a date within the window, and the found set is exactly KNOWN_THRESHOLD_SITES
          AssertionError: a threshold site not in KNOWN_THRESHOLD_SITES … + ["scripts/mutation-runner/run.mjs :: ARMS_FLOOR=45"]
restore:  sha256 980f0f2238cc15e3b3d0668fdab81d2a28562d7eba60b1f59c8406d727f318f6 (identical), `ARMS_FLOOR=45` present
```

### 3. `GREEN_LOG` vs the LIVE `ci.yml` count-recheck (Group C item 10 — the hard lockstep)

```
neuter:   GREEN_LOG's `biting: 45` and the per-file row's `biting 45` back to 30
command:  vitest -t "GREEN: a log whose two tallies agree passes and says so"
observed: × GREEN: a log whose two tallies agree passes and says so
          floors from run.mjs: FILES_FLOOR=1 ARMS_FLOOR=45 WAIVED_CEILING=0
          ::error::ARMS_FLOOR regression: 30 biting arm(s) < floor 45. An annotation stopped biting,
                   was deleted, or was converted to a waiver without lowering the floor in scripts/mutation-runner/run.mjs.
restore:  sha256 a6105fc2818a6ed17f59d5bd1bba3e5b9fb2c3c803320d83a6123902bd08d483 (identical)
```

This is the arm that proves the GREEN_LOG fixture is a **pin**, not decoration: it extracts the LIVE `ci.yml` step, which reads the LIVE floors out of `run.mjs`.

### 4. `totalAnchored` (Group C item 7)

```
neuter:   expect(totalAnchored).toBe(45) → toBe(30)
observed: × counts markers at line start only — the PARSER's count is STRICTLY below a naive substring count
          AssertionError: expected 45 to be 30
restore:  sha256 a6105fc2… (identical), `expect(totalAnchored).toBe(45);` present
```

### 5. Four further corpus-count pins, observed RED against the grown corpus *before* they were moved

These fired on the real bytes during the Task 3 verify run, which is the same evidence in the other direction — the corpus moved and the pins refused it:

| Pin | Observed failure | Moved to |
|---|---|---|
| `result.structured` length (parser test :513) | `expected [ { arm: 'SHAPE 1', …(5) }, …(44) ] to have a length of 30 but got 45` | 45 |
| `MEASURED.stmtEndLine` for SHAPE 1 (:1223) | `expected { arm: 'SHAPE 1', …(3) } to match object …` | 2599 → **2712** (the `$$;` closer travelled; 380 / 207 / CONTEXT 174 did not) |
| needle count (:1358) | `expected 59 to be 49` | 59 |
| `result.prose` length (:1499) | `expected […] to have a length of 30 but got 45` | 45 |

### 6. The floor's own separation

Recorded in the table above and verbatim in the dated block beside the constant.

## Deviations from Plan

### Auto-fixed issues

**1. [Rule 3 — blocking] Four corpus-count pins the plan's Group C did not name.**
- **Found during:** Task 3, first run of the four lockstep vitest files.
- **Issue:** the plan's Group C named `totalAnchored`, `GREEN_LOG`, `armsSeen` and `stepsSeen`. `mutation-annotation-parser.test.ts` carries four MORE pins over the same corpus — `result.structured` length (30), `result.prose` length (30), the rule-3b needle count (49), and the hand-measured SHAPE 1 attribution literals including `stmtEndLine: 2599`. All four went RED against the grown file.
- **Fix:** all four re-measured and moved (45, 45, 59, 2712), each with a dated RE-MEASURED note. The SHAPE 1 literals were re-derived by calling `gateAttributionRecords` on the live bytes, and only the end line moved — recorded explicitly, because a reader would reasonably expect the raise line to move too.
- **Files:** `src/__tests__/mutation-annotation-parser.test.ts`. **Commit:** `9cffb584`.
- **Why this is not scope creep:** they are the same lockstep class the plan's Group C exists for, in a file `files_modified` already names. Leaving them stale is a red build.

**2. [Rule 1 — stale claim] `VAC06-DEMOS.md` still said `sql-mutation` had never run on ubuntu.**
- **Found during:** Task 3, Group D sweep of the same file.
- **Issue:** the "Everything here was measured on macOS" bullet asserted the jobs "have not yet run on an ubuntu runner". That has been false since 2026-09-02 (`workflow_dispatch` run 33620169220 at `89cbef8b`), and `CLAUDE.md` already records it.
- **Fix:** the original sentence kept in the past tense as lineage, plus a dated CURRENCY note citing the run id — and keeping the honest caveat that one green run measures one host.
- **Files:** `scripts/mutation-runner/VAC06-DEMOS.md`. **Commit:** `9cffb584`.

**3. [Rule 1 — stale claim] `GRAMMAR.md` header still said the reference file's 15 sections were outstanding.**
- **Issue:** `GRAMMAR.md:3-5` read "backfills 43 more idiom gate files … plus the 15 un-twinned sections of the reference file". This plan closed those 15, so the sentence became false the moment the twins landed.
- **Fix:** rewritten count-free and dated.
- **Files:** `scripts/mutation-runner/GRAMMAR.md`. **Commit:** `9cffb584`.

**4. [Rule 1 — stale claim] `run.mjs`'s self-test preamble said "the REAL ARMS_FLOOR is 30".**
- **Fix:** → 45, date moved to 2026-09-03. It is a currency claim about the live value, not a dated historical measurement, so it moves.

### Deliberately NOT done

- **No PR, no push.** The executor was explicitly instructed that the orchestrator lands the batch. Task 3's `<human-check>` is therefore **open** — see "Issues Encountered".
- **`timeout-minutes` left at 15.** Measured 45 lanes at ~0.9 s locally; the ubuntu measurement at 32 lanes was 80 s. Nowhere near the ~10-minute trigger PATTERNS § P4 item 13l sets. The justifying COMMENT was rewritten to stop restating an arm count and to point at the run's own `per-arm lane time:` line instead.
- **`parse.mjs:1014` ("unchanged at 1 of 71") and `run.mjs:400` ("1 of 71 — see FILES_FLOOR") left alone.** Both are still TRUE — `FILES_FLOOR` did not move — and `parse.mjs` is not in this plan's `files_modified`.
- **Dated historical blocks left untouched.** `run.mjs` lines carrying the 2026-08-29 and 2026-09-01 measurements, `VAC06-DEMOS.md`'s pasted run log, `GRAMMAR.md`'s `arms: 30/30/0` Shape 4 sentence and `CLAUDE.md`'s run-33620169220 quote are all LINEAGE. Each gained a CURRENCY note beside it rather than being rewritten.
- **`GRAMMAR.md:330` "All 104 identities in today's corpus" left alone** — no identity was added, only comments, so it is still true.
- **No entry added to `TODOS.md` or `.planning/WINDOWS.md`.** No dead arm, no waiver candidate, no stub, no skipped test and no unrun `<verify>` arose from this plan.

## Issues Encountered

- **⛔ The `<human-check>` is NOT satisfied, and nothing in this plan can satisfy it.** It requires `sql-mutation` GREEN **on ubuntu**, SHA-bound to a PR head. No PR was opened and nothing was pushed, so no CI run exists for `b5fa08c2` / `c850a790` / `9cffb584`. **Plan 03's precondition reads this.** The orchestrator must land the batch and write the run id, head SHA and job wall clock into D6 above before plan 03 starts. Everything runnable locally was run and is green, but a local macOS green is a measurement of one host.
- **The worktree had no `node_modules`.** GSD provisions none, and on this repo `npx vitest` DOWNLOADS a different vitest while `npx tsc` resolves a joke package. Resolved by symlinking the main checkout's `node_modules` and invoking everything from `./node_modules/.bin/`. Not a code change; recorded because every batch executor after this one hits it.
- **`53a55e1c` is not an ancestor of this worktree's HEAD**, so the literal reading of Task 1's precondition ("the tree is at or after merge commit 53a55e1c") is false. It was satisfied in substance and verified as such: `git diff --stat origin/main HEAD` over `scripts/mutation-runner/`, `supabase/tests/`, `src/__tests__/`, `.github/workflows/ci.yml` and `CLAUDE.md` is **EMPTY** — every file this plan touches is byte-identical to `origin/main`, which *is* `53a55e1c`. The branch is 29 commits ahead of main in `.planning/` docs only. The `sql-mutation` half was asserted directly: `gh run view 33683536312 --json headSha,conclusion` → `{"headSha":"f44a517c776318e77c86ddac8f498b083253e7be","conclusion":"success"}`.
- **No database was contacted.** Every lane ran through `scripts/pg-lane/run.sh` on throwaway clusters under `$TMPDIR` (`mkdtempSync(join(tmpdir(), "mutation-runner-"))`). No `supabase` CLI invocation; this checkout is linked to PRODUCTION.
- **280 vitest tests are skipped suite-wide.** Pre-existing, unrelated to this plan, unchanged by it. Stated rather than omitted.

## User Setup Required

None.

## Next Phase Readiness

**Plan 03 is BLOCKED on one thing and one thing only:** the ubuntu SHA-bound `sql-mutation` green for this batch (D6 above). Everything else it inherits is in place:

- The lockstep-pin choreography has now been executed end to end at scale 15, and it cost **four more pins than the plan's checklist named**. Plan 04 (the ledger family, ~41 arms) should expect the same four: `result.structured`, `result.prose`, the needle count, and — whenever the annotated file is `test_strategy_shares_rls.sql` — the SHAPE 1 `stmtEndLine` literal.
- The section-inclusion pin is armed **for every file in `corpus.annotatedFiles`**, so from now on a batch that annotates a new file must twin *every* section of it in the same PR or the build fails. That is a real constraint on how batches may be split, and it is now enforced rather than described.
- Per-lane cost is **~0.9 s** at 45 arms locally. Straight-line to the phase's ~260-arm end state that is well inside `timeout-minutes: 15`; the trigger to revisit is a measured run approaching ~10 minutes.
- `WAIVED_CEILING` is still 0 and no waiver template exists in the corpus. The first real waiver remains a founder decision.

## Self-Check: PASSED

- `supabase/tests/test_strategy_shares_rls.sql` — FOUND (modified, 45 prose / 45 twins)
- `scripts/mutation-runner/run.mjs` — FOUND (`ARMS_FLOOR = 45`)
- `scripts/mutation-runner/GRAMMAR.md` — FOUND
- `scripts/mutation-runner/VAC06-DEMOS.md` — FOUND
- `scripts/pg-lane/README.md` — FOUND
- `src/__tests__/gate-family-meta.test.ts` — FOUND (`ARMS_FLOOR=45`)
- `src/__tests__/mutation-runner-floors.test.ts` — FOUND
- `src/__tests__/mutation-annotation-parser.test.ts` — FOUND (section-inclusion pin)
- `.github/workflows/ci.yml` — FOUND
- `CLAUDE.md` — FOUND
- Commit `b5fa08c2` — FOUND
- Commit `c850a790` — FOUND
- Commit `9cffb584` — FOUND
- `git status --porcelain` — EMPTY
