---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 09
subsystem: mutation-runner
tags: [red-under-m, red-under-setup, batch, pg-lane, floor-ratchet, lockstep-pins, reduced-scope, redunder-pgcron, redunder-savepoint, redunder-laneblocked-blind]

requires:
  - phase: 164.4-00
    provides: "the DECIDED stand-in fixture strategy, the discovery loop, and the three authoring rules every twin here obeys"
  - phase: 164.4-02
    provides: "the SECTION-INCLUSION pin that is live for every file in corpus.annotatedFiles"
  - phase: 164.4-08
    provides: "FILES_FLOOR 23 / ARMS_FLOOR 219, and the ubuntu run 33854344121 (219 arms, 333 s, mean 1.0s) this plan's precondition asserts"
provides:
  - "FIVE of the six planned gate files fully annotated and PROVEN: 20 sections, every one with a prose RED-UNDER and a machine twin that reddened its OWN arm first on a real throwaway pg-lane cluster"
  - "A full-corpus run at exit 0 with `coverage: files 28/71`, `arms: 239/239/0`, `biting: 239`, tallies agreeing, 0 defects"
  - "FILES_FLOOR 23 -> 28 and ARMS_FLOOR 219 -> 239, ratcheted to the run's OWN printed values with both separation directions driven on real lanes; every lockstep pin moved with them"
  - "SC-4's named hazard discharged: test_wizard_composite_fence.sql's three SKIP notices are INERT, measured, with the no-skip summary notices quoted"
  - "WAIVED_CEILING held at 0 through a founder-decided ROOT-CAUSE FIX to an un-twinnable arm, the second such decision in this phase"

actuals:
  tokens: 21501
  tasks: 3
  commits: 12

tech-stack:
  added: []
  patterns:
    - "`CREATE TABLE IF NOT EXISTS` in a stand-in fixture SHADOWS the real migration's own `CREATE TABLE IF NOT EXISTS`, and the shadow is silent until something downstream names a column the stand-in lacks. Order the REAL migration FIRST, and make the fixture order-robust with a following `ADD COLUMN IF NOT EXISTS` so it is correct on either side."
    - "An arm whose failure mode surfaces as an unhandled driver error is un-falsifiable even though the production change is trivial — and the FIX is to give the assertion the exception handler its neighbour already has, not to book a waiver. The constraint is the GATE's structure, never the grammar."
    - "A gate with TWO independent dedup layers reds only when BOTH go. Mutating either alone is a measured NO-RED that reads as `mutation applied, arm did not redden`. The twin has to be LAYERED, and the file has to SAY SO, or the next reader re-derives the same dead end."
    - "A SELF-VERIFYING migration can bite more than once. Re-basing the first check it makes and assuming that is all produces a lane with NO `TEST FAILED` at all — `wrong-first-failure`, not a passing arm. Count the migration's post-verify assertions before counting the twin's steps."
    - "The runner's `lane-blocked` class is derived from the GATE FILE's executable text. A gate whose APPLY LIST contains a pg_cron-requiring migration is equally un-baselineable and is classified `pending`, so the deferral is invisible to the very print that exists to name it."

key-files:
  created:
    - scripts/pg-lane/fixtures/20-fixture-app-role-helper.sql
    - scripts/pg-lane/fixtures/21-fixture-api-keys-credential-columns.sql
    - scripts/pg-lane/fixtures/22-fixture-portfolio-alerts.sql
    - scripts/pg-lane/fixtures/23-fixture-contact-requests.sql
    - scripts/pg-lane/fixtures/24-fixture-enqueue-compute-job-chain.sql
  modified:
    - supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql
    - supabase/tests/test_wizard_composite_fence.sql
    - supabase/tests/test_weight_snapshot_seed_secdef.sql
    - supabase/tests/test_csv_finalize_auth_guard.sql
    - supabase/tests/test_resync_retry_single_job.sql
    - scripts/mutation-runner/run.mjs
    - src/__tests__/gate-family-meta.test.ts
    - src/__tests__/mutation-runner-floors.test.ts
    - src/__tests__/mutation-annotation-parser.test.ts
    - CLAUDE.md
    - TODOS.md

key-decisions:
  - "BLOCKER 2 RESOLVED BY THE ROOT-CAUSE FIX, NOT A WAIVER (orchestrator, by the founder's own wave-9 precedent). test_resync_retry_single_job.sql assertion (b)'s INSERT is now wrapped in the `BEGIN … EXCEPTION WHEN unique_violation` idiom THE SAME FILE already uses at assertion (c). No assertion, message or identity was touched. WAIVED_CEILING stays 0."
  - "BLOCKER 1 RESOLVED BY DEFERRAL TO ITS OWN PLAN (founder). pg_cron goes ON the pg-lane as separate infrastructure work, retiring [REDUNDER-PGCRON] entirely and unblocking test_compute_jobs_error_kind_copy_parity.sql plus the four already-deferred files (~100 sections). Nothing in scripts/pg-lane/ or the classifier was touched here."
  - "Floors ratcheted to the RUN's printed 28 / 239, NOT to the plan's projected 29 / 242. The plan's arithmetic assumed six files; five landed. A floor derived from a plan rather than a run is the sequential-floor hazard this phase already recorded."
  - "The comment-only rule was broken EXACTLY ONCE, in its own commit (ce25c576), for the authorised (b) fix. Across the whole batch the added NON-COMMENT line count in supabase/tests/*.sql is 13, and all 13 are that one authorised block (5 of them the original INSERT re-indented, so 8 lines are genuinely new)."
  - "Nothing under supabase/migrations/** was edited (`git diff --stat d131f493..HEAD -- supabase/migrations/` is empty). No lane ever ran against a Supabase database. .github/workflows/ci.yml was not touched — the timeout question is settled and its ONE-LINE SWAP marker is discharged."
  - "Two migrations were EXCLUDED from an apply list by measurement, not preference: 20260417031851 and 20260510173005 both carry SAVEPOINT inside a PL/pgSQL DO body and cannot parse. A scaffold fixture carrying the REAL helper body substitutes; a hardcoded `RETURN FALSE` stub was rejected because it would make every admin-policy arm structurally unfalsifiable."

status: complete
---

# Phase 164.4 Plan 09: Batch 6 — five files proven, floors ratcheted, both blockers decided

Five of the six planned gate files (20 of 23 sections) are fully annotated and proven on real
throwaway pg-lane clusters. The full corpus run exits 0 with `arms: 239/239/0` and no defects, and
both floors are ratcheted to the numbers that run PRINTED. The sixth file is deferred to the
pg_cron-on-lane plan by founder decision.

## The two escalated blockers, and what was decided

### Blocker 2 — assertion (b) — RESOLVED BY THE ROOT-CAUSE FIX

The previous executor measured `test_resync_retry_single_job.sql` assertion (b) as a waiver
candidate: its only falsifying mutation — narrowing the SV unique index to `(strategy_id)` —
made its own two-row INSERT raise a raw `23505` with `occurrences of "TEST FAILED (" in the lane
output: 0`, because that INSERT had no exception handler.

The orchestrator applied the precedent the founder set for wave 9's `[REDUNDER-WAIVER-01]`: fix the
gate's structure, do not book an exception. The INSERT is now wrapped in the
`BEGIN … EXCEPTION WHEN unique_violation` idiom **the same file already uses at assertion (c)** — a
pure addition around an existing statement, with no assertion, message or identity changed.

MEASURED on a real lane, same apply list, before and after (commit `ce25c576`):

| Lane | Before the fix | After the fix |
|---|---|---|
| unmutated baseline | exit 0, (a)/(b)/(c) OK | exit 0, (a)/(b)/(c) OK |
| SV index narrowed to `(strategy_id)` | exit 3, **0** occurrences of `TEST FAILED (` | exit 3, **1** occurrence, and it is (b) |

The post-fix failure, verbatim:

```
psql:supabase/tests/test_resync_retry_single_job.sql:201: ERROR:  P0001: TEST FAILED (b): two
  draft strategy_verifications rows for strategy 6d6d6d6d-…-041 with DISTINCT wizard_session_ids
  (7e7e7e7e-…-001, 7e7e7e7e-…-002) collided on a unique violation, but distinct sessions must
  insert cleanly. …
CONTEXT:  PL/pgSQL function inline_code_block line 24 at RAISE
LOCATION:  exec_stmt_raise, pl_exec.c:3911
```

A single-frame CONTEXT with LOCATION last — all three of GRAMMAR rule 3c's attribution legs hold.
**`WAIVED_CEILING` stays 0.** This is the phase's SECOND arm rescued by a structural fix rather than
an exception.

### Blocker 1 — `test_compute_jobs_error_kind_copy_parity.sql` — DEFERRED TO ITS OWN PLAN

Founder decision: retire `[REDUNDER-PGCRON]` entirely by making the pg-lane able to host pg_cron.
That unblocks this file **plus the four already-deferred ones** (~100 sections) and is separate
infrastructure work with its own plan and PR.

So this batch did **not** touch `scripts/pg-lane/run.sh`, did not install pg_cron, and did not widen
`gateNeedsPgCron`. The file is left unannotated and stays in `pending: 12`. The classifier defect
that makes that placement misleading is booked as `[REDUNDER-LANEBLOCKED-BLIND]` (below), with a
tripwire pinned in the parser test so it cannot be forgotten.

## Task 3 — the numbers, read off the run

`node scripts/mutation-runner/run.mjs` → **exit 0**, 297 s wall clock on the authoring box:

```
coverage: files 28/71
  pending: 12 idiom file(s) without RED-UNDER — …
arms: 239/239/0   (executed/annotated/waived)
biting: 239   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 239   (arm lanes actually spawned — tallied inside runLane, independent of the
                         239 the verdict loop counted; plus 28 baseline / 28 restore leg(s))
per-arm lane time: mean 1.0s over 239 arm run(s)
lane-probe: pg_cron absent — lane-blocked class is current
✅ No defects. Every annotated arm bit its own arm first.
```

The two independent tallies AGREE (`arms:` executed 239, `lane-invocations:` 239), and the 28
per-file `biting` counts **sum to 239**, the aggregate — verified by summing the runner's own lines.

### Floors: old → new

| Constant | Old | New | Basis |
|---|---|---|---|
| `FILES_FLOOR` | 23 | **28** | the run's own `coverage: files 28/71` |
| `ARMS_FLOOR` | 219 | **239** | the run's own `biting: 239` |
| `WAIVED_CEILING` | 0 | **0 — UNEDITED** | no waiver was authored |

⚠️ The plan projected `FILES_FLOOR 29` / `ARMS_FLOOR 242` on six files. **Five landed**, so the
floors are the run's printed 28 / 239, never the plan's arithmetic.

### Separation, both directions, on real lanes

Driven through the real verdict loop (`runCorpus({filesFloor, armsFloor})`), 2026-09-04:

```
filesFloor=28 armsFloor=239  filesAnnotated=28 biting=239 defects=0 exit=0  SILENT  (298.7 s)
filesFloor=29 armsFloor=240  defects=2 exit=1  FIRES BOTH                          (295.8 s)
   [floor] FILES_FLOOR regression: 28 annotated file(s) < floor 29
   [floor] ARMS_FLOOR regression: 239 biting arm(s) < floor 240
```

So 28 / 239 is exactly the separation point, not a value below it.

## The five proven files

### Section-coverage one-liner (verbatim)

```
test_strategy_verifications_wizard_session_tenant_scope.sql sections 5 untwinned 0
test_wizard_composite_fence.sql                             sections 5 untwinned 0
test_weight_snapshot_seed_secdef.sql                        sections 4 untwinned 0
test_compute_jobs_error_kind_copy_parity.sql                sections 3 untwinned 3   1/A-3 | 2/A-3 | 3/F-3
test_csv_finalize_auth_guard.sql                            sections 3 untwinned 0
test_resync_retry_single_job.sql                            sections 3 untwinned 0
```

Section counts match the plan exactly (5 / 5 / 4 / 3 / 3 / 3 = 23). **20 twinned, 3 deferred.**

Per-file, off the full run:

```
file test_strategy_verifications_wizard_session_tenant_scope.sql: sections 5 / judged 5 / annotated 5 / waived 0 / biting 5
file test_wizard_composite_fence.sql:                             sections 5 / judged 5 / annotated 5 / waived 0 / biting 5
file test_weight_snapshot_seed_secdef.sql:                        sections 4 / judged 4 / annotated 4 / waived 0 / biting 4
file test_csv_finalize_auth_guard.sql:                            sections 3 / judged 3 / annotated 3 / waived 0 / biting 3
file test_resync_retry_single_job.sql:                            sections 3 / judged 3 / annotated 3 / waived 0 / biting 3
```

### The fifth apply list, now REFERENCED (`test_resync_retry_single_job.sql`, 22 entries)

Fixtures `01-core`, `02-sanitize-tables`, `03-compute-jobs`, `07-supabase-default-privileges`,
`11-api-keys-created-at`, `15-auth-role`, `20-app-role-helper`, `21-api-keys-credential-columns`,
`23-contact-requests`, `24-enqueue-compute-job-chain`; then `20260411144407_compute_jobs_queue`,
fixture `04-compute-jobs-targets`, `20260416125430_contact_request_metadata`,
`20260418194206_scoring_weight_overrides`, `20260420073003_allocator_holdings`,
`20260501055202_strategy_verifications`, `20260501055213_strategy_verifications_rls_polish`,
`20260510172738_strategy_verifications_state_machine`,
`20260510175507_process_key_long_compute_job_kinds_repair`,
`20260515210300_scoring_weight_overrides_high_hardening`,
`20260717233529_allocator_equity_derived_surface`,
`20260726000225_strategy_verifications_tenant_scope_uniq`.

Re-proven GREEN at HEAD before any edit (exit 0, all three assertions OK). The other four lists are
unchanged from the previous session and recorded in the commits that carry them.

### The three resync twins, and why each has the shape it has

| Arm | Shape | Why |
|---|---|---|
| `a` | LAYERED, 2 edits across 2 migrations | the gate is DEFENCE IN DEPTH — see below |
| `b` | LAYERED, 3 edits in 1 migration | the migration self-verifies TWICE |
| `c` | `sql` DROP INDEX | deleting the CREATE aborts that same self-verify |

**⛔ Finding 2 confirmed and acted on: the file's header claim about (a) is wrong.** The header names
`compute_jobs_one_inflight_per_kind_strategy` as the dedup anchor. Excluding `process_key_long` from
that index ALONE leaves the gate at exit 0 — **proved applied** by re-reading `pg_indexes.indexdef`,
so a NO-RED and not a failed apply. Measured this session, the converse is equally a NO-RED: deleting
`_enqueue_compute_job_internal`'s strategy-branch select-existing alone leaves the index to dedup via
`ON CONFLICT DO NOTHING`, after which the lost-the-race re-read hands back the same id. (a) reds only
when BOTH halves go — which is exactly what its own message says ("without the (strategy_id, kind)
dedup a seam retry mints a second job"). The twin is layered across `20260420073003` (the LAST
migration to define the internal — `20260515210300` replaces only the PUBLIC wrapper) and
`20260416125430` (the last to define the index). All of this is written into the gate's header so the
next reader does not re-derive the dead end.

**(b) needed a THIRD layer, and the runner found it.** `20260726000225` self-verifies twice: STEP 3
check (a) compares the indexed column list, and check (c) counts the unique indexes covering
`wizard_session_id` and demands exactly 1 — which a `(strategy_id)`-only index makes 0. Re-basing
only the first aborted the apply with `PYAPI-01: expected exactly 1 unique index covering
wizard_session_id, found 0`; the lane emitted no `TEST FAILED` at all and the runner reported
`wrong-first-failure`. That is the control working, and it is why the arm has three steps.

## SC-4: the fence file's SKIP evidence

`test_wizard_composite_fence.sql` hides three assertions behind `SKIP (Part 3a / 3b-ii / 3c-ii …)`
notices keyed on migration `20260814120000`. The apply list carries that migration, and the
baseline was **checked** rather than assumed:

| Check | Result |
|---|---|
| `grep -c 'SKIP'` over the whole baseline lane output | **0** — not one occurrence of the token, anywhere |
| Part 3a summary notice | `Part 3a OK: neither anon nor authenticated holds EXECUTE …, and service_role holds it on both (ALL assertions …` — the **no-skip** branch, not `PASS WITH 1 SKIP` |
| File summary notice | `test_wizard_composite_fence: ALL PASS (… refuses authenticated for BOTH matching and mismatched identities …)` — the **no-skip** variant |

So the three guarded assertions RAN. No section of that file is behind a firing SKIP, and none was
twinned while skipped.

## Lockstep pins moved with the floors

| Group | What moved |
|---|---|
| A | `run.mjs` `FILES_FLOOR` 23→28 and `ARMS_FLOOR` 219→239, each with a NEW dated block (COMMAND / SAMPLE SIZE / COVERAGE / SEPARATION / RECORD). Earlier blocks left as LINEAGE; only the CURRENCY lines rewritten. `WAIVED_CEILING` untouched. |
| B | `gate-family-meta.test.ts` `KNOWN_THRESHOLD_SITES` strings + dates. |
| C | `mutation-runner-floors.test.ts`: `totalAnchored` 219→239 + lineage note; `GREEN_LOG` coverage / arms / biting / lane-invocations / mean / **28** per-file rows and their arithmetic; the RED arms derived from it (they carry literal `219`s and did NOT follow automatically — a plan-level assumption corrected here). `mutation-annotation-parser.test.ts`: `armsSeen` 219→239, `stepsSeen` 205→226, the needles pin 205→226, scanCorpus title/`filesAnnotated` 23→28 and its 28-name list. |
| D | `CLAUDE.md` § SQL gate integrity jobs currency sentence. |

⚠️ `GREEN_LOG`'s `pending:` line is a SYNTHETIC fixture naming `c.sql`, deliberately held at 1 — a
standing decision from plans 04-08 with the reason written above the line. **Not** moved to the live
count.

⚠️ `.github/workflows/ci.yml` deliberately UNTOUCHED. The timeout question is settled (wave 9
measured 333 s for 219 arms at `mean 1.0s` on ubuntu; the wave-8 1.7 s was host variance) and its
`ONE-LINE SWAP` marker is already discharged. This batch's local wall clock is 297 s for 239 arms at
the same `mean 1.0s`.

Two parser-test controls were **re-pointed rather than deleted**: `test_wizard_composite_fence.sql`'s
comment-only pg_cron mention must still never make it lane-blocked, but the file is annotated now, so
the control follows it (`annotatedFiles` contains it, `laneBlockedFiles` does not) — what it pins is
the CLASSIFIER, not the file's coverage state. And
`test_compute_jobs_error_kind_copy_parity.sql ∈ pendingFiles` is now pinned as a **tripwire** for
`[REDUNDER-LANEBLOCKED-BLIND]`, flipping the day either half is resolved.

## Findings (booked in TODOS.md, not fixed here)

1. **`[REDUNDER-LANEBLOCKED-BLIND]` — NEW.** `gateNeedsPgCron` (`parse.mjs:996`) scans a gate file's
   own executable text and is blind to its `RED-UNDER-SETUP` apply list, so a file blocked only by a
   pg_cron-dependent MIGRATION is miscounted into `pending:` instead of `lane-blocked:` — the one
   print whose purpose is to name every exclusion does not name it. Superseded **in practice** if the
   pg_cron-on-lane plan lands, but a genuine runner defect either way, so it is booked separately from
   `[REDUNDER-PGCRON]` (a substrate problem).
2. **`[REDUNDER-SAVEPOINT]` is a CLASS OF THREE, not one** — entry extended. Beyond
   `20260416201929_audit_log_hardening.sql`, this plan measured
   `20260417031851_user_app_roles.sql:433` and
   `20260510173005_process_key_long_idempotency_drain.sql:397`, both
   `ERROR 42601 syntax error at or near "TO"`. An AST-level scan closes the class at **exactly 3**.
   None has ever applied to a vanilla cluster as written, so either the deployed bodies differ from
   the repo (VAC-04's own subject) or they never applied.

## Deviations from plan

1. **[Rule 4 — scope, DECIDED] The batch is 5 of 6 files.** The sixth is un-baselineable without
   pg_cron and was deferred by founder decision to its own plan. Floors ratcheted to the run's
   printed values rather than the plan's six-file arithmetic.
2. **[Rule 2 — missing critical functionality] One authorised non-comment edit.** Assertion (b)'s
   INSERT was wrapped in the file's own exception idiom (commit `ce25c576`). This is the batch's only
   break of the comment-only rule and it is the resolution of an escalated blocker, not an
   improvisation.
3. **[Rule 1 — bug] The plan's Group-C claim that "the three RED arms derive from GREEN_LOG and
   follow automatically" is FALSE at HEAD.** Fifteen literal `219`/`23`/`24`/`218`/`220` integers sit
   in the RED arms' own regexes and expectations. They were found by running the tests, and moved.
4. **[Rule 1 — bug] A scripted edit to `mutation-annotation-parser.test.ts` matched the wrong
   occurrence** of `expect(corpus.annotatedFiles).toEqual([`, clobbering the IN-01 structured-only
   test. Caught immediately by a vitest PARSE_ERROR, repaired, and the resulting diff re-read
   hunk-by-hunk to confirm it contains only the intended changes.
5. **[Rule 2] Two stand-in fixture headers were stale.** `23-fixture-contact-requests.sql` and
   `24-fixture-enqueue-compute-job-chain.sql` each declared themselves "NOT YET REFERENCED BY ANY
   `RED-UNDER-SETUP` LINE" and named the condition for deleting them. Both are now referenced;
   headers corrected (commit `ff2a3f4a`). Neither was deleted.

## Comment-only invariant

```
git diff --stat d131f493..HEAD -- supabase/migrations/   -> empty (nothing touched)
added NON-COMMENT lines across supabase/tests/*.sql      -> 13, ALL of them the authorised
                                                            assertion-(b) exception handler.
                                                            5 of the 13 are the ORIGINAL two-row
                                                            INSERT re-indented one level inside
                                                            the new block, so only 8 lines are
                                                            genuinely new: `BEGIN`, `EXCEPTION`,
                                                            `WHEN unique_violation THEN`,
                                                            `RESET ROLE;`, the 3-line RAISE and
                                                            `END;`.
added NON-COMMENT lines in the annotation commit d950bc97 -> 0
```

## Known Stubs

None. Three sections in `test_compute_jobs_error_kind_copy_parity.sql` are UNTWINNED and that is a
founder-decided deferral, not a stub: the file remains in the runner's `pending:` list, so nothing
claims coverage that does not exist. The one way that placement is misleading is itself booked as
`[REDUNDER-LANEBLOCKED-BLIND]` and pinned by a tripwire test.

## Verification (run SERIALLY, one agent, nothing else on the box)

| Step | Result |
|---|---|
| `node scripts/mutation-runner/run.mjs` | **exit 0**, 299 s, `files 28/71`, `arms: 239/239/0`, `biting: 239`, tallies agree, no defects |
| `node scripts/mutation-runner/run.mjs --self-test` | **exit 0** — both floor modes, the waiver ceiling, the wrong-identity mode, MEASURE_FAIL, the absurdity floor, the stand-in target refusal, the stale-deferral probe and the 164.3.1-11 regression corpus all fire |
| `npx vitest run` × the 4 lockstep files | **4 passed, 211/211 tests** |
| `npx vitest run src/__tests__/contracts src/__tests__/drift-check-scripts.test.ts` | **6 passed, 438/438** — run because a file-scoped pass cannot clear contract tests, which scan all of `src/` |
| `node scripts/verify-plan-anchors.mjs --pending` | **exit 0**, 34 claims across 4 pending plans, no stale claims (the `run.mjs` edits shifted line numbers) |
| `[ -z "$(git status --porcelain)" ]` | **TREE CLEAN** |

## Coverage ledger

| Item | Status |
|---|---|
| Five files, 20 sections, 0 untwinned, all biting | `complete` |
| Full corpus run exit 0, `arms: 239/239/0`, tallies agree, per-file rows sum to 239, tree clean | `complete` |
| SC-4: the fence file's three SKIPs proven inert with the no-skip notices quoted | `complete` |
| Blocker 2 — assertion (b) resolved by the ROOT-CAUSE FIX; `WAIVED_CEILING` still 0 | `complete` |
| Blocker 1 — `test_compute_jobs_error_kind_copy_parity.sql` deferred to the pg_cron-on-lane plan | `complete` (decided; the work is that plan's) |
| Floors 28 / 239 and every lockstep pin (Task 3), both separation directions on real lanes | `complete` |
| `sql-mutation` GREEN on ubuntu, SHA-bound to the PR head; run id + wall clock recorded | **`pass`** — closed by the orchestrator, see below |

### Human-check CLOSED 2026-09-04 (orchestrator)

Landed as **PR #741**, head SHA `f9f455b697e6d7137dea293b8e6c0ddb00af2cde`, squash-merged as
`a55ff918`, v0.77.10.0. **24/24 checks green**, SHA-bound. `sql-mutation` SUCCESS, run
**33872399902**, **369 s** wall (12:22:05 → 12:28:14), and the job's own lines:

```
coverage: files 28/71        arms: 239/239/0        biting: 239
lane-invocations: 239        per-arm lane time: mean 1.0s over 239 arm run(s)
lane-probe: pg_cron absent — lane-blocked class is current
✅ No defects. Every annotated arm bit its own arm first.
```

Third consecutive ubuntu run at `mean 1.0s`/lane, so the wave-8 1.7 s outlier stays falsified.

⚠️ **BUT THE PROJECTION IN ci.yml UNDERCOUNTS, AND THIS RUN SHOWS BY HOW MUCH.** It multiplies
*arms* by per-lane time, and the job also runs a **baseline and a restore leg per FILE** — the
runner prints those separately (`plus 28 baseline / 28 restore leg(s)`), and they are lanes too.
Measured marginal cost wave 9 → wave 10: **+20 arms but +36 s**, because 5 new files added 10
further legs on top of the 20 arm lanes. Corrected phase-end estimate: ~265 arms + ~80
baseline/restore legs ≈ 345 lane runs ≈ 345 s + ~130 s fixed ≈ **7.9 min**, not the 6.6 min the
comment currently projects. Still comfortably inside `timeout-minutes: 15`, so this is a MODEL
correction and not a budget problem — but the next batch to touch that comment should fix the
formula to count legs, not arms, rather than restating 6.6.

## Commits

| Commit | What |
|---|---|
| `f014e7d0` | `test(164.4-09): apply list + A1 twin for the wizard-session tenant-scope gate` |
| `7436305a` | `test(164.4-09): twin A2-A5 of the wizard-session tenant-scope gate` |
| `37b2a31a` | `test(164.4-09): apply list + 5 twins for the wizard composite fence gate` |
| `58e101ce` | `test(164.4-09): apply list + 4 twins for the weight-snapshot seed SECDEF gate` |
| `8f512e78` | `test(164.4-09): apply list + 3 twins for the csv-finalize auth-guard gate` |
| `ddb3bc66` | `chore(164.4-09): two measured stand-ins for the resync-retry apply list` |
| `d131f493` | `docs(164.4-09): SUMMARY — four files proven, two blocked, floors held` |
| `ce25c576` | `fix(164.4-09): make assertion (b) able to name itself under a narrowed SV index` |
| `d950bc97` | `test(164.4-09): apply list + 3 twins for the resync-retry single-job gate` |
| `ff2a3f4a` | `docs(164.4-09): the two resync stand-ins are referenced now, not orphaned` |
| `1926e6a1` | `chore(164.4-09): FILES_FLOOR 23->28, ARMS_FLOOR 219->239, all lockstep pins` |
| `19a6c4e7` | `docs(164.4-09): book [REDUNDER-LANEBLOCKED-BLIND]; SAVEPOINT class is 3, not 1` |

## Self-Check: PASSED

All five claimed fixtures exist on disk; all twelve claimed commits resolve in `git log`;
`FILES_FLOOR = 28`, `ARMS_FLOOR = 239` and `WAIVED_CEILING = 0` read back from `run.mjs` at HEAD;
`git diff d131f493..HEAD -- supabase/migrations/` is empty; the working tree is clean.
