---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 04
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet]
wave: 5
depends_on: ["164.4-03"]
files_modified:
  - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
  - supabase/tests/test_retention_orphaned_running.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 2: test_strategy_analytics_stuck_computing_reaper.sql (29 sections) and test_retention_orphaned_running.sql (25 sections) fully annotated and proven"
  - "FILES_FLOOR 2 → 4, ARMS_FLOOR → measured (expected 138)"
affects: [164.4-05]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Both files carry exactly one RED-UNDER-SETUP that goes GREEN on the lane, and every section (29 + 25) has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok), both files restoring GREEN, per-file judged == annotated == biting (SC-2, SC-3)"
    - "FILES_FLOOR, ARMS_FLOOR (and WAIVED_CEILING if a waiver was accepted) equal the run's printed numbers with dated blocks and both separation directions (SC-3, SC-4)"
  artifacts:
    - path: "supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql"
      provides: "29 sections twinned; one RED-UNDER-SETUP"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_retention_orphaned_running.sql"
      provides: "25 sections twinned; one RED-UNDER-SETUP"
      contains: "RED-UNDER-SETUP"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 4, annotatedFiles four names sorted"
      pattern: "scanCorpus"
---

<objective>
Batch 2, largest-first: `test_strategy_analytics_stuck_computing_reaper.sql` (planner-measured 926
lines, 56 idiom raises, 31 identities → **29 sections**, 4 migration stamps named, 12 banners) and
`test_retention_orphaned_running.sql` (776 lines, 50 raises, **25 identities = 25 sections**, 11
stamps named, 6 banners). Both are compute-jobs gates and will likely share the stand-in Plan 00
built for the reconcile file; each still needs its OWN discovered apply list proven GREEN. Derive
sections from identities (reaper's `4a` / `4a/seed` / `6a/G1` / `6a/G1/D-01/SC-17-01` are distinct
sections under the rule; the banners are coarse Part blocks). Re-derive with the one-liner.

Purpose: 54 sections while the run is ~146 lanes.
Output: 54 twins, two setup lines, FILES_FLOOR 4, ARMS_FLOOR → measured (expected 138).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Read before implementing: Plan 00's record (decision, authoring rules, discovery loop), Plan 03's
SUMMARY (the file-move choreography as actually executed, the reconcile apply list to start from),
PATTERNS § P1, § P2.1-P2.3, § P3, § P4.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reaper file — setup line proven GREEN + ONE section end-to-end</name>
  <files>supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql</files>
  <precondition>164.4-03-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`).</precondition>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-04-PLAN.md` and halt on non-zero.

    Discover the reaper file's apply list with the iterative lane loop (stand-ins per Plan 00's
    DECISION first, then the real migrations the file names — `grep -a -o -E '20[0-9]{12}' <file> | sort -u`
    as the seed — with the LAST-defining migration of every object under test; the first ERROR of
    each red run names the next missing object; `--workdir` outside the repo). If a relation is
    missing that no migration in the list creates, extend an existing additive fixture or add
    `NN-fixture-<area>.sql` in the `02-*` shape (record the new file in the SUMMARY's
    files_modified). Write the `RED-UNDER-SETUP` line at column 0 in the header with its 3-line
    prose paragraph; re-prove exit 0 on the lane.
    Author ONE section (`1/JOB-01`) exactly per the P1.1 shape: real identity as `arm`, mutation of
    the code under test in the last-defining migration, measured `occurrences`, `neuter` only for
    an idiom arm firing first. `--parse-only` then the diagnostic lane; read the verdict line.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>The reaper file has one proven setup line and one twin whose lane reads RED (identity ok).</done>
</task>

<task type="auto">
  <name>Task 2: Twin the remaining 28 reaper sections and all 25 retention sections</name>
  <files>supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql, supabase/tests/test_retention_orphaned_running.sql</files>
  <action>
    Repeat Task 1's setup discovery for `test_retention_orphaned_running.sql` (11 stamps named —
    the longest apply list so far; expect a higher per-lane time and record it). Then run the
    section-coverage one-liner over both files, paste its output into the SUMMARY, and twin every
    untwinned section per the rules: real identity as `arm`; mutate the code under test in the
    last-defining migration in the setup list; `occurrences` measured with `grep -a -o -F`; `sql`
    step for exactly-pinned grant drift; LAYERED `apply` when a migration self-verifies; `neuter`
    only for idiom arms that fire first; no fixture targets. Waivers only for arms that bite but
    cannot be mutated mechanically, reason in the twin, +1 WAIVED_CEILING each, >5 cumulative →
    STOP and escalate. A dead arm is a TODOS finding, not a waiver. Never edit an assertion, a
    message or an identity; the diff on both gate files is added comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql supabase/tests/test_retention_orphaned_running.sql</automated>
    Expected `sections 29 untwinned 0` and `sections 25 untwinned 0`; a booked dead-arm finding is the only permitted exception and is named in the SUMMARY.
  </verify>
  <done>Both files fully twinned (or booked), both setup lines proven, parse-only green.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 2 → 4, ARMS_FLOOR → measured, pins and prose in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, CLAUDE.md</files>
  <action>
    Run `node scripts/mutation-runner/run.mjs`, iterate on named defects, read every number OFF THE
    RUN LOG (expected `coverage: files 4/71`, `biting: 138`). In ONE commit with the annotations:
    Group A — `run.mjs`: `FILES_FLOOR` → 4 and `ARMS_FLOOR` → measured biting, each with a compact
    dated block (RE-DERIVED date, plan 164.4-04, HEAD sha, the printed lines, SAMPLE SIZE, COVERAGE
    naming the four files, SEPARATION both directions, RECORD); currency lines edited, lineage
    untouched; `WAIVED_CEILING` +n only for accepted waivers with reasons.
    Group B — `KNOWN_THRESHOLD_SITES` strings and dates.
    Group C — `totalAnchored` → measured; `GREEN_LOG` coverage/arms/biting/lane-invocations/mean/
    per-file lines (four, summing to the aggregate)/spawned string; `armsSeen`/`stepsSeen` →
    measured; scanCorpus pin → "4 of 71", four sorted names.
    Group D — `CLAUDE.md` currency sentence. `timeout-minutes`: raise to 30 WITH the measurement in
    its comment only if the measured run is ≥ 10 min (~146 lanes here — record the wall clock).
    Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + wall clock in the SUMMARY.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 4/71`, `biting:` == ARMS_FLOOR; all pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |
| stand-in fixture → gate | provides only what the gate names; never a twin target |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep | medium | mitigate | reason in twin, printed; ceiling +1 per reviewed waiver; >5 → STOP |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations in the apply list |
| T-164.4-05 | Repudiation | red baseline counted as coverage | medium | mitigate | baseline proven before authoring; per-file `judged` read off the run |
| T-164.4-08 | Denial of Service | lane time growth (11-migration apply list) | low | mitigate | per-lane time recorded; timeout raised only with a measurement |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 29 and 25 sections, 0 untwinned; parse-only exit 0.
- Full run: `coverage: files 4/71`, `biting:` == ARMS_FLOOR, tallies agree, exit 0; four lockstep vitest files green; tree clean; `--pending` anchors green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- Two more files fully proven; floors moved by the measured delta in the same commit; the longest apply list's per-lane cost is now a measurement.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-04-SUMMARY.md` when done. Record: one-liner output, both apply lists, run log lines, floors old → new, per-lane time for the 11-migration list, waivers/findings, PR number, SHA-bound run id and wall clock.
</output>
