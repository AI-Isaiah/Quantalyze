---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 04
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet, ledger-refresh, proven-baseline]
wave: 5
depends_on: ["164.4-03"]
files_modified:
  - supabase/tests/test_ledger_refresh_composite_arm.sql
  - supabase/tests/test_ledger_refresh_fanout.sql
  - supabase/tests/test_ledger_refresh_staleness.sql
  - scripts/mutation-runner/run.mjs
  - scripts/mutation-runner/parse.mjs
  - scripts/mutation-runner/VAC06-DEMOS.md
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 1 of the new files: the ledger_refresh family (15 + 15 + 11 = 41 sections) fully annotated and proven, starting from the ONLY proven-GREEN apply list in the phase (Plan 00: exit 0, ALL 10 ARMS EXECUTED, mean 1.01 s/lane)"
  - "FILES_FLOOR 1 → 4 and ARMS_FLOOR 45 → measured (expected 86), the first FILE move of the phase with the scanCorpus pin and coverage prose moved in lockstep"
affects: [164.4-05]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "test_ledger_refresh_composite_arm.sql carries exactly one RED-UNDER-SETUP whose apply list is Plan 00's recorded GREEN list verbatim and in order; fanout and staleness each carry their own proven list; every section (15 + 15 + 11) has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0; every new twin is RED (identity ok) and each file restores GREEN; per-file lines show judged == annotated == biting for all three (SC-2, SC-3)"
    - "FILES_FLOOR and ARMS_FLOOR equal the run's printed coverage and biting, with dated blocks and both separation directions beside each constant; the scanCorpus exact pin lists all four annotated files (SC-3)"
    - "A waiver candidate is a STOP checkpoint to the founder, WAIVED_CEILING stays 0 unless the founder accepts one on the record; a dead arm is a TODOS finding (SC-4)"
    - "A non-idiom raise that fires first is handled by a mutation it does not observe — never by editing it (SC-4)"
  artifacts:
    - path: "supabase/tests/test_ledger_refresh_composite_arm.sql"
      provides: "15 sections twinned; one RED-UNDER-SETUP equal to Plan 00's apply-list block"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_ledger_refresh_fanout.sql"
      provides: "15 sections twinned; one RED-UNDER-SETUP"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_ledger_refresh_staleness.sql"
      provides: "11 sections twinned; one RED-UNDER-SETUP"
      contains: "RED-UNDER-SETUP"
    - path: "scripts/mutation-runner/run.mjs"
      provides: "FILES_FLOOR=4 and ARMS_FLOOR at the measured value"
      contains: "FILES_FLOOR"
  key_links:
    - from: "supabase/tests/test_ledger_refresh_composite_arm.sql"
      to: "164.4-00-FIXTURE-STRATEGY.md"
      via: "the RED-UNDER-SETUP apply list is the fenced apply-list block Plan 00 proved GREEN, same 10 entries, same order (04-fixture-compute-jobs-targets.sql sits BETWEEN two migrations)"
      pattern: "RED-UNDER-SETUP"
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 4, annotatedFiles four names sorted"
      pattern: "scanCorpus"
---

<objective>
Batch 1 of the new files, re-pointed by Plan 00's measurement: the ledger-refresh family.
`test_ledger_refresh_composite_arm.sql` (651 lines, 15 identities → **15 sections**, 2 non-idiom
raises, `grep -a -c cron` → 0) is the ONLY gate file in the phase with a PROVEN pg-lane baseline:
Plan 00 took it to exit 0 with `ALL 10 ARMS EXECUTED (A-J) and passed` on a 10-entry apply list
(2 core fixtures + `03-fixture-compute-jobs.sql` + `20260411144407_compute_jobs_queue` +
`04-fixture-compute-jobs-targets.sql` + `20260710120000_strategy_keys` + `20260710130000_stitch_
composite_kind` + the three `20260825*` ledger migrations), timed three times, mean **1.01 s/lane**.
That list is the starting point for `test_ledger_refresh_fanout.sql` (598 lines, **15 sections**,
2 non-idiom raises) and `test_ledger_refresh_staleness.sql` (417 lines, **11 sections**), which
test the same subsystem — expect at most a migration or two added; each file still declares its
OWN setup line. Section counts re-confirmed at HEAD 007708dd by `gateSectionCount` (15 / 15 / 11).

⚠️ The four non-idiom raises are invisible to attribution and un-neuterable (RESEARCH Pitfall 3):
if one fires first under a mutation the verdict is `NO-IDENTITY` — pick a mutation it does not
observe. Three of these files declare sentinel NOTICEs counted by the ci.yml sentinel gate —
comment-only annotations cannot move that count. All three files RAISE (never SKIP) on an absent
object (`161.1-REVIEW WR-03`), so a wrong apply list is a red baseline, not a green skip.

This is the first plan that annotates NEW files, so it is the first FILES_FLOOR move: the
`scanCorpus … 1 of 71` pin, the `annotatedFiles` list, `coverage: files` in GREEN_LOG and the
"1 of 71" currency prose all move here. Re-derive sections with the one-liner in Task 2.

Purpose: the proven starting point lands first, while the run is short (~94 lanes).
Output: 41 twins, three setup lines, FILES_FLOOR 4, ARMS_FLOOR 45 → measured (expected 86).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md
@scripts/mutation-runner/GRAMMAR.md

Per-task reading lists are in each task's `<read_first>`. Splice Plan 00's fenced apply-list
block under **bash** — zsh does not word-split `$APPLY`.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs WAIVED_CEILING -->
<!-- verify-symbol: scripts/mutation-runner/parse.mjs scanCorpus -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: composite_arm — Plan 00's proven setup line + ONE section end-to-end (parse, lane RED, restore GREEN)</name>
  <files>supabase/tests/test_ledger_refresh_composite_arm.sql</files>
  <precondition>164.4-03-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`); 164.4-00-FIXTURE-STRATEGY.md carries the fenced `apply-list` block for this file.</precondition>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (the fenced apply-list block for this file — read it with awk; the three authoring rules; the discovery loop cost, 8 iterations for this file), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1 shapes, § P1.1 placement, § P2.1 setup line placement — column 0 in the header preceded by a 3-line prose paragraph; stand-ins-first is a DEFAULT not an invariant: Plan 00 measured that 04-fixture-compute-jobs-targets.sql must sit between two migrations), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-RESEARCH.md (§ Common Pitfalls 3 and 6), scripts/mutation-runner/GRAMMAR.md (rule 4: no fixture targets), supabase/tests/test_strategy_shares_rls.sql:40-61 (the reference header shape to copy)</read_first>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-04-PLAN.md` and halt on non-zero.

    Add the file's `RED-UNDER-SETUP` line at column 0 in its header, preceded by a 3-line prose
    paragraph in the shape of the reference's header, with the apply list from Plan 00's fenced
    block VERBATIM and IN ORDER (10 entries; `04-fixture-compute-jobs-targets.sql` after
    `20260411144407_compute_jobs_queue.sql`). Exactly one such line per file. Re-prove the baseline
    on the lane once, under bash, `--workdir` OUTSIDE the repo (`bash scripts/pg-lane/run.sh
    --workdir "$SCRATCH/lane" --apply <the 10 entries> --gate supabase/tests/test_ledger_refresh_
    composite_arm.sql` → exit 0, `ALL 10 ARMS EXECUTED`) before writing any twin — a red baseline
    means every arm is skipped with judged 0.

    Then author ONE section: take the file's first identity, read its branch and the migration
    object it exercises (the last-defining migration is one of the three `20260825*` files — a twin
    must target THAT one, rule 2 of Plan 00's authoring rules), locate the file's two non-idiom
    raises and choose a mutation of the code under test that neither observes (never the message,
    never the guard), measure `occurrences` with `grep -a -o -F` against the exact target file,
    confirm the target is in the setup list, and insert the prose + twin pair in the P1.1 shape
    directly above the section's first assertion statement. No twin may target
    `scripts/pg-lane/fixtures/**` (GRAMMAR rule 4 refuses it at parse time). Run `--parse-only` and
    the diagnostic lane and read the verdict line.

    ⚠️ From this point until Task 2 completes, the parser test's section-inclusion pin (armed in
    Plan 02: twin sections ⊇ identity sections for every annotated file) is EXPECTED RED for this
    file — it now carries one twin over 15 sections, which is exactly the state the pin refuses.
    Do NOT touch the pin, do NOT loosen it, and do not run it as a gate before Task 2; it goes
    green when the last section is twinned and is asserted in Task 3's lockstep vitest.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_ledger_refresh_composite_arm.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
    Static walk green with the new file counted (the `  pending:` line drops to 38), and the first
    twin's own lane reads RED (identity ok).
  </verify>
  <done>One setup line (Plan 00's list) and one proven twin exist in composite_arm; the baseline lane exits 0; nothing else changed.</done>
</task>

<task type="auto">
  <name>Task 2: Setup lines for fanout and staleness; twin every remaining section in all three files</name>
  <files>supabase/tests/test_ledger_refresh_composite_arm.sql, supabase/tests/test_ledger_refresh_fanout.sql, supabase/tests/test_ledger_refresh_staleness.sql</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (the discovery loop and the additive-fixture rule), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1, § P2.1-P2.3), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md (§ Waivers & un-falsifiable arms), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-RESEARCH.md (§ Common Pitfalls 3: non-idiom raises are invisible to attribution), scripts/mutation-runner/GRAMMAR.md (Shape 1b/3 layered apply; Shape 4 — what NOT to author)</read_first>
  <action>
    Discover and prove setup lines for fanout and staleness with the lane loop, STARTING from
    composite_arm's 10-entry list (iterate until exit 0; the first ERROR names the missing object;
    a relation no listed migration creates → extend an existing additive fixture or add
    `NN-fixture-<area>.sql` in the `02-*` shape, IF NOT EXISTS only, recorded in the SUMMARY;
    record each file's discovery iteration count and per-lane wall clock). Run the section-coverage
    one-liner (the verify command) over the three files, paste its output into the SUMMARY, and
    work from ITS list. For every untwinned section author one prose + twin pair as in Task 1: the
    `arm` is the section's real identity string; the mutation edits the migration that LAST
    defines the object (grep the function/view name across `supabase/migrations/`, take the newest
    that is in the setup list — if the newest is NOT in the list, the list is wrong: fix the list and
    re-prove the baseline); `occurrences` measured; a `sql` step for grant drift a migration pins
    exactly; a LAYERED `apply` when a migration's own verification block would abort the apply;
    `neuter` only for an idiom arm that would fire first; for a section shadowed by a non-idiom
    raise, a mutation that raise does not observe.

    Waivers: `WAIVED_CEILING` is 0 and this plan does not move it. If a section's arm DOES bite but
    no mechanical mutation reaches it without tripping a non-idiom raise, do NOT write a Shape 4
    twin: STOP, record the arm, the shadowing raise's line and the mutations tried in the SUMMARY,
    and return a checkpoint to the founder. An arm that cannot fail at all is a FINDING →
    `TODOS.md` + SUMMARY, no twin, said loudly. Never delete or weaken a prose annotation, an arm
    or a failure message; never rename an identity; `git diff` on the three gate files shows only
    added comment lines.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_ledger_refresh_composite_arm.sql supabase/tests/test_ledger_refresh_fanout.sql supabase/tests/test_ledger_refresh_staleness.sql</automated>
    Parity and grammar hold; the one-liner prints `sections 15 untwinned 0`, `sections 15 untwinned 0`,
    `sections 11 untwinned 0` and exits 0 (a booked dead-arm finding is the only permitted exception
    and must be named in the SUMMARY).
  </verify>
  <done>All 41 sections twinned (or booked), three setup lines proven GREEN on the lane, parse-only green, diffs are comment lines only.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 1 → 4, ARMS_FLOOR → measured, every pin and prose site in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, scripts/mutation-runner/parse.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, scripts/mutation-runner/VAC06-DEMOS.md, CLAUDE.md</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-02-SUMMARY.md (the rehearsed pin choreography and any surprises), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-03-SUMMARY.md (the lane-blocked and lane-probe lines GREEN_LOG now carries; the ubuntu wall clock), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P3 the floor-ratchet block shape, § P4 the lockstep-pin checklist — every item), scripts/mutation-runner/run.mjs (FILES_FLOOR, ARMS_FLOOR, WAIVED_CEILING and their dated blocks), scripts/mutation-runner/parse.mjs:1024-1055 (the scanCorpus doc comment), src/__tests__/gate-family-meta.test.ts (KNOWN_THRESHOLD_SITES), src/__tests__/mutation-runner-floors.test.ts (GREEN_LOG, totalAnchored), src/__tests__/mutation-annotation-parser.test.ts (armsSeen, stepsSeen, the scanCorpus exact pin, the section-inclusion pin), CLAUDE.md (§ SQL gate integrity jobs)</read_first>
  <action>
    Run `node scripts/mutation-runner/run.mjs`; iterate on named defects (a `baseline` defect means
    the setup list, not the twins; `wrong-first-failure` means a neuter or a different mutation;
    `no-red` means the wrong bytes or the wrong migration copy; `NO-IDENTITY` means a non-idiom
    raise fired first — change the mutation, not the raise). Read `coverage:`, `arms:`, `biting:`,
    `lane-invocations:`, `per-arm lane time:`, the `lane-blocked:` line and all four per-file lines
    OFF THE RUN LOG. Expected `coverage: files 4/71`, `biting: 86` (45 + 41); pin what was measured.

    In ONE commit with the annotations (PATTERNS § P4):
    Group A — `run.mjs`: `FILES_FLOOR` 1 → 4 with a compact dated block appended above it
    (`RE-DERIVED <date> (plan 164.4-04) — MEASURED … coverage: files 4/71 … COVERAGE: the four files
    by name … SEPARATION: fires at 3, silent at 4 … RECORD 164.4-04-SUMMARY.md`); `ARMS_FLOOR` →
    measured biting with the same block shape (SAMPLE SIZE, both directions); currency lines
    updated (the FILES_FLOOR comment's "1 of 71" claim and the CURRENCY block); every dated
    historical block untouched. `WAIVED_CEILING` unedited. `parse.mjs`: the scanCorpus doc comment's
    "unchanged at 1 of 71" → count-free ("moves as Phase 164.4 batches land; the exact pin lives in
    the parser test").
    Group B — `KNOWN_THRESHOLD_SITES`: `FILES_FLOOR=4`, `ARMS_FLOOR=<measured>`, dates updated.
    Group C — `mutation-runner-floors.test.ts`: `totalAnchored` → measured total prose markers
    (45 + 41 = 86 expected); `GREEN_LOG`: `coverage: files 4/71`, `arms:`, `biting:`,
    `lane-invocations:` (both numbers), `mean … over N arm run(s)`, `  pending: 36 …`, FOUR per-file
    lines whose biting sums to the aggregate, the `"… arm lane(s) spawned"` assertion string.
    `mutation-annotation-parser.test.ts`: `armsSeen`, `stepsSeen` → measured; the scanCorpus pin:
    title "4 of 71", `filesAnnotated` 4, `annotatedFiles` the four basenames sorted; `filesTotal`
    stays 71; the lane-blocked exact-set pin unchanged.
    Group D — `VAC06-DEMOS.md` "Coverage is files 1/71 … the other 70 files are Phase 164.4" →
    count-free ("coverage is the run log's coverage: line; the remaining idiom files land in 164.4
    batches; four are lane-blocked"); `CLAUDE.md` currency sentence → this batch's measured `arms:`
    and `coverage:`. `timeout-minutes`: unchanged (~94 lanes; ≈ 1.6 min of lanes projected on
    ubuntu at RESEARCH's 1.03 s/lane, plus the job's fixed checkout / node / cluster-init overhead —
    the 80 s job at 32 lanes is NOT a per-lane figure, it includes that overhead). Record the
    ubuntu job wall clock AND the ubuntu log's `per-arm lane time:` mean in the SUMMARY when the PR
    lands: every later batch projects from that per-lane figure.
    Verify SERIALLY, nothing else on the box.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + the deletion guard), confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head (`gh run view <id> --json headSha,conclusion`), record run id + SHA + the job's measured wall clock in the SUMMARY. Plan 05's precondition reads it.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 4/71` and `biting:` == ARMS_FLOOR; all pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run and its wall clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; mutations touch scratch copies only |
| stand-in fixture → gate | a stand-in provides only what the gate names; twins may not target it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep (first likely candidates: non-idiom shadowing) | medium | mitigate | no executor-authored waivers; STOP checkpoint to the founder with the shadowing line; WAIVED_CEILING stays 0 |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal (GRAMMAR rule 4); apply list lists real migrations for every mutated object |
| T-164.4-05 | Repudiation | a red baseline counted as coverage | medium | mitigate | baseline re-proven before authoring; per-file `judged` read off the run |
| T-164.4-07 | Repudiation | floor pinned from the artifact | medium | mitigate | every number quoted from the run log in the dated block |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: `sections 15 untwinned 0` / `15 / 0` / `11 / 0`; parse-only exit 0.
- Full run: `coverage: files 4/71`, `lane-blocked: 4 file(s) …`, `biting:` == ARMS_FLOOR, tallies agree, four per-file lines with judged == annotated == biting, exit 0.
- Four lockstep vitest files green; tree clean; `node scripts/verify-plan-anchors.mjs --pending` green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- The proven-baseline family is fully proven; FILES_FLOOR and ARMS_FLOOR moved by the measured delta in the same commit; the first file-move choreography (scanCorpus pin, coverage prose) is rehearsed for the seven batches that follow; the first non-idiom-shadowing cases are resolved on the record.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-04-SUMMARY.md` when done. Record: the one-liner output, the three apply lists (with discovery iteration counts and per-lane times), the run log's lines, floors old → new, waiver candidates (with the shadowing lines) / findings, PR number, SHA-bound ubuntu run id and wall clock.
</output>
