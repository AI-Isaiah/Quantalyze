---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 01
type: execute
subsystem: mutation-runner
tags: [wave-0, runner-output, unreachable-print, per-file-breakdown, fixture-target-refusal, count-recheck]
wave: 2
depends_on: ["164.4-00"]
files_modified:
  - scripts/mutation-runner/run.mjs
  - scripts/mutation-runner/parse.mjs
  - scripts/mutation-runner/GRAMMAR.md
  - scripts/mutation-runner/fixtures/selftest/fixture-target-gate.sql
  - .github/workflows/ci.yml
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - .planning/WINDOWS.md
autonomous: true
requirements: [SC-1, SC-3, SC-4]
provides:
  - "The runner PRINTS the 27 excluded non-idiom files by name on every run, in both modes, and CI MEASURE_FAILs when the line is absent or its count disagrees with its names"
  - "A per-file `judged / annotated / waived / biting` breakdown, cross-summed against the aggregate in-process and again by CI"
  - "A parse-time refusal of any twin targeting scripts/pg-lane/fixtures/**, with a red self-test fixture and a scenario that proves it fires"
affects: [every batch plan, ci-frontend-aggregator]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every run of the runner, full or --parse-only, prints an `unreachable:` line naming the 27 non-idiom files, and the sql-mutation count-recheck exits 1 if that line is missing or names a different number of files than it claims (SC-1 as amended)"
    - "Every run prints one `  file <name>: judged J / annotated A / waived W / biting B` line per annotated file, so a file whose baseline went red (judged 0) or that is half-annotated is visible in CI output; the per-file biting sum equals the aggregate `biting:` or the run exits 1 (SC-3)"
    - "A RED-UNDER-M step whose `file` is under scripts/pg-lane/fixtures/ is refused at parse time with a message naming the rule, in --parse-only, in the full run and in --self-test (T-164.4-01)"
    - "The floors did not move: this plan annotates nothing, and the SUMMARY records the measured run (files 1/71, arms 30/30/0, biting 30) as the unchanged value"
  artifacts:
    - path: "scripts/mutation-runner/run.mjs"
      provides: "the two new print lines at the existing emission seam, both modes"
      contains: "unreachable:"
    - path: ".github/workflows/ci.yml"
      provides: "count-recheck assertions for both new lines, MEASURE_FAIL on absence"
      contains: "unreachable"
    - path: "scripts/mutation-runner/fixtures/selftest/fixture-target-gate.sql"
      provides: "the red fixture that proves the fixture-target refusal fires"
      contains: "scripts/pg-lane/fixtures/"
  key_links:
    - from: "scripts/mutation-runner/parse.mjs"
      to: "scripts/mutation-runner/run.mjs"
      via: "scanCorpus returns the unreachable/pending classification; both coverage print sites read it"
      pattern: "unreachableFiles"
    - from: "src/__tests__/mutation-runner-floors.test.ts"
      to: ".github/workflows/ci.yml"
      via: "GREEN_LOG carries the new lines; countRecheckScript() runs the LIVE step against it"
      pattern: "GREEN_LOG"
---

<objective>
Wave 0 for the backfill: the runner output and CI assertions that make the amended criterion 1 and
criterion 3 machine-visible, plus the one new refusal the annotation batches must meet. No gate
file is annotated here; the floors stay at their measured values.

⭐ ARM UNIT AND DENOMINATOR, on the record (ROADMAP ⚖️ SCOPE AMENDMENT, founder 2026-09-02): the
arm unit is the **SECTION** — one `RED-UNDER-M` per named assertion group, derived from the identity
strings by stripping a trailing sub-suffix (`SHAPE 2a` → `SHAPE 2`, `TRIGGER 3d-i` → `TRIGGER 3`,
`ANON 1b-grant` → `ANON 1`; an identity with no such suffix is its own section), NEVER from
`-- ====` banners, which are non-uniform (8/12/6 banners for 39/29/25 sections in the top files).
Planner-measured at HEAD 9d08bc2a with that rule over the 44 `TEST FAILED (` idiom files: **355
sections / 516 identities**; the reference file has 35 sections and 30 twins over 20 of them, so
the phase authors **335 new twins → ~365 total**. The 27 non-idiom files are OUT and are PRINTED
by name (Task 1). Every batch plan re-derives its own file's count with the one-liner it carries
and works from the parser's output, not from these figures.

Purpose: a silent exclusion or a half-annotated file must fail the build, not read as coverage.
Output: two new runner lines in both modes, three CI assertions, one parse-time refusal with its
red fixture, the lockstep test fixtures extended, GRAMMAR updated.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Read before implementing: PATTERNS § P6 (the emission seam and the `-m1` prefix-grep constraint that
forces INDENTED new lines), § P2.4 (the fixture-target rule), § No Analog Found (the excluded-file
print copies the IDEA of `lint-sql-gates.mjs` `DELEGATED_MECHANISMS`: a deliberate absence, derived
and asserted). Anchors, verified at HEAD 9d08bc2a, one per line:

- the aggregate print block: scripts/mutation-runner/run.mjs:2155-2202

- the per-item analog, the waived line: scripts/mutation-runner/run.mjs:2198

- the static-mode coverage print: scripts/mutation-runner/run.mjs:2436

- the corpus scan function: scripts/mutation-runner/parse.mjs:884-904

- the count-recheck step, located by its EXACT name (do not rename it): .github/workflows/ci.yml:1028

- the test that extracts that step by name: src/__tests__/mutation-runner-floors.test.ts:842-845

- the GREEN log fixture the live step is run against: src/__tests__/mutation-runner-floors.test.ts:880-894

- the house rule for a new control (a rule without a firing fixture does not merge): scripts/lint-sql-gates.mjs:37-40

</context>

<tasks>

<task type="tracer">
  <name>Task 1: The `unreachable:` line — derived in scanCorpus, printed in both modes, asserted by CI, pinned by tests</name>
  <files>scripts/mutation-runner/parse.mjs, scripts/mutation-runner/run.mjs, .github/workflows/ci.yml, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts</files>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-01-PLAN.md` and halt on non-zero.

    Wire ONE path through every layer. (1) `scanCorpus` in `parse.mjs`: for each `.sql` with no
    line-anchored marker, classify by the runner's own notion of code — `tokenizeStatements` over
    the file, then over the statements' executable text: any `RAISE EXCEPTION` and no
    `TEST FAILED (` carrier → `unreachable`; a carrier present → `pending`; no code-level raise at
    all → `inert` (measured 0 today; if one ever appears it is a finding and is printed). Return
    `unreachableFiles`, `pendingFiles`, `inertFiles`, each a sorted basename array. Do not hand-roll
    a comment-blind grep for this — the tokenizer is the one definition of code
    (RESEARCH § Don't Hand-Roll). If the `RAISE EXCEPTION` regex lives only in `run.mjs`, move it to
    `parse.mjs` and import it from there; never duplicate it.
    (2) `run.mjs`: at BOTH coverage print sites (full run and `--parse-only`), immediately after the
    `coverage:` line, log at column 0
    `unreachable: N file(s) raise outside the runner's identity idiom — <basenames space-separated> (TODOS [REDUNDER-NONIDIOM])`
    and, indented two spaces, `  pending: K idiom file(s) without RED-UNDER — <basenames>` (and
    `  inert: …` only when non-empty). The `unreachable:` prefix is new and distinct, so the four
    existing `-m1` prefix greps are untouched; the coverage line keeps its exact `$`-anchored shape.
    Do NOT spell the identity literal in the printed text.
    (3) the ci.yml count-recheck step named in the context list — keep the name byte-identical.
    After the coverage assertion, extract the unreachable line with `grep -a -m1 -E '^unreachable: [0-9]+ file\(s\) '`,
    MEASURE_FAIL with its own message on absence (the amended criterion 1: a silent exclusion fails
    exactly like a missing annotation), read N, count the `[A-Za-z0-9_]+\.sql` tokens on the line,
    and MEASURE_FAIL when they differ. Echo the line. No literal 27 anywhere in ci.yml — the
    successor phase shrinks the set and the runner derives it.
    (4) `mutation-runner-floors.test.ts`: extend `GREEN_LOG` with an `unreachable: 2 file(s) … — a.sql b.sql (TODOS [REDUNDER-NONIDIOM])`
    line and a `  pending:` line; add RED arms: line deleted → exit 1 containing `MEASURE_FAIL` and
    `NO 'unreachable:` ; count changed to 3 with 2 names → exit 1 naming the mismatch. Extend the
    print-contract pin that runs the REAL `--parse-only` to require the `unreachable:` line.
    (5) `mutation-annotation-parser.test.ts`: beside the `scanCorpus reports 1 of 71 files annotated`
    pin, add an exact-list pin of `unreachableFiles` against the 27 sorted basenames (the list in
    RESEARCH § Option (a) table and PATTERNS; re-derive it from the parser and paste the parser's
    output), plus the invariant `pendingFiles.length === filesTotal − filesAnnotated − unreachableFiles.length − inertFiles.length`
    so the pin never needs to move as batches land.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only | grep -a -E '^unreachable: 27 file\(s\) ' | grep -a -o -E '[A-Za-z0-9_]+\.sql' | wc -l | grep -a -q -E '^ *27$' && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts</automated>
    The real static run prints the line with 27 names for a claimed 27; the GREEN/RED count-recheck
    arms and the exact-list pin are green. Run vitest ALONE on the box (memory: contention manufactures timeouts).
  </verify>
  <done>Both modes print `unreachable:` with 27 names; ci.yml MEASURE_FAILs on absence or count/name mismatch; GREEN_LOG carries the line and two RED arms prove the step can fail; the 27-name exact-list pin and the pending invariant are green.</done>
</task>

<task type="auto">
  <name>Task 2: Per-file `judged / annotated / waived / biting` breakdown, cross-summed in-process and by CI</name>
  <files>scripts/mutation-runner/run.mjs, .github/workflows/ci.yml, src/__tests__/mutation-runner-floors.test.ts</files>
  <action>
    In the corpus loop of `runCorpus`, keep a per-file tally: `annotated` (twins), `waived`,
    `judged` (arms of this file that reached a verdict — executed minus this file's unjudged), and
    `biting` (judged minus this file's `no-red` / `wrong-first-failure` / `synthesised-identity`
    defects). A file skipped on a `baseline` defect keeps judged 0 / biting 0 while `annotated` is
    still counted — that visible gap is the point (RESEARCH Pitfall 2: ratchet on `biting`, never
    on `annotated`). Print, after `lane-invocations:` and before the `  waived:` lines, one line per
    annotated file in the per-item shape of the waived line named in the context list:
    `  file <basename>: judged J / annotated A / waived W / biting B`
    In `--parse-only` print the same lines with judged 0 / biting 0 and annotated/waived real.
    Add to `absurdityViolations` (the in-process floor) two relations: Σ per-file `biting` ===
    `bitingArms` and Σ per-file `annotated` === `armsAnnotated`; a disagreement is an `absurdity`
    defect naming both numbers.
    `ci.yml` count-recheck, after the lane-invocations assertion: extract every `^  file .*: judged`
    line; MEASURE_FAIL when there are none; MEASURE_FAIL when their count differs from
    `files_annotated` (already read from the coverage line); sum the `biting N` fields with `awk`
    and MEASURE_FAIL when the sum differs from `biting` (already read). No literal integer restated.
    `mutation-runner-floors.test.ts`: add to `GREEN_LOG` the line
    `  file test_strategy_shares_rls.sql: judged 30 / annotated 30 / waived 0 / biting 30`; RED arms:
    all file lines deleted → MEASURE_FAIL; a second file line added while coverage still says 1 →
    count mismatch; the one line's `biting 30` changed to `biting 29` → sum mismatch. Also extend the
    self-test scenario for the FILES_FLOOR regression only if its assertions read the log shape
    (check first; do not add an assertion that cannot fail).
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --parse-only | grep -a -q -E '^  file test_strategy_shares_rls\.sql: judged 0 / annotated 30 / waived 0 / biting 0$' && npx vitest run src/__tests__/mutation-runner-floors.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    The full run is still green with the new lines (floors unchanged at 1 / 30 / 0), the static
    mode prints the per-file line in its documented shape, the count-recheck GREEN/RED arms pass,
    and the tree is clean after the runner (no dirty-checkout).
  </verify>
  <done>One indented per-file line per annotated file in both modes; in-process and CI cross-sums exist and are proven able to fail by the RED arms; the full run remains exit 0 with `arms: 30/30/0`.</done>
</task>

<task type="auto">
  <name>Task 3: Parse-time refusal of fixture targets, its red fixture and self-test scenario, GRAMMAR and prose</name>
  <files>scripts/mutation-runner/parse.mjs, scripts/mutation-runner/run.mjs, scripts/mutation-runner/fixtures/selftest/fixture-target-gate.sql, src/__tests__/mutation-annotation-parser.test.ts, scripts/mutation-runner/GRAMMAR.md, .github/workflows/ci.yml, .planning/WINDOWS.md</files>
  <action>
    In parse.mjs, where a step's file is validated as repo-relative — the throw at
    scripts/mutation-runner/parse.mjs:198

    add a refusal for any step file whose path begins with scripts/pg-lane/fixtures/ carrying the
    message: `"file" targets a pg-lane stand-in fixture (scripts/pg-lane/fixtures/**) — a mutation to a stand-in proves the fixture author's guess, not production; target a supabase/migrations/** file, the gate itself, or use a sql step`.
    Parse time is the right seam (PATTERNS § P2.4): a refused annotation is malformed, so it is
    never counted as a twin and parity reds as well — the same shape as rule 3a. Measured at HEAD:
    0 twins target a fixture, so the rule refuses nothing that exists.
    Add `scripts/mutation-runner/fixtures/selftest/fixture-target-gate.sql`, deliberately defective
    in the style of the existing selftest fixtures (header stating it is never part of the green
    corpus), whose single twin edits `scripts/pg-lane/fixtures/01-fixture-core.sql`. Add
    `--self-test` scenario sixteen: `runCorpus` over that file must exit 1 with a `parse` defect
    whose message contains `stand-in fixture`, and must NOT report `no-red`. Renumber every SELF-TEST
    banner's denominator from fifteen to sixteen, including the closing PASSED banner's list, and
    append the sixteenth, with today's date, to the self-test step comment in ci.yml whose lines are
    .github/workflows/ci.yml:991-995

    In the parser test, under the rule-3a describe block that starts at
    src/__tests__/mutation-annotation-parser.test.ts:390

    add both polarities: a step whose file is under supabase/migrations/ is accepted, a step whose
    file is under scripts/pg-lane/fixtures/ is refused with the message.
    GRAMMAR.md: in the edit and insert-after schema rows for the file key, add "May not be under
    scripts/pg-lane/fixtures/" with the one-sentence WHY, and update the header claim about
    "~70 more gate files" to the amended scope (43 remaining idiom files plus the reference's 15
    un-twinned sections; 27 non-idiom files excluded and printed); that header claim sits at
    scripts/mutation-runner/GRAMMAR.md:3

    WINDOWS.md entry 28 still reads open while CLAUDE.md says it closed on run 33620169220 at
    89cbef8b — set the entry to fixed with that evidence and a closed timestamp, so the two stop
    disagreeing (PATTERNS item 13b); the row is
    .planning/WINDOWS.md:45

    The floors do not move in this plan: after the final full run, record in the SUMMARY the
    measured `coverage: files 1/71`, `arms: 30/30/0`, `biting: 30`, `lane-invocations: 30` as the
    unchanged ratchet value — the end-of-every-plan rule satisfied by a measurement, not skipped.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-annotation-parser.test.ts src/__tests__/mutation-runner-floors.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && [ -z "$(git status --porcelain)" ]</automated>
    Sixteen self-test scenarios pass including the new refusal; the four lockstep test files are
    green; ci.yml parses; the tree is clean. Run these SERIALLY, nothing else on the box.
  </verify>
  <done>A twin targeting a stand-in is refused at parse time in all three modes, proven by a red fixture and a scenario; GRAMMAR states the rule; WINDOWS entry 28 agrees with CLAUDE.md; the SUMMARY records the unchanged measured floors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate/migration/fixture files → runner → throwaway cluster | annotations are executed input; a twin can name any repo file |
| runner stdout → ci.yml count-recheck | CI reads numbers out of a text file it did not produce |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4-01 | Tampering | a twin mutating a stand-in fixture to manufacture a RED | high | mitigate | parse-time refusal on the `scripts/pg-lane/fixtures/` prefix + red fixture + scenario sixteen (Task 3) |
| T-164.4-04 | Repudiation | a silent subset — the runner covering 44 of 71 while the log reads as full coverage | high | mitigate | `unreachable:` line derived by the runner and MEASURE_FAIL-asserted by CI with a count/name cross-check (Task 1) |
| T-164.4-05 | Repudiation | a file whose baseline went red still counting as annotated coverage | medium | mitigate | per-file `judged` column; Σ biting cross-summed in-process and by CI (Task 2) |
| T-164.3-21 | Repudiation | waiver creep hiding non-biting arms | medium | mitigate | carried forward: per-file `waived` column printed; WAIVED_CEILING unchanged at 0 |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed by this plan |
</threat_model>

<verification>
- `node scripts/mutation-runner/run.mjs` exit 0 printing `coverage: files 1/71`, `unreachable: 27 file(s) …` with 27 names, one `  file …` line, `arms: 30/30/0`, `biting: 30`, `lane-invocations: 30`.
- `node scripts/mutation-runner/run.mjs --self-test` passes sixteen scenarios.
- The four lockstep vitest files green; `git status --porcelain` empty.
- No line anchor in any later plan points into the files this plan changed (they cite symbols) — spot-check with `node scripts/verify-plan-anchors.mjs --pending`.
</verification>

<success_criteria>
- Criterion 1 as amended is emitted by the gate itself: the exclusion is printed by name and asserted by CI. Criterion 3 gains a per-file view a half-annotated file cannot hide from. The batches inherit one new refusal, proven able to fire.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-01-SUMMARY.md` when done. Record: the exact printed `unreachable:` line, the per-file line for the reference file, the sixteen-scenario self-test result, and the unchanged measured floors (files 1/71, arms 30/30/0, biting 30).
</output>
