---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 03
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet]
wave: 4
depends_on: ["164.4-02"]
files_modified:
  - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
  - scripts/mutation-runner/run.mjs
  - scripts/mutation-runner/parse.mjs
  - scripts/mutation-runner/VAC06-DEMOS.md
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 1 of the new files: test_reconcile_dropped_enqueue_sweep.sql (39 sections) fully annotated and proven"
  - "FILES_FLOOR 1 → 2 and ARMS_FLOOR → measured (expected 84), the first FILE move of the phase with the scanCorpus pin and coverage prose moved in lockstep"
affects: [164.4-04]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "test_reconcile_dropped_enqueue_sweep.sql carries exactly one RED-UNDER-SETUP whose apply list goes GREEN on the lane, and every one of its 39 sections has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0; every new twin is RED (identity ok) and the file restores GREEN; the per-file line shows judged 39 / annotated 39 / biting 39 (SC-2, SC-3)"
    - "FILES_FLOOR and ARMS_FLOOR equal the run's printed coverage and biting, with dated blocks and both separation directions beside each constant; the scanCorpus exact pin lists both annotated files (SC-3)"
    - "Every waiver, if any, is in the twin with its reason and WAIVED_CEILING moved by exactly that count; dead arms are TODOS findings (SC-4)"
  artifacts:
    - path: "supabase/tests/test_reconcile_dropped_enqueue_sweep.sql"
      provides: "39 sections twinned; one RED-UNDER-SETUP"
      contains: "RED-UNDER-SETUP"
    - path: "scripts/mutation-runner/run.mjs"
      provides: "FILES_FLOOR=2 and ARMS_FLOOR at the measured value"
      contains: "FILES_FLOOR"
  key_links:
    - from: "supabase/tests/test_reconcile_dropped_enqueue_sweep.sql"
      to: "164.4-00-FIXTURE-STRATEGY.md"
      via: "the RED-UNDER-SETUP apply list is the one Plan 00 proved GREEN, in the same order"
      pattern: "RED-UNDER-SETUP"
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 2, annotatedFiles both names sorted"
      pattern: "scanCorpus"
---

<objective>
Annotate and prove the largest idiom file, `test_reconcile_dropped_enqueue_sweep.sql`:
planner-measured 1160 lines, 63 idiom raises, **39 identities = 39 sections** (its identities are
path-shaped — `1/JOB-04/B4`, `2/arm C2b/JOB-04/B4/SC#3` — with no trailing sub-suffix, so the
section rule collapses nothing and one twin per identity is required), 0 non-idiom raises, 9
distinct migration stamps named in the file. It has 8 `-- ====` banners for 39 sections — derive
sections from identities, never from banners. Re-derive with the one-liner in Task 2.

This is the first plan that annotates a NEW file, so it is the first FILE_FLOOR move: the
`scanCorpus … 1 of 71` pin, the `annotatedFiles` list, `coverage: files` in GREEN_LOG and the
"1 of 71" currency prose all move here. Its baseline apply list was proven GREEN by Plan 00 and
recorded in that plan's fenced apply-list block — use it verbatim, in that order.

Purpose: the biggest single-file batch, landed while the run is still short (~88 lanes).
Output: 39 twins, one setup line, FILES_FLOOR 2, ARMS_FLOOR → measured (expected 84).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Read before implementing: Plan 00's record (the apply list, the fixture decision, the three
authoring rules), Plan 02's SUMMARY (the rehearsed pin choreography and any surprises), PATTERNS
§ P1 (shapes), § P2.1 (setup line placement: column 0 in the header, preceded by a 3-line prose
paragraph, stand-ins first, real migrations after, last-defining migration last), § P3 and § P4.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/parse.mjs scanCorpus -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Setup line + ONE section end-to-end — parse, lane RED, restore GREEN</name>
  <files>supabase/tests/test_reconcile_dropped_enqueue_sweep.sql</files>
  <precondition>164.4-02-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`); 164.4-00-FIXTURE-STRATEGY.md carries a fenced apply-list block for this file.</precondition>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-03-PLAN.md` and halt on non-zero.

    Add the file's `RED-UNDER-SETUP` line at column 0 in its header, preceded by a 3-line prose
    paragraph in the shape of the reference's header (what the twins are, what the line declares),
    with the apply list from Plan 00's record verbatim and in order. Exactly one such line per
    file. Re-prove the baseline on the lane once (`bash scripts/pg-lane/run.sh --workdir "$SCRATCH/lane" --apply <list> --gate supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` → exit 0)
    before writing any twin — a red baseline means every arm is skipped with judged 0.

    Then author ONE section: take the file's first identity (`1/JOB-04`), read its branch and the
    migration object it exercises, choose a mutation of the code under test (never the message,
    never the guard), measure `occurrences` against the exact target file, confirm the target is in
    the setup list, and insert the prose + twin pair in the P1.1 shape directly above the section's
    first assertion statement. Where a broader `whole-block` arm fires first under the same
    mutation, encode a `neuter` (idiom arms only). Run `--parse-only` and the diagnostic lane and
    read the verdict line.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_reconcile_dropped_enqueue_sweep.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
    Static walk green with the new file counted (the `unreachable:` and `pending:` lines shift by
    one), and the first twin's own lane reads RED (identity ok).
  </verify>
  <done>One setup line and one proven twin exist in the file; the baseline lane exits 0; nothing else changed.</done>
</task>

<task type="auto">
  <name>Task 2: Twin the remaining 38 sections</name>
  <files>supabase/tests/test_reconcile_dropped_enqueue_sweep.sql</files>
  <action>
    Run the section-coverage one-liner (the verify command) first and paste its output into the
    SUMMARY; work from ITS list. For every untwinned section author one prose + twin pair as in
    Task 1: the `arm` is the section's real identity string; the mutation edits the migration that
    LAST defines the object (grep the function or table name across `supabase/migrations/` and take
    the newest that is in the setup list — if the newest is NOT in the list, the list is wrong, fix
    the list and re-prove the baseline); `occurrences` measured with `grep -a -o -F`; a `sql` step
    for grant drift a migration pins exactly; a LAYERED `apply` when a migration's own verification
    block would abort the apply; `neuter` only for an idiom arm that would fire first. No twin may
    target `scripts/pg-lane/fixtures/` (parse refuses it since Plan 01).

    Waivers: only for an arm that DOES bite but cannot be mutated mechanically — reason inside the
    twin (Shape 4); each one is +1 to WAIVED_CEILING in Task 3 with the reason beside the constant;
    if the cumulative phase total would exceed 5, STOP and return a checkpoint to the founder rather
    than ratchet. An arm that cannot fail at all is a FINDING → `TODOS.md` + SUMMARY, no twin, said
    loudly. Never delete or weaken a prose annotation, an arm or a failure message to make a run
    pass; never rename an identity; `git diff` on the gate file must show only added comment lines.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_reconcile_dropped_enqueue_sweep.sql</automated>
    Parity and grammar hold; the one-liner prints `sections 39 untwinned 0` and exits 0 (a booked
    dead-arm finding is the only permitted exception and must be named in the SUMMARY).
  </verify>
  <done>All 39 sections twinned (or booked), parse-only green, diff is comment lines only.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 1 → 2, ARMS_FLOOR → measured, every pin and prose site in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, scripts/mutation-runner/parse.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, scripts/mutation-runner/VAC06-DEMOS.md, CLAUDE.md</files>
  <action>
    Run `node scripts/mutation-runner/run.mjs`; iterate on named defects (a `baseline` defect means
    the setup list, not the twins; `wrong-first-failure` means a neuter or a different mutation;
    `no-red` means the wrong bytes or the wrong migration copy). Read `coverage:`, `arms:`,
    `biting:`, `lane-invocations:`, `per-arm lane time:` and both per-file lines OFF THE RUN LOG.
    Expected `coverage: files 2/71`, `biting: 84`; pin what was measured.

    In ONE commit with the annotations (PATTERNS § P4):
    Group A — `run.mjs`: `FILES_FLOOR` 1 → 2 with a compact dated block appended above it
    (`RE-DERIVED <date> (plan 164.4-03) — MEASURED … coverage: files 2/71 … COVERAGE: the two files by
    name … SEPARATION: fires at 1, silent at 2 … RECORD 164.4-03-SUMMARY.md`); `ARMS_FLOOR` → measured
    biting with the same block shape (SAMPLE SIZE, both directions); currency lines updated
    (the FILES_FLOOR comment's "1 of 71" and "other … files" claims, the CURRENCY block); every dated
    historical block untouched. `WAIVED_CEILING` only if a waiver was accepted. `parse.mjs`: the
    scanCorpus doc comment's "unchanged at 1 of 71" → count-free ("moves as Phase 164.4 batches land;
    the exact pin lives in the parser test").
    Group B — `KNOWN_THRESHOLD_SITES`: `FILES_FLOOR=2`, `ARMS_FLOOR=<measured>`, dates updated.
    Group C — `mutation-runner-floors.test.ts`: `totalAnchored` → measured total prose markers (45 + 39
    = 84 expected); `GREEN_LOG`: `coverage: files 2/71`, `arms:`, `biting:`, `lane-invocations:` (both
    numbers), `mean … over N arm run(s)`, TWO per-file lines whose biting sums to the aggregate, the
    `"… arm lane(s) spawned"` assertion string. `mutation-annotation-parser.test.ts`: `armsSeen`,
    `stepsSeen` → measured; the scanCorpus pin: title "2 of 71", `filesAnnotated` 2, `annotatedFiles`
    the two basenames sorted; `filesTotal` stays 71.
    Group D — `VAC06-DEMOS.md` "Coverage is files 1/71 … the other 70 files are Phase 164.4" →
    count-free ("coverage is the run log's coverage: line; the remaining idiom files land in 164.4
    batches"); `CLAUDE.md` currency sentence → this batch's measured `arms:` and `coverage:`.
    `timeout-minutes`: unchanged unless the measured run (ubuntu, from Plan 02's run, or local ÷ 1.6)
    is ≥ 10 min — ~88 lanes here, so it will not be.
    Verify SERIALLY, nothing else on the box.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + the deletion guard), confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head, record run id + SHA + the job's measured wall clock in the SUMMARY. Plan 04's precondition reads it.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 2/71` and `biting:` == ARMS_FLOOR; all pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run and its wall clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; mutations touch scratch copies only |
| stand-in fixture → gate | a stand-in provides only what the gate names; twins may not target it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep | medium | mitigate | reason in the twin, printed; WAIVED_CEILING +1 per reviewed waiver; >5 cumulative → STOP |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal (Plan 01); apply list lists real migrations for every mutated object |
| T-164.4-05 | Repudiation | a red baseline counted as coverage | medium | mitigate | baseline re-proven before authoring; per-file `judged` read off the run |
| T-164.4-07 | Repudiation | floor pinned from the artifact | medium | mitigate | every number quoted from the run log in the dated block |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: `sections 39 untwinned 0`; parse-only exit 0.
- Full run: `coverage: files 2/71`, `biting:` == ARMS_FLOOR, tallies agree, per-file line `judged 39 / annotated 39 / waived 0 / biting 39`, exit 0.
- Four lockstep vitest files green; tree clean; `node scripts/verify-plan-anchors.mjs --pending` green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- The largest new file is fully proven; FILES_FLOOR and ARMS_FLOOR moved by the measured delta in the same commit; the first file-move choreography (scanCorpus pin, coverage prose) is rehearsed for the nine batches that follow.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-03-SUMMARY.md` when done. Record: the one-liner output, the setup apply list, the run log's lines, floors old → new, waivers/findings, PR number, SHA-bound ubuntu run id and wall clock.
</output>
