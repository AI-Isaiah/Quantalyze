---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 02
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, reference-file, lockstep-pins, floor-ratchet]
wave: 3
depends_on: ["164.4-01"]
files_modified:
  - supabase/tests/test_strategy_shares_rls.sql
  - scripts/mutation-runner/run.mjs
  - scripts/mutation-runner/GRAMMAR.md
  - scripts/mutation-runner/VAC06-DEMOS.md
  - scripts/pg-lane/README.md
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - .github/workflows/ci.yml
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "The reference file meets criterion 1: all 35 sections carry a twin (15 new), every one proven RED under its own mutation and GREEN restored"
  - "ARMS_FLOOR ratcheted from 30 to the measured biting count (expected 45) with the full lockstep-pin choreography rehearsed once at the cheapest possible scale"
  - "Currency prose made count-free wherever it was a currency claim, so later batches touch fewer sites"
affects: [164.4-03, every batch plan after it]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every section of test_strategy_shares_rls.sql has a RED-UNDER prose marker and an adjacent RED-UNDER-M twin; the parity gate passes; the section-coverage one-liner reports 0 untwinned (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok) and the file GREEN on restore; biting == executed == lane-invocations (SC-2)"
    - "ARMS_FLOOR equals the biting count read off THAT run, with a dated MEASURED block and both separation directions beside the constant; FILES_FLOOR stays 1 because no new FILE was annotated (SC-3)"
    - "Any waiver candidate is a STOP-and-escalate checkpoint, not an executor decision: WAIVED_CEILING stays 0 unless the founder accepts a named waiver on the record; a dead arm is a TODOS finding, not a waiver (SC-4)"
    - "⭐ The `annotated >= sections` pin is ARMED here, in the parser test, for every file the runner judged. Plan 01 could only PRINT `sections S` — the reference file was 30 twins over 35 sections until this plan closed the 15, so the pin would have failed on the very run Plan 01 measured. Once armed it is the DURABLE half-annotation control: after the phase ends, a file carrying 1 twin over 39 sections fails the build, which the batch plans' Task 2 one-liner cannot do because it only runs while a batch plan is executing (SC-1/SC-3)"
    - "The pin is proven able to fail before it is trusted: temporarily drop one twin from the reference file, observe the parser arm RED naming the file and both counts, restore from a byte backup (never `git checkout --`), confirm sha256 identical and `git status --porcelain` empty. A pin nobody watched fail is not yet a pin"
  artifacts:
    - path: "supabase/tests/test_strategy_shares_rls.sql"
      provides: "45 twins over 35 sections"
      contains: "RED-UNDER-M"
    - path: "scripts/mutation-runner/run.mjs"
      provides: "ARMS_FLOOR at the measured value with its dated block"
      contains: "ARMS_FLOOR"
  key_links:
    - from: "scripts/mutation-runner/run.mjs"
      to: "src/__tests__/gate-family-meta.test.ts"
      via: "KNOWN_THRESHOLD_SITES embeds the constant's VALUE; both directions asserted"
      pattern: "ARMS_FLOOR="
    - from: "src/__tests__/mutation-runner-floors.test.ts"
      to: ".github/workflows/ci.yml"
      via: "GREEN_LOG must carry the new floor numbers or the LIVE count-recheck step reds the GREEN arm"
      pattern: "GREEN_LOG"
---

<objective>
Close the reference file. `test_strategy_shares_rls.sql` was the "1 file fully annotated" baseline
and never was: planner-measured at HEAD 9d08bc2a with the SECTION rule (Plan 01's objective), it
holds **35 sections / 103 identities / 30 twins over 20 sections**, so **15 sections have no twin**:
`SHAPE 2, TENANT 1, TENANT 2, TENANT 3, TENANT 4, NO-DELETE 1, REVOKE 1, REVOKE 2, TRIGGER 1,
TRIGGER 2, MONOTONIC 1, ANON 1, ANON 2, SERVICE-ROLE 0, SERVICE-ROLE 1`. Twin each of them, drive
the full run green, and ratchet ARMS_FLOOR to the measured biting count. Re-confirmed at HEAD
007708dd by the runner's own `gateSectionCount` (35) and its `--parse-only` row
(`sections 35 / annotated 30`).

This batch is taken out of largest-first order on purpose (PATTERNS § P5, "adjustment worth
taking"): its `RED-UNDER-SETUP` already exists, so it is the cheapest possible rehearsal of the
whole lockstep-pin choreography every later batch repeats. Count discipline: re-derive the section
list with the one-liner in Task 2 and work from the parser's output, not from this objective.

⚠️ Line-number consequence. Inserting 15 prose+twin pairs shifts every line after each insertion.
GRAMMAR.md labels its four worked shapes with line cites into this file (Shape 1 `:417-419`,
Shape 1b `:376-378`, Shape 2 `:1561-1577`, Shape 3 `:675-681`, correct at HEAD) — Task 3
re-resolves them. No later plan cites a line in this file.

Purpose: criterion 1 holds for the one file that already claimed it, and the ratchet mechanism is
proven at scale 15 before it runs at scale 41 (Plan 04, the ledger family).
Output: 15 new twins, ARMS_FLOOR 30 → measured (expected 45), all pins moved in the same commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Read before implementing: PATTERNS § P1 (the four shape templates, all in this file: Shape 1 at
supabase/tests/test_strategy_shares_rls.sql:417-419, Shape 1b at :376-378, Shape 2 at :1561-1577,
Shape 3 at :675-681), § P1.2 (the arm is a REAL identity, never a section label), § P3 (the
floor-ratchet block shape, LINEAGE vs CURRENCY), § P4 (the lockstep-pin checklist — every item).
The file header at supabase/tests/test_strategy_shares_rls.sql:40-61 records which arms had their
neuter re-observed and the existing setup line. Plan 01's SUMMARY names the runner lines this
batch's GREEN_LOG must carry (`unreachable:`, `  pending:`, the per-file row).

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs WAIVED_CEILING -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs gateSectionCount -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts countRecheckScript -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts scanCorpus -->
<!-- verify-symbol: src/__tests__/vac06-mechanism-arms.test.ts SANITIZE -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One un-twinned section end-to-end — twin, parse, lane RED, restore GREEN</name>
  <files>supabase/tests/test_strategy_shares_rls.sql</files>
  <precondition>164.4-01-SUMMARY.md records a merged PR (#731) whose head SHA (f44a517c) has a `sql-mutation` run on ubuntu (33683536312) with conclusion success — re-assert with `gh run view 33683536312 --json headSha,conclusion`; the tree is at or after merge commit 53a55e1c.</precondition>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-02-PLAN.md` and halt on non-zero.

    Pick `NO-DELETE 1` (a single-identity section: the arm string is exactly `NO-DELETE 1`). Read its
    branch and the migration objects it exercises, choose a mutation that changes what the gate
    DOES (the grant, policy or trigger under test — never the failure message or its guard: rules
    3a/3b), measure `occurrences` with `grep -a -o -F '<needle>' <exact file> | wc -l` BEFORE writing
    the twin (rule 2), confirm the target file is in the existing `RED-UNDER-SETUP` list (else
    `bad-file-ref`), and insert the pair in the P1.1 shape: prose line(s) first, twin on the very next
    line, both at comment line start with two-space indent, the last thing before the assertion's
    own statements. If the section's raise sits behind an exact-set pin that fires first, encode the
    prerequisite as a `neuter` field (Shape 2) — the header lines 40-44 name the arms whose neuters
    were re-observed; if the natural mutation is grant drift the migration pins exactly, use a `sql`
    step. Run `--parse-only` (exit 0, parity 31/31 for this file) and the diagnostic lane for that
    arm; read the verdict line — `RED (identity ok)` — not the exit code (`--file` never exits 0).
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node scripts/mutation-runner/run.mjs --file supabase/tests/test_strategy_shares_rls.sql --arm "NO-DELETE 1" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
    Static parity holds with the new twin counted, and the arm's own lane reports it as the FIRST
    failure with attributable identity.
  </verify>
  <done>`NO-DELETE 1` carries prose + twin, parses clean, and its diagnostic lane reads `RED (identity ok)`; nothing else in the file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Twin the remaining 14 sections</name>
  <files>supabase/tests/test_strategy_shares_rls.sql</files>
  <action>
    Re-derive the section list and the untwinned set with the section-coverage one-liner (it is the
    verify command below; run it first and paste its output into the SUMMARY). For each untwinned
    section author one prose + twin pair exactly as in Task 1: the `arm` is one REAL identity of
    that section (e.g. `SHAPE 2a`, `TENANT 1a`, `REVOKE 2a`, `ANON 1a`, `SERVICE-ROLE 0-acl`),
    the mutation targets the migration that LAST defines the object, `occurrences` is measured, the
    pair sits immediately above the section's first assertion statements. Where a section's
    identities test different bits, a second twin on a different identity is allowed (the
    reference's own practice) but one per section is the requirement. Use `neuter` only for an
    idiom arm that fires first; use a `sql` step for grant drift the migration pins exactly; use a
    LAYERED multi-step `apply` when the migration's own verification block would abort the apply
    (Shape 1b/3). ⚠️ `SERVICE-ROLE 2a-2d` were the four backward scans the neuter tokenizer REFUSED
    at 164.3.1-09; they are twinned already and are not in this list — do not touch them.

    Waivers: `WAIVED_CEILING` is 0 and this plan does not move it. If an arm DOES bite but cannot be
    mutated mechanically, do NOT write a Shape 4 twin: STOP, record the arm and the reason in the
    SUMMARY, and return a checkpoint to the founder (a waiver is a founder decision, not an executor
    one — CONTEXT § Waivers). An arm that cannot fail at all is a FINDING → `TODOS.md` + SUMMARY, no
    twin, stated loudly. Never delete or weaken a prose annotation, an arm or a failure message to
    make a run pass — the corpus is the specification (D-02). Do not rename any identity: the
    companion identities pinned by vac06-mechanism-arms would red. After all pairs: `--parse-only`
    exit 0.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_strategy_shares_rls.sql</automated>
    Parity and grammar hold statically, and the one-liner prints `sections 35 untwinned 0` and exits
    0. If a dead-arm finding was booked, its section is the ONLY permitted untwinned entry and the
    SUMMARY names it — a silent non-zero here is a failure.
  </verify>
  <done>Every section of the reference file has a twin (or a booked dead-arm finding), the file's twin count is 45 by line-anchored count, and `git diff` shows only added comment lines.</done>
</task>

<task type="auto">
  <name>Task 3: Drive the full run green, ratchet ARMS_FLOOR from the run, arm the annotated>=sections pin, move every lockstep pin and prose site in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, scripts/mutation-runner/GRAMMAR.md, scripts/mutation-runner/VAC06-DEMOS.md, scripts/pg-lane/README.md, .github/workflows/ci.yml, CLAUDE.md</files>
  <action>
    Run `node scripts/mutation-runner/run.mjs` and iterate on the defects it names
    (`wrong-first-failure` → a missing neuter or a different mutation; `no-red` → the twin edits the
    wrong bytes or the wrong migration copy; `occurrence-mismatch` → re-measure). Read
    `coverage:`, `arms:`, `biting:`, `lane-invocations:`, `per-arm lane time:` and the per-file line
    OFF THE RUN LOG — never off a grep of the file (a floor picked by reading the artifact always
    passes). Expected `biting: 45`; pin whatever was measured.

    Then, in ONE commit with the annotations (PATTERNS § P4, every item):
    Group A — `run.mjs`: `ARMS_FLOOR` → measured biting; APPEND a compact dated block immediately
    above the constant carrying, verbatim in the family's vocabulary: `RE-DERIVED <date> (plan
    164.4-02) — MEASURED on the full-corpus run at HEAD <sha>`, the printed lines, `SAMPLE SIZE`
    (arms executed, all RED (identity ok)), `COVERAGE` (still 1 file, namely the reference), both
    SEPARATION directions (fires at biting−1, silent at biting), `RECORD 164.4-02-SUMMARY.md`. Edit
    the CURRENCY lines (the block that says "CURRENCY, stated where the VALUE is" and the FILES_FLOOR
    comment's "other 70 files" → "the remaining idiom files") and leave every dated historical block
    untouched (LINEAGE). `FILES_FLOOR` stays 1 and `WAIVED_CEILING` stays 0.
    Group B — `gate-family-meta.test.ts` `KNOWN_THRESHOLD_SITES`: the `ARMS_FLOOR=` string to the new
    value, trailing comment date updated.
    Group C — `mutation-runner-floors.test.ts`: `totalAnchored` pin 30 → 45 (and its MEASURED note);
    `GREEN_LOG`: `arms:`, `biting:`, `lane-invocations:` (both numbers in the parenthesis), `mean … over N arm run(s)`,
    the per-file line for the reference (`sections 35 / judged 45 / annotated 45 / waived 0 / biting 45`), and the GREEN assertion string `"… arm lane(s) spawned"` —
    leave `coverage: files 1/71` (true), leave `LEGIT`. `mutation-annotation-parser.test.ts`:
    `armsSeen` 30 → 45 and `stepsSeen` to the measured non-`sql` step count; the `scanCorpus … 1 of 71`
    pin is UNCHANGED (no new file). ARM the durable pin: for every file in `corpus.annotatedFiles`,
    `annotated >= gateSectionCount(text)` — and prove it can fail (drop one twin via a scratch copy
    or a byte-backed edit, observe the arm RED naming the file and both counts, restore from the
    byte backup, `shasum -a 256` identical).
    Group D — prose, made COUNT-FREE where it is a currency claim so later batches do not re-touch
    it: `ci.yml` "for each of the 30 annotated arms" → "for each annotated arm"; `ci.yml` "would still
    print arms: 30/30/0" → "would still print its arms: line"; `scripts/pg-lane/README.md` budgeting
    note → per-lane cost with a pointer to the run log's `per-arm lane time:` line; `VAC06-DEMOS.md`
    "four of the 30 arms" → "four of the arms" (its pasted run log stays: lineage); `GRAMMAR.md`
    Shape 4 sentence "all 30 bite (arms: 30/30/0, 2026-08-29)" stays as the dated measurement and
    gains a one-line currency note; `CLAUDE.md` § SQL gate integrity jobs keeps the run-33620169220
    quote and gains one currency sentence with this batch's measured `arms:`; re-resolve GRAMMAR's
    four Shape line cites against the file after insertion (`grep -an '^[ \t]*--[ \t]*RED-UNDER'`).
    `timeout-minutes`: unchanged (~47 lanes; the measured ubuntu job at 32 lanes was 80 s).
    Finally the verify below, SERIALLY, nothing else on the box.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land this batch as its own PR (`/ship`, then `/gsd-pr-branch` + the deletion guard in CLAUDE.md § PR branches), then confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head (`gh run view <id> --json headSha,conclusion`), and record the run id + SHA + the job's measured wall clock in the SUMMARY. Plan 03's precondition reads it.</human-check>
    Full run exit 0 with the pinned floors equal to the printed numbers; self-test green; the four
    lockstep files green; tree clean.
  </verify>
  <done>Every twin bites, ARMS_FLOOR equals the run's `biting:`, the annotated>=sections pin is armed and was observed RED once, every checklist item moved in the same commit, GRAMMAR's Shape cites resolve, and the SUMMARY records the SHA-bound ubuntu-green run id and wall clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; mutations touch scratch copies only |
| batch PR → main | the floors and pins land together or the aggregator reds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep hiding a non-biting arm | medium | mitigate | no executor-authored waivers; a waiver candidate is a STOP checkpoint to the founder; WAIVED_CEILING stays 0 |
| T-164.4-06 | Tampering | a twin that satisfies the detector, not the arm (identity injection / rewrite) | high | mitigate | rules 3a/3b/3c already refuse it; every twin here mutates the code under test, never a message or guard |
| T-164.4-07 | Repudiation | a floor pinned by reading the artifact rather than the run | medium | mitigate | Task 3 reads every number off the run log and quotes it in the dated block |
| T-164.4-08 | Denial of Service | lane time growth past `timeout-minutes` | low | mitigate | measured per batch; ~47 lanes here against an 80 s job at 32 lanes |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- Section-coverage one-liner: `sections 35 untwinned 0` for the reference file.
- Full run: `coverage: files 1/71`, `arms: 45/45/0` (or measured), `biting:` == `ARMS_FLOOR`, tallies agree, exit 0.
- Four lockstep vitest files green; `git status --porcelain` empty; `node scripts/verify-plan-anchors.mjs --pending` green after the commit.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- The reference file satisfies criterion 1 under the SECTION unit; the ratchet moved by the measured delta in the same commit as the annotations; the choreography is proven before the large batches.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-02-SUMMARY.md` when done. Record: the one-liner output, the run log's lines, ARMS_FLOOR old → new, waiver candidates (expected 0) and any dead-arm finding, the merged PR number, the SHA-bound ubuntu `sql-mutation` run id and its wall clock.
</output>
