---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 00
subsystem: testing
tags: [pg-lane, fixtures, mutation-runner, red-under, postgres, pg_cron, spike]

requires:
  - phase: 164.3
    provides: "scripts/pg-lane/run.sh (throwaway PostgreSQL lane) and scripts/mutation-runner/ (RED-UNDER runner, floors, tallies)"
provides:
  - "DECISION: stand-ins — the fixture strategy every RED-UNDER-SETUP line in this phase is written against, decided by a rule fixed before any probe ran"
  - "A baseline-GREEN, three-times-timed apply list (mean 1.01 s/lane) plus two additive pg-lane fixtures"
  - "The F1 residual closed by measurement: 0 CONTEXT:/LOCATION:-shaped RAISE message lines across all 71 gate files"
  - "MEASURED BLOCKER [REDUNDER-PGCRON]: 3 idiom gate files (71 of 355 sections) can never reach a GREEN pg-lane baseline"
  - "MEASURED FINDING [REDUNDER-SAVEPOINT]: a committed migration cannot apply to a vanilla PostgreSQL 16 at all"
affects: [164.4-01, 164.4-02, 164.4-03, every batch plan in phase 164.4]

actuals:
  tokens: 7700
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns:
    - "Additive pg-lane stand-in fixtures (NN-fixture-<area>.sql), IF NOT EXISTS only, never a second destructive base"
    - "A stand-in fixture may sit BETWEEN two migrations when the table it patches is migration-created"

key-files:
  created:
    - .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md
    - scripts/pg-lane/fixtures/03-fixture-compute-jobs.sql
    - scripts/pg-lane/fixtures/04-fixture-compute-jobs-targets.sql
  modified:
    - TODOS.md
    - .planning/STATE.md
    - .planning/ROADMAP.md

key-decisions:
  - "DECISION: stand-ins. The stubbed real migration chain failed rule (i) terminally at stub iteration 3 of 8 (51 of 262 migrations) and never reached a baseline to time."
  - "Task 2's target file was SUBSTITUTED by measurement: the plan's largest idiom file cannot be baselined on the pg-lane at all, so the stand-in loop was proven on test_ledger_refresh_composite_arm.sql instead."
  - "Neither blocker was worked around: no package installed, no migration edited, no pg_extension row faked. Both booked to TODOS.md."

patterns-established:
  - "Decision-by-pre-stated-rule: the accept/reject rule is committed BEFORE the probes, so the numbers cannot be read to taste"
  - "A terminal stop is a finding about the thing probed, never a reason to widen the stub"

requirements-completed: []

coverage:
  - id: D1
    description: "The fixture strategy is chosen by a MEASURED rule stated before the probes ran, with both candidates' numbers, the stop point and an explicit DECISION line on the record"
    requirement: "SC-2"
    verification:
      - kind: other
        ref: "grep -a -q -E '^DECISION: (stand-ins|stubbed-chain)$' 164.4-00-FIXTURE-STRATEGY.md"
        status: pass
      - kind: integration
        ref: "bash scripts/pg-lane/run.sh --workdir $SCRATCH/lane --apply <stubs+262 migrations> --gate test_reconcile_dropped_enqueue_sweep.sql (3 stub iterations, exits 3/3/3, walls 1.31/1.24/1.77)"
        status: pass
    human_judgment: false
  - id: D2
    description: "An apply list that takes a real idiom gate file to exit 0 on a fresh throwaway cluster, timed three times, with the additive fixtures it needs"
    requirement: "SC-2"
    verification:
      - kind: integration
        ref: "bash scripts/pg-lane/run.sh --workdir ${TMPDIR}/164-4-00-lane --apply $APPLY --gate supabase/tests/test_ledger_refresh_composite_arm.sql → exit 0, ALL 10 ARMS EXECUTED (A-J), mean 1.01 s over 3 runs"
        status: pass
    human_judgment: false
  - id: D3
    description: "The corpus-wide F1 residual check — no RAISE message line shaped like a psql CONTEXT:/LOCATION: field"
    verification:
      - kind: other
        ref: "grep -a -n -E '^(CONTEXT|LOCATION):' supabase/tests/*.sql → 0 hits (exit 1)"
        status: pass
    human_judgment: false
  - id: D4
    description: "The plan's must-have 'the largest idiom file has an apply list that goes GREEN' — AMENDED BY MEASUREMENT, NOT MET"
    requirement: "SC-2"
    verification:
      - kind: integration
        ref: "bash scripts/pg-lane/run.sh --apply $APPLY --gate supabase/tests/test_reconcile_dropped_enqueue_sweep.sql → exit 3 at :471, 'TEST FAILED (1/JOB-04): pg_cron is NOT installed on this database'"
        status: fail
    human_judgment: true
    rationale: "The failure is a LANE property, not an apply-list defect: pg_cron has no control file on the lane and the gate RAISEs on its absence by design. Whether to give the lane pg_cron (a substrate + CI change) or to defer the three affected files is a scope decision a human must take before any batch containing them. Tracked as [REDUNDER-PGCRON]."
  - id: D5
    description: "The three authoring rules every batch plan inherits, restated on the record with the lane header quoted"
    verification:
      - kind: other
        ref: "164.4-00-FIXTURE-STRATEGY.md § 'Authoring rules carried into every batch'"
        status: pass
    human_judgment: false

duration: 19 min
completed: 2026-09-02
status: complete
---

# Phase 164.4 Plan 00: Fixture strategy spike Summary

**DECISION: stand-ins — the stubbed 262-migration chain died at 51/262 on a PL/pgSQL `SAVEPOINT` that cannot parse, while the stand-in candidate goes GREEN at a 1.01 s/lane mean; and the phase's largest gate file turns out to be un-baselineable on the lane at all, for want of `pg_cron`.**

## Performance

- **Duration:** 19 min
- **Started:** 2026-09-02T19:11:32Z
- **Completed:** 2026-09-02T19:30:01Z
- **Tasks:** 3
- **Files modified:** 4 created/modified (+ 3 planning ledgers)

## Accomplishments

- **The fixture strategy is decided by measurement, not assumption.** The decision rule was fixed in the plan and committed (`d70a5f74`) before a single probe ran; both candidates' numbers are on the record. `DECISION: stand-ins`.
- **The stubbed real chain is dead, and the reason is a finding.** 3 stub iterations of the 8 allowed took it from migration 10 to migration 51 of 262, where `20260416201929_audit_log_hardening.sql` issues `SAVEPOINT` / `ROLLBACK TO SAVEPOINT` inside a PL/pgSQL `DO` body — a construct PL/pgSQL has no grammar for. Probe M1 reproduced it from an 8-line DO body on an empty cluster, proving no stub can remove it.
- **A real, proven, timed stand-in baseline exists.** `test_ledger_refresh_composite_arm.sql` reaches exit 0 with `ALL 10 ARMS EXECUTED (A-J) and passed` and no skip of its own, over 8 discovery iterations, at 1.04/0.99/1.00 s — mean **1.01 s/lane**, projecting **~5.9 min** for the 453-lane end state against a ~20-min threshold.
- **A 20%-of-corpus blocker was found and is NOT worked around.** Three idiom gate files (71 of 355 sections, including the phase's rank-1 file) RAISE on absent `pg_cron`; the lane has no `pg_cron` control file. Booked as `[REDUNDER-PGCRON]`.
- **The F1 residual is closed:** 0 `CONTEXT:`/`LOCATION:`-shaped RAISE message lines across all 71 gate files.

## Task Commits

1. **Task 1: Probe the stubbed real chain (tracer)** — `82907683` (docs)
2. **Task 2: Stand-in apply list + additive fixtures** — `e4e41b2c` (feat)
3. **Task 3: Apply the decision rule, F1 residual, authoring rules** — `b0df78f0` (docs)

Pre-task housekeeping: `f2b8a5c`-class ledger commit recording the orchestrator's phase-start STATE churn (see Deviations, Rule 3).

**Plan metadata:** this commit (docs: complete plan)

## Files Created/Modified

- `.planning/phases/164.4-.../164.4-00-FIXTURE-STRATEGY.md` — the decision record: 15 quoted lane invocations with exit codes and wall clocks, the terminal stop points, the fenced `apply-list` block, the `DECISION:` line, the F1 residual, and the three authoring rules.
- `scripts/pg-lane/fixtures/03-fixture-compute-jobs.sql` — additive, pre-migration. Primary keys for `portfolios` / `api_keys` (two real FKs point at them), the `api_keys` / `strategies` columns the staleness view and the gate's seeds name, and `strategy_analytics` / `strategy_analytics_series` stand-ins carrying only the columns those bodies read.
- `scripts/pg-lane/fixtures/04-fixture-compute-jobs-targets.sql` — additive, applied AFTER `20260411144407`. Only `compute_jobs.allocator_id` and `.api_key_id`, both named by the `compute_jobs_kind_target_coherence` constraint under test.
- `TODOS.md` — three findings booked: `[REDUNDER-PGCRON]`, `[REDUNDER-SAVEPOINT]`, `[WINDOWS-LEDGER-DRIFT]`.

## Decisions Made

- **`DECISION: stand-ins`.** Rule (i) failed terminally, so rule (ii) never applied — the chain had no baseline to time. Recorded with both candidates' numbers beside each other, as the plan required.
- **The scratchpad `00-supabase-stubs.sql` stays uncommitted.** The chain lost; promoting its stub would leave a fixture nothing uses.
- **Task 2's target file was substituted, on the record.** Ranks 2-4 were rejected on measurement, not preference: rank 2 (the reference) already has an apply list and would price nothing, rank 3 green-skips its cron parts (its arms would be unfalsifiable), rank 4 is un-baselineable for the same `pg_cron` reason as rank 1.
- **`04-fixture-compute-jobs-targets.sql` sits BETWEEN two migrations.** PATTERNS § P2.1's "stand-ins first" is therefore a default, not an invariant — Plan 01's no-fixture-target refusal must key on the twin's `"file"`, never on a fixture's position.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Committed the orchestrator's phase-start ledger churn before Task 1**
- **Found during:** setup, before Task 1
- **Issue:** `.planning/STATE.md`, `state.json` and `milestone.lock` were already modified when this executor started (the execute-phase orchestrator advanced them to phase 164.4). Task 1's done-criterion is "`git status --porcelain` shows only the record" and Task 3's verify asserts a clean tree; pre-existing churn would have failed both for reasons unrelated to this plan.
- **Fix:** committed them alone, first, with a message saying so.
- **Files modified:** `.planning/STATE.md`, `.planning/state.json`, `.planning/milestone.lock`
- **Verification:** `git status --porcelain` empty before Task 1 began.
- **Committed in:** the ledger commit preceding `82907683`

**2. [Rule 2 - Missing Critical] Substituted Task 2's target gate file after measuring that the planned one is un-baselineable**
- **Found during:** Task 2
- **Issue:** `test_reconcile_dropped_enqueue_sweep.sql:268` RAISEs when `pg_extension` has no `pg_cron` row — deliberately, as its own anti-green-skip contract. The lane has no `pg_cron` control file (`pg_available_extensions` → 0 rows; `CREATE EXTENSION pg_cron` → `0A000`). Its baseline can therefore never be GREEN, under EITHER candidate strategy, and `run.mjs:1836` never judges an arm in a red-baseline file. Delivering nothing would have left the whole phase's strategy unpriced.
- **Fix:** proved the stand-in discovery loop on `test_ledger_refresh_composite_arm.sql` (rank 5, 15 sections, `grep -a -c cron` → 0), the largest idiom file the lane can baseline without withholding arms. Both gates were then run against the recorded apply list and both outcomes recorded — the substituted one GREEN, the planned one RED at `:471` on the `pg_cron` arm itself.
- **Files modified:** `164.4-00-FIXTURE-STRATEGY.md`, `TODOS.md`
- **Verification:** `bash scripts/pg-lane/run.sh --workdir ${TMPDIR}/164-4-00-lane --apply $APPLY --gate supabase/tests/test_ledger_refresh_composite_arm.sql` → exit 0, read back from the record's own fenced block.
- **Committed in:** `e4e41b2c`

**3. [Rule 2 - Missing Critical] Booked three findings rather than working around any of them**
- **Found during:** Tasks 1-3
- **Issue:** `[REDUNDER-PGCRON]` (lane substrate), `[REDUNDER-SAVEPOINT]` (a production migration that cannot apply to vanilla PG16), `[WINDOWS-LEDGER-DRIFT]` (the broken-windows ledger refuses every append).
- **Fix:** all three written to `TODOS.md` with their measurements. Explicitly NOT done: installing `pg_cron` (a package on two hosts plus `shared_preload_libraries` on the lane's `pg_ctl -o` line — Rule 4 territory and the plan books zero installs), editing any `supabase/migrations/` file, or faking a `pg_extension` row (which would make arm `1/JOB-04` unfalsifiable while reporting it covered — threat T-164.4-01).
- **Verification:** `git diff --name-only d70a5f74..HEAD -- supabase/` → 0 files.
- **Committed in:** `b0df78f0` and this commit

---

**Total deviations:** 3 auto-fixed (1 blocking, 2 missing-critical)
**Impact on plan:** No scope creep and no substrate changed. One `must_haves.truth` is UNMET and amended by measurement — see below; that is the honest outcome of a spike, not a silent pass.

## Issues Encountered

- **`must_haves.truths[2]` is NOT met:** *"The largest idiom file has an apply list that goes GREEN on a vanilla-PostgreSQL lane, so Plan 03 starts on a proven baseline instead of an assumed one."* It is not reachable on today's lane. Plan 03 must be re-pointed at `test_ledger_refresh_composite_arm.sql`, and the three `pg_cron` files must not be batched until `[REDUNDER-PGCRON]` is resolved.
- **`.planning/WINDOWS.md` is write-blocked.** `gsd-tools windows append` refuses with `frontmatter open/waived/fixed/total=26/0/2/28 but entries yield 29/0/2/31`. Measured cause: the file carries a markdown table (ids 1-28) AND a JSON block with 3 further entries the frontmatter never counted; drift predates this plan (`last_updated: 2026-08-29`). This plan's two ledger entries went to `TODOS.md` instead and the drift itself is booked as `[WINDOWS-LEDGER-DRIFT]`. Not repaired here: which representation is canonical is a decision, and editing the counts blind would make the ledger agree with itself while still being wrong.
- **`requirements.mark-complete` was correctly withheld for `SC-2`** — `requirements.ready-ids` returned `0/1`, because a sibling plan in this phase also declares SC-2 and has no SUMMARY yet.
- **zsh does not word-split an unquoted `$APPLY`.** The plan's own Task 2 verify one-liner fails with `apply file not found: <the whole list>` under this session's default shell. Every verify here was run under `bash`; the record carries the warning so no batch plan loses an hour to it.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

**Ready:**
- Plans 01/02 (runner + CI, and the reference file's 15 un-twinned sections) are unaffected — the reference file's apply list already exists and does not touch `cron`.
- Plan 03 has a real, proven, timed apply list and two committed additive fixtures to copy, plus the measured discovery cost (8 iterations for one 651-line, 5-stamp file).

**Blockers before batching:**
1. `[REDUNDER-PGCRON]` must be resolved before any batch containing `test_reconcile_dropped_enqueue_sweep.sql` (39 sections), `test_retention_orphaned_running.sql` (25) or `test_derive_allocator_keys_fanout.sql` (7). `test_strategy_analytics_stuck_computing_reaper.sql` (29) baselines green but withholds arms behind `SKIP Part 2/3` — those arms will report non-biting.
2. Plan 03's target file must be re-pointed away from the rank-1 file.
3. The phase's coverage denominator and `FILES_FLOOR`/`ARMS_FLOOR` projections assumed all 44 idiom files are reachable. 71 of 355 sections are not, today.

---
*Phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota*
*Completed: 2026-09-02*

## Self-Check: PASSED

- `164.4-00-FIXTURE-STRATEGY.md`, `03-fixture-compute-jobs.sql`, `04-fixture-compute-jobs-targets.sql` — all FOUND on disk.
- Commits `82907683`, `e4e41b2c`, `b0df78f0` — all FOUND in `git log --oneline --all`.
- Task verifications re-run at close: Task 1 (probe-row grep + `run.sh --self-test` 5/5) exit 0; Task 2 (apply list read back from the record → substituted gate) exit 0; Task 3 (`DECISION:` + F1 residual + `--parse-only` + clean tree) exit 0.
- `git diff --name-only d70a5f74..HEAD -- supabase/` → 0 files: nothing under `supabase/` changed.
