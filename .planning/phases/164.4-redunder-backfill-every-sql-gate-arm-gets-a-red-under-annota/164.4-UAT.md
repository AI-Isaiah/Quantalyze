---
status: complete
phase: 164.4-redunder-backfill
source: 164.4-VERIFICATION.md (single human_verification item)
started: 2026-09-05T19:30:00Z
updated: 2026-09-05T19:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. SHA-bound ubuntu `sql-mutation` green at the 39/262 end state
expected: "`sql-mutation` succeeds on ubuntu-latest with `coverage: files 39/71`,
  `arms: 262/262/0`, `biting: 262`, `lane-invocations: 262`, tallies agree,
  `lane-probe: pg_cron absent`, `pending: 1`."
result: pass
resolved_by: superseding-measurement
reported: "Close on superseding evidence — founder decision 2026-09-05."

⛔ **The named state is unmeasurable and always will be.** Phase 164.4.1 deliberately
put pg_cron ON the pg-lane. A run printing `lane-probe: pg_cron absent` at
`arms: 262/262/0` cannot be produced without reverting that phase.

⚠️ **The check was never attempted as written.** `gh run list --branch
phase-164.4-redunder-backfill` returns no runs at all.

**Accepted in its place** — read directly off the ubuntu job log (job `101294336893`),
not off any prose restatement of it:

| Field | Named check | Measured |
|---|---|---|
| run / sha | any, SHA-bound | `33961609382` @ `1aa8bb7088e978320041b6a97d187b8247b8fe3d` |
| job verdict | exit 0 | `sql-mutation` **success** |
| coverage | `files 39/71` | `files 44/71` |
| arms | `262/262/0` | `363/363/0` |
| biting | `262` | `363` |
| lane-invocations | `262` | `363` (tallies agree) |
| lane | `pg_cron absent` | `pg_cron AVAILABLE`, `lane-blocked: 0` |
| defects | none | `✅ No defects` |
| per-arm | — | `mean 1.1s` |

⚠️ The overall RUN concluded `failure`; the `sql-mutation` JOB concluded `success`.
The job is what carries this evidence.

**Why this is stronger, not weaker.** Every one of the 39 files and 262 arms in
164.4's end state sits inside the 44/363 corpus, measured on the same runner and the
same lane. The risk the check existed to retire is named in its own `why_human`:
"scaling 30 → ~260 arms on an unobserved host is how a floor rots". Observing a
LARGER corpus green on that host retires that risk more completely than the 262-arm
reading would have.

⛔ **What this does NOT claim:** that the 262-arm, pg_cron-ABSENT configuration was
ever observed on ubuntu. It was not, and it now cannot be.

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

None from UAT.

⚠️ **Not gaps, but still open** — warnings WR-V1, WR-V2 and WR-V3 in
`164.4-VERIFICATION.md` are untouched by this resolution. All three are stale-integer
and missing-reason defects in `STATE.md`, `scripts/mutation-runner/run.mjs` and
`.github/workflows/ci.yml`. They are gate hygiene and are owned by **Phase 164.6
GATE-HYGIENE**, not by this phase.
