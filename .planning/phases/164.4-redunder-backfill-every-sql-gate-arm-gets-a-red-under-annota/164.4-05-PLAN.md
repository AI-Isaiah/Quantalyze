---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 05
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet]
wave: 6
depends_on: ["164.4-04"]
files_modified:
  - supabase/tests/test_wizard_composite_members.sql
  - supabase/tests/test_capital_ownership_allocation_guard.sql
  - supabase/tests/test_create_wizard_strategy_for_key.sql
  - supabase/tests/test_scenario_shares_rls.sql
  - supabase/tests/test_strategy_keys_rls.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 2: five files (11 + 10 + 9 + 9 + 9 = 48 sections) fully annotated and proven"
  - "FILES_FLOOR 4 → 9, ARMS_FLOOR → measured (expected 134)"
affects: [164.4-06]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the five files carries one RED-UNDER-SETUP proven GREEN on the lane and every section has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok) and all five files restoring GREEN; per-file judged == annotated == biting (SC-2)"
    - "Floors equal the run's printed numbers with dated blocks and both separation directions; the scanCorpus pin lists nine files (SC-3)"
    - "A waiver candidate is a STOP checkpoint to the founder (WAIVED_CEILING stays 0 unless accepted on the record); dead arms are TODOS findings (SC-4)"
  artifacts:
    - path: "supabase/tests/test_wizard_composite_members.sql"
      provides: "11 sections twinned"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_capital_ownership_allocation_guard.sql"
      provides: "10 sections twinned (21 identities; 0 stamps named — migrations found by object name)"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_strategy_keys_rls.sql"
      provides: "9 sections twinned (13 identities)"
      contains: "RED-UNDER-SETUP"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 9"
      pattern: "scanCorpus"
---

<objective>
Batch 2, largest-first among the 39 annotatable new files (the four lane-blocked files are deferred
and printed by the runner since Plan 03): `test_wizard_composite_members.sql` (409 lines, 11 ids →
**11 sections**, 1 stamp), `test_capital_ownership_allocation_guard.sql` (899 lines, 21 ids →
**10 sections**, **0 stamps named** — its migrations must be found by object name),
`test_create_wizard_strategy_for_key.sql` (231 lines, **9 sections**, 3 stamps; a sentinel file),
`test_scenario_shares_rls.sql` (483 lines, **9 sections**, 1 stamp) and
`test_strategy_keys_rls.sql` (295 lines, 13 ids → **9 sections**, 1 stamp). Section counts
re-confirmed at HEAD 007708dd by `gateSectionCount`. The two `*_rls` files are grant/policy gates:
expect `sql` steps (GRANT/REVOKE, policy drop) for drift a migration pins exactly, never an edit of
the pinned grant. Re-derive sections with the one-liner.

Purpose: 48 sections; the run grows to ~152 lanes.
Output: 48 twins, five setup lines, FILES_FLOOR 9, ARMS_FLOOR → measured (expected 134 = 86 + 48).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Per-task reading lists are in each task's `<read_first>`.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: wizard_composite_members — setup proven GREEN + ONE section end-to-end</name>
  <files>supabase/tests/test_wizard_composite_members.sql</files>
  <precondition>164.4-04-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`).</precondition>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (the DECISION on stand-ins, the three authoring rules, the discovery loop), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-04-SUMMARY.md (the ledger apply lists to start from for compute-jobs-adjacent objects; the file-move choreography as executed), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1 shapes, § P1.1 placement, § P2.1 setup line placement), scripts/mutation-runner/GRAMMAR.md (rule 4: no fixture targets), supabase/tests/test_strategy_shares_rls.sql:40-61 (the reference header shape)</read_first>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-05-PLAN.md` and halt on non-zero.

    Discover the file's apply list with the iterative lane loop (stand-ins per Plan 00's DECISION
    first, then the real migrations the file names — `grep -a -o -E '20[0-9]{12}' <file> | sort -u`
    as the seed — with the LAST-defining migration of every object under test; the first ERROR of
    each red run names the next missing object; `--workdir` OUTSIDE the repo; under bash). If a
    relation is missing that no migration in the list creates, extend an existing additive fixture
    or add `NN-fixture-<area>.sql` in the `02-*` shape (IF NOT EXISTS only; record it in the
    SUMMARY). Write the `RED-UNDER-SETUP` line at column 0 in the header with its 3-line prose
    paragraph; re-prove exit 0 on the lane and record the iteration count and per-lane wall clock.
    Author ONE section (the file's first identity) exactly per the P1.1 shape: real identity as
    `arm`, mutation of the code under test in the last-defining migration, measured `occurrences`,
    `neuter` only for an idiom arm firing first, no fixture targets. `--parse-only` then the
    diagnostic lane; read the verdict line.

    ⚠️ From this point until Task 2 completes, the parser test's section-inclusion pin (armed in
    Plan 02: twin sections ⊇ identity sections for every annotated file) is EXPECTED RED for this
    file — one twin over 11 sections is exactly the state the pin refuses. Do NOT touch the pin,
    do NOT loosen it, and do not run it as a gate before Task 2; it goes green when the last
    section is twinned and is asserted in Task 3's lockstep vitest.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_wizard_composite_members.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>The first file has a proven setup line and one twin whose lane reads RED (identity ok).</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all five files</name>
  <files>supabase/tests/test_wizard_composite_members.sql, supabase/tests/test_capital_ownership_allocation_guard.sql, supabase/tests/test_create_wizard_strategy_for_key.sql, supabase/tests/test_scenario_shares_rls.sql, supabase/tests/test_strategy_keys_rls.sql</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (discovery loop, additive-fixture rule), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1, § P2.1-P2.3), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md (§ Waivers & un-falsifiable arms), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-RESEARCH.md (§ Common Pitfalls 3), scripts/mutation-runner/GRAMMAR.md (the sql step for grant/policy drift; Shape 1b/3 layered apply; Shape 4 — what NOT to author), supabase/tests/test_strategy_shares_rls.sql (the reference's own sql-step twins on its RLS sections — the shape the two *_rls files here follow)</read_first>
  <action>
    Discover and prove a setup line for each of the other four files (object-name lookup across
    `supabase/migrations/` for `test_capital_ownership_allocation_guard.sql`, which names no stamp).
    Run the section-coverage one-liner over all five, paste it into the SUMMARY, and twin every
    untwinned section: real identity as `arm`; mutate the code under test in the last-defining
    listed migration; `occurrences` measured with `grep -a -o -F`; `sql` step for exactly-pinned
    grant/policy drift; LAYERED `apply` for self-verifying migrations; `neuter` only for idiom arms
    that fire first; no fixture targets. Waivers: `WAIVED_CEILING` stays 0 — an arm that bites but
    cannot be mutated mechanically is a STOP checkpoint to the founder with the arm and the
    mutations tried, not an executor-authored Shape 4. A dead arm is a TODOS finding, no twin, said
    loudly. Never edit an assertion, message or identity; the diff on every gate file is added
    comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_wizard_composite_members.sql supabase/tests/test_capital_ownership_allocation_guard.sql supabase/tests/test_create_wizard_strategy_for_key.sql supabase/tests/test_scenario_shares_rls.sql supabase/tests/test_strategy_keys_rls.sql</automated>
    Expected 11 / 10 / 9 / 9 / 9 sections, 0 untwinned each (a booked dead-arm finding is the only permitted exception and is named in the SUMMARY).
  </verify>
  <done>All 48 sections twinned (or booked); five setup lines proven; parse-only green.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 4 → 9, ARMS_FLOOR → measured, pins and prose in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, CLAUDE.md</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-04-SUMMARY.md (the pin choreography as executed; the ubuntu per-lane mean and job wall clock), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P3, § P4 — every item), scripts/mutation-runner/run.mjs (FILES_FLOOR, ARMS_FLOOR, WAIVED_CEILING and their dated blocks), src/__tests__/gate-family-meta.test.ts (KNOWN_THRESHOLD_SITES), src/__tests__/mutation-runner-floors.test.ts (GREEN_LOG, totalAnchored), src/__tests__/mutation-annotation-parser.test.ts (armsSeen, stepsSeen, scanCorpus exact pin, section-inclusion pin), CLAUDE.md (§ SQL gate integrity jobs)</read_first>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 9/71`, `biting: 134`). In ONE commit with the annotations: Group A `run.mjs`
    `FILES_FLOOR` → 9 and `ARMS_FLOOR` → measured biting, each with a compact dated block
    (RE-DERIVED date, plan 164.4-05, HEAD sha, the printed lines, SAMPLE SIZE, COVERAGE naming the
    nine files, SEPARATION both directions, RECORD); currency lines edited, lineage untouched;
    `WAIVED_CEILING` unedited. Group B `KNOWN_THRESHOLD_SITES` strings and dates. Group C
    `totalAnchored` → measured; `GREEN_LOG` coverage 9/71 / arms / biting / lane-invocations / mean /
    `  pending: 31 …` / nine per-file lines summing to the aggregate / spawned string;
    `armsSeen`/`stepsSeen` → measured; scanCorpus pin "9 of 71" + nine sorted names. Group D
    `CLAUDE.md` currency sentence. `timeout-minutes`: unchanged unless the projection is ≥ 10 min.
    Project = ~152 lanes × the ubuntu per-lane figure recorded in Plan 04's SUMMARY (the ubuntu
    log's `per-arm lane time:` mean; RESEARCH's 1.03 s/lane if that field is missing) + Plan 04's
    measured non-lane job overhead (its job wall clock minus lanes × per-lane) — ≈ 2.6 min of
    lanes at 1.03 s/lane. Never scale a LOCAL wall clock by a constant to get an ubuntu figure:
    ubuntu is measured slightly SLOWER per lane than local (1.03 vs ~1.0 s), not faster, and an
    under-projection here is a job timeout that reads as a wedged cluster. Record the ubuntu wall
    clock and per-lane mean in the SUMMARY. Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + wall clock in the SUMMARY. Plan 06's precondition reads it.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 9/71`, `biting:` == ARMS_FLOOR; pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run and its wall clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |
| stand-in fixture → gate | provides only what the gate names; never a twin target |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep | medium | mitigate | no executor-authored waivers; STOP checkpoint to the founder; WAIVED_CEILING stays 0 |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline counted as coverage | medium | mitigate | baseline proven before authoring; per-file `judged` read off the run |
| T-164.4-08 | Denial of Service | lane time growth (five new apply lists) | low | mitigate | per-lane time recorded per file; timeout raised only with a measurement |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 11 / 10 / 9 / 9 / 9 sections, 0 untwinned; parse-only exit 0.
- Full run: `coverage: files 9/71`, `biting:` == ARMS_FLOOR, tallies agree, exit 0; lockstep vitest green; tree clean; `--pending` anchors green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- Five more files fully proven; floors moved by the measured delta in the same commit; the first grant/policy `sql`-step twins are on the record for the RLS gates that follow.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-05-SUMMARY.md` when done. Record: one-liner output, the five apply lists (iteration counts, per-lane times), run log lines, floors old → new, waiver candidates / findings, PR number, SHA-bound run id and wall clock.
</output>
