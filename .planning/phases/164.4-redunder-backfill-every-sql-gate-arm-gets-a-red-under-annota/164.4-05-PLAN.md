---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 05
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet, ledger-refresh]
wave: 6
depends_on: ["164.4-04"]
files_modified:
  - supabase/tests/test_ledger_refresh_composite_arm.sql
  - supabase/tests/test_ledger_refresh_fanout.sql
  - supabase/tests/test_ledger_refresh_staleness.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 3: the three ledger_refresh gates (15 + 15 + 11 sections) fully annotated and proven on one shared stand-in"
  - "FILES_FLOOR 4 → 7, ARMS_FLOOR → measured (expected 179)"
affects: [164.4-06]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the three files carries one RED-UNDER-SETUP proven GREEN and every section has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok) and all three files restoring GREEN (SC-2)"
    - "Floors equal the run's printed numbers with dated blocks and both separation directions; the scanCorpus pin lists seven files (SC-3)"
    - "Any non-idiom raise that fires first is handled by a mutation it does not observe, or by a waiver naming its line — never by editing it (SC-4)"
  artifacts:
    - path: "supabase/tests/test_ledger_refresh_composite_arm.sql"
      provides: "15 sections twinned"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_ledger_refresh_fanout.sql"
      provides: "15 sections twinned"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_ledger_refresh_staleness.sql"
      provides: "11 sections twinned"
      contains: "RED-UNDER-SETUP"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 7"
      pattern: "scanCorpus"
---

<objective>
Batch 3: the ledger-refresh family — `test_ledger_refresh_composite_arm.sql` (planner-measured 651
lines, 15 identities → **15 sections**, 4 stamps, **2 non-idiom raises**, 20 banners that DO
delimit arms here), `test_ledger_refresh_fanout.sql` (598 lines, **15 sections**, 2 stamps, **2
non-idiom raises**) and `test_ledger_refresh_staleness.sql` (417 lines, **11 sections**, 1 stamp).
They test the same subsystem, so discover ONE apply list for the first and expect the other two to
reuse it with at most a migration or two added; each file still declares its own setup line.
⚠️ The four non-idiom raises are invisible to attribution and un-neuterable (RESEARCH Pitfall 3):
if one fires first under a mutation the verdict is `NO-IDENTITY` — pick a mutation it does not
observe; if none exists, a waiver whose reason names the shadowing raise's line. Three of these
files declare sentinel NOTICEs counted by the ci.yml sentinel gate — comment-only annotations
cannot move that count. Re-derive sections with the one-liner.

Purpose: 41 sections; run ~193 lanes.
Output: 41 twins, three setup lines, FILES_FLOOR 7, ARMS_FLOOR → measured (expected 179).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Read before implementing: Plan 00's record, Plan 04's SUMMARY, PATTERNS § P1, § P2, § P3, § P4;
RESEARCH § Common Pitfalls 3 and 6.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs WAIVED_CEILING -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: composite_arm — setup proven GREEN + ONE section end-to-end</name>
  <files>supabase/tests/test_ledger_refresh_composite_arm.sql</files>
  <precondition>164.4-04-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`).</precondition>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-05-PLAN.md` and halt on non-zero.
    Discover the apply list with the lane loop (stand-ins per Plan 00's DECISION, then the real
    last-defining migrations; `--workdir` outside the repo; additive `NN-fixture-<area>.sql` only for
    relations no listed migration creates — record any new fixture in the SUMMARY). Write the
    setup line at column 0 in the header with its 3-line paragraph; re-prove exit 0. Author ONE
    section (the file's first identity) in the P1.1 shape with a measured `occurrences`; run
    `--parse-only` and the diagnostic lane; read the verdict line. Before choosing the mutation,
    locate the file's two non-idiom raises and confirm the mutation cannot trip them first.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_ledger_refresh_composite_arm.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>composite_arm has a proven setup line and one twin whose lane reads RED (identity ok).</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all three files</name>
  <files>supabase/tests/test_ledger_refresh_composite_arm.sql, supabase/tests/test_ledger_refresh_fanout.sql, supabase/tests/test_ledger_refresh_staleness.sql</files>
  <action>
    Discover and prove setup lines for fanout and staleness (start from composite_arm's list).
    Run the section-coverage one-liner over the three files, paste it into the SUMMARY, and twin
    every untwinned section: real identity as `arm`; mutate the code under test in the
    last-defining migration in the setup list; `occurrences` measured; `sql` step for exactly-pinned
    grants; LAYERED `apply` for self-verifying migrations; `neuter` only for idiom arms; no fixture
    targets. For a section shadowed by a non-idiom raise, choose a mutation that raise does not
    observe; only if none exists, a waiver naming that raise's line (+1 WAIVED_CEILING in Task 3;
    >5 cumulative → STOP and escalate). Dead arm = TODOS finding, no twin, said loudly. Never edit
    an assertion, message or identity; diffs are added comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_ledger_refresh_composite_arm.sql supabase/tests/test_ledger_refresh_fanout.sql supabase/tests/test_ledger_refresh_staleness.sql</automated>
    Expected 15 / 15 / 11 sections, 0 untwinned each (a booked dead-arm finding is the only permitted exception).
  </verify>
  <done>All 41 sections twinned (or booked); three setup lines proven; parse-only green.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 4 → 7, ARMS_FLOOR → measured, pins and prose in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, CLAUDE.md</files>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 7/71`, `biting: 179`). In ONE commit with the annotations: Group A `run.mjs`
    floors with compact dated blocks (both separation directions, COVERAGE naming the seven files,
    RECORD), currency lines, `WAIVED_CEILING` +n with reasons only for accepted waivers; Group B
    registry strings and dates; Group C `totalAnchored`, `GREEN_LOG` (coverage 7/71, arms, biting,
    lane-invocations, mean, seven per-file lines summing to the aggregate, spawned string),
    `armsSeen`/`stepsSeen`, scanCorpus pin "7 of 71" + seven names; Group D `CLAUDE.md` currency
    sentence. `timeout-minutes` raised to 30 with the measurement only if the run is ≥ 10 min
    (~193 lanes). Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + wall clock in the SUMMARY.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 7/71`, `biting:` == ARMS_FLOOR; pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep (first likely waivers: non-idiom shadowing) | medium | mitigate | reason names the shadowing line; ceiling +1 per reviewed waiver; >5 → STOP |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline counted as coverage | medium | mitigate | baseline proven first; per-file `judged` read off the run |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 15 / 15 / 11 sections, 0 untwinned; parse-only exit 0.
- Full run: `coverage: files 7/71`, `biting:` == ARMS_FLOOR, tallies agree, exit 0; lockstep vitest green; tree clean; `--pending` anchors green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- The ledger-refresh family is fully proven; the first non-idiom-shadowing cases are resolved on the record; floors moved by the measured delta.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-05-SUMMARY.md` when done. Record: one-liner output, the three apply lists, run log lines, floors old → new, waivers (with the shadowing lines) / findings, PR number, SHA-bound run id and wall clock.
</output>
