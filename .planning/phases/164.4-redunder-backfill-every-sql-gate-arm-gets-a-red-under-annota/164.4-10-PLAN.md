---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 10
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet]
wave: 11
depends_on: ["164.4-09"]
files_modified:
  - supabase/tests/test_allocator_equity_pre_terminus_flag.sql
  - supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql
  - supabase/tests/test_metrics_by_basis_write.sql
  - supabase/tests/test_set_compute_job_progress.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 7: the four remaining non-mixed files (2 sections each = 8 sections) fully annotated and proven"
  - "FILES_FLOOR 29 → 33, ARMS_FLOOR → measured (expected 250); every non-mixed, non-lane-blocked idiom file is now covered and the runner's `pending:` line names exactly the seven mixed files"
affects: [164.4-11]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the four files carries one RED-UNDER-SETUP proven GREEN and every section has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok) and all four files restoring GREEN (SC-2)"
    - "Floors equal the run's printed numbers with dated blocks and both separation directions; the scanCorpus pin lists thirty-three files; the `  pending:` line names exactly the seven mixed files and the `lane-blocked:` line exactly the four deferred files (SC-3)"
    - "A waiver candidate is a STOP checkpoint to the founder; the cumulative waiver count (expected 0) is stated for Plan 11's gate; dead arms are TODOS findings (SC-4)"
  artifacts:
    - path: "supabase/tests/test_set_compute_job_progress.sql"
      provides: "2 sections twinned (5 identities)"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_metrics_by_basis_write.sql"
      provides: "2 sections twinned (2 stamps)"
      contains: "RED-UNDER-SETUP"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 33"
      pattern: "scanCorpus"
---

<objective>
Batch 7, the last non-mixed files: `test_allocator_equity_pre_terminus_flag.sql` (106 lines, **2
sections**, 1 stamp; its `:49` SKIP fires only when migration `20260529160000` is absent — list
it), `test_enqueue_compute_job_dedupe_non_terminal.sql` (207 lines, **2**, 0 stamps),
`test_metrics_by_basis_write.sql` (123 lines, **2**, 2 stamps) and
`test_set_compute_job_progress.sql` (187 lines, 5 ids → **2 sections**, 1 stamp). Small files,
four apply lists (two are compute-jobs gates — start from Plan 04's list). After this batch the
runner's `  pending:` line must name exactly the seven ⚠️ mixed files Plan 11 takes, and the
`lane-blocked:` line exactly the four deferred files — read both off the run and quote them in the
SUMMARY; together they are the phase's own statement of what is left. Re-derive with the one-liner.

Purpose: 8 sections; run ~316 lanes.
Output: 8 twins, four setup lines, FILES_FLOOR 33, ARMS_FLOOR → measured (expected 250 = 242 + 8).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Per-task reading lists are in each task's `<read_first>`.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: allocator_equity_pre_terminus_flag — setup proven GREEN + ONE section end-to-end</name>
  <files>supabase/tests/test_allocator_equity_pre_terminus_flag.sql</files>
  <precondition>164.4-09-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`).</precondition>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (the DECISION on stand-ins, the three authoring rules, the discovery loop), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-09-SUMMARY.md (apply lists, ubuntu wall clock and per-lane mean), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-04-SUMMARY.md (the compute-jobs apply list to start from), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1 shapes, § P1.1 placement, § P2.1 setup line placement), scripts/mutation-runner/GRAMMAR.md (rule 4: no fixture targets), supabase/tests/test_allocator_equity_pre_terminus_flag.sql:49 (the SKIP keyed on migration 20260529160000), supabase/tests/test_strategy_shares_rls.sql:40-61 (the reference header shape)</read_first>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-10-PLAN.md` and halt on non-zero.
    Discover the apply list with the lane loop (stand-ins per Plan 00's DECISION, then the real
    last-defining migrations including `20260529160000`; `--workdir` outside the repo; under bash;
    additive fixture only for relations no listed migration creates, recorded in the SUMMARY).
    Confirm the baseline prints no `SKIP`. Write the setup line at column 0 in the header with its
    3-line paragraph; re-prove exit 0. Author ONE section (the first identity) in the P1.1 shape
    with a measured `occurrences`; `--parse-only`; diagnostic lane; read the verdict.

    ⚠️ From this point until Task 2 completes, the parser test's section-inclusion pin (armed in
    Plan 02: twin sections ⊇ identity sections for every annotated file) is EXPECTED RED for this
    file — one twin over 2 sections is exactly the state the pin refuses. Do NOT touch the pin,
    do NOT loosen it, and do not run it as a gate before Task 2; it goes green when the last
    section is twinned and is asserted in Task 3's lockstep vitest.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_allocator_equity_pre_terminus_flag.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>The first file has a proven setup line and one twin whose lane reads RED (identity ok).</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all four files</name>
  <files>supabase/tests/test_allocator_equity_pre_terminus_flag.sql, supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql, supabase/tests/test_metrics_by_basis_write.sql, supabase/tests/test_set_compute_job_progress.sql</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (discovery loop, additive-fixture rule), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-04-SUMMARY.md (the compute-jobs apply list — two of these files are compute-jobs gates), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1, § P2.1-P2.3), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md (§ Waivers & un-falsifiable arms), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-RESEARCH.md (§ Common Pitfalls 3), scripts/mutation-runner/GRAMMAR.md (Shape 1b/3 layered apply; Shape 4 — what NOT to author)</read_first>
  <action>
    Discover and prove a setup line for each of the other three files (object-name lookup for
    `test_enqueue_compute_job_dedupe_non_terminal.sql`, which names no stamp). Run the
    section-coverage one-liner over all four, paste it into the SUMMARY, and twin every untwinned
    section: real identity as `arm`; mutate the code under test in the last-defining listed
    migration; `occurrences` measured; `sql` step for exactly-pinned grant/policy drift; LAYERED
    `apply` for self-verifying migrations; `neuter` only for idiom arms; no fixture targets.
    Waivers: `WAIVED_CEILING` stays 0 — a biting-but-unmutatable arm is a STOP checkpoint to the
    founder. Dead arm = TODOS finding. Never edit an assertion, message or identity; diffs are
    added comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_allocator_equity_pre_terminus_flag.sql supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql supabase/tests/test_metrics_by_basis_write.sql supabase/tests/test_set_compute_job_progress.sql</automated>
    Expected 2 sections and 0 untwinned in each of the four (a booked dead-arm finding is the only permitted exception).
  </verify>
  <done>All 8 sections twinned (or booked); four setup lines proven; parse-only green.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 29 → 33, ARMS_FLOOR → measured, pins and prose in the same commit, remaining-work lines quoted</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, CLAUDE.md</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-09-SUMMARY.md (the pin choreography as executed; the ubuntu per-lane mean and job wall clock; the waiver tally so far), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P3, § P4 — every item), scripts/mutation-runner/run.mjs (FILES_FLOOR, ARMS_FLOOR, WAIVED_CEILING and their dated blocks), src/__tests__/gate-family-meta.test.ts (KNOWN_THRESHOLD_SITES), src/__tests__/mutation-runner-floors.test.ts (GREEN_LOG, totalAnchored), src/__tests__/mutation-annotation-parser.test.ts (armsSeen, stepsSeen, scanCorpus exact pin, pendingFiles, section-inclusion pin), CLAUDE.md (§ SQL gate integrity jobs)</read_first>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 33/71`, `biting: 250`; the `  pending:` line must name exactly the seven mixed
    files `test_api_keys_exchange_not_user_writable.sql test_api_keys_insert_not_client_writable.sql
    test_guard_wizard_draft_updates_auth_uid.sql test_profiles_privileged_columns_locked.sql
    test_strategy_keys_publish_integrity.sql test_sync_status_marked_refresh_protected.sql
    test_wizard_session_idempotency.sql` — if it names anything else, a non-mixed file was missed
    and this batch is not done; the `lane-blocked:` line must still name exactly the four deferred
    files). In ONE commit with the annotations: Group A `run.mjs` floors with compact dated blocks
    (both separation directions, COVERAGE, RECORD), currency lines, `WAIVED_CEILING` unedited;
    Group B registry strings and dates; Group C `totalAnchored`, `GREEN_LOG` (coverage 33/71, arms,
    biting, lane-invocations, mean, `  pending: 7 …` with the seven names, thirty-three per-file
    lines summing to the aggregate, spawned string), `armsSeen`/`stepsSeen`, scanCorpus pin "33 of
    71" + names, and a pin that `pendingFiles` equals exactly the seven mixed basenames; Group D
    `CLAUDE.md` currency sentence. State in the SUMMARY the cumulative waiver count (expected 0)
    and `WAIVED_CEILING` value. `timeout-minutes` per the standing rule (~316 lanes × the ubuntu
    per-lane mean from Plan 09's SUMMARY + its measured non-lane overhead — never a local wall clock
    scaled by a constant; raise only with a measurement; ≥ 20 min is a STOP). Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && node scripts/mutation-runner/run.mjs --parse-only | grep -a -q -E '^  pending: 7 idiom file\(s\) ' && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + wall clock + the cumulative waiver count in the SUMMARY. Plan 11's precondition reads it.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 33/71`, `biting:` == ARMS_FLOOR, `  pending:` naming exactly the seven mixed files, `lane-blocked:` exactly the four deferred; pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run and the waiver tally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep | medium | mitigate | no executor-authored waivers; cumulative tally stated for Plan 11's gate |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline or green-SKIP counted as coverage | medium | mitigate | baseline proven and NOTICE output read first; per-file `judged` read off the run |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 2 sections, 0 untwinned, four times; parse-only exit 0.
- Full run: `coverage: files 33/71`, `  pending:` == the seven mixed files, `lane-blocked:` == the four deferred, `biting:` == ARMS_FLOOR, tallies agree, exit 0; lockstep vitest green; tree clean; `--pending` anchors green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- Every non-mixed, non-lane-blocked idiom file is proven; the runner's own output says only the seven mixed files remain and names the four deferred ones with their reason; the waiver tally is on the record for Plan 11's gate.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-10-SUMMARY.md` when done. Record: one-liner output, the four apply lists, run log lines including the `  pending:` and `lane-blocked:` lines verbatim, floors old → new, cumulative waivers and WAIVED_CEILING, findings, wall clock, PR number, SHA-bound run id.
</output>
