---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 09
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, red-under-setup, lockstep-pins, floor-ratchet]
wave: 10
depends_on: ["164.4-08"]
files_modified:
  - supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql
  - supabase/tests/test_wizard_composite_fence.sql
  - supabase/tests/test_weight_snapshot_seed_secdef.sql
  - supabase/tests/test_compute_jobs_error_kind_copy_parity.sql
  - supabase/tests/test_csv_finalize_auth_guard.sql
  - supabase/tests/test_resync_retry_single_job.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - CLAUDE.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 6: six files (5 + 5 + 4 + 3 + 3 + 3 = 23 sections) fully annotated and proven"
  - "FILES_FLOOR 23 → 29, ARMS_FLOOR → measured (expected 242)"
affects: [164.4-10]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the six files carries one RED-UNDER-SETUP proven GREEN and every section has a prose marker and a twin (SC-1)"
    - "The full-corpus run exits 0 with every new twin RED (identity ok) and all six files restoring GREEN (SC-2)"
    - "Floors equal the run's printed numbers with dated blocks and both separation directions; the scanCorpus pin lists twenty-nine files (SC-3)"
    - "A waiver candidate is a STOP checkpoint to the founder; dead arms are TODOS findings (SC-4)"
    - "test_wizard_composite_fence.sql's three `SKIP (Part 3a/3b-ii/3c-ii …)` notices (keyed on migration 20260814120000) are made inert by the apply list so the guarded assertions RUN; a section still behind a firing SKIP is a recorded finding, never a twin (SC-4)"
  artifacts:
    - path: "supabase/tests/test_wizard_composite_fence.sql"
      provides: "5 sections twinned (20 identities; 5 stamps; one non-idiom raise; its pg_cron mention at :698 is a comment — the file is NOT lane-blocked)"
      contains: "RED-UNDER-SETUP"
    - path: "supabase/tests/test_compute_jobs_error_kind_copy_parity.sql"
      provides: "3 sections twinned (4 stamps)"
      contains: "RED-UNDER-SETUP"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 29"
      pattern: "scanCorpus"
---

<objective>
Batch 6, largest-first: `test_strategy_verifications_wizard_session_tenant_scope.sql` (300
lines, **5 sections**, 2 stamps), `test_wizard_composite_fence.sql` (780 lines, 20 ids → **5
sections**, 5 stamps, **1 non-idiom raise**; `:222`, `:509`, `:600` SKIP when `20260814120000`
is absent — list it and confirm the baseline prints no SKIP), `test_weight_snapshot_seed_secdef.sql`
(221 lines, **4**, 2 stamps), `test_compute_jobs_error_kind_copy_parity.sql` (213 lines, 13 raises
→ **3 sections**, 4 stamps; compute-jobs — start from Plan 04's ledger list, which already
carries `03-fixture-compute-jobs.sql` and `20260411144407`), `test_csv_finalize_auth_guard.sql`
(207 lines, **3**, 1 stamp) and `test_resync_retry_single_job.sql` (220 lines, **3**, 0 stamps).
Section counts re-confirmed at HEAD 007708dd. Re-derive with the one-liner.

Purpose: 23 sections; run ~300 lanes.
Output: 23 twins, six setup lines, FILES_FLOOR 29, ARMS_FLOOR → measured (expected 242 = 219 + 23).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Per-task reading lists are in each task's `<read_first>`.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: strategy_verifications_wizard_session_tenant_scope — setup proven GREEN + ONE section end-to-end</name>
  <files>supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql</files>
  <precondition>164.4-08-SUMMARY.md exists and records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`).</precondition>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (the DECISION on stand-ins, the three authoring rules, the discovery loop), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-08-SUMMARY.md (timeout decision, ubuntu wall clock and per-lane mean, apply lists), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-04-SUMMARY.md (the compute-jobs apply list to start from), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1 shapes, § P1.1 placement, § P2.1 setup line placement), scripts/mutation-runner/GRAMMAR.md (rule 4: no fixture targets), supabase/tests/test_strategy_shares_rls.sql:40-61 (the reference header shape)</read_first>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-09-PLAN.md` and halt on non-zero.
    Discover the apply list with the lane loop (stand-ins per Plan 00's DECISION, then the real
    last-defining migrations; `--workdir` outside the repo; under bash; additive fixture only for
    relations no listed migration creates, recorded in the SUMMARY). Write the setup line at column
    0 in the header with its 3-line paragraph; re-prove exit 0. Author ONE section (the first
    identity) in the P1.1 shape with a measured `occurrences`; `--parse-only`; diagnostic lane;
    read the verdict.

    ⚠️ From this point until Task 2 completes, the parser test's section-inclusion pin (armed in
    Plan 02: twin sections ⊇ identity sections for every annotated file) is EXPECTED RED for this
    file — one twin over 5 sections is exactly the state the pin refuses. Do NOT touch the pin,
    do NOT loosen it, and do not run it as a gate before Task 2; it goes green when the last
    section is twinned and is asserted in Task 3's lockstep vitest.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>The first file has a proven setup line and one twin whose lane reads RED (identity ok).</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all six files</name>
  <files>supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql, supabase/tests/test_wizard_composite_fence.sql, supabase/tests/test_weight_snapshot_seed_secdef.sql, supabase/tests/test_compute_jobs_error_kind_copy_parity.sql, supabase/tests/test_csv_finalize_auth_guard.sql, supabase/tests/test_resync_retry_single_job.sql</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-00-FIXTURE-STRATEGY.md (discovery loop, additive-fixture rule), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P1, § P2.1-P2.3), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md (§ Waivers & un-falsifiable arms), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-RESEARCH.md (§ Common Pitfalls 3 — the fence file's non-idiom raise), scripts/mutation-runner/GRAMMAR.md (Shape 1b/3 layered apply; Shape 4 — what NOT to author), supabase/tests/test_wizard_composite_fence.sql:222 (SKIP Part 3a), supabase/tests/test_wizard_composite_fence.sql:509 (SKIP Part 3b-ii), supabase/tests/test_wizard_composite_fence.sql:600 (SKIP Part 3c-ii), supabase/tests/test_wizard_composite_fence.sql:698 (the comment-only pg_cron mention — this file is NOT lane-blocked)</read_first>
  <action>
    Discover and prove a setup line for each of the other five files (object-name lookup for
    `test_resync_retry_single_job.sql`; for the fence file confirm the baseline lane's NOTICE output
    carries no `SKIP` before twinning Parts 3a/3b/3c). Run the section-coverage one-liner over all
    six, paste it into the SUMMARY, and twin every untwinned section: real identity as `arm`;
    mutate the code under test in the last-defining listed migration; `occurrences` measured;
    `sql` step for exactly-pinned grant/policy drift (the SECDEF seed gate); LAYERED `apply` for
    self-verifying migrations; `neuter` only for idiom arms; for the fence file's non-idiom raise,
    a mutation it does not observe; no fixture targets. Waivers: `WAIVED_CEILING` stays 0 — a
    biting-but-unmutatable arm is a STOP checkpoint to the founder. Dead arm = TODOS finding.
    Never edit an assertion, message or identity; diffs are added comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql supabase/tests/test_wizard_composite_fence.sql supabase/tests/test_weight_snapshot_seed_secdef.sql supabase/tests/test_compute_jobs_error_kind_copy_parity.sql supabase/tests/test_csv_finalize_auth_guard.sql supabase/tests/test_resync_retry_single_job.sql</automated>
    Expected 5 / 5 / 4 / 3 / 3 / 3 sections, 0 untwinned each (a booked dead-arm finding is the only permitted exception).
  </verify>
  <done>All 23 sections twinned (or booked); six setup lines proven; parse-only green.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green, FILES_FLOOR 23 → 29, ARMS_FLOOR → measured, pins and prose in the same commit</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, CLAUDE.md</files>
  <read_first>.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-08-SUMMARY.md (the pin choreography as executed; the timeout decision; the ubuntu per-lane mean and job wall clock), .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md (§ P3, § P4 — every item), scripts/mutation-runner/run.mjs (FILES_FLOOR, ARMS_FLOOR, WAIVED_CEILING and their dated blocks), src/__tests__/gate-family-meta.test.ts (KNOWN_THRESHOLD_SITES), src/__tests__/mutation-runner-floors.test.ts (GREEN_LOG, totalAnchored), src/__tests__/mutation-annotation-parser.test.ts (armsSeen, stepsSeen, scanCorpus exact pin, section-inclusion pin), CLAUDE.md (§ SQL gate integrity jobs)</read_first>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 29/71`, `biting: 242`). In ONE commit with the annotations: Group A `run.mjs`
    floors with compact dated blocks (both separation directions, COVERAGE, RECORD), currency
    lines, `WAIVED_CEILING` unedited; Group B registry strings and dates; Group C `totalAnchored`,
    `GREEN_LOG` (coverage 29/71, arms, biting, lane-invocations, mean, `  pending: 11 …`,
    twenty-nine per-file lines summing to the aggregate, spawned string), `armsSeen`/`stepsSeen`,
    scanCorpus pin "29 of 71" + names; Group D `CLAUDE.md` currency sentence. `timeout-minutes`:
    re-check Plan 08's decision against this batch's projection (~300 lanes × the ubuntu per-lane
    mean from Plan 08's SUMMARY + its measured non-lane overhead — never a local wall clock scaled
    by a constant) and, once landed, its measured ubuntu wall clock; raise to 30 with the
    measurement only if ≥ 10 min; ≥ 20 min is a STOP per Plan 08's ceiling rule. Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + wall clock in the SUMMARY. Plan 10's precondition reads it.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 29/71`, `biting:` == ARMS_FLOOR; pins moved in the same commit; SUMMARY records the SHA-bound ubuntu-green run and its wall clock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep | medium | mitigate | no executor-authored waivers; STOP checkpoint; WAIVED_CEILING stays 0 |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline or green-SKIP counted as coverage | medium | mitigate | baseline proven and NOTICE output read before authoring; per-file `judged` read off the run |
| T-164.4-08 | Denial of Service | run approaching the job timeout | medium | mitigate | wall clock measured; Plan 08's timeout decision re-checked |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 5 / 5 / 4 / 3 / 3 / 3 sections, 0 untwinned; parse-only exit 0.
- Full run: `coverage: files 29/71`, `biting:` == ARMS_FLOOR, tallies agree, exit 0; lockstep vitest green; tree clean; `--pending` anchors green.
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- Six more files fully proven; floors moved by the measured delta in the same commit.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-09-SUMMARY.md` when done. Record: one-liner output, the six apply lists, run log lines, floors old → new, the fence file's baseline NOTICE check, waiver candidates / findings, wall clock, PR number, SHA-bound run id.
</output>
