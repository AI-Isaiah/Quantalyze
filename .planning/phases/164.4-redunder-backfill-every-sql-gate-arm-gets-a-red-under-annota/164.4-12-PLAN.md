---
phase: 164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota
plan: 12
type: execute
subsystem: mutation-runner
tags: [red-under-m, batch, mixed-files, waivers, lockstep-pins, floor-ratchet, phase-close]
wave: 13
depends_on: ["164.4-11"]
files_modified:
  - supabase/tests/test_api_keys_exchange_not_user_writable.sql
  - supabase/tests/test_strategy_keys_publish_integrity.sql
  - supabase/tests/test_api_keys_insert_not_client_writable.sql
  - supabase/tests/test_sync_status_marked_refresh_protected.sql
  - supabase/tests/test_guard_wizard_draft_updates_auth_uid.sql
  - supabase/tests/test_profiles_privileged_columns_locked.sql
  - supabase/tests/test_wizard_session_idempotency.sql
  - scripts/mutation-runner/run.mjs
  - scripts/mutation-runner/GRAMMAR.md
  - scripts/mutation-runner/VAC06-DEMOS.md
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - .github/workflows/ci.yml
  - CLAUDE.md
  - TODOS.md
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-4]
provides:
  - "Batch 10, the seven mixed files (15 sections, 128 non-idiom raises between them) fully annotated and proven, waivers recorded with reasons"
  - "End state: FILES_FLOOR 44, ARMS_FLOOR → measured (expected 365), WAIVED_CEILING at the measured count; coverage files 44/71 with the 27 excluded files printed and the pending line empty"
affects: [phase-close, REDUNDER-NONIDIOM]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the 44 idiom files carries a RED-UNDER-SETUP and every section a prose marker and a twin; the runner's `pending:` line is empty and its `unreachable:` line names the 27 non-idiom files (SC-1 as amended)"
    - "The full-corpus run exits 0 with every non-waived twin RED (identity ok) and every file restoring GREEN; biting == executed == lane-invocations (SC-2)"
    - "FILES_FLOOR = 44, ARMS_FLOOR = the run's biting, WAIVED_CEILING = the run's waived, each with a dated block and both separation directions; every prose site quoting the figures is current or explicitly lineage (SC-3)"
    - "Every waiver is a twin with its reason, printed every run, and the cumulative total is ≤ 5 — or the plan STOPPED and escalated before ratcheting (SC-4)"
  artifacts:
    - path: "supabase/tests/test_profiles_privileged_columns_locked.sql"
      provides: "1 section twinned beside 7 compound-head SET ROLE lines the R4-C01 tokenizer refuses to neuter"
      contains: "RED-UNDER-SETUP"
    - path: "scripts/mutation-runner/run.mjs"
      provides: "FILES_FLOOR=44 and the end-state ARMS_FLOOR / WAIVED_CEILING with dated blocks"
      contains: "FILES_FLOOR"
  key_links:
    - from: "src/__tests__/mutation-annotation-parser.test.ts"
      to: "scripts/mutation-runner/parse.mjs"
      via: "scanCorpus exact pin: filesAnnotated 44, unreachableFiles 27, pendingFiles 0"
      pattern: "scanCorpus"
---

<objective>
The seven ⚠️ mixed files, batched LAST by CONTEXT because they are the waiver generator: their
non-idiom raises outnumber their idiom raises, and a non-idiom raise that fires first under a
mutation scores `NO-IDENTITY` and cannot be neutered. Planner-measured: 
`test_api_keys_exchange_not_user_writable.sql` (961 lines, 5 ids → **4 sections**, **38
non-idiom**, 7 stamps), `test_strategy_keys_publish_integrity.sql` (371 lines, 5 ids → **4**, 10
non-idiom, 1 stamp), `test_api_keys_insert_not_client_writable.sql` (372 lines, **3**, 11
non-idiom, 1 stamp), `test_sync_status_marked_refresh_protected.sql` (763 lines, 2 ids → **1**,
38 non-idiom, 2 stamps; sentinel file), `test_guard_wizard_draft_updates_auth_uid.sql` (244 lines,
**1**, 9 non-idiom, 0 stamps), `test_profiles_privileged_columns_locked.sql` (140 lines, **1**, 7
non-idiom, 1 stamp — carries seven `SET ROLE postgres; IF NOT ok THEN` compound-head lines the
164.3.1 tokenizer refuses as a neuter target, so do not plan on neutering there) and
`test_wizard_session_idempotency.sql` (317 lines, **1**, 15 non-idiom, 5 stamps). 15 sections.

⛔ WAIVER GATE, before any twin: read Plan 11's cumulative waiver count. For each of the 15
sections, first look for a mutation the shadowing non-idiom raises do not observe (mutate the
object only the idiom arm reads). A waiver is the LAST resort, its reason naming the shadowing
raise's line, and each one is +1 to `WAIVED_CEILING` with the reason beside the constant. If the
cumulative phase total would exceed 5, STOP, write the SUMMARY with the count and the arms, and
return a checkpoint to the founder instead of ratcheting the ceiling — a rising ceiling is the
failure mode it exists to catch.

Purpose: criterion 1 holds for all 44 idiom files; the phase ends with the floors at their measured
end state and the exclusion printed by the gate.
Output: 15 twins, seven setup lines, FILES_FLOOR 44, ARMS_FLOOR → measured (expected 365), the final
prose sweep, the successor TODO cross-referenced.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-CONTEXT.md
@.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md

Read before implementing: Plan 00's record, Plan 11's SUMMARY (waiver tally), PATTERNS § P1.3
Shape 4 (the waiver form; the corpus has none — the first real one is a reviewed decision, not a
template fill), § P4 Group D (the full prose list, for the final sweep); RESEARCH § Waivers and
§ Common Pitfalls 3; GRAMMAR § Neuter semantics.

<!-- verify-symbol: scripts/mutation-runner/run.mjs FILES_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs ARMS_FLOOR -->
<!-- verify-symbol: scripts/mutation-runner/run.mjs WAIVED_CEILING -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/mutation-runner-floors.test.ts GREEN_LOG -->
<!-- verify-symbol: src/__tests__/mutation-annotation-parser.test.ts annotatedFiles -->
<!-- verify-symbol: TODOS.md REDUNDER-NONIDIOM -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1: api_keys_exchange_not_user_writable — setup proven GREEN + ONE section end-to-end past 38 non-idiom raises</name>
  <files>supabase/tests/test_api_keys_exchange_not_user_writable.sql</files>
  <precondition>164.4-11-SUMMARY.md exists, records a merged PR whose head SHA has a `sql-mutation` run on ubuntu with conclusion success (assert with `gh run view <id> --json headSha,conclusion`), and states the cumulative waiver count.</precondition>
  <action>
    FIRST run `node scripts/verify-plan-anchors.mjs .planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-12-PLAN.md` and halt on non-zero.
    Discover the apply list with the lane loop (stand-ins per Plan 00's DECISION, then the real
    last-defining migrations; `--workdir` outside the repo; additive fixture only for relations no
    listed migration creates, recorded in the SUMMARY). Write the setup line at column 0 in the
    header with its 3-line paragraph; re-prove exit 0. List the file's 38 non-idiom raises and the
    objects each reads; choose, for the first idiom section, a mutation none of them observes.
    Author that ONE section in the P1.1 shape with a measured `occurrences`; `--parse-only`;
    diagnostic lane; read the verdict — a `NO-IDENTITY` here means a non-idiom raise fired first:
    change the mutation, not the raise.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && F=supabase/tests/test_api_keys_exchange_not_user_writable.sql && ARM=$(grep -a -m1 -E '^[ \t]*--[ \t]*RED-UNDER-M:' "$F" | sed -E 's/^[^{]*\{"arm":"([^"]*)".*/\1/') && test -n "$ARM" && node scripts/mutation-runner/run.mjs --file "$F" --arm "$ARM" 2>&1 | grep -a -q 'RED (identity ok)'</automated>
  </verify>
  <done>The first mixed file has a proven setup line and one twin whose lane reads RED (identity ok) with no non-idiom raise ahead of it.</done>
</task>

<task type="auto">
  <name>Task 2: Twin every remaining section in all seven files, waivers only as a last resort</name>
  <files>supabase/tests/test_api_keys_exchange_not_user_writable.sql, supabase/tests/test_strategy_keys_publish_integrity.sql, supabase/tests/test_api_keys_insert_not_client_writable.sql, supabase/tests/test_sync_status_marked_refresh_protected.sql, supabase/tests/test_guard_wizard_draft_updates_auth_uid.sql, supabase/tests/test_profiles_privileged_columns_locked.sql, supabase/tests/test_wizard_session_idempotency.sql, TODOS.md</files>
  <action>
    Discover and prove a setup line for each of the other six files (object-name lookup for
    `test_guard_wizard_draft_updates_auth_uid.sql`, which names no stamp). Run the section-coverage
    one-liner over all seven, paste it into the SUMMARY, and twin every untwinned section per the
    rules: real identity as `arm`; mutate the code under test in the last-defining listed migration;
    `occurrences` measured; `sql` step for exactly-pinned grant/policy drift (these are
    not-user-writable / not-client-writable gates — the `sql` GRANT shape will be the common one);
    LAYERED `apply` for self-verifying migrations; `neuter` only for idiom arms and never in
    `test_profiles_privileged_columns_locked.sql`'s compound-head branches; no fixture targets.
    Shadowed by a non-idiom raise → a mutation it does not observe; only when none exists, a Shape
    4 waiver whose reason names that raise's file:line. Keep a running waiver tally; the moment it
    would exceed 5 cumulative, STOP and escalate. A dead arm is a FINDING → `TODOS.md` beside
    `[REDUNDER-NONIDIOM]`, no twin, said loudly. Never edit an assertion, message or identity;
    diffs on the gate files are added comment lines only.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only && node -e 'const fs=require("fs");const sec=x=>x.replace(/(\d)[a-z]*(-[A-Za-z]+)?$/,"$1");let bad=0;for(const f of process.argv.slice(1)){const L=fs.readFileSync(f,"utf8").split("\n");const code=L.filter(l=>!/^\s*--/.test(l)).join("\n");const ids=new Set([...code.matchAll(/TEST FAILED \(([^)]*)\)/g)].map(m=>sec(m[1])));const tw=new Set(L.filter(l=>/^[ \t]*--[ \t]*RED-UNDER-M:/.test(l)).map(l=>sec(JSON.parse(l.replace(/^[ \t]*--[ \t]*RED-UNDER-M:[ \t]*/,"")).arm)));const miss=[...ids].filter(x=>!tw.has(x));bad+=miss.length;console.log(f,"sections",ids.size,"untwinned",miss.length,miss.join(" | "))}process.exit(bad?1:0)' supabase/tests/test_api_keys_exchange_not_user_writable.sql supabase/tests/test_strategy_keys_publish_integrity.sql supabase/tests/test_api_keys_insert_not_client_writable.sql supabase/tests/test_sync_status_marked_refresh_protected.sql supabase/tests/test_guard_wizard_draft_updates_auth_uid.sql supabase/tests/test_profiles_privileged_columns_locked.sql supabase/tests/test_wizard_session_idempotency.sql</automated>
    Expected 4 / 4 / 3 / 1 / 1 / 1 / 1 sections, 0 untwinned each (a booked dead-arm finding is the only permitted exception). Waivers count as twins here and in parity.
  </verify>
  <done>All 15 sections twinned (or booked); seven setup lines proven; parse-only green; the waiver tally is ≤ 5 cumulative or the plan stopped.</done>
</task>

<task type="auto">
  <name>Task 3: Full run green at the end state — FILES_FLOOR 44, ARMS_FLOOR and WAIVED_CEILING measured, final pin and prose sweep</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, scripts/mutation-runner/GRAMMAR.md, scripts/mutation-runner/VAC06-DEMOS.md, .github/workflows/ci.yml, CLAUDE.md, TODOS.md</files>
  <action>
    Run the full runner, iterate on named defects, read every number off the run log (expected
    `coverage: files 44/71`, `unreachable: 27 file(s) …`, an EMPTY `pending:` (or the line absent
    if the runner omits empty lists — quote whichever the runner prints), `biting: 365` minus
    waivers, `arms: E/A/W` with W the waiver count). In ONE commit with the annotations:
    Group A — `run.mjs`: `FILES_FLOOR` → 44 and `ARMS_FLOOR` → measured biting, each with a dated
    block (both separation directions, COVERAGE naming all 44 or pointing at the parser test's
    exact list, RECORD); `WAIVED_CEILING` → measured W, its block listing every waiver's arm and
    reason (the same evidence the twin prints); currency lines edited to the end state ("Phase
    164.4 raises FILES_FLOOR as it backfills…" → past tense with the date; the 27 excluded files
    pointed at `[REDUNDER-NONIDIOM]`); lineage blocks untouched.
    Group B — registry strings `FILES_FLOOR=44`, `ARMS_FLOOR=<measured>`, `WAIVED_CEILING=<W>`, dates.
    Group C — `totalAnchored`, `GREEN_LOG` (coverage 44/71, arms E/A/W, biting, lane-invocations,
    mean, forty-four per-file lines summing to the aggregate, `  waived:` lines if W>0, spawned
    string), `armsSeen`/`stepsSeen`, scanCorpus pin "44 of 71" + all 44 names, and an assertion that
    `pendingFiles` is empty.
    Group D — final prose sweep against PATTERNS § P4 Group D: `CLAUDE.md` § SQL gate integrity
    jobs currency sentence to the end state (files 44/71, arms E/A/W, the 27 printed); `GRAMMAR.md`
    header and Shape 4 currency ("the real corpus has none" is false once W>0 — rewrite to the
    measured count with the date and keep the 2026-08-29 sentence as lineage); `VAC06-DEMOS.md`
    currency lines; the `ci.yml` `sql-mutation` TIMEOUT comment re-justified with this batch's
    measured wall clock (~453 lanes; raise to 30 if ≥ 10 min); `TODOS.md` `[REDUNDER-NONIDIOM]`
    gains one line: "164.4 closed <date>: 44/71 files, <E> arms biting, <W> waived; the runner prints
    these 27 by name every run". Then grep the repo for the superseded figures (`30/30/0`,
    `files 1/71`, `other 70 files`, `~70 more`) with `grep -a -rn` over `CLAUDE.md scripts/ src/__tests__/ .github/`
    and classify every hit as lineage (a dated record of a run) or currency (fix it) in the
    SUMMARY. Verify SERIALLY.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/vac06-mechanism-arms.test.ts && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && node scripts/mutation-runner/run.mjs --parse-only | grep -a -q -E '^coverage: files 44/71$' && [ -z "$(git status --porcelain)" ]</automated>
    <human-check>Land as its own PR (`/ship`, `/gsd-pr-branch` + deletion guard); confirm `sql-mutation` GREEN on ubuntu SHA-BOUND to the PR head; record run id + SHA + wall clock in the SUMMARY. Then run `/gsd-verify-work 164.4`.</human-check>
  </verify>
  <done>Full run exit 0 with `coverage: files 44/71`, the 27 excluded files printed, `pending:` empty, `biting:` == ARMS_FLOOR, waived == WAIVED_CEILING; every pin and prose site at the end state in the same commit; SUMMARY records the SHA-bound ubuntu-green run, the final numbers and the lineage/currency classification of every superseded figure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation → runner → throwaway cluster | twins are executed input; scratch copies only |
| waiver twin → floors | a waiver satisfies parity and spawns no lane; bounded from above by WAIVED_CEILING in-process and in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-21 | Repudiation | waiver creep — this is the batch where it is first exercised for real | high | mitigate | mutation-first search per section; reason names the shadowing raise; ceiling +1 per reviewed waiver; >5 cumulative → STOP and escalate, never ratchet |
| T-164.4-01 | Tampering | twin targeting a stand-in | high | mitigate | parse-time refusal; real migrations listed |
| T-164.4-05 | Repudiation | red baseline counted as coverage | medium | mitigate | baseline proven first; per-file `judged` read off the run |
| T-164.4-09 | Tampering | neutering a compound-head branch that leaks `SET ROLE postgres` | high | mitigate | the R4-C01 tokenizer refuses it (`neuter-missed`); this plan does not neuter in that file |
| T-164.4-04 | Repudiation | stale-integer claims after the last floor move | medium | mitigate | the final grep sweep classifies every superseded figure as lineage or currency |
| T-164.4-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed |
</threat_model>

<verification>
- One-liner: 4 / 4 / 3 / 1 / 1 / 1 / 1 sections, 0 untwinned; parse-only exit 0.
- Full run: `coverage: files 44/71`, `unreachable: 27 file(s) …`, `pending:` empty, `biting:` == ARMS_FLOOR, waived == WAIVED_CEILING ≤ 5, tallies agree, exit 0; lockstep vitest green; ci.yml parses; tree clean; `--pending` anchors green (this is the last pending plan).
- Landed as one PR; `sql-mutation` green on ubuntu at the PR head SHA.
</verification>

<success_criteria>
- Criterion 1 (as amended), 2, 3 and 4 all hold at once, emitted by the gate itself: 44/71 annotated and proven, 27 printed by name, floors at their measured end state, waivers recorded with reasons and bounded. The phase is ready for `/gsd-verify-work 164.4`.
</success_criteria>

<output>
Create `.planning/phases/164.4-redunder-backfill-every-sql-gate-arm-gets-a-red-under-annota/164.4-12-SUMMARY.md` when done. Record: one-liner output, the seven apply lists, run log lines verbatim (coverage, unreachable, pending, arms, biting, lane-invocations, every waived line), floors old → end state, every waiver's arm + reason + shadowing line, findings, wall clock and timeout decision, PR number, SHA-bound run id, and the lineage/currency classification table.
</output>
