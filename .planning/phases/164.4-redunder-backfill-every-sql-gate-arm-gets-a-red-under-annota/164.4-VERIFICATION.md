---
phase: 164.4-redunder-backfill
verified: 2026-09-04T20:15:00Z
status: human_needed
score: 4/4 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Land the phase branch as a PR and obtain a SHA-BOUND ubuntu `sql-mutation` green at the 39/262 end state. Read `headRefOid` on every poll iteration; record run id + head SHA + wall clock in the SUMMARY."
    expected: "`sql-mutation` succeeds on ubuntu-latest with `coverage: files 39/71`, `arms: 262/262/0`, `biting: 262`, `lane-invocations: 262`, tallies agree, `lane-probe: pg_cron absent`, `pending: 1`. Wall clock under 10 min (ci.yml's legs projection is 8.2 min at the worst observed 1.07 s/leg)."
    why_human: "Requires pushing a branch, opening a PR and waiting on a remote CI host. The 262-arm end state has been measured ONLY on the macOS authoring box (PostgreSQL 16.13). CONTEXT made ubuntu-green a phase gate precisely because 'scaling 30 → ~260 arms on an unobserved host is how a floor rots', and run 33804312706 already demonstrated a 70% per-lane divergence between the two hosts. Plan 11's own `<human-check>` is OPEN and STATE.md records it as such."
warnings:
  - id: WR-V1
    file: .planning/STATE.md:88
    issue: "Surviving pre-amendment claim: '255 sections / ~265 twins over the 40 reachable files', under the heading '## Scope Amendments (in force)'. ROADMAP.md's PARALLEL sentence was corrected on 2026-09-04 to '252 sections / 262 twins across the 39 reachable idiom files'; STATE's was not."
  - id: WR-V2
    file: scripts/mutation-runner/run.mjs:2669 and :2675
    issue: "Uncorrected '40/71' end-state claims in live source. run.mjs:160 carries the same claim and WAS corrected at :161-169; these two were missed because plan 11's lineage sweep grep pattern did not include `40/71`."
  - id: WR-V3
    file: scripts/mutation-runner/run.mjs:2827, .github/workflows/ci.yml
    issue: "The `pending:` class line carries no reason-for-deferral and no TODO id (unlike `unreachable:` and `lane-blocked:`), and is the only class line ci.yml does not MEASURE_FAIL on absence or cross-check count-vs-names."
---

# Phase 164.4: REDUNDER-BACKFILL Verification Report

**Phase Goal:** Every arm in the 39 idiom files the pg-lane can reach of `supabase/tests/*.sql`
carries a `RED-UNDER` annotation naming the exact mutation that makes it fail, and the Phase 164.3
mutation runner PROVES each one bites — red under its own mutation, green when restored. Four more
files printed as `lane-blocked:` every run and a fifth under `pending:`, all five owed to Phase
164.4.1.

**Verified:** 2026-09-04T20:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (the four ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | Every arm in the 39 reachable idiom files carries a `RED-UNDER`; the 27 non-idiom, 4 lane-blocked and 1 pending files are ALL named by the runner every run; a silent exclusion fails | ✓ VERIFIED | Partition recomputed independently from the runner's own output (below). 39 annotated / 262 prose / 262 twins / 0 waivers / `✅ No static defects`. Exact sets pinned in vitest, 177/177 pass. |
| SC-2 | The runner EXECUTES all of them — each RED under its own mutation and GREEN when restored | ✓ VERIFIED (host-limited) | Full-corpus run at HEAD: exit 0, `arms: 262/262/0`, `biting: 262`, `lane-invocations: 262`, tallies agree, tree byte-identical before and after; `--self-test` exit 0, all modes fire. ⚠️ macOS only — see Human Verification. |
| SC-3 | The coverage figure is CI output and floor-ratcheted; no silent caps | ✓ VERIFIED | `FILES_FLOOR=39` / `ARMS_FLOOR=262` each carry a dated block with BOTH separation directions driven through the real verdict loop; ci.yml reads all three constants FROM run.mjs and MEASURE_FAILs on every absent line; bidirectional lockstep vitest pins. |
| SC-4 | Any arm that cannot be given a falsifying mutation is RECORDED with its reason, never silently skipped | ✓ VERIFIED | 0 waivers in the corpus; `WAIVED_CEILING = 0` unedited since 35c0418f (git -L proof); both near-waiver arms resolved by root-cause fixes I read in the diff; 3 coverage findings booked to `TODOS.md`. |

**Score:** 4/4 truths verified (0 present, behavior-unverified)

### SC-1 — the partition, recomputed rather than read

`node scripts/mutation-runner/run.mjs --parse-only` → exit 0. I parsed its output with an
independent script rather than reading its summary lines:

```
dir 71   union 71   missing from union: []   extra: []
overlaps: []
rows 39   sum annotated 262   sum sections 252   sum waived 0
unreachable count field 27  names 27
lane-blocked count 4        names 4
pending count 1             names ['test_compute_jobs_error_kind_copy_parity.sql']
annotated file rows 39  sum prose 262  sum twins 262
```

- **39 + 27 + 4 + 1 = 71**, and the four name-sets are **pairwise disjoint** and their union is
  **byte-equal to `ls supabase/tests/*.sql`** (71 files). Nothing is silently excluded, and nothing
  is double-counted into two classes to make an arithmetic work.
- **Sum of `sections` = 252, sum of `annotated` = 262** — the ROADMAP's corrected figures, derived
  here rather than quoted.
- Every excluded file is printed BY NAME. `unreachable:` carries its reason plus
  `(TODOS [REDUNDER-NONIDIOM])`; `lane-blocked:` carries its reason plus
  `(deferred 2026-09-03, TODOS [REDUNDER-PGCRON])`. **`pending:` names its one file with a reason
  but no TODO id** — see WR-V3.
- The half-annotation hole is closed by SET INCLUSION, not a count:
  `mutation-annotation-parser.test.ts:1751` asserts every SECTION an annotated file raises for also
  carries a twin. That is the correct shape — `annotated >= sections` would be satisfied by a file
  with two twins on half its sections, which is exactly the state the reference file was in (30
  twins over 20 of 35 sections).

### SC-2 — behavioral evidence

SC-2 is a behavior-dependent truth (RED-then-restore-GREEN is a state transition presence checks
cannot see). The behavioral evidence exists and was measured at HEAD on this tree:

| Behavior | Command | Result | Status |
|---|---|---|---|
| Every annotated arm reddens on its own mutation and the tree restores | `node scripts/mutation-runner/run.mjs` | exit 0, `coverage: files 39/71`, `arms: 262/262/0`, `biting: 262`, `lane-invocations: 262`, tallies agree, tree byte-identical | ✓ PASS (macOS) |
| Every defect kind can still fire | `node scripts/mutation-runner/run.mjs --self-test` | exit 0, all modes fire | ✓ PASS |
| Static corpus clean | `node scripts/mutation-runner/run.mjs --parse-only` | exit 0, `✅ No static defects` | ✓ PASS (re-run by me) |
| Lockstep floor + partition pins | `npx vitest run mutation-annotation-parser.test.ts mutation-runner-floors.test.ts` | 2 files, **177 passed** | ✓ PASS (run by me) |
| Full corpus not re-run | — | deliberately skipped per instruction (~6 min, asserts a globally clean tree) | ? SKIP |

⚠️ The 262-arm end state has been proven on the **authoring host only**. The ubuntu SHA-bound green
is owed and is the single human-verification item.

### SC-3 — floors, separation, and the ceiling

| Constant | Value | Dated block | Both separation directions measured? |
|---|---|---|---|
| `FILES_FLOOR` (`run.mjs:518`) | 39 | RE-DERIVED 2026-09-04, HEAD `1aaba266`, plan 11 | ✓ `filesFloor=39 → defects=0 SILENT (352.8 s)`; `filesFloor=40 → FIRES: 'FILES_FLOOR regression: 39 annotated file(s) < floor 40' (349.3 s)` |
| `ARMS_FLOOR` (`run.mjs:1006`) | 262 | RE-DERIVED 2026-09-04, plan 11 | ✓ `armsFloor=262 → defects=0 SILENT`; `armsFloor=263 → FIRES: 'ARMS_FLOOR regression: 262 biting arm(s) < floor 263'` |
| `WAIVED_CEILING` (`run.mjs:1047`) | 0 | PINNED 2026-09-02, CURRENCY note 2026-09-04 | n/a — **UNEDITED**, see below |

Both separation runs were driven **through the real verdict loop on real lanes**
(`runCorpus({filesFloor, armsFloor})`), not by asserting the constant against itself.

**`WAIVED_CEILING` is provably unedited.** `git log -L '/^export const WAIVED_CEILING/,+1'` returns
exactly ONE commit — `35c0418f` (Phase 164.3.1), the phase base. It has not been touched by any of
the twelve 164.4 plans.

**Both floors ratcheted monotonically, never lowered**, once per plan as CONTEXT required:

```
ARMS_FLOOR   30 → 45 → 86 → 134 → 163 → 189 → 219 → 239 → 247 → 262
FILES_FLOOR   1 →  4 →   9 →  13 →  17 →  23 →  28 →  32 →  39
```

**No number is restated in ci.yml.** The workflow imports `m.FILES_FLOOR, m.ARMS_FLOOR,
m.WAIVED_CEILING` from `run.mjs` and a vitest arm asserts the workflow contains no literal
comparison (`expect(ci).not.toMatch(/waived_ceiling" -(eq|ne|lt|gt) "?[0-9]/)`).

**The vitest pins are bidirectional** — they fail on a REGRESSION *and* on a STALE ratchet, for all
three constants, so the floors cannot drift below the corpus and the ceiling cannot be silently
raised.

### SC-4 — waivers

- `grep -rn '"waiver"' supabase/tests/*.sql` → **0**. Cumulative waivers across the whole phase: 0.
  No founder-unapproved waiver can exist, because no waiver exists.
- The two arms that came close were resolved by root-cause fixes, and I read both diffs rather than
  the SUMMARY's description of them:
  - `test_get_published_trust_signals.sql` — assertion `(5)` moved ahead of `(1)-(3)`. The moved
    block is **byte-identical**, the `(5)` label is deliberately not renumbered, and the moved check
    uses `has_function_privilege('anon', …)`, which takes the role as an argument and so is correct
    outside the `SET LOCAL ROLE anon` span it now precedes. Correct.
  - `test_resync_retry_single_job.sql` — assertion `(b)`'s INSERT wrapped in the file's own
    `BEGIN … EXCEPTION WHEN unique_violation` idiom. The handler **re-raises** `TEST FAILED (b)`
    (with `RESET ROLE` first) rather than swallowing, and catches only `unique_violation`, so every
    other error still propagates. Not a vacuity introduction.
- The un-falsifiable classes are recorded with reasons and TODO ids: 4 `lane-blocked:`
  (`[REDUNDER-PGCRON]`) and 1 `pending:` (owed to Phase 164.4.1, `[REDUNDER-LANEBLOCKED-BLIND]`).
  Three arms whose conjunct could not be isolated are booked as findings, not buried:
  `[REDUNDER-COVGAP-01]`, `[REDUNDER-COVGAP-02]`, and the classifier defect
  `[REDUNDER-LANEBLOCKED-BLIND]` — all four ids present in root `TODOS.md`.

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `sql-mutation` job | `frontend` aggregator | `needs:` list **and** the result loop | ✓ WIRED | `ci.yml:1736` and `ci.yml:1754` — a failure fails the aggregate, it cannot pass quietly |
| `ci.yml` assertion step | `run.mjs` floor constants | `node -e import(…).then(m => …FILES_FLOOR, ARMS_FLOOR, WAIVED_CEILING)` | ✓ WIRED | No literal restated in the workflow; unreadable value is its own MEASURE_FAIL |
| `ci.yml` | runner's printed classification | `^unreachable:` / `^lane-blocked:` / `^lane-probe:` greps + count-vs-names cross-checks | ✓ WIRED | Absent line = MEASURE_FAIL, never a silent zero |
| `ci.yml` | `pending:` line | — | ⚠️ PARTIAL | Not asserted in ci.yml; its shape IS pinned against **real** runner output by `mutation-runner-floors.test.ts:463`, which runs on every push. See WR-V3. |
| Corpus classification | exact file sets | `mutation-annotation-parser.test.ts` | ✓ WIRED | 39 annotated names, 27 unreachable names, 4 lane-blocked names, `pendingFiles` as a one-name SET, and a five-class partition assertion — all pinned by name, not by count |
| Gate corpus | production objects | `RED-UNDER-M` `apply` targets | ✓ WIRED | See data-flow trace below |

### Data-Flow Trace — do the mutations reach production objects?

This is where a vacuity would hide: an annotation that reddens its arm by mutating the *test*
rather than the object under test.

| Target class | Count | Verdict |
|---|---|---|
| `supabase/migrations/**` | majority | ✓ FLOWING — real production DDL/function bodies |
| `kind: "sql"` steps (ALTER POLICY, REVOKE …) | several | ✓ FLOWING — real live objects |
| `scripts/pg-lane/fixtures/**` | **0** | ✓ REFUSED AT PARSE TIME — GRAMMAR rule 4, `parse.mjs:272`, with a firing self-test fixture |
| `supabase/tests/**` (the gate file itself) | 15 | ✓ LEGITIMATE — audited individually, below |

The 15 self-targeting mutations are an **explicitly permitted** shape (GRAMMAR rule 4: "Target a
`supabase/migrations/**` file, the gate file itself, or use a `sql` step"), fenced by rules 3a/3b
(`refuseSelfSatisfying`, `refuseRetargetingFailureLiteral`) which refuse injecting a `TEST FAILED`
literal or retargeting the arm's own raise. I read every one:

- `test_create_wizard_strategy_for_key.sql` arm **D** — mutates the file's own comment-stripper.
  Correct: arm D's *entire claim* is that the stripper ran; the stripper exists nowhere else. The
  annotation says so and cites rule 3b.
- `test_api_keys_exchange_not_user_writable.sql` arm **5c scrub half** — mutates the gate's own
  `REVOKE`. Correct: the arm asserts the gate withdrew the door it opened, and no migration can
  falsify that. The annotation records the measurement showing every other route fires 30 lines
  earlier.
- `test_scenario_downgrade_sweep.sql` (6) — mutates the gate's embedded **copy** of the sweep DETECT
  clause. The prose is explicit ("in THIS assertion's copy of the sweep DETECT clause") and scoped
  ("the post-condition's identical clause is a separate arm and is left alone"). Honest about what
  it proves. The residual — a copy-gate can only prove its copy — is a *coverage* limit, and adding
  coverage is explicitly out of this phase's scope.
- `test_strategy_shares_rls.sql` TENANT 2a / 5b — LAYERED: the primary step is a real
  `ALTER POLICY`; the gate-file edits only remove a setup step that would otherwise abort earlier,
  with `neuter` entries for the preceding arms. First-failure discipline, not vacuity.
- `test_scenarios_rls.sql` "sanity" and `test_capital_ownership_allocation_guard.sql` "7i setup" —
  both target the seed the arm asserts about.

**No mutation targets a stand-in fixture, and none is self-satisfying.**

### Anti-Patterns Found

| Scope | Pattern | Result |
|---|---|---|
| All files changed 35c0418f..HEAD under `scripts/ src/ .github/ supabase/ CLAUDE.md` | `TBD` / `FIXME` / `XXX` | **0** — the debt-marker gate is clean |
| Same set | `TODO` / `HACK` / `PLACEHOLDER` | 6 hits, all benign prose (`…with their reason and TODO id`; pre-existing `PLACEHOLDER NEXT_PUBLIC_*` build comments) |

**Corpus-wide "comment-only" claim, re-derived independently.** I diffed all 39 changed
`supabase/tests/*.sql` with `-U0`, stripped `+`/`-` markers, dropped `--` lines and blanks, and
counted what remained:

```
supabase/tests/test_get_published_trust_signals.sql : 12
supabase/tests/test_resync_retry_single_job.sql     : 18
(39 changed gate files total)
```

Exactly **two** files carry executable changes, both audited above and both correct. This matches
the code review's independent measurement.

**`ci.yml` removed lines across the phase:** exactly one non-comment line, `-          cache: npm`.
No assertion, guard or grep was deleted or loosened.

## Warnings

### WR-V1 — a superseded figure survives in STATE.md while ROADMAP's twin was corrected

**File:** `.planning/STATE.md:88`

Under the heading `## Scope Amendments (in force)`:

> **Arm unit = SECTION** (not per-raise, not per-identity): **255 sections / ~265 twins over the 40
> reachable files**; 100 sections in 4 lane-blocked files owed to `[REDUNDER-PGCRON]`

`ROADMAP.md`'s **parallel** arm-unit sentence was corrected on 2026-09-04 to "**252 sections / 262
twins** across the 39 reachable idiom files (… the projection was 255 / ~265 over 40 files)". STATE's
was not, and carries no ⛔ SUPERSEDED marker. The ARITHMETIC CORRECTION paragraph at STATE.md:99 says
"#2's count **below** was 40 files / 255 sections" — it points at line 110, not at line 88 above it.

This is not a false SUMMARY claim: plan 11's table enumerates six ROADMAP sites plus one STATE site
(the amendment-#2 paragraph at :110, which *is* correctly marked SUPERSEDED), and STATE.md:194's
"all seven ledger sites" is literally satisfied by that count. The amendment-#1 arm-unit paragraph
was simply never in the enumerated set. But the result is the exact drift STATE's own next sentence
warns about — *"an amendment landing in ONE file leaves the refused claim alive in the others"* —
sitting under a heading that declares it in force.

**Not a merge blocker** under the project stopping rule (not user-facing, not data-integrity).
**Fix:** correct the sentence to 252 / 262 / 39, or mark it ⛔ SUPERSEDED and point at the
ARITHMETIC CORRECTION.

### WR-V2 — two uncorrected `40/71` end-state claims in live source

**File:** `scripts/mutation-runner/run.mjs:2669` and `:2675`

```
// state is `files 40/71`, not 44/71, and the deferred four are printed under
…
// ⚠️ CURRENCY 2026-09-03 (plan 164.4-07): the classes are unchanged and the
// end state is still 40/71; only the annotated/pending SPLIT has moved …
```

`:2675` is the **latest** currency note in that block, so a reader reaches it as the current
reading. The same claim at `run.mjs:160` **was** caught and corrected in place at `:161-169`
("that `40/71` … is now FALSE. The reachable end state is `coverage: files 39/71`"); these two were
missed because plan 11's lineage-vs-currency sweep grepped
`30/30/0|files 1/71|other 70 files|~70 more|44/71|355 sections|WAIVED_CEILING` — `40/71` is not in
the pattern set.

Mitigating: `:2677` says *"Read the run's own `coverage:` and `pending:` lines, never this
sentence."* **Fix:** append a 2026-09-04 correction, as `:161` already models.

### WR-V3 — `pending:` is the weakest of the three class lines

**File:** `scripts/mutation-runner/run.mjs:2827`, `.github/workflows/ci.yml`

`unreachable:` and `lane-blocked:` each carry a reason **and** a TODO id, and ci.yml MEASURE_FAILs on
their absence *and* cross-checks the claimed count against the `*.sql` names printed beside it.
`pending:` carries neither a TODO id nor a deferral reason — its text is
`"N idiom file(s) without RED-UNDER"`, which at this phase's end state **misdescribes its sole
member**: `test_compute_jobs_error_kind_copy_parity.sql` is not un-annotated work, it is a founder
deferral to Phase 164.4.1. And ci.yml asserts nothing about the line at all.

The exposure is bounded, not open: `mutation-runner-floors.test.ts:463` runs the **real** runner and
asserts `/^ {2}pending: \d+ idiom file\(s\) without RED-UNDER — /m`, and
`mutation-annotation-parser.test.ts:1892` pins `pendingFiles` as the exact one-name set. Both run on
every push. So SC-1's literal standard — *the runner names all three sets every run* — is met and
machine-guarded.

**Fix:** give `pending:` the same treatment as its two siblings (reason + TODO id in the string, and
a count-vs-names arm in ci.yml). Worth doing in 164.4.1, which drives this class to a different
shape anyway.

## Info

- **`WAIVED_CEILING`'s CURRENCY note says "all EIGHT arms moves of this phase".** `ARMS_FLOOR` moved
  **nine** times in 164.4 (plans 02, 04, 05, 06, 07, 08, 09, 10, 11). Defensible if "arms moves"
  means the eight file-batches and treats plan 02's reference-file deepening separately, but the
  constant moved nine times. The load-bearing part of the claim — cumulative waivers **0** — is
  independently confirmed.
- **Code review IN-01 is still open.** The record describes the `ci.yml` change as comment-only; it
  is comment-only **plus** the `cache: npm` removal. I re-measured: exactly one removed non-comment
  line in the whole workflow, and the removal is correct (the job runs no `npm ci` and no `npx`).
- **All 12 SUMMARYs carry `status: complete`.** All 12 plans are checked off in ROADMAP.
- The four coverage/defect findings this phase surfaced are all promoted to root `TODOS.md`
  (`[REDUNDER-COVGAP-01]`, `[REDUNDER-COVGAP-02]`, `[REDUNDER-WINDOWS-01]`,
  `[VITEST-CORPUS-TIMEOUT-01]`), not left only in the phase-local `deferred-items.md` that PR
  filtering drops.

## Human Verification Required

### 1. SHA-bound ubuntu `sql-mutation` green at the 39/262 end state

**Test:** Land the branch as its own PR (`/gsd-ship`, then `/gsd-pr-branch` **with the deletion
guard** — `git diff --diff-filter=D --name-only main..<branch>-pr -- .planning/` must be empty).
Poll the PR head SHA on every iteration and read `sql-mutation`'s conclusion bound to that SHA.

**Expected:** success on `ubuntu-latest`, with the job log showing `coverage: files 39/71`,
`arms: 262/262/0`, `biting: 262`, `lane-invocations: 262`, tallies agree,
`lane-probe: pg_cron absent`, `lane-blocked: 4`, `pending: 1`, and a wall clock under 10 minutes.
Record run id + head SHA + wall clock in the SUMMARY and replace ci.yml's legs **projection** with
that measurement.

**Why human:** Needs a push, a PR and a remote host. The 262-arm end state is measured on the macOS
authoring box only. CONTEXT made ubuntu-green a phase gate because "scaling 30 → ~260 arms on an
unobserved host is how a floor rots", and run 33804312706 already showed a 70% per-lane divergence
between the two hosts (1.7 s vs 1.0 s). Plan 11's `<human-check>` is OPEN and STATE.md records it as
such.

## Gaps Summary

**None.** All four success criteria are achieved in the codebase, and the evidence is the gate's own
output rather than any SUMMARY's account of it:

- The five classes **partition** the 71-file corpus exactly and disjointly — recomputed by me from
  the runner's output, not read off its summary lines — with every excluded file named.
- 262 twins over 252 sections in 39 files, **0 waivers**, 0 static defects, and a set-inclusion pin
  (not a count) closing the half-annotated-file hole.
- Both floors carry dated blocks with **both separation directions** driven through the real verdict
  loop; `WAIVED_CEILING` is 0 and git-provably unedited across the entire phase.
- Only two gate files carry executable changes, both audited: a byte-identical reorder and an
  exception handler that **re-raises** its arm identity rather than swallowing it.
- Every mutation targets a real production object, the gate's own asserted mechanism, or a `sql`
  step. Zero target a pg-lane stand-in — refused at parse time by GRAMMAR rule 4, which ships with a
  firing self-test fixture.

Three WARNINGs are recorded above. All three are **record-hygiene residuals in prose** — a stale
figure in STATE.md, two stale figures in run.mjs comments, and an under-guarded printed line whose
shape is nonetheless pinned against real runner output. None causes a truth to fail, and none meets
the project stopping rule's bar for blocking. WR-V1 and WR-V2 deserve a fix on this branch, because
this is the phase whose entire thesis is that a claim and the thing it claims about must be
compared.

The single open item is the ubuntu SHA-bound CI green, which is the phase's own recorded human
check.

---

_Verified: 2026-09-04T20:15:00Z_
_Verifier: Claude (gsd-verifier)_
