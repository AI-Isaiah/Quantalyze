---
phase: 136-mt5recon-equity-reconstruction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/mt5_deals.py
  - analytics-service/services/broker_dailies.py
  - analytics-service/tests/test_mt5_deal_reconstruction.py
autonomous: true
requirements: [MT5RECON-01, MT5RECON-02]
must_haves:
  truths:
    - "A deposit day's return equals the hand-derived trading return (300/100_400), never the +10.3% cash spike"
    - "An unclassifiable or ambiguous DEAL_TYPE (CORRECTION/CHARGE/INTEREST/CANCELED/DIVIDEND/unknown) raises a typed error — never silently dropped or coerced"
    - "Zero-cash-rotation ledger ⇒ external flow F=0 and returns equal the pure-PnL hand literals"
    - "An MT5 return series annualizes on √252; flipping the clock to √365 turns the fixture test RED"
  artifacts:
    - path: "analytics-service/services/mt5_deals.py"
      provides: "PURE I/O-free DEAL_TYPE classifier + server-time→UTC day bucketing"
      exports: ["classify_deal", "Mt5DealClassificationError", "_MT5_TRADING_DEAL_TYPES", "_MT5_EXTERNAL_FLOW_DEAL_TYPES"]
    - path: "analytics-service/services/broker_dailies.py"
      provides: "combine_mt5_deal_ledger — third combiner sibling beside :174/:230"
      contains: "def combine_mt5_deal_ledger"
    - path: "analytics-service/tests/test_mt5_deal_reconstruction.py"
      provides: "Hand-derived economic oracles (MT5RECON-01/02)"
      min_lines: 150
  key_links:
    - from: "analytics-service/services/broker_dailies.py"
      to: "services/nav_twr.chain_linked_twr (via combine_realized_and_funding or direct)"
      via: "flow-in-numerator TWR — the ONE shared engine"
      pattern: "combine_realized_and_funding|chain_linked_twr"
    - from: "analytics-service/services/broker_dailies.py"
      to: "services/mt5_deals.py"
      via: "classification import — combiner never re-implements the table"
      pattern: "from services.mt5_deals import"
---

<objective>
Build the MT5 deal-ledger → daily-return reconstruction core: a PURE fail-loud
DEAL_TYPE classifier (`services/mt5_deals.py`) and `combine_mt5_deal_ledger`
(third sibling in `broker_dailies.py`), pinned by hand-derived economic oracles
including the load-bearing deposit-day-is-not-a-spike test and a √252-vs-√365
mutation guard.

Purpose: MT5RECON-01's money-math core — everything downstream (worker branch,
plan 136-03) composes these two functions. Oracle discipline is NON-NEGOTIABLE:
3 money bugs once survived 6 review passes behind self-referential fixtures.
Output: `mt5_deals.py`, `combine_mt5_deal_ledger`, `tests/test_mt5_deal_reconstruction.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-mt5recon-equity-reconstruction/136-CONTEXT.md
@.planning/phases/136-mt5recon-equity-reconstruction/136-RESEARCH.md
@analytics-service/services/broker_dailies.py
@analytics-service/services/deribit_txn.py
@analytics-service/services/nav_twr.py
@analytics-service/services/mt5_client.py
@analytics-service/tests/test_mt5_golden_fixtures.py

<interfaces>
The combiner MUST return the exact sibling shape (broker_dailies.py:174/:230):

```python
def combine_native_ledger(...) -> tuple[pd.Series, dict[str, Any]]: ...
def combine_sfox_balance_history(usd_value, flows_by_day) -> tuple[pd.Series, dict[str, Any]]: ...
# combine_realized_and_funding(realized_pnl_records, funding_rows, account_balance,
#     balance_error=False, *, external_flows=None, open_unrealized_usd=0.0)
#     -> tuple[pd.Series, dict]   (broker_dailies.py:141 — anchor-to-today backward
#     roll + flow-in-numerator + gap_fill, threads open_unrealized_usd to the honest core)
```

Mt5Client contract (mt5_client.py): `account_info() -> dict` (materialized full
account dict — `equity`, `balance`, `login`, `currency` per the MT5 account_info
contract), `history_deals_get(from_ts, to_ts) -> list[dict]` where None from the
terminal is ALREADY converted to a typed raise at the client (:233-243) and `()`
means honest empty. Deal rows carry `type` (DEAL_TYPE_* int), `entry`, `profit`,
`swap`, `commission`, `fee`, `time` (broker-server-time epoch, VERBATIM — :237-239).

DEAL_TYPE constants (MetaTrader5 standard enum, `[ASSUMED]` until the 134/139
live spike — plan 136-05 checkpoint): BUY=0, SELL=1, BALANCE=2, CREDIT=3,
CHARGE=4, CORRECTION=5, BONUS=6, COMMISSION=7, COMMISSION_DAILY=8,
COMMISSION_MONTHLY=9, COMMISSION_AGENT_DAILY=10, COMMISSION_AGENT_MONTHLY=11,
INTEREST=12, BUY_CANCELED=13, SELL_CANCELED=14, DIVIDEND=15,
DIVIDEND_FRANKED=16, TAX=17. `DEAL_TYPE_BALANCE == 2` is
`[VERIFIED: scripts/mt5_spike.py:88]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/mt5_deals.py — pure fail-loud DEAL_TYPE classifier + UTC day seam</name>
  <files>analytics-service/services/mt5_deals.py, analytics-service/tests/test_mt5_deal_reconstruction.py</files>
  <read_first>
    - analytics-service/services/deribit_txn.py:516-563 (CASH_BEARING/INFORMATIONAL frozensets + import-time disjointness assert), :608-660 (the 'correction' fail-loud lesson), :919 (_row_utc_day fail-loud day bucketing)
    - analytics-service/scripts/mt5_spike.py:88-89 (_DEAL_TYPE_BALANCE = 2)
    - docs/mt5-spike-gonogo.md §7 (Phase-136 combiner = the ONE server-time→UTC normalize seam)
  </read_first>
  <behavior>
    - classify_deal on type 2 (BALANCE) → "external_flow"; types 3 (CREDIT), 6 (BONUS) → "external_flow"
    - classify_deal on types 0/1 (BUY/SELL) and 7-11 (COMMISSION family) → "trading"
    - classify_deal on types 4 (CHARGE), 5 (CORRECTION), 12 (INTEREST), 13/14 (CANCELED), 15/16 (DIVIDEND), 17 (TAX), and ANY unlisted int (e.g. 99) → raises Mt5DealClassificationError naming the type value (allow-list, not block-list — the deribit 'correction' lesson; per user decision Q2 the ambiguous middle defaults FAIL-LOUD, exact classification locked behind the plan 136-05 human-verify checkpoint)
    - import-time assert: _MT5_TRADING_DEAL_TYPES and _MT5_EXTERNAL_FLOW_DEAL_TYPES are disjoint
    - deal_utc_day(time_value, server_utc_offset_s) subtracts the offset then buckets to a UTC ISO day; a missing/undatable/non-numeric time raises (never silently dropped) — mirror deribit_txn._row_utc_day's fail-loud posture
    - a non-numeric/NaN/Inf profit/swap/commission/fee on a classified deal raises (the _coerce_float discipline, nav_twr.py:223) — a silent-NaN must never sail into a 'complete' track record
  </behavior>
  <action>
    Create `analytics-service/services/mt5_deals.py` — PURE, I/O-free (stdlib +
    typing only; no network, no pandas needed for classification) so correctness
    tests are network-free, mirroring deribit_txn.py's pure-core discipline.
    Define int-keyed frozensets `_MT5_TRADING_DEAL_TYPES = {0, 1, 7, 8, 9, 10, 11}`
    and `_MT5_EXTERNAL_FLOW_DEAL_TYPES = {2, 3, 6}` with an import-time
    disjointness assert (deribit_txn.py:552 precedent). Export
    `Mt5DealClassificationError` (typed, message carries the deal TYPE CODE and
    row COUNT context only — NEVER raw USD amounts; the nav_twr leak-safe raise
    discipline) and `classify_deal(deal: Mapping) -> Literal["trading","external_flow"]`
    which raises on anything outside the two allow-lists. Document per-type
    rationale in comments citing 136-RESEARCH.md Q2, and tag types 4/12/13/14/15/16/17
    plus the per-row field fold rule as [ASSUMED pending 136-05 checkpoint / live
    spike]. Add `deal_utc_day(time_value, server_utc_offset_s: int) -> str` — the
    ONE server-time→UTC normalize seam (subtract offset, then UTC date isoformat;
    fail-loud on undatable input). Add `deal_cash_effect(deal) -> float` summing
    profit + swap + commission + fee with fail-loud float coercion (reject
    NaN/Inf/non-numeric); document the [ASSUMED A3] fold convention (each field
    summed once per row; broker double-posting commission as both field and
    separate row is caught by the reconcile gate + checkpoint).
    Write the classifier tests FIRST in
    `analytics-service/tests/test_mt5_deal_reconstruction.py` (new file; header
    docstring states the oracle discipline: literals hand-derived, NEVER
    regenerated from the SUT). Do NOT import csv_validator/pandera-dependent
    modules — keep the test importable on a pandera-less local env.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_deal_reconstruction.py -x -k "classif or utc_day or unknown"</automated>
  </verify>
  <done>Classifier allow-lists match the locked Q2 table; unknown/ambiguous types raise typed with no USD in the message; disjointness asserted at import; UTC-day seam subtracts the offset before bucketing; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: combine_mt5_deal_ledger — third combiner sibling, anchor-to-equity, flow-in-numerator</name>
  <files>analytics-service/services/broker_dailies.py, analytics-service/tests/test_mt5_deal_reconstruction.py</files>
  <read_first>
    - analytics-service/services/broker_dailies.py:141-171 (combine_realized_and_funding — the anchored realized-records path), :230-389 (combine_sfox_balance_history — the chain_linked_twr direct path + the cashflow-separation docstring :249-253), :124-138 (gap_fill_daily_returns — ledger-complete 0.0 fill)
    - analytics-service/services/nav_twr.py:56-96 (DUST_NAV_FLOOR, FLOW_DOM_RATIO, UNREALIZED_MATERIALITY_RATIO=0.05), :201-212 (NAV_TWR_GUARD_KEYS incl. unrealized_pnl_in_anchor), :572-602 (reconcile_flow_residual, tolerance max(1.00, 1e-6·|terminal|))
    - analytics-service/services/external_flows.py (ExternalFlow shape — the DQ-02 evidence type sfox_flows_by_day also emits, cited job_worker.py:3181-3186)
  </read_first>
  <behavior>
    - Deposit day (BALANCE +10_000 same day as +300 trading PnL, prior close 100_400): return == 300/100_400 (hand literal ≈ 0.0029880478), NOT (110_700-100_400)/100_400 ≈ +0.1026
    - Withdrawal day (BALANCE −5_000 same day as +300 PnL): return == 300/100_400 — the outflow neither depresses nor inflates
    - Zero-cash-rotation ledger (no BALANCE deals): external flow F=0 on every day; returns equal the pure hand PnL literals (e.g. +400/100_000 = 0.0040 then −200/110_700 on the fixture below)
    - No-activity day between deals: 0.0 return (MT5 is ledger-COMPLETE — gap_fill 0.0 is CORRECT here, unlike sfox's sampled NAV)
    - A ledger containing an unclassifiable deal type raises Mt5DealClassificationError before any series is produced (nothing partial)
    - The fixture includes at least one NEGATIVE day so the reconstructed series can never trip quantstats 0.0.81 price-detection (Pitfall 4)
  </behavior>
  <action>
    Add `combine_mt5_deal_ledger(deals, account_equity, account_balance, *,
    server_utc_offset_s=0) -> tuple[pd.Series, dict[str, Any]]` to
    `services/broker_dailies.py` directly beside its two siblings (:174/:230),
    returning the byte-identical (gap-filled float Series on ascending daily
    DatetimeIndex unit [us], plain NavTWRMeta dict) shape. Steps: (1) classify
    every deal via services.mt5_deals (unknown → the typed raise propagates);
    (2) bucket per UTC day via deal_utc_day(time, server_utc_offset_s) — the ONE
    normalize seam; (3) daily trading PnL = Σ deal_cash_effect over trading-type
    rows per day; daily external flow = Σ profit field of external_flow-type rows
    per day; (4) anchor to the LIVE account_equity (anchor-to-today, reconstruct
    backward — the module's :26-32 convention) and produce flow-in-numerator
    returns r_t = (NAV_t − NAV_{t−1} − F_t)/NAV_{t−1}. RECOMMENDED composition:
    shape the per-day PnL into fetch_daily_pnl-style records (order_type
    'daily_pnl', the funding_rows_to_daily_pnl_records shape at :108-121), build
    the daily flows as dated ExternalFlow entries, and delegate to
    combine_realized_and_funding(records, [], account_balance=account_equity,
    external_flows=flows, open_unrealized_usd=account_equity − account_balance)
    so the anchor roll, flow numerator, DQ-01 guard set, uPnL-wedge flag
    (unrealized_pnl_in_anchor via the honest core, v1.8 FLOW-04) and gap_fill all
    come from the shared engine — a bespoke r_t loop is FORBIDDEN (Don't
    Hand-Roll). Direct chain_linked_twr (the sfox :356-361 pattern) is an
    acceptable alternative if record-shaping proves awkward, but the wedge flag
    and reconcile_flow_residual construction check must then be wired explicitly.
    Docstring: state the cashflow-separation identity, the ledger-COMPLETE
    0.0-fill rationale (contrast the sfox F2/F6 sampled-NAV prohibition), the
    realized-basis + uPnL-wedge convention (open_unrealized = equity − balance),
    and the [ASSUMED] offset/fold caveats. NEVER touch the two existing siblings.
    Then add the money-math oracle tests to test_mt5_deal_reconstruction.py.
    Canonical hand fixture (write this arithmetic IN the test comments, paper-derived):
    deals on 5 UTC days — day2 BUY close profit +500, commission −100 (day PnL
    +400); day3 no deals; day4 BALANCE +10_000 AND SELL profit +300; day5 SELL
    profit −200. Anchor equity 110_500, balance 110_500 (no open positions) ⇒
    initial = 110_500 − (400+0+300−200) − 10_000 = 100_000. NAV closes:
    100_000 / 100_400 / 100_400 / 110_700 / 110_500. Hand returns: 0.0040, 0.0,
    300/100_400, −200/110_700. Assert each as a literal (pytest.approx abs=1e-12
    against the exact fractions). Variants: withdrawal (−5_000, anchor 95_500),
    zero-cash-rotation (drop the BALANCE deal, anchor 100_700 ⇒ F=0). Also
    test_unknown_deal_type_raises at the combiner level (a CORRECTION row inside
    an otherwise-valid ledger kills the whole combine).
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_deal_reconstruction.py -x</automated>
  </verify>
  <done>combine_mt5_deal_ledger returns the sibling (returns, meta) shape; deposit/withdrawal/zero-rotation/flat-day/unknown-type tests green against hand literals; no bespoke r_t loop (flow correction rides the shared engine); existing sibling tests untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: √252-not-√365 mutation guard + Python crypto-registry guard + quantstats-detection guard</name>
  <files>analytics-service/tests/test_mt5_deal_reconstruction.py</files>
  <read_first>
    - analytics-service/tests/test_mt5_golden_fixtures.py:1-60 (the 252-basis + sample-std ddof=1 conventions and the shown-arithmetic oracle style), :357-412 (T1 hand-computed SQRT_252 KPI precedent)
    - analytics-service/services/metrics.py:32-46 (DEFAULT_PERIODS_PER_YEAR=252, periods_per_year_for_asset_class)
    - analytics-service/services/closed_sets.py:194-212 (CRYPTO_VENUES — the single-sourced Python √365 registry; mt5 must stay OUT)
  </read_first>
  <behavior>
    - test_annualizes_252_not_365: the reconstructed fixture returns, annualized at periods_per_year_for_asset_class("traditional") == 252, produce a volatility equal to the hand-derived literal std(returns, ddof=1) × √252 — AND that literal differs from the ×√365 value by the exact √(365/252) factor, so flipping MT5 onto the crypto clock turns the assert RED (Sharpe/vol flip is the mutation kill)
    - test_mt5_not_in_python_crypto_registry: "mt5" not in services.closed_sets.CRYPTO_VENUES (guards the DEFERRED unknown→crypto latent-bug class at the single MD-01 source — a future 'add mt5 to CRYPTO_VENUES' regression reddens here)
    - test_returns_series_unambiguous_for_quantstats: the canonical fixture series contains at least one strictly negative value and no value > 1.0, so quantstats 0.0.81 _prepare_returns can never mis-read it as prices (Pitfall 4 / the DEFERRED Sharpe sign-flip bug)
  </behavior>
  <action>
    Add the three guards to test_mt5_deal_reconstruction.py. For the mutation
    test: derive the volatility literal from the HAND return values of Task 2's
    canonical fixture (0.0040, 0.0, 300/100_400, −200/110_700) — sample std with
    ddof=1, arithmetic shown step-by-step in a comment exactly like
    test_mt5_golden_fixtures T1 (sqrt(252) = 15.8745078663877) — and assert BOTH
    vol_252 == literal AND vol_252 != literal × sqrt(365/252) (state in the
    comment: this second assert is what makes a silent clock flip RED). The
    literals derive from the hand returns, never from combine_mt5_deal_ledger or
    compute_all_metrics output. Compute the SUT value through
    services.metrics.compute_all_metrics with
    periods_per_year=periods_per_year_for_asset_class("traditional") so the test
    exercises the REAL KPI engine at the real clock selector, per D:
    "MT5 stamps asset_class='traditional' (√252)".
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_deal_reconstruction.py -x</automated>
  </verify>
  <done>All three guards green; the mutation test comment demonstrates the 365-clock value is a DIFFERENT literal; "mt5" absent from CRYPTO_VENUES asserted; full test file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker deal rows → combiner | untrusted broker-shaped dicts (types, amounts, timestamps) cross into money-math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-01 | Tampering | classify_deal | mitigate | allow-list classification; unknown/ambiguous type raises Mt5DealClassificationError (capital can never masquerade as performance) |
| T-136-02 | Tampering | deal_cash_effect / combiner inputs | mitigate | fail-loud float coercion rejects NaN/Inf/non-numeric (nav_twr._coerce_float discipline) — no silent-NaN 'complete' record |
| T-136-03 | Info disclosure | Mt5DealClassificationError messages | mitigate | raises carry type codes/counts only, never raw USD (nav_twr leak-safe raise convention) |
| T-136-04 | Spoofing | deposit-day return | mitigate | flow-in-numerator identity + hand-oracle test pins the economics (a deposit can never inflate the track record) |
| T-136-SC | Tampering | package installs | accept | NO packages installed this phase (RESEARCH Package Legitimacy Audit: N/A — in-repo composition only) |
</threat_model>

<verification>
cd analytics-service && pytest tests/test_mt5_deal_reconstruction.py -x — all green.
cd analytics-service && pytest tests/test_mt5_golden_fixtures.py tests/test_equity_reconstruction*.py -x — existing siblings untouched (local pandera gap: csv_validator-importing suites may need `pip install 'pandera==0.32.1' --break-system-packages`; CI has it).
grep -n "def combine_mt5_deal_ledger" analytics-service/services/broker_dailies.py — exists beside :174/:230.
</verification>

<success_criteria>
- combine_mt5_deal_ledger + mt5_deals.py exist, pure/fail-loud, sibling-shaped
- Deposit-day-not-a-spike, withdrawal, zero-cash-rotation, unknown-type, √252 mutation, CRYPTO_VENUES-exclusion, quantstats-guard tests all green against HAND literals
- No fixture value regenerated from the SUT (oracle discipline)
</success_criteria>

<output>
Create `.planning/phases/136-mt5recon-equity-reconstruction/136-01-SUMMARY.md` when done.
</output>
