---
phase: 136-mt5recon-equity-reconstruction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/closed-sets.ts
  - src/lib/closed-sets.test.ts
  - src/app/api/strategies/create-with-key/route.ts
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
autonomous: true
requirements: [MT5RECON-02]
must_haves:
  truths:
    - "isCryptoExchange('mt5') === false (and 'MT5' case-insensitively false) while the five crypto venues stay true"
    - "A key-created mt5 strategy carries asset_class='traditional' after create-with-key AND after finalize-wizard (the finalize force-derive can no longer flip it back to crypto)"
    - "annualizationPeriods('traditional') === 252 — so every #597 TS surface annualizes an MT5 strategy on √252"
  artifacts:
    - path: "src/lib/closed-sets.ts"
      provides: "explicit CRYPTO_EXCHANGES subset; isCryptoExchange narrowed off SUPPORTED_EXCHANGES membership"
      contains: "CRYPTO_EXCHANGES"
    - path: "src/app/api/strategies/create-with-key/route.ts"
      provides: "per-source asset_class stamp (traditional for mt5)"
    - path: "src/app/api/strategies/finalize-wizard/route.ts"
      provides: "venue-aware apiKeyId asset_class derive (no unconditional crypto)"
  key_links:
    - from: "src/app/api/strategies/create-with-key/route.ts"
      to: "src/lib/closed-sets.ts"
      via: "isCryptoExchange(exchange) drives the stamp"
      pattern: "isCryptoExchange"
    - from: "src/app/api/strategies/finalize-wizard/route.ts"
      to: "src/lib/closed-sets.ts"
      via: "isCryptoExchange over the key's exchange"
      pattern: "isCryptoExchange"
---

<objective>
Narrow the TS annualization surface so MT5 is TRADITIONAL √252 (MT5RECON-02):
`isCryptoExchange` gets an explicit crypto subset EXCLUDING `'mt5'`, and the two
routes that force-derive `asset_class:'crypto'` for key-backed strategies become
venue-aware. Closes the MT5-specific instance of the DEFERRED unknown→crypto
latent bug on the TS side (the Python registry `CRYPTO_VENUES` already excludes
mt5 — guarded by plan 136-01's registry test).

Purpose: an MT5 series that annualizes √365 inflates Sharpe ~×1.20 vs peers —
a trust-integrity defect on an allocator-facing ranking.
Output: narrowed closed-sets + two venue-aware routes + updated vitest suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-mt5recon-equity-reconstruction/136-CONTEXT.md
@src/lib/closed-sets.ts
@src/lib/closed-sets.test.ts

AGENTS.md rule: this is a MODIFIED Next.js — read the relevant guide under
node_modules/next/dist/docs/ before editing the two API routes.

<interfaces>
Current TS gate (closed-sets.ts:245-250) — membership in SUPPORTED_EXCHANGES IS
the crypto signal today; the docstring at :240-243 already anticipates this
narrowing:

```typescript
export const SUPPORTED_EXCHANGES = ["binance", "okx", "bybit", "deribit", "sfox", "mt5"] as const; // :39
export function isCryptoExchange(exchange: string | null | undefined): boolean {
  if (!exchange) return false;
  return (SUPPORTED_EXCHANGES as readonly string[]).includes(exchange.toLowerCase());
}
export function annualizationPeriods(assetClass: string | null | undefined): number {
  return assetClass === "crypto" ? 365 : 252; // :258 — needs NO edit once asset_class stamps correctly
}
```

The two hardcodes to fix:
- create-with-key/route.ts:354 — `.update({ asset_class: "crypto" })` unconditional (comment :337-341 says row default is NOT NULL 'traditional')
- finalize-wizard/route.ts:533-534 — `asset_class: apiKeyId || isCompositeForAssetClass ? "crypto" : fields.asset_class`
- composite/add-key/route.ts:329-330 is a COMMENT only (no force-derive there) — audit, no edit.

Known consumer: MetadataStep.tsx:107 `assetClassLocked = isCryptoExchange(detectedExchange)` —
mt5 becomes unlocked, which is acceptable (MT5 wizard UI is dark behind
NEXT_PUBLIC_MT5_ENABLED until Phase 138; the finalize venue-aware derive is the
authoritative stamp regardless of what the form shows). Do not edit it (Rule 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrow isCryptoExchange to an explicit CRYPTO_EXCHANGES subset</name>
  <files>src/lib/closed-sets.ts, src/lib/closed-sets.test.ts</files>
  <read_first>
    - src/lib/closed-sets.ts:21-56 (SUPPORTED_EXCHANGES + the decoupling doctrine), :228-285 (the #597 annualization block)
    - src/lib/closed-sets.test.ts:71-90 (the existing "every SUPPORTED exchange is crypto" test — MUST be rewritten, it now encodes the wrong invariant)
    - analytics-service/services/closed_sets.py:194-212 (CRYPTO_VENUES — the Python mirror the TS subset must match value-for-value)
  </read_first>
  <behavior>
    - isCryptoExchange('mt5') === false; isCryptoExchange('MT5') === false
    - isCryptoExchange(x) === true for each of binance/okx/bybit/deribit/sfox (case-insensitive)
    - isCryptoExchange('nyse'/null/undefined/'') === false (unchanged)
    - CRYPTO_EXCHANGES ⊂ SUPPORTED_EXCHANGES enforced at compile time (satisfies readonly SupportedExchange[])
    - a test asserts the TS subset equals Python CRYPTO_VENUES member-for-member as a literal (binance/okx/bybit/deribit/sfox) so the two registries cannot silently drift
  </behavior>
  <action>
    In closed-sets.ts add `export const CRYPTO_EXCHANGES = ["binance", "okx",
    "bybit", "deribit", "sfox"] as const satisfies readonly SupportedExchange[];`
    directly above isCryptoExchange, with a comment naming the mirror
    (analytics-service/services/closed_sets.py CRYPTO_VENUES — the single-sourced
    Python #597 registry) and citing MT5RECON-02 + the DEFERRED unknown→crypto
    latent bug this narrows against. Rewrite isCryptoExchange to test membership
    in CRYPTO_EXCHANGES (keep the null/case handling byte-identical) and update
    its docstring — the "when a non-crypto venue is ever added" future-note is
    NOW (mt5, per D: MT5 = forex/CFD = traditional √252). Rewrite the
    closed-sets.test.ts block at :71-90: the crypto five stay true, mt5/MT5
    false, plus the literal-mirror assertion. Do NOT touch UI_EXCHANGE_CODES /
    FUNDING_EXCHANGES / annualizationPeriods / blendPeriodsPerYear — asset_class
    is their signal and needs no edit (RESEARCH §5 site 1).
  </action>
  <verify>
    <automated>npx vitest run src/lib/closed-sets.test.ts</automated>
  </verify>
  <done>isCryptoExchange('mt5') false with the five crypto venues true; compile-time subset constraint holds (tsc clean); mirror-literal test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Venue-aware asset_class stamps in create-with-key and finalize-wizard</name>
  <files>src/app/api/strategies/create-with-key/route.ts, src/app/api/strategies/create-with-key/route.test.ts, src/app/api/strategies/finalize-wizard/route.ts, src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/create-with-key/route.ts:330-365 (the force-derive block + its non-blocking error handling)
    - src/app/api/strategies/finalize-wizard/route.ts:485-545 (the asset_class persist block, the F-1b √365-mismatch worker note at :508-514, and what venue/apiKey data is already in scope)
    - src/app/api/strategies/composite/add-key/route.ts:25-35, :325-335 (comment-only — confirm no code hardcode)
  </read_first>
  <behavior>
    - create-with-key with exchange 'mt5' → strategies.asset_class updated to 'traditional' (explicit stamp per D, not a skipped update); any crypto venue → 'crypto' byte-identical to today
    - finalize-wizard on a single-key draft whose api_key.exchange is 'mt5' → asset_class 'traditional' (the :534 force-derive no longer overwrites the create-with-key stamp back to crypto)
    - finalize-wizard composite case → 'crypto' UNCHANGED (every composite member venue today is crypto; an mt5 composite member fails loud at the stitch unknown-venue gate, job_worker.py:5203 — honest, out of 136 scope)
    - finalize-wizard CSV/manual case → fields.asset_class validated path UNCHANGED
  </behavior>
  <action>
    create-with-key :354: replace the unconditional crypto update with
    `asset_class: isCryptoExchange(exchange) ? "crypto" : "traditional"` (import
    from src/lib/closed-sets), updating the :337-341 comment ("every venue is
    crypto" is no longer true — cite MT5RECON-02). Keep the non-blocking error
    posture byte-identical. finalize-wizard :533-534: make the apiKeyId arm
    venue-aware — resolve the key's exchange (the route already owns/validates
    apiKeyId; reuse an existing owner-scoped api_keys read if one is in scope,
    else add a minimal `select exchange from api_keys where id = apiKeyId`
    owner-scoped read) and stamp `isCryptoExchange(exchange) ? "crypto" :
    "traditional"`; the isCompositeForAssetClass arm stays 'crypto' with a
    comment citing the stitch fail-loud gate for non-crypto members. Preserve the
    :508-514 F-1b rationale comment and extend it: the worker's composite clock
    cross-check reads Python CRYPTO_VENUES which already excludes mt5, so the two
    sides agree by construction. Add route tests: mt5 → traditional, bybit →
    crypto (regression), composite → crypto, and a wiring test proving the
    finalize path INVOKES the venue-aware derive (test the wiring, not just the
    helper — a neutered call site must fail the test).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/create-with-key/route.test.ts src/app/api/strategies/finalize-wizard/route.test.ts</automated>
  </verify>
  <done>mt5 single-key strategies stamp traditional at BOTH routes; crypto venues byte-identical; composite arm unchanged; wiring tests fail when the derive is neutered; npm run lint clean on touched files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → key routes | user-controlled exchange value drives the asset_class stamp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-05 | Tampering | isCryptoExchange / asset_class stamp | mitigate | closed-set allow-list (CRYPTO_EXCHANGES satisfies SupportedExchange[]); exchange already zod-validated at route entry against exchangeEnum |
| T-136-06 | Elevation (metric inflation) | MT5 √365 annualization | mitigate | traditional stamp + plan 136-01 mutation test; ranking-page Sharpe cannot inflate ×1.20 |
| T-136-07 | Repudiation | silent registry drift TS vs Python | mitigate | literal mirror test pins CRYPTO_EXCHANGES == CRYPTO_VENUES member-for-member |
| T-136-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
npx vitest run src/lib/closed-sets.test.ts src/app/api/strategies/create-with-key/route.test.ts src/app/api/strategies/finalize-wizard/route.test.ts — green.
npx tsc --noEmit — clean.
grep -n "asset_class" src/app/api/strategies/create-with-key/route.ts — no unconditional "crypto" literal remains in the update call.
</verification>

<success_criteria>
- isCryptoExchange excludes mt5; explicit CRYPTO_EXCHANGES subset mirrors Python CRYPTO_VENUES
- Both force-derive sites venue-aware; mt5 = traditional end-to-end at the TS layer
- Existing crypto-venue behavior byte-identical (regression tests prove it)
</success_criteria>

<output>
Create `.planning/phases/136-mt5recon-equity-reconstruction/136-02-SUMMARY.md` when done.
</output>
