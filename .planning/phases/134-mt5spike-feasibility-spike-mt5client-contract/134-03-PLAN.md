---
phase: 134-mt5spike-feasibility-spike-mt5client-contract
plan: 03
type: execute
wave: 3
depends_on: [134-01, 134-02]
files_modified:
  - analytics-service/requirements.in
  - analytics-service/requirements.txt
  - analytics-service/docs/mt5-spike-gonogo.md
autonomous: false
requirements: [MT5SPIKE-01, MT5GW-02]
user_setup:
  - service: mt5-broker-demo
    why: "The four MT5SPIKE-01 live proofs need a real broker demo/investor account as test substrate"
    env_vars:
      - name: MT5_SPIKE_LOGIN
        source: "Broker demo-account signup (account number)"
      - name: MT5_SPIKE_INVESTOR_PASSWORD
        source: "Broker account settings — the INVESTOR (read-only) password, not the master password"
      - name: MT5_SPIKE_SERVER
        source: "Broker — the EXACT MT5 server string (wrong server fails identically to a bad password)"
      - name: MT5_SPIKE_HOST
        source: "Address of the running gmag11 v2.3 gateway container (private network only)"
      - name: MT5_SPIKE_PORT
        source: "Gateway RPyC port — verify against the container (18812 constructor default vs 8001 common image map)"
    dashboard_config:
      - task: "Stand up gmag11/MetaTrader5-Docker v2.3 (amd64, ~4 GB, persistent /config); one-time VNC install + investor login at :3000"
        location: "Docker host of the founder's choosing — PRIVATE network only, never a public port"
must_haves:
  truths:
    - "mt5linux==1.0.3 and rpyc==5.2.3 are human-verified against their PyPI/source pages BEFORE any manifest/lockfile change — the [ASSUMED] supply-chain tags are resolved by a human, never auto-approved"
    - "mt5linux==1.0.3 lands in requirements.in and the uv-compiled requirements.txt lock (via make lock), pinning rpyc==5.2.3 transitively"
    - "The four MT5SPIKE-01 live legs are either executed by the founder with results recorded in docs/mt5-spike-gonogo.md, or explicitly recorded as human_needed in the SUMMARY — a skipped live gate is NEVER claimed done"
  artifacts:
    - path: analytics-service/requirements.in
      provides: "mt5linux==1.0.3 direct-dependency pin with rationale comment"
      contains: "mt5linux==1.0.3"
    - path: analytics-service/requirements.txt
      provides: "regenerated uv lock containing mt5linux==1.0.3 and rpyc==5.2.3"
      contains: "mt5linux==1.0.3"
  key_links:
    - from: analytics-service/requirements.in
      to: analytics-service/requirements.txt
      via: "make lock (uv pip compile --python-version 3.12 --universal) — never hand-edit the lock"
      pattern: "mt5linux"
    - from: analytics-service/docs/mt5-spike-gonogo.md
      to: "MT5SPIKE-01 verification status"
      via: "founder-filled leg verdicts (or standing human_needed placeholders)"
      pattern: "human_needed|GO"
---

<objective>
Execute the human-gated legs of Phase 134: (1) the supply-chain legitimacy gate on
`mt5linux==1.0.3` + `rpyc==5.2.3` (both [ASSUMED] — slopcheck was unavailable to
research; this checkpoint is NEVER auto-approvable), (2) landing the verified pin
in the uv manifest/lock, and (3) the founder's live four-leg spike run against a
real broker demo account + running gateway.

Purpose: MT5GW-02's transport package cannot silently enter the lockfile, and
MT5SPIKE-01's live proofs need founder substrate (demo credentials + gateway) that
does not exist in this run. This plan is where both gates live — explicitly,
blockingly, and honestly: if the founder cannot run the live legs this cycle, the
phase SUMMARY records them as human_needed, never as passed.

Output: verified dependency pin in requirements.in + requirements.txt; the live
go/no-go verdicts in analytics-service/docs/mt5-spike-gonogo.md (or standing
human_needed placeholders + a SUMMARY that says so).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-RESEARCH.md
@.planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-02-SUMMARY.md
@analytics-service/requirements.in
@analytics-service/docs/mt5-spike-gonogo.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Supply-chain legitimacy gate — mt5linux==1.0.3 + rpyc==5.2.3 ([ASSUMED], never auto-approvable)</name>
  <read_first>
    - .planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-RESEARCH.md — Package Legitimacy Audit (lines 109-121: mt5linux [ASSUMED] single-maintainer/low-download; rpyc [ASSUMED]-but-effectively-OK; wheel inspected pure-Python, no postinstall scripts) and the version-verification block (line 107)
  </read_first>
  <action>
    PAUSE and present the supply-chain evidence below to the human. Do NOT edit
    requirements.in, requirements.txt, or run any pip/uv install before explicit
    approval. This gate is blocking-human: workflow.auto_advance is IGNORED for
    package-legitimacy checkpoints. On rejection, stop the plan and surface the
    decision to the founder (alternative transport decision needed) — never work
    around the gate.
  </action>
  <what-built>
    Research verified from primary sources: mt5linux 1.0.3 (2026-02-20) is pure-Python
    (py3-none-any), 4 modules, NO setup/postinstall scripts (wheel inspected);
    requires_dist pins rpyc==5.2.3, plumbum==1.7.0, pyparsing, numpy; source repo
    github.com/lucas-campagna/mt5linux. But slopcheck could not run in the research
    session, so BOTH packages carry [ASSUMED] and this gate is blocking-human —
    workflow.auto_advance is IGNORED for legitimacy checkpoints.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/mt5linux/ — confirm latest is 1.0.3, release
       history reaches back to 0.1.0 (2022), and the project links to
       github.com/lucas-campagna/mt5linux (repo exists, code matches the wheel
       mechanics described in 134-RESEARCH.md: rpyc.classic.connect + conn.eval).
    2. Open https://pypi.org/project/rpyc/ — confirm 5.2.3 exists and the project
       is the long-lived tomerfiliba-org/rpyc (~15 years, mainstream).
    3. Check the CLAUDE.md Banned Packages table — neither package is listed.
    4. Note the standing risk: mt5linux is single-maintainer + low-download; the
       pin is EXACT (==1.0.3) so a future hijacked release cannot float in.
  </how-to-verify>
  <acceptance_criteria>
    - Human has confirmed both PyPI pages + the mt5linux source repo and typed approval
    - No [SLOP] indicators found (typosquat name, missing repo, postinstall hooks, fresh empty account)
    - Decision recorded in the eventual SUMMARY: approved / rejected with reason
  </acceptance_criteria>
  <verify>
    <automated>MISSING — human gate: approval is the verification; git diff must show zero dependency-file changes before the "approved" signal</automated>
  </verify>
  <done>Human typed "approved" (or rejection recorded + plan halted); no dependency-file change exists prior to the signal.</done>
  <resume-signal>Type "approved" to unblock Task 2, or describe the concern (rejection = the milestone needs an alternative transport decision — surface to the founder, do not work around)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Land the verified pin — requirements.in + make lock (uv-compiled requirements.txt)</name>
  <files>analytics-service/requirements.in, analytics-service/requirements.txt</files>
  <read_first>
    - analytics-service/requirements.in — full file (the human-edited SOURCE manifest; header documents the make-lock workflow: `cd analytics-service && make lock`, compile against Python 3.12 universal, NEVER the local venv; the ccxt entry is the rationale-comment convention to match)
  </read_first>
  <action>
    ONLY after Task 1 approval. Add to analytics-service/requirements.in a pinned
    entry `mt5linux==1.0.3` with a rationale comment in the file's established style:
    worker-side pure network client for the self-hosted MT5 gateway (v1.15
    MT5GW-02); pure-Python wheel, no postinstall (wheel inspected 2026-07-23);
    single-maintainer package — pinned EXACT and human-verified at the plan 134-03
    supply-chain gate; pins rpyc==5.2.3 transitively; imported LAZILY by
    services/mt5_client.py only (never at module import), so nothing else in the
    worker touches it until Phase 135/136.

    Then regenerate the lock: `cd analytics-service && make lock` (uv pip compile
    --python-version 3.12 --universal). NEVER hand-edit requirements.txt. Inspect
    the lock diff: the ONLY additions must be mt5linux==1.0.3 and its declared
    transitive set (rpyc==5.2.3, plumbum==1.7.0, pyparsing, and numpy if not
    already present) — any other package movement is a red flag: stop and surface
    it, do not commit a drifted lock.

    Then prove the offline gates still hold WITHOUT importing the new package:
    run the full analytics-service suite and re-assert the lazy-import guard
    (the test_module_import_does_not_require_mt5linux test from plan 134-01 must
    still pass — installing the package must not tempt a module-level import).
  </action>
  <verify>
    <automated>grep -q "^mt5linux==1.0.3" analytics-service/requirements.in && grep -q "mt5linux==1.0.3" analytics-service/requirements.txt && grep -q "rpyc==5.2.3" analytics-service/requirements.txt && cd analytics-service && pytest tests/test_mt5_client_contract.py tests/test_mt5_spike_harness.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^mt5linux==1.0.3" analytics-service/requirements.in` returns 1 (with rationale comment above it)
    - `grep -q "mt5linux==1.0.3" analytics-service/requirements.txt` and `grep -q "rpyc==5.2.3" analytics-service/requirements.txt` both succeed, and the lock header still shows the uv pip compile command (regenerated, not hand-edited)
    - lock diff contains ONLY mt5linux + its declared transitives (rpyc, plumbum, pyparsing, numpy-if-new)
    - `cd analytics-service && pytest -q` exits 0
  </acceptance_criteria>
  <done>mt5linux==1.0.3 pinned in the source manifest with rationale; uv lock regenerated cleanly; full suite green; lazy-import discipline intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Founder live spike run — the four MT5SPIKE-01 proofs (human_needed; a skipped gate is NEVER claimed done)</name>
  <read_first>
    - analytics-service/docs/mt5-spike-gonogo.md — the runbook + template the founder follows and fills
    - .planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-RESEARCH.md — Open Questions 1-4 (the live unknowns this run resolves) and Pitfall 6 (expected Wine-login failure modes)
  </read_first>
  <action>
    PAUSE and hand the live run to the founder. Do NOT simulate, stub, or
    fabricate any live-leg result. Present the runbook
    (analytics-service/docs/mt5-spike-gonogo.md) and wait. If the founder defers,
    record the four MT5SPIKE-01 live legs as human_needed in the SUMMARY and phase
    verification — a skipped live gate is NEVER claimed done.
  </action>
  <files>analytics-service/docs/mt5-spike-gonogo.md</files>
  <what-built>
    Everything automatable is done: the Mt5Client contract + offline CI gate
    (plan 134-01), the four-leg spike harness with offline-proven report/exit-code
    logic and structural no-trade-path guarantees (plan 134-02), the runbook +
    founder-fillable go/no-go template (plan 134-02), and the human-verified
    mt5linux pin (Tasks 1-2). What remains is exactly what Claude cannot do:
    provision a broker demo account, stand up the Wine gateway, and run the live
    legs.
  </what-built>
  <how-to-verify>
    Follow analytics-service/docs/mt5-spike-gonogo.md Runbook end-to-end:
    1. Obtain a broker demo account (login + INVESTOR password + exact server
       string; optionally the master password for the leg-2 comparison).
    2. Stand up gmag11/MetaTrader5-Docker v2.3 on an amd64 host with a persistent
       /config volume; one-time VNC install + investor login at :3000; keep the
       RPyC port PRIVATE-network-only (unauthenticated RCE channel — never public).
    3. Verify the actual RPyC port (18812 vs 8001) and install deps:
       `cd analytics-service && pip install -r requirements.txt` (brings
       mt5linux==1.0.3 from Task 2).
    4. Export the MT5_SPIKE_* env vars and run
       `cd analytics-service && python -m scripts.mt5_spike > /tmp/mt5_spike_report.json`;
       expect exit 0 and per-leg verdicts.
    5. Transcribe the results into docs/mt5-spike-gonogo.md, replacing the
       human_needed placeholders for: leg 1 unattended-login success rate + verdict,
       leg 2 observed order_check retcode/comment + trade_allowed (investor and, if
       available, master), leg 3 deal count/field presence/DEAL_TYPE_BALANCE/history
       depth, leg 4 confirmed server-time offset + DST note; sign the overall
       verdict. If leg 1 is NO-GO, record the native-Windows-VPS escape-hatch
       election — never paper over.
  </how-to-verify>
  <acceptance_criteria>
    - EITHER: docs/mt5-spike-gonogo.md has all four leg verdicts + overall verdict filled by the founder (no human_needed remaining in verdict cells) and the report ran exit-0
    - OR: the founder defers → the SUMMARY and phase verification record MT5SPIKE-01's four live legs as human_needed with this checkpoint as the standing gate — NOT passed, NOT skipped-silently
    - In no case does any artifact claim a live proof that did not run
  </acceptance_criteria>
  <verify>
    <automated>grep -c "human_needed" analytics-service/docs/mt5-spike-gonogo.md — 0 in verdict cells if founder ran the legs; otherwise SUMMARY must contain "human_needed" for MT5SPIKE-01 live legs</automated>
  </verify>
  <done>Either founder-recorded verdicts replace every human_needed verdict cell in docs/mt5-spike-gonogo.md, or the SUMMARY explicitly records the four live legs as human_needed with this checkpoint standing.</done>
  <resume-signal>Type "spike complete — verdicts recorded" (with the overall GO/NO-GO), or "defer — record human_needed" to close the phase with the live legs explicitly outstanding</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip/uv → PyPI | third-party package code enters the worker image — supply-chain surface |
| founder shell → RPyC bridge → broker | live credentials + an unauthenticated-RCE transport touch a real account |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-SC | Tampering | pip install mt5linux/rpyc | mitigate | blocking human checkpoint (Task 1) BEFORE any manifest change; exact ==1.0.3 pin; lock-diff inspection (only declared transitives may move); wheel already source-inspected (no postinstall); NEVER auto-approvable |
| T-134-10 | Elevation of Privilege | RPyC SlaveService during the live run | mitigate | runbook mandates private-network-only bridge; credentials env-only; the harness drives the no-trade-surface Mt5Client facade |
| T-134-11 | Information Disclosure | live-run report/transcription | mitigate | sanitize_evidence + assert_sanitized on the emitted report (plan 134-02); go/no-go doc records server string + verdicts, never passwords |
| T-134-12 | Repudiation (claimed-but-unrun proofs) | phase verification | mitigate | Task 3 acceptance forbids claiming unrun legs; human_needed placeholders survive until the founder fills them; SUMMARY must state the standing gate explicitly |
</threat_model>

<verification>
- Task 1 human approval recorded before any requirements change exists in the working tree
- `grep -q "^mt5linux==1.0.3" analytics-service/requirements.in && grep -q "rpyc==5.2.3" analytics-service/requirements.txt` succeeds
- `cd analytics-service && pytest -q` exits 0 after the lock lands
- MT5SPIKE-01 live-leg status is one of {founder-recorded verdicts in docs/mt5-spike-gonogo.md, explicit human_needed in the SUMMARY} — never silently passed
</verification>

<success_criteria>
- Supply-chain gate satisfied by a human; mt5linux==1.0.3 + rpyc==5.2.3 pinned in manifest + lock with a clean diff
- The four live proofs are either founder-verified with recorded verdicts (phase goal fully met) or explicitly standing as human_needed (phase closes with the gate honestly open)
</success_criteria>

<output>
Create `.planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-03-SUMMARY.md` when done
</output>
