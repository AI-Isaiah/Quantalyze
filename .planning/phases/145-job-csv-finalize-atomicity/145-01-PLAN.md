---
phase: 145-job-csv-finalize-atomicity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/test_csv_finalize_auth_guard.sql
  - .planning/phases/145-job-csv-finalize-atomicity/145-repro-harness.sql
  - .planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md
  - .planning/research/PITFALLS.md
  - .planning/research/SUMMARY.md
autonomous: true
requirements: [JOB-06]
tags: [supabase, sql-gate, plpgsql, reproduction, csv-finalize, security-definer]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "A CI gate exists that calls the REAL deployed `finalize_csv_strategy` with no `request.jwt.claims` and asserts SQLSTATE 42501 with the exact message `finalize_csv_strategy called without an auth session` — the guard's liveness is a CI fact, not a grep (D-01, D-02)"
    - "The same gate's Part B asserts 42501 on an identity mismatch (claims sub != p_user_id) — both halves of the guard are pinned"
    - "The gate was observed RED under neuter (claims satisfied -> expected raise absent) before being committed GREEN"
    - "A FRESH arm-2 grep executed in this plan (not copied from CONTEXT/RESEARCH) shows exactly the user-scoped call sites and zero service-role callers, recorded verbatim in 145-REPRODUCTION.md (D-01)"
    - "Arm 3's pytest run (from analytics-service/, python3) is recorded verbatim WITH the mock-level overclaim caveat stated in the artifact"
    - "145-REPRODUCTION.md states the split explicitly — the GUARD is live, the PATH is closed — and marks arm 4 PENDING with its pre-registered pass/fail oracle (D-02)"
    - "PITFALLS.md and SUMMARY.md no longer carry the stale ~792-820 anchors or the superseded 20260501055202 citation — corrected in the SAME commit as the artifact (D-04)"
  artifacts:
    - "supabase/tests/test_csv_finalize_auth_guard.sql — ungated Part A (no session) + Part B (wrong identity), claims idiom, per-part BEGIN/ROLLBACK, no psql backslash meta-commands"
    - ".planning/phases/145-job-csv-finalize-atomicity/145-repro-harness.sql — auth.uid() stub + minimal real-constraint replicas + loader for the REAL 20260728120000 function body, with an anti-vacuity assertion that the real body loaded"
    - ".planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md — arms 1-3 complete with pre-registered oracles; arm 4 + census sections PENDING for Plan 02"
  key_links:
    - "test_csv_finalize_auth_guard.sql <-> 20260728120000:225-228 — the gate pins the exact 42501 message string; if Plan 03's fold renames the function, the gate is re-pointed in Plan 03's same commit, never silently"
    - "145-REPRODUCTION.md <-> TODOS.md:818-820 — the artifact is what Plan 02 cites when closing the stale 42501 bullet; without it the closure is unevidenced"
    - "PITFALLS.md anchors <-> process_key.py:1119-1156 — a stale anchor is how the next reader re-derives a wrong premise from a green gate (D-04)"
---

<objective>
Execute the repo-side half of SC#1's reproduce-first mandate: make the CANNOT-REPRODUCE verdict
executable and committed, not re-derived. Author the arm-1 positive control as a permanent SQL CI
gate (the 42501 guard fires — proven on a throwaway Postgres against the REAL function body),
execute arms 2 (fresh call-site grep) and 3 (existing pytest gates, recorded verbatim), and commit
`145-REPRODUCTION.md` with arms 1-3 complete and arm 4 pre-registered, together with the PITFALLS/
SUMMARY anchor corrections in the same commit.

Purpose: SC#1 (JOB-06) — no budget is spent re-fixing a bug three mechanisms already closed, and
the artifact's positive control stops the negative result from being vacuous.
Output: the auth-guard gate, the reproduction harness, the draft artifact, the corrected research
anchors — one commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-job-csv-finalize-atomicity/145-CONTEXT.md
@.planning/phases/145-job-csv-finalize-atomicity/145-RESEARCH.md
@.planning/phases/145-job-csv-finalize-atomicity/145-PATTERNS.md
@supabase/migrations/20260728120000_csv_finalize_double_submit_idempotency.sql
@supabase/tests/test_csv_finalize_double_submit.sql
@supabase/tests/test_commit_scenario_batch_auth_input.sql
</context>

## Decision map (cited as D-NN below; source: 145-CONTEXT.md locked decisions + the two founder rulings, corrected at HEAD by 145-RESEARCH.md)

| ID | Locked decision honored in this plan |
|----|--------------------------------------|
| D-01 | The SC#1 verdict is CANNOT REPRODUCE and the plan's job is to make it executable and committed via the four arms — not to re-derive it. No 42501 fix is scoped. `X-User-Access-Token` forwarding is never re-added (it exists at route.ts:1324 / process_key.py:1135; a diff "adding" it is the PITFALLS warning sign) |
| D-02 | The artifact states the split explicitly: the GUARD is live, the PATH is closed. Arm 1 becomes a permanent CI gate so a future service-role caller reddens in CI instead of 42501-ing in production |
| D-04 | PITFALLS.md + SUMMARY.md anchor corrections (~792-820 -> :1119-1156; migration 20260501055202 -> 20260728120000) land in the SAME commit as the artifact |

Scope fence: this plan writes NO SC#2 code — no migration, no route change, no Python change. The
reproduction artifact must exist and be committed before any of that is authored (locked ordering;
Plans 03+ depend on it).

<tasks>

<task type="tracer">
  <name>Task 1: Author test_csv_finalize_auth_guard.sql (arm 1) + prove it on a throwaway Postgres against the REAL function body</name>
  <files>supabase/tests/test_csv_finalize_auth_guard.sql, .planning/phases/145-job-csv-finalize-atomicity/145-repro-harness.sql</files>
  <precondition>A local Postgres 16 is available (the 143/144 tracers used PGBIN=/opt/homebrew/opt/postgresql@16/bin with a throwaway cluster on a TMPDIR socket — see the usage block at the top of .planning/phases/144-job-wr-02-orphaned-running/144-throwaway-harness.sql); if psql/initdb are absent, HALT and escalate rather than substituting a grep-only "proof"</precondition>
  <reversibility rating="reversible">Repo-only test + planning artifacts; nothing touches any shared project</reversibility>
  <read_first>
    - 145-RESEARCH.md §4 arm 1 (the two parts, the claims idiom citations, the neuter-RED requirement) and §7 (gate landscape — this file is gate #1 and doubles as the SC#1 arm)
    - supabase/tests/test_csv_finalize_double_submit.sql:1-70 and the claims idiom at :119/:230/:285 (`PERFORM set_config('request.jwt.claims', <json with sub>, true)` to drive auth.uid(); set to NULL to clear)
    - supabase/tests/test_commit_scenario_batch_auth_input.sql:82-139 (the 42501 assertion shape: DO block, capture SQLSTATE/SQLERRM, `IF err_state <> '42501' THEN RAISE`)
    - supabase/migrations/20260728120000_csv_finalize_double_submit_idempotency.sql:196-311 (the LATEST function body — the harness loads THIS body, no other) and :225-234 (the two guards being pinned)
  </read_first>
  <action>
    (1) Write `.planning/phases/145-job-csv-finalize-atomicity/145-repro-harness.sql`: a throwaway-
    cluster harness in the 144-throwaway-harness register (usage block at top, FIDELITY LIMITS
    section — no RLS, no PostgREST, no real Supabase auth). Contents: (a) an `auth` schema stub with
    `auth.uid()` reading `nullif(current_setting('request.jwt.claims', true), '')::jsonb->>'sub'`
    cast to uuid (this is the property the claims idiom drives; cite that the real auth.uid() has
    the same contract); a minimal `auth.users` stub table; (b) minimal real-constraint replicas of
    `public.strategies` (the 5-value status CHECK copied verbatim from 20260716130000:58-61 with
    source cited; `wizard_session_id`; the partial unique index
    `strategies_user_wizard_session_source_uniq` copied verbatim from 20260728120000:167-169) and
    `public.strategy_verifications` (columns the INSERT at 20260728120000:299-305 names, FK with
    ON DELETE CASCADE); (c) a loader section that creates the REAL `finalize_csv_strategy` by
    copying the CREATE OR REPLACE FUNCTION statement from 20260728120000:196-311 VERBATIM (state
    the source range in a comment; any drift between harness copy and migration is a harness bug —
    fix the copy, never the migration); (d) an ANTI-VACUITY assertion immediately after the loader:
    `pg_get_functiondef` of the loaded function must contain the `wizard_session_id` token inside
    the INSERT fragment — proving the REAL body loaded, not a stub (the 143 extraction-vacuity
    lesson; 145-PATTERNS "Anti-Patterns" table).
    (2) Write `supabase/tests/test_csv_finalize_auth_guard.sql`. Header: what the gate is FOR (the
    guard's liveness as a CI fact per D-02 — a future service-role caller must redden here, not
    42501 in production), why the existing green gates cannot catch this, and the note that Plan 03
    re-points this file at the folded function in the same commit as any DROP. Structure: plain
    PL/pgSQL DO blocks, per-part `BEGIN; SET LOCAL lock_timeout = '5s'; ... ROLLBACK;`, UNGATED
    (no presence-check green-skip arm — the 144-§8 anti-pattern), no psql backslash meta-commands
    anywhere including comments, `gen_random_uuid()` everywhere, no fixed UUID literals.
      - Part A (no session): clear claims via `set_config('request.jwt.claims', NULL, true)`; call
        `public.finalize_csv_strategy(<uuid>, <uuid>, 'daily_returns', 'guard probe',
        'pending_review')` inside BEGIN/EXCEPTION capturing SQLSTATE and SQLERRM via GET STACKED
        DIAGNOSTICS; assert SQLSTATE = '42501' AND SQLERRM equals the exact message at
        20260728120000:226 (`finalize_csv_strategy called without an auth session`). FAIL = the
        call returns, or any other SQLSTATE/message — the failure message names the consequence
        (a service-role caller would silently work / the guard was weakened).
      - Part B (wrong identity): seed an auth.users row, set claims with its sub, call with a
        DIFFERENT `p_user_id`; assert 42501 with the mismatch message shape (20260728120000:230-234).
    (3) TRACER PROOF on a throwaway cluster (initdb into TMPDIR per the 144 usage block): apply the
    harness, run the gate with `psql -v ON_ERROR_STOP=1` — both parts must PASS (raise observed
    with both properties). Record the observed NOTICE output verbatim for 145-REPRODUCTION.md arm 1.
    (4) NEUTER-RED: run a VARIANT of Part A that sets valid claims matching p_user_id before the
    call (guard satisfied) — the gate must go RED with "expected raise absent". OBSERVE the actual
    red output, restore the real part, re-run to GREEN, record (neuter, observed message) for the
    SUMMARY and the artifact. A negative result is the easiest place to be vacuous; this
    observation is what stops it. Do NOT commit yet — the commit lands in Task 3 with the artifact
    and anchor corrections (D-04's same-commit requirement).
  </action>
  <verify>
    <automated>psql -h "${TMPDIR:-/tmp}/145-tracer" -U postgres -d q145 -v ON_ERROR_STOP=1 -f supabase/tests/test_csv_finalize_auth_guard.sql && grep -c "request.jwt.claims" supabase/tests/test_csv_finalize_auth_guard.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both parts pass on the harness cluster against the REAL loaded body; the anti-vacuity loader
      assertion passed first.
    - The neuter variant was observed RED with output captured; the real gate restored to GREEN.
    - The file contains no backslash meta-commands and no green-skip presence gate.
    - Nothing committed yet.
  </acceptance_criteria>
  <done>The 42501 guard's liveness is an executable, observed fact against the real deployed body — the positive control that makes SC#1's negative verdict non-vacuous (D-02).</done>
</task>

<task type="auto">
  <name>Task 2: Execute arms 2 + 3 fresh and author 145-REPRODUCTION.md (arms 1-3 complete, arm 4 pre-registered)</name>
  <files>.planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md</files>
  <read_first>
    - 145-RESEARCH.md §4 arms 2-3 (the exact commands and expected classifications) and the §4 preamble (pre-registered outcome)
    - 145-CONTEXT.md "What a reproduction ATTEMPT should actually consist of" (the four-arm design and the overclaim guard)
  </read_first>
  <action>
    (1) ARM 2 — negative control, executed FRESH (D-01; this document-quoting trap is RESEARCH
    Pitfall 9): run
    `grep -rn "finalize_csv_strategy" src/ analytics-service/ --include="*.ts" --include="*.py" | grep -v -e test -e __tests__ -e "\.types\.ts"`
    from the repo root. For EVERY hit, classify call site vs mention, and for each call site record
    which client it runs on (expected at HEAD: route.ts ~:1483 on the SSR cookie client from
    createClient(), and process_key.py ~:1150 on get_user_scoped_supabase — re-verify the line
    numbers live, do not inherit them). PASS = every call site user-scoped; FAIL = any call on
    get_supabase() (the lru_cache service-role singleton, db.py:70-77) or createAdminClient().
    Record the grep output VERBATIM.
    (2) ARM 3 — existing Python gates: `cd analytics-service && python3 -m pytest
    tests/test_process_key.py -k "csv_finalize" -q` (never from repo root — VCR cassettes miss and
    tests make LIVE broker calls; use python3). Record the tail verbatim. If collection errors on a
    missing pandera import, install per the known caveat (`pip install 'pandera==0.32.1'
    --break-system-packages`) and re-run.
    (3) Author `.planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md`:
      - Header: the claim under test (TODOS.md:818-820 verbatim), the pre-registered expected
        outcome (arm 1 GREEN = guard fires; arms 2/3/4 GREEN = no path reaches it => CANNOT
        REPRODUCE), and the split statement per D-02: the GUARD is live, the PATH is closed — a
        flat "not a bug" is wrong and invites someone to delete the guard.
      - Arm 1: gate file path, both observed passes, the observed neuter-RED record.
      - Arm 2: the verbatim grep output + per-hit classification table.
      - Arm 3: the verbatim pytest tail + the explicit caveat that these are mock-level and prove
        WIRING, not absence-of-42501 (the overclaim guard — state it in the artifact, not just here).
      - Arm 4 section: PENDING — executed by Plan 02 in the orchestrator session (Supabase MCP is
        stripped from subagents); pre-register its oracle verbatim: one live TEST finalize -> HTTP
        200 + UUID strategy_id, no 42501 in any layer, row-state baseline captured (doubles as the
        SC#3 before-state and the §3 Step B first run).
      - Census section: PENDING — the four CONTEXT queries, both projects, Plan 02.
      - Verdict line: "PENDING arm 4 — arms 1-3 consistent with CANNOT REPRODUCE". The final
        verdict is written by Plan 02 only after arm 4.
  </action>
  <verify>
    <automated>test -f .planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md && grep -c "PENDING" .planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md && grep -c "42501" .planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md && (cd analytics-service && python3 -m pytest tests/test_process_key.py -k "csv_finalize" -q --collect-only | tail -2)</automated>
  </verify>
  <acceptance_criteria>
    - Arm 2's grep output is verbatim in the artifact with every hit classified; zero service-role
      call sites found (if one IS found, STOP — the verdict premise is falsified; halt and escalate
      to the orchestrator rather than proceeding).
    - Arm 3's tail is verbatim with the wiring-only caveat stated inside the artifact.
    - The artifact contains the D-02 split statement and the pre-registered arm-4 oracle.
  </acceptance_criteria>
  <done>Arms 2 and 3 are executed facts recorded verbatim, and the artifact exists with the split statement — reproduction as execution, not paraphrase (D-01).</done>
</task>

<task type="auto">
  <name>Task 3: Correct the stale research anchors and commit gate + harness + artifact + corrections in ONE commit</name>
  <files>.planning/research/PITFALLS.md, .planning/research/SUMMARY.md</files>
  <read_first>
    - .planning/research/PITFALLS.md:198-210 and .planning/research/SUMMARY.md:20,82,161,199,225 (the stale anchors to correct — re-locate by grep, the line numbers themselves may have drifted)
  </read_first>
  <action>
    (1) In PITFALLS.md and SUMMARY.md, correct every citation of the csv-finalize token-forwarding
    branch from the stale "~792-820" range to `process_key.py:1119-1156`, and every citation of
    `20260501055202` as the home of the finalize guard to `20260728120000` (the LATEST definition;
    the original was superseded twice, via 20260716130500). Where a precise anchor cannot be made
    durable, delete the line-number anchor and keep the file-level citation (per D-04's
    "or delete those anchors" arm). Use Edit with scoped replacements; touch NOTHING else in these
    files (surgical-changes rule — these are shared research docs).
    (2) Grep both files afterward: zero remaining hits for `20260501055202` in a finalize-guard
    context and zero remaining `792` anchors for process_key forwarding.
    (3) COMMIT — ONE commit containing: supabase/tests/test_csv_finalize_auth_guard.sql,
    145-repro-harness.sql, 145-REPRODUCTION.md, and both corrected research docs (D-04's
    same-commit requirement). Message in repo convention, e.g.
    `docs(145-01): SC#1 reproduction arms 1-3 + 42501 auth-guard CI gate + anchor corrections`.
    Verify with `git show --stat` that all five files are in that single commit.
  </action>
  <verify>
    <automated>git show --stat HEAD | grep -c -e "145-REPRODUCTION" -e "test_csv_finalize_auth_guard" -e "PITFALLS" && grep -rn "20260501055202" .planning/research/PITFALLS.md .planning/research/SUMMARY.md | grep -c "finalize" | grep -qx "0" && echo anchors-clean</automated>
  </verify>
  <acceptance_criteria>
    - No stale finalize-guard citation of the superseded migration survives in either research doc;
      forwarding anchors point at :1119-1156 or are deleted.
    - git show --stat HEAD lists the gate, the harness, the artifact, and both research docs in ONE
      commit (D-04).
    - The diff touches only the anchor lines in the research docs — no drive-by edits.
  </acceptance_criteria>
  <done>The next reader of the research docs cannot re-derive the wrong premise, and the whole SC#1 repo-side evidence set is one atomic commit (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated caller -> finalize_csv_strategy | the SECDEF surface whose 42501 guards this plan pins as a CI fact |
| gate seeds -> shared TEST | once merged, the gate's parts run in CI against the shared TEST project — per-part BEGIN/ROLLBACK, no residue |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-01 | Elevation of privilege | future service-role caller of finalize | high | mitigate | Arm-1 gate pins both 42501 raises with the exact message; a future caller reds in CI (D-02) |
| T-145-02 | Repudiation | the CANNOT-REPRODUCE verdict itself | medium | mitigate | Positive control (arm 1) + fresh-grep discipline (arm 2) + verbatim outputs + observed neuter-RED — the negative result cannot be vacuous |
| T-145-03 | Tampering | research docs (stale anchors mislead a future fix) | medium | mitigate | D-04 corrections in the same commit as the artifact |
| T-145-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Auth-guard gate green on the harness cluster against the REAL loaded body; neuter-RED observed and recorded
- Arms 2 and 3 outputs verbatim in 145-REPRODUCTION.md with the wiring-only caveat and the D-02 split statement
- ONE commit carries gate + harness + artifact + both anchor corrections (git show --stat)
</verification>

<success_criteria>
- SC#1's repo-side arms (1-3) are executed and committed; arm 4 is pre-registered with its oracle for Plan 02.
- The arm-1 gate is a permanent CI gate (supabase/tests/ location law) that was observed to fail under neuter.
- Zero SC#2 code exists in the repo at the end of this plan (git diff shows no migration, no route.ts, no process_key.py hunks).
</success_criteria>

<output>
Create `.planning/phases/145-job-csv-finalize-atomicity/145-01-SUMMARY.md` when done, including the
observed neuter-RED record, the arm 2/3 verbatim outputs (or pointers into 145-REPRODUCTION.md), and
the exact commit hash carrying the five-file atomic commit.
</output>
