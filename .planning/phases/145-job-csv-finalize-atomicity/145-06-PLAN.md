---
phase: 145-job-csv-finalize-atomicity
plan: 06
type: execute
wave: 6
depends_on: [145-03, 145-04, 145-05]
files_modified:
  - .planning/phases/145-job-csv-finalize-atomicity/145-TERMINALIZE.md
  - .planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md
autonomous: false
requirements: [JOB-06]
tags: [supabase, live-verification, terminalize, human-gate, csv-finalize, deploy]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "The fold migration is applied to TEST via the Supabase MCP (never `supabase db push`) and exercised LIVE before any merge — the auto-apply-to-PROD trap is never entered blind"
    - "The three re-pointed/new SQL gates were observed RED on TEST pre-apply and GREEN post-apply — the designed RED->GREEN, seen, not assumed"
    - "A live TEST finalize through the NEW path returns 200 + UUID and its row state DIFFS CLEAN against Plan 02's arm-4 baseline — SC#3's 'happy path unchanged' is a measured before/after, not an assertion"
    - "Every row on the measured census list is human-reviewed and TERMINALIZED (strategy_analytics 'failed' + reason, THEN status='archived') — UPDATE-only, the row survives, deletion never happens (D-05, D-13)"
    - "A zero census produces a recorded no-op artifact, not a skipped task (the 143/144 budget-saving register)"
    - "A blocking human checkpoint reviews the full evidence set BEFORE the one-way merge; 143's and 144's migrations show zero hunks in the phase diff (D-10)"
  artifacts:
    - ".planning/phases/145-job-csv-finalize-atomicity/145-TERMINALIZE.md — the reviewed id list with per-row disposition (terminalized / excluded-as-first-hop / no-op), and the exact UPDATE statements run"
    - ".planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md — appended SC#3 after-state diff vs the arm-4 baseline"
  key_links:
    - "TEST apply <-> merge: the merge to main auto-applies to PROD; the live TEST exercise is the only rehearsal — human gate sits between them"
    - "terminalize 'archived' <-> 143's sweep predicate (20260816140000:722 status <> 'archived') and the census predicate — an archived orphan leaves every candidate set without being deleted (D-13)"
    - "terminalize reason string <-> strategy_analytics.computation_error — the 'with a reason' half of founder ruling 1; the SyncProgress poller surface renders it (D-13)"
---

<objective>
The live half: apply the fold to TEST via the Supabase MCP and watch the gates go RED->GREEN,
exercise the new path live and diff the happy-path row state against the measured arm-4 baseline
(SC#3), run the one-time human-reviewed TERMINALIZE over the census list (founder ruling 1), and
hold the blocking human gate before the one-way merge.

Purpose: closes SC#2 at the deployed surface and SC#3 by measurement; discharges the retrospective
half of the founder's TERMINALIZE ruling; nothing reaches PROD unrehearsed or unreviewed.
Output: applied-and-exercised TEST state, 145-TERMINALIZE.md, the SC#3 diff, the approved gate.

⛔ ORCHESTRATOR-SESSION-ONLY, BLOCKING: every task needs the Supabase MCP or a human response.
Do not delegate any task in this plan to a subagent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-job-csv-finalize-atomicity/145-CONTEXT.md
@.planning/phases/145-job-csv-finalize-atomicity/145-RESEARCH.md
@.planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md
@.planning/phases/145-job-csv-finalize-atomicity/145-DECISION.md
</context>

## Decision map (cited as D-NN below)

| ID | Locked decision honored in this plan |
|----|--------------------------------------|
| D-05 | Founder ruling 1: TERMINALIZE, reading (β) — "no orphan row" means no UNEXPLAINED row nothing owns; deletion arms are OUT OF SCOPE; the row survives for audit |
| D-13 | Terminalize shape: strategy_analytics upsert computation_status='failed' + computation_error naming the orphan reason, THEN strategies.status='archived' (the W-4 semantics — archived is terminal/hidden with settled UI meaning). NO new sweep: the A/B/C shape is indistinguishable from the wizard first-hop drop; HUMAN review of a finite measured list is the distinguishing signal (RESEARCH §2). One-time, orchestrator session, id list committed |
| D-10 | 20260816140000 and 20260817120000 untouched — verified by diff before the gate |

## Pre-registered facts the executor relies on (RESEARCH §2, verified at HEAD)

- `strategies.status` is a CLOSED 5-value CHECK (draft, pending_review, published, archived,
  private) — there is NO failed/abandoned value and none is invented (the 144 user_message lesson).
- All three triggers on `strategies` pass an archived-flip: the sentinel guard (name-pattern only),
  the publish-transition guard (blocks INTO 'published' only), the team-review mark guard
  (column-targeted on capital_ownership).
- Nothing collects an archived strategy row, and nothing needs to — the surviving archived row IS
  reading (β)'s desired end-state; say so in the artifact rather than implying a collector exists.

<tasks>

<task type="auto">
  <name>Task 1: Apply to TEST via MCP, observe the gates RED->GREEN, exercise the new path live, diff SC#3</name>
  <files>.planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md</files>
  <precondition>Plans 03-05 are merged into the phase branch at HEAD; the Supabase MCP is available with TEST project access (qmnijlgmdhviwzwfyzlc); Plan 02's arm-4 baseline (row states + the two minted TEST strategy ids) is present in 145-REPRODUCTION.md</precondition>
  <reversibility rating="costly">TEST-only in this task, but the apply DROPs two deployed functions on TEST; the migration header's manual-rollback recipe (recreate both parents + drop the fold + re-grant) is the tested escape hatch — read it before applying</reversibility>
  <read_first>
    - The fold migration file's header (rollback recipe, gate list) and 145-RESEARCH.md Runtime State Inventory (the live-DB objects row)
    - 145-RESEARCH.md §3 Step B / §4 arm 4 (the authed-idiom finalize and the five baseline queries — the SAME queries produce the after-state)
  </read_first>
  <action>
    (1) PRE-APPLY RED: run the three phase SQL gates against TEST
    (`psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_csv_finalize_atomic_fold.sql`
    etc., or the CI sql-tests job) and OBSERVE the structural parts red (the fold does not exist on
    TEST yet) — record the actual output. This is the designed RED half.
    (2) APPLY the fold migration to TEST via the Supabase MCP `apply_migration`, passing the
    file's OWN 14-digit version string and name so the migration-history table matches the repo
    file (the MCP's default now()-stamp drift is the known PR-Y2 class — reconcile explicitly).
    ⛔ Never `supabase db push`. ⛔ PROD is touched by NOTHING in this plan except Task 2's
    reviewed terminalize UPDATEs; the migration reaches PROD only via the post-gate merge.
    (3) POST-APPLY GREEN: re-run all the phase SQL gates plus test_csv_finalize_double_submit.sql
    and test_wizard_session_idempotency.sql against TEST — all green; record verbatim tails.
    (4) LIVE EXERCISE: one manager-path finalize on TEST through the deployed app path with a fresh
    session and 10 rows (the Plan 02 idiom) -> 200 + UUID; run the five baseline queries against
    the new strategy and DIFF field-by-field against Plan 02's arm-4 baseline: identical shapes for
    strategies (status, source, wizard_session_id), strategy_verifications (status, trust_tier,
    flow_type, source), dailies count == row count submitted, compute_jobs kind enqueued,
    strategy_analytics state. Append the diff table to 145-REPRODUCTION.md as the SC#3 after-state.
    Any field-level divergence not explained by the fold's documented contract = STOP, do not
    proceed to the gate, escalate. Also archive (status='archived') the throwaway TEST strategies
    minted here and in Plan 02, listing their ids in the artifact — never delete (D-05 register).
    (5) Commit the artifact update: `docs(145-06): TEST apply RED->GREEN + SC#3 measured diff`.
  </action>
  <verify>
    <automated>psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_csv_finalize_atomic_fold.sql && psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_csv_finalize_double_submit.sql && grep -c "SC#3" .planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md</automated>
  </verify>
  <acceptance_criteria>
    - Pre-apply RED and post-apply GREEN both OBSERVED and recorded verbatim (never trust a
      fixer's green — the RED half proves the gates bind to the apply).
    - Migration-history version on TEST matches the repo filename's 14-digit prefix.
    - The SC#3 diff table exists with zero unexplained divergences; throwaway strategies archived
      and listed.
  </acceptance_criteria>
  <done>The deployed TEST surface runs the fold end-to-end, the gates bind to it, and SC#3 is a measured fact (ROADMAP SC#3)</done>
</task>

<task type="auto">
  <name>Task 2: One-time human-reviewed TERMINALIZE of the measured census list (both projects)</name>
  <files>.planning/phases/145-job-csv-finalize-atomicity/145-TERMINALIZE.md</files>
  <precondition>Plan 02's census section exists in 145-REPRODUCTION.md; the Supabase MCP has access to BOTH projects — PROD writes in this task are the reviewed terminalize UPDATEs ONLY</precondition>
  <reversibility rating="costly">UPDATE-only writes to live user-named rows on PROD; each is reversible by restoring the recorded prior status, which the artifact captures per-row before writing</reversibility>
  <read_first>
    - 145-RESEARCH.md §2 IN FULL (the status vocabulary, the trigger clearances, the no-sweep rationale, the human-review-is-the-signal argument) and 145-CONTEXT.md's founder ruling 1 paragraph
  </read_first>
  <action>
    (1) RE-RUN census queries (1) and (2) fresh on BOTH projects (the Plan 02 list is days old —
    a stale id list is the ledger-claims trap). Record the fresh per-row output.
    (2) HUMAN-REVIEW each candidate row against the distinguishing signals (this review is the
    "different signal" that replaces the impossible predicate — RESEARCH §2 point 3): source='csv'
    AND wizard_session_id present AND a strategy_verifications row present => CSV-path orphan,
    TERMINALIZE; anything else (wizard first-hop shape) => EXCLUDE and note. Record per-row
    disposition BEFORE writing anything, including each row's current status (the rollback anchor).
    (3) For each confirmed id, in order (D-13): upsert strategy_analytics with
    computation_status='failed' and a computation_error naming the reason (fixed literal, e.g.
    csv-finalize orphan reaped by Phase 145 one-time pass — no user data in the string), THEN
    UPDATE strategies SET status='archived' WHERE id = <id> AND status <> 'archived'. UPDATE-only;
    zero DELETE statements in this task under any circumstance (D-05). Verify each write by
    re-selecting the row.
    (4) If the fresh census is ZERO on both projects: write 145-TERMINALIZE.md as the recorded
    no-op (the queries, the zero results, the statement that the fold makes the class unreachable
    prospectively) — the 143/144 budget-saving register, an artifact either way.
    (5) Commit: `docs(145-06): one-time census terminalize — <N> rows / no-op`.
  </action>
  <verify>
    <automated>test -f .planning/phases/145-job-csv-finalize-atomicity/145-TERMINALIZE.md && grep -c -e "archived" -e "no-op" .planning/phases/145-job-csv-finalize-atomicity/145-TERMINALIZE.md && grep -c "DELETE" .planning/phases/145-job-csv-finalize-atomicity/145-TERMINALIZE.md | grep -qx "0" && echo update-only</automated>
  </verify>
  <acceptance_criteria>
    - Fresh census recorded; every candidate has a per-row disposition with its prior status
      captured; excluded rows carry the first-hop rationale.
    - Every terminalized row re-selected showing analytics 'failed'+reason and status='archived';
      zero removal statements anywhere in the pass (count-asserted in the artifact).
    - Zero census => the no-op artifact exists with the queries and results.
  </acceptance_criteria>
  <done>Every measured orphan is EXPLAINED — a real outcome on the analytics surface, an audit-surviving archived row — and the unmeasured classes were not touched (D-05, D-13)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human gate — review the full phase evidence set before the one-way merge</name>
  <what-built>Phase 145 complete on the branch: SC#1's committed CANNOT-REPRODUCE verdict (four arms + census); the folded SECURITY DEFINER transaction applied and exercised on TEST (gates RED->GREEN); the caller wiring per the founder's recorded (i-a)/(i-b) decision; three honest copy sentences + two new Sentry captures; the vacuous test replaced by gates observed RED; the one-time terminalize artifact; three TODOS deferrals filed</what-built>
  <how-to-verify>
    1. Read 145-REPRODUCTION.md — the verdict line, the arm-4 result, the SC#3 before/after diff table (zero unexplained divergences).
    2. Read 145-TERMINALIZE.md — per-row dispositions, UPDATE-only, or the recorded no-op.
    3. Read 145-DECISION.md — confirm the wiring matches your recorded (i-a)/(i-b) choice.
    4. Spot-check the neuter-RED tables in the 03/04/05 SUMMARYs — every gate observed to fail.
    5. Run `git diff main...HEAD -- supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql` — MUST be empty (D-10; Phase 143/144 untouched).
    6. Optionally: one live TEST finalize via the wizard UI — expect normal success; then a duplicate submit of the same session — expect the idempotent 200 echo, not a new row.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to /gsd-ship (the merge auto-applies the migration to PROD — this gate is the last stop before that one-way step), or describe issues to route back to a fix round</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> TEST DB | migration apply + live exercise; rehearsal for the PROD auto-apply |
| orchestrator -> PROD DB | terminalize UPDATEs only, human-reviewed per-row, prior state recorded |
| human gate -> merge | the one-way PROD apply happens only after explicit approval |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-20 | Denial of service | wrong row terminalized (first-hop misclassified) | high | mitigate | Human review per-row with the three-signal test; prior status recorded; archived is reversible by UPDATE |
| T-145-21 | Tampering | PROD auto-apply of an unrehearsed migration | critical | mitigate | TEST apply + RED->GREEN + live exercise + SC#3 diff + blocking human gate, all before merge |
| T-145-22 | Repudiation | terminalize with no audit trail | high | mitigate | 145-TERMINALIZE.md commits queries, per-row prior state, dispositions, and the exact statements run |
| T-145-23 | Tampering | migration-history drift (MCP now()-stamp) | medium | mitigate | apply_migration invoked with the file's own version string; history row verified against the filename prefix |
| T-145-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase |
</threat_model>

<verification>
- Gates observed RED pre-apply and GREEN post-apply on TEST; migration-history version matches the file
- SC#3 diff table appended with zero unexplained divergences; throwaway strategies archived
- Terminalize artifact complete (rows or no-op), UPDATE-only, per-row prior state captured
- D-10 diff check empty; human approval recorded before any merge
</verification>

<success_criteria>
- ROADMAP SC#2 closed at the deployed surface: the atomicity oracle passes against TEST's deployed fold; the residual measured population is terminalized-and-explained (D-05, D-13).
- ROADMAP SC#3 closed by measurement: the before/after diff shows the happy path unchanged, including the CONTRIB-02 'private' contract (pinned by the applied SQL gate + green route tests).
- The human gate stands between everything above and the one-way merge.
</success_criteria>

<output>
Create `.planning/phases/145-job-csv-finalize-atomicity/145-06-SUMMARY.md` when done, including the
RED->GREEN records, the SC#3 diff, the terminalize outcome, and the approval record.
</output>
