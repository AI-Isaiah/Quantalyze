---
phase: 145-job-csv-finalize-atomicity
plan: 03
type: execute
wave: 3
depends_on: [145-01, 145-02]
files_modified:
  - supabase/migrations/20260819120000_csv_finalize_atomic_fold.sql
  - supabase/tests/test_csv_finalize_double_submit.sql
  - supabase/tests/test_csv_finalize_auth_guard.sql
  - supabase/tests/test_csv_finalize_atomic_fold.sql
  - .planning/phases/145-job-csv-finalize-atomicity/145-fold-harness.sql
autonomous: true
requirements: [JOB-06]
tags: [supabase, migration, plpgsql, security-definer, atomicity, sql-gate, csv-finalize]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "ONE SECURITY DEFINER function `finalize_csv_strategy_with_returns` writes strategies + strategy_verifications + csv_daily_returns in ONE transaction, with NO exception-handler clause anywhere in its body — an error at any point rolls back all three writes (D-07)"
    - "A payload engineered to fail AFTER the strategies INSERT (malformed date element, bypassing the route validator by calling the RPC directly) leaves ZERO rows in all three tables — the SC#2 atomicity oracle, EXECUTED against the deployed body, not grepped"
    - "A 23505 from strategies_user_wizard_session_source_uniq aborts the entire folded body — the double-submit gate's rollback part is widened from two tables to THREE"
    - "`p_terminal_status` survives verbatim: a 'private' call writes strategies.status='private'; the whitelist guard raises 22023 first, before any write (D-08, SC#3)"
    - "An EMPTY p_rows array with fmt='trades' succeeds with zero dailies (the adapted guard — RESEARCH Pitfall 2); >5000 rows raises 22023 (the cap survives verbatim)"
    - "Every guard in RESEARCH §1's guard-survival table is present in the fold in the documented order; the probe-oracle guard's dissolution is argued in the migration header, not silently dropped"
    - "The two old RPCs are DROPped and every gate naming them is re-pointed IN THE SAME COMMIT as the migration (the 144-§8/B1 class); grants are re-issued on the new function (DROP loses ACLs) — authenticated only, never service_role (20260522111839:200-208)"
    - "The dailies INSERT is written against the 20260624120000 table shape: names exactly (strategy_id, date, daily_return), leaving api_key_id/allocator_id NULL to satisfy the XOR CHECK (RESEARCH Pitfall 3)"
    - "20260816140000 (143's sweep) and 20260817120000 (144's terminalizer) are untouched — zero hunks (D-10)"
  artifacts:
    - "supabase/migrations/20260819120000_csv_finalize_atomic_fold.sql — the fold + DROPs + grants + COMMENT ON FUNCTION ERRCODE map + fragment-scoped self-verify (filename re-verified strictly greater than the repo tip at execution time)"
    - "supabase/tests/test_csv_finalize_atomic_fold.sql — atomicity oracle, 'private' part, trades-empty part, cap part"
    - "supabase/tests/test_csv_finalize_double_submit.sql — re-pointed at the fold; Part 3 widened to three tables; header prose updated (no stale rationale survives)"
    - "supabase/tests/test_csv_finalize_auth_guard.sql — re-pointed at the fold's two 42501 raises + a structural assertion that the two old function names are gone from pg_proc"
    - ".planning/phases/145-job-csv-finalize-atomicity/145-fold-harness.sql — extends the Plan 01 harness with the 20260624120000-shape csv_daily_returns replica and both old-function loaders"
  key_links:
    - "no-exception-handler property <-> the 23505 rollback guarantee — 20260728120000:80-87 states it at length; the fold inherits it and extends it to three writes; the self-verify pins it (D-07)"
    - "DROP of the old finalize <-> test_csv_finalize_double_submit.sql / test_csv_finalize_auth_guard.sql — both call it BY NAME and red the moment it drops; same-commit re-point (RESEARCH Pitfall 4)"
    - "p_terminal_status DEFAULT 'pending_review' <-> route.ts:1488's explicit 'private' — losing the argument silently promotes private contributions into the admin publish queue (D-08; RESEARCH Pitfall 5)"
    - "fold INSERT writes wizard_session_id <-> strategies_user_wizard_session_source_uniq — without the write, the partial index never bites and the 23505 idempotency story collapses (SEAMRIM-03)"
---

<objective>
Author the SC#2 mechanism: fold `finalize_csv_strategy` + `persist_csv_daily_returns` into ONE
SECURITY DEFINER transaction (`finalize_csv_strategy_with_returns`), re-based on the LATEST parent
bodies (20260728120000:196-311 and 20260522111839:111-186) and written against the 20260624120000
csv_daily_returns shape. Prove atomicity end-to-end on a throwaway Postgres (the tracer), DROP the
two old RPCs with every gate re-pointed in the same commit, and run the SQL neuter-RED matrix.

The migration is CALLER-AGNOSTIC: it is identical under (i-a) and (i-b), so it does not wait on
re-reading the decision — but it DOES wait on Plan 02 (the committed reproduction verdict precedes
any SC#2 code, and the census re-rank is pre-registered).

Purpose: windows B and C stop existing; window A downgrades to window D, which Phase 143's shipped
sweep already heals — SC#2's "no orphan strategy row" becomes true by construction, prospectively.
Output: the migration, three gate files (one new, two re-pointed), the extended harness, the
neuter-RED table — migration + gates in ONE commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-job-csv-finalize-atomicity/145-CONTEXT.md
@.planning/phases/145-job-csv-finalize-atomicity/145-RESEARCH.md
@.planning/phases/145-job-csv-finalize-atomicity/145-PATTERNS.md
@.planning/phases/145-job-csv-finalize-atomicity/145-DECISION.md
@supabase/migrations/20260728120000_csv_finalize_double_submit_idempotency.sql
@supabase/migrations/20260522111839_csv_daily_returns.sql
@supabase/migrations/20260624120000_csv_daily_returns_per_key_axis.sql
@supabase/tests/test_csv_finalize_double_submit.sql
@.planning/phases/145-job-csv-finalize-atomicity/145-repro-harness.sql
</context>

## Decision map (cited as D-NN below)

| ID | Locked decision honored in this plan |
|----|--------------------------------------|
| D-07 | Mechanism = option (i): the folded SECURITY DEFINER transaction — it dissolves windows A/B/C rather than cleaning up after them; what remains is window D, healed by 143 |
| D-08 | `p_terminal_status` survives verbatim in the folded function; its whitelist guard raises FIRST, before any write |
| D-10 | 20260816140000 is untouched — not its predicate, not its conjunct order, not its gates. 20260817120000 (Phase 144, mid-flight) equally untouched |
| D-02 | The auth-guard gate is EXTENDED to the fold in the same commit as the DROP — the arm-1 gate's purpose (a future service-role caller reds in CI) transfers to the new surface |

## Blockers this plan closes

| ID | Blocker | Where |
|----|---------|-------|
| B1 | `test_csv_finalize_double_submit.sql` calls `finalize_csv_strategy` by name — `sql-tests` reds on a correct migration the moment the 5-arg function drops (144-§8's exact failure mode) | Task 2 re-points it in the SAME commit as the migration |
| B2 | `test_csv_finalize_auth_guard.sql` (Plan 01) also calls the old function by name | Task 2 re-points it at the fold + adds the old-names-gone structural assertion |
| B3 | The `20260624120000` restructure (surrogate PK, nullable strategy_id, XOR CHECK) is carried by NO prior phase document — a fold written against the old shape fails the XOR at runtime | Task 1 writes the INSERT against the restructured shape and STEP 0 asserts that shape exists |

<tasks>

<task type="tracer">
  <name>Task 1: Author the fold migration + prove atomicity end-to-end on a throwaway Postgres</name>
  <files>supabase/migrations/20260819120000_csv_finalize_atomic_fold.sql, .planning/phases/145-job-csv-finalize-atomicity/145-fold-harness.sql</files>
  <precondition>145-DECISION.md exists and 145-REPRODUCTION.md carries the final verdict (Plan 02 complete — the locked SC#1-before-SC#2 ordering); a local Postgres 16 is available per the Plan 01 precondition; if either is absent, HALT</precondition>
  <reversibility rating="costly">Repo-only in this plan, but the file will auto-apply to PROD on merge and DROPs two deployed functions — the header MUST carry a manual-rollback recipe (recreate both parents verbatim from 20260728120000 / 20260522111839 + drop the fold + re-grant), and Plan 06 gates the merge behind a human checkpoint</reversibility>
  <read_first>
    - 145-RESEARCH.md §1 IN FULL (re-base table, guard-survival table with per-guard disposition, the 5000-cap note, the changed retry story, the two-callers table) and the Code Examples skeleton (shape only — re-verify every literal against the cited parent lines before writing)
    - 145-PATTERNS.md §1 (header/STEP structure, guard ordering, GRANT discipline incl. the DROP-loses-ACLs rule at 20260601120000:32, the fragment-scoped self-verify idiom at 20260728120000:327-425, the anti-pattern table)
    - supabase/migrations/20260624120000_csv_daily_returns_per_key_axis.sql:9-57 and :113-118 (the table shape the INSERT must satisfy: surrogate PK, nullable strategy_id, unique (strategy_id,date) index, XOR CHECK, owner-coherence trigger gated on api_key_id)
  </read_first>
  <action>
    (0) A6 PRE-DROP GREP (RESEARCH Assumptions A6): fresh repo-wide grep for BOTH old function
    names — `grep -rn -e "finalize_csv_strategy" -e "persist_csv_daily_returns" --include="*.sql" --include="*.ts" --include="*.py" supabase/ src/ analytics-service/ schema/ 2>/dev/null` —
    and disposition EVERY hit in a table for the SUMMARY: call site (re-pointed by Plan 04),
    SQL-gate reference (re-pointed by Task 2, same commit), comment/docstring (updated or noted).
    An undispositioned hit blocks the DROP.
    (1) Verify the repo tip with `ls supabase/migrations/ | tail -3` and write the migration with a
    14-digit prefix STRICTLY greater than the tip (recommended `20260819120000_csv_finalize_atomic_fold.sql`;
    bump the date if Phase 144 follow-ups took it — never backdate; the 144 TS gate class pins tips).
    (2) HEADER, in the 20260728120000 long-form register: why the migration exists (the five-hop
    map, windows B/C dissolving, window A downgrading to D which 20260816140000 heals — cited, not
    modified, per D-10); the RE-BASE ENUMERATION per house law (20260728120000:186-195): parent 1 =
    20260728120000:196-311 (finalize, LATEST), parent 2 = 20260522111839:111-186 (persist, ONLY
    definition), with the EXACT delta list — guards merged in order, persist's redundant auth pair
    dropped (one copy suffices), the probe-oracle guard DISSOLVES (the fold creates the strategy in
    the same transaction; there is no caller-supplied strategy id to probe — state this so the
    missing guard is not read as a regression), the empty-array raise ADAPTED to `IF
    jsonb_array_length(p_rows) > 0 THEN <insert> END IF` (the trades no-series case, today's
    route.ts:521 skip moved into the fold), the upsert clause REPLACED by a plain INSERT (a freshly
    minted id cannot conflict; duplicate dates are a route-boundary 400 at route.ts:233-241; the
    header states the retry story changed so nobody cites an upsert as the retry mechanism); the
    ">>> READ THIS BEFORE SIMPLIFYING <<<" callout for the no-handler-clause property (the 23505
    rollback guarantee for all THREE writes depends on its absence — 20260728120000:80-87 extended);
    the manual-rollback recipe; the gate files named; the PROD-AUTO-APPLY warning; the ERRCODE-map
    comment block extended from 20260522111839:104-110 (22023 / 42501 / 23505 -> downstream TS
    meaning).
    (3) BODY — `BEGIN; SET lock_timeout = '3s';` frame. STEP 0 preflight DO block (fail-loud):
    both old functions exist at expected arity in pg_proc; csv_daily_returns has the restructured
    shape (surrogate id column present; the XOR constraint name `csv_daily_returns_source_xor`
    present) — with the honest what-this-cannot-catch note. STEP 1 creates
    `public.finalize_csv_strategy_with_returns(p_user_id UUID, p_wizard_session_id UUID, p_fmt
    TEXT, p_strategy_name TEXT, p_rows JSONB, p_terminal_status TEXT DEFAULT 'pending_review')
    RETURNS UUID, LANGUAGE plpgsql SECURITY DEFINER, SET search_path = public, pg_catalog` (pg_temp
    deliberately excluded per 20260601120000:95-99). Guards VERBATIM from the parents, in
    RESEARCH §1's order: p_terminal_status whitelist 22023 FIRST (D-08; before any write, per
    20260728120000:212-219); auth NULL -> 42501 with the message
    `finalize_csv_strategy_with_returns called without an auth session` (function-name-accurate;
    Task 2 re-points the gate's message assertion to this literal); identity mismatch -> 42501;
    fmt whitelist 22023; name required/<=80 22023 (two raises, distinguishing substrings preserved);
    jsonb_typeof(p_rows) <> 'array' -> 22023; jsonb_array_length(p_rows) > 5000 -> 22023 (the cap
    verbatim from 20260522111839:160-162). Then: INSERT INTO strategies with the EXACT column list
    from 20260728120000:278-288 (user_id, name, status=p_terminal_status, source='csv',
    strategy_types, subtypes, markets, supported_exchanges, wizard_session_id) RETURNING id;
    INSERT INTO strategy_verifications per :299-305 (status='validated', trust_tier='csv_uploaded',
    flow_type='csv', source='csv', errors/correlation_id NULL; keep the FK-at-COMMIT note :295-298);
    the length-gated dailies INSERT naming exactly (strategy_id, date, daily_return) via
    jsonb_array_elements (B3 — api_key_id/allocator_id stay NULL for the XOR CHECK; the
    owner-coherence trigger never fires for strategy-scoped rows); RETURN the id. NO handler clause
    anywhere in the body. STEP 2: DROP FUNCTION for the old 5-arg finalize and the 3-arg persist
    (exact signatures), then re-issue grants on the fold — `REVOKE ALL ... FROM PUBLIC, anon;
    GRANT EXECUTE ... TO authenticated;` and NOTHING for service_role (narrowing callers to
    service_role NULLs auth.uid() — 20260522111839:200-208; the dropped persist's service_role
    grant dies with it, note it in the header as the tightening PATTERNS flags). COMMENT ON
    FUNCTION restated. STEP 3 self-verify DO block, every failure message naming the CONSEQUENCE:
    prosecdef true; exactly ONE overload with pronargs = 6 (the 20260601120000:577-622 idiom);
    has_function_privilege('authenticated', ...) true AND ('anon', ...) false; fragment-scoped
    anchors per the 20260728120000:399-422 idiom (strpos window between 'INSERT INTO strategies ('
    and 'RETURNING id' must contain wizard_session_id — RAISE "FIX THE ANCHORS" on drift); the body
    contains the 5000 literal; the body does NOT match the handler-opening pair (use the
    class-separated regex form `'EXCEPTION[[:space:]]+WHEN'` — a bare token grep would false-hit
    the RAISE statements); both OLD function names absent from pg_proc; the dailies INSERT fragment
    names exactly the three columns.
    (4) EXTEND the harness into `145-fold-harness.sql` (a NEW file so Plan 01's committed harness
    stays byte-stable): everything in 145-repro-harness.sql PLUS a csv_daily_returns replica in the
    20260624120000 shape (surrogate identity PK, nullable strategy_id, the unique (strategy_id,
    date) index, the XOR CHECK — each copied verbatim with source:line cited) and a loader for the
    REAL persist body (20260522111839:111-186) so STEP 0's both-parents-exist preflight passes.
    Same FIDELITY LIMITS register (no RLS, no real auth).
    (5) TRACER PROOF on a throwaway cluster: (a) run the migration on a cluster WITHOUT the old
    functions -> STEP 0 must raise (fail-loud proof); (b) apply harness then migration -> STEP 3
    passes; (c) behavioral pass, claims idiom throughout: happy path (10-row payload -> UUID
    returned, all three tables populated, status='pending_review' by default); 'private' call ->
    status='private' (D-08); trades-empty ('[]', fmt='trades') -> success, zero dailies; 5001 rows
    -> 22023; ATOMICITY ORACLE: a payload whose Nth element has a malformed date (the cast raises
    after the strategies INSERT would have run) -> ZERO rows in all three tables for that session;
    23505 (second submit, same user+session) -> raise observed AND per-table counts identical
    before/after (all-or-nothing); auth pair (no claims -> 42501 exact message; wrong sub -> 42501).
    Record every observation verbatim for the SUMMARY. Do NOT commit — the commit lands in Task 2
    with the re-pointed gates (B1's same-commit requirement).
  </action>
  <verify>
    <automated>MIG=$(ls supabase/migrations/*csv_finalize_atomic_fold.sql) && grep -c "SECURITY DEFINER" "$MIG" && grep -c "finalize_csv_strategy_with_returns" "$MIG" && grep -v '^--' "$MIG" | grep -cE 'EXCEPTION[[:space:]]+WHEN' | grep -qx "0" && echo no-handler-clause && psql -h "${TMPDIR:-/tmp}/145-tracer" -U postgres -d q145f -v ON_ERROR_STOP=1 -c "SELECT prosecdef FROM pg_proc WHERE proname = 'finalize_csv_strategy_with_returns'"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists; prefix strictly greater than the verified repo tip; A6 grep table
      complete with every hit dispositioned.
    - Fail-loud STEP 0 observed raising on the bare cluster; STEP 3 passed on the harness apply.
    - All eight behavioral observations recorded verbatim, including the atomicity oracle's
      zero-rows-in-three-tables result and the widened 23505 all-or-nothing count check.
    - The comment-stripped body has zero matches for the class-separated handler-clause regex.
    - Nothing committed yet.
  </acceptance_criteria>
  <done>The SC#2 mechanism exists and its atomicity claim has been EXECUTED against real rows on a real Postgres — a mid-body fault provably leaves nothing behind (D-07)</done>
</task>

<task type="auto">
  <name>Task 2: Re-point every gate naming the old RPCs + author the fold behavioral gate; commit migration + all gates in ONE commit</name>
  <files>supabase/tests/test_csv_finalize_double_submit.sql, supabase/tests/test_csv_finalize_auth_guard.sql, supabase/tests/test_csv_finalize_atomic_fold.sql</files>
  <read_first>
    - supabase/tests/test_csv_finalize_double_submit.sql IN FULL (parts 1-4, the vacuity fences at :44-58, the header's would-have-passed-GREEN register) — this is a RE-POINT of an existing money gate, not a rewrite of its intent
    - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql:1-70 (per-part discipline: BEGIN / SET LOCAL lock_timeout / ROLLBACK, ungated Part 1, EXECUTE-the-real-object)
    - 145-RESEARCH.md §7 (the gate table — including that test_wizard_session_idempotency.sql should be UNAFFECTED: verify, don't assume; and that csv-finalize-rpc.test.ts is skipIf live-DB and counts for NOTHING — RESEARCH Pitfall 7)
  </read_first>
  <action>
    (1) RE-POINT `test_csv_finalize_double_submit.sql` at `finalize_csv_strategy_with_returns`
    (6-arg calls with a p_rows payload): Part 1 first-submit now also asserts the dailies landed;
    Part 2's 23505 unchanged in intent; Part 3's rollback assertion WIDENED to THREE tables
    (exactly-one strategies row AND exactly-one verification row AND the first submission's dailies
    count unchanged after the failed second call — verified, not trusted); Part 4's cross-source
    control and its VACUITY FENCE preserved. Update the header prose to the folded contract —
    leaving a stale rationale paragraph is the one-file-scope-amendment failure mode.
    (2) RE-POINT `test_csv_finalize_auth_guard.sql`: Parts A/B now call the fold; the Part A
    message assertion pins the NEW literal (`finalize_csv_strategy_with_returns called without an
    auth session`); ADD a structural part asserting both OLD function names are gone from pg_proc
    (the DROP's receipt, and the gate that reds if someone "helpfully" restores a parent). The
    gate's D-02 purpose transfers: a future service-role caller of the fold reds here.
    (3) AUTHOR `supabase/tests/test_csv_finalize_atomic_fold.sql` — the CI-durable versions of
    Task 1(5)'s behavioral proofs, in the reconcile-sweep register (ungated structural Part 1: the
    fold exists, prosecdef, grants, pronargs; then per-part BEGIN/ROLLBACK behavioral parts):
    atomicity oracle (malformed-date payload -> zero rows in all three tables); 'private' ->
    status='private' (D-08); trades-empty success with zero dailies; 5001 -> 22023. Seed through
    the real FK chain with gen_random_uuid(); no backslash meta-commands; every failure message
    names the consequence. The economic oracle discipline applies: Part assertions pin WHAT IS
    PERSISTED EQUALS WHAT WAS SUBMITTED (row count + a spot-checked date/value pair), never the
    fold's own formula.
    (4) Run ALL FOUR sql gates (the three above + `test_wizard_session_idempotency.sql`) against
    the Task-1 harness cluster with the migration applied — all green (the idempotency gate's green
    is the verify-don't-assume check that the partial index is untouched). On the shared TEST
    project these stay RED at their structural parts until Plan 06 applies — that is designed.
    (5) COMMIT — ONE commit: the migration, the three gate files, and 145-fold-harness.sql (B1/B2
    same-commit; message e.g. `feat(145-03): fold csv-finalize into one SECDEF transaction + re-point all gates in the same commit`).
    Verify with `git show --stat` that all five files are in that single commit and that NO hunk
    touches 20260816140000 or 20260817120000 (D-10).
  </action>
  <verify>
    <automated>psql -h "${TMPDIR:-/tmp}/145-tracer" -U postgres -d q145f -v ON_ERROR_STOP=1 -f supabase/tests/test_csv_finalize_double_submit.sql -f supabase/tests/test_csv_finalize_auth_guard.sql -f supabase/tests/test_csv_finalize_atomic_fold.sql && git show --stat HEAD | grep -c "supabase" && git show HEAD -- supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql | wc -l | grep -qx "0" && echo phase-143-144-untouched</automated>
  </verify>
  <acceptance_criteria>
    - Zero SQL gates reference the old function names as call targets; the old-names-gone
      structural assertion exists; the widened three-table Part 3 exists.
    - All four gates green on the harness cluster; the same-commit set verified via git show --stat.
    - The commit contains no hunk on 143's or 144's migrations (D-10).
  </acceptance_criteria>
  <done>The DROP cannot red `sql-tests` on a correct migration (B1/B2 closed), and SC#2's oracle is a permanent CI gate at the deployed body</done>
</task>

<task type="auto">
  <name>Task 3: SQL neuter-RED matrix — observe every new/changed assertion fail, restore, record</name>
  <files>.planning/phases/145-job-csv-finalize-atomicity/145-fold-harness.sql</files>
  <read_first>
    - .planning/phases/144-job-wr-02-orphaned-running/ (the 144 SUMMARY neuter-RED table format, if present; else 143-03-SUMMARY.md's nine-red table)
  </read_first>
  <action>
    On the harness cluster, for each neuter: deploy a VARIANT function body (CREATE OR REPLACE on
    the throwaway cluster only — repo files are never edited for this), run the affected gate(s),
    OBSERVE the actual RED output, restore the real body, re-run to GREEN, and record (neuter, gate
    part, observed message) in a table for the SUMMARY. Minimum matrix:
    (1) a handler clause added around the dailies INSERT (swallowing the error) -> the atomicity
    oracle reds (strategy + verification rows survive the fault) AND STEP 3's no-handler anchor
    reds — the D-07 pair;
    (2) the dailies INSERT removed from the body (reverting to a two-RPC shape) -> Part 1's
    dailies-landed assertion reds and the atomicity oracle's premise collapses visibly;
    (3) p_terminal_status forced to 'pending_review' internally (argument ignored) -> the 'private'
    part reds (D-08);
    (4) the parents' empty-array raise copied verbatim -> the trades-empty part reds (Pitfall 2);
    (5) the 5000 cap removed -> the cap part reds;
    (6) wizard_session_id dropped from the strategies INSERT -> STEP 3's fragment anchor reds AND
    the 23505 part reds (the index never bites — the two failures are independent evidence);
    (7) grants left at CREATE-default (REVOKE skipped) -> the anon/PUBLIC privilege assertion reds
    (the DROP-loses-ACLs trap made checkable);
    (8) the gates run against a cluster with the parents still present and no fold -> the
    structural parts red (proves they are genuinely ungated, no green-skip).
    If any assertion stays GREEN under its neuter, it is vacuous: fix the assertion (gate files
    only; if the MIGRATION's STEP 3 itself is vacuous, amend it now — nothing has left the repo)
    and re-observe. Every fix commit message names the vacuity it closes.
  </action>
  <verify>
    <automated>psql -h "${TMPDIR:-/tmp}/145-tracer" -U postgres -d q145f -v ON_ERROR_STOP=1 -f supabase/tests/test_csv_finalize_atomic_fold.sql && psql -h "${TMPDIR:-/tmp}/145-tracer" -U postgres -d q145f -v ON_ERROR_STOP=1 -f supabase/tests/test_csv_finalize_auth_guard.sql</automated>
  </verify>
  <acceptance_criteria>
    - All eight neuters observed RED with actual output captured; final state GREEN with the real
      body restored.
    - Zero assertions found vacuous, or each vacuity fixed and re-observed with its own commit.
    - The recorded table is ready for the SUMMARY (Plan 05 extends it with the TS-gate neuters).
  </acceptance_criteria>
  <done>Every SQL gate in the commit has been seen to fail for the reason it exists — inherited green is nothing in a surface that hosted RED-TEAM-M1</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merge -> PROD | supabase/migrations/** on main auto-applies to PROD; this file DROPs two deployed functions and creates a new SECDEF surface |
| authenticated caller -> fold | a NEW SECURITY DEFINER function taking caller-supplied JSONB — migration-reviewer invariants #3/#15/#19 apply for real |
| gate seeds -> shared TEST | per-part BEGIN/ROLLBACK; structural parts designed-RED until Plan 06 applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-08 | Elevation of privilege | fold callable by anon/PUBLIC or service_role | critical | mitigate | REVOKE FROM PUBLIC, anon + GRANT authenticated only, re-issued post-DROP; privilege assertions in STEP 3 + gate; neuter (7) proves them non-vacuous |
| T-145-09 | Tampering (money) | caller-supplied JSONB rows into a SECDEF write | critical | mitigate | Every caller value validated before use (fmt/name/status whitelists, array typeof, 5000 cap, date/double casts); economic oracle pins persisted == submitted |
| T-145-10 | Tampering | partial-state commit (the orphan class itself) | critical | mitigate | No handler clause (STEP 3 + regex-pinned); atomicity oracle EXECUTED at the deployed body; 23505 all-or-nothing widened to three tables |
| T-145-11 | Information disclosure | strategy-id enumeration via error shapes | medium | accept | The fold takes no caller-supplied strategy id — the probe surface dissolved with the parameter; resolve-arm refusal uniformity is Plan 04's scope |
| T-145-12 | Denial of service | 42501 on every finalize via wrong GRANT shape | high | mitigate | Never narrow to service_role (20260522111839:200-208 documented incident class); gate asserts authenticated-EXECUTE true |
| T-145-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase |
</threat_model>

<verification>
- Tracer: fail-loud STEP 0, STEP 3 pass, eight behavioral observations incl. the atomicity oracle, recorded verbatim
- All four SQL gates green on harness; same-commit set proven via git show --stat; 143/144 migrations untouched
- Neuter-RED table complete, eight rows, zero unresolved vacuities
</verification>

<success_criteria>
- SC#2's mechanism exists and is proven offline: a mid-body fault leaves ZERO rows in all three tables; 23505 rolls back all three (ROADMAP SC#2 first arm).
- SC#3's SQL half held: 'private' writes 'private'; the default is 'pending_review' (D-08).
- B1/B2/B3 closed in this plan's single commit; D-10 verified by diff.
</success_criteria>

<output>
Create `.planning/phases/145-job-csv-finalize-atomicity/145-03-SUMMARY.md` when done, including the
A6 disposition table, the tracer observations, the neuter-RED table, and the exact commit hash
carrying the same-commit five-file set.
</output>
