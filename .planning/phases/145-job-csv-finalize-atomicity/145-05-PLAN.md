---
phase: 145-job-csv-finalize-atomicity
plan: 05
type: execute
wave: 5
depends_on: [145-04]
files_modified:
  - src/__tests__/csv-finalize-c14-regression.test.ts
  - TODOS.md
autonomous: true
requirements: [JOB-06]
tags: [vitest, test-vacuity, sentry, csv-finalize, todos, deferral]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "The vacuous RED-TEAM-M1 block (csv-finalize-c14-regression.test.ts:144-190) is DELETED — no test named for a guarantee that does not exist survives, and the string it searched for is not resurrected (D-11)"
    - "Replacement tests exist IN THE SAME COMMIT as the delete, each driving the ACTUAL post-RPC failure arm and asserting a capture/copy that WAS made — and each was observed RED under neuter before restore (founder rule: a test that cannot fail is worse than none)"
    - "The fold-failure capture test pins tags.step='finalize-fold-fail' AND the truthful copy on that arm; the resolve-refusal test pins step='finalize-resolve-refused' AND the accurate 409 copy (D-11, D-12)"
    - "The three deferred items are filed in TODOS.md with their constraints attached — window E (with the census-(3) result cited), the wizard first-hop drop (NOT absorbed into 145), and the inert flag-row/env cleanup (with the 20260620120000:86-89 apply-time RAISE constraint)"
    - "A consolidated neuter-RED table covering every Phase-145 TS gate exists for the phase SUMMARY"
  artifacts:
    - "src/__tests__/csv-finalize-c14-regression.test.ts — vacuous block gone; replacement tests present in the after-failloud idiom"
    - "TODOS.md — three new deferral entries, each with its constraint and evidence pointer"
  key_links:
    - "replacement tests <-> route.ts's new arms — they drive the arms Plan 04 built (mock fold error / mock 23505+mismatch), not the pre-create 400 the vacuous block drove"
    - "TODOS window-E entry <-> census query (3)'s recorded result — non-zero PROD was the pre-registered re-rank trigger; the entry cites the measured number either way"
---

<objective>
Discharge the honesty-gate half of D-11: delete the test that could never fail, replace it in the
same commit with gates that can (each observed RED), and file the three deferrals to TODOS with
their constraints attached so nothing is silently dropped.

Purpose: the phase entered a file that hosted a test asserting the ABSENCE of the guarantee the
phase delivers; it leaves that file with gates that fail when the guarantee breaks.
Output: rebuilt c14 file, three TODOS entries, the consolidated TS neuter-RED table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-job-csv-finalize-atomicity/145-CONTEXT.md
@.planning/phases/145-job-csv-finalize-atomicity/145-RESEARCH.md
@src/__tests__/csv-finalize-c14-regression.test.ts
@src/__tests__/csv-finalize-after-failloud.test.ts
</context>

## Decision map (cited as D-NN below)

| ID | Locked decision honored in this plan |
|----|--------------------------------------|
| D-11 | The vacuous RED-TEAM-M1 test is fixed-or-deleted in this phase. Disposition chosen per RESEARCH §5's recommendation: DELETE the block (the guarantee it names is superseded by the fold — CONTEXT's discretion clause makes landing SC#2 the deletion condition) and replace it in the same commit with gates that CAN fail |
| D-12 | The replacement tests are the CI-durable pins for the two new capture sites Plan 04 added |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the vacuous block; author replacement gates that drive the real arms; observe each RED</name>
  <files>src/__tests__/csv-finalize-c14-regression.test.ts</files>
  <behavior>
    - Test A: fold RPC mocked to error (non-23505) -> asserts captureToSentry called with tags.step='finalize-fold-fail' AND the response carries the truthful nothing-was-saved copy and the expected code
    - Test B: fold mocked to raise 23505 + resolve re-fetch returning a mismatched name -> asserts 409 CSV_SESSION_REUSED, the accurate copy, the 'finalize-resolve-refused' capture, and that NO metadata update ran (checks-before-metadata, the 409-lie fix)
    - Test C: fold mocked to raise 23505 + matching name/range -> asserts 200 echoing the existing id and that the resolve arm persisted NOTHING (the read-only contract)
  </behavior>
  <read_first>
    - src/__tests__/csv-finalize-c14-regression.test.ts:22-75 (the mock scaffold to KEEP: server-only mock, withAuth passthrough, vi.hoisted rpcMock/checkLimitMock, createClient mock) and :144-190 (the block being deleted — read its comments so the SUMMARY can state exactly why it could never fail)
    - src/__tests__/csv-finalize-after-failloud.test.ts:207-291 (the four-test idiom the replacements copy: assert tags.step AND that the console line survives)
    - 145-RESEARCH.md §5 Defect 2 (the vacuity mechanism and the recommended replacement shapes)
  </read_first>
  <action>
    (1) DELETE the entire RED-TEAM-M1 describe block (:144-190). Do not keep any assertion from it;
    do not reintroduce its search string anywhere in src/ (it exists nowhere today — keep it that
    way; the acceptance grep is count-asserted).
    (2) In the SAME file, author Tests A/B/C per the behavior block, reusing the file's existing
    mock scaffold and the after-failloud idiom (assert tags.step and the surviving console.error).
    Update the file header comment to name what the file NOW pins and why the old block was
    deleted (one paragraph, citing 145-REPRODUCTION.md's phase context — no stale prose left, the
    one-file-scope-amendment class).
    (3) NEUTER-RED, per test, against the real route: (A) comment out the fold-failure capture call
    in a scratch edit of route.ts -> Test A must red on the capture assertion; restore. (B) reorder
    the metadata update ahead of the resolve checks in a scratch edit -> Test B's no-metadata
    assertion must red; restore. (C) make the resolve arm perform a write in a scratch edit ->
    Test C's nothing-persisted assertion must red; restore. OBSERVE each actual red output, record
    (test, neuter, observed message) — three rows. If any replacement stays GREEN under its neuter,
    it has inherited the disease this task exists to cure: fix and re-observe before commit.
    (4) Run the file plus its siblings green:
    `npx vitest run src/__tests__/csv-finalize-c14-regression.test.ts src/__tests__/csv-finalize-after-failloud.test.ts --no-file-parallelism`
    (main checkout — worktrees have no node_modules). Commit the file (delete + replacements, one
    commit): `test(145-05): delete the vacuous RED-TEAM-M1 block; replace with gates that can fail`.
  </action>
  <verify>
    <automated>grep -c "RED-TEAM-M1" src/__tests__/csv-finalize-c14-regression.test.ts | grep -qx "0" && grep -rc "orphan strategy row" src/ | grep -v ":0" | wc -l | grep -qx "0" && npx vitest run src/__tests__/csv-finalize-c14-regression.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of the old block's name in the file; zero occurrences of its search string
      anywhere in src/ (both count-asserted).
    - Three observed-RED records captured; file green after restore; siblings still green.
    - Delete + replacements are ONE commit.
  </acceptance_criteria>
  <done>The file that hosted a test that could never fail now holds only gates observed to fail for the reasons they exist (D-11, D-12)</done>
</task>

<task type="auto">
  <name>Task 2: File the three deferrals to TODOS with constraints; consolidate the TS neuter-RED table</name>
  <files>TODOS.md</files>
  <read_first>
    - 145-CONTEXT.md &lt;deferred&gt; section (the four deferred items and their constraints — the fourth, onboard/resync token forwarding, is ALREADY a standing 140.1 obligation and is NOT re-filed)
    - .planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md census section (query (3)'s recorded result — the window-E entry cites it)
  </read_first>
  <action>
    (1) Add three entries to TODOS.md (the ONE ground-truth backlog; scoped Edits, matching the
    file's existing entry register):
      - Window E (dailies present, enqueue errored, strategy_analytics='failed', job never
        re-enqueued): visible + Sentry-alerted but never healed; 143's terminal-analytics conjunct
        deliberately excludes it. Cite census query (3)'s measured count from 145-REPRODUCTION.md.
        If that count was non-zero on PROD, mark the entry priority accordingly (the pre-registered
        re-rank).
      - Wizard first-hop drop: 143's documented non-coverage (20260816140000:259-265), shape-
        identical to windows A-C but with an unsolved distinguishing signal; ⛔ never absorb it by
        widening a predicate; cite the census (1)-minus-(2) number.
      - Inert `process_key_unified_backbone` cleanup (feature_flags row + Vercel/Railway env vars):
        ⛔ constraint attached verbatim — 20260620120000:86-89 RAISEs at apply time if the row
        reads 'off', so a careless delete reddens a future migration apply; zero code readers at
        HEAD (145-REPRODUCTION.md arm 2).
    (2) Consolidate the phase's TS neuter-RED table (Plan 04's five CR-01 rows + Task 1's three
    rows + any after-failloud re-point rows) into one table in this plan's SUMMARY, alongside
    pointers to Plan 03's SQL table — the phase-level evidence Plan 06's human gate presents.
    (3) Commit: `docs(145-05): file window-E / first-hop / flag-cleanup deferrals with constraints`.
  </action>
  <verify>
    <automated>grep -c "20260620120000" TODOS.md && grep -c "first-hop" TODOS.md && git show --stat HEAD | grep -c "TODOS.md"</automated>
  </verify>
  <acceptance_criteria>
    - Three entries present, each carrying its constraint and a measured-number citation; no other
      TODOS entries touched.
    - The consolidated neuter-RED table exists in the SUMMARY draft with every row's observed
      message.
  </acceptance_criteria>
  <done>Every consciously-deferred item is in the single backlog with its trap documented — nothing was silently dropped, nothing was silently absorbed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> route.ts truth claims | the replacement gates are what keep D-11's honest copy honest after this phase ends |
| TODOS.md -> future planning | a deferral without its constraint becomes a future incident (the 20260620120000 RAISE class) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-18 | Repudiation | a vacuous gate asserting a nonexistent guarantee | high | mitigate | Block deleted; replacements each observed RED under neuter; count-asserted absence greps |
| T-145-19 | Tampering | deferred windows silently forgotten or silently absorbed | medium | mitigate | TODOS entries with constraints + measured citations; first-hop exclusion restated |
| T-145-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase |
</threat_model>

<verification>
- Vacuous block gone (count-asserted); replacements green and each observed RED under neuter
- Three TODOS deferrals with constraints and census citations; consolidated neuter-RED table ready
</verification>

<success_criteria>
- D-11 fully discharged: no test on this surface asserts a guarantee that does not exist, and every truth claim the phase introduced is pinned by a gate observed to fail.
- The deferral set matches CONTEXT's deferred list exactly — no more, no less.
</success_criteria>

<output>
Create `.planning/phases/145-job-csv-finalize-atomicity/145-05-SUMMARY.md` when done, including the
three observed-RED records, the consolidated TS neuter-RED table, and the commit hashes.
</output>
