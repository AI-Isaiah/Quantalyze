# Phase 145 — deferred items (out of this plan's scope, in-phase owners named)

Logged by Plan 03 during the A6 pre-drop grep (2026-08-17). None blocks the
DROP; each has a named owner inside this phase.

| # | Item | Found by | Owner | Status |
|---|------|----------|-------|--------|
| 1 | `supabase/schema/functions/finalize_csv_strategy.sql` and `supabase/schema/functions/persist_csv_daily_returns.sql` are `@generated` snapshots (scripts/dump-sql-functions.ts) that now document DROPPED functions. Regenerate with `npm run schema:functions` (needs node_modules — not available in this worktree) so the two stale files disappear and a `finalize_csv_strategy_with_returns.sql` snapshot appears. | Plan 03 A6 grep | Plan 04 or 05 (any node-capable checkout) | ✅ DISCHARGED by Plan 04 (2026-08-17): `npm run schema:functions` run (hermetic text-replay); two stale files deleted, fold snapshot created; `schema:functions:check` green |
| 2 | `src/lib/database.types.ts:3715` and `:3943` carry generated TS types for BOTH dropped RPCs (contra 145-RESEARCH.md Runtime State Inventory, which said the types file does not carry them). Nothing breaks at compile time (the route casts through unknown), but the stale entries invite a future caller of a dropped function. Regenerate or hand-prune when the fold's caller lands. | Plan 03 A6 grep | Plan 04 (caller wiring) | ✅ DISCHARGED by Plan 04: both entries hand-pruned (regeneration needs the migrated live DB — Plan 06's TEST apply is the natural point to regenerate and pick up the fold's own entry; the route keeps the cast-through-unknown idiom either way) |
| 3 | `src/__tests__/audit-coverage.test.ts:209` — `MUTATING_RPC_NAMES` lists `finalize_csv_strategy`. The regex requires a closing quote right after the name, so `.rpc("finalize_csv_strategy_with_returns")` will NOT match: the fold's route call would sit OUTSIDE the audit-coverage law until the new name is added to the list. Add `finalize_csv_strategy_with_returns` (and drop the old name) in the same commit as the route re-point. | Plan 03 A6 grep | Plan 04 | ✅ DISCHARGED by Plan 04: old name swapped for `finalize_csv_strategy_with_returns` in the same commit as the route re-point, with a comment recording why the cast-shaped call sits outside the `\.rpc\(` anchor today |
| 4 | `analytics-service/tests/test_persist_csv_daily_returns_live.py` (skipIf live-DB, never runs in CI) calls the dropped RPC by name; goes 42883 the moment TEST is migrated. Retire or re-point with the Python csv-finalize deletion. | Plan 03 A6 grep | Plan 04 (D-06 obligation 2) | ✅ DISCHARGED by Plan 04: Tests 1-6 (direct RPC callers) retired — successors are the CI SQL gates (`test_csv_finalize_atomic_fold.sql`, `test_csv_finalize_auth_guard.sql`); table-level Tests 7-11 kept, worker-pipeline seeding re-pointed to direct service-role INSERTs; filename kept (two sibling live suites import its helpers) |
| 5 | `src/__tests__/csv-finalize-rpc.test.ts` (skipIf live-DB — NEVER runs in CI, RESEARCH Pitfall 7) still targets the dropped 5-arg `finalize_csv_strategy` by name. Inert until someone runs it manually against a migrated DB, where it goes 42883. Re-point at the fold or retire when Plan 06's TEST apply makes it exercisable. | Plan 04 final sweep | **Plan 06** (Plan 05 evaluated 2026-08-17 and deliberately did NOT re-point it: the file is exercisable only against a live migrated DB, which Plan 05 has no access to — a blind rewrite would be a test whose RED/GREEN cannot be observed, the exact disease 145-05 exists to cure. Plan 06's TEST apply is the first moment a re-point is verifiable; retire-or-re-point there.) | ⏳ open |
