---
phase: 140.1-pyapi-python-service-contract-status-attributability-limiter
plan: 05
type: execute
wave: 2
depends_on: [140.1-02]
files_modified:
  - analytics-service/routers/process_key.py
  - analytics-service/tests/test_process_key_200_discriminator.py
  - analytics-service/tests/test_process_key.py
autonomous: true
requirements: [PYAPI-10]
must_haves:
  truths:
    - "Every 200 from /process-key carries ok: bool — all six shapes, no consumer needs to sniff field presence"
    - "The write-capable-key scope rejection answers 403, never a success status"
  artifacts:
    - path: "analytics-service/tests/test_process_key_200_discriminator.py"
      provides: "Exhaustive six-shape parametrised oracle (PYAPI-10a) + 403 verdict oracle (PYAPI-10b)"
  key_links:
    - from: "all /process-key return sites"
      to: "_envelope_error / success shapes"
      via: "ok + code discriminator keys"
      pattern: "\"ok\":"
---

<objective>
Give the `/process-key` success surface ONE discriminator (PYAPI-10 / C-22): `ok: bool` on all six
200 shapes, a stable `code` whenever `ok` is false, and move the security verdict (scope rejection)
off 200 to 403.

Purpose: today three of six 200 shapes have no discriminator at all, consumers sniff field presence,
and a write-capable-key rejection — a security verdict — is delivered as HTTP success.
Output: unified 200 surface + exhaustive parametrised oracle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-RESEARCH.md  (Q5 PYAPI-10 section — the six-shape table and the coordinated-change hazard)
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-PATTERNS.md  (Class 4; _envelope_error analog at process_key.py:286-301)
</context>

<critical_warnings>
1. **Six shapes, not four** (C-22 undercounts). The complete list with sites (count: 6; sites are
   post-Plan-02 — re-locate by shape keys if the Plan-02 refactor moved lines):
   - Shape 1: {code, idempotent, verification_id, status, trust_tier, correlation_id, queued} —
     BOTH WIZARD_DUPLICATE emitters (was :747-755 and :910-918). No ok today.
   - Shape 2: {queued:true, verification_id, correlation_id} — was :937-941. No ok today.
   - Shape 3: {ok:true, valid, read_only, correlation_id, step:"validate"} — was :493-509.
   - Shape 4: {ok:true, strategy_id, status, correlation_id, step:"finalize"} — was :844-850.
   - Shape 5: synchronous success {verification_id, status, trust_tier, metrics_snapshot, …} — was
     :1198+. No ok, no code — the shape consumers sniff.
   - Shape 6: _envelope_error {ok:false, code, human_message, debug_context, correlation_id,
     recoverable} — was :488 and :1004.
2. **Only the :1004 arm moves to 403.** The :488 arm (_run_validate_only validation failure) keeps
   its 200 + ok:false — RESEARCH's mechanism moves ONLY the security verdict. Do not "fix" :488's
   status.
3. **Do NOT widen to `routers/csv.py`** (:47,73,87,96): its 200-with-ok:false is a documented
   deliberate design (csv.py:103 comment) with its own consumer. Planner decision: OUT of scope.
   A fixer must not "find the third site" on /process-key — on this route there are exactly 2
   ok:false-at-200 sites and only one of them changes status.
4. **Extend `_envelope_error`'s key vocabulary (ok/code/correlation_id), never invent a parallel
   envelope.** job_state (added by Plan 140.1-02) rides these same shapes — verify it survives.
5. **TS coordinated-change hazard:** adding `ok` is additive and safe; moving shape 6 to 403 is
   BREAKING for keys/sync/route.ts:438 and finalize-wizard/route.ts:1419-1420 readers. Record the
   obligation with file:line for 140.2 in the SUMMARY; do not edit TS.
</critical_warnings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ok on all six shapes (PYAPI-10a)</name>
  <files>analytics-service/routers/process_key.py, analytics-service/tests/test_process_key_200_discriminator.py</files>
  <read_first>
    - analytics-service/routers/process_key.py (every return in the handler + _run_validate_only +
      _envelope_error :286-301 — read the WHOLE return surface once, post-Plan-02 state)
    - analytics-service/tests/test_process_key.py fixture style (service-role mocking idiom) — the
      new file copies it
  </read_first>
  <behavior>
    - PYAPI-10a: parametrised over ALL SIX shapes, each reached by a distinct fixture (duplicate-hit,
      queued-fresh, validate-only success, csv-finalize success, synchronous success, envelope
      error). Every 200 body has ok as a bool; whenever ok is False, code is a non-empty string.
      Expected ok values are True/False LITERALS per case in the test — never derived.
    - Shapes 1/2/5 gain ok:true (they are successes); shape 5 also gains a stable success code or
      explicit code:null — pick one, document in the test as a literal.
  </behavior>
  <action>
    Add ok to shapes 1, 2 and 5 at their return sites (both shape-1 emitters — TRAP-5 count: 2).
    Keep every existing key untouched (additive only in this task). Confirm job_state from Plan 02
    is present on the duplicate-path shapes and asserted in at least one parametrised case.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_process_key_200_discriminator.py -q && python3 -m mypy --strict --follow-imports=silent routers/process_key.py</automated>
  </verify>
  <acceptance_criteria>
    - The parametrised test has exactly 6 shape cases; each pins ok as a literal.
    - Mutation M8 (delete ok from shape 5 ONLY) turns 10a RED — run, confirm, revert, record. This
      is the second-member-of-class detector.
    - No existing key removed from any shape (additive diff — executor confirms via git diff).
  </acceptance_criteria>
  <done>All six 200 shapes carry the discriminator; exhaustive oracle in place and mutation-checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope rejection off 200 → 403 (PYAPI-10b)</name>
  <files>analytics-service/routers/process_key.py, analytics-service/tests/test_process_key.py, analytics-service/tests/test_process_key_200_discriminator.py</files>
  <read_first>
    - analytics-service/routers/process_key.py:956-1010 region (_scope_rejected :956-961; the bare
      _envelope_error return at :1004; the JSONResponse-wrapped 4xx idiom at :804-812)
  </read_first>
  <behavior>
    - PYAPI-10b (economic): a write-capable-key rejection answers status 403 (literal), body keeps
      the full envelope (ok:false, code, human_message, recoverable) — the verdict is never under a
      success status.
    - The :488 validate-only arm still answers 200 with ok:false (pinned so this plan provably did
      not over-reach).
  </behavior>
  <action>
    Wrap the :1004 _envelope_error return in a JSONResponse with status_code=403, matching the
    :804-812 wrapping idiom. Extend the existing scope-rejection tests around test_process_key.py's
    :816-870 region to the new status. Record in the SUMMARY the TS obligation set for 140.2: the
    two queued/WIZARD_DUPLICATE readers (keys/sync/route.ts:438, finalize-wizard/route.ts:1419-1420)
    plus any TS consumer of the scope-rejection 200 discovered by grepping src/ for the rejection
    code string — enumerate what the grep found, with file:line, even if empty.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_process_key.py tests/test_process_key_200_discriminator.py -q && python3 -m mypy --strict --follow-imports=silent routers/process_key.py && python3 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - PYAPI-10b green with literal 403; mutation M7 (restore bare-dict 200) turns it RED — run,
      confirm, revert, record.
    - :488 arm pinned at 200 + ok:false (a test asserts it, so scope cannot silently widen).
    - `grep -n "_envelope_error(" analytics-service/routers/process_key.py` — every call site is
      either JSONResponse-wrapped with an explicit 4xx or the :488 validate-only arm (paste the
      enumeration into the SUMMARY; expected: 0 bare security-verdict returns).
    - TS consumer grep enumerated in SUMMARY with file:line results.
    - Full analytics-service suite green at end of plan (`python3 -m pytest -q`) — the wave-2
      full-suite closure VALIDATION requires.
  </acceptance_criteria>
  <done>No security verdict under a success status; scope fence proven intact for the validate-only arm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Python 200 surface→TS consumers | consumers currently sniff field presence |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1-13 | Elevation of Privilege | scope rejection at 200 | mitigate | 403 + envelope (PYAPI-10b, M7) |
| T-140.1-14 | Repudiation | undiscriminated 200 shapes | mitigate | ok on all six shapes (PYAPI-10a, M8) |
| T-140.1-SC | Tampering | package installs | accept | no packages installed in this plan |
</threat_model>

<verification>
- Six-shape oracle green; M7/M8 mutation-checked; mypy --strict clean.
</verification>

<success_criteria>
Phase success criterion 6's machine-readability leg for the 200 surface: one discriminator, no
security verdict under success.
</success_criteria>

<output>
Create `.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-05-SUMMARY.md` when done.
Record: M7/M8 evidence, TS consumer grep results, the csv.py out-of-scope decision restated.
</output>
