---
phase: 140.1-pyapi-python-service-contract-status-attributability-limiter
plan: 07
type: execute
wave: 4
depends_on: [140.1-03, 140.1-04, 140.1-06]
files_modified:
  - analytics-service/routers/exchange.py
  - analytics-service/routers/csv.py
  - analytics-service/routers/portfolio.py
  - analytics-service/routers/optimizer.py
  - analytics-service/services/rate_limit.py
  - analytics-service/tests/test_limiter_identity.py
autonomous: true
requirements: [PYAPI-03]
must_haves:
  truths:
    - "No router declares its own Limiter; all throttling goes through services/rate_limit.py"
    - "No decorated route keys on the request address — the 9-route IP-keyed class is closed, including optimizer.py's shared-limiter-default instance"
    - "Token cost per user-visible flow is documented in one place"
  artifacts:
    - path: "analytics-service/tests/test_limiter_identity.py"
      provides: "Per-route key_func identity assertions + zero-get_remote_address grep gate"
  key_links:
    - from: "9 route decorators"
      to: "services/rate_limit.py key functions"
      via: "explicit key_func= on each decorator"
      pattern: "key_func="
---

<objective>
Close the IP-keyed limiter class (PYAPI-03): delete the 3 private `Limiter()` instances, rekey all
9 IP-keyed routes onto shared key functions, and document token cost per flow.

Purpose: behind Railway's proxy `get_remote_address` collapses to the edge IP (G-10/G-11), so every
"per-IP" limit is actually PLATFORM-WIDE — two users running Scenario Composer can 429 each other on
L-9's 20/minute. The class is IP-KEYING (behaviour), not "private Limiter" (syntax) — enumerating by
syntax misses optimizer.py (X-4 / TRAP-5).
Output: 9 rekeyed routes, 0 private limiters, identity-oracle test file, flow-cost table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-RESEARCH.md  (Q1.4 in full — the L-1..L-9 table, the do-not-touch list, the prescription)
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-PATTERNS.md  (Class 2; shared-instance registration analog; never-raise key-func analog)
</context>

<critical_warnings>
1. **The class is 9 routes across 4 limiter instances.** The complete list (count: 9):
   L-1 exchange.py:336 /api/validate-key · L-2 exchange.py:418 /api/encrypt-key ·
   L-3 exchange.py:447 /api/fetch-trades · L-4 csv.py:57 /api/csv/validate ·
   L-5 portfolio.py:1532 /api/portfolio-analytics · L-6 portfolio.py:1592 /api/portfolio-optimizer ·
   L-7 portfolio.py:1852 /api/portfolio-bridge · L-8 portfolio.py:2147 /api/verify-strategy (Python) ·
   L-9 optimizer.py:31 /api/optimize-weights (imports the SHARED limiter and silently inherits its
   get_remote_address default — the instance the "private Limiter" framing misses).
   Private instances to delete (count: 3): exchange.py:40, csv.py:24, portfolio.py:146.
2. **Do NOT touch:** process_key.py:518 (rekeyed by Plan 06), simulator.py:232 (correctly
   user-keyed), internal.py:67-91 (hand-rolled per-key_id — correct dimension), and
   routers/match.py (NO limiter today — RATE-03/Phase 146 owns adding one; record the gap only).
3. **Do NOT change limit VALUES** (100/hour, 10/hour, 30/hour, 20/minute, 5/hour stay as-is) —
   RATE-04/Phase 146 owns the value audit. This plan changes IDENTITY only.
4. **Key functions must never raise** (G-8) and can only read headers (G-7).
5. **Where no tenant claim is available** (routes called via analytics-client.ts, which does not
   mint X-Tenant-Claim yet — that is a 140.2 obligation), the key function must fall back to an
   explicit "platform:<route-path>" bucket that is DOCUMENTED as a deliberate platform ceiling, not
   an accidental one. Once 140.2 mints the claim on analytics-client.ts, these become per-tenant
   automatically — state this in the docstring.
6. **Grep-gate hygiene:** python comments start with #; the zero-count gate must exclude comment
   lines or it can be defeated/self-invalidated by prose.
</critical_warnings>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 3 private limiters; single shared instance everywhere</name>
  <files>analytics-service/routers/exchange.py, analytics-service/routers/csv.py, analytics-service/routers/portfolio.py, analytics-service/services/rate_limit.py</files>
  <read_first>
    - analytics-service/services/rate_limit.py (the singleton + its Phase-19/API-5 docstring — the
      direct precedent for exactly this repair)
    - analytics-service/routers/exchange.py:35-45, csv.py:20-28, portfolio.py:140-150 (the three
      declarations and their import context)
  </read_first>
  <action>
    Replace each private Limiter declaration with an import of the shared singleton from
    services/rate_limit.py (each private instance today has its own isolated memory:// storage —
    G-3 — so consolidation also unifies counters). Add to rate_limit.py a shared
    tenant-or-platform key function (reusing Plan 06's claim verification; fallback bucket
    "platform:<path>" via request.url.path) with the never-raise shape. Extend the module docstring
    with the token-cost-per-flow table from RESEARCH Q1.4 (flows × buckets × limits) — this is
    PYAPI-03's "documented token cost per flow" clause.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -c "import routers.exchange, routers.csv, routers.portfolio, routers.optimizer" && grep -rn "Limiter(" routers/ | grep -v "^.*#" | wc -l | grep -q "^0$" && python3 -m mypy --strict --follow-imports=silent routers/ services/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Limiter(" analytics-service/routers/` (comment-filtered) returns 0 declarations —
      the only Limiter() construction left in analytics-service is services/rate_limit.py:29 (paste
      grep output into SUMMARY).
    - The flow-cost table exists in rate_limit.py's docstring and names all rows from Q1.4's
      "Token cost per user-visible flow" table.
    - App still boots (import check) and all existing router tests pass.
  </acceptance_criteria>
  <done>One limiter instance service-wide; consolidation documented against the module's own precedent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rekey all 9 routes + identity oracles</name>
  <files>analytics-service/routers/exchange.py, analytics-service/routers/csv.py, analytics-service/routers/portfolio.py, analytics-service/routers/optimizer.py, analytics-service/tests/test_limiter_identity.py</files>
  <read_first>
    - analytics-service/tests/test_simulator_router.py:992-1004 (the key_func identity-assertion
      idiom to mirror per route)
    - analytics-service/routers/process_key.py:518 (the explicit key_func= decorator override shape)
  </read_first>
  <behavior>
    - For EACH of the 9 routes (L-1..L-9): an identity assertion that the decorator's key_func is
      the shared tenant-or-platform function and is NOT slowapi.util.get_remote_address.
    - A behavioural oracle on at least L-9 and L-1: a request carrying a valid tenant claim buckets
      to "...:t:<user_id>..." while a claimless request buckets to the literal
      "platform:/api/optimize-weights" (bucket strings as LITERALS in the test).
    - A grep-gate test (subprocess or pytest-level file scan) asserting zero non-comment occurrences
      of get_remote_address under routers/ — the class-closure gate.
  </behavior>
  <action>
    Add explicit key_func= to all 9 decorators (L-9's fix is adding the override optimizer.py's
    decorator silently lacks). Limit values unchanged. Record in the SUMMARY: (a) the match.py
    no-limiter gap for Phase 146, (b) the analytics-client.ts claim-minting obligation for 140.2
    (until it lands, L-5..L-9 run as documented platform ceilings).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_limiter_identity.py -q && python3 -m pytest -q && python3 -m mypy --strict --follow-imports=silent routers/ services/ models/</automated>
  </verify>
  <acceptance_criteria>
    - test_limiter_identity.py contains exactly 9 per-route identity assertions — one per L-row,
      each naming its route path as a literal; SUMMARY states "9/9 rekeyed" against the L-table.
    - `grep -rn "get_remote_address" analytics-service/routers/ | grep -v '#'` returns 0 lines
      (paste into SUMMARY). services/rate_limit.py may retain the import only as the singleton's
      default for undecorated future routes IF a comment marks it deprecated — or remove it; either
      way routers/ is clean.
    - Bucket-string literals for the L-9/L-1 behavioural oracles are in the test file (grep-verifiable).
    - Full analytics-service pytest green (no route regressed by the rekey).
  </acceptance_criteria>
  <done>The 9-route class is closed by behaviour, gated by count, with per-route identity oracles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Railway edge proxy→uvicorn | client.host is the proxy, not the caller (G-11) |
| tenant claim vs claimless callers | claimless traffic shares documented platform buckets |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1-19 | Denial of Service | 9 accidentally-platform-wide buckets | mitigate | rekey to tenant-or-platform; identity oracles |
| T-140.1-20 | Denial of Service | raising key_func → 500 | mitigate | never-raise shape reused from Plan 06 |
| T-140.1-SC | Tampering | package installs | accept | no packages installed in this plan |
</threat_model>

<verification>
- 9/9 identity oracles green; zero get_remote_address in routers/; full suite green; mypy clean.
</verification>

<success_criteria>
PYAPI-03 closed: no router-declared limiter, no request-address keying, documented per-flow cost.
</success_criteria>

<output>
Create `.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-07-SUMMARY.md` when done.
Record: the 9/9 tally, grep outputs, Phase-146 and 140.2 obligations.
</output>
