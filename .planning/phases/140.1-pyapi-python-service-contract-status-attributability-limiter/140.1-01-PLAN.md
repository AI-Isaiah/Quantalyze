---
phase: 140.1-pyapi-python-service-contract-status-attributability-limiter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql
  - supabase/migrations/<new-timestamp>_strategy_verifications_tenant_scope_uniq.sql
autonomous: true
requirements: [PYAPI-01]
must_haves:
  truths:
    - "Two tenants submitting the same wizard_session_id for their own (different) strategies both succeed — no 23505"
    - "The SQL gate FAILS on today's schema and PASSES on the migrated schema, and it runs in CI (sql-tests job)"
    - "The old single-column unique index is gone; the new composite index enforces (strategy_id, wizard_session_id)"
  artifacts:
    - path: "supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql"
      provides: "CI gate A1-A5 for tenant-scoped uniqueness"
    - path: "supabase/migrations/<ts>_strategy_verifications_tenant_scope_uniq.sql"
      provides: "Composite unique index + drop of the mis-scoped index"
  key_links:
    - from: "supabase/tests/test_*.sql"
      to: ".github/workflows/ci.yml sql-tests job"
      via: "auto-discovery of test_*.sql (ci.yml:692-838)"
      pattern: "test_strategy_verifications_wizard_session_tenant_scope"
---

<objective>
Close the SQL half of PYAPI-01 (C-08, the programme's only CRITICAL): replace the single-column
`strategy_verifications_wizard_session_id_unique_idx` with a tenant-scoped
`UNIQUE (strategy_id, wizard_session_id)` index, gated by a `supabase/tests/test_*.sql` file that is
RED on today's schema.

Purpose: a colliding `wizard_session_id` currently rejects a legitimate tenant's insert on a foreign
collision. The Python query half (the other half of the class) is Plan 140.1-02 — BOTH must land or
the class is not closed.
Output: one migration + one CI-gated SQL test, both applied/verified against the TEST project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-CONTEXT.md
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-RESEARCH.md  (Q3 section + "Migration-ordering hazard" + "CONCURRENTLY" section)
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-PATTERNS.md  (SQL migration analog + SQL test analog sections)
</context>

<critical_warnings>
1. **The existing migration's own comment argues the buggy shape is correct.**
   `supabase/migrations/20260510173005_process_key_long_idempotency_drain.sql:72-73` says
   "wizard_session_id is UUID NOT NULL — plain UNIQUE INDEX (no partial WHERE clause) is correct".
   That reasoning is the defect — it ignored tenancy. Do NOT "fix" toward the comment. The new
   migration's `COMMENT ON INDEX` must explicitly supersede that reasoning.
2. **`strategy_verifications` has NO `user_id` column** (columns per
   `migrations/20260501055202_strategy_verifications.sql:77-98`). The constraint is
   `UNIQUE (strategy_id, wizard_session_id)` — Option A from RESEARCH Q3. Do not add a user_id
   column (Option B was evaluated and rejected: backfill + two write-path changes).
3. **Do not reuse the old index name.** `CREATE UNIQUE INDEX IF NOT EXISTS <old name>` is a silent
   no-op against the old single-column definition. New name:
   `strategy_verifications_strategy_wizard_session_uniq`.
4. **New index BEFORE dropping the old one.** Reversed order opens a window with no uniqueness.
5. **Migrations auto-apply to PROD on merge to `main`.** This is a production event. Apply to TEST
   (`qmnijlgmdhviwzwfyzlc`) via Supabase MCP `apply_migration` BEFORE the PR so the `sql-tests` CI
   job (which runs against TEST) is green; then fix the `schema_migrations` stamp (MCP stamps
   `now()`, not the filename timestamp).
6. **Teaser rows all anchor on `TEASER_ANCHOR_STRATEGY_ID`** (`routers/process_key.py:634`) —
   harmless under the composite index because the teaser mints a fresh uuid4 per call (`:635`).
7. **Deploy-skew window (accepted — state it, don't just "watch"):** the index swap auto-applies
   to PROD at merge, but Railway redeploys the Python service on its own schedule. In between, the
   OLD code's unscoped `.maybe_single()` pre-check (`process_key.py:725`) runs against the NEW
   composite index and can see 2 rows (→ error) if two tenants hold the SAME wizard_session_id in
   that window. Acceptable because that requires a cross-tenant uuid4 collision — negligible.
   Record this window AND the collision rationale in the SUMMARY as the PROD-apply watch obligation.
</critical_warnings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the SQL gate and prove it RED on today's schema</name>
  <files>supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql</files>
  <read_first>
    - supabase/tests/test_strategies_private_owner_isolation.sql (the analog: header-doc, pre-clean
      by fixture email before BEGIN, BEGIN…ROLLBACK wrapping, fixture-scoped counts, positive-control
      discipline at :16-23, :55-64)
    - supabase/migrations/20260501055202_strategy_verifications.sql:77-134 (columns + RLS policy —
      tenancy derives via strategy_id → strategies.user_id)
    - .planning/phases/140.1-*/140.1-RESEARCH.md "What a test that FAILS TODAY looks like" (A1-A5 table)
  </read_first>
  <behavior>
    - A1 (ECONOMIC oracle, RED today with 23505): as service_role, insert an SV row for tenant A's
      strategy with wizard_session_id W, then an SV row for tenant B's DIFFERENT strategy with the
      SAME W. Both inserts must succeed.
    - A2 (isolation, passes today): as role authenticated with B's JWT claims, count(*) on
      strategy_verifications WHERE id = A's fixture id = 0.
    - A3 (positive control, passes today): same session, count(*) WHERE id = B's fixture id = 1 —
      proves the session switch works so A2's zero is isolation, not a broken harness.
    - A4 (drift-pin, RED today): pg_get_indexdef of the live wizard-session unique index on
      strategy_verifications names BOTH strategy_id and wizard_session_id. Qualify the lookup with
      `indexdef LIKE '%wizard_session_id%'` — a SECOND unique index exists on this table
      (`strategy_verifications_public_token_unique_idx`, migration 20260510172738:66-68), so an
      unqualified "the unique index" query returns 2 rows and the assertion can read the wrong one.
    - A5 (RED today): the old name strategy_verifications_wizard_session_id_unique_idx is ABSENT
      from pg_indexes.
  </behavior>
  <action>
    Create the test as plain PL/pgSQL with RAISE EXCEPTION on failure (pgTAP is not installed;
    the sql-tests job runs psql -v ON_ERROR_STOP=1 -f). Copy the analog's hygiene verbatim:
    header comment stating WHAT and WHY BY CONTENT, defensive pre-clean of fixture users by email
    before BEGIN, everything else inside BEGIN…ROLLBACK, every count scoped to a specific fixture
    UUID (never a global count — the shared TEST DB carries other rows). Fixtures: two auth.users,
    two strategies (one per user), then the A1 insert pair. Prove RED: execute the A1 scenario
    against the TEST project (Supabase MCP execute_sql inside a rolled-back transaction, or psql if
    TEST_SUPABASE_DB_URL is available) and record the 23505. Also confirm the gate will actually
    run: repo variable E2E_TEST_DB_CONFIGURED must be 'true' (gh api
    repos/{owner}/{repo}/actions/variables/E2E_TEST_DB_CONFIGURED) — if it is not, STOP and surface
    it; the CRITICAL requirement would have no running proof.
  </action>
  <verify>
    <automated>psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql ; test $? -ne 0  # RED pre-migration (fallback: MCP execute_sql of the A1 pair, expect 23505)</automated>
  </verify>
  <acceptance_criteria>
    - File exists and is auto-discoverable (name matches supabase/tests/test_*.sql).
    - All five assertions A1-A5 present; A1's expected values are literals (fixture UUIDs declared in
      the file), not read back from the index under test.
    - Pre-migration run FAILS (A1 raises 23505; A4/A5 raise) — failure output captured in the SUMMARY.
    - A3 positive control present — A2 cannot pass vacuously.
    - E2E_TEST_DB_CONFIGURED confirmed 'true' (recorded in SUMMARY).
  </acceptance_criteria>
  <done>SQL gate committed, proven RED against the unmigrated TEST schema, CI wiring confirmed live.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the tenant-scope migration and prove the gate GREEN</name>
  <files>supabase/migrations/<new-timestamp>_strategy_verifications_tenant_scope_uniq.sql</files>
  <read_first>
    - supabase/migrations/20260510173005_process_key_long_idempotency_drain.sql:40-89 (the analog:
      SET lock_timeout='3s' inside BEGIN, duplicate pre-flight DO block with ERRCODE
      unique_violation, COMMENT ON INDEX, self-verify block; and :72-73 — the comment being superseded)
    - .planning/phases/140.1-*/140.1-RESEARCH.md "CONCURRENTLY — needed, and the trap" (decision rule
      + 3-file split shape if needed)
  </read_first>
  <action>
    First measure: run SELECT count(*) FROM strategy_verifications against PROD
    (Supabase MCP execute_sql, project khslejtfbuezsmvmtsdn, read-only) and record the number.
    If under ~100k (expected — the 20260510173005 comment says the build completed <1s at then-current
    counts): single transactional migration — BEGIN; SET lock_timeout='3s'; duplicate pre-flight DO
    block retargeted at (strategy_id, wizard_session_id) mirroring 20260510173005:55-67; CREATE
    UNIQUE INDEX strategy_verifications_strategy_wizard_session_uniq ON strategy_verifications
    (strategy_id, wizard_session_id); THEN DROP INDEX strategy_verifications_wizard_session_id_unique_idx;
    COMMENT ON INDEX explicitly superseding the 20260510173005:72-73 single-column reasoning
    (name the old migration, state why tenancy requires the composite); self-verify DO block; COMMIT.
    If ≥100k: use the 3-file CONCURRENTLY split from RESEARCH (pre-flight file, non-transactional
    CONCURRENTLY file with pg_index.indisvalid assertion + INVALID-index cleanup step, drop-old file)
    — never CONCURRENTLY inside a transactional file (error 25001).
    Never edit shipped migration 20260510173005; this is a NEW file layered on it.
    Then: apply to TEST via MCP apply_migration; correct the schema_migrations version stamp to the
    filename timestamp; re-run Task 1's gate — now GREEN.
  </action>
  <verify>
    <automated>psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql  # GREEN post-migration (fallback: MCP execute_sql of A1/A4/A5 assertions)</automated>
  </verify>
  <acceptance_criteria>
    - PROD row count measured and recorded in SUMMARY; form of migration (transactional vs
      CONCURRENTLY) justified by that number against the ~100k rule.
    - New index name is strategy_verifications_strategy_wizard_session_uniq (NOT the old name).
    - CREATE precedes DROP in the file; lock_timeout and duplicate pre-flight present.
    - Applied to TEST; schema_migrations row carries the filename timestamp, not now().
    - Task 1's gate is GREEN on TEST post-migration; A5 confirms the old index is gone.
    - Mutation check M1 (from RESEARCH): reverting the index to single-column on a scratch
      transaction makes A1/A4 RED again — confirmed and reverted (record in SUMMARY).
  </acceptance_criteria>
  <done>Migration committed + applied to TEST with corrected stamp; SQL gate GREEN; PROD apply will occur automatically at merge (watch obligation — including the warning-7 skew window and its uuid4-collision rationale — recorded in SUMMARY).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| tenant→tenant via wizard_session_id | caller-supplied UUID collides across tenants |
| CI→TEST DB | sql-tests job executes against shared live TEST project |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1-01 | Information Disclosure | strategy_verifications unique index | mitigate | composite (strategy_id, wizard_session_id) index; SQL gate A1-A5 |
| T-140.1-02 | Denial of Service | legitimate insert rejected on foreign collision | mitigate | same composite index (A1 is the economic oracle) |
| T-140.1-03 | Tampering | shared TEST DB pollution by the gate | mitigate | pre-clean + BEGIN…ROLLBACK per the analog file |
| T-140.1-SC | Tampering | package installs | accept | no packages installed in this plan (SQL only) |
</threat_model>

<verification>
- `supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql` exists, was RED
  pre-migration (captured), GREEN post-migration.
- Old index absent, new composite index present and valid on TEST.
</verification>

<success_criteria>
Phase success criterion 1 (SQL half): the uniqueness constraint is tenant-scoped and an RLS/SQL gate
under supabase/tests/ fails if it is not. The Python query half is Plan 140.1-02.
</success_criteria>

<output>
Create `.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-01-SUMMARY.md` when done.
Record: PROD row count, RED/GREEN evidence, E2E_TEST_DB_CONFIGURED status, PROD-apply watch obligation.
</output>
