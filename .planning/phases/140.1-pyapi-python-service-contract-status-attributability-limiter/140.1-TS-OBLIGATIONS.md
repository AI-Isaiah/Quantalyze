# Phase 140.1 — Cross-phase obligations ledger

**Produced by:** Plan 140.1-09 (phase gate), 2026-07-26
**Source:** the eight `140.1-0N-SUMMARY.md` files + `analytics-service/docs/STATUS_CONTRACT.md` §6
**Branch:** `feat/v1.16-production-resilience` @ `56fb7167` (never merged)

**Obligation count: 40** *(was 31; recounted 2026-07-26 by Phase **140.1.1**, which added TS-32/TS-33
and closed TS-01/TS-03; recounted 33 → 34 on 2026-07-26 by Phase **140.1.2** plan 02, which added
TS-34; recounted 34 → 35 the same day by **140.1.2** plan 03, which added TS-35; recounted 35 → 36 on
2026-07-27 by Phase **140.2** plan 09, which added TS-36 and closed TS-04; **recount held at 36 on
2026-07-27 by Phase 140.2 plan 12 (the phase gate), which added NO rows and re-dispositioned
thirteen**; **recounted 36 → 40 on 2026-07-27 by the Phase 140.2 REVIEW-FIX pass, which added
TS-37/TS-38 (140.3 — the two copy halves it was fenced out of authoring), TS-39 (141 — the two
breaker properties it bounded but could not close) and TS-40 (ops — W-3, this repo has no branch
protection, so every CI gate is advisory at merge)**)*.

**By owner, recounted 2026-07-28 by plan `140.3-15` from the §1/§2 rows themselves — not copied:**
**140.2 → 0** · **140.3 → 0** (was 2; − TS-20 and − TS-38, BOTH SATISFIED 2026-07-28 by
`140.3-15`. **The `140.3` owner bucket is now EMPTY**, and that is the phase-gate fact `140.3-16`
needs: every row this phase owned is closed. Previously 3; − TS-37, SATISFIED by `140.3-14`. Previously 5; − TS-09 and − TS-17, both
SATISFIED by `140.3-12`. Previously 7; − TS-18 and − TS-19, closed by `140.3-11`. Previously 8; −
TS-34, closed by `140.3-09`. Previously 9; − TS-32, closed by `140.3-06`. **TS-09 no longer "stays"**
— its copy half was the reason it did, and `140.3-12` wrote it) · **141 → 2** (TS-10, TS-39) ·
**146 → 4** · **145 → 2** · **ops → 5** (4 + TS-40) ·
**needs its own plan → 2** · **DONE-IN-140.1.1 → 2** (TS-01, TS-03) · **DONE-IN-140.2 → 4** (TS-04,
TS-06, TS-07, TS-16) · **owner decided by 140.2-12 → 0** (was 1; TS-33 was the sole member and
`140.3-14` EXECUTED it, so the bucket empties rather than shrinks) ·
**DONE-IN-140.3-01 → 2** (TS-05, TS-08) · **DONE-IN-140.3-02 → 6** (TS-02, TS-11, TS-12, TS-13,
TS-14, TS-15) · **DONE-IN-140.3-05 → 1** (TS-35) · **DONE-IN-140.3-06 → 1** (TS-32) ·
**DONE-IN-140.3-09 → 1** (TS-34) · **DONE-IN-140.3-11 → 2** (TS-18, TS-19) ·
**DONE-IN-140.3-12 → 2** (TS-09, TS-17) · **DONE-IN-140.3-14 → 2** (TS-37, TS-33) ·
**DONE-IN-140.3-15 → 2** (TS-38, TS-20).
Sum: 0 + 0 + 2 + 4 + 2 + 5 + 2 + 2 + 4 + 0 + 2 + 6 + 1 + 1 + 1 + 2 + 2 + 2 + 2 = **40** ✓ — the arithmetic
is written out so the count is reproducible rather than asserted, which is how the two stale prose
counts below got there.

> ⚠️ **`140.3-15`'s edit, stated so the movement is not counted twice.** TWO rows left the `140.3`
> owner bucket (**TS-38**, **TS-20**) and BOTH landed in the new `DONE-IN-140.3-15` bucket.
> −2 +2 = 0, so the TOTAL is unchanged at **40**. No row was added and none removed. The `140.3`
> bucket reads **0** as a consequence, not as a target: it was recounted from the §1/§2 rows.

> ⚠️ **Two rows moved, and neither is a new obligation, so the TOTAL is unchanged at 40.** TS-37 left
> the `140.3` bucket and TS-33 left `owner decided by 140.2-12`; both landed in the new
> `DONE-IN-140.3-14` bucket. −1 −1 +2 = 0. Stated explicitly so the movement cannot be counted twice.

> **`140.3-12`'s edit, stated so the movement is not counted twice.** TWO rows changed disposition:
> **TS-09** (PARTIALLY SATISFIED → DONE-IN-140.3-12, completing the split `140.3-01` opened) and
> **TS-17** (OPEN → DONE-IN-140.3-12). **No row was added and none removed**, so the total is
> unchanged at **40**. A dated correction note was added under §2 recording the C-9 numbers this
> plan measured first-hand; it changes no row's disposition.

> 🔴 **Two bookkeeping defects fixed in this recount, stated rather than silently corrected.** The
> previous block's prose said **140.3 → 7** while its own written-out sum used **8**, and there was no
> `DONE-IN-140.3-09` bucket at all — so TS-34 had been removed from the owner column by `140.3-09`
> without being added to a DONE bucket, and the arithmetic only reached 40 because the stale 8
> absorbed the missing 1. Both are corrected above: TS-34 now has its bucket, and the sum reproduces
> from the buckets rather than from a leftover figure. **No row was added or removed by `140.3-11`**;
> two changed disposition (TS-18, TS-19), so the total is unchanged at **40**.

> **`140.3-06`'s edit, stated so the movement is not counted twice.** ONE row changed disposition:
> **TS-32**, SCHEDULABLE(140.3) → DONE-IN-140.3-06. **No row was added and none removed**, so the
> total is unchanged at **40**. ONE row was ANNOTATED without changing owner or disposition:
> **TS-10**, which stays **OPEN and owned by Phase 141** — the annotation records only that its
> premise is now live (`424` reached the wire) and that `job_worker.py` was deliberately NOT touched,
> so Phase 141 does not inherit a false "already done". `140.3-06` is the phase's ONLY Python wave.

> **`140.3-05`'s edit, stated so the movement is not counted twice.** ONE row changed disposition:
> **TS-35**, OPEN(140.3) → DONE-IN-140.3-05. **No row was added and none removed**, so the total is
> unchanged at **40**. Two rows were ANNOTATED without changing owner or disposition: **TS-32**
> (its `BLOCKED-BY` was already annotated CLEARED by `140.3-01`; this edit records that the named
> blocker was the WRONG ONE and that the real one, TS-35, is now also gone — so the remap is
> schedulable and is `140.3-06`'s), and TS-35's own Fact cell, whose damage claim was **under-counted
> by one** (C-6: `DDOS_PROTECTION` was mis-mapping, not falling through). `140.3-03` and `140.3-04`
> closed requirement halves, not TS rows, which is why no bucket exists for either.

> ⚠️ **`140.3-02` is the RE-HOMED plan, and the six rows it closed moved TWICE. Do not count the
> move twice.** Plan `140.2-12` moved TS-02 and TS-11..TS-15 out of **140.2's** column at the 140.2
> gate (which is why that column has read **0** since). This edit moves the same six out of
> **140.3's OPEN** column and into a new `DONE-IN-140.3-02` bucket. Two different transitions on one
> set of rows — the first re-homed them, the second closed them.
>
> **TS-01 and TS-11 were VERIFY obligations, and both were verified rather than edited.** TS-01 is
> NOT moved: it has been `DONE-IN-140.1.1` since PYAPIFIX-01 and `140.3-02` only re-confirmed its
> three compensating invariants still hold. Counting it again here would inflate the ledger by one.
> TS-11 IS moved, because it was still sitting open in 140.3's column awaiting exactly that
> confirm-and-document.

> ⚠️ **Two prose counts were stale at once and BOTH are superseded by the row-by-row truth above.**
> `140.2-CONTEXT.md` said *"31 obligations, 16 addressed to this phase"*; this header said
> *"35 … 140.2 → 14"* and then *"36 … 140.2 → 13"*. **§1's table is the authority.** Plan 140.2-09's
> `140.2 → 13` was arithmetically CORRECT for its moment (the 13 §1 rows still open and owned by
> 140.2: TS-02, TS-05..TS-16). Plan 12 closes 3 of them (TS-06, TS-07, TS-16), re-homes 9 to 140.3
> and 1 to Phase 141, which is why the column is now **0**: `13 − 3 − 9 − 1 = 0`.
>
> **The nine rows that left 140.2's column are RE-HOMED, never SATISFIED.** Marking a re-homed row
> satisfied at this phase's close would be the worst possible outcome of the move — an obligation
> with a tick and no work behind it.

> **Edited by Phase 140.1.1 on 2026-07-26** per this file's own rule *"if an item here is wrong,
> correct it here — do not fork a second list."* Rows touched: **TS-01** (DONE), **TS-02**
> (remedy sharpened, owner unchanged), **TS-03** (DONE), **TS-23** (annotated UNBLOCKED, still
> open), plus **TS-32**/**TS-33** added. **TS-04..TS-22 and TS-24..TS-31 are byte-unchanged.**

> **Edited by Phase 140.1.2 on 2026-07-26**, under the same rule — **TS-34** was added in this file's
> own ID space rather than forked into a parallel `OB-N` list (its OB-9 provenance is cited in the row
> body instead), and the count header above was recounted in the SAME edit: **33 → 34**, `140.3` **4 →
> 5**. Rows touched: **TS-23** (annotated in place — a third 429 shape now exists and PYAPIFIX2-04
> added `Retry-After` to the bare-scalar sites; still open, owner unchanged), plus **TS-34** added.
> **Every other row is byte-unchanged.**
>
> **Edited again by Phase 140.1.2 plan 03 on 2026-07-26** (PYAPIFIX2-01), under the same rule.
> **TS-35** was added in this file's own ID space — **not** as a forked `OB-N` row; its **OB-1**
> provenance is cited in the row body, exactly as plan 02 did for TS-34's OB-9. The count header was
> recounted in the SAME edit: **34 → 35**, `140.3` **5 → 6**. Rows touched: **TS-32** (corrected in
> place — its `:505` coordinate was stale by 18 and its member list was INCOMPLETE by three; it is now
> annotated **PARTIALLY SATISFIED**, with only the `400 → 424` remap still owed and still
> `BLOCKED-BY: TS-05`), plus **TS-35** added. **Every other row is byte-unchanged.** ⚠️ TS-32's file
> references are now given as **code text, not line numbers**: 140.1.2's own plans invalidated those
> coordinates twice inside a single phase.


> **Edited by Phase 140.2 plan 09 on 2026-07-27** (TS-04), under this file's own rule *"if an item
> here is wrong, correct it here — do not fork a second list."* **TS-36** was added in this file's own
> ID space, and the count header was recounted in the SAME edit: **35 → 36**, `140.2` **14 → 13**,
> `146` **3 → 4**. Rows touched: **TS-04** (marked DONE, and its "all nine" claim corrected in place —
> the behaviour-derived answer is **6 reachable from `analytics-client`, 5 live**), plus **TS-36**
> added. **Every other row is byte-unchanged.**
>
> **Edited by Phase 140.2 plan 12 on 2026-07-27 — the phase GATE**, under the same rule. **NO row was
> added and NO row was deleted**; the count stays 36 and the by-owner columns were recounted in the
> SAME edit (see above). Thirteen rows were re-dispositioned so that **no row owned by 140.2 is left
> reading "open" at the phase's close** — a row still open at a phase gate is an obligation with no
> owner, which is exactly how TS-04 nearly went unowned:
> · **CLOSED here:** TS-06, TS-16 (both executed by plan 140.2-06), TS-07 (NEGATIVE — confirmed still
>   correct, no change made, and that IS its terminal state).
> · **RE-HOMED → 140.3, never SATISFIED:** TS-02, TS-11, TS-12, TS-13, TS-14, TS-15 (→ `140.3-02`)
>   and TS-05, TS-08, TS-09 (→ `140.3-01`). Both plans already exist in `.planning/phases/140.3-…/`.
> · **RE-HOMED → Phase 141:** TS-10 (a PYTHON edit owned to a TypeScript-only phase on a false
>   premise — see the row).
> · **RE-DISPOSITIONED, still open:** TS-23 (140.2's half DONE, the rest stays with 146), TS-26
>   (ops — hand-off, do NOT build), TS-32 (**still BLOCKED**, and its unblocker is `140.3-01`, which
>   has NOT run), TS-33 (**owner DECIDED: 140.3**).
> **Every other row is byte-unchanged.**
>
> **Edited by plan `140.3-01` on 2026-07-27**, under this file's own rule *"if an item here is wrong,
> correct it here — do not fork a second list."* **NO row was added and NO row was deleted**; the
> count stays **40** and the by-owner columns were recounted in the SAME edit (above): `140.3`
> **18 → 16**, new bucket **DONE-IN-140.3-01 → 2**. Rows touched: **TS-05** (SATISFIED, **and its
> MEMBER LIST corrected — see below, it named a non-member**), **TS-08** (SATISFIED, six cases),
> **TS-09** (PARTIALLY SATISFIED — type half landed, copy half owed), **TS-32** (its
> `BLOCKED-BY: TS-05` condition annotated CLEARED; owner unchanged), **TS-34** (annotated — half
> landed, half still owed). **Every other row is byte-unchanged.**
>
> ⚠️ **A NOTE ON THE RE-HOME AND THESE TOTALS, stated rather than silently arithmetic'd.** The plan
> that produced this edit was written expecting to move TS-05/TS-08/TS-09 *out of 140.2's column and
> into 140.3's*. **That move had already happened** — plan 140.2-12 did it at the 140.2 gate and
> `140.2` has read **0** since. So this edit moves them out of **140.3's OPEN column** instead, which
> is a different transition on the same three rows. Both are recorded so a reader does not count the
> re-home twice.
>
> **Edited by plan `140.3-03` on 2026-07-27 — a NOTE ONLY, and the count is deliberately UNCHANGED.**
> **NO row was added, NO row was deleted and NO row's disposition was changed**; the count stays **40**
> and every by-owner column is byte-unchanged. That is stated explicitly because the edit *looks* like a
> closure and is not: `140.3-03` closes the REQUIREMENT `SEAMUX-07` (B-14, the fail-OPEN publish gate),
> and **B-14 never had a TS row** — so there is nothing here to move out of an open column. The single
> addition is a dated correction note under **§2** recording that review finding **C-05** was FALSE and
> that the unchecked-cast class had **two** members, both closed. It is filed here rather than forked
> because this file is what the downstream phases read instead of re-deriving. **TS-34's row is
> untouched** — its remaining half (429 / `Retry-After` at `keys/[id]/permissions`) is still owed to
> plan `140.3-09`.

**One obligation is purely NEGATIVE — TS-07: X-6's one-line 429 fix must NOT be scheduled.** Two more
carry a negative half: **TS-31** (do NOT schedule a "fix the six broken tests" sweep — none of them is
broken) and **TS-11** (no change is strictly required at the choke point). A negative obligation is
worth exactly as much as a positive one here, because the cost this programme is trying to avoid is
unnecessary work colliding with necessary work — that is literally how the 37 discarded commits
happened.

> **Why this file exists.** The five discarded fix batches collided because each re-derived the
> contract from the findings doc instead of reading what the previous batch actually shipped. This is
> the artifact 140.2 / 140.3 / 145 / 146 read INSTEAD of re-deriving. If an item here is wrong,
> correct it here — do not fork a second list.

> **Line numbers were re-verified against source on 2026-07-26**, not copied blind from the
> SUMMARYs. Two had drifted and are corrected below with the drift noted (TS-11, TS-15).

---

## How to read this

| Column | Meaning |
|---|---|
| **ID** | stable handle; cite it in the owning phase's plan |
| **Owner** | the phase that must act. `ops` = founder/infra, not a code phase |
| **Kind** | `change` (edit required) · `verify` (spot-check, may be a no-op) · `NEGATIVE` (do NOT do) · `record` (carry the fact) |

---

## 1. Contract readers — Phase 140.2 (SEAMCORE)

### The four that will break something if skipped

| ID | Kind | File:line | What breaks TODAY | What it must become |
|---|---|---|---|---|
| **TS-01** ✅ | change — **DONE-IN-140.1.1** (owner was 140.2; re-owned to **140.1.1** and executed there as PYAPIFIX-01) | **MOVED:** the predicate no longer lives in `route.ts`. It is now `src/lib/process-key-onboard-contract.ts` — a dependency-free leaf, both symbols exported, ONE implementation repo-wide. `route.ts` imports it (`:22`) and calls it (`:1350`). | Inside `isProcessKeyOnboardResponse`, the `queued === true` branch did `if ("code" in r \|\| "idempotent" in r) return false;`. Plan 140.1-02 made a resumed wedge reply `{queued:true, code:"WIZARD_DUPLICATE", idempotent:true, ok:true, job_state:"enqueued"}` — the **normal** reply now. The guard **rejected** it → **502 + Sentry `"process-key onboard contract violation"`** on a successful resume. **Latent, never live** (no caller sent `wizard_session_id` — see TS-33). | **DONE.** The union was widened so the `queued:true` arm admits optional `code`/`idempotent`, and the mixed-envelope rejection was DELETED — `queued` (a job fact) and `code`/`idempotent` (a submission fact) are orthogonal, and `_resume_duplicate_job` exists to produce the state where both are true. ⚠️ **140.2: do NOT re-widen further.** The same edit bought the teeth back with THREE compensating invariants, each pinned by a negative fixture case: **(1)** `code`, when present (either arm), must be a **non-empty** string; **(2)** `idempotent`, when present, must be exactly `true` **and** paired with `code === "WIZARD_DUPLICATE"` (the converse is deliberately not enforced); **(3)** `verification_id` stays **required as a string** on the `queued:true` arm. The `ok` + `job_state` discriminant was **not** adopted: both TS forward arms still drop `job_state` (`route.ts:1352-1361` / `:1364-1375`), so adopting it is a separate, larger change — 140.2's call, and `route.test.ts` now pins the current forwarding so it cannot change silently. **Bound to a shared artifact:** `analytics-service/tests/fixtures/process_key_onboard_contract.json` (COMMITTED — the `python` and `frontend-test` CI jobs have no `needs:` edge) is read by `tests/lib/process-key-onboard-contract-parity.test.ts` (the real predicate, zero mocks) **and** `analytics-service/tests/test_process_key_onboard_contract.py` (the real full-stack reply). Neutering either side reddens the other's suite — proven both directions, first-hand, 2026-07-26. |
| **TS-02** ✅ | change — **SATISFIED by `140.3-02`** (2026-07-27). *(Was: RE-HOMED → 140.3 by the developer scope decision 2026-07-26, recorded by plan 140.2-12 at the 140.2 gate.)* **DONE:** the branch is re-pointed at `code === "WIZARD_DUPLICATE"` ALONE — the safer of the two options this row offered, taken because PYAPI-09d's "resync cannot structurally emit the code" was not re-verified at source and deleting a branch on an unverified premise is the larger risk. The surrounding comment block, which documented the `queued === false` conjunct AS THE CONTRACT, was rewritten in the SAME commit; this row's own warning ("fix both or the guard grows back", the TS-01/TS-03 receipt) is why. ⚠️ **One consequence this row did not anticipate:** the duplicate branch hardcoded `queued: false` in its RESPONSE, so re-pointing it alone would have reported `queued: false` on a resumed wedge — a false statement about a job that IS enqueued, i.e. the lie moved rather than removed. The response now FORWARDS `upstream.queued`. Covered by a failing-without-the-fix case (the resumed wedge) plus two pins: the no-job duplicate (proves the branch WIDENED) and a fresh enqueue with no code (proves it did not over-widen). **Ledger M53 OBSERVED RED and reverted.** | `src/app/api/keys/sync/route.ts:438` | `if (upstream.queued === false && upstream.code === "WIZARD_DUPLICATE")`. **Triply stale.** (a) PYAPI-09d makes resync structurally unable to emit `WIZARD_DUPLICATE` (no caller-supplied session id ⇒ the pre-check cannot hit); (b) when the code *is* emitted, `queued` may now be `true`; (c) every 200 now carries `ok`. The branch is dead code that nonetheless documents a state the service can no longer produce. | **Sharpened by 140.1.1 (owner stays 140.2):** re-point it at **`code === "WIZARD_DUPLICATE"` alone, dropping the `queued` conjunct**. That conjunct is now provably wrong in both directions — TS-01 established that `queued:true` and `WIZARD_DUPLICATE` co-occur on the resumed-wedge reply, so `queued === false &&` silently skips the duplicate branch on exactly the case that most needs it. Deleting the branch outright remains acceptable **only** if resync is confirmed structurally unable to emit the code (PYAPI-09d); re-pointing is the safer of the two and costs the same. Do not leave it asserting a dead contract. |
| **TS-03** ✅ | change — **DONE-IN-140.1.1** (owner was 140.2; re-owned to **140.1.1**) | **MOVED with the code** into `src/lib/process-key-onboard-contract.ts`. ⚠️ The comment block's true extent was **`route.ts:1406-1424`**, wider than the `:1419-1420` cited here — the whole block moved. | Documented `{queued:true, code:WIZARD_DUPLICATE}` as an **ILLEGAL** combination. It is the normal resumed-wedge reply. This comment is what TS-01's guard was written from — fix both or the guard grows back. | **DONE — inverted in the SAME commit as TS-01** (`2f776271`), exactly as this row's own warning required. The comment now states that `queued:true` + `WIZARD_DUPLICATE` means *"a duplicate submission whose wedged job we re-enqueued"*, names the two orthogonal facts, enumerates the three shapes the contract returns, and carries a do-NOT-widen-further note pointing at the three invariants and the shared fixture. |
| **TS-04** ✅ | change — **DONE-IN-140.2-09** | `src/lib/analytics-client.ts` (the header assembly — and, it turned out, NOT a one-liner: `analyticsRequest`'s options were budget key, timeout, method and correlation id, with **no user id anywhere in the module**, and the MAC key `INTERNAL_API_TOKEN` was not read by that file at all. Landing it meant a new shared module `src/lib/tenant-claim.ts`, a required `tenantId` option, an identity threaded into four wrapper signatures and eight route call sites) | Mints **no** `X-Tenant-Claim`. ⚠️ **"All nine" was WRONG and is corrected here per this file's own rule.** The behaviour-derived answer — a `partial(tenant_or_platform_key, scope=…)` sweep of `analytics-service/routers/` cross-referenced against a TS path grep — is **6 reachable from `analytics-client`, 5 live** (L-1 validate-key, L-2 encrypt-key, L-4 optimize-weights, L-7 portfolio-optimizer, L-8 portfolio-bridge) **+ 1 dead** (L-6 portfolio-analytics: the wrapper exists, zero production callers). **L-3 fetch-trades, L-5 csv/validate and L-9 verify-strategy are unreachable from this client by construction** and TS-04 does not touch them — they need their own owner. The reachable routes ran in `platform:<route-path>` buckets — safe and honestly named, but **not** per-tenant isolation. The sharpest is L-9 `/api/optimize-weights` at **20/minute platform-wide**: two allocators running Scenario Composer concurrently can still 429 each other. | Mint the same `<payload>.<exp>.<hex-hmac-sha256>` claim `src/lib/process-key-client.ts` already ships (HMAC-SHA256 over `"<payload>.<exp>"`, key = `INTERNAL_API_TOKEN`, TTL 300s). ✅ **DONE 2026-07-27 by plan 140.2-09, with ZERO Python changes** — no decorator, no key function, no limit value moved. The mint was extracted verbatim into `src/lib/tenant-claim.ts` (ONE implementation repo-wide, four fail-loud refusals added: empty payload, whitespace-only payload, dotted payload, absent secret) and `analyticsRequest` gained a REQUIRED `tenantId`, so **all nine wrappers** mint — including the three whose Python routes have no limiter (`match/recompute`, `match/eval`) or an IP-keyed one (`simulator`), where the claim is inert, harmless and forward-compatible. It flips the **six reachable** routes, not nine. ⚠️ Two riders: **(a)** the secret is read at CALL time and its absence is LOUD — an empty secret still produces a syntactically valid claim that fails `compare_digest`, so a silent fallback to `platform:<path>` was the real risk (RESEARCH A3 could not confirm `INTERNAL_API_TOKEN` is set in the Vercel production env; **that ops check is still owed**). **(b)** The cross-language parity gate is HALF-BUILT — see the new **TS-36**. |

### The status/shape contract

| ID | Kind | File:line | What breaks TODAY | What it must become |
|---|---|---|---|---|
| **TS-05** ✅ | change — **SATISFIED by `140.3-01` on 2026-07-27** (re-homed → 140.3 by a developer scope decision on 2026-07-26; the row's owner column read 140.2 until plan 140.2-12 moved it). The class is **CLOSED at 3 of 3** — **but it is a DIFFERENT 3 than this row originally named, and correcting the membership is the substantive half of this edit.** ⚠️ **THE BEHAVIOURAL PREDICATE, written down so the next reader does not re-derive it from a syntax grep: "does this site read a SEAM response body?" — not "does this site contain the characters `detail ??`".** That syntax grep is what put a non-member in this row, and it is the recurring defect class of this programme. · **DROPPED — `ScenarioCommitDrawer.tsx` is NOT a member** (correction C-2, verified first-hand): (a) the row's path `src/components/scenario/…` does not exist — the file is at `src/app/(dashboard)/allocations/components/…` — and (b) the path was the lesser error. Its `body.detail ??` read is gated on `res.status === 409` **AND** `code === "portfolio_fingerprint_stale"`, and `src/app/api/allocator/scenario/commit/route.ts` **imports no seam module at all** (it is not one of the 15 seam routes). The `detail` it reads is a hand-written scalar that route emits itself, and the nested envelope is structurally unreachable there — the branch requires `code` at the TOP level; the nested envelope puts it at `body.detail.code`. **The file was not modified by `140.3-01`.** · **ADDED — `src/components/portfolio/PortfolioImpactPanel.tsx` IS a member** (correction C-3): it fetches `/api/simulator`, a real seam route. It was mis-cited as *"the safe fix-shape template"* across **three** documents while being the worst member of the class: `ErrorResponseSchema.safeParse(raw)` ran two lines above its `typeof body.detail === "string"` check and `src/lib/api/errorSchema.ts` declares `detail: z.string().optional()`, so a nested envelope **failed the parse**, collapsed to `{}`, and `error` / `retryAfter` / `detail` were discarded together before the check was reached. The user saw `HTTP 500`. | the three Class-5 sites, **corrected**: `src/lib/analytics-client.ts` (`error.detail ?? "Analytics service error"`), `src/app/api/keys/[id]/permissions/route.ts` (``err.detail ?? `Upstream ${res.status}` ``), `src/components/portfolio/PortfolioImpactPanel.tsx` (the body emptied by `const body = parsedError.success ? parsedError.data : {};`). *(Coordinates given as code text, not line numbers — 140.2 moved two of these three files.)* | **`body.detail` is now an OBJECT** on every deliberate 4xx/5xx from `service_error()` — `{code, dependency, retryable, detail}`. All three sites do `err.detail ?? "…"`, so they render **`"[object Object]"`** for those responses. STATUS_CONTRACT §6 **O-5**. | Read the human string from `body.detail.detail` and the machine code from `body.detail.code`. **See TS-07 — the SAME lines are correct as-is for 422/429.** One line, two contracts: branch on `typeof body.detail`. ✅ **DONE via the ONE leaf** — `seamHumanMessage` / `seamErrorCode` from `src/lib/seam-discriminator.ts` (140.2-06), never a second hand-rolled extractor. At `PortfolioImpactPanel` the leaf is applied to the RAW parsed body **above** the narrowing schema; `src/lib/api/errorSchema.ts` was deliberately NOT widened (it has exactly one production consumer, and re-shaping an *error*-body schema for one caller is the documented Pitfall-3 hazard). TS-07's negative obligation held: no 429 human string moved. |
| **TS-06** ✅ | change — **DONE-IN-140.2-06.** `src/lib/seam-discriminator.ts` — a dependency-free leaf with the full verdict table. **O-1** 424 ⇒ `caller-exchange`, never counts, and its venue slug NEVER becomes a key (falsifier **M38**, RED at the final tree, 3 cases). **O-2** the breaker keys on the named `dependency` and only on a counting 503, membership-checked against a frozen closed set; per-dependency keys checked in ONE `mget` over each budget row's declared set (falsifiers **M35**, **M39**, both RED). **O-3** `retryable:false` and 500 never count (`SERVICE-PERMANENT: a 500 records ZERO, including the bodyless text/plain shape`). | 140.2's discriminator (SEAMCORE-01) | `424` is a NEW status on this seam (`EXCHANGE_*` — the caller's venue failed, not us). A discriminator that buckets all 4xx as "caller fault, don't retry" will make a Binance blip permanently un-retryable; one that buckets it as 5xx will trip the breaker on someone else's outage. | **424 = recoverable AND non-counting.** STATUS_CONTRACT §6 **O-1**. Also **O-2**: the breaker keys on the named `dependency`, never globally. Also **O-3**: `retryable:false` and `500` NEVER count. |
| **TS-07** 🚫✅ | **NEGATIVE — SATISFIED BY NOT DOING IT, confirmed at the 140.2 gate.** X-6's one-line 429 fix was NOT scheduled and NOT written by any of this phase's twelve plans. A negative obligation is worth exactly as much as a positive one here, and this is its terminal state. | `src/lib/analytics-client.ts:179` | **X-6's one-line 429 fix is UNNECESSARY. DO NOT SCHEDULE IT.** Plan 140.1-08 kept `detail` a **scalar string** at the TOP level on both the 422 and the 429 (deliberately — that is exactly the property that makes them need zero TS change). `error.detail ?? "Analytics service error"` already produces the correct human copy. | No change. Spend the budget on TS-05 instead, which is the half that IS broken. |
| **TS-08** ✅ | verify — **SATISFIED by `140.3-01` on 2026-07-27.** **Six cases, written out per site rather than parameterised**, because the whole point of this row is that a verify exercising one path certifies the wrong half — so both halves must be visible per site in the test file. Both fixture bodies are HAND-TYPED in each test; no shape is imported from a module under test. · `src/lib/analytics-client.test.ts` — **(1)** `service_error` 500 (OBJECT `detail`) ⇒ the message is `body.detail.detail` and `seamCode` is `body.detail.code`; **(2)** app-global 429 (SCALAR `detail`) ⇒ the message is byte-identical to its pre-plan value. · `src/app/api/keys/[id]/permissions/route.test.ts` — **(3)** 500 ⇒ the operator log carries the human sentence, never `"[object Object]"`; **(4)** 429 ⇒ the logged sentence is byte-identical. · `src/components/portfolio/PortfolioImpactPanel.test.tsx` — **(5)** 500 ⇒ renders `body.detail.detail`, not `HTTP 500`; **(6)** 429 ⇒ the rendered sentence is byte-identical and the Retry button still disables. ⚠️ **The three 429 cases were GREEN before the change and stayed GREEN — that IS the evidence for TS-07's NEGATIVE obligation, not an absence of evidence.** | the three sites, **corrected per TS-05 above**: `analytics-client.ts`, `keys/[id]/permissions/route.ts`, `PortfolioImpactPanel.tsx` — **NOT `ScenarioCommitDrawer.tsx`**, which this row also named and which is not a class member (C-2) | The plan's explicit ask: **spot-check each against the real envelope, do not assume correct-by-construction.** These three lines are hit by BOTH contracts — scalar `detail` (422/429, TS-07: fine) and object `detail` (`service_error` 4xx/5xx, TS-05: broken). A verify that only exercises one path will certify the wrong half. | Drive each site with one 429 AND one `service_error` 500 fixture. |
| **TS-09** ✅ | change — ✅ **SATISFIED 2026-07-27. THE SPLIT IS NOW COMPLETE AND IT TOOK BOTH PLANS: `140.3-01` landed the TYPE half (the two union members + `recogniseSeamErrorCode`), `140.3-12` landed the COPY half (the final sentences).** `grep -c 'TODO-COPY-140.3-12' src/lib/wizardErrors.ts` went **5 → 0**, and the count is asserted in CI at **both** ends (`wizardErrors.test.ts` carried `toBe(5)`, now `toBe(0)` — the closing half of a two-sided check, not a weakened assertion). ⚠️ **The rider below about `RATE_LIMITED` was DISCHARGED, not dropped, and the resolution is the opposite of what the rider assumed.** The rider says DESIGN.md requires the threshold attached, so the copy must NAME the real wait. `140.3-09` made the wait representable (`WizardErrorContext.retryAfterSeconds` → the envelope's `retry_after_seconds`) — but the number belongs to the RENDERER, not the copy table: `ErrorEnvelope` prints "Try again in Ns." from the server's own figure when one arrived and prints nothing when none did. A duration written into the table would be asserted unconditionally, including on the paths where no `Retry-After` was ever sent — an invented number presented as the server's, which is exactly the C-7 violation. So `RATE_LIMITED`'s copy deliberately names NO duration, and a test asserts it contains no `\d+ (seconds|minutes|hours)`. *(Prior state, kept for provenance: PARTIALLY SATISFIED by `140.3-01`.)* ✅ **LANDED (the TYPE half):** `VALIDATION_FAILED` and `RATE_LIMITED` are `WizardErrorCode` union members, and `recogniseSeamErrorCode()` is the recognition branch that maps an incoming seam code to its member instead of falling through to `UNKNOWN` (falsifier **M52**, observed RED and reverted). It is backed by a `ReadonlyMap`, not a `Record`: the code arrives over the wire, so a plain-object index resolves `constructor` / `__proto__` to inherited members and would return a `Function` typed as a `WizardErrorCode`. ⛔ **STILL OWED (the COPY half):** both `WIZARD_ERROR_COPY` entries are **minimal and explicitly NON-FINAL** — they exist only so the `Record<WizardErrorCode, …>` type-checks. Each carries the exact token **`TODO-COPY-140.3-12`**, which is the HAND-OFF MECHANISM: `140.3-12` closes this row by grepping that token to **0**. Treat both as unwritten copy, not shipped strings. The split is honest — a union member is a type, a copy string is a render — and it is stated in the file itself so neither plan assumes the other did it. ⚠️ **A rider `140.3-12` must budget for:** DESIGN.md's voice rule requires stating a limitation *with the threshold attached*, and the honest threshold for `RATE_LIMITED` is the server's `Retry-After` / `retry_after_seconds`. Neither `WizardErrorContext` nor `ErrorEnvelope` carries a wait field, so naming the real wait is currently **unrepresentable**, not merely unwritten (correction C-7). | `src/lib/wizardErrors.ts` | Neither **`VALIDATION_FAILED`** nor **`RATE_LIMITED`** existed in the `WizardErrorCode` union. ⚠️ **This row's grep claim was WRONG in one direction and right in the other, corrected here rather than repeated:** `RATE_LIMITED` did **not** have "zero hits anywhere in `src/`" — it had **7** (all in test files: `resilient-fetch.test.ts` ×2, `seam-discriminator.test.ts` ×5). It had zero hits in `src/lib/wizardErrors.ts`, which is the claim that was load-bearing and which held. The union did have `KEY_RATE_LIMIT` and `CSV_VALIDATION_FAILED`, which are different codes: `KEY_RATE_LIMIT` is the wizard's classification of an **exchange** throttle reached through the substring cascade, `RATE_LIMITED` is **our** limiter refusing the request; `CSV_VALIDATION_FAILED` is a row-level CSV rule failure, `VALIDATION_FAILED` is a request-shape 422 on the API path. Both new codes fell through to `UNKNOWN` — i.e. a throttle rendered as "something went wrong". | Add both to the union + copy table. This is the last mile of PYAPI-08: the Python side is machine-readable, the renderer still cannot read it. ◐ Type half done by `140.3-01`; copy half owed to `140.3-12` alongside TS-35. |
| **TS-10** 🔀 | change — ⚠️ **RE-CONFIRMED STILL OPEN 2026-07-27 by `140.3-11`: `_HTTP_TRANSIENT_4XX` was NOT touched, and `140.3-11` modified ZERO files under `analytics-service/` (zero `.py`, zero Python docs). `140.3-06` remains the phase's only Python wave. Phase 141 inherits this row unchanged, including its instruction to RE-VERIFY — not inherit — the claim that no worker path reaches these two routers.** **RE-HOMED → Phase 141, by plan 140.2-12 at the 140.2 gate. NOT executed here and NOT dropped.** ⚠️ **STILL OPEN, AND ITS PREMISE IS NOW LIVE — read this before assuming it was done. 2026-07-27, `140.3-06`.** `424` **is now on the wire** at all seven `VenueTransientHTTPException` sites (TS-32 satisfied, commit `1f8ad052`), so the *"it does not know `424`"* gap this row describes has stopped being hypothetical. **`job_worker.py`'s `_HTTP_TRANSIENT_4XX` was DELIBERATELY LEFT UNTOUCHED by `140.3-06`** — adding `424` to the transient set is **Phase 141's**, and touching it in the phase's only Python wave would have been a fence breach. Nothing is broken today because **no worker path reaches these two routers**, which is exactly the condition that makes the deferral safe; Phase 141 must re-verify that condition rather than inherit it. **Do NOT assume this was closed alongside TS-32.** ⚠️ **This row was owned to 140.2 on a FALSE premise.** Its stated justification is *"140.2 owns the retry semantics"* — but retry is **Phase 141** (SEAM-05/06), and `140.2-CONTEXT.md` fences 140.2 to **TypeScript only** and forbids enabling retry (`SEAM_RETRIES` stays 0 and is pinned NEGATIVELY — verified 0 at the gate, with all 13 per-row `retries: 0` pins green). Executing a `.py` edit here would have been a fence breach, not a deviation; dropping it would have lost a real obligation. Phase 141 owns both the retry semantics AND the worker's transient classification, so the premise is true THERE. | `analytics-service/services/job_worker.py:_HTTP_TRANSIENT_4XX` | **(Python, but a 140.2 obligation because 140.2 owns the retry semantics.)** `classify_exception` maps any `400 <= s < 500` outside its two sets to `permanent`, and it does not know `424`. No worker path reaches the routers this phase changed, so nothing is broken today — but the moment one proxies them, a venue blip permanently fails a job instead of retrying. STATUS_CONTRACT §6 **O-9**. | Add `424` to the transient set, with a test. |

### Added by Phase 140.1.1 (PYAPI-FIX)

Two obligations this ledger was missing. Both are **pairings**: each names work that is correct only
once some *other* row lands, so neither can be scheduled on its own.

| ID | Kind | Owner | File:line | Fact | What it must become |
|---|---|---|---|---|---|
| **TS-32** ✅ | change | ✅ **SATISFIED 2026-07-27 by `140.3-06` — all SEVEN sites answer `424`.** Commit `1f8ad052`. **The class was re-derived MECHANICALLY** from the `VenueTransientHTTPException` marker type (`grep -rn` over `analytics-service/ --include='*.py'`, excluding tests) — **7 raise sites across 2 files** — and NOT from the informal `C1..C7` prose list, which this ledger's own row had already been caught carrying incomplete (the three MT5 arms). Verified per-file and in BOTH directions with a **multi-line-aware** grep, because the raise sites span two lines and the single-line form `VenueTransientHTTPException(status_code=400` matches **nothing** on either tree: `exchange.py` 424 **0→6** / 400 **6→0**; `portfolio.py` 424 **0→1** / 400 **1→0**. A 6-of-7 delivery was therefore mechanically excluded rather than merely intended. **C7 was remapped despite being on a DEAD route** (`/api/verify-strategy`, no TypeScript caller) purely for CLASS INTEGRITY. **The `BLOCKED-BY: TS-05` correction is made HERE rather than forked:** the remap is **body-neutral** — `main.py`'s handler serialises `{detail, code, recoverable}` from `verdict.status_code` independent of status and the class guard already admitted the whole 4xx band — so TS-05 was never a prerequisite and the real blocker was **TS-35**, landed by `140.3-05` (correction C-4). **The coupling was closed in the SAME commit** — the 14 fixture cases, the fixture's `invariants[]` entry, and `EXPECTED_STATUS` — with **no intermediate red tree**. ⚠️ **The argued rationale was REWRITTEN, not renumbered:** the old failure message argued *for* 400 on the grounds that the TS classifier *"derives BOTH the wizard code and the returned status from the message string"*; that argument expired with TS-35, and the phrase is now absent (`grep -c 'derives BOTH'` **1→0**). The same expired argument was closed at **every other place it was argued in Python** — `exchange.py`'s C1 block, `main.py`'s handler note, and `error_contract.py`'s docstring + guard comment — rather than at the one instance the plan named. 🔴 **FINDING: the plan's coupling analysis under-counted by two files.** It named three artifacts pinning `400`; `tests/test_sfox_validate.py` (C1/C2, 5 parametrised cases) and `tests/test_mt5_validate.py` (C3/C4/C5, 3 cases) **also pin these sites** and reddened — 8 tests. Their expectations were updated with their reasons; **no case was relaxed or deleted** (TRAP-9). The plan's own Task-1 verify command was scoped to a single test file and would NOT have caught this. ⚠️ **Deliberately NOT done here:** TS-10 (`_HTTP_TRANSIENT_4XX`) and TS-18 (rendering the 424). Gates: `mypy --strict` clean (89 files), `pytest -q --cov-fail-under=80` **4778 passed / 96 skipped**, TS parity test **48 passed UNMODIFIED**, **zero** `.ts`/`.tsx` files touched. ~~🟢 **SCHEDULABLE 2026-07-27 — THE REAL BLOCKER IS NOW GONE TOO. `140.3-05` LANDED TS-35.**~~ This row's stated `BLOCKED-BY: TS-05` **named the wrong row**, and that is now settled rather than merely suspected: the seven sites raise `VenueTransientHTTPException`, whose serialiser emits `{detail, code, recoverable}` **independent of status**, so the `400 → 424` remap is body-neutral and `"[object Object]"` is structurally impossible there. TS-05 was never a prerequisite. The real blocker was the one named in the class's own docstring — *"changing the status alone buys nothing, because the status is discarded"* — i.e. **TS-35**, which landed in `140.3-05`. ⇒ **Owner of the remap: `140.3-06`, the next wave.** 🔗 **The hand-off is safe by construction, not by care:** `140.3-05`'s parity test reads `status` FROM each fixture case and contains **zero** `400`/`424` literals outside comments (asserted by a comment-excluding acceptance grep), and asserts only that the fourteen cases agree with each other on a status. `140.3-06` may therefore flip all fourteen `"status": 400` bytes plus `EXPECTED_STATUS` without invalidating a single TypeScript assertion. ~~**BLOCKER CLEARED 2026-07-27 — `BLOCKED-BY: TS-05` no longer holds, because `140.3-01` LANDED TS-05.**~~ Owner unchanged (**140.3, both halves**); the `400 → 424` remap is now **schedulable** by its owning phase. Everything below the strikethrough marker in this cell was written while TS-05 was still owed and is preserved for provenance. ⚠️ **Two riders, so the unblock is not read as "go".** (1) Correction **C-4** (verified first-hand at the 140.3 planning gate) holds that **TS-05 was never the real blocker**: the seven sites now raise `VenueTransientHTTPException`, whose serialiser emits `{detail, code, recoverable}` **independent of status**, so the remap is body-neutral and `"[object Object]"` is structurally impossible there. The real blocker is **TS-35** — `classifyKeyValidationError` derives both the wizard code AND the returned status from the message string and never reads the upstream status, so *"changing the status alone buys nothing, because the status is discarded"*. **Landing TS-05 clears the stated condition on this row; it does not by itself make the remap worth shipping.** (2) The remap reddens `EXPECTED_STATUS = 400` (asserted in 4 places) **and all 14 `"status": 400` cases in the committed fixture `validate_key_venue_transient_contract.json` — which is the exact byte-source TS-35's parity test binds to.** Sequenced wrong, the phase binds a test to bytes it then invalidates. ⇒ **Schedule as ONE unit: TS-35 first, then TS-32 (remap + fixture + `EXPECTED_STATUS` in the same commit).** ~~**OWNER NOW 140.3 (both halves) — and STILL BLOCKED-BY: TS-05.**~~ ⚠️ **Re-dispositioned by plan 140.2-12 at the 140.2 gate, correcting a claim that would otherwise have rotted:** the superseded plan-14 text asserted this row's block was *"CLEARED by plan 140.2-12"*. **It is not.** The plan that lands TS-05 was re-homed on 2026-07-26 and is now **`140.3-01`**, which has **NOT RUN**. So the `400 → 424` remap is **still owed and still blocked**, and the unblocking work is named: `140.3-01`. The remap is also PYTHON, so it could never have landed in this TypeScript-only phase regardless. Since TS-05 left 140.2's column with the re-home, **both halves of this pairing now sit with 140.3** and it should be scheduled as ONE unit there — a 424 without TS-05 is a regression, and TS-05 without the remap leaves the class half-closed. | `analytics-service/routers/exchange.py` — locate by CODE TEXT, not by line: the sFOX 429 arm (C1), the sFOX transport arm (C2), the MT5 probe-timeout arm (C3), the `Mt5AccountMismatchError` arm (C4), the `Mt5ClientError`-transient arm (C5), the `if result["error"]:` ccxt collapse (C6); plus `analytics-service/routers/portfolio.py`'s `if validation.get("error"):` collapse (C7). *(This row previously read `:145`, `:152`, `:505`. **`:505` was stale by 18** — it is a comment line inside the `except Exception` arm, not the collapse — and the list was **INCOMPLETE**: the three MT5 arms C3/C4/C5 are genuine class members that were never carved out at all. Coordinates are deliberately no longer given: 140.1.2's own plans invalidated them twice in one phase.)* | **The M-4 carve-out from PYAPIFIX-02, corrected and now PARTIALLY SATISFIED.** These sites are the *classified* venue-failure path and still answer **400**; only the escape path was remapped to 424 in 140.1.1. They are genuine class members and were deliberately deferred, not missed: migrating them to `service_error(424)` turns `body.detail` from a **scalar string into an OBJECT**, which `src/lib/analytics-client.ts:177-180` feeds into `classifyKeyValidationError` — rendering `"[object Object]"` and dead-ending on a terminal UNKNOWN. Fixing Python first would make the *user-visible* outcome worse than the wrong status code it corrects. ✅ **PARTIALLY SATISFIED by 140.1.2-03 (PYAPIFIX2-01):** all **seven** sites now emit the FLAT PYAPI-08 shape — a scalar `detail` **byte-identical** to before, plus a machine `code` and `recoverable` — so a wizard can classify these verdicts without the substring cascade and with **zero** regression (`analytics-service/tests/test_validate_key_venue_transient.py` pins all seven on the wire; the committed fixture `analytics-service/tests/fixtures/validate_key_venue_transient_contract.json` is the case table). ⚠️ **STILL OWED: the `400 → 424` remap**, unchanged and still **BLOCKED-BY: TS-05**. | **Land TS-05 FIRST** (branch on `typeof body.detail`), then remap all **seven** to `424` / `retryable:true` in the same phase. Schedule them as ONE unit — a 424 without TS-05 is a regression, and TS-05 without the remap leaves the class half-closed. Note that the remap is now the *only* thing owed here and it is **user-invisible until TS-05 lands**, because `classifyKeyValidationError` derives both the wizard code and the returned status from the message string and discards the upstream status entirely. The render half — a `WizardErrorCode` branch reading the new `body.code` — is **TS-35**, and landing TS-35 makes this remap safe. `.planning/REQUIREMENTS.md` PYAPIFIX-02 carries the same `BLOCKED-BY: TS-05` marker so the deferral cannot rot. |
| **TS-33** ✅ | change | **SATISFIED by `140.3-14` on 2026-07-27.** Owner was decided as 140.3 by plan 140.2-12 at the 140.2 gate; it was EXECUTED here, in 140.3's own pass — not retrofitted into `140.3-02`, exactly as the row required. ✅ **LANDED:** ONE field, `wizard_session_id`, inside `postProcessKey`'s `context` — the SAME placement `csv-validate` and `csv-finalize` already use and the one `process_key.py` reads from. No header, no migration, no `Idempotency-Key` port, no second mechanism. 🔴 **THIS ROW'S PREMISE WAS FALSE AS A GENERAL STATEMENT, AND IS CORRECTED IN PLACE (correction C-7 / F-2).** The row says *"no caller sent `wizard_session_id`"* and cites a `grep` returning **ZERO hits**. That grep was scoped to `finalize-wizard/route.ts` and `keys/sync/route.ts` and then generalised. **`csv-validate` and `csv-finalize` BOTH send the id today**, and Python reads it from exactly that context object, so `idempotent_by_session` has been **TRUE on the CSV path all along** and the mechanism was never dead — it was unreachable **from ONE call site**. The distinction is load-bearing: an executor who believed the mechanism dead would not have consulted the CSV path, and the CSV path is the working reference for the field's shape. **True scope: one call site.** ⚖️ **THE SOURCE IS THE DRAFT ROW, NOT THE REQUEST BODY — a deliberate improvement on the CSV path's shape, with the PLACEMENT copied verbatim.** `finalize-wizard` reads `strategies.wizard_session_id` off the owner-scoped draft SELECT it already performs (`.eq("user_id", user.id)`), so the dedupe key costs no extra query and **cannot be supplied, guessed or swapped by the caller of that request**. `create-with-key` and the composite RPC both persist the client's stable session id at draft creation (migration `20260602190000`, F6), so the value is exactly the id the draft was created under. A body field would have re-opened the caller-supplied-id surface that migration `20260726000225` (140.1 / PYAPI-01) had to scope the unique index to `(strategy_id, wizard_session_id)` in order to contain. ⚠️ **The `X-Correlation-Id` header was considered and REJECTED as the source:** it is memoized per PAGE LOAD, so a reload — the likeliest way a user double-submits — mints a new one. Keying the dedupe on a per-request value is **worse than absent** (T-140.3-14-05), and a field-presence test cannot tell the two apart. 🧪 **`null` is forwarded as ABSENCE via a conditional spread, never as a synthesised id.** The column is NULLABLE, and absence reproduces the pre-change wire body byte for byte (Python mints its own uuid4 and runs with the dedupe off — where this route already was). ✅ **The ordering constraint held and was verified, not assumed:** `tests/lib/process-key-onboard-contract-parity.test.ts` is green and `src/lib/process-key-onboard-contract.ts` is **NOT in this plan's diff** — TS-01's do-NOT-re-widen warning is honoured. **Zero migrations added.** 🔒 **REJECTED alternatives stay rejected:** the `Idempotency-Key` port (needs a migration, which auto-applies to PROD with no branch protection) and hardening the Python dedupe as dead code. | `src/app/api/strategies/finalize-wizard/route.ts` — the `postProcessKey` payload construction (the row's `:840-851` citation is stale; locate by `const result = await postProcessKey({`) | **M-11 — onboard's double-submit protection is not broken, it is UNWIRED.** Onboard has **job-level** dedupe only, and that mechanism was unreachable FROM THIS ROUTE: `process_key.py` gates `idempotent_by_session` on a caller-supplied `wizard_session_id`, and `finalize-wizard` sent none. ~~`grep` returns ZERO hits, so no caller sends it~~ ⚠️ **CORRECTED — see the disposition column: the CSV callers DO send it; the grep was scoped to two files and over-generalised.** This is also why H-5/TS-01 was latent rather than live. | Send `wizard_session_id` in the payload — **ONE field**, not a new idempotency subsystem. ⚠️ **STRICTLY ORDERED AFTER PYAPIFIX-01 / TS-01**, which is DONE, so the field was safe to add. |

### Added by Phase 140.1.2 (PYAPI-FIX2)

Two obligations. Each is the **TypeScript half** of a Python fix that landed in 140.1.2 — the Python
side is now correct on the wire in both cases, so these rows are what stand between a correct service
response and a correct user-visible outcome. Neither is a pairing: each can be scheduled on its own,
though both share a root cause (and TS-34 shares a file) with **TS-05**.

| ID | Kind | Owner | File:line | Fact | What it must become |
|---|---|---|---|---|---|
| **TS-34** ✅ | change | **SATISFIED 2026-07-27, ACROSS ITS TWO PLANS: `140.3-01` (the extraction half) and `140.3-09` (the status / `Retry-After` half). Both are named because neither alone closes it.** ✅ **`140.3-09` closed the remaining half:** a `/internal` throttle now answers **429** with the upstream's `Retry-After` **forwarded verbatim**, instead of `PROBE_FAILED` + a hardcoded 502 with the wait discarded. Mechanism, and it is deliberately NOT the one the plan prescribed — see the correction note below §2: the upstream **STATUS** and the upstream `Retry-After` now ride the thrown error's `cause` alongside the machine code `140.3-01` already put there (the stringify-to-`message` hop destroys both), and the catch branches on `status === 429` **below** the `CircuitOpenError` type check and **above** the substring cascade. The gate is the STATUS, not the wire code, which settles the two-vocabulary ambiguity `140.3-05` recorded as an open residual (`RATE_LIMITED` = our limiter at 429 in the app-global contract, = the venue throttling us at 400 in the venue-transient one) and also covers a codeless edge/WAF 429. The wait is read through the ONE parser (`parseRetryAfterSeconds`, HTTP-date safe, never NaN/0/<0) and re-published verbatim; when the upstream advertised none, **no header is attached and the copy names no duration** (TRAP-3). The substring cascade is **kept intact** (`.includes(` count unchanged at 6) as the fallback for genuinely uncoded faults, per TS-35's reasoning. The `CircuitOpenError` arm's own `Retry-After` was **already correct and was PRESERVED, not re-added** — it is pinned by a case labelled a preserve, which counts toward nothing. ⚠️ **RECORDED DECISION (envelope-fragmentation / theme 4):** this route's private `PROBE_BACKEND_UNAVAILABLE` / `PROBE_TIMEOUT` / `PROBE_FAILED` vocabulary **stays SEPARATE from `WizardErrorCode`** and `classifyKeyValidationError` is deliberately **not** adopted here. Measured, not argued: routed through that classifier, **5 of 6** of this route's real thrown messages fall to `{code:"UNKNOWN", status:500}` — the terminal "our team has been notified" dead end — because it classifies KEY-VALIDATION faults while every fault reachable here is a PROXY-INFRASTRUCTURE fault; and its `RATE_LIMITED` verdict is `KEY_RATE_LIMIT` **503**, which cannot produce the **429** this very row demands. The sharing that matters is already in place: the breaker verdict comes from the shared typed `CircuitOpenError` and the ONE `CIRCUIT_OPEN_COPY`. ⚠️ **Residual, enumerated not absorbed:** the seam envelope also carries the wait in its **body** as `retry_after_seconds`; only the **header** is forwarded, because reading the body field would require a new export on the `seam-discriminator` leaf whose `EXPECTED_EXPORTS` set-equality guard belongs to `140.3-01`. Both production 429 producers (`main.py`'s app-global handler and `routers/internal.py`'s per-key throttle) emit the header, verified at source, so the gap is a redundant second copy of the same number — not a missing wait. <br><br>*Historical record of the first half, preserved:* ✅ **The `typeof body.detail` half landed in `140.3-01`:** the `!res.ok` arm now reads the human string through `seamHumanMessage` and the machine code through `seamErrorCode`, so the accepted cosmetic regression this row records — *"the route's own `console.error` copy degrades to `"[object Object]"` on this path"* — **is repaired**; the operator log now carries the human sentence, and the machine code rides on the thrown `Error`'s `cause` so the remaining half can branch on it without re-extracting. ⛔ **STILL OWED:** map `RATE_LIMITED` to a throttle state rather than `PROBE_FAILED`, answer **429 not 502**, and forward the upstream `Retry-After`. That is a second, distinct change to the same block and it belongs to 140.3's response-shape plan (SEAMUX-06) — **two changes to one block in one pass is TRAP-8**, which is how a previous batch's C5 undid its C6. `140.3-01` deliberately left the 502 status, the substring cascade and the `Retry-After` handling untouched. | `src/app/api/keys/[id]/permissions/route.ts` — the `!res.ok` arm (`throw new Error(err.detail ?? \`Upstream ${res.status}\`)`) and the catch's substring cascade below it | **The `/internal` 429 is LAUNDERED into `502 / PROBE_FAILED` and `Retry-After` is DISCARDED.** *(provenance: OB-9, 140.1.2 review evidence.)* The Python half is now correct on both counts — PYAPIFIX2-03 gave the throttle the `RATE_LIMITED` envelope at `body.detail.code`, and the wire has carried `Retry-After: 60` all along (C-9: the drop was never Python's). The route nonetheless flattens the body into an `Error.message` and then SUBSTRING-SNIFFS it (`INTERNAL_API_TOKEN` / `Upstream 5` / `ECONNREFUSED` / `not configured` / `aborted` / `timeout`); a throttle matches **none** of those branches and falls through to `PROBE_FAILED` + **502**. A one-minute throttle therefore renders as a probe failure on OUR side, with no wait advertised and no retry affordance. ⚠️ **PYAPIFIX2-03 neither worsened nor unblocked this** (settled at source, quote-level, 2026-07-26): the old scalar detail matched no branch, and an object detail stringifies to `"[object Object]"` which matches none either — classification is byte-identical across the change. The one accepted regression is cosmetic and server-side: the route's own `console.error` copy degrades to `"[object Object]"` on this path. | Read the machine code from `body.detail.code` and the human string from `body.detail.detail` — this is **TS-05**'s `typeof body.detail` branch on the same file, so land them together — then map `RATE_LIMITED` to a throttle state rather than `PROBE_FAILED`, answer **429 not 502**, and forward the upstream `Retry-After`. |
| **TS-35** ✅ | change | **SATISFIED by `140.3-05` on 2026-07-27.** Owner was 140.3; it closed there. ✅ **LANDED:** a closed, hand-typed `ReadonlyMap` lookup (`VENUE_WIRE_CODE_TO_VERDICT`) in `classifyKeyValidationError`, positioned **below** the `instanceof CircuitOpenError` type check and **above** the substring cascade, reading the wire code off a plain own `seamCode` data property with a `typeof === "string"` guard (never `instanceof` — sixteen route suites mock the seam client wholesale). All six real wire codes map explicitly; the contract's `ZZ_UNRECOGNISED_VENUE_CODE` control falls through to the cascade and then to `UNKNOWN`, which is the correct answer for a code minted after the table. **The cascade was NOT deleted** and is pinned by its own cases. Two new members — `KEY_EXCHANGE_UNAVAILABLE` and `KEY_VENUE_TRANSIENT` — both with **NON-FINAL** copy carrying `TODO-COPY-140.3-12`. `actions` (and therefore `recoverable`) WAS decided here, because it is behaviour; the sentences were not. ⚖️ **The wire `recoverable` is NOT read by the classifier, and that is deliberate.** The wizard's `recoverable` is derived from `actions` and is a RENDER hint; the contract's is "not known to be permanent". They disagree at exactly one code — `AUTH_FAILED`, wire `false` / wizard `true` — because retrying the SAME credentials cannot help (Python's question) while entering DIFFERENT ones can (the wizard's). The divergence is pinned BY NAME in the parity test, so a second one reddens. **Nothing was wired into an automated retry loop** (W-4 rider honoured; retry is Phase 141). 🔗 **Parity artifact:** `tests/lib/validate-key-venue-transient-parity.test.ts` — S-5 pattern, `readFileSync` of the committed fixture, hand-typed roster, `it.each` over the real classifier, **zero module mocks**, and **zero HTTP-status literals** so `140.3-06`'s remap cannot invalidate it. Falsifiers **M60** and **M61** observed RED first-hand and reverted. ⚠️ **RESIDUAL, ENUMERATED NOT SILENTLY ABSORBED:** `finalize-wizard` also puts `draft_state_invalid` (its 409 stale-state arm) and `COMPOSITE_UNSUPPORTED_UNIFIED` in front of a user with no wizard member, so both still render `UNKNOWN`. The first is a pre-existing DELIBERATE choice documented in `SubmitStep.tsx`; the second is `140.3-14`'s (TS-37), which must add its own `KNOWN_FINALIZE_CODES` membership in the same commit. Neither is in TS-35's class and neither was touched. | `src/lib/wizardErrors.ts` — `classifyKeyValidationError` (the substring cascade over `err.message`), plus the `WizardErrorCode` union it returns into; reached via `src/lib/analytics-client.ts`'s `error.detail ?? "Analytics service error"` and consumed by `src/app/api/strategies/create-with-key/route.ts` and `composite/add-key/route.ts` | **THE RENDER HALF of PYAPIFIX2-01 — a Binance maintenance window during key-connect still renders as `UNKNOWN`/500 “our team has been notified” with no retry affordance, even though the wire now carries the answer.** *(provenance: OB-1, 140.1.2 review evidence.)* The classifier is a SUBSTRING CASCADE over the human string and it derives BOTH the wizard code and the returned status from it — the upstream HTTP status is read at `analytics-client.ts` and then never used to decide anything. ~~Two of the five transient verdicts match no branch~~ ⚠️ **CORRECTED IN PLACE 2026-07-27 (correction C-6, re-verified first-hand by `140.3-05` by replaying the cascade's real predicates over the committed fixture's own bytes): the real count is THREE broken, not two, and the third fails SILENTLY.** `EXCHANGE_UNAVAILABLE` (“Exchange is currently unavailable…”) and `NETWORK_UNAVAILABLE` (“Network error reaching the exchange…” — the branch tests `timeout`/`etimedout`, a two-string near-miss) match no branch and reach `UNKNOWN`/500, as this row said. **The third is `DDOS_PROTECTION`, which does NOT reach `UNKNOWN` — it returns `KEY_IP_ALLOWLIST`/502.** Its detail ends *“Check region / IP allowlist.”* and the cascade's fifth branch tests `includes("ip") && includes("allow")`, so **a block at the VENUE'S edge is rendered to the user as a problem with their own key's IP allowlist**, and they are sent to edit restrictions on a key that was never the problem. **An audit asking “does it fall through to UNKNOWN?” cannot see this one**, which is exactly why this row missed it — the same syntax-grep-for-a-behavioural-class defect that mis-scoped TS-05's membership. ⇒ **Any test for this row must bind to the fixture's OUTCOMES, never to UNKNOWN-ness.** Measured under falsifier M60 (branch disabled, cascade alone): the `DDOS_PROTECTION` case reddens with `expected "KEY_VENUE_TRANSIENT", received "KEY_IP_ALLOWLIST"` — an outcome, not an absence. ✅ **The Python half is DONE (140.1.2-03):** all seven class members — exchange.py C1 sfox-429, C2 sfox-transport, C3 mt5-probe-timeout, C4 mt5-account-mismatch, C5 mt5-transient, C6 ccxt-collapse, plus portfolio.py C7 — now answer 400 with a FLAT body `{detail, code, recoverable}` whose `detail` is **byte-identical** to before, so nothing that classifies correctly today changed. Nothing further is needed from Python. | Add a `WizardErrorCode` — `KEY_EXCHANGE_UNAVAILABLE` / `KEY_VENUE_TRANSIENT` — and a branch that reads **`body.code`** instead of substring-matching `body.detail`, rendering a retry affordance when `body.recoverable` is true. Sibling of **TS-09**. Its parity input is the COMMITTED fixture `analytics-service/tests/fixtures/validate_key_venue_transient_contract.json` — bind a TS parity test to those bytes (the S-5 pattern `tests/lib/process-key-onboard-contract-parity.test.ts` already uses) rather than re-deriving the case table. Keep the substring cascade as the fallback until every emitter carries a code; deleting it early re-opens the class from the other side. ⚠️ **W-4 RIDER — read before consuming `recoverable`:** its `unknown ⇒ true` polarity is correct **because on this route the field is a RENDER hint**, not a retry-loop input — `PERMANENT_VALIDATION_ERROR_CODES` is an ALLOW-LIST OF PERMANENT, so non-membership means “not known to be permanent”, and the harm asymmetry (a wrong `true` costs one pointless click; a wrong `false` IS the dead end) points at `true`. **If 140.3 wires `recoverable` into an AUTOMATED retry loop, this polarity must be RE-DERIVED** — the asymmetry does not survive that change of consumer. The worker's automated retry decision is a different field (`error_kind`) on a different path (`long_fetch`). |

### The choke point and its downstream

| ID | Kind | File:line | What breaks TODAY | What it must become |
|---|---|---|---|---|
| **TS-11** ✅ | verify — **VERIFIED by `140.3-02`** (2026-07-27), **NOT edited**. *(Was: RE-HOMED → 140.3 by the developer scope decision 2026-07-26.)* **THE LEDGER'S VERDICT WAS RIGHT: no change was strictly required.** `postProcessKey` is byte-unchanged; only a comment was added. All four envelope outcomes were read on disk and recorded: a 403 write-capable-key verdict and an ordinary 403 validation failure BOTH take the `!res.ok` arm and forward the Python envelope VERBATIM; a 202 enqueue returns `{ok:true, status:202, body}`; and **a 200 with `body.ok === false` returns `{ok: TRUE, status:200, body}`**. ⚠️ **The load-bearing finding, now written at the choke point: there are TWO `ok`s and they answer different questions.** `PostProcessKeyResult.ok` is a TRANSPORT verdict ("did we get a usable 2xx"); `body.ok` is the SEMANTIC one. They disagree in BOTH directions, which is exactly why TS-12/13/14 must read `body.ok` and never `result.ok` and never the status. **The non-throwing contract is CONFIRMED** — 140.2-05's widened `try` covers both body reads, every catch arm returns, and `emptyBodyUnlessSeamRead`'s rethrow lands inside that `try`; no `SeamBodyReadError` can escape, so no caller in TS-12..TS-15 needs a new catch arm. *(Examined and left alone: `getCorrelationId()` is awaited ABOVE the `try`. It is a pre-existing throw path, unrelated to 140.2-05's delivery, and out of this plan's scope — recorded so the next reader does not mistake it for a regression.)* | `src/lib/process-key-client.ts:299-304` — *(drift: plan 140.1-05 recorded `:245-251`; plan 140.1-06 added 54 lines above it. `:245` is now `human_message: CIRCUIT_OPEN_HUMAN_MESSAGE`.)* | `if (!res.ok) { … return { ok:false, response: NextResponse.json(err, { status: res.status }) } }`. The PYAPI-10b write-capable-key verdict now takes **this** arm instead of the success arm. The envelope is forwarded verbatim at 403 — **no change strictly required** — but every caller's `if (!result.ok) return result.response` now short-circuits on a rejection that used to flow through the success path. | Confirm-and-document. This is the SINGLE choke point; TS-12..TS-14 are its downstream. Get this one right and the rest are mechanical. |
| **TS-12** ✅ | change — **SATISFIED by `140.3-02`** (2026-07-27), **with one documented correction to this row's own premise.** *(Was: RE-HOMED → 140.3, 2026-07-26.)* **DONE:** the route branches on `upstream.ok === true`; the `verification_id` sniff no longer decides success. Covered by a failing-without-the-fix case — a 200 carrying `ok:false` AND a top-level `verification_id`, which the sniff read as a success and for which it minted a queryable `public_token`, publishing a teaser factsheet for a key the exchange had REJECTED. **Ledger M54 OBSERVED RED and reverted.** ⚠️ **FINDING — "the 502 fallback's rejection case is now dead — delete it" is HALF TRUE, and the guard was KEPT.** Its REJECTION trigger is indeed dead (a rejection returns at the client's `!result.ok` fork, exactly as TS-11 says). Its **DRIFT** trigger is alive: a 2xx whose body lost `verification_id` still arrives here, and the plan's own instruction was to *"confirm by test that no live input can reach it before deleting; if one can, the ledger's premise is wrong and that is a finding."* One can. Deleting it would answer **200 with a `public_token` persisted against `.eq("id", null)`** — a token queryable against no row, i.e. the "silent success on failure" defect this phase exists to close, and the exact twin of the `isUuid` guard TS-13 says to KEEP one route over. The guard stays; only its RATIONALE narrows, and both halves are now named in a diagnostics-only log line. A green-before-and-after pin marks it AS a pin. | `src/app/api/verify-strategy/route.ts:194` + `:197-198` | `:194` is `if (!result.ok) return result.response;`, then `:197-198` sniffs `typeof upstream.verification_id === "string"` to decide success. The rejection no longer reaches `:197` at all (it returns at `:194`), and shape 5 now carries `ok: true`. | Branch on `upstream.ok === true` instead of sniffing `verification_id`. The `"Verification service returned an invalid response"` 502 fallback's rejection case is now dead — delete it. |
| **TS-13** ✅ | change — **SATISFIED by `140.3-02`** (2026-07-27). *(Was: RE-HOMED → 140.3, 2026-07-26.)* **DONE:** the guard is now `unifiedBody?.ok !== true \|\| !isUuid(unifiedBody?.strategy_id)`. **`isUuid` SURVIVES, exactly as this row required** — the two halves answer different questions and neither subsumes the other. Covered by a failing-without-the-fix case (a 200 carrying `ok:false` AND a VALID uuid, which the id check alone cannot catch and which the success envelope then re-stamped `ok: true` — the route asserting success on a body that said the opposite), plus a green-both-sides pin that `isUuid` still refuses a non-UUID. ⚠️ **DELIBERATELY NOT TOUCHED, and recorded so 145 does not assume it was done: this file's `debug_context` echo of the whole upstream body and its `console.error` of the same are TS-24**, owned jointly with Phase 145, which owns this file's transaction shape. Two phases editing one file in one pass is TRAP-8. | `src/app/api/strategies/csv-finalize/route.ts:1199`, `:1213-1214` | Same pattern: `if (!result.ok) return result.response;` then sniffs `strategy_id`. Shape 4 already carried `ok`, so this is the *sniffing*, not a break. | Branch on `ok`. **Keep** the `isUuid` check — it is defence-in-depth, not redundancy. |
| **TS-14** ✅ | change — **SATISFIED by `140.3-02`** (2026-07-27) for the ROUTE half. *(Was: RE-HOMED → 140.3, 2026-07-26.)* **DONE:** every `/process-key` consumer route now handles the envelope by `ok` rather than by status — discharged by TS-12 (verify-strategy / teaser) and TS-13 (csv-finalize) landing together, since those ARE the synchronous-path consumers. **The M-6 provenance is recorded in THREE places in production source** (the choke point, verify-strategy, csv-finalize) precisely so the next reader does not "simplify" it back to a status check: `validate-only` answers **200 with `ok:false`** where `_scope_rejected` answers **403**, on the IDENTICAL `not val.valid` predicate, and `STATUS_CONTRACT.md` records the carve-out nowhere — so a consumer branching on STATUS is wrong on one of the two paths whichever status it picks. ⚠️ **"AND decide the 403-vs-422 split (see TS-30)" was NOT done, and deliberately so.** The split stays HOMELESS: 403 is not wrong (all three arms are CALLER-class and breaker-inert) but it is coarse, and adopting a status split inside a plan whose whole thesis is *stop branching on status* would have been self-contradictory. Branching on `ok` makes the split a non-issue at these three consumers either way. It still needs its own plan. | every consumer of the `/process-key` synchronous path (`teaser`, `csv`-with-`strategy_id`, `internal_report`) | **Ordinary validation failures moved to 403 too.** `_scope_rejected` (`routers/process_key.py:1295-1299` — *drift corrected by plan 09: recorded as `:1245-1249` from plan 05's tree; plan 06 inserted ~50 lines above it*) is a THREE-arm OR behind ONE return — `not val.valid` **or** `read_only is False` **or** `error_code in {TRADE_SCOPE, WITHDRAW_SCOPE}` — so moving the return to 403 also moved `AUTH_FAILED`, `PERMISSION_DENIED` and a malformed CSV off 200. Anything treating a 200-with-`ok:false` as a renderable validation result must now handle it at 403. | Handle the envelope by **`ok`**, not by status. **AND decide the 403-vs-422 split** — see TS-30. |
| **TS-15** ✅ | change — **SATISFIED by `140.3-02`** (2026-07-27), **and it found a LIVE credential leak on the way.** *(Was: RE-HOMED → 140.3, 2026-07-26.)* **DONE:** both routes forward the session access token to the choke point as a VALUE (never a hand-assembled header), so the client's existing conditional spread emits `X-User-Access-Token`. The two fail in OPPOSITE directions, deliberately: `keys/sync` is BEST-EFFORT (the caller is already authenticated and ownership already proven by the user-scoped select, so an unreadable session must not fail a resync at the money-onboarding chokepoint), while `csv-finalize` stays fail-CLOSED 401 (its SECURITY DEFINER RPC cannot run without `auth.uid()`). `verify-strategy` is PUBLIC: a session is forwarded when one exists, and with none **NOTHING** is forwarded — asserted negatively, because a fabricated value there is elevation of privilege, not a compatibility shim. **Ledger M55 OBSERVED RED and reverted.** ⚠️ **THE FINDING — a correction to plan 140.2-08's site count, and the reason this obligation demanded proof BY EXECUTION rather than by reading.** Driving a token-bearing transport error through the route showed a **SECOND log site still emitting the JWT verbatim: `resilient-fetch.ts`'s OWN transport arm.** Its `scrubSeamError(err)` passed no per-request secrets, so it redacted `Authorization` (an env-list member) and shipped `X-User-Access-Token` in the clear — while its comment claimed it removed *"every credential undici inlined into the message"*. **This predates the plan and was LIVE, not latent: `strategies/csv-finalize` has forwarded that JWT through this exact core since Phase 19.1.** 140.2-08's enumeration was of the CLIENT's log sites and never reached the core's — the same syntax-vs-behaviour enumeration defect this programme keeps hitting. **Fixed as a CLASS:** `credentialHeaderValues()` DERIVES the per-request secrets from the outgoing headers the core was asked to send, so any credential-bearing header is scrubbed whichever caller supplied it; a caller-declared `secrets: []` option would be convention, and a caller who forgets is silently back to leaking. *Examined and rejected as a class member: `instrumentBody`'s body-read log site — it never receives `requestInit`, and a body-read rejection carries no outgoing headers.* Proven at BOTH routes with the REAL client and REAL core, and **both halves of TRAP-1 are asserted** (the JWT is gone AND `ECONNREFUSED` survives). **Ledger M56 OBSERVED RED TWICE — once per log site** — so the coverage is a class result, not an instance one. | `src/app/api/keys/sync/route.ts:412-421` and `src/app/api/verify-strategy/route.ts:179-192` | Only the **csv** flow forwards `X-User-Access-Token`, so a user-scoped (RLS-enforcing) Supabase client is unavailable on onboard/resync. That is why PYAPI-01's second defence layer had to be an explicit Python `strategies` id+user_id filter rather than letting RLS do it. | Forward `X-User-Access-Token` on every authenticated flow, so a user-scoped pre-check becomes possible and the Python ownership filter becomes belt-and-braces rather than the only belt. |
| **TS-16** ✅ | record — **DONE-IN-140.2-06**, and **RE-CONFIRMED by `140.3-02`** (2026-07-27) from the consumer side, which is the side this row's "record" verb was really about. `140.3-02` treats 400 as CALLER unconditionally by not branching on status AT ALL at any of its three consumers: they read `body.ok`, so the "was this previously an outage?" special-case has nowhere to be written. **`body.detail.correlation_id` was deliberately NOT surfaced** — that is **TS-20**, and it belongs to a later 140.3 plan; `140.3-02` logs `correlation_id` to the OPERATOR at verify-strategy's refusal but adds it to no user-facing envelope. Row stays closed; no count change. **The original 140.2-06 record follows, unchanged:** 400 is treated as CALLER **unconditionally**: `seam-discriminator.ts` classifies the whole `status < 500` band as caller-class with no "was this previously an outage?" special case anywhere, pinned by `CALLER: a %i records ZERO, whatever its body says` over 400/401/403/404/422 — whose fixture bodies deliberately carry a `code` and `recoverable`, so a classifier that read the body could not pass by accident. `body.detail.correlation_id` is carried by the leaf's `nestedDetail` reader and is NOT consumed for any breaker decision. Surfacing it in error copy stays **TS-20 (140.3)**. | STATUS_CONTRACT §6 O-4 / plan 140.1-04 | **`400` joins the CALLER bucket from a NEW source.** S-16 (`match.py:1863`, oversized eval window) is the first case where a `400` arrives from a condition that used to be a `503`. A discriminator that special-cases *"was this previously an outage?"* will be wrong. Also: **`body.detail.correlation_id` now exists** on S-15, S-17 and S-18. | Treat 400 as CALLER unconditionally. Surfacing `correlation_id` in error copy is the cheapest support channel we will ever get — 140.3's call (TS-20). |

## 2. Render surface — Phase 140.3 (SEAMUX)

> 🔧 **DATED CORRECTION — 2026-07-27, by plan `140.3-12`, from first-hand greps. C-9 in place.**
> No row's disposition changes here; these are counts every later plan should use instead of the
> ones in CONTEXT.
>
> **(a) The lie class is NINE strings across SEVEN codes — not the five-across-four CONTEXT names,
> and not the seven-across-five this plan's own table listed.** Enumerating by the SENTENCE rather
> than by the code is what found the extras, and every one was verified at source:
>
> | # | Code | The false claim | Why no document had it |
> |---|---|---|---|
> | L1 | `UNKNOWN` | *"Our team has been notified…"* | in CONTEXT |
> | L1b | **`GATE_ANALYTICS_FAILED`** | *"…we have been notified."* | **MISSED — in no document** |
> | L2a | `SYNC_FAILED` | *"We fetched your trades…"* | in RESEARCH |
> | L2b | **`GATE_ANALYTICS_FAILED`** | *"We fetched your trades successfully…"* | **MISSED — in no document** |
> | L3a | `CSV_SUBMIT_FAILED` (title) | *"…your data is unchanged."* | in RESEARCH |
> | L3b | `CSV_SUBMIT_FAILED` (fix) | *"…your data is unchanged."* | in RESEARCH |
> | L4 | `CSV_SUBMIT_NO_STRATEGY_ID` | *"…wizard_session_id idempotency."* | in RESEARCH |
> | **L5** | **`SERVICE_UNAVAILABLE_RETRY`** | *"Your key has not been saved…"* | **found via `140.3-05`'s recorded copy rider** — true at key-connect, STALE at finalize, which this member only started reaching when `140.3-05` aliased `CIRCUIT_OPEN` onto it |
> | **L6** | **`SERVICE_UNREACHABLE`** | *"Nothing was submitted and your draft is unchanged."* | **found first-hand** — the member homes `UPSTREAM_TIMEOUT`, where the request WAS sent, so this is unknowable. It was COPIED from L5's entry one member away, where the same sentence IS knowable |
>
> ⚠️ **The transferable lesson is L5/L6, not the count.** Both are the same sentence, correct on the
> code it was written for and false on the code it was pasted onto. Two of the nine lies were created
> by *copying honest copy*. A per-code audit cannot see that; only a per-sentence one can. The guard
> `140.3-12` shipped is therefore a scan of the WHOLE `WIZARD_ERROR_COPY` table (53 entries, over
> `title` + `cause` + every `fix[]` line — two of the nine lived only in `fix[]`) with hand-typed
> forbidden substrings and a hand-typed size guard. **M71 confirms it is a scan and not a list: the
> mutation was applied to `GATE_ANALYTICS_FAILED` — the code no document listed — and it reddened.**
>
> **(b) The Sentry-less route class is 9 of 15, not the 2 CONTEXT names.** All nine, by name:
> `admin/match/eval` · `admin/match/recompute` · `keys/[id]/permissions` · `portfolio-optimizer` ·
> `scenario/optimize` · `strategies/composite/add-key` · `strategies/create-with-key` ·
> `strategies/csv-validate` · `verify-strategy`. The six WITH ≥1 capture: `bridge` · `keys/sync` ·
> `keys/validate-and-encrypt` · `simulator` · `strategies/csv-finalize` ·
> `strategies/finalize-wizard`.
> **`140.3-12` closed the COPY half only** (stop claiming); **`140.3-13` owns the CAPTURE half**
> (start notifying). ⚠️ **A rider for `140.3-13`:** the replacement copy claims nothing about
> notification **in either direction**, and that is deliberate — telling the user nothing reaches us
> would be a second false claim, because 6 of the 15 routes DO capture and `UNKNOWN` is reachable
> from them. If `140.3-13` adds the missing captures, the claim may return **only with the receipt
> and only where it is true**; the table-wide guard will redden until its `FORBIDDEN` entry is
> deliberately revised.
>
> **(c) `VerificationForm.tsx`'s orphaned client-side `verification_id` sniff was in NO plan's
> `files_modified`** and the file had **no test at all**. Resolved by `140.3-12` — see TS-17's row
> for the verdict and the proof. **`140.3-02` did not delete its server-side twin**, contrary to the
> premise `140.3-12`'s plan was written on.
>
> **(d) `keys/sync`'s two 400 arms** (`MISSING_STRATEGY_ID`, `INVALID_STRATEGY_ID`), which
> `140.3-10` gave wire codes but deliberately left with no wizard copy, now render
> `VALIDATION_FAILED` — reachable **only** because `140.3-12` removed that member's false
> single-producer attribution first. Wiring them **forced** a TRAP-4 guard: the member is
> non-recoverable, so the sole surviving control would have been *"Try another key"*, which deletes
> the draft. The destructive-control check was widened from an `===` on one code to a hand-typed set
> (`errorStateIsDraftGone` → `errorStateHidesDestructiveControl`); **M66c applies unchanged under the
> new name.** No `actions` array was edited, so no code's `recoverable` moved.

| ID | Kind | File:line | What breaks TODAY | What it must become |
|---|---|---|---|---|
| **TS-17** ✅ | change — ✅ **SATISFIED 2026-07-27 by `140.3-12`.** The teaser reads `human_message` before `error`, so a write-capable-key rejection renders its own verdict instead of the generic *"Verification failed"*. 🔴 **T-140-08 IS LIVE ON THIS FIELD — verified at source, not assumed.** `process-key-client.ts` forwards a non-2xx upstream body **VERBATIM** (`NextResponse.json(body, {status: res.status})`), Python's `_envelope_error` applies **no** sanitiser, and `human_message` is filled from sources that pass raw text (`services/ingestion/okx.py` → `result.get("error")`, `csv_adapter.py` → `str(exc)`). So the field is surfaced behind a **SHAPE denylist** (`safeHumanMessage`: URL, scheme, hostname, IP, bare status, traceback markers, 200-char bound) — deliberately **not** an allow-list of known sentences, whose failure mode is that a real rejection renders as the generic copy, i.e. the very defect TS-17 exists to fix. 🔴 **SECOND-THROW VERDICT: KEPT, and the premise for deleting it was FALSE.** The plan described `VerificationForm`'s `"Verification service returned an invalid response"` sniff as the orphaned client mirror of a 502 that `140.3-02` deleted server-side. **`140.3-02` did NOT delete it** — `verify-strategy/route.ts` still answers that 502, and its own docblock records keeping it as a deliberate FINDING against TS-12 (*rejection trigger dead, DRIFT trigger alive*). The identical split holds one hop later: the route has exactly one 2xx exit and it always sets both fields, so the client sniff's rejection trigger is dead too — but on drift the alternative is calling `onResult` with `undefined`, minting a teaser link for a verification that does not exist. Reason recorded in-file beside the throw. ⚠️ **This file was in NO plan's `files_modified`** and had **no test file at all**, so nothing in the repo exercised this unauthenticated surface's error path (the `140.3-11` M77b shape). 18 cases added. | `src/components/landing/VerificationForm.tsx:53-54` | `if (!res.ok) { throw new Error(data.error ?? "Verification failed"); }`. The 403 envelope has **no `error` key** — it carries `human_message` — so a write-capable-key rejection renders the generic *"Verification failed"*. **Net improvement even unfixed:** today the teaser rejection is a 200 with no `verification_id`, so `verify-strategy/route.ts:57-58`'s sibling check turns it into a **502 "Verification service returned an invalid response"** — strictly more misleading. | `data.human_message ?? data.error ?? "Verification failed"`. |
| **TS-18** ✅ | change | ✅ **SATISFIED 2026-07-27 by `140.3-11` — a `424` renders as a NAMED, recoverable venue state at BOTH admin consumers.** ⚠️ **NO IN-REPO ANALOG — `424` had never been emitted before `140.3-06`.** Measured on the untouched tree: `grep -rn "424" --include='*.tsx' src` → **0**; no consumer branched on it anywhere. The render is built from `STATUS_CONTRACT.md` §6 **O-1** (recoverable AND non-counting; must not be collapsed into the un-retryable 4xx). **THE CONTRACT PREMISE WAS VERIFIED AT SOURCE, NOT ASSUMED** (`analytics-service/services/error_contract.py`, the `status_code == 424` arm, read 2026-07-27): a 424 must name a non-empty `dependency`, that name may NOT be a member of `SERVICE_DEPENDENCIES` (*"a 424 names the caller's venue, and a fault in our own dependency is 500/503"*), and it must be `retryable`. The two vocabularies therefore cannot collide, and the venue name is safe to display. **🔑 HOW `dependency` IS REACHED — the decision `140.3-15` MUST REUSE.** `nestedDetail` in `src/lib/seam-discriminator.ts` is unexported by design, so the leaf gained **ONE narrow, purpose-named extractor**, `seamDependencyName(body)`, built on that same private reader; `EXPECTED_EXPORTS` in `seam-discriminator.purity.test.ts` went **3 → 4** in the SAME commit. It is deliberately NOT an export of `nestedDetail` (that hands every consumer the whole envelope and makes the next hand-rolled `body.detail` branch a one-liner — the class `140.3-01` closed) and NOT a generic field getter (a set that cannot say WHICH fields are reachable guards nothing). **➡️ `140.3-15` must add `seamCorrelationId` as a FIFTH narrow member on the same private `nestedDetail`, not a private reader of its own.** It is a SEPARATE function from the private `serviceDependencyOf` because the two answer opposite questions: that one admits ONLY our closed set (it may build a breaker key, T-140-01); this one admits only names OUTSIDE it, refuses every member of ours as defence-in-depth over `_validate`, and bounds the value to a slug SHAPE — never a venue allow-list, whose failure mode is that an exchange nobody remembered renders as no outage. **`AnalyticsUpstreamError` carries it** on exactly the additive-optional contract `140.3-01` gave `seamCode`; it had to be read at the seam chokepoint because that is the last line at which the body exists. **Both routes forward `{error, dependency}`; both consumers render one sentence from ONE module** (`src/components/admin/match/venueOutageCopy.ts`). ⚠️ **`dependency` is `null` on the FLAT 424 shape** (`VenueTransientHTTPException` carries no such key), which is the COMMON case, so the un-named degrade is the branch that actually fires and it is pinned at both consumers with an explicit assertion that no literal `undefined` reaches the DOM. ⚠️ **The wizard side was VERIFIED, not re-implemented:** `140.3-05` routes `EXCHANGE_UNAVAILABLE` / `DDOS_PROTECTION` through `classifyKeyValidationError`'s `body.code` branch to `KEY_EXCHANGE_UNAVAILABLE` / `KEY_VENUE_TRANSIENT`, so no second 424 render path was created. 🔴 **REACHABILITY, stated because it is load-bearing and unflattering:** `analytics-service/routers/match.py` contains **zero** `424`s, so these two routes' upstream cannot produce one TODAY — the render is correct and tested but is exercised in production only once a 424 reaches these endpoints. What IS live today at these routes is TS-19's 400/403/422/429 forwarding. | wherever a `424` renders | A `424` means **the caller's venue failed**, and `body.detail.dependency` names that venue (not one of ours — `error_contract._validate` **refuses** a 424 naming a service dependency, so the two vocabularies cannot collide). Rendering it as "the analytics service is degraded" is a false claim about whose fault it is (SEAMUX-04's own subject). | Render as a **named, recoverable** venue state: *"Binance isn't responding right now — your key is fine."* Offer a retry (424 is `retryable:true`). |
| **TS-19** ✅ | change | ✅ **SATISFIED 2026-07-27 by `140.3-11` — an upstream 4xx now survives BOTH admin match routes.** 🔴 **THE FLATTENING WAS BROADER THAN THIS ROW STATED, and the correction is the point:** B-12 describes it as *"flattens all upstream 4xx to a generic 500"*, which reads as a mapping that exists and is wrong. **Neither route had an `AnalyticsUpstreamError` arm AT ALL.** Measured comment-excluded on the untouched tree, `grep -c "AnalyticsUpstreamError"` was **0** in `recompute/route.ts` and **0** in `eval/route.ts`; only `CircuitOpenError` (→ 503 + `Retry-After`) and `AnalyticsTimeoutError` (→ 504) were branched, and the terminal arm was UNCONDITIONAL — so every other upstream status, 4xx **and** 5xx, became a bodyless 500 carrying "Please try again". **BOTH routes were fixed in ONE commit**, with per-file counts as the receipt (`AnalyticsUpstreamError` **0→2** in each; `status: 500` unchanged at **1** in each, so the terminal arm survives as the untyped fallback and no new 500 was added). ⚠️ **`analytics-service/routers/match.py` raises 400 (`:1783`), 403 (`:1668`), 422 (`:1697`) and 429 (`:1741`) on these very endpoints**, so this was the common case rather than a corner: a deliberate, immediate refusal by the service was reported to an admin as OUR fault. ⚠️ **Only the 4xx range forwards, and that is a DECISION not a shortcut.** The shape is copied from `src/app/api/simulator/route.ts:201` rather than invented; a 5xx `AnalyticsUpstreamError.message` carries the FastAPI `detail`, the `parseResponse()` contract-drift string and the analytics base URL, which is exactly threat **T-140-11** — the leak these two files' own STATIC-bodies docblock exists to prevent. An anti-regression case in each file re-pins that absence THROUGH the new arm. ⚠️ **No header rides along:** `AnalyticsUpstreamError` carries none, so a forwarded upstream 429 reaches the client without its `Retry-After`. Recorded rather than invented — naming a wait no upstream stated is TRAP-3. If that wait is wanted, it is a `Retry-After`-on-the-error-type change and belongs with SEAMUX-06's plumbing, not here. Proven against **real wire bytes**, not only a double: `recompute/route.seam.test.ts` runs the REAL client with only `fetch` faked and asserts a real `424` on the wire leaves the route as `424` while a real `502` still leaves it as the static 500. | `src/app/api/admin/match/*` | **B-12 / OPEN-1**: admin/match currently flattens all upstream 4xx to a generic 500. A `424` from `/api/match/*` therefore renders as "please try again" with **no venue named**, and — worse — as a 500 it looks like our fault. | Stop flattening. Pass the upstream status and envelope through. This is a prerequisite for TS-18 having anything to render. |
| **TS-20** ✅ | change | ✅ **SATISFIED 2026-07-28 by `140.3-15` — the relocated diagnostic reaches the render slot that already existed.** **THE LEAF MECHANISM IS `140.3-11`'s, REUSED BY NAME, so ONE reader is on record for this leaf rather than two.** `seamCorrelationId` is the **FIFTH** narrow, purpose-named extractor over the private `nestedDetail` in `src/lib/seam-discriminator.ts`, added on exactly the contract `140.3-11` set for `seamDependencyName` one plan earlier — NOT a private reader of its own, NOT a widening of `seamDependencyName`, and NOT an export of `nestedDetail` (which `140.3-11` rejected because it hands every consumer the whole envelope, and a generic `seamDetailField(body,name)` getter, which it rejected because an export set that cannot say WHICH fields are reachable guards nothing). **`EXPECTED_EXPORTS` in `seam-discriminator.purity.test.ts` went 4 → 5 in the SAME commit as the export** (`c07b9c39`) — the hard gate — and the leaf is still import-free (`grep -cE "^\s*import " → 0`). It **mirrors `seamErrorCode`'s BOTH-SHAPE read**, because `correlation_id` is TOP-LEVEL on the app-global flat envelopes (`main.py`'s 422/429 handlers, `process_key.py`'s own error envelopes) and NESTED inside `service_error`; a nested-only reader answers `null` on every body from the other family, and a `null` here is indistinguishable from a correctly-absent value. 🔴 **A SHAPE GUARD WAS ADDED AND IT IS NOT DECORATION:** on the app-global handlers the value is `request.headers.get("x-correlation-id")` — **caller-supplied and echoed straight back** — and a consumer renders it VERBATIM into the DOM and copies it to the clipboard in the `QUANTALYZE_DIAG` block. It is bounded by `^[A-Za-z0-9._:-]{1,128}$`, hand-typed in the leaf, duplicating `CORRELATION_ID_SHAPE` in `src/lib/correlation-id.ts` because that module imports `server-only` + `next/headers` and the leaf may import neither; the duplication is also the falsifier between the two. **Wired at BOTH consumers that hold a seam body** — `SubmitStep` (finalize-wizard) and `SyncPreviewStep` (keys/sync), each of which forwards the seam envelope verbatim — through the field `ErrorEnvelope` **already renders** and `buildDiagBlock` **already copies into the `QUANTALYZE_DIAG` payload**, so `envelope.ts` and `ErrorEnvelope.tsx` were **not modified at all**: no second id field, no change to the field's requiredness, and the browser-minted `getWizardCorrelationId()` remains the fallback. ⚠️ **DISPLAY ONLY, consumed for NO breaker decision — TS-16's note re-confirmed**: `seamBreakerVerdict` does not call `seamCorrelationId` and must not, because a breaker key may only come from the closed service set (T-140-01) and this value is caller-influenceable. Proven by POSITIVE identity assertions at both renders (the exact upstream id appears AND the local id it displaced does not), never `toBeDefined()`/`toBeNull()` — `140.3-11`'s M77c showed a deleted carry and a correctly-absent value produce the same answer. | The original statement follows, unchanged: `body.detail.correlation_id` now exists on S-15 (`SCORING_FAILED`), S-17 (`EVAL_FAILED`) and S-18 (`SIMULATION_FAILED`). Today it is emitted and then thrown away by the renderer. ⚠️ **Row premise corrected in place by `140.3-15`:** those three outcomes come from `match.py` / `simulator.py` and are consumed by the admin match cards and `PortfolioImpactPanel`, **none of which renders `ErrorEnvelope`** (`140.3-11`'s FINDING-C). The envelopes that DO reach the canonical renderer are the `/process-key` ones forwarded by `finalize-wizard` and `keys/sync`, and they carry the id at the TOP level. The obligation is closed against the surface that actually renders; the three named outcomes remain unrendered because their surfaces are bespoke cards, which is `140.3-11`'s recorded, still-unresolved DESIGN.md tension and not this row's. | Surface it in the error copy. Plan 140.1-04 moved the diagnostic *out* of the body (it was leaking raw exception text — table names, row payloads, DSNs) and replaced it with this id; if nobody renders the id, the diagnostic is simply lost rather than relocated. |
| **TS-37** ✅ | change — **added by the 140.2 REVIEW-FIX pass (ME-02)** | **SATISFIED by `140.3-14` on 2026-07-27.** ✅ **LANDED:** the cap arm now returns `503 { code: "COMPOSITE_TOO_MANY_MEMBERS" }` with copy that names the limit **with its number** (DESIGN.md §Voice — state the limitation with its threshold attached) and the remedy (remove keys), and whose `actions` are `["request_call", "expand_log"]` — **neither member of `RECOVERABLE_ACTIONS`**, so `buildEnvelope` derives `recoverable: false` and `ErrorEnvelope` renders **NO Retry control**. That suppression IS the fix, not a side effect. ⚠️ **THE SITE COUNT, MEASURED ON THE TREE RATHER THAN INHERITED: the route emitted `COMPOSITE_MEMBERSHIP_UNKNOWN` at FOUR sites and EXACTLY ONE was re-coded.** A raw `grep -c` reads **5** — four `code:` emissions plus one load-bearing explanatory comment — so the comment-excluded count is the only honest one: **4 before, 3 after**. **The three that deliberately KEEP the code AND its retry, by name:** (1) the composite hoist's membership-COUNT probe (`step: "composite-membership-probe"`), (2) the member-LIST read failure (`step: "composite-member-list"`) — the arm that was byte-identical to the cap arm and is therefore the one a careless sweep destroys — and (3) the unified arm's probe (`step: "unified-composite-probe"`). All three are genuinely transient reads; re-coding any of them strips a correct retry from a real transient fault, which is the INVERSE of this defect. 🔗 **The split went through the route AND `KNOWN_FINALIZE_CODES` in `SubmitStep.tsx` in the SAME COMMIT** (`e32c16a9`), exactly as this row warned was required — and that requirement was proven live, not assumed: dropping the membership line alone reddens both SubmitStep cap cases while **all 78 route tests stay GREEN**, so a route-only change really would have shipped invisible. ✅ **The off-by-one half was VERIFIED, not redone:** the read still carries `.limit(MAX_COMPOSITE_MEMBERS + 1)` and the refusal is still `>`, and the three boundary cases (9 → finalises with 9 probes, 10 → finalises with 10 probes, 11 → refuses with 0 probes) are untouched and green. ⚖️ **Two things deliberately NOT done:** the wire STATUS stays **503** (this row asks for a code and copy, not a status change; `SubmitStep` maps off `code` alone, so the permanent/transient distinction is fully carried by the code — the residual is recorded for `140.3-15`/`140.3-16`), and `COMPOSITE_UNSUPPORTED_UNIFIED` was left alone (TS-35's enumerated residual, a different arm, not this row's). 🧪 **Both hand-typed table-size guards were BUMPED 53 → 54, never replaced with a derived `Object.keys(...).length`.** The new copy's NUMBER is additionally pinned CROSS-FILE to the route's own `const MAX_COMPOSITE_MEMBERS` declaration, so the sentence cannot drift from the constant it describes. ⚠️ **One acceptance criterion could not be met as written and was NOT quietly dropped:** the plan required both pre-existing pinned cases to stay unmodified. *"a GENUINE 10-member composite finalises"* is byte-unchanged (it asserts no code at all). *"fails LOUD when the member list OVERFLOWS the cap"* asserted the very byte this row changes, so it was **RE-PINNED and made strictly stronger** — exact new code, plus an explicit negative on the transient one, plus the wire message's limit and remedy. | `src/app/api/strategies/finalize-wizard/route.ts` — the composite-member-cap refusal arm, and whatever renders `COMPOSITE_MEMBERSHIP_UNKNOWN` | **A PERMANENT condition is rendered with TRANSIENT copy.** The cap arm returns `503 { code: "COMPOSITE_MEMBERSHIP_UNKNOWN", error: "Could not load composite members; please retry." }` — byte-identical to the arm for a genuinely transient member-list READ FAILURE. For an overflowing composite the condition never clears, so the client renders a retry affordance that can only ever fail, and the user gets no explanation and no path forward. The review-fix pass closed the OFF-BY-ONE half (the read now fetches `cap + 1`, so a genuine 10-member composite finalises and only 11+ refuses) and deliberately did NOT touch the copy: **authoring user-facing copy is 140.3's fence.** The operator half is already distinguishable — the log says `EXCEEDED` and Sentry carries `step: "composite-member-cap"`. | Give the overflow arm its OWN `code` (the read-failure arm keeps `COMPOSITE_MEMBERSHIP_UNKNOWN`) and copy that names a permanent limit and a remedy — remove a member — instead of "please retry". ⚠️ The two arms are intentionally byte-identical TODAY; splitting them is the change, and the split must go through the route AND its renderer together or the new code falls through to a default. Pinned by "a GENUINE 10-member composite finalises" and "fails LOUD when the member list OVERFLOWS the cap" in `finalize-wizard/route.test.ts`, both of which stay green across a copy-only change. |
| **TS-38** ✅ | change — **added by the 140.2 REVIEW-FIX pass (ME-01)** | ✅ **SATISFIED 2026-07-28 by `140.3-15` — a config fault stops wearing the upstream's envelope.** The `SeamConfigError` arm now **RETURNS** `{ok:false, code:"SEAM_MISCONFIGURED", human_message:…, correlation_id, recoverable:false}` at **500**, instead of falling through to `UPSTREAM_NETWORK_ERROR` 502. **It returns, never throws** — the five caller routes (csv-validate, csv-finalize, finalize-wizard, keys/sync, verify-strategy) have no catch for this function and a case asserts `.resolves`; none of the five route files appears in this plan's diff. ⚠️ **THE STATUS IS A DECISION AND ITS REASON IS THE OBLIGATION.** **500**, because it says "the server that produced THIS response is at fault", which is the literal truth, and because `STATUS_CONTRACT.md` §1 gives 500 to the SERVICE-PERMANENT class ("a deterministic fault can only be cleared by an operator") — this fault exactly. **502 was rejected**: it asserts the UPSTREAM is bad, which is the precise lie this row exists to stop, and keeping it would leave that lie in the status line after removing it from the sentence. **503/504 were rejected**: both signal TRANSIENCE on a condition that is permanent until an operator redeploys, which invites exactly the retry `recoverable:false` removes. No `Retry-After` rides with it (TRAP-3: there is no wait that helps). **`recoverable: false`** on both halves — the wire field, and the wizard entry's `actions` (`request_call` + `expand_log`, carrying **neither** member of `RECOVERABLE_ACTIONS`), so `buildEnvelope` derives it and `ErrorEnvelope` renders **NO Retry control**. That is the fix, not a side effect: a control that can never succeed. **The teaser constraint was honoured** (threat T-140-08 / T-140.3-15-02): the sentence is STATIC and carries no URL, no hostname, no bare 3-digit status and no env-variable name, asserted by a case and readable in the string — *"We could not send this request — our own configuration is wrong. Retrying will not clear it."* **A NEW `WizardErrorCode` MEMBER WAS MINTED, and the three near-misses were read first and rejected with reasons** (this file's own convention is that a second member with the same meaning is how a vocabulary starts lying): `SERVICE_UNREACHABLE` says *"We sent the request and never got an answer"* — nothing was sent, `SeamConfigError` is raised before any store or network I/O; `SERVICE_UNAVAILABLE_RETRY` says *"wait a moment, then try the same action again"* — true of a breaker, false of a config typo; `VALIDATION_FAILED` is closest on BEHAVIOUR and wrong on the FACT (nothing was sent, nothing was shape-checked). **BOTH hand-typed `EXPECTED_TABLE_SIZE` literals bumped 54 → 55 in the same commit as the member, never replaced with a derived length.** `SubmitStep`'s `KNOWN_FINALIZE_CODES` admits the code **in that same commit** (`140.3-14`'s M81: otherwise it ships invisible with every client-side test green — reproduced first-hand as M84b). ⚠️ **THE ME-01 OPERATOR FALSIFIER IS UNTOUCHED AND GREEN** — its case body is byte-identical (verified by diffing the baseline lines, not asserted), and the operator log line in production source is byte-unchanged (TRAP-9). The original statement follows, unchanged: **A CONFIG fault on OUR side still wears the dead-upstream ENVELOPE at the five `postProcessKey` routes.** The review-fix pass gave `SeamConfigError` a consumer in `analytics-client` (rethrown UNWRAPPED, so callers' generic arms answer a 500 "we failed" rather than a 502 "they are down") and, here, an operator-facing log that NAMES it — so a malformed `ANALYTICS_SERVICE_URL` no longer reads to ops as Railway being down. What it could NOT do here is change the returned envelope: this function is declared **never to throw** at its five caller routes (csv-validate, csv-finalize, finalize-wizard, keys/sync, verify-strategy), none of which has a catch for it, so the `analytics-client` remedy is unavailable — and inventing a `code` + `human_message` is authoring copy. A config fault therefore still returns `UPSTREAM_NETWORK_ERROR` 502 / "Could not reach the ingestion service." | Add a distinct arm returning a `SEAM_MISCONFIGURED`-class code with copy that does not blame the upstream and does not invite a retry (the condition is permanent until redeployed). ⚠️ **The anonymous teaser renders this string directly**, so the same static-copy constraint that governs `CIRCUIT_OPEN_HUMAN_MESSAGE` applies: no URL, no status, no service internals. Falsifier already in place: "ME-01: NAMES a SeamConfigError as our own config fault in the operator log" in `process-key-client.test.ts` pins the log half and must stay green. |

> ### 📌 2026-07-27 — a correction recorded here rather than forked: the unchecked-cast class (B-14 / SEAMUX-07) had **TWO** members
>
> **No row was added and no row was removed by this note, so the obligation count is UNCHANGED at 40.**
> `140.3-03` closes a REQUIREMENT (`SEAMUX-07`), not a TS row — B-14 never had one. The note lives here
> because this file is the artifact the downstream phases read INSTEAD of re-deriving, and the claim it
> corrects is repeated in the findings doc.
>
> **The false claim.** Review finding **C-05** asserted that *"the sibling badge route validates; this
> one does not"* — i.e. that only `strategies/finalize-wizard` read the live-permissions body through an
> unchecked cast. **That is FALSE, verified first-hand** (and CONTEXT correction **C-5** says so):
> `src/app/api/keys/[id]/permissions/route.ts` did the identical unchecked cast over the **same**
> `/internal/keys/{id}/permissions` upstream, and validated nothing.
>
> **Why the second member was the worse one.** Its cast sits inside an `unstable_cache` fork with
> `{ revalidate: 60 }`, so a 2xx `{}` did not merely reach `KeyPermissionBadge` as
> `{read: undefined, trade: undefined, withdraw: undefined}` — a confident-looking scope verdict about a
> money-bearing key whose scopes were in fact unknown — it was then **replayed for 60 seconds to callers
> that never saw the body**. That cache is C-06's mechanism, and it means the fail-open outlived the
> request that caused it.
>
> **Both members were fixed by `140.3-03` with ONE schema and ONE fail-closed posture:**
> `LivePermissionsSchema` in `src/lib/analytics-schemas.ts` (`read`/`trade`/`withdraw` REQUIRED, because
> absence is exactly the drift that publishes a write-capable key; `probe_error` optional), with
> `KeyPermissionsPayloadSchema` derived from it by `.extend({detected_at})` so the three scopes have one
> definition across both call sites. At `finalize-wizard` a parse miss resolves to a sentinel carrying
> `probe_error` only, joining the existing fail-CLOSED arm; at `keys/[id]/permissions` it **throws** out
> of the cached callback, because the fork writes an entry only after the callback RESOLVES — an error
> returned as a value would have been memoized, which is the defect restated rather than fixed.
> Falsifier **M67** targets the SECOND member specifically, and **M68** proves the schema is load-bearing
> at both; both OBSERVED RED and reverted.
>
> ⚠️ **Two things `140.3-03` deliberately did NOT do in `keys/[id]/permissions/route.ts`, so no later
> reader assumes they were done:** the `!res.ok` arm, its substring cascade, its hardcoded 502 and its
> dropped `Retry-After` are **TS-34**'s remaining half (plan `140.3-09`), and this file's
> `CIRCUIT_OPEN_COPY` declaration belongs to plan `140.3-04`. Two changes to one block in one pass is
> TRAP-8. **TS-34's disposition above is unchanged by this note.**

> ### 📌 2026-07-27 — D-12's "self-referential oracle" framing was **INVERTED**, corrected here rather than forked
>
> **No row was added, no row was removed and no row's disposition changed by this note, so the
> obligation count is UNCHANGED at 40.** `140.3-04` closes the copy half of a REQUIREMENT
> (`SEAMUX-01`); D-12 was a locked CONTEXT decision, not a TS row. The note lives here because this
> file is the artifact the downstream phases read INSTEAD of re-deriving, and the claim it corrects is
> repeated in CONTEXT's locked `<decisions>` block and in the findings doc.
>
> **The false claim.** D-12 said `CIRCUIT_OPEN_COPY` *"is duplicated across 10 production files and
> re-declared in 12 test files, each asserting byte-identity against **its own copy** — a
> self-referential oracle."* **The second half is backwards**, and acting on it would have deleted a
> working fence. `grep -rn "export const CIRCUIT_OPEN_COPY\|export .*CIRCUIT_OPEN_COPY" src/ tests/`
> returned **0** — nothing was exported anywhere. The twelve test literals therefore *could not* have
> been reads of the module under test: they were hand-typed literals, which is exactly correct oracle
> independence, and `src/app/api/keys/sync/route.seam.test.ts` says so out loud (*"Duplicated
> deliberately: a test that imported the constant from the client could not detect it changing"*).
> Consolidating them onto a shared constant would have converted twelve independent oracles into one
> self-referential oracle — it would have MANUFACTURED the defect D-12 believed already existed. That
> is **TRAP-9**, which is binding. CONTEXT correction **C-1** governs, and was followed.
>
> **The real gap was the other direction: production↔production.** Ten independent production
> constants could drift apart and every test stayed green, because nothing compared any production
> copy to any OTHER production copy. A route could be reworded, its own test updated in the same
> commit, and the suite stayed green while that route now differed from the other nine.
>
> **The closure — ONE leaf, ONE pin file, twelve test literals untouched.**
> `src/lib/seam-copy.ts` holds the single declaration (zero imports, zero env reads, zero module-load
> side effects, guarded by `seam-copy.purity.test.ts` in the shape of the two shipped seam leaves —
> the copy is browser-reachable because the unauthenticated teaser renders it directly, T-140-08).
> All ten production files import it; nine keep the identifier `CIRCUIT_OPEN_COPY`,
> `src/lib/process-key-client.ts` keeps `CIRCUIT_OPEN_HUMAN_MESSAGE` as an import alias.
> `seam-copy.pin.test.ts` pins the bytes to a literal typed in the test file (never read out of the
> module under test) and adds a comment-stripped, repo-wide scan asserting **zero** non-test files
> re-declare the sentence and **exactly ten** import the leaf, with `10` hand-typed rather than taken
> from the discovered list's own length.
>
> ⚠️ **THE COUNTS ARE `10 production / 12 test`, AND BOTH ARE ENUMERATED BY THE SENTENCE.** Recorded
> here so a later reader does not re-derive them from a symbol grep. Nine production files bind
> `CIRCUIT_OPEN_COPY` and the tenth binds the sibling `CIRCUIT_OPEN_HUMAN_MESSAGE`; on the test side
> six declare a named const and **six inline the literal in the assertion**. So
> `grep -rn CIRCUIT_OPEN_COPY` finds **9 of 10** and **6 of 12** and reports success — the exact
> under-delivery shape this programme keeps hitting. Grep
> `"The analytics service is temporarily unavailable"`, never the symbol.
>
> **What `140.3-04` deliberately did NOT do**, so no later reader assumes it was done: it changed
> WHERE the string comes from, never what wraps it. The ten emitters still use five different envelope
> shapes and only two of them carry a `code` — that is envelope fragmentation, requirement
> **SEAMUX-03**, owned by a later plan; changing both in one pass is TRAP-8. No copy was reworded
> (that is `140.3-12`). At `src/app/api/keys/[id]/permissions/route.ts` this plan took **only** the
> `CIRCUIT_OPEN_COPY` declaration `140.3-03` left it; the `!res.ok` arm, its substring cascade, its
> hardcoded 502 and its dropped `Retry-After` remain **TS-34**'s half, still owned by plan `140.3-09`.
> **TS-34's disposition is unchanged by this note.** Falsifiers **M57** (the leaf's bytes, whose
> route-suite fan-out proves the ten emitters really read the leaf) and **M58** (a re-inlined local
> copy at the SECOND emitter, `simulator`) both OBSERVED RED and reverted.

> ### 📌 2026-07-27 — correction C-5 IN PLACE: the stale-result class (B-26 / SEAMUX-09) had **TWO live members plus one latent**, not one
>
> **No row was added and no row was removed by this note, so the obligation count is UNCHANGED at 40.**
> `140.3-07` closes a REQUIREMENT (`SEAMUX-09`); B-26, B-27 and B-28 never had TS rows, so there is
> nothing to move out of an open column. The note lives here because this file is the artifact the
> downstream phases read INSTEAD of re-deriving, and the claim it corrects is repeated in the findings
> doc and in `140.3-PATTERNS.md`.
>
> **How the under-count happened, and the predicate that fixes it.** The findings doc and PATTERNS both
> concluded "a class of ONE" by enumerating **candidates that look like the syntax of the known
> instance**. Enumerated by BEHAVIOUR instead — *"can this component render a FETCHED RESULT while an
> `error` is set?"*, which requires (a) result state, (b) a refetch trigger, and (c) no exclusivity
> between the result and error render paths — the class is:
>
> | Component | Result state | Refetch trigger | Renders result with error set? | Verdict |
> |---|---|---|---|---|
> | `PortfolioOptimizer.tsx` | `suggestions` | `runOptimizer` | **YES** | **LIVE 1 — fixed** |
> | `KeyPermissionBadge.tsx` | `perms` | `load` ("Re-check") | **YES** | **LIVE 2 — fixed** |
> | `ReplacementPanel.tsx` | `candidates` | no live trigger | structurally yes | **LATENT — pinned negatively, deliberately NOT fixed** |
> | `AllocatorMatchQueue.tsx` | `data` | `load` | no — `if (error \|\| !data) return` | immune by guard order |
> | `MatchEvalDashboard.tsx` | `metrics` | `load` | no — `if (error) return` first | immune by guard order |
> | `PortfolioImpactPanel.tsx` | one discriminated `state` | yes | no — `kind:"error"` REPLACES the result | immune by state design |
> | `OutcomesWidget.tsx` / `SendIntroPanel.tsx` | — | no refetch | n/a | not members |
> | `WeightOptimizerSection.tsx` / `partner-import/page.tsx` | `result` | yes | no — `setResult(null)` first | not members; **they ARE the fix shape** |
>
> **The second live member is the more serious one.** `KeyPermissionBadge` renders a stale
> **Read / Trade / Withdraw** verdict beside the error during an outage — a stale *security* claim about
> a money-bearing key, which a user reads as a current fact about their key's scope. A fix that closed
> only `PortfolioOptimizer` would have left it open while reporting SEAMUX-09 done.
>
> **⚠️ B-27 was ALSO a class of three, not a `ReplacementPanel` defect** — the identical
> `setError(e instanceof Error ? e.message : …)` shape sat at all three components above. All three are
> now 0 on that grep. It was **not** closed by replacing the message with a stable sentence: the
> optimizer route answers a breaker trip with `{ error: CIRCUIT_OPEN_COPY }`, so a blanket replacement
> would have DELETED the breaker surface `140.3-04` built. The discriminator is
> `src/lib/route-response-error.ts` — route-authored copy is rendered, every other caught value is
> replaced and goes to `console.error`.
>
> **What `140.3-07` deliberately did NOT do.** `WeightOptimizerSection.tsx` was read as the fix shape
> and **never edited** — its bare `} catch { setStatus("error"); }` (B-13) is plan `140.3-08`'s, and
> editing the file this plan copies from is TRAP-8. `AllocatorMatchQueue`'s missing `res.ok` check is
> also `140.3-08`. The `keys/[id]/permissions` route was read (to drive its real failure shapes) and
> not edited — `140.3-09` still owns TS-34's status/`Retry-After` half. **No TS row changed
> disposition.** Falsifiers **M72** (⚠️ returned GREEN — see the validation ledger; the render-guard
> half carries the behaviour, so the invalidation is pinned structurally), **M72b**, **M73** — the
> class test, RED at member 2 while member 1 stayed GREEN — **M74** and **M74b** were all observed
> first-hand and reverted.

> **Edited by plan `140.3-09` on 2026-07-27** — the plan that owned TS-34's status / `Retry-After`
> half. **TS-34 is now SATISFIED** and is marked so in both its §1 row and its §9.3 summary row, in
> this same edit, so the two cannot contradict each other one screen apart. **This is the ONE
> disposition change; no other row's disposition moved.**
>
> **Recount, written out so it cannot be double-counted.** Obligation total **HELD at 40** — this
> plan closed an existing row, it did not add or remove one. By-owner: **140.3 → 7** (was 8;
> − TS-34). Every other by-owner column is byte-unchanged.
>
> **TS-23 — annotated, disposition UNCHANGED (still OPEN, owner 146).** The third-429-shape fact this
> row records is now *consumed* on the TypeScript side: `keys/[id]/permissions` reads shape (b), the
> nested `service_error` envelope at `routers/internal.py`, and answers 429 with its `Retry-After`
> forwarded. Two carry-forwards for whoever migrates `match.py` / `simulator.py`:
> (1) the TS consumer gates on the **HTTP status**, not on `body.code`, so a migration that changes
> which shape carries the code cannot break it — but a migration that **drops the `Retry-After`
> header** silently removes the only wait the TS side can read, since
> (2) the TS side reads the **header only** and never the body's `retry_after_seconds`, and
> **never** parses a wait out of prose. The row's existing ⚠️ "PRESERVE the header, not re-derive it"
> warning is therefore now load-bearing for a shipped consumer, not merely prospective.
>
> **CORRECTION C-7, recorded in place.** Three claims this ledger's readers may still be carrying:
> - **B-11's mechanism claim was FALSE, and B-11/B-23 are the SAME finding** attributed to two
>   different files; **B-23 is the source-verified one.** B-11 says `Retry-After` is honoured at 1 of
>   4 wizard surfaces and that `SyncPreviewStep` reads it. **ZERO of the four wizard surfaces read
>   it** — `SyncPreviewStep`'s retry affordance is driven by `elapsedMs >= RETRY_THRESHOLD_MS`, a
>   client-side elapsed timer, not a header. The complete client reader class is **three** sites, of
>   which **exactly one** (`PortfolioImpactPanel` → `/api/simulator`) is a seam route; the other two
>   (`useMandateAutoSave` → `/api/preferences`, `StarToggle` → `/api/watchlist/*`) are not seam
>   consumers and were deliberately not touched.
> - **SC4 required PLUMBING before any copy could name a wait.** `WizardErrorContext` and
>   `ErrorEnvelope` carried **no wait field at all**, so "name the real wait" was *unrepresentable*,
>   not merely unimplemented. `140.3-09` added `WizardErrorContext.retryAfterSeconds` and
>   `ErrorEnvelope.retry_after_seconds` (SECONDS, additive-optional under the `cause?` precedent)
>   before writing a single sentence.
> - **The plan's own premise that this route "does not publish `Retry-After` at all" was FALSE** —
>   it emitted the header at four places, including an already-correct `CircuitOpenError` arm. A
>   whole-file `Retry-After` grep is therefore VACUOUS here; the criterion had to be behavioural.

> **2026-07-27 — `140.3-08` CLOSED SEAMUX-05 (observe-the-outcome), plus SEAMUX-04's C-9 sub-finding.**
>
> **NO OBLIGATION COUNT CHANGED.** No row was added, none removed, none re-homed, none changed
> disposition. SEAMUX-05 is a ROADMAP requirement, not a TS row, and the C-9 copy item is recorded in
> CONTEXT rather than as a ledger row. Stated explicitly because a plan that closes work without
> touching the count is exactly the shape that later reads as an unexplained gap.
>
> **SITE COUNT DELIVERED: 4, at 3 files — the planned number, and it is a receipt, not an assertion,
> because the rejected candidates are listed beside it.**
>
> | # | Site | Defect | Closed by |
> |---|---|---|---|
> | 1 | `ApiKeyManager.tsx` — the background sync after an add | B-06: transport-only `.catch()`; a 401/403/500/503 RESOLVED the promise and was invisible | `.then(observe).catch(surface)`, still un-awaited |
> | 2 | `ApiKeyManager.tsx` — `handleSyncTrades` | B-15: any 2xx set `syncStatus("computing")` | the `res.ok` path reads the body and requires `ok: true` |
> | 3 | `AllocatorMatchQueue.tsx` — `handleRecompute` | B-05/B-17: no `res.ok`, no `res.status`; a 503 fell to `else` and called `load()` | `res.ok` first, copying `handleDecision` in the same file; failure → the error-first card |
> | 4 | `WeightOptimizerSection.tsx` — `requestWeights` | B-13: a bare catch bound no variable; one undifferentiated error state | the catch reads the caught value; the `!res.ok` arm reads the status |
>
> **REJECTED CANDIDATES, each read first-hand (a site list with no rejections beside it is Pitfall 1):**
>
> - **`MatchEvalDashboard.tsx` — READ AND REJECTED, the plan's named adjudication.** It has **zero**
>   recompute call sites (`grep -c recompute` → 0). Its ONE fetch is `/api/admin/match/eval` inside
>   `load()`, which already does `if (!res.ok) { … throw }` and renders error-first (`if (error) return`).
>   There is nothing of this class to fix. **Not a fifth member.**
> - **`PreferencesPanel.tsx` — rejected.** It appears in a `recompute` grep only because it takes
>   `onRecomputeRequested`, which the queue binds to the SAME `handleRecompute` fixed above — a prop
>   hand-off, not a second call site. Its own PUT to `/api/admin/match/preferences/{id}` already checks
>   `res.ok` and reads `errBody.error`.
> - **`src/lib/resilient-fetch.ts`, `src/lib/auth/rbac-manifest.ts` — rejected.** They contain the
>   recompute route's PATH as a manifest/breaker-config key. Neither calls it. Both are syntax hits.
> - The four wizard `wizardFetch` sites — they already branch on the response; WHICH code they surface
>   is SEAMUX-03 (`140.3-10`) / SEAMUX-08 (`140.3-13`).
>
> **C-9 recorded in place: `ApiKeyManager` named `SUPABASE_SERVICE_ROLE_KEY` TO THE USER.** Deleted.
> `grep -rn "SUPABASE" src/components/ --include='*.tsx' | grep -v '\.test\.'` was **1** before this
> plan and is **0** after — the class checked across ALL of `src/components/`, not just the instance.
>
> **⚠️ ONE DEFECT FOUND WHILE REWRITING THAT ARM, fixed here rather than left for a second pass.** The
> arm's JSON branch read `err.error` ALONE. Every failure envelope `postProcessKey` builds carries its
> copy under **`human_message`** (`process-key-client.ts`, the `CircuitOpenError` arm: `{ok:false,
> code:"CIRCUIT_OPEN", human_message: <the sentence>}` at 503). So the breaker sentence `140.3-04`
> consolidated onto ONE production source **was reaching this consumer and being overwritten with
> "Trade sync failed"**. Same class as `140.3-07`'s B-27 finding — a failure path swallowing copy a
> route deliberately wrote. `syncFailureMessage()` now reads both keys.
>
> **TRAP-8 held.** `WeightOptimizerSection.tsx`'s `setResult(null)` — the locked shape `140.3-07`
> copied to both live B-26 members — is byte-unchanged: `git diff -U0 <range> -- <file> | grep -c
> "setResult(null)"` → **0**, and a new structural pin in that file's own suite asserts the line still
> precedes the fetch. Falsifiers **M62**, **M63** (the class test, at the SECOND member) and **M64**
> were observed first-hand and reverted.

> ### 📌 2026-07-27 — plan `140.3-10`: TRAP-3 described LIVE code, the `start_fresh` class is FOUR, and the SEAMUX-03 arms still open
>
> **No row was added and no row was removed by this note, so the obligation count is UNCHANGED at 40.**
> `140.3-10` closes requirement clauses (`SEAMUX-03` at one route, the TRAP-4 clause of `SEAMUX-06`,
> the TRAP-3 half of `SEAMUX-04`) — none of which has a TS row. The note lives here because this file
> is the artifact downstream phases read INSTEAD of re-deriving, and two of the three corrections below
> contradict a document a downstream plan would otherwise trust.
>
> #### 1. TRAP-3 DOES describe live code — at `keys/sync`, not at the site the findings doc examined
>
> CONTEXT correction **C-7** downgraded TRAP-3 to *"does not describe live code"*, on the evidence that
> `strategies/finalize-wizard` already uses `.maybeSingle()` and splits `strategyErr` (500, scrubbed)
> from `!strategyRow` (`GATE_DRAFT_GONE` 404). **That reading is correct about the site it looked at
> and wrong about the trap.** `src/app/api/keys/sync/route.ts` destructured `const { data: strategy } =
> … .single()` — **discarding `error`** — and `.single()` answers `data: null` for THREE different
> facts: no such row, a row this user does not own, AND a transport/query failure. So a Supabase blip
> **during our own outage** told the user *"Strategy not found"*, and the state that names is the one
> whose only actionable control throws the draft away.
>
> The fix took its shape from `finalize-wizard/route.ts:624-646` verbatim — that arm is the **template**
> C-7 correctly identified, and it was copied rather than re-invented. The transport arm logs through
> the shipped `scrubSeamError` leaf; no scrub was hand-rolled. **P458's uniform 404 survives the split**
> (a transport fault is a statement about US and was never a statement about the strategy), pinned by a
> byte-identity case asserting the not-found and not-owned responses are indistinguishable in body,
> status AND header set.
>
> ⚠️ **For the next reader:** an audit that asks *"is TRAP-3 live?"* by grepping for `.maybeSingle()`
> finds the template and stops. The defect is the DESTRUCTURE, not the terminal — `const { data } =
> await …maybeSingle()` is equally broken. Grep for the discarded `error`, not for the method.
>
> #### 2. ⚠️ THE `start_fresh` CLASS IS **FOUR**, NOT THE THREE THE RESEARCH TABLE LISTS
>
> `140.3-RESEARCH.md` Q5 gives it as "3 of 3". `grep -n "start_fresh" src/lib/wizardErrors.ts` returns
> **four** `actions` arrays:
>
> | Code | `actions` | Non-destructive control present? |
> |---|---|---|
> | `DRAFT_ALREADY_EXISTS` | `["resume_draft", "start_fresh"]` | ✅ `resume_draft` |
> | `GATE_NO_DATA_SOURCE` | `["start_fresh", "request_call"]` | ✅ `request_call` |
> | `GATE_DRAFT_GONE` | `["start_fresh", "request_call"]` | ✅ `request_call` |
> | **`GUARD_BLOCKED`** | `["clear_and_retry", "start_fresh", "request_call"]` | ✅ `clear_and_retry` — **absent from the research table** |
>
> **This is the correction that matters most for the next reader**, and not because of the missing row
> itself: the guard was consequently written over the **whole `WIZARD_ERROR_COPY` table** (53 entries,
> hand-typed size guard) rather than over a hand-listed three. Enumerating a class from a document's
> list rather than from the data structure is precisely how "3 of 5 log sites" and "5 of 7 routes"
> happened in this programme; the same defect applied to a GUARD produces a guard that silently covers
> three of four today and none of the members a future plan adds. `M66` is the falsifier and proves the
> guard is a table scan, not a list.
>
> #### 3. TRAP-4 remains UNTRIPPED — recorded as a negative pin, with the guards named
>
> The negative pin is `[140.3-10 / TRAP-4] the whole copy table, scanned for destructive-only error
> states` → `EVERY entry carrying a destructive action also offers a non-destructive control`
> (`src/lib/wizardErrors.test.ts`). Two further guards close the LIVE half, which a table scan cannot
> see:
>
> - `[140.3-10 / TRAP-4] Start fresh is confirmed before it destroys anything` — `handleStartFresh` now
>   opens the same confirm dialog the chrome's Delete-draft button has always used, instead of calling
>   `handleDeleteDraft()` directly. It was **strictly more dangerous than the chrome button one
>   component away**.
> - `TRAP-4 — the draft-gone state does NOT render the destructive control` — `GATE_DRAFT_GONE` no
>   longer renders "Try another key" (which fires `handleDeleteDraft()`) as its SOLE control.
>
> ⚠️ **`try_another_key` also reaches a delete** (`onTryAnotherKey` → `handleDeleteDraft()`), and it is
> deliberately NOT in the guard's destructive set. That path discards a draft holding a key the
> exchange just REJECTED — the point of the affordance, documented in-file, pinned by its own
> anti-regression case. Listing it would redden every `KEY_*` entry and turn the guard into noise. The
> asymmetry is recorded rather than smoothed over, because the next reader WILL notice it.
>
> #### 4. SEAMUX-03 — which arms this plan closed, and which remain open, BY ROUTE
>
> **`SEAMUX-03` is NOT closed by this plan.** One route is.
>
> | Route | Non-2xx arms | Carrying a code | Status | Owner |
> |---|---|---|---|---|
> | `keys/sync` | 8 | **8** | ✅ **CLOSED by `140.3-10`** | — |
> | `strategies/finalize-wizard` | 26 | 14 | ✅ the composite-cap arm (TS-37) is CLOSED — its own code `COMPOSITE_TOO_MANY_MEMBERS`, non-recoverable, admitted by `KNOWN_FINALIZE_CODES` in the same commit. The other three `COMPOSITE_MEMBERSHIP_UNKNOWN` arms deliberately unchanged. `captureToSentry` call count **held at 11** across both of this plan's commits. | `140.3-14` |
> | `verify-strategy` | 10 | 1 | ⬜ open | `140.3-13` (SEAMUX-08) |
> | `strategies/csv-finalize` | 17 | 30 | ⬜ not audited by this plan | `140.3-12` / `140.3-13` |
> | `strategies/csv-validate` | 0 | 1 | ⬜ not audited by this plan | `140.3-12` / `140.3-13` |
> | `keys/[id]/permissions` | 8 | 4 | ⬜ **deliberately a SEPARATE `PROBE_*` vocabulary** — decision and its measurement recorded by `140.3-09` under TS-34 | — |
> | `admin/match/*` | — | — | ✅ CLOSED 2026-07-27 by `140.3-11`. ⚠️ the wording here understated it — neither route had an `AnalyticsUpstreamError` arm at all, so 5xx flattened too; only the 4xx range now forwards (T-140-11) | `140.3-11` (TS-19/TS-18) |
>
> Counts are `grep -cE 'status: (4[0-9][0-9]|5[0-9][0-9])'` against a comment-excluded `grep -c 'code:'`,
> measured 2026-07-27. They are an ORDER-OF-MAGNITUDE receipt, not a per-arm audit — a route whose
> code-count exceeds its arm-count (`csv-finalize`) carries codes on 2xx bodies too. **The only row
> asserted arm-by-arm is `keys/sync`**, where all eight are individually pinned.
>
> ⚠️ **`keys/sync`'s eight arms are more than the five the plan enumerated.** The plan's arm table
> named five (two 400s, two 429s, one 404). Reading the file found three more codeless non-2xx arms:
> the NEW transport-error 500 the split creates, and **two pre-existing 503s** (the composite
> membership fail-closed, and the `enqueue_compute_job` failure) that no document listed. All three
> were given codes in the same commit — `COMPOSITE_MEMBERSHIP_UNKNOWN` for the membership fail-closed
> is the code `finalize-wizard` **already emits from its mirror of the same probe**, so this route was
> simply the straggler. Closing the enumerated five and leaving three would have been a "5 of 8".

### 2026-07-27 — SEAMUX-08 CLOSED across TWO halves. Nine of nine seam routes now capture.

> **No row was added and no row was removed by this note, so the obligation count is UNCHANGED at 40.**
> SEAMUX-08 is a ROADMAP requirement (SC6), not a TS-* row; this note records its closure and the
> counts that back it, because the class it closes was under-counted in a locked document.
>
> #### The correction that makes this note necessary
>
> **`140.3-CONTEXT.md` names TWO Sentry-less routes** — *"neither `create-with-key` nor
> `composite/add-key` imports Sentry — 0 hits"*. **The real class was NINE of fifteen**, five times
> the locked text, and C-9 already flagged it. Every one of the nine could land a user on an
> unexplained failure that reached nobody. A plan that closed the two named routes would have closed
> **22%** of the class while reporting the requirement done.
>
> #### The measurement, per route, from the TREE
>
> Measured with `grep -vE '^\s*(//|\*)' "$f" | grep -c "captureToSentry"` — **comment-excluded, never
> a raw count**, because both halves added explanatory prose naming the symbol and a raw `grep -c`
> would score those comments. The PRE column was measured on the untouched tree before either half
> wrote a character; the POST column was measured after both landed, by `140.3-13b`, **reading the
> files rather than reading `140.3-13a`'s SUMMARY**.
>
> | Route | PRE | POST | Closed by |
> |---|---|---|---|
> | `admin/match/eval` | **0** | 2 | 🟢 `140.3-13a` |
> | `admin/match/recompute` | **0** | 2 | 🟢 `140.3-13a` |
> | `keys/[id]/permissions` | **0** | 2 | 🟢 `140.3-13a` |
> | `verify-strategy` | **0** | 5 | 🟢 `140.3-13a` |
> | `strategies/create-with-key` | **0** | 5 | 🔵 `140.3-13b` |
> | `strategies/composite/add-key` | **0** | 5 | 🔵 `140.3-13b` |
> | `strategies/csv-validate` | **0** | 3 | 🔵 `140.3-13b` |
> | `portfolio-optimizer` | **0** | 2 | 🔵 `140.3-13b` |
> | `scenario/optimize` | **0** | 3 | 🔵 `140.3-13b` |
> | `bridge` · `keys/sync` · `keys/validate-and-encrypt` · `simulator` | 2 each | 2 each | already had |
> | `strategies/csv-finalize` | 8 | 8 | already had — ⚠️ the plan tabulated **10**; measured **8** comment-excluded. NOT opened (TS-24 / Phase 145) |
> | `strategies/finalize-wizard` | 11 | 11 | already had |
>
> **Zero-capture seam routes: 9 of 15 → 0 of 15.** 4 (`140.3-13a`) + 5 (`140.3-13b`) = 9.
> **No route is a stated exception; all nine capture.**
>
> #### THE CAPTURE POLICY — one sentence, so a future route does not re-derive it
>
> > **Capture from a seam route's TERMINAL, UNCLASSIFIED error arm — the arm reached when the caught
> > value matched no typed branch — and from any arm that detects an UPSTREAM CONTRACT VIOLATION (a
> > 2xx whose body cannot be used). Capture from NOTHING ELSE: not a breaker short-circuit
> > (`CircuitOpenError`), not an upstream timeout (`AnalyticsTimeoutError`), not a forwarded upstream
> > 4xx, and not a CSRF / auth / rate-limit / validation rejection the route makes itself. Pass the
> > caught value UNMODIFIED to `captureToSentry`, with `tags: { surface, step }` and a `secrets: [...]`
> > array naming every per-request credential in scope.**
>
> It is written out **in full, once**, in a docblock in `src/app/api/admin/match/eval/route.ts`; the
> other eight routes CITE that file rather than restating it. Two copies of a policy is how two
> policies start. `140.3-13b` applied it verbatim and **did not re-derive it**.
>
> **Two ambiguities were recorded in place rather than resolved silently, both by `140.3-13b`:**
> 1. **`scenario/optimize` has no 4xx/5xx range split** (it answers every `AnalyticsUpstreamError`
>    with a flat 502), so "terminal arm only" and "capture the service-permanent 5xx" disagree there.
>    Reading applied: discriminate on **STATUS**, not on arm shape — 5xx captured, 4xx not. Pinned in
>    BOTH directions and falsified by ledger row **M79c**. Adding the range split to the *response* is
>    a status-shape change (TS-34's family) and was deliberately not smuggled in.
> 2. **Both key-connect routes have no ladder of typed catch branches** — the shared
>    `classifyKeyValidationError` IS the ladder — so "matched no typed branch" is expressed as its
>    terminal verdict `code === "UNKNOWN"`. Recorded in-file at both routes.
>
> #### ⚠️ Sentry INGESTION is MANUAL and remains OWED. It is NOT verified by these counts.
>
> The tests at all nine routes prove the capture is **INVOKED** and that its payload is **SCRUBBED in
> both directions of TRAP-1**. They cannot prove an issue lands in org `metaworld-fund-ltd`, and none
> of them claims to. `140.3-VALIDATION.md`'s Manual-Only table carries this as the phase's one
> manual-only verification.
>
> ⚠️ **That table's instruction — "trip the breaker in preview" — DOES NOT WORK under this policy.**
> A breaker short-circuit deliberately captures nothing, at all fifteen routes, by design; running it
> and seeing no issue would read as a broken integration when it is the policy working.
> **THE CORRECT STEP:** point `ANALYTICS_SERVICE_URL` at an unroutable host in a preview deployment so
> the transport rejects, call any of the nine routes, and confirm an issue arrives in org
> `metaworld-fund-ltd` carrying that route's own `surface` tag (e.g. `surface: scenario-optimize`,
> `step: unexpected-error`). It requires a live DSN and a deployment — the local suite fakes the
> `@sentry/nextjs` transport by design, which is precisely what lets the REAL scrub run — so
> **`140.3-13b` did not attempt it locally and does not claim it.**
>
> **SEAMUX-08 is therefore CODE-COMPLETE and UNIT-VERIFIED at 9 of 9, with ingestion OWED.**

## 3. Rate limiting — Phase 146

| ID | Kind | Site | Fact | Action |
|---|---|---|---|---|
| **TS-21** | change | `analytics-service/routers/match.py` — `POST /api/match/recompute`, `GET /api/match/eval` | **RATE-03. Neither route has ANY limiter.** `grep -c "slowapi\|@limiter" routers/match.py` → **0**. The file's only throttle is the per-allocator 30-second `force=True` floor (`FORCE_RECOMPUTE_MIN_INTERVAL_S`), which is a duplicate-work guard, not a rate limit, and does not apply to `force=False` or to `/eval` at all. Both are seam-reachable (`analytics-client.ts:399,418`) and SERVICE_KEY-gated, so the exposure is an authenticated-caller loop, not an anonymous one. **`/eval` is now load-bearing**: plan 140.1-04 changed its paginator-cap answer from `503` to `400`, which removed the accidental back-pressure the old 503 provided via the breaker. | Add slowapi limits mirroring `portfolio.py`, using `partial(tenant_or_platform_key, scope=...)`. `tests/test_limiter_identity.py::test_match_routes_still_have_no_limiter` asserts the gap, so it goes **RED** when you close it — delete that test in the same commit rather than letting the note rot. |
| **TS-22** | change | limit **values** on L-1..L-9 | **RATE-04.** Values were deliberately untouched by 140.1-07 and are pinned as literals by `test_limit_value_is_unchanged_by_the_rekey`. The riskiest is **L-9 `/api/optimize-weights` at 20/minute**: it is a platform ceiling today and becomes **per tenant** the moment TS-04 lands — at which point 20/minute per tenant may be wrong in the *other* direction (too loose). | Audit after TS-04, not before. Sizing a per-tenant bucket against platform-wide observed traffic is how you get the number wrong twice. |
| **TS-23** ⚠️✅➡️ | change — **SPLIT DISPOSITION, recorded by plan 140.2-12 at the 140.2 gate. 140.2's half is DONE; the row stays OPEN with owner 146.** ✅ **DONE (140.2-06):** the discriminator tolerates ALL THREE 429 wire shapes without knowing the route, pinned by one case per shape in `seam-discriminator.test.ts` and end-to-end by `CALLER, THROTTLED: a 429 records ZERO in all three wire shapes` — the flat `main.py` handler body, the nested `service_error` envelope at `internal.py`, and the bare scalar carrying no `code` at all. A 429 records ZERO in every shape, so no shape can trip the breaker while the migration is pending. ➡️ **STILL OWED, owner 146:** the Python migration of `match.py` / `simulator.py` (and `portfolio.py` ×2) **and the decision of which shape WINS** — 140.1.2 deliberately did not decide it and 140.2 is TypeScript-only, so neither could. ⚠️ **Carry-forward the migration must not lose:** 140.1.2 (PYAPIFIX2-04) added `Retry-After` to all four bare-scalar 429s **header-only, bodies byte-unchanged**. The migration must **PRESERVE** that header, not re-derive it — `service_error`'s `retry_after` kwarg sets it via `_retry_after_headers`, and dropping the existing `headers=` kwarg without passing `retry_after` silently regresses the wire. ⚠️ **A wait is NEVER derived by parsing prose** — the TS side reads the header or nothing. | `routers/match.py` + `routers/simulator.py` 429 sites | **FINDING-12.** Both `raise HTTPException(429, detail="<string>")`, so they answer `{"detail": "…"}` while every slowapi 429 answers the `RATE_LIMITED` envelope. **Two 429 shapes now coexist**, and a 140.2 discriminator keying on `body.code` finds none on those two. Neither is in the S-table (which enumerates 5xx-capable sites), and both pre-date phase 140.1. Registration order is irrelevant by construction — a plain `HTTPException(429)` does not have `RateLimitExceeded` in its MRO, so Starlette never routes it to the app-global handler. | Migrate both to `service_error()` / the `RATE_LIMITED` envelope. Owner is shared: 140.2 needs it for the discriminator, 146 owns the limiter surface. **✅ UNBLOCKED by 140.1.1 PYAPIFIX-06(a)** — the helper can now construct a 429 with `Retry-After`. Before that fix `retry_after` required `retryable:true` while the CALLER arm raised on `retryable:true`, so the envelope these two migrations need was **unconstructable**. **Stays OPEN, owner unchanged (140.2 / 146)** — 140.1.1 supplied the arm, not the migration. ⚠️ **Annotated by Phase 140.1.2 (PYAPIFIX2-03): there are now THREE 429 shapes, not two.** `routers/internal.py`'s per-key probe throttle was migrated onto `service_error(429, "RATE_LIMITED", …)` — giving the arm its first call site and proving the migration this row asks for — so the service answers 429 as (a) the flat `main.py` handler body `{ok, code, human_message, detail:<scalar>, correlation_id, recoverable, retry_after_seconds}`, (b) the nested R-2 envelope `{"detail": {code, dependency, retryable, detail}}` at `internal.py`, and (c) the bare scalar `{"detail": "<string>"}` still at `match.py` / `simulator.py` (and `portfolio.py` ×2, which this row never enumerated). **This row's owner must pick which shape WINS** when it migrates match/simulator — 140.1.2 deliberately did not decide it, and adding a fourth shape was explicitly refused there. `internal.py` is the worked example; note the flat `main.py` shape cannot simply be adopted by a raise site, because it is a RETURNED `JSONResponse` from an exception handler. ⚠️ **140.1.2 also added `Retry-After` to all four bare-scalar 429s (PYAPIFIX2-04, header-only, bodies byte-unchanged)** — so the migration this row owns must PRESERVE the header, not re-derive it: `service_error`'s `retry_after` kwarg sets it via `_retry_after_headers`, and dropping the existing `headers=` kwarg without passing `retry_after` would silently regress the wire. |
| **TS-36** ⚠️ | change — **added by Phase 140.2 plan 09 (TS-04)** | **146** | `analytics-service/tests/` — a NEW pytest reader; its input is the COMMITTED fixture `tests/fixtures/tenant-claim-parity.json` (repo root `tests/`, not `analytics-service/tests/`) | **The cross-language parity gate for `X-Tenant-Claim` is HALF-BUILT and this row says so out loud.** Plan 140.2-09 landed the TS half: `src/lib/tenant-claim.ts` is the ONE mint, and BOTH `src/lib/tenant-claim.test.ts` and `src/lib/analytics-client.test.ts` bind to the committed case table (4 cases: payload, secret, exp, expected claim) rather than to a copy-pasted literal. **No Python reader consumes that file yet.** Until one does, the only thing pinning the Python verifier to the TS mint is the single hand-copied literal 140.1 left in `analytics-service/tests/test_process_key.py::test_cross_language_claim_from_ts_client_buckets_to_tenant` — which proves someone once copied correctly, not that the two implementations still agree. This phase is TypeScript-only by its own CONTEXT, so writing the pytest here was out of scope, NOT forgotten. | Add a pytest that reads `tests/fixtures/tenant-claim-parity.json`, feeds each case's `claim` to `services/rate_limit.py:verify_tenant_claim` with that case's `secret` in `INTERNAL_API_TOKEN`, and asserts the returned payload. Bind to the BYTES — do not re-derive the table, and do not fork a second fixture. Neutering either side must redden the other's suite, the way `process_key_onboard_contract.json` already does for TS-01. ⚠️ Two of the four cases carry an `exp` in 2050/2030 precisely so the expiry check does not become a time bomb; keep it that way. Pairs naturally with **TS-21** (adding the match-route limiters) and **TS-22** (auditing the limit VALUES now that they are per-tenant) — all three are 146's. |

## 4. Job / csv-finalize — Phase 145

| ID | Kind | File:line | Fact | Action |
|---|---|---|---|---|
| **TS-24** ➡️ | change — **OWNER CONSOLIDATED TO 145 by plan 140.2-12 at the 140.2 gate. 140.2's half is NOT DONE, and that is stated rather than absorbed.** ⚠️ **AND `140.3-02` DID NOT DO IT EITHER — stated explicitly so 145 does not inherit a false assumption.** `140.3-02` edited this exact file and this exact function (`unifiedCsvFinalizeHandler`) for TS-13, landing one line above the `debug_context` echo, and deliberately left the echo and the `console.error` of the whole upstream body UNTOUCHED. Re-verified after that plan, not asserted: `debug_context` still appears 28× in the file and `unified_response_body: unifiedBody` is still on the 502. Two phases editing one file in one pass is TRAP-8, and 145 owns this file's transaction shape. **145 still owns the whole row.** Phase 140.2 shipped NOTHING for this row — verified against the phase diff, not asserted: `git diff <phase-base>..HEAD --name-only | grep csv-finalize` → **0**, and `debug_context` still appears 28× in that file. Consolidating rather than leaving 140.2 as a co-owner is deliberate: 140.2 is now CLOSED, and a row pointing at a finished phase is an obligation with no owner. **This is what the row itself already asked for** — 145 owns this file's transaction shape (JOB-06), and landing the two halves separately would put two phases in one file, which is TRAP-8, receipted. | `src/app/api/strategies/csv-finalize/route.ts:1216` and `:1225` | **The `debug_context` echo is the TS half of C-13 and is COMPLETELY UNTOUCHED.** `:1216` `console.error`s the whole upstream body; `:1225` returns `debug_context: { unified_response_body: unifiedBody }` inside a 502. **Fixing the Python emission does not fix either** — PYAPI-07 stopped the *service* echoing credentials in its own 422, but this route takes whatever the service sends and re-emits it into a log line and a client-visible body. Class 6, path 2. | Strip to `{code, correlation_id}`. Owner is shared 140.2 / 145 — 145 owns this file's transaction shape (JOB-06), so land them together to avoid two phases in one file (TRAP-8). |
| **TS-25** | change | `routers/process_key.py` → `enqueue_compute_job` | **`p_idempotency_key` — FLAGGED, NOT DONE.** The RPC accepts one (`20260515210300…:258`); `/process-key` passes **none**, on both the fresh and the duplicate path. Dedupe is therefore purely `(strategy_id, kind, non-terminal status)`-based. Note the dedupe has **two** layers, not one — the RPC's optimistic SELECT *and* the `compute_jobs_one_inflight_per_kind_strategy` partial unique index (discovered by plan 140.1-02's falsifiability probe, which the index rejected). | Passing `verification_id` as the key is a cheap tightening. **Not** required by PYAPI-09 and deliberately out of 140.1's scope. |

## 5. Ops / founder

| ID | Kind | Subject | Fact | Action |
|---|---|---|---|---|
| **TS-26** ➡️ | change — **HANDED OFF TO ops by plan 140.2-12 at the 140.2 gate. NOT built here, and deliberately so.** 140.2 shipped NOTHING for this row and that is the correct outcome: the credential-carrying probe belongs on the **`warm-analytics` warmer** and must **NEVER** extend `/health`. Two independent reasons, both now enforced by tests this phase built rather than by convention: **(1) A-12 / STATUS_CONTRACT O-7** — routing `/health` through the seam core feeds `recordSeamFailure` on every cold start, trips `breaker:railway`, and the open breaker then short-circuits the recovery probe: **the mitigation becomes the outage**. Plan 140.2-03's source scan over every `SEAM_EXCLUSIONS` path now FAILS if either warmer imports or calls the core (falsifier **M23**, RED at the final tree, 3 cases). **(2) T-140.1-24** — `/health` deliberately stays HTTP **200** when config is degraded (`config_ok:false` + `config_degraded_secrets`) so the signal cannot become the outage via a Railway restart loop. **Owner: ops / founder.** | C-11 residual — stale Vercel-side `ANALYTICS_SERVICE_KEY` | Plan 140.1-08 added a `config_fault=mismatched` Sentry capture on each 401, rate-limited to 1 per 5 min. That is **detection after the fact**. Proactive detection needs a **credential-carrying probe**, and it **CANNOT be a `/health` extension**: A-12 / STATUS_CONTRACT O-7 forbid routing `/health` through the seam core, because the breaker would then block its own recovery probe. `/health` also deliberately stays **HTTP 200** when config is degraded (`config_ok:false` + `config_degraded_secrets`), so that the signal cannot become the outage via a Railway restart loop (T-140.1-24). | Put the probe on the **`warm-analytics` warmer** — a separate, breaker-exempt call that presents the real credential. Do NOT extend `/health`. |
| **TS-27** | record | Sentry capture throttle is **per process** | `_SECRET_CAPTURE_MIN_INTERVAL_S = 300.0` lives in a module-global monotonic dict. With N replicas a config fault produces up to **N** captures per 5-minute window — bounded, but not 1. | Measured below: **N = 1 today**, so the current behaviour is exactly 1 capture/window. Re-check if replicas ever scale. |
| **TS-28** | record | `memory://` limiter storage is **per replica** (ASSUMPTION-3) | With N replicas every limit is N× looser and buckets reset unevenly on rolling deploys. This matters more now than before the phase, because the numbers are becoming *per-tenant* and a per-tenant number is easier to misread as an SLO than a platform one. | Measured below: **N = 1**, so **no N× multiplier applies today** and the documented limits are the real limits. Re-check before quoting any of them as an SLO, and before scaling replicas — scaling silently multiplies every limit. |
| **TS-29** | record | `FORWARDED_ALLOW_IPS` (ASSUMPTION-2) | If set to `*`, `get_remote_address` returns the leftmost `X-Forwarded-For` entry (caller-controlled) instead of the edge IP. | Measured below: **NOT SET**. Assumption confirmed; no action. It does not change any recommendation either way, because the seam's client is a Vercel lambda behind egress NAT, so per-IP is the wrong dimension regardless. |
| **TS-40** 🚨 | record — **added by the 140.2 REVIEW-FIX pass (VERIFICATION W-3). FOUNDER / REPO-ADMIN ACTION. Outside any agent's control.** | **This repo has NO branch protection, so EVERY CI gate is advisory at merge.** | Confirmed independently, twice: `gh api repos/:owner/:repo/rulesets` returns `[]` and `gh api repos/:owner/:repo/branches/main/protection` returns **404 "Branch not protected"**. The workflow wiring is CORRECT and STRICT — `frontend-seam-redis` is in `needs:` at `ci.yml:782` and takes the strict `elif [ "$result" != "success" ]` arm at `:796`, with no fork-skip tolerance. **Nothing enforces the aggregator at merge.** So the coverage ratchet, the full vitest suite, the mypy/pytest job, and the digest-pinned real-Redis lane this phase BUILT are all bypassable by a merge that ignores a red check. ⚠️ This makes every "gated by CI" claim in the 140.x ROADMAP conditional on a repo setting that does not exist — which is exactly the shape of unfalsifiable assurance this programme exists to end, one level up from the code. | **Founder action, not an agent's:** create a ruleset (or classic branch protection) on `main` requiring the `frontend` aggregator, the `python` job, and `e2e-seeded`. Until then, treat every CI-gate claim in 140.x as "the workflow would have caught it", never "the workflow did stop it". Recorded prominently rather than acted on: an agent changing repo settings is out of scope by construction. |

## 6. Needs its own plan

These two are **not** assignable to 140.2 / 140.3 / 145 / 146 as written. They need `/gsd-phase --insert`
→ `/gsd-plan-phase`, per the programme's governing correction (no finding reaches code except through
the plan → plan-check gate).

| ID | Kind | Subject | Why it needs its own plan |
|---|---|---|---|
| **TS-39** ⚠️ | change — **added by the 140.2 REVIEW-FIX pass (HI-01 residual + ME-04). Owner: Phase 141, which owns the breaker's timing behaviour.** | **Two breaker properties the review-fix pass BOUNDED but could not close, each blocked by a named constraint rather than by effort.** **(a) The tombstone re-arm still ratchets by the burst's write-time spread.** HI-01's fix makes the OPEN EVENT exclusive — `SET ... GET` returns what it displaced, and you own the transition iff what you displaced was not a live lock — but the losing racers have already written by the time they learn they lost, so `expiresAtMs` moves by milliseconds against a 30 s cooldown. BOUNDED, not compounding (once the lock is live every later read returns at the still-live-lock guard). Closing it needs a write CONDITIONED on the value read, i.e. a Lua CAS or a claim key; the claim key costs a second store round trip and **`seam-budgets.invariant.test.ts` SC-4b proves that breaches the Vercel ceiling** — finalize-wizard's composite branch goes to 320 000 ms against 300 000 ms. Verified by making the change and watching SC-4b redden. **(b) ME-04 — the anonymous teaser records onto `breaker:railway`, the key EVERY call site checks.** Five failures inside the 30 s window open the global circuit for every authenticated user. Containment needs a `breaker:teaser` member of `SeamServiceDependency`, a CLOSED set hand-typed in `resilient-fetch.ts`, re-typed in `seam-discriminator.ts`, pinned in `seam-constants.pin.test.ts`, and **mirrored by `SERVICE_DEPENDENCIES` in `analytics-service/services/error_contract.py`, whose `_validate` REFUSES an unknown dependency** — a cross-language change, and 140.2's fence is zero Python. It is also not a re-key: `process-key-sync` is a MIXED row (authenticated csv-validate/csv-finalize spend it too), so containment needs keying by CALLER, not by budget row. | **(a)** Decide the round-trip-vs-Lua trade with SC-4b's arithmetic in hand — raising `STORE_COMMANDS_PER_SEAM_CALL.failing` to 4 requires lowering a budget, lowering the composite fan-out cap, or tightening the breaker store constants, and each is a deployed-behaviour change. **(b)** Decide between a dedicated anonymous key (needs the Python `SERVICE_DEPENDENCIES` edit, so pair it with a 146 pass), excluding anonymous flows from recording (they are lead-generation — but a failing teaser IS a real health signal, so this trades attribution for containment), or keeping the exposure with the corrected comment that now ships. ⚠️ Both are pinned by tests that must stay green: "HI-01: only ONE of N instances re-arming from the SAME tombstone claims the trip" and "ME-04: the anonymous teaser's failures land on the key EVERY call site checks" — the latter asserts the UNCOMFORTABLE fact deliberately, so a change of stance reddens it rather than silently drifting. |
| **TS-30** ⚠️ | change | **FINDING-10 — `routers/simulator.py:92` is a TENTH IP-keyed route** | `return f"simulator:ip:{get_remote_address(request)}"`. Plan 140.1-07's do-not-touch list describes this function as *"correctly user-keyed"* — **that premise is FALSE against the source.** A PR#241 follow-up removed the `X-User-Id` read (the header was unsigned, so a caller could mint a fresh 20/hour bucket per request, and `analytics-client.ts` never forwarded it anyway), moving the per-tenant quota **in-handler** to `_check_simulator_user_rate` and leaving the slowapi decorator as an IP-keyed ceiling — which behind Railway's edge is platform-wide. **Its module docstring is also stale in the same direction**, still claiming the key function *"reads `X-User-Id` … and falls back to the remote address only when the header is absent"* when it reads no header at all. **Cost of leaving it:** `/api/simulator` is 20/hour platform-wide at the slowapi tier — but unlike L-1..L-9 it has a real per-user tier underneath, so the exposure is a ceiling one tenant can exhaust for everyone. Meaningfully less severe than L-9's 20/minute was; same defect class. **Quarantined mechanically** by `IP_KEYED_QUARANTINE = frozenset({"simulator.py"})` with an **equality** assertion, so the exemption cannot silently grow AND goes red the day it is repaired — forcing the allow-list to shrink rather than rot. ⚠️ **PYAPI-03's reconciliation number is 9/9. Do NOT report 10 closed.** |
| **TS-31** 🚫→change | **NEGATIVE then change** | **FINDING-11 — ~~six~~ NINE test modules mount a bare `FastAPI()`** ⚠️ *count corrected by plan 09* | `test_match_router.py:37`, `test_process_key.py:103`/`:516`, `test_simulator_router.py:115`/`:851`/`:1140`/`:1224`/`:1568`, `test_status_contract_exchange_internal.py:391`, `test_process_key_200_discriminator.py:58`, `test_sfox_internal_probe.py:42` — **plus three plan-09 found that the finding missed: `test_status_contract_match_sim_portfolio.py:71`/`:333`, `test_maybe_single_none_guards.py:107`, `test_debug_key_flow_router.py:30`.** (`test_correlation_middleware.py:39` also mounts a bare app but `include_router`s nothing — it is legitimately testing middleware and is NOT in the class.) All nine `include_router` onto a bare app, so they never see the app-global 422/429 handlers and their 422s render in FastAPI's **default, leaking** shape. **NEGATIVE half: nothing in any of them is vacuous today** — all six assert `status_code == 422` only, or assert a handler-produced `_envelope_error` body — so do NOT schedule a "fix the six broken tests" sweep; there are no broken tests. **Positive half: PYAPI-07 is gated by exactly ONE file** (`tests/test_validation_error_contract.py`, which drives the real `main.app`), and `test_process_key.py` is the natural place a future author would "prove the 422 is safe" — and would prove nothing. A shared app-factory fixture closes it. Test-infrastructure change, well outside any current phase's fence. |

## 7. Measurements taken by this plan

### 7.1 PROD wedge blast radius — **0 rows**

RESEARCH Q4 asks for a read-only count of rows already wedged in PROD, because "rows already sitting
at `strategy_verifications.status='draft'` with no `compute_jobs` row are fixed *the next time the
user retries* but are not swept proactively. Do not ship a code fix that leaves existing wedges in
place and call the class closed."

Run **read-only** against PROD `khslejtfbuezsmvmtsdn` via the Supabase Management API
(`POST /v1/projects/{ref}/database/query`). `psql` is not installed locally and the Supabase CLI is
linked to **PROD**, so `--linked` was deliberately not used and **no write of any kind** was issued.

```sql
select
  (select count(*) from public.strategy_verifications) as sv_total,
  (select count(*) from public.strategy_verifications where status = 'draft') as sv_draft,
  (select count(*) from public.strategy_verifications sv
     where sv.status = 'draft'
       and not exists (select 1 from public.compute_jobs cj where cj.strategy_id = sv.strategy_id)
  ) as wedged_draft_no_job,
  (select count(*) from public.strategy_verifications sv
     where sv.status = 'draft'
       and not exists (select 1 from public.compute_jobs cj
                       where cj.strategy_id = sv.strategy_id
                         and cj.status in ('queued','running','pending'))
  ) as draft_no_nonterminal_job;
```

| `sv_total` | `sv_draft` | **`wedged_draft_no_job`** | `draft_no_nonterminal_job` |
|---|---|---|---|
| 20 | 0 | **0** | 0 |

**→ Blast radius: ZERO rows. No backfill is needed. The founder decision RESEARCH asked for is moot.**

**Two anti-vacuity checks, because a `0` from a mistyped literal is exactly the silent-green this
programme exists to eliminate:**

1. **`'draft'` is a legal value**, not a typo — read from the live CHECK constraint:
   `CHECK (status = ANY (ARRAY['draft','validated','metrics_captured','encrypted','report_queued','published']))`.
2. **The status column is populated** — PROD holds `published` × 12 and `validated` × 8, totalling the
   20 rows. `compute_jobs` holds `done` × 1222 and `failed_final` × 106, so the `not exists` join has
   real rows on both sides and is not trivially true.

**Recorded while there:** PROD's `strategy_verifications` still carries the OLD single-column
`strategy_verifications_wizard_session_id_unique_idx` over `{wizard_session_id}` — i.e. migration
`20260726000225` has **not** reached PROD, as expected on an unmerged branch. It auto-applies on merge
to `main` and **is a production event**; plan 140.1-01's PROD-apply watch obligation stands.

### 7.2 Railway — ASSUMPTION-2 and ASSUMPTION-3 both CLOSED

Project `quantalyze-analytics`, environment `production`.

| Check | Result | Consequence |
|---|---|---|
| `FORWARDED_ALLOW_IPS` | **NOT SET** (51 vars, none matching) | ASSUMPTION-2 confirmed. `get_remote_address` returns the edge IP, not a caller-controlled `X-Forwarded-For` entry. No action. |
| replica count — `quantalyze-analytics` | **`numReplicas = 1`** (`europe-west4-drams3a`) | ASSUMPTION-3 confirmed. |
| replica count — `mt5-gateway` | `numReplicas = 1` | — |

**→ N = 1, so limiter numbers are NOT N× looser and the secret-capture throttle really is 1 per
window.** The documented limits are the real limits **today**.

⚠️ **Stated explicitly rather than silently assumed, because this is the number that rots:** the
moment anyone scales `quantalyze-analytics` past one replica, **every** limit in
`services/rate_limit.py` becomes N× looser (`memory://` storage is per-process), buckets reset
unevenly on rolling deploys, and TS-27's capture throttle allows up to N captures per window. Nothing
in the codebase detects that. Re-run this check before scaling, and before quoting any limit as an
SLO.

## 8. Findings consolidated — coverage check

Every finding surfaced during 140.1 execution, and where it landed. **Nothing is silently dropped;
items with no home are declared homeless.**

| Finding | Source | Home |
|---|---|---|
| **FINDING-10** — `simulator.py:92` is a 10th IP-keyed route; the plan's "correctly user-keyed" premise is FALSE | 140.1-07 | **TS-30** — needs its own plan |
| **FINDING-11** — ~~six~~ **nine** modules mount a bare `FastAPI()`; PYAPI-07 gated by exactly one file *(count corrected by plan 09 — the finding under-enumerated its own class, which is the very defect shape this programme exists to catch)* | 140.1-08, corrected 140.1-09 | **TS-31** — needs its own plan |
| **FINDING-13** — `test_simulator_router.py::TestG15_005_RateLimitIsUserKeyed::test_route_uses_user_keyed_key_func_not_ip` asserts only `key_func is not get_remote_address`. That passes while `simulator.py:92` returns `f"simulator:ip:{get_remote_address(request)}"` — the key func *wraps* the IP function rather than *being* it. The class name, the class docstring and the test name all assert "user-keyed"; the route is IP-keyed. **A second misleading test at the exact site FINDING-10 names** | 140.1-09 | **TS-30** — fold into FINDING-10's own plan; repairing `simulator.py:92` must also repair this test's name/claim |
| **FINDING-14** — `TEST_SUPABASE_DB_URL` is wired into the `sql-tests` CI job ONLY (`ci.yml:810`), never into the `python` job (`ci.yml:1030-1033` sets `SUPABASE_TEST_URL` / `SUPABASE_TEST_SERVICE_KEY` / `CI` and nothing else). **31 pytest cases gated on `TEST_SUPABASE_DB_URL` therefore skip in CI exactly as they do locally — they run nowhere** | 140.1-09 | **HOMELESS** — CI wiring, no owning phase |
| **FINDING-15** — `HAS_PY_ENV` is set in **zero** files repo-wide (grep across the whole tree excl. `node_modules`/`.git` returns only the three lines inside `tests/test_outcomes_kpi_parity.py` itself). The 5 Phase-4-vs-Phase-5 KPI parity cases it gates are permanently dormant | 140.1-09 | **HOMELESS** — CI wiring, no owning phase |
| **FINDING-16** — 4 `tests/test_repro_key_flow.py` cases skip on missing **binance** cassettes; `tests/cassettes/` holds only `okx/` and `bybit/`. Missing fixtures skip in CI too (founder gate 16-08 Task 3, still open) | 140.1-09 | **HOMELESS** — founder gate, no owning phase |
| **FINDING-12** — `match.py` / `simulator.py` raise plain `HTTPException(429)`; two 429 shapes coexist | 140.1-08 | **TS-23** — 140.2 / 146 |
| **`_scope_rejected` three-arm OR** — `process_key.py:1245-1249`; a malformed CSV now answers 403 where **422** would be sharper | 140.1-05 | **TS-14** (consumer half) + **TS-30-adjacent**: see the note below — the *split itself* is **HOMELESS** |
| **Vacuous test** — `test_simulator_router.py::test_main_app_state_limiter_is_same_singleton` claims to check `app.state.limiter`; its body is a verbatim copy of its sibling and never touches `app.state` | 140.1-07 (O-7) | **Declared HOMELESS** — see below |
| **RATE-03** — `/api/match/recompute` + `/eval` have NO limiter | 140.1-04, 140.1-07 | **TS-21** — Phase 146 |
| **RATE-04** — limit VALUES untouched; L-9's 20/minute needs a re-look once per-tenant | 140.1-07 | **TS-22** — Phase 146 |
| **C-11 residual** — credential-carrying probe belongs on the warmer, never `/health` | 140.1-08 (O-5) | **TS-26** — 140.2 / ops |
| **Secret-capture throttle is per-process** — N replicas ⇒ up to N captures/window | 140.1-08 (O-9) | **TS-27** — measured N=1 in §7.2 |
| **M6 / M7 numbering collision** — 140.1-08 ran mutations it called "M6"/"M7" which are NOT canonical M6/M8 | 140.1-08 (O-6) | **Reconciled in `140.1-09-SUMMARY.md`'s ledger** as supplementary S-06/S-07; both canonical M6 and M7 have their own first-hand evidence from plans 02 and 05 |
| **O-1..O-9** — the status contract's numbered obligations | 140.1-03, in `docs/STATUS_CONTRACT.md` §6 | **TS-05 (O-5)**, **TS-06 (O-1/O-2/O-3)**, **TS-10 (O-9)**, **TS-26 (O-7)**. §6 remains the canonical copy — it ships in-repo and survives without this file |
| **O-A..O-G** — plan 05's seven consumer obligations | 140.1-05 | O-A→**TS-11**, O-B→**TS-17**, O-C→**TS-12**, O-D→**TS-13**, O-E→**TS-01**, O-F→**TS-02**, O-G→**TS-14** |
| **`p_idempotency_key` not passed** | 140.1-02 | **TS-25** — Phase 145 |
| **`analytics_runner.py:1725` raises `HTTPException(500)` from the WORKER** — a category error that can never render | 140.1-03 | Recorded in `docs/STATUS_CONTRACT.md` §7. **Declared HOMELESS** — see below |
| **Vestigial assertion** — `test_verify_service_key_middleware.py::test_correct_service_key_passes_middleware` still names `503` as "a middleware rejection"; the two rejection statuses are now `401`/`500` | 140.1-04 | **Declared HOMELESS** — deliberately untouched (adding `500` makes it flaky, removing `503` changes nothing) |
| **TRAP-9 casualty class B2** — plans enumerate production sites exhaustively but not the TESTS those changes invalidate. Plan-check found 4; plans 06, 07 and 08 each found **one more the plan did not predict** (5, 6, 7) | 140.1-06/07/08 | **Declared HOMELESS as a process item** — see below |

### Declared HOMELESS — four items with no owning phase

Stated explicitly rather than dropped, per this plan's brief. **None is a code defect that breaks a
user today**; each is a correctness or hygiene debt that no phase in the current roadmap owns.

1. **The 403-vs-422 split on `/process-key`.** `_scope_rejected` is one return behind a three-arm OR,
   so PYAPI-10b necessarily moved ordinary `not val.valid` failures to 403 alongside the two scope
   arms. 403 is not *wrong* — all three arms are CALLER-class and breaker-inert — but it is **coarse**:
   a malformed CSV answering 403 instead of 422 tells the user "you're not allowed" when the truth is
   "that file is broken". Splitting into `403` (scope/authorization) + `422` (malformed input) is a
   clean follow-up. **TS-14 hands consumers the fact; nobody owns the split.**
2. **The vacuous `test_main_app_state_limiter_is_same_singleton`.** Pre-existing, not caused by
   PYAPI-03 — but PYAPI-03 raised the stakes, since nine more routes now depend on that object being
   the one `main.py` registers. Plan 140.1-07 asserted the real invariant in its own file
   (`test_limiter_identity.py::test_app_state_limiter_is_the_same_object_the_decorators_use`) rather
   than editing a file outside its fence, so the invariant IS covered. **The misleading test still
   exists and nobody owns deleting it.**
3. **`services/analytics_runner.py:1725` raises `HTTPException(500)` from the worker.** Its only
   caller is `services/job_worker.py:1947`. An `HTTPException` raised outside an HTTP request can
   never render. Not a contract item, so it has no S-number; flagged in STATUS_CONTRACT §7 and here.
   Nobody owns it.
4. **The B2 process gap.** Plans in this programme enumerate *production* sites exhaustively but not
   the *existing tests* those changes invalidate. Plan-check caught 4 up front; execution then found
   a 5th (140.1-06), a 6th (140.1-07 — 60 tests across two casualty classes plus two latent
   test-infra defects) and a 7th (140.1-08). That is a **planning-template** gap, not a code defect:
   every plan's `read_first` should require a TRAP-9 casualty enumeration with a rewrite-vs-extend
   decision per test. It belongs in the programme's standing lessons, not in a phase.

---

## 9. What Phase 140.3 inherits — written at the 140.2 gate by plan 140.2-12

**⚠️ 140.3's own planning pass MUST start numbering at `140.3-03`.** Slots `01` and `02` are already
taken by two plans re-homed out of 140.2 on 2026-07-26. Numbering from `01` would overwrite them.

### 9.1 The two plans that are already written and sitting in its directory

| Plan | Was | Carries | Hard prerequisite, and it is MET |
|---|---|---|---|
| `140.3-01-PLAN.md` | `140.2-12` | **TS-05, TS-08, TS-09** — the three Class-5 `typeof body.detail` sites × two contracts, plus two `WizardErrorCode` union members | needs `src/lib/seam-discriminator.ts` from **140.2-06** — ✅ shipped, purity-guarded, **zero imports**, bundle-safe for a `"use client"` component. `seamErrorCode` / `seamHumanMessage` are the O-5 fix's shape. |
| `140.3-02-PLAN.md` | `140.2-13` | **TS-02, TS-11, TS-12, TS-13, TS-14, TS-15** — every `/process-key` consumer branches on `ok`; `X-User-Access-Token` forwarded and provably scrubbed | needs `src/lib/seam-redaction.ts` from **140.2-08** — ✅ shipped. ⚠️ **This is a SAFETY ordering, not a convenience one:** the token is a LIVE end-user Supabase JWT, and undici inlines outgoing headers into `err.message`. 140.3-02 must land AFTER 140.2-08, never before. |

**Six Falsifiability Ledger rows (M51–M56) travelled with them** and must appear in
`140.3-VALIDATION.md`. **Phase 140.2 did NOT re-run them and does NOT claim them** — its ledger is
correspondingly 55 rows as planned (56 after M19R), not 57.

### 9.2 The seam machinery 140.3 renders from — all shipped and falsifiable

- **`src/lib/seam-discriminator.ts`** — the attributability leaf and its extractors (`seamErrorCode`,
  `seamHumanMessage`, `seamBreakerVerdict`). ZERO imports; purity-guarded.
- **`src/lib/seam-redaction.ts`** — the ONE redaction leaf. ZERO imports, safe from inside a catch
  block and safe in a browser bundle.
- **`src/lib/seam-errors.ts`** — `CircuitOpenError` (now REFUSING a `retryAfterS` it cannot honestly
  publish) and `SeamBodyReadError`. ⚠️ **Its exported surface is pinned to a hand-typed TWO-member set;
  adding a class means editing `EXPECTED_EXPORTS` in the SAME commit.**
- **Per-dependency breaker keys** — `breaker:<dependency>` plus the residual `breaker:railway`.
- ⚠️ **Any plan adding a budget key, a wrapper, a `budgetKeyFor` arm or a core call site must update
  `EXPECTED_BINDINGS` (and `EXPECTED_BOUND_KEYS` / `EXPECTED_SEAM_CALL_FILES`) in the SAME commit.**
  This is a hard gate, not a convention — falsifier **M22b** proves a census would not catch it.

### 9.3 Rows explicitly left to 140.3, beyond the nine re-homed

| Row | What is owed |
|---|---|
| **TS-32** | ✅ **SATISFIED 2026-07-27 by `140.3-06`** (commit `1f8ad052`) — all seven sites answer `424`, class re-derived mechanically from the marker type (7 sites / 2 files), verified per-file in both directions with a multi-line-aware grep (`exchange.py` 6, `portfolio.py` 1, zero `400` remaining). C7 included despite its dead route, for class integrity. Fixture (14 cases + `invariants[]`) and `EXPECTED_STATUS` closed in the SAME commit; the argued rationale REWRITTEN, not renumbered. `BLOCKED-BY: TS-05` corrected in place — body-neutral remap, real blocker was TS-35 (C-4). 🔴 Two further test files (`test_sfox_validate.py`, `test_mt5_validate.py`) also pinned these sites and were absent from the plan's coupling analysis; 8 tests updated, none relaxed. ~~🟢 **SCHEDULABLE 2026-07-27 — `140.3-05` landed TS-35, the REAL blocker. Owned by `140.3-06`.**~~ The stated `BLOCKED-BY: TS-05` named the wrong row (C-4: the remap is body-neutral). The parity test `140.3-05` shipped reads `status` from the fixture and pins no status literal, so the remap cannot invalidate it. ~~🔓 **UNBLOCKED 2026-07-27 — `140.3-01` HAS now run and landed TS-05.**~~ The `400 → 424` remap is still owed and is now schedulable. ⚠️ But see the row itself: correction **C-4** holds TS-05 was never the real blocker (the remap is body-neutral on `VenueTransientHTTPException`); the real one is **TS-35**, and the remap invalidates the 14 fixture bytes TS-35's parity test binds to. **Schedule TS-35 → TS-32 as ONE unit, in that order.** ~~⛔ **STILL BLOCKED.** … which has NOT run.~~ |
| **TS-33** | ✅ **SATISFIED by `140.3-14`** (2026-07-27), executed in 140.3's own pass as required. ONE field (`wizard_session_id`) in the finalize-wizard `postProcessKey` context, sourced from the owner-scoped draft row rather than the request body. ⚠️ **The row's premise was corrected in place: the CSV path sends the id today, so the mechanism was never dead — the true scope was ONE call site.** TS-01's do-NOT-re-widen warning honoured; parity test green; zero migrations. |
| **TS-34** | ✅ **SATISFIED 2026-07-27 across `140.3-01` (extraction) + `140.3-09` (status / `Retry-After`).** A `/internal` throttle answers **429** with the upstream wait forwarded verbatim; absence of a `Retry-After` yields no header and no claimed duration (TRAP-3). Gated on the upstream STATUS, not the wire code — which also settles `140.3-05`'s two-vocabulary residual. The cascade survives intact and this route's private `PROBE_*` vocabulary stays separate from `WizardErrorCode` **by recorded decision** (measured: 5 of 6 messages fall to UNKNOWN/500 through `classifyKeyValidationError`, whose `RATE_LIMITED` verdict is 503 and so cannot produce the 429 this row requires). See the §1 row for the full mechanism, the decision and the enumerated body-field residual. ~~Land with TS-05, same file.~~ |
| **TS-35** | ✅ **SATISFIED by `140.3-05` (2026-07-27).** The render half of PYAPIFIX2-01 landed: a closed `ReadonlyMap` lookup below the breaker type check and above the surviving cascade, `KEY_EXCHANGE_UNAVAILABLE` + `KEY_VENUE_TRANSIENT` added, and a cross-language parity test bound to the committed fixture. ⚠️ Its stated damage was **under-counted by one**: `DDOS_PROTECTION` was rendering `KEY_IP_ALLOWLIST`, not `UNKNOWN` — corrected in place on the row (C-6). W-4 rider honoured: `recoverable` was NOT wired into any automated retry. |
| **TS-20** | ✅ **SATISFIED 2026-07-28 by `140.3-15`.** `seamCorrelationId` — the FIFTH narrow member of the leaf, on `140.3-11`'s recorded mechanism, `EXPECTED_EXPORTS` 4 → 5 in the same commit — reaches the `correlation_id` slot `ErrorEnvelope` and `buildDiagBlock` already had, at both consumers that hold a seam body. The field is still carried for **display only** and consumed for **no breaker decision** (TS-16 re-confirmed by name: `seamBreakerVerdict` does not call it). See the §2 row for the shape guard and the both-shape read. |

### 9.4 Two copy items 140.2 deliberately did NOT author (its fence forbade it)

1. **The composite fan-out cap arm** returns the EXISTING `COMPOSITE_MEMBERSHIP_UNKNOWN` envelope
   verbatim — *"A transient check couldn't determine…try again"*. That copy is **wrong for a permanent
   condition**: a user with an oversized composite is told to retry, and retrying loops. The operator
   half is already distinct (`step: "composite-member-cap"` + a dedicated log line). **140.3 should
   give this arm its own code and copy.**
2. **The ambiguous-transport outcome** (non-JSON 2xx / 204 / 205 / 304) reuses the EXISTING
   `UPSTREAM_NETWORK_ERROR` 502 envelope and its existing copy. If 140.3 wants it to read differently
   to a user, that is a new code and new copy — **140.3's call, not a gap left by 140.2.**

   ### ✅ 140.3's CALL, MADE AND RECORDED — `140.3-15`, 2026-07-28: **LEAVE IT.**

   140.2 was explicit that this is a DECISION owed to 140.3 and not a gap, and an unrecorded
   non-decision reads to the next phase as an oversight — so the decision is written here rather
   than left silent. **The existing envelope is not a false claim about this outcome.** A Railway
   edge page served as `200 text/html`, and the three null-body statuses, are genuinely ambiguous
   *transport* results: something answered, unusably, and we did not reach the service. *"Could not
   reach the ingestion service"* is true of all of them, `recoverable: true` is honest (a
   maintenance page and a 204 both clear on their own), and 502 correctly says the gateway answered
   unusably. **Nothing here is the false-attribution class SEAMUX-04 exists to close**, which is
   what separates it from TS-38 in the same file: a config fault is OURS and permanent, and its
   envelope asserted the opposite on both counts. Minting a fourth code for a case whose existing
   copy is already true would grow the vocabulary without removing a lie — the exact
   *"a second member with the same meaning"* hazard this table's own convention forbids.
   **Item 1 above (the composite fan-out cap arm) was SATISFIED by `140.3-14`; item 2 is now
   decided. §9.4 is closed.**

---

*Produced by plan 140.1-09. If you are a 140.2 / 140.3 / 145 / 146 planner: read this file INSTEAD of
re-deriving from `140-FINDINGS-CONSOLIDATED.md`. Re-deriving is the collision mechanism that cost the
programme 37 commits.*
*§9 and the 2026-07-27 dispositions added by plan 140.2-12 at the Phase 140.2 gate.*

---

## 📌 Dated note — `140.3-14`, 2026-07-27: TS-37 + TS-33, and the falsifiability position

### TS-01's do-NOT-re-widen warning: CONFIRMED HONOURED, by name

TS-33's field is precisely the thing TS-01's widening was bought FOR, so this plan is the first
opportunity to re-widen the predicate by accident. It did not.

- `src/lib/process-key-onboard-contract.ts` appears in **neither** of this plan's two commits
  (`e32c16a9`, `08a3fb41`). Verified with `git diff --name-only`, not asserted.
- **`tests/lib/process-key-onboard-contract-parity.test.ts` is GREEN** — that is the test that
  proves the three compensating invariants still hold, and it drives the REAL predicate over the
  committed fixture with zero module mocks.
- Zero files under `supabase/migrations/` were added or touched.

The predicate's teeth were also exercised incidentally and first-hand: the duplicate-resume test was
initially written with a stale `Response` mock and returned **502**, which is exactly the H-5 failure
signature this row warns about. The 502 turned out to be a test-harness fault (a `Response` body can
only be read once, and `mockResolvedValue` hands the same object to every call), **not** a contract
rejection — the corrected envelope `{queued:false, verification_id, code:"WIZARD_DUPLICATE",
idempotent:true}` is admitted, as invariants 1 and 2 require.

### ⚠️ NO Falsifiability Ledger rows exist for TS-37 or TS-33 — stated explicitly, not left silent

`140.3-VALIDATION.md` carries **no row** targeting either obligation. Neither M75 (a
zero-SSR-exposure grep pin that merely names `finalize-wizard/route.ts`) nor any of M51–M79c covers
the cap split or the dedupe field. A silent absence is how a change ships unfalsified, so this plan
ran **seven mutations anyway**, all observed RED first-hand and reverted, and proposes them as rows
**M80–M83** for `140.3-16` to adjudicate. Full receipts are in `140.3-VALIDATION.md`.

**The tests that would redden if either change were reverted, by name — so the gate can check rather
than take this on trust:**

| If reverted | Reddens |
|---|---|
| The cap arm's code | `finalize-wizard/route.test.ts` → *"fails LOUD when the member list OVERFLOWS the cap"* |
| The `KNOWN_FINALIZE_CODES` membership | `SubmitStep.test.tsx` → the two `[140.3-14]` cap cases (**and NO route test** — that is the point) |
| The copy's `actions` | `wizardErrors.test.ts` → *"carries NEITHER recoverable action…"* **and** `SubmitStep.test.tsx` → *"renders NO Retry control"* |
| Any of the three TRANSIENT arms (the inverse defect) | `route.test.ts` → that arm's own case only: W-4 (arm 1), CR-01 + the new `[140.3-14 / TS-37]` anti-regression (arm 2), the unified-path case (arm 4) |
| `MAX_COMPOSITE_MEMBERS` drifting from the copy | `wizardErrors.test.ts` → *"names the LIMIT WITH ITS NUMBER, and the number is the route's own cap"* |
| The `wizard_session_id` field | `route.test.ts` → the four `TS-33` cases |
| The id being synthesised per request | the same four — **and this is the one a `toBeDefined()` oracle would sleep through** |

**The four arms are proven INDEPENDENTLY covered.** Each arm was mutated ALONE and reddened only its
own case(s): arm 1 → 1 failed, arm 2 → 2 failed, arm 3 (the cap) → 1 failed, arm 4 → 1 failed, with
all other arms GREEN in every run. That matters here more than usual, because this class is four
emissions of ONE code where only one is the permanent condition — mutating the wrong arm would have
proven nothing.

---

## 📌 Dated note — `140.3-15`, 2026-07-28: TS-38 + TS-20, and the falsifiability position

### The `140.3` owner bucket is EMPTY. Recounted, not asserted.

TS-38 and TS-20 were the last two rows this phase owned. Both are SATISFIED; both moved into the new
`DONE-IN-140.3-15` bucket; the total is unchanged at **40** (−2 +2 = 0) and the sum above is written
out so `140.3-16` can reproduce it rather than trust it.

### ⚠️ NO FALSIFIABILITY LEDGER ROW EXISTED FOR EITHER OBLIGATION — stated, then closed

`140.3-VALIDATION.md` rows M51–M83 contain nothing for TS-38 and nothing for TS-20. The plan
instructed the executor to state that absence and name the tests that would redden. **Stating an
absence is weaker than closing it**, and both `140.3-11` and `140.3-14` set the precedent of
authoring the rows instead — `140.3-11`'s did, and one of them (M77c) found a real hole. **Seven
mutations were run and rows M84 / M84b / M84c / M84d / M85 / M85b / M85c are proposed** for
`140.3-16` to adjudicate. Full receipts are in `140.3-VALIDATION.md`.

The tests that would redden if either change were reverted, named as the plan asked:

| Change reverted | Reddens |
|---|---|
| the `SEAM_MISCONFIGURED` arm in `process-key-client.ts` | 6 of the 8 `[140.3-15 / TS-38]` cases in `process-key-client.test.ts` |
| the wire status 500 → 502 | *"answers a status that attributes the fault to US, never 502"* (M84d) |
| the entry's `actions` regaining `clear_and_retry` | the copy-table case AND the rendered no-Retry case (M84c) |
| `SubmitStep`'s `KNOWN_FINALIZE_CODES` membership | the two `[140.3-15 / TS-38]` render cases — **and NOTHING else** (M84b) |
| the wire→wizard table entry | ONE direct unit case — see the M84 finding below |
| the leaf's nested branch | 4 cases across the leaf AND both consumers (M85) |
| either consumer's `upstreamCorrelationId ??` | that consumer's cases only; the other stays green (M85b) |
| the leaf's `CORRELATION_ID_SHAPE` guard | 3 cases across the leaf and both consumers (M85c) |

### 🔴 M84's finding — the wire→wizard entry is REDUNDANT at the only current consumer

Dropping `["SEAM_MISCONFIGURED", "SEAM_MISCONFIGURED"]` from `SEAM_CODE_TO_WIZARD_CODE` reddened
exactly ONE test, and **all three `SubmitStep` TS-38 render cases stayed GREEN.** The cause is
structural rather than a coverage gap: `SubmitStep` translates first and then falls back to the RAW
code (`const candidate = translated === "UNKNOWN" ? data.code : translated`), and because the wire
code and the wizard member deliberately share a NAME, the raw fallback rescues it. The entry is kept
regardless — it is the ONE recorded wire→wizard table, `recogniseSeamErrorCode("SEAM_MISCONFIGURED")`
returning `UNKNOWN` would be wrong on its face, and any future consumer that follows `SubmitStep`'s
own written instruction to *"TRANSLATE THE WIRE CODE FIRST"* depends on it. Recorded so the next
reader does not delete it on the strength of a narrow mutation result.

### 🔴 A pre-existing 4-member gap found and DELIBERATELY NOT fixed — for `140.3-16`

`SyncPreviewStep`'s `KNOWN_KICKOFF_CODES` is a **SECOND, LOCAL wire→wizard table**, separate from
`recogniseSeamErrorCode`. It admits `RATE_LIMITED`, `GATE_DRAFT_GONE`,
`COMPOSITE_MEMBERSHIP_UNKNOWN`, `MISSING_STRATEGY_ID` and `INVALID_STRATEGY_ID` — and **none** of
`process-key-client`'s four codes. So at that consumer `CIRCUIT_OPEN`, `UPSTREAM_TIMEOUT`,
`UPSTREAM_NETWORK_ERROR` **and** the new `SEAM_MISCONFIGURED` all fall to `SYNC_FAILED`.

This is **pre-existing and not a regression**: the new code is treated identically to its three
siblings, which `140.3-05`, `-10` and `-12` each left as they are. It was NOT closed here for a
reason that is worth the ink: **adding `SEAM_MISCONFIGURED` alone would TRIP TRAP-4 at that site.**
The code is non-recoverable, so no Retry renders, and `SyncPreviewStep`'s remaining control is
"Try another key" → `handleDeleteDraft()`, which DESTROYS the draft. Closing it therefore requires
widening `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` in the same change — exactly the coupling `140.3-12` had
to make for `VALIDATION_FAILED`. That is a 4-code class change with a data-loss failure mode, owned
by SEAMUX-01, not a drive-by. Logged in `deferred-items.md` as **DEF-15-1**.

### `140.3-14`'s routed residual — the cap arm's 503. LEFT, with reasoning, not absorbed

`140.3-14` recorded that `COMPOSITE_TOO_MANY_MEMBERS` still answers **503**, a transient-signalling
status on a permanent condition, and routed it here to adjudicate. **It is outside this plan's
fence and it stays open.** The arm lives in `src/app/api/strategies/finalize-wizard/route.ts`, a
file this plan does not touch and one of the five `postProcessKey` caller routes whose absence from
the diff is an acceptance criterion; changing it would also redden `140.3-14`'s own re-pinned case,
which asserts the 503 explicitly. `140.3-14`'s reason for holding it remains true — `SubmitStep`
maps off `code` alone and never status, so nothing user-visible depends on it. **Recorded as still
open for `140.3-16`, not silently absorbed.** Note for whoever takes it: TS-38's new arm chose
**500** over 503/504 for precisely this reason and does not inherit the mismatch, so the two arms
now disagree about how to signal a permanent condition — closing the cap arm's 503 would make them
agree.

---

## 🏁 PHASE GATE — `140.3-16`, 2026-07-28. Phase 140.3 closes; every obligation dispositioned.

### The count is VERIFIED, not re-asserted — and no row was added or removed

`140.3-15` recounted the header from the §1/§2 rows and wrote the arithmetic out. **The gate
re-added it independently rather than trusting it:**

```
0 + 0 + 2 + 4 + 2 + 5 + 2 + 2 + 4 + 0 + 2 + 6 + 1 + 1 + 1 + 2 + 2 + 2 + 2 = 40 ✓
```

**`140.3-16` adds ZERO rows and removes ZERO rows.** The total stays **40**. A phase gate that
minted obligations to look thorough, or retired them to look finished, would be doing the opposite
of its job.

### ⚖️ THE 18-vs-19 ARITHMETIC, stated explicitly rather than reported at whichever number is convenient

The plan requires this be spelled out, because the two numbers are both true of different things and
picking one silently is how a row goes missing.

- **18** is the count in the ledger's own **`140.3` owner column**: TS-02, TS-05, TS-08, TS-09,
  TS-11, TS-12, TS-13, TS-14, TS-15, TS-17, TS-18, TS-19, TS-20, TS-32, TS-34, TS-35, TS-37, TS-38.
- **+1 = TS-33.** It never sat in that column. It sat in the separate **`owner decided by 140.2-12`**
  bucket — 140.2's gate decided the OWNER without moving the row — while being 140.3's work
  throughout. `140.3-14` executed it in 140.3's own pass, exactly as its row required.
- **⇒ 19 obligations were owed to and closed by Phase 140.3.**

**The independent check:** summing the nine `DONE-IN-140.3-NN` buckets in the header —
2 + 6 + 1 + 1 + 1 + 2 + 2 + 2 + 2 = **19** ✓. Two derivations, one from the owner column and one
from the completion buckets, agreeing. The `140.3` owner bucket now reads **0** as a *consequence*.

### The 19 dispositions — honest verb per row, with the closing plan named

**Never "SATISFIED" for a re-homed row**: the nine that arrived from 140.2 (TS-02, TS-05, TS-08,
TS-09, TS-11, TS-12, TS-13, TS-14, TS-15) were recorded **RE-HOMED → 140.3** by `140.2-12` and are
marked SATISFIED **here**, by the plan that actually did the work, not by the phase that handed
them over.

| # | ID | Verb | Closed by | Note carried forward |
|---|----|------|-----------|----------------------|
| 1 | TS-02 | ✅ SATISFIED | `140.3-02` | branch keys on the CODE, never on `queued`. Falsified by **M53**, re-run at the gate |
| 2 | TS-05 | ✅ SATISFIED | `140.3-01` | **3 of 3 Class-5 sites — but a DIFFERENT 3** (C-2/C-3). Falsified by **M51 A+B** |
| 3 | TS-08 | ✅ SATISFIED (verify) | `140.3-01` | six cases written out per site, not parameterised |
| 4 | TS-09 | ✅ SATISFIED — **across TWO plans** | `140.3-01` (type half) + `140.3-12` (copy half) | neither plan alone closes it; both named |
| 5 | TS-11 | ✅ SATISFIED (verify, **not edited**) | `140.3-02` | the ledger's verdict was right: no change was required |
| 6 | TS-12 | ✅ SATISFIED | `140.3-02` | **with a documented correction to the row's own premise.** Falsified by **M54** |
| 7 | TS-13 | ✅ SATISFIED | `140.3-02` | `isUuid` survives — the DRIFT guard is not dead code |
| 8 | TS-14 | ✅ SATISFIED (route half) | `140.3-02` | envelope handled by `ok`, not by status |
| 9 | TS-15 | ✅ SATISFIED | `140.3-02` | **found a LIVE credential leak on the way.** Falsified by **M55** + **M56 A/B** |
| 10 | TS-17 | ✅ SATISFIED | `140.3-12` | the teaser reads `human_message` before `error` |
| 11 | TS-18 | ✅ SATISFIED — ⚠️ **with a REACHABILITY residual, named not absorbed** | `140.3-11` | the 424 render is correct and falsified (**M77b**, **M77c**). **`match.py` emits ZERO 424s, so it is not reachable in production from those two routes today.** What is LIVE is TS-19's forwarding. See the residual note below |
| 12 | TS-19 | ✅ SATISFIED | `140.3-11` | the flattening was BROADER than the row stated — neither route had an `AnalyticsUpstreamError` arm at all. Falsified by **M77**, re-run at the gate |
| 13 | TS-20 | ✅ SATISFIED | `140.3-15` | `seamCorrelationId`, the FIFTH leaf member. Falsified by **M85 / M85b / M85c** |
| 14 | TS-32 | ✅ SATISFIED | `140.3-06` | all 7 sites answer 424. Falsified by **M76** (a MIDDLE member). ⚠️ **doc residual DEF-11-1 — see below** |
| 15 | TS-33 | ✅ SATISFIED | `140.3-14` | ONE field, executed in 140.3's own pass. Falsified by **M83 A+B** |
| 16 | TS-34 | ✅ SATISFIED — **across TWO plans** | `140.3-01` (extraction) + `140.3-09` (status/`Retry-After`) | both named because neither alone closes it. Falsified by **M65 / M65b** |
| 17 | TS-35 | ✅ SATISFIED | `140.3-05` | the C-6 mis-map closed; oracle bound to OUTCOMES, never UNKNOWN-ness. Falsified by **M60** |
| 18 | TS-37 | ✅ SATISFIED — ⚠️ **with a STATUS residual, LEFT OPEN deliberately** | `140.3-14` | the cap has its own permanent code and copy naming the number. **It still answers 503 — a transient-signalling status on a permanent condition.** See the residual note below |
| 19 | TS-38 | ✅ SATISFIED | `140.3-15` | a config fault stops wearing the upstream's envelope. Falsified by **M84b / M84c / M84d** |

**19 SATISFIED · 0 PARTIALLY SATISFIED · 0 RE-HOMED · 0 unaddressed.** Two carry named residuals
(TS-18, TS-37) which are recorded as residuals of a satisfied row, not as reasons to downgrade it —
140.2's SC4 model.

### 🔴 The three residuals, restated so they are not lost at the phase boundary

1. **TS-18 — reachability.** The render is built, tested and falsified at both consumers, but
   `analytics-service/routers/match.py` emits **zero** 424s, so no live 424 reaches those two routes
   today. The code is correct and dormant. **This is not a defect and not a gap in TS-18** — the row
   asks for the render — but a reader who assumes the path is live would be wrong.
   **Owner: whoever gives `match.py` a venue-transient arm.**
2. **TS-37 — the cap arm's 503.** A permanent condition answered with a transient-signalling status.
   `140.3-15` examined it and **explicitly declined**, because changing it reddens `140.3-14`'s own
   pinned case — the fix and its oracle must move in one commit. **Correctly refused, not missed.**
   **Owner: Phase 141 or a follow-up; it is a one-commit change, not a drive-by.**
3. **DEF-11-1 — `STATUS_CONTRACT.md` still documents `VenueTransientHTTPException` as 400.**
   `140.3-06` remapped all seven sites to **424** and the doc's app-global-handler table was not
   updated, because `140.3-11` (which found it) was fenced to zero files under `analytics-service/`
   and `140.3-06` is the phase's only sanctioned Python wave. **One table cell, and the doc is the
   canonical emit-side reference every consumer plan reads.** ⚠️ **`140.3-16` did NOT fix it either,
   and the reason is deliberate:** this plan changes no production behaviour and touches zero files
   under `analytics-service/` — a gate that repairs what it is measuring cannot measure it.
   **Owner: Phase 141.** *Verified still open at the gate.*

### 📌 Two founder items carried forward — restated, not buried

- **TS-40 — EVERY CI gate in this repo is ADVISORY at merge.** `gh api` returns `rulesets: []` and
  `branches/main/protection` → **404**. No CI check can *block* anything. **Every claim in this
  phase's documents is phrased "the workflow WOULD HAVE caught it", never "the workflow DID stop
  it"**, and that discipline is load-bearing rather than pedantic: the two are different facts and
  only one of them is true here. *(Branch protection is deliberately deferred until paying clients
  by a settled founder decision — recorded, not re-raised.)*
- **The Sentry INGESTION verification is MANUAL and STILL OWED.** `140.3-13a` and `140.3-13b`
  together prove, at all nine routes, that the capture is INVOKED and its payload scrubbed in both
  directions. **No unit test can prove an issue lands in org `metaworld-fund-ltd`**, and none claims
  to. ⚠️ **The stock instruction "trip the breaker in preview" DOES NOT WORK** — a breaker
  short-circuit deliberately captures nothing, at all fifteen routes, by design; doing it and seeing
  no issue would read as a broken integration when it is the policy working correctly. **The correct
  step is to force a TERMINAL-ARM failure**: point `ANALYTICS_SERVICE_URL` at an unroutable host in
  preview so the transport rejects, call any of the nine, and confirm an issue arrives carrying that
  route's own `surface` tag (e.g. `surface: scenario-optimize`, `step: unexpected-error`). Requires a
  preview deployment with live Sentry ingestion and a DSN; **the local suite fakes the
  `@sentry/nextjs` transport by design, which is what lets the REAL scrub run.**
