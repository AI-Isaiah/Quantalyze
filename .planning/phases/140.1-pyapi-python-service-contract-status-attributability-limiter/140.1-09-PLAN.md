---
phase: 140.1-pyapi-python-service-contract-status-attributability-limiter
plan: 09
type: execute
wave: 5
depends_on: [140.1-01, 140.1-02, 140.1-03, 140.1-04, 140.1-05, 140.1-06, 140.1-07, 140.1-08]
files_modified:
  - .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
autonomous: true
requirements: [PYAPI-01, PYAPI-02, PYAPI-03, PYAPI-04, PYAPI-05, PYAPI-06, PYAPI-07, PYAPI-08, PYAPI-09, PYAPI-10]
must_haves:
  truths:
    - "Every CI gate that will run on the PR is green locally: full pytest with coverage, mypy --strict, SQL gates, vitest for the one TS file"
    - "The mutation checks M1-M8 each have first-hand proven-RED evidence and the tree is clean"
    - "140.2/140.3 receive one consolidated obligations artifact; the founder receives the wedged-rows blast-radius number"
  artifacts:
    - path: ".planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md"
      provides: "Consolidated TS/ops obligations ledger for 140.2/140.3 + Phase 146"
  key_links:
    - from: "140.1-TS-OBLIGATIONS.md"
      to: "140.2/140.3 planning inputs"
      via: "phase-directory artifact read by their planners"
      pattern: "140.2"
---

<objective>
Phase gate: run every gate CI will run, complete the mutation-check ledger (M1-M8), consolidate the
cross-phase obligations into one artifact, and measure the PROD wedge blast radius.

Purpose: this phase repairs code that shipped "verified" with 166 latent defects — the gate plan is
where the falsifiability doctrine is enforced end-to-end, and where 140.2/140.3 get their contract
inputs instead of re-deriving them.
Output: green gate evidence, M1-M8 ledger, 140.1-TS-OBLIGATIONS.md, wedged-rows count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-RESEARCH.md  ("Mutation checks the plan must run", "Coverage / gate obligations", ASSUMPTION-2/3, open questions 4-5)
All 140.1-0N-SUMMARY.md files in the phase directory (the per-plan obligation notes being consolidated).
</context>

<critical_warnings>
1. **A milestone pytest run does NOT run mypy** — the strict run here is the last line before PR CI.
   Fix any error via cast()-narrowing, never `# type: ignore`.
2. **Local pytest reds in csv_validator/middleware/mt5 are the known missing-`pandera` gap**, not a
   defect — `pip install 'pandera==0.32.1' --break-system-packages` if hit. CI has it.
3. **Mutation checks already run inside Plans 01-08 are re-verified from their SUMMARY evidence,
   not re-run** — re-run ONLY any mutation whose evidence is missing or ambiguous. After any re-run:
   `git status` clean and `grep -rn MUTANT analytics-service/ src/` empty.
4. **Do not close TODOS.md items or edit ledgers** — obligations go in the phase artifact; backlog
   changes are the orchestrator's call.
</critical_warnings>

<tasks>

<task type="auto">
  <name>Task 1: Full gate run — pytest+coverage, mypy --strict, SQL gates, vitest</name>
  <files>(no source changes expected — fixes only if a gate is red)</files>
  <read_first>
    - analytics-service/pytest.ini and .github/workflows/ci.yml:993-1029 (the exact python-job
      commands to mirror)
    - CLAUDE.md Test Coverage section (vitest thresholds — relevant only if shared fixtures moved)
  </read_first>
  <action>
    Run, in order, and capture outputs: (1) cd analytics-service && python3 -m pytest
    --cov=services --cov=routers --cov=main_worker --cov-fail-under=80 -q; (2) python3 -m mypy
    --strict --follow-imports=silent services/ routers/ models/; (3) both new supabase/tests gates
    against TEST (psql or MCP); (4) npx vitest run for the process-key-client test with
    --no-file-parallelism. If shared contract fixtures changed anywhere in Plans 01-08, run BOTH
    pytest AND the full vitest suite (repo lesson: shared fixtures diff → both suites). Any red:
    root-cause and fix in the owning file, then re-run the full set — never skip silently.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest --cov=services --cov=routers --cov=main_worker --cov-fail-under=80 -q && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <acceptance_criteria>
    - pytest green with coverage ≥80 on the CI-mirrored command (paste the coverage summary line).
    - mypy --strict: 0 errors; no new "type: ignore" vs the pre-phase baseline (grep evidence).
    - Both supabase/tests gates green on TEST; vitest green for the TS file.
  </acceptance_criteria>
  <done>Every gate PR CI will enforce is green locally with pasted evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation ledger M1-M8 complete + clean tree</name>
  <files>(scratch mutations only — reverted)</files>
  <read_first>
    - .planning/phases/140.1-*/140.1-RESEARCH.md "Mutation checks the plan must run before declaring
      done" (the M1-M8 table with each mutation's named oracle)
    - The eight plan SUMMARYs' mutation-evidence sections
  </read_first>
  <action>
    Build the ledger: for each of M1-M8, cite the SUMMARY evidence (which oracle went RED, when) or
    re-run it now. M2 (drop scoping from the race-winner site ONLY), M6 (literal queued:True without
    enqueuing) and M8 (delete ok from shape 5 only) are the second-member-of-class detectors — if
    ANY of the three lacks explicit first-hand evidence, re-run it; do not accept "covered by the
    suite". After any re-run, revert and prove the tree clean.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain)" && ! grep -rn "MUTANT" analytics-service/routers analytics-service/services src/lib/process-key-client.ts</automated>
  </verify>
  <acceptance_criteria>
    - The ledger (in the SUMMARY) lists all 8 mutations, each with its named RED oracle and evidence
      source; count pinned: 8/8.
    - M2, M6, M8 have first-hand evidence (not inference).
    - Working tree clean; no mutation residue anywhere.
  </acceptance_criteria>
  <done>Falsifiability doctrine enforced: every tuning value and every class's second member has a proven-RED oracle.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidated obligations artifact + PROD wedge count + ops checks</name>
  <files>.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md</files>
  <read_first>
    - All 140.1-0N-SUMMARY.md obligation notes
    - .planning/phases/140.1-*/140.1-RESEARCH.md OPEN-1/OPEN-2 (the 424 + Retry-After obligations),
      ASSUMPTION-2/3 (Railway checks), Q4 "Data remediation" (the wedge-count spec)
  </read_first>
  <action>
    Write 140.1-TS-OBLIGATIONS.md consolidating every recorded obligation, each with file:line and
    owning phase — at minimum these ten (count: 10):
    (1) keys/sync/route.ts:438 queued/WIZARD_DUPLICATE reader must handle the truthful
        {queued:true, code:WIZARD_DUPLICATE} envelope → 140.2;
    (2) finalize-wizard/route.ts:1419-1420 documents that envelope as illegal — doc + branch update → 140.2;
    (3) analytics-client.ts X-Tenant-Claim minting (unlocks per-tenant buckets on L-5..L-9) → 140.2;
    (4) X-User-Access-Token forwarding on onboard/resync (keys/sync/route.ts:412-421,
    ⛔ ITEM (4) SUPERSEDED 2026-08-18 by Phase 146.1 / B2 — the forward was REMOVED, not extended; the obligation is discharged by the explicit Python ownership filter. See 140.1-TS-OBLIGATIONS.md TS-15.
        verify-strategy/route.ts:179-192) so a user-scoped pre-check becomes possible → 140.2;
    (5) 424 = recoverable + NON-counting in the discriminator; admin/match must stop flattening
        upstream 4xx to 500 (B-12/OPEN-1) → 140.2;
    (6) breaker records per named `dependency`; retryable:false 500s NEVER count → 140.2;
    (7) 429 body now carries scalar `detail` — NO TS change needed at analytics-client.ts:179;
        do-not-redo note → 140.2;
    (8) ALL THREE Class-5 429/detail stringify sites carry a VERIFICATION obligation — spot-check
        each against the scalar `detail` envelope, not a correct-by-construction assumption:
        ScenarioCommitDrawer.tsx:622, analytics-client.ts:179 (obligation 7's "no change needed"
        still requires this verify), keys/[id]/permissions/route.ts:147; plus csv-finalize
        route.ts:1216/1225 debug_context full-body echo → 140.2/145;
    (9) routers/match.py has NO limiter (/recompute, /eval) → Phase 146 (RATE-03);
    (10) C-11 residual: stale Vercel-side ANALYTICS_SERVICE_KEY is only detectable by a
        credential-carrying probe with the warmer — and NEVER route /health through the seam core
        (A-12) → 140.2/ops.
    Then measure and record in the same artifact:
    - PROD wedged SV rows: read-only MCP query on khslejtfbuezsmvmtsdn counting
      strategy_verifications with status='draft' whose strategy has no compute_jobs row — count
      only; the backfill is a founder decision, not this phase's work.
    - Railway: FORWARDED_ALLOW_IPS presence and analytics-service replica count (ASSUMPTION-2/3).
      If replicas > 1, state explicitly that limiter numbers are N× looser — never silently ship.
  </action>
  <verify>
    <automated>test -f .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md && test "$(grep -c '140\.2' .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md)" -ge 7</automated>
  </verify>
  <acceptance_criteria>
    - The artifact contains all 10 enumerated obligations with file:line and owning phase; count
      stated in the artifact header (≥10).
    - Wedged-rows number recorded with the exact query used.
    - Railway env + replica findings recorded; the N× caveat present if applicable.
    - No TS file and no TODOS.md edited by this plan (git diff shows only the phase artifact).
  </acceptance_criteria>
  <done>140.2/140.3/146 have their inputs; the founder has the blast-radius number; assumptions closed or explicitly carried.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| phase artifacts→downstream planners | obligations must be complete or 140.2 re-derives (the fix-batch collision mechanism) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1-25 | Repudiation | unproven oracles (self-referential pass) | mitigate | M1-M8 ledger with first-hand RED evidence |
| T-140.1-26 | Tampering | mutation residue in tree | mitigate | clean-tree + MUTANT grep gate |
| T-140.1-SC | Tampering | package installs | accept | no packages installed in this plan |
</threat_model>

<verification>
- All gates green with pasted evidence; ledger 8/8; obligations artifact complete (10/10).
</verification>

<success_criteria>
All six phase success criteria hold simultaneously on the fully-composed tree, proven by CI-mirrored
gates and mutation evidence — not by per-plan green alone.
</success_criteria>

<output>
Create `.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-09-SUMMARY.md` when done.
Record: gate outputs, M1-M8 ledger, wedge count, Railway findings.
</output>
