---
phase: 140.1-pyapi-python-service-contract-status-attributability-limiter
plan: 06
type: execute
wave: 3
depends_on: [140.1-04, 140.1-05]
files_modified:
  - analytics-service/main.py
  - analytics-service/services/rate_limit.py
  - analytics-service/routers/process_key.py
  - analytics-service/tests/test_process_key.py
  - analytics-service/tests/test_process_key_auth_order.py
  - src/lib/process-key-client.ts
  - src/lib/process-key-client.test.ts
autonomous: true
requirements: [PYAPI-02, PYAPI-04, PYAPI-06]
must_haves:
  truths:
    - "An unauthenticated /process-key request is rejected BEFORE validation and BEFORE throttling — it can neither enumerate feature flags nor consume throttle budget"
    - "Throttling is bounded per tenant via a forgery-resistant HMAC claim; the anonymous teaser shares ONE dedicated bucket and cannot exhaust any tenant's allowance"
    - "A caller that stops sending the claim degrades to a WARN-visible unverified bucket — the key function never raises"
    - "With INTERNAL_API_TOKEN unset server-side, /process-key ADMITS nothing: every request answers 500 retryable:false — an empty bearer is rejected, never matched against the empty secret"
  artifacts:
    - path: "analytics-service/services/rate_limit.py"
      provides: "Tenant-claim key function (verify half) + platform-ceiling key function"
    - path: "src/lib/process-key-client.ts"
      provides: "X-Tenant-Claim minting (the ONE sanctioned mechanical TS change)"
  key_links:
    - from: "src/lib/process-key-client.ts"
      to: "services/rate_limit.py key function"
      via: "X-Tenant-Claim header, HMAC over INTERNAL_API_TOKEN"
      pattern: "X-Tenant-Claim"
---

<objective>
Make `/process-key` auth-first (PYAPI-04) and per-tenant throttled (PYAPI-02) as ONE wave: extend
`verify_service_key` to gate `/process-key` in middleware, and replace the token-only bucket with an
HMAC-verified tenant claim + dedicated anonymous bucket + stacked platform ceiling (RESEARCH Q1.2:
mechanisms (a)+(c)+(d) as one design).

Purpose: today the gate order is 422 → 429 → 403 (flag enumeration is unthrottled and pre-auth), and
one 100/hour bucket keyed on the shared internal token serves the whole platform — one anonymous IP
drains it in ≈10 minutes (X-3).
Output: middleware auth gate, tenant-claim key function, TS mint, rewritten limiter oracle suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-RESEARCH.md  (Q1 in full — G-1..G-16, the four candidates, Q1.2's bucket table, Q1.3's test table; Q5 PYAPI-04 section)
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-PATTERNS.md  (never-raise key-func analog process_key.py:75-105; main.py never-raise-in-middleware analog)
</context>

<critical_warnings>
1. **PYAPI-02 and PYAPI-04 ship together** (RESEARCH ordering interaction): PYAPI-02 alone lets an
   unauthenticated caller allocate `unverified` buckets; after PYAPI-04, the unverified bucket is
   reachable only by INTERNAL_API_TOKEN holders — exactly the WARN case.
2. **The key function must NEVER raise** (G-8: a raising key_func escapes slowapi and becomes an
   unhandled 500 text/plain — the exact class this programme closes). Parametrise the never-raise
   test over None, "", "a.b", non-UTF8 bytes, 64KB header, non-numeric exp.
3. **The identity must live in a HEADER** (G-7: key_func cannot read the un-awaited body). Never
   trust X-User-Id (client-settable — the PR#241 lesson; G-12).
4. **No new secret, no new library.** The HMAC key derives from INTERNAL_API_TOKEN (G-15 — the
   secret the endpoint already authenticates with); stdlib hmac/hashlib only; hmac.compare_digest
   for comparison (mirror _verify_internal_token's discipline at process_key.py:277).
5. **Middleware must RETURN a JSONResponse, never raise** (main.py:221-228 — QUANTALYZE-4). Use 401
   with an envelope code for missing/invalid bearer; log absent-token vs mismatched-token with
   DISTINCT structured events (PYAPI-06 sites 2/3 — rotation must be distinguishable from attack);
   NEVER log the token or any prefix of it.
6. **`test_process_key_rate_limit_key_func_uses_token_only` (test_process_key.py:2542-2560) must be
   REWRITTEN, not extended** — it pins the behaviour being replaced. Delete-and-replace is
   deliberate (TRAP-9): state it in the commit message.
7. **Keep `_verify_internal_token` in the handler as defence-in-depth** — the middleware gate is
   additive, not a move.
8. **Bucket strings in tests are LITERALS** (e.g. the exact string "process_key:anon"), never
   re-derived from the production helper.
9. **Rollout order inside the plan: Python first (tolerates missing header via fallback), TS mint
   second.** This is what keeps the TS half genuinely mechanical.
10. **Existing tests Task 1 INVALIDATES (TRAP-9 — same discipline as warning 6):**
    - `analytics-service/tests/test_process_key.py:195` (`test_process_key_auth_missing_token`) —
      asserts 403 + "Internal API not configured" for the UNSET-secret case. Broken twice over:
      the middleware now answers first, and the unset-secret contract is 500 retryable:false
      (PYAPI-04d), not 403. Decision: REWRITE — pin literal 500 + literal
      "INTERNAL_TOKEN_UNCONFIGURED" + retryable false.
    - `analytics-service/tests/test_process_key.py:211` (`test_process_key_auth_wrong_token`) —
      asserts 403 "Forbidden" for a mismatched bearer; the middleware answers 401 first.
      Decision: REWRITE — pin literal 401 + the middleware envelope code. The handler's 403 arm
      survives as defence-in-depth (warning 7) but is no longer reachable through the full app; if
      a unit-level pin of `_verify_internal_token` is kept, mark it explicitly as handler-level.
    A relaxed or deleted assertion silently deletes coverage; rewritten tests must pin the NEW
    contract with literals, not merely stop failing. Name both in the SUMMARY.
</critical_warnings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth-first middleware gate on /process-key (PYAPI-04)</name>
  <files>analytics-service/main.py, analytics-service/tests/test_process_key_auth_order.py, analytics-service/tests/test_process_key.py</files>
  <read_first>
    - analytics-service/main.py:176-240 (middleware ordering comment :176-181 — load-bearing; the
      skip-list :200-219; the never-raise comment :221-228)
    - analytics-service/routers/process_key.py:255-278 (_verify_internal_token — the check being
      mirrored into middleware; note it emits 403 today)
    - .planning/phases/140.1-*/140.1-RESEARCH.md Q5 "What the regression test asserts" (the three
      assertions incl. the positive control)
  </read_first>
  <behavior>
    - PYAPI-04a: unauthenticated POST /process-key with body source:"sfox" ⇒ status 401 (not 422)
      AND the response body contains ZERO occurrences of the substrings SFOX, SFOX_ENABLED, MT5,
      MT5_ENABLED — the substring assertion is the real oracle.
    - PYAPI-04b: limit+5 unauthenticated requests, then ONE authenticated request ⇒ the
      authenticated one is NOT 429 (requires a fixture that isolates/resets limiter storage — the
      module-global in-memory Limiter must be swapped or a fresh app constructed per test).
    - PYAPI-04c (positive control): valid token + same invalid body ⇒ 422 WITH the flag name —
      proves 04a's silence is the auth gate, not a broken harness.
    - PYAPI-04d (unset-secret arm): INTERNAL_API_TOKEN UNSET server-side ⇒ literal 500, code
      INTERNAL_TOKEN_UNCONFIGURED, retryable:false — SERVICE-PERMANENT per R-1 (OUR
      misconfiguration; 401 would blame the caller for it). The dedicated oracle: with the secret
      unset, a request with an EMPTY bearer is REJECTED with that 500 — a naive
      `compare_digest(provided, os.getenv("INTERNAL_API_TOKEN") or "")` matches empty-vs-empty
      and ADMITS the request; this test pins the refusal.
  </behavior>
  <action>
    Replace the /process-key skip at main.py:216-219 with an in-middleware bearer check against
    INTERNAL_API_TOKEN. Arm ORDER matters: FIRST, before ANY comparison, if INTERNAL_API_TOKEN is
    unset/empty SERVER-side ⇒ return JSONResponse 500 {code: "INTERNAL_TOKEN_UNCONFIGURED",
    retryable: false} — the operator signal, mirroring Plan 04 Task 3's SERVICE_KEY arm; never
    compare against an empty secret (the empty-vs-empty compare_digest admits the request — the
    exact misconfiguration state PYAPI-06 exists to detect, and Plan 08 deliberately keeps the
    service serving in it). THEN: missing or mismatched bearer ⇒ return JSONResponse 401 with a
    stable envelope code (e.g. UNAUTHENTICATED) — never raise. Emit three DISTINCT structured log
    events (token-absent vs token-mismatch vs secret-unset) with no token material. Keep the handler-side check untouched. Note in the
    contract doc (one line appended to STATUS_CONTRACT.md is acceptable) that /process-key
    unauthenticated is now 401 at the middleware, superseding the handler's 403-first behaviour.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_process_key_auth_order.py -q && python3 -m mypy --strict --follow-imports=silent main.py</automated>
  </verify>
  <acceptance_criteria>
    - 04a/04b/04c/04d all green; mutation M3 (move the gate back below validation, i.e. restore the
      skip) turns 04a RED — run, confirm, revert, record.
    - PYAPI-04d: with INTERNAL_API_TOKEN unset, an EMPTY bearer answers literal 500 + literal
      "INTERNAL_TOKEN_UNCONFIGURED" + retryable false — never 200 (admission) and never 401
      (caller-blame). The unset check provably precedes any compare_digest call.
    - The middleware diff contains no `raise` statement (grep the changed function; paste into SUMMARY).
    - Three distinct log event names exist — token-absent, token-mismatch, secret-unset; none
      references the token variable (executor confirms by reading the diff).
    - The two invalidated tests in warning 10 are REWRITTEN in this task (test_process_key.py:195
      and :211) and run green pinning the new literals.
  </acceptance_criteria>
  <done>Gate order is auth → validation → throttle on /process-key; enumeration and budget-burn both closed with a positive control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tenant-claim key function + anon bucket + stacked ceiling (PYAPI-02)</name>
  <files>analytics-service/services/rate_limit.py, analytics-service/routers/process_key.py, analytics-service/tests/test_process_key.py</files>
  <read_first>
    - analytics-service/routers/process_key.py:75-105 (the existing key function — the never-raise
      shape to preserve), :518 (the decorator to rekey)
    - analytics-service/services/rate_limit.py:1-35 (module docstring documents the Phase-19/API-5
      shared-limiter precedent; the singleton at :29)
    - analytics-service/tests/test_process_key.py:2520-2560 (the singleton-identity idiom AND the
      token-only test being rewritten)
    - .planning/phases/140.1-*/140.1-RESEARCH.md Q1.2 (the three-row bucket table — implement it
      exactly) and Q1.3 (the seven required tests)
  </read_first>
  <behavior>
    Implement Q1.2's bucket table verbatim: valid claim + payload!=public ⇒ "process_key:t:<user_id>";
    valid claim + payload==public ⇒ "process_key:anon"; absent/malformed/bad-HMAC/expired ⇒
    "process_key:unverified:<sha256(bearer)[:16]>" + WARN log. Second stacked decorator carries a
    platform ceiling keyed on the bearer hash (G-9 proves both evaluate). All seven Q1.3 tests, with
    bucket strings as literals: two-tenants-distinct, forged-claim-falls-to-unverified,
    ignores-unsigned-X-User-Id, never-raises (parametrised), teaser-buckets-to-anon,
    two-stacked-limits-both-evaluated (live TestClient — REQUIRED, it is the slowapi-0.1.10
    ASSUMPTION-1 closure), key-func-is-not-get_remote_address identity assertion.
  </behavior>
  <action>
    Add to services/rate_limit.py: claim verification (parse "<user_id>.<exp>.<hmac>",
    hmac.compare_digest against an HMAC-SHA256 over "<user_id>.<exp>" keyed on INTERNAL_API_TOKEN,
    expiry check) and the key function built on it — total ~25 lines, stdlib only, all-str types for
    strict mypy. Rekey process_key.py:518 to the new function and stack the ceiling decorator.
    Sizing (Claude's discretion, pinned as literals in tests): tenant bucket keeps 100/hour; anon
    bucket sized BELOW a tenant bucket (30/hour — OPEN-3's technical recommendation, founder can
    retune); ceiling 500/hour on the bearer hash. REWRITE test_process_key.py:2542's token-only test
    into the unverified-fallback test — a deliberate replacement, named in the commit message.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_process_key.py -q -k "key_func or bucket or stacked or tenant" && python3 -m mypy --strict --follow-imports=silent services/rate_limit.py routers/process_key.py</automated>
  </verify>
  <acceptance_criteria>
    - All 7 Q1.3 tests present and green; count of new/updated key-func tests ≥7 (name them in SUMMARY).
    - Bucket-string literals in tests: "process_key:anon" and the "process_key:t:"/"process_key:unverified:"
      prefixes appear as string literals in the test file (grep the test file, paste).
    - The never-raise test covers all six malformed inputs listed in warning 2.
    - X-User-Id alone can NEVER select a tenant bucket (the forged/unsigned tests pin it).
    - test_two_stacked_limits_both_evaluated runs against a live TestClient and both limits 429 in
      sequence — closes ASSUMPTION-1.
    - The old token-only test is gone; its replacement is documented in the commit body (TRAP-9).
  </acceptance_criteria>
  <done>Per-tenant identity live on the Python side with a safe fallback; ceiling stacked; oracle suite rewritten deliberately.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TS claim minting — the sanctioned mechanical exception</name>
  <files>src/lib/process-key-client.ts</files>
  <read_first>
    - src/lib/process-key-client.ts:120-200 (headers assembly: INTERNAL_API_TOKEN at :124/:155,
      X-User-Id at :159, X-User-Access-Token at :163-165)
    - .planning/phases/140.1-*/140.1-RESEARCH.md Q1.2 "Rollout order" (why this TS change is
      sanctioned: Python ships first with fallback, so this is additive)
  </read_first>
  <behavior>
    - postProcessKey attaches X-Tenant-Claim: "<payload>.<exp>.<hmac-sha256(INTERNAL_API_TOKEN,
      payload + '.' + exp)>" where payload is the userId it already forwards as X-User-Id, or
      "public" for the teaser flow; exp = now + a short TTL (minutes, literal).
    - A vitest pins: claim present on every call; payload matches the userId argument; teaser path
      mints "public"; HMAC verifies against the same algorithm (test computes its own HMAC with
      node:crypto — not by importing the mint helper's internals).
  </behavior>
  <action>
    Add a small mint helper (node:crypto createHmac, ~10 lines) inside process-key-client.ts and set
    the header in the existing headers assembly. EXPLICIT boundary statement (per CONTEXT's one
    exception): this is the purely mechanical TS half required for PYAPI-02 to actually bound
    per-tenant — without it every caller lands in the unverified fallback and the requirement is not
    closed. No other TS file changes; analytics-client.ts claim-minting is a recorded 140.2
    obligation (it is 140.2's file).
  </action>
  <verify>
    <automated>npx vitest run src/lib/process-key-client.test.ts --no-file-parallelism && cd analytics-service && python3 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - X-Tenant-Claim set on all five postProcessKey callers' requests (one code path — the shared
      headers assembly — verified by test).
    - The test's HMAC oracle is computed independently in the test (node:crypto), not imported from
      the client.
    - git diff touches ONLY src/lib/process-key-client.ts (+ its test file) on the TS side —
      executor pastes `git diff --stat -- src/` into the SUMMARY.
    - End-to-end check vs Python: the vitest-minted claim string, fed to the Python key function in
      a pytest (fixture literal), buckets to process_key:t:<user_id> — one cross-language fixture
      literal shared by copy-paste, not by import.
    - The vitest verify targets the literal existing file `src/lib/process-key-client.test.ts` —
      no `-t` name filter (a zero-match filter exits 0 with zero tests run) and no
      `2>/dev/null ||` fallback (it swallows a wrong path); the command fails iff tests fail.
    - Full analytics-service suite green at end of plan (`python3 -m pytest -q`) — the wave-3
      full-suite closure VALIDATION requires.
  </acceptance_criteria>
  <done>Claim minted at the one chokepoint; per-tenant limiting is live end-to-end; TS surface otherwise untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| anonymous internet→teaser→/process-key | Vercel attaches the real internal token to anonymous traffic |
| X-User-Id header | client-settable, must never select a bucket |
| X-Tenant-Claim | forgeable only with INTERNAL_API_TOKEN — the existing trust root |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1-15 | Denial of Service | platform bucket drained by anon teaser | mitigate | anon bucket + per-tenant buckets + ceiling |
| T-140.1-16 | Spoofing | forged tenant claim | mitigate | HMAC compare_digest; forged-claim test |
| T-140.1-17 | Information Disclosure | pre-auth flag enumeration via 422 | mitigate | middleware auth-first + substring oracle |
| T-140.1-18 | Denial of Service | raising key_func → unhandled 500 | mitigate | never-raise parametrised test |
| T-140.1-SC | Tampering | package installs | accept | stdlib hmac/node:crypto only — zero new packages |
</threat_model>

<verification>
- Full pytest + the auth-order suite green; mypy --strict clean; mutation M3 checked.
- vitest for the client green; cross-language fixture literal verified.
</verification>

<success_criteria>
Phase success criteria 3 and 4: anonymous callers cannot exhaust tenant allowance; unauthenticated
requests are rejected before validation and throttling.
</success_criteria>

<output>
Create `.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-06-SUMMARY.md` when done.
Record: M3 evidence, chosen anon/ceiling literals, the analytics-client.ts minting obligation for 140.2.
</output>
