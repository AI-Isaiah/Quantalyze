---
phase: 146-rate
plan: 03
type: execute
wave: 3
depends_on: [146-01, 146-02]
files_modified:
  - .planning/phases/146-rate/146-AUDIT.md
  - TODOS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [RATE-04]
tags: [ratelimit, value-audit, parity, todos, closure]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "146-AUDIT.md §3 holds the RATE-04/TS-22 parity table — every Vercel limiter x every Python limit, per user-visible flow, RE-READ from source at execution HEAD (post plans 01/02, so the two new match rows and the eval row appear) — with a per-mismatch verdict and recommendation (D-146-4)"
    - "ZERO live limit values were changed by this phase — every value-change candidate is filed to TODOS.md with the measured numbers and the shared-bucket warning; the diff proves it (D-146-4)"
    - "The five pre-identified hypotheses (bridge/portfolio-optimizer 30x; validate-and-encrypt dual buckets; verify-strategy anon platform bucket; L-9 optimize-weights per-tenant re-look; H1 retry double-spend) are each explicitly CONFIRMED or REFUTED in §3 — none silently dropped"
    - "H1 (retry double-spend of Python tokens) carries a RECORD-ACCEPT decision in the artifact — an exemption mechanism is out of LIGHT-depth character; reversal point = ship gate"
    - "REQUIREMENTS.md RATE checkboxes remain UNTICKED at plan close — verification owns ticks, not execution"
  artifacts:
    - ".planning/phases/146-rate/146-AUDIT.md — §3 parity table + hypothesis verdicts + recorded decisions (H1 accept, cron surfaces out-of-scope, full-14-route surface note)"
    - "TODOS.md — one bullet per value-change candidate, each carrying both measured values and its recommendation"
  key_links:
    - "§3 table rows <-> ratelimit.ts + router decorator sites at HEAD — every number carries its fresh source citation; a research-inherited number is the exact staleness this phase exists to kill"
    - "TODOS.md candidates <-> D-146-4 — retuning is founder territory; the TODOS bullet is the ONLY output a mismatch may produce here"
    - "§3 <-> test_limit_value_is_unchanged_by_the_rekey + the vitest posture invariant — both literal-pin gates stay green, which IS the proof no value moved"
---

<objective>
Execute the RATE-04 / TS-22 value audit as a committed artifact: rebuild both value tables fresh
from source at HEAD, compare per user-visible flow across the full current surface, record a
verdict + recommendation per mismatch, file every value-change candidate to TODOS (never retune —
D-146-4), and close the phase with checkbox discipline and green gates.

Purpose: SC4 — the substantive remaining RATE question is whether each route has the RIGHT limit;
this answers it as analysis with founder-reserved remediation.
Output: 146-AUDIT.md §3 complete + TODOS candidates, one commit; phase-close verification runs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-rate/146-CONTEXT.md
@.planning/phases/146-rate/146-RESEARCH.md
@.planning/phases/146-rate/146-AUDIT.md
@src/lib/ratelimit.ts
</context>

## Decision map (cited as D-146-N below; source: 146-CONTEXT.md locked decisions)

| ID | Locked decision honored in this plan |
|----|--------------------------------------|
| D-146-2 | No new sweep, no speculative limiter additions — §3 is analysis over the surface plans 01/02 finished; if the audit finds a route needing a NEW limiter, that is a TODOS candidate, not in-phase work. |
| D-146-3 | TS-22 is the 4th ledger row owed by 146 — discharged by this artifact ("Audit after TS-04, not before" — TS-04 landed, so per-tenant sizing is now honest). |
| D-146-4 | Mismatches are RECORDED with a recommendation each; changing live limit VALUES is founder territory — candidates go to TODOS with the measured numbers. This plan changes zero values in code. |

Scope fence: ⛔ no code changes to any limiter value, ⛔ no migration, no package installs, no
AskUserQuestion checkpoints. Every number in §3 is re-read from source at HEAD this session — the
146-RESEARCH.md tables are the METHOD (which files, which comparisons), never the DATA.

<tasks>

<task type="auto">
  <name>Task 1: Build §3 — both value tables fresh at HEAD, per-flow parity verdicts, TODOS candidates</name>
  <files>.planning/phases/146-rate/146-AUDIT.md, TODOS.md</files>
  <read_first>
    - 146-RESEARCH.md "RATE-04" section — the table SCHEMA, the five mismatch hypotheses, and the two ⚠️ blocks (Python literal pins; shared Vercel buckets — userActionLimiter backs ~9 surfaces; scenarioPeerLimiter's docblock is a load-bearing probe-oracle defense)
    - src/lib/ratelimit.ts — every limiter definition + docblock rationale, read fresh
    - The decorator sites across analytics-service/routers/ (optimizer, csv, exchange, portfolio, simulator, match post-146-02) + services/rate_limit.py docstring's /process-key dynamic table and ASSUMPTION-3 (memory:// per replica)
  </read_first>
  <action>
    (1) Rebuild BOTH tables fresh: (a) Vercel — every limited route from the 146-AUDIT.md §1
    census (now including eval) x limiter x value x key shape, values re-read from ratelimit.ts at
    HEAD; (b) Python — every registered slowapi limit (now including the two match scopes) x value
    x scope, plus the /process-key dynamic tiers. Cite file:line fresh for every number. Audit the
    FULL current surface, not the stale "seven" (the requirement's count is an artifact of the
    list RATE-01 replaced — state this in §3's header).
    (2) Compare per user-visible flow (a flow = the Vercel route + the Python endpoint(s) its
    seam call spends tokens on). For EACH flow: effective Vercel budget/hour vs Python budget/hour,
    verdict CONSISTENT or MISMATCH, and for each mismatch a one-paragraph recommendation naming
    WHICH side is wrong and what the corrected value would be. Explicitly confirm/refute all five
    pre-identified hypotheses; a hypothesis the fresh numbers refute is recorded as REFUTED with
    the numbers, not deleted.
    (3) Frame every verdict with the two standing caveats, stated once in §3's header: Python
    slowapi storage is memory:// per replica (values are floors xN replicas — size
    order-of-magnitude only, per research Pitfall 5); and shared Vercel buckets mean a resize for
    one route resizes ~9 (the remedy recommendation for any such flow is "mint a new named
    limiter", never "resize userActionLimiter").
    (4) Record decisions in §3: H1 retry double-spend = RECORD-ACCEPT (a retry exemption is a new
    mechanism, out of LIGHT-depth character; reversal at ship gate); cron surfaces
    (warm-analytics, /cron-recompute) out of scope per requirements decision #7 + A2.
    (5) File each value-change candidate as a TODOS.md bullet (single source): route/flow, both
    measured values, the recommendation, and the constraint that landing it must update the
    literal-pin test (Python) or the limiter docblock + any shared-bucket split (Vercel) in the
    same commit. ⛔ Change no code value anywhere (D-146-4).
  </action>
  <verify>
    <automated>[ "$(grep -c "MISMATCH\|CONSISTENT" .planning/phases/146-rate/146-AUDIT.md | tr -d ' ')" != "0" ] && [ "$(grep -c "RECORD-ACCEPT" .planning/phases/146-rate/146-AUDIT.md | tr -d ' ')" != "0" ] && [ "$(git diff --name-only -- src/lib/ratelimit.ts analytics-service/routers/ | wc -l | tr -d ' ')" = "0" ] && echo audit-clean</automated>
  </verify>
  <acceptance_criteria>
    - Every §3 number carries a fresh HEAD citation; the five hypotheses each have an explicit
      verdict; both caveats stated.
    - Zero code diffs under src/lib/ratelimit.ts and analytics-service/routers/ from this plan.
    - Each mismatch has exactly one TODOS bullet with both measured values (D-146-4).
  </acceptance_criteria>
  <done>SC4 holds as founder-actionable analysis: the RIGHT-limit question is answered per flow, with remediation queued where it belongs and nothing retuned in-phase (TS-22, D-146-4).</done>
</task>

<task type="auto">
  <name>Task 2: Phase close — full gates green, checkbox discipline verified, commit</name>
  <files>.planning/phases/146-rate/146-AUDIT.md</files>
  <read_first>
    - 146-AUDIT.md as it now stands (§1 census, §2 RATE-05 receipt, §3 parity) — the close-out re-reads it as a whole for internal consistency (e.g. §1 route rows agree with §3 flow rows)
  </read_first>
  <action>
    (1) Run the phase-gate suites and record tails in the SUMMARY: `npx vitest run
    src/lib/seam-ratelimit-posture.invariant.test.ts src/lib/api/limiter-ordering.test.ts
    src/app/api/admin/match/eval/route.test.ts --no-file-parallelism`; then from
    analytics-service/ with python3: full pytest and the established `mypy --strict` invocation.
    All green — a red here means a prior plan's claim is false; STOP and escalate to the
    orchestrator rather than patching forward (fail loud).
    (2) Verify closure discipline: REQUIREMENTS.md RATE-01..05 rows remain `- [ ]` (verification
    owns ticks — record the check's output); the phase diff vs the branch base contains zero files
    under supabase/migrations/ (`git diff --name-only` against the merge-base, counted via
    `wc -l | tr -d ' '`, expected 0).
    (3) Append a short §4 "Phase close" to 146-AUDIT.md: date, HEAD hash, the SC1–SC5 status lines
    each pointing at its evidence (§1, plan-01 tests, plan-02 tests, §3, §2), and the open
    reversal points for the ship gate (D-146-1 RATE-05 disposition; H1 accept; TODOS value
    candidates).
    (4) Commit, e.g. `docs(146-03): RATE-04 value parity audit + phase close (values -> TODOS per D-146-4)`.
  
    (W2 plan-check fix) Annotate ROADMAP.md Phase 146 SC4 inline with the D-146-4 disposition,
    mirroring SC5's annotation style: value ADJUSTMENTS are founder-queued via TODOS per locked
    decision D-146-4 (this phase RECORDS mismatches with recommendations; retuning live limits is
    founder territory; reversal point = ship gate). Without the annotation, goal-backward
    verification reads SC4's "adjustments applied" letter as unmet.
</action>
  <verify>
    <automated>[ "$(grep -c "Phase close" .planning/phases/146-rate/146-AUDIT.md | tr -d ' ')" != "0" ] && [ "$(grep -c "^- \[ \] \*\*RATE-0" .planning/REQUIREMENTS.md | tr -d ' ')" = "5" ] && [ "$(git diff --name-only "$(git merge-base HEAD origin/main)"...HEAD -- supabase/migrations/ | wc -l | tr -d ' ')" = "0" ] && echo close-clean</automated>
  </verify>
  <acceptance_criteria>
    - All phase-gate suites green with tails recorded; no red patched forward.
    - The five RATE checkboxes verified still unticked; zero migration files in the phase diff.
    - §4 exists with per-SC evidence pointers and the named ship-gate reversal points.
  </acceptance_criteria>
  <done>The phase is closed with its evidence self-contained in 146-AUDIT.md and every founder-reserved call surfaced, not absorbed — ready for verification and the ship human gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit artifact -> future retuning | a wrong recorded number becomes a wrong founder decision later |
| public repo -> .planning/ contents | the audit is world-readable on push; keep identifiers already-public-grade |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146-07 | Tampering | stale/inherited numbers in §3 | medium | mitigate | every number re-read at HEAD with fresh citation; research tables used as method only |
| T-146-08 | Denial of service | a mismatch "fixed" in-phase resizing a shared bucket | high | mitigate | D-146-4 hard fence — zero value diffs, verified by the git-diff gate; remedies queued to TODOS with the mint-new-limiter constraint |
| T-146-09 | Information disclosure | audit weakens a load-bearing control (scenarioPeerLimiter probe-oracle) | medium | mitigate | recommendation text must cite the docblock before proposing any change to a security-rationale limiter |
| T-146-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase |
</threat_model>

<verification>
- §3 parity table complete with five explicit hypothesis verdicts; H1 RECORD-ACCEPT recorded
- Zero value changes in code (git diff gate); TODOS carries one bullet per candidate with numbers
- Phase-gate suites green (vitest trio, full pytest, mypy --strict); RATE checkboxes unticked; zero migrations
</verification>

<success_criteria>
- SC4 holds (committed value audit with per-mismatch recommendations; adjustments founder-queued
  per D-146-4 rather than applied).
- 146-AUDIT.md is the single self-contained phase evidence artifact: §1 census (SC1), §2 RATE-05
  receipt (SC5), §3 parity (SC4), §4 close with SC2/SC3 test pointers.
- ⛔ REQUIREMENTS.md untouched by execution — verification owns ticks.
</success_criteria>

<output>
Create `.planning/phases/146-rate/146-03-SUMMARY.md` when done, including the suite tails, the
checkbox-discipline check output, the TODOS candidate list, and the commit hash.
</output>