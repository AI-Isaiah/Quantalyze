---
phase: 146-rate
plan: 02
type: execute
wave: 2
depends_on: [146-01]
files_modified:
  - analytics-service/routers/match.py
  - analytics-service/routers/simulator.py
  - analytics-service/routers/portfolio.py
  - analytics-service/services/rate_limit.py
  - analytics-service/tests/test_limiter_identity.py
  - analytics-service/tests/test_tenant_claim_parity.py
  - src/lib/analytics-client.ts
autonomous: true
requirements: [RATE-03]
tags: [slowapi, fastapi, ratelimit, service-error, tenant-claim, pytest, mypy]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "Direct hits on routers/match.py /recompute and /eval past 30/min per tenant answer 429 via slowapi with key_func=partial(tenant_or_platform_key, scope=...) — proven by a behavioral throttle pytest observed RED under a documented neuter first (RATE-03 / TS-21, D-146-2)"
    - "test_match_routes_still_have_no_limiter is DELETED in the SAME commit the limiters land (its docstring orders exactly this), and the literal route-set, distinct-scopes, and shared-key gates all pass with the new routers.match.* entries — no red CI between commits"
    - "The four bare-scalar 429 sites (match.py, simulator.py, portfolio.py x2) answer the service_error RATE_LIMITED envelope with their existing Retry-After PRESERVED — the which-shape-wins decision (nested service_error envelope) is documented in the code and the SUMMARY (TS-23-remainder, D-146-3)"
    - "A new pytest binds services/rate_limit.py:verify_tenant_claim to the BYTES of the committed tests/fixtures/tenant-claim-parity.json — no re-derived table, no forked fixture; the 2050/2030 exp cases kept (TS-36, D-146-3)"
    - "Full pytest from analytics-service/ with python3 AND mypy --strict are green before the plan closes"
  artifacts:
    - "analytics-service/routers/match.py — both handlers carry request: Request and @limiter.limit decorators mirroring portfolio.py's PATTERN (not its 10/hour value)"
    - "analytics-service/tests/test_tenant_claim_parity.py — the cross-language parity reader (new)"
  key_links:
    - "match.py decorators <-> test_limiter_identity.py literal set — the equality reds if either moves alone; same commit"
    - "tenant-claim-parity.json <-> BOTH src/lib/tenant-claim.test.ts (existing) and the new pytest — neutering either side reddens the other's suite"
    - "each migrated 429 site <-> its per-site shape assertion — dropping the headers= kwarg without retry_after silently regresses the wire; the assertion pins Retry-After"
---

<objective>
Close the verified Python gap (RATE-03 / TS-21: slowapi limiters on `routers/match.py`
`/recompute` + `/eval`, with all five pytest/structural gates moved same-commit and the deliberate
tripwire deleted), migrate the four bare-scalar in-handler 429 sites onto the `service_error`
envelope preserving `Retry-After` (TS-23-remainder), and land the TS-36 cross-language
tenant-claim parity pytest.

Purpose: the Python half of the phase — defense-in-depth if a leaked X-Service-Key bypasses
Vercel, one winning 429 wire shape, and the half-built parity gate completed.
Output: limited match routes + gate moves (one commit), 429-shape migration (one commit), parity
pytest (one commit); pytest + mypy --strict green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-rate/146-CONTEXT.md
@.planning/phases/146-rate/146-RESEARCH.md
@analytics-service/routers/portfolio.py
@analytics-service/services/rate_limit.py
@analytics-service/tests/test_limiter_identity.py
</context>

## Decision map (cited as D-146-N below; source: 146-CONTEXT.md locked decisions)

| ID | Locked decision honored in this plan |
|----|--------------------------------------|
| D-146-2 | Gap (b) closed exactly as fenced: match.py `/recompute` + `/eval` get slowapi `partial(tenant_or_platform_key, scope=...)` limits; the 5 pytest/structural gates move same-commit; the tripwire test is DELETED; both handlers gain `request: Request`. No limiters beyond match.py (requirements decision #8); `/cron-recompute` stays out (decision #7 analogy — record, do not absorb). |
| D-146-3 | TS-23-remainder: the four bare-scalar 429 sites migrate onto `service_error`, Retry-After PRESERVED, and ONE shape wins — the nested `service_error` envelope (internal.py is the worked example); document the pick. TS-36: the parity pytest binds to the committed fixture's bytes. |
| D-146-4 | Existing Python limit VALUES elsewhere are untouched. The two NEW match values (30/min) are sized by the fixed defense-in-depth logic (>= max legitimate Vercel-forwarded rate of 20/min), not retunes of anything live. |

Scope fence: ⛔ no migration, no package installs, no AskUserQuestion checkpoints. pytest ONLY from
`analytics-service/` with `python3` (repo-root runs make LIVE broker calls); `mypy --strict` before
close, fixes via `cast()` never `# type: ignore`. If local imports fail on a missing pandera:
`pip install 'pandera==0.32.1' --break-system-packages` (declared dep, known local gap — not a new
package). Re-run every research-cited grep/line-number fresh; the research was cut at the 145 tip.

<tasks>

<task type="tracer">
  <name>Task 1: slowapi limiters on match.py /recompute + /eval, all 5 gates moved, tripwire deleted, ONE commit</name>
  <files>analytics-service/routers/match.py, analytics-service/tests/test_limiter_identity.py, analytics-service/services/rate_limit.py, src/lib/analytics-client.ts</files>
  <read_first>
    - analytics-service/routers/portfolio.py:1570-1580 region (the decorator+key_func PATTERN to mirror; ⚠️ its 10/hour VALUE must NOT be copied — 120x tighter than the Vercel tier forwards)
    - analytics-service/services/rate_limit.py module docstring (slowapi requires a parameter literally named `request`; never a new IP-derived key; the now-false "No limiter at all: routers/match.py" sentence to update)
    - analytics-service/tests/test_limiter_identity.py — the five gates by name: the tripwire (~:589-602), the literal route-set (~:551-569; extend the expected union, ⛔ never touch IP_KEYED_CLASS or its EXPECTED_CLASS_SIZE=9), the shared-key sweep (~:571-587), distinct scopes (~:286), and the throttle-proof pattern test_default_keyed_route_actually_throttles (~:750) — re-locate all by grep, line numbers are 145-tip
    - src/lib/analytics-client.ts ~:922-957 (the two "INERT ... owned by Phase 146" comments and the tenantId already sent on both wrappers)
  </read_first>
  <action>
    (1) In match.py: add `request: Request` as the first parameter of BOTH handlers (slowapi's
    limit evaluation checks for that literal name; `functools.partial` preserves the check), and
    decorate `POST /recompute` with `@limiter.limit("30/minute", key_func=partial(
    tenant_or_platform_key, scope="match_recompute"))` and `GET /eval` likewise with
    `scope="match_eval"` — imports mirroring portfolio.py's. Value rationale (record in a short
    decorator-adjacent comment): defense-in-depth floor sized ABOVE the max legitimate
    Vercel-forwarded rate (20/min per admin via adminActionLimiter on both siblings post-146-01),
    brutally below an unauthenticated-loop abuse rate; storage is memory:// per replica
    (ASSUMPTION-3), so this is an order-of-magnitude floor, not a quota. The tenant claim is
    already minted and sent by the TS wrappers (TS-04) — zero TS wiring changes needed.
    (2) SAME COMMIT, the five gates in test_limiter_identity.py: (a) DELETE
    `test_match_routes_still_have_no_limiter` — its docstring orders deletion the day the gap
    closes; (b) extend the literal route-set equality with the two new `routers.match.*` entries
    (in the `expected` union — ⛔ NOT in IP_KEYED_CLASS, which is the enumerated PYAPI-03 defect
    class with an asserted size); (c) shared-key sweep — satisfied by construction (partial of
    tenant_or_platform_key); (d) distinct-scopes — the two new scope strings are unique; (e) add
    ONE behavioral case mirroring test_default_keyed_route_actually_throttles proving a direct hit
    past 30/min on a match route answers 429.
    (3) NEUTER-RED: neuter = remove the decorator from `/recompute` only. Expected observed REDs:
    the literal route-set equality AND the new behavioral case. OBSERVE both actual outputs, record
    verbatim, restore, re-run GREEN. (The deleted tripwire needs no neuter — its RED was the
    pre-change state; record the pre-change red run of the tripwire BEFORE deleting it as the
    natural observation.)
    (4) SAME COMMIT, prose truth: update rate_limit.py's docstring sentence claiming match.py has
    no limiter (note `/cron-recompute` deliberately stays unlimited — cron surface, service-key
    gated, requirements decision #7 analogy; A2 recorded OUT of scope); update the two
    analytics-client.ts INERT comments to state the claims are now live-consumed by the match
    scopes.
    (5) Run the verify command, then ONE commit, e.g.
    `feat(146-02): slowapi tenant limits on match routes (RATE-03/TS-21) + same-commit gate moves`.
    `git show --stat` must list match.py, test_limiter_identity.py, rate_limit.py,
    analytics-client.ts together.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_limiter_identity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Pre-change tripwire RED observed and recorded; post-change suite green with the tripwire
      gone and both new registry entries present.
    - The decorator-removal neuter was observed RED in BOTH expected gates, outputs recorded.
    - IP_KEYED_CLASS and every existing limit VALUE byte-unchanged (D-146-4); no bespoke key_func.
    - One commit carries code + gates + both prose updates.
  </acceptance_criteria>
  <done>A leaked X-Service-Key looping match routes directly hits a 429 floor (RATE-03, SC3); the gap's tripwire is deleted, not rotting; the claim wire is live end-to-end (TS-21).</done>
</task>

<task type="auto">
  <name>Task 2: TS-23-remainder — migrate the four bare-scalar 429s onto service_error, Retry-After preserved, shape decision documented</name>
  <files>analytics-service/routers/match.py, analytics-service/routers/simulator.py, analytics-service/routers/portfolio.py</files>
  <read_first>
    - The four sites, re-located FRESH by grepping the three routers for in-handler 429 raises (research cited match.py:1742, simulator.py:249, portfolio.py:1964 + :2255 at the 145 tip — re-verify, do not inherit)
    - analytics-service/routers/internal.py's migrated throttle arm — the worked example: `service_error(429, "RATE_LIMITED", retryable=True, retry_after=...)`
    - The 140.1-TS-OBLIGATIONS.md TS-23 row constraints quoted in 146-RESEARCH.md: PRESERVE the header 140.1.2 added; a wait is NEVER derived by parsing prose; a fourth shape was explicitly refused
  </read_first>
  <action>
    (1) Record the decision FIRST (D-146-3): the nested `service_error` envelope WINS as the one
    429 raise-site shape. Rationale to document (in the SUMMARY and a one-line comment at each
    migrated site citing TS-23): internal.py already proved it; the flat main.py handler shape
    cannot be adopted by a raise site (it is a returned JSONResponse from an exception handler);
    the TS discriminator already tolerates all three shapes (140.2-06) so no TS change is forced —
    this migration REDUCES shapes at raise sites from two to one.
    (2) Migrate each of the four sites onto the worked example's call, passing each site's
    EXISTING Retry-After seconds value through the `retry_after` kwarg (read the current
    `headers=` kwarg at each site; for match.py's force-floor 429 the value is the computed
    remaining interval — carry the computation, not a constant). Keep each site's human detail
    string as the `detail`. Do not touch the slowapi-handler 429 path or main.py.
    (3) Per-site pytest assertions: grep the analytics-service tests for existing assertions on
    these four 429s (detail-string or status matches) and RE-POINT them to the envelope shape;
    where a site has none, ADD one asserting status 429 + envelope `code == "RATE_LIMITED"` + the
    Retry-After header value. NEUTER for each new/changed assertion: revert that one site's
    `retry_after` kwarg (header absent) -> observe the assertion RED, record, restore. This is the
    regression that catches the silent-wire-regression trap by construction.
    (4) Verify zero remaining bare-scalar in-handler 429 raises across the three routers (the
    verify command's count), run the touched test files, ONE commit, e.g.
    `refactor(146-02): four bare-scalar 429s -> service_error envelope, Retry-After preserved (TS-23)`.
  </action>
  <verify>
    <automated>cd analytics-service && [ "$(cat routers/match.py routers/simulator.py routers/portfolio.py | grep -c "status_code=429" | tr -d ' ')" = "0" ] && python3 -m pytest tests/ -q -k "429 or retry_after or rate_limit" -x</automated>
    <!-- W1 plan-check fix: the prior HTTPException|429 single-line pipe was MEASURED false-GREEN
         at HEAD (all four raises are multiline). status_code=429 is 4 at HEAD and must reach 0;
         service_error passes 429 positionally so the migrated sites cannot false-RED. -->
  </verify>
  <acceptance_criteria>
    - All four sites answer the envelope; every migrated site's Retry-After value is pinned by an
      assertion that was observed RED under its documented neuter.
    - No new wire shape exists; the shape decision + rationale is documented at the sites and in
      the SUMMARY.
    - The three routers contain zero in-handler bare-scalar 429 raises (comments included — do not
      leave the old raise call quoted in a comment; state "migrated per TS-23" instead).
  </acceptance_criteria>
  <done>One 429 shape wins at every raise site with the wire byte-compatible on Retry-After — the TS-23 remainder is discharged, not re-deferred (D-146-3).</done>
</task>

<task type="auto">
  <name>Task 3: TS-36 parity pytest binding verify_tenant_claim to the committed fixture bytes + mypy --strict close-out</name>
  <files>analytics-service/tests/test_tenant_claim_parity.py</files>
  <read_first>
    - tests/fixtures/tenant-claim-parity.json (REPO ROOT tests/, not analytics-service/tests/ — 4 cases: payload, secret, exp, expected claim; two carry exp 2050/2030 deliberately)
    - analytics-service/services/rate_limit.py `verify_tenant_claim` (def ~:252 at 145 tip — re-locate) and how it reads INTERNAL_API_TOKEN
    - src/lib/tenant-claim.test.ts (how the TS side binds to the same file — the symmetry to mirror, e.g. process_key_onboard_contract.json precedent)
  </read_first>
  <action>
    (1) Write `test_tenant_claim_parity.py`: resolve the fixture path from the test file's location
    up to the repo root (`Path(__file__).resolve()` parents — no hardcoded absolute path), load the
    JSON, and for EACH case: set that case's `secret` into `INTERNAL_API_TOKEN` via monkeypatch,
    feed the case's `claim` to `verify_tenant_claim`, assert the returned payload equals the
    case's expected payload. Bind to the BYTES — never re-derive a claim, never copy the table
    into the test, never fork a second fixture. Keep the 2050/2030 exp cases exactly as committed
    (they exist so the expiry check is not a time bomb). Add one guard assertion that the fixture
    file was found and has >= 4 cases (a silently-empty load is a test that cannot fail).
    (2) NEUTER-RED (cross-language bind proof): neuter = flip one byte of one case's claim in a
    TEMP COPY and point the loader at it (or monkeypatch the loaded case) -> observe the pytest
    RED, record verbatim, restore the real path. Note in the test docstring: this fixture is also
    bound by tenant-claim.test.ts and analytics-client.test.ts on the TS side — editing it reddens
    both suites by design.
    (3) Close-out gates for the WHOLE plan: `cd analytics-service && python3 -m pytest -q` (full
    suite) and the repo's established `mypy --strict` invocation from analytics-service/ (fix any
    error via `cast()`, never `# type: ignore`). Both must be green.
    (4) Commit the new test, e.g. `test(146-02): TS-36 tenant-claim parity pytest bound to committed fixture bytes`.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_tenant_claim_parity.py -q && python3 -m mypy --strict services/rate_limit.py routers/match.py</automated>
  </verify>
  <acceptance_criteria>
    - The pytest reads the repo-root fixture (no second fixture file exists anywhere under
      analytics-service/); all 4 cases asserted; the >= 4 guard present.
    - The flipped-byte neuter RED was observed and recorded; real fixture restored untouched
      (git diff shows tenant-claim-parity.json byte-unchanged).
    - Full pytest AND mypy --strict green from analytics-service/ with python3.
  </acceptance_criteria>
  <done>The two claim implementations are pinned to one committed byte table from BOTH languages — neutering either side reddens the other (TS-36, D-146-3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leaked X-Service-Key -> Railway direct | the bypass RATE-03's slowapi floor exists to throttle |
| TS mint -> Python verify_tenant_claim | cross-language HMAC parity; drift = silent platform-bucket fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146-04 | Elevation / DoS | direct Railway loop on match routes | high | mitigate | 30/min tenant_or_platform_key buckets on both routes; behavioral throttle pytest |
| T-146-05 | Tampering | 429 wire regressed (Retry-After dropped) during envelope migration | medium | mitigate | per-site header assertions, each observed RED under the drop-the-kwarg neuter |
| T-146-06 | Spoofing | TS/Python claim implementations drift apart | medium | mitigate | TS-36 both-sides byte-binding to one fixture |
| T-146-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO new packages (pandera reinstall is a declared existing dep, local-env only) |
</threat_model>

<verification>
- test_limiter_identity.py green with tripwire deleted + 2 new registry entries; decorator-removal neuter RED observed in both expected gates
- Zero bare-scalar 429 raises in the three routers; per-site envelope+Retry-After assertions each observed RED under neuter
- Parity pytest green against the untouched committed fixture; full pytest + mypy --strict green from analytics-service/
</verification>

<success_criteria>
- SC3 holds (direct Railway match hits answer 429 defense-in-depth); TS-21, TS-23-remainder and
  TS-36 discharged with the shape decision documented.
- ⛔ REQUIREMENTS.md checkboxes stay UNTICKED (verification owns ticks); ⛔ zero files under
  supabase/migrations/ in the plan's diff.
</success_criteria>

<output>
Create `.planning/phases/146-rate/146-02-SUMMARY.md` when done, including: the pre-deletion
tripwire RED, all observed neuter-RED records, the which-shape-wins decision paragraph, the
/cron-recompute out-of-scope note, and the three commit hashes.
</output>
