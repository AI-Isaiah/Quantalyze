---
phase: 146-rate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/admin/match/eval/route.ts
  - src/app/api/admin/match/eval/route.test.ts
  - src/lib/seam-ratelimit-posture.invariant.test.ts
  - src/lib/api/limiter-ordering.test.ts
  - .planning/phases/146-rate/146-AUDIT.md
  - TODOS.md
autonomous: true
requirements: [RATE-01, RATE-02, RATE-05]
tags: [ratelimit, upstash, seam, invariant-tests, census]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "Burst GETs to admin/match/eval past 20/min per user.id answer 429 + Retry-After; limiter-misconfig answers 503 SEAM_MISCONFIGURED — both proven by route.test.ts cases each observed RED under a documented neuter first (RATE-02, D-146-2)"
    - "The posture invariant's NO_LIMITER_QUARANTINE is [] and eval sits in EXPECTED_LIMITER_ROUTES, in the SAME commit as the route change — the quarantine equality never reds between commits (D-146-2)"
    - "eval is classified in limiter-ordering.test.ts (NO_INPUT bucket) in the same commit"
    - "146-AUDIT.md carries a FRESH census (route x limiter x value x key shape) derived at execution HEAD by re-running the greps — no row copied from 146-RESEARCH.md, whose tables were cut at the 145 branch tip (RATE-01)"
    - "146-AUDIT.md records RATE-05 as VERIFIED-EXISTING per D-146-1 with fresh-grep receipts (withAuthLimited exists, withRole has no limiter hook, zero non-test withRateLimit symbols, both CI gates present) and the honest residual stated — NO second wrapper or alias is built"
    - "The stale TODOS.md unlimited-routes bullet is retired in TODOS.md itself (single source), pointing at 146-AUDIT.md"
  artifacts:
    - "src/app/api/admin/match/eval/route.ts — checkLimit(adminActionLimiter, keyed on user.id) after the isAdminUser gate, deny routed through rateLimitDenyJson"
    - ".planning/phases/146-rate/146-AUDIT.md — dated §1 census + §2 RATE-05 receipt, every grep output verbatim with its command"
  key_links:
    - "route.ts checkLimit call <-> seam-ratelimit-posture.invariant.test.ts rosters — the equality gate reds if either moves alone; both move in one commit"
    - "146-AUDIT.md §1 <-> the posture invariant — the census must AGREE with the invariant's from-disk derivation; divergence is a STOP, not a footnote"
    - "TODOS.md stale bullet <-> 146-AUDIT.md §1 — the retirement cites the artifact so the closure is evidenced"
---

<objective>
Close the one verified Next.js-layer gap (RATE-02: `admin/match/eval` limiter, mirroring its
sibling recompute, with all three test rosters moved same-commit), then cut the RATE-01 census
FRESH at execution HEAD into `146-AUDIT.md`, and record RATE-05 as VERIFIED-EXISTING with
fresh-grep receipts per D-146-1.

Purpose: RATE-01/02/05 — the TS half of the phase. LIGHT depth (founder ruling v1.16): mechanical,
no deep review round, but every new gate observed RED under neuter.
Output: the limited eval route + moved rosters (one commit), 146-AUDIT.md §1–§2 + retired TODOS
bullet (second commit).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146-rate/146-CONTEXT.md
@.planning/phases/146-rate/146-RESEARCH.md
@src/app/api/admin/match/recompute/route.ts
@src/lib/ratelimit.ts
</context>

## Decision map (cited as D-146-N below; source: 146-CONTEXT.md locked decisions)

| ID | Locked decision honored in this plan |
|----|--------------------------------------|
| D-146-1 | RATE-05 closes as VERIFIED-EXISTING with a fresh-grep receipt. NO second wrapper, NO `withRateLimit` alias — `withAuthLimited` + `withAdminAuth({rateLimitKey})` + the two CI gates ARE the structural successor. Reversal point = ship gate. This plan may not re-open the call. |
| D-146-2 | Scope = the two verified gaps ONLY. This plan closes gap (a): `admin/match/eval` mirrors recompute's inline `adminActionLimiter` + `rateLimitDenyJson`, and the 3 TS test rosters move in the SAME commit. No new sweep, no speculative limiters. |
| D-146-4 | Limit VALUES are founder territory — this plan reuses the existing `adminActionLimiter` (20/min) and mints/retunes nothing. |

Scope fence: ⛔ no migration (any migration in a 146 diff is a scope smell; if one somehow becomes
necessary before 2026-08-19 12:00 UTC it must sort ABOVE `20260819120000`). ⛔ no package installs.
⛔ no AskUserQuestion checkpoints — autonomous; the ship human gate is the reversal point. Every
grep/census in 146-RESEARCH.md was taken at the 145 branch tip: RE-RUN each one here; never cite
the research file's outputs as the deliverable (research Pitfall 1).

<tasks>

<task type="tracer">
  <name>Task 1: Limit admin/match/eval (mirror recompute) + move all 3 rosters + deny-arm tests, ONE commit</name>
  <files>src/app/api/admin/match/eval/route.ts, src/app/api/admin/match/eval/route.test.ts, src/lib/seam-ratelimit-posture.invariant.test.ts, src/lib/api/limiter-ordering.test.ts</files>
  <read_first>
    - src/app/api/admin/match/recompute/route.ts (the exact pattern: import + checkLimit call ~:12,:101, deny body/header shapes)
    - src/lib/seam-ratelimit-posture.invariant.test.ts:73-77 (SEAM_MODULES), :179-213 (EXPECTED_LIMITER_ROUTES + NO_LIMITER_QUARANTINE equality, the "MUST SHRINK IN THE SAME COMMIT" law), :94-100 (docblock prose naming eval as the no-limiter route — goes stale), :256-277 (denyRoutedSites >= checkLimitSites per arm)
    - src/lib/api/limiter-ordering.test.ts:9-15 (completeness law) + the bucket definitions
    - src/lib/ratelimit.ts:119-121 (adminActionLimiter), :325-327 (rateLimitDenyJson / isRateLimitMisconfigured — the single-sourced 503-vs-429 chokepoint)
    - src/app/api/admin/match/eval/route.test.ts (existing cases to extend, not rewrite)
  </read_first>
  <action>
    (1) In eval's route.ts, AFTER the `isAdminUser` 403 gate and BEFORE `evalMatch`: call
    `checkLimit(adminActionLimiter, ...)` keyed on the authenticated `user.id` with a `match-eval:`
    prefix (per D-146-2 and the RATE-02 requirement — key on user.id like the sibling, NOT IP), and
    on failure return via `rateLimitDenyJson` with recompute's exact body/header shapes: 503
    `{error:"Rate limiter unavailable", code:"SEAM_MISCONFIGURED"}` on misconfig, 429
    `{error:"Too many requests", code:"RATE_LIMITED"}` + `Retry-After` + `Cache-Control: private,
    no-store` past limit. Reuse `adminActionLimiter` (20/min) — do NOT mint a new bucket or change
    any value (D-146-4). ⚠️ The file already contains prose MENTIONING the deny helper (research
    Pitfall 6) — the invariant comment-strips before counting, so only a real call satisfies it;
    update that stale prose while there.
    (2) SAME COMMIT, roster 1: posture invariant — move eval from `NO_LIMITER_QUARANTINE` (now
    `[]`) into `EXPECTED_LIMITER_ROUTES`; update the docblock prose at :94-100 that still describes
    eval as the quarantined route.
    (3) SAME COMMIT, roster 2: limiter-ordering — classify eval in the `NO_INPUT` bucket (GET,
    query params only, limiter present).
    (4) SAME COMMIT: extend route.test.ts with two deny-arm cases mirroring the recompute
    behavioural pins (posture invariant :798-833 is the reference shape): (a) limit exhausted ->
    429 body/headers incl. `Retry-After`; (b) misconfigured limiter -> 503 SEAM_MISCONFIGURED. Use
    `vi.spyOn` + `restoreAllMocks`, never `vi.stubGlobal` (CI Node 22 lesson).
    (5) NEUTER-RED, each new case: (a) neuter = stub the limit check to always-success -> the 429
    case must go RED with the deny absent; (b) neuter = stub misconfig detection to false -> the
    503 case must go RED. OBSERVE and record each actual red output verbatim for the SUMMARY,
    restore, re-run to GREEN. A deny-arm test that cannot fail is worse than none.
    (6) Run the three suites (verify command below), then commit route + both rosters + tests as
    ONE commit, e.g. `feat(146-01): rate-limit admin/match/eval (RATE-02) + same-commit roster moves`.
    Confirm with `git show --stat` that all four files are in that single commit.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/match/eval/route.test.ts src/lib/seam-ratelimit-posture.invariant.test.ts src/lib/api/limiter-ordering.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - All three suites green at the commit; the quarantine equality and completeness gates passed
      WITH the roster moves (they red if any of the three files lags the route change).
    - Both neuter variants were observed RED with output captured; real cases restored GREEN.
    - `git show --stat` lists exactly the four files in one commit; no ratelimit.ts value changed.
  </acceptance_criteria>
  <done>eval enforces 20/min per user.id with chokepoint-routed 429/503 denies, provably (RATE-02, SC2); the population invariants agree with the code in the same commit (D-146-2).</done>
</task>

<task type="auto">
  <name>Task 2: RATE-01 census, cut FRESH at execution HEAD, into 146-AUDIT.md §1 + retire the stale TODOS bullet</name>
  <files>.planning/phases/146-rate/146-AUDIT.md, TODOS.md</files>
  <read_first>
    - 146-RESEARCH.md "RATE-01" section (the three-module import edge and WHY a two-module grep silently drops keys/[id]/permissions) — as method, never as data
    - TODOS.md stale bullet naming six routes as unlimited (re-locate by grep, research cited ~:866-868 in the 145 worktree copy)
  </read_first>
  <action>
    (1) Derive the seam-route population FRESH, twice, at HEAD (post Task 1): (a) run the posture
    invariant (its population is derived from disk every run — the living census); (b) an
    independent import-edge grep over `src/app/api` for the THREE seam modules (`analytics-client`,
    `resilient-fetch`, `process-key-client`), comment-stripped so prose mentions do not count
    (Pitfall 6: a naive grep -l reports eval's gap closed while open). Record both commands and
    outputs VERBATIM. The two derivations MUST agree route-for-route; any divergence = STOP,
    reconcile, and only then write the table. Do not build a persistent second scanner — the
    one-off grep is the cross-check; the invariant remains the living gate.
    (2) Author `.planning/phases/146-rate/146-AUDIT.md` §1: dated census table — route x limiter x
    value x key shape x source line — for every seam route (expected post-Task-1: all limited,
    quarantine empty; csv-finalize absent, having left the seam in Phase 145). Header states: cut
    at execution HEAD with the commit hash, supersedes both the stale TODOS list AND the
    146-RESEARCH.md tables (which were cut at the 145 tip). Leave §3 (RATE-04 value parity) as a
    marked PENDING section — Plan 03 owns it.
    (3) In TODOS.md, retire the stale unlimited-routes bullet: annotate it CLOSED with today's
    date + pointer to 146-AUDIT.md §1 (all named routes verified limited; the census is the
    authoritative replacement). TODOS.md is the ONE ground truth — close it there, nowhere else.
    Touch nothing else in TODOS.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/146-rate/146-AUDIT.md && [ "$(grep -c "VERBATIM\|checkLimit" .planning/phases/146-rate/146-AUDIT.md | tr -d ' ')" != "0" ] && [ "$(grep -n "146-AUDIT" TODOS.md | wc -l | tr -d ' ')" != "0" ] && echo census-committed</automated>
  </verify>
  <acceptance_criteria>
    - §1's table rows come from the fresh outputs recorded in the same file — zero rows inherited
      from 146-RESEARCH.md (its route list may match; its outputs may not be the citation).
    - The invariant-vs-grep cross-check agreement is stated explicitly with both outputs.
    - The TODOS bullet is annotated closed in place with the artifact pointer.
  </acceptance_criteria>
  <done>The authoritative gap list exists as a committed, dated, fresh artifact (RATE-01, SC1) and the stale list can no longer be cited.</done>
</task>

<task type="auto">
  <name>Task 3: RATE-05 VERIFIED-EXISTING receipt (D-146-1) into 146-AUDIT.md §2, then commit artifact + TODOS</name>
  <files>.planning/phases/146-rate/146-AUDIT.md</files>
  <read_first>
    - 146-CONTEXT.md D-146-1 (the locked disposition and its reversal point)
    - 146-RESEARCH.md "RATE-05" section (what to re-verify; the honest residual to restate)
  </read_first>
  <action>
    (1) Execute and record VERBATIM (command + output) fresh greps at HEAD proving each leg:
    (a) `withAuthLimited` exists and composes `withAuth` (src/lib/api/withAuthLimited.ts) + its
    current adopter list by importer grep; (b) `withAdminAuth` accepts `rateLimitKey`;
    (c) `withRole` contains no limit call; (d) zero non-test occurrences of a `withRateLimit`
    symbol repo-wide; (e) both CI gates exist and are non-empty — the posture invariant's
    roster+quarantine equality and limiter-ordering's completeness law.
    (2) Write §2: RATE-05 disposition = VERIFIED-EXISTING per D-146-1 — the requirement's
    substance ("HOF composing with withAuth; a new route cannot silently ship limiterless") is
    delivered by the existing wrappers + the two gates; the requirement's named symbol is NOT
    minted (a third functionally-identical wrapper blends conflicting patterns — Rule 7). State
    the HONEST RESIDUAL verbatim from research: a brand-new no-limiter seam route fails the
    quarantine equality as a forcing function ("decide whether intended"), not a prohibition; and
    no existing wrapper fits an admin GET, which is why eval stays hand-wired inline like its
    sibling. State the reversal point: the ship human gate — if the founder holds to the letter of
    the symbol name, the cheapest conforming follow-up is a documented alias, deliberately NOT
    built here.
    (3) Commit 146-AUDIT.md + TODOS.md together, e.g.
    `docs(146-01): RATE-01 fresh census + RATE-05 VERIFIED-EXISTING receipt; retire stale TODOS route list`.
  </action>
  <verify>
    <automated>[ "$(grep -c "VERIFIED-EXISTING" .planning/phases/146-rate/146-AUDIT.md | tr -d ' ')" != "0" ] && [ "$(grep -rn "withRateLimit" src/ --include="*.ts" | grep -v test | wc -l | tr -d ' ')" = "0" ] && echo receipt-committed</automated>
  </verify>
  <acceptance_criteria>
    - Every §2 claim carries its own fresh command+output pair; none cites 146-RESEARCH.md as the
      evidence.
    - No new wrapper, alias, or export was added anywhere (the repo-wide symbol count stays 0
      outside tests) — D-146-1 is structural, not advisory.
    - One commit carries §1+§2 and the TODOS retirement.
  </acceptance_criteria>
  <done>RATE-05 (SC5) is closed as a recorded, receipt-backed decision with its reversal point named — not by silent omission and not by a third wrapper (D-146-1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed admin -> admin/match/eval | compromised admin account looping the eval scrape surface |
| Upstash outage -> deny decision | misconfig must answer 503, never a lying 429 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146-01 | Denial of service / info inference | admin/match/eval scrape loop | high | mitigate | 20/min per user.id via adminActionLimiter behind the isAdminUser gate (auth->validate->limit, B15) |
| T-146-02 | Repudiation / availability | Upstash outage misread as user throttle | medium | mitigate | deny routed through rateLimitDenyJson only; posture invariant's per-arm chokepoint count holds |
| T-146-03 | Tampering | stale census cited as fresh coverage | medium | mitigate | census cut at HEAD with commit hash + invariant cross-check; research tables never cited as data |
| T-146-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase (RESEARCH: Package Legitimacy Audit N/A; any install is scope drift — refuse) |
</threat_model>

<verification>
- Three vitest suites green with the roster moves in the same commit as the route change
- Both deny-arm neuter-REDs observed and recorded verbatim
- 146-AUDIT.md §1 census + §2 receipt committed, every grep fresh at HEAD; TODOS bullet retired
</verification>

<success_criteria>
- SC1 (census artifact), SC2 (eval 429+Retry-After), SC5 (structural successor recorded) hold.
- ⛔ REQUIREMENTS.md checkboxes for RATE-01/02/05 remain UNTICKED — verification owns ticks, not
  execution; state completion only in the SUMMARY.
- git diff vs origin/main shows zero files under supabase/migrations/.
</success_criteria>

<output>
Create `.planning/phases/146-rate/146-01-SUMMARY.md` when done, including both observed
neuter-RED records (neuter, observed output) and the two commit hashes.
</output>
