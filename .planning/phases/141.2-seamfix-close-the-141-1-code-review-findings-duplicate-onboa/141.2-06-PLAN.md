---
phase: 141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa
plan: 06
type: execute
wave: 3
depends_on: ["141.2-01", "141.2-02", "141.2-03", "141.2-04", "141.2-05"]
files_modified:
  - src/lib/seam-constants.pin.test.ts
  - docs/runbooks/seam-breaker.md
  - src/lib/seam-retry-registry.ts
  - TODOS.md
  - CHANGELOG.md
autonomous: true
requirements: [SEAM-05, SEAM-06]
tags: [pin-hygiene, runbook, changelog, d-07, prose-truth, finding-13]

must_haves:
  truths:
    - "Finding 13 / D-05: the tautological trip-count pin is DELETED, and the deletion is justified by an OBSERVED mutation — deleting the per-attempt latch reset reds the real behavioural test at resilient-fetch.retry.test.ts [SEAM-06], proving the coverage claim that makes deletion safe"
    - "The runbook's multi-decision sections (trip-count table; retry-enabled counts) are rewritten ONCE against the final post-D-01/D-03/D-06 tree, with NO derivable integers reprinted in prose"
    - "D-07 is discharged as a recorded DECISION: the re-measure arithmetic (post-D-01+D-03 the retry-eligible population is onboard-with-key only; resync — 48% of all-time traffic — retries no more) lives in the registry cost note; no limiter code changed"
    - "D-08: the UNSHIPPED [0.51.0.0] CHANGELOG entry describes the retry surface that ACTUALLY ships — no new version section minted; 141.2 folded into the same release"
  artifacts:
    - path: "src/lib/seam-constants.pin.test.ts"
      provides: "tautological pin removed; constants still pinned by the adjacent literal test"
    - path: "docs/runbooks/seam-breaker.md"
      provides: "trip-count table and retry-surface prose true against the final tree; one sentence on 429 invisibility"
    - path: "CHANGELOG.md"
      provides: "corrected [0.51.0.0] entry"
    - path: "TODOS.md"
      provides: "D-02 limitation, finding-8 residual, D-01 follow-up, and the two class-census entries"
  key_links:
    - from: "docs/runbooks/seam-breaker.md"
      to: "src/lib/seam-retry-registry.ts"
      via: "prose describes the maps by NAME and defers counts to the pins"
      pattern: "RETRY_SAFE_FLOW_TYPES"
---

<objective>
Close finding 13 and consolidate every piece of prose that MULTIPLE behaviour changes in this
phase invalidate — rewritten once against the final tree, per PATTERNS §7c ("the runbook's
retry counts must be rewritten ONCE, after both land — not twice"). Then discharge D-07
(re-measure, record, no code) and D-08 (correct the unshipped changelog).

Finding 13: `seam-constants.pin.test.ts` asserts `Math.ceil(BREAKER_FAILURE_THRESHOLD / 2)
=== 3` — arithmetic over a constant the test ten lines above pins to 5, so it cannot fail for
any change to the behaviour its message describes. It violates the file's own stated law
(never derive the oracle from the module under test). The behaviour is genuinely covered by
the real-loop test at `resilient-fetch.retry.test.ts` [SEAM-06]; CONTEXT D-05 calls deletion
"defensible and the lazier correct answer" and forbids strengthening it into a second copy.
Deletion is made falsifiable here by running the mutation the old pin could not see (SC-M).

Wave 3 by necessity, not convenience: the trip-count table is invalidated by THREE decisions
(D-03 changes the retry-enabled set; D-06 makes a Retry-After-carrying 503 record ONE failure;
D-01 conditions onboard's grant), and any replacement for the deleted pin's claims "must be
true after D-06 lands, not just true today" (CONTEXT D-05). The changelog correction must
state the retry surface that ACTUALLY ships, which exists only after plans 02 and 05.

Purpose: finding 13 (D-05), D-07, D-08, and the residual prose fences. SC-M.
Output: a pin file with no unfalsifiable assertions, a truthful runbook, a truthful changelog,
and every deferral/limitation booked in TODOS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-CONTEXT.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-RESEARCH.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-PATTERNS.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the pin that cannot fail — and prove the deletion is safe by mutation (SC-M)</name>
  <files>src/lib/seam-constants.pin.test.ts</files>
  <read_first>
    - src/lib/seam-constants.pin.test.ts — the adjacent pair at ~:548-579: the literal-5 threshold pin (KEEP) and the `Math.ceil(BREAKER_FAILURE_THRESHOLD / 2) === 3` trip-count pin (DELETE); the file's own anti-vacuity law in the comments at ~:385-433 (never derive the oracle from the module under test)
    - src/lib/resilient-fetch.retry.test.ts — the [SEAM-06] per-attempt-latch case (~:1092-1109) that genuinely covers the behaviour (drives the real loop with a 503-503 double and asserts EXACTLY 2 recorded failures), plus plan 04's disposition of assumption A6 on that case
    - 141.2-PATTERNS.md §5b — why this pin is the anti-pattern and why deletion (not replacement) is the correct disposition
  </read_first>
  <action>
    DELETE the trip-count pin (the whole `it` block asserting the ceil arithmetic). Do NOT
    write a replacement — the behaviour is covered by the real-loop test, and a new "N user
    requests" assertion would be wrong after D-06 for the status class the upstream contract
    makes mandatory (every SERVICE-TRANSIENT 503 carries a positive Retry-After and now
    records ONE failure, not two). Keep the literal-5 threshold pin unchanged.

    Where the deleted pin sat, leave a short comment (symbol anchors only) recording the
    disposition: the trip-count CLAIM lives in the runbook; the trip-count BEHAVIOUR is pinned
    by the per-attempt-latch case in the retry test file, which reds if the latch reset is
    deleted — this file pins constants, not arithmetic over its own pins.

    Then run MUTATION SC-M — the mutation the old pin could not see: delete `recorded = false`
    (the per-attempt latch reset) in `src/lib/resilient-fetch.ts` → the [SEAM-06] case REDs
    (records 1 failure, expects 2). Paste the RED output; restore; re-run GREEN. This is the
    evidence that deletion sheds no coverage. Revert the mutation cleanly (`git diff` clean on
    resilient-fetch.ts afterward — this plan does not modify that file).
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-constants.pin.test.ts src/lib/resilient-fetch.retry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION SC-M (exact, from the VALIDATION ledger): with the pin already deleted, remove the per-attempt latch reset → `resilient-fetch.retry.test.ts` [SEAM-06] REDs with real output pasted; restore → GREEN. (The old pin stayed green under this exact mutation — state that contrast in the log.)
    - The literal-5 threshold pin still GREEN and byte-unchanged.
    - `git diff --name-only` for this task touches ONLY `src/lib/seam-constants.pin.test.ts`.
    - No new assertion in the file computes its expectation from a constant the file also pins (oracle-independence check).
  </acceptance_criteria>
  <done>The unfalsifiable pin is gone; the behaviour's real coverage is demonstrated by the exact mutation the old pin was blind to; the constants file keeps only assertions that can fail.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the runbook's multi-decision sections ONCE, against the final tree</name>
  <files>docs/runbooks/seam-breaker.md</files>
  <read_first>
    - docs/runbooks/seam-breaker.md — the trip-count table and its "3 user requests instead of 5" claim (~:88-100); the retry-surface prose: "exactly two /process-key flow types" (~:141-145) and "five SEAM_BUDGETS rows carrying retries: 1 and six audited verdicts" (~:148-152). Plans 01 and 04 already rewrote the corruption sentence (~:51-53) and the Retry-After fail-fast section (~:156-168) — verify those read true and do not re-edit them
    - 141.2-PATTERNS.md §4c — the derived-integer rule, verbatim: "never write an integer or a duration in prose that a test can derive"; the inventory table of prose integers this phase moves
    - src/lib/seam-retry-registry.ts and src/lib/seam-constants.pin.test.ts at the CURRENT tree — the post-plan-02/05 shape the prose must describe
  </read_first>
  <action>
    Rewrite the retry-surface prose to the final truth WITHOUT reprinting derivable integers
    (the derived-integer rule is this phase's most-violated constraint — name the maps and
    defer counts to the pins):
    - `RETRY_SAFE_FLOW_TYPES` now carries `onboard` alone, and onboard's grant is CONDITIONAL
      on a usable idempotency key, enforced by `retriesForFlow` at the `postProcessKey`
      chokepoint (a caller stating nothing gets no retry); `resync` carries a NO verdict in
      `RETRY_AUDIT_NO_FLOW_TYPES` until a durable idempotency key exists. Point to the
      registry and its pins for the populations — do not write "exactly one/two" or "six/seven
      verdicts" style integers.
    - Rewrite the trip-count table for the post-D-06/D-03 world: a 503 carrying a positive
      Retry-After fails fast and records ONE failure; only retry-eligible calls whose failures
      lack a contractual wait record two. Express trip progression in terms of
      `BREAKER_FAILURE_THRESHOLD` and the per-attempt latch BY NAME, deferring the numeric
      threshold to `seam-constants.pin.test.ts`.
    - Add ONE sentence (finding 8's residual, per RESEARCH §6): attempts refused by the
      limiter (429) are classified non-counting for the breaker AND write no audit row, so
      neither the breaker nor the flag-monitor denominator can see a platform-ceiling drain.
    - Use the docblock-correction idiom for the table: name the old "3 instead of 5" claim and
      why it no longer holds for the mandatory-header status class.
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-citations.invariant.test.ts && git diff --stat docs/runbooks/seam-breaker.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "user requests instead of" docs/runbooks/seam-breaker.md` → 0 (the stale arithmetic claim is gone — safe absence grep: the replacement prose is not required to name the old phrasing).
    - `grep -c "retriesForFlow" docs/runbooks/seam-breaker.md` ≥ 1 and `grep -c "RETRY_AUDIT_NO_FLOW_TYPES" docs/runbooks/seam-breaker.md` ≥ 1 (presence).
    - Derived-integer sweep recorded in the log: the rewritten sections contain no integer that `seam-retry-registry.test.ts` or `seam-constants.pin.test.ts` derives (map sizes, verdict counts, threshold, trip counts) — reviewed by reading the diff, criterion is the named CONSTRUCT (integers deferred to pins), not a grep count.
    - Plans 01/04's sections verified still true against the final tree (read, not re-edited).
  </acceptance_criteria>
  <done>The runbook describes the shipped retry surface and trip behaviour in symbols and names, with every derivable count living only in the pins that can red.</done>
</task>

<task type="auto">
  <name>Task 3: D-07 recorded, TODOS booked, D-08 changelog corrected</name>
  <files>src/lib/seam-retry-registry.ts, TODOS.md, CHANGELOG.md</files>
  <read_first>
    - 141.2-RESEARCH.md §6 — the completed re-measure arithmetic D-07 asked for (D-01 removes ~0 retry volume; D-03 removes 100% of resync retries — 20 of 42 all-time rows; post-both, the worst case falls an order of magnitude below the 500/hour ceiling on this traffic shape) and the 429-invisibility caveat
    - 141.2-PATTERNS.md §1 census table (the eight out-of-scope unbounded-`.select()` siblings — benchmark/btc flagged HIGHEST risk) and §2 census (the `observability.ts` error-collapse sibling)
    - src/lib/seam-retry-registry.ts — the D-03 cost-note region (the doubled-compute note the finding says is incomplete)
    - CHANGELOG.md — the `[0.51.0.0]` entry (top of file): unshipped, unpushed; currently claims retry for "two /process-key flow types (onboard, resync)" gated on an audit that "says a replay is safe" — falsified by findings 1 and 6
    - TODOS.md — insertion conventions (single ground-truth backlog; one entry per CLASS, not per instance)
  </read_first>
  <action>
    Registry cost note (`seam-retry-registry.ts`, SC-H′/SC-H governed — symbol anchors, no
    coordinates, no derivable integers in evidence strings): extend the cost note with D-07's
    discharge — a retried call spends two tokens of BOTH the per-identity window and the
    platform-wide ceiling; post-D-01/D-03 the retry-eligible population is onboard-with-key
    only, so the measured worst case no longer approaches the ceiling; decision recorded: no
    limiter-aware retry path (D-07), re-raise only if the surface re-expands.

    TODOS.md entries (one per class, founder stopping-rule framing — these are logged, not
    fixed, because they are outside the 13 findings; "would have caught" language):
    1. D-02 known limitation: attempt-grain error rate biases DOWNWARD under retry; a true
       per-request rate needs a server-minted request id the retry reuses (deferred work).
       Reframe/replace the existing D-16 follow-up entries framed on the dedup semantics so
       the backlog does not describe deleted code.
    2. Finding 8 residual: 429-refused attempts are invisible to breaker AND denominator —
       no current signal covers a platform-ceiling drain.
    3. D-01 follow-up (CONTEXT's rejected-but-better end state): client-side stable
       idempotency key so the field is always populated — contract + SV-uniqueness blast
       radius, needs its own decision.
    4. CLASS entry — unbounded `.select()` on unbounded-growth tables (eight sites from the
       PATTERNS census; name `benchmark/btc` as highest risk: ASC-ordered, drops NEWEST data
       past 1000 daily closes; the two cron enqueue sweeps next). One entry, not eight.
    5. CLASS entry — error-collapse-to-healthy-default siblings: `observability.ts`
       `checkStuckNotifications` returns `{stuck: 0}` on a failed read (byte-identical failure
       mode to finding 12, operator-facing).
    Also: mark DEF-141.1-02-A discharged (plan 02 task 2 closed it in code).

    CHANGELOG.md (D-08): correct the `[0.51.0.0]` entry IN PLACE — it is written but not
    pushed/released, so the never-rewrite-shipped-entries rule does not apply. Do NOT mint a
    new version section; 141 + 141.1 + 141.2 land stacked as one release. The corrected entry
    states the retry surface that ACTUALLY ships: seam retry enabled only for `onboard` and
    only when the draft carries a usable idempotency key (enforced at the chokepoint,
    fail-closed); `resync` retry withdrawn — its evidence did not support the grant; plus the
    141.2 repairs in this release: breaker write-path corruption healing + absolute lock
    plausibility bound + per-attempt admission timestamps (fixing a 141.1 regression),
    Retry-After parsed rather than presence-tested, and the flag-monitor denominator restored
    to an uncapped, wire-uninfluenceable, attempt-grained count with a distinct read-failure
    outcome. No safety property may be claimed that the code does not have. VERSION +
    package.json bump is NOT this plan's job — it happens at ship per the RUNPLAN (bump and
    entry finalisation ride the /ship step; founder: bump without asking).
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-retry-registry.test.ts src/lib/seam-citations.invariant.test.ts && grep -c "onboard" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "0.51.1\|0.52.0" CHANGELOG.md` → 0 hits (no new version section minted).
    - The corrected entry no longer claims retry for resync: reviewed by reading the `[0.51.0.0]` diff — the criterion is the named construct (resync described as withdrawn/NO verdict, never as retry-enabled), not a token count.
    - Registry test file + citation invariants GREEN (cost-note edit did not introduce coordinates or break the evidence-count fence).
    - TODOS.md contains the five entries + the DEF-141.1-02-A discharge; the stale D-16-framed follow-ups no longer describe deleted code.
    - `git diff --name-only` for this task is exactly: src/lib/seam-retry-registry.ts, TODOS.md, CHANGELOG.md.
  </acceptance_criteria>
  <done>D-07 is a recorded decision with its arithmetic; every limitation and deferral is booked once in the single backlog; the unshipped changelog tells the truth about what ships.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prose → future maintainers | runbook/changelog claims drive operator behaviour and release trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.2-12 | Repudiation (false safety claim) | CHANGELOG [0.51.0.0] | mitigate | entry corrected to the shipped surface before release; unshipped-entry exception documented in the diff |
| T-141.2-13 | Repudiation (prose/pin drift) | runbook derived integers | mitigate | counts deferred to pins by name; derived-integer rule applied to every rewritten sentence |
| T-141.2-SC | Tampering | package installs | accept | zero package installs in this phase |
</threat_model>

<verification>
- Full phase gate after this wave (it is the last): `npm test` green under Node 22 PATH, `npm run typecheck`, `npm run lint`, `npm run test:coverage` thresholds held
- SC-M mutation observed RED with pasted output, restored GREEN
- VALIDATION.md: ALL rows SC-A…SC-M Observed ✅ (or explicitly skipped-with-reason), Per-Task Verification Map fully transcribed, Oracle Independence checklist complete, `nyquist_compliant: true` set
</verification>

<success_criteria>
- Finding 13 closed by falsifiable deletion; runbook and changelog true against the final tree
- D-07 discharged as a recorded decision; all deferrals/limitations booked once in TODOS.md
- Phase-level sign-off: all 13 findings closed, every ledger row observed
</success_criteria>

<output>
Create `.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-06-SUMMARY.md` when done. Include: SC-M RED output with the contrast statement (the deleted pin stayed green under this mutation); the founder-visible scope paragraph (retry surface now onboard-with-key only; resync withdrawn — 48% of historical traffic); and the phase-level VALIDATION sign-off state.
</output>
