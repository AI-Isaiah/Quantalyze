# 141.2 — autonomous run plan (founder instruction, 2026-08-01)

Reboot-durable on purpose: the 141.1 wave manifest was lost to a host restart because it
lived in `/private/tmp`, which clears on reboot. If this session dies, resume from here.

**Standing instruction:** run the whole sequence without check-ins. Bump the version
without asking. Fix findings from the final review round **only if critical**.

## Sequence

- [x] 1. Insert phase 141.2, write `141.2-FINDINGS.md` (13 findings + failure scenarios)
- [x] 2. Write `141.2-CONTEXT.md` — decisions D-01…D-08 (defaults, not fences)
- [x] 3. `gsd-phase-researcher` → `141.2-RESEARCH.md` (OQ-1…OQ-5)
- [x] 4. `gsd-pattern-mapper` → `141.2-PATTERNS.md`
- [x] 5. Write `141.2-VALIDATION.md` (falsifiability ledger; every SC needs a mutation)
- [x] 6. `gsd-planner` → PLAN.md files (6 plans, 3 waves)
- [x] 7. `gsd-plan-checker` revision loop — PASS on round 2
- [ ] 8. Execute all waves; merge each wave; full gate per wave boundary
      - wave 1: plan 02 ✅ done, plan 03 ✅ done, plan 01 ⚠️ Task 1 committed only
        (host powered off mid-plan 2026-08-01); Task 2 + SUMMARY resumed in the SAME
        worktree `agent-a1d6dba5e109a0e57` @ `250aad5d`. **Nothing merged yet.**
      - waves 2 (04, 05) and 3 (06): not started
- [ ] 9. FINAL ROUND: `gsd-verifier` + `gsd-code-reviewer`
      → fix **ONLY critical** items (user-facing / data-integrity). Log the rest.
- [ ] 10. Bump VERSION + package.json **in the same commit** (project convention)
- [ ] 11. Correct the UNSHIPPED `[0.51.0.0]` CHANGELOG entry per D-08 — it currently
      claims an audit-backed retry for `onboard` AND `resync` that findings 1 and 5
      disprove. Fold 141.2 into the same release; 141 + 141.1 + 141.2 are one change.
- [ ] 12. `/ship` — PR with all required artifacts (changelog, pr-body, tests,
      test-coverage, plan-completion)
- [ ] 13. `/land-and-deploy` — CI must be GREEN before merge; verify deploy after

## Wave-1 worktree map (survives a reboot; `git worktree list` does too)

| plan | worktree branch | tip | state |
|---|---|---|---|
| 01 | `worktree-agent-a1d6dba5e109a0e57` | `250aad5d` | Task 1 only — resumed |
| 02 | `worktree-agent-a71de61d77334f3aa` | `3a2f23da` | complete + SUMMARY |
| 03 | `worktree-agent-a256479f378ca2652` | `0561427e` | complete + SUMMARY |

The six `140.5` worktrees still on disk are all ancestors of this branch — already merged,
nothing to recover from them.

## Non-negotiables carried in

- **Worktrees fork from the DEFAULT branch.** Every executor prompt MUST carry the
  verbatim `<worktree_branch_check>` block with `EXPECTED_BASE` pinned to the phase tip
  (`execute-phase.md:519,573`). 4/4 spawns in 141.1 came up 56–79 commits stale.
- **Dispatch ONE `Agent()` per message** — concurrent `git worktree add` races on
  `.git/config.lock` (`execute-phase.md:531-537`).
- **An interrupted executor's uncommitted diff may be a MUTATION, not progress** —
  141.1-08's orphaned diff was its own acceptance mutation caught mid-probe.
- **Every pin must be proven to fail**, driven through the REAL call site. D-01/D-03 are
  seam-crossing; a helper-level test cannot see them.
- **Prose moves with code** — docblocks + `docs/runbooks/seam-breaker.md` describe the
  D-16 semantics and the current retry verdicts.
- **CI gates are ADVISORY at merge** (branch protection deferred until paying clients),
  so say "would have caught", never "did stop".

## Cross-plan handoffs surfaced during execution (do NOT lose these)

Flagged by executors rather than silently fixed, because each would have collided with a
sibling plan's file ownership. Each must be closed by its owner or it becomes new doc-drift
in the phase that exists to close doc-drift.

- **From 141.2-02 → plan 05:** `process-key-client.ts:499` still reads *"onboard/resync are the
  two allowlisted flows"*. **Now false** after the resync map move. Plan 05 already rewrites
  that exact docblock — it must correct the sentence.
- **From 141.2-02 → plan 03:** `flag-monitor/route.ts:195-198` — *"A retried onboard/resync
  writes a SECOND audit row"* is half-stale for the same reason, and sits inside the function
  plan 03 rewrites. **Plan 03 did NOT catch it** — verified post-hoc: the sentence survives at
  `flag-monitor/route.ts:239-243` in `worktree-agent-a256479f378ca2652`. It is therefore
  **plan 06's prose-truth pass to close**, not optional.

- **Merge hazard, plans 01 ↔ 02:** both touch `src/lib/resilient-fetch.retry.test.ts`. Plan 02
  INVERTED the SC2 resync case (two fetches → one) as an auto-fixed deviation; plan 01's Task 2
  adds a new two-attempt breaker re-arm case to the same file. Resolve at the wave-1 merge —
  keep BOTH; neither supersedes the other.

**Founder-visible scope fact, from 141.2-02's measurement:** `resync` was **20 of 42 all-time
`/process-key` rows (48%)**. Removing its retry grant removes the dominant share of
retry-eligible traffic. With plan 05, the shipped retry surface becomes `onboard`-with-a-
session-id only. 141's rollout was broader than its evidence supported on BOTH flows it
enabled. This must appear in the CHANGELOG and the final summary, not just in a plan SUMMARY.

## Shipping shape

All three phases land stacked on `feat/v1.16-141-jobs-rate-retry`. Phase 141 itself is
already on `main` via PR #655; this branch carries 141 + 141.1 + 141.2 forward, so the
single PR is the whole seam-retry story. Land ONE PR at a time.
