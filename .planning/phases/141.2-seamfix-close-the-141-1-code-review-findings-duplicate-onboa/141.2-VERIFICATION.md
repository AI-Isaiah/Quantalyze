---
phase: 141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa
verified: 2026-08-01T17:31:34Z
verified_at_commit: 030f2ba1
status: human_needed
score: 17/17 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  note: initial verification
gaps: []
deferred: []
human_verification:
  - test: "Re-run the production audit-history read that the CHANGELOG, TODOS.md and the `getDenominator` docblock quote as measured fact: `select count(*), count(distinct metadata->>'correlation_id') from audit_log where entity_type='process_key'` (claimed 42 rows, 42 distinct, 0 carrying a `wizard:` prefix) and the `flow_type` breakdown (claimed resync = 20/42 = 48%)."
    expected: "The numbers still support the two founder-visible claims: (a) the 141.1 dedup collapsed nothing on real traffic, (b) withdrawing resync's grant removes the dominant share of retry-eligible /process-key traffic."
    why_human: "Requires a live production Supabase read. The repository contains no fixture, transcript or probe output for it; the tree can neither confirm nor falsify the numbers."
  - test: "Re-run the production truncation probe: an unbounded `.select()` against `audit_log` (claimed to hold 7350 rows and to return exactly 1000 with HTTP 200 and `error: null`)."
    expected: "The truncation is reproducible, or — if the table has since been trimmed — the CHANGELOG's 'proven truncating in production' wording is re-qualified."
    why_human: "Live PostgREST behaviour against production data volume; not derivable from the repo."
  - test: "Re-run the read-only Upstash probe of the five breaker keys (`breaker:railway`, `breaker:mt5-gateway`, `breaker:supabase`, `breaker:kek`, `breaker:egress-proxy`)."
    expected: "Still absent, keeping the CHANGELOG's framing of finding 10 as hardening rather than incident remediation honest. If any key is now present, that framing needs revisiting before ship."
    why_human: "Requires live Upstash credentials; a point-in-time probe result is not stored in the repo, and the CHANGELOG itself concedes 'a point-in-time probe is not a history'."
  - test: "Capture a real Railway-edge 503 carrying an empty, zero or non-numeric `Retry-After` (the phase's declared `[ASSUMED]` / unobserved case behind finding 9)."
    expected: "Either a captured trace exists — at which point the docblock's and runbook's 'whether the platform edge can is unverified' sentences get upgraded to observed — or the sentences stay as written."
    why_human: "Cannot be induced on demand; the only contract-bound 503 emitter we own (`error_contract._validate`) raises on `retry_after <= 0` and structurally cannot produce one. Declared Manual-Only in 141.2-VALIDATION.md; not a gap."
warnings:
  - id: W1
    severity: warning
    item: "141.2-VALIDATION.md SC-M row and the Oracle Independence note cite `resilient-fetch.retry.test.ts:1092-1109` as the observation / surviving-coverage site. The per-attempt-latch case actually lives at 1305-1323; 1092-1109 is the SC-B fresh-budget case."
    impact: "Stale coordinate inside this phase's own falsifiability ledger — the exact drift class the phase exists to close. The pasted evidence in the same row cites the correct `:1322`, and the code-side tombstone in `seam-constants.pin.test.ts:589-604` refers to the case by NAME, so nothing load-bearing is wrong."
  - id: W2
    severity: warning
    item: ".planning/ROADMAP.md:358 still reads 'the founder ordered all 13 fixed in code'. 141.2-CONTEXT.md:208 carries the same planning-time sentence."
    impact: "That is the phrasing 141.2-VALIDATION.md:195-199 explicitly forbids as a reading of the outcome ('twelve remediated, one dispositioned'). ROADMAP.md is the orchestrator's to update at phase close."
  - id: W3
    severity: warning
    item: "141.2-RUNPLAN.md items 8, 9 and 11 are unchecked. Item 11 (correct the unshipped [0.51.0.0] CHANGELOG entry) was in fact completed at commit 8dd3603c."
    impact: "Stale checkbox only; the work is in the tree. Items 8-13 remaining unchecked is otherwise correct — ship/version-bump has not happened."
  - id: W4
    severity: warning
    item: "DEF-141.2-03-A (stale route coordinates in a skipped test's comment) exists only in the phase's deferred-items.md; it does not appear in TODOS.md."
    impact: "The project's declared single backlog source is root TODOS.md. DEF-141.2-03-B and -03-C ARE reflected there (entries 4 and 1); -03-A is not. Below the founder stopping rule, so it is bookkeeping, not scope."
  - id: W5
    severity: warning
    item: ".planning/STATE.md still reads 'Phase 141.2 (seamfix) — INSERTED, needs planning'."
    impact: "Orchestrator-owned; expected at this point in the workflow. Named so it is not lost."
---

# Phase 141.2: SEAMFIX — Verification Report

**Phase Goal:** Close the 13 verified findings from the xhigh code review of 141.1, each traced across BOTH sides of the seam (TypeScript client + Python service).
**Verified:** 2026-08-01T17:31:34Z at `030f2ba1` (branch `feat/v1.16-141-jobs-rate-retry`, main worktree)
**Status:** human_needed — 17/17 must-haves verified in the tree; four production-measurement claims quoted in shipped prose cannot be re-derived from the repository.
**Re-verification:** No — initial verification.

**Method note.** This phase's own defect class is "a check that was present, ran, and could not fail". Reading the code was therefore not treated as sufficient for the four highest-consequence rows. **Four mutations were applied to production source in this worktree, run, and reverted** (SC-M, SC-A, SC-K, SC-I). Every one reddened at the site the defect actually reaches, with the assertion text pasted below. The tree was confirmed byte-clean (`git status` empty) after each restore.

**Advisory-gate language.** Branch protection is deferred until paying clients, so every CI gate here is advisory at merge. The pins this phase adds **would have caught** these regressions at 141.1; nothing in CI *did* stop them, because they did not exist.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | **Finding 1** — `onboard`'s retry is refused when the call carries no truthy `wizard_session_id`, decided at the shared chokepoint | ✓ VERIFIED | `seam-retry-registry.ts:391-399` — `retriesForFlow` consults the YES map first, then `Boolean(context?.wizard_session_id)`; `undefined` context → 0. Consumed at the single chokepoint `process-key-client.ts:527`. **Mutation run** (`return 1;`): 7 pins RED including the REAL-client chokepoint pins and the transport-grain case (`expected "vi.fn()" to be called 1 times, but got 2 times`). |
| 2 | **Finding 2** — the denominator cannot be moved by a wire-supplied `X-Correlation-Id` | ✓ VERIFIED | `flag-monitor/route.ts:298-304` — `.select("id", { count: "exact", head: true })`. No caller-supplied value is read at all; the dedup was deleted, not hardened. |
| 3 | **Finding 3** — the denominator cannot be silently truncated at PostgREST `max_rows` | ✓ VERIFIED | Same site: `head: true` materialises no rows, so the 1000-row cap is unreachable at any volume. Rationale recorded `route.ts:221-228`. |
| 4 | **Finding 4** — the dedup that collapsed nothing on re-minted server ids is gone | ✓ VERIFIED | The `metadata->>correlation_id` projection no longer exists anywhere in `flag-monitor/route.ts`. The `unattributable` singleton branch went with it (`route.ts:276-279`). |
| 5 | **Finding 5** — attempt 2 is judged by its OWN admission instant | ✓ VERIFIED | `resilient-fetch.ts:2219` (`let`), re-captured at `2341` immediately AFTER the pre-attempt-2 `CircuitOpenError` throw. Reasoning at `2244-2259`. Confirmed live by the SC-M mutation, which reddened the D-09 re-arm pin (`expected 1700000000200 to be greater than 1700000000200`, `resilient-fetch.retry.test.ts:1482`). |
| 6 | **Finding 6** — `resync`'s retry grant is withdrawn, on BOTH sides of the seam | ✓ VERIFIED | `seam-retry-registry.ts:242-270` — `resync` sits in `RETRY_AUDIT_NO_FLOW_TYPES` with the withdrawal recorded and a falsifiable re-enable condition. `RETRY_SAFE_FLOW_TYPES` (`:172-224`) holds `onboard` alone. Python side corrected in the same phase: `process_key.py` 2b comment no longer claims the sequential class is closed (diff `cca4f198..HEAD`). |
| 7 | **Finding 7** — numerator and denominator share ATTEMPT grain | ✓ VERIFIED | `getNumerator` counts Sentry EVENTS (`route.ts:129-135`); `getDenominator` counts audit rows, one per HTTP attempt (`route.ts:237-244` states the emit-order property that makes it so). Downward retry bias named as a limitation at `route.ts:260-267`, not engineered around. |
| 8 | **Finding 8** — recorded as a DISPOSITION, with the residual stated as LIVE everywhere it is summarised | ✓ VERIFIED | `seam-retry-registry.ts:345-377` — "⚠️ **THAT AMPLIFICATION IS STILL LIVE IN THIS CODE.** It is DISPOSITIONED, not remediated… no limiter-aware retry path was built, no limiter constant moved". `TODOS.md:611` entry 2, same wording. `CHANGELOG.md:67-72` — "accepted rather than fixed… No limiter code changed." Runbook blind-spot note `seam-breaker.md:140-146`. **No artifact reads "all 13 fixed in code"** (see W2 for the two planning-artifact exceptions). |
| 9 | **Finding 9** — `Retry-After` is PARSED, not counted | ✓ VERIFIED | `resilient-fetch.ts:2052-2056` delegates to `parseRetryAfterSeconds` (`src/lib/retry/retry-after.ts:36-60`), which returns strictly-positive seconds or `null` for absent/empty/zero/negative/unparseable, and resolves the HTTP-date form against the response's own `Date` header. Parsed value discarded at the `!== null`, so the safety property survives. |
| 10 | **Finding 10** — the breaker ARMS over a corrupt-but-present lock (WRITE path) | ✓ VERIFIED | `resilient-fetch.ts:1482-1484` keeps the RAW value; `:1512` branches the write on `rawLock === null`, not on the decode, so a corrupt value takes the `get: true` displace arm (`:1581-1584`) and `written` is true. **Mutation run** (`if (existing === null)`): 3 `[141.2 / SC-A]` cases RED, including the racer case `expected [] to have a length of 1 but got +0` (no `seam.breaker.open` emitted). |
| 11 | **Finding 11** — an absurd ABSOLUTE epoch is rejected | ✓ VERIFIED | `resilient-fetch.ts:1119` — `if (expiresAtMs > nowMs + MAX_BREAKER_LOCK_SPAN_MS) return null;`, one-sided by design with the past side kept open so tombstones still decode for the close event (`:1092-1098`). `nowMs` is a parameter, so the bound is deterministically testable. |
| 12 | **Finding 12** — a denominator read error is a DISTINCT outcome, never zero traffic | ✓ VERIFIED | `flag-monitor/route.ts:306-312` returns `{ ok: false, reason: "denominator_read_failed" }`. **Mutation run** (`return { kind: "ok", total: 0 }`): both `(D1/SC-I)` cases RED — `expected 'zero_denominator' to be 'denominator_read_failed'`. |
| 13 | **Finding 13** — the tautological pin is DELETED and the surviving coverage can actually fail | ✓ VERIFIED | `Math.ceil(BREAKER_FAILURE_THRESHOLD / 2)` appears nowhere in `src/` except the tombstone comment at `seam-constants.pin.test.ts:589-604`. **Mutation run independently** (delete `recorded = false` at `resilient-fetch.ts:2345`): `seam-constants.pin.test.ts` **1 file passed WHOLE**, while `resilient-fetch.retry.test.ts:1322` went RED — `AssertionError: expected 1 to be 2`. Finding 13 reproduced from scratch by this verifier, not accepted on report. |
| 14 | **Prose matches the tree; no sentence implies a retry surface broader than `onboard`-with-a-key** | ✓ VERIFIED | Both RUNPLAN-flagged hand-offs closed: `process-key-client.ts:509-514` now reads "⚠️ `onboard` IS THE ONLY ALLOWLISTED FLOW, and its grant is CONDITIONAL"; `flag-monitor/route.ts:246-255` names and corrects its own "a retried onboard/resync" sentence. Runbook `seam-breaker.md:188-214` states `onboard` alone + conditional, `resync` withdrawn. Repo-wide grep for `onboard/resync` in a retry context returns only correct uses (`budgetKeyFor` many-to-one at `process-key-client.ts:72`, `seam-retry-registry.ts:93`) and one historical statement at `finalize-wizard/route.ts:1576`. |
| 15 | **Declared scope limits are still stated as LIMITS, not upgraded** | ✓ VERIFIED | SC-H numerator arm: `141.2-VALIDATION.md:108` and `:188-190`, plus `141.2-03-SUMMARY.md:180` and `141.2-06-SUMMARY.md:211` — all say "arithmetically entailed but **not** run as its own mutation". Railway-edge `[ASSUMED]`: `141.2-VALIDATION.md:81` Manual-Only row, `resilient-fetch.ts:2045-2050` ("Whether the platform edge can is UNVERIFIED — no such response has been captured"), `seam-breaker.md:243-244`. Prod probe: `CHANGELOG.md:86-89` — "found all five breaker keys absent, so this landed as hardening, not as incident remediation… a point-in-time probe is not a history". |
| 16 | **No VERSION / package.json bump has happened** | ✓ VERIFIED | `VERSION` = `0.51.0.0`, `package.json:80` = `0.51.0.0`, both last touched at `f6dee566` (141.1-09). `git log cca4f198..HEAD -- VERSION package.json` → 0 commits. RUNPLAN item 10 correctly unchecked. |
| 17 | **The gates are green on a clean tree** | ✓ VERIFIED | Re-run by this verifier, not read from the SUMMARY. See Behavioral Spot-Checks below. |

**Score:** 17/17 truths verified.

---

### Falsifiability Ledger — independent re-observation

The 141.2-VALIDATION.md ledger claims SC-A…SC-M all Observed. Four were re-run here from scratch. **All four reddened at the claimed site**, and in the SC-M case the claimed *contrast* (the deleted pin stays green under the same mutation) was reproduced as well.

| SC | Mutation applied to production source | Result | Verdict |
|----|---------------------------------------|--------|---------|
| SC-M (f13) | delete `recorded = false;` (`resilient-fetch.ts:2345`) | `resilient-fetch.retry.test.ts:1322` RED `expected 1 to be 2`; `resilient-fetch.retry.test.ts:1482` RED `expected 1700000000200 to be greater than 1700000000200`; **`seam-constants.pin.test.ts` 1 file PASSED whole** | ✓ CONFIRMED — the nominated site reddens, the deleted pin provably could not. Finding 13 reproduced independently. |
| SC-A (f10) | `if (rawLock === null)` → `if (existing === null)` (`:1512`) | 3 of 105 RED in `resilient-fetch.test.ts`, incl. `[141.2 / SC-A] a racer that still sees the CORRUPT value… — expected [] to have a length of 1 but got +0` at `:1562` | ✓ CONFIRMED — mutation lands on the WRITE path, which is where the 141.1 defect was. |
| SC-K (f1) | `return Boolean(context?.wizard_session_id) ? 1 : 0;` → `return 1;` (`seam-retry-registry.ts:398`) | 7 RED across 3 files: helper-level (absent/null/empty-string/undefined-context), REAL-chokepoint through `postProcessKey` (`expected 1 to be +0`), and transport grain (`expected "vi.fn()" to be called 1 times, but got 2 times`) | ✓ CONFIRMED — caught at the real call site and on the wire, not only in the helper. |
| SC-I (f12) | read-error arm → `return { kind: "ok", total: 0 }` (`flag-monitor/route.ts:306-312`) | 2 RED — `expected 'zero_denominator' to be 'denominator_read_failed'` (GET and POST) | ✓ CONFIRMED. |

Restore was verified after each: `git status --short` empty, `git diff --stat` empty.

**SC-B, SC-C, SC-D, SC-E, SC-F, SC-G, SC-H, SC-J, SC-L, SC-K-b** were not independently re-mutated. Their ledger rows carry pasted assertion text with matching test-name and file coordinates, and every named pin exists and passes in the tree. Reported as ledger-attested, not verifier-attested.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/resilient-fetch.ts` | corrupt-lock write path, absolute epoch bound, per-attempt admission, parsed `Retry-After` | ✓ VERIFIED | 2541 lines; four distinct fixes at `:1119`, `:1482-1512`, `:2341`, `:2052-2056`. Wired: imported by the seam clients and the four analytics wrappers. |
| `src/lib/retry/retry-after.ts` | the ONE `Retry-After` parser, reused not re-rolled | ✓ VERIFIED | 60 lines; imported at `resilient-fetch.ts:8`. B20 contract respected — no hand-rolled second parse. |
| `src/lib/seam-retry-registry.ts` | `resync` moved to NO, `retriesForFlow` gate, finding-8 disposition | ✓ VERIFIED | 543 lines; `retriesForFlow` at `:391-399`, exported and consumed at exactly one chokepoint. Leaf-pure as documented. |
| `src/lib/process-key-client.ts` | chokepoint consumes `retriesForFlow`; docblock corrected | ✓ VERIFIED | `:527` single-line expression (the wiring guard's extractor is line-scoped, and the guard is green in the full suite). |
| `src/app/api/cron/flag-monitor/route.ts` | server-side COUNT, fail-loud read error | ✓ VERIFIED | `getDenominator` at `:294-315`; `DenominatorResult` union threaded through `handle()` at `:430-432`. |
| `analytics-service/routers/process_key.py` | 2b SCOPE BOUND comment no longer claims the sequential class is closed | ✓ VERIFIED | Comment-only edit, both blocks rewritten (`:1395-1412`, `:1431-1445`). Python suite green. |
| `docs/runbooks/seam-breaker.md` | trip-count and retry-surface prose true against the final tree | ✓ VERIFIED | 398 lines. Read-vs-write split at `:57-67`; conditional trip-count table at `:107-131`; retry surface at `:186-214`; parse-semantics table at `:232-244`. |
| `TODOS.md` | finding 8 booked; the two classes censused | ✓ VERIFIED | `:606-615` — five entries, opening line states "twelve were remediated… finding 8 was DISPOSITIONED". |
| `CHANGELOG.md` `[0.51.0.0]` | unshipped entry describes the surface that actually ships | ✓ VERIFIED | `:3-127`. Retry narrowing is the founder-visible headline (`:12-32`); finding 8 stated as accepted-not-fixed (`:67-72`); the false "1e17 Retry-After" sentence named and corrected in place (`:81-86`). |
| `src/lib/seam-constants.pin.test.ts` | tautological pin deleted, tombstone left | ✓ VERIFIED | The assertion is gone; `:589-604` records why, by NAME rather than by line number. File sheds one case (82 → 81) as planned. |

No artifact was found to be a stub, orphan, or hollow.

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `postProcessKey` | `retriesForFlow` | `retriesOverride:` init arg | ✓ WIRED | `process-key-client.ts:18` import, `:527` call. Proven behaviourally by the SC-K mutation reaching the wire. |
| `retriesForFlow` | `RETRY_SAFE_FLOW_TYPES` | map lookup, YES-first ordering | ✓ WIRED | `seam-retry-registry.ts:397` — absence resolves to 0 without delegating to the budget row. |
| `resilientFetch` retry loop | `recordSeamFailure` | `recordOnce(breakerKey)` closing over `admittedAtMs` | ✓ WIRED | `:2262-2266`; both `recorded` and `admittedAtMs` re-armed per iteration (`:2341`, `:2345`). |
| `recordSeamFailure` trip path | Redis write branch | `rawLock === null` ? `nx` : `get` | ✓ WIRED | `:1512` / `:1525` / `:1581-1584`; `written` gates `emitBreakerTransition` at `:1595`. |
| `hasContractualWait` | `parseRetryAfterSeconds` | delegation | ✓ WIRED | `:2055`; gates the retry fall-through at `:2486`. |
| `handle()` | `getDenominator` | `DenominatorResult` discriminated union | ✓ WIRED | `route.ts:430-432` — a `terminal` result short-circuits before `handleZeroDenominator` can be reached. |
| `keys/validate-and-encrypt` | seam core | explicit `retriesOverride: 0` | ✓ WIRED | `route.ts:247` — the second `onboard` producer is belt-and-braced (registry predicate + explicit 0). |

---

### Data-Flow Trace (Level 4)

| Artifact | Data variable | Source | Produces real data | Status |
|----------|---------------|--------|--------------------|--------|
| `getDenominator` | `count` | Supabase `audit_log` COUNT, `entity_type='process_key'`, windowed | Yes — real server-side count; `error` short-circuits before any default | ✓ FLOWING |
| `getNumerator` | `errorCount` | Sentry events API, indexed fields only | Yes; unreachable-vs-rate-limited distinguished at `:151-187` | ✓ FLOWING (scope caveat self-declared at `:120-128`: the `transaction:/process-key` form is derived, not observed) |
| `retriesForFlow` | `0 \| 1` | `args.context` from the real route bodies | Yes — `finalize-wizard/route.ts:1589-1591` threads the draft's actual `wizard_session_id` via conditional spread; absence flows through as absence and the gate refuses the retry | ✓ FLOWING |
| `recordSeamFailure` | `rawLock` | live `redis.get` | Yes — the raw string, not a decode | ✓ FLOWING |

---

### Behavioral Spot-Checks

All re-run by this verifier at `030f2ba1` on a clean tree.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TS suite green | `npx vitest run` | `Test Files 735 passed \| 19 skipped (754)` · `Tests 10487 passed \| 287 skipped (10774)` — exact match to the VALIDATION sign-off | ✓ PASS |
| Python suite green | `python3 -m pytest analytics-service/tests/ -q` | `4824 passed, 96 skipped in 78.21s` | ✓ PASS |
| Types clean | `npm run typecheck` | `tsc --noEmit` exit 0 | ✓ PASS |
| Lint clean | `npm run lint` | `0 errors, 1 warning` — the pre-existing `EquityChart.tsx:1119` `exhaustive-deps` warning; route-contract and admin-manifest checks OK | ✓ PASS |
| Tautological pin absent | `grep -rn "Math.ceil(BREAKER_FAILURE_THRESHOLD" src/ docs/` | one hit, and it is the tombstone comment | ✓ PASS |
| No VERSION bump | `git log cca4f198..HEAD -- VERSION package.json` | 0 commits | ✓ PASS |

---

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| — | `find scripts -path '*/tests/probe-*.sh'` | no conventional probe scripts exist in this repo | ? SKIP |

The phase declares no `scripts/*/tests/probe-*.sh`. Its verification contract is the Falsifiability Ledger, and that contract was honoured by re-running four of its mutations directly (above) rather than by reading a probe transcript.

---

### Requirements Coverage

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|-------------|--------|----------|
| SEAM-05 | ROADMAP 141.2 | the audit's retry verdicts | ✓ SATISFIED | `resync` verdict withdrawn to the NO map with evidence and a re-enable condition; `onboard`'s YES narrowed by `retriesForFlow`. Registry population fenced per-map (SC-E), which the full suite exercises. |
| SEAM-06 | ROADMAP 141.2 | the alert that watches them + the breaker | ✓ SATISFIED | Denominator uncapped / wire-blind / attempt-grained / fail-loud; breaker arms over a corrupt lock, bounds absolute epochs, and re-arms per attempt. |

No orphaned requirements: `grep "Phase 141.2" .planning/REQUIREMENTS.md` maps no IDs beyond these two.

---

### Anti-Patterns Found

Files modified in `cca4f198..HEAD` were scanned for debt markers and empty-implementation shapes.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `docs/runbooks/seam-breaker.md` | 307-312 | "Known gap:" block | ℹ️ Info | Deliberate, references `DEF-141.1-06-A` which is booked in `TODOS.md:602`. Formal follow-up present ⇒ not a debt-marker gate failure. |
| `TODOS.md` | 606-615 | `TODO`-file content by construction | ℹ️ Info | This IS the backlog file. |

**No `TBD`, `FIXME` or `XXX` markers were introduced by this phase in any modified source file.** No `return null` / `return []` / `=> {}` stub shapes were introduced. The one `return 0` shape the findings named (`getDenominator`'s error collapse) is exactly what this phase removed.

---

### Human Verification Required

All 17 truths are verified in the tree. The four items below are claims quoted as **measured production fact** in shipped prose (CHANGELOG, TODOS.md, docblocks) that cannot be re-derived from the repository. They are named rather than passed silently. None of them affects the correctness of a code fix — each one calibrates how a founder-visible sentence should read.

#### 1. Production audit-history numbers

**Test:** `select count(*), count(distinct metadata->>'correlation_id') from audit_log where entity_type='process_key'`, plus the `flow_type` breakdown.
**Expected:** 42 rows / 42 distinct ids / 0 with a `wizard:` prefix; `resync` = 20/42 (48%).
**Why human:** live production Supabase read; no fixture or transcript in the repo. These two numbers carry the CHANGELOG's headline ("removes the dominant share of retry-eligible traffic") and the `getDenominator` docblock's "the dedup collapsed nothing on real traffic".

#### 2. The truncation probe

**Test:** unbounded `.select()` on `audit_log` at production volume.
**Expected:** ~7350 rows present, exactly 1000 returned, HTTP 200, `error: null`.
**Why human:** live PostgREST behaviour. Supports "proven truncating in production" in `TODOS.md:613`.

#### 3. The five breaker keys

**Test:** read-only Upstash probe of `breaker:railway`, `breaker:mt5-gateway`, `breaker:supabase`, `breaker:kek`, `breaker:egress-proxy`.
**Expected:** all absent — which is what keeps finding 10 framed as hardening rather than incident remediation (`CHANGELOG.md:86-89`).
**Why human:** live credentials required. The CHANGELOG already concedes "a point-in-time probe is not a history"; if a key is now present, the framing needs revisiting before ship.

#### 4. The Railway-edge non-positive `Retry-After` — `[ASSUMED]`, by design

**Test:** capture a real edge 503 carrying `Retry-After: 0`, `""`, or garbage.
**Expected:** either such a trace exists (upgrade the prose) or it does not (leave it as written).
**Why human:** cannot be induced on demand; `error_contract._validate` structurally cannot emit one. **This is a declared limitation, not a gap** — `141.2-VALIDATION.md:81` says "Do not claim this is observed", and the code and runbook both say "unverified". Listed for completeness only.

---

### Warnings (non-blocking)

| ID | Item | Why it matters here |
|----|------|---------------------|
| **W1** | `141.2-VALIDATION.md` SC-M row and the Oracle Independence note cite `resilient-fetch.retry.test.ts:1092-1109` as the observation / surviving-coverage site. The per-attempt-latch case is at **1305-1323**; 1092-1109 is the SC-B fresh-budget case. | A stale coordinate inside this phase's own falsifiability ledger — the drift class the phase exists to close. Not load-bearing: the pasted evidence in the same row correctly cites `:1322`, and the code-side tombstone (`seam-constants.pin.test.ts:589-604`) names the case rather than its line. Fix by naming the case, not by re-numbering. |
| **W2** | `.planning/ROADMAP.md:358` — "the founder ordered all 13 fixed in code". Same sentence in `141.2-CONTEXT.md:208`. | This is exactly the phrasing `141.2-VALIDATION.md:195-199` forbids as a reading of the outcome. Both are planning-time intent statements, and the outcome diverged for finding 8. ROADMAP.md is the orchestrator's to correct at phase close; CONTEXT.md is a frozen artifact and lower priority. |
| **W3** | `141.2-RUNPLAN.md` item 11 (correct the unshipped `[0.51.0.0]` entry) is unchecked, but the work landed at `8dd3603c`. | Stale checkbox. Items 8-13 otherwise correctly reflect that ship and the version bump have not happened. |
| **W4** | `DEF-141.2-03-A` exists only in the phase's `deferred-items.md`, not in `TODOS.md`. | The project's single backlog source is root `TODOS.md`; `-03-B` and `-03-C` ARE reflected there (entries 4 and 1). `-03-A` is comment-drift inside an `it.skip` block — below the founder stopping rule, so bookkeeping only. |
| **W5** | `.planning/STATE.md:6,49` still reads "Phase 141.2 — INSERTED, needs planning". | Orchestrator-owned and expected at this point; named so it is not lost at phase close. |

---

### Gaps Summary

**None.** Every one of the 13 findings resolves at the site the defect actually reaches, and the four mutations this verifier applied to production source all reddened where the ledger said they would — including SC-M, where the phase's own contrast (the deleted pin stays green under the mutation its failure message described) was reproduced from scratch rather than accepted on report.

The three declared scope limits are intact and still worded as limits: SC-H's numerator arm is stated as arithmetically entailed and **not** separately mutated; the Railway-edge non-positive `Retry-After` stays `[ASSUMED]` and unobserved in the ledger, the docblock and the runbook; and finding 10 is framed as hardening on the strength of a probe the CHANGELOG itself calls a point-in-time reading rather than a history.

Finding 8's disposition is stated honestly in all four places it is summarised — the `retriesForFlow` docblock, `TODOS.md`, the `[0.51.0.0]` CHANGELOG entry and the runbook's rate-limit blind-spot note — each saying the amplification is still live and that no limiter code was written. No shipped artifact reads "all 13 fixed in code"; the two that carry that phrasing (W2) are planning artifacts, one of them the orchestrator's to update.

`VERSION` and `package.json` are untouched at `0.51.0.0`, correctly leaving the bump to the ship step. `STATE.md` and `ROADMAP.md` are stale, which is the orchestrator's to close.

Status is `human_needed` rather than `passed` solely because four production measurements quoted as fact in shipped prose cannot be re-derived from this repository. Nothing in the codebase blocks proceeding.

---

_Verified: 2026-08-01T17:31:34Z at `030f2ba1`_
_Verifier: Claude (gsd-verifier) — goal-backward, FORCE stance; four ledger mutations re-run independently_
