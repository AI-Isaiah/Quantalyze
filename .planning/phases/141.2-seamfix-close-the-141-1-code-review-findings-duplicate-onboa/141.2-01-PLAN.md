---
phase: 141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resilient-fetch.ts
  - src/lib/resilient-fetch.test.ts
  - src/lib/resilient-fetch.retry.test.ts
  - docs/runbooks/seam-breaker.md
  - TODOS.md
autonomous: true
requirements: [SEAM-06]
tags: [seam-breaker, decodeBreakerLock, recordSeamFailure, admittedAtMs, regression, write-path]

must_haves:
  truths:
    - "Finding 10: with a corrupt-but-PRESENT lock value in the breaker store, recordSeamFailure's trip path ARMS the circuit (SET…GET displaces the corrupt value) and emitBreakerTransition('open', …) fires — the SET NX dead-end is gone"
    - "Finding 11: a lock with a legal 30 000 ms span at an absurd absolute epoch (open:1e17:1e17+30000) decodes to null — no implausible Retry-After can reach the wire; tombstone (past-side) semantics are UNCHANGED"
    - "Finding 5: each retry attempt's failure is judged by the A-25 guard against that attempt's OWN admission instant — a lock armed between admissions no longer suppresses attempt 2's genuinely-new evidence, and the retried wave can re-arm the breaker with a FRESH lock (identity change, not mere presence)"
    - "TODOS.md's RE-OPENED breaker entry records truthful closure by this plan (with mutation evidence observed), and the decodeBreakerLock docblock no longer claims corruption 'fails forward, unchanged' on the write side"
  artifacts:
    - path: "src/lib/resilient-fetch.ts"
      provides: "rawLock-presence branch in recordSeamFailure's trip path; one-sided absolute plausibility bound in decodeBreakerLock (optional nowMs param); per-attempt admittedAtMs recapture; corrected docblocks"
    - path: "src/lib/resilient-fetch.test.ts"
      provides: "write-path pin driving recordSeamFailure with a corrupt value PRESENT; absolute-epoch decoder cases"
    - path: "src/lib/resilient-fetch.retry.test.ts"
      provides: "two-attempt pin where the lock arms BETWEEN admissions and the breaker re-arms with a lock whose identity provably changed"
  key_links:
    - from: "src/lib/resilient-fetch.ts recordSeamFailure"
      to: "the breaker store"
      via: "branch on the RAW value's presence, not the decoded result"
      pattern: "rawLock === null"
    - from: "src/lib/resilient-fetch.ts retry loop"
      to: "recordSeamFailure's A-25 guard"
      via: "admittedAtMs reassigned after each successful pre-attempt breaker re-check"
      pattern: "let admittedAtMs"
---

<objective>
Close findings 10, 11 and 5 — the breaker cluster. Finding 10 is the phase's TOP priority:
`git log -S "MAX_BREAKER_LOCK_SPAN_MS"` proves it is a pure REGRESSION introduced by 141.1
commit `f308b460`, live on every seam call. A corrupt-but-present lock value now decodes to
`null`, routing `recordSeamFailure`'s trip path into `SET NX` — which cannot overwrite an
existing key — so the breaker can never arm for that key's remaining TTL, across all fifteen
seam routes, with no transition event to say the protection is absent. Finding 11 is the
same function's over-claim (span-only bound admits absurd absolute epochs; TODOS.md's old
"can no longer be minted" discharge was false and is already RE-OPENED at `c58cc383`).
Finding 5 (D-09) is the loop-level sibling: `admittedAtMs` is captured once above the retry
loop, so the A-25 guard judges attempt 2's failure with attempt 1's admission instant and
the retried wave can never re-arm the breaker on the 30s `optimize-weights` budget —
allocators wait 60s+ per click instead of an immediate CIRCUIT_OPEN 503.

Priority defense (research §0.1, §2.7): the flag-monitor cluster sits in a monitor whose
`flag_monitor_zero_denominator_streak` row has NEVER been written in production — real but
not live-firing. This regression is live on every seam call. Plans 01–03 are all wave 1
with no dependencies and dispatch in PARALLEL — the priority is expressed by dispatch order
(the orchestrator sends this plan first) and by review attention, not by wave sequencing.

Purpose: findings 10, 11, 5 (D-04 as corrected by RESEARCH §0.1, and D-09). SC-A, SC-B, SC-C.
Output: a breaker that can always arm, a decoder that rejects implausible epochs one-sidedly,
per-attempt admission timestamps, true prose, and a truthfully-closed TODOS entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-CONTEXT.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-RESEARCH.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-FINDINGS.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-PATTERNS.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Findings 10 + 11 — the trip path branches on RAW presence; the decoder gains a one-sided absolute bound (SC-A, SC-B)</name>
  <files>src/lib/resilient-fetch.ts, src/lib/resilient-fetch.test.ts, TODOS.md</files>
  <read_first>
    - 141.2-RESEARCH.md §3.2 and §3.3 — the exact recommended fix shapes for both findings, including the "Code Examples" section (the rawLock branch and the nowMs-parameterised decoder). Use these shapes; both were argued against the module's own docblock, which already rejects the Lua-CAS and two-key alternatives on the SC-4b 300 000 ms ceiling.
    - src/lib/resilient-fetch.ts — `decodeBreakerLock` (~:1042-1057) and its LO-02/TS-39 docblock above (~:1016-1040, including the "fails forward, unchanged" sentence finding 10 disproves); `recordSeamFailure`'s trip path (~:1400-1500: the `existing === null` branch at ~:1436, the `nx: true` SET at ~:1446, the `get: true` SET at ~:1494, and the still-live-lock + A-25 guards which must keep reading `existing` unchanged)
    - src/lib/seam-breaker-failopen.regression.test.ts (~:166-290) — the precedent for driving `recordSeamFailure` through `resilientFetch` against a fake store and asserting on what was written
    - src/lib/resilient-fetch.test.ts — the existing G2 describe (~:657-730): it seeds `"open:0:100000000000000000"` at ~:727 but exercises ONLY `isBreakerOpen` + one successful fetch — that is exactly why finding 10 shipped green; the new pin must NOT repeat this shape
    - TODOS.md — the "⛔ RE-OPENED 2026-08-01" breaker entry (search for "RE-OPENED 2026-08-01"), owned by this plan
  </read_first>
  <behavior>
    - Test 1 (SC-A, WRITE path): seed the fake store with a corrupt-but-present value (e.g. `open:0:100000000000000000`) under the breaker key, then drive `recordSeamFailure`'s trip path (via `resilientFetch` failures to threshold against the fake store, following the failopen-regression precedent, or by direct drive if exported). Assert: a FRESH valid lock was WRITTEN over the corrupt value AND the open transition was emitted. This test must be RED against the current tree (today the SET NX is refused and nothing is written).
    - Test 2 (SC-A guard, absent case unchanged): with the key genuinely ABSENT, the trip path still uses `nx: true` (concurrent-trip idempotency preserved) — assert a lock is written and a second concurrent trip does not double-emit.
    - Test 3 (SC-B, decoder): `decodeBreakerLock("open:100000000000000000:100000000000030000", nowMs)` returns `null` — legal 30 000 ms span, absurd absolute epoch. Drive `isBreakerOpen` with that value seeded and assert the circuit reads CLOSED and no implausible retryAfterS is produced. RED against the current tree (it currently decodes and mints retryAfterS ≈ 1e14).
    - Test 4 (SC-B guard, tombstone unchanged): a lock with `expiresAtMs <= now` (recent past, legal span) still DECODES — the past side is deliberately unbounded because `isBreakerOpen` needs to decode tombstones to announce the close. Assert existing tombstone behaviour is intact.
    - Test 5: the skewed-writer variant — a lock armed with epochs ~1 hour in the future but a legal span is rejected (decodes null), so a reader never tells users to wait ~3600 s instead of 30.
  </behavior>
  <action>
    RED first: write the behavior tests above and run them — paste the failing output (Tests 1, 3, 5 red; 2, 4 green) into the work log before touching production code.

    Then in `src/lib/resilient-fetch.ts`:

    (a) Finding 10 — in `recordSeamFailure`'s trip path, capture the RAW store value before
    decoding (`rawLock`), keep `existing = decodeBreakerLock(rawLock)` and leave the
    still-live-lock guard and the A-25 guard untouched (they still read `existing`). Replace the
    `existing === null` branch condition for the write decision with a branch on `rawLock === null`:
    key genuinely absent → `nx: true` (unchanged, concurrent-trip idempotency); key PRESENT
    (live lock, tombstone, OR corrupt) → the existing `SET … GET` displacement, with
    `written = displaced === null || displaced.expiresAtMs <= now`. A corrupt displaced value
    decodes to null ⇒ written is true ⇒ the transition is emitted — truthful, this call DID arm
    the circuit. Zero extra Redis round trips (SC-4b ceiling; the docblock's own argument).

    (b) Finding 11 — in `decodeBreakerLock`, add an optional `nowMs: number = Date.now()`
    parameter (keeps the decoder deterministically testable without fake timers — existing
    callers unchanged) and, after the span bound, add the ONE-SIDED absolute plausibility bound:
    `if (expiresAtMs > nowMs + MAX_BREAKER_LOCK_SPAN_MS) return null;`. Do NOT add a past-side
    bound — tombstone semantics depend on decoding expired locks.

    (c) Prose moves with code, SAME commit. In the `decodeBreakerLock` docblock: the sentence
    claiming "The rejection direction is `null` … corruption still fails forward, unchanged" is
    the opposite of true on the write side — per the repo's docblock-correction idiom (see
    `seam-retry-registry.ts` "the sentence that used to stand here" form), name the old claim,
    say it was wrong (finding 10: null conflated "absent" with "present-but-corrupt" and jammed
    the SET NX write), and state the new truth: read side fails forward to CLOSED; write side
    now distinguishes raw presence so a corrupt value is displaced and self-heals on the next
    recorded failure. Also document the new absolute bound and why it is one-sided.
    `resilient-fetch.ts` is on SEAM_CITATION_SURFACE — SYMBOL ANCHORS only in comments, never
    `file.ext:NN`; do not copy this plan's line coordinates into code.

    (d) TODOS.md — update the "⛔ RE-OPENED 2026-08-01" breaker entry to record closure by
    141.2 plan 01: both halves fixed (write-path arming + absolute bound), with the honest
    statement of WHAT the new bound does and does not close (one-sided; past side deliberately
    open for tombstones). Do this only after the mutations below are observed. Advisory-gate
    language discipline: "would have caught", never "did stop".

    (e) One-shot prod probe (read-only, research Open Question 1 / assumption A3): using the
    Upstash REST creds in `.env.local`, `GET breaker:railway` (and the per-dependency keys in
    `breakerKeysFor` if cheap). If a corrupt value IS resident, this fix is an incident
    remediation, not a hardening — record the probe result either way in the SUMMARY. Do not
    write anything to prod Redis.
  </action>
  <verify>
    <automated>npx vitest run src/lib/resilient-fetch.test.ts src/lib/seam-breaker-failopen.regression.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RED-before-GREEN observed and pasted: the SC-A write-path test and the SC-B epoch tests fail against the pre-fix tree (or with the fix reverted), with real vitest output.
    - MUTATION SC-A (exact, from the VALIDATION ledger): revert the trip path to `nx: true` when `existing === null` (decoded-null branching) → the new write-path test REDs (no lock written, no transition). Restore → GREEN. Paste both runs.
    - MUTATION SC-B (exact): remove the absolute-plausibility bound, keeping the span bound → the `open:100000000000000000:100000000000030000` test REDs. Restore → GREEN. Paste both runs.
    - Tombstone guard test GREEN throughout both mutations (proves the bound is one-sided and past-side semantics were not broken).
    - `grep -c "nx: true" src/lib/resilient-fetch.ts` ≥ 1 (the absent-key idempotency arm survives) and the new presence-branch is asserted BY TEST, not by grep — no absence-greps against this file: its docblocks are REQUIRED to name the constructs being discussed (DEF-16-2 rule 1).
    - Prod probe result recorded in the work log (value found, or key absent).
    - `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>The breaker arms over a corrupt-but-present value with a truthful transition event; implausible epochs cannot mint a wire Retry-After; existing tombstone and concurrent-trip semantics are pinned unchanged; docblock and TODOS entry state the truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finding 5 (D-09) — per-attempt admission recapture, following the per-attempt latch analog (SC-C)</name>
  <files>src/lib/resilient-fetch.ts, src/lib/resilient-fetch.retry.test.ts, docs/runbooks/seam-breaker.md</files>
  <read_first>
    - 141.2-RESEARCH.md §3.1 — the confirmed mechanism and the minimal fix shape (make `admittedAtMs` a `let`, reassign immediately after each successful pre-attempt breaker re-check)
    - src/lib/resilient-fetch.ts — `const admittedAtMs = Date.now();` at ~:2099 (above the loop that begins ~:2153); `recordOnce` (~:2125-2129) which closes over it; the `attempt > 0` pre-attempt re-check (~:2170-2179, including the reBreaker.open throw); the per-attempt latch analog: `let recorded = false` at ~:2124, reset at ~:2199, with the docblock at ~:2114-2122 explaining why per-attempt state must be re-derived each iteration — this is the EXACT in-repo analog D-09 orders followed; do not invent a second mechanism
    - src/lib/resilient-fetch.retry.test.ts — the [SEAM-06] per-attempt-latch describe (~:1092-1109) for the fixture/store idioms (`statusSequenceFetch`, `shared.store`, `shared.counters`)
  </read_first>
  <behavior>
    - Test 1 (SC-C): drive `resilientFetch` on a retry-enabled budget through the REAL loop, with the store arranged so a lock ARMS BETWEEN attempt 1's admission and attempt 2's admission and then EXPIRES before attempt 2's pre-attempt re-check (the finding's exact window: admission t=0, lock armed t≈0.2s, attempt-1 failure recorded-and-suppressed by A-25, backoff, re-check passes on the expired lock, attempt 2 fails). The assertion MUST prove the stored lock's IDENTITY changed after attempt 2's failure: decode the value under the breaker key and require a FRESH lock whose `armedAtMs` and `expiresAtMs` are STRICTLY GREATER than the mid-test lock's. A bare presence assertion (`expect(store.get(KEY)).toBeDefined()` or equivalent) is FORBIDDEN — at assertion time the store already holds the expired mid-test lock, so presence passes under the exact suppression this test exists to catch (finding 10's failure shape, one file over: a pin that exercises the wrong observable and ships green). RED against the current tree (the original admittedAtMs ≤ armedAtMs suppresses attempt 2's evidence, so no fresh lock is written).
    - Test 2 (guard): a failure from an attempt that WAS in flight when the lock armed is still suppressed (A-25's stated predicate holds per attempt) — single-attempt admission before arming, failure after: no double-count. GREEN before and after the fix.
  </behavior>
  <action>
    RED first: write the two behavior tests, run, paste the failing output for Test 1.

    In `src/lib/resilient-fetch.ts`: change `const admittedAtMs = Date.now();` to `let`, and
    reassign `admittedAtMs = Date.now();` inside the `if (attempt > 0) { … }` block immediately
    AFTER the pre-attempt breaker re-check passes (after the reBreaker.open throw). `recordOnce`
    closes over the binding, so the last attempt's admission instant is what reaches
    `recordSeamFailure` — correct for the post-return `instrumentBody` closure too. Extend the
    per-attempt-state docblock (the one explaining the `recorded` latch reset) to name the
    admission timestamp as the second piece of per-attempt state and WHY: the A-25 guard's
    predicate is "was this request already in flight when that lock was armed", which is
    meaningful per attempt, not per logical request. Symbol anchors only.

    A helper-level test provably cannot see this defect — the defect is the RELATIONSHIP between
    the loop and `recordSeamFailure` (that is how it shipped green in 141.1). Both tests MUST
    drive `resilientFetch` itself.

    Runbook: this plan deliberately does NOT touch the trip-count table or retry-count prose in
    `docs/runbooks/seam-breaker.md` (those sections are invalidated by multiple decisions and are
    rewritten ONCE in plan 06, per PATTERNS §7c). This task's only runbook edit: the corruption
    sentence in the breaker-store section (~:51-53, "decodes to `null` and reads CLOSED") gains
    the write-side truth in the same commit as Task 1's code — corruption reads CLOSED on the
    read side AND is displaced (self-healing) on the next recorded failure on the write side.
    If Task 1 and Task 2 land as separate commits, move this runbook edit into Task 1's commit.
  </action>
  <verify>
    <automated>npx vitest run src/lib/resilient-fetch.retry.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RED-before-GREEN observed and pasted for Test 1 (real vitest output, failing assertion visible).
    - MUTATION SC-C (exact, from the VALIDATION ledger): hoist the admission timestamp back above the retry loop (single capture) → Test 1 REDs; Test 2 stays GREEN. Restore → both GREEN. Paste both runs.
    - Test 1's re-arm assertion compares lock IDENTITY — decoded `armedAtMs`/`expiresAtMs` strictly greater than the mid-test lock's — never mere presence. Reviewed in the diff: no `toBeDefined()`-only assertion on the breaker key in this test.
    - `grep -c "let admittedAtMs" src/lib/resilient-fetch.ts` → 1 (presence grep — safe under DEF-16-2 rule 4).
    - The existing [SEAM-06] per-attempt-latch case (~:1092-1109) still GREEN — the analog this fix copies is not disturbed.
    - Full quick-run for the file green; `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Each attempt's failure is judged against its own admission instant; the retried wave re-arms the breaker with a provably fresh lock; the A-25 suppression guard still holds for genuinely-stale evidence; the per-attempt-state docblock names both pieces of per-attempt state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| breaker store → trip path | a store value of unknown provenance (corrupt, skewed-writer, tombstone) drives a conditional write |
| breaker store → wire | decoded lock values become `Retry-After` headers, including to the anonymous teaser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.2-01 | DoS (self-inflicted) | `recordSeamFailure` trip path | mitigate | branch on RAW presence: corrupt-but-present values are displaced via the existing `SET … GET`, so the breaker always arms; transition emitted truthfully |
| T-141.2-02 | Information disclosure / DoS | `decodeBreakerLock` → `Retry-After` | mitigate | one-sided absolute plausibility bound (`expiresAtMs > nowMs + MAX_BREAKER_LOCK_SPAN_MS` ⇒ null); A-15 guard retained downstream |
| T-141.2-06 | Tampering | breaker key derivation | accept | keys remain the frozen closed set from `breakerKeysFor` (T-140-01 closed); this plan introduces no caller-derived key |
| T-141.2-SC | Tampering | npm/pip/cargo installs | accept | zero package installs in this phase (RESEARCH: Package Legitimacy Audit — not applicable) |

Breaker read direction is UNCHANGED: it fails FORWARD (store fault ⇒ closed, proceed) per the module docblock and runbook — this plan repairs the WRITE path only. Do not average this with the fail-loud rule (PATTERNS §7b).
</threat_model>

<verification>
- `npx vitest run src/lib/resilient-fetch.test.ts src/lib/resilient-fetch.retry.test.ts src/lib/seam-breaker-failopen.regression.test.ts` green at the final tree
- All three ledger mutations (SC-A, SC-B, SC-C) observed RED with pasted output, then restored GREEN
- `npm run typecheck` and `npm run lint` clean; before declaring stable, reproduce the file runs under CI's Node: `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run <files>`
- Prod Upstash probe result recorded (read-only)
- VALIDATION.md rows SC-A/SC-B/SC-C flipped to Observed ✅ with evidence, and the Per-Task Verification Map Status column filled for findings 10, 11, 5
</verification>

<success_criteria>
- Findings 10, 11, 5 closed with seam-real pins (write path and real loop, not helpers)
- All three mutations from the falsifiability ledger observed RED then GREEN
- Docblocks, the runbook corruption sentence, and the TODOS RE-OPENED entry state the post-fix truth
</success_criteria>

<output>
Create `.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-01-SUMMARY.md` when done. Include: the pasted RED output for SC-A, SC-B, SC-C; the prod probe result; and the statement that finding 10 was a 141.1 regression (attribution: `f308b460`), fixed here.
</output>
