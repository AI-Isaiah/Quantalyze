---
phase: 141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa
plan: 05
type: execute
wave: 2
depends_on: ["141.2-02"]
files_modified:
  - src/lib/seam-retry-registry.ts
  - src/lib/seam-retry-registry.test.ts
  - src/lib/process-key-client.ts
  - src/lib/process-key-client.test.ts
  - src/lib/resilient-fetch.wiring.test.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
autonomous: true
requirements: [SEAM-06]
tags: [seam-retry, onboard, idempotency-key, chokepoint, retriesForFlow, wiring-roster]

must_haves:
  truths:
    - "Finding 1 / D-01: the retry verdict for onboard is conditional on the PRESENCE of a usable idempotency key, decided at the shared `postProcessKey` chokepoint — a null/absent/empty `wizard_session_id` yields `retriesOverride: 0`, proven through the REAL call site"
    - "The TS predicate is `Boolean(context.wizard_session_id)` — byte-matching Python's truthiness check — so an empty string agrees on both sides of the seam; a `!== null` implementation reds a pin"
    - "A caller that states nothing fails CLOSED: missing/undefined context yields retries 0 — a future call site cannot silently inherit a retry it has not earned"
    - "The chokepoint call site stays ONE LINE (`retriesForFlow(args.flow_type, args.context)`) and the wiring roster at resilient-fetch.wiring.test.ts:796 is updated in the SAME commit — the [^\\n]* extractor reds on any multi-line expression"
    - "The onboard evidence states the antecedent is now ENFORCED (by the helper), not merely asserted — and the summary does NOT claim this removes live retry volume (prod census: every post-F6 row carries an id; this is class closure)"
  artifacts:
    - path: "src/lib/seam-retry-registry.ts"
      provides: "leaf-pure `retriesForFlow(flowType, context)` helper (zero value imports preserved); updated onboard evidence"
    - path: "src/lib/process-key-client.ts"
      provides: "one-line chokepoint call site delegating to retriesForFlow; rewritten belt docblock"
    - path: "src/lib/process-key-client.test.ts"
      provides: "chokepoint pins: onboard-without-key 0, onboard-empty-string 0, onboard-with-key 1, missing-context 0"
  key_links:
    - from: "src/lib/process-key-client.ts"
      to: "src/lib/seam-retry-registry.ts"
      via: "one-line delegation the wiring roster pins as a source expression"
      pattern: "retriesForFlow\\(args.flow_type, args.context\\)"
    - from: "retriesForFlow"
      to: "process_key.py idempotent_by_session"
      via: "the same truthiness predicate written identically on both sides"
      pattern: "Boolean\\(context.wizard_session_id\\)"
---

<objective>
Close finding 1 (BLOCKER): `onboard`'s retry-safety evidence rests on `idempotent_by_session`
being true, but `finalize-wizard` forwards NO `wizard_session_id` when the draft's column is
NULL (nullable by design), Python then skips the duplicate pre-check and mints a fresh
server-side uuid4 per attempt — so the unique constraint cannot collide and one user submit
inserts two `strategy_verifications` rows on the money path.

D-01 as corrected (CONTEXT correction 3, RESEARCH §5.1, PATTERNS C-1): this is NOT a one-line
call-site change. `PostProcessKeyArgs` has no retry field; the decision is made inside
`process-key-client.ts:502` from `flow_type` alone — a chokepoint with five callers. The fix
threads idempotency-key presence into that shared chokepoint via a named leaf-pure helper in
the registry, with a ONE-LINE call site: the guard is co-located with the evidence whose
antecedent it enforces, it covers the CLASS (the registry itself names a second onboard
producer, `keys/validate-and-encrypt`, safe today only by a budget-row pin plus a dated
dormancy claim), and the predicate can be made byte-identical to Python's.

Honest framing (RESEARCH §0.5): prod census shows the NULL path carries ~zero live traffic —
every post-F6 producer stamps the id and there are zero drafts. D-01 is CLASS CLOSURE — it
converts an asserted antecedent into an enforced predicate. The summary must not claim it
prevents duplicate writes happening today.

Wave 2, depends on plan 02: the helper reads the post-D-03 registry (onboard is the only YES
flow), plan 02 owns the registry files and the process-key-client.test.ts harness in wave 1.

Purpose: finding 1 (D-01). SC-K, SC-L.
Output: an enforced key-presence predicate at the five-caller chokepoint, fail-closed default,
roster updated, both seam sides running the same predicate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-CONTEXT.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-RESEARCH.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-PATTERNS.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `retriesForFlow` in the registry leaf + the one-line chokepoint + the roster, one commit (SC-K)</name>
  <files>src/lib/seam-retry-registry.ts, src/lib/process-key-client.ts, src/lib/resilient-fetch.wiring.test.ts, src/lib/process-key-client.test.ts, src/lib/seam-retry-registry.test.ts</files>
  <read_first>
    - 141.2-RESEARCH.md §5.1 (the three evidenced reasons for the chokepoint placement, the ONE-LINE hard constraint, and the stated tradeoff: the roster shows the helper's NAME, not the verdict arithmetic — acceptable because the helper lives in the registry and gets a direct unit pin; do NOT "solve" it with a 200-character single-line ternary) and the "Code Examples" predicate block (Python `bool(body.context.get("wizard_session_id"))` ⇔ TS `Boolean(context.wizard_session_id)`)
    - src/lib/resilient-fetch.wiring.test.ts — the extractor at :618 (`retriesOverride:\s*([^\n]*)` — line-scoped; a multi-line ternary is captured PARTIALLY and mismatches) and the hand-typed roster `PROCESS_KEY_RETRY` at :796; its docblock stating the multi-line trap
    - src/lib/process-key-client.ts — `PostProcessKeyArgs` (:317-350, note whether `context` is optional), the belt docblock (~:497-501: "the EXPLICIT `?? 0` is load-bearing (the belt)") and the expression at :502; the module's ONE-chokepoint property comment (~:476-479)
    - src/lib/seam-retry-registry.ts — the leaf's zero-VALUE-imports constraint (:5-20, pinned by its test); the onboard evidence (:167-205) naming both producers and the enforced-antecedent framing this task updates
    - src/lib/process-key-client.test.ts — the resilientFetch-spy harness plan 02 added (extend it; do not build a second harness)
  </read_first>
  <behavior>
    - Test 1 (SC-K, real call site): REAL `postProcessKey({ flow_type: "onboard", context: <no wizard_session_id> })` → captured `init.retriesOverride === 0`. RED today (flow_type alone grants 1).
    - Test 2 (predicate byte-match): `context: { wizard_session_id: "" }` → 0. This is the pin that reds a `!== null` implementation — the empty string must agree with Python's truthiness.
    - Test 3 (positive control): `context: { wizard_session_id: "<uuid>" }` → 1. Both polarities, per the oracle-shape law (a route that suppresses retry unconditionally must fail this).
    - Test 4 (SC-L, fail-closed default): context absent/undefined → 0. A caller that states nothing gets NO retry.
    - Test 5 (helper unit pins, in seam-retry-registry.test.ts): `retriesForFlow` returns 0 for teaser/csv/resync regardless of context (registry absence and the NO verdict dominate), 0 for onboard without a truthy key, 1 for onboard with one; the leaf-purity pin (zero value imports) still holds with the helper added.
  </behavior>
  <action>
    RED first: write Tests 1–4 against the current tree (Tests 1, 2, 4 red — flow_type alone
    currently grants onboard 1 regardless of context; Test 3 green). Paste output.

    In `src/lib/seam-retry-registry.ts`: add the exported leaf-pure helper — signature
    `retriesForFlow(flowType: FlowType, context: Record<string, unknown> | undefined): 0 | 1`.
    Logic: if the flow's registry retries resolve to 0 (absence or NO verdict) return 0;
    otherwise return `Boolean(context?.wizard_session_id) ? 1 : 0`. Pure function, no value
    imports, no I/O — honouring the leaf constraint its test pins. Docblock: this is the
    enforcement of the onboard evidence's ANTECEDENT ("a non-NULL wizard_session_id makes
    idempotent_by_session true") — the predicate is written to byte-match the Python side's
    truthiness check, and `!== null` is called out as the wrong shape (diverges on the empty
    string). Update the onboard evidence prose: the antecedent is now ENFORCED by this helper
    at the chokepoint, not merely asserted; the second producer's dormancy claim is no longer
    load-bearing for safety. Per D-01's rejected alternative: minting a client-side stable key
    is the better end state but out of scope (recorded as a follow-up in plan 06's TODOS pass).
    Symbol anchors only; no coordinates in evidence strings (SC-H guard).

    In `src/lib/process-key-client.ts`: replace the expression at :502 with the ONE-LINE call
    `retriesOverride: retriesForFlow(args.flow_type, args.context),` — one line, no ternary,
    no wrapping. Rewrite the belt docblock above it: the belt (`?? 0`) moves inside the helper;
    the docblock now names the helper as the single gate deciding BOTH the flow verdict and the
    key-presence antecedent, and states the fail-closed default (no context ⇒ no retry).

    In `src/lib/resilient-fetch.wiring.test.ts`, SAME commit: update `PROCESS_KEY_RETRY` at
    :796 to the exact new source string `"retriesForFlow(args.flow_type, args.context)"`.
    Any drift between the call site and the roster reds the wiring test — that is the guard
    working; never widen the extractor to accommodate a multi-line expression.

    In `src/lib/process-key-client.test.ts`: extend plan 02's harness with Tests 1–4.
    In `src/lib/seam-retry-registry.test.ts`: add Test 5's helper unit pins (hand-typed
    expectations; do not derive oracles from the maps).
  </action>
  <verify>
    <automated>npx vitest run src/lib/process-key-client.test.ts src/lib/seam-retry-registry.test.ts src/lib/resilient-fetch.wiring.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RED-before-GREEN pasted for Tests 1, 2, 4.
    - MUTATION SC-K (exact, from the VALIDATION ledger): make the chokepoint decide from `flow_type` alone, ignoring key presence (restore the old registry-lookup expression at the call site, roster temporarily matched so ONLY the behavioural pins can catch it) → Tests 1, 2, 4 RED through the REAL call site. Restore → GREEN. Paste both runs.
    - MUTATION SC-L (exact): in the helper, flip the missing-context default from 0 to 1 → Test 4 REDs (fail-closed is pinned, not incidental). Restore → GREEN. Paste both runs.
    - MUTATION (predicate shape): change `Boolean(context?.wizard_session_id)` to `context?.wizard_session_id !== null && context?.wizard_session_id !== undefined` → Test 2 REDs (empty string diverges). Restore → GREEN. Paste both runs.
    - Wiring roster: `npx vitest run src/lib/resilient-fetch.wiring.test.ts` GREEN at the final tree; during SC-K's mutation the roster/call-site relationship was kept consistent so the catch is attributable to the behavioural pins (state this in the log).
    - The call site is one physical line: `grep -c "retriesForFlow(args.flow_type, args.context)" src/lib/process-key-client.ts` → 1 (presence grep).
    - `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>The five-caller chokepoint enforces key presence with the Python-identical predicate; absence fails closed; the roster matches the one-line site; all mutations observed through the real call site.</done>
</task>

<task type="auto">
  <name>Task 2: Route-side polarity check beside the existing null-session fixture</name>
  <files>src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/finalize-wizard/route.test.ts — the TS-33 describe and its ORACLE SHAPE header (~:2683-2686: assert the POSITIVE before any absence), the null-session absence case (~:2804-2831, fixture `wizard_session_id: null` asserting the context omits the key with positive controls), and the `postProcessKey` wholesale mock (~:337-355) capturing args — note this file CANNOT see the retry decision (the seam client is mocked wholesale; that is exactly why Task 1's pins live at the chokepoint)
  </read_first>
  <action>
    Verify both polarities of the route→chokepoint context contract are pinned beside the
    existing TS-33 cases: (a) the null-session draft sends a context WITHOUT
    `wizard_session_id` (exists at ~:2804 — confirm green, do not duplicate); (b) the
    with-session draft sends a context CARRYING the draft's exact session id (the mirror case —
    if it already exists in the describe, reference it; if absent, add it following the
    ORACLE SHAPE law: positive assertions first, hand-built absence second). Add a comment in
    the describe linking the contract to the chokepoint enforcement: the route forwards
    absence as absence; `retriesForFlow` at the chokepoint turns that absence into
    `retriesOverride: 0` (pinned in the seam-client test, which this route test cannot observe
    because it mocks the seam client wholesale). Symbol anchors only.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both polarity cases exist and are GREEN (null-session ⇒ key absent from context; with-session ⇒ key present and equal to the draft's id).
    - No weakening of the existing absence case's positive controls.
    - The cross-reference comment names `retriesForFlow` and the chokepoint pin by symbol.
  </acceptance_criteria>
  <done>The route-side half of the seam contract (context carries exactly what the draft has) is pinned in both directions, and the division of observability between route test and chokepoint test is stated where the next reader will look.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route context → chokepoint retry decision | the retry grant now depends on data a caller supplies (or omits) |
| TS predicate ↔ Python predicate | the same antecedent must evaluate identically on both sides of the seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.2-05 | Elevation (unearned retry) | postProcessKey chokepoint | mitigate | fail-closed default (no key stated ⇒ no retry, SC-L pinned); helper co-located with the evidence; five callers covered by ONE gate |
| T-141.2-11 | Tampering (seam divergence) | Boolean predicate | mitigate | byte-matched truthiness predicate; empty-string divergence pinned (Test 2); the key itself is read owner-scoped from the DB, never from the request body (V3 unchanged) |
| T-141.2-SC | Tampering | package installs | accept | zero package installs in this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/process-key-client.test.ts src/lib/seam-retry-registry.test.ts src/lib/resilient-fetch.wiring.test.ts src/app/api/strategies/finalize-wizard/route.test.ts` green at final tree
- SC-K, SC-L, and the predicate-shape mutation observed RED with pasted output, restored GREEN
- `npm run typecheck` + `npm run lint` clean; Node 22 PATH reproduction
- VALIDATION.md rows SC-K/SC-L flipped to Observed ✅; Per-Task Verification Map Status filled for finding 1
</verification>

<success_criteria>
- Finding 1 closed at the chokepoint: class enforcement, fail-closed default, Python-identical predicate
- All mutations driven through the real call site; roster updated in the same commit as the expression
- Summary frames D-01 as class closure, not live-traffic reduction
</success_criteria>

<output>
Create `.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-05-SUMMARY.md` when done. Include the three RED outputs and the explicit statement: "Using the chokepoint helper per RESEARCH §5.1; CONTEXT D-01's original one-call-site wording was corrected by the pattern-mapper (C-1) — the chokepoint serves D-01's spirit and closes the class."
</output>
