---
phase: 141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seam-retry-registry.ts
  - src/lib/seam-retry-registry.test.ts
  - src/lib/seam-constants.pin.test.ts
  - src/lib/process-key-client.test.ts
  - analytics-service/routers/process_key.py
autonomous: true
requirements: [SEAM-06]
tags: [seam-retry, resync, registry, map-move, evidence-strings, python-fence]

must_haves:
  truths:
    - "Finding 6 / D-03: `resync` carries a NO verdict — it lives in `RETRY_AUDIT_NO_FLOW_TYPES` with evidence that cites by SYMBOL only, and a real `postProcessKey({flow_type:'resync'})` call sends `retriesOverride: 0` to `resilientFetch`"
    - "The four broken pins are rewritten in the SAME commit as the move: `EXPECTED_SAFE_FLOW_KEYS` is `['onboard']`; the D-05 resync-evidence test reads the NO map (no `.not.toMatch` on `undefined` throw); the deep-freeze pin covers resync's NO entry; the evidence-count fence is per-map hand-typed literals whose breakdown CAN red, not just a total that stays 13"
    - "`seam-constants.pin.test.ts` map sizes read 4 / 1 / 3 / 5 and the OQ-3 resync pin asserts the NO verdict; the rows-at-1 === 5 accounting pin is verified unchanged with the reason stated (onboard still consumes the process-key-enqueue row)"
    - "The `process_key.py` resync pre-check comment no longer claims to close a SEQUENTIAL retry class that D-03 removes — comment-only edit, fence-opening reason stated"
  artifacts:
    - path: "src/lib/seam-retry-registry.ts"
      provides: "resync entry moved from RETRY_SAFE_FLOW_TYPES to RETRY_AUDIT_NO_FLOW_TYPES as a symbol-anchored evidence string"
    - path: "src/lib/seam-retry-registry.test.ts"
      provides: "rewritten key-set, D-05 evidence, deep-freeze and per-map-literal count pins"
    - path: "src/lib/process-key-client.test.ts"
      provides: "resync-through-real-chokepoint pin (retries 0) built on the file's EXISTING coreSpy harness — no second mock of resilient-fetch"
  key_links:
    - from: "src/lib/process-key-client.ts"
      to: "src/lib/seam-retry-registry.ts"
      via: "the unchanged chokepoint expression resolves resync to 0 by registry ABSENCE (the `?? 0` belt)"
      pattern: "RETRY_SAFE_FLOW_TYPES\\[args.flow_type\\]\\?\\.retries \\?\\? 0"
---

<objective>
Close finding 6: 141.1-02 rewrote the `resync` evidence to admit the SEQUENTIAL-retry class
is NOT closed (the worker's 30s tick advances SV#1 out of `status='draft'` during the
250–500 ms backoff, so attempt 2's pre-check matches nothing and inserts SV#2), yet left the
`retries: 1` grant that was issued on the strength of the deleted sentence. D-03: the verdict
must follow its own evidence — `resync` becomes a NO verdict until a durable idempotency key
exists.

This is a MAP MOVE, not a field edit (RESEARCH §0.6): `RetrySafeEntry.retries` is the
literal type `1` by deliberate design, so `resync` cannot carry `retries: 0` in the YES map.
Four named pins break, one of which THROWS rather than fails. The chokepoint expression at
`process-key-client.ts` needs NO change in this plan — resync resolves to 0 by registry
absence via the `?? 0` belt (plan 05 later replaces that expression for D-01; do not touch it
here).

Founder-visible consequence to surface in the SUMMARY, not bury: combined with D-01 (plan 05),
retry is effectively enabled only for `onboard` WITH a session id — 141's retry rollout was
broader than its evidence supported. Prod traffic note (RESEARCH §6): resync is 20 of 42
all-time `/process-key` audit rows, so this removes 100% of the dominant retry volume; D-01
removes ~0.

Purpose: finding 6 (D-03), plus the DEF-141.1-02-A carried deferral. SC-D, SC-E.
Output: a registry whose resync verdict matches its own evidence, all pins moved in the same
commit, and a truthful Python-side comment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-CONTEXT.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-RESEARCH.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-PATTERNS.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The map move + every pin that breaks, one commit (SC-D, SC-E)</name>
  <files>src/lib/seam-retry-registry.ts, src/lib/seam-retry-registry.test.ts, src/lib/seam-constants.pin.test.ts, src/lib/process-key-client.test.ts</files>
  <read_first>
    - 141.2-RESEARCH.md §5.2 — the pin-breakage table with per-pin remedies, and the evidence-string constraint
    - src/lib/seam-retry-registry.ts — the resync YES entry (:206-232), the NO map (see how `teaser`/`csv` entries are shaped: NO entries ARE prose strings), the RetrySafeEntry literal `retries: 1` type (:127)
    - src/lib/seam-retry-registry.test.ts — `EXPECTED_SAFE_FLOW_KEYS` (:44 and the set-equality pin ~:151-154), the `it.each` three-status qualification (~:323-337), the `[D-05] resync does NOT claim the SEQUENTIAL class is closed` pin (~:311-320 — this one THROWS after the move: `.not.toMatch` on a non-string received value is a matcher error), the entry-level deep-freeze pin (~:509), the SC-H no-coordinate guard (:424-458), and `EXPECTED_EVIDENCE_STRING_COUNT = 2 + 4 + 2 + 5` at :402 with its breakdown message at ~:432
    - src/lib/process-key-client.test.ts — the EXISTING `coreSpy` harness at ~:20-37: it already mocks `@/lib/resilient-fetch` via the importOriginal pattern, with `coreSpy` wrapping the real core (reset + re-wired in `beforeEach`). Test 1 builds ON this harness. Do NOT add a second `vi.mock("@/lib/resilient-fetch", …)` in this file — two mocks of the same module in one test file is a mock conflict.
    - src/lib/seam-constants.pin.test.ts — the four hand-typed map sizes (~:385-433), the OQ-3 pin asserting `RETRY_SAFE_FLOW_TYPES.resync?.retries` is 1 (~:454-512, esp. ~:501-506 — reds by construction after the move), the rows-at-1 === 5 pin (~:339-365)
    - src/lib/process-key-client.ts — `postProcessKey` and the retriesOverride expression at :502 (READ ONLY — do not modify in this plan; plan 05 owns it)
  </read_first>
  <behavior>
    - Test 1 (SC-D, REAL call site): in `src/lib/process-key-client.test.ts`, REUSE the file's existing `coreSpy` harness (~:20-37) — it already spies on `resilientFetch` while delegating to the real core. Drive the REAL `postProcessKey({ flow_type: "resync", … })` and assert the captured `init.retriesOverride === 0` (read it from `coreSpy.mock.calls`; stub `coreSpy.mockImplementation` to a resolved response for this case so no live network is needed). Positive control in the same case: `flow_type: "onboard"` still yields `1` (this plan does not change onboard; plan 05 will extend these pins). A helper-level registry test cannot see this — the defect class is seam-crossing; the pin must cross the chokepoint.
    - Test 2 (NO-verdict pin): `RETRY_AUDIT_NO_FLOW_TYPES.resync` exists, is a string, and its prose admits the open window (matches on the symbols it cites, e.g. the draft-status pre-check and the worker transition); `RETRY_SAFE_FLOW_TYPES` has no `resync` key.
    - Test 3 (SC-E, count fence): the evidence-string anti-vacuity fence is restructured into per-map HAND-TYPED literals — flow-YES 1, analytics-YES 4, flow-NO 3, analytics-NO 5 — each asserted against its own map's actual population, with the total derived from those four literals. The breakdown itself can now red; a total-only `.toBe(13)` cannot (2+4+2+5 and 1+4+3+5 are both 13 — that arithmetic coincidence is exactly how the current pin's message goes false while staying green).
  </behavior>
  <action>
    RED first where expressible: write Test 1 against the current tree — it REDs (resync
    currently yields `retriesOverride: 1`). Paste the output. Tests 2–3 are written with the move.

    In `src/lib/seam-retry-registry.ts`: MOVE the `resync` entry out of `RETRY_SAFE_FLOW_TYPES`
    into `RETRY_AUDIT_NO_FLOW_TYPES` as an evidence string, matching the NO-map's existing shape
    (NO entries ARE the prose). The new NO evidence must:
    - state the verdict follows its own evidence: the SEQUENTIAL window is real (the compute
      worker's 30-second tick advances the draft verification out of draft status during the
      backoff, so attempt 2's strategy-scoped pre-check matches nothing and inserts a second row);
    - state the re-enable condition: a durable idempotency key for resync, which does not exist;
    - cite ONLY by symbol — `_resume_duplicate_job`, `compute_jobs_one_inflight_per_kind_strategy`,
      the `status='draft'` pre-check — NEVER `file.ext:NN`: the SC-H guard at
      `seam-retry-registry.test.ts:424-458` reds on any coordinate inside an evidence string;
    - use the docblock-correction idiom: name the deleted grant, say the evidence never supported
      it after the 141.1-02 rewrite, state what is true now.
    Do NOT touch the onboard entry, the analytics maps, or `process-key-client.ts`.

    In `src/lib/seam-retry-registry.test.ts`, SAME commit:
    - `EXPECTED_SAFE_FLOW_KEYS` → `["onboard"]` (hand-typed);
    - confirm the `it.each` three-status qualification still passes over the one remaining key
      (onboard's evidence matches the pending/running/done_pending_children pattern — verify,
      don't assume);
    - REWRITE the `[D-05] resync does NOT claim the SEQUENTIAL class is closed` pin to read
      `RETRY_AUDIT_NO_FLOW_TYPES.resync` (avoiding the `.not.toMatch`-on-`undefined` matcher
      throw), asserting the NO evidence does not claim the class is closed AND does claim the
      window is open (both polarities);
    - move the resync deep-freeze pin to the NO-map freeze handling (check how `teaser`/`csv`
      are frozen there — NO entries are strings);
    - restructure `EXPECTED_EVIDENCE_STRING_COUNT` per Test 3: four per-map hand-typed literals
      (1, 4, 3, 5), each pinned against its own map, total derived from the four literals, and
      the failure message re-spelled to match. The exhaustiveness (YES∪NO) and disjointness
      (YES∩NO=∅) pins should pass unchanged — verify.

    In `src/lib/seam-constants.pin.test.ts`, SAME commit:
    - the four hand-typed map sizes → 4 / 1 / 3 / 5;
    - the OQ-3 pin (~:501-506) currently asserts `RETRY_SAFE_FLOW_TYPES.resync?.retries` is 1 —
      reds by construction; rewrite it to assert the post-D-03 truth: resync is ABSENT from the
      YES map and PRESENT in the NO map (oracle stays hand-typed, no derivation from the module);
    - the rows-at-1 === 5 pin and `EXPECTED_RETRIES` pins: verify UNCHANGED and state why in a
      comment — `SEAM_BUDGETS[*].retries` is pure accounting post-D-08; the `process-key-enqueue`
      row stays 1 because `onboard` (still a YES flow) consumes it; the registry override is the
      only thing that turns retry on. Verify, don't assume — run the file.
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-retry-registry.test.ts src/lib/seam-constants.pin.test.ts src/lib/process-key-client.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RED-before-GREEN pasted for Test 1 (resync through the REAL `postProcessKey` currently sends retries 1).
    - Test 1 is built on the EXISTING `coreSpy` harness — reviewed in the diff: no second `vi.mock` of `@/lib/resilient-fetch` was added to `process-key-client.test.ts`.
    - MUTATION SC-D (exact, from the VALIDATION ledger): move `resync` back into `RETRY_SAFE_FLOW_TYPES` with `retries: 1` (and out of the NO map) → REDs: the chokepoint pin (Test 1), `EXPECTED_SAFE_FLOW_KEYS` set equality, the NO-verdict pin, and the seam-constants map-size pins. Restore → GREEN. Paste both runs.
    - MUTATION SC-E (exact, as reconciled in the VALIDATION ledger — a sanctioned test-side mutation, see the ledger's SC-E exception note): revert the count fence's per-map literals to the pre-move spelling (flow-YES 2, flow-NO 2) while leaving the registry moved → the per-map assertions RED (this is the pin the old total-only `.toBe(13)` could not fail). Restore → GREEN. Paste both runs.
    - The SC-H no-coordinate guard GREEN over the new evidence string (proves symbol-only citation), and its own +/- self-tests untouched.
    - No edit to `src/lib/process-key-client.ts` (`git diff --name-only` for this task's commit excludes it).
    - `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>resync's verdict matches its evidence; every dependent pin moved in the same commit; the count fence can now fail on the exact drift class it fences; the real chokepoint provably sends retries 0 for resync.</done>
</task>

<task type="auto">
  <name>Task 2: Open the Python fence for the comment-only DEF-141.1-02-A correction</name>
  <files>analytics-service/routers/process_key.py</files>
  <read_first>
    - 141.2-RESEARCH.md Open Question 3 — the fence-opening recommendation and its required stated reason
    - analytics-service/routers/process_key.py — the resync draft pre-check region (~:1400) and its SCOPE BOUND comment carrying the over-claim D-05 (141.1) corrected on the TS side but not here (DEF-141.1-02-A, carried in deferred-items)
  </read_first>
  <action>
    FENCE OPENING, stated per the scope fence's own requirement: Python is edited because a
    finding cannot otherwise be fully closed — prose moves with code, and D-03 makes the
    `process_key.py` resync pre-check comment doubly wrong: it claims the pre-check "closes the
    SEQUENTIAL retry class", and after D-03 it describes a TS-side retry that no longer exists.
    This is a COMMENT-ONLY edit; zero behavior change; it discharges DEF-141.1-02-A instead of
    carrying it a third time.

    Rewrite the comment using the docblock-correction idiom: the pre-check remains as
    defense-in-depth against duplicate submits, but it never closed the SEQUENTIAL class (the
    worker tick can advance the draft between attempts), and as of 141.2/D-03 the TS seam issues
    no resync retry at all — the registry carries the NO verdict. Cite by symbol, no
    file/line coordinates, no derivable integers.
  </action>
  <verify>
    <automated>python3 -m py_compile analytics-service/routers/process_key.py && git diff --stat analytics-service/routers/process_key.py</automated>
  </verify>
  <acceptance_criteria>
    - `git diff analytics-service/routers/process_key.py` shows ONLY comment/docstring lines changed (every changed line is a `#` comment or inside a docstring); zero executable-statement changes.
    - `python3 -m py_compile` exit 0.
    - The fence-opening reason is recorded verbatim in the SUMMARY (the fence requires the reason to be stated explicitly).
    - deferred-items / TODOS bookkeeping: DEF-141.1-02-A recorded as discharged by this task (the closing edit itself lives here; the tracker line update may ride plan 06's TODOS pass if editing TODOS.md here would collide with plan 01 in this wave — do NOT edit TODOS.md in this plan).
  </acceptance_criteria>
  <done>The Python-side comment states the truth about both the pre-check's actual guarantee and the post-D-03 retry surface; the fence opening is justified on the record; no behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry verdict → transport | the registry's YES/NO maps are the sole input that can enable a seam retry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.2-03 | Elevation (unaudited retry) | `RETRY_SAFE_FLOW_TYPES` / resync | mitigate | NO verdict enforced by map membership; literal `retries: 1` type makes a quiet re-grant a type change; chokepoint pin drives the REAL call site |
| T-141.2-07 | Repudiation (evidence drift) | registry evidence strings + count fence | mitigate | per-map hand-typed literals replace the total-only fence; SC-H coordinate guard covers the new string |
| T-141.2-SC | Tampering | package installs | accept | zero package installs in this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/seam-retry-registry.test.ts src/lib/seam-constants.pin.test.ts src/lib/process-key-client.test.ts` green at final tree
- SC-D and SC-E mutations observed RED with pasted output, restored GREEN
- `npm run typecheck` + `npm run lint` clean; reproduce test runs under Node 22 PATH before declaring stable
- Python diff comment-only; `py_compile` clean
- VALIDATION.md rows SC-D/SC-E flipped to Observed ✅; Per-Task Verification Map Status filled for finding 6
</verification>

<success_criteria>
- Finding 6 closed: resync is a NO verdict whose evidence matches its grant, proven through the real chokepoint
- All four broken pins rewritten in the same commit; no matcher-error throw; count fence falsifiable
- DEF-141.1-02-A discharged with the fence-opening reason on the record
</success_criteria>

<output>
Create `.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-02-SUMMARY.md` when done. Include: SC-D/SC-E RED output; the founder-visible scope statement (retry now effectively onboard-only, pending plan 05's key-presence condition — resync was 48% of all-time `/process-key` traffic); and the fence-opening reason.
</output>
