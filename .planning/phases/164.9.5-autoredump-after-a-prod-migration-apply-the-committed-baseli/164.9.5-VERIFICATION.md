---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
verified: 2026-09-26T22:30:00Z
status: human_needed
score: 4/6 must-haves verified (2 present and wired, live behaviour unverified until after merge)
verified_at_sha: c505c2030882ddf916a2686c6b900e40984238d1
drift_subjects:
  - .github/workflows/supabase-migrate.yml
  - .gitleaks.toml
  - scripts/baseline-redump.mjs
  - src/__tests__/baseline-redump-wiring.test.ts
  - src/__tests__/supabase-migrate-test-first.test.ts
  - src/__tests__/critical-regressions.test.ts
  - src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts
  - supabase/schema/BASELINE.md
behavior_unverified: 2
overrides_applied: 0
behavior_unverified_items:
  - truth: "SC1: a workflow runs after supabase-migrate.yml's PROD apply job succeeds and takes a read-only schema dump with the existing repository secret"
    test: "After merge, dispatch supabase-migrate.yml on main (D-18). apply-test and apply apply zero migrations, then redump-dump runs end to end"
    expected: "redump-dump is green. If the dump is byte-identical it prints the D-10 no-op notice with changed=false and redump-pr is skipped; otherwise its diff is the first bot PR"
    why_human: "The needs:/if:/environment:/permissions: contract is pinned by the wiring test, but the real supabase link + db dump against PROD, the ghcr login and the sha256-pinned gitleaks install only run on a GitHub runner after merge. No agent may run a database command against a remote"
  - truth: "SC5: it opens a PR carrying the VERSION bump, the CHANGELOG entry and the BASELINE.md provenance, and never auto-merges"
    test: "On the first changed re-dump after the D-30 setting is ON, confirm redump-pr pushes automation/baseline-redump and gh pr create succeeds; then click Approve workflows to run (D-23)"
    expected: "One bot PR with exactly six changed paths, every head-sha run completed with conclusion success, and no merge by automation"
    why_human: "Composition was proved offline end to end (six paths committed as github-actions[bot], version gate clean). The live gh pr create cannot succeed today: the repository setting it needs was measured OFF at verification time"
human_verification:
  - test: "D-30 (owner: founder, before or right after merge). Enable Settings > Actions > General > 'Allow GitHub Actions to create and approve pull requests', then re-read it with gh api repos/{owner}/{repo}/actions/permissions/workflow -q .can_approve_pull_request_reviews"
    expected: "true"
    why_human: "Founder-only repository setting. Measured false by this verifier at 2026-09-26. Consequence while it stays false: the first merge touching supabase/migrations/** after this phase lands turns redump-pr red on main at gh pr create, and Railway skips the analytics deploy while main is red. This phase's own merge touches no migration, so it does not trigger the path itself"
  - test: "D-18 (owner: founder or orchestrator, post-merge). gh workflow run supabase-migrate.yml --ref main, then bind the reading to the run's head sha and read every job's conclusion"
    expected: "apply-test and apply apply zero migrations; redump-dump green with the D-10 no-op notice (changed=false) and redump-pr skipped, or a diff that becomes the first bot PR and is read hunk by hunk. Only this run may justify narrowing the BASELINE.md Version-skew caveat"
    why_human: "Post-merge live run against PRODUCTION credentials on a GitHub runner; cannot run pre-merge and no agent may touch a remote database"
  - test: "D-23 (owner: founder, on the first bot PR). Click 'Approve workflows to run', then list the head-sha runs with name, status and conclusion"
    expected: "Every run completed with conclusion success before merge. Branch protection is off, so a merge with zero completed checks is possible and must not happen. Close-and-reopen only if no approval banner appears"
    why_human: "A human click in the GitHub UI on a PR that does not exist until the first changed re-dump"
---

# Phase 164.9.5: AUTOREDUMP Verification Report

**Phase Goal:** After a migration applies to PROD, the committed baseline is re-dumped and proposed automatically, so main never sits red on baseline-content-drift waiting for a manual dump.
**Verified:** 2026-09-26T22:30:00Z at `c505c2030` (branch `feat/164.9.5-autoredump`)
**Status:** human_needed
**Re-verification:** No. This is the initial verification; no earlier VERIFICATION.md existed.

## Goal Achievement

The code that delivers the goal exists, is wired and was exercised by this verifier. A changed
fixture dump went through the real `--gate-dump` (with real gitleaks 8.30.1) and then the real
`--compose` (with the real `run.sh --check-currency`, `baseline-content-drift-check.mjs` and
`check-baseline-staleness.mjs`). It produced one bot commit of exactly six paths, and the repo's
own version gate accepted that commit. What cannot be verified before merge is the live half:
the PROD dump on a runner and the live `gh pr create`. The second is also blocked today by a
repository setting that was measured OFF (D-30).

### Observable Truths

| # | Truth (ROADMAP success criterion) | Status | Evidence |
|---|---|---|---|
| 1 | Trigger and dump: a workflow runs after `apply` succeeds and takes a read-only dump with the existing secret; no new credential | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `redump-dump` has `needs: [apply]` and `if: needs.apply.result == 'success' && github.ref == 'refs/heads/main'`, `environment: Production`, and the same three values as `apply` (two secrets and one var) in step `env:` only. The dump is `supabase link` + `supabase db dump --linked -f` into `$RUNNER_TEMP` on setup-cli 2.98.2 after a ghcr login. The wiring test pins all of this, and actionlint is clean. The live run is post-merge (human item D-18). |
| 2 | Scan and refuse: the five-class secret scan and gitleaks run, and any hit refuses before a PR | ✓ VERIFIED | `gateDump` runs `runGitleaksGate` (target assertion, then `gitleaks dir --config .gitleaks.toml --redact --ignore-gitleaks-allow`), then `judgeSecretScan`. Both throw before the out dir is written. `compose` re-scans with the node judges. Spot-check: a planted JWT-shaped line exits 1, prints the hit count and line number only, the planted text appears 0 times in the log, and no out dir is written. Self-test arms (226, and 235 with gitleaks) cover every class. |
| 3 | Carried-migrations marker regenerated from the tree of the applied merge | ✓ VERIFIED | `gateDump` refuses when `--merge` is not HEAD, lists `git ls-tree --name-only <merge> supabase/migrations/`, validates each basename against `MIGRATION_BASENAME_STRICT_RE`, and builds the marker with `buildMarker` (header kept, `baseline-sha256:` recomputed, byte-sorted). Spot-check: carried 277, and the composed tree reads `marker-sha=match defects=0`. |
| 4 | Currency checks: `run.sh --check-currency` and baseline-content-drift run | ✓ VERIFIED | `compose` runs `bash scripts/local-stack/run.sh --check-currency`, the drift check's `--self-test`, then the drift check itself, and judges each by its verdict line (`judgeCurrencyLine`, `judgeDriftLine`, `captureLine`). Spot-check with the real runner: `baseline-currency: carried=277 replay=0 marker-sha=match defects=0` and `baseline-content-drift: findings 0`. |
| 5 | The PR: VERSION bump, CHANGELOG entry and BASELINE.md provenance; never auto-merges | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Offline composition is proved. The spot-check committed exactly the six `STAGED_PATHS` as `github-actions[bot]`, with VERSION 0.96.0.1 → 0.96.0.2 byte-equal in package.json. The CHANGELOG entry carries the measured values, and BASELINE.md's `## Provenance` rows plus a `### Regenerated` section were written. `check-version-bump.mjs` against the pre-compose base reported no defects, and `check-baseline-staleness.mjs` reported no defects. `buildPrArgv` can return only `pr create` or `pr edit`, and the wiring test asserts no merge, auto-merge or approval on any live line. The live `gh pr create` is blocked until D-30 is ON (measured `false`), so it is human items D-30 and D-23. |
| 6 | Credential handling: never echoed, least-privilege `permissions:`, ref-guarded to `main` | ✓ VERIFIED | Workflow default `contents: read`. `redump-dump` is exactly `{contents: read, packages: read}` and `redump-pr` is exactly `{contents: write, pull-requests: write}`, so the PROD credential and the write token never share a job (D-21). No `run:` body in either job holds a `${{ }}` expression. Both checkouts use `persist-credentials: false`. There is one `git push`, to a hard-coded `HEAD:refs/heads/automation/baseline-redump` with a lease. Both `if:` clauses require `refs/heads/main`. All of this is pinned by the wiring test, which passed. |

**Score:** 4/6 truths verified (2 present and wired, with live behaviour unverified until after merge)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `scripts/baseline-redump.mjs` | gate, compose, bot-branch check, PR open/edit, self-test | ✓ VERIFIED | 3727 lines. Five CLI modes, and an unknown flag exits 1. The self-test prints 226 assertions (235 with gitleaks), equal to `EXPECTED_ASSERTIONS` and `EXPECTED_GITLEAKS_ASSERTIONS`. |
| `.github/workflows/supabase-migrate.yml` jobs `redump-dump`, `redump-pr` | two jobs, credential split, placed before `apply:` | ✓ VERIFIED | Job order: divergence verdict < `redump-dump` < `redump-pr` < `apply`. actionlint exit 0. |
| `src/__tests__/baseline-redump-wiring.test.ts` | calibrated wiring pins | ✓ VERIFIED | Passed. See spot-checks. |
| `.gitleaks.toml` rule `baseline-dump-password` | gitleaks copy of the password class | ✓ VERIFIED | Behaviourally paired with `SECRET_SCAN_PATTERN` by the `--with-gitleaks` arms (IN-10 records the missing byte pin). |
| `supabase/schema/BASELINE.md` `## Regenerating` automation paragraph | D-19 docs | ✓ VERIFIED | The "AUTOMATED 2026-09-26" paragraph names both jobs, the refusals, the never-merged PR and the manual fallback. |
| `TODOS.md` `[164.9.5-MANUAL-BASELINE-REDUMP]` | closed with evidence | ✓ VERIFIED | `- [x]`, "CLOSED 2026-09-26 by Phase 164.9.5 AUTOREDUMP", with the lineage kept. |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `apply` | `redump-dump` | `needs: [apply]` + `needs.apply.result == 'success'` | WIRED |
| `redump-dump` gate step | `redump-pr` | `outputs.changed` from `steps.gate.outputs.changed`, emitted by `emitToGithubOutput` to `GITHUB_OUTPUT` | WIRED |
| `redump-dump` | `redump-pr` | `baseline-redump` artifact (1 day), uploaded only on `changed == 'true'` after every gate; downloaded then re-hashed against `measured.json` | WIRED |
| `compose` | push/PR step | `steps.compose.outputs.committed == 'true'` gates `--check-bot-branch` and the push step; lease from `steps.botbranch.outputs.lease` | WIRED |
| script `SECRET_SCAN_PATTERN` | BASELINE.md `grep -anE` | byte equality, pinned by the wiring test | WIRED |
| workflow gitleaks pin | `ci.yml` `GITLEAKS_VERSION` + script `GITLEAKS_VERSION` | all 8.30.1, pinned by the wiring test | WIRED |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Real data | Status |
|---|---|---|---|---|
| bot commit (six paths) | dump, marker, measured values | PROD dump (runner) → `gateDump` → artifact → `compose` | Proved with a fixture dump derived from the committed baseline; the PROD source is live-only | ✓ FLOWING (offline); live source is human item D-18 |
| CHANGELOG / BASELINE.md figures | run id, merge sha, sha256 prefixes, shape counts, verbatim gate lines | `measured.json` and the child gates' stdout | Yes. The composed entry quoted the real gate lines verbatim. | ✓ FLOWING |

### Behavioral Spot-Checks

Run by this verifier at `c505c2030`. The end-to-end rows ran in a `git clone --shared` of the
worktree in the session scratchpad, whose `origin` pointed at itself, so nothing reached a remote.
The clone was deleted afterwards.

| Behavior | Result | Status |
|---|---|---|
| `LC_ALL=C node scripts/baseline-redump.mjs --self-test` | `self-test OK: 226 assertion(s)`, exit 0 | ✓ PASS |
| `... --self-test --with-gitleaks` (local gitleaks 8.30.1) | `self-test OK: 235 assertion(s) (with gitleaks)`, exit 0 | ✓ PASS |
| vitest: `baseline-redump-wiring`, `supabase-migrate-test-first`, `critical-regressions`, `ghcr-login-before-image-pull.contract` (temporary `node_modules` symlink, removed afterwards) | 4 files, 232 tests passed | ✓ PASS |
| `actionlint .github/workflows/supabase-migrate.yml` | exit 0, no findings | ✓ PASS |
| `--gate-dump` on the committed dump with one comment line appended | gitleaks 0, secret-scan 0, integrity clean, main-listing equal, completeness 6/6, tables 63, policies 155, carried 277, `changed=true`, out dir with 3 files | ✓ PASS |
| `--compose` on that artifact with the real runner | currency `marker-sha=match defects=0`, drift `findings 0`, VERSION 0.96.0.1 → 0.96.0.2, staged 6, `committed=true`, author github-actions[bot], pr-title and pr-body written | ✓ PASS |
| `check-version-bump.mjs` against the pre-compose base | `changed=6`, no defects | ✓ PASS |
| D-10 re-run of `--gate-dump` on the same dump after the compose commit | `changed=false`, no-op notice, no out dir | ✓ PASS |
| `--gate-dump` on a dump with a planted JWT-shaped line | exit 1, `1 hit(s) at line(s) <n>`, the planted text appears 0 times in the log, no out dir | ✓ PASS |
| Live `redump-dump` / `redump-pr` on a runner | not runnable before merge | ? SKIP → human items |

### Probe Execution

No phase-declared probes. None of the nine plans names a `probe-*.sh`, and there are 0 files at `scripts/*/tests/probe-*.sh`.

### Requirements Coverage

| Requirement | Source | Status | Evidence |
|---|---|---|---|
| TODOS `[164.9.5-MANUAL-BASELINE-REDUMP]` | ROADMAP `### Phase 164.9.5` (TODOS-owned; 0 hits in REQUIREMENTS.md, so not orphaned) | ✓ SATISFIED (code), live proof pending | Closed in TODOS.md with evidence. The automation reproduces BASELINE.md `## Regenerating` mechanically. |

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX` was added by the phase diff. No CI skip token appears in the workflow or in BASELINE.md.

| File | Finding | Severity | Impact |
|---|---|---|---|
| `supabase/schema/BASELINE.md`, CONTEXT.md | WR-11: the truncation limit names the wrong cut line (a cut before the first `CREATE TABLE` is refused by `judgeShapeCounts`), and it overstates the reviewer's signal (a cut in the `GRANT` tail changes no shape count in the PR body) | ⚠️ Warning (recorded limit, D9) | Wording only. Every re-dump still reaches a human-reviewed PR, never `main` directly. |
| `scripts/baseline-redump.mjs` `judgeCompleteness` docstring | IN-08: it contradicts itself about truncation | ℹ️ Info (recorded limit, D9) | Doc only |
| `.github/workflows/supabase-migrate.yml`, the `A RED HERE IS DELIBERATE` comment above `redump-dump:` | IN-09: still lists "a truncated dump" among the refusals | ℹ️ Info (recorded limit, D9) | Comment only. It is the one recorded limit inside a phase-modified file. |
| `scripts/baseline-redump.mjs`, `.gitleaks.toml` | IN-10: spaced password negatives are script-only; the gitleaks rule is paired by behaviour, not bytes | ℹ️ Info (recorded limit, D9) | Coverage gap, not a defect |
| carried | WR-01 (compose onto current main in the window between the two jobs), WR-04 (concurrency group held longer), WR-05 (anonymous open-PR lookup rate limit, fails loud), IN-01, IN-02, IN-05, IN-06, IN-07 | ⚠️/ℹ️ (recorded limits, D9) | Each fails loud or is human-gated. The round-4 review reports 0 CRITICAL / 0 HIGH. |
| CONTEXT "Recorded limits after D-33" | truncation (MEDIUM); an out-of-band PROD extension drop reddens `redump-dump` until a hand re-dump (MEDIUM, loud); compact `password='';` is a scan hit (LOW) | recorded limits | Not verification gaps (founder decision D-33) |

### Human Verification Required

#### 1. D-30: repository setting (owner: founder, before or right after merge)

**Test:** Enable "Allow GitHub Actions to create and approve pull requests", then run `gh api repos/{owner}/{repo}/actions/permissions/workflow -q .can_approve_pull_request_reviews`.
**Expected:** `true`.
**Why human:** This is a founder-only setting. **This verifier measured it as `false` on 2026-09-26.** While it stays off, the first merge touching `supabase/migrations/**` after this phase lands turns `redump-pr` red on `main` at `gh pr create`, and Railway skips the analytics deploy. This phase's own merge touches no migration, so it does not fire the path.

#### 2. D-18: post-merge dispatch (owner: founder or orchestrator, after merge)

**Test:** `gh workflow run supabase-migrate.yml --ref main`, then bind the reading to the head sha and read every job's conclusion.
**Expected:** Zero migrations applied. `redump-dump` is green with the D-10 no-op (`changed=false`) and `redump-pr` is skipped, or it produces a diff that becomes the first bot PR and is read hunk by hunk. If the push is refused, use the RESEARCH F10 extraheader fallback.
**Why human:** It is a live run with PROD credentials on a GitHub runner.

#### 3. D-23: first bot PR (owner: founder)

**Test:** Click "Approve workflows to run", then list the head-sha runs with name, status and conclusion.
**Expected:** All runs `completed` / `success` before merge. Branch protection is off, so a zero-check merge is possible and must not happen.
**Why human:** A UI click on a PR that exists only after the first changed re-dump.

### Gaps Summary

There are no code gaps. Every roadmap success criterion is implemented and wired. Criteria 2, 3,
4 and 6 were proved by the self-test, the wiring test and this verifier's own end-to-end fixture
run. Criteria 1 and 5 are proved statically and offline, but their live behaviour (the PROD dump
on a runner, and the live PR creation) cannot run before merge. Criterion 5's live path also
depends on the founder enabling the D-30 setting, which was measured OFF. The status is therefore
`human_needed`, not `passed`. Recorded limits WR-11 and IN-06..IN-10, the carried round-1/2
items, and the three CONTEXT limits after D-33 are founder-accepted limits under D9, not gaps.

---

_Verified: 2026-09-26T22:30:00Z_
_Verifier: Claude (gsd-verifier)_
