---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
plan: 07
type: execute
wave: 3
depends_on: ["164.9.5-02"]
files_modified:
  - scripts/baseline-redump.mjs
autonomous: true
requirements:
  - "164.9.5-MANUAL-BASELINE-REDUMP"

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low
  # Added 2026-09-26 (checker round 1, scope_sanity): the compose expansion split out of plans 01
  # and 03. Plan numbers are not execution order; `wave` is.

must_haves:
  truths:
    - "The bot's VERSION/package.json/CHANGELOG output can never fail CI's `version-gate`: the three defects of `scripts/check-version-bump.mjs` `judge()` are mirrored and refused before commit, VERSION is a 4-digit string with no trailing newline, and package.json is edited by a single-occurrence replace (per D-15)."
    - "`rewriteBaselineMd` counts and replaces each of the four capture rows EXACTLY once INSIDE `provenanceSpan` (from `## Provenance` to the first following heading at any level) and refuses on zero or two matches THERE; the four `| Shape |` and five `| sha256 |` rows that exist file-wide outside the span are neither counted nor written (per D-16)."
    - "The same writer, run over the REAL committed `supabase/schema/BASELINE.md` in the self-test (read-only), returns no defect, leaves exactly one `RECORDED_SHA_RE_ALL` match equal to the new sha, and leaves every line outside the capture rows and the inserted section byte-unchanged."
    - "The CHANGELOG entry and the dated `### Regenerated` section carry EVERY D-16 measured value, each pinned by a presence arm that has been observed RED: the applying run id, the merge short sha, the old and new sha256 prefixes, the old and new shape counts (tables, policies, function statements, distinct function names, data statements), each newly carried migration basename (or the measured statement that none was), and the verbatim `baseline-currency:` line and the two verbatim `baseline-content-drift:` lines (`functions compared …` and `findings 0`), never the `SCOPE —` line (per D-16)."
    - "The regenerated section never carries the human-judgment 'what it adds' column and never a second full-64-hex sha row; the PR body invites the reviewer to add the column (per D-16)."
    - "The PR body carries the D-23 instructions (Approve workflows to run, read each head-SHA run's conclusion, branch protection off so a zero-check merge is possible, close-and-reopen only as a fallback, never auto-merged, re-dispatch on a moved main or a VERSION collision), each pinned by a presence arm (per D-12 as amended by D-23, D-13)."
  artifacts:
    - path: scripts/baseline-redump.mjs
      provides: "judgeVersionBump; refusing nextVersion/bumpPackageJson/insertChangelogEntry/rewriteBaselineMd; composeRegeneratedSection; the complete composeChangelogEntry; composePrBody; `<out>/pr-body.md` written by --compose"
  key_links:
    - "`judgeVersionBump` mirrors `scripts/check-version-bump.mjs` `DEFECTS` (`version-not-bumped`, `version-package-mismatch`, `changelog-missing-entry`) because that module exits at import."
    - "`--compose` hands the composers the measured object plan 01 assembled (run id, merge, old/new sha and shapes, newly carried basenames, captured gate lines); no composer reads a file or re-measures."
    - "`scripts/check-baseline-staleness.mjs` reads exactly one full-sha row; the regenerated section keeps sha256 in prefix form so it never adds a second."
  prohibitions:
    - "No figure in the entry, the section or the PR body is typed from memory or restated from a doc; every one comes from the measured object (per D-16, CLAUDE.md 'CHANGELOG discipline')."
    - "No skip token and no credential-shaped literal in any template (per D-14). The skip-token guard itself is plan 03's."
    - "No database command against any remote (per D-04)."
---

<objective>
Complete the write half of the tracer. The writers plan 01 built become exactly-once and refuse
by name instead of silently no-opping, the BASELINE.md writer stays inside the `## Provenance`
capture table on the REAL file, and the CHANGELOG entry, the dated `### Regenerated` section and
the PR body carry every value D-16 and D-23 name, each pinned by an arm that can fail.

Purpose: the bot's six paths are the only thing a reviewer reads. A value missing from the entry
or a row rewritten in a historical section is the defect a human is least likely to catch.

Output: `scripts/baseline-redump.mjs` with refusing writers, the complete composers,
`<out>/pr-body.md`, and observed-RED evidence for every arm.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-CONTEXT.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-RESEARCH.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-01-SUMMARY.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-02-SUMMARY.md
@scripts/baseline-redump.mjs
@scripts/check-version-bump.mjs
@scripts/check-baseline-staleness.mjs
@supabase/schema/BASELINE.md

<interfaces>
Unchanged CLI (frozen in plan 01). `--compose` additionally writes `<out>/pr-body.md`.

Writers return `{text, defects}` where `defects` is a list of `{kind, detail}` (counts and line
numbers only). If plan 01 left any writer returning a bare string, change its signature and its
single call site in `--compose` in this plan; nothing outside the script calls them.

Measured facts of the real `supabase/schema/BASELINE.md` at plan time (re-measure at execution;
cite by heading, never by line): the `## Provenance` table holds one each of `| Taken |`,
`| Supabase CLI |`, `| sha256 |` (full 64-hex) and `| Shape |`; the first `### Regenerated`
heading follows it INSIDE the `## Provenance` section; file-wide there are four `| Shape |` rows
and five `| sha256 |` rows, the extra ones under `### Regenerated` headings in prefix form.

Measured shape of `node scripts/baseline-content-drift-check.mjs` stdout at plan time: five lines
beginning `baseline-content-drift:` — `chain files …`, `functions compared …`, `allowlisted rows …`,
`findings 0`, and a long `SCOPE — …` line. D-16's verbatim drift lines are the
`functions compared` line and the `findings` line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: writers that refuse, the Provenance-scoped exactly-once row writer on the real file, and the version-gate mirror</name>
  <precondition>`npx vitest --version` prints a version in this worktree (node_modules resolve); if it does not, run `npm ci` first. `git merge-base --is-ancestor 35d1d412f HEAD` exits 0 (plan 01 Task 1).</precondition>
  <files>scripts/baseline-redump.mjs</files>
  <read_first>scripts/baseline-redump.mjs (nextVersion, bumpPackageJson, insertChangelogEntry, provenanceSpan, rewriteBaselineMd, --compose), scripts/check-version-bump.mjs (judge, DEFECTS), scripts/check-baseline-staleness.mjs (RECORDED_SHA_RE_ALL, the IN-03 exactly-one rule), supabase/schema/BASELINE.md (section `## Provenance` and the first `### Regenerated` under it), src/__tests__/critical-regressions.test.ts (the VERSION == package.json pin, found by grepping `readText("VERSION")`)</read_first>
  <behavior>
    - `nextVersion` refuses `0.93.0`, `0.93.0.3.1`, `v0.93.0.3` and `0.93.0.x`; `0.93.0.3` gives `0.93.0.4`.
    - `bumpPackageJson` refuses when `"version": "<old>"` matches 0 or 2 times, and when the JSON read-back differs.
    - `judgeVersionBump({baseVersion, headVersion, packageVersion, changelog})` returns the same three defect kinds as `check-version-bump.mjs`; each is observed on a crafted input.
    - `insertChangelogEntry` refuses when `## [<new>]` already exists, and when the file lacks `# Changelog` or any `\n## [`; the new heading is the FIRST `## [` after insertion.
    - `rewriteBaselineMd`, on a fixture whose `## Provenance` table holds the four rows and whose nested `### Regenerated` subsection plus a later `## ` section carry EXTRA `| Shape |` and prefix-form `| sha256 |` rows, succeeds and leaves the extra rows byte-unchanged (the scoping arm).
    - `rewriteBaselineMd` refuses, naming the row and the count, when any capture row matches 0 times or 2 times INSIDE the span (calibration fixture: a duplicate `| Shape |` row inside the table), and when the file has no `## Provenance` heading or no `### Regenerated` heading to insert above.
    - Real-file arm (read-only): `rewriteBaselineMd` over `REPO_ROOT/supabase/schema/BASELINE.md` with synthetic values returns no defect; afterwards `RECORDED_SHA_RE_ALL` matches exactly once and equals the synthetic sha; every original line other than the four capture rows is present in the output in the same order.
    - Real-file arms (read-only): `insertChangelogEntry` over the real `CHANGELOG.md` and `bumpPackageJson` over the real `package.json` with the real `VERSION` return no defect.
    - The written VERSION file has no trailing newline.
  </behavior>
  <action>
  Make every writer return `{text, defects}` and refuse rather than silently no-op; a regex
  replace that matched nothing is the defect class here (per D-15, D-16). `--compose` prints
  `::error::` with the defect kind and detail and exits 1 BEFORE anything is staged.

  `rewriteBaselineMd` counts each capture row only on the lines `provenanceSpan` returns (the span
  from the `## Provenance` heading to the first following `^#{1,6} ` heading, as plan 01 built
  it). The count is exactly-once THERE, never file-wide: file-wide the real file carries four
  `| Shape |` rows and five `| sha256 |` rows, and each bot run adds one more of each under its
  own `### Regenerated`, so a file-wide count would pass a first run and refuse every run after.
  Then insert the section `composeRegeneratedSection` returns (Task 2) immediately above the
  FIRST `### Regenerated` heading, which is also the end of the span. Until Task 2 lands, the
  section is the heading plus the provenance sentence; Task 2 fills it.

  Add `judgeVersionBump`, mirroring `check-version-bump.mjs` `judge()`'s three defects by name
  and meaning, and run it in `--compose` after the writers and before staging (per D-15). Do NOT
  import that module: its `process.exit(main())` runs at module scope. Say so in a comment that
  names the mirrored source.

  The real-file arms read `REPO_ROOT` files and never write them; they run in every environment
  the self-test runs in (local, the vitest spawn of plan 09, and `redump-pr` on a checkout of
  main), so a future hand edit of BASELINE.md that breaks the span is caught the next time any
  of those runs.

  RED first: run the new arms against the plan-01 writers and observe the scoping-duplicate,
  zero-match and duplicate-heading arms fail; then implement. Update `EXPECTED_ASSERTIONS`.
  </action>
  <verify>
    <automated>node scripts/baseline-redump.mjs --self-test | grep -E '^baseline-redump self-test OK: [0-9]+ assertion' && node scripts/check-version-bump.mjs --self-test && [ "$(grep -cE '^import .*check-version-bump' scripts/baseline-redump.mjs)" = "0" ]</automated>
    <fails_when>either self-test exits 1, prints `SELF-TEST FAILED`, or the baseline-redump OK line is absent; or the script imports check-version-bump.mjs (which would exit at import)</fails_when>
  </verify>
  <acceptance_criteria>
    - `--self-test` exits 0 with the updated count, including the three real-file arms.
    - `grep -c "check-version-bump.mjs" scripts/baseline-redump.mjs` ≥ 1, as a comment naming the mirrored source.
    - SUMMARY records each writer arm's observed RED, including the file-wide-count variant of `rewriteBaselineMd` (count across the whole file in a scratch copy) going RED on the real-file arm.
  </acceptance_criteria>
  <done>Every writer produces exactly the intended edit or refuses by name, the BASELINE.md writer is proven on the real file to touch only the capture table, and the bot cannot emit a VERSION, package.json or CHANGELOG that CI's version gate would reject.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the complete D-16 CHANGELOG entry and `### Regenerated` section, and the D-23 PR body, each value pinned by a presence arm</name>
  <files>scripts/baseline-redump.mjs</files>
  <read_first>scripts/baseline-redump.mjs (composeChangelogEntry, composeCommitMessage, composePrTitle, the measured object --compose assembles), supabase/schema/BASELINE.md (the newest `### Regenerated` section: its provenance sentence, migrations list and `**MEASURED:**` table are the shape to follow, minus the "what it adds" column), CHANGELOG.md (the first entry's heading and section vocabulary), .planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-RESEARCH.md (F9 on the approval-required runs)</read_first>
  <behavior>
    - With a measured fixture whose every value is distinctive (a run id, a merge sha, two sha256 values, old and new shape counts that all differ, two newly carried basenames, a `baseline-currency:` line, and the two drift lines), one presence arm per value asserts it appears in the CHANGELOG entry, and one per value asserts it appears in the regenerated section. Values: run id; merge short sha; old sha256 prefix; new sha256 prefix; tables old and new; policies old and new; function statements old and new; distinct function names old and new; data statements; each newly carried basename; the currency line verbatim; the `functions compared` drift line verbatim; the `findings 0` drift line verbatim.
    - With a measured fixture carrying NO newly carried basename, the entry and the section both state that the marker diff added none (a measured statement, not an omission).
    - The drift `SCOPE —` line never appears in the entry or the section, even when it is present in the captured stdout the fixture supplies.
    - The regenerated section contains no table header naming "what it adds" and exactly zero full-64-hex sha256 values; its sha256 row is prefix form.
    - The entry's heading is `## [<new>] - <date> — …` and its sections are `### Changed` and `### Notes`, the repo's own vocabulary.
    - `composePrBody` presence arms: the phrase `Approve workflows to run`; the word `conclusion`; a statement that branch protection is off and a zero-check merge is possible; the `gh api "repos/<repo>/actions/runs?head_sha=<sha>"` command with the real repo and head sha substituted; an invitation to add the "what it adds" column; re-dispatching `supabase-migrate.yml` on main when main moved or VERSION collides; close-and-reopen named only as a fallback; a statement that the PR is never auto-merged.
  </behavior>
  <action>
  Complete `composeChangelogEntry(m)` per D-16: keep plan 01's heading and first `### Changed`
  bullet, then add bullets carrying the shape counts old → new, the newly carried basenames (or
  the measured "none"), and the verbatim currency and drift lines in backticks. `### Notes`
  says the dump was taken by the `redump-dump` job read-only after `apply` succeeded, that the
  "what it adds" judgment is left to the reviewer, and names the applying run id again as the
  provenance anchor. Every value comes from the measured object; no composer reads a file.

  Add `composeRegeneratedSection(m)`: heading `### Regenerated <date> — automated re-dump after
  Supabase Migrate run <id>`, a provenance sentence naming the `redump-dump` and `redump-pr`
  jobs, the run id and the merge short sha; a bullet list "Which migrations the new dump now
  carries" with the newly carried basenames and NO "what it adds" column (per D-16); and a
  `**MEASURED:**` two-column table with Taken, Supabase CLI, Shape (old → new), Data statements,
  sha256 in PREFIX form (`` `<old8>…` → `<new8>…` ``, never a second full-64-hex row, RESEARCH F2
  IN-03), and the verbatim currency and drift lines. `rewriteBaselineMd` (Task 1) inserts it.

  Add `composePrBody(m)` with the D-23 instructions in full, and have `--compose` write it to
  `<out>/pr-body.md` beside `pr-title.txt`. Per D-14 no template names any of the six CI skip
  tokens, not even to deny them, and none carries a credential-shaped literal.

  Presence arms follow the behavior list. RED first: write the arms against plan 01's composers
  and observe every missing value go RED; then implement. Calibration: delete ONE field from
  `composeChangelogEntry` in a scratch copy and observe exactly its presence arm go RED; do the
  same for one `composePrBody` phrase. Update `EXPECTED_ASSERTIONS`.
  </action>
  <verify>
    <automated>node scripts/baseline-redump.mjs --self-test | grep -E '^baseline-redump self-test OK: [0-9]+ assertion' && [ "$(grep -ciE '\[(skip|ci|no|actions) (ci|skip|actions)\]|skip-checks' scripts/baseline-redump.mjs)" = "0" ]</automated>
    <fails_when>self-test exits 1 or lacks its OK line; or a bracketed skip token or the unsplit trailer key appears anywhere in the script, comments included</fails_when>
  </verify>
  <acceptance_criteria>
    - `--self-test` exits 0 with the updated count.
    - SUMMARY lists every D-16 and D-23 presence arm by name with its observed RED, plus the two scratch-copy calibrations.
    - SUMMARY carries one composed CHANGELOG entry and one composed regenerated section from the self-test fixture, verbatim, so a reader sees the shape the bot will write.
  </acceptance_criteria>
  <done>The entry, the regenerated section and the PR body carry every measured value D-16 and D-23 name, and removing any one of them turns an arm red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composed text → BASELINE.md / CHANGELOG.md on main | the bot's writers edit the repo's provenance record |
| composed text → PR body | the reviewer's only instructions for reading the PR's checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.5-19 | Denial of service | a version collision or duplicate heading makes the PR fail version-gate | low | mitigate | mirrored `judgeVersionBump` and duplicate-heading refusal (Task 1, D-15); the PR body says to re-dispatch (Task 2) |
| T-164.9.5-33 | Tampering | an unscoped row writer rewrites a historical `### Regenerated` row, or refuses every run after the first | high | mitigate | `rewriteBaselineMd` counts and writes inside `provenanceSpan` only; the scoping fixture and the read-only real-file arm prove it; the file-wide-count RED is observed (Task 1, D-16) |
| T-164.9.5-34 | Repudiation | a CHANGELOG entry or regenerated section states a figure that was not measured, or omits one D-16 names | medium | mitigate | composers read only the measured object; one presence arm per D-16 value, with a scratch-copy calibration observed RED (Task 2) |
</threat_model>

<verification>
`node scripts/baseline-redump.mjs --self-test` is green, including the read-only real-file arms
over BASELINE.md, CHANGELOG.md and package.json, and every presence arm has been observed RED.
</verification>

<success_criteria>
D-15 and D-16 are delivered at full scope in the script: every writer refuses rather than
producing a tree CI rejects, the BASELINE.md writer is scoped on the real file, and every D-16
value is pinned. The D-23 PR-body instructions are pinned.
</success_criteria>

## Artifacts this phase produces (this plan)

- `scripts/baseline-redump.mjs` gains: `judgeVersionBump`, `composeRegeneratedSection`,
  `composePrBody`; `nextVersion`, `bumpPackageJson`, `insertChangelogEntry` and
  `rewriteBaselineMd` return `{text, defects}`; `composeChangelogEntry` carries every D-16 value.
- `--compose` writes `<out>/pr-body.md`.

<output>
Create `.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-07-SUMMARY.md` when done
</output>
