---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/baseline-redump.mjs
autonomous: true
requirements:
  - "164.9.5-MANUAL-BASELINE-REDUMP"

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low
  # Re-sliced 2026-09-26 (checker round 1, scope_sanity): the `### Regenerated` writer, the full
  # D-16 CHANGELOG composer, the PR body composer and the writer refusals moved to plan 07; the
  # bot-branch and PR modes moved to plan 08. The tracer stays end to end, only thinner.

must_haves:
  truths:
    - "The worktree contains merge 35d1d412f (PR #864) before any gate or pin runs: `git merge-base --is-ancestor 35d1d412f HEAD` exits 0, and the three offline gates read green on it (per D-29)."
    - "`scripts/baseline-redump.mjs --gate-dump` turns a dump file into an out dir holding exactly the dump, a marker regenerated from `git ls-tree` of the MERGE commit (never the working tree) with a recomputed `baseline-sha256:` line, and a `measured.json`; it writes `changed=true|false` to `$GITHUB_OUTPUT` (per D-09, D-10, D-18)."
    - "`scripts/baseline-redump.mjs --compose` run in a checkout of main copies the two generated files in, runs `bash scripts/local-stack/run.sh --check-currency`, `node scripts/baseline-content-drift-check.mjs` and `node scripts/check-baseline-staleness.mjs` as child processes, writes the 4th-digit VERSION/package.json bump, one CHANGELOG entry and the four `## Provenance` capture rows, stages EXACTLY the six paths by explicit path, and commits as github-actions[bot] (per D-09, D-15, D-16, D-17). Plan 07 completes the D-16 content of the entry and adds the dated `### Regenerated` section behind the same function names."
    - "The BASELINE.md row writer is SCOPED to the `## Provenance` capture table: the span from the `## Provenance` heading to the first following heading at ANY level (`^#{1,6} `). The first `### Regenerated` heading sits INSIDE `## Provenance`, and the file already carries four `| Shape |` rows and five `| sha256 |` rows file-wide, of which only one of each is the capture table's. A row outside that span is never read or written (per D-16)."
    - "Every figure the writers emit is a MEASURED value (run id, merge short sha, sha256 prefixes, grep shape counts, verbatim gate lines), never prose restated from memory (per D-16, D-27)."
    - "The script's CLI surface is frozen in this plan, so plan 04 can wire the workflow against it in parallel with plan 02 (per D-18)."
  artifacts:
    - path: scripts/baseline-redump.mjs
      provides: "CLI modes --self-test, --gate-dump, --compose; pure judge/compose functions; exported constants SECRET_SCAN_PATTERN, BOT_BRANCH, BOT_NAME, BOT_EMAIL, STAGED_PATHS, GITLEAKS_VERSION, MIGRATION_BASENAME_STRICT_RE, EXPECTED_ASSERTIONS"
  key_links:
    - "`--compose` imports `RECORDED_SHA_RE_ALL` from scripts/check-baseline-staleness.mjs (safe: that module guards its main with `invokedDirectly()`), so the bot's BASELINE.md writer and CI's co-edit gate read the same regex."
    - "`--compose` never imports scripts/check-version-bump.mjs: it calls `process.exit(main())` at module scope. Its three defects are MIRRORED instead."
    - "The workflow jobs of plan 04 call this script by the exact mode and flag names in the `<interfaces>` block below."
  prohibitions:
    - "No database command against any remote. Every run in this plan uses the committed dump or a runtime mutation of it (per D-04)."
    - "Never `git checkout --`, `git restore` or `git stash`. The base sync is a merge."
    - "The script never prints a matched line of the dump, a DSN, a token or a project ref (per D-06)."
---

<objective>
Bring the worktree onto a base that contains PR #864, then build the tracer: `scripts/baseline-redump.mjs`
with its complete CLI surface and one production-quality happy path through BOTH halves of the
automation. A dump goes in, is hashed and turned into a gated out dir with a marker regenerated
from the MERGE tree, and is then composed onto a checkout of main as exactly six staged paths
and one bot commit, with the real offline gates run as child processes.

Purpose: prove the whole data path of the redump end to end on the agent's best early context
before any refusal arm, workflow job or doc is written. Plans 02 (gate side), 07 (writers and
composers), 03 (compose-side judges) and 08 (bot-branch and PR modes) extend this script without
renaming any mode or flag. Plan 04 wires the workflow against the CLI frozen here.

The tracer is deliberately THIN, not partial: each of the six paths is written by a real writer
and the commit is real, but the CHANGELOG entry carries only its heading and one measured
`### Changed` bullet, and BASELINE.md gets only its four capture rows. Plan 07 completes D-16
behind the same function names, with no change to the data path proven here.

Output: `scripts/baseline-redump.mjs` (new), green `--self-test`, and an end-to-end run in a
throwaway local clone that leaves the worktree's tracked files untouched.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-CONTEXT.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-RESEARCH.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-PATTERNS.md
@scripts/check-baseline-staleness.mjs
@scripts/check-baseline-currency.mjs
@scripts/check-version-bump.mjs
@scripts/classify-changed-paths.mjs
@supabase/schema/BASELINE.md

<interfaces>
FROZEN CLI of scripts/baseline-redump.mjs (plan 04 wires exactly these; plans 02, 07, 03 and 08
add behaviour behind them without renaming anything). Unknown or missing arguments are refused with
`::error::` and exit 1, in the `check-baseline-currency.mjs` `main` whitelist idiom.

- `--self-test` : node + git only. Runs every arm that needs no gitleaks binary. Prints
  `baseline-redump self-test OK: <n> assertion(s)` on success and exits 0; exits 1 when an arm
  fails or when the count of arms run differs from `EXPECTED_ASSERTIONS`.
- `--self-test --with-gitleaks` : everything above plus the real-binary gitleaks arms
  (IMPLEMENTED IN PLAN 02; in this plan the flag is not yet accepted).
- `--gate-dump --dump <file> --merge <40-hex sha> --run-id <digits> --cli-version <x.y.z> --out <dir>` :
  run from the repo root of the MERGE checkout. Asserts `--merge` equals `git rev-parse HEAD`.
  Compares against HEAD's committed `supabase/schema/baseline.sql` and marker (D-10). Writes
  `<dir>/baseline.sql`, `<dir>/baseline-carried-migrations.txt`, `<dir>/measured.json` ONLY
  when changed. Appends `changed=true|false` to `$GITHUB_OUTPUT` when that variable is set.
  The no-op path prints `::notice::` and exits 0 from inside the script.
- `--compose --in <dir> --out <dir>` : run from the repo root of a checkout of `main`. Writes the
  six paths, commits as the bot, writes `<out>/pr-title.txt` (this plan) and `<out>/pr-body.md`
  (plan 07 adds the body composer), prints and appends `committed=true|false` to
  `$GITHUB_OUTPUT`.
- `--check-bot-branch --remote <name>` : IMPLEMENTED IN PLAN 08 (D-24 foreign-author refusal
  naming the PR; appends `lease=<sha-or-empty>` to `$GITHUB_OUTPUT`).
- `--open-or-edit-pr --title-file <f> --body-file <f>` : IMPLEMENTED IN PLAN 08 (gh pr list,
  then create or edit; never merge).

Exports (the wiring test of plan 09 imports these; the `invokedDirectly()` guard makes import safe):
`SECRET_SCAN_PATTERN` (a plain STRING), `SECRET_SCAN_RE`, `MIGRATION_BASENAME_STRICT_RE`,
`BOT_BRANCH = "automation/baseline-redump"`, `BOT_NAME = "github-actions[bot]"`,
`BOT_EMAIL = "41898282+github-actions[bot]@users.noreply.github.com"`, `STAGED_PATHS` (the six
paths, sorted), `GITLEAKS_VERSION = "8.30.1"`, `EXPECTED_ASSERTIONS`.

`measured.json` keys written by `--gate-dump` (schema 1): `schema`, `run_id`, `merge`,
`cli_version`, `dump_sha256`, `dump_bytes`, `shapes` {`tables`, `policies`,
`function_statements`, `distinct_functions`, `data_statements`}, `marker_sha256`,
`carried_count`. Plan 02 adds `secret_scan_hits`, `gitleaks`, `gitleaks_version`, `integrity`.

From scripts/check-baseline-staleness.mjs (import): `RECORDED_SHA_RE`, `RECORDED_SHA_RE_ALL`.
From scripts/check-baseline-currency.mjs (reference only, do not import): `MIGRATION_BASENAME_RE`
(looser, `^[0-9]+_…`), `SHA_LINE_RE` (the marker sha line shape).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: base sync (D-29), dependencies, and the three offline gates measured green</name>
  <files>(merge of origin/main; no hand-edited file)</files>
  <read_first>.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-CONTEXT.md (D-29), .planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-RESEARCH.md (F6)</read_first>
  <action>
  Per D-29, the worktree base predates merge 35d1d412f (PR #864), so the offline gates read red
  here (measured at plan time: currency `replay=3`, content-drift DRIFT 5 with findings). Bring in
  current main by MERGE: `git fetch origin`, then `git merge --no-edit origin/main`. Never use
  `git checkout --`, `git restore`, `git stash` or a rebase. At plan time `git merge-tree
  --write-tree HEAD origin/main` reported no conflict. If a conflict appears anyway, resolve
  `.planning/` and `TODOS.md` hunks by keeping BOTH sides. If a conflict touches
  `supabase/schema/`, `.github/` or `scripts/`, STOP and report it rather than choosing a side.

  ⚠️ Carry the corrections to RESEARCH's plan-time facts. `origin/main` VERSION read `0.93.0.3`
  at plan time, not RESEARCH F7's `0.93.0.2`. "Only `supabase/schema/*` differs from main" (F6)
  is also stale: `ci.yml`, `test-restore-from-baseline.yml`, `TODOS.md`, `CHANGELOG.md` and
  `VERSION` moved too. Re-read every value at execution, and cite `ci.yml` by symbol
  (`GITLEAKS_VERSION:` under the `secret-scan:` job), never by line number.

  Then run `npm ci` in the worktree. Prove it by running `npx vitest --version`, not by listing
  the directory, because node_modules can resolve by walking up from a worktree.

  Measure the three offline gates on the merged tree and paste each summary line verbatim into
  SUMMARY:
  - `bash scripts/local-stack/run.sh --check-currency` must print a `baseline-currency:` line
    containing `marker-sha=match defects=0`.
  - `node scripts/baseline-content-drift-check.mjs` must print `baseline-content-drift: findings 0`.
  - `node scripts/check-baseline-staleness.mjs` must exit 0.
  If any of them is red on the merged tree, STOP and report: that is a main-side fact this phase
  must not paper over.
  </action>
  <verify>
    <automated>git merge-base --is-ancestor 35d1d412f HEAD && npx vitest --version && bash scripts/local-stack/run.sh --check-currency | grep -E 'marker-sha=match defects=0' && node scripts/baseline-content-drift-check.mjs | grep -xE 'baseline-content-drift: findings 0' && node scripts/check-baseline-staleness.mjs</automated>
    <fails_when>non-zero exit from any link: merge-base exits 1 (35d1d412f not an ancestor), npx cannot resolve vitest, a grep matches no line, or the staleness gate exits 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `git merge-base --is-ancestor 35d1d412f HEAD` exits 0.
    - `git log -1 --format=%P HEAD` shows two parents (a merge), or HEAD already contained origin/main (recorded in SUMMARY).
    - `npx vitest --version` prints a version.
    - SUMMARY carries the three gate lines verbatim and the VERSION read from the merged tree.
  </acceptance_criteria>
  <done>The worktree contains #864 and current main, node_modules resolve, and all three offline gates are green on the merged tree, with their lines recorded.</done>
</task>

<task type="tracer">
  <name>Task 2: tracer, one dump through gate-dump and compose to six staged paths and a bot commit</name>
  <files>scripts/baseline-redump.mjs</files>
  <read_first>scripts/check-baseline-staleness.mjs (main, selfTest, EXPECTED_ASSERTIONS, invokedDirectly, RECORDED_SHA_RE_ALL), scripts/check-baseline-currency.mjs (main's second-mode whitelist, sha256OrNull, SHA_LINE_RE, MIGRATION_BASENAME_RE), scripts/check-version-bump.mjs (judge and its three DEFECTS), scripts/classify-changed-paths.mjs (emit, the scratch-repo self-test arm), scripts/restore-test-from-baseline.sh (the EXP_TABLES / EXP_POLICIES grep counts), supabase/schema/BASELINE.md (sections `## Provenance`, the newest `### Regenerated`, `## Regenerating`), supabase/schema/baseline-carried-migrations.txt (header comments), CHANGELOG.md (first entry shape), `git show 35d1d412f` (the six-file worked example)</read_first>
  <action>
  Create `scripts/baseline-redump.mjs`, node-builtins only (`node:crypto`, `node:fs`, `node:path`,
  `node:url`, `node:os`, `node:child_process`), with no npm dependency, because `redump-pr` runs it
  with no `npm ci` (RESEARCH Open Question 4). Copy the house scaffold from
  `check-baseline-staleness.mjs`: header comment block, `REPO_ROOT` from the script location,
  `main(argv)` returning an exit code, `invokedDirectly()` copied verbatim, and a `selfTest()` with
  an `ok(cond, msg)` counter and a pinned `EXPECTED_ASSERTIONS`, so a deleted arm fails.

  Implement the FROZEN CLI in the `<interfaces>` block. In this plan, `--self-test`,
  `--gate-dump` and `--compose` are accepted. Every other flag is refused as unknown until the
  plan that implements it widens the whitelist. Every function that touches files takes an
  explicit `repoRoot` parameter, so the self-test can drive it against a scratch repo.

  Exports and constants (per D-06, D-11, D-17, D-26, D-27):
  - `SECRET_SCAN_PATTERN`: a plain STRING byte-equal to the argument of the `grep -anE '…'`
    line in `supabase/schema/BASELINE.md` `## Regenerating`. Copy it from the file; do not retype
    it. `SECRET_SCAN_RE` is `new RegExp(SECRET_SCAN_PATTERN)`. A regex literal's `.source`
    escapes `/` and would never equal the ERE text (RESEARCH Validation row D-06).
  - `MIGRATION_BASENAME_STRICT_RE = /^[0-9]{14}_[a-z0-9_]+\.sql$/`, with a comment saying why it
    is narrower than `check-baseline-currency.mjs`'s `MIGRATION_BASENAME_RE` (D-27: a basename is
    interpolated into a commit message and PR body).
  - `BOT_BRANCH`, `BOT_NAME`, `BOT_EMAIL`, `STAGED_PATHS` (sorted: `CHANGELOG.md`, `VERSION`,
    `package.json`, `supabase/schema/BASELINE.md`, `supabase/schema/baseline-carried-migrations.txt`,
    `supabase/schema/baseline.sql`), and `GITLEAKS_VERSION = "8.30.1"`.

  Pure functions (no I/O, so the self-test drives them directly; each defect is `{kind, detail}`
  where `detail` carries counts and line numbers only):
  - `countShapes(text)`, per D-27, using the RESEARCH F8 regexes: tables `^CREATE TABLE`, policies
    `^CREATE POLICY`, function statements `^CREATE (OR REPLACE )?FUNCTION`, distinct function names
    from the quoted `"schema"."name"` head, data statements `^(INSERT INTO|COPY |SELECT
    pg_catalog\.setval)`. All are multiline and anchored per line. Plan 02 adds the refusals on
    these counts.
  - `buildMarker({headerLines, sha, basenames})`: the header `#` lines preserved in order, then
    `baseline-sha256: <sha>`, then the basenames sorted, newline-terminated, exactly as the
    `## Regenerating` recipe produces.
  - `nextVersion(v)`: parses `^(\d+)\.(\d+)\.(\d+)\.(\d+)$` and increments the 4th field (per
    D-15).
  - `bumpPackageJson(text, oldV, newV)`: a single regex replace of `"version": "<old>"`. It
    re-parses with `JSON.parse` and confirms the version reads back as newV. It never
    re-serialises the file.
  - `insertChangelogEntry(text, entry)`: inserts before the first `\n## [` after the `# Changelog`
    title.
  - `provenanceSpan(lines)`: returns the `[start, end)` line range of the `## Provenance` capture
    table: from the line after the `## Provenance` heading to the first following line matching
    `^#{1,6} ` (a heading at ANY level). Measured at plan time, the first `### Regenerated`
    heading follows the capture table INSIDE `## Provenance`, so stopping at the next `## `
    would sweep in every regenerated section's rows. The file carries four `| Shape |` rows and
    five `| sha256 |` rows file-wide, and each bot run adds one more of each under its own
    `### Regenerated`, so an unscoped count fails on the second run even if the first passed.
  - `rewriteBaselineMd(text, values)`: inside `provenanceSpan` ONLY, replaces the four capture
    rows (`| Taken |`, `| Supabase CLI |`, `| sha256 |` with the full 64-hex in backticks,
    `| Shape |`). Lines outside the span are never read for a match and never written (per D-16).
    Plan 07 adds the exactly-once refusal and the dated `### Regenerated` section, behind this
    same function.
  - `composeChangelogEntry(m)`, `composeCommitMessage(m)`, `composePrTitle(m)`: from ONE fixed
    template. Heading `## [<new>] - <date> — BASELINE: automated re-dump after the PROD apply of
    <merge8>`, then a `### Changed` section with ONE bullet carrying the applying run id, the
    merge short sha and the `<old8>…` → `<new8>…` sha256 prefixes (per D-16). Plan 07 completes
    the entry with every remaining D-16 value and a presence arm per value. Subject
    `chore(release): v<new> — baseline re-dump after the PROD apply of <merge8>`. Per D-14 the
    template names none of the six CI skip tokens, not even to deny them, and carries no
    credential-shaped literal. `composePrBody` (the D-23 instructions) is plan 07's.

  Modes:
  - `--gate-dump`: validate the args (`--merge` 40-hex AND equal to `git rev-parse HEAD`;
    `--run-id` digits only; `--cli-version` `^\d+\.\d+\.\d+$`). Read the dump. Hash it with
    `node:crypto`. List `git ls-tree --name-only <merge> supabase/migrations/`, keep `.sql`
    basenames, strip the directory, and sort (per D-09: the MERGE tree, never the working tree).
    Build the marker from the committed marker's `#` header lines. D-10 comparison: if the dump
    sha256 equals the sha256 of HEAD's committed `supabase/schema/baseline.sql` AND the built
    marker is byte-identical to the committed marker, print `::notice::baseline-redump: dump
    and marker are byte-identical to the committed pair — no PR` and emit `changed=false`. The
    `exit 0` lives here in the script, never in a workflow `run:` body. Otherwise write the three
    out files and emit `changed=true`. Always print one summary line `baseline-redump gate:
    sha256=<8>… bytes=<n> tables=<n> policies=<n> carried=<n> changed=<bool>`, so a clean run
    and a run that did nothing never look alike. The `emit` helper follows
    `classify-changed-paths.mjs`: always print the line, and append to `$GITHUB_OUTPUT` only
    when that variable is set.
  - `--compose`: read `<in>/measured.json` and the two files. Second D-10 check: if they are
    byte-identical to the checkout's committed pair, print `::notice::`, emit
    `committed=false` and return 0, because main may have moved between the jobs. Record the old
    sha and old shapes from the checkout's files, then copy the two files into `supabase/schema/`.
    Run the gates as child processes, printing their output: `bash scripts/local-stack/run.sh
    --check-currency` (capture the `baseline-currency:` line), `node
    scripts/baseline-content-drift-check.mjs --self-test`, then `node
    scripts/baseline-content-drift-check.mjs` (capture the `baseline-content-drift: findings`
    and `functions compared` lines by their exact prefixes; the gate prints five
    `baseline-content-drift:` lines, the last being a long `SCOPE —` line that is never
    captured). A non-zero exit refuses. The line-content judges come in plan 03. Take the old
    version from the checkout's `VERSION`. Write `VERSION` with NO trailing
    newline (RESEARCH F7). Write `package.json`, the CHANGELOG entry and BASELINE.md, then run
    `node scripts/check-baseline-staleness.mjs` as a child process (it must exit 0). Derive
    "newly carried" from the old and new marker diff, and validate each basename against
    `MIGRATION_BASENAME_STRICT_RE` before writing it anywhere. Keep the derived list and the
    captured gate lines in the in-memory measured object the composers read, so plan 07 can
    complete D-16 without touching the data path. Stage with `git add --` and the
    six explicit `STAGED_PATHS`, never `-A` or `.` (per D-17). Commit with `git -c user.name=<BOT_NAME>
    -c user.email=<BOT_EMAIL> -c commit.gpgsign=false commit -F <msgfile>`. Write
    `<out>/pr-title.txt`, then print and emit `committed=true`. Take the date as UTC
    `YYYY-MM-DD`. The composers receive it as a parameter, so the self-test is deterministic.

  Self-test (node + git only, per D-18). One green end-to-end arm in a `mkdtempSync` scratch
  repo, removed in `finally`. The repo holds a minimal `VERSION`, `package.json`, `CHANGELOG.md`,
  `supabase/schema/BASELINE.md`, a marker with `#` header lines, two
  `supabase/migrations/<14 digits>_<name>.sql` files, and
  `supabase/schema/baseline.sql` = a READ-ONLY copy of the real committed dump. The dump under
  test is that file plus one appended newline. The child-gate runner is INJECTED: the self-test
  passes a fake that returns canned green lines, and the CLI passes the real child-process
  runner. Assert: `changed=true`; the marker lists exactly the two committed basenames; a third
  `.sql` file left UNCOMMITTED in the scratch working tree is absent from the marker (the D-09
  failing direction); the marker's sha line equals the dump's sha256; compose's `git diff
  --cached --name-only` before commit equals `STAGED_PATHS`; VERSION bumped in its 4th digit
  with no newline; package.json's version equals VERSION; CHANGELOG's first `## [` heading is
  the new version; BASELINE.md has exactly ONE `RECORDED_SHA_RE_ALL` match and it equals the new
  sha; the rows under the fixture's `### Regenerated` heading are byte-unchanged after the
  rewrite; the commit author and committer are the bot; the unchanged-dump run reports
  `changed=false`. The fixture BASELINE.md mirrors the REAL file's shape, not an idealised one:
  a `## Provenance` heading, the capture table with the four rows, then a `### Regenerated`
  heading INSIDE that section carrying its own `| Shape |` row and a prefix-form `| sha256 |`
  row, then a second `## ` section carrying one more `| Shape |` row. A writer that ignores the
  span rewrites the regenerated rows and fails the byte-unchanged assertion. ⚠️ The D-10 no-op depends on `buildMarker` reproducing the committed marker
  BYTE-FOR-BYTE. If the verify's unmodified-dump run does not print `changed=false`, `diff` the
  built marker against the committed one. The likely causes are basename sort order (the
  committed file came from a shell `sort`, so match its collation, not a locale-aware compare)
  and header-line or trailing-newline handling. Fix the builder, never the equality test. Set
  `EXPECTED_ASSERTIONS` to the number of `ok()` calls, and prove it bites:
  delete one `ok()` in a scratch copy, observe `SELF-TEST FAILED`, and discard the copy.
  </action>
  <verify>
    <automated>node scripts/baseline-redump.mjs --self-test | grep -E '^baseline-redump self-test OK: [0-9]+ assertion' && S="$(mktemp -d "${TMPDIR:-/tmp}/164.9.5-01-XXXX")" && BR="$(git branch --show-current)" && [ -n "$BR" ] && git clone -q --branch "$BR" "$PWD" "$S/c" && cp scripts/baseline-redump.mjs "$S/c/scripts/" && ( cd "$S/c" && B="$(git rev-parse HEAD)" && { cat supabase/schema/baseline.sql; printf '\n'; } > "$S/dump.sql" && node scripts/baseline-redump.mjs --gate-dump --dump "$S/dump.sql" --merge "$B" --run-id 1 --cli-version 2.98.2 --out "$S/art" | grep -E 'changed=true' && node scripts/baseline-redump.mjs --gate-dump --dump supabase/schema/baseline.sql --merge "$B" --run-id 1 --cli-version 2.98.2 --out "$S/art0" | grep -E 'changed=false' && [ ! -e "$S/art0/baseline.sql" ] && node scripts/baseline-redump.mjs --compose --in "$S/art" --out "$S/pr" | grep -E 'committed=true' && git diff --name-only "$B" HEAD > "$S/names" && [ "$(sort "$S/names" | tr '\n' ' ')" = "CHANGELOG.md VERSION package.json supabase/schema/BASELINE.md supabase/schema/baseline-carried-migrations.txt supabase/schema/baseline.sql " ] && [ "$(git log -1 --format='%an|%cn')" = "github-actions[bot]|github-actions[bot]" ] && node scripts/check-baseline-staleness.mjs && bash scripts/local-stack/run.sh --check-currency | grep -E 'marker-sha=match defects=0' ) && P="$(git status --porcelain -- supabase/schema VERSION package.json CHANGELOG.md)" && [ -z "$P" ]</automated>
    <fails_when>non-zero exit from any link; the self-test OK line is absent; the first gate-dump line lacks `changed=true` or the unchanged run lacks `changed=false`; the clone's last commit touches any set of paths other than the six; the author or committer is not github-actions[bot]; the staleness or currency gate is red on the composed clone; compose prints no `committed=true`; or `git status --porcelain` shows the worktree's own tracked schema/version files modified</fails_when>
  </verify>
  <acceptance_criteria>
    - `node scripts/baseline-redump.mjs --self-test` exits 0 and prints `baseline-redump self-test OK: <n> assertion(s)` with n equal to `EXPECTED_ASSERTIONS`.
    - `node scripts/baseline-redump.mjs --bogus` exits 1 and stderr contains `::error::`.
    - `node -e "import('./scripts/baseline-redump.mjs').then(m=>{if(typeof m.SECRET_SCAN_PATTERN!=='string')process.exit(1)})"` exits 0 without running main.
    - `grep -c "grep -anE '" supabase/schema/BASELINE.md` is unchanged by this plan (the script copies the pattern; it does not edit the doc).
    - The end-to-end verify above passes in a throwaway clone, and the worktree's `supabase/schema/*`, `VERSION`, `package.json` and `CHANGELOG.md` are unmodified afterwards.
    - SUMMARY records the deliberate `EXPECTED_ASSERTIONS` RED (one `ok()` deleted in a scratch copy) and the composed clone's CHANGELOG heading and its four BASELINE.md `## Provenance` rows, verbatim.
  </acceptance_criteria>
  <done>A dump flows through gate-dump and compose to one bot commit touching exactly the six paths, on a clone where all three real offline gates stay green. The CLI surface plan 04 consumes is frozen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD dump bytes → script | the dump is untrusted content; it is hashed, counted and scanned, never executed |
| script output → public Actions log | anything printed is world-readable (PUBLIC repo) |
| script → git commit / PR text | composed text becomes a commit message and PR body on a public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.5-01 | Information disclosure | script prints (summary lines, `detail` strings) | high | mitigate | Summary lines carry sha prefixes, byte counts and shape counts only; no function prints dump text. Verified by the end-to-end verify output and, for hits, by plan 02's leak-assert arms |
| T-164.9.5-02 | Tampering | marker built from the working tree instead of MERGE | high | mitigate | `git ls-tree <merge>`, with `--merge` asserted equal to HEAD; the self-test's uncommitted-file arm proves an untracked migration is excluded (D-09) |
| T-164.9.5-03 | Tampering | staging `supabase/.temp/*` or any unintended path | high | mitigate | `git add --` over the six explicit `STAGED_PATHS`; the self-test asserts the cached set equals them (D-17); plan 03 adds the refusal arm |
| T-164.9.5-04 | Tampering | composed text becomes a commit message on main via squash | medium | mitigate | fixed template naming no skip token and no credential-shaped literal (D-14); plan 03 adds the guard that refuses |
| T-164.9.5-05 | Denial of service | second full-sha row makes CI's co-edit gate ambiguous | medium | mitigate | the row writer touches only the `## Provenance` span (`provenanceSpan`); the self-test asserts exactly one `RECORDED_SHA_RE_ALL` match and byte-unchanged rows under the fixture's `### Regenerated`; staleness gate run as a child process; plan 07 keeps the regenerated section in prefix form |
</threat_model>

<verification>
`node scripts/baseline-redump.mjs --self-test` is green, and the end-to-end clone run composes
exactly six paths with the three real offline gates green. The worktree's tracked schema and
version files are unmodified.
</verification>

<success_criteria>
The tracer proves the full data path (dump → gate-dump → out dir → compose → six staged paths →
bot commit) with the real offline gates on the composed tree. ROADMAP criteria 3 and 4 hold on
the happy path, and the CLI surface is frozen for plan 04.
</success_criteria>

## Artifacts this phase produces (this plan)

- New file `scripts/baseline-redump.mjs`.
- CLI modes: `--self-test`, `--gate-dump --dump --merge --run-id --cli-version --out`,
  `--compose --in --out` (implemented here); `--self-test --with-gitleaks` (plan 02);
  `--check-bot-branch --remote`, `--open-or-edit-pr --title-file --body-file` (plan 08).
- Exports: `SECRET_SCAN_PATTERN`, `SECRET_SCAN_RE`, `MIGRATION_BASENAME_STRICT_RE`, `BOT_BRANCH`,
  `BOT_NAME`, `BOT_EMAIL`, `STAGED_PATHS`, `GITLEAKS_VERSION`, `EXPECTED_ASSERTIONS`,
  `countShapes`, `buildMarker`, `nextVersion`, `bumpPackageJson`, `insertChangelogEntry`,
  `provenanceSpan`, `rewriteBaselineMd`, `composeChangelogEntry`, `composeCommitMessage`,
  `composePrTitle` (`composePrBody` is plan 07's).
- `$GITHUB_OUTPUT` keys: `changed` (gate-dump), `committed` (compose).
- Out-dir files: `baseline.sql`, `baseline-carried-migrations.txt`, `measured.json`; `pr-title.txt` (plan 07 adds `pr-body.md`).

<output>
Create `.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-01-SUMMARY.md` when done
</output>
