---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
plan: 05
type: execute
wave: 6
depends_on: ["164.9.5-08", "164.9.5-09"]
files_modified:
  - supabase/schema/BASELINE.md
  - TODOS.md
autonomous: true
requirements:
  - "164.9.5-MANUAL-BASELINE-REDUMP"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`BASELINE.md` `## Regenerating` states that the `redump-dump` and `redump-pr` jobs now automate the procedure after every successful PROD `apply` on main, lists what they refuse on, says the PR is never auto-merged and needs a human to approve its workflows, and states that the manual procedure above remains the fallback (per D-19, D-13, D-23)."
    - "The documented `grep -anE` line and the `## Provenance` sha256 row are byte-unchanged, so the wiring test's pattern equality and the staleness gate's single-row rule still hold (per D-06, D-16)."
    - "The `**Version-skew caveat**` paragraph is NOT narrowed in this phase; it waits on the post-merge dispatch that measures byte identity (RESEARCH assumption A1, D-18)."
    - "TODOS `[164.9.5-MANUAL-BASELINE-REDUMP]` is closed (`- [x]`) with a dated closing sentence citing the script, the two jobs, the wiring test and the post-merge human-verification items (per D-19)."
    - "The post-merge dispatch (D-18) and the first bot PR's Approve-workflows click (D-23) are recorded as human-verification items, and no task runs them."
    - "This phase's own VERSION bump and CHANGELOG entry are written at ship time by `/gsd-ship` under CLAUDE.md 'CHANGELOG discipline', not by any plan (per D-19)."
  artifacts:
    - path: supabase/schema/BASELINE.md
      provides: "`## Regenerating` automation paragraph"
    - path: TODOS.md
      provides: "closed `[164.9.5-MANUAL-BASELINE-REDUMP]` entry"
  key_links:
    - "The `grep -anE` argument in `## Regenerating` must stay byte-equal to `SECRET_SCAN_PATTERN` in scripts/baseline-redump.mjs (pinned by src/__tests__/baseline-redump-wiring.test.ts)."
    - "`scripts/check-baseline-staleness.mjs` reads exactly one full-sha row from BASELINE.md; the new paragraph must not add one."
  prohibitions:
    - "No edit to `supabase/schema/baseline.sql` or the marker, and no dump. Neither this plan nor any executor runs a database command (per D-04)."
    - "No skip token and no credential-shaped literal in any line written here or in the commit message."
    - "Do not touch VERSION, package.json or CHANGELOG.md: the ship owns them."
---

<objective>
Document the automation where the manual procedure lives, and close the ledger entry this phase
owns. Record the two human-verification items the plans deliberately do not run.

Purpose: a reader of `BASELINE.md` `## Regenerating` must learn that the procedure now runs by
itself, what makes it refuse, and that the manual route is still the fallback. The backlog must
say the item is done, and with what evidence.

Output: one paragraph in `BASELINE.md`, one closed `TODOS.md` entry, and the human-verification
list in SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-CONTEXT.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-04-SUMMARY.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-08-SUMMARY.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-09-SUMMARY.md
@supabase/schema/BASELINE.md
@TODOS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BASELINE.md `## Regenerating` gains the automation paragraph (D-19)</name>
  <precondition>`npx vitest --version` prints a version in this worktree; if it does not, run `npm ci` first. Plans 04, 08 and 09 are merged: `grep -c '^  redump-pr:' .github/workflows/supabase-migrate.yml` prints 1 and `src/__tests__/baseline-redump-wiring.test.ts` exists.</precondition>
  <files>supabase/schema/BASELINE.md</files>
  <read_first>supabase/schema/BASELINE.md (sections `## Provenance`, `## Regenerating` in full, including "**Regenerate-then-shrink.**", the SP-M03 paragraph and the `**Version-skew caveat**` paragraph above it), .github/workflows/supabase-migrate.yml (jobs `redump-dump`, `redump-pr`), scripts/baseline-redump.mjs (header comment and mode list), src/__tests__/baseline-wiring-claim.test.ts (what it reads from BASELINE.md)</read_first>
  <action>
  Insert ONE paragraph immediately after the "**Regenerate-then-shrink.**" paragraph in
  `## Regenerating`, in the file's register (bold lead-in, ⛔ for prohibitions, dated). Lead with
  "⭐ **AUTOMATED 2026-09-26 (Phase 164.9.5 AUTOREDUMP).**" Then say, in prose that cites jobs and
  modes by NAME and never by line number:
  - After `supabase-migrate.yml`'s `apply` job succeeds on `main`, the `redump-dump` job re-dumps
    PRODUCTION read-only with the same credential set as `apply`, and `redump-pr` proposes the
    result as ONE pull request from `automation/baseline-redump`, reused across applies (per D-11).
  - What it refuses on, as one list: any hit of the five-class scan above (count and line numbers
    only are printed); any gitleaks finding over the file (explicit `.gitleaks.toml`, redacted,
    inline allow comments ignored, a missing or empty dump refused rather than read as clean); a
    NUL byte, a `SET client_encoding` count other than one, or a home path; zero tables or any
    data statement; a marker not taken from the applied merge; a red currency, content-drift or
    staleness gate on the composed tree; a skip token in the commit or PR text; and a human commit
    already on the bot branch.
  - It writes only the six paths #864 changed. It never writes the "what it adds" column; the
    PR body asks the reviewer to add it (per D-16).
  - ⛔ It never merges. Its CI runs wait for a human to click **Approve workflows to run**, and
    because branch protection is off, a merge with zero completed checks is possible. Read each
    head-SHA run's conclusion before merging (per D-13, D-23).
  - When the dump and marker are byte-identical to the committed pair, it opens nothing (per D-10).
  - The manual procedure above REMAINS the fallback, for a refusal that needs a human reading of
    the dump, or when the automation is unavailable.
  Do NOT edit the `grep -anE` line, the marker recipe, the SP-M03 paragraph, the `## Provenance`
  table, any `### Regenerated` section, or the `**Version-skew caveat**` paragraph. The caveat
  stays as written until the post-merge dispatch measures byte identity (RESEARCH A1).
  After the task commit, record its sha in SUMMARY; the acceptance diff is evaluated against it.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/baseline-redump-wiring.test.ts src/__tests__/baseline-wiring-claim.test.ts && node scripts/check-baseline-staleness.mjs && [ "$(grep -c 'AUTOMATED 2026-09-26 (Phase 164.9.5 AUTOREDUMP)' supabase/schema/BASELINE.md)" = "1" ]</automated>
    <fails_when>vitest exits non-zero (the pattern-equality pin broke because the grep line moved, or the wiring-claim test disagrees); the staleness gate exits 1 (a second full-sha row or an edited sha row); or the paragraph marker count is not exactly 1</fails_when>
  </verify>
  <acceptance_criteria>
    - Evaluated against the task's own commit, never the working tree (after the per-task commit, a diff with no revision range is empty and every check would pass vacuously): `C="$(git log -1 --format=%H -- supabase/schema/BASELINE.md)"` names a commit whose subject contains `164.9.5` (capture `SUBJ="$(git log -1 --format=%s "$C")"` first, then `[[ "$SUBJ" == *164.9.5* ]]` holds); then with `D="$(mktemp)"` and `git diff -U0 "$C~1" "$C" -- supabase/schema/BASELINE.md > "$D"`, `sed -n '/^@@/,$p' "$D" | grep -c '^-'` prints 0 (additions only), `sed -n '/^@@/,$p' "$D" | grep -c '^+'` is at least 1 (the diff is not empty), and `sed -n '/^@@/,$p' "$D" | grep -cE '^\+.*\| sha256 \|'` prints 0. The `sed -n '/^@@/,$p'` prefix drops only the file header (`diff --git`, `index`, `--- a/`, `+++ b/`, all before the first hunk) and keeps every hunk line, so a removed line whose own content begins with `-` (a `- [ ]` bullet shows as `-- [ ]`) is still counted; a `^-[^-]` filter would drop it and let a removed bullet pass as additions-only. Cross-check with `git diff --numstat "$C~1" "$C" -- supabase/schema/BASELINE.md`, whose second column must be `0`. Fails when: the removed count is not 0, the added count is 0, or a sha256 row was added. This task is the phase's only in-worktree edit of BASELINE.md (the bot's compose runs only in throwaway clones), so `C` is stable when the verifier re-runs it later.
    - `grep -c 'automation/baseline-redump' supabase/schema/BASELINE.md` ≥ 1, `grep -c 'Approve workflows to run' supabase/schema/BASELINE.md` ≥ 1, and `grep -ciE 'fallback' supabase/schema/BASELINE.md` ≥ 1.
    - The three verify commands pass.
  </acceptance_criteria>
  <done>A reader of `## Regenerating` learns the procedure is automated, what it refuses on, that it never merges, and that the manual route is the fallback. Every pin that reads BASELINE.md is green.</done>
</task>

<task type="auto">
  <name>Task 2: close `[164.9.5-MANUAL-BASELINE-REDUMP]` and record the human-verification items</name>
  <files>TODOS.md</files>
  <read_first>TODOS.md (the `[164.9.5-MANUAL-BASELINE-REDUMP]` entry, found by grepping its id; the closed entries' shape, found by grepping `^- \[x\]`), .planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-01-SUMMARY.md through 164.9.5-04-SUMMARY.md and 164.9.5-07-SUMMARY.md through 164.9.5-09-SUMMARY.md (mode names and the wiring test's pin list; no count is copied from them)</read_first>
  <action>
  First measure: `grep -c '164.9.5-MANUAL-BASELINE-REDUMP' TODOS.md` must be at least 1 at HEAD.
  At plan time the entry existed ONLY on this branch, added by planning commit `9d763de02`, and
  NOT on `origin/main`. If the count is 0 after plan 01's merge, STOP and report; do not
  re-create the entry from memory.

  Flip that entry's `- [ ]` to `- [x]` and append, indented in the entry's own style, a dated
  closing paragraph starting with "✅ **CLOSED 2026-09-26 by Phase 164.9.5 AUTOREDUMP.**" It
  names: `scripts/baseline-redump.mjs` (its modes by name, and its two self-test commands,
  `node scripts/baseline-redump.mjs --self-test` and `node scripts/baseline-redump.mjs --self-test --with-gitleaks`,
  named WITHOUT an assertion count: write the OK-line shape as `baseline-redump self-test OK: <n> assertion(s)`
  and `... (with gitleaks)`, and add "regenerate the count by running the command". Do NOT copy a
  count from any earlier plan's SUMMARY: plans 07, 03 and 08 raise `EXPECTED_ASSERTIONS` after
  plan 02, and plan 06 (wave 7, after this plan) edits the same script again, so any count written
  here would be stale by the phase's last commit. Run both commands at this task's HEAD anyway,
  and record both OK lines verbatim in SUMMARY bound to `git rev-parse HEAD`, as evidence the
  modes pass at the writing commit, never in TODOS.md); the `redump-dump`
  and `redump-pr` jobs in `supabase-migrate.yml`; `src/__tests__/baseline-redump-wiring.test.ts`;
  and the `BASELINE.md` `## Regenerating` paragraph. Then add one sentence: "Live proof is
  post-merge and human-run: dispatch `supabase-migrate.yml` on main and expect a no-op or one bot
  PR, and approve the workflows on the first bot PR." Keep the original entry text as lineage.
  Cite by symbol, never by line number.

  In SUMMARY (not in any tracked file other than TODOS), write a `## Human verification (post-merge,
  not run by any plan)` section with these two items, for the verifier and the founder UAT queue:
  1. D-18: `gh workflow run supabase-migrate.yml --ref main`. Expect `apply-test` to take the
     shared-TEST mutex (a queue of up to 3600 s) and apply zero migrations. Expect `apply` to
     apply zero. Then `redump-dump` should run end to end and, if byte identity holds
     (assumption A1), print its `::notice::` no-op with `changed=false`, leaving `redump-pr`
     skipped. If it produces a diff instead, the diff IS the first bot PR, and a human reads it
     hunk by hunk. Bind the reading to the run's head sha with the SHA-bound count command from
     CLAUDE.md, and read each run's conclusion. If the push is refused, apply RESEARCH F10's
     extraheader fallback (assumptions A3/A4).
  2. D-23: on the first bot PR, click **Approve workflows to run**, then list the head-SHA runs
     with their conclusions (`gh api "repos/<owner>/<repo>/actions/runs?head_sha=<sha>"` with
     a jq that prints name, status and conclusion), and merge only on a full green set. If no
     approval banner appears, use the close-and-reopen fallback (assumption A2).
  Also record in SUMMARY: this phase's own VERSION bump and CHANGELOG entry are written at ship
  time by `/gsd-ship` per CLAUDE.md "CHANGELOG discipline", in the same `chore(release)` commit
  as the VERSION bump. At ship, confirm that the filtered `-pr` branch's `TODOS.md` diff carries
  this entry and its closure. The entry arrived in a planning commit, so it must survive the
  filter to reach main. Record this task's commit sha in SUMMARY; the acceptance diff is
  evaluated against it.
  </action>
  <verify>
    <automated>awk '/164\.9\.5-MANUAL-BASELINE-REDUMP/{print; exit}' TODOS.md | grep -E '^- \[x\]' && grep -c 'CLOSED 2026-09-26 by Phase 164.9.5 AUTOREDUMP' TODOS.md | grep -qx '1' && npm run check:planning-hygiene</automated>
    <fails_when>the entry's first line is not `- [x]` (awk prints nothing or an unchecked box), the closing marker count is not exactly 1, or the hygiene gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - The entry line begins `- [x]` and carries the closing paragraph exactly once.
    - `grep -cE 'baseline-redump self-test OK: [0-9]' TODOS.md` prints 0 (no assertion count is written into the ledger; fails when a count was copied in), and SUMMARY carries both self-test OK lines verbatim beside the `git rev-parse HEAD` sha they were measured at.
    - Evaluated against the task's own commit: `C="$(git log -1 --format=%H -- TODOS.md)"` names a commit whose subject contains `164.9.5`; then with `D="$(mktemp)"` and `git diff -U0 "$C~1" "$C" -- TODOS.md > "$D"`, `sed -n '/^@@/,$p' "$D" | grep -c '^-'` prints 1 (only the checkbox line changed among existing lines; it shows as `-- [ ]`, which is why the count keeps every hunk line starting with `-` and drops only the pre-hunk file header) and `sed -n '/^@@/,$p' "$D" | grep -c '^+'` is at least 2 (the flipped checkbox and the closing paragraph). Cross-check with `git diff --numstat "$C~1" "$C" -- TODOS.md`: removed column `1`, added column at least `2`. Fails when: the removed count is not exactly 1 (0 means the checkbox was not flipped in this commit; more than 1 means existing lineage text was edited) or the added count is below 2. If a later commit on the branch touches TODOS.md before verification, record that commit's sha in SUMMARY and evaluate against this task's commit sha recorded there instead.
    - SUMMARY has the `## Human verification (post-merge, not run by any plan)` section with both items, and the ship-time VERSION/CHANGELOG note.
    - No line added here contains a skip token, a home path or a project ref (the hygiene gate passes).
  </acceptance_criteria>
  <done>The ledger entry is closed with evidence, and the two live checks are recorded for a human, not run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ledger text → public repo and commit messages | everything written here is world-readable and may be quoted in a squash body |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.5-29 | Repudiation | skip token quoted in docs, ledger or the executor's commit message | medium | mitigate | the plan text never spells a token; the executor writes "the skip tokens" in prose; the hygiene gate runs on the result (Task 2) |
| T-164.9.5-30 | Information disclosure | home path, username or project ref pasted into BASELINE.md, TODOS.md or SUMMARY | medium | mitigate | `npm run check:planning-hygiene` in Task 2's verify; no DSN or ref is ever read by this plan |
| T-164.9.5-31 | Tampering | doc edit silently breaks the pattern equality or the single-sha-row rule | medium | mitigate | wiring test and staleness gate in Task 1's verify; additions-only diff check |
| T-164.9.5-32 | Elevation of privilege | a reader assumes the bot PR is gated by CI (branch protection off) | low | mitigate | the paragraph states that the approval click and conclusion reading are required and that a zero-check merge is possible (D-23) |
</threat_model>

<verification>
The wiring, wiring-claim and staleness gates are green after the BASELINE.md edit. The TODOS
entry is closed exactly once. The hygiene gate passes. Both human-verification items are in
SUMMARY.
</verification>

<success_criteria>
D-19 is delivered: the automation is documented where the manual procedure lives, the ledger
item is closed with evidence, and the live checks are routed to a human.
</success_criteria>

## Artifacts this phase produces (this plan)

- `supabase/schema/BASELINE.md`: the `⭐ **AUTOMATED 2026-09-26 (Phase 164.9.5 AUTOREDUMP).**` paragraph in `## Regenerating`.
- `TODOS.md`: `[164.9.5-MANUAL-BASELINE-REDUMP]` closed with `✅ **CLOSED 2026-09-26 by Phase 164.9.5 AUTOREDUMP.**`.
- SUMMARY section `## Human verification (post-merge, not run by any plan)` (D-18 dispatch, D-23 approval).

<output>
Create `.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-05-SUMMARY.md` when done
</output>
