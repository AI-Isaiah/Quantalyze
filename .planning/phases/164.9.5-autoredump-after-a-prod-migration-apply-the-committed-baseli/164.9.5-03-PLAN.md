---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
plan: 03
type: execute
wave: 4
depends_on: ["164.9.5-07"]
files_modified:
  - scripts/baseline-redump.mjs
autonomous: true
requirements:
  - "164.9.5-MANUAL-BASELINE-REDUMP"

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low
  # Re-sliced 2026-09-26 (checker round 1): the writer refusals moved to plan 07 and the
  # bot-branch/PR modes to plan 08. This plan is the LAST compose-side refusal plan, so its last
  # task runs --compose end to end on the real files.

must_haves:
  truths:
    - "`--compose` refuses unless the child-gate output reads `marker-sha=match defects=0` on exactly one `baseline-currency:` line, `baseline-content-drift: findings 0` on exactly one line matching `^baseline-content-drift: findings \\d+$` (the gate prints five `baseline-content-drift:` lines, including a `SCOPE —` line, and only the findings line is judged), and a zero staleness exit. A content-drift refusal names the likely cause: main holds a newer migration that has not applied yet (per D-09)."
    - "`judgeMeasured` refuses a tampered or partial `measured.json`, and the compose job re-runs the five-class scan and the integrity judge over the downloaded dump with no third-party binary (per D-06, D-08, D-21)."
    - "The staged set equals the six `STAGED_PATHS` exactly; an extra path (for example `supabase/.temp/project-ref`) refuses (per D-17)."
    - "The commit message, PR title and PR body are checked against the six CI skip tokens before commit; a hit refuses (per D-14)."
    - "After the last compose-side refusal lands, `--compose` run on the REAL files in a throwaway clone of the branch prints `committed=true`, commits exactly the six paths as github-actions[bot], leaves the real currency, content-drift and staleness gates green, rewrites only the four `## Provenance` capture rows of the real BASELINE.md, inserts exactly one new `### Regenerated` heading, and writes a non-empty PR title and body (per D-09, D-15, D-16, D-17)."
  artifacts:
    - path: scripts/baseline-redump.mjs
      provides: "judgeCurrencyLine, judgeDriftLine, judgeMeasured, judgeStagedSet, judgeSkipTokens, the compose-side re-scan"
  key_links:
    - "`judgeDriftLine` reads the `baseline-content-drift: findings` line printed by `scripts/baseline-content-drift-check.mjs` `report`; `judgeCurrencyLine` reads the `baseline-currency:` line printed by `scripts/check-baseline-currency.mjs` through `scripts/local-stack/run.sh --check-currency`."
    - "The compose-side re-scan reuses plan 02's `judgeSecretScan` and `judgeIntegrity`, so the credentialed job and the write job judge the dump with the same code."
  prohibitions:
    - "No gitleaks and no npm install in the compose path: `redump-pr` holds the write token and runs no third-party code beyond SHA-pinned checkout, setup-node and download-artifact (RESEARCH F10 mitigation). The compose-side re-scan is the node five-class and integrity judges only."
    - "No plan artifact, commit message or CHANGELOG line quotes any of the six skip tokens. The guard's token list is assembled from fragments in the script."
    - "The end-to-end run happens in a throwaway clone. The worktree's own `supabase/schema/*`, `VERSION`, `package.json` and `CHANGELOG.md` stay unmodified."
---

<objective>
Finish the compose-side refusals and prove the whole write half on real files. The child-gate
outputs are judged by content, not only by exit code, and the drift judge reads the one line that
carries the verdict. The artifact is schema-checked and re-scanned. The staged set and the
skip-token guard refuse. Then, with every compose-side refusal in place, `--compose` runs end to
end against the real committed files in a throwaway clone and must commit.

Purpose: a refusal arm that passes on a fixture and refuses on the real file is the failure this
plan closes. The last compose-side change is followed by a real-file run in the same plan.

Output: `scripts/baseline-redump.mjs` with the compose judges, observed-RED evidence per arm,
and a recorded real-file end-to-end run.
Budget note (accepted, not a defect): the uncalibrated estimate (estimate-calibration sample_count 0,
confidence low) is 110k, 10% over the 100k smart-zone advisory budget. Accepted as recorded by
orchestrator decision at plan-check round 2: tasks and files per plan are within target, and a
further split would fragment a single mechanism across plans.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-CONTEXT.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-RESEARCH.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-02-SUMMARY.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-07-SUMMARY.md
@scripts/baseline-redump.mjs
@scripts/baseline-content-drift-check.mjs

<interfaces>
Unchanged CLI (frozen in plan 01). This plan adds no mode and no flag.

Measured stdout shapes at plan time (re-measure at execution, paste only these lines, never the
`[local-stack] … inputs:` line, which carries an absolute path):
- `bash scripts/local-stack/run.sh --check-currency` prints ONE line beginning `baseline-currency:`,
  for example `baseline-currency: carried=277 replay=0 marker-sha=match defects=0`, among
  lines with other prefixes.
- `node scripts/baseline-content-drift-check.mjs` prints FIVE lines beginning
  `baseline-content-drift:`: `chain files …`, `functions compared …`, `allowlisted rows …`,
  `findings 0`, and a long `SCOPE — …` line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: child-gate line judges scoped to the verdict line, the measured-values schema check, and the compose-side re-scan</name>
  <precondition>`npx vitest --version` prints a version in this worktree (node_modules resolve); if it does not, run `npm ci` first. `git merge-base --is-ancestor 35d1d412f HEAD` exits 0 (plan 01 Task 1).</precondition>
  <files>scripts/baseline-redump.mjs</files>
  <read_first>scripts/baseline-redump.mjs (--compose, the child-gate runner, judgeSecretScan, judgeIntegrity), scripts/check-baseline-currency.mjs (the `baseline-currency:` line format), scripts/baseline-content-drift-check.mjs (`report`: every line it prints)</read_first>
  <behavior>
    - `judgeCurrencyLine(stdout)` passes only when exactly one line begins `baseline-currency:` and it contains `marker-sha=match` and `defects=0`; `marker-sha=mismatch`, `defects=1`, two such lines, or none each refuse.
    - `judgeDriftLine(stdout)` selects the lines matching `^baseline-content-drift: findings \d+$` ONLY and passes only when there is exactly one and its number is 0. The green fixture is the REAL five-line shape (chain files, functions compared, allowlisted rows, `findings 0`, and a `SCOPE —` line), so an implementation that judges "the first `baseline-content-drift:` line" or "any line containing `findings`" fails it.
    - `judgeDriftLine`: `findings 2` refuses with a detail naming "main holds a migration that has not applied to PROD yet; the queued apply run will produce the right dump"; zero findings lines or two findings lines is MEASURE_FAIL, which refuses; a SCOPE line whose prose contains the word `findings` does not count as a findings line.
    - `judgeMeasured(json)` refuses a `measured.json` missing any schema-1 key from plans 01 and 02, with `gitleaks` other than `"clean"`, with `secret_scan_hits` other than 0, or with a `merge`/`run_id` failing the plan-02 validators.
    - The compose-side re-scan runs `judgeSecretScan` and `judgeIntegrity` over the downloaded dump again (defence in depth, no gitleaks binary) and refuses on a hit.
  </behavior>
  <action>
  Add the three judges and the re-scan, and wire them into `--compose` in this order:
  `judgeMeasured`, the re-scan of the artifact dump, the second D-10 no-op check (already there),
  the file copies, then the child gates with `judgeCurrencyLine` and `judgeDriftLine` over their
  captured stdout plus a zero exit (per D-09). A judge that sees no verdict line is MEASURE_FAIL,
  never a pass: a gate that printed nothing is not a green gate.

  Per RESEARCH Open Question 4 (RESOLVED), `dump-sql-functions.ts --check` is NOT run here: it
  needs `npm ci`, which would put third-party code in the write-token job. CI runs it on the bot
  PR through `sql-function-snapshot.yml` after approval. Say so in a comment.

  RED first per arm; for `judgeDriftLine`, also observe a first-line implementation go RED on the
  real five-line fixture in a scratch copy. Update `EXPECTED_ASSERTIONS`.
  </action>
  <verify>
    <automated>node scripts/baseline-redump.mjs --self-test | grep -E '^baseline-redump self-test OK: [0-9]+ assertion' && node scripts/baseline-content-drift-check.mjs | grep -cxE 'baseline-content-drift: findings [0-9]+' | grep -qx 1</automated>
    <fails_when>self-test exits 1 or lacks its OK line; or the real drift gate no longer prints exactly one findings line (the judge's premise changed; re-measure and adapt the judge by meaning, never loosen it to "first line")</fails_when>
  </verify>
  <acceptance_criteria>
    - `--self-test` exits 0 with the updated count.
    - SUMMARY records one observed RED per arm, including the first-line `judgeDriftLine` variant going RED on the five-line fixture.
  </acceptance_criteria>
  <done>Compose refuses on any gate line that is not the green verdict, judges the drift gate on its findings line only, and re-scans the artifact without any third-party binary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: staged-set and skip-token refusals before the commit</name>
  <files>scripts/baseline-redump.mjs</files>
  <read_first>scripts/baseline-redump.mjs (--compose staging and commit, composeCommitMessage, composePrTitle, composePrBody), CLAUDE.md section "CI gate integrity" §1 (the six skip tokens; read them there, never copy them into a plan or commit message)</read_first>
  <behavior>
    - `judgeStagedSet(list)` passes only on exactly `STAGED_PATHS`; one extra (`supabase/.temp/project-ref`), one missing, or a duplicate refuses. A scratch-repo arm creates `supabase/.temp/project-ref` and proves compose never stages it.
    - `judgeSkipTokens(text)` refuses each of the five bracketed tokens and the skip-checks trailer, with and without the space after the colon and in upper case; every token is assembled from fragments inside the arm; the composed message, title and body from plan 07's templates pass.
  </behavior>
  <action>
  Wire into `--compose`, after plan 07's writers and `judgeVersionBump` and the staleness child
  gate: the explicit `git add --` over `STAGED_PATHS`, then `judgeStagedSet` over `git diff
  --cached --name-only` (per D-17), then `judgeSkipTokens` over the commit message, PR title and
  PR body (per D-14). Only then comes the commit.

  The skip-token regex follows RESEARCH F11: case-insensitive, `\s*` around the colon of the
  trailer. Build it from string fragments joined at runtime, so a repo grep for a literal token
  stays clean. Per CLAUDE.md the tokens are dangerous in commit MESSAGES and PR bodies, not in
  file contents, but the fragment form keeps this file out of any future literal-token audit.
  Refusal output names the token CLASS index, never the matched text. Export `judgeSkipTokens`
  so plan 08's `--open-or-edit-pr` re-runs the same function.

  RED first per arm. Update `EXPECTED_ASSERTIONS`.
  </action>
  <verify>
    <automated>node scripts/baseline-redump.mjs --self-test | grep -E '^baseline-redump self-test OK: [0-9]+ assertion' && [ "$(grep -ciE '\[(skip|ci|no|actions) (ci|skip|actions)\]|skip-checks' scripts/baseline-redump.mjs)" = "0" ]</automated>
    <fails_when>self-test exits 1 or lacks its OK line; or the grep count is non-zero, meaning a bracketed skip token or the unsplit trailer key was written into the script (comments included) instead of being assembled from fragments</fails_when>
  </verify>
  <acceptance_criteria>
    - `--self-test` exits 0 with the updated count.
    - The literal-token grep in the verify prints 0.
    - SUMMARY records one observed RED per arm, with arm names only and no token text.
  </acceptance_criteria>
  <done>Compose refuses on any staged-set or skip-token defect before it commits.</done>
</task>

<task type="auto">
  <name>Task 3: `--compose` end to end on the real files, after every compose-side refusal exists</name>
  <files>scripts/baseline-redump.mjs (only if a real-file defect needs a fix; the run itself uses a throwaway clone under TMPDIR)</files>
  <read_first>.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-01-PLAN.md (Task 2's verify: the clone run this task repeats and extends)</read_first>
  <action>
  Re-run plan 01 Task 2's throwaway-clone run, now that every compose-side refusal of plans 07
  and 03 is in the script, and extend it with the checks a fixture cannot give. Commit the
  script first, because the clone is taken from the branch and the verify copies the committed
  script in. The run takes the REAL committed dump plus one appended newline through
  `--gate-dump` and then `--compose` onto a clone of the branch, and must print
  `committed=true`. It then checks on the clone's real files: the last commit touches exactly
  the six `STAGED_PATHS`, authored and committed by github-actions[bot]; the real currency,
  content-drift and staleness gates are green; every line REMOVED from BASELINE.md is one of
  the four capture rows (`| Taken |`, `| Supabase CLI |`, `| sha256 |`, `| Shape |`); exactly
  one `### Regenerated … automated re-dump after Supabase Migrate run 1` heading exists;
  CHANGELOG's first `## [` heading equals VERSION; and the PR title and body files are
  non-empty.

  If any link refuses, that is a real-file defect in a writer or judge: fix it in the script by
  meaning (never by loosening a judge to pass), re-run the self-test, and re-run this verify.
  Paste the gate-dump summary line, the `committed=true` line, the composed CHANGELOG heading
  and the new `### Regenerated` heading into SUMMARY verbatim. Never paste the `[local-stack]`
  inputs line.
  </action>
  <verify>
    <automated>node scripts/baseline-redump.mjs --self-test | grep -E '^baseline-redump self-test OK: [0-9]+ assertion' && S="$(mktemp -d "${TMPDIR:-/tmp}/164.9.5-03-XXXX")" && BR="$(git branch --show-current)" && [ -n "$BR" ] && git clone -q --branch "$BR" "$PWD" "$S/c" && cp scripts/baseline-redump.mjs "$S/c/scripts/" && ( cd "$S/c" && B="$(git rev-parse HEAD)" && { cat supabase/schema/baseline.sql; printf '\n'; } > "$S/dump.sql" && node scripts/baseline-redump.mjs --gate-dump --dump "$S/dump.sql" --merge "$B" --run-id 1 --cli-version 2.98.2 --out "$S/art" | grep -E 'changed=true' && node scripts/baseline-redump.mjs --compose --in "$S/art" --out "$S/pr" | grep -E 'committed=true' && git diff --name-only "$B" HEAD > "$S/names" && [ "$(sort "$S/names" | tr '\n' ' ')" = "CHANGELOG.md VERSION package.json supabase/schema/BASELINE.md supabase/schema/baseline-carried-migrations.txt supabase/schema/baseline.sql " ] && [ "$(git log -1 --format='%an|%cn')" = "github-actions[bot]|github-actions[bot]" ] && node scripts/check-baseline-staleness.mjs && bash scripts/local-stack/run.sh --check-currency | grep -E 'marker-sha=match defects=0' && node scripts/baseline-content-drift-check.mjs | grep -xE 'baseline-content-drift: findings 0' && git diff -U0 "$B" HEAD -- supabase/schema/BASELINE.md > "$S/bdiff" && [ "$(sed -n '/^@@/,$p' "$S/bdiff" | grep -E '^-' | grep -cvE '^-\| (Taken|Supabase CLI|sha256|Shape) \|')" = "0" ] && [ "$(grep -cE '^### Regenerated .*automated re-dump after Supabase Migrate run 1$' supabase/schema/BASELINE.md)" = "1" ] && [ "$(grep -m1 '^## \[' CHANGELOG.md | sed -E 's/^## \[([^]]+)\].*/\1/')" = "$(cat VERSION)" ] && [ -s "$S/pr/pr-title.txt" ] && [ -s "$S/pr/pr-body.md" ] ) && P="$(git status --porcelain -- supabase/schema VERSION package.json CHANGELOG.md)" && [ -z "$P" ]</automated>
    <fails_when>non-zero exit from any link: gate-dump lacks `changed=true`; compose refuses or lacks `committed=true`; the commit touches any set other than the six paths or is not bot-authored; any real gate is red on the composed clone; a removed BASELINE.md line is not one of the four capture rows (counted over every hunk line starting with `-` after the first `@@`, so the `--- a/` header is excluded and a removed line whose content begins with `-`, such as a bullet, is still counted); the new regenerated heading count is not 1; the CHANGELOG head differs from VERSION; a PR text file is empty; or the worktree's own tracked schema/version files are modified</fails_when>
  </verify>
  <acceptance_criteria>
    - The verify passes on the committed script.
    - SUMMARY carries the four verbatim lines named in the action.
  </acceptance_criteria>
  <done>With every compose-side refusal in place, the write half commits on the real files and leaves every real gate green; the fixture-only defect class of checker round 1 cannot recur unseen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact → write job | files produced in the credentialed job cross into the job holding `contents: write` |
| child-gate stdout → compose verdict | a gate's printed verdict decides whether the bot commits |
| composed text → commit on main (squash) | the message and body reach main's history |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.5-15 | Repudiation | a skip token in a commit message or PR body suppresses CI on the PR or on main after squash | high | mitigate | `judgeSkipTokens` over message, title and body before commit (Task 2, D-14); plan 08 re-runs it before gh |
| T-164.9.5-17 | Tampering | unintended path staged (`supabase/.temp/*`) | high | mitigate | explicit `git add --` plus `judgeStagedSet` refusal (Task 2, D-17); real-file six-path check (Task 3) |
| T-164.9.5-18 | Tampering | a tampered or partial artifact composed into the PR | medium | mitigate | `judgeMeasured` schema check and a node re-scan of the dump in the write job (Task 1) |
| T-164.9.5-20 | Elevation of privilege | third-party code running beside the write token | medium | mitigate | no gitleaks, no npm install, no `dump-sql-functions.ts` in compose (Task 1 comment; plan 04 pins the job's steps) |
| T-164.9.5-36 | Tampering | a gate judge reads the wrong line (a SCOPE line, a first line) and passes a red gate or refuses a green one | medium | mitigate | `judgeDriftLine` anchored to `^baseline-content-drift: findings \d+$` with exactly-one; real five-line fixture; first-line variant observed RED (Task 1); real gate run in Task 3 |
</threat_model>

<verification>
`node scripts/baseline-redump.mjs --self-test` is green with every compose-side arm, no
skip-token literal is in the script, and `--compose` commits on the real files in a throwaway
clone with every real gate green.
</verification>

<success_criteria>
D-09, D-14 and D-17 each have an arm that can fail, and the compose half is proven on the real
committed BASELINE.md, CHANGELOG.md, package.json, VERSION, dump and marker after its last
refusal landed.
</success_criteria>

## Artifacts this phase produces (this plan)

- `scripts/baseline-redump.mjs` gains: `judgeCurrencyLine`, `judgeDriftLine`, `judgeMeasured`,
  `judgeStagedSet`, `judgeSkipTokens` (exported), the compose-side re-scan.
- SUMMARY: the real-file end-to-end lines.

<output>
Create `.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-03-SUMMARY.md` when done
</output>
