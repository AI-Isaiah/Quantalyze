---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
plan: 04
type: execute
wave: 2
depends_on: ["164.9.5-01"]
files_modified:
  - .github/workflows/supabase-migrate.yml
  - src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts
  - src/__tests__/supabase-migrate-test-first.test.ts
  - src/__tests__/critical-regressions.test.ts
autonomous: true
requirements:
  - "164.9.5-MANUAL-BASELINE-REDUMP"

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low
  # Re-sliced 2026-09-26 (checker round 1, scope_sanity): the calibrated wiring test (its own new
  # file) moved to plan 09, which depends on this plan.

must_haves:
  truths:
    - "`supabase-migrate.yml` carries two new jobs, `redump-dump` and `redump-pr`, both placed after `prod-credential-divergence-verdict:` and before `apply:`, so `apply` stays the last job in the file (per D-01 as amended by D-21, D-22)."
    - "`redump-dump` has `needs: [apply]` and `if: needs.apply.result == 'success' && github.ref == 'refs/heads/main'`; `redump-pr` has `needs: [redump-dump]` and `if: needs.redump-dump.result == 'success' && needs.redump-dump.outputs.changed == 'true' && github.ref == 'refs/heads/main'`. No `workflow_run` trigger is added (per D-01, D-02, D-21)."
    - "The PROD credential and the write token never share a job: `redump-dump` has `environment: Production` and `permissions` exactly {contents: read, packages: read} and holds the three Supabase values in step `env:` only; `redump-pr` has exactly {contents: write, pull-requests: write}, no environment, no Supabase secret, no setup-cli and no gitleaks (per D-03, D-05 as amended by D-21)."
    - "The dump is taken by `supabase link --project-ref` then `supabase db dump --linked -f` into `$RUNNER_TEMP` on setup-cli `2.98.2`, after a ghcr.io login, and is copied to the artifact only by the script after every gate passed. The artifact holds three files and has `retention-days: 1` (per D-04, D-21)."
    - "gitleaks is installed from the `8.30.1` release tarball verified against a sha256 hard-coded in the workflow, and plan 09's wiring test asserts that version equals `ci.yml`'s `GITLEAKS_VERSION` and the script's constant (per D-07 as amended by D-26)."
    - "No `run:` body in either job interpolates a `${{ }}` expression; values reach scripts through step `env:` or the runner's own environment (per D-03, D-18)."
    - "The only `git push` in the file targets `HEAD:refs/heads/automation/baseline-redump` with a force-with-lease; nothing in the file merges, auto-merges or approves a PR (per D-11, D-13, D-25)."
    - "Both checkouts keep `persist-credentials: false`; every `uses:` is SHA-pinned; there is still exactly one top-level `permissions:` block, and the workflow default stays `contents: read` (per D-05, D-22)."
    - "The header Flow list and the `No GitHub API writes` comment are corrected in the same edit, with the original sentence kept as dated lineage; no new comment claims a Production required reviewer (per D-22)."
    - "Every pre-existing pin on the file stays green: the ghcr contract gains `supabase-migrate.yml:redump-dump` in `PULLING_JOBS`, the softening-token scan covers both new jobs, and the stale `returns 11 lines` comment gets a dated correction (per D-18, D-21)."
  artifacts:
    - path: .github/workflows/supabase-migrate.yml
      provides: "jobs redump-dump and redump-pr; corrected Flow and permissions comments"
    - path: src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts
      provides: "PULLING_JOBS row for supabase-migrate.yml:redump-dump"
  key_links:
    - "Workflow steps call scripts/baseline-redump.mjs by the frozen CLI of plan 01's `<interfaces>` block."
    - "`redump-pr` reads `needs.redump-dump.outputs.changed`, which `redump-dump` maps from `steps.gate.outputs.changed`, which the script appends to `$GITHUB_OUTPUT`."
    - "The push step's lease comes from `steps.botbranch.outputs.lease` (plan 08's `--check-bot-branch`)."
  prohibitions:
    - "Do not change `apply`, `apply-test`, `apply-test-verdict`, `plan`, `dispatch-ref-guard` or `prod-credential-divergence-verdict` behaviour, and do not touch `ci.yml`'s `secret-scan` scope (WINDOWS.md entry 61)."
    - "Neither new job takes shared-TEST advisory key 61616158 or reads a TEST secret."
    - "Do not add a second top-level `permissions:` block or any `workflow_run` trigger."
    - "No new repository secret, variable, dependency or GitHub App (D-12 founder decision stays deferred)."
---

<objective>
Wire the frozen CLI of `scripts/baseline-redump.mjs` into `supabase-migrate.yml` as two jobs that
split the PROD credential from the write token, and keep every existing pin on the file green.
Plan 09 adds the calibrated wiring test that pins the security-relevant shape of both jobs.

Purpose: this plan closes ROADMAP criteria 1 and 6. A job fires only after the PROD apply
succeeded on main, reads PROD with the existing credential set under least privilege, and
never echoes it.

Output: the two jobs, the corrected comments, one `PULLING_JOBS` row, the widened softening
scan, and one dated test-comment correction.

Why this runs in wave 2 beside plan 02: the workflow text names modes that plan 08 implements in
wave 5 (`--check-bot-branch`, `--open-or-edit-pr`). Nothing runs this workflow before the phase
merges, and the frozen CLI in 164.9.5-01-PLAN.md `<interfaces>` is the contract. The only script
behaviour this plan executes is `--self-test`, which exists from plan 01.
Budget note (accepted, not a defect): the uncalibrated estimate (estimate-calibration sample_count 0,
confidence low) is 110k, 10% over the 100k smart-zone advisory budget. Accepted as recorded by
orchestrator decision at plan-check round 2: tasks and files per plan are within target, and a
further split would fragment a single mechanism across plans.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-CONTEXT.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-RESEARCH.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-PATTERNS.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-01-PLAN.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-01-SUMMARY.md
@.github/workflows/supabase-migrate.yml
@.github/workflows/migration-drift-check.yml
@src/__tests__/supabase-migrate-test-first.test.ts
@src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts

<interfaces>
Script CLI consumed (frozen in 164.9.5-01-PLAN.md `<interfaces>`; implemented across plans 01, 02, 07, 03 and 08):
- `node scripts/baseline-redump.mjs --self-test` (node + git only)
- `node scripts/baseline-redump.mjs --self-test --with-gitleaks` (needs gitleaks 8.30.1 on PATH)
- `node scripts/baseline-redump.mjs --gate-dump --dump <file> --merge <sha> --run-id <digits> --cli-version <x.y.z> --out <dir>` → `$GITHUB_OUTPUT` `changed`
- `node scripts/baseline-redump.mjs --compose --in <dir> --out <dir>` → `committed`; files `<out>/pr-title.txt`, `<out>/pr-body.md`
- `node scripts/baseline-redump.mjs --check-bot-branch --remote origin` → `lease`
- `node scripts/baseline-redump.mjs --open-or-edit-pr --title-file <f> --body-file <f>` (needs `GH_TOKEN`) → `pr_number`
- Exports read by plan 09's wiring test: `SECRET_SCAN_PATTERN`, `BOT_BRANCH`, `GITLEAKS_VERSION`.

SHA-pinned actions to reuse verbatim (PATTERNS "SHA-pinned uses"):
`actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0`,
`supabase/setup-cli@3c2f5e2ae34c34e428e8e206e2c4d21fa2d20fbf  # v2.1.1`,
`actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0`,
`actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1`,
`actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1`.

gitleaks release asset `gitleaks_8.30.1_linux_x64.tar.gz`, sha256
`551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb` (RESEARCH F4, verified
against the release checksums file).

Test helpers to COPY (house rule: wiring tests are self-contained, never a shared module), by
symbol from `supabase-migrate-test-first.test.ts`: `jobBlock` (with `NEXT_JOB_RE` /
`ANY_JOB_KEY_RE`), `liveLines`, `liveLineCount`, `calibrate`, `mutateInJob`; from
`ghcr-login-before-image-pull.contract.test.ts`: `readPermissions`, `indentOf`, `isBlank`,
`isComment`; from `gitleaks-allowlist.test.ts`: the `GITLEAKS_VERSION` pin regex.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the redump-dump job (credentialed, read-only), its ghcr roster row, and the corrected header comments</name>
  <precondition>`npx vitest --version` prints a version in this worktree (node_modules resolve); if it does not, run `npm ci` first. The base contains merge 35d1d412f (`git merge-base --is-ancestor 35d1d412f HEAD` exits 0), as plan 01 Task 1 established.</precondition>
  <files>.github/workflows/supabase-migrate.yml, src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts</files>
  <read_first>.github/workflows/supabase-migrate.yml (header Flow comment, the `C-0293(a) fix:` comment above `permissions:`, jobs `plan` (outputs shape), `prod-credential-divergence-verdict`, `apply`), .github/workflows/migration-drift-check.yml (job `check`: permissions, secret assert step, ghcr login step, VAC-04 dump step), .github/workflows/test-restore-from-baseline.yml (the upload-artifact step shape), src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts (`PULLING_JOBS`, `PULL_CMD_RE`, the permissions assertion), src/__tests__/critical-regressions.test.ts (the persist-credentials policy and SHA-pin policy, found by grepping `PERSIST_CRED_EXEMPT` and `40-hex`)</read_first>
  <action>
  Insert a bare job key `  redump-dump:` immediately after the `prod-credential-divergence-verdict`
  job and before `  apply:` (per D-22: the WR-05 calibration in `critical-regressions.test.ts`
  goes red if any job follows `apply:`, and run order comes from `needs:`). Precede it with a
  comment block in the file's voice. The comment says: this job re-dumps the PROD catalogue
  after `apply` succeeded on main (D-01 as amended by D-21). It holds the PROD credential and
  a READ-ONLY token, while `redump-pr` holds the write token and no PROD credential, so the two
  never co-reside (the ghcr contract's "workflow permissions plus packages: read, nothing
  more"). It uses `environment: Production` for the audit trail, which is NOT a stop: say that
  environment has no required reviewer, and do not claim one. A red here is deliberate
  (RESEARCH Open Question 2): every refusal class is one a human must see before a dump reaches
  a public PR, and a red run may hold the Railway deploy (the Wait-for-CI coupling, assumption
  A7).

  Job fields: `needs: [apply]`,
  `if: needs.apply.result == 'success' && github.ref == 'refs/heads/main'`,
  `runs-on: ubuntu-latest`, `timeout-minutes: 20`, `environment: Production`,
  `permissions:` with exactly `contents: read` and `packages: read`, and
  `outputs:` with `changed: ${{ steps.gate.outputs.changed }}`.
  Steps, in order:
  1. checkout at the pinned SHA with `persist-credentials: false`. The default fetch depth is
     enough: the script reads `git ls-tree` of HEAD, which is the merge.
  2. "Assert the PROD values are configured (a skip is not a pass)": the three values in step
     `env:`, with a `missing` accumulator and `::error::` plus `exit 1`, copied from
     `migration-drift-check.yml`'s assert step. It carries no `if:`.
  3. setup-node, pinned, `node-version: 22`.
  4. "Install gitleaks (sha256-pinned release binary)": step `env:` holds
     `GITLEAKS_VERSION: 8.30.1` and `GITLEAKS_SHA256: 551f6fc8…` (the full 64-hex from
     `<interfaces>`). The run body, under `set -euo pipefail`, downloads the tarball URL built
     from `${GITLEAKS_VERSION}` with `curl -fsSL`, verifies it with `sha256sum -c -` against
     `${GITLEAKS_SHA256}`, extracts only the `gitleaks` binary into `${RUNNER_TEMP}/gitleaks-bin`,
     and appends that dir to `${GITHUB_PATH}` (per D-26). It never fetches the checksums file at
     runtime.
  5. "baseline-redump self-test (with gitleaks)": `node scripts/baseline-redump.mjs --self-test --with-gitleaks`.
  6. setup-cli, pinned, `version: 2.98.2` (same pin as `apply`, per D-04).
  7. "Log in to ghcr.io before the CLI pulls its images": copy `migration-drift-check.yml`'s step
     VERBATIM (`GHCR_USER` from the actor, `GHCR_TOKEN` from the job token, both in `env:`;
     `set -euo pipefail`; the `--password-stdin` login).
  8. "Dump the PROD catalogue (read-only)": step `env:` holds exactly
     `SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD` and `SUPABASE_PROJECT_REF` (from secrets,
     secrets and vars, per D-03). The run body, under `set -euo pipefail`, does
     `mkdir -p "${RUNNER_TEMP}/redump"`, then `supabase link --project-ref
     "$SUPABASE_PROJECT_REF"`, then on its own line `supabase db dump --linked -f
     "${RUNNER_TEMP}/redump/baseline.sql"`, with NO `--schema` flag (the `BASELINE.md`
     recipe). The dump goes to `$RUNNER_TEMP`, not the tracked path. That is an implementation
     detail of D-04 (RESEARCH F3): a failed dump truncates its target and must never leave a
     tracked file half-written. Add a comment: never `cat` the dump, and the step's env echo
     is pre-existing GitHub behaviour that the bot's own output never adds to.
  9. "Gate the dump (scan, gitleaks, integrity, shape, marker)", with `id: gate`. No Supabase
     secret is in this step's env. The run body, under `set -euo pipefail`, sets
     `CLI_VERSION="$(supabase --version)"` and calls `node scripts/baseline-redump.mjs
     --gate-dump --dump "${RUNNER_TEMP}/redump/baseline.sql" --merge "${GITHUB_SHA}" --run-id
     "${GITHUB_RUN_ID}" --cli-version "${CLI_VERSION}" --out "${RUNNER_TEMP}/redump-artifact"`.
     Runner-env variables only, no expression interpolation. The no-op exit lives in the script.
  10. "Upload the gated dump (1 day)", with
     `if: steps.gate.outputs.changed == 'true'` and the pinned upload-artifact with `name:
     baseline-redump`, `path:` of the runner temp dir `redump-artifact` via the `runner.temp`
     context in `with:`, `retention-days: 1` and `if-no-files-found: error`. It has no
     `always()`: the default success() is what makes the upload reachable only after every gate
     passed (D-21).

  Header comment edits (per D-22): append to the `Flow:` list, in the same voice,
  `5. redump-dump` (re-dumps PROD after a successful `apply` on main, gates the dump, uploads
  a 1-day artifact only on a change) and `6. redump-pr` (composes the dump onto current main
  and opens or edits the one bot PR; never merges). In the `C-0293(a) fix:` comment, KEEP the
  "No GitHub API writes … `contents: read` is the entire surface" sentence and add directly
  after it: "⛔ CORRECTED 2026-09-26 (Phase 164.9.5): no longer true for the whole file. The
  workflow DEFAULT stays `contents: read`, and exactly one job, `redump-pr`, is uplifted to
  `contents: write` + `pull-requests: write`; `redump-dump` adds only `packages: read`." Do not
  edit the pre-existing required-reviewer sentences: they are out of scope (RESEARCH F1).

  In `ghcr-login-before-image-pull.contract.test.ts` `PULLING_JOBS`, add the row
  `"supabase-migrate.yml:redump-dump"`, with a reason in the roster's voice: it runs `supabase db
  dump --linked`, the VAC-04 image pull (run 36229959793 pulled the remote's postgres image).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts src/__tests__/critical-regressions.test.ts src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/mutex-dead-holder-verdict.test.ts && actionlint .github/workflows/supabase-migrate.yml</automated>
    <fails_when>vitest exits non-zero (a failed assertion, or "No test files found"), or actionlint prints any finding and exits 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^  [A-Za-z_][A-Za-z0-9_-]*:' .github/workflows/supabase-migrate.yml` lists `redump-dump:` after `prod-credential-divergence-verdict:` and before `apply:`, and `apply:` is the last job key.
    - `grep -c '^permissions:' .github/workflows/supabase-migrate.yml` prints 1.
    - `grep -c 'retention-days: 1' .github/workflows/supabase-migrate.yml` prints 1.
    - `grep -c '551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb' .github/workflows/supabase-migrate.yml` prints 1.
    - `grep -c 'CORRECTED 2026-09-26 (Phase 164.9.5)' .github/workflows/supabase-migrate.yml` ≥ 1.
    - The four vitest files pass and actionlint is clean.
  </acceptance_criteria>
  <done>The credentialed dump job exists, in the right place, with the least-privilege shape the ghcr contract requires; every existing pin on the file is green.</done>
</task>

<task type="auto">
  <name>Task 2: the redump-pr job (write token, no PROD credential), the widened softening scan, and the dated job-count correction</name>
  <files>.github/workflows/supabase-migrate.yml, src/__tests__/supabase-migrate-test-first.test.ts, src/__tests__/critical-regressions.test.ts</files>
  <read_first>.github/workflows/supabase-migrate.yml (the redump-dump job from Task 1), .github/workflows/cassette-refresh.yml (job-level permission uplift ONLY; its persisted checkout and third-party PR action are NOT copied), .github/workflows/ci.yml (a step-scoped `GH_TOKEN` step, found by grepping `GH_TOKEN: \$\{\{ github.token \}\}`), src/__tests__/supabase-migrate-test-first.test.ts (`SCANNED_JOBS`, `SOFTENING_TOKENS`, `softeningOffenders`, the "four jobs" test name and comment), src/__tests__/critical-regressions.test.ts (the `returns 11 lines` comment above `ANY_JOB_KEY_RE`)</read_first>
  <action>
  Insert a bare job key `  redump-pr:` immediately after `redump-dump` and before `apply:`, with a
  comment block. The comment explains that the job holds the write token and no PROD credential
  (D-21), and that it runs no third-party code beyond the SHA-pinned checkout, setup-node and
  download-artifact (no npm install, no gitleaks: RESEARCH F10). It explains that a PR opened
  with the job token gets `pull_request` runs in an approval-required state, so a human clicks
  Approve workflows to run (D-23). It states that the PR is never merged by automation (D-13).
  And it states that with branch protection off, `contents: write` could reach main, which is
  an ACCEPTED risk bounded by the hard-coded refspec pinned in plan 09's wiring test (D-25).

  Job fields: `needs: [redump-dump]`,
  `if: needs.redump-dump.result == 'success' && needs.redump-dump.outputs.changed == 'true' && github.ref == 'refs/heads/main'`,
  `runs-on: ubuntu-latest`, `timeout-minutes: 15`, and
  `permissions:` with exactly `contents: write` and `pull-requests: write`. There is NO
  `environment:` and no `actions: write` (per D-05 as amended by D-21, D-12).
  Steps, in order:
  1. checkout, pinned, with `ref: main`, `fetch-depth: 0` and `persist-credentials: false`
     (compose judges current main, per D-15 and RESEARCH F6; the D-24 check needs
     `origin/main`).
  2. setup-node, pinned, 22.
  3. download-artifact, pinned, `name: baseline-redump`, with `path:` being the runner temp dir
     `redump-in`.
  4. "baseline-redump self-test": `node scripts/baseline-redump.mjs --self-test`, WITHOUT the
     gitleaks flag.
  5. "Compose the re-dump onto current main", `id: compose`:
     `node scripts/baseline-redump.mjs --compose --in "${RUNNER_TEMP}/redump-in" --out "${RUNNER_TEMP}/redump-pr"`.
  6. "Refuse to overwrite a human commit on the bot branch", `id: botbranch`,
     `if: steps.compose.outputs.committed == 'true'`:
     `node scripts/baseline-redump.mjs --check-bot-branch --remote origin`. No token (public
     repo, anonymous fetch).
  7. "Push the bot branch and open or edit its PR",
     `if: steps.compose.outputs.committed == 'true'`. Step `env:` holds `GH_TOKEN` from the job
     token and `LEASE` from `steps.botbranch.outputs.lease`. That is the ONLY place the token is
     exposed (D-05). The run body, under `set -euo pipefail`, runs ONE `git push` with
     `-c credential.helper=` and a `-c` credential helper that delegates to `gh auth
     git-credential`, then
     `--force-with-lease="refs/heads/automation/baseline-redump:${LEASE}"`, remote `origin`,
     refspec `HEAD:refs/heads/automation/baseline-redump` (hard-coded, per D-25; RESEARCH F10
     assumption A3). Then it runs `node scripts/baseline-redump.mjs --open-or-edit-pr
     --title-file "${RUNNER_TEMP}/redump-pr/pr-title.txt" --body-file
     "${RUNNER_TEMP}/redump-pr/pr-body.md"`. The body contains no PR-merging or
     review-approving gh subcommand and no auto-merge flag (per D-13). Add a comment naming the
     extraheader fallback of RESEARCH F10 (assumption A4) as the remedy if the first real run's
     push is refused.
  Neither job's live lines may contain any of `SOFTENING_TOKENS`. Keep every no-op exit,
  tolerated failure and warning inside the script.

  In `supabase-migrate-test-first.test.ts`, add two constants for the new job names beside
  `GUARD_JOB` / `TEST_JOB`, and append both to `SCANNED_JOBS` (RESEARCH F2 parity). The comment
  above `SCANNED_JOBS` ("ALL FOUR JOBS PHASE 164.8 ADDED") and the test name ("in ANY of the four
  jobs the phase added") get a dated note. Keep the original wording as lineage and add
  "⛔ WIDENED 2026-09-26 (Phase 164.9.5): six jobs — the two redump jobs joined, because a
  softened redump job reports green on a refusal." Do NOT touch `SOFTENING_TOKENS` or its
  count pin: its two hand-kept siblings must move with it, and this plan does not widen the list.

  In `critical-regressions.test.ts`, add a dated correction after the `returns 11 lines`
  sentence, without changing the assertion code. First re-measure with
  `grep -c '^  [A-Za-z_][A-Za-z0-9_-]*:' .github/workflows/supabase-migrate.yml` on the edited
  file, then write "⛔ CORRECTED 2026-09-26 (Phase 164.9.5): now <measured> lines — the
  `redump-dump` and `redump-pr` jobs sit ABOVE `apply`, which is still the last job." Use the
  measured number, not 13 from memory.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/critical-regressions.test.ts src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts src/__tests__/mutex-dead-holder-verdict.test.ts && actionlint .github/workflows/supabase-migrate.yml</automated>
    <fails_when>vitest exits non-zero (the softening scan names a redump job, WR-05 goes red, the ghcr classifier finds an unrostered setup-cli job) or actionlint reports any finding</fails_when>
  </verify>
  <acceptance_criteria>
    - `awk '/^  redump-pr:/{f=1;next} /^  [A-Za-z_][A-Za-z0-9_-]*:/{f=0} f' .github/workflows/supabase-migrate.yml | grep -vE '^\s*#' | grep -cE 'secrets\.|setup-cli|gitleaks|environment:'` prints 0. Comment lines are filtered first, because this job's own comments legitimately say "no gitleaks" and "no environment".
    - `grep -E 'git .*\bpush\b' .github/workflows/supabase-migrate.yml | grep -vE '^\s*#' | grep -c .` prints 1, and `grep -E 'git .*\bpush\b' .github/workflows/supabase-migrate.yml | grep -vE '^\s*#' | grep -c 'HEAD:refs/heads/automation/baseline-redump'` prints 1. The `git .*` prefix keeps the `on: push:` trigger out of the count. The single live push names only the bot branch, and plan 09's wiring test pins this permanently.
    - `grep -c 'WIDENED 2026-09-26 (Phase 164.9.5)' src/__tests__/supabase-migrate-test-first.test.ts` ≥ 1 and `grep -c 'CORRECTED 2026-09-26 (Phase 164.9.5)' src/__tests__/critical-regressions.test.ts` ≥ 1.
    - The four vitest files pass and actionlint is clean.
  </acceptance_criteria>
  <done>The write job exists without any PROD credential, the softening scan covers both new jobs, and every pre-existing pin reads green with its prose corrected by date.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository secrets → `redump-dump` steps | the PROD access token and DB password enter the runner |
| job token (write) → `redump-pr` push step | a token able to write any branch, main included (no branch protection) |
| workflow text → shell | any `${{ }}` in a `run:` body is a script-injection surface |
| Actions artifact on a PUBLIC repo | downloadable by other users (assumption A5) for its retention window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.5-21 | Information disclosure | PROD credential exposure in logs | high | mitigate | secrets only in the dump step's `env:`; no echo; dump written to `$RUNNER_TEMP` and never printed; wiring pin on secret placement (plan 09, D-03, D-04) |
| T-164.9.5-22 | Tampering / EoP | workflow script injection | high | mitigate | no expression interpolation in any `run:` body; runner env (`GITHUB_SHA`, `GITHUB_RUN_ID`, `RUNNER_TEMP`) and step `env:` only; actionlint (Tasks 1-2) plus plan 09's run-body pin |
| T-164.9.5-23 | Elevation of privilege | token over-privilege: `contents: write` can reach main | high | accept | ACCEPTED per D-25 until a ruleset exists (founder-visible, branch protection deferred until paying clients). Bounded by the hard-coded refspec with lease, exactly one push line pinned by plan 09's test, and no third-party code in the job |
| T-164.9.5-24 | Information disclosure / EoP | credential/token co-residence | high | mitigate | two jobs (D-21): PROD secrets only in `redump-dump` (read-only token plus `packages: read`); write token only in `redump-pr`'s push step; pinned by `toEqual` permissions and the ghcr contract |
| T-164.9.5-25 | Information disclosure | artifact exposure on a public repo | medium | mitigate | upload only when `changed` and only reachable after every gate (default success, no `always()`), three files, `retention-days: 1` (D-21) |
| T-164.9.5-26 | Elevation of privilege | auto-merge of an unreviewed dump | high | mitigate | file-wide absence pin in plan 09 (D-13) plus the script's argv builder (plan 08) |
| T-164.9.5-27 | Tampering | overwrite of human edits on the bot branch | high | mitigate | `--check-bot-branch` step before the push, plus force-with-lease on the observed sha (D-24; the mode is plan 08's) |
| T-164.9.5-28 | Repudiation | skip-token suppression of CI on the bot PR | high | mitigate | guard in the script (plan 03, re-run by plan 08, D-14); the workflow adds no message text of its own |
| T-164.9.5-SC | Tampering | gitleaks binary download (supply chain) | high | mitigate | release tarball verified against a sha256 hard-coded in the workflow; the checksums file is never fetched at runtime; version pinned equal to `ci.yml` by plan 09's test (D-26) |
</threat_model>

<verification>
All four vitest files named in this plan pass, and `actionlint .github/workflows/supabase-migrate.yml`
is clean. `apply:` is still the last job, and there is one top-level `permissions:` block.
</verification>

<success_criteria>
ROADMAP criteria 1 ("a workflow runs after the PROD apply succeeds and takes a read-only dump with
the existing secret") and 6 ("never echoed, least-privilege permissions, ref-guarded to main")
hold in the workflow text; plan 09 pins them with calibrated tests.
</success_criteria>

## Artifacts this phase produces (this plan)

- Workflow jobs `redump-dump` (outputs `changed`; step ids `gate`) and `redump-pr` (step ids `compose`, `botbranch`) in `.github/workflows/supabase-migrate.yml`.
- Artifact name `baseline-redump` (retention 1 day); bot branch `automation/baseline-redump`.
- `PULLING_JOBS` row `supabase-migrate.yml:redump-dump`; `SCANNED_JOBS` gains the two redump jobs.
- Dated corrections: the workflow's `C-0293(a) fix:` comment; `supabase-migrate-test-first.test.ts` "four jobs"; `critical-regressions.test.ts` "returns 11 lines".

<output>
Create `.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-04-SUMMARY.md` when done
</output>
