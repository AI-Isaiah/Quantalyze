---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
plan: 02
type: execute
wave: 2
depends_on: ["164.9.5-01"]
files_modified:
  - scripts/baseline-redump.mjs
autonomous: true
requirements:
  - "164.9.5-MANUAL-BASELINE-REDUMP"

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low
  # Re-estimated 2026-09-26 (checker round 1): three self-test-arm tasks on one file, no workflow or
  # vitest work. Still slightly over the 100k smart zone; advisory, not re-sliced further.

must_haves:
  truths:
    - "`--gate-dump` refuses (exit 1, `::error::`, no out dir, no `changed=true`) on any hit of the five-class secret scan, and its output carries only the hit COUNT and LINE NUMBERS, never the matched text (per D-06)."
    - "grep-equivalent exit mapping is explicit: clean → continue, any hit → REFUSE, a scan that could not run → MEASURE_FAIL, which also refuses (per D-06)."
    - "gitleaks 8.30.1 runs over the dump ONLY after the script asserts the file exists and is non-empty. It runs as `gitleaks dir` with an explicit `--config .gitleaks.toml`, `--redact` and `--ignore-gitleaks-allow`; any finding refuses and prints RuleID and line only; a version other than `GITLEAKS_VERSION` is MEASURE_FAIL (per D-07 as amended by D-26)."
    - "Integrity refuses on a NUL byte, on a `SET client_encoding` count other than exactly one, and on any home-directory path (runner `/home/` or the macOS prefix built from char codes) (per D-08)."
    - "Shape counts refuse on zero tables or any data statement, and every migration basename is validated against `^[0-9]{14}_[a-z0-9_]+\\.sql$` before it is written anywhere (per D-27)."
    - "The marker lists exactly `git ls-tree` of the MERGE commit, and `--merge` differing from HEAD refuses (per D-09); a dump and marker byte-identical to the committed pair emit `changed=false` and a `::notice::` with exit 0 (per D-10)."
    - "The self-test splits by capability: `--self-test` needs only node and git, so vitest in CI and the write job can run it; `--self-test --with-gitleaks` adds the real-binary arms the dump job runs (per D-18, D-21)."
  artifacts:
    - path: scripts/baseline-redump.mjs
      provides: "judgeSecretScan, judgeIntegrity, judgeShapeCounts, judgeGitleaksReport, gitleaksArgv, assertScanTarget, --with-gitleaks self-test mode, EXPECTED_GITLEAKS_ASSERTIONS"
  key_links:
    - "SECRET_SCAN_PATTERN stays byte-equal to the `grep -anE` argument in supabase/schema/BASELINE.md `## Regenerating`; plan 09's wiring test asserts the equality."
    - "The home-prefix constant is built with `String.fromCharCode`, as `scripts/check-planning-hygiene.ts` builds its `HOME_PREFIX`, so no tracked file spells the macOS home prefix."
  prohibitions:
    - "Never commit a JWT-shaped, DSN-shaped or project-ref-shaped literal. Red fixtures are assembled from fragments at runtime in a temp dir (RESEARCH F13); `ci.yml`'s secret-scan scans the push-to-main range."
    - "No database command against any remote (per D-04)."
    - "No `exit 0` path on a failure; MEASURE_FAIL is never a pass."
---

<objective>
Harden the credentialed half of the tracer, `--gate-dump`, with every refusal of CONTEXT
section C. The additions are the five-class secret scan that prints counts and line numbers
only, gitleaks over the single file with the missing/empty-file trap closed, the integrity
checks, the shape-count refusals, the strict basename rule, and explicit arms for the
MERGE-tree marker and the D-10 no-op. Each refusal is proven able to fire by a red fixture built
at runtime.

Purpose: nothing leaves the credentialed job unless every gate passed. This plan makes that true
of the script the job calls.

Output: `scripts/baseline-redump.mjs` with the gate refusals, a `--with-gitleaks` self-test mode,
and observed-RED evidence for each arm in SUMMARY.
Budget note (accepted, not a defect): the uncalibrated estimate (estimate-calibration sample_count 0,
confidence low) is 105k, 5% over the 100k smart-zone advisory budget. Accepted as recorded by
orchestrator decision at plan-check round 2: tasks and files per plan are within target, and a
further split would fragment a single mechanism across plans.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-CONTEXT.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-RESEARCH.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-01-SUMMARY.md
@scripts/baseline-redump.mjs
@.gitleaks.toml

<interfaces>
Unchanged CLI (frozen in plan 01). This plan widens `--self-test` to accept the optional
`--with-gitleaks` flag and adds nothing else.

gitleaks facts measured locally at research time (8.30.1, RESEARCH F4):
- `gitleaks dir <file> --config .gitleaks.toml --redact --no-banner --ignore-gitleaks-allow
  --report-format json --report-path <json> --log-level error`. Exit 1 on leaks, 0 when clean.
- A MISSING or EMPTY target prints a skip warning and EXITS 0 ("no leaks found"). That is the
  false clean this plan closes.
- With `--redact` the JSON report's `Secret` and `Match` fields read `REDACTED`.
- Without `--ignore-gitleaks-allow`, a `gitleaks:allow` comment on a line suppresses its finding.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: five-class scan, integrity and shape-count refusals that never print the matched text</name>
  <precondition>`npx vitest --version` prints a version in this worktree (node_modules resolve); if it does not, run `npm ci` first. `git merge-base --is-ancestor 35d1d412f HEAD` exits 0 (plan 01 Task 1).</precondition>
  <files>scripts/baseline-redump.mjs</files>
  <read_first>scripts/baseline-redump.mjs (as plan 01 left it), supabase/schema/BASELINE.md (`## Regenerating` first code block and the SP-M03 paragraph), scripts/check-planning-hygiene.ts (`HOME_PREFIX`, built from char codes), scripts/restore-test-from-baseline.sh (the "A dump that creates nothing is not a baseline" refusal)</read_first>
  <behavior>
    - One red arm per secret class, each value assembled from fragments at runtime: DSN scheme, Supabase host, 20-letter ref subdomain, the psql connect meta-command, `ALTER DATABASE`, and a JWT-shaped token. Each arm asserts BOTH that the scan refuses with the right line number AND that the fragment-assembled value does not appear in the arm's captured stdout or stderr.
    - Integrity arms: an appended NUL byte, a second `SET client_encoding` line, a file with none, a runner `/home/` path, and a macOS-prefix path built from char codes. Each refuses.
    - Shape arms: a dump with zero `CREATE TABLE` lines refuses; a dump with one `INSERT INTO` line refuses.
    - Basename arms: a merge tree holding `supabase/migrations/2026_bad.sql` refuses by name, with no basename echoed beyond the validated form. The committed corpus passes.
    - The green committed dump passes all of the above.
  </behavior>
  <action>
  Add the pure judges and wire them into `--gate-dump` BEFORE the out dir is written, in this
  order: scan target exists and is non-empty, five-class scan, integrity, shape counts, basename
  validation. gitleaks slots in second in Task 2.

  - `judgeSecretScan(buffer)` per D-06: iterate the dump's lines as `latin1`, so a NUL-bearing
    file is never silently skipped, which is the `grep -a` equivalent. Test each line against
    `SECRET_SCAN_RE` and return the 1-based line numbers. The mode prints
    `baseline-redump secret-scan: <count> hit(s) at line(s) <n,n,…>` and refuses. On a clean
    scan it prints `baseline-redump secret-scan: 0 hit(s)`. It NEVER prints a line's text
    (PUBLIC Actions log; `BASELINE.md`'s own `-anE` command would publish the hit). If the file
    cannot be read, print `MEASURE_FAIL` and refuse. That is the grep exit ≥ 2 case in D-06's
    mapping.
  - `judgeIntegrity(buffer)` per D-08: count NUL bytes with `indexOf(0)`, count `^SET
    client_encoding` lines (it must be exactly 1), and count home-path hits against `/home/` and
    a macOS-prefix constant built with `String.fromCharCode`, as `check-planning-hygiene.ts`
    does. Never write that prefix literally in this file or in any plan artifact. Every defect
    names a count or a line number, never the path.
    D-08's "no local home path / username" clause is delivered by the home-path check, and a
    separate bare-username scan is deliberately NOT added. The reason, recorded so it reads as
    a decision and not an omission: in CI the dump is captured by the runner account, whose
    name is a common English word that occurs in legitimate catalogue text (a false positive
    would refuse every run), and the developer's own username cannot be written into a PUBLIC
    repo as a scan needle at all. A username reaches a dump through a home-directory path,
    which the `/home/` and char-code macOS-prefix arms catch. Say this in a comment above
    `judgeIntegrity`.
  - `judgeShapeCounts(shapes)` per D-27: refuse when `tables === 0` ("a dump that creates nothing
    is not a baseline", after `restore-test-from-baseline.sh`) or when `data_statements > 0` ("a
    schema-only dump carrying data is a leak class of its own").
  - Basenames per D-27: every basename from the MERGE tree's `git ls-tree` must match
    `MIGRATION_BASENAME_STRICT_RE`; a mismatch refuses by count and position. Only `.sql` entries
    are considered, so the `down` directory is dropped, as the recipe's `grep '\.sql$'` does.
  - Add `secret_scan_hits: 0` and `integrity: {nul: 0, client_encoding: 1, home_path: 0}` to
    `measured.json`.

  RED first: write the arms, run `--self-test` against the plan-01 script, observe each new arm
  fail, then implement. Record the RED output (arm names only) in SUMMARY. Update
  `EXPECTED_ASSERTIONS`.
  </action>
  <verify>
    <automated>node scripts/baseline-redump.mjs --self-test | grep -E '^baseline-redump self-test OK: [0-9]+ assertion'</automated>
    <fails_when>exit 1, `SELF-TEST FAILED` in stdout/stderr, or the OK line is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - `--self-test` exits 0 and prints the OK line with the new `EXPECTED_ASSERTIONS`.
    - `grep -c 'fromCharCode' scripts/baseline-redump.mjs` is at least 1.
    - After the task commit (the hygiene gate scans TRACKED files), `npm run check:planning-hygiene` exits 0, with no ABSOLUTE-HOME-PATH hit in the script.
    - `grep -cE 'eyJ[A-Za-z0-9_-]{10,}' scripts/baseline-redump.mjs` prints 0, so no JWT-shaped literal is committed; the red value is assembled from fragments.
    - `gitleaks dir scripts/baseline-redump.mjs --config .gitleaks.toml --redact --no-banner` exits 0, so the committed script itself carries no finding.
    - SUMMARY lists every new arm with its observed RED line.
  </acceptance_criteria>
  <done>Every section-C refusal except gitleaks fires on a runtime red fixture, and no arm's output carries the injected secret-shaped text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gitleaks over the single dump, with the missing/empty-file trap closed and a --with-gitleaks self-test</name>
  <files>scripts/baseline-redump.mjs</files>
  <read_first>scripts/baseline-redump.mjs, .gitleaks.toml (global and scoped allowlists: none names supabase/schema), .planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-RESEARCH.md (F4, Code Examples "gitleaks, redacted, with the missing-file trap closed")</read_first>
  <behavior>
    - Pure `judgeGitleaksReport({rc, reportText})`: rc 0 with a JSON array of length 0 is clean; rc 1 with a non-empty array refuses and returns RuleID plus StartLine per finding; rc 0 with a non-empty array refuses (disagreement is never clean); any other rc is MEASURE_FAIL; unparseable JSON or a non-array is MEASURE_FAIL.
    - Pure `gitleaksArgv(target, reportPath)` always contains `dir`, `--config`, `.gitleaks.toml`, `--redact`, `--ignore-gitleaks-allow`, `--report-format`, `json`; an arm asserts each.
    - `assertScanTarget(path)` refuses a missing path and a zero-byte file BEFORE gitleaks is spawned.
    - `--with-gitleaks` arms (real binary): `gitleaks version` equals `GITLEAKS_VERSION`; the committed dump is clean; a runtime JWT-shaped fixture refuses; the same fixture with a `gitleaks:allow` comment on the line STILL refuses (proves `--ignore-gitleaks-allow`); the refusal output contains `REDACTED` or RuleID/line only and not the fixture value.
  </behavior>
  <action>
  Per D-07 as amended by D-26, the script runs gitleaks itself, so the order is enforced by
  tested code rather than by workflow text. The sequence is `assertScanTarget`, then a
  `spawnSync('gitleaks', gitleaksArgv(...))` run from `repoRoot`, so `.gitleaks.toml` resolves,
  with the config always passed explicitly (CLAUDE.md: auto-loaded config from cwd tests
  nothing). Then `judgeGitleaksReport`. Before the scan, run `gitleaks version`; a version
  other than `GITLEAKS_VERSION` or a missing binary is MEASURE_FAIL, which refuses. The
  report path lives in a temp dir, never in the out dir. The mode prints `baseline-redump
  gitleaks: <n> finding(s)`, then one `RuleID=<id> line=<n>` per finding, and never
  `Secret`/`Match` content, even redacted. Add `gitleaks: "clean"` and `gitleaks_version` to
  `measured.json`. Slot this gate second in `--gate-dump`, after the target assertion and
  before the five-class scan.

  Self-test split, per D-18 and D-21: `--self-test` runs the pure arms (`judgeGitleaksReport`,
  `gitleaksArgv`, `assertScanTarget`) and needs no binary, because vitest in CI and the
  `redump-pr` job have none. `--self-test --with-gitleaks` runs all of that plus the real-binary
  arms and prints `baseline-redump self-test OK: <n> assertion(s) (with gitleaks)`. Pin a second
  constant `EXPECTED_GITLEAKS_ASSERTIONS` for the real-binary arms. A missing binary under
  `--with-gitleaks` fails loud, never skips.

  The `--gate-dump` arms that run in plain `--self-test` must not need the binary. Inject the
  gitleaks runner the way plan 01 injected the child-gate runner: plain self-test passes a fake
  returning `{rc: 0, reportText: "[]"}`, and `--with-gitleaks` passes the real one.

  RED first: the missing-file and empty-file arms must be observed to PASS as clean when
  `assertScanTarget` is bypassed in a scratch copy. That is the measured false clean. Then
  observe them refuse with it in place. Record both in SUMMARY.
  </action>
  <verify>
    <automated>node scripts/baseline-redump.mjs --self-test | grep -E '^baseline-redump self-test OK: [0-9]+ assertion' && node scripts/baseline-redump.mjs --self-test --with-gitleaks | grep -E '^baseline-redump self-test OK: [0-9]+ assertion\(s\) \(with gitleaks\)'</automated>
    <fails_when>either run exits 1, prints `SELF-TEST FAILED`, or lacks its OK line; `--with-gitleaks` on a machine without gitleaks 8.30.1 exits 1 with MEASURE_FAIL (expected locally only if the binary is absent — record it, never skip)</fails_when>
  </verify>
  <acceptance_criteria>
    - Both self-test modes exit 0 locally, where gitleaks 8.30.1 is installed (`gitleaks version` prints `8.30.1`).
    - `grep -c 'ignore-gitleaks-allow' scripts/baseline-redump.mjs` ≥ 1 and `grep -c "'--redact'" scripts/baseline-redump.mjs` ≥ 1.
    - With `PATH` stripped of gitleaks, `node scripts/baseline-redump.mjs --self-test --with-gitleaks` exits 1 and prints `MEASURE_FAIL` (observed once, recorded in SUMMARY).
    - The bypassed-assert RED (false clean on a missing and on an empty file) is recorded in SUMMARY.
  </acceptance_criteria>
  <done>gitleaks cannot report a false clean on a missing or empty dump, honours no inline allow comment, and never prints a secret value. The dump job's self-test mode exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MERGE-tree marker and D-10 no-op, as explicit refusal and idempotency arms</name>
  <files>scripts/baseline-redump.mjs</files>
  <read_first>scripts/baseline-redump.mjs, supabase/schema/BASELINE.md (`## Regenerating` second code block, the marker recipe), scripts/check-baseline-currency.mjs (`SHA_LINE_RE`, the `marker-sha-mismatch` and dump-ahead-of-checkout defects)</read_first>
  <behavior>
    - `--merge` not equal to `git rev-parse HEAD` refuses (a marker taken from a tree other than the applied merge).
    - `--merge` that is not 40 lowercase hex, `--run-id` with a non-digit, and `--cli-version` not `x.y.z` each refuse by name without echoing the value.
    - The marker's `#` header lines are byte-identical to the committed marker's; the `baseline-sha256:` line matches `SHA_LINE_RE` and equals the dump's sha256.
    - A dump byte-identical to HEAD's committed dump, with the marker unchanged, yields `changed=false`, a `::notice::`, exit 0, and no out dir files; appending one byte yields `changed=true`; a committed dump unchanged but a NEW committed migration in the merge tree yields `changed=true` (marker differs).
    - `$GITHUB_OUTPUT` receives exactly one `changed=` line per run when set, and none when unset.
  </behavior>
  <action>
  Add the arg validators and the arms above to the scratch-repo self-test (per D-09 and D-10).
  Use the `classify-changed-paths.mjs` scratch-repo shape: `git -c user.name=self-test -c
  user.email=self-test@invalid -c commit.gpgsign=false`, and `rmSync` in `finally`. Set
  `GITHUB_OUTPUT` to a temp file inside the arm and restore the environment afterwards. Refusal
  messages name the flag and the rule, never the rejected value (a value can be arbitrary text
  from the runner). Update `EXPECTED_ASSERTIONS`. RED first: observe the `--merge`≠HEAD arm pass
  as clean when the equality check is removed in a scratch copy, then restore it.
  </action>
  <verify>
    <automated>node scripts/baseline-redump.mjs --self-test | grep -E '^baseline-redump self-test OK: [0-9]+ assertion' && H="$(git rev-parse HEAD)" && O="$(mktemp -d "${TMPDIR:-/tmp}/164.9.5-02-XXXX")" && node scripts/baseline-redump.mjs --gate-dump --dump supabase/schema/baseline.sql --merge "$H" --run-id 1 --cli-version 2.98.2 --out "$O/o" | grep -E '^::notice::.*no PR|changed=false'</automated>
    <fails_when>self-test exits 1 or lacks its OK line; the gate-dump run over the committed dump at HEAD exits non-zero or prints neither the `::notice::` no-op line nor `changed=false`</fails_when>
  </verify>
  <acceptance_criteria>
    - Both commands above succeed on the merged worktree (the gitleaks gate runs for real in the second command, so gitleaks 8.30.1 must be on PATH).
    - `node scripts/baseline-redump.mjs --gate-dump --dump supabase/schema/baseline.sql --merge 0000000000000000000000000000000000000000 --run-id 1 --cli-version 2.98.2 --out <tmp>` exits 1 with an `::error::` naming `--merge`.
    - SUMMARY records the removed-equality RED.
  </acceptance_criteria>
  <done>The marker is provably taken from the applied merge, the no-op path is idempotent and quiet, and every gate-side decision has an observed-RED arm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD dump → public artifact / PR | the only path a secret in the catalogue could reach the public |
| scanner output → public Actions log | a scanner that prints its hit publishes the secret |
| runner env values → script args | run id, sha and CLI version enter as argv |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.5-06 | Information disclosure | five-class scan output | high | mitigate | counts and line numbers only; each class arm asserts the fragment-built value is absent from captured output (Task 1, D-06) |
| T-164.9.5-07 | Information disclosure | silent false-clean scan (gitleaks exits 0 on a missing or empty target) | high | mitigate | `assertScanTarget` before spawn; bypass RED observed (Task 2, D-26) |
| T-164.9.5-08 | Information disclosure | inline `gitleaks:allow` in a PROD function comment suppressing a finding | high | mitigate | `--ignore-gitleaks-allow` in `gitleaksArgv`, real-binary arm (Task 2) |
| T-164.9.5-09 | Information disclosure | gitleaks printing the secret | high | mitigate | `--redact`; the script prints RuleID and line only (Task 2, D-07) |
| T-164.9.5-10 | Tampering | gitleaks config auto-loaded from cwd or dropped | medium | mitigate | explicit `--config .gitleaks.toml` asserted in `gitleaksArgv`; version pinned to `GITLEAKS_VERSION` (Task 2) |
| T-164.9.5-11 | Information disclosure | data rows or home paths in a schema dump | medium | mitigate | data-statement and home-path refusals (Task 1, D-08, D-27) |
| T-164.9.5-12 | Tampering | crafted migration basename injected into commit text | medium | mitigate | strict 14-digit basename regex before any write (Task 1, D-27) |
| T-164.9.5-13 | Tampering | arg values echoed into logs | low | mitigate | validators name the flag, never the value (Task 3) |
</threat_model>

<verification>
`node scripts/baseline-redump.mjs --self-test` and `--self-test --with-gitleaks` are both green
locally. `--gate-dump` over the committed dump at HEAD is a quiet no-op. Every arm's RED is
recorded.
</verification>

<success_criteria>
ROADMAP criterion 2 ("scan and refuse") holds in the script the dump job calls. Criterion 3's
MERGE-tree rule and D-10's idempotency are pinned by arms that can fail.
</success_criteria>

## Artifacts this phase produces (this plan)

- `scripts/baseline-redump.mjs` gains: `judgeSecretScan`, `judgeIntegrity`, `judgeShapeCounts`,
  `judgeGitleaksReport`, `gitleaksArgv`, `assertScanTarget`, the `--with-gitleaks` self-test
  flag, `EXPECTED_GITLEAKS_ASSERTIONS`.
- `measured.json` gains `secret_scan_hits`, `gitleaks`, `gitleaks_version`, `integrity`.
- Output lines: `baseline-redump secret-scan: …`, `baseline-redump gitleaks: …`, `baseline-redump self-test OK: <n> assertion(s) (with gitleaks)`.

<output>
Create `.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-02-SUMMARY.md` when done
</output>
