---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
plan: 09
type: execute
wave: 3
depends_on: ["164.9.5-04"]
coupling_justified: ["164.9.5-07: same wave 3, file-disjoint (07 edits scripts/baseline-redump.mjs, 09 edits only the wiring test). 09 reads that script at run time (imports SECRET_SCAN_PATTERN, BOT_BRANCH, GITLEAKS_VERSION and spawns --self-test), but 07 changes none of those three constants, and 09 matches the self-test OK line by shape (^baseline-redump self-test OK: \\d+ assertion) without pinning EXPECTED_ASSERTIONS, which 07 raises. Either execution order leaves 09 green, so parallel is safe; a red self-test in 09 would mean 07 broke the script and is caught by 07's own verify too."]
files_modified:
  - src/__tests__/baseline-redump-wiring.test.ts
autonomous: true
requirements:
  - "164.9.5-MANUAL-BASELINE-REDUMP"

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low
  # Added 2026-09-26 (checker round 1, scope_sanity): plan 04's Task 3, moved verbatim into its
  # own plan because it is its own new file and plan 04 was the heaviest plan at 170000.

must_haves:
  truths:
    - "A calibrated vitest wiring test pins the job order, `needs:`/`if:`, `environment:`, `permissions:`, credential placement, the absence of `${{ }}` in every `run:` body, the setup-cli and gitleaks pins, the dump line, the upload gate, `persist-credentials: false`, the absence of any merge/auto-merge/approve token, the single bot-branch push refspec, and the BASELINE.md scan-pattern equality, and every pin has a calibration observed RED (per D-01..D-06, D-11, D-13, D-18, D-21, D-22, D-25, D-26)."
    - "The wiring test's spawn of `node scripts/baseline-redump.mjs --self-test` carries an explicit per-test timeout, because the self-test builds scratch git repos and reads the real dump, which exceeds vitest's default (per D-18)."
  artifacts:
    - path: src/__tests__/baseline-redump-wiring.test.ts
      provides: "calibrated wiring pins for D-01..D-05, D-11, D-13, D-18, D-21, D-22, D-25, D-26 and the BASELINE.md pattern equality"
  key_links:
    - "The test imports `SECRET_SCAN_PATTERN`, `BOT_BRANCH` and `GITLEAKS_VERSION` from scripts/baseline-redump.mjs (plan 01); the `invokedDirectly()` guard keeps its main from running."
    - "The test reads `.github/workflows/supabase-migrate.yml` as plan 04 left it, and `ci.yml`'s secret-scan `GITLEAKS_VERSION` pin."
  prohibitions:
    - "Helpers are COPIED by symbol, never imported from another test (house rule)."
    - "The test file never spells a PR-merge command or the auto flag; those tokens are assembled from fragments."
---

<objective>
Pin every security-relevant property of the `redump-dump` and `redump-pr` jobs with a calibrated
wiring test, so a later edit that softens either job goes red.

Purpose: ROADMAP criteria 1 and 6 hold in plan 04's workflow text; this plan makes them hold
permanently.

Output: `src/__tests__/baseline-redump-wiring.test.ts`, with each pin's observed RED in SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-CONTEXT.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-04-PLAN.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-04-SUMMARY.md
@.github/workflows/supabase-migrate.yml
@src/__tests__/supabase-migrate-test-first.test.ts

<interfaces>
Test helpers to COPY (house rule: wiring tests are self-contained, never a shared module), by
symbol from `supabase-migrate-test-first.test.ts`: `jobBlock` (with `NEXT_JOB_RE` /
`ANY_JOB_KEY_RE`), `liveLines`, `liveLineCount`, `calibrate`, `mutateInJob`; from
`ghcr-login-before-image-pull.contract.test.ts`: `readPermissions`, `indentOf`, `isBlank`,
`isComment`; from `gitleaks-allowlist.test.ts`: the `GITLEAKS_VERSION` pin regex.

Script exports read: `SECRET_SCAN_PATTERN`, `BOT_BRANCH`, `GITLEAKS_VERSION`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: job-shape pins (order, needs/if, environment, permissions, credentials, run bodies, tool pins, dump and upload, checkouts)</name>
  <precondition>`npx vitest --version` prints a version in this worktree; if it does not, run `npm ci` first. Plan 04 is merged: `grep -c '^  redump-pr:' .github/workflows/supabase-migrate.yml` prints 1.</precondition>
  <files>src/__tests__/baseline-redump-wiring.test.ts</files>
  <read_first>src/__tests__/supabase-migrate-test-first.test.ts (its header rule on copied helpers; `jobBlock`, `liveLines`, `liveLineCount`, `calibrate`, `mutateInJob`, the line-exact `if:` pin and its calibration), src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts (`readPermissions`), src/__tests__/gitleaks-allowlist.test.ts (the `GITLEAKS_VERSION` pin regex and how it slices `ci.yml`'s secret-scan block), .github/workflows/supabase-migrate.yml (jobs `redump-dump`, `redump-pr`)</read_first>
  <behavior>
    - Order: `prod-credential-divergence-verdict` < `redump-dump` < `redump-pr` < `apply`, and no job key follows `apply`. Calibration: moving `redump-pr` after `apply` goes RED.
    - Line-exact `needs:` and `if:` for both jobs (D-01, D-02, D-21). Calibrations: dropping the ref clause from either `if:`, and dropping the `changed` clause from `redump-pr`'s, each go RED.
    - `redump-dump` has a live `environment: Production` line and `redump-pr` has none (D-03, D-21).
    - Permissions via the copied `readPermissions`, compared with `toEqual`: `{contents: read, packages: read}` and `{contents: write, pull-requests: write}`. Calibration: adding `actions: write` to either goes RED (D-05).
    - Credential placement: in `redump-pr`, no live line references the secrets context or `SUPABASE_`; in `redump-dump`, every live line referencing the secrets context is an `env:` child `KEY: <expr>` line. Calibration: seeding a secret into `redump-pr` goes RED.
    - No expression interpolation in any `run:` body of either job: a parser collects each `run: |` block's more-indented lines and asserts none contains the expression opener. Calibration: seeding one into the gate step's body goes RED (D-03, D-18).
    - Pins: setup-cli `version: 2.98.2` in `redump-dump` (D-04); the workflow's `GITLEAKS_VERSION:` in `redump-dump` equals `ci.yml`'s secret-scan pin AND the script's exported `GITLEAKS_VERSION`; one 64-hex sha256 and a `sha256sum -c` line (D-26). Calibration: a version mismatch goes RED.
    - Self-test invocations: `redump-dump` runs the gitleaks-flag form, `redump-pr` runs the plain form and contains no gitleaks string in its `liveLines` (comment lines excluded, matching plan 04 Task 2's comment-filtered count, because this job's own comments legitimately say "no gitleaks"); an occurrence on any live line fails the pin.
    - Dump line: `supabase db dump --linked -f` present, with no `--schema` in `redump-dump` (D-04).
    - Upload: `retention-days: 1`, gated on `steps.gate.outputs.changed == 'true'`, and no `always()` in `redump-dump` (D-21).
    - Both jobs' checkouts carry `persist-credentials: false` (D-05, D-22). Calibration: removing one goes RED.
  </behavior>
  <action>
  Create the file in the house wiring-test idiom. Copy the helpers by symbol (never import them
  from another test). Every predicate gets a `calibrate(...)` mutant that must flip it. Failure
  messages say what broke and what to do, in the voice of `supabase-migrate-test-first.test.ts`.
  Read the workflow from disk with `readFileSync`, and import the script's exports with a
  dynamic `import()` of `scripts/baseline-redump.mjs`. Its `invokedDirectly()` guard keeps main
  from running.

  The run-body scan (D-03) is one a comment-only mention could fool, so it operates on
  `liveLines` only. That mirrors `softeningOffenders`, and a MISSING job counts as an offender
  ("an absent job scans clean, which is how a corpus quietly becomes empty").

  RED first: seed each mutant and record in SUMMARY that each pin was observed RED at least once.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/baseline-redump-wiring.test.ts</automated>
    <fails_when>non-zero exit, a failed assertion or calibration, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/baseline-redump-wiring.test.ts` passes.
    - SUMMARY lists every Task 1 pin with its observed RED.
  </acceptance_criteria>
  <done>Every job-shape property of both jobs is pinned by a test that has been seen to fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: file-wide absence and push pins, the scan-pattern equality, and the timed self-test spawn</name>
  <files>src/__tests__/baseline-redump-wiring.test.ts</files>
  <read_first>src/__tests__/baseline-redump-wiring.test.ts (Task 1), src/__tests__/lint-migration-data-dependence.test.ts (spawning a script's `--self-test`), supabase/schema/BASELINE.md (`## Regenerating` first code block)</read_first>
  <behavior>
    - File-wide absence of a PR merge, auto-merge, review approval, or `automerge` token. The tokens are assembled from string fragments in the test, so the test file itself never spells the command. Calibration: seeding one goes RED (D-13).
    - Every live `git push` line in the whole file contains `HEAD:refs/heads/` followed by the script's `BOT_BRANCH`, and there is exactly one. Calibration: a seeded push to `main` goes RED (D-25).
    - `SECRET_SCAN_PATTERN` imported from the script equals the single-quoted argument of the `grep -anE` line in `BASELINE.md` `## Regenerating` (D-06). Calibration: a one-character edit goes RED.
    - Spawning `node scripts/baseline-redump.mjs --self-test` exits 0 and prints a line matching `^baseline-redump self-test OK: \d+ assertion`. It does NOT pin the number, because plans 02, 07, 03 and 08 move `EXPECTED_ASSERTIONS`. The `it(...)` carries an explicit per-test timeout of 120000 ms and the `spawnSync` call its own `timeout` of 110000 ms, so a hung self-test fails with a named timeout rather than vitest's default 5000 ms cutting a healthy run short.
  </behavior>
  <action>
  Add the arms above. The absence scans (D-13) operate on `liveLines` only, like Task 1's
  run-body scan. The spawn arm checks `status === 0` and the OK line, and on failure prints the
  child's stderr tail so a red CI run names the failing arm.

  RED first: seed each mutant; for the spawn arm, point it at a flag the script refuses and
  observe the arm go RED. Record every RED in SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/baseline-redump-wiring.test.ts</automated>
    <fails_when>non-zero exit, a failed assertion or calibration, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/baseline-redump-wiring.test.ts` passes.
    - `grep -c 'calibrate(' src/__tests__/baseline-redump-wiring.test.ts` is at least 12 across both tasks.
    - `grep -cE "gh pr merge|--auto\b" src/__tests__/baseline-redump-wiring.test.ts` prints 0 (tokens are assembled from fragments). <!-- planner-discipline-allow: gh pr merge -->
    - `grep -cE '120_?000' src/__tests__/baseline-redump-wiring.test.ts` is at least 1.
    - SUMMARY lists every Task 2 pin with its observed RED.
  </acceptance_criteria>
  <done>No merge path, no second push target and no drift between the documented scan pattern and the script can land without a red test, and the self-test spawn has a bounded, named timeout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow text → future edits | a later change can soften a job without anyone reading this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.5-21 | Information disclosure | PROD credential exposure in logs | high | mitigate | credential-placement pin with calibration (Task 1) |
| T-164.9.5-22 | Tampering / EoP | workflow script injection | high | mitigate | run-body expression pin on live lines with calibration (Task 1) |
| T-164.9.5-24 | Information disclosure / EoP | credential/token co-residence | high | mitigate | `toEqual` permissions pins and environment pins (Task 1) |
| T-164.9.5-26 | Elevation of privilege | auto-merge of an unreviewed dump | high | mitigate | file-wide absence pin with calibration (Task 2) |
| T-164.9.5-SC | Tampering | gitleaks binary download (supply chain) | high | mitigate | version equality with `ci.yml` and the script, sha256 and `sha256sum -c` pins (Task 1) |
</threat_model>

<verification>
`npx vitest run src/__tests__/baseline-redump-wiring.test.ts` passes, with at least 12
calibrations, each observed RED.
</verification>

<success_criteria>
ROADMAP criteria 1 and 6 are pinned by calibrated tests (D-01..D-06, D-11, D-13, D-18, D-21,
D-22, D-25, D-26).
</success_criteria>

## Artifacts this phase produces (this plan)

- New test file `src/__tests__/baseline-redump-wiring.test.ts`.

<output>
Create `.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-09-SUMMARY.md` when done
</output>
