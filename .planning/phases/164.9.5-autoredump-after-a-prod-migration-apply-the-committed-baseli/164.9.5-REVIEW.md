---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
reviewed: 2026-09-26T18:16:22Z
depth: standard
round: 2
reviewed_head: 730971dc0
diff_base: d9f179572
files_reviewed: 5
files_reviewed_list:
  - .github/workflows/supabase-migrate.yml
  - .gitleaks.toml
  - scripts/baseline-redump.mjs
  - src/__tests__/baseline-redump-wiring.test.ts
  - supabase/schema/BASELINE.md
findings:
  critical: 1
  warning: 4
  info: 1
  total: 6
carried_open_from_round_1:
  - WR-01
  - WR-04
  - IN-01
  - IN-02
status: issues_found
---

# Phase 164.9.5: Code Review Report (round 2, confirmation)

**Reviewed:** 2026-09-26T18:16:22Z
**Depth:** standard
**Scope:** the round-1 fix pass, `d9f179572..730971dc0`, five files
**Status:** issues_found (1 HIGH, caused by the fix pass)

## Summary

This round checks two things. First, whether round 1's HIGH findings are closed at HEAD. Second,
whether the fix pass (8 commits) added a new CRITICAL or HIGH. Round 1 is kept below as lineage,
unchanged.

**Measured locally at HEAD `730971dc0`:**
- `LC_ALL=C node scripts/baseline-redump.mjs --self-test` printed `self-test OK: 216 assertion(s)`
  and exited 0. With `--with-gitleaks` (local gitleaks 8.30.1) it printed `224 assertion(s)`.
  Both equal `EXPECTED_ASSERTIONS = 216` plus `EXPECTED_GITLEAKS_ASSERTIONS = 8`, so the
  calibration is correct.
- vitest ran `baseline-redump-wiring`, `supabase-migrate-test-first`, `critical-regressions` and
  `contracts/ghcr-login-before-image-pull` through a temporary `node_modules` symlink, which was
  removed afterwards. Result: 4 files, 232 tests, all passed.
- `gitleaks git` with this `.gitleaks.toml` over every commit that touched
  `supabase/schema/baseline.sql` (16 commits, `--all`) found no leaks. `git log --all -G` for
  the `sb_secret_` rule's regex found 0 commits, and `git grep` found 0 hits in the tree. **The two
  new rules add no finding to the full-history scan** (the scan behind WINDOWS.md entry 61).
- The new password class of `SECRET_SCAN_PATTERN` gives 0 hits on the committed dump (whose one
  `password` token is `encrypted_password`). It also gives 0 hits across every
  `supabase/migrations/*.sql` line. So it does not refuse today's PROD catalogue.

### Round-1 closure, by symbol

| Round-1 id | Verdict at HEAD | Where |
|---|---|---|
| **CR-01** (HIGH) | **Closed by decision D-30, not by code.** It stays a founder human-verification item: `can_approve_pull_request_reviews` must read `true` before the first changed dump. | CONTEXT D-30 |
| **CR-02** (HIGH) | **Closed.** A foreign commit refuses only while `openPrs` returns at least one open PR headed by the bot branch. With none open, `checkBotBranch` prints a `::notice::` naming the closed or merged PRs at the tip and emits `lease=<tip>`, so the squash-merge wedge is gone. The refusal text now names the open PR and the remedy. `composePrBody` tells the reviewer to land the commit through the PR. Self-test arms `closed7Run` and `foreignNoneRun` pass with a lease. `foreign7Run` with `openAre([7])` refuses. `lookupFail` is MEASURE_FAIL. | `checkBotBranch`, `judgeOpenBotPrs`, `openBotPrsCurlArgv`, `anonOpenBotPrs` |
| **SFH-01** (HIGH) | **Closed.** `gateDump` compares the merge's migration listing with `main`'s, fetched anonymously by `anonMainMigrationListing`, using `judgeMainListing`. `behind` refuses. `ahead` is skipped under D-31. A failed fetch is MEASURE_FAIL. A stale-sha re-run can no longer pair PROD-now with an old marker. | `judgeMainListing`, `anonMainMigrationListing`, `gateDump` |
| WR-02 (MEDIUM) | Closed, and over-closed. The refusal it recommended is now CR-03 below. | `gateDump` (`runAttempt !== "1"`) |
| WR-03 (MEDIUM) | Closed. It adds the `sb_secret_` class and the password class, and the rules `supabase-secret-key` and `baseline-dump-password`. A real-binary arm refuses both by RuleID and prints neither value. | `SECRET_SCAN_PATTERN`, `.gitleaks.toml` |
| SFH-02 (MEDIUM) | Closed. `compose` runs `merge-base --is-ancestor` (exit 128 is MEASURE_FAIL) and a marker-subset check. The subset check is the half that actually closes SFH-02's own scenario: there, the old merge IS an ancestor of the newer `main`. | `compose` |
| SFH-03 (MEDIUM) | **Not closed for truncation.** See WR-06. | `judgeCompleteness` |
| WR-01, WR-04 (MEDIUM/LOW) | Not in the fix pass. Still open as recorded limits. | — |

### Checked in the fix pass and found sound

- **The anonymous `curl` holds no credential.**
  - `-q` comes first, so no `.curlrc` is read.
  - No `-u`, `-n`/`--netrc` or `Authorization` header is passed. The self-test asserts this over
    the argv.
  - The `botbranch` step has no `env:`, so no token is in its environment.
  - `gh` is deliberately not used here, because it would pick up a token by itself.
- **The curl answer is judged strictly.**
  - A curl exit other than 0, a spawn error, a non-200 status or a non-3-digit status is
    MEASURE_FAIL.
  - So are non-JSON, a non-array, and an entry that is closed, has a foreign head, or carries a
    bad number.
  - An unreadable answer is never read as "no open PR".
- **The force reset is bounded.**
  - It happens only when no OPEN PR is headed by the branch.
  - It is under a lease on the judged tip, so a push that lands after the check makes the push
    fail.
  - It prints the released commits' short shas.
  - GitHub keeps `refs/pull/<n>/head` for closed PRs, so a closed-unmerged human commit stays
    reachable. See IN-05 for the residue.
- **D-31's skip hides no failure.**
  - Every dump gate runs first: the scan, gitleaks, integrity, shape and completeness.
  - The skip returns only after they pass.
  - `concurrency: supabase-migrate-${{ github.ref }}` with `cancel-in-progress: false` holds
    the later merge's apply until this run ends, so `ahead` really does mean "not yet applied".
  - "Skip forever" needs every later migration run to fail its apply. In that case the
    local-stack lane still replays the unmarked migration, so no replay is lost.
- **`GITHUB_RUN_ATTEMPT` on `workflow_dispatch`.** A fresh dispatch is attempt 1, so the documented
  remedy works. The wiring test asserts that the job never sets the variable itself.
- **Ancestry needs the objects.** `redump-pr` checks out with `fetch-depth: 0`, so
  `merge-base --is-ancestor` has the merge commit. In `redump-dump` (a shallow checkout), the
  anonymous fetch of `main` only adds the commits above the shallow tip.
- **The gitleaks rules** use RE2-compatible syntax. `baseline-dump-password` is path-scoped to
  `baseline.sql`, and the real-binary self-test arm proves that it fires on that name.

## Narrative Findings (AI reviewer)

## Critical Issues

### CR-03: HIGH (caused by the fix pass) — every re-run of a Supabase Migrate run that reaches `redump-dump` now ends red, including one whose dump is provably correct

**File:** `scripts/baseline-redump.mjs` (`gateDump`, the `if (runAttempt !== "1")` refusal placed
before every gate and before `judgeMainListing`); `src/__tests__/baseline-redump-wiring.test.ts`
(the `GITHUB_RUN_ATTEMPT` arm)

**Issue:** The attempt refusal is unconditional. It fires even when `main`'s migration listing
equals the merge's. In that case PROD is exactly the merge tree and the dump is valid.

**Why this matters: re-running a failed run is the standard recovery.**
- **An `apply-test` flake on shared TEST.** Shared-TEST mutex contention is a known flake class.
  "Re-run failed jobs" re-runs `apply-test`, `apply`, `redump-dump` and `redump-pr` as attempt 2.
  The PROD apply now succeeds, and then `redump-dump` refuses.
- **A transient `redump-dump` failure.** This includes the ghcr throttle that round-1 WR-02 itself
  named as the natural trigger. Re-running it can never go green.

In both cases the merge commit's Supabase Migrate run stays red, whatever the human does on that
run. D-31 (two commits earlier) was adopted precisely because "a red run on `main` makes Railway
skip the analytics deploy" for no defect. This is the same shape. The refusal's own text names a
remedy (dispatch a fresh run), so it is loud. But the red is structural: no re-run of that run
can clear it.

**The check guards nothing that `judgeMainListing` does not already guard.** Take the hazard it was
meant to stop: a re-run after a later migration merge has applied.
- If the later merge added a migration, `main` is `ahead`, and the run is skipped with a notice
  (green, nothing proposed).
- If no migration landed since, the listing is `equal`, PROD is the merge tree, and the dump is
  correct.
- The only residual is an out-of-band PROD write (SFH-04, a recorded limit), and the attempt
  number does not detect that either.

So the check adds false reds and no protection. Round 1 recommended it (WR-02). SFH-01 had already
marked the listing comparison as the precise option that "still allows a legitimate re-run". That
was the better call, and this review owns the recommendation that it overrode.

**Fix:** Delete the refusal and let `judgeMainListing` decide.
- Remove the `runAttempt` parameter from `gateDump`, or keep it only for a log line such as
  `baseline-redump attempt: 2 (main-listing decides)`.
- Remove the `GITHUB_RUN_ATTEMPT` wiring arm, or invert it to assert that attempt 2 with an equal
  listing proceeds.
- Replace the two 4b2 attempt arms with one arm: attempt `"2"`, `main` at the merge, a changed
  dump, and the assertion `changed=true` with an out dir written.
- Recalibrate `EXPECTED_ASSERTIONS` in the same commit.
- Correct the `BASELINE.md` refusal list ("a run attempt other than the first") and D-31's
  "Unchanged" sentence in CONTEXT, which restates the refusal.

```js
// gateDump: no attempt refusal. The main-listing verdict below is the re-run guard:
// a stale re-run lands on "ahead" (skip) or "equal" (PROD == merge tree, dump valid).
```

## Warnings

### WR-05: MEDIUM (caused by the fix pass) — the anonymous open-PR lookup runs on the NORMAL path, and a shared-IP rate limit turns it red

**File:** `scripts/baseline-redump.mjs` (`anonOpenBotPrs`, `checkBotBranch`); workflow step
`Refuse to overwrite a human commit on the bot branch`

**Issue:** The lookup runs whenever a foreign commit sits on the bot branch. That is not rare. The
PR body tells the reviewer to add the "what it adds" column as a commit on the bot branch. So
after every bot PR merged that way, the NEXT changed re-dump makes an unauthenticated
`api.github.com` call from a hosted runner.
- The anonymous budget is per source IP, and hosted-runner IPs are shared.
- A 403 is handled correctly as MEASURE_FAIL, but it is a red `redump-pr` on the `main` merge
  commit, on the documented happy path.
- Re-running `redump-pr` may or may not land on a different IP.

It fails loud, so it is not a silent defect. Its frequency is tied to the workflow's normal
cycle, though, not to an edge case.

**Fix:** Judge the open PRs in the step that already holds a token, and keep the check step
token-free.
- In the push step, run `gh pr list --head automation/baseline-redump --state open --json number`
  BEFORE the `git push`.
- Refuse there when `steps.botbranch.outputs.foreign` (a new output) is `true` and an open PR
  exists.

Alternatively, give the check step `GH_TOKEN: ${{ github.token }}` for this read only, which
raises the budget to 1,000/hr per repository, and record the D-05 exception. Either way, keep the
MEASURE_FAIL judge.

### WR-06: MEDIUM (partial fix) — SFH-03's completeness check cannot see a truncated dump, and its schema half is vacuous on the real corpus

**File:** `scripts/baseline-redump.mjs` (`judgeCompleteness`, `schemaObjectNames`, `gateDump`)

**Issue:** SFH-03's named threat was a dump truncated mid-stream that still exits 0.

**The check does not catch truncation.** Measured on the committed dump:
- All six `CREATE EXTENSION` lines sit in lines 16–55 of 16,130.
- There are **zero** `CREATE SCHEMA` lines (`public` appears only in `COMMENT ON SCHEMA`).

So a dump cut at any point after line 55 keeps every name `judgeCompleteness` reads, and it passes.

**The schema half can never fire on real data.** `was.schemas` is empty, and the only schema arm
in the self-test is a synthetic fixture. A test that cannot fail on the corpus it guards proves
nothing about that corpus.

What remains for truncation downstream:
- `compose`'s content-drift gate (function bodies only);
- the bot PR's CI, which runs only after a human clicks **Approve workflows to run**;
- a human who reads each head-SHA conclusion. Branch protection is off, so a merge with zero
  completed checks is possible.

**Fix:** Add a floor that the tail of the dump governs.
- Refuse when the new dump's `CREATE TABLE` / `CREATE POLICY` / `CREATE FUNCTION` / `GRANT` counts
  fall below the committed dump's counts at the merge. `countShapes` already counts most of
  these.
- Or refuse when the last non-blank statement class differs from the committed dump's (today:
  `ALTER DEFAULT PRIVILEGES`).
- Keep the extension check. Either drop the schema half or say in its comment that it is
  forward-looking, and do not count it as coverage.

### WR-07: MEDIUM (caused by the fix pass) — the marker-subset refusal wedges every re-dump after an applied migration is renamed or deleted, and its remedy text points to a step that refuses again

**File:** `scripts/baseline-redump.mjs` (`compose`, the `dropped > 0` refusal)

**Issue:** The subset check compares `main`'s COMMITTED marker with the new marker, which is built
from the merge tree. The repo has renamed applied migration files before: the 2026-05 PR-Y2
reconciliation recorded in `supabase-migrate.yml`'s header renamed 5.

After such a rename:
- The committed marker still names the old basename.
- Every new marker lacks it.
- `compose` refuses.
- Because the refusal leaves the committed marker unchanged, **every later changed re-dump
  refuses too**: a red `redump-pr` on every migration merge until someone runs a hand re-dump.

The message's remedy, "dispatch a fresh run on main", refuses the same way, so it sends the reader
into a loop.

Before this fix, the automated re-dump was exactly what would have healed the stale marker.

**Fix:** Keep the check. It is what closes SFH-02's own scenario, where the old merge IS an
ancestor.
- Exempt a dropped basename that `main`'s checkout also lacks. That is a rename or deletion on
  `main`, not a stale artifact: compare against `git ls-tree HEAD supabase/migrations/`.
- Otherwise, change the remedy text to name the hand re-dump in `BASELINE.md` `## Regenerating`
  when `main`'s checkout also lacks the dropped migration.
- Add a self-test arm in which the committed marker names a basename the checkout no longer has.

### WR-08: MEDIUM (caused by the fix pass, latent) — the password class refuses plausible PL/pgSQL and comment text, with nothing to say which

**File:** `scripts/baseline-redump.mjs` (`SECRET_SCAN_PATTERN`); `.gitleaks.toml` rule
`baseline-dump-password`; `supabase/schema/BASELINE.md` (the `grep -anE` line)

**Issue:** Today there are 0 hits on the dump and across all migrations (measured). Probing
`SECRET_SCAN_RE` directly shows that the class also matches ordinary schema text:
- a function-body comparison of the shape `WHERE password = <expr>`;
- a comparison of the shape `password = ANY(...)`;
- a `COMMENT ON` literal containing `password = ...`;
- a comment in this repo's house style, `password` followed by a space and an em-dash. The dump is
  read as latin1, so the UTF-8 bytes of `—` fall inside `[^ -&(-~]`.

The regex cannot tell any of these from a credential. The first migration that adds one makes
`redump-dump` refuse on every later run until the regex changes. The refusal prints only line
numbers (by design), so the reader must check out and inspect the dump by hand. This is
fail-loud, so it stays MEDIUM. Its trigger, though, is ordinary application SQL, not a leak.

**Fix:** Narrow the class to the shapes the finding named.
- Match `password=` only inside a single-quoted connection-string literal, e.g. after `CONNECTION '`
  on the same line.
- Match `password '` only after `OPTIONS (`.
- Or refuse any `CREATE SUBSCRIPTION` / `CREATE USER MAPPING` / `CREATE SERVER` statement
  outright (0 in the dump today) and drop the free-text password class.

Move `BASELINE.md`'s grep and the gitleaks rule in the same commit. Add a GREEN self-test arm
holding a `WHERE password = …` function line and an em-dash comment, so the narrowing is pinned.

## Info

### IN-05: The reset releases a closed-unmerged human commit from the branch, and a PR reopened between the check and the push is force-updated

**File:** `scripts/baseline-redump.mjs` (`checkBotBranch`)

**Issue:**
- **Closed without merging.** When the bot PR was closed without merging and carried a human
  commit, the next changed re-dump resets the branch. The commit stays reachable through
  `refs/pull/<n>/head`, and the notice prints its short sha, so nothing is unrecoverable.
- **Reopened in the window.** If someone REOPENS that PR between the `botbranch` step and the
  push, the lease still matches (the tip did not move). The reopened PR is then force-updated to
  the bot commit.

Both are narrow, and both follow from D-24 as amended by CR-02.

**Fix:** None required. Optionally, re-run the open-PR judge in the push step just before the push
(see WR-05's fix, which does this for free).

### Carried from round 1, still open (recorded limits, no fix round)

- **WR-01** (MEDIUM): composing onto the CURRENT `main` can still false-red through the drift gate
  when a migration merge lands between `redump-dump` and `redump-pr`. D-31 closed only the
  `redump-dump` side of that race.
- **WR-04** (LOW), **IN-01**, **IN-02**: unchanged.

---

_Reviewed: 2026-09-26T18:16:22Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard (round 2, confirmation)_

---

# Round 1 (lineage, unchanged)

Round 1's frontmatter, kept verbatim:

```yaml
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
reviewed: 2026-09-26T16:59:07Z
depth: standard
round: 1
reviewed_head: 61a88a187
diff_base: 8f33ddaff
files_reviewed: 8
files_reviewed_list:
  - .github/workflows/supabase-migrate.yml
  - TODOS.md
  - scripts/baseline-redump.mjs
  - src/__tests__/baseline-redump-wiring.test.ts
  - src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts
  - src/__tests__/critical-regressions.test.ts
  - src/__tests__/supabase-migrate-test-first.test.ts
  - supabase/schema/BASELINE.md
findings:
  critical: 2
  warning: 4
  info: 3
  total: 9
status: issues_found
```

## Phase 164.9.5: Code Review Report (round 1)

**Reviewed:** 2026-09-26T16:59:07Z
**Depth:** standard
**Files Reviewed:** 8
**Status:** issues_found

## Summary

This review covers the `redump-dump` and `redump-pr` jobs in `supabase-migrate.yml`, all of
`scripts/baseline-redump.mjs` (the modes, judges, writers, the bot-branch check, the PR create/edit
path and the self-test), the wiring test and the three widened test pins.

**What holds, checked rather than assumed:**
- **Credential split.** The PROD credential stays in step-level `env:` inside `redump-dump`. The
  write token appears only in the push step of `redump-pr`.
- **Permissions.** `redump-dump` has `contents: read, packages: read`. `redump-pr` has
  `contents: write, pull-requests: write`. The workflow default is still `contents: read`.
- **Injection.** No `${{ }}` appears in any `run:` body. `LEASE` and `GH_TOKEN` arrive through
  `env:`, and the lease is a hex sha the script emits itself.
- **Ordering.** `needs: [apply]` combined with `needs.apply.result == 'success'` and the implicit
  `success()` means a failed or skipped apply never reaches either job. Neither job can change
  the apply.
- **Lease.** An empty lease means the branch must not exist, and a tip lease is re-verified
  against the fetched tip.
- **gitleaks.** It runs with an explicit `--config`, `--redact` and `--ignore-gitleaks-allow`, and
  the binary is sha256-pinned. The script refuses a missing or empty target before the scan.
- **Refusals.** Every refusal throws and ends in `::error::` with exit 1. A schema-1 check of
  `measured.json` and a compose-side re-scan back this up.
- **Staged set.** Staging uses the six explicit paths only.
- **Marker.** The marker is taken from the merge tree and bound to the dump's sha256.
- **Tests.** The wiring test's calibrations mutate the workflow and assert red.

Measured locally: the four test files ran green (231 tests), and
`node scripts/baseline-redump.mjs --self-test` passed (198 assertions).

**The two HIGH findings are about what the code does on GitHub, not what its fixtures show.**
- The repository does not allow the workflow token to create a pull request, so the first real
  changed run fails at `gh pr create` (CR-01).
- The documented review path is: add a human commit, then squash-merge. That path leaves the bot
  branch in a state that every later run refuses (CR-02).

Both turn every later migration merge's run red on `main`, and per CONTEXT assumption A7 a red run
here holds the Railway deploy.

## Narrative Findings (AI reviewer)

## Critical Issues

### CR-01: HIGH — the repository does not let the workflow token create a pull request, so every changed re-dump goes red at `gh pr create`

**File:** `.github/workflows/supabase-migrate.yml` (job `redump-pr`, step `Push the bot branch and open or edit its PR`); `scripts/baseline-redump.mjs` (`openOrEditPr`, `buildPrArgv`)

**Issue:** A read-only query of the repository's Actions workflow permissions returns
`{"default_workflow_permissions":"read","can_approve_pull_request_reviews":false}`. That field is
the API form of **"Allow GitHub Actions to create and approve pull requests"**. While it is off, a
`GITHUB_TOKEN` cannot create a pull request. `gh pr create` fails with "GitHub Actions is not
permitted to create or approve pull requests", whatever the job's `pull-requests: write` says.

No pull request authored by `app/github-actions` exists in the repository (checked read-only), so
nothing shows the setting has ever been on. `cassette-refresh.yml` depends on the same capability
through `peter-evans/create-pull-request` with the default token, and it has no bot PR on record
either. A grep of the phase directory for the setting finds nothing: it is neither a recorded
precondition nor a human-verification item.

This is what happens on the first migration merge whose dump changed:
1. `git push` succeeds. The bot branch is now public.
2. `--open-or-edit-pr` runs `gh pr list` (allowed), then `gh pr create` (refused), and throws
   `MEASURE_FAIL: gh pr create exit 1`.
3. `redump-pr` goes red, so the Supabase Migrate run on the `main` merge commit goes red. Per
   assumption A7, that holds the Railway deploy.
4. Every later changed run repeats this. The branch holds only bot commits, so
   `--check-bot-branch` passes, the push succeeds, and create fails again.

D-18's planned post-merge proof (`gh workflow run supabase-migrate.yml --ref main`) takes the D-10
**no-op** path. It never reaches `gh pr create`, so it cannot catch this. The failure first shows
on a real migration merge.

**Fix:** This is a founder setting, not code. Pick one route and record it as a precondition:
- Turn on *Settings → Actions → General → Workflow permissions → "Allow GitHub Actions to create
  and approve pull requests"*. ⚠️ The same box also lets ANY workflow holding
  `pull-requests: write` **approve** PRs. With branch protection off, an approval gates nothing
  today, but record the widened surface in the threat model.
- Or reopen D-12's rejected route: a GitHub App installation token or a fine-grained PAT used for
  the `gh` call only.

Either way:
- Add the setting to the phase's human-verification items and to the `AUTOMATED 2026-09-26`
  paragraph in `BASELINE.md`.
- Make the first proof a run that takes the **changed** path, not only the no-op dispatch.
- Have `openOrEditPr` recognise this refusal and name the setting, because a bare
  `gh pr create exit 1` points the reader nowhere:
```js
// realGh already returns stderr on failure; pass it through to the judge
if (r.status !== 0 && /not permitted to create or approve pull requests/i.test(r.stderr ?? "")) {
  throw new Error("gh pr create refused: the repository setting 'Allow GitHub Actions to create and approve pull requests' is off; the bot branch was pushed but no PR exists");
}
```

### CR-02: HIGH — after a squash merge, the human commit the PR body asks for blocks every later run on the bot branch

**File:** `scripts/baseline-redump.mjs` (`checkBotBranch`, `judgeForeignCommits`, `composePrBody`)

**Issue:** `composePrBody` tells the reviewer to "Add the 'what it adds' column … **as a commit on
this branch**". Read-only settings show that the repository allows squash and rebase merges and
has `delete_branch_on_merge: false`. The repo practice is to squash (CLAUDE.md "CI gate
integrity" §1).

After the reviewer squash-merges (or rebase-merges) the bot PR:
- The bot branch still exists.
- Neither of its commits is an ancestor of `main`.
- `git log origin/main..origin/automation/baseline-redump` still lists the human commit.

On the next PROD apply whose dump changed, `judgeForeignCommits` finds the foreign author, and
`checkBotBranch` throws. It throws again on every apply after that, and nothing clears it.

The refusal text also points the wrong way. The merged PR's `refs/pull/<n>/head` stays frozen at
the tip, so `findPullRefsForSha` returns `[n]`. The message then says "It sits on pull request(s)
#n … **Merge or close that pull request**", which the human has already done. Nothing says the
actual remedy, which is to delete the bot branch.

A PR closed without merging, with a human commit on it, wedges the same way. The result is a
permanent red `redump-pr` on every migration merge. Per A7 that holds the Railway deploy, until
someone works out that the branch has to be deleted by hand.

**Fix:** Refuse only while the human commit is on an **open** PR. Once every PR pointing at the tip
is closed or merged, the work has been accepted or abandoned by a human, so the bot may replace it.
- Look up open PRs anonymously. The repository is public, so
  `GET /repos/{o}/{r}/pulls?head={o}:automation/baseline-redump&state=open` needs no token, which
  keeps the check step token-free. The other option is to reuse `openOrEditPr`'s `gh pr list`
  in the push step, before the push.
- If foreign commits exist and no open PR has the branch as head, print a `::notice::` naming the
  closed PR numbers and emit the tip as the lease. Refuse only when the head of an open PR carries
  the foreign commit.
- Fix the refusal text: for a merged or closed PR, name "delete the branch" as the remedy.
- As a complement, not a substitute (the closed-unmerged case still wedges), consider
  `delete_branch_on_merge: true`.
- Add a self-test arm: foreign commit, tip equal to a closed PR's head, no open PR → pass with a
  notice.

## Warnings

### WR-01: MEDIUM — composing onto CURRENT `main` gives a false red when two migration PRs merge close together

**File:** `scripts/baseline-redump.mjs` (`compose`, `judgeDriftLine`); `.github/workflows/supabase-migrate.yml` (`redump-pr` checkout `ref: main`)

**Issue:** Suppose merge M1 applies to PROD and `redump-dump` takes the M1 dump. Meanwhile M2, which
changes a function, has merged and its run is queued behind this one by the `concurrency:` group.
`redump-pr` checks out `main` (now M2) and runs `baseline-content-drift-check.mjs` against M2's
migration chain. That script does not read the carried-migrations marker (grep for
`carried`/`marker` finds only unrelated prose), so it reports M2's function as drift, and
`judgeDriftLine` refuses. The code predicts this itself ("Likely cause: main holds a migration
that has not applied to PROD yet; the queued apply run will produce the right dump").

The refusal is a red `redump-pr` on M1's merge commit. It is not a notice. That red is a false
alarm, but a human still has to triage it, and per A7 it can skip M1's deploy.

**Fix:** Before composing, compare the merge's and `origin/main`'s `supabase/migrations/` trees. If
`main` carries a migration the merge does not, stop with a `::notice::` ("a later apply is queued;
it will re-dump") and emit `committed=false`, instead of a red. The other option is to compose onto
the merge commit and let CI's version gate catch any version collision.

### WR-02: MEDIUM — re-running an old run's `redump-dump` pairs today's PROD with that run's stale marker

**File:** `scripts/baseline-redump.mjs` (`gateDump`); `.github/workflows/supabase-migrate.yml` (`redump-dump`)

**Issue:** "Re-run failed jobs" on an older Supabase Migrate run re-runs `redump-dump` with that
run's original `github.sha`. A transient ghcr throttle is a natural trigger. `gateDump` only
checks `HEAD == --merge`, and that holds. The re-run then dumps **current** PROD, which already
carries later merges' migrations, and writes the marker from the **old** merge tree.

The composed PR has three problems:
- The marker under-claims what the dump carries. The local-stack lane then replays those
  migrations on top of a dump that already contains them: silently when they are idempotent,
  fatally when they are not.
- The provenance says "after the PROD apply of merge <old>".
- The drift gate on `main` passes, because PROD and `main` agree.

The concurrency argument in D-01(ii) holds only for attempt 1.

**Fix:** In `redump-dump`, refuse when `GITHUB_RUN_ATTEMPT` is not `1`. Pass it through `env:` and
check it in the script, not in a `${{ }}` in `run:`. Point the message at the remedy the PR body
already names, `gh workflow run supabase-migrate.yml --ref main`, which gets a fresh `github.sha`.
Add a wiring-test arm for it.

### WR-03: MEDIUM — neither gate catches a Supabase `sb_secret_` key, and the dump is public as soon as the push lands

**File:** `scripts/baseline-redump.mjs` (`SECRET_SCAN_PATTERN`, `gitleaksArgv`); `supabase/schema/BASELINE.md` (`## Regenerating` grep)

**Issue:**
- **Why the gates are the only stop.** The bot branch is pushed to a public repository before any
  human reads the PR, so the two scans are the only thing between the dump and the public.
- **Neither scan matches the key.** Supabase's current secret-key format is `sb_secret_<random>`,
  which is not a JWT. The five-class pattern has no class for it.
- **Probe result.** Measured locally: gitleaks 8.30.1 with this repo's `.gitleaks.toml` and the
  exact flags `gitleaksArgv` uses returned exit 0, no finding, over a fixture holding a random
  `sb_secret_`-shaped value in a `COMMENT ON` literal.
- **The same class.** Keyword-form connection strings (`host=… password=…`) carry a credential in
  `CREATE USER MAPPING … OPTIONS (password …)` or `CREATE SUBSCRIPTION … CONNECTION`. Neither
  `postgres(ql)?://` nor any other class matches them. The committed dump carries neither
  statement today (measured: 0 hits), so this is a latent gap.

**Fix:** Add classes such as `sb_secret_[A-Za-z0-9_-]{16,}` and `password\s*[= ]\s*'`, or refuse any
`USER MAPPING`/`SUBSCRIPTION` statement outright.
⚠️ `SECRET_SCAN_PATTERN` and the `grep -anE` argument in `BASELINE.md` must move in the same
commit. The self-test and the wiring test both pin them as byte-equal.
Also add a self-test arm that plants a runtime-assembled `sb_secret_` value and asserts the
refusal.

### WR-04: LOW — the concurrency group is now held up to ~35 minutes longer, which widens the existing pending-run cancellation window

**File:** `.github/workflows/supabase-migrate.yml` (`concurrency:`; `redump-dump` `timeout-minutes: 20`, `redump-pr` `timeout-minutes: 15`)

**Issue:** `cancel-in-progress: false` keeps only ONE pending run per group. If a third migration
merge arrives while one run is in progress and one is pending, the pending run is **cancelled**.
The newest run's `--include-all` still applies its migrations, but the cancelled run leaves a
cancelled check on its merge commit. This existed before the phase. The redump jobs now keep the
group busy for up to 35 more minutes after `apply` finishes, which widens the window.

**Fix:** Record it as a known limit in the file's header comment and in the threat model, or
lower the redump timeouts toward the measured runtime once a real run exists.

## Info

### IN-01: The ghcr token stays in `~/.docker/config.json` for the rest of `redump-dump`

**File:** `.github/workflows/supabase-migrate.yml` (`redump-dump`, step `Log in to ghcr.io before the CLI pulls its images`)

**Issue:** `docker login` writes the job token (`contents: read`, `packages: read`) to the runner's
docker config. The dump and gate steps can still read it. The token is read-only and ends with the
job.
**Fix:** Optionally add a `docker logout ghcr.io` step with `if: always()` after the dump step.

### IN-02: `setup-node` in the write-token job gets the write token through its default `token:` input

**File:** `.github/workflows/supabase-migrate.yml` (`redump-pr`, `actions/setup-node` step)

**Issue:** `setup-node` defaults `token:` to `github.token`, which here is `contents: write`. It
uses the token only to fetch Node manifests, and the action is SHA-pinned first-party code. It is
still one more consumer of the write token than D-05's "the push step only" describes.
**Fix:** Set `token: ''` on that step, or record the exception beside D-05.

### IN-03: `botPrStatus` turns a failed lookup into a warning on the no-op path (by design)

**File:** `scripts/baseline-redump.mjs` (`botPrStatus`)

**Issue:** A failed `ls-remote` on the D-10 no-op path produces a `::warning::` and a green
job, deliberately (RESEARCH A7). This is recorded so a later reader does not "fix" it into a red:
the dump gates already passed before this lookup runs.
**Fix:** None. Keep it as is.

---

_Reviewed: 2026-09-26T16:59:07Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_
