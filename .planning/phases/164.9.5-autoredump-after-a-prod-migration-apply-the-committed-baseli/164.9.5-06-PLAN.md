---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
plan: 06
type: execute
wave: 7
depends_on: ["164.9.5-05", "164.9.5-08", "164.9.5-09"]
files_modified:
  - .github/workflows/supabase-migrate.yml
  - scripts/baseline-redump.mjs
autonomous: false
requirements:
  - "164.9.5-MANUAL-BASELINE-REDUMP"

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A GitHub Actions workflow-injection review of both redump jobs ran BEFORE merge: `actionlint` ad hoc over `supabase-migrate.yml` is clean, and a measured checklist covers untrusted-input interpolation, `permissions:`, credential persistence, token exposure and log leakage (per D-20 as amended by D-28)."
    - "Neither `actionlint` nor `zizmor` became a repository dependency: no `package.json`, lockfile, workflow or script change installs or calls either (per D-20, D-28)."
    - "`zizmor` ran only after a human confirmed its legitimacy (package-legitimacy verdict SUS), pinned `==1.30.1`, offline; or its skip is recorded with actionlint plus the checklist standing alone (per D-28)."
    - "Every finding the review raised is fixed in the file it names and re-verified by the full suite; none is waived (review policy: fix everything)."
    - "The SUMMARY records the gate `/gsd-secure-phase 164.9.5` must pass before merge, and hands that skill a threat-to-evidence map covering every T-164.9.5 id across plans 01-05 and 07-09 (per D-20)."
  artifacts:
    - path: .planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-06-SUMMARY.md
      provides: "actionlint output, injection checklist results, zizmor result or recorded skip, threat-to-evidence map, the secure-phase gate record"
  key_links:
    - "`/gsd-secure-phase 164.9.5` is run by the orchestrator (in parallel with gsd-verifier), not by this plan; this plan produces its inputs."
  prohibitions:
    - "No `npm install`, `pip install` or `brew install` of a review tool, and no workflow step that runs one. The tools run ad hoc on the executor's machine only."
    - "No `gh workflow run`, no push and no PR from this plan. The live dispatch is a post-merge human item (plan 05)."
---

<objective>
Run the pre-merge security review D-20 and D-28 require over the new workflow jobs and the
script, fix whatever it finds, and hand `/gsd-secure-phase 164.9.5` an evidence map. The review
is `actionlint` ad hoc, a measured workflow-injection checklist, and optionally `zizmor` behind a
human legitimacy check.

Purpose: this phase adds a job that reads PRODUCTION with a repository secret, and a job that
pushes to a public repository. ROADMAP requires a security review before merge.

Output: SUMMARY with the review outputs and the threat map, plus fixes to the two phase files
only if a finding requires one.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-CONTEXT.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-RESEARCH.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-01-PLAN.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-02-PLAN.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-03-PLAN.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-04-PLAN.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-05-PLAN.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-07-PLAN.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-08-PLAN.md
@.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-09-PLAN.md
@.github/workflows/supabase-migrate.yml
@scripts/baseline-redump.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: actionlint ad hoc plus a measured workflow-injection checklist over both redump jobs; fix every finding</name>
  <precondition>`actionlint --version` prints 1.7.x and `shellcheck --version` prints a version on the executor's machine (both are local tools, never repo dependencies). `npx vitest --version` resolves. Plans 04, 08 and 09 are merged: `grep -c '^  redump-pr:' .github/workflows/supabase-migrate.yml` prints 1 and `src/__tests__/baseline-redump-wiring.test.ts` exists.</precondition>
  <files>.github/workflows/supabase-migrate.yml, scripts/baseline-redump.mjs (only if a finding names them)</files>
  <read_first>.github/workflows/supabase-migrate.yml (jobs `redump-dump`, `redump-pr`, the workflow-level `permissions:` and `concurrency:`), scripts/baseline-redump.mjs (every `console.log`/`console.error`, every `spawnSync`/`execFileSync` call), .planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-RESEARCH.md (F10, F12, "Security Domain")</read_first>
  <action>
  Run `actionlint .github/workflows/supabase-migrate.yml`. It must exit 0 with no output; paste
  the command and its (empty) output into SUMMARY.

  Then run the checklist below per D-20. Each item is a command whose output goes into SUMMARY
  verbatim, with PASS or FINDING:
  1. Untrusted-input interpolation: over the live lines of both jobs, list every `${{ … }}`
     occurrence. PASS only if every one sits in a step `env:` value, a `with:` value, an `if:`,
     `outputs:` or a job-level key. None may sit in a `run:` body, and none may read
     `github.event.*`, `github.head_ref` or any PR/issue text field.
  2. `permissions:`: exactly one top-level block (`contents: read`), `redump-dump` = {contents:
     read, packages: read}, and `redump-pr` = {contents: write, pull-requests: write}. No
     `write-all`, and no `actions:`, `id-token:` or `issues:` scope.
  3. Credential persistence: every `actions/checkout` in the file carries `persist-credentials:
     false`, and no step writes a credential into `.git/config` (grep for `extraheader` and
     `credential.helper` on live lines; the single push step's per-invocation `-c` is the
     allowed form).
  4. Token exposure: `GH_TOKEN` and the job token appear only in the push step's `env:` and the
     ghcr login step's `env:`, and the three Supabase values appear only in the assert and dump
     steps' `env:`.
  5. Log leakage: in `scripts/baseline-redump.mjs`, list every print call that interpolates a
     value, and confirm that none prints dump text, a matched line, a token, a DSN or a project
     ref. Confirm that the dump step never `cat`s, `head`s or `tail`s the dump.
  6. Third-party code in the write job: `redump-pr`'s `uses:` are exactly checkout, setup-node
     and download-artifact, SHA-pinned; it has no `npm ci`, no `curl` and no gitleaks.
  7. Trigger surface: the workflow's `on:` is unchanged (`push` paths, `workflow_dispatch`), and
     there is no `pull_request_target` and no `workflow_run`.
  8. Branch reach: exactly one live `git push`, its refspec is `HEAD:refs/heads/automation/baseline-redump`,
     and it carries a force-with-lease. Record the D-25 accepted risk: `contents: write` could
     reach main while branch protection is off.
  Per the review policy, fix every FINDING in the file it names, in this task, then re-run
  `actionlint` and the full suite. A fix that changes a pinned property must also update the
  wiring test's pin by meaning, and keep its calibration. If this plan changed
  `scripts/baseline-redump.mjs` at all, also re-run plan 03 Task 3's `<automated>` command
  verbatim (the real-file throwaway-clone compose run) and paste its `committed=true` line into
  SUMMARY: that run is the only proof that a script fix did not break the write half on the
  real files.
  </action>
  <verify>
    <automated>actionlint .github/workflows/supabase-migrate.yml && npx vitest run src/__tests__/baseline-redump-wiring.test.ts src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/critical-regressions.test.ts src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts src/__tests__/gitleaks-allowlist.test.ts src/__tests__/baseline-wiring-claim.test.ts && node scripts/baseline-redump.mjs --self-test && node scripts/baseline-redump.mjs --self-test --with-gitleaks</automated>
    <fails_when>actionlint prints any finding or exits non-zero; vitest exits non-zero or reports "No test files found"; either self-test exits 1 or prints `SELF-TEST FAILED`</fails_when>
  </verify>
  <acceptance_criteria>
    - actionlint exits 0 with empty output.
    - SUMMARY carries all eight checklist items with their command output and a PASS or FINDING verdict; every FINDING has a named fix and a re-run.
    - If `scripts/baseline-redump.mjs` changed in this plan, SUMMARY carries the re-run of plan 03 Task 3's verify with its `committed=true` line.
    - `git diff --stat origin/main -- package.json package-lock.json` shows no dependency added by this plan, and `grep -rcE 'actionlint|zizmor' .github/workflows package.json` prints 0 per file.
  </acceptance_criteria>
  <done>Both jobs pass a measured injection review and actionlint, with every finding fixed and re-verified.</done>
</task>

<task type="auto">
  <name>Task 2: threat-to-evidence map and the `/gsd-secure-phase` gate record</name>
  <files>.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-06-SUMMARY.md</files>
  <read_first>every `<threat_model>` block in 164.9.5-01-PLAN.md through 164.9.5-05-PLAN.md and 164.9.5-07-PLAN.md through 164.9.5-09-PLAN.md, and the matching 01-05 and 07-09 SUMMARY files (observed-RED records). Plan 05's SUMMARY exists because this plan depends on plan 05.</read_first>
  <action>
  In SUMMARY, write a `## Threat-to-evidence map` table. It has one row per threat id T-164.9.5-01
  through T-164.9.5-36 plus T-164.9.5-SC, with columns: id, disposition, the mitigation as
  shipped, the verifying artifact (test name, self-test arm name, or checklist item from Task 1),
  and whether its RED was observed (with the SUMMARY that records it). An id with no verifying
  artifact is a FINDING. Fix it the same way as in Task 1, adding the missing arm or pin, and
  never downgrade a disposition to `accept` to make a row pass. T-164.9.5-23 (D-25, write token
  can reach main) is the one ACCEPTED risk. Name its boundary: until a ruleset or branch
  protection exists, which is a founder decision deferred until paying clients.

  Then write `## Pre-merge security gate`, stating: `/gsd-secure-phase 164.9.5` MUST pass before
  the phase PR merges (D-20; the ROADMAP marks this workflow security-sensitive). It is run by the
  orchestrator in parallel with gsd-verifier, not by this plan. Its inputs are the map above,
  Task 1's checklist, and the `<threat_model>` blocks of plans 01-05 and 07-09. Security enforcement is
  ASVS level 1 and blocks on `high`. Also state that after merge a `supabase/migrations/**`
  merge applies to PROD automatically with no human reviewer (CLAUDE.md, corrected 2026-09-23),
  so this pre-merge gate is the last human-read stop before the jobs first run.
  </action>
  <verify>
    <automated>S=.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-06-SUMMARY.md; grep -q '^## Threat-to-evidence map' "$S" && grep -q '^## Pre-merge security gate' "$S" && for i in $(seq -w 1 36) SC; do grep -q "T-164.9.5-$i" "$S" || { echo "missing T-164.9.5-$i"; exit 1; }; done && npm run check:planning-hygiene</automated>
    <fails_when>either heading is absent, the loop prints `missing T-164.9.5-<id>` and exits 1, or the hygiene gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Every threat id 01-36 and SC has a row with a verifying artifact.
    - Exactly one row carries disposition `accept` (T-164.9.5-23), with its boundary named.
    - The `## Pre-merge security gate` section names `/gsd-secure-phase 164.9.5`, ASVS level 1, and block-on-high.
  </acceptance_criteria>
  <done>`/gsd-secure-phase` has a complete, evidence-bound threat map to verify, and the gate is recorded as blocking merge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: optional zizmor run, only after a human confirms the package's legitimacy (SUS verdict)</name>
  <files>.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-06-SUMMARY.md</files>
  <read_first>.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-RESEARCH.md (`## Package Legitimacy Audit`, F12)</read_first>
  <what-built>Tasks 1 and 2 finished the actionlint run, the eight-item injection checklist, and the threat map. zizmor would add `template-injection`, `artipacked`, `excessive-permissions`, `unpinned-uses` and `dangerous-triggers` audits. The package-legitimacy check rated PyPI `zizmor` SUS (latest release too new, download count unknown), so it runs only on explicit human approval. This checkpoint is never auto-approvable.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/zizmor/ and https://github.com/zizmorcore/zizmor (or the project's documented source repository) and confirm that version 1.30.1 exists, is published by the project's maintainers, and matches the source repository's release.
    2. If satisfied, reply `approved`. The executor then runs `uvx zizmor==1.30.1 --offline .github/workflows/supabase-migrate.yml` ad hoc (nothing is added to the repo), pastes the output into SUMMARY, and fixes every finding on the two redump jobs as in Task 1. Pre-existing findings on other jobs are recorded but out of scope.
    3. If not satisfied, reply `skip-zizmor`. The executor records in SUMMARY that zizmor was NOT run and why, and that actionlint plus the checklist stand as the injection review (D-28 makes zizmor optional).
  </how-to-verify>
  <action>Present this checkpoint and wait. On `approved`, run the pinned offline zizmor command, record its output, fix every finding on the redump jobs, then re-run Task 1's verify. On `skip-zizmor`, record the skip and its reason. Never install zizmor into the repo, and never add it to a workflow.</action>
  <verify>
    <automated>grep -qE '^## zizmor( \(skipped\))?' .planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-06-SUMMARY.md && actionlint .github/workflows/supabase-migrate.yml</automated>
    <fails_when>the SUMMARY lacks a `## zizmor` or `## zizmor (skipped)` section, or actionlint reports a finding after any zizmor-driven fix</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY has `## zizmor` (with pasted output and fixes) or `## zizmor (skipped)` (with the human's reason).
    - `grep -rc zizmor .github package.json` prints 0 per file.
  </acceptance_criteria>
  <resume-signal>Type `approved` to run zizmor 1.30.1 offline, or `skip-zizmor` to record the skip.</resume-signal>
  <done>zizmor either ran after human approval with its findings fixed, or its skip is recorded; no tool was added to the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ad hoc review tools → executor machine | third-party binaries run locally against repo files |
| review verdict → merge decision | a missed finding ships a PROD-reading, public-pushing job |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.5-SC | Tampering | zizmor from PyPI (SUS) and the gitleaks tarball | high | mitigate | zizmor only behind a blocking human legitimacy checkpoint, pinned `==1.30.1`, offline, never a repo dependency (Task 3); the gitleaks tarball is sha256-pinned (plan 04) |
| T-164.9.5-22 | Tampering / EoP | workflow script injection | high | mitigate | actionlint plus checklist items 1 and 7 (Task 1) |
| T-164.9.5-24 | Information disclosure | credential/token co-residence | high | mitigate | checklist items 2, 4 and 6 (Task 1) |
| T-164.9.5-23 | Elevation of privilege | `contents: write` can reach main | high | accept | recorded with its boundary (Task 2); bounded by checklist item 8 |
| T-164.9.5-01 | Information disclosure | log leakage from the script | high | mitigate | checklist item 5 (Task 1) |
</threat_model>

<verification>
actionlint is clean. The full suite and both self-test modes are green after any fix. Every threat
id maps to a verifying artifact. The `/gsd-secure-phase 164.9.5` gate is recorded as blocking
merge. zizmor either ran after human approval or its skip is recorded.
</verification>

<success_criteria>
The ROADMAP's "security review before merge" requirement is met by a measured review with fixes,
and `/gsd-secure-phase` receives a complete evidence map (D-20, D-28).
</success_criteria>

## Artifacts this phase produces (this plan)

- SUMMARY sections: actionlint output, the eight-item injection checklist, `## Threat-to-evidence map`, `## Pre-merge security gate`, `## zizmor` or `## zizmor (skipped)`.
- Fixes to `.github/workflows/supabase-migrate.yml` or `scripts/baseline-redump.mjs`, only where a finding names them.
- No new tool, dependency, secret or workflow trigger.

<output>
Create `.planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-06-SUMMARY.md` when done
</output>
