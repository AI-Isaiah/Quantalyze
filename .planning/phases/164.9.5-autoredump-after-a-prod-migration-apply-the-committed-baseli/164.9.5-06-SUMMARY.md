---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
plan: 06
subsystem: infra
tags: [github-actions, security-review, actionlint, workflow-injection, threat-model]

requires:
  - phase: 164.9.5 plans 01-05, 07-09
    provides: "the redump-dump / redump-pr jobs, scripts/baseline-redump.mjs, the wiring test, the docs"
provides:
  - "a measured pre-merge workflow-injection review of redump-dump and redump-pr (actionlint plus an eight-item checklist)"
  - "a threat-to-evidence map for T-164.9.5-01..36 and SC, and the /gsd-secure-phase pre-merge gate record"
affects: ["/gsd-secure-phase 164.9.5", "gsd-verifier 164.9.5"]

# chars/4 over the one file this plan changed (this SUMMARY), at the Task 2 write.
actuals:
  tokens: 9500
  tasks: 2         # of 3; Task 3 is the pending blocking-human zizmor checkpoint
  commits: 1       # MEASURED: git rev-list --count plan_head_before..HEAD at the Task 2 write; the Task 2 commit carrying this file is the 2nd
plan_head_before: 0a391b03a73ffb67e6fef15a5f3cc1a12f969eee

tech-stack:
  added: []
  patterns:
    - "Ad hoc review tools only: actionlint from the local PATH, never a repo dependency"

key-files:
  created:
    - .planning/phases/164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli/164.9.5-06-SUMMARY.md
  modified: []

key-decisions:
  - "No checklist item raised a FINDING, so neither phase file changed and plan 03 Task 3's real-file compose run was not re-run (it is required only when this plan changes the script)"
  - "The default token inputs of checkout and setup-node, and the persisted ghcr login, are recorded as observations and not findings: all are GitHub-owned, sha-pinned or read-only, and bounded by checklist items 3 and 6"
  - "T-164.9.5-23 stays the one accept row, with its boundary named: until a ruleset or branch protection exists (a founder decision deferred until paying clients)"

requirements-completed: ["164.9.5-MANUAL-BASELINE-REDUMP"]

coverage:
  - id: D1
    description: "actionlint and the eight-item workflow-injection checklist over redump-dump and redump-pr, all PASS"
    requirement: "164.9.5-MANUAL-BASELINE-REDUMP"
    verification:
      - kind: other
        ref: "actionlint .github/workflows/supabase-migrate.yml (exit 0, empty output)"
        status: pass
      - kind: unit
        ref: "npx vitest run src/__tests__/baseline-redump-wiring.test.ts (16/16) and the plan's six-file set (254 passed, 1 skipped by design)"
        status: pass
      - kind: other
        ref: "node scripts/baseline-redump.mjs --self-test (198) and --self-test --with-gitleaks (205)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Threat-to-evidence map (37 rows) and the /gsd-secure-phase pre-merge gate record"
    requirement: "164.9.5-MANUAL-BASELINE-REDUMP"
    verification:
      - kind: other
        ref: "Task 2 verify: both headings present, every T-164.9.5-01..36 and SC present, npm run check:planning-hygiene"
        status: pass
    human_judgment: true
    rationale: "The map is the input to /gsd-secure-phase 164.9.5, whose ASVS L1 verdict (block on high) is the judgment; this plan does not run it"
  - id: D3
    description: "Optional zizmor 1.30.1 offline run, or its recorded skip"
    verification: []
    human_judgment: true
    rationale: "Blocking-human checkpoint: the package-legitimacy verdict is SUS, so a human must approve before it runs (D-28)"

# Task 3 (the optional zizmor run) is a blocking-human checkpoint and is NOT resolved.
# The continuation agent sets `status: complete` after it adds `## zizmor` or `## zizmor (skipped)`.
duration: 13min (Tasks 1-2)
started: 2026-09-26T16:12:00Z
status: in-progress
---

# Phase 164.9.5 Plan 06: Pre-merge security review Summary

**actionlint 1.7.12 is clean on `supabase-migrate.yml`, and an eight-item workflow-injection checklist over `redump-dump` and `redump-pr` returned PASS on every item, so neither phase file changed.**

## actionlint (Task 1)

`actionlint` is the local Homebrew binary, version `1.7.12`, with `shellcheck` `0.11.0` on PATH, so the
`run:` bodies were shellchecked too. Neither tool is a repo dependency (see the dependency check below).

```
$ actionlint .github/workflows/supabase-migrate.yml
$ echo "exit=$?"
exit=0
```

The output is empty and the exit code is 0.

## Workflow-injection checklist (Task 1, D-20 as amended by D-28)

The scope is the two jobs, file lines 1276-1474 of `.github/workflows/supabase-migrate.yml` (from
`redump-dump:` to the line before `apply:`), plus `scripts/baseline-redump.mjs` for item 5. A "live
line" is one that is not a YAML comment.

### 1. Untrusted-input interpolation: PASS

```
$ awk 'NR>=1276 && NR<1475 && !/^[[:space:]]*#/ && /\$\{\{/ {print NR": "$0}' .github/workflows/supabase-migrate.yml
1288:       changed: ${{ steps.gate.outputs.changed }}
1300:           SUPABASE_PROJECT_REF: ${{ vars.SUPABASE_PROJECT_REF }}
1301:           SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
1302:           SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
1354:           GHCR_USER: ${{ github.actor }}
1355:           GHCR_TOKEN: ${{ github.token }}
1368:           SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
1369:           SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
1370:           SUPABASE_PROJECT_REF: ${{ vars.SUPABASE_PROJECT_REF }}
1394:           path: ${{ runner.temp }}/redump-artifact
1444:           path: ${{ runner.temp }}/redump-in
1468:           GH_TOKEN: ${{ github.token }}
1469:           LEASE: ${{ steps.botbranch.outputs.lease }}
$ awk 'NR>=1276 && NR<1475 && !/^[[:space:]]*#/' .github/workflows/supabase-migrate.yml | grep -cE 'github\.event|github\.head_ref|\.title|\.body'
0
```

All 13 expressions sit in a job `outputs:` value (1288), a step `env:` value (1300-1302, 1354-1355,
1368-1370, 1468-1469) or a `with:` value (1394, 1444). None is in a `run:` body. The two job `if:`
conditions use bare expressions, which are evaluated by the runner and are not shell text. No live line
reads `github.event.*`, `github.head_ref` or a PR or issue text field. `github.actor` is a login name
and reaches the shell only through `env:`. `steps.botbranch.outputs.lease` is emitted by
`--check-bot-branch` from `git ls-remote` output as a sha or as the empty string, and it also reaches
the shell only through `env:`.

### 2. `permissions:`: PASS

```
$ grep -nE '^permissions:' .github/workflows/supabase-migrate.yml
179:permissions:
$ sed -n '179,181p' .github/workflows/supabase-migrate.yml
permissions:
  contents: read

$ awk '!/^[[:space:]]*#/ && /^    permissions:/{print NR": "$0; for(i=1;i<=3;i++){getline; print NR": "$0}}' .github/workflows/supabase-migrate.yml
1284:     permissions:
1285:       contents: read
1286:       packages: read
1287:     outputs:
1425:     permissions:
1426:       contents: write
1427:       pull-requests: write
1428:     steps:
$ grep -vE '^\s*#' .github/workflows/supabase-migrate.yml | grep -cE 'write-all|read-all|^\s+(actions|id-token|issues):'
0
```

There is one top-level block (`contents: read`). `redump-dump` (1284) is `{contents: read, packages: read}`
and `redump-pr` (1425) is `{contents: write, pull-requests: write}`. No other job in the file declares
permissions. No live line carries `write-all`, `read-all`, `actions:`, `id-token:` or `issues:`.

### 3. Credential persistence: PASS

```
$ grep -nE '^\s*- uses: actions/checkout@' .github/workflows/supabase-migrate.yml | wc -l
       5
$ grep -cE '^\s*persist-credentials: false' .github/workflows/supabase-migrate.yml
5
$ awk '/uses: actions\/checkout@/{c=NR} /persist-credentials:/{print c" -> "NR": "$0}' .github/workflows/supabase-migrate.yml
266 -> 272:           persist-credentials: false
372 -> 374:           persist-credentials: false
1292 -> 1294:           persist-credentials: false
1433 -> 1437:           persist-credentials: false
1487 -> 1492:           persist-credentials: false
$ grep -nE 'extraheader|credential\.helper' .github/workflows/supabase-migrate.yml | grep -vE '^[0-9]+:\s*#'
1472:          git -c credential.helper= -c 'credential.helper=!gh auth git-credential' push --force-with-lease="refs/heads/automation/baseline-redump:${LEASE}" origin HEAD:refs/heads/automation/baseline-redump
$ grep -nE 'git config' .github/workflows/supabase-migrate.yml | grep -vE '^[0-9]+:\s*#'
(no output)
```

All five checkouts in the file carry `persist-credentials: false`, including 1292 (`redump-dump`) and
1433 (`redump-pr`). The one live `credential.helper` is the per-invocation `-c` on the single push, which
is the allowed form. No live line runs `git config`, and no live line mentions `extraheader`. The script
adds nothing here: its own git calls outside the self-test (`anonGit`) pass `-c credential.helper=` and
turn the terminal prompt off, so they can neither pick up a token nor wait for one.

### 4. Token exposure: PASS

```
$ awk 'NR>=1276 && NR<1475 && !/^[[:space:]]*#/ && /(github\.token|GITHUB_TOKEN|GH_TOKEN|secrets\.|vars\.SUPABASE)/ {print NR": "$0}' .github/workflows/supabase-migrate.yml
1300:           SUPABASE_PROJECT_REF: ${{ vars.SUPABASE_PROJECT_REF }}
1301:           SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
1302:           SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
1306:           [ -n "${SUPABASE_PROJECT_REF:-}" ] || missing="${missing} vars.SUPABASE_PROJECT_REF"
1307:           [ -n "${SUPABASE_ACCESS_TOKEN:-}" ] || missing="${missing} secrets.SUPABASE_ACCESS_TOKEN"
1308:           [ -n "${SUPABASE_DB_PASSWORD:-}" ] || missing="${missing} secrets.SUPABASE_DB_PASSWORD"
1355:           GHCR_TOKEN: ${{ github.token }}
1368:           SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
1369:           SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
1370:           SUPABASE_PROJECT_REF: ${{ vars.SUPABASE_PROJECT_REF }}
1468:           GH_TOKEN: ${{ github.token }}
```

The job token appears in two places: the ghcr login step's `env:` (1355) and the push step's `env:`
(1468). The three Supabase values appear only in the assert step's `env:` (1300-1302) and the dump step's
`env:` (1368-1370). Lines 1306-1308 are the assert step reading its own `env:` and naming the missing
variable, never a value. `redump-pr` holds no Supabase value.

Two observations. Neither is a FINDING, and both are recorded so `/gsd-secure-phase` does not have to
rediscover them:

- **Default action inputs.** In `redump-pr`, `actions/checkout` and `actions/setup-node` receive the job
  token through their default `token:` inputs, as every checkout in this repo does. That token is not in
  the file text, so the grep above cannot see it. This is not a finding because both actions are
  GitHub-owned and pinned to a full commit sha (item 6), and checkout does not persist the token (item 3).
  Item 6 is what bounds this path.
- **The ghcr login persists for the rest of `redump-dump`.** `docker login` writes the job token into the
  runner's docker config, and it stays there until the job ends. This is not a finding because that token
  is read-only (`contents: read`, `packages: read`), and the later steps (setup-cli, the Supabase CLI, the
  script and `upload-artifact`) already receive the PROD credential or run first-party code. The step is
  the same template as the `check` job in `migration-drift-check.yml`, which the ghcr contract test pins.

### 5. Log leakage: PASS

Every print call in the script outside the self-test section:

```
$ grep -nE 'console\.(log|error|warn)|process\.std(out|err)\.write' scripts/baseline-redump.mjs   (runtime lines, below the self-test at 1510)
836:  console.log(`${key}=${value}`);
843:  if (r.stdout) process.stdout.write(r.stdout);
844:  if (r.stderr) process.stderr.write(r.stderr);
875:    if (r.status !== 0 && r.status !== 1 && r.stderr) process.stderr.write(r.stderr.slice(0, 2000));
898:  console.log(`baseline-redump gitleaks: ${j.findings.length} finding(s)`);
899:  for (const f of j.findings.slice(0, MAX_LINES_PRINTED)) console.log(`  RuleID=${f.rule} line=${f.line}`);
1042:    console.log("baseline-redump bot-branch: absent");
1066:    console.log(`baseline-redump bot-branch: present tip=${short(tip)} prs=${prs.length > 0 ? prs.map((n) => `#${n}`).join(",") : "none"}`);
1115:  if (r.status !== 0 && r.stderr) process.stderr.write(r.stderr.slice(0, 2000));
1135:    for (const h of hits) console.error(`::error::baseline-redump: skip-token: ${h}`);
1176:  console.log(`baseline-redump pr: ${verb === "create" ? "created" : "edited"} ${url ?? `https://github.com/${repo}/pull/${number}`}`);
1211:  console.log(`baseline-redump secret-scan: ${hits.length} hit(s)${hits.length > 0 ? ` at line(s) ${lineList(hits)}` : ""}`);
1217:  console.log(
1265:  console.log(
1273:    if (status.warning) console.log(status.warning);
1274:    console.log(`::notice::baseline-redump: dump and marker are byte-identical to the committed pair — no PR; ${status.text}`);
1299:  for (const d of defects) console.error(`::error::baseline-redump: ${d.kind}: ${d.detail}`);
1339:  console.log(
1358:    console.log("::notice::baseline-redump: the gated pair is byte-identical to this checkout's committed pair — nothing to commit");
1464:    for (const h of tokenHits) console.error(`::error::baseline-redump: skip-token: ${h}`);
1492:  console.log(
```

What each one interpolates:

| Line | Values | Verdict |
|---|---|---|
| 836 | a job-output key and value: `changed`, `committed`, `lease` (a sha) or `pr_number` | no dump text |
| 843-844 | child-gate stdout and stderr (see below) | no dump text |
| 875 | gitleaks stderr, only when gitleaks could not complete (exit 2 and up): config and I/O errors, never a finding. gitleaks runs with `--redact` | no secret |
| 898-899 | the finding count, then `RuleID` and line number, never `Secret` or `Match` | no matched text |
| 1042, 1066 | the bot branch state, a 12-character tip sha prefix and PR numbers | no token |
| 1115 | gh stderr on a failure only; `GH_TOKEN` is read by gh from the environment and is never on argv | no token |
| 1135, 1464 | the skip-token class index, never the token | no text |
| 1176 | the PR URL, built from the repository slug and the PR number | no token |
| 1211, 1339 | the hit count and line numbers only | no matched line |
| 1217 | the integrity counts (NUL, `client_encoding`, home path) | no path |
| 1265 | a sha256 prefix, the byte count, the shape counts, the carried count and `changed` | no dump text |
| 1273-1274 | the bot-PR status sentence (PR numbers and a tip prefix) | no token |
| 1299 | writer, version and staged-set defects: counts, versions, keys, paths and row names | no dump text |
| 1358 | a fixed sentence | none |
| 1492 | the VERSION transition, two sha256 prefixes and two counts | no dump text |

Refusal messages follow the same rule: the argument validators name the flag and never the value, and the
basename refusal names a count and positions and never the name. Self-test arms prove both (for example `a
malformed --merge refuses by name without echoing the value` and `a merge tree holding
supabase/migrations/2026_bad.sql refuses by count and position, never echoing the name`).

The one pass-through is `realRunner` (843-844), which prints the stdout and stderr of the three child gates
`--compose` runs: `scripts/local-stack/run.sh --check-currency`, `scripts/baseline-content-drift-check.mjs`
and `scripts/check-baseline-staleness.mjs`. The drift check's finding messages name function identifiers,
statuses and hashes only (`content-drift`, `allowlist-hash-moved`, `allowlist-stale`), never a body line.
The children run in `redump-pr`, after the compose-side five-class re-scan has passed, over a dump that the
same job is about to publish in a public PR. The pass-through therefore discloses nothing the PR does not.

The dump step never prints the dump:

```
$ awk 'NR>=1276 && NR<1475 && !/^[[:space:]]*#/' .github/workflows/supabase-migrate.yml | grep -cE '\b(cat|head|tail|less|more)\b'
0
```

### 6. Third-party code in the write job: PASS

```
$ awk 'NR>=1418 && NR<1475 && !/^[[:space:]]*#/ && /uses:/ {print NR": "$0}' .github/workflows/supabase-migrate.yml
1433:       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
1438:       - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0
1441:       - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
$ awk 'NR>=1418 && NR<1475 && !/^[[:space:]]*#/' .github/workflows/supabase-migrate.yml | grep -cE '\b(npm|npx|curl|wget|gitleaks|pip)\b'
0
$ grep -nE "^import .* from ['\"]" scripts/baseline-redump.mjs scripts/check-baseline-staleness.mjs scripts/baseline-content-drift-check.mjs scripts/check-baseline-currency.mjs | grep -vE "from ['\"](node:|\./)"
(no output)
```

`redump-pr` uses exactly checkout, setup-node and download-artifact, each pinned to a 40-hex sha. It has no
`npm ci`, `npx`, `curl`, `wget`, `pip` or gitleaks. The node scripts it runs import only `node:` builtins and
sibling scripts, so the job needs no `node_modules`. For comparison, `redump-dump` uses checkout,
setup-node, `supabase/setup-cli` and `upload-artifact`, each sha-pinned (1292, 1317, 1339, 1391).

### 7. Trigger surface: PASS

```
$ sed -n '119p;150,155p' .github/workflows/supabase-migrate.yml
on:
  push:
    branches: [main]
    paths:
      - 'supabase/migrations/**'
  workflow_dispatch:
$ grep -cE 'pull_request_target|workflow_run' .github/workflows/supabase-migrate.yml
0
$ git diff 8f33ddaff HEAD -- .github/workflows/supabase-migrate.yml | awk '/^@@/'
@@ -23,6 +23,14 @@ name: Supabase Migrate
@@ -152,7 +160,12 @@ concurrency:
@@ -1233,6 +1246,232 @@ jobs:
```

`on:` is still `push` (branch `main`, path `supabase/migrations/**`) plus `workflow_dispatch`. The three
phase hunks are the header "Flow:" comment (line 23), the corrected permissions comment below
`concurrency:` (line 152) and the two jobs (line 1233). None touches the `on:` block. There is no
`pull_request_target` and no `workflow_run` anywhere in the file, comments included. `8f33ddaff` is the
merge base of this branch and `origin/main`.

### 8. Branch reach: PASS, with the D-25 accepted risk recorded

```
$ grep -nE 'git\b.*\bpush\b' .github/workflows/supabase-migrate.yml | grep -vE '^[0-9]+:\s*#' | grep -vE 'db push'
1472:          git -c credential.helper= -c 'credential.helper=!gh auth git-credential' push --force-with-lease="refs/heads/automation/baseline-redump:${LEASE}" origin HEAD:refs/heads/automation/baseline-redump
$ grep -nE '"push"' scripts/baseline-redump.mjs
2668:      g(["push", "-q", bare, "HEAD:refs/heads/main"], { cwd: work });
2674:        g(["push", "-q", bare, `HEAD:refs/heads/${BOT_BRANCH}`], { cwd: work });
2675:        if (pull7) g(["push", "-q", bare, `${tip}:refs/pull/7/head`], { cwd: work });
$ grep -vE '^\s*#' .github/workflows/supabase-migrate.yml | grep -cE 'gh pr merge|--auto\b|pr review|--approve'
0
```

There is exactly one live `git push` in the workflow. Its refspec is
`HEAD:refs/heads/automation/baseline-redump`, and it carries
`--force-with-lease=refs/heads/automation/baseline-redump:${LEASE}`. When the branch is absent, the
`--check-bot-branch` step emits an empty lease, which makes the push require that the branch does not
exist (self-test arm `an absent bot branch emits an EMPTY lease (the push's 'must not exist') and prints
'bot-branch: absent'`). The three script pushes sit inside the self-test and push to a local scratch bare
repository, never to a remote. No live line merges, enables auto-merge or approves.

**Accepted risk (D-25, T-164.9.5-23).** Branch protection and rulesets are off on `main`, so the
`contents: write` token in `redump-pr` could push to `main` if the job ran code that asked it to. This is
bounded by one hard-coded refspec pinned as the only push in the file (wiring test `exactly one live git
push in the file, to HEAD:refs/heads/automation/baseline-redump (D-25)`), by the token being exposed in
one step only (item 4), and by the job running no third-party code beyond three GitHub-owned, sha-pinned
actions (item 6). It stays accepted until a ruleset or branch protection exists, which is a founder
decision deferred until paying clients.

### Findings and fixes

None. All eight items passed, so neither `.github/workflows/supabase-migrate.yml` nor
`scripts/baseline-redump.mjs` changed in this plan. Plan 03 Task 3's real-file compose run was therefore
not re-run: that re-run is required only when this plan changes the script.

## Gate runs (Task 1 verify plus the orchestrator's set)

```
$ actionlint .github/workflows/supabase-migrate.yml
actionlint exit=0

$ node scripts/baseline-redump.mjs --self-test
baseline-redump self-test OK: 198 assertion(s)
self-test exit=0

$ node scripts/baseline-redump.mjs --self-test --with-gitleaks
baseline-redump self-test OK: 205 assertion(s) (with gitleaks)
self-test --with-gitleaks exit=0

$ npx vitest run src/__tests__/baseline-redump-wiring.test.ts src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/critical-regressions.test.ts src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts src/__tests__/gitleaks-allowlist.test.ts src/__tests__/baseline-wiring-claim.test.ts
 Test Files  6 passed (6)
      Tests  254 passed | 1 skipped (255)
plan-set exit=0

$ npx vitest run src/__tests__/baseline-redump-wiring.test.ts src/__tests__/contracts/ghcr-login-before-image-pull.contract.test.ts src/__tests__/critical-regressions.test.ts src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/mutex-dead-holder-verdict.test.ts
 Test Files  5 passed (5)
      Tests  249 passed (249)
orch-set exit=0
```

The 249 is 16 wiring tests plus 233 in the other four files, which matches the baselines.

**The one skipped test (Rule 12).** It is `.gitleaks.toml — real scanner behavior (H-0017) > advertises skip
reason when the gitleaks binary is unavailable` in `gitleaks-allowlist.test.ts`. It skips by design when
the gitleaks binary IS on PATH, which it is here (`gitleaks version` prints `8.30.1`). It is pre-existing
and not part of this phase.

## Dependency check (Task 1 acceptance)

```
$ git diff --stat 8f33ddaff HEAD -- package.json package-lock.json
(no output)
$ git log --oneline 8f33ddaff..HEAD -- package.json
(no output)
$ git diff --stat origin/main -- package.json package-lock.json
 package.json | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
$ git diff origin/main -- package.json
-  "version": "0.97.0.1"
+  "version": "0.96.0.1"
$ grep -rcE 'actionlint|zizmor' .github/workflows package.json | grep -vc ':0$'
0
$ grep -rcE 'actionlint|zizmor' .github/workflows package.json | wc -l
      16
```

The `origin/main` diff is only `main`'s own version bump. `origin/main` moved ahead of this branch after it
forked. No commit on this branch touches `package.json` or the lockfile, so no dependency was added. All 16
files (15 workflows and `package.json`) print a count of 0 for `actionlint|zizmor`.

## Threat-to-evidence map (Task 2)

There is one row per threat id from the `<threat_model>` blocks of plans 01-05 and 07-09 (this plan's own
register re-lists 01, 22, 23, 24 and SC). "Artifact" names the self-test arm (by its printed name), the
wiring test (`baseline-redump-wiring.test.ts`, by its `it()` name) or the Task 1 checklist item that
verifies the mitigation today. "RED observed" says where a run of that artifact against a broken
implementation was recorded. Every artifact below passed at this plan's HEAD (self-test 198 and 205, wiring
16/16, checklist 8/8).

| Id | Disposition | Mitigation as shipped | Verifying artifact | RED observed |
|---|---|---|---|---|
| T-164.9.5-01 | mitigate | Summary lines carry sha prefixes, byte counts and shape counts; refusals carry counts, positions and flag names, never values | Checklist item 5; arms `secret class '…' refuses at line 16131, prints only the count and line, and writes nothing` (6 classes) and `a malformed --merge refuses by name without echoing the value` | Yes: 02-SUMMARY Task 1 RED (the six class arms FAIL) and Task 3 RED (the not-HEAD arm FAILed because plan 01 echoed both sha prefixes) |
| T-164.9.5-02 | mitigate | Marker from `git ls-tree <merge>`, with `--merge` asserted equal to HEAD | Arms `an UNCOMMITTED migration in the working tree is absent from the marker (D-09)` and `a --merge that is a real commit but not HEAD refuses, naming --merge and the rule, echoing neither sha` | Yes: 02-SUMMARY "removed-equality RED" (the `head !== merge` check deleted, and the not-HEAD arm alone FAILs) |
| T-164.9.5-03 | mitigate | `git add --` over the six explicit `STAGED_PATHS`, then `judgeStagedSet` | Arms `STAGED_PATHS is the six paths, sorted`, `the cached set before commit equals STAGED_PATHS (D-17)`, `judgeStagedSet refuses one extra path (supabase/.temp/project-ref), naming it` | Yes: 01-SUMMARY (the STAGED_PATHS arm removed, so the self-test count refuses); 03-SUMMARY Task 2 RED (four `judgeStagedSet` arms FAIL) |
| T-164.9.5-04 | mitigate | Fixed templates that name no skip token; the D-14 guard refuses before commit | Arms `the fixed templates (entry, commit message, PR title, regenerated section, PR body) carry no CI skip token (D-14)` and `judgeSkipTokens passes the commit message, PR title and PR body composed from the fixed templates` | Yes: 03-SUMMARY Task 2 RED (the templates-pass arm FAILs) |
| T-164.9.5-05 | mitigate | The row writer touches only the `## Provenance` span, and the staleness gate runs as a child | Arms `BASELINE.md carries exactly ONE full-sha row, and it is the new sha` and `[real BASELINE.md, read-only] no defect, exactly ONE RECORDED_SHA_RE_ALL row and it is the new sha, …`; `node scripts/check-baseline-staleness.mjs` (05-SUMMARY: `recorded-rows=1 defects=0`) | Yes: 07-SUMMARY "File-wide-count RED" (a file-wide row count turns the real-file arm RED) |
| T-164.9.5-06 | mitigate | The five-class scan prints its count and line numbers only | The six `secret class '…' refuses at line 16131, prints only the count and line, and writes nothing` arms | Yes: 02-SUMMARY Task 1 RED |
| T-164.9.5-07 | mitigate | `assertScanTarget` before gitleaks is spawned | Arms `assertScanTarget refuses a missing path`, `… a zero-byte file`, `[real gitleaks] a MISSING target is refused before gitleaks is spawned (it would exit 0, a false clean)`, `[real gitleaks] an EMPTY target …` | Yes: 02-SUMMARY "bypassed-assert RED": with the assert reduced to `return`, the real gitleaks passed both targets as clean |
| T-164.9.5-08 | mitigate | `--ignore-gitleaks-allow` in `gitleaksArgv` | Arms `gitleaksArgv: dir <target>, an explicit --config .gitleaks.toml, --redact, --ignore-gitleaks-allow, a JSON report` and `[real gitleaks] the same fixture with an inline allow comment STILL refuses (--ignore-gitleaks-allow)` | Yes: 02-SUMMARY direct 8.30.1 measurement (the allow comment gives `rc=0` without the flag and `rc=1` with it) |
| T-164.9.5-09 | mitigate | `--redact`; the script prints RuleID and line only | Arms `a gitleaks finding refuses, printing RuleID and line only, never the Secret/Match field` and `[real gitleaks] a runtime JWT-shaped fixture refuses, printing RuleID and line only` | Yes: 02-SUMMARY Task 2 RED (the RuleID-only arm FAILs) |
| T-164.9.5-10 | mitigate | Explicit `--config .gitleaks.toml`; the version is probed against `GITLEAKS_VERSION` | Arms `gitleaksArgv: …` and `[real gitleaks] 'gitleaks version' is the pinned 8.30.1`; wiring `gitleaks: one version across the workflow, ci.yml and the script; sha256-checked before extraction (D-26)` | Yes: 09-SUMMARY calibrations (the workflow's or `ci.yml`'s version drifts, and the pin goes RED). A RED of the `--config` element alone is not separately recorded |
| T-164.9.5-11 | mitigate | Data-statement and home-path refusals | Arms `a dump with one INSERT INTO line refuses (a schema-only dump carrying data)`, `integrity refuses a runner home path, naming the line and never the path`, `integrity refuses a macOS home path, …` | Yes: 02-SUMMARY Task 1 RED |
| T-164.9.5-12 | mitigate | The strict 14-digit basename rule applies before any write | Arms `MIGRATION_BASENAME_STRICT_RE takes exactly 14 digits and lower snake case` and `a merge tree holding supabase/migrations/2026_bad.sql refuses by count and position, never echoing the name` | Yes: 02-SUMMARY Task 1 RED |
| T-164.9.5-13 | mitigate | Validators name the flag, never the value | Arms `a malformed --merge refuses by name without echoing the value`, `a malformed --run-id …`, `a malformed --cli-version …`, and the not-HEAD arm | Partly: 02-SUMMARY Task 3 RED covers the not-HEAD arm. The three validator arms were already green at RED, because plan 01 built them |
| T-164.9.5-14 | mitigate | `--check-bot-branch` refuses a foreign author and names the PR; the push leases the observed sha | Arms `a commit on the bot branch authored by anyone but the bot REFUSES (D-24), naming the branch, a foreign count of 1 and pull request #7, and emits no lease` and `the refusal never prints the foreign commit's message or its author's email` | Yes: 08-SUMMARY "Foreign-author RED" (the author check removed, and three arms FAIL) |
| T-164.9.5-15 | mitigate | `judgeSkipTokens` over the message, title and body before commit, and again before gh | Arms `judgeSkipTokens refuses token class 1 of 6 …` through `class 6 of 6 (the trailer) …`, and `a skip token reaching the composed PR body refuses before the commit, …` | Yes: 03-SUMMARY Task 2 RED, plus the scratch variant with `judgeSkipTokens` returning `[]` (all six class arms and the compose arm FAIL) |
| T-164.9.5-16 | mitigate | `buildPrArgv` can only create or edit | Arm `neither argv carries the merge subcommand, the auto flag, a review or an approval (D-13)`; wiring `the workflow carries no PR merge, auto-merge or approval token on any live line (D-13)` | Yes: 08-SUMMARY Task 2 RED (`RED_EVIDENCE_OK` for the forbidden-token argv target); 09-SUMMARY M5 |
| T-164.9.5-17 | mitigate | Explicit `git add --` plus the `judgeStagedSet` refusal | Arms `an untracked supabase/.temp/project-ref is never staged: …`, `a path already in the index beyond the six refuses at judgeStagedSet as 'staged-extra', …`, `the bot commit touches exactly the six paths` | Yes: 03-SUMMARY (the scratch variant with `git add -A` and the judge neutered turns the `.temp` arm FAIL) |
| T-164.9.5-18 | mitigate | `judgeMeasured` schema check plus a node re-scan in the write job | Arms `judgeMeasured refuses a measured.json missing any one of the 21 schema-1 keys, …`, `compose re-runs the five-class scan on the artifact dump: …`, `compose runs judgeMeasured first: …` | Yes: 03-SUMMARY Task 1 RED (eight `judgeMeasured` arms and the compose re-scan arms FAIL) |
| T-164.9.5-19 | mitigate | `judgeVersionBump` and the duplicate-heading refusal run before any write | Arms `judgeVersionBump: VERSION equal to the base is 'version-not-bumped' …`, `insertChangelogEntry refuses a '## [<new>]' heading that already exists`, `a writer defect in --compose prints ::error:: with its kind and refuses before … anything is staged` | Yes: 07-SUMMARY Task 1 RED (23 writer-contract arms FAIL) |
| T-164.9.5-20 | mitigate | No gitleaks, no npm install and no third-party code in `redump-pr` | Checklist item 6; wiring `redump-dump runs the gitleaks self-test; redump-pr the plain one, with no gitleaks on a live line` | Yes: 09-SUMMARY calibration (a `redump-pr` self-test with `--with-gitleaks` goes RED) |
| T-164.9.5-21 | mitigate | PROD secrets only in two step `env:` blocks of `redump-dump`; the dump is written to `$RUNNER_TEMP` and never printed | Checklist items 4 and 5; wiring `credentials: step-level env: only in redump-dump; none in redump-pr but its one GH_TOKEN (D-03, D-05)` | Yes: 09-SUMMARY M1 (a secret seeded into `redump-pr` goes RED) plus three more calibrations |
| T-164.9.5-22 | mitigate | No expression in any `run:` body, and the trigger surface is unchanged | actionlint (exit 0); checklist items 1 and 7; wiring `no run: body in either job contains an ${{ }} expression (D-03, D-18)` | Yes: 09-SUMMARY M2 (an expression seeded into the gate body goes RED, and so does one in a shell comment) |
| T-164.9.5-23 | **accept** | D-25: branch protection is off, so `contents: write` could reach `main`. Bounded by the one hard-coded refspec with a lease, one token-bearing step, and no third-party code in the job | Checklist item 8; wiring `exactly one live git push in the file, to HEAD:refs/heads/automation/baseline-redump (D-25)` | The bound's RED: 09-SUMMARY M6 (a second push to `main` goes RED). **Boundary:** accepted only until a ruleset or branch protection exists on `main`, which is a founder decision deferred until paying clients |
| T-164.9.5-24 | mitigate | Two jobs (D-21): the PROD credential with a read-only token plus `packages: read`; the write token in `redump-pr`'s push step only | Checklist items 2, 4 and 6; wiring `permissions are exactly {contents: read, packages: read} and {contents: write, pull-requests: write} (D-05)` and `redump-dump runs in environment Production and redump-pr in none (D-03, D-21)`; `ghcr-login-before-image-pull.contract.test.ts` | Yes: 09-SUMMARY M3 (`actions: write` added to `redump-dump` fails `toEqual`) |
| T-164.9.5-25 | mitigate | Upload only when `changed`, only after every gate (default success, no `always()`), three files, `retention-days: 1` | Wiring `the upload is gated on the gate step's changed output, kept 1 day, with no always() (D-21)` | Yes: 09-SUMMARY M4 (`always()` added goes RED), plus calibrations for the removed `if:` and a 7-day retention |
| T-164.9.5-26 | mitigate | No merge, auto-merge or approval path in the workflow or the script | Wiring `the workflow carries no PR merge, auto-merge or approval token on any live line (D-13)`; checklist item 8 (grep count 0) | Yes: 09-SUMMARY M5 plus three more calibrations (auto-merge flag, review approval, auto-merge action) |
| T-164.9.5-27 | mitigate | A `--check-bot-branch` step before the push, and `--force-with-lease` on the sha it observed | Checklist item 8; arm `a bot branch whose fetched tip differs from the ls-remote tip is MEASURE_FAIL: the lease must be the sha whose commits were judged`; `supabase-migrate-test-first.test.ts` softening scan over `redump-pr` | Yes: 04-SUMMARY (`\|\| true` seeded onto the `--check-bot-branch` line turns the softening scan RED); 08-SUMMARY foreign-author RED |
| T-164.9.5-28 | mitigate | The script's guard runs before commit and again before gh; the workflow adds no message text of its own | Arm `judgeSkipTokens re-runs over the title and body: a hit in either refuses by class index before gh is called at all (D-14)`; checklist item 1 (no expression, and so no composed text, in any `run:` body) | Yes: 08-SUMMARY Task 2 RED (`RED_EVIDENCE_OK` for the skip-token refusal target) |
| T-164.9.5-29 | mitigate | Plans, docs and commit messages say "the skip tokens" in prose | Constraint-7 sweep, run for this plan: `git log --format='%B' 8f33ddaff..HEAD` over 54 branch commits gives 0 hits; `supabase-migrate.yml`, `BASELINE.md` and `TODOS.md` give 0 each | Yes, measured this plan: the same regex over a scratch file holding a fragment-built token counts 1 (the file was deleted) |
| T-164.9.5-30 | mitigate | No home path, username or project ref in tracked text | `npm run check:planning-hygiene` (05-SUMMARY; re-run for this plan after each `git add`: `OK — 7451 tracked files scanned`) | Not recorded in this phase. The gate's own suite, `check-planning-hygiene.test.ts`, predates the phase |
| T-164.9.5-31 | mitigate | Doc edits are additions-only and keep the pattern equality and the single-sha-row rule | Wiring `SECRET_SCAN_PATTERN equals the grep -anE pattern BASELINE.md documents (D-06)`; the self-test arm `SECRET_SCAN_PATTERN is byte-equal to the grep -anE argument in BASELINE.md`; staleness gate `recorded-rows=1 defects=0` | Yes: 09-SUMMARY M7 (a one-character edit to BASELINE.md's pattern turns the wiring test AND the self-test RED) |
| T-164.9.5-32 | mitigate | BASELINE.md and the PR body state that approval and reading the conclusions are required, and that a zero-check merge is possible | 05-SUMMARY marker count (`Approve workflows to run` 1); arms `D-23 presence (PR body): branch protection is off, so a zero-check merge is possible` and `… the 'Approve workflows to run' instruction` | Yes for the PR body: 07-SUMMARY Task 2 RED (36 of 42 presence arms FAIL). The BASELINE.md paragraph is pinned by a count only |
| T-164.9.5-33 | mitigate | `rewriteBaselineMd` counts and writes inside `provenanceSpan` only | Arms `rewriteBaselineMd rewrites the capture rows only, leaves the nested and later EXTRA rows byte-unchanged, …`, `… refuses a DUPLICATE '\| Shape \|' row …`, and the real-file read-only arm | Yes: 07-SUMMARY "File-wide-count RED" |
| T-164.9.5-34 | mitigate | Composers read only the measured object, with one presence arm per D-16 value | The 28 `D-16 presence (…)` arms (14 for the entry, 14 for the section), plus the two `no newly carried migration is a measured 'none'` arms | Yes: 07-SUMMARY Task 2 RED, plus four scratch-copy calibrations |
| T-164.9.5-35 | mitigate | Exact tip-sha match on `/head` refs; zero matches is a measured negative; an `ls-remote` failure is MEASURE_FAIL | Arms `findPullRefsForSha returns the refs/pull/<n>/head refs at the sha in NUMERIC ascending order, …`, `the same foreign commit with NO pull ref at its tip refuses and states, as a measured negative, …`, `an ls-remote that fails … is MEASURE_FAIL and never emits a lease` | Yes: 08-SUMMARY "Removed-naming RED" and Task 1 RED |
| T-164.9.5-36 | mitigate | `judgeDriftLine` is anchored to `^baseline-content-drift: findings \d+$`, exactly once | Arms `judgeDriftLine passes the REAL five-line shape on its 'findings 0' line only (…)`, `judgeDriftLine: two findings lines are MEASURE_FAIL`, `… trailing text …` | Yes: 03-SUMMARY first-line and any-line variants (the five-line green arm FAILs under both) |
| T-164.9.5-SC | mitigate | The gitleaks 8.30.1 tarball is checked against a sha256 hard-coded in the workflow (`sha256sum -c`), and the checksums file is never fetched. zizmor runs only behind a blocking human checkpoint, pinned `==1.30.1`, offline, and never as a dependency | Gitleaks half: wiring `gitleaks: one version across the workflow, ci.yml and the script; sha256-checked before extraction (D-26)`. zizmor half: the Task 3 checkpoint, plus the Task 1 dependency check (`actionlint\|zizmor` count 0 in every workflow and `package.json`) | Gitleaks half: yes, 09-SUMMARY calibrations (`sha256sum -c` removed; the pinned sha256 shortened by one hex digit). zizmor half: **pending Task 3** (blocking-human). No zizmor has run for this plan |

**No row lacks an artifact, so Task 2 raised no FINDING.** Exactly one row, T-164.9.5-23, carries
`accept`, and none was downgraded to make a row pass. Three rows say honestly that part of their RED was not
recorded: T-10 (the `--config` element alone), T-13 (three validator arms already green at RED) and T-30
(the hygiene gate's RED predates the phase). Each still has a passing artifact today, so none is a FINDING.

## Pre-merge security gate (Task 2)

**`/gsd-secure-phase 164.9.5` MUST pass before the phase PR merges** (D-20). The ROADMAP marks this
workflow as security-sensitive: it adds a job that reads PRODUCTION with a repository secret and a job that
pushes to a public repository.

- **Who runs it.** The orchestrator runs it, in parallel with `gsd-verifier`. This plan does not run it.
- **Its inputs.** The `## Threat-to-evidence map` above, the Task 1 `## Workflow-injection checklist`, and
  the `<threat_model>` blocks of plans 01-05 and 07-09.
- **Its bar.** Security enforcement is ASVS level 1, and it blocks on `high`. A `high` finding means the PR
  does not merge.
- **Why this is the last stop.** After merge, any merge that touches `supabase/migrations/**` applies to
  PROD automatically, with no human reviewer (CLAUDE.md, corrected 2026-09-23: the founder removed the
  `Production` environment's required reviewer). The first such apply is also the first run of
  `redump-dump` and `redump-pr`. This pre-merge gate is therefore the last human-read stop before both
  jobs run for the first time.
- **What stays open after it.** T-164.9.5-23 remains an accepted risk, bounded as the map states. The live
  proof of the push and PR path (`gh workflow run supabase-migrate.yml --ref main`, which should no-op per
  D-10) is a post-merge human-verification item owned by plan 05, not by this gate.

## Task Commits

1. **Task 1: actionlint and the eight-item injection checklist**: `543784001` (docs). It records the review in this file. No code changed, because no item raised a FINDING.
2. **Task 2: threat-to-evidence map and the `/gsd-secure-phase` gate record**: the `docs(164.9.5-06)` commit that adds these sections.
3. **Task 3: optional zizmor**: NOT executed. It is a `checkpoint:human-verify` with `gate="blocking-human"`, awaiting `approved` or `skip-zizmor`. This file has no `## zizmor` section yet, on purpose.

## Deviations from Plan

None in Tasks 1-2. The plan was executed as written. One process note: Task 1 changed no file, so its record went into this SUMMARY as its own commit, and Task 2 appended to it. That gives each task an atomic commit.

## Next

The plan is paused at Task 3. The continuation agent adds `## zizmor` (the pasted output, and fixes for any finding on the two redump jobs, followed by Task 1's verify re-run) or `## zizmor (skipped)` (the human's reason). It then sets `status: complete`.
