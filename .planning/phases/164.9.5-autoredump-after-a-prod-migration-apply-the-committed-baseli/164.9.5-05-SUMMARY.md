---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
plan: 05
subsystem: infra
tags: [baseline, schema-dump, docs, todos, github-actions]

requires:
  - phase: 164.9.5
    provides: "scripts/baseline-redump.mjs (plans 01-03, 07, 08), the redump-dump / redump-pr jobs (plan 04), the wiring test (plan 09)"
provides:
  - "BASELINE.md `## Regenerating` paragraph documenting the automated re-dump, its refusals, the never-merge rule and the manual fallback"
  - "closed TODOS `[164.9.5-MANUAL-BASELINE-REDUMP]` entry with the phase evidence"
  - "two post-merge human-verification items (D-18 dispatch, D-23 approval)"
affects: [164.9.5-06, ship, founder-uat-queue]

actuals:
  tokens: 1400      # chars/4 over the realized diff (5478 chars, plan_head_before..HEAD)
  tasks: 2
  commits: 2        # MEASURED: git rev-list --count plan_head_before..HEAD at SUMMARY write
plan_head_before: 12ed2ffd9c8642a7495c647c2c9ddf1e15c52a40

tech-stack:
  added: []
  patterns:
    - "Docs describe the automation by job and mode NAME, never by line number"
    - "Ledger records the self-test command and its OK-line shape, never an assertion count"

key-files:
  created: []
  modified:
    - supabase/schema/BASELINE.md
    - TODOS.md

key-decisions:
  - "The Version-skew caveat stays unnarrowed until the post-merge dispatch measures byte identity (RESEARCH A1, D-18)"
  - "Skip tokens are named only as 'the skip trailer' in the new prose (CLAUDE.md CI gate integrity 1)"

patterns-established:
  - "Additions-only doc edits proven against the task commit with git diff -U0 C~1 C"

requirements-completed:
  - "164.9.5-MANUAL-BASELINE-REDUMP"

coverage:
  - id: D1
    description: "BASELINE.md `## Regenerating` gains the AUTOMATED 2026-09-26 paragraph (additions only, pattern pin and single sha row intact)"
    requirement: "164.9.5-MANUAL-BASELINE-REDUMP"
    verification:
      - kind: unit
        ref: "npx vitest run src/__tests__/baseline-redump-wiring.test.ts src/__tests__/baseline-wiring-claim.test.ts"
        status: pass
      - kind: other
        ref: "node scripts/check-baseline-staleness.mjs"
        status: pass
      - kind: other
        ref: "git diff --numstat 77e549b7c~1 77e549b7c -- supabase/schema/BASELINE.md (23 added, 0 removed)"
        status: pass
    human_judgment: false
  - id: D2
    description: "TODOS `[164.9.5-MANUAL-BASELINE-REDUMP]` closed with a dated evidence paragraph, no assertion count"
    requirement: "164.9.5-MANUAL-BASELINE-REDUMP"
    verification:
      - kind: other
        ref: "awk first-match line begins '- [x]'; closing marker count 1; npm run check:planning-hygiene"
        status: pass
      - kind: other
        ref: "git diff --numstat 72aa0342b~1 72aa0342b -- TODOS.md (18 added, 1 removed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Live proof of the automation (post-merge dispatch and first bot PR approval)"
    verification: []
    human_judgment: true
    rationale: "Requires a merge to main, a PROD-credentialed workflow run and a human click in the GitHub UI; no plan runs it (D-18, D-23)"

duration: 8min
completed: 2026-09-26
status: complete
---

# Phase 164.9.5 Plan 05: Document the automation and close the ledger entry Summary

**`BASELINE.md` `## Regenerating` now says the `redump-dump` / `redump-pr` jobs re-dump PRODUCTION after every successful `apply` on `main`, lists every refusal class, says the one bot PR never merges and needs a human to approve its workflows, and keeps the manual procedure as the fallback. `[164.9.5-MANUAL-BASELINE-REDUMP]` is closed with that evidence.**

## Performance

- **Duration:** about 8 min
- **Started:** 2026-09-26T16:05:32Z
- **Completed:** 2026-09-26T16:13Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- One additions-only paragraph (`⭐ **AUTOMATED 2026-09-26 (Phase 164.9.5 AUTOREDUMP).**`) placed right after "**Regenerate-then-shrink.**". It names the jobs, the bot branch `automation/baseline-redump`, and the script's modes (`--gate-dump`, `--compose`, `--check-bot-branch`, `--open-or-edit-pr`). It lists the refusals: the five-class scan (count and line numbers only), gitleaks (explicit config, redacted, inline allows ignored, missing or empty dump refused), NUL / `SET client_encoding` / home path, zero tables or data statements, a marker not taken from the applied merge, a red currency / content-drift / staleness gate, the skip trailer, and a commit on the bot branch that the bot did not author. It also covers the six-path write set, the no-op `::notice::`, never merging with **Approve workflows to run**, the zero-check merge risk, and the manual fallback.
- The `grep -anE` line, marker recipe, SP-M03 paragraph, `## Provenance` table, every `### Regenerated` section and the `**Version-skew caveat**` paragraph are unchanged.
- The TODOS entry is flipped to `- [x]` and given a `✅ **CLOSED 2026-09-26 by Phase 164.9.5 AUTOREDUMP.**` paragraph. It names the script and its modes, the two self-test commands with the OK-line shape and no count, the two jobs, the wiring test and the BASELINE.md paragraph. It ends with the live-proof sentence.

## Task Commits

1. **Task 1: BASELINE.md `## Regenerating` gains the automation paragraph (D-19)**: `77e549b7c` (docs)
2. **Task 2: close `[164.9.5-MANUAL-BASELINE-REDUMP]` and record the human-verification items**: `72aa0342b` (docs)

The acceptance diffs are evaluated against these two shas. No later commit in this plan touches either file.

## Files Created/Modified

- `supabase/schema/BASELINE.md`: the automation paragraph in `## Regenerating` (23 lines added, 0 removed)
- `TODOS.md`: checkbox flip plus the closing paragraph (18 added, 1 removed)

## Gate evidence (verbatim, run in this worktree)

After the Task 1 edit:

- `npx vitest run src/__tests__/baseline-redump-wiring.test.ts src/__tests__/baseline-wiring-claim.test.ts` gave `Test Files  2 passed (2)` and `Tests  18 passed (18)`
- `node scripts/check-baseline-staleness.mjs` gave `✅ No defects — supabase/schema/baseline.sql matches the sha256 recorded in supabase/schema/BASELINE.md` (`recorded-rows=1 defects=0`), rc 0
- `bash scripts/local-stack/run.sh --check-currency` gave `baseline-currency: carried=277 replay=0 marker-sha=match defects=0`, rc 0
- `node scripts/baseline-content-drift-check.mjs` gave `baseline-content-drift: findings 0`
- `node scripts/baseline-redump.mjs --self-test` gave `baseline-redump self-test OK: 198 assertion(s)`
- Marker counts: `AUTOMATED 2026-09-26 (Phase 164.9.5 AUTOREDUMP)` 1, `automation/baseline-redump` 1, `Approve workflows to run` 1, `fallback` (case-insensitive) 1
- Additions-only against `77e549b7c`: removed=0, added=23, sha256 rows added=0, numstat `23	0`

After the Task 2 edit:

- The awk first-match line is `- [x] **\`[164.9.5-MANUAL-BASELINE-REDUMP]\` ...`, the closing-marker count is 1, and `npm run check:planning-hygiene` exits 0
- `grep -cE 'baseline-redump self-test OK: [0-9]' TODOS.md` gave `0`
- Against `72aa0342b`: removed=1, added=18, numstat `18	1`

Self-test OK lines, measured at `git rev-parse HEAD` = `72aa0342b7cad8146f77b9d0bd7599416febe73f`:

```
baseline-redump self-test OK: 198 assertion(s)
baseline-redump self-test OK: 205 assertion(s) (with gitleaks)
```

(gitleaks 8.30.1 is on PATH locally.) Plan 06 edits the script again, so these counts are evidence for this commit only.

## Human verification (post-merge, not run by any plan)

1. **D-18: the post-merge dispatch.** Run `gh workflow run supabase-migrate.yml --ref main`.
   - `apply-test` should take the shared-TEST mutex (it may queue for up to 3600 s) and apply zero migrations. `apply` should apply zero too.
   - `redump-dump` should then run end to end. If byte identity holds (assumption A1), it prints its `::notice::` no-op with `changed=false` and `redump-pr` is skipped.
   - If it produces a diff instead, that diff IS the first bot PR, and a human reads it hunk by hunk.
   - Bind the reading to the run's head sha with CLAUDE.md's SHA-bound count command (`gh api "repos/<owner>/<repo>/actions/runs?head_sha=<sha>" -q '.workflow_runs | length'`), and read each run's conclusion.
   - If the push is refused, apply RESEARCH F10's extraheader fallback (assumptions A3/A4).
   - Only this dispatch can justify narrowing the `**Version-skew caveat**` in BASELINE.md. It stays as written until then.
2. **D-23: the first bot PR's approval.** Click **Approve workflows to run** on the first bot PR.
   - Then list the head-SHA runs with their conclusions: `gh api "repos/<owner>/<repo>/actions/runs?head_sha=<sha>"` with a jq that prints name, status and conclusion.
   - Merge only on a full green set. Branch protection is off, so a merge with zero completed checks is possible.
   - If no approval banner appears, use the close-and-reopen fallback (assumption A2).

## Ship-time note (VERSION / CHANGELOG)

This phase's own VERSION bump and CHANGELOG entry are written at ship time by `/gsd-ship` under CLAUDE.md "CHANGELOG discipline", in the same `chore(release)` commit as the VERSION bump. No plan writes them. At ship, confirm that the filtered `-pr` branch's `TODOS.md` diff carries both this entry and its closure. The entry first arrived in a planning commit, so it has to survive the filter to reach main.

## Decisions Made

- The Version-skew caveat is left unnarrowed (RESEARCH A1). The post-merge dispatch is what measures byte identity.
- The prose names the skip tokens only as "the skip trailer" and never spells one.
- `requirements.mark-complete` was skipped because `164.9.5-MANUAL-BASELINE-REDUMP` is a TODOS id and has 0 hits in `.planning/REQUIREMENTS.md`.

## Deviations from Plan

None. The plan was executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None. The two live checks above are post-merge human-verification items.

## Next Phase Readiness

Ready for plan 06 (wave 7). The TODOS closing paragraph and the BASELINE.md paragraph name the script's modes but no assertion count, so plan 06's further edits to the script do not make them stale.

## Self-Check: PASSED

- FOUND: supabase/schema/BASELINE.md (marker count 1)
- FOUND: TODOS.md (closing marker count 1)
- FOUND: commit 77e549b7c
- FOUND: commit 72aa0342b

---
*Phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli*
*Completed: 2026-09-26*
