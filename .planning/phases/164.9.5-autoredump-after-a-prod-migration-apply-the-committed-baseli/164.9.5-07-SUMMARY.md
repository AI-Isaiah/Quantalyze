---
phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli
plan: 07
subsystem: infra
tags: [baseline, supabase, changelog, version-gate, github-actions, self-test]

requires:
  - phase: 164.9.5-01
    provides: "the baseline-redump tracer: --gate-dump, --compose, the plan-01 writers and composers"
  - phase: 164.9.5-02
    provides: "the gate-side refusals and the 71-arm self-test this plan extends"
provides:
  - "writers that return {text, defects} and refuse by name: nextVersion, bumpPackageJson, insertChangelogEntry, rewriteBaselineMd"
  - "rewriteBaselineMd scoped to the ## Provenance span, proven read-only on the real BASELINE.md"
  - "judgeVersionBump, a mirror of check-version-bump.mjs judge() and its three DEFECTS"
  - "composeChangelogEntry and composeRegeneratedSection carrying every D-16 value; composePrBody carrying every D-23 instruction"
  - "--compose writes <out>/pr-body.md after the bot commit"
affects: [164.9.5-03, 164.9.5-08, 164.9.5-09]

actuals:
  tokens: 10100    # chars/4 over the +/- lines of the realized diff (40434 chars)
  tasks: 2
  commits: 4       # MEASURED: git rev-list --count f35fcd533..HEAD before the SUMMARY commit
plan_head_before: f35fcd533dcfa5dfe54828dd54ee4a1abc1b9b3a
commits: 4

tech-stack:
  added: []
  patterns:
    - "writer contract: {text, defects}, text null on any defect, defects named by kind with counts and row names only"
    - "--compose computes every text and runs the version-gate mirror before writing any of the four files"
    - "presence arms: one ok() per measured value per target, over a fixture whose every value is distinctive"

key-files:
  created: []
  modified:
    - scripts/baseline-redump.mjs

key-decisions:
  - "provenanceSpan returns null instead of throwing, so rewriteBaselineMd can report 'provenance-missing' as a defect. Nothing outside the script calls it."
  - "rewriteBaselineMd requires the first '### Regenerated' heading to be the heading that ends the span, and re-reads its own output: exactly one full-sha row equal to the new sha, and the span now ending at the inserted heading. That last check is what keeps the NEXT run's count scoped."
  - "versionDefects is renamed judgeVersionBump, as the plan names it. No other file referenced the old name."
  - "The PR body's repository comes from GITHUB_REPOSITORY, which Actions always sets, validated as <owner>/<name>. A local run falls back to DEFAULT_REPO, because plan 03's scratch-clone verify runs --compose with no such variable and must still get a pr-body.md."
  - "pr-body.md is written after the bot commit, because the head sha the gh api command names exists only then."

patterns-established:
  - "Writers refuse by name: a regex replace that matched nothing is a defect, never a silent no-op."
  - "A presence arm per measured value, calibrated by deleting that value in a scratch copy."

requirements-completed: ["164.9.5-MANUAL-BASELINE-REDUMP"]

coverage:
  - id: D1
    description: "Writers return {text, defects} and refuse by name (version shape, package.json 0/2 occurrences, read-back, unparseable JSON, duplicate/no-title/no-heading CHANGELOG)"
    requirement: "164.9.5-MANUAL-BASELINE-REDUMP"
    verification:
      - kind: unit
        ref: "node scripts/baseline-redump.mjs --self-test#SELF-TEST 1b/4"
        status: pass
    human_judgment: false
  - id: D2
    description: "rewriteBaselineMd counts and writes the four capture rows only inside provenanceSpan, inserts the section at the span end, and is proven read-only on the real BASELINE.md"
    requirement: "164.9.5-MANUAL-BASELINE-REDUMP"
    verification:
      - kind: unit
        ref: "node scripts/baseline-redump.mjs --self-test#rewriteBaselineMd rewrites the capture rows only"
        status: pass
      - kind: unit
        ref: "node scripts/baseline-redump.mjs --self-test#[real BASELINE.md, read-only]"
        status: pass
    human_judgment: false
  - id: D3
    description: "judgeVersionBump mirrors check-version-bump.mjs's three defects; --compose refuses on any writer or version-gate defect before writing or staging"
    requirement: "164.9.5-MANUAL-BASELINE-REDUMP"
    verification:
      - kind: unit
        ref: "node scripts/baseline-redump.mjs --self-test#judgeVersionBump; a writer defect in --compose prints ::error::"
        status: pass
      - kind: other
        ref: "node scripts/check-version-bump.mjs --self-test"
        status: pass
    human_judgment: false
  - id: D4
    description: "CHANGELOG entry and ### Regenerated section carry every D-16 value; the PR body carries every D-23 instruction; --compose writes pr-body.md"
    requirement: "164.9.5-MANUAL-BASELINE-REDUMP"
    verification:
      - kind: unit
        ref: "node scripts/baseline-redump.mjs --self-test#SELF-TEST 1c/4"
        status: pass
      - kind: integration
        ref: "scratch-clone --gate-dump + --compose over the real tree (plan 03's verify shape)"
        status: pass
    human_judgment: false
  - id: D5
    description: "The PR body's wording is clear enough for a human reviewer to act on"
    verification: []
    human_judgment: true
    rationale: "Every instruction is pinned by a presence arm, but whether a reviewer reads and follows them is a judgment no test makes. The first real bot PR is where this is seen."

duration: 19min
completed: 2026-09-26
status: complete
---

# Phase 164.9.5 Plan 07: the redump writers refuse by name, and the entry, section and PR body carry every measured value

**The four writers in `scripts/baseline-redump.mjs` now return `{text, defects}` and refuse by name instead of silently not editing. `rewriteBaselineMd` counts the capture rows only inside `## Provenance`, is proven on the real `BASELINE.md`, and inserts the regenerated section at the span's end. `judgeVersionBump` mirrors CI's version gate. The CHANGELOG entry, the `### Regenerated` section and the new `pr-body.md` carry every D-16 value and every D-23 instruction, each pinned by an arm that has been seen to fail.**

## Performance

- **Duration:** about 19 min
- **Started:** 2026-09-26T14:56:45Z
- **Completed:** 2026-09-26T15:15Z
- **Tasks:** 2, each RED then GREEN
- **Files modified:** 1 (`scripts/baseline-redump.mjs`, +561 / -71)

## Accomplishments

- The plain `--self-test` goes from 71 to **133** assertions. `--self-test --with-gitleaks` goes to 140, with gitleaks 8.30.1 on PATH.
- **Writers refuse by name (D-15, D-16).** Every refusal is a `{kind, detail}` defect whose detail carries counts, versions and row names only:
  - `nextVersion`: `version-shape`.
  - `bumpPackageJson`: `package-version-count`, `package-json-unparseable`, `package-version-readback`.
  - `insertChangelogEntry`: `changelog-no-title`, `changelog-no-entry-heading`, `changelog-entry-heading`, `changelog-duplicate-heading`, `changelog-not-first`.
  - `rewriteBaselineMd`: `provenance-missing`, `capture-row-count` (names the row and the count), `regenerated-missing`, `regenerated-not-span-end`, `section-not-span-end`, `sha-row-count`.
- **`--compose` refuses before it writes (T-164.9.5-19).** It computes all four texts first, then runs `judgeVersionBump` on them. Only then does it write VERSION, package.json, CHANGELOG.md and BASELINE.md. A defect prints one `::error::baseline-redump: <kind>: <detail>` line per defect and refuses, and nothing is staged. A self-test arm proves this on a scratch clone whose CHANGELOG already carries the next version.
- **Scoped BASELINE.md writer (T-164.9.5-33).** Three arms prove the scoping:
  - A fixture carrying EXTRA `| Shape |` and prefix-form `| sha256 |` rows under a nested `### Regenerated` and a later `## ` section is rewritten with those rows byte-unchanged.
  - A read-only arm over the REAL `supabase/schema/BASELINE.md` returns no defect. The output carries exactly one `RECORDED_SHA_RE_ALL` row, equal to the synthetic sha, and every original line except the four capture rows appears in the same order.
  - After insertion, the span ends at the new heading, so the next run's count is scoped the same way.
- **D-16 values, pinned.** The entry has 14 presence arms and the section has 14, over a fixture whose every value is distinctive. There is one more arm per target for the measured "none". Further arms check the heading and section vocabulary, the section's missing "what it adds" column, its zero full-64-hex values and its prefix-form sha256 row. A compose-level arm proves the drift gate's `SCOPE —` line reaches neither text, even though the injected gate printed it.
- **D-23 PR body, pinned.** Eight presence arms cover it. `--compose` writes `<out>/pr-body.md` beside `pr-title.txt`, naming the bot commit's own head sha and the repository.
- **End to end on the real tree.** I cloned this branch to scratch and ran plan 03's verify shape: `--gate-dump` then `--compose` over the committed dump plus one newline. It printed `changed=true` and `committed=true` and staged exactly the six paths. The author and committer were `github-actions[bot]`. The staleness gate, the currency gate and the content-drift gate were green on the composed tree. The BASELINE.md diff removed 0 lines other than capture rows. There was exactly one `### Regenerated … run 1` heading. The first `## [` heading equalled VERSION (`0.96.0.2`), and `pr-body.md` was 1876 bytes.

## Task Commits

1. **Task 1 RED:** `7f445eca2` (test): the writer-contract arms fail against the plan-01 writers.
2. **Task 1 GREEN:** `7dbf1d3d7` (feat): the writers refuse by name, scoped to the Provenance span.
3. **Task 2 RED:** `18f7d5db7` (test): the D-16 and D-23 presence arms fail against the plan-01 composers.
4. **Task 2 GREEN:** `dda2ec29a` (feat): the entry, the regenerated section and the PR body carry every measured value.

## Evidence (verbatim)

### Task 1 RED (`7f445eca2`): 23 new arms FAIL, 68 hold, 91 ran, rc=1

```
  FAIL — nextVersion bumps the 4th field, returning {text, defects}
  FAIL — nextVersion refuses 0.93.0, 0.93.0.3.1, v0.93.0.3, 0.93.0.x and a trailing newline as 'version-shape'
  FAIL — bumpPackageJson refuses 2 '"version"' occurrences, naming the count
  FAIL — bumpPackageJson refuses 0 occurrences, naming the count
  FAIL — bumpPackageJson refuses when the one replaced occurrence is not the top-level version (the JSON read-back differs)
  FAIL — bumpPackageJson refuses a result that is not JSON
  FAIL — judgeVersionBump: a well-formed bump has no defect
  FAIL — judgeVersionBump: VERSION equal to the base is 'version-not-bumped' (check-version-bump.mjs DEFECTS[0])
  FAIL — judgeVersionBump: VERSION and package.json differing is 'version-package-mismatch' (DEFECTS[1])
  FAIL — judgeVersionBump: a moved VERSION with no CHANGELOG heading is 'changelog-missing-entry' (DEFECTS[2])
  FAIL — insertChangelogEntry puts the new heading FIRST and leaves the prior entry byte-unchanged
  FAIL — insertChangelogEntry refuses a '## [<new>]' heading that already exists
  FAIL — insertChangelogEntry refuses a file without the '# Changelog' title
  FAIL — insertChangelogEntry refuses a file with no '\n## [' heading
  FAIL — rewriteBaselineMd rewrites the capture rows only, leaves the nested and later EXTRA rows byte-unchanged, and inserts the section as the new end of the span
  FAIL — rewriteBaselineMd refuses a DUPLICATE '| Shape |' row inside the table, naming the row and the count 2 (calibration)
  FAIL — rewriteBaselineMd refuses a capture row that matches 0 times, naming the row and the count 0
  FAIL — rewriteBaselineMd refuses a file with no '## Provenance' heading
  FAIL — rewriteBaselineMd refuses a file with no '### Regenerated' heading to insert above
  FAIL — [real BASELINE.md, read-only] no defect, exactly ONE RECORDED_SHA_RE_ALL row and it is the new sha, every other original line kept in order
  FAIL — [real CHANGELOG.md, read-only] the next version is absent, and inserting its entry has no defect and puts it first
  FAIL — [real package.json + VERSION, read-only] the bump has no defect and reads back as the next version
  FAIL — a writer defect in --compose prints ::error:: with its kind and refuses before VERSION, package.json, CHANGELOG or BASELINE.md is written or anything is staged
=== SELF-TEST FAILED: 91 assertion(s) run, at least one did not hold ===
```

Each arm fails on an assertion about the new contract, never on a crash. A writer that throws or returns a bare string reads back as `defects: null`. `gsd-tools check tdd-red-evidence` returned `RED_EVIDENCE_OK` for three target arms: the duplicate `| Shape |` calibration, the zero-match `| Taken |` row and the duplicate CHANGELOG heading. The record is a mechanical TAP projection of the self-test's own `ok`/`FAIL` lines, because the self-test does not print TAP itself.

### Task 1 GREEN (`7dbf1d3d7`)

```
baseline-redump self-test OK: 91 assertion(s)

=== SELF-TEST PASSED: 5/5, every defect kind fired on its own input ===
VERIFY-T1-PASS
```

The last lines are `node scripts/check-version-bump.mjs --self-test` and the plan's no-import check. `grep -c "check-version-bump.mjs" scripts/baseline-redump.mjs` gave `5`, all in comments and arm names, and `grep -cE '^import .*check-version-bump'` gave `0`.

**File-wide-count RED (acceptance criterion).** I made a scratch copy with the capture-row loop changed from `for (let i = start; i < end; i++)` to `for (let i = 0; i < lines.length; i++)`. It went `rc=1`:

```
  FAIL — rewriteBaselineMd rewrites the capture rows only, leaves the nested and later EXTRA rows byte-unchanged, and inserts the section as the new end of the span
  FAIL — rewriteBaselineMd refuses a DUPLICATE '| Shape |' row inside the table, naming the row and the count 2 (calibration)
  FAIL — [real BASELINE.md, read-only] no defect, exactly ONE RECORDED_SHA_RE_ALL row and it is the new sha, every other original line kept in order
=== SELF-TEST 3/4: --compose in the same scratch repo, with the child gates injected
::error::baseline-redump: capture-row-count: the '## Provenance' capture table carries 2 '| sha256 |' row(s); exactly 1 is required
::error::baseline-redump: capture-row-count: the '## Provenance' capture table carries 3 '| Shape |' row(s); exactly 1 is required
::error::baseline-redump: refusing to compose: 2 defect(s) (capture-row-count, capture-row-count); nothing was staged
```

The real-file arm goes RED under a file-wide count, as the plan predicted. The duplicate-Shape calibration fails too, because the file-wide count reports 3 rather than 2. The copy was deleted.

### Task 2 RED (`18f7d5db7`): 36 of 42 new arms FAIL, 133 ran, rc=1

```
  FAIL — D-16 presence (CHANGELOG entry): tables old and new
  FAIL — D-16 presence (CHANGELOG entry): policies old and new
  FAIL — D-16 presence (CHANGELOG entry): function statements old and new
  FAIL — D-16 presence (CHANGELOG entry): distinct function names old and new
  FAIL — D-16 presence (CHANGELOG entry): newly carried basename 1
  FAIL — D-16 presence (CHANGELOG entry): newly carried basename 2
  FAIL — D-16 presence (CHANGELOG entry): the baseline-currency line, verbatim
  FAIL — D-16 presence (CHANGELOG entry): the functions-compared drift line, verbatim
  FAIL — D-16 presence (CHANGELOG entry): the findings drift line, verbatim
  FAIL — D-16 presence (CHANGELOG entry): data statements old and new
  FAIL — D-16 presence (regenerated section): the old sha256 prefix
  FAIL — D-16 presence (regenerated section): the new sha256 prefix
  FAIL — D-16 presence (regenerated section): tables old and new
  FAIL — D-16 presence (regenerated section): policies old and new
  FAIL — D-16 presence (regenerated section): function statements old and new
  FAIL — D-16 presence (regenerated section): distinct function names old and new
  FAIL — D-16 presence (regenerated section): newly carried basename 1
  FAIL — D-16 presence (regenerated section): newly carried basename 2
  FAIL — D-16 presence (regenerated section): the baseline-currency line, verbatim
  FAIL — D-16 presence (regenerated section): the functions-compared drift line, verbatim
  FAIL — D-16 presence (regenerated section): the findings drift line, verbatim
  FAIL — D-16 presence (regenerated section): data statements old and new
  FAIL — D-16 presence (CHANGELOG entry): no newly carried migration is a measured 'none', not an omission
  FAIL — D-16 presence (regenerated section): no newly carried migration is a measured 'none', not an omission
  FAIL — the entry's heading is '## [<new>] - <date> — …' and its sections are ### Changed then ### Notes
  FAIL — the section heading ends with the run id, it has no 'what it adds' column, zero full-64-hex values, and a prefix-form sha256 row
  FAIL — D-23 presence (PR body): the 'Approve workflows to run' instruction
  FAIL — D-23 presence (PR body): reading each run's conclusion
  FAIL — D-23 presence (PR body): branch protection is off, so a zero-check merge is possible
  FAIL — D-23 presence (PR body): the SHA-bound gh api command with the real repo and head sha
  FAIL — D-23 presence (PR body): the invitation to add the 'what it adds' column
  FAIL — D-23 presence (PR body): re-dispatching supabase-migrate.yml on main when main moved or VERSION collides
  FAIL — D-23 presence (PR body): close-and-reopen named only as a fallback
  FAIL — D-23 presence (PR body): the PR is never auto-merged
  FAIL — --compose writes <out>/pr-body.md beside pr-title.txt, carrying the repo and the bot commit's own head sha, and no skip token
  FAIL — the captured drift lines reach the entry and the section verbatim, and the SCOPE line the gate also printed reaches neither
=== SELF-TEST FAILED: 133 assertion(s) run, at least one did not hold ===
```

Six new arms held at RED because plan 01's composers already wrote those values: in the entry, the run id, the merge short sha and both sha256 prefixes; in the Task-1 section, the run id and the merge short sha. Calibrations 3 and 4 below show each of the six going RED. `check tdd-red-evidence` returned `RED_EVIDENCE_OK` for three targets: the entry's tables arm, the PR body's approve-button arm and the `pr-body.md` arm.

### Task 2 GREEN (`dda2ec29a`)

```
baseline-redump self-test OK: 133 assertion(s)
VERIFY-T2-PASS
baseline-redump self-test OK: 140 assertion(s) (with gitleaks)
```

`VERIFY-T2-PASS` is the plan's skip-token grep over the script returning 0 and chained after the OK line.

### Scratch-copy calibrations (Task 2 acceptance; every copy deleted)

| # | What the scratch copy removed | Arms that went RED |
|---|---|---|
| 1 | the currency line from `composeChangelogEntry`'s gates bullet | exactly `D-16 presence (CHANGELOG entry): the baseline-currency line, verbatim` |
| 2 | the never-auto-merged sentence from `composePrBody` | exactly `D-23 presence (PR body): the PR is never auto-merged` |
| 3 | the sha256 prefixes from the entry's first bullet (arms that held at RED) | the entry's old and new sha256-prefix arms, plus the pre-existing section-3 arm `CHANGELOG's first entry is the new version and carries the run id and the new sha prefix` |
| 4 | run id and merge replaced inside the entry and section composers only (arms that held at RED) | the entry's and the section's run-id and merge arms (4), the section-heading arm, and the same pre-existing section-3 arm |

Calibrations 1 and 2 are the plan's "exactly its arm" calibrations, and each turned exactly one arm RED. Calibrations 3 and 4 are extra. They exist so that the six arms that held at RED have also been seen to fail.

### The composed entry and section, verbatim from the self-test fixture

```
## [4.5.6.8] - 2026-03-04 — BASELINE: automated re-dump after the PROD apply of 0a1b2c3d

### Changed
- `supabase/schema/baseline.sql` re-dumped from PRODUCTION by Supabase Migrate run `987654321`, after the PROD apply of merge `0a1b2c3d`: sha256 `a1b2c3d4…` → `e6f7a8b9…`.
- Shape, old → new: tables 61 → 62, policies 151 → 153, function statements 117 → 119, distinct function names 113 → 115, data statements 2 → 0.
- Migrations the dump newly carries, from the marker diff: `20260301000000_first_fixture.sql`, `20260302000000_second_fixture.sql`.
- `supabase/schema/BASELINE.md` gets the new `## Provenance` capture rows and a dated `### Regenerated 2026-03-04` section; `baseline-carried-migrations.txt` is regenerated from the merge tree; VERSION and package.json 4.5.6.7 → 4.5.6.8.
- The gates on the composed tree, verbatim: `baseline-currency: carried=279 replay=0 marker-sha=match defects=0`, `baseline-content-drift: functions compared 119 — MATCH 116, DRIFT 3, SNAPSHOT_MISSING 0, SNAPSHOT_ONLY 0, UNCOMPARABLE 0`, `baseline-content-drift: findings 0`.

### Notes
- The dump was taken read-only by the `redump-dump` job after the `apply` job of Supabase Migrate run `987654321` succeeded, and this entry was composed by the `redump-pr` job. Run `987654321` is the provenance anchor.
- The "what it adds" judgment for each newly carried migration is a human one, so it is left to the reviewer. Every figure above is measured.
```

```
### Regenerated 2026-03-04 — automated re-dump after Supabase Migrate run 987654321

Taken read-only by the `redump-dump` job of Supabase Migrate run `987654321`, after that run's `apply` job applied merge `0a1b2c3d` to PRODUCTION, and composed onto `main` by the `redump-pr` job. Every value below is measured.

**Which migrations the new dump now carries** — from the marker diff:

- `20260301000000_first_fixture.sql`
- `20260302000000_second_fixture.sql`

**MEASURED:**

| | |
|---|---|
| Taken | 2026-03-04 |
| Supabase CLI | 2.98.2 |
| Shape | tables 61 → 62, policies 151 → 153, function statements 117 → 119, distinct function names 113 → 115 |
| Data statements | 2 → 0 |
| sha256 | `a1b2c3d4…` → `e6f7a8b9…` |
| Currency gate | `baseline-currency: carried=279 replay=0 marker-sha=match defects=0` |
| Body drift | `baseline-content-drift: functions compared 119 — MATCH 116, DRIFT 3, SNAPSHOT_MISSING 0, SNAPSHOT_ONLY 0, UNCOMPARABLE 0`; `baseline-content-drift: findings 0` |
```

The fixture's `data statements 2 → 0` exists only to make that arm distinctive. On a real run the gate refuses any dump that carries data, so the new value is always 0.

### End-to-end scratch-clone run (plan 03's verify shape, `LC_ALL=C`)

```
baseline-redump gate: sha256=e228656e… bytes=787780 tables=63 policies=155 carried=277 changed=true
changed=true
committed=true
CHANGELOG.md VERSION package.json supabase/schema/BASELINE.md supabase/schema/baseline-carried-migrations.txt supabase/schema/baseline.sql
github-actions[bot]|github-actions[bot]
✅ No defects — supabase/schema/baseline.sql matches the sha256 recorded in supabase/schema/BASELINE.md
baseline-currency: carried=277 replay=0 marker-sha=match defects=0
baseline-content-drift: findings 0
removed-non-capture=0
regen-headings=1
first-heading=0.96.0.2 VERSION=0.96.0.2
1876 pr-body.md
80 pr-title.txt
```

The clone lived under the session scratch directory and was deleted afterwards. Nothing in this checkout's `supabase/schema`, `VERSION`, `package.json` or `CHANGELOG.md` changed. The real files are only ever read.

## Files Created/Modified

- `scripts/baseline-redump.mjs`:
  - The writer contract, `judgeVersionBump`, `composeRegeneratedSection`, `composePrBody` and `DEFAULT_REPO`.
  - `--compose` computes before it writes, and writes `pr-body.md`.
  - `EXPECTED_ASSERTIONS` goes 71 → 133.

## Decisions Made

See `key-decisions` in the frontmatter. In brief:
- `provenanceSpan` returns null instead of throwing.
- The span must end at the first `### Regenerated`, and the writer re-reads its own output.
- `versionDefects` is renamed `judgeVersionBump`.
- The repository comes from `GITHUB_REPOSITORY`, with a validated default for local runs.
- `pr-body.md` is written after the commit, so it names the real head sha.

## Deviations from Plan

**1. [Rule 3 - Blocking] node_modules was symlinked instead of running `npm ci`.** The precondition says to run `npm ci` if vitest does not resolve. The orchestrator note and standing constraint 15 say to symlink the main checkout's node_modules. `npx vitest --version` printed `vitest/4.1.10 darwin-arm64 node-v25.8.1`, and I removed the link before each commit. No tracked file changed.

**2. [Rule 1 - Bug, in my own arm] Two Task-1 arms had their regex in the wrong order.** At Task 1 GREEN, the duplicate-Shape and zero-Taken arms matched `/Shape.*\b2\b/` against a detail that reads `carries 2 '| Shape |' row(s)`, where the count comes before the row name. I anchored both on the detail's real wording (`carries 2 .\| Shape \|. row`) before the GREEN commit. The writer was already correct, so the RED had been observed for the right reason. The first GREEN run's FAIL was the arm, not the writer.

**3. Six Task-2 arms held at RED.** Plan 01's composers already wrote the run id, the merge short sha and the sha256 prefixes in the entry, and the run id and merge in the Task-1 section. The plan says to observe "every missing value" go RED, and these values were not missing. Calibrations 3 and 4 show all six going RED.

**4. The Task 2 GREEN commit message says "four scratch calibrations each turn exactly the removed value's arm red".** That is exact for calibrations 1 and 2 only. Calibrations 3 and 4 also turn a pre-existing section-3 arm RED, as the table above records. The message is not amended, because that would rewrite a committed sha.

**Total deviations:** 4. One is environmental (1), one is a fix to my own arm before commit (2), and two are recording notes (3, 4). **Impact:** none on the script's behaviour.

## Issues Encountered

None beyond the deviations.

## Known Stubs

None. `composeRegeneratedSection` was a two-line placeholder between the Task 1 and Task 2 commits, as the plan directs, and is complete at `dda2ec29a`.

## Next Phase Readiness

- Plan 03 can wire the skip-token guard into `--compose` after `judgeVersionBump` and before `git commit` / `--open-or-edit-pr`. `composePrBody` and `pr-body.md` exist under the names it reads.
- Plan 09's pins are untouched: `SECRET_SCAN_PATTERN`, `BOT_BRANCH`, `GITLEAKS_VERSION` and the OK-line shape.
- Nothing is left red or skipped in this plan's gates.

## Self-Check: PASSED

- `scripts/baseline-redump.mjs` FOUND.
- Commits `7f445eca2`, `7dbf1d3d7`, `18f7d5db7` and `dda2ec29a` FOUND in `git log`.
- `node scripts/baseline-redump.mjs --self-test` gives `baseline-redump self-test OK: 133 assertion(s)`.

---
*Phase: 164.9.5-autoredump-after-a-prod-migration-apply-the-committed-baseli*
*Completed: 2026-09-26*
