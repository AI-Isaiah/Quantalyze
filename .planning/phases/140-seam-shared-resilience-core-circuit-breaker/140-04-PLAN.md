---
phase: 140-seam-shared-resilience-core-circuit-breaker
plan: 04
type: execute
wave: 3
depends_on: [140-02]
files_modified:
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.test.ts
  - src/app/api/strategies/create-with-key/route.ts
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/api/strategies/create-with-key/route.audit.test.ts
  - src/app/api/strategies/composite/add-key/route.ts
  - src/app/api/strategies/composite/add-key/route.test.ts
autonomous: true
requirements: [SEAM-04]

must_haves:
  truths:
    - "A CircuitOpenError during key-connect surfaces as 503 + a real wizard error code with retry copy — NEVER the UNKNOWN/500 'something went wrong, team notified' terminal branch"
    - "Classification branches on the error TYPE before the substring cascade — no lower.includes('circuit') anywhere (research Pitfall 2 warning sign)"
    - "wizardErrors.ts imports CircuitOpenError ONLY from the dependency-free leaf — the client bundle for 10 'use client' wizard components gains ZERO server-only code (B-1)"
  artifacts:
    - path: "src/lib/wizardErrors.ts"
      provides: "SERVICE_UNAVAILABLE_RETRY code + copy; type-checked CircuitOpenError early-return via @/lib/seam-errors"
      contains: "SERVICE_UNAVAILABLE_RETRY"
    - path: "src/app/api/strategies/create-with-key/route.ts"
      provides: "passes the error OBJECT to the classifier + maxDuration export"
      contains: "export const maxDuration = 300"
    - path: "src/app/api/strategies/composite/add-key/route.ts"
      provides: "same"
      contains: "export const maxDuration = 300"
  key_links:
    - from: "src/lib/wizardErrors.ts"
      to: "CircuitOpenError from @/lib/seam-errors"
      via: "instanceof early-return BEFORE the ~12 substring branches (leaf import — browser-safe, never mocked)"
      pattern: "instanceof CircuitOpenError"
---

<objective>
Close the Class-2 cascade-500: `classifyKeyValidationError` (wizardErrors.ts:890) classifies
by substring-matching `err.message`, so a breaker trip during key-connect falls through to
`{code:"UNKNOWN", status:500}` — the wizard shows "something went wrong, team notified"
during a Railway outage. Add a TYPE-checked `SERVICE_UNAVAILABLE_RETRY` branch before the
substring cascade and thread the error object (not its message) from both wizard routes.
The CircuitOpenError import comes from the browser-safe leaf — wizardErrors is value-imported
by 10 "use client" components and must never pull the server-only seam graph (B-1).

Output: breaker trips during key-connect surface as honest, retryable 503 wizard copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-RESEARCH.md
@src/lib/seam-errors.ts
@src/lib/wizardErrors.ts
@src/lib/analytics-client.ts

<interfaces>
From @/lib/seam-errors (dependency-free leaf, 140-01): `CircuitOpenError` (`.retryAfterS: number`).
This is the ONLY legal import source for wizardErrors: wizardErrors.ts has ZERO server-only
imports today and is value-imported by 10 "use client" components (MetadataStep:10,
CsvPreviewStep:4, MultiKeyConnectStep, SyncPreviewStep, CsvSubmitStep, ConnectKeyStep,
SubmitStep, CsvUploadStep, CsvValidationEnvelope, StrategyGrid). Importing from
`@/lib/analytics-client` or `@/lib/resilient-fetch` would chain in `@upstash/*` plus
resilient-fetch's non-tree-shakeable module-load side effects — shipping them to the browser
for every wizard user (BLOCKER B-1; cf. the `src/lib/analytics.ts:1` server-only convention).
The leaf import also survives every test mock shape: `create-with-key/route.audit.test.ts:51`
and `add-key/route.test.ts:48` mock analytics-client with BARE factories — a class routed
through those mocks would be undefined (B-2); the leaf is never mocked.

From @/lib/analytics-client (after 140-02): `validateKey`/`encryptKey` may throw `CircuitOpenError` (the leaf class).
Current classifier: `classifyKeyValidationError(message: string): { code: WizardErrorCode; status: number }` at wizardErrors.ts:890-961, ~12 `lower.includes(...)` branches, terminal `{code:"UNKNOWN", status:500}` at :960. Its own comment at :919-926 documents the substring-collision fragility — do NOT add a substring branch.
Call sites passing `.message` today: `create-with-key/route.ts:438-452`, `composite/add-key/route.ts:393`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wizardErrors — SERVICE_UNAVAILABLE_RETRY code, copy, and type-checked classification</name>
  <files>src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts</files>
  <read_first>src/lib/wizardErrors.ts:1-160 (WizardErrorCode union + WIZARD_ERROR_COPY shape) and :880-961 (classifier + its collision-invariant comment), src/lib/wizardErrors.test.ts IN FULL — especially :29-52, the copy-invariant suite every WIZARD_ERROR_COPY entry must satisfy, and the allowed action-ID set it enforces</read_first>
  <behavior>
    - `classifyKeyValidationError(new CircuitOpenError(30))` → `{ code: "SERVICE_UNAVAILABLE_RETRY", status: 503 }` — checked via instanceof BEFORE any substring logic
    - A CircuitOpenError whose message contains "timeout" or "rate" STILL classifies as SERVICE_UNAVAILABLE_RETRY (type wins over substring — the collision regression)
    - Existing string inputs classify exactly as before (every current test unmodified and green)
    - `{code:"UNKNOWN"}` terminal behavior unchanged for unrecognized strings
    - The new WIZARD_ERROR_COPY entry passes the pre-existing copy-invariant suite (wizardErrors.test.ts:29-52) with NO invariant weakened
  </behavior>
  <action>
    1. Add `"SERVICE_UNAVAILABLE_RETRY"` to the `WizardErrorCode` union and a `WIZARD_ERROR_COPY` entry with honest retry copy (headline: service temporarily unavailable; body: your key was NOT saved, retry in a moment — match the copy register of existing entries; no invented internals). The entry MUST satisfy the four pre-existing invariants enforced by wizardErrors.test.ts:29-52 (checker W-11 — write to them, do not iterate blindly): (a) non-empty `title` longer than 4 characters; (b) `docsHref` matching `/^\/security(#|$)/`; (c) non-empty `cause` and `fix`; (d) `actions` drawn ONLY from the fixed allowed-ID set that the test enforces — read the set from the test file and pick the retry-appropriate existing IDs; do NOT invent a new action ID.
    2. Change `classifyKeyValidationError`'s signature to `(error: unknown)`: first branch `error instanceof CircuitOpenError` — import from `@/lib/seam-errors`, NEVER from `@/lib/analytics-client` or `@/lib/resilient-fetch` (B-1: wizardErrors is client-bundle-reachable via 10 "use client" components; the leaf is the only browser-safe home; add a comment saying exactly this so a future refactor doesn't "simplify" the import) → `{ code: "SERVICE_UNAVAILABLE_RETRY", status: 503 }`; then derive `message` (`error instanceof Error ? error.message : String(error)`) and run the EXISTING substring cascade untouched. Add a comment citing research Pitfall 2: type-check precedes substrings by design; never add a `lower.includes("circuit")` branch.
    3. Extend wizardErrors.test.ts with the behavior cases, including the collision regression (CircuitOpenError with a message containing "timeout" → still SERVICE_UNAVAILABLE_RETRY, not KEY_NETWORK_TIMEOUT). The copy-invariant suite at :29-52 iterates all entries — confirm it picks up the new entry automatically (it should; if it uses an explicit list, add the new code to it).
    Note: the two route call sites break on the signature change — they are fixed in Task 2 of THIS plan; typecheck gates at the plan level, so commit both tasks before pushing.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SERVICE_UNAVAILABLE_RETRY" src/lib/wizardErrors.ts` ≥ 3 (union + copy + classifier)
    - `grep -cE 'includes\("circuit' src/lib/wizardErrors.ts` = 0
    - `grep -c "from \"@/lib/seam-errors\"" src/lib/wizardErrors.ts` = 1 AND `grep -cE "from \"@/lib/(analytics-client|resilient-fetch)\"" src/lib/wizardErrors.ts` = 0 (B-1 guard)
    - `grep -n "instanceof CircuitOpenError" src/lib/wizardErrors.ts` appears at a LOWER line number than the first `lower.includes` in the classifier
    - `npx vitest run src/lib/wizardErrors.test.ts` green with all pre-existing cases (including the :29-52 copy invariants) unmodified
  </acceptance_criteria>
  <done>Classifier is type-first; new code + invariant-compliant copy shipped; collision regression pinned; client bundle untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the error object through both wizard routes + pin maxDuration + keep the audit suite green</name>
  <files>src/app/api/strategies/create-with-key/route.ts, src/app/api/strategies/create-with-key/route.test.ts, src/app/api/strategies/create-with-key/route.audit.test.ts, src/app/api/strategies/composite/add-key/route.ts, src/app/api/strategies/composite/add-key/route.test.ts</files>
  <read_first>src/app/api/strategies/create-with-key/route.ts:430-460 (classifier call site + response construction), src/app/api/strategies/composite/add-key/route.ts:385-400 (ditto), src/app/api/strategies/create-with-key/route.test.ts (uses importActual for `@/lib/api/withAuth` at :986 — NOT for analytics-client; checker-verified that ZERO of the seam-mocking test files importActual analytics-client, which is exactly why CircuitOpenError must come from the never-mocked leaf), src/app/api/strategies/create-with-key/route.audit.test.ts (BARE analytics-client mock at :51 — exercises the wizardErrors catch path; checker-flagged as the file that breaks with no owner if omitted), src/app/api/strategies/composite/add-key/route.test.ts (bare factory at :48)</read_first>
  <behavior>
    - SC-5b: validateKey rejecting with CircuitOpenError → route responds 503, body carries code "SERVICE_UNAVAILABLE_RETRY" (via the route's existing wizard-error response shape), NEVER code "UNKNOWN"/status 500
    - All existing wizard-error route tests pass (message-based classification path unchanged); route.audit.test.ts green under the new classifier signature
  </behavior>
  <action>
    In both routes, change the classifier call from `classifyKeyValidationError(message)` to passing the caught error object itself (the research-verified 2-line change per site). Where the catch block narrows to `Error` first, pass the original unknown. Add `export const maxDuration = 300;` with the invariant-test comment convention to BOTH route files. Include the `Retry-After` header on the 503 response when the classified code is SERVICE_UNAVAILABLE_RETRY and the error is a CircuitOpenError (read `.retryAfterS` — instanceof via the leaf import in the route).

    Extend both route.test.ts files with the SC-5b case: import the REAL `CircuitOpenError` from `@/lib/seam-errors` (never through the mocked client module — add-key's factory is bare, B-2) and make the mocked `validateKey` reject with it; leave each file's existing mock factory shape untouched.

    Run `route.audit.test.ts` (create-with-key) — it mocks analytics-client with a bare factory and drives the wizardErrors catch path, and it is in NO other plan's files_modified: verify it stays green under the classifier signature change (the route now passes the error object; the audit test's expectations about response codes/copy may reference classifier output). If it reds, update ONLY its expectations to the new call shape — never weaken its audit assertions.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.test.ts src/app/api/strategies/create-with-key/route.test.ts src/app/api/strategies/create-with-key/route.audit.test.ts src/app/api/strategies/composite/add-key/route.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const maxDuration = 300" src/app/api/strategies/create-with-key/route.ts` = 1; same for composite/add-key = 1
    - Both route tests assert: CircuitOpenError → status 503 AND body code ≠ "UNKNOWN"
    - `grep -cE "classifyKeyValidationError\((err|error)\.message\)" src/app/api/strategies/create-with-key/route.ts src/app/api/strategies/composite/add-key/route.ts` = 0 (object threaded, not message)
    - `npx vitest run src/app/api/strategies/create-with-key/route.audit.test.ts` green (audit suite owned + verified by this plan)
    - `npm run typecheck` green (signature change fully propagated)
  </acceptance_criteria>
  <done>Breaker trips during key-connect produce honest 503 retry copy in both wizard routes; maxDuration pinned; audit suite explicitly kept green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authed wizard user → key-connect routes | Request bodies carry raw exchange API secrets |
| Server modules → client bundle | wizardErrors is value-imported by 10 "use client" components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-14 | Information Disclosure | wizard copy leaking internals | mitigate | SERVICE_UNAVAILABLE_RETRY copy is static, passes the :29-52 copy invariants; no upstream detail, no URLs |
| T-140-15 | Denial of Service | user 4xx misclassified as outage | mitigate | 4xx never trips breaker (140-01 SC-3c); classifier keeps KEY_AUTH_FAILED semantics for real 400s |
| T-140-16 | Information Disclosure | secrets in classifier/logs | mitigate | classifier receives the error object only, never the request body; no new log lines carry credentials |
| T-140-31 | Information Disclosure | server-only seam graph shipped to browser via wizardErrors import (B-1) | mitigate | CircuitOpenError imported ONLY from the zero-import leaf; acceptance grep forbids analytics-client/resilient-fetch imports in wizardErrors.ts |
| T-140-SC | Tampering | package installs | accept | zero installs |
</threat_model>

<verification>
- `npx vitest run src/lib/wizardErrors.test.ts src/app/api/strategies/create-with-key/route.test.ts src/app/api/strategies/create-with-key/route.audit.test.ts src/app/api/strategies/composite/add-key/route.test.ts --no-file-parallelism` green
- `npm run typecheck` green
</verification>

<success_criteria>
- SEAM-04 Class-2 closed: type-checked SERVICE_UNAVAILABLE_RETRY before the substring cascade; collision regression pinned; both call sites thread the object
- The DOGFOOD-3-class failure ("something went wrong, team notified" during an infra outage) is structurally impossible for breaker trips
- wizardErrors' client-bundle footprint unchanged (leaf-only import, grep-guarded)
</success_criteria>

<output>
Create `.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-04-SUMMARY.md` when done.
</output>
