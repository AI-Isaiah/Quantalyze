---
phase: 140-seam-shared-resilience-core-circuit-breaker
plan: 02
type: execute
wave: 2
depends_on: [140-01]
files_modified:
  - src/lib/analytics-client.ts
  - src/lib/analytics-client.test.ts
  - src/lib/process-key-client.ts
  - src/lib/process-key-client.test.ts
  - src/lib/resilient-fetch.wiring.test.ts
autonomous: true
requirements: [SEAM-01, SEAM-04]

must_haves:
  truths:
    - "Both analyticsRequest() and postProcessKey() demonstrably invoke the ONE resilientFetch core (spy-proven, not convention)"
    - "postProcessKey returns the typed 503 CIRCUIT_OPEN envelope {ok:false, code, human_message, correlation_id, recoverable} + Retry-After when the breaker is open"
    - "analytics-client maps BOTH AbortError and TimeoutError to AnalyticsTimeoutError (broadened check) and lets CircuitOpenError propagate unwrapped"
    - "Public signatures and error classes unchanged — all 16 existing seam-client-mocking route tests keep passing untouched"
  artifacts:
    - path: "src/lib/analytics-client.ts"
      provides: "all wrappers routed through resilientFetch with per-call-site budgetKeys; re-exports CircuitOpenError from the leaf (back-compat convenience only)"
      contains: "resilientFetch"
    - path: "src/lib/process-key-client.ts"
      provides: "postProcessKey through resilientFetch; CIRCUIT_OPEN envelope arm"
      contains: "CIRCUIT_OPEN"
    - path: "src/lib/resilient-fetch.wiring.test.ts"
      provides: "SC-1c — both clients invoke ONE core; X-User-Id + X-Service-Key byte-for-byte passthrough"
  key_links:
    - from: "src/lib/analytics-client.ts"
      to: "src/lib/resilient-fetch.ts"
      via: "resilientFetch(budgetKey, path, init)"
      pattern: "resilientFetch\\("
    - from: "src/lib/process-key-client.ts"
      to: "src/lib/seam-errors.ts"
      via: "resilientFetch + CircuitOpenError catch (leaf import)"
      pattern: "CircuitOpenError"
---

<objective>
Adapt BOTH seam chokepoints through the one core (SEAM-01) without changing any public
signature or exported error class, and give `postProcessKey` its typed `503 CIRCUIT_OPEN`
envelope arm (SEAM-04) so all five Mechanism-B callers inherit breaker handling for free.
Prove the wiring with SC-1c.

Purpose: hardening cannot cover one path and silently miss the money-onboarding path.
Output: both clients delegating; wiring test green; zero regressions in the 16 mock-based route tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-RESEARCH.md
@src/lib/seam-errors.ts
@src/lib/resilient-fetch.ts
@src/lib/analytics-client.ts
@src/lib/process-key-client.ts

<interfaces>
From src/lib/resilient-fetch.ts (created by 140-01):
- `resilientFetch(budgetKey: SeamBudgetKey, path: string, init: Omit<RequestInit,"signal"> & { timeoutMsOverride?: number }): Promise<Response>` — owns ANALYTICS_URL base, breaker check, AbortSignal.timeout, failure recording. Rethrows timeout/network errors UNWRAPPED; returns non-ok Responses (clients keep their own !ok handling); throws `CircuitOpenError` (has `retryAfterS: number`).
- `SEAM_BUDGETS`, `SeamBudgetKey`.

From src/lib/seam-errors.ts (the dependency-free leaf, 140-01): `CircuitOpenError`. This is
the canonical import path for ALL code and ALL tests. The leaf is never `vi.mock`ed, so
`instanceof CircuitOpenError` works under every existing mock shape.

Preservation contract (research §10.4 — 16 route tests mock these wholesale):
- `validateKey/encryptKey/postProcessKey/...` signatures MUST NOT change
- `AnalyticsTimeoutError` / `AnalyticsUpstreamError` MUST keep their exact names/shapes
- `CircuitOpenError` is additionally RE-EXPORTED from analytics-client.ts as a back-compat
  convenience ONLY. Checker-verified reality: 7 of the 8 seam-mocking route test files use
  FULL `vi.mock` factories (bare or with hand-written class shims, zero importActual) — a
  re-export through the mocked module is `undefined` in those tests. Do NOT rely on re-export
  pickup through mocks anywhere; downstream plans (140-03/04/05/06) import from the leaf.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route analyticsRequest() through the core with per-call-site budgetKeys</name>
  <files>src/lib/analytics-client.ts, src/lib/analytics-client.test.ts</files>
  <read_first>src/lib/analytics-client.ts (full — every wrapper), src/lib/analytics-client.test.ts (existing timeout-mapping test at :375-398 and __INTERNAL_analyticsRequest usage), src/lib/resilient-fetch.ts (core signature + budget keys), src/lib/seam-errors.ts</read_first>
  <behavior>
    - REG-1: existing timeout mapping unchanged — DOMException("aborted","TimeoutError") rejection → AnalyticsTimeoutError; NEW: a plain Error with name "AbortError" ALSO maps to AnalyticsTimeoutError (the broadened check, regression-pinned per research §8)
    - CircuitOpenError thrown by the core propagates to the caller UNWRAPPED — it must NOT be swallowed by the generic "Analytics service is not reachable" arm
    - Upstream !ok behavior unchanged: JSON detail → AnalyticsUpstreamError(detail, status); non-JSON → AnalyticsUpstreamError(text, status)
  </behavior>
  <action>
    Rework the internal `analyticsRequest(path, body, options)` to delegate to `resilientFetch`: add a REQUIRED `budgetKey: SeamBudgetKey` field to its `options` object (the function is module-internal — only `__INTERNAL_analyticsRequest` escapes, for OBSERV-01 unit tests; update those test call sites in the same commit). Timeout resolution becomes `options.timeoutMs` → passed as `timeoutMsOverride`, else the table value. Remove the local `ANALYTICS_URL` constant and the `AbortSignal.timeout`/`signal` line — the core owns both. Keep header construction (Content-Type, X-Api-Version, X-Correlation-Id, conditional X-Service-Key) EXACTLY as-is in the client and pass via init — byte-for-byte preservation. Keep the version-mismatch console.warn and the !ok → AnalyticsUpstreamError mapping in the client unchanged.

    Catch-block ordering (load-bearing): first rethrow `err instanceof CircuitOpenError` untouched — import the class from `@/lib/seam-errors`; then the BROADENED abort check `err instanceof Error && (err.name === "AbortError" || err.name === "TimeoutError")` → `AnalyticsTimeoutError(path, timeoutMs)`; then the existing generic "not reachable" Error.

    Wrapper → budgetKey mapping (delete the two hardcoded `{ timeoutMs: 15_000 }` literals): `validateKey`→"validate-key", `encryptKey`→"encrypt-key", `optimizeScenarioWeights`→"optimize-weights", `computePortfolioAnalytics`→"portfolio-analytics" (do NOT delete this dead export — operator decision 5), `runPortfolioOptimizer`→"portfolio-optimizer" (keep its optional `timeoutMs` param honored as `timeoutMsOverride` — the route currently passes 15_000 which equals the table value; plan 140-05 rewires the route constant), `findReplacementCandidates`→"bridge", `simulateAddCandidate`→"simulator", `recomputeMatch`→"match-recompute", `evalMatch`→"match-eval". Re-export `CircuitOpenError` (sourced from `./seam-errors`) from analytics-client.ts as back-compat convenience — with a comment stating the canonical import path is `@/lib/seam-errors` and that mocked-module re-exports are undefined in full-factory tests.

    Extend `analytics-client.test.ts`: pin the broadened AbortError mapping and the CircuitOpenError passthrough — import the REAL `CircuitOpenError` from `@/lib/seam-errors` and mock `@/lib/resilient-fetch` so `resilientFetch` rejects with it; assert the wrapper rejects with that same instance (not a wrapped generic Error). `afterEach(() => vi.unstubAllGlobals())` in any new describe using fetch stubs.
  </action>
  <verify>
    <automated>npx vitest run src/lib/analytics-client.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/analytics-client.test.ts` green including the pre-existing :375-398 timeout test UNMODIFIED
    - `grep -c "AbortSignal.timeout" src/lib/analytics-client.ts` = 0; `grep -c "ANALYTICS_SERVICE_URL" src/lib/analytics-client.ts` = 0
    - `grep -c "timeoutMs: 15_000" src/lib/analytics-client.ts` = 0 (hardcoded budgets gone)
    - `grep -n "seam-errors" src/lib/analytics-client.ts` ≥ 1 (CircuitOpenError sourced from the leaf; re-export present)
    - Exported wrapper signatures byte-identical (git diff shows no changes to exported function parameter lists)
  </acceptance_criteria>
  <done>analyticsRequest delegates to the core with required budgetKeys; error taxonomy preserved + broadened abort check + CircuitOpenError passthrough, all regression-pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route postProcessKey() through the core + CIRCUIT_OPEN envelope arm</name>
  <files>src/lib/process-key-client.ts, src/lib/process-key-client.test.ts</files>
  <read_first>src/lib/process-key-client.ts (full — envelope shapes at :144-175, header block at :112-125, CT-7 comment), src/lib/process-key-client.test.ts (asserts headers — must keep passing), src/lib/resilient-fetch.ts (SEAM_BUDGETS keys), src/lib/seam-errors.ts (CircuitOpenError.retryAfterS)</read_first>
  <behavior>
    - SC-5d: when the core throws CircuitOpenError, postProcessKey returns `{ok:false, response}` where response is status 503, body `{ok:false, code:"CIRCUIT_OPEN", human_message:<static copy>, correlation_id, recoverable:true}`, and header `Retry-After: String(err.retryAfterS)`
    - Existing envelopes unchanged: TimeoutError/AbortError → 504 UPSTREAM_TIMEOUT; other throw → 502 UPSTREAM_NETWORK_ERROR; upstream !ok → status passthrough
    - Budget split: flow_type "resync"/"onboard" → budgetKey "process-key-enqueue" (15s); "teaser"/"csv" → "process-key-sync" (60s) — mirrors the server's `_is_long_fetch` split
  </behavior>
  <action>
    Replace the raw `fetch(`${ANALYTICS_URL}/process-key`, ...)` with `resilientFetch(budgetKey, "/process-key", init)` where budgetKey derives from `args.flow_type` per the behavior block (add a comment citing `analytics-service/routers/process_key.py:_is_long_fetch` as the source of the split). Remove the local `ANALYTICS_URL` constant. Preserve the ENTIRE header block byte-for-byte (Authorization Bearer, X-Correlation-Id, X-User-Id CT-4, conditional X-User-Access-Token) and `cache: "no-store"`.

    Catch ordering: `CircuitOpenError` FIRST (imported from `@/lib/seam-errors`) → build the 503 CIRCUIT_OPEN NextResponse per the behavior block with static human copy ("The analytics service is temporarily unavailable. Please try again in a moment." — never include upstream detail; this is the locked envelope shape, ROADMAP decision 8) and `Retry-After` header; then the existing isAbort arm (update its log line so the hardcoded "after 60s" becomes the resolved budget ms — the constant is no longer universally 60s); then the network arm. Log lines keep the routeTag + correlation_id convention; never log `args.context` (carries raw API secrets).

    Extend `process-key-client.test.ts`: SC-5d test — import the REAL `CircuitOpenError` from `@/lib/seam-errors` and mock `@/lib/resilient-fetch` so it rejects with `new CircuitOpenError(30)`; assert status 503, body.code "CIRCUIT_OPEN", all five envelope fields, Retry-After header present. Plus one enqueue-vs-sync budgetKey selection assertion (spy on resilientFetch: "resync" → "process-key-enqueue", "teaser" → "process-key-sync"). Keep the existing header-assertion tests passing unmodified. `afterEach(() => vi.unstubAllGlobals())`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/process-key-client.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/process-key-client.test.ts` green, existing header tests unmodified
    - `grep -c "CIRCUIT_OPEN" src/lib/process-key-client.ts` ≥ 1; `grep -c "AbortSignal.timeout" src/lib/process-key-client.ts` = 0; `grep -c "ANALYTICS_SERVICE_URL" src/lib/process-key-client.ts` = 0
    - `grep -n "seam-errors" src/lib/process-key-client.ts` ≥ 1 (leaf import)
    - `PostProcessKeyArgs` and `PostProcessKeyResult` types byte-identical
    - SC-5d test asserts all five envelope fields AND the Retry-After header
  </acceptance_criteria>
  <done>postProcessKey delegates with the enqueue/sync budget split; CIRCUIT_OPEN envelope arm shipped — all 5 Mechanism-B routes inherit it via their existing `result.response` passthrough.</done>
</task>

<task type="auto">
  <name>Task 3: SC-1c wiring test — both clients invoke the ONE core</name>
  <files>src/lib/resilient-fetch.wiring.test.ts</files>
  <read_first>140-VALIDATION.md (SC-1c row), src/lib/analytics-client.ts and src/lib/process-key-client.ts (post-adaptation import shape), src/test/helpers/fetch.ts</read_first>
  <action>
    Create `src/lib/resilient-fetch.wiring.test.ts` proving SC-1c: partially mock `@/lib/resilient-fetch` (importActual, then `vi.spyOn`-style wrap of `resilientFetch` resolving a minimal ok JSON Response — this file is OURS and may use importActual; the constraint on downstream route tests does not apply here). Call `validateKey("deribit","k","s")` (with ANALYTICS_SERVICE_KEY env set) and `postProcessKey({flow_type:"resync", source:"test", context:{}, userId:"u"})` (with INTERNAL_API_TOKEN env set). ⚠️ Env MUST be set BEFORE the client modules load: `SERVICE_KEY` is captured at module scope (`analytics-client.ts:15`, `process.env.ANALYTICS_SERVICE_KEY ?? ""`) and the `X-Service-Key` header is conditional on it being truthy (`:87`) — so set the env vars, then `vi.resetModules()` and **dynamically import** both clients (mirror the precedent at `analytics-client.test.ts:33-60`). A `beforeEach` env set after a static top-level import yields `""` and the header assertion fails. Assert the spy was called EXACTLY once per client call, with budgetKey "validate-key" and "process-key-enqueue" respectively. This is the repo's "test the wiring, not just the helper" discipline: it fails if either client regresses to a raw fetch. Header-forwarding assertions (byte-for-byte guards): the init passed to the spy for postProcessKey contains the exact `X-User-Id` value supplied (CT-4), AND the init passed for validateKey contains the exact `X-Service-Key` value from ANALYTICS_SERVICE_KEY (a dropped X-Service-Key silently unauths every analytics call). `afterEach(() => vi.unstubAllGlobals())`; restore env in afterEach.
  </action>
  <verify>
    <automated>npx vitest run src/lib/resilient-fetch.wiring.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/resilient-fetch.wiring.test.ts` green
    - Test asserts budgetKey values "validate-key" AND "process-key-enqueue" explicitly
    - Test asserts X-User-Id passthrough on the process-key init AND X-Service-Key passthrough on the validate-key init
    - Full check: `npm test` green (proves the 16 mock-based route tests survived the adaptation) and `npm run typecheck` green
  </acceptance_criteria>
  <done>ONE-core wiring spy-proven for both chokepoints; header passthrough pinned; whole suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route handler → seam clients | Request bodies carry raw exchange API secrets |
| Clients → Railway (via core) | Auth headers: X-Service-Key, Bearer INTERNAL_API_TOKEN, X-User-Access-Token, X-User-Id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-07 | Spoofing/Elevation | header forwarding through core | mitigate | headers constructed in clients, passed byte-for-byte; wiring test asserts X-User-Id AND X-Service-Key passthrough (CT-4 + service-auth regression guards) |
| T-140-08 | Information Disclosure | CIRCUIT_OPEN envelope copy | mitigate | static human_message; detail only in console.error with routeTag + correlation_id, never args.context |
| T-140-09 | Information Disclosure | client log lines | mitigate | never log request bodies (raw API secrets); process-key log lines carry correlation_id only |
| T-140-10 | Denial of Service | signature drift breaking 16 route-test mocks | mitigate | public signatures + error classes frozen; CircuitOpenError canonically imported from the never-mocked leaf (re-export from analytics-client is back-compat only, never relied on through mocks) |
| T-140-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/analytics-client.test.ts src/lib/process-key-client.test.ts src/lib/resilient-fetch.wiring.test.ts --no-file-parallelism` green
- `npm test` green (16 mock-based route tests unchanged and passing)
- `npm run typecheck` + `npm run lint` green
</verification>

<success_criteria>
- SEAM-01: grep for `AbortSignal.timeout` in both clients returns zero — the core is the only owner; wiring test proves both clients invoke it
- SEAM-04 (client layer): CIRCUIT_OPEN 503 envelope in the locked `{ok,code,human_message,correlation_id,recoverable}` shape + Retry-After, inherited by all 5 postProcessKey callers
- Zero public-signature changes (16 existing mocks untouched and green)
</success_criteria>

<output>
Create `.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-02-SUMMARY.md` when done.
</output>
