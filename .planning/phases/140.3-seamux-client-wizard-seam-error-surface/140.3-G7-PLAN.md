---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: G7
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [SEAMUX-03]
files_modified:
  - src/app/api/strategies/csv-validate/route.ts
  - src/app/api/strategies/csv-validate/route.test.ts

must_haves:
  truths:
    - "Every error arm /api/strategies/csv-validate emits carries a top-level machine `code` ON THE WIRE — proven by asserting response BODIES, not by grepping for the `code:\"` spelling."
    - "The measurement correction is on the record: the verifier's receipt grep counts 0 for this route because csvErrorBody passes `code` positionally/shorthand — the wire has been coded all along, minus any arm the audit finds otherwise."
    - "No error response body contains raw CSV cell values (QA ISSUE-005 — pandera internals / 'nan' column leak class). The route's debug_context carries only the metadata fields it carries today."
    - "The route finally has a test file — the only one of the ten SEAMUX-03 routes without one."
  artifacts:
    - path: "src/app/api/strategies/csv-validate/route.test.ts"
      provides: "NEW per-arm body.code assertions + PII guard"
      min_lines: 120
    - path: "src/app/api/strategies/csv-validate/route.ts"
      provides: "Any arm found codeless during the audit gains its code via csvErrorEnvelope (never inline)"
      contains: "csvErrorBody"
  key_links:
    - from: "src/app/api/strategies/csv-validate/route.test.ts"
      to: "src/app/api/strategies/csv-validate/route.ts"
      via: "expect(body.code) per arm on the parsed response"
      pattern: "code"
---

<objective>
Close the SEAMUX-03 gap on `/api/strategies/csv-validate` — with a
MEASUREMENT-FIRST posture, because this route is not like the other nine.

The 140.3-VERIFICATION counted it "0 code-bearing arms / 1 bare {error:}" with
`grep -cE 'code:\s*"'`. That grep is blind to this route's style: every arm
routes through `csvErrorEnvelope(code, human_message, ...)` →
`csvErrorBody(code, ...)`, which emits `{ ok: false, code, human_message,
debug_context, correlation_id }` using SHORTHAND `code,` — including the
rate-limit deny (`CSV_RATE_LIMIT` / `SEAM_MISCONFIGURED` bodies via
rateLimitDenyJson, 140.4-13). On the WIRE the route may already be fully coded.
The job is therefore: (1) an arm-by-arm wire audit that would CATCH a codeless
arm rather than assume none, (2) fix any arm the audit finds, (3) the missing
test file asserting `body.code` per arm — the falsifiable fence the other nine
routes get in G4–G6 and this one currently lacks entirely, (4) a PII guard for
QA ISSUE-005.

Purpose: SEAMUX-03 closure that survives re-verification, with the receipt
measured at the wire.
Output: audited route + NEW route.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VERIFICATION.md
@src/app/api/keys/sync/route.ts
@src/lib/wizardErrors.ts
@src/lib/envelope.ts

<interfaces>
This route's OWN envelope mechanism (do not migrate it to buildEnvelope — that
is the DESIGN.md §Error Envelope founder decision logged in VERIFICATION §5.2,
explicitly out of scope here):

  csvErrorBody(code, human_message, debug_context = {}) →
    { ok: false, code, human_message, debug_context, correlation_id: null }
  csvErrorEnvelope(code, human_message, debug_context, status = 400, init)
    → NextResponse with NO_STORE_HEADERS base (route.ts:109)

Known arms at HEAD (all already code-bearing on the wire):
  :144 CSV_FILE_TOO_LARGE (Content-Length) · :155 CSV_INVALID_FORMAT
  (multipart) · :160 CSV_INVALID_FORMAT (file field) · :164 CSV_FILE_TOO_LARGE
  (file.size) · :176 CSV_INVALID_FORMAT (fmt enum) · :191 CSV_INVALID_FORMAT
  (wizard_session_id) · :218 deny via rateLimitDenyJson with
  csvErrorBody("CSV_RATE_LIMIT",…) / csvErrorBody("SEAM_MISCONFIGURED",…) ·
  :291 CSV_UPSTREAM_FAIL 503 (INTERNAL_API_TOKEN missing) · :320 `!result.ok`
  passthrough (coded by process-key-client) · :352 CSV_UPSTREAM_FAIL terminal.

CODE VOCABULARY if an arm needs a code: reuse this route's own CSV_* tokens
where the fact matches; SEAM_MISCONFIGURED for config faults; never mint a
synonym. (CSV_INVALID_FORMAT / CSV_RATE_LIMIT are pre-existing route tokens —
they predate this plan and are NOT to be renamed to union members; renaming
breaks CsvUploadStep consumers and PostHog code stability.)

QA ISSUE-005 (TODOS): the upstream Python leak class — pandera internals and a
column literally named 'nan' reaching users. This route forwards upstream
failure bodies on `!result.ok`. The route's OWN arms must keep debug_context to
the metadata fields they carry today (content_length, size_bytes,
fmt_received); the new tests must prove no raw cell value from an uploaded
fixture appears in any route-emitted error body. Fixing the upstream
forwarded-4xx arm itself is DEF-140.4-C — tracked, NOT this plan's scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Arm-by-arm wire audit; code any arm found bare</name>
  <read_first>
    src/app/api/strategies/csv-validate/route.ts (the WHOLE file — the audit is the deliverable),
    src/app/api/keys/sync/route.ts (the template's arm-by-arm SUMMARY shape from 140.3-10),
    src/lib/ratelimit.ts:368-383 (rateLimitDenyJson override semantics),
    src/lib/envelope.ts (WizardErrorCode import chain — context only)
  </read_first>
  <files>src/app/api/strategies/csv-validate/route.ts</files>
  <action>
    ENUMERATE EVERY response-producing site in the route: each
    `csvErrorEnvelope(` call, the `rateLimitDenyJson(` deny, every raw
    `NextResponse.json(`, and the `result.response` passthrough. For EACH,
    record in the SUMMARY: line, status, the top-level `code` value the WIRE
    body carries, and HOW it carries it (csvErrorBody shorthand / explicit /
    upstream-supplied). The audit must be capable of failing: an arm whose body
    lacks a top-level `code` is a finding, and gets one via `csvErrorEnvelope`
    (NEVER an inline `{ error, code }` literal — that would give this route a
    second envelope definition, the exact drift M-14 exists to prevent).

    Expected outcome per the plan-time read: ZERO codeless arms (all 10 known
    arms already carry codes). If that holds, this task's code change is NIL
    and the SUMMARY says so plainly — do NOT invent an edit. Record the
    measurement correction explicitly: the 140.3-VERIFICATION receipt command
    (`grep -cE 'code:\s*"'`) reads 0 on this file because the code rides
    positionally through csvErrorBody; the correct receipt for THIS route is
    the test file Task 2 adds, which asserts the parsed response body.

    Do NOT restructure working arms to satisfy a grep spelling. Do NOT touch
    the forwarded `!result.ok` arm (DEF-140.4-C owns its content). Do NOT
    migrate the route onto buildEnvelope (founder-decision scope, §5.2).
  </action>
  <verify>
    <automated>npx tsc --noEmit && test $(grep -vE '^\s*(//|\*)' src/app/api/strategies/csv-validate/route.ts | grep -cE 'csvErrorEnvelope\(|csvErrorBody\(') -ge 10</automated>
  </verify>
  <acceptance_criteria>
    SUMMARY contains the complete arm inventory (line, status, code, carriage
    mechanism) with an explicit codeless-arm count (expected 0, measured); any
    codeless arm found is fixed through csvErrorEnvelope; the measurement
    correction (grep-blindness, positional code) is stated with the exact
    verifier command it corrects; `npx tsc --noEmit` clean; zero behaviour
    change if the audit finds zero codeless arms.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create route.test.ts — per-arm code assertions + ISSUE-005 PII guard</name>
  <read_first>
    src/app/api/strategies/csv-validate/route.ts (the arms under test),
    src/app/api/keys/validate-and-encrypt/route.test.ts (the sibling test shape to match — mocking, request building),
    src/app/api/keys/sync/route.ts (template context)
  </read_first>
  <files>src/app/api/strategies/csv-validate/route.test.ts</files>
  <behavior>
    - Each reachable error arm's parsed response body carries `ok: false` and the exact expected `code` (CSV_FILE_TOO_LARGE on both size arms, CSV_INVALID_FORMAT on the four format arms, CSV_RATE_LIMIT / SEAM_MISCONFIGURED on the deny bodies, CSV_UPSTREAM_FAIL on the token-missing 503 and the terminal arm)
    - A whole-body assertion: for EVERY exercised error arm, `typeof body.code === "string" && body.code.length > 0` — the arm-agnostic fence that catches a future codeless arm
    - PII guard: an uploaded CSV fixture containing a sentinel cell value (e.g. "SENTINEL_PII_9241") produces error responses whose serialized bodies do NOT contain the sentinel — on the too-large arm, the bad-fmt arm, and the terminal-throw arm (postProcessKey mocked to throw)
  </behavior>
  <action>
    Create the file matching the sibling route-test shape (mock
    `@/lib/process-key-client`'s postProcessKey, `@/lib/ratelimit`'s checkLimit
    per the repo's established route-test conventions — copy the mocking
    idiom from keys/validate-and-encrypt/route.test.ts, this route's closest
    sibling). Build multipart requests with real FormData/File so the parse
    arms are exercised as production does.

    Cover the behaviors above. For the deny arms, drive checkLimit to the
    throttled and misconfigured outcomes and assert both csvErrorBody-built
    bodies (code + human_message). For the terminal arm, make the mocked
    postProcessKey THROW and assert CSV_UPSTREAM_FAIL with a body that never
    echoes the fixture's cell values (ISSUE-005: the route must not become the
    place where raw cells reach the wire).

    Tests must encode WHY (Rule 9): the describe block names SEAMUX-03 and
    states that a dropped `code` on any arm is a client-discrimination
    regression, not a cosmetic change.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/csv-validate</automated>
  </verify>
  <acceptance_criteria>
    src/app/api/strategies/csv-validate/route.test.ts exists (was the only
    SEAMUX-03 route with no test file); every exercised error arm asserts its
    exact code; the arm-agnostic body.code fence and the three-arm PII sentinel
    guard are present and pass; neutering check performed once locally: with
    one arm's code temporarily removed the suite reddens (do not commit the
    mutation — record RED observed in the SUMMARY); `npx vitest run
    src/app/api/strategies/csv-validate` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed client → /api/strategies/csv-validate | untrusted multipart upload, up to 10 MB of user financial data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.3-G7-01 | Information disclosure | error bodies vs raw CSV cells (ISSUE-005 class) | mitigate | sentinel-cell PII guard across three arms; debug_context stays metadata-only |
| T-140.3-G7-02 | Tampering | second envelope definition drift | mitigate | any fix goes through csvErrorEnvelope/csvErrorBody only; no inline envelope literals |
| T-140.3-G7-03 | Repudiation | audit that cannot fail | mitigate | the arm inventory records the wire code per arm with an explicit codeless count; test fence is arm-agnostic |
| T-140.3-G7-SC | Tampering | npm/pip installs | mitigate | ZERO packages installed by this plan set |
</threat_model>

<verification>
- `npx vitest run src/app/api/strategies/csv-validate` exits 0
- `npx tsc --noEmit` exits 0; `npm run lint` — 0 new errors
- SUMMARY carries the wire-level receipt superseding the verifier's grep for this route
</verification>

<success_criteria>
Every csv-validate error arm is proven coded at the wire by tests that redden
on a dropped code; the ISSUE-005 sentinel guard passes; the measurement
correction is recorded so a re-verification does not re-open this route off
the old grep.
</success_criteria>

<output>
Create `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-G7-SUMMARY.md`
with the full arm inventory and the measurement-correction note.
</output>
