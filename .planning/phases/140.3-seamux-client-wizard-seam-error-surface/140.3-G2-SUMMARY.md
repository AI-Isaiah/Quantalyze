---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: G2
subsystem: wizard-error-surface / documentation-oracles
type: execute
gap_closure: true
requirements: [GC-3, GC-4]
tags: [sc2-residual, trap-4, negative-pin, stale-prose-counts, frozen-island, falsifiability-ledger]
status: complete
base_commit: e6f0379435ca6a60b6ff92969385adc63c3c2078
head_commit: a77d607e
branch: feat/v1.16-production-resilience

requires:
  - "src/app/api/strategies/finalize-wizard/route.ts:1551 (the 409 arm that emits COMPOSITE_UNSUPPORTED_UNIFIED)"
  - "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx:513 (the draft_state_invalid sibling)"
  - "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx:1341 (DESTRUCTIVE_CONTROL_IS_WRONG_FOR — the TRAP-4 prerequisite)"
  - "src/lib/wizardErrors.ts:1543 (production source already naming both residual codes)"
  - ".planning/phases/140.3-.../140.3-G1-SUMMARY.md (wave 1; the Gap-closure addendum section this plan appends to)"
provides:
  - "A falsifiable negative pin for the SC2 residual whose FAILURE MESSAGE names the same-commit TRAP-4 prerequisite"
  - "Falsifiability Ledger rows M89a (could not run — type error, and that is the finding) and M89 (RED)"
  - "DEF-G2-1, DEF-G2-2, DEF-G2-3 — three deferred rows, each with Phase 140.4 named DIRECTLY as owner"
  - "A dated CORRECTIONS block on 140.3-16-SUMMARY.md, body byte-unchanged (sha-proven)"
affects:
  - "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx (28 -> 29 cases)"
  - "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx (comment only)"
  - "tests/visual/recharts-touchtooltip-usage.test.ts (docblock only; assertions byte-unchanged)"

tech_stack:
  added: []
  patterns:
    - "A negative pin whose deliverable is its failure message, not its assertion"
    - "Adjudicate a residual with a guard rather than with prose, so admitting the code cannot land silently"
    - "Delete an unowned count from prose; name the guard by symbol instead — a pointer cannot drift"
    - "Annotate delivered history with a dated CORRECTIONS append; never edit the body"
    - "Prove body byte-identity with a truncated-file sha, not with a word-count grep the correction itself contaminates"

key_files:
  created:
    - ".planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-G2-SUMMARY.md (this file, gitignored)"
  modified:
    - "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx"
    - "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
    - "tests/visual/recharts-touchtooltip-usage.test.ts"
    - ".planning/phases/140.3-.../deferred-items.md (122 -> 242 lines, appended, gitignored)"
    - ".planning/phases/140.3-.../140.3-VALIDATION.md (1821 -> 2048 lines, appended, gitignored)"
    - ".planning/phases/140.3-.../140.3-16-SUMMARY.md (493 -> 594 lines, appended, gitignored)"

decisions:
  - "GC-3 closed by ADJUDICATION (route b), not by admitting the code (route a) — TRAP-4 coupling"
  - "M89's prescribed mutation is a TYPE ERROR and was replaced with the type-clean realisation of route (a)"
  - "TouchTooltip.tsx reverted rather than unfrozen: never delete a working fence to land a comment (TRAP-9)"
  - "No count was transcribed anywhere as a replacement — the guard is named by symbol instead"

metrics:
  duration_minutes: 38
  completed: 2026-07-28
  tasks: 3
  commits: 3
  suite_before: "9791 passed | 287 skipped"
  suite_after: "9792 passed | 287 skipped (734 files)"
---

# Phase 140.3 Plan G2: SC2 Residual Adjudication + Stale-Count Class Summary

**One-liner:** Converted SC2's unrecorded residual into a falsifiable negative pin whose failure
message names the TRAP-4 prerequisite a future fixer must ship in the same commit, and removed the
hand-maintained counts that no oracle owned — except three that a frozen-island gate correctly
refused, which are deferred rather than force-landed.

⚠️ **Branch protection on `main` is OFF** by a settled founder decision. Every gate below is
**advisory at merge**: each one **would have caught** the regression it describes. None **did stop**
one, and none can. (Not re-raised.)

---

## 1. Pre-values, re-measured on the UNTOUCHED tree before any edit

`BASE = e6f0379435ca6a60b6ff92969385adc63c3c2078` (G1's head). Nothing inherited from the plan.

| # | Measurement | Plan's pre-value | **Re-measured** | ✓ |
|---|---|---|---|---|
| Q1 | `COMPOSITE_UNSUPPORTED_UNIFIED` in `SubmitStep.test.tsx` | 0 | **0** | ✓ |
| Q2 | The emitting arm | `route.ts:1551`, 409 | **`:1551`**, body read at `:1545-1553` and copied verbatim into the mock | ✓ |
| Q3 | `KNOWN_FINALIZE_CODES` members | 9 | **9** (enumerated at source) | ✓ |
| Q4 | The sibling that already pins the other residual | `:513` | **`:513`** | ✓ |
| Q5 | `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` | `SyncPreviewStep.tsx:1341`, 2 members | **`:1341`**, `GATE_DRAFT_GONE` + `VALIDATION_FAILED` | ✓ |
| Q6 | `SubmitStep.test.tsx` | 28 passed | **28 passed** | ✓ |
| Q7 | `Object.keys(WIZARD_ERROR_COPY).length` | 55 | **55** | ✓ |
| Q8 | `EXPECTED_TABLE_SIZE` literals | `:1348` = 55, `:1491` = 55 | **both 55** — untouched, and `wizardErrors.test.ts` is absent from the diff | ✓ |
| Q9 | `size guard stays at 53` | 1 | **1** | ✓ |
| Q10 | `across 18 files` | 1 | **1** | ✓ |
| Q10b | `18× inline` | 1 | **1** | ✓ |
| Q11 | `18 charts` | 1 | **1** | ✓ |
| Q11-trap | `all 18 charts` (the WRONG anchor) | 0 | **0** — the wrap trap reproduces exactly | ✓ |
| Q12 | `all 18 tooltip-bearing` | 1 | **1** | ✓ |
| Q13 | Actual `TouchTooltip` importers | 20 | **20**, confirmed **three independent ways** | ✓ |
| Q14 | `recharts-touchtooltip-usage.test.ts` | 2 passed | **2 passed** | ✓ |
| Q16 | The false zero-fetch claim | `:90` | **`:90`** | ✓ |
| Q17 | The stale 54 | `:283` and `:325` | **`:283` and `:325`** | ✓ |
| Q18 | `deferred-items.md` | 122 lines; DEF-15-1 owner = closed plan | **122**; `:43` reads *"SEAMUX-01 / `140.3-16` to route"* | ✓ |
| Q19 | Ledger row | M89 | **M85c + G1's M86/M86b/M87/M88 → M89** | ✓ |
| Q20 | Sign-off inconsistency | `:962-963` unticked, "Approval: pending" | **exactly as described**; frontmatter `:5` = `nyquist_compliant: true`; Oracle Independence `:883-889` fully `[x]` | ✓ |
| — | Full suite | 9791 \| 287 | **9791 \| 287** | ✓ |

⚠️ **One plan pre-value did NOT reproduce as a *usable* mutation — Q13's method warning bit me too.**
My first importer measurement returned **10**, not 20, because I grepped only the absolute specifier
`@/components/charts/TouchTooltip`. Ten files under `src/components/charts/` import it **relatively**
(`./TouchTooltip`) and were invisible. Corrected and re-derived three ways (absolute edge + relative
edge = 20; `grep -rl '<TouchTooltip'` = 20). This is the same class as the plan's named `grep -h`
prohibition: **a filter that silently halves a population reads exactly like a correct answer.**

### Acceptance-criterion hygiene — "could a broken or untouched tree return this value?"

- **The five phrase anchors** — **NO.** Each has a re-measured pre-value of **1** on the untouched
  tree, so a no-op cannot produce the pass value of 0.
- **`grep -c 'all 18 charts'`** — **YES, it could**, pre-value **0**. Not used. Confirmed the wrap at
  lines 37/38 first-hand rather than trusting the plan's warning.
- **`grep -c '18' TouchTooltip.tsx == 0`** — **unsatisfiable by correct code.** Line 29 legitimately
  carries `useBreakpoint.ts:18-30`. Verified post-edit that this grep still returns 1, correctly.
- **`grep -c 'zero' 140.3-16-SUMMARY.md` as the body-unchanged check** — ⚠️ **self-defeating and
  rejected.** My corrections block *quotes* the false sentence, so the count moved 6 → 7 by design.
  This is the plan's own warning ("you are about to write corrections *containing* the stale phrases
  you are grepping for") landing on the criterion the plan itself suggested. Replaced with a
  cryptographic proof (§4).
- **`SubmitStep.test.tsx` = 29 passed** — **NO.** Pre-value 28.
- **The M89 receipt** — **NO**, and this is the row that matters: an inert pin and a codebase where
  the code is merely unrecognised both produce "resolves to UNKNOWN". Only making the code recognised
  separates them.

---

## 2. Task 1 — GC-3, the SC2 residual pinned as a deliberate, guarded decision

**Route (b), adjudication, as the plan decided.** `KNOWN_FINALIZE_CODES` is **unchanged** — still 9
members — and `SubmitStep.tsx` is absent from the diff.

One case added to `SubmitStep.test.tsx`, placed immediately after its `:513` sibling, copying that
case's shape exactly (`vi.spyOn(globalThis, "fetch")` + `jsonResponse(…, 409)` + `renderStep()` +
`vi.waitFor`). The mocked body is the route's real one, read at `finalize-wizard/route.ts:1545-1553`,
not invented. **`vi.stubGlobal` appears nowhere** (DEF-16-1).

**The failure message is the deliverable.** It states, in order: (1) `finalize-wizard:1551` emits the
code on the LIVE unified-backbone path so a real user reaches it; (2) non-membership of
`KNOWN_FINALIZE_CODES` is deliberate, collapses the `wizard_error` funnel to `UNKNOWN`, and touches
SC2's recognition and SC6's specificity clauses — recorded, not endorsed; (3) *"IF YOU ARE READING
THIS BECAUSE YOU JUST ADMITTED THE CODE: that is the right fix and this test is doing its job"* —
then widen `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` **in the same commit**, with the data-loss mechanism
spelled out (no Retry → "Try another key" → `handleDeleteDraft()` as the sole affordance), TRAP-4
named as a locked CONTEXT decision and DEF-15-1 named as the sibling precedent; (4) update the case,
do not delete it. **Verified to survive the round trip — the whole message printed on the M89 red run
(§4).**

A cross-reference comment was added above the `:513` sibling **and** above the new case, so the pair
is discoverable from either end.

**Deviation from the plan's literal wording, deliberate:** the plan asked the message to say
`DESTRUCTIVE_CONTROL_IS_WRONG_FOR` has *"2 members"*. **No count was written.** The plan's own
must-have truth #3 forbids a touched file from reciting a count it does not own — and this plan
exists to delete exactly that. The symbol and file are named; the size is not. Writing "2" here
while deleting "18" three files away would have been incoherent.

**Verify:** `29 passed` (pre 28). `grep -c 'COMPOSITE_UNSUPPORTED_UNIFIED' SubmitStep.test.tsx` →
**3** (pre **0**, criterion ≥ 2). `tsc --noEmit` exit 0.
**Commit:** `39ea3b20`

### Part B — deferred rows

`deferred-items.md` **122 → 242 lines** (appended, never rewritten). Three rows, not two:

- **DEF-G2-1** — the SC2 residual. Records both codes, the measured 9-member set,
  `wizardErrors.ts:1543`'s existing honesty, the TRAP-4 coupling, and the two pins. **Owner: `Phase
  140.4`, named directly** — because DEF-15-1's own owner line resolves to the already-closed
  `140.3-16` (Q18), which is precisely the failure naming a phase avoids. States that DEF-G2-1 and
  DEF-15-1 are the same coupling on sibling code sets and that doing them separately trips TRAP-4
  twice. **Carries the new M89a finding** (below): route (a) is *not* one line.
- **DEF-G2-2** — the two partial source-scan guards (`seam-log-coverage.test.ts:329`,
  `seam-budgets.invariant.test.ts:755`), both verified at source, both with **no live violation at
  HEAD** stated explicitly, wrapper-aware idiom cited, owner Phase 140.4. Recorded so *"unmentioned"*
  cannot read as *"unfound"*.
- **DEF-G2-3** — ⚠️ **new, not in the plan**: the three `TouchTooltip.tsx` counts blocked by the
  frozen-island gate (§3).

⚠️ **One citation in my own append was wrong and was corrected before commit:** I wrote
`src/lib/audit-coverage.test.ts:309`, following `GAP-PATTERNS` §7.2's prefix-less form. The real path
is **`src/__tests__/audit-coverage.test.ts:309`**. Verified at source (`HELPER_MUTATORS` + the
per-name `\b<name>\s*\(` matcher at `:431-441`) and fixed in place. A deferred row whose "copy this
idiom" pointer does not resolve is worth very little.

---

## 3. Task 2 — GC-4, and the gate that refused three of it

### ✅ Landed (2 of 5 source sites)

| Site | Was | Now |
|---|---|---|
| `SyncPreviewStep.tsx:165` | *"the copy table's hand-typed size guard stays at 53"* — false at HEAD | A pointer: `EXPECTED_TABLE_SIZE` in `src/lib/wizardErrors.test.ts`, **plus** why that literal is hand-typed on purpose (deriving it re-creates the self-referential shape both seam pins forbid). **55 is not written.** |
| `recharts-touchtooltip-usage.test.ts:11` | *"across all 18 tooltip-bearing charts"* | Number dropped, **and** a note recording what the guard beside it actually asserts: `toEqual([ALLOWED])`, a single-entry allow-list that never read the population size — so the count was never enforced by anything and must not be reintroduced. |

**Docblock only.** `git diff` on the recharts file shows **zero non-comment lines**; still **2
passed**. Both `EXPECTED_TABLE_SIZE = 55` literals untouched; `wizardErrors.test.ts` absent from the
diff.

### ⛔ Blocked (3 of 5) — `TouchTooltip.tsx` is a Phase 52 FROZEN client island

The three `TouchTooltip.tsx` edits landed in `2d58fd45` and the **full suite caught them**:

```
FAIL src/__tests__/phase-52-frozen-spine-guards.test.ts >
     exit gate (frozen client island): src/components/charts/TouchTooltip.tsx
     is zero-diff vs the phase baseline
Tests  1 failed | 9791 passed | 287 skipped (10079)
```

The gate asserts `CHANGED.not.toContain(island)` over
`git diff --name-only $(git merge-base origin/main HEAD) HEAD`. It is **content-blind**: a
comment-only edit reddens it exactly as an RSC-ification would.

**Decision: revert, do not unfreeze.** The gate *has* a carve-out mechanism — Phase 103 removed
`HistogramChart.tsx` and `MasterBrush.tsx` from `FROZEN_ISLANDS` — but that was a reviewed unfreeze
with a substantive cause (both read `usePayload()` directly and rendered cash under an MTM label) and
a replacement gate. Unfreezing an island to land a **comment** meets none of that bar, and the
guard's own inline note reads *"TouchTooltip.tsx + useTapPin.ts STAY FROZEN below."* **Deleting a
working fence to fix documentation is TRAP-9, which this plan is explicitly bound by.** Reverted in
`a77d607e` (`git diff` vs the phase baseline: empty); routed to **DEF-G2-3, owner Phase 140.4**, to
ride the next reviewed unfreeze or a phase-boundary re-baseline.

⚠️ **This is an unplanned falsification of a guard nobody mutated.** The frozen-spine gate **would
have caught** an unreviewed reshape of the chart-interactivity spine — it was proven live by an edit
that had every appearance of being harmless. A zero-diff freeze that reddens on a comment is working
as designed; the content-sensitive alternative is the version that lets the "harmless" edit through.

**Commits:** `2d58fd45` (the five prose deletions), `a77d607e` (the frozen-island revert).

### The five anchors, post

| Anchor | File | Pre | Post | Landed |
|---|---|---|---|---|
| `size guard stays at 53` | `SyncPreviewStep.tsx` | 1 | **0** | ✅ |
| `across 18 files` | `TouchTooltip.tsx` | 1 | **1** | ⛔ DEF-G2-3 |
| `18× inline` | `TouchTooltip.tsx` | 1 | **1** | ⛔ DEF-G2-3 |
| `18 charts` | `TouchTooltip.tsx` | 1 | **1** | ⛔ DEF-G2-3 |
| `all 18 tooltip-bearing` | `recharts-touchtooltip-usage.test.ts` | 1 | **0** | ✅ |

No-replacement control `grep -cE '\b(55|20) (files|charts|entries|importers)\b'` → **0** on all three
source files. **No number was transcribed anywhere.**

---

## 4. Task 2 Part B + Task 3 — the phase-record sites and the ledger

### The dated CORRECTIONS block on `140.3-16-SUMMARY.md`

Appended, **body never touched**: `493 → 594` lines, with entries `C-G2-1` (the `:90` false
zero-fetch property claim — quoted, measured, and named as the sentence `VERIFICATION.md` gaps[1]
`artifacts[1].issue` cites verbatim, with the two G1 files where the property is now properly proven
and the re-runnable command) and `C-G2-2` (`:283`/`:325`, 54 → stale, **corrected through
`EXPECTED_TABLE_SIZE`** with the guard cited by symbol; 55 stated once as a dated measurement with its
command, never as a recited count in normative prose). Header states that where the body conflicts,
**the correction governs** — the `140.3-CONTEXT.md` C-1..C-9 precedent.

**Body byte-identity is sha-proven, not sampled:**

```
pre-append  shasum -a 256 140.3-16-SUMMARY.md          → ab1474119ea6c9bc…
post-append head -493 … | shasum -a 256                → ab1474119ea6c9bc…   ✅ identical
```

### `140.3-VALIDATION.md` — appended, 1821 → 2048 lines

Prior content byte-identical (`head -1821 | shasum` → `69d2e1f422300a0b`, matching the pre-append
whole-file sha); frontmatter and `closed_by: 140.3-16` intact. Appended under G1's
`## Gap-closure addendum` section — which was verified present at `:1603` before writing, as the plan
required.

#### ⚠️ M89a — the prescribed mutation could NOT be run. It is a TYPE ERROR. That is the finding.

The plan specified: add `"COMPOSITE_UNSUPPORTED_UNIFIED"` to `KNOWN_FINALIZE_CODES`. Attempted
verbatim; `tsc` rejects it:

```
SubmitStep.tsx:148:72 - error TS2769: No overload matches this call.
  Type '"COMPOSITE_UNSUPPORTED_UNIFIED"' is not assignable to type 'WizardErrorCode'.
```

The set is `new Set<WizardErrorCode>([…])` and the code is **not a `WizardErrorCode`**
(`grep -c '"COMPOSITE_UNSUPPORTED_UNIFIED"' src/lib/wizardErrors.ts` → **0**). **The ledger forbids
type errors as mutations**, so M89a was reverted un-run and recorded as the reason rather than
quietly swapped out.

**It materially strengthens DEF-G2-1:** route (a) is **not** a one-line change. It is either a new
`WIZARD_ERROR_COPY` member with authored copy — which moves the table size and so must bump **both**
`EXPECTED_TABLE_SIZE` literals in the same commit — or an alias row in the ONE wire→wizard table.
Both costs are now written down for Phase 140.4.

#### 🔴 M89 — the type-clean realisation of route (a)

| Field | Value |
|---|---|
| Target | `src/lib/wizardErrors.ts:1567` — **production source** |
| Mutation | one row in `SEAM_CODE_TO_WIZARD_CODE`: `["COMPOSITE_UNSUPPORTED_UNIFIED", "COMPOSITE_TOO_MANY_MEMBERS"]` |
| Why faithful | This IS route (a) performed as production source prescribes — `SubmitStep.tsx`'s own comment says the mapping *"lives in `wizardErrors.ts` … the ONE wire→wizard table — never in a second copy here."* `recogniseSeamErrorCode` then translates onto a member that IS in `KNOWN_FINALIZE_CODES`, so `surfaced` is no longer `UNKNOWN`. Semantic, one token from the subject's own vocabulary, `tsc` exit **0** |
| Result | 🔴 **`1 failed | 28 passed (29)`** — the new pin, failing on its own assertion with the full prerequisite-naming message rendered, not a crash and not a type error |

**Green-side observations, both confirmed BY NAME under `--reporter=verbose`:**

| Must stay GREEN | Result | Proves |
|---|---|---|
| `maps a 409 unknown code (draft_state_invalid) to UNKNOWN (recoverable)` (`:513`) | ✅ passed | The two residuals are **independently** covered — one assertion is not doing double duty |
| `[140.3-05] an unrecognised code still resolves to UNKNOWN — the membership check survives` (`:214`) | ✅ passed | The membership check is untouched; the mutation admitted one code by translation, it did not widen the check into a bare cast |

**Blast radius — and the finding is that it is NARROW.** Under the mutant,
`vitest run 'src/app/(dashboard)/strategies/new/wizard/' src/lib/wizardErrors.test.ts` →
**`1 failed | 416 passed (417)`**. Exactly one test in 417 noticed. The plan warned that an
*over-broad* radius is a coupling finding; the opposite occurred and is also one: **`SEAM_CODE_TO_WIZARD_CODE`
has no size or membership guard of its own.** A row can be added to the ONE wire→wizard table — the
table whose docblock argues at length that aliasing a second member with the same meaning is *"how a
vocabulary starts lying"* — and nothing reddens except a pin that happens to exercise the affected
code end-to-end. **Logged, not fixed** (outside the fence, and adding a guard mid-mutation would have
invalidated the receipt); routed with DEF-G2-2's guard-decay class.

**Restoration:** `git checkout -- src/lib/wizardErrors.ts`; `git diff --stat` on both
`wizardErrors.ts` and `SubmitStep.tsx` → **empty**; `grep -rn MUTANT src/` → **0**; `29 passed`
restored. **No row returned GREEN.**

#### GC-4's no-M-row, explained (as the plan required)

Recorded explicitly in the addendum, because an unexplained absence in a ledger reads as a skipped
row: prose deletion has **no runtime behaviour to mutate**, a comment edit is not a semantic
production change, and per `GAP-RESEARCH` Q4 the deliberate outcome is a **pointer, not a new guard**
(a drift-detector for a comment was considered and rejected there by name). Its receipts are the five
anchored phrase greps with their pre-values — now recorded with **three of five marked blocked**,
because a criterion whose result is "refused by a gate" is a result, and silently dropping the three
would leave the class looking closed.

**The DEF-16-2 recurrence is recorded**, as required: `'all 18 charts'` had a pre-value of **0**
(the sentence wraps 37/38, grep is line-based), was that criterion's entire receipt set, and was
caught at plan review and corrected to `'18 charts'` (pre-value 1). Two further rejected criteria are
recorded alongside it — the unsatisfiable bare-`18` anchor, and the self-defeating `grep -c 'zero'`
body check that my own correction contaminated.

#### Sign-off reconciliation (Q20) — stated, NOT resolved

The addendum records that `:962-963` leaves *"Oracle Independence checklist complete"* and
*"`nyquist_compliant: true` set in frontmatter"* **unticked** with **"Approval: pending"**, while
frontmatter `:5` reads `nyquist_compliant: true` and the Oracle Independence section at `:883-889` is
fully `[x]` under an explicit *"COMPLETED BY INSPECTION at the final tree by `140.3-16`"*. **The
evidence supports the "complete" side; only the summary checklist lags.** The boxes were **not**
ticked, the frontmatter was **not** edited, and "Approval: pending" stands — a gap-closure executor
tidying another agent's sign-off is how an approval record stops meaning anything. Resolution belongs
to the phase owner. Recorded because it is the GC-4 defect class one level up: a checklist that
contradicts its own document is a hand-maintained claim with no oracle.

---

## 5. Verification (run in the plan's order)

| # | Check | Result |
|---|---|---|
| 1 | `BASE` recorded before any edit | `e6f0379435ca6a60b6ff92969385adc63c3c2078` |
| 2 | Scoped vitest (wizard steps, tests/visual, wizardErrors) | ✅ green |
| 3 | `npx tsc --noEmit` | ✅ **exit 0** |
| 4 | `npm run lint` (full script) | ✅ **0 errors, 1 pre-existing warning** (`EquityChart.tsx:1119`); `check-admin-route-manifest` OK (20 routes); `check-route-contract` OK (56 routes) |
| 5 | `npm test` — **FULL suite** | ✅ **9792 passed \| 287 skipped** (734 files) — baseline **9791**, **+1**, no decrease |
| 6 | `git diff --stat $BASE..HEAD -- package.json package-lock.json` | ✅ **empty** — zero packages |
| 7 | `.py` in the diff | ✅ **0** |
| 8 | `.planning/` in the diff | ✅ **0** — all four ledger files are real edits, gitignored, never `git add`ed, never force-added |
| 8b | `e2e/` in the diff | ✅ **0** |
| 9 | `SubmitStep.tsx` in the diff | ✅ **0** — no production behaviour changed; M89 fully reverted |
| 9b | `wizardErrors.ts` / `wizardErrors.test.ts` / `TouchTooltip.tsx` in the diff | ✅ **0 each** |
| 10 | `grep -rn MUTANT src/` | ✅ **0**. `git status --short` → **exactly 2**, and they are the two documented pre-existing entries (` M TODOS.md`, `?? analytics-service/scripts/nautilus_factsheet.py`) |
| 11 | `mypy` / `pytest` | ✅ **not run** — no Python touched |

**Net changed files (3, all source):**

```
src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx
src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
tests/visual/recharts-touchtooltip-usage.test.ts
```

**Scope discipline confirmed:** SEAMUX-03 untouched; `complete-status-scan.test.ts` untouched;
`analytics-service/` untouched; `REQUIREMENTS.md` untouched; **`STATE.md`, `ROADMAP.md` and
`DESIGN.md` untouched**; `140.3-G3` **not executed** (it is a decision brief, `executable: false`);
no `gsd-sdk query state.*` was ever run.

---

## 6. Deviations from Plan

### 1. [Rule 1 — the prescribed mutation does not compile] M89's target moved, and the reason is recorded as M89a

- **Found during:** Task 3.
- **Issue:** `KNOWN_FINALIZE_CODES` is `Set<WizardErrorCode>`; `COMPOSITE_UNSUPPORTED_UNIFIED` is not
  a `WizardErrorCode`. The plan's literal mutation is a **TS2769 type error**, which this ledger
  forbids as a mutation shape.
- **Fix:** Ran the type-clean realisation of the same edit — one alias row in
  `SEAM_CODE_TO_WIZARD_CODE`, the mechanism `SubmitStep.tsx`'s own comment prescribes. Recorded the
  attempt as **M89a** rather than deleting it: *why* route (a) is not a one-liner is a finding that
  belongs to Phase 140.4.
- **Commit:** none (both mutations reverted).

### 2. [Rule 3 + TRAP-9 — a gate refused the edit and the gate won] `TouchTooltip.tsx` reverted, DEF-G2-3 opened

- **Found during:** Task 2, by the **full suite** — not by inspection, which is the argument for
  running it rather than a scoped path.
- **Issue:** the file is a Phase 52 frozen client island under a content-blind zero-diff gate.
- **Fix:** reverted to the phase baseline (`a77d607e`); did **not** remove it from `FROZEN_ISLANDS`.
  Three GC-4 members routed to DEF-G2-3, owner Phase 140.4, with an explicit *"do not fix this by
  unfreezing the island"* warning.
- **Consequence, stated plainly:** GC-4 is **partially** closed — 3 of 6 members removed, 2 corrected
  by dated append, **3 deferred**. The plan's success criterion *"all SIX GC-4 members handled"* is
  met in the sense that every member is now either fixed or carries a named owner and a measured
  reason, but **three stale counts remain in the tree today.**

### 3. [Rule 3 — consistency with the plan's own must-have] no count in the new failure message

- The plan asked the message to cite *"2 members"* of `DESTRUCTIVE_CONTROL_IS_WRONG_FOR`. Not written.
  Must-have truth #3 forbids a touched file reciting a count it does not own, and reciting one while
  deleting five elsewhere would be incoherent. Symbol and file named; size not.

### 4. [Rule 1 — my own citation was wrong] `audit-coverage.test.ts` path corrected before commit

- Wrote `src/lib/audit-coverage.test.ts:309` (following `GAP-PATTERNS`' prefix-less form); the real
  path is `src/__tests__/audit-coverage.test.ts:309`. Verified at source and corrected in place.

**No architectural changes. No Rule 4 escalations. No packages. No auth gates. No checkpoints hit.**

---

## 7. Known Stubs

None. No hardcoded empty values, placeholder text or unwired data sources were introduced. The three
`TouchTooltip.tsx` counts that remain stale are **pre-existing**, are not stubs, and carry a named
owner (DEF-G2-3).

## 8. Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema change at a trust boundary.

| Threat | Disposition | Satisfied by |
|---|---|---|
| T-140.3G2-01 DoS (user data) via a destructive sole affordance | **mitigate by refusal** | `KNOWN_FINALIZE_CODES` unchanged (9 members); the pin reddens on admission and names the same-commit prerequisite. Falsified by **M89** |
| T-140.3G2-02 Repudiation (funnel reports `UNKNOWN`) | accept, recorded | **DEF-G2-1**, owner **Phase 140.4** named directly — not an indirection to a closed plan |
| T-140.3G2-03 Tampering (documentation decay) | mitigate, **partial** | 2 source sites de-numbered + guard named; SUMMARY annotated by dated append, body sha-identical. **3 sites blocked → DEF-G2-3** |
| T-140.3G2-04 Tampering (guard decay, deferred) | transfer → Phase 140.4 | **DEF-G2-2**, both with "no live violation at HEAD" stated |
| T-140.3G2-05 Supply chain | mitigate | **Zero packages proposed or installed.** `package.json` / `package-lock.json` diff empty |

---

## 9. Commits

| Hash | Message |
|---|---|
| `39ea3b20` | `test(140.3-G2): pin the SC2 residual — COMPOSITE_UNSUPPORTED_UNIFIED resolves to UNKNOWN by decision` |
| `2d58fd45` | `docs(140.3-G2): drop five unowned prose counts, name the guard instead` |
| `a77d607e` | `revert(140.3-G2): restore TouchTooltip.tsx — it is a Phase 52 FROZEN client island` |

`commit_docs: false` — this SUMMARY and all three `.planning/` ledger edits are gitignored and
**not** committed.

---

## Self-Check: PASSED

**Files claimed created/modified — all present on disk:**

```
FOUND: src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx
FOUND: src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
FOUND: tests/visual/recharts-touchtooltip-usage.test.ts
FOUND: .planning/phases/140.3-.../deferred-items.md         (242 lines, was 122)
FOUND: .planning/phases/140.3-.../140.3-VALIDATION.md       (2048 lines, was 1821)
FOUND: .planning/phases/140.3-.../140.3-16-SUMMARY.md       (594 lines, was 493)
FOUND: .planning/phases/140.3-.../140.3-G2-SUMMARY.md       (this file)
```

**Commits claimed — all present in `git log`:**

```
FOUND: 39ea3b20
FOUND: 2d58fd45
FOUND: a77d607e
```

**Counts re-verified after the final commit:**
`SubmitStep.test.tsx` = 29 tests · `COMPOSITE_UNSUPPORTED_UNIFIED` mentions = 3 ·
`recharts-touchtooltip-usage.test.ts` = 2 tests, assertions byte-unchanged ·
`EXPECTED_TABLE_SIZE` = 55 at `:1348` and `:1491`, untouched · five phrase anchors = 0/1/1/1/0 ·
no-replacement control = 0 on all three files · `140.3-16-SUMMARY.md` body sha `ab1474119ea6c9bc`
unchanged · `140.3-VALIDATION.md` prior-content sha `69d2e1f422300a0b` unchanged ·
`grep -rn MUTANT src/` = 0 · `git status --short` = 2 (both pre-existing) ·
full suite = **9792 passed | 287 skipped**.
