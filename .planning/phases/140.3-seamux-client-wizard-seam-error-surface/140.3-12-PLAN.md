---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: 12
type: execute
wave: 12
depends_on: ["140.3-10", "140.3-11"]
files_modified:
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.test.ts
  - src/components/landing/VerificationForm.tsx
  - src/components/landing/VerificationForm.test.tsx
  - .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
autonomous: true
requirements: [SEAMUX-04]

must_haves:
  truths:
    - "No copy claims a team was notified from a path where nothing is notified"
    - "No copy asserts work happened, or didn't, that the client cannot know"
    - "The anonymous teaser renders the rejection's own human_message instead of a generic 'Verification failed'"
    - "An idempotency promise is made only on the flow that actually sends the id"
  artifacts:
    - path: "src/lib/wizardErrors.ts"
      provides: "The copy table with every false claim removed and TS-09's real strings in place of 140.3-01's placeholders"
      contains: "WIZARD_ERROR_COPY"
  key_links:
    - from: "src/components/landing/VerificationForm.tsx"
      to: "src/lib/process-key-client.ts"
      via: "the 403 envelope carries human_message, never error — the teaser must read the field that exists"
      pattern: "human_message"
---

<objective>
Delete every copy string that makes a **false claim about the user's data, their key, or what we did
about it**, and write the real copy `140.3-01` deliberately left as placeholders.

Purpose: requirement `SEAMUX-04`; obligations `TS-17` and the copy half of `TS-09`.

⚠️ **This plan is the SOLE owner of every final user-facing sentence in `src/lib/wizardErrors.ts`.**
`140.3-01` and `140.3-05` both added union members with **non-final** entries, each tagged with the
marker token `TODO-COPY-140.3-12`, and were forbidden from authoring copy because `RATE_LIMITED`'s copy
must name the real wait and the wait field does not exist until `140.3-09`. Consequently this plan
writes copy for **more codes than the lie inventory below** — run
`grep -n 'TODO-COPY-140.3-12' src/lib/wizardErrors.ts` and close all of them.

This is the phase's copy pass and it is **almost entirely one file**. `src/lib/wizardErrors.ts` owns
every lie the review programme found. That is a single-file edit, not a fan-out — but the file is also
touched by five earlier plans, which is why it is scheduled here rather than early.

## ⚠️ THE LIE INVENTORY IS SEVEN STRINGS ACROSS FIVE CODES — two more than any source document lists

CONTEXT names two lies. RESEARCH found two more. **A first-hand grep at this planning gate found a
fifth code and a seventh string.** Every one verified at source:

| # | Code | String (grep anchor) | Why it is false |
|---|---|---|---|
| L1 | `UNKNOWN` | `Our team has been notified and is looking into it.` | **9 of 15 seam routes have ZERO Sentry captures.** Nothing is notified. |
| L1b | `GATE_ANALYTICS_FAILED` | `we have been notified.` | Same. **Not in CONTEXT.** |
| L2a | `SYNC_FAILED` | `We fetched your trades but the analytics computation did not complete.` | Asserted on **every** non-2xx from `keys/sync`, including a breaker 503 where nothing was fetched. |
| **L2b** | **`GATE_ANALYTICS_FAILED`** | `We fetched your trades successfully, but the risk metrics pipeline errored out.` | **Same lie, second code — found first-hand at this gate. In NO source document.** "Successfully" makes it stronger, not weaker. |
| L3a | `CSV_SUBMIT_FAILED` | `Your file validated cleanly, but saving the strategy hit an error.` … `your data is unchanged.` | Asserted after a 500 from a handler that runs `finalize_csv_strategy`; C-02 proves uvicorn does not cancel on disconnect, so "unchanged" is **unknowable to the client**. |
| L3b | `CSV_SUBMIT_FAILED` | `Click Submit strategy again — your data is unchanged.` | Same claim, and it **steers the retry that dead-ends** (C-01). |
| L4 | `CSV_SUBMIT_NO_STRATEGY_ID` | `duplicates are prevented by wizard_session_id idempotency.` | **TRUE for CSV, FALSE for every other flow** — `csv-validate` and `csv-finalize` do send the id; `finalize-wizard` does not (that is TS-33, plan `140.3-14`). A promise scoped wider than the mechanism. |

**Enumerate by the STRING, not by the code.** Two of the seven live on a code no source document
mentions, and they were found only because the grep was run against the sentence.

## The Sentry class is 9 of 15, not the 2 CONTEXT names

`grep -c "captureToSentry"` across the fifteen seam routes:

**ZERO captures (9):** `admin/match/eval` · `admin/match/recompute` · `keys/[id]/permissions` ·
`portfolio-optimizer` · `scenario/optimize` · `strategies/composite/add-key` ·
`strategies/create-with-key` · `strategies/csv-validate` · `verify-strategy`
**≥1 capture (6):** `bridge` · `keys/sync` · `keys/validate-and-encrypt` · `simulator` ·
`strategies/csv-finalize` · `strategies/finalize-wizard`

**This plan makes the COPY honest. Plan `140.3-13` decides which routes get a capture.** The two halves
are deliberately split: a sentence that is true because we stopped claiming, and a sentence that is
true because we started notifying, are different remedies and only one of them is copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-CONTEXT.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-PATTERNS.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-05-SUMMARY.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-09-SUMMARY.md

<read_first>
- `DESIGN.md` §Voice/microcopy — **read it before writing a single sentence.** Declarative,
  sentence-case captions; state limitations with the threshold attached and give the number; no
  adjectives where a number exists; active voice. Also §AI-Slop Ban (no generic filler, no
  exclamation, semantic glyphs only) and §Error Envelope (the canonical renderer and its
  authoring rule).
- `src/lib/wizardErrors.ts` **as `140.3-10` left it** — the whole `WIZARD_ERROR_COPY` table. Locate the
  seven strings by their text, not by line: five earlier plans have edited this file. Also read:
  - the in-file comment near the top that already criticises the *"something went wrong, our team has
    been notified"* pattern as *"both untrue…"* — **the file argues against its own copy; the
    remedy is written down and was never applied.**
  - the comment lower down about the *"our team has been notified" dead end* and the breaker trip
    reaching that terminal.
  - the `SERVICE_UNAVAILABLE_RETRY` entry — its copy is already correct and is the tonal model:
    *"…this is on our side, not your key."*
  - the `VALIDATION_FAILED` / `RATE_LIMITED` entries `140.3-01` left as explicit placeholders, marked
    in-file as this phase's to author.
- `src/components/landing/VerificationForm.tsx` — locate
  `throw new Error(data.error ?? "Verification failed");` (TS-17) and, twelve lines on,
  `throw new Error("Verification service returned an invalid response");`.
  ⚠️ **That second throw is the CLIENT MIRROR of the dead 502 `140.3-02` deleted server-side.** If
  the server no longer rejects, this sniff may now be unreachable or newly wrong — it is **not** in
  `140.3-02`'s `files_modified`, so nobody has re-derived it. Do that here.
- `src/lib/process-key-client.ts` — the envelope shape the teaser actually receives
  (`{ ok:false, code, human_message, correlation_id, recoverable }`). **The 403 has no `error` key at
  all**, which is why `data.error ??` always falls through to the generic string.
- `src/lib/envelope.ts` — `RECOVERABLE_ACTIONS`. Changing `actions` changes `recoverable` and
  therefore whether a retry renders. **If a copy fix requires an `actions` change, that is a behaviour
  change — say so explicitly rather than sliding it in as copy.**
</read_first>

<interfaces>
**What each false claim must become — the remedy is stated so it is not re-litigated per string:**

- **L1 / L1b (notification).** Either stop claiming, or claim only where a capture exists. This plan
  takes the first: remove the claim. `140.3-13` may add captures afterwards, and if it does, the copy
  can be revisited **there** with the receipt. Do not write "our team has been notified" on any code
  reachable from a zero-capture route — M71's mutation restores exactly that and must redden.
- **L2a / L2b (asserting work we cannot know happened).** The client cannot know whether trades were
  fetched. Replace with copy that states what IS known — the request failed at a named stage — and
  what the user can do. **Do not replace one unknowable assertion with a softer unknowable
  assertion.**
- **L3a / L3b (asserting work we cannot know did NOT happen).** "Your data is unchanged" is
  unknowable after a 500 from a handler that commits. It is also a **retry-steering** claim into the
  C-01 dead end. State the uncertainty and give a non-destructive way forward.
  ⚠️ **Do not add a retry affordance that assumes PYAPI-09's reply-contract fix has landed** — CONTEXT
  is explicit that this phase must not steer users into that dead end.
- **L4 (a promise scoped wider than its mechanism).** The idempotency guarantee is real on the CSV
  path and absent elsewhere. Either scope the sentence to the flow that has it, or drop the mechanism
  name. **Do not promise it globally in anticipation of TS-33** — that lands in `140.3-14` and a copy
  promise that runs ahead of its mechanism is how this class started.
- **TS-09's placeholders — and every other placeholder this phase left.** Write real copy for
  `VALIDATION_FAILED` and `RATE_LIMITED` (left by `140.3-01`) **and for the members `140.3-05` added**
  (`KEY_EXCHANGE_UNAVAILABLE`, `KEY_VENUE_TRANSIENT`, and whatever `CIRCUIT_OPEN` resolved to).
  **`140.3-05` was explicitly forbidden from writing final copy** precisely so it lands here: it runs
  four waves before `140.3-09`'s wait plumbing, and `RATE_LIMITED`'s copy must **name the real wait**.
  Use the wait field `140.3-09` added if a wait is available; **do not invent a number**.
  ⚠️ Re-run `grep -n 'TODO-COPY-140.3-12' src/lib/wizardErrors.ts` first and author copy for **every**
  entry it returns — do not work from this list alone.
  ⚠️ **Grep the MARKER token, never the word "placeholder".** That word has **three pre-existing hits**
  in this file — a live `SIZE_MB_PLACEHOLDER` constant used for file-size interpolation at two call
  sites, plus docblocks. It is interpolation machinery, **not** a copy marker, and it must survive
  this plan untouched.

**TS-17.** The 403 envelope carries `human_message`, never `error`. One line:
`data.human_message ?? data.error ?? "Verification failed"`.
⚠️ **This is the ANONYMOUS teaser.** Whatever it renders obeys the same static-copy constraint as
`CIRCUIT_OPEN_HUMAN_MESSAGE` (threat T-140-08): no upstream URL, no status, no service internals.
Confirm the envelope's `human_message` cannot carry any of those before surfacing it — the seam's
copy is static by design, but this is a rendered upstream field and the check is cheap.

**Out of scope, stated so it is not smuggled in:**
- Adding Sentry captures — `140.3-13`.
- `TS-33`'s `wizard_session_id` field — `140.3-14`. **This plan must not write copy that assumes it.**
- `TS-37`'s composite-cap copy and `TS-38`'s `SEAM_MISCONFIGURED` copy — `140.3-14` / `140.3-15`.
  Those are NEW codes needing new copy; this plan fixes EXISTING strings.
- Any `actions` change made silently. If one is needed, it is a behaviour change and must be called
  out with its `recoverable` consequence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all seven false claims and write TS-09's real copy</name>
  <files>src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts</files>
  <read_first>
- `DESIGN.md` §Voice/microcopy, §AI-Slop Ban, §Error Envelope — **before writing.**
- `src/lib/wizardErrors.ts` — the seven strings by text, the two in-file comments that already argue
  against this copy, the `SERVICE_UNAVAILABLE_RETRY` tonal model, and the two placeholders.
- `src/lib/envelope.ts` — `RECOVERABLE_ACTIONS`.
- `140.3-09-SUMMARY.md` — the wait field's name and units, so `RATE_LIMITED`'s copy can use it.
  </read_first>
  <action>
Re-run the enumeration greps first (the sentences, not the codes) and confirm the inventory is still
seven strings across five codes. If the count differs, the class boundary moved — record the new count
as the receipt rather than working from this plan's table.

Rewrite each of the seven per the remedies in `<interfaces>`, and write real copy for the two
placeholders. Every sentence obeys DESIGN.md: declarative, sentence-case, active voice, the limitation
stated with its threshold and the number given, no adjectives where a number exists, no exclamation,
no generic filler.

The file's own comments already argue that the "team has been notified" pattern is untrue. **Update
those comments to record that the remedy was applied and by which plan** — a comment that still
describes the defect beside the fix is how the defect grows back (the TS-01/TS-03 pairing is the
receipt for that).

If any fix requires an `actions` change, state it in the SUMMARY as a **behaviour** change with its
`recoverable` consequence. Do not slide it in as copy.

Add cases that fail without the change:
  - a table-wide honesty assertion: **no entry anywhere in `WIZARD_ERROR_COPY`** contains the
    notification claim, the fetched-trades claim, or the data-unchanged claim. Scan the TABLE with
    hand-typed forbidden substrings — do not assert against five named codes, because two of the
    seven strings lived on a code no document listed;
  - the two new entries render their real copy (expected sentences **hand-typed in the test**;
    reading `WIZARD_ERROR_COPY[code].title` on the expected side is the self-referential defect);
  - a hand-typed table-size guard so a shrinking table cannot pass silently.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/wizardErrors.test.ts src/lib/envelope.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `grep -vE '^\s*(//|\*)' src/lib/wizardErrors.ts | grep -ci "been notified"` → **0** — comment lines excluded, so the updated explanatory comments do not mask a surviving string.
- `grep -vE '^\s*(//|\*)' src/lib/wizardErrors.ts | grep -ci "we fetched your trades"` → **0**.
- `grep -vE '^\s*(//|\*)' src/lib/wizardErrors.ts | grep -ci "data is unchanged"` → **0**.
- `grep -vE '^\s*(//|\*)' src/lib/wizardErrors.ts | grep -ci "wizard_session_id idempotency"` → **0**.
- **PRE-TASK GUARD, run FIRST: `grep -c 'TODO-COPY-140.3-12' src/lib/wizardErrors.ts` must be **> 0** before this task edits anything.** Record the number and the code names. ⚠️ A zero pre-task count does **not** mean "nothing owed" — it means `140.3-01`/`140.3-05` failed to emit the marker, and the closing criterion below would then pass vacuously while the copy is silently never written. **A zero here is a FINDING against those plans: STOP, report it, and identify the un-marked entries by reading the `WizardErrorCode` union against `WIZARD_ERROR_COPY`.**
- **RECONCILE, same step: the pre-task count must EQUAL `2 + N`**, where `2` is `140.3-01`'s marked entries and `N` is the number recorded under `## Marker accounting` in **`140.3-05`'s SUMMARY**. ⚠️ **A `> 0` guard alone catches a TOTAL emit failure but not a PARTIAL one** — if `140.3-05` added members without marking them, this task closes only the markers that exist and the unmarked codes ship with **no copy and no failing check anywhere**. If the counts disagree, **STOP and reconcile before authoring any copy**: the shortfall names exactly which codes were missed. If `140.3-05`'s SUMMARY has no `## Marker accounting` heading, that is itself the finding.
- **POST-TASK: `grep -c 'TODO-COPY-140.3-12' src/lib/wizardErrors.ts` → 0.** Every marker counted in the pre-task guard is gone because real copy replaced it. The two numbers together — `N > 0` then `0` — are what make this a check rather than an assertion.
- **`grep -c "SIZE_MB_PLACEHOLDER" src/lib/wizardErrors.ts` is UNCHANGED from its pre-task value (2 call sites + 1 declaration).** That constant is live file-size interpolation machinery, not a copy marker. ⚠️ An earlier draft of this criterion asserted `grep -c "placeholder" → 0`, whose only satisfaction path was deleting this working constant and gutting three docblocks. **Do not do that.**
- The table-wide honesty test iterates `WIZARD_ERROR_COPY` and its forbidden-substring list is a hand-typed literal in the test file; its table-size assertion is a hand-typed number, not `Object.keys(...).length`.
  </acceptance_criteria>
  <done>
All seven false claims are gone from the copy table, the two placeholders carry real DESIGN.md-conformant
copy, the file's own comments record that the remedy was applied, and a table-wide honesty test guards
the class rather than five named codes.
  </done>
</task>

<task type="auto">
  <name>Task 2: TS-17 — the anonymous teaser renders the rejection it actually receives</name>
  <files>src/components/landing/VerificationForm.tsx, src/components/landing/VerificationForm.test.tsx</files>
  <read_first>
- `src/components/landing/VerificationForm.tsx` — both throws.
- `src/lib/process-key-client.ts` — the envelope shape (`human_message`, no `error` key on the 403).
- `src/app/api/verify-strategy/route.ts` **as `140.3-02` left it** — specifically whether the
  `"Verification service returned an invalid response"` 502 rejection was deleted server-side.
  `140.3-02-SUMMARY.md` records the decision; read it. **The client-side mirror of that sniff is in
  THIS file and was in nobody's `files_modified`.**
- `src/lib/process-key-client.ts`'s `CIRCUIT_OPEN_HUMAN_MESSAGE` docblock — the static-copy constraint
  (threat T-140-08) that governs anything the anonymous teaser renders.
  </read_first>
  <action>
Read `human_message` before `error`, so a write-capable-key rejection renders its real explanation
instead of the generic *"Verification failed"*.

Then **re-derive the second throw.** `140.3-02` deleted the server-side `verification_id` sniff as
dead. This client-side sniff is its mirror and is still live. Determine from source whether it is now
unreachable, still correct, or newly wrong, and act accordingly — delete it with the unreachability
proven, or keep it with the reason recorded. **"It was not in scope" is not an outcome here**; it was
in nobody's scope, which is exactly why it must be resolved now.

Confirm before surfacing `human_message` that the envelope cannot carry an upstream URL, a status or
service internals into an anonymous browser. The seam's copy is static by design, but this renders an
upstream field to an unauthenticated visitor and the check is cheap.

Add cases that fail without the change:
  - a 403 rejection carrying only `human_message` renders that message, not "Verification failed";
  - a body with neither field still renders the generic fallback;
  - whichever verdict was reached on the second throw is covered by a case that would fail if the
    verdict were wrong.
`vi.unstubAllGlobals()` in `afterEach`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/landing/VerificationForm.test.tsx src/app/api/verify-strategy/</automated>
  </verify>
  <acceptance_criteria>
- `grep -vE '^\s*(//|\*|#)' src/components/landing/VerificationForm.tsx | grep -c "human_message"` ≥ 1, and it appears **before** `data.error` in the same expression (verify by reading the line).
- The second throw is either absent (`grep -c "returned an invalid response"` → 0) **or** present with a comment recording why it is still live and what proves it — one or the other, never silence.
- The three cases pass.
- No rendered string in the 403 case contains `http`, a bare status number, or a service hostname — assert this in the test.
  </acceptance_criteria>
  <done>
The teaser renders the rejection's own `human_message`. The orphaned client-side sniff is resolved
with its verdict recorded. Nothing internal reaches an anonymous browser.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ledger, then mutation M71</name>
  <files>.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md</files>
  <read_first>
- The obligations ledger, rows **TS-09**, **TS-17** and the count header. **Gitignored, no git backup
  — targeted edits only.**
- `140.3-VALIDATION.md` row M71.
  </read_first>
  <action>
Mark **TS-09** SATISFIED (completing the split: `140.3-01` landed the union members and the
recognition branch; this plan landed the copy strings). Mark **TS-17** SATISFIED by `140.3-12`,
recording the second-throw verdict.

Record the **C-9 corrections in place**, with the counts as receipts:
  - the lie class is **seven strings across five codes**, two more than any source document listed,
    and name the two that were missed (`GATE_ANALYTICS_FAILED`'s notification claim and its second
    *"We fetched your trades successfully"*), plus the fact that they were found by grepping the
    SENTENCE rather than the code;
  - the Sentry-less route class is **9 of 15**, not the 2 CONTEXT names — list all nine — and that
    this plan closed the copy half while `140.3-13` owns the capture half;
  - that the orphaned client-side `verification_id` sniff in `VerificationForm.tsx` was in **no
    plan's `files_modified`** and was resolved here.
Recount the header's per-owner totals in the SAME edit.

Then OBSERVE this mutation, with no other agent active on the tree. Afterwards `git status` clean and
`grep -rn MUTANT src/` empty.
  M71 — restore *"Our team has been notified"* to a code reachable from a zero-Sentry route ⇒ must
        redden the table-wide copy-honesty assertion. ⚠️ If it stays green, the assertion is scanning
        a hand-listed set of codes rather than the table — fix the test, not the ledger.
Record the observed failure output verbatim. An "asserted only" row is not acceptable.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ src/components/landing/ && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
- The ledger's `wc -l` is at or above its pre-edit value; the header's per-owner totals were recounted in the same edit.
- The note lists all nine zero-Sentry routes by name, and both missed lie strings by code.
- TS-09's row states the split is now complete and names both plans.
- M71's receipt pastes the failing test name and assertion text.
  </acceptance_criteria>
  <done>
TS-09 and TS-17 are SATISFIED, the seven-string/five-code and 9-of-15 corrections are recorded in
place, and M71 observed RED against a table-wide assertion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server state → user-facing assertion | Copy claims what happened to the user's data; the client cannot observe it |
| Upstream `human_message` → anonymous browser | An upstream-authored string is rendered to an unauthenticated visitor |
| Copy → user action | A sentence can steer a user into a dead end or a destructive control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.3-12-01 | Repudiation | Claiming a team was notified from a path with zero Sentry captures — an audit trail asserted and absent | mitigate | The claim is removed from the copy table; a table-wide honesty test with hand-typed forbidden substrings guards the class; M71 falsifies it. |
| T-140.3-12-02 | Repudiation | Asserting the user's data is unchanged after a 500 from a handler that commits | mitigate | Replaced with copy that states the uncertainty and gives a non-destructive way forward; no retry affordance is added that assumes PYAPI-09 landed. |
| T-140.3-12-03 | Information disclosure | An upstream `human_message` carrying a URL, a status or service internals into an anonymous browser (T-140-08) | mitigate | Verified before surfacing; a test asserts no `http`, no bare status and no hostname in the rendered 403 case. |
| T-140.3-12-04 | Repudiation | Promising `wizard_session_id` idempotency on flows that do not send it | mitigate | The promise is scoped to the CSV path or its mechanism name dropped; the plan forbids anticipating TS-33. |
| T-140.3-12-05 | Repudiation | Enumerating the lie class by CODE, missing the two strings on `GATE_ANALYTICS_FAILED` | mitigate | Enumeration is by sentence; the table-wide test scans every entry rather than a named list. |
| T-140.3-12-06 | Denial of service (user data) | A copy fix silently changing `actions` and therefore `recoverable`, altering which controls render | mitigate | Any `actions` change must be declared as a behaviour change with its consequence; `140.3-10`'s table-wide TRAP-4 guard remains green. |
| T-140.3-12-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. |
</threat_model>

<verification>
- `npx vitest run src/lib/ src/components/ "src/app/(dashboard)/strategies/new/wizard/"` green — the
  copy table is read by ten `"use client"` components and every wizard step.
- `npx tsc --noEmit` exit 0 · `npm run lint` 0 errors.
- Zero Python files modified.
- **`140.3-10`'s table-wide TRAP-4 guard must still be green** — a copy edit that changed `actions`
  without saying so would show up here.
- **Full suite once at this wave boundary** (`npm test`).
</verification>

<success_criteria>
- Zero copy strings claim notification, claim trades were fetched, or claim data is unchanged.
- The idempotency promise is scoped to the flow that has the mechanism.
- The teaser renders the rejection's own message; the orphaned sniff is resolved either way.
- M71 observed RED against a table-wide assertion, not a hand-listed set.
</success_criteria>

<output>
Create `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-12-SUMMARY.md` when done.
Record: the re-run enumeration count; all seven replacement sentences verbatim, with the old string
beside each; any `actions` change declared as a behaviour change; the second-throw verdict for
`VerificationForm` with its proof; the nine zero-Sentry routes handed to `140.3-13`; and the M71
receipt.
</output>
