---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: G9
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [SEAMUX-03]
files_modified:
  - src/app/api/admin/strategy-review/route.ts
  - src/app/api/admin/strategy-review/route.test.ts

must_haves:
  truths:
    - "Every error arm /api/admin/strategy-review emits carries a top-level machine `code` — this is the 10th route the class-map found and the 140.3-VERIFICATION list MISSED (instance-not-class hazard); it must not be dropped again."
    - "The route's in-scope basis is stated honestly: its only seam import is scrubSeamError (seam-redaction); it makes no analytics-service call; it is in the SEAMUX-03 class by the seam-importing class definition, and every arm is an admin-reachable route arm."
    - "One fact, one token: all ~13 byte-identical 'Cannot verify strategy data source' 503 arms carry the SAME code, and all four 409 re-check arms carry the SAME code — mirroring keys/sync's two-throttle-arms doctrine."
    - "No status, sentence, header, or P458-style uniformity property changed — additive `code:` only."
  artifacts:
    - path: "src/app/api/admin/strategy-review/route.ts"
      provides: "22+ coded error arms across GET/POST handlers"
      contains: 'code: "REVIEW_SOURCE_READ_FAILED"'
    - path: "src/app/api/admin/strategy-review/route.test.ts"
      provides: "Per-arm body.code assertions incl. the arm-agnostic every-error-has-a-code fence"
  key_links:
    - from: "src/app/api/admin/strategy-review/route.test.ts"
      to: "src/app/api/admin/strategy-review/route.ts"
      via: "expect(body.code) per arm"
      pattern: "code"
---

<objective>
Close the SEAMUX-03 gap on `/api/admin/strategy-review` — the route the
140.3-VERIFICATION's nine-route list did NOT contain. A fresh class-map of ALL
seam-importing production routes under src/app/api (`grep -rlE "from
'@/lib/(analytics-client|process-key-client|seam-)'"`, tests excluded) found
ten zero-code routes; this is the tenth. Skipping the class-map on P140 cost 37
scrapped fix commits — this plan exists so the same instance-not-class miss
does not recur inside the gap closure itself.

Scope honesty (required by the gap directive): this route's only seam import is
`scrubSeamError` from `@/lib/seam-redaction` (route.ts:17). It never calls the
analytics service — its error arms are Supabase reads and publish-gate
refusals. It is in the SEAMUX-03 class by the class definition (seam-importing
production route), the requirement's words are "every seam-touching route …
on every arm", and every arm below is a reachable admin-facing route arm — so
ALL arms get codes; none is skipped as "not-seam". If the executor finds a
genuinely unreachable arm, it says so in the SUMMARY with the reachability
argument rather than silently skipping it.

Purpose: the admin review surface discriminates on stable tokens (SEAMUX-03).
Output: one route (many arms) + per-arm test assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VERIFICATION.md
@src/app/api/keys/sync/route.ts
@src/lib/wizardErrors.ts
@src/lib/envelope.ts

<interfaces>
CODE VOCABULARY — identical to G4–G8 (BINDING):
1. Union members: VALIDATION_FAILED (request-shape rejection), RATE_LIMITED
   (our limiter), GUARD_BLOCKED (a publish/finalize gate refused —
   finalize-wizard:1181 precedent "This draft cannot be finalized"), UNKNOWN.
2. Cross-route non-union tokens shared with G5/G6/G8 — spell EXACTLY:
   UNAUTHENTICATED (401), FORBIDDEN (403 admin gate).
3. NEW route tokens minted by this plan (keys/sync non-union precedent;
   justification: these are admin-only facts no wizard renders, and
   WizardErrorCode membership forces user-facing copy + the
   EXPECTED_TABLE_SIZE=55 guard + a TRAP-4 scan):
   - REVIEW_SOURCE_READ_FAILED — every "Cannot verify strategy data source.
     Please try again." 503 arm (a read WE depend on failed; the answer is
     about US, never about the strategy). ONE token for ALL of them: to the
     caller they are one fact — "the review gate could not read its evidence;
     retry" — and WHICH read failed is our internal accounting (the log names
     it), exactly the argument keys/sync:108-120 makes for its two RATE_LIMITED
     arms.
   - REVIEW_RECHECK_FAILED — every 409 "Cannot approve: …" re-check arm (state
     changed during review; publish refused). One fact class, one token; the
     prose already names which threshold moved.
   Never mint any further token in this route; a new fact found during
   enumeration maps to the nearest of the above or UNKNOWN, with the choice
   argued in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Code every error arm of /api/admin/strategy-review</name>
  <read_first>
    src/app/api/admin/strategy-review/route.ts (the WHOLE 558-line file — enumerate every NextResponse.json site in BOTH handlers before assigning anything),
    src/app/api/keys/sync/route.ts (THE TEMPLATE, esp. the one-fact-one-token doctrine at :108-120),
    src/lib/wizardErrors.ts:37-268 (the union),
    src/app/api/admin/strategy-review/route.test.ts (existing test shape)
  </read_first>
  <files>src/app/api/admin/strategy-review/route.ts</files>
  <behavior>
    - 401 → "UNAUTHENTICATED"; 403 → "FORBIDDEN"; the three body-validation 400s → "VALIDATION_FAILED"; 429 → "RATE_LIMITED"
    - ALL "Cannot verify strategy data source" 503 arms → "REVIEW_SOURCE_READ_FAILED" (same token on every one)
    - The gate-refusal 400 ("Cannot approve: ${gate.reason}") → "GUARD_BLOCKED"; ALL 409 re-check arms → "REVIEW_RECHECK_FAILED"
  </behavior>
  <action>
    ENUMERATE BEFORE FIXING: list EVERY NextResponse.json call in the file with
    line numbers in the SUMMARY (both the GET and POST handlers if both exist —
    read the whole file; the plan-time scan covered POST only). Known floor: 22
    error arms — 401 (:62), 403 (:67), body-not-object 400 (:74), invalid-JSON
    400 (:81), zod 400 (:102), 429 (:114), thirteen "Cannot verify strategy
    data source" 503s (:159 :172 :183 :194 :201 :211 :236 :266 :332 :343 :350
    :412 :431), gate-refusal 400 (:273), four 409s (:369 :375 :384 :440).
    Anything beyond these found during enumeration gets mapped per the
    vocabulary rules and recorded.

    Assign:
    - :62 → "UNAUTHENTICATED"; :67 → "FORBIDDEN" (the route's own comment says
      callers branch on the failure mode — the code makes that branch stable).
    - :74, :81, :102 → "VALIDATION_FAILED".
    - :114 429 → "RATE_LIMITED", additive only. Do NOT re-plumb this inline
      deny through rateLimitDenyJson — that migration is SEAMRIM-05's class
      (140.4), not this plan's; a codeless inline arm becomes a coded inline
      arm, nothing more.
    - All thirteen source-read 503s → "REVIEW_SOURCE_READ_FAILED". Byte-same
      body on every one (same sentence + same code), preserving the existing
      uniformity; the console.error beside each arm keeps naming which read
      failed.
    - :273 gate refusal → "GUARD_BLOCKED" (union member; the same
      a-gate-refused fact finalize-wizard stamps with it).
    - :369, :375, :384, :440 409s → "REVIEW_RECHECK_FAILED".

    ADDITIVE ONLY: no status, sentence, or header changes; `{ error, code }`
    key order per the template. The P458-style property that byte-identical
    bodies stay byte-identical across sibling arms MUST survive — same fact,
    same full body.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test $(grep -vE '^\s*(//|\*)' src/app/api/admin/strategy-review/route.ts | grep -cE '(^|[^a-zA-Z_])code:') -ge 22</automated>
  </verify>
  <acceptance_criteria>
    strategy-review/route.ts: every NextResponse.json error body has a `code:`;
    comment-stripped `code:` count >= 22 (was 0); exactly two new tokens exist
    in the file (REVIEW_SOURCE_READ_FAILED, REVIEW_RECHECK_FAILED) and no
    synonym of either; the SUMMARY carries the full arm inventory plus the
    scope-honesty note (seam import = scrubSeamError only; in class by
    definition; all arms coded); `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-arm code assertions in route.test.ts</name>
  <read_first>
    src/app/api/admin/strategy-review/route.test.ts (existing shape — mocks, request builders, which arms are already exercised),
    src/app/api/admin/strategy-review/route.ts (as edited by Task 1)
  </read_first>
  <files>src/app/api/admin/strategy-review/route.test.ts</files>
  <behavior>
    - Each already-exercised error arm's assertion gains/adds an exact `body.code` expectation
    - New cases cover at least: one source-read 503 (REVIEW_SOURCE_READ_FAILED), the gate-refusal 400 (GUARD_BLOCKED), one 409 (REVIEW_RECHECK_FAILED), the 429 (RATE_LIMITED), 401/403 (UNAUTHENTICATED/FORBIDDEN), one validation 400 (VALIDATION_FAILED)
    - An arm-agnostic fence: every non-2xx response produced across the suite carries `typeof body.code === "string"` (a shared afterEach/helper assertion or an explicit sweep case)
  </behavior>
  <action>
    Extend the existing test file in its own mocking idiom (it already drives
    Supabase read failures for the C-3 arms — reuse those fixtures rather than
    building new ones). Where a case already asserts a 503 sentence, add the
    code expectation beside it — test the WIRING, not a helper: assertions run
    against the parsed response body of the invoked route handler.

    The describe block names SEAMUX-03 and states the WHY (Rule 9): a dropped
    `code` is a client-discrimination regression on the admin review surface.
    Perform the neutering check once locally (remove one arm's code, observe
    RED, restore — record RED observed in the SUMMARY; never commit the
    mutation).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/strategy-review</automated>
  </verify>
  <acceptance_criteria>
    route.test.ts asserts codes on at least the eight arm classes listed in
    behavior; the arm-agnostic body.code fence exists; RED-on-neutering
    observed and recorded; `npx vitest run src/app/api/admin/strategy-review`
    exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed client → admin gate | non-admins must learn nothing beyond 401/403 |
| review gate → Supabase reads | read failures must answer about US, never fabricate strategy facts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.3-G9-01 | Information disclosure | 13 uniform 503 arms | mitigate | ONE token + byte-identical bodies preserved — which read failed stays in logs, off the wire |
| T-140.3-G9-02 | Elevation of privilege | publish-gate arms | mitigate | additive-only; gate logic, statuses and fail-closed reads untouched; GUARD_BLOCKED/REVIEW_RECHECK_FAILED name refusal, not internals |
| T-140.3-G9-03 | Information disclosure | 401/403 codes | accept | UNAUTHENTICATED/FORBIDDEN restate the status; no new signal |
| T-140.3-G9-SC | Tampering | npm/pip installs | mitigate | ZERO packages installed by this plan set |
</threat_model>

<verification>
- `npx vitest run src/app/api/admin/strategy-review` exits 0
- `npx tsc --noEmit` exits 0; `npm run lint` — 0 new errors
- The route joins the coded set under the 140.3-VERIFICATION §3.1 re-run command (it was absent from that list; the SUMMARY states it now belongs in the class-map's 10-route roster)
</verification>

<success_criteria>
The tenth route — the one the verification list missed — emits a top-level
`code` on every arm; exactly two new tokens, each used uniformly across its
fact class; per-arm tests redden on any dropped code; the scope-honesty note is
on the record.
</success_criteria>

<output>
Create `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-G9-SUMMARY.md`
with the full arm inventory (line, status, code), the scope-honesty note, and
the RED-on-neutering record.
</output>
