---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: G6
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [SEAMUX-03]
files_modified:
  - src/app/api/bridge/route.ts
  - src/app/api/bridge/route.test.ts
  - src/app/api/portfolio-optimizer/route.ts
  - src/app/api/portfolio-optimizer/route.test.ts

must_haves:
  truths:
    - "Every error arm /api/bridge itself emits carries a top-level machine `code`."
    - "Every error arm /api/portfolio-optimizer itself emits carries a top-level machine `code`, including both rateLimitDenyJson bodies (currently the codeless builder defaults)."
    - "The B-26 money-bearing body shape ({ status: 'failed', suggestions: null, error }) is preserved byte-for-byte with `code` added — PortfolioOptimizer.tsx's discard-then-render contract is untouched."
    - "A forwarded upstream 4xx preserves err.seamCode; UNKNOWN only when the upstream carried none."
  artifacts:
    - path: "src/app/api/bridge/route.ts"
      provides: "9+ coded error arms"
      contains: 'code: "PORTFOLIO_NOT_FOUND"'
    - path: "src/app/api/portfolio-optimizer/route.ts"
      provides: "8+ coded error arms incl. coded deny bodies"
      contains: 'code: "CIRCUIT_OPEN"'
    - path: "src/app/api/bridge/route.test.ts"
      provides: "Per-arm body.code assertions"
    - path: "src/app/api/portfolio-optimizer/route.test.ts"
      provides: "Per-arm body.code assertions incl. both deny bodies"
  key_links:
    - from: "src/app/api/portfolio-optimizer/route.ts"
      to: "src/lib/ratelimit.ts"
      via: "throttledBody/misconfiguredBody overrides carrying codes"
      pattern: "throttledBody"
    - from: "src/app/api/bridge/route.test.ts"
      to: "src/app/api/bridge/route.ts"
      via: "expect(body.code) per arm"
      pattern: "code"
---

<objective>
Close the SEAMUX-03 gap on the other two direct analytics-client consumer
routes: `/api/bridge` (0/7 coded at HEAD) and `/api/portfolio-optimizer`
(0/5). Same catch-arm anatomy and same vocabulary as sibling plan 140.3-G5
(scenario/optimize + simulator); zero file overlap, same wave.

Special care on portfolio-optimizer: its error bodies are the B-26
money-bearing shape `{ status: "failed", suggestions: null, error }` that
`PortfolioOptimizer.tsx` uses to DISCARD stale suggestions — the shape must be
preserved exactly, `code` added beside it. And its rate-limit deny currently
uses `rateLimitDenyJson`'s codeless builder defaults, which need the
keys/sync-shape body overrides.

Purpose: a client discriminates the fault on a stable token (SEAMUX-03).
Output: two routes with coded arms + per-arm test assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VERIFICATION.md
@src/app/api/keys/sync/route.ts
@src/lib/wizardErrors.ts
@src/lib/envelope.ts

<interfaces>
CODE VOCABULARY — identical to plan G5 (BINDING; do not re-derive):
1. Union members where the fact matches (wizardErrors.ts:37-268):
   VALIDATION_FAILED (request-shape rejection), RATE_LIMITED (our limiter),
   SEAM_MISCONFIGURED (limiter/config misconfiguration), UNKNOWN (terminal
   fallback).
2. Established seam WIRE tokens: CIRCUIT_OPEN, UPSTREAM_TIMEOUT,
   UPSTREAM_NETWORK_ERROR (transport-level "unreachable" ONLY — never on an
   arm where the upstream answered).
3. Forwarded upstream arms: `err.seamCode ?? "UNKNOWN"`
   (AnalyticsUpstreamError.seamCode, analytics-client.ts:119).
4. Cross-route non-union tokens shared with G5 — spell EXACTLY:
   UNAUTHENTICATED (inline 401), FORBIDDEN (inline 403 ownership refusal),
   PORTFOLIO_NOT_FOUND (404 portfolio — same token G5 puts on simulator's 404).
   New in this plan: MISSING_PORTFOLIO_ID (the named-id-param arm, keys/sync
   MISSING_STRATEGY_ID precedent).
   Justification for non-union tokens: WizardErrorCode is the wizard COPY
   table's key space (each member forces copy + EXPECTED_TABLE_SIZE=55 bump +
   TRAP-4 scan); these arms are not wizard-rendered; the keys/sync template
   itself ships four non-union tokens.

`rateLimitDenyJson(rl, options)` (ratelimit.ts:368): default bodies are
CODELESS. Routes on the defaults must pass `throttledBody: { error: "Too many
requests", code: "RATE_LIMITED" }` and `misconfiguredBody: { error: "Rate
limiter unavailable", code: "SEAM_MISCONFIGURED" }` — builder-default sentences
byte-kept, codes added (keys/sync:136-158 shape).

Shared-helper arms are OUT OF SCOPE: withAuth's 401 (bridge is a withAuth
route) and approval-gate's 403 are helper-emitted across the whole API surface;
SEAMUX-03's adjudicated class is arms the route file emits (the verifier
accepted keys/sync with withAuth's 401 untouched). Note exclusions in SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Code every error arm of /api/bridge</name>
  <read_first>
    src/app/api/bridge/route.ts (whole file — enumerate ALL NextResponse.json sites first),
    src/app/api/keys/sync/route.ts (THE TEMPLATE),
    src/lib/analytics-client.ts:106-130 (AnalyticsUpstreamError.seamCode),
    src/app/api/bridge/route.test.ts (existing test shape)
  </read_first>
  <files>src/app/api/bridge/route.ts, src/app/api/bridge/route.test.ts</files>
  <behavior>
    - Both 400 input arms answer "VALIDATION_FAILED"; limiter-misconfigured 503 answers "SEAM_MISCONFIGURED"; 429 answers "RATE_LIMITED" (retryAfter body field kept)
    - 404 answers "PORTFOLIO_NOT_FOUND" (same spelling as simulator's in G5)
    - Breaker 503 answers "CIRCUIT_OPEN"; 4xx forward answers `err.seamCode ?? "UNKNOWN"`; 504 answers "UPSTREAM_TIMEOUT"; terminal 500 answers "UNKNOWN"
  </behavior>
  <action>
    ENUMERATE BEFORE FIXING: list every NextResponse.json call with line
    numbers in the SUMMARY, then assign. Floor: 9 own arms (VERIFICATION
    measured 7; the inline 429 + misconfigured 503 pair and the terminal arm
    make 9 — treat 9 as the floor, keep counting). bridge is a withAuth route —
    its 401 is helper-owned, excluded, noted.

    Mapping:
    - :64 invalid JSON, :74 schema parse → "VALIDATION_FAILED".
    - :92 limiter-misconfigured 503 → "SEAM_MISCONFIGURED" (keep Retry-After).
    - :104 429 → "RATE_LIMITED" (keep the `retryAfter` body field).
    - :125 404 "Portfolio not found" → "PORTFOLIO_NOT_FOUND".
    - :159 breaker 503 → "CIRCUIT_OPEN" beside the untouched CIRCUIT_OPEN_COPY
      sentence + Retry-After header.
    - :181 4xx forward (`err.message`) → `code: err.seamCode ?? "UNKNOWN"`.
    - :188 504 → "UPSTREAM_TIMEOUT".
    - terminal 500 "Bridge scoring failed. Please try again." (~:200) →
      "UNKNOWN" (Sentry capture untouched).

    ADDITIVE ONLY — no status, sentence, header, or key-order changes. Extend
    route.test.ts with per-arm code assertions in the file's existing shape,
    incl. a seamCode-forwarding case.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/bridge && test $(grep -vE '^\s*(//|\*)' src/app/api/bridge/route.ts | grep -cE '(^|[^a-zA-Z_])code:') -ge 9</automated>
  </verify>
  <acceptance_criteria>
    bridge/route.ts: zero bare `{ error }` arms remain among route-emitted
    arms; comment-stripped `code:` count >= 9 (was 0); route.test.ts asserts
    the exact code per arm above; `npx vitest run src/app/api/bridge` exits 0;
    `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Code every error arm of /api/portfolio-optimizer (B-26 shape preserved)</name>
  <read_first>
    src/app/api/portfolio-optimizer/route.ts (whole file — enumerate first; note the { status, suggestions, error } body shape),
    src/components/portfolio/PortfolioOptimizer.tsx (the B-26 consumer — read how it branches on the body BEFORE touching the shape),
    src/app/api/keys/sync/route.ts:134-158 (the rateLimitDenyJson override template),
    src/app/api/portfolio-optimizer/route.test.ts (existing test shape)
  </read_first>
  <files>src/app/api/portfolio-optimizer/route.ts, src/app/api/portfolio-optimizer/route.test.ts</files>
  <behavior>
    - 401 answers "UNAUTHENTICATED"; invalid-JSON 400 answers "VALIDATION_FAILED"; missing portfolio_id answers "MISSING_PORTFOLIO_ID"; ownership 403 answers "FORBIDDEN"
    - The deny arm answers `{ error: "Too many requests", code: "RATE_LIMITED" }` (429) / `{ error: "Rate limiter unavailable", code: "SEAM_MISCONFIGURED" }` (503)
    - Breaker/timeout/unreachable arms keep `{ status: "failed", suggestions: null, error }` byte-identical with `code` added: "CIRCUIT_OPEN" / "UPSTREAM_TIMEOUT" / "UPSTREAM_NETWORK_ERROR"
  </behavior>
  <action>
    Enumerate all arms with line numbers first (floor: 8 own arms; VERIFICATION
    measured 5 bare shapes — the deny expansion and enumeration will find more;
    keys/sync's "8 arms, not the 5 every source document listed" is the warning
    here). The approval-gate 403, if present via helper, is excluded and noted.

    Mapping:
    - :64 401 → "UNAUTHENTICATED".
    - :81 invalid JSON → "VALIDATION_FAILED".
    - :89 "portfolio_id is required" → "MISSING_PORTFOLIO_ID" (named-id-param
      fact; keys/sync MISSING_STRATEGY_ID precedent — NOT VALIDATION_FAILED,
      which this plan set reserves for structural body rejections).
    - the `rateLimitDenyJson(rl, { headers: NO_STORE_HEADERS })` deny (~:111):
      add `throttledBody: { error: "Too many requests", code: "RATE_LIMITED" }`
      and `misconfiguredBody: { error: "Rate limiter unavailable", code:
      "SEAM_MISCONFIGURED" }` — the builder's default sentences byte-kept
      (RATE_LIMITED, not KEY_RATE_LIMIT: this is not the key-connect family).
    - :148 403 "Forbidden" (ownership) → "FORBIDDEN".
    - :202 breaker 503 → add `code: "CIRCUIT_OPEN"` INSIDE the existing
      `{ status: "failed", suggestions: null, error: CIRCUIT_OPEN_COPY }` body
      — every existing key and value byte-unchanged (B-26: PortfolioOptimizer
      discards suggestions off this shape; changing it re-opens the
      stale-money-data defect this phase fixed). Token-refund call untouched.
    - :218 504 → add `code: "UPSTREAM_TIMEOUT"` to the same shape.
    - :251 503 "Analytics service unreachable" → add `code:
      "UPSTREAM_NETWORK_ERROR"` (here "unreachable" IS the transport fact, so
      the token is honest). Refund calls untouched.
    - Any terminal 500 found during enumeration → "UNKNOWN".

    Extend route.test.ts with per-arm code assertions, incl. BOTH deny bodies
    and one case asserting the breaker body still carries
    `status: "failed"` AND `suggestions: null` AND the code — so a shape
    regression and a code drop both redden.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/portfolio-optimizer && test $(grep -vE '^\s*(//|\*)' src/app/api/portfolio-optimizer/route.ts | grep -cE '(^|[^a-zA-Z_])code:') -ge 8</automated>
  </verify>
  <acceptance_criteria>
    portfolio-optimizer/route.ts: zero bare error arms remain among
    route-emitted arms; comment-stripped `code:` count >= 8 (was 0); the
    { status, suggestions, error } shape byte-preserved on all three analytics
    arms (asserted in tests); both deny bodies coded; `npx vitest run
    src/app/api/portfolio-optimizer` exits 0; `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed client → both routes | untrusted JSON bodies; money-bearing optimizer output on portfolio-optimizer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.3-G6-01 | Tampering | B-26 body shape on portfolio-optimizer | mitigate | shape byte-preserved, code additive; test asserts status/suggestions/code together |
| T-140.3-G6-02 | Information disclosure | new `code` tokens | mitigate | closed-set tokens; none names env vars/hosts; seamCode forwards only what the arm already forwards |
| T-140.3-G6-03 | DoS | deny-arm overrides | mitigate | limiter semantics untouched; builder still decides 429-vs-503; sentences byte-kept |
| T-140.3-G6-SC | Tampering | npm/pip installs | mitigate | ZERO packages installed by this plan set |
</threat_model>

<verification>
- `npx vitest run src/app/api/bridge src/app/api/portfolio-optimizer` exits 0
- `npx tsc --noEmit` exits 0; `npm run lint` — 0 new errors
- Both routes leave 140.3-VERIFICATION §3.1's zero-code set under its own re-run command
</verification>

<success_criteria>
Both routes emit a top-level `code` on every arm they own; PORTFOLIO_NOT_FOUND
spelling matches G5's simulator token exactly; the B-26 body shape is
byte-preserved; per-arm tests redden on any dropped code.
</success_criteria>

<output>
Create `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-G6-SUMMARY.md`
with the full arm inventory (line, status, code) for both routes and the named
helper-arm exclusions.
</output>
