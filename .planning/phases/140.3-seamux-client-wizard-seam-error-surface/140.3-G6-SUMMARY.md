---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: G6
subsystem: api
tags: [seam-errors, error-envelope, next-route-handler, rate-limit, machine-codes]

# Dependency graph
requires:
  - phase: 140.3-G4/G5
    provides: SEAMUX-03 coded-arm pattern + BINDING code vocabulary (keys/sync template, scenario/optimize sibling)
  - phase: 140.3-01 (TS-05)
    provides: AnalyticsUpstreamError.seamCode — the forwarded-4xx machine code
provides:
  - "/api/bridge: 9 route-emitted error arms each carry a top-level machine `code`"
  - "/api/portfolio-optimizer: 9 code: sites incl. both rateLimitDenyJson deny bodies; B-26 money-bearing shape byte-preserved"
affects: [140.3-verification, SEAMUX-03-aggregate-closure, wizard-error-copy-consumers]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive machine `code` on every route-emitted seam-error arm (no status/sentence/header/key-order change)"
    - "rateLimitDenyJson throttledBody/misconfiguredBody overrides add codes to the codeless builder defaults"
    - "`code: err.seamCode ?? \"UNKNOWN\"` forwards the upstream's own code on a 4xx forward"

key-files:
  created: []
  modified:
    - src/app/api/bridge/route.ts
    - src/app/api/bridge/route.test.ts
    - src/app/api/portfolio-optimizer/route.ts
    - src/app/api/portfolio-optimizer/route.test.ts

key-decisions:
  - "Both 400 input arms answer VALIDATION_FAILED (structural body rejection); optimizer's missing-portfolio_id answers MISSING_PORTFOLIO_ID (named-id-param fact, keys/sync MISSING_STRATEGY_ID precedent)"
  - "404 answers PORTFOLIO_NOT_FOUND — same spelling as G5's simulator 404"
  - "optimizer 503-unreachable arm answers UPSTREAM_NETWORK_ERROR (here 'unreachable' IS the transport fact); breaker CIRCUIT_OPEN; timeout UPSTREAM_TIMEOUT"
  - "Helper-emitted arms excluded and noted: bridge withAuth 401; optimizer CSRF 403 + approval-gate 403"

patterns-established:
  - "B-26 preservation: on portfolio-optimizer the { status:'failed', suggestions:null, error } shape stays byte-identical with `code` appended last"

requirements-completed: []  # SEAMUX-03 is an aggregate across 9 routes — deliberately NOT marked complete here (objective directive)

# Metrics
duration: ~15min
completed: 2026-07-31
---

# Phase 140.3 Plan G6: Bridge + Portfolio-Optimizer Typed {code} Arms Summary

**Every error arm `/api/bridge` (9) and `/api/portfolio-optimizer` (9 `code:` sites) emits now carries a stable top-level machine `code`; the B-26 money-bearing optimizer body shape is byte-preserved with `code` added beside it.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-07-31T03:43:00Z
- **Completed:** 2026-07-31T03:52:00Z
- **Tasks:** 2 (both TDD: RED test → GREEN feat)
- **Files modified:** 4

## Accomplishments
- `/api/bridge`: 9 route-emitted arms coded (comment-stripped `code:` count 0 → 9).
- `/api/portfolio-optimizer`: 9 `code:` sites coded (0 → 9), including BOTH `rateLimitDenyJson` deny bodies (429 RATE_LIMITED / 503 SEAM_MISCONFIGURED) that were on the builder's codeless defaults.
- B-26 shape `{ status:"failed", suggestions:null, error }` byte-preserved on all three optimizer analytics arms; tests assert status/suggestions/code together so a shape regression AND a dropped code both redden.
- Forwarded upstream 4xx on bridge preserves `err.seamCode` (UNKNOWN only when absent), pinned by a two-way test.

## Bridge arm inventory (`src/app/api/bridge/route.ts`)

| Line | Status | Arm | Code |
|------|--------|-----|------|
| 65  | 400 | invalid JSON | VALIDATION_FAILED |
| 84  | 400 | schema parse (UUID) reject | VALIDATION_FAILED |
| 100 | 503 | limiter misconfigured | SEAM_MISCONFIGURED |
| 118 | 429 | real throttle (retryAfter body field kept) | RATE_LIMITED |
| 138 | 404 | portfolio not found | PORTFOLIO_NOT_FOUND |
| 172 | 503 | breaker open | CIRCUIT_OPEN |
| 198 | err.status (4xx) | upstream 4xx forward | `err.seamCode ?? "UNKNOWN"` |
| 205 | 504 | upstream timeout | UPSTREAM_TIMEOUT |
| 219 | 500 | terminal | UNKNOWN |

**Excluded (helper-owned):** withAuth 401 (`@/lib/api/withAuth`) — SEAMUX-03's adjudicated class is arms the route file emits; the verifier accepted keys/sync with withAuth's 401 untouched.

## Portfolio-optimizer arm inventory (`src/app/api/portfolio-optimizer/route.ts`)

| Line | Status | Arm | Code |
|------|--------|-----|------|
| 70  | 401 | inline unauth | UNAUTHENTICATED |
| 87  | 400 | invalid JSON | VALIDATION_FAILED |
| 97  | 400 | missing portfolio_id | MISSING_PORTFOLIO_ID |
| 123 | 429 | deny throttledBody | RATE_LIMITED |
| 126 | 503 | deny misconfiguredBody | SEAM_MISCONFIGURED |
| 166 | 403 | ownership refusal | FORBIDDEN |
| 224 | 503 | breaker open (B-26 shape) | CIRCUIT_OPEN |
| 241 | 504 | upstream timeout (B-26 shape) | UPSTREAM_TIMEOUT |
| 281 | 503 | analytics unreachable (B-26 shape) | UPSTREAM_NETWORK_ERROR |

**Excluded (helper-owned):** CSRF 403 (`assertSameOrigin`) and approval-gate 403 (`assertProfileApproved`) — both emitted across the whole API surface by shared helpers, not by this route file.

## Task Commits

1. **Task 1 RED: bridge per-arm code assertions** - `81524ba5` (test) — 10 fail against codeless route; mock AnalyticsUpstreamError gains optional `seamCode` arg
2. **Task 1 GREEN: bridge coded arms** - `f55c0868` (feat) — 61/61 pass; `code:` count 0 → 9
3. **Task 2 RED: optimizer per-arm code assertions** - `c21fca41` (test) — 9 fail; deny + breaker + timeout + unreachable assert shape AND code together
4. **Task 2 GREEN: optimizer coded arms (B-26 preserved)** - `9ace6d54` (feat) — 32/32 pass; `code:` count 0 → 9

## Files Created/Modified
- `src/app/api/bridge/route.ts` - 9 error arms coded, additive only
- `src/app/api/bridge/route.test.ts` - per-arm `body.code` assertions incl. seamCode-forwarding both ways
- `src/app/api/portfolio-optimizer/route.ts` - 9 `code:` sites, B-26 shape byte-preserved, deny bodies coded via overrides
- `src/app/api/portfolio-optimizer/route.test.ts` - per-arm `body.code`; both deny bodies + all three B-26 arms assert status/suggestions/code together

## Decisions Made
- **MISSING_PORTFOLIO_ID (not VALIDATION_FAILED)** for the optimizer's absent-`portfolio_id` arm: a named-id-param fact distinct from structural body rejection, matching the keys/sync `MISSING_STRATEGY_ID` precedent.
- **UPSTREAM_NETWORK_ERROR** on the optimizer's 503-unreachable arm: this arm has no typed upstream branch, so it is reached by a transport failure — "unreachable" is the honest transport token here (contrast the bridge/scenario 4xx-forward arms, which never assert a transport code because the upstream answered).
- **`code` appended LAST** on the B-26 body so every pre-existing key/value/order is byte-unchanged.

## Deviations from Plan

None — plan executed exactly as written. Both tasks landed at the plan's stated code floors (bridge ≥ 9, optimizer ≥ 8; both measured 9). No auto-fixes required.

## Issues Encountered
- STATE.md uses the milestone-level (non phase/plan-counter) format, so `state.advance-plan` and `state.record-metric` SDK handlers could not parse it. Handled per the existing PG4/PG5 precedent: added a Performance Metrics row and updated `stopped_at`/`last_updated` by direct edit. `state.update-progress` and `roadmap.update-plan-progress` ran cleanly.

## Verification
- `npx vitest run src/app/api/bridge src/app/api/portfolio-optimizer` → 5 files, 93 tests, all pass.
- `npx tsc --noEmit` → exit 0.
- `npx eslint` on the 4 touched files → exit 0.
- Comment-stripped `code:` gate: bridge 9 (≥9), optimizer 9 (≥8).

## Next Phase Readiness
- SEAMUX-03 coded-arm work now covers bridge + portfolio-optimizer alongside G4 (verify-strategy, validate-and-encrypt) and G5 (scenario/optimize, simulator) and keys/sync. SEAMUX-03 is an aggregate across the 9 seam-importing routes and is **deliberately NOT marked complete** here — the aggregate close is the verifier's call once all routes are covered.

---
*Phase: 140.3-seamux-client-wizard-seam-error-surface*
*Completed: 2026-07-31*
