---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: G5
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [SEAMUX-03]
files_modified:
  - src/app/api/scenario/optimize/route.ts
  - src/app/api/scenario/optimize/route.test.ts
  - src/app/api/simulator/route.ts
  - src/app/api/simulator/route.test.ts

must_haves:
  truths:
    - "Every error arm /api/scenario/optimize itself emits carries a top-level machine `code`."
    - "Every error arm /api/simulator itself emits carries a top-level machine `code`, including the inline 429 and the limiter-misconfigured 503."
    - "A forwarded upstream 4xx preserves the upstream's own machine code (err.seamCode) and answers UNKNOWN only when the upstream carried none."
    - "The breaker 503 arms carry CIRCUIT_OPEN with the CIRCUIT_OPEN_COPY sentence and Retry-After header byte-unchanged."
  artifacts:
    - path: "src/app/api/scenario/optimize/route.ts"
      provides: "11+ coded error arms"
      contains: 'code: "CIRCUIT_OPEN"'
    - path: "src/app/api/simulator/route.ts"
      provides: "10+ coded error arms"
      contains: 'code: "PORTFOLIO_NOT_FOUND"'
    - path: "src/app/api/scenario/optimize/route.test.ts"
      provides: "Per-arm body.code assertions"
    - path: "src/app/api/simulator/route.test.ts"
      provides: "Per-arm body.code assertions"
  key_links:
    - from: "src/app/api/simulator/route.ts"
      to: "src/lib/analytics-client.ts"
      via: "err.seamCode forwarded on the 4xx-forward arm"
      pattern: "seamCode"
    - from: "src/app/api/scenario/optimize/route.test.ts"
      to: "src/app/api/scenario/optimize/route.ts"
      via: "expect(body.code) per arm"
      pattern: "code"
---

<objective>
Close the SEAMUX-03 gap on two of the four direct analytics-client consumer
routes: `/api/scenario/optimize` (0/12 coded at HEAD) and `/api/simulator`
(0/8). Their catch-arm anatomy is identical (CircuitOpenError 503 →
AnalyticsUpstreamError → AnalyticsTimeoutError 504 → terminal 500), so one
vocabulary covers both. Sibling plan 140.3-G6 covers `bridge` +
`portfolio-optimizer` with the same vocabulary; the two plans share zero files
and run in the same wave.

Purpose: a client discriminates the fault on a stable token (SEAMUX-03).
Output: two routes with coded arms + per-arm test assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VERIFICATION.md
@src/app/api/keys/sync/route.ts
@src/lib/wizardErrors.ts
@src/lib/envelope.ts

<interfaces>
CODE VOCABULARY — order of preference (BINDING; identical across G4–G9):
1. Existing `WizardErrorCode` union member whose FACT matches
   (src/lib/wizardErrors.ts:37-268): VALIDATION_FAILED ("a request-shape
   rejection on the API path" — the union's own definition), RATE_LIMITED (OUR
   limiter refused; NOT KEY_RATE_LIMIT, which is the key-connect family's),
   SEAM_MISCONFIGURED (our limiter/config misconfiguration), UNKNOWN
   (terminal/unclassified fallback — create-with-key:457 precedent).
2. Established seam WIRE tokens (process-key-client emits them; the client-side
   SEAM_CODE_TO_WIZARD_CODE at wizardErrors.ts:1936 recognises them):
   CIRCUIT_OPEN (breaker open, nothing sent), UPSTREAM_TIMEOUT (our analytics
   deadline fired), UPSTREAM_NETWORK_ERROR (transport failure on our hop).
3. Forwarded upstream arms preserve the upstream's own code:
   `AnalyticsUpstreamError.seamCode: string | null` (analytics-client.ts:119).
   Fallback when null: "UNKNOWN". Never assert UPSTREAM_NETWORK_ERROR on an arm
   where the upstream ANSWERED — that claims a transport fault not observed
   (the phase's copy-that-asserts-something-false class).
4. Stable NON-union cross-route tokens minted by this plan set, per the
   keys/sync template's own non-union precedent (MISSING_STRATEGY_ID etc.):
   - UNAUTHENTICATED — every inline 401 no-session arm (the honest fact is "no
     authenticated session on this request"; SESSION_EXPIRED would assert an
     expiry not observed).
   - PORTFOLIO_NOT_FOUND — the 404 portfolio arm; simulator and bridge (G6)
     share this SAME token — one fact, one token across routes, exactly like
     GATE_DRAFT_GONE across keys/sync + finalize-wizard.
   Use these spellings EXACTLY; a synonym in either plan is a defect.
   Justification for non-union: WizardErrorCode is the wizard COPY table
   (Record<WizardErrorCode, WizardErrorCopy> — each member forces user-facing
   copy + the EXPECTED_TABLE_SIZE=55 guard + a TRAP-4 scan). These arms are not
   wizard-rendered; the template route itself ships four non-union tokens.

Shared-helper arms are OUT OF SCOPE: `withAuth`'s 401 (withAuth.ts:59) and
`assertProfileApproved`'s 403 (approval-gate.ts:39) are emitted by helpers
shared across the whole API surface (73+ routes). SEAMUX-03's adjudicated class
is arms the ROUTE FILE emits — the verifier accepted keys/sync as complete with
withAuth's 401 untouched. Note each excluded helper arm in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Code every error arm of /api/scenario/optimize</name>
  <read_first>
    src/app/api/scenario/optimize/route.ts (whole file — enumerate ALL NextResponse.json sites first),
    src/app/api/keys/sync/route.ts (THE TEMPLATE),
    src/lib/wizardErrors.ts:37-268 + :1936-1975 (union + seam code map),
    src/lib/analytics-client.ts:106-130 (AnalyticsUpstreamError.seamCode),
    src/app/api/scenario/optimize/route.test.ts (existing test shape)
  </read_first>
  <files>src/app/api/scenario/optimize/route.ts, src/app/api/scenario/optimize/route.test.ts</files>
  <behavior>
    - 401 arm answers code "UNAUTHENTICATED"; all six 400 input arms answer "VALIDATION_FAILED"
    - Breaker 503 answers "CIRCUIT_OPEN" (sentence + Retry-After byte-unchanged); 504 answers "UPSTREAM_TIMEOUT"
    - The 502 AnalyticsUpstreamError arm answers `err.seamCode ?? "UNKNOWN"`; the terminal 500 answers "UNKNOWN"
  </behavior>
  <action>
    ENUMERATE BEFORE FIXING: list every NextResponse.json call with line
    numbers in the SUMMARY, classify success/error/passthrough, then assign.
    Floor: 11 own error arms (VERIFICATION measured 12 bare shapes; one is the
    helper-emitted approval 403, which is excluded — say so).

    Mapping:
    - :75 401 → `code: "UNAUTHENTICATED"`.
    - :91 invalid JSON, :99 objective, :107 series-shape, :114 series-count,
      :125 per-series, :139 malformed-point → all `code: "VALIDATION_FAILED"`
      (request-shape rejections; the prose already names the offending field —
      the code names the fact class, exactly as the union's own docblock
      defines it).
    - :196 breaker 503 → `code: "CIRCUIT_OPEN"` added beside the
      CIRCUIT_OPEN_COPY sentence; status, sentence, Retry-After header
      byte-unchanged (SEAMUX-01's one-source-of-truth copy leaf must not be
      touched).
    - :208 504 → `code: "UPSTREAM_TIMEOUT"`.
    - :259 502 "The optimizer is unavailable right now." → `code: err.seamCode
      ?? "UNKNOWN"` (preserve the upstream's machine code; the sentence stays —
      140.3-13b's Sentry capture on this arm is untouched).
    - :275 terminal 500 → `code: "UNKNOWN"`.

    ADDITIVE ONLY — no status, sentence, header, or key-order changes; match
    the template's `{ error, code }` key order. Then extend route.test.ts with
    a per-arm `describe("SEAMUX-03 — a machine code on every arm")` block in
    the file's existing mock shape, including one case proving the 502 arm
    forwards a seamCode-bearing AnalyticsUpstreamError's code verbatim.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/scenario/optimize && test $(grep -vE '^\s*(//|\*)' src/app/api/scenario/optimize/route.ts | grep -cE '(^|[^a-zA-Z_])code:') -ge 11</automated>
  </verify>
  <acceptance_criteria>
    scenario/optimize/route.ts: every error body the route emits has a `code:`;
    comment-stripped `code:` count >= 11 (was 0); route.test.ts asserts
    UNAUTHENTICATED on the 401, VALIDATION_FAILED on the six 400s, CIRCUIT_OPEN
    on the breaker 503, UPSTREAM_TIMEOUT on the 504, seamCode-forwarding on the
    502, UNKNOWN on the terminal 500; `npx vitest run
    src/app/api/scenario/optimize` exits 0; `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Code every error arm of /api/simulator</name>
  <read_first>
    src/app/api/simulator/route.ts (whole file — enumerate first),
    src/app/api/keys/sync/route.ts (template),
    src/lib/analytics-client.ts:106-130,
    src/app/api/simulator/route.test.ts (existing test shape)
  </read_first>
  <files>src/app/api/simulator/route.ts, src/app/api/simulator/route.test.ts</files>
  <behavior>
    - 401 answers "UNAUTHENTICATED"; both 400 input arms answer "VALIDATION_FAILED"
    - Limiter-misconfigured 503 answers "SEAM_MISCONFIGURED"; the 429 answers "RATE_LIMITED" (retryAfter body field kept)
    - 404 answers "PORTFOLIO_NOT_FOUND"; breaker 503 answers "CIRCUIT_OPEN"; 4xx forward answers `err.seamCode ?? "UNKNOWN"`; 504 answers "UPSTREAM_TIMEOUT"; terminal 500 answers "UNKNOWN"
  </behavior>
  <action>
    Enumerate all arms with line numbers first (floor: 10 own arms; the
    approval-gate 403 is helper-emitted — excluded, note it).

    Mapping:
    - :74 401 → "UNAUTHENTICATED".
    - :91 invalid JSON, :99 schema parse → "VALIDATION_FAILED".
    - :117 limiter-misconfigured 503 → "SEAM_MISCONFIGURED" (union member;
      keys/sync uses it for the identical fact). Keep the Retry-After header.
    - :128 429 → "RATE_LIMITED" (the union's "OUR limiter" token; NOT
      KEY_RATE_LIMIT — this is not the key-connect family). Keep the
      `retryAfter` body field byte-unchanged.
    - :155 404 "Portfolio not found" → "PORTFOLIO_NOT_FOUND" (cross-route token
      shared with bridge in plan G6 — spell it exactly).
    - :188 breaker 503 → "CIRCUIT_OPEN" beside the untouched CIRCUIT_OPEN_COPY
      sentence + Retry-After.
    - :205 4xx forward (`err.message`, upstream status — the operator-curated
      Python detail) → `code: err.seamCode ?? "UNKNOWN"`; message and status
      forwarding unchanged.
    - :212 504 → "UPSTREAM_TIMEOUT".
    - :226 terminal 500 → "UNKNOWN" (Sentry capture untouched).

    ADDITIVE ONLY. Extend route.test.ts with per-arm code assertions in its
    existing shape, incl. the seamCode-forwarding case and one asserting the
    429 body still carries `retryAfter`.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/simulator && test $(grep -vE '^\s*(//|\*)' src/app/api/simulator/route.ts | grep -cE '(^|[^a-zA-Z_])code:') -ge 10</automated>
  </verify>
  <acceptance_criteria>
    simulator/route.ts: zero bare `{ error }` arms remain; comment-stripped
    `code:` count >= 10 (was 0); route.test.ts asserts the exact code per arm
    listed above; `npx vitest run src/app/api/simulator` exits 0; `npx tsc
    --noEmit` clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed client → both routes | untrusted JSON bodies; compute-expensive analytics round-trips behind limiters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.3-G5-01 | Information disclosure | new `code` tokens | mitigate | closed-set stable tokens; none names env vars/hosts; seamCode forwarding forwards only what the body already forwards |
| T-140.3-G5-02 | DoS | 429/503 limiter arms | mitigate | limiter behaviour untouched — additive code only; Retry-After and retryAfter fields byte-kept |
| T-140.3-G5-03 | Information disclosure | PORTFOLIO_NOT_FOUND on 404 | accept | the 404 already discloses non-existence-or-unowned uniformly; the code adds no asymmetry |
| T-140.3-G5-SC | Tampering | npm/pip installs | mitigate | ZERO packages installed by this plan set |
</threat_model>

<verification>
- `npx vitest run src/app/api/scenario/optimize src/app/api/simulator` exits 0
- `npx tsc --noEmit` exits 0; `npm run lint` — 0 new errors
- Both routes leave 140.3-VERIFICATION §3.1's zero-code set under its own re-run command
</verification>

<success_criteria>
Both routes emit a top-level `code` on every arm they own; forwarded upstream
codes survive verbatim; per-arm tests redden if any code is dropped; no
behaviour change beyond the additive field.
</success_criteria>

<output>
Create `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-G5-SUMMARY.md`
with the full arm inventory (line, status, code) for both routes and the named
helper-arm exclusions.
</output>
