---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: G8
subsystem: api
tags: [seam, error-surface, machine-code, admin-match, nextjs, route-handler, SEAMUX-03]

# Dependency graph
requires:
  - phase: 140.3 (G5/G6, 140.3-10 keys/sync template)
    provides: the BINDING code vocabulary (VALIDATION_FAILED, SEAM_MISCONFIGURED, RATE_LIMITED, UNKNOWN, CIRCUIT_OPEN, UPSTREAM_TIMEOUT, UNAUTHENTICATED, FORBIDDEN, MISSING_STRATEGY_ID precedent) and the rateLimitDenyJson override pattern
  - phase: 140.3-11 (TS-19/TS-18)
    provides: AnalyticsUpstreamError.seamCode + .dependency preserved on the 4xx-forward arm
provides:
  - "admin/match/eval: 6 coded error arms (was 0)"
  - "admin/match/recompute: 10 coded error arms incl. 2 coded deny bodies (was 0)"
  - "the TS-19 4xx-forward arms now carry the upstream's own seamCode with dependency intact"
affects: [140.3-VERIFICATION, SEAMUX-03 aggregate, remaining seam-importing routes still bare]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Machine `code` on every route-owned error arm; inline non-union tokens (UNAUTHENTICATED/FORBIDDEN/MISSING_ALLOCATOR_ID) so admin arms never force wizard COPY"
    - "Breaker-vocab leak assertion scoped to `.error` (human copy), NOT the raw body — the machine `code: CIRCUIT_OPEN` is an exempt stable discriminator"

key-files:
  created: []
  modified:
    - src/app/api/admin/match/eval/route.ts
    - src/app/api/admin/match/eval/route.test.ts
    - src/app/api/admin/match/recompute/route.ts
    - src/app/api/admin/match/recompute/route.test.ts
    - src/app/api/admin/match/recompute/route.seam.test.ts

key-decisions:
  - "MISSING_ALLOCATOR_ID (named-id param, keys/sync MISSING_STRATEGY_ID precedent) kept DISTINCT from VALIDATION_FAILED (structural unparseable-JSON rejection) — a test drives both so a collapse reddens"
  - "err.seamCode ?? UNKNOWN on the 4xx forward; error + dependency byte-unchanged (TS-18/TS-19)"
  - "Deny bodies overridden with codes, builder-default SENTENCES byte-kept (RATE_LIMITED / SEAM_MISCONFIGURED)"

patterns-established:
  - "Pattern: breaker-vocab regex assertion belongs on `.error`, not the raw body, once a CIRCUIT_OPEN machine code is on the wire"

requirements-completed: []  # SEAMUX-03 is an aggregate across 9 routes — NOT marked complete by this plan (two routes only)

# Metrics
duration: 15min
completed: 2026-07-31
---

# Phase 140.3 Plan G8: Admin Match Routes Typed `{code}` Envelope Summary

**Every error arm the two admin match routes (`/api/admin/match/eval`, `/api/admin/match/recompute`) themselves emit now carries a top-level machine `code`; the TS-19 4xx-forward arms carry the upstream's own `seamCode` with `dependency` intact (SEAMUX-03, two of the nine routes).**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-07-31T06:07:00Z
- **Completed:** 2026-07-31T06:13:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- `admin/match/eval`: 6 coded arms (comment-stripped `code:` count 0 → 6)
- `admin/match/recompute`: 10 coded arms incl. 2 coded deny bodies (0 → 10)
- Per-arm `body.code` tests added to both route.test.ts files; seamCode+dependency survival pinned on the 4xx forward
- `npx vitest run src/app/api/admin/match` → 164 passed (9 files); `npx tsc --noEmit` clean; eslint on the 5 changed files clean

## Arm Inventory

### `admin/match/eval/route.ts` (6 arms)

| Line (orig) | Arm | Status | Code |
|-------------|-----|--------|------|
| 137 | inline 401 "Unauthorized" | coded | `UNAUTHENTICATED` |
| 140 | inline 403 "Forbidden" | coded | `FORBIDDEN` |
| 177 | breaker 503 (CIRCUIT_OPEN_COPY + Retry-After, inside handler / after gate — T-140-12) | coded | `CIRCUIT_OPEN` |
| 195 | 504 TIMEOUT_COPY | coded | `UPSTREAM_TIMEOUT` |
| 245 | 4xx forward (TS-19; error + dependency byte-unchanged) | coded | `err.seamCode ?? "UNKNOWN"` |
| 270 | terminal 500 GENERIC_COPY | coded | `UNKNOWN` |

Floor was 5; the VERIFICATION "6 bare shapes" resolved to 6 genuine own arms (no destructuring false-positive). No helper-owned arms in this route.

### `admin/match/recompute/route.ts` (10 `code:` occurrences across 9 arms)

| Line (orig) | Arm | Status | Code |
|-------------|-----|--------|------|
| 71 | inline 401 "Unauthorized" | coded | `UNAUTHENTICATED` |
| 74 | inline 403 "Forbidden" | coded | `FORBIDDEN` |
| 81 | 400 "Invalid request body" (unparseable JSON — structural) | coded | `VALIDATION_FAILED` |
| 85 | 400 "allocator_id is required" (named-id param) | coded | `MISSING_ALLOCATOR_ID` |
| 96 | `rateLimitDenyJson` throttled body (sentence byte-kept) | coded | `RATE_LIMITED` |
| 96 | `rateLimitDenyJson` misconfigured body (sentence byte-kept) | coded | `SEAM_MISCONFIGURED` |
| 128 | breaker 503 (inside handler / after gate — T-140-12) | coded | `CIRCUIT_OPEN` |
| 146 | 504 TIMEOUT_COPY | coded | `UPSTREAM_TIMEOUT` |
| 193 | 4xx forward (TS-19; error + dependency byte-unchanged) | coded | `err.seamCode ?? "UNKNOWN"` |
| 219 | terminal 500 GENERIC_COPY | coded | `UNKNOWN` |

Floor was 8; the deny expands into two builder bodies (both coded), so the comment-stripped `code:` count is 10. The 503-vs-429 decision stays the chokepoint builder's — only the bodies gained codes.

## Task Commits

1. **Task 1: Code every error arm of `/api/admin/match/eval`** — `44e98284` (feat)
2. **Task 2: Code every error arm of `/api/admin/match/recompute`** — `7203b4ca` (feat)

_Both tasks were `tdd="true"`: the failing case was observed first (Task 1's breaker-vocab conflict surfaced a RED before the fix landed), then implemented green in a single per-task commit._

## Files Created/Modified
- `src/app/api/admin/match/eval/route.ts` — 6 arms coded; 4xx forward gains `code: err.seamCode ?? "UNKNOWN"`
- `src/app/api/admin/match/eval/route.test.ts` — new `[140.3-G8 / SEAMUX-03]` describe with per-arm `body.code` assertions incl. seamCode+dependency survival; breaker-vocab assertion narrowed to `.error`
- `src/app/api/admin/match/recompute/route.ts` — 9 arms coded (10 `code:`), incl. coded deny bodies
- `src/app/api/admin/match/recompute/route.test.ts` — per-arm `body.code` assertions incl. both deny bodies + MISSING_ALLOCATOR_ID-vs-VALIDATION_FAILED distinction; SEAMRIM-05 deny `toEqual`s updated to include codes
- `src/app/api/admin/match/recompute/route.seam.test.ts` — breaker-vocab assertion narrowed to `.error`

## Decisions Made
- **MISSING_ALLOCATOR_ID vs VALIDATION_FAILED kept distinct** (per plan): the named-id fact and the structural-rejection fact are two different things; a test asserts `body.code` is `MISSING_ALLOCATOR_ID` and `not.toBe("VALIDATION_FAILED")` so a future collapse reddens.
- **Non-union tokens for gate/named-id arms** (per plan): admin-only arms must not force wizard COPY (WizardErrorCode is the wizard copy table's key space); the keys/sync template already ships non-union tokens.
- **T-140-12 ordering preserved**: no breaker-aware branch hoisted above the admin gate; the 401/403 arms carry only the auth verdict. Pinned by the existing unauthenticated+open-breaker cases.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Pre-existing breaker-vocab assertion matched the whole raw body, not the human copy**
- **Found during:** Task 1 (eval), recurred in Task 2 (recompute route.test + route.seam.test)
- **Issue:** Three tests asserted `expect(raw).not.toMatch(/circuit|breaker|upstash|railway/i)` against the ENTIRE JSON body. Adding the plan's BINDING wire token `code: "CIRCUIT_OPEN"` (which contains "circuit") broke them — a genuine plan-vs-existing-test conflict.
- **Fix:** Narrowed each assertion to the parsed `.error` (the human-facing COPY), which still must never leak breaker vocabulary, while the machine `code` legitimately rides `.code`. This matches the already-shipped sibling `src/app/api/scenario/optimize/route.test.ts`, which scopes the identical assertion to `body.error`. The threat (T-140-05 / T-140-08 — breaker internals off the wire) is intact for the prose; the stable discriminator is exempt by design.
- **Files modified:** eval/route.test.ts, recompute/route.test.ts, recompute/route.seam.test.ts
- **Verification:** `npx vitest run src/app/api/admin/match` → 164 passed
- **Committed in:** `44e98284` (eval), `7203b4ca` (recompute + seam)

**2. [Rule 1 - Bug] SEAMRIM-05 deny-body `toEqual` assertions broke once the deny bodies gained codes**
- **Found during:** Task 2
- **Issue:** Two existing exact-body assertions (`toEqual({ error: "Rate limiter unavailable" })` and `toEqual({ error: "Too many requests" })`) could not survive the additive `code` field the plan mandates on the deny bodies.
- **Fix:** Extended both to `{ error, code }` with the sentence BYTE-KEPT (SEAM_MISCONFIGURED / RATE_LIMITED). This is the intended consequence of the plan's deny-body override, not new behavior.
- **Files modified:** recompute/route.test.ts
- **Verification:** SEAMRIM-05 describe passes (byte-kept sentence + code + Retry-After + Cache-Control).
- **Committed in:** `7203b4ca`

---

**Total deviations:** 2 auto-fixed (both Rule 1 — pre-existing test assertions incompatible with the plan's additive `code`).
**Impact on plan:** No production behavior changed beyond the mandated additive `code` fields. The plan's note "leave route.seam.test.ts alone unless a seam-arm assertion naturally belongs there" was honored in spirit — the seam test was touched ONLY to repair an assertion the plan's own CIRCUIT_OPEN code broke, not to add coverage. No scope creep.

## Plan Defect Noted
- The plan's inventory (Task 1) hedged that VERIFICATION's "6 bare shapes" might include "a destructuring false-positive". The settled inventory is **6 genuine own arms** in eval — no false positive. Recorded, no action needed.
- The plan did not anticipate that adding `code: "CIRCUIT_OPEN"` would collide with three pre-existing whole-body `not.toMatch(/circuit.../)` assertions (including one in `route.seam.test.ts`, which the plan asked to leave alone). Handled as Rule 1 above; the sibling scenario/optimize route had already established the correct `.error`-scoped form.

## Issues Encountered
- One RED observed during Task 1 (the breaker-vocab whole-body assertion) — resolved by scoping to `.error` per the established sibling pattern.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Two of the nine SEAMUX-03 routes now fully coded. **SEAMUX-03 is NOT marked complete** — it is an aggregate across all nine seam-importing routes; the remaining routes are out of this plan's scope.
- T-140-12 ordering and TS-18/TS-19 `dependency` preservation both intact and test-pinned, so no regression risk carried forward.

## Self-Check: PASSED

- SUMMARY.md present at expected path
- Task commits `44e98284`, `7203b4ca` and SUMMARY commit `c5d1cb3f` all present in git log
- Both modified route files present on disk

---
*Phase: 140.3-seamux-client-wizard-seam-error-surface*
*Completed: 2026-07-31*
