---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: G8
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [SEAMUX-03]
files_modified:
  - src/app/api/admin/match/eval/route.ts
  - src/app/api/admin/match/eval/route.test.ts
  - src/app/api/admin/match/recompute/route.ts
  - src/app/api/admin/match/recompute/route.test.ts

must_haves:
  truths:
    - "Every error arm the two admin match routes themselves emit carries a top-level machine `code` — REQUIREMENTS.md names 'the admin match routes' verbatim in SEAMUX-03."
    - "The TS-19 4xx-forward arms preserve the upstream's own machine code (err.seamCode — this is the arm where the Python flat-424 code rides) and keep the `dependency` field byte-unchanged."
    - "recompute's rateLimitDenyJson deny bodies gain codes with the builder-default sentences byte-kept."
  artifacts:
    - path: "src/app/api/admin/match/eval/route.ts"
      provides: "5+ coded error arms"
      contains: 'code: "CIRCUIT_OPEN"'
    - path: "src/app/api/admin/match/recompute/route.ts"
      provides: "8+ coded error arms incl. coded deny bodies"
      contains: 'code: "MISSING_ALLOCATOR_ID"'
    - path: "src/app/api/admin/match/eval/route.test.ts"
      provides: "Per-arm body.code assertions"
    - path: "src/app/api/admin/match/recompute/route.test.ts"
      provides: "Per-arm body.code assertions"
  key_links:
    - from: "src/app/api/admin/match/eval/route.ts"
      to: "src/lib/analytics-client.ts"
      via: "err.seamCode + err.dependency forwarded together on the 4xx arm"
      pattern: "seamCode"
    - from: "src/app/api/admin/match/recompute/route.test.ts"
      to: "src/app/api/admin/match/recompute/route.ts"
      via: "expect(body.code) per arm"
      pattern: "code"
---

<objective>
Close the SEAMUX-03 gap on the two admin match routes: `/api/admin/match/eval`
(0/6 coded at HEAD) and `/api/admin/match/recompute` (0/9). Near-twin routes
(same catch-arm anatomy, same TS-19 upstream-4xx forwarding with `dependency`),
so one task each with a shared vocabulary.

The 4xx-forward arms matter most here: 140.3-11 (TS-19) made these routes stop
FLATTENING upstream 4xx bodies, and `AnalyticsUpstreamError.seamCode` carries
the Python machine code (e.g. the flat-424 venue-transient codes) precisely so
a consumer can read it. Putting `code: err.seamCode ?? "UNKNOWN"` on that arm
completes the thread TS-19 started.

Purpose: admin consumers discriminate on stable tokens (SEAMUX-03).
Output: two routes with coded arms + per-arm test assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VERIFICATION.md
@src/app/api/keys/sync/route.ts
@src/lib/wizardErrors.ts
@src/lib/envelope.ts

<interfaces>
CODE VOCABULARY — identical to G5/G6 (BINDING; do not re-derive):
1. Union members: VALIDATION_FAILED (request-shape rejection),
   SEAM_MISCONFIGURED (limiter misconfiguration), RATE_LIMITED (our limiter),
   UNKNOWN (terminal fallback).
2. Established seam WIRE tokens: CIRCUIT_OPEN, UPSTREAM_TIMEOUT.
3. Forwarded upstream arms: `err.seamCode ?? "UNKNOWN"`
   (AnalyticsUpstreamError.seamCode, analytics-client.ts:119); the `dependency`
   field stays byte-unchanged beside it.
4. Cross-route non-union tokens shared across this plan set — spell EXACTLY:
   UNAUTHENTICATED (inline 401), FORBIDDEN (inline 403 admin-gate refusal).
   New in this plan: MISSING_ALLOCATOR_ID (named-id-param, keys/sync
   MISSING_STRATEGY_ID precedent).
   Justification for non-union: WizardErrorCode is the wizard COPY table's key
   space (each member forces user-facing copy + EXPECTED_TABLE_SIZE=55 bump +
   TRAP-4 scan); admin-only arms must not force wizard copy; the keys/sync
   template itself ships four non-union tokens.

`rateLimitDenyJson` defaults are codeless (ratelimit.ts:368-383); override with
throttledBody { error: "Too many requests", code: "RATE_LIMITED" } +
misconfiguredBody { error: "Rate limiter unavailable", code:
"SEAM_MISCONFIGURED" } — sentences byte-kept.

⚠️ T-140-12 (BINDING, from both routes' own comments): every breaker-aware
branch lives INSIDE the handler, AFTER the admin gate. Do not hoist anything
above the gate — a pre-gate breaker branch turns "is Railway degraded?" into an
unauthenticated oracle.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Code every error arm of /api/admin/match/eval</name>
  <read_first>
    src/app/api/admin/match/eval/route.ts (whole file — enumerate ALL NextResponse.json sites first; note the T-140-12 comment),
    src/app/api/keys/sync/route.ts (THE TEMPLATE),
    src/lib/analytics-client.ts:106-130 (seamCode + dependency),
    src/app/api/admin/match/eval/route.test.ts (existing test shape)
  </read_first>
  <files>src/app/api/admin/match/eval/route.ts, src/app/api/admin/match/eval/route.test.ts</files>
  <behavior>
    - 401 answers "UNAUTHENTICATED"; 403 answers "FORBIDDEN"
    - Breaker 503 answers "CIRCUIT_OPEN"; 504 answers "UPSTREAM_TIMEOUT"; terminal 500 answers "UNKNOWN"
    - The 4xx forward answers `{ error: err.message, dependency: err.dependency, code: err.seamCode ?? "UNKNOWN" }` — dependency untouched
  </behavior>
  <action>
    Enumerate all arms with line numbers first (floor: 5 own arms; VERIFICATION
    measured 6 bare shapes — one may be a destructuring false-positive; the
    inventory settles it).

    Mapping:
    - :137 401 "Unauthorized" → "UNAUTHENTICATED".
    - :140 403 "Forbidden" → "FORBIDDEN".
    - :177 breaker 503 → "CIRCUIT_OPEN" beside the untouched CIRCUIT_OPEN_COPY
      sentence + Retry-After. Position INSIDE the handler stays (T-140-12).
    - :195 504 TIMEOUT_COPY → "UPSTREAM_TIMEOUT".
    - :245 4xx forward → add `code: err.seamCode ?? "UNKNOWN"`; `error` and
      `dependency` byte-unchanged (TS-18/TS-19 contract: dependency names the
      caller's venue on the nested 424 shape and is null otherwise).
    - :270 terminal 500 GENERIC_COPY → "UNKNOWN" (the only capture in the
      route — untouched).

    ADDITIVE ONLY. Extend route.test.ts with per-arm code assertions in its
    existing shape; the 4xx case must drive an AnalyticsUpstreamError carrying
    a seamCode AND a dependency and assert all three fields survive together.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/match/eval && test $(grep -vE '^\s*(//|\*)' src/app/api/admin/match/eval/route.ts | grep -cE '(^|[^a-zA-Z_])code:') -ge 5</automated>
  </verify>
  <acceptance_criteria>
    admin/match/eval/route.ts: zero bare error arms remain; comment-stripped
    `code:` count >= 5 (was 0); route.test.ts asserts UNAUTHENTICATED/FORBIDDEN
    on the gate arms, CIRCUIT_OPEN, UPSTREAM_TIMEOUT, seamCode+dependency
    survival on the 4xx forward, UNKNOWN on the terminal; `npx vitest run
    src/app/api/admin/match/eval` exits 0; `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Code every error arm of /api/admin/match/recompute</name>
  <read_first>
    src/app/api/admin/match/recompute/route.ts (whole file — enumerate first),
    src/app/api/keys/sync/route.ts:134-158 (deny override template),
    src/app/api/admin/match/recompute/route.test.ts and route.seam.test.ts (existing test shapes)
  </read_first>
  <files>src/app/api/admin/match/recompute/route.ts, src/app/api/admin/match/recompute/route.test.ts</files>
  <behavior>
    - 401 answers "UNAUTHENTICATED"; 403 answers "FORBIDDEN"; invalid-JSON 400 answers "VALIDATION_FAILED"; missing allocator_id answers "MISSING_ALLOCATOR_ID"
    - Deny bodies answer RATE_LIMITED (429) / SEAM_MISCONFIGURED (503), sentences byte-kept
    - Breaker → "CIRCUIT_OPEN"; 504 → "UPSTREAM_TIMEOUT"; 4xx forward → `err.seamCode ?? "UNKNOWN"` with dependency kept; terminal 500 → "UNKNOWN"
  </behavior>
  <action>
    Enumerate all arms with line numbers first (floor: 8 own arms; the deny
    expands to two bodies — VERIFICATION's 9 bare shapes will resolve in the
    inventory).

    Mapping:
    - :71 401 → "UNAUTHENTICATED"; :74 403 → "FORBIDDEN".
    - :81 "Invalid request body" 400 → "VALIDATION_FAILED".
    - :85 "allocator_id is required" 400 → "MISSING_ALLOCATOR_ID" (named-id
      param — the keys/sync MISSING_STRATEGY_ID precedent, NOT
      VALIDATION_FAILED which this plan set reserves for structural
      rejections).
    - the `rateLimitDenyJson(rl, { headers: NO_STORE_HEADERS })` deny (~:96):
      add throttledBody { error: "Too many requests", code: "RATE_LIMITED" } +
      misconfiguredBody { error: "Rate limiter unavailable", code:
      "SEAM_MISCONFIGURED" } (builder-default sentences byte-kept; RATE_LIMITED
      not KEY_RATE_LIMIT — not the key-connect family).
    - :128 breaker 503 → "CIRCUIT_OPEN" (inside the handler, after the gate —
      T-140-12). :146 504 → "UPSTREAM_TIMEOUT".
    - :193 4xx forward → `code: err.seamCode ?? "UNKNOWN"`; error + dependency
      byte-unchanged.
    - :219 terminal 500 → "UNKNOWN".

    ADDITIVE ONLY. Extend route.test.ts (leave route.seam.test.ts alone unless
    a seam-arm assertion naturally belongs there — match each file's existing
    concern split) with per-arm code assertions incl. both deny bodies.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/match/recompute && test $(grep -vE '^\s*(//|\*)' src/app/api/admin/match/recompute/route.ts | grep -cE '(^|[^a-zA-Z_])code:') -ge 8</automated>
  </verify>
  <acceptance_criteria>
    admin/match/recompute/route.ts: zero bare error arms remain;
    comment-stripped `code:` count >= 8 (was 0); route.test.ts asserts the
    exact code per arm incl. both deny bodies and MISSING_ALLOCATOR_ID; `npx
    vitest run src/app/api/admin/match/recompute` exits 0; `npx tsc --noEmit`
    clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed client → admin gate | non-admin callers must learn nothing beyond 401/403 |
| admin → analytics service | TS-19 forwards upstream 4xx bodies to admins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.3-G8-01 | Information disclosure | pre-gate oracle (T-140-12) | mitigate | all breaker-aware codes stay INSIDE the handler after the admin gate; 401/403 arms carry only UNAUTHENTICATED/FORBIDDEN — no seam state |
| T-140.3-G8-02 | Information disclosure | seamCode on 4xx forward | accept | admin-only surface; the code is read off a body already forwarded to admins by TS-19 |
| T-140.3-G8-03 | DoS | deny overrides | mitigate | limiter semantics untouched; builder decides 429-vs-503; sentences byte-kept |
| T-140.3-G8-SC | Tampering | npm/pip installs | mitigate | ZERO packages installed by this plan set |
</threat_model>

<verification>
- `npx vitest run src/app/api/admin/match/eval src/app/api/admin/match/recompute` exits 0
- `npx tsc --noEmit` exits 0; `npm run lint` — 0 new errors
- Both routes leave 140.3-VERIFICATION §3.1's zero-code set under its own re-run command
</verification>

<success_criteria>
Both admin match routes emit a top-level `code` on every arm they own; the
TS-19 forward arms carry the upstream's own code with dependency intact;
per-arm tests redden on any dropped code; nothing moved above the admin gate.
</success_criteria>

<output>
Create `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-G8-SUMMARY.md`
with the full arm inventory (line, status, code) for both routes.
</output>
