---
phase: 140.3-seamux-client-wizard-seam-error-surface
verified: 2026-07-28T11:06:02Z
verifier_head: e93044c1
status: gaps_found
score: 15/16 must-haves verified (7/7 ROADMAP success criteria · 8/9 SEAMUX requirements)
overrides_applied: 0
re_verification:
  previous_status: null
  note: "Initial verification — no prior VERIFICATION.md existed in the phase directory."
gaps:
  - truth: "SEAMUX-03 — every seam-touching route answers with the repo's typed error envelope carrying a `code`, on every arm"
    status: failed
    reason: >-
      Independently re-measured at HEAD and CONFIRMED OPEN. 9 of the 15 seam-importing
      routes carry ZERO code-bearing arms. The gate's receipt is accurate, name for name.
      This is NOT measured by any of the 7 success criteria, so a green criteria table
      does not imply this requirement was delivered.
    artifacts:
      - path: "src/app/api/verify-strategy/route.ts"
        issue: "0 `code:` arms / 11 bare `{ error: … }` shapes"
      - path: "src/app/api/admin/match/eval/route.ts"
        issue: "0 / 6"
      - path: "src/app/api/admin/match/recompute/route.ts"
        issue: "0 / 9"
      - path: "src/app/api/scenario/optimize/route.ts"
        issue: "0 / 12"
      - path: "src/app/api/simulator/route.ts"
        issue: "0 / 8"
      - path: "src/app/api/bridge/route.ts"
        issue: "0 / 7"
      - path: "src/app/api/portfolio-optimizer/route.ts"
        issue: "0 / 5"
      - path: "src/app/api/keys/validate-and-encrypt/route.ts"
        issue: "0 / 10"
      - path: "src/app/api/strategies/csv-validate/route.ts"
        issue: "0 / 1"
    missing:
      - "A deliberate arm-by-arm pass over the 9 zero-code routes, in the shape 140.3-10 used on keys/sync (which turned out to be 8 arms, not the 5 every source document listed)."
      - "REQUIREMENTS.md explicitly names the public teaser, the CSV routes and the admin match routes — all are among the nine."
  - truth: "The poll-disjointness negative pin can fail when the retry-storm hazard it exists to prevent is introduced"
    status: failed
    reason: >-
      NEW — no SUMMARY, ledger row or docblock mentions this. `src/lib/seam-poll-disjointness.pin.test.ts`
      detects seam calls with the regex /\bfetch\s*\(/, but the file on its own roster
      (`SyncPreviewStep.tsx`) makes every network call through `wizardFetch(` — a thin
      `fetch` wrapper in `src/lib/wizard/wizard-correlation.ts:58`. The regex cannot see it.
      Proven in memory without touching the tree: all three of the pin's predicates return
      IDENTICAL values on HEAD and on a mutant that retargets the repeating poll tick at a
      live seam route. The pin stays fully GREEN under the exact mutation it was written to catch.
      The underlying SAFETY PROPERTY still holds at HEAD (the tick's real target,
      `/api/strategies/[id]/sync-progress`, imports no seam module) — but the guard is vacuous.
    artifacts:
      - path: "src/lib/seam-poll-disjointness.pin.test.ts"
        issue: "The `ZERO fetch calls exist on the poll path` case passes on a tree where a roster file makes 3 network calls, 2 of them to the seam route /api/keys/sync. Unlike its sibling SSR pin, it has no `the regex matches an edge and NOT a prose mention` self-test for its own needle."
      - path: ".planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-16-SUMMARY.md"
        issue: "States the poll chain `contains zero fetch( calls of any kind. Every read is @/lib/supabase/client.` — factually false for SyncPreviewStep.tsx, which fires a fire-and-forget wizardFetch ON the tick at line 745."
      - path: ".planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md"
        issue: "M75c mutated with a literal `fetch(\"/api/keys/sync\")` in useStrategySyncPoller.ts — a spelling this codebase's poll path does not use — so the row proves the regex works on a shape that cannot occur, not on the shape that can."
    missing:
      - "Match the call edge (wizardFetch / any *Fetch wrapper), or match the seam ROUTE PATHS, not the identifier `fetch(`."
      - "A self-test in the SSR pin's shape: assert the needle matches a real call in this repo's own idiom and does NOT match a prose mention."
      - "Re-run M75c using `wizardFetch(` — the idiom SyncPreviewStep.tsx:745 already demonstrates is live on the tick."
  - truth: "SC2's residual is named, per the gate's own stated methodology"
    status: partial
    reason: >-
      NEW. `finalize-wizard/route.ts:1551` emits `code: \"COMPOSITE_UNSUPPORTED_UNIFIED\"` (409),
      which is NOT a member of `KNOWN_FINALIZE_CODES` in `SubmitStep.tsx` (9 members, measured).
      `wizardErrors.ts:1543` documents in production source that this code and `draft_state_invalid`
      "also reach SubmitStep without a wizard member". They therefore render as UNKNOWN and emit
      `wizard_error {code:"UNKNOWN"}` — touching SC2's recognition clause and SC6's specificity clause.
      The gate named residuals on SC4, SC6 and SC7 but marked SC2 unqualified ✅ SATISFIED.
      The finding is honestly recorded in-source and in the TS-35 ledger row; only the SC2
      adjudication omits it.
    artifacts:
      - path: "src/app/api/strategies/finalize-wizard/route.ts"
        issue: "Line 1551 emits COMPOSITE_UNSUPPORTED_UNIFIED; the unified backbone path is live in prod."
      - path: "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx"
        issue: "KNOWN_FINALIZE_CODES omits COMPOSITE_UNSUPPORTED_UNIFIED and draft_state_invalid."
    missing:
      - "Either admit both codes (with the DESTRUCTIVE_CONTROL coupling DEF-15-1 documents), or record the residual against SC2 the way SC4/SC6/SC7 record theirs."
deferred: []
human_verification:
  - test: "Read the ~12 authored/rewritten user-facing sentences in wizardErrors.ts, seam-copy.ts and process-key-client.ts against DESIGN.md §Voice."
    expected: "Declarative, sentence-case, active voice, no exclamation, no filler; limitation stated with its threshold."
    why_human: "Tests prove the false claims are gone; they cannot prove the replacements read well. Four sub-checks were discharged programmatically (see Partially Discharged below) — the prose-quality judgement was not."
  - test: "Start a composite draft, force the keys/sync 404 arm, click the error state's forward action FIVE times."
    expected: "The confirm dialog appears each time and the draft still exists."
    why_human: "ZERO e2e specs reference start_fresh, handleDeleteDraft or 'Try another key' (measured). Covered only by jsdom cases asserting the dialog's open attribute. Only a real flow proves a draft survives."
  - test: "Point ANALYTICS_SERVICE_URL at an unroutable host in preview, call any of the nine terminal-arm routes, confirm the issue lands in Sentry org metaworld-fund-ltd carrying that route's own `surface` tag."
    expected: "An issue arrives with e.g. surface: scenario-optimize, step: unexpected-error."
    why_human: "Requires a live DSN. The local suite fakes the @sentry/nextjs transport by design (which is what lets the REAL scrub run). ⚠️ The stock 'trip the breaker in preview' instruction is WRONG — breaker short-circuits capture nothing by design at all 15 routes."
---

# Phase 140.3: SEAMUX — Client & Wizard Seam Error Surface — Verification Report

**Phase Goal:** When the seam fails, every surface says something true, offers a way forward that isn't destructive, and tells us it happened.
**Verified:** 2026-07-28T11:06:02Z at HEAD `e93044c1`
**Status:** `gaps_found`
**Re-verification:** No — initial verification.

> ⚠️ **Every gate cited below is ADVISORY at merge.** Branch protection on `main` is OFF by a settled
> founder decision. Each gate **would have caught** a regression; **none of them did stop one**,
> because none of them can block anything. Not re-raised.

---

## 0. Method

Nothing in this report rests on a SUMMARY claim. Every number was re-measured at HEAD with a
re-runnable command, and for each grep I asked the phase's own question — *could a broken
implementation return this same number?* Where the answer was yes, the grep was replaced or
discarded. That discipline produced the one genuinely new defect in §5.

**Gates re-run independently by this verifier (not read from SUMMARY):**

| Gate | Command | Result | SUMMARY claimed | Match |
|---|---|---|---|---|
| Vitest full suite | `npm test` | **9779 passed \| 287 skipped** (714 files) | 9779 \| 287 | ✅ exact |
| TypeScript | `npx tsc --noEmit` | exit **0** | 0 | ✅ |
| ESLint + manifests | `npm run lint` | **0 errors, 1 warning** (`EquityChart.tsx`, pre-existing) | same | ✅ exact |
| mypy strict | `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` | **Success, 89 source files** | clean, 89 files | ✅ exact |
| pytest | `python3 -m pytest -q` | **4778 passed, 96 skipped** | 4778 / 96 | ✅ exact |
| The 3 seam pins | `npx vitest run src/lib/seam-{ssr-exposure,poll-disjointness,copy}.pin.test.ts` | **18 passed** (7 + 6 + 5) | +13 from the two new pins | ✅ |
| Packages installed | `git diff 59be65cb..HEAD -- package.json package-lock.json` | **empty** | ZERO | ✅ |
| e2e specs touched | `git diff --name-only 59be65cb..HEAD -- e2e/` | **empty** | untouched | ✅ |
| New debt markers | `git diff 59be65cb..HEAD \| grep '^+' \| grep -E '\b(FIXME\|XXX\|TBD)\b'` | **0** | — | ✅ |
| Gate's own scope | `git show --stat e93044c1` | **2 files, both `.ts` tests, +523/-0** | zero production behaviour changed | ✅ |

Phase base for all diffs: `59be65cb` (last commit before `e5784b19 test(140.3-01)`).
**Every headline gate figure in `140.3-16-SUMMARY.md` reproduced exactly.** That is unusual and worth
saying plainly: the receipts in this phase are real.

---

## 1. The 7 ROADMAP Success Criteria — each with a re-runnable receipt

Read verbatim from `.planning/ROADMAP.md` lines 212–218.

| # | Criterion (abridged) | Verdict | Receipt re-run at HEAD |
|---|---|---|---|
| **1** | Breaker open ⇒ every surface renders the breaker's own copy; no surface asserts work happened, or didn't, that the client cannot know | ✅ **VERIFIED** | `grep -rn "The analytics service is temporarily unavailable" --include='*.ts' --include='*.tsx' src/ \| grep -v '\.test\.'` → **exactly 1 hit**, `src/lib/seam-copy.ts:66`. `grep -rl 'from "@/lib/seam-copy"' src/ \| grep -v test` → **10 emitters**, 9 routes + `process-key-client.ts`. The four banned strings (`been notified`, `We fetched your trades`, `SUPABASE_SERVICE_ROLE_KEY`, `check your credentials`) survive in production source **only inside `//` comments explaining their removal** and in non-user-facing infra (`seam-redaction.ts` denylist, `supabase/admin.ts` env read) — hand-checked line by line, zero user-facing occurrences |
| **2** | Drift between any two production copies fails a test; a code emitted by a route is a code the classifier recognises | 🟡 **VERIFIED — with an UNNAMED residual (new, §5.3)** | Drift half: solid. One production declaration; `seam-copy.pin.test.ts` pins it with a hand-typed sentence and a hand-typed `EXPECTED_LEAF_CONSUMERS = 10`, plus 12 pre-existing independent route-test literals (13 files total, measured). Recognition half: `SEAM_CODE_TO_WIZARD_CODE` + `KNOWN_FINALIZE_CODES` (9 members) cover the seam's own codes — **but `finalize-wizard:1551` emits `COMPOSITE_UNSUPPORTED_UNIFIED`, which is in neither.** `wizardErrors.ts:1543` documents this in production source. Not falsified; not unqualified |
| **3** | Every seam call site fails on an unrecognised/unparseable body; an unrecognised 200 never starts a poll for a job never enqueued | ✅ **VERIFIED** | **5 of 5** `postProcessKey` consumers carry `if (!result.ok) return result.response` — `csv-validate:259`, `finalize-wizard:1595`, `csv-finalize:1199`, `keys/sync:544`, `verify-strategy:271`. Client side: `ApiKeyManager` (`res.ok` ×3), `AllocatorMatchQueue` (×3), `WeightOptimizerSection:160`. `SyncPreviewStep:594` fails CLOSED on the kickoff `!res.ok` before any poll starts |
| **4** | Recoverable error always offers retry; retry never the sole route to a destructive control (**TRAP-4**); `Retry-After` honoured for the breaker's 503, not only 429 | 🟡 **VERIFIED IN CODE — residual: never exercised by a real browser** | TRAP-4 guard is a genuine **table scan** — `wizardErrors.test.ts:1362` iterates `Object.entries(WIZARD_ERROR_COPY)`, backed by a hand-typed `EXPECTED_TABLE_SIZE = 55` whose docblock records the 53→54→55 trajectory **and states the reasoning was re-run before the number moved**. `WizardClient.tsx:728` — `handleStartFresh` now only calls `setConfirmDelete(true)`. 503: `PortfolioImpactPanel.tsx:157` reads the header **above** the `res.status === 429` branch and routes a non-429 wait into the same countdown state; `SyncPreviewStep:612` reads it status-agnostically. Plumbing end-to-end: `WizardErrorContext.retryAfterSeconds` → `envelope.ts:89` → `ErrorEnvelope.tsx:128`. 🔴 **Zero e2e specs reference the destructive path** (measured) |
| **5** | A publish/permission gate fed by a drifted analytics response fails CLOSED | ✅ **VERIFIED** | Member 1: `finalize-wizard:215` `LivePermissionsSchema.safeParse(await res.json())` → `PROBE_PARSE_MISS` on `!success`. Member 2 (the caching one C-5 found): `keys/[id]/permissions:330` `KeyPermissionsPayloadSchema.safeParse` sits **INSIDE** the `unstable_cache` callback with `{ revalidate: 60 }`, so an unvalidated verdict is never memoized. One schema declaration in `analytics-schemas.ts:363`, shared |
| **6** | Funnel events carry the specific code from every wizard variant; failures reach Sentry wherever the copy claims | 🟡 **VERIFIED IN CODE — residual: INGESTION unverified** | **15 of 15** seam routes carry `captureToSentry` — counted per file, none zero (`scenario/optimize` 2, `add-key` 4, `create-with-key` 4, `csv-finalize` 7, `csv-validate` 2, `finalize-wizard` 10, `verify-strategy` 4, remaining eight 1 each). **8 wizard variants** emit `wizard_error`: `MetadataStep`, `WizardClient`, `SyncPreviewStep`, `ConnectKeyStep`, `MultiKeyConnectStep`, `CsvUploadStep`, `SubmitStep`, `CsvSubmitStep`. 🔴 Ingestion is unprovable from the tree |
| **7** | A failed recompute never leaves the previous result on screen as if current | ✅ **VERIFIED** | Both live B-26 members discard **before** refetch: `KeyPermissionBadge.tsx:97` `setPerms(null)`, `PortfolioOptimizer.tsx:211` `setSuggestions(null)`. The template the requirement names is real and was itself hardened: `WeightOptimizerSection.tsx:150` `setResult(null)` + `:160` `!res.ok` (modified by `140.3-08`, commit `20fd9619`) |

**Score: 7/7 satisfied — three carrying residuals the gate named, one carrying a residual it did not (SC2).**

---

## 2. The 9 SEAMUX requirements — kept SEPARATE from the criteria, per the gate's finding

The criteria measure **behaviour under the breaker**. SEAMUX-03 measures **envelope coverage per
arm**. No criterion measures the latter. The gate's central finding — that all 7 criteria can pass
while a requirement stays open — is **confirmed, and this table is why it must not be collapsed into
the one above.**

| Req | Verdict | Owner | Receipt re-measured at HEAD |
|---|---|---|---|
| **SEAMUX-01** | ✅ CLOSED | — | 1 production declaration; **10/10** emitters import the leaf; 12 pre-existing test literals deliberately untouched (C-1 / TRAP-9 — consolidating them would delete a working fence). Residual **DEF-15-1** below |
| **SEAMUX-02** | ✅ CLOSED | — | `tests/lib/validate-key-venue-transient-parity.test.ts` exists; `grep -c "vi.mock"` → **0**, so it runs the real classifier over the committed fixture's own bytes |
| **SEAMUX-03** | 🔴 **OPEN** | **Phase 141+** | **Independently re-derived, see §3.1. 9 of 15 routes carry ZERO code-bearing arms** |
| **SEAMUX-04** | ✅ CLOSED | — | The honesty scan iterates **all** `Object.entries(WIZARD_ERROR_COPY)` over `title` + `cause` + every `fix[]` line, guarded by a hand-typed size literal. The `fix[]`-reach receipt is a separate case, so a title-only regression is visible |
| **SEAMUX-05** | ✅ CLOSED | — | 4 sites, all observing: `ApiKeyManager` ×2 (`:240` explicit, `:305`/`:380` background), `AllocatorMatchQueue:198/252/317`, `WeightOptimizerSection:160` |
| **SEAMUX-06** | ✅ CLOSED (code) | Founder (browser) | Wait field plumbed end-to-end; `Retry-After` status-agnostic at 2 client surfaces + forwarded server-side at `keys/[id]/permissions:500`; TRAP-4 guarded table-wide |
| **SEAMUX-07** | ✅ CLOSED | — | **2 of 2** members schema-validate and fail closed; member 2's validation is inside the 60 s cache callback |
| **SEAMUX-08** | ✅ CLOSED (emission) | Founder (ingestion) | **15/15** routes capture; **8** wizard variants emit. Ingestion unverifiable from the tree |
| **SEAMUX-09** | ✅ CLOSED | — | 2 live members invalidate before refetch; `ReplacementPanel` pinned negatively as the latent third |

**⇒ 8 of 9 closed. SEAMUX-03 OPEN with a named owner. The gate's adjudication is upheld, not softened.**

`.planning/REQUIREMENTS.md` line 128's SEAMUX-03 checkbox must stay `- [ ]`.

---

## 3. The 11 open items — each confirmed or refuted from the tree

| # | Item | Verdict | Evidence measured at HEAD | Owner |
|---|---|---|---|---|
| 1 | **SEAMUX-03 not closed**; 9 of 15 routes zero code-bearing arms | 🔴 **CONFIRMED** | §3.1 — my list matches the gate's **name for name**, and 3 of its 6 non-zero counts (`keys/sync` 10, `csv-finalize` 26, `finalize-wizard` 13) match to the digit | Phase 141+ |
| 2 | **Sentry ingestion unverified**; the stock "trip the breaker" step is WRONG | 🔴 **CONFIRMED — and the correction is right** | 15/15 routes carry `captureToSentry`, so emission is real. The breaker-short-circuit arms deliberately capture nothing, so the stock check would show zero issues on a working integration. Unprovable locally; the suite fakes the transport by design | Founder / ops |
| 3 | **TRAP-4 dialog never exercised by a real browser** | 🔴 **CONFIRMED** | `grep -rln "start_fresh\|Try another key\|Delete draft\|handleDeleteDraft" e2e/` → **zero files**. Production fix IS present (`WizardClient.tsx:728`). e2e dir untouched by the phase, so the 6 local e2e failures cannot be phase-caused | Founder |
| 4 | **DEF-11-1** — `STATUS_CONTRACT.md` documents `VenueTransientHTTPException` as **400** | 🔴 **CONFIRMED STILL OPEN** | `analytics-service/docs/STATUS_CONTRACT.md:498` still reads `**400** (class admits any 4xx)`. Meanwhile **all 7 raise sites answer 424** — verified individually: `exchange.py` :185 :200 :393 :409 :431 :620, `portfolio.py` :2355. Exactly one table cell is stale | Phase 141 |
| 5 | **DEF-11-2** — 5 local `match-queue.spec.ts` failures | ✅ **REFUTED AS A REGRESSION** | `git diff --name-only 59be65cb..HEAD -- e2e/` is **empty**. The phase touched no e2e spec, so it cannot have caused them. Local auth state, as logged | Local env |
| 6 | **TS-37 residual** — cap arm answers **503** on a permanent condition | 🔴 **CONFIRMED, correctly left** | `finalize-wizard:993` returns `{ status: 503 }` with `code: "COMPOSITE_TOO_MANY_MEMBERS"`. Behaviourally sound: `SubmitStep` maps off `code` alone and the code is non-recoverable, so no Retry renders. Only the wire status misleads | Phase 141+, one commit |
| 7 | **DEF-15-1** — `SyncPreviewStep`'s SECOND local table admits none of `process-key-client`'s 4 codes | 🔴 **CONFIRMED. Refusal ENDORSED** | `KNOWN_KICKOFF_CODES` (`:147`) = exactly 5 members: `RATE_LIMITED`, `GATE_DRAFT_GONE`, `COMPOSITE_MEMBERSHIP_UNKNOWN`, `MISSING_STRATEGY_ID`, `INVALID_STRATEGY_ID`. `process-key-client` emits `CIRCUIT_OPEN`(:441), `SEAM_MISCONFIGURED`(:486), `UPSTREAM_TIMEOUT`(:520) + `UPSTREAM_NETWORK_ERROR` — **none admitted**. The one-line fix genuinely trips TRAP-4: `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` (`:1341`) holds only 2 codes, so a new non-recoverable member leaves "Try another key" → `handleDeleteDraft()` as the sole control | SEAMUX-01 follow-up |
| 8 | **DESIGN.md §Error Envelope tension** | 🔴 **CONFIRMED — and BROADER than logged (§5.2)** | DESIGN.md:328 mandates `buildEnvelope()`, no inline-string envelopes. **5 real callers.** `csv-validate/route.ts:67` runs a parallel local `csvErrorEnvelope()` factory with 10 inline-string sites. `CsvUploadStep.tsx`/`CsvSubmitStep.tsx` use **neither** helper although DESIGN.md names "CSV upload" as an envelope surface. **And the declared enforcement — "Code-review block enforced by grep at PR-time" — does not exist** in `.github/`, `scripts/`, or as a test | Founder decision, then a migration plan |
| 9 | **TS-20 premise corrected** — `SCORING_FAILED`/`EVAL_FAILED`/`SIMULATION_FAILED` do not reach `ErrorEnvelope` | ✅ **CONFIRMED** | `<ErrorEnvelope`/`<WizardErrorEnvelope` render sites are **4 wizard steps only**. Those three codes render as bespoke cards. Correction accepted | Closed |
| 10 | **`match.py` emits ZERO 424s** ⇒ TS-18's render correct but not reachable today | ✅ **CONFIRMED** | `grep -rn "424" analytics-service/routers/match.py` → **no output**. TS-19's 400/403/422/429 forwarding is what is live | Whoever gives `match.py` a venue-transient arm |
| 11 | **Three manual verifications OWED**; the `140.3-16` checkpoint AUTO-APPROVED | 🔴 **CONFIRMED — and this verifier could not discharge them either** | `workflow.auto_advance: true` on a `gate="blocking"` (not `blocking-human`) checkpoint auto-approves. That is auto-approval, **NOT founder confirmation**. All three carried into this report's `human_verification` frontmatter. Four sub-checks of #1 discharged programmatically — §4 | Founder |

**None dropped. Seven genuinely open; two refuted/accepted as corrections; two confirmed as correct refusals.**

### 3.1 SEAMUX-03 receipt — re-derived independently, NOT copied

The 15 seam routes were derived from scratch as *files under `src/app/api/` importing
`analytics-client`, `process-key-client` or `resilient-fetch`* — a definition I chose before reading
the gate's list. It produced exactly 15, and the zero-code set matched name for name.

```
per seam route (production file only):   code:"…" arms  /  bare {error: shapes
  strategies/csv-finalize                      26  /   5      ← gate said 26/5   ✅ exact
  strategies/create-with-key                   19  /   1
  strategies/composite/add-key                 18  /   0
  strategies/finalize-wizard                   13  /  19      ← gate said 13/…   ✅ code count exact
  keys/sync                                    10  /   8      ← gate said 10/…   ✅ exact (the ONE route taken arm-by-arm)
  keys/[id]/permissions                         2* /   8
  ── ZERO code-bearing arms — NINE routes ────────────────────
  verify-strategy 0/11 · admin/match/eval 0/6 · admin/match/recompute 0/9
  scenario/optimize 0/12 · simulator 0/8 · bridge 0/7
  portfolio-optimizer 0/5 · keys/validate-and-encrypt 0/10 · strategies/csv-validate 0/1
```

\* 3 raw hits, one of which is a type declaration rather than a response arm.

**Could a broken implementation produce this same result?** No — the discriminating measurement is
the count of routes at **zero**, and zero is only reachable by a route that emits no machine code on
any arm. Re-run: `for f in $(grep -rl --include='*.ts' -E "@/lib/(analytics-client|process-key-client|resilient-fetch)" src/app/api/ | grep -v '\.test\.'); do echo "$f $(grep -cE '(^|[^a-zA-Z_])code:\s*"' $f)"; done`

---

## 4. Partially discharging the owed manual verifications

Three checks are owed and none can be fully closed from a repo. Four **objectively checkable**
sub-clauses of manual verification #1 were discharged programmatically, which narrows the founder's
remaining work to prose quality alone:

| DESIGN.md §Voice sub-check | Method | Result |
|---|---|---|
| The composite-cap copy **names the member limit's number** and the remedy | Read `finalize-wizard:988-992` | ✅ interpolates `${MAX_COMPOSITE_MEMBERS}` three times; remedy is "Remove keys until 10 or fewer remain, then submit again" |
| The `SEAM_MISCONFIGURED` copy does not blame the upstream and offers no retry | Read `wizardErrors.ts:1100-1114` | ✅ "our own configuration is wrong … This is not your key, your exchange or your data." `actions` = `request_call` + `expand_log`, neither in `RECOVERABLE_ACTIONS`, so `recoverable: false` and no Retry renders |
| No sentence claims a team was notified / trades were fetched / data unchanged | Comment-stripped scan of the copy table | ✅ zero non-comment hits |
| Nothing user-facing names an env var or a service hostname | Comment-stripped scan for `ANALYTICS_SERVICE_URL`, `SUPABASE_*`, `INTERNAL_API_TOKEN`, `process.env`, `localhost`, `http(s)://` across the copy table | ✅ **0 non-comment hits** |

**Still owed and NOT dischargeable here:** the prose-quality judgement, the TRAP-4 browser flow, and
Sentry ingestion. All three carry forward in frontmatter.

---

## 5. Findings NO SUMMARY mentions

### 5.1 🔴 The poll-disjointness pin cannot fail in the hazard's real shape — BLOCKER-adjacent

This is the phase's own signature defect — *an acceptance check whose truth value was never measured
against the thing it claims to measure* — shipped inside the **phase gate's own two-file deliverable**,
by the plan that had just documented seven other executors falling into it.

`src/lib/seam-poll-disjointness.pin.test.ts` detects seam calls with `/\bfetch\s*\(/`. But
`SyncPreviewStep.tsx` — one of its three roster modules — routes **every** network call through
`wizardFetch(`, a thin `fetch` wrapper (`src/lib/wizard/wizard-correlation.ts:58`). The needle cannot
see it.

**Measured at HEAD:**

```
pin regex /\bfetch\s*\(/ matches SyncPreviewStep.tsx : false
actual network calls made by SyncPreviewStep.tsx     : [ '/api/keys/sync',
                                                        '/api/strategies/${strategyId}/sync-progress',
                                                        '/api/keys/sync' ]
```

Two of those three are `/api/keys/sync` — **a seam route** (#5 on my own 15-route list). And the
middle one is fired **on the repeating tick**: `SyncPreviewStep.tsx:745`,
`void wizardFetch(...)`, whose own comment reads *"piggyback the per-key progress projection on this
tick … cadence follows POLL_BACKOFF_MS"*.

**Falsified in memory, without touching the tree** — retargeting that one tick URL at a live seam
route (`/api/scenario/optimize`, a one-token edit):

```
HEAD    | fetch-test offender: false | seam-import offender: false | supabase-positive: true
MUTANT  | fetch-test offender: false | seam-import offender: false | supabase-positive: true
```

**All three predicates identical.** The pin stays fully GREEN under the exact retry-storm mutation it
was written to prevent.

Three compounding facts:

1. **The gate SUMMARY's supporting sentence is false.** *"The poll chain … contains **zero `fetch(`
   calls of any kind**. Every read is `@/lib/supabase/client`."* — `SyncPreviewStep.tsx` makes three
   network calls, one of them on the tick.
2. **M75c, the falsifier the gate added specifically because the pin would otherwise ship
   unfalsified, used a literal `fetch("/api/keys/sync")`** injected into `useStrategySyncPoller.ts`.
   That proves the regex catches a spelling this poll path does not use. It does not touch the
   spelling it does use. *Only a positive case can falsify a carry* — and this carry was never given one.
3. **The sibling SSR pin does not have this defect, and the contrast is the diagnosis.** It carries a
   dedicated case, *"the import-edge regex matches an edge and NOT a prose mention"*, asserting its
   needle against four hand-written positives and negatives. The poll pin has **no equivalent
   self-test for its `fetch(` needle.** Same author, same commit, one file guarded its own oracle and
   one did not.

**Severity — stated honestly.** The underlying safety property **still holds at HEAD**: the tick's
real target `/api/strategies/[id]/sync-progress` imports no seam module (verified, `grep -c` → 0), so
no retry storm exists today and **no success criterion is falsified.** What is broken is the *guard*:
the phase installed a fence that reads as protection and cannot fail. That is worse than no fence,
by the pin's own docblock's argument.

**Owner:** a follow-up to `140.3-16`. Fix shape: match the call edge (`wizardFetch` / any `*Fetch`
wrapper) or the seam route paths, add the SSR pin's needle self-test, and re-run M75c using
`wizardFetch(` — the idiom line 745 already proves is live on the tick.

### 5.2 The DESIGN.md §Error Envelope tension is broader than logged, and its enforcement does not exist

Item 8 is logged as "several components use inline strings". Measured, it is three distinct things:

- **5 real `buildEnvelope` callers** (`ConnectKeyStep`, `MultiKeyConnectStep`, `SubmitStep`,
  `SyncPreviewStep`, `venueOutageCopy.ts`) versus DESIGN.md's "every error path MUST call it".
- **`csv-validate/route.ts:67` declares a parallel local `csvErrorEnvelope()` factory** with 10 call
  sites carrying inline strings (`"Invalid multipart body."`, `"Missing file field."`,
  `"Service unavailable."`). Not a stray string — a second envelope mechanism.
- **`CsvUploadStep.tsx` and `CsvSubmitStep.tsx` use neither `buildEnvelope` nor
  `WizardErrorEnvelope`**, although DESIGN.md:312 names "CSV upload" as an ErrorEnvelope surface.
  Both *do* emit `wizard_error` funnel events, so they are seam-aware — just not envelope-aware.
- **DESIGN.md:329 states the rule is "enforced by grep at PR-time". No such grep exists** anywhere in
  `.github/`, `scripts/`, or as a test. The stated enforcement mechanism is absent — an unfalsified
  authoring rule, which is the same defect class as §5.1 one level up.

Out of scope for this phase; recorded so the founder decision in item 8 is taken on the real size.

### 5.3 SC2 carries a residual the gate did not name

Covered in `gaps`. `COMPOSITE_UNSUPPORTED_UNIFIED` (and `draft_state_invalid`) reach `SubmitStep`
without a wizard member and render as `UNKNOWN`, also collapsing the funnel code — so it touches SC6's
specificity clause too. **The finding itself is honestly recorded in production source**
(`wizardErrors.ts:1543`) and in the TS-35 ledger row; only the SC2 adjudication row omits it, while
SC4, SC6 and SC7 each name theirs.

### 5.4 A stale number in production source, and the same number stale in the gate's own receipt

The copy table's size moved 53 → 54 (`140.3-14`, `COMPOSITE_TOO_MANY_MEMBERS`) → 55 (`140.3-15`,
`SEAM_MISCONFIGURED`). Both `EXPECTED_TABLE_SIZE` guards are correctly at **55** and pass.

But two documents were left behind:

- **`SyncPreviewStep.tsx:165`** — a production comment reading *"the copy table's hand-typed size
  guard stays at 53."* Written by `140.3-12`; neither `-14` nor `-15` updated it. **False at HEAD.**
- **`140.3-16-SUMMARY.md`'s SEAMUX-04 receipt** cites *"a scan of all **54** table entries"*. Stale
  by one — inherited from `140.3-14` rather than re-measured at the final tree.

Cosmetic in effect; notable in kind. Both guards themselves were re-derived properly and the
`-15` docblock explicitly records re-running the reasoning **before** bumping the literal — which is
exactly right. The prose around them was not held to the same standard.

### 5.5 Two of `140.3-13b`'s five routes have Sentry assertions that no mutation ever falsified

M79 ran at three routes (`add-key`, `create-with-key`, `scenario/optimize`). The other two —
`csv-finalize` and `portfolio-optimizer` — **do** carry capture assertions (6 and 3 respectively,
measured), so coverage exists; it was simply never falsified per-route. Defensible, because M79 run A
demonstrated per-file discrimination directly (`add-key` reddens while all ten `create-with-key` cases
stay GREEN). Recorded because the ledger's own stated rule is that `surface` tags are per route.

### 5.6 DEF-16-1 is absent from the gate SUMMARY's "All TEN logged open items" table

`deferred-items.md` logs **DEF-16-1** (four pre-existing test files stub `fetch` with no
`vi.unstubAllGlobals()` — this repo's known CI-only failure cause) and **DEF-16-2**. Neither appears
in the SUMMARY's ten-item table. Both are properly recorded with owners in `deferred-items.md`, so
nothing is lost — but a reader trusting the SUMMARY's "None dropped" would miss DEF-16-1.

---

## 6. Obligations — every TS-* ID accounted for

`140.1-TS-OBLIGATIONS.md`'s phase-gate section is internally consistent and its arithmetic
**re-added independently by this verifier**:

- Header total: `0+0+2+4+2+5+2+2+4+0+2+6+1+1+1+2+2+2+2` = **40** ✅
- `DONE-IN-140.3-NN` buckets: `2+6+1+1+1+2+2+2+2` = **19** ✅
- Owner-column 18 + TS-33 (which sat in the `owner decided by 140.2-12` bucket) = **19** ✅ — two
  derivations from different columns, agreeing.

| ID | Verb | Closed by | Verified at HEAD |
|---|---|---|---|
| TS-02, TS-11, TS-12, TS-13, TS-14, TS-15 | ✅ SATISFIED | `140.3-02` | 5/5 `postProcessKey` consumers branch on `ok`; `X-User-Access-Token` forwarded (`process-key-client:414`) and its scrub coupled |
| TS-05, TS-08 | ✅ SATISFIED | `140.3-01` | 3 Class-5 sites — the C-2/C-3 corrected membership (`ScenarioCommitDrawer` out, `PortfolioImpactPanel` in) |
| TS-09 | ✅ SATISFIED across two plans | `140.3-01` + `140.3-12` | type half + copy half |
| TS-17 | ✅ SATISFIED | `140.3-12` | teaser reads `human_message` before `error` |
| TS-18 | ✅ SATISFIED — reachability residual | `140.3-11` | **`match.py` emits zero 424s — confirmed.** Render correct and dormant |
| TS-19 | ✅ SATISFIED | `140.3-11` | both admin routes stop flattening |
| TS-20 | ✅ SATISFIED | `140.3-15` | `seamCorrelationId` reaches the render slot (`SubmitStep:219`) |
| TS-32 | ✅ SATISFIED — doc residual DEF-11-1 | `140.3-06` | **all 7 raise sites answer 424 — each verified individually**; all 7 `.py` files traced to the single commit `1f8ad052` |
| TS-33 | ✅ SATISFIED | `140.3-14` | `wizard_session_id` at finalize-wizard |
| TS-34 | ✅ SATISFIED across two plans | `140.3-01` + `140.3-09` | status + `Retry-After` |
| TS-35 | ✅ SATISFIED | `140.3-05` | parity test, `vi.mock` count **0** |
| TS-37 | ✅ SATISFIED — status residual | `140.3-14` | own code + copy naming the number; **still 503** |
| TS-38 | ✅ SATISFIED | `140.3-15` | `SEAM_MISCONFIGURED`, non-recoverable, blames no one |

**19 SATISFIED · 0 PARTIALLY SATISFIED · 0 RE-HOMED · 0 unaddressed.** Two carry named residuals
(TS-18 reachability, TS-37 status). No obligation is unaccounted for.

---

## 7. Falsifiability Ledger — spot-audited, not trusted

- `grep -c '| ⬜ |' 140.3-VALIDATION.md` → **0**. ✅ No pending row.
- `grep -c '⬜'` → **2**, and I checked both rather than accept the first number: lines 1128 and 1258
  are `### ⬜ STILL OWED after 140.3-13a/13b` **section headers**, not unrun rows. The gate's anchor
  is honest. *(The gate itself documented that the original criterion — glyph followed by the word
  "pending" — returned 0 while three rows were genuinely unrun. Confirmed as DEF-16-2.)*
- 13a's owed item *"140.3-13b owes M78-equivalent runs at its five routes"* was **discharged by M79**
  at 3 of the 5; residual noted at §5.5.
- 49 distinct M-rows present (M51–M85c). No `⬜` among them.
- `grep -rn MUTANT src/` → **0**; `git status --short` shows only the two documented pre-existing
  entries (`TODOS.md` modified, `analytics-service/scripts/nautilus_factsheet.py` untracked).

---

## 8. Anti-patterns

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src/lib/seam-poll-disjointness.pin.test.ts` | 145-162 | Guard that cannot fail in the hazard's real shape | ⚠️ **WARNING** | §5.1 — no live defect; the fence is inert |
| `src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx` | 165 | Stale production comment ("size guard stays at 53"; actual 55) | ℹ️ Info | Misleads the next editor |
| `src/app/api/strategies/csv-validate/route.ts` | 67 + 10 sites | Parallel local envelope factory vs DESIGN.md:328 | ⚠️ WARNING | §5.2, pre-existing |
| `keys/validate-and-encrypt/route.ts:155`, `csv-finalize/route.ts:511`, `wizardErrors.ts:667` | — | `TODO` markers | ℹ️ Info | **All three PRE-EXISTING** (`git diff` for added debt-marker lines → empty) and each references formal follow-up (phase-19+, Phase 19.x, phase-17) |

**Zero `FIXME` / `XXX` / `TBD` introduced by this phase.** Debt-marker gate: **PASS.**
**One type suppression across the whole phase**, on a Supabase test mock — not production.

---

## 9. What I would have written to STATE.md / ROADMAP.md

**Not written — the orchestrator owns those files, `.planning/` is gitignored with no git backup, and
a truncating rewrite has destroyed a ledger on this project before.**

**ROADMAP.md, Phase 140.3:** tick all 17 plan checkboxes `- [ ]` → `- [x]`, and append:

> **Outcome (2026-07-28, verified at `e93044c1`):** **7/7 success criteria SATISFIED** — three with
> residuals the phase named (TRAP-4 unexercised in a browser; Sentry ingestion unverified; the cap
> arm's 503), and **one residual the phase gate did not name** (SC2: `COMPOSITE_UNSUPPORTED_UNIFIED`
> reaches `SubmitStep` without a wizard member and renders as UNKNOWN).
> **8 of 9 requirements closed — SEAMUX-03 REMAINS OPEN**, independently re-measured: 9 of 15 seam
> routes carry zero code-bearing arms; only `keys/sync` was taken arm-by-arm (8 arms, not the 5 every
> source document listed). Owner: a deliberate arm-by-arm plan, Phase 141+.
> **19/19 obligations SATISFIED** (two with named residuals). Suite 9331 → 9779; tsc 0; lint 0 errors;
> mypy clean 89 files; pytest 4778/96; zero packages installed.
> ⚠️ **One NEW gate-introduced defect found at verification:** `seam-poll-disjointness.pin.test.ts`
> cannot fail in the retry-storm shape (its `fetch(` needle is blind to this repo's own `wizardFetch`
> wrapper). The safety property holds at HEAD; the guard does not.
> ⚠️ **Three manual verifications remain OWED** — the `140.3-16` checkpoint auto-approved under
> `workflow.auto_advance`, which is auto-approval, not founder confirmation.

**REQUIREMENTS.md:** mark SEAMUX-01, -02, -04, -05, -06, -07, -08, -09 complete.
**Do NOT tick SEAMUX-03** (line 128) — the traceability row should read "OPEN — 9 of 15 seam routes
carry zero code-bearing arms; owner Phase 141+".

**STATE.md:** Phase 140.3 complete with one requirement open. Decisions to record —
"SEAMUX-03 adjudicated NOT CLOSED on a measured receipt, independently re-derived at verification";
"22 ledger rows re-run at the final tree on a derived discriminator; every one reddened";
"the phase gate's own poll pin found vacuous at verification — the phase's signature defect class,
inside the artifact written to prevent it".
Blockers — the three owed manual verifications; SEAMUX-03's open owner; the poll-pin repair.

---

## 10. Gaps Summary

The phase **achieved its goal**. When the seam fails, the surfaces do now say something true (one
copy leaf, ten emitters, a nine-lie class killed under a whole-table scan), they do offer a
non-destructive way forward (`handleStartFresh` opens a dialog; a table-wide TRAP-4 guard;
`Retry-After` honoured status-agnostically), and they do tell us it happened (15/15 routes capture,
8 wizard variants emit). Every headline gate figure in the SUMMARY reproduced exactly — which, given
what Phase 140 did with a byte-identical suite result, is worth stating as a finding in itself.

Three things stop this being `passed`:

1. **SEAMUX-03 is open**, and I re-derived that from scratch rather than inheriting it. The gate was
   right to refuse to round 8/9 up to 9/9, and right that no success criterion measures it. Nine
   routes — including the public teaser path, both CSV routes and both admin match routes, all named
   in the requirement text — answer with bare `{ error: … }` on every arm.
2. **The phase gate shipped an instance of the phase's own signature defect.** The poll-disjointness
   pin cannot fail in the shape it exists to catch, and its assigned falsifier used a spelling the
   subject does not use. No user is at risk today; the fence is simply inert, and inert fences are
   what this phase spent sixteen plans arguing are worse than none.
3. **Three manual verifications are owed and one checkpoint auto-approved.** The founder has not read
   the copy, has not clicked the destructive path, and has not seen a Sentry issue arrive. Four
   objectively checkable sub-clauses of the copy check were discharged here (§4); the rest cannot be.

None of the three is a reason to re-open the sixteen plans. Items 1 and 2 want one focused follow-up
plan each; item 3 wants twenty minutes of founder time.

---

_Verified: 2026-07-28T11:06:02Z at HEAD `e93044c1`_
_Verifier: Claude (gsd-verifier) — goal-backward, adversarial stance_
