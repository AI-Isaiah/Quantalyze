---
phase: "164.6.2"
plan: "05"
kind: measurement
supersedes: 164.6.2-MEASUREMENT.md
supersedes_note: "OUTCOME 0 — the measurement that could not be taken, because the heal was not deployed"
measured_by: agent (no VNC session, no login, no credential read)
measured_on: 2026-09-15
measured_on_note: "⚠️ The plan's Task 1(e) named 2026-09-14 as the date; the wave actually executed on 2026-09-15. The REAL date is recorded. A planning-time date is not a measurement."
branch_head: 28efbde3
deployed_analytics_sha: f901e4da
---

# Phase 164.6.2 — D-06 measurement record, WAVE 5 (post-ship)

⭐ **THIS TIME THE MEASUREMENT WAS TAKEABLE.** The OUTCOME 0 record this file supersedes could not
take it: the deployed analytics image was `5dead88a`, at which the boot heal does not exist, so a
gateway restart had exactly one uninformative outcome and was correctly not performed. The phase
branch has since merged and the merge has been observed to deploy. Both preconditions were
RE-MEASURED at execution time — not trusted from any brief — before anything was restarted.

⛔ **No VNC session was opened before, during or after this wave. No login was performed by any
instrument or agent. No credential was entered, read back, or requested.** That prohibition is
not ceremony around the criterion; it IS the criterion.

---

## Preconditions

Re-measured at execution time on 2026-09-15, in this order, before any event.

precondition merged: PASS
precondition deployed: PASS
deployed sha: f901e4da99cd100fa912e296b87b3e2c296d38b7

**How, and what each check actually asserts:**

1. `git fetch origin main`, then `git merge-base --is-ancestor 1f938d6d origin/main` → exit 0.
   `origin/main` is at `f901e4da`. The phase's merge commit `1f938d6d` (PR #797, v0.77.43.0) is on
   `main`.
2. `curl -fsS --max-time 20 …/health` read at `2026-09-15T10:39:43Z` → `status ok`,
   `config_ok true`, `config_degraded_secrets []`, worker age ≈1 s (the worker is ticking).
   The reported `git_sha` is the 40-hex value on the `deployed sha:` line above.
3. `git merge-base --is-ancestor 1f938d6d <deployed sha>` → exit 0, and
   `git ls-tree <deployed sha> -- analytics-service/services/mt5_relogin.py` → a blob
   (`e55dbe0a04103e32535b6a0a93a329cbc79daeaa`). The heal module is present in the tree of the
   commit production is actually running.

⛔ **THE CHECK IS CONTAINMENT, NOT EQUALITY, AND THE NEXT READER MUST NOT "FIX" IT BACK.** The
deployed sha is `f901e4da` and the merge commit is `1f938d6d`; they are NOT equal, and they were
never expected to be. `f901e4da` (PR #798, v0.77.43.1 — the deploy-probe fix that made an
unreadable `/health` report UNKNOWN instead of a stale deploy) is a **descendant** of the merge
commit. Railway SKIPPED the merge commit's own deployment at `2026-09-14T12:54:46Z`, and the
descendant deployed successfully at `2026-09-14T14:08:11Z`. Equality was only ever a proxy for the
property that actually matters — *does the running process contain the heal?* — and containment
(ancestor + the module present in that commit's tree) asserts that property directly. An equality
check here would red a correct deploy.

⚠️ ROADMAP criterion 7 still reads *"MATCHES the merge commit"*. That sentence is the equality
proxy, and this record deliberately does not satisfy it as written. The amendment is routed at the
Task 3 checkpoint on every outcome; it is not a reading and is not contingent on one.

## Baseline (pre-event)

baseline authorized: false
baseline run id 34959312135
baseline defect count: 1

**The terminal's own oracle, three consecutive readings, all agreeing** (rows 1, 2 and 4 of the
poll log; 2 min 40 s from first to last): `initialize: false`,
`last_error: [-6, "Terminal: Authorization failed"]`, `terminal_info: null`. Because
`terminal_info` is null, `connected` and `trade_allowed` have no readable value at all — recorded
as UNREADABLE, ⛔ never as `false`. A null before a heal and a `false` before a heal look identical
and mean different things.

**The criterion's own oracle, one narrowed `workflow_dispatch`**, dispatched at
`2026-09-15T10:41:39Z`, created `2026-09-15T10:41:42Z`. Read from the PUBLISHED LOG ARCHIVE
(`gh api …/actions/runs/…/logs`, member `0_probe.txt`), region anchored on the execution-only line
`NARROWED DIAGNOSTIC dispatch of arm mt5:` — ⛔ never the exit code, never the run conclusion,
never a scheduled run's posture-flipped `success`. Verbatim from that region:

```
arms: 1/4/0   (executed/registered/credential-blocked)
seam-invocations: 1   (fetch 0 / sql 0 / ssh 1)

❌ 1 defect(s):

  KIND                        ARM         SUBJECT
  --------------------------  ----------  --------------------------
  mt5-not-authorized          mt5         -6
```

⭐ **A PRE-EVENT FINDING THAT IS NEW, MEASURED HERE, AND NOT THE CRITERION-2 OBSERVATION.** The
terminal was authorized yesterday and is not authorized now, with **no analytics restart in
between**. Evidence, all first-party:

- The analytics deployment list shows the CURRENT deployment created `2026-09-14T14:08:11Z`
  (SUCCESS, `main` @ `f901e4da`) and **nothing newer**. The process running now is the one that
  booted then.
- That process's own heal log carries exactly two lines, at `2026-09-14 14:55:38Z`:
  `mt5 boot heal: starting — …` followed by the verdict `mt5 boot heal: already_authorized`. The
  heal found the broker session in place and correctly did nothing. So the terminal WAS authorized
  at `14:55:38Z` on 2026-09-14.
- Roughly twenty hours later, at `2026-09-15T10:40Z`, the same terminal reads `-6` on three
  consecutive probes and the prober's arm agrees.

⛔ This is a statement about session DURABILITY, not about the heal, and ⛔ it is emphatically not
criterion 2: no gateway restart preceded it. What it does establish is that the starting state for
the event under test is a genuinely lost session — which makes the restart informative in a way the
OUTCOME 0 attempt could not be — and that an unattended MT5 session in this deployment was observed
to survive less than one day. That second half is material to the Task 3 decision on a keepalive.

⚠️ The heal verdict quoted above is PRE-EVENT and is deliberately written as prose. The
machine-readable verdict token belongs to the active phase and appears exactly once in this record.

## Restart stamp

restart stamp: 2026-09-15T10:46:35Z

The event under test, performed with the restart verb — `railway restart -s mt5-gateway -y`
("restart the latest deployment of a service, without rebuilding"), ⛔ not `redeploy`. ⚠️ **The
command emits no stdout and does not return promptly**: it printed nothing at all and stayed
resident for about 24 minutes before exiting 0, long after the terminal had already come back. Its
silence was therefore recorded rather than read as a failure or a block, and the attribution below
rests on the gateway's OWN logs rather than on the CLI's output — which is the right dependency in
any case. ⛔ No VNC session was opened and no login was performed by any instrument or agent,
before, during or after.

⚠️ **The restart verb creates NO new deployment row** — `railway deployment list --service
mt5-gateway` still shows `2026-09-01T11:35:28Z SUCCESS` as the latest, unchanged. A reader looking
for the event in the deployment list will not find it; the evidence is the container's log.

**The passive window was declared BEFORE the first poll at 30 minutes** (RESEARCH's figure). It did
not need it.

passive window: CLOSED at the first post-restart poll — no operator restart of analytics was
performed, and none was needed

## First authorized reading

first authorized reading: 2026-09-15T10:50:18Z

`initialize: true`, `last_error: [1, "Success"]`, `connected: true`, `trade_allowed: true`,
`tradeapi_disabled: false`. Confirmed by the SECOND consecutive reading at `2026-09-15T10:52:47Z`,
byte-identical in all five permitted fields, so the authorized state is recorded as a STATE and not
as a single reading. ⛔ Only the five permitted fields are written; the terminal's install path and
its build were returned by the probe and are deliberately not recorded here.

## Delta

delta: 00:03:43

`2026-09-15T10:50:18Z` minus `2026-09-15T10:46:35Z`.

⚠️ **THIS NUMBER IS AN UPPER BOUND, AND SAYING SO IS THE HONEST PART.** It is the interval from the
restart command to the first reading that OBSERVED the terminal authorized — not the interval to
the moment it actually re-authorized, which is unobserved and lies somewhere in
`(10:46:58Z, 10:50:18Z]`. The gateway's rpyc bridge announced itself ready at `10:46:55Z` and the
container's own boot finished at `10:46:58Z`, so the true re-authorization could be as short as
**≈23 seconds** after the restart. The gap in the poll log between `10:46:35Z` and `10:50:18Z` has
a mundane cause and it is recorded rather than smoothed over: the `railway restart` invocation held
the shell for its first 180 s, so no probe could be issued inside that span. ⛔ A future reading
that wants the true number must poll from the second the bridge answers. The bound is nevertheless
decisive for the decision it informs — whatever the exact value, it is under four minutes, not the
unbounded window RESEARCH expected.

## What closed it

what closed it: the MT5 terminal's OWN saved-session re-authorization when the gateway container
restarted — evidenced by the container's shutdown and fresh-boot log lines, and by the MEASURED
ABSENCE of every other candidate closer

window class: BOUNDED

**The attribution, as four measured facts rather than an inference.** ⛔ D-06 forbids a window
recorded without what closed it, and "the restart probably did it" is not an attribution.

1. **The gateway container really did restart, 9 seconds after the stamp.** Its own log carries
   `X connection to :1 broken (explicit kill or server shutdown).` at `2026-09-15T10:46:44Z`,
   then the fresh boot sequence, then
   `SLAVE/8001[MainThread]: server started on [0.0.0.0]:8001` at `2026-09-15T10:46:55Z`, and
   `[7/7] The mt5linux server is running on port …` at `2026-09-15T10:46:58Z`. ⭐ Independent
   corroboration that this is a new PROCESS and not a reconnect: the rpyc worker-thread counter
   RESET — the last connection before the restart was served by `Thread-342`, the first one after
   it by `Thread-1`.
2. **No analytics restart occurred, so no boot heal could have fired.** The analytics deployment
   list is unchanged: the current deployment is still the one created `2026-09-14T14:08:11Z`, and
   there is nothing newer. ⛔ The operator did NOT restart the analytics service — the active phase
   of the plan never ran, because the passive window closed on its own.
3. **No boot heal line exists in the window.** `railway logs … --filter '"mt5 boot heal"'` re-read
   after the closing observation still returns exactly the two lines from `2026-09-14 14:55:38Z`
   and nothing from 2026-09-15. The heal fires at ANALYTICS startup only (D-08), and analytics did
   not start.
4. **Nothing logged in — and this is the fact that closes the alternative explanations.** Between
   the rpyc server starting at `10:46:55Z` and the first authorized reading at `10:50:18Z` the
   gateway accepted exactly ONE client: a loopback connection at `10:50:21Z` (`127.0.0.1`,
   `Thread-1`), which is the diagnostic's own probe. The analytics service connects from the
   private-network address, not loopback, and no such connection appears in that span. ⛔ And the
   diagnostic is READ-ONLY by construction: it calls `initialize()` and `terminal_info()` and never
   `login()`. So the terminal was ALREADY authorized when the first client reached it.

⭐ **WHAT THIS MEANS, AND IT IS NOT WHAT THE PHASE EXPECTED.** RESEARCH's expected finding was
`UNBOUNDED-WITHOUT-ANALYTICS-RESTART` — the terminal stays dark until an analytics restart fires
the boot heal. The measurement says otherwise: on THIS event the terminal recovered its broker
session by itself, from the auto-login saved in the Wine prefix on the persistent volume, inside
four minutes and with the heal never running. The heal was deployed, armed and idle.

⚠️ **This does NOT make the heal redundant, and the record should not be read that way.** It heals
a different state. The mt5 arm's own shipped remedy already draws the distinction: *"a redeploy does
NOT fix this: the saved login lives on the persistent volume, so a terminal with no usable
credential comes straight back with no usable credential."* That sentence is about an UNUSABLE
credential. What was measured here is a terminal whose saved credential was still usable and whose
SESSION had simply lapsed — and a restart fixes that, while a restart cannot fix the other. ⛔ The
two failure modes are not distinguishable from outside the terminal (that is precisely why the
arm's remedy sends a human to the Journal tab), so a single BOUNDED observation must not be
generalised into "gateway restarts always heal it".

⚠️ **The pre-event finding bounds the other side of the same question, and the founder should weigh
both together:** the session was authorized at `2026-09-14T14:55:38Z` and was found dark at
`2026-09-15T10:40Z` — so an unattended session in this deployment survived less than one day, and
nothing noticed except the hourly prober. A four-minute recovery after a restart says nothing about
how long the terminal sits dark when nobody restarts anything.

## Prober run id

- **BASELINE dispatch (pre-event), taken by this plan:** run 34959312135 — narrowed
  `workflow_dispatch`, `arm=mt5`, created `2026-09-15T10:41:42Z`, before the event.
- **POST-RESTART dispatch — the run criterion 2 closes on:**

post-restart run id 34960346817
post-restart run created: 2026-09-15T10:53:20Z

⭐ It is a DIFFERENT dispatch from the baseline, its creation stamp is strictly AFTER the restart
stamp (`10:53:20Z` > `10:46:35Z`), and it appears nowhere in the OUTCOME 0 record — which cited four
runs and contained zero post-restart observations, the defect this line exists to make impossible.

## Prober verdict

post-restart defect count: 0

Read from the PUBLISHED LOG ARCHIVE of run 34960346817 (`gh api …/actions/runs/…/logs`, member
`0_probe.txt`), region anchored on the execution-only line `NARROWED DIAGNOSTIC dispatch of arm
mt5:`. Verbatim from that region:

```
mt5: initialize=true last_error=1 connected=true trade_allowed=true

⚠️ NARROWED DIAGNOSTIC RUN (--arm): arm floors NOT enforced.
   This mode never exits 0, so it can never be mistaken for a passing gate.

arms: 1/4/0   (executed/registered/credential-blocked)
seam-invocations: 1   (fetch 0 / sql 0 / ssh 1)

No defects in the narrowed scope.
```

⛔ **NOT THE EXIT CODE AND NOT THE RUN CONCLUSION, AND THIS RUN SHOWS WHY IT MATTERS.** GitHub
reports this run's conclusion as `failure`, and the step ended non-zero. That number is not a
reading: a narrowed run is 1 when it found defects and 2 when it found none, by `run.mjs`'s own
`exitCode` rule, so it is never 0 and never the verdict. The baseline run ended 1 and this one
ended 2 — both exactly as the rule predicts, in opposite states. A reader who took the conclusion
as the verdict would record this ZERO-DEFECT run as a failure.

⭐ The arm's own capability line, `mt5: initialize=true last_error=1 connected=true
trade_allowed=true`, is an INDEPENDENT confirmation of the terminal's state: it is measured by the
prober's credential-less path from GitHub's runner, not by `scripts/mt5-diag.sh`, and it agrees
with the diagnostic on all four flags. ⛔ The two instruments do not disagree, so there is no
disagreement to adjudicate.

## Question THREE verdict

The contested sentence (D-09): *a login is an ACCOUNT CHANGE, and MT5 re-clears the
Expert-Advisors options on every account change.*

R1: true
R2: UNAVAILABLE
R2 event: none within the bounded wait
r2 pending: a real per-account login is expected at the next scheduled MT5 job; take R2 then

verdict: UNSETTLED

**R1**, per plan 04 task 1(e)'s exact rule — `trade_allowed` at the FIRST reading that shows the
terminal authorized — is the `2026-09-15T10:50:18Z` reading, and it is `true`. ⭐ This is the first
time in this phase that R1 has had a value at all: the OUTCOME 0 record recorded it UNAVAILABLE
because `terminal_info()` returns null while the `-6` is in force, so `trade_allowed` had no
readable value.

**R2 requires a REAL per-account login** — the event the contested sentence is actually about — and
none occurred inside the bounded wait of 30 minutes past the closing observation. ⛔ A login was NOT
triggered to manufacture one: neither by an agent, nor by a VNC session, nor by making either
instrument log in. ⛔ And the absence was WAITED OUT and then MEASURED, not assumed: the wait ran to
`2026-09-15T11:23:32Z` and the gateway's own connection log for the whole span from the closing
observation to that deadline contains exactly ONE client — a loopback connection at
`2026-09-15T10:53:56Z` served by `Thread-3`, which is the prober's own credential-less arm for the
post-restart dispatch. The analytics service's private-network address, which is where a real
per-account login would come from, does not appear once in that span.

⭐ **That absence is itself a small finding about cadence:** per-account MT5 work is sporadic here.
The gateway's connection history shows analytics-side clusters around `04:00Z`–`05:01Z` on
2026-09-15 and a single connection on 2026-09-14, so a 30-minute window had a low prior of
containing one. Whoever takes R2 should wait for a scheduled job rather than expect one on demand.

⚠️ **A boot heal would NOT have settled it either.** The heal is the credentialed-`initialize`
form, which is a PROXY for a per-account login and not the event the sentence describes — and no
heal ran in this window in any case.

⛔ **CONSEQUENCE, AND IT IS BINDING FOR THIS WAVE:** the verdict licenses NOTHING here. All four
copies of the stale account-change sentence stay byte-unchanged — the two in
`scripts/prod-prober/arms/mt5.mjs` and the two in `scripts/mt5-diag.sh` — and both files are
hash-pinned to their planning-time blobs by this plan's `INSTRUMENTS-READ-ONLY` gate on every task.
⭐ R1 now exists, which is strictly more than the phase had before; R2 is a PENDING reading, not a
failure, and whoever takes it next needs only the next real per-account login plus one diagnostic
probe.

## Outcome

outcome: CLOSED

⭐ **ROADMAP CRITERION 2 IS CLOSED ON AN OBSERVATION — the phase's load-bearing criterion, and the
one the founder refused to close on the code alone.** The gateway was restarted at
`2026-09-15T10:46:35Z`; the terminal was observed authorized on two consecutive readings from
`2026-09-15T10:50:18Z`; and the POST-RESTART narrowed prober dispatch, run 34960346817, created
after the restart and read from its own published log archive, reports **no defects in the narrowed
scope** for the mt5 arm.

⛔ **No VNC session was opened before, during or after. No login was performed by any instrument or
agent. No credential was entered, read back, or requested.** The observation is worth exactly what
that prohibition makes it worth.

| Outcome | Applies? | Why |
|---|---|---|
| **CLOSED** | ✅ | post-restart defect count 0, on a dispatch created after the restart, no human in the loop |
| OPEN | ❌ | would require a non-zero count after the ONE analytics restart; the passive window closed on its own and no analytics restart was performed |
| UNREADABLE | ❌ | every instrument answered: `/health`, the diagnostic three times before and twice after, and both prober archives |
| PREMISE-FAILED | ❌ | both preconditions re-measured PASS at execution time |

⚠️ **What this record does NOT establish, stated so nobody reads more into it than it holds.**
(1) It does not show the boot heal working in production — the heal never ran, and its only
production verdict to date is the `already_authorized` no-op of 2026-09-14. (2) It does not show
that a gateway restart ALWAYS recovers the session; it shows that THIS restart recovered THIS
lapsed session, from a saved credential that was still usable. (3) RESEARCH assumption A3 — a
broker-side block on the account — remains untested, because the state that would test it did not
arise. ⭐ That is a better position than OUTCOME 0's, where A3 could not be tested either but the
reason was our own absent code.

## Poll log

All stamps UTC. ⛔ Only the five permitted diagnostic fields are recorded — never the terminal's
install path, never its build.

| # | Stamp | Instrument | Reading |
|---|---|---|---|
| 1 | `2026-09-15T10:40:13Z` | `./scripts/mt5-diag.sh` | `initialize: false`, `last_error: [-6, "Terminal: Authorization failed"]`, `terminal_info: null` → `connected` / `trade_allowed` UNREADABLE |
| 2 | `2026-09-15T10:41:24Z` | `./scripts/mt5-diag.sh` | IDENTICAL to reading 1 — the SECOND CONSECUTIVE agreement, so `-6` is recorded as a STATE and not as transport noise |
| 3 | `2026-09-15T10:41:42Z` | narrowed prober, run 34959312135 | `mt5-not-authorized` / `-6`, defect count 1 |
| 4 | `2026-09-15T10:42:53Z` | `./scripts/mt5-diag.sh` | IDENTICAL to readings 1 and 2, 2 min 40 s after the first |
| — | `2026-09-15T10:46:35Z` | **THE EVENT** — `railway restart -s mt5-gateway -y` | not a reading; the restart stamp |
| 5 | `2026-09-15T10:50:18Z` | `./scripts/mt5-diag.sh` | `initialize: true`, `last_error: [1, "Success"]`, `connected: true`, `trade_allowed: true`, `tradeapi_disabled: false` — the FIRST authorized reading |
| 6 | `2026-09-15T10:52:47Z` | `./scripts/mt5-diag.sh` | IDENTICAL to reading 5 in all five permitted fields — the SECOND CONSECUTIVE agreement, so *authorized* is recorded as a STATE |
| 7 | `2026-09-15T10:53:20Z` | narrowed prober, run 34960346817 | `mt5: initialize=true last_error=1 connected=true trade_allowed=true`; **no defects in the narrowed scope**, defect count 0 |

⛔ No single reading was treated as a state — neither the `-6` before the event nor the authorized
state after it. No IPC-timeout reading (`-10005` / `-10004`) occurred in this window, so the
transport-noise rule was never in play, and the diagnostic never once failed to return a `PROBE`
line, so no reading had to be recorded as UNREADABLE.

⚠️ **The honest gap: no reading exists between `10:42:53Z` and `10:50:18Z`.** The `railway restart`
invocation held the shell for its first 180 s, so the ~2-minute cadence could not be maintained
across the event itself. This is why the delta is an upper bound and is stated as one.

**Gateway container events inside the window, from the container's OWN log** (the restart verb
creates no deployment row, so this is where the event is visible):

| Stamp | Event |
|---|---|
| `2026-09-15T10:46:44Z` | `X connection to :1 broken (explicit kill or server shutdown).` — the container going down, 9 s after the restart stamp |
| `2026-09-15T10:46:55Z` | `SLAVE/8001[MainThread]: server started on [0.0.0.0]:8001` — a FRESH rpyc server; the worker-thread counter reset from `Thread-342` to `Thread-1` |
| `2026-09-15T10:46:58Z` | `[7/7] The mt5linux server is running on port …` — the container's boot sequence complete |
| `2026-09-15T10:50:21Z` | the ONLY client accepted between the server starting and the first authorized reading: a loopback connection served by `Thread-1` — the read-only diagnostic's own probe |

**Deployment events for correlation** (analytics, production) — ⭐ note the ABSENCE, which is
load-bearing for the attribution: there is no analytics deployment on 2026-09-15 at all.

| Stamp | Event |
|---|---|
| `2026-09-14T14:08:11Z` | analytics deployment, `main` @ `f901e4da` — SUCCESS, **still current throughout this entire measurement** |
| `2026-09-14T12:54:46Z` | analytics deployment, `main` @ `1f938d6d` — **SKIPPED** (the merge commit's own deployment never ran) |

---

## Supplementary: how long the terminal sat dark before anyone restarted anything

⚠️ **SUPPLEMENTARY AND OUTSIDE THE CRITERION-2 WINDOW.** Nothing below is part of the closure; it is
recorded because the Task 3 decision is about whether the measured window is ACCEPTABLE, and the
delta alone answers only half of that question. The delta measures *recovery once a restart
happens*. This measures *how long nothing happens*.

Read from the hourly scheduled prober runs' own published log archives, taking only the mt5 arm's
capability line:

| Run | Created | The arm's capability line |
|---|---|---|
| run 34890630894 | `2026-09-14T20:03:48Z` | `mt5: initialize=true last_error=1 connected=true trade_allowed=true` |
| run 34909635593 | `2026-09-14T23:36:36Z` | `mt5: initialize=true last_error=1 connected=true trade_allowed=true` |
| run 34921728678 | `2026-09-15T02:34:53Z` | `mt5: initialize=true last_error=1 connected=true trade_allowed=true` |
| run 34945651195 | `2026-09-15T08:12:00Z` | ⚠️ **no capability line in the archive** — see the caveat below |

**The bracket, stated only as far as the evidence reaches.** The terminal was authorized at
`2026-09-15T02:34:53Z` and was measured dark at `2026-09-15T10:40:13Z` by this plan's own baseline.
So the session was lost somewhere inside that **≈8 h 05 m** span, and it had then been dark for
however much of it remained when this wave restarted the gateway — with nothing in the system
noticing, acting, or being able to act. Counting back to the last heal verdict, the session had
survived roughly **11 h 39 m** (from `2026-09-14T14:55:38Z` to `02:34:53Z`) before it lapsed.

⛔ **THE 08:12 RUN IS RECORDED AS NOT-READ, NOT AS A `-6`.** A SCHEDULED run carries no
`NARROWED DIAGNOSTIC dispatch of arm mt5:` line, and that line is the only execution anchor this
plan has proven; without it the log cannot be separated from the self-test step, which prints
`mt5-not-authorized` by name, prints whole remedy strings, and even prints
`No defects in the narrowed scope.` for its own scenarios. ⚠️ A whole-log grep of that archive
returns all of those, and every one of them is a fixture. ⭐ The absence of a capability line is
nonetheless *consistent* with a failed `initialize()` — the same absence is visible in this wave's
own baseline narrowed run, which reported the `-6` — but *consistent with* is not *measured*, and
this record does not upgrade it. The bracket above therefore opens at `02:34:53Z`, the last reading
that is unambiguous, rather than at the tighter time a guess would give.

⛔ Never cite a scheduled run's `conclusion` as a reading either: the probe step is posture-flipped
to exit 0 on schedule so an hourly red cannot deadlock Railway's wait-for-CI.

---

## Observation: the env account vs. the connected MT5 key

⛔ **NOT DETERMINABLE BY AN AGENT, and the reason is a prohibition rather than a gap** — carried
forward unchanged from the OUTCOME 0 record. Comparing the account behind `MT5_LOGIN` with the
account on the only connected MT5 key requires READING the value of `MT5_LOGIN`, and criterion 3
forbids any agent from reading a credential back. Only key NAMES have ever been observed. If the
terminal authorizes but per-account work then refuses, an account mismatch between the env
credential and the stored key is the first thing to check — and it is a founder-side check.
