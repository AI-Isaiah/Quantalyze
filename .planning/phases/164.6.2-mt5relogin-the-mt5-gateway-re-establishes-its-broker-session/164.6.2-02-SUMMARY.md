---
phase: "164.6.2"
plan: "02"
subsystem: infra
tags: [mt5, boot-heal, lifespan, crash-handler, terminal-lease, ast-derived-gate, falsifier]

requires:
  - phase: "164.6.2"
    plan: "01"
    provides: "`mt5_terminal_key(host, port)` (the lease key without a client), `assert_session_authorized()` (the credential-FREE `-6` detector) and `initialize_with_credentials()` with by-value redaction on BOTH failure arms — the ORDERED security precondition for routing the first credential"
  - phase: "153.5"
    provides: "WIZFORM-ABANDON / D-36 — the lease's epoch bump and the client's lazy-bind fence, which is what makes 'construct inside the lease' correct and 'touch from preflight' wrong"
  - phase: "153.3"
    provides: "D-29 `mt5_terminal_lease(key, wait_s=…)` — the BOUNDED acquire arm the boot heal needs so a busy terminal means skip, not queue"
  - phase: "164.8.3"
    provides: "the shipped `-6` vocabulary the branch keys on"
provides:
  - "`services/mt5_relogin.py` — `heal_mt5_terminal_session()`, the ONE lease site, structurally incapable of raising, of blocking the loop, or of touching the terminal outside its own lease"
  - "the criterion-1 wiring: ONE `create_task` entry in `main.lifespan`'s existing task list, after both startup guards, never awaited inline"
  - "a durable in-memory criterion-1 falsifier with a surgical-excision proof"
  - "the CONC-2/D-08 preflight-touches-nothing gate — DERIVED from `Mt5Client`'s own `_assert_live` fence, floored against vacuity, calibrated on synthetic source"
  - "`_PRODUCTION_LEASE_SITES` re-cut to six, with the walk's measured `main.py` scope gap recorded beside the new entry"
affects: ["164.6.2 plan 03 (the founder handover)", "164.6.2 plan 04"]

actuals:
  tokens: 20643
  tasks: 3
  # MEASURED, never narrated: `git rev-list --count 26952f79..HEAD` at SUMMARY time,
  # BEFORE this file's own docs commit. The three task commits below.
  commits: 3

plan_head_before: 26952f793ad4131fa3fd6f84c7bf6b5d4def2630

tech-stack:
  added: []
  patterns:
    - "the heal's whole body inside ONE top-level `except Exception`, asserted as a SHAPE (AST) as well as a behaviour — because a future edit can add a statement outside the guard without any behaviour test noticing"
    - "prove 'off the event loop' by recording the OS thread identity at construction, not by reading the source"
    - "a structural gate whose subject set is DERIVED from the fence the production class already applies (`self._assert_live`), so a future verb is covered without a pin edit"
    - "an in-memory source mutant as a durable falsifier: no byte backup, no restore, no destructive-edit hazard in CI"

key-files:
  created:
    - analytics-service/services/mt5_relogin.py
    - analytics-service/tests/test_mt5_relogin.py
  modified:
    - analytics-service/main.py
    - analytics-service/tests/test_mt5_concurrency.py

key-decisions:
  - "The kill switch is consulted as the FIRST statement inside the guard, before any construction — the WR-08 residual (a gate sitting AFTER the transport opened) is explicitly not repeated at the first site that carries a credential."
  - "The lease acquire is BOUNDED at 2.0s. A terminal already busy at boot is evidence the session is fine; skipping costs the next holder nothing because a timed-out acquire holds, bumps and stamps nothing."
  - "The verdict string is NOT an oracle, and lever B measured why: with the heal call removed the module still logged `healed`."
  - "The preflight gate derives its verb set from `self._assert_live`, not from `self._mt5.<x>` — the latter misses verbs that reach the session through a helper."
  - "`[164.6.2-KILLSWITCH-COMMENT-DRIFT]` recorded, NOT fixed: `services/closed_sets.py` claims `\"TRUE \"` reads OFF; the shipped code strips before it lowercases, so it reads ON."

patterns-established:
  - "Pattern: a wiring pin carries TWO independent legs — the named-call predicate AND a hand-typed count measured independently of the name — so a predicate that silently stops matching cannot take both halves down together."
  - "Pattern: when a neuter's target also appears as PROSE in a docstring, grep the CALL EXPRESSION, not the bare symbol. Comment filtering does not strip docstrings, and the bare-symbol count here fell 2 -> 1 rather than to 0."

# ⛔ EMPTY DELIBERATELY, same reasoning as plan 01's. `requirements: ["MT5-GATEWAY-LOGIN-01"]`
# is carried by plans 01, 02, 03 AND 04 — it is a PHASE-level requirement and 2 of 4 plans
# does not discharge it. The id is also MEASURED ABSENT from `.planning/REQUIREMENTS.md`
# and `TODOS.md`. `requirements.mark-complete` was NOT run.
requirements-completed: []

coverage:
  - id: D1
    description: "⛔ CRITERION 1 — the analytics WORKER re-establishes the terminal's session from the env triple ONCE at its own startup, in `main.lifespan`, and the code path is exercised by a test that FAILS when the call is removed."
    requirement: "MT5-GATEWAY-LOGIN-01"
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_CRITERION_1_lifespan_starts_the_heal_exactly_once_as_a_task"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_the_criterion_1_predicate_reds_on_a_mutant_with_the_entry_excised"
        status: pass
      - kind: other
        ref: "LEVER A — create_task entry excised from main.lifespan; BOTH legs observed RED (call pin 0/1, count pin 4/5); transcript in this SUMMARY"
        status: pass
      - kind: other
        ref: "LEVER B — credentialed-initialize call excised from the blocking body; RED on the double's recorded kwargs, surgically (already-authorized and all never-raises cases stayed green); transcript in this SUMMARY"
        status: pass
    human_judgment: false
  - id: D2
    description: "The heal uses the CREDENTIALED initialize form with values taken from the ENVIRONMENT — asserted against the double's RECORDED KWARGS, never against the implementation's own formula."
    requirement: "MT5-GATEWAY-LOGIN-01"
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_a_minus_six_terminal_is_healed_with_the_values_from_the_environment"
        status: pass
    human_judgment: false
  - id: D3
    description: "It heals ONLY `-6`. A truthy bare initialize stops the heal; an IPC code stops it and names the code."
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_an_already_authorized_terminal_is_never_sent_a_credential"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_an_ipc_fault_is_not_healed_and_the_verdict_names_the_code (3 codes)"
        status: pass
    human_judgment: false
  - id: D4
    description: "⛔ THE BOOT HEAL CANNOT TAKE THE SERVICE DOWN BY ANY OF THE THREE MEASURED ROUTES — a raise before yield, a blocked event loop, or `_crash_handler` setting SHUTDOWN."
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_no_failure_mode_escapes_the_heal (5 injections, one at the first statement inside the guard)"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_the_heal_body_sits_entirely_inside_one_top_level_except_exception"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_a_hung_terminal_is_abandoned_at_the_budget_and_raises_nothing"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_exactly_one_bounded_lease_over_a_client_built_inside_the_thread"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_the_heal_is_never_awaited_inline_and_runs_after_both_startup_guards"
        status: pass
    human_judgment: false
  - id: D5
    description: "D-02 — absent/invalid configuration LOGS ONCE, NAMES ONLY, returns, and a second call in the same process adds no record."
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_absent_credentials_log_once_with_names_only_and_return"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_a_second_call_in_the_same_process_does_not_log_again"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_structurally_invalid_credentials_log_once_and_disclose_nothing"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_an_unusable_gateway_endpoint_logs_once_and_constructs_nothing (3 cases)"
        status: pass
    human_judgment: false
  - id: D6
    description: "⛔ `main.REQUIRED_PLATFORM_SECRETS` is byte-unchanged and `/health` gains no MT5 key — both pinned against the LIVE objects (T-140.1-24)."
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_the_mt5_variables_were_NOT_added_to_the_required_platform_secrets"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_mt5_relogin.py#test_the_health_body_gained_no_mt5_key"
        status: pass
      - kind: unit
        ref: "the two SHIPPED lifespan ordering pins, unedited (test_secret_misconfig_signal.py, test_local_worker_prod_guard.py)"
        status: pass
    human_judgment: false
  - id: D7
    description: "⛔ `job_worker._make_mt5_session` still performs NO terminal touch and takes no lease — pinned by a DERIVED structural gate with a floor and a calibration (D-08, T-164.6.2-10)."
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_mt5_concurrency.py#test_the_mt5_preflight_touches_no_terminal_session"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_mt5_concurrency.py#test_the_preflight_touch_scanner_fires_on_a_spliced_session_verb"
        status: pass
    human_judgment: false
  - id: D8
    description: "The lease roster is re-cut to six in the SAME diff as the new lease site, so the `==` assertion never lands red."
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_mt5_concurrency.py#test_no_production_function_holds_two_terminal_leases"
        status: pass
    human_judgment: false

duration: 52min
completed: 2026-09-13
status: complete
---

# Phase 164.6.2 Plan 02: the boot heal, wired into the production path — Summary

**The analytics worker now re-establishes the MT5 terminal's broker session from the env triple once at its own startup — via a task that is structurally incapable of raising, of blocking the event loop, or of touching the terminal outside its single bounded lease — and criterion 1 is discharged as a MEASUREMENT, with both levers observed RED and a durable in-suite falsifier that re-checks it on every CI run.**

## Performance

- **Duration:** ~52 min
- **Tasks:** 3/3
- **Files:** 2 created, 2 modified
- **Commits:** 3 — MEASURED `git rev-list --count 26952f79..HEAD` at SUMMARY time (the three task commits; this file's own docs commit is not in that count because it did not exist when the count was taken)

## Accomplishments

- **Criterion 1 is DISCHARGED AS A MEASUREMENT, not a claim.** The heal runs once at startup in `main.lifespan`, sends the credentialed `initialize()` with values taken from the environment (asserted against the double's recorded kwargs), and a test was OBSERVED to fail when the call was removed — on two independent levers, each with its neuter proven applied first.
- **The sharpest hazard in the plan is closed, and closed twice.** `_crash_handler` sets `SHUTDOWN` on ANY background-task exception, so a raising heal would stop the dispatch, watchdog and enqueue loops behind a green `/health` — a silent analytics outage. The never-raises property is asserted BEHAVIOURALLY (five injections, one at the very first statement inside the guard) and STRUCTURALLY (the function's AST carries exactly one `Try` and nothing outside it).
- **"Off the event loop" is MEASURED, not read off the source.** The construction recorder captures `threading.get_ident()`, and the test asserts it differs from the test's own — so the `rpyc.classic.connect` that carries no timeout of its own is provably inside the `to_thread` body where the `wait_for` bounds it.
- **A gate that never existed now exists.** `job_worker._make_mt5_session` performs no terminal touch — the fact that STRUCK D-01 and produced D-08 — and until this plan **nothing pinned it**. It is now pinned by a gate whose verb set is DERIVED from `Mt5Client`'s own `_assert_live` fence, floored against vacuity, and calibrated on synthetic source.
- **The roster re-cut answered its own question first.** Does the new site touch an `Mt5Client` already touched under a different lease? It does not — construct inside, close inside. And the walk's measured `main.py` scope gap is recorded beside the new entry, because that gap is exactly why the heal lives in `services/`.
- **The phase is shippable WITHOUT the founder.** It deploys safely with `MT5_LOGIN`/`MT5_PASSWORD`/`MT5_SERVER` still unset — their measured state today — because absent configuration logs once and returns.
- **Nothing shipped was disturbed.** `services/job_worker.py`, `main_worker.py`, `Mt5Client.login` and both shipped lifespan ordering pins are byte-unchanged. No floor was re-cut anywhere.

## Task Commits

1. **Task 1: the heal module, its behaviour suite, the re-cut lease roster and the preflight pin** — `023b4dec` (feat)
2. **Task 2: wire the heal into `main.lifespan`** — `6d6f9e5b` (feat)
3. **Task 3: the criterion-1 falsifier — durable in-suite, plus two levers observed RED** — `22fa8db3` (test)

## Files Created/Modified

- `analytics-service/services/mt5_relogin.py` — **NEW**, 378 lines. `heal_mt5_terminal_session` (the async entry and the ONE lease site), `read_env_mt5_credentials`, `read_env_gateway_endpoint`, `_heal_blocking`, `_log_configuration_fault_once` + `_reset_relogin_log_throttle_for_tests`, `_MT5_RELOGIN_BUDGET_S` / `_MT5_RELOGIN_LEASE_WAIT_S`, `_MT5_NO_AUTHORIZED_ACCOUNT_CODE`.
- `analytics-service/tests/test_mt5_relogin.py` — **NEW**, 1100 lines. The behaviour suite, the doubles, the wiring pins, the two refusal pins and the durable falsifier. 40 tests.
- `analytics-service/main.py` — **+12 / -0**: one lazy import, one `create_task` entry in the existing `tasks` list, one comment block.
- `analytics-service/tests/test_mt5_concurrency.py` — **+263 / -1**: the roster re-cut to six with its scope-gap note, and the CONC-2 preflight gate plus its calibration.

## Verify readings

⚠️ **`python` is not on this box's PATH; `analytics-service/.venv/bin/python` is (3.12.13).** Every command below is the plan's command with that interpreter substituted, run from `analytics-service/` as the plan requires. Nothing else was changed. `-p no:warnings` was added to a few runs purely to keep a `pandera` `FutureWarning` from burying the output; it changes no outcome.

### Task 1

| Leg | Command | Reading |
|---|---|---|
| 1 | `pytest tests/test_mt5_relogin.py tests/test_mt5_concurrency.py -x -q` | `58 passed in 3.34s` |
| 2 | `pytest tests/ -k mt5 -q` | `535 passed, 2 skipped, 5045 deselected in 14.06s` |
| 3 | `pytest tests/test_structlog_frozen_proxy.py tests/test_raw_5xx_census.py -q` | `29 passed in 9.70s` |
| 4 | `mypy --strict --follow-imports=silent services/` | `Success: no issues found in 80 source files` |
| 5 | the boot-safety shape tripwire | `BOOT-SAFETY-SHAPE OK (11)` |
| done | `git diff --name-only` | the three `<files>` paths; NEITHER `services/job_worker.py` NOR `main.py` |

### Task 2

| Leg | Command | Reading |
|---|---|---|
| 1 | `pytest tests/test_mt5_relogin.py tests/test_secret_misconfig_signal.py tests/test_local_worker_prod_guard.py tests/test_health.py -q` | `67 passed in 2.25s` |
| 2 | the main.py added-line bound | `MAIN-DIFF-BOUNDED OK (12 added lines)` |
| 3 | the health/secrets untouched tripwire | `HEALTH-UNTOUCHED OK` |
| 4 | the out-of-scope tripwire | `OUT-OF-SCOPE-CLEAN OK` |

⚠️ Leg 2 landed at **12 of a permitted 12**. Not a near-miss to be shrugged at: the comment block above the entry is 10 lines and the plan's ceiling is what stopped it becoming 15. It is recorded so a future edit to that comment knows it is spending from a budget with nothing left.

### Task 3

| Leg | Command | Reading |
|---|---|---|
| 1 | `pytest tests/test_mt5_relogin.py -q` | `40 passed in 2.35s` |
| 2 | the tree-restored tripwire | `TREE-RESTORED OK` |
| 3 | `pytest tests/ -k mt5 -q` && `mypy --strict … services/ routers/ models/` | `541 passed, 2 skipped, 5045 deselected in 12.68s`; `Success: no issues found in 93 source files` |

### Plan-level `<verification>`

```
pytest tests/ -k mt5 -q
  -> 541 passed, 2 skipped, 5045 deselected in 12.68s

pytest tests/test_secret_misconfig_signal.py tests/test_local_worker_prod_guard.py \
       tests/test_health.py tests/test_structlog_frozen_proxy.py \
       tests/test_raw_5xx_census.py -q
  -> 57 passed in 10.86s

mypy --strict --follow-imports=silent services/ routers/ models/
  -> Success: no issues found in 93 source files

git diff --name-only 26952f79..HEAD
  -> analytics-service/main.py
     analytics-service/services/mt5_relogin.py
     analytics-service/tests/test_mt5_concurrency.py
     analytics-service/tests/test_mt5_relogin.py
     (exactly the four `files_modified`; NEITHER services/job_worker.py NOR main_worker.py)
```

Baseline before any edit, for contrast: `pytest tests/ -k mt5 -q` was `495 passed, 2 skipped` at plan base.

## ⛔ Calibration transcripts — 2026-09-13

Both levers follow the same five-step harness: `cp` byte backup into the **session scratchpad** (outside the repo — a backup inside it is an untracked file that changes what the porcelain check means), targeted in-place edit, **assert the neuter applied before believing any result**, run and capture verbatim, restore from the backup and prove it with `cmp`. ⛔ `git checkout --` was not used anywhere in this harness; it restores to HEAD and would have destroyed the uncommitted work tasks 1 and 2 had just written.

### LEVER A — the wiring

**Neuter:** the single line `asyncio.create_task(heal_mt5_terminal_session(), name="mt5_boot_heal"),` removed from `main.lifespan`'s `tasks` list. ⚠️ The COMMENT above it was deliberately left in place.

**Applied-proof (comments filtered, taken BEFORE anything was run):**

```
=== PRE-NEUTER MEASUREMENT (comments filtered) ===
filtered occurrences of the entry: 1
UNfiltered occurrences (comment does not name the symbol): 2
line count:      942
backup sha: 73904dd510fdc86b6be0557106eccf1c34ec25ccb6252b73634066c1fa2cbdf0

=== ASSERT THE NEUTER APPLIED (comments filtered) ===
filtered occurrences of the entry: 0
lazy import survives (so this is not an import deletion): 1
line count now:      941
differs from backup: DIFFER — the neuter genuinely landed
```

⚠️ **A correction to the plan's own instrument, recorded because the next reader should not re-derive it.** The plan predicted the comment would NAME the function, making the unfiltered grep misleading. The comment as written names the *behaviour*, `_crash_handler` and D-08 but not the symbol, so the unfiltered count is 2 (the lazy import plus the entry) and falls to 1 rather than staying at 1. The comment-filtered grep on the CALL EXPRESSION was used anyway — it is the correct instrument either way, and the lazy-import survivor check is what proves the neuter was surgical rather than an import deletion.

**RED, verbatim (the criterion-1 pin):**

```
E       AssertionError: `main.lifespan` must start heal_mt5_terminal_session() EXACTLY ONCE inside a create_task; found 0 at []. Zero means the criterion-1 wiring is gone — production would never heal a lost broker session and the whole phase would be inert.
E       assert 0 == 1
E        +  where 0 = len([])

tests/test_mt5_relogin.py:841: AssertionError
=========================== short test summary info ============================
FAILED tests/test_mt5_relogin.py::test_CRITERION_1_lifespan_starts_the_heal_exactly_once_as_a_task
FAILED tests/test_mt5_relogin.py::test_the_heal_is_never_awaited_inline_and_runs_after_both_startup_guards
FAILED tests/test_mt5_relogin.py::test_the_heal_task_is_named_and_tracked_like_the_worker_loops
FAILED tests/test_mt5_relogin.py::test_the_criterion_1_predicate_reds_on_a_mutant_with_the_entry_excised
4 failed, 36 passed in 2.34s
```

⭐ **BOTH LEGS RED, which is the shape the plan required.** The call pin fires first and short-circuits the test, so the count leg was measured directly:

```
call pin  : heal_task_lines = [] (required: exactly 1) -> RED
count pin : create_task count = 4 expected 5 -> RED
```

Had only the count pin red, the call predicate would have been matching something other than the heal and the lever would not have counted.

⭐ **The durable falsifier also red, and for the RIGHT reason** — `the excision removed NOTHING — the marker 'create_task(heal_mt5_terminal_session(' no longer matches the shipped wiring … a falsifier that mutates nothing always 'passes'`. That is its anti-vacuity leg biting on a source that no longer carries the wiring, exactly as designed.

**Restore, `cmp`-proven:**

```
cmp: IDENTICAL to the byte backup
sha now: 73904dd510fdc86b6be0557106eccf1c34ec25ccb6252b73634066c1fa2cbdf0
expected: 73904dd510fdc86b6be0557106eccf1c34ec25ccb6252b73634066c1fa2cbdf0
line count:      942
filtered occurrences back: 1
git status --porcelain  ->  main.py has NO entry at all
40 passed in 2.32s
```

### LEVER B — the heal itself

**Neuter:** the single line `client.initialize_with_credentials(login, password, server)` removed from `_heal_blocking`, leaving the detector call, the `-6` branch and the verdict return in place.

**Applied-proof (comments filtered, taken BEFORE anything was run):**

```
=== PRE-NEUTER MEASUREMENT (comments filtered) ===
filtered occurrences of the CALL 'client.initialize_with_credentials(': 1
filtered occurrences of the bare SYMBOL (prose in docstrings included): 2
detector call (must SURVIVE the neuter): 1
line count:      378
backup sha: 5db19f7da4cf46b48239c91d20d4809aca6c49ef60339069e81628807b604f0b

=== ASSERT THE NEUTER APPLIED (comments filtered) ===
filtered occurrences of the CALL: 0
bare SYMBOL count (docstring prose survives): 1
detector call SURVIVES (the neuter is surgical): 1
the -6 branch SURVIVES: 2
line count now:      377
cmp: DIFFER — the neuter genuinely landed
```

⭐ **THE INSTRUMENT MATTERED HERE, MEASURABLY.** Comment filtering strips `#` lines; it does **not** strip DOCSTRINGS. The module docstring names `Mt5Client.initialize_with_credentials` in prose, so the bare-symbol count fell **2 → 1**, not to 0. An agent checking "is the symbol gone?" would have read that as *the neuter did not apply* and either re-applied it or abandoned the lever. Grepping the CALL EXPRESSION — with a receiver — is what gives a clean 1 → 0.

**RED, verbatim:**

```
E       AssertionError: assert ['initialize'] == ['initialize'...credentialed']
E
E         Right contains one more item: 'initialize_credentialed'
E         Use -v to get more diff

tests/test_mt5_relogin.py:451: AssertionError
------------------------------ Captured log call -------------------------------
INFO     quantalyze.analytics.mt5_relogin:mt5_relogin.py:369 mt5 boot heal: healed
=========================== short test summary info ============================
FAILED tests/test_mt5_relogin.py::test_a_minus_six_terminal_is_healed_with_the_values_from_the_environment
FAILED tests/test_mt5_relogin.py::test_the_client_is_closed_on_every_path[healed]
FAILED tests/test_mt5_relogin.py::test_the_client_is_closed_on_every_path[heal-raises-at-the-transport]
3 failed, 37 passed in 2.47s
```

The plan names the double's recorded kwargs as the expected oracle. The `call_order` assertion sits one line above it and short-circuits, so that leg was driven directly:

```
recorded initialize kwargs : [{'timeout': 20000}]
credentialed calls recorded: []
LEG ASSERTED BY THE TEST   : len(credentialed) == 1  ->  False (RED)
```

⭐ **THE NEUTER WAS SURGICAL, and the plan's attribution condition holds.** The already-authorized case and **every** never-raises case stayed GREEN. The only three reds are the three cases that assert a credentialed call happened (`test_the_client_is_closed_on_every_path`'s two `expect_credentialed=True` parametrizations, and the criterion test itself), so the red is fully attributable to the removed call.

⭐ **AND THE LEVER MEASURED SOMETHING THE PLAN ONLY ASSERTED.** Look at the captured log: with the heal call gone, the module still emitted **`mt5 boot heal: healed`**. The verdict comes from the branch, not from the call. Any test that had used the return value or the verdict string as its oracle would have stayed GREEN over a heal that heals nothing. This is the plan's *"never assert on the heal's return value as the oracle"* demonstrated live rather than believed.

**Restore, `cmp`-proven:**

```
cmp: IDENTICAL to the byte backup
sha now: 5db19f7da4cf46b48239c91d20d4809aca6c49ef60339069e81628807b604f0b
expected: 5db19f7da4cf46b48239c91d20d4809aca6c49ef60339069e81628807b604f0b
line count:      378
the CALL is back (grepped by the newest code, not by hash alone): 1
40 passed in 2.35s
TREE-RESTORED OK
```

## Decisions Made

- **The kill switch is the FIRST statement inside the guard.** Not merely "before the transport" — before the credential read too, so the disabled path constructs nothing and reads nothing. Proved by an empty construction recorder over five off-values.
- **`_MT5_RELOGIN_LEASE_WAIT_S = 2.0`, env-overridable.** Short on purpose. The reasoning is written at the constant: a busy terminal is a terminal somebody is already successfully using, which is itself evidence the session is fine, and a timed-out acquire holds/bumps/stamps nothing so skipping costs the next holder zero.
- **`_MT5_RELOGIN_BUDGET_S` is DERIVED** from `MT5_REQUEST_TIMEOUT_S + 10.0`, the house idiom, so a retuned rpyc bound carries through instead of leaving a hardcoded ceiling to drift.
- **The verb set for the preflight gate derives from `self._assert_live`, not from `self._mt5.<x>`.** Measured: the `_assert_live` derivation yields 10 methods, the `self._mt5` one yields 5 — the latter misses every verb that reaches the session through a helper (`_raise_last`, `account_info`, `terminal_info`, `restart`, `_materialize_rows`). Floor is a hand-typed `>= 8`, never computed from the set it bounds.
- **The wiring pin carries two independent legs.** The named-call predicate AND a hand-typed `create_task` count measured independently of the heal's name — so a predicate that silently stopped matching cannot take both halves down together. Lever A confirmed both fire.
- **The durable falsifier mutates IN MEMORY only.** No byte backup, no restore, no `git checkout --` hazard in CI. Its surgical-excision proof (bounded character delta, still parses, the other four task entries survive) is what stops a green arriving from having deleted half the file.

## Deviations from Plan

### 1. [Rule 1 — a premise in the plan's `<behavior>` does not hold at HEAD] `"TRUE "` reads ON, not OFF

- **Found during:** Task 1, the first run of the kill-switch test.
- **Issue:** The plan's `<behavior>` bullet says *"unset, empty, `"1"`, `"on"`, `"TRUE "` all count as off"*, quoting `services/closed_sets.py`'s comment, which states the same in **three** separate blocks (for `sfox_enabled_server`, `mt5_enabled_server` and one more gate). The shipped code is `(os.getenv(...) or "").strip().lower() == "true"` — it **strips before it lowercases**, so `"TRUE "` and `" true"` read **ON**. Measured against the live function 2026-09-13: `'TRUE ' -> True`, `' true' -> True`, while `''`/`'1'`/`'on'`/`'false'`/unset all → `False`.
- **Assessment:** a COMMENT that is wrong, not a gate that is broken. The switch is still fail-closed for every non-`"true"` token, and whitespace tolerance on a variable a human types into a Railway text box is arguably the friendlier behaviour. But it is the opposite of what the comment promises a reader deciding whether a trailing space is safe.
- **Fix:** the test asserts the MEASURED truth. `"TRUE "` was removed from the off-list and a named test — `test_the_kill_switch_tolerates_surrounding_whitespace_MEASURED` — pins the five ON forms and records the contradiction in its docstring.
- **⛔ NOT fixed in `closed_sets.py`.** That file is outside this plan's `<files>`, the claim spans two independent shipped kill switches at three sites, and re-wording a fail-closed gate's contract from inside an unrelated execution is the class of edit this repo forbids. Booked as `[164.6.2-KILLSWITCH-COMMENT-DRIFT]` (see **Deferred / booked**).
- **Committed in:** `023b4dec`.

### 2. [Rule 2 — coverage the plan's own behaviour list implied but did not enumerate] the blank-password arm routes through the ABSENT branch, not the INVALID one

- **Found during:** Task 1.
- **Issue:** The plan groups "blank password" under *structurally invalid*. As implemented, the presence check (`not (value or "").strip()`) catches a blank or whitespace-only password first, so it reports through the **absent** arm, naming `MT5_PASSWORD`.
- **Assessment:** behaviourally equivalent for every property the plan asserts (log once, names only, no value, return, raise nothing) and strictly more useful to an operator: the absent arm names WHICH variable is empty, while the invalid arm cannot without risking a value-shaped hint. The invalid arm is still exercised — by a non-numeric login, which passes presence and fails the parse seam.
- **Fix:** none needed; recorded so the plan's literal wording is not re-applied by a later reader.
- **Committed in:** `023b4dec`.

### 3. [Housekeeping] one leftover expression cleaned before the task-2 commit

- A `task_lists[0].value.elements if False else task_lists[0].value.elts` crept into the task-list pin during drafting and was reduced to `task_lists[0].value.elts` before the commit. No behaviour change; recorded because the intermediate state existed in the working tree.

---

**Total deviations:** 3 (1 measured premise correction, 1 arm-routing note, 1 housekeeping).
**Impact on plan:** none on scope or outcome. Deviation 1 is the only substantive one and it is recorded rather than acted on, per the phase's own rule about premises that do not hold at HEAD.

## Issues Encountered

- **`python` is not on PATH on this box; `analytics-service/.venv/bin/python` is (3.12.13).** Every plan command was run with that interpreter, from `analytics-service/`. Carried forward from plan 01's SUMMARY, and it will recur in plans 03 and 04.
- **No Python linter is wired for `analytics-service/` in CI** — the gate is `mypy --strict --follow-imports=silent services/ routers/ models/` plus pytest, both clean. There is no `ruff`/`flake8` step to run.
- **`pandera` emits a `FutureWarning` on import** that buries short pytest summaries. `-p no:warnings` was used on some runs for readability only; it changes no outcome and every reading above was also obtained without it at least once.

## Known Stubs

None. Nothing in this plan is placeholder, mocked-out or deferred-in-place. The heal is complete and production-wired; the variables it reads are unset today, which is their measured state and the deployable case this plan was designed for.

## Threat Flags

None. Every surface this plan adds is inside the `<threat_model>` already written for it (`T-164.6.2-06` … `-14`). No new network endpoint, auth path, file access pattern or schema change at a trust boundary was introduced — the heal opens the SAME private-mesh RPyC transport the four shipped MT5 call sites already open, under the same lease, and adds no port, no route and no public surface.

## Deferred / booked

- **`[164.6.2-KILLSWITCH-COMMENT-DRIFT]`** — `services/closed_sets.py` claims in three comment blocks that `"TRUE "` reads OFF; the shipped code strips before it lowercases, so `"TRUE "` and `" true"` read ON. Affects `sfox_enabled_server` as well as `mt5_enabled_server`. ⛔ NOT a behaviour change to make casually — a reader who "fixes the code to match the comment" would make a trailing space fail-close a live go-live flag. The correct fix is to correct the COMMENT at all three sites. Pinned by `test_the_kill_switch_tolerates_surrounding_whitespace_MEASURED`, which reds if the SEMANTICS ever move. ⛔ Per the phase's rule, plan 04's decision checkpoint routes this to a NAMED phase via `/gsd-phase --edit`; it must never be hand-edited into the ROADMAP.
- **`main.py`'s added-line budget is spent.** The task-2 tripwire permits 2–12 added lines and the diff is exactly 12. Any future edit to that comment block must either shrink it or re-cut the bound deliberately.
- **Carried forward from plan 01, still open:** `[164.6.2-RAISE-LAST-SHAPE-ONLY]` and the second hand-spelled `f"{host}:{port}"` in `routers/exchange.py`'s MT5 lease site. Neither is touched here. ⭐ The second one is now *more* trivially closable: `mt5_terminal_key` has a second production consumer, so the de-duplication is no longer a one-call-site argument.

## ⚠️ WINDOWS ledger append REFUSED — pre-existing, not caused here

`gsd_run windows append --kind deviation` for `[164.6.2-KILLSWITCH-COMMENT-DRIFT]` was not written, for the same reason plan 01 recorded:

```
Error: Ledger counts disagree with entries: frontmatter open/waived/fixed/total=36/0/11/47
but entries yield 40/0/10/50.
```

`.planning/WINDOWS.md` is in a PRE-EXISTING inconsistent state — its frontmatter tallies do not match its own entry list, and this is the second consecutive plan to hit the identical refusal. ⛔ Nothing was written and ⛔ the tallies were NOT hand-reconciled: re-cutting a shared cross-phase register's counts on a guess, from inside an unrelated execution, is exactly the class of edit this repo forbids. The item is recorded in **Deferred / booked** above and is pinned by a live test. Whoever next runs `/gsd-ship` will hit the same refusal and should re-derive the tallies deliberately.

## Next Phase Readiness

**Wave 3 (plan 03 — the founder handover) is unblocked, and its premise is now stronger than "the code exists".**

1. The path the handover documents is **built and proven**: setting `MT5_LOGIN` / `MT5_PASSWORD` / `MT5_SERVER` on the analytics service and restarting it is the whole operator action, because the heal already runs at every boot.
2. The failure modes the handover must describe are **measured, not guessed**: variables unset → one WARNING naming the missing variables; gateway unreachable → one WARNING and the boot continues; terminal busy → skipped at a 2 s bound; a non-`-6` code → the code is named and nothing is healed.
3. **Nothing in the handover requires an agent to enter a credential** (criterion 3), and nothing in this plan did.

⚠️ Carry forward: `python` is not on PATH (use `analytics-service/.venv/bin/python`); D-06's healing-window measurement is still owed and is what closes the phase; and `[164.6.2-KILLSWITCH-COMMENT-DRIFT]` needs a destination phase at plan 04's decision checkpoint.

---
*Phase: 164.6.2 — MT5RELOGIN*
*Completed: 2026-09-13*

## Self-Check: PASSED

- All three claimed TASK commits resolve: `023b4dec`, `6d6f9e5b`, `22fa8db3`.
- All four claimed artifacts exist on disk (2 created, 2 modified).
- The commit COUNT is MEASURED, not narrated: `git rev-list --count 26952f79..HEAD` = **3** at SUMMARY-write time, against the ledger base recorded in this file's frontmatter.
- `git diff --name-only 26952f79..HEAD` = exactly the four `files_modified`, and lists NEITHER `services/job_worker.py` NOR `main_worker.py`.
- The WINDOWS append was re-attempted at SUMMARY time and refused with the byte-identical pre-existing message; `git status --porcelain -- .planning/WINDOWS.md` is empty, so nothing was written.

## State updates — ⛔ the FOURTEENTH recorded handler clobber

Per the founder's 2026-09-11 instruction the handlers were RUN FIRST and then `git diff`ed, rather than pre-emptively hand-edited. The measurement:

| Handler | Verdict |
|---|---|
| `state.advance-plan` | REFUSED with `ambiguous_position_phase` (two `Phase:` entries in Current Position) and wrote NOTHING — `git diff` was empty after it, so this is one refusal that genuinely does not write. Current Position deliberately left untouched rather than hand-reconciled inside an execution. |
| `state.update-progress` + `state.record-metric` + `state.record-session` | ⛔ CLOBBERED, byte-for-byte the same shape as wave 1's. 105 insertions / 13 deletions: the ALL-REFS-derived progress block reset from 20/171/165/54 to disk-derived 14/145/137/38, `current_phase:` MOVED to the bottom of the frontmatter, `gsd_state_version` requoted `1.0` -> `"1.0"`, **92** blank lines injected between comment lines, the body `Progress:` bar rewritten 42% -> 38%, and the `stopped_at` narrative wiped. Restored from a `cp` byte backup, `RESTORE-CMP-OK`, with `git status --porcelain` then empty for the file. |
| `roadmap.update-plan-progress 164.6.2` | ⭐ CLEAN, for the second consecutive plan. 2 insertions / 2 deletions, both wanted (`**Plans:** 1/4` -> `2/4` and the plan-02 checkbox). Kept as-is. |
| `requirements.mark-complete` | NOT RUN, deliberately — see the `requirements-completed` note in this file's frontmatter. |

Hand-applied to `STATE.md` afterwards, and nothing else. **Verified: the final diff is exactly 8 changed lines, all intended** — `state_head`, `last_updated`, `stopped_at`, `last_activity_desc`, the Performance-metrics row, the Session block's two lines, and a MEASURED census delta.

⭐ **The census delta is measured, not copied.** `completed_plans` 165 -> **166** (this plan's SUMMARY). `total_plans` is **unchanged at 171**: all four of Phase 164.6.2's PLANs were added at `2eb79d09` and were already counted into the denominator at wave 1. `completed_phases` and `percent` are **unchanged**: 164.6.2 is 2 of 4, so the phase is not complete and the phase-weighted percent cannot move.
