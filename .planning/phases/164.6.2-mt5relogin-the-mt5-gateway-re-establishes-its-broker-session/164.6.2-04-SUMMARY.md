---
phase: "164.6.2"
plan: "04"
subsystem: ops
tags: [mt5, measurement, d-06, premise-failure, deploy-ordering, prober, gate-intent-drift]

requires:
  - phase: "164.6.2"
    plan: "01"
    provides: "`initialize_with_credentials()` with by-value redaction on both failure arms — the verb the heal calls"
  - phase: "164.6.2"
    plan: "02"
    provides: "`heal_mt5_terminal_session()` and the ONE `create_task` in `main.lifespan` — ⛔ MEASURED ABSENT from the deployed sha, which is this plan's central finding"
  - phase: "164.6.2"
    plan: "03"
    provides: "the founder handover; its blocking human-action gate, which the founder CLEARED — all three variables are set"
  - phase: "164.8.3"
    provides: "the shipped `mt5-not-authorized` kind, observed working against live production here for the first time"
provides:
  - "`164.6.2-MEASUREMENT.md` — the D-06 record, carrying OUTCOME 0: the measurement was not takeable"
  - "⭐ RESEARCH assumption A5 promoted from assumption to MEASURED FACT"
  - "⛔ the deploy-ordering defect: criterion 2 closes on a production observation of code that only exists on the unmerged branch"
  - "three MEASURED defects in this plan's own verify instruments"
affects: ["164.6.2 criterion 2 (does NOT close)", "any future re-measurement wave", "164.11 DEPLOYGATE"]

actuals:
  # chars/4 over the realized diff `git diff a4475b95..HEAD`, the SAME scale the
  # plan's `estimate` used. NOT a harness token count.
  tokens: 4550
  tasks: 3   # all three — task 3 DECIDED by the founder 2026-09-14; the PHASE stays open by that decision
  # MEASURED, never narrated: `git rev-list --count a4475b95..HEAD` taken at SUMMARY
  # time, BEFORE this file's own docs commit.
  commits: 1

plan_head_before: a4475b950de661306a13a1ebe6ae3a9dc1c961c4

tech-stack:
  added: []
  patterns:
    - "Pattern: before measuring a production behaviour, PROVE the code that implements it is in the deployed artifact. `/health`'s `git_sha` plus `git grep` against that sha settles it in two commands; no artifact in this phase asked the question."

key-files:
  created:
    - .planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md
  modified: []

key-decisions:
  - "⛔ THE GATEWAY WAS DELIBERATELY NOT RESTARTED. The mechanism under test is absent from the deployed image, so the restart had exactly one possible outcome, entailed by the absence of the code rather than by anything about the broker. Recording such a run as outcome 3 would name RESEARCH assumption A3 — a broker-side block — as the live suspect against a state nobody measured. That is threat T-164.6.2-24's class applied to the measurement instead of to the remedy."
  - "⛔ OUTCOME 0 was named rather than forcing the reading into one of the plan's three endings. All three presuppose that the heal RAN; none is true here. A fourth classification is the honest answer and it is stated as such."
  - "Task 2 edited NOTHING, on an UNSETTLED question-THREE verdict — a branch the plan planned by name. Both instruments are `cmp`-proven byte-identical to their pre-edit backups."
  - "⛔ The three `check:planning-hygiene` violations were NOT hand-patched, for the fourth consecutive wave. One of them is THIS plan's own disclosure-scan command, which I RAN — editing the instrument I am measuring with, to make a different gate go green, is the hand-patch antipattern."
  - "⛔ `state.advance-plan` was NOT run and `roadmap.update-plan-progress` was NOT run. Plan 04 is not complete; its blocking decision checkpoint is open. A `Current Plan: 05` or a `4/4` is the inference the checkpoint exists to forbid, in machine-readable form."
  - "Three defects in this plan's OWN verify legs were reported, not routed around and not edited away."

patterns-established:
  - "Pattern: a gate that counts EVIDENCE REFERENCES can be satisfied by evidence of the wrong kind. `PROBER-OBSERVATION-RECORDED` requires two run references to prove a POST-RESTART dispatch was taken; four legitimate references satisfy the count while no post-restart run exists. Count the OBSERVATION, not the citations."

requirements-completed: []

coverage:
  - id: D1
    description: "The D-06 measurement is taken: the gateway is restarted and the healing window recorded with its cause."
    requirement: "MT5-GATEWAY-LOGIN-01"
    verification:
      - kind: other
        ref: "⛔ NOT TAKEN — the mechanism under test is absent from the deployed sha. Recorded as OUTCOME 0 with nine measured facts."
        status: fail
    human_judgment: true
  - id: D2
    description: "⛔ CRITERION 2 — the prober's mt5 arm returns no defect after a gateway restart with no VNC session in between."
    verification:
      - kind: other
        ref: "NOT CLOSED. No restart was performed, so no post-restart dispatch exists. Baseline prober run 34812749949 reads defect count 1."
        status: fail
    human_judgment: true
  - id: D3
    description: "⭐ RESEARCH assumption A5 — setting the Railway variables triggers an analytics redeploy — is OBSERVED rather than relied on."
    verification:
      - kind: other
        ref: "CONFIRMED as measured fact: two redeploys fired, 2026-09-14T05:58:54Z (superseded) and 06:07:35Z (SUCCESS)."
        status: pass
    human_judgment: false
  - id: D4
    description: "The founder gate is confirmed KEY-ONLY, with D-00 intact."
    verification:
      - kind: other
        ref: "MT5_LOGIN / MT5_PASSWORD / MT5_SERVER PRESENT on quantalyze-analytics production; 0 of the three on mt5-gateway. Values never entered any context — names piped through `cut -d= -f1`."
        status: pass
    human_judgment: false
  - id: D5
    description: "Question THREE is settled or explicitly recorded as unsettled, with its verdict machine-readable."
    verification:
      - kind: other
        ref: "`verdict: UNSETTLED` — `terminal_info` null on every reading, so `trade_allowed` has no readable value. R1 and R2 both unavailable."
        status: pass
    human_judgment: false
  - id: D6
    description: "⛔ The instruments stay READ-ONLY and unedited; no login by any instrument; no VNC session."
    verification:
      - kind: other
        ref: "`INSTRUMENTS-READ-ONLY OK`, `DIAG-NONEXEC-ONLY OK (31 pinned lines)`, `VERBATIM-PAIR OK (9 lines)`, `BLAST-RADIUS OK (0)`, `STALE-LICENCE OK (verdict UNSETTLED, 0 markers)`; both files `cmp`-IDENTICAL to their pre-edit backups."
        status: pass
    human_judgment: false
  - id: D7
    description: "⛔ Nothing written carries a credential, an account number, a broker server string, a terminal path or build, or an absolute home path."
    verification:
      - kind: other
        ref: "`NO-DISCLOSURE-SHAPES OK`; `check:planning-hygiene` reports ZERO findings naming the new artifact."
        status: pass
    human_judgment: false
  - id: D8
    description: "⛔ Task 3 — the blocking decision checkpoint. NOT SATISFIED, deliberately."
    verification:
      - kind: manual
        ref: "`checkpoint:decision`, `gate=blocking-human`. OPEN. Nothing auto-selected."
        status: pending
    human_judgment: true

duration: 27min
completed: 2026-09-14

# ⚠️ `status: complete` means THE AGENT-EXECUTABLE HALF IS COMPLETE — tasks 1 and 2 of 3.
# It does NOT mean the plan achieved its objective, and it emphatically does not mean
# the PHASE closed. The three keys below are the machine-readable form of that.
status: complete
blocking_gate_open: false   # the DECISION gate was ANSWERED; see `phase_closes` — the phase is held open BY the decision, not by an unanswered gate
blocking_gate_task: "Task 3 — checkpoint:decision, gate=blocking-human — DECIDED 2026-09-14: hold open on OUTCOME 0, ship, measure in a post-ship WAVE 5"
phase_closes: false
criterion_2_closed: false
---

# Phase 164.6.2 Plan 04: the D-06 measurement — Summary

**The measurement could not be taken, and the reason is a structural ordering defect no artifact in this phase recorded: criterion 2 closes on a PRODUCTION observation of code that exists only on the unmerged phase branch. The founder's variables are set, the redeploy fired and succeeded — and the deployed analytics image has no boot heal in it.**

## Performance

- **Duration:** ~27 min
- **Tasks:** 2 of 3 — ⛔ task 3 is the blocking decision checkpoint and is DELIBERATELY OPEN
- **Files:** 1 created, 0 modified
- **Commits:** 1 — MEASURED `git rev-list --count a4475b95..HEAD` at SUMMARY time (this file's own docs commit is not in that count because it did not exist when the count was taken)

## Task Commits

1. **Task 1: the D-06 measurement record** — `7721940b` (docs)
2. **Task 2: the stale account-change sentence** — ⛔ **NO COMMIT, and that is the deliverable.** The question-THREE verdict is UNSETTLED, so the task edits nothing. `git diff` over both instruments is empty by construction.

## ⛔ THE PREMISE FAILURE — the deployed analytics image has no boot heal

Nine measured facts, all re-derivable; the full table with methods is in `164.6.2-MEASUREMENT.md`.

```
/health           git_sha 5dead88a…   (= main = Phase 164.8.3's ship)
git ls-tree 5dead88a -- analytics-service/services/mt5_relogin.py   -> EMPTY (file absent)
git grep -c heal_mt5_terminal_session   5dead88a -- analytics-service/  -> no match
git grep -c initialize_with_credentials 5dead88a -- analytics-service/  -> no match
git grep -c mt5_terminal_key            5dead88a -- analytics-service/  -> no match
deployed lifespan create_task           -> 4 tasks   (dispatch, watchdog, daily_enqueue, healthz)
branch-HEAD lifespan create_task        -> 5 tasks   (+ mt5_boot_heal)
MT5_LOGIN at 5dead88a                   -> only MT5_LOGIN_TIMEOUT_MS, an unrelated IPC ceiling
analytics deployments by branch         -> {"main": 20}  of the last 20
```

**The chain, with the broken link named:**

> variables set ✅ → redeploy fires ✅ (A5 confirmed) → a new analytics process starts ✅ →
> `main.lifespan` runs ❌ **without `heal_mt5_terminal_session`** → nothing re-establishes the
> session → the `-6` stands.

⚠️ **`164.6.2-HANDOVER.md` section 3 is false OF PRODUCTION.** It promises the founder that a new analytics process starts the heal. True of the branch, false of the deployed image. The handover was careful to mark the redeploy (A5) as an assumption and was right to — what no artifact asked is **which code the redeploy deploys**. Measured across RESEARCH, CONTEXT, HANDOVER and all four PLAN files: the merge/deploy ordering appears **nowhere**.

### Why the gateway was NOT restarted

⛔ **Stated as a decision so a missing stamp never reads as an omission.** The only mechanism that could heal the session fires at ANALYTICS startup (D-08) and is not deployed. A gateway restart therefore has exactly one possible outcome, and that outcome is entailed by the absence of the code — not by the broker, not by the saved account, not by A3. It would distinguish nothing, and the terminal is already at `-6`, so it is additionally a null event against an already-lost session.

⛔ **This is OUTCOME 0, not outcome 3.** Outcome 3's whole content is *"A3 — a broker-side block — is the live suspect"*. A3 is **not** the live suspect; the measured suspect is that production runs `main`. The broker has not been tested at all, and writing A3 into the record would implicate it for the absence of our own code.

## Verify readings — Task 1 (five legs)

| Leg | Banner | Reading |
|---|---|---|
| 1 | `MEASUREMENT-RECORD-COMPLETE` | ✅ `OK (9 sections)` |
| 2 | `PROBER-OBSERVATION-RECORDED` | ✅ `OK (7 run refs)` — ⚠️ see the gate-intent finding below |
| 3 | `NO-DISCLOSURE-SHAPES` | ✅ `OK` |
| 4 | `INSTRUMENTS-READ-ONLY` | ✅ `OK` |
| 5 | `npm run check:planning-hygiene` | ⛔ **RED — exit 1, 3 violations, ALL PRE-EXISTING, ZERO naming this wave's artifact** |

Leg 2 emitted its documented stderr warning (`WARN: the record mentions an exit code`). That is **deliberate**: the record mentions an exit code only to CORRECT this plan's own baseline claim, and says in the same paragraph that the exit code is never the verdict.

## Verify readings — Task 2 (seven legs)

| Leg | Banner | Reading |
|---|---|---|
| 1 | prober self-test | ⚠️ **underlying test GREEN** (`SELF-TEST PASSED: 80/80`, **exit 0**) but the LEG's compound predicate exits 1 — see the instrument defect below |
| 2 | `npx vitest run src/__tests__/prod-prober-wiring.test.ts` | ✅ `1 passed (1)` / `87 passed (87)` |
| 3 | `STALE-LICENCE` | ✅ `OK (verdict UNSETTLED, 0 markers)` |
| 4 | `FOUR-COPIES-CORRECTED` | ✅ `SKIPPED (verdict UNSETTLED — nothing is edited)` — the documented banner |
| 5 | `VERBATIM-PAIR` | ✅ `OK (9 lines)` — matches the plan's measured baseline exactly |
| 6 | `DIAG-NONEXEC-ONLY` | ✅ `OK (31 pinned lines)` — matches the plan's measured 31 |
| 7 | `BLAST-RADIUS` | ✅ `OK (0)` |

**Pre-edit backups** (taken before anything was touched; ⛔ outside the repo), `BASE_SHA` `7721940b` recorded in the same directory BEFORE any edit:

```
cc3def93453d942cb520644e48e1c6e8f8a7b7e71db41b1fc948c10f7c2bbe15  mt5-diag.sh
f82567b5be695927d43867414d22dfe5dfbb6d111e6c7f736080bfea2b69633f  mt5.mjs

cmp $BKD/mt5.mjs      scripts/prod-prober/arms/mt5.mjs   -> arm:  IDENTICAL
cmp $BKD/mt5-diag.sh  scripts/mt5-diag.sh                -> diag: IDENTICAL
git diff --stat 7721940b -- scripts/                     -> (empty)
```

⛔ **The fence CALIBRATION (task 2 step (d)) was NOT run, correctly.** The plan scopes it to the STALE branch — *"run this only on the STALE branch, where the fence is actually doing work"*. On an UNSETTLED verdict there is no edit for the fence to license, so a neuter/restore harness would be mutating a production instrument to calibrate a gate that is guarding nothing. Recorded as deliberately skipped, with the plan's own scoping sentence as the authority.

## ⛔ THREE MEASURED DEFECTS IN THIS PLAN'S OWN VERIFY INSTRUMENTS

All three found by RUNNING the legs, all reported rather than edited away. ⛔ None was worked around.

**1. `PROBER-OBSERVATION-RECORDED` counts CITATIONS, not the OBSERVATION.** The leg requires two or more run references and exists to prove a POST-RESTART dispatch was taken. This record carries four legitimate references (one baseline dispatch, two scheduled baselines, one prior-outage citation) and **no post-restart run at all** — so the leg prints `OK (7 run refs)` while its stated intent is unmet. ⛔ The banner is NOT evidence that criterion 2 closed, and the artifact says so in bold at the point of citation.

**2. The self-test leg's `FAILED` grep is over-broad and cannot pass at its own stated baseline.** The leg is `grep -qE 'SELF-TEST PASSED: N/N' && ! grep -qi 'FAILED'`. MEASURED at HEAD on byte-unmodified files:

```
SELF-TEST PASSED: 80/80 scenarios          <- the banner the leg requires
node scripts/prod-prober/run.mjs --self-test ; exit=0    <- the plan's own stated baseline
grep -ci 'FAILED'  ->  3
```

All three matches are `ok — ` PASSING assertion labels that quote the phrase `initialize() failed` / `a failed psql`. Two of the three are **Phase 164.8.3's own (h1)/(h3) legs** — assertions that exist precisely to name the `-6` state, whose text unavoidably contains the word. The instruments are `cmp`-identical to their backups and `git diff -- scripts/` is empty, so this is **pre-existing and not a regression**: a leg that reds on a green self-test.

**3. The plan's `<measured_baseline>` says a narrowed dispatch "exits 2 BY DESIGN". MEASURED: it exits 1.** The prohibition is unaffected and still correct — the exit code is never the verdict — but a future reader checking for a `2` will not find one.

## The readings that WERE taken, and what they establish

- **Two consecutive diagnostic readings**, 06:13:24Z and 06:16:54Z, byte-identical: `initialize: false`, `last_error: [-6, "Terminal: Authorization failed"]`, `terminal_info: null`. ⛔ `-6` recorded as a STATE, not as transport noise; no IPC-timeout reading occurred at all.
- **Baseline narrowed prober, run 34812749949**, read from the published runner log: `arms: 1/4/0`, `❌ 1 defect(s)`, `mt5-not-authorized` / `mt5` / `-6`. **defect count: 1.**
- ⭐ **Phase 164.8.3's `-6` naming CONFIRMED WORKING AGAINST LIVE PRODUCTION for the first time** — the arm names the code with its own remedy instead of the error-table catch-all, and its derived clause selected the *"came back null"* wording correctly against a genuinely null `terminal_info`.
- ⭐ **A5 PROMOTED FROM ASSUMPTION TO FACT** — two analytics redeploys fired from the variable set.
- ⭐ **Two real analytics restarts observed NOT to heal the session**, with a code-level reason rather than a speculative one. When the heal does ship, these are exactly the events expected to close it.
- **The founder gate re-confirmed key-only:** all three variables PRESENT on `quantalyze-analytics` production, 0 of 3 on `mt5-gateway` — D-00 intact. ⛔ No value entered any context; names were piped through `cut -d= -f1`.

## Deviations from Plan

**1. [Rule 3 — blocking, reported not designed around] The measurement's own precondition does not hold at HEAD** — the deployed image has no heal. ⛔ Not worked around; the gateway was not restarted and no reading was forced into one of the plan's three outcomes. Recorded as OUTCOME 0.

**2. [Rule 1 — the artifact, not the gate] Run references were re-spelled into the permitted form.** First draft wrote them as `` run `NNNN` ``; the plan's permitted forms (`run id N`, `run N`, `actions/runs/N`) admit no backtick, so `PROBER-OBSERVATION-RECORDED` read 0 refs AND the disclosure mask would not have masked them. ⛔ Fixed the ARTIFACT — the gate was not touched. **Files:** `164.6.2-MEASUREMENT.md`. **Commit:** `7721940b`.

**3. [Reported, NOT fixed] The three `check:planning-hygiene` violations** — fourth consecutive wave, byte-identical list, zero naming this wave's files. One of the three is THIS plan's own disclosure-scan command, which I ran. See the routing section.

**4. [Reported, NOT fixed] The three instrument defects above.**

## ⛔ `.planning/WINDOWS.md` append REFUSED — pre-existing, FOURTH consecutive wave

```
Error: Ledger counts disagree with entries: frontmatter open/waived/fixed/total=36/0/11/47
       but entries yield 40/0/10/50.
```

Byte-identical to the message all three prior waves recorded. **Nothing was written** — confirmed by an empty `git status --short .planning/WINDOWS.md`. ⛔ Not hand-reconciled: guessing which of two disagreeing counts is right would corrupt a SHARED cross-phase register for every other phase.

## ⛔ STATE.md — the handlers that were DELIBERATELY NOT RUN, and why

Per the founder's standing *"try gsd-tools FIRST"* rule I run handlers and revert measured collateral. **Two were not run at all, and that is a decision rather than an omission:**

- **`state.advance-plan` — NOT RUN.** Advancing the plan counter past an OPEN `gate="blocking-human"` checkpoint writes the very inference the gate exists to forbid; a `STATE.md` reading `Current Plan: 05` is permission in machine-readable form. Wave 3 refused it for the same reason.
- **`roadmap.update-plan-progress` — NOT RUN.** It would move this phase to `4/4`. Plan 04 did not achieve its objective and its checkpoint is open; `4/4` beside a phase whose load-bearing criterion is unclosed is a false claim in the one table a reader trusts. ⚠️ This is the first wave to withhold it — it was clean three waves running, and it is being withheld on what it would ASSERT, not on how it behaves.

The open gate is recorded as a BLOCKER instead.

## Self-Check: PASSED

- `164.6.2-MEASUREMENT.md` — FOUND on disk, 282 lines.
- Commit `7721940b` — resolves in `git log --oneline --all`.
- Commit count MEASURED, not narrated: `git rev-list --count a4475b95..HEAD` = 1.
- Both instruments `cmp`-proven byte-identical to their pre-edit backups; `git diff -- scripts/ .github/ analytics-service/` empty.
- ⛔ No VNC session. No login by any instrument. No credential entered, read back or requested. No account number, broker server string, terminal `path`/`build` or absolute home path in anything written.

## ⛔ STOPPED AT THE GATE — task 3 is OPEN

Task 3 is a `checkpoint:decision` with `gate="blocking-human"`. `workflow.auto_advance` does not apply to a `blocking-human` gate and **nothing was auto-selected**. ⛔ ROADMAP criterion 2 does **not** close and the phase does **not** close.

---

## Task 3 — DECISION (founder, 2026-09-14): ANSWERED. The phase is HELD OPEN *by* the decision.

⭐ **The recommended FOURTH option was taken: hold 164.6.2 OPEN on OUTCOME 0, ship the branch, take the real measurement as a post-ship WAVE 5.** Criterion 2 is UNCHANGED; only its precondition is now explicit.

⛔ **Both plan options were REJECTED, and for the reasons the measurement gave:** `window-acceptable` presupposes a measured window that does not exist; `hold-open-outcome-3` blames a broker-side block (A3) for our own absent code.

⛔ **The keepalive is NOT booked.** No window was measured, and choosing an interval now is precisely the guess D-06 exists to prevent. It becomes bookable only once wave 5 produces a number.

**DECISION 2 — `[164.6.2-RAISE-LAST-SHAPE-ONLY]` does NOT go to 164.6 GATE-HYGIENE.** The kind-mismatch argument was accepted: it edits `Mt5Client.login()` on the live job-processing path, while 164.6's remaining scope is lint rules, tests and prose corrections; bundling a live-path code edit there would let one red gate block three unrelated items. It stays in 164.6.2 as a post-ship plan of its own — ⛔ explicitly NOT inside wave 5, because wave 5's instruments must stay READ-ONLY.

### Routed deferrals — every one entered via `/gsd-phase --edit`, none hand-edited

| Item | Destination | Landed as |
|---|---|---|
| `[164.6.2-KILLSWITCH-COMMENT-DRIFT]` | **164.6 GATE-HYGIENE** (Queued) | criterion 17 |
| `[164.6.2-PLAN04-GATE-INTENT-DRIFT]` | **164.8.4 GATERESIDUE** (Queued) | criterion 10 — all three instrument defects recorded concretely enough to act on without re-deriving |
| Gateway image digest pin | **164.11 DEPLOYGATE** (Queued) | criterion 7 |
| The deploy-observation dependency | **164.11 DEPLOYGATE** | criterion 8 — named so wave 5's precondition has an owner |
| `[164.6.2-CRITERION2-NEEDS-DEPLOY]` | **164.6.2** (held open) | criterion 6 |
| Wave 5, the post-ship measurement | **164.6.2** | criterion 7 |
| Question THREE (UNSETTLED) | **164.6.2** | criterion 8 — carries task 1(e)'s exact procedure |
| `[164.6.2-RAISE-LAST-SHAPE-ONLY]` | **164.6.2**, its own post-ship plan | criterion 9 |
| Keepalive / reconnect hook | ⛔ **NOT BOOKED** | no window measured — booking an interval now is the guess D-06 forbids |
| `initialize()` idempotence | **Phase 155 (MT5-VERIFY)** | already owned; no new destination needed |

⛔ **No blank destination, and nothing booked into a shipped phase.** 164.8.3 is Complete 4/4 and received nothing. Target statuses were checked before writing: 164.6 `empty`, 164.8.4 `no_directory`, 164.11 `empty` (all future, editable), 164.6.2 `partial` → in_progress, edited with `--force` and the warning recorded.

**Workflow gates honoured, both directions:**

```
milestone-scope BEFORE : complete / 37 phases
milestone-scope AFTER  : complete / 37 phases   -> UNCHANGED, write accepted (no rollback)
roadmap validate       : {"warnings": []}
insertion audit        : all 8 new criteria land at the END of their phase's numbered
                         Success Criteria list and BEFORE **Requirements** — verified
                         line by line, NOT assumed from the diff hunk header, which
                         labelled three of the four hunks `Plans:` and would have been
                         read as a misplacement
```

### ✅ `[164.6.2-HYGIENE-PROSE-SELF-TRIP]` — CLOSED by the founder, not by me

Commit `a2ac07ae`. `npm run lint` is **GREEN**: `check-planning-hygiene OK — 6385 tracked files scanned`.

⛔ **A RESPELLING, NOT A RELAXATION, and it was proven rather than asserted.** The two RUNNABLE regexes had their literal needle respelled to a bracket form matching identical language; the prose hit was spelled escaped, the remedy the linter's own message names. The founder re-calibrated the full disclosure gate four ways afterwards — all three legitimate run-id forms GREEN, a bare 8-digit account shape RED, an absolute home path RED, a `"build"` key RED.

⭐ **Consequence for this plan: Task 1's verify is now FULLY GREEN.** Re-run against the plan file *as it now stands*:

```
leg 3  NO-DISCLOSURE-SHAPES OK          (extracted verbatim from the UPDATED plan) exit 0
leg 5  npm run check:planning-hygiene   exit 0   <- was the ONLY red leg
```

### ⚠️ `164.6.2-HANDOVER.md` section 3 CORRECTED

It promised the founder that a new analytics process starts the heal — **true of the branch, false of production**. Section 3 now states the deploy precondition explicitly: A5 is marked CONFIRMED rather than assumed; the deployed sha `5dead88a` is named with what is absent from it; and ⛔ *"nothing heals until this phase's branch is MERGED to `main` AND that merge is OBSERVED to have DEPLOYED"* — with 164.11's five skipped deployments carried as the reason **merging is necessary but not sufficient**. Sections 1, 2, 4 and 5 are untouched and the `MT5_ENABLED` / D-02 paragraphs are preserved.

## ⛔ THE SEVENTEENTH STATE.md CLOBBER — the corruption is DETERMINISTIC

`state.add-roadmap-evolution` ×4 and `state.add-blocker` ×1 were RUN first per the founder's standing rule, each against a byte backup, each diffed.

| Handler | Collateral measured |
|---|---|
| `state.add-roadmap-evolution` ×4 | 92 injected blank lines, 1 blank deleted. ⭐ **The `progress:` block was NOT recomputed** (37/20/171/166/54 preserved) and no prose was corrupted — a measured DEPARTURE from the three prior recordings of this verb, each of which reset progress to disk-derived values. The standing warning is narrower than it reads. |
| `state.add-blocker` ×1 | ⛔ **The same four lines inverted AGAIN** — `none delegated` → `delegated`, `None is CARDINAL` → `is CARDINAL`, both `Round N found none` mangled; `none` count 29 → 25; 92 blank lines injected. **Wave 3 repaired these exact four lines and the handler re-broke them**, so the corruption is deterministic and recurs on every call. |

**Remedy both times:** restored from a `cmp`-proven byte backup, then hand-applied only the intended keys. Final reading: **6 insertions / 1 deletion**, 0 injected blank lines, `none` back at 29, all four historically-inverted phrases verified present **by grep, not by hash alone**, progress keys preserved, and no placeholder text leaked. The single deletion is a deliberate amendment of two now-stale claims in this wave's own blocker (the checkpoint is no longer OPEN; the hygiene blocker is lifted).

⛔ `state.advance-plan` and `roadmap.update-plan-progress` were **still not run** — the founder confirmed the refusal. The phase is held open by decision, so `4/4` would remain a false claim.

## ⛔ `.planning/WINDOWS.md` — refusal recorded a FOURTH time

Byte-identical message, nothing written, not hand-reconciled. The defect is now four waves old.
