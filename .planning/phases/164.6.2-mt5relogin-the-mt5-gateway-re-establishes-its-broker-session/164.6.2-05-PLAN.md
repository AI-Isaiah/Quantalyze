---
phase: "164.6.2"
plan: "05"
type: execute
wave: 5
depends_on: ["164.6.2-04"]
gap_closure: true
files_modified:
  - .planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT-WAVE5.md
  # Task 3 routes via `/gsd-phase --edit`, which writes the ROADMAP. Declared because the wave
  # guard reads frontmatter. ⛔ Nothing under scripts/, .github/, analytics-service/, deploy/ or
  # docs/ is touched by this plan — this is a MEASUREMENT wave, not a code wave.
  - .planning/ROADMAP.md
autonomous: false
requirements: ["MT5-GATEWAY-LOGIN-01"]

estimate:
  # Long wall-clock, small diffs: ~20-35 diagnostic polls, two prober dispatches with log-archive
  # reads, one (possibly two) service restarts, and one record. Calibration factor 1 (0 samples),
  # so tokens == raw_tokens. Confidence is DERIVED from the sample count, not self-rated.
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "⛔ CRITERION 2 CLOSES ON AN OBSERVATION, NEVER ON THE CODE (D-06): the mt5-gateway service is RESTARTED and a POST-RESTART narrowed prober dispatch is observed to report `defect count: 0` for the mt5 arm, read from the PUBLISHED RUNNER LOG ARCHIVE, with NO VNC session and NO login by any instrument or agent before, during or after. That prohibition IS the criterion."
    - "⛔ THE TWO PRECONDITIONS ARE RE-ASSERTED AT EXECUTION TIME, not trusted from any brief: (1) the phase's merge commit `1f938d6d` is an ancestor of `origin/main`; (2) the analytics `/health` `git_sha` names a commit that CONTAINS the heal — `git merge-base --is-ancestor 1f938d6d <deployed_sha>` exits 0 AND `git ls-tree <deployed_sha> analytics-service/services/mt5_relogin.py` is non-empty. ⛔ NOT string equality with the merge commit: the deployed sha is a DESCENDANT (`f901e4da`, PR #798), equality was only ever a proxy for containment, and a reader who 'fixes' the check back to equality reds a correct deploy."
    - "⛔ THE OBSERVATION IS COUNTED, NOT THE CITATIONS (closing `[164.6.2-PLAN04-GATE-INTENT-DRIFT]` defect (a) for THIS record): the post-restart run id is written on its own machine-readable line, its `createdAt` is proven to be AFTER the restart stamp, it differs from the baseline dispatch, and it does not appear anywhere in the OUTCOME 0 record. A gate that counts run references was satisfied by four citations and zero observations; this one cannot be."
    - "⛔ IT IS IMPOSSIBLE TO PRODUCE A GREEN READING THAT DOES NOT MEAN THE CRITERION CLOSED: `outcome: CLOSED` requires `post-restart defect count: 0` plus a recorded delta, cause and window class; `outcome: OPEN` requires a NON-ZERO count taken AFTER a confirmed analytics restart with the heal's own verdict line recorded; `outcome: UNREADABLE` forbids any count and names the instrument; `outcome: PREMISE-FAILED` forbids a restart stamp. One gate enforces the biconditional."
    - "⛔ AN UNREADABLE READING IS NOT A VERDICT. An empty `/health`, a diagnostic that returns no `PROBE` line twice, a dispatch whose log archive cannot be fetched — each is recorded as UNREADABLE with the instrument named, never as a stale, failing or `-6` state. This repo fixed the identical conflation in the deploy probe in PR #798 (an empty `/health` was reported as a stale-deploy mismatch); this plan does not reproduce that shape."
    - "⛔ D-06's WINDOW IS A MEASURED NUMBER WITH A MEASURED CAUSE: the restart stamp, the first authorized reading, the delta, and WHAT CLOSED IT — attributed to a named event (the terminal's own saved-session re-authorization after the gateway restart, an analytics boot heal from a restart that happened anyway, or the operator's analytics restart), with the heal's own log verdict line (`mt5 boot heal: healed` / `already_authorized` / `not_healed:<reason>`) as the evidence, never a guess. The keepalive interval is NOT chosen here (D-06): it becomes bookable only once this record carries a number, and only by the founder at Task 3."
    - "⭐ QUESTION THREE (D-09) RIDES ALONG WITH PLAN 04 TASK 1(e)'s EXACT PROCEDURE: R1 = `trade_allowed` immediately after the FIRST reading that shows the terminal authorized; R2 = after a REAL per-account login, with the event that produced R2 named. A second boot heal is the credentialed-initialize form and only a PROXY — an R2 from it is recorded as a proxy reading and the verdict stays UNSETTLED. `terminal_info()` still null ⇒ UNSETTLED again, and all FOUR copies of the stale sentence stay byte-unchanged; 'no change needed until the reading exists' is a planned outcome (D-09)."
    - "⛔ THE INSTRUMENTS STAY READ-ONLY AND UNEDITED, PROVEN THREE WAYS. `scripts/mt5-diag.sh`, the prober arm and the runner are not modified — not even a comment — and at every gate: `git status --porcelain` over `scripts/ .github/ analytics-service/ deploy/ docs/` is empty (uncommitted edits), `git diff --quiet f901e4da HEAD` over the same roots is clean (COMMITTED edits — executors commit after every task, and a porcelain-only check was MEASURED to print OK after a committed edit), and both D-09 sentence sites hash to their planning-time blobs (a laundered base). A wave that both mutates and measures production destroys the observation criterion 2 exists to make (ROADMAP criterion 9's reasoning, applied here). Any correction the reading LICENSES (D-09 on a STALE verdict) is routed at Task 3 to its own plan, never performed inside this one — and that routing is honest only because this gate can fail."
    - "⛔ HISTORY STAYS AS WRITTEN: `164.6.2-MEASUREMENT.md` (the OUTCOME 0 record) is hash-pinned byte-identical; the wave-5 record is a NEW file that SUPERSEDES it by name. Plan 04's `<measured_baseline>` 'exits 2 BY DESIGN' sentence is not edited — its exact semantics are recorded in the SUMMARY beside the routed defect instead."
    - "⛔ NO CREDENTIAL, ACCOUNT NUMBER, BROKER SERVER STRING, TERMINAL PATH/BUILD OR ABSOLUTE HOME PATH reaches the record. Only the five permitted diagnostic fields are written; every run reference is in one of the three maskable forms; the heal's log line is quoted only through its `<reason>` segment, never its `<detail>` segment, because `mt5linux` f-string-interpolates arguments into remotely-eval'd code (T-134-01) and any captured diagnostic text is treated as potentially credential-bearing until scanned."
  artifacts:
    - .planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT-WAVE5.md
  key_links:
    - "`/health` `git_sha` -> `git merge-base --is-ancestor 1f938d6d <sha>` + `git ls-tree <sha> analytics-service/services/mt5_relogin.py` -> the running analytics process CONTAINS `heal_mt5_terminal_session` -> a gateway restart is an informative event. Without this link the restart has exactly one outcome and the measurement is OUTCOME 0 again."
    - "`railway restart -s mt5-gateway -y` -> the terminal container restarts -> `./scripts/mt5-diag.sh` (repo root) reads `initialize` / `last_error` / `terminal_info` / `connected` / `trade_allowed` at ~2-minute granularity, second-consecutive rule -> the delta between the restart stamp and the first authorized reading IS D-06's number."
    - "`gh workflow run prod-prober.yml -f arm=mt5` -> `gh run list -w prod-prober.yml --json databaseId,createdAt,event` (the `workflow_dispatch` run created after the dispatch stamp) -> `gh api repos/{owner}/{repo}/actions/runs/<id>/logs` (a zip; `0_probe.txt` is the probe job's full log) -> the region after the execution-only line `NARROWED DIAGNOSTIC dispatch of arm mt5:` carries exactly one `prod-prober: NARROWED DIAGNOSTIC run of arm mt5` banner, one `arms:` line and either `❌ N defect(s):` or `No defects in the narrowed scope.` -> `post-restart defect count: N`. ⛔ Never the exit code, never the run conclusion."
    - "`railway logs -s quantalyze-analytics -e production -n 200 --filter '\"mt5 boot heal\"'` -> the heal's own lines (`starting —`, then exactly one of `healed` / `already_authorized` / `not_healed:<reason>:<detail>` / `skipped —` / `ABANDONED`) with their stamps -> WHAT CLOSED IT, attributed rather than inferred. `railway deployment list --service quantalyze-analytics --environment production --json` -> whether an analytics restart occurred inside the window at all."
  prohibitions:
    - "⛔ Never open a VNC session, never log in, never ask the founder to, and never make either instrument log in. A hand-established session destroys the observation criterion 2 exists to make; D-06 rejected prober self-healing explicitly."
    - "⛔ Never read the narrowed run's EXIT CODE or the run's `conclusion` as the verdict. MEASURED at HEAD in `scripts/prod-prober/run.mjs` (symbol `exitCode`): a narrowed run exits 2 when it found NO defects and 1 when it found defects — so the exit code is never 0 and never the reading. Scheduled runs are additionally posture-flipped to exit 0 so an hourly red check cannot deadlock Railway's wait-for-CI; their `conclusion: success` proves nothing."
    - "⛔ Never `gh run view --log` — it TRUNCATES (measured 2026-09-04, STATE.md). Fetch the run's log ARCHIVE. ⛔ Never grep the WHOLE log for a defect kind: the self-test step prints fixture names such as `mt5-not-authorized` before the probe step runs; anchor on the execution-only line and read the region after it."
    - "⛔ Never edit `scripts/mt5-diag.sh`, `scripts/prod-prober/arms/mt5.mjs`, `scripts/prod-prober/run.mjs`, `.github/workflows/prod-prober.yml`, anything under `analytics-service/`, or any historical `.planning/` record. `Mt5Client.login` and `[164.6.2-RAISE-LAST-SHAPE-ONLY]` are OUT of this wave (ROADMAP criterion 9, founder decision 2, 2026-09-14)."
    - "⛔ Never paste the diagnostic's `path` or `build` fields, a `not_healed:…:<detail>` segment, a raw rpyc traceback, an account number, a broker server string, or an absolute home path into the record. `.planning/` is TRACKED in a PUBLIC repo."
    - "⛔ Never treat a single reading as a state. A missing `PROBE` line in the first minutes after the restart is the container starting, not a reading; a single `-10005`/`-10004` is transport noise; the SECOND consecutive reading is the confirmation."
    - "⛔ Never restart the analytics service twice, never add a retry, never loop. The active phase is ONE restart; a still-red terminal after it is `outcome: OPEN`, and its tie-breaker (the terminal's Journal tab) needs VNC — the founder, not an agent."
    - "⛔ Never `railway up` from local (it uploads the working tree, not the merged commit); never a local `uvicorn main:app` (it claims REAL prod compute jobs); `pytest` is not needed here and runs ONLY from `analytics-service/`."
    - "⛔ Never book the keepalive interval, never re-litigate criterion 1's shape, never hand-edit a deferral into the ROADMAP — every routed item goes through `/gsd-phase --edit` with a NAMED destination."
    - "⛔ Never cite `file:line` — cite BY SYMBOL (`[164.7-CITATION-DRIFT-01]`)."
---

<objective>
Close ROADMAP criterion 2 — the phase's load-bearing criterion — on an OBSERVATION: restart the
`mt5-gateway` Railway service with no human in the loop and observe the prober's mt5 arm return no
defect afterwards, honestly recording the D-06 window (how long, and what closed it) and taking
question THREE's `trade_allowed` readings along the way.

Purpose: plan 04 could not take this measurement — the deployed analytics image was `5dead88a`,
which carries no heal, so the restart had exactly one uninformative outcome and was correctly NOT
performed (OUTCOME 0). The founder's decision of 2026-09-14 was to hold the phase open, ship, and
measure post-ship as this wave. The branch has since MERGED (PR #797 → `1f938d6d`) and the merge has
been OBSERVED to DEPLOY — but only via a DESCENDANT commit (PR #798 → `f901e4da`), because Railway
SKIPPED the deployment of `1f938d6d` itself. That last fact is why the precondition is re-asserted
here at execution time rather than trusted.

Output: `164.6.2-MEASUREMENT-WAVE5.md` — a record in the same nine-section shape as the OUTCOME 0
record it supersedes, carrying a machine-readable outcome that a gate proves consistent with the
readings; a founder decision on the measured window; and every item the reading licenses routed to
a named destination.

⛔ **THIS IS A MEASUREMENT WAVE, NOT A CODE WAVE.** Nothing under `analytics-service/`, `scripts/`,
`.github/`, `deploy/` or `docs/` is edited. The deliverable is an observation, including an honest
negative. Per D-06, D-08 and D-09 as recorded in `164.6.2-CONTEXT.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-CONTEXT.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-RESEARCH.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-VERIFICATION.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-04-PLAN.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-04-SUMMARY.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-HANDOVER.md
</context>

<measured_baseline>
Measured during planning, 2026-09-14, at HEAD `f901e4da` (= `origin/main`). ⛔ Every live figure
here is a DATED reading; Task 1 re-measures all of it before anything is restarted.

| Fact | Value | How measured |
|---|---|---|
| The phase's merge commit | `1f938d6d` (PR #797, v0.77.43.0) | `git log --oneline -3 origin/main` |
| The follow-up that actually deployed | `f901e4da` (PR #798, v0.77.43.1 — the deploy-probe fix: an unreadable `/health` is UNKNOWN, not a stale deploy) | same |
| ⛔ Precondition 1 — merged | `git merge-base --is-ancestor 1f938d6d origin/main` → exit 0 | run at planning time |
| ⛔ Precondition 2 — deployed AND contains the heal | `/health` at `2026-09-14T15:11:36Z`: `status ok`, `git_sha f901e4da99cd100fa912e296b87b3e2c296d38b7`, `config_ok true`, worker age ~20 s. `git merge-base --is-ancestor 1f938d6d f901e4da` → exit 0. `git ls-tree f901e4da analytics-service/services/mt5_relogin.py` → blob `e55dbe0a04103e32535b6a0a93a329cbc79daeaa` | `curl` + `git` at planning time |
| ⚠️ THE DEPLOYGATE INSTANCE, measured not narrated | `railway deployment list` for analytics, production: `1f938d6d` **SKIPPED** at `2026-09-14T12:54:46Z`; `f901e4da` **SUCCESS** at `2026-09-14T14:08:11Z`; `5dead88a` REMOVED (was current at `06:07:35Z`) | `railway deployment list --service quantalyze-analytics --environment production --json` |
| ⭐ Consequence for the baseline | The analytics process running now BOOTED at ~`14:08Z` WITH the heal present. Its `mt5 boot heal:` lines are the first production heal ever — so the pre-event diagnostic may ALREADY read authorized. That is a PRE-EVENT finding about the prior `-6` outage, NOT the criterion-2 observation, and it is labelled as such | reasoning from the two rows above; Task 1 measures it |
| The restart verb | `railway restart -s mt5-gateway -y` — *"Restart the latest deployment of a service (without rebuilding)"*; `-y` skips the confirmation dialog | `railway restart --help` |
| The terminal oracle | `./scripts/mt5-diag.sh` from the repo ROOT (the Railway link is directory-scoped; `railway status` at the root names project `quantalyze-analytics`, environment `production`). Prints ONE `PROBE` line with `initialize`, `last_error`, `terminal_info`, and — when readable — `trade_allowed`, `tradeapi_disabled`, `connected`, `build`, `path`. On no line it prints `mt5-diag: no PROBE line returned — the gateway may be down, or rpyc unreachable` and exits 1 | script read at HEAD; `railway status` |
| The criterion's own oracle | `gh workflow run prod-prober.yml -f arm=mt5` — inputs `mode` (default `probe`) and `arm` (string, default empty) | workflow read at HEAD |
| ⛔ Narrowed exit-code semantics, EXACT | `scripts/prod-prober/run.mjs`, symbol `exitCode`: `defects.length > 0 ? 1 : narrowed ? 2 : 0`. So a narrowed run exits **1 with defects, 2 with none, never 0**. Plan 04's `<measured_baseline>` said "exits 2 BY DESIGN" and plan 04 measured 1 — BOTH were reading the same rule from different states. The prohibition (never the exit code) is unaffected | `run.mjs` read at HEAD |
| ⛔ Scheduled-run conclusions are NOT readings | The probe step's POSTURE LINE (plan 164.1-06) exits 0 on scheduled runs so an hourly red check-run cannot deadlock Railway's wait-for-CI. `gh run list` shows scheduled runs `conclusion: success` at `08:19:58Z` and `02:30:34Z` on a day the terminal read `-6` at `06:16Z` | `prod-prober.yml` read at HEAD; `gh run list` |
| How to read a published runner log | `gh api repos/{owner}/{repo}/actions/runs/<id>/logs > run.zip` (37 KB for the baseline run); `unzip -p run.zip 0_probe.txt` is the probe job's full log, each line prefixed by a timestamp. ⛔ `gh api …/actions/jobs/<id>/logs` REFUSES without `--allow-escape-sequences`; ⛔ `gh run view --log` TRUNCATES | tested against the baseline run |
| ⛔ Whole-log greps are POLLUTED | The self-test step prints `mt5-not-authorized` and even a `prod-prober: NARROWED DIAGNOSTIC run of arm …` banner for its own scenarios (banner count 2 in the baseline log; the real one is the second). The workflow's execution-only line `NARROWED DIAGNOSTIC dispatch of arm mt5:` appears exactly ONCE (the printed script source carries `${ARM}` instead); the region after it holds exactly one banner, one `arms:` line, and the table | `awk`/`grep` over the baseline archive |
| The region extraction, PROVEN on the baseline archive | `awk '/NARROWED DIAGNOSTIC dispatch of arm mt5:/{f=1} f'` → `prod-prober: NARROWED DIAGNOSTIC run of arm mt5` / `arms: 1/4/0` / `❌ 1 defect(s):` / `mt5-not-authorized  mt5  -6` → count 1. The zero-defect form in narrowed mode is the line `No defects in the narrowed scope.` (symbol `defects.length === 0` branch in `run.mjs`) | run against run 34812749949's archive |
| The run's creation stamp | `gh run view <id> --json createdAt,event,conclusion` → e.g. `2026-09-14T06:16:19Z`, `workflow_dispatch` | `gh` at planning time |
| The heal's log vocabulary (by symbol, `analytics-service/services/mt5_relogin.py`) | `heal_mt5_terminal_session` logs `mt5 boot heal: starting — …` (INFO, always followed by a second line), then exactly one of: `mt5 boot heal: healed` / `mt5 boot heal: already_authorized` (INFO, `_VERDICT_HEALED` / `_VERDICT_ALREADY_AUTHORIZED`), `mt5 boot heal: not_healed:<reason>:<detail>` (WARNING, `_VERDICT_NOT_HEALED_PREFIX`; reasons `ipc_fault`, `credential_refused`, `heal_sent_ipc_fault_on_reprobe`, `still_unauthorized`), `mt5 boot heal: skipped — …` (INFO: kill switch, or terminal busy — D-29), `mt5 boot heal: ABANDONED at the Ns budget` (ERROR), or the catch-all `mt5 boot heal did not complete`. The task is `create_task(…, name="mt5_boot_heal")` in `main.lifespan`. ⛔ The `<detail>` segment carries scrubbed exception text — never quote it | module read at HEAD |
| Reading those lines | `railway logs -s quantalyze-analytics -e production -n 200 --filter '"mt5 boot heal"'` (`-n` disables streaming; `--filter` is Railway's text search) | `railway logs --help` |
| The heal's budget ceiling | `_MT5_RELOGIN_BUDGET_CEILING_S = 300.0` — the active phase's poll window need not exceed a few minutes past this | module read at HEAD |
| The OUTCOME 0 record's blob, for the history pin | `164.6.2-MEASUREMENT.md` hashes to `1c331ad19b12dac6ce8d9f1ea7b714427b85ddfb` at HEAD | `git rev-parse HEAD:<path>` |
| The scheduled baseline | hourly, `cron: "0 * * * *"`, observed to drift (a run at `08:19:58Z`) | workflow + `gh run list` |
| Tooling auth | `railway whoami` → logged in; `gh auth status` → logged in, scopes include `workflow`; repo `AI-Isaiah/Quantalyze` | run at planning time |
| ⛔ The read-only BASE for the committed-edit check | `f901e4da` — the wave branch `phase-164.6.2-wave5` forks from it (`git merge-base origin/main HEAD` at planning time), it is the planning-time HEAD, the deployed sha and `origin/main`. ⛔ `1f938d6d` is NOT the branch point: pinning to it would admit PR #798's diff under the read-only roots. `git diff --quiet f901e4da HEAD -- scripts/ .github/ analytics-service/ deploy/ docs/` → exit 0 at planning time | `git merge-base`, `git diff --quiet` |
| The two D-09 sentence sites, hash-pinned | `scripts/mt5-diag.sh` → blob `5114fd798ffe392813e5336a679e1c6f9fbd2aaf`; `scripts/prod-prober/arms/mt5.mjs` → blob `aa56210c4663e884f9c31979b2224b0462da34c3` — identical at `f901e4da` and at HEAD | `git rev-parse <ref>:<path>` |
| ⛔ Why the porcelain check alone was NOT a gate | MEASURED by the plan checker: `echo "# edited" >> scripts/mt5-diag.sh && git commit -am neuter` leaves `git status --porcelain` at 0 lines, and executors commit after every task. The committed-edit check and the hash pins exist because of that measurement | checker probe, 2026-09-14 |
| Real `workflow_dispatch` prober runs usable to calibrate the GitHub cross-check | run 34706551355 (`2026-09-12T16:52:37Z`) and run 34620923255 (`2026-09-11T16:15:19Z`) — both `workflow_dispatch`, both prod-prober, NEITHER cited in the OUTCOME 0 record | `gh run list -w prod-prober.yml --json databaseId,createdAt,event` |

⚠️ **RESEARCH assumption A3 is the one thing that can keep criterion 2 open after a REAL measurement:**
the design assumes the `-6` is the saved account being refused, not a broker-side block on the account.
Plan 04 could not test it — the heal was not deployed. This wave CAN: if the heal runs
(`mt5 boot heal: starting —` observed) and reports `not_healed:credential_refused` or
`not_healed:still_unauthorized`, A3 becomes a live suspect against a state that was actually measured —
which is exactly what OUTCOME 0 could not say. The tie-breaker stays the terminal's Journal tab, which
needs VNC, i.e. the founder.

⚠️ **RESEARCH assumption A5 is a measured fact, twice over:** setting the variables fired a redeploy
(plan 04), and the merge fired a deploy that was SKIPPED and then a deploy that SUCCEEDED (this table).
The ROADMAP names both A3 and A5 by letter; nothing here re-derives them.
</measured_baseline>

<the_outcomes>
## This plan has FOUR legitimate endings, each with a machine-readable `outcome:` line. None is "keep trying".

- **`outcome: CLOSED`** — the post-restart narrowed prober reports `post-restart defect count: 0` for the
  mt5 arm, created AFTER the restart stamp, with no VNC and no login by anyone. ⛔ **Criterion 2 CLOSES.**
  The record carries `delta:`, `what closed it:` and `window class:` — `BOUNDED` (something closed it
  inside the passive window without an operator restart of analytics: the terminal's own saved-session
  re-authorization after the gateway restart, or an analytics boot heal from a restart/deploy that
  happened anyway, each attributed by its log line and stamp) or `UNBOUNDED-WITHOUT-ANALYTICS-RESTART`
  (the passive window expired red and the operator's analytics restart's boot heal closed it — RESEARCH's
  expected answer, and D-06 working as designed, not a failure). Which class it is decides Task 3's
  question, not the closure.
- **`outcome: OPEN`** — the passive window expired red AND the active phase ran (ONE analytics restart,
  `mt5 boot heal: starting —` observed in the new process's log) AND the post-restart narrowed prober still
  reports `mt5-not-authorized` (`post-restart defect count:` ≥ 1). ⛔ Criterion 2 does NOT close and the
  phase does NOT close. The heal's own verdict line is the discriminator and is recorded:
  `not_healed:credential_refused` / `not_healed:still_unauthorized` ⇒ A3 (a broker-side block, or a
  wrong env triple — a founder-side check, criterion 3) is the live suspect against a MEASURED state;
  `not_healed:ipc_fault` / `heal_sent_ipc_fault_on_reprobe` ⇒ an IPC fault class this phase does not
  own — record it; `ABANDONED` ⇒ unknowable from the log, and the ONE permitted re-measure is a second
  diagnostic poll, never a second restart. Hand the founder the Journal-tab tie-breaker. Do not loop.
- **`outcome: UNREADABLE`** — the CLOSING instrument (the post-restart narrowed prober) could not be read:
  the dispatch could not be created, the run never appeared, or its log archive could not be fetched or
  carries no anchored region. ⛔ NOT a verdict, NOT a `-6`, NOT stale. Record `unreadable instrument:` and
  stop. (A diagnostic that returns no `PROBE` line is recorded as UNREADABLE in the poll log for THAT
  reading; the outcome still follows the prober. An unreadable `/health` in Task 1 is
  `precondition deployed: UNREADABLE` and also ends in this outcome — the deployed commit is UNKNOWN,
  not known-wrong.)
- **`outcome: PREMISE-FAILED`** — Task 1's re-assert found the merge not on `origin/main`, or the deployed
  sha not containing the heal. ⛔ The gateway is NOT restarted (`restart stamp: NONE`), exactly as plan 04
  did; the live suspect is deploy ordering (`[164.6.2-CRITERION2-NEEDS-DEPLOY]`, owner Phase 164.11
  DEPLOYGATE), never A3.

⛔ In OPEN, UNREADABLE and PREMISE-FAILED the question-THREE verdict is UNSETTLED unless R1 was readable
before the event, and no instrument is ever edited on any outcome in this wave.
</the_outcomes>

<tasks>

<task type="auto">
  <name>Task 1: re-assert the two preconditions at execution time, then take the pre-event baseline</name>

  <precondition>`railway whoami` succeeds, `gh auth status` succeeds, and `railway status` run from the repo ROOT names project `quantalyze-analytics` / environment `production` (the diagnostic script's own stated need — the link is directory-scoped). If any of the three fails, halt: nothing below is readable without them.</precondition>

  <read_first>
    - `164.6.2-MEASUREMENT.md` — the OUTCOME 0 record, in full: its nine sections are the SHAPE the new record takes, and its `Prober run id` section carries the run ids the observation gate must NOT find re-used.
    - `164.6.2-04-PLAN.md` — `<measured_baseline>` and task 1 sub-tasks (a) and (f): the five permitted diagnostic fields, the three permitted run-reference forms, and the nine section labels.
    - `164.6.2-HANDOVER.md` section 3 — the corrected precondition prose and why merging is necessary but not sufficient.
    - This plan's `<measured_baseline>` and `<the_outcomes>`.
    - `scripts/mt5-diag.sh` header (READ-ONLY, why, repo ROOT).
  </read_first>

  <files>.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT-WAVE5.md</files>

  <action>
    ⛔ **Re-measure, do not trust.** Every figure in `<measured_baseline>` is a dated reading and the
    deployment that matters was SKIPPED once already today. Nothing is restarted in this task.

    **(a) Precondition 1 — the merge is on `main`.** `git fetch origin main`, then
    `git merge-base --is-ancestor 1f938d6d origin/main`. Exit 0 ⇒ write `precondition merged: PASS`;
    otherwise `precondition merged: FAIL`, and this record ends at `outcome: PREMISE-FAILED` with
    `restart stamp: NONE` — stop here, write the record's frontmatter and sections with the reason, and
    do not proceed to Task 2.

    **(b) Precondition 2 — the merge is DEPLOYED, i.e. the running analytics process CONTAINS the heal.**
    Read `curl -fsS --max-time 20 https://quantalyze-analytics-production.up.railway.app/health` and
    parse `git_sha`, `status` and `config_ok` with `node -e` (⚠️ `python` is NOT on PATH; if a Python is
    needed anywhere use `analytics-service/.venv/bin/python`). Record the UTC stamp of the read.
    ⛔ **The check is CONTAINMENT, not equality.** Assert BOTH: `git merge-base --is-ancestor 1f938d6d
    <git_sha>` exits 0, AND `git ls-tree <git_sha> -- analytics-service/services/mt5_relogin.py` prints a
    blob. ⚠️ State this reasoning in the record so the next reader does not "fix" the check back to
    `git_sha == 1f938d6d`: the deployed sha is EXPECTED to be a descendant (`f901e4da` — PR #798 landed
    after Railway skipped `1f938d6d`), equality was only ever a proxy for the property that matters, and
    an equality check reds a correct deploy. If the sha is not known locally, `git fetch origin main`
    first; a sha still unknown after that is recorded as `precondition deployed: FAIL` (a deploy of
    something not on `main` is its own finding), never worked around.
    ⛔ **An unreadable `/health` is UNKNOWN, not FAIL.** If the body is empty, not JSON, or `git_sha` is
    absent/empty/`unknown`, retry up to three times at 60-second spacing (the deploy-verify workflow's own
    convergence shape). Still unreadable ⇒ `precondition deployed: UNREADABLE`, `outcome: UNREADABLE`,
    `unreadable instrument: analytics /health`, and stop — do not restart anything, and do not write
    `-6`, "stale" or "not deployed" anywhere: PR #798 fixed this exact conflation in the deploy probe.
    Write `deployed sha: <the full 40-hex git_sha>` on its own line (the disclosure scan masks 40-hex; in
    prose use the 8-character short form). Also record `status`, `config_ok` and whether the deployed sha
    is the merge commit itself or a descendant (informational).

    **(c) The pre-event diagnostic baseline — TWO consecutive readings.** From the repo ROOT run
    `./scripts/mt5-diag.sh`, stamp it, wait ~2 minutes, run it again. Record ONLY `initialize`,
    `last_error`, `terminal_info` (null or not), and — when readable — `connected` and `trade_allowed`.
    ⛔ Never `path`, never `build` — they carry a production filesystem path and a terminal build into a
    PUBLIC tracked file. Write `baseline authorized: true` if BOTH consecutive readings show
    `initialize: true`, `false` if both show `initialize: false`, and `UNREADABLE` if either produced no
    `PROBE` line (record the script's own error sentence, which carries no secret).
    ⭐ **Expect `true` to be possible, and label it correctly if it is.** The running process booted at
    ~`14:08Z` today with the heal present (see `<measured_baseline>`). If the baseline is authorized,
    that is a PRE-EVENT finding — the boot heal at deploy time most plausibly closed the `-6` that stood
    since at least 2026-09-13 — and it is evidence for the deploy-restart path of D-06, NOT the
    criterion-2 observation, which requires the gateway restart in Task 2. Attribute it: read
    `railway logs -s quantalyze-analytics -e production -n 200 --filter '"mt5 boot heal"'` and record the
    verdict line's stamp and its prefix through the `<reason>` segment only (e.g. `mt5 boot heal: healed`
    or `mt5 boot heal: not_healed:credential_refused`) — ⛔ never the `<detail>` segment, never any other
    log line, and scan what you quote for disclosure shapes before pasting. If the baseline is authorized
    it also carries `trade_allowed`; that reading is the CANDIDATE for question THREE's R1 (Task 2 assigns
    R1 by the rule "the FIRST reading that shows the terminal authorized").

    **(d) The pre-event prober baseline — ONE narrowed dispatch.** Stamp, then
    `gh workflow run prod-prober.yml -f arm=mt5`. Find the run with
    `gh run list -w prod-prober.yml --json databaseId,createdAt,event,status --limit 5` — the
    `workflow_dispatch` run whose `createdAt` is after your stamp — then `gh run watch <id> --exit-status`
    (⛔ a non-zero exit here is BY DESIGN: 1 with defects, 2 without; it is not the reading). Fetch the
    archive: `gh api repos/{owner}/{repo}/actions/runs/<id>/logs > "$TMPDIR/prober-baseline.zip"` (outside
    the repo), `unzip -p … 0_probe.txt`, strip the leading timestamp column, then extract the region
    AFTER the execution-only line `NARROWED DIAGNOSTIC dispatch of arm mt5:` and read from it: the
    `arms:` line, and either `❌ N defect(s):` with the mt5 rows or `No defects in the narrowed scope.`.
    ⛔ Whole-log greps are polluted by the self-test step's fixture names — anchor first. Write
    `baseline run id <id>` and `baseline defect count: N` (or `UNREADABLE` with the reason if the archive
    could not be fetched or carries no anchored region). Quote only the `arms:` line and the defect-table
    rows for the mt5 arm; scan them before pasting.
    ⚠️ The hourly SCHEDULED run's `conclusion: success` is posture-flipped and is NOT a reading; do not
    cite it as one.

    **(e) Write the record — a NEW file, never an edit of the OUTCOME 0 record.** Create
    `164.6.2-MEASUREMENT-WAVE5.md` with frontmatter `phase: "164.6.2"`, `plan: "05"`, `kind: measurement`,
    `supersedes: 164.6.2-MEASUREMENT.md` (OUTCOME 0 — the measurement that could not be taken),
    `measured_by: agent (no VNC session, no login, no credential read)`, `measured_on: 2026-09-14`,
    `branch_head: <short sha>`, `deployed_analytics_sha: <short sha>`. Then a `## Preconditions` section
    carrying the machine-readable lines `precondition merged: PASS|FAIL`,
    `precondition deployed: PASS|FAIL|UNREADABLE`, `deployed sha: <40-hex>`, the containment reasoning,
    and a `## Baseline (pre-event)` section carrying `baseline authorized: true|false|UNREADABLE`,
    `baseline run id <id>`, `baseline defect count: N`, the attribution from (c) if any, and the first
    rows of the `## Poll log` table in EXACTLY this row shape so the gates can count them:
    `| N | \`YYYY-MM-DDTHH:MM:SSZ\` | \`./scripts/mt5-diag.sh\` | reading |` (instrument column is the
    literal script path in backticks; prober rows use `narrowed prober, run <id>`; `/health` rows use
    `analytics /health`). On PASS/PASS the remaining sections are written by Task 2; on any stop outcome
    write all nine sections now (`restart stamp: NONE`, stated as a decision with its reasoning, exactly
    as OUTCOME 0 did) plus `outcome:`, for UNREADABLE `unreadable instrument:`, AND the sentence "No VNC
    session was opened before, during or after; no login was performed by any instrument or agent" —
    the closure gate requires the phrase "no VNC session" on EVERY path, a stop included.
    ⛔ **THE COLUMN-1 RULE FOR EVERY MACHINE-READABLE LINE, and it is what keeps the gates honest.** Each
    `token: VALUE` field the gates read — `precondition merged:`, `precondition deployed:`,
    `deployed sha:`, `baseline authorized:`, `baseline run id`, `baseline defect count:`,
    `restart stamp:`, `outcome:`, `unreadable instrument:` and, in Task 2, the rest — is written EXACTLY
    ONCE, at column 1 of its own line, with no list marker, no bold, no backticks around the token.
    The gates extract with `^token:` and take the first match, so prose may discuss any token freely
    mid-sentence (e.g. "an `outcome: PREMISE-FAILED` would have meant …") but ⛔ never at column 1 in
    the `token: VALUE` shape — a column-1 prose line is read as the value.
    ⛔ **Run references** are written ONLY as `run id NNNNNNNNNNN`, `run NNNNNNNNNNN` or a full
    `actions/runs/NNNNNNNNNNN` URL — the disclosure scan masks exactly those, and ONLY when the run is
    TEN or more digits (a GitHub run id is eleven), so an eight-digit account number is never masked
    even behind a marker; every other six-or-more-digit run is banned. `baseline run id <id>` and
    `post-restart run id <id>` both satisfy the mask. ⛔ No account number, broker server string,
    absolute home path, terminal path or build anywhere.
  </action>

  <verify>
    <automated>PD=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session; F=$PD/164.6.2-MEASUREMENT-WAVE5.md; test -f "$F" || { echo "MISSING ARTIFACT: $F"; exit 1; }; PM=$(grep -m1 -oiE '^precondition merged:[[:space:]]*(PASS|FAIL)' "$F" | grep -oiE 'PASS|FAIL' | tr '[:lower:]' '[:upper:]'); PDP=$(grep -m1 -oiE '^precondition deployed:[[:space:]]*(PASS|FAIL|UNREADABLE)' "$F" | grep -oiE 'PASS|FAIL|UNREADABLE' | tr '[:lower:]' '[:upper:]'); test -n "$PM" -a -n "$PDP" || { echo "PRECONDITION-LINES-MISSING (merged='$PM' deployed='$PDP') — both must be column-1 lines"; exit 1; }; if test "$PM" != PASS -o "$PDP" != PASS; then grep -qiE '^outcome:[[:space:]]*(PREMISE-FAILED|UNREADABLE)' "$F" || { echo "PRECONDITION-NOT-PASS-BUT-NO-STOP-OUTCOME: merged=$PM deployed=$PDP and no column-1 PREMISE-FAILED/UNREADABLE outcome line"; exit 1; }; grep -qiE '^restart stamp:[[:space:]]*NONE' "$F" || { echo "STOP-OUTCOME-WITHOUT-RESTART-STAMP-NONE: merged=$PM deployed=$PDP"; exit 1; }; grep -qi 'no vnc session' "$F" || { echo "STOP-OUTCOME-WITHOUT-NO-VNC-STATEMENT"; exit 1; }; fi; if test "$PM" = PASS; then git merge-base --is-ancestor 1f938d6d origin/main || { echo "MERGE-CLAIM-FALSE: 1f938d6d is not an ancestor of origin/main"; exit 1; }; fi; if test "$PDP" = PASS; then S=$(grep -m1 -oE '^deployed sha:[[:space:]]*[0-9a-f]{40}' "$F" | grep -oE '[0-9a-f]{40}'); test -n "$S" || { echo "DEPLOYED-SHA-NOT-RECORDED: precondition deployed is PASS but no column-1 40-hex deployed sha line exists"; exit 1; }; git cat-file -e "$S^{commit}" 2>/dev/null || { echo "DEPLOYED-SHA-UNKNOWN-LOCALLY: $S (git fetch origin main first)"; exit 1; }; git merge-base --is-ancestor 1f938d6d "$S" || { echo "DEPLOY-DOES-NOT-CONTAIN-THE-MERGE: 1f938d6d is not an ancestor of $S"; exit 1; }; LT=$(git ls-tree "$S" -- analytics-service/services/mt5_relogin.py) || { echo "LS-TREE-FAILED: could not read the tree at $S"; exit 1; }; printf '%s' "$LT" | grep -q . || { echo "HEAL-MODULE-ABSENT-AT-DEPLOYED-SHA: $S"; exit 1; }; grep -qiE 'descendant|containment' "$F" || { echo "CONTAINMENT-REASONING-MISSING: the record must say in words why the check is descendant-plus-blob and not equality"; exit 1; }; fi; echo "PRECONDITION-REASSERTED OK (merged=$PM deployed=$PDP)"</automated>
    <fails_when>non-zero exit, or the printed line is not a `PRECONDITION-REASSERTED OK` banner. ⛔ This leg RE-RUNS the two git assertions against the sha the record claims was deployed, so a record that says PASS on a stale brief reds. It reds on `MERGE-CLAIM-FALSE` if the merge is not on `origin/main`; on `DEPLOY-DOES-NOT-CONTAIN-THE-MERGE` if the deployed sha does not descend from `1f938d6d` — ⭐ which is the containment check, so a descendant such as `f901e4da` PASSES and an equality check would not have; on `HEAL-MODULE-ABSENT-AT-DEPLOYED-SHA` if the blob is missing (the OUTCOME 0 state); on `CONTAINMENT-REASONING-MISSING` if the record carries the command but not the words; and — whenever EITHER precondition is not PASS, including a `FAIL`/`PASS` pair — on a record that was not turned into an explicit stop outcome with `restart stamp: NONE` and the no-VNC statement. A precondition failure that is not written down is the premise failure this phase already had once. ⚠️ Every extraction is anchored at column 1 (`^token:`), so prose that mentions a token mid-sentence cannot be mistaken for the value.</fails_when>

    <automated>PD=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session; F=$PD/164.6.2-MEASUREMENT-WAVE5.md; PM=$(grep -m1 -oiE '^precondition merged:[[:space:]]*(PASS|FAIL)' "$F" | grep -oiE 'PASS|FAIL' | tr '[:lower:]' '[:upper:]'); PDP=$(grep -m1 -oiE '^precondition deployed:[[:space:]]*(PASS|FAIL|UNREADABLE)' "$F" | grep -oiE 'PASS|FAIL|UNREADABLE' | tr '[:lower:]' '[:upper:]'); if test "$PM" != PASS -o "$PDP" != PASS; then echo "BASELINE-RECORDED SKIPPED (merged=$PM deployed=$PDP — no event will be performed, so no baseline is required)"; exit 0; fi; grep -qiE '^baseline authorized:[[:space:]]*(true|false|UNREADABLE)' "$F" || { echo "BASELINE-AUTHORIZED-LINE-MISSING (column-1 line required)"; exit 1; }; BR=$(grep -m1 -oE '^baseline run id[[:space:]]+[0-9]{10,}' "$F" | grep -oE '[0-9]{10,}'); test -n "$BR" || { echo "BASELINE-RUN-ID-MISSING: the pre-event narrowed dispatch must be recorded as a column-1 'baseline run id N' (a GitHub run id is 10+ digits)"; exit 1; }; grep -qiE '^baseline defect count:[[:space:]]*([0-9]+|UNREADABLE)' "$F" || { echo "BASELINE-DEFECT-COUNT-MISSING"; exit 1; }; command -v gh >/dev/null || { echo "UNREADABLE-FOR-GATE: gh is absent, so the baseline run cannot be cross-checked against GitHub — this gate does not pass on an uncheckable claim"; exit 1; }; J=$(gh run view "$BR" --json createdAt,event,workflowName 2>/dev/null) || { echo "UNREADABLE-FOR-GATE: gh run view $BR failed — the recorded baseline run cannot be confirmed to exist"; exit 1; }; printf '%s' "$J" | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const j=JSON.parse(s);if(j.event!=="workflow_dispatch"){console.log("BASELINE-RUN-NOT-A-DISPATCH: GitHub says event="+j.event);process.exit(1)}if(!/prod-prober/.test(j.workflowName||"")){console.log("BASELINE-RUN-WRONG-WORKFLOW: "+j.workflowName);process.exit(1)}})' || exit 1; n=$(grep -cE '^\| *[0-9]+ *\| *`20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z` *\| *`\./scripts/mt5-diag\.sh`' "$F"); test "$n" -ge 2 || { echo "BASELINE-POLL-ROWS-INSUFFICIENT: $n stamped diag rows, the second-consecutive rule needs two"; exit 1; }; echo "BASELINE-RECORDED OK ($n diag rows; run $BR confirmed on GitHub as a prod-prober workflow_dispatch)"</automated>
    <fails_when>non-zero exit, or the printed line is not a `BASELINE-RECORDED OK` banner (or the documented SKIPPED banner when either precondition is not PASS). Fewer than two stamped diagnostic rows means a single reading was treated as a state; a missing `baseline run id` means the pre-event prober reading was never taken, and Task 2's observation gate needs it to prove the post-restart id is a DIFFERENT dispatch. ⛔ The recorded run id is cross-checked against GitHub (`gh run view`): it must exist, be a `workflow_dispatch`, and belong to the prod-prober workflow — a mis-copied or invented id, or an absent `gh`, is reported LOUDLY as `UNREADABLE-FOR-GATE`, never passed.</fails_when>

    <automated>PD=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session; F=$PD/164.6.2-MEASUREMENT-WAVE5.md; M=$(sed -E 's#(actions/runs/|[Rr]un id:?[[:space:]]+|[Rr]un[[:space:]]+)[0-9]{10,}#RUNREF#g; s#[0-9a-f]{40}#SHA40#g' "$F" | grep -nE '[0-9]{6,}|/Us[e]rs/|/home/[a-z]|"path"|"build"' || true); if test -n "$M"; then printf '%s\n' "$M"; echo "DISCLOSURE-SHAPE FOUND"; exit 1; fi; echo "NO-DISCLOSURE-SHAPES OK"</automated>
    <fails_when>non-zero exit, or the printed line is not `NO-DISCLOSURE-SHAPES OK`. A six-or-more-digit run outside a run-reference marker is the shape of an account number; the two quoted field names are the diagnostic's production filesystem path and terminal build; the two path prefixes are absolute home paths. ⭐ The mask is plan 04's with TWO tightenings: a run-reference marker masks only a TEN-or-more-digit run (a GitHub run id is eleven digits; an eight-digit account number written as `run 12345678` is NOT masked and still reds), and a 40-hex string (the deployed `git_sha`, which the precondition leg needs in full) is masked as `SHA40` — an eight-digit account number is not 40 characters and stays banned. ⛔ If this reds, remove the disclosure from the artifact — never widen the mask.</fails_when>

    <automated>BASE=f901e4da; ROOTS="scripts/ .github/ analytics-service/ deploy/ docs/"; S=$(git status --porcelain -- $ROOTS) || exit 1; n=$(printf '%s' "$S" | grep -c .); test "$n" -eq 0 || { printf '%s\n' "$S"; echo "INSTRUMENT-MODIFIED-UNCOMMITTED: $n path(s) changed under a read-only root"; exit 1; }; git cat-file -e "$BASE^{commit}" 2>/dev/null || { echo "BASE-UNRESOLVABLE: $BASE is not a commit in this checkout"; exit 1; }; if ! git diff --quiet "$BASE" HEAD -- $ROOTS; then git --no-pager diff --stat "$BASE" HEAD -- $ROOTS; echo "INSTRUMENT-MODIFIED-COMMITTED: a read-only root differs between the wave's branch point $BASE and HEAD"; exit 1; fi; for pin in "scripts/mt5-diag.sh 5114fd798ffe392813e5336a679e1c6f9fbd2aaf" "scripts/prod-prober/arms/mt5.mjs aa56210c4663e884f9c31979b2224b0462da34c3"; do set -- $pin; H=$(git hash-object "$1"); test "$H" = "$2" || { echo "D09-SITE-CHANGED: $1 hashes to $H, not to its planning-time blob $2"; exit 1; }; done; echo "INSTRUMENTS-READ-ONLY OK (porcelain clean; $BASE..HEAD clean over the read-only roots; both D-09 sites hash-pinned)"</automated>
    <fails_when>non-zero exit, or the printed line is not an `INSTRUMENTS-READ-ONLY OK` banner. This wave READS; any modification under those roots means an instrument or the thing being measured was changed mid-measurement, which invalidates every reading taken after it. ⛔ **Three checks, because one was measured not to bite.** The porcelain check catches an UNCOMMITTED edit only; GSD executors commit after every task, so `echo "# edited" >> scripts/mt5-diag.sh && git commit -am neuter` left porcelain at 0 and the earlier one-check form printed OK. The second check pins HEAD against the wave's BRANCH POINT — `f901e4da`, which is the planning-time HEAD, the deployed sha and `origin/main` at fork time; ⛔ `1f938d6d` is NOT the branch point and would admit PR #798's diff — so a committed edit anywhere under the read-only roots reds with `INSTRUMENT-MODIFIED-COMMITTED`. The third check hash-pins BOTH D-09 sentence sites against their planning-time blobs, so re-pinning `BASE` to launder a committed edit reds with `D09-SITE-CHANGED`. ⚠️ If the wave branch is ever rebased onto a `main` that legitimately touched those roots, this gate reds — report it and let the founder re-pin deliberately; never re-pin silently.</fails_when>

    <automated>PD=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session; O=$PD/164.6.2-MEASUREMENT.md; test -f "$O" || { echo "OUTCOME-0-RECORD-MISSING"; exit 1; }; H=$(git hash-object "$O"); test "$H" = 1c331ad19b12dac6ce8d9f1ea7b714427b85ddfb || { echo "HISTORY-REWRITTEN: $O hashes to $H, not to its HEAD blob"; exit 1; }; echo "OUTCOME-0-RECORD-UNTOUCHED OK"</automated>
    <fails_when>non-zero exit, or the printed line is not `OUTCOME-0-RECORD-UNTOUCHED OK`. The OUTCOME 0 record is a correct account of what production ran on 2026-09-14 morning; the wave-5 record SUPERSEDES it by name, it does not overwrite it. ⛔ Never clear this by re-pinning the hash.</fails_when>

    <automated>npm run check:planning-hygiene</automated>
    <fails_when>non-zero exit. Fix the artifact, ⛔ never the gate.</fails_when>
  </verify>

  <acceptance_criteria>
    - Both preconditions were RE-MEASURED at execution time and their results written as machine-readable lines; the deployed sha was asserted to CONTAIN the merge (descendant + blob), never to EQUAL it, and the record states why.
    - An unreadable `/health` was recorded as UNREADABLE, never as stale or not-deployed; a failed precondition ended the plan at `outcome: PREMISE-FAILED` with `restart stamp: NONE` and no restart.
    - Two consecutive pre-event diagnostic readings and one pre-event narrowed prober dispatch are recorded with stamps, the prober verdict read from the log ARCHIVE's anchored region — never the exit code, never a scheduled run's conclusion.
    - If the baseline is already authorized, it is labelled a PRE-EVENT finding attributed to a named heal log line and stamp, and explicitly NOT the criterion-2 observation.
    - Only the five permitted diagnostic fields appear; every run reference is in a maskable form; nothing under the read-only roots changed; the OUTCOME 0 record is byte-identical.
  </acceptance_criteria>

  <done>
    `164.6.2-MEASUREMENT-WAVE5.md` exists with its preconditions and baseline sections, all six verify
    banners pass, and the record either says both preconditions PASS (Task 2 proceeds) or carries a
    complete stop outcome (Task 2 does not run and the reason is written down).
  </done>
</task>

<task type="auto">
  <name>Task 2: the event, the passive window, the closing observation, question THREE — and the record</name>

  <precondition>The wave-5 record carries `precondition merged: PASS` AND `precondition deployed: PASS` (Task 1's gate re-proved both against `origin/main` and the recorded sha). On any other pair this task does NOT run: the record already ends at a stop outcome, and restarting a gateway against an absent heal is OUTCOME 0 repeated.</precondition>

  <read_first>
    - `164.6.2-04-PLAN.md` task 1 sub-tasks (b) through (f) — the procedure this task carries, and the `<the_three_outcomes>` block whose endings map onto `<the_outcomes>` here.
    - `164.6.2-MEASUREMENT.md` — `Restart stamp`, `What closed it` and `Prober run id` as written for OUTCOME 0, so the new sections are written in the same register and the gate's "not a citation" check has its set of ids.
    - `164.6.2-RESEARCH.md` — question FIVE (the procedure and its three bites), question THREE (R1/R2, what would settle it), Pitfall 6 (a null before a heal is not a measurement), assumption A3.
    - `.planning/milestones/v1.17-phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md` Row 6 — the counter-evidence question THREE is measured against.
    - `analytics-service/services/mt5_relogin.py` — `heal_mt5_terminal_session`'s docstring and the verdict constants, so the log lines are recognised by name and the `<detail>` segment is recognised as the part never to quote.
    - This plan's `<measured_baseline>` (the proven region extraction) and `<the_outcomes>`.
  </read_first>

  <files>.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT-WAVE5.md</files>

  <action>
    ⛔ **No VNC session and no login by any instrument or agent, before, during or after. That prohibition
    IS the criterion.** An agent that logs in to make the reading green has destroyed the observation.

    **(a) THE EVENT UNDER TEST.** Take the UTC stamp with `date -u +%Y-%m-%dT%H:%M:%SZ` and immediately run
    `railway restart -s mt5-gateway -y` (the restart verb — criterion 2's verb; not `redeploy`). Write
    `restart stamp: <ISO>` on its own line. Record the command's output only if it carries no service id
    or URL beyond the service name.

    **(b) THE PASSIVE WINDOW — does anything close it without an operator restart of analytics?** Poll
    `./scripts/mt5-diag.sh` from the repo ROOT at ~2-minute granularity for a BOUNDED window stated in the
    record before the first poll: **30 minutes** (RESEARCH's figure — enough to separate "something closed
    it" from "nothing will"). Each reading is a stamped `## Poll log` row in the prescribed shape. Rules:
    - In the first minutes after the restart the script may print `mt5-diag: no PROBE line returned` — that
      is the container starting, recorded as `no reading (gateway starting)`, not as a state.
    - A single `-10005` or `-10004` is transport noise; a single anything is not a state. A state is
      recorded only on the SECOND consecutive reading that agrees.
    - If a reading shows `initialize: true`, take the next reading ~2 minutes later; if it agrees, write
      `first authorized reading: <stamp of the FIRST of the two>` and stop the passive poll.
    - If the window expires with the last two consecutive readings still `initialize: false`, write
      `first authorized reading: NONE (passive window)` and proceed to (c).
    In parallel, at the start and end of the window (and whenever a reading flips), correlate:
    `railway deployment list --service quantalyze-analytics --environment production --json` for any
    analytics deployment or restart inside the window (stamp + short sha + status only), and
    `railway logs -s quantalyze-analytics -e production -n 200 --filter '"mt5 boot heal"'` for heal
    lines with stamps. ⭐ If the terminal came back authorized WITHOUT any analytics restart in the window
    and WITHOUT a new heal line, the closer is the terminal's own saved-session re-authorization after
    the gateway restart — write that, with the absence of both as the evidence. If a heal line with a new
    stamp appears, the closer is that boot heal, and the deployment row that caused it is named.

    **(c) THE ACTIVE PHASE — only if the passive window expired red.** This is the mechanism the design
    relies on (D-08: the heal fires at ANALYTICS startup, nowhere else), and measuring its latency is the
    second half of D-06. Take a stamp, run `railway restart -s quantalyze-analytics -y` ONCE, and write
    `analytics restart stamp: <ISO>`. Wait for `/health` to answer again with the same `git_sha` (the new
    process is up), then read `railway logs … --filter '"mt5 boot heal"'` until the new process's
    `mt5 boot heal: starting —` line and its second line appear (the budget ceiling is 300 s; allow ~7
    minutes). Write `heal verdict line: <the line's prefix through the reason segment>` — one of
    `mt5 boot heal: healed`, `mt5 boot heal: already_authorized`, `mt5 boot heal: not_healed:<reason>`,
    `mt5 boot heal: skipped — <first clause>`, `mt5 boot heal: ABANDONED`, or `mt5 boot heal did not
    complete`. ⛔ Never the `<detail>` segment after the reason's colon, never adjacent log lines. Then poll
    the diagnostic as in (b) for up to 10 minutes with the same second-consecutive rule; on an
    authorized pair write `first authorized reading:` with the FIRST stamp of the pair. ⭐ If it closes
    here, the honest entry for what closed it is "the boot heal at the operator restart of the analytics
    service", the window class is `UNBOUNDED-WITHOUT-ANALYTICS-RESTART`, and that is RESEARCH's expected
    finding — D-06 working as designed, and a finding, not a failure. ⚠️ A production restart fences
    in-flight jobs by the worker's own claim discipline (plan 04 accepted this shape); do it once, at a
    moment `/health`'s worker is ticking, and never a second time.
    ⚠️ If the terminal is STILL not authorized after the confirmed restart with a verdict line recorded,
    that is heading for `outcome: OPEN`. Stop restarting. Take the closing observation in (d) anyway —
    the record must carry the arm's own verdict on the end state whatever that state is.

    **(d) THE CLOSING OBSERVATION — the PROBER's, not the diagnostic's.** Criterion 2 closes on *observing
    the prober's mt5 arm return no defect*; `scripts/mt5-diag.sh` is a different instrument on a different
    path, and the arm being credential-less BY DESIGN is exactly why its reading is the one that closes
    the criterion. After the first authorized reading — or after the active phase concluded either way —
    stamp, dispatch `gh workflow run prod-prober.yml -f arm=mt5`, find the `workflow_dispatch` run created
    after your stamp with `gh run list -w prod-prober.yml --json databaseId,createdAt,event,status
    --limit 5`, `gh run watch <id> --exit-status` (non-zero by design), then read
    `gh run view <id> --json createdAt` and fetch the archive with
    `gh api repos/{owner}/{repo}/actions/runs/<id>/logs > "$TMPDIR/prober-post.zip"`. Extract the region
    after the execution-only line `NARROWED DIAGNOSTIC dispatch of arm mt5:` from `0_probe.txt` (strip the
    timestamp column first) and read from it ONLY: the `arms:` line and either `❌ N defect(s):` with its
    mt5 rows or `No defects in the narrowed scope.`. Write, each on its own line:
    `post-restart run id <id>`, `post-restart run created: <createdAt>`, `post-restart defect count: N`
    (0 is the closing reading; a non-zero N on OPEN is just as required — the gate checks that a count was
    RECORDED, never that it was favourable). Quote the `arms:` line and the mt5 rows verbatim in the
    `Prober verdict` section after scanning them. ⛔ Never the exit code (1 with defects, 2 without — by
    `run.mjs`'s own rule), never the run's `conclusion`, never a scheduled run's result.
    ⚠️ If the dispatch cannot be created, the run never appears within 10 minutes, the archive cannot be
    fetched, or the archive carries no anchored region: `outcome: UNREADABLE`,
    `unreadable instrument: narrowed prober (<which step>)`, no `post-restart defect count:` line at all.
    ⚠️ If the arm and the diagnostic DISAGREE, that disagreement is the finding — record both readings and
    their stamps and do not pick a winner. The outcome line follows the PROBER (it is the criterion's
    instrument) and the disagreement is stated beside it.

    **(e) THE NUMBER AND THE SENTENCE (D-06).** Write `delta: HH:MM:SS` (first authorized reading minus
    restart stamp; `UNDEFINED` if never authorized), `what closed it: <one sentence naming the event and
    its evidence>` (or `NOTHING — still open at the last reading`), and `window class: BOUNDED |
    UNBOUNDED-WITHOUT-ANALYTICS-RESTART | N/A`. ⛔ A window recorded without what closed it is the implicit
    number D-06 forbids. ⛔ The keepalive interval is NOT chosen or proposed here — the number is presented
    to the founder at Task 3, and only then does a keepalive become bookable (D-06, and the founder's
    2026-09-14 decision that it is not booked until a number exists).

    **(f) QUESTION THREE — plan 04 task 1(e)'s EXACT procedure, carried, not re-derived (D-09).**
    Immediately after the FIRST reading that shows the terminal authorized (which may be the pre-event
    baseline from Task 1 — the rule is the first authorized reading, wherever it fell), record that
    reading's `trade_allowed` as `R1: true|false` (or `R1: UNAVAILABLE` with the reason — `terminal_info`
    null throughout). Then wait, bounded, for a REAL per-account login to occur — a scheduled derive or
    balance job, an allocator read, or a key validation — evidenced by analytics log lines or a deployment-
    independent job event, and run the diagnostic again: `R2: true|false` with `R2 event: per-account login
    — <the job or event> at <stamp>`. ⛔ Do NOT trigger a login by logging in yourself, and do NOT make
    either instrument log in. ⚠️ A second BOOT HEAL is the credentialed-initialize form and only a PROXY
    for the event the contested sentence is about — if the only event after R1 was a boot heal, write the
    reading but as `R2: <value>` with `R2 event: boot heal (PROXY) at <stamp>` and the verdict stays
    UNSETTLED with the proxy reading recorded. Bound the wait at 30 minutes past the closing observation;
    if no real per-account login occurred, write `R2: UNAVAILABLE`, `R2 event: none within the bounded
    wait`, and `r2 pending: a real per-account login is expected at the next scheduled MT5 job; take R2
    then` — the reading can be taken later by whoever executes the licensed correction, because the four
    copies are not edited in this wave. Write the verdict on its own line, in exactly one of three forms:
    `verdict: STALE` (R1 true and R2 true from a REAL per-account login — the sentence is stale and the
    auto-disable boxes are still unticked), `verdict: CURRENT` (R2 false where R1 was true — the hazard is
    live), `verdict: UNSETTLED` (either reading unavailable, or R2 only from a proxy — say why). ⛔ In
    this wave the verdict LICENSES nothing here: all four copies of the sentence stay byte-unchanged
    whatever it says (INSTRUMENTS-READ-ONLY), and a STALE verdict is routed at Task 3.

    **(g) THE ARTIFACT — the same nine sections as OUTCOME 0, plus the wave-5 lines.** Complete
    `164.6.2-MEASUREMENT-WAVE5.md` with these exact section labels (the completeness gate greps each by
    name): `Restart stamp`, `First authorized reading`, `Delta`, `What closed it`, `Prober run id`,
    `Prober verdict`, `Question THREE verdict`, `Outcome`, `Poll log`. Inside them, on their own lines,
    the machine-readable fields above, plus `outcome: CLOSED|OPEN|UNREADABLE|PREMISE-FAILED` in the
    `Outcome` section and a sentence containing the phrase "no VNC session" stating that none was opened
    and no login was performed by any instrument or agent. ⛔ Task 1(e)'s COLUMN-1 RULE applies to every
    field here too — `restart stamp:`, `first authorized reading:`, `delta:`, `what closed it:`,
    `window class:`, `analytics restart stamp:`, `heal verdict line:`, `post-restart run id`,
    `post-restart run created:`, `post-restart defect count:`, `R1:`, `R2:`, `R2 event:`, `verdict:`,
    `outcome:`, `unreadable instrument:` — exactly once each, column 1, plain; discuss them in prose
    anywhere else, never in that shape at column 1. Carry the raw stamped readings in the poll log,
    the deployment events inside the window for correlation, and — as an observation, not a task — the
    env-account-vs-connected-key note plan 04 carried forward (⛔ NOT determinable by an agent: it needs
    the value of `MT5_LOGIN`, which criterion 3 forbids reading; a founder-side check). Name what this
    wave learned about plan 04's own instruments without editing them: gate (a) is fixed HERE by counting
    the observation; gate (b)'s over-broad `FAILED` grep is not reused because no instrument is edited and
    no self-test is needed; gate (c)'s "exits 2" is EXACTLY "2 with zero defects, 1 with defects" per
    `run.mjs`'s `exitCode` rule — recorded for `[164.6.2-PLAN04-GATE-INTENT-DRIFT]` (Phase 164.8.4) via the
    SUMMARY, not as a new deferral. ⛔ No account number, broker server string, `not_healed` detail
    segment, terminal path/build, or absolute home path. Run references only in the three maskable forms.
  </action>

  <verify>
    <automated>PD=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session; F=$PD/164.6.2-MEASUREMENT-WAVE5.md; test -f "$F" || { echo "MISSING ARTIFACT"; exit 1; }; for k in "Restart stamp" "First authorized reading" "Delta" "What closed it" "Prober run id" "Prober verdict" "Question THREE verdict" "Outcome" "Poll log"; do grep -qiF "$k" "$F" || { echo "MISSING SECTION: $k"; exit 1; }; done; echo "MEASUREMENT-RECORD-COMPLETE OK (9 sections)"</automated>
    <fails_when>non-zero exit, or the printed line is not `MEASUREMENT-RECORD-COMPLETE OK (9 sections)`. A named missing section means D-06's record is partial. The nine labels are the OUTCOME 0 record's, so the two records are comparable section by section.</fails_when>

    <automated>PD=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session; F=$PD/164.6.2-MEASUREMENT-WAVE5.md; O=$PD/164.6.2-MEASUREMENT.md; OUT=$(grep -m1 -oiE '^outcome:[[:space:]]*(CLOSED|OPEN|UNREADABLE|PREMISE-FAILED)' "$F" | grep -oiE 'CLOSED|OPEN|UNREADABLE|PREMISE-FAILED' | tr '[:lower:]' '[:upper:]'); test -n "$OUT" || { echo "NO-OUTCOME-LINE: the record must carry a column-1 outcome: CLOSED|OPEN|UNREADABLE|PREMISE-FAILED"; exit 1; }; if test "$OUT" = PREMISE-FAILED; then echo "POST-RESTART-OBSERVATION-TAKEN SKIPPED (outcome PREMISE-FAILED — no event was performed, by design)"; exit 0; fi; if test "$OUT" = UNREADABLE; then grep -qiE '^unreadable instrument:[[:space:]]*[^[:space:]]' "$F" || { echo "UNREADABLE-WITHOUT-NAMED-INSTRUMENT"; exit 1; }; grep -qiE '^restart stamp:[[:space:]]*(NONE|20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z)' "$F" || { echo "UNREADABLE-WITHOUT-A-RESTART-STAMP-LINE: write NONE if the stop came before the event (Task 1) or the ISO stamp if after it"; exit 1; }; echo "POST-RESTART-OBSERVATION-TAKEN OK (outcome UNREADABLE; instrument named; no verdict claimed)"; exit 0; fi; RS=$(grep -m1 -oE '^restart stamp:[[:space:]]*20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' "$F" | grep -oE '20[0-9T:-]+Z'); test -n "$RS" || { echo "RESTART-STAMP-NOT-MACHINE-READABLE: outcome $OUT needs a column-1 'restart stamp: YYYY-MM-DDTHH:MM:SSZ'"; exit 1; }; PR=$(grep -m1 -oE '^post-restart run id[[:space:]]+[0-9]{10,}' "$F" | grep -oE '[0-9]{10,}'); test -n "$PR" || { echo "POST-RESTART-RUN-ID-MISSING: criterion 2 closes on a POST-RESTART dispatch and none is recorded on a column-1 line"; exit 1; }; PC=$(grep -m1 -oE '^post-restart run created:[[:space:]]*20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z' "$F" | grep -oE '20[0-9T:-]+Z'); test -n "$PC" || { echo "POST-RESTART-RUN-CREATED-MISSING"; exit 1; }; command -v gh >/dev/null || { echo "UNREADABLE-FOR-GATE: gh is absent, so the post-restart run cannot be cross-checked against GitHub — this gate does not pass on an uncheckable claim"; exit 1; }; J=$(gh run view "$PR" --json createdAt,event,workflowName 2>/dev/null) || { echo "UNREADABLE-FOR-GATE: gh run view $PR failed — the recorded post-restart run cannot be confirmed to exist"; exit 1; }; printf '%s' "$J" | PC="$PC" node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const j=JSON.parse(s);if(j.event!=="workflow_dispatch"){console.log("POST-RESTART-RUN-NOT-A-DISPATCH: GitHub says event="+j.event);process.exit(1)}if(!/prod-prober/.test(j.workflowName||"")){console.log("POST-RESTART-RUN-WRONG-WORKFLOW: "+j.workflowName);process.exit(1)}if(j.createdAt!==process.env.PC){console.log("POST-RESTART-CREATED-MISMATCH: GitHub says "+j.createdAt+", the record says "+process.env.PC);process.exit(1)}})' || exit 1; test "$PC" \> "$RS" || { echo "OBSERVATION-PRECEDES-EVENT: run created $PC is not after restart stamp $RS"; exit 1; }; BR=$(grep -m1 -oE '^baseline run id[[:space:]]+[0-9]{10,}' "$F" | grep -oE '[0-9]{10,}'); test "$PR" != "$BR" || { echo "POST-RESTART-ID-IS-THE-BASELINE: $PR"; exit 1; }; if grep -oE '(actions/runs/|[Rr]un id:?[[:space:]]+|[Rr]un[[:space:]]+)[0-9]{10,}' "$O" | grep -oE '[0-9]{10,}' | grep -qx "$PR"; then echo "POST-RESTART-ID-IS-A-CITATION: $PR already appears in the OUTCOME 0 record"; exit 1; fi; echo "POST-RESTART-OBSERVATION-TAKEN OK (run $PR created $PC > restart $RS; confirmed on GitHub as a prod-prober workflow_dispatch)"</automated>
    <fails_when>non-zero exit, or the printed line is not a `POST-RESTART-OBSERVATION-TAKEN OK` banner (or one of its two documented SKIPPED/UNREADABLE forms). ⛔ **This is the leg that counts the OBSERVATION instead of the citations** — the fix for `[164.6.2-PLAN04-GATE-INTENT-DRIFT]` defect (a), applied to this record. It requires a dedicated column-1 `post-restart run id`, CROSS-CHECKS it against GitHub (`gh run view`: the run exists, is a `workflow_dispatch`, belongs to the prod-prober workflow, and its `createdAt` equals the recorded `post-restart run created:` — so a fabricated or mis-copied line reds with `UNREADABLE-FOR-GATE` or `POST-RESTART-CREATED-MISMATCH` rather than ordering fine), proves that `createdAt` is lexically AFTER the restart stamp (both ISO UTC, so string order is time order), proves it is not the baseline dispatch, and proves it appears nowhere in the OUTCOME 0 record's run references. Plan 04's leg printed `OK (7 run refs)` with zero post-restart runs among them; this leg cannot. ⚠️ On `outcome: UNREADABLE` the restart stamp may be `NONE` (a Task 1 stop, before any event) or an ISO stamp (a Task 2 stop, after it) — an honest unreadable record is never made to inherit a failing path's gate. ⛔ An absent `gh` is reported LOUDLY, never passed. ⛔ Never clear this leg by editing a stamp — the stamps are the measurement.</fails_when>

    <automated>PD=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session; F=$PD/164.6.2-MEASUREMENT-WAVE5.md; OUT=$(grep -m1 -oiE '^outcome:[[:space:]]*(CLOSED|OPEN|UNREADABLE|PREMISE-FAILED)' "$F" | grep -oiE 'CLOSED|OPEN|UNREADABLE|PREMISE-FAILED' | tr '[:lower:]' '[:upper:]'); DC=$(grep -m1 -oiE '^post-restart defect count:[[:space:]]*[0-9]+' "$F" | grep -oE '[0-9]+$'); case "$OUT" in CLOSED) test "$DC" = 0 || { echo "CLOSED-WITHOUT-ZERO: post-restart defect count is '${DC:-absent}'"; exit 1; }; grep -qiE '^what closed it:[[:space:]]*[^[:space:]]' "$F" || { echo "CLOSED-WITHOUT-CAUSE: D-06 forbids a window without what closed it"; exit 1; }; grep -qiE '^delta:[[:space:]]*[0-9]{2}:[0-9]{2}:[0-9]{2}' "$F" || { echo "CLOSED-WITHOUT-DELTA: needs a column-1 delta: HH:MM:SS"; exit 1; }; grep -qiE '^window class:[[:space:]]*(BOUNDED|UNBOUNDED-WITHOUT-ANALYTICS-RESTART)' "$F" || { echo "CLOSED-WITHOUT-WINDOW-CLASS"; exit 1; }; grep -qiE '^first authorized reading:[[:space:]]*20[0-9]{2}-' "$F" || { echo "CLOSED-WITHOUT-FIRST-AUTHORIZED-STAMP"; exit 1; };; OPEN) test -n "$DC" -a "$DC" != 0 || { echo "OPEN-WITHOUT-A-NONZERO-COUNT: '${DC:-absent}'"; exit 1; }; grep -qiE '^analytics restart stamp:[[:space:]]*20[0-9]{2}-' "$F" || { echo "OPEN-WITHOUT-THE-ACTIVE-PHASE: OPEN is reachable only after ONE confirmed analytics restart"; exit 1; }; grep -qiE '^heal verdict line:[[:space:]]*mt5 boot heal' "$F" || { echo "OPEN-WITHOUT-THE-HEAL-VERDICT: the heal's own line is the discriminator between A3 and an IPC class"; exit 1; };; UNREADABLE) test -z "$DC" || { echo "UNREADABLE-WITH-A-COUNT: a count is a reading, so the instrument was readable"; exit 1; }; grep -qiE '^unreadable instrument:[[:space:]]*[^[:space:]]' "$F" || { echo "UNREADABLE-WITHOUT-NAMED-INSTRUMENT"; exit 1; };; PREMISE-FAILED) grep -qiE '^restart stamp:[[:space:]]*NONE' "$F" || { echo "PREMISE-FAILED-BUT-RESTARTED"; exit 1; }; test -z "$DC" || { echo "PREMISE-FAILED-WITH-A-POST-RESTART-COUNT"; exit 1; };; *) echo "NO-OUTCOME-LINE: a column-1 outcome: line is required"; exit 1;; esac; grep -qi 'no vnc session' "$F" || { echo "NO-VNC-STATEMENT-MISSING: the record must state that no VNC session was opened"; exit 1; }; grep -qiE 'not_healed:[a-z_]+:' "$F" && { echo "HEAL-DETAIL-SEGMENT-QUOTED: only the prefix through the reason may be recorded"; exit 1; }; echo "CRITERION2-CLOSURE-CONSISTENT OK (outcome $OUT, post-restart defect count ${DC:-none})"</automated>
    <fails_when>non-zero exit, or the printed line is not a `CRITERION2-CLOSURE-CONSISTENT OK` banner. ⛔ **This is the biconditional that makes a green-but-meaningless reading unreachable.** CLOSED demands a ZERO post-restart count AND a delta AND a cause AND a window class AND a first-authorized stamp; OPEN demands a NON-ZERO count taken only after the ONE analytics restart with the heal's own verdict line on record; UNREADABLE forbids any count; PREMISE-FAILED forbids a restart. It also reds if the heal line was quoted past its reason segment (`not_healed:<reason>:<detail>` — the detail is scrubbed exception text and never belongs in a public file) or if the no-VNC statement is missing. ⭐ It never checks that the count was favourable — a non-zero count on OPEN is exactly as required as a zero on CLOSED.</fails_when>

    <automated>PD=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session; F=$PD/164.6.2-MEASUREMENT-WAVE5.md; V=$(grep -m1 -oiE '^verdict:[[:space:]]*(STALE|CURRENT|UNSETTLED)' "$F" | grep -oiE 'STALE|CURRENT|UNSETTLED' | tr '[:lower:]' '[:upper:]'); test -n "$V" || { echo "Q3-VERDICT-MISSING: a column-1 line verdict: STALE|CURRENT|UNSETTLED is required"; exit 1; }; grep -qiE '^R1:[[:space:]]*(true|false|UNAVAILABLE)' "$F" || { echo "R1-MISSING (column-1 line required)"; exit 1; }; grep -qiE '^R2:[[:space:]]*(true|false|UNAVAILABLE)' "$F" || { echo "R2-MISSING (column-1 line required)"; exit 1; }; grep -qiE '^R2 event:[[:space:]]*[^[:space:]]' "$F" || { echo "R2-EVENT-MISSING: the event that produced R2 must be named on a column-1 line (or 'none')"; exit 1; }; if test "$V" != UNSETTLED; then grep -qiE '^R2 event:[[:space:]]*per-account login' "$F" || { echo "VERDICT-ON-A-PROXY: $V requires R2 from a REAL per-account login; a boot heal is a PROXY and yields UNSETTLED with the proxy reading recorded"; exit 1; }; fi; echo "QUESTION-THREE-RECORDED OK (verdict $V)"</automated>
    <fails_when>non-zero exit, or the printed line is not a `QUESTION-THREE-RECORDED OK` banner. R1, R2 and the event that produced R2 are the three things the ROADMAP's criterion 8 requires, and a STALE or CURRENT verdict is admissible only when R2 came from a real per-account login — the event the contested sentence is actually about. A proxy-based verdict would license a correction of operator instructions on a reading of a different event (threat T-164.6.2-24's class). ⚠️ All four extractions are anchored at column 1, so prose such as "the OUTCOME 0 record's `verdict: UNSETTLED`" mid-sentence cannot be read as this record's verdict.</fails_when>

    <automated>PD=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session; F=$PD/164.6.2-MEASUREMENT-WAVE5.md; M=$(sed -E 's#(actions/runs/|[Rr]un id:?[[:space:]]+|[Rr]un[[:space:]]+)[0-9]{10,}#RUNREF#g; s#[0-9a-f]{40}#SHA40#g' "$F" | grep -nE '[0-9]{6,}|/Us[e]rs/|/home/[a-z]|"path"|"build"' || true); if test -n "$M"; then printf '%s\n' "$M"; echo "DISCLOSURE-SHAPE FOUND"; exit 1; fi; grep -qiE 'exit code' "$F" && echo "WARN: the record mentions an exit code — 1 with defects, 2 without, and never the verdict" >&2; echo "NO-DISCLOSURE-SHAPES OK"</automated>
    <fails_when>non-zero exit, or the printed line is not `NO-DISCLOSURE-SHAPES OK`. Same mask as Task 1's leg. The `WARN` on an exit-code mention is stderr only and non-failing — the record is allowed to state the semantics in order to say they are not the verdict.</fails_when>
    <!-- planner-discipline-allow: exit code -->

    <automated>BASE=f901e4da; ROOTS="scripts/ .github/ analytics-service/ deploy/ docs/"; S=$(git status --porcelain -- $ROOTS) || exit 1; n=$(printf '%s' "$S" | grep -c .); test "$n" -eq 0 || { printf '%s\n' "$S"; echo "INSTRUMENT-MODIFIED-UNCOMMITTED: $n path(s) changed under a read-only root"; exit 1; }; git cat-file -e "$BASE^{commit}" 2>/dev/null || { echo "BASE-UNRESOLVABLE: $BASE is not a commit in this checkout"; exit 1; }; if ! git diff --quiet "$BASE" HEAD -- $ROOTS; then git --no-pager diff --stat "$BASE" HEAD -- $ROOTS; echo "INSTRUMENT-MODIFIED-COMMITTED: a read-only root differs between the wave's branch point $BASE and HEAD"; exit 1; fi; for pin in "scripts/mt5-diag.sh 5114fd798ffe392813e5336a679e1c6f9fbd2aaf" "scripts/prod-prober/arms/mt5.mjs aa56210c4663e884f9c31979b2224b0462da34c3"; do set -- $pin; H=$(git hash-object "$1"); test "$H" = "$2" || { echo "D09-SITE-CHANGED: $1 hashes to $H, not to its planning-time blob $2"; exit 1; }; done; echo "INSTRUMENTS-READ-ONLY OK (porcelain clean; $BASE..HEAD clean over the read-only roots; both D-09 sites hash-pinned)"</automated>
    <fails_when>non-zero exit, or the printed line is not an `INSTRUMENTS-READ-ONLY OK` banner. Same three checks as Task 1's leg (porcelain for uncommitted edits; `git diff --quiet f901e4da HEAD` over the read-only roots for COMMITTED edits, since executors commit after every task; hash pins on both D-09 sentence sites against their planning-time blobs). ⛔ This is the leg that makes the wave's D-09 routing HONEST rather than labelled: on ANY verdict the four copies stay byte-unchanged here, and a committed correction — the exact temptation criterion 9 and Task 3's routing forbid inside this wave — reds with `INSTRUMENT-MODIFIED-COMMITTED` or `D09-SITE-CHANGED`.</fails_when>

    <automated>PD=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session; O=$PD/164.6.2-MEASUREMENT.md; H=$(git hash-object "$O"); test "$H" = 1c331ad19b12dac6ce8d9f1ea7b714427b85ddfb || { echo "HISTORY-REWRITTEN: $O hashes to $H"; exit 1; }; echo "OUTCOME-0-RECORD-UNTOUCHED OK"</automated>
    <fails_when>non-zero exit, or the printed line is not `OUTCOME-0-RECORD-UNTOUCHED OK`. History stays as written.</fails_when>

    <automated>npm run check:planning-hygiene</automated>
    <fails_when>non-zero exit. Fix the artifact, ⛔ never the gate.</fails_when>
  </verify>

  <acceptance_criteria>
    - The gateway was restarted with the restart verb, with NO VNC session before, during or after, and the UTC stamp is recorded on a machine-readable line.
    - Every poll reading is a stamped row; no single reading was treated as a state; a missing `PROBE` line right after the restart was recorded as the container starting, not as a state.
    - ⛔ A POST-RESTART narrowed prober dispatch was taken — distinct from the baseline and from every OUTCOME 0 citation — and its run id, its `createdAt` (after the restart stamp), its `arms:` line, its defect rows and a machine-readable `post-restart defect count: N` were read from the log ARCHIVE's anchored region, never from an exit code or a run conclusion.
    - The record names the delta AND what closed it AND the window class, with the closer attributed to a named event and its evidence (a heal verdict line prefix with stamp, a deployment row, or the measured absence of both).
    - The active phase, if it ran, was ONE analytics restart with its stamp and the heal's verdict line recorded through the reason segment only.
    - R1, R2 and the event that produced R2 are recorded; a proxy R2 yields UNSETTLED; `r2 pending` is written when no real per-account login occurred inside the bounded wait.
    - The `outcome:` line is one of the four, and the consistency gate holds; an unreadable instrument is named and never classified as a state.
    - No credential, account number, broker server string, `not_healed` detail segment, terminal path/build or absolute home path appears; nothing under the read-only roots changed; the OUTCOME 0 record is byte-identical.
  </acceptance_criteria>

  <done>
    `164.6.2-MEASUREMENT-WAVE5.md` carries all nine sections, all eight verify banners pass, and the
    record states in unambiguous sentences the outcome, D-06's number and cause, the post-restart prober
    verdict with its run id and count, and the question-THREE verdict with R1, R2 and R2's event.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: DECISION — the MEASURED window's verdict, criterion 2's closure state, and where the reading's consequences go</name>

  <decision>
    Given the measured number: is the healing window acceptable as it stands (close the phase, book no
    keepalive), or does it earn a keepalive / reconnect hook as a NAMED follow-up phase? And, on a
    non-CLOSED outcome, does the phase stay open pending the founder-only tie-breaker? And which plan
    receives the D-09 correction if the reading licensed it?
  </decision>

  <context>
    ⛔ **NOT auto-approvable.** D-06 reserved the window's acceptability for the founder deliberately, on
    the stated grounds that an interval chosen before a measurement is chosen against a guess. Plan 04's
    checkpoint REJECTED `window-acceptable` only because no window existed then; it exists now, so the
    question is live for the first time. Auto-selecting any option here would make the decision the
    measurement was taken to inform. `workflow.auto_advance` does not apply to a `blocking-human` gate.

    **Present, in this order, before asking — every item read from `164.6.2-MEASUREMENT-WAVE5.md`, not
    paraphrased from memory:**
      1. The two precondition lines and the deployed sha (short form), with the containment reasoning.
      2. D-06's number and the sentence beside it: `restart stamp`, `first authorized reading`, `delta`,
         `what closed it`, `window class` — and, if the active phase ran, `analytics restart stamp` and
         the `heal verdict line`.
      3. The `outcome:` line and, from `<the_outcomes>`, what it implicates: CLOSED ⇒ criterion 2 is
         closed on the observation; OPEN ⇒ the heal's verdict line and the Journal-tab tie-breaker (VNC,
         founder-only), with A3 named as a suspect against a MEASURED state only if the verdict was
         `credential_refused` / `still_unauthorized`; UNREADABLE ⇒ the instrument named, nothing
         concluded; PREMISE-FAILED ⇒ deploy ordering, owner Phase 164.11.
      4. Question THREE's `verdict:` with R1, R2 and `R2 event:` (and `r2 pending` if written).
      5. The post-restart prober row(s) and count, and the baseline row(s) and count, side by side.
      6. The items awaiting a destination, each with a RECOMMENDED phase — ⛔ never a blank destination.

    ⛔ Whichever option is taken, it is NEVER a re-litigation of criterion 1's shape (a founder decision
    taken on a measurement, restated in criterion 2's own text). ⛔ The keepalive's INTERVAL is not
    designed at this checkpoint — only whether a named phase for it is booked.
  </context>

  <options>
    <option id="window-acceptable">
      <name>The measured window is acceptable — criterion 2 closed on the observation; close the phase, book no keepalive</name>
      <pros>No new phase and no interval tuned against a partially-understood failure mode; the system is measurably better than the hand login it replaced, and the number that says so is on record.</pros>
      <cons>MT5 stays dark for the measured delta after any gateway restart (unbounded until an analytics restart, if that is the measured class), and nobody is watching for the next one beyond the hourly prober.</cons>
    </option>
    <option id="book-keepalive">
      <name>Criterion 2 closed, but the window is too long — book a keepalive / reconnect hook as a NAMED follow-up phase</name>
      <pros>Closes the gap the measurement just proved, and the interval is chosen THERE against a number rather than a guess — exactly what D-06 deferred it for. Criterion 2 still closes here; it closed on the observation.</pros>
      <cons>A new phase, entered via `/gsd-phase` (insert), whose periodic terminal touch must be designed against the lease and the MT5CONC-02 account bracket on the ONE shared terminal.</cons>
    </option>
    <option id="hold-open">
      <name>Outcome OPEN or UNREADABLE — hold the phase OPEN</name>
      <pros>Honest: criterion 2 closes on an observation and there is none (or the observation says the heal did not clear it). For OPEN the record now carries the ONE discriminator plan 04 could not — the heal's own verdict line against a deployed heal — and the founder holds the tie-breaker (the terminal's Journal tab, VNC). For UNREADABLE the instrument is named and a re-run is a plan-05 re-execution, not a new plan.</pros>
      <cons>The phase does not close and the tie-breaker needs founder time. ⛔ Looping, retrying or "improving" the heal against an unmeasured cause is strictly worse and is not an option here.</cons>
    </option>
  </options>

  <resume-signal>
    Select `window-acceptable`, `book-keepalive` or `hold-open`, and confirm the destination for each
    routed item below.
  </resume-signal>

  <routing>
    **Every item goes through `/gsd-phase --edit` (or `--insert` for a new phase), ⛔ never by hand-editing
    the ROADMAP, and ⛔ never into a phase that has already SHIPPED:**
      - **The D-09 correction, if `verdict: STALE`** — all FOUR copies of the stale account-change
        sentence (the remedy string and the quoted header block in `scripts/prod-prober/arms/mt5.mjs`;
        the header comment block and the printed `NOTE` heredoc body in `scripts/mt5-diag.sh`).
        RECOMMENDED destination: **this phase, as its own plan 06**, carrying plan 04 task 2's gates
        VERBATIM (`STALE-LICENCE`, `FOUR-COPIES-CORRECTED`, `VERBATIM-PAIR`, `DIAG-NONEXEC-ONLY`,
        `BLAST-RADIUS`, the fence calibration) but licensed by the WAVE-5 record's verdict line — ⛔ not by
        the OUTCOME 0 record's UNSETTLED, and ⛔ not inside this wave, for the same reason
        `[164.6.2-RAISE-LAST-SHAPE-ONLY]` is not: a neuter/restore calibration over a production instrument
        does not belong in the wave that measures production.
      - **The D-09 reading, if `verdict: UNSETTLED` with `r2 pending`** — the same plan 06 takes R2 first
        (after the next real per-account login, evidenced in the analytics log) and only then decides
        whether it edits. If UNSETTLED because `terminal_info` was null throughout (outcome OPEN), route
        with the outcome: it stays with this phase and re-opens when the terminal is readable.
      - **A keepalive / reconnect hook** — book ONLY under `book-keepalive`, as a NAMED phase whose design
        belongs to it. Its interval is chosen against the measured `delta` and `window class`, which this
        record now carries.
      - **`[164.6.2-RAISE-LAST-SHAPE-ONLY]`** — UNCHANGED by this wave: stays in this phase as a post-ship
        plan of its own (ROADMAP criterion 9, founder decision 2, 2026-09-14). Nothing to route.
      - **`[164.6.2-PLAN04-GATE-INTENT-DRIFT]`** — already routed to Phase 164.8.4 GATERESIDUE (criterion
        10). This wave's exact exit-code semantics (1 with defects, 2 without) and the anchored-region
        extraction are recorded in the SUMMARY for that item; no new deferral.
      - **The env-account-vs-connected-key observation** — founder-side check, not a phase item; carried
        in the record as an observation.

    ⛔ Do not close the phase on OPEN or UNREADABLE. ⛔ Do not book anything into 164.8.3 (Complete,
    shipped). Check each destination's status before routing and prefer an UPCOMING unplanned phase.
  </routing>

  <done>
    The founder has stated whether the MEASURED window is acceptable; criterion 2's closure state is
    recorded in the SUMMARY as `criterion_2_closed: true|false` consistent with the record's `outcome:`
    line (true ONLY on CLOSED); `phase_closes:` is recorded consistent with the option taken (never true
    on `hold-open`, and on a STALE verdict only after plan 06 lands); every applicable item has a NAMED
    destination entered via `/gsd-phase --edit` with no blank destination and nothing booked into a
    shipped phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live production terminal and analytics logs -> a tracked, PUBLIC planning artifact | diagnostic readings carry a production filesystem path and a terminal build; heal log lines carry scrubbed exception text in their detail segment; an account number is eight digits |
| the measurement instruments -> the thing being measured | an instrument that mutates, or an agent that logs in, destroys the observation and the fault it is measuring |
| a stale brief -> a production restart | a precondition trusted rather than re-measured turns a restart into OUTCOME 0 again, or reds a correct deploy on an equality check |
| a citation-counting gate -> a closure claim | a banner satisfied by references to OTHER runs can be read as "the observation was taken" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.2-28 | Information disclosure | `164.6.2-MEASUREMENT-WAVE5.md` in a PUBLIC, TRACKED directory | critical | mitigate | Only the five permitted diagnostic fields; run refs only in the three maskable forms; `NO-DISCLOSURE-SHAPES` masks run refs and the 40-hex deployed sha and bans every other six-plus-digit run, absolute home paths and the two forbidden field names; `npm run check:planning-hygiene` at every task. |
| T-164.6.2-29 | Information disclosure | the heal's `not_healed:<reason>:<detail>` line and any other `railway logs` text pasted from the analytics service | high | mitigate | Only the prefix through the `<reason>` segment may be recorded; `CRITERION2-CLOSURE-CONSISTENT` reds on a `not_healed:<reason>:` followed by anything; no adjacent log lines are quoted; all captured output is scanned before it is pasted (T-134-01: `mt5linux` f-string-interpolates arguments into remotely-eval'd code). |
| T-164.6.2-30 | Tampering | making `scripts/mt5-diag.sh` or the prober arm a mutator, any login by an agent or instrument, or a COMMITTED edit to a D-09 site inside the measurement wave | high | mitigate | `INSTRUMENTS-READ-ONLY` at every task checks three ways: porcelain (uncommitted), `git diff --quiet f901e4da HEAD` over `scripts/ .github/ analytics-service/ deploy/ docs/` (committed — the porcelain-only form was MEASURED to pass a committed edit), and hash pins on both D-09 sites (a laundered base); no VNC session (a required statement in the record); the only production verbs are `railway restart` (twice at most: gateway once, analytics once) and read-only probes. D-06 and 164.8.3's criterion 5 both forbid a self-healing instrument. |
| T-164.6.2-39 | Spoofing | a fabricated or mis-copied post-restart run id / `createdAt` ordering fine against the record's own restart stamp | high | mitigate | `POST-RESTART-OBSERVATION-TAKEN` and `BASELINE-RECORDED` cross-check the recorded run id against GitHub (`gh run view`: exists, `workflow_dispatch`, prod-prober workflow, `createdAt` equal to the recorded line); an absent `gh` or a failed lookup is `UNREADABLE-FOR-GATE`, reported loudly and never passed. |
| T-164.6.2-31 | Repudiation | a citation-counting gate read as proof of the observation (plan 04 defect (a)) | high | mitigate | `POST-RESTART-OBSERVATION-TAKEN` requires a dedicated post-restart run id whose `createdAt` is after the restart stamp, distinct from the baseline, and absent from the OUTCOME 0 record's references. |
| T-164.6.2-32 | Repudiation | an unreadable reading recorded as a failing, stale or `-6` state (the PR #798 conflation) | high | mitigate | `UNREADABLE` is its own outcome with a named instrument and no count; `precondition deployed: UNREADABLE` on an empty `/health`; `CRITERION2-CLOSURE-CONSISTENT` forbids a count on UNREADABLE and a claim without a count on CLOSED/OPEN. |
| T-164.6.2-33 | Repudiation | reading a narrowed run's EXIT CODE or a run's `conclusion` as the verdict | high | mitigate | The verdict is read from the log ARCHIVE's execution-anchored region (proven on the baseline run); the exact exit semantics (1 with defects, 2 without; scheduled runs posture-flipped to 0) are recorded so no number is mistaken for a reading. |
| T-164.6.2-34 | Repudiation | a precondition asserted by string equality (reds a correct descendant deploy) or trusted from a stale brief (restarts against an absent heal) | high | mitigate | `PRECONDITION-REASSERTED` re-runs `merge-base --is-ancestor` + `ls-tree` against the sha the record claims; the record must state the containment reasoning; a non-PASS pair forces `outcome: PREMISE-FAILED` with `restart stamp: NONE`. |
| T-164.6.2-35 | Repudiation | a question-THREE verdict taken on a PROXY event licensing a correction of operator instructions | medium | mitigate | `QUESTION-THREE-RECORDED` admits STALE/CURRENT only with `R2 event: per-account login …`; a boot-heal R2 is recorded as a proxy and the verdict stays UNSETTLED; no correction is performed in this wave regardless. |
| T-164.6.2-36 | Denial of service | the ONE analytics restart in the active phase interrupting an in-flight job | medium | accept | A deploy-shaped restart the worker's claim discipline and Railway's restart policy already absorb daily; performed once, at a moment the worker is ticking, never twice; plan 04 accepted the same shape. |
| T-164.6.2-37 | Tampering | rewriting history — the OUTCOME 0 record, or plan 04's "exits 2" sentence | medium | mitigate | `OUTCOME-0-RECORD-UNTOUCHED` pins the record to its HEAD blob; plan 04 is not edited; corrections live in the wave-5 record and the SUMMARY beside the routed defect. |
| T-164.6.2-38 | Repudiation | a deferral with no owner, or the keepalive interval chosen at the checkpoint | high | mitigate | Every routed item names an UPCOMING phase via `/gsd-phase --edit`; the keepalive is booked only as a NAMED phase whose design happens there; nothing into 164.8.3. |
| T-164.6.2-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed, added, bumped or re-resolved by this plan. No install task exists, so no legitimacy checkpoint is injected. |
</threat_model>

<verification>
- `164.6.2-MEASUREMENT-WAVE5.md` exists, carries all NINE named sections and a column-1 `outcome:` line, and passes the gates that RUN ON ITS PATH: on every path `PRECONDITION-REASSERTED`, `NO-DISCLOSURE-SHAPES`, `INSTRUMENTS-READ-ONLY`, `OUTCOME-0-RECORD-UNTOUCHED` and `npm run check:planning-hygiene` (Task 1's legs); on a PASS/PASS precondition pair additionally `BASELINE-RECORDED` and, once Task 2 has run, `MEASUREMENT-RECORD-COMPLETE`, `POST-RESTART-OBSERVATION-TAKEN`, `CRITERION2-CLOSURE-CONSISTENT` and `QUESTION-THREE-RECORDED`. ⚠️ On a Task 1 stop (`outcome: PREMISE-FAILED` or `UNREADABLE` with `restart stamp: NONE`) Task 2 never runs and its legs are NOT claimed — an honest unreadable record does not inherit a failing path's gate; if they are run anyway, both `POST-RESTART-OBSERVATION-TAKEN` and `CRITERION2-CLOSURE-CONSISTENT` are written to pass on that shape.
- At the end of every task: `git status --porcelain` over the read-only roots is empty, `git diff --quiet f901e4da HEAD` over them is clean, and both D-09 sites hash to their planning-time blobs.
- `git hash-object` of `164.6.2-MEASUREMENT.md` equals its HEAD blob.
- The founder's decision at Task 3 is recorded, and the SUMMARY's `criterion_2_closed` / `phase_closes` keys agree with the record's `outcome:` line and the option taken.
- Every applicable deferral has a NAMED destination phase entered via `/gsd-phase --edit`.

⚠️ CHANGELOG discipline applies if anything ships from this wave (a routed correction plan, for instance): enumerate every commit, read the full diff, group by theme, one unified entry newest-first, cross-check every commit maps to a bullet, in the same commit as the VERSION bump. ⛔ Never `npm version`. This wave itself ships no code.
</verification>

<success_criteria>
- ⛔ CRITERION 2 IS CLOSED ON AN OBSERVATION — a post-restart narrowed prober dispatch reporting zero defects for the mt5 arm after a gateway restart with no VNC session and no login by anyone — or the phase is deliberately HELD OPEN on a MEASURED negative (OPEN) or a named unreadable instrument (UNREADABLE), which are also correct endings.
- D-06's window is a recorded NUMBER with a recorded CAUSE and CLASS, not a guess, and the founder has decided on it.
- Question THREE carries R1, R2 and R2's event under plan 04 task 1(e)'s exact rule, with a verdict that licenses nothing inside this wave and routes its consequence to a named plan.
- Nothing this wave measured was worked around, and nothing it touched was an instrument.
</success_criteria>

<output>
Create `.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-05-SUMMARY.md` when done, carrying `criterion_2_closed:` and `phase_closes:` in its frontmatter, consistent with the record's `outcome:` line and the Task 3 decision.
</output>
