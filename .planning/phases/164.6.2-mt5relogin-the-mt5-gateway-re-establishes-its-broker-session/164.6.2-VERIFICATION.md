---
phase: 164.6.2-mt5relogin
verified: 2026-09-14T00:00:00Z
status: gaps_found
score: 6/9 roadmap success criteria verified (3 correctly out-of-scope / not-yet-applicable, 1 load-bearing criterion NOT closed)
covered_files: [".planning/ROADMAP.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-01-PLAN.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-01-SUMMARY.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-02-PLAN.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-02-SUMMARY.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-03-PLAN.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-03-SUMMARY.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-04-PLAN.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-04-SUMMARY.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-CONTEXT.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-HANDOVER.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-REVIEW.md", ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-SECURITY.md", "analytics-service/main.py", "analytics-service/services/mt5_client.py", "analytics-service/services/mt5_relogin.py", "analytics-service/tests/test_mt5_client_contract.py", "analytics-service/tests/test_mt5_concurrency.py", "analytics-service/tests/test_mt5_relogin.py", "docs/runbooks/mt5-go-live.md"]
covered_digest: "v1:sha256:ff8a697bc86536e2a39eaabf8ec328d5c0b5ef48f4f5ce55c47485921fb74399"
gaps:
  - truth: "The MT5 terminal's broker session is re-established WITHOUT a human — proven by restarting mt5-gateway and observing the prober's mt5 arm return no defect, with no VNC session in between (ROADMAP criterion 2, the phase's own load-bearing criterion)."
    status: failed
    reason: "This is NOT a code defect. The heal (`heal_mt5_terminal_session`) is built, unit-tested, falsified, security-reviewed and mypy-strict clean on the phase branch — but the phase's own plan-04 measurement (164.6.2-MEASUREMENT.md, run 2026-09-14) found the DEPLOYED analytics image is `5dead88a` (= main), at which the heal module does not exist and `main.lifespan` starts 4 tasks, not 5. The branch has never been merged or deployed (`branch: main` on 20/20 of the last 20 analytics deployments). Restarting the gateway today would have exactly one, uninformative outcome, so plan 04 correctly declined to perform it (`OUTCOME 0 — premise failure`) rather than manufacture a false reading. Criterion 2 is therefore observably NOT met in the codebase/production today, and the phase's own artifacts (ROADMAP criterion 6, MEASUREMENT.md, 04-SUMMARY.md `phase_closes: false` / `criterion_2_closed: false`) agree it is not met."
    artifacts:
      - path: ".planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md"
        issue: "Records OUTCOME 0 — the restart was deliberately not performed because the mechanism under test is absent from the deployed sha. No post-restart prober dispatch exists."
    missing:
      - "Merge the phase branch to `main`."
      - "Confirm the merge DEPLOYED (analytics `/health` `git_sha` matches the merge commit) — ROADMAP criterion 7 / Phase 164.11's finding that 5 of the last analytics deployments were silently SKIPPED means merging is necessary but not sufficient."
      - "Execute the already-scoped Wave 5 (ROADMAP criterion 7): restart mt5-gateway, poll to a second consecutive reading, dispatch a POST-RESTART narrowed prober run, read its verdict from the published log (never the exit code), and record the delta + what closed it."
      - "This is already named, scoped and routed in the ROADMAP (criteria 6/7) and in 04-SUMMARY.md's decision log — it does not need new planning, only execution after ship."
deferred:
  - truth: "Question THREE (D-09, whether a login re-clears the Expert-Advisors auto-disable options) is settled."
    addressed_in: "This phase's own Wave 5 (ROADMAP criterion 8, not a separate later phase)"
    evidence: "164.6.2-MEASUREMENT.md records `verdict: UNSETTLED` because `terminal_info()` is null while `-6` is in force; task 2 correctly edited nothing (`STALE-LICENCE OK`, both instruments cmp-identical to their pre-edit backups). ROADMAP criterion 8 explicitly rides the settlement on Wave 5's R1/R2 procedure."
  - truth: "`[164.6.2-RAISE-LAST-SHAPE-ONLY]` (Mt5Client.login's falsy arm is shape-scrubbed only) is closed."
    addressed_in: "164.6.2, a named post-ship plan of its own (not Wave 5, not 164.6 GATE-HYGIENE)"
    evidence: "ROADMAP criterion 9 and 04-SUMMARY.md's routing table both name this destination explicitly; it is pre-existing on every shipped call site, not a regression introduced by this phase, and is pinned by a measurement test so a future close attempt reds loudly if it regresses."
behavior_unverified_items: []
human_verification:
  - test: "After the phase branch merges and is OBSERVED to have deployed (health `git_sha` matches), restart the `mt5-gateway` Railway service with no VNC session before/during/after, poll `scripts/mt5-diag.sh` to a second consecutive authorized reading, then dispatch `gh workflow run prod-prober.yml -f arm=mt5` and read the published runner log's verdict."
    expected: "The mt5 arm reports no defect (defect count 0) without any human credential entry or VNC login, closing ROADMAP criterion 2."
    why_human: "Requires a production deploy, a real service restart and a production observation window — none of which a code verifier can perform or simulate. This is explicitly the phase's own founder-mandated closing gate (criterion 2's text: closes ONLY on an OBSERVED restart self-heal, never on the code alone)."
---

# Phase 164.6.2: MT5RELOGIN Verification Report

**Phase Goal:** The MT5 terminal's broker session is re-established WITHOUT a human — env-driven login (`MT5_LOGIN`/`MT5_PASSWORD`/`MT5_SERVER`), with the analytics WORKER re-establishing the session from them at its own startup in `main.lifespan`.
**Verified:** 2026-09-14
**Status:** gaps_found
**Re-verification:** No — initial verification

## Judgment call, stated plainly

This is not a case of missing code, a stub, or a wiring failure. **Everything the phase's own success criteria ask the agent to BUILD is built, well-tested, security-reviewed, and confirmed present on the branch at HEAD.** What is missing is the thing ROADMAP criterion 2 explicitly, twice-amended, and by founder mandate treats as the actual deliverable: an **observed** production self-heal, taken by restarting the gateway and reading the prober — not an inference from green tests. The phase's own plan-04 measurement discovered mid-execution that this observation is currently *impossible to take honestly*, because the branch that carries the heal has never been merged or deployed — production runs `main` (`5dead88a`), which has zero occurrences of `heal_mt5_terminal_session`.

The phase's own artifacts do not dispute this. ROADMAP criterion 6 states "CRITERION 2 IS HELD OPEN ON OUTCOME 0"; 04-SUMMARY.md's frontmatter states `phase_closes: false` and `criterion_2_closed: false`; the ROADMAP's own Plans list leaves plan 04's checkbox unchecked. A verifier that read only the SUMMARY bullet points and the "648 passed" test count would be tempted to call this `passed` — that would be rubber-stamping a claim the phase's own record explicitly declines to make. Per the adversarial stance this task requires, and because ROADMAP criterion 2 is stated to be the phase's *load-bearing* criterion, I am treating its non-closure as a genuine, must-have gap rather than routing it to `human_needed` or `passed`. **The remedy is not more code — it is ship (merge + confirm deploy) then execute the already-scoped Wave 5 measurement.** I recommend that be the very next action.

## Goal Achievement — ROADMAP Success Criteria

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Login env-driven; worker re-establishes session once at its own startup in `main.lifespan`, exercised by a falsifying test | ✓ VERIFIED | See Artifacts/Key-Links below. `initialize_with_credentials()` exists on `Mt5Client` (never `login()`), redaction lands in the same commit, `heal_mt5_terminal_session()` is wired into `main.lifespan` as a `create_task` after both startup guards, never awaited inline, structurally incapable of raising. Durable falsifier `test_the_criterion_1_predicate_reds_on_a_mutant_with_the_entry_excised` passes on shipped source (2 passed). |
| 2 | ⭐ Load-bearing: session re-established without a human, PROVEN by restarting the gateway and observing the prober's mt5 arm return no defect, no VNC in between | ✗ FAILED (NOT CLOSED) | `164.6.2-MEASUREMENT.md`: OUTCOME 0 — the restart was not performed because the mechanism is absent from the deployed sha. No post-restart prober dispatch exists. See `gaps` above. |
| 3 | Not in scope — no credential entered by an agent | ✓ VERIFIED | 03-SUMMARY.md / 04-SUMMARY.md: founder gate cleared key-only (`--kv \| cut -d= -f1`); SECURITY.md T-164.6.2-16/17/18/19 all closed with re-verified evidence. |
| 4 | Not in scope — `-6` defect vocabulary, deferred to 164.8.3 | ✓ VERIFIED (N/A) | 164.8.3 shipped (v0.77.42.0); this phase's artifacts confirm zero re-acquisition of that naming; `164.6.2-04-PLAN.md`'s D-09 fold is scoped to ONE sentence only and is explicitly licensed/audited (`STALE-LICENCE`). |
| 5 | Not in scope — changing what the gateway reads | ✓ VERIFIED | `git diff --name-only -- deploy/ scripts/ .github/` empty per plan verifies; SECURITY.md AR-01 re-verified (no railway.json/Dockerfile/port change). |
| 6 | Criterion 2 held open on OUTCOME 0 (founder decision, 2026-09-14) | ✓ VERIFIED (describes the gap above) | ROADMAP text and 04-SUMMARY.md agree verbatim on the nine measured facts. |
| 7 | Wave 5 — the post-ship measurement that closes criterion 2 | — NOT YET EXECUTABLE | Correctly scoped as future work in the ROADMAP; not part of this verification's pass/fail, but it is the concrete remedy for the one gap above. |
| 8 | Question THREE (D-09, `trade_allowed`) unsettled, rides on Wave 5 | ✓ VERIFIED (correctly deferred) | Plan 04 task 2 edited nothing (`verdict: UNSETTLED`); both instruments proven byte-identical to pre-edit backups. See `deferred`. |
| 9 | `[164.6.2-RAISE-LAST-SHAPE-ONLY]` stays as a named post-ship plan of its own | ✓ VERIFIED (correctly routed) | ROADMAP criterion 9 and 04-SUMMARY.md's routing table agree; pinned by a measurement test rather than silently left. |

**Score:** 6/9 criteria fully verified as stated in the ROADMAP text (criteria 3-6, 8-9 either close cleanly or correctly describe an open/deferred state); criterion 1 (the buildable half) verified; criterion 2 (the load-bearing observable half) is the one genuine gap; criterion 7 is scoped future work, not a verification target today.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Question THREE (D-09) settlement | This phase's own Wave 5 (ROADMAP criterion 8) | `verdict: UNSETTLED`, task 2 correctly edited nothing |
| 2 | `[164.6.2-RAISE-LAST-SHAPE-ONLY]` | This phase, a named post-ship plan (ROADMAP criterion 9) | Pre-existing, pinned, routed with reasons in 04-SUMMARY.md |

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `analytics-service/services/mt5_client.py` — `initialize_with_credentials`, `assert_session_authorized`, `_redact_credential_values`, `_credential_renderings`, `mt5_terminal_key` | Credentialed heal verb + credential-free detector + by-value redaction (raw + repr + ascii + json.dumps, longest-first, single global pass) | ✓ VERIFIED | Read at HEAD. `mypy --strict` clean (`Success: no issues found in 2 source files`). `Mt5Client.login` now also routed through the shared redactor (D-07 amendment, closing `[164.6.2-LOGIN-ESCAPE-BLIND]`, a live disclosure found in round-2 review). |
| `analytics-service/services/mt5_relogin.py` — `heal_mt5_terminal_session`, `_heal_blocking`, `_env_float`, `_relogin_budget_s`, `_relogin_lease_wait_s` | Kill-switch-gated, leased, off-loop, bounded, structurally incapable of raising; never-raises asserted structurally (AST) and behaviourally | ✓ VERIFIED | Read at HEAD. Both tuning knobs now read per-call with floor/ceiling validation (fixes CR-02/WR-03/WR-06 from round-1 review — confirmed present in code, not just claimed). |
| `analytics-service/main.py` — one `create_task(heal_mt5_terminal_session(), name="mt5_boot_heal")` | Wired into `lifespan`'s existing task list, after both startup guards, never awaited inline | ✓ VERIFIED | `grep -n` confirms the entry at line 314, after the dispatch/watchdog/enqueue/healthz tasks in the same list literal. `REQUIRED_PLATFORM_SECRETS` and `/health` unchanged (T-140.1-24 respected). |
| `analytics-service/tests/test_mt5_client_contract.py`, `test_mt5_relogin.py`, `test_mt5_concurrency.py` | Behavioural suites, signature-derived redaction gate, falsifiers, calibrated neuter/restore levers | ✓ VERIFIED | `pytest tests/ -k mt5 -q` → **648 passed, 2 skipped** (independently re-run, matches orchestrator's figure). Falsifier `test_the_criterion_1_predicate_reds_on_a_mutant_with_the_entry_excised` independently re-run: 2 passed. |
| `services/redact.py` / `src/lib/admin/pii-scrub.ts` — `DENYLIST_EXACT` | `"password"`, `"investor_password"` added; `include_local_variables=False` on Sentry init | ✓ VERIFIED | Confirmed present in both files (cross-language mirror intact), and `sentry_init.py:368` confirmed `include_local_variables=False`. Closes WR-05 (Sentry frame-locals credential leak). |
| `164.6.2-HANDOVER.md`, `docs/runbooks/mt5-go-live.md` | Founder handover with placeholders only; runbook documents the boot heal beside Step 2 | ✓ VERIFIED | Both exist; `npm run check:planning-hygiene` reported green after the founder's fix of `[164.6.2-HYGIENE-PROSE-SELF-TRIP]` (commit `a2ac07ae`), per 04-SUMMARY.md. Not independently re-scanned for disclosure shapes in this pass — low risk, prior gate result trusted (planning-hygiene is a repo-owned automated gate, not a claim). |
| `164.6.2-MEASUREMENT.md` | D-06 measurement record | ✓ VERIFIED (as a FAILED-observation record) | Exists, all 9 required sections present, honestly records OUTCOME 0 rather than forcing a false reading into outcomes 1-3. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `main.lifespan` | `services.mt5_relogin.heal_mt5_terminal_session` | lazy import + `asyncio.create_task(...)` inside the existing `tasks` list | ✓ WIRED | Confirmed at `main.py` line ~286 (import) / ~314 (task). Ordering pins (`test_lifespan_calls_the_startup_assertion_before_starting_the_worker`, `test_merged_lifespan_calls_the_guard`) pass. |
| `heal_mt5_terminal_session` | `Mt5Client.assert_session_authorized` / `initialize_with_credentials` | constructed inside `to_thread`, single bounded lease by `mt5_terminal_key` | ✓ WIRED | Confirmed by code read + passing behavioural suite; `_PRODUCTION_LEASE_SITES` re-cut to include the new site. |
| `Mt5Client.initialize_with_credentials` transport-raise/falsy arms | `_redact_credential_values` | by-value, multi-rendering, longest-first global pass | ✓ WIRED, and independently confirmed to cover the escaping class round-1 review found missing (CR-01) | Code read confirms `_credential_renderings` returns raw+repr+ascii+json.dumps; single sorted-by-length pass across all three literals combined (fixes the ordering bug B1 found separately). |
| `main.lifespan` | production deploy | merge to `main` → Railway redeploy of `quantalyze-analytics` | ✗ NOT YET TRAVERSED | This is the missing link. The branch exists and is internally consistent; it has not reached `main` and therefore never reached production, so the chain from "code exists" to "goal observably true in the running system" is incomplete. |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| MT5 test suite is green on the branch | `cd analytics-service && .venv/bin/python -m pytest tests/ -k mt5 -q` | `648 passed, 2 skipped, 5048 deselected` | ✓ PASS |
| Criterion-1 falsifier proves the wiring, not just its presence | `pytest tests/test_mt5_relogin.py -k criterion_1 -q` | `2 passed` | ✓ PASS |
| Redaction/relogin modules type-clean | `mypy --strict --follow-imports=silent services/mt5_relogin.py services/mt5_client.py` | `Success: no issues found in 2 source files` | ✓ PASS |
| Round-1 critical findings (CR-01 password-repr leak, CR-02 startup-abort) actually fixed in code, not just claimed fixed | direct code read of `_credential_renderings`, `_redact_credential_values`, `_env_float`/`_relogin_budget_s`/`_relogin_lease_wait_s` | Escape-aware multi-rendering redaction present; per-call float parsing with floor/ceiling, never raises | ✓ PASS |
| Sentry/redact denylist fix (WR-05) actually shipped | `grep -n '"password"\|investor_password' services/redact.py src/lib/admin/pii-scrub.ts`, `grep -n include_local_variables sentry_init.py` | present in both files; `include_local_variables=False` confirmed | ✓ PASS |
| ⭐ The actual production observation (ROADMAP criterion 2) | n/a — requires a live gateway restart | Not taken; explicitly not takeable pre-deploy | ✗ FAIL (this is the phase's own gap, not a spot-check artifact) |

## Requirements Coverage

| Requirement | Source | Status | Evidence |
|-------------|--------|--------|----------|
| `MT5-GATEWAY-LOGIN-01` | ROADMAP text (measured absent from `TODOS.md` and `.planning/REQUIREMENTS.md` by design — confirmed: `grep -n "MT5-GATEWAY-LOGIN-01" .planning/REQUIREMENTS.md` → no match) | ✓ SATISFIED for the buildable half / ⛔ NOT satisfied for the phase as a whole | Carried by all four plans; explicitly `requirements-completed: []` in every SUMMARY because criterion 2 (the criterion the requirement text names) has not closed. This is consistent, not an omission — the plans themselves refused to claim completion here. |

No orphaned requirements found for this phase.

## Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| — | none found | — | `grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` over `mt5_client.py`, `mt5_relogin.py`, `main.py`, and the three test files returned zero hits. |
| `164.6.2-REVIEW.md` frontmatter | stale `status: issues_found`, `critical: 2` | ℹ️ Info, not a gap | Predates the round-2 fixer. Independently re-verified in code (not taken on the SUMMARY's word): CR-01 and CR-02 are both fixed and hardened beyond the review's own suggested patch (escape-aware redaction covers repr+ascii+json.dumps; env-float parsing adds a floor as well as a ceiling, closing a round-2-discovered sub-round-trip hazard). Per this repo's own convention (`gsd-verifier` should judge the code, not the stale gate field), this is NOT counted as a blocker. |

## Human Verification Required

1. **Test:** After the branch merges to `main` and the deploy is confirmed (analytics `/health` `git_sha` == merge commit), restart the `mt5-gateway` Railway service with no VNC session before, during, or after; poll `scripts/mt5-diag.sh` at ~2-minute granularity to a second consecutive authorized reading; dispatch `gh workflow run prod-prober.yml -f arm=mt5`; read the verdict from the published runner log (never the exit code).
   **Expected:** The mt5 arm reports zero defects, closing ROADMAP criterion 2 and the phase.
   **Why human:** This is a live production action (service restart, real broker session, timed observation window) that cannot be performed or simulated from a code review; it is also explicitly reserved by founder decision as the ONLY thing that may close this criterion ("closes ONLY on an OBSERVED restart self-heal, never on the code alone").

## Gaps Summary

The phase built exactly what ROADMAP criterion 1 and the security/code-review process demanded, and did so carefully — falsifier-tested wiring, a signature-derived redaction gate proven to fail on two independent levers, all round-1 and round-2 review findings (including two critical, live disclosure classes: password surviving `repr()`-escaping, and a startup-abort typo hazard) independently confirmed fixed in the actual code rather than taken on the SUMMARY's word. Security audit shows `threats_open: 0` with an independent adversarial disclosure probe, re-verified here for two of the highest-risk items.

The one gap is criterion 2 — the phase's own declared load-bearing criterion — which the phase's own plan-04 measurement discovered cannot yet be honestly closed: the branch has never merged or deployed, so there is no mechanism in production capable of re-establishing anything, and restarting the gateway today would prove nothing. This is not a coding defect requiring a new plan; it is a sequencing gap the phase's own artifacts already name and route (ROADMAP criteria 6-7, 04-SUMMARY.md's decision log): merge → confirm deploy → run the already-scoped Wave 5. Recommending `gaps_found` here is not second-guessing the founder's 2026-09-14 decision to ship and measure post-ship — it is the honest classification of "is the phase's own stated goal observably true today", which the phase's own record answers no.

---

_Verified: 2026-09-14_
_Verifier: Claude (gsd-verifier)_
