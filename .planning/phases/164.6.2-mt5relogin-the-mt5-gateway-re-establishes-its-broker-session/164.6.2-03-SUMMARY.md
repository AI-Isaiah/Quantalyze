---
phase: "164.6.2"
plan: "03"
subsystem: docs
tags: [mt5, founder-handover, runbook, credential-boundary, human-action-gate, premise-failure]

requires:
  - phase: "164.6.2"
    plan: "01"
    provides: "the credential-free `-6` detector, `initialize_with_credentials()` with by-value redaction on both failure arms, and `mt5_terminal_key` — the path the handover's three variables actually feed"
  - phase: "164.6.2"
    plan: "02"
    provides: "`heal_mt5_terminal_session()` and the ONE `create_task` entry in `main.lifespan` — the shipped behaviour this plan DOCUMENTS rather than predicts, including the four measured failure modes (variables unset / gateway down / terminal busy / non-`-6` code)"
  - phase: "164.8.3"
    provides: "the shipped `mt5-not-authorized` prober kind that instrument 3 in the handover points at"
provides:
  - "`164.6.2-HANDOVER.md` — the founder's whole surface for criterion 3: the exact `railway variables --set` invocation with a PLACEHOLDER in every value position, targeting `quantalyze-analytics` and never `mt5-gateway`"
  - "`docs/runbooks/mt5-go-live.md` `### Step 2a` — the boot heal documented BESIDE Step 2, with the D-08 limit stated as a limit"
  - "the BLOCKING human-action gate that plan 04 opens on"
affects: ["164.6.2 plan 04 (the D-06 measurement, which cannot begin until the gate clears)"]

actuals:
  # chars/4 over the realized diff `git diff bb001e27..HEAD`, the SAME scale the
  # plan's `estimate` used. NOT a harness token count.
  tokens: 2534
  tasks: 1   # of 2 — task 2 is the founder gate and is DELIBERATELY NOT DONE
  # MEASURED, never narrated: `git rev-list --count bb001e27..HEAD` taken at
  # SUMMARY time, BEFORE this file's own docs commit.
  commits: 2

plan_head_before: bb001e27e5b0b2968ed5b803a8631a4350b51bf8

tech-stack:
  added: []
  patterns:
    - "a credential handover artifact whose SAFETY is structural, not procedural: every value position is an angle-bracket placeholder, so an unsubstituted paste is refused by the shell instead of silently setting junk"
    - "the confirmation channel is KEY-ONLY by construction — the artifact tells the founder to say 'they are set' in words and tells the agent the listing's raw output is never pasted anywhere"
    - "a runbook subsection that documents a new mechanism WITHOUT weakening the manual step it supersedes, because the FIRST instance of the thing still requires the manual step"

key-files:
  created:
    - .planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-HANDOVER.md
  modified:
    - docs/runbooks/mt5-go-live.md

key-decisions:
  - "⛔ The three PRE-EXISTING `check:planning-hygiene` violations were NOT hand-patched. Two of the three live in PLAN files — one of them is THIS plan's own verify command and one is plan 04's, which wave 4 will RUN. Editing a measurement instrument a future wave depends on, mid-execution, to make my own gate go green is the failure mode the phase's own fences exist to prevent. Reported as a premise failure and routed instead."
  - "Rule 7 applied to the duplicate `Step 2a`: PICKED one, did not average. The later bulleted section survives because it is better cited (names `heal_mt5_terminal_session`, `MT5-GATEWAY-LOGIN-01` and D-06); the one fact only the removed version carried was folded in rather than lost."
  - "Rule 2 security addition: the handover names the CLI's `--stdin` form for `MT5_PASSWORD` and warns off `--skip-deploys`. A password passed as a command-line argument is visible in the process list and lands in shell history — T-164.6.2-15's class, on the one command in this phase that carries a real credential."
  - "⛔ The prober's `mt5-not-authorized` remedy string still tells the operator to re-do the VNC login and still carries the stale `Account=1` sentence (D-09). NOT touched — the plan fences `scripts/prod-prober/**` byte-unchanged until plan 04 has READ with it. Recorded so plan 04 does not re-derive it."

patterns-established:
  - "Pattern: when a file is mutated between the Write and the commit, every verify reading taken before the mutation is VOID. Re-run the legs against the committed bytes — a green measured on a byte sequence that did not ship is exactly the anti-vacuity failure this repo's test discipline names."

# ⛔ EMPTY DELIBERATELY, and for the third consecutive plan. `MT5-GATEWAY-LOGIN-01`
# is carried by all FOUR plans — it is a PHASE-level requirement, 3 of 4 plans does
# not discharge it, and criterion 2 explicitly does not close until the D-06
# observation exists. The id is also MEASURED ABSENT from `.planning/REQUIREMENTS.md`
# and `TODOS.md`. `requirements.mark-complete` was NOT run.
requirements-completed: []

coverage:
  - id: D1
    description: "A handover artifact exists carrying the EXACT `railway variables --set` invocation for the three variables, with a PLACEHOLDER in every value position, targeting the ANALYTICS service (criterion 3, D-00)."
    requirement: "MT5-GATEWAY-LOGIN-01"
    verification:
      - kind: other
        ref: "measured on the COMMITTED bytes — placeholder value positions 3/3; `HANDOVER-NAMES-THE-THREE OK (5)`; the service named in the command itself"
        status: pass
    human_judgment: false
  - id: D2
    description: "It states what happens next AND marks the redeploy-fires-the-heal claim as an ASSUMPTION rather than a promise."
    verification:
      - kind: other
        ref: "`grep -ci 'assumption A5'` = 1 on the committed bytes; section 3 names it RESEARCH assumption A5 and routes it to plan 04's observation"
        status: pass
    human_judgment: false
  - id: D3
    description: "It states what the founder must NOT do — four bullets: not the gateway, not master credentials, never paste a value, do not re-do the VNC login first."
    verification:
      - kind: other
        ref: "section 4 carries exactly 4 `⛔` bullets, measured on the committed bytes"
        status: pass
    human_judgment: false
  - id: D4
    description: "`docs/runbooks/mt5-go-live.md` no longer presents the one-time VNC login as the ONLY mechanism; the boot heal sits BESIDE Step 2 with the D-08 limit stated as a limit."
    verification:
      - kind: other
        ref: "`### Step 2a` heading count 1 (after de-duplication); Step 2 and Step 3 both intact and byte-unchanged; all four required points present 4/4"
        status: pass
    human_judgment: false
  - id: D5
    description: "⛔ Nothing written by this plan contains a credential, an account number, a broker server string, or an absolute home path."
    verification:
      - kind: other
        ref: "`NO-DISCLOSURE-SHAPES OK` on both files, re-measured on the committed bytes"
        status: pass
      - kind: other
        ref: "`check:planning-hygiene` reports ZERO findings on either file written here — see the premise failure below for why the gate is nonetheless red"
        status: pass
    human_judgment: false
  - id: D6
    description: "Nothing under `deploy/`, `scripts/` or `.github/` is touched — the measurement instruments stay byte-unchanged until plan 04 has READ with them."
    verification:
      - kind: other
        ref: "`INSTRUMENTS-UNTOUCHED OK`, measured twice (before and after the de-duplication commit)"
        status: pass
    human_judgment: false
  - id: D7
    description: "⛔ CRITERION 3 — no agent entered, read, echoed or requested a credential."
    verification:
      - kind: other
        ref: "no `railway variables --set` was run; no `--kv` listing was run; the only `railway` invocations in this plan were `--help` reads (`railway variables --help`, `railway variable set --help`, `railway --version`)"
        status: pass
    human_judgment: false
  - id: D8
    description: "⛔ CRITERION 2 — the founder gate. NOT SATISFIED, deliberately."
    verification:
      - kind: manual
        ref: "task 2 is a `checkpoint:human-action` with `gate=blocking-human`. It is OPEN. No inference was made."
        status: pending
    human_judgment: true

duration: 41min
completed: 2026-09-14

# ⚠️ `status: complete` means THE AGENT-EXECUTABLE HALF IS COMPLETE — task 1 of 2.
# It does NOT mean the plan is closed. Task 2 is a blocking-human gate and is OPEN.
# The three keys below are the machine-readable form of that, because a reader
# that only parses `status:` would draw exactly the wrong conclusion.
status: complete
blocking_gate_open: true
blocking_gate_task: "Task 2 — checkpoint:human-action, gate=blocking-human (set the three Railway variables)"
plan_04_may_begin: false
---

# Phase 164.6.2 Plan 03: the founder handover — Summary

**The founder now has one artifact carrying the exact command, with a placeholder in every value position, and the runbook no longer tells an operator that a hand VNC login is the only way back from a lost session — and the phase now STOPS at the gate it was designed to stop at, with no value having passed through an agent and nothing inferred about whether the variables are set.**

## Performance

- **Duration:** ~41 min
- **Tasks:** 1 of 2 — ⛔ task 2 is the founder gate and is DELIBERATELY OPEN
- **Files:** 1 created, 1 modified
- **Commits:** 2 — MEASURED `git rev-list --count bb001e27..HEAD` at SUMMARY time (this file's own docs commit is not in that count because it did not exist when the count was taken)

## Task Commits

1. **Task 1: the handover artifact and the runbook subsection** — `0036f45f` (docs)
2. **Task 1 follow-up: de-duplicate `Step 2a`** — `2d738684` (docs)

## Files Created/Modified

- `.planning/phases/164.6.2-…/164.6.2-HANDOVER.md` — **NEW**, 131 lines. Frontmatter (`status: awaiting-founder`) plus five sections: the ask and why investor credentials suffice; the exact command; what happens next with A5 marked as an assumption; four prohibitions; and the three READ-ONLY instruments plan 04 runs.
- `docs/runbooks/mt5-go-live.md` — **+25 / −0** net. ONE new `### Step 2a` subsection between Step 2 and Step 3. ⛔ Step 2 and Step 3 are byte-unchanged.

## ⛔ PREMISE FAILURE — `npm run check:planning-hygiene` is RED at HEAD, and it was red BEFORE I wrote anything

The plan's task-1 verify leg 1 requires this gate green. **It cannot be green at HEAD, and not because of anything this plan wrote.** Measured before any edit and again after the commit — the violation list is byte-identical both times:

```
[check-planning-hygiene] 3 violation(s):
  - ABSOLUTE-HOME-PATH: …/164.6.2-01-SUMMARY.md:422
  - ABSOLUTE-HOME-PATH: …/164.6.2-03-PLAN.md:166
  - ABSOLUTE-HOME-PATH: …/164.6.2-04-PLAN.md:306
```

**Zero findings name `164.6.2-HANDOVER.md` or `docs/runbooks/mt5-go-live.md`.** The plan's own `<fails_when>` scopes the interpretation precisely — *"any finding **on the new artifact**"* — and there is none.

**The root cause, and it is self-referential.** All three occurrences are the bare structural prefix the gate's Rule 2 matches, with **no username after it** — so nothing identifying leaks. They are:

- **`164.6.2-01-SUMMARY.md`** — a sentence in wave 1's self-check *claiming* the artifact is clean, which trips the gate **by naming the pattern in prose**. The claim is true and the sentence asserting it is the violation.
- **`164.6.2-03-PLAN.md`** — this plan's own **verify leg 2 command**, which greps for that prefix as a literal. So the plan's instrument violates the plan's other instrument.
- **`164.6.2-04-PLAN.md`** — the same shape inside plan 04's verify command.

The gate's own docstring prescribes the remedy (*"To NAME the pattern in prose, spell it escaped"*) and has **no path allowlist by construction** — deliberately, because a path carve-out is the exact blindness it was built to fix.

### Why it was NOT fixed here

⛔ **Reported, not designed around, and not hand-patched.** Three reasons, in order of weight:

1. **Plan 04's line is a RUNNABLE instrument wave 4 will execute.** Editing its regex mid-phase to make my own gate go green risks breaking the measurement plan 04 exists to take. The phase already fences the instruments byte-unchanged "until plan 04 has READ with them"; this is the same hazard one layer up.
2. **`164.6.2-03-PLAN.md` is the plan I am executing.** Editing a plan mid-execution to satisfy its own verify is the hand-patch antipattern, not a fix.
3. **The plan's `<done>` says `git diff --name-only` lists EXACTLY the two paths in `<files>`.** Touching three more planning files breaks a stated done criterion to satisfy a different one.

⚠️ **This blocks `npm run lint`**, of which `check:planning-hygiene` is the last link. It must be resolved before the phase ships. **Routed to plan 04's decision checkpoint**, alongside the two deferrals already waiting there — the fix is three one-line prose/regex edits and belongs in one deliberate decision, not three improvised ones.

⚠️ **This SUMMARY deliberately spells the prefix escaped** (`\/Users\/`) rather than transcribing leg 2's command verbatim, precisely so it does not become the FOURTH occurrence. The command as run is otherwise byte-identical to the plan's.

## ⛔ A FILE I AUTHORED WAS REPLACED BETWEEN THE `Write` AND THE COMMIT

**Recorded because it silently voided four green verify readings, and because the next agent should not trust a pre-commit measurement.**

`164.6.2-HANDOVER.md` was written, all four verify legs run GREEN against it, and then — **between that measurement and `git commit`** — the file on disk was replaced by a different 131-line version (frontmattered, bulleted, carrying facts mine lacked: the `MT5_ENABLED` kill-switch note, D-02's one-warning behaviour, D-03). The commit captured the **replacement**, not the measured draft. The working tree was clean afterwards, so nothing was half-applied — but the readings no longer described what shipped.

**Remedy applied:** every leg was RE-RUN against the committed bytes, plus five acceptance spot-checks. One reading MOVED — `HANDOVER-NAMES-THE-THREE` fell **7 → 5** (still ≥ 3, still OK). Had the floor been 6, a stale green would have carried a failing artifact past the gate.

The same thing then happened to the runbook, and there it produced a **real defect**: a second `### Step 2a` appended beside the committed one, so the file carried **two subsections under the same heading** making the same four points. Resolved in `2d738684` by Rule 7 — **picked one, did not average**. The later bulleted section survives (better cited: it names `heal_mt5_terminal_session`, `MT5-GATEWAY-LOGIN-01` and D-06); the earlier prose one was removed in full; the single fact only it carried — that restarting the *gateway* never helped anyway, because the saved login lives on the persistent `/config` volume — was folded into the survivor.

⚠️ **One consequence to note honestly:** commit `0036f45f`'s message claims a Rule 2 addition recording that the CLI flags `--set` as LEGACY. The committed artifact carries the `--stdin` form and the `--skip-deploys` warning, but **not** the legacy note — that sentence was in the replaced draft. The measurement itself stands (below); the artifact simply does not restate it.

## Verify readings

⚠️ **Every reading below was taken on the COMMITTED bytes**, after the replacement described above. Pre-commit readings are void and are not reported as evidence.

| Leg | Command | Reading |
|---|---|---|
| 1 | `npm run check:planning-hygiene` | ⛔ **RED — 3 violations, all PRE-EXISTING, ZERO on either file written here.** Byte-identical list before and after. See the premise failure above. |
| 2 | the disclosure-shape scan over both files | `NO-DISCLOSURE-SHAPES OK` |
| 3 | the instruments-untouched tripwire | `INSTRUMENTS-UNTOUCHED OK` (measured twice — after each commit) |
| 4 | the names-the-three banner | `HANDOVER-NAMES-THE-THREE OK (5)` |

**Acceptance spot-checks, on the committed bytes:**

```
placeholder value positions (--set "MT5_*=<…>") : 3   (every value position)
names mt5-gateway as the WRONG target            : 2
marks A5 as an assumption                        : 1
section count (## 1..5)                          : 5
section-4 ⛔ bullets                              : 4
Step 2a heading count                            : 1   (was 2 before 2d738684)
Step 2 / Step 3 intact                           : 1 / 1  (byte-unchanged)
four required points in the surviving Step 2a    : 4/4
```

**`git diff --name-only bb001e27..HEAD`** → exactly the two paths in `files_modified`; nothing under `deploy/`, `scripts/` or `.github/`.

**The `railway` CLI readings** (the only `railway` invocations this plan made — all `--help`, none touching a variable):

```
railway --version              -> railway 4.36.1
railway variables --help       -> --set is documented "(legacy, use 'variable set' instead)";
                                  --set-from-stdin <KEY> and --skip-deploys both exist
railway variable set --help    -> --stdin (single KEY only), --service, --environment, --skip-deploys
```

⭐ The `--stdin` form is why the handover carries it: a password passed in `argv` is visible in the process list and lands in shell history. That is T-164.6.2-15's class on the one command in this phase that carries a real credential.

## Deviations from Plan

**1. [Rule 2 — missing security control] The handover names the `--stdin` form and warns off `--skip-deploys`**
- **Found during:** Task 1, reading `railway variables --help` to confirm the plan's command shape.
- **Issue:** the plan's specified invocation puts the broker password in `argv`. Measured: the CLI offers `--stdin`, which does not.
- **Fix:** the plan's exact `--set` invocation is present as required (the must_have is honoured literally); the `--stdin` form is named beside it as the safer route for `MT5_PASSWORD` only. `--skip-deploys` is called out as a flag NOT to pass, because it would leave the variables set and the session still lost.
- **Files:** `164.6.2-HANDOVER.md`. **Commit:** `0036f45f`.

**2. [Rule 1 — bug] Duplicate `### Step 2a`**
- **Found during:** post-commit re-verification, after the concurrent replacement.
- **Fix:** picked one section, folded in the one fact the other uniquely carried, removed the duplicate.
- **Files:** `docs/runbooks/mt5-go-live.md`. **Commit:** `2d738684`.

**3. [Reported, NOT fixed] The three `check:planning-hygiene` violations** — see the premise failure above. Routed to plan 04's decision checkpoint.

## ⛔ `.planning/WINDOWS.md` append REFUSED — pre-existing, third consecutive wave

```
Error: Ledger counts disagree with entries: frontmatter open/waived/fixed/total=36/0/11/47
       but entries yield 40/0/10/50.
```

Byte-identical to the message both prior waves recorded. **Nothing was written** — confirmed by an empty `git status --short .planning/WINDOWS.md`. ⛔ Not hand-reconciled: this is a SHARED cross-phase register and guessing which of two disagreeing counts is right would corrupt it for every other phase. Waves 1 and 2 refused for the same reason; this is the third refusal and the defect is now three waves old.

## Deferrals still awaiting a destination phase

Carried forward, **not lost** — all three land at plan 04's decision checkpoint:

1. `[164.6.2-KILLSWITCH-COMMENT-DRIFT]` — from wave 2.
2. `[164.6.2-RAISE-LAST-SHAPE-ONLY]` — from wave 2.
3. **NEW — `[164.6.2-HYGIENE-PROSE-SELF-TRIP]`** — the three pre-existing hygiene violations above. ⚠️ This one is not merely a deferral: it blocks `npm run lint` and therefore the ship.

⚠️ Also noted, owned by plan 04 and NOT touched here: the prober's `mt5-not-authorized` remedy string still instructs the operator to re-do the VNC login and still carries the stale `Account=1` sentence D-09 folds into this phase. `scripts/prod-prober/**` is fenced byte-unchanged until plan 04 has READ with it.

## ⛔ THE FIFTEENTH `STATE.md` CLOBBER — and this one INVERTED TWO CLAIMS

Handlers were RUN first, per the founder's standing instruction, then diffed. `roadmap.update-plan-progress` was **clean again** (third wave running: `2/4 → 3/4` and one checkbox — nothing else). `state.*` was not.

`git diff --stat` showed **111 changed lines in `STATE.md` for two blockers**. The diff decomposed into three species, and the third is new and serious:

1. **Whitespace vandalism** — a blank line injected after **every** comment line in the lineage banners, roughly doubling them.
2. **Disk-derived progress** — `Progress: 42% → 38%`. ⛔ Reverted: `STATE.md`'s own banner states that disk-derived counts are WRONG in this checkout, because the `-pr` filter strips `.planning/phases/**` for four complete phases. The verified ALL-REFS census (37/20/171/166/54) was preserved.
3. ⛔ **MEANING-INVERTING PROSE CORRUPTION.** `state.add-blocker`'s "removes placeholders" behaviour stripped the bare word `none`/`None` from **four unrelated lines** elsewhere in the file — almost certainly a global, case-insensitive removal of the `None` placeholder token. Two of the four **inverted a claim**:

| Before | After the handler | Effect |
|---|---|---|
| `Gates re-run by the orchestrator …, none delegated:` | `…, delegated:` | ⛔ **INVERTS a verification claim** — "nothing was delegated" became "it was delegated" |
| `**None is CARDINAL**` | `**is CARDINAL**` | ⛔ **INVERTS a severity claim** — "no residual is cardinal" became "it is cardinal" |
| `Round 6 found none, and both reviewers…` | `Round 6 found , and both…` | mangled |
| `Round six found none, so opening…` | `Round six found , so opening…` | mangled |

**Remedy:** `STATE.md` restored from the byte backup (`cmp`-proven identical), then the two blockers and the session block hand-applied. Re-measured after repair: **4 changed lines total, 0 injected blank lines, all four `none` phrases INTACT, progress bar and all five census keys preserved.**

⚠️ **`state.advance-plan` was DELIBERATELY NOT RUN.** Advancing the plan counter past a `gate="blocking-human"` checkpoint writes the very inference criterion 2 exists to forbid — a `STATE.md` reading "Current Plan: 04" is permission in machine-readable form. The gate is recorded as a BLOCKER instead.

⭐ **The founder's "try gsd-tools FIRST, intervene only on a MEASURED clobber" rule worked exactly as intended** — and the byte backup taken before running them is the only reason the two inversions were caught rather than committed.

## Self-Check: PASSED

- `164.6.2-HANDOVER.md` — FOUND on disk.
- `docs/runbooks/mt5-go-live.md` — FOUND on disk, `### Step 2a` present exactly once.
- Commits `0036f45f` and `2d738684` — both resolve in `git log --oneline --all`.
- Commit count MEASURED, not narrated: `git rev-list --count bb001e27..HEAD` = 2.
- ⛔ No credential was entered, read, echoed or requested. No `railway variables --set` and no `--kv` listing was run.

## ⛔ STOPPED AT THE GATE — task 2 is OPEN

Task 2 is a `checkpoint:human-action` with `gate="blocking-human"`. `workflow.auto_advance` does not apply and **nothing was inferred**. The gate clears only on an explicit founder statement in words PLUS a key-only listing showing all three names. Plan 04 begins there and not before.
