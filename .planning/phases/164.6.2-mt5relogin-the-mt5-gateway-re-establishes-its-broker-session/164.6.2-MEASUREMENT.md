---
phase: "164.6.2"
plan: "04"
kind: measurement
status: BLOCKED — the event under test was deliberately NOT performed
measured_by: agent (no VNC session, no login, no credential read)
measured_on: 2026-09-14
branch_head: a4475b95
deployed_analytics_sha: 5dead88a
---

# Phase 164.6.2 — D-06 measurement record

**⛔ THE MEASUREMENT WAS NOT TAKEN, AND THE REASON IS A PREMISE FAILURE THAT NOBODY IN THIS
PHASE RECORDED.** The three Railway variables are set, the redeploy fired and succeeded — and the
deployed analytics image **does not contain the boot heal this phase built.** Production deploys
`main`; the heal lives only on the unmerged phase branch. There is therefore no mechanism in
production that could re-establish the terminal's session, and restarting the gateway today would
produce a reading with no discriminating power whatsoever.

⛔ **This is NOT outcome 3.** Outcome 3 names RESEARCH assumption A3 — a broker-side block — as the
live suspect. The measured live suspect here is *"the heal is not deployed"*, and writing A3 into
the record would implicate the broker for the absence of our own code. Naming the wrong cause to
an operator is this phase's own defect class, one level up.

---

## The premise that failed, measured rather than argued

| # | Fact | Value | How measured |
|---|---|---|---|
| 1 | The three variables are set on the ANALYTICS service | `MT5_LOGIN`, `MT5_PASSWORD`, `MT5_SERVER` all PRESENT | `railway variables … --kv` piped through `cut -d= -f1` — ⛔ key names only, no value entered any context. 55 key names visible in total. |
| 2 | D-00 holds — they are NOT on the gateway | 0 of the three present on `mt5-gateway` | same key-only method |
| 3 | ⭐ RESEARCH assumption A5 is now a MEASURED FACT, not an assumption | setting the variables fired **two** analytics redeploys, at `2026-09-14T05:58:54Z` (superseded) and `2026-09-14T06:07:35Z` (**SUCCESS**) | `railway deployment list --service quantalyze-analytics --environment production --json` |
| 4 | The analytics service is UP and its worker is ticking | `status ok`, `config_ok true`, `config_degraded_secrets []`, worker age ~30 s at `2026-09-14T06:14:01Z` | `curl …/health` |
| 5 | ⛔ **The deployed sha is `5dead88a` — `main`, i.e. Phase 164.8.3's ship** | `git_sha 5dead88a…` in `/health`, and the same sha in the deployment meta | `/health` + deployment meta agree |
| 6 | ⛔ **The boot heal is ABSENT from that sha** | `analytics-service/services/mt5_relogin.py` **does not exist** at `5dead88a`; `heal_mt5_terminal_session`, `initialize_with_credentials` and `mt5_terminal_key` have **ZERO** occurrences anywhere under `analytics-service/` at that sha | `git ls-tree` + `git grep -c` against the deployed sha |
| 7 | ⛔ **The deployed `lifespan` starts FOUR tasks, not five** | `dispatch_loop`, `watchdog_loop`, `daily_enqueue_loop`, `healthz_bridge`. At branch HEAD there is a fifth, `mt5_boot_heal` | `git show 5dead88a:analytics-service/main.py` vs the working tree |
| 8 | The deployed code never reads the three variables | the only `MT5_LOGIN` hits at that sha are `MT5_LOGIN_TIMEOUT_MS`, an unrelated IPC ceiling | `git show … | grep -n` |
| 9 | ⛔ **Production deploys `main`, and only `main`** | **20 of the last 20** analytics deployments carry `branch: main`. The phase branch has never been deployed and is not merged | deployment meta, tallied by branch |

**The causal chain, with the broken link named:**

> variables set ✅ → redeploy fires ✅ (A5 confirmed) → a new analytics process starts ✅ →
> `main.lifespan` runs ❌ **without `heal_mt5_terminal_session`, because the deployed sha predates
> plan 02** → nothing re-establishes the session → the `-6` stands.

⚠️ **`164.6.2-HANDOVER.md` section 3 is now known to be false OF PRODUCTION.** It tells the founder
*"A new analytics process runs `main.lifespan`, which starts `heal_mt5_terminal_session` as a
background task."* That is true of the branch and false of the deployed image. The handover marked
the redeploy (A5) as an assumption and was right to; what no artifact in this phase asked was
**which code the redeploy deploys.** Measured across `164.6.2-RESEARCH.md`, `-CONTEXT.md`,
`-HANDOVER.md` and all four PLAN files: the merge/deploy ordering is named **nowhere**.

---

## Restart stamp

**NONE — the gateway was deliberately NOT restarted.** Stated as a decision, with its reasoning,
because a missing stamp must never read as an omission:

1. The event under test is *"restart the gateway; does the session come back with no human?"*. The
   only mechanism in the system that could bring it back is the boot heal, which fires at ANALYTICS
   startup (D-08) and **is not in the deployed image** (facts 5–7 above).
2. A gateway restart therefore has exactly ONE possible outcome — the `-6` persists — and that
   outcome is entailed by the absence of the code, not by anything about the broker, the saved
   account, or A3. It would distinguish nothing.
3. The terminal is ALREADY at `-6` (poll log below), so the restart is additionally a null event
   against an already-lost session: it cannot even establish a cleaner starting state.
4. ⛔ Recording such a run as outcome 3 would name A3 as the live suspect against an unmeasured
   state. That is threat T-164.6.2-24's class applied to the measurement instead of to the remedy.

⛔ **No VNC session was opened, before, during or after. No login was performed by any instrument.
No credential was entered, read back, or requested.**

## First authorized reading

**NONE. The terminal was never authorized at any point during this window**, and no mechanism
capable of authorizing it was running in production.

Both consecutive diagnostic readings and the prober's own arm agree: `initialize: false`,
`last_error: [-6, "Terminal: Authorization failed"]`, `terminal_info: null`.

⚠️ **`trade_allowed` was UNREADABLE throughout**, exactly as the plan predicted — `terminal_info` is
`null` while the authorization failure is in force, so the flag has no value to report. ⛔ Record
this as a NULL, and note explicitly that **a null before a heal and a `false` before a heal look
identical and mean different things**. This null is a statement that nothing could be read; it is
NOT a measurement of the Expert-Advisors options.

## Delta

**UNDEFINED — and it is undefined by construction, not by a failed attempt.** A delta is
`first authorized reading − restart stamp`, and neither term exists. Nothing was restarted and
nothing was ever authorized.

⭐ **What CAN be stated as a bound, and it is the useful half:** the terminal has been at `-6`
continuously across every reading taken here, and the standing evidence puts the current outage at
least a day old (run 34758502223, 2026-09-13, already reported `-6`). Until the phase branch
reaches `main` and deploys, **the healing window is not merely unbounded — there is no healing
mechanism in production at all.** That is a stronger and more actionable statement than the
unbounded-window answer RESEARCH expected, and it is the one D-06 actually gets today.

## What closed it

**NOTHING CLOSED IT. It is still open as of the last reading in the poll log.**

The honest entry for this field is: *the mechanism that would close it — `heal_mt5_terminal_session`
at analytics startup — is not present in the deployed image, so no analytics restart, scheduled job
or redeploy can close it. Two analytics restarts DID occur in this window (`05:58:54Z` and
`06:07:35Z`) and neither closed it, which is exactly what facts 5–7 predict.*

⭐ That last sentence is itself a measurement worth keeping: **two real analytics restarts were
observed to NOT heal the session**, and the reason is now known and code-level rather than
speculative. When the heal does ship, the same two events are the ones expected to close it.

## Prober run id

- **BASELINE dispatch (pre-event), taken by this plan:** run 34812749949 — narrowed
  `workflow_dispatch`, `arm=mt5`, created `2026-09-14T06:16:19Z`, step completed `06:16:51Z`.
- **Standing scheduled baselines** (handover instrument 3, no dispatch needed): run 34799447328
  (`2026-09-14T02:30:34Z`) and run 34790895414 (`2026-09-13T23:51:29Z`).
- **Prior standing evidence for the current outage:** run 34758502223 (2026-09-13).

⛔ **THERE IS NO POST-RESTART DISPATCH, AND ITS ABSENCE IS THE POINT.** ROADMAP criterion 2 closes
on a POST-RESTART narrowed run returning no defect. No restart was performed (see `Restart stamp`),
so that run does not exist and criterion 2 is **NOT closed**.

⚠️ **A finding about the gate itself, recorded rather than exploited.** This plan's
`PROBER-OBSERVATION-RECORDED` leg counts run references and requires **two or more**. This record
carries four legitimately — so the leg's literal count is satisfied while its stated intent (*"the
post-restart observation was taken"*) is not. ⛔ The banner that leg prints is **NOT** evidence that
criterion 2 closed, and nothing here should be read as if it were. Routed as a named deferral.

## Prober verdict

Read from the **published runner log** of run 34812749949, verbatim. ⛔ Not from the exit code.

```
prod-prober: NARROWED DIAGNOSTIC run of arm mt5
credential: RAILWAY_API_TOKEN present
credential: RAILWAY_PROJECT_ID present
credential: RAILWAY_MT5_SERVICE present
credential: RAILWAY_ENVIRONMENT present

⚠️ NARROWED DIAGNOSTIC RUN (--arm): arm floors NOT enforced.
   This mode never exits 0, so it can never be mistaken for a passing gate.

arms: 1/4/0   (executed/registered/credential-blocked)
seam-invocations: 1   (fetch 0 / sql 0 / ssh 1)

❌ 1 defect(s):

  KIND                        ARM         SUBJECT
  --------------------------  ----------  --------------------------
  mt5-not-authorized          mt5         -6
      MT5 initialize() failed with -6: the rpyc bridge ANSWERED, so the terminal is up and the
      IPC transport is not implicated — but NO ACCOUNT IS AUTHORIZED on it. That is why
      terminal_info() came back null and neither connected nor trade_allowed could be read.
      One state, one cause — not a code to look up.
```

**defect count: 1**

⛔ That count is the **BASELINE** (pre-event) reading for the mt5 arm. There is no post-event count,
because there was no event. A `1` here is a recorded measurement, not a failure to reach zero.

⭐ **Two things this run establishes that are worth banking anyway.** First, Phase 164.8.3's shipped
`mt5-not-authorized` kind is confirmed working **against live production** — the arm names the `-6`
on subject `-6` with its own remedy instead of sending the operator to an error table, which is
164.8.3's criterion 2 observed in the field for the first time. Second, its derived clause selected
the *"came back null"* wording correctly against a genuinely null `terminal_info`, so the row does
not contradict its own transcript.

⚠️ **One correction to this plan's `<measured_baseline>`:** it states a narrowed dispatch *"exits 2
BY DESIGN"*. MEASURED here, the step completed with **exit code 1**. The prohibition is unaffected
and still correct — the exit code is never the verdict — but the specific number in the plan is
wrong, and a future reader checking for a `2` would not find one.

## Question THREE verdict

The contested sentence (D-09): *a login is an ACCOUNT CHANGE, and MT5 re-clears the Expert-Advisors
options on every account change.*

- **R1 — `trade_allowed` immediately after the first successful heal:** ⛔ **UNAVAILABLE.** There was
  no successful heal, so the moment R1 is defined at does not exist. `terminal_info` was `null` on
  every reading.
- **R2 — `trade_allowed` after a subsequent real per-account login:** ⛔ **UNAVAILABLE**, for the
  same reason and for a second one: with the terminal unauthorized there is no session for a
  per-account login to ride on.
- **The event that produced R2:** none. ⛔ No login was triggered, and neither instrument was made
  to log in.

**verdict: UNSETTLED**

**Why, in one sentence:** `terminal_info()` returns `null` while the `-6` is in force, so
`trade_allowed` has no readable value at all — which is precisely the condition this plan's own
`<measured_baseline>` names as the reason the correction had to ride on a successful heal rather
than get a plan of its own.

⛔ **CONSEQUENCE, AND IT IS BINDING:** task 2 edits NOTHING. All four copies of the stale
account-change claim stay exactly as they ship, and `scripts/prod-prober/arms/mt5.mjs` and
`scripts/mt5-diag.sh` are proven byte-identical to their pre-edit backups. Editing operator
instructions against an unmeasured state is threat T-164.6.2-24 and the plan planned for this
branch by name.

## Outcome

**OUTCOME 0 — PREMISE FAILURE. The measurement was not takeable, and this is none of the plan's
three endings.**

| Outcome | Applies? | Why |
|---|---|---|
| 1 — heal works, window acceptable | ❌ | no heal ran; no window exists |
| 2 — heal works, window unbounded without an analytics restart | ❌ | two analytics restarts occurred and neither healed, because the heal is not deployed |
| 3 — heal does not clear it (A3, broker-side block) | ❌ **and asserting it would be wrong** | outcome 3's whole content is *"A3 is the live suspect"*. A3 is NOT the live suspect: the measured suspect is that production runs `main`, which has no heal. The broker has not been tested at all. |
| **0 — the event under test could not be performed** | ✅ | the mechanism under test is absent from the deployed image (facts 5–9) |

⛔ **ROADMAP criterion 2 does NOT close and the phase does NOT close.** It closes on an observation,
and there is none.

⭐ **What the phase nonetheless gained today, all of it real:** A5 promoted from assumption to
measured fact; the founder gate confirmed key-only with D-00 intact; 164.8.3's `-6` naming confirmed
working against live production; two analytics restarts observed NOT to heal, with a code-level
reason; and a structural ordering defect found that would otherwise have been discovered only after
a ship claimed to close a criterion it could not have closed.

## Poll log

All stamps UTC. ⛔ Only the five permitted diagnostic fields are recorded — never the terminal's
install `path`, never its `build`.

| # | Stamp | Instrument | Reading |
|---|---|---|---|
| 1 | `2026-09-14T06:13:24Z` | `./scripts/mt5-diag.sh` | `initialize: false`, `last_error: [-6, "Terminal: Authorization failed"]`, `terminal_info: null` → `connected` / `trade_allowed` UNREADABLE |
| 2 | `2026-09-14T06:14:01Z` | analytics `/health` | `status ok`, `git_sha 5dead88a…`, `config_ok true`, `config_degraded_secrets []`, worker age ~30 s |
| 3 | `2026-09-14T06:16:19Z` | narrowed prober, run 34812749949 | `mt5-not-authorized` / `-6`, **defect count 1** |
| 4 | `2026-09-14T06:16:54Z` | `./scripts/mt5-diag.sh` | **IDENTICAL to reading 1** — `initialize: false`, `[-6, "Terminal: Authorization failed"]`, `terminal_info: null` |

⭐ **Readings 1 and 4 are the SECOND CONSECUTIVE confirmation the plan requires**, 3.5 minutes
apart, so `-6` is recorded as a STATE and not as transport noise. ⛔ No single reading was treated
as a state; no IPC-timeout reading occurred at all in this window, so the `-10005` noise rule was
never in play.

**Deployment events inside the same window, for correlation:**

| Stamp | Event |
|---|---|
| `2026-09-14T05:58:54Z` | analytics deployment, `main` @ `5dead88a` — SUCCESS then superseded |
| `2026-09-14T06:07:35Z` | analytics deployment, `main` @ `5dead88a` — **SUCCESS, current** |

Two analytics process restarts, neither followed by any change in the terminal's state.

---

## Observation: the env account vs. the connected MT5 key

⛔ **NOT DETERMINABLE BY AN AGENT, and the reason is a prohibition rather than a gap.** Comparing the
account behind `MT5_LOGIN` with the account on the only connected MT5 key requires READING the value
of `MT5_LOGIN`, and criterion 3 forbids any agent from reading a credential back. Only key NAMES
were ever observed here.

⚠️ **Carried forward for whoever takes the real measurement:** the strict-equality account bracket
fails loud on a mismatch, so if the heal ships and the terminal authorizes but per-account work then
refuses, an account mismatch between the env credential and the stored key is the first thing to
check — and it is a founder-side check, not an agent-side one.

## ⛔ What must happen before this measurement can be taken at all

Stated as facts, not as a recommendation — the choice between them is the founder's, at plan 04's
decision checkpoint:

1. The phase branch must reach `main` and deploy, so that `heal_mt5_terminal_session` is actually
   running in production. Production deploys `main` and nothing else (fact 9).
2. ⚠️ That ship is currently blocked by `[164.6.2-HYGIENE-PROSE-SELF-TRIP]` — `npm run lint` is RED
   on three PRE-EXISTING hygiene violations, none of them written by this wave.
3. Only then do the restart, the poll, the post-restart prober dispatch and question THREE's R1/R2
   become takeable — **in that order, and all of them in one sitting**, because R1 is readable only
   in the moments after a heal actually works.

⛔ This creates an ordering the phase never planned for: **a criterion that closes on a production
observation cannot be closed before the code ships.** Resolving that is a founder decision, not an
executor one.
