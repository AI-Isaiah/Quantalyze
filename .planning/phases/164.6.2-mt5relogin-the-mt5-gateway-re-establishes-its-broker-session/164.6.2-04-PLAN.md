---
phase: "164.6.2"
plan: "04"
type: execute
wave: 4
depends_on: ["164.6.2-03"]
files_modified:
  - .planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md
  - scripts/prod-prober/arms/mt5.mjs
  - scripts/mt5-diag.sh
  # CONDITIONAL — touched ONLY when a shipped self-test assertion has to be fixed to grep what
  # actually matters (task 2(a)). The BLAST-RADIUS gate already permits exactly this one path;
  # it is declared here because the wave guard reads frontmatter, not the gate.
  - scripts/prod-prober/run.mjs
  - .planning/ROADMAP.md
autonomous: false
requirements: ["MT5-GATEWAY-LOGIN-01"]

estimate:
  # Raised from 38000 at the 2026-09-13 revision pass: task 2 gained a four-lever fence
  # calibration (three neuters + a GREEN control, each with an applied-proof and a cmp restore)
  # and three new verify legs. Calibration factor 1 (0 samples), so tokens == raw_tokens.
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "⛔ CRITERION 2 IS CLOSED ON AN OBSERVATION, NEVER ON THE CODE: the mt5-gateway service is RESTARTED and the prober's mt5 arm is observed to return no defect afterwards, with NO VNC session in between."
    - "⛔ THE CLOSING OBSERVATION IS THE PROBER ARM'S OWN, AND IT IS A REQUIRED ELEMENT OF THE RECORD: a POST-RESTART narrowed prober dispatch, its run id, and its defect-table verdict are named sections of `164.6.2-MEASUREMENT.md` and are required by the completeness gate. ⚠️ `mt5-diag.sh` is a DIFFERENT instrument on a DIFFERENT path; the arm is credential-less BY DESIGN, which is exactly why its reading — not the diagnostic's — is the one criterion 2 closes on."
    - "⛔ D-09 GOVERNS THE `-6` REMEDY EDIT IN THIS PLAN, and it is the ONLY thing that licenses it. ROADMAP criterion 4 fences the `-6` defect vocabulary to Phase 164.8.3 PROBERAUTH; D-09 (founder, 2026-09-13, taken AFTER 164.8.3 shipped and could no longer receive the work) folds ONE stale sentence back into this phase because this phase produces the measurement that settles it. ⛔ Nothing else about the `-6` vocabulary is re-acquired here — no new defect kind, no new fixture, no classification change."
    - "⛔ ALL FOUR COPIES OF THE STALE CLAIM ARE CORRECTED, NOT THREE: the `mt5-not-authorized` remedy string and the arm's quoted header block in `scripts/prod-prober/arms/mt5.mjs`, and BOTH the header comment block and the PRINTED `NOTE` heredoc body in `scripts/mt5-diag.sh`. ⚠️ The heredoc body is the copy an operator reads AT the incident, and it is not comment-prefixed — a fence that classifies non-executable text by a leading `#` structurally forbids correcting the one that matters most."
    - "⛔ THE EDIT IS LICENSED BY THE MEASUREMENT, AND THE LICENCE BITES: a correction present in either instrument IMPLIES `164.6.2-MEASUREMENT.md` records a STALE question-THREE verdict, and a non-STALE verdict IMPLIES both files are byte-identical to their pre-edit backups. A gate that passes identically whether or not the reading exists is a step that cannot fail."
    - "⛔ THE READ-ONLY FENCE PINS BYTES, NOT COMMENT PREFIXES, and it is SHOWN able to fail. The shebang, the shell options, the variable defaults, the `PY` heredoc body and the `railway ssh` invocation are byte-compared against a pre-edit `cp` backup; the header comment block and the `NOTE` body are the only editable regions. Three neuter levers — a shebang swap, a `#`-prefixed line injected into the `PY` heredoc body, and a variable-default change — are each proven APPLIED and then observed RED, and a fourth GREEN control proves the fence still permits the correction it must permit."
    - "⛔ THE ARM'S `VERBATIM` CLAIM IS ENFORCED, NOT ASSERTED IN PROSE: the two paired paragraph blocks are extracted and compared by a gate in the same verify as the self-test. Before this phase the claim was a sentence with no mechanism; after it, correcting one block without the other reds."
    - "⛔ D-06: the healing WINDOW is MEASURED and RECORDED, never guessed — the wall-clock of the restart, the first reading that shows the terminal authorized, the delta, and WHAT CLOSED IT (the worker's own startup, a scheduled job, or an operator restart of the analytics service)."
    - "⚠️ RESEARCH's expected answer — that the window is UNBOUNDED until something restarts the analytics service — is treated as a LIKELY finding to be confirmed or refuted, not as a failure. If it lands that way, that IS D-06 working as designed and the keepalive follow-up writes itself."
    - "⭐ Question THREE is FALSIFIED or CONFIRMED for free on the same run: `trade_allowed` is read after the first successful heal and again after a subsequent real per-account login, and both readings are recorded. The repo currently contradicts itself about this in shipped code, and that one reading settles it."
    - "The stale claim is corrected ONLY after the measurement, and a measurement showing the claim still CORRECT closes the correction task as `no change needed, and here is the evidence` — a legitimate outcome, planned as one."
    - "⛔ Phase 164.8.3's shipped criterion-2 self-test still passes at its baseline, and if an assertion greps a phrase the correction would change, the ASSERTION is fixed to grep what actually matters — never weakened."
    - "⛔ The instruments stay READ-ONLY: no login is added to `scripts/mt5-diag.sh` or to the prober arm, and every byte of the diag script OUTSIDE its two non-executable prose regions is unchanged. ⭐ `NON-EXECUTABLE` covers printed prose as well as comments — the `NOTE` heredoc body is text the script emits, not shell it runs — while BEHAVIOUR stays pinned, which is what 164.8.3's criterion 5 actually fences."
    - "Every deferral this phase produces NAMES A PHASE and is entered via `/gsd-phase --edit` — never hand-edited into the ROADMAP."
  artifacts:
    - .planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md
  key_links:
    - "`railway restart -s mt5-gateway` -> the terminal loses its saved session -> `./scripts/mt5-diag.sh` reads `initialize` / `last_error` / `trade_allowed` -> `gh workflow run prod-prober.yml -f arm=mt5` publishes the arm's own verdict -> the delta between the restart and the first authorized reading IS D-06's number."
    - "the analytics service's own restart -> `main.lifespan` -> the boot heal (plan 02) -> `initialize(login=…, password=…, server=…)` -> the terminal's session. This is the ONLY mechanism in the system that re-establishes it, which is why 'what closed it' is the load-bearing half of the record."
    - "the `mt5-not-authorized` REMEDY in `scripts/prod-prober/arms/mt5.mjs` <-> the criterion-2 self-test scenario in `scripts/prod-prober/run.mjs`, whose legs assert the four required elements are PRESENT and the error-table lookup instruction is ABSENT. Any correction is checked against those predicates BEFORE it is written."
    - "the arm's header block <-> `scripts/mt5-diag.sh`'s header — the arm states it carries those paragraphs VERBATIM, so correcting one without the other makes the arm's own claim false."
  prohibitions:
    - "⛔ Never perform a VNC login, and never ask the founder to. A hand-established session destroys the observation criterion 2 exists to make."
    - "⛔ Never make an instrument a mutator. `scripts/mt5-diag.sh` and the prober arm call `initialize()` / `last_error()` / `terminal_info()` and NEVER `login()` — D-06 rejected prober self-healing explicitly, and 164.8.3's criterion 5 fences the diag script."
    - "⛔ Never read the narrowed prober arm's EXIT CODE as a verdict. `-f arm=<name>` exits 2 BY DESIGN so a diagnostic run can never be mistaken for a pass; the reading is the published runner log."
    - "⛔ Never paste the diag script's `path` or `build` fields into any artifact. `.planning/` is TRACKED in a PUBLIC repo and those carry a production filesystem path and a terminal build."
    - "⛔ Never quote the MT5 account number, a broker server string, or a password. Never an absolute home path."
    - "⛔ Never treat a single `-10005` as a measurement. `mt5-diag.sh` shares the terminal's IPC bridge with real work, so a probe overlapping a validate can read `-10005` legitimately. Poll at ~2-minute granularity; a SECOND consecutive reading is the confirmation."
    - "⛔ Never re-litigate criterion 1's shape. If the window is judged too long, the answer is a keepalive or a reconnect hook in a NAMED follow-up phase — the shape was a founder decision taken on a measurement."
    - "⛔ Never widen the prober arm's projection. `connected` and `trade_allowed` are PRINTED AND NEVER JUDGED; a re-cleared flag must not become a defect here."
    - "⛔ Never edit `.github/workflows/prod-prober.yml`, `scripts/prod-prober/run.mjs`'s fixtures, or any historical `.planning/` record. History stays as written."
    - "⛔ Never hand-edit a deferral into `.planning/ROADMAP.md`. Every one goes through `/gsd-phase --edit` and names a destination phase; ⛔ never leave a destination blank."
    - "⛔ Never run a full `npx vitest` — scope every frontend run to the one file."
    - "⛔ Never cite `file:line` — cite BY SYMBOL (`[164.7-CITATION-DRIFT-01]`)."
---

<objective>
Close criterion 2 on an OBSERVATION, measure D-06's healing window rather than guessing it, settle
the repo's self-contradiction about whether a login re-disables algo trading, and route everything
this phase found to a named destination.

Purpose: the founder was offered "close on the code, verify later" and REJECTED it. Plans 01-03
built and handed over; nothing before this plan proves the session comes back. D-06's reasoning is
explicit — a keepalive designed before any measurement is an interval tuned against a guess — so
this plan's deliverable is a NUMBER and a SENTENCE ("what closed it"), not a fix.

Output: `164.6.2-MEASUREMENT.md`; a corrected (or evidenced-as-correct) `-6` remedy; and the
phase's deferrals booked into named phases.

⛔ **WHY THIS PLAN EDITS A FILE ROADMAP CRITERION 4 FENCES — READ THIS BEFORE THE TASKS.** Criterion
4 defers the `-6` defect vocabulary to Phase 164.8.3 PROBERAUTH and warns that this phase must not
re-acquire the naming. **D-09 (founder, 2026-09-13) folds ONE SENTENCE of it back in, and D-09
governs.** The reason is stated there and is not a loophole: 164.8.3 is COMPLETE and SHIPPED
(4/4, `5dead88a`, v0.77.42.0), so it cannot receive the work; a Phase 164.8.4 would be a third
outside item against a fence re-armed hours earlier; and a new phase is a full cycle for one
sentence. It lands HERE because THIS phase produces the measurement that settles it — criterion 2's
heal run is the first moment `trade_allowed` is readable at all.
⛔ **The fold is ONE SENTENCE WIDE.** No new defect kind, no new remedy entry, no new fixture, no
classification change, no `terminal_info()` recording change — all of that stays 164.8.3's. A reader
arriving from criterion 4 should find this paragraph before they find the edit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-CONTEXT.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-RESEARCH.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-02-SUMMARY.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-03-SUMMARY.md
</context>

<measured_baseline>
Measured during planning, 2026-09-13, at HEAD `03beb482`.

| Fact | Value | How measured |
|---|---|---|
| Prober self-test baseline | `SELF-TEST PASSED: 80/80 scenarios`, exit 0 | `node scripts/prod-prober/run.mjs --self-test` run at HEAD |
| The restart verb | `railway restart -s mt5-gateway` — *"Restart the latest deployment of a service (without rebuilding)"*; `railway redeploy -s … -y` is the heavier alternative | `railway --help` read |
| The terminal oracle | `./scripts/mt5-diag.sh` from the repo ROOT (the Railway link is directory-scoped), printing a `PROBE` line with `initialize`, `last_error`, `terminal_info`, and — when readable — `trade_allowed`, `tradeapi_disabled`, `connected`, `build`, `path` | script read at HEAD |
| The criterion's own oracle | `gh workflow run prod-prober.yml -f arm=mt5`; inputs are `mode` (default `probe`) and `arm` (default empty) | workflow read at HEAD |
| The scheduled baseline | hourly, `cron: "0 * * * *"` | workflow read at HEAD |
| ⛔ `/health` MT5 signal | NONE. Its body is `status`, `version`, `git_sha`, `worker_last_tick_at`, `worker_age_s`, `config_ok`, `config_degraded_secrets`. Useful here for ONE thing: confirming the analytics service came back after its own restart | handler read at HEAD |
| The `-6` remedy's contested sentence | the `mt5-not-authorized` entry in `scripts/prod-prober/arms/mt5.mjs` tells the operator to re-check the Expert-Advisors options because *a login is an ACCOUNT CHANGE and MT5 re-clears those options on every account change* — naming the 2026-08-13 investigation | read at HEAD |
| The counter-evidence | `156-LIVE-ACCEPTANCE.md` Row 6 records, from a LIVE run on the SAME day, that the founder unticked both auto-disable boxes and `trade_allowed` read true BEFORE the connect and still true AFTER it | read at HEAD |
| The self-test legs that touch that remedy | four required elements PRESENT (the VNC console, the save-password step, the Expert-Advisors options, the Journal); the error-table lookup instruction ABSENT with a splice calibration; the `-6` and `-10005` remedies non-convergent on the modal-dialog wording; no six-or-more-digit run; no markdown emphasis | `run.mjs`'s mt5 criterion-2 scenario read at HEAD |
| ⚠️ The arm's header | states it carries `scripts/mt5-diag.sh`'s READ-ONLY paragraphs **VERBATIM**, and that those paragraphs are the WHOLE reason the arm executes a COMMITTED constant | read at HEAD |
| ⛔ **THE COPY COUNT IS FOUR, NOT THREE** | the remedy string AND the quoted header block in `scripts/prod-prober/arms/mt5.mjs`; the header comment block AND the `NOTE` heredoc body in `scripts/mt5-diag.sh` | all four read at HEAD |
| ⛔ **The fourth copy is PRINTED PROSE, not a comment** | the `NOTE` heredoc the diag script emits on EVERY run carries the claim in its `trade_allowed=false` branch, telling the operator that without the two unticked boxes the next login turns algo trading straight back off. Its lines are NOT `#`-prefixed | `cat`-heredoc body read at HEAD |
| The diag script's editable vs pinned regions | PINNED: the shebang, `set -euo pipefail`, the three variable defaults, the `PY` heredoc body, the `base64` line and the `railway ssh` invocation — **31 lines**. EDITABLE PROSE: the leading header comment block, and the `NOTE` heredoc body | region extractor run over the file at HEAD |
| The verbatim pair, checked rather than assumed | extracting the arm's quoted block and the diag header block and normalising away the two comment prefixes yields IDENTICAL text today, so the pair claim is TRUE at HEAD and a gate can hold it there | extractor + `diff` run at HEAD, exit 0 |

⭐ **THE SEVERITY, READ CORRECTLY: the sentence is STALE, not FALSE.** It describes what happens
while the terminal's auto-disable option is ARMED, and the 2026-08-13 unticking is precisely what
disarmed it. The cost is one wasted operator step, not a data-integrity or safety problem. ⛔ Do not
plan it as a bug fix and do not let it inflate this phase.

⛔ **AND IT IS UNMEASURABLE UNTIL THE HEAL WORKS.** `terminal_info()` returns null while the
authorization failure is in force, so neither `trade_allowed` nor the state of the two auto-disable
boxes can be read today. That is exactly why the correction rides on this plan's run and does not
get a plan of its own.

⛔ **WHY THE READ-ONLY FENCE PINS BYTES INSTEAD OF INFERRING FROM A LEADING `#`.** An earlier form of
this plan's fence filtered the diff for lines whose first non-space character is a hash and required
the remainder to be empty. MEASURED, it is wrong in both directions at once. It is too NARROW: the
`NOTE` heredoc body is not hash-prefixed, so the fence structurally FORBADE correcting the copy an
operator reads mid-incident, leaving the plan fixing the two copies nobody reads and shipping the
one that matters — a point-fix enforced by a gate rather than chosen. And it is too WIDE: line 1 of
the script is a shebang, which is hash-prefixed and would have classified as a comment, and any
hash-prefixed line inside the `PY` heredoc is Python payload the script SENDS, not shell it runs.
⭐ The correct boundary is NON-EXECUTABLE versus EXECUTABLE, and it is decided by REGION, not by
prefix: two prose regions are editable, and every other byte is pinned against a pre-edit backup.

⚠️ **RESEARCH assumption A3, and it is the one thing that can stop this plan closing:** the whole
design assumes the `-6` is the SAVED ACCOUNT being refused, not a broker-side block on the account
itself. If the account is disabled at the broker, no code change helps, the heal will not clear it,
and the tie-breaker is the terminal's Journal tab — ⛔ which needs VNC, i.e. the founder, not an
agent. Plan for that outcome explicitly rather than looping on it.
</measured_baseline>

<the_three_outcomes>
## This plan has THREE legitimate endings and all three are planned. None of them is "keep trying".

1. **The heal works and the window is acceptable.** Criterion 2 closes on the observation; D-06's
   number is recorded; no keepalive is booked.
2. **The heal works and the window is unbounded without an analytics restart** — RESEARCH's expected
   answer. Criterion 2 STILL closes (it closes on the prober returning no defect without a human,
   not on a deadline), the number is recorded as unbounded-with-a-stated-bound, and a keepalive is
   booked as a NAMED follow-up phase.
3. **The heal does not clear the authorization failure** (assumption A3: a broker-side block).
   ⛔ Criterion 2 does NOT close and the phase does NOT close. Record the readings, state that A3 is
   the live suspect, and hand the founder the ONE thing that separates the two causes — the
   terminal's Journal tab, which needs VNC. ⛔ Do not loop, do not add retries, do not "improve" the
   heal against an unmeasured cause.

⛔ In outcome 3 the remedy-correction task does NOT run: it is gated on a reading that does not
exist, and editing operator instructions against an unmeasured state is the defect class this
milestone removes.
</the_three_outcomes>

<tasks>

<task type="auto">
  <name>Task 1: run the measurement — criterion 2, D-06's window, and question THREE's falsification</name>

  <read_first>
    - `scripts/mt5-diag.sh` — the whole header (⛔ READ-ONLY and why), the usage note (repo ROOT, the link is directory-scoped), and the fields the PROBE line carries.
    - `scripts/prod-prober/arms/mt5.mjs` — the header, and the note that `connected` and `trade_allowed` are PRINTED AND NEVER JUDGED.
    - `.github/workflows/prod-prober.yml` — the `workflow_dispatch` inputs and the step that `cat`s the runner log unconditionally, which is why a narrowed run's reading is published despite exit 2.
    - `.planning/milestones/v1.17-phases/156-connect-refactor-…/156-LIVE-ACCEPTANCE.md` — Row 6, the counter-evidence, in full.
    - This plan's `<measured_baseline>` and `<the_three_outcomes>`.
  </read_first>

  <files>.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md</files>

  <action>
    ⛔ **Re-measure before acting.** RESEARCH's live readings carry a 7-day currency and the founder
    has acted on the environment since. Run the diagnostic FIRST and record what it says now.

    **(a) BASELINE, before anything is restarted.** Record, with a UTC wall-clock stamp: the
    diagnostic's PROBE line; the analytics service's `/health` (to confirm it is up and to capture
    its deployed sha); and one narrowed prober arm dispatch, noting the run id. ⛔ Record only
    `initialize`, `last_error`, `terminal_info`, `connected` and `trade_allowed` — never `path`,
    never `build`; those carry a production filesystem path and a terminal build into a PUBLIC
    tracked file.
    ⛔ **HOW A RUN ID IS WRITTEN, and it is not cosmetic.** A GitHub Actions run id is an
    eleven-digit number and an MT5 account number is an eight-digit one, so a blanket ban on long
    digit runs would forbid the very evidence this record is required to carry. Write every run
    reference in ONE of exactly these forms so the disclosure scan can mask it and keep banning
    everything else: `run id NNNNNNNNNNN`, `run NNNNNNNNNNN`, or a full `actions/runs/NNNNNNNNNNN`
    URL. ⛔ A long digit run in ANY other position still reds the scan, and that is the account
    number's shape.

    ⚠️ The "before the heal" `trade_allowed` reading the phase brief asks for is taken HERE, and it
    is expected to be unreadable: `terminal_info` is null while the authorization failure is in
    force. Record the null as a null and label it explicitly as NOT a measurement of anything — a
    null before the heal and a false before the heal look identical and mean different things.

    **(b) THE EVENT UNDER TEST.** Restart the gateway service with the restart verb (not the
    redeploy — restart is the lighter one and it is criterion 2's verb). Record the UTC wall-clock
    of the restart. ⛔ No VNC session, before, during or after. That prohibition is the criterion.

    **(c) PASSIVE PHASE — does anything close it on its own?** Poll the diagnostic at ~2-minute
    granularity, recording each reading with its stamp, for a BOUNDED wall-clock window agreed in
    advance and stated in the artifact (RESEARCH suggests thirty minutes is enough to distinguish
    "something closed it" from "nothing will"). ⛔ Treat a single IPC-timeout reading as noise — the
    diagnostic shares the terminal's bridge with real work — and require a SECOND consecutive one
    before recording it as a state. If a reading shows the terminal authorized, STOP and record what
    plausibly closed it: the analytics service's own startup, a scheduled job, or something else.

    ⛔ **THEN TAKE THE CLOSING OBSERVATION — AND IT IS THE PROBER'S, NOT THE DIAGNOSTIC'S.** Criterion
    2 closes on *observing the prober's mt5 arm return no defect afterwards*. `scripts/mt5-diag.sh`
    is a DIFFERENT instrument on a DIFFERENT path, and the arm being credential-less BY DESIGN is
    precisely why its reading is the one that closes the criterion — a diagnostic-only record leaves
    the phase's load-bearing criterion supported by an instrument it does not name. So dispatch a
    narrowed prober run AFTER the first authorized diagnostic reading and record THREE things: the
    POST-RESTART run id, the arm's defect-table verdict read from the published runner log, and the
    defect COUNT for the mt5 arm (zero is the closing reading). ⛔ Read the published runner log,
    never the exit code, which is 2 by design for a narrowed run.
    ⚠️ If the arm and the diagnostic DISAGREE, that disagreement is the finding — record both
    readings and their stamps and do not pick a winner; two instruments on two paths disagreeing is
    information, and resolving it by preferring the convenient one is how a phase closes on a
    reading it did not take.

    **(d) ACTIVE PHASE — only if the passive window expired still red.** Restart the ANALYTICS
    service and record the UTC stamp. This is the mechanism the design actually relies on, and
    measuring its latency is the second half of D-06. Poll as before, and when a reading shows the
    terminal authorized take the SAME closing prober observation (c) specifies — post-restart run
    id, defect-table verdict from the published log, defect count. ⭐ Whatever the result, the
    honest entry for "what closed it" is now "an operator restart of the analytics service", which
    means the window is UNBOUNDED without one — that is outcome 2 and it is a finding, not a
    failure.
    ⚠️ If the terminal is STILL not authorized after a confirmed successful analytics restart with
    the three variables present, that is outcome 3. Stop. Do not restart again, do not add a retry.
    Record the readings and hand over the Journal-tab tie-breaker. ⛔ **Take ONE final narrowed
    prober dispatch before stopping, even here** — the record must carry the arm's own verdict on
    the end state whatever that state is, and a defect the arm names is evidence exactly as much as
    a defect it does not.

    **(e) QUESTION THREE, riding along for free.** Immediately after the FIRST reading that shows the
    terminal authorized, run the diagnostic and record `trade_allowed` — call this reading R1. Then
    wait for a real per-account login to occur — a scheduled derive or balance job, an allocator
    read, or a key validation — and run the diagnostic again: call this R2. Record WHICH event
    produced R2 and how it was confirmed, because the answer's strength depends on it: a real
    per-account login is the exact event the contested sentence is about, while a second boot heal
    is the credentialed-initialize form and is only a proxy for it. ⛔ Do not trigger a login by
    logging in yourself, and ⛔ do not make either instrument log in.
    Record the verdict in one sentence of three possible forms: R1 and R2 both report the flag true
    => the contested sentence is STALE and the auto-disable boxes are still unticked; R2 reports it
    false where R1 reported true => the sentence is CURRENT and the hazard is live; either reading
    unavailable => UNSETTLED, and say why.

    **(f) The artifact.** Write `164.6.2-MEASUREMENT.md` with, at minimum, these named sections so a
    machine can check the record is complete — the completeness gate greps for each heading by name,
    so use these exact labels:

      - `Restart stamp`
      - `First authorized reading`
      - `Delta`
      - `What closed it`
      - `Prober run id` — the POST-RESTART narrowed dispatch, written in one of (a)'s three
        permitted run-reference forms. ⛔ The BASELINE run id from (a) does not satisfy this
        section: a baseline taken before the event under test cannot be the observation that closes
        a criterion about what happened after it. State both, and label which is which.
      - `Prober verdict` — the arm's defect-table verdict READ FROM THE PUBLISHED RUNNER LOG for
        that POST-RESTART run, and on its own line, machine-readably, `defect count: N` for the mt5
        arm. ⛔ Never the narrowed run's exit code, which is 2 by design. ⭐ `N` is whatever was
        measured: zero is the closing reading for criterion 2, and a non-zero `N` on outcome 3 is
        just as required — the gate checks that a count was RECORDED, never that it was favourable.
      - `Question THREE verdict` — carrying R1, R2, the event that produced R2, and on its own line,
        machine-readably, `verdict: STALE` or `verdict: CURRENT` or `verdict: UNSETTLED`. ⛔ That
        line is what licenses or forbids task 2's edit, and task 2's gate reads it — prose alone
        leaves the licence unenforceable.
      - `Outcome`
      - `Poll log`

    Carry R1 and R2 inside the question-THREE section and the raw stamped readings inside the poll
    log. ⛔ No account number, no broker server string, no `path`, no `build`, no absolute home path.
    ⚠️ Also record, as an observation rather than a task, whether the env account and the only
    connected MT5 key are the same account — the strict-equality account bracket fails loud on a
    mismatch, so this is a note for the next reader, not something to resolve here.
  </action>

  <verify>
    <automated>F=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md; test -f "$F" || { echo "MISSING ARTIFACT"; exit 1; }; for k in "Restart stamp" "First authorized reading" "Delta" "What closed it" "Prober run id" "Prober verdict" "Question THREE verdict" "Outcome" "Poll log"; do grep -qiF "$k" "$F" || { echo "MISSING SECTION: $k"; exit 1; }; done; echo "MEASUREMENT-RECORD-COMPLETE OK (9 sections)"</automated>
    <fails_when>non-zero exit, or the printed line is not `MEASUREMENT-RECORD-COMPLETE OK (9 sections)`. A named missing section means D-06's record is partial — and a window recorded without "what closed it" is precisely the implicit number D-06 forbids.
    ⛔ **`Prober run id` and `Prober verdict` are REQUIRED ELEMENTS, and the earlier seven-section form of this gate did not carry them.** ROADMAP criterion 2 closes on *observing the PROBER'S mt5 arm return no defect*, and `First authorized reading` is the `mt5-diag.sh` PROBE line — a different instrument on a different path. Without these two sections a complete-looking banner is reachable with the phase's load-bearing criterion supported only by the diagnostic script.</fails_when>

    <automated>F=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md; R=$(grep -oE '(actions/runs/|[Rr]un id:?[[:space:]]+|[Rr]un[[:space:]]+)[0-9]{6,}' "$F" | grep -c .); test "$R" -ge 2 || { echo "RUN-REFS-INSUFFICIENT: found $R, need the baseline dispatch AND the post-restart one"; exit 1; }; grep -qiE 'defect count:[[:space:]]*[0-9]+' "$F" || { echo "DEFECT-COUNT-NOT-RECORDED: the Prober verdict section carries no machine-readable count line"; exit 1; }; grep -qiE 'verdict:[[:space:]]*(STALE|CURRENT|UNSETTLED)' "$F" || { echo "QUESTION-THREE-VERDICT-NOT-MACHINE-READABLE: task 2's licence gate reads this line"; exit 1; }; grep -qiE 'exit code' "$F" && echo "WARN: the record mentions an exit code — the narrowed run exits 2 BY DESIGN and is never the verdict" >&2; echo "PROBER-OBSERVATION-RECORDED OK ($R run refs)"</automated>
    <fails_when>non-zero exit, or the printed line is not a `PROBER-OBSERVATION-RECORDED OK` banner. Fewer than two run references means only one dispatch is on record and the post-restart observation — the one criterion 2 actually closes on — was never taken or never written down. A missing `defect count: N` line means the verdict is prose a machine cannot check. ⭐ The count is NOT gated on being zero: a non-zero count on outcome 3 is a required reading, and a gate that demanded a favourable number would pressure the executor to keep probing until it got one.</fails_when>

    <automated>F=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md; M=$(sed -E 's#(actions/runs/|[Rr]un id:?[[:space:]]+|[Rr]un[[:space:]]+)[0-9]{6,}#RUNREF#g' "$F" | grep -nE '[0-9]{6,}|/Us[e]rs/|/home/[a-z]|"path"|"build"' || true); if test -n "$M"; then printf '%s\n' "$M"; echo "DISCLOSURE-SHAPE FOUND"; exit 1; fi; echo "NO-DISCLOSURE-SHAPES OK"</automated>
    <fails_when>non-zero exit, or the printed line is not `NO-DISCLOSURE-SHAPES OK`. A six-or-more-digit run is the shape of an account number; the two quoted field names are the diagnostic's production filesystem path and terminal build. This file is world-readable on push.
    ⛔ **The masking step is what makes this gate SATISFIABLE, and it narrows nothing that matters.** A GitHub Actions run id is eleven digits and an MT5 account number is eight, so a blanket digit-run ban and the required run-id sections above are mutually unsatisfiable — the earlier form of this gate would have reded on the very evidence the record is required to carry. Masking recognises a long digit run ONLY when it is immediately preceded by a run-reference marker; a long digit run anywhere else still reds, which is the account number's position. ⛔ If this gate reds, remove the disclosure from the artifact — never widen the mask.</fails_when>

    <automated>S=$(git status --porcelain -- scripts/ .github/ analytics-service/) || exit 1; n=$(printf '%s' "$S" | grep -c .); test "$n" -eq 0 && echo "INSTRUMENTS-READ-ONLY OK"</automated>
    <fails_when>non-zero exit, or the printed line is not `INSTRUMENTS-READ-ONLY OK`. This task READS; any modification under those roots means an instrument was changed mid-measurement, which invalidates every reading taken after it.</fails_when>

    <automated>npm run check:planning-hygiene</automated>
    <fails_when>non-zero exit. Fix the artifact, ⛔ never the gate.</fails_when>
  </verify>

  <acceptance_criteria>
    - The gateway was restarted with the restart verb, with NO VNC session before, during or after, and the UTC stamp is recorded.
    - Every poll reading is recorded with its stamp; no single IPC-timeout reading was treated as a state.
    - The narrowed prober arm was dispatched and its verdict READ FROM THE PUBLISHED LOG, not from an exit code.
    - ⛔ A POST-RESTART prober dispatch was taken — distinct from the baseline one — and the record carries its run id, its defect-table verdict from the published log, and a machine-readable `defect count: N`. This is the observation ROADMAP criterion 2 closes on; the diagnostic's PROBE line is a different instrument on a different path and does not substitute for it.
    - Every run reference is written in one of the three permitted forms, so the disclosure scan masks it and keeps banning long digit runs everywhere else.
    - The record names the delta AND what closed it, and classifies the run as outcome 1, 2 or 3.
    - R1 and R2 are recorded with the event that produced R2 named, and the question-THREE verdict is one of the three stated forms.
    - No credential, account number, broker server string, `path`, `build` or absolute home path appears anywhere.
    - Nothing under `scripts/`, `.github/` or `analytics-service/` was modified.
  </acceptance_criteria>

  <done>
    `164.6.2-MEASUREMENT.md` exists, all five verify banners pass, and the artifact states the
    outcome number, the question-THREE verdict and the POST-RESTART prober verdict with its run id
    and `defect count: N` in unambiguous sentences.
  </done>
</task>

<task type="auto">
  <name>Task 2: correct the stale account-change sentence — GATED on task 1's reading, with "no change needed" as a planned outcome</name>

  <read_first>
    - `.planning/phases/164.6.2-…/164.6.2-MEASUREMENT.md` — task 1's question-THREE verdict. ⛔ Read it FIRST; this task's shape is decided by it.
    - `scripts/prod-prober/arms/mt5.mjs` — the `mt5-not-authorized` entry of the remedy table in full, and the header block that states it carries the diagnostic script's paragraphs VERBATIM.
    - `scripts/prod-prober/run.mjs` — the mt5 criterion-2 self-test scenario in full: the four-required-elements leg, the lookup-instruction negative leg and its splice calibration, the `-6`-versus-IPC non-convergence leg, the digit-run leg and its calibration, the emphasis leg and its calibration. ⛔ Read these BEFORE writing a single replacement word.
    - `scripts/mt5-diag.sh` — READ THE WHOLE FILE, not just the header. Two regions carry the claim: the header comment block, and the `NOTE` heredoc body the script PRINTS on every run. ⛔ The heredoc body is the copy an operator reads at the incident and it is NOT comment-prefixed.
    - `.planning/milestones/v1.17-phases/156-connect-refactor-…/156-LIVE-ACCEPTANCE.md` Row 6.
    - `164.6.2-MEASUREMENT.md`'s machine-readable `verdict:` line — this task's licence, and the `STALE-LICENCE` leg reads the same line.
  </read_first>

  <files>
    scripts/prod-prober/arms/mt5.mjs,
    scripts/mt5-diag.sh,
    scripts/prod-prober/run.mjs
  </files>
  ⚠️ `scripts/prod-prober/run.mjs` is CONDITIONAL — touched ONLY when a shipped self-test assertion
  has to be fixed to grep what actually matters (step (a)), and that edit must be named in the
  commit message. It is declared here and in the frontmatter because the wave guard reads
  frontmatter while `BLAST-RADIUS` reads the diff.

  <behavior>
    - If task 1's verdict is STALE: the operator-facing text no longer asserts that a login re-clears the Expert-Advisors options as a present-tense fact; it states what was MEASURED, with the date, and it still tells the operator to re-check those options.
    - ⛔ ALL FOUR copies carry the correction, not three: the remedy string and the quoted header block in the arm, and BOTH the header comment block and the printed `NOTE` heredoc body in the diag script. Each of the four regions is checked INDIVIDUALLY, because a file-wide count cannot tell four corrected regions from one region corrected four times.
    - The arm's `VERBATIM` claim is MACHINE-CHECKED: the arm's quoted block and the diag header block are extracted, normalised for their two comment prefixes, and required to be identical.
    - If task 1's verdict is CURRENT: nothing is edited; the SUMMARY records the reading that confirms the sentence, with its stamps; and BOTH files are proven byte-identical to their pre-edit backups.
    - If task 1's verdict is UNSETTLED (outcome 3, or either reading unavailable): nothing is edited; both files are proven byte-identical to their backups; the item is routed in task 3 as a named deferral with the reason.
    - ⛔ The licence BITES both ways: a correction present in either instrument reds unless the measurement records STALE, and a non-STALE verdict reds unless both files are unchanged. No arrangement of files and readings passes every gate without the reading that licenses it.
    - In every case `node scripts/prod-prober/run.mjs --self-test` passes at its baseline count with exit 0.
    - In every case the four required elements are still present in the `-6` defect ROW, the lookup-table instruction is still absent, and the `-6` and IPC remedies still do not converge.
    - `scripts/mt5-diag.sh` is byte-unchanged OUTSIDE its two non-executable prose regions — the leading header comment block and the `NOTE` heredoc body. The shebang, the shell options, the variable defaults, the `PY` heredoc body and the `railway ssh` invocation are byte-pinned against a pre-edit backup.
    - That fence is SHOWN able to fail: three neuter levers (a shebang swap, a hash-prefixed line injected into the `PY` heredoc body, a variable-default change) are each proven APPLIED and observed RED, and a GREEN control proves it still permits a prose-only correction.
  </behavior>

  <action>
    ⛔ **GATE FIRST, AND THE GATE IS ENFORCED, NOT ADVISED.** Read task 1's question-THREE verdict.
    If it is CURRENT or UNSETTLED, this task edits NOTHING: record the evidence in the SUMMARY and
    stop. ⭐ "No change needed, and here is the reading that says so" is a legitimate completion of
    this task, not a failure — the phase's value here is the MEASUREMENT, and the edit was always
    conditional on it.

    ⛔ **The earlier form of this instruction was PROSE ONLY and could not fail.** Every one of this
    task's automated legs passed identically whether the edit was licensed by a STALE reading or
    written against no reading at all. This repo has a dated, measured record of exactly that shape —
    *a step that cannot fail did not fail; it was simply skipped.* The licence is therefore a GATE
    below (`STALE-LICENCE`), and it is a biconditional: a correction present in either instrument
    IMPLIES the measurement records STALE, and a non-STALE verdict IMPLIES both files are byte-equal
    to their pre-edit backups.

    **(0) BEFORE TOUCHING ANYTHING, take the pre-edit byte backups the gates compare against.**
    Create `BKD="${MT5_BACKUP_DIR:-${TMPDIR:-/tmp}/164.6.2-preedit}"` (⛔ never inside the repo),
    `cp scripts/mt5-diag.sh "$BKD/mt5-diag.sh"` and `cp scripts/prod-prober/arms/mt5.mjs
    "$BKD/mt5.mjs"`, record both `shasum` values in the SUMMARY, and write the CURRENT commit —
    `git rev-parse HEAD > "$BKD/BASE_SHA"` — into the same directory.
    ⛔ **The path is DERIVED, not exported.** Shell state does not survive between tool calls, so a
    leg reading an env var set in this step would find it unset; every leg recomputes `BKD` by the
    same expression and fails loud on a missing backup rather than passing vacuously.
    ⛔ **`BASE_SHA` is recorded BEFORE the first edit and is what the pristine check compares
    against — never `HEAD`.** `HEAD` moves when the correction is committed, and a pristine check
    reading a moved `HEAD` reds with the message *"the backup was taken after an edit"*, whose
    naive remedy is to re-take the backup: exactly the laundering the check exists to prevent. ⛔ **Never `git checkout --` anywhere in this task.** It restores to HEAD and silently
    destroys uncommitted work, which in a neuter/restore harness is the whole of the work just done.
    ⭐ The fence leg additionally proves the pre-edit backup is byte-identical to the committed file,
    so a backup re-taken AFTER an edit reds instead of laundering it.

    Proceed to the edit only when the verdict is STALE.

    **(a) Check the replacement against the shipped assertions BEFORE writing it.** The criterion-2
    self-test scenario asserts, on the `-6` DEFECT ROW: that four named elements are present — the
    VNC console, the save-password step, the Expert-Advisors options and the Journal; that no
    error-table lookup instruction appears; that the `-6` text carries none of the IPC remedy's
    distinguishing modal-dialog wording; that no six-or-more-digit run appears; and that no markdown
    emphasis appears. ⭐ **The Expert-Advisors element is REQUIRED and stays.** The correction is to
    the CAUSAL CLAIM wrapped around it, never to the step itself — re-checking those options after a
    login is still the right instruction, because the whole question is whether something re-cleared
    them. Draft the replacement, then confirm by hand that it satisfies all five predicates, and only
    then write it. ⛔ If a predicate would genuinely bite on a correct replacement, fix the
    ASSERTION so it greps what actually matters — ⛔ never weaken what it checks, and never widen a
    negative predicate to admit the new text.

    **(b) The corrected passage — ONE form, written once, used in all four places.** Replace the
    present-tense causal claim with what is MEASURED: state that a login is an account change and
    that MT5 re-clears those options WHEN the terminal's auto-disable setting is armed; that the
    setting was disarmed on the dated investigation and a live run the same day observed the flag
    surviving a login; and that this phase measured it again on its own date with the result
    recorded. Keep the instruction to re-check. ⛔ Keep the sentence short: this string lands in a
    PUBLIC Actions log and a PUBLIC auto-filed issue, and it is read by an operator mid-incident.

    ⛔ **THE CORRECTION CARRIES A MACHINE-CHECKABLE MARKER, and the gates below find it by that
    marker.** The clause naming this phase's own measurement is written in the exact form
    `re-measured YYYY-MM-DD` with the execution date in ISO form, and it appears in EVERY one of the
    four corrected regions. ⭐ This is what lets a gate distinguish four regions each corrected once
    from one region corrected four times, and it is what the licence gate counts. ⚠️ An ISO date has
    no six-or-more-digit run, so it is safe against the shipped self-test's digit-run leg — the
    existing remedy already names a dated investigation in the same shape.

    <!-- planner-discipline-allow: re-measured -->
    <!-- planner-region-allow: [Rr]e-measured 20[0-9][0-9] -->

    **(c) FOUR COPIES, NOT THREE — correct all of them, in one commit.** MEASURED at HEAD, the
    contested claim ships in four places:

      - **R1 — the remedy string.** The `mt5-not-authorized` entry of the arm's remedy table.
      - **R2 — the arm's quoted header block.** The paragraphs the arm's header says it carries
        VERBATIM from the diagnostic script.
      - **R3 — the diag script's header comment block.** R2's source.
      - **R4 — the diag script's printed `NOTE` heredoc body.** ⭐ **THIS IS THE ONE THAT MATTERS
        MOST AND IT WAS THE ONE THE EARLIER FENCE FORBADE.** It is what the script PRINTS ON EVERY
        RUN, in the `trade_allowed=false` branch, telling the operator that without the two unticked
        boxes the next login turns algo trading straight back off and they get one working sync. An
        operator reads R4 AT the incident; R1 lands in a log they may never open; R2 and R3 are
        comments nobody reads mid-incident. ⛔ Correcting R1-R3 and shipping R4 is the point-fix the
        fix-campaign rule forbids — *close the whole class across the surface* — and doing it because
        a gate structurally forbade the fourth is worse than doing it by oversight.

    ⛔ **R2 and R3 are a PAIR by the arm's own claim, and the claim is now ENFORCED.** The arm's
    header states it carries those paragraphs VERBATIM; correcting one without the other makes the
    arm's own claim FALSE, replacing the old defect with a new one. The earlier plan asked the
    executor to "keep them byte-identical" with no mechanism, which left the claim exactly as
    unenforced after this phase as before it — a control weaker than the sentence beside it. The
    `VERBATIM-PAIR` leg below extracts both blocks, normalises away the two comment prefixes, and
    diffs them. ⭐ It passes at HEAD today, so it holds a true property true rather than asserting a
    new one.

    ⛔ **In `scripts/mt5-diag.sh` the edit is NON-EXECUTABLE-ONLY, which is WIDER than comment-only
    and still pins BEHAVIOUR.** R3 is comments and R4 is printed prose; both are editable. Everything
    else — the shebang, `set -euo pipefail`, the three variable defaults, the `PY` heredoc body, the
    `base64` line and the `railway ssh` invocation — is byte-pinned against the pre-edit backup by
    the `DIAG-NONEXEC-ONLY` leg. That is what 164.8.3's criterion 5 actually fences: the script does
    not gain a mutating call, and it is proven by bytes rather than inferred from a leading hash.
    ⚠️ Keep the paragraphs' other content intact: the terminal-config-file warning and the
    live-oracle sentence are unrelated and still correct, and so is R4's `tradeapi_disabled` line.

    **(d) CALIBRATE THE READ-ONLY FENCE — neuter, observe RED, restore. Run this only on the STALE
    branch, where the fence is actually doing work.** ⛔ Per this repo's discipline a gate that
    CANNOT FAIL is worse than none, and ⛔ **a neuter that does not APPLY reads as GREEN** — so every
    lever asserts its own application before any result is believed. Plans 01 and 02 calibrate their
    gates this way; this fence did not, and it is the fence licensing an edit to a production
    instrument.

    **Take a SECOND, POST-EDIT backup first** (`BK_POST`), because the pre-edit backup is now stale
    for restore purposes: restoring from it would silently revert the correction this task just
    wrote. ⛔ Byte backups go stale the moment you edit — re-take after every edit, and verify each
    restore by GREPPING FOR THE NEWEST FIX (the `re-measured` marker), not by a hash alone.

    For each lever, in this exact order: `cp` the post-edit file aside → apply the neuter → **prove
    the neuter APPLIED with a `grep -c` on the mutated construct** → run the `DIAG-NONEXEC-ONLY` leg
    and capture its output VERBATIM → restore with `cp` from `BK_POST` → prove the restore with
    `cmp` AND by grepping the correction marker back.

      - **LEVER 1 — the shebang.** Rewrite line 1 to a different interpreter path. Expected RED.
        ⭐ This is the hole that motivated the rebuild: the shebang begins with a hash, so the
        earlier prefix-inference fence classified a shebang rewrite as a COMMENT and passed it.
      - **LEVER 2 — a hash-prefixed line inside the `PY` heredoc body.** Insert one. Expected RED.
        ⭐ Second hole: inside that heredoc a hash line is Python payload the script SENDS to the
        gateway, not shell it runs, so prefix inference passed it too.
      - **LEVER 3 — an executable line.** Change one variable default. Expected RED, proving the
        fence still catches the ordinary case.
      - **GREEN CONTROL — a prose-only edit.** Change one word in the header comment block and one
        word in the `NOTE` body. Expected GREEN. ⛔ Do not skip this: a fence that reds on everything
        would pass all three levers and forbid the correction the task exists to make, and only this
        control separates a working fence from a stuck one.

    Record all four transcripts, dated, in the SUMMARY beside the durable gate. ⛔ A calibration
    whose transcript is not recorded is a claim, not a measurement.

    **(e) ⛔ Do not touch history.** The dated record and the earlier `.planning/` artifacts stay
    exactly as written. They are a correct record of what the code said at the time, and rewriting
    them would erase the evidence that settled this.
  </action>

  <verify>
    <automated>ST=$(node scripts/prod-prober/run.mjs --self-test 2>&1); printf '%s\n' "$ST" | tail -3 >&2; printf '%s\n' "$ST" | grep -qE 'SELF-TEST PASSED: [0-9]+/[0-9]+' && ! printf '%s\n' "$ST" | grep -qi 'FAILED'</automated>
    <fails_when>non-zero exit; or no `SELF-TEST PASSED: N/N` banner; or the word FAILED appears anywhere. A failure naming the four-required-elements leg means the correction dropped a required element — restore it. One naming the lookup-instruction leg means the replacement introduced an error-table instruction. One naming the non-convergence leg means the `-6` text drifted toward the IPC remedy's wording.</fails_when>

    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
    <fails_when>non-zero exit, or the summary line does not report one passed test file. ⛔ Scope stays on this one file — a full suite run OOM-killed this box twice on 2026-09-13.</fails_when>

    <automated>BKD="${MT5_BACKUP_DIR:-${TMPDIR:-/tmp}/164.6.2-preedit}"; for b in $(printf '%s\n' "$BKD/mt5.mjs" "$BKD/mt5-diag.sh"); do test -f "$b" || { echo "NO-BACKUP: step (0) must cp the pre-edit file to $b"; exit 1; }; done; F=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md; A=scripts/prod-prober/arms/mt5.mjs; D=scripts/mt5-diag.sh; V=$(grep -ioE 'verdict:[[:space:]]*(STALE|CURRENT|UNSETTLED)' "$F" | grep -oiE 'STALE|CURRENT|UNSETTLED' | head -1 | tr '[:lower:]' '[:upper:]'); test -n "$V" || { echo "NO-VERDICT: task 1 recorded no machine-readable question-THREE verdict"; exit 1; }; N=$(cat "$A" "$D" | grep -oE '[Rr]e-measured 20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]' | grep -c .); if test "$V" = STALE; then test "$N" -ge 4 || { echo "LICENCE-UNUSED: verdict is STALE but only $N of the 4 regions carry the correction marker"; exit 1; }; else test "$N" -eq 0 || { echo "EDIT-WITHOUT-LICENCE: verdict is $V but $N correction markers are present"; exit 1; }; cmp -s "$BKD/mt5.mjs" "$A" || { echo "EDIT-WITHOUT-LICENCE: the arm differs from its pre-edit backup on a $V verdict"; exit 1; }; cmp -s "$BKD/mt5-diag.sh" "$D" || { echo "EDIT-WITHOUT-LICENCE: the diag script differs from its pre-edit backup on a $V verdict"; exit 1; }; fi; echo "STALE-LICENCE OK (verdict $V, $N markers)"</automated>
    <fails_when>non-zero exit, or the printed line is not a `STALE-LICENCE OK` banner. ⛔ **This is the leg that makes the measurement gate BITE.** Before it, the gate was prose only — every automated leg in this task passed identically whether the edit was licensed by a STALE reading or written against no reading at all, which is the shape this repo has a dated, measured record of: *a step that cannot fail did not fail; it was simply skipped.* The implication runs both ways: STALE with fewer than four correction markers means the licence was granted and not used across the whole surface; a CURRENT or UNSETTLED verdict with any marker present, or with either file differing from its pre-edit backup, means an operator instruction was edited against an unmeasured state — threat T-164.6.2-24. ⛔ Never clear this by editing the verdict line; the verdict is task 1's measurement, not this task's input to choose.</fails_when>

    <automated>F=.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-MEASUREMENT.md; A=scripts/prod-prober/arms/mt5.mjs; D=scripts/mt5-diag.sh; V=$(grep -ioE 'verdict:[[:space:]]*(STALE|CURRENT|UNSETTLED)' "$F" | grep -oiE 'STALE|CURRENT|UNSETTLED' | head -1 | tr '[:lower:]' '[:upper:]'); if test "$V" != STALE; then echo "FOUR-COPIES-CORRECTED SKIPPED (verdict $V — nothing is edited)"; exit 0; fi; MK='[Rr]e-measured 20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]'; R1=$(awk '/^  "mt5-not-authorized":/{f=1; print; next} f == 1 && /^  "mt5-/{exit} f{print}' "$A" | grep -cE "$MK"); R2=$(awk '/READ-ONLY, AND DELIBERATELY SO/{f=1;next} /Those two paragraphs are the WHOLE reason/{f=0} f' "$A" | grep -cE "$MK"); R3=$(awk '/READ-ONLY, and deliberately so/{f=1} /^# Usage:/{f=0} f' "$D" | grep -cE "$MK"); R4=$(awk '$0 ~ /^cat / && index($0,"NOTE"){n=1;next} n && $0=="NOTE"{n=0} n' "$D" | grep -cE "$MK"); echo "regions R1=$R1 R2=$R2 R3=$R3 R4=$R4"; for v in "$R1" "$R2" "$R3" "$R4"; do test "$v" -ge 1 || { echo "UNCORRECTED-REGION: one of the four copies still ships the stale form"; exit 1; }; done; echo "FOUR-COPIES-CORRECTED OK"</automated>
    <fails_when>non-zero exit, or the printed line is not `FOUR-COPIES-CORRECTED OK` (or the documented SKIPPED banner on a non-STALE verdict). ⛔ The four regions are counted SEPARATELY on purpose: a file-wide tally cannot distinguish four regions corrected once each from one region corrected four times, and the copy most likely to be missed is R4 — the `NOTE` heredoc body the script PRINTS ON EVERY RUN, which an operator reads at the incident while R1 sits in a log they may never open. ⛔ A zero in any region means a point-fix; close the whole class.
    ⚠️ If an extraction anchor moves — a heading reworded, the remedy key renamed — the region count reads 0 and this gate reds. Fix the ANCHOR, never the requirement.</fails_when>

    <automated>A=scripts/prod-prober/arms/mt5.mjs; D=scripts/mt5-diag.sh; T=$(mktemp -d); awk '/READ-ONLY, AND DELIBERATELY SO/{f=1;next} /Those two paragraphs are the WHOLE reason/{f=0} f' "$A" | sed 's|^[[:space:]]*\*||; s/^[[:space:]]*//; s/[[:space:]]*$//' | grep -v '^=\{3,\}$' | grep -v '^$' > "$T/arm.txt"; awk '/READ-ONLY, and deliberately so/{f=1} /^# Usage:/{f=0} f' "$D" | sed 's|^#||; s/^[[:space:]]*//; s/[[:space:]]*$//' | grep -v '^$' > "$T/diag.txt"; L=$(grep -c . "$T/arm.txt"); test "$L" -ge 8 || { echo "PAIR-EXTRACTION-COLLAPSED: $L lines, the extractor lost its anchors"; exit 1; }; diff "$T/arm.txt" "$T/diag.txt" || { echo "VERBATIM-CLAIM-FALSE: the arm header says it carries those paragraphs VERBATIM and it does not"; exit 1; }; echo "VERBATIM-PAIR OK ($L lines)"</automated>
    <fails_when>non-zero exit, or the printed line is not a `VERBATIM-PAIR OK` banner. ⭐ **MEASURED at HEAD: this leg passes today (9 lines, exit 0), so it holds a TRUE property true rather than asserting a new one.** The arm's header declares it carries the diagnostic script's paragraphs VERBATIM, and that declaration was the whole argument for this task touching a second file — yet the earlier plan asked only that the executor "keep them byte-identical", with no mechanism, leaving the claim exactly as unenforced after this phase as before it. A control weaker than the sentence beside it is not a control. ⛔ If this reds, correct the block that was missed — never delete the VERBATIM sentence to make the claim vacuous.</fails_when>

    <automated>D=scripts/mt5-diag.sh; BKD="${MT5_BACKUP_DIR:-${TMPDIR:-/tmp}/164.6.2-preedit}"; BK="$BKD/mt5-diag.sh"; test -f "$BK" || { echo "NO-BACKUP: step (0) must cp the pre-edit file to $BK"; exit 1; }; BASE=$(cat "$BKD/BASE_SHA" 2>/dev/null || true); test -n "$BASE" || { echo "NO-BASE-SHA: step (0) must record git rev-parse HEAD into $BKD/BASE_SHA BEFORE any edit"; exit 1; }; T=$(mktemp -d); git show "$BASE:$D" > "$T/base.sh" || { echo "BASE-SHA-UNRESOLVABLE: $BASE does not name a commit carrying $D"; exit 1; }; cmp -s "$BK" "$T/base.sh" || { echo "BACKUP-NOT-PRISTINE: the backup differs from $D as committed at the recorded pre-edit base $BASE, so the backup was taken after an edit"; exit 1; }; PRE=$(awk 'NR==1{next} $0=="set -euo pipefail"{exit} {print}' "$D" | grep -vcE '^[[:space:]]*#|^[[:space:]]*$'); test "$PRE" -eq 0 || { echo "PRE-SET-REGION-EXECUTABLE: $PRE non-comment line(s) above set -euo pipefail, which the pin extractor skips"; exit 1; }; X() { awk 'NR==1{print "X|"$0;next} !s && $0=="set -euo pipefail"{s=1;print "X|"$0;next} !s{next} $0 ~ /^cat / && index($0,"NOTE"){n=1;print "X|"$0;next} n && $0=="NOTE"{n=0;print "X|"$0;next} n{next} {print "X|"$0}' "$1"; }; X "$BK" > "$T/base.pins"; X "$D" > "$T/now.pins"; P=$(grep -c . "$T/now.pins"); test "$P" -ge 30 || { echo "PIN-REGION-COLLAPSED: only $P pinned lines, the region extractor lost its anchors"; exit 1; }; for pin in 'X|#!/usr/bin/env bash' 'X|set -euo pipefail' 'X|railway ssh --service' 'X|print("PROBE "'; do grep -qF "$pin" "$T/now.pins" || { echo "PIN MISSING: $pin"; exit 1; }; done; cmp "$T/base.pins" "$T/now.pins" || { echo "EXECUTABLE-REGION-CHANGED"; exit 1; }; echo "DIAG-NONEXEC-ONLY OK ($P pinned lines)"</automated>
    <fails_when>non-zero exit, or the printed line is not a `DIAG-NONEXEC-ONLY OK` banner. ⭐ **MEASURED at HEAD: 31 pinned lines, exit 0.** This leg BYTE-PINS everything outside the two prose regions — the shebang, `set -euo pipefail`, the three variable defaults, the whole `PY` heredoc body, the `base64` line and the `railway ssh` invocation — against a pre-edit `cp` backup, and separately proves that backup is byte-identical to the committed file so a backup re-taken after an edit reds instead of laundering it.
    ⛔ **It replaces a comment-prefix filter that was wrong in BOTH directions.** Too narrow: the `NOTE` heredoc body is not hash-prefixed, so the old fence structurally FORBADE correcting the copy an operator reads mid-incident. Too wide: line 1 is a shebang, which IS hash-prefixed, so a shebang rewrite classified as a comment and passed — as would any hash-prefixed line inside the `PY` heredoc, where a hash line is Python payload the script SENDS, not shell it runs. Both holes are neuter levers in the calibration.
    ⛔ `PIN-REGION-COLLAPSED` or a missing pin means the extractor lost an anchor and the comparison went vacuous — fix the anchor, never lower the floor. ⛔ Restore ONLY from a `cp` backup verified with `cmp`; `git checkout --` restores to HEAD and silently destroys the correction this task just wrote.</fails_when>

    <automated>D=$(git diff --name-only -- .github/ analytics-service/ scripts/prod-prober/fixtures/ scripts/prod-prober/run.mjs) || exit 1; n=$(printf '%s' "$D" | grep -c .); test "$n" -le 1 && echo "BLAST-RADIUS OK ($n)"</automated>
    <fails_when>non-zero exit, or the printed line is not a `BLAST-RADIUS OK` banner. More than one path means this task reached past the remedy and its verbatim pair. ⚠️ The one permitted path is the prober runner, and ONLY when an assertion had to be fixed to grep what actually matters — that edit must be called out by name in the commit message.</fails_when>
  </verify>

  <acceptance_criteria>
    - The task's shape was GATED on task 1's machine-readable verdict line, the gate decision is stated in the SUMMARY, and the `STALE-LICENCE` leg ENFORCES it in both directions rather than leaving it to prose.
    - Pre-edit `cp` backups of BOTH files were taken before anything was touched, their `shasum` values are in the SUMMARY, and the fence proves the diag backup is byte-identical to the committed file.
    - If edited: the replacement was checked against all five shipped self-test predicates BEFORE being written; the Expert-Advisors element and the re-check instruction survive; and ⛔ ALL FOUR regions — the remedy string, the arm's quoted header block, the diag header comment block and the printed `NOTE` heredoc body — carry the correction, in one commit, each proven individually.
    - The arm's `VERBATIM` claim is enforced by an extract-and-diff leg, not by an instruction to the executor.
    - If not edited: the SUMMARY carries the reading and the stamps that justify leaving it, both files are `cmp`-proven byte-identical to their pre-edit backups, and the item is routed in task 3.
    - `scripts/mt5-diag.sh` is byte-identical to its pre-edit backup outside the header comment block and the `NOTE` body; the shebang, the shell options, the variable defaults, the `PY` heredoc body and the `railway ssh` invocation are unchanged.
    - The READ-ONLY fence was CALIBRATED: three neuter levers (shebang, a hash-prefixed line inside the `PY` heredoc, a variable default) each proven APPLIED with a `grep -c` and then observed RED, plus a prose-only GREEN control; all four transcripts are recorded verbatim and dated in the SUMMARY with their `cmp` restore confirmations.
    - Every restore was from a `cp` backup verified with `cmp` AND by grepping the correction marker back; ⛔ `git checkout --` was not used anywhere in this task.
    - The prober self-test passes at its baseline count with exit 0, and the wiring test file passes.
    - No historical record was rewritten.
  </acceptance_criteria>

  <done>
    All seven verify banners pass, and the SUMMARY states which of the three verdict branches was
    taken and why, with the reading quoted — plus, on the STALE branch, the four calibration
    transcripts and both backup `shasum` values.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: DECISION — the window verdict, and every deferral routed to a NAMED phase</name>

  <decision>
    Is the MEASURED healing window acceptable as it stands, or does it earn a keepalive / reconnect
    hook as a NAMED follow-up phase? And which phase receives each of this phase's deferrals?
  </decision>

  <context>
    ⛔ **NOT auto-approvable.** The window's acceptability is a judgement about how long MT5 may be
    dark, and D-06 reserved it for the founder deliberately, on the stated grounds that an interval
    chosen before a measurement is chosen against a guess. Auto-selecting the first option here
    would make the decision the measurement was taken to inform.

    **Present, in this order, before asking:**
      1. D-06's number and the sentence beside it: the restart stamp, the first authorized reading,
         the delta, and WHAT CLOSED IT.
      2. The outcome classification (1, 2 or 3 from `&lt;the_three_outcomes&gt;`) and, for outcome 3, the
         Journal-tab tie-breaker that needs VNC.
      3. Question THREE's verdict, with R1 and R2 and the event that produced R2.
      4. The items awaiting a destination, each with a RECOMMENDED phase — ⛔ never a blank
         destination.

    ⛔ Whichever option is taken, it is NEVER a re-litigation of criterion 1's shape. That was a
    founder decision taken on a measurement, and the ROADMAP says so in criterion 2's own text.
  </context>

  <options>
    <option id="window-acceptable">
      <name>The window is acceptable — close the phase, book no keepalive</name>
      <pros>No new phase, no interval tuned against a partially-understood failure mode, and the system is measurably better than the hand-login it replaced.</pros>
      <cons>MT5 stays dark for the measured delta after any gateway restart, and nobody is watching for the next one beyond the hourly prober.</cons>
    </option>
    <option id="book-keepalive">
      <name>The window is too long — book a keepalive / reconnect hook as a NAMED follow-up phase</name>
      <pros>Closes the gap the measurement just proved, and the interval is now chosen against a number rather than a guess — exactly what D-06 deferred it for.</pros>
      <cons>A new phase, and a periodic terminal touch adds contention on the ONE shared terminal that must itself be designed against the lease and the account bracket.</cons>
    </option>
    <option id="hold-open-outcome-3">
      <name>Outcome 3 — the heal did not clear it; hold the phase OPEN</name>
      <pros>Honest: criterion 2 closes on an observation and there is none. Hands the founder the ONE thing that separates a refused saved account from a broker-side block — the terminal's Journal tab.</pros>
      <cons>The phase does not close, and the tie-breaker needs a VNC session, which is founder time. ⛔ But looping, retrying, or "improving" the heal against an unmeasured cause is strictly worse.</cons>
    </option>
  </options>

  <resume-signal>
    Select: `window-acceptable`, `book-keepalive`, or `hold-open-outcome-3` — and confirm the
    destination phase for each routed item.
  </resume-signal>

  <routing>
    **The items to route, each via `/gsd-phase --edit`, ⛔ never by hand-editing the ROADMAP:**
      - **A keepalive / reconnect hook** — book ONLY if the window was judged unacceptable. Its
        design belongs to that phase, not to this decision; D-06's whole argument is that an interval
        chosen before a measurement is chosen against a guess, and the measurement is now in hand.
      - **`[164.6.2-RAISE-LAST-SHAPE-ONLY]`** (plan 01) — the shared last-error raiser scrubs by
        SHAPE only, so the falsy-return arm of the shipped per-account login verb is not by-value
        redacted. Pre-existing, not a regression, and closing it means editing that verb, which D-07
        fences.
      - **The question-THREE item, if UNSETTLED** — the contested sentence and the reading that
        would settle it, with the exact procedure task 1 used, so the next phase does not re-derive
        it.
      - **The gateway image digest pin** — `deploy/mt5-gateway/railway-gateway.md` prescribes pinning
        the image by digest and the provenance line in the go-live runbook is still blank. Real,
        adjacent, and explicitly deferred by this phase's CONTEXT.

    ⛔ **Do not close the phase on outcome 3.** Criterion 2 closes on an observation; without it the
    phase stays open and the founder decides whether to take the VNC step that produces the
    tie-breaker.
    ⛔ Do not book anything into a phase that has already SHIPPED. Check the destination is an
    UPCOMING phase before routing, and prefer an upcoming unplanned one.
  </routing>

  <done>
    The founder has stated whether the measured window is acceptable; every one of the applicable
    items has a NAMED destination phase entered via `/gsd-phase --edit`, with no blank destinations
    and no booking into a shipped phase; and the phase's closure state (closed, or held open on
    outcome 3) is recorded in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live production terminal -> a tracked, PUBLIC planning artifact | readings carry a production filesystem path, a terminal build and potentially an account number |
| the measurement instruments -> the thing being measured | an instrument that mutates destroys the observation and the fault it is measuring |
| an operator mid-incident -> the remedy string in a PUBLIC Actions log and a PUBLIC auto-filed issue | wrong or stale instructions cost real investigation time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.2-20 | Information disclosure | `164.6.2-MEASUREMENT.md` in a PUBLIC, TRACKED directory | critical | mitigate | Only the five permitted diagnostic fields are recorded; a machine scan refuses account-number-shaped digit runs, absolute home paths and the two forbidden field names; `npm run check:planning-hygiene` runs. |
| T-164.6.2-21 | Tampering | making `scripts/mt5-diag.sh` or the prober arm a mutator | high | mitigate | ⛔ No login is added to either; task 1 modifies nothing at all and asserts it; task 2's diag edit is proven NON-EXECUTABLE-ONLY by a BYTE PIN against a pre-edit `cp` backup (shebang, shell options, variable defaults, `PY` heredoc body, `railway ssh` invocation), not by inferring a comment from a leading hash — and that fence is calibrated by three neuter levers observed RED plus a prose-only GREEN control. D-06 and 164.8.3's criterion 5 both forbid a self-healing instrument. |
| T-164.6.2-26 | Repudiation | the arm's `VERBATIM` claim about the diag paragraphs | medium | mitigate | The two paired blocks are extracted, normalised for their comment prefixes and diffed in the same verify as the self-test, so correcting one without the other reds. ⚠️ Pre-existing gap: the claim shipped with no mechanism, and this phase's own widening argument rests on it. |
| T-164.6.2-27 | Repudiation | editing the remedy with no measurement, behind a gate that cannot fail | high | mitigate | `STALE-LICENCE` is a biconditional: a correction marker present in either instrument IMPLIES a STALE verdict line in `164.6.2-MEASUREMENT.md`, and a non-STALE verdict IMPLIES both files are `cmp`-identical to their pre-edit backups. Replaces a prose-only instruction that every automated leg passed regardless of. |
| T-164.6.2-22 | Repudiation | reading a narrowed prober run's EXIT CODE as a verdict | high | mitigate | The narrowed run exits 2 BY DESIGN; the reading is taken from the published runner log, and the plan says so at every point it dispatches one. |
| T-164.6.2-23 | Information disclosure | a corrected remedy string reaching a PUBLIC issue | medium | mitigate | The shipped self-test's digit-run and emphasis legs already gate the string; the correction is checked against all five predicates BEFORE it is written, and the self-test is re-run. |
| T-164.6.2-24 | Repudiation | editing operator instructions against an UNMEASURED state | high | mitigate | Task 2 is GATED on task 1's verdict and edits nothing on CURRENT or UNSETTLED; "no change needed, and here is the evidence" is a planned completion. |
| T-164.6.2-25 | Repudiation | a deferral with no owner | high | mitigate | All four items are routed via `/gsd-phase --edit` to a NAMED, UPCOMING phase; ⛔ no blank destinations and no booking into a shipped phase. |
| T-164.6.2-SC | Tampering | npm/pip/cargo installs | high | accept | ⭐ NO package is installed, added, bumped or re-resolved by this phase. No install task exists, so no legitimacy checkpoint is injected. |
</threat_model>

<verification>
- `164.6.2-MEASUREMENT.md` exists, carries all NINE named sections — including `Prober run id` and `Prober verdict`, the post-restart observation criterion 2 actually closes on — and is clean under both the run-id-masked disclosure scan and `npm run check:planning-hygiene`.
- `node scripts/prod-prober/run.mjs --self-test` prints a `SELF-TEST PASSED: N/N` banner and exits 0.
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts` passes.
- `STALE-LICENCE` holds in both directions: no correction without a STALE verdict, no non-STALE verdict with either file changed.
- All FOUR copies of the stale claim carry the correction, each region proven separately; the arm's `VERBATIM` pair diffs clean.
- `scripts/mt5-diag.sh` is byte-identical to its pre-edit backup outside the header comment block and the `NOTE` heredoc body, and the fence was calibrated on three levers plus a GREEN control.
- Every applicable deferral has a NAMED destination phase entered via `/gsd-phase --edit`.

⚠️ CHANGELOG discipline applies at ship time and is NOT optional: enumerate every commit on the
branch, read the full diff, group by theme, write ONE unified entry per version newest-first, and
CROSS-CHECK that every commit maps to at least one bullet. The entry is written BEFORE the push, in
the same commit as the VERSION bump, and `VERSION` and `package.json` must be BYTE-EQUAL 4-digit
strings. ⛔ Never run `npm version`.
</verification>

<success_criteria>
- ⛔ CRITERION 2 IS CLOSED ON AN OBSERVATION: the gateway was restarted and the prober's mt5 arm returned no defect afterwards, with no VNC session in between — or the phase is deliberately HELD OPEN on outcome 3, which is also a correct ending.
- D-06's window is a recorded NUMBER with a recorded CAUSE, not a guess.
- The repo no longer contradicts itself about whether a login re-disables algo trading — either because the sentence was corrected, or because the reading that confirms it is now on record.
- Nothing this phase found is left without a named owner.
</success_criteria>

<output>
Create `.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-04-SUMMARY.md` when done.
</output>
