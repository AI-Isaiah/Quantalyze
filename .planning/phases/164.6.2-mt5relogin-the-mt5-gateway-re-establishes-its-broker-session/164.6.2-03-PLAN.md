---
phase: "164.6.2"
plan: "03"
type: execute
wave: 3
depends_on: ["164.6.2-02"]
files_modified:
  - docs/runbooks/mt5-go-live.md
  - .planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-HANDOVER.md
autonomous: false
requirements: ["MT5-GATEWAY-LOGIN-01"]

user_setup:
  - service: mt5-broker-credentials
    why: "Criterion 1's boot heal has nothing to authenticate with until the three variables exist. ⛔ Criterion 3: NO AGENT ENTERS A CREDENTIAL — the founder sets them, and the phase closes only after criterion 2 is OBSERVED."
    env_vars:
      - name: MT5_LOGIN
        source: "The broker's INVESTOR (read-only) account number for the mt5-gateway terminal. ⛔ Set on the quantalyze-analytics service, production environment — NOT on mt5-gateway (D-00)."
      - name: MT5_PASSWORD
        source: "The INVESTOR password for that account. ⛔ Investor credentials suffice: only equity and positions are read (criterion 5). Never the master/trading password."
      - name: MT5_SERVER
        source: "The broker server string the terminal connects to, exactly as MetaTrader spells it."
    dashboard_config:
      - task: "Confirm the three variables landed on the ANALYTICS service and not the gateway, and confirm the redeploy they trigger completed"
        location: "Railway -> project -> quantalyze-analytics -> Variables (production environment)"

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A handover artifact exists that hands the founder the EXACT `railway variables --set` invocation for the three variables, with PLACEHOLDERS in every value position and the target service named as the analytics service (criterion 3, D-00)."
    - "It states, in one sentence each, what the founder should expect to happen next: the variable set triggers a redeploy of the analytics service, that redeploy IS a worker restart, and that restart fires the first heal."
    - "It states what the founder must NOT do: set them on the gateway; use master rather than investor credentials; paste a value into any chat, PR, issue or planning file."
    - "`docs/runbooks/mt5-go-live.md` no longer presents the one-time VNC login as the ONLY mechanism that establishes the session — the boot heal is documented beside it, with the measured limit that it fires at analytics STARTUP and not on a gateway restart."
    - "⛔ Nothing written by this plan contains a credential, an account number, a broker server string, or an absolute home path. `npm run check:planning-hygiene` is green."
    - "The founder gate is EXPLICIT and BLOCKING: plan 04 cannot begin until the variables are set, and no agent may proceed past it by inferring that they probably are."
  artifacts:
    - .planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-HANDOVER.md
    - docs/runbooks/mt5-go-live.md
  key_links:
    - "the handover's `railway variables --set` invocation -> the quantalyze-analytics production service -> a Railway redeploy -> a new analytics process -> `main.lifespan` -> the boot heal (plan 02) -> the terminal's broker session."
    - "`docs/runbooks/mt5-go-live.md` Step 2 (MT5GW-01, the one-time VNC login with save-account/auto-login) is the mechanism this phase makes non-load-bearing; the new text sits beside it rather than replacing it, because the FIRST session still has to exist."
  prohibitions:
    - "⛔ NO AGENT ENTERS A CREDENTIAL (criterion 3). Never run `railway variables --set` with a real value; never read one; never ask the founder to paste one back. The artifact contains placeholders and the founder substitutes them in their own terminal."
    - "⛔ Never quote the MT5 account number anywhere. The repo is PUBLIC and `.planning/` is TRACKED, so a planning file is world-readable on push."
    - "⛔ Never write an absolute home path into a tracked file."
    - "⛔ Never direct the credentials at the `mt5-gateway` service. D-00 is a founder decision: the worker-side shape was chosen precisely so the broker password does not move into the container environment."
    - "⛔ Never touch `deploy/mt5-gateway/**` and never produce a Dockerfile — D-00, a founder decision and not a gap."
    - "⛔ Never edit `scripts/mt5-diag.sh`, `scripts/prod-prober/**` or `.github/workflows/prod-prober.yml` in this plan. The measurement instruments stay byte-unchanged until plan 04 has READ with them."
    - "⛔ Never rewrite Step 2's existing instructions. The first session still requires a human; this phase removes the need to REPEAT it."
    - "⛔ Never mark the checkpoint satisfied by inference. `auto_advance` does not apply to a human-action gate, and 'the variables are probably set by now' is exactly the proven-locally-never-observed shape criterion 2 exists to remove."
    - "⛔ Never cite `file:line` — cite BY SYMBOL and by heading (`[164.7-CITATION-DRIFT-01]`)."
---

<objective>
Hand the founder everything they need to set the three Railway variables, and record in the runbook
that the session is no longer re-established only by hand.

Purpose: criterion 3 fences this hard — the agent builds the login path and hands over the exact
commands; the founder sets them; the phase CLOSES only after criterion 2 is observed. The founder
was offered "close on the code, verify later" and REJECTED it, on the grounds that it is exactly the
proven-locally-never-observed shape this milestone removes. Plans 01 and 02 have made the phase
shippable without the founder; this plan is where it stops being closeable without them.

Output: `164.6.2-HANDOVER.md` and one new subsection in `docs/runbooks/mt5-go-live.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-CONTEXT.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-RESEARCH.md
@.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-02-SUMMARY.md
</context>

<measured_baseline>
| Fact | Value | How measured |
|---|---|---|
| Where the variables go | the `quantalyze-analytics` service, production environment — ⛔ NOT `mt5-gateway` (D-00) | CONTEXT D-00, RESEARCH Runtime State Inventory |
| Their state today | `MT5_LOGIN` and `MT5_SERVER` measured ABSENT on the analytics service; `MT5_ENABLED=true`, `MT5_GATEWAY_HOST` and `MT5_GATEWAY_PORT` already set | RESEARCH, `railway variables … --kv` filtered to non-secret keys |
| What the gateway reads | `CUSTOM_USER`, `PASSWORD` (both VNC) and `mt5server_port`, nothing else | `deploy/mt5-gateway/railway-gateway.md` |
| Setting variables triggers | a Railway redeploy of the analytics service, which is itself a worker restart — ⭐ so it should fire the first heal | RESEARCH assumption A5, marked LOW risk and to be OBSERVED rather than relied on |
| Runbook Step 2 heading | `Step 2 — ONE-TIME VNC INSTALL + INVESTOR LOGIN (MT5GW-01)`, which prescribes the save-account / auto-login tick | read at HEAD |
| Planning-hygiene gate | `npm run check:planning-hygiene` (also the last link of `npm run lint`) | `package.json` read at HEAD |

⚠️ **A5 is an ASSUMPTION, not a fact, and the handover must say so.** If the redeploy does not fire
or the heal runs before the gateway is reachable, that is not a failure — it is D-06's measurement
producing its answer. ⛔ Do not promise the founder it will heal immediately.
</measured_baseline>

<tasks>

<task type="auto">
  <name>Task 1: the handover artifact and the runbook subsection</name>

  <read_first>
    - `docs/runbooks/mt5-go-live.md` — Step 1 (STAND-UP) and Step 2 (the one-time VNC install and investor login) in full, plus Step 5 (GATE-CHECK) for the file's voice on explicit-never-assumed verification.
    - `.planning/phases/164.6.2-…/164.6.2-CONTEXT.md` — D-00 and the "NOT in scope" fence, verbatim.
    - `.planning/phases/164.6.2-…/164.6.2-02-SUMMARY.md` — what the heal actually does, so the handover describes the shipped behaviour rather than the planned one.
    - `scripts/check-planning-hygiene.ts` — what it scans for, so the artifact is written to pass it rather than fixed afterwards.
  </read_first>

  <files>
    .planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-HANDOVER.md,
    docs/runbooks/mt5-go-live.md
  </files>

  <action>
    **(a) The handover artifact.** Create `164.6.2-HANDOVER.md` in the phase directory. Five short
    sections, in this order:
      1. **What the founder is being asked to do, in one sentence**, and the one thing that makes it
         safe: investor (read-only) credentials suffice, because only equity and positions are read.
      2. **The exact command**, as a single `railway variables --set` invocation naming the analytics
         service and the production environment, with ⛔ a PLACEHOLDER in every value position.
         Beside it, the one-line reason the service is the analytics one and not the gateway: D-00
         rejected moving the broker password into the container environment.
      3. **What happens next, and what is an ASSUMPTION rather than a promise.** Setting variables
         triggers a redeploy of the analytics service; that redeploy is a worker restart; the boot
         heal fires on that restart. ⚠️ Mark the redeploy claim as the assumption it is — it is to be
         OBSERVED in plan 04, not relied on — and state plainly that the heal fires at analytics
         STARTUP and not when the gateway restarts, so a window exists and measuring it is D-06's
         whole job.
      4. **What NOT to do**, as four bullets: not on the gateway; not master credentials; never paste
         a value into a chat, a PR, an issue or a planning file (this repo is PUBLIC and `.planning/`
         is TRACKED); and do not re-do the VNC login first — the point of the phase is that it stops
         being necessary, and doing it by hand would mask whether the heal worked.
      5. **How the founder will know it worked**, naming the three READ-ONLY instruments by path and
         invocation, and stating that plan 04 runs them: the repo's own diagnostic script, the
         narrowed prober arm dispatch, and the scheduled hourly prober.
    ⛔ Every value position is a placeholder. ⛔ No account number, no broker server string, no
    password, no absolute home path, anywhere in this file.

    **(b) The runbook subsection.** In `docs/runbooks/mt5-go-live.md`, add ONE new subsection
    immediately after Step 2's existing content, titled so it reads as a sibling of that step rather
    than a replacement. It says four things and nothing more:
      - The first session still requires the one-time VNC login Step 2 describes. ⛔ Do not delete or
        soften Step 2 — it is still how a session comes into existence at stand-up.
      - Since this phase, the analytics service re-establishes the terminal's session from three of
        its own environment variables, once at each of its own startups. Name the three variables and
        the SERVICE they live on.
      - ⚠️ The measured limit, stated as a limit: the heal fires at ANALYTICS startup, not when the
        gateway restarts, so a gateway restart leaves a window in which the prober can still report
        the authorization failure. Point at the phase for the measured number rather than restating
        a number here, which would rot.
      - What to do if the session is lost: check the three variables are set, then restart the
        ANALYTICS service — ⛔ not the gateway, which is what restarting used to mean here.
    ⛔ Cite by symbol and by heading, never `file:line`.
    ⛔ Do not touch `deploy/mt5-gateway/**`, `scripts/mt5-diag.sh`, `scripts/prod-prober/**` or
    `.github/workflows/prod-prober.yml`.
  </action>

  <verify>
    <automated>npm run check:planning-hygiene</automated>
    <fails_when>non-zero exit. Any finding on the new artifact means a value, a path or a shape the gate refuses reached a tracked planning file — fix the artifact, ⛔ never the gate.</fails_when>

    <automated>grep -rInE '[0-9]{6,}|/Us[e]rs/|/home/[a-z]' .planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-HANDOVER.md docs/runbooks/mt5-go-live.md | grep -vE '^[^:]+:[0-9]+:[[:space:]]*(#|//|<!--)' | grep -c . | { read n; test "$n" -eq 0 && echo "NO-DISCLOSURE-SHAPES OK"; }</automated>
    <fails_when>non-zero exit, or the printed line is not `NO-DISCLOSURE-SHAPES OK`. A six-or-more-digit run is the shape of an MT5 account number; a user-home prefix is an absolute local path. Both are world-readable once pushed. ⚠️ If a legitimate long number is genuinely needed, it is not — restate it as a placeholder.</fails_when>

    <automated>D=$(git diff --name-only -- deploy/ scripts/ .github/) || exit 1; n=$(printf '%s' "$D" | grep -c .); test "$n" -eq 0 && echo "INSTRUMENTS-UNTOUCHED OK"</automated>
    <fails_when>non-zero exit, or the printed line is not `INSTRUMENTS-UNTOUCHED OK`. Any path under those three roots appearing means this plan reached into D-00's fence or into the measurement instruments before they had been READ with.</fails_when>

    <automated>grep -cE 'MT5_LOGIN|MT5_PASSWORD|MT5_SERVER' .planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-HANDOVER.md | { read n; test "$n" -ge 3 && echo "HANDOVER-NAMES-THE-THREE OK ($n)"; }</automated>
    <fails_when>non-zero exit, or the printed line is not a `HANDOVER-NAMES-THE-THREE OK` banner. Fewer than three matching lines means the artifact does not actually name all three variables and the founder would have to guess one.</fails_when>
  </verify>

  <acceptance_criteria>
    - `164.6.2-HANDOVER.md` exists with the five sections, names all three variables and the analytics service, and carries a placeholder in every value position.
    - It marks the redeploy-fires-the-heal claim as an assumption to be observed, not a promise.
    - `docs/runbooks/mt5-go-live.md` carries a new subsection beside Step 2 that documents the boot heal AND its measured limit, without weakening Step 2.
    - `npm run check:planning-hygiene` is green and the disclosure-shape scan is clean.
    - Nothing under `deploy/`, `scripts/` or `.github/` is touched.
  </acceptance_criteria>

  <done>
    All four verify banners pass; `git diff --name-only` lists exactly the two paths in `&lt;files&gt;`.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: FOUNDER GATE — set the three Railway variables on the analytics service</name>

  <action>Set `MT5_LOGIN`, `MT5_PASSWORD` and `MT5_SERVER` on the `quantalyze-analytics` service — the ONE step in this phase no agent may perform (criterion 3).</action>

  <instructions>
    ⛔ **STOP. This gate is BLOCKING and is NOT auto-approvable.** `workflow.auto_advance` does not
    apply: criterion 3 is a founder decision that no agent enters a credential, and criterion 2
    closes ONLY on an observation that cannot exist until this is done.

    **What the agent has already done:** built the credentialed heal path and its security control
    (plan 01), wired it into `main.lifespan` and falsified the wiring (plan 02), and written
    `164.6.2-HANDOVER.md` with the exact `railway variables --set` invocation, placeholders in every
    value position (plan 03 task 1). Nothing is left for the founder except the credential itself.

    Present the founder with `164.6.2-HANDOVER.md` and ask them to run the command it contains, in
    their own terminal, with their own values.

    **What the founder does:**
      1. Sets `MT5_LOGIN`, `MT5_PASSWORD` and `MT5_SERVER` on the `quantalyze-analytics` service,
         production environment, using the INVESTOR (read-only) credentials.
      2. Confirms in the Railway dashboard that the three landed on the ANALYTICS service and not on
         `mt5-gateway`.
      3. Tells the agent, in words, that they are set — ⛔ without quoting any value.

    **What the agent must NOT do, in any circumstance:**
      - ⛔ Run `railway variables --set` with a real value.
      - ⛔ Read a value back, echo one, or ask the founder to paste one for confirmation.
      - ⛔ Infer that the variables are probably set and proceed. A green `/health`, a passing test,
        a successful redeploy and a quiet log are all consistent with them being absent — the heal
        is designed to log once and continue in exactly that case. ⚠️ Proceeding on inference is the
        proven-locally-never-observed shape criterion 2 was written to remove.
      - ⛔ Perform a VNC login to "help it along". That would establish the session by hand and
        destroy the observation criterion 2 exists to make.
  </instructions>

  <verification>
    KEY-ONLY, never value-reading: `railway variables --service quantalyze-analytics --environment
    production --kv` lists KEYS. Filter the output to the three KEY NAMES and confirm all three are
    present. ⚠️ That command's raw output carries values for other variables in the same stream — ⛔
    never paste it into an artifact, a commit message, a PR body or a chat transcript; report only
    "all three names present" or which name is missing.
    ⛔ A green `/health`, a passing test, a completed redeploy and a quiet log are ALL consistent
    with the variables being absent — the heal logs once and continues in exactly that case — so
    none of them verifies this gate.
  </verification>

  <resume-signal>
    The founder states in words that the three variables are set on the analytics service (⛔ without
    quoting any value), AND the key-only listing shows all three names. Type "set" to continue. Only
    then does plan 04 begin.
  </resume-signal>

  <done>
    The founder has confirmed in words that the three variables are set on the analytics service,
    and a KEY-ONLY listing shows all three names. No value was read, echoed or written anywhere.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder's terminal -> Railway service variables | the only place a real broker credential is ever typed |
| this repository -> the public internet | the repo is PUBLIC and `.planning/` is TRACKED, so every artifact here is world-readable on push |
| the runbook -> a future operator | stale instructions here become wasted human steps or a masked observation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.2-15 | Information disclosure | `164.6.2-HANDOVER.md` in a PUBLIC, TRACKED planning directory | critical | mitigate | Placeholders in every value position; a disclosure-shape scan for account-number-shaped digit runs and absolute home paths; `npm run check:planning-hygiene` in the verify. |
| T-164.6.2-16 | Information disclosure | the agent reading or echoing a credential during the gate | critical | mitigate | The checkpoint forbids running the set command, reading a value back, and asking for a paste; confirmation is KEY-ONLY and its raw output is never pasted into an artifact. |
| T-164.6.2-17 | Elevation of privilege | credentials placed on `mt5-gateway` instead of the analytics service | high | mitigate | The handover names the target service in the command itself and states D-00's rejection beside it; the founder confirms placement in the dashboard. |
| T-164.6.2-18 | Elevation of privilege | master rather than investor credentials | high | mitigate | The handover states investor credentials suffice because only equity and positions are read (criterion 5), in the same sentence that asks for them. |
| T-164.6.2-19 | Repudiation | the gate satisfied by inference rather than observation | high | mitigate | The checkpoint enumerates every signal that is consistent with the variables being ABSENT, and requires an explicit founder statement plus a key-only listing. |
| T-164.6.2-SC | Tampering | npm/pip/cargo installs | high | accept | ⭐ NO package is installed, added, bumped or re-resolved by this phase. No install task exists, so no legitimacy checkpoint is injected. |
</threat_model>

<verification>
- `npm run check:planning-hygiene` exits 0.
- The disclosure-shape scan over both written files is clean.
- `git diff --name-only` lists exactly the two paths in `files_modified` and nothing under `deploy/`, `scripts/` or `.github/`.
- The founder has stated the variables are set; a key-only listing shows all three names.
</verification>

<success_criteria>
- The founder has everything needed to act, in one artifact, with no value ever passing through an agent.
- The runbook no longer presents a hand login as the only mechanism, and states the heal's measured limit rather than implying it is instant.
- Plan 04's measurement is unblocked, and it is unblocked by an OBSERVATION rather than an assumption.
</success_criteria>

<output>
Create `.planning/phases/164.6.2-mt5relogin-the-mt5-gateway-re-establishes-its-broker-session/164.6.2-03-SUMMARY.md` when done.
</output>
