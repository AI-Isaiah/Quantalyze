---
status: partial
phase: 161.1-ledger-refresh
source: 161.1-VERIFICATION.md (1 human_verification item; 4/4 requirements LEDGER-01..04 already verified, shipped v0.73.0.0)
started: 2026-09-05T22:30:00Z
updated: 2026-09-05T22:30:00Z
---

## Current Test

[testing paused — 1 item outstanding, founder-gated by design and not closable by an agent]

## Tests

### 1. ACTIVATION — register the schedule for `enqueue_ledger_refresh_for_strategies` (and the composite arm) on PROD
expected: mt5 (and any future sfox/deribit) strategies show `strategy_analytics.computed_at` advancing on the chosen cadence, closing the gap against the ccxt venues
result: blocked
blocked_by: server
reason: |
  FOUNDER-GATED BY DESIGN, and correctly so. The phase shipped DORMANT on purpose (LEDGER-02);
  activation is a live operation that registers a recurring compute job on production, which an
  agent must not do. Nothing about this item is waiting on analysis or effort.

  ✅ DORMANCY RE-VERIFIED FROM THE REPO SIDE, 2026-09-05. Both functions still exist
  (`20260825130000_ledger_refresh_fanout_dormant.sql`, `20260825140000_ledger_refresh_composite_arm.sql`)
  and NO migration schedules either one — `grep -rn "cron.schedule" supabase/migrations/ | grep -i ledger`
  returns only `20260825120000_ledger_refresh_staleness_view.sql:116`, which is the comment saying
  "This file registers NO pg_cron schedule and none may ever be [added]". Neither
  `enqueue_ledger_refresh_for_strategies` nor `enqueue_ledger_composite_refresh` appears among the
  15 job names the repo's `cron.schedule` call sites declare. The designed end state is intact.

  ⛔ HONEST LIMIT — the LIVE half was NOT re-measured this session, and I am not asserting it.
  The 2026-08-28 record measured PROD directly (13 active `cron.job` rows, none calling either
  function; mt5 strategies 81.7 h stale against okx at 5.4 h). I could not repeat that today:
  `psql` is absent from this machine (`command -v psql` → ABSENT) and reading `.env` for a DSN was
  DENIED by permission prompt, which I did not route around. So the staleness figures above are the
  2026-08-28 reading, not a current one, and the gap may be wider or narrower now. Do not read this
  UAT as evidence that the gap is unchanged.

  ⚠️ Read `strategy_analytics.computed_at`, NEVER `api_keys.last_sync_at`. The mt5 keys report a
  recent `last_sync_at` while their analytics are days stale — that is exactly the lie this phase
  was written against, and it is the trap anyone re-measuring will fall into first.

  ⚠️ No sfox or deribit strategy exists on PROD, so those two arms have no live population to
  observe either way. Activation cannot be judged on them.

  ⛔ **UPDATE 2026-09-05, SAME DAY — ACTIVATION WAS ATTEMPTED ON PROD AND IS BLOCKED BY A DEFECT,
  not by founder gating.** The founder authorised activation (hourly). All three forms were run in
  the Supabase SQL editor as `postgres` against PRODUCTION:

  | Form | Result |
  |---|---|
  | `ALTER DATABASE postgres SET app.ledger_refresh_enabled = 'true'` | `ERROR: 42501: permission denied to set parameter` |
  | `ALTER ROLE postgres SET app.ledger_refresh_enabled = 'true'` | `ERROR: 42501: permission denied to set parameter` |
  | `SET app.ledger_refresh_enabled = 'true'` (session) | ✅ `true` |

  So this phase shipped a fail-closed activation switch **that cannot be thrown on this platform**.
  Both operator-facing forms the runbook offers (Step 1a and the 1c fallback) return 42501; only a
  session-scoped `SET` works, and no out-of-band operator action is session-scoped. NOTHING was
  scheduled — `cron.schedule` was never run, so PROD is unchanged and the phase remains dormant.

  ⚠️ **My prediction was WRONG and the record should say so.** I told the founder to expect 1a to
  fail (correct — the same 42501 was measured for `app.analytics_service_key` this morning) but
  predicted the 1c role-level fallback would SUCCEED, reasoning that altering one's own role is a
  different permission check. It is not, for a placeholder GUC. Recorded because the runbook's 1c
  step rests on the same wrong assumption.

  ⛔ A workaround EXISTS and was REJECTED by the founder in favour of the proper fix: setting the
  flag inside the cron command (`$$SET app.… = 'true'; SELECT enqueue_…();$$`) works, but holds the
  fail-closed guard open by its own caller and silently turns the runbook's FAST rollback into a
  no-op that reports success. The founder chose to wait.

  ⭐ **NEW OWNER: Phase 164.7 APPSETTINGS** (inserted 2026-09-05), which retires every
  `current_setting('app.` reader — 12 sites across 4 settings — and whose success criterion 3 is
  this very activation being thrown and OBSERVED working on PROD. This item is therefore no longer
  "founder-gated, indefinite"; it is blocked on a named phase with a measured cause.

## Summary

total: 1
passed: 0
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

- truth: "Activating LEDGER-02 after Phase 164.1 commits its cron manifest will trip the new drift arm unless the manifest is updated in the same act"
  status: failed
  reason: >
    ORDERING HAZARD found 2026-09-05 while reading this item against the phases now in flight. It
    is not a defect in either phase; it is an interaction neither one currently mentions, and the
    two are being built in the same milestone.
    Phase 164.1 CRON-DRIFT-01 commits a manifest of the ACHIEVABLE PROD `cron.job` configuration
    and fails loud when live PROD drifts from it — over ALL rows, not just jobid 1 (that scope is
    a locked CONTEXT decision). This item's whole purpose is to ADD a new recurring job to PROD by
    hand, which is drift by that arm's definition. So whoever activates the ledger refresh after
    164.1's manifest lands will turn the prober red, and the red will be CORRECT: PROD really will
    have a job the committed manifest does not.
    That is the arm working, not failing — but only if the operator knows to re-capture the
    manifest in the same act. If they do not, the first reading is an alarm that looks like a bug
    in the new prober, on its first week, which is the fastest way to teach people to ignore it.
    Phase 164.5's CRON-DRIFT-01-REPAIR has the same shape and already carries a pre-flight
    discipline; this one has none because it predates the prober.
  severity: minor
  test: 1
  root_cause: "161.1 was designed and shipped before a cron-drift oracle existed; its activation runbook cannot mention a gate that did not yet exist."
  artifacts:
    - path: ".planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-VERIFICATION.md"
      issue: "the ACTIVATION item's runbook does not mention re-capturing the 164.1 cron manifest"
  owner: "Phase 164.5 CRON-DRIFT-01-REPAIR carries the same shape and already has the pre-flight discipline; this ordering note belongs beside it."
  missing:
    - "When 164.1 lands, add one line to this item's activation instruction: register the schedule AND re-run the prober's `--capture-manifest` dispatch, review the new row for credential material, and commit the updated manifest in the same change."
    - "Founder call on ordering: activating BEFORE 164.1's manifest is captured avoids the interaction entirely, since the capture would simply include the new job. That is the cheaper sequence if activation is imminent."
  debug_session: ""
