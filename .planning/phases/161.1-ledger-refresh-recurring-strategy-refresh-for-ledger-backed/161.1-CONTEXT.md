# Phase 161.1 — CONTEXT

Founder decisions and re-measured PROD context. Counts and dates only — this repo is public
and `.planning/` is tracked.

## PROD staleness census — re-measured 2026-08-25 (supersedes the 2026-08-24 census)

Read-only query against PROD. Venue resolved via `strategies.api_key_id` **and**
`strategy_keys` (single-key strategies link directly; only composites use `strategy_keys` —
a census joining only `strategy_keys` sees exactly one row and is wrong).

| venue | class | strategies | composites | complete | complete_with_warnings | failed | max age (d) |
|---|---|---|---|---|---|---|---|
| deribit | **LEDGER** | 1 | **1** | 0 | 1 | 0 | 36 |
| mt5 | **LEDGER** | 4 | 0 | 0 | 4 | 0 | 21 |
| sfox | **LEDGER** | **0** | – | – | – | – | – |
| (no key / CSV) | other | 38 | 0 | 3 | 0 | 34 | 92 |
| bybit | other | 1 | 0 | 0 | 0 | 1 | 0 |
| okx | other | 4 | 0 | 2 | 0 | 2 | 0 |

`computation_status` vocabulary as it actually exists: `failed` (37), `complete_with_warnings`
(5), `complete` (5). **`complete` is NOT the only success value** — a predicate written as
`status = 'complete'` treats every healthy ledger strategy as broken, because all 5 live
ledger rows are `complete_with_warnings`.

### What the census changes

1. **The LEDGER cohort is 5 strategies total.** The v1.11 wedge that motivates the fan-out
   fear was 24 keys. `LIMIT` sizing here is near-trivial. Do NOT over-engineer the bounding —
   keep the partition cap because the shape is right, not because the volume demands it.
2. **sfox has ZERO strategies in PROD.** Its arm ships unexercised. It must still be in the
   refresh set (criterion 4 pins it), but no test may assert non-empty sfox output.
3. **mt5 is not failing — it is simply never re-running.** All 4 are `complete_with_warnings`,
   none `failed`. This confirms the root cause and rules out "the refresh is erroring" as a
   competing explanation.
4. **The 34 `failed` rows are the CSV cohort, not the ledger venues.** Out of scope here. See
   OQ-5 sibling defect below — worth filing separately, not absorbing into this phase.
5. ccxt venues (okx, bybit) sit at 0 days — the ccxt recompute path demonstrably works, which
   is what makes the ledger gap a gap rather than a global outage.

## Founder decisions

### D1 — Composite ledger strategies: DEFERRED (founder, 2026-08-25)

> "on mt5 no composites" / "for now we defer mt5 composites. In the future, there might be"

**Scope of the deferral: MT5 only.** For MT5 this costs nothing — 4 strategies, 0 composites.

⚠️ **This is a CURRENT FACT, not a structural invariant.** The founder explicitly expects MT5
composites may exist later. The `strategy_keys` exclusion conjunct MUST therefore be present
and commented in the fan-out predicate — a future MT5 composite must be *skipped deliberately*,
never silently mishandled. Do not omit the conjunct on the grounds that the count is zero today.

⛔ **COLLISION the planner must resolve — do not guess.** The ONLY deribit strategy in PROD
**is** a composite. If the exclusion is written as "exclude all composites," deribit gets ZERO
coverage: present in the refresh set, with nothing to refresh, and criterion 4's pin still
passes because the venue is technically listed. Options:
  - (a) Exclude composites for **mt5 only** (literal reading of the founder call), so the
        deribit composite is still refreshed.
  - (b) Exclude all composites, and explicitly record that deribit ships at zero live coverage
        this phase.
Surface this as a founder decision; (a) matches what was actually said.

### D2 — Cohort scoping

Scope off `_LEDGER_BACKED_SOURCES` (`long_fetch.py:63`), **never** off absence from
`EXCHANGE_CLASSES`. deribit is in `EXCHANGE_CLASSES`; an absence-scoped cohort silently drops it.
See ROADMAP CORRECTION 1.

## Carried constraints (see ROADMAP for full text)

- ⛔ Do NOT add mt5 to `RECONCILABLE_EXCHANGES` (BYB-02 corruption class).
- ⛔ Do NOT re-register `derive-allocator-key-dailies`, and do NOT `cron.schedule` from a
  migration (runbook `flipretry-derived-equity-go-live.md:171`; migrations auto-apply to PROD).
- ⛔ Re-enqueuing `process_key_long` is a provable no-op (`long_fetch.py:154`, `:193`).
- ⛔ Criterion 3 may not key on `computed_at` (re-stamped on every transition, including the
  `failed` arm) nor `last_sync_at`. Must conjoin `computation_status`, treating BOTH `complete`
  and `complete_with_warnings` as success.
- ⛔ No TS mirror of the venue set (`strategyGate.invariant.test.ts` bans venue literals).
- ⛔ A7: the recurring mt5 `derive_broker_dailies` → `strategy_analytics` path has never run
  end-to-end. First verification = one manual enqueue, one MT5 strategy, observed to completion,
  BEFORE anything is scheduled.

## Open questions still owned by the founder

- **OQ-5 (sibling defect, file separately):** ccxt strategies with no new fills also never
  recompute — the `stored > 0` predicate at `routers/cron.py:471-472`. Not this phase.
- **OQ-1 / OQ-3:** whether `/api/cron-sync` is scheduled in PROD, and whether pg_net + `app.*`
  GUCs are live. Both only gate rejected alternatives; not blocking the recommended approach.
