---
phase: 161.1-ledger-refresh
plan: 04
type: execute
wave: 4
depends_on: ["161.1-01", "161.1-02", "161.1-03"]
files_modified:
  - supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql
  - supabase/tests/test_ledger_refresh_composite_arm.sql
  - analytics-service/tests/test_ledger_refresh_gates.py
  - docs/runbooks/ledger-refresh-go-live.md
autonomous: false
requirements: [LEDGER-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Deribit — whose only live strategy is a composite — has a real recurring refresh path, not just set membership."
    - "A composite with any mt5 member is excluded, deliberately and visibly, per the founder's MT5-scoped deferral."
    - "The composite path is proven end-to-end on one real composite before it is scheduled."
  artifacts:
    - supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql
    - supabase/tests/test_ledger_refresh_composite_arm.sql
  key_links:
    - "The composite refresh unit is stitch_composite, a separate registered strategy-scoped kind — strategy-mode derive structurally cannot serve a composite."
    - "The composite arm is a SEPARATE function, so it can be scheduled, bounded and rolled back independently of the single-key arm."
---

<objective>
Give deribit real live coverage by refreshing ledger-backed composites through their own chain
(LEDGER-01), gated on the D-COMP decision taken in plan 01 task 4.

Purpose: CONTEXT records the founder's composite deferral as scoped to MT5 — "on mt5 no composites
/ for now we defer mt5 composites". Measured at HEAD, the only deribit strategy in PROD IS a
composite, and a composite has `strategies.api_key_id = NULL`
(`finalize-wizard/route.ts:1177,1388-1392`), which the single-key arm shipped in plan 02
structurally cannot serve. Without this plan, deribit sits in the refresh set with nothing to
refresh, and criterion 4's venue pin passes on membership alone — green while the venue rots.

Output: a second, independently schedulable fan-out on the `stitch_composite` kind, its matched-pair
SQL gate, an extension of the existing static gates, a founder tracer for the one live composite,
and the runbook section that activates it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-CONTEXT.md
@.planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-01-SUMMARY.md
@.planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-02-SUMMARY.md

Source files the tasks read (each once):
@supabase/migrations/20260710130000_stitch_composite_kind.sql
</context>

<gate>
⛔ READ THIS FIRST. This plan is conditional on the D-COMP decision recorded in
`161.1-01-SUMMARY.md` (plan 01, task 4).

- **If D-COMP resolved to (a′) — ship the composite arm:** execute all three tasks below.
- **If D-COMP resolved to (b) — single-key arm only:** execute NOTHING in this plan. Do not create
  the migration, the test, or the gate extension. Instead confirm that plan 03 task 2 edit 3
  already recorded, in TODOS.md and in the runbook's "what this deliberately does not cover"
  section, that deribit ships at zero live coverage this phase because its only live strategy is a
  composite. If that record is missing or softer than that sentence, add it. Then write the SUMMARY
  stating the plan closed unexecuted by decision, and stop.

Do not infer the answer. If `161.1-01-SUMMARY.md` does not record a D-COMP answer, halt and ask.
</gate>

<planning_decisions>
**D-11 — the composite arm is a SEPARATE function, not a second branch inside the single-key
fan-out.** Three reasons, each measured: the kinds differ (`stitch_composite` versus
`derive_broker_dailies`); the handler ceilings differ (20 minutes versus 15 —
`job_worker.py:502`), so the per-tick bound derives to a different integer; and separate functions
mean the founder can schedule, bound and roll back one without touching the other, which matters
because the composite path carries per-member exchange crawls and was the subject of the
stitch-composite crawl-bounding incident.

**D-12 — `stitch_composite` is the right unit and already exists.** It is a registered
strategy-scoped kind (`20260710130000`, admitted by both compute_jobs CHECKs), it fans over
`strategy_keys` members by seq, and it writes the headline `strategy_analytics` row DIRECTLY in one
atomic upsert (`job_worker.py:5350`, `:7177`) rather than chaining — `JOB_CHAIN_FOLLOW_ON` records
it as chain-terminal. So it reaches `strategy_analytics`, which is what criterion 1 requires.

**D-13 — the mt5 exclusion is on MEMBERSHIP, not on the strategy's headline venue.** A composite
with any mt5 member is excluded, because D-01's deferral is about the MT5 path, and a mixed
composite would drag the MT5 terminal into the composite crawl. The staleness view already exposes
a has-mt5-member flag for exactly this (plan 01, D-06).
</planning_decisions>

<tasks>

<task type="auto">
  <name>Task 1: The composite fan-out — separate function, separate bound, same dormancy</name>
  <files>supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql</files>
  <read_first>
    supabase/migrations/20260710130000_stitch_composite_kind.sql — the kind registration and both CHECK clauses, confirming stitch_composite is admitted with a strategy_id target and all other targets NULL.
    supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql (this phase, plan 02) — mirror its structure exactly: activation-setting guard, advisory lock with the release-on-exception arm, candidate CTE selecting from the staleness view, per-row enqueue with the unique-violation swallow, count return, REVOKE, self-verify DO block.
  </read_first>
  <action>
Create ONE migration defining `public.enqueue_ledger_composite_refresh()`, zero parameters,
RETURNS INTEGER, SECURITY DEFINER, search_path pinned — the same hygiene triple as the single-key
arm, for the same reason: a caller-supplied threshold on a cross-tenant definer function is the
recorded incident class.

⛔ Same dormancy contract as plan 02. This migration registers no schedule. It reads the SAME
activation setting as the single-key arm so one reset kills both arms at once, and it takes its
OWN advisory lock key so the two functions never block each other.

Candidate CTE, selecting from the staleness view:
  - the view's is-stale verdict
  - `is_composite` is TRUE (the mirror image of plan 02's conjunct)
  - the has-mt5-member flag is FALSE. Comment it with D-01 and D-13 in full: composites are
    deferred FOR MT5 by founder call; a composite with any mt5 member is skipped deliberately, and
    a future all-mt5 composite must land here as a visible skip, never as silent mishandling.
  - strategy lifecycle status in the same pair plan 02 used
  - at least one member key is eligible — active, not revoked, not disconnected. ⚠️ Decide and
    comment the semantics explicitly: a composite whose members are partly disconnected is still
    refreshable over its remaining declared windows, so require at least one eligible member rather
    than all members eligible, and say so.
  - the same attempt cooldown shape as plan 02 but on the composite kind
  - the same non-terminal in-flight guard
  - per-tick bound: derive it here rather than copying plan 02's integer. The composite handler
    ceiling is 20 minutes and dispatch is sequential, so at an hourly tick n × 1200 s must stay
    under 3600 s, giving n at most 3. Write that derivation as a comment above the integer. No
    per-venue partition is needed while the live composite cohort is a single strategy; state that
    as the reason rather than omitting it silently, and order oldest-first so a future larger
    cohort cannot starve a tail.

Enqueue with `enqueue_compute_job(p_strategy_id := …, p_kind := 'stitch_composite', p_metadata :=
…)` carrying a source token distinct from the single-key arm's, so the two mechanisms are
distinguishable in the queue. Unique-violation swallow per row.

Self-verify DO block, RAISE EXCEPTION style: function exists with zero arguments, is definer with a
pinned search_path, anon and authenticated hold no EXECUTE, the staleness view exists, and the
composite kind is still admitted by both compute_jobs CHECK clauses (copy the assertion shape from
the kind-registration migration — a CHECK re-base that drops the kind is a recorded silent-failure
mode in this repo). ⛔ The block must not call the function.
  </action>
  <verify>
    <automated>psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql</automated>
  </verify>
  <done>The migration applies to TEST, the self-verify block passes, and calling the function with the activation setting unset returns 0 and inserts nothing.</done>
  <reversibility rating="reversible">Second function, zero callers, fail-closed; DROP FUNCTION is a complete rollback and the single-key arm is untouched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Matched-pair gate for the composite arm, plus the static-gate extension</name>
  <files>supabase/tests/test_ledger_refresh_composite_arm.sql, analytics-service/tests/test_ledger_refresh_gates.py</files>
  <read_first>
    supabase/tests/test_ledger_refresh_fanout.sql (this phase, plan 02) — the seeding idiom for composites already exists in its arm D; reuse that shape rather than inventing a second one.
  </read_first>
  <action>
Two files.

**The SQL gate.** Same harness conventions used across this phase: plain PL/pgSQL DO block, RAISE
EXCEPTION, rolls back, no psql meta-commands, presence gate for test-DB lag — and the executor must
confirm from the output that the assertions EXECUTED rather than skipped.

Arms:
  - DORMANCY: activation setting unset, one stale eligible deribit composite seeded, function
    returns 0 and inserts nothing.
  - POSITIVE: setting on, the same composite is enqueued as exactly one `stitch_composite` row with
    `strategy_id` set and every other target NULL.
  - MT5 EXCLUSION (D-13): a stale composite with one deribit member and one mt5 member is NOT
    enqueued. Then a stale composite whose members are all mt5 is NOT enqueued. Both are the
    founder's deferral made testable.
  - NEGATIVE CONTROL: a FRESH deribit composite is not enqueued — without this arm a body that
    enqueues every composite passes the positive arm.
  - SINGLE-KEY EXCLUSION: a stale single-key deribit strategy is NOT enqueued by this function; it
    belongs to plan 02's arm. Together with plan 02 arm D this pins that the two functions
    partition the cohort rather than overlapping.
  - DEDUPE and COOLDOWN: same two arms as plan 02, on the composite kind.
  - BOUND: seed 5 stale eligible non-mt5 composites, call once, assert the per-tick integer bounds
    the result.

**The static-gate extension.** Add to the existing `analytics-service/tests/test_ledger_refresh_gates.py`
a module constant for this migration's filename with the same pointer-hygiene comment, and extend
the existing gates to cover it: no pg_cron registration verb in its raw text (build the tokens at
runtime exactly as the existing gate does), no venue literals in its function body, the activation
setting read with a strict equality comparison, the derived per-tick integer at or below 3, and the
definer/search_path/REVOKE triple. Reuse the existing anti-vacuity floor helper — do not add a
second, weaker one.

Run `mypy --strict` over the modified Python file before considering the task done.
  </action>
  <verify>
    <automated>psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_ledger_refresh_composite_arm.sql</automated>
    <automated>cd analytics-service &amp;&amp; python3 -m pytest tests/test_ledger_refresh_gates.py -x -q</automated>
    <human-check>RED-proof, mandatory: (1) delete the has-mt5-member conjunct — the MT5 EXCLUSION arm MUST fail; restore byte-identical. (2) delete the is-composite conjunct — the SINGLE-KEY EXCLUSION arm MUST fail; restore byte-identical. (3) raise the per-tick integer above the derived ceiling — the static gate MUST fail; restore byte-identical. Record all three observations and their restorations in the SUMMARY.</human-check>
  </verify>
  <done>Every arm executes and passes, the extended static gates pass, `mypy --strict` is clean, and the three neuterings were observed RED first-hand and restored byte-identical.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Composite tracer on PROD, then the runbook's composite section</name>
  <action>Founder runs ONE manual composite enqueue on PROD and observes it to completion; the runbook's composite section is written only if it passes.</action>
  <instructions>
The composite refresh path has the same standing A7 had for mt5: it has never run recurrently.
Before the composite schedule is documented as activatable, one manual `stitch_composite` enqueue
for the one live deribit composite must be observed to completion. The rule this phase runs on is
that no unproven path gets scheduled. The composite handler is a 20-minute ceiling with per-member
exchange crawls; if it fails or wedges on this account, that is a finding that changes the runbook,
not a detail to discover after activation.

⚠️ Claude cannot do this step — it is a production write consumed by the live worker. Claude has
already done everything automatable: the function and both gates are written and green on TEST.

1. BEFORE: run the staleness view and record the composite row's `computation_status`,
   `last_return_date`, `days_since_last_return` and `is_stale`.
2. Enqueue exactly one job: `SELECT enqueue_compute_job(p_strategy_id := '<the composite id>',
   p_kind := 'stitch_composite', p_metadata := jsonb_build_object('source',
   'ledger-refresh-composite-tracer'));`
3. Watch `compute_jobs` for that strategy until the row reaches a terminal status. Allow 30 minutes
   against the 20-minute handler ceiling.
4. AFTER: re-run the view for that row.

Report counts, dates and statuses only — no ids, no project refs:
  - BEFORE and AFTER `computation_status`, `last_return_date`, `is_stale`
  - the derive job's wall-clock duration
  - any `error_message`, scrubbed
  </instructions>
  <verification>
PASS = `last_return_date` advanced and `is_stale` flipped FALSE with a status in the success pair.
Then, and only then, add the composite section to `docs/runbooks/ledger-refresh-go-live.md`: its own
registration statement naming a distinct job name and a minute offset that does not collide with
the single-key job's tick, its own verification query, its own unschedule rollback, and a note that
the shared activation setting kills both arms while unscheduling kills one.

FAIL = anything else, including a green job whose `last_return_date` did not move. Do NOT write the
runbook section. Record the failure with its error, leave the composite function dormant and
unscheduled, and file the finding in TODOS.md — deribit then ships at zero live coverage this phase
by measurement rather than by omission, which is a materially different and honest outcome.
  </verification>
  <resume-signal>Report the BEFORE/AFTER values and type "COMPOSITE PASS" or "COMPOSITE FAIL" with the observed status and scrubbed error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| definer function → `compute_jobs` | A second cross-tenant enqueue surface |
| composite handler → member venues | The composite path performs per-member exchange crawls with decrypted member keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161.1-15 | Elevation of Privilege | `enqueue_ledger_composite_refresh` | critical | mitigate | Zero parameters, pinned search_path, REVOKE ALL from PUBLIC/anon/authenticated, all three asserted by the self-verify block |
| T-161.1-16 | Denial of Service | 20-minute composite jobs on the shared worker | high | mitigate | Separate derived per-tick bound (at most 3 at an hourly tick), separate advisory lock, attempt cooldown, staleness gate; separate schedule so it can be unregistered without touching the single-key arm |
| T-161.1-17 | Tampering | mt5 composites entering the composite crawl | high | mitigate | Membership-level exclusion (D-13) pinned by two dedicated test arms, both proven RED |
| T-161.1-18 | Tampering | schedule smuggled into this migration | critical | mitigate | The static gate is extended to scan this file's raw text for both registration verbs |
| T-161.1-19 | Information Disclosure | tracer evidence in a public repo | high | mitigate | The checkpoint's report contract admits counts, dates and statuses only |
| T-161.1-SC | Tampering | npm/pip/cargo installs | high | accept | Zero packages installed by this plan |
</threat_model>

<verification>
- Both SQL files and the Python gate run green, with assertions EXECUTED not skipped.
- `cd analytics-service &amp;&amp; python3 -m mypy --strict tests/test_ledger_refresh_gates.py` is clean.
- Three neuterings in task 2 observed RED and restored byte-identical.
- Task 3 reports PASS before any composite runbook section is written.
- Full suite before the wave closes: `cd analytics-service &amp;&amp; python3 -m pytest` and `npm run test`.
</verification>

<success_criteria>
- LEDGER-01 covers all three ledger venues in substance, not only in set membership: deribit's live composite has a real recurring path.
- The MT5 composite deferral is implemented as a visible, tested skip rather than an absence.
- Both arms remain dormant at merge and share one kill switch.
</success_criteria>

<output>
Create `.planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-04-SUMMARY.md` when done.
If D-COMP resolved to (b), the SUMMARY records that this plan closed unexecuted by decision, and
names where the zero-coverage fact is written down.
</output>
