---
phase: 161.1-ledger-refresh
verified: 2026-08-28T00:00:00Z
status: human_needed
score: 4/4 requirements verified (LEDGER-01..04), shipped v0.73.0.0
verification_kind: retrospective
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "ACTIVATION — register the schedule for enqueue_ledger_refresh_for_strategies (and the composite arm) on PROD and confirm ledger-backed strategy_analytics starts advancing."
    expected: "mt5 (and any future sfox/deribit) strategies show strategy_analytics.computed_at advancing on the chosen cadence, closing the gap against the ccxt venues."
    why_human: "The phase shipped DORMANT on purpose (LEDGER-02). Activation is a founder-gated LIVE OP on production scheduling — an agent must not register a recurring compute job."
    blocked: "FOUNDER-GATED BY DESIGN, and the dormancy is CONFIRMED LIVE 2026-08-28. `cron.job` on PROD holds 13 active jobs and NOT ONE calls `enqueue_ledger_refresh_for_strategies` or `enqueue_ledger_composite_refresh`. That is the designed end state of this phase, not an omission. The gap it exists to close is still open and still measurable: joined through api_keys, mt5 strategies (4) carry a newest `strategy_analytics.computed_at` of 2026-08-25 09:49Z — 81.7 hours old, oldest 2026-08-04 — while okx strategies (5) sit at 5.4 hours, newest today. Same asymmetry the ROADMAP measured on 2026-08-24. ⚠️ Read `strategy_analytics.computed_at`, NEVER `api_keys.last_sync_at`: the mt5 keys report a recent last_sync_at while their analytics are four days stale, which is exactly the lie this phase was written against. No sfox or deribit strategy exists on PROD, so those two arms have no live population to observe either way."
---

# Phase 161.1: LEDGER-REFRESH — Verification Report

**Phase Goal:** Every ledger-backed venue (mt5/sfox/deribit) has a recurring path that refreshes
`strategy_analytics` after onboarding — shipped DORMANT, with activation a founder-gated live op.
**Shipped:** v0.73.0.0
**Status:** human_needed — all four requirements met; the single remaining item is the founder-gated activation.

⚠️ **This is a RETROSPECTIVE verification, written 2026-08-28.** The phase shipped without a
`VERIFICATION.md` at all, so `roadmap.cjs` could never report it complete no matter what landed —
`isPhaseComplete()` reads this file's `status:` and nothing else. Every claim below was measured at
HEAD or against the production database on the date written, not reconstructed from the SUMMARYs.

## Requirements

| ID | Requirement | Status | Evidence measured 2026-08-28 |
|---|---|---|---|
| LEDGER-01 | A recurring enqueuer reaches `strategy_analytics` for every ledger-backed venue | ✓ | `public.enqueue_ledger_refresh_for_strategies` is LIVE on PROD, `prosecdef = true` |
| LEDGER-02 | Ships DORMANT behind two locks, no schedule registered | ✓ | `cron.job` holds 13 active jobs; NONE calls either ledger enqueuer |
| LEDGER-03 | Staleness observable on a timestamp that advances only when new data lands | ✓ | `public.ledger_refresh_parse_series_date` live; the mt5-vs-okx spread below is read off `strategy_analytics.computed_at`, and it does not move |
| LEDGER-04 | A regression pin fails if any ledger venue is dropped from the refresh set | ✓ | `supabase/tests/test_ledger_refresh_composite_arm.sql` present at HEAD |

All four are `- [x]` in `.planning/REQUIREMENTS.md` (:100-103) with `Complete` traceability rows
(:201-204).

## Plans

5 of 5 plans have SUMMARYs. Four read `status: complete`; **161.1-04 reads
`status: awaiting-checkpoint`** — a stale marker, not unfinished work. Its two named deliverables
both exist and are deployed: `supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql`
and `supabase/tests/test_ledger_refresh_composite_arm.sql` at HEAD, and
`public.enqueue_ledger_composite_refresh()` live on PROD as SECURITY DEFINER.

⚠️ **Ledger drift corrected alongside this file:** the ROADMAP phase block read
`**Plans:** 3/5 plans executed` while the progress table in the same file read `5/5 | Complete |
v0.73.0.0`. 5/5 is the true figure.

## The gap this phase exists to close is still open — by design

| venue | strategies | newest `strategy_analytics.computed_at` | age |
|---|---|---|---|
| okx (ccxt) | 5 | 2026-08-28 14:06Z | **5.4 h** |
| mt5 (ledger) | 4 | 2026-08-25 09:49Z | **81.7 h** |

The machinery is built and deployed; nothing is scheduled to call it. Flipping that is the
founder's live op, recorded as the one human item above.
