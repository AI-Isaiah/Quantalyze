---
phase: 161.1-ledger-refresh
plan: 05
type: execute
wave: 3
depends_on: ["161.1-01", "161.1-02"]
files_modified:
  - analytics-service/tests/test_ledger_refresh_gates.py
autonomous: true
requirements: [LEDGER-02, LEDGER-04]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Dropping any venue from the refresh set turns a test RED, proven by neutering."
    - "The phase's migrations register no schedule anywhere in the repo, and the check does not depend on enumerating filenames."
    - "A broken region extraction fails the anti-vacuity floor instead of turning every negative assertion green — for the view body, the is_stale expression, the function body and the Python guard region alike."
    - "The refresh marker literal cannot drift between the SQL that writes it and the Python that reads it."
  artifacts:
    - analytics-service/tests/test_ledger_refresh_gates.py
  key_links:
    - "Sliced out of 161.1-02 (checker finding W-9). File-disjoint from plans 01-03; reads only artifacts plans 01 and 02 produce, so it must run AFTER wave 2."
    - "Plan 04 (wave 4) EXTENDS this same file with the composite-arm gate. This plan creates it; plan 04 must not re-create it."
---

<objective>
Pin, statically, the four things about this phase that no behavioural test can reach: that the venue
set cannot drift from its Python authority, that no schedule has been smuggled into a migration,
that the bound literals and the fail-closed comparison are still what D-09 and D-08 derived, and
that the cross-language literals introduced in plan 02 (the refresh marker, the terminal-success
status set) cannot drift apart.

Purpose: every assertion in this file is an ABSENCE or an equality against an extracted region, and
both are green over an empty derivation. The anti-vacuity floor is therefore not decoration here —
it is the majority of the work.

Output: one pytest file under `analytics-service/tests/`. It reads files. It starts no service and
touches no database.

⛔ Sliced out of `161.1-02-PLAN.md` per checker finding W-9 (that plan was over budget at 95 k with
seven neuter/restore cycles). Nothing in this plan touches SQL, the worker, or the runbook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-CONTEXT.md
@.planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-01-SUMMARY.md
@.planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-02-SUMMARY.md

Source files the task reads (each once):
@analytics-service/tests/test_main_worker.py
</context>

<planning_decisions>
**D-17 — why the SQL-side single-source gate is load-bearing, stated on a TRUE premise.**
This is plan 02's D-16, restated here because gate 2's failure message is where the wrong version
was going to be written down.

⚠️ ROADMAP:262-263 and CONTEXT's carried-constraints bullet both say
`src/lib/strategyGate.invariant.test.ts` "BANS venue literals in TS". **Measured at HEAD that is
false as written:** `:64` scopes `BANNED_VENUE_LITERALS` to `GATE_PATH = src/lib/strategyGate.ts`
and nothing else, so a venue set added to `src/lib/closed-sets.ts` would not trip it. The
no-TS-mirror rejection still stands, on two claims that ARE true:

  1. the ROADMAP's own fence — `_LEDGER_BACKED_SOURCES` (`long_fetch.py:63`) is the sole authority,
     and a Vercel-cron / TS implementation is ruled out unless a drift gate is explicitly accepted;
  2. the measured incident — a hand-copied mirror drifted to one venue while Python held three and
     cost a funded MT5 account its publish path.

⛔ Gate 2's failure message cites (1) and (2). It must NOT claim the TS invariant test is a
repo-wide venue-literal ban. A gate whose failure message asserts something falsifiable-and-false
teaches the next reader a wrong rule, and the next reader will act on it.

**D-18 — dormancy is checked by GLOB plus a repo-wide symbol count, never by enumerating two
filenames.** An earlier draft named the two migration files explicitly. A third migration added
later — by this phase's own plan 04, or by anyone — could register a schedule and the gate would
pass. Two complementary scans replace the enumeration (gate 3a/3b), and each carries its own
anti-vacuity floor, because a glob that matches nothing and a symbol search that finds nothing both
look exactly like success.
</planning_decisions>

<tasks>

<task type="auto">
  <name>Task 1: The static gate file — drift, dormancy, bounds, and the cross-language literals</name>
  <files>analytics-service/tests/test_ledger_refresh_gates.py</files>
  <read_first>
    analytics-service/tests/test_main_worker.py lines 2260-2350 — the in-repo cross-language drift-gate precedent: the migration-name module constant with its pointer-hygiene comment, the region-scoped body extraction (NOT a whole-file grep, precisely because prose comments trip one), and the anti-vacuity guard that stops a broken extraction from making every negative assertion pass.
    analytics-service/tests/test_mt5_derive_branch.py around line 675 — the existing import of the ledger source set from long_fetch, proving the import works under this suite.
    The plan 01 and plan 02 SUMMARYs — for the two migration filenames, the exact activation setting name, the exact `metadata->>'source'` marker literal, and the name of the terminal-success `frozenset` added to `job_worker.py`. Take these from the SUMMARYs, not from memory.
  </read_first>
  <action>
Create the Python gate file. It runs under pytest from `analytics-service/` only.

Module constants, each with the pointer-hygiene comment the precedent carries — "this name and the
migration move together in the same commit; a stale pointer keeps the gate green while it guards a
body nothing runs":
  - the plan-01 view migration filename
  - the plan-02 fan-out migration filename

Helpers: resolve each path from the test file's location up to the repo root (mirror the
precedent's `parents[2]` resolution); read the text; extract the region each assertion is about
rather than scanning whole files.

Region extraction, load-bearing — FOUR regions:
  - the VIEW BODY = the text of the CREATE …VIEW statement only, up to its terminating semicolon.
  - the IS_STALE EXPRESSION = the sub-region of the view body defining that column. The view
    legitimately EXPOSES the two rejected timestamps as informational columns with explanatory
    comments (plan 01 D-03), so a whole-body negative grep would trip on the documentation. Scope
    the negative assertions to this expression. ⛔ Keep this region-scoping; it is not an
    over-complication, it is what stops a false positive on the view's own prose.
  - the FUNCTION BODY = the text between the fan-out function's dollar-quote delimiters.
  - the GUARD REGION = in `analytics-service/services/job_worker.py`, the text of the
    `_stamp_strategy_analytics_failed` closure, from its `async def` line to the start of the next
    sibling definition at the same indentation. Gates 8 and 9 read this.

**Anti-vacuity floor, before any assertion, applied to ALL FOUR regions equally.** Assert each file
exists; assert each extracted region is longer than a hand-typed minimum character count; assert
each region contains a hand-typed sentinel substring proving the extraction landed. ⛔ The IS_STALE
EXPRESSION gets its OWN length floor and its OWN sentinel — it is a separately-extracted third
region, not a slice of an already-floored one, and gate 6's negative assertions run against it. Do
not let it ride on gate 6's positive assertion for its validity: a positive and a negative
assertion over the same empty string disagree by luck, not by construction. Sentinels: the view
name in the view body; the returns-series column name in the is_stale expression; the enqueue call
in the function body; the helper's own name in the guard region.

Gates:

1. LEDGER-04 DRIFT. Extract the venue literal list from the view body's ledger venue array and
   assert set-equality with `_LEDGER_BACKED_SOURCES` imported from
   `services.ingestion.long_fetch`. Assert the extracted set has exactly three members, so an
   extraction returning one venue cannot pass. Failure message must say: change the Python constant
   FIRST, then this migration.
2. SINGLE SOURCE (plan 01 D-05). Assert the FUNCTION BODY contains none of the six venue names. A
   second venue literal is a second drift surface, and this repo has already paid for one. ⛔ Write
   the failure message from D-17: cite the ROADMAP fence (`_LEDGER_BACKED_SOURCES` is the sole
   authority; a TS implementation is out unless a drift gate is explicitly accepted) and the
   measured incident (a hand-copied mirror drifted to one venue while Python held three and cost a
   funded MT5 account its publish path). Do NOT write that `strategyGate.invariant.test.ts` bans
   venue literals in TS — measured, it bans them in `src/lib/strategyGate.ts` only (`:64`).
3. LEDGER-02 DORMANCY, in two complementary scans (D-18) — ⛔ do NOT enumerate migration filenames.
   3a. GLOB `supabase/migrations/*ledger_refresh*.sql`. Assert the glob matched at least the two
       known files (an empty or one-file glob makes the following assertion vacuous — fail on the
       count first). Assert none of the matched files' RAW TEXT — comments included — contains
       either pg_cron registration verb qualified by its schema. A third ledger-refresh migration
       added later is covered automatically; that is the point of the glob.
   3b. REPO-WIDE SYMBOL COUNT, which closes the hole 3a still leaves (a schedule registered from a
       migration whose filename lacks the phase token). Count the files under
       `supabase/migrations/` whose text contains the fan-out function's name. Assert the count is
       EXACTLY 1 — the migration that defines it. Exactly, not "at most": a 0 means the search is
       broken, and reporting that as success is the failure mode this whole file exists to prevent.
       Any second occurrence is either a schedule registration or a call site, and this phase
       forbids both from a migration.
   Build every search token by concatenating two string fragments at runtime so this gate file
   cannot match itself when the repo is scanned, and comment that trick so nobody "cleans it up".
   ⛔ Keep the trick — it is correct and it is load-bearing. Failure messages must name the
   WORKER-03 rule and the runbook that owns activation.
4. LEDGER-02 LOCK B. Assert the function body reads the activation setting name and that its
   comparison is against the exact lowercase true string — a truthiness check would open the flag
   on `1` and `on`.
5. LEDGER-04 BOUNDS. Assert the function body contains a per-tick LIMIT whose integer is at most 4,
   a per-venue rank cap whose integer is at most 2, and a partition-by window clause. ⛔ Also assert
   the LIMIT is STRICTLY GREATER than the cap — at LIMIT = cap, plan 02's task 2 arm G stops
   discriminating the cap from the limit and its neutering goes green (D-09). Each failure message
   quotes D-09's CORRECTED derivation: one refreshed strategy costs up to 1500 s, not 900 s,
   because `derive_broker_dailies` auto-chains to `compute_analytics_from_csv` on the same
   sequentially-dispatching worker; the binding constraint is the 20-hour attempt cooldown, not
   this LIMIT; the LIMIT is a burst cap. A future editor raising the number has to re-derive it,
   and must not re-derive it from the retracted one-hop arithmetic.
6. CRITERION 3 COLUMN. Assert the IS_STALE EXPRESSION references the returns-series column and
   references neither of the two rejected timestamp columns, and that the view body's status
   comparison admits BOTH success values (assert the with-warnings value appears in the status
   predicate). Failure message cites the reaper migration's own warning and plan 01 D-04's census.
7. SECDEF HYGIENE. Assert the function body's declaration pins `search_path` and that the migration
   revokes EXECUTE from anon and authenticated.
8. MARKER DRIFT (plan 02 D-15). Extract the `'source'` value the fan-out writes into
   `p_metadata` from the FUNCTION BODY, extract the literal the guard compares against from the
   GUARD REGION, and assert string equality. These are the two ends of a cross-language contract
   with no compiler between them: if they drift, the fan-out still enqueues and the guard still
   compiles, and the only symptom is that the next failed refresh silently un-publishes a funded
   account. Failure message must say exactly that.
9. SUCCESS-SET DRIFT (plan 02 D-15, plan 01 D-04). Import the terminal-success `frozenset` from
   `services.job_worker`, extract the status success set from the VIEW BODY, and assert
   set-equality. Assert both have exactly two members. The `complete`-only mistake is the one
   CONTEXT's census singles out — all 5 live ledger rows are `complete_with_warnings` — and it
   would silently disarm the guard on every account it exists to protect.

Run `mypy --strict` over the new file before considering the task done — this repo's analytics
service is strict-typed and the milestone pytest run does not cover it.

⚠️ Plan 04 (wave 4) EXTENDS this file with the composite-arm gate. Leave the module constants and
the region-extraction helpers factored so a tenth gate can be appended without re-deriving them.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_ledger_refresh_gates.py -x -q</automated>
    <human-check>RED-proof, mandatory: (1) delete one venue from the view's ledger array — gate 1 MUST fail; restore byte-identical. (2) rename the IS_STALE sentinel so THAT region alone comes back empty — the anti-vacuity floor MUST fail rather than gate 6 silently passing; restore byte-identical. (3) raise the per-tick LIMIT integer to 25 — gate 5 MUST fail; restore byte-identical. (4) point the is_stale expression at the analytics timestamp column — gate 6 MUST fail; restore byte-identical. (5) change one character of the marker literal in the migration — gate 8 MUST fail; restore byte-identical. Record all five observations and their restorations in the SUMMARY.</human-check>
  </verify>
  <done>All nine gates pass, `mypy --strict` is clean on the new file, and the five neuterings were each observed RED first-hand and restored byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merge to main → PROD | `supabase/migrations/**` auto-applies on merge; a schedule reaching a migration reaches production |
| Python authority → SQL copy | The venue set, the refresh marker and the success-status set each exist in two languages with no compiler between them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161.1-22 | Tampering | schedule smuggled into a migration | critical | mitigate | Gate 3a globs every `*ledger_refresh*` migration and scans raw text for both registration verbs; gate 3b asserts the fan-out function's name appears in EXACTLY one migration file, closing the differently-named-migration hole that filename enumeration leaves |
| T-161.1-23 | Tampering | venue set, refresh marker or success-status set drifting from its authority | high | mitigate | Gates 1, 2, 8 and 9: set-equality and string-equality against the Python constants, all four behind the anti-vacuity floor |
| T-161.1-24 | Repudiation | a gate that passes over an empty extraction | high | mitigate | Length floor plus a hand-typed sentinel on ALL FOUR regions independently, including the separately-extracted IS_STALE expression; neutering (2) proves the floor fires |
| T-161.1-SC | Tampering | npm/pip/cargo installs | high | accept | Zero packages installed by this plan |
</threat_model>

<verification>
- `cd analytics-service && python3 -m pytest tests/test_ledger_refresh_gates.py -x -q` passes with all nine gates EXECUTED.
- `cd analytics-service && python3 -m mypy --strict tests/test_ledger_refresh_gates.py` is clean.
- Five neuterings each observed RED and restored byte-identical.
- Full-suite sanity before the wave closes: `cd analytics-service && python3 -m pytest`.
</verification>

<success_criteria>
- LEDGER-04: dropping a venue from the refresh set is RED, observed by neutering, behind an anti-vacuity floor.
- LEDGER-02: no migration in the repo registers a schedule for this fan-out, checked without enumerating filenames.
- The refresh marker and the terminal-success status set cannot drift between SQL and Python.
- No gate in this file can pass over an empty region extraction.
</success_criteria>

<output>
Create `.planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-05-SUMMARY.md` when done.
Record the five RED observations and their restorations, and the four sentinel strings chosen (plan
04 appends a tenth gate to this file and needs them).
</output>
