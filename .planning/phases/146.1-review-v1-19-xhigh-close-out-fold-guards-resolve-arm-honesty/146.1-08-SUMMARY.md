# Plan 146.1-08 — SUMMARY (pre-merge gate: census + TEST rehearsal)

**Status: census CLEAR, TEST rehearsal PASSED, both guards VERIFIED BEHAVIOURALLY on the
deployed objects. The merge checkpoint is OPEN and is the founder's.**

## Census checkpoint → `census-clear` (orchestrator, Supabase MCP, 2026-08-18)

**PROD `khslejtfbuezsmvmtsdn` — read-only, as fenced:**

| Read | Result |
|---|---|
| tip `max(version)` | `20260819120000` — UNMOVED. Both new stamps sort above it; the backdated guard will not trip. |
| `orphaned_running_reaped:%` count | **0** — plan 03's dated ZERO claim HOLDS. No prose correction required. |
| status census | 1,658 jobs: 1,536 `done`, 122 `failed_final`, **0 pending, 0 running** |

**TEST `qmnijlgmdhviwzwfyzlc`:**

| Read | Result |
|---|---|
| fold present | 1 — `20260819120000` IS applied (research assumption A2 CONFIRMED, not assumed) |
| `orphaned_running_reaped:%` count | **602** — the terminalizer HAS fired on TEST |
| sweep cron, pre-change | jobid **18**, `35 * * * *` |

⭐ **RESEARCH §4.7's hazard was MEASURED AND DOES NOT EXIST — and the research estimate was
wrong.** §4.7 warned that terminalized TEST rows could make "a few hundred TEST strategies
sweep-eligible at ≤25/hour", feeding the 05:30 UTC shared-DB flake. Taken at face value the 602
looked worse than its ~396 estimate. Simulating the actual predicate, both arms:

- eligible under the OLD bare zero-jobs conjunct: **0**
- eligible under the NEW marker-exempt conjunct: **0**
- **delta: 0**

Two independent reasons, each sufficient: of TEST's 8,265 strategies **exactly one** has any
`csv_daily_returns` row (and it already carries terminal analytics, so it is excluded anyway);
and **596 of the 602** marked jobs have `strategy_id IS NULL`, while the sweep joins on
`cj.strategy_id = s.id` — a null-strategy job can never make a strategy eligible. Only **6
distinct strategies** are marked at all.

⚠️ Pre-existing and NOT caused by this work: TEST holds 2,579 `pending` compute_jobs (the known
no-worker backlog) and 1 `running`.

## Rehearsal on TEST — both migrations applied, both gates run

| Step | Result |
|---|---|
| `20260819130000` apply | **PASS** — STEP-0 pre-flight and STEP-4 self-verify (44 RAISE sites) both silent. A self-verify that RAISEs is the migration refusing itself; neither did. |
| `20260819130500` apply | **PASS** — STEP-2 self-verify (25 RAISE sites) silent. |
| ledger drift | MCP `apply_migration` stamped `20260818211853` / `20260818212435`; both reconciled to the filename versions. The `name` column was also normalised to the CLI-derived form so TEST matches what PROD will receive on merge. |
| both gate files | Ran to completion, no exception. ⚠️ Evidence is a trailing sentinel SELECT, NOT the notice text — see the caveat below. |
| sweep cron | jobid **18 → 20**, OBSERVED not assumed (unschedule+schedule drops and re-inserts the row). |

⚠️ **HONEST CAVEAT ON THE GATE FILES.** The Supabase MCP transport **discards `RAISE NOTICE`
and `RAISE WARNING`** — proved, not assumed (a probe raising a known literal returned nothing).
So the gate files' own `ALL PASS` notices could NOT be read. What IS established: a raised
exception aborts the whole multi-statement call (the same semantics as `ON_ERROR_STOP=1`), so a
trailing sentinel returning proves execution passed the file's final statement without raising.
Both files were re-run byte-exact against the repo (md5-verified) after a first run had abridged
some comment blocks. ⭐ For the notices to be quoted literally these gates need a psql run —
which is exactly how CI runs them (`.github/workflows/ci.yml:1032`), and that run is part of the
merge checkpoint's green-CI requirement. Note gate 2 has no literal `ALL PASS` string; its
terminal notice is `Part 4 OK: the per-tick bound holds …`.

## ⭐ Orchestrator's OWN behavioural verification (the part that is not taken on trust)

The rehearsal above proves the files *ran*. This proves the guards *work*, executed by the
orchestrator directly against the DEPLOYED function on TEST, under a real
`request.jwt.claims` session (without one the SECDEF auth guard 42501s first — itself a
correct result, and the reason A1 is direct-RPC-only):

| Probe | SQLSTATE | Message |
|---|---|---|
| `p_rows := NULL` | **22023** | `p_rows is required (got NULL)` |
| `p_rows := '[]'`, fmt `daily_returns` | **22023** | `p_rows is empty and fmt daily_returns accept…` |
| `daily_return = 10.5` | **22023** | unusable element — ⭐ **this is Part 6j**, the near-boundary probe the plan-checker forced in; without it a widening to `BETWEEN -10 AND 100` is invisible |
| `daily_return = "NaN"` | **22023** | unusable element (NaN passes ANY `BETWEEN`, which is why 10.5 is needed as a second probe) |
| `daily_return = "Infinity"` | **22023** | unusable element |
| `date = '9999-12-31'` | **22023** | unusable element |
| duplicate dates | **22023** | `p_rows contains duplicate dates - a track re…` |
| **CONTROL `daily_return = 9.9`** | — | **COMMITTED** — proves the guard DISCRIMINATES rather than refusing everything |

⛔ The control committed a real row on shared TEST; the orchestrator DELETED it immediately
(1 strategy + 1 verification + 1 daily; zero compute_jobs touched). TEST is clean.

**B4 verified against the authoritative oracle — the DEPLOYED `cron.job.command`, not the file:**
marker predicate PRESENT; blanket `status IN ('pending'…)` ABSENT; blanket
`status NOT IN ('failed_final'|'done'…)` ABSENT; `LIMIT 25` per-tick bound INTACT; job active.

## What is still NOT proven

- ⛔ **Nothing has been applied to PROD.** Both migrations auto-apply on merge; that is the
  merge checkpoint's whole purpose.
- The `B4-BLANKET` neuter (arms C2 **and** C3 must BOTH redden under a blanket widening) was not
  run — it requires mutating the deployed body on TEST. The narrow predicate is verified present
  and the blanket absent, which is the property that matters at merge time.
- Gate notice text unread (transport limitation above); CI's psql run is the closing evidence.

## Merge checkpoint — OPEN, founder's call

Requires: green CI on the PR **including `sql-tests` and `migration-policy`**, then explicit
approval. On merge both migrations auto-apply to PROD, after which the post-merge task re-counts
`orphaned_running_reaped:%` on PROD and compares it to the census above.
