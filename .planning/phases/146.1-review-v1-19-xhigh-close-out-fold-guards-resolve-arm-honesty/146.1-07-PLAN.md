---
phase: 146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty
plan: 07
type: execute
wave: 5
depends_on: ["146.1-05", "146.1-06"]
files_modified:
  - src/lib/database.types.ts
  - src/app/api/strategies/csv-finalize/route.ts
  - src/__tests__/audit-coverage.test.ts
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/lib/wizard/localStorage.ts
  - src/lib/wizard/localStorage.test.ts
  - analytics-service/routers/process_key.py
  - TODOS.md
autonomous: false
requirements: [C4]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`supabase.rpc(\"finalize_csv_strategy_with_returns\", …)` is called through the generated types with NO cast-through-unknown, and the audit-coverage law SEES that call site and is satisfied by an emission or an explicit pragma — never by invisibility (C4)."
    - "A burned csv-submit signature survives a page reload: the wizard does not re-issue a submission the server already refused, even across a remount (C4/RT-3)."
    - "A `flow_type='csv'` + `step='finalize'` request to the Python service returns a 422 whose message states that CSV finalize moved to the Next.js route — with NO new error code entering the WIZFORM-02 population (C4)."
    - "Every stale comment in the v1.19 batch states something a grep can confirm at HEAD."
  artifacts:
    - src/lib/database.types.ts
    - TODOS.md
  key_links:
    - "The cast deletion and the audit pragma are COUPLED. Removing the cast makes the `\\.rpc\\(` anchor in audit-coverage.test.ts see a mutating RPC name that is currently exempt only because it is invisible. They MUST land in one commit."
    - "`src/lib/database.types.ts` is GENERATED by `npx supabase gen types typescript` against the LIVE remote schema — never hand-edit it. The fold function already exists on PROD (shipped in 20260819120000), and 146.1-01 changes only its body, not its signature, so the generated entry is identical either way."
    - "RT-3 is defense in depth. The OPERATIVE fence is the server-side equality refusal at route.ts:820-863, shipped 2026-08-18. RT-3 must not be described as the thing that closes the hole."
    - "The Python tombstone is MESSAGE-ONLY. A new error code must enter the WIZFORM-02 coverage-law population, and that class is recorded OPEN — server-classified codes still render as UNKNOWN."
---

<objective>
Land the **C4** residue that could not ride any earlier plan, in dependency order.

1. **Types regen + cast deletion + audit pragma (COUPLED).** `route.ts:550-570` invokes the fold
   through `(supabase.rpc as unknown as (...) => ...)`. That cast exists because
   `src/lib/database.types.ts` predates the fold. It also, as a side effect, makes the call
   INVISIBLE to `audit-coverage.test.ts`'s `\.rpc\(` anchor — the RPC name sits in
   `MUTATING_RPC_NAMES` (`:216`) with a comment saying so explicitly. Regenerating the types and
   deleting the cast makes the call visible, and it then falls under the audit law. ⛔ These are one
   change, not two.
2. **RT-3** — persist the burned csv-submit signature in the signed `saveWizardState` envelope.
   `WizardClient.tsx:352` holds it in a `useRef` that a reload destroys.
3. **Python tombstone** — a `flow_type='csv'` + `step='finalize'` request falls through to a 422
   `MISSING_STRATEGY_ID` whose message is about a missing `strategy_id`, which is not why it was
   refused.
4. **Stale-comment batch** — grouped into ONE commit at the end, per the standing rule that prose
   and citations are never blocking.

Purpose: these are the items that were correctly deferred out of every other plan because they are
coupled to something that had to land first. Shipping them last is the design, not an afterthought.
Output: generated types in step with the schema, one fewer type-system escape hatch, a wizard fence
that survives a reload, and a Python refusal that says what it means.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-CONTEXT.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-RESEARCH.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-PATTERNS.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-05-SUMMARY.md

@src/__tests__/audit-coverage.test.ts
@src/lib/wizard/localStorage.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: confirm a credentialled `supabase gen types` run is possible before task 1</name>
  <what-built>Nothing yet. This checkpoint gates task 1, which needs a credentialled `npx supabase gen types typescript` run against the LIVE remote schema. That requires a Supabase access token and a project ref, which a GSD executor session does not have and must not be given.</what-built>
  <how-to-verify>
    In the ORCHESTRATOR session (not a worktree agent), confirm you can regenerate types:

    1. `npx supabase --version` succeeds.
    2. A Supabase access token is present in the environment (do NOT echo it).
    3. `select proname from pg_proc where proname = 'finalize_csv_strategy_with_returns'` returns
       one row on the project you will generate against — the fold shipped in `20260819120000`, so
       it should already exist. If it does not, STOP: the types regen would produce a file that
       omits the function and the cast deletion in task 1 would not typecheck.

    Then either:
    - **(a)** run the regen yourself and hand the regenerated `src/lib/database.types.ts` to the
      executor, or
    - **(b)** confirm the executor session has the credentials and let it run the regen inline.

    ⚠️ If NEITHER is possible, do not skip task 1 and do not hand-edit the generated file. Instead
    defer tasks 1 only, record the deferral in `TODOS.md` with this plan's name and the exact
    blocker, and let tasks 2-4 proceed. A hand-edited generated file is silently overwritten by the
    next regeneration and is the documented drift hazard this file's own header warns about.
  </how-to-verify>
  <resume-signal>Reply "regen-ready-a", "regen-ready-b", or "regen-blocked" (which authorises deferring task 1 ONLY, with the TODOS entry).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Regenerate database types, delete the cast-through-unknown, and satisfy the audit law in one commit</name>
  <files>src/lib/database.types.ts, src/app/api/strategies/csv-finalize/route.ts, src/__tests__/audit-coverage.test.ts</files>
  <precondition>The checkpoint above resolved to `regen-ready-a` or `regen-ready-b`. On `regen-blocked` this task is deferred with a TODOS entry and MUST NOT be attempted by hand-editing `src/lib/database.types.ts`.</precondition>
  <precondition>`node_modules/.bin/vitest` and `node_modules/.bin/tsc` exist. GSD worktree agents get no node_modules; `npx tsc` resolves a decoy package and would report a meaningless green. Run `npm ci` or HALT.</precondition>
  <read_first>
    - `src/lib/database.types.ts:1-20` — the GENERATED-FILE header, the regeneration command, and the NUMERIC-precision audit note. Read it before running anything: this file has a documented precision hazard and its own review history.
    - `src/app/api/strategies/csv-finalize/route.ts:540-575` — the cast and the six-argument shape it hand-declares.
    - `src/__tests__/audit-coverage.test.ts:205-235` — `MUTATING_RPC_NAMES` with the fold's entry at `:216` and the comment explaining that the cast hides the call from the `\.rpc\(` anchor; `MUTATING_RPC_RE` at `:230-232`; and the brace-balance walk plus the `@audit-skip:` pragma window (8 lines above the chain start).
  </read_first>
  <action>
Regenerate `src/lib/database.types.ts` with the command named in its own header
(`npx supabase gen types typescript` against the live remote schema). ⛔ **Never hand-edit it** —
the header states that the next regeneration silently overwrites hand-amended rows.

⚠️ **Diff-review the regenerated file before accepting it.** `docs/deferred-findings.md:186`
records that this generator's output false-positives on a naive diff. Expect churn beyond the fold
function. For every change outside `finalize_csv_strategy_with_returns`, decide whether it is
generator noise or a real schema drift you did not intend to ship, and say which in the SUMMARY. If
a NUMERIC column newly appears as `number`, re-read the audit note in the header before accepting.

Then delete the cast in `route.ts:550-570` and call `supabase.rpc("finalize_csv_strategy_with_returns", {...})`
through the generated types. The six argument names and types must match the generated signature
exactly; if they do not, the generated file is authoritative and the hand-declared shape was wrong —
which is precisely the drift the cast was hiding.

⛔ **The audit consequence lands in the SAME commit.** Removing the cast makes `MUTATING_RPC_RE`
match this call site, and `audit-coverage.test.ts` then requires `logAuditEvent(` or
`logAuditEventAsUser(` inside the ENCLOSING function body — a brace-balance walk — or an
`@audit-skip:` pragma within 8 lines above the chain start. Choose deliberately:
- If this mutation SHOULD be audited, emit the audit event. That is the better answer and it is
  what the audit law is for.
- If it should not, add an `@audit-skip:` pragma whose reason names WHY (not "not needed") and
  points at where the mutation IS recorded instead.

Whichever you choose, update the `MUTATING_RPC_NAMES` comment at `:210-216` — it currently explains
the entry by saying the cast hides the call, and that explanation stops being true.

⚠️ 146.1-05 collapsed the two handlers, which MOVED enclosing-function boundaries. Re-read the
current file before deciding where the pragma or the emission belongs; anchors in this plan are from
`76adf961`.

⛔ Do NOT remove `finalize_csv_strategy_with_returns` from `MUTATING_RPC_NAMES`. It belongs there
more than ever now that the anchor can see it.

Commit as ONE commit covering the regen, the cast deletion and the audit resolution.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
R=src/app/api/strategies/csv-finalize/route.ts
grep -q 'supabase.rpc as unknown as' "$R" && fail "the cast-through-unknown survives"
grep -q 'finalize_csv_strategy_with_returns' src/lib/database.types.ts || fail "the regenerated types do not contain the fold - regen ran against the wrong project or the function is absent there"
grep -q 'finalize_csv_strategy_with_returns' src/__tests__/audit-coverage.test.ts || fail "the RPC name was removed from MUTATING_RPC_NAMES - it belongs there more than ever"
npx --no-install vitest run src/__tests__/audit-coverage.test.ts src/lib/database.types.test.ts --no-file-parallelism --reporter=basic || fail "audit-coverage or types gate red"
npx --no-install vitest run src/__tests__/csv-finalize-cross-submission-merge.test.ts src/__tests__/csv-validate-route.test.ts src/__tests__/csv-finalize-c14-regression.test.ts --no-file-parallelism --reporter=basic || fail "route suites red"
npm run typecheck || fail "typecheck red - the generated signature and the call site disagree"
npm run lint || fail "lint red"
echo OK
    </automated>
  </verify>
  <reversibility rating="reversible">The cast can be restored in one edit and the generated file regenerated from the same command. The audit decision (emit vs pragma) is the part worth getting right first time — it is recorded in the commit message and the pragma reason.</reversibility>
  <done>`src/lib/database.types.ts` is regenerated by the documented command (never hand-edited) with its diff reviewed and classified; the cast is gone and the call goes through the generated types; `audit-coverage.test.ts` is green with a deliberate emission or a reason-bearing pragma; the `MUTATING_RPC_NAMES` comment tells the truth; one commit covers all three.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (RT-3): persist the burned csv-submit signature in the signed wizard-state envelope</name>
  <files>src/lib/wizard/localStorage.ts, src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx, src/lib/wizard/localStorage.test.ts</files>
  <read_first>
    - `src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx:352` (`failedCsvSubmitSigRef`), `:556` (read), `:561` (clear), `:577` (write), and `csvSubmissionSignature` — the function that derives the signature.
    - `src/lib/wizard/localStorage.ts:31` (the async-signing note), `:240-270` (`saveWizardState` and its warn-on-failure path), and the load/verify side. `src/lib/wizard/localStorage.test.ts:420-445` (the signed-envelope round-trip case) is the shape to extend.
  </read_first>
  <behavior>
    - A submission whose signature the server already refused is BURNED: after a reload/remount, the wizard still knows it was burned and does not silently re-issue the identical submission.
    - The burned signature travels INSIDE the signed payload, so a tampered value fails verification exactly as every other field does. It is not a second, unsigned storage key.
    - A save whose signing fails still warns and does not throw — the existing `saveWizardState` failure contract is unchanged.
    - Round-trip: save with a burned signature → load → the value is present and equal.
    - Round-trip with no burned signature → load → the field is absent/null and nothing downstream treats absence as a burn.
    - Clearing the burn (the existing `:561` path) persists the cleared state, so a legitimately different resubmit is not permanently blocked.
  </behavior>
  <action>
⚠️ **State the priority honestly in every comment you write.** RT-3 is **defense in depth**. The
OPERATIVE fence is the server-side equality refusal at `route.ts:820-863`, shipped 2026-08-18. Do
not describe RT-3 as the thing that closes the hole, and do not weaken the server fence on the
strength of it.

Add the burned signature as a field of the wizard-state payload that `saveWizardState` signs, and
read it back on load. ⛔ Do NOT add a second, separate localStorage key: a value stored outside the
signed envelope is tamperable, and the whole reason this envelope is signed is that its contents
steer behaviour.

In `WizardClient.tsx`, keep the `useRef` as the in-session cache and hydrate it from the loaded
state on mount; write through to the persisted state at the existing set (`:577`) and clear
(`:561`) sites, so the ref and the envelope cannot disagree. ⚠️ `saveWizardState` is async (Web
Crypto signing) — follow the file's existing call convention at each site; do not introduce a
floating promise.

Extend `src/lib/wizard/localStorage.test.ts` beside the existing signed-envelope round-trip case
(`:420-445`) for every behaviour above.

⭐ The oracle is the OBSERVABLE outcome — after a simulated reload, is the identical submission
still refused/suppressed? — never "does the payload contain a field called X". A field that
round-trips but is read by nothing is exactly the wiring-versus-helper failure this repo has
recorded.

Commit as its own commit.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
npx --no-install vitest run src/lib/wizard/localStorage.test.ts --no-file-parallelism --reporter=basic || fail "localStorage suite red"
npx --no-install vitest run src/app/\(dashboard\)/strategies/new/wizard --no-file-parallelism --reporter=basic || true
grep -rn "localStorage.setItem" src/app/\(dashboard\)/strategies/new/wizard/WizardClient.tsx && fail "a raw unsigned localStorage write was added - the value must ride inside the signed envelope"
npm run typecheck || fail "typecheck red"
npm run lint || fail "lint red"
echo OK
    </automated>
  </verify>
  <reversibility rating="reversible">One optional field on an existing signed payload plus two write-through call sites. Removing it restores today's in-memory-only behaviour with no data migration — absent fields already load as absent.</reversibility>
  <done>The burned signature rides inside the signed wizard-state envelope (never a second key), is hydrated on mount and written through at both the set and clear sites, survives a simulated reload, and is asserted by an observable-outcome oracle; `saveWizardState`'s failure contract is unchanged; every comment names the server-side equality refusal as the operative fence.</done>
</task>

<task type="auto">
  <name>Task 3: Python tombstone message (no new code) + the stale-comment batch, one commit each</name>
  <files>analytics-service/routers/process_key.py, TODOS.md</files>
  <read_first>
    - `analytics-service/routers/process_key.py:1119-1147` — the deliberate fall-through comment and the 422 `MISSING_STRATEGY_ID` envelope it lands in.
    - `146.1-RESEARCH.md` §7 (the C4 residue table) and Open Question 5.
  </read_first>
  <action>
**Python tombstone — MESSAGE-ONLY. ⛔ Do NOT mint `CSV_FINALIZE_MOVED` or any other new code.**
A new code must enter the WIZFORM-02 coverage-law population, and that class is recorded OPEN:
server-classified codes still render as `code: UNKNOWN` at the wizard, so a new code would ship
into a known-broken classification path. Keep `MISSING_STRATEGY_ID`; change only the
`human_message` so a `flow_type='csv'` + `step='finalize'` request is told what actually happened —
CSV finalize moved to the Next.js route in migration `20260819120000`, and this service is no longer
a writer for that flow. The surrounding comment at `:1119-1130` already says the fall-through is
deliberate; the user-facing sentence is the part that does not.

⚠️ If the message is branched on `flow_type`/`step`, keep the DEFAULT message byte-identical for
every other caller — a message change that leaks into unrelated 422s is a regression dressed as
copy. Add a test for both arms.

File `CSV_FINALIZE_MOVED` in `TODOS.md` as a NAMED option gated on WIZFORM-02 closing, with the
cost stated (it must enter the coverage-law population and pass the classification path that
currently renders UNKNOWN). An option filed without its gate is an option that gets picked up
prematurely.

**Stale-comment batch — ONE commit, last.** Per the standing stopping rule, prose and citations are
never blocking; they are grouped so they never contend with real work. Work from the v1.19 list in
`TODOS.md`. For EACH comment, before editing: run the grep that would confirm the claim at HEAD. If
the grep confirms it, leave the comment alone and record that you checked. If it does not, correct
the comment to say what the grep shows. ⛔ Never "improve" a comment you did not verify — an
unverified rewrite is how a false claim gets a fresh date and becomes more believable.

⛔ **Scope fence.** Do NOT touch any file owned by 146.1-06 (`src/lib/process-key-client.ts`,
`src/app/api/keys/sync/route.ts`, `src/app/api/verify-strategy/route.ts`,
`src/lib/resilient-fetch.ts`, `src/lib/seam-redaction.ts`,
`analytics-service/services/db.py`) — 146.1-06 already corrected their claims in its own commit and
a second pass would either duplicate or contradict it.

Two commits: the tombstone message, then the comment batch.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
cd analytics-service
grep -q 'CSV_FINALIZE_MOVED' routers/process_key.py && fail "a new code was minted - WIZFORM-02 is OPEN"
python3 -m pytest tests/test_process_key.py tests/test_process_key_200_discriminator.py -q || fail "process_key suites red"
python3 -m mypy --strict --follow-imports=silent services/ routers/ models/ || fail "mypy red"
cd ..
grep -q 'CSV_FINALIZE_MOVED' TODOS.md || fail "the deferred option was not filed"
grep -q 'WIZFORM-02' TODOS.md || fail "the option is filed without its gate"
git diff --name-only HEAD~2 | grep -E 'process-key-client\.ts|keys/sync/route\.ts|verify-strategy/route\.ts|resilient-fetch\.ts|seam-redaction\.ts|services/db\.py' && fail "a 146.1-06-owned file was touched"
npm test || fail "TS suite red"
npm run lint || fail "lint red"
echo OK
    </automated>
  </verify>
  <done>The Python 422 for `flow_type='csv'` + `step='finalize'` says CSV finalize moved to the Next.js route, with `MISSING_STRATEGY_ID` unchanged, the default message byte-identical for every other caller, and both arms tested; `CSV_FINALIZE_MOVED` is filed in TODOS.md gated on WIZFORM-02 with its cost; every comment in the batch was grep-verified before being edited or left alone; no 146.1-06-owned file was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → wizard behaviour | The wizard state steers whether a submission is re-issued; the envelope is signed precisely because its contents are behaviour-bearing and the user's own machine can edit them. |
| generated types → runtime RPC call | The cast currently lets the call site declare its own argument shape, so a schema/route disagreement cannot be caught by the compiler. |
| route mutation → audit log | The audit law's whole population is discovered by a source regex; a call the regex cannot see is an unaudited mutation with a green gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.1-35 | Repudiation | A mutating RPC invisible to the audit-coverage scanner | medium | mitigate | Task 1 removes the cast so the anchor sees the call, then satisfies the law with a deliberate emission or a reason-bearing pragma — never by restoring invisibility. |
| T-146.1-36 | Tampering | Argument-shape drift hidden by a cast-through-unknown | medium | mitigate | The call goes through the generated types; `npm run typecheck` becomes a real check on the six-argument shape. |
| T-146.1-37 | Tampering | A behaviour-bearing value stored outside the signed envelope | medium | mitigate | RT-3's burned signature rides INSIDE the signed payload; `<verify>` grep-refuses a raw `localStorage.setItem` in the wizard client. |
| T-146.1-38 | Repudiation | A refusal message that misstates its own reason | low | mitigate | Message-only tombstone; both branch arms tested so unrelated 422s keep their wording. |
| T-146.1-39 | Repudiation | A new error code entering a known-broken classification path | medium | accept | Explicitly NOT minted. WIZFORM-02 is recorded OPEN; the option is filed in TODOS.md gated on its closure, with the cost stated. |
| T-146.1-40 | Tampering | An unverified comment rewrite giving a false claim a fresh date | low | mitigate | Every comment in the batch is grep-verified at HEAD before being edited or deliberately left alone; the check is recorded. |
| T-146.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed. `npx supabase gen types` in task 1 is a one-shot invocation of the documented generator named in the generated file's own header, not a dependency addition. Package Legitimacy Audit recorded not-applicable in `146.1-RESEARCH.md`. |
</threat_model>

<verification>
1. Each task's `<verify>` exits 0.
2. `npm test` green; `npm run typecheck`, `npm run lint` green.
3. `npm run test:coverage` still clears lines 82 / statements 80 / functions 74 / branches 72 — the blocking `frontend-coverage` CI job.
4. `cd analytics-service && python3 -m pytest` green and `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` green. ⛔ pytest ONLY from `analytics-service/` with `python3` — a repo-root run misses VCR cassettes and makes LIVE broker calls.
5. `npm run schema:functions:check` green (146.1-01's snapshot is still current after everything).
6. If the checkpoint resolved to `regen-blocked`, TODOS.md carries the deferral with this plan's name and the exact blocker, and `src/lib/database.types.ts` is UNCHANGED — not hand-edited.
</verification>

<success_criteria>
- [ ] The checkpoint was answered before task 1 was attempted.
- [ ] `src/lib/database.types.ts` was regenerated by the documented command, never hand-edited, and every out-of-scope diff hunk is classified as generator noise or intended schema drift in the SUMMARY.
- [ ] `route.ts` calls `supabase.rpc("finalize_csv_strategy_with_returns", …)` through the generated types with no cast-through-unknown, and `npm run typecheck` passes on the real signature.
- [ ] `audit-coverage.test.ts` is green via a deliberate audit emission or a reason-bearing `@audit-skip:` pragma; `finalize_csv_strategy_with_returns` is still in `MUTATING_RPC_NAMES`; its explanatory comment is updated.
- [ ] Regen + cast deletion + audit resolution are ONE commit.
- [ ] The burned csv-submit signature persists inside the SIGNED wizard-state envelope, hydrates on mount, is written through at set and clear, and survives a simulated reload — asserted on the observable outcome, not on payload shape.
- [ ] No raw `localStorage.setItem` was added to the wizard client.
- [ ] Every RT-3 comment names the server-side equality refusal (`route.ts:820-863`) as the operative fence.
- [ ] The Python 422 for the csv/finalize combination states that finalize moved; `MISSING_STRATEGY_ID` is unchanged; no new code is minted; the default message is byte-identical for other callers; both arms are tested.
- [ ] `CSV_FINALIZE_MOVED` is filed in TODOS.md as an option GATED on WIZFORM-02 closing, with its cost.
- [ ] Every comment in the stale batch was grep-verified at HEAD before being edited or left alone.
- [ ] No file owned by 146.1-06 appears in this plan's diff.
</success_criteria>

<falsifiability_ledger>
| # | Success criterion | What makes it go RED | Named neuter — RUN IT, then restore |
|---|---|---|---|
| F-07-1 | The audit law now SEES the fold call | Restore the cast | **Neuter C4-RECAST**: re-add `as unknown as`. `audit-coverage.test.ts` must go from RED-without-a-pragma to GREEN — demonstrating that the gate's green today was bought by invisibility, not by compliance. ⚠️ Run this in the OTHER direction too: with the cast removed and NO pragma/emission, the gate must RED. If it stays green, the anchor is not matching and task 1 achieved nothing. |
| F-07-2 | The generated signature is really being typechecked | Change one argument name at the call site | **Neuter C4-ARGNAME**: rename `p_fmt` to `p_format` in the call. `npm run typecheck` must RED. Under the cast it stayed green — that is the drift the cast was hiding, and this neuter is the proof. |
| F-07-3 | The regen ran against the right schema | — | Falsified by inspection: `grep -q finalize_csv_strategy_with_returns src/lib/database.types.ts`. An absent function means the regen hit the wrong project. Record the project ref used (NOT any token) in the SUMMARY. |
| F-07-4 | The burn survives a reload | Keep the burn in the `useRef` only | **Neuter RT3-REFONLY**: remove the write-through at `:577`. The reload round-trip case must RED. If it stays green, the test is round-tripping the payload without ever reading it back into behaviour — helper, not wiring. |
| F-07-5 | The burn is clearable | Never persist the clear | **Neuter RT3-STICKY**: remove the write-through at `:561`. The "a legitimately different resubmit is not blocked" case must RED. Without this arm, a permanent lockout ships as a feature. |
| F-07-6 | The burn is signed, not free-floating | Store it under a second unsigned key | **Neuter RT3-UNSIGNED**: `<verify>`'s `localStorage.setItem` grep REDs, and a tamper case (edit the stored value, then load) must show verification failing rather than the tampered burn taking effect. |
| F-07-7 | No new Python code minted | Mint one | `<verify>` greps `CSV_FINALIZE_MOVED` out of `routers/process_key.py`. This criterion exists because CONTEXT's C4 line names that code and the recommendation is deliberately NOT to ship it yet. |
| F-07-8 | Other callers' 422 message unchanged | Change the shared default | **Neuter C4-BLEED**: point the new sentence at the default arm. The non-csv 422 test must RED. Without that arm, a copy change silently re-words unrelated errors. |
| F-07-9 | Comments were verified, not just rewritten | — | Not test-falsifiable — falsified by RECORD. The SUMMARY must list, per comment, the grep command run and its result. A comment with no recorded grep is treated as unverified and must be reverted. |
| F-07-10 | Coverage thresholds hold | — | `npm run test:coverage` under any of lines 82 / statements 80 / functions 74 / branches 72 fails the blocking `frontend-coverage` CI job. RT-3 adds branches in the wizard client; they need fixtures. |
</falsifiability_ledger>

<output>
Create `.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-07-SUMMARY.md` when done.

The SUMMARY MUST contain: (1) the checkpoint answer and, if `regen-blocked`, the TODOS deferral text
verbatim; (2) the `database.types.ts` diff classification (per hunk: generator noise or intended
drift) and the project ref used — NEVER a token; (3) the audit decision (emission or pragma) with
its reason; (4) per Falsifiability Ledger row, OBSERVED with verbatim failing text or NOT OBSERVED;
and (5) the per-comment grep log for the stale-comment batch.
</output>
