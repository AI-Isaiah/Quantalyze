---
phase: 146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql
  - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
  - src/__tests__/reconcile-dropped-enqueue-sweep.test.ts
autonomous: true
requirements: [B4, C3]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A strategy whose ONLY compute_jobs row is `failed_final` with `last_error LIKE 'orphaned_running_reaped:%'` — a terminalizer-produced orphan — becomes sweep-eligible and is healed (new arm C4)."
    - "A strategy whose only compute_jobs row is a GENUINE `failed_final` (last_error NULL or any non-marker text) is still NOT healed — shipped arm C2 stays GREEN."
    - "A strategy whose only compute_jobs row is `done` is still NOT healed — shipped arm C3 stays GREEN."
    - "A strategy with a RUNNING `derive_broker_dailies` job is still NOT healed — shipped arm C1 stays GREEN."
    - "The re-registered cron job keeps the jobname `reconcile_dropped_enqueue_sweep`, the schedule `35 * * * *`, and every previously anchored token (LIMIT 25 word-bounded, one AS MATERIALIZED, FOR UPDATE SKIP LOCKED, ON CONFLICT DO NOTHING, interval '1 hour', reconcile-sweep, detected_at, complete_with_warnings, archived, two MAX(DG.CREATED_AT))."
    - "The sweep's per-tick bound of 25 is unchanged, so widening the candidate set cannot flood the queue."
  artifacts:
    - supabase/migrations/20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql
    - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
  key_links:
    - "The narrow predicate keys on the terminalizer's OWN audit marker (`20260817120000:622,641`), whose 2-occurrence count is already gated at `20260817120000:741` — so marker drift is caught upstream."
    - "C2 (must-NOT-heal, genuine failed_final) and C4 (must-heal, terminalized orphan) form the DISCRIMINATING PAIR. Without C4 the fix is unproven; without C2 the fix is indistinguishable from the blanket widening."
    - "`public.compute_jobs` must still occur exactly 2 times in the cron body, or THREE sibling gates move in the same commit (this migration's STEP 2, test_reconcile_dropped_enqueue_sweep.sql Part 1, src/__tests__/reconcile-dropped-enqueue-sweep.test.ts:184-188)."
---

<objective>
Close **B4**: the Phase 144 terminalizer and the Phase 143 reconciliation sweep do not compose. A
chain-mid orphan gets terminalized to `failed_final`, and that row then permanently trips the
sweep's ANY-status `NOT EXISTS (compute_jobs)` conjunct — so the strategy is recovered by nobody,
with no user surface unless the user is still on the page (the 15-minute amber backstop).

⚠️ **B4 IS PREVENTIVE HARDENING, NOT A LIVE BREAK — and every artifact this plan writes must say
so.** MEASURED on PROD via Supabase MCP on 2026-08-18: the live population is **ZERO**. No row
carries the `orphaned_running_reaped:` marker; the terminalizer has never fired in production
since it shipped; `running` = 0 and `pending` = 0. All 122 `failed_final` rows are GENUINE job
failures (expired API keys, insufficient trade history, MT5 unreachable, geo-blocks, code bugs) —
which is precisely the population arms C2 and C3 exist to protect. The reachable population is
narrowed further because the sweep's `strategy_analytics` conjunct already excludes most
terminalized orphans once Phase 142's reaper runs.

⛔ **THE ROSTER'S ORIGINAL FIX IS WRONG. Do NOT widen the conjunct to "no NON-TERMINAL row".**
That blanket widening REDDENS two shipped gates:
`supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` arm C2 (`:558-561`) and arm C3
(`:563-568`) assert that a `failed_final`-only and a `done`-only strategy are NOT healed, and C2's
message names a real failure mode — *"a settled permanent failure belongs to nobody; re-enqueueing
it turns it into an hourly retry loop with no attempt ceiling, and the partial unique index does
not cover failed_final so nothing downstream stops it."* Deleting or inverting those arms to make
a fix pass is exactly the "fix the test" antipattern.

**Use the narrow predicate**: readmit ONLY terminalizer-produced orphans, keyed on the marker the
terminalizer itself stamps. This keeps C1/C2/C3 GREEN, is strictly more honest (a lost-worker row
is not a handler verdict), and needs a NEW must-heal arm C4 so C2 and C4 form a discriminating
pair.

Purpose: close the residual Phase 144 created as the price of its audit trail, without widening the
sweep into an unbounded retry loop over settled permanent failures.
Output: one forward cron-re-registration migration, one new must-heal arm on the SQL gate, and the
TS drift gate kept in step. Plus **C3** (the TEST-project sweep cron residual) documented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-CONTEXT.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-RESEARCH.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-PATTERNS.md

@supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql
@supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql
@supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Forward migration — re-register the sweep with the terminalizer-marker exemption</name>
  <files>supabase/migrations/20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql</files>
  <read_first>
    - `supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql` — `:585-605` (the idempotent unschedule-then-schedule idiom and its hard-RAISE pg_cron-absent arm), `:612-700` (clause-by-clause commentary), `:710-770` (the registered body; the target conjunct is at `:726-731`), `:713-726` (the `v_jobs` occurrence-count gate and its named siblings at `:836-840`), `:775-931` (STEP 2 self-verify: schedule equality, positive ILIKE anchors, occurrence counts, word-bounded numeric regex at `:875-878`, negative anchors, closing NOTICE).
    - `supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql:499-503` (the marker documented as THE operator query), `:617-644` (the terminalizer body and the two `last_error` literals at `:622`/`:641`), `:741` (the 2-occurrence count gate on the marker), `:288-292` (why the marker carries no identifier or row data), `:422` (unschedule+schedule CHANGES the jobid — say so if anything pins it), `:78-103` (free cron minutes).
    - `146.1-PATTERNS.md` §2 — the full convention list for re-registering a shipped cron body, including the whitespace-exactness rule and the dollar-tag prose trap.
  </read_first>
  <action>
Create `supabase/migrations/20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql`.

⛔ **The stamp is fixed: `20260819130500`.** PROD's `schema_migrations` tip was VERIFIED by the
orchestrator via Supabase MCP on 2026-08-18 as `20260819120000`. This stamp sorts above it, is
distinct from 146.1-01's `20260819130000`, and stays valid past the 2026-08-19 12:00 UTC expiry of
the sort-above rule. A `date -u` stamp today produces `20260818…`, below the tip, and trips the
`migration-policy.yml` backdated guard. ⛔ Do not touch `.github/migrate-backdated-allowlist.txt`.

⛔ **Never edit `20260816140000` in place.** It is applied, and it names its own three sibling
gates at `:836-840` which move with it. `test_main_worker.py:1430-1431` states the rule as an
assertion message for the neighbouring family: widen in a NEW migration.

**The change is ONE conjunct.** The deployed body's zero-jobs test (`20260816140000:726-731`) is a
bare `NOT EXISTS (SELECT 1 FROM public.compute_jobs cj WHERE cj.strategy_id = s.id)`. Add a single
`AND NOT (...)` inside that subquery's WHERE, excluding rows where `cj.status = 'failed_final'`
AND `cj.last_error LIKE 'orphaned_running_reaped:%'`. Nothing else in the body changes.

⛔ **Do NOT widen to "no NON-TERMINAL row".** See the objective. If the founder later prefers the
blanket wording, that is a legitimate call — but it requires deleting or inverting arms C2 and C3
and their safety rationale, which is a much larger and riskier edit. Record the blanket option in
the header as a NAMED, NOT-TAKEN alternative with its cost, so the decision is visible and
reversible. Do not bake it in.

**Header block** in the `20260816140000` register: why this migration exists; ⚠️ the PROD census
(zero marked rows, terminalizer never fired, 122 genuine `failed_final`) stated as a DATED
measurement, so a future reader re-measures rather than inherits; the exact reachable population
this heals (dailies present + terminalizer-produced `failed_final` + `strategy_analytics` absent
or `pending` — a first-ever job that died in the `derive_broker_dailies` upsert-then-enqueue
window, `job_worker.py:4742` → `:5201-5209`, named in the terminalizer header at `:56-64`); the
NOT-TAKEN blanket alternative and its cost; the ⚠️ PROD AUTO-APPLY paragraph; the gate list; a
note that `cron.unschedule` + `cron.schedule` CHANGES the jobid (`20260817120000:422`); and a
Manual rollback block. Pre-document the `BEGIN;`/`COMMIT;` deviation from migration-reviewer
invariant #14 exactly as `20260816140000` does.

`BEGIN;` then `SET lock_timeout = '5s';` — the cron-migration family value, not the fold's `'3s'`.

**Re-registration**, copying `20260816140000:585-605` exactly: assert pg_cron is installed and
`RAISE EXCEPTION` with `ERRCODE = 'feature_not_supported'` if not — ⛔ never a `RAISE NOTICE`
skip; the file itself forbids copying that pattern, because the hole would stay open behind a
green apply. Then `IF EXISTS (SELECT 1 FROM cron.job WHERE jobname = 'reconcile_dropped_enqueue_sweep')
THEN PERFORM cron.unschedule('reconcile_dropped_enqueue_sweep'); END IF;` then
`PERFORM cron.schedule('reconcile_dropped_enqueue_sweep', '35 * * * *', $cron$ ... $cron$);`.
⛔ Never `cron.unschedule` any other jobid, and NEVER jobid 9 (`derive-allocator-key-dailies`).
Same jobname, same `35 * * * *` — no new cron minute is needed.

**Body transcription discipline.** Reproduce the deployed body from
`20260816140000:710-770` with the single conjunct change. ⚠️ **Whitespace is load-bearing**:
`20260817120000:599-601` records that `status = 'running'` is written with SINGLE SPACES in all
four places because the shipped gate anchors on that exact spelling, and that TEST and PROD md5s
already differ purely because of whitespace. Preserve the existing spacing everywhere you are not
changing. ⛔ **The dollar-tag prose trap** (`20260816140000:~560-575`): never write the body's
opening dollar tag inside a comment in this file — the file-text gates extract between the first
and second occurrence and would extract comments instead. Keep every gate-relevant token out of
prose.

Preserve every anchored token: word-bounded `LIMIT 25`, exactly one `AS MATERIALIZED`,
`FOR UPDATE SKIP LOCKED`, `ON CONFLICT DO NOTHING`, `interval '1 hour'`, `reconcile-sweep`,
`detected_at`, `complete_with_warnings`, `archived`, the two `MAX(DG.CREATED_AT)` reads. And the
negative anchors: the body must NOT contain `computed_at`, `updated_at`, `enqueue_compute_job`,
or an `IN (SELECT … LIMIT …)` subquery.

⚠️ **`public.compute_jobs` must still occur exactly TWICE** in the re-registered body (the
conjunct's FROM plus the INSERT target). The narrow fix does not add an occurrence — VERIFY THIS
BY COUNTING IN YOUR DRAFTED BODY before writing the self-verify. If the count moves, THREE sibling
gates move in the same commit, all named at `20260816140000:836-840`: this migration's own STEP 2,
`supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` Part 1, and
`src/__tests__/reconcile-dropped-enqueue-sweep.test.ts:184-188`.

**STEP 2 self-verify**, replicating `20260816140000:775-931` against `cron.job.command` **read
back from the catalog, never re-typed**: schedule string EQUALITY (⛔ never a `::INT` cast,
`:800-802`); positive anchors; occurrence counts for tokens that legitimately appear more than
once; word-bounded numeric regex for `LIMIT 25` using the `([^0-9]|$)` alternation (the `|$` arm
is required, not decorative — a `%LIMIT 25%` form is satisfied by `LIMIT 2500`, MEASURED); the
negative anchors; and **a NEW positive anchor for the marker literal `orphaned_running_reaped`**,
so a future edit that silently deletes the exemption is caught the same way every other conjunct
is. Every RAISE names the CONSEQUENCE of the missing token, not the token
(`20260816140000:778-780`). Close with a `RAISE NOTICE`.

⛔ **The existing gate prose "ANY kind AND ANY status" becomes false with this change.** It lives
in an applied file that must not be edited — so the correction rides in THIS migration's own
commentary and STEP 2 message text. Task 2 corrects the SQL gate's copy of the claim.

Finish with `COMMIT;`.
  </action>
  <verify>
    <automated>
M=supabase/migrations/20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql
fail(){ echo "FAIL: $1"; exit 1; }
test -f "$M" || fail "migration missing"
grep -q "orphaned_running_reaped" "$M" || fail "marker predicate missing"
grep -q "failed_final" "$M" || fail "status arm of the exemption missing"
grep -qE "status[[:space:]]+IN[[:space:]]*\([[:space:]]*'pending'" "$M" && fail "blanket non-terminal widening detected - it reddens shipped arms C2 and C3"
grep -q "cron.unschedule('reconcile_dropped_enqueue_sweep')" "$M" || fail "not re-registered idempotently"
grep -qE "cron\.unschedule\((9|'?derive-allocator" "$M" && fail "another cron job is being unscheduled - forbidden"
grep -q "'35 \* \* \* \*'" "$M" || fail "schedule changed"
grep -q "feature_not_supported" "$M" || fail "pg_cron-absent arm is not a hard RAISE"
grep -qE "RAISE NOTICE.*pg_cron.*(skip|not installed)" "$M" && fail "pg_cron-absent arm degraded to a NOTICE skip"
N=$(grep -o 'public\.compute_jobs' "$M" | wc -l | tr -d ' ')
[ "$N" -ge 2 ] || fail "public.compute_jobs occurs $N time(s) in the file - the conjunct or the INSERT target is gone"
for tok in "AS MATERIALIZED" "FOR UPDATE SKIP LOCKED" "ON CONFLICT DO NOTHING" "interval '1 hour'" "reconcile-sweep" "detected_at" "complete_with_warnings" "archived"; do
  grep -qF "$tok" "$M" || fail "anchored token lost: $tok"
done
grep -qE 'LIMIT[[:space:]]+25([^0-9]|$)' "$M" || fail "LIMIT 25 bound lost or widened"
for neg in "computed_at" "enqueue_compute_job"; do
  grep -qF "$neg" "$M" && fail "negative anchor violated: $neg present"
done
grep -q "cron.job" "$M" || fail "STEP 2 does not read the body back from the catalog"
grep -qE '\(\[\^0-9\]\|\$\)' "$M" || fail "numeric anchor is not word-bounded - LIMIT 2500 would satisfy it"
echo OK
    </automated>
  </verify>
  <reversibility rating="costly">Merging supabase/migrations/** to main auto-applies to PROD. Rollback is a further forward migration re-registering the prior body; the Manual rollback block is that script. `cron.unschedule` + `cron.schedule` changes the jobid, so any operator runbook citing the old jobid needs re-reading.</reversibility>
  <done>The migration exists at the fixed stamp, changes exactly one conjunct via the terminalizer marker, re-registers the same jobname at the same schedule with a hard pg_cron-absent RAISE, preserves every positive and negative anchor, adds a positive anchor for the marker literal, and its STEP 2 reads the body back from `cron.job.command`. ⛔ Do NOT commit yet — task 2 commits with this one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Arm C4 — the must-heal counterpart that makes C2 a discriminator</name>
  <files>supabase/tests/test_reconcile_dropped_enqueue_sweep.sql, src/__tests__/reconcile-dropped-enqueue-sweep.test.ts</files>
  <precondition>`node_modules/.bin/vitest` exists in this checkout — GSD worktree agents get none and `npx vitest` exits MODULE_NOT_FOUND. Run `npm ci` or HALT.</precondition>
  <read_first>
    - `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` — `:170-345` (Part 1 structural, deliberately ungated), `:373-626` (Part 2 directional arms; seeds at `:477-483`, arm C1 `:547-552`, C2 `:554-561`, C3 `:563-568`, and the Part 2 closing NOTICE at `:626` which enumerates every arm and must be extended).
    - `src/__tests__/reconcile-dropped-enqueue-sweep.test.ts:180-192` — the `public.compute_jobs` 2-occurrence assertion and its consequence-naming message.
  </read_first>
  <behavior>
    - **New seed `v_c4`**: a strategy with dailies present, `strategy_analytics` absent (or `pending`), and its ONLY `compute_jobs` row `('compute_analytics_from_csv', v_c4, 'failed_final', 'normal', 3, now())` with `last_error = 'orphaned_running_reaped: no worker completed this job within the 4h claim window'` — the exact literal the terminalizer stamps at `20260817120000:622`.
    - **Arm C4 asserts `= 1` sweep-marked job** for `v_c4`: the terminalized orphan IS healed.
    - **Arm C2's seed gains an explicit `last_error`** that is NOT the marker — set it to a genuine failure text (e.g. a broker-auth failure sentence) rather than leaving it NULL. Reason: a NULL `last_error` makes C2 pass under the narrow predicate for the wrong reason (`NULL LIKE '…'` is NULL, so the `NOT (...)` is NULL and the row still counts). It passes either way, but only the explicit non-marker text proves the predicate is discriminating on the MARKER rather than on the presence of any text.
    - **Arms C1, C2, C3 keep their existing assertions and messages unchanged** and must stay GREEN.
    - A NULL-`last_error` `failed_final` row is ALSO not healed — add it as sub-arm C2b if it can be seeded without perturbing existing counts, because `NULL LIKE 'x%'` returning NULL is precisely the three-valued-logic trap this phase's A1 half exists to close.
  </behavior>
  <action>
Extend `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` Part 2 with arm C4 and the C2
seed strengthening described in `<behavior>`.

⛔ **Do NOT delete, invert or weaken arms C1, C2 or C3.** They are the shipped safety argument.
C2 and C4 are a MATCHED PAIR: C2 says a settled permanent failure is not the sweep's business,
C4 says a lost-worker row was never a verdict in the first place. Either arm alone is
non-discriminating — C4 alone passes under the blanket widening, C2 alone passes under no fix at
all. State that pairing explicitly in the arm comments, so a future editor who reds one of them
knows the other is the reason they cannot just relax it.

Follow the file's existing Part 2 conventions: seed through the real FK chain with
`gen_random_uuid()`, add `v_c4` to the same seed block as `v_c1..v_c3` (`:477-483`), and write the
arm in the same `SELECT count(*) INTO v_cnt … IF v_cnt <> N THEN RAISE EXCEPTION 'TEST FAILED
(2/arm C4/JOB-04/SC#3): …'` shape. The message must name the consequence, in the register of C2's:
a terminalizer-produced orphan that stays excluded is a strategy with dailies and no analytics
that NOBODY recovers — not the worker (the job is terminal), not the sweep (this conjunct), and
not the user (no surface after the 15-minute amber backstop).

Extend the Part 2 closing `RAISE NOTICE` at `:626` to enumerate the new arm — that NOTICE is the
file's own manifest and a gate whose manifest does not describe it is how stale claims survive.

Correct the now-false "ANY kind AND ANY status" prose wherever it appears in THIS file (Part 1's
commentary and the Part 1 closing NOTICE at `:345` describe the five conjuncts). The conjunct is
now "any kind and any status EXCEPT a terminalizer-marked `failed_final`". Do not touch
`20260816140000` — it is applied.

In `src/__tests__/reconcile-dropped-enqueue-sweep.test.ts`, re-read the `public.compute_jobs`
occurrence assertion at `:184-188`. If your drafted body kept the count at 2 (it should — the
narrow fix adds a predicate, not a table reference), the assertion needs no numeric change, but
its message prose must be extended to mention the marker exemption so the gate documents the body
it is actually pinning. If the count moved, fix the BODY, not the gate — a count change means you
added a compute_jobs reference the narrow fix does not need.

Commit tasks 1 and 2 TOGETHER as one commit (the 144-§8 same-commit rule).
  </action>
  <verify>
    <automated>
T=supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
fail(){ echo "FAIL: $1"; exit 1; }
grep -q "arm C4" "$T" || fail "arm C4 missing - the fix is unproven without a must-heal counterpart"
grep -q "orphaned_running_reaped" "$T" || fail "C4 does not seed the terminalizer marker"
grep -q "arm C2" "$T" || fail "arm C2 was deleted - that is the fix-the-test antipattern"
grep -q "arm C3" "$T" || fail "arm C3 was deleted"
grep -q "arm C1" "$T" || fail "arm C1 was deleted"
grep -qE "v_c4" "$T" || fail "no C4 seed variable"
awk '/arm C2:/,/arm C3:/' "$T" | grep -q "last_error" || fail "C2 seed still has no explicit non-marker last_error - it would pass for the wrong reason"
grep -qE '\\!|\\copy|\\COPY|\\o ' "$T" && fail "psql meta-command present - the sql-tests job bails on these"
grep -q "ANY kind AND ANY status" "$T" && fail "the now-false conjunct description survives in this file"
npx --no-install vitest run src/__tests__/reconcile-dropped-enqueue-sweep.test.ts --no-file-parallelism --reporter=basic || fail "TS drift gate red"
echo OK
    </automated>
  </verify>
  <done>Arm C4 exists with the exact terminalizer marker literal and asserts the orphan IS healed; C2's seed carries explicit non-marker `last_error`; C1/C2/C3 are otherwise unchanged and their messages intact; the Part 2 manifest NOTICE names C4; the stale "ANY kind AND ANY status" prose is corrected in this file only; the TS drift gate passes; tasks 1+2 are ONE commit.</done>
</task>

<task type="auto">
  <name>Task 3: C3 — document the TEST-project sweep-cron residual; do not "clean it up"</name>
  <files>supabase/tests/test_reconcile_dropped_enqueue_sweep.sql</files>
  <read_first>
    - `146.1-RESEARCH.md` §4.7 — the interaction with the 05:30 UTC shared-TEST-DB flake, the ≤25/tick bound, and the ~396 stuck `running` rows the terminalizer's header records on TEST as of 2026-08-17.
    - `146.1-CONTEXT.md` C3 — "a future non-terminal dailies-bearing seed variant would feed one permanently-unclaimable 'pending' row into the 05:30 flake. Document-or-guard; lowest priority."
  </read_first>
  <action>
Add a dated section to the header of `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql`
documenting the TEST-project cron residual (C3, tracked as TODOS D-13).

⛔ **Document-or-guard ONLY. This is NEVER a cleanup migration.** The standing constraints are
absolute: never DELETE a `pending` `compute_jobs` row; never `cron.unschedule(9)`
(`derive-allocator-key-dailies`); never touch the `process_key_unified_backbone` flag row. C3 is
the one item in this phase that could tempt a cleanup, which is why it gets an explicit refusal in
writing.

The section must state: (a) that every seed in this file rolls back, so this file is not the
source of the residual; (b) that a FUTURE non-terminal dailies-bearing seed variant, if it ever
escaped a rollback, would feed one permanently-unclaimable `pending` row into the 05:30 UTC
TEST-DB backlog (jobid 9 fans one job per `api_key` on a project with no draining worker, and
stale `pending` sorts ahead in `claim_compute_jobs_with_priority`); (c) that this migration's
widened candidate set is bounded at ≤25/tick by the `LIMIT`, so it cannot flood; (d) ⚠️ that the
terminalizer's own header records ~396 genuinely stuck `running` rows on TEST as of **2026-08-17**
— a DATED claim, not a fact — and that if those are ever terminalized, the narrow fix could make a
few hundred TEST strategies sweep-eligible at ≤25/hour, which is why 146.1-07 counts
terminalizer-marked rows on TEST BEFORE merge; and (e) the explicit refusal above.

⚠️ Write (d) as a claim with its date attached, in the register this repo uses for dated censuses.
A census quoted without its date is how a dated claim becomes an assumed fact.

Commit as its own commit.
  </action>
  <verify>
    <automated>
T=supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
fail(){ echo "FAIL: $1"; exit 1; }
grep -q "D-13" "$T" || fail "C3 residual not cross-referenced to its tracker"
grep -q "2026-08-17" "$T" || fail "the TEST census is quoted without its date"
grep -qE "05:30" "$T" || fail "the flake interaction is not named"
grep -qiE "never (delete|unschedule)" "$T" || fail "the explicit refusal is not written down"
git diff --stat "$T"
echo OK
    </automated>
  </verify>
  <done>The gate file's header carries a dated C3 section naming the residual, the ≤25/tick bound, the dated TEST census, the pre-merge count obligation on 146.1-07, and an explicit written refusal of the cleanup-migration path. No migration, no row deletion, no cron change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron scheduler → `compute_jobs` queue | The sweep INSERTs `pending` rows on a schedule with no human in the loop; its candidate predicate is the only bound on what it enqueues, alongside `LIMIT 25`. |
| migration file → PROD database | Merging `supabase/migrations/**` to `main` auto-applies with no deploy step. |
| terminalizer cron → sweep cron | Two unrelated cron bodies whose only shared vocabulary is the `last_error` marker literal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.1-11 | Denial of Service | Blanket "no NON-TERMINAL row" widening | high | mitigate | Explicitly NOT taken. The narrow marker-scoped predicate keeps arms C2/C3 GREEN; `<verify>` grep-refuses the blanket status-list form. C2's own message names the unbounded-hourly-retry failure mode. |
| T-146.1-12 | Denial of Service | Widened candidate set floods the TEST 05:30 queue | medium | mitigate | `LIMIT 25` per tick preserved and word-bound-anchored; 146.1-07 counts terminalizer-marked rows on TEST before merge; C3 documented in task 3. |
| T-146.1-13 | Tampering | The marker exemption silently deleted by a future edit | medium | mitigate | New positive STEP 2 anchor on the `orphaned_running_reaped` literal, plus arm C4 as the behavioural counterpart. |
| T-146.1-14 | Repudiation | A gate that passes for the wrong reason (NULL `last_error`) | medium | mitigate | C2's seed gains explicit non-marker `last_error`; sub-arm C2b covers the NULL case. `NULL LIKE 'x%'` is NULL — the same three-valued-logic trap A1 closes on the fold. |
| T-146.1-15 | Denial of Service | Unrehearsed migration auto-applies to PROD | high | transfer | 146.1-07's blocking orchestrator gate: TEST rehearsal via Supabase MCP, SQL gates run on TEST, and one real cron tick inspected in `cron.job_run_details` before merge. |
| T-146.1-16 | Elevation of Privilege | Unscheduling the wrong cron job | high | mitigate | Only `reconcile_dropped_enqueue_sweep` by NAME; `<verify>` grep-refuses any other `cron.unschedule` target, jobid 9 above all. |
| T-146.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed. Package Legitimacy Audit recorded not-applicable in `146.1-RESEARCH.md`. |
</threat_model>

<verification>
1. Each task's `<verify>` block exits 0.
2. `npx vitest run src/__tests__/reconcile-dropped-enqueue-sweep.test.ts --no-file-parallelism` green.
3. `npm run lint` and `npm run typecheck` green.
4. ⛔ The SQL arms are NOT executed by this plan (no local psql client). 146.1-07 runs
   `test_reconcile_dropped_enqueue_sweep.sql` against TEST after applying the migration there, and
   THAT is the gate. Completing this plan means the arms are written and statically conformant, not
   that the predicate works.
</verification>

<success_criteria>
- [ ] `supabase/migrations/20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql` exists at the fixed stamp.
- [ ] Exactly one conjunct changed: the marker-scoped `AND NOT (cj.status = 'failed_final' AND cj.last_error LIKE 'orphaned_running_reaped:%')` inside the existing `NOT EXISTS`. No status-list widening anywhere.
- [ ] Same jobname, same `35 * * * *`, idempotent unschedule-then-schedule, hard `feature_not_supported` RAISE when pg_cron is absent, and no other cron job touched.
- [ ] `public.compute_jobs` still occurs exactly twice in the registered body; if it did not, the BODY was fixed, not the gates.
- [ ] Every positive anchor preserved and every negative anchor still absent; `LIMIT 25` word-bounded with the `([^0-9]|$)` arm.
- [ ] STEP 2 reads `cron.job.command` back from the catalog (never re-typed) and adds a positive anchor for the marker literal.
- [ ] Arm C4 exists and asserts the terminalized orphan IS healed; arms C1/C2/C3 unchanged and still asserting NOT-healed; C2's seed carries explicit non-marker `last_error`.
- [ ] The Part 2 manifest NOTICE enumerates C4; the stale "ANY kind AND ANY status" prose is corrected in the gate file only.
- [ ] The header of the gate file carries the dated C3 section with its explicit refusal of a cleanup migration.
- [ ] Every artifact describes B4 as PREVENTIVE hardening with the zero-population PROD census stated and dated. No document produced by this plan calls it a live break.
- [ ] Tasks 1+2 are ONE commit; task 3 is its own commit.
</success_criteria>

<falsifiability_ledger>
| # | Success criterion | What makes it go RED | Named neuter (run against TEST in 146.1-07 where a DB is needed) |
|---|---|---|---|
| F-03-1 | The terminalized orphan is healed | Revert the conjunct to the bare `NOT EXISTS` | **Neuter B4-REVERT**: arm C4 must RED while C1/C2/C3 stay GREEN. If C4 stays green under the revert, its seed is not actually excluded today and the whole premise is wrong. |
| F-03-2 | Genuine permanent failures stay excluded | Widen to the blanket non-terminal status list | **Neuter B4-BLANKET**: arms C2 **and** C3 must BOTH RED. This is the measurement that refutes the roster's original fix — run it and record both failures verbatim. |
| F-03-3 | The predicate discriminates on the MARKER, not on text presence | Change the marker `LIKE` to `last_error IS NOT NULL` | **Neuter B4-ANYTEXT**: arm C2 must RED (its seed now carries genuine non-marker failure text). Under the original NULL-`last_error` C2 seed this neuter false-PASSes — which is exactly why task 2 strengthens the seed. |
| F-03-4 | Three-valued logic is handled | — | Sub-arm C2b (NULL `last_error`, `failed_final`) must stay NOT-healed. `NULL LIKE 'x%'` is NULL; if C2b flips, the `NOT (...)` composition is wrong. |
| F-03-5 | The exemption cannot be silently deleted | Delete the marker clause from the body but leave its comment | **Neuter B4-COMMENT**: the STEP 2 marker anchor must RAISE. If the anchor matched the COMMENT rather than the code, it false-PASSes — which is the `%5000%` failure class; the anchor must run over a comment-stripped or catalog-read body. |
| F-03-6 | The per-tick bound holds | Widen `LIMIT 25` to `LIMIT 2500` | Existing Part 4 (the bound half) must RED, and the STEP 2 word-bounded regex must RAISE. A `%LIMIT 25%` form passes on `2500` — MEASURED in this repo. |
| F-03-7 | The occurrence-count siblings are in step | Add a third `public.compute_jobs` reference | `src/__tests__/reconcile-dropped-enqueue-sweep.test.ts:184-188`, Part 1, and STEP 2 must ALL RED together. If only one REDs, the three are out of sync and the count claim is not actually triple-anchored. |
| F-03-8 | Preventive, not live | — | Falsified by measurement, not by test: 146.1-07 re-counts `last_error LIKE 'orphaned_running_reaped:%'` on PROD. If the count is non-zero at merge time, the "ZERO population" claim in every header written by this plan is STALE and must be corrected before merge. |
</falsifiability_ledger>

<output>
Create `.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-03-SUMMARY.md` when done.

The SUMMARY MUST record the drafted body's `public.compute_jobs` occurrence count as COUNTED (not
assumed), and mark every DB-requiring neuter in the ledger as `DEFERRED TO 146.1-07` by name so the
orchestrator inherits an explicit list.
</output>
