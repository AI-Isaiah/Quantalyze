---
phase: 146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty
plan: 05
type: execute
wave: 3
depends_on: ["146.1-04"]
files_modified:
  - src/app/api/strategies/csv-finalize/route.ts
  - src/__tests__/csv-finalize-cross-submission-merge.test.ts
  - src/__tests__/csv-validate-route.test.ts
  - src/__tests__/csv-finalize-c14-regression.test.ts
autonomous: true
requirements: [A4, A3, C2]

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An ECHOED (23505-resolved) outcome does NOT issue the metadata UPDATE — a same-series retry with a flipped `asset_class` can no longer rewrite a committed strategy's annualization clock while its stored KPIs keep the old one (A4)."
    - "A FRESH (created) outcome still issues the metadata UPDATE exactly as today (A4 positive counterpart)."
    - "`FinalizeAtomicOutcome`'s ok-arm carries `fresh` as a REQUIRED boolean, so the compiler names every construction site (A4)."
    - "A transport failure (postgrest-js resolves with `{error:{code:\"\"}, status:0}`) no longer receives copy asserting a complete rollback — the POST never retries, so the single POST may have reached PostgREST and committed before the connection died (A3)."
    - "A 2xx-with-lost-id outcome (`!error` and a non-UUID return) no longer receives copy asserting a complete rollback — a 2xx from PostgREST means the transaction committed and only the id failed to reach us (A3)."
    - "A SQLSTATE-bearing RPC error (a 5-character `code`) keeps its existing copy verbatim — the fold has no handler clause, so a raise aborts and every write rolls back; that claim is TRUE and must not be softened (A3)."
    - "One handler serves both the manager (`pending_review`) and contribution (`private`) flows, and the source-shape / arity gates still pin a single typed args object with an explicit `dailyReturnsSeries: CsvDailyReturnRow[]` field (C2)."
  artifacts:
    - src/app/api/strategies/csv-finalize/route.ts
  key_links:
    - "`code: \"\"` is FALSY — a naive `if (error?.code)` collapses the transport class into the lost-id class. The discriminator must be an explicit length check on a string code."
    - "C2 MUST land AFTER A4 and A3. Collapsing first then editing once is fine; editing two handlers then collapsing doubles the edit surface and doubles the chance one arm drifts."
    - "Cheapest conforming collapse: KEEP the name `unifiedCsvFinalizeHandler`, keep the single typed args object with `dailyReturnsSeries`, and add `terminalStatus` + `logPrefix` as fields of that same object. Tests 8b/8c then pass unchanged and only the doc-comment moves. Any rename or second positional parameter reds all three gates."
    - "audit-coverage.test.ts does a brace-balance walk for `logAuditEvent(` inside the ENCLOSING function body — moving a mutation into a new enclosing function relocates that window, so any existing `@audit-skip:` pragma must travel with the code it exempts."
---

<objective>
Second half of the **route.ts serial cluster** (mandated order B3 → A2 → A4 → A3 → C2; 146.1-04
delivered B3 → A2 → C1). ⛔ ONE agent, serial, this wave only — concurrent agents in one checkout
share the git index.

- **A4** — echoed 23505 outcomes still run `applyCsvMetadataUpdate` with THIS request's metadata
  (`route.ts:1512-1519` manager, `:1611-1618` contribution). `buildMetadataUpdatePayload` copies
  `asset_class` verbatim when present (`:505-507`), and the docblock above it (`:498-504`) states
  the consequence: an absent field stays absent → column default null → 252 downstream. So a
  same-series retry with a flipped `asset_class` rewrites the strategy's annualization clock
  (√252 vs √365) while the stored KPIs keep the OLD one. **This is the money-math half of the
  roster** — a Sharpe computed on one calendar convention, labelled with another.
- **A3** — the arm at `route.ts:585-632` tells the user "Nothing was saved — the submission rolled
  back completely" for THREE outcome classes, and that sentence is only true for one of them.
  `wizardErrors.ts:1971-1979` (SEAMUX-04) already conceded the same claim is unmakeable
  client-side and removed it from `CSV_SUBMIT_FAILED`. The route's copy is the older, unreasoned
  side of that contradiction; it loses.
- **C2** — `unifiedCsvFinalizeHandler` (`:1470-1555`) and `contributionCsvFinalizeHandler`
  (`:1579-1644`) are TOKEN-IDENTICAL except four tokens, VERIFIED exhaustively by comment-stripped
  difflib: the function name, `terminalStatus`, and `logPrefix` twice. Not one statement, argument
  or ordering differs.

Purpose: the route's error envelope is the product's voice at its worst moment. Asserting an
observation the server did not make is the same defect class as fabricating a track record.
Output: a fresh/echoed discriminator that gates a real side effect, honest copy on two arms,
and ~90 lines of duplicated handler collapsed with its gates rewritten (not deleted).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-CONTEXT.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-RESEARCH.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-PATTERNS.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-04-SUMMARY.md

@src/app/api/strategies/csv-finalize/route.ts
@src/lib/wizardErrors.ts
@src/__tests__/csv-validate-route.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (A4): a required `fresh` discriminator on FinalizeAtomicOutcome, gating the metadata UPDATE</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/__tests__/csv-finalize-cross-submission-merge.test.ts</files>
  <precondition>`node_modules/.bin/vitest` exists. GSD worktree agents get none; `npx vitest` exits MODULE_NOT_FOUND and `npx tsc` resolves a decoy package. Run `npm ci` or HALT.</precondition>
  <precondition>146.1-04 is merged into this working tree and `src/app/api/strategies/csv-finalize/route.ts` already carries the B3 constraint gate and the A2 terminal-status refusal. Re-derive every line anchor below from the CURRENT file — 146.1-04 shifted them. Anchors in this plan are from `76adf961`.</precondition>
  <read_first>
    - `src/app/api/strategies/csv-finalize/route.ts:534-536` (the `FinalizeAtomicOutcome` union), `:573-577` (the fresh-create success return), `:871-876` (the resolve echo return), `:498-507` (`buildMetadataUpdatePayload` and its consequence docblock), `:1106-…` (`applyCsvMetadataUpdate`), `:1512-1531` and `:1611-1627` (the two handlers' metadata-UPDATE and enqueue call sites).
    - `src/app/api/strategies/create-with-key/route.ts:936-943` — the `deduped: true` envelope field, the naming precedent for this concept.
  </read_first>
  <behavior>
    - Fresh create → `fresh: true` → `applyCsvMetadataUpdate` IS invoked. Asserted on the `.update()` spy, not on the return value.
    - 23505 resolve echo → `fresh: false` → `applyCsvMetadataUpdate` is NOT invoked. `updateCalls` has length 0.
    - The echo case with a CHANGED `asset_class` in the request → still zero updates, and the committed row's `asset_class` is untouched. This is the economic oracle: the strategy's annualization clock (√365 for crypto, √252 for trad) must not be rewritten by a retry while the stored KPIs keep the old convention.
    - Fresh create with a non-empty `dailyReturnsSeries` → the analytics enqueue still fires.
    - Echo with a non-empty `dailyReturnsSeries` → the analytics enqueue still fires. Re-enqueueing an echo is harmless-to-good (the sweep would do it anyway); leaving it is deliberate.
    - `fmt = 'trades'` → the series is empty on BOTH the fresh and echo paths, so no enqueue happens on either. That is PRE-EXISTING behaviour documented at `route.ts:1334-1337` and is NOT an A4 regression. ⛔ Do not "fix" it here.
  </behavior>
  <action>
Extend the `FinalizeAtomicOutcome` union's ok-arm with `fresh: boolean`.

⚠️ **Required, never optional.** A `fresh?: boolean` lets a future third return site default to
`undefined` → falsy → silently treated as an echo. Required forces the compiler to name every
construction site, which is the whole value of the change. Set `fresh: true` at the create return
and `fresh: false` at the resolve echo return.

**The side effect to gate: `applyCsvMetadataUpdate`.** Skip it entirely when `!fresh`, in BOTH
handlers (they are still two at this point in the wave; C2 collapses them in task 3). An echo is
by definition the SAME submission — honouring its metadata is honouring a mutation the user did
not ask for, applied to a row they already committed.

⚠️ **Skip, do not "re-enqueue after".** The enqueue is `after()`-scheduled and non-blocking, so
ordering between it and the UPDATE is not guaranteed; a "re-enqueue after the UPDATE" design has
no ordering to rely on. Skipping is simpler and strictly safer.

**Leave `enqueueCsvAnalyticsAfter` alone.** It is gated on
`args.dailyReturnsSeries.length > 0` and that gate is correct on both paths.

Tests go in `src/__tests__/csv-finalize-cross-submission-merge.test.ts`, which already carries the
`updateCalls` write-vector recorder (`:82-85`).

⭐ **The oracle is BEHAVIOURAL: "was the UPDATE issued?"** ⛔ Never assert `fresh === false` on the
return value, and never assert that the source contains a particular branch. A test that asserts
`fresh === false` proves nothing about whether the metadata UPDATE is skipped — the union field
could be perfectly correct and wired to nothing. Drive the real handler and watch the write vector.

Commit as its own commit.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
R=src/app/api/strategies/csv-finalize/route.ts
grep -qE 'fresh\?:' "$R" && fail "fresh is optional - a future third return site would default to an echo silently"
grep -qE 'fresh:\s*boolean' "$R" || fail "the union does not carry a required fresh field"
[ "$(grep -cE 'fresh:\s*(true|false)' "$R")" -ge 2 ] || fail "fewer than two construction sites set fresh"
npx --no-install vitest run src/__tests__/csv-finalize-cross-submission-merge.test.ts --no-file-parallelism --reporter=basic || fail "suite red"
npm run typecheck || fail "typecheck red - a construction site was missed"
echo OK
    </automated>
  </verify>
  <done>`FinalizeAtomicOutcome`'s ok-arm carries a REQUIRED `fresh: boolean` set at both construction sites; `applyCsvMetadataUpdate` is skipped on `!fresh` in both handlers; the enqueue gate is unchanged; tests assert on the `.update()` spy (never on the return value or the source) and cover fresh-updates, echo-no-update, echo-with-changed-asset_class, and both enqueue paths; typecheck is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (A3): split the fold-failure copy so the commit-agnostic classes stop asserting a rollback</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/__tests__/csv-validate-route.test.ts, src/__tests__/csv-finalize-c14-regression.test.ts</files>
  <read_first>
    - `src/app/api/strategies/csv-finalize/route.ts:573-583` (how the arm is entered), `:585-632` (the arm, including the `debug_context: { rpc_error_code: error?.code ?? null }` at `:628` — the discriminator is already computed), `:695-722` (`failClosed`, the commit-agnostic sibling that already exists in this file), `:1005-1010` (a comment claiming this route forwards `X-User-Access-Token` — see the last paragraph of the action).
    - `src/lib/wizardErrors.ts:1971-1979` (the SEAMUX-04 concession, verbatim) and `:2001-2015` (`CSV_SUBMIT_FAILED` — the ALREADY-APPROVED commit-agnostic wording: title, cause, and the "open /strategies in another tab first" fix step).
    - `146.1-RESEARCH.md` §5.1 — the three outcome classes with their arrival paths, whether a commit is possible in each, and what the copy honestly earns.
  </read_first>
  <behavior>
    - **Class 1** — `error` present with a 5-character string `code` (22023 / 42501 / 22007 / 22P02 / 23502 / …): PostgREST returned a body with a SQLSTATE. The fold has no handler clause, so the raise aborts the function and every write rolls back. Copy is TRUE. **Keep it byte-identical.**
    - **Class 2** — `error` present with `code === ""` and `status: 0`: postgrest-js RESOLVES (does not reject) on a transport failure, and `RETRYABLE_METHODS` is `['GET','HEAD','OPTIONS']` so the RPC POST never retries. The single POST may have reached PostgREST and committed before the connection died. Copy must be commit-agnostic.
    - **Class 3** — `!error` and a non-UUID return (TS-13 lost id): a 2xx from PostgREST means the transaction COMMITTED; only the id failed to reach us. Copy must be commit-agnostic and lean toward "it may well have been saved".
    - Classes 2 and 3 carry a Sentry `step` tag distinct from `finalize-fold-fail`, so the two sub-classes are distinguishable in Sentry rather than merged into one bucket.
    - All three classes keep code `CSV_FINALIZE_FAIL`, status 500, `NO_STORE_HEADERS`, and the four envelope invariants (`ok:false`, UPPER_SNAKE `code`, `human_message`, `debug_context`, `correlation_id`).
  </behavior>
  <action>
Branch the `human_message` inside the existing arm on the outcome class.

⚠️ **The discriminator is `error?.code`, and it is already computed** at `:628`. ⛔ **`code: ""` is
FALSY**, so a naive truthiness test collapses classes 2 and 3 into one. Discriminate explicitly:
class 1 is `typeof error?.code === "string" && error.code.length === 5`; class 2 is `error`
present and not class 1; class 3 is `!error`.

⛔ **Keep ONE code: `CSV_FINALIZE_FAIL`.** Minting a second code costs same-commit edits to
`KNOWN_CSV_FINALIZE_CODES` (`CsvSubmitStep.tsx:161-167`), `EXPECTED_TABLE_SIZE` in
`wizardErrors.invariant.test.ts`, and `seam-wire-vocabulary.invariant.test.ts` (which derives the
route's emitted codes from comment-stripped source and reds BY NAME), and buys nothing the copy
branch does not: `CSV_FINALIZE_FAIL` is a `KNOWN_CSV_FINALIZE_CODES` member, so **the route's own
sentence is what renders** (`CsvSubmitStep.tsx:330-338` builds the envelope from
`data.human_message`). `wizardErrors.ts:1937-1941` states the rule: a second code for the same
fact is the two-names-one-fact drift.

**Reuse the approved voice, do not invent a fourth register.** `CSV_SUBMIT_FAILED`
(`wizardErrors.ts:2001-2015`) is the already-reasoned commit-agnostic wording, and its recovery
step — open `/strategies` in another tab; if the strategy is listed, the save completed — is the
right instruction here too, and it composes with the 23505 resolve arm (an unchanged resubmit from
this wizard resolves to the strategy already started instead of creating a second one). Say that.

Give classes 2 and 3 their own Sentry `step` tag (the existing tags in this file are
`finalize-fold-fail`, `finalize-resolve-read-fail`, `finalize-resolve-refused`; pick one that is
grep-unique). Without it the two sub-classes are indistinguishable in Sentry and nobody can ever
measure how often the honest arm fires.

Carry a **dated provenance comment** naming: the claim removed, the three classes and why the
claim was only true for one, `wizardErrors.ts:1971-1985` as the precedent that already made this
call on the client side, and the postgrest-js `RETRYABLE_METHODS` fact that makes class 2's outcome
unknowable. `146.1-PATTERNS.md` §7 makes this comment mandatory.

**42501 → 401: DEFER, do not implement.** `withAuth` ran milliseconds earlier, so the
reachability is a session expiring inside one request, and a 401 from a POST the wizard treats as
retryable would send the user to a login redirect mid-submit. The honest improvement is a
distinguishing `debug_context`, not a status change. Add the `debug_context` distinction and
record the deferral with its reasoning in the same provenance comment.

**Also in this task, correct one false comment.** `route.ts:1005-1010` claims that this route's
outgoing headers carry `X-User-Access-Token`. `csv-finalize/route.ts` does not call
`postProcessKey` at all (VERIFIED: zero matches at `76adf961`), so the claim is already false at
HEAD, independent of the 146.1-06/B2 decision. Correct it to name what this route actually sends,
and keep the `scrubSeamError` call and its rationale intact — the scrub is cheap insurance and is
NOT what was wrong. ⛔ Do not touch the `X-User-Access-Token` handling in `keys/sync`,
`verify-strategy` or `process-key-client.ts` — those are 146.1-06's, and it lands in the next
wave.

Tests: extend `src/__tests__/csv-validate-route.test.ts` and/or
`src/__tests__/csv-finalize-c14-regression.test.ts` with one fixture per class.

⭐ **Assert on the ABSENCE of the rollback claim in classes 2 and 3, not merely on the presence of
the new sentence.** A presence-only assertion is satisfiable by concatenating the new sentence onto
the old one — the same satisfiable-by-addition hole as the `%5000%` substring gate. And assert the
class-1 sentence is still present verbatim, because "make everything vague" is the failure mode on
the other side.

Commit as its own commit.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
R=src/app/api/strategies/csv-finalize/route.ts
grep -qE 'error\.code\.length === 5|code\.length === 5' "$R" || fail "no explicit length discriminator - code:\"\" is falsy and collapses classes 2 and 3"
grep -q 'CSV_FINALIZE_FAIL' "$R" || fail "the arm's code changed"
grep -qE 'code:\s*"CSV_FINALIZE_UNKNOWN|CSV_FINALIZE_UNCONFIRMED' "$R" && fail "a second code was minted for the same fact"
npx --no-install vitest run src/__tests__/csv-validate-route.test.ts src/__tests__/csv-finalize-c14-regression.test.ts src/__tests__/csv-finalize-cross-submission-merge.test.ts --no-file-parallelism --reporter=basic || fail "suite red"
npx --no-install vitest run src/lib/seam-wire-vocabulary.invariant.test.ts src/lib/wizardErrors.invariant.test.ts --no-file-parallelism --reporter=basic || fail "vocabulary/roster gates red"
npx --no-install vitest run src/lib/seam-log-coverage.test.ts src/lib/seam-redaction.test.ts --no-file-parallelism --reporter=basic || fail "seam log/redaction gates red - the comment edit disturbed a scrub site"
npm run typecheck || fail "typecheck red"
echo OK
    </automated>
  </verify>
  <done>The arm discriminates the three classes by an explicit 5-character code-length test; class 1's sentence is byte-identical; classes 2 and 3 carry commit-agnostic copy in the `CSV_SUBMIT_FAILED` voice under a distinct Sentry step tag with one code unchanged; a dated provenance comment names the removed claim, the three classes, the SEAMUX-04 precedent, the `RETRYABLE_METHODS` fact and the 42501→401 deferral; `route.ts:1005-1010`'s false forwarding claim is corrected with the scrub intact; tests assert on ABSENCE for 2/3 and on PRESENCE for 1.</done>
</task>

<task type="auto">
  <name>Task 3 (C2): collapse the two token-identical handlers, rewriting the gates that pin them</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/__tests__/csv-validate-route.test.ts</files>
  <read_first>
    - `src/app/api/strategies/csv-finalize/route.ts:1426-1443` (the two call sites), `:1466-1469` (the doc-comment that names the source-shape constraint), `:1470-1555` and `:1579-1644` (the two handlers).
    - `src/__tests__/csv-validate-route.test.ts:1412-1447` (Test 8a runtime), `:1450-1473` (Test 8b source-shape, two regexes), `:1475-1497` (Test 8c arity lock, handler AND call site).
    - `src/__tests__/audit-coverage.test.ts:195-235` — the brace-balance walk and the `@audit-skip:` pragma window.
  </read_first>
  <action>
⚠️ **C2 lands LAST, after A4 and A3 — never before.** Collapsing first and then editing the
collapsed handler is fine; editing two handlers and THEN collapsing doubles the edit surface and
doubles the chance one arm drifts. A4 and A3 are already done in this plan's tasks 1 and 2.

The two bodies are TOKEN-IDENTICAL apart from four tokens, VERIFIED exhaustively by
comment-stripped difflib: the function name, `terminalStatus: "pending_review"` vs `"private"`,
and `logPrefix` (twice — once in the fold call, once in the enqueue call). Nothing else — not one
statement, argument or ordering — differs. **Before collapsing, re-run that diff yourself** on the
CURRENT file: tasks 1 and 2 edited both handlers, and if they drifted, the collapse would silently
adopt one arm's version. A diff that is no longer empty means fix the drift first.

**⭐ The cheapest conforming shape — take it.** KEEP the name `unifiedCsvFinalizeHandler`, keep the
single typed args object with the explicit `dailyReturnsSeries: CsvDailyReturnRow[]` field, and add
`terminalStatus: "pending_review" | "private"` and `logPrefix: string` as fields of that SAME
object. Then Tests 8b and 8c pass unchanged and only the doc-comment moves. **Any rename or a
second positional parameter reds all three gates by name.** There is no
`contributionCsvFinalizeHandler` source-shape gate (grep found zero references outside
`route.ts`), so the collapse only has to satisfy the `unifiedCsvFinalizeHandler` pins.

⛔ **Rewrite the gates, never delete them.** If your chosen shape does move Test 8b or 8c, the
replacement must pin the SAME properties on the new single handler: a single typed args object, an
explicit `dailyReturnsSeries: CsvDailyReturnRow[]` field, and single-args arity at both the
declaration and the call site. A gate deleted because it went red is a gate that was doing its job.

**Keep `logPrefix` DISTINCT in the output.** `grep -rn "csv-finalize unified"` and
`grep -rn "csv-finalize contribution"` across the whole repo BEFORE changing them — three gate
files and operator runbooks may pin those literals. Deriving `logPrefix` from `terminalStatus`
inside the handler is acceptable; changing the emitted strings is not, unless the grep comes back
empty and you say so in the SUMMARY.

⚠️ **audit-coverage is a brace-balance walk.** `src/__tests__/audit-coverage.test.ts` requires
`logAuditEvent(` / `logAuditEventAsUser(` inside the ENCLOSING function body, or an
`@audit-skip:` pragma within 8 lines above the chain start. **Merging two enclosing functions into
one relocates that window.** Any existing `@audit-skip:` pragma must travel with the code it
exempts. Run that suite explicitly.

Update the doc-comment at `:1466-1469` to describe the collapsed handler and to keep pointing at
the gates that pin it — the in-code pointer is what stops the next reader from "simplifying" the
signature.

Commit as its own commit.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
R=src/app/api/strategies/csv-finalize/route.ts
grep -q "contributionCsvFinalizeHandler" "$R" && fail "the second handler survives - the collapse did not happen"
[ "$(grep -c 'async function unifiedCsvFinalizeHandler' "$R")" -eq 1 ] || fail "not exactly one handler declaration"
grep -qE 'unifiedCsvFinalizeHandler\(args:\s*\{' "$R" || fail "arity lock shape broken - a second positional parameter or a non-object args"
grep -qE 'dailyReturnsSeries:\s*(readonly\s+)?CsvDailyReturnRow\[\]' "$R" || fail "the explicit dailyReturnsSeries field is gone"
grep -q 'csv-finalize unified' "$R" || fail "the unified logPrefix literal changed - grep the repo before moving it"
grep -q 'csv-finalize contribution' "$R" || fail "the contribution logPrefix literal changed"
npx --no-install vitest run src/__tests__/csv-validate-route.test.ts src/__tests__/audit-coverage.test.ts --no-file-parallelism --reporter=basic || fail "source-shape / arity / audit-coverage gates red"
npx --no-install vitest run src/__tests__/csv-finalize-cross-submission-merge.test.ts src/__tests__/csv-finalize-c14-regression.test.ts --no-file-parallelism --reporter=basic || fail "behavioural suites red"
npm run typecheck || fail "typecheck red"
npm run lint || fail "lint red"
echo OK
    </automated>
  </verify>
  <done>One `unifiedCsvFinalizeHandler` serves both flows via `terminalStatus` and `logPrefix` fields on the same typed args object; `contributionCsvFinalizeHandler` is gone; Tests 8a/8b/8c pass (unchanged if the cheap shape was taken, rewritten to pin the same properties otherwise — never deleted); both `logPrefix` literals still appear in the output; audit-coverage is green with any `@audit-skip:` pragma relocated with its code; the doc-comment describes the collapsed handler and still names its gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated browser → `POST /api/strategies/csv-finalize` | Untrusted payload including `asset_class`, which decides the annualization clock for every KPI. |
| Vercel route → PostgREST (`supabase.rpc`) | postgrest-js RESOLVES on transport failure rather than rejecting; the RPC POST is not in `RETRYABLE_METHODS`, so the commit outcome after a transport failure is genuinely unknowable. |
| route → end user (error envelope) | The 500 envelope's `human_message` is what the wizard renders verbatim for `KNOWN_CSV_FINALIZE_CODES` members. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.1-23 | Tampering | `applyCsvMetadataUpdate` on an echoed outcome | high | mitigate | A4: required `fresh` field, metadata UPDATE skipped on `!fresh`. Without it a retry with a flipped `asset_class` rewrites a committed strategy's √252-vs-√365 clock while its stored KPIs keep the old one — a silently wrong Sharpe on a verified-track-record product. |
| T-146.1-24 | Repudiation | The route asserts a rollback it did not observe | high | mitigate | A3: explicit 5-char code-length discrimination; classes 2 and 3 get commit-agnostic copy in the already-approved `CSV_SUBMIT_FAILED` voice; tests assert on ABSENCE of the removed claim. |
| T-146.1-25 | Repudiation | Two distinct failure sub-classes merged in one Sentry bucket | medium | mitigate | Distinct `step` tag for classes 2/3, so the honest arm's firing rate is measurable. |
| T-146.1-26 | Information Disclosure | A credential reaching the Vercel log through a comment-driven "cleanup" | medium | mitigate | The `:1005-1010` correction fixes the false claim and KEEPS `scrubSeamError`; `<verify>` runs `seam-log-coverage.test.ts` and `seam-redaction.test.ts`. |
| T-146.1-27 | Repudiation | A mutation escaping the audit law by moving enclosing function | medium | mitigate | C2 relocates any `@audit-skip:` pragma with its code and runs `audit-coverage.test.ts` explicitly; the gate is a brace-balance walk over the ENCLOSING body. |
| T-146.1-28 | Denial of Service | Roster/vocabulary drift from a newly minted code | low | mitigate | One code kept; `<verify>` runs both invariant suites and grep-refuses the likely new names. |
| T-146.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed. Package Legitimacy Audit recorded not-applicable in `146.1-RESEARCH.md`. |
</threat_model>

<verification>
1. Each task's `<verify>` exits 0.
2. Full CSV-finalize suite green: `npx vitest run src/__tests__/csv-finalize-cross-submission-merge.test.ts src/__tests__/csv-finalize-c14-regression.test.ts src/__tests__/csv-validate-route.test.ts src/__tests__/csv-finalize-rpc.test.ts src/__tests__/audit-coverage.test.ts --no-file-parallelism`.
3. Invariant suites green: `npx vitest run src/lib/seam-wire-vocabulary.invariant.test.ts src/lib/wizardErrors.invariant.test.ts src/lib/seam-log-coverage.test.ts src/lib/seam-redaction.test.ts --no-file-parallelism`.
4. `npm run typecheck`, `npm run lint` green.
5. `npm test` green before hand-off to wave 4. ⚠️ Local flakes → `--no-file-parallelism`. A CI-only failure is NOT a flake (CI is Node 22, local is Node 25) — reproduce with `PATH=/opt/homebrew/opt/node@22/bin:$PATH`.
6. Coverage: `npm run test:coverage` must still clear the blocking thresholds (lines 82 / statements 80 / functions 74 / branches 72). A3 adds branches; if branch coverage dips below 72 the CI `frontend-coverage` job fails, so the new class-2/class-3 fixtures are not optional decoration.
</verification>

<success_criteria>
- [ ] `FinalizeAtomicOutcome`'s ok-arm carries `fresh: boolean` as a REQUIRED field, set explicitly at both construction sites.
- [ ] `applyCsvMetadataUpdate` is not invoked on an echoed outcome; asserted on the `.update()` spy, never on the return value or on source text.
- [ ] The echo case with a changed `asset_class` issues zero updates.
- [ ] `enqueueCsvAnalyticsAfter`'s gate is unchanged, and the `fmt='trades'` no-enqueue behaviour is untouched.
- [ ] The fold-failure arm discriminates classes by `typeof error?.code === "string" && error.code.length === 5`, never by truthiness.
- [ ] Class 1's sentence is byte-identical to HEAD; classes 2 and 3 carry commit-agnostic copy and a distinct Sentry step tag.
- [ ] Exactly one error code (`CSV_FINALIZE_FAIL`) is emitted by that arm; both roster invariant suites green with no roster edits.
- [ ] Tests assert ABSENCE of the removed claim for classes 2/3 and PRESENCE of the unchanged sentence for class 1.
- [ ] A dated provenance comment names the removed claim, the three classes, `wizardErrors.ts:1971-1985`, the `RETRYABLE_METHODS` fact, and the 42501→401 deferral with its reason.
- [ ] `route.ts:1005-1010`'s false forwarding claim is corrected; `scrubSeamError` and its rationale are intact; seam log/redaction gates green.
- [ ] Exactly one `unifiedCsvFinalizeHandler` declaration; `contributionCsvFinalizeHandler` gone; Tests 8a/8b/8c pass and were rewritten (not deleted) if they moved.
- [ ] Both `logPrefix` literals still appear in the emitted output, or the SUMMARY records the repo-wide grep that showed nothing pins them.
- [ ] `audit-coverage.test.ts` green with any `@audit-skip:` pragma relocated with its code.
- [ ] Three commits, one per task, in the order A4 → A3 → C2.
</success_criteria>

<falsifiability_ledger>
| # | Success criterion | What makes it go RED | Named neuter — RUN IT, then restore |
|---|---|---|---|
| F-05-1 | An echo issues no metadata UPDATE | Set `fresh: true` unconditionally at the resolve echo return | **Neuter A4-ALWAYSFRESH**: the echo fixture must show `applyCsvMetadataUpdate` invoked (`updateCalls` length 1), which the passing test forbids. ⚠️ If the test asserted `fresh === false` instead of the spy, this neuter still REDs but proves nothing about the side effect — that is Pitfall 4, testing the helper instead of the wiring. |
| F-05-2 | The compiler names every construction site | Make `fresh` optional | **Neuter A4-OPTIONAL**: `fresh?: boolean` + remove one construction site's assignment. `npm run typecheck` must stay GREEN — which is exactly the silence the required field prevents. Record that green as the evidence FOR requiredness. |
| F-05-3 | The fresh path still updates metadata | Skip the UPDATE unconditionally | **Neuter A4-SKIPALL**: the fresh-create positive case must RED. Without it, "skip on echo" is indistinguishable from "never update". |
| F-05-4 | Classes 2 and 3 are separated from class 1 | Replace the length check with `if (error?.code)` | **Neuter A3-TRUTHY**: the class-2 fixture (`code: ""`, `status: 0`) must flip into the class-1 copy and RED on the absence assertion. `code: ""` is falsy — this is the single most likely wrong implementation. |
| F-05-5 | Class 2/3 copy is honest | Collapse the branch back to the single old sentence | **Neuter A3-REVERT**: the class-2 and class-3 fixtures must RED **on the absence assertion**. ⚠️ Presence-only assertions false-PASS here whenever both sentences are present — same class as the `%5000%` gate. |
| F-05-6 | Class 1 keeps its true claim | Make class 1 commit-agnostic too | **Neuter A3-VAGUE**: the class-1 fixture must RED on the presence assertion. Without this arm, "make everything vague" passes. |
| F-05-7 | One code, no roster churn | Mint a second code | `seam-wire-vocabulary.invariant.test.ts` REDs by name and `wizardErrors.invariant.test.ts` REDs on `EXPECTED_TABLE_SIZE`. Minting IS the neuter; both suites are in `<verify>`. |
| F-05-8 | The collapsed handler keeps its pinned shape | Convert to two positional args | **Neuter C2-ARITY**: Test 8c must RED on the declaration regex. |
| F-05-9 | The explicit series field survives | Replace `dailyReturnsSeries: CsvDailyReturnRow[]` with a spread or `any` | **Neuter C2-SHAPE**: Test 8b must RED. |
| F-05-10 | The two handlers had not drifted before collapsing | — | Falsified by measurement: re-run the comment-stripped difflib on the CURRENT file before task 3 and record the output in the SUMMARY. A non-empty diff means tasks 1/2 drifted the arms and the collapse would silently adopt one of them. An unrecorded diff is an unmeasured claim. |
| F-05-11 | Audit coverage survives the merge | Delete an `@audit-skip:` pragma while merging | **Neuter C2-PRAGMA**: `audit-coverage.test.ts` must RED naming the unexempted mutation. |
| F-05-12 | Branch coverage holds | — | `npm run test:coverage` below 72% branches fails the blocking `frontend-coverage` CI job. A3 adds two branches; shipping them without fixtures is the way this criterion goes red silently in CI rather than locally. |
</falsifiability_ledger>

<output>
Create `.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-05-SUMMARY.md` when done.

The SUMMARY MUST record, per Falsifiability Ledger row, OBSERVED (with the verbatim failing
assertion) or NOT OBSERVED. Every neuter here is runnable locally — `NOT OBSERVED` means skipped
and must be stated, not omitted. It MUST also record the pre-collapse difflib output (F-05-10) and
the repo-wide `logPrefix` literal grep result.
</output>
