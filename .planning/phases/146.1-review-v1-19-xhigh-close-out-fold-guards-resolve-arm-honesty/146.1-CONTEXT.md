# Phase 146.1 — REVIEW: v1.19 xhigh close-out — CONTEXT

**Origin: founder-directed xhigh code review of the ENTIRE v1.19 milestone
(`43069db9..4e3effb0`, PRs #687/#688/#689/#690), run 2026-08-18. 10 finder angles,
18 individually-verified candidates, 15 confirmed findings. Founder instruction verbatim:
"fix what's easy and put the rest into a new phase plus the deferred items that fit the
v1.19 topic." The easy fixes shipped same-day on `fix/v1.19-review-easy`; THIS phase holds
everything else. Phase number is 146.1 because 147 was consumed by v1.17 (SCEN-01).**

## Already fixed (2026-08-18, same-day easy batch — do NOT re-plan)

- Dead `CSV_DUPLICATE_SESSION` retry fence + its vacuous predicate test DELETED
  (`CsvSubmitStep.tsx`; the code was in no gate set and had zero emitters).
- `TestTerminalizerWindowInvariant` added (`test_main_worker.py`): couples the 4h arm-A
  window (20260817120000) to `p_batch_size × max(TIMEOUT_PER_KIND)` — observed RED at a
  simulated 5h ceiling.
- The three false "nobody forwards X-User-Access-Token" comments corrected
  (`process-key-client.ts` ×2, `process_key.py`) — they now name the two live forwarders.
- Fold self-verify check (d) rewritten from unbounded `%5000%` LIKE to a comment-stripped
  bounded regex — proven on TEST: current body PASSes, widened `50000` RAISEs,
  guard-deleted-comment-kept RAISEs (the old check false-PASSed that exact case).

## The roster (severity order; file anchors are at 4e3effb0)

### A — live user-facing / data-integrity (the phase's core)

- **A1. Fold commits zero-dailies strategies on `p_rows` NULL or `[]`**
  (`20260819120000:270`; every rows/fmt guard is non-NULL-safe, MEASURED on a throwaway
  postgres:16 — `p_rows := NULL` commits a zero-dailies strategy, and `p_fmt <> 'trades'`
  is itself NULL-unsafe so the fix must use `IS DISTINCT FROM`). Casts also accept
  `"NaN"`, `"Infinity"`, `1e300`, `9999-12-31`; `NOT (x BETWEEN -10 AND 10)` catches
  NaN + ±Inf + magnitude in one predicate. Phase 143's sweep structurally cannot heal the
  result (EXISTS(csv_daily_returns) conjunct).
  ⚠️ **SEVERITY CORRECTED 2026-08-18 — the roster's original claim was WRONG.** I wrote
  that `[]`+`daily_returns` is reachable via the route's legacy-client arm. It is NOT:
  `route.ts:1337-1352` already 400s `(fmt==='daily_returns'||fmt==='daily_nav') &&
  dailyReturnsSeries.length===0` (VERIFIED by reading it). A1's real reachability is
  **authenticated direct-RPC, own-tenant only** — the SECDEF body is the boundary such a
  caller cannot bypass, which is why it is still worth closing, but this is NOT a live
  user-facing break and must not be planned as one.
  Fix: RAISE 22023 on NULL, and on empty when `p_fmt IS DISTINCT FROM 'trades'`.
  ⚠️ `CREATE OR REPLACE`, NOT a DROP (house law per PATTERNS.md) — re-base on the LATEST
  definition after grepping ALL migrations touching this function; do NOT copy the fold's
  DROP-and-restate-grants prose, it would be false. Recommended stamp `20260819130000`
  (PROD tip VERIFIED via MCP 2026-08-18 = `20260819120000`, so this sorts above it and
  stays valid past the 2026-08-19 12:00 UTC expiry).
  Absorbs TODOS "Fold value guards" (NaN/Infinity/far-future) + "fmt-blind empty-rows".
- **A2. Resolve arm never compares terminal status** (`route.ts:876`): the unique index
  spans BOTH flows (source='csv' for manager and contribution) and `wizardSessionId`
  restores from one shared localStorage key gated only on source — so a manager-flow
  resubmit can 200-echo a committed 'private' contribution row that then never enters the
  admin review queue (and vice versa). Fix: refuse on
  `existingRow.status !== args.terminalStatus`.
- **A3. "Nothing was saved — rolled back completely" is asserted for arms where the
  commit outcome is unknowable or provably COMMITTED** (`route.ts:627`): postgrest-js
  resolves transport failures as `{error:{code:""}, status:0}` (POSTs never retry), and
  the TS-13 lost-id 2xx arm is routed into the same copy. SEAMUX-04
  (`wizardErrors.ts:1971`) already concedes the claim is unmakeable client-side. Fix:
  split transport/lost-id onto commit-agnostic copy. Absorbs TODOS "Copy honesty on the
  fold-failure arm sub-classes" (+ its 42501→401 consideration).
- **A4. Echoed 23505 outcomes still run `applyCsvMetadataUpdate` with THIS request's
  metadata and skip re-enqueue for fmt='trades'** (`route.ts:1524`): a same-series retry
  with a changed asset_class overwrites the committed row's class while stored KPIs keep
  the OLD √252-vs-√365 clock. Fix: add a fresh-vs-echoed discriminator to
  `FinalizeAtomicOutcome`; skip (or re-enqueue after) metadata UPDATE on echoes.

### B — structural / latent (two contradict declared milestone goals)

- **B1. Python rate limiting is still bypassable by omission** (`rate_limit.py:218`
  posture): no default_limits, no middleware, the absence-tripwire was deleted in 146-02,
  and every surviving gate is self-referential over `_route_limits`. Fix: enumerate
  `app.routes` and assert each is limited or in an explicit quarantine roster (the TS
  side's NO_LIMITER_QUARANTINE equivalent). This is RATE-03's "a new route cannot
  silently bypass" made true on the Python side.
- **B2. X-User-Access-Token drop-vs-wire adjudication** (`process-key-client.ts:649`):
  keys/sync + verify-strategy forward a LIVE user JWT into a service whose only reader
  has zero callers — pure exposure surface — while the Phase 140.2 obligation records
  forwarding as intended. DECIDE: stop forwarding (and amend the obligation) or wire the
  user-scoped client. Comments were corrected same-day; the decision was NOT made.
- **B3. Any-23505 enters the resolve arm undiscriminated** (`route.ts:581`):
  `pgConstraintName()` exists and is used by create-with-key + composite/add-key; the
  fold's dailies INSERT has no ON CONFLICT, so a duplicate-date payload reaching the RPC
  raises a dailies-index 23505 → wrong arm → 503 retry-invitation for a permanent defect.
  Enter the resolve arm only for `strategies_user_wizard_session_source_uniq`.
  Absorbs TODOS "23505 second source".
- **B4. Terminalizer × sweep do not compose** (`20260817120000:56` header,
  TODOS RESEARCH §6 residual): a chain-mid orphan's `failed_final` row permanently trips
  the sweep's ANY-status NOT EXISTS conjunct — recovered by nobody, no user surface
  unless the user is still on the page (15-min amber backstop).
  ⛔ **THE ROSTER'S PROPOSED FIX IS WRONG — do not "widen the conjunct to no NON-TERMINAL
  row".** That blanket widening REDDENS two shipped gates:
  `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` arms C2 (`:558-561`) and C3
  (`:563-568`) assert that `failed_final`-only and `done`-only strategies are NOT healed,
  and C2 names the real failure mode — *"a settled permanent failure belongs to nobody;
  re-enqueueing it turns it into an hourly retry loop with no attempt ceiling."*
  ✅ **Use the narrow predicate instead** — readmit ONLY terminalizer-produced orphans,
  keyed on the marker the terminalizer itself stamps (`20260817120000:622,641`):
      AND NOT (cj.status = 'failed_final'
               AND cj.last_error LIKE 'orphaned_running_reaped:%')
  This keeps C2/C3 GREEN and needs a NEW must-heal arm C4 so C2/C4 form a discriminating
  pair. ⚠️ New migration (unschedule-then-schedule); ⛔ do NOT edit `20260816140000` in
  place; it names its own three sibling gates (`:836-840`) that move same-commit.
  Recommended stamp `20260819130500`.
  ⚠️ **MEASURED ON PROD 2026-08-18 (MCP): live population is ZERO.** No row carries the
  `orphaned_running_reaped:` marker — the terminalizer has never fired in production since
  it shipped; `running`=0 and `pending`=0. All 122 `failed_final` rows are genuine job
  failures (expired API keys, insufficient trade history, MT5 unreachable, geo-blocks,
  code bugs), which is exactly the population C2/C3 exist to protect. **B4 is preventive,
  not a live break — plan it as hardening, and say so.** Its reachable population is
  further narrowed because the sweep's `strategy_analytics` conjunct already excludes most
  terminalized orphans once Phase 142's reaper runs.
- **B5. `csv-finalize-rpc.test.ts` still calls the DROPped `finalize_csv_strategy`**
  (6 sites, all skipIf-live): can never pass again against a live DB.
  ⚠️ **CORRECTED 2026-08-18:** the "nine 22023 assertions" figure in the review was a
  `grep -c` of the string — there are **three** real guard tests. ⛔ And the file also
  holds **12 pure-unit tests that DO run in CI**, including live `asset_class` coverage —
  so **deleting the file is not an option**; re-point it. The three guard cases pair
  naturally with A1's new raises in `supabase/tests/test_csv_finalize_atomic_fold.sql`.
  Absorbs TODOS "Re-point csv-finalize-rpc.test.ts" (+ stale cover-citation in
  strategy-verifications-rls.test.ts:17).

### C — adjudication / hygiene

- **C1. Interior-values echo residual** (`route.ts:856`): equal count + boundaries with
  different interior values echoes ok:true against the OLD series — a mid-file correction
  silently discarded. FOUNDER CALL: content hash over the payload vs copy that admits the
  echo did not re-verify interiors. (The count-mismatch case already chose refusal on the
  same "fabricated success is the worst outcome" logic.)
- **C2. The two ~90-line handlers are token-identical** except terminalStatus/logPrefix
  (`route.ts:1470/:1579`, verified exhaustively): collapse to one parameterized handler;
  MUST update the source-shape/audit-coverage gates pinning the two signatures
  (`:1466-1469`) same-commit.
- **C3. TEST-project sweep cron residual** (mitigated, tracked TODOS D-13): a future
  non-terminal dailies-bearing seed variant would feed one permanently-unclaimable
  'pending' row into the 05:30 flake. Document-or-guard; lowest priority.
- **C4. Remaining v1.19-topical TODOS items to absorb**: resolve-arm fail-closed pins
  (refetchErr / no-committed-row, RED under step-tag neuters); fold gate Part 3d
  (`p_terminal_status='published'` → 22023); types regen post-fold + delete the
  cast-through-unknown; Python tombstone `CSV_FINALIZE_MOVED` arm; service_role
  default-ACL vs header claim (REVOKE or document); RT-3 persist the burned csv-submit
  signature in the signed saveWizardState envelope; stale-comment batch (cosmetic,
  grouped).

## Explicitly OUT of scope

- **RATE-04 limit-value retuning** — founder territory (D-146-4); the 5 candidates stay
  in TODOS with their measured numbers until the founder rules.
- The 274 mypy errors in `tests/test_match_router.py` — general hygiene, not JOB/RATE
  (TODOS, Phase 146 close-out notes).
- v1.18 founder-gated MT5 work — untouched by this phase.

## Constraints (standing)

- Migrations auto-apply to PROD on merge; A1/B4 migrations need migration-reviewer +
  TEST rehearsal first. The 20260819120000 sort-above rule expires 2026-08-19 12:00 UTC.
- Every new/changed gate observed RED under a documented neuter.
- Tests: economic/behavioral oracles, never the impl's own formula.

## Sequencing (from RESEARCH, measured — not negotiable)

⛔ **A2 + A3 + A4 + B3 + C2 all edit `src/app/api/strategies/csv-finalize/route.ts`.**
ONE agent, SERIAL, in the order **B3 → A2 → A4 → A3 → C2**. Do not parallelise them
across agents: concurrent agents in one checkout share the git index, and file-disjoint
is not the same as isolated.

⛔ **A1 + B5 + the new fold-gate arms all edit
`supabase/tests/test_csv_finalize_atomic_fold.sql`.** ONE plan.

✅ Only three lanes are safely parallel: the **A1 migration**, **B1** (Python), **B4**
(sweep migration).

## Founder calls — surfaced, NOT decided by the planner

- **C1 (interior-values echo).** A content hash needs a THIRD migration plus a backfill.
  Research recommends shipping honest copy now and filing the hash as follow-up. The
  planner must NOT bake in the hash; plan the copy and leave the hash as a named option.
- **B2 (X-User-Access-Token).** Confirmed: the Python reader has ZERO production callers
  and a gate at `test_process_key.py:2220` actively pins the non-use. Research recommends
  dropping the forward from both routes and amending the 140.2 obligation SAME-COMMIT
  (an obligation that survives the code it justifies is how the false comments arose).
- **RATE-04 limit VALUES** remain founder territory — out of scope entirely.
