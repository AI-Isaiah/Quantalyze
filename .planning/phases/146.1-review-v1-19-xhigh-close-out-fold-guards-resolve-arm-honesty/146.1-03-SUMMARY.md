# Plan 146.1-03 — SUMMARY (B4 terminalizer × sweep composition + C3)

**Status: code COMPLETE, statically verified. Composition NOT proven — that is 146.1-08's job.**

⚠️ **Authorship note.** The assigned executor stalled at task 3 (600s no progress, laptop
power loss) with **all its work still UNCOMMITTED and the migration file UNTRACKED** — one
`git clean` would have destroyed it silently. The orchestrator backed the files up,
committed them (`039168da`), inspected every artifact, ran the gates centrally, and wrote
this SUMMARY. The "verified" column is the orchestrator's own measurement.

## Delivered

| Item | Artifact |
|---|---|
| B4 | `supabase/migrations/20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql` |
| B4 gates | new must-heal arm C4 in `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` + the two sibling gates |
| C3 | dated TEST-project sweep-cron residual section in the gate header |

## Orchestrator-measured verification

- **The NARROW marker predicate is what shipped**: the exemption keys on
  `cj.last_error LIKE 'orphaned_running_reaped:%'`, the marker the terminalizer itself
  stamps.
- **ZERO executable blanket widenings.** Checked BOTH spellings — `status IN ('pending',…)`
  and `status NOT IN ('failed_final','done',…)`. Neither appears in executable code. This
  matters because a blanket widening reddens shipped arms C2 and C3, and C2 names the
  reason: re-enqueueing a settled permanent failure creates an hourly retry loop with no
  attempt ceiling.
- **Cron safety**: exactly ONE `cron.unschedule` call, naming only
  `reconcile_dropped_enqueue_sweep`. Zero references to job 9 or any other cron. No
  `pending` row is ever deleted.
- **C2/C4 form a discriminating pair** — C2 proves a settled failure is NOT healed, C4
  proves a terminalizer orphan IS. Either alone is not evidence.
- C3 residual section present and DATED 2026-08-18.
- tsc clean · vitest incl. `reconcile-dropped-enqueue-sweep.test.ts` passed · pytest 137
  passed · mypy --strict clean.

## The claim this migration makes, and its expiry

The header records, as a DATED claim: **PROD population is ZERO** — measured via MCP on
2026-08-18, no row carries the `orphaned_running_reaped:` marker, the terminalizer has
never fired in production, `running`=0, `pending`=0, and all 122 `failed_final` rows are
genuine job failures (expired API keys, insufficient trade history, MT5 unreachable,
geo-blocks, code bugs) — precisely the population C2/C3 exist to protect.

**B4 is therefore PREVENTIVE HARDENING, not the repair of a live break.** 146.1-08 re-counts
at merge time and must force a prose correction if the number moved. A dated claim that is
never re-measured becomes folklore.

## NOT verified — do not read this plan's completion as proof

⛔ **No SQL arm has been EXECUTED and the migration has never run.** No DB access here.
Deferred to **146.1-08** by name: B4-BLANKET (the decisive one — arms C2 AND C3 must BOTH
red under a blanket widening; that double-RED is the evidence for the narrow predicate, not
a preference), B4-MARKER, B4-C4-HEAL, B4-COMMENT, B4-CRON-TICK.

⛔ Merging `supabase/migrations/**` to main AUTO-APPLIES to PROD.
