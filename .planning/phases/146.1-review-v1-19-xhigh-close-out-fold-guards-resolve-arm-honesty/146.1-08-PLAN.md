---
phase: 146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty
plan: 08
type: execute
wave: 6
depends_on: ["146.1-01", "146.1-02", "146.1-03", "146.1-04", "146.1-05", "146.1-06", "146.1-07"]
files_modified:
  - .planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-08-SUMMARY.md
  - TODOS.md
autonomous: false
requirements: [A1, B4]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Both new migrations have been APPLIED to the TEST project and both SQL gate files have been EXECUTED there and passed, BEFORE the PR is merged."
    - "Every migration-dependent neuter deferred by 146.1-01 and 146.1-03 has been RUN on TEST and observed RED, then restored."
    - "PROD's `schema_migrations` remote tip has been RE-CONFIRMED at merge time and is still below both new stamps."
    - "The `orphaned_running_reaped:` population has been RE-COUNTED on PROD at merge time, so the ZERO-population claim written into 146.1-03's headers is either confirmed or corrected before it ships."
    - "After merge, both migrations are confirmed APPLIED on PROD and one real tick of the re-registered sweep has been inspected in `cron.job_run_details`."
  artifacts:
    - .planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-08-SUMMARY.md
  key_links:
    - "Merging supabase/migrations/** to main AUTO-APPLIES to PROD. There is no deploy step and no prompt. This plan IS the only thing between a written migration and the production database."
    - "The research agent had NO DB access and ran zero SQL. Every DB-dependent claim in 146.1-RESEARCH.md is tagged [ASSUMED] and listed in its Assumptions Log. This plan discharges them."
    - "MCP `apply_migration` stamps `now()`. Rehearse in a way that does not create filename drift between the repo and the TEST project."
---

<objective>
The pre-merge and post-merge gate for the two migrations this phase ships. **This plan cannot be
delegated to a worktree agent.** TEST (`qmnijlgmdhviwzwfyzlc`) and PROD (`khslejtfbuezsmvmtsdn`)
are reachable only through Supabase MCP in the ORCHESTRATOR session.

⛔ **Merging `supabase/migrations/**` to `main` AUTO-APPLIES to PROD.** No deploy step, no prompt.
This plan is the only thing standing between a written migration and the production database.

It also discharges the research phase's Assumptions Log. `146.1-RESEARCH.md` was written with **no
DB access and zero SQL executed** — assumptions A1 (PROD remote tip), A2 (`20260819120000` applied
to TEST), A4 (B4's reachable population), A5 (the `public.compute_jobs` occurrence count) and A8
(every quoted census is dated, not re-measured) are all still open, and several DB-requiring
neuters were explicitly deferred here by 146.1-01 and 146.1-03.

Purpose: a dated claim is not a fact. Everything this phase asserts about a live database gets
re-measured here, before the merge that cannot be undone without another forward migration.
Output: a receipts document, and either a green merge or a named blocker.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-CONTEXT.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-RESEARCH.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-01-SUMMARY.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: pre-apply census on PROD and TEST (orchestrator, Supabase MCP)</name>
  <what-built>Two forward migrations written but NOT yet applied anywhere: `20260819130000_csv_finalize_fold_input_guards.sql` (146.1-01) and `20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql` (146.1-03). Two SQL gate files extended with new Parts and a new arm C4, executed nowhere.</what-built>
  <how-to-verify>
    ORCHESTRATOR SESSION ONLY, via Supabase MCP. Do not delegate. Run these reads BEFORE applying
    anything, and paste every result into the SUMMARY.

    **PROD (`khslejtfbuezsmvmtsdn`) — read-only:**
    1. `select max(version) from supabase_migrations.schema_migrations` — RE-CONFIRM the tip. It was
       `20260819120000` on 2026-08-18. If it is now HIGHER than either new stamp, STOP: an
       out-of-band migration landed and the backdated guard in `migration-policy.yml` will trip.
    2. `select count(*) from public.compute_jobs where last_error like 'orphaned_running_reaped:%'`
       — RE-COUNT. It was ZERO on 2026-08-18. If it is now non-zero, the "ZERO live population"
       sentence written into 146.1-03's migration header and gate prose is STALE and MUST be
       corrected before merge.
    3. `select status, count(*) from public.compute_jobs group by status` — the surrounding census.
    ⛔ READ ONLY on PROD. Never DELETE a `pending` row, never `cron.unschedule(9)`, never touch the
    `process_key_unified_backbone` flag row.

    **TEST (`qmnijlgmdhviwzwfyzlc`):**
    4. `select count(*) from pg_proc where proname = 'finalize_csv_strategy_with_returns'` — confirm
       it is 1. If it is 0, `20260819120000` is NOT applied to TEST (research assumption A2) and
       146.1-01's `CREATE OR REPLACE` will fail there; resolve that first.
    5. `select count(*) from public.compute_jobs where last_error like 'orphaned_running_reaped:%'`
       — the TEST count. `146.1-RESEARCH.md` §4.7 warns that if TEST's ~396 stuck `running` rows
       (a DATED 2026-08-17 claim from `20260817120000:511-514`, not a fact) were ever terminalized,
       the narrow fix could make a few hundred TEST strategies sweep-eligible at ≤25/hour, feeding
       the 05:30 UTC backlog. Record the number.
    6. `select jobid, jobname, schedule from cron.job where jobname = 'reconcile_dropped_enqueue_sweep'`
       — the pre-change jobid, so the post-change jobid change is observed rather than assumed.
  </how-to-verify>
  <resume-signal>Paste all six results, then reply "census-clear" (proceed), or "census-stale: &lt;what changed&gt;" (a prose correction is required in 146.1-01/03 before proceeding), or "census-blocked: &lt;why&gt;".</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Rehearse both migrations on TEST and RUN both SQL gate files there</name>
  <files>.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-08-SUMMARY.md</files>
  <precondition>The census checkpoint resolved to `census-clear`, or to `census-stale` AND the required prose corrections have been made and committed in 146.1-01 / 146.1-03.</precondition>
  <precondition>Supabase MCP is reachable from THIS session for both the TEST and PROD project refs. A worktree agent cannot satisfy this; if MCP is unavailable, HALT and report — do NOT merge on the strength of static checks alone.</precondition>
  <action>
Apply `20260819130000` then `20260819130500` to TEST, in stamp order, via Supabase MCP.

⚠️ **MCP `apply_migration` stamps `now()`**, which creates filename drift between the repo and the
TEST project's `schema_migrations`. Rehearse in whichever way avoids that drift for this
repository's convention — and whatever you choose, RECORD what TEST's `schema_migrations` now
contains for these two entries, so the next reader knows exactly what TEST's history looks like and
does not mistake drift for a missing migration.

Watch each migration's own self-verify block. Both carry one (146.1-01 STEP 4, 146.1-03 STEP 2) and
a self-verify that RAISEs is the migration refusing itself — that is a PASS of the gate and a FAIL
of the migration. Report the exact RAISE text; do not retry blindly.

Then EXECUTE both gate files against TEST under `ON_ERROR_STOP=1`:
`supabase/tests/test_csv_finalize_atomic_fold.sql` (Parts 1-5 unchanged, plus the new 3d, 6a-6i,
7a-7c) and `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` (Parts 1-4, arms A/B/C
including the new C4). This is the FIRST real execution of everything 146.1-01 and 146.1-03 wrote —
until now they were only statically checked.

Capture every `RAISE NOTICE ... OK` line. A gate file that reaches its final `ALL PASS` notice has
passed; a file that stops early has not, regardless of what came before it.

⛔ If a Part fails, fix the MIGRATION or the GATE on its merits. Never relax an assertion to make a
run green — the gates in this phase exist because three substring gates in this repo have already
false-PASSed.

Also confirm the re-registered cron job: `select jobid, jobname, schedule from cron.job where
jobname = 'reconcile_dropped_enqueue_sweep'`. The jobid WILL have changed
(`20260817120000:422` records that `unschedule` + `schedule` changes it); the jobname and the
`35 * * * *` schedule must not have.
  </action>
  <verify>
    <automated>
# Orchestrator-run receipts check over the SUMMARY this plan writes.
S=.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-08-SUMMARY.md
fail(){ echo "FAIL: $1"; exit 1; }
test -f "$S" || fail "no receipts document"
grep -q '20260819130000' "$S" || fail "A1 migration application not recorded"
grep -q '20260819130500' "$S" || fail "B4 migration application not recorded"
grep -q 'test_csv_finalize_atomic_fold: ALL PASS' "$S" || fail "the fold gate did not reach its final ALL PASS notice"
grep -qE 'test_reconcile_dropped_enqueue_sweep.*(ALL PASS|Part 4 OK)' "$S" || fail "the sweep gate did not reach its final notice"
grep -q 'arm C4' "$S" || fail "the new must-heal arm was not exercised"
grep -qE 'Part 6[a-i]' "$S" || fail "the new fold Parts were not exercised"
grep -qi 'jobid' "$S" || fail "the cron jobid change was not observed"
echo OK
    </automated>
  </verify>
  <done>Both migrations applied cleanly to TEST with their self-verify blocks passing; both gate files ran under `ON_ERROR_STOP=1` and reached their final ALL PASS notices; the new Parts 3d/6a-6i/7a-7c and arm C4 all executed; the cron job's new jobid, unchanged jobname and unchanged schedule are recorded; TEST's `schema_migrations` state for the two entries is recorded verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Run every DB-dependent neuter that 146.1-01 and 146.1-03 deferred here</name>
  <files>.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-08-SUMMARY.md</files>
  <read_first>
    - `146.1-01-SUMMARY.md` and `146.1-03-SUMMARY.md` — each carries an explicit list of ledger rows marked `DEFERRED TO 146.1-07`/`146.1-08`. That list is this task's worklist; ⚠️ if either SUMMARY is missing the list, the plan's own output contract was not met and the neuters must be re-derived from the plans' Falsifiability Ledgers.
  </read_first>
  <action>
⭐ **A test that CANNOT FAIL is worse than none.** Every neuter below is run on TEST by applying a
deliberately broken variant, observing the RED, then restoring the correct definition. ⛔ NEVER on
PROD. ⛔ NEVER leave a neutered definition behind — re-apply the correct migration after each and
re-run the gate to confirm GREEN before moving on.

From 146.1-01's ledger, at minimum:
- **A1-NULL** — remove the NULL guard; Part 6a must RAISE.
- **A1-FMT** — swap `IS DISTINCT FROM` back to `<>`; Part 6c must RAISE while 6a/6b stay green.
- **A1-VALUE** — widen the magnitude bound. ⚠️ **TWO probes required**: Part 6e (`1e300`) must RED,
  and note that Part 6d (`"NaN"`) will still PASS under a widened bound because NaN fails ANY
  `BETWEEN`. A NaN-only neuter false-PASSes; record BOTH observations.
- **A1-DUP** — remove the duplicate-date guard; Part 6h must RED **with SQLSTATE 23505 as the
  observed value**, proving the assertion is exact-code and not class-pinned.
- **A1-ORDER** — move a guard below the strategies INSERT; the affected Part's row-count arm must
  RED.
- **A1-DENYALL** — make a guard unconditional; Part 6i (the unwrapped conforming payload) must RED
  with a raw SQLSTATE under `ON_ERROR_STOP=1`.
- **A1-TRADES** — scope the empty guard to all fmts; the pre-existing Part 4 must RED.
- **A1-SUBSTR** — widen the 5000 literal to 50000; the STEP 4 self-verify must RAISE. Under a
  substring form it PASSes, which was MEASURED on TEST on 2026-08-18.
- **C4-3D** — remove the terminal-status guard; Part 3d must RED.

From 146.1-03's ledger, at minimum:
- **B4-REVERT** — restore the bare `NOT EXISTS`; arm C4 must RED while C1/C2/C3 stay GREEN.
- **B4-BLANKET** — apply the blanket non-terminal widening; arms **C2 AND C3 must BOTH RED**. This
  is the measurement that refutes the roster's original proposed fix. Record both failures verbatim
  — it is the evidence for the adjudication, and without it the narrow fix is a preference rather
  than a finding.
- **B4-ANYTEXT** — change the marker `LIKE` to `last_error IS NOT NULL`; arm C2 must RED.
- **B4-COMMENT** — delete the marker clause but keep its comment; the STEP 2 marker anchor must
  RAISE. If it PASSes, the anchor matched the comment and is the `%5000%` failure class.
- **B4-BOUND** — widen `LIMIT 25` to `LIMIT 2500`; Part 4 must RED and the word-bounded regex must
  RAISE.

For EACH: record the neuter name, the exact statement changed, the verbatim RAISE/failure text, and
the confirmation that the correct definition was restored and re-verified GREEN.

⚠️ A neuter that does NOT produce a RED is a finding, not a nuisance. Do not make the neuter more
aggressive until it fails — fix the GATE, re-run the full file, and record what was wrong. A gate
that needed a bigger break to notice was not watching the thing it claimed to watch.
  </action>
  <verify>
    <automated>
S=.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-08-SUMMARY.md
fail(){ echo "FAIL: $1"; exit 1; }
for n in A1-NULL A1-FMT A1-VALUE A1-DUP A1-ORDER A1-DENYALL A1-TRADES A1-SUBSTR C4-3D B4-REVERT B4-BLANKET B4-ANYTEXT B4-COMMENT B4-BOUND; do
  grep -q "$n" "$S" || fail "neuter $n not recorded"
done
grep -qi 'restored' "$S" || fail "no restoration confirmations recorded - a neutered definition may still be live on TEST"
grep -qi 'NaN' "$S" || fail "the two-probe A1-VALUE observation is not recorded"
echo OK
    </automated>
  </verify>
  <done>All fourteen named neuters were RUN on TEST, each producing a RED with its verbatim failure text captured, each followed by restoration and a re-run confirming GREEN; the A1-VALUE two-probe result is recorded explicitly including the NaN false-PASS behaviour; the B4-BLANKET double-RED (C2 and C3) is recorded as the evidence refuting the roster's original fix; no neutered definition remains live on TEST.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: merge gate — the migration auto-applies to PROD on merge</name>
  <what-built>Everything: two migrations rehearsed and gate-verified on TEST, fourteen neuters observed RED and restored, and the full TS + Python suites green from plans 01-07. The only remaining action is the merge that auto-applies both migrations to PROD.</what-built>
  <how-to-verify>
    Before merging, confirm all of the following and paste the evidence:

    1. `npm test`, `npm run lint`, `npm run typecheck`, `npm run test:coverage` (thresholds
       lines 82 / statements 80 / functions 74 / branches 72) and `npm run schema:functions:check`
       are all green on the PR head.
    2. `cd analytics-service && python3 -m pytest` and
       `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` are green.
       ⛔ pytest ONLY from `analytics-service/` with `python3`.
    3. CI is green on the PR — including the `sql-tests` job and the `frontend` aggregator.
       ⚠️ CI is Node 22 and local is Node 25; a CI-only failure is NOT a flake. Reproduce with
       `PATH=/opt/homebrew/opt/node@22/bin:$PATH` before dismissing anything.
       ⚠️ If the `python` job is red at ~05:30 UTC with exactly 10 failures, that is the known
       shared-TEST-DB backlog flake — confirm the count and the timing before treating it as real.
    4. PROD's `max(version)` is STILL below `20260819130000` (re-run it; the earlier read may be
       hours old).
    5. `migration-policy.yml` passed on the PR — it queries the remote tip live and fails closed.
    6. The PR contains BOTH migrations AND both gate files. A migration merging without its gates
       violates the 144-§8 rule that this phase's plan set satisfies at PR level.
    7. ⚠️ This repository is PUBLIC and `.planning/` is TRACKED. Run a **no-allowlist** secret scan
       over the diff — the gitleaks allowlist is PATH-based over `.planning/`, so an allowlisted run
       proves nothing about the planning artifacts this phase adds.

    ⛔ Do NOT merge if any of the above is unmet. There is no post-merge undo for a migration; the
    only remedy is another forward migration.
  </how-to-verify>
  <resume-signal>Reply "merge-approved" with the evidence pasted, or "merge-blocked: &lt;which item&gt;".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-merge PROD verification and receipts</name>
  <files>.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-08-SUMMARY.md, TODOS.md</files>
  <precondition>The merge checkpoint resolved to `merge-approved` and the PR has been merged to `main`, so both migrations have auto-applied to PROD.</precondition>
  <action>
Verify on PROD via Supabase MCP, READ-ONLY, and record every result.

1. `select version from supabase_migrations.schema_migrations order by version desc limit 5` —
   both new stamps are present and are now the tip.
2. `select prosecdef, pronargs from pg_proc where proname = 'finalize_csv_strategy_with_returns'`
   — still exactly one 6-arg `SECURITY DEFINER` overload.
3. `has_function_privilege('authenticated', ..., 'EXECUTE')` is true; the same for `anon` and for
   `service_role` is false — the grants survived `CREATE OR REPLACE` and the new `service_role`
   REVOKE took effect.
4. `pg_get_functiondef(...)` contains the new guards. ⚠️ Match with a **word-bounded regex over a
   comment-stripped body**, not a substring — the same rule the migration's own self-verify follows,
   for the same reason.
5. `select jobid, jobname, schedule from cron.job where jobname = 'reconcile_dropped_enqueue_sweep'`
   — one row, the same jobname, `35 * * * *`, and a jobid that differs from the pre-change value
   recorded in the census checkpoint.
6. `select command from cron.job where jobname = 'reconcile_dropped_enqueue_sweep'` — the deployed
   body contains the marker exemption.
7. ⭐ **Inspect ONE REAL TICK.** Wait for the next `:35` and read
   `select status, return_message, start_time, end_time from cron.job_run_details where jobid = <new jobid> order by start_time desc limit 3`.
   The terminalizer's own header (`20260817120000:503-514`) makes exactly this demand of this class
   of change: a re-registered cron body that has never been observed running is a body nobody has
   verified. A `succeeded` status with a sane duration is the receipt.
8. Re-run the `orphaned_running_reaped:%` count on PROD and compare it to the census checkpoint's
   number. Record the delta — if the terminalizer has started firing between the two reads, that is
   newly live population for the fix that just shipped and it belongs in the receipts.

Then close the loop in `TODOS.md`: tick every v1.19-topical item this phase absorbed (fold value
guards; fmt-blind empty-rows; re-point `csv-finalize-rpc.test.ts` and the stale
`strategy-verifications-rls.test.ts` citation; 23505 second source; copy honesty on the
fold-failure sub-classes including the 42501→401 consideration, marked DEFERRED with its reason;
D-13 sweep-cron residual, marked DOCUMENTED), and leave standing every item this phase deliberately
did NOT close, each with its reason: RATE-04 limit values (founder territory, D-146-4); the 274
mypy errors in `tests/test_match_router.py`; C1 option (a) content hash; `CSV_FINALIZE_MOVED` gated
on WIZFORM-02; B2 option (b).

⛔ Do not tick an item this phase did not actually close. A ticked item is a claim that someone
will later rely on without re-checking.
  </action>
  <verify>
    <automated>
S=.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-08-SUMMARY.md
fail(){ echo "FAIL: $1"; exit 1; }
grep -qi 'job_run_details' "$S" || fail "no real cron tick was inspected - the terminalizer header demands one for this class of change"
grep -qi 'prosecdef' "$S" || fail "SECDEF not re-confirmed on PROD"
grep -qi 'service_role' "$S" || fail "the new REVOKE was not confirmed on PROD"
grep -qE 'succeeded|failed' "$S" || fail "no cron run status recorded"
grep -q 'RATE-04' TODOS.md || fail "the deliberately-not-closed items are not standing with their reasons"
grep -q 'D-146-4' TODOS.md || fail "the founder-territory marker is missing"
echo OK
    </automated>
  </verify>
  <done>PROD shows both stamps at the tip, one 6-arg SECDEF overload with `authenticated` EXECUTE and `anon`/`service_role` shut out, the new guards present under a word-bounded comment-stripped match, one `reconcile_dropped_enqueue_sweep` job at `35 * * * *` with a new jobid and the marker exemption in its deployed body, and ONE REAL TICK observed in `cron.job_run_details`; the PROD marker count delta is recorded; TODOS reflects exactly what closed and exactly what did not, each with its reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR merge → PROD database | `supabase/migrations/**` auto-applies on merge to `main`. This boundary has no human gate other than this plan. |
| orchestrator session → TEST / PROD via Supabase MCP | The only path with DB credentials in this workflow. Worktree agents have none, by design. |
| public repository → the internet | `.planning/` is TRACKED and this repo is PUBLIC; every census, project ref and plan pushed here is world-readable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.1-41 | Denial of Service | An unrehearsed migration auto-applying to PROD | critical | mitigate | Blocking census checkpoint, TEST rehearsal, both gate files executed on TEST, and a blocking merge checkpoint requiring green CI including `sql-tests` and `migration-policy`. |
| T-146.1-42 | Tampering | A gate that passes because it cannot fail | high | mitigate | Fourteen named neuters RUN on TEST with verbatim RED text captured, each restored and re-verified. A neuter that does not fire is treated as a finding about the gate. |
| T-146.1-43 | Elevation of Privilege | Grant drift surviving `CREATE OR REPLACE` unnoticed | medium | mitigate | Post-merge PROD checks on `authenticated` / `anon` / `service_role` EXECUTE privileges. |
| T-146.1-44 | Denial of Service | A re-registered cron body that has never been observed running | high | mitigate | One real tick inspected in `cron.job_run_details`, the demand `20260817120000:503-514` makes of this exact class of change. |
| T-146.1-45 | Denial of Service | The widened candidate set worsening the 05:30 TEST backlog | medium | mitigate | TEST marker count taken in the census checkpoint before applying; ≤25/tick bound preserved and verified. ⛔ The remedy is never a migration and never a `pending`-row delete. |
| T-146.1-46 | Repudiation | A stale dated census shipping as a present-tense fact | medium | mitigate | Both the PROD tip and the marker count are RE-MEASURED at merge time, and `census-stale` forces a prose correction in 146.1-01/03 before proceeding. |
| T-146.1-47 | Information Disclosure | A secret pushed into the public planning tree | high | mitigate | A no-allowlist secret scan over the diff is a merge-checkpoint item; the gitleaks allowlist is PATH-based over `.planning/` and an allowlisted run proves nothing here. Project refs may be recorded; tokens must never be. |
| T-146.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed. Package Legitimacy Audit recorded not-applicable in `146.1-RESEARCH.md`. |
</threat_model>

<verification>
1. Both blocking checkpoints answered, with their evidence pasted into the SUMMARY.
2. Both migrations applied to TEST with their self-verify blocks passing.
3. Both SQL gate files executed on TEST under `ON_ERROR_STOP=1`, each reaching its final ALL PASS notice.
4. All fourteen named neuters observed RED on TEST and restored, with verbatim failure text.
5. PROD tip re-confirmed below both stamps at merge time; `migration-policy.yml` green on the PR.
6. Post-merge: both stamps at the PROD tip, SECDEF + grants intact, `service_role` shut out, guards present under a word-bounded comment-stripped match, one cron row at `35 * * * *` with the marker exemption, and one real tick observed.
7. TODOS reconciled: everything closed is ticked, everything not closed stands with its reason.
</verification>

<success_criteria>
- [ ] The census checkpoint was answered BEFORE anything was applied, with all six reads pasted.
- [ ] If the PROD marker count or the tip had moved, the prose in 146.1-01 / 146.1-03 was corrected and committed BEFORE proceeding.
- [ ] Both migrations applied cleanly to TEST; TEST's `schema_migrations` state for both entries is recorded verbatim, including any MCP `now()` stamping drift.
- [ ] Both gate files executed on TEST and reached their final ALL PASS notices; new Parts 3d / 6a-6i / 7a-7c and arm C4 all ran.
- [ ] All fourteen neuters RUN, each RED, each with verbatim text, each restored and re-verified GREEN; no neutered definition remains on TEST.
- [ ] The A1-VALUE two-probe result is recorded INCLUDING the NaN false-PASS behaviour under a widened bound.
- [ ] The B4-BLANKET double-RED (arms C2 and C3) is recorded as the evidence refuting the roster's original proposed fix.
- [ ] The merge checkpoint was answered with all seven evidence items, including the no-allowlist secret scan.
- [ ] Post-merge PROD verification complete, including ONE REAL TICK in `cron.job_run_details`.
- [ ] TODOS ticks only what this phase closed; RATE-04 (D-146-4), the `test_match_router.py` mypy errors, C1 option (a), `CSV_FINALIZE_MOVED` (gated on WIZFORM-02) and B2 option (b) all stand with their reasons.
- [ ] ⛔ Nothing on PROD was written by this plan other than the merge's own auto-apply: no `pending` row deleted, no `cron.unschedule(9)`, no `process_key_unified_backbone` flag row touched.
</success_criteria>

<falsifiability_ledger>
| # | Success criterion | What makes it go RED | How it is falsified |
|---|---|---|---|
| F-08-1 | Migrations are safe to merge | A gate Part fails on TEST | Task 1 halts. ⚠️ The failure mode to guard against is the opposite: a gate file that stops EARLY still prints many `OK` notices. The criterion is the FINAL `ALL PASS` line, not the presence of OK lines. |
| F-08-2 | The gates can actually fail | A neuter produces no RED | Treated as a finding about the GATE, not the neuter. Fix the gate, re-run the whole file, record what was wrong. ⛔ Never escalate the neuter until it fails. |
| F-08-3 | The narrow B4 fix is right, not merely preferred | — | **B4-BLANKET must produce TWO REDs (C2 and C3).** If it produces one or zero, the discriminating pair does not discriminate and the adjudication in `146.1-CONTEXT.md` is unproven. This is the single most important measurement in this plan. |
| F-08-4 | The A1 value guard is really bounded | — | **A1-VALUE must be run with BOTH probes.** A NaN-only observation false-PASSes under a widened bound, because NaN fails any `BETWEEN`. A summary showing only the NaN probe is an incomplete measurement and must be re-run. |
| F-08-5 | The self-verify anchors read code, not comments | — | **B4-COMMENT and A1-SUBSTR** are the two neuters that specifically distinguish a real anchor from a substring that matches prose. Both have historical precedent in this repo; if either PASSes under its neuter, the anchor is decorative. |
| F-08-6 | PROD is in the state we think | The tip moved, or the marker count moved | The census checkpoint's re-reads. A `census-stale` answer forces a prose correction — the claim in the migration headers is not allowed to ship stale. |
| F-08-7 | The sweep actually runs after re-registration | The tick shows `failed` or never appears | `cron.job_run_details` for the NEW jobid. A green apply proves the body parsed, not that it runs. |
| F-08-8 | Grants survived the replace | `service_role` still has EXECUTE, or `anon` regained it | The three `has_function_privilege` reads on PROD. `CREATE OR REPLACE` preserves ACLs — this check exists because "preserves" is a claim about Postgres, not an observation about this database. |
| F-08-9 | Nothing forbidden was done on PROD | — | Falsified by RECORD: the SUMMARY must state that every PROD interaction was a `select` (plus the merge's own auto-apply). Any write recorded, other than that, is a violation of a standing constraint. |
| F-08-10 | TODOS is honest | An item is ticked that this phase did not close | Falsified by cross-reading the ticks against the plan SUMMARYs. A ticked item is a claim someone will later rely on without re-checking — that is precisely why over-ticking is worse than under-ticking. |
</falsifiability_ledger>

<output>
Create `.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-08-SUMMARY.md` — this SUMMARY **is** the deliverable, not a report about one.

It MUST contain, verbatim rather than paraphrased: the six census reads (before) and their
post-merge counterparts (after); the TEST `schema_migrations` state for both entries; every
`RAISE NOTICE ... OK` line and both final ALL PASS lines from the two gate files; a row per neuter
with its name, the statement changed, the verbatim failure text, and its restoration
confirmation; both checkpoint answers with their pasted evidence; the pre- and post-change cron
jobids; the `cron.job_run_details` tick row; and the TODOS reconciliation as a two-column
closed / still-open table with a reason on every still-open row.

⚠️ Project refs may be recorded. Tokens, keys and connection strings must NEVER appear — this
repository is PUBLIC and `.planning/` is TRACKED.
</output>
