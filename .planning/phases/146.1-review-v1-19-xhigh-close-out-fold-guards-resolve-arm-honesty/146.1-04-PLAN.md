---
phase: 146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty
plan: 04
type: execute
wave: 2
depends_on: ["146.1-01", "146.1-02", "146.1-03"]
files_modified:
  - src/app/api/strategies/csv-finalize/route.ts
  - src/__tests__/csv-finalize-cross-submission-merge.test.ts
  - TODOS.md
autonomous: true
requirements: [B3, A2, C1, C4]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A 23505 naming `csv_daily_returns_strategy_date_key` does NOT enter the resolve arm — the strategies re-fetch is never issued and the caller does not receive a 503 retry invitation for a permanent input defect (B3)."
    - "A 23505 naming `strategies_user_wizard_session_source_uniq` (or any member of WIZARD_SESSION_CONSTRAINTS) still enters the resolve arm, unchanged (B3)."
    - "A 23505 whose constraint name is UNPARSEABLE (`pgConstraintName` returns null) still enters the resolve arm — pre-existing behaviour is preserved for the UNKNOWN case, and the arm's own fail-closed path answers 503 rather than fabricating an echo (B3)."
    - "A manager-flow resubmit (terminalStatus 'pending_review') that resolves onto a committed 'private' contribution row is REFUSED with 409 CSV_SESSION_REUSED, and vice versa (A2)."
    - "A row whose `status` column is absent/undefined is NOT read as a mismatch — the guard mirrors the name check's `typeof === 'string' &&` shape (A2)."
    - "The 200 echo response no longer asserts that the committed series was re-verified element-by-element; it states what was actually compared (count and boundaries) (C1)."
    - "A failed resolve re-fetch (`refetchErr`) and a resolve that finds no committed row each answer 503 CSV_PERSIST_FAIL and issue ZERO metadata updates, pinned by tests that go RED under a step-tag neuter (C4)."
  artifacts:
    - src/__tests__/csv-finalize-cross-submission-merge.test.ts
  key_links:
    - "`resolveExistingStrategyOrRefuse`'s `args` parameter type (route.ts:684-689) declares only {userId, wizardSessionId, strategyName, rows} — structural typing lets the wider caller object compile. Widening that type to carry terminalStatus IS the A2 fix; without it there is nothing to compare against."
    - "A2 reuses the EXISTING `refuse()` closure (route.ts:753-784) — 409 / CSV_SESSION_REUSED / NO_STORE_HEADERS / Sentry step `finalize-resolve-refused`. Minting a new code would force same-commit edits to KNOWN_CSV_FINALIZE_CODES and seam-wire-vocabulary.invariant.test.ts."
    - "`pgConstraintName` parses `message` and NEVER `details` — `details` carries client-supplied key values, so reading it would let a caller steer which arm answers them. B3 must not 'improve' that."
    - "null from pgConstraintName means UNPARSEABLE, not 'no constraint violated'. It MUST keep the pre-existing resolve behaviour, or a mis-routed 23505 turns a fail-closed 503 into a fail-open 500 with the wrong copy."
---

<objective>
First half of the **route.ts serial cluster**. ⛔ `src/app/api/strategies/csv-finalize/route.ts` is
edited by five roster items and they MUST NOT be parallelised: concurrent agents in one checkout
share the git index, and file-disjoint is not the same as isolated. The mandated order is
**B3 → A2 → A4 → A3 → C2**. This plan owns **B3 → A2**, plus **C1** (which lives in the same
resolve function) and the **C4** resolve-arm fail-closed pins. 146.1-05 owns A4 → A3 → C2 in the
next wave.

- **B3** — any-23505 enters the resolve arm undiscriminated (`route.ts:581-583`). The fold's
  dailies INSERT is a plain INSERT with no `ON CONFLICT`, so a duplicate-date payload raises 23505
  on `csv_daily_returns_strategy_date_key` (`20260624120000:55-56`) → wrong arm → a 503 retry
  invitation for a permanent input defect that will fail identically forever.
- **A2** — the resolve arm never compares terminal status (`route.ts:~876`). The partial unique
  index is `(user_id, wizard_session_id, source) WHERE wizard_session_id IS NOT NULL`
  (`20260728120000:167-169`) — `status` is NOT in the key — and `src/lib/wizard/localStorage.ts:379-381`
  restores `wizardSessionId` unconditionally from ONE shared storage key. So a manager-flow
  resubmit can 200-echo a committed `'private'` contribution row that then never enters the admin
  review queue, and vice versa. This is an access-control defect (ASVS V4), not a cosmetic one.
- **C1** — the interior-values echo residual (`route.ts:816-819`, `:820-863`). FOUNDER CALL,
  recommended option shipped here: **honest copy now**, content hash filed as a named follow-up.
- **C4** — the resolve arm's two fail-closed paths (`:737`, `:748-751`) are unpinned.

Purpose: the resolve arm is the only place in the CSV wizard where the server answers **200 with a
strategy id it did not create in this request**. Every discrimination it fails to make is a
fabricated success on a product whose entire value proposition is a verified track record.
Output: three discriminations in `route.ts`, honest echo copy, and a test suite that goes RED
under named one-line neuters.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-CONTEXT.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-RESEARCH.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-PATTERNS.md

@src/app/api/strategies/csv-finalize/route.ts
@src/lib/api/pgConstraintName.ts
@src/__tests__/csv-finalize-cross-submission-merge.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (B3): enter the resolve arm only for a wizard-session constraint</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/__tests__/csv-finalize-cross-submission-merge.test.ts</files>
  <precondition>`node_modules/.bin/vitest` exists in this checkout. GSD worktree agents get no node_modules and `npx vitest` exits MODULE_NOT_FOUND while `npx tsc` resolves a decoy package. Run `npm ci` or HALT — do not report a green on an unrun suite.</precondition>
  <read_first>
    - `src/lib/api/pgConstraintName.ts` in full (99 lines) — the exported `WIZARD_SESSION_CONSTRAINTS` set at `:80-83` already contains `strategies_user_wizard_session_source_uniq` plus the superseded `strategies_user_wizard_session_uniq`; the `:34-37` docblock states that `null` is the UNKNOWN case and callers must keep pre-existing behaviour for it; `:15-32` states why `details` is never parsed.
    - `src/app/api/strategies/create-with-key/route.ts:906-990` — the canonical three-way arm, and the `step: "draft-rpc-unknown-constraint"` Sentry capture on a named-but-unrecognised constraint.
    - `src/app/api/strategies/csv-finalize/route.ts:573-583` (the 23505 branch), `:585-632` (the fold-failure arm it falls through to — do NOT edit its copy here, that is 146.1-05/A3), `:640-646` (the docblock that currently only HOPES the dailies index is unreachable).
    - `src/__tests__/csv-finalize-cross-submission-merge.test.ts:1-34` (docstring conventions), `:53-88` (hoisted fixtures), `:100-157` (chained builder mocks), `:184-189` (`DUP_KEY_ERROR`, the exact PostgREST error shape this task keys on), `:439` (the existing fail-closed case).
  </read_first>
  <behavior>
    - Given an RPC error `{code: "23505", message: 'duplicate key value violates unique constraint "strategies_user_wizard_session_source_uniq"'}` → the resolve arm runs (the `strategies` re-fetch spy IS called). Unchanged behaviour; this is the anti-regression positive.
    - Given `{code: "23505", message: '… unique constraint "strategies_user_wizard_session_uniq"'}` (the superseded name) → the resolve arm still runs.
    - Given `{code: "23505", message: 'duplicate key value violates unique constraint "csv_daily_returns_strategy_date_key"'}` → the resolve arm does NOT run: the `strategies` re-fetch spy is NOT called, zero metadata updates are issued, and the response is the fold-failure envelope, NOT the 503 retry invitation.
    - Given `{code: "23505", message: "duplicate key value violates a unique constraint"}` (no parseable name → `pgConstraintName` returns null) → the resolve arm DOES run. Pre-existing behaviour for UNKNOWN.
    - Given a named-but-unrecognised constraint → Sentry receives a capture whose `step` tag is distinct from `finalize-resolve-refused` and whose extra carries the constraint name, so the class is observable rather than silent.
  </behavior>
  <action>
In `src/app/api/strategies/csv-finalize/route.ts`, gate the 23505 branch at `:581-583`.

Import `pgConstraintName` and `WIZARD_SESSION_CONSTRAINTS` from `@/lib/api/pgConstraintName` —
the leaf already exists and is already used by `create-with-key` and `composite/add-key`, so this
is a third call site of a shared helper, not a new mechanism.

Inside the `error?.code === "23505"` branch, compute the constraint name once, then enter
`resolveExistingStrategyOrRefuse` when the name is `null` OR is a member of
`WIZARD_SESSION_CONSTRAINTS`. Otherwise fall through to the fold-failure arm.

⛔ **`null` MUST enter the resolve arm.** The leaf's own contract says `null` means the name was
UNPARSEABLE, not that no constraint was violated, and callers must keep pre-existing behaviour for
it. Pre-existing behaviour here is any-23505 → resolve, and the resolve arm already fails closed
when it cannot find a committed row — so a mis-routed non-session 23505 answers 503, never a
fabricated echo. Making `null` bypass the arm converts a fail-closed 503 into a fail-open 500 with
the wrong copy. Write that reason into the code comment; it is the non-obvious half of this fix.

⛔ **Do NOT read `error.details`.** `pgConstraintName` parses `message` only, and `:15-32` gives
the security rationale: `details` carries client-supplied key values, so reading it would let a
caller steer which arm answers them. Do not "improve" the leaf.

For the named-but-unrecognised case, add a fail-loud observation before falling through, copying
the `create-with-key:906-990` shape: `console.error` with the prefix + correlation id + the
constraint name, and `captureToSentry` with a `step` tag that is grep-unique in this file (the
existing tags are `finalize-fold-fail`, `finalize-resolve-read-fail`, `finalize-resolve-refused`)
and an extra carrying `pg_code` and `constraint`. ⛔ Never log the raw error — use
`scrubSeamError(error)` as the sibling arms do at `:588-596`.

Update the docblock at `:640-646`. It currently states that the dailies index 23505 is
hypothetical. It is not: `csv_daily_returns_strategy_date_key` is real
(`20260624120000:55-56`) and the fold's dailies INSERT has no `ON CONFLICT`. Note that
146.1-01's A1 duplicate-date guard removes the one concrete payload that reaches it, and that this
fence is defense in depth against any future source. ⛔ Do NOT edit `20260819120000`'s prose —
it is an applied migration; 146.1-01's new `COMMENT ON FUNCTION` supersedes it.

Tests go in `src/__tests__/csv-finalize-cross-submission-merge.test.ts`. Copy the file's
conventions exactly: `// @vitest-environment node` after the docstring, the `server-only` /
`withAuth` / ratelimit / supabase / sentry mocks, the `next/server` partial mock that neuters
`after()`, `vi.hoisted` typed fixtures, and the chained builder mocks. Add a named constant beside
`DUP_KEY_ERROR` for the dailies-index error shape.

⭐ **The oracle is BEHAVIOURAL, never structural.** Assert on whether the `strategies` re-fetch
was issued (the existing `resolveFetch.filters` recorder) and on whether `updateCalls` is empty —
never on whether the source contains a particular `if`. A test that asserts the shape of the fix
cannot detect a fix that is wired to nothing.

Commit as its own commit.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
R=src/app/api/strategies/csv-finalize/route.ts
grep -q "pgConstraintName" "$R" || fail "leaf not imported"
grep -q "WIZARD_SESSION_CONSTRAINTS" "$R" || fail "constraint set not used"
grep -qE 'error\??\.details|error\.details' "$R" && fail "details is being read - pgConstraintName parses message only, by security design"
grep -q "csv_daily_returns_strategy_date_key" "$R" || fail "the second 23505 source is not named in the docblock"
npx --no-install vitest run src/__tests__/csv-finalize-cross-submission-merge.test.ts --no-file-parallelism --reporter=basic || fail "suite red"
npm run typecheck || fail "typecheck red"
echo OK
    </automated>
  </verify>
  <done>The 23505 branch enters the resolve arm only for `null` or a `WIZARD_SESSION_CONSTRAINTS` member; a dailies-index 23505 falls through with a fail-loud Sentry capture under a grep-unique step tag; `details` is never read; the docblock names the real second source; five new behavioural cases pass and typecheck is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (A2 + C4): refuse a terminal-status mismatch, and pin both fail-closed paths</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/__tests__/csv-finalize-cross-submission-merge.test.ts</files>
  <read_first>
    - `src/app/api/strategies/csv-finalize/route.ts:683-692` (the function signature and the `args` parameter type — `terminalStatus` is NOT declared there today, which is why the comparison cannot be written yet), `:695-722` (`failClosed` and its 503 `CSV_PERSIST_FAIL` envelope), `:730-737` (the re-fetch, which ALREADY selects `status`, and the `refetchErr` fail-closed line), `:743-751` (the no-committed-row fail-closed arm), `:753-786` (the `refuse` closure), `:787-793` (the name check and its `typeof === "string" &&` guard).
    - `146.1-PATTERNS.md` §5 — the `refuse()` builder verbatim, the envelope invariants, and the Sentry step-tag convention.
  </read_first>
  <behavior>
    - `terminalStatus: "pending_review"` + committed row `status: "private"` → 409, code `CSV_SESSION_REUSED`, `NO_STORE_HEADERS`, zero metadata updates, one Sentry capture at step `finalize-resolve-refused`.
    - `terminalStatus: "private"` + committed row `status: "pending_review"` → same refusal (both directions, not just one).
    - `terminalStatus: "pending_review"` + committed row `status: "pending_review"` + a conforming identical payload → still the normal 200 echo. The positive counterpart; without it the refusal is indistinguishable from refusing everything.
    - Committed row with `status: undefined` → NOT treated as a mismatch; the request proceeds to the name/boundary checks.
    - The status comparison runs BEFORE the name check: a manager-flow request landing on a `private` row is a wrong-FLOW answer regardless of whether the names happen to match, and the Sentry reason must be unambiguous.
    - **C4a**: `refetchErr` set → 503 `CSV_PERSIST_FAIL`, zero metadata updates, Sentry step `finalize-resolve-read-fail`.
    - **C4b**: re-fetch returns no row (or a non-UUID id) → 503 `CSV_PERSIST_FAIL`, zero metadata updates.
  </behavior>
  <action>
**The actual gate is the parameter type.** `resolveExistingStrategyOrRefuse`'s `args` type at
`route.ts:684-689` declares only `{ userId, wizardSessionId, strategyName, rows }`. TypeScript
structural typing lets the caller at `:582` pass a wider object and compile, so `terminalStatus`
is present at runtime but invisible to the function. Widen that type to include
`terminalStatus: "pending_review" | "private"`. One line, and nothing else in A2 is possible
without it.

The re-fetch at `:730-735` already selects `status` — **no query change**. It is currently used
only to echo at `:875`.

Insert the comparison immediately after the `!existingRow || !isUuid(existingRow.id)` fail-closed
arm (`:743-751`) and BEFORE the name check at `:787-793`.

⚠️ `existingRow.status` is typed `string | undefined`. Write the guard as
`typeof existingRow.status === "string" && existingRow.status !== args.terminalStatus` — mirroring
the name check's own `typeof === "string" &&` shape at `:789-791`, which exists for exactly this
reason: **a missing column must not be read as a mismatch.** Reading absence as a value is the
"absence is not a value" defect class this codebase has already closed elsewhere.

Use the **existing** `refuse(reason)` closure — call it with a reason string naming the terminal
status mismatch. ⛔ Do NOT mint a new error code. `refuse` already emits `CSV_SESSION_REUSED` /
409 / `NO_STORE_HEADERS`, captures to Sentry at `step: "finalize-resolve-refused"`, and logs. A new
code would force same-commit edits to `KNOWN_CSV_FINALIZE_CODES`
(`CsvSubmitStep.tsx:161-167`), `EXPECTED_TABLE_SIZE` in `wizardErrors.invariant.test.ts`, and
`seam-wire-vocabulary.invariant.test.ts`, which derives the route's emitted codes from
comment-stripped source and reds BY NAME. `create-with-key/route.ts:960-966` gives this exact
reason for declining to mint one.

Write the mechanism into the code comment, citing `20260728120000:167-171` (the index COMMENT
explains that `source` is in the key precisely because `localStorage.ts:379-381` restores
`wizardSessionId` unconditionally from one shared key) — so the next reader understands why two
different flows can collide on one fence.

**C4 — pin the two fail-closed paths.** Both already exist in the code (`:737` and `:748-751`);
neither is asserted. Add cases for each in the same test file. The oracle is the RESPONSE plus the
write-vector: 503 `CSV_PERSIST_FAIL` AND `updateCalls` empty. A read we could not make is never
read as "nothing is there" — that sentence is already the register the file uses at `:439`.

Commit as its own commit.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
R=src/app/api/strategies/csv-finalize/route.ts
grep -qE 'terminalStatus:\s*"pending_review"\s*\|\s*"private"' "$R" || fail "the args parameter type was not widened - without it there is nothing to compare"
grep -qE 'typeof existingRow\.status === "string"' "$R" || fail "the status guard does not mirror the name check's absence handling"
grep -c 'CSV_SESSION_REUSED' "$R" | grep -qE '^[1-9]' || fail "refusal code missing"
grep -q 'CSV_TERMINAL_STATUS' "$R" && fail "a new error code was minted - reuse refuse(); a new code moves three roster gates"
npx --no-install vitest run src/__tests__/csv-finalize-cross-submission-merge.test.ts --no-file-parallelism --reporter=basic || fail "suite red"
npx --no-install vitest run src/lib/seam-wire-vocabulary.invariant.test.ts src/lib/wizardErrors.invariant.test.ts --no-file-parallelism --reporter=basic || fail "vocabulary/roster gates red - a code was minted or moved"
npm run typecheck || fail "typecheck red"
echo OK
    </automated>
  </verify>
  <done>The `args` type carries `terminalStatus`; the comparison sits between the no-row fail-closed arm and the name check, guards absence with `typeof === "string"`, and calls the existing `refuse()`; both directions plus the same-status positive plus the undefined-status case pass; both fail-closed paths are pinned to 503 + zero updates; the vocabulary and roster invariant suites are untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (C1): honest echo copy for the interior-values residual, hash filed as a named option</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/__tests__/csv-finalize-cross-submission-merge.test.ts, TODOS.md</files>
  <read_first>
    - `src/app/api/strategies/csv-finalize/route.ts:816-819` (the documented, accepted residual, verbatim: equal count and boundaries with different interior values still echoes), `:820-863` (the count + [min,max] boundary comparison — the two reads the arm actually makes), `:871-876` (the echo return).
    - `src/lib/wizardErrors.ts:1971-2000` — SEAMUX-04's precedent for REMOVING an unmakeable claim while keeping the uncertainty, and the dated provenance-comment style that must accompany any copy edit.
  </read_first>
  <behavior>
    - A resubmit whose payload has the SAME count and the SAME first/last dates as the committed series but DIFFERENT interior values still resolves to a 200 echo (behaviour unchanged — this is not a refusal change).
    - The 200 echo envelope states what was actually compared. It does not state or imply that the committed series was verified equal to the submitted one element-by-element.
    - A count mismatch, a boundary mismatch, a superset payload, and an empty-payload-against-series-row are all still REFUSED — every existing case in this suite stays green.
  </behavior>
  <action>
⚖️ **FOUNDER CALL — the recommended option is planned here; the alternative is NOT baked in.**
Option (b), honest copy, ships now. Option (a), a content hash over the payload, needs new
persisted state — a THIRD migration in a phase that already carries two, each with its own
PROD-risk TEST rehearsal, plus a backfill question for every already-committed row and a
nullable-hash fail-open period. ⛔ Do NOT implement a hash, a checksum, or a new column in this
plan. If the founder later chooses (a), it is its own phase.

The behaviour does not change. What changes is that the 200 envelope stops implying an
observation the arm did not make. The arm compares **count and the [min, max] boundaries** — two
reads — and the source already documents the residual at `:816-819`. Today the response speaks as
though the committed series was confirmed to be the submitted one.

Adjust the echo copy so it says what was compared, in the register `wizardErrors.ts:1971-2000`
established for `CSV_SUBMIT_FAILED`: state the fact, do not editorialise, and do not add a scare.
⛔ Do NOT mint a new code — this is a 200, and the copy is the envelope's own message field.
⛔ Do NOT change the refusal thresholds; the count-mismatch and boundary-mismatch cases already
chose refusal on "fabricated success is the worst outcome" logic and they stay exactly as they
are.

Carry a **dated provenance comment** above the changed copy naming: the claim removed, why it was
unmakeable from two boundary reads, the C1 founder call and which option was taken, and a pointer
to the TODOS follow-up. `146.1-PATTERNS.md` §7 makes this comment mandatory for any copy edit in
this codebase; a copy change without one is how the three false forwarding comments this phase
already corrected came to exist.

File the follow-up in `TODOS.md` as a NAMED option, not a vague "improve later": title it
explicitly as C1 option (a); state that it needs a content hash persisted at create time (a new
column or a `strategy_verifications` field), therefore a third migration plus a backfill; state
the nullable-hash fail-open period as a known cost; and record that option (b) shipped in
Phase 146.1 and is what a reader will find in the code today. An option filed without its cost is
an option nobody can decide.

Add a test asserting the echo envelope no longer carries the over-claiming phrase, and keep every
existing refusal case green. ⭐ Assert on the ABSENCE of the removed claim, not merely on the
presence of the new sentence — a presence-only assertion is satisfiable by concatenating the new
sentence onto the old one.

Commit as its own commit.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
R=src/app/api/strategies/csv-finalize/route.ts
grep -qiE 'sha256|createHash|checksum|content_hash|contentHash' "$R" && fail "a hash was implemented - C1 option (a) is explicitly NOT in scope"
grep -q "C1" TODOS.md || fail "the follow-up option was not filed"
grep -qiE 'backfill' TODOS.md || fail "the follow-up is filed without its cost"
npx --no-install vitest run src/__tests__/csv-finalize-cross-submission-merge.test.ts --no-file-parallelism --reporter=basic || fail "suite red"
npx --no-install vitest run src/lib/seam-wire-vocabulary.invariant.test.ts --no-file-parallelism --reporter=basic || fail "vocabulary gate red"
npm run typecheck || fail "typecheck red"
npm run lint || fail "lint red"
echo OK
    </automated>
  </verify>
  <reversibility rating="reversible">Copy-only. Reverting is a one-line prose change; no schema, no code path, no persisted state. The not-taken option is filed with its full cost so the decision can be re-opened with the same information.</reversibility>
  <done>The echo copy states what the arm compared and no longer implies element-wise verification; a dated provenance comment names the removed claim and the founder call; no hash, checksum or column exists anywhere in the diff; the follow-up option is filed in TODOS.md WITH its cost; every existing refusal case is still green; the new test asserts on absence of the removed claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated browser → `POST /api/strategies/csv-finalize` | Untrusted payload; the caller controls the CSV rows, the strategy name and (via localStorage) the wizard session id. |
| manager flow ↔ contribution flow | Two distinct product flows share ONE localStorage wizard-session key and ONE partial unique index that does not include `status`. The route is the only place they can be told apart. |
| PostgREST error body → arm selection | The error `message` steers which arm answers. `details` carries client-supplied key values and must never steer anything. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.1-17 | Information Disclosure / Elevation of Privilege | `resolveExistingStrategyOrRefuse` cross-flow echo | high | mitigate | A2: refuse on `existingRow.status !== args.terminalStatus` via the existing `refuse()`, placed before the name check. Both directions tested. A manager-flow caller must never receive a `private` contribution row's id. |
| T-146.1-18 | Tampering | Arm selection steered by attacker-controlled constraint text | medium | mitigate | B3 keys on `pgConstraintName`, which parses `message` only and never `details`; `<verify>` grep-refuses any `details` read. Unrecognised names fall through and are Sentry-captured under a distinct step tag. |
| T-146.1-19 | Repudiation | 503 retry invitation for a permanent input defect | medium | mitigate | B3 routes a dailies-index 23505 to the fold-failure arm instead of the resolve arm's retryable 503. |
| T-146.1-20 | Repudiation | A 200 asserting an observation the server did not make | medium | mitigate | C1 honest copy; the assertion is on ABSENCE of the removed claim so concatenation cannot satisfy it. |
| T-146.1-21 | Repudiation | Absence read as a value (`status` column missing → false mismatch) | medium | mitigate | `typeof existingRow.status === "string" &&` guard, mirroring the name check; a dedicated undefined-status test case. |
| T-146.1-22 | Denial of Service | Vocabulary roster drift from a newly minted code | low | mitigate | No new code is minted; `<verify>` runs `seam-wire-vocabulary.invariant.test.ts` and `wizardErrors.invariant.test.ts` and grep-refuses a new `CSV_TERMINAL_STATUS` code. |
| T-146.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed; `npm ci` in the precondition restores the committed lockfile only. Package Legitimacy Audit recorded not-applicable in `146.1-RESEARCH.md`. |
</threat_model>

<verification>
1. Each task's `<verify>` exits 0.
2. `npx vitest run src/__tests__/csv-finalize-cross-submission-merge.test.ts src/__tests__/csv-validate-route.test.ts src/__tests__/csv-finalize-c14-regression.test.ts --no-file-parallelism` green.
3. `npx vitest run src/lib/seam-wire-vocabulary.invariant.test.ts src/lib/wizardErrors.invariant.test.ts --no-file-parallelism` green — proof that no code was minted and no roster moved.
4. `npm run typecheck` and `npm run lint` green.
5. `git diff --stat` shows `src/app/api/strategies/csv-finalize/route.ts` touched by exactly this plan's three commits, and NO other plan's files.
6. ⚠️ Local vitest flakes: re-run with `--no-file-parallelism`. CI is Node 22 and local is Node 25 — a CI-only failure is NOT a flake; reproduce it with `PATH=/opt/homebrew/opt/node@22/bin:$PATH`.
</verification>

<success_criteria>
- [ ] The 23505 branch calls `pgConstraintName` once and enters the resolve arm iff the result is `null` or a `WIZARD_SESSION_CONSTRAINTS` member.
- [ ] A dailies-index 23505 does NOT issue the `strategies` re-fetch, issues zero metadata updates, and does not answer with the retryable 503.
- [ ] An unparseable 23505 STILL enters the resolve arm, with the reason written in the code.
- [ ] `error.details` is read nowhere in the route.
- [ ] A named-but-unrecognised constraint produces a `console.error` with `scrubSeamError` and a Sentry capture under a step tag grep-unique in this file.
- [ ] `resolveExistingStrategyOrRefuse`'s `args` type declares `terminalStatus: "pending_review" | "private"`.
- [ ] Both cross-flow directions are refused with 409 `CSV_SESSION_REUSED` via the existing `refuse()`; the same-status identical-payload case still 200-echoes; an undefined `status` is not a mismatch.
- [ ] The status comparison sits after the no-row fail-closed arm and before the name check.
- [ ] Zero new error codes; `seam-wire-vocabulary.invariant.test.ts` and `wizardErrors.invariant.test.ts` green with no roster edits.
- [ ] Both resolve-arm fail-closed paths pinned to 503 `CSV_PERSIST_FAIL` + zero metadata updates.
- [ ] The echo copy no longer implies element-wise verification; a dated provenance comment names the removed claim and the founder call; no hash/checksum/column anywhere in the diff.
- [ ] TODOS.md carries C1 option (a) as a NAMED follow-up WITH its cost (third migration + backfill + nullable-hash fail-open period).
- [ ] Three commits, one per task.
</success_criteria>

<falsifiability_ledger>
| # | Success criterion | What makes it go RED | Named neuter — RUN IT, then restore |
|---|---|---|---|
| F-04-1 | A dailies-index 23505 avoids the resolve arm | Widen the predicate back to any-23505 | **Neuter B3-ANY**: delete the `pgConstraintName` gate so every 23505 resolves. The dailies-index case must go RED on the re-fetch-spy assertion. If it stays green, the test is asserting on the response body only and cannot see the wrong arm being entered. |
| F-04-2 | An unparseable 23505 still resolves | Make `null` bypass the arm | **Neuter B3-NULLOUT**: change the condition to `constraint !== null && WIZARD_SESSION_CONSTRAINTS.has(constraint)`. The null-name case must go RED — this pins the leaf's UNKNOWN contract, which is the half a reviewer is most likely to "simplify". |
| F-04-3 | Cross-flow echo refused | Delete the status comparison | **Neuter A2-DROP**: remove the `existingRow.status !== args.terminalStatus` line. BOTH direction cases must flip from 409 to 200. One-direction-only coverage would let a half-fix pass. |
| F-04-4 | Absence is not a mismatch | Drop the `typeof === "string"` guard | **Neuter A2-ABSENCE**: the undefined-status case must flip from 200 to 409. Without this case, a guard that refuses every row with no `status` column ships green. |
| F-04-5 | The refusal is not refuse-everything | — | The same-status identical-payload positive case is the counterpart. **Neuter A2-DENYALL**: make `refuse()` unconditional; that positive case must RED. Without it, F-04-3 is satisfied by a route that refuses all resubmits. |
| F-04-6 | Fail-closed paths answer 503 with no writes | Delete `if (refetchErr) return failClosed(...)` at `:737` | **Neuter C4-READ**: the C4a case must RED on BOTH the status code and `updateCalls`. Asserting only the status code would miss a fail-closed path that still issued a metadata UPDATE. |
| F-04-7 | No-committed-row fails closed | Delete the `!existingRow \|\| !isUuid(...)` arm | **Neuter C4-NOROW**: the C4b case must RED. |
| F-04-8 | The echo no longer over-claims | Revert the copy | **Neuter C1-REVERT**: restore the previous sentence; the new case must RED **on the absence assertion**. ⚠️ If the test only asserts the presence of the new sentence, this neuter false-PASSes whenever both sentences are present — that is the concatenation hole, and it is the same class as the `%5000%` substring failure. |
| F-04-9 | No new code minted | Mint one | `seam-wire-vocabulary.invariant.test.ts` REDs by name and `wizardErrors.invariant.test.ts` REDs on `EXPECTED_TABLE_SIZE`. Both are in `<verify>`; neither needs a neuter because minting a code IS the neuter. |
| F-04-10 | No hash shipped for C1 | — | `<verify>` grep-refuses `sha256\|createHash\|checksum\|content_hash`. This criterion exists because option (a) is the tempting answer and it is out of scope. |
</falsifiability_ledger>

<output>
Create `.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-04-SUMMARY.md` when done.

The SUMMARY MUST record, per Falsifiability Ledger row, the neuter as OBSERVED (with the verbatim
failing assertion text) or NOT OBSERVED. Every neuter in this plan is runnable locally — there is
no DB dependency — so `NOT OBSERVED` here means it was skipped, and must be stated as such rather
than omitted.

The SUMMARY MUST also carry a one-line handoff to 146.1-05 naming the exact state of
`route.ts` at hand-off: which arms moved, which line ranges shifted, and that A4 → A3 → C2 remain.
</output>
