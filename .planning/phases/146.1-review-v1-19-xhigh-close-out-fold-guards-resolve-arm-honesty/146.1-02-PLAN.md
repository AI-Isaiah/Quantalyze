---
phase: 146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/tests/test_limiter_route_coverage.py
autonomous: true
requirements: [B1]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A new `@router.post` added to any FastAPI router without a `@limiter.limit` decorator makes `cd analytics-service && python3 -m pytest tests/test_limiter_route_coverage.py` FAIL, naming the offending `module.function` key (B1)."
    - "Removing a `@limiter.limit` from an already-limited route makes the same gate FAIL, naming the key that fell out."
    - "Deleting a quarantine entry whose route no longer exists makes the gate FAIL — a stale exemption cannot outlive the thing it exempted, because the partition is compared by EQUALITY not containment."
    - "The gate cannot pass vacuously: if the route walker returns an empty or truncated population, an explicit sanity band fails first with its own message."
    - "`/process-key`'s two CALLABLE (dynamic) limits are asserted in `_dynamic_route_limits` specifically, so converting them to static limits or dropping them is visible."
  artifacts:
    - analytics-service/tests/test_limiter_route_coverage.py
  key_links:
    - "The gate derives its population from `main.app.routes` (the WHOLE surface), never from `limiter._route_limits` — the existing gates are self-referential over that dict and a route with no decorator is structurally invisible to them."
    - "`limiter._route_limits` is keyed by `f\"{func.__module__}.{func.__name__}\"` of the UNDECORATED handler (slowapi/extension.py:669) and the decorator uses `functools.wraps`, so `route.endpoint.__module__ + '.' + route.endpoint.__name__` is the exact join key. MEASURED by probe."
    - "`import main` (not individual routers) is the correct import — `_route_limits` is populated at decoration time and `main` performs every `include_router`."
---

<objective>
Close **B1**: Python rate limiting is still bypassable by omission. There is no `default_limits`,
no `SlowAPIMiddleware`, the absence-tripwire `test_match_routes_still_have_no_limiter` was deleted
in 146-02, and every surviving gate is self-referential over `limiter._route_limits` — a route
with no decorator never enters that dict, so it is invisible to
`test_rate_limited_route_set_is_a_literal` (`tests/test_limiter_identity.py:567-594`) and to
`test_every_registered_router_limit_is_shared_or_quarantined` (`:596-612`) by construction.

This plan makes RATE-03's promise — "a new route cannot silently bypass" — TRUE on the Python
side, using the same shape the TypeScript analogue uses
(`src/lib/seam-ratelimit-posture.invariant.test.ts`, whose own docblock records that it borrowed
the shape FROM `analytics-service/tests/test_limiter_identity.py` — so this is a shape coming
home, not an import).

Purpose: the anti-bypass-by-omission control for the Python API surface (ASVS V13, STRIDE DoS).
Output: one new pytest file. **Zero runtime behaviour changes** — no `default_limits`, no
middleware, no decorator moves.

⛔ **Out of scope, do not touch:** RATE-04 limit VALUE retuning is founder territory (D-146-4)
and the five candidates stay in `TODOS.md` with their measured numbers until the founder rules.
This plan asserts that every route is limited *or consciously quarantined*; it says nothing about
whether any limit is the right number.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-CONTEXT.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-RESEARCH.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-PATTERNS.md

@analytics-service/tests/test_limiter_identity.py
@src/lib/seam-ratelimit-posture.invariant.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Write the route-enumeration coverage gate</name>
  <files>analytics-service/tests/test_limiter_route_coverage.py</files>
  <precondition>`cd analytics-service && python3 -c "import main"` succeeds. It fails locally with `ModuleNotFoundError: pandera` on a stock machine — `pandera==0.32.1` is a DECLARED dependency that CI has and local envs often lack. Install it (`pip install 'pandera==0.32.1' --break-system-packages`) before starting; if `import main` still fails for a different reason, HALT and report it rather than writing a gate you cannot run.</precondition>
  <read_first>
    - `analytics-service/tests/test_limiter_identity.py` — `:58` and `:227` (the import-order comments about decoration-time population and sibling suites reloading modules), `:72-82` (the autouse limiter-reset fixture), `:465-546` (`TestClassClosure`, the quarantine-roster shape), `:546-566` (`test_app_state_limiter_is_the_same_object_the_decorators_use`, which already imports `main`), `:567-612` (the two self-referential gates this plan is the successor to).
    - `src/lib/seam-ratelimit-posture.invariant.test.ts:169-247` and `:288-306` — the partition, the vacuity fence and its message ("a guard that scans nothing agrees with everything, forever"), the `NO_LIMITER_QUARANTINE` equality assertion and its docblock reason ("a containment check would let a stale exemption outlive the thing it exempted").
    - `146.1-RESEARCH.md` §2.2 (the three probe-measured mechanics), §2.4 (the source-derived 20-route census and the 7-entry candidate quarantine roster with a cited reason per entry), §2.5 (the recommended skeleton and assertion order).
  </read_first>
  <behavior>
    - The module imports `main` INSIDE the test/fixture body, not at module scope, and reads `main.app.routes`.
    - The population is `[r for r in main.app.routes if isinstance(r, fastapi.routing.APIRoute)]`. FastAPI's built-in doc routes (`/openapi.json`, `/docs`, `/docs/oauth2-redirect`, `/redoc`) are plain `starlette.routing.Route` and are excluded by that isinstance check — MEASURED. ⛔ Never filter by path prefix.
    - The join key for a route is `f"{route.endpoint.__module__}.{route.endpoint.__name__}"`.
    - The limited key set is `set(limiter._route_limits) | set(limiter._dynamic_route_limits)`.
    - **Vacuity fence, asserted FIRST**: the APIRoute population is at least 18, and at least 10 of the derived keys are in the limited set. Each with a message naming the consequence.
    - **Partition equality**: `derived_keys - limited_keys == NO_LIMITER_QUARANTINE`, compared as sets by EQUALITY. Failure message reports both directions (`unexpected=` sorted, `missing=` sorted).
    - **Totality**: every derived key is in exactly one of {limited, quarantine} — no key in both, no key in neither.
    - **Dynamic arm**: the `routers.process_key.*` key(s) are present in `_dynamic_route_limits` specifically, not merely in the union.
    - The quarantine roster is a hand-typed frozenset with one cited reason comment per entry; it is NOT derived from anything.
  </behavior>
  <action>
Create `analytics-service/tests/test_limiter_route_coverage.py`.

⚠️ **Filename conflict, adjudicated.** `146.1-PATTERNS.md` §"File Classification" names this file
`test_route_limit_closure.py`; `146.1-RESEARCH.md` §2.5 names it `test_limiter_route_coverage.py`
and uses that name consistently in its Validation Architecture and Test Map tables. **Use
`test_limiter_route_coverage.py`** — RESEARCH gives the reason (the existing
`test_limiter_identity.py` is scoped to *identity*, i.e. which bucket a limited route keys on;
*coverage*, i.e. whether a route is limited at all, is a different property and mixing them makes
the vacuity fences harder to reason about) and PATTERNS' contribution is the file's SHAPE, which
this task follows in full. Record the adjudication in the module docstring so nobody re-opens it.

⛔ **A NEW FILE, not an addition to `test_limiter_identity.py`.** Both run in the same `python` CI
job, so nothing is lost.

**Module docstring** — state, in this order: the defect (rate limiting is bypassable by omission);
the mechanism (`_route_limits` is populated at decoration time, so an undecorated route never
enters it and is structurally invisible to every gate that iterates it); what the sibling gates
do NOT cover, naming `test_limiter_identity.py:567-594` and `:596-612` by line; and the
`test_match_routes_still_have_no_limiter` history — that gate was DELETED in 146-02 because the
two match routes now carry `30/minute` limits, and it must NOT be resurrected. Follow
`test_main_worker.py:1374-1385`'s register.

**Imports and ordering.** Import `main` inside the test body or a module-scoped fixture, never at
module import time — `test_limiter_identity.py:58` and `:227` document why (decoration-time
population, and sibling suites in the same process reload modules). Importing `main` is the safest
single import because it performs every `include_router`; importing individual routers risks a
partial surface, which is the vacuity failure this gate exists to prevent.

Add the **autouse limiter-reset fixture** copied from `test_limiter_identity.py:72-82` — reset
before AND after so test ordering never matters.

**The quarantine roster.** Hand-type a `frozenset[str]` of the endpoint keys that are
deliberately unlimited. `146.1-RESEARCH.md` §2.4 lists seven candidates with their anchors and
stated reasons: the three `routers/debug_key_flow.py` admin-gated SSE diagnostics, `routers/cron.py`
`/cron-sync`, `routers/match.py` `/cron-recompute` (which `services/rate_limit.py`'s docstring
declares unlimited on purpose), `routers/internal.py` `/keys/{key_id}/permissions` (which carries
a hand-rolled per-`key_id` 10/min token bucket at `:125`/`:231`), and `main.py` `/health`.
⚠️ **That census is SOURCE-derived, not runtime-derived** — the researcher could not
`import main` locally. Treat it as the EXPECTED roster and let a RED first run correct it: a RED
first run is the correction, not a failure. Each entry gets a comment carrying its file anchor,
its reason, and the shrink instruction ("if it is ever repaired, SHRINK this roster; the equality
below is deliberate so a repair cannot leave a stale exemption behind" —
`test_limiter_identity.py:484-486`).

⛔ **EQUALITY, never containment.** Copy the TS docblock's reason into the Python one verbatim in
substance: a containment check lets a stale exemption outlive the thing it exempted, and a NEW
entry appearing in the unlimited partition means an UNLIMITED route — decide whether that is
intended and write the reason down beside the roster.

⛔ **Do NOT add `default_limits` to the `Limiter`, and do NOT install `SlowAPIMiddleware`.** Both
were considered and rejected in `146.1-RESEARCH.md`'s Alternatives table: `default_limits` is a
runtime behaviour change on 20 live routes including cron and internal surfaces — that is
RATE-04-class work and out of scope — and the middleware path re-enters `__evaluate_limits` with
`in_middleware=True`, double-counting against `/process-key`'s dynamic limits. The enumeration
gate is the structural successor with zero runtime effect.

Assertion messages: every parse/derive step gets a "re-anchor, do not delete" message in the
register of `test_main_worker.py:1416-1419` — the gate must tell a future reader to re-point it,
never to remove it.

Run the gate from `analytics-service/` with `python3`. ⛔ A repo-root pytest run misses VCR
cassettes and makes LIVE broker calls.
  </action>
  <verify>
    <automated>
cd analytics-service
python3 -c "import main" || { echo "FAIL: import main - install pandera==0.32.1"; exit 1; }
python3 -m pytest tests/test_limiter_route_coverage.py -x -q || exit 1
python3 -m pytest tests/test_limiter_identity.py tests/test_rate_limit_contract.py -q || exit 1
F=tests/test_limiter_route_coverage.py
fail(){ echo "FAIL: $1"; exit 1; }
grep -q "main.app.routes" "$F" || fail "population is not derived from app.routes - the gate is self-referential"
grep -q "APIRoute" "$F" || fail "no APIRoute isinstance partition"
grep -qE 'startswith\("/docs|startswith\("/openapi|startswith\("/redoc' "$F" && fail "path-prefix filtering - use the isinstance check, doc routes are plain starlette Routes"
grep -q "_dynamic_route_limits" "$F" || fail "the CALLABLE-limit arm (/process-key) is not asserted"
grep -q "default_limits" "$F" && fail "default_limits touched - that is a runtime change and RATE-04 territory"
grep -q "SlowAPIMiddleware" "$F" && fail "middleware installed - explicitly rejected, it double-counts dynamic limits"
grep -qE '\bissubset\b|\b<=\b.*QUARANTINE|in NO_LIMITER_QUARANTINE' "$F" && fail "containment check found - the partition must be compared by EQUALITY"
grep -q "test_match_routes_still_have_no_limiter" "$F" || fail "the deleted-gate history is not recorded, so someone will resurrect it"
echo OK
    </automated>
  </verify>
  <done>`cd analytics-service && python3 -m pytest tests/test_limiter_route_coverage.py -x` is green; the population comes from `main.app.routes`; the partition is set-equality against a hand-typed quarantine roster where every entry carries an anchor, a reason and a shrink instruction; a vacuity fence asserts first; the dynamic arm is separately pinned; no runtime behaviour changed.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the gate RED under the named neuter, restore, and record the receipt</name>
  <files>analytics-service/tests/test_limiter_route_coverage.py</files>
  <action>
⭐ **A test that CANNOT FAIL is worse than none.** This task exists because the gate written in
task 1 asserts a negative ("no route is silently unlimited") and a negative assertion over a
derived population is the single most common way this repo has shipped a vacuous gate.

Run **three** named neuters, one at a time, restoring fully between each. For each, record the
EXACT assertion text pytest printed — not "it failed", the text.

**Neuter B1-OMIT (the load-bearing one).** Add a throwaway `@router.post("/gate-probe")` handler
to `analytics-service/routers/cron.py` with NO `@limiter.limit` decorator. Run the gate. The
partition-equality assertion must FAIL, and the failure message must name `routers.cron.gate_probe`
in its `unexpected=` list. Delete the probe. ⛔ Do NOT commit the probe, and do NOT leave it
behind a skip.

**Neuter B1-DROP.** Comment out the `@limiter.limit` decorator on one already-limited route (use
`routers/optimizer.py:42-43`, the smallest single-decoration site). The gate must FAIL naming that
route's key in `unexpected=`. Restore.

**Neuter B1-STALE.** Add one fictional key to the quarantine roster (e.g. a module that does not
exist). The gate must FAIL naming it in `missing=` — this proves the equality direction that
catches a stale exemption, which a containment check would silently allow. Remove it.

⚠️ If any neuter does NOT produce a RED, the gate is vacuous. Do not "fix the neuter" — fix the
gate, then re-run all three. A neuter that has to be made more aggressive to fail is telling you
the gate has a hole.

Then run the full Python suite and the type gate, and commit the gate file alone.
  </action>
  <verify>
    <automated>
cd analytics-service
python3 -m pytest tests/ -q || exit 1
python3 -m mypy --strict --follow-imports=silent services/ routers/ models/ || exit 1
cd ..
git status --porcelain analytics-service/routers/ | grep . && { echo "FAIL: a router file is dirty - a neuter probe was left behind"; exit 1; }
grep -rn "gate-probe\|gate_probe" analytics-service/routers/ && { echo "FAIL: throwaway probe still present"; exit 1; }
echo OK
    </automated>
  </verify>
  <done>All three neuters were RUN (not reasoned about) and each produced a RED with its assertion text captured verbatim in the SUMMARY; every router file is clean; the full `analytics-service` pytest suite and `mypy --strict` over `services/ routers/ models/` are green; only the new test file is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → FastAPI route surface (`main.app.routes`) | Any registered route is reachable; whether it is rate-limited is decided entirely by the presence of a decorator on the handler. |
| developer adding a route → CI | The gap this plan closes: today nothing in CI observes a NEW route's limiter status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.1-06 | Denial of Service | New FastAPI route shipped without `@limiter.limit` | high | mitigate | Partition-equality gate over `main.app.routes` vs `_route_limits ∪ _dynamic_route_limits`, with a hand-typed quarantine roster. Neuter B1-OMIT proves it fires. |
| T-146.1-07 | Denial of Service | A limit silently removed from an existing route | medium | mitigate | Same equality assertion catches it from the other direction. Neuter B1-DROP proves it. |
| T-146.1-08 | Repudiation | A stale quarantine entry outliving its route | medium | mitigate | EQUALITY not containment; neuter B1-STALE proves the `missing=` direction fires. |
| T-146.1-09 | Repudiation | The gate passes vacuously on a truncated route walk | high | mitigate | Sanity band asserted BEFORE the partition (≥18 APIRoutes, ≥10 limited), each with a consequence-naming message. |
| T-146.1-10 | Denial of Service | `default_limits`/middleware silently retunes 20 live routes | medium | accept | Explicitly NOT taken. RATE-04 limit values are founder territory (D-146-4). The gate has zero runtime effect; `<verify>` grep-refuses both mechanisms. |
| T-146.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | Installs nothing new. `pandera==0.32.1` in the precondition is an ALREADY-DECLARED dependency present in CI; the local install only reconciles a known local-env gap. `146.1-RESEARCH.md` records the Package Legitimacy Audit as not applicable to this phase. |
</threat_model>

<verification>
1. `cd analytics-service && python3 -m pytest tests/test_limiter_route_coverage.py -x` green.
2. `cd analytics-service && python3 -m pytest tests/` green (no sibling suite broken by the new import of `main`).
3. `cd analytics-service && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` green. (`tests/` is outside the canonical mypy scope, so the new file carries no strict-typing burden — but the suite must not have perturbed the scoped modules.)
4. All three neuters observed RED with captured text.
5. `git status --porcelain analytics-service/routers/` is empty.
</verification>

<success_criteria>
- [ ] `analytics-service/tests/test_limiter_route_coverage.py` exists and passes from `analytics-service/` with `python3`.
- [ ] Population derived from `main.app.routes` filtered by `isinstance(r, APIRoute)` — no path-prefix filtering, no derivation from `_route_limits`.
- [ ] Join key is `endpoint.__module__ + "." + endpoint.__name__`.
- [ ] A vacuity sanity band (≥18 routes, ≥10 limited) asserts BEFORE the partition, each with a consequence-naming message.
- [ ] The unlimited partition is compared to a hand-typed `frozenset` by EQUALITY; every entry carries a file anchor, a cited reason and a shrink instruction.
- [ ] `_dynamic_route_limits` is separately asserted to still hold the `/process-key` key(s).
- [ ] The module docstring names `test_limiter_identity.py:567-594` and `:596-612` as the self-referential gates this succeeds, and records that `test_match_routes_still_have_no_limiter` was deleted in 146-02 and must not be resurrected.
- [ ] The docstring records the `test_limiter_route_coverage.py` vs `test_route_limit_closure.py` filename adjudication.
- [ ] Zero runtime changes: no `default_limits`, no `SlowAPIMiddleware`, no decorator added/removed/moved in any router.
- [ ] Neuters B1-OMIT, B1-DROP and B1-STALE each observed RED, with the assertion text recorded in the SUMMARY.
</success_criteria>

<falsifiability_ledger>
| # | Success criterion | What makes it go RED | Named neuter |
|---|---|---|---|
| F-02-1 | An undecorated new route reddens the gate | Derive the population from `_route_limits` instead of `app.routes`, or use containment | **B1-OMIT**: throwaway `@router.post("/gate-probe")` in `routers/cron.py`, no limiter. Must fail with `routers.cron.gate_probe` in `unexpected=`. If it passes, the gate is still self-referential and the whole plan failed. |
| F-02-2 | A removed limit reddens the gate | Same as above | **B1-DROP**: comment out `@limiter.limit` at `routers/optimizer.py:42-43`. Must fail naming that key. |
| F-02-3 | A stale exemption reddens the gate | Change equality to containment | **B1-STALE**: add a fictional key to the quarantine roster. Must fail in the `missing=` direction. This is the direction containment silently swallows. |
| F-02-4 | The gate cannot pass on an empty walk | Delete the sanity band | Monkeypatch/stub the route list to `[]` in a scratch run; the sanity band must fail FIRST with its own message, before the partition assertion trivially succeeds. |
| F-02-5 | The dynamic arm is not lost | Convert `/process-key`'s callable limits to static | The `_dynamic_route_limits` assertion goes RED while the union-based partition stays green — which is exactly why it is asserted separately. |
| F-02-6 | Zero runtime effect | Add `default_limits` to the Limiter | `<verify>`'s grep fails, and `tests/test_rate_limit_contract.py` / `test_limiter_identity.py` would move — a runtime change cannot land silently under this plan. |
| F-02-7 | The census was source-derived, not measured | — | Falsified on FIRST RUN: if the roster is wrong, the gate REDs and the diff names the correction. ⚠️ Record the first-run diff in the SUMMARY even if it was green — "the census was correct" is itself a measurement worth having, and it is the only evidence that `import main` produced the full surface. |
</falsifiability_ledger>

<output>
Create `.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-02-SUMMARY.md` when done.

The SUMMARY MUST contain: the FINAL quarantine roster as committed (not the source-derived
candidate list), the delta from `146.1-RESEARCH.md` §2.4's expected roster if any, and the
verbatim pytest assertion text from each of the three neuters.
</output>
