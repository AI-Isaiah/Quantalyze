---
phase: 146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty
plan: 06
type: execute
wave: 4
depends_on: ["146.1-05"]
files_modified:
  - src/lib/process-key-client.ts
  - src/app/api/keys/sync/route.ts
  - src/app/api/verify-strategy/route.ts
  - src/lib/process-key-client.test.ts
  - src/app/api/keys/sync/route.seam.test.ts
  - src/app/api/keys/sync/route.test.ts
  - src/app/api/verify-strategy/route.seam.test.ts
  - src/lib/resilient-fetch.ts
  - src/lib/seam-redaction.ts
  - analytics-service/services/db.py
  - .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
autonomous: true
requirements: [B2]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No live end-user Supabase JWT crosses the Vercel→Railway boundary on a keys/sync call (B2)."
    - "No live end-user Supabase JWT crosses the Vercel→Railway boundary on a verify-strategy call (B2)."
    - "A re-introduction of the forward is a CONSCIOUS act: an invariant gate derives the emitter set from source and compares it to an EMPTY roster by equality, so a new emitter reds CI by name."
    - "The core's transport-error redaction still scrubs every credential-bearing outgoing header, because `credentialHeaderValues()` DERIVES the per-request secrets from the headers the core was asked to send — it never depended on a caller-declared array."
    - "The Phase 140.2 forward-looking obligation is AMENDED in the same commit as the code change, everywhere the claim appears — not only in the file the executor happened to have open."
    - "The Python-side ownership pre-check is unchanged: it is still the explicit `strategies` id+user_id filter, and `test_process_key.py:2220-2221` still pins the non-use of `get_user_scoped_supabase`."
  artifacts:
    - .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
  key_links:
    - "The Python reader `get_user_scoped_supabase` (services/db.py:80-118) has ZERO production callers — referenced only by tests/test_db.py and by two NEGATIVE assertions — and `analytics-service/tests/test_process_key.py:2220-2221` actively pins the non-use with `not hasattr(...)`. Nothing in analytics-service reads the header at all; only db.py's docstring mentions it."
    - "An obligation that survives the code it justifies is how the three false forwarding comments this phase already corrected came to exist. The amendment is NOT optional and NOT deferrable to a later commit."
    - "`resilient-fetch.ts`'s outgoing-header scrub list KEEPS the header name — removing it would be an instance fix that re-opens the class for any future emitter."
---

<objective>
Discharge **B2**: `X-User-Access-Token` — drop-versus-wire. <!-- planner-discipline-allow: X-User-Access-Token -->

**The verified facts.** Two live forwarders, both session-conditional: `keys/sync/route.ts:435-459`
and `verify-strategy/route.ts:253-305`. The Python reader `get_user_scoped_supabase`
(`analytics-service/services/db.py:80-118`) has **ZERO production callers** — it is referenced only
by `tests/test_db.py` and by two NEGATIVE assertions. A gate at
`analytics-service/tests/test_process_key.py:2220-2221` **actively pins the non-use**
(`not hasattr(process_key_module, "get_user_scoped_supabase")`). Nothing in `analytics-service`
reads the header at all — only `db.py`'s docstring mentions it. Meanwhile a LIVE end-user Supabase
JWT crosses the Vercel→Railway boundary on every keys/sync and verify-strategy call, and is
embedded verbatim by undici into transport error messages, which is precisely why
`process-key-client.ts:650-657` scrubs it.

⚖️ **FOUNDER CALL — the recommended option is planned; the alternative is NOT baked in.**
**Option (a): STOP forwarding, and amend the Phase 140.2 obligation in the SAME commit.** A
credential that is transmitted and never read is pure exposure surface, and the obligation it
serves has an in-place substitute — the explicit Python ownership filter at
`routers/process_key.py:1147-1157` — which is already shipped and gated.

⛔ **Option (b), wiring the user-scoped client, is NOT in scope.** It would require flipping
`test_process_key.py:2220`, and an RLS analysis for every read that would newly run as the user
rather than `service_role` — a money-path change in a close-out phase, and it makes the live JWT
*load-bearing* rather than merely present. Record it as a NAMED, NOT-TAKEN option with its cost.

⭐ **The amendment is the load-bearing half, not the footnote.** A scope amendment that touches
one file is incomplete: the refused claim survives in the other artifacts and is re-cited later as
fact. `git grep` the claim across the WHOLE repo and amend every instance in this same commit.

Purpose: reduce a live-credential exposure surface to zero and close the obligation/behaviour gap
that generated three false comments in this codebase already.
Output: two routes that no longer read the session for this purpose, a client with no forwarding
option, a source-derived zero-emitter gate, and an amended obligation record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-CONTEXT.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-RESEARCH.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-PATTERNS.md

@src/lib/process-key-client.ts
@src/app/api/keys/sync/route.ts
@src/app/api/verify-strategy/route.ts
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Stop forwarding — both routes and the client option — keeping every scrub intact</name>
  <files>src/app/api/keys/sync/route.ts, src/app/api/verify-strategy/route.ts, src/lib/process-key-client.ts, src/lib/resilient-fetch.ts, src/lib/seam-redaction.ts</files>
  <precondition>`node_modules/.bin/vitest` exists. GSD worktree agents get none; `npx vitest` exits MODULE_NOT_FOUND. Run `npm ci` or HALT.</precondition>
  <precondition>146.1-05 is merged into this working tree. It corrected `src/app/api/strategies/csv-finalize/route.ts:1005-1010`'s false forwarding claim; ⛔ do NOT re-edit that file in this plan.</precondition>
  <read_first>
    - `src/app/api/keys/sync/route.ts:430-460` (the session read and the non-blocking catch), `:590-615` (the args type and the pass-through).
    - `src/app/api/verify-strategy/route.ts:15-25` (the file docblock's claim), `:245-310` (the session read, the two pass-through sites at `:285` and `:305`).
    - `src/lib/process-key-client.ts:345-362` (the option and its docblock), `:495-510` (the conditional header spread), `:645-665` (the scrub call and its per-request-secret argument).
    - `src/lib/resilient-fetch.ts:1910-1926` (the comment claiming a kept option has future callers), `:2510-2520` and `:2540-2550` (the outgoing-header scrub enumeration and `credentialHeaderValues()`).
    - `src/lib/seam-redaction.ts:20-30` (the docblock claim).
  </read_first>
  <action>
Remove the forwarding at all three levels, in this order.

**1. The two routes.** In `keys/sync/route.ts` remove the session read whose only purpose is this
header, its non-blocking catch and its warn, and stop passing the value onward. Do the same at
both pass-through sites in `verify-strategy/route.ts`. ⚠️ Read each surrounding block before
cutting: `keys/sync` is deliberately BEST-EFFORT (an unreadable session must not fail a resync at
the money-onboarding chokepoint) and `verify-strategy` is PUBLIC (with no session, NOTHING is
forwarded, asserted negatively because a fabricated value there is elevation of privilege). If a
session read serves any OTHER purpose in the same block, keep that purpose and remove only this
one. Do not collapse an unrelated try/catch.

**2. The client option.** Remove the `userAccessToken` option from `postProcessKey`'s args type,
its docblock paragraph, and the conditional header spread at `:503-504`.

⚠️ **Before removing, VERIFY the redaction class fix does not depend on it.**
`resilient-fetch.ts`'s `credentialHeaderValues()` DERIVES the per-request secrets from the outgoing
headers the core was asked to send — that derivation is what made the 140.3-02 fix a CLASS result
rather than an instance one, and it is why removing a caller-declared secret cannot re-open the
leak. Read `:2510-2550` and confirm this before touching `process-key-client.ts:661`. If the
derivation turns out to depend on the caller-declared array, STOP and report — the removal order
must then change.

**3. Comments and scrub lists.**
⛔ **KEEP the header name in `resilient-fetch.ts`'s outgoing-header scrub enumeration.** Removing
it would be an instance fix that re-opens the class the moment any future emitter appears. Correct
only the surrounding prose: `:1914-1923` currently reasons about "any future caller of the kept
`userAccessToken` option", which stops being true.
Correct `seam-redaction.ts:24`'s docblock and `verify-strategy/route.ts`'s file docblock the same
way — state what the boundary carries TODAY and note that the header remains on the scrub list on
purpose. ⛔ Do not delete any `scrubSeamError` call or any scrub-list entry.

**4. Python-side comment only.** `analytics-service/services/db.py`'s `get_user_scoped_supabase`
docstring is the only place in `analytics-service` that mentions the header. Amend it to record
that no caller forwards it as of Phase 146.1, so the function's own docs stop implying a live
transport. ⛔ Do NOT delete `get_user_scoped_supabase` and do NOT touch
`analytics-service/tests/test_process_key.py:2220-2221` — that gate pins the non-use and must stay
GREEN unchanged. Removing the function would be scope creep into option (b)'s territory from the
opposite direction.

Commit as its own commit — but ⚠️ read task 3 first: the obligation amendment must land in the
SAME commit as this code change. Stage this task's changes, complete task 3's amendment, and make
ONE commit covering both. Task 2's test inversion may be its own commit.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
H='X-User-Access-Token'
# No route or client may SET the header on an outgoing request any more.
grep -rn "\"$H\":" src/app/ src/lib/ --include='*.ts' --include='*.tsx' | grep -v '\.test\.' | grep . && fail "an outgoing emitter survives"
grep -rn 'userAccessToken' src/app/api/keys/sync/route.ts src/app/api/verify-strategy/route.ts src/lib/process-key-client.ts | grep -v '^\s*\*' | grep . && fail "the option or a caller survives"
# The scrub list must NOT have been pruned.
grep -q "$H" src/lib/resilient-fetch.ts || fail "the header was removed from the core scrub enumeration - that is an instance fix that re-opens the class"
grep -q 'credentialHeaderValues' src/lib/resilient-fetch.ts || fail "the derived-secrets mechanism was disturbed"
grep -rq 'scrubSeamError' src/lib/process-key-client.ts || fail "a scrub call was deleted"
# The Python non-use gate must be untouched.
git diff --quiet analytics-service/tests/test_process_key.py || fail "the non-use gate was edited - it must stay green unchanged"
grep -q 'get_user_scoped_supabase' analytics-service/services/db.py || fail "the reader was deleted - that is option (b) territory from the wrong side"
npm run typecheck || fail "typecheck red"
echo OK
    </automated>
  </verify>
  <reversibility rating="reversible">The transport is roughly twenty lines to rebuild if the 140.2 obligation is ever re-opened, and this plan's SUMMARY records exactly which lines were removed from which anchors. The scrub plumbing and the header's place on the core scrub list are deliberately KEPT, so a rebuild is a re-add, not a re-design.</reversibility>
  <done>Neither route reads the session for this purpose; `postProcessKey` has no forwarding option; no non-test file under `src/` sets the header on an outgoing request; the core's scrub enumeration and `credentialHeaderValues()` derivation are intact and the header is still on the scrub list; `db.py`'s docstring is amended; `get_user_scoped_supabase` and its non-use gate are untouched; typecheck is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invert the wire assertions and add a zero-emitter equality gate</name>
  <files>src/lib/process-key-client.test.ts, src/app/api/keys/sync/route.seam.test.ts, src/app/api/keys/sync/route.test.ts, src/app/api/verify-strategy/route.seam.test.ts</files>
  <read_first>
    - `src/lib/process-key-client.test.ts:51-56` (the describe's premise), `:88-103` (the forwards-when-set case), `:108-120` (the omits-when-unset case).
    - `src/app/api/keys/sync/route.seam.test.ts:67`, `:131`, `:415-445` (the ON THE WIRE case and its scrub-side fixture at `:445`).
    - `src/app/api/verify-strategy/route.seam.test.ts:28`, `:237-247` (the ON THE WIRE case), `:272` (the negative assertion for the no-session path), `:283` (the scrub-side fixture).
    - `src/lib/seam-log-coverage.test.ts:22`, `:757`, `:938` — prose and enumeration that reference the header; check whether any of it is an assertion or only a comment before editing.
  </read_first>
  <behavior>
    - `keys/sync` with a readable session → the outgoing request headers do NOT contain the header. (Inverted from `:422-435`.)
    - `verify-strategy` with a readable session → the outgoing request headers do NOT contain the header. (Inverted from `:237-247`.)
    - `verify-strategy` with NO session → still nothing forwarded. Unchanged; this case must stay green untouched, and it is the proof that the inversion did not simply delete assertions.
    - `postProcessKey` never emits the header regardless of its arguments.
    - **Scrub regression preserved**: a transport error whose message embeds a token-bearing header is still redacted by the core, and `ECONNREFUSED` still survives the scrub. Both halves — the credential is gone AND the diagnostic survives. Keep the existing fixtures at `keys/sync/route.seam.test.ts:445` and `verify-strategy/route.seam.test.ts:283`; they exercise the class fix, which outlives this caller.
    - **New zero-emitter gate**: the set of `src/` non-test files that SET this header on an outgoing request is derived from source and compared by EQUALITY to an EMPTY roster.
  </behavior>
  <action>
Invert the three "ON THE WIRE" assertions to "NOT on the wire", and update each describe/docstring
to state the new premise and WHY — a live end-user JWT was crossing a service boundary whose only
reader has zero callers. ⛔ Do NOT delete these cases. An inverted assertion still fires when
someone re-adds the forward; a deleted one never does, and deletion is indistinguishable from
"we stopped caring".

⛔ **Keep every scrub-side fixture.** The `ECONNREFUSED`-with-embedded-headers fixtures exist
because the core's redaction is a CLASS fix (`credentialHeaderValues()` derives secrets from
outgoing headers) that must keep working for whatever the boundary carries next. They are not
about this caller.

**Add the zero-emitter gate.** House shape, copied from
`src/lib/seam-ratelimit-posture.invariant.test.ts:169-306` and
`src/__tests__/admin-csrf-ratelimit-grep.test.ts:64-80`:
1. Derive the population from disk — walk `src/` for non-test `.ts`/`.tsx` files whose source sets
   this header on an outgoing request object.
2. **A vacuity fence FIRST**, with a message naming the consequence: assert the walker actually
   visited a plausible number of files, because a walker that stopped walking agrees with every
   assertion below it, forever. Corroborate by asserting the walker DOES find the header name in
   `src/lib/resilient-fetch.ts`'s scrub enumeration — a known, deliberate, non-emitting occurrence.
   That corroborator is what proves the scan is looking in the right place at all.
3. Compare the emitter set to an **empty roster by EQUALITY**, never containment, with the roster
   declared as a named constant carrying the reason and the shrink/grow instruction: a NEW entry
   here means a live end-user JWT is crossing a service boundary again — decide whether that is
   intended and write the reason down beside this roster.
4. Cite the B2 adjudication and the amended obligation in the docblock so the gate explains itself.

Put the gate wherever the seam invariants live (`src/lib/*.invariant.test.ts` is the established
home). Run it, then run the neuters in the Falsifiability Ledger.

Commit as its own commit.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
npx --no-install vitest run src/lib/process-key-client.test.ts src/app/api/keys/sync/route.seam.test.ts src/app/api/keys/sync/route.test.ts src/app/api/verify-strategy/route.seam.test.ts src/lib/seam-redaction.test.ts src/lib/seam-log-coverage.test.ts src/lib/resilient-fetch.test.ts src/lib/resilient-fetch.retry.test.ts --no-file-parallelism --reporter=basic || fail "seam suites red"
G=$(git diff --name-only HEAD~1 2>/dev/null; ls src/lib/*.invariant.test.ts)
grep -rl 'ECONNREFUSED' src/app/api/keys/sync/route.seam.test.ts src/app/api/verify-strategy/route.seam.test.ts >/dev/null || fail "a scrub-side fixture was deleted - the class fix lost its coverage"
grep -q 'no session' src/app/api/verify-strategy/route.seam.test.ts || fail "the no-session negative case was collateral damage"
npm run typecheck || fail "typecheck red"
npm run lint || fail "lint red"
echo OK
    </automated>
  </verify>
  <done>The three wire assertions are INVERTED (not deleted) with restated premises; the no-session negative case and both scrub-side `ECONNREFUSED` fixtures are untouched and green; a source-derived zero-emitter gate exists with a vacuity fence, a non-emitting corroborator, and empty-roster EQUALITY; all seam suites, typecheck and lint are green.</done>
</task>

<task type="auto">
  <name>Task 3: Amend the Phase 140.2 obligation — everywhere it appears, in the same commit as task 1</name>
  <files>.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md</files>
  <read_first>
    - `.planning/phases/140.1-.../140.1-TS-OBLIGATIONS.md:251` — the TS-15 row. Note carefully: TS-15 itself is marked ✅ SATISFIED and that is HISTORICALLY TRUE — the forwarding did ship. What this plan retires is the row's forward-looking obligation in its last column ("Forward `X-User-Access-Token` on every authenticated flow, so a user-scoped pre-check becomes possible and the Python ownership filter becomes belt-and-braces rather than the only belt"). ⛔ Do NOT rewrite history; supersede it with a dated note.
  </read_first>
  <action>
⭐ **A scope amendment that touches ONE file is incomplete.** Before editing anything, enumerate
every instance of the claim across the WHOLE repository — not just `.planning/`, and not just the
file you have open. Run a repo-wide search for the forward-looking sentence and its paraphrases
(the "on every/all authenticated flows" phrasing, the "user-scoped pre-check" phrasing, the
"belt-and-braces rather than the only belt" phrasing, and the "only csv forwards" phrasing), and
paste the full hit list into the SUMMARY. Known homes to expect, from
`146.1-RESEARCH.md` §9.4 and this session's grep: `140.1-TS-OBLIGATIONS.md:251`,
`140.1-RESEARCH.md:472-475` and `:1062`, `140.1-RESEARCH-B.md`, `140.1-01-SUMMARY.md:232`,
`140.1-02-SUMMARY.md:306`, `140.1-09-PLAN.md:130`. Treat that list as a starting point that your
grep must reproduce or extend, never as the answer.

Amend each instance with a **dated superseding note**, not a deletion:
- what was decided (option (a), stop forwarding);
- when (2026-08-18 review, Phase 146.1) and on what evidence (zero production callers of
  `get_user_scoped_supabase`; a gate at `test_process_key.py:2220-2221` actively pinning the
  non-use; nothing in `analytics-service` reading the header);
- what the in-place substitute is (the explicit `strategies` id+user_id filter at
  `routers/process_key.py:1147-1157`, already shipped and gated), so the obligation is DISCHARGED
  by substitution rather than abandoned;
- the NOT-TAKEN option (b) with its cost (flipping `test_process_key.py:2220`; an RLS analysis for
  every read that would newly run as the user rather than `service_role`);
- and where the code now lives (this plan's commit), so a reader can get from claim to diff in one
  hop.

⛔ **Never delete the historical TS-15 SATISFIED record.** It was true when written. Superseding
notes preserve the audit trail; rewrites destroy it and make the next reviewer distrust the whole
ledger.

⚠️ `.planning/` is TRACKED and this repository is PUBLIC. `git add` these files — do not assume
they are ignored — and write nothing in the amendment that is not already public in this repo (no
tokens, no project refs beyond those already committed).

⚠️ Also check `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` for any row that cites this
obligation as outstanding, and amend the same way if found.

Stage this amendment WITH task 1's code change and make ONE commit. An obligation that survives
the code it justifies is exactly how the three false forwarding comments this phase already
corrected came to exist; splitting them across commits re-creates the window.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
O=.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
grep -q '146.1' "$O" || fail "the obligation record does not reference the superseding phase"
grep -q '2026-08-18' "$O" || fail "the amendment is undated"
grep -qi 'not.taken\|NOT TAKEN\|option (b)' "$O" || fail "the not-taken option and its cost are not recorded"
grep -q 'TS-15' "$O" || fail "the historical TS-15 record was destroyed rather than superseded"
# Every remaining live claim must be annotated, not silently left standing.
for f in $(git grep -l -- 'authenticated flow' .planning/ | sort -u); do
  grep -q '146.1' "$f" || { echo "UNAMENDED: $f"; UNAMENDED=1; }
done
[ -n "$UNAMENDED" ] && fail "at least one instance of the claim survives unamended - a one-file amendment is an incomplete amendment"
git status --porcelain .planning/ | grep . || fail "nothing staged under .planning/ - it is TRACKED, git add it"
echo OK
    </automated>
  </verify>
  <done>Every repo-wide instance of the forward-looking obligation carries a dated superseding note naming the decision, its evidence, the in-place substitute, the not-taken option with its cost, and the commit; the historical TS-15 SATISFIED record is preserved; `.planning/` changes are staged; the amendment and task 1's code change are ONE commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel route → Railway `analytics-service` | Crossed on every keys/sync and verify-strategy call. Today it carries a LIVE end-user Supabase JWT that no reader on the far side consumes. |
| undici transport error → Vercel log | undici inlines outgoing headers into `err.message`; the core's derived-secret scrub is the only thing between that and the log. |
| `.planning/` → the public internet | This repository is PUBLIC and `.planning/` is TRACKED. Everything written in the amendment is world-readable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.1-29 | Information Disclosure | Live end-user JWT crossing a service boundary with zero readers | high | mitigate | Option (a): remove the forward at both routes and remove the client option. Exposure goes to zero rather than being merely scrubbed on the error path. |
| T-146.1-30 | Information Disclosure | The forward silently returning in a later change | medium | mitigate | Source-derived zero-emitter gate compared by EQUALITY to an empty roster, with a vacuity fence and a non-emitting corroborator. |
| T-146.1-31 | Information Disclosure | The class-level scrub weakened as "cleanup" | high | mitigate | The header STAYS on the core scrub enumeration; `credentialHeaderValues()` and every `scrubSeamError` call are preserved; `<verify>` grep-refuses their removal and runs the redaction + log-coverage suites. |
| T-146.1-32 | Repudiation | An obligation outliving the code it justifies | high | mitigate | Repo-wide amendment in the SAME commit, with a loop in `<verify>` that fails on any unamended instance. This exact failure mode produced three false comments in this codebase already. |
| T-146.1-33 | Elevation of Privilege | Drifting into option (b) from the wrong side (deleting the reader, flipping the non-use gate) | medium | mitigate | `get_user_scoped_supabase` and `test_process_key.py:2220-2221` are explicitly untouched; `<verify>` asserts both. |
| T-146.1-34 | Information Disclosure | Secrets written into a public planning artifact | medium | mitigate | The amendment restates only facts already committed to this public repo; no tokens, no new project refs. |
| T-146.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed. Package Legitimacy Audit recorded not-applicable in `146.1-RESEARCH.md`. |
</threat_model>

<verification>
1. Each task's `<verify>` exits 0.
2. `npm test` green. ⚠️ Local flakes → `--no-file-parallelism`; a CI-only failure is not a flake (CI Node 22 vs local Node 25).
3. `cd analytics-service && python3 -m pytest tests/test_process_key.py tests/test_db.py -q` green — the non-use gate and the reader's own tests are untouched.
4. `cd analytics-service && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` green (the `db.py` docstring edit must not perturb it).
5. `npm run lint`, `npm run typecheck` green.
6. `git show --stat` on the amendment commit shows BOTH the code change and the `.planning/` amendment.
</verification>

<success_criteria>
- [ ] Neither `keys/sync/route.ts` nor `verify-strategy/route.ts` reads the session in order to forward this header, and neither passes the value onward.
- [ ] `postProcessKey` has no forwarding option, no conditional header spread for it, and no docblock paragraph describing it.
- [ ] No non-test file under `src/` sets the header on an outgoing request; the invariant gate asserts this by EQUALITY against an empty roster.
- [ ] The header REMAINS on `resilient-fetch.ts`'s outgoing-header scrub enumeration; `credentialHeaderValues()` and every `scrubSeamError` call are intact.
- [ ] The three "ON THE WIRE" assertions are inverted, not deleted; the `verify-strategy` no-session negative case and both `ECONNREFUSED` scrub fixtures are unchanged and green.
- [ ] `db.py`'s docstring records that no caller forwards it as of Phase 146.1; `get_user_scoped_supabase` still exists; `test_process_key.py:2220-2221` is byte-unchanged and green.
- [ ] `resilient-fetch.ts`, `seam-redaction.ts` and `verify-strategy/route.ts`'s docblock claims are corrected to describe today's boundary.
- [ ] Every repo-wide instance of the forward-looking obligation carries a dated superseding note with the decision, the evidence, the in-place substitute, the not-taken option (b) and its cost, and the commit reference.
- [ ] The historical TS-15 SATISFIED record is preserved, not rewritten.
- [ ] The code change and the obligation amendment are ONE commit.
- [ ] ⛔ Nothing in this plan's diff touches `src/app/api/strategies/csv-finalize/route.ts` — 146.1-05 owns it.
</success_criteria>

<falsifiability_ledger>
| # | Success criterion | What makes it go RED | Named neuter — RUN IT, then restore |
|---|---|---|---|
| F-06-1 | keys/sync does not put the JWT on the wire | Re-add the session read and the pass-through | **Neuter B2-READD-SYNC**: restore the forward at `keys/sync`. The inverted seam assertion must RED, AND the zero-emitter gate must RED naming the file. Two independent detectors — if only one fires, the other is not actually wired. |
| F-06-2 | verify-strategy does not put the JWT on the wire | Re-add the two pass-throughs | **Neuter B2-READD-VERIFY**: same, at both sites. Both sites matter: a one-site neuter cannot detect a half-revert. |
| F-06-3 | The zero-emitter gate is not vacuous | Break the file walker (point it at an empty dir) | **Neuter B2-BLINDWALK**: the vacuity fence must fail FIRST, before the equality assertion trivially passes. And the non-emitting corroborator in `resilient-fetch.ts` must stop being found — that corroborator is the only thing proving the scan reads the right files. |
| F-06-4 | The class-level scrub survives | Remove the header from the core scrub enumeration | **Neuter B2-UNSCRUB**: `seam-redaction.test.ts` and/or `seam-log-coverage.test.ts` must RED, and the `ECONNREFUSED` fixtures must show the credential surviving. If they stay green, the scrub coverage was about the caller, not the class, and must be strengthened before this plan ships. |
| F-06-5 | The diagnostic survives the scrub | Scrub the whole message | The `ECONNREFUSED` half of each fixture must RED. Both halves are load-bearing: redacting everything is not a fix, it is a different bug. |
| F-06-6 | The no-session path still forwards nothing | Forward a fabricated empty value when no session exists | The `verify-strategy` no-session case must RED. On a PUBLIC route a fabricated value is elevation of privilege, not a compatibility shim. |
| F-06-7 | The obligation is amended everywhere | Amend only `140.1-TS-OBLIGATIONS.md` | The `<verify>` loop over `git grep -l 'authenticated flow' .planning/` must report at least one UNAMENDED file. ⚠️ Run this neuter deliberately — revert all but one amendment and confirm the loop catches it. A loop that never fires is not a check. |
| F-06-8 | History is preserved | Delete the TS-15 SATISFIED row | The `grep -q 'TS-15'` check REDs. This criterion exists because "clean up the stale row" is the tempting wrong move. |
| F-06-9 | The Python non-use gate is untouched | Edit `test_process_key.py:2220` | `git diff --quiet` on that file REDs. Flipping it is option (b), which is out of scope. |
| F-06-10 | The reader is not deleted | Delete `get_user_scoped_supabase` | `grep -q` REDs. Deleting the reader would foreclose option (b) unilaterally — the founder retains that choice. |
</falsifiability_ledger>

<output>
Create `.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-06-SUMMARY.md` when done.

The SUMMARY MUST contain: (1) the FULL repo-wide grep hit list for the obligation claim, before and
after amendment; (2) per Falsifiability Ledger row, OBSERVED with the verbatim failing assertion or
NOT OBSERVED; (3) the exact line ranges removed from each of the three source files, so option (a)
is reversible from the record alone; and (4) confirmation, read from `resilient-fetch.ts:2510-2550`,
that `credentialHeaderValues()` derives per-request secrets from outgoing headers and therefore did
not depend on the removed caller-declared array.
</output>
