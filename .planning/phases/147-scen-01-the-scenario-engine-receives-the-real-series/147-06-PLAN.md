---
phase: 147-scen-01-the-scenario-engine-receives-the-real-series
plan: 06
type: execute
wave: 4
depends_on: ["147-05"]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/__tests__/phase-147-series-resolution-guards.test.ts
  - TODOS.md
  - .planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-VALIDATION.md
autonomous: true
requirements: [SCEN-01]

must_haves:
  truths:
    - "Reopening a saved scenario or refreshing the page re-fetches every added strategy's series — the founder's acceptance anchor survives a browser refresh (orchestrator ruling: P6 is IN SCOPE)"
    - "A future bare daily_returns reader anywhere in src/ fails CI — the no-third-mechanism invariant (SC2) is structural, not observed"
    - "getPortfolioStrategies consumers audited; findings logged to TODOS.md with NO scope expansion"
  artifacts:
    - path: "src/__tests__/phase-147-series-resolution-guards.test.ts"
      provides: "SC2 structural grep-gate: repo-scan + per-surface allowlist pins"
      min_lines: 60
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Hydration effect firing fetchAddedReturns for reopened/refreshed drafts"
      contains: "fetchAddedReturns"
  key_links:
    - from: "ScenarioComposer.tsx hydration effect"
      to: "fetchAddedReturns"
      via: "same guard predicate as the :2096 add seam (!strategyById.has(id) && addedReturnsById[id] === undefined)"
      pattern: "fetchAddedReturns\\("
    - from: "src/__tests__/phase-147-series-resolution-guards.test.ts"
      to: "all four fixed read sites + 2 reference pages"
      via: "readFileSync allowlist pins asserting both columns + resolver invocation"
      pattern: "resolveDailyReturnSeries"
---

<objective>
Close the phase: (1) fix the reopen/refresh gap — `fetchAddedReturns` fires only on the two
add seams, so a page refresh or saved-scenario reopen renders `[]` again and the SCEN-01
symptom survives the fix (RESEARCH P6; orchestrator ruled IN SCOPE because the founder's
acceptance anchor fails on first refresh otherwise); (2) build the SC2 structural grep-gate
that makes "one mechanism" a CI invariant; (3) run the class-closure audit of
getPortfolioStrategies consumers (log to TODOS.md, do NOT expand scope) and finalize
147-VALIDATION.md.

Purpose: SC2's structural clause + the only path to a refresh-stable acceptance walkthrough.
Output: hydration effect + test, the phase-147 gate with recorded non-vacuity, closed ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-RESEARCH.md
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-PATTERNS.md
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hydration effect — reopened/refreshed drafts re-fetch added series (P6)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (targeted: :1281-1317 — fetchAddedReturns + lazyAbortRef idempotence guard; :2093-2102 — the add-seam guard predicate to REUSE verbatim; :1484 — openSavedScenario, which never calls fetchAddedReturns)
    - 147-PATTERNS.md §"ScenarioComposer.tsx" P6 subsection (the two existing call sites, the idempotence excerpt at :1282-1285, and the guard predicate)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (the LAZY_ID fixture + fetch-mock seam from 147-05)
  </read_first>
  <behavior>
    - Test reopen: mount the composer with a draft already containing an added strategy NOT in the book (simulating localStorage hydration / saved-scenario reopen) → the returns route fetch fires for that id without any user interaction, and the row leaves the loading state with the fetched series rendered
    - Test book skip: a draft-added strategy that IS in the book → NO lazy fetch fires (the book value is authoritative; same guard as the add seam)
    - Test idempotence: an id already resolved in addedReturnsById or in-flight via lazyAbortRef → NO duplicate fetch across re-renders (effect re-runs must not double-fetch)
    - Test failure surface: the hydration-triggered fetch failing → the existing WR-01 retry/error surface renders, not a silent []
  </behavior>
  <action>
    Add ONE effect (useEffect) that iterates scenario.draft.addedStrategies and calls
    fetchAddedReturns(a.id) for each id passing the EXACT guard predicate the :2096 add seam
    uses — `!strategyById.has(id) && addedReturnsById[id] === undefined` — relying on
    fetchAddedReturns' own lazyAbortRef idempotence (:1282-1285) as the in-flight guard; do
    NOT build a second in-flight mechanism, a ref-flag, or a mount-once latch beyond the
    guard predicate (Rule 7: the fetch function already dedupes). Dependencies: the
    addedStrategies list, strategyById, addedReturnsById, fetchAddedReturns. This single
    effect covers BOTH un-fixed entry paths (openSavedScenario at :1484 and the
    localStorage-draft hydration) because both mutate draft.addedStrategies on a mount where
    addedReturnsById starts empty. Comment names Phase 147 / RESEARCH P6 and the two add-seam
    call sites as the prior art. Tests FIRST, RED observed, then implement. This is a
    DISTINCT root cause (no fetch) from the phase's column fix — say so in the commit message.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fetchAddedReturns(" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` == 4 (two existing seams + the callback definition + exactly one new effect call site)
    - All four behavior tests green; RED observed first
    - No new state, ref, or dedup mechanism introduced — the diff adds one effect and nothing else to the lifecycle (source inspection)
    - Existing add-seam and BridgeDrawer tests unmodified and green
  </acceptance_criteria>
  <done>The acceptance walkthrough survives a page refresh: reopened scenarios fetch and render real series.</done>
</task>

<task type="auto">
  <name>Task 2: SC2 structural grep-gate with recorded non-vacuity</name>
  <files>src/__tests__/phase-147-series-resolution-guards.test.ts</files>
  <read_first>
    - src/__tests__/phase-84-asset-class-flow.test.ts (full, 49 lines — the PRIMARY analog: ROOT/read helper, one it() per surface, block-slicing for queries.ts)
    - src/__tests__/phase-63-series-space-guards.test.ts (lines 30-109 — fail-loud readSource on a missing file, explicit allowlist with per-file rationale, the Rule-9 non-vacuity docstring paragraph to reproduce)
    - src/__tests__/no-store-coverage.test.ts (lines 29-33 — why a forces-classify-everything corpus walk is scoped carefully)
    - 147-RESEARCH.md §"The SC2 grep-gate: how to build it" (all six design requirements)
  </read_first>
  <action>
    Create the gate with TWO layers, honoring the LOCKED CONTEXT decision (repo-wide scan)
    while following the in-repo allowlist discipline:
    LAYER A (repo-wide, the locked decision): walk src/**/*.ts and *.tsx EXCLUDING *.test.*,
    __tests__, and .d.ts; for each file, find every select/embed string mentioning
    strategy_analytics-sourced "daily_returns" (regex over .select("...") literals and
    `strategy_analytics (` embed blocks); FAIL naming file+match if the same select string
    lacks "returns_series" — tolerating `strategy_analytics (*)` splats (both columns present
    by construction: queries.ts:218, :516, compare/page.tsx:68) and the PUBLIC_ANALYTICS_COLUMNS
    constant (does not include daily_returns; if the scan trips on its dead `daily_returns?:
    unknown` type annotation at queries.ts:414, that is a type not a select — the regex must
    target select strings, not type annotations).
    LAYER B (per-surface allowlist pins, one it() per file so a failure names the surface):
    for each of returns/route.ts, og/factsheet/[id]/route.tsx, queries.ts (block-sliced to
    the getMyAllocationDashboard embed via the marker-literal technique), share-resolve.ts,
    scenario-share/[token]/page.tsx (the sibling read literal "strategy_id, returns_series"),
    factsheet/[id]/v2/page.tsx and discovery/[slug]/[strategyId]/page.tsx (the two reference
    implementations, pinned so they can never regress): assert the file both fetches/receives
    returns_series AND invokes resolveDailyReturnSeries( — assert the CALL, not an import
    specifier (the leaf extraction means specifiers legitimately differ). A missing allowlist
    file is a FAILURE, not a skip (Rule 12) — copy phase-63's readSource error message shape.
    Grep-gate hygiene: strip comment lines before counting so header prose cannot
    self-invalidate the gate.
    NON-VACUITY (Rule 9, recorded): temporarily plant a bare daily_returns select in
    src/lib/queries.ts — the SECOND member of the reader class per the ledger's
    instance-fix note — observe BOTH layers go red, revert, and record the experiment in the
    test-file docstring AND the commit message (phase-63:47-52 shape). Paste the red output
    into 147-VALIDATION.md row SC-2.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-147-series-resolution-guards.test.ts src/__tests__/phase-84-asset-class-flow.test.ts src/__tests__/phase-63-series-space-guards.test.ts src/__tests__/phase-29-frozen-spine-guards.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Gate green on the fixed tree; the planted bare select reddened BOTH layers (evidence pasted in 147-VALIDATION.md SC-2 row and named in the commit message)
    - One it() per allowlisted surface; failure output names the offending file
    - The two reference pages are pinned (deleting the resolver call from factsheet v2 page would redden the gate — assert structure supports this)
    - Missing-file arm throws with a Rule-12 message (verified by the test's own source, phase-63 style)
    - All three pre-existing structural gates still green
  </acceptance_criteria>
  <done>SC2 is structural: no bare reader, no third mechanism, can merge without CI failing loudly.</done>
</task>

<task type="auto">
  <name>Task 3: Consumer audit + validation ledger closure + full-suite gate</name>
  <files>TODOS.md, .planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-VALIDATION.md</files>
  <read_first>
    - .planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-VALIDATION.md (the per-task map and falsifiability ledger rows to close)
    - src/lib/queries.ts (lines 1290-1310 — getPortfolioStrategies, which already selects both columns; its CONSUMERS are the audit target)
    - TODOS.md (append format — single ground-truth backlog; add items ONLY here)
  </read_first>
  <action>
    (1) AUDIT (orchestrator ruling: grep, log, do NOT fix): grep every consumer of
    getPortfolioStrategies for bare `daily_returns` reads that would still see null for real
    strategies (the query fetches both columns but a consumer reading only daily_returns still
    strands the series). Also record the dead `daily_returns?: unknown` annotation at
    queries.ts:414 (PUBLIC_ANALYTICS_COLUMNS does not select it) if the gate flagged it. Log
    each finding to TODOS.md as "DEF-147-A/B/…" with file:line and the one-line fix shape —
    NO code changes in this task.
    (2) LEDGER: verify every Falsifiability Ledger row in 147-VALIDATION.md is Observed ✅
    with pasted evidence (SC-1 from 147-04, SC-1(route)+SC-3 from 147-02, SC-3(share) from
    147-03, SC-4 from 147-05, SC-4(book) from 147-04, SC-2 from Task 2 of this plan). Any
    row still pending: STOP and run that mutation now against the finished tree, then record.
    Tick the Oracle Independence checklist (no test imports a constant from the module under
    test; SC-3 economics are hand-computed literals). Update frontmatter:
    nyquist_compliant: true, wave_0_complete: true, status: executed. Fill the Per-Task
    Verification Map Status column from the actual runs.
    (3) PHASE GATE: run the full suite + lint + both legacy structural gates. Local flakes →
    --no-file-parallelism; a CI-only failure signature means Node-version skew (CI=22,
    local may be 25 — reproduce with PATH=/opt/homebrew/opt/node@22/bin before calling it a flake).
  </action>
  <verify>
    <automated>npm run test -- --no-file-parallelism && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - TODOS.md carries the audit findings (or an explicit "audit clean: 0 bare consumers" entry with the grep command used)
    - 147-VALIDATION.md: every ledger row Observed ✅ with evidence; Oracle Independence checklist complete; frontmatter nyquist_compliant: true
    - Full vitest suite green, lint 0 errors, tsc clean
    - Zero production-code diffs in this task (`git diff --name-only` shows only TODOS.md + 147-VALIDATION.md)
  </acceptance_criteria>
  <done>Phase evidence is complete and self-consistent; the backlog carries the class-closure findings; the tree is green end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client hydration → authed returns route | The new effect fires requests to the existing authed route only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-16 | DoS (self-inflicted fetch storm) | hydration effect | mitigate | Reuses the add-seam guard predicate + fetchAddedReturns' lazyAbortRef idempotence; the idempotence behavior test proves no duplicate fetch across re-renders |
| T-147-17 | Information Disclosure | hydration fetch scope | accept | Identical route, identical auth, identical 404 oracle as the add seam — no new surface, only a new trigger time |
| T-147-18 | Tampering | future bare-reader regression | mitigate | The grep-gate is the control: repo-wide layer + allowlist pins, non-vacuity observed and recorded |
| T-147-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase |
</threat_model>

<verification>
- `npm run test -- --no-file-parallelism` full suite green; `npm run lint` 0 errors; `npx tsc --noEmit` clean
- phase-29, phase-63, phase-84, and phase-147 structural gates all green
- 147-VALIDATION.md fully closed (every SC row Observed ✅, nyquist_compliant: true)
</verification>

<success_criteria>
- Refresh/reopen renders real series (the founder can close the browser and the anchor still holds)
- SC2 enforced structurally with recorded non-vacuity on the second class member
- Audit logged to TODOS.md; zero silent scope expansion
</success_criteria>

<output>
Create `.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-06-SUMMARY.md` when done
</output>
