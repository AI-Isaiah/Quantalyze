---
phase: 147-scen-01-the-scenario-engine-receives-the-real-series
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/factsheet/resolve-series.ts
  - src/lib/factsheet/allocator-portfolio-payload.ts
  - src/lib/closed-sets.ts
  - src/lib/closed-sets.series-state.test.ts
autonomous: true
requirements: [SCEN-01]

must_haves:
  truths:
    - "resolveDailyReturnSeries and equityCurveToDailyReturns are importable from a leaf module with zero build-payload transitive imports"
    - "Existing consumers (factsheet v2 page, discovery page, allocator-portfolio-payload.test.ts) compile and pass with zero diff to their import specifiers"
    - "deriveEmptySeriesState maps missing-analytics-row to computing ONLY while the strategy is younger than 16h; older or unparseable age maps to empty"
  artifacts:
    - path: "src/lib/factsheet/resolve-series.ts"
      provides: "Leaf module: equityCurveToDailyReturns + resolveDailyReturnSeries (verbatim move)"
      exports: ["equityCurveToDailyReturns", "resolveDailyReturnSeries"]
    - path: "src/lib/closed-sets.ts"
      provides: "SERIES_STATES closed set, SeriesState type, deriveEmptySeriesState predicate"
      contains: "deriveEmptySeriesState"
    - path: "src/lib/closed-sets.series-state.test.ts"
      provides: "Unit tests for the 16h age-bound and status mapping"
  key_links:
    - from: "src/lib/factsheet/allocator-portfolio-payload.ts"
      to: "src/lib/factsheet/resolve-series.ts"
      via: "re-export (back-compat for 2 pages + 1 test file)"
      pattern: "export \\{.*resolveDailyReturnSeries.*\\} from \"\\./resolve-series\""
    - from: "src/lib/factsheet/resolve-series.ts"
      to: "src/lib/portfolio-math-utils.ts"
      via: "normalizeDailyReturns import (WR-05 canonical parser)"
      pattern: "normalizeDailyReturns"
---

<objective>
Create the foundation both downstream waves build on: (1) extract the ONE series-resolution
mechanism (`resolveDailyReturnSeries` + `equityCurveToDailyReturns`) into a leaf module so the
OG route and public share page can import it without dragging `build-payload`'s 18 transitive
imports; (2) create the `SeriesState` closed set and the single server-side
`deriveEmptySeriesState` discriminator with the 16h missing-row age bound that kills the
permanent-spinner class (RESEARCH P5, orchestrator ruling).

Purpose: SC2 demands ONE mechanism (Rule 7); every Wave-2 read site calls these two exports.
Output: `src/lib/factsheet/resolve-series.ts` (new), widened `closed-sets.ts`, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-RESEARCH.md
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-PATTERNS.md
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-CONTEXT.md
</context>

<interfaces>
<!-- Contracts downstream plans (147-02/03/04/05) import. Executor: produce EXACTLY these signatures. -->

From src/lib/factsheet/resolve-series.ts (NEW — moved verbatim from allocator-portfolio-payload.ts:15-58):
```typescript
export function equityCurveToDailyReturns(points: DailyPoint[]): DailyReturn[];
export function resolveDailyReturnSeries(dailyReturnsRaw: unknown, returnsSeriesRaw: unknown): DailyReturn[];
```

From src/lib/closed-sets.ts (NEW additions):
```typescript
export const SERIES_STATES = ["available", "computing", "empty"] as const;
export type SeriesState = (typeof SERIES_STATES)[number];
export const MISSING_ROW_COMPUTING_WINDOW_MS: number; // 16 * 60 * 60 * 1000
export function deriveEmptySeriesState(
  status: string | null,
  strategyCreatedAt: string | null,
  nowMs?: number, // default Date.now(); injected for deterministic tests
): SeriesState; // returns only "computing" | "empty" (callers use it only when the resolved series is empty)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract resolve-series.ts leaf with back-compat re-exports</name>
  <files>src/lib/factsheet/resolve-series.ts, src/lib/factsheet/allocator-portfolio-payload.ts</files>
  <read_first>
    - src/lib/factsheet/allocator-portfolio-payload.ts (lines 1-60 — the two functions being moved and their imports)
    - .planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-PATTERNS.md §"src/lib/factsheet/resolve-series.ts" (the verbatim code excerpt and the import list the leaf needs)
    - src/lib/factsheet/allocator-portfolio-payload.test.ts (lines 140-215 — the tests that must stay green with zero diff)
  </read_first>
  <action>
    Per the locked CONTEXT decision "no new fetch abstraction" and RESEARCH's recommended
    module layout (Claude's-discretion item, adopted): MOVE `equityCurveToDailyReturns`
    (allocator-portfolio-payload.ts:15-37) and `resolveDailyReturnSeries` (:51-58) verbatim —
    do not retype, do not reformat bodies, keep both docstrings including the docstring that
    names this exact bug — into new file `src/lib/factsheet/resolve-series.ts`. The leaf
    imports ONLY: `type DailyPoint` + `normalizeDailyReturns` from
    `@/lib/portfolio-math-utils`, and `type DailyReturn` from `./types`. It must NOT import
    `./build-payload` or `FactsheetPayload` (that is the entire point of the extraction —
    keeping the 18-module build-payload graph out of the OG route and public share page).
    In `allocator-portfolio-payload.ts`: delete the two function bodies, add
    `export { equityCurveToDailyReturns, resolveDailyReturnSeries } from "./resolve-series";`
    so `factsheet/[id]/v2/page.tsx`, `discovery/[slug]/[strategyId]/page.tsx`, and
    `allocator-portfolio-payload.test.ts` keep their existing import specifier at zero diff.
    Add a one-line comment on the re-export naming Phase 147 / SC2 (one mechanism) as the reason.
  </action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/allocator-portfolio-payload.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/factsheet/resolve-series.ts` exists; `grep -c "resolveDailyReturnSeries" src/lib/factsheet/resolve-series.ts` >= 1 and `grep -c "build-payload\|FactsheetPayload" src/lib/factsheet/resolve-series.ts` == 0
    - `grep -n "from \"./resolve-series\"" src/lib/factsheet/allocator-portfolio-payload.ts` returns exactly one re-export line covering both symbols
    - `src/app/factsheet/[id]/v2/page.tsx` and `src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx` have ZERO diff (`git diff --name-only` does not list them)
    - `npx vitest run src/lib/factsheet/allocator-portfolio-payload.test.ts` exits 0 with no test deleted or modified
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Leaf module exists with a clean import graph; all existing resolver consumers compile and pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SeriesState closed set + deriveEmptySeriesState with the 16h age bound</name>
  <files>src/lib/closed-sets.ts, src/lib/closed-sets.series-state.test.ts</files>
  <read_first>
    - src/lib/closed-sets.ts (lines 400-483 — the STRATEGY_ANALYTICS_COMPUTATION_STATUSES + isComputedAnalytics idiom this MUST mirror: const-as-const tuple, derived type, one exported predicate with a why-comment)
    - .planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-RESEARCH.md §Common Pitfalls P5 (the evidence chain for why missing-row must be age-bounded) and §Code Examples deriveEmptySeriesState
    - src/lib/closed-sets.test.ts (lines 1-40 — house test style for this module)
  </read_first>
  <behavior>
    - Test 1: status "pending" → "computing"; status "computing" → "computing" (regardless of created_at)
    - Test 2: status null + strategyCreatedAt 1h before nowMs → "computing" (young strategy, legitimate warm-up)
    - Test 3: status null + strategyCreatedAt exactly 57,600,000ms (literal in test, NOT imported) + 1ms before nowMs → "empty" (past the 16h bound — permanent-spinner guard)
    - Test 4: status null + strategyCreatedAt null → "empty"; status null + strategyCreatedAt "not-a-date" → "empty" (unknown age → honest absence)
    - Test 5: status "complete" → "empty"; "complete_with_warnings" → "empty"; "failed" → "empty" (terminal absence; failed is NOT special-cased — UI-SPEC: failed renders muted "No data", never red)
    - Test 6: unknown junk status ("garbage") → "empty" (conservative default)
  </behavior>
  <action>
    In `src/lib/closed-sets.ts`, near STRATEGY_ANALYTICS_COMPUTATION_STATUSES (:423-446), add:
    `SERIES_STATES = ["available", "computing", "empty"] as const`, derived type `SeriesState`,
    exported const `MISSING_ROW_COMPUTING_WINDOW_MS = 16 * 60 * 60 * 1000` with a comment
    naming its source — `STRATEGY_ANALYTICS_REAP_THRESHOLD` = "16 hours" (`analytics-service/job_worker.py:547`),
    mirrored by the pg_cron reaper migration `20260802120000` — so there is ONE threshold in
    the system, not two (RESEARCH Don't-Hand-Roll row 6). Then `deriveEmptySeriesState(status,
    strategyCreatedAt, nowMs = Date.now())` per the interfaces block: "pending"/"computing" →
    "computing"; null status → parse created_at with Date.parse, non-finite → "empty", else
    "computing" iff nowMs − parsed < MISSING_ROW_COMPUTING_WINDOW_MS; every other status →
    "empty". Docstring must state: (a) callers invoke this ONLY when the resolved series is
    empty ("available" is decided by series length at the call site), (b) the P5 evidence —
    strategy creation does NOT guarantee an analytics row (no trigger; finalize enqueue is
    non-blocking; no cron backstops a MISSING row), so unbounded missing-row → "computing" is
    the permanent-spinner class Phase 142 killed. Write the six behavior tests FIRST in new
    file `src/lib/closed-sets.series-state.test.ts` (sibling-file convention:
    closed-sets.mt5-flag.test.ts), observe RED, then implement. Oracle independence: the 16h
    boundary is the literal 57600000 typed into the test, never imported from closed-sets.
  </action>
  <verify>
    <automated>npx vitest run src/lib/closed-sets.series-state.test.ts src/lib/closed-sets.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All six behavior tests exist and pass; RED was observed before implementation (record in commit message)
    - `grep -c "57600000\|16 \* 60 \* 60 \* 1000" src/lib/closed-sets.series-state.test.ts` >= 1 (literal boundary in test) and `grep -c "MISSING_ROW_COMPUTING_WINDOW_MS" src/lib/closed-sets.series-state.test.ts` == 0 (constant NOT imported by the test — oracle independence)
    - `deriveEmptySeriesState` never returns "available" (source inspection: the function's return statements only produce "computing" or "empty")
    - Existing `closed-sets.test.ts` unchanged and green
  </acceptance_criteria>
  <done>SeriesState vocabulary and the single age-bounded discriminator exist, unit-proven at the 16h boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure library code; no I/O, no request handling, no data crossing a trust boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-01 | Tampering | resolve-series.ts move | mitigate | Verbatim move pinned by the untouched allocator-portfolio-payload.test.ts suite — any semantic drift in the differencing (the money-math) reddens existing economic-invariant tests |
| T-147-02 | DoS (permanent spinner as availability lie) | deriveEmptySeriesState | mitigate | 16h age bound unit-tested at the exact boundary; unknown age degrades to "empty", never to an unbounded "computing" |
| T-147-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH Package Legitimacy Audit: not applicable); any `npm install` appearing in execution is a stop-and-escalate signal |
</threat_model>

<verification>
- `npx vitest run src/lib/factsheet/allocator-portfolio-payload.test.ts src/lib/closed-sets.series-state.test.ts src/lib/closed-sets.test.ts --no-file-parallelism` exits 0
- `npx tsc --noEmit` exits 0
- `git diff --name-only` lists ONLY the four files in files_modified
</verification>

<success_criteria>
- Leaf module importable by the OG route / share page without the build-payload graph
- deriveEmptySeriesState behavior matrix proven, including the 57,600,000ms boundary
- Zero diff to the two already-correct detail pages (they are the SC2 reference implementation)
</success_criteria>

<output>
Create `.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-01-SUMMARY.md` when done
</output>
