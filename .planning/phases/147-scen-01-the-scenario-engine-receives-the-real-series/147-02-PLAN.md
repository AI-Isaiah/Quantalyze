---
phase: 147-scen-01-the-scenario-engine-receives-the-real-series
plan: 02
type: execute
wave: 2
depends_on: ["147-01"]
files_modified:
  - src/app/api/strategies/[id]/returns/route.ts
  - src/app/api/strategies/[id]/returns/route.test.ts
  - src/app/api/og/factsheet/[id]/route.tsx
  - src/app/api/og/factsheet/[id]/route.test.tsx
autonomous: true
requirements: [SCEN-01]

must_haves:
  truths:
    - "The returns route emits a non-empty differenced series when only returns_series is populated (the SCEN-01 bug proper is dead)"
    - "A wealth index starting at exactly 1.0 never yields a +100% day one through the route (SC3, wiring-level)"
    - "The route emits series_state: computing for pending/computing rows, empty for terminal-with-no-series, and the missing-row case is age-bounded at 16h"
    - "The OG sparkline/headline is finite when only returns_series is populated"
  artifacts:
    - path: "src/app/api/strategies/[id]/returns/route.ts"
      provides: "Widened select + resolver + series_state on ReturnsResponse"
      contains: "resolveDailyReturnSeries"
    - path: "src/app/api/og/factsheet/[id]/route.tsx"
      provides: "Widened embed + resolver on the OG headline path"
      contains: "resolveDailyReturnSeries"
    - path: "src/app/api/og/factsheet/[id]/route.test.tsx"
      provides: "Wave-0 gap closed: first test file for the OG route"
  key_links:
    - from: "src/app/api/strategies/[id]/returns/route.ts"
      to: "src/lib/factsheet/resolve-series.ts"
      via: "resolveDailyReturnSeries(row?.daily_returns, row?.returns_series)"
      pattern: "resolveDailyReturnSeries\\("
    - from: "src/app/api/strategies/[id]/returns/route.ts"
      to: "src/lib/closed-sets.ts"
      via: "deriveEmptySeriesState on the empty-series arm"
      pattern: "deriveEmptySeriesState\\("
    - from: "src/app/api/og/factsheet/[id]/route.tsx"
      to: "strategy_analytics ( daily_returns, returns_series )"
      via: "widened embed select"
      pattern: "daily_returns, returns_series"
---

<objective>
Fix bare readers #1 (the SCEN-01 bug proper — the composer's lazy-fetch route) and #3 (the OG
factsheet sparkline). Widen both reads to `returns_series`, resolve through the ONE mechanism,
and thread the additive `series_state` discriminator on the returns route so the composer can
render honest computing/empty states (UI-SPEC §3). Response happy-path shape stays
byte-identical — zero downstream change when a real series exists.

Purpose: this route is what the composer lazily fetches for a drawer-added strategy; today it
returns `[]` for EVERY service-computed strategy (0/27 real rows have daily_returns).
Output: both routes fixed, route.test.ts extended (SC1/SC3/P5 matrix), new OG route test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-RESEARCH.md
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-PATTERNS.md
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-UI-SPEC.md
</context>

<interfaces>
From src/lib/factsheet/resolve-series.ts (created by 147-01):
```typescript
export function resolveDailyReturnSeries(dailyReturnsRaw: unknown, returnsSeriesRaw: unknown): DailyReturn[];
```
From src/lib/closed-sets.ts (created by 147-01):
```typescript
export type SeriesState = "available" | "computing" | "empty";
export function deriveEmptySeriesState(status: string | null, strategyCreatedAt: string | null, nowMs?: number): SeriesState;
```
New wire contract this plan creates (consumed by 147-05):
```typescript
// added to ReturnsResponse (returns/route.ts:75-106 interface)
series_state: "available" | "computing" | "empty";
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Returns route — widen select, resolve, emit series_state (SC1 + SC3 + P5)</name>
  <files>src/app/api/strategies/[id]/returns/route.ts, src/app/api/strategies/[id]/returns/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/[id]/returns/route.ts (full file, 285 lines — the select at :219-223, the T-29-02 redaction at :225-236, the ReturnsResponse interface at :75-106, the pinned probe at :191-197)
    - src/app/api/strategies/[id]/returns/route.test.ts (lines 1-120 — the R1-R8 coverage-matrix docstring convention, STATE.analyticsRow fixture at :87-92, observedFilters.analyticsSelect capture at :107; and :203-226 — the strategy_analytics mock arm)
    - src/__tests__/phase-84-asset-class-flow.test.ts (lines 36-47 — the pins that MUST stay green)
    - 147-PATTERNS.md §"src/app/api/strategies/[id]/returns/route.ts" (select-width analog from factsheet v2 page:45, redaction excerpt, response-interface JSDoc style, and the ⛔ pinned-literal warning)
    - src/app/factsheet/[id]/v2/page.tsx (lines 45-71 — the reference implementation; copy its select width + resolver call, NOT its composite arm)
    - node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md (AGENTS.md mandate: read the route-handlers guide before editing this handler — this Next.js version may differ from training data)
  </read_first>
  <behavior>
    - Test SC1: STATE.analyticsRow = { daily_returns: null, returns_series: [{date:"2026-01-01",value:1.0},{date:"2026-01-02",value:1.05},{date:"2026-01-03",value:0.945},{date:"2026-01-04",value:1.0395}] } → 200 body daily_returns has length 3 (N−1, NEVER assert 4) with hand-computed literals: [0].value ≈ 0.05, [1].value ≈ −0.1, [2].value ≈ 0.1 (toBeCloseTo, precision 10) and body.series_state === "available"
    - Test SC3 (wealth index never raw): same fixture — assert body.daily_returns[0].value is NOT ≈ 1.0 (the +100%-day-one raw-forward bug) and length is NOT 4. Economics pinned as literals typed into the test, never re-derived by calling equityCurveToDailyReturns in the test (money-math oracle rule)
    - Test select width: STATE.observedFilters.analyticsSelect contains "returns_series" AND "computation_status" (harness already captures it — no harness change needed for this assertion)
    - Test series_state=computing: analyticsRow { daily_returns: null, returns_series: null, computation_status: "computing" } → body.daily_returns is [] and body.series_state === "computing"
    - Test series_state=empty (terminal): computation_status "complete" → "empty"; "failed" → "empty" (never an error envelope — absence is not an error, UI-SPEC)
    - Test P5 (missing row, old): analyticsRow = null + strategies created_at 17h ago → series_state === "empty" (permanent-spinner guard). Missing row + created_at 1h ago → "computing"
    - Test regression: existing R1-R8 cases byte-unchanged and green; daily_returns present and non-empty short-circuits (direct series wins, resolver contract)
  </behavior>
  <action>
    Widen the analytics select at :219-223 to the exact string
    "daily_returns, returns_series, computation_status, data_quality_flags". Replace the
    :251-252 bare normalizeDailyReturns(raw) with
    `resolveDailyReturnSeries(row?.daily_returns, row?.returns_series)` imported from
    `@/lib/factsheet/resolve-series` (DailyReturn[] is structurally identical to DailyPoint[]
    — assign directly, do NOT write a mapper). Derive series_state: length > 0 → "available";
    else `deriveEmptySeriesState(status, createdAt)` where status is the row's
    computation_status coerced to string|null. For createdAt (needed ONLY when the analytics
    row is missing): per the orchestrator's pinned-literal ruling, do NOT widen the
    `.select("id, asset_class")` probe at :191-197 (pinned byte-for-byte by
    phase-84-asset-class-flow.test.ts:42) — issue a SEPARATE lazy read
    `supabase.from("strategies").select("created_at").eq("id", id).maybeSingle()` executed
    only inside the empty-series + missing-row branch (zero cost on the happy path); if that
    read errors, pass null (deriveEmptySeriesState then answers "empty" — honest absence,
    never a spinner). Add `series_state` to the exported ReturnsResponse interface with a
    JSDoc in the asset_class/is_composite house style (route.ts:75-106) stating: emitted only
    AFTER the withPublishedOrOwner 404 probe passes, so it reveals nothing the existing 404
    existence-oracle did not already gate. PRESERVE untouched: the T-29-02 static error
    envelope + captureToSentry (:225-236), NO_STORE_HEADERS on every response arm, the strict
    `data_quality_flags.composite === true` boolean forwarding (T-111-03), and the
    :191-197 probe byte-for-byte. Extend route.test.ts FIRST (RED observed): widen
    STATE.analyticsRow's type with returns_series + computation_status, add a strategies
    created_at arm to the mock builder for the new lazy read, and append the new cases to the
    R-numbered docstring matrix (do not add tests silently). Then implement. After GREEN,
    run the SC-1 and SC-3 falsifiability mutations from 147-VALIDATION.md (drop
    `returns_series` from the select → SC1 test red; forward the wealth index raw by passing
    it through normalizeDailyReturns instead of the resolver → SC3 test red), observe RED,
    revert, and record evidence in 147-VALIDATION.md rows SC-1(route) and SC-3.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/strategies/[id]/returns/route.test.ts" src/__tests__/phase-84-asset-class-flow.test.ts src/__tests__/no-store-coverage.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Route source contains the exact select string "daily_returns, returns_series, computation_status, data_quality_flags" and a resolveDailyReturnSeries( call; `grep -c "normalizeDailyReturns(raw)" src/app/api/strategies/[id]/returns/route.ts` == 0
    - Line :191-197 probe `.select("id, asset_class")` byte-unchanged (phase-84 test green proves it)
    - All behavior tests green; the two mutation runs observed RED and reverted, evidence pasted into 147-VALIDATION.md
    - Existing R1-R8 tests unmodified; no test deleted
    - phase-84-asset-class-flow.test.ts and no-store-coverage.test.ts both green
    - No hard-coded `toBe(136)` or `toBe(4)` day-count assertion anywhere (P4: differencing yields N−1)
  </acceptance_criteria>
  <done>The composer's lazy-fetch route returns the real differenced series for every service-computed strategy, with an honest age-bounded series_state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OG factsheet route — widen embed, resolve, first-ever test file</name>
  <files>src/app/api/og/factsheet/[id]/route.tsx, src/app/api/og/factsheet/[id]/route.test.tsx</files>
  <read_first>
    - src/app/api/og/factsheet/[id]/route.tsx (full file — the embed select at :31-39, the Array.isArray gate at :59-70, the fail-soft catch at :41-45, the CDN header block at :123-126)
    - 147-PATTERNS.md §"src/app/api/og/factsheet/[id]/route.tsx" and §"No Analog Found" (next/og has ZERO mock precedent — the sanctioned strategy: mock ImageResponse to capture the element tree, or spy on computeOgHeadline; do NOT render a real PNG under vitest)
    - src/app/api/strategies/[id]/sync-progress/route.test.ts (lines 1-80 — the vi.hoisted + vi.mock("@/lib/supabase/server") harness in house style)
    - src/lib/factsheet/og-metrics.test.ts (headline-metric oracle style)
    - node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md (AGENTS.md mandate: read the route-handlers guide before editing this handler — this Next.js version may differ from training data)
  </read_first>
  <behavior>
    - Test 1 (SC1-OG): strategies row with strategy_analytics embed { daily_returns: null, returns_series: <the same 4-point wealth-index fixture as Task 1> } → computeOgHeadline receives 3 rows and the captured sharpe/cagr/maxDd are finite (Number.isFinite), not the blank-OG fallback
    - Test 2: embed with daily_returns populated (array of {date,value}) → headline computed from the direct series (regression; resolver's direct-first contract)
    - Test 3: both columns null → fallback card path, no throw (fail-soft discipline preserved)
    - Test 4: select string assertion — the mocked query captures the embed select and it contains "returns_series"
  </behavior>
  <action>
    Widen the embed at :31-39 to `strategy_analytics ( daily_returns, returns_series )`. Keep
    the embed unwrap (Array.isArray on strategy_analytics — it IS an embed, unlike the returns
    route). Replace the `Array.isArray(dailyRaw)` gate and the hand-rolled `.map` row coercion
    at :59-70 with `resolveDailyReturnSeries(analytics?.daily_returns, analytics?.returns_series)`
    from `@/lib/factsheet/resolve-series` (the LEAF import — this route must not gain the
    build-payload graph); feed the resolved array to computeOgHeadline only when length >= 2,
    else keep the existing fallback. PRESERVE untouched: the outer fail-soft try/catch
    (:41-45 — a broken OG image must never 500), withPublishedOnly on the read, and the CDN
    Cache-Control block at :123-126 byte-for-byte (P10: staleness is CDN-owned; note in the
    SUMMARY that a re-unfurl shows the fix within the 24h CDN TTL). Write
    route.test.tsx FIRST (RED observed): vi.mock("next/og") exporting a stub ImageResponse
    class that records its constructor arg; vi.mock the supabase server client in the
    sync-progress harness style; spy on or import-intercept computeOgHeadline to capture its
    input rows. No new dependency, no PNG rendering.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/og/factsheet/[id]/route.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - OG route source contains "daily_returns, returns_series" in the embed select and a resolveDailyReturnSeries( call; the `Array.isArray(dailyRaw)` gate and the inline row-coercion .map are gone
    - The import is from "@/lib/factsheet/resolve-series" (leaf), NOT from "./build-payload" or via a specifier that pulls build-payload (verify with `npx tsc --noEmit` + grep)
    - Test 1 proves finite headline metrics from a returns_series-only row (the founder's real-strategy OG cards stop rendering blank)
    - CDN header block at :123-126 byte-unchanged (`git diff` on the file shows no hunk touching Cache-Control)
    - All four behavior tests green; RED observed before implementation
  </acceptance_criteria>
  <done>OG cards for real (service-computed) strategies render a real sparkline/headline; the route has its first test file (Wave-0 gap closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → returns route | Authed allocator request; visibility gated by withPublishedOrOwner 404 probe |
| anon internet → OG route | PUBLIC unfurl traffic; withPublishedOnly + CDN cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-03 | Information Disclosure | returns route widened select | mitigate | `analytics_read` RLS is table-level (published OR owner) — no column grants exist [RESEARCH verified], so widening leaks nothing; returns_series is already served publicly for published strategies by factsheet v2. NO_STORE_HEADERS on every arm (pinned by no-store-coverage.test.ts) |
| T-147-04 | Information Disclosure | series_state as existence oracle | mitigate | Emitted only AFTER the 404 probe passes; an anon probe for an unpublished id still 404s before any discriminator is computed — asserted by the existing 404-path tests staying green |
| T-147-05 | Information Disclosure | Postgres error text | mitigate | T-29-02 static envelope + captureToSentry survives the widening byte-for-byte; acceptance grep confirms no error.message forwarding was introduced |
| T-147-06 | Information Disclosure | OG route (public) | mitigate | withPublishedOnly unchanged; returns_series only read for published rows; response is an image, raw series never serialized into it |
| T-147-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase; next/og is stubbed in tests, never added as a new dependency |
</threat_model>

<verification>
- `npx vitest run "src/app/api/strategies/[id]/returns/route.test.ts" "src/app/api/og/factsheet/[id]/route.test.tsx" src/__tests__/phase-84-asset-class-flow.test.ts src/__tests__/no-store-coverage.test.ts --no-file-parallelism` exits 0
- `npx tsc --noEmit` and `npm run lint` exit 0 (no-raw-published-predicate stays satisfied)
- 147-VALIDATION.md rows SC-1(route) and SC-3 carry Observed ✅ with pasted RED evidence
</verification>

<success_criteria>
- A returns_series-only analytics row produces a non-empty differenced series through BOTH routes
- series_state matrix proven at route level including the 16h missing-row bound
- Phase-84 pins, no-store pins, and the R1-R8 matrix all green untouched
</success_criteria>

<output>
Create `.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-02-SUMMARY.md` when done
</output>
