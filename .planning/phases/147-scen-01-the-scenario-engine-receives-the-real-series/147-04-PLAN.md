---
phase: 147-scen-01-the-scenario-engine-receives-the-real-series
plan: 04
type: execute
wave: 2
depends_on: ["147-01"]
files_modified:
  - src/lib/queries.ts
  - src/lib/queries.my-allocation.test.ts
autonomous: true
requirements: [SCEN-01]

must_haves:
  truths:
    - "An allocator whose strategy is in their OWN portfolio gets the real series through the book path — the lazy-fetch rescue is skipped for book strategies, so this site alone decides the founder's acceptance anchor (RESEARCH P2)"
    - "The book payload's strategy_analytics.daily_returns is the RESOLVED differenced array under the same field name — six downstream consumers fixed by one edit, zero client change"
    - "Raw returns_series and computation_status never cross to the client; a derived series_state field does (is_composite shape)"
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "getMyAllocationDashboard widened select + server-side resolution + strip + derived series_state"
      contains: "resolveDailyReturnSeries"
  key_links:
    - from: "src/lib/queries.ts getMyAllocationDashboard select (~:3405)"
      to: "strategy_analytics ( ... returns_series, computation_status ... )"
      via: "widened embed inside the pinned phase-84 slice"
      pattern: "returns_series"
    - from: "src/lib/queries.ts payload projection (~:3536)"
      to: "src/lib/factsheet/resolve-series.ts"
      via: "resolveDailyReturnSeries(analyticsObj.daily_returns, analyticsObj.returns_series)"
      pattern: "resolveDailyReturnSeries\\("
---

<objective>
Fix bare reader #4 — the one CONTEXT.md missed and the one that feeds the book path.
`getMyAllocationDashboard` selects `strategy_analytics ( daily_returns, ... )` with no
`returns_series`; that projection is what `addedStrategyReturnsLookup` consults FIRST, and the
composer deliberately SKIPS the lazy fetch for any strategy already in the allocator's book.
The founder's MT5 strategy is in their own portfolio — without this plan, Plans 02/03 ship and
the founder still sees 0.00 (RESEARCH P2). Resolve server-side in queries.ts, emit the
resolved array under the SAME field name, strip the raw columns, and add a derived
`series_state` in the is_composite shape for 147-05's chip.

Purpose: SC1's acceptance anchor lives here; also fixes six other payload consumers in one edit.
Output: widened + resolving getMyAllocationDashboard, behavioral tests in the existing harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-RESEARCH.md
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-PATTERNS.md
</context>

<interfaces>
From src/lib/factsheet/resolve-series.ts + src/lib/closed-sets.ts (created by 147-01):
```typescript
export function resolveDailyReturnSeries(dailyReturnsRaw: unknown, returnsSeriesRaw: unknown): DailyReturn[];
export type SeriesState = "available" | "computing" | "empty";
export function deriveEmptySeriesState(status: string | null, strategyCreatedAt: string | null, nowMs?: number): SeriesState;
```
New payload contract this plan creates (consumed by 147-05's book-path derivation):
```typescript
// MyAllocationDashboardPayload["strategies"][number]["strategy"] gains a sibling of is_composite:
series_state: "available" | "computing" | "empty";
// strategy_analytics.daily_returns is now ALWAYS the resolved DailyPoint[] (or the object is null);
// returns_series and computation_status are STRIPPED before the payload crosses to the client.
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the select, resolve server-side, strip the raw columns</name>
  <files>src/lib/queries.ts, src/lib/queries.my-allocation.test.ts</files>
  <read_first>
    - src/lib/queries.ts (lines 3390-3440 — the strategy:strategies!inner join + strategy_analytics embed being widened; lines 3520-3570 — the data_quality_flags strip-then-project idiom being mirrored; lines 1650-1690 — the payload Pick<> and the is_composite sibling field)
    - src/lib/queries.my-allocation.test.ts (lines 2425-2545 — the psProvenance fixture builder and the CONSTIT-02 describe block; the new tests extend THIS harness; also lines 1-120 for the mock-surface shape)
    - src/__tests__/phase-84-asset-class-flow.test.ts (lines 23-32 — the queries.ts block-slice pin: adding columns INSIDE the strategy_analytics ( ... ) block is safe; relocating either marker literal is not)
    - 147-PATTERNS.md §"src/lib/queries.ts" (all three excerpt anchors + P3 warning)
  </read_first>
  <behavior>
    - Test SC1-book: psProvenance-style fixture with strategy_analytics { daily_returns: null, returns_series: [{date:"2026-01-01",value:1.0},{date:"2026-01-02",value:1.05},{date:"2026-01-03",value:0.945},{date:"2026-01-04",value:1.0395}], computation_status: "complete", ... } → payload row's strategy.strategy_analytics.daily_returns has length 3 with literal values ≈ [0.05, −0.1, 0.1] (toBeCloseTo precision 10, hand-computed — never produced by calling the resolver in the test)
    - Test strip: same fixture → "returns_series" NOT a key of the emitted strategy_analytics, "computation_status" NOT a key either (same T-111-03 shape as the existing data_quality_flags strip test at :2521-2541); JSON.stringify(row) does not contain "returns_series"
    - Test direct-first: daily_returns populated (non-empty array) → emitted daily_returns equals the direct series, returns_series ignored
    - Test raw-forward guard: the SC1-book fixture's emitted [0].value is NOT ≈ 1.0 and length is NOT 4
  </behavior>
  <action>
    Widen the strategy_analytics embed at ~:3405 by adding `returns_series` and
    `computation_status` INSIDE the existing parenthesized block (safe under the phase-84
    slice pin — it asserts containment between the two marker literals, verify by reading the
    pin first; do NOT move either marker). In the payload projection at ~:3527-3541, mirror
    the existing data_quality_flags idiom exactly: destructure `returns_series: _rs` and
    `computation_status: _cs` out of analyticsObj alongside `data_quality_flags: _dqf`, and
    emit `daily_returns: resolveDailyReturnSeries(analyticsObj.daily_returns,
    analyticsObj.returns_series)` (import from "@/lib/factsheet/resolve-series") over
    ...analyticsRest under the SAME field name — DailyReturn[] is structurally identical to
    the field's existing type; the six downstream consumers (ScenarioComparePanel,
    strategies-row-adapter, AlphaBetaDecomposition, composite-returns, RiskDecomposition,
    CorrelationMatrix) are byte-unchanged. P3 guard: if the widened select's inferred row type
    or the Pick<> at ~:1666 errors under tsc, widen the intermediate typing at the
    analyticsObj cast (Record<string, unknown> is already the idiom) rather than adding
    computation_status to the client-facing Pick — the raw column must NOT appear on the
    payload type. Write the four behavior tests FIRST in a new describe block
    "getMyAllocationDashboard — Phase 147 SCEN-01 series resolution" in
    queries.my-allocation.test.ts (extend the psProvenance builder's strategy_analytics arg
    with optional returns_series/computation_status), observe RED, implement, GREEN. Then run
    the SC-1 falsifiability mutation from 147-VALIDATION.md — THE SECOND CLASS MEMBER, per the
    ledger's instance-fix note: in queries.ts pass `undefined` instead of
    `analyticsObj.returns_series` to the resolver → SC1-book test red; revert; record
    evidence in 147-VALIDATION.md row SC-1.
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.my-allocation.test.ts src/__tests__/phase-84-asset-class-flow.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - queries.ts getMyAllocationDashboard embed contains "returns_series" and "computation_status"; the projection contains resolveDailyReturnSeries( ; both marker literals of the phase-84 slice byte-unchanged (gate green proves it)
    - All four behavior tests green; RED observed first; SC-1 mutation (queries.ts — the second class member) observed RED, reverted, evidence pasted into 147-VALIDATION.md
    - Emitted payload contains neither "returns_series" nor "computation_status" keys (strip tests green)
    - `npx tsc --noEmit` exits 0 with NO change to the client-facing Pick<> that exposes a raw series column
    - Existing my-allocation tests unmodified and green
  </acceptance_criteria>
  <done>The book path carries the real resolved series; the founder's own-portfolio strategy stops collapsing to [] with no lazy-fetch rescue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derived series_state on the book payload (is_composite shape)</name>
  <files>src/lib/queries.ts, src/lib/queries.my-allocation.test.ts</files>
  <read_first>
    - src/lib/queries.ts (lines 1657-1669 — the is_composite: boolean sibling-field declaration on the payload type; lines 3527-3541 — where the derivation slots in, post Task 1)
    - src/lib/closed-sets.ts (the deriveEmptySeriesState contract from 147-01)
    - 147-UI-SPEC.md §3 (the normative series_state derivation table — book rows must use the SAME table as the route, not a second rule; that is why the derivation calls the SAME shared predicate)
  </read_first>
  <behavior>
    - Test state-available: resolved series non-empty → strategy.series_state === "available"
    - Test state-computing: analytics row { daily_returns: null, returns_series: null, computation_status: "computing" } → series_state === "computing"
    - Test state-empty-terminal: computation_status "complete" (and separately "failed") with no series → series_state === "empty"
    - Test state-missing-row: strategy_analytics null + strategy created_at 17h before now → series_state === "empty"; created_at 1h before now → "computing" (the 16h bound reaches the book path too)
  </behavior>
  <action>
    Add `series_state` to the payload strategy type at ~:1665 as a sibling of `is_composite`
    (typed with SeriesState imported from "@/lib/closed-sets") and derive it in the projection:
    resolved length > 0 → "available", else `deriveEmptySeriesState(status, strategyCreatedAt)`
    where status is analyticsObj?.computation_status coerced to string|null (null when the
    analytics row is missing) and strategyCreatedAt comes from the strategy join row — add
    `created_at` INSIDE the strategy:strategies!inner ( ... ) block if not already selected
    (containment-safe under the phase-84 slice pin; verify by reading the pin's marker logic
    first). The derivation comment must say: ONE rule shared with the returns route via
    deriveEmptySeriesState — UI-SPEC §3 forbids a second table (SC2). Write the behavior
    tests FIRST (extend the same new describe block; fixture created_at offsets computed from
    a fixed now via the test's own Date mocking or by constructing ISO strings relative to
    Date.now()), observe RED, implement. Then run the SC-4(book) falsifiability mutation from
    147-VALIDATION.md (in queries.ts, hard-code the terminal-empty arm to "computing") →
    state-empty-terminal test red; revert; record evidence.
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.my-allocation.test.ts src/__tests__/phase-84-asset-class-flow.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Payload type declares series_state as a sibling of is_composite; the raw computation_status still does NOT appear on the payload (strip test from Task 1 still green)
    - All four state tests green; RED observed first; SC-4(book) mutation observed RED, reverted, evidence in 147-VALIDATION.md
    - queries.ts contains exactly ONE call to deriveEmptySeriesState (no inlined second rule): `grep -v "^\s*//" src/lib/queries.ts | grep -c "deriveEmptySeriesState("` == 1
    - phase-84 gate green (both marker literals untouched)
  </acceptance_criteria>
  <done>Book rows carry an honest, age-bounded series_state derived by the same single rule as the route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client payload | getMyAllocationDashboard runs on the admin client, ownership-gated by .eq("portfolio_id", portfolio.id); its payload crosses to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-10 | Information Disclosure | raw returns_series / computation_status to client | mitigate | Both stripped in the projection via the established _dqf destructure idiom; behavior tests assert key-absence AND JSON.stringify absence (T-111-03 shape) |
| T-147-11 | Elevation of Privilege | admin-client read scope | mitigate | Query scope byte-unchanged (.eq portfolio_id ownership gate untouched); only columns within the already-authorized embed widen. No new query added |
| T-147-12 | Information Disclosure | series_state on an owner-only payload | accept | The payload is the owner's own dashboard — series_state reveals nothing about third-party strategies beyond what the owner's book already shows |
| T-147-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/queries.my-allocation.test.ts src/__tests__/phase-84-asset-class-flow.test.ts --no-file-parallelism` exits 0
- `npx tsc --noEmit` and `npm run lint` exit 0
- 147-VALIDATION.md rows SC-1 and SC-4(book) Observed ✅ with pasted RED evidence
</verification>

<success_criteria>
- Book-path strategies contribute their real differenced series (the founder's own-portfolio anchor case)
- series_state derived by the ONE shared rule; raw columns provably never reach the client
- Six downstream payload consumers fixed with zero change to any of them
</success_criteria>

<output>
Create `.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-04-SUMMARY.md` when done
</output>
