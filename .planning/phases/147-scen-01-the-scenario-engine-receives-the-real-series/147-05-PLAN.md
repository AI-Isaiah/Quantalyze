---
phase: 147-scen-01-the-scenario-engine-receives-the-real-series
plan: 05
type: execute
wave: 3
depends_on: ["147-02", "147-04"]
files_modified:
  - src/app/(dashboard)/allocations/components/CoverageStateChip.tsx
  - src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [SCEN-01]

must_haves:
  truths:
    - "An added strategy still computing renders chip SYNCING + the syncing note with role=status — never 0.00 metrics with no signal (SC4)"
    - "A terminal no-series strategy renders muted NO DATA — never red, never opacity-50/line-through, toggle stays ON, inputs stay enabled"
    - "series_state is consumed via additive-field tolerance: a stale deploy or malformed value degrades to no chip, never a throw, never a false Syncing"
    - "Exactly one chip per row; zero contributing constituents never renders the literal 0.00 in a blend KPI cell"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/CoverageStateChip.tsx"
      provides: "CoverageState union extended in place with syncing + no-series (UI-SPEC §1 — no second chip component)"
      contains: "syncing"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "series_state tolerance narrowing, per-id state map with all four seams, chip precedence, data-series-state attr, both notes"
      contains: "data-series-state"
  key_links:
    - from: "ScenarioComposer.tsx lazy-fetch .then handler (~:1332-1360)"
      to: "series_state from the returns route (147-02)"
      via: "literal-match narrowing with conservative 'available' default"
      pattern: "series_state"
    - from: "ScenarioComposer.tsx book merge (~:2055-2075)"
      to: "payload strategy.series_state (147-04)"
      via: "same tolerance narrowing — ONE derivation table, no client re-derivation from array length"
      pattern: "series_state"
    - from: "ScenarioComposer.tsx added-row branch (~:5582)"
      to: "CoverageStateChip"
      via: "chip precedence ternary (UI-SPEC §2 order)"
      pattern: "CoverageStateChip"
---

<objective>
Render the honest empty/degraded state (ROADMAP SC4) on scenario-composer added-strategy rows:
extend the ONE chip vocabulary with `syncing`/`no-series`, thread `series_state` from both the
lazy-fetch route (147-02) and the book payload (147-04) through the composer with
additive-field tolerance, and render the two locked notes — per the approved 147-UI-SPEC,
which is the binding contract for every visual decision in this plan.

Purpose: today an empty series renders "0 overlapping days" / 0.00 everywhere with no signal.
Output: chip union + precedence + notes + data-series-state attr, full SC4 test matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-UI-SPEC.md
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-PATTERNS.md
@DESIGN.md
</context>

<interfaces>
Consumed (created by 147-02 / 147-04):
```typescript
// returns route response (additive): series_state: "available" | "computing" | "empty"
// book payload: MyAllocationDashboardPayload strategies[].strategy.series_state: same union
```
Created here (consumed by tests + 147-06):
```typescript
export type CoverageState = "in-blend" | "manually-excluded" | "auto-excluded" | "syncing" | "no-series";
// <li data-testid="scenario-constituent-added" data-series-state={"available"|"computing"|"empty"}>
// note: <p data-testid="scenario-series-state-note"> (+ role="status" aria-live="polite" on syncing only)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend CoverageStateChip with syncing + no-series</name>
  <files>src/app/(dashboard)/allocations/components/CoverageStateChip.tsx, src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/CoverageStateChip.tsx (full file — the union + CHIP record at :24-47 and the docstring state→label→token table at :3-23 to extend in the same format)
    - src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx (full file — the per-state it() shape with the never-red negative assertion at ~:64-70, and the states ladder test at :51-70 that MUST gain both new members)
    - 147-UI-SPEC.md §1 (the exact label/class table) and the Semantic-color gate (why syncing=amber, no-series=muted, never red)
  </read_first>
  <behavior>
    - Test 1: state "syncing" → label "Syncing" (rendered uppercase by BASE), className contains text-warning AND bg-warning-bg AND border-warning-border, and does NOT match /text-negative|bg-red|text-red/
    - Test 2: state "no-series" → label "No data", className contains text-text-muted AND bg-track, and does NOT match /text-negative|bg-red|text-red/
    - Test 3: the states ladder test enumerates all FIVE union members (a new member missing from the ladder leaves the base-shape pin partially vacuous — extend it)
    - Test 4: existing three states byte-unchanged (labels "In blend"/"Excluded"/"Outside window" still pinned)
  </behavior>
  <action>
    Extend the CoverageState union in place with "syncing" and "no-series" and add both to the
    CHIP record: syncing → { label: "Syncing", cls: "text-warning bg-warning-bg border border-warning-border" }
    (the EXACT amber trio auto-excluded uses — deliberate reuse, UI-SPEC "Deliberate token
    reuse" section; do not mint a new color) and no-series → { label: "No data",
    cls: "text-text-muted bg-track" } (the exact muted pair manually-excluded uses). Do NOT
    rename the file, the type, or the component (UI-SPEC §1 / Rule 3). Extend the docstring's
    state→label→token table with the two new rows in the same format, and update its
    "coverage-window blend" phrasing to name the widened axis (blend membership + series
    availability). The component stays presentation-only — it must NOT import closed-sets or
    learn about computation_status. Tests FIRST, RED observed, then implement.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Union has exactly five members; CHIP record has exactly five entries; no second chip component exists (`ls src/app/(dashboard)/allocations/components/ | grep -ci "serieschip\|seriesstatechip"` == 0)
    - Both new states pass the never-red negative assertion (UI-SPEC acceptance item 5)
    - CoverageStateChip.tsx imports nothing new except types already present; `grep -c "closed-sets\|computation_status" src/app/(dashboard)/allocations/components/CoverageStateChip.tsx` == 0
    - Existing three-state tests unmodified and green; ladder test covers 5 states
  </acceptance_criteria>
  <done>The one chip vocabulary answers the widened question; presentation-only contract intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread series_state through the composer (tolerance, map seams, precedence, notes)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (targeted ranges only — :1281-1371 fetch lifecycle + tolerance analog; :2055-2135 book merge, add seam, purge; :5570-5645 added-row branch, chip slot after TrustTierLabel, row shell)
    - 147-PATTERNS.md §"ScenarioComposer.tsx" (all four seam excerpts — settle writer :1307-1317, purge :2130-2135, book-vs-lazy merge :2055-2075, add seam :2093-2102 — plus the chip precedence analog :5437 and render slot :5622-5624)
    - 147-UI-SPEC.md §2 (row anatomy: what must NOT change — no opacity-50/line-through, toggle ON, inputs enabled, border stays border-border) and §3 (the derivation table + the ⛔ on client-side length===0 derivation)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (lines 1155-1360 — the LAZY_ID fixture block + latestReturnsLookup/latestAssetClassLookup probe pattern; ~:242 book payload builder)
  </read_first>
  <behavior>
    - Test tolerance: lazy-fetch response with series_state "computing" → row data-series-state="computing"; response with series_state absent (stale deploy) or "garbage" → data-series-state="available", NO chip from the new states, NO throw
    - Test book path: payload strategy with series_state "computing" and empty daily_returns → same syncing rendering as the lazy path (ONE table — the client never re-derives from array length)
    - Test syncing note: chip text SYNCING, note text exactly "First metrics arrive in ~10–15 min — not in the blend yet", note element has role="status" (UI-SPEC acceptance 1)
    - Test empty note: series_state "empty" → chip NO DATA, note exactly "No return series available — not in the blend", note has NO role="status" (idle live region = announcement spam)
    - Test precedence: !enabled beats series_state (manually-excluded chip, no note duplication); series_state "computing" beats coverageEligible in-blend
    - Test purge: removing the added strategy then re-adding starts clean (seriesState map purged alongside addedReturnsById)
  </behavior>
  <action>
    (1) Tolerance narrowing at the lazy-fetch .then (~:1332-1360): widen the response type with
    series_state?: unknown and narrow with the exact BLEND-01 idiom — accept only the literals
    "computing" | "empty", anything else collapses to "available" (never a throw, never a
    false Syncing; comment names the stale-deploy rationale like the asset_class block).
    (2) New per-id map `addedSeriesStateById: Record<string, SeriesState>` with ALL FOUR seams
    mirroring the addedAssetClassById precedent: settle writer (:1307-1317 — settle gains a
    seriesState arg), purge on remove (:2130-2135 idiom), and a book-vs-lazy merged lookup
    beside addedStrategyReturnsLookup (:2055-2075): for each added strategy, book row present →
    narrow strategyById.get(a.id)?.strategy.series_state with the SAME literal-match tolerance;
    else addedSeriesStateById[a.id] ?? "available". ⛔ Do NOT derive any state from
    daily_returns.length — UI-SPEC §3 forbids it; the server owns the discriminator.
    (3) Chip precedence in the added-row branch (~:5582), exact UI-SPEC §2 order: !enabled →
    "manually-excluded"; state==="computing" → "syncing"; state==="empty" → "no-series";
    coverageEligible[a.id] → "in-blend"; else null (auto-excluded group renders its own —
    unchanged). Render slot unchanged (after TrustTierLabel, className="shrink-0"); exactly
    one chip per row.
    (4) Add data-series-state={mapped "available"|"computing"|"empty"} to the
    scenario-constituent-added <li>.
    (5) Notes, verbatim UI-SPEC copy: syncing → <p role="status" aria-live="polite"
    data-testid="scenario-series-state-note" className="text-fixed-11 font-medium text-warning">
    "First metrics arrive in ~10–15 min — not in the blend yet"; no-series → <p
    data-testid="scenario-series-state-note" className="text-fixed-11 font-medium text-text-muted">
    "No return series available — not in the blend". Rendered below the identity cluster,
    inside the row.
    (6) Touch NOTHING else: row shell classes, the !enabled opacity-50/line-through arm, the
    weight/leverage inputs, the UNIFY-04 loading banner, and src/lib/scenario.ts (frozen
    engine) all stay byte-unchanged. Tests FIRST (extend the LAZY_ID fixture's route mock with
    series_state; add a latestSeriesStateLookup probe if needed), RED observed, implement.
    Then run the SC-4 falsifiability mutation from 147-VALIDATION.md (in the composer's
    tolerance narrowing, map "empty" to "computing") → empty-note test red; revert; record
    evidence in 147-VALIDATION.md row SC-4.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All six behavior tests green; RED observed first; SC-4 mutation observed RED, reverted, evidence in 147-VALIDATION.md
    - `grep -c "daily_returns.length === 0" src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` returns no NEW hit deriving a chip state (UI-SPEC §3 ban)
    - The two note strings appear verbatim (grep for "First metrics arrive in ~10–15 min — not in the blend yet" and "No return series available — not in the blend" → exactly 1 hit each in the component)
    - src/lib/scenario.ts has ZERO diff (frozen engine)
    - Existing composer tests unmodified and green
  </acceptance_criteria>
  <done>Composer rows tell the truth about series availability through one server-derived discriminator.</done>
</task>

<task type="auto">
  <name>Task 3: SC4 acceptance matrix — the UI-SPEC's eight falsifiable items</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - 147-UI-SPEC.md §Falsifiable Acceptance (all eight items — this task turns each into a test if Task 2 has not already)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (the state of the file after Task 2 — extend, never duplicate)
  </read_first>
  <action>
    Ensure every UI-SPEC acceptance item has exactly one covering test (Task 2 covers items
    1-2 and 6 partially; add the rest): (3) neither new state's row renders opacity-50 or
    line-through, and the include toggle reports aria-checked="true"; (4) neither weight nor
    leverage input is disabled; (5) computation_status-driven "failed" arriving as
    series_state "empty" renders the MUTED "No data" chip and no
    text-negative/bg-negative/border-negative class anywhere in the row; (6) no composer row
    ever renders two CoverageStateChip instances (query all chips within one row li); (7) with
    zero contributing constituents, no blend KPI cell renders the literal string "0.00" (the
    existing EmptyStateCard path at ~:4427 is the renderer — assert the KPI surface, do not
    build a second empty state); (8) the UNIFY-04 data-testid="scenario-loading-returns"
    banner still renders while a lazy fetch is in flight (regression guard — the new states
    must not fold into or replace it). Use the existing fixture machinery; no production code
    changes in this task. If items 1-6 or 8 FAIL, the fix belongs in Task 2's files and
    must be made there before this task closes. If item 7 FAILS, ESCALATE to the
    orchestrator instead: item 7 asserts against the blend KPI surface (KpiStrip output),
    and KpiStrip.tsx is NOT in this plan's files_modified — fixing it here would be silent
    scope expansion (Rule 12: fail loud, do not fix out-of-scope files).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Eight distinct assertions traceable to the eight UI-SPEC items (test names reference the item, e.g. "UI-SPEC #7")
    - The 0.00 test asserts against the rendered blend KPI cells, not against a helper's return value (wiring, not helper)
    - Full composer + chip test files green; zero skipped tests
  </acceptance_criteria>
  <done>SC4 is provably closed at the component level; the honest-empty contract is pinned against regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server response → client state | series_state literals from route/payload enter client React state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-13 | Tampering | malformed series_state injection into UI state | mitigate | Literal-match tolerance narrowing — only "computing"/"empty" accepted, everything else collapses to "available"; proven by the garbage-value test |
| T-147-14 | Information Disclosure | client re-derivation drift (state lies) | mitigate | No client derives state from array length (UI-SPEC §3 ban, acceptance grep); the server-side single table is the only source |
| T-147-15 | Spoofing (false success) | 0.00 rendered as a real metric | mitigate | UI-SPEC item 7 test — zero contributing constituents never renders literal 0.00; em-dash contract |
| T-147-SC | Tampering | npm/pip/cargo installs | accept | Zero installs; zero new components; zero icons/spinners (DESIGN.md AI-Slop Ban) |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx" --no-file-parallelism` exits 0
- `npx tsc --noEmit` and `npm run lint` exit 0 (no-raw-font-px stays satisfied — only existing text-fixed-11 classes used)
- 147-VALIDATION.md row SC-4 Observed ✅ with pasted RED evidence
</verification>

<success_criteria>
- Both new states render per the approved UI-SPEC with verbatim copy, correct tones, never red
- Tolerance narrowing proven against stale-deploy and garbage inputs
- All eight UI-SPEC falsifiable acceptance items covered by named tests
</success_criteria>

<output>
Create `.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-05-SUMMARY.md` when done
</output>
