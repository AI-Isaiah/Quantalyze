---
phase: 147-scen-01-the-scenario-engine-receives-the-real-series
plan: 03
type: execute
wave: 2
depends_on: ["147-01"]
files_modified:
  - src/app/scenario-share/[token]/share-resolve.ts
  - src/app/scenario-share/[token]/share-resolve.test.ts
  - src/app/scenario-share/[token]/page.tsx
autonomous: true
requirements: [SCEN-01]

must_haves:
  truths:
    - "A share recipient sees the same real series the owner's composer blends — never the zeros the dead daily_returns column produces"
    - "The share fix ships with ZERO new migrations (the frozen-spine CI gate forbids touching the share RPC)"
    - "A wealth index reaching the share projection is differenced, never forwarded raw"
  artifacts:
    - path: "src/app/scenario-share/[token]/share-resolve.ts"
      provides: "resolveSharedScenario accepts optional returnsSeriesById and resolves through the ONE mechanism; stays PURE"
      contains: "resolveDailyReturnSeries"
    - path: "src/app/scenario-share/[token]/page.tsx"
      provides: "Phase-84-shaped caller-side sibling read of strategy_analytics.returns_series bounded to RPC-returned ids"
      contains: "returns_series"
  key_links:
    - from: "src/app/scenario-share/[token]/page.tsx"
      to: "strategy_analytics"
      via: "separate sibling read .select(\"strategy_id, returns_series\").in(\"strategy_id\", seriesIds)"
      pattern: "strategy_id, returns_series"
    - from: "src/app/scenario-share/[token]/page.tsx"
      to: "src/app/scenario-share/[token]/share-resolve.ts"
      via: "resolveSharedScenario(row, assetClassById, returnsSeriesById)"
      pattern: "resolveSharedScenario\\("
---

<objective>
Fix bare reader #2: the public scenario-share projection. The series arrives via the
`get_shared_scenario` SECURITY DEFINER RPC whose `series` jsonb carries only the dead
`daily_returns` column — and the RPC CANNOT be widened (frozen-spine CI gate hard-fails any
migration matching /scenario|share/i). Apply the Phase-84 sibling-read pattern: a separate
caller-side `returns_series` read in page.tsx bounded to the RPC-returned ids, threaded into
the pure resolve layer as an optional param, resolved through the ONE mechanism.

Purpose: share recipients currently see the same silent zeros; CONTEXT locks this site in scope.
Output: widened pure resolver + sibling read, share-resolve.test.ts SC1/SC3 cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-RESEARCH.md
@.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-PATTERNS.md
</context>

<interfaces>
From src/lib/factsheet/resolve-series.ts (created by 147-01):
```typescript
export function resolveDailyReturnSeries(dailyReturnsRaw: unknown, returnsSeriesRaw: unknown): DailyReturn[];
```
Signature this plan widens (share-resolve.ts:152-162 — third optional param mirrors the Phase-84 assetClassById idiom):
```typescript
export function resolveSharedScenario(
  row: SharedScenarioRow,
  assetClassById?: Record<string, string | null>,
  returnsSeriesById?: Record<string, unknown>,  // NEW — absent id / undefined lookup → resolver falls back to s.daily_returns alone, byte-identical to pre-147
): ResolvedSharedScenario;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: share-resolve.ts — optional returnsSeriesById param + resolver (pure layer)</name>
  <files>src/app/scenario-share/[token]/share-resolve.ts, src/app/scenario-share/[token]/share-resolve.test.ts</files>
  <read_first>
    - src/app/scenario-share/[token]/share-resolve.ts (lines 140-220 — the resolveSharedScenario signature at :152-162 with the Phase-84 assetClassById JSDoc to mirror, the seriesById loop at :182-185 being changed, the optional-lookup consumption at :210-212)
    - src/app/scenario-share/[token]/share-resolve.test.ts (lines 1-80 — makeSeries/makeSeriesFrom deterministic fixture-builder style and the WHY-docstring convention)
    - 147-PATTERNS.md §"src/app/scenario-share/[token]/share-resolve.ts" (verbatim excerpts of all three anchors)
    - src/__tests__/phase-63-series-space-guards.test.ts (lines 83-109 — share-resolve.ts is IN this gate's scan set; the file must stay import-clean: no network, no Next import)
  </read_first>
  <behavior>
    - Test SC1-share: row.series entry { strategy_id: "s1", daily_returns: null } + returnsSeriesById { s1: [{date:"2026-01-01",value:1.0},{date:"2026-01-02",value:1.05},{date:"2026-01-03",value:0.945},{date:"2026-01-04",value:1.0395}] } → the projected leg for s1 has 3 daily points with literal values ≈ [0.05, −0.1, 0.1] (toBeCloseTo precision 10; hand-computed, never re-derived via equityCurveToDailyReturns in the test)
    - Test SC3-share (wiring, not helper): same fixture — leg day one is NOT ≈ 1.0 and leg length is NOT 4 (raw-forward and off-by-one both pinned)
    - Test back-compat: returnsSeriesById omitted entirely → output byte-identical to the pre-147 behavior for a daily_returns-populated series (existing tests stay green untouched)
    - Test precedence: BOTH populated (daily_returns non-empty AND returnsSeriesById present) → the direct daily_returns wins (resolver's direct-first contract)
  </behavior>
  <action>
    Add the third optional param `returnsSeriesById?: Record<string, unknown>` to
    resolveSharedScenario with a JSDoc in the exact voice of the Phase-84 assetClassById block
    (:153-161): name the frozen-spine gate as the reason the data arrives caller-side, and
    state the conservative default (absent → resolver falls back to s.daily_returns alone).
    Change the :182-185 loop body to
    `resolveDailyReturnSeries(s.daily_returns, returnsSeriesById?.[s.strategy_id])` imported
    from `@/lib/factsheet/resolve-series` — the LEAF specifier; this file is scanned by
    phase-63-series-space-guards and documented PURE, so the import graph must stay free of
    network/Next modules (the leaf guarantees it; importing allocator-portfolio-payload.ts
    directly would drag build-payload in — do not). Write the four behavior tests FIRST in
    share-resolve.test.ts using a new deterministic wealth-index fixture builder sibling to
    makeSeries (explicit dates, no randomness), observe RED, implement, GREEN. Then run the
    SC-3(share) falsifiability mutation from 147-VALIDATION.md (replace the resolver call
    with bare normalizeDailyReturns(s.daily_returns) — the pre-147 line) → SC1-share and
    SC3-share tests red; revert; record evidence in 147-VALIDATION.md.
  </action>
  <verify>
    <automated>npx vitest run "src/app/scenario-share/[token]/share-resolve.test.ts" src/__tests__/phase-63-series-space-guards.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - share-resolve.ts contains resolveDailyReturnSeries( and imports it from "@/lib/factsheet/resolve-series"; `grep -c "normalizeDailyReturns(s.daily_returns)" src/app/scenario-share/[token]/share-resolve.ts` == 0
    - All four behavior tests green; existing share-resolve tests unmodified and green
    - phase-63-series-space-guards.test.ts green (file still in scan set, still import-clean)
    - Mutation observed RED and reverted; evidence in 147-VALIDATION.md
  </acceptance_criteria>
  <done>The pure share projection resolves the wealth index through the ONE mechanism with a conservative absent-lookup default.</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx — sibling returns_series read bounded to RPC ids</name>
  <files>src/app/scenario-share/[token]/page.tsx</files>
  <read_first>
    - src/app/scenario-share/[token]/page.tsx (lines 120-230 — the Phase-84 asset_class sibling read at :162-198 to mirror verbatim including both error arms, and the resolveSharedScenario call site at :205)
    - src/__tests__/phase-84-asset-class-flow.test.ts (line 47 — the pinned `.select("id, asset_class")` literal; the new read MUST be a separate query)
    - 147-PATTERNS.md §"src/app/scenario-share/[token]/page.tsx" (the two ⚠ constraints: separate query; withPublishedOnly does NOT apply to a strategy_analytics read — state the rationale in the comment)
  </read_first>
  <action>
    Immediately after the existing assetClassById block, add a second sibling read in the same
    shape: build `returnsSeriesById: Record<string, unknown>` from
    `admin.from("strategy_analytics").select("strategy_id, returns_series").in("strategy_id", seriesIds)`,
    wrapped in try/catch with BOTH error arms logged in the established voice
    ("[scenario-share/page] returns_series read failed/threw" — error-absent ≠ legit-absent,
    same as :176-197) and degrading to an empty lookup (never throw on a public page). The
    comment block must state: (a) this is the Phase-84 sibling-read pattern because the
    frozen-spine gate (phase-29-frozen-spine-guards.test.ts:141) forbids widening the
    get_shared_scenario RPC; (b) the read is bounded to `.in("strategy_id", seriesIds)` where
    seriesIds are the RPC's own output — those ids are already status='published'-gated inside
    the SECDEF function (migration 20260622120000:205), which is why withPublishedOnly (a
    strategies-table predicate) does not apply here and why reading an arbitrary id on this
    page would be a disclosure bug. Do NOT touch the pinned `.select("id, asset_class")`
    query. Pass the new map as the third arg: `resolveSharedScenario(row, assetClassById,
    returnsSeriesById)`. Raw returns_series never reaches the client: resolveSharedScenario
    consumes it server-side (RSC) and emits only the resolved DailyPoint arrays.
  </action>
  <verify>
    <automated>npx vitest run "src/app/scenario-share/[token]" src/__tests__/phase-84-asset-class-flow.test.ts src/__tests__/phase-29-frozen-spine-guards.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contains a SEPARATE query with the literal "strategy_id, returns_series" and `.in("strategy_id", seriesIds)`; the pinned `.select("id, asset_class")` literal is byte-unchanged (phase-84 test green proves it)
    - `git status --porcelain supabase/migrations/` is empty — ZERO new migration files (phase-29 gate green proves the class; the git check proves the instance)
    - resolveSharedScenario is called with three args; `npx tsc --noEmit` exits 0
    - Both error arms (PostgREST {data:null,error} AND throw) are logged and degrade to an empty lookup — source inspection against the :176-197 analog
    - Existing page.test.tsx (if its harness exercises this path) still green; if the harness cannot capture the new read, record in the SUMMARY that the behavioral pin lives in share-resolve.test.ts and the structural pin arrives with the 147-06 grep-gate
  </acceptance_criteria>
  <done>Share recipients receive projections built from the real series, with zero DDL and the frozen spine untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon internet → share page | PUBLIC token-gated page; data access via SECDEF RPC + service-role sibling reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-07 | Information Disclosure | sibling read over-returns | mitigate | `.in("strategy_id", seriesIds)` bound to the RPC's own output — ids already published-gated inside the SECDEF function (20260622120000:205); the read can never widen the share token's visibility scope because its id universe IS the RPC's. Rationale stated in-source |
| T-147-08 | Information Disclosure | raw wealth index to client | mitigate | resolveSharedScenario consumes returnsSeriesById server-side (RSC); only resolved DailyPoint arrays are emitted — same wire surface as pre-147 |
| T-147-09 | Tampering | share RPC widening | mitigate | Structurally prevented: phase-29-frozen-spine-guards.test.ts fails the build on any /scenario|share/i migration; acceptance criterion additionally asserts zero new migration files |
| T-147-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase |
</threat_model>

<verification>
- `npx vitest run "src/app/scenario-share/[token]" src/__tests__/phase-84-asset-class-flow.test.ts src/__tests__/phase-29-frozen-spine-guards.test.ts src/__tests__/phase-63-series-space-guards.test.ts --no-file-parallelism` exits 0
- `npx tsc --noEmit` exits 0; zero files under supabase/migrations/ touched
- 147-VALIDATION.md SC-3(share) row Observed ✅ with pasted RED evidence
</verification>

<success_criteria>
- Share projection for a returns_series-only strategy carries the real differenced series
- Frozen spine untouched; all three pre-existing structural gates green
- Absent-lookup default proven byte-identical to pre-147 behavior
</success_criteria>

<output>
Create `.planning/phases/147-scen-01-the-scenario-engine-receives-the-real-series/147-03-SUMMARY.md` when done
</output>
