---
phase: 167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a
plan: 03
type: execute
wave: 2
depends_on: ["167.2-01", "167.2-02"]
files_modified:
  - src/hooks/useStrategySyncPoller.ts
  - src/hooks/useStrategySyncPoller.test.ts
  - src/components/strategy/SyncProgress.tsx
  - src/components/strategy/SyncProgress.poll.test.tsx
  - src/components/strategy/ApiKeyManager.tsx
  - src/components/strategy/ApiKeyManager.poll.test.tsx
  - src/components/strategy/ApiKeyManager.test.tsx
autonomous: true
requirements: [KCS-02, KCS-05, KCS-16]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low
  # Re-sliced on checker W-05 (was 115k with the KCS-18 job-state check): KCS-18 moved to plan 167.2-10 (wave 3), keeping the key-card chain serial and file ownership unchanged.

must_haves:
  truths:
    - "KCS-02: an attempt accepts a terminal status only with evidence that it belongs to THIS attempt: (a) this attempt's panel has itself observed computing, or (b) the polled row's computed_at differs from the computed_at read immediately before this attempt's enqueue. Both are server-written values; no client clock is compared with a server clock."
    - "KCS-02: a terminal read with neither piece of evidence (the previous run's complete, or the previous run's failed with an unchanged computed_at) is not forwarded; the panel stays on Computing analytics... and keeps polling."
    - "KCS-02 (measured fast-fail case, RESEARCH Q1): a job that fails before any handler writes computing, on a strategy whose previous run also failed, ends the attempt as Sync failed because the bridge moves computed_at (evidence b); no third evidence arm is added for it."
    - "KCS-02: the baseline is read after the link update and immediately before the enqueue (RESEARCH P3), bounded by BASELINE_READ_BOUND_MS; an error or expiry yields an unknown baseline (evidence a only) plus a console.error, never a thrown attempt; an absent analytics row is the legit baseline computed_at null."
    - "KCS-02: useStrategySyncPoller passes computed_at as a third onStatus argument from its INTERVAL arm only; the ladder (wizard) arm's select string and call are byte-unchanged."
    - "KCS-05: ApiKeyManager's rule 'only the attempt ends itself' (endAttempt), the enqueued / settling checks, TERMINAL_REREAD_BOUND_MS and R1-R5 are unchanged; the evidence gate lives in SyncProgress so the ~16 ApiKeyManager.test.tsx sites that drive capturedOnStatusChange keep their meaning."
    - "KCS-02 (characterization): SyncProgress PIN 5 — FORWARDED moves so each terminal row carries this attempt's evidence, and PIN 5b NO-EVIDENCE pins that a terminal with no evidence is never forwarded; the interval arm's onStatus receives computed_at as a third argument and the ladder arm's onStatus receives exactly two."
    - "KCS-16: neutering evidence (b) turns the fast-fail test RED; neutering evidence (a) and (b) together turns the stale-terminal test RED (the stale terminal is forwarded) and PIN 5b RED. Each restore is GREEN; all recorded in the SUMMARY."
    - statement: "S2 sync panel: empty, loading, error, populated, partial, overflow and zero-one-many probe categories (UI-SPEC § Probe coverage, auto backstop rows for S2) are answered by the real-poller tests in this plan, plan 10 and plan 04; the render at 320px is a held-out visual check."
      verification: backstop
  artifacts:
    - path: src/components/strategy/SyncProgress.tsx
      provides: "EvidenceBaseline type, evidenceBaseline prop, the per-attempt evidence gate"
    - path: src/hooks/useStrategySyncPoller.ts
      provides: "onStatus third argument computedAt (interval arm only)"
    - path: src/components/strategy/ApiKeyManager.tsx
      provides: "BASELINE_READ_BOUND_MS, readEvidenceBaseline, attemptSeq keying of the panel"
  key_links:
    - from: src/hooks/useStrategySyncPoller.ts
      to: src/components/strategy/SyncProgress.tsx
      via: "onStatus(status, error, computedAt)"
      pattern: "computed_at \\?\\? null"
    - from: src/components/strategy/ApiKeyManager.tsx
      to: src/components/strategy/SyncProgress.tsx
      via: "key={attemptSeq} and evidenceBaseline={…}"
      pattern: "evidenceBaseline="
---

<objective>
Close 167 D-18 residual (3) (KCS-02): a sync attempt shows a result only when the server's own
values prove the result is this attempt's. (The warned-row early terminal research found, KCS-18,
is closed by plan 167.2-10, which runs next and builds on this gate.)

Purpose: today most resyncs of a strategy with a previous terminal row end at the first poll with
the PREVIOUS run's result (a stale "Up to date", or a false "Sync failed"). This is the owner being
told something about the wrong run.
Output: the interval arm of the poller forwards computed_at; SyncProgress gates terminals per
attempt; ApiKeyManager reads a bounded baseline before the enqueue and keys the panel per attempt;
the characterization pins that encoded "any terminal is forwarded" move.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-CONTEXT.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-RESEARCH.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-PATTERNS.md
Read after the dependency plans land (not @-referenced, they do not exist until then): the 167.2-01
and 167.2-02 SUMMARY files in this phase directory.
@src/hooks/useStrategySyncPoller.ts
@src/components/strategy/SyncProgress.tsx
@src/components/strategy/ApiKeyManager.tsx
@src/lib/sync-progress.ts
(src/lib/compute-state.ts is created by plan 01; read it once plan 01 has landed.)
</context>

<hard_constraints>
Carried into every task of this plan:
- KCS-13: The executor's update_requirements step will report KCS-nn ids as not_found — expected
  no-op; never add them to REQUIREMENTS.md and never run requirements.mark-complete by hand.
- Verify logs: before running any `<automated>` command, `export KCS_SP=<your session scratchpad directory>`
  (never /tmp; parallel agents share it, so the per-plan-and-task `kcs-NN-tN.log` names stay
  distinct). The commands expand `${KCS_SP:?KCS_SP_unset}` and fail loudly if it is unset. ⛔ Never
  write the expanded scratchpad path into any tracked file: it carries the local home path and
  username, which `npm run check:planning-hygiene` rejects in a public repo.
- No migration (KCS-15); nothing under supabase/ changes; a task that needs one stops at a
  `checkpoint:decision` (KCS-17).
- Never run supabase / psql against any remote database (CLI linked to PRODUCTION). Unit tests
  only. No uvicorn. No credentials.
- Public repo: synthetic ids and timestamps only.
- Never `git checkout -- <path>`, `git restore`, `git stash`, `npm version`; no VERSION or CHANGELOG
  edit; no CI skip token in any commit message.
- No floor lowered, no WAIVED_CEILING raise, no allowlist widened, no waiver. Cite by SYMBOL.
- ⛔ No client clock is compared with a server clock anywhere in the gate (ROADMAP, KCS-02).
- ⛔ The ladder arm of useStrategySyncPoller (the wizard's) stays byte-unchanged: its select string
  and its onStatus call.
- Every moved pin keeps its lineage comment and names the KCS decision that moved it.
</hard_constraints>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the per-attempt evidence gate, from the poller through the panel to the card</name>
  <files>src/hooks/useStrategySyncPoller.ts, src/components/strategy/SyncProgress.tsx, src/components/strategy/ApiKeyManager.tsx, src/components/strategy/ApiKeyManager.poll.test.tsx, src/components/strategy/ApiKeyManager.test.tsx</files>
  <read_first>
    - src/hooks/useStrategySyncPoller.ts (`UseStrategySyncPollerOptions.onStatus`, the INTERVAL arm's select and `onStatusRef.current(...)` call, the LADDER arm — which must not change)
    - src/components/strategy/SyncProgress.tsx (the `useStrategySyncPoller({...})` call and its `onStatus` forward filter, `SyncProgressProps`, the 167-06 fix round 2 comment on polling in computing only)
    - src/components/strategy/ApiKeyManager.tsx (`handleSyncTrades` after plan 02, `TERMINAL_REREAD_BOUND_MS` and its race in `handleSyncStatusChange` — the named-bound precedent, the `SyncAttempt` docblock, the `<SyncProgress` mount)
    - src/components/strategy/ApiKeyManager.poll.test.tsx (client mock exposing only `.single()` under `.eq()`, `analyticsRow`, the `fetch` mock, `readsBeforeEnqueue` pins, "CONTROL: a terminal read AFTER the enqueue still ends the attempt with its success")
    - src/components/strategy/ApiKeyManager.test.tsx (`vi.mock("@/lib/supabase/client")` and `vi.mock("./SyncProgress")`)
    - 167.2-RESEARCH.md § Q1, Pattern 1, P3, P4
  </read_first>
  <action>
    Hook (KCS-02): widen `onStatus` to `(status, error, computedAt?: string | null) => void` and pass `data.computed_at ?? null` as the third argument in the INTERVAL arm only. The ladder arm is byte-unchanged (its select string, its call).
    SyncProgress (KCS-02, RESEARCH Pattern 1): export `type EvidenceBaseline = { computedAt: string | null } | "unknown"` and add an optional prop `evidenceBaseline?: EvidenceBaseline` defaulting to "unknown". Hold a `sawComputingRef`. In the poller's `onStatus`: a `computing` status sets the ref and is forwarded as today; a terminal (complete, complete_with_warnings, error) is forwarded only when `sawComputingRef.current` is true OR the baseline is known and the third argument differs from `baseline.computedAt`; otherwise it is dropped and polling continues. Pending stays dropped (PIN 6). Document the gate in a comment that names KCS-02 and states it compares two server-written values only.
    ApiKeyManager (KCS-02): add `BASELINE_READ_BOUND_MS = 15_000` beside `TERMINAL_REREAD_BOUND_MS` with a docblock stating the evidence (the same class as the list re-read; RESEARCH Q2) and a `readEvidenceBaseline()` helper that reads `strategy_analytics.select("computed_at").eq("strategy_id", strategyId).maybeSingle()` on the browser client, raced against the bound exactly like the terminal re-read (Promise.race, clearTimeout in finally). A clean answer returns `{ computedAt: data?.computed_at ?? null }` (an absent row is legit null); an error, a throw or the bound expiring returns "unknown" and logs with `console.error` naming the bound and the strategy. In `handleSyncTrades`, call it after `handleLinkKey` resolves and immediately before the enqueue `fetch` (RESEARCH P3), then `if (attemptRef.current !== attempt) return;` before sending. Keep the baseline in component state set before `setSyncStatus("computing")`, and add an `attemptSeq` state incremented at attempt registration; mount the panel as `<SyncProgress key={attemptSeq} evidenceBaseline={…} …>` so `sawComputingRef` resets per attempt (the panel otherwise stays mounted from `error` into a Retry).
    Tests. ApiKeyManager.test.tsx: extend the client mock deliberately so `from("strategy_analytics").select(…).eq(…)` also offers `maybeSingle()` answering `{ data: { computed_at: null }, error: null }`; add one assertion that a Resync reads the baseline on `strategy_analytics`. ApiKeyManager.poll.test.tsx (real poller): add `maybeSingle` under `eq()` that counts baseline reads separately from polls; MOVE the two `expect(readsBeforeEnqueue).toBe(0)` pins to "exactly one baseline read and zero polls before the enqueue" (reason kept: no POLL before the enqueue); MOVE "CONTROL: a terminal read AFTER the enqueue still ends the attempt with its success" so the row carries a computed_at different from the baseline. Add: STALE-TERMINAL (baseline T0, poll returns complete with computed_at T0 for 5 ticks -> panel stays Computing analytics..., no Up to date); FAST-FAIL (baseline failed T0, poll failed T1 with no computing tick -> Sync failed); COMPUTING-THEN-SUCCESS (computing tick then complete with computed_at T0 -> accepted); UNKNOWN-BASELINE (baseline read errors -> only a computing tick admits the terminal). In this task the poll test's fetch mock answers /api/strategies/…/sync-progress with a real read `{ jobStatus: "done", stalled: false, memberProgress: [] }` so plan 167.2-10's job-state check has a mock to find.
    Neuter checks (KCS-16), each RED then restored GREEN, recorded in the SUMMARY: (a) make the gate forward every terminal -> STALE-TERMINAL RED; (b) remove evidence (b) -> FAST-FAIL RED.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/ApiKeyManager.poll.test.tsx src/components/strategy/ApiKeyManager.test.tsx src/hooks/useStrategySyncPoller.test.ts --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-03-t1.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-03-t1.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-03-t1.log && echo OK</automated>
    <fails_when>no OK line is printed: STALE-TERMINAL shows Up to date, FAST-FAIL never reaches Sync failed, a wizard-side hook test (ladder arm) fails, or the log reports zero tests run</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff -U0 "$(git merge-base HEAD main)"..HEAD -- src/hooks/useStrategySyncPoller.ts` touches no line containing `"computation_status, computation_error"` (the ladder select) and adds exactly one `computed_at ?? null` argument.
    - `grep -c "Date.now" src/components/strategy/SyncProgress.tsx` equals the merge-base count, captured status-first: `BASE_SRC=$(git show "$(git merge-base HEAD main):src/components/strategy/SyncProgress.tsx") || exit 1; printf '%s\n' "$BASE_SRC" | grep -c "Date.now"` (the gate adds no clock read; the baseline is fixed at the merge-base, not at a moving HEAD).
    - `grep -c "BASELINE_READ_BOUND_MS" src/components/strategy/ApiKeyManager.tsx` is at least 2 and `grep -c "key={attemptSeq}" src/components/strategy/ApiKeyManager.tsx` is 1.
    - ApiKeyManager.poll.test.tsx contains cases named STALE-TERMINAL, FAST-FAIL, COMPUTING-THEN-SUCCESS and UNKNOWN-BASELINE.
    - The SUMMARY records both neuter runs RED and restore GREEN.
  </acceptance_criteria>
  <done>A resync ends only on a result the server proves is its own; the fast-fail case ends truthfully through evidence (b).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the characterization pins that encoded "any terminal is forwarded" move to the evidence gate (KCS-02)</name>
  <files>src/components/strategy/SyncProgress.poll.test.tsx, src/hooks/useStrategySyncPoller.test.ts</files>
  <read_first>
    - src/components/strategy/SyncProgress.tsx (as left by Task 1: the evidence gate and `evidenceBaseline`)
    - src/components/strategy/SyncProgress.poll.test.tsx ("PIN 5 — FORWARDED" it.each, PIN 6, `analyticsRow`, `renderPoller`)
    - src/hooks/useStrategySyncPoller.test.ts ("INTERVAL-CTRL", "SYM-interval", "LADDER-CTRL")
    - 167.2-RESEARCH.md Pattern 1
  </read_first>
  <behavior>
    - PIN 5 (moved): with `evidenceBaseline={{ computedAt: null }}` and a row carrying a non-null computed_at, each DB terminal (complete, complete_with_warnings, failed) is forwarded as today's mapping.
    - PIN 5b NO-EVIDENCE: default baseline ("unknown"), a complete row with no computing tick -> never forwarded over 5 ticks.
    - PIN 5c COMPUTING-ADMITS: default baseline, one computing tick then complete -> computing forwarded, then complete forwarded once.
    - The interval arm's onStatus receives (status, error, computed_at); the ladder arm's onStatus receives exactly two arguments.
  </behavior>
  <action>
    Tests only (per KCS-02); SyncProgress.tsx and the hook are not edited here. SyncProgress.poll.test.tsx: MOVE "PIN 5 — FORWARDED" so each terminal row carries evidence as in the behaviour list; its reason comment keeps its lineage and gains "KCS-02: a terminal is forwarded only with this attempt's evidence". ADD PIN 5b NO-EVIDENCE and PIN 5c COMPUTING-ADMITS. PIN 1-4 and PIN 6-9 bodies are not touched. useStrategySyncPoller.test.ts: add INTERVAL-COMPUTED-AT (the interval arm's onStatus third argument equals the row's computed_at) and LADDER-TWO-ARGS (the ladder arm's onStatus is called with exactly two arguments). If any other existing case in either file goes red because of Task 1's gate, it MOVES with a KCS-02 note and its lineage kept, and the SUMMARY lists it.
    Neuter check (KCS-16): make the gate forward every terminal -> PIN 5b RED; restore GREEN; record in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/SyncProgress.poll.test.tsx src/components/strategy/SyncProgress.test.ts src/hooks/useStrategySyncPoller.test.ts src/components/strategy/ApiKeyManager.poll.test.tsx src/components/strategy/ApiKeyManager.test.tsx "src/app/(dashboard)/strategies/new/wizard" --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-03-t2.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-03-t2.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-03-t2.log && echo OK && npm run typecheck && npx eslint src/hooks/useStrategySyncPoller.ts src/components/strategy/SyncProgress.tsx src/components/strategy/ApiKeyManager.tsx</automated>
    <fails_when>no OK line is printed (PIN 5b forwards a terminal, PIN 5 misses a mapping, a wizard test fails because the ladder arm changed), tsc reports an error, or eslint reports an error</fails_when>
  </verify>
  <acceptance_criteria>
    - SyncProgress.poll.test.tsx contains cases named `PIN 5b` and `PIN 5c`; the PIN 1-4 and PIN 6-9 test bodies are byte-unchanged (`git diff "$(git merge-base HEAD main)"..HEAD` on that file shows no hunk inside them).
    - useStrategySyncPoller.test.ts contains INTERVAL-COMPUTED-AT and LADDER-TWO-ARGS.
    - The SUMMARY records the neuter run RED and restore GREEN, and lists every moved pin with its reason.
  </acceptance_criteria>
  <done>The panel's own characterization says what the gate now guarantees: no terminal is forwarded without this attempt's evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner browser -> strategy_analytics (RLS analytics_read) | the baseline and poll reads are owner-scoped reads of server-written values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.2-03-01 | Repudiation | a previous run's terminal shown as this attempt's result | high | mitigate | per-attempt evidence gate (sawComputing or computed_at change) keyed by `attemptSeq`; tests STALE-TERMINAL, FAST-FAIL |
| T-167.2-03-02 | Tampering | client clock skew forging evidence | medium | mitigate | the gate compares two server-written computed_at values; no Date.now in the gate (acceptance check) |
| T-167.2-03-04 | Information disclosure | new reads widen what the browser sees | low | accept | the baseline selects `computed_at` only under the existing owner policy; sync-progress is the existing whitelisted projection |
| T-167.2-03-05 | Denial of service | an unbounded baseline read holding the attempt | medium | mitigate | BASELINE_READ_BOUND_MS race; expiry degrades to an unknown baseline and continues |
</threat_model>

<verification>
- All test files in Task 2's verify command green (including the wizard directory, which proves the ladder arm unchanged); `npm run typecheck` 0; eslint over the three source files 0.
- `git diff --stat "$(git merge-base HEAD main)"..HEAD -- supabase/` prints nothing.
- Schema-push gate: not applicable (no schema file in scope).
</verification>

<success_criteria>
- Residual (3) of 167 D-18 is closed with (a)+(b). The warned-row early terminal (KCS-18) is plan 167.2-10's.
- The wizard's ladder arm is byte-unchanged.
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|---|---|---|
| `EvidenceBaseline` | exported type | src/components/strategy/SyncProgress.tsx |
| `evidenceBaseline` | optional prop on `SyncProgressProps` | src/components/strategy/SyncProgress.tsx |
| `BASELINE_READ_BOUND_MS` | const (15 000) | src/components/strategy/ApiKeyManager.tsx |
| `readEvidenceBaseline` | helper inside `ApiKeyManager` | src/components/strategy/ApiKeyManager.tsx |
| `attemptSeq` | state keying the panel per attempt | src/components/strategy/ApiKeyManager.tsx |
| `onStatus` third parameter `computedAt` | hook option widening | src/hooks/useStrategySyncPoller.ts |

<output>
Create `.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-03-SUMMARY.md` when done
</output>
