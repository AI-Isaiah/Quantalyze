---
phase: 167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a
verified: 2026-09-24T08:10:00Z
status: human_needed
score: 9/9 must-haves verified
verified_at_sha: 44719729d9e9ff45feaab0f18674819677cbef95
drift_subjects:
  - src/components/strategy/ApiKeyManager.tsx
  - src/components/strategy/SyncProgress.tsx
  - src/components/strategy/chain-job-state.ts
  - src/components/strategy/key-card-copy.ts
  - src/components/strategy/ApiKeyForm.tsx
  - src/hooks/useStrategySyncPoller.ts
  - src/lib/compute-state.ts
  - src/lib/compute-jobs-read.ts
  - src/lib/strategy-shape.ts
  - src/lib/status-surface-copy.ts
  - src/lib/sync-progress.ts
  - src/app/api/strategies/[id]/sync-progress/route.ts
  - src/app/(dashboard)/strategies/page.tsx
  - src/app/(dashboard)/strategies/[id]/edit/page.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx
  - src/app/factsheet/[id]/v2/page.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet-share/[token]/page.tsx
behavior_unverified: 0
overrides_applied: 0
deferred:
  - truth: "A strategy is called computed on /strategies only when its factsheet can actually build (round-1 review WR-02, computed-but-unbuildable)"
    addressed_in: "Phase 167.2.1"
    evidence: "ROADMAP Phase 167.2.1 FACTSHEETBUILDABLE goal names 167.2 review WR-02, recorded UNFIXABLE-IN-PHASE 2026-09-24"
  - truth: "An RLS-filtered (empty, error-free) membership read cannot silence the Delete confirm's composite warning (167.2-REVIEW-SFH-R2 R2-L4 (b))"
    addressed_in: "Phase 167.2.1"
    evidence: "deferred-items.md and 167.2-REVIEW-FIX-R2.md name Phase 167.2.1 as owner (needs a migration, KCS-15 forbids one here). NOTE: the ROADMAP 167.2.1 section text names WR-02 only; see Gaps Summary."
human_verification:
  - test: "Open the owner factsheet and the share link of a strategy whose first compute failed (after deploy)"
    expected: "Neither says the data is being computed; the owner sees the authored failed line and a shape-true remedy; the recipient sees 'This factsheet isn't available yet' with no internal cause"
    why_human: "Copy judgement against the founder's words (VALIDATION.md Manual-Only row, KCS-09/KCS-11)"
  - test: "View the S2 key-card panel (Sync not started / Sync not confirmed / No result yet / Delete confirm with composite warning) at 320px width and at 200% zoom"
    expected: "No clipping or overflow; amber tones per UI-SPEC; the composite warning names every composite legibly"
    why_human: "Visual layout; no test renders geometry"
  - test: "View the composite key card note (KCS23-COMPOSITE) and the member-only key list on a real composite's edit page"
    expected: "The note is true of the list as rendered (member keys only), no Resync / Use & Sync / Add Key shown"
    why_human: "Visual and copy read-through on real data"
  - test: "View the share-page card in both arms and the /strategies footer band (key pill plus recipient note beside ShareableLink)"
    expected: "One pill per distinct untrusted status, no causal claim about the strategy's numbers; the recipient note matches what the share page actually shows"
    why_human: "Visual appearance and cross-surface copy agreement"
  - test: "Read through every new locked string (key-card-copy.ts, status-surface-copy.ts) against DESIGN.md Voice / microcopy"
    expected: "Declarative, sentence case, active voice, no timing promise the code cannot defend"
    why_human: "Copy quality is a judgement call"
---

# Phase 167.2: KEYCARDSYNC Verification Report

**Phase Goal:** On a manager's key card a sync result is only ever shown about the key it came from, and a withheld success cannot reappear through a re-read. Widened three times: the `/strategies` list marks rows fed by an untrusted key; the owner's pending factsheet states the real compute state; the share link (and the owner's share affordance) tells a running compute from a failed or stalled one and never promises "a few minutes" for a terminal failure.
**Verified:** 2026-09-24
**Status:** human_needed
**Re-verification:** No, initial verification.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A key-card attempt ends only with a terminal that belongs to THIS attempt (KCS-02, KCS-18, residual 3) | VERIFIED | `SyncProgress` `onStatus` gate: forwards a terminal only after a `computing` read (a) or a `computed_at` that differs from `evidenceBaseline` (b), then one `readChainJobState` read that must be `settled`. `useStrategySyncPoller` interval arm passes `computed_at` and drops out-of-order reads (`issuedSeq`/`appliedSeq`). **Own neuter drill:** removing the (a)/(b) guard turned 5 tests RED (PIN 5b NO-EVIDENCE, STALE-TERMINAL, UNKNOWN-BASELINE, KEYED-PER-ATTEMPT, the unbounded-baseline case); restored, `cmp` identical, `git diff` clean. |
| 2 | No attempt starts while a factsheet-chain job is in flight or the job state is unreadable, so one key's job never answers for another key (review CR-01 / WR-06) | VERIFIED | `ApiKeyManager` `handleSyncTrades`: `readChainJobState` before the link and enqueue; non-`settled` ends as `unconfirmed` with `chain_in_flight` / `chain_unreadable[_persistent]`. **Own neuter drill:** disabling the `gate.kind !== "settled"` branch turned 8 tests RED (GATE-E2E, GATE-BOUND, both GATE-PERSISTENT, GATE-TRANSIENT, CR01-OTHER-KEY-REFUSED, WR06-RESYNC-REFUSED, GATE-UNREADABLE); restored, `cmp` identical, `git diff` clean. |
| 3 | The post-add sync runs as the tracked attempt; Add Key is blocked while an attempt is live (KCS-01, residual 1) | VERIFIED | Tests KCS01-BLOCKED, "an add cannot start while an attempt awaits its own enqueue", "R6 superseded by KCS-01", and the moved SEAMUX-05 pins pass. |
| 4 | A withheld success is retired at withholding and a re-read cannot resurrect it, including another tab's fix or delete (KCS-04, residual 2) | VERIFIED | `loadKeys` retires a shown success whose subject it finds untrusted via `retireWithheldSuccess`; tests CROSS-TAB-HEAL and CROSS-TAB-DELETE plus the R3/R5 remedy pins pass. |
| 5 | Link update and enqueue are bounded; expiry says the sync could not be confirmed, never failed; poll give-up is not a failure line (KCS-03, KCS-22, residual 4) | VERIFIED | `LINK_UPDATE_BOUND_MS` 15 s, `ENQUEUE_BOUND_MS` 180 s (inside the 126 s to 300 s evidence window of KCS-03). Tests LINK-HANG, ENQUEUE-HANG, CAP-GIVEUP, NOROW-NOT-ON-ERROR pass. |
| 6 | A composite's key card offers no control that rewrites `strategies.api_key_id`, lists only member keys, and Delete warns by name per the founder decision (KCS-23 as amended) | VERIFIED | `listedKeys` filters to `compositeMemberKeyIds` on a composite; tests COMPOSITE-NO-LINK, COMPOSITE-MEMBERS-ONLY, COMPOSITE-EMPTY, SHAPE-UNKNOWN-REMEDY, R2-WR02-* pass. The 167.2-06 deferred item (KCS23-COMPOSITE line false for non-member keys) is closed by CR-02. |
| 7 | `/strategies` marks each row whose feeding key (linked key or any composite member) is untrusted, with the existing pill and no causal claim, owner-scoped and off every factsheet path (KCS-06) | VERIFIED | `strategies/page.tsx` imports only the request client, `AllocatorSyncStatus`, `readOwnerComputeJobs`; no admin client, no payload builder. 29 tests in `page.key-pill.test.tsx` pass. |
| 8 | The owner pending factsheet states the real compute state with a shape-true remedy; the public lane says one neutral sentence; "Some strategies stay in this state" is gone (KCS-07..10, KCS-19..21) | VERIFIED | `readOwnerPendingStatus` runs only when `lane === "owner"`; `KCS10_PUBLIC_SENTENCE` on the public lane; the removed sentence has 0 hits in non-test `src`. Cache fences `phase-148-owner-lane-cache-isolation`, `page.owner-lane.test.tsx`, `page.no-cache-reach.test.ts`, `page.cache-isolation.test.tsx`: zero diff since the merge-base and green. The discovery fallback's "still computing" claim (07 deferred item) was also replaced, with `page.pending-fallback.test.tsx`. |
| 9 | The share page and the owner share affordance tell in-progress from failed / stalled / never started / unreadable, in neutral copy, never promising "a few minutes"; share creation stays allowed (KCS-11, KCS-12) | VERIFIED | `readShareComputeState` is bounded by the matched strategy id, on the admin client, projecting five fields (no `last_error`, no `error_kind`, no whole `metadata`); the SECURITY BOUNDARY header documents read (3). `recipientArm` sends `unreadable` to `not_available`. "few minutes" has 0 hits in the share/factsheet/list sources. **Own neuter drill:** mapping `failed` to `in_progress` in `recipientArm` turned 13 tests RED across the share page (TRIGGER-SHAPE-B, ARM-B x4), the owner factsheet (TRIGGER-SHAPE, SHARE-NOTE-B), `/strategies` (MINT-B) and the unit derivation; restored, `cmp` identical, `git diff` clean. |

**Score:** 9/9 truths verified (0 present, behavior-unverified)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Round-1 review WR-02: computed-but-unbuildable row shows no share note | Phase 167.2.1 | ROADMAP 167.2.1 goal cites it by name |
| 2 | REVIEW-SFH-R2 R2-L4 (b): an RLS-filtered empty membership read silences the Delete warning | Phase 167.2.1 | `deferred-items.md`, `167.2-REVIEW-FIX-R2.md` (needs a migration; KCS-15) |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/compute-state.ts` | one derivation (KCS-07/19/20) | VERIFIED | used by the route, owner factsheet, share page, `/strategies`, `chain-job-state.ts` |
| `src/components/strategy/chain-job-state.ts` | shared bounded job-state read | VERIFIED | imported by `ApiKeyManager` and `SyncProgress` |
| `src/lib/status-surface-copy.ts`, `src/components/strategy/key-card-copy.ts` | locked copy tables | VERIFIED | pinned by `status-surface-copy.test.ts`, `key-card-copy.test.ts` |
| `src/lib/strategy-shape.ts`, `src/lib/compute-jobs-read.ts` | shape predicate, owner job read | VERIFIED | wired into the edit page, owner factsheet, `/strategies` |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `ApiKeyManager` | `/api/strategies/[id]/sync-progress` | `readChainJobState` pre-attempt gate | WIRED (drill 2) |
| `SyncProgress` | `useStrategySyncPoller` | `onStatus(status, error, computedAt)` | WIRED (drill 1) |
| share page | `compute_jobs` | admin read bounded by matched id, `recipientArm` | WIRED (drill 3) |
| sync-progress route | `compute-state` | `selectFactsheetJob`, `shouldPreferStitch` | WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Typecheck | `npx tsc --noEmit --incremental false` | exit 0 | PASS |
| Phase test set | `vitest run src/components/strategy src/hooks 'src/app/(dashboard)/strategies' src/app/factsheet src/app/factsheet-share src/lib 'src/app/(dashboard)/discovery' src/app/api/strategies` | 355 files passed; 7004 passed, 9 skipped, 0 failed (this set excludes allocations, so it is smaller than the orchestrator's 483 / 8930 reading) | PASS |
| Cross-cutting `src/__tests__` files that name a touched module (10 files, incl. phase-148 cache isolation, phase-164 share-lane headers) | `vitest run <those 10>` | 10 files, 177 passed | PASS |
| KCS-15 no migration | `git diff --stat $(git merge-base HEAD origin/main) -- supabase analytics-service` | empty | PASS |
| Debt markers (TBD / FIXME / XXX) in changed non-test `src` files | grep | 0 hits | PASS |

### Probe Execution

Not applicable: no probe declared by the phase.

### Requirements Coverage

KCS-01..KCS-23 are phase-local decision ids (KCS-13); `REQUIREMENTS.md` carries none of them, so no requirement row is orphaned. Each KCS id maps to a truth above (KCS-13/14/16/17 are discipline items; KCS-16 is exercised by the three drills; KCS-17 holds: no one-way choice shipped).

### Anti-Patterns Found

None blocking.

### Human Verification Required

1. **Real failed composite, owner factsheet and share link.** After deploy, open both for a strategy whose first compute failed. Neither may say the data is being computed. Why human: copy judgement against the founder's words (VALIDATION Manual-Only row).
2. **S2 panel at 320px and 200% zoom**, including the Delete confirm's composite warning. Why human: layout.
3. **Composite key card note and member-only list** on a real composite. Why human: visual and copy read-through.
4. **Share-page card (both arms) and the `/strategies` footer band** (pill plus recipient note). Why human: visual and cross-surface agreement.
5. **Copy read-through** of every new locked string against DESIGN.md Voice. Why human: judgement.

### Gaps Summary

No code gap. The phase goal, in all three widenings, is present, wired and behaviourally proven by tests, and the three central guarantees each went RED under this verifier's own neuter.

One routing-record defect for the orchestrator, not a phase gap. R2-L4 (b) (an RLS-filtered membership read can silence the Delete warning before an `ON DELETE CASCADE` removes a key from a draft composite) is data-integrity class. `deferred-items.md` and `167.2-REVIEW-FIX-R2.md` name Phase 167.2.1 as its owner, but the ROADMAP's 167.2.1 section names only WR-02 (0 ROADMAP hits for `R2-L4` or the membership read). `deferred-items.md` also still says 167.2.1 "is not in the ROADMAP yet", which is now stale. Recommended: `/gsd-phase --edit 167.2.1` to add R2-L4 (b) to its goal or criteria.

---

_Verified: 2026-09-24_
_Verifier: Claude (gsd-verifier)_
