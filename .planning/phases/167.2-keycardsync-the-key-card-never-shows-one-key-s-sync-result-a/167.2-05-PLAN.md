---
phase: 167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a
plan: 05
type: execute
wave: 5
depends_on: ["167.2-04"]
files_modified:
  - src/components/strategy/key-card-copy.ts
  - src/components/strategy/key-card-copy.test.ts
  - src/components/strategy/SyncProgress.tsx
  - src/components/strategy/SyncProgress.poll.test.tsx
  - src/components/strategy/ApiKeyManager.tsx
  - src/components/strategy/ApiKeyManager.poll.test.tsx
autonomous: true
requirements: [KCS-03, KCS-22, KCS-14, KCS-16]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "KCS-03: the enqueue (the /api/keys/sync request and its body read) is raced against ENQUEUE_BOUND_MS = 180_000, which sits above the 126 s slow-but-successful enqueue the existing ApiKeyManager.poll.test.tsx pin honours and below the route's maxDuration = 300 (RESEARCH Q2); on expiry the attempt ends in the UI-only unconfirmed state with label 'Sync not confirmed' and detail KCS03-ENQUEUE, which does not say the sync failed."
    - "KCS-03: the link update (strategies.update inside handleSyncTrades, which after plan 02 also serves the post-add sync) is raced against LINK_UPDATE_BOUND_MS = 15_000; on expiry the attempt ends unconfirmed with label 'Sync not started' and detail KCS03-LINK, and NO /api/keys/sync request is ever sent for that attempt."
    - "KCS-03 (RESEARCH P5): an answer that lands after its bound expired writes nothing: `if (attemptRef.current !== attempt) return` runs before `attempt.enqueued = true` and before any panel write, so a late 202 starts no poll and moves no state."
    - "KCS-03 / KCS-22: the unconfirmed panel uses the DESIGN.md opaque trio (bg-warning-bg, text-warning, WarningIcon), offers NO Retry, and the key's Resync / Use & Sync re-enable because the attempt ended; neither KCS03 string contains 'failed' or 'error'."
    - "KCS-03: the rendered durations (15 seconds, 3 minutes) come from LINK_UPDATE_BOUND_MS and ENQUEUE_BOUND_MS through formatBoundDuration."
    - "KCS-14: key-card-copy.test.ts pins every S1-S2 string character-for-character with literal expectations: KCS01-BLOCKED, KCS01-BLOCKED-NOLABEL, KCS03-LINK (label + detail), KCS03-ENQUEUE (label + detail), KCS22-CAP, KCS22-NOROW, and formatBoundDuration's outputs."
    - "Accessibility (UI-SPEC § Accessibility S2): the SyncProgress status-label <span> carries role=status, aria-live=polite, aria-atomic=true and stays mounted for the whole attempt; the elapsed counter is outside it."
    - "KCS-16: neutering the late-answer guard turns LATE-202 RED (the panel polls after expiry); neutering the enqueue race turns ENQUEUE-HANG RED; each restore GREEN; recorded in the SUMMARY."
    - statement: "long-text (UI-SPEC § UI Considerations, S2 detail in the edit-page sidebar column): the KCS03-* / KCS22-* details are two-sentence prose in a narrow column and will wrap; readability at 320px is a held-out visual check owed at verify time."
      verification: backstop
  artifacts:
    - path: src/components/strategy/key-card-copy.ts
      provides: "LINK_UPDATE_BOUND_MS, ENQUEUE_BOUND_MS, PanelStopReason widened with link_bound / enqueue_bound, PANEL_STOP_COPY rows KCS03-LINK / KCS03-ENQUEUE"
    - path: src/components/strategy/key-card-copy.test.ts
      provides: "character-level pins for every key-card string"
    - path: src/components/strategy/SyncProgress.tsx
      provides: "SyncStatus member unconfirmed; live-region status label"
  key_links:
    - from: "src/components/strategy/ApiKeyManager.tsx handleSyncTrades"
      to: src/components/strategy/key-card-copy.ts
      via: "Promise.race against LINK_UPDATE_BOUND_MS and ENQUEUE_BOUND_MS"
      pattern: "ENQUEUE_BOUND_MS"
---

<objective>
Close 167 D-18 residual (4) (KCS-03): the link update and the enqueue each get a client-side bound,
and when one expires the card says it could not confirm the sync started, which is true, rather
than that it failed, which it cannot know.

Purpose: today an enqueue that never answers spins until the route's 300 s maxDuration, and the
link update ahead of it has no bound at all.
Output: two evidenced bound constants, a UI-only unconfirmed panel state with two locked lines, a
late-answer guard, the S2 live region, and one test file pinning every key-card string.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-CONTEXT.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-RESEARCH.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-UI-SPEC.md
@src/components/strategy/ApiKeyManager.tsx
@src/components/strategy/SyncProgress.tsx
@src/app/api/keys/sync/route.ts
@DESIGN.md
(The plan-04 SUMMARY in this phase directory: read once plan 04 has landed.)
</context>

<hard_constraints>
Carried into every task of this plan:
- KCS-13: The executor's update_requirements step will report KCS-nn ids as not_found — expected
  no-op; never add them to REQUIREMENTS.md and never run requirements.mark-complete by hand.
- Verify logs: before running any `<automated>` command, `export KCS_SP=<your session scratchpad directory>`
  (never /tmp; parallel agents share it, so the per-plan-and-task `kcs-NN-tN.log` names stay
  distinct). The commands expand `${KCS_SP:?KCS_SP_unset}` and fail loudly if it is unset. ⛔ Never
  write the expanded scratchpad path into any tracked file: it carries the local home path and
  username, which `npm run check:planning-hygiene` rejects in a public repo.
- No migration (KCS-15); nothing under supabase/ changes; a task that needs one stops at a
  `checkpoint:decision` (KCS-17).
- Never run supabase / psql against any remote database (CLI linked to PRODUCTION). Unit tests
  only. No uvicorn. No credentials.
- Public repo: synthetic ids only.
- Never `git checkout -- <path>`, `git restore`, `git stash`, `npm version`; no VERSION or CHANGELOG
  edit; no CI skip token in any commit message.
- No floor lowered, no WAIVED_CEILING raise, no allowlist widened, no waiver. Cite by SYMBOL.
- ⛔ The expiry does NOT reuse `syncStatus: "error"`: STATUS_CONFIG.error renders "Sync failed",
  exactly the claim KCS-03 forbids (RESEARCH P5).
- ⛔ The unconfirmed panel does not copy `text-amber-500` / `bg-amber-500/10`; it uses the pinned
  opaque trio `bg-warning-bg` + `text-warning` (UI-SPEC § Color).
- /api/keys/sync is NOT edited (its maxDuration stays 300).
</hard_constraints>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the enqueue bound, end to end — a hung enqueue ends as "Sync not confirmed", and a late 202 writes nothing</name>
  <files>src/components/strategy/key-card-copy.ts, src/components/strategy/SyncProgress.tsx, src/components/strategy/ApiKeyManager.tsx, src/components/strategy/ApiKeyManager.poll.test.tsx</files>
  <read_first>
    - src/components/strategy/ApiKeyManager.tsx (`handleSyncTrades` as left by plans 02-04: registration, `handleLinkKey`, `readEvidenceBaseline`, the enqueue `fetch`, `isSyncEnqueued`, `attempt.enqueued = true; setSyncStatus("computing")`, the catch and `endAttempt`; `TERMINAL_REREAD_BOUND_MS` race as the precedent; `panelStopReason`)
    - src/components/strategy/key-card-copy.ts (`PanelStopReason`, `PANEL_STOP_COPY`, `formatBoundDuration` from plan 04)
    - src/components/strategy/SyncProgress.tsx (`STATUS_CONFIG`, the no_result render from plan 04, `WarningIcon`)
    - src/components/strategy/ApiKeyManager.poll.test.tsx ("the previous run's computing row and a 126 s enqueue (past the 120 s cap)…" — must stay green)
    - src/app/api/keys/sync/route.ts (`maxDuration`)
    - 167.2-UI-SPEC.md § KCS-03 and 167.2-RESEARCH.md § Q2, P5
  </read_first>
  <action>
    key-card-copy.ts (per KCS-03): add `LINK_UPDATE_BOUND_MS = 15_000` and `ENQUEUE_BOUND_MS = 180_000`, each with a docblock stating its evidence window (link: the same single-row PostgREST class as TERMINAL_REREAD_BOUND_MS; enqueue: above the 126 s pinned slow success, 12x the 15 s upstream `process-key-enqueue` budget, 120 s below maxDuration). Widen `PanelStopReason` with `"link_bound" | "enqueue_bound"` and add their `PANEL_STOP_COPY` rows VERBATIM from 167.2-UI-SPEC.md (labels "Sync not started" / "Sync not confirmed"; the details interpolate `formatBoundDuration(LINK_UPDATE_BOUND_MS)` and `formatBoundDuration(ENQUEUE_BOUND_MS)`).
    SyncProgress: add `"unconfirmed"` to `SyncStatus` with a `STATUS_CONFIG` entry `{ icon: WarningIcon, color: "text-warning", bgColor: "bg-warning-bg" }`; render its label from `PANEL_STOP_COPY[stopReason]` and the detail in the same `text-xs text-text-secondary mt-1 ml-6` block as no_result; no Retry.
    ApiKeyManager (KCS-03): wrap the enqueue exchange (the fetch, the `!res.ok` branch, the body read and `isSyncEnqueued`) in one async function and race it against `ENQUEUE_BOUND_MS` exactly like the TERMINAL_REREAD_BOUND_MS race (a timer promise, clearTimeout in finally, a console.error naming the bound and the key id on expiry). Do not abort the request (aborting cannot stop a server-side enqueue and would turn a late 202 into a rejection; the guard below handles it). On expiry: if `endAttempt(attempt)` answers true, set `unconfirmed`, `panelStopReason = "enqueue_bound"`, `syncError = null`. Before `attempt.enqueued = true`, and before every panel write after an await in `handleSyncTrades`, add `if (attemptRef.current !== attempt) return;` so a late answer after expiry (or after the attempt otherwise ended) writes nothing.
    Tests (fake timers, real poller) in ApiKeyManager.poll.test.tsx: ENQUEUE-HANG (the sync request never settles -> at 180 s the panel shows "Sync not confirmed" and the exact KCS03-ENQUEUE detail, no "Sync failed", no Retry, Resync enabled); LATE-202 (the request resolves 202 at 200 s -> no analytics poll follows and the panel still reads "Sync not confirmed"); the existing 126 s case stays green and this task does not edit it. Its body as this task finds it is the post-plan-03 body: plan 03 already MOVED its `readsBeforeEnqueue` pin to "exactly one baseline read and zero polls before the enqueue", and that moved body is the one to leave untouched.
    Neuter checks (KCS-16), each RED then restored GREEN, recorded: (a) remove the race -> ENQUEUE-HANG RED; (b) remove the late-answer guard -> LATE-202 RED.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/ApiKeyManager.poll.test.tsx src/components/strategy/ApiKeyManager.test.tsx src/components/strategy/SyncProgress.poll.test.tsx --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-05-t1.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-05-t1.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-05-t1.log && echo OK</automated>
    <fails_when>no OK line is printed: ENQUEUE-HANG shows Sync failed or keeps spinning, LATE-202 starts polling, the 126 s pin goes red, or the log reports zero tests run</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "ENQUEUE_BOUND_MS" src/components/strategy/ApiKeyManager.tsx` is at least 2 and `grep -c "export const ENQUEUE_BOUND_MS = 180_000" src/components/strategy/key-card-copy.ts` is 1.
    - Before the task's first edit, capture the pre-task file with `git show HEAD:src/components/strategy/ApiKeyManager.tsx > "${KCS_SP:?KCS_SP_unset}/kcs-05-pre.tsx"` (it must exit 0), then `N0=$(grep -c "attemptRef.current !== attempt" "$KCS_SP/kcs-05-pre.tsx")` (HEAD is the post-plan-04 commit this task starts from); after the task, `grep -c "attemptRef.current !== attempt" src/components/strategy/ApiKeyManager.tsx` is at least N0 + 1, and `grep -B3 "attempt.enqueued = true" src/components/strategy/ApiKeyManager.tsx` shows that guard within the three lines above the assignment.
    - `grep -c "AbortController" src/components/strategy/ApiKeyManager.tsx` is 0.
    - ApiKeyManager.poll.test.tsx contains ENQUEUE-HANG and LATE-202; the 126 s case is not edited by this task: run before committing, while HEAD is still the post-plan-04 commit the task started from, `git diff HEAD -- src/components/strategy/ApiKeyManager.poll.test.tsx` shows no hunk inside that case. (A merge-base diff is the wrong baseline here, because it always carries plan 03's deliberate move of the case's `readsBeforeEnqueue` pin.)
    - The SUMMARY records both neuter runs RED and restore GREEN.
  </acceptance_criteria>
  <done>An enqueue that never answers ends in bounded time with a line that tells the truth, and its late answer cannot resurrect the attempt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the link bound, the S2 live region, and one test file pinning every key-card string</name>
  <files>src/components/strategy/ApiKeyManager.tsx, src/components/strategy/SyncProgress.tsx, src/components/strategy/key-card-copy.test.ts, src/components/strategy/SyncProgress.poll.test.tsx, src/components/strategy/ApiKeyManager.poll.test.tsx</files>
  <read_first>
    - src/components/strategy/ApiKeyManager.tsx (`handleLinkKey`, its call at the top of `handleSyncTrades`, the Task 1 race shape)
    - src/components/strategy/SyncProgress.tsx (the status-row `<span>` holding `activeLabel`, the elapsed-time `<span>` sibling)
    - src/components/strategy/key-card-copy.ts (every export after Task 1)
    - src/components/exchanges/AllocatorSyncStatus.test.tsx (character-level assertion style, explicit U+2014 / U+2026 checks)
    - 167.2-UI-SPEC.md § KCS-01, § KCS-03, § KCS-22 and § Accessibility row "S2 live region"
  </read_first>
  <behavior>
    - LINK-HANG: strategies.update never answers -> at 15 s the panel shows "Sync not started" and the exact KCS03-LINK detail; zero /api/keys/sync requests were made for the attempt; no Retry; Resync enabled.
    - A link update that answers after the bound writes nothing to the panel and sends no enqueue.
    - The status-label span has role="status", aria-live="polite", aria-atomic="true"; the same node persists from syncing through computing to a terminal state; the elapsed counter is not inside it.
    - key-card-copy.test.ts: every S1-S2 string equals its UI-SPEC literal; formatBoundDuration(15_000) = "15 seconds", (30_000) = "30 seconds", (120_000) = "2 minutes", (180_000) = "3 minutes"; formatBoundDuration(90_000) throws.
  </behavior>
  <action>
    ApiKeyManager (KCS-03): race `handleLinkKey(keyId)` against `LINK_UPDATE_BOUND_MS` with the same shape as the Task 1 race. On expiry: console.error naming the bound; if `endAttempt(attempt)` answers true, set `unconfirmed` with `panelStopReason = "link_bound"` and `syncError = null`, and return before the baseline read and the enqueue, so no enqueue is ever sent for that attempt ("Sync not started" is used only here because only here does the card KNOW nothing was enqueued, UI-SPEC § KCS-03).
    SyncProgress (UI-SPEC § Accessibility): put `role="status" aria-live="polite" aria-atomic="true"` on the status-label `<span>` only; keep the elapsed counter a sibling outside it; do not add a second live region. The panel sits outside every key Card, so the existing `within(card)…ByRole("status")` pins in ApiKeyManager.test.tsx are unaffected — run them to prove it.
    Create src/components/strategy/key-card-copy.test.ts with the behaviour list's copy and duration cases, expected strings typed as literals (never imported from the module), including KCS01-BLOCKED with a synthetic label.
    Tests: ApiKeyManager.poll.test.tsx gains LINK-HANG and LINK-LATE; SyncProgress.poll.test.tsx gains LIVE-REGION (attributes present, node identity preserved across a syncStatus change via rerender).
    Neuter check (KCS-16): remove the link race -> LINK-HANG RED; restore GREEN; record.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/key-card-copy.test.ts src/components/strategy/SyncProgress.poll.test.tsx src/components/strategy/SyncProgress.test.ts src/components/strategy/ApiKeyManager.poll.test.tsx src/components/strategy/ApiKeyManager.test.tsx src/components/strategy/ApiKeyForm.test.tsx --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-05-t2.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-05-t2.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-05-t2.log && echo OK && npm run typecheck && npx eslint src/components/strategy/ApiKeyManager.tsx src/components/strategy/SyncProgress.tsx src/components/strategy/key-card-copy.ts</automated>
    <fails_when>no OK line is printed (LINK-HANG sends an enqueue, a copy pin mismatches by one character, or a within(card) status pin now finds two regions), tsc reports an error, or eslint reports an error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "LINK_UPDATE_BOUND_MS" src/components/strategy/ApiKeyManager.tsx` is at least 2.
    - `grep -c 'aria-live="polite"' src/components/strategy/SyncProgress.tsx` is 1.
    - key-card-copy.test.ts contains assertions for KCS01-BLOCKED, KCS01-BLOCKED-NOLABEL, KCS03-LINK, KCS03-ENQUEUE, KCS22-CAP and KCS22-NOROW (`grep -c` for each id is at least 1).
    - The SUMMARY records the neuter run RED and restore GREEN.
  </acceptance_criteria>
  <done>Both awaits ahead of the poll are bounded; each expiry is announced, truthful and Retry-free; every key-card string is locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner browser -> strategies.update (RLS), /api/keys/sync | two awaits that previously had no client-side bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.2-05-01 | Denial of service | a hung link or enqueue holding the marker and every Resync disabled | medium | mitigate | LINK_UPDATE_BOUND_MS / ENQUEUE_BOUND_MS races end the attempt through endAttempt |
| T-167.2-05-02 | Tampering | a late 202 after expiry starting a poll for an attempt the UI already ended | medium | mitigate | `attemptRef.current !== attempt` guard before every post-await write; LATE-202 test |
| T-167.2-05-03 | Repudiation | a bound expiry reported as a failure, prompting a duplicate enqueue | medium | mitigate | unconfirmed state with no Retry and copy that says the sync may still be running |
</threat_model>

<verification>
- All test files in Task 2's verify command green; `npm run typecheck` 0; eslint 0.
- `git diff --stat "$(git merge-base HEAD main)"..HEAD -- supabase/ src/app/api/keys/sync/` prints nothing.
- Schema-push gate: not applicable.
</verification>

<success_criteria>
- Residual (4) of 167 D-18 is closed: nothing on the card waits unbounded, and an expiry never claims failure.
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|---|---|---|
| `LINK_UPDATE_BOUND_MS` | const (15 000) | src/components/strategy/key-card-copy.ts |
| `ENQUEUE_BOUND_MS` | const (180 000) | src/components/strategy/key-card-copy.ts |
| `PanelStopReason` members `link_bound`, `enqueue_bound` | type widening | src/components/strategy/key-card-copy.ts |
| `unconfirmed` | `SyncStatus` member | src/components/strategy/SyncProgress.tsx |
| key-card copy pins | test file | src/components/strategy/key-card-copy.test.ts |

<output>
Create `.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-05-SUMMARY.md` when done
</output>
