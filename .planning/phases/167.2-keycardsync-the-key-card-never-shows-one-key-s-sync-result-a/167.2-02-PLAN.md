---
phase: 167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/strategy/ApiKeyManager.tsx
  - src/components/strategy/ApiKeyManager.test.tsx
  - src/components/strategy/ApiKeyForm.tsx
  - src/components/strategy/ApiKeyForm.test.tsx
  - src/components/strategy/key-card-copy.ts
autonomous: true
requirements: [KCS-01, KCS-05, KCS-14, KCS-16]
user_setup: []

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low
  # Overrun accepted (110k vs 100k smart-zone budget, low confidence, 0 calibration samples): both tasks edit ApiKeyManager.tsx and ApiKeyManager.test.tsx in the same wave-1 serial key-card chain, so a split would add a serial wave for two tasks that share every file, with no parallelism gained.

must_haves:
  truths:
    - "KCS-01: with no tracked attempt live, the sync that follows a successful Add Key RUNS AS the tracked attempt: it takes the in-flight marker (syncingKeyId = the new key), its subject is the new key (lastAttemptedKeyId), it enters computing on enqueue evidence and is polled, and a non-2xx or non-enqueued answer reaches the panel as Sync failed with the route's own message."
    - "KCS-01: while a tracked attempt is live, the Add Key form's Connect Key submit is disabled, carries aria-describedby pointing at the KCS01-BLOCKED line (or KCS01-BLOCKED-NOLABEL when the attempt's key has no label), and neither a click nor an Enter-key submit sends /api/keys/validate-and-encrypt."
    - "KCS-01 (reverse overlap, RESEARCH P7): while an Add Key is in flight (Validating...), every key's Resync and Use & Sync button is disabled, and handleSyncTrades started from any path other than the add's own post-validate call returns without registering an attempt."
    - "KCS-05: every 167 D-18 rule R1-R5 and its neuter-pinned test is kept; only R6's clause 'while an attempt is live the post-add catch reaches the console only' is replaced, because no post-add sync can start while an attempt is live. Every SEAMUX-05 and coexistence pin whose premise KCS-01 removes MOVES to pin the prevention and keeps its lineage comment; none is deleted."
    - "KCS01-BLOCKED and KCS01-BLOCKED-NOLABEL render character-for-character as locked in 167.2-UI-SPEC § KCS-01, with ASCII U+0022 quotes around the label and the label rendered as escaped React text."
    - "ApiKeyForm's new prop is optional and defaults to today's behaviour: AllocatorExchangeManager passes nothing and its existing tests stay green unedited."
    - "KCS-16: removing the Add Key block, or the add-in-flight Resync disable, turns a named test RED; restoring it turns it GREEN (recorded in the SUMMARY)."
    - "long-text (UI-SPEC § UI Considerations, S1 {keyLabel}, resolved): the label is user text rendered as escaped React text inside a wrapping <p>, and a 200-character label does not truncate the sentence's instruction (test asserts the full sentence ends with 'before connecting another key.')."
    - statement: "S1 key-card add form: empty, loading, error, populated, partial, overflow, zero-one-many and long-text probe categories (UI-SPEC § Probe coverage, auto backstop rows for S1) are answered by the KCS01 tests in this plan plus the existing ApiKeyForm tests; overflow at 320px is a held-out visual check owed at verify time."
      verification: backstop
  artifacts:
    - path: src/components/strategy/key-card-copy.ts
      provides: "addKeyBlockedReason (KCS01-BLOCKED / KCS01-BLOCKED-NOLABEL)"
    - path: src/components/strategy/ApiKeyForm.tsx
      provides: "optional submitBlockedReason prop"
    - path: src/components/strategy/ApiKeyManager.tsx
      provides: "post-add sync through handleSyncTrades; addInFlightRef; attempt-live guard"
  key_links:
    - from: "src/components/strategy/ApiKeyManager.tsx handleAddKey"
      to: "src/components/strategy/ApiKeyManager.tsx handleSyncTrades"
      via: "the post-validate call registers the tracked attempt for the new key"
      pattern: "handleSyncTrades\\(newKeyId"
    - from: src/components/strategy/ApiKeyManager.tsx
      to: src/components/strategy/ApiKeyForm.tsx
      via: "submitBlockedReason={addKeyBlockedReason(...)} while syncingKeyId is set"
      pattern: "submitBlockedReason="
---

<objective>
Close 167 D-18 residual (1) in both halves (KCS-01): the sync after an Add Key becomes the card's
one tracked attempt, and the overlap that made it untracked is prevented in both directions
(Add Key blocked while an attempt is live; Resync / Use & Sync blocked while an add is in flight).

Purpose: a post-add sync failure today can reach only the console, and a post-add success is never
polled. After this plan the owner sees every post-add outcome on the panel, about the new key.
Output: ApiKeyManager routes the post-add sync through handleSyncTrades; ApiKeyForm gains one
optional prop; key-card-copy.ts carries the two locked KCS01 strings; the SEAMUX-05 and coexistence
pins that KCS-01 invalidates MOVE with their reasons.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-CONTEXT.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-RESEARCH.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-UI-SPEC.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-CONTEXT.md
@src/components/strategy/ApiKeyManager.tsx
@src/components/strategy/ApiKeyForm.tsx
@src/components/exchanges/AllocatorExchangeManager.tsx
@DESIGN.md
</context>

<hard_constraints>
Carried into every task of this plan:
- KCS-13: The executor's update_requirements step will report KCS-nn ids as not_found — expected
  no-op; never add them to REQUIREMENTS.md and never run requirements.mark-complete by hand.
- Verify logs: before running any `<automated>` command, `export KCS_SP=<your session scratchpad directory>`
  (never /tmp; parallel agents share it, so the per-plan-and-task `kcs-NN-tN.log` names stay
  distinct). The commands expand `${KCS_SP:?KCS_SP_unset}` and fail loudly if it is unset. ⛔ Never
  write the expanded scratchpad path into any tracked file: it carries the local home path and
  username, which `npm run check:planning-hygiene` rejects in a public repo.
- No migration (KCS-15); nothing under supabase/ changes. A task that finds it needs one stops at a
  `checkpoint:decision` (KCS-17).
- Never run supabase / psql against any remote database (the CLI is linked to PRODUCTION). Unit
  tests only. No uvicorn. No credentials.
- Public repo, tracked .planning/: synthetic ids and labels only in tests and artifacts.
- Never `git checkout -- <path>`, `git restore`, `git stash`, `npm version`; no VERSION or CHANGELOG
  edit; no CI skip token in any commit message.
- No coverage / mutation floor lowered, no WAIVED_CEILING raise, no allowlist widened, no waiver.
  Cite code by SYMBOL.
- ApiKeyManager's `retireWithheldSuccess` stays the one retirement helper and runs from event
  handlers only (never a useEffect watching `withholdPanelSuccess`).
- Tests that move keep their lineage comment and say which KCS decision moved them; no pin is
  deleted.
</hard_constraints>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the post-add sync runs as the tracked attempt, end to end</name>
  <files>src/components/strategy/ApiKeyManager.tsx, src/components/strategy/ApiKeyManager.test.tsx</files>
  <read_first>
    - src/components/strategy/ApiKeyManager.tsx (`handleAddKey` including the post-add `fetch("/api/keys/sync")` chain and its R6 / WR-01 comments, `handleSyncTrades`, `endAttempt`, the `SyncAttempt` docblock, `retireWithheldSuccess` and its R5 docblock)
    - src/components/strategy/ApiKeyManager.test.tsx describe "ApiKeyManager — SEAMUX-05: both sync call sites observe the HTTP outcome" (cases "observes a 503 on the BACKGROUND sync after an add (B-06)", "observes an unrecognised 200 on the BACKGROUND sync too", "ANTI-REGRESSION: a well-formed background sync leaves the panel idle") and describe "ApiKeyManager — 160-02 conversion" (case "surfaces the link failure instead of syncing against the wrong key (NEW-C37-03 preserved)")
    - src/components/strategy/ApiKeyManager.test.tsx describe "coexistence with the local sync panel (R1-R6, D-18)" (cases "R5 remedy by Add Key…", "CONTROL (R5 on Add Key)…", "R6 (corrected)…", "ATTEMPT SCOPE, post-add route…", "the attempt is live from the CLICK…")
    - 167.2-RESEARCH.md § Q6 (the list of pins that move) and 167.2-CONTEXT.md KCS-01, KCS-05
  </read_first>
  <action>
    In `handleAddKey` (per KCS-01), after the validate-and-encrypt response yields `newKeyId`, stop linking the strategy and stop firing the untracked post-add `fetch("/api/keys/sync")` chain. Instead: keep the existing `retireWithheldSuccess()` call (R5, kept per KCS-05), close the form, and start the sync by calling `handleSyncTrades(newKeyId)` WITHOUT awaiting it ("don't block the UI" stays a requirement), then `await loadKeys()` and `router.refresh()` as today. `handleSyncTrades` already registers the attempt synchronously before its first await, links the key (`handleLinkKey`), sends the enqueue, applies `isSyncEnqueued`, enters `computing` and ends itself through `endAttempt`, so the post-add sync now takes the marker, is polled, has the new key as its subject, and reports its own failure on the panel. The link failure that `handleAddKey` used to throw into the form now surfaces as that attempt's `Sync failed` with the same `Failed to link key to strategy: …` message (no /api/keys/sync request is sent after a failed link — the NEW-C37-03 guarantee is kept).
    Remove the now-unreachable post-add `.then/.catch` chain, but MOVE its comment lineage (B-06, FINDING-10, the R6 correction, WR-01) into a short note above the `handleSyncTrades(newKeyId)` call saying the residual it described is closed by KCS-01 because the post-add sync is the tracked attempt. `SYNC_UNAVAILABLE_COPY`, `syncFailureMessage` and `isSyncEnqueued` stay (the tracked path uses all three).
    Move the tests whose premise changed, each keeping its lineage comment plus one sentence naming KCS-01: the two "observes … on the BACKGROUND sync" cases keep asserting the panel reaches `error` with the route's sentence and now also assert the Resync marker was held during the attempt; "ANTI-REGRESSION: a well-formed background sync leaves the panel idle" INVERTS to "a well-formed post-add 202 enters computing, is polled, and names the new key as the panel subject" (its reason becomes: a successful enqueue must be polled and must not manufacture an error surface); the NEW-C37-03 link-failure case asserts the message reaches the panel / error line and that no /api/keys/sync request was made; "R5 remedy by Add Key" asserts the withheld success is not re-shown (the panel now shows the new attempt's in-flight status, not `complete`); "CONTROL (R5 on Add Key)" moves to "an Add Key replaces a shown success with the new key's attempt" (KCS-01 makes the add start an attempt, which is what replaces the panel).
    Neuter check (KCS-16): restore the untracked post-add fetch temporarily, confirm the inverted ANTI-REGRESSION case goes RED, restore, confirm GREEN; record in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/ApiKeyManager.test.tsx src/components/strategy/ApiKeyManager.poll.test.tsx --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-02-t1.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-02-t1.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-02-t1.log && echo OK</automated>
    <fails_when>no OK line is printed: a moved SEAMUX-05 case still expects the panel idle after a 202, a coexistence case fails, or the log reports zero tests run</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleSyncTrades(newKeyId" src/components/strategy/ApiKeyManager.tsx` is 1.
    - Inside `handleAddKey`, no `fetch("/api/keys/sync"` call remains (`awk '/async function handleAddKey/,/async function handleLinkKey/' src/components/strategy/ApiKeyManager.tsx | grep -c 'api/keys/sync'` is 0).
    - No test was net-deleted. Capture the base copy first, then compare counts (no fallible git inside a pipeline): `BASE=$(git merge-base HEAD main) && git show "$BASE:src/components/strategy/ApiKeyManager.test.tsx" > ${KCS_SP:?KCS_SP_unset}/kcs-02-base-ApiKeyManager.test.tsx && test "$(grep -cE '^[[:space:]]*it\(' src/components/strategy/ApiKeyManager.test.tsx)" -ge "$(grep -cE '^[[:space:]]*it\(' ${KCS_SP:?KCS_SP_unset}/kcs-02-base-ApiKeyManager.test.tsx)" && echo NO-NET-DELETE` prints NO-NET-DELETE.
    - Each moved case contains the string `KCS-01`.
    - The SUMMARY records the neuter run RED and restore GREEN.
  </acceptance_criteria>
  <done>A post-add sync is indistinguishable from a Resync of the new key: tracked, polled, and reported on the panel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Key blocked while an attempt is live, and Resync blocked while an add is in flight</name>
  <files>src/components/strategy/ApiKeyForm.tsx, src/components/strategy/ApiKeyForm.test.tsx, src/components/strategy/key-card-copy.ts, src/components/strategy/ApiKeyManager.tsx, src/components/strategy/ApiKeyManager.test.tsx</files>
  <read_first>
    - src/components/strategy/ApiKeyForm.tsx (`ApiKeyFormProps`, `handleSubmit` and its NEW-C37-02 `loading` guard, the `error` caption slot and the submit `Button`)
    - src/components/strategy/ApiKeyForm.test.tsx (existing harness)
    - src/components/exchanges/AllocatorExchangeManager.tsx (its `<ApiKeyForm` mount, which must stay byte-identical in render)
    - src/components/strategy/ApiKeyManager.tsx (the Resync / Use & Sync buttons' `disabled={!!syncingKeyId}`, the R4 / R5 key-scoped disables and their reasoning, `handleSyncTrades` registration block, `attemptRef`)
    - 167.2-UI-SPEC.md § KCS-01 (both strings, placement, `aria-describedby`, P7) and § Accessibility row S1
    - src/components/exchanges/AllocatorSyncStatus.tsx (named locked-constant style)
  </read_first>
  <behavior>
    - With an attempt live on a key labelled "Example Bybit", opening the form and pressing Connect Key sends no validate request; the button is disabled; the text "Wait for the sync of \"Example Bybit\" to finish before connecting another key." is present and is the button's accessible description.
    - Same with an empty or whitespace-only label -> "Wait for the current sync to finish before connecting another key."
    - Pressing Enter inside a form input while blocked sends no validate request.
    - When the attempt ends, the line unmounts and Connect Key is enabled.
    - While an add's validate request is pending, every Resync / Use & Sync button is disabled; clicking one sends no link update and no /api/keys/sync request.
    - ApiKeyForm without the new prop renders the same DOM as before (no caption, submit enabled when not loading, no aria-describedby).
    - A 200-character label renders the whole sentence, ending with "before connecting another key.".
  </behavior>
  <action>
    Create src/components/strategy/key-card-copy.ts (no "use client"; plain constants) exporting `addKeyBlockedReason(label: string | null | undefined): string`, built from two module-level constants copied VERBATIM from 167.2-UI-SPEC.md § KCS-01 (KCS01-BLOCKED with ASCII U+0022 quotes around the interpolated label; KCS01-BLOCKED-NOLABEL when the label is null, empty or whitespace). This file is the home of every S1-S3 locked string; plans 04 and 05 extend it.
    ApiKeyForm (per KCS-01): add `submitBlockedReason?: string | null` to `ApiKeyFormProps`, default null. When set: render `<p id={…} className="text-xs text-text-muted mt-3">` holding the reason directly after the existing `error` line (the id from React `useId`), set the submit `Button` `disabled={loading || !!submitBlockedReason}` with `aria-describedby` pointing at that id, and return early in `handleSubmit` beside the existing `loading` guard so the Enter-key path is blocked the same way. Label and style of the button are unchanged. With the prop absent the render is byte-identical (AllocatorExchangeManager passes nothing).
    ApiKeyManager (per KCS-01 and RESEARCH P7): pass `submitBlockedReason={syncingKeyId ? addKeyBlockedReason(keys.find((k) => k.id === syncingKeyId)?.label) : null}` to the form. Add an `addInFlightRef` (a ref, because `loading` is async state — the NEW-C37-02 lesson): `handleAddKey` returns early when `loading` is set OR `attemptRef.current !== null` (defence in depth behind the disabled button), sets `addInFlightRef.current = true` before its first await, sets it back to false immediately before its own `handleSyncTrades(newKeyId)` call and in `finally`. `handleSyncTrades` returns early, registering nothing, when `addInFlightRef.current` is true or `attemptRef.current !== null` (the card never starts a second sync while one is live, KCS-01). Every key's Resync and Use & Sync button becomes `disabled={!!syncingKeyId || loading}`; the R4 / R5 key-scoped disables on Update password and Delete are untouched (KCS-05).
    Move the coexistence cases whose premise is now impossible (KCS-01), keeping each one's lineage comment: "R6 (corrected): a post-add sync that fails while another key's attempt is computing…" becomes "R6 superseded by KCS-01: Connect Key is blocked while another key's attempt is computing, and the attempt ends itself"; "ATTEMPT SCOPE, post-add route…" becomes "an add cannot start while an attempt awaits its own enqueue (Connect Key disabled, Enter blocked)"; "the attempt is live from the CLICK…" becomes "a Resync cannot start while an add's validate is pending (P7)". Add the KCS01 copy pins (exact textContent, both strings) and the ApiKeyForm cases from the behaviour list in ApiKeyForm.test.tsx, plus one case asserting the no-prop render has no caption and an enabled submit.
    Neuter checks (KCS-16), each RED then restored GREEN, recorded in the SUMMARY: (a) drop `submitBlockedReason` from the ApiKeyManager mount -> the blocked-submit case goes RED; (b) drop `|| loading` from the Resync disable and the `addInFlightRef` guard -> the P7 case goes RED.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/ApiKeyForm.test.tsx src/components/strategy/ApiKeyManager.test.tsx src/components/strategy/ApiKeyManager.poll.test.tsx src/components/exchanges/AllocatorExchangeManager.test.tsx --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-02-t2.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-02-t2.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-02-t2.log && echo OK && npm run typecheck && npx eslint src/components/strategy/ApiKeyManager.tsx src/components/strategy/ApiKeyForm.tsx src/components/strategy/key-card-copy.ts</automated>
    <fails_when>no OK line is printed, a KCS01 copy assertion reports a character mismatch, AllocatorExchangeManager.test.tsx fails, tsc reports an error, or eslint reports an error</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --stat "$(git merge-base HEAD main)"..HEAD -- src/components/exchanges/` prints nothing (AllocatorExchangeManager untouched).
    - `grep -c "submitBlockedReason" src/components/strategy/ApiKeyForm.tsx` is at least 3 (prop type, default, use).
    - ApiKeyManager.test.tsx contains the exact literal `Wait for the current sync to finish before connecting another key.` and a KCS01-BLOCKED literal with a synthetic label.
    - `grep -c "addInFlightRef" src/components/strategy/ApiKeyManager.tsx` is at least 3.
    - The SUMMARY records both neuter runs RED and restore GREEN.
  </acceptance_criteria>
  <done>The two operations that could overlap never do: Add Key is blocked for the whole of a live attempt with a key-scoped reason, and no attempt can start during an add.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner browser -> /api/keys/validate-and-encrypt, /api/keys/sync | the card sends credentials once and then asks the server to enqueue a sync; both routes are unchanged |
| owner browser -> strategies.update (RLS) | the link write, now made only inside the tracked attempt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.2-02-01 | Tampering | overlapping add + resync re-linking strategies.api_key_id to the wrong key | high | mitigate | Add Key blocked while an attempt is live; Resync / Use & Sync blocked while an add is in flight; both guarded in handlers by refs, not only by disabled buttons |
| T-167.2-02-02 | Repudiation | a post-add sync failure visible only in the console | medium | mitigate | the post-add sync is the tracked attempt; its failure reaches the panel (moved SEAMUX-05 pins) |
| T-167.2-02-03 | Information disclosure | the blocked line interpolates a user-authored key label | low | mitigate | rendered as escaped React text; no dangerouslySetInnerHTML; label is the owner's own |
| T-167.2-02-04 | Denial of service | duplicate validate-and-encrypt submits | low | mitigate | NEW-C37-02 loading guard kept; new early return on a live attempt |
</threat_model>

<verification>
- The four test files in Task 2's verify command are green; `npm run typecheck` exits 0; eslint over the three source files exits 0.
- `git diff --stat "$(git merge-base HEAD main)"..HEAD -- supabase/ src/components/exchanges/` prints nothing.
- Schema-push gate: not applicable (no schema file in scope).
</verification>

<success_criteria>
- Residual (1) of 167 D-18 is closed in both halves: a post-add failure reaches the panel, and a post-add outcome is polled.
- No pin was deleted; every moved pin names KCS-01 and keeps its lineage.
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|---|---|---|
| `addKeyBlockedReason` | function | src/components/strategy/key-card-copy.ts |
| `submitBlockedReason` | optional prop on `ApiKeyFormProps` | src/components/strategy/ApiKeyForm.tsx |
| `addInFlightRef` | ref inside `ApiKeyManager` | src/components/strategy/ApiKeyManager.tsx |

<output>
Create `.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-02-SUMMARY.md` when done
</output>
