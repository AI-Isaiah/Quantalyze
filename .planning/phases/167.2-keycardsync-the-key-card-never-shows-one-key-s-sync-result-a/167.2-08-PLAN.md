---
phase: 167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a
plan: 08
type: execute
wave: 2
depends_on: ["167.2-01"]
files_modified:
  - src/app/factsheet-share/[token]/page.tsx
  - src/app/factsheet-share/[token]/page.test.tsx
autonomous: true
requirements: [KCS-11, KCS-07, KCS-14, KCS-16, KCS-17]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "KCS-11: on a valid token whose payload cannot be built, the share page reads the matched strategy's compute jobs and renders one of two viewer-facing arms: arm (a) KCS11-A ('This factsheet isn't ready yet' / 'The link works — the strategy's performance data is being prepared. Try again later.') for queued, retrying and running; arm (b) KCS11-B ('This factsheet isn't available yet' / 'The link works — the strategy's performance data is not available yet. Check with the person who shared this link before trying again.') for stalled, failed, finished-without-payload, never_started and unreadable."
    - "KCS-11: the measured trigger shape (stitch_composite failed_final / permanent) renders arm (b); no rendered share-page text names an internal cause, job state, error kind or owner action, and none contains 'failed', 'error', 'computed', 'minutes' or 'few minutes'."
    - "KCS-11 (fail closed on the promise): a compute_jobs read error, a throw, or a non-exhaustive empty window renders arm (b), never arm (a); the error is logged server-side by message only."
    - "KCS-11 (bounded read): the added read is the admin client's `.from('compute_jobs')` with projection exactly `status, kind, created_at, claimed_at, member_progress_at:metadata->>member_progress_at`, bounded ONLY by `.eq('strategy_id', match.strategy_id)`, ordered by created_at descending and limited to COMPUTE_STATE_READ_LIMIT; it never selects last_error, error_kind, metadata whole, owner identity, api_keys, holdings or AUM, and it runs only in the `!payload` branch."
    - "KCS-11 / SL-1: the module still has no cache reach: page.no-cache-reach.test.ts and page.cache-isolation.test.tsx stay GREEN and UNEDITED, and the new code names no cache primitive, not even in a string or comment."
    - "KCS-11 / KCS-17 (recorded): the 167 D-04 checkpoint:decision for a plan touching the share route is NOT taken, because the read is bounded exactly as the SECURITY BOUNDARY header demands and adds no cache reach — proven by PROJECTION-PIN, BOUND-PIN and the two unedited SL-1 tests. No one-way choice (no token-format change, no widened projection) is made."
    - "KCS-11: the SECURITY BOUNDARY header enumerates three reads and states the third's bound and projection; `page.test.tsx`'s admin mock keeps its throw for any table other than strategy_shares and compute_jobs."
    - "KCS-16: neutering the arm selection (always arm a) turns TRIGGER-SHAPE-B RED; neutering the fail-closed catch (unreadable -> arm a) turns READ-ERROR-B RED; each restore GREEN; recorded in the SUMMARY."
    - "empty / loading / error (UI-SPEC § UI Considerations, S9, resolved): no chain job on record renders KCS11-B; the page is a server render correct at request time; a read error renders KCS11-B, logged."
    - statement: "S9 share pending: empty, loading, error, populated, partial, overflow, zero-one-many and long-text probe categories (UI-SPEC § Probe coverage, auto backstop rows for S9) are answered by the two-arm tests in this plan; the centred text-micro body at 320px is a held-out visual check."
      verification: backstop
    - "Planner assumption (UI-SPEC § UI Considerations, S9 KCS11-B overflow, unresolved): the KCS11-B body is longer than the string it replaces and renders centred at text-micro inside max-w-3xl; it is assumed to hold on two to three lines at desktop width and to wrap cleanly at 320px. Not measured here; the verifier's held-out visual check owns it, and EmptyStateCard is not restyled."
  artifacts:
    - path: src/app/factsheet-share/[token]/page.tsx
      provides: "bounded compute_jobs read and two-arm pending card"
    - path: src/app/factsheet-share/[token]/page.test.tsx
      provides: "arm, projection and bound pins"
  key_links:
    - from: "src/app/factsheet-share/[token]/page.tsx (!payload branch)"
      to: src/lib/compute-state.ts
      via: "admin compute_jobs read -> deriveComputeState -> recipientArm"
      pattern: "recipientArm\\("
    - from: "src/app/factsheet-share/[token]/page.tsx"
      to: src/lib/status-surface-copy.ts
      via: "SHARE_CARD_COPY[arm]"
      pattern: "SHARE_CARD_COPY"
---

<objective>
The share page tells a recipient whether the factsheet is being prepared or is simply not available,
and never promises it will appear on its own when the compute failed, stalled or never started
(KCS-11).

Purpose: the founder saw a link say "still being computed … try again in a few minutes" for a
strategy whose compute had failed permanently — "a promise that will not come true".
Output: one bounded admin read in the `!payload` branch, the plan-01 derivation, and the two locked
arms; the SECURITY BOUNDARY header updated to name the third read.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-CONTEXT.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-RESEARCH.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-UI-SPEC.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-CONTEXT.md
@src/app/factsheet-share/[token]/page.tsx
@src/app/factsheet-share/[token]/page.test.tsx
@src/app/factsheet-share/[token]/page.no-cache-reach.test.ts
@src/app/factsheet-share/[token]/page.cache-isolation.test.tsx
@src/app/api/admin/strategy-review/route.ts
@DESIGN.md
(src/lib/compute-state.ts and src/lib/status-surface-copy.ts are created by plan 01; read them once plan 01 has landed.)
</context>

<hard_constraints>
Carried into every task of this plan:
- KCS-13: The executor's update_requirements step will report KCS-nn ids as not_found — expected
  no-op; never add them to REQUIREMENTS.md and never run requirements.mark-complete by hand.
- Verify logs: before running any `<automated>` command, `export KCS_SP=<your session scratchpad directory>`
  (never /tmp; parallel agents share it, so the per-plan-and-task `kcs-NN-tN.log` names stay
  distinct). The commands expand `${KCS_SP:?KCS_SP_unset}` and fail loudly if it is unset. ⛔ Never
  write the expanded scratchpad path into any tracked file: it carries the local home path and
  username, which `npm run check:planning-hygiene` rejects in a public repo.
- No migration (KCS-15); nothing under supabase/ changes; a task that needs one stops at a
  `checkpoint:decision` (KCS-17).
- Never run supabase / psql against any remote database (CLI linked to PRODUCTION). Unit tests
  only. No uvicorn. No credentials. No real share token anywhere; test tokens are synthetic.
- Public repo: synthetic ids only.
- Never `git checkout -- <path>`, `git restore`, `git stash`, `npm version`; no VERSION or CHANGELOG
  edit; no CI skip token in any commit message.
- No floor lowered, no WAIVED_CEILING raise, no allowlist widened, no waiver. Cite by SYMBOL.
- ⛔ SL-1: `page.no-cache-reach.test.ts` and `page.cache-isolation.test.tsx` stay GREEN and
  UNEDITED; the page imports nothing new from next/cache and names no cache primitive anywhere,
  including comments and strings (the no-cache-reach test scans stripped source).
- ⛔ The added read is bounded ONLY by the HMAC-matched strategy id; if a task finds it needs any
  wider projection or a second bound, it stops at a `checkpoint:decision` (KCS-17: a widened
  projection on the public share route is founder-owned).
- `page.test.tsx`'s admin mock keeps its throw for arbitrary tables; it is extended with a
  `compute_jobs` arm, never loosened.
- AGENTS.md: read node_modules/next/dist/docs/ for server components before editing the page.
</hard_constraints>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): a valid token over a failed compute renders "not available yet", end to end through the bounded read</name>
  <files>src/app/factsheet-share/[token]/page.tsx, src/app/factsheet-share/[token]/page.test.tsx</files>
  <read_first>
    - src/app/factsheet-share/[token]/page.tsx (the SECURITY BOUNDARY header, `findShareMatch` and its error-absent logging, the `!payload` branch with its `console.warn` breadcrumb and `EmptyStateCard`)
    - src/app/factsheet-share/[token]/page.test.tsx (the `@/lib/supabase/admin` mock with its arbitrary-table throw, the payload-builder mock, "renders the pending card rather than redirecting to gone")
    - src/app/factsheet-share/[token]/page.no-cache-reach.test.ts and page.cache-isolation.test.tsx (what they scan and that all four cache-isolation cases render WITH a payload)
    - src/app/api/admin/strategy-review/route.ts (the bounded admin `compute_jobs` read precedent)
    - src/lib/compute-state.ts, src/lib/status-surface-copy.ts (plan 01)
    - 167.2-RESEARCH.md § Q4 "Share-page read" and Code Examples; 167.2-UI-SPEC.md § KCS-11
  </read_first>
  <action>
    In page.tsx (per KCS-11): inside the `!payload` branch only, after the existing `console.warn` breadcrumb, call a module-local `readShareComputeState(strategyId)` that queries the admin client: `.from("compute_jobs").select("status, kind, created_at, claimed_at, member_progress_at:metadata->>member_progress_at").eq("strategy_id", strategyId).order("created_at", { ascending: false }).limit(COMPUTE_STATE_READ_LIMIT)`. Normalise each row into `ComputeJobRow` shape (`metadata: { member_progress_at }`) so the stitch heartbeat rule applies unchanged, then `deriveComputeState({ rows, readExhaustive: rows.length < COMPUTE_STATE_READ_LIMIT, nowMs: Date.now() })`. On `{ error }` log `console.error("[factsheet-share/page] compute-state read failed", { message: error.message })` (message only, like `findShareMatch`) and derive `{ readError: true }`; on a throw, log the same way and derive the same. The arm is `recipientArm(state) === "in_progress" ? "in_progress" : "not_available"` (unreadable fails closed to arm b), and `EmptyStateCard` receives `SHARE_CARD_COPY[arm].heading` / `.body`. If the generated types do not know the JSON-path alias, cast the result type locally rather than widening the projection (RESEARCH A3: a syntax failure lands in the error branch, which is arm b).
    Update the SECURITY BOUNDARY header: "Two reads" becomes three; add (3) the `compute_jobs` read, bounded by the matched strategy id, projecting only the five fields above, run only when the payload is pending, never through the cached wrapper. Keep every existing header paragraph.
    page.test.tsx: extend the admin mock with a `compute_jobs` arm that records the projection string passed to `select`, every `eq(col, val)` pair, and the `order` / `limit` arguments, and answers a queued `{ data, error }`; KEEP the throw for any other table. TRIGGER-SHAPE-B: valid token, payload null, rows = a stitch_composite failed_final plus older chain rows -> html contains "isn&#x27;t available yet" and the KCS11-B body, and contains none of "ready yet", "being prepared", "failed", "error", "minutes". Move the existing pending pin ("renders the pending card rather than redirecting to gone") so its fixture queues a running process_key_long row, keeping its "isn&#x27;t ready yet" anchor as arm (a).
    Neuter check (KCS-16): force arm (a) -> TRIGGER-SHAPE-B RED; restore GREEN; record.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet-share/[token]/" --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-08-t1.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-08-t1.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-08-t1.log && echo OK</automated>
    <fails_when>no OK line is printed: TRIGGER-SHAPE-B renders "ready yet", the arbitrary-table throw fires, no-cache-reach or cache-isolation goes red, or the log reports zero tests run</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --stat "$(git merge-base HEAD main)"..HEAD -- "src/app/factsheet-share/[token]/page.no-cache-reach.test.ts" "src/app/factsheet-share/[token]/page.cache-isolation.test.tsx"` prints nothing.
    - `grep -c 'from("compute_jobs")' "src/app/factsheet-share/[token]/page.tsx"` is 1.
    - page.test.tsx still contains the literal `token page read an arbitrary table`.
    - The SUMMARY records the neuter run RED and restore GREEN, and records that the 167 D-04 checkpoint:decision was not taken and why (bounded read, no cache reach, the two SL-1 tests unedited and green).
  </acceptance_criteria>
  <done>A recipient of a link over a failed compute is told the factsheet is not available yet and to check with the sharer, not to try again in a few minutes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pin the bound, the projection and every fail-closed path</name>
  <files>src/app/factsheet-share/[token]/page.tsx, src/app/factsheet-share/[token]/page.test.tsx</files>
  <read_first>
    - src/app/factsheet-share/[token]/page.tsx and page.test.tsx (as left by Task 1)
    - src/lib/compute-state.ts (`recipientArm`, the state list)
    - 167.2-UI-SPEC.md § C mapping table (S9 column)
  </read_first>
  <behavior>
    - PROJECTION-PIN: the recorded select string equals exactly `status, kind, created_at, claimed_at, member_progress_at:metadata->>member_progress_at` (no last_error, no error_kind, no bare metadata).
    - BOUND-PIN: the only recorded eq pair is ("strategy_id", <the matched strategy id>); order is created_at descending; limit is 100.
    - ARM-A cases: pending, done_pending_children, failed_retry, running process_key_long, running stitch with a fresh heartbeat -> KCS11-A.
    - ARM-B cases: failed_final with each error kind (the column is not projected, so errorKind null), running stitch with a heartbeat older than 12 minutes (stalled), done (finished without payload), no rows with a short window (never_started) -> KCS11-B.
    - READ-ERROR-B: `{ error }` -> KCS11-B and console.error called with a message-only object; READ-THROWS-B: the builder throws -> KCS11-B, no uncaught throw; FULL-WINDOW-B: 100 non-chain rows -> KCS11-B (unreadable).
    - PAYLOAD-NO-READ: a built payload renders the recipient view and performs NO compute_jobs read.
  </behavior>
  <action>
    Add the behaviour cases to page.test.tsx, expectations typed as literals, synthetic ids. If any case fails, fix page.tsx within the Task 1 design (the arm mapping and the fail-closed catch); do not widen the projection or add a bound.
    Neuter check (KCS-16): map an unreadable state to arm (a) -> READ-ERROR-B RED; restore GREEN; record.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet-share/[token]/" src/lib/compute-state.test.ts --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-08-t2.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-08-t2.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-08-t2.log && echo OK && npm run typecheck && npx eslint "src/app/factsheet-share/[token]/page.tsx"</automated>
    <fails_when>no OK line is printed (PROJECTION-PIN records an extra column, BOUND-PIN records a second eq, an unreadable path renders arm a, or PAYLOAD-NO-READ records a compute_jobs read), tsc reports an error, or eslint reports an error</fails_when>
  </verify>
  <acceptance_criteria>
    - page.test.tsx contains PROJECTION-PIN, BOUND-PIN, READ-ERROR-B, READ-THROWS-B, FULL-WINDOW-B and PAYLOAD-NO-READ.
    - `git diff --stat "$(git merge-base HEAD main)"..HEAD -- "src/app/factsheet-share/[token]/page.no-cache-reach.test.ts" "src/app/factsheet-share/[token]/page.cache-isolation.test.tsx"` still prints nothing.
    - The SUMMARY records the neuter run RED and restore GREEN.
  </acceptance_criteria>
  <done>The public route's new read is pinned to one bound and one five-field projection, and every way it can fail lands on the arm that promises nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous recipient -> /factsheet-share/[token] | sessionless; the constant-time HMAC match is the only authorisation |
| page (service_role admin client) -> compute_jobs | a new read on a public route, bounded by the matched strategy id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.2-08-01 | Information disclosure | share page widened to arbitrary job or strategy data | high | mitigate | `.eq("strategy_id", match.strategy_id)` only; BOUND-PIN records every eq pair; runs only after the HMAC match and only in `!payload` |
| T-167.2-08-02 | Information disclosure | job metadata (source, correlation_id, verification_id) or last_error reaching the page | high | mitigate | five-field projection with a JSON-path alias for the heartbeat only; PROJECTION-PIN asserts the exact string |
| T-167.2-08-03 | Information disclosure | a recipient learning the failure cause | medium | mitigate | two neutral arms; tests assert no "failed", "error", state or kind names in the render (164.2 criterion 9) |
| T-167.2-08-04 | Tampering / Information disclosure | a recipient-dependent read poisoning a shared cache | high | mitigate | SL-1 unchanged: no cache primitive named; no-cache-reach and cache-isolation tests unedited and green |
| T-167.2-08-05 | Denial of service | the extra read on a public route | low | accept | runs only on a valid token with a pending payload, after `publicIpLimiter`; limit 100 rows, five columns |
| T-167.2-08-06 | Information disclosure | server error text logged or rendered | low | mitigate | message-only console.error, nothing rendered from the error |
</threat_model>

<verification>
- `npx vitest run "src/app/factsheet-share/[token]/"` green with the two SL-1 files unedited; `npm run typecheck` 0; eslint 0.
- `git diff --stat "$(git merge-base HEAD main)"..HEAD -- supabase/` prints nothing.
- Schema-push gate: not applicable.
</verification>

<success_criteria>
- A link over a failed, stalled or never-started compute promises nothing; a link over a running compute says the data is being prepared without a number of minutes.
- The route's security boundary is unchanged in kind and documented in its header.
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|---|---|---|
| `readShareComputeState` | module-local async function | src/app/factsheet-share/[token]/page.tsx |
| third read in the SECURITY BOUNDARY header | documentation | src/app/factsheet-share/[token]/page.tsx |
| arm / projection / bound pins | tests | src/app/factsheet-share/[token]/page.test.tsx |

<output>
Create `.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-08-SUMMARY.md` when done
</output>
