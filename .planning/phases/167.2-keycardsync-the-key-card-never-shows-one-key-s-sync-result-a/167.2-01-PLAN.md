---
phase: 167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/compute-state.ts
  - src/lib/compute-state.test.ts
  - src/lib/status-surface-copy.ts
  - src/lib/status-surface-copy.test.ts
  - src/lib/strategy-shape.ts
  - src/lib/strategy-shape.test.ts
  - src/lib/sync-progress.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/api/strategies/[id]/sync-progress/route.ts
  - src/app/api/strategies/[id]/sync-progress/route.test.ts
autonomous: true
requirements: [KCS-07, KCS-08, KCS-13, KCS-14, KCS-15, KCS-16, KCS-17, KCS-19, KCS-20, KCS-21]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "KCS-07: one pure, dependency-free derivation (`deriveComputeState` in src/lib/compute-state.ts) maps a strategy's compute-job rows to exactly one of queued, retrying, running, stalled, failed, finished, never_started, unreadable, and the sync-progress route, the owner factsheet, the share page and the /strategies list all call it (the route through the same `selectFactsheetJob` / `isStitchStalled` / `memberProgressOf` helpers), so the job-selection rule is written once."
    - "KCS-07: a non-stitch job is never stalled; a running stitch_composite is stalled only when its heartbeat (member_progress_at, else claimed_at) is older than STALL_THRESHOLD_MS on the caller-supplied server clock."
    - "KCS-08: the running phase is decided by job kind from the closed compute_jobs_kind_check set: process_key_long, sync_trades and derive_broker_dailies are fetch; compute_analytics_from_csv and compute_analytics are compute; a running stitch_composite with a member not yet successful carries key N of M (N = successful + 1, never above M); any unrecognised kind is phase unknown."
    - "KCS-19: done_pending_children derives to queued (in flight), and the StitchJobStatus and SyncProgressResponse.jobStatus comments in src/lib/sync-progress.ts say so."
    - "KCS-20: job selection is the latest stitch_composite, else the latest job whose kind is in FACTSHEET_CHAIN_KINDS, in the derivation AND in /api/strategies/[id]/sync-progress; a newer reconcile_strategy or sync_funding row never displaces a failed_final chain job, and the route's jobStatus null now means no factsheet-chain job is visible."
    - "KCS-20: an empty chain selection derives never_started only when the read was exhaustive (rows.length < COMPUTE_STATE_READ_LIMIT); a full window with no chain job, a read error, or an unrecognised status derives unreadable, never an in-progress claim."
    - "The measured trigger shape (a three-member composite whose stitch_composite job is failed_final / permanent) derives failed with errorKind permanent and recipient arm not_available (CONTEXT Specifics)."
    - "KCS-14 / KCS-13: every S4-S9 string in 167.2-UI-SPEC § Copywriting Contract (KCS06-*, KCS09-*, KCS10-PUBLIC, KCS11-*, KCS12-*, KCS21-*) lives in src/lib/status-surface-copy.ts and is pinned character-for-character by status-surface-copy.test.ts with expectations typed as literals, never imported from the module under test."
    - "KCS-16: each new derivation rule (chain filter, done_pending_children, exhaustiveness, stitch-only stall) has a test that goes RED when the rule is neutered and GREEN when it is restored, recorded in the SUMMARY."
    - "KCS-21 / KCS-23 (shared predicate): a strategy's shape is resolved once, by resolveStrategyShape in src/lib/strategy-shape.ts: csv by source, composite when its strategy_keys member count is greater than zero, single when a key is linked, unlinked otherwise, and unknown when the member count could not be read (never silently single)."
    - "KCS-15 / KCS-17: no file under supabase/ is created or modified; no one-way choice is made."
    - "PIN-COMPOSITE-BYTES and PIN-COMPOSITE-WINS in route.test.ts stay byte-unchanged and green."
  artifacts:
    - path: src/lib/compute-state.ts
      provides: "FACTSHEET_CHAIN_KINDS, COMPUTE_STATE_READ_LIMIT, ComputeJobRow, ComputeState, RecipientArm, selectFactsheetJob, memberProgressOf, isStitchStalled, isFactsheetJobInFlight, jobPhaseOf, deriveComputeState, recipientArm"
    - path: src/lib/status-surface-copy.ts
      provides: "ownerStateLine, ownerRemedy, recipientShareNote, SHARE_CARD_COPY, KCS10_PUBLIC_SENTENCE, untrustedKeyCaption"
    - path: src/lib/strategy-shape.ts
      provides: "StrategyShape, countCompositeMembers, resolveStrategyShape (the one composite predicate: strategy_keys member count > 0; an unknowable count is unknown, never single)"
    - path: src/lib/compute-state.test.ts
      provides: "table-driven derivation tests"
    - path: src/lib/status-surface-copy.test.ts
      provides: "character-level copy pins"
  key_links:
    - from: "src/app/api/strategies/[id]/sync-progress/route.ts"
      to: src/lib/compute-state.ts
      via: "selectFactsheetJob + isStitchStalled + memberProgressOf replace the inline selection/stall/projection block"
      pattern: "selectFactsheetJob\\("
    - from: src/lib/status-surface-copy.ts
      to: src/lib/compute-state.ts
      via: "ownerStateLine / recipientShareNote are keyed on ComputeState / RecipientArm"
      pattern: "from \"@/lib/compute-state\""
---

<objective>
Build the one compute-state derivation every Section C surface calls (KCS-07, KCS-08), with the two
research corrections folded in (KCS-19: done_pending_children is in flight; KCS-20: selection is
restricted to factsheet-chain kinds, in the sync-progress route too), plus the locked copy module
for surfaces S4-S9 so the three Wave 2 surface plans only wire it.

Purpose: the founder's complaint is that the owner and the share recipient are told "still
computing" for a strategy whose compute failed. That is fixed by ONE rule for what state a strategy
is in, written once, and one locked table of what each state says.
Output: src/lib/compute-state.ts, src/lib/status-surface-copy.ts, their tests, the route switched to
the shared selection, and corrected contract comments in src/lib/sync-progress.ts.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-CONTEXT.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-RESEARCH.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-PATTERNS.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-UI-SPEC.md
@src/lib/sync-progress.ts
@src/app/api/strategies/[id]/sync-progress/route.ts
@src/lib/closed-sets.ts
@src/lib/share-affordance.ts
@DESIGN.md
</context>

<hard_constraints>
Carried into every task of this plan:
- KCS-13: The executor's update_requirements step will report KCS-nn ids as not_found — expected
  no-op; never add them to REQUIREMENTS.md and never run requirements.mark-complete by hand.
- Verify logs: before running any `<automated>` command, `export KCS_SP=<your session scratchpad directory>`
  (never /tmp; parallel agents share it, so the per-plan-and-task `kcs-NN-tN.log` names stay
  distinct). The commands expand `${KCS_SP:?KCS_SP_unset}` and fail loudly if it is unset. ⛔ Never
  write the expanded scratchpad path into any tracked file: it carries the local home path and
  username, which `npm run check:planning-hygiene` rejects in a public repo.
- No migration (KCS-15). Nothing under supabase/ is created or edited. If a task finds it needs a
  migration, it stops at a `checkpoint:decision` and reports it as founder-owned (KCS-17).
- Never run supabase / psql against any remote database (the CLI in this checkout is linked to
  PRODUCTION). Unit tests only. No uvicorn. No credentials.
- The repo is PUBLIC and .planning/ is tracked: synthetic ids and names only (e.g.
  `00000000-0000-4000-8000-000000000001`, "Example Strategy"). Never copy a real id or name from
  ROADMAP.md or from `page.stale-analytics.test.tsx`.
- Never `git checkout -- <path>`, `git restore`, `git stash`, `npm version`. No VERSION or
  CHANGELOG edit. No CI skip token in any commit message.
- Nothing lowers a coverage or mutation floor, raises WAIVED_CEILING, widens an allowlist or adds a
  waiver. Cite code by SYMBOL, never by line number.
- The new modules carry NO "use client" directive and import nothing from next/cache or next/server
  (server pages call them; phase-148-owner-lane-cache-isolation scans for cache reach).
</hard_constraints>

<interfaces>
Contract the three Wave 2 surface plans (06, 07, 08) and the key-card plans 03 and 10 consume. Executors of
those plans read these names; do not rename them.

src/lib/compute-state.ts (dependency-free except type/const imports from ./sync-progress):
- `FACTSHEET_CHAIN_KINDS`: readonly tuple `["process_key_long","sync_trades","derive_broker_dailies","compute_analytics_from_csv","compute_analytics"]` (stitch_composite is selected separately and preferred).
- `COMPUTE_STATE_READ_LIMIT = 100` (the RPC default; RESEARCH P11).
- `interface ComputeJobRow { kind?: string; status?: string; error_kind?: string | null; claimed_at?: string | null; created_at?: string; metadata?: { member_progress?: unknown; member_progress_at?: string | null } | null }` (lifted from the route's local interface, `error_kind` added).
- `type ComputeState = { state: "queued" } | { state: "retrying" } | { state: "running"; phase: "fetch" | "compute" | "unknown"; memberOf?: { n: number; m: number } } | { state: "stalled" } | { state: "failed"; errorKind: "permanent" | "transient" | "unknown" | "orphaned" | null } | { state: "finished" } | { state: "never_started" } | { state: "unreadable" }`.
- `type RecipientArm = "in_progress" | "not_available" | "unreadable"`.
- `selectFactsheetJob(rows: readonly (ComputeJobRow | null | undefined)[]): ComputeJobRow | null`.
- `memberProgressOf(row: ComputeJobRow): MemberProgressEntry[]` (empty for non-stitch).
- `isStitchStalled(row: ComputeJobRow, nowMs: number): boolean`.
- `isFactsheetJobInFlight(status: string | null | undefined): boolean` (true for pending, running, failed_retry, done_pending_children).
- `jobPhaseOf(kind: string | null | undefined): "fetch" | "compute" | "unknown"`.
- `deriveComputeState(input: { rows: readonly (ComputeJobRow | null | undefined)[]; readExhaustive: boolean; nowMs: number } | { readError: true }): ComputeState`.
- `recipientArm(state: ComputeState): RecipientArm`.

src/lib/strategy-shape.ts (type-only import of `SupabaseClient` from @supabase/supabase-js; no "use client"):
- `type StrategyShape = "single" | "unlinked" | "composite" | "csv"`.
- `countCompositeMembers(client: SupabaseClient, strategyId: string): Promise<{ ok: true; count: number } | { ok: false; message: string }>` — a head count of `strategy_keys` rows for the strategy on WHATEVER client the caller passes (the request client in every consumer; RLS `strategy_keys_owner`); an error, a null count, or a throw is `ok: false` (the fail-closed posture of `compositeMemberCount` in src/app/api/keys/sync/route.ts). The caller logs.
- `resolveStrategyShape(input: { source: string | null | undefined; apiKeyId: string | null | undefined; memberCount: { ok: true; count: number } | { ok: false; message: string } }): StrategyShape | "unknown"` — source "csv" -> csv; unknown count -> unknown; count > 0 -> composite; apiKeyId set -> single; else unlinked.

src/lib/status-surface-copy.ts:
- imports `StrategyShape` from ./strategy-shape.
- `ownerStateLine(state: ComputeState): { id: string; text: string; tone: "muted" | "amber" | "red" }`.
- `ownerRemedy(state: ComputeState, shape: StrategyShape | "unknown", strategyId: string): { id: string; before: string; link?: { text: string; href: string }; after: string }` (rendered text = before + link.text + after).
- `recipientShareNote(mode: ShareAffordanceMode, arm: RecipientArm): string`.
- `SHARE_CARD_COPY: { in_progress: { heading: string; body: string }; not_available: { heading: string; body: string } }`.
- `KCS10_PUBLIC_SENTENCE: string`.
- `untrustedKeyCaption(exchangeCodes: readonly string[], keyCount: number): string`.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): lift the job selection into src/lib/compute-state.ts, restrict it to factsheet-chain kinds, and route /api/strategies/[id]/sync-progress through it</name>
  <files>src/lib/compute-state.ts, src/lib/compute-state.test.ts, src/app/api/strategies/[id]/sync-progress/route.ts, src/app/api/strategies/[id]/sync-progress/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/[id]/sync-progress/route.ts (the `GET` body: `ComputeJobRow`, the `isNewer` reducer, the member projection with its Security L1 guard, the "STALL IS STITCH-ONLY" block, the header paragraph that defines what `jobStatus: null` means)
    - src/app/api/strategies/[id]/sync-progress/route.test.ts (`NOW_ISO`, `stitchRow`, `otherKindRow`, PIN-COMPOSITE-BYTES, PIN-COMPOSITE-WINS, WIDEN-SINGLE-KEY-LATEST)
    - src/lib/sync-progress.ts (`coerceMemberProgressStatus`, `MemberProgressEntry`, `StitchJobStatus`, `STALL_THRESHOLD_MS`, the DEPENDENCY-FREE header)
    - 167.2-RESEARCH.md § Q3 (kind table, chain set, P8, P11) and 167.2-PATTERNS.md § src/lib/compute-state.ts
    - node_modules/next/dist/docs/ route-handler guide (AGENTS.md: this Next.js has breaking changes; read before editing a route file)
  </read_first>
  <action>
    Create src/lib/compute-state.ts with the header discipline of src/lib/sync-progress.ts (dependency-free by design, safe in a server page, a route and a client bundle, no "use client", nothing from next/cache). Export `FACTSHEET_CHAIN_KINDS`, `COMPUTE_STATE_READ_LIMIT`, `ComputeJobRow`, `selectFactsheetJob`, `memberProgressOf` and `isStitchStalled` exactly as named in the plan's interfaces block.
    `selectFactsheetJob` keeps the route's `isNewer` reducer and the stitch preference verbatim in behaviour, and changes ONLY the fallback: the non-stitch candidate is the newest row whose `kind` is in `FACTSHEET_CHAIN_KINDS` (per KCS-20; RESEARCH P8: recurring cron kinds reconcile_strategy and sync_funding, and compute_intro_snapshot / poll_positions, can be a strategy's latest row and must not be selected). Null / undefined rows are skipped.
    `memberProgressOf` is the route's field-by-field member projection moved verbatim, including the Security L1 non-string guard and `coerceMemberProgressStatus`, returning [] for a non-stitch row.
    `isStitchStalled(row, nowMs)` is the route's stall rule moved verbatim with `Date.now()` replaced by the `nowMs` parameter, and its "STALL IS STITCH-ONLY, AND MUST STAY THAT WAY" comment moved with it (KCS-07). Keep the NaN property: an unparseable heartbeat is never stalled.
    In the route, replace the inline selection / projection / stall block with calls to those three helpers, and raise the RPC's `p_limit` from 20 to `COMPUTE_STATE_READ_LIMIT` (RESEARCH P11: a chain-filtered fallback over 20 rows can be hidden by daily cron rows; route.test.ts does not pin p_limit). Keep EVERY guard and its order unchanged: `isUuid` before the limiter, `checkLimit(syncProgressLimiter, …)`, the ownership `.single()` 404, the `DEGRADED` branch, the `IDLE` body, and the `{ jobStatus, stalled, memberProgress }` whitelist. Update the route's header paragraph that defines `jobStatus: null` so it says null means no factsheet-chain job is visible (KCS-20: a deliberate behaviour change on an internal route; the wizard's SyncPreviewStep reads the same field and new strategies only have chain rows).
    Tests (TDD: write the route case first and watch it fail against the old inline block): in route.test.ts add CHAIN-FILTER cases beside the WIDEN-* cases: (1) a failed_final process_key_long plus a NEWER done reconcile_strategy returns jobStatus failed_final; (2) a NEWER sync_funding running row does not displace a done compute_analytics_from_csv row; (3) rows of only non-chain kinds return the IDLE body (jobStatus null). Leave PIN-COMPOSITE-BYTES and PIN-COMPOSITE-WINS byte-unchanged. In the new src/lib/compute-state.test.ts add unit cases for `selectFactsheetJob` (stitch preferred over a newer chain row; chain filter; null rows skipped), `isStitchStalled` (non-stitch with an ancient claimed_at is false; stitch running with heartbeat older than STALL_THRESHOLD_MS on a pinned `nowMs` is true; garbage heartbeat is false) and `memberProgressOf`. Type expected kind sets as literals in the test (oracle independence, as in closed-sets.untrusted-key-status.test.ts).
    Neuter check (KCS-16): temporarily revert the fallback to "latest of any kind", run the route test, confirm CHAIN-FILTER case (1) goes RED, restore, confirm GREEN. Record both runs in the SUMMARY. Do not commit the neutered state.
  </action>
  <verify>
    <automated>npx vitest run src/lib/compute-state.test.ts "src/app/api/strategies/[id]/sync-progress/route.test.ts" --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-01-t1.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-01-t1.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-01-t1.log && echo OK</automated>
    <fails_when>the command prints no OK line: vitest exits non-zero, a CHAIN-FILTER case reports jobStatus "done" for the reconcile row, or the log reports zero tests run</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "selectFactsheetJob(" "src/app/api/strategies/[id]/sync-progress/route.ts"` is at least 1 and `grep -c "latestAny" "src/app/api/strategies/[id]/sync-progress/route.ts"` is 0.
    - `grep -c "COMPUTE_STATE_READ_LIMIT" "src/app/api/strategies/[id]/sync-progress/route.ts"` is at least 1.
    - `git diff --stat "$(git merge-base HEAD main)"..HEAD -- "src/app/api/strategies/[id]/sync-progress/route.test.ts"` shows only additions inside new CHAIN-FILTER cases; the PIN-COMPOSITE-BYTES and PIN-COMPOSITE-WINS test bodies are byte-identical to the phase base (`git merge-base HEAD main`) (`git diff "$(git merge-base HEAD main)"..HEAD` on that file shows no hunk touching them).
    - `head -20 src/lib/compute-state.ts | grep -c "use client"` is 0 and `grep -cE "next/(cache|server)" src/lib/compute-state.ts` is 0.
    - The SUMMARY records the neuter run (RED, naming the CHAIN-FILTER case) and the restore run (GREEN).
  </acceptance_criteria>
  <done>The route answers from the shared, chain-filtered selection; composite responses are byte-identical; a newer cron row can no longer hide a failed factsheet chain job; the neuter proof is recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deriveComputeState, recipientArm and the kind classification, with the done_pending_children and retention corrections</name>
  <files>src/lib/compute-state.ts, src/lib/compute-state.test.ts, src/lib/sync-progress.ts, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx</files>
  <read_first>
    - src/lib/compute-state.ts (as left by Task 1)
    - src/lib/sync-progress.ts (`StitchJobStatus` docblock, `SyncProgressResponse.jobStatus` doc, `MEMBER_PROGRESS_STATUSES` + `coerceMemberProgressStatus` as the closed-tuple coercion pattern)
    - 167.2-CONTEXT.md KCS-07, KCS-08, KCS-19, KCS-20 and § Specifics (the three-key composite trigger, "key 3 of 3")
    - 167.2-RESEARCH.md P9, P10, P11 and § Q3 kind table
    - 167.2-UI-SPEC.md § C "One mapping, three consumers" table and the `{n}` / `{m}` rule under KCS-09
  </read_first>
  <behavior>
    - pending -> queued; done_pending_children -> queued (KCS-19); failed_retry -> retrying; running -> running; done -> finished; failed_final -> failed carrying errorKind.
    - error_kind coerced through the closed tuple permanent / transient / unknown / orphaned; anything else, including null, -> errorKind null.
    - running stitch_composite with heartbeat older than STALL_THRESHOLD_MS at nowMs -> stalled; a running process_key_long whose claimed_at is a year old -> running, never stalled.
    - running phase: process_key_long, sync_trades, derive_broker_dailies -> fetch; compute_analytics_from_csv, compute_analytics -> compute; any other kind string -> unknown (KCS-08).
    - running stitch_composite with members [successful, successful, in_process] -> running, phase fetch, memberOf { n: 3, m: 3 }; members all successful -> phase compute with no memberOf; a single member not yet successful -> memberOf { n: 1, m: 1 }; empty member_progress -> phase fetch with no memberOf.
    - no chain row and readExhaustive true -> never_started; no chain row and readExhaustive false -> unreadable; { readError: true } -> unreadable; a selected row whose status is outside the six-value domain -> unreadable.
    - the trigger shape: three-member composite, stitch_composite failed_final / permanent, older chain rows present -> failed { errorKind: "permanent" } and recipientArm not_available.
    - recipientArm: queued, retrying, running -> in_progress; stalled, failed, finished, never_started -> not_available; unreadable -> unreadable.
    - isFactsheetJobInFlight: true for pending, running, failed_retry, done_pending_children; false for done, failed_final, null, garbage.
  </behavior>
  <action>
    Add `ComputeState`, `RecipientArm`, `jobPhaseOf`, `isFactsheetJobInFlight`, `deriveComputeState` and `recipientArm` to src/lib/compute-state.ts with the exact signatures in the plan's interfaces block, implementing the behaviour list above (KCS-07, KCS-08, KCS-19, KCS-20). Status, kind and error_kind are coerced through closed readonly tuples declared in the module (house pattern: `MEMBER_PROGRESS_STATUSES` + `coerceMemberProgressStatus`), never by ad-hoc string equality spread through the function. `deriveComputeState` selects with `selectFactsheetJob`, derives stalled with `isStitchStalled`, and derives memberOf from `memberProgressOf`: m = entry count, n = count of `successful` + 1, capped at m (UI-SPEC § KCS-09). The caller supplies `nowMs` (server clock), so no function in the module calls `Date.now()`.
    Correct two comments in src/lib/sync-progress.ts, types unchanged: the `StitchJobStatus` docblock must say done_pending_children is in flight (a child waiting on its parents, counted non-terminal by the SQL bridge and the in-flight unique index), not terminal-success (KCS-19); the `SyncProgressResponse.jobStatus` doc must say null means no factsheet-chain job is visible (KCS-20).
    Comment-only edit in src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx (per KCS-20): the `FINISHED_JOB_STATUSES` docblock and the `jobsHaveFinished` docblock both define the route's null `jobStatus` by the 154-04 row-count reading. Rewrite that definition in both to say null means no factsheet-chain job is visible (KCS-20), and note that for a wizard strategy, whose only jobs are chain kinds, this still reads as nothing enqueued yet. Keep the rest of each docblock (the piggyback-timing and degraded-route reasoning in `jobsHaveFinished`, the 156-PRE / TWIN-1b lineage) and change no code line: `FINISHED_JOB_STATUSES`, `isJobInFlight` and `jobsHaveFinished` keep their bodies.
    Tests first in src/lib/compute-state.test.ts: one table-driven block per behaviour line, expected states typed as literals, a pinned NOW_MS in the style of route.test.ts, synthetic ids only.
    Neuter checks (KCS-16), each run and restored, recorded in the SUMMARY: (a) map done_pending_children to finished -> the KCS-19 case goes RED; (b) treat a non-exhaustive empty read as never_started -> the exhaustiveness case goes RED; (c) drop the isStitch guard in the stall rule -> the ancient-claimed_at case goes RED.
  </action>
  <verify>
    <automated>npx vitest run src/lib/compute-state.test.ts "src/app/api/strategies/[id]/sync-progress/route.test.ts" --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-01-t2.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-01-t2.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-01-t2.log && echo OK</automated>
    <fails_when>no OK line is printed: a derivation case asserts a different state (for example done_pending_children reads finished, or the trigger shape reads running), or the log reports zero tests run</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "Date.now" src/lib/compute-state.ts` is 0.
    - `grep -n "done_pending_children" src/lib/sync-progress.ts` shows the docblock describing it as in flight; `git diff "$(git merge-base HEAD main)"..HEAD -- src/lib/sync-progress.ts` changes comment lines only (no `export` line in the diff).
    - `grep -c "no factsheet-chain job is visible" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"` is at least 2, `grep -c 'zero .compute_jobs. rows' "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"` is 0, and `git diff HEAD -- "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"` (run before committing) changes comment lines only.
    - compute-state.test.ts contains a case named for the trigger shape asserting `{ state: "failed", errorKind: "permanent" }` and `recipientArm(...) === "not_available"`.
    - The SUMMARY records the three neuter runs RED and the restore GREEN.
  </acceptance_criteria>
  <done>Every job status and kind maps to exactly one locked state; no new job state exists beyond the rule sync-progress already ships (stalled); the trigger shape derives a terminal, not-available state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the locked copy module for surfaces S4-S9, and the one strategy-shape predicate</name>
  <files>src/lib/status-surface-copy.ts, src/lib/status-surface-copy.test.ts, src/lib/strategy-shape.ts, src/lib/strategy-shape.test.ts</files>
  <read_first>
    - 167.2-UI-SPEC.md § Copywriting Contract sections B (KCS06-ONE, KCS06-MANY, KCS12-MINT-A, KCS12-MINT-B, KCS12-UNREADABLE, KCS12-PUBLIC) and C (every KCS09-*, KCS21-*, KCS10-PUBLIC, KCS11-A, KCS11-B row and the fixed remedy strings), plus the character conventions list at the top of that section
    - src/components/exchanges/AllocatorSyncStatus.tsx (named locked constants, U+2014 as `EM_DASH`) and src/components/exchanges/AllocatorSyncStatus.test.tsx (character-level assertions)
    - src/lib/closed-sets.ts (`EXCHANGE_DISPLAY`, `UNTRUSTED_KEY_STATUS_CHIP_LABEL satisfies Record<…>` totality pattern)
    - src/lib/share-affordance.ts (`ShareAffordanceMode`, the RSC-boundary header)
    - src/lib/compute-state.ts (as left by Task 2)
    - DESIGN.md § Voice / microcopy and § Semantic-color gates
  </read_first>
  <behavior>
    - ownerStateLine returns the UI-SPEC state line and tone for every derived state: queued muted, retrying amber, running-fetch / running-member / running-compute / running-unknown muted, stalled amber (text interpolates STALL_THRESHOLD_MS / 60_000 = 12), failed permanent red, failed transient / orphaned / unknown / null amber, finished amber, never_started muted, unreadable muted.
    - ownerRemedy returns the fixed reload remedy for queued / retrying / running, the permanent contact remedy for failed permanent, the check contact remedy for finished, "Reload this page to try again." for unreadable, and the KCS21 shape remedy for stalled, every non-permanent failed and never_started. Shape "unknown" in a shape-remedy state returns "Reload this page to try again." (planner decision, see action).
    - KCS21-SINGLE and KCS21-UNLINKED carry a link { text: "edit page", href: "/strategies/{id}/edit" }; KCS21-CSV carries { text: "upload a new CSV strategy", href: "/strategies/new/wizard?source=csv" }; KCS21-COMPOSITE carries no link. before + link.text + after equals the UI-SPEC string exactly.
    - recipientShareNote: mint-token + in_progress -> KCS12-MINT-A; mint-token + not_available -> KCS12-MINT-B; mint-token + unreadable -> KCS12-UNREADABLE; public-url + any arm -> KCS12-PUBLIC.
    - SHARE_CARD_COPY.in_progress = KCS11-A heading and body; SHARE_CARD_COPY.not_available = KCS11-B heading and body; both bodies contain U+2014 and neither contains "failed", "error" or "minutes".
    - KCS10_PUBLIC_SENTENCE equals the KCS10-PUBLIC string and its lower-cased form contains neither "failed" nor "error".
    - untrustedKeyCaption(["bybit"], 1) -> "Exchange key · Bybit"; (["bybit","okx","bybit"], 3) -> "Exchange keys · Bybit, OKX"; (["bybit"], 2) -> "Exchange keys · Bybit"; a code absent from EXCHANGE_DISPLAY renders as the stored code unchanged.
    - resolveStrategyShape: source "csv" -> csv (whatever the count); { ok: false } -> unknown; count 3 -> composite; count 0 with apiKeyId -> single; count 0 without apiKeyId -> unlinked.
    - countCompositeMembers against a client double: { count: 2, error: null } -> { ok: true, count: 2 }; { count: null, error: null } -> ok false; { error: {...} } -> ok false; a throwing builder -> ok false; the double records table "strategy_keys", a head count, and `.eq("strategy_id", <id>)`.
  </behavior>
  <action>
    Create src/lib/status-surface-copy.ts (no "use client"; server pages call it) exporting the symbols in the plan's interfaces block. Every string is a module-level named constant copied VERBATIM from 167.2-UI-SPEC.md § Copywriting Contract, with the special characters written as named constants the way AllocatorSyncStatus.tsx does (U+2014 em-dash in the two KCS11 bodies, U+00B7 middle dot in the KCS06 captions, ASCII U+0027 apostrophes). Key the state-line and remedy tables with `satisfies Record<…>` over the derived-state discriminants so a missing row is a compile error (KCS-14).
    Numbers inside copy are rendered from constants, never typed as literals in the sentence: the stalled line interpolates `STALL_THRESHOLD_MS / 60_000`.
    Planner decision (recorded here and in the SUMMARY, KCS-21 discretion): when the strategy's shape could not be read ("unknown"), a state that needs a shape remedy renders the already-locked KCS09-UNREADABLE remedy sentence "Reload this page to try again." rather than guessing a shape, because a remedy naming a control the screen does not paint is the false-remedy class UI-SPEC § KCS-21 exists to prevent. No new copy is authored.
    `untrustedKeyCaption` builds KCS06-ONE for keyCount 1 and KCS06-MANY for keyCount of 2 or more, joining DISTINCT display names from `EXCHANGE_DISPLAY` in first-seen order with ", ".
    Tests in src/lib/status-surface-copy.test.ts: one assertion per UI-SPEC pin id, expected strings typed as literals in the test file (never imported from the module), explicit assertions that U+2014 and U+00B7 are present where the spec marks them, and a totality case iterating every ComputeState variant.
    Create src/lib/strategy-shape.ts with `StrategyShape`, `countCompositeMembers` and `resolveStrategyShape` exactly as in the interfaces block (KCS-21, KCS-23: the owner factsheet remedy and the key card both need the strategy's shape, and the composite predicate must be written once). The composite predicate mirrors `compositeMemberCount` in src/app/api/keys/sync/route.ts: a member count greater than zero is a composite, and an error OR a null count with no error is UNKNOWN, never single (a possible composite must not be offered a single-key remedy or a link control). The `strategy_keys` table predates the generated database types, so use the same type-only `SupabaseClient` cast src/app/api/strategies/composite/members/route.ts uses; the runtime client is still the RLS-scoped one the caller passes. Tests in src/lib/strategy-shape.test.ts per the behaviour list.
    Neuter check (KCS-16): change one character of KCS11-B's body, confirm RED, restore, confirm GREEN; and make a null count resolve to single, confirm the unknown case RED, restore GREEN. Record both in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/lib/status-surface-copy.test.ts src/lib/strategy-shape.test.ts src/lib/compute-state.test.ts --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-01-t3.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-01-t3.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-01-t3.log && echo OK && npm run typecheck && npx eslint src/lib/compute-state.ts src/lib/status-surface-copy.ts src/lib/strategy-shape.ts src/lib/sync-progress.ts "src/app/api/strategies/[id]/sync-progress/route.ts" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx" && npm run check:planning-hygiene</automated>
    <fails_when>no OK line is printed, tsc reports an error (for example a ComputeState variant missing from a satisfies-keyed table), eslint reports an error, or check:planning-hygiene exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - status-surface-copy.test.ts contains one test per pin id: KCS06-ONE, KCS06-MANY, KCS09-QUEUED, KCS09-RETRYING, KCS09-RUN-FETCH, KCS09-RUN-MEMBER, KCS09-RUN-COMPUTE, KCS09-RUN-UNKNOWN, KCS09-STALLED, KCS09-FAIL-PERMANENT, KCS09-FAIL-TRANSIENT, KCS09-FAIL-ORPHANED, KCS09-FAIL-UNKNOWN, KCS09-FAIL-OTHER, KCS09-FINISHED, KCS09-NEVER, KCS09-UNREADABLE, KCS09-RELOAD, KCS09-CONTACT-PERMANENT, KCS09-CONTACT-CHECK, KCS21-SINGLE, KCS21-UNLINKED, KCS21-COMPOSITE, KCS21-CSV, KCS12-MINT-A, KCS12-MINT-B, KCS12-UNREADABLE, KCS12-PUBLIC, KCS10-PUBLIC, KCS11-A, KCS11-B (`grep -c` for each id in the test file is at least 1).
    - `head -20 src/lib/status-surface-copy.ts | grep -c "use client"` is 0.
    - `npm run typecheck` exits 0.
    - The SUMMARY records the KCS11-B neuter RED and restore GREEN.
  </acceptance_criteria>
  <done>Every S4-S9 sentence exists once, locked verbatim, keyed so a missing state fails compilation; the surface plans import rather than author copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner browser -> /api/strategies/[id]/sync-progress | an authenticated owner polls job state; the route projects RPC rows into a whitelisted body |
| compute_jobs rows -> derivation | worker-written strings (status, kind, error_kind, metadata) are untrusted input to the pure derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.2-01-01 | Information disclosure | sync-progress route projection | medium | mitigate | the route keeps the `{ jobStatus, stalled, memberProgress }` whitelist; `memberProgressOf` is the verbatim field-by-field projection with the Security L1 guard; WIDEN-REDACTION stays green |
| T-167.2-01-02 | Tampering | derivation input coercion | low | mitigate | status, kind and error_kind pass through closed tuples; unknown status -> unreadable, unknown kind -> phase unknown, unknown error_kind -> null (ASVS V5) |
| T-167.2-01-03 | Spoofing | route ownership fence | medium | mitigate | the `isUuid` -> limiter -> ownership `.single()` 404 order is unchanged; only the post-RPC block is replaced |
| T-167.2-01-04 | Denial of service | raised p_limit 20 -> 100 | low | accept | the RPC already caps at 1000 and is `auth.uid()`-scoped; the existing syncProgressLimiter (60 / 60 s) bounds call rate |
| T-167.2-01-05 | Repudiation | a false "running" claim to the owner | medium | mitigate | read error or non-exhaustive empty window derives unreadable, never an in-progress state (tests in Task 2) |
| T-167.2-01-06 | Tampering | an unreadable member count treated as single-key, offering a link control on a composite | high | mitigate | countCompositeMembers returns ok:false on error or null count; resolveStrategyShape maps it to unknown, never single (neuter-tested) |
| T-167.2-01-07 | Information disclosure | countCompositeMembers reads strategy_keys | low | accept | head count only, no rows; callers pass the RLS-scoped request client (`strategy_keys_owner`) |
</threat_model>

<verification>
- `npx vitest run src/lib/compute-state.test.ts src/lib/status-surface-copy.test.ts "src/app/api/strategies/[id]/sync-progress/route.test.ts"` green.
- `npm run typecheck` exits 0; `npx eslint` over the four source files exits 0; `npm run check:planning-hygiene` exits 0.
- `git diff --stat "$(git merge-base HEAD main)"..HEAD -- supabase/` prints nothing (KCS-15).
- Schema-push gate: not applicable — no schema file is in scope (no supabase/migrations change).
</verification>

<success_criteria>
- One derivation, one selection rule, one locked copy table exist and are unit-pinned.
- The route's composite responses are byte-identical; its single-key fallback is chain-filtered.
- Every neuter check recorded RED then GREEN.
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|---|---|---|
| `FACTSHEET_CHAIN_KINDS` | const tuple | src/lib/compute-state.ts |
| `COMPUTE_STATE_READ_LIMIT` | const (100) | src/lib/compute-state.ts |
| `ComputeJobRow` | interface | src/lib/compute-state.ts |
| `ComputeState` | type | src/lib/compute-state.ts |
| `RecipientArm` | type | src/lib/compute-state.ts |
| `selectFactsheetJob` | function | src/lib/compute-state.ts |
| `memberProgressOf` | function | src/lib/compute-state.ts |
| `isStitchStalled` | function | src/lib/compute-state.ts |
| `isFactsheetJobInFlight` | function | src/lib/compute-state.ts |
| `jobPhaseOf` | function | src/lib/compute-state.ts |
| `deriveComputeState` | function | src/lib/compute-state.ts |
| `recipientArm` | function | src/lib/compute-state.ts |
| `StrategyShape` | type | src/lib/strategy-shape.ts |
| `countCompositeMembers` | function | src/lib/strategy-shape.ts |
| `resolveStrategyShape` | function | src/lib/strategy-shape.ts |
| `ownerStateLine` | function | src/lib/status-surface-copy.ts |
| `ownerRemedy` | function | src/lib/status-surface-copy.ts |
| `recipientShareNote` | function | src/lib/status-surface-copy.ts |
| `SHARE_CARD_COPY` | const | src/lib/status-surface-copy.ts |
| `KCS10_PUBLIC_SENTENCE` | const | src/lib/status-surface-copy.ts |
| `untrustedKeyCaption` | function | src/lib/status-surface-copy.ts |

<output>
Create `.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-01-SUMMARY.md` when done
</output>
