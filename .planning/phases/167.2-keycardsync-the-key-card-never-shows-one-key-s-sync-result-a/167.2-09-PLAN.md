---
phase: 167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a
plan: 09
type: execute
wave: 2
depends_on: ["167.2-01"]
files_modified:
  - src/app/(dashboard)/strategies/page.tsx
  - src/app/(dashboard)/strategies/page.key-pill.test.tsx
  - src/app/(dashboard)/strategies/page.share-affordance.test.tsx
  - src/app/(dashboard)/strategies/page.wizard-draft-banner.test.tsx
autonomous: true
requirements: [KCS-06, KCS-12, KCS-07, KCS-14, KCS-16]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "KCS-06: a /strategies row is marked when ANY of its feeding keys is untrusted (isUntrustedKeySyncStatus), where the feeding keys are strategies.api_key_id plus every strategy_keys member of the strategy (all members, not only open windows: the stitch fans out over every member)."
    - "KCS-06: the mark is the existing AllocatorSyncStatus pill, mounted unchanged with syncStatus, syncError, lastSyncAt and exchange exactly as the key card mounts it (locked copy: 'Key revoked' / 'Sign-in failed' and their helpers); no Badge, no chip, no new colour, token or copy family."
    - "KCS-06: one line per DISTINCT untrusted status on the row, in first-seen order over [api_key_id, then members by seq]; each line's left caption is KCS06-ONE ('Exchange key · {Exchange}') or KCS06-MANY ('Exchange keys · {Exchange1}, {Exchange2}') from untrustedKeyCaption, with display names from EXCHANGE_DISPLAY; the pill's syncError / lastSyncAt / exchange come from the first key seen in that status."
    - "KCS-06: the row makes no claim about the strategy's numbers; the row's existing name link to /strategies/{id}/edit is unchanged; a healthy row renders no footer band unless the KCS-12 note applies."
    - "KCS-06 (D-04 / Rule 12): every new read is owner-scoped on the request client (strategy_keys_owner, api_keys_owner with an allowlisted projection `id, exchange, sync_status, sync_error, last_sync_at`, never `*`) and touches no factsheet path; an api_keys read error renders no mark at all and logs; a strategy_keys read error still marks from api_key_id alone (truthful, partial) and logs."
    - "KCS-12: for a row whose strategy_analytics.computation_status fails isComputedAnalytics (the builder's own first gate, fetchAndBuildPayload's STALE-01; recorded as the 'has a computed factsheet' predicate), the footer band carries the recipient note from recipientShareNote(shareAffordanceMode(isPublishedStatus(status)), recipientArm(state)): KCS12-MINT-A / KCS12-MINT-B / KCS12-UNREADABLE for unpublished rows, KCS12-PUBLIC for published rows."
    - "KCS-12: the state for that note comes from get_user_compute_jobs called PER uncomputed row with p_strategy_id and p_limit = COMPUTE_STATE_READ_LIMIT (Promise.all), never one unfiltered call; an RPC error renders KCS12-UNREADABLE and logs. A computed row performs no RPC and shows no note. ShareableLink's mint path and label logic are unchanged."
    - "KCS-12 known limit (recorded, not fixed, WITH A DESTINATION): a computed row whose series cannot build (fewer than two dated points after normalisation, an empty series, or a composite read-path data defect) reads as 'has a factsheet' on the list and shows no note, while its recipient sees the pending page. Tightening was checked and is not reachable with a column this page can read: the builder decides it from the full daily_returns / returns_series jsonb and, for a composite, from strategy_analytics_series and csv_daily_returns read on the ADMIN client (strategy_analytics_series is deny-all under RLS), so the owner's request client cannot evaluate it, and no scalar column records it. DESTINATION: this phase's own verification residual. The executor records it in the 167.2-09 SUMMARY under a heading 'Verification residual (user-facing, routed by the orchestrator)' with the evidence above; the orchestrator routes it to a named phase at verification. The executor does NOT edit ROADMAP.md or TODOS.md for it."
    - "KCS-16: neutering the member union (api_key_id only) turns MEMBER-ONLY-UNTRUSTED RED; neutering the computed-row skip turns COMPUTED-NO-NOTE RED; each restore GREEN; recorded in the SUMMARY."
    - "zero-one-many (UI-SPEC § UI Considerations, S4, resolved): 0 untrusted statuses -> no band; 1 -> one line; 2 distinct -> two lines, each status once; several keys in one status -> KCS06-MANY with distinct exchanges."
    - "error (UI-SPEC § UI Considerations, S5, resolved): an owner compute-state read failure renders KCS12-UNREADABLE, logged server-side, never an in-progress claim."
    - "Accessibility (UI-SPEC § Accessibility S4, ruled): each AllocatorSyncStatus mount keeps its own helper live region; on this static server render the regions never change, so N marked rows add no announcements; no wrapper role is added."
    - statement: "overflow (UI-SPEC § UI Considerations, S4/S5 footer band at 320px): `flex items-start justify-between gap-3` lets the caption wrap while the pill and helper stay right-aligned; not measured at 320px or 200% zoom — a held-out visual check owed at verify time."
      verification: backstop
    - statement: "S4 and S5 probe categories (UI-SPEC § Probe coverage, auto backstop rows: empty, loading, error, populated, partial, overflow, zero-one-many, long-text) are answered by page.key-pill.test.tsx's cases plus the overflow visual check above."
      verification: backstop
  artifacts:
    - path: "src/app/(dashboard)/strategies/page.tsx"
      provides: "owner-scoped key-status and compute-state reads; footer band with AllocatorSyncStatus lines and the share note"
    - path: "src/app/(dashboard)/strategies/page.key-pill.test.tsx"
      provides: "pill and note tests with a table-dispatch double"
  key_links:
    - from: "src/app/(dashboard)/strategies/page.tsx"
      to: src/components/exchanges/AllocatorSyncStatus.tsx
      via: "one mount per distinct untrusted status"
      pattern: "<AllocatorSyncStatus"
    - from: "src/app/(dashboard)/strategies/page.tsx"
      to: src/lib/compute-state.ts
      via: "per-row get_user_compute_jobs -> deriveComputeState -> recipientArm"
      pattern: "recipientArm\\("
---

<objective>
The /strategies list, where a manager lands, marks every row whose feeding key is untrusted with the
same key-level pill the key card shows (KCS-06, 167 D-19), and tells the owner, beside each row's
share control, what a recipient of that link sees right now when the strategy has no computed
factsheet (KCS-12).

Purpose: the owner can already copy a link to a strategy whose factsheet is not there ("weird that
i can already post or copy a link but cant see it"); the note makes that visible without taking the
capability away (KCS-12's decision).
Output: three owner-scoped reads plus per-row RPC reads on the list page, a footer band on each
row card that needs one, and a new test file with the two existing test doubles extended.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-CONTEXT.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-RESEARCH.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-PATTERNS.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-UI-SPEC.md
@src/app/(dashboard)/strategies/page.tsx
@src/app/(dashboard)/strategies/page.share-affordance.test.tsx
@src/app/(dashboard)/strategies/page.wizard-draft-banner.test.tsx
@src/components/exchanges/AllocatorSyncStatus.tsx
@src/app/api/strategies/composite/members/route.ts
@src/lib/share-affordance.ts
@src/lib/closed-sets.ts
@DESIGN.md
(src/lib/compute-state.ts and src/lib/status-surface-copy.ts are created by plan 01; read them once plan 01 has landed.)
</context>

<assumption_delta_decision>
The assumption-delta detector fired (pluralization). The identity question here: which keys "feed"
a strategy — `strategies.api_key_id` alone, or the key SET (`api_key_id` plus `strategy_keys`
members)?
- Primary noun: the feeding-key set. Decision: **promote** — the row's mark is computed over the
  set, and `api_key_id` is one member of it (for a composite it is usually null).
- Invariant test (accepted): MEMBER-ONLY-UNTRUSTED — a composite whose only untrusted key is a
  `strategy_keys` member (api_key_id null) is still marked; it goes red if a future change falls back
  to `api_key_id` alone.
</assumption_delta_decision>

<hard_constraints>
Carried into every task of this plan:
- KCS-13: The executor's update_requirements step will report KCS-nn ids as not_found — expected
  no-op; never add them to REQUIREMENTS.md and never run requirements.mark-complete by hand.
- Verify logs: before running any `<automated>` command, `export KCS_SP=<your session scratchpad directory>`
  (never /tmp; parallel agents share it, so the per-plan-and-task `kcs-NN-tN.log` names stay
  distinct). The commands expand `${KCS_SP:?KCS_SP_unset}` and fail loudly if it is unset. ⛔ Never
  write the expanded scratchpad path into any tracked file: it carries the local home path and
  username, which `npm run check:planning-hygiene` rejects in a public repo.
- No migration (KCS-15); nothing under supabase/ changes; a task that needs one stops at a
  `checkpoint:decision` (KCS-17).
- Never run supabase / psql against any remote database (CLI linked to PRODUCTION). Unit tests
  only. No uvicorn. No credentials.
- Public repo: synthetic ids, names and labels only.
- Never `git checkout -- <path>`, `git restore`, `git stash`, `npm version`; no VERSION or CHANGELOG
  edit; no CI skip token in any commit message.
- No floor lowered, no WAIVED_CEILING raise, no allowlist widened, no waiver. Cite by SYMBOL.
- ⛔ RSC boundary: the page may RENDER `AllocatorSyncStatus` and `ShareableLink` (client components)
  but may only CALL predicates from modules without "use client" (`@/lib/closed-sets`,
  `@/lib/share-affordance`, `@/lib/compute-state`, `@/lib/status-surface-copy`).
- ⛔ `api_keys` is column-granted: project only allowlisted columns, never `select("*")`.
- ⛔ Local equality on key status or analytics status is forbidden: use `isUntrustedKeySyncStatus`
  and `isComputedAnalytics`.
- The two existing test doubles keep their `Unexpected table` throw; they are extended with the new
  tables and an `rpc` stub, never loosened to accept any table.
- AGENTS.md: read node_modules/next/dist/docs/ for server components before editing the page.
</hard_constraints>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): a single-key row whose key failed sign-in carries the key card's pill, end to end</name>
  <files>src/app/(dashboard)/strategies/page.tsx, src/app/(dashboard)/strategies/page.key-pill.test.tsx, src/app/(dashboard)/strategies/page.share-affordance.test.tsx, src/app/(dashboard)/strategies/page.wizard-draft-banner.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/page.tsx (the strategies select, the batched `contact_requests` `.in("strategy_id", strategyIds)` read, the row `Card` layout, the `ShareableLink` mount and its comment)
    - src/app/(dashboard)/strategies/page.share-affordance.test.tsx (probe-style child mocks, the table-dispatch double with its `Unexpected table` throw, the `row(id, status)` fixture)
    - src/app/(dashboard)/strategies/page.wizard-draft-banner.test.tsx (its own double)
    - src/components/strategy/ApiKeyManager.tsx (the S1b `AllocatorSyncStatus` mount inside `isUntrustedKeySyncStatus(key.sync_status) && (...)`)
    - src/lib/constants.ts (`API_KEY_USER_COLUMNS_ARR`) and src/lib/closed-sets.ts (`isUntrustedKeySyncStatus`, `EXCHANGE_DISPLAY`)
    - src/lib/status-surface-copy.ts (`untrustedKeyCaption`, plan 01)
    - 167.2-UI-SPEC.md § KCS-06 (placement classes, caption table)
  </read_first>
  <action>
    page.tsx (per KCS-06): after the strategies read, when `strategyIds` is non-empty, read `api_keys` on the request client with `.select("id, exchange, sync_status, sync_error, last_sync_at").in("id", keyIds)` where keyIds is the distinct non-null `api_key_id` values (Task 2 adds the members). Log a read error (`console.error("[strategies/page] api_keys read failed", message)`) and treat it as "no marks". For each row, collect its feeding keys, keep those where `isUntrustedKeySyncStatus(sync_status)`, group by status in first-seen order, and render, only when a row has something to show, a footer band `<div className="mt-3 border-t border-border pt-3 space-y-2">` at the bottom of the row `Card`; each status line is `<div className="flex items-start justify-between gap-3">` holding `<p className="text-xs text-text-muted">{untrustedKeyCaption(exchanges, count)}</p>` and `<AllocatorSyncStatus syncStatus={status} syncError={first.sync_error} lastSyncAt={first.last_sync_at} exchange={first.exchange} />`. The existing row markup above the band is unchanged.
    Extend BOTH existing doubles deliberately: add `api_keys` (and, for Task 2, `strategy_keys`) to the allowed tables with a default `{ data: [], error: null }`, add an `rpc` stub answering `{ data: [], error: null }`, keep the `Unexpected table` throw for everything else; their existing cases stay green.
    New page.key-pill.test.tsx: copy the share-affordance harness; replace `AllocatorSyncStatus` with a probe that records its props and renders them as data attributes. SINGLE-KEY-SIGNIN: one row with api_key_id -> key sign_in_failed on bybit -> one line, caption exactly "Exchange key · Bybit", probe props { syncStatus: "sign_in_failed", exchange: "bybit", … }. HEALTHY-NO-BAND: key status complete -> no band element. KEYS-READ-ERROR: api_keys error -> no probe mounted and console.error called.
    Neuter check (KCS-16): drop the untrusted filter (mark every key) -> HEALTHY-NO-BAND RED; restore GREEN; record.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/page.key-pill.test.tsx" "src/app/(dashboard)/strategies/page.share-affordance.test.tsx" "src/app/(dashboard)/strategies/page.wizard-draft-banner.test.tsx" "src/app/(dashboard)/strategies/error.test.tsx" --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-09-t1.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-09-t1.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-09-t1.log && echo OK</automated>
    <fails_when>no OK line is printed: an existing double throws `Unexpected table: api_keys`, HEALTHY-NO-BAND finds a band, the caption mismatches by a character, or the log reports zero tests run</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'select("\*")' "src/app/(dashboard)/strategies/page.tsx"` is 0.
    - Both existing test files still contain the literal `Unexpected table`.
    - page.key-pill.test.tsx contains SINGLE-KEY-SIGNIN, HEALTHY-NO-BAND and KEYS-READ-ERROR.
    - The SUMMARY records the neuter run RED and restore GREEN.
  </acceptance_criteria>
  <done>A manager on /strategies sees, on the row, the same "Sign-in failed" pill and remedy sentence the key card shows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: composite members count as feeding keys; distinct statuses, several keys, and partial read failures</name>
  <files>src/app/(dashboard)/strategies/page.tsx, src/app/(dashboard)/strategies/page.key-pill.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/page.tsx (as left by Task 1)
    - src/app/api/strategies/composite/members/route.ts (the `strategy_keys` owner read and its type-only `SupabaseClient` cast)
    - 167.2-RESEARCH.md § Q5 (all members count, read plan) and 167.2-UI-SPEC.md § KCS-06 (KCS06-MANY, distinct statuses)
  </read_first>
  <behavior>
    - MEMBER-ONLY-UNTRUSTED: composite row (api_key_id null) whose third member is revoked -> one line "Exchange key · OKX" with a Key revoked probe.
    - TWO-STATUSES: members bybit sign_in_failed and okx revoked -> two lines, each status once, in first-seen order by seq.
    - SAME-STATUS-MANY: two bybit members and one okx member all sign_in_failed -> one line "Exchange keys · Bybit, OKX".
    - SHARED-KEY-ONCE: the api_key_id key is also a member -> counted once.
    - MEMBERS-READ-ERROR: strategy_keys errors -> the api_key_id key is still judged; console.error called.
    - The strategy_keys read is `.select("strategy_id, api_key_id, seq").in("strategy_id", strategyIds)`, and the api_keys read receives the union of both id sources.
  </behavior>
  <action>
    page.tsx (per KCS-06): before the api_keys read, read `strategy_keys` with `.select("strategy_id, api_key_id, seq").in("strategy_id", strategyIds)` on the request client through the same type-only `SupabaseClient` cast src/app/api/strategies/composite/members/route.ts uses (generated types predate the table; the runtime client stays RLS-scoped). Log an error and continue with api_key_id alone (a truthful, partial mark). Feeding keys per row are `[api_key_id, ...members ordered by seq]`, de-duplicated by id; the api_keys read takes the union of all feeding key ids. Grouping and captions as in Task 1.
    Tests first per the behaviour list (synthetic ids).
    Neuter check (KCS-16): ignore strategy_keys -> MEMBER-ONLY-UNTRUSTED RED; restore GREEN; record.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/page.key-pill.test.tsx" "src/app/(dashboard)/strategies/page.share-affordance.test.tsx" "src/app/(dashboard)/strategies/page.wizard-draft-banner.test.tsx" --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-09-t2.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-09-t2.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-09-t2.log && echo OK</automated>
    <fails_when>no OK line is printed: MEMBER-ONLY-UNTRUSTED renders no line, TWO-STATUSES renders a status twice, SHARED-KEY-ONCE double-counts, or the log reports zero tests run</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '"strategy_keys"' "src/app/(dashboard)/strategies/page.tsx"` is at least 1.
    - page.key-pill.test.tsx contains MEMBER-ONLY-UNTRUSTED, TWO-STATUSES, SAME-STATUS-MANY, SHARED-KEY-ONCE and MEMBERS-READ-ERROR.
    - The SUMMARY records the neuter run RED and restore GREEN.
  </acceptance_criteria>
  <done>A composite is marked when any member's key is untrusted, each status once, naming the exchanges it is about.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the share note on rows without a computed factsheet (KCS-12)</name>
  <files>src/app/(dashboard)/strategies/page.tsx, src/app/(dashboard)/strategies/page.key-pill.test.tsx, src/app/(dashboard)/strategies/page.share-affordance.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/page.tsx (as left by Task 2; the `ShareableLink` mount and `isPublishedStatus`)
    - src/lib/share-affordance.ts (`shareAffordanceMode`, `isPublishedStatus`)
    - src/lib/closed-sets.ts (`isComputedAnalytics`) and src/lib/factsheet/fetch-and-build-payload.ts (the STALE-01 `isComputedAnalytics` gate — the predicate's source)
    - src/lib/compute-state.ts (`deriveComputeState`, `recipientArm`, `COMPUTE_STATE_READ_LIMIT`) and src/lib/status-surface-copy.ts (`recipientShareNote`)
    - src/app/(dashboard)/strategies/page.share-affordance.test.tsx (its `ShareableLink` probe and the unpublished / published row cases)
    - 167.2-UI-SPEC.md § KCS-12 (S5 table and placement) and § C mapping table (S5 column)
  </read_first>
  <behavior>
    - COMPUTED-NO-NOTE: computation_status complete (or complete_with_warnings) -> no note and no rpc call for that row.
    - MINT-A: unpublished, computing, rpc rows = a running process_key_long -> KCS12-MINT-A in the band.
    - MINT-B: unpublished, failed, rpc rows = stitch_composite failed_final permanent -> KCS12-MINT-B.
    - UNREADABLE: unpublished, rpc error -> KCS12-UNREADABLE and console.error.
    - PUBLIC: published row, computation_status failed -> KCS12-PUBLIC whatever the rpc answers.
    - The rpc is called once per uncomputed row with { p_strategy_id: <row id>, p_limit: 100 }, never with a null p_strategy_id.
    - The note renders after any S4 lines, as `<p className="text-xs text-text-muted">`; a row with a note and no untrusted key still renders the band.
    - ShareableLink receives exactly the same props as before (the share-affordance probe cases stay green).
  </behavior>
  <action>
    page.tsx (per KCS-12): add the embed `strategy_analytics ( computation_status )` to the strategies select and normalise it (object or single-element array). The "has a computed factsheet" predicate is `isComputedAnalytics(computation_status)` — recorded here and in the SUMMARY as the builder's own first gate (STALE-01), with the known limit and its destination (this phase's verification residual) in this plan's must_haves. For rows failing it, call `supabase.rpc("get_user_compute_jobs", { p_strategy_id: row.id, p_limit: COMPUTE_STATE_READ_LIMIT })` per row with `Promise.all`; derive with `deriveComputeState` (readExhaustive = rows.length < limit, nowMs = Date.now()), an `{ error }` or throw deriving `{ readError: true }` with a `console.error` naming the stage. The note text is `recipientShareNote(shareAffordanceMode(isPublishedStatus(row.status)), recipientArm(state))`, rendered in the footer band after the S4 lines. ShareableLink's props and position are unchanged.
    Tests: add the behaviour cases to page.key-pill.test.tsx (rpc stub recording args); extend page.share-affordance.test.tsx's `row` fixture with an embedded `strategy_analytics` (default complete, so its existing cases see no note) and keep its cases green unedited in their assertions.
    Neuter check (KCS-16): drop the computed-row skip -> COMPUTED-NO-NOTE RED; restore GREEN; record.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/" src/lib/compute-state.test.ts src/lib/status-surface-copy.test.ts --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-09-t3.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-09-t3.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-09-t3.log && echo OK && npm run typecheck && npx eslint "src/app/(dashboard)/strategies/page.tsx" && npm run check:planning-hygiene</automated>
    <fails_when>no OK line is printed (a computed row calls the rpc, a note mismatches its locked string, a share-affordance case fails), tsc reports an error, eslint reports an error, or check:planning-hygiene exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "p_strategy_id" "src/app/(dashboard)/strategies/page.tsx"` is at least 1 and no call passes `p_strategy_id: null`.
    - `grep -c "isComputedAnalytics(" "src/app/(dashboard)/strategies/page.tsx"` is at least 1.
    - page.key-pill.test.tsx contains COMPUTED-NO-NOTE, MINT-A, MINT-B, UNREADABLE and PUBLIC.
    - The SUMMARY records the neuter run RED and restore GREEN, the predicate decision, and the known limit under the heading `Verification residual (user-facing, routed by the orchestrator)` with the not-decidable-from-an-owner-readable-column evidence; ROADMAP.md and TODOS.md are not edited by this plan.
  </acceptance_criteria>
  <done>Beside every share control for a strategy without a computed factsheet, the owner reads what a recipient of that link sees right now.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner session -> strategy_keys, api_keys, strategy_analytics (RLS owner policies) | new owner-scoped list reads |
| owner session -> get_user_compute_jobs (SECDEF, auth.uid()-scoped) | per-row job-state reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.2-09-01 | Information disclosure | api_keys encrypted columns via a wide select | high | mitigate | allowlisted five-column projection; acceptance grep forbids `select("*")` |
| T-167.2-09-02 | Information disclosure | another owner's keys or jobs on the list | medium | mitigate | request client only (RLS `strategy_keys_owner`, `api_keys_owner`, `analytics_read`); the RPC is `auth.uid()`-scoped |
| T-167.2-09-03 | Repudiation | a failed read shown as "healthy" or "in progress" | medium | mitigate | api_keys error -> no mark + log; RPC error -> KCS12-UNREADABLE + log (Rule 12, error-absent is not legit-absent) |
| T-167.2-09-04 | Denial of service | N per-row RPCs on a long list | low | accept | only uncomputed rows call it (normally few); each is limited to 100 rows; server render |
| T-167.2-09-05 | Tampering | a causal claim about the strategy's numbers | low | mitigate | the caption names a key and the pill names the key's state (167 D-02 / D-03); no number is rendered |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/strategies/"` green; `npm run typecheck` 0; eslint 0; `npm run check:planning-hygiene` 0.
- `git diff --stat "$(git merge-base HEAD main)"..HEAD -- supabase/ src/components/strategy/ShareableLink.tsx src/components/exchanges/AllocatorSyncStatus.tsx` prints nothing.
- Schema-push gate: not applicable.
</verification>

<success_criteria>
- Every row fed by an untrusted key is marked with the locked pill; no healthy row is.
- Every share control for an uncomputed strategy says what the recipient sees.
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|---|---|---|
| per-row feeding-key resolution and status grouping | server logic in `StrategiesPage` | src/app/(dashboard)/strategies/page.tsx |
| footer band (S4 lines + S5 note) | JSX in the row `Card` | src/app/(dashboard)/strategies/page.tsx |
| strategies select embed `strategy_analytics ( computation_status )` | select widening | src/app/(dashboard)/strategies/page.tsx |
| pill and note tests | new test file | src/app/(dashboard)/strategies/page.key-pill.test.tsx |

<output>
Create `.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-09-SUMMARY.md` when done
</output>
