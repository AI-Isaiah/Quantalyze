---
phase: 167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a
plan: 06
type: execute
wave: 6
depends_on: ["167.2-01", "167.2-05"]
files_modified:
  - src/app/(dashboard)/strategies/[id]/edit/page.tsx
  - src/app/(dashboard)/strategies/[id]/edit/page.test.tsx
  - src/components/strategy/key-card-copy.ts
  - src/components/strategy/ApiKeyManager.tsx
  - src/components/strategy/ApiKeyManager.test.tsx
autonomous: true
requirements: [KCS-23, KCS-04, KCS-05, KCS-14, KCS-16]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "KCS-23: on a composite strategy (strategy_keys member count > 0, resolved by countCompositeMembers on the request client) the key card renders NO Resync, NO Use & Sync and NO Add Key control and no Add Key form; each key card keeps its avatar, label, exchange line, MT5 account line, Update password, Delete and AllocatorSyncStatus block unchanged."
    - "KCS-23: the composite card shows exactly one line directly under the 'Exchange API Keys' header, KCS23-COMPOSITE, character-for-character: 'This composite strategy reads from every key below. Keys are not linked or synced from this card: contact support@quantalyze.com to change which keys it uses or to re-run its computation.'"
    - "KCS-23 (defence in depth): handleSyncTrades, handleLinkKey and handleAddKey return without writing strategies.api_key_id whenever keyShape is not 'single' (composite or unknown), so no code path on this card can re-link a composite, or a strategy that might be one, to one key."
    - "KCS-23 (fail closed on the link controls, not on the page): when the edit page cannot read the member count (ok:false), it logs server-side with console.error (strategy id and message) and still renders the key card with keyShape 'unknown': no Resync, no Use & Sync, no Add Key and no Add Key form, AND no composite note (it could not classify the strategy); every key keeps Update password, Delete and its AllocatorSyncStatus block, so the remedy for a 'Sign-in failed' pill (the pill plan 09 surfaces on /strategies, whose row name links here) stays reachable. The page never throws to the error boundary for this read. The CSV branch performs no count read."
    - "KCS-04: when a terminal success arrives and the bounded re-read's APPLIED rows show the panel's subject key untrusted, the success is RETIRED at that moment (the panel returns to idle), and no later applied re-read — the load-error Retry, Update password's onUpdated, the post-add read, another tab's fix — can re-show it."
    - "KCS-04: every applied key-list read that shows the subject key untrusted while the panel holds a terminal success retires it through retireWithheldSuccess, which stays the ONE retirement helper (a new call site and an optional argument, not a second mechanism), keeps its functional updater, and never touches syncing, computing, error, no_result or unconfirmed."
    - "KCS-04 / KCS-05: the WR-04 read ordering is preserved: a read dropped as out of date answers with the applied read's ok AND subject status; R1-R5 and their neuter-pinned tests stay green; no useEffect watches withholdPanelSuccess."
    - "zero-one-many (UI-SPEC § UI Considerations, S3, resolved): a composite with zero readable keys shows the existing empty copy plus KCS23-COMPOSITE; no control appears for any key count."
    - "KCS-16: removing the terminal-arm retirement turns CROSS-TAB-HEAL RED; rendering the link buttons on a composite turns COMPOSITE-NO-LINK RED; each restore GREEN; recorded in the SUMMARY."
    - statement: "S3 composite key card: empty, loading, error, populated, partial, overflow, zero-one-many and long-text probe categories (UI-SPEC § Probe coverage, auto backstop rows for S3) are answered by COMPOSITE-NO-LINK, COMPOSITE-EMPTY and the existing key-card tests; the one-line note's wrap at 320px is a held-out visual check."
      verification: backstop
  artifacts:
    - path: src/components/strategy/ApiKeyManager.tsx
      provides: "optional keyShape prop ('single' | 'composite' | 'unknown'); retirement at the applied re-read"
    - path: "src/app/(dashboard)/strategies/[id]/edit/page.tsx"
      provides: "member-count read and keyShape pass-through; an unreadable count renders keyShape 'unknown' (link controls fail closed, remedy controls kept)"
    - path: src/components/strategy/key-card-copy.ts
      provides: "COMPOSITE_CARD_NOTE (KCS23-COMPOSITE)"
  key_links:
    - from: "src/app/(dashboard)/strategies/[id]/edit/page.tsx"
      to: src/components/strategy/ApiKeyManager.tsx
      via: "keyShape={count.ok ? (count.count > 0 ? 'composite' : 'single') : 'unknown'}"
      pattern: "keyShape="
    - from: "src/components/strategy/ApiKeyManager.tsx loadKeys"
      to: "src/components/strategy/ApiKeyManager.tsx retireWithheldSuccess"
      via: "applied rows' subject status"
      pattern: "retireWithheldSuccess\\("
---

<objective>
Two last key-card defects, both about a key's result or a key's link being attributed to the wrong
thing:
- KCS-23: on a composite strategy, every key shows `Use & Sync` (and the card offers `Add Key`),
  and both write `strategies.api_key_id` unconditionally, silently turning the composite into a
  single-key strategy. A data-integrity defect on the component this phase owns, fixed here.
- KCS-04 (167 D-18 residual 2): a success withheld beside a "Sign-in failed" pill can reappear
  through a re-read after another tab fixes or deletes the key; it is retired at the moment it is
  withheld instead.

Output: the edit page tells the card whether it is a composite, or that it could not tell (the
card then offers no link control but keeps its remedy controls), the card offers no link control on
a composite and says why, and retirement moves to the applied re-read.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-CONTEXT.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-RESEARCH.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-UI-SPEC.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-CONTEXT.md
@src/app/(dashboard)/strategies/[id]/edit/page.tsx
@src/app/(dashboard)/strategies/error.tsx
@src/components/strategy/ApiKeyManager.tsx
@src/app/api/strategies/composite/members/route.ts
@DESIGN.md
(src/lib/strategy-shape.ts is created by plan 01; the plan-05 SUMMARY lands before this plan runs. Read both then.)
</context>

<assumption_delta_decision>
The assumption-delta detector fired (pluralization: "another key's", "a second surface"). The real
identity question in this plan: a strategy is fed by ONE key (`strategies.api_key_id`) or by a SET
(`strategy_keys` members, composite).
- Primary noun for READS: the strategy's feeding-key set (`api_key_id` plus `strategy_keys`
  members). Decision for reads: **promote** — shape is resolved from the member set
  (resolveStrategyShape), never inferred from `api_key_id` alone.
- `strategies.api_key_id` as the single-key WRITE target: **add-alongside**, accepted debt. This
  plan removes the write on composites (KCS-23) rather than generalising the column, because
  generalising it is a schema change and KCS-15 forbids a migration. What would force a later
  promote: any surface that must change composite membership self-serve.
- Invariant test (accepted): COMPOSITE-NO-LINK — for a composite, no control on the card can
  write `api_key_id`; it goes red the moment a future change reintroduces the single-key
  assumption on this card.
</assumption_delta_decision>

<hard_constraints>
Carried into every task of this plan:
- KCS-13: The executor's update_requirements step will report KCS-nn ids as not_found — expected
  no-op; never add them to REQUIREMENTS.md and never run requirements.mark-complete by hand.
- Verify logs: before running any `<automated>` command, `export KCS_SP=<your session scratchpad directory>`
  (never /tmp; parallel agents share it, so the per-plan-and-task `kcs-NN-tN.log` names stay
  distinct). The commands expand `${KCS_SP:?KCS_SP_unset}` and fail loudly if it is unset. ⛔ Never
  write the expanded scratchpad path into any tracked file: it carries the local home path and
  username, which `npm run check:planning-hygiene` rejects in a public repo.
- No migration (KCS-15); nothing under supabase/ changes; a task that needs one stops at a
  `checkpoint:decision` (KCS-17).
- Never run supabase / psql against any remote database (CLI linked to PRODUCTION). Unit tests
  only. No uvicorn. No credentials.
- Public repo: synthetic ids, labels and emails only; support@quantalyze.com is the existing
  public support address and may appear.
- Never `git checkout -- <path>`, `git restore`, `git stash`, `npm version`; no VERSION or CHANGELOG
  edit; no CI skip token in any commit message.
- No floor lowered, no WAIVED_CEILING raise, no allowlist widened, no waiver. Cite by SYMBOL.
- AGENTS.md: read the relevant guide in node_modules/next/dist/docs/ (server components, async
  `params`, error boundaries) before editing the edit page.
- ⛔ No `useEffect` watching `withholdPanelSuccess`; retirement runs from event handlers and from
  the applied read only.
- The edit page's existing test mock is extended deliberately (dispatch on the table name); its
  existing cases stay green.
</hard_constraints>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): a composite's card offers no link control, from the edit page's member count to the card</name>
  <files>src/app/(dashboard)/strategies/[id]/edit/page.tsx, src/app/(dashboard)/strategies/[id]/edit/page.test.tsx, src/components/strategy/key-card-copy.ts, src/components/strategy/ApiKeyManager.tsx, src/components/strategy/ApiKeyManager.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/[id]/edit/page.tsx (the `strategy.source === "csv"` branch and the `<ApiKeyManager` mount)
    - src/app/(dashboard)/strategies/[id]/edit/page.test.tsx (the `@/lib/supabase/server` mock, `renderEditPage`, the three existing cases)
    - src/lib/strategy-shape.ts (`countCompositeMembers`, from plan 01)
    - src/components/strategy/ApiKeyManager.tsx (`ApiKeyManagerProps`, header `Add Key` button, the `ApiKeyForm` mount, the Resync / Use & Sync buttons, `handleLinkKey`, `handleSyncTrades`, `handleAddKey`)
    - 167.2-UI-SPEC.md § KCS-23 and 167.2-CONTEXT.md KCS-23 (including the 2026-09-23 clarification on Add Key)
  </read_first>
  <action>
    Edit page (KCS-23): in the non-CSV branch only, call `countCompositeMembers(supabase, strategy.id)` on the request client. Resolve `keyShape`: `ok: true` with a count above 0 is "composite", `ok: true` with 0 is "single", `ok: false` is "unknown" — on `ok: false` also `console.error` with the strategy id and message. Pass `keyShape` to `ApiKeyManager`. Do NOT throw on `ok: false` (orchestrator ruling on checker W-06: a page-level failure would hide Update password and Delete, the remedy for the Sign-in failed pill plan 09 shows on /strategies, whose name link lands here; neither UI-SPEC nor CONTEXT asks for a page failure). Extend page.test.tsx's server mock to dispatch on the table name (strategies keeps its current chain; strategy_keys answers a queued `{ count, error }`), keep the three existing cases green, and add: COMPOSITE-PROP (count 2 -> ApiKeyManager receives keyShape "composite"; update the ApiKeyManager stub to record props), SINGLE-PROP (count 0 -> "single"), COUNT-FAILS (error -> the page renders, ApiKeyManager receives keyShape "unknown", nothing throws, and console.error is called with the strategy id), CSV-NO-COUNT (a CSV strategy performs no strategy_keys read).
    key-card-copy.ts: add `COMPOSITE_CARD_NOTE`, the KCS23-COMPOSITE string VERBATIM from 167.2-UI-SPEC.md.
    ApiKeyManager (KCS-23): add optional `keyShape?: "single" | "composite" | "unknown"` (default "single", so every existing render is unchanged). "single" here means NOT a composite (a single-key or an unlinked strategy, both of which may be linked from this card); it is a card-local tri-state, deliberately not plan 01's `StrategyShape`, whose csv / unlinked arms the card does not need. A derived `linkControlsAllowed = keyShape === "single"`. When it is false (composite OR unknown): do not render the header `Add Key` button or the `ApiKeyForm`; render neither the Resync nor the Use & Sync button on any key; keep Update password, Delete and the AllocatorSyncStatus block as they are. Only when `keyShape === "composite"`: also render `<p className="text-xs text-text-muted">{COMPOSITE_CARD_NOTE}</p>` directly under the header row in the same `space-y-4` stack. "unknown" renders no note and no new copy (UI-SPEC defines none; the server log is the signal). Defence in depth: `handleLinkKey`, `handleSyncTrades` and `handleAddKey` return immediately when `linkControlsAllowed` is false.
    Tests in ApiKeyManager.test.tsx: COMPOSITE-NO-LINK (keyShape "composite" with three keys -> no button named Resync, Use & Sync or Add Key; Update password and Delete present on an MT5 key; strategiesUpdateMock never called); COMPOSITE-NOTE (exact textContent of the note); COMPOSITE-EMPTY (keyShape "composite" with zero keys -> the existing empty copy plus the note, no Add Key); SHAPE-UNKNOWN-REMEDY (keyShape "unknown" with an MT5 key in sign_in_failed -> no Resync, Use & Sync or Add Key, NO composite note text, the Sign-in failed pill, Update password and Delete present and Update password opens its form; strategiesUpdateMock never called); SINGLE-UNCHANGED (keyShape omitted and "single" -> the controls render as before).
    Neuter checks (KCS-16), each RED then restored GREEN, recorded: (a) render the link buttons regardless of keyShape -> COMPOSITE-NO-LINK and SHAPE-UNKNOWN-REMEDY RED; (b) make the edit page throw on ok:false -> COUNT-FAILS RED.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/[id]/edit/page.test.tsx" src/components/strategy/ApiKeyManager.test.tsx --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-06-t1.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-06-t1.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-06-t1.log && echo OK</automated>
    <fails_when>no OK line is printed: a composite or unknown-shape card shows Use & Sync, Resync or Add Key, the unknown-shape card hides Update password or Delete or shows the composite note, the note mismatches by a character, COUNT-FAILS throws instead of rendering keyShape "unknown", or the log reports zero tests run</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "keyShape=" "src/app/(dashboard)/strategies/[id]/edit/page.tsx"` is 1 and `grep -c "countCompositeMembers(" "src/app/(dashboard)/strategies/[id]/edit/page.tsx"` is 1.
    - `grep -c "COMPOSITE_CARD_NOTE" src/components/strategy/ApiKeyManager.tsx` is at least 1.
    - ApiKeyManager.test.tsx contains COMPOSITE-NO-LINK, COMPOSITE-NOTE, COMPOSITE-EMPTY, SHAPE-UNKNOWN-REMEDY and SINGLE-UNCHANGED; page.test.tsx contains COMPOSITE-PROP, SINGLE-PROP, COUNT-FAILS and CSV-NO-COUNT.
    - The SUMMARY records both neuter runs RED and restore GREEN.
  </acceptance_criteria>
  <done>No control on a composite's card, or on a card whose shape could not be read, can rewrite its api_key_id; a composite's card says in one line where the remedy lives; an unreadable count still leaves Update password and Delete in reach.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: retire a withheld success at the applied re-read, so another tab's change cannot resurrect it</name>
  <files>src/components/strategy/ApiKeyManager.tsx, src/components/strategy/ApiKeyManager.test.tsx</files>
  <read_first>
    - src/components/strategy/ApiKeyManager.tsx (`loadKeys` with `keysReadSeqRef` / `appliedKeysReadRef` (WR-04), the terminal arm of `handleSyncStatusChange` and its `setSyncStatus(reread ? status : "idle")`, `panelSubjectUntrusted`, `withholdPanelSuccess`, `retireWithheldSuccess` and its full docblock, `lastAttemptedKeyId`, the `onUpdated` and load-error Retry call sites)
    - src/components/strategy/ApiKeyManager.test.tsx describe "coexistence with the local sync panel (R1-R6, D-18)" (R2, R3, R5 cases, "reads are ordered … (WR-04)", "a terminal success is not shown until the terminal arm's re-read resolves…")
    - 167.2-CONTEXT.md KCS-04, KCS-05 and 167.2-RESEARCH.md Pattern 4
  </read_first>
  <behavior>
    - CROSS-TAB-HEAL: resync key K (MT5); the terminal re-read's rows show K sign_in_failed -> the panel returns to idle (no Up to date); then a load-error Retry (or onUpdated) re-read shows K idle (fixed in another tab) -> the panel does NOT re-appear with Up to date; K's pill clears.
    - CROSS-TAB-DELETE: same, but the later re-read no longer contains K -> no Up to date.
    - LATER-UNTRUST: a success shown for healthy K; a later applied re-read shows K revoked -> the success is retired (panel idle); a further re-read showing K healthy does not re-show it.
    - An out-of-date read dropped by WR-04 answers with the applied read's subject status.
    - A panel in syncing / computing / error / no_result / unconfirmed is never changed by a retirement.
    - CONTROL: a healthy K's success survives an unrelated re-read (R3's guard case stays green).
  </behavior>
  <action>
    ApiKeyManager (KCS-04, RESEARCH Pattern 4): mirror `lastAttemptedKeyId` into a `lastAttemptedKeyIdRef` updated wherever the state is set (loadKeys is a useCallback with deps `[currentKeyId]`, so it must read the ref). Widen `loadKeys` to resolve `{ ok: boolean; subjectStatus: string | null }`, computed from the rows it actually APPLIED; extend `appliedKeysReadRef` to store the applied subject status so a dropped out-of-date read answers with the applied read's outcome (WR-04 kept). Give `retireWithheldSuccess` an optional argument, `retireWithheldSuccess(subjectUntrusted = panelSubjectUntrusted)`, keeping its functional updater (`prev => isComputedAnalytics(prev) ? "idle" : prev`), and call it from inside `loadKeys` after rows are applied with `isUntrustedKeySyncStatus(subjectStatus)` — this is the new call site KCS-04 names, not a second mechanism. In the terminal arm, set the success only when the re-read resolved ok AND its subject status is not untrusted; otherwise land on idle (the moment of withholding is the moment of retirement). Update every caller that read the boolean (`reread = outcome`, the Retry button, `onUpdated`, `handleAddKey`, `handleSyncTrades`). Keep `withholdPanelSuccess` as a render-time backstop and rewrite its and `retireWithheldSuccess`'s docblocks to say KCS-04 replaced R2's "withheld" with "retired" (KCS-05) — the lineage paragraphs stay.
    Tests first in the coexistence describe: CROSS-TAB-HEAL, CROSS-TAB-DELETE, LATER-UNTRUST, and IN-FLIGHT-UNTOUCHED (an applied read showing the subject untrusted while the panel is computing leaves computing and the marker alone). Every existing R1-R6 / WR-* case stays green unedited unless its assertion is literally the old withhold-then-reshow behaviour; any such case MOVES with a KCS-04 note and its lineage.
    Neuter checks (KCS-16), each RED then restored GREEN, recorded: (a) remove the terminal-arm subject check -> CROSS-TAB-HEAL RED; (b) remove the loadKeys call site -> LATER-UNTRUST RED.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/ApiKeyManager.test.tsx src/components/strategy/ApiKeyManager.poll.test.tsx src/components/strategy/SyncProgress.poll.test.tsx src/components/strategy/key-card-copy.test.ts src/components/strategy/ApiKeyForm.test.tsx src/components/exchanges/AllocatorExchangeManager.test.tsx "src/app/(dashboard)/strategies/[id]/edit/page.test.tsx" --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-06-t2.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-06-t2.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-06-t2.log && echo OK && npm run typecheck && npx eslint src/components/strategy/ApiKeyManager.tsx src/components/strategy/key-card-copy.ts "src/app/(dashboard)/strategies/[id]/edit/page.tsx"</automated>
    <fails_when>no OK line is printed (CROSS-TAB-HEAL shows Up to date after the heal, an R1-R5 case goes red, or WR-04 ordering fails), tsc reports an error, or eslint reports an error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "useEffect" src/components/strategy/ApiKeyManager.tsx` equals the pre-task count, captured before committing (while HEAD is still the commit this task started from) with `git show HEAD:src/components/strategy/ApiKeyManager.tsx > "${KCS_SP:?KCS_SP_unset}/kcs-06-pre.tsx"` (it must exit 0) followed by `grep -c useEffect "$KCS_SP/kcs-06-pre.tsx"` (no new effect).
    - `grep -c "function retireWithheldSuccess" src/components/strategy/ApiKeyManager.tsx` is 1 (still one helper).
    - ApiKeyManager.test.tsx contains CROSS-TAB-HEAL, CROSS-TAB-DELETE, LATER-UNTRUST and IN-FLIGHT-UNTOUCHED.
    - The SUMMARY records both neuter runs RED and restore GREEN, and lists any existing case that moved with its reason.
  </acceptance_criteria>
  <done>A success judged against an untrusted key is gone the moment it is judged, and nothing another tab does can bring it back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner browser -> strategies.update (RLS) | the only write this card makes to the strategy row; now impossible on a composite |
| edit page (RSC, request client) -> strategy_keys (RLS strategy_keys_owner) | a new head-count read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.2-06-01 | Tampering | Use & Sync / Add Key re-linking a composite's api_key_id (silent data-integrity change) | high | mitigate | controls not rendered unless keyShape is "single" AND handlers return early otherwise; COMPOSITE-NO-LINK asserts no strategies.update |
| T-167.2-06-02 | Tampering | an unreadable member count treated as single-key | high | mitigate | ok:false renders keyShape "unknown" (no link or add control, handlers return early) and logs; COUNT-FAILS and SHAPE-UNKNOWN-REMEDY tests |
| T-167.2-06-05 | Denial of service | an unreadable member count hiding the key remedy (Update password, Delete) behind a page failure | medium | mitigate | the page never throws on ok:false; SHAPE-UNKNOWN-REMEDY asserts the remedy controls render |
| T-167.2-06-03 | Repudiation | a withheld success resurrected by another tab's change | medium | mitigate | retirement at the applied re-read; CROSS-TAB-HEAL / CROSS-TAB-DELETE / LATER-UNTRUST |
| T-167.2-06-04 | Information disclosure | the count read's error message reaching the client | low | mitigate | the message is logged server-side only; keyShape carries no error text to the client component |
</threat_model>

<verification>
- All test files in Task 2's verify command green; `npm run typecheck` 0; eslint 0.
- `git diff --stat "$(git merge-base HEAD main)"..HEAD -- supabase/` prints nothing.
- Schema-push gate: not applicable.
</verification>

<success_criteria>
- A composite cannot be turned into a single-key strategy from its own edit page.
- 167 D-18 residual (2) is closed: retirement happens at the moment of withholding.
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|---|---|---|
| `keyShape` | optional prop on `ApiKeyManagerProps` (`"single" \| "composite" \| "unknown"`) | src/components/strategy/ApiKeyManager.tsx |
| `COMPOSITE_CARD_NOTE` | const (KCS23-COMPOSITE) | src/components/strategy/key-card-copy.ts |
| `lastAttemptedKeyIdRef` | ref inside `ApiKeyManager` | src/components/strategy/ApiKeyManager.tsx |
| `loadKeys` resolving `{ ok, subjectStatus }` | signature widening | src/components/strategy/ApiKeyManager.tsx |
| `retireWithheldSuccess(subjectUntrusted?)` | optional argument | src/components/strategy/ApiKeyManager.tsx |

<output>
Create `.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-06-SUMMARY.md` when done
</output>
