---
phase: 167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a
plan: 07
type: execute
wave: 2
depends_on: ["167.2-01"]
files_modified:
  - src/app/factsheet/[id]/v2/page.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx
  - src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx
autonomous: true
requirements: [KCS-09, KCS-10, KCS-12, KCS-21, KCS-07, KCS-08, KCS-14, KCS-16]
user_setup: []

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low
  # Overrun accepted (105k vs 100k smart-zone budget, low confidence, 0 calibration samples): all three tasks edit the same owner/public factsheet page pair (page.tsx, FactsheetView.tsx) and one new test file; a split would add a serial wave on shared files, and the 5% overrun is inside the uncalibrated estimate's error.

must_haves:
  truths:
    - "KCS-09: on the OWNER lane of the pending factsheet (the `!payload` branch with lane === 'owner'), the page reads the strategy's compute jobs through get_user_compute_jobs on the REQUEST-scoped client (p_strategy_id = the strategy id, p_limit = COMPUTE_STATE_READ_LIMIT), derives the state with deriveComputeState (readExhaustive = rows.length < limit, nowMs = Date.now()), and renders one state line and one remedy line from ownerStateLine / ownerRemedy inside <section aria-label='Computation status'>."
    - "KCS-09: the measured trigger shape — a three-member composite whose stitch_composite job is failed_final / permanent — renders 'The last computation stopped on a problem that retrying alone will not resolve.' in text-negative with 'Contact support@quantalyze.com to resolve it.', and the owner render contains neither 'still computing' nor 'being prepared' nor 'being computed'."
    - "KCS-09: an RPC {error} or a throw (including a request client with no rpc member, as in page.owner-lane.test.tsx) renders KCS09-UNREADABLE ('The computation status for this strategy could not be read.' + 'Reload this page to try again.'), logs with console.error and captureToSentry, and never renders an in-progress claim."
    - "KCS-09 / D-04: the owner read and its result are lane-local variables passed only to JSX; they never enter FactsheetPayload, fetchAndBuildPayload or buildFactsheetPayloadCached; phase-148-owner-lane-cache-isolation.test.ts and page.owner-lane.test.tsx stay GREEN and UNEDITED; the new owner test asserts zero unstable_cache calls on the owner pending render."
    - "KCS-09: last_error is never rendered: no string in the state or remedy table has a slot for it, and the RPC redacts it to NULL."
    - "KCS-21: the remedy is keyed on the strategy's SHAPE via resolveStrategyShape: single-key API -> KCS21-SINGLE with an inline 'edit page' link to /strategies/{id}/edit; API with no key linked -> KCS21-UNLINKED; composite -> KCS21-COMPOSITE (support, no control); csv -> KCS21-CSV with an inline 'upload a new CSV strategy' link to /strategies/new/wizard?source=csv; shape unknown -> 'Reload this page to try again.'. Inline links carry text-accent underline underline-offset-4."
    - "KCS-21: shape inputs are read without widening Lane A: Lane B's select gains `source, api_key_id` (Lane A columns stay a subset, and the literal `strategy_analytics ( computed_at )` stays a substring, so owner-lane test 9 stays green), and the member count is read with countCompositeMembers on the request client ONLY inside the owner `!payload` branch."
    - "KCS-10: the PUBLIC lane placeholder renders the masthead, the H1 and exactly one sentence, KCS10-PUBLIC ('The detailed factsheet for this strategy is not available yet.'), in the existing mt-6 text-fixed-13 text-text-secondary slot; the italic second paragraph is gone from both lanes; the rendered public text contains none of 'still computing', 'few minutes', 'Some strategies stay in this state', 'dev-server console', 'bundled benchmark window', '2023-04-26', 'failed', 'error'."
    - "KCS-12 (S7): OwnerUnpublishedPanel gains an optional shareNote prop rendered as its last child `<p className='mt-2 text-fixed-12 text-text-muted'>`; the owner pending lane passes recipientShareNote('mint-token', recipientArm(state)) (KCS12-MINT-A / -B / -UNREADABLE); the full factsheet render passes nothing and is byte-identical."
    - "KCS-16: neutering the owner state block back to the old fixed copy turns TRIGGER-SHAPE RED; neutering the unreadable degrade (rethrowing) turns RPC-THROWS RED; each restore GREEN; recorded in the SUMMARY."
    - "empty (UI-SPEC § UI Considerations, S6, resolved): no factsheet-chain job on record renders KCS09-NEVER plus the shape remedy and never claims a computation is running."
    - "loading (S6, resolved): the owner block is a server render correct at request time; no client loading state is authored and nothing renders 'being computed' without a derived in-progress state. Planner decision (CONTEXT discretion): the block renders once per request and does not poll; KCS09-RELOAD names the refresh path."
    - "error (S6 and S7, resolved): a read error or throw renders KCS09-UNREADABLE (S6) and KCS12-UNREADABLE (S7), each logged server-side."
    - "populated (S6, resolved): every derived state maps to exactly one state line and one remedy line (the per-state table test)."
    - "partial (S6, resolved): a finished run with no buildable factsheet renders KCS09-FINISHED + KCS09-CONTACT-CHECK."
    - "zero-one-many (S6 KCS09-RUN-MEMBER, resolved): {n} never exceeds {m}; m = 1 renders key 1 of 1; all members successful routes to KCS09-RUN-COMPUTE."
    - "overflow (S6 remedy line with inline link, resolved): the 760px article wraps normally; no white-space: nowrap is added."
    - statement: "S8 public pending placeholder: overflow and long-text probe categories (UI-SPEC § Probe coverage, auto backstop rows for S8) — a single sentence in the existing slot; its wrap at 320px is a held-out visual check."
      verification: backstop
    - "Planner assumption (UI-SPEC § Probe coverage, S6 and S7 rows 'unclassified', unresolved): state coverage for S6 and S7 rests on the explicit UI Considerations rows above and on the § Copywriting Contract tables; this plan tests every state-table row for S6 and all three S7 note arms, and adds no coverage beyond them."
  artifacts:
    - path: src/app/factsheet/[id]/v2/page.tsx
      provides: "owner pending state block, shape resolution inside the owner !payload branch, KCS10 public sentence"
    - path: src/app/factsheet/[id]/v2/FactsheetView.tsx
      provides: "OwnerUnpublishedPanel optional shareNote"
    - path: src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx
      provides: "owner pending lane tests with an rpc-capable request mock"
  key_links:
    - from: "src/app/factsheet/[id]/v2/page.tsx (owner !payload branch)"
      to: src/lib/compute-state.ts
      via: "supabase.rpc('get_user_compute_jobs') -> deriveComputeState"
      pattern: "deriveComputeState\\("
    - from: "src/app/factsheet/[id]/v2/page.tsx"
      to: src/lib/status-surface-copy.ts
      via: "ownerStateLine / ownerRemedy / recipientShareNote / KCS10_PUBLIC_SENTENCE"
      pattern: "ownerStateLine\\("
---

<objective>
The owner's pending factsheet states what is actually happening (KCS-09), with a remedy that names
a control that exists for this strategy's shape (KCS-21); the public placeholder stops promising a
compute that may not be coming (KCS-10); and the owner's share panel says what a recipient of the
link sees right now (KCS-12, S7).

Purpose: the founder's words are the acceptance test — "It should clearly say what is happening."
The measured trigger was a composite whose first stitch failed permanently while this page said
"still computing".
Output: an owner-lane state block fed by the plan-01 derivation, a one-sentence public placeholder,
and one optional prop on OwnerUnpublishedPanel.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-CONTEXT.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-RESEARCH.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-PATTERNS.md
@.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-UI-SPEC.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-CONTEXT.md
@src/app/factsheet/[id]/v2/page.tsx
@src/app/factsheet/[id]/v2/FactsheetView.tsx
@src/app/factsheet/[id]/v2/page.owner-lane.test.tsx
@src/__tests__/phase-148-owner-lane-cache-isolation.test.ts
@DESIGN.md
(src/lib/compute-state.ts, src/lib/status-surface-copy.ts and src/lib/strategy-shape.ts are created by plan 01; read them once plan 01 has landed.)
</context>

<hard_constraints>
Carried into every task of this plan:
- KCS-13: The executor's update_requirements step will report KCS-nn ids as not_found — expected
  no-op; never add them to REQUIREMENTS.md and never run requirements.mark-complete by hand.
- Verify logs: before running any `<automated>` command, `export KCS_SP=<your session scratchpad directory>`
  (never /tmp; parallel agents share it, so the per-plan-and-task `kcs-NN-tN.log` names stay
  distinct). The commands expand `${KCS_SP:?KCS_SP_unset}` and fail loudly if it is unset. ⛔ Never
  write the expanded scratchpad path into any tracked file: it carries the local home path and
  username, which `npm run check:planning-hygiene` rejects in a public repo.
- No migration (KCS-15); nothing under supabase/ changes; a task that needs one stops at a
  `checkpoint:decision` (KCS-17).
- Never run supabase / psql against any remote database (CLI linked to PRODUCTION). Unit tests
  only. No uvicorn. No credentials.
- Public repo: synthetic ids and names in every new test ("Example Composite",
  `00000000-0000-4000-8000-0000000000a1`). ⛔ Do NOT copy the real id or name quoted in
  page.stale-analytics.test.tsx's comments or its V2b title; do not edit the V2b title.
- Never `git checkout -- <path>`, `git restore`, `git stash`, `npm version`; no VERSION or CHANGELOG
  edit; no CI skip token in any commit message.
- No floor lowered, no WAIVED_CEILING raise, no allowlist widened, no waiver. Cite by SYMBOL.
- ⛔ D-04 / SL-1: `src/__tests__/phase-148-owner-lane-cache-isolation.test.ts` and
  `src/app/factsheet/[id]/v2/page.owner-lane.test.tsx` stay GREEN and UNEDITED. The new read lives
  only in the owner `!payload` branch, never touches `buildFactsheetPayloadCached` /
  `fetchAndBuildPayload` / `FactsheetPayload`, and `unstable_cache(` still appears exactly once in
  page.tsx.
- ⛔ Lane A's select string is not changed.
- AGENTS.md: read node_modules/next/dist/docs/ for server components, `next/link` and async
  `params` before editing page.tsx.
- DESIGN.md: tone by meaning (§ Semantic-color gates): muted text-text-secondary, amber
  text-warning, red text-negative only for permanent; no new colour or token.
</hard_constraints>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the owner pending page states the trigger shape's real state, end to end</name>
  <files>src/app/factsheet/[id]/v2/page.tsx, src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/page.tsx (the lane-local `ownershipMark` / `hasActiveShare` declarations and the share-state read's error-absent logging; the `!payload` branch: its `console.warn` hint (kept), `pendingName`, the `OwnerUnpublishedPanel` mount and the two `<p>` slots)
    - src/app/factsheet/[id]/v2/page.owner-lane.test.tsx (module mocks, `STATE`, `mockRequestClient`, `mockAdmin`, `givenOwnerDraft`, test 6 — to COPY into a new file; this file is not edited)
    - src/lib/compute-state.ts and src/lib/status-surface-copy.ts (plan 01)
    - 167.2-UI-SPEC.md § KCS-09 (placement, classes, state table) and § Surface Map S6
  </read_first>
  <action>
    In page.tsx (per KCS-09): inside the `!payload` branch, when `lane === "owner"`, read `supabase.rpc("get_user_compute_jobs", { p_strategy_id: signature.id, p_limit: COMPUTE_STATE_READ_LIMIT })` in a try/catch on the request-scoped client. On `{ error }`: `console.error("[factsheet/v2/page] compute-state read failed", { id, code, message })` plus `captureToSentry(error, { tags: { route: "factsheet/v2/page", stage: "compute-state" } })` and derive `{ readError: true }`. On a throw: the same log shape with the caught value, same derivation. Otherwise `deriveComputeState({ rows, readExhaustive: rows.length < COMPUTE_STATE_READ_LIMIT, nowMs: Date.now() })`. Hold the result in a lane-local variable next to the other owner facts and pass it only to JSX.
    Replace the two placeholder paragraphs on the OWNER lane with `<section aria-label="Computation status">` holding the state line `<p className={"mt-6 text-fixed-13 " + toneClass}>` (muted text-text-secondary, amber text-warning, red text-negative, from `ownerStateLine(state).tone`) and the remedy line `<p className="mt-3 text-fixed-12 text-text-muted">` (no italic) built from `ownerRemedy(state, shape, signature.id)`; when the remedy carries a link, render it with `next/link` and `className="text-accent underline underline-offset-4"`. In this tracer task pass shape "unknown" (Task 2 resolves it); the trigger shape's remedy (permanent) does not depend on shape. Keep the `console.warn` payload-pending hint exactly as it is (criterion 9's developer channel).
    New test file page.owner-compute-state.test.tsx: copy page.owner-lane.test.tsx's module mocks, STATE and request-mock pattern, and ADD an `rpc` member to this file's own request mock that records `[name, args]` and returns a queued `{ data, error }` (the existing owner-lane file stays unedited). TRIGGER-SHAPE: owner draft, admin build null, rpc returns a stitch_composite failed_final / permanent row plus two older chain rows -> the rendered text contains the KCS09-FAIL-PERMANENT sentence and KCS09-CONTACT-PERMANENT, the state line element has class text-negative, and the text contains none of "still computing", "being prepared", "being computed"; the recorded rpc args are `{ p_strategy_id: <id>, p_limit: 100 }`; `unstable_cache` was called 0 times.
    Neuter check (KCS-16): render the old fixed copy on the owner lane -> TRIGGER-SHAPE RED; restore GREEN; record.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx" "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" src/__tests__/phase-148-owner-lane-cache-isolation.test.ts --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-07-t1.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-07-t1.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-07-t1.log && echo OK</automated>
    <fails_when>no OK line is printed: TRIGGER-SHAPE finds "still computing", owner-lane test 6 or 9 goes red, phase-148 reports a cache reach, or the log reports zero tests run</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --stat "$(git merge-base HEAD main)"..HEAD -- "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" src/__tests__/phase-148-owner-lane-cache-isolation.test.ts` prints nothing.
    - `grep -c "unstable_cache(" "src/app/factsheet/[id]/v2/page.tsx"` is 1 (unchanged).
    - `grep -c "get_user_compute_jobs" "src/app/factsheet/[id]/v2/page.tsx"` is 1.
    - The SUMMARY records the neuter run RED and restore GREEN.
  </acceptance_criteria>
  <done>The owner of a failed composite reads that the computation stopped and whom to contact, not that it is still computing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: every state row, the shape-keyed remedy, and the unreadable degrade</name>
  <files>src/app/factsheet/[id]/v2/page.tsx, src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/page.tsx (as left by Task 1; the Lane B select string and the comment above it explaining the superset rule)
    - src/app/factsheet/[id]/v2/page.owner-lane.test.tsx test 9 (Lane B superset and the `strategy_analytics ( computed_at )` substring) — read, do not edit
    - src/lib/strategy-shape.ts (plan 01: `countCompositeMembers`, `resolveStrategyShape`)
    - src/lib/status-surface-copy.ts (`ownerStateLine`, `ownerRemedy`)
    - 167.2-UI-SPEC.md § KCS-09 table, § KCS-21 table and link rules
  </read_first>
  <behavior>
    - One case per KCS09 row: queued (pending and done_pending_children), retrying, running fetch, running member (two successful + one in_process -> "key 3 of 3"), running compute, running unknown kind, stalled (stitch, heartbeat older than 12 minutes), failed permanent / transient / orphaned / unknown / null, finished, never_started (empty exhaustive rows), unreadable — each asserts the exact state-line text, its tone class, and the exact remedy textContent.
    - Shape remedies: single (api_key_id set, count 0) -> KCS21-SINGLE with an anchor to /strategies/{id}/edit; unlinked -> KCS21-UNLINKED; composite (count 3) -> KCS21-COMPOSITE with no anchor; csv (source csv) -> KCS21-CSV with an anchor to /strategies/new/wizard?source=csv; count read fails -> "Reload this page to try again." plus a console.error.
    - RPC-ERROR: rpc returns { error } -> KCS09-UNREADABLE + console.error + captureToSentry called with stage compute-state.
    - RPC-THROWS: the request mock has no rpc member -> KCS09-UNREADABLE, no uncaught throw.
    - A newer done reconcile_strategy row after a failed_final chain job -> the failed line, not the finished line (KCS-20 through the shared derivation).
  </behavior>
  <action>
    page.tsx (KCS-21): add `source, api_key_id` to the Lane B select string only (Lane A is untouched; the Lane B list stays a superset of Lane A plus `status`, and the literal `strategy_analytics ( computed_at )` stays in it). Inside the owner `!payload` branch, in the same try as the RPC read, call `countCompositeMembers(supabase, signature.id)`; log an `ok: false` answer the same way as the RPC error (stage `strategy-shape`); then `resolveStrategyShape({ source: ownerSource, apiKeyId: ownerApiKeyId, memberCount })` and pass the shape to `ownerRemedy`. `ownerSource` / `ownerApiKeyId` are lane-local variables captured from `ownRow` where `lane = "owner"` is set (beside `ownershipMark`), because `signature`'s type is Lane A's column list; they never reach the payload. The count read runs only on the owner pending render, never on the full render, so owner-lane test 9's request-select count (3, full render) is unaffected.
    Tests first per the behaviour list, with row fixtures in the style of route.test.ts (`NOW_ISO`, synthetic ids) and expectations typed as literals.
    Neuter check (KCS-16): rethrow inside the catch -> RPC-THROWS RED; restore GREEN; record.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx" "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" src/__tests__/phase-148-owner-lane-cache-isolation.test.ts "src/app/factsheet/[id]/v2/page.smoothed-wiring.test.tsx" --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-07-t2.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-07-t2.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-07-t2.log && echo OK</automated>
    <fails_when>no OK line is printed: a state row's text or tone mismatches, the composite remedy renders a link, RPC-THROWS propagates, owner-lane test 9 goes red, or the log reports zero tests run</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "countCompositeMembers(" "src/app/factsheet/[id]/v2/page.tsx"` is 1 and it appears after the `if (!payload)` line (`awk` order check recorded in the SUMMARY).
    - The Lane A select string in page.tsx is byte-identical to the phase base (`git merge-base HEAD main`) (`git diff "$(git merge-base HEAD main)"..HEAD` on that file shows no hunk on the `withPublishedOnly(` select line).
    - page.owner-compute-state.test.tsx contains RPC-ERROR, RPC-THROWS and one case per KCS09 and KCS21 pin id.
    - The SUMMARY records the neuter run RED and restore GREEN.
  </acceptance_criteria>
  <done>Every derived state has exactly one truthful sentence and one remedy that names a control this strategy's edit page actually paints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the public placeholder says one neutral sentence, and the owner's share panel says what a recipient sees</name>
  <files>src/app/factsheet/[id]/v2/page.tsx, src/app/factsheet/[id]/v2/FactsheetView.tsx, src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx, src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (`OwnerUnpublishedPanel`, `OwnerUnpublishedNotice`, the optional-prop-with-safe-default pattern `hasActiveShare = false`)
    - src/app/factsheet/[id]/v2/FactsheetView.share-affordance.test.tsx (must stay green)
    - src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx (V2 and V2b: positives to MOVE, negatives to KEEP; the comments carry a real id — do not copy it)
    - src/lib/status-surface-copy.ts (`KCS10_PUBLIC_SENTENCE`, `recipientShareNote`) and src/lib/compute-state.ts (`recipientArm`)
    - 167.2-UI-SPEC.md § KCS-10 and § KCS-12 (S7)
  </read_first>
  <behavior>
    - Public lane pending render: exactly one sentence under the H1, equal to KCS10_PUBLIC_SENTENCE; no italic paragraph; none of the forbidden substrings; still no banner node.
    - Owner lane pending render: OwnerUnpublishedPanel's last child is the share note: KCS12-MINT-A for a running job, KCS12-MINT-B for a failed job, KCS12-UNREADABLE for an unreadable read.
    - The full factsheet render passes no shareNote: OwnerUnpublishedPanel renders no extra <p>.
  </behavior>
  <action>
    page.tsx (KCS-10): on the PUBLIC lane of the `!payload` branch, render only `<p className="mt-6 text-fixed-13 text-text-secondary">{KCS10_PUBLIC_SENTENCE}</p>` under the H1; remove the italic paragraph from both lanes (the owner lane already replaced it in Task 1). Keep the criterion-9 comment, reworded to describe the one neutral sentence.
    FactsheetView.tsx (KCS-12, S7): add `shareNote?: string` to `OwnerUnpublishedPanel`, rendered as its last child after the control row as `<p className="mt-2 text-fixed-12 text-text-muted">`; absent -> no element (byte-identical full render). `ShareLinkButton`, `ShareRevokeControl` and `OwnerUnpublishedNotice` are unchanged. page.tsx passes `shareNote={recipientShareNote("mint-token", recipientArm(state))}` on the owner pending mount only (the owner pending lane is reachable only for an unpublished strategy, so the mode is always mint-token, UI-SPEC § S7).
    page.stale-analytics.test.tsx: MOVE V2's positive `still computing` anchor to the KCS10 sentence and V2b's two positives to the KCS10 sentence; KEEP every negative; ADD negatives for "Some strategies stay in this state", "few minutes" and "still computing". Rename V2's title to say it falls to the neutral public placeholder; leave V2b's title untouched. Each moved assertion carries a KCS-10 note.
    page.owner-compute-state.test.tsx: add SHARE-NOTE-A / -B / -UNREADABLE cases and a FULL-RENDER-NO-NOTE case.
    Neuter check (KCS-16): restore the old public copy -> V2 RED; restore GREEN; record.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/" src/__tests__/phase-148-owner-lane-cache-isolation.test.ts --reporter=verbose > ${KCS_SP:?KCS_SP_unset}/kcs-07-t3.log 2>&1; RC=$?; tail -60 ${KCS_SP:?KCS_SP_unset}/kcs-07-t3.log; test "$RC" = 0 && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${KCS_SP:?KCS_SP_unset}/kcs-07-t3.log && echo OK && npm run typecheck && npx eslint "src/app/factsheet/[id]/v2/page.tsx" "src/app/factsheet/[id]/v2/FactsheetView.tsx" && npm run check:planning-hygiene</automated>
    <fails_when>no OK line is printed (a criterion-9 negative fires, FactsheetView.share-affordance tests go red, or owner-lane / phase-148 guards go red), tsc reports an error, eslint reports an error, or check:planning-hygiene exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --stat "$(git merge-base HEAD main)"..HEAD -- "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" src/__tests__/phase-148-owner-lane-cache-isolation.test.ts` still prints nothing.
    - The V2b test title line in page.stale-analytics.test.tsx is byte-identical to the phase base (`git merge-base HEAD main`).
    - `grep -c "shareNote" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` is at least 2.
    - No new test file line contains a UUID other than synthetic `00000000-0000-4000-8000-…` ids (`grep -nE '[0-9a-f]{8}-[0-9a-f]{4}-' "src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx" | grep -v '00000000-0000-4000-8000'` prints nothing).
    - The SUMMARY records the neuter run RED and restore GREEN.
  </acceptance_criteria>
  <done>The public reader is told only what is true in every state; the owner is told what their link shows right now.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner session -> get_user_compute_jobs (SECDEF, auth.uid()-scoped, last_error NULL) | a new owner read on the pending render |
| owner session -> strategy_keys (RLS strategy_keys_owner) | a new head count on the pending render |
| per-request owner facts -> id-keyed public cache | the D-04 one-way boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.2-07-01 | Information disclosure | owner job state cached into the public payload | high | mitigate | lane-local variable, JSX-only, inside the owner `!payload` branch; phase-148 + owner-lane guards unedited and green; zero unstable_cache calls asserted |
| T-167.2-07-02 | Information disclosure | raw exception text shown to the owner | medium | mitigate | the RPC nulls last_error; the copy table has no slot for it; ASVS V7 |
| T-167.2-07-03 | Information disclosure | internal state named on the public lane | medium | mitigate | the public lane reads nothing and renders one neutral sentence; criterion-9 negatives kept and extended |
| T-167.2-07-04 | Repudiation | a failed compute described as in progress | high | mitigate | TRIGGER-SHAPE test; unreadable degrade never claims progress |
| T-167.2-07-05 | Spoofing | a non-owner reaching the owner branch | medium | accept | unchanged: the owner lane is gated on the session-keyed Lane B probe and the row's status (owner-lane tests 7-9 unedited) |
</threat_model>

<verification>
- `npx vitest run "src/app/factsheet/[id]/v2/" src/__tests__/phase-148-owner-lane-cache-isolation.test.ts` green; `npm run typecheck` 0; eslint 0; `npm run check:planning-hygiene` 0.
- `git diff --stat "$(git merge-base HEAD main)"..HEAD -- supabase/` prints nothing.
- Schema-push gate: not applicable.
</verification>

<success_criteria>
- A reader of the owner pending page can answer "is anything happening, and do I need to do something?" in one read.
- The D-04 cache fences are unedited and green.
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|---|---|---|
| owner compute-state block (`<section aria-label="Computation status">`) | JSX in the owner `!payload` branch | src/app/factsheet/[id]/v2/page.tsx |
| Lane B select `source, api_key_id` | select widening (Lane B only) | src/app/factsheet/[id]/v2/page.tsx |
| `shareNote` | optional prop on `OwnerUnpublishedPanel` | src/app/factsheet/[id]/v2/FactsheetView.tsx |
| owner pending lane tests | new test file | src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx |

<output>
Create `.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-07-SUMMARY.md` when done
</output>
