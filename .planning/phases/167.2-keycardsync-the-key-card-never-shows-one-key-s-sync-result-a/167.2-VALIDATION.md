---
phase: "167.2"
slug: "keycardsync-the-key-card-never-shows-one-key-s-sync-result-a"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-23"
---

# Phase 167.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest + @testing-library/react (jsdom per-file where declared) |
| **Config file** | `vitest.config.ts` (coverage ratchet: read thresholds there, never from prose) |
| **Quick run command** | `npx vitest run <touched test files>` |
| **Full suite command** | `npm test` then `npx tsc --noEmit` then `npx eslint <changed files>` |
| **Estimated runtime** | ~3 seconds (quick set); full suite longer |

---

## Sampling Rate

- **After every task commit:** Run the quick run over the files that task touched, plus `npx tsc --noEmit`
- **After every plan wave:** Run the 13 baseline files named in RESEARCH plus the new ones
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

Filled by the planner per task. The decision → test map lives in `167.2-RESEARCH.md`
§Validation Architecture; the KCS-18..KCS-23 amendments in CONTEXT add: a warned-row terminal
held while a chain job is in flight (KCS-18, `ApiKeyManager.poll.test.tsx`), `done_pending_children`
→ queued (KCS-19, `compute-state.test.ts`), chain-kind selection in the route (KCS-20,
`sync-progress/route.test.ts`), the shape-keyed remedy table (KCS-21), the non-failure cap line
(KCS-22, `SyncProgress.poll.test.tsx`), and no link control on a composite card (KCS-23,
`ApiKeyManager.test.tsx`).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (planner fills) | | | KCS-nn | | | | | | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/lib/compute-state.ts` + `src/lib/compute-state.test.ts`
- [ ] Extend the strategies-page test doubles with the `strategy_keys` / `api_keys` / `strategy_analytics` embed and an `rpc` stub, keeping their unexpected-table throws
- [ ] New owner-state page test with its own request mock that has `rpc`; the existing owner-lane test stays unedited
- [ ] `ApiKeyManager.poll.test.tsx` mock: add `maybeSingle` for the baseline read

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Owner pending factsheet and share page read clearly on a real failed composite | KCS-09, KCS-11 | Copy judgement against the founder's words | After deploy, open the owner factsheet and the share link of a strategy whose first compute failed; neither says the data is being computed |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
