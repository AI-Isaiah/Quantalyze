---
phase: "167.2"
slug: "keycardsync-the-key-card-never-shows-one-key-s-sync-result-a"
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
block_on: high
created: "2026-09-24"
audited_at_sha: 44719729d9e9ff45feaab0f18674819677cbef95
diff_base_sha: 4d9f823ba6425578762f6b51727ced21fd833024
---

# Phase 167.2 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

**Scope.** The audit covers the `<threat_model>` blocks of all 10 plans (47 threats) and the new
surface added by the two review-fix rounds (`167.2-REVIEW-FIX.md`, `167.2-REVIEW-FIX-R2.md`).
Every verdict is bound to `audited_at_sha`. The phase diff is `diff_base_sha...audited_at_sha`,
restricted to `src/`: 37 files, 19 of them non-test source.

**Config.** `.planning/config.json` sets neither `workflow.security_asvs_level` nor
`workflow.security_block_on`, so the workflow defaults apply: ASVS L1 and `block_on: high`.
The high-severity threats were also traced to the correct boundary (L2 depth), because the
share page and the owner lane are public-facing.

**No migration (KCS-15): confirmed.** 0 files under `supabase/` changed between `diff_base_sha`
and `audited_at_sha`, and 0 under `supabase/migrations/`. Every new read uses an existing
policy or RPC: `strategy_keys_owner`, `api_keys_owner`, `analytics_read`, and
`get_user_compute_jobs`, which is SECURITY DEFINER, `auth.uid()`-scoped and nulls `last_error`.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| anonymous recipient → `/factsheet-share/[token]` | sessionless; the constant-time HMAC match (`findShareMatch`) is the only authorisation | token (capability) |
| share page (service-role admin client) → `compute_jobs`, `strategy_keys` | new reads on a public route, bounded by the matched strategy id | five job fields; one head count |
| owner browser → `/api/strategies/[id]/sync-progress` | owner-scoped, rate-limited (`syncProgressLimiter`) projection route | whitelisted `{jobStatus, stalled, memberProgress}` plus a closed `degraded` / `degradedReason` |
| owner session → `get_user_compute_jobs` (SECDEF) | owner job-state reads on the owner factsheet, `/strategies`, the edit page and the route | redacted job rows (server side only) |
| owner session → `strategy_keys` / `api_keys` / `strategy_analytics` / `strategies` (RLS) | head counts, member-key ids, key-status columns, `computed_at` baselines, the link write | owner-scoped rows |
| per-request owner facts → id-keyed public cache | the D-04 one-way boundary (`buildFactsheetPayloadCached`) | nothing may cross |
| server errors → Sentry / logs / DOM | captures, console lines, rendered copy | error text (internal) |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation / Evidence | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-167.2-01-01 | Information disclosure | sync-progress projection | medium | mitigate | the route builds `body` from `jobStatus`, `stalled` and `memberProgressOf` only. The added `api_key_id` select feeds `shouldPreferStitch` and never reaches the body. Pinned by `WIDEN-REDACTION` and the exact-key whitelist assertions in `route.test.ts`. | closed |
| T-167.2-01-02 | Tampering | derivation input coercion | low | mitigate | `isComputeJobStatus` (closed `COMPUTE_JOB_STATUSES`), `jobPhaseOf` (unknown kind → `"unknown"`), `coerceJobErrorKind` (closed `JOB_ERROR_KINDS`, else null) in `compute-state.ts` | closed |
| T-167.2-01-03 | Spoofing | route ownership fence | medium | mitigate | `isUuid` → `checkLimit(syncProgressLimiter)` → `.eq("user_id", user.id).single()` 404. The phase diff changes none of those three lines (0 matching `+`/`-` lines). | closed |
| T-167.2-01-04 | Denial of service | raised `p_limit` | low | accept | see Accepted Risks AR-01 | closed |
| T-167.2-01-05 | Repudiation | false "running" to the owner | medium | mitigate | `deriveComputeState({ readError: true })` gives unreadable. The route answers DEGRADED, never IDLE, for an error, a non-array answer, a full window and a bad status (the `degrade` helper). | closed |
| T-167.2-01-06 | Tampering | unreadable count treated as single | high | mitigate | `countCompositeMembers` returns `ok:false` on an error, a null count or a throw. `resolveStrategyShape` maps `!memberCount.ok` to `"unknown"`. L2: the edit page takes that result before it renders any control. | closed |
| T-167.2-01-07 | Information disclosure | `strategy_keys` count | low | accept | see AR-02 | closed |
| T-167.2-02-01 | Tampering | overlapping add + resync re-link | high | mitigate | the handlers guard with refs, not only with disabled buttons. `handleAddKey` returns early on `loading \|\| addInFlightRef.current \|\| attemptRef.current !== null`, and `handleSyncTrades` on `addInFlightRef.current \|\| attemptRef.current !== null`. L2: both guards sit inside the handlers, which is the write boundary. | closed |
| T-167.2-02-02 | Repudiation | post-add failure console-only | medium | mitigate | the post-add sync runs as the tracked attempt (the KCS-01 block in `handleAddKey` → `handleSyncTrades`) | closed |
| T-167.2-02-03 | Information disclosure | key label in the blocked line | low | mitigate | rendered as React text. `dangerouslySetInnerHTML` appears in 0 of the 19 phase source files. | closed |
| T-167.2-02-04 | Denial of service | duplicate validate-and-encrypt | low | mitigate | the `loading` guard (NEW-C37-02) is kept in the same early return as the live-attempt check | closed |
| T-167.2-03-01 | Repudiation | a previous run's terminal shown as this attempt's result | high | mitigate | the `SyncProgress` evidence gate. A terminal is forwarded only when `sawComputingRef` is set or `computed_at` differs from `evidenceBaseline`. The panel is keyed by `attemptSeq`. `STALE-TERMINAL` and `FAST-FAIL` are present. | closed |
| T-167.2-03-02 | Tampering | client clock forging evidence | medium | mitigate | the gate compares two server-written `computed_at` values, with no `Date.now` in the comparison | closed |
| T-167.2-03-04 | Information disclosure | new baseline reads | low | accept | see AR-03 | closed |
| T-167.2-03-05 | Denial of service | unbounded baseline read | medium | mitigate | `BASELINE_READ_BOUND_MS` race. On expiry the baseline is unknown and the attempt continues (captured, stage `evidence-baseline`). | closed |
| T-167.2-04-01 | Tampering | Retry during a live `failed_retry` | medium | mitigate | the Retry button renders only under `syncStatus === "error"`, so `no_result` and `unconfirmed` render no Retry. Round 1 also gates every attempt through `readChainJobState` (fail closed). | closed |
| T-167.2-04-02 | Information disclosure | `computation_error` shown to the owner | low | accept | see AR-04 | closed |
| T-167.2-04-03 | Repudiation | give-up rendered as a failure | medium | mitigate | the retired timeout sentence has 0 hits in `ApiKeyManager.tsx`, `SyncProgress.tsx` and `key-card-copy.ts`. `CAP-GIVEUP` and the `NOROW*` pins are present. | closed |
| T-167.2-04-04 | Repudiation | "nothing recorded yet" after an error or after rows | medium | mitigate | `useStrategySyncPoller` escalates `missing_row` only when `!sawRow && cleanRead`. `TRANSIENT-ERROR-AFTER-ROWS` and `NOROW-NOT-ON-ERROR` are present. | closed |
| T-167.2-05-01 | Denial of service | hung link or enqueue | medium | mitigate | the `LINK_UPDATE_BOUND_MS` and `ENQUEUE_BOUND_MS` races each end through `endAttempt`. `LINK-HANG` and `ENQUEUE-HANG` are present. | closed |
| T-167.2-05-02 | Tampering | late 202 starting a poll | medium | mitigate | an `attemptRef.current !== attempt` guard follows every post-await step in `handleSyncTrades`. `LATE-202` is present. | closed |
| T-167.2-05-03 | Repudiation | bound expiry reported as a failure | medium | mitigate | an expiry sets `unconfirmed` with a stop reason and no Retry. Only a late `verdict` answer (R2-M1 `EnqueueAnswerError`) replaces the panel. | closed |
| T-167.2-06-01 | Tampering | re-linking a composite's `api_key_id` | high | mitigate | `linkControlsAllowed = keyShape === "single"`. The controls render only under it. `handleLinkKey` throws, and `handleAddKey` / `handleSyncTrades` return early, when it is false. L2: the guard is in the only function that writes `strategies.api_key_id`. `COMPOSITE-NO-LINK` is present. | closed |
| T-167.2-06-02 | Tampering | unreadable count treated as single | high | mitigate | the edit page sets `keyShape = "unknown"` on `!members.ok`, and the history cross-check (`readOwnerCompositeHistory`) also fails closed to `"unknown"`. `COUNT-FAILS` and `SHAPE-UNKNOWN-REMEDY` are present. | closed |
| T-167.2-06-03 | Repudiation | withheld success resurrected cross-tab | medium | mitigate | `retireSuccessIf` runs at the applied re-read. `CROSS-TAB-HEAL`, `CROSS-TAB-DELETE` and `LATER-UNTRUST` are present. | closed |
| T-167.2-06-04 | Information disclosure | count-read error text to the client | low | mitigate | the edit page logs `members.message` server-side and passes only `keyShape` and `compositeMemberKeyIds` to the client component | closed |
| T-167.2-06-05 | Denial of service | unreadable count hiding the remedy | medium | mitigate | `readCompositeMemberKeyIds` never throws. The page renders `unknown` and does not fail. `SHAPE-UNKNOWN-REMEDY` is present. | closed |
| T-167.2-07-01 | Information disclosure | owner job state cached into the public payload | high | mitigate | `readOwnerPendingStatus` runs only when `lane === "owner"` inside `!payload`, on the request client. Its result is a lane-local value handed to JSX only. The phase adds 0 `unstable_cache(` lines. `phase-148-owner-lane-cache-isolation.test.ts` and `page.owner-lane.test.tsx` are unedited in the phase diff. L2: the value never reaches `fetchAndBuildPayload` or `buildFactsheetPayloadCached`. | closed |
| T-167.2-07-02 | Information disclosure | raw exception text to the owner | medium | mitigate | the RPC nulls `last_error`. A code sweep finds 0 non-comment `last_error` references in the 19 phase source files. The owner copy renders authored strings only. | closed |
| T-167.2-07-03 | Information disclosure | internal state on the public lane | medium | mitigate | the public lane reads nothing and renders `KCS10_PUBLIC_SENTENCE`. The discovery fallback now renders the same constant (review round 1, WR-01). | closed |
| T-167.2-07-04 | Repudiation | failed compute described as in progress | high | mitigate | `deriveComputeState` maps `failed_final` to failed and a read error to unreadable, and neither is an in-progress arm. `TRIGGER-SHAPE` is present (2 files). | closed |
| T-167.2-07-05 | Spoofing | non-owner reaching the owner branch | medium | accept | see AR-05 | closed |
| T-167.2-08-01 | Information disclosure | share page widened to arbitrary data | high | mitigate | `readShareComputeState` runs `.eq("strategy_id", strategyId)`, where `strategyId` is `match.strategy_id`. It is called only after `findShareMatch` and only inside `!payload`. `BOUND-PIN` asserts that exactly one eq pair is used. The kind `.in` filter from round 1 (`KIND-FILTER-PIN`) narrows the read further and does not widen it. L2: the call site is after the rate limit, the format guard and the HMAC match. | closed |
| T-167.2-08-02 | Information disclosure | job metadata or `last_error` reaching the page | high | mitigate | the select string is the five-field projection, with the heartbeat lifted through the JSON-path alias. `PROJECTION-PIN` asserts the exact string. | closed |
| T-167.2-08-03 | Information disclosure | recipient learning the failure cause | medium | mitigate | the recipient sees only `SHARE_CARD_COPY.in_progress` or `.not_available`, which are two authored sentences. Nothing from the job rows is interpolated. The `NEVER_ON_THE_CARD` negatives cover `failed`, `error`, `computed`, `minutes`, `permanent`, `stitch` and `stalled` (164.2 criterion 9). | closed |
| T-167.2-08-04 | Tampering / Information disclosure | recipient-dependent cache poisoning | high | mitigate | the share module names no cache primitive (a `SOURCE` not-contains `unstable_cache` assertion). `page.cache-isolation.test.tsx` and `page.no-cache-reach.test.ts` are unedited in the phase diff. `force-dynamic` is kept. | closed |
| T-167.2-08-05 | Denial of service | extra read on a public route | low | accept | see AR-06 | closed |
| T-167.2-08-06 | Information disclosure | server error text logged or rendered | low | mitigate | the console lines carry `{strategyId, message}` server-side only. The capture carries tags only. The render uses authored copy only. | closed |
| T-167.2-09-01 | Information disclosure | `api_keys` wide select | high | mitigate | `/strategies` uses `KEY_STATUS_COLUMNS`, an allowlist of five columns. The card uses `API_KEY_USER_COLUMNS`. No `select("*")` targets `api_keys` in the phase files. The one `select("*")` in a phase file is a head count on `strategy_keys`, and the edit page's `strategies` select is not in the phase diff. | closed |
| T-167.2-09-02 | Information disclosure | another owner's keys or jobs | medium | mitigate | the request client (`createClient` from `@/lib/supabase/server`) is used throughout `/strategies`, and the list read carries `.eq("user_id", user.id)`. The RPC is `auth.uid()`-scoped. `/strategies` has no admin client. | closed |
| T-167.2-09-03 | Repudiation | failed read shown as healthy or in progress | medium | mitigate | a `keysError` or `membersError` read sets `keyStatusUnreadable`, which renders `KEY_STATUS_UNREADABLE_NOTE`. An RPC error folds to unreadable. A list error or `data: null` renders `STRATEGIES_LIST_UNREADABLE` (rounds 1 and 2, H-1 and IN-03). | closed |
| T-167.2-09-04 | Denial of service | N per-row RPCs | low | accept | see AR-07 | closed |
| T-167.2-09-05 | Tampering | causal claim about the strategy's numbers | low | mitigate | the pill and the caption name the key and its state. No metric is rendered from the new reads. | closed |
| T-167.2-10-01 | Repudiation | mid-chain warned row shown as final | high | mitigate | `SyncProgress` forwards a success only after `readChainJobState` returns `settled`, which requires `isFactsheetJobInFlight` to be false. `WARNED-ROW-MID-CHAIN` and `WARNED-ROW-KEYCARD` are present. | closed |
| T-167.2-10-02 | Repudiation | unreadable job state read as "nothing in flight" | medium | mitigate | `readChainJobState` returns `unreadable` on a non-ok response, a non-object body, `degraded`, a bad `jobStatus` type, a throw, or the `CHAIN_JOB_READ_BOUND_MS` race. `DEGRADED-HOLDS` and `GATE-BOUND` are present. | closed |
| T-167.2-10-03 | Denial of service | extra sync-progress reads | low | accept | see AR-08 | closed |
| T-167.2-10-04 | Information disclosure | new read widens the browser's view | low | accept | see AR-09 | closed |

*Status: open · closed · open — below {block_on} threshold (non-blocking)*
*Severity: critical > high > medium > low. Only open threats at or above workflow.security_block_on count toward threats_open.*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Surfaces the brief named, and what was verified

| Surface | Verdict |
|---------|---------|
| Share page admin `compute_jobs` read | Bounded to the matched id and projects five fields (T-08-01 and T-08-02). The one output is the choice between two neutral sentences, so criterion 9 holds (T-08-03). |
| Share page admin `strategy_keys` head count (review round 1, IN-03) | This read is not in the register (see UF-01). It is `countCompositeMembers` with `head: true` and `.eq("strategy_id", …)` on the same matched id, so no row and no column leaves the database. Its only effect is the `preferStitch` input. |
| Owner lane: `readOwnerPendingStatus`, `countCompositeMembers`, `readOwnerCompositeHistory` | All run on the request client. The owner factsheet has no `createAdminClient` call. |
| Delete-confirm `strategy_keys` membership read | Runs on the browser client, which is RLS-scoped. `strategy_keys_owner` is `USING (owner_id = auth.uid())`, and `enforce_strategy_keys_owner_coherence` ties a member key's owner to its strategy's owner. So the embedded composite names are the owner's own strategies. They are rendered as React text. |
| `/api/strategies/[id]/sync-progress` | The fence order is unchanged and the limiter is unchanged (0 diff lines in `src/lib/ratelimit.ts`). `api_key_id` is selected but never emitted. `degradedReason` is a closed union (`window_full` \| `bad_status`). |
| Sentry captures | 29 `captureToSentry(` sites in the phase source files, 27 of them added by the phase. 0 carry an id in their message or tag arguments (a grep-level check). As round 2 IN-01 corrected, breadcrumbs, page URLs and a server capture's request URL can still carry non-secret, owner-internal UUIDs. See UF-04. |
| Internal cause or raw error text to public or share viewers | None. The share page and the public lanes render authored constants only (T-07-03, T-08-03, T-08-06). |
| Cache isolation | 0 `unstable_cache(` lines added. The four cache-isolation and owner-lane test files are unedited in the phase diff. |
| KCS-15 no migration | Confirmed: 0 `supabase/` files changed. |

---

## Unregistered Flags (review-fix surface with no plan-time threat ID)

These are warnings, not blockers. The `## Threat Flags` sections of all 10 SUMMARYs map to
registered IDs or report none. The items below entered in the review-fix rounds.

| Flag | Surface | Assessment |
|------|---------|------------|
| UF-01 | share page: admin `strategy_keys` head count (round 1, IN-03) | Mitigated in code: the count is head-only and bounded by the matched id. ⚠️ Documentation drift: the module's SECURITY BOUNDARY header still says "Three reads happen here", but there are now four. That header is the file's own guard against unbounded queries, so it should name the fourth read. Also, the T-01-07 accept rationale says callers pass the RLS-scoped request client. This caller passes the service-role client (see AR-02). |
| UF-02 | Delete-confirm membership read with an embedded composite name and status (round 2, WR-02, founder decision) | Closed at L1: the read is owner-scoped by RLS, bounded (`BASELINE_READ_BOUND_MS`), rendered as escaped text, and a failure is captured with tags only. |
| UF-03 | R2-L4 (b) residual: a regressed `strategy_keys_owner` answers `[]`, so the confirm shows no warning, and the FK cascade then removes the key from a draft composite | The residual has a precondition (an RLS regression, which is itself a defect), and the database's publish guard still protects a published composite. **Routing: open at `audited_at_sha`, closed in the working tree.** At `audited_at_sha`, the ROADMAP's Phase 167.2.1 entry (FACTSHEETBUILDABLE) has 0 mentions of this residual. During the audit, an uncommitted orchestrator edit added it to 167.2.1 as success criterion (5) and put a dated correction in `deferred-items.md`. The routing holds once that edit is committed. |
| UF-04 | Sentry breadcrumbs and URLs carry owner-internal UUIDs (round 2, IN-01) | Recorded as corrected rather than scrubbed (autonomous decision, round 2). The ids are not credentials, and message and tags stay id-free. A UUID breadcrumb scrub would be an instrumentation-wide change outside this phase. |
| UF-05 | More reads on owner routes: the round-1 pre-attempt gate (`readChainJobState` before every attempt), the round-1 failure-arm read (WR-03), and the `COMPUTE_STATE_READ_LIMIT_MAX` re-ask in `readOwnerComputeJobs` / `readOwnerCompositeHistory` | Still behind the unchanged `syncProgressLimiter` (60 / 60 s per user and strategy). The panel keeps one in-flight guard (`jobCheckInFlightRef`), and each re-ask is a single bounded call. This widens the load assumptions behind AR-01, AR-07 and AR-08, but it does not break them. |

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-01 | T-167.2-01-04 | The RPC caps at 1000 and is `auth.uid()`-scoped. `syncProgressLimiter` (60 / 60 s) bounds the call rate. Since review round 1 the route can re-ask once at the 1000 cap (UF-05), which is still inside the RPC's own cap. | plan 167.2-01 threat model; re-verified by this audit | 2026-09-24 |
| AR-02 | T-167.2-01-07 | A head count only, with no rows. Correction: the share page calls `countCompositeMembers` on the service-role client, not the request client. It stays bounded by the HMAC-matched id and returns only a count (UF-01). | plan 167.2-01; re-verified | 2026-09-24 |
| AR-03 | T-167.2-03-04 | The baseline selects `computed_at` only, under `analytics_read`. The sync-progress projection is unchanged in shape. | plan 167.2-03 | 2026-09-24 |
| AR-04 | T-167.2-04-02 | `computation_error` is owner-readable, server-scrubbed, and rendered as escaped React text. Raw `last_error` is never read. | plan 167.2-04 | 2026-09-24 |
| AR-05 | T-167.2-07-05 | The owner lane stays gated on the session-keyed Lane B probe and the row status. `page.owner-lane.test.tsx` is unedited in the phase diff. | plan 167.2-07 | 2026-09-24 |
| AR-06 | T-167.2-08-05 | The read runs only on a valid token with a pending payload, after `publicIpLimiter` (10 / 60 s per IP). It returns up to 100 rows and five columns, plus one head count. | plan 167.2-08 | 2026-09-24 |
| AR-07 | T-167.2-09-04 | Only uncomputed and unpublished rows call the RPC (round 1, IN-04, skips published rows). Each call is 100 rows, with one re-ask at the cap in the rare full-window case. The page is server-rendered. | plan 167.2-09 | 2026-09-24 |
| AR-08 | T-167.2-10-03 | One read at a time (`ONE-IN-FLIGHT`), plus one gate read per user-initiated attempt. Both fall under the 60 / min `syncProgressLimiter`. | plan 167.2-10 | 2026-09-24 |
| AR-09 | T-167.2-10-04 | The route is the existing whitelisted owner projection. The client reads only `jobStatus`, `degraded` and `degradedReason` from it. | plan 167.2-10 | 2026-09-24 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-09-24 | 47 (38 mitigate, 9 accept, 0 transfer) | 47 | 0 (0 blocking, 0 non-blocking) | gsd-security-auditor, at `audited_at_sha` |

**Method.** No DB command was run. Each mitigation was located by symbol in the implementation.
Each cited test pin was confirmed present by name in the test tree, and every pin cited above has
at least 1 hit. The audit did not run the tests. Green status for them is taken from the
review-fix reports' recorded runs, and this audit does not claim it. Proof-of-absence checks are
recorded as counts only.

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-09-24. Two follow-ups are non-blocking: UF-01 (header drift), and UF-03 (routing closes when the orchestrator's pending ROADMAP edit is committed).
