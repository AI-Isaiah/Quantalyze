# Phase 167.2: KEYCARDSYNC - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-23
**Phase:** 167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a
**Mode:** `--auto` (founder asleep). Every area auto-selected; every choice taken from repo evidence.
**Areas discussed:** Post-add sync slot, Stale terminal evidence, Unbounded enqueue, Cross-tab withheld success, List pill, Compute-state derivation, Owner pending factsheet, Public placeholder, Share page arms, Share link before first compute

---

## Post-add sync slot (residual 1)

| Option | Description | Selected |
|--------|-------------|----------|
| Disable Add Key while an attempt is live; post-add sync becomes the tracked attempt otherwise | Prevents the overlap, same shape as 167 R4/R5 | ✓ |
| Queue the post-add sync behind the live attempt | Needs a queue; invisible if the user leaves | |
| Keep the parallel sync, report its failure on the new key's card | Two concurrent attempts, two subjects | |

**Choice:** [auto] option 1. **Notes:** SEAMUX-05 pins move deliberately (ROADMAP names the cost).

## Stale terminal evidence (residual 3)

| Option | Description | Selected |
|--------|-------------|----------|
| Seen `computing` OR `computed_at` changed vs pre-enqueue read | ROADMAP's named fix, server values only | ✓ |
| Client-clock comparison against `computed_at` | Forbidden by the ROADMAP | |

**Choice:** [auto] option 1, with a researcher measurement of the fast-failure case and a third
arm from `get_user_compute_jobs` if (a)+(b) cannot end it.

## Unbounded enqueue (residual 4)

| Option | Description | Selected |
|--------|-------------|----------|
| Client-side bound on link + enqueue, honest "could not confirm" copy | Below maxDuration 300, above measured slow enqueues | ✓ |
| Leave to the platform's maxDuration | Status quo, up to 300 s spinner | |

## Cross-tab withheld success (residual 2)

| Option | Description | Selected |
|--------|-------------|----------|
| Retire at the moment of withholding | ROADMAP's named redesign of R2 | ✓ |
| Re-check trust on every re-read | Still re-shows if the other tab fixed the key | |

## List pill (167 D-19)

| Option | Description | Selected |
|--------|-------------|----------|
| Mark when ANY feeding key (api_key_id + strategy_keys) is untrusted, reuse existing pill | Covers composites | ✓ |
| Mark only on `api_key_id` | Misses composites | |

## Compute-state derivation

| Option | Description | Selected |
|--------|-------------|----------|
| One pure derivation shared by route, owner page, share page, list | One job-selection rule | ✓ |
| Per-surface reads | Divergent rules | |

## Owner pending factsheet

| Option | Description | Selected |
|--------|-------------|----------|
| Request-scoped job read in the owner branch, authored copy per state and error_kind | D-04-safe | ✓ |
| Put state in the payload | Forbidden by 167 D-04 (one-way leak) | |

## Public placeholder

| Option | Description | Selected |
|--------|-------------|----------|
| One neutral sentence true in every state | Stops the false "still computing" claim | ✓ |
| Leave unchanged | Keeps a false promise on the public lane | |

## Share page arms

| Option | Description | Selected |
|--------|-------------|----------|
| Two neutral arms: in progress / not available yet; unreadable falls to the second | Criterion 9 + no false promise | ✓ |
| Full state names to the recipient | Violates criterion 9 | |

## Share link before first compute (the ROADMAP's open question)

| Option | Description | Selected |
|--------|-------------|----------|
| Keep allowed; owner affordance says what recipients see | Phase 164 design intent, reversible, fixes the named copy defect | ✓ |
| Refuse mint until the first successful compute | New route gate; does not cover computed-then-failed | |

**Choice:** [auto] option 1; evidence in CONTEXT KCS-12.

## Claude's Discretion

Copy wording within DESIGN.md; the enqueue bound's value; whether the owner state block polls;
which existing pill component the list reuses.

## Deferred Ideas

Refusing early share links; a non-stitch stall signal; state-aware copy for an owner on the
published lane. None is a data-integrity or user-facing defect.
