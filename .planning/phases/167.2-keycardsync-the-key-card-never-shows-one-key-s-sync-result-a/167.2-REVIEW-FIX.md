---
phase: 167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a
fixed_at: 2026-09-24T07:00:00Z
review_path: .planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-REVIEW.md
review_sfh_path: .planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-REVIEW-SFH.md
iteration: 1
base_sha: e34f35892
findings_in_scope: 27
fixed: 26
skipped: 1
status: partial
---

# Phase 167.2: Code Review Fix Report (round 1)

**Fixed at:** 2026-09-24
**Source reviews:** `167.2-REVIEW.md` (CR-01, CR-02, WR-01..WR-07, IN-01..IN-04) and
`167.2-REVIEW-SFH.md` (H-1..H-3, M-1..M-7, L-1..L-4)
**Iteration:** 1 (autonomous: the founder was asleep, so the orchestrator took the decisions below)

**Summary:**
- Findings in scope: 27 (fix scope "all", every severity)
- Fixed: 26, in 20 commits on `feat/167.2-keycardsync`, from `e34f35892` to `538668fb8`. None are pushed.
- Skipped: 1. WR-02 is UNFIXABLE-IN-PHASE; the exact reason is below.

**Method.** Test-first. Every behaviour fix got a test that was run and observed RED before the
change. Every new guard was then neutered in place, observed RED, restored from a `cp` byte
backup, and checked with `cmp`. The neutered and restored guards are CR-01/WR-06 (the gate),
CR-02, M-7, WR-05, WR-03, M-2, M-3, WR-04, M-1, IN-02 and IN-03. IN-01's guard is the lint:
routing `loadKeys` back through the render-scoped wrapper was seen to raise the exhaustive-deps
warning. No `git checkout --`, restore or stash was used. The `node_modules` symlink was removed
before every commit. `check-planning-hygiene` printed OK after `git add`, before each commit.
Every commit message was swept for the CI skip token (0 hits).

**Where verification ran.** In the phase's dedicated checkout (branch `feat/167.2-keycardsync`),
with `node_modules` symlinked from the main checkout. This fixer created no GSD worktree, because
the orchestrator named this checkout as the only place to work. The numbers below can be
reproduced from this branch's tree.

## Final gates (run at `538668fb8`)

| Gate | Result |
|---|---|
| `npx tsc --noEmit` | exit 0 |
| `eslint` on the 28 touched `.ts`/`.tsx` files | exit 0, 0 problems |
| `vitest run src/components/strategy src/hooks 'src/app/(dashboard)/strategies' src/app/factsheet src/app/factsheet-share src/lib 'src/app/(dashboard)/discovery' src/app/api/strategies` | **355 files passed; 6969 tests passed, 9 skipped, 0 failed**. Baseline at `e34f35892` was 354 files and 6902 passed, 9 skipped. |

⚠️ This is outside the gate and unrelated to this round: `src/__tests__` (which the gate does
not cover) was also run once. It showed 1 failure per run, a DIFFERENT shell-script test each time
(`lint-sql-gates`, `test-restore-workflow-wiring`, a Railway exit-code test), each at about 5 to
7 s, with load average around 10. That is the timeout-shaped flake class, and none of those
files is touched here. They are not claimed as passing.

## Orchestrator decisions taken (autonomous round, 2026-09-24)

Each decision is recorded here. Each new or changed string is also a dated row in
`167.2-UI-SPEC.md` § "Review-fix amendments (2026-09-24, round 1, autonomous)".

| Finding | Decision taken |
|---|---|
| CR-02 | Filter, don't re-word. The edit page reads the composite's `strategy_keys` member key ids (RLS `strategy_keys_owner`, no migration), and the card lists only those. The locked KCS23-COMPOSITE string is kept and becomes true. |
| CR-01 + WR-06 | Before every attempt (Resync, Use & Sync, the post-add sync, the panel's Retry), read the sync-progress projection. If a chain job is in flight, or the read is unreadable, refuse before the link and the enqueue (fail closed), with authored lines KCS-GATE-INFLIGHT / KCS-GATE-UNREADABLE. The WR-02 pin was moved with a lineage comment. |
| WR-01 | The discovery fallback renders `KCS10_PUBLIC_SENTENCE`. |
| WR-02 | Not decidable owner-side without a migration or an admin read, so it is recorded as UNFIXABLE-IN-PHASE (below). |
| WR-03 | An evidenced failure is forwarded only when no chain job is in flight (the same read as KCS-18). |
| WR-04 | `api_key_id` is read back before the enqueue. A mismatch, or an unreadable read, refuses the attempt (`unconfirmed`, KCS-LINK-UNVERIFIED). |
| WR-05 | **REFUSE, on every card, and do not hide.** A Delete on a key that is a member of ANY composite is refused with KCS-DELETE-COMPOSITE, and a membership read that fails also refuses. **Why this and not "hide Delete on the composite card":** a single-key card lists EVERY key the owner has, so hiding Delete on the composite card alone would leave the same cascade one click away on any other card. Delete stays visible, so KCS-23's "`Delete` behaves as before" still holds for every non-member key. |
| WR-07 | The false duration hints are removed. After 60 s the panel shows KCS-SLOW, which states the panel's own limit and no duration for the sync (consistent with RESEARCH P2). |
| H-1 / H-3 / M-6 | An errored read is never shown as "healthy" or "empty". Authored lines KCS-LIST-UNREADABLE / KCS-KEYSTATUS-UNREADABLE are added, and failures go to `captureToSentry` with **tags only: no key id and no strategy id in any capture**. The existing capture site sends neither, and one test asserts it. Ids stay in the server and console logs. ⛔ **CORRECTED 2026-09-24 (round 2, 167.2-REVIEW-R2 IN-01):** that claim was broader than the code guarantees. What holds is narrower: no id appears in any capture's **message or tags**. The Sentry EVENT around a capture can still carry non-secret, owner-internal ids in three places. The client's console breadcrumbs do, because the neighbouring `console.error` / `console.warn` lines embed `[strategy_id=…]` / `[key_id=…]`. The fetch breadcrumb and page URL do (`/api/strategies/<id>/sync-progress`, `/strategies/<id>/edit`). So does a server capture's request URL. `scrubSentryEvent` scrubs share-token shapes, not UUIDs. Decision (autonomous, round 2): correct the claim rather than scrub. The ids are not secret, and a UUID scrub of every breadcrumb is an instrumentation-wide change outside this phase. The sentence above is kept as lineage. |
| H-2 | The `unknown` shape shows KCS-SHAPE-UNKNOWN and is captured (`stage: "strategy-shape"`), like `readOwnerPendingStatus`. The empty state on a card with no Add Key becomes KCS-EMPTY-NOLINK. |
| M-2 | A sequence counter, in the poller's interval arm only. The ladder arm is byte-unchanged (LADDER-TWO-ARGS). |
| M-4 | The route returns DEGRADED, not IDLE, for the three could-not-tell cases. |
| M-7 | A zero member count with no linked key is cross-checked against the job history (SECURITY DEFINER RPC). A stitch on record, or an unreadable history, gives `unknown` (fail closed). |

## Fixed Issues

### CR-01: After a give-up, `Use & Sync` on another key inherits the previous key's running job
**Files modified:** `src/components/strategy/chain-job-state.ts` (new), `ApiKeyManager.tsx`, `SyncProgress.tsx`, `key-card-copy.ts`, and the three test files
**Commit:** `8f84aa374` (shared with WR-06)
**Applied fix:** A pre-attempt job-state gate in `handleSyncTrades`, which runs before the link and the enqueue. The read moved out of `SyncProgress` into the shared `readChainJobState` (the same fail-closed reading, now bounded at 15 s). Refusals end the attempt as the amber `unconfirmed` panel with reason `chain_in_flight` / `chain_unreadable`.
**Status:** fixed: requires human verification (logic: it gates every attempt on a live read)

### CR-02: `KCS23-COMPOSITE` names keys the composite does not use
**Files modified:** `src/lib/strategy-shape.ts` (`readCompositeMemberKeyIds`), `strategies/[id]/edit/page.tsx`, `ApiKeyManager.tsx`, and tests
**Commit:** `72fc7e05d`
**Applied fix:** The member key ids are read once (the count comes from the same read) and passed as `compositeMemberKeyIds`. A composite card lists only those keys, and with none passed it lists no key (fail closed). The edit-page test double moved from "head-count only" to "api_key_id only", with a lineage note.

### WR-01: The discovery fallback promised a compute that may never come
**Files modified:** `discovery/[slug]/[strategyId]/page.tsx`, a new `page.pending-fallback.test.tsx`
**Commit:** `6144e42a9`
**Applied fix:** The fallback renders `KCS10_PUBLIC_SENTENCE`. The render test fails if "still computing", the pipeline name or the timing promise comes back.

### WR-03: Another job's failure shown as this attempt's "Sync failed"
**Files modified:** `SyncProgress.tsx`, `SyncProgress.poll.test.tsx`
**Commit:** `43e7dd5d8`
**Applied fix:** The error arm takes the KCS-18 job-state check and forwards a failure only when the chain is settled. An unreadable read holds it. The FAILED-NO-READ pin, which encoded the hole, moved to FAILED-SETTLED-FORWARDS.
**Status:** fixed: requires human verification (logic)

### WR-04: A late link update can re-link the strategy behind a newer attempt
**Files modified:** `ApiKeyManager.tsx`, `key-card-copy.ts`, and tests
**Commit:** `76b97c68a`
**Applied fix:** `readLinkedKeyId` (bounded) runs between the link and the baseline read. Any key but this attempt's, or a failed read, refuses with KCS-LINK-UNVERIFIED and sends no enqueue. `router.refresh()` moved from `handleLinkKey` into the live-attempt path.
**Residual (recorded in code):** a stalled link that lands between this read-back and the handler's own read of the column. The CR-01 gate narrows that window.
**Status:** fixed: requires human verification (logic)

### WR-05: `Delete` silently strips a key from an unpublished composite
**Files modified:** `ApiKeyManager.tsx`, `key-card-copy.ts`, and tests
**Commit:** `eb5c94e24`
**Applied fix:** `handleDeleteKey` first reads the key's `strategy_keys` memberships. A member is refused with KCS-DELETE-COMPOSITE, and a failed read is refused with the existing `DELETE_FAILED_COPY`. See the decision table for why this refuses rather than hides.

### WR-06: Resync re-enables at once and defeats KCS-22's no-Retry rule
**Commit:** `8f84aa374` (the CR-01 gate)
**Applied fix:** Resync / Use & Sync stay ENABLED after a give-up (a give-up is not a failure), but a click passes the gate, so a Resync during `failed_retry` is refused before any re-POST. The WR-02 pin in `ApiKeyManager.test.tsx` was renamed "…enabled again (… gated since WR-06)", with a lineage comment.
**Status:** fixed: requires human verification (logic)

### WR-07: The in-flight panel promised durations the phase disproved
**Files modified:** `SyncProgress.tsx`, `key-card-copy.ts`, tests, UI-SPEC
**Commit:** `540b3cbcf`
**Applied fix:** Both hints were deleted. After 60 s the panel shows KCS-SLOW ("Large accounts can take longer. This panel checks for 2 minutes; the sync may still be running after that.", with the duration computed from the poll budget), in muted text instead of AA-failing amber-500. This closes UI-SPEC Open Question 3.

### IN-01: The `eslint-disable` on `loadKeys` was a latent stale-closure trap
**Commit:** `0f36914f2`
**Applied fix:** A stable `retireSuccessIf` core (useCallback, empty deps). `loadKeys` now depends on it honestly with no disable, and `retireWithheldSuccess` is a thin wrapper.

### IN-02: An unmount mid-attempt left the continuations live
**Commit:** `0f36914f2`
**Applied fix:** An unmount cleanup clears `attemptRef` (and `unconfirmedAttemptRef`), so every liveness guard drops the continuations. IN02-UNMOUNT asserts that no list read and no `router.refresh()` happen after an unmount.

### IN-03: A stale stitch wins job selection forever on a converted composite
**Files modified:** `compute-state.ts` (optional `preferStitch`), the route, the owner factsheet, `/strategies`, the share page, and tests
**Commit:** `0a436607c`
**Applied fix:** This uses the reviewer's membership option (the PROD measurement option needs a DB command, which is not allowed this round). With `preferStitch: false`, a stitch answers only when it is at least as new as the newest chain row. Every surface passes `false` only when a member read SUCCEEDED with zero members; an unreadable count keeps the stitch rule. The share page's count is an admin HEAD count bounded by the matched id.
**Status:** fixed: requires human verification (logic)

### IN-04: `/strategies` ran an RPC whose answer it discarded
**Commit:** `fb9935dfa`
**Applied fix:** `readRecipientArm` is skipped for a published row.

### H-1: A failed list read rendered "No strategies yet"
**Commit:** `b81d883e4` (shared with H-3)
**Applied fix:** The error is destructured, logged and captured (`stage: "list"`), and rendered as KCS-LIST-UNREADABLE, never the empty state.

### H-2: An unreadable member count silently removed every sync control
**Commit:** `8a21444e3`
**Applied fix:** KCS-SHAPE-UNKNOWN is shown under the header, KCS-EMPTY-NOLINK is the empty state when there is no Add Key, and the edit page captures the failure (`stage: "strategy-shape"`).

### H-3: A failed key-status or member read rendered every row healthy
**Commit:** `b81d883e4`
**Applied fix:** Both failures are captured (`key-status` / `strategy-keys`), and one page-level line, KCS-KEYSTATUS-UNREADABLE, is shown.

### M-1: Late enqueue or link answers were dropped to the console
**Commit:** `f8427b949` (shared with L-4)
**Applied fix:** A late answer never resumes the attempt, and still starts no poll. While this attempt's `enqueue_bound` panel is still shown, a late 2xx shows KCS-LATE-STARTED and a late rejection shows "Sync failed" with the route's message. Every late answer is captured at warning level. The LATE-202 pin moved with a lineage note.
**Status:** fixed: requires human verification (logic)

### M-2: Out-of-order interval reads could admit a stale terminal
**Commit:** `289088943`
**Applied fix:** A per-read sequence number in the interval arm; a response older than the newest one applied is dropped. The ladder arm is unchanged.
**Status:** fixed: requires human verification (logic)

### M-3: Give-up copy said "may still be running" without asking anything
**Commit:** `5737a7a12` (the baseline-read capture it also asks for landed in `538668fb8`)
**Applied fix:** The give-up reads the job state once, and `failed_final` is forwarded as a failure. The hook names the cap `"unreadable"` when there was no clean read, which selects the new KCS-UNREADABLE copy. The ERROR-FROM-START and PIN 7 pins moved with lineage notes.
**Status:** fixed: requires human verification (logic)

### M-4: sync-progress answered IDLE for three could-not-tell cases
**Commit:** `728c27c1f`
**Applied fix:** DEGRADED plus a log and a capture for a non-array RPC answer, a window still full at the RPC cap, and an out-of-domain status (new `isComputeJobStatus`). The read moved into `src/lib/compute-jobs-read.ts`.

### M-5: A full-window `unreadable` was silent and could not be fixed by reloading
**Commit:** `d6d92023d`
**Applied fix:** `readOwnerComputeJobs` re-asks once at the RPC's 1000-row cap. A window still full is logged and captured as `compute-state-window-full` on the owner page and on `/strategies`. The share page's admin read is narrowed with `.in("kind", [...FACTSHEET_CHAIN_KINDS, "stitch_composite"])`. The owner NON-EXHAUSTIVE pin moved with a lineage note.

### M-6: Bound expiries and compute-state read failures reached the console only
**Commit:** `538668fb8`
**Applied fix:** `captureToSentry` at every listed site, with stage tags (see that commit's message). The share-page log carries `strategyId`. Tags only in every capture. ⛔ **CORRECTED 2026-09-24 (round 2, IN-01):** "tags only" means the capture's message and tags carry no id. The surrounding Sentry event can still carry ids in breadcrumbs and URLs. See the H-1 / H-3 / M-6 row above.

### M-7: An RLS-filtered zero count enabled the destructive link write
**Commit:** `561c1a72c`
**Applied fix:** `resolveStrategyShape` gained `compositeHistory` (from `compositeHistoryOf`). A zero count with no linked key and a stitch on record, or an unreadable history, gives `unknown`. It is used by the edit page and the owner factsheet.
**Residual:** a composite DRAFT whose members are hidden and which has no stitch on record yet still reads as unlinked.
**Status:** fixed: requires human verification (logic)

### L-1: A timed-out terminal re-read erased a completed sync without a trace
**Commit:** `089d83119` (shared with L-2)
**Applied fix:** KCS-FINISH-UNVERIFIED is kept from the withhold until the next attempt; no list read clears it.

### L-2: A subject missing from the re-read, or `data: null`, counted as trusted
**Commit:** `089d83119`
**Applied fix:** `data: null` takes the error arm. The new `subjectPresent` field means a success is shown only for a present, trusted subject.

### L-3: `get_user_compute_jobs` answers `[]` for no session and for no jobs alike
**Commits:** `728c27c1f`, `d6d92023d` (documentation only, which is the finding's proposed fix; no migration, per KCS-15)
**Applied fix:** The precondition is documented in `readOwnerComputeJobs` and at each call site (the route, `/strategies`, the owner factsheet): call it only after `getUser()` / `withAuth` resolved a user. Every caller already does. The finding's "ideally treat an empty answer as suspect" was not built, because every caller has the upstream guard.

### L-4: A throw from the post-enqueue `loadKeys` reported "Sync failed"
**Commit:** `f8427b949`
**Applied fix:** The post-enqueue re-read is in its own try, which logs and keeps the attempt `computing`. The IN-02 ended-attempt pin moved with a lineage note.

## Skipped Issues

### WR-02: A computed row whose series cannot build shows no recipient note on `/strategies`
**File:** `src/app/(dashboard)/strategies/page.tsx` (the `shareNotes` filter)
**Reason: UNFIXABLE-IN-PHASE.** Whether a payload builds is decided only by `fetchAndBuildPayload`
(`src/lib/factsheet/fetch-and-build-payload.ts`), which runs on the **service-role admin client**
(`createAdminClient`). It reads the series through `composite-read-path` /
`resolveDailyReturnSeries` behind deny-all RLS. The owner-scoped request client that `/strategies`
uses cannot read those rows, and no owner-readable scalar records "payload buildable". The only
fix the reviewer names, a server-written "payload buildable" fact (for example a column
maintained by the bridge), needs a **migration**, which KCS-15 forbids. Running the builder or
the series read per row needs an **admin read**. The orchestrator ruled both out for this
round. The reviewer's interim idea (every note links to the owner factsheet) was not taken: it
is new copy on every computed row, and the orchestrator's decision for WR-02 was fix-or-record.
**Routing:** for the orchestrator. It needs a named phase for the bridge-maintained buildability
fact.
**Original issue:** `isComputedAnalytics` is only the builder's first gate. A `complete` row whose
series cannot build gets no note, while its recipient sees KCS11-B.

## Items for the orchestrator

1. **The WR-05 decision changes KCS-23's "`Delete` behaves as before"** for member keys only: a
   member's Delete is now refused. It is recorded in the UI-SPEC amendment row. The deviation
   policy may also want a dated note in `167.2-CONTEXT.md` and the ROADMAP. This fixer did not
   edit either.
2. **WR-02** needs a phase (above).
3. The commit message of `289088943` has a cosmetic typo ("OUT- OF-ORDER"). It was not amended.
4. Every string added this round is new user-facing copy authored without the founder. Each one
   is in the UI-SPEC amendment table for review.

---

_Fixed: 2026-09-24_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
