---
phase: 156-connect-refactor-the-venue-the-server-validated-is-the-venue
plan: 01
type: execute
wave: 1
landing: PR-A
depends_on: []
files_modified:
  - .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md
autonomous: false
requirements: [CONNECT-01, CONNECT-03]

must_haves:
  truths:
    - "A1 is measured, not assumed: a supabase-js client built with SUPABASE_SERVICE_ROLE_KEY reaches PostgREST with auth.role() = 'service_role'"
    - "A2 is measured: auth.uid() is NULL for that same client"
    - "A3 is measured: the sql-tests CI connection role is named, and whether it is superuser/owner is recorded"
    - "A4 is measured: service_role's current EXECUTE privilege on both wizard RPCs is recorded"
    - "If A1 or A2 is FALSIFIED, the phase STOPS and returns to the orchestrator — the privilege design is wrong, not merely mis-tuned"
  artifacts:
    - path: ".planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md"
      provides: "The measured values every later plan reads instead of re-assuming"
      contains: "A1"
      min_lines: 40
  key_links:
    - from: "156-MEASUREMENTS.md"
      to: "156-03-PLAN.md (Migration A body gate)"
      via: "the measured auth.role() value is the literal the gate compares against"
      pattern: "auth\\.role\\(\\)"
---

<objective>
Close assumptions **A1, A2, A3, A4** from `156-RESEARCH.md`'s Assumptions Log by measurement
against **TEST**, before a single line of the privilege design is written.

Purpose: RESEARCH is HIGH confidence on the codebase trace and **MEDIUM on the two Supabase
runtime facts the whole design rests on**. `156-PATTERNS.md` found no in-repo analog that settles
them — `log_audit_event_service`'s in-body gate admits `auth.role() IN ('authenticated',
'service_role')`, so its years of production success proves the union, **not A1**. If A1 is false
the in-body gate is either always-refuse (total outage) or always-pass (a security no-op that no
test would catch).

Output: `156-MEASUREMENTS.md` — four claims, each PASS/FAIL with the raw observed value pasted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-RESEARCH.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-PATTERNS.md

⛔ **PROJECT CONSTRAINTS — binding on every task in this phase.**
- **NEVER `supabase db push`.** Merging `supabase/migrations/**` to `main` **auto-applies to PROD**.
  PROD = `khslejtfbuezsmvmtsdn`. TEST = `qmnijlgmdhviwzwfyzlc`. **This plan touches TEST only.**
- ⛔ **This plan creates NO file under `supabase/migrations/`.** The probe is transient DDL issued
  through Supabase MCP `execute_sql`, **never** `apply_migration` — `apply_migration` records a row
  in `schema_migrations` and manufactures the exact timestamp drift `migration-drift-check` fails on.
- `.planning/` is TRACKED and the repo is **PUBLIC**. ⛔ Never paste a key, a JWT, a connection
  string or a PROD census into `156-MEASUREMENTS.md`. Record **role names and booleans only**.
- MCP tools are **stripped from subagents** (upstream `anthropics/claude-code#13898`). Task 2 is a
  blocking checkpoint for exactly that reason — it is not optional politeness.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the four probes and the MEASUREMENTS skeleton</name>
  <files>.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md</files>
  <read_first>
    - `.planning/phases/156-.../156-RESEARCH.md` § "Assumptions Log" (A1–A6) and § "SC3 — How a
      service-role writer proves the row belongs to the caller" (Traps A/B/C)
    - `.planning/phases/156-.../156-PATTERNS.md` § "No Analog Found" — states precisely why no
      in-repo shape settles A1/A2
    - `.github/workflows/ci.yml`, the `sql-tests` job — to read which secret supplies the psql
      connection (`secrets.TEST_SUPABASE_DB_URL`) and confirm the meta-command filter and the
      `vars.E2E_TEST_DB_CONFIGURED` gate
    - `supabase/tests/test_api_key_delete_atomicity.sql:255-265` — the in-repo service-role-shaped
      call idiom, for contrast: it proves the **GUC** path, not that supabase-js sets that GUC
  </read_first>
  <action>
Create `156-MEASUREMENTS.md` with frontmatter `status: pending` and one section per claim. Each
section states the claim, why it is load-bearing, the EXACT command to run, and an empty
`**Observed:**` line. Do not guess any value.

**A1 + A2 — the load-bearing pair.** Probe design: a transient function
`public.__p156_probe()` returning `jsonb_build_object('auth_role', auth.role(), 'auth_uid',
auth.uid(), 'current_user', current_user, 'session_user', session_user)`, `LANGUAGE sql`,
`STABLE`, **SECURITY INVOKER** (a DEFINER probe would report the owner and answer the wrong
question — this is Trap C in miniature), with `GRANT EXECUTE TO service_role`. Record the three
steps as literal commands: (i) MCP `execute_sql` CREATE + GRANT against project
`qmnijlgmdhviwzwfyzlc`; (ii) `curl -s -X POST "$TEST_SUPABASE_URL/rest/v1/rpc/__p156_probe" -H
"apikey: $KEY" -H "Authorization: Bearer $KEY" -H "Content-Type: application/json" -d '{}'` where
`$KEY` is the TEST **service-role** key; (iii) MCP `execute_sql` `DROP FUNCTION public.__p156_probe()`.
⛔ Step (iii) is mandatory and must be listed as its own line — a probe left behind on TEST is a
SECURITY-INVOKER identity oracle granted to `service_role`.
⚠️ The curl must go through **PostgREST**, not through MCP. MCP `execute_sql` uses its own
connection and cannot answer A1 at all; a value read that way is a false PASS.

**A3 — the `sql-tests` connection role.** Command:
`psql "$TEST_SUPABASE_DB_URL" -c "SELECT current_user, session_user, (SELECT rolsuper FROM pg_roles WHERE rolname = current_user) AS is_super"`
using the **same secret the CI job uses**, not the MCP connection. Record the role name and
`is_super`. This governs whether the seven direct RPC call sites in `supabase/tests/` survive the
PR-B `REVOKE` at the **grant layer** (RESEARCH G3/G4).

**A4 — today's ACL.** MCP `execute_sql`:
`SELECT has_function_privilege('service_role','public.create_wizard_strategy(uuid,text,text,text,text,text,text,text,integer,text,uuid,text)','EXECUTE') AS cws, has_function_privilege('service_role','public.add_wizard_composite_key(uuid,text,text,text,text,text,text,text,integer,text,uuid)','EXECUTE') AS awck`.
⚠️ Note the **asymmetric arity** — `create_wizard_strategy` is **12** args since `20260812083206`,
`add_wizard_composite_key` is **11**. A copied 11-arg literal for the first will
`regprocedure`-error or address nothing.

Add a final section **"Decision gate"** stating verbatim: if A1 ≠ `service_role` or A2 ≠ NULL,
plans 03/04/07 are INVALID as written and the executor must STOP and report to the orchestrator
rather than adapt the design in place.
  </action>
  <verify>
    <automated>test -f .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md && grep -qc 'A1' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md && grep -q '__p156_probe' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md && grep -q 'DROP FUNCTION' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md && ! grep -rn 'khslejtfbuezsmvmtsdn' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `156-MEASUREMENTS.md` exists and contains a section for each of A1, A2, A3, A4
    - The A1/A2 probe function is declared **SECURITY INVOKER** (the string `SECURITY INVOKER` or
      an explicit statement that no `SECURITY DEFINER` is used appears in the A1 section)
    - A `DROP FUNCTION public.__p156_probe()` teardown step is listed as its own numbered step
    - The A1 invocation is a `curl` against `/rest/v1/rpc/__p156_probe`, NOT an MCP `execute_sql`
    - The A3 command references `TEST_SUPABASE_DB_URL` (the CI secret), not the MCP connection
    - The A4 signature literals are 12-arg for `create_wizard_strategy` and 11-arg for
      `add_wizard_composite_key`
    - The file contains **no** key, JWT, connection string, or the PROD project ref
      `khslejtfbuezsmvmtsdn`
    - A "Decision gate" section states the STOP condition for a falsified A1 or A2
  </acceptance_criteria>
  <done>The four probes are written down precisely enough that a second operator could run them without asking a question, and the teardown cannot be forgotten.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run the four probes against TEST</name>
  <action>⛔ CHECKPOINT — no autonomous work in this task. Present the verification steps below to the operator and WAIT. Do not substitute an automated approximation, do not proceed on an assumed result, and do not mark this task complete without the pasted output.</action>
  <what-built>Task 1 wrote `156-MEASUREMENTS.md` containing four exact commands. Nothing has been run — subagents have no MCP access and no TEST credentials.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md`.
    2. Run the **A1/A2** three steps in order against TEST `qmnijlgmdhviwzwfyzlc`
       (MCP `execute_sql` CREATE+GRANT → `curl` with the TEST **service-role** key → MCP
       `execute_sql` DROP). ⛔ Confirm the DROP returned success before moving on.
    3. Run the **A3** `psql` command using the same `TEST_SUPABASE_DB_URL` value the CI
       `sql-tests` job consumes.
    4. Run the **A4** MCP `execute_sql` query.
    5. Paste the four raw results back. ⛔ Redact nothing except credentials — the whole point is
       the raw value. ⛔ Paste no credential.
    Expected if RESEARCH is right: A1 = `service_role`, A2 = `null`, A4 = `false`/`false`.
    A3 is genuinely unknown; whatever it says is the answer.
  </how-to-verify>
  <done>The operator ran every listed step and pasted the raw results back; the following task records them.</done>
  <resume-signal>Paste the four raw results, or describe what failed</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the measurements and arm the decision gate</name>
  <files>.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md</files>
  <read_first>
    - The checkpoint output from Task 2
    - `.planning/phases/156-.../156-RESEARCH.md` § "Assumptions Log" — the "Risk if wrong" column
      is what each verdict has to be read against
  </read_first>
  <action>
Fill each `**Observed:**` line with the pasted raw value and set a per-claim verdict of
`PASS (measured)` or `FAIL (measured)`. Flip the frontmatter to `status: measured` and add
`measured_on: <date>` and `measured_against: qmnijlgmdhviwzwfyzlc`.

Then write a short **"What this changes"** subsection per claim:
- **A1 PASS** ⇒ the in-body gate compares `auth.role()` against the literal `service_role`; plan 03
  and plan 07 are unblocked as written.
- **A2 PASS** ⇒ Trap B is confirmed real: a relaxed `IF v_auth_uid IS NOT NULL AND v_auth_uid <>
  p_user_id` never fires, so plan 07's final bodies must contain **zero** `auth.uid()` and the
  post-verify must assert its absence structurally.
- **A3** ⇒ name the connection role and state, in one sentence, whether the seven direct RPC call
  sites in `supabase/tests/` clear the **grant layer** after the PR-B `REVOKE`. If the role is not
  superuser and not the function owner, say so loudly and note that plan 09 must add a
  `GRANT`-independent path or the two composite gates go red for a reason unrelated to the change.
- **A4** ⇒ state whether `GRANT EXECUTE … TO service_role` is a genuine addition (expected) or
  already present (harmless).

⛔ If A1 or A2 is `FAIL`, do NOT continue and do NOT adapt the design: write the failure into the
Decision-gate section, leave frontmatter at `status: blocked`, and report to the orchestrator that
the privilege design in `156-RESEARCH.md` D1 is falsified.
  </action>
  <verify>
    <automated>grep -q 'status: measured\|status: blocked' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md && test "$(grep -c 'PASS (measured)\|FAIL (measured)' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md)" -ge 4 && ! grep -qi 'TBD\|pending\|assumed' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Each of A1, A2, A3, A4 carries a raw observed value and a `PASS (measured)` / `FAIL (measured)` verdict
    - No `TBD`, `pending` or `assumed` token survives anywhere in the file
    - Frontmatter is `status: measured` (all pass) or `status: blocked` (A1 or A2 failed)
    - The A3 subsection names the connection role literally and states the grant-layer consequence
      for the seven `supabase/tests/` call sites in one sentence
    - No credential appears in the file
  </acceptance_criteria>
  <done>Every later plan in this phase reads a measured value where RESEARCH had an assumption, and a falsified A1/A2 stops the phase instead of silently reshaping it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → TEST database | Transient DDL is issued against a shared TEST project other CI jobs use concurrently |
| `.planning/` → public GitHub | Everything written here is world-readable on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-01 | Elevation of Privilege | `public.__p156_probe()` left on TEST | mitigate | Probe is SECURITY **INVOKER** (reports the caller, grants nothing) and the DROP is a mandatory numbered step asserted by Task 1's acceptance criteria |
| T-156-02 | Information Disclosure | `156-MEASUREMENTS.md` in a public repo | mitigate | Record role names and booleans only; Task 1's automated verify greps for the PROD project ref; both tasks forbid credentials explicitly |
| T-156-03 | Tampering | Probe run against PROD by mistake | mitigate | Both the checkpoint and the context block name `qmnijlgmdhviwzwfyzlc` as the only target and `khslejtfbuezsmvmtsdn` as forbidden |
| T-156-04 | Repudiation | A false PASS on A1 from reading the value through MCP instead of PostgREST | mitigate | Task 1 states the MCP path "cannot answer A1 at all; a value read that way is a false PASS"; the acceptance criterion requires the `curl` form |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | **Zero packages are installed by this phase** (`156-RESEARCH.md` § Package Legitimacy Audit). No install task exists in any plan; slopcheck is not applicable |
</threat_model>

<verification>
- `156-MEASUREMENTS.md` exists, is `status: measured`, and carries four raw observed values
- `psql`/MCP were run against TEST only; `public.__p156_probe` no longer exists on TEST
- No `supabase/migrations/**` file was created by this plan
</verification>

<success_criteria>
A1, A2, A3 and A4 are facts with pasted evidence rather than entries in an Assumptions Log, and
plans 03/04/07 either proceed on measured ground or are stopped before any SQL is written.
</success_criteria>

<output>
Create `.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-01-SUMMARY.md` when done
</output>
