---
phase: 156-connect-refactor-the-venue-the-server-validated-is-the-venue
plan: 08
type: execute
wave: 7
landing: PR-B
depends_on: ["156-07"]
files_modified:
  - supabase/tests/test_api_keys_exchange_not_user_writable.sql
autonomous: true
requirements: [CONNECT-01, CONNECT-04]

must_haves:
  truths:
    - "Calling create_wizard_strategy as authenticated is refused with SQLSTATE 42501 and mints no row"
    - "The identical assertions exist for add_wizard_composite_key — the twin, which has none today"
    - "A privileged call still SUCCEEDS and stores attested_venue = exchange, so 'refused' cannot mean 'the function is gone'"
    - "A dropped comment marker reddens instead of silently skipping the whole 5-block"
    - "Assertions 5b, 5c and 5e are untouched"
  artifacts:
    - path: "supabase/tests/test_api_keys_exchange_not_user_writable.sql"
      provides: "the SC1 proof — both RPC doors shut, both proven shut for the right reason"
      contains: "42501"
  key_links:
    - from: "supabase/tests/test_api_keys_exchange_not_user_writable.sql"
      to: "api_keys.attested_venue column comment"
      via: "col_description LIKE marker gates that arm or skip the whole block"
      pattern: "col_description"
---

<objective>
Invert assertion **5d** and mint its missing composite twin **5f/5g**, so the SQL gate proves the RPC
door is **shut** on **both** functions — for the right reason, with nothing minted, and with an
anti-vacuity positive control on each.

Purpose: 5d today asserts the door is **OPEN** — it fails *if the RPC cannot be called by the row
owner*. That inversion is SC1. And 5d only ever exercised `create_wizard_strategy`;
`add_wizard_composite_key` has the identical door and **no assertion anywhere in this file**. Adding
5d without 5f/5g closes one of two identical doors, which is precisely the instance-not-class defect
this phase was created to end.

Output: one SQL gate file whose 5-block covers both twins in both polarities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-PATTERNS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-07-SUMMARY.md

⛔ **THE GREEN-CI TRAP — the single most dangerous thing in this plan.**
The whole 5a–5e block is gated at `:242-253` on a bare
`col_description(api_keys.attested_venue) LIKE '%20260811210000%'`. If that is false, control falls
to `RAISE NOTICE 'SKIP (5): …'` at `:419-421` — **a NOTICE, exit code 0, green CI, zero coverage.**
Everything this plan adds sits inside that block. A gate that reports success while asserting nothing
is worse than no gate, because it stops anyone looking.

⛔ **SQL gates live in `supabase/tests/test_*.sql`.** `*_live.py` and `it.skipIf` vitest **never run
in CI** and may not be counted toward any success criterion here.

⚠️ **Pitfall 9 — a grey `sql-tests` is NOT a pass.** The job shares the `shared-test-db` concurrency
group with `python` and `e2e-seeded`; a cancelled job renders **grey**, and branch protection is
deferred on this repo, so a lost gate looks like a pass. Re-run it; never interpret it.

<interfaces>
**The four in-file conventions to copy — all already present in this file.**

- `:242-253` the marker gate; `:254-265` **(5a)** the marker-present positive control, whose own
  comment states the reasoning: *"Asserting that here is what turns 'the re-stamp dropped the
  marker' into a FAILURE instead of assertions 2 and 3 quietly skipping forever — and a skip is the
  worse outcome, because it looks green."*
- `:267-281` **(5b)** the anti-vacuity positive control on the privileged path, before any negative:
  *"Without this, 'the client's value was scrubbed' is indistinguishable from 'this column never
  stores anything', and 5c would pass vacuously."*
- `:389-418` **(5e)** the SQLSTATE discipline: refused **and refused for the RIGHT reason** —
  *"it was blocked for the wrong reason, so this assertion is not testing the coupling constraint."*
- `:329-387` **(5d)** the body being inverted, including its `SET LOCAL ROLE authenticated` /
  `RESET ROLE` bracket and the ambient JWT claim set at `:135-137` (role `authenticated`, `sub` =
  `v_uid`). ⭐ After Phase 156 that block has **both** doors shut — grant layer *and* body role gate
  — which is what makes it a clean 42501 assertion.

⛔ **DO NOT TOUCH 5b, 5c or 5e.** 5c asserts the client re-INSERT **SUCCEEDS** and stores NULL —
153.6 D-02/D-03 keep that path open on purpose, and it is the fail-toward behaviour of the three
sibling client-INSERT surfaces. 5e is SC4's fence.

⚠️ `:350`'s existing call passes **11 positional args to the 12-arg function** (the 12th has
`DEFAULT NULL`). That is legal and is not a bug — keep it positional-with-default or make it named,
but do not "fix" it into an arity mismatch. The 12-arg signature pin lives at
`test_api_keys_venue_identity_uniq.sql:41`.

⚠️ `153.6-07-SUMMARY.md` records the hazard that **a blunt 5d mutation is intercepted by 5b before it
reaches 5d.** Design each falsifiability mutation to isolate the assertion under test.

**Local run:** `psql` is not on PATH — `/opt/homebrew/opt/postgresql@16/bin/psql -v ON_ERROR_STOP=1
-f <file>` against a throwaway PG16 fixture (`initdb -U postgres`, **loopback TCP not a unix
socket** — the scratchpad path is 121 bytes against a 103-byte `sun_path` limit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invert 5d and mint the composite twin 5f/5g</name>
  <files>supabase/tests/test_api_keys_exchange_not_user_writable.sql</files>
  <read_first>
    - `supabase/tests/test_api_keys_exchange_not_user_writable.sql` in full (424 lines) — especially
      `:130-145` (the ambient JWT claim and fixture uids), `:242-281` (marker gate, 5a, 5b),
      `:283-327` (5c — do not touch), `:329-418` (5d and 5e)
    - `supabase/tests/test_api_key_delete_atomicity.sql:140-190, :255-265` — the role-shaping idiom
      (`'{"role":"service_role"}'`, commented *"Service-role-shaped call"*) and the mandatory
      `set_config('request.jwt.claims', NULL, true)` reset at the end
    - The Migration B file from plan 07 — for the exact signature literals and the new migration id
  </read_first>
  <action>
**Invert 5d.** Keep its `SET LOCAL ROLE authenticated` / `RESET ROLE` bracket and its ambient
`authenticated` role claim — after Phase 156 **both** doors are shut for that caller, which is the
point. Replace the assertion with three parts, in this order:

1. **Privilege polarity, both directions.** `has_function_privilege('authenticated', <12-arg sig>,
   'EXECUTE')` must be **FALSE** and `has_function_privilege('service_role', <12-arg sig>, 'EXECUTE')`
   must be **TRUE**. ⚠️ The negative alone also passes on a database where the function was
   **dropped** — the positive is what makes it mean something.
2. **Refused for the RIGHT reason.** Call the RPC, capture `SQLSTATE` into a variable, and require
   exactly **`42501`**. Copy 5e's failure-message shape verbatim in spirit: an error raised with any
   other SQLSTATE means it was blocked for the wrong reason and the assertion is not testing the
   grant. ⚠️ After this phase there are **two** distinct refusal mechanisms that both yield 42501
   (grant layer and in-body gate) — that is fine and expected; do not try to distinguish them here.
3. **Nothing was minted.** Count `api_keys` rows for the fixture uid before and after, and require
   the delta to be **0**. "An error was raised" is the weaker claim.

Then a **5d-positive anti-vacuity control**, shaped on 5b: the SAME RPC call made as the
**privileged** role (`RESET ROLE` + `set_config('request.jwt.claims', '{"role":"service_role"}',
true)`, the `test_api_key_delete_atomicity.sql:261` idiom) with a valid `p_user_id` must **SUCCEED**
and the minted row must carry `attested_venue = exchange` and non-NULL. Without it, "refused" is
indistinguishable from "the signature drifted / the function is gone" — i.e. from
**connect-a-key is broken**, which this gate would then green-light.

**Mint 5f/5g** — the identical pair for `add_wizard_composite_key(<11-arg sig>)`: 5f the three-part
negative, 5g the privileged positive. Use the same fixture-uid discipline and a **distinct**
`wizard_session_id` so the composite advisory-lock space (`wizcomposite:`) is exercised rather than
colliding with 5d's `wizdraft:` draft.

⛔ Reset the JWT claim (`set_config('request.jwt.claims', NULL, true)`) after every role-shaped call,
matching this file's and `test_api_key_delete_atomicity.sql`'s existing discipline. A leaked claim
silently changes the meaning of every later assertion in the transaction.
⛔ Do not modify 5b, 5c or 5e. Do not modify assertions 1–4.
⛔ `RAISE` format strings: single literal or dollar-quoted, never `||`-concatenated.
  </action>
  <verify>
    <automated>/opt/homebrew/opt/postgresql@16/bin/psql -v ON_ERROR_STOP=1 -f supabase/tests/test_api_keys_exchange_not_user_writable.sql "$PG16_FIXTURE_URL" 2>&1 | tee /tmp/156-08-t1.txt; grep -qi 'SKIP (5)' /tmp/156-08-t1.txt && echo "FAIL: block skipped" && exit 1; grep -c '5f\|5g' /tmp/156-08-t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - The run against a fully-migrated PG16 fixture reports the 5-block **executing**, not
      `SKIP (5)` — a `SKIP` line in the output is a hard failure of this task
    - 5d asserts `has_function_privilege('authenticated', …)` FALSE **and**
      `has_function_privilege('service_role', …)` TRUE
    - 5d requires SQLSTATE exactly `42501` and asserts a zero row delta
    - A 5d-positive control exists making the same call service-role-shaped, asserting SUCCESS and
      `attested_venue = exchange`
    - 5f/5g exist with the same structure against the **11-arg** `add_wizard_composite_key` and a
      distinct wizard session id
    - `git diff` shows **zero** changed lines inside `:267-281` (5b), `:283-327` (5c) and
      `:389-418` (5e), and inside assertions 1–4
    - Every role-shaped call is followed by a claim reset
    - **Falsifiability, observed and pasted:** (i) re-GRANT `authenticated` EXECUTE on the fixture →
      5d reds naming the privilege, and 5f reds independently; (ii) neuter the privileged positive
      (make it call with the `authenticated` claim) → the positive control reds while the negatives
      stay green, proving the two are independent. ⚠️ Design each so 5b does not intercept it.
  </acceptance_criteria>
  <done>Both RPC doors are asserted shut, for the right reason, with nothing minted — and each negative is paired with a positive that fails if the function merely vanished.</done>
</task>

<task type="auto">
  <name>Task 2: Arm the new marker and make a dropped marker RED instead of green</name>
  <files>supabase/tests/test_api_keys_exchange_not_user_writable.sql</files>
  <read_first>
    - `supabase/tests/test_api_keys_exchange_not_user_writable.sql:242-265` — the existing marker
      gate and the 5a control, whose comment explains why a skip is the worse outcome
    - The Migration B file from plan 07 — its 14-digit id and the exact re-stamped
      `COMMENT ON COLUMN … attested_venue` text
    - `.planning/phases/156-.../156-07-SUMMARY.md` — the `marker_ok` boolean measured on TEST
  </read_first>
  <action>
Add a second marker read, `v_revoke_live`, matching this migration's own 14-digit id on the same
`attested_venue` column comment, and gate **only** the new 5d-inverted / 5f / 5g assertions on it, so
a database carrying Migration A but not Migration B skips those three cleanly rather than failing
for a state it is legitimately in.

Then add the two cross-checks that make a dropped marker **red** rather than silently green:

**(5a′) Older marker survives the newer re-stamp.** If `v_revoke_live` is true, then `v_attest_live`
must also be true. Migration B re-stamps the comment, so this is exactly the
*"a comment re-stamp dropped the gate marker, so the assertions above SKIPPED rather than ran"*
failure the existing 5a control catches for the previous generation. Copy 5a's message shape and
name Migration B as the file that re-stamped it.

**(5a″) Privilege state and marker must agree.** If `has_function_privilege('authenticated',
<12-arg sig>, 'EXECUTE')` is **FALSE** — i.e. this database *has* Migration B — then `v_revoke_live`
must be **true**. ⭐ This is the assertion that closes the trap in the direction that actually bites:
without it, a re-stamp that drops the new id leaves the door correctly shut, the 5d/5f/5g block
silently skipped, and CI green — the exact shape that would let a future regression through
unobserved. Read the privilege from `pg_proc`/`has_function_privilege`, never from the comment, or
the cross-check has only one source.

Update the `SKIP` NOTICE text so it distinguishes the two skippable states (no `20260811210000` ⇒
Migration `20260811210000` not applied here; no new id ⇒ Migration B not applied here) — an operator
reading a skip must be able to tell which.
  </action>
  <verify>
    <automated>/opt/homebrew/opt/postgresql@16/bin/psql -v ON_ERROR_STOP=1 -f supabase/tests/test_api_keys_exchange_not_user_writable.sql "$PG16_FIXTURE_URL" 2>&1 | tee /tmp/156-08-t2.txt; ! grep -qi 'SKIP (5' /tmp/156-08-t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `v_revoke_live` exists, reads the same `attested_venue` column comment, and matches Migration
      B's 14-digit id
    - Only the new 5d-inverted / 5f / 5g assertions are gated on `v_revoke_live`; 5b/5c/5e stay on
      the existing gate
    - The (5a′) assertion reds when `v_revoke_live` is true and `v_attest_live` is false
    - The (5a″) assertion reds when `authenticated` lacks EXECUTE but `v_revoke_live` is false, and
      reads the privilege from `has_function_privilege`, not from a comment
    - The SKIP NOTICE distinguishes the two skippable states by marker
    - **Falsifiability, observed and pasted:** strip `20260811210000` from the fixture's column
      comment → (5a′) reds; strip only the new id while leaving the door shut → (5a″) reds. Both
      messages pasted into the plan summary.
    - Full-file run against a fully-migrated fixture emits **no** `SKIP (5` line
  </acceptance_criteria>
  <done>The one failure mode that would make this whole phase's gate report success while asserting nothing now fails loudly, in both directions, from two independent sources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `authenticated` JWT → PostgREST RPC | The exact boundary this file asserts is now closed |
| CI signal → reviewer confidence | A skipped or grey gate is indistinguishable from a passing one unless made loud |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-39 | Elevation of Privilege | `create_wizard_strategy` reachable by `authenticated` | mitigate | 5d inverted: privilege FALSE, SQLSTATE 42501, zero row delta |
| T-156-40 | Elevation of Privilege | `add_wizard_composite_key` — the twin with no assertion at all today | mitigate | 5f/5g minted with the identical three-part structure and its own positive control |
| T-156-41 | *(control failure)* | A dropped comment marker turning the block into `RAISE NOTICE 'SKIP (5)'` at exit 0 | mitigate | (5a′) and (5a″) cross-check marker against marker and marker against privilege, from two independent sources |
| T-156-42 | Repudiation | A vacuous negative that passes because the function was dropped | mitigate | Every negative is paired with a privileged positive asserting SUCCESS and `attested_venue = exchange` |
| T-156-43 | Repudiation | A grey `sql-tests` job read as a pass | mitigate | Pitfall 9 stated in-context; the phase gate requires an observed-green run, not an absent-red one |
| T-156-44 | Tampering | A leaked `request.jwt.claims` GUC changing the meaning of later assertions | mitigate | Claim reset mandated after every role-shaped call and asserted in acceptance |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this phase; no install task exists |
</threat_model>

<verification>
- Full-file `psql -v ON_ERROR_STOP=1` run against a fully-migrated PG16 fixture: all assertions
  execute, **no `SKIP (5` line**, exit 0
- Four falsifiability mutations observed RED and pasted (re-GRANT, neutered positive, each dropped marker)
- `git diff` proves 5b, 5c, 5e and assertions 1–4 are byte-unchanged
- `sql-tests` observed **green** (not grey) in CI against TEST
</verification>

<success_criteria>
The phase's central claim — a caller with a valid session cannot set `attested_venue` by any route —
is a falsifiable, executed assertion on **both** RPCs, and the gate cannot report success while
asserting nothing.
</success_criteria>

<output>
Create `.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-08-SUMMARY.md` when done
</output>
