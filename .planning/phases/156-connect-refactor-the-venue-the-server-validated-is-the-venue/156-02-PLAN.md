---
phase: 156-connect-refactor-the-venue-the-server-validated-is-the-venue
plan: 02
type: execute
wave: 2
landing: PR-A
depends_on: ["156-01"]
files_modified:
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/api/strategies/composite/add-key/route.test.ts
autonomous: true
requirements: [CONNECT-02, CONNECT-03]

must_haves:
  truths:
    - "Both wizard route test files assert the RPC is reached through the ADMIN client, and fail if it is reached through the user-scoped client"
    - "Both assert p_user_id is withAuth's user.id and that no request-body field can reach it"
    - "Both assert a missing SUPABASE_SERVICE_ROLE_KEY answers 503 SEAM_MISCONFIGURED — never a silent success"
    - "The composite twin has a @/lib/supabase/admin mock, which it does not have today"
    - "Every new assertion was OBSERVED RED against the unchanged routes before plan 04 makes it green"
  artifacts:
    - path: "src/app/api/strategies/create-with-key/route.test.ts"
      provides: "single-key route contract for the service-role writer"
      contains: "createAdminClient"
    - path: "src/app/api/strategies/composite/add-key/route.test.ts"
      provides: "composite twin contract — the missing admin mock (G11)"
      contains: "@/lib/supabase/admin"
  key_links:
    - from: "src/app/api/strategies/composite/add-key/route.test.ts"
      to: "@/lib/supabase/admin"
      via: "vi.mock supplying createAdminClient"
      pattern: "vi\\.mock\\(\\s*[\"']@/lib/supabase/admin"
---

<objective>
Re-cut both wizard route test files to the **post-156 contract, and observe them RED against the
unchanged routes**, before plan 04 touches a line of production code.

Purpose: Pitfall 5. `create-with-key/route.test.ts:229-238`'s docblock currently asserts that a
missing service key *"must degrade to a dark fence and never to a failed submit"*. That sentence
is correct for the venue fence and becomes **false** for the RPC — after 156 a missing service key
means the RPC cannot be called at all. A test left as-is passes **only if the route keeps a
user-scoped fallback**, which is the bug. Writing the route first and the test second would let
that fallback ship green.

Output: two test files whose new assertions fail for the right, named reasons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-PATTERNS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md

⛔ **This plan lands RED on purpose.** CI on the phase branch is expected to fail between this wave
and wave 3. Do not "fix" it by changing the route here — plan 04 owns the route.

⚠️ **GSD worktree agents get NO `node_modules`.** `npx vitest` exits 1 with `MODULE_NOT_FOUND` and
`npx tsc` resolves an unrelated joke package. **Symlink the main checkout's `node_modules` into the
worktree before trusting ANY result.** A broken environment exits 1 exactly like a failing test —
grep for the per-test NAME among the failures, never read a bare exit code.

⚠️ **CI runs Node 22; local is Node 25.** A CI-only vitest failure here is **not** a flake —
reproduce with `PATH=/opt/homebrew/opt/node@22/bin`.

<interfaces>
Contracts the executor needs — do not go hunting for them.

`src/lib/api/withAuth.ts:32-73` — `withAuth(handler)` calls `supabase.auth.getUser()` and passes
the verified `user` into the handler. Both routes enter as
`export const POST = withAuth(async (req: NextRequest, user: User) => {` and both already pass
`p_user_id: user.id`. **No new plumbing is needed for the identity — only a new assertion.**

`src/lib/supabase/admin.ts` — `createAdminClient()` is `import "server-only"` and **THROWS** when
`SUPABASE_SERVICE_ROLE_KEY` is absent.

`src/lib/ratelimit.ts:325-326` — `isRateLimitMisconfigured(rl) ? 503 : 429`. **503 is the
established status for "our own configuration is wrong".**

`src/lib/wizardErrors.ts:430` + `:2166-2183` — `SEAM_MISCONFIGURED` is **already** a member of the
wizard code union with authored copy ("A setting on our side is wrong… Nothing was submitted and
nothing was changed… This is not your key, your exchange or your data.") and is deliberately
**non-recoverable**. ⛔ **Mint no new code.** `EXPECTED_TABLE_SIZE` pins at
`wizardErrors.test.ts:1486` and `:1750` and PARITY-05's ledger police additions to that union;
adding a member here would move pins this phase has no business moving.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-cut the single-key route test to the service-role-writer contract</name>
  <files>src/app/api/strategies/create-with-key/route.test.ts</files>
  <read_first>
    - `src/app/api/strategies/create-with-key/route.test.ts:200-260` — the existing
      `@/lib/supabase/server` mock (`:205-213`, where `rpc` lives today) and the existing
      `adminClientThrows` mock (`:215-238`, whose closing docblock sentence is the expectation
      being falsified)
    - `src/app/api/strategies/create-with-key/route.ts:160-210` — the venue-identity fence's
      **fail-SOFT** try/catch, so the divergence is understood rather than deleted by accident
    - `src/app/api/strategies/create-with-key/route.ts:500-515` — the in-file
      `misconfiguredBody: { code: "SEAM_MISCONFIGURED", … }` shape to assert against
    - `src/app/api/strategies/create-with-key/route.ts:645-700, :745-800` — `validateKey`,
      `encryptKey` and the `.rpc("create_wizard_strategy", …)` argument object
    - `.planning/phases/156-.../156-PATTERNS.md` § 6 — G9/G10 verbatim
  </read_first>
  <behavior>
    - **Reached via the admin client:** with the admin mock healthy, a successful connect resolves
      `rpcMock` from the **`@/lib/supabase/admin`** mock's returned object.
    - **NOT reachable via the user-scoped client:** the `@/lib/supabase/server` mock keeps an `rpc`
      property that **throws a distinctive error naming the wrong client** if it is ever called.
      Without this, "the route still uses the user-scoped client" and "the route was rewired" are
      indistinguishable and the assertion passes either way.
    - **CONNECT-02 venue binding:** the `p_exchange` argument passed to the RPC is strictly equal
      to the first argument `validateKey` was called with, and to the first argument `encryptKey`
      was called with. Assert the identity across all three, not the literal `"binance"`.
    - **CONNECT-03b ownership binding:** `p_user_id` equals the `user.id` `withAuth` supplied, for
      a request whose body carries a **different** `user_id` / `p_user_id` field. The body value
      must not appear in the RPC arguments at all.
    - **Fail-closed (G10 re-cut):** with `adminClientThrows.value = true`, the response is **503**
      with body `code === "SEAM_MISCONFIGURED"`, and `rpcMock` was **never** called — i.e. nothing
      was submitted. ⛔ Not a 200, not a 500, and not a success by any other path.
    - The existing **venue-identity-fence** cases that rely on `adminClientThrows` keeping the
      fence dark are preserved as-is where they still describe the fence; only the RPC expectation
      moves.
  </behavior>
  <action>
Move `rpc` off the `@/lib/supabase/server` mock and onto the object `createAdminClient()` returns
in the `@/lib/supabase/admin` mock. Leave a `rpc` on the server mock that throws an error whose
message names the wrong client (G9's ⭐ note) and assert in at least one case that it is not
reached. Extend the admin mock's returned object so it still supplies `from(table).select()` for
the venue fence — this mock now serves two consumers.

Rewrite the `:215-238` docblock: the "must degrade to a dark fence and never to a failed submit"
sentence is scoped explicitly to the **venue-identity fence** and a new sentence states the
opposite for the RPC, naming Phase 156 and CONNECT-03. Do not delete the sentence — the fence
behaviour it describes is unchanged and still tested.

Add the five behaviour cases above with names containing the literal token `156` so they can be
grepped out of a failure list in a `node_modules`-broken worktree.

⛔ Change **no** route file in this task. ⛔ Add **no** member to the wizard code union.
Mock-hygiene convention: extend via `importActual` rather than replacing, matching
`composite/add-key/route.test.ts:87-98`, so a mock cannot drift from real behaviour.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/create-with-key/route.test.ts --no-file-parallelism 2>&1 | tee /tmp/156-02-t1.txt; grep -c '156' /tmp/156-02-t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - The named new cases **FAIL** against the unchanged route, and the failure output is recorded
      verbatim in the plan summary — a bare non-zero exit code is NOT acceptance (broken-worktree
      hazard)
    - `grep -n 'vi.mock("@/lib/supabase/admin"' src/app/api/strategies/create-with-key/route.test.ts`
      returns a mock whose `createAdminClient` return value exposes **both** `rpc` and `from`
    - The `@/lib/supabase/server` mock's `rpc` throws, and at least one case asserts it was not called
    - A case asserts `p_exchange === ` the `validateKey` first argument (identity, not a literal)
    - A case sends a body containing a conflicting `user_id` and asserts the RPC argument object's
      `p_user_id` equals the `withAuth` user id and that the body value appears in no RPC argument
    - A case asserts `status === 503` and `body.code === "SEAM_MISCONFIGURED"` with `rpcMock` not called
    - `git diff --name-only` shows **only** this test file
    - `grep -rn 'SEAM_MISCONFIGURED' src/lib/wizardErrors.ts` still returns the pre-existing member;
      `EXPECTED_TABLE_SIZE` at `wizardErrors.test.ts:1486` and `:1750` is unchanged
  </acceptance_criteria>
  <done>The single-key route's post-156 contract is written down as failing tests, and the wrong-client and silent-fallback outcomes are each independently detectable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give the composite twin the admin mock it has never had, and the same three contracts</name>
  <files>src/app/api/strategies/composite/add-key/route.test.ts</files>
  <read_first>
    - `src/app/api/strategies/composite/add-key/route.test.ts:80-120` — the `@/lib/ratelimit`
      extend-don't-replace convention (`:87-98`) and the server-only mock (`:107-114`) whose
      comment states the route "calls ONLY supabase.rpc"
    - `src/app/api/strategies/create-with-key/route.test.ts` — the file just re-cut in Task 1; the
      composite assertions are its twin and must not diverge in shape
    - `src/app/api/strategies/composite/add-key/route.ts:42-67` — the file's own
      "STRUCTURAL MIRROR … exactly three intentional divergences" contract
    - `src/app/api/strategies/composite/add-key/route.ts:255-270, :380-405` — the
      `SEAM_MISCONFIGURED` shape already present here, and the single `.rpc` call site
  </read_first>
  <behavior>
    - Identical to Task 1's five cases, against `add_wizard_composite_key` and `p_wizard_session_id`.
    - Additionally: **every existing composite case still passes for its original reason.** Today
      this file has no `@/lib/supabase/admin` mock at all; once plan 04 makes the route call
      `createAdminClient()`, the real implementation throws for missing env and **every** test in
      the file reds for the wrong reason, drowning the real signal (G11 / Pitfall 6).
  </behavior>
  <action>
Add a `vi.mock("@/lib/supabase/admin", …)` with the same `adminClientThrows` switch shape Task 1
uses, and move `rpc` onto its returned object. Keep a throwing `rpc` on the
`@/lib/supabase/server` mock and update that mock's comment — the sentence "calls ONLY
supabase.rpc" stops being true of the user-scoped client and becomes true of the admin client.

Port the five behaviour cases verbatim in shape, with the composite argument names. ⭐ The twin
must not diverge: `composite/add-key/route.ts:42-67` declares that every behaviour not on its
three-item divergence list mirrors the sibling. The service-role writer is not on that list, so it
lands identically — and if this plan deliberately introduces a divergence, it is added to that
list in plan 04 rather than left implicit.

⛔ Change **no** route file in this task.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/composite/add-key/route.test.ts --no-file-parallelism 2>&1 | tee /tmp/156-02-t2.txt; grep -c '156' /tmp/156-02-t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '@/lib/supabase/admin' src/app/api/strategies/composite/add-key/route.test.ts`
      returns a `vi.mock` factory — the file has none today
    - The five new cases exist with the same names as Task 1's, differing only in the RPC name and
      the composite argument names, and each **FAILS** against the unchanged route with the failure
      output recorded verbatim
    - Pre-existing composite cases still pass (compare the pass count against the pre-change run
      and state both numbers in the summary)
    - The `@/lib/supabase/server` mock comment no longer claims the user-scoped client is the one
      that calls `.rpc`
    - `git diff --name-only` shows **only** this test file
  </acceptance_criteria>
  <done>The composite twin has the mock and the contract its sibling has, so plan 04 cannot land a single-key-only fix — the defect class this phase exists to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → wizard route | Untrusted body fields cross here; `user.id` must come from `withAuth`, never the body |
| route → Postgres RPC | After plan 04 this crosses as `service_role`, which has BYPASSRLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-05 | Elevation of Privilege | `p_user_id` sourced from the request body | mitigate | A dedicated case sends a conflicting body `user_id` and asserts it reaches no RPC argument (CONNECT-03b) |
| T-156-06 | Elevation of Privilege | A user-scoped fallback surviving in the route | mitigate | The server mock's `rpc` throws and is asserted un-called; the 503 case asserts `rpcMock` was never reached |
| T-156-07 | Denial of Service | Missing `SUPABASE_SERVICE_ROLE_KEY` 500s a connect | mitigate | Asserted as 503 + `SEAM_MISCONFIGURED`, whose authored copy explicitly does not blame the user's key |
| T-156-08 | Repudiation | A green test in a `node_modules`-less worktree read as proof | mitigate | Case names carry the `156` token and acceptance requires grepping names out of the failure list, never a bare exit code |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this phase; no install task exists |
</threat_model>

<verification>
- Both files' new cases FAIL for their named reasons against unchanged routes
- No file outside `src/app/api/strategies/**/route.test.ts` is modified
- `npx tsc --noEmit` is clean (a red test is expected; a type error is not)
- `npm run lint` reports no new errors
</verification>

<success_criteria>
The post-156 route contract exists as executable, currently-failing assertions on **both** twins,
including the two outcomes that would otherwise ship silently: the user-scoped fallback and the
body-supplied uid.
</success_criteria>

<output>
Create `.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-02-SUMMARY.md` when done
</output>
