---
phase: 156-connect-refactor-the-venue-the-server-validated-is-the-venue
plan: 07
type: execute
wave: 6
landing: PR-B
depends_on: ["156-06"]
files_modified:
  - supabase/migrations/20260814120000_wizard_rpcs_revoke_authenticated.sql
  - supabase/schema/functions/create_wizard_strategy.sql
  - supabase/schema/functions/add_wizard_composite_key.sql
autonomous: false
requirements: [CONNECT-01, CONNECT-03, CONNECT-04, CONNECT-05]

must_haves:
  truths:
    - "authenticated holds no EXECUTE on either wizard RPC"
    - "Neither RPC body contains auth.uid() — the relaxed-guard silent no-op is structurally impossible"
    - "Both bodies gate on auth.role() = 'service_role' and nothing wider"
    - "service_role still holds EXECUTE on both — the outage guard survives the narrowing"
    - "The attested_venue column comment still contains the 20260811210000 marker AND carries this migration's id"
    - "The CHECK api_keys_attested_venue_matches_exchange is still present and convalidated"
  artifacts:
    - path: "supabase/migrations/20260814120000_wizard_rpcs_revoke_authenticated.sql"
      provides: "the SC1 change — withdraw authenticated EXECUTE, narrow both bodies to service_role"
      contains: "REVOKE"
      min_lines: 250
  key_links:
    - from: "supabase/migrations/20260814120000_wizard_rpcs_revoke_authenticated.sql"
      to: "supabase/tests/test_api_keys_exchange_not_user_writable.sql"
      via: "the column comment's gate marker, which arms or SKIPs the entire 5a–5e block"
      pattern: "20260811210000"
---

<objective>
Ship **Migration B**: withdraw `authenticated` EXECUTE from both wizard RPCs and narrow both bodies
to a `service_role`-only gate carrying **zero** `auth.uid()`. This is the change that closes
CONNECT-01.

Purpose: the grant layer is the only door a browser cannot walk around — PostgREST exposes every
`public`-schema function, so an ASVS V4 control enforced only in the route is enforced only in the
tier the caller can skip. The in-body gate is defence-in-depth for the day someone re-grants.

⛔ **Precondition:** `156-LIVE-ACCEPTANCE.md` must read `status: pass`. If it does not, stop.

Output: one new migration applied to TEST, plus both regenerated function snapshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-RESEARCH.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-PATTERNS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md

⛔ **THE GATE-MARKER TRAP — read this before touching any COMMENT.**
`supabase/tests/test_api_keys_exchange_not_user_writable.sql:242-253` gates its **entire 5a–5e
block** on a bare `col_description(api_keys.attested_venue) LIKE '%20260811210000%'`. This migration
re-stamps that comment. **If the re-stamp drops the 14-digit substring `20260811210000`, the whole
block falls through to `RAISE NOTICE 'SKIP (5)'` at `:419-421` — a NOTICE, exit code 0, green CI,
zero coverage** on the very assertions this phase exists to add. The re-stamped text must
**preserve `20260811210000`** and **add this migration's own id**, and the new id becomes the gate
for plan 08's new 5f/5g so the two markers cross-check.

⛔ **COMMENT ORDERING IS MEASURED, NOT STYLISTIC.** `20260811210000:702-712` records it:
*"MEASURED on a local PG16 fixture 2026-08-12: in the old order, a re-stamp with the marker removed
COMMITTED while (d) reported '20260811210000 marker preserved'."* Every `COMMENT ON` statement in
this file sits **before** the `DO $verify$` block, or the verify reads the comment it just wrote.

⛔ **D-156-1's transitional arm is DELETED here.** `156-PATTERNS.md` § 1's verdict now applies at
full force: the precedent `log_audit_event_service` admits
`auth.role() IN ('authenticated','service_role')` at `20260515113753:148`, and `authenticated` is
**the exact caller this phase exists to lock out**. Admitting it in-body would make the
defence-in-depth layer a permanent no-op — a future GRANT leak would then pass *both* layers. Carry
the precedent's `auth.role()` choice, its `BEGIN … EXCEPTION` fail-closed wrapper, its self-contained
REVOKE/GRANT re-assertion and its `pg_get_functiondef` body canary. **Reject its width. Use
`<> 'service_role'`.** Rule 7: pick one, say why, do not average.

⛔ **PROJECT CONSTRAINTS.** Never `supabase db push`. TEST = `qmnijlgmdhviwzwfyzlc`, PROD =
`khslejtfbuezsmvmtsdn`; merging `supabase/migrations/**` auto-applies to PROD. A shipped migration is
never edited — `20260810120000`, `20260811210000`, `20260812083206` **and Migration A** are all
applied and read-only. `CREATE OR REPLACE`, never `DROP`+`CREATE`. `npm run schema:functions` +
commit both snapshots in the same commit. MCP is stripped from subagents ⇒ Task 2 is a blocking
checkpoint.

<interfaces>
**Re-base ground truth: `supabase/schema/functions/*.sql` as regenerated by plan 03** — their
`-- source migration:` headers now name Migration A. ⛔ Re-basing on anything older reverts either
Migration A's gate or, worse, PR #675's `attested_venue` stamp (Pitfall 8), and every MT5 finalize
then answers a permanent `KEY_SCOPE_CHECK_UNAVAILABLE`.

Signature literals — ⚠️ asymmetric arity, and the analog at `20260811210000:869-882` carries the
**stale 11-arg** form of the first:
- `public.create_wizard_strategy(uuid,text,text,text,text,text,text,text,integer,text,uuid,text)` — 12
- `public.add_wizard_composite_key(uuid,text,text,text,text,text,text,text,integer,text,uuid)` — 11

**The final in-body shape** (both bodies, identical): declare `v_jwt_role TEXT`, set it inside a
`BEGIN … EXCEPTION WHEN OTHERS THEN v_jwt_role := NULL; END` wrapper around `auth.role()`, then
`IF v_jwt_role IS DISTINCT FROM 'service_role' THEN RAISE … USING ERRCODE = 'insufficient_privilege'`,
then `IF p_user_id IS NULL THEN RAISE … USING ERRCODE = 'invalid_parameter_value'`.
⛔ **Zero occurrences of `auth.uid()`** — not a relaxed comparison, not a commented-out one. Post-verify
(e) greps `pg_get_functiondef` for the literal string.
⛔ **Never `current_user` / `session_user`** (Trap C — `current_user` in a SECDEF body is the owner,
so the gate always passes; the bug that made `prevent_profile_role_change` a no-op).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Migration B — the service_role-only gate, the REVOKE, and the re-stamped prose</name>
  <files>supabase/migrations/20260814120000_wizard_rpcs_revoke_authenticated.sql</files>
  <read_first>
    - `supabase/schema/functions/create_wizard_strategy.sql` and
      `supabase/schema/functions/add_wizard_composite_key.sql` **as regenerated by plan 03** — the
      verbatim re-base source
    - The Migration A file plan 03 authored — its two-arm gate is the region being replaced, and its
      post-verify (e) polarity is the one being inverted
    - `supabase/migrations/20260515113753_log_audit_event_service_hardened.sql:130-160`, `:200-205`,
      `:245-268` — the gate, the compact REVOKE/GRANT re-assertion, the body canary
    - `supabase/migrations/20260811210000_api_keys_attested_venue.sql:702-712` (comment ordering),
      `:713-747` (the current `attested_venue` column comment — the text being re-stamped, whose
      first line carries the gate marker), `:865-943` (post-verify (e)/(f)/(g))
    - `supabase/migrations/20260812083206_api_keys_venue_account_id.sql:735-780` — the DROP+CREATE
      hazard text and the `COMMENT ON FUNCTION create_wizard_strategy` text carrying
      *"⛔ `p_venue_account_id` is NOT VALIDATED here and `authenticated` can call this RPC directly"*
    - `supabase/tests/test_api_keys_exchange_not_user_writable.sql:242-265` and `:415-421` — the
      marker gate, the 5a positive control, and the SKIP branch this must not trigger
    - `.claude/agents/migration-reviewer.md` invariants 1, 2, 3, 11, 14, **17**, **19**, 20, 21
    - `.claude/agents/rls-policy-auditor.md` — BYPASSRLS and the `auth.role()` handling section
  </read_first>
  <action>
Write `supabase/migrations/20260814120000_wizard_rpcs_revoke_authenticated.sql` in this order.

**§0 Header.** State that this is landing 2 of 2 and the SC1 change; name Migration A as its
precondition and `156-LIVE-ACCEPTANCE.md` as the evidence that it was live before this shipped.
Include ⛔ blocks for:
(i) **the G7/G8 re-apply hazard, now ARMED**: `20260811210000:869-882` and `20260812083206:861-868`
post-verify that `authenticated` **HAS** EXECUTE. On an ordered fresh replay they run before this
file and pass; but **re-applying either of them after this migration will ABORT.** Say so, and name
`docs/runbooks/migration-failure.md` as the place to look if it happens.
(ii) `20260811210000` §1b (`:225-290`, incl. `:277`) is **superseded by this file** and must never be
edited (invariant 11).
(iii) **The honest ceiling** — what this does NOT buy: any server route holding
`createAdminClient()` can still pass any uid. That is the standing `service_role` trust boundary
(ADR-0001/ADR-0003), identical to `log_audit_event_service(p_user_id, …)` which the Python worker
calls with a uid of its choosing. What changed is *"any browser session can forge an attestation"* →
*"only our own server code can"*. ⛔ Write **"the venue is the one this server observed a successful
read-only authentication at"**, never "the venue cannot be forged".
(iv) **IN-04, restated accurately**: these RPCs run as their **owner** (`postgres`), not as
`service_role`, so the scrub trigger's `service_role` allowlist entry is **still unused**; this
migration does not become its beneficiary and does not remove it.
(v) **Where the ownership check now lives**: the DB no longer checks it. `p_user_id` is `withAuth`'s
`getUser()`-verified `user.id` (ADR-0022 layer 2) and the grant layer is what makes that
trustworthy. Name the route-level unit assertion as the surviving control.

**§1 / §2 `CREATE OR REPLACE`** both functions, re-based **verbatim** from the plan-03 snapshots,
with only the guard block replaced by the final shape in `<interfaces>`. Preserve byte-for-byte:
`SECURITY DEFINER`, `SET search_path = public, pg_catalog`, `SET lock_timeout = '3s'`, the
`wizdraft:` / `wizcomposite:` advisory-lock fences, the select-existing replay fence with its
red-team MEDIUM-1 NULL-`api_key_id` note, the lazy composite-draft creation, and both INSERT stamps
(`attested_venue` from `p_exchange`, `NULLIF(btrim(p_venue_account_id), '')`).

Write the in-body comment that plan 03 promised: the `authenticated` arm is deleted, why
`auth.uid()` is absent rather than relaxed (Trap B — a relaxed comparison is a **permanent silent
no-op** under `service_role`; the ownership check would vanish with no error and no test failure),
and why `current_user` is not used (Trap C).

**§3 Privileges.** For **both** signatures, the compact self-contained re-assertion:
`REVOKE ALL ON FUNCTION … FROM PUBLIC;` `REVOKE ALL ON FUNCTION … FROM anon;`
`REVOKE ALL ON FUNCTION … FROM authenticated;` `GRANT EXECUTE ON FUNCTION … TO service_role;`
⛔ The `GRANT … TO service_role` is re-issued here even though Migration A issued it — the file must
be self-contained, exactly as `20260515113753:200-205` argues, and forgetting it is a total outage.

**§4 `COMMENT ON COLUMN public.api_keys.attested_venue`** — the SC5 re-strengthening.
⛔ **The re-stamped text MUST contain the literal `20260811210000`** (the 5a–5e gate marker) **and**
this migration's own 14-digit id. ⛔ It must also retain the two sentences 153.6 put there for
reasons that outlive this phase: **NULL means PROBE (fail-toward)**, and **never fall back to
`api_keys.exchange`**. Upgrade only the writer clause: written only by the two wizard RPCs, which
only `service_role` may invoke, from the venue the route observed a successful read-only
authentication at.

**§5 `COMMENT ON FUNCTION`** for both, replacing the
*"authenticated can call this RPC directly"* clause carried at `20260812083206:772-775`.
⚠️ `p_venue_account_id` is **RESTATED, not closed**: only the server can pass it (true now); the
value still has **no in-database oracle** (still false). Do not silently upgrade it.

**§6 `DO $verify$`** — ⛔ **after** every `COMMENT ON`. Abort on:
(a) `has_function_privilege('authenticated', <sig>, 'EXECUTE')` is **TRUE** — both (the SC1 assertion);
(b) `has_function_privilege('anon', <sig>, 'EXECUTE')` is TRUE — both;
(c) `has_function_privilege('service_role', <sig>, 'EXECUTE')` is **FALSE** — both (the outage guard);
(d) either body lacks its advisory-lock substring or lacks `attested_venue` (the stale-re-base canary);
(e) either body lacks `auth.role()` **or contains `auth.uid()`** — ⚠️ **this polarity is the inverse
of Migration A's (e); say so in the comment**, because a reader diffing the two files will otherwise
"correct" it;
(f) either function is not `prosecdef`, or its `pg_get_userbyid(proowner)` is outside the scrub
trigger's allowlist (re-assert `20260811210000:891-912`);
(g) `api_keys_attested_venue_matches_exchange` is absent or not `convalidated` (CONNECT-04);
(h) `col_description` on `api_keys.attested_venue` does **not** contain `20260811210000`, **or** does
not contain this migration's id — the gate-marker guard.

⚠️ **Invariant 21:** every `RAISE` format string is a single literal or dollar-quoted (`$msg$…$msg$`,
tag distinct from `$verify$`); a `||`-concatenated `RAISE` fails at apply with 42601 and blocks every
later migration. ⚠️ **Invariant 14:** no `BEGIN`/`COMMIT`/`ROLLBACK`. ⚠️ **Invariant 16:** zero
tool-call XML artifacts.
⚠️ **Invariant 17 — this file DOES contain `REVOKE EXECUTE … FROM authenticated`, so pre-empt the
reviewer with the analysis written into the header:** neither wizard RPC is called by any trigger
function. `api_keys` carries two SECURITY INVOKER scrub triggers and `strategies` carries
`guard_wizard_draft_updates_trigger`; none of them `PERFORM`s either wizard RPC, so no DML by any
role acquires a 42501 from this REVOKE. State the greps that establish it.
⚠️ **Invariant 19** — this migration **removes** a SECDEF-EXECUTE-to-`authenticated` grant. Note it
as the finding being *closed*, so a reviewer does not read the diff backwards.
  </action>
  <verify>
    <automated>M=supabase/migrations/20260814120000_wizard_rpcs_revoke_authenticated.sql; grep -vE '^\s*--' $M | grep -c 'REVOKE ALL ON FUNCTION' | grep -qx 6 && grep -vE '^\s*--' $M | grep -c 'GRANT EXECUTE ON FUNCTION' | grep -qx 2 && grep -q '20260811210000' $M && ! grep -qE '</?(content|invoke|function_calls|antml:|parameter)' $M && ! grep -vE '^\s*--' $M | grep -qE '^\s*(BEGIN|COMMIT|ROLLBACK)\s*;' && test "$(grep -c 'SET search_path = public, pg_catalog' $M)" -eq 2 && test "$(awk '/COMMENT ON/{c=NR} /DO \$verify\$/{v=NR} END{print (c<v)?"ok":"bad"}' $M)" = ok</automated>
  </verify>
  <acceptance_criteria>
    - Exactly **6** `REVOKE ALL ON FUNCTION` (PUBLIC / anon / authenticated × 2 signatures) and
      exactly **2** `GRANT EXECUTE … TO service_role` on non-comment lines
    - Neither function body contains `auth.uid()`; both contain `auth.role()` — verified by
      inspecting the two `CREATE OR REPLACE` bodies in this file, not the whole file
    - Neither body contains `current_user` or `session_user` as a role test
    - Both bodies retain `SECURITY DEFINER`, `SET search_path = public, pg_catalog`,
      `SET lock_timeout = '3s'`, their advisory-lock fence, and their `attested_venue` stamp
    - The `COMMENT ON COLUMN … attested_venue` text contains the literal `20260811210000`, this
      migration's own 14-digit id, the NULL-means-PROBE sentence and the never-fall-back-to-`exchange`
      sentence
    - Every `COMMENT ON` statement precedes the `DO $verify$` block (asserted by line number)
    - Post-verify covers (a)–(h) and (e)'s comment explicitly states its polarity is the inverse of
      Migration A's
    - No `DROP FUNCTION`; no `BEGIN`/`COMMIT`/`ROLLBACK`; no tool-call XML; every `RAISE` format
      string is a single literal or dollar-quoted
    - The header contains the invariant-17 REVOKE analysis, the G7/G8 re-apply warning, the honest
      ceiling sentence, and the IN-04 restatement
  </acceptance_criteria>
  <done>The grant layer is shut on both twins, the in-body gate is service_role-only with no uid comparison left to silently pass, and the comment that arms the SQL gate still carries its marker.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Apply Migration B to TEST and read back the stamped version</name>
  <action>⛔ CHECKPOINT — no autonomous work in this task. Present the verification steps below to the operator and WAIT. Do not substitute an automated approximation, do not proceed on an assumed result, and do not mark this task complete without the pasted output.</action>
  <what-built>Task 1 authored `supabase/migrations/20260814120000_wizard_rpcs_revoke_authenticated.sql`. It has been applied to nothing.</what-built>
  <how-to-verify>
    1. ⛔ Confirm the target is **TEST `qmnijlgmdhviwzwfyzlc`**, not PROD. ⛔ Never `supabase db push`.
    2. Apply the file **verbatim** via MCP `apply_migration` — drive the shipped bytes, do not retype.
    3. Read back the stamped version:
       `SELECT version, name FROM supabase_migrations.schema_migrations ORDER BY version DESC LIMIT 3;`
    4. Confirm the effect:
       `SELECT has_function_privilege('authenticated','public.create_wizard_strategy(uuid,text,text,text,text,text,text,text,integer,text,uuid,text)','EXECUTE') AS cws_auth, has_function_privilege('authenticated','public.add_wizard_composite_key(uuid,text,text,text,text,text,text,text,integer,text,uuid)','EXECUTE') AS awck_auth, has_function_privilege('service_role','public.create_wizard_strategy(uuid,text,text,text,text,text,text,text,integer,text,uuid,text)','EXECUTE') AS cws_sr, has_function_privilege('service_role','public.add_wizard_composite_key(uuid,text,text,text,text,text,text,text,integer,text,uuid)','EXECUTE') AS awck_sr;`
       Expected: **false / false / true / true**.
    5. Confirm the gate marker survived:
       `SELECT col_description('public.api_keys'::regclass, (SELECT attnum FROM pg_attribute WHERE attrelid='public.api_keys'::regclass AND attname='attested_venue' AND NOT attisdropped)) LIKE '%20260811210000%' AS marker_ok;`
       Expected: **true**. A `false` here means plan 08's entire 5a–5e block would SKIP silently —
       stop and fix Task 1.
    6. Paste the stamped version, the four booleans and `marker_ok`.
  </how-to-verify>
  <done>The operator ran every listed step and pasted the raw results back; the following task records them.</done>
  <resume-signal>Paste the stamped `version`, the four privilege booleans and `marker_ok`, or describe the apply failure</resume-signal>
</task>

<task type="auto">
  <name>Task 3: PR-Y2 rename and regenerate both snapshots</name>
  <files>supabase/migrations/20260814120000_wizard_rpcs_revoke_authenticated.sql, supabase/schema/functions/create_wizard_strategy.sql, supabase/schema/functions/add_wizard_composite_key.sql</files>
  <read_first>
    - `.github/workflows/migration-drift-check.yml` — the PR-Y2 remedy text and the
      `PRE_EXISTING_DRIFT` arm
    - `.github/workflows/migration-policy.yml` — the backdated guard: rename **forward** only
    - The renamed Migration A filename from plan 03 — Migration B's prefix must sort **after** it
  </read_first>
  <action>
`git mv` the migration so its 14-digit prefix equals the version pasted in Task 2, keeping the
`_wizard_rpcs_revoke_authenticated.sql` suffix. ⚠️ Verify the new prefix sorts **after** Migration
A's renamed prefix and after `20260812083206`.

Run `npm run schema:functions` and commit both regenerated snapshots in the **same commit** as the
migration. Confirm each snapshot's `-- source migration:` header now names this file, that neither
snapshot body contains `auth.uid()`, and that both still contain their advisory-lock fence and
`attested_venue` stamp.

Run `npm run schema:functions:check` and `npx tsc --noEmit`. ⛔ Do not hand-edit either snapshot — if
the generator output surprises you, the migration is wrong.
  </action>
  <verify>
    <automated>ls supabase/migrations/*_wizard_rpcs_revoke_authenticated.sql && npm run schema:functions:check && npx tsc --noEmit && ! grep -q 'auth.uid()' supabase/schema/functions/create_wizard_strategy.sql && ! grep -q 'auth.uid()' supabase/schema/functions/add_wizard_composite_key.sql && grep -q 'pg_advisory_xact_lock' supabase/schema/functions/create_wizard_strategy.sql && grep -q 'attested_venue' supabase/schema/functions/add_wizard_composite_key.sql</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one file matches `supabase/migrations/*_wizard_rpcs_revoke_authenticated.sql`, prefix
      equal to the Task-2 version and sorting after Migration A's
    - Neither regenerated snapshot contains `auth.uid()`
    - Both snapshots retain `pg_advisory_xact_lock`, their distinct lock namespace, and `attested_venue`
    - Both `-- source migration:` headers name this migration
    - `npm run schema:functions:check` exits 0; `npx tsc --noEmit` clean;
      `src/lib/database.types.ts` unmodified
    - Migration and both snapshots in ONE commit
  </acceptance_criteria>
  <done>The declared canonical bodies match the DDL that just landed, so `sql-function-snapshot` CI is green and the next re-base starts from the right place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PostgREST `/rest/v1/rpc/*` | Every `public`-schema function is exposed; the GRANT is the only door the browser cannot walk around |
| PostgREST → SECDEF body | Only the JWT role claim is trustworthy; `current_user` is the owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-31 | Elevation of Privilege | Direct PostgREST RPC invocation bypassing the route's checks | mitigate | `REVOKE ALL … FROM authenticated` on both signatures; post-verify (a) aborts if either still holds EXECUTE |
| T-156-32 | Elevation of Privilege | The relaxed uid guard that never fires under `service_role` (Trap B) | mitigate | Zero `auth.uid()` in either body; post-verify (e) greps `pg_get_functiondef` for its absence; the snapshot check re-proves it outside the migration |
| T-156-33 | Elevation of Privilege | An in-body gate widened to `IN ('authenticated','service_role')` | mitigate | `IS DISTINCT FROM 'service_role'` mandated; the precedent's width is rejected in writing with its reason |
| T-156-34 | Elevation of Privilege | `current_user` used as the role test ⇒ always-pass | mitigate | Trap C cited with both in-repo precedents; acceptance forbids `current_user`/`session_user` as a role test |
| T-156-35 | Denial of Service | `service_role` losing EXECUTE during the narrowing | mitigate | The GRANT is re-issued self-containedly; post-verify (c) aborts; Task 2 step 4 measures it on TEST |
| T-156-36 | Denial of Service | 42501 for every DML from a trigger calling a revoked helper (invariant 17) | accept | Analysed and written into the header: neither wizard RPC is called by any trigger function; the three relevant triggers `PERFORM` neither |
| T-156-37 | *(control failure)* | The re-stamp drops the `20260811210000` marker ⇒ 5a–5e SKIP, green CI, zero coverage | mitigate | Post-verify (h) asserts both markers; Task 2 step 5 measures `marker_ok` on TEST before plan 08 relies on it |
| T-156-38 | Denial of Service | Re-applying `20260811210000` or `20260812083206` after this migration aborts | accept | Documented in the header with the runbook pointer — the abort is correct behaviour, it just must not surprise anyone |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this phase; no install task exists |
</threat_model>

<verification>
- On TEST: `authenticated` = false/false, `service_role` = true/true, `marker_ok` = true
- Neither regenerated snapshot contains `auth.uid()`; both retain their fence and stamp
- `npm run schema:functions:check` 0; `npx tsc --noEmit` clean
- ⛔ `supabase db push` never run; no applied migration edited
</verification>

<success_criteria>
`authenticated` cannot reach either wizard RPC at the grant layer, cannot pass the in-body gate if it
somehow did, and the column comment that arms the SQL gate still carries the marker that keeps that
gate from silently skipping.
</success_criteria>

<output>
Create `.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-07-SUMMARY.md` when done
</output>
