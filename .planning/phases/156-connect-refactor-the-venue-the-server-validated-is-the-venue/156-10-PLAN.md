---
phase: 156-connect-refactor-the-venue-the-server-validated-is-the-venue
plan: 10
type: execute
wave: 8
landing: PR-B
depends_on: ["156-08", "156-09"]
files_modified:
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
  - src/app/api/strategies/create-with-key/route.ts
  - src/app/api/strategies/composite/add-key/route.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - CHANGELOG.md
  - VERSION
  - package.json
autonomous: true
requirements: [CONNECT-01, CONNECT-02, CONNECT-03, CONNECT-04, CONNECT-05]

must_haves:
  truths:
    - "Every site 153.6 had to weaken now states what is true — and states the ceiling, not more"
    - "The PARITY-04 threat_flag: deferred-control is cleared, with the evidence that permits clearing it"
    - "venue_account_id and IN-04 are RESTATED accurately, neither silently upgraded nor deleted"
    - "A repo grep for the weakened claim returns only negated or historical usage"
    - "The full suite is green, including sql-tests observed green (not grey) in CI against TEST"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PARITY-04 closed; CONNECT-01..05 marked complete with evidence"
      contains: "CONNECT-01"
    - path: "CHANGELOG.md"
      provides: "the landing-2-of-2 entry that closes the control"
  key_links:
    - from: "src/app/api/strategies/finalize-wizard/route.ts"
      to: "api_keys.attested_venue"
      via: "the probe-gate comment that told readers not to upgrade the claim"
      pattern: "attested"
---

<objective>
Close the loop: re-strengthen every claim 153.6 had to weaken, clear the PARITY-04
`threat_flag: deferred-control`, and prove the phase with one full-suite gate.

Purpose: CR-01's finding was never only a code defect — it was *"a control whose actual guarantee is
an undocumented coincidence, shipped under a written claim of privilege."* Fixing the code and
leaving the prose understated recreates the same class in the opposite direction: the next engineer
re-derives from a comment that no longer describes the system, and the security posture becomes
folklore again.

⛔ The symmetric failure is **over**-claiming. The ceiling is *"the venue is the one this server
observed a successful read-only authentication at"* — **never** "the venue cannot be forged".

Output: corrected prose at five sites, closed ledger rows, and a green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-RESEARCH.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-PATTERNS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md

⛔ **`20260811210000` §1b (P2) CANNOT BE EDITED.** It is an applied migration;
`migration-reviewer` invariant 11 makes any edit an immediate CRITICAL. Plan 07's migration header
supersedes it **by name**. The temptation is real — this plan is where it will be felt.

⛔ **Do NOT weaken any assertion while updating prose (P6).** In
`finalize-wizard/route.test.ts` the `[153.6-04 / PARITY-04]` docblock and the `describe` title
describe the narrower guarantee. The **words** change; the **assertions** do not. If a wording change
seems to require an assertion change, stop — the assertion was describing something else.

⚠️ **PARITY-04 may only flip to ✅ once CI has OBSERVED the new assertions green.** Its own text says
*"Goes fully ✅ when CI observes assertion 5 green."* A grey `sql-tests` is not an observation
(Pitfall 9 — shared `shared-test-db` concurrency group, cancelled jobs render grey, branch protection
is deferred on this repo).

⚠️ **GSD worktree agents get NO `node_modules`** — symlink before trusting a result.
⚠️ **CI runs Node 22, local Node 25** — a CI-only vitest failure is not a flake.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-strengthen the five prose sites, to exactly what is true and no further</name>
  <files>src/app/api/strategies/finalize-wizard/route.ts, src/app/api/strategies/finalize-wizard/route.test.ts, src/app/api/strategies/create-with-key/route.ts, src/app/api/strategies/composite/add-key/route.ts</files>
  <read_first>
    - `src/app/api/strategies/finalize-wizard/route.ts:1200-1230` — **P5**, the paragraph saying
      *"it is NOT a venue the server independently validated… Do not upgrade this comment to
      'server-validated' without the deferred connect-flow refactor that would make it true"*
    - `src/app/api/strategies/finalize-wizard/route.ts:1245-1265` — the ⛔ *"NEVER `?? apiKeyExchange`"*
      paragraph that must stay **byte-intact**
    - `src/app/api/strategies/finalize-wizard/route.test.ts` — the `[153.6-04 / PARITY-04]` docblock
      and `describe` title (**P6**)
    - `src/app/api/strategies/create-with-key/route.ts:100-110` and `:765-785` — **P7**, as plan 04
      left them (transitional wording)
    - `src/app/api/strategies/composite/add-key/route.ts` — its twin transitional paragraph
    - The Migration B header authored by plan 07 — its honest-ceiling and IN-04 wording, so the code
      prose and the DDL prose agree word for word rather than approximately
  </read_first>
  <action>
**P5 — `finalize-wizard/route.ts:1200-1230`.** Phase 156 *is* the refactor that paragraph named.
Rewrite it to state: `attested_venue` is written only by the two wizard RPCs, which only
`service_role` may invoke, from the venue the route observed a successful read-only authentication
at; a browser cannot reach those RPCs over PostgREST. **Keep the CHECK sentence** — the coupling
remains an independently useful fence against a future writer (CONNECT-04), not a redundancy that
this phase retires.
⛔ Keep `:1245-1265` byte-intact: a null attestation is still a legacy row or a scrubbed client
INSERT, and a fallback there would still make the whole thing a no-op for every row that has one.

**P6 — `finalize-wizard/route.test.ts`.** Update the docblock and the `describe` title to the new
guarantee and add a `[156 / CONNECT-01]` reference. ⛔ Change no assertion, no expected value, and no
case name that another gate greps for.

**P7 — `create-with-key/route.ts:100-110` and `:765-785`, plus the composite twin.** Move plan 04's
transitional wording to final: `authenticated` EXECUTE **is** withdrawn; only the server can reach
these RPCs.
⚠️ For `p_venue_account_id`, **RESTATE precisely, do not upgrade**: 156 closes the *reachability*
half — only the server can pass the value — and the value still has **no in-database oracle**, so
"the venue confirmed it" remains false. Keep it recorded as an open CR-01-class residual at exactly
that scope.

**The ceiling sentence**, worded identically across all three route files and Migration B's header:
what the server observed is *"these credentials authenticated read-only at the venue named"*. For
every ccxt venue the adapter is selected by that venue string, so claiming `okx` with Binance
credentials fails; for `mt5` the call goes through the MT5 gateway, which will not accept ccxt
credentials. It is **not** a claim about where the user's capital actually sits. ⛔ Any server route
holding `createAdminClient()` can still pass any uid — the standing `service_role` trust boundary
(ADR-0001 / ADR-0003), identical to `log_audit_event_service(p_user_id, …)`.

**Then grep the repo for the weakened claim** and confirm every surviving occurrence is negated,
historical (a `.planning/` artifact, a CHANGELOG entry, an applied migration), or inside
`20260811210000` itself. This is 153.6-07's own method — *"a repo grep for the claim now returns only
the negated usage"*. Record the grep command and its output in the summary.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts src/app/api/strategies/create-with-key/route.test.ts src/app/api/strategies/composite/add-key/route.test.ts --no-file-parallelism && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `finalize-wizard/route.ts:1200-1230` no longer contains the "NOT a venue the server
      independently validated" claim, and `:1245-1265` is byte-unchanged (prove with `git diff -U0`)
    - `finalize-wizard/route.test.ts` shows **zero** changed lines matching `expect(`, `toBe(`,
      `toEqual(` or `toHaveBeenCalled` — only comments, the docblock and the `describe` title moved
    - All three route files carry the identical ceiling sentence, and it matches Migration B's header
    - The `p_venue_account_id` paragraph states the reachability half closed **and** that no
      in-database oracle exists; it does not claim the venue was confirmed
    - The repo grep for the weakened claim returns only negated/historical usage, with the command
      and output pasted in the summary
    - All three route test suites pass; `tsc` clean; `lint` 0 errors
  </acceptance_criteria>
  <done>Every comment a future engineer will re-derive from describes the system that now exists, at its real strength and not one notch above.</done>
</task>

<task type="auto">
  <name>Task 2: Close the ledgers — PARITY-04's threat_flag, CONNECT-01..05, and the landing-2 changelog</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, CHANGELOG.md, VERSION, package.json</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the PARITY-04 requirement entry and its Traceability row (which
      carries `threat_flag: deferred-control` and the sentence *"Goes fully ✅ when CI observes
      assertion 5 green"*), and the CONNECT-01..05 entries + Traceability rows minted at planning
    - `.planning/ROADMAP.md` — the Phase 156 block, its five Success Criteria, the Progress table row
      and the Requirement Coverage table
    - `.planning/STATE.md` — Current Position, Roadmap Evolution, Decisions
    - `CHANGELOG.md` — the plan-05 "landing 1 of 2" entry this one completes
    - `.planning/phases/156-.../156-LIVE-ACCEPTANCE.md` and the plan 08/09 summaries — the evidence
      that permits clearing the flag
  </read_first>
  <action>
**PARITY-04.** Replace the ⛔ ACCEPTED RESIDUAL paragraph with the closure, and **clear
`threat_flag: deferred-control`**, naming the two migrations, the inverted 5d, the new 5f/5g, and the
CI run that observed them green. ⛔ Do not delete the residual's history — restate it as *closed by
Phase 156* so the reasoning that produced remedy (b) survives for whoever reads the migration chain.
Flip the checkbox to `[x]` only if the CI observation actually happened; if `sql-tests` was grey,
leave it and say so.

**CONNECT-01..05.** Flip each checkbox and fill each Traceability row with the concrete evidence:
migration filenames, the SQL assertion names (5d / 5f / 5g / 5a′ / 5a″), the guard test path, and the
live-acceptance date. ⚠️ Two rows carry deliberate qualifications that must survive: CONNECT-05's
`venue_account_id` restatement (reachability half only) and its IN-04 restatement (the RPCs run as
**owner**, so the `service_role` allowlist entry is still unused).

**ROADMAP.** Mark Phase 156 complete, set the Progress table row to `10/10`, replace the `TBD`
Requirements line with `CONNECT-01..05`, add the phase to the Requirement Coverage table, and record
in Notes that the phase shipped as **two PRs** with the live gate between them — this is the worked
example a future privilege change will copy.

**STATE.** Update Current Position, add the phase to Roadmap Evolution, and record the decisions this
phase actually took, at minimum: **D-156-1** (the transitional two-arm gate — branched, never
unioned — because a `service_role`-only body in Migration A would refuse the still-live old deploy);
**D-156-2** (the live PROD gate sits *between* the two landings, so PR B cannot convert a
working-but-open state into a broken-and-closed one); **D-156-3** (`SEAM_MISCONFIGURED` at 503 reused
rather than a new code minted, because `EXPECTED_TABLE_SIZE` pins and PARITY-05's ledger police that
union); **D-156-4** (`156-PATTERNS.md`'s rejection of the precedent's `IN (...)` width — Rule 7, pick
one and say why).

**Close PR B.** Bump `VERSION` and `package.json` in lockstep in ONE commit and add the
`CHANGELOG.md` entry: landing 2 of 2, `authenticated` EXECUTE withdrawn, CR-01 remedy (a) complete,
PARITY-04's deferred control closed. ⛔ Do not claim the venue cannot be forged.

⛔ Never bundle a commit with edits: edit → verify (grep/tsc) → commit, each its own step.
⚠️ These four `.planning` ledgers are TRACKED and previously destroyed once by a `head|sed` pipe —
edit them in place, never regenerate them, and spot-check line counts before and after.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts --no-file-parallelism && test "$(cat VERSION)" = "$(node -p "require('./package.json').version")" && ! grep -q 'threat_flag: deferred-control' .planning/REQUIREMENTS.md && test "$(grep -c 'CONNECT-0' .planning/REQUIREMENTS.md)" -ge 10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'threat_flag: deferred-control' .planning/REQUIREMENTS.md` returns 0
    - All five CONNECT checkboxes are `[x]` and each Traceability row names a migration file, a SQL
      assertion, and a date
    - PARITY-04's history is restated as closed, not deleted, and its checkbox flipped **only** if a
      green (not grey) `sql-tests` run was observed — otherwise the row says so explicitly
    - CONNECT-05's row still carries the `venue_account_id` reachability-only qualification and the
      IN-04 "still unused" statement
    - ROADMAP Phase 156 shows `10/10`, `CONNECT-01..05`, and the two-PR note
    - STATE records D-156-1 through D-156-4 by name
    - `cat VERSION` equals `package.json`'s version, changed in ONE commit; the CHANGELOG entry says
      "landing 2 of 2" and does not claim unforgeability
    - Line counts of the four `.planning` ledgers grew (never shrank) — state before/after in the summary
  </acceptance_criteria>
  <done>The deferred control that shipped live on PROD in PR #675 is recorded closed with the evidence that closed it, and the qualifications that are still true are still written down.</done>
</task>

<task type="auto">
  <name>Task 3: The phase gate — full suite, and the twin-pairing checklist with nothing left implicit</name>
  <files>.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-10-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/156-.../156-PATTERNS.md` § "Shared Patterns — A. Twin-pairing" — the eight-row
      checklist, including the four rows that had **no twin** at plan time
    - `.planning/phases/156-.../156-RESEARCH.md` § "Validation Architecture — Sampling Rate" — the
      phase-gate definition
    - Every `156-*-SUMMARY.md` produced so far, for the counts to reconcile
  </read_first>
  <action>
Run the phase gate and reconcile every number rather than reporting a bare pass:
`npm run lint`, `npx tsc --noEmit`, `npx vitest run --coverage` (thresholds 82/80/74/72),
`npm run schema:functions:check`, and the four SQL gate files under
`/opt/homebrew/opt/postgresql@16/bin/psql -v ON_ERROR_STOP=1` against a fully-migrated throwaway
PG16 fixture. Then confirm `sql-tests` is **green in CI against TEST** — ⛔ grey is not a pass; re-run
it (Pitfall 9).

Write the **twin-pairing table** into the summary with a file:line for every row, and an explicit
verdict for the four rows that had no twin at plan time:
`route test admin mock` (plan 02), `SQL gate 5d/5f-5g` (plan 08), `stale-re-base canary` (plan 09),
and `MUTATING_RPC_NAMES` — the last of which is **deliberately still missing** and must be recorded
as **logged to `TODOS.md`, not fixed** (Rule 3), with its TODOS entry cited. ⛔ A row may be marked
closed only with the file:line that closes it; "done" without a coordinate is not acceptance.

Finally, state the two things this phase did **not** close, so the next reader does not have to
rediscover them: the `asset_class` stamp still reads the forgeable `apiKeyExchange`, and
`p_venue_account_id` has no in-database oracle. Both are in `TODOS.md`; cite the entries.

⚠️ If a coverage threshold regressed, the new fail-closed `catch` arms are untested — that is a real
gap in this phase's own posture, not a threshold to lower.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm run schema:functions:check && npx vitest run --coverage</automated>
  </verify>
  <acceptance_criteria>
    - Full vitest green with all four coverage thresholds clear; pass/skip counts stated and
      reconciled against the earlier plan summaries
    - `tsc` clean; `lint` 0 errors; `schema:functions:check` exits 0
    - All four SQL gate files run clean against a fully-migrated PG16 fixture with **no `SKIP (5`** line
    - `sql-tests` observed **green** in CI against TEST, with the run linked — grey explicitly rejected
    - The twin-pairing table has a file:line for every closed row, and `MUTATING_RPC_NAMES` is marked
      logged-not-fixed with its `TODOS.md` citation
    - The two un-closed residuals are stated with their `TODOS.md` citations
  </acceptance_criteria>
  <done>The phase is proven by an executed gate rather than an assembled one, and every twin either has a coordinate or an explicit, cited reason for not having one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| written claim → next engineer's derivation | An understated or overstated comment becomes tomorrow's design input |
| CI signal → release decision | A grey gate is indistinguishable from a green one unless explicitly rejected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-51 | Repudiation | Over-claiming ("the venue cannot be forged") | mitigate | One ceiling sentence, worded identically in three route files and the migration header; acceptance requires the `p_venue_account_id` qualification to survive |
| T-156-52 | Repudiation | Clearing `threat_flag: deferred-control` without an observed green gate | mitigate | Acceptance permits the flip only on an observed-green (not grey) `sql-tests` run, mirroring PARITY-04's own wording |
| T-156-53 | Tampering | Editing applied migration `20260811210000` §1b to fix stale prose | mitigate | Context block quotes invariant 11; the correction lives in Migration B's header, which supersedes §1b by name |
| T-156-54 | Tampering | Weakening an assertion while "updating prose" | mitigate | Acceptance requires zero changed `expect`/`toBe`/`toEqual`/`toHaveBeenCalled` lines in the finalize-wizard test |
| T-156-55 | Repudiation | Marking a twin row done without a coordinate | mitigate | Task 3 forbids a closed row without a file:line and requires the `MUTATING_RPC_NAMES` row to cite its TODOS entry |
| T-156-56 | Tampering | Regenerating a `.planning` ledger with a pipe and truncating it | mitigate | Edit in place; before/after line counts stated in the summary |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this phase; no install task exists |
</threat_model>

<verification>
- All five prose sites re-strengthened; `:1245-1265` byte-intact; zero assertion changes in the
  finalize-wizard test
- `threat_flag: deferred-control` gone from `.planning/REQUIREMENTS.md`; CONNECT-01..05 all `[x]`
  with evidence
- `VERSION` equals `package.json` version, one commit, CHANGELOG says landing 2 of 2
- Full suite + coverage green; four SQL gates clean with no `SKIP (5`; `sql-tests` green in CI
</verification>

<success_criteria>
`api_keys.attested_venue` is what its name claims — written by the server, from the venue the server
validated, reachable by nothing else — and every comment, requirement and ledger row says exactly
that, with the two residuals that remain named rather than buried.
</success_criteria>

<output>
Create `.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-10-SUMMARY.md` when done
</output>
