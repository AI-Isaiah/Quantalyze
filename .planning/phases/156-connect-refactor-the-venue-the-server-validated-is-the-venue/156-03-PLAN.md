---
phase: 156-connect-refactor-the-venue-the-server-validated-is-the-venue
plan: 03
type: execute
wave: 2
landing: PR-A
depends_on: ["156-01"]
files_modified:
  - supabase/migrations/20260813120000_wizard_rpcs_service_role_writer.sql
  - supabase/schema/functions/create_wizard_strategy.sql
  - supabase/schema/functions/add_wizard_composite_key.sql
  - supabase/tests/test_wizard_composite_fence.sql
autonomous: false
requirements: [CONNECT-03, CONNECT-04]

must_haves:
  truths:
    - "service_role can EXECUTE both wizard RPCs — without this, PR A is a total connect-a-key outage"
    - "authenticated can STILL EXECUTE both, and its auth.uid() ownership check still fires, for the whole PR-A window"
    - "Neither body lost its advisory-lock fence, its attested_venue stamp, its SECDEF attributes or its search_path"
    - "The CHECK api_keys_attested_venue_matches_exchange is still present and convalidated"
    - "The migration is applied to TEST before merge and its filename matches the stamped remote version"
    - "The branched gate is verified by SHAPE, not by intent: a flat union cannot satisfy the acceptance criteria or post-verify (e2)"
    - "create_wizard_strategy gains a CI-RUN cross-user assertion — it has NONE today, so a dropped ownership raise would land on PROD unobserved"
    - "All four existing SQL gate files are measured green under Migration A, not assumed to be"
  artifacts:
    - path: "supabase/migrations/20260813120000_wizard_rpcs_service_role_writer.sql"
      provides: "transitional two-arm role gate + GRANT EXECUTE TO service_role on both RPCs"
      contains: "GRANT EXECUTE"
      min_lines: 250
    - path: "supabase/schema/functions/create_wizard_strategy.sql"
      provides: "regenerated canonical snapshot naming the new migration as its source"
    - path: "supabase/schema/functions/add_wizard_composite_key.sql"
      provides: "regenerated canonical snapshot naming the new migration as its source"
    - path: "supabase/tests/test_wizard_composite_fence.sql"
      provides: "Part 3c — the create_wizard_strategy cross-user twin of Part 3b, running in the sql-tests CI job"
      contains: "Part 3c"
  key_links:
    - from: "supabase/migrations/20260813120000_wizard_rpcs_service_role_writer.sql"
      to: "service_role"
      via: "GRANT EXECUTE on both wizard RPC signatures"
      pattern: "GRANT EXECUTE ON FUNCTION public\\.(create_wizard_strategy|add_wizard_composite_key)"
    - from: "supabase/schema/functions/create_wizard_strategy.sql"
      to: "supabase/migrations/20260813120000_wizard_rpcs_service_role_writer.sql"
      via: "-- source migration: header regenerated by npm run schema:functions"
      pattern: "source migration: 20260813120000"
    - from: "supabase/tests/test_wizard_composite_fence.sql"
      to: "public.create_wizard_strategy"
      via: "Part 3c presents a JWT sub that differs from p_user_id and requires insufficient_privilege"
      pattern: "Part 3c"
---

<objective>
Ship **Migration A**: both wizard RPCs accept a `service_role` caller and `service_role` gains
`EXECUTE`, **while `authenticated` keeps both its grant and its full `auth.uid()` ownership check.**

Purpose: this is the zero-outage half of the two-migration split. `CONTRIBUTING.md` §2 — *"the merge
IS the apply"* — and the Vercel build fires on the same merge **with no ordering between them**.
Whichever wins, a single-migration phase breaks every connect-a-key for the width of the window:
deploy-first hits a `service_role` with no EXECUTE; migration-first refuses the still-live old
deploy's `authenticated` client. Migration A is deliberately **additive** so both clients work at
every instant of that window.

Output: one new migration applied to TEST, plus the two regenerated function snapshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-RESEARCH.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-PATTERNS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-VALIDATION.md

⛔ **D-156-1 — THE TRANSITIONAL GATE, AND WHY IT IS NOT THE SHAPE `156-PATTERNS.md` REJECTS.**

`156-PATTERNS.md` § 1 is emphatic that `log_audit_event_service`'s
`auth.role() NOT IN ('authenticated','service_role')` width must be **rejected**, because admitting
`authenticated` in-body makes the defence-in-depth layer a permanent no-op. **That verdict is about
the FINAL body, which plan 07 installs.** It is correct and it stands.

Migration A cannot install the final body, because during the PR-A merge window the **old deploy is
still live and still calls with the user-scoped client**. A `service_role`-only body gate would
refuse it — reproducing the exact outage the split exists to prevent.

So Migration A installs a **two-arm** gate, which is not the rejected shape:

- `service_role` arm → proceed (the new writer).
- `authenticated` arm → **the existing `auth.uid()` check runs unchanged**: NULL uid raises, and
  `auth.uid() <> p_user_id` raises. The cross-user elevation guard (T-88-03) is fully intact.
- anything else (including a NULL/absent role claim) → raise `insufficient_privilege`.

⛔ **The shape that must NOT be written is the flat union** — a single
`IF v_role NOT IN ('authenticated','service_role') THEN RAISE` with the uid check deleted. That
would delete the ownership guard for the `authenticated` caller *while `authenticated` still holds
EXECUTE*, opening cross-user elevation for the width of the window. That is strictly worse than the
residual this phase closes. The arms must be **branched**, never unioned.

**Migration A is safe to sit at indefinitely** — it is a strict superset of today's caller set with
today's guarantees for today's caller. It does not close CONNECT-01; plan 07 does.

⛔ **PROJECT CONSTRAINTS.**
- **NEVER `supabase db push`.** Merging `supabase/migrations/**` to `main` **auto-applies to PROD**
  (`khslejtfbuezsmvmtsdn`). New migrations go to TEST (`qmnijlgmdhviwzwfyzlc`) via MCP
  `apply_migration` **before** merge. MCP is stripped from subagents ⇒ Task 2 is a blocking checkpoint.
- **A shipped migration is NEVER edited.** `20260810120000`, `20260811210000`, `20260812083206` are
  applied to PROD **and** TEST ⇒ **read-only**. `migration-reviewer` invariant 11 makes any edit an
  immediate CRITICAL. `20260811210000` §1b reads as if 156 is still deferred — ⛔ **do not edit it**;
  supersede it **by name** in this migration's header (Pitfall 10).
- **`CREATE OR REPLACE`, never `DROP`+`CREATE`.** No signature changes here.
  `20260812083206:739-747`: *"DROP DESTROYS [the ACL]. A freshly created function's default ACL
  grants EXECUTE to PUBLIC… That is a privilege ESCALATION introduced by the act of dropping,
  silently, with no error."* `CREATE OR REPLACE` also preserves the `COMMENT` and keeps
  `test_api_keys_venue_identity_uniq.sql:41`'s signature pin valid.
- **`npm run schema:functions` + commit both snapshots in the SAME commit as the migration** (G14 —
  153.6-07 was bitten by exactly this).

<interfaces>
**Re-base ground truth — read ONLY from the snapshots, whose `-- source migration:` header names the
latest source (Pitfall 8: a stale re-base silently reverts PR #675 and every MT5 finalize then
answers a permanent `KEY_SCOPE_CHECK_UNAVAILABLE`).**

| Function | Snapshot | source migration | Params |
|---|---|---|---|
| `create_wizard_strategy` | `supabase/schema/functions/create_wizard_strategy.sql` (142 lines) | `20260812083206_api_keys_venue_account_id.sql` | **12** |
| `add_wizard_composite_key` | `supabase/schema/functions/add_wizard_composite_key.sql` (105 lines) | `20260811210000_api_keys_attested_venue.sql` | **11** |

Exact signature literals for every `has_function_privilege` / `COMMENT ON FUNCTION` / `REVOKE` /
`GRANT` in this migration — ⚠️ the analog at `20260811210000:869-882` carries the **stale 11-arg**
`create_wizard_strategy` literal; copying it verbatim `regprocedure`-errors or addresses nothing:
- `public.create_wizard_strategy(uuid,text,text,text,text,text,text,text,integer,text,uuid,text)`
- `public.add_wizard_composite_key(uuid,text,text,text,text,text,text,text,integer,text,uuid)`

**Current ACL (confirm against `156-MEASUREMENTS.md` A4 before writing):**

| Function | PUBLIC | anon | authenticated | service_role |
|---|---|---|---|---|
| `create_wizard_strategy` | REVOKEd `20260812083206:748` | REVOKEd | **GRANTed** `:752` | **none** |
| `add_wizard_composite_key` | REVOKEd `20260710180000:146` | REVOKEd | **GRANTed** `:149` | **none** |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Migration A — transitional two-arm gate + GRANT to service_role</name>
  <files>supabase/migrations/20260813120000_wizard_rpcs_service_role_writer.sql</files>
  <read_first>
    - `supabase/schema/functions/create_wizard_strategy.sql` (all 142 lines) and
      `supabase/schema/functions/add_wizard_composite_key.sql` (all 105 lines) — the verbatim
      re-base source
    - `supabase/migrations/20260515113753_log_audit_event_service_hardened.sql:130-160` (the
      `auth.role()` + `BEGIN…EXCEPTION` fail-closed wrapper), `:200-205` (the self-contained
      REVOKE/GRANT re-assertion), `:245-268` (the `pg_get_functiondef` body-substring canary)
    - `supabase/migrations/20260811210000_api_keys_attested_venue.sql:702-712` (the MEASURED
      comment-ordering rule), `:865-943` (post-verify (e)/(f)/(g) shapes), `:515-552` (the
      SECURITY INVOKER scrub trigger and why DEFINER would be a no-op)
    - `supabase/migrations/20260812083206_api_keys_venue_account_id.sql:735-780` (the DROP+CREATE
      hazard text and the `COMMENT ON FUNCTION` re-stamp)
    - `supabase/migrations/20260411144407_compute_jobs_queue.sql:285-325` — `_assert_owner`, the
      shape to NOT write (`IF v_auth_uid IS NULL THEN RETURN; -- service-role path, skip the check`)
    - `.claude/agents/migration-reviewer.md` invariants 1, 2, 3, 11, 14, 17, 19, 20, 21
    - `.claude/agents/rls-policy-auditor.md` — BYPASSRLS, SECDEF + `search_path`, `auth.role()` handling
  </read_first>
  <action>
Write `supabase/migrations/20260813120000_wizard_rpcs_service_role_writer.sql` in this order — the
order is load-bearing.

**§0 Header.** State: what (a service-role writer for both wizard RPCs), why (CR-01 remedy (a),
closing PARITY-04's `threat_flag: deferred-control`), and a ⛔ block covering:
(i) the two-migration split and the named follow-up migration that withdraws `authenticated`
EXECUTE — with the sentence that **this file alone does not close CONNECT-01**;
(ii) the D-156-1 transitional gate and the ⛔ warning that the arms are **branched, not unioned**;
(iii) `20260811210000` §1b (`:225-290`, incl. `:277`) is **superseded by this file** and must never
be edited (invariant 11);
(iv) the G7/G8 re-apply hazard: `20260811210000:869-882` and `20260812083206:861-868` post-verify
that `authenticated` **HAS** EXECUTE — after the follow-up migration lands, re-applying either of
those files out of order would abort;
(v) **IN-04, restated accurately**: these RPCs run as their **owner** (`postgres`), not as
`service_role`, so the scrub trigger's `service_role` allowlist entry remains **unused**. This phase
does not become its beneficiary and does not remove it.

**§1 `CREATE OR REPLACE FUNCTION public.create_wizard_strategy(<12 args>)`.** Re-based **verbatim**
from the snapshot. Preserve byte-for-byte: `SECURITY DEFINER`, `SET search_path = public,
pg_catalog`, `SET lock_timeout = '3s'`, the `pg_advisory_xact_lock(hashtext('wizdraft:' || …))` F6
fence, the select-existing replay fence with its red-team MEDIUM-1 NULL-`api_key_id` note, and the
INSERT's `attested_venue` and `NULLIF(btrim(p_venue_account_id), '')` stamps.

**The ONLY region that changes** is the `DECLARE`/guard block. Declare `v_jwt_role TEXT` and set it
inside a `BEGIN … EXCEPTION WHEN OTHERS THEN v_jwt_role := NULL; END` wrapper around `auth.role()`
(carried from `20260515113753`; strictly safer than a bare `COALESCE` and composes with it), keep
`v_auth_uid UUID := auth.uid()`, then branch:
`IF v_jwt_role = 'service_role' THEN` (no-op / proceed)
`ELSIF v_jwt_role = 'authenticated' THEN` the two existing raises, **unchanged in text and
ERRCODE** — NULL uid raises `insufficient_privilege`, `v_auth_uid <> p_user_id` raises
`insufficient_privilege` naming both values;
`ELSE RAISE … USING ERRCODE = 'insufficient_privilege'` naming the observed role.
Then a `p_user_id IS NULL` check raising `invalid_parameter_value`.

⛔ **THE BRANCH IS CHECKED BY SHAPE, NOT BY INTENT.** Write these three literals exactly, in
**both** bodies — the acceptance criteria and post-verify (e2) grep for them:
- `IF v_jwt_role = 'service_role' THEN` — at least one per body
- `ELSIF v_jwt_role = 'authenticated' THEN` — at least one per body, spelled `ELSIF`, never a
  second free-standing `IF`
- `v_auth_uid <> p_user_id` governing a `RAISE` within two lines

⛔ **THE SHAPE COUNTS ARE BOUND PER FUNCTION, NOT FILE-WIDE.** This task's verify command slices the
non-comment text at the first `GRANT EXECUTE ON FUNCTION` (§3 begins after both bodies), splits the
remainder on the two `CREATE OR REPLACE FUNCTION` boundaries, and requires **each half** to carry
all three literals. A file-wide count of 2 is **not** sufficient: a body carrying two duplicate
`ELSIF` arms in ONE function while the other took the union satisfies a file-wide `≥ 2` and ships
the flat union in the function nobody counted.

⛔ **`v_jwt_role` may NEVER appear as the operand of `IN (` or `NOT IN (` — in ANY case.** ⚠️ SQL
is case-insensitive, so a lowercase `not in (` is the identical defect and evades a case-sensitive
grep entirely; the anti-union grep therefore runs `-i`. That one token is
what collapses the branched gate into the flat union D-156-1 forbids. Read this forbidden body and
check it against the looser criteria:

  `v_auth_uid UUID := auth.uid();` (declared, never compared) +
  `IF v_jwt_role NOT IN ('authenticated','service_role') THEN RAISE …`

It contains `auth.role()`. It contains `auth.uid()`. It has no `auth.uid() IS NULL THEN RETURN`. It
has no `IS NOT NULL AND … <> p_user_id`. It satisfies **every** criterion the loose form checks —
while deleting the cross-user guard for a role that still holds EXECUTE for the whole PR-A window.
**A declaration is not a comparison.** The same hole opens by accident if a 142-line verbatim
re-base simply drops the `v_auth_uid <> p_user_id` raise from the `authenticated` arm.

⛔ Use `auth.role()`, never `current_user` and never `session_user` (Trap C — inside a SECDEF body
`current_user` is the **owner**, so the gate always passes; this is the bug that made
`prevent_profile_role_change` a no-op, named at `20260811210000:518-523` and `20260411103316:313-321`.
`session_user` is `authenticator` for every PostgREST request).
⛔ Do **not** write `IF v_auth_uid IS NULL THEN RETURN` or
`IF v_auth_uid IS NOT NULL AND v_auth_uid <> p_user_id THEN RAISE` — under `service_role` both are
permanent silent no-ops (Trap B).

Write an in-body comment block stating the transitional status, naming the follow-up migration, and
recording that the `authenticated` arm is deleted there.

**§2** the same for `add_wizard_composite_key(<11 args>)`, re-based from its snapshot with the
**distinct** `wizcomposite:` advisory-lock space and the lazy composite-draft creation preserved.

**§3 Privileges — the outage guard.** For **both** signatures:
`REVOKE ALL ON FUNCTION … FROM PUBLIC;` `REVOKE ALL ON FUNCTION … FROM anon;`
`GRANT EXECUTE ON FUNCTION … TO service_role;`
⛔ **Do NOT revoke from `authenticated` in this file** — that is the follow-up migration's entire
content, and doing it here re-creates the outage window.
⛔ `GRANT EXECUTE … TO service_role` is **mandatory**: `service_role` has no EXECUTE today and is
not a member of `authenticated` (Pitfall 3 — omitting it is a total connect-a-key outage, both arms,
every venue).

**§4 `COMMENT ON FUNCTION`** for both, re-stamping the transitional truth. ⛔ Do **not** re-stamp
`COMMENT ON COLUMN public.api_keys.attested_venue` in this file — its current text
(`20260811210000:713-745`) is still **accurate** while `authenticated` holds EXECUTE, and every
comment touch risks the `LIKE '%20260811210000%'` gate marker. The column comment is plan 07's.

**§5 `DO $verify$` post-verify** (⚠️ this section must sit **after** §4 — `20260811210000:702-712`
records the MEASURED failure of the other order: a marker-dropping re-stamp COMMITTED while the
verify reported "marker preserved"). Abort on:
(a) `has_function_privilege('service_role', <sig>, 'EXECUTE')` is FALSE — **both** (the outage guard);
(b) `has_function_privilege('authenticated', <sig>, 'EXECUTE')` is FALSE — **both** (PR-A must not
withdraw it early);
(c) `has_function_privilege('anon', <sig>, 'EXECUTE')` is TRUE — both;
(d) either body lacks its advisory-lock substring (`wizdraft:` / `wizcomposite:`) or lacks
`attested_venue` — the stale-re-base canary, shaped on `test_api_keys_venue_identity_uniq.sql:198-208`;
(e) either body lacks `auth.role()`, **or lacks `auth.uid()`** — in Migration A the uid check is
required to still be present, because the `authenticated` arm still needs it. ⚠️ This polarity is
the **opposite** of plan 07's and the comment must say so explicitly, or a reader will "fix" it;
(e2) **the ownership COMPARISON is present, not merely the function call** — for **both** functions
`pg_get_functiondef` must contain the substring `v_auth_uid <> p_user_id` **and** the substring
`ELSIF v_jwt_role = 'authenticated'`. ⭐ Shape this exactly like (d)'s advisory-lock canary so the
migration **ABORTS AT APPLY** instead of committing a body whose `auth.uid()` is a dead
declaration. (e) on its own is fully satisfied by `v_auth_uid UUID := auth.uid();` with no
comparison anywhere in the body — that is the flat union, shipped, with post-verify reporting
success.
⛔ **Write (e2) UNROLLED — one explicit `pg_get_functiondef` assertion per function, never a loop
over a signature array.** This task's verify command slices this file from `DO $verify$` to EOF and
requires **two** occurrences of each literal inside that slice; a loop form satisfies the intent but
not the count, and the count is what runs. ⚠️ Inside a PL/pgSQL `LIKE` pattern the quotes are
**doubled** (`'%ELSIF v_jwt_role = ''authenticated''%'`); the slice grep is written
`-ciE "ELSIF v_jwt_role = '{1,2}authenticated'{1,2}"` so it matches the doubled form as well as the
single — do not "fix" the doubling to satisfy it;
(f) either function is not `prosecdef` or its `pg_get_userbyid(proowner)` is outside the scrub
trigger's allowlist (re-assert `20260811210000:891-912`);
(g) `api_keys_attested_venue_matches_exchange` is absent or not `convalidated` (CONNECT-04 — the
fence this phase must be seen not to have dropped).

⚠️ **Invariant 21:** every `RAISE` format string is a **single literal** or dollar-quoted
(`$msg$…$msg$` with a tag that does not collide with `$verify$`). A `||`-concatenated `RAISE` fails
at apply with 42601 and blocks every later migration.
⚠️ **Invariant 14:** no `BEGIN`/`COMMIT`/`ROLLBACK`; `SET LOCAL` only.
⚠️ **Invariant 17 (pre-empt the reviewer):** write the analysis down — this file **grants** and does
not revoke from any role with DML traffic, `api_keys`' two scrub triggers and `strategies`'
`guard_wizard_draft_updates_trigger` call no function whose EXECUTE is touched here, and
`guard_wizard_draft_updates` only blocks `current_user = 'authenticated'` on UPDATE
(`20260411103316:341-347`), which a service-role UPDATE passes.
⚠️ **Invariant 16:** the file must contain zero tool-call XML artifacts.
  </action>
  <verify>
    <automated>M=supabase/migrations/20260813120000_wizard_rpcs_service_role_writer.sql; B=$(mktemp); F=$(mktemp); grep -vE '^[[:space:]]*--' "$M" > "$B"; awk '/GRANT EXECUTE ON FUNCTION/{exit} {print}' "$B" > "$F"; sed -n '/DO \$verify\$/,$p' "$M" > "$B.v"; grep -c 'GRANT EXECUTE ON FUNCTION' "$B" | grep -qx 2 && ! grep -q 'REVOKE.*FROM authenticated' "$B" && ! grep -qE '</?(content|invoke|function_calls|antml:|parameter)' "$M" && ! grep -qE '^[[:space:]]*(BEGIN|COMMIT|ROLLBACK)[[:space:]]*;' "$B" && grep -c 'SET search_path = public, pg_catalog' "$M" | grep -qx 2 && ! grep -qiE "v_jwt_role[^;]*\bNOT +IN *\(|v_jwt_role[^;]*\bIN *\(" "$B" && test "$(grep -A2 'v_auth_uid <> p_user_id' "$B" | grep -c 'RAISE')" -ge 2 && test "$(grep -c 'CREATE OR REPLACE FUNCTION' "$F")" -eq 2 && for h in 1 2; do awk -v h="$h" '/CREATE OR REPLACE FUNCTION/{n++} n==h' "$F" > "$F.$h"; grep -q "ELSIF v_jwt_role = 'authenticated' THEN" "$F.$h" && grep -q "IF v_jwt_role = 'service_role' THEN" "$F.$h" && grep -q 'v_auth_uid <> p_user_id' "$F.$h" || exit 1; done && test "$(grep -c 'v_auth_uid <> p_user_id' "$B.v")" -ge 2 && test "$(grep -ciE "ELSIF v_jwt_role = '{1,2}authenticated'{1,2}" "$B.v")" -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - Exactly **2** `GRANT EXECUTE ON FUNCTION … TO service_role` statements on non-comment lines,
      one per signature, with the 12-arg / 11-arg literals respectively
    - **Zero** `REVOKE … FROM authenticated` on any non-comment line
    - Both bodies contain `auth.role()` **and** `auth.uid()`; both contain
      `SET search_path = public, pg_catalog`, `SECURITY DEFINER`, `SET lock_timeout = '3s'`
    - `grep -c 'pg_advisory_xact_lock'` ≥ 2 and both `wizdraft:` and `wizcomposite:` appear
    - `grep -c 'attested_venue'` shows the stamp survives in both INSERTs
    - Zero `auth.uid() IS NULL THEN RETURN` and zero
      `IS NOT NULL AND .* <> p_user_id` constructions
    - **Shape, not intent (the flat-union tell) — bound PER FUNCTION:** the non-comment text is
      truncated at the first `GRANT EXECUTE ON FUNCTION` and split on the two `CREATE OR REPLACE
      FUNCTION` boundaries; **each half** contains `ELSIF v_jwt_role = 'authenticated' THEN`,
      `IF v_jwt_role = 'service_role' THEN` and `v_auth_uid <> p_user_id` at least once. ⛔ A
      file-wide count of 2 does **not** satisfy this — two duplicate arms in one body while the
      other takes the union would pass a file-wide count
    - **Zero** occurrences of `v_jwt_role` as the operand of `IN (` or `NOT IN (` on any
      non-comment line, matched **case-insensitively** (`grep -i`) — lowercase `not in (` is the
      same defect and evades a case-sensitive grep
    - `v_auth_uid <> p_user_id` appears ≥ 2 times on non-comment lines, each within two lines of a
      `RAISE` — a bare `v_auth_uid UUID := auth.uid();` declaration does **NOT** satisfy this
    - Post-verify **(e2)** exists, is written **unrolled** (one assertion per function, not a loop),
      and greps `pg_get_functiondef` for BOTH `v_auth_uid <> p_user_id` and
      `ELSIF v_jwt_role = 'authenticated'` on **both** functions, aborting the apply if either
      substring is absent. ⛔ **Machine-checked, not prose-only:** this task's verify command slices the
      file from `DO $verify$` to EOF and requires ≥ 2 occurrences of `v_auth_uid <> p_user_id` and
      ≥ 2 of `ELSIF v_jwt_role = '{1,2}authenticated'{1,2}` (case-insensitive, tolerating the
      doubled quotes a PL/pgSQL `LIKE` pattern requires) inside that slice
    - No `COMMENT ON COLUMN public.api_keys.attested_venue` statement in this file
    - The `DO $verify$` block appears **after** every `COMMENT ON` statement
    - No `DROP FUNCTION`; no `BEGIN`/`COMMIT`/`ROLLBACK`; no tool-call XML artifact
    - Every `RAISE` format string is a single literal or dollar-quoted (no `||` before the first comma)
    - The header names the follow-up migration, names `20260811210000` §1b as superseded, and
      contains the invariant-17 analysis paragraph
  </acceptance_criteria>
  <done>Migration A exists, is additive at the ACL, keeps the cross-user guard alive for the still-live old deploy, and post-verifies its own outage guard.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Apply Migration A to TEST and read back the stamped version</name>
  <action>⛔ CHECKPOINT — no autonomous work in this task. Present the verification steps below to the operator and WAIT. Do not substitute an automated approximation, do not proceed on an assumed result, and do not mark this task complete without the pasted output.</action>
  <what-built>Task 1 authored `supabase/migrations/20260813120000_wizard_rpcs_service_role_writer.sql`. It has been applied to nothing — subagents have no MCP access.</what-built>
  <how-to-verify>
    1. ⛔ Confirm you are targeting **TEST `qmnijlgmdhviwzwfyzlc`**, never PROD `khslejtfbuezsmvmtsdn`.
       ⛔ Never run `supabase db push`.
    2. Apply the file **verbatim** via Supabase MCP `apply_migration`. Do not retype it; drive the
       shipped bytes. Expect the `$verify$` block's NOTICEs and a successful commit.
    3. Read back the stamped version:
       `SELECT version, name FROM supabase_migrations.schema_migrations ORDER BY version DESC LIMIT 3;`
       MCP stamps with `now()`, so the recorded version will **not** equal `20260813120000`.
    4. Confirm the effect:
       `SELECT has_function_privilege('service_role','public.create_wizard_strategy(uuid,text,text,text,text,text,text,text,integer,text,uuid,text)','EXECUTE') AS cws_sr, has_function_privilege('service_role','public.add_wizard_composite_key(uuid,text,text,text,text,text,text,text,integer,text,uuid)','EXECUTE') AS awck_sr, has_function_privilege('authenticated','public.create_wizard_strategy(uuid,text,text,text,text,text,text,text,integer,text,uuid,text)','EXECUTE') AS cws_auth;`
       Expected: **true / true / true** — all three. A `false` on the third means the outage-free
       property was lost and Task 1 must be corrected before proceeding.
    5. Paste the stamped version string and the three booleans.
  </how-to-verify>
  <done>The operator ran every listed step and pasted the raw results back; the following task records them.</done>
  <resume-signal>Paste the stamped `version` and the three privilege booleans, or describe the apply failure</resume-signal>
</task>

<task type="auto">
  <name>Task 3: PR-Y2 rename and regenerate both function snapshots</name>
  <files>supabase/migrations/20260813120000_wizard_rpcs_service_role_writer.sql, supabase/schema/functions/create_wizard_strategy.sql, supabase/schema/functions/add_wizard_composite_key.sql</files>
  <read_first>
    - `.github/workflows/migration-drift-check.yml` — its error text names the remedy verbatim
      ("rename the local file to match the remote `schema_migrations.version`. See PR-Y2 pattern")
      and its `PRE_EXISTING_DRIFT` arm compares pending remote versions against the PR's added files
    - `.github/workflows/migration-policy.yml` — the backdated-migration guard (invariant 2): the
      rename must move the timestamp **forward**, never backward, or the PR is blocked
    - `scripts/dump-sql-functions.ts` and both snapshot headers (`-- @generated … DO NOT EDIT BY HAND`)
  </read_first>
  <action>
`git mv` the migration file so its 14-digit prefix equals the version string pasted in Task 2,
keeping the `_wizard_rpcs_service_role_writer.sql` suffix. ⚠️ Verify the new prefix is **greater**
than the current remote tip (`20260812083206`) — a backward rename trips the backdated guard.

Then run `npm run schema:functions` and commit **both** regenerated snapshots in the **same commit**
as the migration (G14). Confirm each snapshot's `-- source migration:` header now names the renamed
file, and that both snapshots' bodies show the two-arm gate.

Run `npm run schema:functions:check` and `npx tsc --noEmit` — `src/lib/database.types.ts` needs no
regeneration because no signature changed (G13), and `tsc` is the check that proves it.

⛔ Do not hand-edit either snapshot. If `schema:functions` produces something unexpected, the
migration is wrong, not the generator.
  </action>
  <verify>
    <automated>ls supabase/migrations/*_wizard_rpcs_service_role_writer.sql && npm run schema:functions:check && npx tsc --noEmit && test "$(ls supabase/migrations/ | grep -c wizard_rpcs_service_role_writer)" -eq 1 && grep -q "source migration: $(ls supabase/migrations/ | grep wizard_rpcs_service_role_writer | cut -c1-14)" supabase/schema/functions/create_wizard_strategy.sql supabase/schema/functions/add_wizard_composite_key.sql</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one file matches `supabase/migrations/*_wizard_rpcs_service_role_writer.sql`, and its
      14-digit prefix equals the version pasted in Task 2
    - That prefix sorts **after** `20260812083206`
    - `npm run schema:functions:check` exits 0
    - Both snapshots' `-- source migration:` headers name the renamed file
    - Both snapshots' bodies contain `auth.role()` **and** `auth.uid()` and the branched arms
    - `npx tsc --noEmit` is clean and `src/lib/database.types.ts` is unmodified
    - The migration and both snapshots are in ONE commit
  </acceptance_criteria>
  <done>The repo filename and the remote `schema_migrations.version` agree, so `migration-drift-check` is green, and the generated snapshots are not one commit behind the DDL.</done>
</task>

<task type="auto">
  <name>Task 4: Mint the create_wizard_strategy cross-user twin (Part 3c) and run all four SQL gates under Migration A</name>
  <files>supabase/tests/test_wizard_composite_fence.sql</files>
  <read_first>
    - `supabase/tests/test_wizard_composite_fence.sql` in full (226 lines) — the `DECLARE` block at
      `:82-99` (`uid_wrong`, `uid_c`, `session_c`, `raised`, `err_msg`, `row_cnt` are all already
      there), **Part 3b at `:167-182`** (the exact shape to twin), Part 4 at `:184-220` (the
      `create_wizard_strategy` call idiom), and the closing claim reset at `:221`
    - `supabase/tests/test_wizard_session_idempotency.sql` in full (139 lines) — confirm for
      yourself that it contains **no** RPC call and **no** fixture transaction. That is why the twin
      lands in the fence file and not there
    - `.github/workflows/ci.yml`, the `sql-tests` job — the lane this assertion must actually run in
  </read_first>
  <action>
⛔ **WHY THIS TASK EXISTS — the gap that makes Migration A's own gates insufficient.**
`add_wizard_composite_key` **has** a CI-run cross-user assertion: `test_wizard_composite_fence.sql`
Part 3b (`:167-182`) presents `uid_wrong` in the JWT against `p_user_id = uid_a` and requires
`insufficient_privilege`. Plan 09 leaves Part 3b intact until PR B, so it is live for the whole PR-A
window. **`create_wizard_strategy` has nothing equivalent anywhere in `supabase/tests/`.**
Composite-fence Parts 4a/4b (`:194`, `:208`) do call it, but with a **matching** `sub`, asserting
`api_key_id` and F6 idempotency — they are blind to ownership. The only cross-user assertion for
the single-key RPC in the repo is `src/__tests__/wizard-rpcs-live-db.test.ts:211`, which is
`it.skipIf(!HAS_LIVE_DB)` and **never runs in CI** — plan 05 Task 2 says exactly that in its own
words, and forbids counting it toward any success criterion.

Consequence if this is skipped: a flat-union body, **or** a mis-transcription that simply drops the
`v_auth_uid <> p_user_id` raise from the `authenticated` arm during the 142-line verbatim re-base,
passes every other gate in this plan, lands on PROD (the merge **IS** the apply) while
`authenticated` still holds EXECUTE, and sits there until plan 06's browser pass and PR B. In that
window any browser session could POST `/rest/v1/rpc/create_wizard_strategy` with another user's
`p_user_id`. That is strictly worse than the residual this phase exists to close.

**Add Part 3c**, immediately after Part 3b and before Part 4, inside the same `DO $$` block:

1. Declare **one** new variable in the existing `DECLARE` block: `session_spoof UUID :=
   gen_random_uuid();`, distinct from `session_a` and `session_c` so a refused call cannot perturb
   Part 4b's `count(*) = 1` assertion for `(uid_c, session_c)`.
2. Take the before-count: `SELECT count(*) INTO row_cnt FROM public.api_keys WHERE user_id = uid_c;`
   into a dedicated variable (add a second `INTEGER` if `row_cnt` would be clobbered).
3. Set the ambient claim to `json_build_object('sub', uid_wrong::text, 'role', 'authenticated')` —
   the identical idiom Part 3b uses at `:171-172`.
4. Call `public.create_wizard_strategy(uid_c, 'binance', 'spoofed single', 'e', 's', 'p', 'd', 'n',
   1, 'spoof single', session_spoof)` inside a `BEGIN … EXCEPTION WHEN insufficient_privilege THEN
   raised := TRUE; err_msg := SQLERRM; END` bracket, matching Part 3b exactly.
   ⚠️ Eleven positional args against the 12-arg signature is **legal** — `p_venue_account_id`
   defaults NULL, and Part 4a already does this at `:194`. Keep it positional; do not "fix" it into
   an arity mismatch. The 12-arg signature pin lives at `test_api_keys_venue_identity_uniq.sql:41`.
5. Assert `raised` is TRUE, with a message naming **`create_wizard_strategy`** and **cross-user
   elevation (T-88-03)** — Part 3b's message shape. ⛔ Single literal or dollar-quoted, never
   `||`-concatenated.
6. Assert **nothing was minted**: re-count `api_keys` for `uid_c` and require the delta to be **0**.
   "An error was raised" is the weaker claim; plan 08 holds the same discipline for its own negatives.

Write the block comment in this file's voice and state plainly that Part 3c is the
`create_wizard_strategy` twin of Part 3b — that the two RPCs are the same door, and that this file
asserted only one of them until now.

⛔ Do **not** change Part 3a (`:54-71`), Part 3b (`:167-182`), Part 4 (`:184-220`), or the `role`
value of any existing claim site — plan 09 owns those in PR B.
⛔ Do **not** seed `uid_wrong` into `auth.users`: the call is refused before any FK is reached, and
seeding it would make the fixture assert an identity that does not need to exist.
⚠️ This file is edited again in PR B by `156-09-PLAN.md` Task 2, which re-cuts Part 3b **and Part
3c** together. That is deliberate and load-bearing: after Migration B both keep passing via the role
gate while the control they name no longer exists in the database — the precise defect class this
phase exists to end.

**Then run all four SQL gate files** against a PG16 fixture carrying **Migration A** (every file
under `supabase/migrations/**`, including the new one), so this plan's claim that *"PR A breaks no
existing gate"* is measured rather than asserted:
`test_wizard_composite_fence.sql`, `test_wizard_session_idempotency.sql`,
`test_api_keys_exchange_not_user_writable.sql`, `test_api_keys_venue_identity_uniq.sql`.
⚠️ All four must be **green under Migration A** — none of them is inverted until PR B. A red here
means Migration A broke a live gate, and Task 1 is what gets corrected, not the gate.
⚠️ `psql` is not on PATH: `/opt/homebrew/opt/postgresql@16/bin/psql -v ON_ERROR_STOP=1 -f <file>`
against a throwaway PG16 fixture (`initdb -U postgres`, **loopback TCP not a unix socket** — the
scratchpad path is 121 bytes against a 103-byte `sun_path` limit).
⛔ **`set -o pipefail` or `|| exit 1` on every psql invocation.** A `tee`'d pipeline returns
`tee`'s exit status and discards psql's, so an aborted assertion reads as a pass.
  </action>
  <verify>
    <automated>set -o pipefail; for f in supabase/tests/test_wizard_composite_fence.sql supabase/tests/test_wizard_session_idempotency.sql supabase/tests/test_api_keys_exchange_not_user_writable.sql supabase/tests/test_api_keys_venue_identity_uniq.sql; do /opt/homebrew/opt/postgresql@16/bin/psql -v ON_ERROR_STOP=1 -f "$f" "$PG16_FIXTURE_URL" || exit 1; done; grep -q 'Part 3c' supabase/tests/test_wizard_composite_fence.sql && grep -q 'session_spoof' supabase/tests/test_wizard_composite_fence.sql && grep -q 'T-88-03' supabase/tests/test_wizard_composite_fence.sql</automated>
  </verify>
  <acceptance_criteria>
    - `Part 3c` exists in `test_wizard_composite_fence.sql`, calls `public.create_wizard_strategy`
      with `sub` = `uid_wrong` and `p_user_id` = `uid_c`, and requires `insufficient_privilege`
    - Part 3c asserts an `api_keys` row delta of **0** for `uid_c` across the refused call
    - Part 3c uses a `session_spoof` distinct from `session_a` and `session_c`
    - `git diff` shows **zero** changed lines in Part 3a (`:54-71`), Part 3b (`:167-182`) and
      Part 4 (`:184-220`), and no existing claim site's `role` value changed
    - All four SQL gate files exit 0 against a PG16 fixture carrying Migration A, each invocation
      guarded by `set -o pipefail` or `|| exit 1`
    - **Falsifiability, observed and pasted:** on the fixture, `CREATE OR REPLACE` the
      `create_wizard_strategy` body with the flat union (`IF v_jwt_role NOT IN
      ('authenticated','service_role') THEN RAISE …`, `v_auth_uid` declared and never compared) →
      **Part 3c reds** naming cross-user elevation, and the call mints a row. Restore the correct
      body → green. ⭐ Paste both outputs: this is the proof that Task 1's shape criteria, post-verify
      (e2), and this twin catch the same defect from three independent directions.
  </acceptance_criteria>
  <done>The single-key RPC has the CI-run cross-user assertion its composite twin has always had, so a flat union — or a dropped raise — cannot reach PROD with a green board.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST → SECURITY DEFINER function | The JWT role claim is the only caller attribute the body can trust; `current_user` is the owner |
| migration merge → PROD | Merging `supabase/migrations/**` to `main` auto-applies to PROD with no ordering against the Vercel build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-09 | Elevation of Privilege | The flat-union gate (uid check deleted while `authenticated` holds EXECUTE) | mitigate | Three independent detectors: (i) Task 1 acceptance greps for the `ELSIF v_jwt_role = 'authenticated'` literal, for `v_auth_uid <> p_user_id` within two lines of a `RAISE`, and for **zero** `v_jwt_role IN (` / `NOT IN (`; (ii) post-verify **(e2)** aborts the apply if `pg_get_functiondef` lacks the comparison; (iii) Task 4's Part 3c reds in CI. ⚠️ Presence-only greps for `auth.uid()` do **not** mitigate this — a bare declaration satisfies them |
| T-156-17 | Elevation of Privilege | The same hole opened by accident — a 142-line verbatim re-base that drops the `v_auth_uid <> p_user_id` raise | mitigate | Identical to T-156-09: (e2) aborts at apply, Part 3c reds in CI. Neither depends on the author noticing the omission |
| T-156-18 | Repudiation | "PR A breaks no existing gate" asserted but never run | mitigate | Task 4 runs **all four** SQL gate files against a Migration-A PG16 fixture, each under `set -o pipefail` / `\|\| exit 1` so a `tee`'d exit status cannot mask an abort |
| T-156-10 | Elevation of Privilege | `current_user`/`session_user` used as the role gate | mitigate | `auth.role()` mandated in the action and asserted by post-verify (e); Trap C cited with its two in-repo precedents |
| T-156-11 | Elevation of Privilege | `DROP`+`CREATE` restoring PUBLIC EXECUTE | mitigate | `CREATE OR REPLACE` mandated; acceptance greps for zero `DROP FUNCTION`; explicit `REVOKE … FROM PUBLIC, anon` re-asserted anyway |
| T-156-12 | Denial of Service | `service_role` without EXECUTE ⇒ total connect outage | mitigate | Post-verify (a) aborts the migration; Task 2 step 4 measures all three booleans on TEST |
| T-156-13 | Denial of Service | `authenticated` losing EXECUTE early ⇒ outage for the still-live old deploy | mitigate | Post-verify (b) aborts; acceptance greps for zero `REVOKE … FROM authenticated` in this file |
| T-156-14 | Tampering | A stale re-base silently reverting PR #675's `attested_venue` stamp | mitigate | Re-base from the snapshots only; post-verify (d) greps `pg_get_functiondef` for the fence and the stamp |
| T-156-15 | Tampering | Editing an applied migration to fix `20260811210000` §1b's stale prose | mitigate | Header supersedes it by name; invariant 11 quoted in the context block |
| T-156-16 | Denial of Service | `RAISE` with `||` ⇒ 42601 at apply, blocking every later migration | mitigate | Invariant 21 called out; acceptance requires single-literal or dollar-quoted format strings |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this phase; no install task exists |
</threat_model>

<verification>
- Migration A applied to TEST cleanly with its `$verify$` NOTICEs, and `authenticated`,
  `service_role` **both** hold EXECUTE on **both** RPCs on TEST
- `npm run schema:functions:check` exits 0; `npx tsc --noEmit` clean
- **All four SQL gate files** (`test_wizard_composite_fence.sql`, `test_wizard_session_idempotency.sql`,
  `test_api_keys_exchange_not_user_writable.sql`, `test_api_keys_venue_identity_uniq.sql`) run
  **green** under `psql -v ON_ERROR_STOP=1` against a PG16 fixture carrying Migration A — measured,
  not asserted, and never through an unguarded `tee` pipeline
- Part 3c observed RED under the flat-union mutation and green after restore, both pasted
- No file under `supabase/migrations/` other than the new one is modified (`git diff --stat`)
- ⛔ `supabase db push` was never run
</verification>

<success_criteria>
Both wizard RPCs accept a `service_role` caller **and** still accept the old deploy's
`authenticated` caller with its ownership guard intact — **with that guard's survival proven by a
gate that runs in CI**, not by a grep that a bare declaration would satisfy — so PR A can merge
with no connect-a-key outage and no cross-user window in either merge ordering.
</success_criteria>

<output>
Create `.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-03-SUMMARY.md` when done
</output>
