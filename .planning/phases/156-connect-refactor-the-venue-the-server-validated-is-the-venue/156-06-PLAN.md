---
phase: 156-connect-refactor-the-venue-the-server-validated-is-the-venue
plan: 06
type: execute
wave: 5
landing: PR-A → PR-B boundary
depends_on: ["156-05"]
files_modified:
  - .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md
autonomous: false
requirements: [CONNECT-03]

must_haves:
  truths:
    - "PR A is merged, applied to PROD, and the new Vercel deploy is live"
    - "A real browser has completed a single-key connect AND a composite connect on PROD, against the service-role writer"
    - "The row those connects minted carries attested_venue = exchange, written by the RPC under the server's credential"
    - "PR B is not authored until this gate is recorded PASS"
  artifacts:
    - path: ".planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md"
      provides: "the recorded live evidence that gates the second landing"
      min_lines: 30
  key_links:
    - from: "156-LIVE-ACCEPTANCE.md"
      to: "156-07-PLAN.md (Migration B)"
      via: "the recorded PASS is Migration B's precondition"
      pattern: "PASS|FAIL"
---

<objective>
**The hard boundary between the two landings.** PR A ships; the service-role writer is verified
working on PROD by a real browser; only then is `authenticated` EXECUTE withdrawn.

Purpose: the whole reason this phase is two PRs is that no single-migration ordering is
outage-free. That structure buys nothing if PR B is written and merged before anyone has confirmed
the new writer actually works in production. This plan is the confirmation, and it is deliberately
un-skippable.

⭐ A 40-minute browser pass has beaten 10,193 tests in this repo before. The unit suite proves the
route calls the admin client with a mocked client. It cannot prove that Vercel's Production env has
`SUPABASE_SERVICE_ROLE_KEY`, that PostgREST reached `service_role`, or that the RPC's new branch
took the arm we think it took.

Output: `156-LIVE-ACCEPTANCE.md` with a PASS or FAIL and the evidence behind it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-RESEARCH.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md

⛔ PROD = `quantalyze.xyz` / `quantalyze-rho.vercel.app`. **Never `.com`, never a bare `.vercel.app`.**
⛔ PROD Supabase = `khslejtfbuezsmvmtsdn`. Merging `supabase/migrations/**` to `main`
**auto-applies to PROD** — that is how Migration A reaches PROD, and it happens with no ordering
against the Vercel build.

⚠️ **What the merge window looks like, and why it is safe this time.** Migration A is additive: it
GRANTs `service_role` and leaves `authenticated`'s grant and its `auth.uid()` guard standing. So
whichever wins the race, both the old deploy (user-scoped client, `authenticated` arm) and the new
deploy (admin client, `service_role` arm) work. **This is the only window in the phase with that
property** — do not carry the assumption into PR B.

⚠️ Authed PROD QA account: `qa-demo@quantalyze.app`. Headless `browse` cannot hydrate an authed
React surface — use a real browser or CDP.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the live acceptance checklist, with its falsifying observations named</name>
  <files>.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md</files>
  <read_first>
    - `.planning/phases/156-.../156-RESEARCH.md` § "Runtime State Inventory" — the Vercel env row
      (`SUPABASE_SERVICE_ROLE_KEY` must exist in Production **and** Preview) and the note that the
      Railway worker uses the different name `SUPABASE_SERVICE_KEY` and calls neither RPC
    - `.planning/phases/156-.../156-04-SUMMARY.md` — what actually shipped in the routes
    - `src/lib/wizardErrors.ts:2166-2183` — `SEAM_MISCONFIGURED`'s user-facing copy, so a failure
      mode is recognisable on screen rather than guessed at
  </read_first>
  <action>
Create `156-LIVE-ACCEPTANCE.md` with frontmatter `status: pending` and these sections, each with an
empty `**Observed:**` line and an explicit **falsifying observation** — the thing that, if seen,
makes the row FAIL rather than "probably fine":

1. **Deploy state.** The Vercel Production deploy's commit SHA equals PR A's merge commit, and
   `Supabase Migrate` is green for Migration A. Falsifier: a Production deploy older than the merge.
2. **Env precondition.** `SUPABASE_SERVICE_ROLE_KEY` is present in Vercel **Production** and
   **Preview**. Falsifier: absent in Preview — every preview wizard is broken even though
   Production is fine, and nothing in CI would say so.
3. **Single-key connect, real browser, PROD.** Complete a connect on the wizard's single-key path
   for a ccxt venue. Falsifier: any `SEAM_MISCONFIGURED` 503 (means the service key did not reach
   the route) or a 42501-shaped failure (means `GRANT … TO service_role` did not land).
4. **Composite connect, real browser, PROD.** Complete a composite connect with at least two member
   keys. Falsifier: the first member succeeds and a later one fails — the twin diverged.
5. **The minted rows.** For the keys just created, `attested_venue = exchange` and non-NULL.
   Falsifier: `attested_venue IS NULL` — the row went through a client INSERT path, i.e. the wizard
   did not use the RPC at all, and the whole phase would be verifying nothing.
6. **MT5 path, if a working MT5 credential is available.** Best-effort, explicitly optional —
   ⚠️ ALL 3 PROD MT5 keys currently sit at `sync_status='error'`, so an MT5 failure here is
   attributable to that pre-existing defect and is **not** a Phase 156 finding. Say so in the row so
   nobody blocks PR B on it.

Add a **"Gate"** section stating verbatim: rows 1–5 must all read PASS before plan 07 authors
Migration B. A FAIL on any of 1–5 stops the phase and returns to the orchestrator — PR B would
convert a working-but-open state into a broken-and-closed one.

⛔ Record row 5's evidence as booleans and column values only. `.planning/` is TRACKED and the repo
is **PUBLIC** — no key ids, no user ids, no connection strings, no PROD census beyond the two rows
under test.
  </action>
  <verify>
    <automated>test -f .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md && test "$(grep -c 'Falsifier' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md)" -ge 5 && grep -q 'status: pending' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md && ! grep -q 'khslejtfbuezsmvmtsdn' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Six numbered rows exist, rows 1–5 marked gating and row 6 marked optional/non-blocking
    - Each of rows 1–5 carries an explicit **Falsifier** sentence
    - The Gate section names plan 07 as what is blocked and states the STOP condition
    - No credential, user id, key id, or PROD project ref appears in the file
  </acceptance_criteria>
  <done>The live gate is written down as falsifiable observations, so "it seemed to work" cannot pass it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ship PR A and verify the service-role writer on PROD</name>
  <action>⛔ CHECKPOINT — no autonomous work in this task. Present the verification steps below to the operator and WAIT. Do not substitute an automated approximation, do not proceed on an assumed result, and do not mark this task complete without the pasted output.</action>
  <what-built>
Plans 02–05 built and merged-ready: the transitional migration (service_role GRANTed,
`authenticated` untouched), both wizard routes calling `createAdminClient()`, both route test suites
re-cut and green, the CONNECT-02b structural guard, and a "landing 1 of 2" changelog with the
version bump. Migration A is applied to **TEST** only. Nothing has reached PROD.
  </what-built>
  <how-to-verify>
    1. Ship PR A via the normal `/ship` flow from the phase branch — ⛔ never a manual commit, never
       from `main`. Land it with `/land-and-deploy`, one PR at a time.
    2. Confirm `Supabase Migrate` is green and the Vercel **Production** deploy's SHA matches the
       merge commit. ⛔ A grey `sql-tests` is **not** a pass — the job shares the `shared-test-db`
       concurrency group with `python` and `e2e-seeded`, a cancelled job renders grey, and branch
       protection is deferred on this repo. Re-run it rather than reading grey as green.
    3. In a **real browser** (not headless — it cannot hydrate an authed React surface) as
       `qa-demo@quantalyze.app` on `quantalyze.xyz`: complete a **single-key** wizard connect, then
       a **composite** connect with at least two members.
    4. Query PROD for the two rows just minted and record `attested_venue`, `exchange`, and whether
       they are equal. Values only — no ids.
    5. Confirm `SUPABASE_SERVICE_ROLE_KEY` exists in Vercel **Preview** as well as Production.
    6. Walk `156-LIVE-ACCEPTANCE.md` rows 1–6 and report PASS/FAIL per row with what you observed.
    ⛔ If any of rows 1–5 fails, say so plainly and do **not** proceed — PR B would turn a working,
    open state into a broken, closed one.
  </how-to-verify>
  <done>The operator ran every listed step and pasted the raw results back; the following task records them.</done>
  <resume-signal>Report PASS/FAIL per row with the observed evidence, or describe what broke</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the gate result and arm (or stop) the second landing</name>
  <files>.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md</files>
  <read_first>
    - The checkpoint output from Task 2
    - `.planning/phases/156-.../156-MEASUREMENTS.md` § A3 — because plan 09's SQL-gate re-cut
      depends on the connection role, and this is the last chance to notice a conflict before PR B
  </read_first>
  <action>
Fill every `**Observed:**` line from the checkpoint output and set a per-row verdict. Set frontmatter
to `status: pass` (rows 1–5 all PASS) or `status: blocked`, with `verified_on: <date>` and
`verified_against: production`.

On PASS: add a one-paragraph **"What PR B may now assume"** section — the service-role arm of both
RPC bodies is exercised in production, so Migration B's narrowing removes a path that is provably no
longer taken by our own code. ⛔ State the one thing it may **not** assume: any *other* client still
using `authenticated` against these RPCs (a stale browser tab mid-wizard, an unreleased preview
deploy) will start failing at the moment Migration B applies. That is the intended effect and it is
the reason PR B should not be merged into an active wizard session window.

On FAIL: write the failing row and its evidence into the Gate section, leave `status: blocked`, and
report to the orchestrator. ⛔ Do not author Migration B.
  </action>
  <verify>
    <automated>grep -q 'status: pass\|status: blocked' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md && ! grep -qi 'TBD\|pending' .planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-LIVE-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - Every row carries an observed value and a PASS/FAIL verdict; no `TBD`/`pending` survives
    - Frontmatter is `status: pass` or `status: blocked` with a date
    - On PASS, the "What PR B may now assume" section exists and names the stale-session consequence
    - On FAIL, plan 07 is explicitly reported as blocked
  </acceptance_criteria>
  <done>The second landing is gated on evidence from production rather than on the assumption that the first landing worked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merge → PROD | The merge is the apply; Vercel builds independently with no ordering |
| operator → PROD database | Row inspection touches live customer data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-26 | Denial of Service | Migration B merged before the new writer is proven live | mitigate | This plan is a blocking gate; plan 07 `depends_on: ["156-06"]` and the Gate section names the STOP |
| T-156-27 | Denial of Service | `SUPABASE_SERVICE_ROLE_KEY` missing in Vercel **Preview** | mitigate | Row 2 checks Preview explicitly — Production-only success would hide it until a preview wizard broke |
| T-156-28 | Repudiation | A grey `sql-tests` job read as a pass | mitigate | Step 2 states the shared-concurrency-group mechanism and requires a re-run, not an interpretation |
| T-156-29 | Information Disclosure | PROD ids or census pasted into a tracked, public planning file | mitigate | Row 5 records column values and booleans only; the automated verify greps for the PROD project ref |
| T-156-30 | Spoofing | Verifying against the wrong host (`.com`, bare `.vercel.app`) | mitigate | The context block names the two valid hosts and forbids the others |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this phase; no install task exists |
</threat_model>

<verification>
- `156-LIVE-ACCEPTANCE.md` is `status: pass` with six recorded rows
- The PROD deploy SHA matches PR A's merge commit and `Supabase Migrate` is green
- Two real connects (single-key and composite) completed on `quantalyze.xyz`, and their rows carry
  `attested_venue = exchange`
</verification>

<success_criteria>
The service-role writer is a fact in production, observed by a person in a browser, before anything
withdraws the fallback path that is currently keeping connect-a-key alive.
</success_criteria>

<output>
Create `.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-06-SUMMARY.md` when done
</output>
