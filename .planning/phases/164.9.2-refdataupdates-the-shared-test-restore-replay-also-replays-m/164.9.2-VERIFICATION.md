---
phase: 164.9.2-refdataupdates
verified: 2026-09-26T07:26:26Z
status: human_needed
score: 9/10 must-haves verified (1 post-merge human item, 0 failed)
verified_at_sha: e4a11398c27e265d297386b99575f1816e038261
drift_subjects:
  - scripts/extract-reference-inserts.mjs
  - scripts/extract-reference-inserts-fixtures
  - scripts/restore-test-refdata-allowlist.txt
  - scripts/restore-test-from-baseline.sh
  - scripts/restore-test-from-baseline-fixtures
  - scripts/local-stack/run.sh
  - .github/workflows/ci.yml
  - .github/workflows/test-restore-from-baseline.yml
  - src/__tests__/extract-reference-inserts-selftest-floor.test.ts
  - src/__tests__/refdata-allowlist.test.ts
  - src/__tests__/restore-test-from-baseline.test.ts
  - src/__tests__/test-restore-workflow-wiring.test.ts
behavior_unverified: 0
overrides_applied: 0
accepted_deviations:
  - decision: "DO-body DELETE and INSERT ... ON CONFLICT DO UPDATE on a replayed table are declinable ONLY under the machine-checked `freshKeyProof` (keyed by a block-local variable declared once as `uuid := gen_random_uuid()` and never reassigned). DO-body TRUNCATE / MERGE / COPY and a LITERAL DO-body UPDATE are hard refusals. A NON-literal DO-body UPDATE stays declinable by a reasoned decline: line, mirroring D-02 for its top-level twin."
    reason: "The literal round-2 rule (refuse every DO-body DELETE/upsert outright) would keep --audit permanently red on three APPLIED, uneditable migrations (b5b :406/:408, cleanup :188). All three pass the proof, so the census is unchanged."
    accepted_by: "orchestrator (recorded at verification; founder may still take the literal rule, which would need a decision because those migrations have no remedy)"
    accepted_at: "2026-09-25"
re_verification:
  previous_status: gaps_found
  previous_score: 7/10
  previous_verified_at_sha: b24ee7cd68636366e5b1c022d710ec63e3671473
  gaps_closed:
    - "Plan 05 Task 1: the real-dump rehearsal on the local lane boots with every replayed C5 UPDATE applied, and the sentinel's manager_status reads the verified label"
    - "[164.8.1-REPLAY-INSERT-ONLY-SCOPE] closed in the phase PR with a dated note, its original text kept as lineage"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "POST-MERGE, owner: the ORCHESTRATOR (founder-authorized for this occasion, 164.9 precedent). After the phase PR lands on main, dispatch test-restore-from-baseline.yml mode=preflight on shared TEST, then mode=restore with the confirm token read from that preflight's own log line (never copied from a planning document)."
    expected: "Both runs conclude success. The preflight log prints `self-test OK (41/41 arms` (NOT the 34/34 in the plan text) and the C5 note with the replayed update count read from the merged tree's `audit C5 OK:` line (6 at this sha), and passes the unchanged wrong-state leg. Both run ids are recorded under [164.9-CRIT8-RESTORE-DISPATCH-RECORD], closing Phase 164.9 criterion 8 (ROADMAP 164.9.2 success criterion 4), in a follow-up docs PR with a VERSION patch bump and a CHANGELOG entry."
    why_human: "Needs main and a write to shared TEST; the workflow's ref guard refuses any other ref, and an executor or verifier must never dispatch it."
    blocked_by: "BASELINE RE-DUMP. Measured by the verifier at this sha: `node scripts/check-baseline-currency.mjs` (default mode, the one the shared-TEST restore path uses) exits 1 with `baseline-stale ... defects=1`. Main's three newest migrations (20260924230827, 20260924233749, 20260925071300, all arriving via the merge of #860) postdate supabase/schema/baseline.sql, so the preflight will refuse until the dump is regenerated from PROD and its carried-migrations marker rewritten in the same commit (supabase/schema/BASELINE.md, `## Regenerating`). Not this phase's doing (it adds no migration) and never an executor's or verifier's step. The orchestrator must route the re-dump before dispatching."
  - test: "Read the D-A1 outcome from that same preflight run."
    expected: "No 42501 from prevent_profile_privileged_change (trigger profiles_lock_privileged_cols). Its latest definition admits current_user IN ('postgres','service_role','supabase_admin'). A 42501 aborts and rolls back, and is recorded as a role finding, never re-dispatched around. (On the local lane, as postgres, the rehearsal saw no 42501; which role the shared-TEST session runs as is still unmeasured.)"
    why_human: "Which role the shared-TEST restore session runs as is not measurable from the repo."
---

# Phase 164.9.2: REFDATAUPDATES Verification Report

**Phase goal:** a shared-TEST restore rebuilds its reference rows in the state PROD holds them. The
reference-data replay also replays a migration's top-level `UPDATE` when it targets a `public`
table the replay has just filled.
**Verified:** 2026-09-26, at `e4a11398c` (branch `feat/164.9.2-refdataupdates`, after plan 05
Task 1 and the merge of `origin/main` at `96219c04a`, Phase 164.9.1 / #860)
**Status:** human_needed
**Re-verification:** Yes. The previous report (2026-09-25, at `b24ee7cd6`) was `gaps_found` with
two failed truths. Both are closed. The one remaining item is success criterion 4, post-merge by
design, and it is currently BLOCKED on a baseline re-dump.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | SC1: C5 is a new, separately pinned class for top-level UPDATEs on a `public` table already in the replay, never `auth.*`, with its own id, pinned counts, audit census and red+green self-test arms | ✓ VERIFIED (regression check) | Verifier re-ran: extractor self-test `67 kinds, red+green each` (65 → 67 is the round-1 C2 leftover `9811b54a1`, kinds `c2-psql-metachar` / `c2-psql-variable`; floor moved up only). `--audit` rc=0 with its own `audit C5 OK` line |
| 2 | SC2: replayed statements interleave in migration filename order, inside the same transaction as the INSERTs | ✓ VERIFIED | Restore self-test, re-run by the verifier after the merge: arm 38 GREEN (a replayed C5 UPDATE lands after its INSERT inside the transaction) and arm 39 RED pass. These are the old arms 33/34, renumbered, see below |
| 3 | SC3: the wrong-state check and its `verified` default are unchanged, with no waiver, relaxed default or widened allowlist | ✓ VERIFIED | The four `REFDATA_WRONGSTATE_*` default lines diff EMPTY against main's `96219c04a`. Arms 31/32 pass |
| 4 | SC4: a green `mode=preflight`, then a green `mode=restore`, on shared TEST, both recorded by run id | ? HUMAN (post-merge, BLOCKED) | Needs `main`. Owner: the orchestrator. Also blocked today: `check-baseline-currency.mjs` default mode exits 1 (`baseline-stale`, `defects=1`), measured by the verifier. See Human Verification |
| 5 | D-03: both workflows assert the C5 census line and fail with a named MEASURE_FAIL when it is missing | ✓ VERIFIED (regression check) | `audit C5 OK` appears in both `ci.yml` and `test-restore-from-baseline.yml`. `test-restore-workflow-wiring.test.ts` passes |
| 6 | The INSERT half of the emission is byte-identical to the pre-phase emission, and the count floor stays INSERT-only | ✓ VERIFIED (regression check) | Unchanged since the previous run: after `b24ee7cd6` the extractor changed only in `9811b54a1` (the C2 refusal, which rejects more and emits the same), and the merge brought no extractor change from main. The audit still reads `22 entr(ies) … 23 statement(s)` |
| 7 | Plan 05: the real-dump rehearsal on the local lane shows the sentinel's manager_status at the verified label | ✓ VERIFIED (from the executor's raw log, not re-run) | Per the brief the verifier did NOT boot the shared lane. Instead it read the executor's raw log `164.9.2-05-rehearsal.log` (scratchpad, 183 lines, not committed). The log shows: `baseline-currency: carried=274 replay=3 … defects=0`. The extractor emitted `6 C5 update statement(s)` into the lane's `ON_ERROR_STOP=1` load. `up complete`, `up-exit=0`, and no `already running` line, so this run started its own stack. `sentinel manager_status=verified` against `column default='newbie'::text`, with `profiles user triggers enabled=2` and the `prevent_profile_privileged_change` trigger present. 51 corpus files, all `exit=0`. `down-exit=0`, `surviving quantalyze containers -> 0`, `assert-teardown-exit=0`. The code it ran is HEAD's: the boot was at 08:33, after the merge commit `07ba72750` (07:59), and the only later commits (`e4165c13e`, `e4a11398c`) touch `TODOS.md` and the SUMMARY alone (`git diff --stat 07ba72750 HEAD`) |
| 8 | `[164.8.1-REPLAY-INSERT-ONLY-SCOPE]` closed in the phase PR (CONTEXT W5) | ✓ VERIFIED | `e4165c13e` flips the entry to `[x]`. It keeps the "Not closable by editing the allowlist" paragraph and adds a dated 2026-09-26 note saying why that paragraph no longer describes the code (C5 is a new criterion, not a C2 relaxation), plus a CLOSED line citing the `audit C5 OK` census, arms 38/39 by name (with the renumber explained), and plan 05's rehearsal. It routes the shared-TEST evidence to `[164.9-CRIT8-RESTORE-DISPATCH-RECORD]`. The note is dated the 26th, not the plan's 25th, because that is the day it was written (disclosed) |
| 9 | The full phase gate is green in one run on the combined tree | ✓ VERIFIED | The verifier's own runs are below. The bar is now **41/41**: main's 164.9.1 arms kept 33-37, and this branch's moved to 38-41. The plan text's `34/34` is stale |
| 10 | Accepted deviation: DO-body DELETE/upsert declinable only under `freshKeyProof`; every other unprovable DO-body write is a hard refusal (non-literal DO-body UPDATE declinable by reason) | ✓ VERIFIED (regression check) | Census unchanged: `6 declined over 5 file(s); 0 unaccounted` |

**Score:** 9/10 truths verified. 0 are present-but-behavior-unverified, 1 is a post-merge human item
(currently blocked), and 0 failed.

### Merge integrity (new since the previous report)

- `07ba72750` merges `origin/main` at `96219c04a`. Main's arms 33-37 (164.9.1) still print under
  their own names, and this branch's four arms print as 38-41 with their 164.9.2 labels.
  `EXPECTED_ARMS=41`, with a dated RENUMBERED note beside it.
- There are no conflict markers under `scripts/`, `src/` or `.github/`. There is no
  `supabase/migrations/` change against main (the phase adds no migration). No
  `TBD` / `FIXME` / `XXX` appears in any added line outside `.planning/`.

### Required Artifacts

| Artifact | Status | Details |
|---|---|---|
| `scripts/extract-reference-inserts.mjs` | ✓ VERIFIED | C5 classifier plus the C2 psql metachar/variable refusal (`9811b54a1`) |
| `scripts/restore-test-refdata-allowlist.txt` | ✓ VERIFIED | 4 `update:` and 6 `decline:` lines, unchanged |
| `scripts/restore-test-from-baseline.sh` | ✓ VERIFIED | Arms renumbered 38-41, `EXPECTED_ARMS=41` |
| Both workflows | ✓ WIRED | C5 census asserted |
| `TODOS.md` closure | ✓ VERIFIED | Gap 2 closed |
| `164.9.2-05-SUMMARY.md` | ✓ PRESENT | Task 1 complete. Task 2 is explicitly PENDING, and the SUMMARY does not claim it |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| allowlist `update:` lines | emitted `refdata.sql` | extractor emit | ✓ WIRED (6 statements) |
| extractor emit | restore transaction | `build_transaction` | ✓ WIRED (arm 38) |
| extractor emit | local-stack lane | `load_reference_data`, `ON_ERROR_STOP=1` | ✓ WIRED, and now read on the real dump (truth 7) |
| `--audit` C5 census | CI `sql-gate-lint` and the restore workflow | grep + MEASURE_FAIL | ✓ WIRED |

### Data-Flow Trace (Level 4)

| Value | Source | Real data | Status |
|---|---|---|---|
| sentinel `manager_status` after the replay | `20260515095804:60` INSERT, then the `20260521150000:23` UPDATE | Real `baseline.sql`, triggers enabled, on the local lane: reads `verified` against a `newbie` default | ✓ FLOWING on the real dump. Shared TEST remains the post-merge human item |

### Behavioral Spot-Checks (the verifier's own runs, at `e4a11398c`)

```
$ node scripts/extract-reference-inserts.mjs --self-test          rc=0
extract-reference-inserts self-test OK: 67 kinds, red+green each.
$ node scripts/extract-reference-inserts.mjs --audit              rc=0
extract-reference-inserts audit OK: 22 entr(ies), 19 file(s), 8 table(s), 23 statement(s); every pinned count re-measured, no unlisted top-level reference INSERT.
extract-reference-inserts audit C5 OK: 6 update statement(s) over 4 file(s) and 2 table(s) replayed; 6 declined over 5 file(s); 0 unaccounted.
$ bash scripts/restore-test-from-baseline.sh --self-test   (throwaway cluster via the pg-lane PGBIN seam; no remote)   rc=0
restore-test-from-baseline: self-test OK (41/41 arms — …)
  ok 33-37 (164.9.1, main's numbers) · ok 38 GREEN C5 · ok 39 RED C5 · ok 40 RED WR-03 · ok 41 RED SFH R2-06
$ npx vitest run extract-reference-inserts-selftest-floor refdata-allowlist restore-test-from-baseline test-restore-workflow-wiring local-stack-lane-wiring lint-migration-data-dependence   rc=0
 Test Files  6 passed (6)      Tests  291 passed (291)      (0 skipped)
$ node scripts/lint-sql-gates.mjs --self-test                     rc=0
lint-sql-gates self-test OK: 7 rules, red+green each.
$ node scripts/verify-plan-anchors.mjs --pending                  rc=0
OK: 3 plan file(s), no stale claims.      (plan 05 is no longer pending now that its SUMMARY exists)
$ node scripts/check-baseline-currency.mjs                        rc=1   <- the Task 2 blocker, not a phase gate
baseline-currency: baseline=1790285924 migrations=1790401727 defects=1
::error::baseline-stale — the baseline dump is STALE: …
```

The local-stack lane was NOT booted by the verifier, as instructed. Truth 7 rests on the
executor's raw log, which the verifier read line by line.

### Requirements Coverage

| Requirement | Source | Status | Evidence |
|---|---|---|---|
| `[164.8.1-REPLAY-INSERT-ONLY-SCOPE]` | plans 01-05 | ✓ SATISFIED | Mechanism, real-dump reading, and TODOS closure |
| `[164.9-CRIT8-RESTORE-DISPATCH-RECORD]` | plan 05 Task 2 | ? NEEDS HUMAN (post-merge, blocked on re-dump) | Orchestrator dispatch after merge and after the dump is regenerated |

### Anti-Patterns / Info

ℹ️ **Cross-worktree lane collision (disclosed by the executor, verified in its logs).** Every
worktree derives docker `project_id=quantalyze` from the tracked `supabase/config.toml`, and
`scripts/local-stack/run.sh` has no cross-worktree lock. So a second `up` can ATTACH to another
worktree's stack (`supabase start is already running.`, seen in the executor's
`164.9.2-05-lane-up-2.log`) and load into it. That attempt likely disturbed a 164.6.7 lane run,
and that owner should re-run it. The recorded rehearsal is clean: it started its own stack. A
lane lock or a per-worktree project id is out of this phase's scope, and the orchestrator must
route it.

ℹ️ The previous report's renumber-collision warning with 164.9.1 is resolved by the merge.

### Human Verification Required

1. **Criterion 4 dispatch (POST-MERGE, owner: orchestrator, founder-authorized). BLOCKED on a
   baseline re-dump.** `check-baseline-currency.mjs` default mode exits 1 at this sha, because
   main's three newest migrations postdate `supabase/schema/baseline.sql`. First regenerate the
   dump from PROD, with its carried-migrations marker in the same commit. Then, on `main`, run
   `test-restore-from-baseline.yml` `mode=preflight`, followed by `mode=restore` with the confirm
   token read from the preflight's own log line. Both must be green, and the preflight must print
   `41/41`. Record both run ids under `[164.9-CRIT8-RESTORE-DISPATCH-RECORD]` in a follow-up docs
   PR with a VERSION patch bump and a CHANGELOG entry. This is not passed and must not be read as
   passed.
2. **D-A1 read from that preflight.** There must be no `42501` from `profiles_lock_privileged_cols`.
   If one appears, record it as a role finding, and never re-dispatch around it.

### Gaps Summary

There are no pre-merge gaps. Both gaps from the 2026-09-25 report are closed. First, plan 05
Task 1 ran on the real dump after the merge with main. The sentinel reads `verified` over a
`newbie` default, and all 6 C5 updates applied under `ON_ERROR_STOP=1` with triggers enabled.
Second, the TODOS entry is closed with a dated note and its lineage kept. All phase gates re-run
green at HEAD (41/41, 67 kinds, 291 tests). What remains is criterion 4, the shared-TEST
preflight and restore. It is post-merge by construction, belongs to the orchestrator, and is
currently blocked on a baseline re-dump that this phase did not cause.

---

_Verified: 2026-09-26T07:26:26Z_
_Verifier: Claude (gsd-verifier)_
