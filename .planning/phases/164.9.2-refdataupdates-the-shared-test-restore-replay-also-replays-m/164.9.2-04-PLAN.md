---
phase: 164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m
plan: 04
type: execute
wave: 2
depends_on: ["164.9.2-01"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/test-restore-from-baseline.yml
  - src/__tests__/test-restore-workflow-wiring.test.ts
autonomous: true
requirements:
  - "[164.8.1-REPLAY-INSERT-ONLY-SCOPE]"

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Both workflows (ci.yml `sql-gate-lint`, test-restore-from-baseline.yml) fail with a named MEASURE_FAIL when `--audit` exits 0 but prints no `extract-reference-inserts audit C5 OK: …` line (per D-03)."
    - "In the restore workflow the existing `audit OK` grep, its entry-count parse and its zero-entry refusal are byte-identical, and the C5 check is appended after them."
    - "ci.yml's self-test step (`ok_line`, `floor`, and the self-test status) is bounded in the same shape and executed under the harness, because plan 02 moves `SELF_TEST_KINDS_FLOOR` in the same wave (plan-check iteration 2)."
    - "In ci.yml every capture in the census step is BOUNDED (`|| <name>_rc=$?` plus an rc>1 branch, the restore workflow's shape). GitHub runs a `run:` step under `bash -e`, so an unbounded `x=$(grep …)` that finds nothing kills the step BEFORE its `::error::MEASURE_FAIL` line prints: red, but unnamed (plan-checker W1). The pre-existing `census` and `scanned_line` captures in that step have the same defect and are bounded in the same edit, with a dated comment saying so."
    - "BOTH workflows' census steps are EXECUTED under the test harness with a stubbed `node` (`extractRunScript` on each file; the step name is identical in both). Each goes GREEN with both census lines, and RED with the named C5 MEASURE_FAIL text when the C5 line is missing. An `unbind` calibration shows the unbounded twin dies without printing that text, which is the pre-fix ci.yml behaviour."
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "C5 census MEASURE_FAIL block in the audit census step of sql-gate-lint"
    - path: .github/workflows/test-restore-from-baseline.yml
      provides: "the same block in its audit census step"
    - path: src/__tests__/test-restore-workflow-wiring.test.ts
      provides: "stub CENSUS carries a C5 line; executed RED arms for the missing C5 line in BOTH workflows' census steps"
  key_links:
    - "The C5 regex in both workflows must match the extractor's exact census text from plan 01: `^extract-reference-inserts audit C5 OK: [0-9]+ update statement\\(s\\) over [0-9]+ file\\(s\\) and [0-9]+ table\\(s\\) replayed; [0-9]+ declined over [0-9]+ file\\(s\\); 0 unaccounted\\.$`."
  prohibitions:
    - "⛔ No CI skip token anywhere: not in a workflow comment, a commit message or a test string."
    - "⛔ Every new grep that reads a file uses `-a` (the workflow-wide WR-04 rule pinned in test-restore-workflow-wiring.test.ts)."
    - "⛔ The mutex steps copied byte-for-byte from ci.yml are not touched."
---

<objective>
Make both workflows that run the reference-data audit read the new C5 census line, and treat its
absence as a MEASURE_FAIL (per D-03), exactly as they already treat the first census line.

Purpose: a C5 class whose census no workflow reads could disappear and leave every run green.

Output: one appended check per workflow, ci.yml's census step bounded, and executed harness arms (GREEN + RED) for both steps.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-CONTEXT.md
Read the prior plan summary `.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-01-SUMMARY.md` (written when that plan executes; it records the exact reason strings and census text).

<interfaces>
- ci.yml, job `sql-gate-lint`: step "Assert the allowlist audit PRINTED its census over a non-empty corpus" (env `REFDATA_AUDIT_LOG`). It greps `^extract-reference-inserts audit OK: …`, the `migrations scanned: N` line and the entry count, and ends by echoing `$scanned_line` and `$census`.
- test-restore-from-baseline.yml: the corresponding census step after "Reference-data allowlist audit - no unlisted top-level reference INSERT in supabase/migrations". It uses `|| census_rc=$?`-style bounds (the harness's `BOUNDS` list names them).
- test-restore-workflow-wiring.test.ts: `describe(AUDIT_STEP, …)` runs `extractRunScript(WF, AUDIT_STEP)` under `runStep(script, stub)` with a stub `node` that prints `STUB_AUDIT_OUT`, plus `unbind(script, BOUNDS)` for pre-fix calibrations. Its `CENSUS` constant is the stubbed first census line.
- Both census steps carry the SAME step name, `Assert the allowlist audit PRINTED its census over a non-empty corpus`, so `extractRunScript(<file text>, AUDIT_STEP)` extracts either one. The harness today reads only `WF` (`WF_PATH`, the restore workflow); read ci.yml alongside it.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: tracer, the restore workflow refuses a missing C5 census, proven under the harness</name>
  <files>.github/workflows/test-restore-from-baseline.yml, src/__tests__/test-restore-workflow-wiring.test.ts</files>
  <read_first>.github/workflows/test-restore-from-baseline.yml (the audit census step, whole); src/__tests__/test-restore-workflow-wiring.test.ts (the `describe(AUDIT_STEP` block and its `runStep`/`unbind` helpers)</read_first>
  <action>
  Append to the restore workflow's audit census step, after the zero-entry refusal and before the final echoes: a `grep -a -m1 -E` for the exact C5 regex in key_links, bounded the same way the step already bounds its other greps (`|| c5_rc=$?`, and add `" || c5_rc=$?"` to the harness `BOUNDS` list). If the line is empty, print `::error::MEASURE_FAIL: the audit exited 0 but printed NO 'extract-reference-inserts audit C5 OK: …' census line. A C5 class whose census nobody reads can vanish with every run green (Phase 164.9.2, D-03).` and exit 1. Echo the C5 line with the others. Leave every existing line byte-identical.
  In the harness, add the C5 census text to the stub `CENSUS` (or to every GREEN stub that expects exit 0), so existing GREEN arms stay green for the right reason. Add a RED arm: a stub with the first census and scanned lines but no C5 line → exit 1 with the MEASURE_FAIL text; plus the `unbind` calibration showing the unbounded twin does not print it. RED first: add the arm before the workflow change, observe it fail, then add the check.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts</automated>
  </verify>
  <done>The wiring test passes, including the new RED arm. Its pre-change failure was observed and recorded in SUMMARY. The WR-04 `-a` workflow-wide arm stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the same check in ci.yml sql-gate-lint, both census steps bounded, and EXECUTED under the harness</name>
  <files>.github/workflows/ci.yml, src/__tests__/test-restore-workflow-wiring.test.ts</files>
  <read_first>.github/workflows/ci.yml (job `sql-gate-lint`, the census step, whole); the restore workflow's census step as the bounded model; src/__tests__/test-restore-workflow-wiring.test.ts (`extractRunScript`, `runStep`, `unbind`, the `describe(AUDIT_STEP` block)</read_first>
  <behavior>
    - ci.yml's census step, extracted and run with a stub `node` that prints both census lines and a scanned line, exits 0.
    - The same step with the C5 line missing exits 1 AND prints `audit C5 OK: …' census line` (the named MEASURE_FAIL); `unbind` of its bounds reproduces the pre-fix death without that text.
    - The same step with the FIRST census line missing exits 1 AND prints its existing named MEASURE_FAIL. Today it dies unnamed under `bash -e`; this arm records that fix.
    - A grep that exits >1 (unreadable log: point `REFDATA_AUDIT_LOG` at a directory) reaches the rc>1 branch by name.
    - ci.yml's self-test step, under the same harness: GREEN with an OK line and a floor at or below it; named MEASURE_FAIL with no OK line; named UNKNOWN when the floor read fails (`STUB_FLOOR_RC`); named regression when the floor exceeds the printed count.
  </behavior>
  <action>
  In ci.yml's census step, give each capture (`census`, `scanned_line`, and the new C5 line) a `<name>_rc=0` initialiser, `|| <name>_rc=$?`, and an `-gt 1` MEASURE_FAIL branch, copying the restore workflow's shape and wording. Bound the step's leading `node … --audit > "$REFDATA_AUDIT_LOG" 2>&1` the same way (`status=0` … `|| status=$?`), because under `bash -e` a non-zero audit kills the step before `cat` and before its named error. Apply the same bounding to the SIBLING ci.yml step "Assert the extractor self-test PRINTED its kind census and cleared the floor" (iteration 2, item 1): the `node … --self-test` status, `ok_line=$(grep -a -m1 …)` (`|| ok_rc=$?`, rc>1 branch) and `floor=$(node -e …SELF_TEST_KINDS_FLOOR…)` (`|| floor_rc=$?`, non-zero branch naming the floor as UNKNOWN), all following the restore workflow's own self-test step. Plan 02 moves that floor in the same wave, so the step must fail by name, not by an unnamed `bash -e` death. Extend the harness: the existing self-test-step describe (`"Assert the extractor self-test PRINTED its kind census and cleared the floor"`, today run against the restore workflow only) also runs the ci.yml copy. Cover GREEN, a missing OK line (named MEASURE_FAIL), an unreadable floor (`STUB_FLOOR_RC`, named UNKNOWN), and a floor above the census (named regression), each with its `unbind` calibration where a bound exists. Append the C5 block after the zero-entry refusal and echo the C5 line with the others. Add a dated comment (2026-09-25, Phase 164.9.2, plan-checker W1): the step runs under `bash -e`, so an unbounded capture aborts before its own MEASURE_FAIL, and the two pre-existing captures carried that defect too. Every grep keeps `-a`. RED first: write the harness arms for ci.yml, observe the named-text assertions fail against the unbounded step, then edit ci.yml. Record the RED output in SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/refdata-allowlist.test.ts && node scripts/extract-reference-inserts.mjs --audit | grep -aE '^extract-reference-inserts audit C5 OK: [0-9]+ update statement\(s\) over [0-9]+ file\(s\) and [0-9]+ table\(s\) replayed; [0-9]+ declined over [0-9]+ file\(s\); 0 unaccounted\.$'</automated>
  </verify>
  <fails_when>any capture in either ci.yml step (`status`, `census`, `scanned_line`, C5, `ok_line`, `floor`) is unbounded, or a RED harness arm passes without its named reason in the output.</fails_when>
  <done>Both workflows' census steps, and ci.yml's self-test step, are executed under the harness, GREEN and RED, with the named C5 reason. The real audit output matches the regex both workflows grep. `refdata-allowlist.test.ts` (owned by plan 01, read-only here) still passes: its existing BOTH-workflows ordering test is unaffected. The pre-change RED is recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit output → CI verdict | a workflow decides pass/fail from printed census lines |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.2-13 | Repudiation | C5 silently removed while CI stays green | medium | mitigate | MEASURE_FAIL on a missing C5 census line in both workflows, harness RED arm |
| T-164.9.2-14 | Denial of service | a skip token suppressing the gate set | medium | mitigate | no skip token written anywhere (prohibition) |
</threat_model>

<verification>
Both vitest files pass; the real `--audit` output matches the regex both workflows grep.
</verification>

<success_criteria>
D-03 holds: both workflows assert the C5 census line, and the extractor's existing audit OK line is byte-stable. The restore workflow's existing checks are unchanged. ci.yml's existing captures change ONLY by gaining the bound they were missing (W1), and their regexes and messages are unchanged.
</success_criteria>

<output>
Create `.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-04-SUMMARY.md` when done
</output>
