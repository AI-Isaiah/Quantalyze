---
phase: 164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m
plan: 05
type: execute
wave: 3
depends_on: ["164.9.2-02", "164.9.2-03", "164.9.2-04"]
files_modified:
  - TODOS.md
autonomous: false
requirements:
  - "[164.8.1-REPLAY-INSERT-ONLY-SCOPE]"
  - "[164.9-CRIT8-RESTORE-DISPATCH-RECORD]"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On the local Supabase lane, which loads the REAL `supabase/schema/baseline.sql` and runs the REAL extractor over the REAL allowlist, the lane boots, so every replayed UPDATE applies to the final schema with its triggers. The teaser sentinel profile's `manager_status` reads the verified label, the value the unchanged wrong-state leg pins."
    - "The full phase gate is green in one run: extractor self-test + audit, restore self-test 34/34, and the six related vitest files."
    - "Criterion 4 (a green `mode=preflight`, then a green `mode=restore`, on shared TEST, both recorded by run id) is executed by the ORCHESTRATOR after merge, never by an executor. The founder authorized that dispatch for this occasion. The verifier records it as post-merge with that owner (the 164.9 precedent), and its record lands in a follow-up docs PR with a VERSION patch bump and a CHANGELOG entry."
    - "`[164.8.1-REPLAY-INSERT-ONLY-SCOPE]` is closed IN THE PHASE PR, with a dated note and its original text kept as lineage."
  artifacts: []
  key_links:
    - "The preflight is the measurement of D-A1: a 42501 from `prevent_profile_privileged_change` aborts and rolls back, and is recorded as a finding, never re-dispatched around."
  prohibitions:
    - "⛔ The executor runs NO `gh workflow run`, NO psql against any remote, and NO `supabase` CLI command outside `scripts/local-stack/run.sh`. This checkout's CLI is linked to PRODUCTION."
    - "⛔ The confirm token is read from the merged ref's preflight log line, never copied from a planning document."
    - "⛔ Never hand-seed shared TEST, and never edit an applied migration (both rejected in the ROADMAP)."
---

<objective>
Rehearse the fix on the real dump and the real corpus locally, run the whole phase gate once, then
stop at the post-merge checkpoint. There the orchestrator dispatches the shared-TEST preflight and
restore (criterion 4) and records both run ids.

Purpose: plans 01-04 prove the mechanism on fixtures. This plan proves it on the real schema, where
the triggers and final column set live. Then it hands off the one step that needs `main`.

Output: a rehearsal record in SUMMARY, then the orchestrator's post-merge record.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-CONTEXT.md
@.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-RESEARCH.md
Read the prior plan summary `.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-01-SUMMARY.md` (written when that plan executes; it records the exact reason strings and census text).
Read the prior plan summary `.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-03-SUMMARY.md` (written when that plan executes; it records the exact reason strings and census text).
</context>

<tasks>

<task type="auto">
  <name>Task 1: real-dump rehearsal on the local lane, plus the full phase gate</name>
  <files>TODOS.md (the one ledger closure; every measurement goes to SUMMARY only)</files>
  <read_first>scripts/local-stack/run.sh (`load_baseline`, `load_reference_data`, the `.stack-env` handoff and `down`); the `sql-tests` job in .github/workflows/ci.yml (how it reads `LANE_DB_URL` and runs a corpus file)</read_first>
  <action>
  (a) Full phase gate, one run, all output kept in the scratchpad: `node scripts/extract-reference-inserts.mjs --self-test`, `--audit`, `bash scripts/restore-test-from-baseline.sh --self-test`, and `npx vitest run` over refdata-allowlist, extract-reference-inserts-selftest-floor, restore-test-from-baseline, test-restore-workflow-wiring, local-stack-lane-wiring and lint-migration-data-dependence. Also run `node scripts/lint-sql-gates.mjs --self-test` and `node scripts/verify-plan-anchors.mjs --pending`.
  (b) Rehearsal, loopback only: `bash scripts/local-stack/run.sh --check-currency` (record the `baseline-replay:` line), then `bash scripts/local-stack/run.sh up`. A boot failure in `load_reference_data` is a FINDING: record the psql error, which statement and which migration. Diagnose whether a replayed UPDATE names a column the final schema lacks or trips a trigger, then STOP and report it as a blocker. Do not re-classify it in this task: a reclassification moves the C5 pins that plans 01, 02 and 04 set, so it is a replan, never a patch. Never edit a migration. On a green boot, read `LANE_DB_URL` from the lane's `.stack-env` handoff, refuse unless it is loopback (the `capability-probe.mjs --refuse-nonlocal-dsn` seam the sql-tests job uses), and run the `v_wrong_state` inner read verbatim with the production defaults: select `manager_status::text` from `public.profiles` where `id::text` is the sentinel id (`REFDATA_WRONGSTATE_ID`'s default in scripts/restore-test-from-baseline.sh). It must read the verified label. The same read on the pre-phase emission reads the DEFAULT, which RESEARCH measured (`newbie`) and the TEST preflight run `36003106273` rejected.
  (c) On the same lane, run every `supabase/tests/*.sql` file that references the sentinel id or `manager_status`, the way the `sql-tests` job invokes a corpus file. A red file is compared against its reading on `main`'s last `sql-tests` run before it is called a regression.
  (d) `bash scripts/local-stack/run.sh down`, then `--assert-teardown`.
  (e) Close TODOS `[164.8.1-REPLAY-INSERT-ONLY-SCOPE]` IN THE PHASE PR (plan-checker W5): tick it, and add a DATED 2026-09-25 closing note. The note says C5 is a new criterion, not a C2 relaxation, and cites the plan 01 census lines and the plan 03 arms 33/34 by name. It keeps the entry's "Not closable by editing the allowlist" paragraph as lineage, with a dated line saying why that sentence no longer describes the code. The TEST evidence (criterion 4) is recorded under `[164.9-CRIT8-RESTORE-DISPATCH-RECORD]`, not here, because it cannot exist before merge. Edit TODOS.md by hand, never through a handler.
  If Docker is unavailable, SUMMARY says so in its first line. (b)-(c) are then NOT RUN, never "passed", and the PR's `sql-tests` / `frontend-local-stack` jobs are named as the only remaining pre-merge reading.
  </action>
  <verify>
    <automated>node scripts/extract-reference-inserts.mjs --self-test && node scripts/extract-reference-inserts.mjs --audit && bash scripts/restore-test-from-baseline.sh --self-test && npx vitest run src/__tests__/refdata-allowlist.test.ts src/__tests__/extract-reference-inserts-selftest-floor.test.ts src/__tests__/restore-test-from-baseline.test.ts src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/local-stack-lane-wiring.test.ts src/__tests__/lint-migration-data-dependence.test.ts && node scripts/lint-sql-gates.mjs --self-test && node scripts/verify-plan-anchors.mjs --pending</automated>
  </verify>
  <done>The gate is green in one run. The lane rehearsal either reads the verified label for the sentinel (with the command and output in SUMMARY) or is recorded as NOT RUN with the reason. Teardown is asserted.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: POST-MERGE, orchestrator only: criterion 4 dispatch and record</name>
  <action>STOP here. The executor does nothing further. After the PR merges through the normal ship flow (`/gsd-ship`, then land, with a CHANGELOG entry and VERSION bump per CLAUDE.md), the ORCHESTRATOR runs the checklist below. The founder authorized that dispatch for this occasion. It is never run by an executor, because the workflow's ref guard refuses any ref that is not `main`.</action>
  <instructions>
  Already automated by plans 01-05 Task 1: the C5 class, its self-test kinds, restore arms 33/34, the workflow census checks, and the local real-dump rehearsal. What remains needs merged `main`:
  1. Confirm the merge commit is on `main` and carries this phase's extractor and allowlist changes (`git log origin/main`, `git show --stat <merge>`). If it does not, STOP.
  2. `node scripts/check-baseline-currency.mjs` at merged `main`. A migration merged after the dump makes the preflight refuse STALE (RESEARCH A3); this phase adds no migration.
  3. `gh workflow run test-restore-from-baseline.yml --ref main -f mode=preflight`. Find the run id with `gh run list --workflow=test-restore-from-baseline.yml --branch main --limit 3 --json databaseId,headSha,event,status`, bind `headSha` to the merge sha, then `gh run watch <id>`.
  4. Read the preflight log for: the extractor self-test census at the measured kind count; both audit census lines; `self-test OK (34/34 arms`; `refdata: 23 statement(s) replayed into 8 table(s)`; the new `refdata: <u> C5 update statement(s) replayed …` note, where `<u>` is the update-statement count recorded VERBATIM in `164.9.2-01-SUMMARY.md`'s C5 census line, never restated from this plan (plan-checker I4); NO `WRONG STATE after the replay`; NO `42501` (D-A1); the closing `preflight: post-census == pre-census (byte-for-byte) … mode=preflight`; and the token line `for reference, this tree's restore token is <token>`. The conclusion must be `success`. On any red, record it and STOP.
  5. Only on a green preflight: `gh workflow run test-restore-from-baseline.yml --ref main -f mode=restore -f confirm=<token from step 4>`, bound to the merge sha the same way.
  6. Record the `restore: tables=… policies=… functions=… ledger_rows=… survivors=…/… filtered=… mode=restore` line VERBATIM, the CLI post-verify, and evidence the run got past `check_extension_guard`. A post-COMMIT red there means TEST WAS rebuilt: record it as such and never re-dispatch blindly (RESEARCH Pitfall 10).
  7. LANDING PATH for the record (plan-checker W5). It goes in a FOLLOW-UP docs PR opened after the dispatch, on its own feature branch off merged `main`, never in the phase PR (the run ids do not exist before merge). That PR carries:
     - both run ids, conclusions, head sha, the summary line, and the backup artifact's name and retention, written into `164.9.2-05-SUMMARY.md` (an addendum closing Task 2), `164.9-11-SUMMARY.md`, and the ROADMAP `### Phase 164.9` criterion 8 block (which reads PENDING);
     - TODOS `[164.9-CRIT8-RESTORE-DISPATCH-RECORD]`, closed and dated;
     - a dated CLAUDE.md correction in "Currency 2026-09-09" naming the new restore run as what set TEST's current state, keeping the earlier attributions as lineage;
     - because CLAUDE.md, TODOS.md, VERSION and CHANGELOG.md are non-planning paths, a VERSION PATCH bump (4-digit, byte-equal in `VERSION` and `package.json`, never `npm version`) and a CHANGELOG entry written by the CLAUDE.md "CHANGELOG discipline" mechanism, in the same `chore(release)` commit.
     Run `check-planning-hygiene` before committing, and convert any verifier `covered_files`/`covered_digest` per CLAUDE.md. The closure of `[164.8.1-REPLAY-INSERT-ONLY-SCOPE]` is NOT in this PR; it already landed in the phase PR (Task 1(e)).
  8. HOW THE PHASE IS VERIFIED AND SHIPPED WHILE THIS TASK IS OPEN (plan-checker W5), following the Phase 164.9 precedent (`164.9-VERIFICATION.md`: criterion 8 recorded as a SKIP, "post-merge by construction", owner named, with no verdict issued on it). The executor writes `164.9.2-05-SUMMARY.md` at Task 1 and marks Task 2 PENDING, owner the orchestrator. `gsd-verifier` runs before ship. It verifies criteria 1-3 against the tree and records criterion 4 as post-merge, owned by the orchestrator under the founder's authorization for this occasion, with this checklist as its recipe, not as a failure or a gap. The phase then ships through `/gsd-ship` and lands. Criterion 4 is discharged by steps 1-7 and the follow-up PR.
  </instructions>
  <verification>`gh run view <preflight-id> --json conclusion,headSha` and `gh run view <restore-id> --json conclusion,headSha` both read `success` with `headSha` equal to the merge sha, and grep finds both run ids in 164.9-11-SUMMARY.md, the ROADMAP 164.9 criterion 8 block, TODOS and CLAUDE.md.</verification>
  <resume-signal>Both run ids recorded with conclusions, or a named red finding with the run id that produced it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local lane → developer machine | Supabase CLI in this checkout is linked to PRODUCTION |
| GitHub Actions → shared TEST | the restore rebuilds a database other people's CI uses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.2-15 | Tampering | a local command reaching PROD through the linked CLI | critical | mitigate | lane only via `scripts/local-stack/run.sh`; DSN refused unless loopback (`--refuse-nonlocal-dsn`); no `supabase` or psql command against a remote |
| T-164.9.2-16 | Tampering | a restore dispatched from an unreviewed ref or without reading the preflight | high | mitigate | orchestrator-only, post-merge; workflow ref guard + `environment: Test` + marker query + mutex 61616158 + confirm token read from the merged preflight log |
| T-164.9.2-17 | Repudiation | a destructive act on shared TEST not auditable | high | mitigate | step 7 records run ids in five places, including the CLAUDE.md currency correction |
| T-164.9.2-18 | Elevation | replayed UPDATE refused by the profiles privileged-column trigger on TEST's role | medium | accept | measured by the preflight (D-A1); a 42501 rolls back and is recorded as a finding |
</threat_model>

<verification>
Task 1's gate is green; the rehearsal reading is recorded or explicitly NOT RUN. Task 2 is done when both run ids are recorded.
</verification>

<success_criteria>
Criterion 4 closes Phase 164.9 criterion 8, recorded by run id in every ledger that carries it.
</success_criteria>

<output>
Create `.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-05-SUMMARY.md` when done
</output>
