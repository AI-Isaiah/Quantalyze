---
phase: 164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m
plan: 02
status: complete
subsystem: shared-TEST restore / reference-data replay
tags: [refdata, extractor, self-test, C5, ratchet, vitest]
requires:
  - "164.9.2-01 (the C5 class and its refusal reasons)"
provides:
  - "20 C5 SELF_TEST_KINDS entries, one per C5 refusal reason, each with a red+green fixture pair"
  - "SELF_TEST_KINDS_FLOOR = 39 (layer 1), measured"
  - "REQUIRED_KINDS pins all 20 c5-* ids by name (layer 2)"
  - "the ORDER leg (c5-update-count-drift.green): emit order is (basename, offset), and an UPDATE never moves refdata-expect"
affects:
  - "plan 04 (ci.yml and the restore workflow read SELF_TEST_KINDS_FLOOR and the self-test OK line; the line format is unchanged, only its number)"
tech-stack:
  added: []
  patterns:
    - "one self-test kind per refusal REASON, not per planned shape"
key-files:
  created:
    - "scripts/extract-reference-inserts-fixtures/c5-*.{red,green}/ (40 fixture dirs)"
  modified:
    - scripts/extract-reference-inserts.mjs
    - src/__tests__/extract-reference-inserts-selftest-floor.test.ts
decisions:
  - "The kind list follows the invariant (every C5 refusal reason has a red leg), not the plan's list of 14: 6 more reasons plan 01 implemented had no leg, so the measured count is 39, not the plan's expected 33"
metrics:
  duration: "about 15 min"
  completed: 2026-09-25
estimate:
  tokens: 100000
  tasks: 2
actuals:
  tokens: 15300
  tasks: 2
  commits: 3
plan_head_before: 4e3c8407d1a81212475671fccbb6039a24a59692
---

# Phase 164.9.2 Plan 02: every C5 refusal has its own red+green self-test kind, ratcheted at 39

The extractor's self-test grew from 19 to 39 kinds. Each of the 20 new `c5-*` kinds has a red fixture that exits 1 with its own reason text and a green twin that exits 0. Both ratchet layers pin the new count: `SELF_TEST_KINDS_FLOOR` in the script, and `REQUIRED_KINDS` by name in vitest.

## Gate output (verbatim, at HEAD `b382f1589`)

```
extract-reference-inserts self-test OK: 39 kinds, red+green each.
extract-reference-inserts audit OK: 22 entr(ies), 19 file(s), 8 table(s), 23 statement(s); every pinned count re-measured, no unlisted top-level reference INSERT.
extract-reference-inserts audit C5 OK: 6 update statement(s) over 4 file(s) and 2 table(s) replayed; 2 declined over 2 file(s); 0 unaccounted.
 Test Files  2 passed (2)
      Tests  24 passed (24)
```
(vitest: `extract-reference-inserts-selftest-floor` + `refdata-allowlist`, the plan's verify.) The self-test OK line keeps its format, so both workflows' `^extract-reference-inserts self-test OK: [0-9]+ kinds, red\+green each\.$` grep still matches.

Wider sweep, outside the plan's verify: `drift-check-scripts`, `local-stack-lane-wiring`, `gitleaks-allowlist` → `770 passed | 1 skipped`. The skip is the existing gitleaks-binary-unavailable test, which depends on the local environment. `lint-migration-data-dependence --self-test` and `check-baseline-currency --self-test` both pass. eslint is clean on both changed source files.

## Reason → kind map (the invariant: every C5 refusal reason has a red leg)

| # | C5 refusal reason (where it lives) | Kind | Mode | In the plan's list? |
|---|---|---|---|---|
| 1 | C5 line on a non-public table (`parseAllowlist`) | `c5-update-auth-target` | emit | yes |
| 2 | duplicate C5 line (`parseAllowlist`) | `c5-duplicate-line` | emit | yes |
| 3 | C5 lines but NO INSERT line (`loadEntries`) | `c5-no-insert-line` | emit | **added** |
| 4 | CTE-prefixed UPDATE (`matchUpdate`) | `c5-cte-prefixed-update` | emit | yes |
| 5 | UPDATE not terminated by `;` (`matchUpdate`) | `c5-update-unterminated` | emit | **added** |
| 6 | not of the shape `UPDATE [ONLY] t [[AS] alias] SET` (`matchUpdate`) | `c5-update-off-shape` | emit | **added** |
| 7 | dollar-quoted body in the span (`matchUpdate`) | `c5-update-dollar-tag` | emit | yes |
| 8 | token FROM/USING/SELECT/RETURNING/WITH (`updateLiteralCheck`) | `c5-update-nonliteral` | emit | yes |
| 9 | a bare `$` (positional parameter) (`updateLiteralCheck`) | `c5-update-positional-param` | emit | **added** |
| 10 | a non-literal call (`updateLiteralCheck`) | `c5-update-function-call` | emit | yes |
| 11 | update: pinned but the only UPDATE is in a body (C1) (`c5Verdict`) | `c5-update-dollar-body` | emit | yes |
| 12 | update: count drift (`c5Verdict`) | `c5-update-count-drift` | emit | yes |
| 13 | top-level literal UPDATE of `auth.users` (`c5Verdict`, `--audit`) | `c5-audit-auth-update` | audit | **added** |
| 14 | a decline: over a literal UPDATE (`c5Verdict`) | `c5-decline-literal` | audit | yes |
| 15 | unlisted literal UPDATE (`c5Verdict`) | `c5-audit-unlisted-update` | audit | yes |
| 16 | decline: count drift (`c5Verdict`) | `c5-decline-count-drift` | audit | yes |
| 17 | top-level non-literal UPDATE of `auth.users` (`c5Verdict`, `--audit`) | `c5-audit-auth-nonliteral-update` | audit | **added** |
| 18 | a non-literal UPDATE beside an update: line only (the reason passes through) | covered by the red legs of rows 7-10, which all carry an `update:` line | emit | yes |
| 19 | unlisted non-literal UPDATE, "classify by hand" (`c5Verdict`) | `c5-audit-unlisted-nonliteral-update` | audit | yes |
| 20 | C5 line on a table no INSERT fills (`modeAudit` membership) | `c5-audit-unfilled-table` | audit | yes |
| 21 | top-level DELETE/TRUNCATE/MERGE on a replayed table (`modeAudit`) | `c5-audit-unlisted-delete` (DELETE leg) | audit | yes |

Not treated as a C5 reason: the field-3 malformed-count refusal. It predates C5 and is shared with INSERT lines. Plan 01 only extended its wording to name the `update:<n>` / `decline:<n>` spellings. Row 21 has one leg for one reason string. The TRUNCATE, MERGE and CTE-prefixed head regexes that feed it have no separate legs.

Several green legs pin an acceptance and not only an absence:
- `c5-update-function-call.green` pins that `AND (` is a boolean group, not a call (plan 01 deviation 1).
- `c5-update-positional-param.green` pins that a `$` inside a string literal is masked away.
- `c5-update-off-shape.green` pins that `ONLY` and an `AS` alias are admitted.
- `c5-duplicate-line.green` pins that one `update:` plus one `decline:` line on the same pair is legal, and its `greenAbsent: /FROM fx_src/` pins that the declined join is not emitted.

## The ORDER leg (`c5-update-count-drift.green`)

Its allowlist lists the `update:1` line FIRST, then the later file's INSERT, then the earlier file's INSERT LAST. The measured stdout body:
```
INSERT INTO fx_ref (id, label) VALUES (1, 'seed') ON CONFLICT (id) DO NOTHING;
-- refdata: 20260101000000_fx_a.sql:2 public.fx_ref
UPDATE fx_ref SET label = 'v' WHERE id = 1;
-- refdata-update: 20260101000000_fx_a.sql:3 public.fx_ref
INSERT INTO fx_ref2 (id, label) VALUES (1, 'later') ON CONFLICT (id) DO NOTHING;
-- refdata: 20260102000000_fx_b.sql:3 public.fx_ref2
-- refdata-expect: public.fx_ref=1
-- refdata-expect: public.fx_ref2=1
```
`greenStdout: /INSERT INTO fx_ref [\s\S]*UPDATE fx_ref [\s\S]*INSERT INTO fx_ref2/`, `greenAbsent: /^-- refdata-expect: public\.fx_ref=(?!1$)/m`.

## RED first

- **Task 1:** the 12 emit-mode entries were added before any fixture existed. The self-test printed 24 `SELF-TEST FAIL` lines, 2 per kind (red for the wrong reason, `allowlist not found`, and green exited 1), and exited 1. After the fixtures were written: `OK: 31 kinds`. Each red's stderr was then read one by one to confirm it fires for its own reason, not only on the `expect` substring.
- **Task 2:** the 8 audit entries were added before their fixtures. The self-test printed 16 `SELF-TEST FAIL` lines and exited 1. After the fixtures: `OK: 39 kinds`.
- **Layer 2 before the floor moved** (floor still 19, corpus 39):
```
     × matches the measured corpus exactly — drift in EITHER direction fails 3ms
AssertionError: RATCHET STALE: the extractor declares 39 self-test kind(s) but SELF_TEST_KINDS_FLOOR is still 19. Raise SELF_TEST_KINDS_FLOOR in scripts/extract-reference-inserts.mjs to 39, from a MEASURED `--self-test` run.: expected 39 to be 19 // Object.is equality
      Tests  1 failed | 6 passed (7)
```

## Guards neutered, observed RED, restored

Each neuter was applied to `scripts/extract-reference-inserts.mjs` after a fresh byte backup `164.9.2-02-extractor.bak`, a per-plan name in the shared scratchpad. After each run the backup was copied back and checked with `cmp` (`cmp OK` every time), and the self-test re-ran green at `OK: 39 kinds`.

**(1) `updateLiteralCheck` returns null**, exit 1:
```
SELF-TEST FAIL: c5-update-nonliteral.red exited 0, expected 1 — the refusal did not fire.
SELF-TEST FAIL: c5-update-function-call.red exited 0, expected 1 — the refusal did not fire.
SELF-TEST FAIL: c5-update-positional-param.red exited 0, expected 1 — the refusal did not fire.
SELF-TEST FAIL: c5-duplicate-line.green exited 1, expected 0 — the refusal fires on a corpus it must accept. Got: extract-reference-inserts: REFUSED 20260101000000_fx_a.sql:5 [public.fx_ref]: the C5 line pins update:1 top-level statement(s) but 2 were measured. A pinned count moves ONLY when an applied migration is edited — understand the edit, do not re-pin the number
SELF-TEST FAIL: c5-decline-literal.green exited 1, expected 0 — the refusal fires on a corpus it must accept. Got: extract-reference-inserts: REFUSED 20260101000000_fx_a.sql:3 [public.fx_ref]: a literal UPDATE is replayed, not declined — this (file, table) carries a decline: line but the statement is literal. Add an update: line for it; a decline must never hide a replayable effect (C5)
SELF-TEST FAIL: c5-decline-count-drift.red exited 1 but its message does not carry "the C5 line pins decline:2 top-level statement(s) but 1 were measured" — a leg that fires for the WRONG reason is not evidence. Got: extract-reference-inserts: REFUSED 20260101000000_fx_a.sql:3 [public.fx_ref]: a literal UPDATE is replayed, not declined — this (file, table) carries a decline: line but the statement is literal. Add an update: line for it; a decline must never hide a replayable effect (C5)
SELF-TEST FAIL: c5-decline-count-drift.green exited 1, expected 0 — the refusal fires on a corpus it must accept. Got: extract-reference-inserts: REFUSED 20260101000000_fx_a.sql:3 [public.fx_ref]: a literal UPDATE is replayed, not declined — this (file, table) carries a decline: line but the statement is literal. Add an update: line for it; a decline must never hide a replayable effect (C5)
SELF-TEST FAIL: c5-audit-unlisted-nonliteral-update.red exited 1 but its message does not carry "classify by hand: decline it with a reason" — a leg that fires for the WRONG reason is not evidence. Got: extract-reference-inserts: REFUSED 20260101000000_fx_a.sql:4 [public.fx_ref]: NO C5 allowlist line; add an update: line for this top-level literal UPDATE of a table the replay fills, with the reason it matters (C5)
SELF-TEST FAIL: c5-audit-unlisted-nonliteral-update.green exited 1, expected 0 — the refusal fires on a corpus it must accept. Got: extract-reference-inserts: REFUSED 20260101000000_fx_a.sql:3 [public.fx_ref]: a literal UPDATE is replayed, not declined — this (file, table) carries a decline: line but the statement is literal. Add an update: line for it; a decline must never hide a replayable effect (C5)
SELF-TEST FAIL: c5-audit-auth-nonliteral-update.red exited 1 but its message does not carry "a top-level non-literal UPDATE of auth.users, and C5 targets public only" — a leg that fires for the WRONG reason is not evidence. Got: extract-reference-inserts: REFUSED 20260101000000_fx_a.sql:3 [auth.users]: a top-level UPDATE of auth.users, which an INSERT entry replays, and C5 targets public only — no allowlist line can account for it, because auth.users survives the restore and replaying the UPDATE would mutate a live row on shared TEST. Move the change out of the migration's top level, or take it to review
```
The plan required failures naming `c5-update-nonliteral` and `c5-update-function-call`. Both fired.

**(2) emit sort removed** (blocks kept in push order), exit 1:
```
SELF-TEST FAIL: c5-update-count-drift.green stdout does not match /INSERT INTO fx_ref [\s\S]*UPDATE fx_ref [\s\S]*INSERT INTO fx_ref2/ — the green leg proved nothing.
```

**(3) extra calibration, not in the plan's list: UPDATEs fed into `perTable`**, the W2 guard. Exit 1:
```
SELF-TEST FAIL: c5-update-count-drift.green stdout matches /^-- refdata-expect: public\.fx_ref=(?!1$)/m, which it must NOT.
```

**(4) extra calibration, not in the plan's list: the `c5-update-dollar-body` kind deleted.** Both layers went red. Layer 1, the self-test, exit 1:
```
SELF-TEST FAIL: SELF_TEST_KINDS_FLOOR regression: the corpus declares 38 kind(s), below the pinned floor of 39. A refusal kind was deleted — restore it, or lower the floor DELIBERATELY in review with the reason. Clearing this by lowering the floor retires the control the deletion just removed.
```
Layer 2, vitest:
```
     × matches the measured corpus exactly — drift in EITHER direction fails 4ms
     × every pinned kind (measured 2026-09-09, C5 added 2026-09-25) is still declared — delete-one-add-one nets zero on the COUNT and fails here 1ms
AssertionError: REGRESSION: the extractor declares 38 self-test kind(s), below the pinned floor of 39. A refusal kind was deleted — restore it. The floor comes down only in review, with the reason.: expected 38 to be 39 // Object.is equality
AssertionError: refusal kind(s) removed from the extractor's self-test corpus: c5-update-dollar-body. Each one is a shape the extractor refuses; without its leg, nothing proves the refusal still fires.: expected [ 'c5-update-dollar-body' ] to deeply equal []
      Tests  2 failed | 5 passed (7)
```

## Commits

| Task | Commit | Message |
|---|---|---|
| 1 | `d3cbb00f0` | test(164.9.2-02): red+green self-test kinds for every emit-mode C5 refusal |
| 2 | `b382f1589` | test(164.9.2-02): audit-mode C5 kinds and both self-test ratchet layers at 39 |

`commits: 3` is measured with `git rev-list --count 4e3c8407d..HEAD`, as the protocol requires. One of the three is plan 03's `32ef481f9`, which landed between this plan's two commits because plans 03 and 04 share this checkout. This plan made 2 commits of its own, plus the docs commit for this file.

## Deviations from Plan

**1. [Rule 2 - Correctness] Six more kinds than the plan listed, so the floor is 39, not 33**
- **Found during:** Task 1, while listing every C5 reason string in plan 01's code (the plan's own instruction: "If plan 01 implemented a C5 refusal this list does not cover, add a kind for it too").
- **Issue:** six C5 refusal reasons had no red leg: the C5-only allowlist refusal (plan 01 deviation 2), the unterminated UPDATE, the off-shape head, the bare-`$` positional parameter, and the two `auth.users` branches in `c5Verdict` (literal and non-literal). Deleting any of them would have left the self-test green.
- **Fix:** added `c5-no-insert-line`, `c5-update-unterminated`, `c5-update-off-shape` and `c5-update-positional-param` in Task 1, and `c5-audit-auth-update` and `c5-audit-auth-nonliteral-update` in Task 2. Task 1's done-count therefore read 31, not 27, and the measured total is 39, not 33. The floor was set from the measured run, as the prohibition requires. No other plan pins 33: checked by grep over plans 03-05, `.github`, `src` and the scripts.
- **Commits:** `d3cbb00f0`, `b382f1589`.

**2. Test title corrected.** In the vitest file, the `REQUIRED_KINDS` test title said "every kind measured on 2026-09-09". Once the list carried the 2026-09-25 C5 ids, that title was a false sentence inside a gate. It now reads "every pinned kind (measured 2026-09-09, C5 added 2026-09-25) is still declared …". Nothing else references the title (grep). The assertion is unchanged.

No existing kind, fixture or `expect` string was edited.

## Environment note

The worktree's `node_modules` symlink was created at 09:15 local time by a sibling plan running in the same checkout. The constraint says to remove the symlink before each commit, but this plan did not create it, and removing it would break the sibling's in-flight runs. It was left in place. It is gitignored (`.gitignore:4:/node_modules`) and was never staged. Every commit staged explicit paths only, and `check-planning-hygiene` printed OK before each commit.

## Threat surface

No new surface. T-164.9.2-07 (a C5 refusal silently deleted) is mitigated: two layers, both observed red on one deletion (neuter 4). T-164.9.2-08 (a leg that fires for the wrong reason) is mitigated: every `expect` is copied from the source's reason text, and neuter 1 shows the self-test rejecting legs that fire for a different C5 reason. The fixtures hold no credential-shaped literal, no real table data and no path.

## Not done here (by instruction)

STATE.md, ROADMAP.md and REQUIREMENTS.md were not touched, and no gsd-tools state or roadmap handler was run. `[164.8.1-REPLAY-INSERT-ONLY-SCOPE]` closes in plan 05.

## Self-Check: PASSED

- scripts/extract-reference-inserts.mjs, src/__tests__/extract-reference-inserts-selftest-floor.test.ts: FOUND
- 40 fixture dirs `scripts/extract-reference-inserts-fixtures/c5-*.{red,green}`: FOUND (20 kinds × 2)
- commits d3cbb00f0, b382f1589: FOUND in `git log`
