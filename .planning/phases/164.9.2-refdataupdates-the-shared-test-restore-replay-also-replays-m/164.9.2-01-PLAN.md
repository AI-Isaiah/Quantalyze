---
phase: 164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract-reference-inserts.mjs
  - scripts/restore-test-refdata-allowlist.txt
  - src/__tests__/refdata-allowlist.test.ts
autonomous: true
requirements:
  - "[164.8.1-REPLAY-INSERT-ONLY-SCOPE]"

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The extractor emits the literal top-level UPDATEs the allowlist names with `update:<n>`, and the teaser sentinel's `manager_status` UPDATE in 20260521150000_universal_signup_approval_gate.sql appears AFTER the sentinel's INSERT in 20260515095804_teaser_anchor_strategy.sql in the emitted SQL (criterion 2)."
    - "Emit order is (migration basename, byte offset), never allowlist-line order: the 20260429063138 strategies UPDATE is emitted BEFORE every 20260515095804 INSERT even though its allowlist line sits below them."
    - "The INSERT half of the emission is byte-identical to the pre-phase emission: same blocks, same order, same `-- refdata:` and `-- refdata-expect:` trailers. UPDATE blocks carry only `-- refdata-update:` trailers, so the restore's count floor stays INSERT-only."
    - "`--audit` accounts for EVERY top-level UPDATE/DELETE/TRUNCATE/MERGE on a table an INSERT entry fills: a literal UPDATE needs an `update:` line, a non-literal one a `decline:` line with a reason, anything else is a named refusal. It prints a separate C5 census line; the existing `audit OK` line keeps its four INSERT-only numbers byte-for-byte (per D-03)."
    - "C5 never admits a non-`public` target. `auth.users`, the INSERT-only C3 exception, is refused for `update:` and `decline:` lines."
  artifacts:
    - path: scripts/extract-reference-inserts.mjs
      provides: "C5 parse kinds, updateLiteralCheck, matchUpdate, sorted emit, C5 audit + census"
    - path: scripts/restore-test-refdata-allowlist.txt
      provides: "4 `update:` lines + 2 `decline:` lines, C5 criterion text, dated SCOPE BOUNDARY and W3 corrections"
    - path: src/__tests__/refdata-allowlist.test.ts
      provides: "ENTRY_COUNT (INSERT, 22) and C5_ENTRY_COUNT (6); class-aware re-measurement and C5 calibration"
  key_links:
    - "`filterRefdataAllowlist` in scripts/check-baseline-currency.mjs keys on field 1 only, so the local-stack lane receives C5 lines unchanged; no lane change is needed."
    - "The restore's `refuse_bad_refdata_allowlist` reads ONLY `-- refdata-expect:` trailers; UPDATEs must never produce one."
  prohibitions:
    - "⛔ No floor lowered, no waiver, no INSERT allowlist line widened or re-counted. C5 is a new, separately pinned class."
    - "⛔ No database command against any remote. This plan is node + vitest only."
---

<objective>
Add criterion C5 to the reference-data replay: a top-level literal `UPDATE` on a `public` table the
replay already fills is replayed, in migration filename order, beside the INSERTs. Non-literal
UPDATEs on those tables are declined by name (per D-02). Everything else the audit cannot classify is
refused by name.

Purpose: a shared-TEST restore rebuilds the teaser sentinel profile with `manager_status` at the value
PROD holds, which is the value Phase 164.9 plan 07's wrong-state leg pins. That leg stays unchanged
(criterion 3). It goes green because the replay now reproduces the UPDATE.

Output: the extended extractor, the six C5 allowlist lines, and the vitest pins for them.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-CONTEXT.md
@.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-RESEARCH.md
@scripts/extract-reference-inserts.mjs
@scripts/restore-test-refdata-allowlist.txt
@src/__tests__/refdata-allowlist.test.ts

<interfaces>
Symbols in scripts/extract-reference-inserts.mjs this plan extends (read them first):
- `parseAllowlist(text)` returns `{ entries, malformed }`; each entry is `{lineNo, file, qualified, schema, table, count, consumer}`. Field 3 today must match a positive integer.
- `schemaAllowed(qualified)` returns true for `public.*` or exactly `auth.users` (the C3 exception). C5 must NOT use it for `update:`/`decline:` lines.
- `literalCheck(maskedStatement)`, `prepareFile(src)`, `headRe`, `cteHeadRe`, `quotedId`, `matchTable(src, prep, qualified)` (returns ok/body/rejected spans), `refuse(io, {file, line, table, reason})`, `loadEntries`, `classifyEntry`, `modeEmit`, `modeAudit`.
- `dollarBodyRanges` and `insideBody` are imported by scripts/lint-migration-data-dependence.mjs and scripts/local-stack/nonpublic-objects.mjs. Keep both exports' signatures unchanged.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: tracer, one `update:` line replayed end-to-end in filename order</name>
  <files>scripts/extract-reference-inserts.mjs, scripts/restore-test-refdata-allowlist.txt</files>
  <read_first>scripts/extract-reference-inserts.mjs (parseAllowlist, literalCheck, matchTable, classifyEntry, modeEmit, modeAudit), supabase/migrations/20260521150000_universal_signup_approval_gate.sql, supabase/migrations/20260515095804_teaser_anchor_strategy.sql</read_first>
  <action>
  First, before any edit, record the reference emission from the pre-phase tree, into the scratchpad and never the repo: run the extractor from `git show 96b5db4c5:scripts/extract-reference-inserts.mjs` and `git show 96b5db4c5:scripts/restore-test-refdata-allowlist.txt` copies against `supabase/migrations`, and save stdout plus its sha256. RESEARCH measured the prefix `de3d303c26b2235c`; if the recomputed one differs, record both and use the recomputed one. Its `lint-sql-gates.mjs` import must resolve, so place the copy beside the repo's `scripts/` or copy that file too.

  Then, in scripts/extract-reference-inserts.mjs:
  (a) `parseAllowlist` accepts field 3 as `<n>` (kind `insert`, unchanged grammar), `update:<n>` (kind `update`) or `decline:<n>` (kind `decline`), with `<n>` a positive integer. Every entry gains a `kind` property. For `update`/`decline` lines, field 2 must be `public.*`. `auth.users` is refused by a named malformed reason saying C5 targets public only, because auth.users survives the DROP and an UPDATE there would mutate shared TEST's live row. The `# reason` field stays required, as for INSERT lines.
  (b) Add an exported `updateLiteralCheck(masked)`, run over the MASKED statement (RESEARCH Pattern 2). It refuses the word tokens FROM, USING, SELECT, RETURNING and WITH; any `$`; and any `identifier(` call except `IN (` and `now(`. Each refusal returns a reason naming C5. Add `matchUpdate(src, prep, qualified)` beside `matchTable`, sharing `prepareFile`, `inBody` and `quotedId`. Its head regex is `UPDATE [ONLY] <target> [[AS] alias] SET`, schema-qualified or bare, quoted spellings too. It returns ok/body/rejected spans like `matchTable`. A CTE-prefixed `WITH … UPDATE <target>` is rejected by name (RESEARCH Pitfall 7), mirroring `cteHeadRe`. Unterminated spans and `$`-tags are rejected as `matchTable` does.
  (c) Ok spans from BOTH matchers carry the byte offset of the statement's first token (`first`). `modeEmit` collects `{file, first, text}` for every ok span of every `insert` and `update` entry, then sorts by (file basename, first), and only then pushes (RESEARCH Pattern 4). INSERT blocks keep their exact current text and `-- refdata: <file>:<line> <table>` trailer. UPDATE blocks end with `-- refdata-update: <file>:<line> <table>` and contribute NOTHING to `perTable`, so no `-- refdata-expect:` trailer ever counts an UPDATE (RESEARCH Pitfall 2). The existing stderr summary line keeps its INSERT-only numbers, byte-stable. A separate stderr line reports the update count. `decline` entries emit nothing.
  (d) `classifyEntry` dispatches on `kind`. For an `update` entry, the pinned count must equal the measured ok spans, and any rejected span refuses with its reason. `--audit` must not crash on the new kinds (Task 2 adds the audit's C5 rules).
  (e) Update the generated-SQL header comment in `modeEmit` so it no longer says the file carries only INSERTs, if it does. The within-file order is still authored order.

  In scripts/restore-test-refdata-allowlist.txt, add one line in a new `C5` section at the END of the file (below every INSERT line, so line order differs from filename order on purpose): `20260521150000_universal_signup_approval_gate.sql` TAB `public.profiles` TAB `update:3` TAB a `#` reason naming its three top-level UPDATEs. Name the manager_status one as the one that moves the teaser sentinel to the value the wrong-state leg reads. Cite anchors by statement text, not line number.
  </action>
  <verify>
    <automated>node scripts/extract-reference-inserts.mjs --self-test && node scripts/extract-reference-inserts.mjs --audit && node scripts/extract-reference-inserts.mjs > "$TMPDIR/c5-emit.sql" && grep -n -e '^-- refdata-update: 20260521150000' -e '^-- refdata: 20260515095804.*public.profiles' "$TMPDIR/c5-emit.sql"</automated>
  </verify>
  <done>Self-test still prints `19 kinds`, and `--audit` still prints `audit OK: 22 entr(ies), 19 file(s), 8 table(s), 23 statement(s)`. The emission carries 3 `-- refdata-update:` trailers. The sentinel profile INSERT's trailer line number is LOWER than every 20260521150000 update trailer's. With the UPDATE blocks removed, the emission equals the recorded pre-phase emission byte for byte (sha recorded in SUMMARY).</done>
</task>

<task type="auto">
  <name>Task 2: the C5 audit accounts for every UPDATE, plus the remaining lines and the census</name>
  <files>scripts/extract-reference-inserts.mjs, scripts/restore-test-refdata-allowlist.txt</files>
  <read_first>scripts/extract-reference-inserts.mjs (modeAudit, its audit-unlisted rule and census block); the six migration files named in RESEARCH Q2's table</read_first>
  <action>
  In `modeAudit` (ONE classification path; reuse `prepareFile`, never a second lexer):
  (a) Let the INSERT-filled tables be the `qualified` values of `insert` entries. For every migration and every top-level span (outside `inBody`) whose head is UPDATE, DELETE, TRUNCATE or MERGE on one of those tables, the (file, table) pair must be accounted for:
    - a literal UPDATE with no `update:` line refuses: "NO C5 allowlist line; add an update: line";
    - a non-literal UPDATE with no line refuses: "classify by hand: decline it with a reason, this extractor does not replay joins";
    - DELETE/TRUNCATE/MERGE refuses: "C5 replays UPDATE only";
    - an `update:` line on a non-literal statement refuses with the `updateLiteralCheck` reason;
    - a `decline:` line on a literal statement refuses: "a literal UPDATE is replayed, not declined";
    - an `update:`/`decline:` count different from the measured count refuses with the same "pins N … M were measured" shape the INSERT side uses.
  Use the heads of `headRe`/`cteHeadRe` for the matching; a CTE-prefixed data-modifying statement on such a table is refused by name.
  (b) Membership: an `update:`/`decline:` line whose table no `insert` entry fills refuses in `--audit` ONLY. Emit mode checks `public.*` alone, because the lane's filtered allowlist can legitimately drop the INSERT line (RESEARCH Pitfall 6).
  (c) The existing census lines and the `extract-reference-inserts audit OK: …` line count `insert` entries ONLY, so the four numbers stay 22/19/8/23 byte-for-byte (per D-03). Add after it exactly one line, in exactly this shape (CONTEXT "Census line"): `extract-reference-inserts audit C5 OK: <u> update statement(s) over <uf> file(s) and <ut> table(s) replayed; <d> declined over <df> file(s); 0 unaccounted.` Optionally add indented C5 detail lines above it. Nothing prints values or statement text (public Actions log); reasons carry file, line, table and shape only.

  In the allowlist, add the remaining C5 lines in the C5 section, per RESEARCH Q2 and D-02:
  - `update:1` for 20260429063138_seed_is_example_backfill.sql on public.strategies;
  - `update:1` for 20260709130000_strategies_asset_class.sql on public.strategies;
  - `update:1` for 20260716120000_backfill_staff_role_both.sql on public.profiles;
  - `decline:1` for 20260521190243_canonicalize_supported_exchanges.sql on public.strategies, reason: a FROM/sub-select with function calls, a non-literal join C5 does not replay (per D-02);
  - `decline:1` for 20260530120000_admin_role_mutate.sql on public.profiles, reason: an UPDATE … FROM user_app_roles, a table the replay never fills (per D-02).
  Write into the header: C5's criterion text beside C1-C4 (what it admits, the public-only rule, the no-`auth.*` rule, the decline spelling and why it exists); a DATED 2026-09-25 correction to the `SCOPE BOUNDARY` block (it says "five" UPDATEs; the census is 8, 6 replayed and 2 declined, as C5), keeping the original text as lineage; and a dated correction to the W3 recipe. The old recipe counts C5 lines' files too (19 → 25, RESEARCH Pitfall 4). Replace it with a class-aware recipe that filters field 3 to a bare integer before `cut -f1 | sort -u | wc -l`, add a second recipe for the C5 lines, and keep the old paragraph as lineage. The `22 ENTRIES over 19 FILES over 8 TABLES … 23 STATEMENTS` sentence stays true for the INSERT class; add the C5 counterpart (6 lines, 6 files, 2 tables, 6 replayed + 2 declined statements).
  Re-derive every number by running `--audit`. The numbers above come from RESEARCH; if the run disagrees, the run is right, and SUMMARY records the difference.
  </action>
  <verify>
    <automated>node scripts/extract-reference-inserts.mjs --audit | grep -E '^extract-reference-inserts audit (OK: 22 entr\(ies\), 19 file\(s\), 8 table\(s\), 23 statement\(s\);|C5 OK: 6 update statement\(s\) over 4 file\(s\) and 2 table\(s\) replayed; 2 declined over 2 file\(s\); 0 unaccounted\.$)' | wc -l | grep -qx ' *2' && node scripts/extract-reference-inserts.mjs --self-test</automated>
  </verify>
  <done>Both census lines print with the numbers above. Deleting any one C5 line from a scratch copy of the allowlist (passed via `--allowlist`) makes `--audit` exit 1 naming that file and table; this was observed for one `update:` line and one `decline:` line, then the copy was discarded, and SUMMARY records both outputs. Self-test is still green at 19 kinds (plan 02 adds the C5 kinds).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: vitest pins for the C5 class, with ENTRY_COUNT split in two</name>
  <files>src/__tests__/refdata-allowlist.test.ts</files>
  <read_first>src/__tests__/refdata-allowlist.test.ts (ENTRY_COUNT, measure(), "every pinned count agrees…", "CALIBRATION", the AIM block)</read_first>
  <behavior>
    - `ENTRY_COUNT` stays exported at 22 and now counts `kind === "insert"` entries only. A new exported `C5_ENTRY_COUNT = 6` counts `update` + `decline` entries, asserted as 4 + 2 by kind.
    - "every pinned count agrees…" branches on kind: insert entries are measured with `matchTable`, update entries with `matchUpdate`'s ok spans. For decline entries, the non-literal rejected UPDATE spans must equal the pin.
    - A C5 CALIBRATION: an `update:` count moved by +1 and by -1 in a string copy is caught in both directions; a `decline:` pin moved by +1 is caught.
    - AIM: `parseAllowlist` separates an `update:` line, a `decline:` line and a malformed `update:` line on `auth.users` (named reason), and `updateLiteralCheck` returns non-null for a FROM join and a `current_setting(` call and null for a literal SET … WHERE … IN (…).
  </behavior>
  <action>
  Extend src/__tests__/refdata-allowlist.test.ts per the behavior block, in the file's existing idioms (named failure messages that say what to do, calibrations that mutate a copy and assert the check notices). Keep every existing assertion's meaning. Update the header comment's pin inventory for the split. RED first: write the C5 assertions, run them against Task 2's allowlist with `C5_ENTRY_COUNT` deliberately set to 5, observe the named failure, then set 6. Record the RED output in SUMMARY. Grep the file for every literal `22` and `ENTRY_COUNT` use and decide each by its meaning, never by blanket replacement.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/refdata-allowlist.test.ts src/__tests__/local-stack-lane-wiring.test.ts src/__tests__/extract-reference-inserts-selftest-floor.test.ts</automated>
  </verify>
  <done>All three files pass. The deliberate C5_ENTRY_COUNT=5 RED was observed and recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration corpus → replayed SQL | statement bytes from applied migrations are executed inside the restore transaction on shared TEST |
| allowlist → classifier | a human-edited file decides which statements execute |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.2-01 | Tampering | C5 target schema | high | mitigate | `update:`/`decline:` lines refused unless `public.*`; `auth.users` explicitly refused (Task 1a) |
| T-164.9.2-02 | Elevation | side-effecting call inside a replayed UPDATE (`net.http_*`, `pg_notify`, `current_setting`) | high | mitigate | `updateLiteralCheck` call ban; only `IN (` and `now(` admitted (Task 1b) |
| T-164.9.2-03 | Tampering | out-of-order replay leaves a row in a stale state that passes the count gates | high | mitigate | (basename, offset) sort; the 20260429063138 line placed below its successors in the allowlist so the real corpus exercises the sort (Task 1c) |
| T-164.9.2-04 | Tampering | UPDATE counted into the restore's count floor, reading SHORT and aborting every restore | medium | mitigate | UPDATE blocks emit only `-- refdata-update:` trailers; byte-identity check of the INSERT half (Task 1) |
| T-164.9.2-05 | Repudiation | an UPDATE on a replayed table silently skipped | medium | mitigate | `--audit` accounts for every UPDATE/DELETE/TRUNCATE/MERGE; decline requires a reason and refuses a literal statement (Task 2) |
| T-164.9.2-06 | Information disclosure | reasons printed to a public Actions log | low | mitigate | reasons carry file, line, table and shape only, never values |
</threat_model>

<verification>
`node scripts/extract-reference-inserts.mjs --self-test && node scripts/extract-reference-inserts.mjs --audit` exit 0 with both census lines. The three vitest files pass. The INSERT half of the emission is byte-identical to the pre-phase emission.
</verification>

<success_criteria>
C5 exists as a separately pinned class with its own census line and vitest pins (criterion 1, excluding the self-test kinds, which are plan 02). The emission interleaves by filename order (criterion 2). No existing INSERT pin moved.
</success_criteria>

<output>
Create `.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-01-SUMMARY.md` when done
</output>
