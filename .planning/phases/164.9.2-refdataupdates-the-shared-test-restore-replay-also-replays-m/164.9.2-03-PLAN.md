---
phase: 164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m
plan: 03
type: execute
wave: 2
depends_on: ["164.9.2-01"]
files_modified:
  - scripts/restore-test-from-baseline.sh
  - scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql
  - scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt
  - src/__tests__/restore-test-from-baseline.test.ts
autonomous: true
requirements:
  - "[164.8.1-REPLAY-INSERT-ONLY-SCOPE]"

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Inside ONE restore transaction on a throwaway cluster, a fixture row inserted at its schema DEFAULT and then UPDATEd by a later statement in the same allowlisted migration comes back at the UPDATEd value, and the UNCHANGED wrong-state leg is quiet on it (arm 33 GREEN, criteria 2 and 3)."
    - "The same restore with the fixture's `update:` line removed from a scratch allowlist aborts naming `WRONG STATE after the replay` and `public.fx_keep.status for id=3`, and rolls back (arm 34 RED)."
    - "Neutering the emit sort in a COPY of the extractor, pointed at by arm 33's `ARM_REFDATA_EXTRACTOR` seam, turns arm 33 RED, because the fixture allowlist lists the `update:` line ABOVE the INSERT line. The tracked extractor, which plan 02 edits in the same wave, is never touched by this plan (plan-checker W3)."
    - "`REFDATA_ENTRY_N` counts INSERT-class lines only, so its note (`… allowlist line(s) …, each with a pinned statement count`) stays true and reads 22 (fixture 1), and an allowlist holding only C5 lines is refused as empty (plan-checker W6)."
    - "The `v_wrong_state` predicate, the four REFDATA_WRONGSTATE_* seams and their defaults, and arms 31/32 are byte-identical. Only the RAISE's explanatory clause is corrected, dated, per D-01."
    - "A restore run logs how many C5 UPDATE statements it replayed, as a separate note line, so the shared-TEST preflight log is evidence that C5 ran."
  artifacts:
    - path: scripts/restore-test-from-baseline.sh
      provides: "arms 33/34, EXPECTED_ARMS=34, C5 note line, INSERT-only REFDATA_ENTRY_N, D-01 dated RAISE correction"
    - path: scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql
      provides: "one literal top-level UPDATE of row 3 after its INSERT"
    - path: scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt
      provides: "`update:1` line placed ABOVE the INSERT line"
    - path: src/__tests__/restore-test-from-baseline.test.ts
      provides: "every EXPECTED_ARMS=32 literal re-pinned to 34 with a new MEASURED date"
  key_links:
    - "`refdata_n` in `build_transaction` counts `-- refdata: ` trailers only, and `refuse_bad_refdata_allowlist` reads `-- refdata-expect: ` only, so UPDATE blocks cannot move the count floor or the existing note."
    - "The `restore:` summary line must not grow a field; restore-test-from-baseline.test.ts pins that arms 9, 12 and 18 match it byte-exactly."
  prohibitions:
    - "⛔ Criterion 3: the wrong-state predicate, seams, default and arms 31/32 are not edited. No waiver, no relaxed default."
    - "⛔ No new fixture migration FILE; the ledger arms pin the fixture file count (RESEARCH Pitfall 9)."
    - "⛔ No command against shared TEST or PRODUCTION; `--self-test` boots a throwaway cluster."
    - "⛔ No backtick, no quoted row value (`'verified'`, `'newbie'`) inside the TXN heredocs; refusal 9 and arm 31's value-leak check forbid both."
---

<objective>
Prove, inside the restore transaction on a throwaway cluster, that a replayed UPDATE lands after its
INSERT and that the unchanged wrong-state leg is the oracle that tells the two apart. Then correct
the one sentence of that leg's RAISE message that C5 makes false (per D-01).

Purpose: criteria 2 and 3 at the transaction level. Plan 01 proves the order in the emitted SQL; this
proves it where the SQL executes.

Output: fixture UPDATE and allowlist line, arms 33 (GREEN) and 34 (RED), the arm ratchet moved 32 → 34,
a C5 note line, and the dated RAISE correction.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-CONTEXT.md
@.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-RESEARCH.md
Read the prior plan summary `.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-01-SUMMARY.md` (written when that plan executes; it records the exact reason strings and census text).
@scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql
@scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt

<interfaces>
In scripts/restore-test-from-baseline.sh, by symbol:
- `run_leg` / `arm_env` (the arm launcher), with the locals `ARM_REFDATA_ALLOWLIST` (default `$FIXTURES/refdata-allowlist.txt`) and `ARM_WRONGSTATE_ID` (default `1`) that feed `REFDATA_ALLOWLIST` and `REFDATA_WRONGSTATE_ID`.
- `arm_wrongstate_red` (arm 31, id=2) and `arm_wrongstate_green` (arm 32, id=1): the shapes to copy. Arm 24 (`arm_bad_refdata_allowlist`) shows the scratch-copy allowlist idiom.
- `build_transaction`: `refdata_n` awk, the `note "refdata: ${refdata_n} statement(s) replayed …"` line, and the `TXN_REFDATA_GATE` heredoc holding `v_wrong_state` and its RAISE.
- The arm table comment block (`31  RED  value-pinning leg: WRONG STATE`), the "Arms 28-32 are THIS phase's" paragraph, the `MEASURED 2026-09-21 … prints 32/32` line, `EXPECTED_ARMS=32`, and the `run_arm "31 …"` / `run_arm "32 …"` lines.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: tracer, a fixture UPDATE replayed inside the restore transaction (arms 33/34)</name>
  <files>scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql, scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt, scripts/restore-test-from-baseline.sh, src/__tests__/restore-test-from-baseline.test.ts</files>
  <read_first>scripts/restore-test-from-baseline.sh (arms 24, 31, 32, run_arm list, arm table, EXPECTED_ARMS block, build_transaction replay section); src/__tests__/restore-test-from-baseline.test.ts (every `EXPECTED_ARMS=32` literal and the `prints N/N` prose check)</read_first>
  <action>
  (a) In `20260103000000_fixture_c.sql`, AFTER its two INSERTs, add one top-level literal statement: UPDATE fx_keep, setting status to the verified label, WHERE id = 3. Add a comment above it: row 3 is inserted without `status`, so it starts at the fixture's DEFAULT, and this UPDATE is the fixture analog of 20260521150000's sentinel UPDATE (164.9.2 C5). Row counts and statement counts of the INSERT class do not move, so arms 22, 23 (legs c/d) and 24 (the 2-over-3 shape) are unaffected. Do not change the existing lines, and keep the allowlist's `:34 and :36` anchors valid (append only).
  (b) In the fixture allowlist, add `20260103000000_fixture_c.sql` TAB `public.fx_keep` TAB `update:1` TAB a `#` reason, placed ABOVE the existing INSERT line, so line order differs from statement order on purpose.
  (c) Add two arms after arm 32, in the same shape:
    - `arm_c5_update_green` (arm 33 GREEN): `ARM_WRONGSTATE_ID="3"`, restore mode; exit 0; no `WRONG STATE after the replay`; the premise check `SELECT status FROM public.fx_keep WHERE id = 3;` reads the verified label; the run log carries the C5 note line from Task 2 (add that assertion in Task 2).
    - `arm_c5_update_red` (arm 34 RED): a scratch copy of the fixture allowlist with the `update:1` line removed (arm 24's idiom), `ARM_WRONGSTATE_ID="3"`; exit 1; the log carries `WRONG STATE after the replay` and `public.fx_keep.status for id=3`; no raw quoted value (arm 31's check); the `e2e_leftover` stray survives (rolled back).
    Register both with `run_arm "33 GREEN …"` / `run_arm "34 RED …"` labels citing 164.9.2 C5, extend the arm table comment, and add a paragraph after "Arms 28-32 are THIS phase's" saying arms 33-34 are Phase 164.9.2's, and that the falsifiers were observed RED and are recorded in SUMMARY.
  (d) `EXPECTED_ARMS=32` → `EXPECTED_ARMS=34`, with the MEASURED line re-dated (2026-09-25, `prints 34/34`) from a real run. In restore-test-from-baseline.test.ts, grep every `EXPECTED_ARMS=32` and `32` arm literal and re-pin each by meaning. The calibration that moves the constant (`EXPECTED_ARMS=33` today) must move to a value that is still a mutation (e.g. 35). Never blanket-replace.
  </action>
  <verify>
    <automated>bash scripts/restore-test-from-baseline.sh --self-test 2>&1 | tail -5 | grep -F 'self-test OK (34/34 arms' && npx vitest run src/__tests__/restore-test-from-baseline.test.ts</automated>
  </verify>
  <done>`--self-test` prints 34/34 and exits 0; arms 31 and 32 are unchanged and still pass; vitest is green. Before the fixture UPDATE existed, arm 33 was observed RED (the premise read the DEFAULT), and that output is in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: C5 note line, INSERT-only entry count (W6), D-01 dated RAISE correction, neutered-sort falsifier on a COPY</name>
  <files>scripts/restore-test-from-baseline.sh, src/__tests__/restore-test-from-baseline.test.ts</files>
  <read_first>scripts/restore-test-from-baseline.sh (`refuse_bad_refdata_allowlist`: the `REFDATA_ENTRY_N` awk, the ZERO-entries refusal and the `note "refdata: … table(s) from ${REFDATA_ENTRY_N} allowlist line(s) …, each with a pinned statement count"` line; `build_transaction`: `refdata_n`, its `note` line, `TXN_REFDATA_GATE`, `refuse_backticks_in_txn_heredocs`; arm 24 leg (b); the `run_leg` default `ARM_REFDATA_EXTRACTOR`)</read_first>
  <action>
  (a) C5 note. In `build_transaction`, beside `refdata_n`, count `-- refdata-update: ` trailers in `$RESTORE_OUT_DIR/refdata.sql` into `refdata_update_n`, using the same awk shape. Emit a SEPARATE `note` line after the existing one, which stays byte-identical: `refdata: ${refdata_update_n} C5 update statement(s) replayed in migration filename order inside the same transaction`. Do not add a field to the `restore:` summary line. Arm 33 asserts this note with `1`.
  (b) `REFDATA_ENTRY_N` (plan-checker W6). Decision: it counts only INSERT-class lines, those whose field 3 is a bare positive integer. Today it counts every non-comment line, so the C5 lines would move it from 22 to 28 on the real allowlist and from 1 to 2 on the fixture. Its note would then describe a `decline:` line as a pinned statement count, and its ZERO-entries refusal would pass an allowlist holding only C5 lines, which replays no row the count floor can measure. With the field-3 filter, the note stays true word for word and still reads 22 (fixture 1). The zero refusal then fires on "no INSERT line", which is the defect it exists for. Keep the refusal text's `carries ZERO entries` and `that is the Phase 164.8 defect, not a clean run` substrings that arm 24 leg (b) greps; you may add a clause saying C5 lines alone do not count. Pins: arm 33 asserts `from 1 allowlist line(s)` although the fixture allowlist now holds 2 lines. A new leg in arm 34 (not a new arm) feeds a scratch allowlist holding ONLY the fixture's `update:1` line and asserts the ZERO-entries refusal with exit 1. A vitest assertion in restore-test-from-baseline.test.ts pins the live awk's field-3 filter, and a calibration shows that removing the filter fails it.
  (c) Per D-01, correct ONLY the explanatory clause of the `v_wrong_state` RAISE in `TXN_REFDATA_GATE`. Keep the `restore aborted: reference row(s) came back in the WRONG STATE after the replay — %` prefix and the `%` argument byte-identical. Replace the sentences claiming the replay reproduces INSERT effects only and that the gap is not closable by editing the allowlist with a dated clause, CORRECTED 2026-09-25 (Phase 164.9.2 C5): literal top-level UPDATEs on replayed public tables now replay in migration filename order, so a row still in the wrong state means an UPDATE it needs was declined, left unlisted, or replayed out of order; see the allowlist's C5 and SCOPE BOUNDARY blocks. The clause carries no backtick, no `$` and no quoted row value, and it doubles any single quote. Do not touch the predicate, the seams or their defaults. Correct the two header comments that restate the old claim (the REFDATA_WRONGSTATE seam comment block and the leg's SQL comment) with the same dated wording, keeping the old sentence as lineage.
  (d) Neutered-sort falsifier, on a COPY, never the tracked extractor (plan-checker W3). Plan 02 edits scripts/extract-reference-inserts.mjs in the same wave, so this plan never writes to it. Copy `scripts/extract-reference-inserts.mjs` and `scripts/lint-sql-gates.mjs` (its import) into the scratchpad directory `164.9.2-03-neuter/`. Change the COPY's `modeEmit` to push blocks in entry order (no sort). Before observing anything, run the copy UN-neutered once with `--allowlist`/`--migrations` pointing at the fixtures. It must emit the same bytes as the tracked extractor. A copy that emits nothing (arm 24 leg (c) records a path-spelling main-guard that once did exactly this under a symlinked temp dir) would make arm 33 fail for the wrong reason. The observed failure must be the premise message (row 3 at the DEFAULT), not an extraction refusal. Take a byte backup of scripts/restore-test-from-baseline.sh as `164.9.2-03-restore.bak` (per-plan name; parallel agents share one scratchpad directory). Temporarily set `local ARM_REFDATA_EXTRACTOR=` to the copy's path inside `arm_c5_update_green` only, which is the same seam arm 24 leg (c) uses for its stub. Run `--self-test` and observe arm 33 FAIL: with the `update:1` line listed above the INSERT, the UPDATE runs before row 3 exists and matches nothing, so the premise reads the DEFAULT. Restore the script from the backup, `cmp` it, and re-run to 34/34. Record the failing arm output verbatim in SUMMARY. Never `git checkout --`, restore or stash.
  </action>
  <verify>
    <automated>bash scripts/restore-test-from-baseline.sh --self-test 2>&1 | tail -5 | grep -F 'self-test OK (34/34 arms' && npx vitest run src/__tests__/restore-test-from-baseline.test.ts src/__tests__/test-restore-workflow-wiring.test.ts && git diff --quiet HEAD -- scripts/extract-reference-inserts.mjs</automated>
  </verify>
  <done>Arm 33's log carries the C5 note (`1`) and `from 1 allowlist line(s)`. Arm 34's only-C5 leg refuses with ZERO entries. The RAISE prefix, predicate and seams are unchanged; `git diff` shows only the clause, comments, the awk filter, the note and the arms. The neutered-sort RED was observed through the copy and is recorded, this plan never modified the tracked extractor (`git diff --quiet` in verify), and 34/34 pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extractor output → restore transaction on shared TEST | emitted SQL is executed verbatim inside BEGIN … COMMIT |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.2-09 | Tampering | a relaxed wrong-state leg hides a stale row | high | mitigate | predicate, seams, defaults and arms 31/32 byte-identical (criterion 3, D-01); arm 34 proves the leg still fires on the UPDATE's absence |
| T-164.9.2-10 | Tampering | order regression commits a stale row | high | mitigate | arm 33 plus the neutered-sort RED observed through a copy |
| T-164.9.2-12b | Tampering | an allowlist holding only C5 lines passes the empty check and replays no countable row | medium | mitigate | INSERT-only `REFDATA_ENTRY_N`; arm 34 only-C5 leg |
| T-164.9.2-11 | Information disclosure | corrected RAISE text leaks a value into the public log | low | mitigate | no quoted values; arm 31's value-leak grep still runs; arm 34 carries the same check |
| T-164.9.2-12 | Elevation | backtick or `$` expansion in the unquoted TXN heredoc | medium | mitigate | refusal 9 (`refuse_backticks_in_txn_heredocs`) plus arm 26 still green |
</threat_model>

<verification>
`bash scripts/restore-test-from-baseline.sh --self-test` prints 34/34; the restore and wiring vitest files pass. (The `lint-sql-gates` sanity run moved to plan 05's phase gate, to trim this plan.)
</verification>

<success_criteria>
Criteria 2 and 3 hold at the transaction level on a throwaway cluster. The only change to the wrong-state leg is the dated prose correction D-01 allows.
</success_criteria>

<output>
Create `.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-03-SUMMARY.md` when done
</output>
