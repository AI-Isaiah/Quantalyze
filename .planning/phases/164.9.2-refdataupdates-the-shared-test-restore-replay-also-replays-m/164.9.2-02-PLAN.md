---
phase: 164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m
plan: 02
type: execute
wave: 2
depends_on: ["164.9.2-01"]
files_modified:
  - scripts/extract-reference-inserts.mjs
  - src/__tests__/extract-reference-inserts-selftest-floor.test.ts
  - scripts/extract-reference-inserts-fixtures/c5-update-nonliteral.red/
  - scripts/extract-reference-inserts-fixtures/c5-update-nonliteral.green/
  - scripts/extract-reference-inserts-fixtures/c5-update-function-call.red/
  - scripts/extract-reference-inserts-fixtures/c5-update-function-call.green/
  - scripts/extract-reference-inserts-fixtures/c5-update-dollar-tag.red/
  - scripts/extract-reference-inserts-fixtures/c5-update-dollar-tag.green/
  - scripts/extract-reference-inserts-fixtures/c5-update-dollar-body.red/
  - scripts/extract-reference-inserts-fixtures/c5-update-dollar-body.green/
  - scripts/extract-reference-inserts-fixtures/c5-update-auth-target.red/
  - scripts/extract-reference-inserts-fixtures/c5-update-auth-target.green/
  - scripts/extract-reference-inserts-fixtures/c5-cte-prefixed-update.red/
  - scripts/extract-reference-inserts-fixtures/c5-cte-prefixed-update.green/
  - scripts/extract-reference-inserts-fixtures/c5-update-count-drift.red/
  - scripts/extract-reference-inserts-fixtures/c5-update-count-drift.green/
  - scripts/extract-reference-inserts-fixtures/c5-decline-literal.red/
  - scripts/extract-reference-inserts-fixtures/c5-decline-literal.green/
  - scripts/extract-reference-inserts-fixtures/c5-decline-count-drift.red/
  - scripts/extract-reference-inserts-fixtures/c5-decline-count-drift.green/
  - scripts/extract-reference-inserts-fixtures/c5-audit-unlisted-update.red/
  - scripts/extract-reference-inserts-fixtures/c5-audit-unlisted-update.green/
  - scripts/extract-reference-inserts-fixtures/c5-audit-unlisted-nonliteral-update.red/
  - scripts/extract-reference-inserts-fixtures/c5-audit-unlisted-nonliteral-update.green/
  - scripts/extract-reference-inserts-fixtures/c5-audit-unlisted-delete.red/
  - scripts/extract-reference-inserts-fixtures/c5-audit-unlisted-delete.green/
  - scripts/extract-reference-inserts-fixtures/c5-audit-unfilled-table.red/
  - scripts/extract-reference-inserts-fixtures/c5-audit-unfilled-table.green/
autonomous: true
requirements:
  - "[164.8.1-REPLAY-INSERT-ONLY-SCOPE]"

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every refusal C5 added in plan 01 has a red+green self-test kind that exits 1 on its red fixture WITH the named reason and exits 0 on its green fixture (criterion 1: red+green self-test arms)."
    - "The self-test's ORDER leg proves emit order is (basename, offset): its green fixture lists an UPDATE's allowlist line BEFORE an earlier file's INSERT line, and stdout must show INSERT, then UPDATE, then the later file's INSERT."
    - "`SELF_TEST_KINDS_FLOOR` equals the measured `--self-test` count, and `REQUIRED_KINDS` names every new C5 kind, so deleting one reddens both layers."
    - "Neutering `updateLiteralCheck` and neutering the emit sort were each observed to turn the self-test RED, naming the kind, and then restored."
  artifacts:
    - path: scripts/extract-reference-inserts.mjs
      provides: "13 new SELF_TEST_KINDS entries; SELF_TEST_KINDS_FLOOR = 32"
    - path: src/__tests__/extract-reference-inserts-selftest-floor.test.ts
      provides: "REQUIRED_KINDS + 13 C5 ids; dated census comment"
  key_links:
    - "Both workflows grep the self-test OK line `^extract-reference-inserts self-test OK: [0-9]+ kinds, red\\+green each\\.$`; its format must not change, only its number."
  prohibitions:
    - "⛔ No existing kind, fixture or `expect` string is edited to make a new leg pass."
    - "⛔ The floor is set from a MEASURED run, never from arithmetic in this plan."
---

<objective>
Give every C5 refusal from plan 01 its own red+green self-test kind. Ratchet the extractor's kind
floor (layer 1) and the vitest `REQUIRED_KINDS` list (layer 2) to the measured count.

Purpose: criterion 1 requires red+green arms. A refusal with no red leg is a control nothing proves
can fire.

Output: 13 fixture pairs, 13 `SELF_TEST_KINDS` entries, the floor raised from 19 to the measured
count (expected 32), and 13 ids appended to `REQUIRED_KINDS`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-CONTEXT.md
@.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-RESEARCH.md
Read the prior plan summary `.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-01-SUMMARY.md` (written when that plan executes; it records the exact reason strings and census text).
@scripts/extract-reference-inserts.mjs
@src/__tests__/extract-reference-inserts-selftest-floor.test.ts

<interfaces>
- `SELF_TEST_KINDS` entries are `{id, why, audit?, expect, greenStdout?, greenAbsent?}`. `runLeg(kind, colour)` runs the extractor with `--allowlist <fixture>/allowlist.txt --migrations <fixture>/migrations` (plus `--audit` when `audit: true`). A red leg must exit 1 with `expect` in stderr; a green leg must exit 0 and match `greenStdout` if given.
- Fixture layout, copied from an existing pair such as `scripts/extract-reference-inserts-fixtures/audit-unlisted.red/`: `allowlist.txt` (header comment naming the fixture) plus `migrations/<timestamp>_fx_*.sql`. Fixtures use the table `fx_ref` (unqualified in SQL, `public.fx_ref` in the allowlist).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: emit-mode C5 kinds, including the ORDER leg</name>
  <files>scripts/extract-reference-inserts.mjs, scripts/extract-reference-inserts-fixtures/c5-update-nonliteral.{red,green}/, c5-update-function-call.{red,green}/, c5-update-dollar-tag.{red,green}/, c5-update-dollar-body.{red,green}/, c5-update-auth-target.{red,green}/, c5-cte-prefixed-update.{red,green}/, c5-update-count-drift.{red,green}/</files>
  <read_first>scripts/extract-reference-inserts.mjs (SELF_TEST_KINDS, runLeg, selfTest, and every C5 refusal reason plan 01 added: grep the file for `C5`); two existing fixture pairs, e.g. c1-dollar-body and count-mismatch-high</read_first>
  <behavior>
    - c5-update-nonliteral: red `update:1` on an `UPDATE fx_ref SET a = x.b FROM x WHERE …` → the FROM/USING/SELECT reason; green is a literal `UPDATE fx_ref SET a = 'v' WHERE k IN ('…')` with `greenStdout: /UPDATE fx_ref SET/`.
    - c5-update-function-call: red `SET a = current_setting('x')` → the call-ban reason; green `SET a = now()`.
    - c5-update-dollar-tag: red a `$q$…$q$` literal in the SET → the dollar reason; green the same statement with a plain literal.
    - c5-update-dollar-body: red the only UPDATE sits inside a `DO $$ … $$` body with `update:1` → the inside-body reason (C1 for UPDATEs); green the same UPDATE at top level.
    - c5-update-auth-target: red an `update:1` line on `auth.users` → the malformed "C5 targets public only" reason; green the same line on `public.fx_ref`.
    - c5-cte-prefixed-update: red `WITH s AS (SELECT 1) UPDATE fx_ref …` → the CTE-prefixed-UPDATE reason; green the plain UPDATE.
    - c5-update-count-drift: red pins `update:2` over one statement → the "pins 2 … 1 were measured" reason. The green leg is the ORDER leg: two migration files, the earlier one inserting into and updating fx_ref, the later one inserting into fx_ref2. The allowlist lists the UPDATE line FIRST and the earlier file's INSERT line LAST. `greenStdout: /INSERT INTO fx_ref [\s\S]*UPDATE fx_ref [\s\S]*INSERT INTO fx_ref2/`.
  </behavior>
  <action>
  Write each fixture pair, then append one `SELF_TEST_KINDS` entry per kind. Each `why` names the threat it closes (for example, the call ban closes a side-effecting `net.http_*` or `pg_notify` inside a replayed UPDATE). Each `expect` is a substring of the exact reason plan 01 prints: copy it from the source, never paraphrase. RED first: add the entries before the fixtures exist or while a red fixture is still green, observe `SELF-TEST FAIL` naming the kind, then complete it. If plan 01 implemented a C5 refusal this list does not cover, add a kind for it too, and record it in SUMMARY. The invariant is that every C5 refusal reason has a red leg; map reason to kind in a SUMMARY table.
  </action>
  <verify>
    <automated>node scripts/extract-reference-inserts.mjs --self-test</automated>
  </verify>
  <done>The self-test prints `OK: 26 kinds` (19 + 7), before the floor moves in Task 2. The ORDER leg's green stdout matches its regex.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: audit-mode C5 kinds, both ratchet layers, and two neutered-guard observations</name>
  <files>scripts/extract-reference-inserts.mjs, src/__tests__/extract-reference-inserts-selftest-floor.test.ts, scripts/extract-reference-inserts-fixtures/c5-decline-literal.{red,green}/, c5-decline-count-drift.{red,green}/, c5-audit-unlisted-update.{red,green}/, c5-audit-unlisted-nonliteral-update.{red,green}/, c5-audit-unlisted-delete.{red,green}/, c5-audit-unfilled-table.{red,green}/</files>
  <read_first>src/__tests__/extract-reference-inserts-selftest-floor.test.ts (REQUIRED_KINDS, the dated `OK: 19 kinds` comment, the RATCHET STALE assertion)</read_first>
  <behavior>
    - All six kinds carry `audit: true`, and each green fixture is fully accounted, so `--audit` exits 0 and prints the C5 census line (`greenStdout: /audit C5 OK: /`).
    - c5-decline-literal: red `decline:1` on a literal UPDATE → "a literal UPDATE is replayed, not declined"; green `decline:1` on a FROM-join UPDATE.
    - c5-decline-count-drift: red `decline:2` over one non-literal UPDATE → the count reason; green `decline:1`.
    - c5-audit-unlisted-update: red a literal top-level UPDATE on an INSERT-filled table with no C5 line → the "add an update: line" reason; green with the line.
    - c5-audit-unlisted-nonliteral-update: red a FROM-join UPDATE with no line → the "decline it with a reason" reason; green with a `decline:1` line.
    - c5-audit-unlisted-delete: red a top-level `DELETE FROM fx_ref` on the INSERT-filled table → "C5 replays UPDATE only"; green a DELETE on a table no INSERT entry fills (ignored).
    - c5-audit-unfilled-table: red an `update:1` line on a public table no INSERT entry fills → the membership reason; green the line on the filled table.
  </behavior>
  <action>
  Write the six fixture pairs and kinds as in Task 1, RED first. Then run `--self-test`, read the printed count (expected 32), and set `SELF_TEST_KINDS_FLOOR` to exactly that number with a dated MEASURED comment. Append all 13 C5 ids to `REQUIRED_KINDS`, and update its dated comment to the measured count and date (2026-09-25). Keep the 2026-09-09 sentence as lineage. The RATCHET STALE vitest must go red with the old floor still in place: observe that once before raising the floor, and record it.
  Neutered-guard observations, each on a byte backup of scripts/extract-reference-inserts.mjs taken right before the edit (`cp` to the scratchpad; never `git checkout --`, restore or stash):
  (1) make `updateLiteralCheck` return null → `--self-test` must fail naming `c5-update-nonliteral` (and `c5-update-function-call`); restore from the backup and confirm with `cmp`;
  (2) remove the emit sort (push in entry order) → `--self-test` must fail naming `c5-update-count-drift` (ORDER leg green stdout); restore and `cmp`.
  Record both failing outputs verbatim in SUMMARY. Re-take the backup after each restore.
  </action>
  <verify>
    <automated>node scripts/extract-reference-inserts.mjs --self-test && node scripts/extract-reference-inserts.mjs --audit && npx vitest run src/__tests__/extract-reference-inserts-selftest-floor.test.ts src/__tests__/refdata-allowlist.test.ts</automated>
  </verify>
  <done>The self-test prints `OK: 32 kinds` (or the measured count, with the reason in SUMMARY). The floor and `REQUIRED_KINDS` agree with it. Both neutered-guard REDs are recorded verbatim, and the file is `cmp`-identical to its pre-neuter backup after each restore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self-test corpus → CI verdict | the self-test is the only proof each C5 refusal fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.2-07 | Tampering | a C5 refusal silently deleted | high | mitigate | two-layer ratchet (SELF_TEST_KINDS_FLOOR in the script, REQUIRED_KINDS by name in vitest) |
| T-164.9.2-08 | Repudiation | a leg that fires for the wrong reason | medium | mitigate | `expect` copied from the exact reason text; neutered-guard observations prove the specific legs bite |
</threat_model>

<verification>
`node scripts/extract-reference-inserts.mjs --self-test` prints the measured count; `--audit` is still green; both vitest files pass.
</verification>

<success_criteria>
Criterion 1's "red+green self-test arms" hold for every C5 refusal, and both ratchet layers pin the new count.
</success_criteria>

<output>
Create `.planning/phases/164.9.2-refdataupdates-the-shared-test-restore-replay-also-replays-m/164.9.2-02-SUMMARY.md` when done
</output>
