---
phase: 166.1-qstatsrecompute
plan: 05
type: execute
wave: 2
depends_on: ["166.1-04"]
files_modified:
  - src/lib/correlation-math.ts
  - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx
  - src/lib/portfolio-stats.ts
  - src/app/(dashboard)/allocations/lib/scenario-benchmark.ts
  - src/app/(dashboard)/allocations/lib/scenario-stress.ts
  - src/lib/dispersion-floor-sites-risk.test.ts
  - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "correlation-math.pearson returns null when either leg's dispersion is residue, exactly as it does today for an exactly constant leg, so rollingCorrelation shows a gap for a constant-yield window instead of a residue number (D-15, D-07)"
    - "The allocator CorrelationMatrix widget renders the same cell for a constant-yield leg as for an all-zero leg (D-15, D-07)"
    - "portfolio-stats: computeRollingMetric's Sharpe, computeAlphaBeta's beta and computeRiskDecomposition's percentages give, on a constant-yield input, exactly what an all-zero input gives today (0) (D-15, D-07)"
    - "computeScenarioBenchmark and computeScenarioStress detect a constant-yield benchmark or series with the shared floor 1e-12 * max(1, |mean|) instead of the relative-only 1e-12 * (|mean| + 1e-12), which misses daily 1e-5, daily 1e-4 and 1% APY (measured 2026-09-25) (D-15)"
    - "A cent-rounded 1% APY NAV still gives a finite correlation, Sharpe and beta at the sites with that control, so widening the floor goes red (D-07)"
    - "Every T6-T12 test was written first and observed RED on the unedited site (or recorded as a pin with the reason, for any yield id the old relative floor already caught), and every site was neutered, observed RED and restored from a byte backup proven with cmp"
  artifacts:
    - path: src/lib/dispersion-floor-sites-risk.test.ts
      provides: "red tests for T6 and T8-T12"
    - path: src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx
      provides: "the T7 render test"
  key_links:
    - from: src/lib/correlation-math.ts
      to: src/lib/dispersion-floor.ts
      via: "pearson gates on dispersionIsReal for both legs"
      pattern: "dispersion-floor"
    - from: src/app/(dashboard)/allocations/lib/scenario-benchmark.ts
      to: src/lib/dispersion-floor.ts
      via: "the three degeneracy tests call dispersionIsResidue"
      pattern: "dispersionIsResidue"
---

<objective>
Second TS slice of D-15: the correlation helpers, `portfolio-stats.ts`, and the two scenario libraries
whose relative-only floor misses the compounding-NAV residue (RESEARCH A5 rows T6-T12), all moved onto
plan 04's `src/lib/dispersion-floor.ts`.

Purpose: these compute the allocator's correlation matrix and rolling correlation, the rolling Sharpe,
alpha/beta and risk shares, and the scenario benchmark and stress numbers from daily returns. Each
divides by a dispersion that is about 1.3e-16 on a constant yield.
Output: five edited files and one test file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md
@.planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md
Also read `.planning/phases/166.1-qstatsrecompute/166.1-04-SUMMARY.md` (the floor module's final API and
the fixture's exports).

**Environment, drill rule and the D-07 invariant in TS** are exactly plan 04's: MAIN checkout's
`node_modules` linked only for the run and removed before every commit; exit 127 is a FAIL; quote every
`(dashboard)` path; `tsc` in the background with a 600 s watchdog; byte backups under a `166.1-05`
scratchpad name, neuter, RED, `cp`, `cmp`; each test computes the site's output on the constant yield
AND on an all-zero series and asserts the ratio field identical. Never `git checkout --`, `git restore`,
`git stash`.

**Symbols this plan edits (cite by symbol; line numbers drift).**
<!-- verify-symbol: src/lib/correlation-math.ts pearson -->
<!-- verify-symbol: src/lib/correlation-math.ts rollingCorrelation -->
<!-- verify-symbol: src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx pearson -->
<!-- verify-symbol: src/lib/portfolio-stats.ts computeRollingMetric -->
<!-- verify-symbol: src/lib/portfolio-stats.ts computeAlphaBeta -->
<!-- verify-symbol: src/lib/portfolio-stats.ts computeRiskDecomposition -->
<!-- verify-symbol: src/app/(dashboard)/allocations/lib/scenario-benchmark.ts computeScenarioBenchmark -->
<!-- verify-symbol: src/app/(dashboard)/allocations/lib/scenario-benchmark.ts teIsDegenerate -->
<!-- verify-symbol: src/app/(dashboard)/allocations/lib/scenario-benchmark.ts pIsDegenerate -->
<!-- verify-symbol: src/app/(dashboard)/allocations/lib/scenario-stress.ts computeScenarioStress -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): correlation-math.pearson and its rollingCorrelation consumer end-to-end on the shared floor (T6), red first</name>
  <files>src/lib/correlation-math.ts, src/lib/dispersion-floor-sites-risk.test.ts</files>
  <read_first>
    - src/lib/correlation-math.ts - `pearson` and `rollingCorrelation` (the doc comment's "flat window - correlation undefined" gap contract)
    - src/lib/correlation-math.test.ts - existing cases (must stay green)
    - src/lib/dispersion-floor.ts and src/__tests__/fixtures/dispersion-nav.ts - plan 04's API
  </read_first>
  <behavior>
    - `pearson(navConstantYield(y), noise)` is null for every yield id, identical to `pearson(zeros, noise)`; two correlated noisy legs give a finite value; a cent-rounded 1% APY leg against noise gives a finite value
    - `rollingCorrelation` over a series whose every window is a constant yield returns the same points (gaps) as over an all-zero series
  </behavior>
  <action>
    Per D-15 and D-07. Step 0: `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-05-PLAN.md`, halt on non-zero. Write the T6 tests first and observe RED on the unedited `pearson`. Then gate `pearson` on `dispersionIsReal` for both legs, using each leg's sample sd (`Math.sqrt(varA / (n - 1))`) and its mean `ma` / `mb`; otherwise null as today. Keep `n < 2` handling. Drill T6.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/dispersion-floor-sites-risk src/lib/correlation-math</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dispersion-floor' src/lib/correlation-math.ts` prints at least 1 and `grep -c 'denom > 0 ? cov / denom : null' src/lib/correlation-math.ts` prints 0
    - the SUMMARY records T6 RED ids and the T6 drill row
  </acceptance_criteria>
  <done>pearson and rollingCorrelation report no correlation for a residue leg, proven by a test observed red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: T7-T10 - the CorrelationMatrix widget and portfolio-stats' rolling Sharpe, beta and risk decomposition</name>
  <files>src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx, src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx, src/lib/portfolio-stats.ts, src/lib/dispersion-floor-sites-risk.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx - the local `pearson` and how cells are rendered; src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.boundary.test.tsx for the render style
    - src/lib/portfolio-stats.ts - `computeRollingMetric` (Sharpe arm), `computeAlphaBeta` (beta), `computeRiskDecomposition`; src/lib/portfolio-stats.test.ts
    - src/app/(dashboard)/allocations/lib/scenario-benchmark.ts - its own comment explains why it guards a constant benchmark BEFORE calling `computeAlphaBeta`; keep that caller's behaviour unchanged
  </read_first>
  <behavior>
    - T7: rendering the widget with one constant-yield leg produces the same cell text and colour for that leg's pairs as rendering it with an all-zero leg; a noisy pair is unchanged
    - T8: `computeRollingMetric(..., "sharpe", ...)` on a constant-yield series gives the same points as on an all-zero series (0)
    - T9: `computeAlphaBeta` with a constant-yield benchmark gives `beta` 0 and the same `alpha` formula result as an all-zero benchmark of the same length would (both computed in the test)
    - T10: `computeRiskDecomposition` over a covariance matrix built from two constant-yield strategies gives every `percentage` 0, identical to an all-zero matrix; a noisy matrix keeps percentages summing to 100
  </behavior>
  <action>
    Per D-15 and D-07. Write the tests first, observe RED, record the ids. T7: gate the widget's local `pearson` on `dispersionIsReal` for both legs (sample sd from the sums and the leg mean), else 0 as today; do not add an export; test through render in `CorrelationMatrix.residue.test.tsx`, in the render style of the existing boundary test. T8: publish the Sharpe only when NOT `dispersionIsResidue(s, m)`, else 0. T9: beta is 0 when `dispersionIsResidue(Math.sqrt(varB / (n - 1)), meanB)`, else today's quotient. T10: percentages are 0 when `dispersionIsResidue(Math.sqrt(Math.max(totalVariance, 0)), 0)` (no mean exists; absolute floor, the TS twin of Python S7). Drill T7-T10.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/dispersion-floor-sites-risk src/lib/portfolio-stats "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dispersion-floor' "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx" src/lib/portfolio-stats.ts` prints at least 1 for each file
    - `grep -c 'varB > 0 ? cov / varB' src/lib/portfolio-stats.ts` prints 0 and `grep -c 'totalVariance > 0 ?' src/lib/portfolio-stats.ts` prints 0
    - the SUMMARY carries the T7-T10 RED ids and drill rows
  </acceptance_criteria>
  <done>The widget and portfolio-stats emit, on a constant yield, exactly what they emit on an all-zero input, each proven by a test observed red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: T11-T12 - the two scenario libraries' relative-only floors move to the shared floor, then the plan's vitest, tsc and eslint gates</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-benchmark.ts, src/app/(dashboard)/allocations/lib/scenario-stress.ts, src/lib/dispersion-floor-sites-risk.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-benchmark.ts - `computeScenarioBenchmark`: `teIsDegenerate`, `benchmarkIsDegenerate`, `pIsDegenerate` and their comment blocks
    - src/app/(dashboard)/allocations/lib/scenario-stress.ts - `computeScenarioStress` and its degeneracy test
    - src/app/(dashboard)/allocations/lib/scenario-benchmark.test.ts and scenario-stress.test.ts - existing constant-benchmark cases (must stay green)
    - .planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md - A5 (which yield ids the old floor misses)
  </read_first>
  <behavior>
    - T11: with a constant-yield benchmark, beta, alpha, correlation and the information ratio equal what an all-zero benchmark gives today (null), for EVERY yield id; with a constant-yield portfolio, correlation and IR are null likewise
    - T12: a constant-yield series takes the same degenerate branch as an all-zero series
    - Honesty: yield ids the old relative floor ALREADY caught stay green on the old code (measured 2026-09-25 for six of the eleven ids: daily 1e-3, 10% APY and 100% APY caught; daily 1e-5, daily 1e-4 and 1% APY missed). Measure and record the full RED set and the full already-caught set
  </behavior>
  <action>
    Per D-15. Write the tests first and observe RED on the unedited files; record the RED ids and the already-caught ids (these are pins for those ids, like Python S6). Replace each hand-written `<= 1e-12 * (Math.abs(mean) + 1e-12)` test with `dispersionIsResidue(std, mean)` on the same std and mean; keep each degenerate branch's output and every comment's reasoning, updated to say the floor is the shared one and why (`max(1, |mean|)` catches the absolute ~1e-16 residue a relative-only floor misses). Drill T11 and T12 by restoring the old expression. Then run the gates below. Record per site that the fixed output is one the site already emits for an exact constant, so no renderer changes.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/dispersion-floor src/lib/correlation-math src/lib/portfolio-stats "src/app/(dashboard)/allocations"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or "No test files found"</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint src/lib/correlation-math.ts "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx" src/lib/portfolio-stats.ts "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts" "src/app/(dashboard)/allocations/lib/scenario-stress.ts" src/lib/dispersion-floor-sites-risk.test.ts "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx"</automated>
    <fails_when>non-zero exit (127 = FAIL), any tsc error line, or any eslint error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '1e-12 \* (Math.abs' "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts" "src/app/(dashboard)/allocations/lib/scenario-stress.ts"` prints 0 for each file
    - `grep -c 'dispersionIsResidue' "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts" "src/app/(dashboard)/allocations/lib/scenario-stress.ts"` prints at least 1 for each file
    - the SUMMARY carries the T6-T12 drill table, the RED and already-caught id sets for T11/T12, and the per-site "already emitted" line
    - `git diff --stat -- analytics-service/ supabase/` is empty for this plan's commits, and `test -L node_modules` is false at every commit
  </acceptance_criteria>
  <done>T6-T12 read the shared TS floor, each with a test observed red (already-caught ids recorded as pins), and vitest, tsc and eslint are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daily returns -> TS-computed ratio -> allocator widgets and scenario panels | a fabricated correlation, beta or risk share renders to an allocator |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.1-23 | Tampering (integrity of a displayed number) | T6-T12 | medium | mitigate | shared floor; red test per site; D-07 equality with the all-zero input; drill table |
| T-166.1-24 | Denial of service (over-wide floor hides real data) | the shared floor at these sites | medium | mitigate | cent-rounded 1% APY controls go red if the floor widens |
</threat_model>

<verification>
- `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-05-PLAN.md` exits 0 before Task 1 starts.
- Both Task 3 verify commands exit 0.
</verification>

<success_criteria>
- D-15, second slice: T6-T12 use the shared TS floor, each with a test observed red or a recorded pin.
</success_criteria>

<output>
Create `.planning/phases/166.1-qstatsrecompute/166.1-05-SUMMARY.md` when done. Remove the node_modules
link, stage the SUMMARY, then run
`QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && test -x "$QZ_MAIN/node_modules/.bin/tsx" && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK before committing.
</output>
