---
phase: 166.1-qstatsrecompute
plan: 05
type: execute
wave: 2
depends_on: ["166.1-04"]
files_modified:
  - src/lib/correlation-math.ts
  - src/lib/correlation-math.test.ts
  - src/lib/return-stats.pearson-contract.test.ts
  - src/components/strategy/CompareCorrelationMatrix.tsx
  - src/components/strategy/CompareCorrelationMatrix.residue.test.tsx
  - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx
  - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx
  - src/lib/portfolio-stats.ts
  - src/lib/portfolio-stats.test.ts
  - src/app/(dashboard)/allocations/lib/scenario-benchmark.ts
  - src/app/(dashboard)/allocations/lib/scenario-stress.ts
  - src/lib/return-stats.sites-risk.test.ts
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "There is ONE TS Pearson correlation: correlation-math.ts is deleted, /compare's correlation matrix and the allocator CorrelationMatrix widget call return-stats' pearson, and the widget's local pearson is gone (D-17)"
    - "The dead rollingCorrelation and computeRollingMetric (0 production callers, measured 2026-09-25) are deleted with their tests, and portfolio-stats.ts itself stays on disk (scenario-backbone-gates SC-3 keep-list) (D-17)"
    - "On a compounding-NAV constant yield: /compare's pair cell is null (the flat-window gap, as for an all-zero leg), the CorrelationMatrix widget renders the same cell as for an all-zero leg, computeAlphaBeta's beta is 0 and computeRiskDecomposition's percentages are 0 exactly as for an all-zero input (D-07, D-17)"
    - "computeScenarioBenchmark and computeScenarioStress detect a constant-yield benchmark or series with the shared floor instead of the relative-only 1e-12 * (|mean| + 1e-12), which misses daily 1e-5, daily 1e-4 and 1% APY (measured 2026-09-25), and the benchmark's correlation is return-stats' pearson (D-15, D-17)"
    - "G11.E.5's null contract for an undefined correlation is kept: its test cases moved with pearson, unchanged in what they assert (D-17)"
    - "A cent-rounded 1% APY NAV still gives a finite correlation and beta at the sites with that control, so widening the floor goes red (D-07)"
    - "Every site test was written first and observed RED on the unedited site (or recorded as a pin, for a yield id the old relative floor already caught), and every site was neutered, observed RED and restored from a byte backup proven with cmp"
  artifacts:
    - path: src/lib/return-stats.pearson-contract.test.ts
      provides: "the G11.E.5 pearson contract cases, moved from correlation-math.test.ts and pointed at return-stats"
    - path: src/lib/return-stats.sites-risk.test.ts
      provides: "red tests for T9-T12"
    - path: src/components/strategy/CompareCorrelationMatrix.residue.test.tsx
      provides: "the T6 render test"
    - path: src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx
      provides: "the T7 render test"
  key_links:
    - from: src/components/strategy/CompareCorrelationMatrix.tsx
      to: src/lib/return-stats.ts
      via: "import { pearson } from @/lib/return-stats"
      pattern: "@/lib/return-stats"
    - from: src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx
      to: src/lib/return-stats.ts
      via: "the widget's cells call the shared pearson"
      pattern: "@/lib/return-stats"
    - from: src/lib/portfolio-stats.ts
      to: src/lib/return-stats.ts
      via: "computeAlphaBeta's beta is the shared beta"
      pattern: "@/lib/return-stats"
    - from: src/app/(dashboard)/allocations/lib/scenario-benchmark.ts
      to: src/lib/return-stats.ts
      via: "the degeneracy tests and the correlation call the shared module"
      pattern: "@/lib/return-stats"
---

<objective>
Second slice of D-17 (compute once): the allocator and /compare correlation, beta, risk-share and scenario
benchmark/stress sites (CONTEXT D-17 rows T6-T12) stop carrying their own formulas and call plan 04's
`src/lib/return-stats.ts`; the two dead formula copies are deleted.

Purpose: `correlation-math.ts` `pearson`, the CorrelationMatrix widget's private `pearson`, and
`scenario-benchmark.ts`'s own sample correlation are three copies of one formula, each dividing by a
dispersion that is about 1.3e-16 on a constant yield; `computeAlphaBeta`, `computeRiskDecomposition` and
the two scenario libraries each carry their own degeneracy test, two of them a relative-only floor that
misses the compounding-NAV residue. After this plan each figure is computed in one place.
Output: one module deleted (its contract tests moved), two dead functions deleted, five sites on the shared
module, three new test files and one moved.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md
@.planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md
Also read `.planning/phases/166.1-qstatsrecompute/166.1-04-SUMMARY.md` (the final `return-stats.ts` API at
its top, and the fixture's exports). This plan does NOT edit `src/lib/return-stats.ts`,
`src/lib/return-stats.test.ts` or `src/lib/return-stats.sites.test.ts` (plan 04's files; plan 06 runs in
the same wave). An API gap in `return-stats.ts` is a STOP-and-report, not a local helper.

**Environment, drill rule, the D-07 invariant in TS and the never-edit-a-golden rule** are exactly plan
04's: MAIN checkout's `node_modules` linked only for the run and removed before every commit; exit 127 is
a FAIL; quote every `(dashboard)` path; `tsc` in the background with a 600 s watchdog; byte backups under
a `166.1-05` scratchpad name, neuter, RED, `cp`, `cmp`; each test computes the site's output on the
constant yield AND on an all-zero series and asserts the ratio field identical. Never `git checkout --`,
`git restore`, `git stash`. A deletion is a `git rm` of a tracked file, recorded in the SUMMARY.

**Ownership (CONTEXT D-17).** Phase 169 plan 08 feeds the Risk tab's widgets the per-key book and drops the
precomputed legacy correlation matrix; it asserts it edits nothing under `widgets/risk/`. Phase 169 plan
11 moves `AlphaBetaDecomposition.tsx` from `computeAlphaBeta` onto `jointMetrics`. This plan edits neither
`RiskTabPanel.tsx`, `book-risk-input.ts` nor `AlphaBetaDecomposition.tsx`.

**Dead code, measured 2026-09-25.** `rollingCorrelation` has no importer outside
`src/lib/correlation-math.test.ts` (its only consumer, `CorrelationWithBenchmark`, reads the persisted
`btc_rolling_correlation_90d` since audit G11.A P64); `computeRollingMetric` has no importer outside
`src/lib/portfolio-stats.test.ts`. Re-measure both before deleting; if either has gained a production
importer, STOP and report.

**Symbols this plan edits (cite by symbol; line numbers drift).**
<!-- verify-symbol: src/lib/correlation-math.ts pearson -->
<!-- verify-symbol: src/lib/correlation-math.ts rollingCorrelation -->
<!-- verify-symbol: src/components/strategy/CompareCorrelationMatrix.tsx pearson -->
<!-- verify-symbol: src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx pearson -->
<!-- verify-symbol: src/lib/portfolio-stats.ts computeRollingMetric -->
<!-- verify-symbol: src/lib/portfolio-stats.ts computeAlphaBeta -->
<!-- verify-symbol: src/lib/portfolio-stats.ts computeRiskDecomposition -->
<!-- verify-symbol: src/app/(dashboard)/allocations/lib/scenario-benchmark.ts computeScenarioBenchmark -->
<!-- verify-symbol: src/app/(dashboard)/allocations/lib/scenario-benchmark.ts teIsDegenerate -->
<!-- verify-symbol: src/app/(dashboard)/allocations/lib/scenario-benchmark.ts benchmarkIsDegenerate -->
<!-- verify-symbol: src/app/(dashboard)/allocations/lib/scenario-benchmark.ts pIsDegenerate -->
<!-- verify-symbol: src/app/(dashboard)/allocations/lib/scenario-stress.ts computeScenarioStress -->
<!-- verify-symbol: src/app/(dashboard)/allocations/lib/scenario-stress.ts seriesIsDegenerate -->
<!-- verify-symbol: src/lib/scenario-backbone-gates.test.ts portfolio-stats.ts -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): one Pearson - correlation-math.ts deleted, /compare's matrix on return-stats' pearson end-to-end (T6), the dead rollingCorrelation gone, red first</name>
  <files>src/lib/correlation-math.ts, src/lib/correlation-math.test.ts, src/lib/return-stats.pearson-contract.test.ts, src/components/strategy/CompareCorrelationMatrix.tsx, src/components/strategy/CompareCorrelationMatrix.residue.test.tsx</files>
  <read_first>
    - src/lib/correlation-math.ts - `pearson` (the G11.E.5 doc comment) and `rollingCorrelation`
    - src/lib/correlation-math.test.ts - the `pearson` and `rollingCorrelation` describe blocks
    - src/components/strategy/CompareCorrelationMatrix.tsx - how it calls `pearson` and renders a null cell (the em-dash); src/app/(dashboard)/compare/page.test.tsx for how a `CompareItem` is built
    - src/lib/return-stats.ts and src/__tests__/fixtures/dispersion-nav.ts - plan 04's API
    - src/components/charts/CorrelationWithBenchmark.tsx - the G11.A P64 doc block (why `rollingCorrelation` is dead; read only)
    - src/lib/diversification.ts - its doc comment names `correlation-math.ts::pearson` (update that one sentence only if it is a live claim; it is plan 04's file, so if it needs a change, record it in the SUMMARY instead of editing it)
  </read_first>
  <behavior>
    - T6: `CompareCorrelationMatrix` rendered with one constant-yield pick and one noisy pick shows the same cell for that pair as with an all-zero pick (the flat-window null); two noisy picks keep a finite cell
    - The G11.E.5 contract cases from `correlation-math.test.ts`'s `pearson` block pass UNCHANGED in what they assert when pointed at `@/lib/return-stats`
    - No live code imports `correlation-math` after the change
  </behavior>
  <action>
    Per D-17 and D-07. Step 0: `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-05-PLAN.md`, halt on non-zero. Re-measure the dead-code premise (`grep -rn "rollingCorrelation" src` excluding `correlation-math*`); STOP if a production importer exists. Write the T6 render test first in `src/components/strategy/CompareCorrelationMatrix.residue.test.tsx` (jsdom, the repo default) and observe RED against today's `correlation-math` `pearson` behind `CompareCorrelationMatrix`. Then: `git mv src/lib/correlation-math.test.ts src/lib/return-stats.pearson-contract.test.ts`, change its import to `@/lib/return-stats`, and delete its `rollingCorrelation` describe block (the function is dead); leave every `pearson` case's assertion as it is. `git rm src/lib/correlation-math.ts`. Point `CompareCorrelationMatrix.tsx` at `import { pearson } from "@/lib/return-stats"`. Drill T6: byte backup of `CompareCorrelationMatrix.tsx`, replace the import with a local copy of the OLD `denom > 0` pearson, RED, `cp` restore, `cmp`.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/return-stats src/components/strategy "src/app/(dashboard)/compare" && test ! -e src/lib/correlation-math.ts && test -z "$(grep -rln --include='*.ts' --include='*.tsx' 'correlation-math"' src)"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, "No test files found", `correlation-math.ts` still on disk, or any file still importing it</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-status "$(git merge-base HEAD origin/main)" -- src/lib/correlation-math.ts` shows `D`, and the moved test file exists with no `rollingCorrelation` token
    - `grep -c '@/lib/return-stats' src/components/strategy/CompareCorrelationMatrix.tsx` prints 1
    - the SUMMARY records the T6 RED ids, the dead-code re-measurement, and the T6 drill row
  </acceptance_criteria>
  <done>The TS side has one Pearson correlation; /compare's matrix reads it and shows no residue correlation, proven by a test observed red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: T7-T10 - the CorrelationMatrix widget on the shared pearson, the dead computeRollingMetric deleted, computeAlphaBeta and computeRiskDecomposition on the shared module</name>
  <files>src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx, src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx, src/lib/portfolio-stats.ts, src/lib/portfolio-stats.test.ts, src/lib/return-stats.sites-risk.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx - the local `pearson`, the `precomputed` arm, and how cells render; CorrelationMatrix.boundary.test.tsx and CorrelationMatrix.all-columns.test.tsx for the render style (both must pass unedited)
    - src/lib/portfolio-stats.ts - `computeRollingMetric`, `computeAlphaBeta`, `computeRiskDecomposition`; src/lib/portfolio-stats.test.ts
    - src/lib/scenario-backbone-gates.test.ts - SC-3 keep-list (`portfolio-stats.ts` must stay on disk)
    - src/app/(dashboard)/allocations/lib/scenario-benchmark.ts - its comment explaining why it guards a constant benchmark BEFORE calling `computeAlphaBeta`
  </read_first>
  <behavior>
    - T7: rendering the widget with one constant-yield leg produces the same cell text and colour for that leg's pairs as rendering it with an all-zero leg; a noisy pair is unchanged
    - T9: `computeAlphaBeta` with a constant-yield benchmark gives `beta` 0 and the same `alpha` as an all-zero benchmark of the same length (both computed in the test); a noisy benchmark keeps today's beta
    - T10: `computeRiskDecomposition` over a covariance matrix built from two constant-yield strategies gives every `percentage` 0, identical to an all-zero matrix; a noisy matrix keeps percentages summing to 100
    - Dead: `computeRollingMetric` is absent from `portfolio-stats.ts` and from its test file, and `portfolio-stats.ts` is still on disk
  </behavior>
  <action>
    Per D-17 and D-07. Write the T7, T9 and T10 tests first, observe RED, record the ids. T7: delete the widget's local `pearson` and call `pearson(av, bv) ?? 0` from `@/lib/return-stats` (0 is what the widget emits today for an exactly constant leg); keep the `precomputed` arm untouched (169 plan 08 decides what feeds it); test through render in `CorrelationMatrix.residue.test.tsx`, in the style of the boundary test. T8: re-measure that `computeRollingMetric` has no production importer, then delete the function and its describe block in `portfolio-stats.test.ts` (nothing else in that test file changes). T9: `computeAlphaBeta`'s beta becomes `beta(r, b) ?? 0` from `@/lib/return-stats`; alpha keeps its formula over that beta. T10: the percentages are 0 when `dispersionIsResidue(Math.sqrt(Math.max(totalVariance, 0)), 0)` (no mean exists there; the absolute floor, the TS twin of Python S7). Drill T7, T9 and T10 (restore each old expression, RED, `cp` restore, `cmp`).
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/return-stats src/lib/portfolio-stats src/lib/scenario-backbone-gates "src/app/(dashboard)/allocations/widgets"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '@/lib/return-stats' "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx" src/lib/portfolio-stats.ts` prints at least 1 for each file, and `grep -c '^function pearson' "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx"` prints 0
    - `grep -c 'computeRollingMetric' src/lib/portfolio-stats.ts src/lib/portfolio-stats.test.ts` prints 0 for each file, and `test -f src/lib/portfolio-stats.ts` succeeds
    - `grep -c 'varB > 0 ? cov / varB' src/lib/portfolio-stats.ts` prints 0 and `grep -c 'totalVariance > 0 ?' src/lib/portfolio-stats.ts` prints 0
    - `git diff --stat "$(git merge-base HEAD origin/main)" -- "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.boundary.test.tsx" "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.all-columns.test.tsx"` is empty
    - the SUMMARY carries the T7, T9, T10 RED ids and drill rows, and the T8 deletion with its re-measurement
  </acceptance_criteria>
  <done>The widget and portfolio-stats compute correlation and beta through the one module and emit, on a constant yield, what they emit on an all-zero input; the dead rolling metric is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: T11-T12 - the two scenario libraries' relative-only floors and private correlation move to the shared module, then the plan's vitest, tsc and eslint gates</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-benchmark.ts, src/app/(dashboard)/allocations/lib/scenario-stress.ts, src/lib/return-stats.sites-risk.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-benchmark.ts - `computeScenarioBenchmark`: `teIsDegenerate`, `benchmarkIsDegenerate`, `pIsDegenerate`, the sample correlation (`sampCov / (stdP * stdB)`) and their comment blocks
    - src/app/(dashboard)/allocations/lib/scenario-stress.ts - `computeScenarioStress` and `seriesIsDegenerate`
    - src/app/(dashboard)/allocations/lib/scenario-benchmark.test.ts and scenario-stress.test.ts - existing constant-benchmark cases (must stay green unedited)
    - .planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md - A5 (which yield ids the old floor misses)
  </read_first>
  <behavior>
    - T11: with a constant-yield benchmark, beta, alpha, correlation and the information ratio equal what an all-zero benchmark gives today (null), for EVERY yield id; with a constant-yield portfolio, correlation and IR are null likewise; with noisy inputs the correlation equals today's to 3 decimals (the display precision) and is exactly the shared `pearson`
    - T12: a constant-yield series takes the same degenerate branch as an all-zero series
    - Honesty: yield ids the old relative floor ALREADY caught stay green on the old code (measured 2026-09-25 for six of the eleven ids: daily 1e-3, 10% APY and 100% APY caught; daily 1e-5, daily 1e-4 and 1% APY missed). Measure and record the full RED set and the full already-caught set
  </behavior>
  <action>
    Per D-15 and D-17. Write the tests first and observe RED on the unedited files; record the RED ids and the already-caught ids (these are pins for those ids, like Python S6). Replace each hand-written `<= 1e-12 * (Math.abs(mean) + 1e-12)` test with `dispersionIsResidue(std, mean)` on the same std and mean. Replace the benchmark file's private sample correlation with `pearson(p, b)` from `@/lib/return-stats` (null keeps today's null), removing the now-unused sample-sd lines. Keep each degenerate branch's output and each comment's reasoning, updated to say the floor and the correlation are the shared ones and why (`max(1, |mean|)` catches the absolute ~1e-16 residue a relative-only floor misses). Drill T11 and T12 by restoring the old expressions. Then run the gates below. Record per site T6-T12 that the fixed output is one the site already emits for an exact constant, so no renderer changes.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/return-stats src/lib/portfolio-stats src/lib/scenario-backbone-gates src/components/strategy "src/app/(dashboard)/allocations"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or "No test files found"</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint src/lib/return-stats.pearson-contract.test.ts src/components/strategy/CompareCorrelationMatrix.tsx src/components/strategy/CompareCorrelationMatrix.residue.test.tsx "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx" "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx" src/lib/portfolio-stats.ts src/lib/portfolio-stats.test.ts "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts" "src/app/(dashboard)/allocations/lib/scenario-stress.ts" src/lib/return-stats.sites-risk.test.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), any tsc error line, or any eslint error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '1e-12 \* (Math.abs' "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts" "src/app/(dashboard)/allocations/lib/scenario-stress.ts"` prints 0 for each file
    - `grep -c '@/lib/return-stats' "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts" "src/app/(dashboard)/allocations/lib/scenario-stress.ts"` prints at least 1 for each file, and `grep -c 'sampCov / (stdP \* stdB)' "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts"` prints 0
    - `git diff --stat "$(git merge-base HEAD origin/main)" -- "src/app/(dashboard)/allocations/lib/scenario-benchmark.test.ts" "src/app/(dashboard)/allocations/lib/scenario-stress.test.ts"` is empty
    - the SUMMARY carries the T6-T12 drill table, the RED and already-caught id sets for T11/T12, and the per-site "already emitted" line
    - `git diff --stat -- analytics-service/ supabase/ src/lib/return-stats.ts` is empty for this plan's commits, and `test -L node_modules` is false at every commit
  </acceptance_criteria>
  <done>T6-T12 compute through the one return-stats module (dead copies deleted), each with a test observed red or a recorded pin, and vitest, tsc and eslint are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daily returns -> TS-computed ratio -> allocator widgets, /compare and scenario panels | a fabricated correlation, beta or risk share renders to an allocator |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.1-23 | Tampering (integrity of a displayed number) | T6-T12 | medium | mitigate | one shared module; red test per site; D-07 equality with the all-zero input; drill table |
| T-166.1-24 | Denial of service (over-wide floor hides real data) | the shared floor at these sites | medium | mitigate | cent-rounded 1% APY controls go red if the floor widens |
| T-166.1-30 | Repudiation (a contract lost in a deletion) | correlation-math.ts removal | low | mitigate | the G11.E.5 pearson cases move with the function via `git mv`, assertions unchanged; the dead-code premise is re-measured before each deletion |
</threat_model>

<verification>
- `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-05-PLAN.md` exits 0 before Task 1 starts.
- Both Task 3 verify commands exit 0.
</verification>

<success_criteria>
- D-17, second slice: one Pearson, one beta and one floor serve T6-T12; the dead rolling correlation and rolling metric are gone; each site has a test observed red or a recorded pin.
</success_criteria>

<output>
Create `.planning/phases/166.1-qstatsrecompute/166.1-05-SUMMARY.md` when done. Remove the node_modules
link, stage the SUMMARY, then run
`QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && test -x "$QZ_MAIN/node_modules/.bin/tsx" && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK before committing.
</output>
