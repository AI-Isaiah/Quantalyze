---
phase: 166.1-qstatsrecompute
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/dispersion-floor.ts
  - src/lib/dispersion-floor.test.ts
  - src/__tests__/fixtures/dispersion-nav.ts
  - src/lib/dispersion-floor-sites.test.ts
  - src/app/(dashboard)/compare/lib/holding-compare-adapter.ts
  - src/lib/sample-basis-ratios.ts
  - src/lib/scenario.ts
  - src/lib/diversification.ts
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The TS side has ONE residue floor, src/lib/dispersion-floor.ts, with the Python rule verbatim: floor = 1e-12 * max(1, |mean|), residue means sd <= floor, real means sd > floor, and a NaN sd is neither (D-15)"
    - "A TS test pins DISPERSION_RESIDUE_REL to the value in the Python source, so the two languages cannot drift apart silently (D-15)"
    - "A compounding-NAV constant yield gives, at T1-T5, EXACTLY the output an all-zero series of the same length gives today at the same field: /compare's holding Sharpe is null, sampleBasisRatios' Sharpe is null, computeScenario's Sharpe is null and its correlation cell for the constant leg is 0, and diversificationRatio is null (D-07, D-15)"
    - "A cent-rounded compounding NAV at 1% APY still yields a finite Sharpe at T1-T3 and a finite correlation at T4, so widening the floor goes red (D-07)"
    - "Every T1-T5 test was written first and observed RED on the unedited site, and every site was neutered, observed RED and restored from a byte backup proven with cmp"
    - "vitest on the touched tests, tsc --noEmit and eslint over the touched files are green, and no renderer, rule name or label changed"
  artifacts:
    - path: src/lib/dispersion-floor.ts
      provides: "DISPERSION_RESIDUE_REL, residueFloor, dispersionIsResidue, dispersionIsReal"
    - path: src/__tests__/fixtures/dispersion-nav.ts
      provides: "navConstantYield, apyToDaily and CONSTANT_YIELDS, the TS twin of analytics-service/tests/dispersion_fixtures.py"
    - path: src/lib/dispersion-floor-sites.test.ts
      provides: "red tests for T1-T5"
  key_links:
    - from: src/app/(dashboard)/compare/lib/holding-compare-adapter.ts
      to: src/lib/dispersion-floor.ts
      via: "dispersionIsResidue(std, mean) replaces std > 0 in the Sharpe"
      pattern: "dispersionIsResidue"
    - from: src/lib/scenario.ts
      to: src/lib/dispersion-floor.ts
      via: "the Sharpe and the correlation matrix read the shared floor"
      pattern: "dispersion-floor"
---

<objective>
Bring Phase 166's residue floor to the TypeScript side (CONTEXT D-15): one TS module holding the floor,
and the four files the plan checker named - `/compare`'s holding adapter, `sampleBasisRatios`, the
Sharpe and correlation in `computeScenario`, and `diversificationRatio` - moved onto it, each with a red
test built from a compounding-NAV constant yield.

Purpose: measured in Node 2026-09-25, `pct_change` over a compounding NAV leaves a sample sd of about
1.3e-16 at any yield, so a `> 0` guard passes and a Sharpe of 1.5e12 to 3.5e14 is computed and shown.
The TS side was believed to only read stored values (corrected in CONTEXT and RESEARCH A5).
Output: `src/lib/dispersion-floor.ts`, a shared TS fixture, two test files and four edited sites.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md
@.planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md

**Environment.** TS tools run through the MAIN checkout's `node_modules`, linked into the worktree only
for the run: `QZ_WT="$(git worktree list --porcelain)"`, `QZ_MAIN` = its first `worktree` path, then
`{ test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; }`. Remove the link before every
commit (`test -L node_modules && rm node_modules`) and re-link after. Exit 127 is a FAIL. No database,
no credential. Quote every path containing `(dashboard)`. `tsc --noEmit` over the whole project is
slow: run it in the background with a 600 s watchdog.

**Coupling with PR A (declared, CONTEXT D-15).** `src/app/(dashboard)/compare/lib/holding-compare-adapter.ts`
is also edited on the unmerged `feat/167.1.2-hide` branch, which turns `function reconstructAndAnalyze(`
into an exported function (measured 2026-09-25: that is the only line of its diff that touches the
Sharpe's function; the Sharpe lines themselves are not in it). Task 1 step 0 decides the route:
- `git fetch origin main`, then check whether `origin/main`'s copy of the file exports
  `reconstructAndAnalyze` while this branch's copy does not. If so, PR A merged first: STOP before any
  edit and report "PR A merged: rebase feat/166.1 onto origin/main before plan 04". The orchestrator
  rebases between waves (a rebase inside a running wave would move other executors' base), then re-runs
  this plan, which then tests `reconstructAndAnalyze` directly.
- Otherwise PR A has not merged: add NO export (so the only overlap stays one declaration line), and
  test T1 through the exported `fetchHoldingCompareItem` with a stub client, in the style of the file's
  existing test. Record the route taken in the SUMMARY.

**Drill rule.** Byte backup in the session scratchpad under a `166.1-04` name immediately before the
neuter, observe RED, restore with `cp`, prove with `cmp`. Never `git checkout --`, `git restore`,
`git stash`.

**The D-07 invariant in TS.** For each site, compute in the test the site's output on the NAV constant
yield AND on an all-zero series of the same length, and assert the ratio field is identical (null to
null, 0 to 0, NaN to NaN via `Object.is`). Never hard-code a value the code under test produces.

**Symbols this plan edits (cite by symbol; line numbers drift).**
<!-- verify-symbol: src/app/(dashboard)/compare/lib/holding-compare-adapter.ts reconstructAndAnalyze -->
<!-- verify-symbol: src/app/(dashboard)/compare/lib/holding-compare-adapter.ts fetchHoldingCompareItem -->
<!-- verify-symbol: src/lib/sample-basis-ratios.ts sampleBasisRatios -->
<!-- verify-symbol: src/lib/scenario.ts computeScenario -->
<!-- verify-symbol: src/lib/scenario.ts statA.std -->
<!-- verify-symbol: src/lib/diversification.ts diversificationRatio -->
<!-- verify-symbol: src/lib/diversification.ts percentContributionToRisk -->
<!-- verify-symbol: analytics-service/services/metrics.py _DISPERSION_RESIDUE_REL -->
<!-- verify-symbol: analytics-service/tests/test_metrics.py _Q166R2_CONSTANT_YIELDS -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the TS floor module and fixture, reaching ONE site end-to-end - /compare's holding Sharpe (T1) - with a red test</name>
  <files>src/lib/dispersion-floor.ts, src/lib/dispersion-floor.test.ts, src/__tests__/fixtures/dispersion-nav.ts, src/lib/dispersion-floor-sites.test.ts, src/app/(dashboard)/compare/lib/holding-compare-adapter.ts</files>
  <read_first>
    - analytics-service/services/metrics.py - `_DISPERSION_RESIDUE_REL`, `_residue_floor`, `_dispersion_is_residue`, `_dispersion_is_real` and their docblocks (the rule this module mirrors; if plan 01 has landed, the same text lives in `analytics-service/services/dispersion.py`)
    - analytics-service/tests/test_metrics.py - `_q166r2_nav_constant_yield`, `_q166r2_apy`, `_Q166R2_CONSTANT_YIELDS` (the fixture this plan mirrors, ids included)
    - src/app/(dashboard)/compare/lib/holding-compare-adapter.ts - `reconstructAndAnalyze` (the Sharpe) and `fetchHoldingCompareItem`; src/app/(dashboard)/compare/lib/holding-compare-adapter.test.ts for the stub-client style
    - vitest.config.ts - confirm a `src/__tests__/fixtures/*.ts` helper is imported by tests without being collected as a test, and whether coverage excludes it
    - .planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md - A5
  </read_first>
  <behavior>
    - Floor unit: `residueFloor(1e-4)` equals `1e-12`, `residueFloor(-3)` is 3e-12 within a relative 1e-12, `residueFloor(NaN)` is NaN; `dispersionIsResidue(0, m)` is true; `dispersionIsResidue(NaN, m)` and `dispersionIsReal(NaN, m)` are both false; `dispersionIsReal(4.8e-7, 1e-4)` is true (boundary values derived from the stated rule, not from the implementation)
    - Cross-language pin: the numeric literal assigned to `DISPERSION_RESIDUE_REL` (or `_DISPERSION_RESIDUE_REL`) in `analytics-service/services/dispersion.py` or `analytics-service/services/metrics.py`, whichever defines it, read as text, equals the TS constant; the test fails if neither file defines it
    - Fixture sanity: for every id in `CONSTANT_YIELDS`, the sample sd of `navConstantYield(y)` is above 0 and at most `residueFloor(mean)` (so the fixture really is residue, not exact zero), and the cent-rounded 1% APY series' sd is above the floor
    - T1: for every yield id, the holding's `sharpe` is null (today about 1e12 to 1e14), identical to the all-zero (flat NAV) holding's `sharpe`; the cent-rounded 1% APY NAV gives a finite `sharpe`
  </behavior>
  <action>
    Per D-15 and D-07. Step 0: run `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-04-PLAN.md` (halt on non-zero), then take the PR A route decision from the context and record it. Step 1: create `src/lib/dispersion-floor.ts` as a leaf with no imports, exporting `DISPERSION_RESIDUE_REL` (1e-12), `residueFloor(mean)`, `dispersionIsResidue(sd, mean)` and `dispersionIsReal(sd, mean)`. The rule is the Python one verbatim: floor is the constant times the larger of 1 and the absolute mean; residue is `sd` at or below the floor; real is `sd` strictly above it; a NaN on either side makes both false. The module doc comment says it is the TS twin of `analytics-service/services/dispersion.py`, names Phase 166 round 2 CR-01 as the reason for `max(1, |mean|)`, and says the constant is pinned to the Python source by a test. Step 2: create `src/__tests__/fixtures/dispersion-nav.ts` exporting `navConstantYield(dailyYield, n = 366, start = 10_000, cents = false)` (NAV at step t is `start * (1 + dailyYield) ** t`, optionally rounded to cents, then each return is the NAV ratio minus 1), `apyToDaily(apy)`, and `CONSTANT_YIELDS` with the same ids and values as the Python dict. Step 3: write `src/lib/dispersion-floor.test.ts` (floor unit, cross-language pin, fixture sanity) and the T1 tests in `src/lib/dispersion-floor-sites.test.ts`; observe T1 RED on the unedited adapter and record the RED ids. Step 4: fix T1 in `reconstructAndAnalyze`: the Sharpe is null when `dispersionIsResidue(std, mean)`; otherwise today's formula. Keep the population variance and every other field untouched. Step 5: drill T1 (restore `std > 0`, RED, `cp` restore, `cmp`).
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/dispersion-floor "src/app/(dashboard)/compare/lib/holding-compare-adapter"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export const DISPERSION_RESIDUE_REL\|^export function residueFloor\|^export function dispersionIsResidue\|^export function dispersionIsReal' src/lib/dispersion-floor.ts` prints 4
    - `grep -c '^import' src/lib/dispersion-floor.ts` prints 0
    - `grep -c 'dispersionIsResidue' "src/app/(dashboard)/compare/lib/holding-compare-adapter.ts"` prints at least 1, and `grep -c 'std > 0 ?' "src/app/(dashboard)/compare/lib/holding-compare-adapter.ts"` prints 0
    - the SUMMARY records the PR A route taken, T1 RED before the fix with its yield ids, and the T1 drill row
  </acceptance_criteria>
  <done>The TS floor has one home pinned to the Python constant, and /compare's holding Sharpe is null on a compounding constant yield, proven by a test observed red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: T2-T5 - sampleBasisRatios, computeScenario's Sharpe and correlation, diversificationRatio - each red first and drilled, then the plan's vitest, tsc and eslint gates</name>
  <files>src/lib/sample-basis-ratios.ts, src/lib/scenario.ts, src/lib/diversification.ts, src/lib/dispersion-floor-sites.test.ts</files>
  <read_first>
    - src/lib/sample-basis-ratios.ts - `sampleBasisRatios` (the Sharpe; its Sortino is NOT in the class: leave it)
    - src/lib/scenario.ts - `computeScenario`: the portfolio Sharpe block and the correlation-matrix loop over `strategyStats` (`statA.std`, `statB.std`); its Sortino stays
    - src/lib/scenario.test.ts - how a scenario input is built in tests
    - src/lib/diversification.ts - `diversificationRatio`, `computeDiversification` (how `sigmaP` is built), and `percentContributionToRisk` / `effectiveNumberOfBets` (NOT in the class, RESEARCH A5: leave them)
    - src/lib/diversification.test.ts - how its input is built
  </read_first>
  <behavior>
    - T2: `sampleBasisRatios(navConstantYield(y), basis).sharpe` is null for every yield id, identical to the all-zero series'; the cent-rounded control is finite; Sortino and max drawdown are identical before and after the edit (computed in the test from one input)
    - T3: `computeScenario` with a single constant-yield member gives portfolio `sharpe` null, identical to a single all-zero member
    - T4: with one constant-yield member and one noisy member, the correlation cell between them equals the cell an all-zero member gets today (0) and the noisy-noisy cell with a second noisy member stays finite; the "avg |corr|" figure equals the all-zero case's
    - T5: `diversificationRatio` for a book whose every member is a constant yield is null, identical to an all-zero book's; a book of noisy members keeps a finite ratio
  </behavior>
  <action>
    Per D-15 and D-07. Write each site's tests first in `src/lib/dispersion-floor-sites.test.ts` and observe RED on the unedited site, recording the RED ids. Then: T2 and T3 publish the Sharpe only when NOT `dispersionIsResidue(volDaily, meanR)` (the daily sd and the daily mean, never the annualised vol against the daily mean). T4 builds each strategy's sd and mean once and treats a pair as 0 unless both legs pass `dispersionIsReal(std, mean)` (the per-strategy stats object gains the mean if it does not carry one). T5: `diversificationRatio` returns null when `dispersionIsResidue(sigmaP, 0)` (no portfolio mean is available there, so the absolute floor applies, as in the Python S7; update its doc comment's "Returns null when" sentence). Import from `@/lib/dispersion-floor` in each file. Drill every site (byte backup, restore the old guard, RED, `cp` restore, `cmp`) and put T1-T5 in the SUMMARY drill table. Then run the gates below. Confirm and record per site that the fixed output is a value the site already emits for an exactly constant input, so no renderer, rule name or label changes; if any site would emit a new shape, STOP and report.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/dispersion-floor src/lib/scenario src/lib/diversification "src/app/(dashboard)/compare" "src/app/(dashboard)/allocations/components/ScenarioComposer"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or "No test files found"</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint src/lib/dispersion-floor.ts src/lib/dispersion-floor.test.ts src/__tests__/fixtures/dispersion-nav.ts src/lib/dispersion-floor-sites.test.ts "src/app/(dashboard)/compare/lib/holding-compare-adapter.ts" src/lib/sample-basis-ratios.ts src/lib/scenario.ts src/lib/diversification.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), any tsc error line, or any eslint error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dispersion-floor' src/lib/sample-basis-ratios.ts src/lib/scenario.ts src/lib/diversification.ts` prints at least 1 for each file
    - `grep -c 'volatility > 0 ?' src/lib/sample-basis-ratios.ts src/lib/scenario.ts` prints 0 for each file, and `grep -c 'statA.std > 0 && statB.std > 0' src/lib/scenario.ts` prints 0
    - the SUMMARY carries the drill table for T1-T5 with each `cmp` result, and the per-site "already emitted for an exact constant" line
    - `git diff --stat -- analytics-service/ supabase/` is empty for this plan's commits
    - `test -L node_modules` is false at every commit
  </acceptance_criteria>
  <done>T1-T5 read the shared TS floor, each with a test observed red and a drill, and vitest, tsc and eslint are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daily returns -> TS-computed ratio -> rendered page | a fabricated Sharpe or correlation computed in the browser or a server component reaches /compare and the scenario composer |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.1-19 | Tampering (integrity of a displayed number) | T1-T5 | medium | mitigate | shared `dispersion-floor.ts`; red test per site from a compounding-NAV constant yield; D-07 equality with the all-zero input; drill table |
| T-166.1-20 | Tampering (cross-language drift) | the floor constant | low | mitigate | a test reads the Python source's constant and fails on any difference |
| T-166.1-21 | Denial of service (over-wide floor hides real data) | the TS floor | medium | mitigate | cent-rounded 1% APY controls at T1-T4 go red if the floor widens |
| T-166.1-22 | Tampering (merge conflict silently resolved wrong) | holding-compare-adapter.ts vs PR A | low | mitigate | declared coupling; the plan halts for a rebase if PR A merged, and adds no export otherwise |
</threat_model>

<verification>
- `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-04-PLAN.md` exits 0 before Task 1 starts.
- Both Task 2 verify commands exit 0.
</verification>

<success_criteria>
- D-15, first slice: the TS floor exists once, pinned to Python, and T1-T5 use it, each with a test observed red.
</success_criteria>

<output>
Create `.planning/phases/166.1-qstatsrecompute/166.1-04-SUMMARY.md` when done. Remove the node_modules
link, stage the SUMMARY, then run
`QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && test -x "$QZ_MAIN/node_modules/.bin/tsx" && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK before committing.
</output>
