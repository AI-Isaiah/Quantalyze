---
phase: 166.1-qstatsrecompute
verified: 2026-09-26T15:55:00Z
status: human_needed
score: 10/10 must-haves verified
verified_at_sha: 09fc1649fda5b05638473bc587c342906cef4c17
drift_subjects:
  - analytics-service/services/dispersion.py
  - analytics-service/services/metrics.py
  - analytics-service/services/portfolio_optimizer.py
  - analytics-service/services/csv_validator.py
  - analytics-service/services/allocated_capital.py
  - analytics-service/services/equity_reconstruction.py
  - analytics-service/services/optimizer.py
  - analytics-service/services/portfolio_risk.py
  - analytics-service/services/analytics_runner.py
  - analytics-service/services/match_engine.py
  - analytics-service/services/strategy_matching.py
  - analytics-service/services/simulator_scoring.py
  - analytics-service/services/bridge_scoring.py
  - analytics-service/routers/portfolio.py
  - analytics-service/tests/dispersion_fixtures.py
  - analytics-service/tests/test_dispersion_floor_sites.py
  - analytics-service/tests/test_correlation_residue_sites.py
  - analytics-service/tests/test_csv_validator.py
  - analytics-service/tests/test_portfolio_risk.py
  - analytics-service/tests/test_coverage_extras.py
  - analytics-service/tests/test_portfolio_compute_integration.py
  - analytics-service/tests/test_metrics.py
  - src/lib/types.ts
  - src/lib/portfolio-analytics-adapter.ts
  - src/lib/analytics-schemas.ts
  - src/lib/portfolio-insights.ts
  - src/lib/wizardErrors.ts
  - src/components/portfolio/WhatWedDoCard.tsx
  - src/components/portfolio/ReplacementCard.tsx
  - src/components/portfolio/RiskAttribution.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx
behavior_unverified: 0
overrides_applied: 0
deferred:
  - truth: "SC-1 Census first (PROD census of the rows Phase 166 changes)"
    addressed_in: "Phase 166.3"
    evidence: "ROADMAP ### Phase 166.1 success criterion 1 is struck through: 'Moved to Phase 166.3 at the 2026-09-26 split' (CONTEXT D-23)"
  - truth: "SC-2 Normal job path (PROD rows recomputed through the compute-job path, one at a time, before/after per row)"
    addressed_in: "Phase 166.3"
    evidence: "ROADMAP ### Phase 166.1 success criterion 2 is struck through: 'Moved to Phase 166.3 at the 2026-09-26 split'; old plan 02 is 166.3-01"
  - truth: "SC-3 TypeScript extension (D-15, D-17: T1-T20 compute-once through src/lib/return-stats.ts)"
    addressed_in: "Phase 166.2"
    evidence: "ROADMAP criterion 3 notes: '[split 2026-09-26: the TS part is Phase 166.2's.]'; old plans 04-08 are 166.2-01..07"
human_verification:
  - test: "SHIP BLOCKER (blocks the PR, not the verdict). Before the PR opens: merge origin/main, cut a NEW chore(release) commit (never amend, never rebase) that re-derives the version, and re-run the Python sweep on the merged tree."
    expected: "Plan 09's derivation command prints 'version-derivation: origin/main=0.97.0.0 release=0.98.0.0'; VERSION and package.json byte-equal at 0.98.0.0; the ENGINEFLOOR heading moves to [0.98.0.0] with Phase 168's [0.97.0.0] entry kept below it; full pytest (TEST DB vars unset) green with the census line, strict mypy green, ruff no new finding, verify-plan-anchors --pending OK."
    why_human: "Owner: the ship step (orchestrator). Measured now: origin/main is 3b923498e (Phase 168, #867) at VERSION 0.97.0.0, the same string this branch released, so the plan's own derivation command is RED ('origin/main=0.97.0.0 branch=0.97.0.0 want=0.98.0.0'). This is the fails_when case plan 09 Task 3 names. git merge-tree shows conflicts in CHANGELOG.md and .planning/STATE.md only, no code path. It needs a new commit and a merge, which a verifier does not make."
  - test: "Phase 166.3 RECOMPUTE: the founder runs the PROD census and the one-at-a-time recompute through the normal compute-job path."
    expected: "Before/after counts per row, published in 166.3's SUMMARY as counts only; Q6 SQN residual counts recorded."
    why_human: "Owner: founder (PROD access; D-10: no agent runs SQL against PROD). Criteria 1 and 2 moved to 166.3 at the split; the CHANGELOG records the state as 'founder-gated, pending'. Never passed here."
  - test: "WR-03 product call: a book with one constant-yield sleeve now shows an average pairwise correlation over the defined pairs only, and a flat added or swapped leg shows '—' for its correlation delta."
    expected: "The founder or orchestrator confirms the skip-and-count rule is the wanted product behaviour (the alternative, None when any leg is flat, was rejected in CONTEXT)."
    why_human: "Owner: orchestrator (fixer flagged it in 166.1-REVIEW-FIX.md 'Logic changes that need human verification'). Tests pin the stated rule; whether it is the right rule is a judgement."
  - test: "Post-deploy browser UAT of the D7 renders and the WR-01 wizard copy: upload a constant positive daily-returns CSV in the strategy wizard; open a portfolio whose book or candidate is a constant yield and view the optimizer card, the replacement card, the simulator panel and the risk attribution table."
    expected: "The wizard shows the headline 'Your file failed validation', the label 'Daily returns never change', no row count and no number; every undefined statistic reads '—' in muted text, never 0.00 or 'unchanged'; a flat row is left out of the risk stacked bar."
    why_human: "Owner: orchestrator, in the logged-in browser after deploy. Visual copy and rendering; vitest pins the components but not the deployed page."
---

# Phase 166.1: ENGINEFLOOR Verification Report

**Phase Goal:** Every Python site outside `services/metrics.py` that divides by a standard deviation (the exact-zero guards S1-S8) or correlates a leg (C1-C8) reads Phase 166's relative dispersion floor from ONE module, `services/dispersion.py`, so a compounding-NAV constant yield produces exactly what an all-zero series produces at that site, each proven by a red test; one release commit ships it. Amended by founder decisions D-24 (a constant positive CSV is rejected at every length) and D7 ("Show — everywhere": an undefined statistic stays None/null end to end and renders "—").
**Verified:** 2026-09-26T15:55:00Z at `09fc1649f`
**Status:** human_needed
**Re-verification:** No, initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | The floor lives in ONE module; `metrics.py` re-binds only the three functions under the old private names (D-03, D-23 revision) | ✓ VERIFIED | `services/dispersion.py` defines `DISPERSION_RESIDUE_REL`, `residue_floor`, `dispersion_is_residue`, `dispersion_is_real`, and the helpers `pairwise_correlation_or_none`, `dispersing_corrwith` and `average_pairwise_correlation`. `metrics.py` imports the three functions `as _residue_floor` / `_dispersion_is_residue` / `_dispersion_is_real` and binds no constant. The module imports only numpy and pandas (a leaf). `test_metrics.py` runs green inside the 1135-pass run. |
| 2 | Variance sites S1-S7 read the floor; a compounding-NAV constant yield gives the all-zero output at each (SC-3; D-05, D-06) | ✓ VERIFIED | Callers found by grep: `portfolio_optimizer._compute_sharpe` and `find_improvement_candidates` (S1, S2), `csv_validator._check_sharpe_sentinel` (S3), `allocated_capital._annualised_sharpe` (S4), `equity_reconstruction` `compute_sharpe` (S5), `optimizer.optimize_weights` `residue_floor` (S6), `portfolio_risk.compute_risk_decomposition` (S7). No `std == 0` / `std > 0` dispersion guard is left in those files. **Drill by the verifier, in a scratch copy outside the repo:** with `dispersion_is_residue` / `_real` neutered to `sd == 0` / `sd > 0`, the S1, S2, S3, S4, S5 and S7 tests go RED (22/11/12/11/11/11 failures). S6 stays green because its gate reads `residue_floor` directly, so it is the declared D-06 pin. With the floor itself forced to 0, the S6 tests also go RED (11). |
| 3 | S8, the SQN block, is on the floor (D-16, plan 01b) | ✓ VERIFIED | `analytics_runner._compute_derived_trade_metrics` guards with `std_r > 0 and not dispersion_is_residue(std_r, mean_r)` (IN-01 kept the structural divide guard). The six `test_s8_*` tests pass, including "floor changes no other key" and "real dispersion is finite". Under the predicate neuter, 3 S8 tests go RED. The no-production-caller fact (Phase 106) is recorded in the CHANGELOG Notes. |
| 4 | Correlation sites C1-C8 use the shared helpers; two same-yield strategies never match; an empty candidate set is an explicit None (D-02) | ✓ VERIFIED | C1 and C2 are in `portfolio_risk` (the matrix mask and the rolling `both_move` on `residue_floor`). C3 and C4 are in `portfolio_optimizer` (`pairwise_correlation_or_none`, and `_avg_corr` over `average_pairwise_correlation`). C5 is in `match_engine`. C6 and C7 are in `routers/portfolio.py` (the benchmark correlation, and `dispersing_corrwith` in `verify_strategy`). C8 is `strategy_matching.find_matched_strategy` (`dispersing_corrwith`). Under the predicate neuter, the tests go RED at C1, C3-C8, the helpers, HIGH-2 and WR-03. C2 goes RED under the floor-to-zero drill. The C7 tests drive the real `verify_strategy`. |
| 5 | The cent-rounded NAV control keeps a finite ratio, so widening the floor goes red (D-07) | ✓ VERIFIED | `test_s1/s3/s4/s5_*_real_quantisation_dispersion_*` and `test_pairwise_helper_cent_rounded_nav_keeps_a_correlation` pass at HEAD. |
| 6 | D-24: an exactly constant POSITIVE daily-returns CSV is rejected at every length, with one message that names no number; a constant zero series keeps its verdict | ✓ VERIFIED | **Verifier probe through `validate_csv`:** 24 inputs (lengths 2, 3, 5, 10, 30, 120, 365 and 1000 at constant 0.001, 0.0001 and 0.01) were all rejected with `daily_returns_constant`. Compounding-NAV yields of 1e-4 and 3e-4 were rejected, with no digit in the message. Constant zero at 5 and 120 rows was accepted, and a noisy series was accepted. `test_d24_*` go RED (4) under the predicate neuter. |
| 7 | D7 "Show — everywhere": an undefined statistic stays None/null end to end and renders "—"; a constant yield reads the same as an all-zero series | ✓ VERIFIED | **Python:** `_delta` returns None in both scorers, `_leg_is_flat` guards the correlation deltas, `find_improvement_candidates` keeps `corr_with_portfolio` and `sharpe_lift` None, and `compute_risk_decomposition` emits None shares. **Types and schemas:** `types.ts` and `analytics-schemas.ts` type these fields `number \| null` / `.nullable()`, and the adapter drops `?? 0`. **Renderers, traced by the verifier:** `WhatWedDoCard` drops the clause, `ReplacementCard` shows "— {label}", `RiskAttribution` shows "—" and leaves the row out of the bar, `PortfolioOptimizer.formatMetric` gives null → "—", `PortfolioImpactPanel` already rendered null, and `AllocatorMatchQueue` only types the fields and does not render them. `tsc --noEmit` exits 0. Known limits (MEDIUM-A/B and the others) are listed below; none breaks this truth. |
| 8 | The Python gates are green (pytest with the TEST DB vars unset, qstats census, strict mypy, ruff no new finding, pending anchors) | ✓ VERIFIED | See Behavioral Spot-Checks. The shipped tree is byte-identical to `1c2fff365`: `git diff --stat 1c2fff365 HEAD -- . ':(exclude).planning'` is empty. So the fixer's full-suite run (`6760 passed, 90 skipped`) applies to HEAD. The verifier ran the phase subset itself. |
| 9 | ONE release commit: VERSION = package.json, 4-digit, a minor bump derived from origin/main at release time; ONE unified CHANGELOG entry carrying the 166.3 state, the stored `portfolio_analytics` residue, the SQN residual, D-11, no migration and the split; no CI-skip token in any branch message | ✓ VERIFIED (at the release SHA) | `46fcca88e chore(release): v0.97.0.0` was derived from origin/main 0.96.0.0 (plan 09 SUMMARY verify 3). VERSION is `0.97.0.0` and package.json is `"version": "0.97.0.0"`. The `[0.97.0.0]` ENGINEFLOOR entry carries every required Notes bullet (read by the verifier). The skip-token sweep over every message in `ea4167a3f..HEAD` finds 0. ⚠️ **origin/main has since moved to 0.97.0.0 (Phase 168), so the derivation is red now.** See Human Verification item 1: it blocks the PR, not this verdict, and plan 09 designs the ship-time re-run for exactly this case. |
| 10 | The branch's `src/` changes are limited to 166.1's own Python outputs' direct consumers (plan 09 truth 3, "no src/ change", as amended by D7) | ✓ VERIFIED (superseded by D7) | The founder's D7 amends D-23's no-`src/` rule. It is recorded in CONTEXT, in ROADMAP `### Phase 166.1` and in a CHANGELOG correction. `git diff ea4167a3f HEAD -- src` touches 9 source files, exactly the D7 list, plus their tests and `PortfolioImpactPanel.test.tsx` (test only). |

**Score:** 10/10 truths verified (0 present, behavior-unverified)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|---|---|---|
| 1 | SC-1 census | Phase 166.3 | ROADMAP criterion 1 struck through, "Moved to Phase 166.3 at the 2026-09-26 split" |
| 2 | SC-2 normal job path (PROD recompute) | Phase 166.3 | ROADMAP criterion 2 struck through; old plan 02 is 166.3-01 |
| 3 | SC-3 TypeScript sites T1-T20 | Phase 166.2 | ROADMAP criterion 3 note "[split 2026-09-26: the TS part is Phase 166.2's.]" |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `analytics-service/services/dispersion.py` | the floor and the three correlation helpers | ✓ VERIFIED | Substantive. Imported by 11 service/router modules (grep). |
| `analytics-service/tests/dispersion_fixtures.py` | the constant-yield generator | ✓ VERIFIED | Used by both site test files. |
| `analytics-service/tests/test_dispersion_floor_sites.py` | S1-S8 red tests | ✓ VERIFIED | 25 site tests. They bite under the drill. |
| `analytics-service/tests/test_correlation_residue_sites.py` | C1-C8 red tests | ✓ VERIFIED | 24 site tests plus the helper, HIGH-2 and WR-03 tests. They bite under the drill. |
| `analytics-service/services/csv_validator.py` | D-24 sentinel | ✓ VERIFIED | The probe above. |
| `analytics-service/tests/test_csv_validator.py` | D-24 tests and the flipped 5-row fixture | ✓ VERIFIED | `test_d24_*` go RED under the neuter. |
| `VERSION`, `package.json`, `CHANGELOG.md` | release | ✓ VERIFIED at the release SHA | The version collision with main is a ship-step item. |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `services/metrics.py` | `services/dispersion.py` | `from services.dispersion import (... as _dispersion_is_residue ...)` | WIRED |
| `services/optimizer.py` | `services/dispersion.py` | `residue_floor(returns.mean(axis=0))` | WIRED |
| `services/analytics_runner.py` | `services/dispersion.py` | `dispersion_is_residue(std_r, mean_r)` | WIRED |
| `routers/portfolio.py` | `services/dispersion.py` | `pairwise_correlation_or_none` (C6), `dispersing_corrwith` (C7) | WIRED |
| `services/strategy_matching.py` | `services/dispersion.py` | `dispersing_corrwith` | WIRED |
| `services/csv_validator.py` | `services/dispersion.py` | `dispersion_is_residue(float(sd), float(r.mean()))` | WIRED |
| `validate_csv` | `_check_sharpe_sentinel` | `all_errors.extend(...)`; reached from `routers/csv.py` and the `process_key` CSV path | WIRED |
| Python None outputs | rendered "—" | adapter → types/schemas → the four cards | WIRED |

### Behavioral Spot-Checks

These were run by the verifier at `09fc1649f`, from `analytics-service/`, with `env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY` and the main checkout's analytics venv:

| Behavior | Command | Result | Status |
|---|---|---|---|
| Phase pytest subset (21 files: both site files, the csv_validator trio, metrics, portfolio_risk, coverage_extras, compute_integration, simulator, bridge, portfolio_optimizer, match_engine, optimizer, allocated_capital, equity_reconstruction, qstats_gate) | `.venv/bin/python -m pytest -q …` | `1135 passed, 127 warnings in 43.46s` | ✓ PASS |
| qstats census | `pytest tests/test_qstats_gate.py -s` | `qstats-gate census: 13 quantstats node(s) in services/metrics.py, 11 mirror(s), 0 violation(s); arms kwarg-proven=12 exempt=1 inline=11` (79 passed) | ✓ PASS |
| strict mypy | `mypy --strict services/ routers/ models/` | `Success: no issues found in 97 source files` | ✓ PASS |
| ruff no new finding | `ruff check --output-format=concise` over the 22 phase `.py` files at HEAD and at the merge-base `ea4167a3f`, line numbers stripped | 14 at HEAD, 14 at base, **0 new** (all pre-existing F401/F821/F841/E402) | ✓ PASS |
| vitest, phase TS tests | `npx vitest run --test-timeout=30000` on CsvValidationEnvelope, PortfolioImpactPanel, ReplacementCard, RiskAttribution, WhatWedDoCard, analytics-schemas, portfolio-analytics-adapter, portfolio-insights, PortfolioOptimizer, wizardErrors | `Test Files 10 passed (10)`, `Tests 504 passed (504)` | ✓ PASS |
| vitest, invariants and critical regressions | wizardErrors.invariant, wizardErrors.roster-render, critical-regressions | `Tests 335 passed (335)` | ✓ PASS |
| tsc | `npx tsc --noEmit` | exit 0 | ✓ PASS |
| pending anchors | `node scripts/verify-plan-anchors.mjs --pending` | `OK: 11 plan file(s), no stale claims.` | ✓ PASS |
| D-24 through `validate_csv` | throwaway probe (above) | all 24 constant-positive inputs and 2 NAV yields rejected; zero and noisy inputs accepted | ✓ PASS |
| Tests can fail | neuter drill in a scratch copy outside the repo (predicates to `== 0` / `> 0`; separately the floor to 0) | 215 failed / 95 passed across the two site files and csv_validator; C2 and S6 go red only under the floor drill | ✓ PASS |
| Version derivation now | plan 09 Task 3 verify command | `origin/main=0.97.0.0 branch=0.97.0.0 want=0.98.0.0`, RED | ✗ ship-step item (Human 1) |

`node_modules` was symlinked for the TS gates and removed afterwards.

### Probe Execution

Step 7c: SKIPPED. The phase declares no `scripts/*/tests/probe-*.sh`.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| SC-3 (Python sites) | 01, 01b, 03, 09 | Exact-zero guards close on the relative floor, and each site has a red test | ✓ SATISFIED | Truths 1-6 |
| SC-1, SC-2 | none (moved) | Census and recompute | DEFERRED to 166.3 | ROADMAP split note |
| SC-3 (TS extension) | none (moved) | T1-T20 | DEFERRED to 166.2 | ROADMAP split note |

No orphaned requirement.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| (the 31 changed source files) | — | TBD / FIXME / XXX | none found | — |
| `services/simulator_scoring.py` | `_zero_deltas` | 0.0 deltas on the `insufficient_data` branch | ℹ️ Info (LOW-B) | Not rendered: the panel gates on `status === "ok"` |

### Known Limits (round 2: 0 CRITICAL / 0 HIGH; founder rule, no fix round for MEDIUM-or-lower)

None of these breaks a success criterion.

- **REVIEW MEDIUM-A / SFH-R2 MEDIUM-2.** `data_quality` stores `avg_pairwise_correlation_pairs_used` / `_total`, but nothing under `src/` reads them. The Avg Correlation KPI and the `generate_narrative` sentence present a subset average without a qualifier.
- **REVIEW MEDIUM-B.** The bridge `fit_label` is judged on the axes that exist (`_axis` scores a missing axis 0), so a flat book shows "— Sharpe", "— Corr" and a three-axis-looking "Weak fit" badge.
- **SFH-R2 MEDIUM-1.** The file-level cause sentence ("no single row is at fault") also fires for `parse_error`, whose message names a line. `parse_error` has no `CSV_RULE_LABELS` entry.
- **REVIEW LOW-A / SFH-R2 LOW-1.** `WhatWedDoCard` falls back to "to diversify the portfolio" when no statistic backs it. This is pre-existing; the only mount is the demo page.
- **REVIEW LOW-B.** `_zero_deltas()` sends 0.0 on the wire on an unrendered branch.
- **IN-05, accepted loss.** Two identical constant-yield series can no longer match at C7 or C8. This is carried in the CHANGELOG Notes.
- **`deferred-items.md`.** Three load-dependent full-suite flakes live in files the phase does not touch: `test_limiter_identity` ordering, the `test_feedback_engine` timeout and the `test_mt5_session_monitor` cadence.

### Observed Outside the Goal (pre-existing, user-facing, NOT routed; orchestrator must route)

- **Drawdown-delta sign inverted in three scorers.** The sites are `simulator_scoring.simulate_add_candidate` (`_delta(current_max_dd, proposed_max_dd)`), `portfolio_optimizer.find_improvement_candidates` and `match_engine._compute_portfolio_fit_components` (`current_max_dd - new_max_dd`). The same expressions are at the phase base. `bridge_scoring` was already corrected under H-1065. The verifier's grep finds 0 entries in `TODOS.md` or ROADMAP, on this branch and on origin/main.
- **`RiskAttribution` percent double-scale.** The producer emits `marginal_risk_pct` and `weight_pct` on a 0-100 scale, and `formatPercent` multiplies by 100 again. The phase's NEW `RiskAttribution.test.tsx` feeds fractions (0.8, 0.3) that the producer never sends, so it cannot catch the unit mismatch. That is a test-fidelity limit, not a 166.1 gap. It is not routed.

### Human Verification Required

1. **Ship blocker: re-derive the version on the merged tree** (owner: ship step / orchestrator). Merge origin/main; the conflicts are in `CHANGELOG.md` and `.planning/STATE.md` only. Make a NEW `chore(release)` commit at `0.98.0.0`, keeping Phase 168's `[0.97.0.0]` entry. Re-run the Python sweep on the merged tree. Expect `version-derivation: origin/main=0.97.0.0 release=0.98.0.0`. The ROADMAP goal says "one release commit". CONTEXT D-23 revision item 6 and plan 09 Task 3's `fails_when` prescribe this second, re-deriving commit. That is founder-approved design, not drift.
2. **Phase 166.3 PROD recompute** (owner: founder; needs PROD access and D-10). It is founder-gated and pending, and is never passed here.
3. **WR-03 product call** (owner: orchestrator). Confirm the skip-and-count average-correlation rule and the "—" for a flat added or swapped leg.
4. **Post-deploy browser UAT** (owner: orchestrator, in the logged-in browser). Check the wizard's constant-CSV copy and the D7 "—" renders on the optimizer, replacement, simulator and risk-attribution surfaces.

### Gaps Summary

There is no code gap. The phase goal holds in the codebase at `09fc1649f`, for the Python half the ROADMAP now scopes:

- One floor module.
- Every S and C site on it.
- Each site's test proven able to fail by the verifier's own drill.
- D-24 rejection at every length, measured.
- D7 null-to-"—" traced end to end.
- Every gate green.

The status is `human_needed` for two reasons:

- Main moved to the same version string after the release commit. That is the plan's anticipated ship-time re-derivation, and it blocks the PR, not the verdict.
- The PROD recompute and the UAT items can only happen after merge or with founder access.

---

_Verified: 2026-09-26T15:55:00Z_
_Verifier: Claude (gsd-verifier)_
