# Phase 166.1: QSTATSRECOMPUTE - Context

**Gathered:** 2026-09-25
**Status:** Ready for planning
**Mode:** orchestrator-autonomous. `/gsd-discuss-phase` did not run. Every decision below was taken by
the orchestrator (or ratified from the researcher's recommendation) on repo evidence and says so. No
AskUserQuestion was used. The founder's only inputs are the Phase 166 OPEN-2 answer and the census
counts in D-01, both quoted verbatim.

<domain>
## Phase Boundary

Two independent halves.

- **Code half.** Every standard-deviation divisor outside `services/metrics.py` that still tests for
  exact zero moves to Phase 166's relative dispersion floor, and so does every correlation site that
  divides by a leg's standard deviation without any guard. Each gets a red test built from a
  compounding-NAV constant yield.
- **Recompute half.** Every PRODUCTION `strategy_analytics` row that Phase 166 changes, and that was
  computed before the 166 worker deploy, is recomputed through the normal compute-job path, one
  strategy at a time, with a before/after check per row.

Out of scope: any migration; any change to `services/metrics.py` behaviour (it only re-imports the
moved floor); any TS change; `strategy_verifications.metrics_snapshot` rows (D-11).

⛔ **CORRECTED 2026-09-25 (plan-check revision, D-15).** "Any TS change" is no longer out of scope, and
the premise behind it was false. The TS side does NOT only read stored values: at least twenty sites
under `src/` compute a Sharpe, a correlation, a beta, an information ratio, a skew or kurtosis, a
diversification ratio or a risk share from daily returns behind a `> 0` / `!== 0` guard, or behind a
relative-only floor that misses the compounding-NAV residue (measured in Node 2026-09-25: residue sd
about 1.3e-16, Sharpe 1.5e12 to 3.5e14). They are in scope under D-15. What stays out of scope in TS
is any change to the CSV sentinel's rule name or label (D-04) and any change to how a renderer shows
a null. The sentence above is kept as lineage.

</domain>

<decisions>
## Implementation Decisions

### Founder input (measured, counts only)

- **D-01 (2026-09-25, founder, measured input; not reversible: it is a measurement).** The five
  read-only census SELECTs from `166-09-SUMMARY.md` ran on PROD. The founder ran them; the
  orchestrator relayed counts only. Success criterion 1 is therefore MET.

  | # | Census query | Count |
  |---|---|---|
  | 1 | Guess-trigger strategies (`min(r) >= 0 AND max(r) >= 1` over `daily_returns_grid`) | 0 |
  | 2 | BTC benchmark, largest daily return | +12.2% (no day at or above +100%) |
  | 3 | `strategy_analytics` rows with alpha AND beta exactly 0 (166 D-15 class) | 0 |
  | 4 | Strategies with a non-empty `rolling_alpha` series (166 D-17 class) | 10 |
  | 5 | Rows with a `probabilistic_sharpe_ratio` key / with a numeric one (166 D-16 class) | 15 / 15 |

  Consequence: the recompute exists for the 166 D-16 (PSR) and 166 D-17 (rolling alpha) corrections
  only. The set is 15 to 25 strategies before the `computed_at` filter; the classification query in
  plan 02 gives the exact figure.

### Code half

- **D-02 (2026-09-25, orchestrator, OQ1; reversible: a revert of one task).** The unguarded
  correlation sites are folded into plan 01 as their own task. Reason: the same root cause as the
  exact-zero guards (a ratio over a residue standard deviation), Phase 166 round 2 already fixed the
  `metrics.py` siblings for exactly this reason, and the repo rule is to close the whole class rather
  than park it. The research named six sites. The planner's re-grep of `.corr(` / `corrwith` over
  `services/` and `routers/` found **two more**, both `corrwith` matching sites
  (`strategy_matching.find_matched_strategy` and the `verify_strategy` matching block in
  `routers/portfolio.py`). They are the same class and are included. A dated note in ROADMAP
  `### Phase 166.1` records that criterion 3 now covers correlation sites too.
  ⭐ **Amended 2026-09-25 (plan-check revision, B3).** The correlation class C1-C8 is its own plan,
  `166.1-03`, in wave 2 after plan 01, instead of a task inside plan 01. Plan 01 was over budget with
  it. The scope is unchanged.
- **D-03 (2026-09-25, orchestrator, ratifying research A3; reversible).** One new leaf module,
  `analytics-service/services/dispersion.py`, holds Phase 166's floor as the single source:
  `DISPERSION_RESIDUE_REL`, `residue_floor`, `dispersion_is_residue` and `dispersion_is_real`, moved
  verbatim with their docblocks. `services/metrics.py` re-binds them under the old private names
  (`_DISPERSION_RESIDUE_REL`, `_residue_floor`, `_dispersion_is_residue`, `_dispersion_is_real`), so
  metrics behaviour and every existing test attribute stay byte-identical. Reason: `optimizer.py` is
  declared PURE, and `csv_validator.py`, `optimizer.py` and `allocated_capital.py` must not import
  quantstats and scipy through `services.metrics`. The same module carries the two small correlation
  helpers D-02 needs (a pairwise correlation that returns None unless both legs really disperse, and a
  `corrwith` restricted to dispersing columns), so the class has one implementation.
- **D-04 (2026-09-25, orchestrator, ratifying the researcher's S3 call; reversible: one conditional).**
  The CSV Sharpe sentinel keeps REJECTING a constant positive series, but no longer prints the
  fabricated ~1e15 number. A residue series with a mean above the risk-free rate gets the same
  `daily_sharpe_sentinel` rule with a message that names no number. A residue series at or below the
  risk-free rate gets no error (today's all-zero path). Real dispersion keeps today's code. No TS
  change: the rule name and its label are untouched. Reason: accepting the upload would flip a
  user-facing verdict; the number is the only fabrication.
- **D-05 (2026-09-25, orchestrator, ratifying research A2 row S7; reversible).**
  `portfolio_risk.compute_risk_decomposition` is in scope although the ROADMAP list omits it. It is an
  exact-zero guard of the same class and splits a residue portfolio volatility 48.6% / 51.4%.
- **D-06 (2026-09-25, orchestrator; reversible).** `optimizer.optimize_weights` is RE-POINTED to the
  shared floor. Its test is an honest PIN, not a red test: its old absolute `<= 1e-12` gate equals the
  new floor for any `|mean| <= 1`, so the test cannot go red against today's code. The drill neuters
  the gate to `== 0` instead, and the SUMMARY says so.
- **D-07 (2026-09-25, planner discretion; reversible).** The test invariant for every fixed site is:
  a compounding-NAV constant yield produces EXACTLY the output that an exactly-constant (all-zero)
  series of the same length produces today at that site. That makes each fix a statement about
  economics (no dispersion means no ratio), not about the implementation. A cent-rounded NAV (real
  quantisation dispersion) is the control on the other side of the floor, so widening the floor goes
  red. Because every fixed site then emits only an output shape it already emits for an exactly
  constant input, no new shape reaches any TS reader.

### Recompute half

- **D-08 (2026-09-25, orchestrator, OQ2; reversible per row: a later natural derive supersedes it).**
  Single-key broker strategies recompute through `compute_analytics_from_csv` ALONE, from the stored
  `csv_daily_returns`: no venue call, the input held fixed, so the before/after comparison is
  meaningful. Recorded residual: the `metrics_json_by_basis` mark-to-market PSR on those rows stays
  pre-fix until the strategy's next natural derive (persisted-only, no reader under `src/`).
- **D-09 (2026-09-25, orchestrator, OQ3; reversible).** A composite in the set (a strategy with any
  `strategy_keys` row) is recomputed ONLY through `stitch_composite`, never `compute_analytics_from_csv`
  (which NULLs `metrics_json_by_basis` on a composite). A composite with an untrusted member key
  (`sync_status` in `revoked` / `sign_in_failed`, or `disconnected_at` set) is not enqueued; it is a
  recorded residual.
- **D-10 (2026-09-25, orchestrator; one-way per enqueue, so every enqueue is a founder step).** No
  agent runs SQL against PROD, or any remote database. Every PROD statement is a founder step written
  out verbatim in plan 02, read-only except the enqueue. The database marker query from CLAUDE.md
  runs first. One tracer strategy is enqueued and watched to a terminal status with the full
  before/after check before any other; the rest go one at a time. Each enqueue carries the metadata
  `source` label `qstats-recompute-166.1`. The protected `ledger-refresh` marker is never used.
  Precondition: the Railway worker runs a commit at or after the 166 merge `bb31564f9`.
- **D-11 (2026-09-25, researcher, ratified; not reversible without rewriting an audit record, which is
  the reason it is not done).** `strategy_verifications.metrics_snapshot` is a point-in-time record of
  a verification run. A residue Sharpe captured there before the fix is historical and is NOT
  recomputed.
- **D-12 (2026-09-25, orchestrator; reversible).** The repo is public. `.planning/` records counts and
  anonymised row labels (R1..Rn) only, never strategy ids, names, key labels or metric values of
  private strategies. The founder keeps the before-snapshot locally.
- **D-13 (2026-09-25, orchestrator; reversible).** No migration. The recompute uses the existing
  `enqueue_compute_job` RPC and the existing worker.
- **D-14 (2026-09-25, planner discretion; reversible).** The release plan depends on the code plan
  only. The recompute is PROD operations and ships nothing, so the CHANGELOG entry records plan 02's
  state at release time, counts only, and does not block on it.
  ⭐ **Amended 2026-09-25 (plan-check revision).** "The code plan" is now five plans: 01 (Python
  variance sites), 03 (Python correlation sites), 04, 05 and 06 (TS sites, D-15). The release plan is
  `166.1-07` and depends on all five. It still does not depend on plan 02.

### Plan-check revision (2026-09-25)

- **D-15 (2026-09-25, ORCHESTRATOR DECISION, founder rule "close the whole class, not point-fixes";
  reversible per site).** The TS sites that compute a ratio over a daily-return dispersion behind a
  `> 0` / `!== 0` guard, or behind a relative-only floor, are IN SCOPE. The plan checker named four
  files (`holding-compare-adapter.ts`, `sample-basis-ratios.ts`, the Sharpe and correlation in
  `scenario.ts`, `diversification.ts`). The planner's re-grep found the rest of the class, listed in
  RESEARCH `### A5`. Every TS site gets the SAME floor as the Python helper, from ONE new TS module
  (`src/lib/dispersion-floor.ts`: `1e-12 * max(1, |mean|)`, NaN answers "not residue" and "not real"),
  a red test built from a compounding-NAV constant yield written first, and a neuter, RED, restore
  drill. The D-07 invariant holds in TS too: each fixed site emits exactly what it emits today for an
  exactly constant input at the same field (null, 0 or NaN, whichever that site already uses), so no
  renderer changes. Three TS plans, file-disjoint: 04 (the module plus the four named files), 05
  (correlation helpers, `portfolio-stats.ts`, the two scenario floors), 06 (`src/lib/factsheet/`).
  ⚠️ Coupling: `holding-compare-adapter.ts` is also edited on the unmerged `feat/167.1.2-hide` branch
  (it exports `reconstructAndAnalyze`). Plan 04 fetches `origin/main` first and rebases over that
  change if it merged; if it has not merged, plan 04 adds no export of its own.
  ⭐ **Amended 2026-09-25 (revision after the founder's D-17 direction; the sentence above is kept as
  lineage and is superseded).** PR A (#859, `feat(167.1.2): PR A`) is MERGED on `origin/main`, and the
  orchestrator merged `origin/main` into `feat/166.1` at `7bfce8490` before wave 1. Measured on this
  branch: `reconstructAndAnalyze` is `export function`, marked "@internal Exported for unit testing
  only (Phase 167.1.2 / D-13)", and the item withholds its analytics while
  `HOLDING_COMPARE_HISTORY_STATE` is `"rebuilding"`. So plan 04 has no route decision and no stop: it
  tests `reconstructAndAnalyze` directly and adds no export. The remaining coupling on that file is
  167.1.2 plan 11 (it decides the `"ready"` flip and edits the same file); see D-17's coupling table.
  The file split in the paragraph above ("04 the module plus the four named files, 05 correlation
  helpers ..., 06 `src/lib/factsheet/`") is superseded by D-17.
- **D-16 (2026-09-25, plan checker finding B1, ratified by the orchestrator; reversible: one
  conditional).** `analytics_runner._compute_derived_trade_metrics`'s SQN block is site S8 of the
  variance class. Its guard is `var_r > 0` then `std_r > 0` over per-trade R-multiples. Measured
  2026-09-25 with the main checkout's venv: 21 identical losses of 7.7 give SQN -4.03e16, 37 give
  -5.40e16, while 21 identical losses of 1.0 give None (the sum is exact). SQN is rendered in
  `TradeAndPositionPanel`. It moves to `dispersion_is_residue(std_r, mean_r)`. The re-sweep that found
  it, and every hit it recorded as not in the class, is RESEARCH `### A2` addendum.

### Founder direction (2026-09-25): compute once, read it everywhere

- **D-17 (2026-09-25, FOUNDER DIRECTION, verbatim: "Why don't you calculate Sharpe once and the 20
  places all read it from there?"; relayed by the orchestrator; reversible per site: each site's edit
  is one function body).** The TS half (T1-T20) is restructured around computing each figure ONCE,
  instead of pasting the floor guard into twenty local copies of the same formula. D-15's "every TS
  site gets the SAME floor from ONE module" stands; what changes is that the per-site FORMULAS go too.
  Every site is classified, from the code at `7bfce8490`, into:
  - **Tier 1**: the site recomputes a figure the analytics service already persists for the SAME
    series (`strategy_analytics` columns, `metrics_json`, `portfolio_analytics`). Remedy: delete the
    local computation and read the persisted value; a null renders the existing honest absence. No
    floor guard is needed there, because the Python side already has it (Phase 166, plans 01 and 03).
  - **Tier 2**: the series exists only on the TS side at that moment (a what-if blend, a user's pick
    of strategies, a resample, a per-key book the persisted rows do not describe, a comparator over its
    covered span). Remedy: ONE shared module that every Tier-2 site calls, and the per-site formula is
    removed.
  - **Dead**: the function has no production caller (measured by grep over `src/`, tests excluded).
    Remedy: delete it with its tests. A formula nobody reads is a second copy waiting to be reused.

  **The shared module (planner discretion within D-17, recorded).** `src/lib/return-stats.ts`, a new
  file, holds Phase 166's floor (`DISPERSION_RESIDUE_REL`, `residueFloor`, `dispersionIsResidue`,
  `dispersionIsReal`, the Python rule verbatim and pinned to the Python constant by a test) AND the
  ratio functions every Tier-2 site calls: `dispersion(xs, ddof)` returning the mean and an sd that is
  exactly 0 when the dispersion is residue (the D-07 invariant stated once: no dispersion means the
  value an exact constant gives), `sharpe`, `pearson` and `beta`. It builds on the repo's existing
  primitives `mean` and `stdDev` in `src/lib/portfolio-math-utils.ts` rather than adding a third mean,
  so there is one mean, one sd, one Sharpe, one correlation and one beta. The name is not
  `dispersion-floor.ts` (the orchestrator's example) because the module now holds the ratios, not only
  the floor. The arithmetic order of each shared function is the one the factsheet already uses
  (`compute.ts` for the Sharpe; the sums form of `build-payload.ts` `pearsonCorr` for the
  correlation), so the factsheet snapshot, which rounds every number to 12 significant digits, cannot
  move; a site whose unrounded value moves in a pinned test is a STOP-and-report, never a snapshot or
  golden update.
  ⚠️ **Two arithmetic consequences, recorded 2026-09-26 (plan-check round 3, I3).** (a) `jointMetrics`'
  information ratio moves to `sharpe(active)` over `active[i] = rets[i] - bench[i]`, and its tracking
  error to `dispersion(active, 0).sd * Math.sqrt(periodsPerYear)`. Today both are built from
  `mean(rets) - mean(bench)`; the shared path takes `mean(active)`, a DIFFERENT summation, so the
  values agree to rounding, not bitwise. The snapshot rounds to 12 significant digits, and plan 06b's
  test `T16 info_ratio and tracking_error move to the active series` pins the noisy-input values to
  12 significant digits of today's formula (a larger move is a STOP). (b) `sharpe()` returns null on
  any non-finite input. Every adopting site answers null with the value it emits today for a
  degenerate input, so `compute` still reports a Sharpe of 0 for a series holding a NaN (today's
  `s > 0` is false on a NaN `s`, which also gives 0), and `computeOgHeadline` still reports NaN. Plan
  06a's test `T13 non-finite input keeps today's sharpe` pins the `compute` case, computed against a
  series holding one NaN, so a later change to the null mapping goes red.

  **The classification (measured 2026-09-25 at `7bfce8490`; owner = the plan that edits the site).**

  | # | Site (by symbol) | Series at that moment | Persisted twin for the same series? | Tier | Owner | Remedy |
  |---|---|---|---|---|---|---|
  | T1 | `holding-compare-adapter.ts` `reconstructAndAnalyze` Sharpe | one holding's `allocator_equity_snapshots.breakdown` levels | none | 2 | 166.1-04 | shared `sharpe` (population, 365). Withheld today (`"rebuilding"`, 167.1.2 D-13); fixed for the `"ready"` branch 167.1.2 plan 11 decides |
  | T2 | `sample-basis-ratios.ts` `sampleBasisRatios` Sharpe | the composer's own book from live equity levels | none (`portfolio_analytics` describes the legacy portfolio-strategies set, 169 plan 08's premise) | 2 | 166.1-04 | shared `sharpe` (sample). Its "standalone replica of the frozen engine" rationale ends: T2 and T3 call one function |
  | T3 | `scenario.ts` `computeScenario` Sharpe | the what-if blend | none | 2 | 166.1-04 | shared `sharpe` (sample) |
  | T4 | `scenario.ts` `computeScenario` correlation matrix | the what-if constituents | none | 2 | 166.1-04 | shared `pearson`, null answered as today's 0 |
  | T5 | `diversification.ts` `diversificationRatio` | what-if weights over a covariance matrix | none | 2 | 166.1-04 | shared `dispersionIsResidue(sigmaP, 0)` (a ratio of sigmas, no second formula to consolidate) |
  | T6 | `correlation-math.ts` `pearson` | `/compare` picks (`CompareCorrelationMatrix`) | none | 2 | 166.1-05 | `pearson` MOVES into `return-stats.ts` (plan 04 writes it in this sums form); `correlation-math.ts` is deleted and its one importer re-pointed |
  | T6b | `correlation-math.ts` `rollingCorrelation` | none: 0 production callers | — | Dead | 166.1-05 | delete. Its only consumer, `CorrelationWithBenchmark`, already reads the PERSISTED `btc_rolling_correlation_90d` (audit G11.A P64 deleted the client fallback): the Tier-1 precedent in this repo |
  | T7 | `CorrelationMatrix.tsx` local `pearson` | the Risk tab's per-key set (169 plan 08 drops the precomputed legacy matrix) | the persisted matrix describes a different set | 2 | 166.1-05 | local function deleted; shared `pearson`, null answered as today's 0 |
  | T8 | `portfolio-stats.ts` `computeRollingMetric` | none: 0 production callers | — | Dead | 166.1-05 | delete with its describe block (`portfolio-stats.ts` itself stays: it is on `scenario-backbone-gates.test.ts`'s SC-3 keep-list) |
  | T9 | `portfolio-stats.ts` `computeAlphaBeta` beta | the scenario vs BTC (`scenario-benchmark.ts`); `AlphaBetaDecomposition` moves to `jointMetrics` under 169 plan 11 | none | 2 | 166.1-05 | shared `beta`, null answered as today's 0 |
  | T10 | `portfolio-stats.ts` `computeRiskDecomposition` | the Risk tab's per-key covariance (169 plan 08) | `portfolio_analytics.risk_decomposition`, legacy set | 2 | 166.1-05 | shared `dispersionIsResidue(sqrt(totalVariance), 0)` |
  | T11 | `scenario-benchmark.ts` `computeScenarioBenchmark` | the what-if vs BTC | none | 2 | 166.1-05 | the hand-written relative floors become the shared predicate; its own correlation becomes shared `pearson` |
  | T12 | `scenario-stress.ts` `computeScenarioStress` | the what-if series | none | 2 | 166.1-05 | shared predicate |
  | T13 | `factsheet/compute.ts` `compute` Sharpe, skew, kurtosis, `ann_vol` | the builder shared by the strategy, allocator-portfolio and scenario payloads and the levered browser re-derive | strategy arm only | 2 for the function; the strategy HEADLINE read is Tier 1 | function: 166.1-06; headline read: **169 plan 04** | `dispersion` + shared `sharpe`; skew and kurtosis keep their `s > 0` gate over the snapped sd |
  | T14 | `factsheet/og-metrics.ts` `computeOgHeadline` Sharpe | the strategy series | yes (`strategy_analytics.sharpe`) when rankable | 1 for the rankable read; 2 for the arm that still computes (amended by D-19) | persisted read: **169 plan 04**; computed arm: **166.1-06** (D-19) | 169 plan 04 reads the persisted value when the row is rankable; 166.1-06 replaces the local `s > 0` Sharpe the function still computes otherwise with shared `sharpe`, null answered as today's NaN (hide) |
  | T15 | `factsheet/bootstrap.ts` `headlineStats` | resampled series | none | 2 | 166.1-06 | shared `sharpe`, null answered as today's 0 |
  | T16 | `factsheet/joint.ts` `jointMetrics` beta, correlation, information ratio | strategy vs any of five comparators over the covered span (169 D-09), and the portfolio and scenario payloads | persisted BTC beta/correlation only, over a different span | 2 | 166.1-06 | shared `beta`, `pearson`, and `sharpe` over the active series; local `mean`/`pstdev` removed |
  | T17 | `factsheet/rolling.ts` `rollingBeta`, `rollingSharpe` | rolling windows of every payload series and comparator | strategy arm only (`rolling_metrics.sharpe_30d/90d/365d`, a different day basis) | 2 | 166.1-06 | shared `beta`, `sharpe`; local `mean`/`pstdev` removed |
  | T18 | `factsheet/comparator-block.ts` vol-match scale | a comparator's `ann_vol` from `compute` | none | 2, fixed AT ITS SOURCE | 166.1-06 (test only) | `compute`'s snapped `ann_vol` is 0 on residue, so the existing `ann_vol > 0` guard is right. `comparator-block.ts` is NOT edited (169 plans 05a, 05b and 07 own it) |
  | T19 | `factsheet/build-payload.ts` `pearsonCorr` | strategy vs benchmark list, and the correlation matrix | none per pair | 2 | 166.1-06 | the local body is replaced by shared `pearson` (null answered as today's NaN); call sites untouched |
  | T20 | `factsheet/allocator.ts` `buildAllocatorMetrics` correlation | blends | none | 2 | 166.1-06 | shared `pearson`, null answered as today's 0 |

  **Split with Phase 169 PAGETRUTH (measured 2026-09-25 against `feat/169-pagetruth` at `cf0cb2bdb`).**
  169's phase boundary is the same principle ("each contradiction is fixed at ONE source of truth,
  never patched per page"); the founder's sentence itself is not in 169's CONTEXT at that sha
  (grep: 0 hits), so this row set rests on 169's plans, not on a quoted decision.
  - OWNED BY 169, excluded here: the factsheet KPI headline CAGR and Sharpe for single-key strategies
    (169 plan 04, `readSingleKeyBasisOpts` feeding `overlayBasisScalars`); the OG card's Sharpe and
    CAGR (169 plan 04, `computeOgHeadline` + the OG route); the discovery / recommendations /
    my-strategies lists (already persisted); `AlphaBetaDecomposition` moving onto `jointMetrics`
    (169 plan 11); the Risk tab reading the per-key book (169 plan 08); the discovery builder
    consolidation (169 D-23). 166.1 edits none of `composite-read-path.ts`,
    `fetch-and-build-payload.ts`, `comparator-block.ts`, `types.ts`, `AlphaBetaDecomposition.tsx`,
    `RiskTabPanel.tsx`, `book-risk-input.ts`, `og-metrics.test.ts`, the OG route or the discovery page.
    (Amended by D-19: `og-metrics.ts` left this list; its computed Sharpe is 166.1-06's.)
  - SAME FILE, DIFFERENT EDIT (text-disjoint hunks; whichever phase merges second rebases and re-runs
    its own site tests): `factsheet/compute.ts` (169 plan 06 edits the return windows; 166.1-06 edits
    the Sharpe / skew / kurtosis / `ann_vol` lines), `factsheet/build-payload.ts` (169 plans 05a and
    05b edit comparator coverage and 167.2.1 plan 01 the resolve stage; 166.1-06 replaces only the
    body of `pearsonCorr`), and, since D-19, `factsheet/og-metrics.ts` (169 plan 04 adds the
    persisted-scalars branch; 166.1-06 replaces the computed Sharpe lines only).
  - Recorded for the orchestrator, NOT acted on here (each is a single-source question on a 169-owned
    read path; after 166.1 none of them can show a residue number, because the TS computation behind
    it calls the shared, floored function): (a) [SUPERSEDED by D-19: planned in 166.1-06, no longer a
    second-merger rule] the non-rankable fallback in 169 plan 04's
    `computeOgHeadline` still computes a Sharpe locally; whichever of 166.1 and 169 merges second
    points that fallback at `return-stats.sharpe`, mirroring 169 D-22's second-merger rule;
    (b) the strategy arm's factsheet skew and kurtosis could read `metrics_json.skewness` /
    `kurtosis`, and (c) its rolling Sharpe could read `rolling_metrics.sharpe_*`; both readers sit in
    `build-payload.ts` / the 169 overlay path, and (c) changes the day basis. Whether 169 takes (b)
    and (c) is 169's call.

  **Other couplings (text-disjoint; second merger rebases).** `src/lib/scenario.ts`: 167.1.2 plan 06
  edits `computeScenario`'s `totalWeight` (branch `feat/167.1.2-accounttruth`); 166.1-04 edits its
  Sharpe and correlation lines. `holding-compare-adapter.ts`: 167.1.2 plan 11 decides the `"ready"`
  flip; 166.1-04 edits the Sharpe line of `reconstructAndAnalyze` only. The `scenario.ts` freeze
  (SCENARIO-05) is RETIRED (the phase-29 to phase-32 guards say so, v1.5 ADR-001); `scenario.ts` is
  protected by `scenario.test.ts`'s pins, which must stay green unedited.

  **Guard for the principle itself.** Plan 07 adds a source-scan gate in the style of
  `src/lib/scenario-backbone-gates.test.ts` (Phase 108, which deleted a "second Sharpe" module and
  pinned its absence): every Tier-2 site file imports `@/lib/return-stats`, the retired local
  formulas and dead functions are absent from live code, and a liveness fixture proves the matcher
  fires. A second copy of a formula then fails CI instead of waiting for a reviewer.

### Plan-check round 2 (2026-09-25)

- **D-18 (2026-09-25, planner, applying the checker's findings; reversible).** BLOCKER: plan 01 Task 3
  hands pytest a literal list of the four `tests/test_*.py` files that call
  `_compute_derived_trade_metrics`, and asserts that a `--include='test_*.py'` grep still finds
  exactly those four. Measured: the unfiltered grep also returns `tests/fixtures/regen_golden.py`, a
  regeneration script, which pytest would import as a test module when named explicitly. W2: plan 07
  Task 1 carries automated verifies for ruff (no NEW finding against the merge-base, over the union of
  plans 01 and 03's Python files, named literally) and eslint (over the union of plans 04-06's TS
  files, named literally). W3 (optional split of plan 01 Task 2 into S2-S4 / S5-S7) is DECLINED:
  splitting inside the plan adds a fourth task without lowering the plan's cost, and moving S5-S7 to
  a new plan would share `tests/test_dispersion_floor_sites.py` with plan 01 and force a sequential
  wave for no reduction in any single task's context. Info: plan 07 computes the version at ship time
  from the then-current `origin/main` (0.92.0.0 at this revision; 164.9.1 is expected to take
  0.93.0.0), never from a number written in the plan.

### Respawned revision (2026-09-26)

- **D-19 (2026-09-26, planner, finishing the D-17 revision after an outage; reversible per site).**
  Cross-checked against Phase 169 at `feat/169-pagetruth` `b1f165935`, whose CONTEXT now carries the
  founder principle as its own **D-25** ("calculate Sharpe once; every page reads it"). The paragraph
  above that measured "0 hits" at `cf0cb2bdb` is kept as lineage; it is stale at `b1f165935`.
  - **The og-metrics gap, closed.** 169 D-25 lists `og-metrics.ts` and `comparator-block.ts` among the
    files it leaves to "166.1 plan 06" (its reading of this branch at `a7240515d`), while D-17 above
    had excluded `og-metrics.ts` as 169's. Each phase disclaimed the arm of `computeOgHeadline` that
    still COMPUTES a Sharpe (169 plan 04 step 2: "Otherwise compute exactly as today"), and that arm is
    an in-class site (`s > 0` over a population sd, measured). With both disclaiming it, it had no
    owner. Resolution, respecting 169 D-25's split: 169 plan 04 keeps the persisted read (Tier 1);
    166.1-06 Task 1 replaces the computed arm's variance, sd and Sharpe lines with shared `sharpe`,
    null answered as NaN (the card's existing hide value), and does not edit `og-metrics.test.ts`
    (169 plan 04's file). Different edits in one function; the second merger rebases. Plan 07's gate
    adds `og-metrics.ts` to the import list (16 site files).
  - **`comparator-block.ts`:** 169 D-25 names it for 166.1-06; D-17's T18 fixes it at its source
    (`compute`'s `ann_vol`) with no edit. Compatible: no two edits on that file. Recorded for the
    orchestrator so 169's list can drop it; not acted on here (169's file).
  - **Zoom-window KPIs:** the orchestrator relayed that 169 now plans zoom-window KPIs through
    `compute()`. Measured 2026-09-26: no 169 plan, CONTEXT or ROADMAP line at `b1f165935` mentions a
    zoom window (grep over `.planning/phases/169-pagetruth`: 0 files). If it lands through
    `compute()`, it inherits 166.1-06's floored `compute` with no further 166.1 edit.
    ⛔ **CORRECTED 2026-09-26 (plan-check round 3, I1); the two sentences above are kept as
    lineage.** At `feat/169-pagetruth` `7f4dc1316`, Phase 169 HAS a zoom plan: **169 plan 14
    ZOOMKPIS**. It derives the zoom-window KPIs through `deriveSeriesBundle`, i.e. `compute()` and
    `jointMetrics` on the slice, with no new formula. Its `files_modified` (`basis-context.tsx`,
    `FactsheetView.tsx`, `MetricsColumn.tsx`, `AnalyticalPanels.tsx` and three new tests under
    `src/app/factsheet/[id]/v2/`) share NO file with any 166.1 plan. It inherits the floored
    `compute` (166.1-06a) and the floored `jointMetrics` (166.1-06b) with no 166.1 edit.
  - **D-18 corrections:** the ruff and eslint verifies are in plan 07 **Task 2** (Task 1 is the D-17
    gate), and the eslint union is plans 04-07's TS files. The Info line's version numbers are lineage;
    at this revision `origin/main` reads `0.93.0.0` (164.9.1 merged) and the branch `0.92.0.0`, and
    plan 07 still computes the bump at ship time from then-current `origin/main`.
  - **Plan 04's former STOP-and-rebase step** is absent (PR A merged at `7bfce8490`, the D-15
    amendment above); nothing in plans 04-07 waits on `feat/167.1.2-hide`.

### Plan-check round 3 (2026-09-26, the phase will be EXECUTED)

- **D-20 (2026-09-26, planner, applying the round-3 checker findings; reversible per plan).** The
  founder lifted the planning-only stop on 2026-09-26, so the plans were made execution-ready.
  - **B1, the compute-once gate is widened (plan 07 Task 1).** Three layers, not one: (1) the 16-file
    import rule; (2) EVERY retired expression the plans' acceptance criteria grep for, lifted into the
    gate as a `{ file, token, plan }` table (28 pairs, each measured present at `d1bd9f50f`), so a
    retired copy that comes back fails CI rather than only a one-time executor grep; (3) a WHOLE-TREE
    shape matcher over live code under `src/` (tests, `__tests__` and fixtures excluded) for the
    annualised-ratio (Sharpe / information ratio), Pearson and beta shapes anywhere outside
    `src/lib/return-stats.ts`. Measured with a prototype of those shapes at `d1bd9f50f`: 25 hits, the
    20 in-class sites plus 5 Sortino lines and 1 Treynor line, 0 false positives. The allowlist is
    explicit, commented and COUNT-PINNED (a stale entry fails): Sortino (a downside-deviation
    denominator, not a dispersion ratio; RESEARCH A5) and Treynor (divides by beta, not a dispersion).
    Annualised volatility and tracking error are a dispersion times `Math.sqrt(N)`, not a ratio, so the
    shapes do not match them by design, and negative liveness fixtures prove it. A liveness fixture
    proves a Sharpe in a NEW file trips the matcher, and the gate is neuter-drilled four ways.
    Consequence for plan 05b: `computeScenarioBenchmark`'s information ratio,
    `(excessMean * periodsPerYear) / te`, is a Sharpe over the excess series (`te` is
    `stdDev(diff, true) * Math.sqrt(periodsPerYear)`), so it moves to `sharpe(diff, { periodsPerYear,
    ddof: 1 })`, which is the same arithmetic in the same order; leaving it local would have left a
    twenty-first copy the matcher catches.
  - **B2, the release sweep checks PENDING plans only.** The sweep ran the anchor verifier over every
    166.1 plan, including executed plans whose anchors this phase deletes (`correlation-math.ts`
    `pearson`, `compute.ts` `pstdev`), which would exit 1 forever. It now runs
    `node scripts/verify-plan-anchors.mjs --pending`, the exact form CI's `plan-anchor-verify` runs.
  - **B3, T_DEPLOY is the go-live of the FIRST fixed worker deployment (plan 02 Task 1 Step 1a).** The
    active deployment's time is the wrong value: a row computed between the first fixed deployment
    and a later redeploy would be classed pre-fix and recomputed for nothing, and a row computed on a
    pre-166 worker after a rollback would be missed. T_DEPLOY is read from the worker's DEPLOYMENT
    HISTORY: the go-live time of the earliest successful deployment whose commit descends from
    `bb31564f9`, after the last deployment that does not (so every row computed at or after T_DEPLOY
    ran fixed code). Each listed sha's ancestry is checked with `git merge-base --is-ancestor`
    (RESEARCH B4). Where the history shows when the preceding unfixed deployment was removed, T_DEPLOY
    is the later of that time and the go-live, because an old worker can finish an in-flight job after
    the new one goes live.
  - **W1, merge `origin/main` before the sweep (plan 07 Task 2).** Phase 169 PR 1 (`compute.ts`,
    `build-payload.ts`, the build-payload snapshot, `og-metrics.ts` `computeOgHeadline`; 169 CONTEXT
    D-25 as corrected 2026-09-26 at `7f4dc1316`) and 167.1.2 plans 06 and 11 (`scenario.ts`,
    `holding-compare-adapter.ts`) may land on `main` first. Plan 07 merges `origin/main`, re-runs the
    T1-T20 site tests and the gate on the merged tree, and records the merged base sha, before plan
    08's sweep.
  - **W2, budget: plans 05 and 06 split, plan 07 split, plan 04 accepted.** At 110k and 120k tokens
    (1.1x and 1.2x the 100k smart-zone budget) plans 05 and 06 are split into file-disjoint halves
    that stay in wave 2: **05a** (T6-T7, one Pearson), **05b** (T8-T12, `portfolio-stats.ts` and the
    two scenario libraries), **06a** (T13, T14's computed arm, T15, T18: the headline family),
    **06b** (T16, T17, T19, T20: benchmark, rolling and correlation). 06b gets its own site-test file
    (`src/lib/factsheet/return-stats.sites-joint.test.ts`) so the halves share no file. Letter
    suffixes (the Phase 169 `05a` / `05b` precedent) keep Phase 169 D-25's citations of "166.1 plan
    05" and "166.1 plan 06" pointing at the right families. The user's suggested 05 split (T6-T8 /
    T9-T12) was NOT taken: T8 and T9-T10 share `portfolio-stats.ts`, which would force a third wave.
    Plan 07 (gate, merge, sweep, release: four tasks) is split into **07** (the gate and the merge,
    wave 3) and **08** (the sweep and the release commit, wave 4). **Plan 04 stays whole at 105k
    (1.05x), accepted:** Task 1 fixes the `return-stats.ts` API every later plan reads, Task 2 is four
    small site swaps, and a split would move T2-T5 into wave 2 with a second site-test file and split
    the module-level drill (zero `DISPERSION_RESIDUE_REL`, T1-T5 go red) across two plans.
  - **I2:** plan 05a owns `src/lib/diversification.ts`'s module doc sentence naming
    `correlation-math.ts::pearson` (measured: `diversification.test.ts` does not call it, so the
    sentence is already false). 05a runs after plan 04 (wave 2 after wave 1), so the file is not
    shared within a wave.
  - **I4:** no change. Tier 1 belongs to 169 plan 04.

### Claude's Discretion

- Test file layout (`tests/dispersion_fixtures.py` shared helper; one site-test file for the variance
  sites and one for the correlation sites).
- The exact wording of the D-04 sentinel message, within: no number, names the reason.

</decisions>

<canonical_refs>
## Canonical References

- `.planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md` - site inventory, SQL pack, pitfalls
- `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-CONTEXT.md` - OPEN-2 answer
- `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-09-SUMMARY.md` - the 166 D-10 before/after table and census SQL
- `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-REVIEW-FIX-R2.md` - the floor's origin (CR-01)
- `docs/runbooks/ledger-refresh-go-live.md` - the repair-enqueue precedent
- `analytics-service/services/metrics.py` - `_residue_floor`, `_rolling_correlation`, the headline `correlation` guard

</canonical_refs>

<deferred>
## Deferred Ideas

- A full `derive_broker_dailies` for single-key broker rows to refresh the by-basis PSR now (D-08
  chose hop 2 alone; the next natural derive refreshes it).
- Recomputing `strategy_verifications.metrics_snapshot` (D-11: never).

</deferred>

---

*Phase: 166.1-qstatsrecompute*
