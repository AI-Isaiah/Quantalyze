---
phase: 166.1-qstatsrecompute
plan: 06b
type: execute
wave: 2
depends_on: ["166.1-04"]
files_modified:
  - src/lib/factsheet/joint.ts
  - src/lib/factsheet/rolling.ts
  - src/lib/factsheet/build-payload.ts
  - src/lib/factsheet/allocator.ts
  - src/lib/factsheet/return-stats.sites-joint.test.ts
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The factsheet builder computes beta, correlation, the information ratio and the rolling Sharpe through return-stats and carries no private mean, pstdev or Pearson of its own in joint.ts, rolling.ts or build-payload.ts (D-17)"
    - "On a constant-yield leg, jointMetrics' beta, correlation and information ratio, rollingBeta and rollingSharpe are exactly what an all-zero leg gives today (D-07, D-17)"
    - "jointMetrics' information ratio is sharpe(active) and its tracking error dispersion(active, 0).sd * sqrt(periodsPerYear); on a noisy input both agree with today's formula to 12 significant digits (a different mean path, not bitwise), pinned by the test 'T16 info_ratio and tracking_error move to the active series' (D-17 I3)"
    - "build-payload's benchmark correlations and correlation matrix report NaN, and buildAllocatorMetrics' correlation 0, for a constant-yield leg, exactly as for an all-zero leg (D-07, D-17)"
    - "A cent-rounded 1% APY NAV still gives a finite beta and correlation at the sites with that control, so widening the floor goes red (D-07)"
    - "Every T16, T17, T19 and T20 test was written first and observed RED on the unedited site, every edited site was neutered, observed RED and restored from a byte backup proven with cmp, and the existing factsheet snapshot and golden tests pass without an update"
  artifacts:
    - path: src/lib/factsheet/return-stats.sites-joint.test.ts
      provides: "red tests for T16, T17, T19 and T20"
  key_links:
    - from: src/lib/factsheet/joint.ts
      to: src/lib/return-stats.ts
      via: "beta, pearson and sharpe over the active series"
      pattern: "@/lib/return-stats"
    - from: src/lib/factsheet/rolling.ts
      to: src/lib/return-stats.ts
      via: "each window's beta and Sharpe"
      pattern: "@/lib/return-stats"
    - from: src/lib/factsheet/build-payload.ts
      to: src/lib/return-stats.ts
      via: "pearsonCorr's body is the shared pearson"
      pattern: "@/lib/return-stats"
---

<objective>
Third slice of D-17 (compute once), second half (D-20 W2 split): the factsheet builder's benchmark,
rolling and correlation sites (CONTEXT D-17 rows T16, T17, T19, T20) stop carrying their own mean,
standard deviation, Sharpe, correlation and beta and call plan 04's `src/lib/return-stats.ts`.

Purpose: `joint.ts` and `rolling.ts` each carry a private `mean` / `pstdev`, and `joint.ts`,
`build-payload.ts` and `allocator.ts` each carry a Pearson form. On a constant-yield leg each shows a
residue beta, correlation or information ratio. Plan 06a takes the headline family.
Output: four edited files and one test file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md
@.planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md
Also read `.planning/phases/166.1-qstatsrecompute/166.1-04-SUMMARY.md` (the final `return-stats.ts` API at
its top, and the fixture's exports). This plan does NOT edit `src/lib/return-stats.ts` or plan 04's test
files. Plans 05a, 05b and 06a run in the same wave and share no file with this plan (06a's site tests live
in `src/lib/factsheet/return-stats.sites.test.ts`; this plan's in `return-stats.sites-joint.test.ts`). An
API gap is a STOP-and-report.

**Environment, drill rule, the D-07 invariant in TS and the never-edit-a-golden rule** are exactly plan
04's: MAIN checkout's `node_modules` linked only for the run and removed before every commit; exit 127 is
a FAIL; `tsc` in the background with a 600 s watchdog; byte backups under a `166.1-06b` scratchpad name,
neuter, RED, `cp`, `cmp`; each test computes the site's output on the constant yield AND on an all-zero
series and asserts the field identical (`Object.is` for NaN). Never `git checkout --`, `git restore`,
`git stash`. Never quote a retired expression verbatim in an updated code comment: this plan's acceptance greps count comment lines. Never update a vitest snapshot to make a test pass: a snapshot diff is a finding to report.
The snapshot rounds every finite number to 12 significant digits, and `return-stats`' `pearson` is
`pearsonCorr`'s own sums form.

**Ownership and couplings (CONTEXT D-17).** Phase 169 owns `comparator-block.ts`, `types.ts`,
`composite-read-path.ts` and `fetch-and-build-payload.ts`; this plan edits none of them.
`build-payload.ts` is shared with 169 plans 05a/05b and 167.2.1 plan 01 in DIFFERENT hunks (comparator
coverage and the resolve stage): this plan replaces only the BODY of `pearsonCorr` and adds one import
line, and leaves every call site as it is. Whichever phase merges second rebases; plan 07 Task 2 does
that for this phase. `src/app/factsheet/[id]/v2/basis-context.tsx` (the levered re-derive and 169 plan
14's zoom window) calls `jointMetrics`; read only.

**Which ddof.** Population (`ddof: 0`) in `joint.ts`, `rolling.ts`, `allocator.ts` and `build-payload.ts`.

**Symbols this plan edits (cite by symbol; line numbers drift).**
<!-- verify-symbol: src/lib/factsheet/joint.ts jointMetrics -->
<!-- verify-symbol: src/lib/factsheet/joint.ts pstdev -->
<!-- verify-symbol: src/lib/factsheet/rolling.ts rollingBeta -->
<!-- verify-symbol: src/lib/factsheet/rolling.ts rollingSharpe -->
<!-- verify-symbol: src/lib/factsheet/rolling.ts pstdev -->
<!-- verify-symbol: src/lib/factsheet/build-payload.ts pearsonCorr -->
<!-- verify-symbol: src/lib/factsheet/build-payload.ts buildFactsheetPayload -->
<!-- verify-symbol: src/lib/factsheet/allocator.ts buildAllocatorMetrics -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the benchmark family - jointMetrics end-to-end, rollingBeta and rollingSharpe - on return-stats, private mean/pstdev removed (T16, T17), red first</name>
  <files>src/lib/factsheet/joint.ts, src/lib/factsheet/rolling.ts, src/lib/factsheet/return-stats.sites-joint.test.ts</files>
  <read_first>
    - src/lib/factsheet/joint.ts - `jointMetrics`: beta (`varB > 0`), correlation (`s > 0 && sb > 0`), tracking error (`teSum` around `m - mb`) and information ratio (`trackingError > 0`); Treynor and up/down capture are NOT in the class (RESEARCH A5); the private `mean` / `pstdev`
    - src/lib/factsheet/rolling.ts - `rollingBeta` (`varB !== 0`), `rollingSharpe` (`s > 0`), and `rollingVol` / `rollingSortino` (NOT in the class, but they use the private `mean` / `pstdev` this task removes)
    - src/lib/factsheet/joint.test.ts, rolling.test.ts, src/lib/scenario-blend-adapter.test.ts (it pins `rolling.ts`'s POPULATION-std convention; must pass unedited), src/app/factsheet/[id]/v2/basis-context.tsx (the levered re-derive calls `jointMetrics`; read only)
  </read_first>
  <behavior>
    - T16: `jointMetrics(noise, navConstantYield(y))` gives `beta` and `corr` identical to `jointMetrics(noise, zeros)`; `jointMetrics(bench + navConstantYield(y), bench)`-shaped input (a constant active return) gives `info_ratio` and `tracking_error` identical to the zero-active case; Treynor, alpha, `r2` and captures on a noisy input are unchanged to 12 significant digits
    - T16 info_ratio and tracking_error move to the active series: on a noisy input, `info_ratio` equals today's `((m - mb) * periodsPerYear) / trackingError` and `tracking_error` today's `sqrt(teSum / n) * sqrt(periodsPerYear)`, both rebuilt in the test from the inputs, to 12 significant digits (a different mean path, so not bitwise; D-17 I3)
    - T17: `rollingBeta(noise, constantYieldBench, w)` equals `rollingBeta(noise, zerosBench, w)`; `rollingSharpe(navConstantYield(y), w)` equals `rollingSharpe(zeros, w)`; `rollingVol` and `rollingSortino` on a noisy series are bitwise unchanged
  </behavior>
  <action>
    Per D-17 and D-07. Step 0: `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-06b-PLAN.md`, halt on non-zero. Write the tests first in `src/lib/factsheet/return-stats.sites-joint.test.ts` and observe RED; record the ids (the 12-digit agreement case is GREEN on the old code by construction: record it as a pin). `jointMetrics`: beta is `beta(rets, bench) ?? 0`; `corr` is `pearson(rets, bench) ?? 0` (the sums form; the population and sums forms agree to rounding, and the snapshot rounds to 12 digits); the tracking error is `dispersion(active, 0).sd * Math.sqrt(periodsPerYear)` over `active[i] = rets[i] - bench[i]`, and the information ratio is `sharpe(active, { periodsPerYear, ddof: 0 }) ?? 0`; alpha keeps its formula over the new beta; `r2` stays `corr * corr`; Treynor keeps its formula (not in the class). `rollingBeta`: each window's value is `beta(winS, winB) ?? 0`; `rollingSharpe`: `sharpe(win, { periodsPerYear, ddof: 0 }) ?? 0`. Replace the private `mean` / `pstdev` in both files with `mean` / `stdDev(xs, false)` from `@/lib/portfolio-math-utils` and delete them. Drill T16 and T17 (restore each old expression, RED, `cp` restore, `cmp`).
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/factsheet/return-stats.sites-joint src/lib/factsheet/joint src/lib/factsheet/rolling src/lib/scenario-blend-adapter src/lib/factsheet/build-payload "src/app/factsheet"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, "No test files found", or any "obsolete" / "mismatched" snapshot line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '@/lib/return-stats' src/lib/factsheet/joint.ts src/lib/factsheet/rolling.ts` prints at least 1 for each file
    - `grep -c 'function pstdev\|function mean' src/lib/factsheet/joint.ts src/lib/factsheet/rolling.ts` prints 0 for each file
    - `grep -c 'varB !== 0 ? cov / varB' src/lib/factsheet/rolling.ts` prints 0, `grep -c 's > 0 ? (m \* periodsPerYear) / (s \* sqrtN)' src/lib/factsheet/rolling.ts` prints 0, and `grep -c 'varB > 0 ? cov / varB\|s > 0 && sb > 0\|trackingError > 0 ?' src/lib/factsheet/joint.ts` prints 0
    - the SUMMARY carries the T16-T17 RED ids, the 12-digit pin, and the drill rows
  </acceptance_criteria>
  <done>The factsheet benchmark and rolling numbers come from return-stats and equal the all-zero output on a constant yield, each proven by a test observed red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the two remaining correlations - build-payload's pearsonCorr body and buildAllocatorMetrics (T19, T20) - then the plan's vitest, tsc and eslint gates</name>
  <files>src/lib/factsheet/build-payload.ts, src/lib/factsheet/allocator.ts, src/lib/factsheet/return-stats.sites-joint.test.ts</files>
  <read_first>
    - src/lib/factsheet/build-payload.ts - `pearsonCorr` and its two callers inside `buildFactsheetPayload` (the benchmark-correlation list and the correlation matrix; NaN is the degenerate value); the import block
    - src/lib/factsheet/allocator.ts - `buildAllocatorMetrics` (`s`, `mmS`, `corr`, `annVol`, `mmAnnVol`)
    - src/lib/factsheet/build-payload.test.ts and its `__snapshots__` file; src/lib/factsheet/allocator.test.ts
  </read_first>
  <behavior>
    - T19: the correlation `buildFactsheetPayload` reports for a constant-yield strategy against a benchmark is identical (`Object.is`) to what an all-zero strategy gets (NaN); if the payload cannot be built in a test without a database fixture, test through the nearest exported caller and record which
    - T20: `buildAllocatorMetrics` correlation with a constant-yield leg equals the all-zero leg's (0); a noisy pair keeps its value to 12 significant digits
    - The existing `build-payload` snapshot test passes WITHOUT a snapshot update
  </behavior>
  <action>
    Per D-17 and D-07. Write the tests first and observe RED; record the ids. Replace the BODY of `pearsonCorr` with `return pearson(a, b) ?? NaN;` using `pearson` from `@/lib/return-stats` (one import line added; the function name and both call sites stay, so the hunk is disjoint from 169's and 167.2.1's edits in this file). In `buildAllocatorMetrics`, `corr` becomes `pearson(rets, mmRets) ?? 0`, and `s` / `mmS` come from `dispersion(..., 0).sd` so `annVol` and `mmAnnVol` are 0 on residue like an exact constant; keep the blend-vol arithmetic. Add no export. Drill T19 and T20 (restore each old expression, RED, `cp` restore, `cmp`). Then run the gates below, and record per site T16, T17, T19 and T20 that the fixed output is one the site already emits for an exact constant, so no renderer changes; if any would emit a new shape, STOP and report.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/factsheet src/lib/return-stats src/lib/scenario-blend-adapter "src/app/factsheet" "src/app/(dashboard)/allocations/widgets/performance" "src/app/(dashboard)/discovery"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, "No test files found", or any "obsolete" / "mismatched" snapshot line</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint src/lib/factsheet/joint.ts src/lib/factsheet/rolling.ts src/lib/factsheet/build-payload.ts src/lib/factsheet/allocator.ts src/lib/factsheet/return-stats.sites-joint.test.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), any tsc error line, or any eslint error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '@/lib/return-stats' src/lib/factsheet/build-payload.ts src/lib/factsheet/allocator.ts` prints at least 1 for each file
    - `grep -c 'denom > 0 ? cov / denom : NaN\|Math.sqrt(va \* vb)' src/lib/factsheet/build-payload.ts` prints 0 and `grep -c 's > 0 && mmS > 0 ?' src/lib/factsheet/allocator.ts` prints 0
    - `git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/factsheet/__snapshots__/ src/lib/factsheet/comparator-block.ts src/lib/factsheet/types.ts src/lib/factsheet/fetch-and-build-payload.ts src/lib/return-stats.ts` is empty for this plan's commits
    - the SUMMARY carries the T16-T20 drill table (T18 is 06a's) and the per-site "already emitted" line
    - `git diff --stat -- analytics-service/ supabase/` is empty for this plan's commits, and `test -L node_modules` is false at every commit
  </acceptance_criteria>
  <done>No beta, correlation, information ratio or rolling Sharpe in the factsheet builder is computed outside return-stats or over a residue dispersion, every edited site was drilled, snapshots are unchanged, and vitest, tsc and eslint are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dailies -> TS factsheet math -> public factsheet page, allocator overview, scenario preview | a fabricated beta, correlation or information ratio reaches a published factsheet or an allocator's book view |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.1-25b | Tampering (integrity of a displayed number) | T16, T17, T19, T20 | medium | mitigate | one shared module; red test per site; D-07 equality with the all-zero input; drill table |
| T-166.1-26b | Tampering (a regression hidden by a snapshot refresh) | build-payload snapshot | medium | mitigate | snapshot directory must be unchanged; a snapshot diff is reported, never accepted |
| T-166.1-27b | Denial of service (over-wide floor hides real data) | the shared floor at these sites | medium | mitigate | cent-rounded 1% APY controls go red if the floor widens |
| T-166.1-31b | Tampering (cross-phase clobber) | build-payload.ts shared with 169 / 167.2.1 | medium | mitigate | edit confined to `pearsonCorr`'s body and one import line; 169-owned files diff-gated to empty; plan 07 Task 2 merges origin/main and re-runs the site tests |
</threat_model>

<verification>
- `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-06b-PLAN.md` exits 0 before Task 1 starts.
- Both Task 2 verify commands exit 0.
</verification>

<success_criteria>
- D-17, third slice, second half: the factsheet builder's beta, correlation, information ratio and rolling Sharpe come from return-stats, each with a test observed red, snapshots unchanged.
</success_criteria>

<output>
Create `.planning/phases/166.1-qstatsrecompute/166.1-06b-SUMMARY.md` when done. Remove the node_modules
link, stage the SUMMARY, then run
`QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && test -x "$QZ_MAIN/node_modules/.bin/tsx" && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK before committing.
</output>
