---
phase: 166.1-qstatsrecompute
plan: 09
subsystem: analytics-service
status: complete
tags: [dispersion-floor, csv-validator, sharpe-sentinel, release, D-24]
requires:
  - phase: 166.1-01
    provides: services/dispersion.py floor, S1-S7 and the S3 residue branch
  - phase: 166.1-01b
    provides: S8 SQN on the floor
  - phase: 166.1-03
    provides: correlation helpers, C1-C8
provides:
  - D-24, a constant positive daily-returns CSV rejected at every length with one message
  - the whole-phase Python gate sweep at one SHA containing origin/main
  - release 0.97.0.0 (ENGINEFLOOR) with one unified CHANGELOG entry
affects: [166.2, 166.3]
tech-stack:
  added: []
  patterns: [red-test-first on the unedited source, neuter drills from byte backups, version derived from origin/main at release time]
key-files:
  created: []
  modified:
    - analytics-service/services/csv_validator.py
    - analytics-service/tests/test_csv_validator.py
    - VERSION
    - package.json
    - CHANGELOG.md
key-decisions:
  - "D-24 carried: _check_sharpe_sentinel drops its strictly-positive std gate; exact 0 and residue both go through dispersion_is_residue to the one existing message"
  - "Version 0.97.0.0, derived from origin/main 0.96.0.0 (minor bump on meaning: user-visible values change, including the D-24 CSV verdict)"
  - "Phase 166.3 recompute recorded as founder-gated, pending (its 01 SUMMARY does not exist)"
requirements-completed: [SC-3]
duration: ~20 min
completed: 2026-09-26
plan_head_before: 4bb47eac4cb1a83c181922c8039aee4ac4228bbc
actuals:
  tokens: 5770
  tasks: 3
  commits: 10
coverage:
  - deliverable: "D-24: a constant positive daily-returns CSV is rejected at every length, one message, constant zero keeps its verdict"
    human_judgment: false
    verification:
      - kind: test
        ref: "analytics-service/tests/test_csv_validator.py#test_d24_*"
        status: pass
      - kind: command
        ref: "Task 1 verify commands 1 and 2"
        status: pass
  - deliverable: "Whole-phase Python gate sweep at SWEEP_SHA 3a8f8ad01, containing origin/main"
    human_judgment: false
    verification:
      - kind: command
        ref: "Task 2 verify commands 1-3 (pytest + census + mypy; ruff no-new; ancestor + src/ + anchors + hygiene)"
        status: pass
  - deliverable: "Release commit 0.97.0.0 with the unified ENGINEFLOOR CHANGELOG entry"
    human_judgment: false
    verification:
      - kind: command
        ref: "Task 3 verify commands 1-4 (byte-equal version, CRITICAL-02 + check-version-bump, version-derivation, skip-token sweep)"
        status: pass
---

# Phase 166.1 Plan 09: D-24, the Python gate sweep and the ENGINEFLOOR release Summary

An exactly constant positive daily-returns CSV is now rejected by `_check_sharpe_sentinel` at every
length, with one message that names no number, and a constant zero series stays accepted. The
proof was red on the unedited sentinel before the fix. Every Python gate of the phase is green at
one SHA that contains `origin/main`, and one `chore(release)` commit moves VERSION and package.json
to 0.97.0.0 with a single ENGINEFLOOR CHANGELOG entry that maps every branch commit.

## Task 1 (tracer): D-24, red test first

**RED commit `a6007b3d4`** (test file only; `services/` untouched, HEAD at the run was `4bb47eac4`).
New tests in `tests/test_csv_validator.py`:
- `test_d24_constant_positive_daily_returns_rejected_at_every_length[2|3|5|20|120|365]`
- `test_d24_pinned_five_row_constant_fixture_is_rejected` (the old happy-path fixture, flipped, with a D-24 comment noting it was ACCEPTED before)
- `test_d24_one_message_for_every_constant_positive_series_names_no_number` (all six lengths plus a compounding-NAV yield `100 * 1.0001 ** i`, `pct_change().dropna()`)
- `test_d24_constant_zero_series_keeps_its_verdict[5|120]` (control)

Red run on the unedited sentinel, summary line verbatim: `5 failed, 33 passed, 1 warning in 3.31s`.
FAILED there:
- `test_d24_constant_positive_daily_returns_rejected_at_every_length[2]`, `[3]`, `[5]`
- `test_d24_pinned_five_row_constant_fixture_is_rejected`
- `test_d24_one_message_for_every_constant_positive_series_names_no_number`

Already PASSED there (as the plan expected): `[20]`, `[120]`, `[365]` (they reach the residue
branch today) and both zero-control ids.

**GREEN commit `d1b91d5d0`.**
- `_check_sharpe_sentinel` runs for `len(r) >= 2`, computes `sd = r.std(ddof=1)` once, and sends
  an exactly-0 and a residue `sd` through `dispersion_is_residue` to the one existing message.
  `r.mean() - DEFAULT_RISK_FREE_DAILY > 0` stays the positive test. The Sharpe-over-std path divides
  by the same `sd` value and its message is unchanged. A NaN `sd` still gets no error.
- The block comment cites D-24 and says it supersedes plan 01's D-04 verdict-preservation reason.
  It does not spell the removed clause.
- Dependants: `_daily_returns_df` defaults to a varied series (`0.001, 0.002, -0.001, 0.003,
  -0.002` cycled; `daily_return: float | None = None`, and a float still builds a constant
  series). `test_pii_undeclared_columns_dropped_from_preview` uses a varied series and asserts
  membership. `test_dollar_form_sentinel_lets_normal_decimal_returns_through` varies around |0.01|
  and now also asserts `ok`. The success path of `test_correlation_id_slot_always_present` now
  asserts `ok is True`, so it cannot silently become a rejection. The module docstring names the
  D-24 tests.

Task 1 verify command 1: the pytest tail read `142 passed, 1 warning in 13.62s`, and the command
exited 0 (PASSED lines name `test_d24_`, and `grep -c 'r.std(ddof=1) > 0'` = 0). Task 1 verify
command 2 exited 0, and exited 0 again after Task 2's merge and Task 3's release.

**Drills** (each from a byte backup of the GREEN source; the pytest selection was both files):

| Drill | Neuter | Summary line | Red ids | Restore |
|---|---|---|---|---|
| (i) | the plan-start clause `if len(r) >= 2 and r.std(ddof=1) > 0:` restored exactly | `5 failed, 137 passed, 1 warning in 42.15s` | `[2]`, `[3]`, `[5]`, the pinned fixture, the one-message test | `cp` + `cmp` clean (`cmp-restore-1 clean`) |
| (ii) | positive test `> 0` changed to `>= 0` | `3 failed, 139 passed, 1 warning in 29.40s` | `test_d24_constant_zero_series_keeps_its_verdict[5]`, `[120]`, and `test_dispersion_floor_sites.py::test_s3_all_zero_returns_pass` | `cp` + `cmp` clean (`cmp-restore-2 clean`) |

Acceptance:
- `grep -c 'D-24'`: 12 in `tests/test_csv_validator.py`, 3 in `services/csv_validator.py`.
- `_check_sharpe_sentinel` carries one constant-series message string.

Tracer gate: automated-only verify, `end-of-phase` mode. Both verify commands passed on the
committed tree, so expansion continued with no checkpoint.

## Task 2: contain origin/main, then the sweep

- `git fetch origin main`: `origin/main` was `ea4167a3f` (VERSION 0.96.0.0), 6 commits ahead, and
  `--is-ancestor` exited 1.
- **PRE_MERGE** = `d1b91d5d0a4d2740855a1e8a1ad92c8798431e78`
- **MERGED_BASE** = `ea4167a3f82a03306f29dcd2a10cbdc38768117b`
- **Merge commit** = `3a8f8ad01` ("Merge origin/main into feat/166.1 before the release sweep").
- **Conflicted paths: none.** `.planning/ROADMAP.md` and `.planning/STATE.md` auto-merged, and
  both sides were kept. The ROADMAP carries main's 164.6.5 and 167.2.1 sections and this branch's
  166.1, 166.2 and 166.3 sections.
- Working-tree handling: `.planning/STATE.md` held uncommitted orchestrator edits, and main also
  changed that file. The edits were saved as a byte backup plus a diff. The file was set to the
  HEAD content with `git show HEAD:.planning/STATE.md > .planning/STATE.md`, the merge ran, and
  the diff was re-applied with `patch` (it applied cleanly). `state.json` and `milestone.lock` were
  not touched by main, stayed dirty through the merge, and `cmp`-matched their backups.
- `git diff --stat MERGED_BASE HEAD -- src/`: empty. The branch carries no TypeScript change.
- `analytics-service/services/` files brought from main (`git diff --stat <merge-base> MERGED_BASE`):
  `job_worker.py`, `mt5_client.py`, `mt5_probe.py`, `mt5_relogin.py`, `mt5_session_episodes.py`,
  `mt5_session_monitor.py`, `mt5_validation.py`. None is in the union. A grep of their added lines
  for `.std(`, `.corr(`, `corrwith`, `np.std`, `stdev` and `sharpe` found nothing, so there is **no
  new standard-deviation or correlation division to report**.
- **SWEEP_SHA** = `3a8f8ad015072615891f8cc45a5b7dbbe5526a52`
- **SWEEP_BASE** = `ea4167a3f82a03306f29dcd2a10cbdc38768117b`, equal to MERGED_BASE.

**Verify 1 (pytest -n auto, census grep, strict mypy): exit 0.**
- pytest: `6737 passed, 90 skipped, 475 warnings in 151.40s (0:02:31)`
- the census grep printed: `qstats-gate census: 13 quantstats node(s) in services/metrics.py, 11 mirror(s), 0 violation(s); arms kwarg-proven=12 exempt=1 inline=11`
- mypy: `Success: no issues found in 101 source files`

**Verify 2 (ruff, no new finding over the 01 + 01b + 03 + 09 union): exit 0.**
- merge-base `ea4167a3f`
- BEFORE count `14`, AFTER count `14`
- `comm -13` printed nothing

**Verify 3 (ancestor, empty `src/`, anchors, hygiene): exit 0.**
- anchors: `scanned: 12 pending plan file(s) of 334` and `OK: 12 plan file(s), no stale claims.` (95 claims checked)
- hygiene: `[check-planning-hygiene] OK — 7446 tracked files scanned, none carry the local username or an absolute home path`
- No pending plan of another phase cited a moved symbol.

Fingerprint keys: `grep -rlE '^(covered_digest|covered_files):'` over this phase directory
matched 0 files. A plain-text grep finds one prose line in `166.1-RESEARCH.md` that states the
ban itself; it is not a key.

## Task 3: the release commit

- VERSION readings: `origin/main` = `0.96.0.0`. This branch read `0.92.0.0` at plan start
  (lineage only), and `0.96.0.0` after the merge.
- **Chosen: `0.97.0.0`**, a minor bump on meaning. VERSION was written with no trailing newline,
  package.json `"version"` was edited directly, and `npm version` was never run.
- Verify 1: exit 0 (byte-equal, 4-part, ENGINEFLOOR heading at the top).
- Verify 2: exit 0.
  - vitest CRITICAL-02: `Test Files  1 passed (1)`, `Tests  169 passed (169)`
  - `=== SELF-TEST PASSED: 5/5, every defect kind fired on its own input ===`
  - live run: `base=0.96.0.0 head=0.97.0.0 package=0.97.0.0 changed=42` and `✅ No defects`
- Verify 3: exit 0, printing `version-derivation: origin/main=0.96.0.0 release=0.97.0.0`.
- Verify 4: exit 0. No message in `origin/main..HEAD`, merges included, carries a CI-skip token.
- `git show --name-only --format= 46fcca88e` lists exactly `CHANGELOG.md`, `VERSION`, `package.json`.
- The node_modules symlink was removed before the commit, and `test -L node_modules` was false.
- `grep -c ENGINEFLOOR CHANGELOG.md` = 1, and the entry names no file under `src/`.

### Commit checklist: `git log <merge-base>..HEAD --oneline --no-merges`, 29 commits before the release

| # | Commit | Subject (short) | Bullet it maps to |
|---|---|---|---|
| 1 | `d1b91d5d0` | feat(166.1-09) D-24 sentinel | Changed (CSV sentinel), Tests (D-24) |
| 2 | `a6007b3d4` | test(166.1-09) D-24 red | Changed (CSV sentinel), Tests (D-24) |
| 3 | `4bb47eac4` | docs(166.1-09) plan-check round 1 | Notes: D-24 supersedes / planning commits (split bullet) |
| 4 | `3c9e95c0e` | docs(166.1-09) carry D-24 | Notes: D-24 supersedes |
| 5 | `64ebacb69` | docs(166.1) D-24 decision | Notes: D-24 supersedes; Changed (D-24) |
| 6 | `104b5ee42` | docs(166.1-03) complete | Notes: planning commits (split bullet) |
| 7 | `d44fb8598` | feat(166.1-03) C7/C8 | Fixed (C7, C8); Changed (match status) |
| 8 | `ebbcfb239` | test(166.1-03) C7/C8 red | Tests (site files) |
| 9 | `89cc5f0b2` | feat(166.1-03) C2-C6 | Fixed (C2-C6); Changed (correlation cells) |
| 10 | `aea667ad8` | test(166.1-03) C2-C6 red | Tests (site files) |
| 11 | `64af22b25` | feat(166.1-03) helpers + C1 | Fixed (helpers, C1) |
| 12 | `d3cd3edff` | docs(166.1-01b) complete | Notes: planning commits |
| 13 | `8dae1306a` | feat(166.1-01b) S8 | Fixed (S8); Notes: SQN residual |
| 14 | `9d007cff6` | test(166.1-01b) S8 red | Tests (site files) |
| 15 | `a832d1165` | docs(166.1-01) complete | Notes: planning commits |
| 16 | `614d7d8de` | feat(166.1-01) S2-S7 | Fixed (S2-S7); Changed (optimizer, risk decomposition) |
| 17 | `6484229f6` | feat(166.1-01) floor module + S1 | Fixed (floor home, S1) |
| 18 | `d04945785` | docs(166.2) 166.2-07 worktree | Notes: the D-23 split |
| 19 | `ae543bf92` | docs(166.1,166.2) separate branches | Notes: the D-23 split |
| 20 | `a78ca118a` | docs(166.1) anchor blocker, re-binds | Notes: the D-23 split; Fixed (floor home, re-binds) |
| 21 | `44151866e` | docs(166.1) release plan 09 | Notes: the D-23 split |
| 22 | `40f683fee` | docs(166.1) split into three | Notes: the D-23 split |
| 23 | `7f7518231` | docs(166.1) round-5 revision | Notes: planning commits |
| 24 | `de4f44277` | docs(166.1) round-4 revision | Notes: planning commits |
| 25 | `df4d9921d` | docs(166.1) round-3 revision | Notes: planning commits |
| 26 | `d1bd9f50f` | docs(166.1) re-plan TS half | Notes: the D-23 split (TS half is 166.2) |
| 27 | `a7240515d` | docs(166.1) revise plans | Notes: planning commits |
| 28 | `3f26b01e5` | docs(166.1) create plans | Notes: planning commits |
| 29 | `1f6b2213e` | docs(166.1) research | Notes: planning commits |

Merge commits, listed separately and mapped to the Notes "Two merges" bullet:
- `7bfce8490`, origin/main (PR #859), before wave 1
- `3a8f8ad01`, origin/main at `ea4167a3f`, before the release sweep

The release commit is `46fcca88e`. All 29 commits and both merges map to at least one bullet;
no commit is unmapped. The entry's themes are Root cause, Fixed, Changed, Tests and Notes.

## Commits of this plan

| Commit | Subject |
|---|---|
| `a6007b3d4` | test(166.1-09): D-24 a constant positive CSV must be rejected at every length, red on the unedited sentinel |
| `d1b91d5d0` | feat(166.1-09): D-24 the Sharpe sentinel rejects a constant positive CSV at every length, one message |
| `3a8f8ad01` | Merge origin/main into feat/166.1 before the release sweep |
| `46fcca88e` | chore(release): v0.97.0.0 — ENGINEFLOOR |

`actuals.commits` = `git rev-list --count 4bb47eac4..HEAD` = 10, measured before the SUMMARY
commit. That count includes the 6 `origin/main` commits the merge brought. First-parent, this
plan made 4 commits.

## Deviations from Plan

**1. [Rule 3 - Blocking] Uncommitted orchestrator edits to `.planning/STATE.md` would have blocked the merge**
- **Found during:** Task 2
- **Issue:** `origin/main` changed `.planning/STATE.md`, and the worktree held uncommitted
  orchestrator edits to the same file. `git merge` refuses in that state, and stash, checkout and
  restore are banned here.
- **Fix:** byte backup plus a saved diff, then `git show HEAD:.planning/STATE.md > .planning/STATE.md`,
  the merge, and a clean `patch` re-apply of the saved diff. `state.json` and `milestone.lock` were
  left dirty and `cmp`-verified. Neither was staged.
- **Commit:** `3a8f8ad01` (the merge commit itself carries no orchestrator edit)

**2. [Rule 2 - Test intent] Two dependants now assert `ok`**
- `test_correlation_id_slot_always_present`'s success path and
  `test_dollar_form_sentinel_lets_normal_decimal_returns_through` now assert `ok is True`, so each
  test can tell a success from a rejection (the silent loss the plan names).
- **Commit:** `d1b91d5d0`

**Total deviations:** 2 auto-fixed (1 blocking, 1 test-intent). **Impact:** none on scope.

## Known Stubs

None.

## Next

Phase 166.1 plans are all executed. Code review, verification and ship remain. The ship step must
re-run Task 3's version-derivation command immediately before the PR opens.

## Self-Check: PASSED

- The five modified files exist, and so does this SUMMARY.
- Commits `a6007b3d4`, `d1b91d5d0`, `3a8f8ad01` and `46fcca88e` are all in `git log`.
- Acceptance re-run after the release:
  - Task 1 verify 2: exit 0.
  - `--is-ancestor origin/main HEAD`: exit 0.
