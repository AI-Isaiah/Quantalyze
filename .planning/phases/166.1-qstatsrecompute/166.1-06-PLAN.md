---
phase: 166.1-qstatsrecompute
plan: 06
type: execute
wave: 2
depends_on: ["166.1-04"]
files_modified:
  - src/lib/factsheet/compute.ts
  - src/lib/factsheet/og-metrics.ts
  - src/lib/factsheet/bootstrap.ts
  - src/lib/factsheet/joint.ts
  - src/lib/factsheet/rolling.ts
  - src/lib/factsheet/comparator-block.ts
  - src/lib/factsheet/build-payload.ts
  - src/lib/factsheet/allocator.ts
  - src/lib/factsheet/dispersion-floor-sites.test.ts
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The factsheet's headline family computed from daily returns - compute's Sharpe, skew and kurtosis, computeOgHeadline's Sharpe, and bootstrapCI's Sharpe - gives on a compounding-NAV constant yield exactly what an all-zero series gives today at the same field (0, 0, 0, NaN, and the all-zero CI) (D-15, D-07)"
    - "The factsheet's benchmark family - jointMetrics' beta, correlation and information ratio, rollingBeta, rollingSharpe, and buildComparatorBlock's vol-match scale - gives on a constant-yield leg exactly what an all-zero leg gives today (D-15, D-07)"
    - "build-payload's pearsonCorr and buildAllocatorMetrics' correlation report no correlation for a constant-yield leg, exactly as for an all-zero leg (D-15, D-07)"
    - "A cent-rounded 1% APY NAV still gives a finite Sharpe, beta and correlation at the sites with that control, so widening the floor goes red (D-07)"
    - "Every T13-T20 test was written first and observed RED on the unedited site, every site was neutered, observed RED and restored from a byte backup proven with cmp, and the existing factsheet snapshot tests stay green without a snapshot update"
  artifacts:
    - path: src/lib/factsheet/dispersion-floor-sites.test.ts
      provides: "red tests for T13-T20"
  key_links:
    - from: src/lib/factsheet/compute.ts
      to: src/lib/dispersion-floor.ts
      via: "the Sharpe, skew and kurtosis gate on dispersionIsResidue(s, m)"
      pattern: "dispersion-floor"
    - from: src/lib/factsheet/joint.ts
      to: src/lib/dispersion-floor.ts
      via: "beta, correlation and information ratio gate on the shared floor"
      pattern: "dispersion-floor"
---

<objective>
Third TS slice of D-15: every ratio over a daily-return dispersion in `src/lib/factsheet/` (RESEARCH A5
rows T13-T20) moves onto plan 04's `src/lib/dispersion-floor.ts`.

Purpose: the factsheet derives its headline, benchmark, rolling and allocator numbers from dailies in
TS ("dailies are canonical"), so a constant-yield strategy or leg shows a Sharpe of about 1e13, a
fabricated skew and kurtosis, or a residue correlation. Each site gets a red test first.
Output: eight edited files and one test file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md
@.planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md
Also read `.planning/phases/166.1-qstatsrecompute/166.1-04-SUMMARY.md` (the floor module's final API and
the fixture's exports).

**Environment, drill rule and the D-07 invariant in TS** are exactly plan 04's: MAIN checkout's
`node_modules` linked only for the run and removed before every commit; exit 127 is a FAIL; `tsc` in the
background with a 600 s watchdog; byte backups under a `166.1-06` scratchpad name, neuter, RED, `cp`,
`cmp`; each test computes the site's output on the constant yield AND on an all-zero series and asserts
the ratio field identical (`Object.is` for NaN). Never `git checkout --`, `git restore`, `git stash`.
Never update a vitest snapshot to make a test pass: a snapshot diff is a finding to report.

**Which mean and sd.** Each site uses the sd it already computes (population `pstdev` in `compute.ts`,
`rolling.ts`, `joint.ts`; `Math.sqrt(v)` in `og-metrics.ts`, `bootstrap.ts`, `allocator.ts`) and the
mean of the same series. The shared floor does not care whether the sd is sample or population; the
residue is the same order.

**Symbols this plan edits (cite by symbol; line numbers drift).**
<!-- verify-symbol: src/lib/factsheet/compute.ts compute -->
<!-- verify-symbol: src/lib/factsheet/og-metrics.ts computeOgHeadline -->
<!-- verify-symbol: src/lib/factsheet/bootstrap.ts headlineStats -->
<!-- verify-symbol: src/lib/factsheet/bootstrap.ts bootstrapCI -->
<!-- verify-symbol: src/lib/factsheet/joint.ts jointMetrics -->
<!-- verify-symbol: src/lib/factsheet/rolling.ts rollingBeta -->
<!-- verify-symbol: src/lib/factsheet/rolling.ts rollingSharpe -->
<!-- verify-symbol: src/lib/factsheet/comparator-block.ts buildComparatorBlock -->
<!-- verify-symbol: src/lib/factsheet/comparator-block.ts vmScale -->
<!-- verify-symbol: src/lib/factsheet/build-payload.ts pearsonCorr -->
<!-- verify-symbol: src/lib/factsheet/build-payload.ts buildFactsheetPayload -->
<!-- verify-symbol: src/lib/factsheet/allocator.ts buildAllocatorMetrics -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the headline family - compute (Sharpe, skew, kurtosis), computeOgHeadline and bootstrapCI - on the shared floor (T13-T15), red first</name>
  <files>src/lib/factsheet/compute.ts, src/lib/factsheet/og-metrics.ts, src/lib/factsheet/bootstrap.ts, src/lib/factsheet/dispersion-floor-sites.test.ts</files>
  <read_first>
    - src/lib/factsheet/compute.ts - `compute` (Sharpe, `skew`, `kurt`; the Sortino `ddDev` is NOT in the class) and its callers in `build-payload.ts`
    - src/lib/factsheet/og-metrics.ts - `computeOgHeadline` (the Sharpe; NaN on an exact constant)
    - src/lib/factsheet/bootstrap.ts - `headlineStats` and `bootstrapCI` (its Sortino is NOT in the class)
    - src/lib/factsheet/compute.test.ts, og-metrics.test.ts - input builders
    - src/lib/factsheet/build-payload.test.ts - whether a payload fixture builder exists (for the end-to-end check below)
  </read_first>
  <behavior>
    - T13: `compute(navConstantYield(y), dates, ...)` gives `sharpe`, `skew` and `kurt` identical to the all-zero series' (0, 0, 0) for every yield id; the cent-rounded 1% APY series gives a finite non-zero Sharpe
    - T14: `computeOgHeadline` Sharpe on the constant yield is NaN, identical (`Object.is`) to the all-zero series'
    - T15: `bootstrapCI(navConstantYield(y))` Sharpe fields equal `bootstrapCI(zeros)` Sharpe fields (same seed, same length)
    - End-to-end: if `build-payload.test.ts` has a payload fixture builder, the payload's headline Sharpe for a constant-yield strategy equals the all-zero strategy's; if it has none, record that and rely on `compute`
  </behavior>
  <action>
    Per D-15 and D-07. Step 0: `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-06-PLAN.md`, halt on non-zero. Write the T13-T15 tests first in `src/lib/factsheet/dispersion-floor-sites.test.ts` and observe RED; record the RED ids. Then gate each ratio on NOT `dispersionIsResidue(s, m)` (importing from `@/lib/dispersion-floor`), keeping each site's degenerate value (0 in `compute` and `headlineStats`, NaN in `computeOgHeadline`). In `compute`, one predicate gates Sharpe, skew and kurtosis together. Drill T13-T15.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/factsheet/dispersion-floor-sites src/lib/factsheet/compute src/lib/factsheet/og-metrics</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dispersion-floor' src/lib/factsheet/compute.ts src/lib/factsheet/og-metrics.ts src/lib/factsheet/bootstrap.ts` prints at least 1 for each file
    - `grep -c 'const sharpe = s > 0 ?' src/lib/factsheet/compute.ts src/lib/factsheet/bootstrap.ts` prints 0 for each file, and `grep -c 'sharpe = s > 0 ?' src/lib/factsheet/og-metrics.ts` prints 0
    - the SUMMARY records T13-T15 RED ids, drill rows, and whether the end-to-end payload check ran
  </acceptance_criteria>
  <done>The factsheet headline Sharpe, skew and kurtosis are what an all-zero series gives on a constant yield, proven by tests observed red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the benchmark family - jointMetrics, rollingBeta, rollingSharpe and the comparator vol-match scale (T16-T18)</name>
  <files>src/lib/factsheet/joint.ts, src/lib/factsheet/rolling.ts, src/lib/factsheet/comparator-block.ts, src/lib/factsheet/dispersion-floor-sites.test.ts</files>
  <read_first>
    - src/lib/factsheet/joint.ts - `jointMetrics`: beta (`varB > 0`), correlation (`s > 0 && sb > 0`), information ratio (`trackingError > 0`); Treynor and up/down capture are NOT in the class (RESEARCH A5)
    - src/lib/factsheet/rolling.ts - `rollingBeta` (`varB !== 0`), `rollingSharpe` (`s > 0`); `rollingSortino` and `rollingVol` are NOT in the class
    - src/lib/factsheet/comparator-block.ts - `buildComparatorBlock` and `vmScale`; `b !== 0` on an equity level is NOT in the class
    - src/lib/factsheet/joint.test.ts, rolling.test.ts - input builders
  </read_first>
  <behavior>
    - T16: `jointMetrics(noise, navConstantYield(y))` gives `beta` and correlation identical to `jointMetrics(noise, zeros)`; `jointMetrics(navConstantYield(y), zeros)` gives an information ratio identical to `jointMetrics(zeros, zeros)`'s (the tracking error of a constant difference is residue)
    - T17: `rollingBeta(noise, constantYieldBench)` equals `rollingBeta(noise, zerosBench)`; `rollingSharpe(navConstantYield(y))` equals `rollingSharpe(zeros)`
    - T18: with a constant-yield benchmark, the block's vol-matched curve equals the block's UNSCALED benchmark curve (scale 1), which is what an exactly constant benchmark gets today; a noisy benchmark keeps a scale different from 1
  </behavior>
  <action>
    Per D-15 and D-07. Write the tests first and observe RED; record the ids. Then: beta 0 when `dispersionIsResidue(sqrt(varB), mb)`; correlation 0 unless both `dispersionIsReal(s, m)` and `dispersionIsReal(sb, mb)`; information ratio 0 when `dispersionIsResidue(daily tracking sd, mean of the difference)`; `rollingBeta` 0 on a residue window's benchmark; `rollingSharpe` 0 on a residue window; `vmScale` 1 when the benchmark's daily sd is residue against its mean (derive the daily sd from the same numbers `compute` used, not from a second formula). Drill T16-T18.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/factsheet/dispersion-floor-sites src/lib/factsheet/joint src/lib/factsheet/rolling</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dispersion-floor' src/lib/factsheet/joint.ts src/lib/factsheet/rolling.ts src/lib/factsheet/comparator-block.ts` prints at least 1 for each file
    - `grep -c 'varB !== 0 ? cov / varB' src/lib/factsheet/rolling.ts` prints 0 and `grep -c 'varB > 0 ? cov / varB' src/lib/factsheet/joint.ts` prints 0
    - the SUMMARY carries the T16-T18 RED ids and drill rows
  </acceptance_criteria>
  <done>The factsheet benchmark numbers are what an all-zero leg gives on a constant yield, each proven by a test observed red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the two remaining correlations - build-payload's pearsonCorr and buildAllocatorMetrics (T19-T20) - then the plan's vitest, tsc and eslint gates</name>
  <files>src/lib/factsheet/build-payload.ts, src/lib/factsheet/allocator.ts, src/lib/factsheet/dispersion-floor-sites.test.ts</files>
  <read_first>
    - src/lib/factsheet/build-payload.ts - `pearsonCorr` and its two callers inside `buildFactsheetPayload` (the benchmark-correlation list and the correlation matrix; NaN is the degenerate value)
    - src/lib/factsheet/allocator.ts - `buildAllocatorMetrics` (its `corr`)
    - src/lib/factsheet/build-payload.test.ts and its `__snapshots__` file; src/lib/factsheet/allocator.test.ts
  </read_first>
  <behavior>
    - T19: the correlation `buildFactsheetPayload` reports for a constant-yield strategy against a benchmark is identical (`Object.is`) to what an all-zero strategy gets (NaN); if the payload cannot be built in a test without a database fixture, test `pearsonCorr` through the nearest exported caller and record which
    - T20: `buildAllocatorMetrics` correlation with a constant-yield leg equals the all-zero leg's (0)
    - The existing `build-payload` snapshot test passes WITHOUT a snapshot update
  </behavior>
  <action>
    Per D-15 and D-07. Write the tests first and observe RED; record the ids. `pearsonCorr` returns NaN unless both legs pass `dispersionIsReal` (sample or population sd from its sums, and each leg's mean); `buildAllocatorMetrics` gives 0 unless both `dispersionIsReal(s, m)` and `dispersionIsReal(mmS, mmMean)`. Add no export. Drill T19-T20. Then run the gates below, and record per site T13-T20 that the fixed output is one the site already emits for an exact constant, so no renderer changes; if any would emit a new shape, STOP and report.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/factsheet src/lib/dispersion-floor "src/app/factsheet"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, "No test files found", or any "obsolete" / "mismatched" snapshot line</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint src/lib/factsheet/compute.ts src/lib/factsheet/og-metrics.ts src/lib/factsheet/bootstrap.ts src/lib/factsheet/joint.ts src/lib/factsheet/rolling.ts src/lib/factsheet/comparator-block.ts src/lib/factsheet/build-payload.ts src/lib/factsheet/allocator.ts src/lib/factsheet/dispersion-floor-sites.test.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), any tsc error line, or any eslint error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dispersion-floor' src/lib/factsheet/build-payload.ts src/lib/factsheet/allocator.ts` prints at least 1 for each file
    - `git diff --stat -- src/lib/factsheet/__snapshots__/` is empty for this plan's commits
    - the SUMMARY carries the T13-T20 drill table and the per-site "already emitted" line
    - `git diff --stat -- analytics-service/ supabase/` is empty for this plan's commits, and `test -L node_modules` is false at every commit
  </acceptance_criteria>
  <done>No ratio in src/lib/factsheet is computed over a residue dispersion, every site was drilled, snapshots are unchanged, and vitest, tsc and eslint are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dailies -> TS factsheet math -> public factsheet page and OG image | a fabricated Sharpe, skew, beta or correlation reaches a published factsheet and its share image |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.1-25 | Tampering (integrity of a displayed number) | T13-T20 | medium | mitigate | shared floor; red test per site; D-07 equality with the all-zero input; drill table |
| T-166.1-26 | Tampering (a regression hidden by a snapshot refresh) | build-payload snapshot | medium | mitigate | snapshot directory must be unchanged; a snapshot diff is reported, never accepted |
| T-166.1-27 | Denial of service (over-wide floor hides real data) | the shared floor at these sites | medium | mitigate | cent-rounded 1% APY controls go red if the floor widens |
</threat_model>

<verification>
- `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-06-PLAN.md` exits 0 before Task 1 starts.
- Both Task 3 verify commands exit 0.
</verification>

<success_criteria>
- D-15, third slice: T13-T20 use the shared TS floor, each with a test observed red, snapshots unchanged.
</success_criteria>

<output>
Create `.planning/phases/166.1-qstatsrecompute/166.1-06-SUMMARY.md` when done. Remove the node_modules
link, stage the SUMMARY, then run
`QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && test -x "$QZ_MAIN/node_modules/.bin/tsx" && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK before committing.
</output>
