---
phase: 166.1-qstatsrecompute
plan: 06
type: execute
wave: 2
depends_on: ["166.1-04"]
files_modified:
  - src/lib/factsheet/compute.ts
  - src/lib/factsheet/bootstrap.ts
  - src/lib/factsheet/joint.ts
  - src/lib/factsheet/rolling.ts
  - src/lib/factsheet/build-payload.ts
  - src/lib/factsheet/allocator.ts
  - src/lib/factsheet/og-metrics.ts
  - src/lib/factsheet/return-stats.sites.test.ts
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The factsheet builder computes dispersion, Sharpe, correlation and beta through return-stats and carries no private mean, pstdev or Pearson of its own in joint.ts, rolling.ts or build-payload.ts; compute.ts keeps one only where replacing it would move an existing test's value, recorded with the reason (D-17)"
    - "On a compounding-NAV constant yield, compute's sharpe, skew, kurt and ann_vol, and bootstrapCI's Sharpe fields, are EXACTLY what an all-zero series gives today (0, 0, 0, 0 and the all-zero CI) (D-07, D-17)"
    - "On a constant-yield leg, jointMetrics' beta, correlation and information ratio, rollingBeta and rollingSharpe are exactly what an all-zero leg gives today, and buildComparatorBlock's vol-matched curve equals its unscaled benchmark curve (scale 1) with comparator-block.ts UNEDITED, because compute's ann_vol is 0 at the source (D-07, D-17)"
    - "build-payload's benchmark correlations and correlation matrix report NaN, and buildAllocatorMetrics' correlation 0, for a constant-yield leg, exactly as for an all-zero leg (D-07, D-17)"
    - "A cent-rounded 1% APY NAV still gives a finite Sharpe, beta and correlation at the sites with that control, so widening the floor goes red (D-07)"
    - "Every T13-T20 test was written first and observed RED on the unedited site, every edited site was neutered, observed RED and restored from a byte backup proven with cmp, and the existing factsheet snapshot and golden tests pass without an update"
    - "computeOgHeadline computes its Sharpe with return-stats' sharpe (population, the asset-class periods), answered as NaN (its \"hide\" value) on null, so on a compounding-NAV constant yield the OG card's computed Sharpe is NaN exactly as for an all-zero series; this is the computed arm only, the one Phase 169 D-25 assigns to 166.1 plan 06, and the persisted-value read is Phase 169 plan 04's (D-17, D-19)"
    - "comparator-block.ts, types.ts, fetch-and-build-payload.ts and og-metrics.test.ts are not edited by this plan (Phase 169 owns them, CONTEXT D-17, D-19)"
  artifacts:
    - path: src/lib/factsheet/return-stats.sites.test.ts
      provides: "red tests for T13-T20 (T14's computed arm)"
  key_links:
    - from: src/lib/factsheet/compute.ts
      to: src/lib/return-stats.ts
      via: "dispersion(rets, 0) supplies m and a residue-snapped s; sharpe() supplies the Sharpe"
      pattern: "@/lib/return-stats"
    - from: src/lib/factsheet/joint.ts
      to: src/lib/return-stats.ts
      via: "beta, pearson and sharpe over the active series"
      pattern: "@/lib/return-stats"
    - from: src/lib/factsheet/og-metrics.ts
      to: src/lib/return-stats.ts
      via: "computeOgHeadline's computed Sharpe is sharpe(values, population, periodsPerYear) ?? NaN"
      pattern: "@/lib/return-stats"
    - from: src/lib/factsheet/comparator-block.ts
      to: src/lib/factsheet/compute.ts
      via: "vmScale's existing ann_vol > 0 guard reads the snapped ann_vol (no edit to comparator-block.ts)"
      pattern: "ann_vol"
---

<objective>
Third slice of D-17 (compute once): the factsheet builder's ratio sites (CONTEXT D-17 rows T13, T15-T20, and T14's computed arm per D-19)
stop carrying their own mean, standard deviation, Sharpe, correlation and beta and call plan 04's
`src/lib/return-stats.ts`.

Purpose: the factsheet builds its headline, benchmark, rolling and allocator numbers in TS for the
strategy, allocator-portfolio and scenario payloads and the levered browser re-derive, with three private
copies of `mean`/`pstdev` and two private Pearson forms. On a constant-yield strategy or leg each one
shows a Sharpe of about 1e13, a fabricated skew and kurtosis, a residue correlation, or a vol-matched
curve scaled by 1e13. Row T14 (the OG card's Sharpe) is split (CONTEXT D-19): Phase 169 plan 04 makes the
card read the PERSISTED Sharpe when the row is rankable; the Sharpe `computeOgHeadline` still COMPUTES
(no persisted value passed, or not rankable) is this plan's, because Phase 169 D-25 lists `og-metrics.ts`
under 166.1 plan 06 and a local `s > 0` Sharpe left there would be a twenty-first copy. Row T18 (the
comparator vol-match) is fixed at its source, `compute`'s `ann_vol`, so `comparator-block.ts` stays
untouched.
Output: seven edited files and one test file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md
@.planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md
Also read `.planning/phases/166.1-qstatsrecompute/166.1-04-SUMMARY.md` (the final `return-stats.ts` API at
its top, and the fixture's exports). This plan does NOT edit `src/lib/return-stats.ts` or plan 04's and
plan 05's test files (plan 05 runs in the same wave). An API gap is a STOP-and-report.

**Environment, drill rule, the D-07 invariant in TS and the never-edit-a-golden rule** are exactly plan
04's: MAIN checkout's `node_modules` linked only for the run and removed before every commit; exit 127 is
a FAIL; `tsc` in the background with a 600 s watchdog; byte backups under a `166.1-06` scratchpad name,
neuter, RED, `cp`, `cmp`; each test computes the site's output on the constant yield AND on an all-zero
series and asserts the field identical (`Object.is` for NaN). Never `git checkout --`, `git restore`,
`git stash`. Never update a vitest snapshot to make a test pass: a snapshot diff is a finding to report.
The snapshot rounds every finite number to 12 significant digits, and `return-stats`' arithmetic is the
factsheet's own (`compute`'s Sharpe order, `pearsonCorr`'s sums form), so it must not move.

**Ownership and couplings (CONTEXT D-17, D-19).** Phase 169 owns `comparator-block.ts` (plans 05a, 05b,
07), `types.ts`, `composite-read-path.ts`, `fetch-and-build-payload.ts`, `og-metrics.test.ts` and the OG
route; this plan edits none of them. Three files are shared with other phases in DIFFERENT hunks:
`og-metrics.ts` (169 plan 04 adds an optional persisted-scalars argument and a branch that uses the
persisted values when rankable; this plan replaces only the three lines that compute the variance, the
sd and the Sharpe inside the `values.length >= 30` block, keeps the 30-observation gate, and adds one
import line; whichever phase merges second rebases and re-runs both phases' og-metrics tests), `compute.ts` (169 plan 06 edits the return windows; this plan edits only the lines
that compute `m`, `s`, `ann_vol`, `sharpe`, `skew`, `kurt` and the private helpers they used) and
`build-payload.ts` (169 plans 05a/05b and 167.2.1 plan 01 edit comparator coverage and the resolve
stage; this plan replaces only the BODY of `pearsonCorr` and one import line, and leaves every call
site as it is). Keep every edit inside those hunks so whichever phase merges second rebases cleanly.

**Which ddof.** Each site keeps the ddof it uses today: population (`ddof: 0`) in `compute.ts`,
`og-metrics.ts`, `joint.ts`, `rolling.ts`, `allocator.ts` and `build-payload.ts`; read `bootstrap.ts` `headlineStats` and
keep whatever it divides by.

**Symbols this plan edits (cite by symbol; line numbers drift).**
<!-- verify-symbol: src/lib/factsheet/compute.ts compute -->
<!-- verify-symbol: src/lib/factsheet/compute.ts pstdev -->
<!-- verify-symbol: src/lib/factsheet/bootstrap.ts headlineStats -->
<!-- verify-symbol: src/lib/factsheet/bootstrap.ts bootstrapCI -->
<!-- verify-symbol: src/lib/factsheet/joint.ts jointMetrics -->
<!-- verify-symbol: src/lib/factsheet/rolling.ts rollingBeta -->
<!-- verify-symbol: src/lib/factsheet/rolling.ts rollingSharpe -->
<!-- verify-symbol: src/lib/factsheet/rolling.ts pstdev -->
<!-- verify-symbol: src/lib/factsheet/comparator-block.ts buildComparatorBlock -->
<!-- verify-symbol: src/lib/factsheet/build-payload.ts pearsonCorr -->
<!-- verify-symbol: src/lib/factsheet/build-payload.ts buildFactsheetPayload -->
<!-- verify-symbol: src/lib/factsheet/allocator.ts buildAllocatorMetrics -->
<!-- verify-symbol: src/lib/factsheet/og-metrics.ts computeOgHeadline -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the headline family - compute (Sharpe, skew, kurtosis, ann_vol), bootstrapCI and computeOgHeadline's computed Sharpe - on return-stats (T13, T15, T14's computed arm), proven end-to-end through the comparator vol-match (T18) with comparator-block.ts unedited, red first</name>
  <files>src/lib/factsheet/compute.ts, src/lib/factsheet/bootstrap.ts, src/lib/factsheet/og-metrics.ts, src/lib/factsheet/return-stats.sites.test.ts</files>
  <read_first>
    - src/lib/factsheet/compute.ts - `compute` (`m`, `s`, `annVol`, `sharpe`, `skew`, `kurt`; the Sortino `ddDev` is NOT in the class), the private `mean` and `pstdev` and every other use of them in the file
    - src/lib/factsheet/bootstrap.ts - `headlineStats` and `bootstrapCI` (its Sortino is NOT in the class)
    - src/lib/factsheet/og-metrics.ts - `computeOgHeadline` (the `values.length >= 30` block: `m`, `v`, `s`, the Sharpe line; the drawdown and CAGR code is NOT in the class) and src/lib/factsheet/og-metrics.test.ts (must pass unedited; Phase 169 plan 04 owns that file). Before editing, run `git fetch origin main` and `git log --oneline HEAD..origin/main -- src/lib/factsheet/og-metrics.ts`: if Phase 169 plan 04 has merged, the function carries a persisted-scalars branch; edit only the computed arm and leave that branch as it is
    - src/lib/factsheet/comparator-block.ts - `buildComparatorBlock` and how `vmScale` reads `benchSummary.ann_vol` (READ ONLY)
    - src/lib/factsheet/compute.test.ts, compute.metrics.test.ts, compute.dd.test.ts, and src/__tests__/phase-52-frozen-spine-guards.test.ts (compute.ts is carved out of the frozen islands for reviewed math edits; confirm it stays carved out)
    - src/lib/factsheet/build-payload.test.ts - its snapshot rounding and whether a payload fixture builder exists
  </read_first>
  <behavior>
    - T13: `compute(navConstantYield(y), dates, ...)` gives `sharpe`, `skew`, `kurt` and `ann_vol` identical to the all-zero series' (0, 0, 0, 0) for every yield id; the cent-rounded 1% APY series gives a finite non-zero Sharpe; on a noisy series the Sharpe equals the compute-order expression built in the test from `mean` and `stdDev`, and the existing compute tests pass unedited
    - T15: `bootstrapCI(navConstantYield(y))` Sharpe fields equal `bootstrapCI(zeros)` Sharpe fields (same seed, same length)
    - T14 (computed arm): `computeOgHeadline(rows from navConstantYield(y), "crypto").sharpe` is NaN for every yield id, `Object.is`-identical to the all-zero rows' (today about 1e13); the cent-rounded 1% APY rows give a finite Sharpe; `cagr` and `maxDd` on a noisy input are bitwise unchanged; the existing og-metrics tests (including the crypto / traditional ratio of sqrt(365/252)) pass unedited. Called WITHOUT any persisted-scalars argument, so the test pins the computed arm whether or not Phase 169 plan 04 has merged
    - T18 (end-to-end, no edit to comparator-block.ts): `buildComparatorBlock` with a constant-yield benchmark returns a vol-matched curve equal to the block's unscaled benchmark curve, which is what an all-zero benchmark gets today; a noisy benchmark keeps a scale different from 1
    - If `build-payload.test.ts` exposes a payload fixture builder, the payload's headline Sharpe for a constant-yield series equals the all-zero series'; if it has none, record that and rely on `compute`
  </behavior>
  <action>
    Per D-17 and D-07. Step 0: `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-06-PLAN.md`, halt on non-zero. Write the T13, T14, T15 and T18 tests first in `src/lib/factsheet/return-stats.sites.test.ts` and observe RED; record the RED ids. Then in `compute`: take `m` and `s` from `dispersion(rets, 0)` (so `s` is 0 on residue and `annVol` follows), take the Sharpe from `sharpe(rets, { periodsPerYear, ddof: 0, rf })` answered as 0 on null (today's degenerate value), and keep skew and kurtosis on their existing `s > 0` gate, which is now correct over the snapped `s`. Replace every other use of the private `mean` / `pstdev` in `compute.ts` with `mean` / `stdDev(xs, false)` from `@/lib/portfolio-math-utils` (the same left-to-right sums, so bitwise equal) and delete the private helpers; if replacing a use moves an existing test's value, keep that one private use and record which and why in the SUMMARY (the Sharpe / skew / kurtosis / `ann_vol` lines are not optional). In `headlineStats`, the Sharpe becomes `sharpe(rets, { periodsPerYear, ddof })` answered as 0 on null, with the ddof it uses today. T14's computed arm, per D-19: in `computeOgHeadline`, replace the `m` / `v` / `s` lines and the Sharpe expression inside the `values.length >= 30` block with `sharpe(values, { periodsPerYear, ddof: 0 }) ?? NaN` (NaN is the card's existing hide value), keep the 30-observation gate, the drawdown and the CAGR code as they are, and add the one import line; if Phase 169 plan 04 has merged, its persisted branch is untouched. Do not edit `comparator-block.ts` or `og-metrics.test.ts`. Drill T13 (restore the old `s` and Sharpe lines), T15, T14 (restore the old `s > 0 ?` Sharpe line), and T18 through T13's neuter (with `compute` neutered, the T18 test must also go RED; record it). `cp` restore, `cmp`.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/factsheet/return-stats.sites src/lib/factsheet/compute src/lib/factsheet/og-metrics src/app/api/og src/lib/factsheet/build-payload src/__tests__/phase-52-frozen-spine-guards && test -z "$(git diff --name-only "$(git merge-base HEAD origin/main)" -- src/lib/factsheet/comparator-block.ts src/lib/factsheet/og-metrics.test.ts src/lib/factsheet/__snapshots__)"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, "No test files found", any "obsolete" / "mismatched" snapshot line, or a diff in comparator-block.ts, og-metrics.test.ts or the snapshot directory</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '@/lib/return-stats' src/lib/factsheet/compute.ts src/lib/factsheet/bootstrap.ts src/lib/factsheet/og-metrics.ts` prints at least 1 for each file, and `grep -c 's > 0 ? (m \* periodsPerYear)' src/lib/factsheet/og-metrics.ts` prints 0
    - `grep -c 'const sharpe = s > 0 ?' src/lib/factsheet/compute.ts src/lib/factsheet/bootstrap.ts` prints 0 for each file, and `grep -c '^function pstdev' src/lib/factsheet/compute.ts` prints 0 (or the SUMMARY records the kept use and why)
    - the SUMMARY records T13, T14 (computed arm), T15 and T18 RED ids, drill rows, whether Phase 169 plan 04 had merged into `og-metrics.ts` at edit time, and whether the end-to-end payload check ran
  </acceptance_criteria>
  <done>The factsheet headline Sharpe, skew, kurtosis, vol, bootstrap Sharpe and the OG card's computed Sharpe come from return-stats and equal the all-zero output on a constant yield, and the comparator vol-match is right without touching its file, each proven by a test observed red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the benchmark family - jointMetrics, rollingBeta and rollingSharpe - on return-stats, private mean/pstdev removed (T16, T17)</name>
  <files>src/lib/factsheet/joint.ts, src/lib/factsheet/rolling.ts, src/lib/factsheet/return-stats.sites.test.ts</files>
  <read_first>
    - src/lib/factsheet/joint.ts - `jointMetrics`: beta (`varB > 0`), correlation (`s > 0 && sb > 0`), tracking error and information ratio (`trackingError > 0`); Treynor and up/down capture are NOT in the class (RESEARCH A5); the private `mean` / `pstdev`
    - src/lib/factsheet/rolling.ts - `rollingBeta` (`varB !== 0`), `rollingSharpe` (`s > 0`), and `rollingVol` / `rollingSortino` (NOT in the class, but they use the private `mean` / `pstdev` this task removes)
    - src/lib/factsheet/joint.test.ts, rolling.test.ts, src/lib/scenario-blend-adapter.test.ts (it pins `rolling.ts`'s POPULATION-std convention; must pass unedited), src/app/factsheet/[id]/v2/basis-context.tsx (the levered re-derive calls `jointMetrics`; read only)
  </read_first>
  <behavior>
    - T16: `jointMetrics(noise, navConstantYield(y))` gives `beta` and `corr` identical to `jointMetrics(noise, zeros)`; `jointMetrics(bench + navConstantYield(y), bench)`-shaped input (a constant active return) gives `info_ratio` and `tracking_error` identical to the zero-active case; Treynor, alpha, `r2` and captures on a noisy input are unchanged to 12 significant digits
    - T17: `rollingBeta(noise, constantYieldBench, w)` equals `rollingBeta(noise, zerosBench, w)`; `rollingSharpe(navConstantYield(y), w)` equals `rollingSharpe(zeros, w)`; `rollingVol` and `rollingSortino` on a noisy series are bitwise unchanged
  </behavior>
  <action>
    Per D-17 and D-07. Write the tests first and observe RED; record the ids. `jointMetrics`: beta is `beta(rets, bench) ?? 0`; `corr` is `pearson(rets, bench) ?? 0` (the sums form; the population and sums forms agree to rounding, and the snapshot rounds to 12 digits); the tracking error is `dispersion(active, 0).sd * Math.sqrt(periodsPerYear)` over `active[i] = rets[i] - bench[i]`, and the information ratio is `sharpe(active, { periodsPerYear, ddof: 0 }) ?? 0`; alpha keeps its formula over the new beta; `r2` stays `corr * corr`. `rollingBeta`: each window's value is `beta(winS, winB) ?? 0`; `rollingSharpe`: `sharpe(win, { periodsPerYear, ddof: 0 }) ?? 0`. Replace the private `mean` / `pstdev` in both files with `mean` / `stdDev(xs, false)` from `@/lib/portfolio-math-utils` and delete them. Drill T16 and T17 (restore each old expression, RED, `cp` restore, `cmp`).
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/factsheet/return-stats.sites src/lib/factsheet/joint src/lib/factsheet/rolling src/lib/scenario-blend-adapter src/lib/factsheet/build-payload "src/app/factsheet"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, "No test files found", or any "obsolete" / "mismatched" snapshot line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '@/lib/return-stats' src/lib/factsheet/joint.ts src/lib/factsheet/rolling.ts` prints at least 1 for each file
    - `grep -c '^function pstdev\|^function mean' src/lib/factsheet/joint.ts src/lib/factsheet/rolling.ts` prints 0 for each file
    - `grep -c 'varB !== 0 ? cov / varB' src/lib/factsheet/rolling.ts` prints 0 and `grep -c 'varB > 0 ? cov / varB' src/lib/factsheet/joint.ts` prints 0
    - the SUMMARY carries the T16-T17 RED ids and drill rows
  </acceptance_criteria>
  <done>The factsheet benchmark and rolling numbers come from return-stats and equal the all-zero output on a constant yield, each proven by a test observed red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the two remaining correlations - build-payload's pearsonCorr body and buildAllocatorMetrics (T19, T20) - then the plan's vitest, tsc and eslint gates</name>
  <files>src/lib/factsheet/build-payload.ts, src/lib/factsheet/allocator.ts, src/lib/factsheet/return-stats.sites.test.ts</files>
  <read_first>
    - src/lib/factsheet/build-payload.ts - `pearsonCorr` and its two callers inside `buildFactsheetPayload` (the benchmark-correlation list and the correlation matrix; NaN is the degenerate value); the import block
    - src/lib/factsheet/allocator.ts - `buildAllocatorMetrics` (`s`, `mmS`, `corr`, `annVol`, `mmAnnVol`)
    - src/lib/factsheet/build-payload.test.ts and its `__snapshots__` file; src/lib/factsheet/allocator.test.ts
  </read_first>
  <behavior>
    - T19: the correlation `buildFactsheetPayload` reports for a constant-yield strategy against a benchmark is identical (`Object.is`) to what an all-zero strategy gets (NaN); if the payload cannot be built in a test without a database fixture, test through the nearest exported caller and record which
    - T20: `buildAllocatorMetrics` correlation with a constant-yield leg equals the all-zero leg's (0); a noisy pair keeps its value to 12 significant digits
    - The existing `build-payload` snapshot test passes WITHOUT a snapshot update
  </behavior>
  <action>
    Per D-17 and D-07. Write the tests first and observe RED; record the ids. Replace the BODY of `pearsonCorr` with `return pearson(a, b) ?? NaN;` using `pearson` from `@/lib/return-stats` (one import line added; the function name and both call sites stay, so the hunk is disjoint from 169's and 167.2.1's edits in this file). In `buildAllocatorMetrics`, `corr` becomes `pearson(rets, mmRets) ?? 0`, and `s` / `mmS` come from `dispersion(..., 0).sd` so `annVol` and `mmAnnVol` are 0 on residue like an exact constant; keep the blend-vol arithmetic. Add no export. Drill T19 and T20. Then run the gates below, and record per site T13-T20 (T14's computed arm) that the fixed output is one the site already emits for an exact constant, so no renderer changes; if any would emit a new shape, STOP and report.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/factsheet src/lib/return-stats src/lib/scenario-blend-adapter "src/app/factsheet" "src/app/(dashboard)/allocations/widgets/performance" "src/app/(dashboard)/discovery"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, "No test files found", or any "obsolete" / "mismatched" snapshot line</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint src/lib/factsheet/compute.ts src/lib/factsheet/bootstrap.ts src/lib/factsheet/og-metrics.ts src/lib/factsheet/joint.ts src/lib/factsheet/rolling.ts src/lib/factsheet/build-payload.ts src/lib/factsheet/allocator.ts src/lib/factsheet/return-stats.sites.test.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), any tsc error line, or any eslint error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '@/lib/return-stats' src/lib/factsheet/build-payload.ts src/lib/factsheet/allocator.ts` prints at least 1 for each file
    - `grep -c 'denom > 0 ? cov / denom : NaN' src/lib/factsheet/build-payload.ts` prints 0 and `grep -c 's > 0 && mmS > 0 ?' src/lib/factsheet/allocator.ts` prints 0
    - `git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/factsheet/__snapshots__/ src/lib/factsheet/comparator-block.ts src/lib/factsheet/og-metrics.test.ts src/lib/factsheet/types.ts src/lib/factsheet/fetch-and-build-payload.ts src/lib/return-stats.ts` is empty for this plan's commits
    - the SUMMARY carries the T13-T20 drill table and the per-site "already emitted" line
    - `git diff --stat -- analytics-service/ supabase/` is empty for this plan's commits, and `test -L node_modules` is false at every commit
  </acceptance_criteria>
  <done>No ratio in the factsheet builder is computed outside return-stats or over a residue dispersion, every edited site was drilled, snapshots are unchanged, and vitest, tsc and eslint are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dailies -> TS factsheet math -> public factsheet page, allocator overview, scenario preview | a fabricated Sharpe, skew, beta or correlation reaches a published factsheet or an allocator's book view |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.1-25 | Tampering (integrity of a displayed number) | T13-T20 (T14's computed arm; the OG card is a public social preview) | medium | mitigate | one shared module; red test per site; D-07 equality with the all-zero input; drill table |
| T-166.1-26 | Tampering (a regression hidden by a snapshot refresh) | build-payload snapshot | medium | mitigate | snapshot directory must be unchanged; a snapshot diff is reported, never accepted |
| T-166.1-27 | Denial of service (over-wide floor hides real data) | the shared floor at these sites | medium | mitigate | cent-rounded 1% APY controls go red if the floor widens |
| T-166.1-31 | Tampering (cross-phase clobber) | compute.ts, build-payload.ts, og-metrics.ts shared with 169 / 167.2.1 | medium | mitigate | edits confined to named hunks; 169-owned files diff-gated to empty; second merger rebases |
</threat_model>

<verification>
- `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-06-PLAN.md` exits 0 before Task 1 starts.
- Both Task 3 verify commands exit 0.
</verification>

<success_criteria>
- D-17, third slice: the factsheet builder's ratios and the OG card's computed Sharpe come from return-stats (T14's persisted read left to Phase 169 plan 04, D-19; T18 fixed at its source), each with a test observed red, snapshots unchanged.
</success_criteria>

<output>
Create `.planning/phases/166.1-qstatsrecompute/166.1-06-SUMMARY.md` when done. Remove the node_modules
link, stage the SUMMARY, then run
`QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && test -x "$QZ_MAIN/node_modules/.bin/tsx" && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK before committing.
</output>
