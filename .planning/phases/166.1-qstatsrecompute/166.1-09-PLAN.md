---
phase: 166.1-qstatsrecompute
plan: 09
type: execute
wave: 3
depends_on: ["166.1-01b", "166.1-03"]
files_modified:
  - analytics-service/services/csv_validator.py
  - analytics-service/tests/test_csv_validator.py
  - VERSION
  - package.json
  - CHANGELOG.md
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An exactly constant POSITIVE daily-returns CSV is rejected by `_check_sharpe_sentinel` at EVERY length, whether its standard deviation comes out exactly 0 (a short series) or as float residue (a longer one, or a constant yield taken from a compounding NAV), through `services.dispersion.dispersion_is_residue` with ONE message for both that names no number; a constant ZERO series keeps today's verdict (accepted); a varied series is judged by the Sharpe-over-std path exactly as before; the new test was RED on the unedited `csv_validator.py` before the fix, and the pinned 5-row constant fixture now asserts REJECTED with a comment citing D-24 (D-24)"
    - "Every Python gate the phase touched is green at one recorded SHA that contains origin/main: the full analytics-service suite (with the four TEST database variables unset) including the qstats-gate census line, whose presence a command asserts, CI's strict mypy, ruff with no NEW finding against the merge-base over the union of the Python files plans 01, 01b and 03 touched, the plan anchor verifier in CI's --pending form, and planning hygiene (D-18 W2, D-20 B2, D-23)"
    - "If origin/main moved past the branch, the sweep merges it first, records PRE_MERGE, MERGED_BASE and every conflicted path, keeps the services/dispersion.py helper call inside every 166.1 hunk, and runs the sweep on the merged tree; the branch then carries no src/ change, so the release cannot claim TypeScript work (D-23)"
    - "VERSION and package.json carry the SAME 4-digit string, bumped on MEANING (user-visible values change) as a minor-level bump derived from origin/main's VERSION re-read at release time (second component + 1, last two zeroed), so whichever of 166.1 and 166.2 merges second never reuses the first's version; a command asserts the derivation, and the ship step re-runs it immediately before the PR opens; npm version is never run (D-18, D-23 revision)"
    - "ONE unified CHANGELOG entry covers EVERY commit on the branch, each mapped to at least one bullet, in the repo's own section vocabulary; it records Phase 166.3's recompute state at release time as counts only or 'founder-gated, pending' (D-14), that stored portfolio_analytics values computed before this release keep any residue value until that portfolio's next analytics compute, the SQN residual (D-21 W1), D-11, no migration, and the 2026-09-26 split: the TypeScript half ships as Phase 166.2 and this entry claims none of it (D-23)"
    - "No commit message on the branch contains a CI-skip trailer token, not even in a sentence denying it: a command sweeps EVERY message in origin/main..HEAD, merge messages included (D-23 revision)"
  artifacts:
    - path: analytics-service/services/csv_validator.py
      provides: "`_check_sharpe_sentinel` without the exact-zero gate, so every constant positive series reaches the D-04 residue branch (D-24)"
    - path: analytics-service/tests/test_csv_validator.py
      provides: "the D-24 tests (every length, a compounding-NAV yield, the constant-zero control) and the 5-row constant fixture flipped to rejected"
    - path: VERSION
      provides: "next minor 4-digit version"
    - path: package.json
      provides: "version byte-equal to VERSION"
    - path: CHANGELOG.md
      provides: "one unified entry for Phase 166.1 ENGINEFLOOR at the top"
  key_links:
    - from: analytics-service/services/csv_validator.py
      to: analytics-service/services/dispersion.py
      via: "`_check_sharpe_sentinel` calls `dispersion_is_residue` for an exact-zero AND a residue standard deviation (D-24)"
      pattern: "dispersion_is_residue"
    - from: VERSION
      to: package.json
      via: "src/__tests__/critical-regressions.test.ts CRITICAL-02 asserts byte equality"
      pattern: "\"version\""
    - from: VERSION
      to: CHANGELOG.md
      via: "scripts/check-version-bump.mjs judge: a moved VERSION needs a [<version>] heading"
      pattern: "## \\["
---

<objective>
First carry founder decision D-24 (2026-09-26), red test first: an exactly constant positive
daily-returns CSV is rejected at every length. Then run the whole-phase Python gate sweep at one SHA that contains `origin/main` (merging it first if `main`
moved), and write the single `chore(release)` commit: VERSION, package.json and one unified CHANGELOG
entry for Phase 166.1 ENGINEFLOOR that maps every branch commit.

Purpose: the repo rule (CLAUDE.md "CHANGELOG discipline") is that every ship writes an entry in the same
commit as the VERSION bump, built from an enumerated commit checklist. Per D-14 (as amended) and D-23 this
plan waits on the code plans 01b and 03 (01 transitively) and NOT on the recompute, which is now Phase
166.3's founder-gated production work; the recompute is recorded as whatever state it is in, counts only.
Per D-20 B2 the anchor check is CI's `--pending` form. Per D-23 this is the Python half only: the TS half
(the former plans 04-08) ships as Phase 166.2 with its own release plan `166.2-07`, and this entry does
not claim it.
Per the D-23 revision (2026-09-26, separate branches) this plan runs on `feat/166.1`, which carries
Phase 166.1 alone; Phase 166.2 releases from its own branch, `feat/166.2`, cut from `feat/166.1` at
the split.
Output: the D-24 `test(...)` red commit and its `feat(...)` fix commit, at most one merge commit (only
if `main` moved), and one release commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md
@.planning/phases/166.1-qstatsrecompute/166.1-VALIDATION.md
@VERSION
Also read `166.1-01-SUMMARY.md`, `166.1-01b-SUMMARY.md` and `166.1-03-SUMMARY.md` in this phase
directory, and, if it exists, `.planning/phases/166.3-recompute-prod-rows-computed-before-phase-166-are-recomputed/166.3-01-SUMMARY.md`
(the recompute's state and its Q6 counts; absent means "founder-gated, pending").

**Environment.** Python from the worktree's `analytics-service/` directory only (from the repo root
pytest misses cassettes and makes live broker calls), with the MAIN checkout's venv resolved at run time
(`QZ_WT` / `QZ_VENV_PY`, as in plan 01: the first `worktree` path of `git worktree list --porcelain`
joined with `/analytics-service/.venv/bin/python`; never written into a file; exit 127 = FAIL, never a
pass). ruff is on PATH, not in the venv. TS tools (only CRITICAL-02 and the hygiene script need them)
through the main checkout's `node_modules`, linked only for the run and removed before every commit when
it is a symlink. No database, no credential, no uvicorn (a local uvicorn claims real production compute
jobs). Every pytest run is prefixed with
`env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL`.
Run `git fetch origin main` before ANY `git merge-base` against `origin/main`: a stale local ref makes
the merge-base, the version comparison and the commit checklist wrong without an error.

**Containing origin/main (decided from evidence; there is no merge plan in this phase any more).** The
pre-split phase had a merge-and-re-prove plan (old 07); D-23 moved it to Phase 166.2 as `166.2-06`, so
this plan owns the step for the Python half. Measured at planning time (2026-09-26): after
`git fetch origin main`, `git merge-base --is-ancestor origin/main HEAD` exited 1 with `origin/main` two
commits ahead, so at execution the merge is expected, not hypothetical. Rule: fetch; record
`git rev-parse HEAD` as PRE_MERGE and `git rev-parse origin/main` as MERGED_BASE; if `--is-ancestor`
exits 0, record "already up to date at MERGED_BASE"; otherwise `git merge --no-ff origin/main` with the
message `Merge origin/main into feat/166.1 before the release sweep` (no CI-skip trailer token anywhere).
Merging first also makes the ruff BEFORE snapshot's merge-base resolve to `main`'s tip, so the ruff
comparison measures only this phase's lines. The full pytest run that follows IS the re-proof of the
site tests on the merged tree; the `--is-ancestor` assertion in the last Task 2 verify command is the
certificate.

**The Python file union (named literally; zsh does not word-split an unquoted variable).** Plans 01, 01b
and 03, relative to `analytics-service/`: `services/dispersion.py`, `services/metrics.py`,
`services/portfolio_optimizer.py`, `services/csv_validator.py`, `services/allocated_capital.py`,
`services/equity_reconstruction.py`, `services/optimizer.py`, `services/portfolio_risk.py`,
`services/analytics_runner.py`, `services/match_engine.py`, `services/strategy_matching.py`,
`routers/portfolio.py`, `tests/test_metrics.py`, `tests/dispersion_fixtures.py`,
`tests/test_dispersion_floor_sites.py`, `tests/test_correlation_residue_sites.py`, plus this plan's D-24
test file `tests/test_csv_validator.py`. This is exactly the union of the three code plans'
`files_modified` and this plan's two Python files (`services/csv_validator.py` is already in it).

**Version (CONTEXT D-18, as amended by the D-23 revision).** Never take a version number from this
plan, and never from this branch's own VERSION file. At release time run `git fetch origin main`,
re-read `origin/main`'s `VERSION` (`git show origin/main:VERSION`), increment the second component and
zero the last two. Phases 166.1 and 166.2 release on separate branches, and whichever merges second
must not reuse the first's version: the derivation is asserted by a Task 3 verify command, and the
ship step re-runs that command immediately before the PR opens, with nothing in between. If it goes
red (Phase 166.2 merged after this release commit), re-run Task 3 as a NEW `chore(release)` commit
that re-derives the version and replaces this entry's heading; never amend, never rebase. Record the
`origin/main` reading, this branch's reading (lineage only) and the result in the SUMMARY. (Lineage
only, not an input: at the 2026-09-26 planning the branch read `0.92.0.0` and the fetched
`origin/main` `0.93.0.1`, later `0.93.0.2`; main moves again before this plan runs, and Phase 166.2
may ship first.)

**Task numbering after the D-24 amendment (2026-09-26).** Before the amendment this plan had two tasks.
`166.1-CONTEXT.md` (D-23 revision items 2, 3 and 6) still names them by the old numbers: its "Task 1"
(the census-line grep) is this plan's **Task 2**, and its "Task 2" (the CI-skip sweep and the version
derivation) is this plan's **Task 3**. Task 1 is now the D-24 tracer.

**D-24 (founder decision, 2026-09-26; CONTEXT D-24, ROADMAP `### Phase 166.1`).** Today
`_check_sharpe_sentinel` enters its body only under `r.std(ddof=1) > 0`. A short constant series sums to
an exactly-0 standard deviation and skips the check (ACCEPTED); a longer one leaves float residue, enters,
and reaches plan 01's D-04 residue branch (REJECTED). Plan 01 kept that length-decided boundary on
purpose (`166.1-01-SUMMARY.md` deviation 1, D-04 verdict preservation); D-24 supersedes that reason.
Measured at planning time by deleting only the `and r.std(ddof=1) > 0` clause and running the full suite
from `analytics-service/` (then restoring from a byte backup, `cmp` clean): `2 failed, 6589 passed, 90
skipped`, the two being `test_valid_daily_returns_passes` (the pinned 5-row fixture) and
`test_pii_undeclared_columns_dropped_from_preview` (its own 5 x 0.001 frame, asserting ok). A THIRD
dependant cannot fail and would silently lose its meaning: `test_correlation_id_slot_always_present`
calls `_daily_returns_df()` as its "success path", so after D-24 that envelope is a REJECTION and the
success path goes untested. And `test_dollar_form_sentinel_lets_normal_decimal_returns_through` says
"must validate cleanly" over a constant 0.01 x 20 series. `DEFAULT_RISK_FREE_DAILY` is `0.0`, so the
existing `r.mean() - DEFAULT_RISK_FREE_DAILY > 0` test is the "positive" test, and a constant ZERO series
(mean 0) is not rejected: it keeps today's verdict, as D-24 requires. The e2e CSV fixtures
(`e2e/csv-upload-flow.spec.ts`, `e2e/wizard-axe.spec.ts`) use varied returns, or already fail on a
backwards date; none depends on a constant CSV being accepted.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): D-24, red test first - an exactly constant positive daily-returns CSV is rejected at every length, with one message</name>
  <files>analytics-service/tests/test_csv_validator.py, analytics-service/services/csv_validator.py</files>
  <read_first>
    - .planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md - D-24, D-04
    - .planning/phases/166.1-qstatsrecompute/166.1-01-SUMMARY.md - deviation 1 (the reason D-24 supersedes)
    - analytics-service/services/csv_validator.py - `_check_sharpe_sentinel`, `DEFAULT_RISK_FREE_DAILY`
    - analytics-service/services/dispersion.py - `dispersion_is_residue`, `residue_floor`
    - analytics-service/tests/test_csv_validator.py - `_daily_returns_df`, `test_valid_daily_returns_passes`, `test_pii_undeclared_columns_dropped_from_preview`, `test_correlation_id_slot_always_present`, `test_dollar_form_sentinel_lets_normal_decimal_returns_through`, the module docstring
    - analytics-service/tests/test_dispersion_floor_sites.py - the S3 block (`_assert_residue`, `_assert_names_no_fabricated_number`)
  </read_first>
  <action>
    Per D-24. (1) RED. In `tests/test_csv_validator.py` add, with a comment block citing D-24:
    (a) `test_d24_constant_positive_daily_returns_rejected_at_every_length`, parametrized over lengths
    2, 3, 5, 20, 120 and 365 of a constant 0.001 series (through `validate_csv`, fmt `daily_returns`):
    `ok` is False and exactly one error carries rule `daily_sharpe_sentinel`; (b) one test that collects
    that error's message across ALL those lengths plus a constant yield taken from a compounding NAV
    (`nav = 100 * 1.0001 ** i`, `pct_change().dropna()`, fed as `daily_return`) and asserts the set of
    messages has exactly ONE member, which contains no run of 7 or more digits and no `e+` (the D-04
    no-number rule); (c) a control, `test_d24_constant_zero_series_keeps_its_verdict`: a constant 0.0
    series at lengths 5 and 120 carries NO `daily_sharpe_sentinel` error and `ok` is True (D-24 puts a
    constant zero out of scope; it must keep today's accepted verdict). Flip the pinned fixture:
    `test_valid_daily_returns_passes` keeps its happy-path name and meaning by moving to a VARIED series,
    and the 5-row constant 0.001 frame it used becomes an explicit REJECTED assertion (inside (a) or its
    own `test_d24_pinned_five_row_constant_fixture_is_rejected`), with a comment citing D-24 and noting it
    was ACCEPTED before this decision. Run `tests/test_csv_validator.py` against the UNEDITED
    `services/csv_validator.py` and record the pytest summary line and which parametrized ids FAILED: the
    short lengths (at least the 5-row case) must be RED, the long ones and the compounding-NAV case may
    already pass (they reach the residue branch today), and the zero control must PASS. A red run in which
    no D-24 case fails means the test cannot fail: STOP. Commit only the test file as
    `test(166.1-09): D-24 a constant positive CSV must be rejected at every length, red on the unedited sentinel`.
    (2) GREEN. In `_check_sharpe_sentinel` remove only the exact-zero gate: the block runs for
    `len(r) >= 2`, computes `r.std(ddof=1)` once, and routes an exactly-0 AND a residue standard deviation
    through `dispersion_is_residue` to the existing message (one message for both, no second string); keep
    `r.mean() - DEFAULT_RISK_FREE_DAILY > 0` as the "positive" test (so a constant zero is not rejected)
    and the Sharpe-over-std path for a real dispersion byte-for-byte. A NaN standard deviation (non-finite
    rows) must keep today's outcome, no sentinel error: `dispersion_is_residue` is False on NaN and the
    Sharpe comparison is False on NaN. Rewrite the block comment that says an exactly zero std "never
    enters this block, as before, so no verdict moves": it is now false; cite D-24 and say it supersedes
    plan 01's D-04 verdict-preservation reason (do not quote the removed clause literally in the new comment:
    the first verify command greps for it). (3) Keep every other test's intent: change
    `_daily_returns_df`'s DEFAULT series to a varied one (for example cycling 0.001, 0.002, -0.001, 0.003,
    -0.002; `daily_return: float | None = None`, a float still builds a constant series) so
    `test_valid_daily_returns_passes` and the success path of `test_correlation_id_slot_always_present`
    stay real successes; give `test_pii_undeclared_columns_dropped_from_preview` a varied series and
    replace its `row["daily_return"] == 0.001` assertion with membership in that series; give
    `test_dollar_form_sentinel_lets_normal_decimal_returns_through` a varied series around |0.01| so its
    "validate cleanly" comment stays true. Update the module docstring's "daily_sharpe_sentinel - covered
    by integration" to name the D-24 tests. Do not touch the S3 tests in `test_dispersion_floor_sites.py`
    except where one pins the exact-zero skip (none did at planning time: the measured run above left
    them green). (4) Drills, each from a byte backup of `services/csv_validator.py` restored and `cmp`
    clean, each recorded in the SUMMARY with its pytest summary line: (i) restore the removed
    exact-zero clause of the gate (the strictly-positive standard-deviation condition) exactly as it
    stood at plan start: the short-length D-24 cases go RED; (ii) change the positive test
    to `>= 0`: the zero control goes RED. Never `git checkout --`, `git restore` or `git stash`.
    Commit the source and the test edits of steps (2) and (3) as
    `feat(166.1-09): D-24 the Sharpe sentinel rejects a constant positive CSV at every length, one message`.
    Run the hygiene check after every `git add`; no CI-skip trailer token in either message.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && QZ_LOG="$(mktemp)" && { env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL "$QZ_VENV_PY" -m pytest -q -rA tests/test_csv_validator.py tests/test_dispersion_floor_sites.py > "$QZ_LOG" 2>&1; QZ_RC=$?; tail -n 15 "$QZ_LOG"; test "$QZ_RC" = 0; } && grep -Eq '^PASSED .*::test_d24_' "$QZ_LOG" && test "$(grep -c 'r.std(ddof=1) > 0' services/csv_validator.py)" = 0</automated>
    <fails_when>non-zero exit (127 = FAIL): any test in the two files failed or errored, no PASSED line names a d24 test (the new tests are absent or were deselected), or `services/csv_validator.py` still carries the `r.std(ddof=1) > 0` exact-zero gate; also " failed" or " error" in the pytest summary line</fails_when>
    <automated>QZ_RED="$(git log --format=%H -1 --grep='^test(166\.1-09): D-24')" && QZ_FEAT="$(git log --format=%H -1 --grep='^feat(166\.1-09): D-24')" && test -n "$QZ_RED" && test -n "$QZ_FEAT" && test "$(git rev-parse "$QZ_FEAT^")" = "$QZ_RED" && test -z "$(git diff --name-only "$QZ_RED^" "$QZ_RED" -- analytics-service/services/)" && test -n "$(git diff --name-only "$QZ_RED^" "$QZ_RED" -- analytics-service/tests/test_csv_validator.py)" && test -n "$(git diff --name-only "$QZ_RED" "$QZ_FEAT" -- analytics-service/services/csv_validator.py)"</automated>
    <fails_when>non-zero exit: no `test(166.1-09): D-24` or no `feat(166.1-09): D-24` commit is found by subject, the feat commit's parent is not the red commit, the red commit touched a file under `analytics-service/services/` (it must carry the test alone, so its run measured the UNEDITED sentinel) or did not touch `tests/test_csv_validator.py`, or the feat commit did not change `services/csv_validator.py`. The command anchors by commit subject, never by `HEAD~N`, so it gives the same verdict when re-run after Task 2's merge and Task 3's release</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY quotes the red run's pytest summary line on the unedited sentinel and names the D-24 parametrized ids that FAILED there (at least the 5-row case), with the zero control passing
    - the SUMMARY quotes both drills' pytest summary lines (gate restored: short lengths RED; `>= 0`: zero control RED) and the `cmp` restore for each
    - `grep -c 'D-24' analytics-service/tests/test_csv_validator.py` prints at least 2 (the new tests and the flipped fixture's comment) and `grep -c 'D-24' analytics-service/services/csv_validator.py` prints at least 1
    - `_check_sharpe_sentinel` carries one constant-series message string, not two
  </acceptance_criteria>
  <done>A constant positive daily-returns CSV is rejected at every length with one message, a constant zero keeps its verdict, and the proof was red before the fix.</done>
</task>

<task type="auto">
  <name>Task 2: contain origin/main, then the whole-phase Python gate sweep at one SHA, recorded verbatim</name>
  <files>none of this plan's own - a read-only sweep whose results go to the SUMMARY; a merge commit brings origin/main's files only if main moved</files>
  <read_first>
    - CLAUDE.md - "CHANGELOG discipline", "CI gate integrity", "Completion is HISTORICAL"
    - .planning/phases/166.1-qstatsrecompute/166.1-VALIDATION.md - row 166.1-09-02
    - .planning/phases/166.1-qstatsrecompute/166.1-01-SUMMARY.md, 166.1-01b-SUMMARY.md, 166.1-03-SUMMARY.md
  </read_first>
  <action>
    Per D-18 W2, D-20 B2 and D-23. (1) Apply the context's "Containing origin/main" rule. On a merge conflict: for a hunk this phase does not own, keep origin/main's side; inside a hunk in the Python file union that plans 01, 01b or 03 wrote, keep the call to the `services.dispersion` helper (`dispersion_is_residue`, `dispersion_is_real`, or the two correlation helpers plan 03 added) applied to whatever `main`'s code now looks like, and let the site tests decide; for `.planning/ROADMAP.md`, `.planning/STATE.md` or `.planning/state.json` keep BOTH `main`'s edits to other phases and this branch's 166.1 / 166.2 / 166.3 entries (taking one side silently drops either the split registration or another phase's progress). Record every conflicted path and its resolution in the SUMMARY. (2) After the merge (or the up-to-date finding), `git diff --stat MERGED_BASE HEAD -- src/` must print nothing: this branch carries no TypeScript change, and a non-empty result means the branch holds TS work this release must not claim, so STOP and report the paths. Also list, in the SUMMARY, which files under `analytics-service/services/` the merge brought from `main` (`git diff --stat PRE_MERGE MERGED_BASE -- analytics-service/services/` when a merge happened); a new standard-deviation or correlation division that `main` added in a file outside the union is reported to the orchestrator as a finding, not fixed here. (3) Record `git rev-parse HEAD` as SWEEP_SHA and `git merge-base HEAD origin/main` as SWEEP_BASE (equal to MERGED_BASE after a fresh fetch), then run the three verify commands below in order and paste the final summary line of each into the SUMMARY, plus: the `qstats-gate census:` line from the pytest output, the ruff BEFORE and AFTER line counts with the empty `comm -13` result, the anchor verifier's `scanned:` and `OK:` lines, the hygiene script's result, and a grep over this phase directory for the two verification fingerprint keys CLAUDE.md bans (expect zero). The anchor check is `--pending` (CI's `plan-anchor-verify` form) and NOT a glob over this phase's plans: executed plans may cite code this phase replaced on purpose (the exact-zero guards), so re-checking them could exit 1 forever; a pending plan of ANOTHER phase (including 166.2 or 166.3) that cites a symbol this phase moved or removed is a real cross-phase finding: STOP and report it with the plan path and the symbol. Any red result stops the plan; do not release on red. Long runs go in the background with a 600 s watchdog.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && QZ_LOG="$(mktemp)" && { env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL "$QZ_VENV_PY" -m pytest -q -n auto > "$QZ_LOG" 2>&1; QZ_RC=$?; tail -n 40 "$QZ_LOG"; test "$QZ_RC" = 0; } && grep -E '^qstats-gate census: [0-9]+ quantstats node' "$QZ_LOG" && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent --config-file=pyproject.toml services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py</automated>
    <fails_when>non-zero exit (127 = FAIL): pytest exited non-zero, or the `grep -E '^qstats-gate census: [0-9]+ quantstats node'` over the captured pytest log found no census line in its success form (an absent line, or only the `FAILED TO BUILD` form, exits 1), or mypy exited non-zero; also " failed" / " error" in the pytest summary, or mypy lacks "Success: no issues found"</fails_when>
    <automated>cd analytics-service && command -v ruff > /dev/null && git fetch -q origin main && QZ_BASE="$(git merge-base HEAD origin/main)" && QZ_TMP="$(mktemp -d)" && git -C .. archive "$QZ_BASE" analytics-service | tar -x -C "$QZ_TMP" && qz_ruff() { for f in services/dispersion.py services/metrics.py services/portfolio_optimizer.py services/csv_validator.py services/allocated_capital.py services/equity_reconstruction.py services/optimizer.py services/portfolio_risk.py services/analytics_runner.py services/match_engine.py services/strategy_matching.py routers/portfolio.py tests/test_metrics.py tests/dispersion_fixtures.py tests/test_dispersion_floor_sites.py tests/test_correlation_residue_sites.py tests/test_csv_validator.py; do test -e "$f" && ruff check --output-format=concise "$f"; done | grep -E '^[^ ]+:[0-9]+:[0-9]+: ' | sed -E 's/:[0-9]+:[0-9]+:/:/' | sort; } && (cd "$QZ_TMP/analytics-service" && qz_ruff) > "$QZ_TMP/before" && qz_ruff > "$QZ_TMP/after" && wc -l < "$QZ_TMP/before" && wc -l < "$QZ_TMP/after" && test -z "$(comm -13 "$QZ_TMP/before" "$QZ_TMP/after")"</automated>
    <fails_when>non-zero exit (ruff absent, the fetch or archive failed, or `comm -13` printed a line: a NEW ruff finding over the plan 01 + 01b + 03 + 09 union)</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && git fetch -q origin main && git merge-base --is-ancestor origin/main HEAD && test -z "$(git diff --name-only "$(git merge-base HEAD origin/main)" HEAD -- src/)" && node scripts/verify-plan-anchors.mjs --pending && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts</automated>
    <fails_when>non-zero exit (127 = FAIL; exit 1 from merge-base means main moved again and step (1) must be re-run before the sweep; a non-empty src/ diff means the branch carries TS work), or the anchor verifier prints "FAIL:" or does not end with an "OK:" line, or a hygiene violation line</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY records PRE_MERGE, MERGED_BASE, the merge commit sha (or "already up to date at MERGED_BASE"), every conflicted path with its resolution, SWEEP_SHA and SWEEP_BASE, and the summary line of each verify command
    - the first verify command exits 0, which requires its grep to find a `qstats-gate census: <N> quantstats node(s) ...` line in the captured pytest log; the SUMMARY quotes the line that grep printed
    - the SUMMARY quotes the ruff BEFORE and AFTER counts and the empty `comm -13` result, the anchor verifier's `scanned:` and `OK:` lines, and the empty `src/` diff
    - the grep over this phase directory for the two fingerprint keys CLAUDE.md bans prints zero matches
  </acceptance_criteria>
  <done>Every Python gate of the phase is green at one recorded SHA that contains origin/main and carries no src/ change.</done>
</task>

<task type="auto">
  <name>Task 3: the single chore(release) commit - VERSION and package.json byte-equal, one unified ENGINEFLOOR CHANGELOG entry mapped from an enumerated commit checklist</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <read_first>
    - CLAUDE.md - "CHANGELOG discipline (founder instruction, 2026-09-09)" and "CI gate integrity" trap 1
    - CHANGELOG.md - the top entry (copy its heading separators byte-for-byte: a hyphen between version and date, an em-dash before the title)
    - VERSION, package.json
    - .planning/phases/166.1-qstatsrecompute/166.1-VALIDATION.md - row 166.1-09-03
    - .planning/phases/166.1-qstatsrecompute/166.1-01-SUMMARY.md, 166.1-01b-SUMMARY.md, 166.1-03-SUMMARY.md, this plan's Task 1 commits and, if present, the Phase 166.3 plan 01 SUMMARY named in the context
  </read_first>
  <action>
    Follow CLAUDE.md's mechanism exactly. (0) `git fetch origin main`, then re-read `origin/main`'s VERSION (context rule "Version", D-18 as amended: the new version is derived from `origin/main`'s alone). (1) `git log "$(git merge-base HEAD origin/main)"..HEAD --oneline --no-merges`: enumerate and COUNT every commit of this branch; keep the list as a checklist in the SUMMARY. List the merge commits separately (`7bfce8490`, which brought PR A #859 before wave 1, and Task 2's merge at MERGED_BASE if one happened); they bring other phases' work, which those phases' own entries cover, and map to one Notes bullet. The branch also carries planning-only commits for the 2026-09-26 split and for the 166.2 / 166.3 plans; map them to the split Notes bullet. (2) `git diff "$(git merge-base HEAD origin/main)"...HEAD`: read what changed; what a commit says and what it changed can diverge. (3) Group by theme before writing a line, in the repo's section vocabulary. Fixed: the Python variance sites S1-S8 (per D-05 for `portfolio_risk.compute_risk_decomposition`, D-06 for the re-pointed `optimizer.optimize_weights`, D-16 for the SQN block in `analytics_runner._compute_derived_trade_metrics`) and the correlation sites C1-C8 (per D-02, including the two `corrwith` matching sites), each now reading Phase 166's relative dispersion floor, so a compounding-NAV constant yield produces exactly what an all-zero series produces at that site (D-07). Changed (what users will see differ): optimizer suggestions no longer rank a constant-yield candidate on a fabricated ratio; the risk decomposition no longer splits a zero risk into shares; correlation cells and match status for constant-yield strategies show the existing honest absence instead of a residue correlation; the CSV Sharpe sentinel rejects an exactly constant positive daily-returns CSV at EVERY length (D-24, founder decision 2026-09-26): before this release a short constant series was ACCEPTED and a longer one REJECTED, the boundary being float summation, so an upload that used to pass can now be refused; one message for both, naming no number (D-04); the rule name and its label are unchanged, and a constant ZERO series keeps its verdict. Root cause: a standard deviation derived from a compounding NAV is about 1e-16, never exactly 0, so an exact-zero guard lets a ratio of about 1e15 through. Tests: the variance site file and the correlation site file with the shared fixture module, a red test per site from a compounding-NAV constant yield with a cent-rounded control on the other side of the floor, and the neuter, RED, restore drills (S6 is an honest pin, not a red test, per D-06); the D-24 tests in `test_csv_validator.py` (every length, a compounding-NAV yield, the constant-zero control), red on the unedited sentinel, and the fixtures that had relied on a constant CSV being accepted moved to varied series. Notes: (a) the floor's single home is `services/dispersion.py`, and `services/metrics.py` re-binds its three functions under the old private names (the constant lives only in `dispersion.py`, per D-23's 2026-09-26 revision) so its behaviour and tests stay byte-identical (D-03); (b) D-11: `strategy_verifications.metrics_snapshot` rows are point-in-time records and are not recomputed; (c) STORED `portfolio_analytics` values computed before this release, namely the correlation matrix, the risk decomposition and the average pairwise correlation, keep any residue value until that portfolio's next analytics compute, because the recompute touches `strategy_analytics` only; (d) Phase 166.3's recompute state at release time, counts only (read from its SUMMARY), or "founder-gated, pending" if it has not run (D-14); (e) the SQN residual (D-21 W1): S8 floors the SQN formula, but that function has had no production caller since Phase 106 (`b196de6c8`) and both current success writers store `trade_metrics` as NULL, so the fix changes no displayed SQN by itself; a STORED residue SQN (absolute value above 1e10) stays on its row until that row is rewritten, a ledger venue never recomputes on its own, a Phase 166.3 recompute clears it by NULLing the row's whole `trade_metrics` (which also empties that row's trade panel), and the Q6 counts before and after, split published / not published, are carried as counts only with the named phase any published residual was routed to, or "founder-gated, pending"; (f) no migration (D-13); (g) the 2026-09-26 split (D-23): this release is the Python half; the TypeScript half ships as Phase 166.2 COMPUTEONCE and the recompute runs as Phase 166.3 RECOMPUTE, and this entry claims neither; (h) the merges of `origin/main` (at `7bfce8490` and, if Task 2 merged, at MERGED_BASE); (i) D-24 supersedes plan 01's D-04 verdict-preservation reason for the exact-zero skip. The two D-24 commits map to the `### Changed` and Tests bullets. Do not name any TS file, TS site or TS module as changed by this release. (4) Set the new version in VERSION and in package.json's `"version"` by direct edit, never `npm version`. Write ONE entry at the top, `## [<version>] - <execution date> — ENGINEFLOOR: every Python ratio site reads the one dispersion floor, so a constant yield never produces a fabricated ratio`. (5) Cross-check that every commit from step 1 maps to at least one bullet; list the mapping in the SUMMARY. An unmapped commit is a defect, not "minor". (6) No credential-shaped literal, home path, username, strategy name or id, or metric value of a private strategy in the entry (D-12), and no CI-skip trailer token anywhere in the commit message, not even denied (this task's fourth verify command sweeps every message on the branch, merges included, not just this one). (7) Run CRITICAL-02 and the CI version gate (the second verify command; `origin/main` must resolve first). Remove the node_modules link, then commit exactly the three files as `chore(release): v<version> — ENGINEFLOOR` with the session's attribution lines.
  </action>
  <verify>
    <automated>test "$(tr -d '\n' < VERSION)" = "$(node -p 'require("./package.json").version')" && tr -d '\n' < VERSION | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$' && head -40 CHANGELOG.md | grep -Eq "^## \[$(tr -d '\n' < VERSION | sed 's/\./\\./g')\] - .* — ENGINEFLOOR: "</automated>
    <fails_when>non-zero exit: VERSION and package.json differ, VERSION is not 4-digit, or no ENGINEFLOOR heading for that version at the top of CHANGELOG.md</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/__tests__/critical-regressions.test.ts && test -f scripts/check-version-bump.mjs && git fetch -q origin main && git rev-parse --verify --quiet origin/main > /dev/null && node scripts/check-version-bump.mjs --self-test && node scripts/check-version-bump.mjs</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" / "No test files found", the self-test does not print "SELF-TEST PASSED", or the live run prints any "::error::" defect instead of "No defects"</fails_when>
    <automated>git fetch -q origin main && QZ_BASEV="$(git show origin/main:VERSION)" && QZ_BASEV="$(printf '%s' "$QZ_BASEV" | tr -d '\n')" && QZ_WANT="$(printf '%s\n' "$QZ_BASEV" | awk -F. 'NF==4{print $1"."($2+1)".0.0"}')" && test -n "$QZ_WANT" && test "$(tr -d '\n' < VERSION)" = "$QZ_WANT" && echo "version-derivation: origin/main=$QZ_BASEV release=$QZ_WANT"</automated>
    <fails_when>non-zero exit: origin/main's VERSION is unreadable or not 4-part, or this branch's VERSION is not origin/main's second component + 1 with the last two zeroed (a sibling release merged since the bump; re-run Task 3 as a new release commit). The ship step re-runs this exact command immediately before the PR opens.</fails_when>
    <automated>git fetch -q origin main && QZ_MSGS="$(git log origin/main..HEAD --format=%B)" && test -n "$QZ_MSGS" && n=$(printf '%s\n' "$QZ_MSGS" | grep -aEic '\[(ci skip|skip ci|no ci|skip actions|actions skip)\]|skip-checks:true' || true); [ "$n" = 0 ]</automated>
    <fails_when>non-zero exit: the range is empty (a stale or missing origin/main), or any commit message on the branch, the release, Task 2's merge and every earlier commit included, carries a CI-skip trailer token in any of the six forms CLAUDE.md lists</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --name-only --format= <release sha>` lists exactly CHANGELOG.md, VERSION, package.json
    - the fourth verify command exits 0: no message in `git log origin/main..HEAD --format=%B` carries a CI-skip token, Task 2's merge message, the D-24 messages and the release message included
    - the third verify command exits 0 and the SUMMARY quotes its `version-derivation:` line
    - the SUMMARY carries the commit checklist with its count, the merge commits listed separately, the commit-to-bullet mapping (every commit mapped), and both VERSION readings with the chosen result
    - `grep -c 'ENGINEFLOOR' CHANGELOG.md` prints at least 1, and the new entry names no file under `src/` as changed
    - `test -L node_modules` is false at commit time
  </acceptance_criteria>
  <done>One release commit carries a byte-equal 4-digit bump and a unified ENGINEFLOOR entry that maps every branch commit and records the recompute state as counts only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit message / CHANGELOG -> public repo and CI | a skip token suppresses CI even when quoted; CHANGELOG is scanned by gitleaks on push to main; the repo is public |
| sweep SHA -> the shipped tree | a sweep on a tree without origin/main certifies a tree that will not ship |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.1-12 | Tampering (CI suppression) | every commit message on the branch | high | mitigate | Task 3's fourth verify command greps every message in `origin/main..HEAD` (Task 1's D-24 commits, Task 2's merge and the release included) for all six token forms listed in CLAUDE.md |
| T-166.1-35 | Repudiation (a duplicated version) | VERSION | medium | mitigate | Task 3's third verify command derives the version from `origin/main`'s at release time and is re-run immediately before the PR opens |
| T-166.1-13 | Information disclosure | CHANGELOG entry | medium | mitigate | the recompute and SQN residual are counts only; no strategy id, name, metric value, home path, username or credential-shaped literal (D-12); hygiene check in Task 2 |
| T-166.1-14 | Repudiation (unshipped or over-claimed fact) | CHANGELOG | medium | mitigate | enumerated commit checklist with a commit-to-bullet mapping in the SUMMARY; the entry names no `src/` file, so it cannot claim Phase 166.2's TS work |
| T-166.1-36 | Tampering (a verdict change with no red proof) | `_check_sharpe_sentinel` (D-24) | medium | mitigate | Task 1 commits the D-24 test ALONE first and records it RED on the unedited sentinel; its second verify command asserts the red commit touched no `services/` file; two neuter drills prove the length cases and the zero control can each fail |
| T-166.1-34 | Repudiation (a green sweep on the wrong tree) | the sweep | medium | mitigate | the sweep fetches, merges origin/main itself if main moved, asserts `git merge-base --is-ancestor origin/main HEAD` and an empty `src/` diff in its last verify command, and records SWEEP_SHA |
</threat_model>

<verification>
- Both Task 1 verify commands, all three Task 2 verify commands and all four Task 3 verify commands exit 0 (VALIDATION rows 166.1-09-01, 166.1-09-02 and 166.1-09-03).
</verification>

<success_criteria>
- An exactly constant positive daily-returns CSV is rejected at every length with one message, a constant zero keeps its verdict, and the proof was red first (D-24); every Python gate of the phase is green at one SHA containing origin/main; at most one merge commit and exactly one release commit; VERSION equals package.json and is origin/main's next minor; no message on the branch carries a CI-skip token; the ENGINEFLOOR entry maps every branch commit, records Phase 166.3's recompute state as counts only, and claims no TypeScript change.
</success_criteria>

<output>
Create `.planning/phases/166.1-qstatsrecompute/166.1-09-SUMMARY.md` when done.
</output>
