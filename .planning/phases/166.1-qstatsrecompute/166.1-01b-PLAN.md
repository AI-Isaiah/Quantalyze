---
phase: 166.1-qstatsrecompute
plan: 01b
type: execute
wave: 2
depends_on: ["166.1-01"]
files_modified:
  - analytics-service/services/analytics_runner.py
  - analytics-service/tests/test_dispersion_floor_sites.py
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A per-trade R-multiple set of identical losses (21 losses of 7.7) gives SQN None from analytics_runner._compute_derived_trade_metrics, the same as 21 identical losses of 1.0 do today, while 20 losses alternating 7.7 / 7.71 still give a finite SQN (D-16, site S8)"
    - "Every other key _compute_derived_trade_metrics returns is identical between the unedited and the edited function on all three inputs (D-16)"
    - "The S8 test was neutered, observed RED, and restored from a byte backup checked with cmp; the S1-S8 drill table is complete across plans 01 and 01b"
    - "The SUMMARY records, as measured fact, that _compute_derived_trade_metrics has no production caller since Phase 106 (b196de6c8), so S8 changes no stored or displayed SQN by itself; stored residue SQN is plan 166.3-01's Q6 count (D-21 W1)"
    - "The full analytics-service suite, CI's strict mypy and ruff with no NEW finding over this plan's two files are green, and the qstats-gate census line is unchanged"
  artifacts:
    - path: analytics-service/tests/test_dispersion_floor_sites.py
      provides: "the S8 red tests (test_s8_sqn_...), added beside plan 01's S1-S7"
  key_links:
    - from: analytics-service/services/analytics_runner.py
      to: analytics-service/services/dispersion.py
      via: "dispersion_is_residue(std_r, mean_r) in the SQN block"
      pattern: "dispersion_is_residue"
---

<objective>
Move the SQN block S8 in `analytics_runner._compute_derived_trade_metrics` onto Phase 166's floor
(D-16), with a red test and a drill, then run the plan's pytest, mypy and ruff gates. Split out of plan
01 by D-21 W3 so plan 01 fits the context budget; the task is unchanged from plan 01's former Task 3.

Purpose: 21 identical per-trade losses of 7.7 give an SQN of -4.03e16 today, because the R-multiple
standard deviation is float residue, not exactly 0. Measured at `df4d9921d` (D-21 W1): the function has
had NO production caller since Phase 106 (`b196de6c8`) and both current success writers store
`trade_metrics` as NULL, so this fix corrects the formula for any future caller and changes no stored
value; stored residue SQN is counted and routed by plan 166.3-01 (Q6).
Output: the edited SQN block and the S8 tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md
Also read `.planning/phases/166.1-qstatsrecompute/166.1-01-SUMMARY.md` (the floor's final public names in
`services/dispersion.py`, the S1-S7 drill table this plan completes, and the qstats-gate census BEFORE
reading).

**Environment.** Identical to plan 01: Python runs from the worktree's `analytics-service/` directory
only (a repo-root run misses cassettes and makes live broker calls). The interpreter is the MAIN
checkout's venv, resolved at run time and never written into a file (`QZ_WT` / `QZ_VENV_PY` as in the
verify commands). Exit 127 is a FAIL, never a pass. No database, no credential, no uvicorn (a local
uvicorn claims real PROD compute jobs). ruff is on PATH, not in the venv. Every pytest run is prefixed
with `env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL`.

**Wave.** Wave 2, after plan 01 (it imports `services/dispersion.py` and appends to plan 01's
`tests/test_dispersion_floor_sites.py`). It shares no file with plan 03 (wave 2), whose files are
`services/dispersion.py`, `portfolio_risk.py`, `portfolio_optimizer.py`, `match_engine.py`,
`strategy_matching.py`, `routers/portfolio.py` and `tests/test_correlation_residue_sites.py`.

**ruff rule (no NEW findings).** Plan 01's mechanism over this plan's two files: BEFORE is ruff over the
merge-base tree exported with `git archive` into a `mktemp -d` directory, AFTER is the worktree, line
and column stripped, sorted, and `comm -13 BEFORE AFTER` must print nothing. `analytics_runner.py`
already carries findings; they stay untouched.

**Drill rule.** Byte backup taken immediately before the edit, under a session-scratchpad name that
includes `166.1-01b`, RED observed, `cp` restore, `cmp` proof. Never `git checkout --`, `git restore` or
`git stash`.

**Symbols this plan edits (cite by symbol; line numbers drift).**
<!-- verify-symbol: analytics-service/services/analytics_runner.py _compute_derived_trade_metrics -->
<!-- verify-symbol: analytics-service/services/analytics_runner.py std_r -->
<!-- verify-symbol: analytics-service/services/analytics_runner.py SQN_TRADE_COUNT_CAP -->
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): the SQN block S8 reads the shared floor end-to-end, with a red test and a drill</name>
  <files>analytics-service/services/analytics_runner.py, analytics-service/tests/test_dispersion_floor_sites.py</files>
  <read_first>
    - analytics-service/services/analytics_runner.py - `_compute_derived_trade_metrics` in full (the METRICS-08 SQN block, `SQN_TRADE_COUNT_CAP`, `_finite_pnl`, and the T-12-05-03 docstring paragraph about guarded divisors) and the module's imports
    - analytics-service/services/dispersion.py - `dispersion_is_residue` (plan 01's public name)
    - analytics-service/tests - the four `test_*.py` files that call `_compute_derived_trade_metrics` (`test_analytics_runner.py`, `test_metrics_minigolden.py`, `test_metrics_parity.py`, `test_position_reconstruction.py`; measured 2026-09-25 with `grep -rl --include='test_*.py'`) and their SQN tests, including the golden fixture that self-anchors SQN (it must stay green). `tests/fixtures/regen_golden.py` also names the function; it is a regeneration SCRIPT, never a pytest target (CONTEXT D-18)
    - .planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md - the A2 addendum (S8 measurement and the re-sweep's not-in-class table)
    - .planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md - D-07, D-16, D-21 (W1: no production caller; W3: this split)
  </read_first>
  <behavior>
    - Residue: 21 per-trade losses of 7.7 (and 37 of 7.7), with `avg_losing_trade` passed as their float mean, give `sqn` None; today they give about -4.03e16 and -5.40e16 (measured 2026-09-25)
    - D-07 control: 21 losses of 1.0 (an exactly representable mean) give `sqn` None today and after the fix, and the residue case's `sqn` EQUALS it
    - Real dispersion: 20 losses alternating 7.7 / 7.71 plus one 7.7 give a finite `sqn`
    - Every other key the function returns (`expectancy`, `risk_reward_ratio`, `weighted_risk_reward_ratio`, `profit_factor_long`, `profit_factor_short`) is identical between the unedited and the edited function on all three inputs (compute both in the test from one input; never hard-code them)
  </behavior>
  <action>
    Per D-16 and D-21 W3. (0) Run `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-01b-PLAN.md` (halt on non-zero), and confirm `analytics-service/services/dispersion.py` defines `dispersion_is_residue` (plan 01 landed; halt otherwise). (1) Record the production-caller fact for the SUMMARY: `grep -rn "_compute_derived_trade_metrics" analytics-service --include='*.py' | grep -v '/tests/'` lists only the definition, docstrings and comments (no call); if a production CALL now exists, record it and say so in the SUMMARY, because plan 166.3-01's Q6 reasoning (D-21 W1) then needs re-reading by the orchestrator. (2) Write the S8 tests in `tests/test_dispersion_floor_sites.py`, each named `test_s8_sqn_...`, driving `_compute_derived_trade_metrics` with an empty volume dict and a positions dict carrying `win_rate` 0, `avg_winning_trade` 0, `avg_losing_trade` equal to the float mean of the losses, `winners_count` 0, `losers_count`, and `realized_pnl_per_trade` rows `{"realized_pnl": p, "side": "long"}`. Observe them RED on the unedited function and record the RED ids. (3) Edit the SQN block only: import `dispersion_is_residue` from `services.dispersion`; keep `var_r`; compute `std_r = math.sqrt(var_r)` when `var_r > 0` else 0.0 as today; publish `sqn` only when NOT `dispersion_is_residue(std_r, mean_r)`. An exactly-zero `std_r` is residue, so today's `std_r > 0` arm is subsumed; do not keep both tests. Add one comment line naming Phase 166.1 D-16 and the measured -4.03e16. Leave the Sortino-like and weighted-R:R guards alone (research A2 addendum: not in class). (4) Drill: byte backup of `analytics_runner.py` under a `166.1-01b` scratchpad name, restore the old `if std_r > 0:` guard, record RED, `cp` back, prove with `cmp`, and add the S8 row to the drill table (the SUMMARY carries plan 01's S1-S7 rows by reference and this S8 row).
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL "$QZ_VENV_PY" -m pytest tests/test_dispersion_floor_sites.py -q -k "sqn or s8" && env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL "$QZ_VENV_PY" -m pytest tests/test_analytics_runner.py tests/test_metrics_minigolden.py tests/test_metrics_parity.py tests/test_position_reconstruction.py -q && test "$(grep -rl --include='test_*.py' "_compute_derived_trade_metrics" tests | sort | tr '\n' ' ')" = "tests/test_analytics_runner.py tests/test_dispersion_floor_sites.py tests/test_metrics_minigolden.py tests/test_metrics_parity.py tests/test_position_reconstruction.py "</automated>
    <fails_when>non-zero exit (127 = FAIL), " failed" / " error" in the pytest summary, "no tests ran" / "deselected" covering every test for the `-k` selection, or the `test_*.py` grep no longer names exactly the four literal files plus `tests/test_dispersion_floor_sites.py` (a new caller appeared or one moved: add it to the literal list, never pass a grep result to pytest)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'services.dispersion' analytics-service/services/analytics_runner.py` prints at least 1, and `grep -v '^\s*#' analytics-service/services/analytics_runner.py | grep -c 'if std_r > 0:'` prints 0
    - the SUMMARY records the S8 tests RED before the fix (the RED ids), the S8 drill row (neuter, RED ids, `cmp` result), and the production-caller grep result (D-21 W1)
  </acceptance_criteria>
  <done>S8 reads the shared floor, with a test observed red and a drill proven by cmp.</done>
</task>

<task type="auto">
  <name>Task 2: the plan's full-suite, strict mypy and no-new-finding ruff gates, and the qstats-gate census check</name>
  <files>none - gates only; results go to the SUMMARY</files>
  <read_first>
    - .planning/phases/166.1-qstatsrecompute/166.1-01-SUMMARY.md - the qstats-gate census BEFORE reading and plan 01's gate lines
  </read_first>
  <action>
    Per D-21 W3 (the gates plan 01's former Task 3 ran, over this plan's files). Run the two verify commands below (long runs in the background with a 600 s watchdog), then the qstats-gate census line (from the full-suite output), and check it equals plan 01's BEFORE reading. Any red result stops the plan. Record in the SUMMARY the re-sweep result: S8 was the only new in-class hit, with the not-in-class table referenced by name (RESEARCH A2 addendum).
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent --config-file=pyproject.toml services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py</automated>
    <fails_when>non-zero exit (127 = FAIL), or " failed" / " error" in the pytest summary, or mypy lacks "Success: no issues found"</fails_when>
    <automated>cd analytics-service && command -v ruff > /dev/null && git fetch -q origin main && QZ_BASE="$(git merge-base HEAD origin/main)" && QZ_TMP="$(mktemp -d)" && git -C .. archive "$QZ_BASE" analytics-service | tar -x -C "$QZ_TMP" && qz_ruff() { for f in services/analytics_runner.py tests/test_dispersion_floor_sites.py; do test -e "$f" && ruff check --output-format=concise "$f"; done | grep -E '^[^ ]+:[0-9]+:[0-9]+: ' | sed -E 's/:[0-9]+:[0-9]+:/:/' | sort; } && (cd "$QZ_TMP/analytics-service" && qz_ruff) > "$QZ_TMP/before" && qz_ruff > "$QZ_TMP/after" && wc -l < "$QZ_TMP/before" && wc -l < "$QZ_TMP/after" && test -z "$(comm -13 "$QZ_TMP/before" "$QZ_TMP/after")"</automated>
    <fails_when>non-zero exit (ruff absent, the fetch or archive failed, or `comm -13` printed a line: a NEW ruff finding)</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY quotes the full-suite summary line, the mypy "Success" line, the ruff BEFORE and AFTER line counts with the empty `comm -13` result, and the qstats-gate census line (identical to plan 01's BEFORE)
    - `git diff --stat -- src/ supabase/` is empty
  </acceptance_criteria>
  <done>The full suite, strict mypy and the no-new-finding ruff gate are green with S8 on the floor, and the census is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analytics-service -> persisted / returned numbers | a fabricated SQN would reach `trade_metrics` for any future caller of `_compute_derived_trade_metrics` |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.1-02 | Tampering (integrity of a displayed number) | S8 SQN in `TradeAndPositionPanel` | medium | mitigate | `dispersion_is_residue(std_r, mean_r)`; red test on 21 identical losses of 7.7; drill; stored residue SQN counted and routed by plan 166.3-01 Q6 (D-21 W1) |
| T-166.1-05b | Tampering (PROD reached from a test) | pytest run location | high | mitigate | pytest only from `analytics-service/` (cassettes), the four TEST database variables unset; no uvicorn; no DB or credential in this plan |
</threat_model>

<verification>
- Task 1's verify and both Task 2 verify commands exit 0.
- `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-01b-PLAN.md` exits 0 before Task 1 starts.
</verification>

<success_criteria>
- Success criterion 3 for the SQN block (D-16): S8 uses Phase 166's floor with a test observed red and a drill; the full suite, strict mypy and ruff (no new finding) are green; the census is unchanged.
</success_criteria>

<output>
Create `.planning/phases/166.1-qstatsrecompute/166.1-01b-SUMMARY.md` when done. Stage it, then run the
hygiene check through the MAIN checkout's tsx by explicit path:
`QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && test -x "$QZ_MAIN/node_modules/.bin/tsx" && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK before committing.
</output>
