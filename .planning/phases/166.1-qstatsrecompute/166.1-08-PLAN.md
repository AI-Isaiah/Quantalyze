---
phase: 166.1-qstatsrecompute
plan: 08
type: execute
wave: 4
depends_on: ["166.1-07"]
files_modified:
  - VERSION
  - package.json
  - CHANGELOG.md
autonomous: true
requirements: [SC-1, SC-2, SC-3]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every gate the phase touched is green at one recorded SHA that contains origin/main: full analytics-service suite (with the four TEST database variables unset), CI's strict mypy, the qstats-gate census, ruff with no NEW finding against the merge-base over the union of the Python files plans 01, 01b and 03 touched, the full vitest suite, tsc, eslint over the union of the TS files plans 04-07 touched, the plan anchor verifier in CI's --pending form, and planning hygiene (D-18 W2, D-20 B2, D-20 W1)"
    - "VERSION and package.json carry the SAME 4-digit string, bumped on MEANING (user-visible values change) as a minor-level bump over the higher of this branch's and origin/main's VERSION, both re-read at ship time; npm version is never run (D-18)"
    - "ONE unified CHANGELOG entry covers EVERY commit on the branch, each mapped to at least one bullet, in the repo's own section vocabulary; it records plan 02's recompute state at release time as counts only (D-14), that stored portfolio_analytics values computed before this release keep any residue value until that portfolio's next analytics compute, that a stored residue SQN changes only when its row is rewritten (plan 02's Q6 counts, D-21 W1), and the D-17 / D-19 split with Phase 169"
    - "No commit message contains a CI-skip trailer token, not even in a sentence denying it"
  artifacts:
    - path: VERSION
      provides: "next minor 4-digit version"
    - path: package.json
      provides: "version byte-equal to VERSION"
    - path: CHANGELOG.md
      provides: "one unified entry for Phase 166.1 at the top"
  key_links:
    - from: VERSION
      to: package.json
      via: "src/__tests__/critical-regressions.test.ts CRITICAL-02 asserts byte equality"
      pattern: "\"version\""
---

<objective>
Run the whole-phase gate sweep at one SHA that already contains `origin/main` (plan 07 merged it), and
write the single `chore(release)` commit: VERSION, package.json and one unified CHANGELOG entry that maps
every branch commit.

Purpose: the repo rule (CLAUDE.md "CHANGELOG discipline") is that every ship writes an entry in the same
commit as the VERSION bump, built from an enumerated commit checklist. Per D-14 (as amended) this plan
waits on the code plans (through plan 07) and not on plan 02; plan 02's PROD recompute is recorded as
whatever state it is in, counts only. Per D-20 B2 the anchor check is CI's `--pending` form: executed
plans cite symbols this phase deletes by design.
Output: one release commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md
@VERSION
Also read `166.1-01-SUMMARY.md`, `166.1-01b-SUMMARY.md`, `166.1-03-SUMMARY.md`, `166.1-04-SUMMARY.md`, `166.1-05a-SUMMARY.md`,
`166.1-05b-SUMMARY.md`, `166.1-06a-SUMMARY.md`, `166.1-06b-SUMMARY.md`, `166.1-07-SUMMARY.md` (its
MERGED_BASE), and `166.1-02-SUMMARY.md` if it exists, in this phase directory.

**Environment.** Python from the worktree's `analytics-service/` directory only, with the MAIN
checkout's venv resolved at run time (`QZ_WT` / `QZ_VENV_PY`, as in plan 01; never written into a file;
exit 127 = FAIL). ruff is on PATH, not in the venv. TS tools through the main checkout's `node_modules`,
linked only for the run and removed before every commit when it is a symlink. No database, no credential.
Every pytest run is prefixed with
`env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL`.
Run `git fetch origin main` before ANY `git merge-base` against `origin/main`: a stale local ref makes
the merge-base, the version comparison and the commit checklist wrong without an error.

**Main moved since plan 07?** If, after `git fetch origin main`, `git merge-base --is-ancestor origin/main HEAD`
exits non-zero, `main` has moved since plan 07's merge: re-run plan 07 Task 2 (merge and re-prove, with
its SUMMARY row appended) before this plan's sweep. The sweep's SHA must contain `origin/main`.

**Version (CONTEXT D-18, Info, as amended by D-19).** Never take a version number from this plan. At
ship time run `git fetch origin main`, re-read `origin/main`'s `VERSION` (`git show origin/main:VERSION`)
and this branch's, take the higher, increment the second component and zero the last two. Record both
readings and the result in the SUMMARY. (Lineage only, not an input: at the 2026-09-26 revision
`origin/main` read `0.93.0.0` after Phase 164.9.1 merged, and this branch `0.92.0.0`; main moves again
before this plan runs.)

**The file unions (named literally; zsh does not word-split an unquoted variable).**
- Python, plans 01, 01b and 03 (`services/` and `tests/` relative to `analytics-service/`): `services/dispersion.py`,
  `services/metrics.py`, `services/portfolio_optimizer.py`, `services/csv_validator.py`,
  `services/allocated_capital.py`, `services/equity_reconstruction.py`, `services/optimizer.py`,
  `services/portfolio_risk.py`, `services/analytics_runner.py`, `services/match_engine.py`,
  `services/strategy_matching.py`, `routers/portfolio.py`, `tests/test_metrics.py`,
  `tests/dispersion_fixtures.py`, `tests/test_dispersion_floor_sites.py`,
  `tests/test_correlation_residue_sites.py`.
- TS, plans 04-07 (files that exist after the phase; `correlation-math.ts` and its test were deleted or
  moved by plan 05a): the 16 Tier-2 site files listed in plan 07's context, plus `src/lib/return-stats.ts`,
  `src/lib/return-stats.test.ts`, `src/lib/return-stats.sites.test.ts`, `src/lib/return-stats.sites-risk.test.ts`,
  `src/lib/return-stats.pearson-contract.test.ts`, `src/lib/return-stats.single-source.test.ts`,
  `src/__tests__/fixtures/dispersion-nav.ts`, `src/components/strategy/CompareCorrelationMatrix.residue.test.tsx`,
  `src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx`,
  `src/lib/portfolio-stats.test.ts`, `src/lib/factsheet/return-stats.sites.test.ts`,
  `src/lib/factsheet/return-stats.sites-joint.test.ts`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): whole-phase gate sweep at one SHA containing origin/main, recorded verbatim</name>
  <files>none - read-only sweep; results go to the SUMMARY</files>
  <read_first>
    - CLAUDE.md - "CHANGELOG discipline", "CI gate integrity", "Completion is HISTORICAL"
    - .planning/phases/166.1-qstatsrecompute/166.1-VALIDATION.md - the per-task map
    - .planning/phases/166.1-qstatsrecompute/166.1-07-SUMMARY.md - MERGED_BASE
  </read_first>
  <action>
    Per D-18 W2, D-20 B2 and D-20 W1. Run `git fetch origin main` first and apply the context's "Main moved since plan 07?" rule. Record `git rev-parse HEAD` as SWEEP_SHA and `git merge-base HEAD origin/main` as SWEEP_BASE, then run every verify command below and paste the final summary line of each into the SUMMARY, plus: the qstats-gate census line, the ruff BEFORE and AFTER line counts with the empty `comm -13` result, the anchor verifier's `scanned:` and `OK:` lines, and a grep over this phase directory for the two verification fingerprint keys CLAUDE.md bans (expect zero). The anchor check is `--pending` (CI's `plan-anchor-verify` form) and NOT a glob over this phase's plans: executed plans 04-06b cite symbols this phase deletes on purpose (`correlation-math.ts` `pearson`, `compute.ts` `pstdev`), so re-checking them would exit 1 forever; a pending plan of ANOTHER phase that cites a symbol this phase deleted is a real cross-phase finding: STOP and report it with the plan path and the symbol. Any red result stops the plan; do not release on red. Long runs go in the background with a 600 s watchdog.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent --config-file=pyproject.toml services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py</automated>
    <fails_when>non-zero exit (127 = FAIL), or " failed" / " error" in the pytest summary, or mypy lacks "Success: no issues found"</fails_when>
    <automated>cd analytics-service && command -v ruff > /dev/null && git fetch -q origin main && QZ_BASE="$(git merge-base HEAD origin/main)" && QZ_TMP="$(mktemp -d)" && git -C .. archive "$QZ_BASE" analytics-service | tar -x -C "$QZ_TMP" && qz_ruff() { for f in services/dispersion.py services/metrics.py services/portfolio_optimizer.py services/csv_validator.py services/allocated_capital.py services/equity_reconstruction.py services/optimizer.py services/portfolio_risk.py services/analytics_runner.py services/match_engine.py services/strategy_matching.py routers/portfolio.py tests/test_metrics.py tests/dispersion_fixtures.py tests/test_dispersion_floor_sites.py tests/test_correlation_residue_sites.py; do test -e "$f" && ruff check --output-format=concise "$f"; done | grep -E '^[^ ]+:[0-9]+:[0-9]+: ' | sed -E 's/:[0-9]+:[0-9]+:/:/' | sort; } && (cd "$QZ_TMP/analytics-service" && qz_ruff) > "$QZ_TMP/before" && qz_ruff > "$QZ_TMP/after" && wc -l < "$QZ_TMP/before" && wc -l < "$QZ_TMP/after" && test -z "$(comm -13 "$QZ_TMP/before" "$QZ_TMP/after")"</automated>
    <fails_when>non-zero exit (ruff absent, the fetch or archive failed, or `comm -13` printed a line: a NEW ruff finding over the plan 01 + 01b + 03 union)</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint "src/app/(dashboard)/compare/lib/holding-compare-adapter.ts" src/lib/sample-basis-ratios.ts src/lib/scenario.ts src/lib/diversification.ts src/components/strategy/CompareCorrelationMatrix.tsx "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx" src/lib/portfolio-stats.ts "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts" "src/app/(dashboard)/allocations/lib/scenario-stress.ts" src/lib/factsheet/compute.ts src/lib/factsheet/bootstrap.ts src/lib/factsheet/joint.ts src/lib/factsheet/rolling.ts src/lib/factsheet/build-payload.ts src/lib/factsheet/allocator.ts src/lib/factsheet/og-metrics.ts src/lib/return-stats.ts src/lib/return-stats.test.ts src/lib/return-stats.sites.test.ts src/lib/return-stats.sites-risk.test.ts src/lib/return-stats.pearson-contract.test.ts src/lib/return-stats.single-source.test.ts src/__tests__/fixtures/dispersion-nav.ts src/components/strategy/CompareCorrelationMatrix.residue.test.tsx "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx" src/lib/portfolio-stats.test.ts src/lib/factsheet/return-stats.sites.test.ts src/lib/factsheet/return-stats.sites-joint.test.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, any tsc error line, or any eslint error over the plan 04-07 union</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && git fetch -q origin main && git merge-base --is-ancestor origin/main HEAD && node scripts/verify-plan-anchors.mjs --pending && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts</automated>
    <fails_when>non-zero exit (127 = FAIL; exit 1 from merge-base means main moved and plan 07 Task 2 must be re-run), or the anchor verifier prints "FAIL:" or does not end with an "OK:" line, or a hygiene violation line</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY records SWEEP_SHA, SWEEP_BASE (equal to or a descendant of plan 07's MERGED_BASE) and the summary line of each check
    - the quoted pytest output contains a line starting `qstats-gate census:`
    - the SUMMARY quotes the ruff BEFORE and AFTER counts and the empty `comm -13` result, the eslint exit status over the named TS union, and the anchor verifier's `scanned:` and `OK:` lines
  </acceptance_criteria>
  <done>Every phase gate is green at one recorded SHA that contains origin/main.</done>
</task>

<task type="auto">
  <name>Task 2: the single chore(release) commit - VERSION and package.json byte-equal, one unified CHANGELOG entry mapped from an enumerated commit checklist</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <read_first>
    - CLAUDE.md - "CHANGELOG discipline (founder instruction, 2026-09-09)" and "CI gate integrity" trap 1
    - CHANGELOG.md - the top entry (copy its heading separators byte-for-byte: a hyphen between version and date, an em-dash before the title)
    - VERSION, package.json
    - .planning/phases/166.1-qstatsrecompute/166.1-01-SUMMARY.md, 166.1-01b-SUMMARY.md and, if present, 166.1-02-SUMMARY.md
  </read_first>
  <action>
    Follow CLAUDE.md's mechanism exactly. (0) `git fetch origin main`, then re-read `origin/main`'s VERSION and this branch's (context rule). (1) `git log "$(git merge-base HEAD origin/main)"..HEAD --oneline --no-merges`: enumerate and COUNT every commit of this branch (the merge commits from plan 07 and `7bfce8490` bring other phases' work, which their own entries already cover; list the merge commits separately with their MERGED_BASE and map them to one Notes bullet); keep the list as a checklist in the SUMMARY. (2) `git diff "$(git merge-base HEAD origin/main)"...HEAD`: read what changed. (3) Group by theme before writing, in the repo's section vocabulary: Fixed (the Python variance sites S1-S8 and correlation sites C1-C8, per D-02/D-05/D-06/D-16, and the TS sites per D-15/D-17), Changed (what users will see differ: optimizer suggestions no longer rank a constant-yield candidate, risk decomposition no longer splits a zero risk, correlation cells and match status for constant-yield strategies, the TS-computed Sharpe / correlation / beta / information ratio / ratio cells on /compare, the scenario composer and benchmark panel, the Risk tab and the factsheet, the OG card's computed Sharpe, the CSV sentinel message names no number per D-04), Removed (`correlation-math.ts`, the dead `rollingCorrelation` and `computeRollingMetric`, and every private copy of the Sharpe, Pearson, beta, mean and pstdev formulas the sites carried), Root cause (a standard deviation derived from a compounding NAV is about 1e-16, never exactly 0; and twenty private copies of the same ratio formula, which is why D-17 computes each once), Tests (the Python site files and shared fixture module, the TS site files and fixture, the compute-once source-scan gate with its whole-tree matcher and count-pinned allowlist, the drill tables; S6 is a pin), Notes (the floor's single home in each language and the metrics re-bind; `src/lib/return-stats.ts` as the one TS home; the D-17 / D-19 split with Phase 169 (T14's persisted read is 169 plan 04's, its computed arm is this phase's; 169 plan 14's zoom KPIs inherit the floored `compute`), naming the items CONTEXT D-17 records for the orchestrator; D-11 verification snapshots not recomputed; STORED `portfolio_analytics` values computed before this release, namely the correlation matrix, the risk decomposition and the average pairwise correlation, keep any residue value until that portfolio's next analytics compute, because plan 02 recomputes `strategy_analytics` only; plan 02's recompute state at release time as counts only, or "founder-gated, pending" if it has not run; the SQN residual (D-21 W1): S8 floors `_compute_derived_trade_metrics`'s SQN, but that function has had no production caller since Phase 106 (`b196de6c8`) and both current success writers store `trade_metrics` as NULL, so the fix changes no displayed SQN by itself; a STORED residue SQN (|sqn| above 1e10) stays on its row until that row is rewritten, a ledger venue never recomputes on its own, a plan 02 recompute clears it by NULLing the row's whole `trade_metrics` (which also empties that row's trade panel), and plan 02's Q6 counts, before and after, split published / not published, are carried as counts only, with the named phase any published residual was routed to, or "founder-gated, pending"; no migration; the merge of `origin/main` at MERGED_BASE). (4) Set the new version in VERSION and in package.json's `"version"` by direct edit, never `npm version`. Write ONE entry at the top, `## [<version>] - <execution date> — QSTATSRECOMPUTE: the last exact-zero dispersion guards go, each TS ratio is computed once, and PROD rows computed before Phase 166 are recomputed`. (5) Cross-check that every commit from step 1 maps to at least one bullet; list the mapping in the SUMMARY. (6) No credential-shaped literal, home path, username, strategy name or id in the entry, and no CI-skip trailer token anywhere in the commit message. (7) Run CRITICAL-02 and the CI version gate (`node scripts/check-version-bump.mjs --self-test`, then `node scripts/check-version-bump.mjs`; confirm `origin/main` resolves first). Remove the node_modules link, then commit exactly the three files as `chore(release): v<version> — QSTATSRECOMPUTE` with the session's attribution lines.
  </action>
  <verify>
    <automated>test "$(tr -d '\n' < VERSION)" = "$(node -p 'require("./package.json").version')" && tr -d '\n' < VERSION | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$' && head -40 CHANGELOG.md | grep -Eq "^## \[$(tr -d '\n' < VERSION | sed 's/\./\\./g')\] - "</automated>
    <fails_when>non-zero exit: VERSION and package.json differ, VERSION is not 4-digit, or no heading for that version at the top of CHANGELOG.md</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/__tests__/critical-regressions.test.ts && test -f scripts/check-version-bump.mjs && git fetch -q origin main && git rev-parse --verify --quiet origin/main > /dev/null && node scripts/check-version-bump.mjs --self-test && node scripts/check-version-bump.mjs</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" / "No test files found", the self-test does not print "SELF-TEST PASSED", or the live run prints any "::error::" defect instead of "No defects"</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --name-only --format= <release sha>` lists exactly CHANGELOG.md, VERSION, package.json
    - the release message, captured first (`QZ_MSG="$(git log -1 --format=%B <release sha>)" && test -n "$QZ_MSG"`), matches none of the six CI-skip token forms listed in CLAUDE.md under a case-insensitive grep
    - the SUMMARY carries the commit checklist with its count, the merge commits listed separately, the commit-to-bullet mapping (every commit mapped), and both VERSION readings with the chosen result
    - `grep -c 'QSTATSRECOMPUTE' CHANGELOG.md` prints at least 1
    - `test -L node_modules` is false at commit time
  </acceptance_criteria>
  <done>One release commit carries a byte-equal 4-digit bump and a unified entry that maps every branch commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit message / CHANGELOG -> public repo and CI | a skip token suppresses CI even when quoted; CHANGELOG is scanned by gitleaks on push to main |
| sweep SHA -> the shipped tree | a sweep on a tree without origin/main certifies a tree that will not ship |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.1-12 | Tampering (CI suppression) | release commit message | high | mitigate | acceptance greps the captured message for all six token forms |
| T-166.1-13 | Information disclosure | CHANGELOG entry | medium | mitigate | counts only for the recompute; no id, name, home path or credential-shaped literal; hygiene check in Task 1 |
| T-166.1-14 | Repudiation (unshipped fact) | CHANGELOG | medium | mitigate | enumerated commit checklist with a commit-to-bullet mapping in the SUMMARY |
| T-166.1-34 | Repudiation (a green sweep on the wrong tree) | the sweep | medium | mitigate | the sweep asserts `git merge-base --is-ancestor origin/main HEAD` after a fetch, and re-runs plan 07 Task 2 if main moved |
</threat_model>

<verification>
- All four Task 1 verify commands and both Task 2 verify commands exit 0.
</verification>

<success_criteria>
- Every phase gate is green at one SHA containing origin/main; one release commit; VERSION equals package.json; the entry maps every branch commit and records the recompute state as counts only.
</success_criteria>

<output>
Create `.planning/phases/166.1-qstatsrecompute/166.1-08-SUMMARY.md` when done.
</output>
