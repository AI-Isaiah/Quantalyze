---
phase: 166.1-qstatsrecompute
plan: 07
type: execute
wave: 3
depends_on: ["166.1-01", "166.1-03", "166.1-04", "166.1-05", "166.1-06"]
files_modified:
  - VERSION
  - package.json
  - CHANGELOG.md
autonomous: true
requirements: [SC-1, SC-2, SC-3]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every gate the phase touched is green at one recorded SHA: full analytics-service suite (with the four TEST database variables unset), CI's strict mypy, the qstats-gate census, ruff with no NEW finding over every Python file plans 01 and 03 touched, the full vitest suite, tsc, eslint over every TS file plans 04-06 touched, the plan anchor verifier and planning hygiene"
    - "VERSION and package.json carry the SAME 4-digit string, bumped on MEANING: user-visible values change (portfolio optimizer suggestions, risk decomposition, correlation cells, match status, the CSV sentinel message, SQN, and the TS-computed Sharpe, correlation, beta and ratio cells on /compare, the scenario composer and the factsheet), so a minor-level bump over the higher of this branch's and origin/main's VERSION; npm version is never run"
    - "ONE unified CHANGELOG entry covers EVERY commit on the branch, each mapped to at least one bullet, in the repo's own section vocabulary, and it records plan 02's recompute state at release time as counts only (D-14), and that stored portfolio_analytics values computed before this release (correlation matrix, risk decomposition, avg pairwise correlation) keep any residue value until that portfolio's next analytics compute"
    - "No commit message contains a CI-skip trailer token, not even in a sentence denying it"
  artifacts:
    - path: VERSION
      provides: "next minor 4-digit version"
    - path: package.json
      provides: "version byte-equal to VERSION"
    - path: CHANGELOG.md
      provides: "one unified entry for Phase 166.1 at the top"
  key_links:
    - from: VERSION
      to: package.json
      via: "src/__tests__/critical-regressions.test.ts CRITICAL-02 asserts byte equality"
      pattern: "\"version\""
---

<objective>
Run the whole-phase gate sweep at one SHA and write the single `chore(release)` commit: VERSION,
package.json and one unified CHANGELOG entry that maps every branch commit.

Purpose: the repo rule (CLAUDE.md "CHANGELOG discipline") is that every ship writes an entry in the same
commit as the VERSION bump, built from an enumerated commit checklist. Per D-14 (as amended) this plan waits
on the five code plans (01, 03, 04, 05, 06) and not on plan 02; plan 02's PROD recompute is recorded in
the entry as whatever state it is in, counts only.
Output: one release commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md
@VERSION
Also read `166.1-01-SUMMARY.md`, `166.1-03-SUMMARY.md`, `166.1-04-SUMMARY.md`, `166.1-05-SUMMARY.md`,
`166.1-06-SUMMARY.md`, and `166.1-02-SUMMARY.md` if it exists, in this phase directory.

**Environment.** Python from the worktree's `analytics-service/` directory only, with the MAIN
checkout's venv resolved at run time (`QZ_WT` / `QZ_VENV_PY`, as in plan 01; never written into a file;
exit 127 = FAIL). TS tools through the main checkout's `node_modules`, linked only for the run and
removed before every commit when it is a symlink. No database, no credential. Every pytest run is
prefixed with `env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL`.
Run `git fetch origin main` before ANY `git merge-base` against `origin/main`: a stale local ref makes
the merge-base, the version comparison and the commit checklist wrong without an error.

**Version at planning time:** `VERSION` = `0.91.0.0` on this branch and on `origin/main` (measured
2026-09-25). A phase ship moves the second component. Re-read both at execution; the new version is
the higher of the two with the second component incremented and the last two zeroed (at least
`0.92.0.0`).
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): whole-phase gate sweep at one SHA, recorded verbatim</name>
  <files>none - read-only sweep; results go to the SUMMARY</files>
  <read_first>
    - CLAUDE.md - "CHANGELOG discipline", "CI gate integrity", "Completion is HISTORICAL"
    - .planning/phases/166.1-qstatsrecompute/166.1-VALIDATION.md - the per-task map
  </read_first>
  <action>
    Run `git fetch origin main` first. Record `git rev-parse HEAD` as SWEEP_SHA, then run and paste the
    final summary line of each into the SUMMARY: the full `pytest -q -n auto` from `analytics-service/`
    (TEST database variables unset); CI's mypy command verbatim; the qstats-gate test with its census
    line; the ruff no-NEW-finding gate over the union of the Python files plans 01 and 03 touched (the
    same merge-base `git archive` mechanism plan 01 Task 3 uses, file list read from those two plans'
    `files_modified`, named literally in the loop); the full vitest suite, `tsc --noEmit`, and eslint over
    every `.ts`/`.tsx` file plans 04-06 touched (file list from their `files_modified`); the anchor
    verifier over this phase's PLAN files; the planning-hygiene check; a grep over this phase directory
    for the two verification fingerprint keys CLAUDE.md bans (expect zero). Any red result stops the
    plan; do not release on red. Long runs go in the background with a 600 s watchdog.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent --config-file=pyproject.toml services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py</automated>
    <fails_when>non-zero exit (127 = FAIL), or " failed" / " error" in the pytest summary, or mypy lacks "Success: no issues found"</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run && ./node_modules/.bin/tsc --noEmit</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or any tsc error line</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-0[0-9]-PLAN.md && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), or the anchor verifier prints "FAIL:", or a hygiene violation line</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY records SWEEP_SHA and the summary line of each check
    - the quoted pytest output contains a line starting `qstats-gate census:`
  </acceptance_criteria>
  <done>Every phase gate is green at one recorded SHA.</done>
</task>

<task type="auto">
  <name>Task 2: the single chore(release) commit - VERSION and package.json byte-equal, one unified CHANGELOG entry mapped from an enumerated commit checklist</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <read_first>
    - CLAUDE.md - "CHANGELOG discipline (founder instruction, 2026-09-09)" and "CI gate integrity" trap 1
    - CHANGELOG.md - the top entry (copy its heading separators byte-for-byte: a hyphen between version and date, an em-dash before the title)
    - VERSION, package.json
    - .planning/phases/166.1-qstatsrecompute/166.1-01-SUMMARY.md and, if present, 166.1-02-SUMMARY.md
  </read_first>
  <action>
    Follow CLAUDE.md's mechanism exactly. (0) `git fetch origin main`, then re-read `origin/main`'s VERSION. (1) `git log "$(git merge-base HEAD origin/main)"..HEAD --oneline`: enumerate and COUNT every commit; keep the list as a checklist in the SUMMARY. (2) `git diff "$(git merge-base HEAD origin/main)"...HEAD`: read what changed. (3) Group by theme before writing, in the repo's section vocabulary: Fixed (the Python variance sites S1-S8 and correlation sites C1-C8, per D-02/D-05/D-06/D-16, and the TS sites T1-T20 per D-15), Changed (what users will see differ: optimizer suggestions no longer rank a constant-yield candidate, risk decomposition no longer splits a zero risk, correlation cells and match status for constant-yield strategies, SQN on identical-loss trade books, the TS-computed Sharpe / correlation / beta / ratio cells on /compare, the scenario composer and the factsheet, the CSV sentinel message names no number per D-04), Root cause (a standard deviation derived from a compounding NAV is about 1e-16, never exactly 0), Tests (the Python site files and shared fixture module, the TS site files and fixture, the drill tables; S6 is a pin), Notes (the floor's single home in each language and the metrics re-bind; D-11 verification snapshots not recomputed; STORED `portfolio_analytics` values computed before this release, namely the correlation matrix, the risk decomposition and the average pairwise correlation, keep any residue value until that portfolio's next analytics compute, because plan 02 recomputes `strategy_analytics` only; plan 02's recompute state at release time as counts only, or "founder-gated, pending" if it has not run; no migration). (4) Set the new version (context rule) in VERSION and in package.json's `"version"` by direct edit, never `npm version`. Write ONE entry at the top, `## [<version>] - <execution date> — QSTATSRECOMPUTE: the last exact-zero dispersion guards go, and PROD rows computed before Phase 166 are recomputed`. (5) Cross-check that every commit from step 1 maps to at least one bullet; list the mapping in the SUMMARY. (6) No credential-shaped literal, home path, username, strategy name or id in the entry, and no CI-skip trailer token anywhere in the commit message. (7) Run CRITICAL-02 and the CI version gate (`node scripts/check-version-bump.mjs --self-test`, then `node scripts/check-version-bump.mjs`; confirm `origin/main` resolves first). Remove the node_modules link, then commit exactly the three files as `chore(release): v<version> — QSTATSRECOMPUTE` with the session's attribution lines.
  </action>
  <verify>
    <automated>test "$(tr -d '\n' < VERSION)" = "$(node -p 'require("./package.json").version')" && tr -d '\n' < VERSION | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$' && head -40 CHANGELOG.md | grep -Eq "^## \[$(tr -d '\n' < VERSION | sed 's/\./\\./g')\] - "</automated>
    <fails_when>non-zero exit: VERSION and package.json differ, VERSION is not 4-digit, or no heading for that version at the top of CHANGELOG.md</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/__tests__/critical-regressions.test.ts && test -f scripts/check-version-bump.mjs && git fetch -q origin main && git rev-parse --verify --quiet origin/main > /dev/null && node scripts/check-version-bump.mjs --self-test && node scripts/check-version-bump.mjs</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" / "No test files found", the self-test does not print "SELF-TEST PASSED", or the live run prints any "::error::" defect instead of "No defects"</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --name-only --format= <release sha>` lists exactly CHANGELOG.md, VERSION, package.json
    - the release message, captured first (`QZ_MSG="$(git log -1 --format=%B <release sha>)" && test -n "$QZ_MSG"`), matches none of the six CI-skip token forms listed in CLAUDE.md under a case-insensitive grep
    - the SUMMARY carries the commit checklist with its count and the commit-to-bullet mapping (every commit mapped)
    - `grep -c 'QSTATSRECOMPUTE' CHANGELOG.md` prints at least 1
    - `test -L node_modules` is false at commit time
  </acceptance_criteria>
  <done>One release commit carries a byte-equal 4-digit bump and a unified entry that maps every branch commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit message / CHANGELOG -> public repo and CI | a skip token suppresses CI even when quoted; CHANGELOG is scanned by gitleaks on push to main |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.1-12 | Tampering (CI suppression) | release commit message | high | mitigate | acceptance greps the captured message for all six token forms |
| T-166.1-13 | Information disclosure | CHANGELOG entry | medium | mitigate | counts only for the recompute; no id, name, home path or credential-shaped literal; hygiene check in Task 1 |
| T-166.1-14 | Repudiation (unshipped fact) | CHANGELOG | medium | mitigate | enumerated commit checklist with a commit-to-bullet mapping in the SUMMARY |
</threat_model>

<verification>
- All three Task 1 verify commands and both Task 2 verify commands exit 0.
</verification>

<success_criteria>
- One release commit; VERSION equals package.json; the entry maps every branch commit and records the recompute state as counts only.
</success_criteria>

<output>
Create `.planning/phases/166.1-qstatsrecompute/166.1-07-SUMMARY.md` when done.
</output>
