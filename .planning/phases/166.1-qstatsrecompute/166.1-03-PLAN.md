---
phase: 166.1-qstatsrecompute
plan: 03
type: execute
wave: 2
depends_on: ["166.1-01"]
files_modified:
  - analytics-service/services/dispersion.py
  - analytics-service/services/portfolio_risk.py
  - analytics-service/services/portfolio_optimizer.py
  - analytics-service/services/match_engine.py
  - analytics-service/services/strategy_matching.py
  - analytics-service/routers/portfolio.py
  - analytics-service/tests/test_correlation_residue_sites.py
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No correlation site outside metrics.py reports a correlation for a leg whose dispersion is float residue: each of C1-C8 emits exactly what it emits today for an exactly constant (all-zero) leg at the same output key (D-02, D-07)"
    - "Two strategies with the SAME compounding constant yield never match each other in find_matched_strategy or in the verify_strategy matching block, while a noisy target and a near-identical noisy candidate still match (D-02)"
    - "find_matched_strategy returns None EXPLICITLY when no candidate column disperses, and logs no 'matching failed' warning for that case"
    - "A cent-rounded 1% APY NAV leg against noise still gives a finite correlation from the pairwise helper, so widening the floor goes red (D-07)"
    - "Every C1-C8 test was written first and observed RED on the unedited site, and every site was neutered, observed RED and restored from a byte backup proven with cmp"
    - "ruff reports no NEW finding over the touched Python files, and the full analytics-service suite (TEST database variables unset) and CI's strict mypy are green"
  artifacts:
    - path: analytics-service/services/dispersion.py
      provides: "pairwise_correlation_or_none and dispersing_corrwith, next to plan 01's floor"
    - path: analytics-service/tests/test_correlation_residue_sites.py
      provides: "red tests for C1-C8"
  key_links:
    - from: analytics-service/routers/portfolio.py
      to: analytics-service/services/dispersion.py
      via: "the benchmark correlation (C6) and the verify_strategy matching block (C7) call the shared helpers"
      pattern: "services.dispersion"
    - from: analytics-service/services/strategy_matching.py
      to: analytics-service/services/dispersion.py
      via: "dispersing_corrwith replaces the raw corrwith"
      pattern: "dispersing_corrwith"
---

<objective>
Close the correlation half of the class: every site outside `metrics.py` that divides by a leg's
standard deviation through pandas `corr` / `corrwith` with no guard (C1-C8) stops reporting a
correlation for a leg whose dispersion is float residue.

Purpose: pandas returns a residue correlation instead of NaN over a compounding constant yield
(measured: `compute_correlation_matrix` gives -0.0362 for a 1e-4 NAV yield against noise). Phase 166
round 2 fixed the `metrics.py` siblings for exactly this reason. Split out of plan 01 by the plan-check
revision (CONTEXT D-02 as amended); scope unchanged.
Output: two helpers in `services/dispersion.py`, six edited modules, one test file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md
@.planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md
Also read `.planning/phases/166.1-qstatsrecompute/166.1-01-SUMMARY.md` (the floor's final names, the
fixture module's public names, and the qstats-gate census BEFORE reading).

**Environment.** Identical to plan 01: Python from the worktree's `analytics-service/` only, the MAIN
checkout's venv resolved at run time (`QZ_WT` / `QZ_VENV_PY`, never written into a file, exit 127 is a
FAIL), no database, no credential, no uvicorn, ruff on PATH. Every pytest run is prefixed with
`env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL`.
Drills: byte backup in the session scratchpad under a `166.1-03` name, neuter, RED, `cp` restore,
`cmp`. Never `git checkout --`, `git restore` or `git stash`.

**ruff rule (no NEW findings).** Same mechanism as plan 01 Task 2 (the gates moved there from its former Task 3, D-21 W3): BEFORE is ruff over the merge-base
tree exported with `git archive`, AFTER is ruff in the worktree, both line-stripped and sorted, and
`comm -13 BEFORE AFTER` must print nothing. Pre-existing findings stay untouched.

**Symbols this plan edits (cite by symbol; line numbers drift).**
<!-- verify-symbol: analytics-service/services/metrics.py _rolling_correlation -->
<!-- verify-symbol: analytics-service/services/portfolio_risk.py compute_correlation_matrix -->
<!-- verify-symbol: analytics-service/services/portfolio_risk.py compute_rolling_correlation -->
<!-- verify-symbol: analytics-service/services/portfolio_risk.py compute_avg_pairwise_correlation -->
<!-- verify-symbol: analytics-service/services/portfolio_optimizer.py _avg_corr -->
<!-- verify-symbol: analytics-service/services/portfolio_optimizer.py corr_with_portfolio -->
<!-- verify-symbol: analytics-service/services/match_engine.py _compute_corr_with_portfolio -->
<!-- verify-symbol: analytics-service/services/strategy_matching.py find_matched_strategy -->
<!-- verify-symbol: analytics-service/services/strategy_matching.py _MATCH_CORRELATION_THRESHOLD -->
<!-- verify-symbol: analytics-service/routers/portfolio.py verify_strategy -->
<!-- verify-symbol: analytics-service/routers/portfolio.py benchmark_comparison -->
<!-- verify-symbol: analytics-service/routers/portfolio.py corrs_clean -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the two correlation helpers land in services/dispersion.py and reach ONE site end-to-end - portfolio_risk.compute_correlation_matrix (C1) - with a red test</name>
  <files>analytics-service/services/dispersion.py, analytics-service/services/portfolio_risk.py, analytics-service/tests/test_correlation_residue_sites.py</files>
  <read_first>
    - analytics-service/services/dispersion.py - plan 01's floor (the names you build on)
    - analytics-service/services/metrics.py - `_rolling_correlation` (the `both_move` mask) and the headline `correlation` guard in `compute_all_metrics` (the pairwise pattern): the templates, per D-02
    - analytics-service/services/portfolio_risk.py - `compute_correlation_matrix` (C1), `compute_avg_pairwise_correlation` (its consumer: a None cell must be skipped there as an all-zero column's is today)
    - analytics-service/tests/dispersion_fixtures.py - the NAV generator and yields dict
    - .planning/phases/166.1-qstatsrecompute/166.1-CONTEXT.md - D-02, D-07
  </read_first>
  <behavior>
    - Helper: `pairwise_correlation_or_none(a, b)` is None when either leg is a NAV constant yield (every yield id), and a finite value for two correlated noisy legs
    - Helper control: a cent-rounded 1% APY NAV leg against noise gives a finite value
    - Helper: `dispersing_corrwith(frame, target)` drops a residue column, keeps a noisy column, and is EMPTY when the target is residue
    - C1 D-07 invariant: the matrix with one leg a NAV constant yield EQUALS the matrix with that leg replaced by an all-zero series of the same length (both computed in the test); the constant leg's row and column are None including its diagonal; the noisy-noisy cell stays finite
  </behavior>
  <action>
    Per D-02 and D-07. Before any edit, run `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-03-PLAN.md` and halt on non-zero. Add to `services/dispersion.py` exactly two helpers and nothing else: `pairwise_correlation_or_none(a, b)` (align by index, drop rows where either is missing, return None unless both legs pass `dispersion_is_real` on that overlap, otherwise the Pearson correlation, with a non-finite result mapped to None) and `dispersing_corrwith(frame, target)` (the `corrwith` of the candidate columns against `target`, restricted to columns whose own dispersion is real, and an EMPTY float Series when the target's dispersion is not real). Keep the module's leaf rule: numpy and pandas only. Write `tests/test_correlation_residue_sites.py` first with the helper tests and C1, and observe C1 RED on the unedited `compute_correlation_matrix`, recording the RED yield ids. Before editing C1, confirm and record that an all-zero column already yields None at its row, column and diagonal today (the no-new-shape proof for C1). Then fix C1: mask a non-dispersing column's row and column to NaN before `_safe_float`, so it reads None exactly as an all-zero column does today.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL "$QZ_VENV_PY" -m pytest tests/test_correlation_residue_sites.py tests/test_portfolio_risk.py -q</automated>
    <fails_when>non-zero exit (127 = FAIL), or " failed" / " error" in the pytest summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def pairwise_correlation_or_none\|^def dispersing_corrwith' analytics-service/services/dispersion.py` prints 2
    - `grep -c '^from services\|^import services' analytics-service/services/dispersion.py` prints 0
    - the SUMMARY records C1 RED before the fix (yield ids) and C1's pre-existing all-zero output
  </acceptance_criteria>
  <done>Both helpers exist in the leaf module and C1 reports None for a residue leg, proven by a test observed red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: C2-C6 - rolling correlation, the optimizer's two correlations, the match engine and the benchmark correlation</name>
  <files>analytics-service/services/portfolio_risk.py, analytics-service/services/portfolio_optimizer.py, analytics-service/services/match_engine.py, analytics-service/routers/portfolio.py, analytics-service/tests/test_correlation_residue_sites.py</files>
  <read_first>
    - analytics-service/services/portfolio_risk.py - `compute_rolling_correlation` (C2)
    - analytics-service/services/portfolio_optimizer.py - `corr_with_portfolio` inside `find_improvement_candidates` (C3, with its `else 0` short-overlap branch) and `_avg_corr` (C4), plus its callers in `simulator_scoring.py`, `bridge_scoring.py` and `match_engine.py` (each must already handle a None return, because an all-zero column gives None today; confirm and record)
    - analytics-service/services/match_engine.py - `_compute_corr_with_portfolio` (C5)
    - analytics-service/routers/portfolio.py - the BTC `benchmark_comparison` block (C6) and its `len(aligned) >= 30` gate
  </read_first>
  <behavior>
    - D-07 invariant per site: the output with one leg a NAV constant yield EQUALS the output with that leg replaced by an all-zero series of the same length (both measured in the test, never hard-coded)
    - C2: a pair with a constant leg yields an empty series; a noisy-noisy pair is unchanged
    - C3, C5, C6: the correlation reported for a constant leg is what an all-zero leg gets today; two correlated noisy legs keep a finite value
    - C4: `_avg_corr` is None when any column is residue, matching the all-zero column
  </behavior>
  <action>
    Per D-02 and D-07. Write each site's tests first and observe RED on the unedited site. Before editing each site, record its pre-existing all-zero output at the same key (None, an empty series, 0, or an absent key); that list is the proof no new output shape reaches a TS reader. If any site would emit a shape it never emitted before, STOP and report instead of changing TS. Then: C2 applies the `both_move` rolling mask from `_rolling_correlation` before `.dropna()`. C3, C5 and C6 call `pairwise_correlation_or_none` (C3 keeps its `else 0` short-overlap branch; C6 keeps `_safe_float` semantics and its `len(aligned) >= 30` gate). C4 returns None when any column fails `dispersion_is_real`. Import from `services.dispersion` only. Drill every site (byte backup, neuter to the old line, RED, `cp` restore, `cmp`) and add the rows to the SUMMARY drill table.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL "$QZ_VENV_PY" -m pytest tests/test_correlation_residue_sites.py tests/test_portfolio_risk.py tests/test_portfolio_optimizer.py tests/test_match_engine.py tests/test_dispersion_floor_sites.py -q</automated>
    <fails_when>non-zero exit (127 = FAIL), or " failed" / " error" in the pytest summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'services.dispersion' analytics-service/services/portfolio_optimizer.py analytics-service/services/match_engine.py analytics-service/routers/portfolio.py analytics-service/services/portfolio_risk.py` prints at least 1 for each file
    - the SUMMARY lists, for C2-C6, the pre-existing all-zero output at the same key and the drill row
  </acceptance_criteria>
  <done>C2-C6 report no correlation over a residue leg, each proven by a test observed red and a drill.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: C7 and C8 - the two corrwith matching sites - then the whole-plan pytest, mypy and ruff (no new finding) gates</name>
  <files>analytics-service/services/strategy_matching.py, analytics-service/routers/portfolio.py, analytics-service/tests/test_correlation_residue_sites.py</files>
  <read_first>
    - analytics-service/services/strategy_matching.py - `find_matched_strategy` in full (C8): the `idxmax` over `corrs`, `_MATCH_CORRELATION_THRESHOLD`, and the broad `except` that logs "matching failed"
    - analytics-service/routers/portfolio.py - the `corrwith` matching block inside `verify_strategy` (C7) and its `corrs_clean` NaN filter
    - analytics-service/tests/test_routers_audit_2026_05_17.py - the existing `verify_strategy` source-pin style
  </read_first>
  <behavior>
    - C7 and C8: two strategies with the SAME constant yield never match each other (today's residue correlation may exceed the 0.95 threshold); a noisy target with a near-identical noisy candidate still matches
    - C8: when every candidate column is residue, or the target is, `find_matched_strategy` returns None through an explicit empty-result branch, and the captured log holds NO "matching failed" line (today `idxmax` on an empty Series would raise into the broad `except` and log a warning)
  </behavior>
  <action>
    Per D-02. Write the C7/C8 tests first and observe RED. C7 has no pure seam: test `dispersing_corrwith` directly for its semantics and pin C7 with a source test asserting the `verify_strategy` block calls `dispersing_corrwith`, in the existing source-pin style. Drive C8 through `find_matched_strategy` with a minimal stub client object exposing the `table(...).select(...).eq/in_(...).limit(...).execute()` chain, and capture logs with `caplog` for the no-warning assertion. Then: C7 replaces its `corrwith` call with `dispersing_corrwith` and keeps `corrs_clean`, its threshold and its NaN handling. C8 replaces its `corrwith` call with `dispersing_corrwith`, drops NaN, and returns None explicitly when the result is empty, BEFORE `idxmax`; the broad `except` stays for real failures. Record C7's and C8's pre-existing all-zero output (no match, `matching_status` "no_match") as the no-new-shape proof. Drill both. Then run the gates below; the qstats-gate census line must still equal plan 01's BEFORE reading.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL "$QZ_VENV_PY" -m pytest tests/test_correlation_residue_sites.py tests/test_process_key.py tests/test_routers_audit_2026_05_17.py -q</automated>
    <fails_when>non-zero exit (127 = FAIL), or " failed" / " error" in the pytest summary</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY -u SUPABASE_TEST_DB_URL "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent --config-file=pyproject.toml services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py</automated>
    <fails_when>non-zero exit (127 = FAIL), or " failed" / " error" in the pytest summary, or mypy lacks "Success: no issues found"</fails_when>
    <automated>cd analytics-service && command -v ruff > /dev/null && git fetch -q origin main && QZ_BASE="$(git merge-base HEAD origin/main)" && QZ_TMP="$(mktemp -d)" && git -C .. archive "$QZ_BASE" analytics-service | tar -x -C "$QZ_TMP" && qz_ruff() { for f in services/dispersion.py services/portfolio_risk.py services/portfolio_optimizer.py services/match_engine.py services/strategy_matching.py routers/portfolio.py tests/test_correlation_residue_sites.py; do test -e "$f" && ruff check --output-format=concise "$f"; done | grep -E '^[^ ]+:[0-9]+:[0-9]+: ' | sed -E 's/:[0-9]+:[0-9]+:/:/' | sort; } && (cd "$QZ_TMP/analytics-service" && qz_ruff) > "$QZ_TMP/before" && qz_ruff > "$QZ_TMP/after" && wc -l < "$QZ_TMP/before" && wc -l < "$QZ_TMP/after" && test -z "$(comm -13 "$QZ_TMP/before" "$QZ_TMP/after")"</automated>
    <fails_when>non-zero exit (ruff absent, the fetch or archive failed, or `comm -13` printed a line: a NEW ruff finding)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*#' analytics-service/services/strategy_matching.py | grep -c '\.corrwith('` prints 0, and the same holds for `analytics-service/routers/portfolio.py`
    - `grep -c 'dispersing_corrwith' analytics-service/routers/portfolio.py analytics-service/services/strategy_matching.py` prints at least 1 for each file
    - the SUMMARY lists, per site C1-C8, the pre-existing all-zero output at the same key (the no-new-shape proof), and the drill rows for C1-C8
    - the SUMMARY quotes the full-suite summary line, the mypy "Success" line, the ruff BEFORE and AFTER counts with the empty `comm -13` result, and the qstats-gate census line (identical to plan 01's BEFORE)
    - `git diff --stat -- src/ supabase/` is empty for this plan's commits
  </acceptance_criteria>
  <done>No correlation outside metrics.py is computed over a residue leg, C8 answers an empty candidate set with an explicit None and no warning, every site was drilled, and the full suite, strict mypy and the no-new-finding ruff gate are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analytics-service -> persisted / returned numbers | a fabricated correlation reaches portfolio analytics, the benchmark comparison, match reasons and the verification match status |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.1-15 | Tampering (false match) | C7/C8 `corrwith` matching | medium | mitigate | `dispersing_corrwith` excludes residue columns and a residue target; test that two same-yield constant strategies never match |
| T-166.1-16 | Tampering (integrity of a displayed number) | C1-C6 correlation cells | medium | mitigate | `pairwise_correlation_or_none` / masks; D-07 equality with the all-zero leg per site; drill table |
| T-166.1-17 | Repudiation (a silent failure reads as "no match") | C8 broad `except` | low | mitigate | explicit empty-result None before `idxmax`, so the warning keeps meaning a real failure; caplog test |
| T-166.1-18 | Denial of service (over-wide floor hides real data) | the helpers | medium | mitigate | cent-rounded NAV control on the pairwise helper goes red if the floor widens |
</threat_model>

<verification>
- `node scripts/verify-plan-anchors.mjs .planning/phases/166.1-qstatsrecompute/166.1-03-PLAN.md` exits 0 before Task 1 starts.
- Task 3's second and third verify commands exit 0.
</verification>

<success_criteria>
- Success criterion 3 extended by D-02: the eight correlation sites use Phase 166's floor through the two shared helpers, each with a test observed red and a drill.
</success_criteria>

<output>
Create `.planning/phases/166.1-qstatsrecompute/166.1-03-SUMMARY.md` when done. Stage it, then run
`QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && test -x "$QZ_MAIN/node_modules/.bin/tsx" && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK before committing.
</output>
