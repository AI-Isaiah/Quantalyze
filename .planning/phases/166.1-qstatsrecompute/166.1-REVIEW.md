---
phase: 166.1-qstatsrecompute
reviewed: 2026-09-26T15:38:45Z
depth: standard
round: 2
reviewed_head: dc690c802
diff_base: 29152a912
files_reviewed: 32
files_reviewed_list:
  - CHANGELOG.md
  - analytics-service/routers/portfolio.py
  - analytics-service/services/analytics_runner.py
  - analytics-service/services/bridge_scoring.py
  - analytics-service/services/csv_validator.py
  - analytics-service/services/dispersion.py
  - analytics-service/services/match_engine.py
  - analytics-service/services/portfolio_optimizer.py
  - analytics-service/services/portfolio_risk.py
  - analytics-service/services/simulator_scoring.py
  - analytics-service/tests/test_correlation_residue_sites.py
  - analytics-service/tests/test_coverage_extras.py
  - analytics-service/tests/test_csv_validator.py
  - analytics-service/tests/test_dispersion_floor_sites.py
  - analytics-service/tests/test_portfolio_compute_integration.py
  - analytics-service/tests/test_portfolio_risk.py
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx
  - src/components/portfolio/PortfolioImpactPanel.test.tsx
  - src/components/portfolio/ReplacementCard.test.tsx
  - src/components/portfolio/ReplacementCard.tsx
  - src/components/portfolio/RiskAttribution.test.tsx
  - src/components/portfolio/RiskAttribution.tsx
  - src/components/portfolio/WhatWedDoCard.test.tsx
  - src/components/portfolio/WhatWedDoCard.tsx
  - src/lib/analytics-schemas.test.ts
  - src/lib/analytics-schemas.ts
  - src/lib/portfolio-analytics-adapter.test.ts
  - src/lib/portfolio-analytics-adapter.ts
  - src/lib/portfolio-insights.ts
  - src/lib/types.ts
  - src/lib/wizardErrors.ts
findings:
  critical: 0
  warning: 2
  info: 2
  total: 4
status: issues_found
---

# Phase 166.1: Code Review Report (round 2, confirmation round)

**Reviewed:** 2026-09-26T15:38:45Z
**Depth:** standard, with the consumers of every newly nullable field traced into `src/`
**Scope:** `git diff 29152a912..dc690c802`, excluding `.planning/` (32 files)
**Status:** issues_found (no CRITICAL, no HIGH)

## Round-2 verdict

**Severity, in the brief's vocabulary: CRITICAL 0 / HIGH 0 / MEDIUM 2 / LOW 2.**
Mapping to the frontmatter keys: MEDIUM is `warning`, LOW is `info`. Under the policy, none of the four earns a fix round. They are recorded below as known limits.

- **(a) Both round-1 HIGHs are closed at HEAD.** The symbols are cited below, and the closure was measured with the phase's own fixture.
- **(b) The fix pass introduced no new CRITICAL or HIGH.** Every consumer of the fields the pass made nullable was traced, in Python and in TypeScript, and none of them crashes or fabricates a value on null.
- **(c) D7 and D-24 hold on every path traced.**
  - **D7:** no 0-for-undefined reaches a rendered page on the fields this phase touches. A constant-yield leg and an all-zero leg give None in the same positions.
  - **D-24:** a constant positive daily-returns CSV is rejected at every length of 2 or more rows.
  - One half-closed round-1 MEDIUM remains: MEDIUM-A below.

## (a) Round-1 CRITICAL/HIGH closure

Round 1 had no CRITICAL. It had two HIGHs, both from the SFH report.

### SFH HIGH-1: C3's None rendered as "correlation toward 0.00". CLOSED.

- `portfolio_optimizer.find_improvement_candidates`:
  - `corr_with_portfolio` is `pairwise_correlation_or_none(port_baseline, aligned[cid])`, with no length fallback.
  - `sharpe_lift` is None when `current_sharpe` is None.
  - `score` uses an explicit `0.0` for a None lift.
- `portfolio_optimizer.generate_narrative`: `sharpe_lift is not None and sharpe_lift > 0`, so a None lift no longer raises TypeError.
- `portfolio-analytics-adapter.parseOptimizerSuggestionRow`: `corr_with_portfolio` and `sharpe_lift` are `asNumber(...)` with no `?? 0`.
- `types.OptimizerSuggestionRow`: both fields are `number | null`.
- `WhatWedDoCard`:
  - `corrLine` requires `corr !== null && Number.isFinite(corr)`.
  - `sharpeLine` requires `lift !== null`.
  - The hide gate applies only to a non-null negative or non-finite lift.
- Other readers are already null-safe:
  - `PortfolioOptimizer.MetricCell` → `formatMetric` gives "—" for null.
  - `AllocatorMatchQueue` types both fields `?: number | null`.
- **Measured at HEAD:**
  - A book of one `nav_constant_yield(1e-4)` leg plus a noisy candidate gives `corr_with_portfolio None, sharpe_lift None`.
  - A book of one all-zero leg gives the same pair.

### SFH HIGH-2 / WR-02: the simulator and bridge deltas coerced None to 0.0. CLOSED.

- `simulator_scoring._delta` and `bridge_scoring._delta` both return None when either operand is None.
- `simulator_scoring.simulate_add_candidate`: `corr_delta` is None when `_leg_is_flat(aligned, candidate_id)`, otherwise `_delta(...)`. The NEW-C11-02 current-side case is subsumed, because `_delta` returns None for a None `current_avg_corr`.
- `bridge_scoring.find_replacement_candidates`:
  - `corr_delta` is None when the incumbent or the candidate is flat.
  - The composite ranks through `_axis`, which gives an explicit 0 for a missing axis.
  - `composite_score` is always a float, so the `sorted(...)` key never sees None.
- `analytics-schemas.BridgeCandidateSchema` and `types.BridgeCandidate`: the three deltas are nullable.
- `ReplacementCard`:
  - `formatDelta` renders `— {label}`.
  - `deltaColor(null)` is `text-text-muted`.
- `simulatorSchema` already typed all four simulator deltas `z.number().nullable()`, and `PortfolioImpactPanel.deltaChips` already rendered null. So `dd_delta` and `concentration_delta` becoming nullable through `_delta` breaks no parse.
- **Measured at HEAD**, with the constant-yield leg and the all-zero leg side by side. The None positions are identical, which is D7 parity:

| scenario | constant yield | all-zero |
|---|---|---|
| simulator, flat candidate | `corr_delta None` | `corr_delta None` |
| simulator, flat book | `sharpe_delta None, corr_delta None` | same |
| bridge, flat candidate | `corr_delta None` | same |
| bridge, flat incumbent | `corr_delta None` | same |
| bridge, flat book | `sharpe_delta None, corr_delta None` | same |
| `compute_risk_decomposition`, flat book | `marginal_risk_pct None, component_var None` | same |

## (b) Regression hunt on the fix pass: no new CRITICAL/HIGH

Traced and clean:

- **Python response shapes.**
  - `PortfolioBridgeResponse` and `PortfolioOptimizerResponse` in `models/schemas.py` carry `list[dict[str, Any]]` with `extra="allow"`, so a None delta passes validation.
  - `optimizer_suggestions` is persisted as JSONB on `portfolio_analytics`, with no column typing.
- **`data_quality` keys.** `avg_pairwise_correlation_pairs_used` and `avg_pairwise_correlation_pairs_total` land in unconstrained JSONB. No `.strict()` Zod schema covers `data_quality`: the only `.strict()` schemas in `analytics-schemas.ts` are the compute-jobs row and the tick response.
- **`match_engine._compute_portfolio_fit_components`.** A None `corr_reduction` feeds `_normalize_min_max`, which maps None to 0.0, so there is no crash and no fabricated value.
- **`portfolio_optimizer._avg_corr` → `dispersion.average_pairwise_correlation`.**
  - The result is the upper-triangle mean over finite pairs, which is the same number as the old `(sum - n) / (n(n-1))` whenever every column disperses.
  - `_safe_float(None)` is None.
  - Every caller passes a `dropna`'d frame or a reindex of one: `window_alignment.aligned_concat`, `port_df.loc[all_returns.index]` and `port_df.reindex(aligned.index)`.
- **`_leg_is_flat` column lookups cannot KeyError.**
  - `candidate_id` is the column name `align_current_and_proposed` assigns.
  - `"__cand__"` is match_engine's own name.
  - The incumbent is guaranteed in `port_df` by the early return.
- **`compute_risk_decomposition` None shares.** Every consumer handles null:
  - `generate_narrative` filters None rows before `max`.
  - `parseRiskDecompositionRow` keeps null.
  - `RiskAttribution` skips null in the stacked bar and renders "—" for the share and the assessment. `formatPercent(null)` is "—".
  - `portfolio-insights.computeBiggestRisk` filters null.
  - `computeUnderperformance` and `computeConcentrationCreep` read only `standalone_vol` and `weight_pct`.
- **`CsvValidationEnvelope` row counting.**
  - `validate_csv` emits 1-based rows: `row_idx = int(row_idx_raw) + 1`, and 0 is the absent-row sentinel. So `isRealRow = row >= 1` cannot misclassify a real first-row pandera failure.
  - No e2e spec asserts the old "N row(s) failed validation" copy for a file-level error. The `e2e/` CSV bodies are varied series, so D-24 does not reject any e2e happy path.
- **`daily_returns_constant` error code.** `csv_adapter` mints the error code `DAILY_RETURNS_CONSTANT`. Neither that code nor the old `DAILY_SHARPE_SENTINEL` is mapped anywhere in `src/`, so both fall to the same generic lookup, and the wizard copy change is limited to `CSV_RULE_LABELS`.

**Gates I re-ran at HEAD `dc690c802`:**
- **pytest**, from `analytics-service/` with the four TEST database variables unset. The 6 changed test files plus every `test_simulator*`, `test_bridge*`, `test_portfolio_optimizer*` and `test_match_engine*` file: `590 passed, 1 warning`.
- **vitest.** The 7 touched test files plus the insights, PortfolioOptimizer and ReplacementPanel tests: `Test Files 10 passed (10)`, `Tests 277 passed (277)`.
- **`tsc --noEmit -p .`:** exit 0.
- **`mypy --strict services/ routers/ models/`:** `Success: no issues found in 97 source files`.

**Taken from `166.1-REVIEW-FIX.md` and not re-run:**
- the full 6760-test suite;
- ruff, whose binary is not in the analytics venv used here;
- eslint;
- the neuter/RED drills.

## (c) D7 and D-24

- **D7.** Section (a) covers every surface this phase made nullable, and each was probed. No remaining `?? 0`, `else 0` or `0.0` coercion on those fields reaches a rendered page.
  - `simulator_scoring._zero_deltas()` still emits `0.0` on the `insufficient_data` branch. It is not rendered, because `PortfolioImpactPanel` renders deltas only when `status === "ok"`. It is listed as LOW-B.
- **D-24.** `csv_validator._check_sharpe_sentinel` applies to any daily-returns frame with `len(r) >= 2` after `dropna`:
  - An exactly-zero std enters `dispersion_is_residue`.
  - `DEFAULT_RISK_FREE_DAILY = 0.0`, so every constant positive series gets `daily_returns_constant`.
  - A 1-row file has no std and is outside "constant".
  - Constant zero and constant negative series are outside D-24 by its own text.
  - The one-message, no-number rule (D-04) holds.

## Warnings (MEDIUM — known limits, no fix round)

### MEDIUM-A (WR-01-R2): the pairs-used disclosure is recorded but never rendered, so round-1 SFH MEDIUM-1 is half-closed

**Symbols:**
- `routers/portfolio._compute_portfolio_analytics` writes the `data_quality` keys `avg_pairwise_correlation_pairs_used` and `avg_pairwise_correlation_pairs_total`.
- `portfolio_optimizer.generate_narrative` is the "Average pairwise correlation is X, which is …" sentence.

**Issue:**
- A grep of `src/` finds no reader of either key.
- The portfolio page's average-correlation KPI and the narrative sentence still present a subset average as the whole book's.
- The WR-03 skip rule makes this more common than before: a book with one constant-yield sleeve now always gets an average over the remaining pairs, where the scorers used to get None.
- The data to disclose it now exists, but no page shows it.

**Fix (when routed):**
- In `generate_narrative`, when `pairs_used < pairs_total`, append "(over N of M strategy pairs)". This needs the counts passed in beside `avg_pairwise_correlation`.
- Show the same qualifier on the KPI tile from `data_quality`.

### MEDIUM-B (WR-02-R2): the bridge fit label judges a candidate from the axes that exist and does not say so

**Symbols:** `bridge_scoring.find_replacement_candidates` (`composite` via `_axis`, then `_fit_label`) and `ReplacementCard`.

**Issue:**
- For a flat book, measured: `sharpe_delta None, corr_delta None, fit_label "Weak fit"`. The card shows "— Sharpe", "— Corr" and a "Weak fit" badge.
- The badge is computed from the drawdown axis alone, with 0 stood in for the two missing axes.
- This is not a D7 fabrication: the explicit 0 was the SFH HIGH-2 prescription, and no statistic is invented. But the badge reads as a three-axis judgement.

**Fix (when routed):** either suppress the fit badge when two or more axes are None, or add a "partial" qualifier.

## Info (LOW — known limits)

### LOW-A (IN-01-R2): WhatWedDoCard falls through to "diversify the portfolio" when both the lift and the correlation are null

**Symbol:** `WhatWedDoCard`, `const why = wins.length > 0 ? … : "diversify the portfolio"`.

**Issue:** for a constant-yield or all-zero book, the card now renders "Add X at a 10% allocation to diversify the portfolio" when no statistic backs the claim. Before the fix it rendered "…toward 0.00", so this is an improvement and not a regression. The generic fallback is pre-existing.

**Fix:** hide the card, or state no reason, when `wins` is empty and both the lift and the correlation are null.

### LOW-B (IN-02-R2): `_zero_deltas()` still emits 0.0 deltas on the insufficient-data branch

**Symbol:** `simulator_scoring._zero_deltas`, used by `simulate_add_candidate`'s `overlap_days < MIN_DATA_POINTS` return and the helper at the file's end.

**Issue:** this branch is not rendered today (the panel gates on `status === "ok"`), but it is a 0-for-undefined on the wire, which D7's end-to-end rule forbids.

**Fix:** return None for all four deltas and rename the helper to `_null_deltas`.

## Observed outside the round-2 brief: pre-existing at the phase base, NOT counted above

⚠️ **The drawdown delta has an inverted sign in three scorers, and it is user-facing.**

The fix pass did not introduce this, and it is not a 0-for-undefined, so it is outside (a), (b) and (c) and is not counted. It is recorded here because a reviewer who saw it and stayed silent would be the failure mode this review exists to prevent.

**Symbols:**
- `simulator_scoring.simulate_add_candidate`: `dd_delta = _delta(current_max_dd, proposed_max_dd)`.
- `portfolio_optimizer.find_improvement_candidates`: `dd_improvement = current_max_dd - new_max_dd`.
- `match_engine._compute_portfolio_fit_components`: `dd_improvement = current_max_dd - new_max_dd`.

**The sign error:**
- `portfolio_optimizer._max_drawdown` returns a value ≤ 0 (`drawdown.min()`), so `current - new` is NEGATIVE when the new drawdown is shallower.
- The simulator chip hint says "Positive = shallower maximum drawdown".
- `WhatWedDoCard` says "improve drawdown by X" when `dd_improvement > 0`.
- `match_engine._normalize_min_max` treats higher as better, so ranking rewards a DEEPER drawdown.

**Measured at HEAD:**
- A book whose max drawdown went from -0.464 to -0.236 after adding a low-vol candidate gave `dd_delta -0.228`.
- A constant-yield book plus a noisy candidate, whose drawdown got worse, gave `dd_improvement +0.0087`. The card renders that as "improve drawdown by 0.87%".

**Pre-existing:** the same expressions are at the phase base `ea4167a3f`. `bridge_scoring` was already corrected under H-1065 (`dd_delta = _delta(new_dd, current_dd)`), and `types.ts` documents the corrected bridge convention. `TODOS.md` has no entry.

**Routing:** under the deferral rule this is user-facing, so it needs a phase. The orchestrator should route it; this review did not edit `TODOS.md` or `ROADMAP.md`.

**Fix shape:** use `new - current` at all three sites, mirroring H-1065. Add a test in which a shallower drawdown gives a positive delta.

---

_Reviewed (round 2): 2026-09-26T15:38:45Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: standard_

## Round 1 (lineage)

Round 1 was reviewed at `2026-09-26T14:07:24Z`, depth deep, over 21 files. It found 0 Critical, 3 Warnings (WR-01..03) and 6 Info (IN-01..06), 9 in total, with status `issues_found`. Its outcomes are in `166.1-REVIEW-FIX.md`. The round-1 report follows unchanged, except that its YAML frontmatter was folded into this sentence.


# Phase 166.1: Code Review Report (round 1)

**Reviewed:** 2026-09-26T14:07:24Z
**Depth:** deep (callers traced across `services/`, `routers/` and the wizard renderer)
**Files Reviewed:** 21
**Status:** issues_found

## Summary

Scope was `git diff ea4167a3f..HEAD` excluding `.planning/`. For depth, I also traced callers outside the diff: `services/simulator_scoring.py`, `services/bridge_scoring.py`, `services/ingestion/csv_adapter.py`, `routers/process_key.py` and `CsvValidationEnvelope.tsx`.

**The floor itself is sound.** `services/dispersion.py` is a verbatim move and a true leaf (numpy and pandas only). `metrics.py` re-binds only the three functions, as D-23 says. Every S-site and C-site reads the one floor with the right argument:

- **Dispersion checks:** each takes the site's own std and its own mean (the excess mean at S5).
- **ddof:** it matches the ratio the site computes. That is pandas ddof=1 at S1–S5, S8 and C1–C8, and numpy ddof=0 at S6, where the pre-existing gate already used ddof=0.
- **NaN std:** each site handles it on its own path, as the helper docstrings promise.

**Measured by me:**
- Rolling std over a compounding-yield window that follows a volatile stretch stays at ≤ 7.1e-17, below the 1e-12 floor, so C2's `both_move` mask does not leak.
- Phase test files: `565 passed`.
- A 5-row constant 0.001 CSV now returns `daily_sharpe_sentinel` with the no-number message.

**Taken from the SUMMARYs, not re-run:** the red-test and neuter drills. The source is read-only for this review.

**Release checks: clean.**
- `VERSION` and the `package.json` version are byte-equal `0.97.0.0`. Both are 8 bytes with no trailing newline, the same shape as the base.
- `origin/main` is `0.96.0.0`, so the minor bump is correctly derived.
- Every non-merge commit in `ea4167a3f..HEAD` maps to a bullet or to the planning-commit note in the new CHANGELOG entry. The two merges are named.

**No Critical findings.** There are three Warnings.

1. **WR-01, the mandatory finding.** D-24 routes every short constant upload into a rule label that states a comparison that was never made.
2. **WR-02.** The newly-None correlation at C4 lands in two scorers that turn None into a confident `0.0`.
3. **WR-03.** There are now two meanings of "average pairwise correlation".

**Could not verify** (policy constraints): the full-suite, mypy and ruff numbers quoted in the CHANGELOG. I re-ran only the four phase test files.

## Narrative Findings (AI reviewer)

## Warnings

### WR-01: `daily_sharpe_sentinel` label "Daily Sharpe > 10 looks unrealistic" now headlines every short constant-positive upload, whose Sharpe was never computed

**Files:**
- `analytics-service/services/csv_validator.py`: `_check_sharpe_sentinel`, the residue branch (about lines 250-258).
- `src/lib/wizardErrors.ts`: `CSV_RULE_LABELS.daily_sharpe_sentinel` (about line 6086) and `formatCsvRuleCauseSingle`.
- `src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx`: the `causeText` single-rule arm and the `<summary>` label.

**Issue:** D-24 (`d1b91d5d0`) sends an exactly-zero std into the residue branch. So a 2-, 3- or 5-row constant positive daily-returns CSV, which was accepted before, now gets `rule: "daily_sharpe_sentinel"`. Before D-24 it never reached the sentinel.

I traced the emission: `dispersion_is_residue(0.0, 0.001)` is True, `r.mean() - DEFAULT_RISK_FREE_DAILY > 0` is True, and the branch emits the rule with `row: 0`. I confirmed this by running `validate_csv` on a 5-row constant 0.001 body.

The wizard renders this rule, measured against `CsvValidationEnvelope.tsx`, as:

1. Headline: **"1 row failed validation"**. `errors.length === 1`, although this is a dataset-level rule with the absent-row sentinel `row: 0`.
2. Cause line: **"Rule violated: Daily Sharpe > 10 looks unrealistic. Expand below for the row-level breakdown."**
3. Collapsed `<details>` summary: the same label again.
4. Only inside the expanded bullet: the true cause, "Daily returns do not vary, so the Sharpe is unbounded; …".

Why the label misstates the cause:
- For sd = 0 the Sharpe is undefined, not a number above 10. The user is told a Sharpe was measured and judged too high, when the real problem is that the series never changes.
- The "row-level breakdown" pointer promises rows that do not exist.
- The sentinel also runs on the unvalidated frame after a `SchemaErrors` (`df_validated = df`). So a constant-return file with an unrelated schema error, such as the `bad-dates` body in `e2e/csv-upload-flow.spec.ts`, now shows two rule categories, one of them this misleading label.

**Why Warning and not Critical:**
- The verdict is correct and was a founder decision (D-24).
- The label is loosely true in the limit (an unbounded Sharpe is "> 10").
- The accurate sentence is one click away.
- No stored data is affected. I traced `validate_csv` callers: only `routers/csv.py` and `services/ingestion/csv_adapter.py`, both on raw upload bytes. Nothing re-validates stored `csv_daily_returns`, so 166.3's recompute does not flip existing rows.

**Reason the orchestrator may re-tier:** it is user-facing copy that misstates the cause on a path this phase created, and the repo's review policy lets user-facing defects block.

**Ownership:** D-04 locked "the rule name and its label are untouched, no TS change". D-24 changed which uploads reach that label without revisiting that clause, and D-23 moved every TS edit to 166.2. So this needs either a D-04 amendment in this phase or a named route to 166.2. It must not stay unowned.

**Fix (preferred):** emit a distinct rule key from the residue branch, so the label states the real cause and the Sharpe-number branch keeps its label:
```python
# csv_validator._check_sharpe_sentinel, residue branch
errors.append({
    "rule": "daily_returns_constant",
    "row": 0,
    "message": "Daily returns do not vary, so the Sharpe is unbounded; "
               "a constant positive return is not a realistic track record",
})
```
```ts
// wizardErrors.ts CSV_RULE_LABELS
daily_returns_constant: "Daily returns never change",
```
Also:
- Make `CsvValidationEnvelope` skip the "N rows failed validation" headline and the "row-level breakdown" pointer when every error has `row < 1`.
- Update the D-24 tests' `_sentinel_errors` filter to the new key.

**Minimal alternative** (keeps the key and changes TS only): relabel to "Sharpe unrealistically high or undefined (returns do not vary)".

### WR-02: the newly-None `_avg_corr` (C4) becomes a confident `0.0` "Correlation unchanged" in `simulate_add_candidate`, and a neutral `0.0` in `bridge_scoring`

**Files:**
- `analytics-service/services/portfolio_optimizer.py`: `_avg_corr`.
- Consumers outside the diff: `services/simulator_scoring.py` `simulate_add_candidate` (`corr_delta`), and `services/bridge_scoring.py` (`corr_delta = _delta(current_corr, new_corr)`).

**Issue:** C4 now returns None whenever any column's dispersion is residue, so a constant-yield candidate makes `proposed_avg_corr` None. `simulate_add_candidate` guards only `current_avg_corr is None` (the NEW-C11-02 guard). It then calls `_delta(current_avg_corr, proposed_avg_corr)`, and `_delta` coerces None to `0.0`.

**Measured:**
- Portfolio of two noisy legs plus a compounding 1e-4 constant-yield candidate: `deltas.corr_delta == 0.0`.
- An all-zero candidate: also `0.0`.

**Why it matters:** NEW-C11-02's own comment says the panel renders `0.0` as "Correlation unchanged (±0.000)", which is exactly the fabricated certainty that guard exists to prevent. The all-zero case was already wrong. This phase moves every constant-yield candidate (a stablecoin-lending shape) into that shape, where before it produced a residue number.

The same coercion hits `sharpe_delta` when the current or proposed blend is residue (S1 now returns None). In `bridge_scoring`, `_delta` makes the correlation axis silently neutral in `composite`. D-07 demands parity with the all-zero output, but parity with a known-wrong output is not a reason to widen it.

**Fix:**
```python
# simulator_scoring.simulate_add_candidate
corr_delta = (
    None
    if current_avg_corr is None or proposed_avg_corr is None
    else current_avg_corr - proposed_avg_corr
)
```
Apply the same "None on either side stays None" rule to `sharpe_delta` and `dd_delta` there, since the schema and panel already render None as "—". In `bridge_scoring`, keep `_delta`'s neutral `0.0` for ranking, but surface the axis as None in the returned `corr_delta` and `sharpe_delta`. Add a test with a constant-yield candidate asserting `corr_delta is None`.

### WR-03: two meanings of "average pairwise correlation"; one constant-yield sleeve voids the correlation axis in every scorer but not in the risk panel

**Files:**
- `analytics-service/services/portfolio_optimizer.py`: `_avg_corr`, the all-or-nothing `dispersion_is_real` gate.
- `analytics-service/services/portfolio_risk.py`: `compute_correlation_matrix` (C1 mask) and `compute_avg_pairwise_correlation` (skips None cells).

**Issue:** After C1, the portfolio page's average pairwise correlation masks the flat leg and averages the remaining pairs. After C4, `_avg_corr` returns None for the whole frame if any one column is flat.

`_avg_corr` feeds four scorers: `find_improvement_candidates`, `match_engine._compute_portfolio_fit_components`, `bridge_scoring` and `simulator_scoring`. So a real portfolio holding one constant-yield sleeve shows an average correlation on the risk panel, while every scorer drops the diversification axis for every candidate (`corr_reduction` None, or 0).

This is D-07-compliant (an all-zero column already gave None), but the two sites now answer the same question differently for the same input. The scorer behaviour silently disables one of three ranking axes for a common allocation shape.

**Fix:** decide one semantics and share it. The consistent choice is to mask non-dispersing columns and average only the defined pairs, as C1 plus `compute_avg_pairwise_correlation` already do:
```python
def _avg_corr(df):
    moving = [c for c in df.columns
              if dispersion_is_real(float(df[c].std()), float(df[c].mean()))]
    if len(moving) < 2:
        return None
    corr = df[moving].corr()
    n = len(corr)
    return _safe_float(float((corr.values.sum() - n) / (n * (n - 1))))
```
If "None when any leg is flat" is the intended rule, record it as a decision and make `compute_avg_pairwise_correlation` agree.

## Info

### IN-01: S8's structural divide guard became a predicate with a NaN hole

**File:** `analytics-service/services/analytics_runner.py`: `_compute_derived_trade_metrics`, the SQN block.

**Issue:** The old `if std_r > 0:` guaranteed a non-zero divisor. The new `if not dispersion_is_residue(std_r, mean_r):` relies on the floor comparison. With `mean_r` NaN (possible only if the `sum(r_multiples)` of finite values overflows to inf−inf), `residue_floor` is NaN, `0.0 <= NaN` is False, and `mean_r / 0.0` raises `ZeroDivisionError` on Python floats. The CHANGELOG notes that the function has no production caller since Phase 106, so this is Info.

**Fix:** `if std_r > 0 and not dispersion_is_residue(std_r, mean_r):`

### IN-02: C3's `else 0` arm is dead and type-inconsistent

**File:** `analytics-service/services/portfolio_optimizer.py`: `find_improvement_candidates`, `corr_with_portfolio`.

**Issue:** `if len(aligned) < 30: continue` runs earlier in the loop, so `len(aligned) > 10` is always True. The `else 0` arm cannot run, and if it ever did it would emit a fabricated `0`, where the rest of the class uses None.

**Fix:** Drop the conditional: `corr_with_portfolio = pairwise_correlation_or_none(port_baseline, aligned[cid])`.

### IN-03: CHANGELOG over-states S6

**File:** `CHANGELOG.md`, `[0.97.0.0]`, first paragraph.

**Issue:** It says the eight variance sites "still tested `== 0` or `> 0`, or did not test at all". S6 tested `<= 1e-12` and was already correct for `|mean| <= 1`, as the same entry's S6 bullet and D-06 say.

**Fix:** "seven of the eight variance sites …; S6 already used an absolute 1e-12 and is re-pointed as a pin".

### IN-04: C7 is tested through a re-implementation, not through `verify_strategy`

**File:** `analytics-service/tests/test_correlation_residue_sites.py`: `_c7_block` and `test_c7_verify_strategy_matches_through_dispersing_corrwith`.

**Issue:** `_c7_block` re-writes the router block, and the router is pinned only by source-text substrings. A divergence in the real block would stay green, for example `>=` against the threshold, a different `aligned` construction, or the `len(aligned) >= 30` gate.

**Fix:** Drive `verify_strategy` once with mocked supabase and venue, as the C6 test already does for `_compute_portfolio_analytics`, and assert `matching_status == "no_match"` for two same-yield legs.

### IN-05: exact-duplicate constant-yield series can no longer match (C7/C8)

**Files:**
- `analytics-service/services/strategy_matching.py`: `find_matched_strategy`.
- `analytics-service/routers/portfolio.py`: the `verify_strategy` matching block.

**Issue:** Matching is documented as "best-effort enrichment", which is why this is Info. Before this phase, a verified series identical to a published constant-yield strategy correlated at 1.0 because the two were the same bytes, and it matched. Now it can never match. This is a consistent consequence of D-02/D-07, but it is not recorded as an accepted loss.

**Fix:** Record it in the CHANGELOG `### Notes`, or add an explicit equality fallback for non-dispersing legs, e.g. `np.allclose(frame[col], target, rtol=0, atol=residue_floor(target.mean()))`.

### IN-06: the test mutates a module global without restoring it

**File:** `analytics-service/tests/test_correlation_residue_sites.py`: `_c6_correlation`.

**Issue:** `portfolio_mod._compute_semaphore = asyncio.Semaphore(3)` is never restored. The pattern is copied from `test_portfolio_compute_integration.py`, but it adds a second test that leaves a semaphore that may be loop-bound behind for later tests.

**Fix:** `monkeypatch.setattr(portfolio_mod, "_compute_semaphore", asyncio.Semaphore(3))`.

---

_Reviewed: 2026-09-26T14:07:24Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: deep_
