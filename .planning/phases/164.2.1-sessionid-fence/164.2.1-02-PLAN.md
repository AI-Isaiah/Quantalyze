---
phase: 164.2.1-sessionid-fence
plan: 02
type: execute
wave: 2
depends_on: [164.2.1-01]
files_modified:
  - .planning/phases/164.2.1-sessionid-fence/164.2.1-02-SUMMARY.md
autonomous: true
requirements: [SC-2, SC-3, SC-4, SC-5]
tags: [wizard, localStorage, anti-vacuity, neuter-proof, vitest]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "With the helper's key clause neutered, the new unit pins AND the key-B component case go RED while all six SEAMRIM-03 pins stay GREEN (SC-2 + SC-3 discriminated in one run)"
    - "With the call site's 4th argument dropped (helper intact), the key-B component case goes RED — the wiring test cannot be satisfied by a correct helper the call site never feeds"
    - "Both neutered files are restored from BYTE backups and shasum -a 256 before == after; the neuter token count is 0 after restore"
    - "Full suite green ALONE on the box, tsc clean, nothing under supabase/ changed, localStorage.test.ts diff against main has zero removed lines"
  artifacts:
    - ".planning/phases/164.2.1-sessionid-fence/164.2.1-02-SUMMARY.md — carries both shasum lines per file and the RED output verbatim"
  key_links:
    - "The neuter never touches `(loaded.source ?? \"api\") === source` — SEAMRIM pins staying green under the neuter is what proves the fix did not narrow the source gate"
---

<objective>
Prove criterion 2: the fix is SHOWN to fail when neutered, then restored byte-identically. Two
independent neuters (helper clause; call-site argument) each must redden the phase's new tests
while the SEAMRIM-03 pins stay green. Then run the full gates for SC-3/SC-4/SC-5 and the full
suite. This plan writes no production code; it only edits files transiently and restores them.

Purpose: this milestone exists to remove tests that cannot fail. Criterion 1 was once declared
satisfied on a read; the read was wrong. The neuter run is the evidence, not the inspection.
Output: SUMMARY.md with the RED output and both sha lines per neutered file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.2.1-sessionid-fence/164.2.1-01-SUMMARY.md
@.planning/phases/164.2.1-sessionid-fence/164.2.1-VALIDATION.md
@.planning/phases/164.2.1-sessionid-fence/164.2.1-RESEARCH.md
@src/lib/wizard/localStorage.ts
@src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx

⛔ NEVER `git checkout --` in this plan — it restores HEAD and destroys the uncommitted fix. Restore
ONLY from the byte backup taken in the same task. Byte backups go stale — take them immediately
before the neuter, after the last edit. Count gate tokens PRE-edit.
⛔ Supabase CLI is linked to PRODUCTION — no db commands anywhere.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neuter → RED → byte-identical restore, at the helper AND at the call site (SC-2, SC-3)</name>
  <files>.planning/phases/164.2.1-sessionid-fence/164.2.1-02-SUMMARY.md</files>
  <read_first>
    - 164.2.1-RESEARCH.md § Q6 — the exact sequence and the anchor line
    - 164.2.1-VALIDATION.md § Anti-Vacuity Protocol
    - src/lib/wizard/localStorage.ts — `sessionKeyMatches` as landed by plan 01 (the anchor line is `if (incoming === null) return true;`)
    - WizardClient.tsx — the 4-arg `deriveWizardResumeOverrides(` call in the post-mount effect
  </read_first>
  <action>
    Let S be the session scratchpad (`/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad`), F1 = `src/lib/wizard/localStorage.ts`, F2 = the `WizardClient.tsx` path, TESTS = `src/lib/wizard/localStorage.test.ts` plus the `ContributionWizardOverlay.sessionid-fence.test.tsx` path. Run each command in its OWN Bash block (a `;`-chained grep only returns the last status).
    <!-- planner-discipline-allow: NEUTERED -->
    Neuter A (helper): `cp F1 S/localStorage.ts.green`; `shasum -a 256 F1 | tee S/f1.before`; PRE-EDIT `grep -c 'if (incoming === null) return true;' F1` must print 1 (if not, STOP — the anchor drifted, re-resolve by symbol, do not guess); `sed -i '' 's/if (incoming === null) return true;/return true; \/\/ NEUTERED/' F1`; `grep -c NEUTERED F1` prints 1; run `npx vitest run TESTS 2>&1 | tail -40` — EXPECTED: the key-mismatch unit `it`s (stored A / incoming B; absent stored / incoming B; null stored / incoming B; the burn-drop case) and the component SC-1c and SC-1e cases FAIL; the A/A unit case, the positive control SC-1d and ALL SIX SEAMRIM-03 pins ("carries the wizardSessionId forward WITHIN a source…", "does NOT leak an abandoned API draft's session id into a CSV resume", "does NOT leak a CSV draft's…", "DOES restore a CSV payload's…", "treats a source-less legacy payload as 'api'…", "leaves the existing step and strategyName rules untouched") PASS. Capture the tail verbatim to `S/neuterA.log`. Restore: `cp S/localStorage.ts.green F1`; `shasum -a 256 F1 | tee S/f1.after`; `diff S/f1.before S/f1.after && echo RESTORED-BYTE-IDENTICAL`; `grep -c NEUTERED F1` prints 0; `grep -c 'if (incoming === null) return true;' F1` prints 1 again (verify the restore by grepping the newest fix, not by hash alone); rerun TESTS — GREEN.
    Neuter B (call site, the wiring): `cp F2 S/WizardClient.tsx.green`; `shasum -a 256 F2 | tee S/f2.before`; PRE-EDIT confirm the 4th-argument line `initialDraft?.api_key_id ?? preselectKey?.id ?? null,` exists exactly once INSIDE the `deriveWizardResumeOverrides(` call (the identical expression also seeds the `apiKeyId` useState — target the call-site occurrence by line number resolved with `grep -n`, and record that line number in SUMMARY); replace that argument with `null,` via `sed -i '' '<line>s/.../null,/'` ; `grep -c` the original expression must have dropped by exactly 1; run the component file ONLY — EXPECTED: SC-1c and SC-1e FAIL — with `null` incoming the gate has NO claim and RESTORES key A's id, so `createCalls[0].wizard_session_id === A_SESSION` and both `!==` assertions fail; SC-1d and WIRE PASS. Capture to `S/neuterB.log`. Restore from `S/WizardClient.tsx.green`, `shasum` after == before, grep the expression count back to its pre-edit value, rerun — GREEN.
    Write SUMMARY.md § "Criterion 2 — neuter evidence" containing, per neuter: both `shasum` lines, the anchor grep counts before/during/after, and the RED tail verbatim (test names, not paraphrase). If either neuter leaves everything GREEN, the phase has NOT closed criterion 1 — STOP and report, do not restore-and-continue silently.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && S=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad && diff "$S/f1.before" "$S/f1.after" && diff "$S/f2.before" "$S/f2.after" && test "$(grep -c NEUTERED src/lib/wizard/localStorage.ts)" = 0 && grep -qE 'FAIL|✗|×' "$S/neuterA.log" && grep -qE 'FAIL|✗|×' "$S/neuterB.log" && grep -q 'does NOT leak an abandoned API draft' "$S/neuterA.log" && ! grep -E '(✗|×|FAIL).*does NOT leak' "$S/neuterA.log" && npx vitest run src/lib/wizard/localStorage.test.ts "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.sessionid-fence.test.tsx" && grep -q 'RESTORED-BYTE-IDENTICAL\|shasum' .planning/phases/164.2.1-sessionid-fence/164.2.1-02-SUMMARY.md</automated>
  </verify>
  <done>Both neuter logs show failures naming the new arms; neuter A's log shows the SEAMRIM pins passing; both files' sha before == after; the neuter token is absent; the two test files are green after restore; SUMMARY carries the evidence verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gates — full suite alone, tsc, migration untouched, pins unmodified (SC-3, SC-4, SC-5)</name>
  <files>.planning/phases/164.2.1-sessionid-fence/164.2.1-02-SUMMARY.md</files>
  <action>
    <!-- planner-discipline-allow: ^-[^-] -->
    Run, each in its own block, with NOTHING else on the box (contention fakes regressions — vitest → then tsc, never concurrently): `npm test` (full suite), `npx tsc --noEmit`. Then the three static gates: `git diff --stat main -- supabase/` must print nothing (SC-4 — the migration is untouched); `git diff main -- src/lib/wizard/localStorage.test.ts | grep -c '^-[^-]'` must print 0 (SC-5 — pure append; if any pin had to change it must be recorded as an INVERSION with its history, and this plan expects none); `grep -c 'if (loaded.wizardSessionId && (loaded.source ?? "api") === source' src/lib/wizard/localStorage.ts` must print 1 (SC-3 — source clause byte-identical). Record the full-suite counts (files/tests/duration) and each gate's output in SUMMARY.md. A red full suite is a STOP, not a "flake": re-run alone once; if still red, report the failing test names.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && test -z "$(git diff --stat main -- supabase/)" && test "$(git diff main -- src/lib/wizard/localStorage.test.ts | grep -c '^-[^-]')" = 0 && test "$(grep -c 'if (loaded.wizardSessionId && (loaded.source ?? "api") === source' src/lib/wizard/localStorage.ts)" = 1 && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>Full suite green alone on the box; tsc clean; `supabase/` diff empty; `localStorage.test.ts` has zero removed lines vs main; the SEAMRIM source clause is present exactly once and unchanged; SUMMARY records the numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree ↔ neuter harness | a transient production-code edit that must not survive; the only writer is this task's `sed`, the only restorer is the byte backup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.2.1-05 | Tampering | neutered `localStorage.ts` / `WizardClient.tsx` accidentally committed | high | mitigate | restore from byte backup, `shasum` before == after asserted in `<verify>`, token count 0 asserted, `git checkout --` forbidden; plan 02 commits only SUMMARY.md |
| T-164.2.1-06 | Repudiation | a "RED" claimed but never observed (vacuous evidence) | medium | mitigate | `<verify>` greps the captured neuter logs for failure markers AND for a passing SEAMRIM pin; the RED tail is pasted verbatim into SUMMARY |
| T-164.2.1-SC | Tampering | npm installs | low | accept | none in this plan |
</threat_model>

<verification>
- Both neuters RED on the new arms, SEAMRIM green under neuter A, restores byte-identical
- `npm test` alone green; `npx tsc --noEmit` clean
- `git diff --stat main -- supabase/` empty; `localStorage.test.ts` append-only vs main
</verification>

<success_criteria>
- SC-2 evidenced with pasted shasum lines and RED output
- SC-3, SC-4, SC-5 gates each pass by command, recorded in SUMMARY
- Working tree contains no neuter residue; only SUMMARY.md is new in this plan
</success_criteria>

<output>
Create `.planning/phases/164.2.1-sessionid-fence/164.2.1-02-SUMMARY.md` when done
</output>
