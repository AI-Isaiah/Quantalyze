---
phase: 164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/test_prod_prober_cadence.sql
  - .github/workflows/ci.yml
  - src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts
autonomous: true
requirements: [PROBER-CADENCE-UNDELIVERED-01]
tags: [sql-gate, ci, anti-vacuity, pg-lane, observability]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`sql-tests` never hands `supabase/tests/test_prod_prober_cadence.sql` to `psql`. Proven by a stub-`psql` invocation log the contract test reads: every other corpus file's basename appears in it, that one does not."
    - "The exclusion is a JOB-level decision taken BEFORE `psql -f` is invoked, not a file-level bail-out. WR-03's two nets stay fully armed for every executed file, and the .sql file never gains a `RAISE NOTICE 'SKIP:'`/`RETURN` shape."
    - "The exclusion is PRINTED on every run — a `::notice file=…::` line per excluded file carrying the marker's own reason, plus a census line before the loop and a count in the closing summary. A run in which the exclusion happened silently is impossible."
    - "The excluded file stays inside the STATIC accounting: its sentinel is still counted, its roster-vs-count coherence still checked, its `n_arms` ≤ non-comment RAISE-EXCEPTION bound still enforced. `SENTINEL_FLOOR` and `ARMS_FLOOR` are unchanged, and so is ci.yml's per-file derivation table."
    - "The closing summary no longer overclaims: it reports how many files ran out of how many were found, how many were excluded, and how many sentinels were VERIFIED against this run as distinct from how many were DECLARED."
    - "The defect this phase removes is reproducible in the harness and measurably gone: injecting a `relation \"net._lane_posts\" does not exist` psql failure at a NON-excluded file reddens the step, and the same injection aimed at the excluded file cannot, because the file is never executed."
  artifacts:
    - "supabase/tests/test_prod_prober_cadence.sql — a single-line `-- LANE-ONLY: {json}` header declaration naming the object, the fixture, the owning job and the reason"
    - ".github/workflows/ci.yml — `lane_only_marker()` predicate, the pre-loop census + print, the per-file execution branch, the honest summary line and standing limit (5)"
    - "src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts — stub invocation log + failure injection, and three new scenarios"
  key_links:
    - "the marker line in the .sql file ↔ `lane_only_marker()`'s `^-- LANE-ONLY:` anchor in ci.yml — a marker that is indented, wrapped, or spelled differently is invisible and the file silently starts running against shared TEST again"
    - "the `else` branch in the per-file loop ↔ the static accounting that follows it — the static block must sit OUTSIDE the branch, or `sentinels_declared` falls to 10, `arms_declared` to 206, and both floors start failing every run"
    - "`$out` is a SINGLE shared log file overwritten per iteration — every `$out`-derived check must live inside the execution branch, or a skipped file inherits the PREVIOUS file's output and is judged on it"
---

<objective>
Stop `sql-tests` from executing `supabase/tests/test_prod_prober_cadence.sql` against shared TEST, by
a file-level exclusion the job decides before `psql` is invoked and prints on every run — while the
whole static-analysis half of the anti-skip block keeps running for that file and neither floor moves.

Purpose: main is RED (run `35347643700`, merge `eec8a659`) on
`ERROR: relation "net._lane_posts" does not exist`. `sql-tests` is blocking in the `frontend`
aggregator and Railway skips the analytics-service deploy while main CI is red, so this one file is
what keeps `POST /api/prober-cadence-alert` off production.

Output: the marker, the ci.yml mechanism, and a harness that can tell the difference between "the
file was excluded" and "the file ran and passed".
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-CONTEXT.md
@.planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-RESEARCH.md
@.planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-PATTERNS.md

@src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts
@scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — the marker is declared, the job reads it, the file is never executed, and the static accounting survives</name>
  <files>supabase/tests/test_prod_prober_cadence.sql, .github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` — the step named `Run SQL self-tests against test Supabase project`. Read it whole from the `- name:` line to the `Release shared-test-db mutex (best effort)` step. Measured at HEAD it starts at `:3169`; cite it by STEP NAME from here on, never by line number (the `[164.7-CITATION-DRIFT-01]` class).
    - `supabase/tests/test_prod_prober_cadence.sql` lines 1-200 only — the header block and the `RED-UNDER-SETUP` declaration. Do not read the 500 lines of arms; nothing in this task edits them.
    - `scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql` lines 1-40 — the header that states the object is never applied to TEST or PROD. Its wording is the source for the marker's `reason`.
  </read_first>
  <action>
Two edits, one mechanism.

**(a) Declare the marker in the gate file.** Add ONE physical line to
`supabase/tests/test_prod_prober_cadence.sql`, in the header block immediately ABOVE the existing
`RED-UNDER-SETUP` line so the two structured declarations sit together. It must begin at column 0
with the exact token `-- LANE-ONLY: ` followed by a single-line JSON object — the same
`-- TOKEN: {json}` idiom `RED-UNDER-SETUP` already uses in this file, which is the precedent
PATTERNS.md names. Keys: `object` set to `net._lane_posts`; `fixture` set to
`scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql`; `job` set to `sql-mutation`; `reason` a
prose sentence stating that every arm in the file asserts against the pg-net stand-in that fixture
34's own header marks as never applied to TEST or PROD, that shared TEST carries the real `pg_net`
so a version of this gate made to run there would issue genuine outbound HTTP from shared CI
infrastructure on every run, and that the file's arms execute and are mutation-checked twin-by-twin
on the pg-lane under `sql-mutation`.

⛔ Do NOT add a `RAISE NOTICE` skip, a `RETURN`, or any executable change to this file. Its arms,
its roster, its sentinel and its `RED-UNDER-SETUP` list are untouched — the only edit is one comment
line. WR-03 exists to fail a file that RUNS and bails out; nothing here makes this file do that.

**(b) Implement the exclusion in `.github/workflows/ci.yml`.** All of it inside the
`Run SQL self-tests against test Supabase project` step.

*One predicate, defined once.* Add a shell function `lane_only_marker` taking one argument and
echoing the file's marker line or the empty string — implemented as `grep` with the `-a` and `-m1`
flags (the NUL-safe convention NET 1 already uses on `"$f"`, and the reason this repo's own
`wizardErrors.test.ts` lesson exists) anchored on `^-- LANE-ONLY:`, with a `|| true` so a
no-match does not trip `set -e`. Define it once, above the discovery block, and use it in BOTH
places below so the printed set and the skipped set cannot drift apart. ⚠️ Use a string/counter,
never a bash array, for the census: an empty-array expansion under `set -u` is not portable to the
bash the contract test spawns.

*Census and print, BEFORE the loop, AFTER the empty-glob guard.* Immediately after the existing
`Found ${#files[@]} test file(s):` print, walk `files[@]` with `lane_only_marker`, count the
matches into `lane_only_count`, and emit one `::notice file=$f::` annotation per member whose
message states the file is NOT EXECUTED against shared TEST and carries the marker's own text
(strip the `-- LANE-ONLY: ` prefix). When the count is zero, print a line saying so. ⛔ Do NOT
remove anything from `files[@]`: the array stays whole and `${#files[@]}` must keep reporting the
real corpus size, which `src/__tests__/lint-sql-gates.test.ts` pins at 74.

*Skip EXECUTION inside the loop.* At the top of the `for f in "${files[@]}"` body, set
`lane_marker` from `lane_only_marker "$f"`. Wrap the entire existing execution region in
`if [ -n "$lane_marker" ]; then … else … fi`, with the existing body moved verbatim into the
`else`. The wrapped region is exactly: the `=== Running: $f ===` banner, the `psql` invocation, the
`cat "$out"`, the non-zero-status propagation, NET 1, NET 2, their shared error block, the
`partials` census, and the `blockterm`/`bt_hits` block. The `if` branch prints a banner naming the
file as lane-only and echoes the marker's reason. Also increment an `executed` counter in the
`else` branch.

⛔ EVERY one of those is `"$out"`-derived, and `$out` is ONE file reused across iterations. A
lane-only file that fell through them would be judged on the PREVIOUS file's captured output —
NET 1 and NET 2 would attribute another file's skip notice to it. This is why the branch is drawn
around the whole region and not only around the three checks the RESEARCH correction enumerates.

*Keep the STATIC accounting OUTSIDE the branch.* The `sentinel` extraction and everything it gates
— `sentinels_declared`, `arms_declared`, the roster-vs-count coherence check, and the
`n_arms` ≤ non-comment RAISE-EXCEPTION bound — all read `"$f"` only and must run for EVERY file,
lane-only included. Inside that block, the single `"$out"`-derived check (the one that greps the
captured log for the declared sentinel) gets three arms: for a lane-only file, print a line saying
the sentinel is DECLARED but not checked against a run and naming `sql-mutation` on the pg-lane as
the job that owns this file's execution coverage; otherwise keep today's found/not-found pair,
incrementing a new `sentinels_verified` counter on the found arm.

*Make the closing summary honest.* Today it claims all N tests passed and N sentinels verified,
both of which become false. Rewrite it to report the executed count out of the found count, the
excluded count, and `sentinels_verified` as distinct from `sentinels_declared`. ⚠️ The phrase
`SQL self-tests passed` must survive as a contiguous substring — the contract test asserts on it —
so keep the shape "N of M SQL self-tests passed". Append a standing limit **(5)** to the printed
limits paragraph: a lane-only file's arms are COUNTED here but not EXECUTED here, their execution
and mutation coverage live in `sql-mutation` on the pg-lane, and nothing in this job would notice
if that job stopped running them.

*Argue the distinction, in a comment block above the new mechanism.* Locked constraint 1 requires
the artifact to ARGUE it rather than assume it. State: a whole-file printed skip is a file that
CONNECTED, RAN, and then withheld its assertions while exiting 0 — it makes a claim about this lane
that is false, and it is indistinguishable from a pass in the only channel the loop used to read,
which is why WR-03 fails it. A LANE-ONLY exclusion is a decision the JOB takes before `psql -f` is
ever invoked, written down in the workflow, printed on every run with its reason, over a file that
makes NO claim about this lane because it was never handed to it — and whose coverage claim is made
by a different job that can satisfy its preconditions. The difference is who decides and whether a
claim is made. Then state what stays armed: both nets for all other files, and for this file the
whole static layer, so the cheapest neutering — delete an arm, edit the count down — is still
caught here.

⛔ Neither floor moves and ci.yml's per-file derivation table keeps its row for this file. The
static block is what makes that correct: the counts it feeds are unchanged because they were never
execution-derived. ⚠️ Prefer not to restate either floor's value inside the new comment prose —
`src/__tests__/drift-check-scripts.test.ts` counts those literals in this file, and this repo has a
four-times-stale record of restated constants.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts && node scripts/lint-sql-gates.mjs --self-test && node scripts/lint-sql-gates.mjs && node scripts/mutation-runner/run.mjs --parse-only && git diff -w --stat .github/workflows/ci.yml</automated>
    <human-check>`git diff -w .github/workflows/ci.yml` shows ONLY the new lines — no content change inside the region that moved into the `else`. A whitespace-blind diff that shows edits inside the moved region means something other than indentation changed there.</human-check>
  </verify>
  <done>
`npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` passes all 18
pre-existing tests (baseline measured at HEAD: 18 passed, ~20 s), including the E1/E3 derivation
bijection and the floors pin, with neither floor and no table row edited.
`node scripts/lint-sql-gates.mjs --self-test` reports its rule count and the corpus scan exits 0.
`node scripts/mutation-runner/run.mjs --parse-only` still prints
`supabase/tests/test_prod_prober_cadence.sql: 7 prose / 7 twin(s) / 0 waiver(s)` and
`✅ No static defects` — the new comment line did not disturb `RED-UNDER-SETUP` parsing.
`git diff -w` over ci.yml shows no content change inside the region wrapped into the `else`.
  </done>
  <reversibility rating="reversible">The mechanism is additive and confined to one workflow step plus one comment line; reverting restores today's behaviour exactly.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the exclusion MEASURABLE — stub invocation log, failure injection, and three scenarios</name>
  <files>src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts</files>
  <read_first>
    - `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` lines 1-190 — `extractRunScript`, `PSQL_STUB`, `runGate`, and the existing `STUB_*` env conventions. Read once; the scenarios below follow the same shape as the ones at lines 190-300.
  </read_first>
  <behavior>
    - Given a default run over the real corpus, the stub-psql invocation log contains the basename of every corpus file EXCEPT `test_prod_prober_cadence.sql`, and contains at least 70 entries — so "the log is empty" cannot pass as "the file was excluded".
    - Given a default run, stdout carries a `::notice file=supabase/tests/test_prod_prober_cadence.sql::` annotation whose text names the file as not executed and carries the marker's reason, and the closing summary names a non-zero excluded count.
    - Given a psql failure injected at a NON-excluded file reproducing the shipped defect, the step exits non-zero and names that file. Given the SAME injection aimed at `test_prod_prober_cadence.sql`, the step exits 0 — because the file is never handed to psql. The pair is the measurement; either arm alone is not.
    - Given a default run, the step's summary reports declared-sentinel and declared-arm totals equal to the step's OWN `SENTINEL_FLOOR` and `ARMS_FLOOR` values (read out of the extracted script, never restated in the test), proving the excluded file stayed inside the static accounting.
    - Given a default run, the step does NOT print the per-run sentinel-verified line for `test_prod_prober_cadence.sql` — the guard-the-guard that proves the skipped `$out`-derived check really is skipped and this scenario cannot decay into a duplicate of the green-path test.
  </behavior>
  <action>
Extend `PSQL_STUB` with two env-driven capabilities, in the same style as the existing
`STUB_SKIP_BASENAME` / `STUB_NO_SENTINEL` / `STUB_DARK` switches:

- `STUB_INVOCATION_LOG` — when set and the invocation carries a `-f` argument, append the file's
  basename to that path. This is the artifact that turns "was it executed?" from an inference into
  a measurement.
- `STUB_FAIL_BASENAME` — when the `-f` file's basename matches, print a line reproducing the
  shipped defect verbatim (a psql `ERROR:` naming `relation "net._lane_posts" does not exist`) and
  exit 3, which is the exit code CI run `35347643700` recorded.

Then add three `it(...)` blocks to the existing `describe`, each with the 90-second timeout the
corpus-wide scenarios already use, and each carrying a comment stating what it would have caught:

1. The never-executed scenario. Allocate a log path under the existing temp `workdir`, pass it via
   `STUB_INVOCATION_LOG`, run the gate at the repo root, then read the log. Assert the excluded
   basename is absent, that the log length exceeds 70 (so an empty or missing log fails rather than
   passes), and that stdout carries both the `::notice file=…::` annotation for the excluded file
   and an excluded count in the summary.
2. The calibration pair. First arm: `STUB_FAIL_BASENAME` set to a file that is NOT excluded —
   `test_retention_crons_safe.sql` — asserting a non-zero exit and an error naming that file.
   Second arm: the identical injection aimed at `test_prod_prober_cadence.sql`, asserting exit 0
   and that the defect string never reaches stdout. Write the comment so the next reader
   understands the first arm exists to prove the injection is real, and that without it the second
   arm would pass against a stub that simply never fires.
3. The static-accounting scenario. Parse `SENTINEL_FLOOR` and `ARMS_FLOOR` out of `SCRIPT` with the
   regexes the file already uses at its floors pin, and assert the run's summary line reports
   declared totals equal to them — proving the excluded file's sentinel and arms were still counted.
   ⛔ Do NOT write either integer as a literal in this test: the repo already holds four mirrors of
   those numbers and a fifth is how they go stale. Add the guard-the-guard assertion that the
   per-run sentinel-verified line was NOT printed for the excluded file.

Finally, one honesty fix to an existing test: the scenario titled
`passes a run where every file executes and prints its sentinel` becomes false the moment a file is
excluded. Rename it so it says every NON-EXCLUDED file, and add an assertion that the run reported
the exclusion. ⛔ Change nothing else about it — its `SQL self-tests passed` and exit-0 assertions
are pre-existing coverage.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts</automated>
  </verify>
  <done>
The suite runs 21 tests, all passing: the 18 pre-existing ones (one of them renamed, not replaced)
plus the three new scenarios. The never-executed scenario's log assertion is driven by a real file
written by the stub, not by a string match on stdout.
  </done>
</task>

<task type="auto">
  <name>Task 3: Calibrate — neuter each of the three new properties, observe RED, restore byte-identically</name>
  <files>.github/workflows/ci.yml, supabase/tests/test_prod_prober_cadence.sql, src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts</files>
  <read_first>
    - `~/.claude/CLAUDE.md` and this repo's `CLAUDE.md` — the destructive-edit rule. Re-read it before the first mutation.
  </read_first>
  <action>
Three neuter-observe-restore cycles. This is the anti-vacuity discipline this repo scopes to gates
pinning data-integrity or user-facing behaviour: the exclusion is what lets a deploy reach
production, so its gates must be shown able to fail.

⛔ PROTOCOL, and it is not optional. Before EACH mutation, `cp` a byte backup of the file to the
session scratchpad and record `shasum -a 256` of the original. Restore by copying the backup BACK
over the file — NEVER by `git checkout --`, which restores to HEAD and would silently destroy every
uncommitted edit from tasks 1 and 2. After each restore, re-take `shasum -a 256` and confirm it
equals the recorded original. Re-take the backup after any intervening edit; a stale backup is how
this repo has lost work before.

**Calibration 1 — the exclusion is never silent.** In ci.yml, delete the `::notice file=…::`
emission from the pre-loop census (leave the counting intact). Run the suite. EXPECT: the
never-executed scenario reds on its missing-annotation assertion, while the calibration pair stays
green — which is what proves the two scenarios measure different properties. Restore, re-verify.

**Calibration 2 — the static accounting really does survive.** In ci.yml, wrap the
`sentinels_declared` increment so it is skipped for a lane-only file. Run the suite. EXPECT: the
static-accounting scenario reds because the declared total no longer reaches the step's own
`SENTINEL_FLOOR`, and the step itself fails — which is exactly the self-inflicted red the RESEARCH
correction predicts for the rejected `continue`-at-top design. Restore, re-verify.

**Calibration 3 — the marker is what does the work.** In
`supabase/tests/test_prod_prober_cadence.sql`, delete the `-- LANE-ONLY:` line. Run the suite.
EXPECT: the never-executed scenario reds (the basename now appears in the invocation log), and the
second arm of the calibration pair reds (the injected defect now reaches the step and fails it) —
which together prove the marker, not some incidental property of the corpus, is what removes the
file from the lane. Restore, re-verify.

Record all three observations — the mutation, the assertion that reddened, and the restored digest
— in the plan SUMMARY. An observation not written down is not a calibration.

⚠️ Then run the collateral check. Five other suites name this .sql file, and the repo has a
measured incident where a directory-scoped run was green while a `src/__tests__/` mirror was red.
Run the named suites, then the FULL suite serially, with nothing else running on the box.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && npx vitest run src/__tests__/drift-check-scripts.test.ts src/__tests__/lint-sql-gates.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/mutation-runner-floors.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts</automated>
  </verify>
  <done>
Three calibrations performed and recorded, each naming the mutation, the specific assertion that
went RED, and a post-restore `shasum -a 256` equal to the pre-mutation digest. The five collateral
suites pass, including `drift-check-scripts.test.ts`'s floor mirror (which still finds the
unmoved floor literals in ci.yml) and `lint-sql-gates.test.ts`'s 74-file corpus pin. A full
`npm test` run is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI job → shared TEST database | `sql-tests` holds a credential to infrastructure other people's CI also uses; anything it executes there is not private |
| repo corpus → `sql-tests` execution set | a `.sql` file now declares, in its own text, that the job must not run it — a self-granted exemption crossing into the job's decision |
| gate file → shared `pg_net` | the excluded file's real-world analogue performs outbound HTTP with a Vault-held service key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.1.1.1-01 | Elevation of Privilege | the `-- LANE-ONLY:` marker as a self-granted exemption | high | mitigate | Plan 02 pins the excluded SET as sites and cross-checks each marker's declared object against the file's own non-comment body, so a marker pasted onto an unrelated gate to silence it fails the contract test |
| T-164.1.1.1-02 | Information Disclosure | "fixing forward" by applying fixture 34 to TEST or wiring the gate to real `pg_net` | high | mitigate | File-level exclusion, never accommodation. The marker's `reason` and the ci.yml argument block both state that shared TEST carries the real `pg_net` and that a working version of this gate would issue genuine outbound HTTP from shared CI on every run |
| T-164.1.1.1-03 | Repudiation | a silent exclusion leaving no record of what did not run | medium | mitigate | Task 1's per-file `::notice` census, the excluded count in the summary, standing limit (5), and Task 2's scenario asserting the annotation is present |
| T-164.1.1.1-04 | Tampering | using the exclusion to walk the arm floors down | high | mitigate | The static accounting stays outside the execution branch, both floors and the derivation table are unchanged, and Task 2's static-accounting scenario plus Task 3's calibration 2 prove the counts still reach the step's own floors |
| T-164.1.1.1-SC | Tampering | npm/pip/cargo installs | low | accept | Measured: this plan runs no package-manager install. Every task edits files already in the checkout, and RESEARCH.md records that no Package Legitimacy Audit is warranted |
</threat_model>

<verification>
- `npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` — 21 passing.
- `node scripts/lint-sql-gates.mjs --self-test` then `node scripts/lint-sql-gates.mjs` — both exit 0.
- `node scripts/mutation-runner/run.mjs --parse-only` — `✅ No static defects`, prober file still 7 prose / 7 twins.
- `npm test` — full suite serial, green.
- ⛔ `sql-tests` itself is NOT runnable here: it needs `TEST_SUPABASE_DB_URL`, a shared-TEST credential. The contract test's extracted-script harness over the real corpus with a stub `psql` is the local proxy, and the real falsification is a CI run on push. Do not invent a local invocation of the job.
- ⚠️ CHANGELOG and VERSION are the ship step's, not this plan's. Do not add repo-global ledgers to this plan's file set.
</verification>

<success_criteria>
1. `supabase/tests/test_prod_prober_cadence.sql` is never passed to `psql` by the `sql-tests` step, proven by a stub invocation log rather than by inspection.
2. The exclusion is printed per-file with its reason, counted in the summary, and named in the step's standing limits.
3. `SENTINEL_FLOOR`, `ARMS_FLOOR`, and ci.yml's per-file derivation table are byte-unchanged, and the excluded file still contributes to the declared totals.
4. The gate file gains exactly one comment line and no executable change — no printed skip, no `RETURN`.
5. Each of the three new properties has been neutered, observed RED, and restored to a matching digest.
</success_criteria>

<output>
Create `.planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-01-SUMMARY.md` when done.
</output>
