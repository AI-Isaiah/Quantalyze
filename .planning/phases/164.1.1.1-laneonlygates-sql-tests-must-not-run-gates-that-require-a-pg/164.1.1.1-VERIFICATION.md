---
phase: 164.1.1.1-laneonlygates
verified: 2026-09-18T18:45:00Z
status: passed
score: 11/11 must-haves verified
covered_files:
  - ".github/workflows/ci.yml"
  - ".planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-01-PLAN.md"
  - ".planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-01-SUMMARY.md"
  - ".planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-02-PLAN.md"
  - ".planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-02-SUMMARY.md"
  - ".planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-CONTEXT.md"
  - ".planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-PATTERNS.md"
  - ".planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-RESEARCH.md"
  - ".planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/164.1.1.1-REVIEW.md"
  - ".planning/phases/164.1.1.1-laneonlygates-sql-tests-must-not-run-gates-that-require-a-pg/deferred-items.md"
  - "src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts"
  - "supabase/tests/test_prod_prober_cadence.sql"
covered_digest: "v1:sha256:4e2d63cc04093dae8dbca29ce94492e358626cdd26471dc5036455c67f91c54c"
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Push this branch (or land the PR) and observe the actual `sql-tests` job in GitHub Actions against real shared TEST."
    status: DISCHARGED 2026-09-18 — observed, not inferred
    evidence: |
      PR #817 merged as f04d224a. CI run 35370424448 on the PR head: sql-tests = success,
      whole run conclusion success. Read verbatim from the job log (105688184321):

        === Skipping (LANE-ONLY): supabase/tests/test_prod_prober_cadence.sql ===
        73 of 74 SQL self-tests passed — no whole-file skips; 1 excluded (LANE-ONLY, still
        counted in the static accounting above); 10 of 11 completion sentinel(s) verified
        against THIS run, covering 213 declared arms.

      Matches the prediction on every term: excluded count 1, executed 73, ARMS_FLOOR's 213
      still covered. `relation "net._lane_posts" does not exist` occurs 0 times in the log —
      the error that made run 35347643700 red is gone.

      ⚠️ The summary honestly reports 10 of 11 sentinels verified rather than 11: the excluded
      file's sentinel genuinely was not checked this run, while its 7 arms still count in the
      static accounting. That asymmetry is the designed behaviour, not a shortfall.
    why_human: "The contract test's stub-psql harness is an accepted LOCAL PROXY per both plans' own `<verification>` sections; it cannot exercise real GitHub Actions, real shared-TEST credentials, or a real psql against a live server. That link is now closed by the run above."
---

# Phase 164.1.1.1: LANEONLYGATES Verification Report

**Phase Goal:** `sql-tests` executes only gates that CAN run against shared TEST. A gate that
requires a pg-lane-only object declares so in its own text, is excluded by the JOB before `psql` is
ever invoked — never by an in-file bail-out, which WR-03 exists to fail — and every exclusion is
printed on every run. The excluded SET is pinned as SITES, not a count, and bound to the object the
file's own assertion bodies query, so neither a new exclusion nor a one-for-one swap can land
unseen. The floors do NOT move.

**Verified:** 2026-09-18T18:45:00Z
**Status:** human_needed (all 11 must-have truths verified by code inspection, live test runs, and
independently-reproduced calibration; one item — the real GitHub Actions run against shared TEST —
cannot be exercised locally and has not yet happened on this unpushed branch)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `sql-tests` never hands `test_prod_prober_cadence.sql` to `psql` | ✓ VERIFIED | Ran `ci-anti-skip-gate.contract.test.ts` live: 30/30 pass, including `STUB_INVOCATION_LOG` scenario proving the excluded basename is absent from >70 real invocations. Code read of `ci.yml`'s per-file loop (:3407-3417) confirms `lane_marker` is checked and the `psql` invocation lives only in the `else` branch. |
| 2 | Exclusion is a JOB-level decision before `psql -f`, never an in-file bail-out; WR-03's two nets stay armed | ✓ VERIFIED | Read `ci.yml:3407-3555`: `psql -f "$f"`, NET 1, NET 2, `partials`, `blockterm` are all inside the `else` branch verbatim (unmoved region confirmed by plan 01's `git diff -w` check, re-confirmed by my own read). Gate file (`test_prod_prober_cadence.sql`) gained exactly one comment line — no `RAISE NOTICE`/`RETURN` added (`git diff --stat` shows 1 insertion, 0 deletions). |
| 3 | Exclusion is PRINTED on every run (`::notice::` per file, excluded count in summary) | ✓ VERIFIED | Read `ci.yml:3241-3254` (pre-loop census + `::notice file=$f::`) and `:3806` (closing summary states executed/found/excluded/sentinels-verified-vs-declared). Contract test scenario asserts the annotation and excluded count directly. |
| 4 | `SENTINEL_FLOOR`(11)/`ARMS_FLOOR`(213) and the per-file derivation table are unchanged; excluded file still counted | ✓ VERIFIED | `git diff origin/main...HEAD -- .github/workflows/ci.yml` shows **zero** hits for `SENTINEL_FLOOR`/`ARMS_FLOOR` — both integers and the whole derivation-table comment block are byte-identical to `origin/main`. Manually re-summed the 11-row table: `13+9+6+15+18+8+7+4+103+7+23 = 213`, `test_prod_prober_cadence.sql 7` present as a row. |
| 5 | Closing summary is honest (no longer claims all N passed when some were excluded) | ✓ VERIFIED | `ci.yml:3806` reads `"${executed} of ${#files[@]} SQL self-tests passed — no whole-file skips; ${lane_only_count} excluded..."`, plus a 5th standing limit (`:3807`) stating the excluded file's coverage lives in `sql-mutation`. |
| 6 | Defect (shipped `relation "net._lane_posts" does not exist`) reddens a non-excluded file and never reaches the excluded one | ✓ VERIFIED | Contract test's calibration-pair scenario (`STUB_FAIL_BASENAME`) passes live; Task 3's SUMMARY calibration log independently reproduced this via manual neuter/restore with matching `shasum -a 256` before/after. |
| 7 | Excluded set pinned as SITES (not a count), re-derived from corpus | ✓ VERIFIED | `LANE_ONLY_SITES` + `deriveLaneOnlySites()` present and exercised live (30/30 pass). Plan 02's M1b calibration (marker MOVED between files, count unchanged at 1) reddened the SET pin — the exact property a count cannot have. |
| 8 | Marker cannot be pasted onto an unrelated gate to silence it (FORWARD cross-check) | ✓ VERIFIED — **and the round-2 comment-stripping regression I independently reproduced and confirmed fixed.** See below. |
| 9 | A lane-only gate cannot land unmarked (REVERSE cross-check) | ✓ VERIFIED — same fix as #8; REVERSE reads the same `stripSqlComments()`. |
| 10 | Vault near-miss (`test_analytics_service_settings_and_vault_tick.sql`) stays OUT of the derived set | ✓ VERIFIED | File carries no `-- LANE-ONLY:` marker and contains 0 occurrences of `net._lane_posts` (both grepped directly). Contract test's guard passes live. |
| 11 | Excluded file's mutation coverage stated from a regenerated (not restated) runner line | ✓ VERIFIED | Ran `node scripts/mutation-runner/run.mjs --parse-only` myself: `test_prod_prober_cadence.sql: 7 prose / 7 twin(s) / 0 waiver(s)`, `coverage: files 47/74`, exit 0, matches SUMMARY exactly. **Real (non-static) mutation coverage independently confirmed** from CI job `105624583310` (`sql-mutation`, `head_branch: main`, `conclusion: success`): log line `file test_prod_prober_cadence.sql: sections 7 / judged 7 / annotated 7 / waived 0 / biting 7`, with `arms: 402/402/0` and `lane-invocations: 402` — the two independent tallies agree. |

**Score:** 11/11 truths verified (0 present-but-behavior-unverified)

### The BL-01 regression fix (round 2) — independently reproduced

The review's round-2 BLOCKER (`stripSqlComments` stripping block comments *before* line comments,
which silently deleted real assertion bodies — 21,070 and 16,065 bytes out of two corpus files —
and would have let a file query a declared LANE-ONLY object invisibly to the REVERSE check) was
fixed in commit `e087f754` by reordering the two strip passes.

I did not trust this from the SUMMARY/REVIEW text alone. I:
1. Backed up `ci-anti-skip-gate.contract.test.ts` and recorded its `shasum -a 256`
   (`788d532e...`).
2. Reverted `stripSqlComments` to the pre-fix (block-comments-first) order.
3. Ran only the `"stripSqlComments does not eat the assertion body it is meant to expose"` describe
   block: **2 of 3 new regression-pin tests went RED** — `keeps real SQL that sits between two
   incidental, unrelated /*-shaped substrings` (lost `net._lane_posts` from a synthetic file) and
   `never shrinks any real corpus file's body` (flagged `test_ledger_refresh_composite_arm.sql
   (-3060 bytes)` and `test_ledger_refresh_fanout.sql (-3561 bytes)` as damaged) — exactly the
   defect class BL-01 described, reproduced independently rather than re-run from the fixed state.
4. Restored the file from the backup; `shasum -a 256` matched the pre-mutation digest exactly;
   `npx vitest run` on the full contract file returned 30/30 passing again.

This confirms the fix is real, holds under a direct reversion, and the two regression pins that
guard it actually bite.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/tests/test_prod_prober_cadence.sql` | One `-- LANE-ONLY: {json}` header line, no executable change | ✓ VERIFIED | Line 182, `git diff --stat` origin/main: `1 file changed, 1 insertion(+)`. JSON parses; `object`/`fixture`/`job`/`reason` keys present. |
| `.github/workflows/ci.yml` | `lane_only_marker()`, census+print, per-file branch, static-accounting survival, honest summary | ✓ VERIFIED | All present and read directly; floors/derivation table byte-unchanged vs `origin/main`. |
| `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` | Stub invocation log, failure injection, SITES register, FORWARD/REVERSE cross-checks, vault guard | ✓ VERIFIED | 30/30 tests pass live; every described scenario present and exercised. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `-- LANE-ONLY:` marker line (`.sql` file) | `lane_only_marker()` predicate (`ci.yml`) | `^-- LANE-ONLY:` grep anchor | ✓ WIRED | Same literal anchor confirmed in both files (`ci.yml:3218` grep pattern; contract test `LANE_ONLY_ANCHOR = "-- LANE-ONLY:"` used with `.startsWith`). |
| Marker's declared `object` | File's own comment-stripped text | FORWARD cross-check | ✓ WIRED | Verified live + independently re-broken/re-fixed (see BL-01 section above). |
| Derivation's marker regex | `ci.yml`'s exclusion predicate | Both read the SAME `.sql` corpus with the SAME anchor | ✓ WIRED | Confirmed by reading both call sites; no drift possible without a red SET-pin test. |
| `else` branch (execution region) | Static accounting block | Region boundary | ✓ WIRED | Read directly: `psql`, NET 1/2, partials, blockterm all inside `else`; sentinel/roster/arms-bound checks all outside it, gated only by a 3-arm branch on the ONE `$out`-derived check. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Contract suite exercises the real corpus with a stub `psql` | `npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` | `Test Files 1 passed (1)`, `Tests 30 passed (30)` | ✓ PASS |
| Regression pins for BL-01 actually bite | Reverted strip order, re-ran the one describe block, restored, re-verified digest | 2/3 assertions reddened on revert; suite green + digest-equal after restore | ✓ PASS |
| Static mutation-runner parse matches the SUMMARY's regenerated evidence | `node scripts/mutation-runner/run.mjs --parse-only` | `test_prod_prober_cadence.sql: 7 prose / 7 twin(s) / 0 waiver(s)`, exit 0, `✅ No static defects` | ✓ PASS |
| `lint-sql-gates` corpus scan clean | `node scripts/lint-sql-gates.mjs --self-test && node scripts/lint-sql-gates.mjs` | `7 rules, red+green each`; `scanned 74 file(s); 0 finding(s)` | ✓ PASS |
| Collateral suites (drift-check-scripts, mutation-annotation-parser, mutation-runner-floors, gate-family-meta, check-planning-hygiene, verify-plan-anchors) | `npx vitest run <6 files>` | `Test Files 4 passed (4)`, `Tests 780 passed (780)`; hygiene/anchor suites separately `100 passed (100)` | ✓ PASS |

### Probe Execution

Not applicable — this phase has no `scripts/*/tests/probe-*.sh` artifacts; its verification
instrument is the contract test's stub-`psql` harness, exercised above.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| `PROBER-CADENCE-UNDELIVERED-01` (TODOS-tracked, per this repo's convention — no `REQUIREMENTS.md` entry exists; ROADMAP §164.1.1.1 is the requirement text) | 01, 02 | `sql-tests` blocks `frontend` aggregator / Railway deploy while red on `net._lane_posts` | ✓ SATISFIED (mechanism) | All 11 truths above. TODOS/ROADMAP closure itself is owned by a later act (this residual is a *mechanism* fix, not the TODOS-entry closure — the entry's owner is Phase 164.1.1, not this phase). |

No orphaned requirements found for this phase in `.planning/REQUIREMENTS.md` or the ROADMAP's
phase-scoped requirement mapping.

### Anti-Patterns Found

None blocking. Two items are **deliberately, honestly left open** rather than closed — recording
them per the instruction not to bury a known-open WARNING:

| File | Pattern | Severity | Status |
|------|---------|----------|--------|
| `.github/workflows/ci.yml:3217` (`lane_only_marker()`) + contract test's FORWARD check | WR-01: the LANE-ONLY marker is a **self-granted exemption** — nothing verifies the named object is genuinely absent from TEST/PROD, only that the JSON parses and the object-string appears somewhere in the file's own non-comment text (which a bad-faith or careless marker could satisfy with an unreachable/cosmetic reference) | ⚠️ WARNING (accepted) | **Still open.** `164.1.1.1-CONTEXT.md`'s `<deferred>` section pre-accepted this gap before planning ("An optional cross-check asserting the marker's named fixture also appears in the file's own RED-UNDER-SETUP apply list"); the review named it explicitly (WR-01) and confirmed it is a robustness gap, not a data-integrity defect today, since the one real marker in the corpus is legitimate. I confirmed by reading `deriveLaneOnlySites()` (:613-653): the fixture-cross-check is not implemented, matching the review and CONTEXT.md exactly. |
| `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts:638` | IN-04: `-- LANE-ONLY: null` (valid JSON) throws a raw, file-unnamed `TypeError` on destructure instead of the guard's own named-file error | ℹ️ INFO (accepted) | **Still open.** Reproduced directly: `JSON.parse("null")` then destructuring `{object, fixture}` throws `TypeError: Cannot destructure property 'object' of 'payload' as it is null.` It still fails loud — no silent pass — so it does not meet round 2's fix bar per the review's own verdict, and I agree with that classification. |

Both are consistent with what the REVIEW.md and SUMMARYs already state; I am not surfacing anything
new, only confirming these two disclosures are accurate and still current at HEAD.

## Human Verification Required

### 1. Confirm the real `sql-tests` job on a pushed CI run

**Test:** Push `chore/164.1.1.1-laneonlygates` (or land the PR) and inspect the `sql-tests` job's
GitHub Actions log for the head commit.
**Expected:** The job reports the LANE-ONLY census/notice for `test_prod_prober_cadence.sql`
(`::notice file=supabase/tests/test_prod_prober_cadence.sql::LANE-ONLY — not executed against
shared TEST...`), an excluded count of 1, an executed count of 73 (of 74 files found), and the job
exits 0 — the first green `sql-tests` run since `35347643700` went red on
`ERROR: relation "net._lane_posts" does not exist`.
**Why human:** `git ls-remote --heads origin chore/164.1.1.1-laneonlygates` returns nothing — this
branch has never been pushed, and no GitHub Actions run exists for any commit on it (checked all
eight commits by SHA via the GitHub API). The contract test's stub-`psql` harness is an accepted
local proxy — both plans' own `<verification>` sections say so explicitly ("sql-tests itself is NOT
runnable here... the real falsification is a CI run on push. Do not invent a local invocation of
the job") — but it cannot exercise the real GitHub Actions runner, real shared-TEST credentials, or
the real `psql` binary. Everything mechanically verifiable without that has been verified above;
this one link in the causal chain is inherently external.

## Gaps Summary

No gaps. All 11 must-have truths (6 from plan 01, 5 from plan 02) are verified by direct code
inspection, live test execution (30/30 passing), and independently-reproduced calibration —
including re-breaking and re-confirming the fix for round 2's BLOCKER (`BL-01`, the
`stripSqlComments` ordering defect) myself rather than trusting the SUMMARY's account of it. Both
`SENTINEL_FLOOR`/`ARMS_FLOOR` and the per-file derivation table are confirmed byte-identical to
`origin/main`. Two WARNING/INFO items (WR-01, IN-04) are honestly still open, exactly as the
REVIEW.md and SUMMARYs disclose — neither is a blocker, neither is silently buried, and WR-01 is a
pre-accepted deferral recorded in `164.1.1.1-CONTEXT.md` before planning began.

The only reason this report is not `passed` is that the branch has not yet been pushed, so the one
truth this phase's own plans flag as unverifiable locally — a real `sql-tests` GitHub Actions run
against shared TEST — has not happened yet. This is not a defect in the implementation; it is the
expected next step before merge.

---

*Verified: 2026-09-18T18:45:00Z*
*Verifier: Claude (gsd-verifier)*
