---
phase: "167.1"
slug: "aumtrust-the-headline-aum-says-when-it-includes-holdings-fro"
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
block_on: high
created: "2026-09-24"
audited_at_sha: 7e1614c141991f3c25dac1ebd0dd755d18bb622b
---

# Phase 167.1 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
> Scope: the threat models of plans 01–04 (executed). Plans 05 (founder D-06 checkpoint) and
> 06 (release) are NOT executed at the audited sha; their threats are recorded as
> **not yet applicable**, not as open, and must be re-audited once those plans run.

`asvs_level` and `block_on` are the workflow defaults: `.planning/config.json` sets neither
`workflow.security_asvs_level` nor `workflow.security_block_on`.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| SSR payload -> client composer | `payload.apiKeys` and `holdingsSummary` arrive tenant-scoped by the existing SSR reads; the phase only joins them client-side | key id + sync status, holding equity (tenant-private) |
| SSR holdings -> Holdings tab / Open Positions | `source_key_sync_status` (and the new `source_key_missing` flag) are derived from the already-scoped key list | key sync status (tenant-private) |
| client figure -> commit route | the composer AUM is display and draft sizing only; the server recomputes its own AUM | dollar figure (untrusted input to the server) |
| browser -> Sentry (third party) | NEW in review round 2 (WR-04): a warning-level capture of the missing-key anomaly | an integer count and constant strings only |
| repo -> public | the repo and `.planning/` are public; CHANGELOG prose is gitleaks-scanned on push | planning prose, test fixtures |

---

## Threat Register

### Executed plans (01–04)

| Threat ID | Category | Component | Severity | Disposition | Mitigation / Evidence | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-167.1-01 | Information Disclosure | `scenario-aum-untrusted-note` copy | low | mitigate | Copy is built only by `buildKeyTrustClause` (`allocations/lib/live-holdings-summary.ts`) from `render.amount(part.amount)`, `part.unavailable`, and the constants `UNTRUSTED_KEY_SET_NOUN` / `UNKNOWN_KEY_STATUS_SET_NOUN` (`src/lib/closed-sets.ts`); no key id, label, venue or `sync_error` is interpolated. The composer's `buildUntrustedAumClause` passes only `formatUsd` and constant words. | closed |
| T-167.1-02 | Tampering (XSS) | marker rendering | low | mitigate | Rendered as React text children of `<span>` in `ScenarioComposer`; `dangerouslySetInnerHTML` occurrence count is 0 in every one of the 10 non-planning files the branch changes. | closed |
| T-167.1-03 | Tampering / money-figure integrity | `liveHoldingsSum` rewire | high | mitigate | `summarizeLiveHoldings` keeps the old loop's walk (`Object.entries(toggleByScopeRef)`), filters (`on`, `holding:` prefix, `holdingByRef` hit, contributing-set narrowing) and accumulation order; untrusted holdings are added to `total` and never subtracted. `liveHoldingsSum = liveHoldingsSummary.total`. Pin 1 in `live-holdings-summary.test.ts` asserts an untrusted holding stays in the total. `src/lib/queries.ts` and the scenario commit route: 0 changed files vs the merge base. | closed |
| T-167.1-04 | Repudiation | the defect being fixed | medium | mitigate | `showUntrustedMarker` in `ScenarioComposer` gates on `untrusted.count > 0 \|\| unknownStatus.count > 0` (count, never amount); pins 1–5 present in `live-holdings-summary.test.ts`. | closed |
| T-167.1-05 | Elevation / cross-tenant read | `statusByKeyId` join | low | accept | Verified the premise: `statusByKeyId` is built solely from `payload.apiKeys`; `managerSideKeyIds` reads only `payload.eligibleApiKeyIds` / `payload.allocatorEligibleApiKeyIds`; no new read is added (`queries.ts`, `supabase/`, `analytics-service/`, the API routes: 0 changed files). See Accepted Risks Log. | closed |
| T-167.1-06 | Tampering / money-figure integrity | `totalUnrealized` rewrite | high | mitigate | `OpenPositionsTable`: one `for` pass, same finite-else-0 rule, every row added to `totalUnrealized` unconditionally before the part classification; the untrusted test is the shared `isUntrustedKeySyncStatus`. Surface tests in `untrusted-key-status.surfaces.test.tsx`. | closed |
| T-167.1-07 | Information Disclosure | `open-positions-untrusted-note` copy | low | mitigate | Footer copy is `capitalizeFirst(buildKeyTrustClause(...))` with `formatPnl`, the constant word `P&L` and the constant unit pair; the new row label is the constant `UNKNOWN_KEY_STATUS_ROW_LABEL`. No venue, symbol, key id or `sync_error` interpolated. | closed |
| T-167.1-08 | Tampering (XSS) | footer cell | low | mitigate | React text children of the `<td>`; `dangerouslySetInnerHTML` count 0 (see T-167.1-02). | closed |
| T-167.1-17 | Tampering / money-figure integrity | override-note rewrite | high | mitigate | Note number is still `formatUsd(liveHoldingsSum)`; the clause renders only inside `{showUntrustedMarker && (...)}`, so with no flagged holding the text is `Overrides live-holdings total $X.` as before. `queries.ts` and the commit route unchanged (0 files). | closed |
| T-167.1-18 | Repudiation | State B | medium | mitigate | `overrideNoteShowsLive` branch nests the clause in the override note; the standalone marker requires `fieldShowsLive`, so one figure never carries two markers. | closed |
| T-167.1-19 | Tampering (XSS) | State B span | low | mitigate | React text only; count 0 (see T-167.1-02). | closed |
| T-167.1-10 | Information Disclosure | `closed-sets.ts` / ROADMAP prose | high | mitigate | `closed-sets.ts` diff adds only two noun constants and comment prose; test fixtures use synthetic key ids. Range gitleaks scan of the branch (merge base..HEAD, 42 commits): no leaks found. `check-planning-hygiene` run after this file was written: OK. | closed |
| T-167.1-12 | Repudiation (a claim the phase changed money) | branch before the checkpoint | medium | mitigate | Plan 04 SUMMARY records the interim byte-identity gate; re-measured at the audited sha: `src/lib/queries.ts`, `src/app/api/**`, `supabase/`, `analytics-service/` → 0 changed files vs the merge base. Plan 06 must re-run it after plan 05. | closed |
| T-167.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | `package.json` / `package-lock.json`: 0 changed files on the branch. | closed |

### Not yet applicable (plans 05 and 06 not executed at the audited sha)

| Threat ID | Category | Component | Severity | Disposition | Note | Status |
|-----------|----------|-----------|----------|-------------|------|--------|
| T-167.1-13 | Spoofing (of consent) | D-06 checkpoint | high | mitigate | Plan 05 not executed | not yet applicable |
| T-167.1-14 | Tampering / money-figure integrity | option (b) wiring | high | mitigate | Plan 05 not executed. Today `excludedUntrusted` is computed but rendered nowhere, and never enters `total`. | not yet applicable |
| T-167.1-15 | Repudiation (hidden amount) | D-20 residual | low | accept | Plan 05 not executed; the residual is already stated in code on `LiveHoldingsSummary.excludedUntrusted`. Accept entry to be logged when plan 05 lands. | not yet applicable |
| T-167.1-16 | Information Disclosure | the excludes clause | low | mitigate | Plan 05 not executed; no excludes clause renders. | not yet applicable |
| T-167.1-09 | Denial of Service (CI suppressed) | release commit / CHANGELOG | high | mitigate | Plan 06 not executed | not yet applicable |
| T-167.1-11 | Tampering (ledger clobber) | `gsd-tools windows fixed` | medium | mitigate | Plan 06 not executed | not yet applicable |
| T-167.1-20 | Information Disclosure | CHANGELOG / WINDOWS prose | high | mitigate | Plan 06 not executed | not yet applicable |
| T-167.1-21 | Repudiation | release notes / branch | medium | mitigate | Plan 06 not executed | not yet applicable |
| T-167.1-22 | Spoofing (of the founder's D-06 answer) | CHANGELOG D-06 bullet | medium | mitigate | Plan 06 not executed | not yet applicable |

*Status: open · closed · open — below high threshold (non-blocking) · not yet applicable*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Review-round surfaces checked (not in any plan register)

No executed SUMMARY carries a `## Threat Flags` section. The round-2 review fixes added surface
that no plan register names; each was checked at the audited sha:

| Surface | Origin | Finding |
|---------|--------|---------|
| Sentry capture of the missing-key anomaly | review round 2 WR-04 | **Unregistered flag, verified safe.** The `useEffect` in `ScenarioComposer` depends only on `unknownStatusCount`; `captureToSentry` receives a constant-message `Error`, constant tags (`component`, `reason`), `extra` with the single integer key `unknown_status_count`, and `level: "warning"`. No key id, holding or venue value reaches the message, tags or extra. The capture goes through `src/lib/sentry-capture.ts`, which also scrubs. Pinned by the WR-04 test in `ScenarioComposer.test.tsx`, which asserts exact tags and extra and the absence of every fixture key id in the serialized capture. |
| `console.error` for the same anomaly | review WR-05 / round 2 IN-02 | Verified: the template interpolates only the count. Pinned by the IN-02 assertion that no fixture key id appears in the logged output. |
| Rendered copy and aria text | review WR-02, round 2 WR-05 | Verified: `aria-describedby` references only the element ids `scenario-aum-untrusted-note` / `scenario-aum-override-note`, whose text is the constant-noun clause above; the row marker text is the constant `UNKNOWN_KEY_STATUS_ROW_LABEL`. No role or live region is added. No internal cause (sync error, key label, venue) reaches rendered text. |
| `managerSideKeyIds` missing-field fallback | review round 2 WR-06 | Verified: returns `[]` when either payload field is `undefined`, so no eligible key is marked manager-side and nothing is hidden from `$Y` (over-disclose, never hide). Pinned by the WR-06 case in `live-holdings-summary.test.ts`. |
| No server / query / migration change (D-03) | CONTEXT D-03 / D-11 | Verified: the branch changes 10 non-planning files, all under `src/app/(dashboard)/allocations/` plus `src/lib/closed-sets.ts`; `queries.ts`, API routes, `supabase/`, `analytics-service/`, package manifests: 0. |

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-167.1-01 | T-167.1-05 | The `statusByKeyId` join uses two arrays the SSR already scoped to the caller (`getUserApiKeys` by `user_id`, holdings by `allocator_id`), so a foreign key id cannot appear; the phase adds no read (D-11). Premise re-verified at the audited sha. | Plan-time register (plan 01), under founder decision D-11 | 2026-09-24 |

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-09-24 | 14 in scope (plans 01–04; T-167.1-SC counted once) + 9 not yet applicable (plans 05–06) | 14 | 0 | gsd-security-auditor (ASVS L1, block_on high) |

**Limits of this audit (fail-loud).** Verification is static (grep and diff at the audited sha,
plus a range gitleaks scan). The phase test files were NOT executed by this audit: the worktree
has no `node_modules`, and none was installed or linked to avoid writing into it. The pins cited
above were confirmed present by reading them, not by running them. Plans 05 and 06 must be
audited when they execute: re-run `/gsd-secure-phase 167.1` then.

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed (for plans 01–04)
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-09-24 (plans 01–04); plans 05–06 pending execution
