---
phase: 167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro
verified: 2026-09-24T04:55:00Z
status: human_needed
score: 11/13 must-haves verified (2 PENDING-FOUNDER, not gaps)
verified_at_sha: 7e1614c141991f3c25dac1ebd0dd755d18bb622b
drift_subjects:
  - src/app/(dashboard)/allocations/lib/live-holdings-summary.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx
  - src/app/(dashboard)/allocations/components/HoldingsTable.tsx
  - src/app/(dashboard)/allocations/HoldingsTabPanel.tsx
  - src/lib/closed-sets.ts
  - src/lib/queries.ts
  - src/app/api/allocator/scenario/commit/route.ts
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "D-06 founder decision (plan 05, checkpoint:decision): choose (a) leave the book-mode exclusion of a revoked key's holdings and record it, (b) disclose it as 'excludes $Y from keys needing attention' (recommended), or (c) include and flag (reverses D-03, triggers replan)."
    expected: "The answer is recorded, dated, in 167.1-CONTEXT.md D-06 and the ROADMAP 167.1 entry; plan 05 then implements exactly that option."
    why_human: "PENDING-FOUNDER. Owner: founder. Reason: it changes what the composer's headline money figure MEANS (Phase 151 F2 reconciliation vs 'nothing silently disappears'); CONTEXT D-06 reserves it for the founder. Not a code gap."
  - test: "Plan 06 release: WINDOWS entry 66 closed with the D-06 answer, final byte-identity gate re-run, ONE chore(release) commit with VERSION/package.json byte-equal and a unified CHANGELOG entry carrying the known limits."
    expected: "WINDOWS 66 reads fixed; the D-03 gate is still empty after plan 05; the CHANGELOG states the D-06 outcome and never calls D-06 open."
    why_human: "PENDING-FOUNDER. Owner: founder (unblocks it by answering D-06), then the executor. Reason: plan 06 depends on plan 05 by design (D-14: the last plan releases). Measured at this sha: WINDOWS 66 is still 'todo/open' and the branch has not touched CHANGELOG.md, VERSION or package.json, as expected."
  - test: "D-18 re-open decision (review round 1 WR-04 and IN-06): should the marker also appear when untrusted holdings push the live total to <= 0 (UI-SPEC state 4, field blank + 'Required to size and commit.') and when a committed manual value equals the live total (state 6)?"
    expected: "Founder either confirms D-18 as locked (the two states stay recorded known limits) or re-opens it for a follow-up change."
    why_human: "PENDING-FOUNDER. Owner: founder. Reason: both findings conflict with locked D-18 and UI-SPEC U-04; the fixer correctly declined to work around a locked decision and recorded them in CONTEXT <known_limits>."
  - test: "At 320px width and at 200% zoom, open the Scenario composer in book mode with a key needing attention: check State A beside PORTFOLIO AUM and State B inside the override note (including the two-part 'and ... from keys with an unknown sync status' form)."
    expected: "The sentence wraps onto its own line(s) inside the flex-wrap row without clipping, overlap or truncation."
    why_human: "UI-SPEC backstop (M1). No test measures wrapping; plans 01 and 03 lift it as verification: backstop."
  - test: "At 320px width, open Holdings > Open Positions with a sign_in_failed position and read the footer qualifier under 'Total unrealized P&L (equity contribution)'."
    expected: "The colSpan=7 left-aligned qualifier stays readable at the visible start of the horizontally scrolling ResponsiveTable."
    why_human: "UI-SPEC backstop (M2), plan 02 truth with verification: backstop."
  - test: "Read the review-added copy in a browser: '(value unavailable for N holding(s))', '(P&L unavailable for N position(s))', 'keys with an unknown sync status', and the 'Sync status unknown' row marker on the Holdings tab."
    expected: "The wording reads as a plain disclosure in DESIGN.md's muted voice and a reader sent from the composer to the Holdings tab can find the named rows."
    why_human: "The fixer flagged R1 WR-03/WR-05 and R2 WR-05/IN-01 as 'requires human verification (copy)'. Typed-string tests pin the exact strings; they cannot judge whether the copy is clear."
---

# Phase 167.1: AUMTRUST Verification Report

**Phase Goal (as corrected 2026-09-23, D-01/D-13):** the holdings-derived dollar totals an allocator actually sees, the Scenario composer's PORTFOLIO AUM field and its override note and the Open Positions footer "Total unrealized P&L (equity contribution)", keep their value and say how much of it comes from keys needing attention (`isUntrustedKeySyncStatus`). The original goal sentence names a KPI-strip AUM cell that does not exist. It is kept in the ROADMAP as lineage.
**Verified:** 2026-09-24T04:55:00Z at `7e1614c141991f3c25dac1ebd0dd755d18bb622b` (branch `feat/167.1-aumtrust`)
**Status:** human_needed
**Re-verification:** No. This is the first verification.

Plans 01-04 are executed and verified below. Plan 05 (D-06) is a founder-owned `checkpoint:decision` that is still OPEN, and plan 06 (the release) runs after it. Both are recorded as PENDING-FOUNDER human items. They are not passed and they are not code gaps.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | D-01/D-13: the premise is corrected. The KPI strip has no AUM cell, `liveBaselineMetrics.aum` is rendered nowhere, and the ROADMAP keeps the old Goal as lineage with a dated correction | ✓ VERIFIED | `KpiStrip.tsx` mentions AUM only in Phase 64 PRESENT-01 comments. No `src/app` reader of `liveBaselineMetrics.aum` exists outside a composer comment. The ROADMAP `### Phase 167.1` entry carries both `⛔ CORRECTED 2026-09-23` notes |
| 2 | D-02/D-03 (pin 1): with a `sign_in_failed` holding summed, the PORTFOLIO AUM field keeps the full total, and State A "Includes $X from keys needing attention." renders beside it with the noun taken from `UNTRUSTED_KEY_SET_NOUN` | ✓ VERIFIED | `summarizeLiveHoldings` adds every summed holding's equity to `total` before classifying it. The State A span is gated by `showUntrustedMarker && fieldShowsLive`. The noun literal occurs 0 times in the hosts. **Independent neuter drill:** `out.total += equity` changed to skip untrusted holdings, and 14 AUMTRUST/pin tests went RED. The file was restored from a byte backup (cmp identical, git diff empty) |
| 3 | D-04/D-19: `$X` is a strict subset computed in the SAME pass as the total, over the same holdings and on the same equity basis | ✓ VERIFIED | One `for` loop in `summarizeLiveHoldings` accumulates `total`, `untrusted`, `unknownStatus` and `excludedUntrusted`, all on `holdingEquityContributionLocal`. The walk, the `holding:` prefix, the narrowing and the accumulation order match the pre-phase `liveHoldingsSum` memo line for line (compared against the merge base). The composer sets `liveHoldingsSum = liveHoldingsSummary.total` |
| 4 | D-05 / pin 5: the only membership test is the shared predicate, and in the degrade branch a `revoked` holding is summed and flagged | ✓ VERIFIED | `isUntrustedKeySyncStatus` is the only status test in the helper and in the footer pass. The one `=== "revoked"` grep hit is a pre-existing Phase 167 comment. The unit file's `vi.mock("@/lib/closed-sets")` widening double exists, and the drill-4 record in plan 01's SUMMARY is consistent with the code |
| 5 | D-07: every disclosure gates on a COUNT, not on the amount. A ≤ 0 derivative still discloses, and an all-trusted book shows no marker | ✓ VERIFIED | `showUntrustedMarker` reads `untrusted.count > 0 \|\| unknownStatus.count > 0`, and the footer row reads `untrusted.count > 0 \|\| unknownStatus.count > 0`. Component tests pin `Includes -$1,235 …` and the absence states |
| 6 | D-18 (supersedes D-08's placement): exactly one marker, and only while the live total is on screen. State A goes beside the field. State B nests inside `scenario-aum-override-note` | ✓ VERIFIED | `fieldShowsLive` requires `sanitizedManualAum === undefined` and `overrideNoteShowsLive` requires it defined, so the two cannot both hold. The override-note condition matches the pre-phase JSX byte for byte. With no untrusted holding the note text is unchanged (regression test). WR-04 and IN-06 are D-18 known limits held for the founder (human item 3) |
| 7 | D-16: the Open Positions footer total keeps summing untrusted rows and adds a muted qualifier `Includes <formatPnl> from keys needing attention.`, from one pass | ✓ VERIFIED | One `for` loop in `OpenPositionsTable` yields `totalUnrealized` and its parts. `open-positions-untrusted-note` is a `colSpan={7}` row with `text-xs text-text-muted`. The table is rendered in production by `HoldingsTabPanel`, which `AllocationsTabs` renders. **Independent neuter drill:** excluding untrusted rows from `totalUnrealized` turned 3 `[167.1]` tests RED. The file was restored (cmp identical, git diff empty) |
| 8 | D-17: `ExposureByClass` and `portfolio-exposure` are untouched | ✓ VERIFIED | `git diff --stat <merge-base> HEAD` on both paths prints nothing |
| 9 | D-03/D-10/D-11: no money number and no server path changed. `queries.ts`, the scenario commit route, `supabase/` and `analytics-service/` are byte-unchanged, and `liveBaselineMetrics` gained no field | ✓ VERIFIED | `git diff --stat origin/main -- src/lib/queries.ts supabase analytics-service src/app/api/allocator/scenario/commit/route.ts` prints 0 lines, and the same paths against the merge base also print 0 lines. ⚠️ This is interim: plan 06 re-measures after plan 05 |
| 10 | D-12: the closed-sets prose no longer says the predicate misses a headline AUM or that the flag is deferred, and it names both real consumers | ✓ VERIFIED | The block above `UNTRUSTED_KEY_SYNC_STATUSES` is headed "WHAT THIS PREDICATE REACHES, AND HOW". It names `summarizeLiveHoldings` → `scenario-aum-untrusted-note` and the footer pass → `open-positions-untrusted-note`, and keeps the SFH-M2 lineage. The only code added to `closed-sets.ts` is the two review-round nouns |
| 11 | Review rounds 1 and 2: every in-scope finding is closed, or held against a locked decision (unknown-status part, unavailable counts, `aria-describedby`, `managerSideKeyIds`, count-only Sentry capture) | ✓ VERIFIED | R1: 8 fixed, 3 held against D-18/U-05. R2: 12 of 12 fixed. The code for each item is present at HEAD (`managerSideKeyIds`, `holdingEquityIsReported`, `buildKeyTrustClause`, `aumInputDescribedBy`, `captureToSentry` with `extra: { unknown_status_count }` only). The full allocations suite is green (below) |
| 12 | D-06: the founder's answer is recorded and carried out (plan 05) | ⏳ PENDING-FOUNDER | The decision is open by design. The pins that make the answer visible exist: the unit test `D-06: a revoked key outside a non-empty contributing set is ABSENT …` and the component test `D-06 (component pin, decision OPEN) …`. No "excludes" copy renders from this phase (the composer's `excludes` hits are pre-existing leverage copy). Owner: founder. See human item 1 |
| 13 | D-14: WINDOWS 66 is closed and ONE release commit is written (plan 06) | ⏳ PENDING-FOUNDER | Blocked on plan 05 by design. At this sha WINDOWS row 66 still reads `todo … open`, and the branch has not touched CHANGELOG.md, VERSION or package.json. Owner: founder (by answering D-06), then the executor. See human item 2 |

**Score:** 11/13 truths verified. The 2 remaining are PENDING-FOUNDER. 0 are failed, and 0 are present but behaviour-unverified.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/app/(dashboard)/allocations/lib/live-holdings-summary.ts` | single-pass summary, clause builder, manager-side helper | ✓ VERIFIED | Substantive (≈300 lines). Imported by `ScenarioComposer.tsx` and `OpenPositionsTable.tsx` |
| `src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts` | unit pins 1, 2, 4, 5, degrade and D-06 | ✓ VERIFIED | Green. The drills recorded in the SUMMARYs agree with the code, and one drill was reproduced independently |
| `src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` | State A, State B, gates, `aria-describedby` | ✓ VERIFIED | Wired into the production `AllocationsTabs` |
| `src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx` | footer qualifier | ✓ VERIFIED | Wired through `HoldingsTabPanel` |
| `src/app/(dashboard)/allocations/components/HoldingsTable.tsx` / `HoldingsTabPanel.tsx` | `source_key_missing` row marker (R2 WR-05) | ✓ VERIFIED | The panel sets the flag from `keyStatusById.has`, and both tables render `UNKNOWN_KEY_STATUS_ROW_LABEL` |
| `src/lib/closed-sets.ts` | corrected prose plus two nouns | ✓ VERIFIED | See truth 10 |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `payload.apiKeys[].sync_status` | `summarizeLiveHoldings` | `statusByKeyId` memo | ✓ WIRED |
| `summarizeLiveHoldings().total` | PORTFOLIO AUM field, `scenarioAum`, override note | `liveHoldingsSum = liveHoldingsSummary.total` | ✓ WIRED |
| `summarizeLiveHoldings().untrusted/unknownStatus` | `scenario-aum-untrusted-note` (State A and State B) | `buildUntrustedAumClause` → `buildKeyTrustClause` | ✓ WIRED |
| `OpenPositionRow.source_key_sync_status` / `source_key_missing` | `open-positions-untrusted-note` | footer pass → `buildKeyTrustClause` with `formatPnl` | ✓ WIRED |
| `summarizeLiveHoldings().excludedUntrusted` | (nothing yet) | plan 05 under D-06 (b) | ⏳ deliberately unrendered pending D-06 |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Real data | Status |
|---|---|---|---|---|
| State A / State B marker | `liveHoldingsSummary.untrusted` | SSR payload `holdingsSummary` + `apiKeys` (no new read) | yes | ✓ FLOWING |
| Footer qualifier | `untrusted` / `unknownStatus` parts | `openPositionRows` built in `HoldingsTabPanel` from the payload | yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Typecheck | `npx tsc --noEmit` | exit 0, 0 `error TS` | ✓ PASS |
| Allocations and closed-sets suites | `npx vitest run "src/app/(dashboard)/allocations" src/lib/closed-sets --reporter=dot` | 135 files, 2079/2079 passed, 0 skipped | ✓ PASS |
| D-03 guard bites (composer) | neuter `out.total += equity` → run `-t "AUMTRUST\|pin 1"` → restore | 14 failed while neutered. Restore cmp identical | ✓ PASS |
| D-03 guard bites (footer) | neuter `totalUnrealized += pnl` → run `-t "167.1"` → restore | 3 failed while neutered. Restore cmp identical | ✓ PASS |
| D-03 static | `git diff --stat origin/main -- src/lib/queries.ts supabase analytics-service` | empty | ✓ PASS |

### Probe Execution

Step 7c: SKIPPED. This is a UI disclosure phase, and no plan declares or implies a probe.

### Requirements Coverage

The phase has no REQUIREMENTS.md IDs (the ROADMAP says "Requirements: TBD"). The CONTEXT decisions are the contract:

| Decision | Plan | Status |
|---|---|---|
| D-01, D-02, D-04, D-05, D-07, D-09, D-19 | 01 | ✓ SATISFIED |
| D-16, D-17 | 02 | ✓ SATISFIED |
| D-08 / D-18, D-15 (pins 1-6) | 01, 03 | ✓ SATISFIED (D-18 re-open is with the founder) |
| D-03, D-10, D-11, D-12, D-13 | 04 | ✓ SATISFIED (interim D-03) |
| D-06, D-20 | 05 | ⏳ PENDING-FOUNDER |
| D-14 | 06 | ⏳ PENDING-FOUNDER (blocked on 05) |

No orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| (added source lines on the branch) | none | TBD / FIXME / XXX / TODO / HACK / PLACEHOLDER | none | 0 hits |
| `live-holdings-summary.ts` | `excludedUntrusted` | computed but not rendered | ℹ️ Info | Deliberate. It measures the D-06 exclusion, and plan 05 decides whether it renders |

### Known limits (from CONTEXT `<known_limits>`, carried to plan 06's release notes)

- `holdingScopeKey` omits `api_key_id`, so the latest-asof collapse can merge two accounts' positions before any total is computed. The phase's guarantees apply to the rows that survive that collapse. This is routed to the founder as its own proposed phase.
- Whether a soft-disconnected key counts as "needing attention" is the founder's call. The predicate is not widened.
- WR-04 (state 4) and IN-06 (state 6) are held by D-18, and IN-05 (the sign conventions differ) is kept by UI-SPEC U-05.

### Human Verification Required

1. **D-06 decision (PENDING-FOUNDER, owner: founder).** Choose (a), (b) (recommended) or (c) for the silent book-mode absence of a revoked key's holdings. Plan 05 then implements exactly that option.
2. **Plan 06 release (PENDING-FOUNDER, owner: founder, then the executor).** Close WINDOWS 66, re-run the D-03 gate, and write the single `chore(release)` commit carrying the D-06 outcome.
3. **D-18 re-open (PENDING-FOUNDER, owner: founder).** Decide whether WR-04 (live total ≤ 0) and IN-06 (manual value equals the live total) should show the marker, or stay recorded limits.
4. **M1 wrap backstop.** Check State A and State B, including the two-part clause, at 320px and at 200% zoom.
5. **M2 wrap backstop.** Check the Open Positions qualifier at 320px inside `ResponsiveTable`.
6. **Copy read-through.** Read the review-added "unavailable" and "unknown sync status" wording and the row marker.

### Gaps Summary

There are no code gaps. Everything plans 01-04 promised exists, is wired into the production allocations page, is fed by data already in the SSR payload, and is pinned by tests that go RED when their guard is removed. I reproduced two of those drills independently. The money figures are byte-unchanged on every server path. The phase is not complete: plans 05 and 06 are gated on a founder decision by design, so the status is `human_needed` and not `passed`.

---

_Verified: 2026-09-24T04:55:00Z_
_Verifier: Claude (gsd-verifier)_
