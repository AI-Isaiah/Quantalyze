---
phase: 167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro
verified: 2026-09-24T05:56:00Z
status: human_needed
score: 14/15 must-haves verified (1 is plan 06, the release step, not yet run by design; not a gap)
verified_at_sha: ccdae1705caa5578f540eb53c4fbf16ce3dac4be
drift_subjects:
  - src/app/(dashboard)/allocations/lib/live-holdings-summary.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx
  - src/app/(dashboard)/allocations/components/HoldingsTable.tsx
  - src/app/(dashboard)/allocations/HoldingsTabPanel.tsx
  - src/lib/closed-sets.ts
  - src/lib/queries.ts
  - src/app/api/allocator/scenario/commit/route.ts
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 11/13 (2 PENDING-FOUNDER)
  previous_verified_at_sha: 7e1614c141991f3c25dac1ebd0dd755d18bb622b
  gaps_closed:
    - "D-06: the founder answered (b) on 2026-09-24, recorded in CONTEXT and ROADMAP, and plan 05 implemented it"
    - "D-18 re-open: the founder re-opened it on 2026-09-24; state 6 now shows State A and state 4 shows State C"
    - "Review round 3 (plan 05): 6 of 6 findings fixed, including the WR-01 widening (excludedUnknownStatus)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "At 320px width and at 200% zoom, open the Scenario composer in book mode with a key needing attention. Check State A beside PORTFOLIO AUM, State B inside the override note, and State C inside the 'Required to size and commit.' hint. Include the longest forms: 'Includes $X and excludes $Y from keys needing attention.' and the two-noun forms ('..., and excludes $Z from keys with an unknown sync status')."
    expected: "Each sentence wraps onto its own line(s) inside the flex-wrap row without clipping, overlap or truncation. In State C the hint and its clause read as one sentence."
    why_human: "UI-SPEC long-text backstop (M1). jsdom does not measure wrapping, and plan 05 plus the round-3 fix made the strings longer (plan 05 SUMMARY coverage D4, human_judgment: true)."
  - test: "At 320px width, open Holdings > Open Positions with a sign_in_failed position and read the footer qualifier under 'Total unrealized P&L (equity contribution)'."
    expected: "The colSpan=7 left-aligned qualifier stays readable at the visible start of the horizontally scrolling ResponsiveTable."
    why_human: "UI-SPEC backstop (M2), plan 02 truth with verification: backstop."
  - test: "Read the new strings in a browser: 'Excludes $Y from keys needing attention.', 'Includes $X and excludes $Y from keys needing attention.', State C 'Required to size and commit. The live-holdings total is -$X, which ...', 'excludes $Z from keys with an unknown sync status', plus the earlier review-added copy ('(value unavailable for N holding(s))', '(P&L unavailable for N position(s))', 'keys with an unknown sync status', and the 'Sync status unknown' row marker on the Holdings tab)."
    expected: "The wording reads as a plain disclosure in DESIGN.md's muted voice, 'includes' and 'excludes' are not confusable at a glance, and a reader sent from the composer to the Holdings tab can find the named rows."
    why_human: "Typed-string tests pin the exact strings. They cannot judge whether the copy is clear. REVIEW-FIX-R3 marks WR-01 'requires human verification (a copy and logic change on a money disclosure)'."
---

# Phase 167.1: AUMTRUST Verification Report

**Phase Goal (as corrected 2026-09-23, D-01/D-13):** the holdings-derived dollar totals an allocator actually sees keep their value and say how much of it comes from keys needing attention (`isUntrustedKeySyncStatus`). Those totals are the Scenario composer's PORTFOLIO AUM field and its override note, and the Open Positions footer "Total unrealized P&L (equity contribution)". The founder's 2026-09-24 answers extend this: the composer also says what the modelled-book narrowing leaves out (D-06 (b)), and the marker shows whenever the figure on screen includes untrusted dollars (D-18 re-opened). The original goal sentence names a KPI-strip AUM cell that does not exist. It is kept in the ROADMAP as lineage.
**Verified:** 2026-09-24T05:56:00Z at `ccdae1705caa5578f540eb53c4fbf16ce3dac4be` (branch `feat/167.1-aumtrust`, origin/main merged)
**Status:** human_needed
**Re-verification:** Yes. The previous report (`human_needed`, 11/13 at `7e1614c1`) was issued before plan 05 ran. Its two PENDING-FOUNDER items (D-06 and the D-18 re-open) are now answered, implemented and verified below. Its reasoning for truths 1-11 is kept where it still holds, and each was re-checked at this sha.

**Remaining release step (not a gap).** Plan 06 (WINDOWS 66 closed, the final D-03 re-measure, and ONE `chore(release)` commit with VERSION/package.json byte-equal and a unified CHANGELOG entry) has not run. It follows this verification by design (D-14: the last plan releases). At this sha WINDOWS row 66 still reads `todo … open`, and `CHANGELOG.md`, `VERSION` and `package.json` show no diff against the merge base, which is expected. Plan 05's SUMMARY carries the `## Release-notes handoff (for plan 06)` section that plan 06 reads. ⚠️ That handoff predates the round-3 fixes. It still lists "a dropped holding whose key is missing from the key list is named by no part" under Notes and Known limits. REVIEW-FIX-R3 WR-01 closed that, so plan 06 must not carry that sentence into the CHANGELOG as a current limit. It should add the `excludes $Z from keys with an unknown sync status` string to Added instead.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | D-01/D-13: the premise is corrected. The KPI strip has no AUM cell, `liveBaselineMetrics.aum` is rendered nowhere, and the ROADMAP keeps the old Goal as lineage with a dated correction | ✓ VERIFIED | Unchanged since the previous report. None of `KpiStrip`, `queries.ts` or the ROADMAP correction notes moved in plan 05 or round 3. The ROADMAP `### Phase 167.1` entry still carries both `⛔ CORRECTED 2026-09-23` notes |
| 2 | D-02/D-03 (pin 1): with a `sign_in_failed` holding summed, the PORTFOLIO AUM field keeps the full total, and State A renders beside it. The noun comes from `UNTRUSTED_KEY_SET_NOUN` | ✓ VERIFIED | In `summarizeLiveHoldings`, `out.total += equity` runs for every summed holding before any part is classified. The composer has 0 `"keys needing attention"` string literals: all 5 hits are comments, and the text comes from `buildKeyTrustClause` through the shared nouns |
| 3 | D-04/D-19: every disclosed part comes from the SAME single pass as the total, over the same holdings and on the same equity basis | ✓ VERIFIED | `summarizeLiveHoldings` has one `for` loop. It fills `total`, `untrusted`, `unknownStatus`, `excludedUntrusted` and `excludedUnknownStatus`, all through `addTo` with `holdingEquityContributionLocal`. I re-compared the total path against the merge-base `liveHoldingsSum` memo: the walk, the `holding:` prefix, the narrowing `continue` and the accumulation are equivalent. The composer sets `liveHoldingsSum = liveHoldingsSummary.total` |
| 4 | D-05 / pin 5: the only membership test is the shared predicate, and in the degrade branch a `revoked` holding is summed and flagged | ✓ VERIFIED | `isUntrustedKeySyncStatus` is the only status test in the helper, and it is applied before the branch split. The WR-05 `.has` rule (absent means unknown, never trusted) now applies on both sides of the narrowing |
| 5 | D-07: every disclosure gates on a COUNT, never on the amount | ✓ VERIFIED | `showUntrustedMarker` reads `.count > 0` on all four parts. `buildKeyTrustClause` selects each part on `.count > 0`. **Independent drill 2** (below): with the gate switched to `.amount > 0` for the two excluded parts, the two `$0` count-gate tests went RED |
| 6 | D-18 as re-opened 2026-09-24: exactly one marker, and it shows whenever the figure on screen includes untrusted dollars. State A appears beside the field (state 3, and state 6 with manual equal to live). State B nests in the override note. State C nests in the blank-field hint (state 4). State 7 stays absent | ✓ VERIFIED | `fieldShowsLive`, `overrideNoteShowsLive` and `fieldBlankHintShows` cannot hold together, so at most one of the three `scenario-aum-untrusted-note` sites renders. Each state asserts `getAllByTestId(...).toHaveLength(1)`. The override-note and hint conditions match the merge-base JSX, so the behaviour without a marker is byte-identical (regression tests `AUMTRUST state 4 regression` and the override-note regression). **Independent drill 4:** removing the state 6 disjunct from `fieldShowsLive` turned `AUMTRUST state 6 (D-18 REOPENED, review IN-06)` RED. State 6 requires exact float equality. REVIEW-R3 IN-03 recorded this, and it is documented beside the flag and in UI-SPEC § 1 |
| 7 | D-16: the Open Positions footer total keeps summing untrusted rows and adds the muted qualifier from one pass | ✓ VERIFIED | Plan 05 did not change `OpenPositionsTable`. It calls `buildKeyTrustClause` with three arguments, so both excluded parts are `undefined` and the function returns the pre-plan `includes …` expression. The footer drill from the previous report still stands, and the full suite is green |
| 8 | D-17: `ExposureByClass` and `portfolio-exposure` are untouched | ✓ VERIFIED | `git diff --stat <merge-base> HEAD` on `widgets/positions/ExposureByClass.tsx` and `src/lib/portfolio-exposure.ts` prints 0 lines |
| 9 | D-03/D-10/D-11: no money number and no server path changed | ✓ VERIFIED | `git diff --stat $(git merge-base HEAD origin/main) -- src/lib/queries.ts supabase analytics-service src/app/api/allocator/scenario/commit/route.ts` prints 0 lines, and the same paths against `origin/main` also print 0 lines. The two excluded parts never enter `total`. **Independent drill 1** (below): adding the dropped holding's equity to `total` in the narrowing arm turned 22 tests RED |
| 10 | D-12: the closed-sets prose is corrected and names both consumers | ✓ VERIFIED | Unchanged since the previous report. `closed-sets.ts` was not touched after plan 04 apart from the two review-round nouns, which the clause builder uses |
| 11 | Review rounds 1-3: every in-scope finding is closed. None is held any longer against D-18, because the founder re-opened it | ✓ VERIFIED | R1: 8 fixed. Its 3 D-18 holds (WR-04, IN-06, and the U-05 sign-convention note) are now resolved: WR-04 and IN-06 by the re-open, and IN-05 is kept by UI-SPEC U-05. R2: 12 of 12 fixed. R3: 6 of 6 fixed. The WR-01 code (`excludedUnknownStatus`), the WR-02 oracle test, the IN-02 rename to `fieldBlankHintShows`, the IN-01 comments and the IN-03/IN-04 dated UI-SPEC rows are all present at HEAD |
| 12 | D-06: the founder's answer is recorded and carried out. Under (b) the ONE marker says "excludes $Y from keys needing attention" per UI-SPEC § 3 (State A and State B, excludes-only and both), gated on the excluded count, with the total unchanged | ✓ VERIFIED | CONTEXT D-06 carries `⭐ RESOLVED 2026-09-24 (founder): option (b)`, and the ROADMAP `### Phase 167.1` entry carries the dated founder-decisions sentence and the plan 05 checkbox `[x]`. `buildUntrustedAumClause` passes `summary.excludedUntrusted`. The four M3 tests (`D-06 (b) M3 State A/B, excludes only/both`) and the `$0` count-gate test pass. Drills 1 and 2 bite on this code |
| 13 | D-20: `$Y` excludes manager-side keys the payload names (`eligibleApiKeyIds` minus `allocatorEligibleApiKeyIds`, via `managerSideKeyIds`). A missing field yields no manager-side key, so the error direction is over-disclosure, never hiding. The residual is stated | ✓ VERIFIED | `managerSideKeyIds` returns `[]` when either field is `undefined`. `summarizeLiveHoldings` checks `!managerSide.has(...)` before either excluded bucket. The residual is stated in the `excludedUntrusted` doc (by predicate, per R2 WR-03) and in the plan 05 release-notes handoff |
| 14 | Review round 3 WR-01: a holding the narrowing drops whose key is missing from the key list is disclosed ("excludes $Z from keys with an unknown sync status") and never silently vanishes. The `apiKeys` path fails open on the excludes side | ✓ VERIFIED | The narrowing arm's `else if (!args.statusByKeyId.has(h.api_key_id))` adds to `excludedUnknownStatus`. The composer passes it as the fifth argument and counts it in the gate. **Independent drill 3:** removing that `addTo` turned 5 tests RED (2 unit, 3 component) |
| 15 | D-14: WINDOWS 66 is closed and ONE release commit is written (plan 06) | ⏳ PENDING (release step, by design) | Not run yet. Plan 06 follows this verification (D-14). At this sha WINDOWS 66 reads `todo … open`, and CHANGELOG/VERSION/package.json have 0 diff lines against the merge base. This is recorded as the remaining release step, not a gap. See the note on the handoff above |

**Score:** 14/15 truths verified. The remaining one is plan 06, the release, which has not run by design. 0 failed, 0 present but behaviour-unverified. Every behaviour-dependent truth (2, 3, 5, 6, 9, 12, 14) is backed by a passing test that I observed going RED under a neuter drill, either my own or, for truth 7, a drill reproduced in the previous report.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/app/(dashboard)/allocations/lib/live-holdings-summary.ts` | single-pass summary with 5 fields, `managerSideKeyIds`, `buildKeyTrustClause` with the optional excluded parts | ✓ VERIFIED | 371 lines. Imported by `ScenarioComposer.tsx` and `OpenPositionsTable.tsx` |
| `src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts` | unit pins, the D-06 (b) builder cases, WR-01 and WR-02 | ✓ VERIFIED | Green. Drills 1 and 3 turned it RED |
| `src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` | States A/B/C, the widened count gate, `aria-describedby` from the same flags | ✓ VERIFIED | Wired into the production `AllocationsTabs` |
| `src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx` | footer qualifier, byte-identical after plan 05 | ✓ VERIFIED | Wired through `HoldingsTabPanel` |
| `src/app/(dashboard)/allocations/components/HoldingsTable.tsx` / `HoldingsTabPanel.tsx` | `source_key_missing` row marker (R2 WR-05) | ✓ VERIFIED | Unchanged since the previous report |
| `src/lib/closed-sets.ts` | corrected prose and the two nouns | ✓ VERIFIED | See truth 10 |
| `167.1-CONTEXT.md` / ROADMAP `### Phase 167.1` | dated D-06 RESOLVED, D-18 REOPENED and D-06 (b) WIDENED | ✓ VERIFIED | All three lines are present in CONTEXT. The ROADMAP carries the founder-decisions sentence |
| `167.1-05-SUMMARY.md` `## Release-notes handoff (for plan 06)` | plan 06 input | ✓ VERIFIED (stale on one line) | Present and names option (b). See the note under "Remaining release step" about the WR-01 sentence |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `payload.apiKeys[].sync_status` | `summarizeLiveHoldings` | `statusByKeyId` memo | ✓ WIRED |
| `payload.eligibleApiKeyIds` / `allocatorEligibleApiKeyIds` | `excludedUntrusted` / `excludedUnknownStatus` | `managerSideKeyIds` → `managerSideApiKeyIds` | ✓ WIRED |
| `summarizeLiveHoldings().total` | PORTFOLIO AUM field, `scenarioAum`, override note, State C hint | `liveHoldingsSum = liveHoldingsSummary.total` | ✓ WIRED |
| `summarizeLiveHoldings()` (all four parts) | `scenario-aum-untrusted-note` in States A, B and C | `buildUntrustedAumClause` → `buildKeyTrustClause` | ✓ WIRED |
| `OpenPositionRow.source_key_sync_status` / `source_key_missing` | `open-positions-untrusted-note` | footer pass → `buildKeyTrustClause` (3 arguments) | ✓ WIRED |
| `167.1-05-SUMMARY.md` handoff | plan 06's CHANGELOG entry | plan 06 Task 2 | ⏳ plan 06 not run (by design) |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Real data | Status |
|---|---|---|---|---|
| State A / B / C marker | `liveHoldingsSummary` (four parts) | SSR payload `holdingsSummary`, `apiKeys`, `contributingApiKeyIds`, `eligibleApiKeyIds`, `allocatorEligibleApiKeyIds`. No new read | yes | ✓ FLOWING |
| Footer qualifier | `untrusted` / `unknownStatus` parts | `openPositionRows` built in `HoldingsTabPanel` from the payload | yes | ✓ FLOWING |

### Behavioral Spot-Checks

All checks ran in the worktree at `ccdae1705`, with `node_modules` symlinked from the main checkout and removed afterwards.

| Behavior | Command | Result | Status |
|---|---|---|---|
| Typecheck | `npx tsc --noEmit` | exit 0 | ✓ PASS |
| Allocations and closed-sets suites | `npx vitest run 'src/app/(dashboard)/allocations' src/lib/closed-sets --reporter=dot` | 135 files, 2101/2101 passed, 0 skipped, exit 0 | ✓ PASS |
| D-03 static | `git diff --stat $(git merge-base HEAD origin/main) -- src/lib/queries.ts supabase analytics-service src/app/api/allocator/scenario/commit/route.ts` | 0 lines (also 0 lines against `origin/main`) | ✓ PASS |
| Release files untouched before plan 06 | `git diff --stat <merge-base> HEAD -- CHANGELOG.md VERSION package.json` | 0 lines | ✓ PASS (expected) |
| **Drill 1: excluded dollars never enter a total.** Neuter: `out.total += equity` inserted before `continue` in the narrowing arm | unit + composer files | 22 failed (7 unit, 15 component), e.g. `expected '460000' to be '40000'` | ✓ RED, then restored (`cp` backup, `cmp` identical, `git status` clean) |
| **Drill 2: the marker is gated on the count.** Neuter: `excludedUntrusted.count > 0` / `excludedUnknownStatus.count > 0` → `.amount > 0` in `showUntrustedMarker` | composer file | 2 failed: `D-06 (b) count gate …` and `review round 3 WR-01 count gate …` (`Unable to find … scenario-aum-untrusted-note`) | ✓ RED, then restored (`cmp` identical) |
| **Drill 3: WR-01 widening.** Neuter: remove `addTo(out.excludedUnknownStatus, h, equity)` | unit + composer files | 5 failed (2 unit, 3 component) | ✓ RED, then restored (`cmp` identical) |
| **Drill 4: D-18 state 6.** Neuter: `fieldShowsLive` back to `sanitizedManualAum === undefined` only | composer, `-t "AUMTRUST\|D-06\|WR-01"` | 1 failed: `AUMTRUST state 6 (D-18 REOPENED, review IN-06)` | ✓ RED, then restored (`cmp` identical) |

After all four restores, `git status --short -- src` printed 0 lines.

### Probe Execution

Step 7c: SKIPPED. This is a UI disclosure phase, and no plan declares or implies a probe.

### Requirements Coverage

The phase has no REQUIREMENTS.md IDs (the ROADMAP says "Requirements: TBD"). The CONTEXT decisions are the contract:

| Decision | Plan | Status |
|---|---|---|
| D-01, D-02, D-04, D-05, D-07, D-09, D-19 | 01 | ✓ SATISFIED |
| D-16, D-17 | 02 | ✓ SATISFIED |
| D-08 / D-18 (re-opened 2026-09-24), D-15 (pins 1-6) | 01, 03, 05 | ✓ SATISFIED |
| D-03, D-10, D-11, D-12, D-13 | 04 (re-measured here) | ✓ SATISFIED |
| D-06, D-20 | 05 (+ R3 WR-01) | ✓ SATISFIED |
| D-14 | 06 | ⏳ release step, not yet run (by design) |

No orphaned requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| (lines added under `src/` on the branch) | none | TBD / FIXME / XXX / TODO / HACK / PLACEHOLDER | none | 0 hits |
| `167.1-05-SUMMARY.md` release-notes handoff | Notes / Known limits | a limit that REVIEW-FIX-R3 WR-01 has since closed | ℹ️ Info | Plan 06 must not print it as a current limit (see "Remaining release step") |

### Known limits (from CONTEXT `<known_limits>` and the plan 05 SUMMARY, for plan 06's release notes)

- `holdingScopeKey` omits `api_key_id`, so the latest-asof collapse can merge two accounts' positions before any total is computed. The phase's guarantees apply to the rows that survive that collapse. This is routed to its own phase (167.1.1 HOLDINGKEYSCOPE in the ROADMAP).
- Whether a soft-disconnected key counts as "needing attention" is the founder's call. The predicate is not widened.
- D-20 residual: a manager-side key outside `eligibleApiKeyIds` cannot be told apart from the payload, so its untrusted holdings may be counted in `$Y`. It over-discloses and never hides.
- State 7 (a manual value with a live total ≤ 0) shows no marker by design, because nothing on screen contains the live total there.
- State 6 is exact float equality (R3 IN-03). Typing the rounded live figure lands in State B.
- IN-05 (the sign conventions differ between the two surfaces) is kept by UI-SPEC U-05.
- The previous report listed WR-04 and IN-06 as D-18 limits. Both were removed by the D-18 re-open and are no longer limits.

### Human Verification Required

1. **M1 wrap backstop.** At 320px and at 200% zoom, check State A, State B and State C, including the longest two-noun forms.
2. **M2 wrap backstop.** Check the Open Positions qualifier at 320px inside `ResponsiveTable`.
3. **Copy read-through.** Read the plan-05 and round-3 strings ("Excludes …", "Includes … and excludes …", State C, "… from keys with an unknown sync status") and the earlier review-added "unavailable" wording and row marker.

The previous report's three PENDING-FOUNDER items are closed: D-06 is answered, the D-18 re-open is answered and implemented, and plan 06 is now just the release step.

### Gaps Summary

There are no code gaps. The founder's two answers are recorded in CONTEXT and the ROADMAP and are implemented exactly: D-06 (b) says what the modelled-book narrowing excludes, and D-18 was re-opened. The round-3 review is fully fixed, and the WR-01 widening means a dropped holding whose key is missing from the key list is now disclosed instead of vanishing. Every total is byte-unchanged on every server path. Four of my own neuter drills went RED and were restored byte-identical: excluded dollars leaking into the total, the amount gate, the WR-01 bucket, and the D-18 state 6 disjunct. The status is `human_needed` because three visual and copy checks cannot be done in jsdom. Plan 06, the release, is the remaining step by design. Its handoff needs one sentence updated for R3 WR-01.

---

_Verified: 2026-09-24T05:56:00Z_
_Verifier: Claude (gsd-verifier)_
