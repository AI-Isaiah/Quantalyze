---
phase: 167.1-aumtrust
plan: 01
subsystem: ui
tags: [react, vitest, allocations, scenario-composer, aum, closed-sets]

requires:
  - phase: 167-credtrust
    provides: "isUntrustedKeySyncStatus, UNTRUSTED_KEY_SET_NOUN, TRUSTED_OR_NEUTRAL_KEY_SYNC_STATUSES (src/lib/closed-sets.ts)"
  - phase: 151
    provides: "the composer's liveHoldingsSum loop, the F2 contributing-set narrowing, the E1 equity basis"
provides:
  - "summarizeLiveHoldings: the live-holdings total, its untrusted subset and excludedUntrusted from ONE pass"
  - "LiveHoldingsSummary interface"
  - "holdingEquityContributionLocal moved out of the composer and exported (body unchanged)"
  - "statusByKeyId + liveHoldingsSummary memos; liveHoldingsSum = summary.total"
  - "fieldShowsLive / overrideNoteShowsLive / showUntrustedMarker gates"
  - "buildUntrustedAumClause, the one clause builder (plan 03 State B and plan 05 read it)"
  - "State A scenario-aum-untrusted-note beside the PORTFOLIO AUM field"
  - "describe 'ScenarioComposer — AUMTRUST (Phase 167.1)' with the tracer case"
affects: [167.1-03, 167.1-05, 167.1-06]

actuals:
  tokens: 7850     # chars/4 over the added lines of the realized diff
  tasks: 2
  commits: 2
plan_head_before: 7939c3acf40459c6e8d45429ea984342a59e5298

tech-stack:
  added: []
  patterns:
    - "Single-pass classifier: a displayed total and its disclosed subset come from one helper, never two loops"
    - "Partial vi.mock double that DELEGATES to the real predicate unless a vi.hoisted toggle widens it"

key-files:
  created:
    - "src/app/(dashboard)/allocations/lib/live-holdings-summary.ts"
    - "src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts"
  modified:
    - "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
    - "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"

key-decisions:
  - "167.1-01: capitalizeFirst is a small module-level helper next to buildUntrustedAumClause. No capitalize utility exists in src/lib or src/app, and the builder keeps its lower-case clause so State B can nest it unchanged."
  - "167.1-01: the composer test imports the real buildHoldingRef by merging it into the existing type-only holding-outcome-adapter import, so no second import line is added for the same module."

patterns-established:
  - "One clause builder per disclosed fact, with sentence-casing applied at the standalone site only"

requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-07, D-09, D-10, D-11, D-15, D-18, D-19]

coverage:
  - id: D1
    description: "A sign_in_failed holding stays in the PORTFOLIO AUM field's total and a muted State A sentence names its dollars"
    requirement: "D-02"
    verification:
      - kind: automated_ui
        ref: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx#AUMTRUST tracer (D-02/D-03, D-15 pin 1)"
        status: pass
    human_judgment: false
  - id: D2
    description: "summarizeLiveHoldings returns the total byte-identical to the old loop, plus the untrusted subset and excludedUntrusted from one pass"
    requirement: "D-04"
    verification:
      - kind: unit
        ref: "src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts#pin 1, pin 2 (x2), pin 4, pin 5, degrade branch, D-06"
        status: pass
      - kind: other
        ref: "five neuter -> RED -> restore drills on live-holdings-summary.ts, each restored and cmp-verified"
        status: pass
    human_judgment: false
  - id: D3
    description: "No money number changed: composer suite unchanged and green, queries.ts, the commit route, supabase/ and analytics-service/ untouched"
    requirement: "D-03"
    verification:
      - kind: unit
        ref: "npx vitest run ScenarioComposer.test.tsx (336/336, F2 and AUM-01 blocks unmodified)"
        status: pass
      - kind: other
        ref: "git diff --stat $(git merge-base HEAD origin/main) -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/ (empty)"
        status: pass
    human_judgment: false
  - id: D4
    description: "The State A sentence reads well beside the field and wraps cleanly in the flex-wrap row at narrow widths"
    verification: []
    human_judgment: true
    rationale: "Visual placement and wrapping are asserted by no test; the UI-SPEC's overflow claim is structural (one flex item, no width class) and needs a human eye."

duration: 7min
completed: 2026-09-24
status: complete
---

# Phase 167.1 Plan 01: AUMTRUST State A marker and single-pass summary Summary

**The composer's PORTFOLIO AUM field keeps a sign_in_failed key's holdings in its total and now says "Includes $X from keys needing attention." beside it, with the total and X computed in one pass by a new `summarizeLiveHoldings` helper.**

## Performance

- **Duration:** about 7 min
- **Started:** 2026-09-24T01:34:53Z
- **Completed:** 2026-09-24T01:41:59Z
- **Tasks:** 2
- **Files modified:** 4 (2 created, 2 modified)

## Accomplishments

- `summarizeLiveHoldings` in `allocations/lib/live-holdings-summary.ts` owns the loop that `liveHoldingsSum` used to own. The walk order, the `continue` chain and the accumulation order into `total` are unchanged, so the figure is byte-identical (D-03). Each holding's equity and untrusted flag are computed once, and `untrusted` and `excludedUntrusted` accumulate in the same iteration (D-04 / D-19). The only membership test is `isUntrustedKeySyncStatus`.
- `holdingEquityContributionLocal` moved into that module unchanged and is exported. `equityByApiKeyId` and the summary now share one definition. `ScenarioComparePanel.tsx`'s inline copy was left alone, as the plan says.
- Composer: new `statusByKeyId` and `liveHoldingsSummary` memos, with `const liveHoldingsSum = liveHoldingsSummary.total`, so `scenarioAum`, the seeding effect, `committedAumText` and the override note read the same value as before. `fieldShowsLive` and `overrideNoteShowsLive` are computed once above the return, and the override note's condition now reads `overrideNoteShowsLive`. `showUntrustedMarker` is gated on the untrusted COUNT (D-07 / D-18).
- State A: one `scenario-aum-untrusted-note` span, `text-xs text-text-muted`, with no role, aria attribute, glyph or margin. It sits right after the input and before `scenario-aum-modelled-note`. Its text comes from the single clause builder `buildUntrustedAumClause`, which uses `UNTRUSTED_KEY_SET_NOUN` and `formatUsd`. The noun literal appears 0 times in the composer.
- Tests: the AUMTRUST describe block with the tracer case, and a new unit file with 9 tests covering D-15 pins 1, 2, 4 and 5, the degrade branch and the D-06 pin. Every pin was seen going RED.

## Task Commits

1. **Task 1: tracer, the field keeps the sign_in_failed holding and says so (State A)**: `7eff1d43a` (feat). TDD: the tracer test was written first and went RED with `TestingLibraryElementError: Unable to find an element by: [data-testid="scenario-aum-untrusted-note"]`. The field-value assertions before it had already passed, so it failed for the right reason. It was GREEN after the wiring, and the full composer suite passed 336/336.
2. **Task 2: unit pins on summarizeLiveHoldings with the shared-predicate double**: `197bd3bf0` (test). The file was written after the helper existed, so its RED evidence is the five neuter drills below, not a failing first run.

## Neuter drills (Task 2): neuter, RED, restore

Each drill: `cp` byte backup of `live-holdings-summary.ts`, one scripted edit, a `cmp` confirming the file changed, a vitest run, a restore from the backup, and a `cmp` confirming the restored file is byte-identical. The final `git diff` of the helper was empty.

| # | Neuter | Target pin | Pasted RED line | Restore |
|---|---|---|---|---|
| 1 | `if (untrusted) continue;` before `out.total += equity;` | pin 1 | `→ expected 480000 to be 492345 // Object.is equality` (5 failed: pin 1, both pin 2 cases, pin 5, degrade) | cmp IDENTICAL |
| 2 | untrusted accumulator adds `h.value_usd` | pin 2 | `AssertionError: expected { amount: 912345, count: 2 } to deeply equal { amount: 9845, count: 2 }` (2 failed: both pin 2 cases) | cmp IDENTICAL |
| 3 | untrusted accumulated before the narrowing check | pin 4 | `AssertionError: expected { amount: 55555, count: 1 } to deeply equal { amount: +0, count: +0 }` (2 failed: pin 4, D-06) | cmp IDENTICAL |
| 4 | predicate call replaced by a local equality on `sign_in_failed` | pin 5 widened arm | `→ expected { amount: +0, count: +0 } to deeply equal { amount: 7000, count: 1 }` (3 failed: pin 5, degrade, D-06) | cmp IDENTICAL |
| 5 | narrowing `continue` deleted | D-06 | `→ expected 483210 to be 480000 // Object.is equality` (2 failed: pin 4, D-06) | cmp IDENTICAL |

## Files Created/Modified

- `src/app/(dashboard)/allocations/lib/live-holdings-summary.ts`: `summarizeLiveHoldings`, `LiveHoldingsSummary`, and `holdingEquityContributionLocal` (moved). Imports `@/lib/queries` as a type only.
- `src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts`: the unit pins, the `vi.hoisted` + `vi.mock("@/lib/closed-sets")` delegating double, and the distinct-triple self-proof.
- `src/app/(dashboard)/allocations/components/ScenarioComposer.tsx`: the memos, the hoisted gates, the clause builder, the State A span and the override-note condition.
- `src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx`: the `buildHoldingRef` import and the `describe("ScenarioComposer — AUMTRUST (Phase 167.1)")` tracer block.

## Verification (plan-level)

- `npx vitest run live-holdings-summary.test.ts ScenarioComposer.test.tsx --reporter=dot`: 2 files, 345/345 passed.
- The Task 1 `<verify>` with `-t "AUMTRUST|partial-book dollars|AUM-01"`: 51 passed, OK. It was re-run for the tracer feedback gate (auto mode) and passed again.
- The Task 2 `<verify>`: 9 passed, OK. `npm run typecheck`: exit 0, no `error TS`. The `vi.mock` factory shape typechecks, which settles RESEARCH A5.
- `npx eslint` on the three source/test files named in the plan: exit 0.
- `git diff --stat $(git merge-base HEAD origin/main) -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/`: empty (D-03 / D-10 / D-11).
- Acceptance greps: toggle-map walk in the composer 1 → 0; `function holdingEquityContributionLocal` in the composer 0; references to it in the composer 5; `"keys needing attention"` literal 0; `UNTRUSTED_KEY_SET_NOUN` 3; local status equality in the helper 0; `D-06` test titles 1; `vi.mock("@/lib/closed-sets"` 1 in the unit file and 0 in the composer suite; every `api_key_id` uses an `aumtrust-key-*` constant.

## Decisions Made

- `capitalizeFirst` is a module-level helper beside `buildUntrustedAumClause`, because no capitalize utility exists in the repo. The builder stays lower-case so plan 03 can nest the same clause in the override note unchanged.
- The "Required to size and commit." hint keeps its own condition. The plan names only the override note for the rewrite, and changing the hint as well would be a change the plan did not ask for.

## Deviations from Plan

None. The plan was executed as written.

One process note: the planning-hygiene check ran after the Task 1 commit rather than between `git add` and `git commit`. It returned OK over the committed tree. For Task 2 it ran in the right order.

## Issues Encountered

None.

## Known Stubs

None. `excludedUntrusted` is computed but not rendered on purpose. The plan ships it for the D-06 pin, and plan 05, which records the founder's D-06 answer, decides whether anything renders it.

## User Setup Required

None. No external service configuration is required.

## Next Phase Readiness

- Plan 03 can nest `buildUntrustedAumClause(...)` inside `scenario-aum-override-note` under `showUntrustedMarker && overrideNoteShowsLive`, and add the absence-state and tone assertions to the AUMTRUST describe block.
- Plan 05 (D-06) can read `liveHoldingsSummary.excludedUntrusted` and extend the clause builder.

---
*Phase: 167.1-aumtrust*
*Completed: 2026-09-24*

## Self-Check: PASSED
