---
phase: 167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro
fixed_at: 2026-09-24T02:46:53Z
review_path: .planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-REVIEW-R2.md
iteration: 2
findings_in_scope: 12
fixed: 12
skipped: 0
status: all_fixed
---

# Phase 167.1: Code Review Fix Report, round 2

**Fixed at:** 2026-09-24
**Source review:** `.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-REVIEW-R2.md`
**Iteration:** 2 (the final fix round under repo policy; verification comes next)
**Scope:** `all`. That covers WR-01 to WR-03 and IN-01 to IN-03 from the code reviewer, plus WR-04 to WR-06 and IN-04 to IN-06 from the silent-failure-hunter section.

**Summary:**
- Findings in scope: 12
- Fixed: 12, in 11 commits. Reviewer IN-01 and silent-failure-hunter IN-05 describe the same defect, so one commit closes both.
- Skipped: 0

**Where verification ran:** in the orchestrator-pinned phase worktree (branch `feat/167.1-aumtrust`), with `node_modules` symlinked from the main checkout and removed before every commit. No separate isolated worktree was created, so every figure below can be reproduced from this worktree.

**Method:** every behaviour fix had a failing test first. Every new guard was neutered, observed RED, then restored from a `cp` byte backup and checked with `cmp`. No git checkout, restore or stash was used.

## Fixed Issues

### WR-01: the composer's manager-side derivation was unpinned

**Files modified:** `src/app/(dashboard)/allocations/lib/live-holdings-summary.ts`, `live-holdings-summary.test.ts`, `components/ScenarioComposer.tsx`, `components/ScenarioComposer.test.tsx`
**Commit:** 378c3fb9c
**Applied fix:**
- `managerSideKeyIds(eligible, allocatorEligible)` is now a pure exported helper next to `summarizeLiveHoldings`, and the composer calls it.
- Unit cases use sets that differ in both directions.
- The composer suite now has a pass-through spy on `summarizeLiveHoldings` (the real helper still runs). One case asserts both the `managerSideApiKeyIds` argument and the resulting `excludedUntrusted` (3,210 over 1 holding, not 58,765 over 2), with hand-typed values.

The reviewer's neuter (the composer passes `[]`) is now RED. A reversed difference in the helper turns two tests RED.

### WR-02: `holdingEquityIsReported`'s NaN and non-finite-spot branches were unpinned

**Files modified:** `live-holdings-summary.test.ts`
**Commit:** 0620a4a6f
**Applied fix:**
- A table-driven case covers null, NaN and ±Infinity for both holding types, plus a known zero. It asserts the hand-listed contribution and that `holdingEquityIsReported` is false exactly where the contribution is a substituted 0.
- A `summarizeLiveHoldings` case with an untrusted NaN-P&L derivative and an untrusted NaN-value spot holding pins `unavailable: 2` and an unchanged total.

The reviewer's neuter (`derivative → pnl != null`, `spot → true`) now turns 7 tests RED.

### WR-03: the D-20 residual was stated as one status

**Files modified:** `live-holdings-summary.ts`, `live-holdings-summary.test.ts`, `167.1-05-PLAN.md`, `167.1-06-PLAN.md`, `167.1-UI-SPEC.md`
**Commit:** 952340138
**Applied fix:**
- The `excludedUntrusted` docblock now states the residual by its predicate: a manager-side key outside `eligibleApiKeyIds` (revoked, soft-disconnected or inactive) cannot be told apart, so its holdings may be counted. That over-discloses and never hides.
- The same correction is dated, with the original kept as lineage, at four sites in plan 05 (the must-have truth, the option-(b) cons, the release-notes handoff and threat T-167.1-15), at plan 06's release-notes known limit, and in UI-SPEC M3.
- A unit case pins that a soft-disconnected manager-side key's untrusted holding is counted. It goes RED under a neuter that hides it.

The plan frontmatter still parses as YAML.

### WR-04: the missing-key `console.error` reached no operator

**Files modified:** `components/ScenarioComposer.tsx`, `components/ScenarioComposer.test.tsx`
**Commit:** 5673eb381
**Applied fix:**
- The composer now calls `captureToSentry`, following the `ScenarioCommitDrawer` pattern: level `warning`, tags `{ component: "ScenarioComposer", reason: "holding_key_missing_from_key_list" }`, and `extra: { unknown_status_count }` only.
- The console line stays for local dev.
- The composer suite records captures, using the drawer suite's recorder shape. The test asserts the level, the exact tags and the count, and that no key id appears in the message, tags or extra. A control book sends no capture.

The test was RED before the fix, and RED again under two neuters: a key id in `extra`, and a key id in the message.

### WR-05: the Holdings tab read a missing key as trusted

**Files modified:** `HoldingsTabPanel.tsx`, `HoldingsTabPanel.spot-derivative-split.test.tsx`, `components/HoldingsTable.tsx`, `components/OpenPositionsTable.tsx`, `components/ScenarioComposer.tsx`, `components/untrusted-key-status.surfaces.test.tsx`, `lib/live-holdings-summary.ts`, `src/lib/closed-sets.ts`, `167.1-UI-SPEC.md`
**Commit:** e8eb1c518
**Status:** fixed: requires human verification (logic and new copy)
**Applied fix:**
- **Panel.** A key present with a null status stays `"unknown"` and trusted. A key absent from `apiKeys` sets a new optional `source_key_missing` on its spot and open-position rows.
- **Row marker.** Both tables show a muted `Sync status unknown` marker (`UNKNOWN_KEY_STATUS_ROW_LABEL`, test id `holding-key-status-unknown`). It is not struck through, has no role, and the untrusted filter does not hide the row.
- **Open Positions footer.** Missing-key rows are counted as a separate unknown-status part in the same pass, and the qualifier renders on either count.
- **One wording source.** `buildKeyTrustClause` (in `live-holdings-summary.ts`) now builds the clause for both the composer marker and the footer. The nouns come from `UNTRUSTED_KEY_SET_NOUN` and the new `UNKNOWN_KEY_STATUS_SET_NOUN`. Each surface passes its own renderer (`formatUsd` / `formatPnl`, U-05). Every pre-existing string is byte-identical.
- **UI-SPEC.** A dated ADDED note records the new copy.

Tests were RED before the change and under five neuters:
1. The panel never flags.
2. The panel flags a present null.
3. The footer drops the part.
4. The footer reads a present null as missing.
5. The table drops the marker.

### WR-06: a missing payload field hid holdings from `$Y`

**Files modified:** `live-holdings-summary.ts`, `live-holdings-summary.test.ts`, `components/ScenarioComposer.tsx`
**Commit:** 117d0fdd0
**Status:** fixed: requires human verification (logic)
**Applied fix:** `managerSideKeyIds` takes the raw payload fields and returns `[]` if either is undefined, so an unknown difference over-discloses. The composer no longer applies `?? []` before the call. The unit case was RED against the previous behaviour.

### IN-01 (reviewer) and IN-05 (silent-failure-hunter): the merged "(value unavailable for N …)" suffix

**Files modified:** `components/ScenarioComposer.tsx`, `components/ScenarioComposer.test.tsx`, `167.1-UI-SPEC.md`
**Commit:** f8941d3b1
**Status:** fixed: requires human verification (copy)
**Applied fix:** Each part now carries its own unavailable count, directly after that part. A single-part clause is byte-identical to before. A typed-string composer case pins the two-part form, `Includes $0 from keys needing attention (value unavailable for 1 holding) and $4,444 from keys with an unknown sync status.`, and was RED before the change. The UI-SPEC has a dated row for this form. WR-05 later moved this logic into the shared builder unchanged.

### IN-02: the WR-05 log pin did not assert "no key id"

**Files modified:** `components/ScenarioComposer.test.tsx`
**Commit:** 470af9fde
**Applied fix:** The test now asserts that no logged argument contains the missing key's id or either listed key's id. It goes RED under a neuter that logs the ids.

### IN-03: mis-indented nested call in State B

**Files modified:** `components/ScenarioComposer.tsx`
**Commit:** 1fa81224d
**Applied fix:** Re-indented the call. Whitespace only (`git diff -w` was empty).

### IN-04: a dropped missing-key holding leaves `$Y` without trace

**Files modified:** `live-holdings-summary.ts`
**Commit:** ad3b11d16
**Applied fix:** I took the finding's docblock option. The `excludedUntrusted` docblock now says that such a holding lands in no part and no on-screen figure. It also says that a D-06 answer wanting it named needs an `excludedUnknownStatus` part rather than a wider reading of this one. The existing "missing-key holding the narrowing drops" unit case pins the behaviour. Comment only.

### IN-06: test hygiene

**Files modified:** `components/ScenarioComposer.test.tsx`, `live-holdings-summary.test.ts`
**Commit:** 7bf621932
**Applied fix:**
- The missing-key book's comment now says SSR cannot emit this payload, so the cases built on it are a guard, not a reachable state.
- Removed the redundant `.not.toBe(71_110)`. The exact `toEqual` already rules that value out.

## Round-1 fixes re-checked (all still hold)

- **R1 WR-01 (the D-20 narrowing in the helper):** the D-20 unit case is green, and it is now also pinned at the composer (R2 WR-01).
- **R1 WR-02 (`aria-describedby`):** both accessible-description cases are green, including the absent-attribute case.
- **R1 WR-03 ("value/P&L unavailable"):** every typed string is unchanged and green on both surfaces. It is now built by the shared builder.
- **R1 WR-05 (`unknownStatus` via `.has`):** the helper and composer cases are green, and the Holdings tab now matches the composer (R2 WR-05).
- **R1 IN-01, IN-02, IN-03:** no tombstone came back, the unclamped `$5,000` beside `1000` pin is green, and `AT_KEY_REVOKED` is unchanged.
- **Known limits:** R1 WR-04 and IN-06 stay recorded in `<known_limits>` behind D-18, and D-18 is unchanged. R1 IN-05 stays kept by U-05.
- **Plan 05:** not executed. Nothing renders "excludes": the D-06 component pin is green, and a grep of the three host files finds only pre-existing, unrelated leverage copy.

## Verification (gate list, run at `7bf621932` in the phase worktree)

- The three phase suites (`live-holdings-summary.test.ts`, `ScenarioComposer.test.tsx`, `untrusted-key-status.surfaces.test.tsx`): 3 files, **415/415** pass. Round 1 ended at 391.
- Everything under `src/app/(dashboard)/allocations` plus `src/lib/closed-sets.*test.ts`: **135 files, 2079/2079** pass. Round 1 ended at 2054.
- `npx tsc --noEmit`: exit 0.
- `eslint` on the ten touched source and test files: exit 0.
- D-03: `git diff --stat origin/main -- src/lib/queries.ts supabase analytics-service` prints nothing. This round did not touch the `summarizeLiveHoldings` loop, `holdingEquityContributionLocal`, or the Open Positions `totalUnrealized` accumulation. Every pre-existing total pin is unchanged and green.
- Construction rules:
  - `grep -c '"keys needing attention"'` returns 0 on the composer, the helper, `OpenPositionsTable` and `HoldingsTable`.
  - The only `=== "revoked"` hit is the pre-existing comment in `OpenPositionsTable.tsx`.
  - The new noun lives only in `closed-sets.ts`.
- D-07 / D-08 / D-09: both disclosures still gate on counts. Every marker case asserts exactly one `scenario-aum-untrusted-note`. The new row marker is `text-xs text-text-muted` with no role and no live region.
- No key id reaches any log, Sentry payload or copy, and tests pin this for the console line and the Sentry capture.
- `scripts/check-planning-hygiene.ts`: OK after every `.planning` write.

---

_Fixed: 2026-09-24_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 2_
