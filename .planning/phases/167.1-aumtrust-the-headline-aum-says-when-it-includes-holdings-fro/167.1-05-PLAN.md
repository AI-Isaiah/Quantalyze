---
phase: 167.1-aumtrust
plan: 05
type: execute
wave: 4
depends_on: [167.1-03, 167.1-04]
files_modified:
  - src/app/(dashboard)/allocations/lib/live-holdings-summary.ts
  - src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - .planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-CONTEXT.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [D-06, D-20]
estimate:
  tokens: 70000     # revised 2026-09-23: CHANGELOG/VERSION work moved to plan 06
  raw_tokens: 70000
  tasks: 3
  confidence: low   # DERIVED: estimate-calibration reports sample_count 0, factor 1
must_haves:
  truths:
    - "D-06: the founder's answer (a, b or c) is recorded, dated, in 167.1-CONTEXT.md D-06 and in the ROADMAP `### Phase 167.1` entry — the open question does not stay open silently, and nothing in plans 01-04 waited on it."
    - "D-06 (b) only / D-20: in book mode, when the live total is on screen and at least one holding dropped by the contributing-set narrowing has an untrusted key (excluding known manager-side keys, eligibleApiKeyIds minus allocatorEligibleApiKeyIds), the ONE scenario-aum-untrusted-note element carries 'excludes $Y from keys needing attention' composed per UI-SPEC § 3 — never a second element; the total is unchanged."
    - "D-06 (b) only / UI-SPEC conditional (covered, M3): the rendered textContent matches every row of the UI-SPEC § 3 table exactly — State A excludes-only, State A both, State B excludes-only and State B both; the render gate is on the excluded COUNT (not the amount) AND the live total being on screen."
    - "D-06 (a) only: no source file changes; the D-06 pins from plans 01 (unit) and 03 (component) stay green unmodified and are the record of the chosen behaviour."
    - "D-06 (c): this plan STOPS without changing code — option (c) changes a money number (reverses D-03) and is rated costly in CONTEXT, so it returns for replanning instead of being improvised; plan 06 (the release) does not run until that replan has placed its new plans ahead of it."
    - "D-20: a revoked manager-side key cannot be told apart from the payload, so its holdings may be counted in $Y — over-disclosure, never hiding — and that residual is stated in the code comment and in this plan's release-notes handoff, which plan 06 carries into the CHANGELOG."
    - "D-14 (revision 2026-09-23): this plan edits no CHANGELOG.md, VERSION or package.json; it hands its outcome text to plan 06 in a '## Release-notes handoff' section of its SUMMARY, so the one chore(release) commit carries the final CHANGELOG."
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/live-holdings-summary.ts"
      provides: "(b) only: an excludeKeyIds argument applied to the excludedUntrusted bucket alone"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "(b) only: buildUntrustedAumClause composing includes/excludes; widened marker gate"
    - path: ".planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-CONTEXT.md"
      provides: "dated D-06 resolution"
    - path: ".planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-05-SUMMARY.md"
      provides: "'## Release-notes handoff' section plan 06 reads"
  key_links:
    - from: "summarizeLiveHoldings().excludedUntrusted"
      to: "scenario-aum-untrusted-note (excludes clause)"
      via: "buildUntrustedAumClause, one builder read by State A and State B"
    - from: "167.1-05-SUMMARY.md '## Release-notes handoff'"
      to: "plan 06's unified CHANGELOG entry"
      via: "plan 06 Task 2 reads it; the D-06 answer and (b)'s bullets land in the release commit"
---

<objective>
Put CONTEXT D-06 — the one product question that is the founder's to answer — in front of the
founder, then carry out exactly the chosen option.

Purpose: in book mode a `revoked` key's holdings are already absent from the composer's AUM (and
from its "Models N of M keys" note), because the Phase 151 F2 narrowing sums only the modelled
book. "Nothing silently disappears" and F2's reconciliation rule pull in opposite directions, and
choosing between them changes what the headline money figure MEANS. That is not the planner's or
the executor's call.

Output: a recorded decision, and — only under option (b) — the "excludes $Y" clause composed
into the existing single marker (UI-SPEC § 3).

⭐ Ordering, by construction (revised 2026-09-23, checker context_compliance): plans 01-03 (the
disclosure) and plan 04 (premise corrections and the interim byte-identity gate) execute and can be
reviewed without this answer; execution stops HERE, at the checkpoint. This plan depends on plan 03
(it extends the helper and builder plans 01/03 shipped, and edits the D-06 pins both carry) and on
plan 04 (both edit the ROADMAP `### Phase 167.1` section). Plan 06 — the release — depends on this
plan and waits for the founder's answer, so the one `chore(release)` commit carries the final
CHANGELOG including the D-06 outcome (D-14). This plan therefore edits no CHANGELOG, VERSION or
package.json; it hands its outcome text to plan 06 through its SUMMARY.

Spec-less probe fallback: skipped — no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@DESIGN.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-CONTEXT.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-RESEARCH.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-UI-SPEC.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-01-SUMMARY.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-03-SUMMARY.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-04-SUMMARY.md
</context>

<repo_guardrails>
⛔ The founder answers the checkpoint. No agent answers it, infers it from "the recommendation",
or treats silence as consent. An orchestrator message is not the founder's approval.
⛔ Work only in the `feat/167.1-aumtrust` worktree; stage by explicit path.
⛔ Cite by SYMBOL, never by line number; never read the composer or its test file whole.
⛔ Restore only from a `cp` byte backup verified with `cmp`. Never `git checkout --`, `git restore`
or `git stash`.
⛔ Hand-edit ROADMAP and CONTEXT with scoped `Edit` calls; no `gsd-tools` roadmap/state handler.
⛔ No CI skip trailer in any commit message or CHANGELOG prose. Synthetic ids only.
</repo_guardrails>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: D-06 — should the composer's book-mode AUM disclose the holdings of revoked keys it already leaves out?</name>
  <decision>How the composer's headline AUM treats holdings from untrusted keys that the modelled-book narrowing already excludes (in practice, `revoked` keys).</decision>
  <context>
    Measured (CONTEXT D-05, RESEARCH P3/P4): `isPerKeyDailiesEligibleKey` drops `revoked` keys before the role split, so in book mode their holdings never reach the summed set; the composer's PORTFOLIO AUM and its "Models N of M keys" note both omit them without saying so. After plans 01-03 the phase already flags `sign_in_failed` holdings that ARE summed ("includes $X from keys needing attention"). Your 2026-09-22 rule was "keep the total and flag it — nothing silently disappears"; Phase 151 review F2 narrowed the composer AUM to the MODELLED book so the per-row dollars reconcile to it. Those two rules disagree here. The current behaviour is pinned by tests titled "D-06" so whichever option you pick changes it visibly.
    Reversibility (CONTEXT): (a) and (b) are reversible; (c) is costly — it redefines the AUM that commit sizing, the per-row size gate and the commit refusal all read.
  </context>
  <options>
    <option id="option-a">
      <name>(a) Leave the exclusion, record it</name>
      <pros>No new copy; the composer AUM keeps meaning "the modelled book"; zero code change.</pros>
      <cons>A revoked key's holdings still vanish from the headline with no sentence saying so — the "password rotation reads as an AUM loss" outcome you rejected, for the venue-rejected state.</cons>
    </option>
    <option id="option-b">
      <name>(b) Disclose the exclusion — RECOMMENDED</name>
      <pros>Total unchanged, F2 reconciliation untouched; the same single marker gains "excludes $Y from keys needing attention" (e.g. "Includes $12,345 and excludes $8,000 from keys needing attention."), so nothing disappears silently. Additive and reversible.</pros>
      <cons>D-20 residual: a revoked MANAGER-side key cannot be told apart from an allocator key without a new server field, so its holdings may be counted in $Y — it over-discloses, never hides. (sign_in_failed manager-side keys ARE recognised and left out.)</cons>
    </option>
    <option id="option-c">
      <name>(c) Include revoked holdings in the total and flag them</name>
      <pros>Matches "keep the total and flag it" literally; the existing "includes $X" copy covers it.</pros>
      <cons>Changes a money number (reverses this phase's D-03) and breaks F2: the rows the composer renormalises would no longer add up to the AUM they are sized from. Needs its own replan, not an executor improvisation.</cons>
    </option>
  </options>
  <resume-signal>Reply "a", "b" or "c" (optionally with a note). Silence is not an answer; the plan waits.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Carry out the answer — (b) end-to-end "excludes $Y" in the one marker; (a) no code; (c) stop</name>
  <reversibility rating="reversible">Option (b) is additive copy over an existing bucket; removing it is one clause and its tests. (c) is not implemented here (costly, returns for replan).</reversibility>
  <files>src/app/(dashboard)/allocations/lib/live-holdings-summary.ts, src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - The founder's answer from Task 1.
    - `live-holdings-summary.ts` (`summarizeLiveHoldings`, `excludedUntrusted`) and its test file's tests titled `D-06` (plan 01).
    - `ScenarioComposer.tsx`: grep `buildUntrustedAumClause`, `showUntrustedMarker`, `liveHoldingsSummary`, and both `scenario-aum-untrusted-note` sites; and `ScenarioComposer.test.tsx`: grep `"D-06` inside the AUMTRUST describe (plan 03).
    - `src/lib/queries.ts` type `MyAllocationDashboardPayload`: `eligibleApiKeyIds`, `allocatorEligibleApiKeyIds`, `contributingApiKeyIds` (read only).
    - 167.1-UI-SPEC.md § 3 (M3 table and exact textContent) and 167.1-RESEARCH.md P4.
  </read_first>
  <behavior>
    (b) only:
    - Unit: a `sign_in_failed` key present in `eligibleApiKeyIds` but not in `allocatorEligibleApiKeyIds` (manager-side) and outside the contributing set adds nothing to `excludedUntrusted`; an allocator-side `sign_in_failed` key outside the contributing set does; a `revoked` key (in neither set) does (the D-20 residual, asserted and commented).
    - Component, one case per UI-SPEC § 3 textContent row, exactly one marker element in each:
      State A excludes-only: `Excludes $8,000 from keys needing attention.`;
      State A both: `Includes $12,345 and excludes $8,000 from keys needing attention.`;
      State B excludes-only: override note reads `Overrides live-holdings total $50,000, which excludes $8,000 from keys needing attention.` (nested marker `excludes $8,000 from keys needing attention`);
      State B both: override note reads `Overrides live-holdings total $50,000, which includes $12,345 and excludes $8,000 from keys needing attention.`
    - Count gate: an excluded untrusted holding whose equity contribution is 0 still renders the excludes clause (`Excludes $0 from keys needing attention.` in State A) — the gate is on the excluded COUNT; this is the case the amount-gate drill below turns RED.
    - The field's value is unchanged in every case (the total is not touched).
    - Absent when the live total is not on screen, and in blank mode.
  </behavior>
  <action>
    Branch on the recorded answer.
    (c): change nothing. Write in the SUMMARY that (c) reverses D-03 and is rated costly, and return a checkpoint to the orchestrator asking for `/gsd-plan-phase 167.1` in revision mode with D-06 = (c), noting that plan 06 (the release) must stay the LAST plan — the replan puts its new plans ahead of plan 06 in `depends_on`. Stop this plan here (Task 3 still records the answer).
    (a): change no source file. The D-06 pins from plan 01 (unit) and plan 03 (component) already encode the chosen behaviour; run them to confirm they are green and continue to Task 3.
    (b): write the failing tests first (edit the plan-01 unit and plan-03 component D-06 tests DELIBERATELY: their titles keep the `D-06` prefix and their assertions flip from "no excludes copy" to the UI-SPEC § 3 strings). ⛔ Distinct-triple fixture rule: `holdingByRef` and the SSR dedup key on `buildHoldingRef({ venue, symbol, holding_type })`, not on the key, so every new fixture holding (the excluded revoked and manager-side ones included) gets a distinct `(venue, symbol, holding_type)` triple and passes the AUMTRUST describe's self-proof — a shared triple collapses two holdings into one and makes an excluded amount vanish for a reason unrelated to the code under test. Then per D-20 add an optional `excludeKeyIds` argument to `summarizeLiveHoldings` that removes known manager-side keys from the `excludedUntrusted` bucket ONLY — `total` and `untrusted` must not read it. In the composer compute the manager-side set once as `eligibleApiKeyIds` minus `allocatorEligibleApiKeyIds` and pass it in. Comment the D-20 residual at the argument: a revoked manager-side key is in neither set, so it may be counted — over-disclosure, never hiding. Extend `buildUntrustedAumClause` so its lower-case output follows the UI-SPEC § 3 table (includes only / excludes only / includes and excludes, noun once at the end from the constant); State A capitalises and adds the full stop, State B prefixes `, which ` and the override note supplies the full stop — ONE builder, no second element. Widen `showUntrustedMarker` from "untrusted count > 0" to "untrusted count > 0 OR excludedUntrusted count > 0", keeping the book-mode and live-total-on-screen conjuncts exactly as they are.
    (b) neuters, each on a `cmp`-verified byte backup: pass `excludeKeyIds` into the `total` path → a total assertion RED; gate the excludes clause on amount > 0 → a zero-amount excluded case RED; render the excludes clause as a second span → the one-marker assertion RED. Paste the RED lines into the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts' --reporter=dot > /tmp/167.1-05-t2a.log 2>&1 && npx vitest run 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx' --reporter=dot -t "AUMTRUST|D-06|partial-book dollars|AUM-01" > /tmp/167.1-05-t2b.log 2>&1; RC=$?; tail -15 /tmp/167.1-05-t2a.log /tmp/167.1-05-t2b.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167.1-05-t2a.log /tmp/167.1-05-t2b.log && grep -qE 'Tests +[0-9]+ passed' /tmp/167.1-05-t2b.log && echo OK</automated>
    <fails_when>non-zero exit from either run, "0 passed" in either summary, or no "Tests N passed" line for the composer filter</fails_when>
  </verify>
  <acceptance_criteria>
    - Under every option, `git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts` prints nothing.
    - Under (a) or (c), `git diff --stat HEAD -- 'src/app/(dashboard)/allocations/'` prints nothing at the end of this task.
    - Under (b), `grep -c 'data-testid="scenario-aum-untrusted-note"' 'src/app/(dashboard)/allocations/components/ScenarioComposer.tsx'` still returns 2 (no new element), `grep -c '"keys needing attention"'` on the composer returns 0, and `grep -cE '(it|test)\("D-06' 'src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts'` is at least 1.
    - Under (b), the SUMMARY records three neuter → RED → restore drills with `cmp` verdicts.
  </acceptance_criteria>
  <done>The chosen option is implemented (b), confirmed as already-true (a), or handed back for replanning (c); the total never moved.</done>
</task>

<task type="auto">
  <name>Task 3: Record the answer where the phase is read, and hand the release-notes text to plan 06</name>
  <files>.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-CONTEXT.md, .planning/ROADMAP.md</files>
  <read_first>
    - `167.1-CONTEXT.md` D-06 bullet; `.planning/ROADMAP.md` `### Phase 167.1` section (including plan 04's correction note).
    - Project CLAUDE.md "CHANGELOG discipline" section vocabulary (the handoff uses it, so plan 06 can paste rather than re-derive).
  </read_first>
  <action>
    In `167.1-CONTEXT.md`, append under the D-06 bullet a dated `⭐ RESOLVED <date> (founder): option (<letter>)` line with the founder's note if any, keeping the OPEN text as lineage. In the ROADMAP `### Phase 167.1` section add one dated sentence recording the D-06 answer (scoped `Edit`, no other section).
    ⛔ Edit no `CHANGELOG.md`, `VERSION` or `package.json` (D-14, revision 2026-09-23: plan 06 is the last plan and writes the one release commit). Instead, end this plan's SUMMARY with a section headed exactly `## Release-notes handoff (for plan 06)` carrying, in this repo's CHANGELOG section vocabulary: the dated D-06 answer and the founder's note; under (b) an `### Added` bullet for the excludes clause (State A and State B, one marker), a `### Tests` bullet for the flipped D-06 pins and the three drills, and a `### Notes` bullet for the D-20 residual (a revoked manager-side key may be counted in $Y — over-discloses, never hides); under (a) a `### Notes` bullet that a revoked key's holdings stay absent from the book-mode AUM by decision, pinned by the D-06 tests; under (c) a `### Notes` bullet that the decision was taken and its change awaits a replan, and that plan 06 must not run until that replan's plans precede it. ⛔ No skip-trailer token and no credential-shaped literal in the handoff text.
    Run `npm run check:planning-hygiene` after the `.planning/` edits.
  </action>
  <verify>
    <automated>test "$(grep -c 'RESOLVED' .planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-CONTEXT.md || true)" -ge 1 && npm run check:planning-hygiene > /tmp/167.1-05-t3-hyg.log 2>&1 && BASE="$(git merge-base HEAD origin/main)" && git diff --stat "$BASE" -- CHANGELOG.md VERSION package.json > /tmp/167.1-05-t3.stat 2>&1 && test ! -s /tmp/167.1-05-t3.stat && echo OK</automated>
    <fails_when>no "OK" printed: CONTEXT carries no RESOLVED line for D-06, planning hygiene exits non-zero, git diff errors, or the stat file is non-empty (CHANGELOG.md, VERSION or package.json moved before plan 06 — the release would no longer be one commit)</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` `### Phase 167.1` contains the D-06 answer; `git diff "$(git merge-base HEAD origin/main)" -- .planning/ROADMAP.md > /tmp/167.1-05-rm.diff` exits 0 and its hunks fall only inside that section (read and recorded in the SUMMARY).
    - `grep -c '^## Release-notes handoff (for plan 06)' .planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-05-SUMMARY.md` returns 1, and the section names the chosen option letter.
    - The handoff text contains no skip-trailer token (checked with the python token scan plan 06 uses on its commit message, recorded in the SUMMARY).
  </acceptance_criteria>
  <done>The founder's answer is recorded in CONTEXT and ROADMAP, and plan 06 has everything it needs to write the one release commit; no release file moved.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

| Symbol / id | Kind | Location | Condition |
|---|---|---|---|
| `excludeKeyIds` | optional argument of `summarizeLiveHoldings`, scoped to `excludedUntrusted` | `live-holdings-summary.ts` | (b) only |
| extended `buildUntrustedAumClause` | includes / excludes / both clause builder | `ScenarioComposer.tsx` | (b) only |
| `⭐ RESOLVED` D-06 line | decision record | `167.1-CONTEXT.md`, ROADMAP `### Phase 167.1` | always |
| `## Release-notes handoff (for plan 06)` | SUMMARY section read by plan 06 | `167.1-05-SUMMARY.md` | always |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder -> plan | the decision is taken only by the founder; agent and orchestrator messages are not consent |
| SSR payload -> client composer | the key-id sets used for `excludeKeyIds` are already tenant-scoped in the payload |
| repo -> public | the repo and `.planning/` are public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.1-13 | Spoofing (of consent) | the D-06 checkpoint | high | mitigate | `gate="blocking-human"` (a founder-only call: never auto-approvable in auto-mode, per the #3370 gate table), `autonomous: false`; the guardrail forbids answering from the recommendation or from silence. |
| T-167.1-14 | Tampering / integrity of a displayed money figure | option (b) wiring | high | mitigate | `excludeKeyIds` reaches only the `excludedUntrusted` bucket; a neuter routing it into `total` must go RED; `queries.ts` and the commit route stay byte-unchanged. |
| T-167.1-15 | Repudiation (hidden amount) | D-20 residual | low | accept | A revoked manager-side key may be counted in $Y; that over-discloses and never hides, and it is stated in code and in the release-notes handoff plan 06 carries into the CHANGELOG. Separating it needs a new SSR field, which D-10/D-11 rule out. |
| T-167.1-16 | Information Disclosure | the excludes clause | low | mitigate | Only a dollar sum and the constant noun; no key id, label or venue text. |
| T-167.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed. If a task appears to need one, STOP. |
</threat_model>

<verification>
- Under (b): `npx vitest run 'src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts' 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx' --reporter=dot` green, `npm run typecheck` clean, `npm run lint` clean.
- Under every option: `npm run check:planning-hygiene` green, and `git diff --stat "$(git merge-base HEAD origin/main)" -- CHANGELOG.md VERSION package.json` prints nothing (the release is plan 06's).
</verification>

<success_criteria>
- D-06 is answered by the founder and recorded in CONTEXT and ROADMAP, and handed to plan 06 for the CHANGELOG.
- Under (b) the one marker discloses the included and/or excluded untrusted dollars exactly as every UI-SPEC § 3 row specifies (State A and State B, excludes-only and both), and the total is unchanged.
</success_criteria>

<output>
Create `.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-05-SUMMARY.md` when done
</output>
