---
phase: 167.1-aumtrust
plan: 06
type: execute
wave: 5
depends_on: [167.1-05]
files_modified:
  - .planning/WINDOWS.md
  - CHANGELOG.md
  - VERSION
  - package.json
autonomous: true
requirements: [D-14, D-03, D-06, D-10, D-11]
estimate:
  tokens: 60000     # created 2026-09-23 from plan 04's former Tasks 2-3 (checker context_compliance)
  raw_tokens: 60000
  tasks: 2
  confidence: low   # DERIVED: estimate-calibration reports sample_count 0, factor 1
must_haves:
  truths:
    - "D-14: this is the LAST plan of the phase, and it writes the release commit — VERSION and package.json carry byte-equal 4-part strings, bumped on meaning (a user-visible disclosure, no money-math change), with ONE unified CHANGELOG entry built by enumerate -> group -> cross-check, all in one chore(release) commit; no commit after it on this branch edits CHANGELOG.md, VERSION or package.json."
    - "D-14: `.planning/WINDOWS.md` entry 66 reads `fixed` (resolved in the same plan as the release, as D-14 specifies), its disposition sentence names the D-06 answer, and no other row moved."
    - "D-06: the unified CHANGELOG entry states the founder's D-06 answer taken from plan 05's '## Release-notes handoff (for plan 06)' section — under (b) the excludes clause is an Added bullet and the D-20 residual a Notes bullet; under (a) the recorded absence is a Notes bullet. D-06 is never described as open."
    - "D-03 / D-10 / D-11 (static, final): across the whole branch INCLUDING plan 05's changes, src/lib/queries.ts, src/app/api/allocator/scenario/commit/route.ts, supabase/ and analytics-service/ are byte-unchanged."
    - "Under D-06 option (c) this plan does not run: plan 05 hands back for a replan, and the replan keeps this plan last by adding its new plans to this plan's depends_on."
  artifacts:
    - path: ".planning/WINDOWS.md"
      provides: "row 66 fixed, with a disposition sentence"
    - path: "CHANGELOG.md"
      provides: "one unified entry for the phase's version, including the D-06 outcome"
    - path: "VERSION"
      provides: "bumped 4-part version, byte-equal to package.json"
  key_links:
    - from: "167.1-05-SUMMARY.md '## Release-notes handoff (for plan 06)'"
      to: "CHANGELOG.md unified entry"
      via: "Task 2 reads the handoff and folds it into the grouped entry"
    - from: "VERSION"
      to: "package.json version"
      via: "[CRITICAL-02] in src/__tests__/critical-regressions.test.ts"
---

<objective>
Ship the phase: close WINDOWS entry 66, re-prove the money math is byte-unchanged across the whole
branch now that plan 05 may have edited source, and write the ONE release commit (D-14).

Purpose: D-14 says the last plan writes the release commit, and puts WINDOWS 66 in that same plan.
Before the 2026-09-23 revision the release sat in plan 04 (wave 2) while the D-06 plan ran after it
and edited CHANGELOG.md — and under option (b) shipped source — in later commits with no version
bump (checker context_compliance). The release now runs after plan 05, so the one `chore(release)`
commit carries the final CHANGELOG including the D-06 outcome.

⚠️ Execution consequence, stated so nobody reads it as a stall: plans 01-04 run autonomously; plan
05 stops at the founder's `checkpoint:decision`; THIS plan waits for that answer. The phase cannot
ship before the answer in any ordering, because plan 05 is `autonomous: false`, so nothing is lost
by releasing last. Under option (c) this plan does not run until the replan's plans precede it.

Output: a closed ledger row, a final byte-identity reading, and one `chore(release)` commit.

Spec-less probe fallback: skipped — no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-CONTEXT.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-01-SUMMARY.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-02-SUMMARY.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-03-SUMMARY.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-04-SUMMARY.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-05-SUMMARY.md
</context>

<repo_guardrails>
⛔ **Precondition: plan 05's SUMMARY exists and carries `## Release-notes handoff (for plan 06)`
naming option (a) or (b).** If it names (c), or the section is absent, STOP — do not release.
⛔ Work only in the `feat/167.1-aumtrust` worktree; stage by explicit path, never `git add -A`.
⛔ **Never write a CI skip trailer in a commit message or in CHANGELOG prose**, not even to deny it
— GitHub honours the token anywhere and a squash carries it into `main`. Say "the skip trailer".
⛔ **No credential-shaped literal, real key id, real strategy name, home path or username** in any
committed text. The repo and `.planning/` are public.
⛔ **Never `npm version`** (it rewrites the lockfile and can normalise to 3 digits).
⛔ **`gsd-tools` state and roadmap handlers clobber `STATE.md` / `ROADMAP.md`.** Before any
`gsd-tools` call, `cp` a byte backup of every ledger it could touch; afterwards `git diff` and
revert only the collateral it caused.
⛔ Restore only from a `cp` byte backup verified with `cmp`. Never `git checkout --`, `git restore`
or `git stash`.
⛔ Cite by SYMBOL, never by line number.
⚠️ Run `npm run check:planning-hygiene` after every `.planning/` write; ⛔ never read its result
with `tail -1` (the count line and the OK line are different lines).
</repo_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: Close WINDOWS entry 66 and re-prove the money math is byte-unchanged across the whole branch (D-14, D-03, D-10, D-11)</name>
  <precondition>`.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-05-SUMMARY.md` contains a `## Release-notes handoff (for plan 06)` section naming option (a) or (b).</precondition>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - `.planning/WINDOWS.md`: the frontmatter, and the trailing 4-backtick `json` entries block, which is the ledger's SOLE source of truth — the markdown table above it is RENDERED from that block by `renderTable`, and every `gsd-tools windows` write refuses with `WINDOWS_LEDGER_TABLE_DRIFT` when the on-disk table disagrees with the JSON. Never hand-edit the rendered table.
    - The JSON objects for entries 64 and 65: the house precedent for a disposition on a `fixed` entry is a sentence in the entry's `reason` field (entry 65: "Fixed in 167-04 task 3 (D-16): …"); the original `description` stays as the lineage of the defect.
    - `$HOME/.claude/gsd-core/bin/lib/broken-windows.cjs`: `markFixed` (sets only `status` and `resolved_at`, then recomputes counts — the `windows fixed <id>` verb takes NO reason argument, so no verb can record a disposition on a fixed entry), `parseLedger`, `renderLedger`, `renderTable`, `extractTableRegion`, `withLedgerLock` (all exported).
    - Project CLAUDE.md "gsd-tools state handlers CLOBBER" and the memory rule "try gsd-tools first, intervene only on a measured clobber".
    - 167.1-05-SUMMARY.md `## Release-notes handoff (for plan 06)` (the D-06 answer: option letter and the date the founder gave it).
  </read_first>
  <action>
    Per D-14, entry 66 closes in this plan, in two steps: the tool verb for the status, then the disposition recorded in the JSON source of truth and the table re-rendered from it.
    (1) Backups. `cp` byte backups of `.planning/WINDOWS.md` to `/tmp/167.1-06-WINDOWS.pre` (this exact path — the automated check reads it as the pre-image) and of `.planning/STATE.md`, `.planning/ROADMAP.md` and `.planning/state.json` to plan-specific names (parallel agents share one scratch directory). `cmp` each backup against its source.
    (2) Status via the tool. From the worktree root run `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows fixed 66`. It rewrites the whole file through `renderLedger`, so the EXPECTED change set is: in the JSON block, entry 66's `status` (`open` to `fixed`) and `resolved_at` (null to a timestamp); the re-rendered table row whose id cell is 66; and the frontmatter `open_count` (down 1), `fixed_count` (up 1) and `last_updated`. None of that is collateral — do NOT revert it. Collateral is anything else: any other JSON entry, any other table row, `waived_count` / `total_count`, the header prose, or any change to `.planning/STATE.md`, `.planning/ROADMAP.md` or `.planning/state.json`. Diff each file against its backup; revert only collateral, from the backups, verified with `cmp`, and record in the SUMMARY what the tool changed and what, if anything, was reverted.
    (3) Disposition in the source of truth. Because the verb takes no reason, record it with the module's own exported functions under its own lock — never by hand-editing the table, and never by hand-editing the JSON alone (the next tool write would refuse on table drift, and the table would not show the sentence). In a single `node -e` run from the worktree root: require `$HOME/.claude/gsd-core/bin/lib/broken-windows.cjs`; inside `withLedgerLock(process.cwd(), fn)` read `.planning/WINDOWS.md`, `parseLedger` it, and STOP with an error unless `renderLedger(ledger)` is byte-identical to the file read (the round-trip identity that proves re-rendering moves nothing else — measured true on 2026-09-24); STOP unless entry 66 is `fixed` with an empty `reason`; set entry 66's `reason` to the disposition sentence; write `renderLedger(ledger)` back to the file. That one render updates the JSON `reason` and the table row's `reason` cell together, so table and JSON agree by construction.
    The disposition sentence, with the option letter and date taken verbatim from plan 05's handoff, and with NO apostrophes (the `node -e` body is single-quoted in the shell): "Resolved by Phase 167.1 (D-14): disclosed, not changed. The composer live-holdings total and the Open Positions footer name the untrusted part; no money number changed (D-03). D-06 (a revoked key holdings absent from the book-mode composer AUM) answered by the founder with option (LETTER) on YYYY-MM-DD." — LETTER is `a` or `b`, the date is ISO.
    (4) Run `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` and confirm it exits 0 (the tool reads the ledger it will next write), then run the automated check below. The whole procedure was rehearsed on a scratch copy on 2026-09-24: after step 2 alone the check fails naming the missing disposition sentence; after step 3 it prints `W66 OK`, and the diff against the pre-image is exactly frontmatter lines 3/5/7, table row 66, and the JSON `status` / `reason` / `resolved_at` lines of entry 66.
    (5) Finally run the branch-wide byte-identity gate below — it supersedes plan 04's interim reading because option (b) in plan 05 edited source — and record its output in the SUMMARY.
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs"),os=require("os");const bw=require(os.homedir()+"/.claude/gsd-core/bin/lib/broken-windows.cjs");const fail=m=>{console.error("W66 FAIL: "+m);process.exit(1)};if(!fs.existsSync("/tmp/167.1-06-WINDOWS.pre"))fail("pre-image backup /tmp/167.1-06-WINDOWS.pre missing");const pre=bw.parseLedger(fs.readFileSync("/tmp/167.1-06-WINDOWS.pre","utf8"));const raw=fs.readFileSync(".planning/WINDOWS.md","utf8");const post=bw.parseLedger(raw);const e=post.entries.find(x=>x.id===66);if(!e)fail("no entry 66 in the JSON block");if(e.status!=="fixed")fail("entry 66 status is "+e.status);if(!e.resolved_at)fail("entry 66 resolved_at is null");const r=e.reason||"";if(!r.startsWith("Resolved by Phase 167.1 (D-14): disclosed, not changed"))fail("entry 66 reason does not carry the disposition sentence");if(!r.includes("no money number changed (D-03)")||!/D-06 .*option \((a|b)\) on \d{4}-\d{2}-\d{2}/.test(r))fail("disposition sentence lacks the D-03 clause or the D-06 option and date");if(bw.extractTableRegion(raw,post.total_count)!==bw.renderTable(post.entries))fail("rendered table disagrees with the JSON block");if(post.entries.length!==pre.entries.length)fail("entry count moved");for(const p of pre.entries){const q=post.entries.find(x=>x.id===p.id);if(!q)fail("entry "+p.id+" vanished");const a={...p},b={...q};if(p.id===66){for(const k of ["status","resolved_at","reason"]){delete a[k];delete b[k]}}if(JSON.stringify(a)!==JSON.stringify(b))fail("entry "+p.id+" changed outside the permitted fields")}if(post.open_count!==pre.open_count-1||post.fixed_count!==pre.fixed_count+1||post.waived_count!==pre.waived_count||post.total_count!==pre.total_count)fail("frontmatter counts moved other than open-1 / fixed+1");console.log("W66 OK")' && BASE="$(git merge-base HEAD origin/main)" && git diff --stat "$BASE" -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/ > /tmp/167.1-06-t1.stat 2>&1 && test ! -s /tmp/167.1-06-t1.stat && npm run check:planning-hygiene > /tmp/167.1-06-t1-hyg.log 2>&1 && echo OK</automated>
    <fails_when>no "OK" printed. The ledger half prints `W66 FAIL: <reason>` and exits 1 when: the pre-image backup is missing; the ledger does not parse or its frontmatter counts disagree with its JSON entries (`parseLedger` throws); entry 66 in the JSON block is not `fixed` or has a null `resolved_at`; entry 66's `reason` lacks the disposition sentence, its "no money number changed (D-03)" clause, or a D-06 "option (a|b) on YYYY-MM-DD"; the rendered table region differs from `renderTable` of the JSON entries (a table-only or JSON-only edit); any other entry, or any entry-66 field other than status / resolved_at / reason, differs from the pre-image; or the counts moved other than open -1 / fixed +1. MEASURED 2026-09-24 against today's `.planning/WINDOWS.md` (pre-image = the same file): `W66 FAIL: entry 66 status is open`, exit 1. The rest fails when git diff errors, the stat file is non-empty (a money-math, SSR, migration or Python path changed on the branch), or planning hygiene exits non-zero.</fails_when>
  </verify>
  <acceptance_criteria>
    - The ledger check in `<automated>` prints `W66 OK`: entry 66 in the JSON source of truth is `status: "fixed"` with a timestamped `resolved_at` and the disposition sentence in `reason`, and the rendered table agrees with the JSON byte-for-byte.
    - `grep -a '^open_count:' .planning/WINDOWS.md` shows one less than `/tmp/167.1-06-WINDOWS.pre`, `grep -a '^fixed_count:'` one more, `waived_count` and `total_count` unchanged.
    - `diff /tmp/167.1-06-WINDOWS.pre .planning/WINDOWS.md` touches only: the `open_count`, `fixed_count` and `last_updated` frontmatter lines; the table row whose id cell is 66; and entry 66's `status`, `reason` and `resolved_at` lines in the JSON block. Anything else is collateral and must have been reverted.
    - `.planning/STATE.md`, `.planning/ROADMAP.md` and `.planning/state.json` are `cmp`-identical to this task's backups (recorded in the SUMMARY).
  </acceptance_criteria>
  <done>Entry 66 is fixed with a disposition sentence naming the D-06 answer, no collateral survives, and the whole branch provably changes no money math.</done>
</task>

<task type="auto">
  <name>Task 2: The release commit — VERSION, package.json and one unified CHANGELOG entry carrying the D-06 outcome (D-14, D-06)</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <read_first>
    - Project CLAUDE.md "CHANGELOG discipline" (the six-step mechanism and the section vocabulary) and "CI gate integrity" trap 1 (the skip trailer).
    - `CHANGELOG.md` top two entries for voice and structure.
    - `VERSION`, `package.json` `"version"`, and `git show origin/main:VERSION`.
    - `src/__tests__/critical-regressions.test.ts`: grep `CRITICAL-02`.
    - 167.1-01 … 167.1-05 SUMMARYs, especially 167.1-05-SUMMARY.md `## Release-notes handoff (for plan 06)`, and this plan's Task 1 result.
  </read_first>
  <action>
    Version (decided at planning, on meaning): this phase adds a user-visible disclosure and changes no money math and no behaviour, which in this repo's history is a third-segment bump for an inserted decimal phase (compare `0.82.1.0` and `0.80.1.0`), not a second-segment milestone bump — true under D-06 (a) and (b) alike, since (b) only adds disclosure copy. Take the higher of `VERSION` at HEAD and at `origin/main` (both `0.86.0.1` when planned) and bump the third segment, zeroing the fourth: `0.86.1.0` if nothing moved. Write it to `VERSION` (no trailing newline, matching the current file) and to `package.json` `"version"` by direct edit, byte-equal. ⛔ Never `npm version`.
    CHANGELOG, in the CLAUDE.md order: (1) `git log "$(git merge-base HEAD origin/main)"..HEAD --oneline` and COUNT the commits — a checklist, which now includes plans 03 and 05; (2) read the full diff over the same range; (3) group by theme before writing; (4) write ONE entry `## [X.Y.Z.B] - <date> — AUMTRUST: <one-line outcome>` at the top, replacing any earlier entry for commits on this branch; (5) cross-check that every enumerated commit maps to at least one bullet, planning-doc commits included (a `### Notes` bullet for the phase's planning artifacts is enough); (6) infer, never ask.
    Use this repo's section vocabulary: `### Added` (the composer disclosure and its State A / State B placement; the Open Positions footer qualifier; under D-06 (b), the excludes clause from plan 05's handoff), `### Changed` (the loop moved into `summarizeLiveHoldings`, values byte-identical; the corrected closed-sets prose and ROADMAP premise), `### Tests` (the unit and component pins, each proven RED under a named neuter, and plan 05's D-06 pins), `### Notes` (known limits and anything recorded rather than fixed). Fold plan 05's handoff bullets in verbatim where they fit, re-grouped by theme. ⚠️ Known limits to carry in `### Notes`: the D-06 answer as recorded (under (a): a `revoked` key's holdings stay absent from the book-mode composer AUM by founder decision, pinned by the D-06 tests; under (b): the D-20 residual — a revoked manager-side key may be counted in the excludes figure, over-disclosing and never hiding); `ExposureByClass` is not qualified (D-17); `mandate-gates.ts` `minAumGate` has no production caller and was not touched; the UI-SPEC long-text backstops (320px / 200% zoom) are owed as held-out visual checks, not measured; WINDOWS 66 closed.
    ⛔ No skip-trailer token anywhere in the entry or the commit message. ⛔ No credential-shaped literal, real id or real name.
    Commit `VERSION`, `package.json` and `CHANGELOG.md` together by explicit path as `chore(release): v<version> — AUMTRUST` (one commit). This is the last commit of the phase that touches any of the three files.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts --reporter=dot > /tmp/167.1-06-t2.log 2>&1; RC=$?; tail -15 /tmp/167.1-06-t2.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167.1-06-t2.log && test "$(cat VERSION)" = "$(node -p 'require("./package.json").version')" && head -5 CHANGELOG.md | grep -qF "## [$(cat VERSION)]" && echo OK</automated>
    <fails_when>no "OK" printed: critical-regressions exits non-zero or reports 0 passed ([CRITICAL-02] VERSION/package.json mismatch), VERSION differs from package.json, or the top CHANGELOG entry is not for the new version</fails_when>
    <automated>git log -1 --format=%B > /tmp/167.1-06-t2.msg && python3 -c "import sys; m=open('/tmp/167.1-06-t2.msg').read().lower(); bad=[t for t in ['[skip ci]','[ci skip]','[no ci]','[skip actions]','[actions skip]','skip-checks:true'] if t in m]; print('BAD' if bad else 'OK'); sys.exit(1 if bad else 0)"</automated>
    <fails_when>prints BAD or exits 1: the release commit message carries a CI skip token</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format=%s` starts with `chore(release): v` and `git show --stat HEAD` lists exactly `VERSION`, `package.json`, `CHANGELOG.md`.
    - `VERSION` matches `^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$`.
    - The SUMMARY records the commit COUNT from step 1 and states that every commit maps to a bullet (or names the one that does not and why).
    - The entry contains the string `D-06` together with the recorded option letter, and does not contain the phrase `D-06 is OPEN` (`grep -c 'D-06 is OPEN'` over the new entry prints 0).
  </acceptance_criteria>
  <done>One `chore(release)` commit — the phase's last change to CHANGELOG.md, VERSION and package.json — carries a byte-equal version bump and a unified, cross-checked CHANGELOG entry that records the founder's D-06 answer.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

| Symbol / id | Kind | Location |
|---|---|---|
| WINDOWS row 66 `fixed` | ledger state | `.planning/WINDOWS.md` |
| final byte-identity reading | recorded measurement | `167.1-06-SUMMARY.md` |
| `## [0.86.1.0]` (or the measured successor) | CHANGELOG entry | `CHANGELOG.md` |
| version bump | release | `VERSION`, `package.json` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branch -> `main` | a squash merge concatenates every branch commit message into the merge body |
| repo -> public | the repo and `.planning/` are public; CHANGELOG prose is scanned by gitleaks on push |
| founder -> release | the release must carry the founder's recorded D-06 answer, never an inferred one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.1-09 | Denial of Service (CI suppressed) | release commit message / CHANGELOG | high | mitigate | Task 2's second verify rejects any skip token in the commit message; the prose names "the skip trailer" instead. |
| T-167.1-11 | Tampering (ledger clobber) | `gsd-tools windows fixed` | medium | mitigate | Byte backups first; `git diff` the four ledgers after; revert only measured collateral with `cmp` verification. |
| T-167.1-20 | Information Disclosure | CHANGELOG / WINDOWS prose | high | mitigate | Synthetic ids only; no credential-shaped literal; `npm run check:planning-hygiene` after the `.planning/` write. |
| T-167.1-21 | Repudiation (a claim the phase changed money, or a release that hides a later change) | release notes / branch | medium | mitigate | Task 1's final branch-wide byte-identity gate runs AFTER plan 05; the release is the last plan, so no source or CHANGELOG change lands after it without a version bump. |
| T-167.1-22 | Spoofing (of the founder's D-06 answer) | CHANGELOG D-06 bullet | medium | mitigate | The option letter is copied from plan 05's handoff, which records the founder's own reply; the precondition stops the release when the handoff is absent or names (c). |
| T-167.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed. If a task appears to need one, STOP. |
</threat_model>

<verification>
- `npm run check:planning-hygiene` green (read the whole output, not `tail -1`).
- `npx vitest run src/__tests__/critical-regressions.test.ts --reporter=dot` green.
- `npm run lint` green.
- Branch-wide: `git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/` prints nothing.
</verification>

<success_criteria>
- WINDOWS 66 is fixed with no collateral, in the same plan as the release (D-14).
- One release commit, version byte-equal, CHANGELOG unified and cross-checked, D-06 recorded as the founder answered it — and no later commit on the branch edits the release files.
</success_criteria>

<output>
Create `.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-06-SUMMARY.md` when done
</output>
