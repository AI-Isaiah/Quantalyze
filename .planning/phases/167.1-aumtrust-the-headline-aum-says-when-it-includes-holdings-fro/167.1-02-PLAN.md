---
phase: 167.1-aumtrust
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx
  - src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx
autonomous: true
requirements: [D-16, D-17, D-03, D-04, D-07, D-09, D-11, D-15]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low   # DERIVED: estimate-calibration reports sample_count 0, factor 1
must_haves:
  truths:
    - "D-16: when at least one Open Positions row's source_key_sync_status satisfies isUntrustedKeySyncStatus, a sentence directly under 'Total unrealized P&L (equity contribution)' reads 'Includes <formatPnl amount> from keys needing attention.', with the noun from UNTRUSTED_KEY_SET_NOUN."
    - "D-03 applied to M2: the footer total keeps its current value — it still sums the untrusted rows — and equals trusted plus untrusted in a mixed table."
    - "D-04 applied to M2: the qualifier's amount is summed in the SAME single pass as totalUnrealized, over the same rows, with the same non-finite-to-0 rule — never a second reduce with a re-typed filter."
    - "D-07 applied to M2: the qualifier renders on untrusted row count >= 1, not on amount > 0 — an untrusted row with null P&L renders 'Includes +$0 from keys needing attention.'; a negative sum renders with U+2212 through formatPnl."
    - "UI-SPEC empty (covered, M2): an all-trusted table renders no open-positions-untrusted-note and its footer is byte-identical to today; a table with zero rows renders no footer (existing 'No open derivative positions.')."
    - "UI-SPEC tone (covered, M2) / D-09: the qualifier cell is px-4 pb-2 text-xs text-text-muted, sentence case (not inside the uppercase label cell), no role, no aria-live, no pnlColor, and does not match /warning|amber|danger|destructive|accent/i."
    - "UI-SPEC overflow (covered, M2): the qualifier is a colSpan={7} left-aligned cell inside the existing ResponsiveTable, which owns horizontal scrolling."
    - "UI-SPEC loading/error (covered, M2): the qualifier is a synchronous derivation over rows the table already receives — no fetch, no loading or error state."
    - "D-17: ExposureByClass (Gross/Net/Long/Short) is untouched by this phase — exposure figures, not AUM, and qualifying them would need a new projection D-11 rules out."
    - statement: "M2 qualifier stays readable inside ResponsiveTable at 320px"
      verification: backstop
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx"
      provides: "one-pass footer accumulator (totalUnrealized, untrustedUnrealized, untrustedCount) and the open-positions-untrusted-note row"
    - path: "src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx"
      provides: "describe '[167.1] AUMTRUST — OpenPositionsTable footer qualifier'"
  key_links:
    - from: "OpenPositionRow.source_key_sync_status"
      to: "open-positions-untrusted-note"
      via: "the shared predicate (untrustedKeyChipLabel !== null or isUntrustedKeySyncStatus) inside the one footer pass"
---

<objective>
Apply the founder's "keep the total and flag it" rule to the second holdings-derived dollar total
research found D-01 had missed: the Holdings tab's Open Positions footer "Total unrealized P&L
(equity contribution)", which sums every derivative row including the rows the same table strikes
through with an untrusted-key chip (D-16, amendment 2026-09-23).

Purpose: the rows are honest today; the footer is not qualified. This plan adds one muted footer
row naming the untrusted part and changes no number.

Output: one single-pass footer accumulator, one `<tfoot>` row per UI-SPEC § 2, and tests each
proven able to fail. Independent of plan 01 (file-disjoint, same wave).

Spec-less probe fallback: skipped — no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
@DESIGN.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-CONTEXT.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-PATTERNS.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-UI-SPEC.md
@src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx
@src/lib/closed-sets.ts
</context>

<repo_guardrails>
⛔ Work only in the `feat/167.1-aumtrust` worktree; stage by explicit path.
⛔ Cite by SYMBOL, never by line number.
⛔ Restore only from a `cp` byte backup verified with `cmp`, re-taken after every edit. Never
`git checkout -- <path>`, `git restore` or `git stash`.
⛔ Synthetic identifiers only in fixtures. ⛔ No CI skip trailer in any commit message.
⛔ Never `npm install`. ⛔ No database command, no Python.
⚠️ `grep -c` exits 1 on zero — wrap as `$(grep -c … || true)`.
⚠️ `HoldingsTable.tsx` is the lockstep PRECEDENT only and is NOT modified. `ExposureByClass` is
OUT of scope (D-17) — do not touch `widgets/positions/ExposureByClass.tsx` or
`src/lib/portfolio-exposure.ts`.
</repo_guardrails>

<interfaces>
Measured at worktree HEAD:
- `OpenPositionsTable.tsx` imports `untrustedKeyChipLabel` from `@/lib/closed-sets` (with a
  comment forbidding a local status equality); has file-local `formatUsd` (unsigned) and
  `formatPnl` (`+` for n >= 0, U+2212 for n < 0, null gives an em dash) and `pnlColor`.
- `OpenPositionsTable({ rows })` computes `totalUnrealized` with one `rows.reduce` using the
  rule "finite `unrealized_pnl_usd` else 0", and renders a single-row `<tfoot>` whose label cell
  spans 6 columns and whose value cell renders `formatPnl(totalUnrealized)`.
- `untrusted-key-status.surfaces.test.tsx` (jsdom) has `makePosition(over)` (default
  `unrealized_pnl_usd: 1_500`, `source_key_sync_status: "complete"`), `UNTRUSTED = ["revoked",
  "sign_in_failed"] as const`, `CHIP_TEXT`, and `describe("[D-16] OpenPositionsTable — the
  derivative surface answers the predicate")`.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a sign_in_failed position's P&L is still in the footer and the footer says so" — one path</name>
  <files>src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx, src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx</files>
  <read_first>
    - `OpenPositionsTable.tsx` whole (it is small): `totalUnrealized`, `<tfoot>`, `formatPnl`, the `@/lib/closed-sets` import and its comment.
    - `untrusted-key-status.surfaces.test.tsx`: the file docblock, `makePosition`, `UNTRUSTED`, and the `[D-16] OpenPositionsTable` describe.
    - `HoldingsTable.tsx`: grep `D-16 sites 1 and 2` (the lockstep-comment precedent and the noun-bearing muted footer sentence) — read only, do not modify.
    - 167.1-UI-SPEC.md § Copywriting Contract "2. M2" (markup, class, exact textContent table).
  </read_first>
  <behavior>
    - Two rows: a trusted row (P&L 1,000) and a `sign_in_failed` row (P&L 300). The footer total cell reads `+$1,300` (the untrusted row is still counted, D-03), and `open-positions-untrusted-note` reads exactly `Includes +$300 from keys needing attention.` (typed literal, D-16).
  </behavior>
  <action>
    Write the failing test first in a new `describe("[167.1] AUMTRUST — OpenPositionsTable footer qualifier")` in the surfaces test file, reusing `makePosition`.
    In `OpenPositionsTable.tsx`, replace the bare `rows.reduce` with ONE pass over `rows` that yields `totalUnrealized` (same value, same finite-else-0 rule), `untrustedUnrealized` and `untrustedCount` — a single `for` loop or a single reduce over an object accumulator. The untrusted test is the shared predicate: reuse `untrustedKeyChipLabel(r.source_key_sync_status) !== null` (the row's own rule) or add `isUntrustedKeySyncStatus` to the existing `@/lib/closed-sets` import. ⛔ No local equality on the status column. Add `UNTRUSTED_KEY_SET_NOUN` to the same import statement and never restate the noun (D-02 construction rule). Put a comment on the pass in the `HoldingsTable` lockstep voice: the total and its untrusted part come from one derivation so they cannot drift (D-04), and it discloses, never subtracts (D-03).
    Inside the existing `<tfoot>`, AFTER the total row, render iff `untrustedCount > 0` one `<tr className="bg-page/40">` (no `border-t`) holding one `<td colSpan={7} data-testid="open-positions-untrusted-note" className="px-4 pb-2 text-xs text-text-muted">` whose text is `Includes `, `formatPnl(untrustedUnrealized)`, ` from `, the noun constant, and a full stop. Use `formatPnl` — the renderer of the total it qualifies — not the file's `formatUsd` and not `@/lib/dollar-validation` (UI-SPEC U-05). No `pnlColor`, no role, no uppercase (UI-SPEC U-06, U-09).
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx' --reporter=dot -t "167.1" > /tmp/167.1-02-t1.log 2>&1; RC=$?; tail -20 /tmp/167.1-02-t1.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167.1-02-t1.log && grep -qE 'Tests +[0-9]+ passed' /tmp/167.1-02-t1.log && echo OK</automated>
    <fails_when>non-zero exit, "0 passed" in the summary, or no "Tests N passed" line (the -t filter matched nothing)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-testid="open-positions-untrusted-note"' 'src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx'` returns 1.
    - `grep -c '"keys needing attention"' 'src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx'` returns 0; `grep -c 'UNTRUSTED_KEY_SET_NOUN' 'src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx'` is at least 2 (import and use).
    - `grep -cE 'rows\.reduce\(' 'src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx'` is at most 1 and `grep -cE 'rows\.filter\(' 'src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx'` returns 0 (one pass, no re-typed filter).
    - `grep -cE '=== *"(revoked|sign_in_failed)"' 'src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx'` returns 0.
    - The tracer test was observed RED before the implementation and GREEN after.
  </acceptance_criteria>
  <done>The footer total is unchanged and, when an untrusted row is summed, a muted sentence under it names the untrusted P&L; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expansion — every status, the absence and edge cases, and the neighbouring suites stay green</name>
  <files>src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx</files>
  <read_first>
    - `OpenPositionsTable.tsx` (Task 1 output).
    - The `[167.1]` describe from Task 1.
    - `src/app/(dashboard)/allocations/components/OpenPositionsTable.all-columns.test.tsx`, `src/app/(dashboard)/allocations/HoldingsTabPanel.spot-derivative-split.test.tsx`, `src/app/(dashboard)/allocations/HoldingsTabPanel.exposure.test.tsx`: grep for `tfoot`, `Total unrealized`, `getAllByRole("row")` or row counts that a new footer row could move.
  </read_first>
  <behavior>
    - `it.each(UNTRUSTED)`: each untrusted status on a single row renders the qualifier with the typed literal for the default P&L (`Includes +$1,500 from keys needing attention.`).
    - Healthy control: an all-`complete` table renders no `open-positions-untrusted-note`, and the `<tfoot>` holds exactly one `<tr>`.
    - Mixed table (two trusted, two untrusted with distinct statuses): the total cell equals the hand-typed trusted + untrusted sum; the qualifier equals the hand-typed untrusted sum only.
    - Negative: an untrusted row with P&L -1,234.6 renders `Includes −$1,235 from keys needing attention.` (U+2212).
    - Null P&L: an untrusted row with `unrealized_pnl_usd: null` renders `Includes +$0 from keys needing attention.` (count, not amount — D-07).
    - Tone: the cell's class contains `text-text-muted` and `text-xs`, does not match `/warning|amber|danger|destructive|accent|uppercase/i`, and the cell has no `role`.
  </behavior>
  <action>
    Add the `<behavior>` cases to the `[167.1]` describe. Type every expected string literally (never import the noun or call the formatter in an oracle). Name the U+2212 character in a comment beside its literal so a reader does not "fix" it to a hyphen.
    If a neighbouring suite counts footer rows or table rows and reds on the new `<tr>`, read the failure, and adjust only an assertion that counted rows without meaning to pin the footer's shape; record each such adjustment and its reason in the SUMMARY. ⛔ Never weaken an assertion that pins the total's value.
    Prove the pins can fail on a `cp` byte backup of `OpenPositionsTable.tsx`, `cmp`-verified after each restore: (1) exclude untrusted rows from `totalUnrealized` → the tracer and mixed-table total RED (D-03); (2) render on `untrustedUnrealized > 0` → the null-P&L case RED (D-07); (3) render the amount with the file's unsigned `formatUsd` → the sign literals RED; (4) render the row unconditionally → the healthy control RED. Paste each RED line into the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx' 'src/app/(dashboard)/allocations/components/OpenPositionsTable.all-columns.test.tsx' 'src/app/(dashboard)/allocations/HoldingsTabPanel.spot-derivative-split.test.tsx' 'src/app/(dashboard)/allocations/HoldingsTabPanel.exposure.test.tsx' --reporter=dot > /tmp/167.1-02-t2.log 2>&1; RC=$?; tail -25 /tmp/167.1-02-t2.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167.1-02-t2.log && grep -qE 'Tests +[0-9]+ passed' /tmp/167.1-02-t2.log && echo OK</automated>
    <fails_when>non-zero exit, any failed test in the four files, "0 passed", or no "Tests N passed" line</fails_when>
  </verify>
  <acceptance_criteria>
    - The `[167.1]` describe carries at least six cases (`it.each` counted per status): verified from the reporter's pass count for `-t "167.1"` being at least 7.
    - The file contains the U+2212 literal in an oracle: `grep -c '−\$1,235' 'src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx'` is at least 1.
    - `git diff --stat "$(git merge-base HEAD origin/main)" -- 'src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx' src/lib/portfolio-exposure.ts 'src/app/(dashboard)/allocations/components/HoldingsTable.tsx'` prints nothing (D-17, precedent untouched).
    - The SUMMARY records four neuter → RED → restore drills with pasted RED lines and `cmp` verdicts, and names any neighbouring-suite assertion that was adjusted (or states none was).
  </acceptance_criteria>
  <done>M2 is pinned across both untrusted statuses, the healthy control, mixed, negative and null cases; neighbouring suites are green.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

| Symbol / id | Kind | Location |
|---|---|---|
| `untrustedUnrealized`, `untrustedCount` | locals of the single footer pass (beside `totalUnrealized`) | `OpenPositionsTable.tsx` |
| `open-positions-untrusted-note` | test id on the new `<tfoot>` cell | `OpenPositionsTable.tsx` |
| `describe("[167.1] AUMTRUST — OpenPositionsTable footer qualifier")` | test block | `untrusted-key-status.surfaces.test.tsx` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR holdings -> Holdings tab | `OpenPositionRow.source_key_sync_status` is already on each row from the tenant-scoped holdings read |
| repo -> public | the repo and `.planning/` are public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.1-06 | Tampering / integrity of a displayed money figure | `totalUnrealized` rewrite | high | mitigate | The one pass keeps the same finite-else-0 rule over the same rows; the mixed-table and tracer tests fail if an untrusted row leaves the total (neuter 1). |
| T-167.1-07 | Information Disclosure | `open-positions-untrusted-note` copy | low | mitigate | Only a signed dollar sum and the constant noun; no venue, symbol, key id or `sync_error` text interpolated. |
| T-167.1-08 | Tampering (XSS) | footer cell | low | mitigate | React text nodes only; no `dangerouslySetInnerHTML`. |
| T-167.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed (RESEARCH Package Legitimacy Audit: not applicable). If a task appears to need one, STOP. |
</threat_model>

<verification>
- The four-file vitest run in Task 2 green.
- `npm run typecheck` clean.
- `npx eslint 'src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx' 'src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx'` clean.
</verification>

<success_criteria>
- The Open Positions footer total is unchanged for every table; a table with at least one untrusted row carries exactly one muted sentence naming the untrusted P&L with the total's own sign convention.
- Four neuter drills observed RED and restored under `cmp`.
</success_criteria>

<output>
Create `.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-02-SUMMARY.md` when done
</output>
