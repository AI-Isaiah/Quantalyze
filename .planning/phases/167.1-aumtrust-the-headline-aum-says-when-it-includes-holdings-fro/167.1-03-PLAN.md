---
phase: 167.1-aumtrust
plan: 03
type: execute
wave: 2
depends_on: [167.1-01]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [D-07, D-08, D-09, D-15, D-18]
estimate:
  tokens: 65000     # split out of plan 01 (its former Task 3) on 2026-09-23, checker scope_sanity
  raw_tokens: 65000
  tasks: 2
  confidence: low   # DERIVED: estimate-calibration reports sample_count 0, factor 1
must_haves:
  truths:
    - "D-18 (supersedes D-08's placement sentence) / D-08: exactly one scenario-aum-untrusted-note element exists whenever the live total is on screen — State A beside the field when no manual value is committed and the live total is > 0; State B nested inside scenario-aum-override-note when a committed manual value differs from a live total > 0; absent when the field is blank (live <= 0), when manual equals live, and when manual is set with live <= 0."
    - "UI-SPEC § 1 State B: the override note's textContent is exactly 'Overrides live-holdings total $50,000, which includes $12,345 from keys needing attention.' and the nested marker reads 'includes $12,345 from keys needing attention', built by the same buildUntrustedAumClause State A uses."
    - "UI-SPEC regression (covered): with no untrusted holding, scenario-aum-override-note's textContent is byte-identical to today ('Overrides live-holdings total $X.'), and every pre-existing override-note, F2 and AUM-01 test stays green unmodified."
    - "D-07 / D-15 pin 3 (component): the marker renders when at least one summed holding is untrusted (count >= 1, not amount > 0) — a lone untrusted derivative with negative unrealized P&L renders 'Includes -$1,235 from keys needing attention.'; an all-trusted book and blank mode render no scenario-aum-untrusted-note element at all — never a '$0' claim when no untrusted holding exists."
    - "D-15 pin 4 (component): a sign_in_failed key that is allocator-eligible but not contributing adds nothing to the field and renders no marker."
    - "D-06 / D-15 pin 6, component half (the pin only — the decision is plan 05's): a revoked key's holding outside the eligible and contributing sets is absent from the field's value and no 'excludes' copy renders anywhere in the composer — pinned by a component test whose title starts with 'D-06'."
    - "D-09 / UI-SPEC U-01: the marker is text-xs text-text-muted with no role, no aria-live, no glyph and no sign colour; its class (State A) or its override note's class (State B) does not match /warning|amber|danger|destructive|accent/i."
    - "Placement follows the COMMITTED manual value (sanitizedManualAum), never the per-keystroke input text (RESEARCH Pitfall 3)."
    - statement: "M1 State B (and, under D-06 option (b), with M3) wraps without clipping at 320px and at 200% zoom"
      verification: backstop
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "State B: the override note composes buildUntrustedAumClause into a nested scenario-aum-untrusted-note span; State A and State B mutually exclusive"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
      provides: "the AUMTRUST describe extended with State B, every absence state, D-07, pin 4, tone and the component D-06 pin"
  key_links:
    - from: "overrideNoteShowsLive && showUntrustedMarker"
      to: "scenario-aum-override-note (State B nested span)"
      via: "buildUntrustedAumClause, the one builder State A also reads"
    - from: "sanitizedManualAum (committed)"
      to: "State A vs State B placement"
      via: "fieldShowsLive / overrideNoteShowsLive, hoisted in plan 01"
---

<objective>
Finish the composer marker plan 01 started: render State B inside the override note, and pin every
UI-SPEC § 1 state — including every state where the marker must be ABSENT — with tests each
proven able to fail (D-07, D-08, D-15, D-18).

Purpose: plan 01 wired the helper, the gate and State A end to end. A committed manual value that
differs from the live total moves the live total into the override note, so the disclosure has to
move with it (D-18), and "never two markers for one number" (D-08) has to be proven, not assumed.

Output: the State B JSX, and the rest of the AUMTRUST describe. This plan is the former Task 3 of
plan 01, split out by the 2026-09-23 revision (checker scope_sanity: plan 01 estimated 130000
tokens against a 100000 budget). It runs after plan 01 on the same two files, so it is wave 2.

Spec-less probe fallback: skipped — no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@DESIGN.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-CONTEXT.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-RESEARCH.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-UI-SPEC.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-01-SUMMARY.md
</context>

<repo_guardrails>
⛔ **Work only in the `feat/167.1-aumtrust` worktree.** Stage files by explicit path; never
`git add -A` / `git add .`.
⛔ **Cite by SYMBOL, never by line number.** `ScenarioComposer.tsx` is ~7,800 lines and its test
file ~15,400. Re-locate every anchor with `grep -n '<symbol>'` and read a bounded window; never
read either file whole.
⛔ **Restore only from a `cp` byte backup verified with `cmp`, re-taken after every edit.** Never
`git checkout -- <path>`, `git restore` or `git stash`.
⛔ **Synthetic identifiers only** in fixtures (`aumtrust-key-*`, placeholder venues and symbols).
The repo and `.planning/` are public.
⛔ **Never write a CI skip trailer in a commit message**, not even to deny it.
⛔ Never `npm install`. No database command, no Python, no `uvicorn` (D-11).
⚠️ `grep -c` exits 1 on a zero count — wrap as `$(grep -c … || true)`. ⚠️ Quote the
parenthesised paths for zsh.
</repo_guardrails>

<interfaces>
Produced by plan 01 (read its SUMMARY; re-locate by symbol):

- `ScenarioComposer.tsx`: `liveHoldingsSummary` memo, `liveHoldingsSum = liveHoldingsSummary.total`,
  hoisted `fieldShowsLive` / `overrideNoteShowsLive` / `showUntrustedMarker`, module-level
  `buildUntrustedAumClause(includedAmount)`, and the State A `scenario-aum-untrusted-note` span.
  The override note's JSX condition already reads `overrideNoteShowsLive`; its text is unchanged.
- `ScenarioComposer.test.tsx`: `describe("ScenarioComposer — AUMTRUST (Phase 167.1)")` with its own
  payload builder, fixture self-proof and the tracer case.
- ⛔ Distinct-triple fixture rule (from plan 01): `holdingByRef` and the SSR dedup key on
  `buildHoldingRef({ venue, symbol, holding_type })`, not on the key. Two fixture holdings sharing a
  triple collapse to one row and one silently vanishes from the sum. Every holding added in this
  plan gets a distinct triple, and each new fixture passes the AUMTRUST describe's self-proof (set
  of `buildHoldingRef` values has the same size as the holdings array).
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a committed override still says the live total it overrides includes untrusted dollars" — State B only</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - `ScenarioComposer.tsx`: the AUM row JSX (grep `scenario-aum-override-note`), `buildUntrustedAumClause`, `showUntrustedMarker`, `fieldShowsLive`, `overrideNoteShowsLive` (plan 01 output).
    - 167.1-UI-SPEC.md § 1 (the seven-state table, State B markup and exact textContent) and § UI Considerations.
    - `ScenarioComposer.test.tsx`: grep every `scenario-aum-override-note` site (about 5) — they must stay green unmodified; and the AUMTRUST describe plan 01 added.
    - 167.1-RESEARCH.md § Common Pitfalls 2 and 3.
  </read_first>
  <behavior>
    - State B: after committing a manual value (change + blur) that differs from a live total of 50,000 with 12,345 untrusted, `scenario-aum-override-note` textContent is exactly `Overrides live-holdings total $50,000, which includes $12,345 from keys needing attention.`, the nested `scenario-aum-untrusted-note` reads `includes $12,345 from keys needing attention`, and `getAllByTestId("scenario-aum-untrusted-note").length === 1` (D-08, D-18).
    - All-trusted book with an override: the note is byte-identical to `Overrides live-holdings total $50,000.` and no marker exists.
    - Placement follows committed state: typing into the field without blur does not move or remove the marker.
  </behavior>
  <action>
    Write the failing tests first (RED), then implement.
    Implement State B in the override note exactly as UI-SPEC § 1 specifies: when `showUntrustedMarker && overrideNoteShowsLive`, the note renders `Overrides live-holdings total {formatUsd(liveHoldingsSum)}`, then `, which ` and an unclassed `<span data-testid="scenario-aum-untrusted-note">` carrying the clause from `buildUntrustedAumClause`, then the full stop. With no marker the note's textContent must be byte-identical to today — no space before the full stop; check JSX whitespace carefully. State A (plan 01) and State B must be mutually exclusive through `fieldShowsLive` / `overrideNoteShowsLive`, so one number never gets two markers (D-08, D-18). Placement keys on `sanitizedManualAum` (committed), never on the per-keystroke input text (Pitfall 3).
    Add the `<behavior>` cases to the AUMTRUST describe. Type every expected string literally (the file's own oracle-independence rule); never import the noun or call the formatter in an oracle. Fixtures follow the distinct-triple rule in `<interfaces>`.
    Prove the one-marker gate can fail, on a `cp` byte backup of `ScenarioComposer.tsx`, `cmp`-verified after restore: render State A regardless of the override → the "exactly one marker" case RED (D-08). Paste the RED line into the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx' --reporter=dot -t "AUMTRUST|partial-book dollars|AUM-01|override" > /tmp/167.1-03-t1.log 2>&1; RC=$?; tail -25 /tmp/167.1-03-t1.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167.1-03-t1.log && grep -qE 'Tests +[0-9]+ passed' /tmp/167.1-03-t1.log && echo OK</automated>
    <fails_when>non-zero exit, "0 passed" in the summary, or no "Tests N passed" line (the -t filter matched nothing)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-testid="scenario-aum-untrusted-note"' 'src/app/(dashboard)/allocations/components/ScenarioComposer.tsx'` returns 2 (State A and State B sites, mutually exclusive by gate).
    - `grep -c 'overrideNoteShowsLive' 'src/app/(dashboard)/allocations/components/ScenarioComposer.tsx'` is at least 3 (definition, the note's condition, the State B branch).
    - `git diff --quiet "$(git merge-base HEAD origin/main)" -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts` exits 0 (D-03, D-10: SSR and route math untouched).
    - The one-marker drill was observed RED and restored under `cmp`.
  </acceptance_criteria>
  <done>A committed override that differs from the live total carries the disclosure inside the override note, one marker only; with no untrusted holding the note is byte-identical to today; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Every absence state, the count gate, pin 4, tone and the component D-06 pin (D-07, D-09, D-15, D-18)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - The AUMTRUST describe after Task 1; `ScenarioComposer.tsx` `showUntrustedMarker` and the State A / State B sites.
    - `ScenarioComposer.test.tsx`: the F2 describe's modelled-note tone assertions and its blank-mode toggle (`getByRole("radio", { name: /Blank slate/i })`).
    - 167.1-UI-SPEC.md § 1 seven-state table (states 1, 2, 4, 6, 7 are the absence states).
  </read_first>
  <behavior>
    - Absent: blank mode; all-trusted book; field blank because the live total is <= 0 while an untrusted holding exists; manual equal to live; manual set with live <= 0 (UI-SPEC states 1, 2, 4, 6, 7).
    - D-07: a book whose only untrusted holding is a derivative with negative unrealized P&L (trusted spot keeping the live total > 0) renders `Includes -$1,235 from keys needing attention.` (hyphen-minus, from `formatUsd`).
    - D-15 pin 4 (component): a sign_in_failed key that is allocator-eligible but not contributing adds nothing to the field and renders no marker.
    - Tone: State A class contains `text-text-muted`, does not match `/warning|amber|danger|destructive|accent/i`, has no `role` and no `aria-live`; in State B assert the class through `closest('[data-testid="scenario-aum-override-note"]')`.
    - D-06 pin, titled starting "D-06": a revoked key with a holding, outside eligible and contributing sets, is absent from the field's value, and `queryByText(/excludes/i)` is null — "a revoked key's holdings are silently absent today; plan 05's founder decision must edit this test deliberately".
  </behavior>
  <action>
    These cases pin behaviour plan 01 and Task 1 already built, so each is expected GREEN on first run; the RED evidence comes from the neuter drills below, and a case that cannot be made RED by its drill is a defect to fix before commit (D-15). If a case is RED on first run, that is a real gap in the gate: fix the source (only `showUntrustedMarker`, the two hoisted booleans or the State A/B sites), never the oracle.
    Add the `<behavior>` cases to the AUMTRUST describe. Type every expected string literally; never import the noun or call the formatter in an oracle. The D-06 test's comment names CONTEXT D-06 as OPEN, names plan 05 as the place it is decided, and says which option (b) would change it. Fixtures follow the distinct-triple rule in `<interfaces>` — the D-07 derivative in particular needs its own `venue`/`symbol` so its `buildHoldingRef` differs from the trusted spot holding's.
    Prove the gates can fail, one at a time, on a `cp` byte backup of `ScenarioComposer.tsx`, `cmp`-verified after each restore: (1) gate the marker on `untrusted.amount > 0` instead of the count → the negative-derivative case RED (D-07); (2) drop `fieldShowsLive || overrideNoteShowsLive` from `showUntrustedMarker` → the blank-field case RED (D-18); (3) render the marker when the count is 0 → the all-trusted case RED (pin 3). Paste each RED line into the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx' --reporter=dot > /tmp/167.1-03-t2.log 2>&1; RC=$?; tail -25 /tmp/167.1-03-t2.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167.1-03-t2.log && grep -qE 'Tests +[0-9]+ passed' /tmp/167.1-03-t2.log && echo OK</automated>
    <fails_when>non-zero exit, any failed test in the whole composer file (a regression in an existing override-note, F2 or AUM-01 test), "0 passed", or no "Tests N passed" line</fails_when>
    <automated>npm run typecheck > /tmp/167.1-03-t2-tsc.log 2>&1; RC=$?; tail -20 /tmp/167.1-03-t2-tsc.log; test "$RC" = 0 && echo OK</automated>
    <fails_when>non-zero exit, or any "error TS" line in the log</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE '(it|test)\("D-06' 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx'` is at least 1.
    - The whole composer test file is green, and every pre-existing `scenario-aum-override-note` assertion passes without edits (`git diff "$(git merge-base HEAD origin/main)" -- 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx' > /tmp/167.1-03-t2.diff` exits 0, then `grep -c '^-[^-]' /tmp/167.1-03-t2.diff` prints 0: only additions, inside the AUMTRUST describe).
    - The SUMMARY records three neuter → RED → restore drills (plus Task 1's one) with pasted RED lines and `cmp` verdicts.
  </acceptance_criteria>
  <done>Every UI-SPEC § 1 state renders exactly as specified, the D-06 behaviour is pinned by name in the component suite, and the whole composer suite is green.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

| Symbol / id | Kind | Location |
|---|---|---|
| State B nested `scenario-aum-untrusted-note` | test id (span inside `scenario-aum-override-note`) | `ScenarioComposer.tsx` |
| AUMTRUST absence / D-07 / pin 4 / tone / `D-06` cases | component tests | `ScenarioComposer.test.tsx` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR payload -> client composer | `payload.apiKeys` and `holdingsSummary` arrive already tenant-scoped; this plan only changes where an existing derived string renders |
| repo -> public | the repo and `.planning/` are public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.1-17 | Tampering / integrity of a displayed money figure | override-note rewrite | high | mitigate | The note's number stays `formatUsd(liveHoldingsSum)`; with no untrusted holding its textContent is byte-identical (regression case plus the unmodified pre-existing override-note tests); `queries.ts` and the commit route stay byte-unchanged (Task 1 acceptance). |
| T-167.1-18 | Repudiation (a figure presented as current when part is not) | State B | medium | mitigate | The disclosure follows the live total into the override note (D-18), pinned by the State B case and the one-marker drill. |
| T-167.1-19 | Tampering (XSS) | State B span | low | mitigate | React text nodes only; no `dangerouslySetInnerHTML`; only a dollar sum and the constant noun reach the element. |
| T-167.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed. If a task appears to need one, STOP. |
</threat_model>

<verification>
- `npx vitest run 'src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts' 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx' --reporter=dot` green.
- `npm run typecheck` clean.
- `npx eslint 'src/app/(dashboard)/allocations/components/ScenarioComposer.tsx'` clean.
- `git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/` prints nothing (D-03, D-10, D-11).
</verification>

<success_criteria>
- A book with at least one untrusted summed holding shows exactly one muted disclosure naming the untrusted dollars, in whichever element shows the live total; every absence state renders no marker.
- Four component neuter drills were each observed RED and restored under `cmp`.
</success_criteria>

<output>
Create `.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-03-SUMMARY.md` when done
</output>
