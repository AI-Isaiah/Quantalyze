---
phase: 167.1-aumtrust
plan: 05
subsystem: ui
tags: [react, vitest, scenario-composer, aum, disclosure, d-06, d-18]
status: complete

requires:
  - phase: 167.1 (plans 01-04)
    provides: summarizeLiveHoldings with excludedUntrusted and the manager-side set, buildKeyTrustClause, State A / State B, the D-06 pins
provides:
  - "D-06 (b): the composer's one AUM marker says 'excludes $Y from keys needing attention' (UI-SPEC section 3), gated on the excluded COUNT"
  - "D-18 reopened: the marker shows in state 6 (State A) and state 4 (new State C inside the 'Required to size and commit.' hint)"
  - "buildKeyTrustClause's optional excluded part, byte-identical output for callers that pass none (Open Positions)"
affects: [167.1-06 release, CHANGELOG]

actuals:
  tokens: 11400      # chars/4 over the realized diff 5d62a1a60..0c27a029e (45,649 chars)
  tasks: 3
  commits: 2         # MEASURED: git rev-list --count 5d62a1a60..HEAD at SUMMARY write
plan_head_before: 5d62a1a60ffe8bee78d98b6c76a11aa40a4b316a

tech-stack:
  added: []
  patterns:
    - "One clause builder, with the shared noun said once only when both sides are plain amounts of the one set"
    - "A disclosure that names the figure it qualifies when that figure is not otherwise on screen (State C)"

key-files:
  created: []
  modified:
    - src/app/(dashboard)/allocations/lib/live-holdings-summary.ts
    - src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
    - .planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-CONTEXT.md
    - .planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-UI-SPEC.md
    - .planning/ROADMAP.md

key-decisions:
  - "D-06 = (b), the founder's answer of 2026-09-24 (AskUserQuestion via the orchestrator, first recorded in 5d62a1a60). It is not an executor inference."
  - "D-18 REOPENED by the founder the same day. This is a founder-directed scope addition to this plan: state 6 shows State A, state 4 shows a new State C in the hint, and state 7 stays absent."
  - "State C copy: 'Required to size and commit. The live-holdings total is -$X, which <clause>.' The blank field has no number of its own, so the hint names the live total and the clause qualifies that, which is the State B construction."
  - "The noun is said once ('includes $X and excludes $Y from keys needing attention') only when both sides are plain amounts of the untrusted set. With an unknown-status part or an '(… unavailable …)' count, each side keeps its own noun."

patterns-established:
  - "A marker site added under a new placement gate must carry that gate itself (STATE D-167.1-03-A). State C renders inside `hintShowsLive &&`."

requirements-completed: [D-06, D-20]

coverage:
  - id: D1
    description: "M3 excludes clause, all four UI-SPEC section 3 rows (State A and State B, excludes-only and both), one marker each, the field unchanged"
    requirement: D-06
    verification:
      - kind: unit
        ref: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx#D-06 (b) M3"
        status: pass
      - kind: unit
        ref: "src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts#buildKeyTrustClause — D-06 (b)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The excludes gate is the excluded COUNT: a counted $0 renders 'Excludes $0 from keys needing attention.'"
    requirement: D-20
    verification:
      - kind: unit
        ref: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx#D-06 (b) count gate"
        status: pass
    human_judgment: false
  - id: D3
    description: "D-18 reopen: state 6 carries State A and state 4 carries State C in the hint. The plain hint is byte-identical, and state 7 and blank mode stay absent."
    verification:
      - kind: unit
        ref: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx#AUMTRUST state 4 / state 6"
        status: pass
    human_judgment: false
  - id: D4
    description: "State C and M3 wrap readably at 320px and 200% zoom (UI-SPEC long-text backstop)"
    verification: []
    human_judgment: true
    rationale: "Visual wrapping is not asserted by jsdom. It is the UI-SPEC's held-out backstop, which grows with the longer State C and M3 strings."

duration: 7min
completed: 2026-09-24
---

# Phase 167.1 Plan 05: D-06 (b) and the D-18 reopen Summary

**The composer's one AUM marker now says what the modelled-book narrowing leaves out ("Excludes $8,000 from keys needing attention.", "Includes $12,345 and excludes $8,000 from keys needing attention."). It also stays on screen when the manual value equals the live total, and when the live total is at or below zero, where the "Required to size and commit." hint now names that total. No total moved.**

## Performance

- **Duration:** about 7 min
- **Started:** 2026-09-24T05:25:46Z
- **Completed:** 2026-09-24T05:32:19Z
- **Tasks:** 3 (Task 1 was the checkpoint, answered by the founder)
- **Files modified:** 7

## The checkpoint (Task 1): the founder's answer and where it comes from

Task 1 is a `checkpoint:decision` with `gate="blocking-human"`. No agent answered it. The founder
answered it on 2026-09-24 through the orchestrator's AskUserQuestion, and the orchestrator recorded
the answers in CONTEXT and the ROADMAP in commit `5d62a1a60` before this plan was dispatched:

1. **D-06 = option (b):** add "excludes $Y from keys needing attention", with the precision D-20
   gives. It is additive and reversible.
2. **D-18 REOPENED ("Reopen, show the marker"):** whenever the figure on screen includes untrusted
   dollars, the marker shows. That covers (i) a live total ≤ 0 (review WR-04, UI-SPEC state 4) and
   (ii) a manual AUM exactly equal to the live total (review IN-06). **This is a founder-directed
   scope addition to plan 05**, which as planned covered D-06 only.

## Accomplishments

- `buildKeyTrustClause` takes an optional excluded part and composes the UI-SPEC section 3 table:
  includes-only, excludes-only, and both with the noun said once. Open Positions passes no excluded
  part, so its output is byte-identical to before.
- The composer's `buildUntrustedAumClause` passes the whole summary. `showUntrustedMarker` counts
  `excludedUntrusted.count` as well, keeping the book-mode conjunct and the placement conjuncts.
- D-18 reopen:
  - `fieldShowsLive` now also covers manual equal to live (state 6 → State A).
  - A new `hintShowsLive` covers state 4. The hint (now `scenario-aum-required-note`) gains
    "The live-holdings total is -$X, which <clause>." with the one nested marker.
  - `aria-describedby` points at the hint in State C only.
  - State 7 stays absent.
- The totals are byte-identical. `src/lib/queries.ts`, the commit route, `supabase/` and
  `analytics-service/` show no diff against the merge base.

## Task Commits

1. **Task 1: D-06 checkpoint.** Answered by the founder and recorded in `5d62a1a60`, which predates
   this plan. This plan made no commit for it.
2. **Task 2: (b) end to end, plus the D-18 reopen:** `943e72d9f` (feat)
3. **Task 3: record the answer in CONTEXT, UI-SPEC and ROADMAP:** `0c27a029e` (docs)

## Files Created/Modified

- `live-holdings-summary.ts`: the optional excluded part on `buildKeyTrustClause`. The
  `excludedUntrusted` doc now says it is rendered (D-06 resolved).
- `live-holdings-summary.test.ts`: six `D-06 (b)` builder cases.
- `ScenarioComposer.tsx`: the widened gate, `hintShowsLive`, State C, and the builder wrapper
  taking the summary.
- `ScenarioComposer.test.tsx`: four existing pins flipped deliberately. Seven cases are new, and
  the state 4 case was replaced by two.
- `167.1-CONTEXT.md`: the RESOLVED line under D-06, an implemented note under the D-18 reopen,
  and WR-04 / IN-06 struck from `<known_limits>` with a dated note (lineage kept).
- `167.1-UI-SPEC.md`: dated rows for states 4 and 6, the State C copy, U-04 marked superseded,
  section 3 marked shipped, and the composition rows.
- `.planning/ROADMAP.md`: one dated sentence and the plan 05 checkbox.

## Tests: RED against the old code, and the neuter drills

**Every new or flipped test was observed RED before the change** (run with filter
`AUMTRUST|D-06|D-20`): `Tests  11 failed | 20 passed | 335 skipped (366)`. Representative lines:

```
AssertionError: expected 'Includes $12,345 from keys needing at…' to be 'Includes $12,345 and excludes $8,000 …'
AssertionError: expected 'Overrides live-holdings total $50,000.' to be 'Overrides live-holdings total $50,000…'
TestingLibraryElementError: Unable to find an element by: [data-testid="scenario-aum-required-note"]   (x2)
TestingLibraryElementError: Unable to find an element by: [data-testid="scenario-aum-untrusted-note"]  (x6)
```

The builder unit cases were RED too: `Tests  5 failed | 30 passed (35)`, e.g.
`Received: "includes $12345 from keys needing attention and $4444 from keys with an unknown sync status"`.

**Flipped deliberately.** Each flipped test has a dated comment saying why:

| Test | Old assertion | New assertion |
|---|---|---|
| `D-06 (component pin, …)` | no marker, `queryByText(/excludes/i)` null | `Excludes $55,555 from keys needing attention.` |
| `AUMTRUST D-15 pin 4 (component)` | "renders no marker" | field still 480,000, marker `Excludes $12,345 from keys needing attention.` (D-20: an allocator-eligible untrusted key with no series IS in `$Y`) |
| `AUMTRUST … state 4` | absent | State C text |
| `AUMTRUST … state 6` | absent | State A text plus the accessible description |

**Neuter → RED → restore.** Each drill was restored from a `cp` byte backup, and `cmp` confirmed the
restore every time:

| # | Neuter | Result | Restore |
|---|---|---|---|
| 1 | Excluded bucket leaks into `total` (`out.total += equity` in the narrowed-out branch). This is the T-167.1-14 tampering path. | unit `4 failed` (`expected 483210 to be 480000`); component `7 failed` (`expected '492345' to be '480000'`, `'535555'`, `'58000' to be '50000'`) | `RESTORED-1`, `RESTORED-1b` |
| 2 | Gate the excludes clause on amount > 0, in both the builder and the composer gate | component `1 failed`, the count-gate case (`Unable to find … scenario-aum-untrusted-note`); unit `2 failed` (`Received: "includes "`) | `RESTORED-2L`, `RESTORED-2C` |
| 3 | Render the excludes clause as a SECOND span beside an includes-only first span | component `6 failed` (`expected [ <span …(3)></span>, …(1) ] to have a length of 1 but got 2`) | `RESTORED-3` |
| 4 | Drop `hintShowsLive` from the marker gate (D-18, state 4) | `2 failed` (`expected 'Required to size and commit.' to be 'Required to size and commit. The live…'`) | `RESTORED-4` |
| 5 | `fieldShowsLive` back to "no manual value" only (D-18, state 6) | `1 failed` (`Unable to find … scenario-aum-untrusted-note`) | `RESTORED-5` |

Drill 1 departs from the plan's wording, "pass `excludeKeyIds` into the `total` path". The literal
version cannot bite in production shapes: a manager-side key is never in the contributing set, so
the manager-side set never reaches the summed branch outside the degrade case. The drill used here
is the threat the plan names (T-167.1-14): the excluded bucket leaking into the total.

**Suite readings:**

- `npx vitest run 'src/app/(dashboard)/allocations/'` → `Test Files 129 passed (129)`,
  `Tests 2007 passed (2007)`. This includes the full composer suite and Open Positions.
- The plan's Task 2 verify printed `VERIFY-OK`: unit `35 passed`; composer filter `81 passed | 285 skipped`.
- `npm run typecheck` exit 0.
- `eslint` on the four files exit 0.

None of these runs timed out, so no single-file re-run was needed.

## Decisions Made

See `key-decisions`. The one copy decision the founder's answer left open is State C's wording. It
was taken from the State B construction and written as a dated UI-SPEC row, so a reviewer can
change it in one place.

## Deviations from Plan

**1. [Founder-directed scope addition] The D-18 reopen (states 4 and 6)**
- **Found during:** dispatch. The founder's answer of 2026-09-24 put it in this plan.
- **Change:** State A in state 6. A new State C in the hint for state 4. The test id and `id`
  `scenario-aum-required-note` on the existing hint, and its `aria-describedby` in State C only.
- **Files:** `ScenarioComposer.tsx`, `ScenarioComposer.test.tsx`, CONTEXT, UI-SPEC
- **Committed in:** `943e72d9f`, `0c27a029e`

**2. [Plan drift, already shipped] `excludeKeyIds` already existed as `managerSideApiKeyIds`, and
`buildUntrustedAumClause` delegates to `buildKeyTrustClause`**
- Code review round 2 (WR-01 / WR-06) had already added the manager-side argument, required and
  read only by `excludedUntrusted`, and pinned it, including the `managerSideKeyIds` helper. The
  unit behaviours the plan asks for were therefore already green, and no change was made to
  `summarizeLiveHoldings`. The clause extension went into the shared `buildKeyTrustClause`
  (review round 2 WR-05 moved the wording there), with an OPTIONAL fourth parameter so Open
  Positions is unchanged.

**3. [Acceptance criterion reads 3, not 2] `grep -c 'data-testid="scenario-aum-untrusted-note"'` on the composer**
- The criterion was written for M3 alone ("no new element"). M3 added no site. The third site is
  State C, from the founder's D-18 addition. The three sites are mutually exclusive
  (`fieldShowsLive`, `overrideNoteShowsLive` and `hintShowsLive` cannot hold together). Every
  state asserts `getAllByTestId(...).toHaveLength(1)`, and drill 3 proves that assertion bites.

**4. [Drill wording] Drill 1.** See the note under the drill table.

**Total deviations:** 1 founder-directed scope addition, 1 plan-drift adaptation, 1
criterion-count explanation, 1 drill-wording substitution. **Impact:** no money figure changed, no
new component, colour or token was added, and the Open Positions output is byte-identical.

## Issues Encountered

None.

## Known limits carried to the release notes

- The D-20 residual stands. A manager-side key outside `eligibleApiKeyIds` (revoked,
  soft-disconnected or inactive) cannot be told apart from the payload, so its untrusted holdings
  may be counted in `$Y`. It over-discloses and never hides.
- A dropped holding whose key is missing from the key list is in no part (review round 2 IN-04).
  The excludes clause says nothing about it, and it is in no on-screen figure either.
- State 7 (a manual value with a live total ≤ 0) stays absent by design. Nothing on screen
  contains the live total there.
- UI-SPEC's long-text backstop (320px / 200% zoom) is still unmeasured, and the State C and M3
  strings make it longer.

## User Setup Required

None.

## Next Phase Readiness

Plan 06, the release, is unblocked. It re-measures D-03 byte-identity, closes WINDOWS entry 66,
and writes the one `chore(release)` commit from the handoff below.

## Release-notes handoff (for plan 06)

**Decision, dated:** D-06 answered **(b)** by the founder on 2026-09-24 (no note beyond the letter).
The same day the founder **reopened D-18** ("Reopen, show the marker"), so the marker shows
whenever the figure on screen includes untrusted dollars.

### Added
- The Scenario composer's AUM marker names holdings the modelled book leaves out:
  `Excludes $8,000 from keys needing attention.` or, with an included part,
  `Includes $12,345 and excludes $8,000 from keys needing attention.` It is one marker in both
  placements. Beside the field it is State A. Inside the override note it reads
  `Overrides live-holdings total $50,000, which excludes $8,000 from keys needing attention.`
  (State B). It renders on the excluded COUNT, so a counted $0 still reads `Excludes $0 …`.
  The total is unchanged.
- State C: when the live total is ≤ 0 and the field is blank, the hint names that total, e.g.
  `Required to size and commit. The live-holdings total is -$4,000, which includes $1,000 from keys needing attention.`
  The field's accessible description is that hint.

### Changed
- A manual AUM exactly equal to the live total now carries the marker beside the field (State A).
  It used to show neither note (review IN-06).
- The marker no longer vanishes when the live total is ≤ 0 (review WR-04). Both were known limits
  of the first review round and are now fixed.

### Tests
- The D-06 component pin, pin 4 (component) and the state 4 / state 6 absence pins were flipped
  deliberately, each with a dated comment. There are new cases for every UI-SPEC section 3 row,
  the excluded-count gate, State C and its byte-identical regression, and six builder unit cases.
  Every new case was observed RED before the change. Five neuter drills each went RED and were
  restored from a `cmp`-verified byte backup: the excluded bucket leaking into the total, an
  amount gate, a second marker span, and each D-18 branch removed.

### Notes
- D-20 residual: a manager-side key outside `eligibleApiKeyIds` (revoked, soft-disconnected or
  inactive) cannot be told apart from the payload, so its untrusted holdings may be counted in
  `$Y`. It over-discloses and never hides.
- State 7 (a manual value with a live total ≤ 0) stays without a marker. Nothing on screen
  contains the live total there.
- A dropped holding whose key is missing from the key list is named by no part (review round 2 IN-04).

Skip-trailer token scan of this section: see the self-check below.

## Self-Check: PASSED

- FOUND: `src/app/(dashboard)/allocations/lib/live-holdings-summary.ts`, `src/app/(dashboard)/allocations/components/ScenarioComposer.tsx`
- FOUND commits: `943e72d9f`, `0c27a029e` (and the founder-answer commit `5d62a1a60`)
- Task 3 verify: `RESOLVED` count in CONTEXT = 1. Planning hygiene OK. `CHANGELOG.md` / `VERSION`
  / `package.json` diff against the merge base is empty.
- ROADMAP diff against the merge base: one hunk, `@@ -2899,13 +2899,35 @@`, which lies inside the
  `### Phase 167.1` section (lines 2899–2931; the next section starts at 2932).
- D-03: `git diff --stat <merge-base> -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase analytics-service` prints nothing.
- The composer carries 0 `"keys needing attention"` literals. The unit file carries 7 `it("D-06` titles.
