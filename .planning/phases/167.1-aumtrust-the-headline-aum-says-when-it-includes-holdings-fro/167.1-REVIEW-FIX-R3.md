---
phase: 167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro
fixed_at: 2026-09-24T08:30:00Z
review_path: .planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-REVIEW-R3.md
iteration: 3
findings_in_scope: 6
fixed: 6
skipped: 0
status: all_fixed
---

# Phase 167.1: Code Review Fix Report, round 3 (plan 05)

**Fixed at:** 2026-09-24
**Source review:** `.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-REVIEW-R3.md` (committed as `1d4e21306`)
**Iteration:** 3 (the plan-05 review round)
**Scope:** `all`: WR-01, WR-02, IN-01 to IN-04.

**Summary:**
- Findings in scope: 6
- Fixed: 6 (1 behaviour change, 1 test, 4 comment or doc changes)
- Skipped: 0

**Decisions taken (orchestrator direction, recorded here and in CONTEXT):**
- WR-01: **fixed, not just recorded** (the review's option 1). The founder's D-06 intent is "the
  composer says what it excludes". The dated CONTEXT note under D-06 closes review round 2 IN-04
  and this WR-01.
- WR-01, Sentry: **no client Sentry capture was added** for the new part. The review's option 1
  suggested adding it to the Sentry count. Two facts made that a duplicate. First, the one
  production-reachable path (a key row on an unsupported exchange) is already escalated to Sentry
  at the trust boundary by `getUserApiKeys` (`reason: unknown_exchange_in_api_key_row`). Second,
  `apiKeys` is non-optional in the payload type, so the `?? []` path is defensive only. The
  existing `holding_key_missing_from_key_list` capture is unchanged.
- IN-03: behaviour left as it is. The exact-equality semantics are documented.

## Fixed Issues

### WR-01: the excludes clause is silent for dropped holdings whose key is missing from the key list

**Files modified:** `src/app/(dashboard)/allocations/lib/live-holdings-summary.ts`,
`src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts`,
`src/app/(dashboard)/allocations/components/ScenarioComposer.tsx`,
`src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx`,
`167.1-UI-SPEC.md`, `167.1-CONTEXT.md`
**Commit:** `82d3b2198`
**Status:** fixed: requires human verification (a copy and logic change on a money disclosure)
**Applied fix:**
- `summarizeLiveHoldings` now returns a new `excludedUnknownStatus` part. It is filled in the same
  single loop, in the narrowing arm, for keys that are not manager-side: an untrusted key goes to
  `excludedUntrusted` as before, and a key absent from `statusByKeyId` (the WR-05 `.has` rule)
  goes to `excludedUnknownStatus`. The `total` path is untouched, so D-03 still holds.
- The D-20 manager-side rule applies to the new part too.
- An absent `payload.apiKeys` (an empty status map) now fails open on the excludes side, the same
  way it already did on the includes side.
- `buildKeyTrustClause` takes the new part as an optional fifth argument and names it with
  `UNKNOWN_KEY_STATUS_SET_NOUN`, after the excluded untrusted part. When it is present, the
  shared-noun form is not used.
- The composer passes the new part through `buildUntrustedAumClause` and adds its count to the
  `showUntrustedMarker` gate. The gate reads the count, never the amount.
- The Open Positions footer passes neither excluded part, so it is byte-identical.
- The "missing-key holding the narrowing drops" unit case was flipped deliberately, with a dated
  comment.
- UI-SPEC § 3 has a dated WR-01 row with every new string. CONTEXT D-06 has a dated "WIDENED" note.

Rendered, State A: `Excludes $4,444 from keys with an unknown sync status.`

**TDD evidence:**
- 9 new cases: 6 unit and 3 component, covering State A, State B with an includes part, and the
  $0 count gate. All 9 were **RED against the prior code**.
- Neuter drills. Each drill was restored from a `cp` backup and checked with `cmp`:

  | Neutered | Cases that went RED |
  |---|---|
  | the `excludedUnknownStatus` accumulation | 5 |
  | the manager-side filter | 3, including the new D-20 case |
  | the `excludedUnknown === null` term of `oneNoun` | 1 |
  | the composer's gate term | 2 |
  | the composer's fifth argument | 3 |

### WR-02: the `untrusted.unavailable === 0` term of `oneNoun` is untested

**Files modified:** `src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts`
**Commit:** `8ee560cd8`
**Applied fix:** Added the review's literal oracle:
`buildKeyTrustClause(part(0, 1, 1), NONE, RENDER, part(8_000, 1))` must return
`"includes $0 from keys needing attention (value unavailable for 1 holding), and excludes $8000 from keys needing attention"`.
Neuter drill: with the term removed, the case went RED and received
`"includes $0 and excludes $8000 from keys needing attention"`. It was restored from a `cp`
backup, checked with `cmp`, and is GREEN again.

### IN-01: stale comments after the reopen

**Files modified:** `ScenarioComposer.tsx`, `live-holdings-summary.ts`
**Commit:** `4d0feade8`
**Applied fix:**
- The `aumInputDescribedBy` comment now says three contributors (State A, the override note, and
  State C's hint).
- The `managerSideKeyIds` JSDoc sentence is marked superseded by D-06 (b), dated, and kept as
  lineage.

### IN-02: `hintShowsLive` is true where no live total is shown

**Files modified:** `ScenarioComposer.tsx`
**Commit:** `bd82cc894`
**Applied fix:** Renamed the flag `fieldBlankHintShows`, updating all 4 code uses. The comment now
says the flag gates the hint in any mode, and that the hint names the live total only when
`showUntrustedMarker` is also true. The old name is recorded as lineage. There is no behaviour
change, and the composer suite passes unmodified.

### IN-03: the State 6 fix covers only exact float equality

**Files modified:** `ScenarioComposer.tsx`, `167.1-UI-SPEC.md`
**Commit:** `d443ea916`
**Applied fix:** Behaviour unchanged, as the review [9] choice. The comment beside `fieldShowsLive`
and a dated UI-SPEC § 1 row now say three things:
- State 6 is exact float equality.
- Typing the rounded live figure lands in State B.
- Nobody should plan against State 6 as the common case.

### IN-04: UI-SPEC rows the reopen did not update

**Files modified:** `167.1-UI-SPEC.md`
**Commit:** `67694814a`
**Applied fix:** Dated "SUPERSEDED 2026-09-24" notes were added to the UI Considerations
`placement` row and the Accessibility "Programmatic association" row. The Accessibility note names
`scenario-aum-required-note` as the third description, used in State C only. The original text is
kept as lineage.

## Verification

All gates ran in **the main checkout of the `quantalyze-167.1` worktree** (branch
`feat/167.1-aumtrust`), at HEAD `67694814a`. `node_modules` was symlinked for each run and removed
before each commit. No hand-rolled review-fix worktree was created.

| Gate | Result |
|---|---|
| `npx tsc --noEmit` | exit 0 |
| eslint on the 4 touched source and test files | exit 0 |
| `npx vitest run 'src/app/(dashboard)/allocations' src/lib/closed-sets` | 135 files, 2101 tests passed |
| `git diff --stat $(git merge-base HEAD origin/main) -- src/lib/queries.ts supabase analytics-service` | empty |
| `check-planning-hygiene` before each commit | OK |
| sweep of each commit message for the CI skip token | 0 hits |

No DB command was run. No VERSION or CHANGELOG change was made. Nothing was pushed.

---

_Fixed: 2026-09-24_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 3_
