---
phase: "167.1"
slug: "aumtrust-the-headline-aum-says-when-it-includes-holdings-fro"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-23"
---

# Phase 167.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Decision → test map is in
> `167.1-RESEARCH.md` § Validation Architecture; this file is the execution-time contract.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (jsdom) + @testing-library/react |
| **Config file** | `vitest.config.ts` (coverage ratchet read from it, never restated) |
| **Quick run command** | `npx vitest run 'src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts' && npx vitest run 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx' -t "AUMTRUST"` |
| **Full suite command** | `npm test` plus `npx tsc --noEmit` and `npm run lint` |
| **Estimated runtime** | quick ~5 s; full suite several minutes |

---

## Sampling Rate

- **After every task commit:** quick run command
- **After every plan wave:** full suite command
- **Before `/gsd-verify-work`:** full suite green, neuter → RED evidence recorded per D-15
- **Max feedback latency:** 60 seconds for the quick command

---

## Per-Task Verification Map

Filled by the planner's `<verify>` blocks; each `<automated>` carries a `<fails_when>`.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (per plan) | — | — | D-02..D-20 | — | tenant-scoped payload only | unit / component / static | see PLAN.md | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts` — unit pins D-03/04/05/06/07 + closed-sets test double
- [ ] `describe("ScenarioComposer — AUMTRUST (Phase 167.1)")` in `ScenarioComposer.test.tsx` — synthetic ids only
- [ ] `OpenPositionsTable` footer case (D-16)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Anti-vacuity | D-15 | neuter → RED → restore is a process, recorded in SUMMARY | apply the named neuter, run, paste RED, restore, run green |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
