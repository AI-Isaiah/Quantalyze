---
phase: 167.1-aumtrust
plan: 04
subsystem: docs
tags: [closed-sets, roadmap, premise-correction, byte-identity]

requires:
  - phase: 167.1-01
    provides: "summarizeLiveHoldings, scenario-aum-untrusted-note (State A)"
  - phase: 167.1-02
    provides: "the OpenPositionsTable footer pass and open-positions-untrusted-note"
  - phase: 167.1-03
    provides: "State B inside the override note"
provides:
  - "Corrected SFH-M2 paragraph above UNTRUSTED_KEY_SYNC_STATUSES (comment only)"
  - "Dated CORRECTED 2026-09-23 notes in ROADMAP ### Phase 167.1 (Goal and Requirements kept as lineage)"
  - "Interim branch-wide byte-identity reading before the plan 05 founder checkpoint"
affects: [167.1-05, 167.1-06]

actuals:
  tokens: 610      # chars/4 over the added lines of the realized diff (2425 chars)
  tasks: 2
  commits: 1
plan_head_before: f7f672607b6dc46082c1ba7fe1ba3b460cbf5c83

tech-stack:
  added: []
  patterns:
    - "Premise correction keeps the original sentence as lineage and appends a dated CORRECTED note"

key-files:
  created: []
  modified:
    - "src/lib/closed-sets.ts"
    - ".planning/ROADMAP.md"

key-decisions:
  - "167.1-04: the ROADMAP D-03 correction says the four money paths WERE byte-unchanged when plan 04 measured them, and names plan 06 as the re-measure, rather than asserting it as a standing fact. Plan 05 option (b) edits source, so an unqualified claim could go stale."

patterns-established: []

requirements-completed: [D-12, D-13, D-03, D-10, D-11, D-17]

coverage:
  - id: C1
    description: "No tracked source prose says the predicate reaches no rendered total or that the flag is deferred; SFH-M2 lineage kept"
    requirement: "D-12"
    verification:
      - kind: other
        ref: "grep -c 'booked as a' src/lib/closed-sets.ts = 0; '167.1' >= 1; 'SFH-M2' >= 1; closed-sets.untrusted-key-status.test.ts 7/7 passed"
        status: pass
      - kind: other
        ref: "git diff $(git merge-base HEAD origin/main) -- src/lib/closed-sets.ts: grep -cE '^[+-][^+/-]' = 0 (comment lines only)"
        status: pass
    human_judgment: false
  - id: C2
    description: "ROADMAP 167.1 keeps its Goal sentence and carries a dated correction naming the real surfaces, the absent KPI AUM cell, D-03 and D-17"
    requirement: "D-13"
    verification:
      - kind: other
        ref: "python section check on ### Phase 167.1: CORRECTED 2026-09-23 present and original Goal sentence present -> OK"
        status: pass
    human_judgment: false
  - id: C3
    description: "queries.ts, the scenario commit route, supabase/ and analytics-service/ byte-unchanged on the branch; the four phase test files green"
    requirement: "D-03"
    verification:
      - kind: other
        ref: "git diff --stat bbc5ea195d554149b10b31f79a410e9b49b1fd76 -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/ (empty)"
        status: pass
      - kind: unit
        ref: "4 test files, 386/386 passed"
        status: pass
    human_judgment: false

duration: 4min
completed: 2026-09-24
status: complete
---

# Phase 167.1 Plan 04: Premise corrections and the interim byte-identity gate Summary

**The SFH-M2 comment in `closed-sets.ts` and the ROADMAP 167.1 entry no longer say the untrusted-key predicate misses a "headline AUM" or that the phase changes money. Both now name the two real disclosure surfaces and keep the old wording as lineage. A branch-wide measurement shows `queries.ts`, the commit route, `supabase/` and `analytics-service/` byte-unchanged before the founder checkpoint.**

## Performance

- **Duration:** about 4 min
- **Started:** 2026-09-24T01:57:30Z
- **Completed:** 2026-09-24T02:01:30Z
- **Tasks:** 2 (1 committed; Task 2 is measurement only)
- **Files modified:** 2

## Accomplishments

- **D-12, `src/lib/closed-sets.ts`.** The "WHAT THIS PREDICATE DOES NOT REACH" paragraph is now headed "WHAT THIS PREDICATE REACHES, AND HOW" and keeps the `SFH-M2` token and the "an earlier version of this comment claimed otherwise" lineage. It names `summarizeLiveHoldings`, which feeds `scenario-aum-untrusted-note`, and the `OpenPositionsTable` footer pass, which feeds `open-positions-untrusted-note`, and says both disclose rather than subtract. It says `totalAum` in `queries.ts` still sums every holding but `liveBaselineMetrics.aum` is rendered nowhere (Phase 64 PRESENT-01). It says the `isPerKeyDailiesEligibleKey` book-mode exclusion of `revoked` is the D-06 founder question. The sentence deferring the flag to a follow-up is gone. No code line and no other paragraph changed. `closed-sets.untrusted-key-status.test.ts` was not touched, and its SFH-M2 cross-reference still resolves.
- **D-13, `.planning/ROADMAP.md` `### Phase 167.1`.** The `**Goal:**` and `**Requirements**` lines are byte-unchanged. A dated `⛔ CORRECTED 2026-09-23 (Phase 167.1 D-01/D-13)` note follows the Goal. It covers the missing KPI-strip AUM cell, the composer field and override note, the Open Positions footer (D-16), and the `ExposureByClass` exclusion (D-17). A second dated note follows Requirements and marks the money-number framing as lineage (D-03). The `**Plans:**` count and plan list were not touched by Task 1.

## Task Commits

1. **Task 1 (tracer): correct the false premise in source prose and the ROADMAP**: `7e3360f11` (docs). Both `<verify>` commands printed OK. Tracer feedback gate (auto mode): the verify was re-run inside Task 2's four-file run (closed-sets test green) before moving on.
2. **Task 2: interim byte-identity gate**: no commit (measurement only; `git status --short` showed only the two pre-existing unstaged ledgers `.planning/milestone.lock` and `.planning/state.json`, which this plan does not stage).

## Interim byte-identity reading (D-03 / D-10 / D-11)

Merge base `bbc5ea195d554149b10b31f79a410e9b49b1fd76`, HEAD `7e3360f11`.

```
$ git diff --stat "$BASE" -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/
(empty; /tmp/167.1-04-t2.stat has size 0)
```

Four phase test files (`live-holdings-summary.test.ts`, `ScenarioComposer.test.tsx`, `untrusted-key-status.surfaces.test.tsx`, `closed-sets.untrusted-key-status.test.ts`): `Test Files 4 passed (4)`, `Tests 386 passed (386)`. The `<verify>` printed OK.

Every non-`.planning/` path the branch touches (`git diff --name-only "$BASE" HEAD`), mapped to the plan whose `files_modified` names it:

| Path | Plan(s) |
|---|---|
| `src/app/(dashboard)/allocations/lib/live-holdings-summary.ts` | 01 |
| `src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts` | 01 |
| `src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` | 01, 03 |
| `src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx` | 01, 03 |
| `src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx` | 02 |
| `src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx` | 02 |
| `src/lib/closed-sets.ts` | 04 |

No path is unaccounted for. ⚠️ This is an interim reading. Plan 06 re-runs the gate after the D-06 answer, because plan 05 option (b) edits source.

## Acceptance measurements (Task 1)

- `git diff <base> -- src/lib/closed-sets.ts | grep -cE '^[+-][^+/-]'` printed `0`: prose only.
- `git diff --stat <base> -- src/lib/closed-sets.untrusted-key-status.test.ts` printed nothing.
- This plan's ROADMAP hunks (`git diff -U0` before commit): `@@ -2901,0 +2902 @@` and `@@ -2902,0 +2904 @@`. `### Phase 167.1:` is at line 2899 and the next section, `### Phase 167.2:`, is at line 2930, so both hunks are inside 167.1. The branch-wide ROADMAP diff also has the plan 01-03 ticks, and those are in the same section too.

## Verification (plan-level)

- `npm run check:planning-hygiene`: `[check-planning-hygiene] OK — 6933 tracked files scanned …` (whole output read). It also ran after `git add` and before the commit, and printed OK.
- `npx vitest run src/lib/closed-sets.untrusted-key-status.test.ts`: 7/7 passed.
- `npm run lint`: exit 0.
- `npx eslint src/lib/closed-sets.ts`: exit 0.
- Branch-wide byte-identity stat: empty (above).

## Decisions Made

- The D-03 ROADMAP correction is dated to plan 04's measurement ("were byte-unchanged … when plan 04 measured them (plan 06 re-measures after the D-06 answer)"). It is not written as a standing fact, so option (b) in plan 05 cannot make it false without anyone noticing.

## Deviations from Plan

None. The plan was executed as written.

## Issues Encountered

None.

## Known Stubs

None.

## User Setup Required

None.

## Next Phase Readiness

- Plan 05 (`checkpoint:decision`, D-06) is next, and execution stops there for the founder's answer. The corrected SFH-M2 paragraph already names D-06 as the open question, so plan 05 needs no further prose change here unless option (b) ships. If it does, that paragraph's last sentence should be updated to say the absence is disclosed.
- Plan 06 re-runs the byte-identity gate and writes the release commit.

---
*Phase: 167.1-aumtrust*
*Completed: 2026-09-24*

## Self-Check: PASSED
