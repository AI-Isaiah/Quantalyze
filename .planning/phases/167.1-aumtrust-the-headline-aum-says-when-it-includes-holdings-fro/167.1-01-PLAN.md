---
phase: 167.1-aumtrust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/lib/live-holdings-summary.ts
  - src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-07, D-09, D-10, D-11, D-15, D-18, D-19]
estimate:
  tokens: 85000     # revised 2026-09-23: Task 3 (State B + absence states) split out into plan 03 (checker scope_sanity, 130000 vs 100000 budget)
  raw_tokens: 85000
  tasks: 2
  confidence: low   # DERIVED: estimate-calibration reports sample_count 0, factor 1
must_haves:
  truths:
    - "D-03 / D-15 pin 1: with one sign_in_failed holding in the summed set, scenario-aum-input shows the SAME total it showed before this phase — the untrusted holding is still counted, and the field does NOT equal total minus the untrusted amount."
    - "D-02 / D-01: in book mode with the live total in the field, a muted sentence beside the field reads 'Includes $X from keys needing attention.' where X is the untrusted contribution; the noun comes from UNTRUSTED_KEY_SET_NOUN, never a restated literal."
    - "D-04 / D-19 / D-15 pin 2: X is summed in the SAME single pass as the total (summarizeLiveHoldings), over exactly the holdings the total sums, restricted to keys isUntrustedKeySyncStatus marks untrusted, on the holdingEquityContributionLocal equity basis — including a derivative whose contribution is <= 0."
    - "D-15 pin 4: an untrusted holding that is not toggled on, or whose key is not in contributingApiKeyIds, contributes nothing to X."
    - "D-05 / D-15 pin 5: the only membership test is isUntrustedKeySyncStatus — widening UNTRUSTED_KEY_SYNC_STATUSES through a test double changes the result; with contributingApiKeyIds empty (the degrade branch) a revoked holding is summed AND flagged."
    - "D-06 / D-15 pin 6, unit half (the pin only — the decision is plan 05's; the component half is plan 03's): a revoked key's holding outside the contributing set is ABSENT from the total and from X and is counted in excludedUntrusted — pinned by a unit test whose title starts with 'D-06'."
    - "D-07: the marker gate reads the untrusted COUNT (count >= 1, not amount > 0) — showUntrustedMarker is defined on liveHoldingsSummary.untrusted.count; unit pin 2 proves the count includes a <= 0 derivative. (The component cases for D-07 and pin 3 are plan 03's.)"
    - "D-18 (gate definition): showUntrustedMarker = book mode AND untrusted count > 0 AND (fieldShowsLive OR overrideNoteShowsLive), where both booleans are hoisted from the two existing AUM-row conditions and the override note's JSX reads overrideNoteShowsLive. This plan renders State A only; plan 03 renders State B and pins every absence state."
    - "D-09 / UI-SPEC U-01 (State A): the marker is text-xs text-text-muted with no role, no aria-live, no glyph and no sign colour."
    - "UI-SPEC overflow (covered): the M1 marker is one flex item in the existing flex-wrap row and wraps to its own line like the override note; it adds no margin, width or truncation class."
    - "UI-SPEC loading/error (covered): the marker is a synchronous derivation of data already in the SSR payload — no fetch, no loading state, no error state; non-finite inputs coerce to 0 through holdingEquityContributionLocal."
    - "D-10 / D-11: liveBaselineMetrics gains no field; src/lib/queries.ts, the commit route, supabase/ and analytics-service/ are untouched by this plan."
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/live-holdings-summary.ts"
      provides: "summarizeLiveHoldings (single-pass total + untrusted + excludedUntrusted), LiveHoldingsSummary, holdingEquityContributionLocal (moved, exported)"
    - path: "src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts"
      provides: "unit pins D-15 1/2/4/5/6 and the degrade branch, with a vi.hoisted-toggled closed-sets double"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "statusByKeyId + liveHoldingsSummary memos, liveHoldingsSum = summary.total, hoisted display gates, the State A scenario-aum-untrusted-note"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
      provides: "describe 'ScenarioComposer — AUMTRUST (Phase 167.1)' beside the F2 block, carrying the tracer case (plan 03 extends it)"
  key_links:
    - from: "payload.apiKeys[].sync_status"
      to: "summarizeLiveHoldings statusByKeyId"
      via: "statusByKeyId memo keyed on api key id, joined on holding.api_key_id"
    - from: "summarizeLiveHoldings().total"
      to: "liveHoldingsSum -> scenarioAum, committedAumText, the seeding effect, scenario-aum-override-note"
      via: "const liveHoldingsSum = liveHoldingsSummary.total (value unchanged)"
    - from: "summarizeLiveHoldings().untrusted.count"
      to: "scenario-aum-untrusted-note"
      via: "showUntrustedMarker = book mode && count > 0 && (fieldShowsLive || overrideNoteShowsLive)"
---

<objective>
Put the founder's "keep the total and flag it" decision (2026-09-22, binding) on the one headline
holdings-derived dollar figure an allocator actually sees: the Scenario composer's PORTFOLIO AUM
(USD) field and the override note that quotes the same live total (D-01).

Purpose: today a holding sourced from a `sign_in_failed` key is summed into that figure with no
sign that part of it is not current, while the Holdings tab strikes the same rows through. This
plan adds a disclosure beside the figure and changes no money number (D-03).

Output: a pure single-pass helper `summarizeLiveHoldings` (D-19) that owns the loop
`liveHoldingsSum` used to own, the composer rewired to read its `total` byte-identically, the
State A muted `scenario-aum-untrusted-note` element placed per UI-SPEC § 1, and unit tests that
were each observed RED under a named neuter (D-15).

Split (revision 2026-09-23, checker scope_sanity): State B nesting, every UI-SPEC § 1 absence
state, the component half of the D-06 pin and the tone assertions are plan 03, which runs next on
the same two composer files. This plan leaves the composer coherent in between: the override
note's text is unchanged and State A is the only rendered marker.

Spec-less probe fallback: skipped — this phase has no requirement IDs to probe (visible skip,
recorded here as instructed; no probe predicates generated, no SPEC.md exists).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
@DESIGN.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-CONTEXT.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-RESEARCH.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-PATTERNS.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-UI-SPEC.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-VALIDATION.md
@src/lib/closed-sets.ts
@src/lib/dollar-validation.ts
@src/app/(dashboard)/allocations/lib/mandate-gates.ts
</context>

<repo_guardrails>
⛔ **Work only in the `feat/167.1-aumtrust` worktree.** Stage files by explicit path; never
`git add -A` / `git add .`.
⛔ **Cite by SYMBOL, never by line number.** `ScenarioComposer.tsx` is ~7,800 lines and its test
file ~15,400. Re-locate every anchor with `grep -n '<symbol>'` and read a bounded window; never
read either file whole.
⛔ **Restore only from a `cp` byte backup verified with `cmp`, re-taken after every edit.** Never
`git checkout -- <path>`, `git restore` or `git stash` (the stash stack is shared across worktrees
and `git checkout --` silently destroys uncommitted work in a neuter/restore harness).
⛔ **Synthetic identifiers only** in fixtures (`aumtrust-key-a`, `aumtrust-key-b`, placeholder
venues). The repo and `.planning/` are public.
⛔ **Never write a CI skip trailer in a commit message**, not even to deny it.
⛔ Never `npm install` — `node_modules` resolves by walking up from the worktree.
⛔ No database command, no Python, no `uvicorn`. This plan is client-side TypeScript only (D-11).
⚠️ `grep -c` exits 1 on a zero count — wrap as `$(grep -c … || true)`. ⚠️ Quote the
parenthesised paths for zsh.
</repo_guardrails>

<interfaces>
Measured at worktree HEAD (symbols, not lines):

- `ScenarioComposer.tsx` today has ONE toggle-map walk (the `liveHoldingsSum` memo) and
  `holdingEquityContributionLocal` has TWO callers (that memo and `equityByApiKeyId`). After this
  plan: zero walks in the composer, the function lives in the new module, and both callers import
  it from there.
- Existing AUM-row conditions (the new gates must REUSE them, not re-type them):
  `sanitizedManualAum === undefined && liveHoldingsSum <= 0` (the "Required to size and commit."
  hint) and `sanitizedManualAum !== undefined && liveHoldingsSum > 0 && sanitizedManualAum !== liveHoldingsSum`
  (the override note).
- `closed-sets.ts`: `UNTRUSTED_KEY_SYNC_STATUSES = ["revoked", "sign_in_failed"]`,
  `isUntrustedKeySyncStatus(status: string | null | undefined)`, `UNTRUSTED_KEY_SET_NOUN`. No
  `server-only` import; the composer already imports from it.
- `dollar-validation.ts`: `formatUsd(n: number | null): string` — whole dollars, hyphen-minus on
  negatives, already imported by the composer.
- Composer test file-scope helpers to reuse: `makePayload`, `HOLDING_BTC`, `winApiKey` (returns
  `sync_status: null`), `ALLOCATOR_A`, `lsStore`, `localStorageMock`, and the F2 describe's
  `f2Payload` / `renderF2` / `aumField` idiom.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a sign_in_failed holding is still in the AUM field and the field says so" — State A only</name>
  <files>src/app/(dashboard)/allocations/lib/live-holdings-summary.ts, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - `src/app/(dashboard)/allocations/components/ScenarioComposer.tsx`: grep and read bounded windows around `function holdingEquityContributionLocal`, `const equityByApiKeyId`, `const holdingByRef`, `const liveHoldingsSum`, `const sanitizedManualAum`, `committedAumText`, and `data-testid="scenario-aum-input"` (the AUM row JSX through `scenario-aum-modelled-note`).
    - `src/app/(dashboard)/allocations/lib/mandate-gates.ts` header: the module-shape analog (type-only import from `@/lib/queries`, no `"use client"`).
    - `src/lib/closed-sets.ts`: `isUntrustedKeySyncStatus`, `UNTRUSTED_KEY_SET_NOUN`.
    - 167.1-PATTERNS.md § 1 (the loop to MOVE, and the two long comments that travel with it) and § 3 (composer anchors a–d).
    - 167.1-UI-SPEC.md § Copywriting Contract, "Construction rule" and "1. M1" State A.
    - `ScenarioComposer.test.tsx`: grep `describe("ScenarioComposer — review round 2: partial-book dollars` and read that block's fixtures and helpers.
  </read_first>
  <behavior>
    - Book with a trusted spot holding (key `aumtrust-key-a`, `sync_status: null`) and a `sign_in_failed` spot holding (key `aumtrust-key-b`), both contributing: `scenario-aum-input` value equals the hand-typed sum of BOTH, and does NOT equal the trusted amount alone (D-03, D-15 pin 1).
    - The same render shows exactly one `scenario-aum-untrusted-note` whose textContent is the typed literal `Includes $<untrusted amount formatted by hand> from keys needing attention.` (D-02). The oracle is typed, never built from the constant or the formatter.
  </behavior>
  <action>
    Write the failing component test first (RED), then wire the one path.

    (a) Create `src/app/(dashboard)/allocations/lib/live-holdings-summary.ts` per D-19. Module docblock states why it exists: the total and its untrusted subset must come from ONE pass so they cannot drift (D-04), and it is a plain module so its unit test can double `@/lib/closed-sets` without touching the composer suite. Imports: `import type { MyAllocationDashboardPayload } from "@/lib/queries"` (type-only; `@/lib/queries` is `server-only`) and `import { isUntrustedKeySyncStatus } from "@/lib/closed-sets"`. No `"use client"`.
    Move `holdingEquityContributionLocal` here VERBATIM and `export` it; update its docblock to say it now lives here and why it is still a local copy of the SSR `holdingEquityContribution`. Delete the composer's copy and import it in the composer so `equityByApiKeyId` and the new memo share one definition. Leave `ScenarioComparePanel.tsx`'s inline copy alone (Rule 3).
    Export `interface LiveHoldingsSummary { total: number; untrusted: { amount: number; count: number }; excludedUntrusted: { amount: number; count: number } }` and `function summarizeLiveHoldings(args: { toggleByScopeRef; holdingByRef; contributingApiKeyIds; statusByKeyId })`. The body is the composer's existing loop, same walk order and same `continue` chain (enabled flag, `holding:` prefix, `holdingByRef` hit, then the contributing-set narrowing), carrying the "the narrowing applies only when there IS a modelled set" and E1 basis comments with it. Per holding compute the equity contribution once and the untrusted flag once through `isUntrustedKeySyncStatus(statusByKeyId.get(h.api_key_id))`. A holding dropped by the narrowing adds to `excludedUntrusted` iff untrusted, then continues. A summed holding adds to `total` always, and to `untrusted` iff untrusted. `total` accumulates in the same order as the old loop so it is bit-identical (D-03). ⛔ No local equality on the status column anywhere in this file (167 D-16). `excludedUntrusted` ships now because the D-06 pin (Task 2 here, and plan 03's component half) measures the revoked exclusion through it; plan 05 (the founder's D-06 answer) decides whether anything renders it.
    (b) In `ScenarioComposer.tsx`: add a `statusByKeyId` memo (`Map` from `payload.apiKeys ?? []` id to `sync_status`, deps `[payload.apiKeys]`), copying the `holdingByRef` "two memos over one big memo" precedent. Replace the `liveHoldingsSum` memo with a `liveHoldingsSummary` memo calling `summarizeLiveHoldings` (deps: the draft toggle map, `holdingByRef`, `payload.contributingApiKeyIds`, `statusByKeyId`), then `const liveHoldingsSum = liveHoldingsSummary.total;` so every downstream reader (`scenarioAum`, the seeding effect, `committedAumText`, the override note) is unchanged. Keep the Phase 151 AUM-01 / F2 / E1 prose above the new memo.
    (c) Hoist two booleans above the `return`, derived from the two existing AUM-row conditions: `fieldShowsLive` and `overrideNoteShowsLive`. Rewrite the override note's JSX condition to read `overrideNoteShowsLive` (same logic, so it cannot fork). Add `showUntrustedMarker = entryMode === "book" && liveHoldingsSummary.untrusted.count > 0 && (fieldShowsLive || overrideNoteShowsLive)` per D-07 and D-18.
    (d) Add a module-level clause builder in the composer, `buildUntrustedAumClause(includedAmount: number): string`, returning the lower-case clause `includes <formatUsd(amount)> from <UNTRUSTED_KEY_SET_NOUN>` — ONE builder so State A (this task) and State B (plan 03) cannot drift, and so plan 05 can extend it under D-06 option (b) (UI-SPEC § 3). Extend the existing `@/lib/closed-sets` import with `UNTRUSTED_KEY_SET_NOUN`; never restate the noun as a string (D-02). In State A (`showUntrustedMarker && fieldShowsLive`) render one `<span data-testid="scenario-aum-untrusted-note" className="text-xs text-text-muted">` immediately after the `<input>` and before `scenario-aum-modelled-note`, whose text is the clause with its first letter capitalised, followed by a full stop. No role, no aria attribute, no glyph, no margin (D-09, UI-SPEC U-01/U-07). Put a block comment on it in the F2 note's voice: a headline money figure may not change meaning silently; it discloses, never subtracts (D-02/D-03).
    (e) Add `describe("ScenarioComposer — AUMTRUST (Phase 167.1)")` beside the F2 describe, with its own payload builder (copy `f2Payload`'s shape, ids `aumtrust-key-*`, untrusted key via `{ ...winApiKey("aumtrust-key-b"), sync_status: "sign_in_failed" }`), a fixture self-proof of the hand-computed amounts, and the tracer case from `<behavior>`. Pick amounts with a large gap so a basis mix-up cannot hide in rounding.
    ⛔ Distinct-triple fixture rule: `holdingByRef` (and the SSR dedup behind `holdingsSummary`) keys on `buildHoldingRef({ venue, symbol, holding_type })`, not on the key, so two fixture holdings sharing a `(venue, symbol, holding_type)` triple collapse to ONE map entry and one of them silently vanishes from the sum. Every holding in every AUMTRUST fixture gets a distinct triple (for example a distinct placeholder `symbol` per key); the fixture self-proof asserts that the set of `buildHoldingRef(h)` values over the fixture's holdings has the same size as the holdings array.
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx' --reporter=dot -t "AUMTRUST|partial-book dollars|AUM-01" > /tmp/167.1-01-t1.log 2>&1; RC=$?; tail -25 /tmp/167.1-01-t1.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167.1-01-t1.log && grep -qE 'Tests +[0-9]+ passed' /tmp/167.1-01-t1.log && echo OK</automated>
    <fails_when>non-zero exit, "0 passed" in the summary, or no "Tests N passed" line (the -t filter matched nothing)</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f 'src/app/(dashboard)/allocations/lib/live-holdings-summary.ts'` and it exports `summarizeLiveHoldings`, `LiveHoldingsSummary`, `holdingEquityContributionLocal`.
    - `grep -c -F 'Object.entries(scenario.draft.toggleByScopeRef)' 'src/app/(dashboard)/allocations/components/ScenarioComposer.tsx'` returns 0 (the walk moved; it was 1 at base).
    - `grep -c 'function holdingEquityContributionLocal' 'src/app/(dashboard)/allocations/components/ScenarioComposer.tsx'` returns 0; `grep -c 'holdingEquityContributionLocal' 'src/app/(dashboard)/allocations/components/ScenarioComposer.tsx'` is at least 2 (the import plus `equityByApiKeyId`).
    - `grep -c '"keys needing attention"' 'src/app/(dashboard)/allocations/components/ScenarioComposer.tsx'` returns 0 and `grep -c 'UNTRUSTED_KEY_SET_NOUN' 'src/app/(dashboard)/allocations/components/ScenarioComposer.tsx'` is at least 1.
    - `grep -cE '=== *"(revoked|sign_in_failed)"' 'src/app/(dashboard)/allocations/lib/live-holdings-summary.ts'` returns 0.
    - The tracer test was observed RED before the wiring and GREEN after; the F2 and AUM-01 tests pass unmodified.
  </acceptance_criteria>
  <done>A sign_in_failed contributing holding is still in the field's total and the field carries one muted "Includes $X from keys needing attention." sentence; the loop lives in one helper; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit pins on summarizeLiveHoldings, including the shared-predicate double (D-15 pins 1, 2, 4, 5, 6)</name>
  <files>src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts</files>
  <read_first>
    - `src/app/(dashboard)/allocations/lib/live-holdings-summary.ts` (Task 1 output).
    - `src/app/(dashboard)/allocations/lib/mandate-gates.test.ts` header and fixtures (layout analog, the `DashboardHolding` alias).
    - `src/app/api/strategies/finalize-wizard/route.test.ts`: grep `vi.mock("@/lib/ratelimit"` and `vi.hoisted` (the partial-module-double analog).
    - `src/lib/closed-sets.ts`: `TRUSTED_OR_NEUTRAL_KEY_SYNC_STATUSES` (to pick a status the real predicate calls trusted).
    - `src/lib/closed-sets.untrusted-key-status.test.ts` docblock (ORACLE INDEPENDENCE rule).
    - 167.1-RESEARCH.md § Common Pitfalls 1, 4, 5.
  </read_first>
  <behavior>
    - Pin 1 (D-03): one trusted + one sign_in_failed contributing holding — `total` equals the hand-typed sum of both.
    - Pin 2 (D-04, D-07): `untrusted.amount` equals the hand-listed untrusted subset exactly, including a sign_in_failed DERIVATIVE whose `unrealized_pnl_usd` is negative (its notional `value_usd` must not leak in); `untrusted.count` counts it even though its amount is <= 0.
    - Pin 4: a sign_in_failed holding whose toggle is `false`, and a sign_in_failed holding whose key is not in a non-empty contributing set, add nothing to `total` or `untrusted`. Non-`holding:` refs and refs missing from `holdingByRef` are ignored.
    - Pin 5 (D-05): a holding whose key status is a member of `TRUSTED_OR_NEUTRAL_KEY_SYNC_STATUSES` (for example `error`) is NOT counted under the real predicate, and IS counted (right amount, count 1) when the `vi.hoisted` toggle widens the double.
    - Degrade branch (D-05, Pitfall 5): `contributingApiKeyIds: []` with a revoked holding — it is in `total` AND in `untrusted`.
    - Pin 6, titled starting "D-06": a revoked key absent from a non-empty contributing set — absent from `total` and `untrusted`, present in `excludedUntrusted` with its amount and count 1.
  </behavior>
  <action>
    Create the unit file following the `mandate-gates.test.ts` layout (plain node test, no jsdom header, relative import of `./live-holdings-summary`, `type DashboardHolding = MyAllocationDashboardPayload["holdingsSummary"][number]`, `build*` fixture factories with overrides, synthetic ids `aumtrust-key-*`).
    Double `@/lib/closed-sets` with the `finalize-wizard/route.test.ts` shape: `vi.mock("@/lib/closed-sets", async (importOriginal) => …)` spreading the original module and replacing only `isUntrustedKeySyncStatus` with a function that delegates to the REAL predicate unless a `vi.hoisted` toggle is set, in which case it also answers true for `error`. Reset the toggle in `beforeEach`. Say in a comment why the double delegates (every other case must exercise the real predicate) and why it lives here and not in the composer suite (no test in the repo mocks this module; hoisted into the 335-test composer file it would apply to all of them).
    Oracles are hand-typed numbers over hand-listed subsets, never a re-run of the helper's own filter (ORACLE INDEPENDENCE). Distinct-triple rule: build each fixture's `holdingByRef` with `buildHoldingRef` imported from `./holding-outcome-adapter` (as the composer does) and give every holding a distinct `(venue, symbol, holding_type)` triple — a shared triple collapses two holdings into one map entry and makes the hand-typed oracle wrong for a reason unrelated to the helper. Do not assert `untrusted.amount <= total` as an invariant — a <= 0 derivative breaks it by design.
    Prove each pin can fail, one at a time, on a `cp` byte backup of `live-holdings-summary.ts` restored and `cmp`-verified after each drill (re-take the backup after every edit): (1) `continue` before adding an untrusted summed holding to `total` → pin 1 RED; (2) use `value_usd` for the untrusted accumulator → pin 2 RED; (3) accumulate `untrusted` before the narrowing check → pin 4 RED; (4) replace the predicate call with a local equality on the sign-in-failed status → pin 5 widened arm RED; (5) delete the narrowing `continue` → the D-06 pin RED. Paste each RED line into the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts' --reporter=dot > /tmp/167.1-01-t2.log 2>&1; RC=$?; tail -20 /tmp/167.1-01-t2.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167.1-01-t2.log && grep -qE 'Tests +[0-9]+ passed' /tmp/167.1-01-t2.log && echo OK</automated>
    <fails_when>non-zero exit, "0 passed" in the summary, or no "Tests N passed" line</fails_when>
    <automated>npm run typecheck > /tmp/167.1-01-t2-tsc.log 2>&1; RC=$?; tail -20 /tmp/167.1-01-t2-tsc.log; test "$RC" = 0 && echo OK</automated>
    <fails_when>non-zero exit, or any "error TS" line in the log (for example the vi.mock factory shape failing to typecheck, RESEARCH A5)</fails_when>
  </verify>
  <acceptance_criteria>
    - The unit file contains at least one test title starting with `D-06`: `grep -cE '(it|test)\("D-06' 'src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts'` is at least 1.
    - `grep -c 'vi.mock("@/lib/closed-sets"' 'src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts'` returns 1; `grep -rc 'vi.mock("@/lib/closed-sets"' 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx'` returns 0.
    - No fixture carries a non-synthetic identifier (every `api_key_id` in the file matches `aumtrust-`).
    - The SUMMARY records five neuter → RED → restore drills, each with the pasted RED line and a `cmp` verdict.
  </acceptance_criteria>
  <done>The helper's arithmetic is pinned by tests each proven able to fail; typecheck is clean.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

| Symbol / id | Kind | Location |
|---|---|---|
| `summarizeLiveHoldings` | exported function (single pass) | `src/app/(dashboard)/allocations/lib/live-holdings-summary.ts` |
| `LiveHoldingsSummary` | exported interface `{ total, untrusted, excludedUntrusted }` | same |
| `holdingEquityContributionLocal` | exported function (MOVED from the composer, body unchanged) | same |
| `statusByKeyId`, `liveHoldingsSummary` | memos | `ScenarioComposer.tsx` |
| `fieldShowsLive`, `overrideNoteShowsLive`, `showUntrustedMarker` | derived booleans | `ScenarioComposer.tsx` |
| `buildUntrustedAumClause` | module-level clause builder (plan 03 reads it for State B; plan 05 may extend it) | `ScenarioComposer.tsx` |
| `scenario-aum-untrusted-note` | test id (State A span here; plan 03 adds the State B nested span) | `ScenarioComposer.tsx` |
| `describe("ScenarioComposer — AUMTRUST (Phase 167.1)")` | test block | `ScenarioComposer.test.tsx` |
| `live-holdings-summary.test.ts` | unit test file | `src/app/(dashboard)/allocations/lib/` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR payload -> client composer | `payload.apiKeys` and `holdingsSummary` arrive already tenant-scoped (`getUserApiKeys` `.eq("user_id")`, `allocator_holdings` `.eq("allocator_id")`); the marker only joins them |
| client figure -> commit route | the composer's AUM is display and draft sizing; the server recomputes its own AUM |
| repo -> public | the repo and `.planning/` are public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.1-01 | Information Disclosure | `scenario-aum-untrusted-note` copy | low | mitigate | The copy carries only a dollar sum and the constant noun — no key id, label, venue or `sync_error` text is interpolated (UI-SPEC construction rule). |
| T-167.1-02 | Tampering (XSS) | marker rendering | low | mitigate | React text nodes only; no `dangerouslySetInnerHTML`; no venue-supplied string reaches the element. |
| T-167.1-03 | Tampering / integrity of a displayed money figure | `liveHoldingsSum` rewire | high | mitigate | `total` keeps the old loop's walk order and filters (bit-identical); D-15 pin 1 fails if an untrusted holding is dropped; `src/lib/queries.ts` and the commit route are byte-unchanged (acceptance gate). |
| T-167.1-04 | Repudiation (a figure presented as current when part is not) | the defect being fixed | medium | mitigate | The marker itself, gated on count not amount, pinned by D-15 pins 1-6 each proven RED. |
| T-167.1-05 | Elevation / cross-tenant read | `statusByKeyId` join | low | accept | The join uses two arrays the SSR already scoped to the caller; a foreign key id cannot appear. No new read is added (D-11). |
| T-167.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed (RESEARCH Package Legitimacy Audit: not applicable). If a task appears to need one, STOP. |
</threat_model>

<verification>
- `npx vitest run 'src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts' 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx' --reporter=dot` green.
- `npm run typecheck` clean.
- `npx eslint 'src/app/(dashboard)/allocations/lib/live-holdings-summary.ts' 'src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts' 'src/app/(dashboard)/allocations/components/ScenarioComposer.tsx'` clean.
- `git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/` prints nothing (D-03, D-10, D-11).
</verification>

<success_criteria>
- The composer field's value is unchanged for every book; with no manual value committed, a book with at least one untrusted summed holding shows one muted State A disclosure naming the untrusted dollars beside the field (State B and the absence states land in plan 03).
- Five unit neuter drills were each observed RED and restored under `cmp` (plan 03 carries the four component drills).
</success_criteria>

<output>
Create `.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-01-SUMMARY.md` when done
</output>
