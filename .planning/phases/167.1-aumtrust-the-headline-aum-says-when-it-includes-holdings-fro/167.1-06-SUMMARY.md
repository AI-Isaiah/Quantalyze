---
phase: 167.1-aumtrust
plan: 06
subsystem: release
tags: [release, changelog, windows-ledger, d-14, d-06]
status: complete

requires:
  - phase: 167.1 (plans 01-05)
    provides: the shipped disclosures, the founder's D-06 (b) answer, and plan 05's release-notes handoff
provides:
  - "WINDOWS entry 66 fixed, with a disposition sentence naming D-06 (b) of 2026-09-24"
  - "The final branch-wide D-03 byte-identity reading, taken after plan 05 and review round 3"
  - "v0.89.0.0: one chore(release) commit carrying VERSION, package.json and one unified CHANGELOG entry"
affects: [167.1 ship, CHANGELOG]

actuals:
  tokens: 4900       # chars/4 over the realized diff c7fb96514..7c0e57f7a (19,631 chars)
  tasks: 2
  commits: 2         # MEASURED: git rev-list --count c7fb96514..HEAD at SUMMARY write (the SUMMARY's own docs commit follows)
plan_head_before: c7fb965146dce98407a015012d835d46b7a0f518

tech-stack:
  added: []
  patterns:
    - "A disposition on a fixed WINDOWS entry is written through the ledger module's parse and render, under its lock, after a round-trip identity check"

key-files:
  created: []
  modified:
    - .planning/WINDOWS.md
    - CHANGELOG.md
    - VERSION
    - package.json

key-decisions:
  - "Version 0.89.0.0 (second-segment bump), on the orchestrator's direction for a user-facing disclosure change. The plan named a third-segment bump (0.88.1.0 at today's main). Recorded as a deviation."
  - "The CHANGELOG lists 'Excludes $Z from keys with an unknown sync status' under Added. Plan 05's handoff line 'a dropped holding whose key is missing from the key list is named by no part' was stale after review round 3 WR-01 and is not printed as a current limit."

requirements-completed: [D-14, D-03, D-06, D-10, D-11]

coverage:
  - id: D1
    description: "WINDOWS entry 66 is fixed with the disposition sentence, the table agrees with the JSON, and nothing else moved"
    requirement: D-14
    verification:
      - kind: other
        ref: "plan 06 Task 1 automated check (W66 OK) against /tmp/167.1-06-WINDOWS.pre"
        status: pass
    human_judgment: false
  - id: D2
    description: "Branch-wide D-03/D-10/D-11: queries.ts, the scenario commit route, supabase/ and analytics-service/ show no diff against the merge base"
    requirement: D-03
    verification:
      - kind: other
        ref: "git diff --stat $(git merge-base HEAD origin/main) -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/ (empty)"
        status: pass
    human_judgment: false
  - id: D3
    description: "v0.89.0.0 release commit: VERSION and package.json byte-equal, one unified CHANGELOG entry recording D-06 (b)"
    requirement: D-06
    verification:
      - kind: unit
        ref: "src/__tests__/critical-regressions.test.ts (166 passed)"
        status: pass
    human_judgment: false

duration: 6min
completed: 2026-09-24
---

# Phase 167.1 Plan 06: the release Summary

**WINDOWS entry 66 is fixed with the disposition "disclosed, not changed", and D-06 (b) is recorded in it. A branch-wide re-measure after plan 05 and review round 3 shows no money-math, SSR, migration or Python path changed. v0.89.0.0 ships as one `chore(release)` commit with a unified CHANGELOG entry, and every one of the 58 branch commits before it maps to a bullet.**

## Performance

- **Duration:** about 6 min
- **Started:** 2026-09-24T06:10:59Z
- **Completed:** 2026-09-24T06:16:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- **WINDOWS 66 closed (D-14).**
  - `windows fixed 66` changed exactly what it should: entry 66's `status` and `resolved_at`, its
    rendered table row, `open_count` 51 to 50, `fixed_count` 15 to 16, and `last_updated`.
  - I diffed it against the pre-image and found no collateral. `STATE.md`, `ROADMAP.md` and
    `state.json` were `cmp`-identical to their backups afterwards.
  - The disposition sentence went into `reason` through `parseLedger` / `renderLedger` under
    `withLedgerLock`, after the round-trip identity check passed.
  - `windows status` exits 0. The plan's check prints `W66 OK`.
- **Final byte-identity reading (D-03 / D-10 / D-11).**
  `git diff --stat e325673b4 -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/`
  printed nothing. The merge base is `origin/main`'s tip, since `main` is merged into the branch.
  This supersedes plan 04's interim reading.
- **The release commit (D-14).**
  - VERSION and package.json read `0.89.0.0`, byte-equal, and VERSION has no trailing newline.
  - One CHANGELOG entry, `## [0.89.0.0] - 2026-09-24 — AUMTRUST: …`, uses Added / Changed /
    Fixed (three review rounds) / Security / Tests / Notes.
  - Its Notes record D-06 as answered (b) by the founder on 2026-09-24.
  - The commit is `chore(release): v0.89.0.0 — AUMTRUST` and touches exactly those 3 files.

## Task Commits

1. **Task 1: close WINDOWS 66 and re-prove D-03 branch-wide:** `3bba699b1` (docs)
2. **Task 2: the release commit:** `7c0e57f7a` (chore(release))

## CHANGELOG cross-check: 58 of 58 mapped

The command was `git log $(git merge-base HEAD origin/main)..HEAD~1 --oneline --no-merges`. It gives
the 57 commits of plans 01-05, the reviews and the verification, plus this plan's Task 1 commit.
That is **58**. The three `origin/main` merges bring in other phases' work only, and a Notes
bullet names them.

| Theme (entry section) | Commits |
|---|---|
| Added: State A, footer, State B, D-06 (b) + State C | `7eff1d43a`, `73d18979f`, `56fa60397`, `943e72d9f` |
| Added / Fixed R3: unknown-status excludes part | `82d3b2198` |
| Added / Fixed R1–R2: unavailable counts, unknown-status part, Holdings row marker | `e58ed56fe`, `6a8dd2635`, `f8941d3b1`, `e8eb1c518` |
| Changed: closed-sets prose + ROADMAP correction (D-12/D-13) | `7e3360f11` |
| Changed / Fixed R3: rename, comments, State 6 note, UI-SPEC rows | `bd82cc894`, `4d0feade8`, `d443ea916`, `67694814a` |
| Fixed R1 | `5ed4bdbc1`, `950a3d0af`, `4ed071a1b`, `9d1fc716b`, `c32d0d9c3` |
| Fixed R2 | `378c3fb9c`, `117d0fdd0`, `0620a4a6f`, `952340138`, `ad3b11d16`, `470af9fde`, `1fa81224d`, `7bf621932` |
| Security: count-only Sentry capture | `5673eb381` |
| Tests | `197bd3bf0`, `762187821`, `fa2a77460`, `8ee560cd8` |
| Notes: WINDOWS 66 closed | `3bba699b1` |
| Notes: the planning record (context, research, UI-SPEC, plans, SUMMARYs, state records, reviews, fix reports, verification, security, the founder answers) | `20d812977`, `a3f7a55bf`, `de0ded861`, `cf994639e`, `84e40f506`, `b64cb3002`, `2c71b2c37`, `3404f717c`, `e39a141e3`, `3aebd2571`, `f7f672607`, `957378795`, `983d60068`, `eb1758fab`, `e00220385`, `4e4eff2ac`, `7e1614c14`, `0c8b94950`, `5d62a1a60`, `0c27a029e`, `b0796651e`, `d7b47afc7`, `1d4e21306`, `1036b69be`, `129d8852a` |

That is 4 + 1 + 4 + 1 + 4 + 5 + 8 + 1 + 4 + 1 + 25 = 58. No commit is unmapped.

## Files Created/Modified

- `.planning/WINDOWS.md`: entry 66 fixed, with its disposition. The table and the JSON were
  re-rendered together.
- `CHANGELOG.md`: the unified `0.89.0.0` entry.
- `VERSION`, `package.json`: `0.89.0.0`.

## Decisions Made

See `key-decisions`. The D-06 letter and date in both the ledger and the CHANGELOG are copied
from plan 05's handoff, which records the founder's own reply of 2026-09-24.

## Deviations from Plan

**1. [Rule 1: orchestrator-directed] The version is 0.89.0.0, not a third-segment bump**
- **Found during:** Task 2.
- **Issue:** The plan says to take the higher of HEAD and `origin/main` and bump the third segment.
  That gives `0.88.1.0` today, since `main` is `0.88.0.0` after 167.2. The orchestrator directed
  `0.89.0.0` for a user-facing disclosure change.
- **Fix:** Used `0.89.0.0`, byte-equal in both files. `npm version` was not used, and
  `package-lock.json` was not touched.
- **Committed in:** `7c0e57f7a`

**2. [Rule 1: stale handoff line] The plan 05 handoff's "named by no part" limit is not printed**
- **Found during:** Task 2, from the verifier's note in `167.1-VERIFICATION.md`.
- **Issue:** Review round 3 WR-01 (`82d3b2198`) fixed that limit after the handoff was written.
- **Fix:** The CHANGELOG lists `Excludes $Z from keys with an unknown sync status` under Added and
  says the holding "used to vanish". The limit does not appear under Notes.

**3. [Precision] The release commit message was amended once before any push**
- It first said "57 branch commits". The measured count before the release commit is 58, because
  this plan's Task 1 commit is included. The amended message says 58. The tree did not change.

**Total deviations:** 3. **Impact:** none on the money math. Every other true known limit
(the D-20 residual, the State 6 exact equality, the holdingScopeKey collapse routed to
167.1.1, the soft-disconnected question, U-05 signs, D-17, `minAumGate`, founder browser checks)
is carried in Notes.

## Verification

| Gate | Result |
|---|---|
| Task 1 automated check | `W66 OK`, D-03 stat empty, hygiene `OK` |
| `windows status` | exit 0 |
| `npx vitest run src/__tests__/critical-regressions.test.ts` | 166 passed, exit 0 (node_modules symlinked, removed before commit) |
| `npx vitest run 'src/app/(dashboard)/allocations' src/lib/closed-sets` at the release head's source | 135 files, 2101 passed |
| VERSION equals package.json version; top CHANGELOG entry is `[0.89.0.0]` | OK |
| Skip-trailer sweep: CHANGELOG.md, both commit messages | 0 hits; the plan's python check prints `OK` |
| `gitleaks dir` on a copy of CHANGELOG.md with `.gitleaks.toml` | no leaks found |
| `check-planning-hygiene` before each commit | OK |
| New entry: `D-06 is OPEN` count / `option (b)` count | 0 / 3 |

⚠️ **`npm run lint` was not run.** This plan changes no lintable source. It touches only
`.planning/WINDOWS.md`, `CHANGELOG.md`, `VERSION` and `package.json`, and `package.json` parses
as JSON. This is a declared skip, not a pass.

## Issues Encountered

None. No DB command was run and nothing was pushed.

## User Setup Required

None.

## Next Phase Readiness

The phase stays `human_needed` until the three browser checks in `167.1-VERIFICATION.md` are done.
They will be run after deploy: M1 and M2 wrap at 320px and 200% zoom, and the copy read-through.
The phase is not marked complete here. It is ready for `/gsd-ship`.

## Self-Check: PASSED

- FOUND: `.planning/WINDOWS.md` (entry 66 `fixed`), `CHANGELOG.md` (`## [0.89.0.0]`), `VERSION`
  (`0.89.0.0`), `package.json` (`0.89.0.0`).
- FOUND commits: `3bba699b1`, `7c0e57f7a`.
