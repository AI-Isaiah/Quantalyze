# Phase 167.1: AUMTRUST - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-23
**Phase:** 167.1-aumtrust
**Mode:** `--auto` (autonomous; founder not prompted). Every area auto-selected; each choice taken
from repo evidence at 9fd60115 and recorded in CONTEXT.md.
**Areas discussed:** Which figure is flagged, Money math, Reconcilability, Revoked-key absence,
Rendering rules, Process

---

## Which figure is flagged

| Option | Description | Selected |
|--------|-------------|----------|
| KPI strip AUM cell | The ROADMAP's wording | |
| Re-add an AUM cell to the KPI strip, flagged | Reverses Phase 64 PRESENT-01 | |
| The composer's live-holdings total (field seed + override note) | The only rendered holdings-derived dollar total, measured | ✓ |

**Choice:** [auto] composer live-holdings total (D-01). The KPI strip has no AUM cell since Phase 64,
and `liveBaselineMetrics.aum` has no renderer.

## Money math

| Option | Description | Selected |
|--------|-------------|----------|
| Keep total, flag contribution | Founder decision 2026-09-22 | ✓ |
| Exclude untrusted holdings | Rejected by the founder | |

**Choice:** founder decision honoured (D-02, D-03). Not re-litigated.

## Reconcilability

| Option | Description | Selected |
|--------|-------------|----------|
| Compute the untrusted amount in the same pass as the total | Subset by construction | ✓ |
| Compute it separately from `holdingsSummary` | Can drift from the displayed total | |

**Choice:** [auto] same pass / one shared helper (D-04).

## Revoked-key absence

| Option | Description | Selected |
|--------|-------------|----------|
| (a) Leave exclusion, record it | Composer AUM stays the modelled book | |
| (b) Disclose the exclusion | Additive "excludes $Y" sentence | recommended |
| (c) Include and flag | Breaks Phase 151 F2 reconciliation | |

**Choice:** OPEN — founder's call (D-06), planned behind a `checkpoint:decision`, not blocking the rest.

## Rendering rules

| Option | Description | Selected |
|--------|-------------|----------|
| Render when any summed holding has an untrusted key | Honest for ≤ 0 derivative equity | ✓ |
| Render only when amount > 0 | Hides untrusted zero/negative contributions | |

**Choice:** [auto] D-07; one marker on whichever element shows the live total (D-08); tone to the UI-SPEC (D-09).

## Process

**Choice:** no migration expected (D-11, 3-reviewer gate if one appears); stale comment + ROADMAP
premise corrected with lineage (D-12, D-13); release commit (D-14); fail-able tests (D-15).

## Claude's Discretion

- Helper shape for the single-pass sum; test-file placement.

## Deferred Ideas

- Existing Phase 151 AUM TODOS items; the dead `minAumGate`.
