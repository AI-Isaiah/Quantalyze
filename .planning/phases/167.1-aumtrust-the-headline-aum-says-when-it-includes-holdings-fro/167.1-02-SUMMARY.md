---
phase: 167.1-aumtrust
plan: 02
subsystem: ui
tags: [react, vitest, allocations, open-positions, closed-sets]

requires:
  - phase: 167-credtrust
    provides: "isUntrustedKeySyncStatus, UNTRUSTED_KEY_SET_NOUN, untrustedKeyChipLabel (src/lib/closed-sets.ts)"
provides:
  - "OpenPositionsTable: one footer pass yielding totalUnrealized, untrustedUnrealized, untrustedCount"
  - "open-positions-untrusted-note: a muted tfoot row under the P&L total, rendered on the untrusted count"
  - "describe '[167.1] AUMTRUST — OpenPositionsTable footer qualifier' (8 cases)"
affects: [167.1-06]

actuals:
  tokens: 1880     # chars/4 over the added lines of the realized diff
  tasks: 2
  commits: 2
plan_head_before: e39a141e305e028c4ab4030e1cab23de9e534b68

tech-stack:
  added: []
  patterns:
    - "Single-pass footer accumulator: a displayed total and its disclosed subset come from one loop with one finite-else-0 rule"

key-files:
  created: []
  modified:
    - "src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx"
    - "src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx"

key-decisions:
  - "167.1-02: the footer pass calls isUntrustedKeySyncStatus directly (added to the existing closed-sets import) instead of untrustedKeyChipLabel(...) !== null, since the footer needs membership only, not a label. Both call the same predicate."
  - "167.1-02: the reduce became a plain for loop over three let accumulators. A reduce over an object accumulator would have met the plan's one-pass rule too; the loop reads more plainly."

patterns-established:
  - "The Open Positions footer discloses the untrusted part of its total and never subtracts it"

requirements-completed: [D-16, D-17, D-03, D-04, D-07, D-09, D-11, D-15]

coverage:
  - id: M2-1
    description: "A sign_in_failed position's P&L stays in the footer total and a muted sentence under it names that P&L"
    requirement: "D-16"
    verification:
      - kind: automated_ui
        ref: "src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx#[167.1] tracer"
        status: pass
    human_judgment: false
  - id: M2-2
    description: "Both untrusted statuses, the healthy control, a mixed table, a negative sum (U+2212) and a null P&L are pinned; tone is muted with no role"
    requirement: "D-07"
    verification:
      - kind: automated_ui
        ref: "src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx#[167.1] AUMTRUST (8 cases)"
        status: pass
      - kind: other
        ref: "four neuter -> RED -> restore drills on OpenPositionsTable.tsx, each restored and cmp-verified"
        status: pass
    human_judgment: false
  - id: M2-3
    description: "ExposureByClass, portfolio-exposure and the HoldingsTable precedent are untouched"
    requirement: "D-17"
    verification:
      - kind: other
        ref: "git diff --stat $(git merge-base HEAD origin/main) on the three paths (empty)"
        status: pass
    human_judgment: false
  - id: M2-4
    description: "The qualifier stays readable inside ResponsiveTable at 320px"
    verification: []
    human_judgment: true
    rationale: "The plan marks this truth as a backstop. The colSpan={7} left-aligned cell is asserted structurally (colspan attribute), but readability at 320px needs a human eye."

duration: 3min
completed: 2026-09-24
status: complete
---

# Phase 167.1 Plan 02: Open Positions footer qualifier Summary

**The Open Positions footer "Total unrealized P&L (equity contribution)" keeps summing every derivative row, and when any row comes from a key needing attention a muted row under it now reads "Includes +$X from keys needing attention.", with the total and X computed in one pass.**

## Performance

- **Duration:** about 3 min
- **Started:** 2026-09-24T01:45:30Z
- **Completed:** 2026-09-24T01:47:41Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- `OpenPositionsTable`: the `rows.reduce` for `totalUnrealized` became one `for` loop that yields `totalUnrealized`, `untrustedUnrealized` and `untrustedCount`, all under the same finite-else-0 rule (D-04). The total's value is unchanged and still includes untrusted rows (D-03). The untrusted test is `isUntrustedKeySyncStatus`, which was added to the existing `@/lib/closed-sets` import. The pass carries a comment in the `HoldingsTable` lockstep voice.
- A new `<tr className="bg-page/40">` after the total row, rendered only when `untrustedCount > 0`. It holds one `colSpan={7}` cell with `data-testid="open-positions-untrusted-note"` and class `px-4 pb-2 text-xs text-text-muted`. The text is `Includes {formatPnl(untrustedUnrealized)} from {UNTRUSTED_KEY_SET_NOUN}.`, so the part uses the same sign convention as the total it qualifies. It has no `pnlColor`, no role and no uppercase.
- Tests: a `[167.1] AUMTRUST — OpenPositionsTable footer qualifier` describe block with 8 cases: the tracer, an `it.each` over both untrusted statuses, the healthy control, the mixed table, the negative case, the null case and the tone check. Every oracle is a typed literal.

## Task Commits

1. **Task 1 (tracer): a sign_in_failed position stays in the footer total and the footer says so**: `73d18979f` (feat). TDD: the tracer was written first and went RED with `TestingLibraryElementError: Unable to find an element by: [data-testid="open-positions-untrusted-note"]`. The `+$1,300` total assertion before it already passed, so the test failed for the right reason. It went GREEN after the change. Tracer feedback gate (auto mode): the `<verify>` was re-run end to end and printed OK before any expansion work.
2. **Task 2 (expansion): statuses, absence and edge cases**: `762187821` (test). Four-file `<verify>`: 4 files, 35/35 passed, OK. The `-t "167.1"` count is 8, which meets the plan's floor of 7.

## Neuter drills (Task 2): neuter, RED, restore

Each drill used a `cp` byte backup of `OpenPositionsTable.tsx`, one scripted edit (confirmed by `cmp` to have changed the file), a vitest run on the `[167.1]` block, a restore from the backup, and a `cmp` confirming the restored file was byte-identical. After the drills, `git diff --stat` on the file was empty.

| # | Neuter | Target | Pasted RED line | Restore |
|---|---|---|---|---|
| 1 | untrusted rows excluded from `totalUnrealized` | D-03 | `→ expected '+$1,000' to be '+$1,300' // Object.is equality` and `→ expected '+$2,500' to be '+$3,100'` (2 failed: tracer, mixed table) | cmp IDENTICAL |
| 2 | row gated on `untrustedUnrealized > 0` | D-07 | `→ Unable to find an element by: [data-testid="open-positions-untrusted-note"]` (2 failed: negative, null P&L) | cmp IDENTICAL |
| 3 | amount rendered with the file's unsigned `formatUsd` | sign literals | `→ expected 'Includes $300 from keys needing atten…' to be 'Includes +$300 from keys needing atte…'`, and for the negative case `expected 'Includes -$1,235 …' to be 'Includes −$1,235 …'` (6 failed) | cmp IDENTICAL |
| 4 | row rendered unconditionally (`true ? (`) | healthy control | `expect(element).not.toBeInTheDocument()` / `expected document not to contain element, found <td` (1 failed: healthy control) | cmp IDENTICAL |

## Files Created/Modified

- `src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx`: the widened closed-sets import, the single footer pass and the `open-positions-untrusted-note` row.
- `src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx`: the `[167.1]` describe block and its local `footerTotal` helper.

## Verification (plan-level)

- Four-file vitest run (surfaces, `OpenPositionsTable.all-columns`, `HoldingsTabPanel.spot-derivative-split`, `HoldingsTabPanel.exposure`): 35/35 passed.
- `npm run typecheck`: exit 0, 0 `error TS`.
- `npx eslint` on both files: exit 0.
- Acceptance greps on `OpenPositionsTable.tsx`: test id 1; `"keys needing attention"` literal 0; `UNTRUSTED_KEY_SET_NOUN` 2; `rows.reduce(` 0; `rows.filter(` 0. In the test file, `−\$1,235` appears 1 time.
- D-17: `git diff --stat $(git merge-base HEAD origin/main) -- ExposureByClass.tsx src/lib/portfolio-exposure.ts HoldingsTable.tsx` printed nothing.
- Neighbouring suites: none of the three greps for `tfoot`, `Total unrealized`, `getAllByRole("row")` or `querySelectorAll("tr")` matched. **No neighbouring assertion was adjusted.**

## Decisions Made

- The footer pass calls `isUntrustedKeySyncStatus` directly rather than `untrustedKeyChipLabel(...) !== null`. The footer needs membership, not a label, and the plan allowed either. Both go through the same predicate.
- The reduce became a `for` loop over three `let` accumulators. It is the plainer of the two shapes the plan allowed.

## Deviations from Plan

None in the code. There is one note on the acceptance measurement:

- **Acceptance grep `=== *"(revoked|sign_in_failed)"` returns 1, not 0.** The one match is the pre-existing Phase 167 comment above the closed-sets import (`the SEVENTH former \`=== "revoked"\` equality`). The same grep over the file at this plan's base, `git show HEAD:…`, already returned 1 before any edit. No code equality on the status column exists. The comment belongs to another phase and records why the equality was removed, so it was left untouched (surgical-change rule). The plan's expected 0 was off by that comment.

## Issues Encountered

None.

## Known Stubs

None.

## User Setup Required

None. No external service configuration is required.

## Next Phase Readiness

- Plan 06 (ship) can cite `open-positions-untrusted-note` and the `[167.1]` describe block in the CHANGELOG entry.
- The 320px readability truth is a human backstop (coverage M2-4) for the phase verifier.

---
*Phase: 167.1-aumtrust*
*Completed: 2026-09-24*

## Self-Check: PASSED
