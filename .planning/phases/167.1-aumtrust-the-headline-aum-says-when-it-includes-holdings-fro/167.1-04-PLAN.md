---
phase: 167.1-aumtrust
plan: 04
type: execute
wave: 3
depends_on: [167.1-01, 167.1-02, 167.1-03]
files_modified:
  - src/lib/closed-sets.ts
  - .planning/ROADMAP.md
autonomous: true
requirements: [D-12, D-13, D-03, D-10, D-11, D-17]
estimate:
  tokens: 45000     # revised 2026-09-23: WINDOWS 66 and the release commit moved to plan 06 (D-14: the LAST plan releases)
  raw_tokens: 45000
  tasks: 2
  confidence: low   # DERIVED: estimate-calibration reports sample_count 0, factor 1
must_haves:
  truths:
    - "D-12: the block comment above UNTRUSTED_KEY_SYNC_STATUSES no longer says the predicate does not reach a headline AUM or that flagging it is deferred; it names the real consumers after this phase (summarizeLiveHoldings feeding scenario-aum-untrusted-note, and the OpenPositionsTable footer qualifier), says the totals are disclosed and not changed, states liveBaselineMetrics.aum is unrendered, and cites Phase 167.1 — while keeping the SFH-M2 lineage token so the test docblock's cross-reference stays valid."
    - "D-13: the ROADMAP `### Phase 167.1` entry keeps its original Goal sentence as lineage and carries a dated '⛔ CORRECTED 2026-09-23' note naming the real surfaces (composer live-holdings total, Open Positions footer), the absent KpiStrip AUM cell (Phase 64 PRESENT-01), that no money number changes (D-03), and the ExposureByClass exclusion (D-17)."
    - "D-03 / D-10 / D-11 (static, interim): across the branch as it stands after plans 01-03, src/lib/queries.ts, src/app/api/allocator/scenario/commit/route.ts, supabase/ and analytics-service/ are byte-unchanged — measured before the founder checkpoint, and re-measured by plan 06 after it."
    - "D-14 is NOT this plan's: the release commit and WINDOWS entry 66 (which D-14 puts in the same plan as the release) are plan 06, the last plan, so the one chore(release) commit carries the D-06 outcome (revision 2026-09-23, checker context_compliance)."
  artifacts:
    - path: "src/lib/closed-sets.ts"
      provides: "corrected prose above UNTRUSTED_KEY_SYNC_STATUSES (no code change)"
    - path: ".planning/ROADMAP.md"
      provides: "dated premise correction inside ### Phase 167.1"
  key_links:
    - from: "src/lib/closed-sets.untrusted-key-status.test.ts docblock"
      to: "the SFH-M2 note in src/lib/closed-sets.ts"
      via: "the SFH-M2 token kept in the rewritten note"
---

<objective>
Correct the two places that still state the false "headline AUM" premise (D-12 source prose, D-13
ROADMAP), and prove the money math is byte-unchanged across the branch before the founder
checkpoint.

Purpose: after plans 01 and 02 the predicate reaches two rendered totals, so a comment saying it
reaches none, and a ROADMAP goal naming a KPI cell that has not existed since Phase 64, would send
the next reader the wrong way.

Output: corrected prose and a recorded byte-identity reading. This is the LAST autonomous plan
before plan 05's founder checkpoint. The release commit (D-14) and WINDOWS entry 66 are NOT here:
revision 2026-09-23 moved them to plan 06, the last plan, so that one `chore(release)` commit
carries the final CHANGELOG including the D-06 outcome (D-14 "the last plan writes the release
commit", and D-14 puts WINDOWS 66 in that same plan).

⚠️ Execution consequence, stated so nobody reads it as a stall: `/gsd-execute-phase 167.1` runs
plans 01-04 autonomously and stops at plan 05's `checkpoint:decision`; plan 06 (the release) waits
for the founder's answer. The phase cannot ship before that answer either way — plan 05 is
`autonomous: false` — so releasing first would gain nothing and would put later CHANGELOG edits
outside the release commit.

Spec-less probe fallback: skipped — no requirement IDs to probe (visible skip).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-CONTEXT.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-PATTERNS.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-01-SUMMARY.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-02-SUMMARY.md
@.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-03-SUMMARY.md
</context>

<repo_guardrails>
⛔ Work only in the `feat/167.1-aumtrust` worktree; stage by explicit path, never `git add -A`.
⛔ **Never write a CI skip trailer in a commit message or in CHANGELOG prose**, not even to deny it
— GitHub honours the token anywhere and a squash carries it into `main`. Say "the skip trailer".
⛔ **No credential-shaped literal, real key id, real strategy name, home path or username** in any
committed text. The repo and `.planning/` are public.
⛔ **Never `npm version`** (it rewrites the lockfile and can normalise to 3 digits).
⛔ **`gsd-tools` state and roadmap handlers clobber `STATE.md` / `ROADMAP.md`.** Hand-edit the
ROADMAP with a scoped `Edit`, never `Write`. Before any `gsd-tools` call, `cp` a byte backup of
every ledger it could touch; afterwards `git diff` and revert only the collateral it caused.
⛔ Restore only from a `cp` byte backup verified with `cmp`. Never `git checkout --`, `git restore`
or `git stash`.
⛔ Cite by SYMBOL, never by line number.
⚠️ Run `npm run check:planning-hygiene` after every `.planning/` write; ⛔ never read its result
with `tail -1` (the count line and the OK line are different lines).
</repo_guardrails>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the false headline-AUM premise is corrected wherever it is stated" — source prose (D-12) and ROADMAP (D-13)</name>
  <files>src/lib/closed-sets.ts, .planning/ROADMAP.md</files>
  <read_first>
    - `src/lib/closed-sets.ts`: grep `WHAT THIS PREDICATE DOES NOT REACH` and read that paragraph through the next `⛔ THE EQUALITY SHAPE WAS THE DEFECT` paragraph; also grep `UNTRUSTED_KEY_SET_NOUN =` for the noun's own comment voice.
    - `src/lib/closed-sets.untrusted-key-status.test.ts` file docblock (it cross-references the SFH-M2 note).
    - `.planning/ROADMAP.md`: grep `### Phase 167.1` and read that section only; grep `CORRECTED 2026-09-09` for the house correction voice.
    - 167.1-CONTEXT.md D-01, D-03, D-12, D-13, D-16, D-17; 167.1-01-SUMMARY.md, 167.1-02-SUMMARY.md and 167.1-03-SUMMARY.md (the symbols and test ids that actually shipped).
  </read_first>
  <action>
    (a) D-12 — rewrite ONLY the paragraph headed "WHAT THIS PREDICATE DOES NOT REACH (review round 1, SFH-M2 …)" in `src/lib/closed-sets.ts`, in the file's own correction voice. Keep the `SFH-M2` token and the "an earlier version of this comment claimed otherwise" lineage. New content: after Phase 167.1 the predicate DOES reach the two rendered holdings-derived dollar totals, as a DISCLOSURE — `summarizeLiveHoldings` (`allocations/lib/live-holdings-summary.ts`) feeds the composer's `scenario-aum-untrusted-note`, and the `OpenPositionsTable` footer pass feeds `open-positions-untrusted-note`; both totals keep their value and name the untrusted part (founder decision 2026-09-22, keep the total and flag it). State that `src/lib/queries.ts`'s `totalAum` still sums every holding with no key-status test, and that `liveBaselineMetrics.aum` is not rendered anywhere (Phase 64 PRESENT-01 removed the KPI-strip AUM cell), so that sum is not a surface. State that in book mode a `revoked` key's holdings are outside the composer's summed set because `isPerKeyDailiesEligibleKey` excludes them, and that disclosing that absence is Phase 167.1 D-06 (a founder decision). Remove the sentence that deferred the flag to a follow-up. ⛔ Touch no code and no other paragraph in this file (Rule 3). Leave `closed-sets.untrusted-key-status.test.ts` untouched: its sentence stays true and its SFH-M2 cross-reference stays valid.
    (b) D-13 — with a scoped `Edit` inside `### Phase 167.1` only, keep the `**Goal:**` sentence byte-unchanged as lineage and append after it a `⛔ **CORRECTED 2026-09-23 (Phase 167.1 D-01/D-13).**` note: the KPI strip has had no AUM cell since Phase 64 PRESENT-01 and `liveBaselineMetrics.aum` is unrendered; the holdings-derived dollar totals an allocator sees are the Scenario composer's PORTFOLIO AUM field / override note and the Open Positions footer (D-16, added by research); both now carry the disclosure; `ExposureByClass` is out of scope because it reports exposure and would need a new projection (D-17). In the `**Requirements**` line, keep its text and add a dated correction that the phase changes NO money number — it discloses (D-03) — so the "money-number change" framing is lineage, not current. ⛔ Do not touch the `**Plans:**` count or the plan list, and no other phase section.
  </action>
  <verify>
    <automated>test "$(grep -c 'booked as a' src/lib/closed-sets.ts || true)" = 0 && test "$(grep -c '167\.1' src/lib/closed-sets.ts || true)" -ge 1 && test "$(grep -c 'SFH-M2' src/lib/closed-sets.ts || true)" -ge 1 && npx vitest run src/lib/closed-sets.untrusted-key-status.test.ts --reporter=dot > /tmp/167.1-04-t1.log 2>&1 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167.1-04-t1.log && npm run check:planning-hygiene > /tmp/167.1-04-t1-hyg.log 2>&1 && echo OK</automated>
    <fails_when>no "OK" printed: the deferral sentence is still present, "167.1" or "SFH-M2" is absent from closed-sets.ts, the closed-sets test exits non-zero or reports 0 passed, or the planning-hygiene check exits non-zero</fails_when>
    <automated>python3 -c "import re,sys; s=open('.planning/ROADMAP.md',encoding='utf-8').read(); m=re.search(r'### Phase 167\.1:.*?(?=\n### Phase )', s, re.S); b=m.group(0) if m else ''; ok=('CORRECTED 2026-09-23' in b) and (\"The allocator KPI strip's headline AUM stops presenting\" in b); print('OK' if ok else 'MISSING'); sys.exit(0 if ok else 1)"</automated>
    <fails_when>prints MISSING or exits 1: the dated correction is absent from the 167.1 section, or the original Goal sentence was not kept as lineage</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff "$(git merge-base HEAD origin/main)" -- src/lib/closed-sets.ts > /tmp/167.1-04-cs.diff` exits 0 and every changed line in `/tmp/167.1-04-cs.diff` starts with `+//` or `-//` (prose only): `grep -cE '^[+-][^+/-]' /tmp/167.1-04-cs.diff` prints 0.
    - `git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/closed-sets.untrusted-key-status.test.ts` prints nothing.
    - `git diff "$(git merge-base HEAD origin/main)" -- .planning/ROADMAP.md > /tmp/167.1-04-rm.diff` exits 0 and every hunk header in it falls inside `### Phase 167.1` (checked by reading the diff; recorded in the SUMMARY).
  </acceptance_criteria>
  <done>No tracked text claims the predicate reaches no rendered total or that the phase changes money; both corrections keep lineage.</done>
</task>

<task type="auto">
  <name>Task 2: Interim byte-identity gate — prove plans 01-03 changed no money math before the founder checkpoint (D-03, D-10, D-11)</name>
  <files>(none — measurement only; the output is recorded in 167.1-04-SUMMARY.md)</files>
  <read_first>
    - 167.1-01-SUMMARY.md, 167.1-02-SUMMARY.md, 167.1-03-SUMMARY.md (what each plan says it touched).
    - `git log "$(git merge-base HEAD origin/main)"..HEAD --stat` (what the branch actually touched).
  </read_first>
  <action>
    This is the last autonomous plan before plan 05's founder checkpoint, so it proves — while nobody is waiting on the answer — that the reviewable part of the phase changed no money number. Run the branch-wide byte-identity gate below over `src/lib/queries.ts`, `src/app/api/allocator/scenario/commit/route.ts`, `supabase/` and `analytics-service/`, and record its (empty) output in the SUMMARY. Then list every non-`.planning/` path the branch touched (`git diff --name-only "$(git merge-base HEAD origin/main)" HEAD`) and confirm each is one the plans 01-04 `files_modified` lists name; a path no plan names is a defect to report, not to absorb.
    ⚠️ This gate is re-run by plan 06 after the D-06 answer, because option (b) in plan 05 edits source; this interim reading does not replace that one.
  </action>
  <verify>
    <automated>BASE="$(git merge-base HEAD origin/main)" && git diff --stat "$BASE" -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/ > /tmp/167.1-04-t2.stat 2>&1 && test ! -s /tmp/167.1-04-t2.stat && npx vitest run 'src/app/(dashboard)/allocations/lib/live-holdings-summary.test.ts' 'src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx' 'src/app/(dashboard)/allocations/components/untrusted-key-status.surfaces.test.tsx' src/lib/closed-sets.untrusted-key-status.test.ts --reporter=dot > /tmp/167.1-04-t2.log 2>&1 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167.1-04-t2.log && grep -qE 'Tests +[0-9]+ passed' /tmp/167.1-04-t2.log && echo OK</automated>
    <fails_when>no "OK" printed: git diff errors, the stat file is non-empty (a money-math, SSR, migration or Python path changed on the branch), any of the four phase test files fails, or the run reports 0 passed / no "Tests N passed" line</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the empty stat output and the `git diff --name-only "$(git merge-base HEAD origin/main)" HEAD` list, with every path mapped to the plan that names it.
    - `git status --short` shows no change made by this task (it is measurement only).
  </acceptance_criteria>
  <done>The branch, as it stands before the founder checkpoint, provably changes no money math; the four phase test files are green.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan)

| Symbol / id | Kind | Location |
|---|---|---|
| corrected SFH-M2 paragraph | prose above `UNTRUSTED_KEY_SYNC_STATUSES` | `src/lib/closed-sets.ts` |
| `⛔ CORRECTED 2026-09-23` note | ROADMAP lineage correction | `.planning/ROADMAP.md` `### Phase 167.1` |
| interim byte-identity reading | recorded measurement | `167.1-04-SUMMARY.md` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public | the repo and `.planning/` are public; CHANGELOG prose is scanned by gitleaks on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.1-10 | Information Disclosure | closed-sets.ts / ROADMAP prose | high | mitigate | Synthetic ids only; no credential-shaped literal; `npm run check:planning-hygiene` after every `.planning/` write. |
| T-167.1-12 | Repudiation (a claim the phase changed money) | branch as reviewed before the checkpoint | medium | mitigate | Task 2's branch-wide byte-identity gate over `queries.ts`, the commit route, `supabase/` and `analytics-service/`, recorded in the SUMMARY; plan 06 re-runs it after plan 05. |
| T-167.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages are installed. If a task appears to need one, STOP. |
</threat_model>

<verification>
- `npm run check:planning-hygiene` green (read the whole output, not `tail -1`).
- `npx vitest run src/lib/closed-sets.untrusted-key-status.test.ts --reporter=dot` green.
- `npm run lint` green.
- Branch-wide: `git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/queries.ts src/app/api/allocator/scenario/commit/route.ts supabase/ analytics-service/` prints nothing.
</verification>

<success_criteria>
- No tracked text still states the false premise; both corrections keep lineage.
- The branch as it stands before the founder checkpoint provably changes no money math.
</success_criteria>

<output>
Create `.planning/phases/167.1-aumtrust-the-headline-aum-says-when-it-includes-holdings-fro/167.1-04-SUMMARY.md` when done
</output>
