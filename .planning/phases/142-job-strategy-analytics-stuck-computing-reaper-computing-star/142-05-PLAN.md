---
phase: 142-job-strategy-analytics-stuck-computing-reaper-computing-star
plan: 05
type: execute
wave: 3
depends_on: ["142-03", "142-04", "142-06"]
files_modified:
  - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
autonomous: true
requirements: [JOB-01, JOB-02, JOB-03]
must_haves:
  truths:
    - "The SQL gate EXECUTEs the REAL deployed cron.job.command (never a re-typed predicate) and reddens — not green-skips — when the migration is unapplied (the test_retention_orphaned_running green-skip trap is NOT reproduced; the counter-analog test_sync_status_supersede_failed_per_kind is)"
    - "All directional arms hold on TEST: stranded row reaped with the exact four-column outcome; fresh-computed_at+old-stamp REAPED and old-computed_at+fresh-stamp NOT (SC#2 both directions); active-job row survives; NULL-stamp row survives; N+1 stranded rows ⇒ exactly LIMIT reaped per tick — every count SCOPED to the seeded strategy_id set, with all non-seeded computing rows neutralized (stamp NULLed) inside the rollback so shared-TEST-DB state cannot crowd the LIMIT budget"
    - "SC-2b holds behaviorally: driving the REAL mark_compute_job RPCs twice against an in-flight chain does not advance the stamp — using a sentinel pre-set stamp because now() is frozen inside the test transaction (the naive two-call design cannot fail)"
    - "The migration is MCP-applied to TEST (qmnijlgmdhviwzwfyzlc) BEFORE the gate is trusted as evidence, and the RED→GREEN flip across the apply was observed"
  artifacts:
    - path: "supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql"
      provides: "structural (ungated) + behavioral (pg_cron-gated) reaper/bridge gate"
      min_lines: 150
  key_links:
    - from: "supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql"
      to: "cron.job.command for reap_strategy_analytics_stuck_computing"
      via: "SELECT command INTO v_command; EXECUTE v_command (the S-6 oracle)"
      pattern: "EXECUTE v_command"
    - from: "supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql"
      to: "mark_compute_job_done / mark_compute_job_failed"
      via: "real-RPC drive for the stamp-transition and exit-clear arms (never calling the bridge directly)"
      pattern: "mark_compute_job_done"
---

<objective>
Prove the migration on the TEST project: author the SQL gate for the reaper and the bridge's
stamp semantics, MCP-apply migration 20260802120000 to TEST ([BLOCKING] — ordered before any
gate run is trusted), observe the RED→GREEN flip, run the deployed-body mutation set from the
Falsifiability Ledger, and close the phase with the full verification gate.

Purpose: the SQL runtime is where JOB-01's hardest invariant (conditional stamping — SC-2b)
and all of JOB-02's behavior live. CI executes SQL gates ONLY as `supabase/tests/test_*.sql`
against TEST; an unapplied migration must produce a RED gate, not a green skip — a gate that
green-skips when the object under test is absent is not evidence (PATTERNS correction #3).

Output: `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql`, migration applied
to TEST, observed mutation evidence, full phase gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-CONTEXT.md
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-PATTERNS.md
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md

Fixed facts this plan consumes:
- Migration: `supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql`
  (plan 142-04). Jobname `reap_strategy_analytics_stuck_computing`, cadence `*/15 * * * *`,
  LIMIT 25, four-column reap SET, threshold = STRATEGY_ANALYTICS_REAP_THRESHOLD's value.
- TEST project: `qmnijlgmdhviwzwfyzlc` (MCP apply_migration; ⚠️ it stamps now() as the
  migration timestamp — note the filename-vs-stamp drift in the SUMMARY). PROD
  `khslejtfbuezsmvmtsdn` is applied automatically on merge to main — NEVER apply manually.
- Harness: plain PL/pgSQL under `psql -v ON_ERROR_STOP=1`; pgTAP NOT installed; CI
  auto-discovers `supabase/tests/test_*.sql` by glob (no workflow edit); the ci.yml preflight
  rejects psql meta-commands; every RAISE format string is a single literal (no ||).
- ⚠️ Branch protection is OFF (founder decision) — every CI gate is advisory at merge. All
  prose/SUMMARY claims must say "would have caught", never "did stop".
- ⚠️ Frozen-clock trap (planner-derived, load-bearing for SC-2b): each part runs inside its
  own BEGIN…ROLLBACK (per-part framing — see Task 1's Frame), so `now()` is CONSTANT within
  a part's transaction; Part 3's two RPC calls see the same frozen `now()`. A naive
  "call the RPC twice, compare stamps" test CANNOT fail under the unconditional-stamp
  mutation (both calls write the same frozen now()). The SC-2b arm must pre-set a SENTINEL
  stamp (e.g. now() - interval '3 hours') between the two RPC calls and assert the stamp
  still equals the sentinel afterward — the conditional form keeps it; the unconditional
  mutation overwrites it with now() ⇒ mismatch ⇒ RED. Seeds must use clock-offset arithmetic
  (now() - interval '…'), never sleeps.
</context>

<interfaces>
Gate skeleton (composite of the two analogs — the structural part is UNGATED on purpose):

```sql
-- Part 1 (UNGATED — MUST redden when the migration is unapplied; the TDD RED proof):
--   pg_attribute: computing_started_at exists, timestamptz, NOT attnotnull, NOT atthasdef
--   pg_get_functiondef('sync_strategy_analytics_status(uuid)'):
--     positive: conditional CASE stamp, IS DISTINCT FROM, SECURITY DEFINER, search_path,
--               retained 20260710150000 anchors (d\.kind\s*=\s*f\.kind)
--     negative: !~ 'computing_started_at\s*=\s*now\(\)'  (the C-3 unconditional form)
--   pg_cron PRESENT check: IF pg_extension has pg_cron THEN the cron job MUST be registered
--     (absence = RAISE EXCEPTION, never a skip) with schedule = '*/15 * * * *' by string
--     equality (P-10: no hour-band ::INT cast) and the body anchors (positive: public.
--     strategy_analytics, computing_started_at, SKIP LOCKED, computation_warned, the interval
--     literal; negative: computed_at, updated_at, interval '4 hours')
-- Part 2+ (each wrapped BEGIN;…ROLLBACK;, gated ONLY on pg_extension pg_cron absence):
--   behavioral arms, all via SELECT command INTO v_command FROM cron.job …; EXECUTE v_command
```

Seed chain (test_sync_status_supersede_failed_per_kind.sql:98-149 idiom, gen_random_uuid ids):
auth.users → public.profiles → public.strategies → public.strategy_analytics (+ compute_jobs
rows where an arm needs them). Minimal insert precedent: test_metrics_by_basis_write.sql:63.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql</name>
  <files>supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql</files>
  <read_first>
    - supabase/tests/test_retention_orphaned_running.sql (FULL — header contract, EXECUTE-the-deployed-body oracle at :156-164, four-arm directional seed, teardown; and the :71-83 green-skip gates you must NOT copy)
    - supabase/tests/test_sync_status_supersede_failed_per_kind.sql (FULL — the counter-analog: ungated Part 1 that reddens when unapplied, real-RPC drive discipline at :25-32, FK seed chain at :98-149, functiondef anchors at :53-86)
    - supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql (the object under test — read the deployed body, jobname, literal values)
    - supabase/tests/test_metrics_by_basis_write.sql:63 (minimal strategy_analytics seed)
    - .github/workflows/ci.yml:830-975 (sql-tests glob discovery + the psql meta-command preflight)
  </read_first>
  <action>
    Author the gate per the interfaces skeleton. Header states: oracle discipline (EXECUTE the
    deployed cron.job.command, never a re-typed predicate), pgTAP absence, the usage psql
    line, AND the anti-green-skip contract: Part 1 is deliberately ungated and FAILS when the
    migration is unapplied (naming the supersede-test precedent); the ONLY permitted skip is
    pg_cron genuinely absent (local dev), and even then Part 1's column/functiondef
    assertions still run.

    Part 1 — structural (ungated), per the skeleton: column shape via pg_attribute (typed
    timestamptz, nullable, no default); functiondef positive + negative anchors (including
    the retained per-kind supersession anchor so a bad re-base reddens here too); cron
    registration as an ASSERTION under the pg_cron-present condition; schedule string
    equality '*/15 * * * *'; body positive/negative ILIKE anchors including the threshold
    interval literal (typed into this test as a literal — a pinned expectation) and the
    NEGATIVE anchors for computed_at / updated_at / interval '4 hours'.

    Part 2 — reap behavior (BEGIN;…ROLLBACK;, seeds via gen_random_uuid FK chains, arms
    labeled with one-line MUST-reap/MUST-survive comments per the retention-test style; all
    ages via now() - interval arithmetic):
      Shared-TEST-DB isolation (load-bearing — this gate runs on every PR against the shared
      TEST project; this is the recurring concurrent-CI flake class, not a one-time hazard):
      the deployed command is a GLOBAL `ORDER BY computing_started_at ASC LIMIT 25` over the
      whole table, and plan 142-04's backfill stamps every pre-existing TEST `computing` row
      from computed_at — those rows carry OLD stamps that sort AHEAD of these seeds and can
      crowd them out of the 25-row budget (arm A's terminal assertion would then fail for a
      reason unrelated to the reaper). Therefore, INSIDE the BEGIN…ROLLBACK and BEFORE EACH
      `EXECUTE v_command`, neutralize every non-seeded computing row:
        UPDATE public.strategy_analytics
           SET computing_started_at = NULL
         WHERE computation_status = 'computing'
           AND strategy_id <> ALL (<the seeded strategy_id array>);
      NULL-stamp rows are skipped by the deployed predicate itself (the writer-bug skip
      rule), so neutralization uses the reaper's own semantics — no new mechanism — and the
      ROLLBACK guarantees it never persists. Every reap-count assertion in Part 2 is scoped
      to the seeded strategy_id set — NEVER a global count.
      arm A (stranded, the SC-1/SC-1b carrier): computing, computing_started_at = now() -
        interval '100 days', computed_at = now() (FRESH — this row is simultaneously SC#2
        direction 1), computation_warned = TRUE, no compute_jobs rows. After EXECUTE
        v_command assert ALL FOUR columns: status='failed', computation_warned=FALSE,
        computation_error equals the exact literal from the migration (string equality),
        computing_started_at IS NULL. The warned=TRUE seed is what makes the SC-1b launder
        block observable.
      arm B (SC#2 direction 2): computing, computing_started_at = now() (fresh),
        computed_at = now() - interval '100 days' (old) ⇒ MUST survive untouched.
      arm C: computing, old stamp, but ONE active compute_jobs row (status 'running') ⇒ MUST
        survive (the NOT EXISTS conjunct — the safety carrier).
      arm D: computing, computing_started_at IS NULL, no jobs ⇒ MUST survive (writer-bug
        skip rule).
      arm E (LIMIT bound): seed 26 stranded rows in a loop; count reaped rows SCOPED TO
        THE SEEDED strategy_id SET ⇒ first EXECUTE reaps exactly 25 of the seeded 26;
        second EXECUTE reaps the remaining seeded 1 ⇒ bounded AND progressing. Never assert
        a global reaped count — a single pre-existing stranded row would break it
        deterministically.

    Part 3 — stamp transition + exit clears via REAL RPCs (never calling
    sync_strategy_analytics_status directly — the supersede test's header explains why the
    direct call is a vacuum):
      seed a strategy with strategy_analytics at 'pending' and three claimed compute_jobs
      (use the same claim/token mechanics the supersede test uses to drive
      mark_compute_job_done). (3a) mark job1 done ⇒ row resolves computing AND
      computing_started_at IS NOT NULL (transition stamps). (3b — SC-2b, frozen-clock
      design): UPDATE the row's computing_started_at to a sentinel (now() - interval
      '3 hours'); mark job2 done ⇒ row still computing; assert computing_started_at =
      sentinel EXACTLY (the conditional form keeps it; the unconditional mutation overwrites
      with the frozen now() and reddens). (3c) mark job3 done ⇒ terminal resolution via
      branch (b)/(c); assert computing_started_at IS NULL (exit clear). If driving
      mark_compute_job_failed to failed_final is cheap with the same seed idiom, add the
      branch-(b) clear arm too; if not, the branch-(b) clear stays covered by Part 1's
      functiondef anchor — say which in a comment.

    Frame (checker W-4/W-5): PER-PART transactions ONLY — each part opens its own `BEGIN;`
    followed immediately by `SET LOCAL lock_timeout = '5s';` and closes with `ROLLBACK;`
    (the one legitimate ROLLBACK location). NEVER wrap the whole file in an outer BEGIN:
    psql's nested BEGIN emits `WARNING: there is already a transaction in progress` and
    creates NO savepoint, so the FIRST inner ROLLBACK would terminate the outer transaction
    and any later part lacking its own BEGIN would AUTOCOMMIT its seeds onto the shared
    TEST project. Both analogs are per-part only (test_retention_orphaned_running.sql:54/191;
    test_sync_status_supersede_failed_per_kind.sql:98/149, 162/203, 214/268) — T-142-17's
    mitigation names this framing as the control. The `SET LOCAL lock_timeout` bounds the
    row locks the neutralization UPDATE takes on pre-existing computing rows, so two
    concurrent PR gate runs against shared TEST fail loud instead of serializing on an
    unbounded wait (the recurring concurrent-CI flake class). Teardown DELETE FROM
    auth.users belt-and-suspenders, no fixed UUID literals (shared-TEST-DB concurrency), no
    psql meta-commands, every RAISE a single literal with % placeholders.
  </action>
  <verify>
    <automated>bash -c "f=supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql; test -f \$f && grep -q 'EXECUTE v_command' \$f && grep -q 'mark_compute_job_done' \$f && grep -cE '</?(content|invoke|function_calls|antml:|parameter)' \$f | grep -qx 0 && echo gate-shape-ok"</automated>
  </verify>
  <acceptance_criteria>
    - The file contains `EXECUTE v_command` (S-6 oracle) and drives `mark_compute_job_done` for Part 3 — zero direct calls to sync_strategy_analytics_status outside pg_get_functiondef.
    - Part 1 has NO presence-gate RETURN on cron.job absence — job absence RAISEs when pg_cron is present; the only skip is the pg_extension check, and the column/functiondef section runs before it.
    - The SC-2b arm sets a sentinel stamp between the two RPC calls and asserts equality against the sentinel; the frozen-now() rationale appears as a comment.
    - Arm A asserts all FOUR outcome columns including string-equality on computation_error; arm A's seed has computation_warned = TRUE.
    - Arm E seeds 26 stranded rows and asserts, SCOPED to the seeded strategy_id set, exactly 25 seeded rows reaped on tick 1 and the remaining seeded 1 on tick 2 — no global counts anywhere in Part 2.
    - Part 2 neutralizes non-seeded computing rows (`SET computing_started_at = NULL WHERE … strategy_id <> ALL(<seeded ids>)`) inside the rollback before each `EXECUTE v_command`, so arm A's stranded seed cannot be crowded out of the LIMIT-25 budget by pre-existing backfilled TEST rows.
    - No fixed UUID literals; PER-PART BEGIN;…ROLLBACK; framing with NO outer whole-file transaction, and `SET LOCAL lock_timeout = '5s'` immediately after every BEGIN (the count of `SET LOCAL lock_timeout` lines equals the count of BEGIN lines); no psql meta-commands (`grep -E '\\\\(copy|COPY|o|!)' ` finds nothing).
  </acceptance_criteria>
  <done>Gate authored: ungated structural part, five behavioral arms, real-RPC stamp part with the sentinel design.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migration to TEST via MCP — observe RED→GREEN — run deployed-body mutations</name>
  <files>(TEST database state via Supabase MCP; no repo files modified)</files>
  <read_first>
    - supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql (what is being applied)
    - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql (the gate from Task 1)
    - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md (§Falsifiability Ledger rows SC-1, SC-1b, SC-2, SC-2b — the mutations to observe)
  </read_first>
  <action>
    Ordering is the point of this task (schema-push requirement): the gate is NOT evidence
    until the migration is applied to TEST.
    1. RED first: run the gate via psql against `$TEST_SUPABASE_DB_URL` with ON_ERROR_STOP=1
       BEFORE applying. Observe Part 1 fail (missing column) and paste the failure — this is
       the gate's own cannot-green-skip proof.
    2. Apply: Supabase MCP `apply_migration` against TEST project qmnijlgmdhviwzwfyzlc with
       the migration file's content and name. Record that MCP stamps now() as the applied
       timestamp (drift vs the 20260802120000 filename — known behavior, note in SUMMARY).
       NEVER target PROD (khslejtfbuezsmvmtsdn) — PROD applies automatically on merge.
    3. GREEN: re-run the gate; the whole file must pass. Paste the tail output.
    4. Deployed-body mutation observations. CAPTURE FIRST, RESTORE FROM THE CAPTURE —
       never by re-executing the migration block from the repo (a repo-side drift would
       silently land a wrong body on shared TEST, and these mutations run OUTSIDE any
       rollback):
       - BEFORE any mutation, capture and paste into the SUMMARY:
         `SELECT pg_get_functiondef('public.sync_strategy_analytics_status(uuid)'::regprocedure);`
         and `SELECT command, schedule FROM cron.job WHERE
         jobname='reap_strategy_analytics_stuck_computing';`
       - Each mutation: mutate on TEST via MCP execute_sql → run gate → observe RED →
         restore FROM the captured text (cron.unschedule + cron.schedule with the CAPTURED
         command for job-body mutations; CREATE OR REPLACE from the CAPTURED functiondef
         for the bridge mutation) → re-run gate GREEN.
       - SC-1: cron.unschedule + re-schedule the job with the body's 'failed' replaced by
         'pending' ⇒ arm A's terminal assertion RED.
       - SC-1b: re-schedule with the computation_warned = FALSE line deleted ⇒ arm A's
         warned assertion RED (the launder gate).
       - SC-2: re-schedule with computing_started_at replaced by computed_at in the WHERE
         ⇒ the SC#2 directional arms RED (both directions flip: A survives wrongly, B reaps
         wrongly).
       - SC-2b: CREATE OR REPLACE the bridge with the branch-(a) stamp made unconditionally
         now() ⇒ Part 3's sentinel assertion RED (and Part 1's negative functiondef anchor
         RED). Restore from the CAPTURED functiondef.
       - AFTER the final restore: re-SELECT pg_get_functiondef and cron.job.command and
         assert each is BYTE-IDENTICAL to its pre-mutation capture; record the equality
         check in the SUMMARY. A mismatch is a STOP — shared TEST must end in the exact
         captured state.
       Paste every RED and the final GREEN. A mutation that cannot be run is recorded as
       SKIPPED with the reason — never as caught.
    5. Leave TEST in the exact post-migration state (jobname scheduled with the true body;
       bridge at the migration's definition): final full gate run GREEN + a cron.job
       SELECT for the jobname showing schedule '*/15 * * * *'.
  </action>
  <verify>
    <automated>psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY carries, in order: the pre-apply RED paste, the MCP apply confirmation, the post-apply GREEN paste, four mutation REDs (SC-1, SC-1b, SC-2, SC-2b) each with the exact failing RAISE text, and the final restore GREEN.
    - TEST end-state verified: gate green AND `SELECT jobname, schedule FROM cron.job WHERE jobname='reap_strategy_analytics_stuck_computing'` returns one row with '*/15 * * * *'.
    - Pre-mutation captures (pg_get_functiondef + cron.job command/schedule) appear in the SUMMARY, every restore was executed FROM the captured text, and the final re-captures are byte-identical to the pre-mutation captures (equality recorded in the SUMMARY).
    - PROD was never targeted by any MCP call in this task.
  </acceptance_criteria>
  <done>Migration live on TEST, RED→GREEN observed across the apply, all four deployed-body mutations observed RED first-hand and restored.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suites, type gates, ledger reconciliation</name>
  <files>(verification only; no repo files modified beyond the VALIDATION.md status columns if the executor maintains them)</files>
  <read_first>
    - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md (Per-Task Verification Map + Falsifiability Ledger + Sign-Off — the columns to reconcile)
    - CLAUDE.md (coverage gates: pytest --cov-fail-under=80; vitest ratchet lines 82 / stmts 80 / fns 74 / branches 72)
  </read_first>
  <action>
    Run the full phase gate in this order and record each result:
    1. `cd analytics-service && pytest -q` (from analytics-service/ — repo-root runs miss the
       VCR cassette dir and make live broker calls).
    2. `cd analytics-service && mypy --strict .` (zero errors, zero new type-ignores — the
       GSD milestone path runs pytest only, so this is the last line of defense before PR CI).
    3. `npm run lint` (the repo lint script also runs check-admin-route-manifest.ts and
       check-route-contract.ts — plan 142-03 edited four API route files, so this gate is
       load-bearing here; pre-push lint is the house rule), then `npm run typecheck` and
       the full vitest suite (`npm test`; locally add --no-file-parallelism if flakes
       appear — known local behavior).
    4. `npx vitest run src/__tests__/contracts/check-zod-db-check-parity.test.ts` (must be
       untouched-green: this phase adds no enum value).
    5. The SQL gate once more against TEST (freshness).
    Reconcile the Falsifiability Ledger: every row SC-1..SC-5c must be Observed ✅ with
    pasted evidence gathered across plans 142-01 (SC-3), 142-02 (SC-4 structural +
    SC-4b behavioral control pair), 142-03 (SC-5/SC-5b/SC-5c — the m1–m3 stamp mutations),
    142-04 (SC-3b), and this plan (SC-1, SC-1b, SC-2, SC-2b) — or explicitly SKIPPED with reason. Confirm `grep -rn MUTANT` across the repo returns 0 and
    `git status` shows no stray mutation residue. Note in the SUMMARY (advisory-CI honesty):
    these gates WOULD HAVE CAUGHT the mutations; branch protection being off means nothing
    "did stop" anything at merge time. Cadence language rule (all SUMMARY prose): the */15
    cadence is POST-THRESHOLD DETECTION LATENCY — worst case ≈ threshold + 15 min — never
    "bounds how long a user stares at a spinner"; do not restate CONTEXT's pre-threshold
    spinner rationale, and do not weaken the live-poll-overclaim prohibition. List the two operator follow-ups that are OUT of
    execution scope: (a) post-merge PROD check `SELECT jobname, schedule FROM cron.job` and
    the PROD backfill census; (b) the manual wizard render check from VALIDATION.md
    §Manual-Only Verifications.
  </action>
  <verify>
    <automated>cd analytics-service && pytest -q && mypy --strict . && cd .. && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Full pytest green from analytics-service/ with coverage ≥ 80; mypy --strict zero errors; npm run lint zero errors; npm run typecheck zero errors; parity vitest green; SQL gate green on TEST.
    - Ledger reconciliation table in the SUMMARY: 11 mutation rows (SC-1, SC-1b, SC-2, SC-2b, SC-3, SC-3b, SC-4, SC-4b, SC-5, SC-5b, SC-5c — matching the VALIDATION.md ledger 1:1 after its SC-4/SC-4b split) each Observed-with-paste or Skipped-with-reason; zero rows marked caught-without-run.
    - `npm run lint` exits 0 (route-manifest + route-contract checks included).
    - `grep -rn MUTANT` repo-wide returns 0.
    - The two out-of-scope operator follow-ups are listed, phrased as would-have/pending — no claim of PROD verification.
  </acceptance_criteria>
  <done>All suites and type gates green, ledger fully reconciled with first-hand evidence, honest scope notes recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP apply → TEST database | Schema mutation of the shared TEST project; concurrent CI runs use it (seeds must be uuid-random, transactions rolled back) |
| merge to main → PROD database | Auto-apply: the merged migration IS the prod deploy; the gate here is the last pre-merge evidence |
| test seeds → shared TEST DB | Leaked fixture rows pollute other suites (the e2e-seeded pollution incident class) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-16 | Tampering | Gate green-skips while the reaper is unproven (migration unapplied on TEST) | mitigate | Part 1 ungated + the RED-before-apply observation; apply task is [BLOCKING] and ordered before any gate run counts as evidence |
| T-142-17 | DoS/Tampering | Test seeds or mutation residue left on shared TEST DB | mitigate | BEGIN…ROLLBACK framing, gen_random_uuid ids, teardown DELETE, final end-state verification restoring the true cron body and bridge |
| T-142-18 | Repudiation | Verification claims exceed what advisory CI can enforce | mitigate | SUMMARY language rule: "would have caught", never "did stop"; PROD checks listed as pending operator steps, not claimed |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed in this plan |
</threat_model>

<verification>
- Gate RED before apply (observed), GREEN after apply (observed), four deployed-body mutations RED (observed), final GREEN with true body restored.
- Full pytest + mypy --strict + lint + typecheck + parity vitest + SQL gate all green.
- Falsifiability Ledger 100% reconciled (11 rows, SC-1..SC-5c) with pasted evidence or explicit skips.
</verification>

<success_criteria>
- The reaper and the bridge's stamp semantics are proven against the DEPLOYED objects on TEST, with every ledger mutation observed first-hand.
- The gate cannot green-skip: an unapplied migration is a red CI run.
- TEST left in the exact shipped state; PROD untouched until merge.
</success_criteria>

<output>
Create `.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-05-SUMMARY.md` when done, carrying: the RED→GREEN apply evidence, all mutation pastes, the ledger reconciliation table, and the pending operator follow-ups (PROD cron check + manual wizard render check).
</output>
