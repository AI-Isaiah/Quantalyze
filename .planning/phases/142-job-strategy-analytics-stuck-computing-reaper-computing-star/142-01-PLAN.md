---
phase: 142-job-strategy-analytics-stuck-computing-reaper-computing-star
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/services/ingestion/long_fetch.py
  - analytics-service/tests/test_main_worker.py
autonomous: true
requirements: [JOB-03]
must_haves:
  truths:
    - "The job-chain topology (which kind enqueues which follow-on) exists as ONE named module-level constant that the three production enqueue sites READ — the constant is load-bearing, not decorative"
    - "The canonical Python reaper-threshold constant exists as a Postgres interval string, derived from the chain-inclusive ceiling, NOT from batch_size × max_per_kind_timeout (that is the compute_jobs formula — C-6)"
    - "A CI invariant beside TestWatchdogInvariant fails when the threshold constant drops below the chain-inclusive worst case (SC-3), and a sane-upper-bound test fails on a unit typo"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "JOB_CHAIN_FOLLOW_ON topology constant + STRATEGY_ANALYTICS_REAP_THRESHOLD interval constant beside TIMEOUT_PER_KIND; enqueue sites at ~:1857 and ~:4885 read the topology"
      contains: "JOB_CHAIN_FOLLOW_ON"
    - path: "analytics-service/services/ingestion/long_fetch.py"
      provides: "tail_kind selection at :583 reads JOB_CHAIN_FOLLOW_ON instead of inline literals"
      contains: "JOB_CHAIN_FOLLOW_ON"
    - path: "analytics-service/tests/test_main_worker.py"
      provides: "class TestReaperThresholdInvariant — chain-inclusive headroom + sane upper bound"
      contains: "TestReaperThresholdInvariant"
  key_links:
    - from: "analytics-service/services/ingestion/long_fetch.py:583"
      to: "services.job_worker.JOB_CHAIN_FOLLOW_ON"
      via: "function-scoped import (mirrors the existing :43/:74 import style)"
      pattern: "JOB_CHAIN_FOLLOW_ON"
    - from: "analytics-service/tests/test_main_worker.py"
      to: "JOB_CHAIN_FOLLOW_ON + TIMEOUT_PER_KIND"
      via: "invariant computes the chain-inclusive ceiling from the topology, never from the impl's own expression"
      pattern: "JOB_CHAIN_FOLLOW_ON"
---

<objective>
Build the JOB-03 threshold infrastructure on the Python side: a single-source job-chain topology
constant that the three production enqueue sites read, a canonical reaper-threshold constant
(Postgres interval string), and the CI invariants (chain-inclusive headroom + sane upper bound)
beside `TestWatchdogInvariant`.

Purpose: JOB-03 requires the reaper threshold to be derived from `strategy_analytics`'s own
chain math with a CI invariant that can actually fail. Per non-negotiable C-6, the formula
`batch_size × max_per_kind_timeout` is the `compute_jobs` formula and MUST NOT be re-applied —
it under-estimates this table's window by ~4× because a `strategy_analytics` row is `computing`
for a whole multi-hop chain, not one job. Per PATTERNS.md correction #2, a topology constant
that merely restates the enqueue literals without the sites reading it is decorative and will
drift, pinning the JOB-03 oracle to a fiction.

Output: `JOB_CHAIN_FOLLOW_ON` + `STRATEGY_ANALYTICS_REAP_THRESHOLD` in `services/job_worker.py`,
three rewired enqueue sites, `TestReaperThresholdInvariant` in `tests/test_main_worker.py`.
Plans 142-04 (migration literal) and 142-05 (SQL gate) consume the threshold constant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-CONTEXT.md
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-PATTERNS.md

Decisions fixed by this plan set (all five plans share these; do not re-derive):
- Topology single-source choice: **option (a)** from PATTERNS.md item 7 — the three enqueue
  sites READ the constant. Why: it makes the constant load-bearing (a wrong entry changes
  production enqueue behavior and existing job-flow tests catch it), whereas option (b)'s
  match-the-literals gate is a second artifact that can itself rot. `long_fetch.py` already
  imports from `services.job_worker` function-scoped (`:43`, `:74`, `:218`), so placing the
  constant beside `TIMEOUT_PER_KIND` per PATTERNS item 7 creates no circular import.
- Constant names: `JOB_CHAIN_FOLLOW_ON` (topology) and `STRATEGY_ANALYTICS_REAP_THRESHOLD`
  (a Postgres interval STRING — one canonical representation; tests parse it with the existing
  `_watchdog_seconds` helper at `test_main_worker.py:978`).
- Threshold decision (research Open Question 1, resolved here, not inherited): the deployed
  interval MUST exceed the chain-inclusive ceiling — Falsifiability Ledger row SC-3
  ("divide by 10 → below the ceiling → RED") fixes that semantics and is not renegotiable.
  The `NOT EXISTS (active compute_jobs)` conjunct carries reap-correctness (non-negotiable 6);
  the interval is defense-in-depth debounce. Expected landing: ceiling ≈ 43,920 s (~12.2 h)
  → threshold `'16 hours'` (smallest whole 4-hour multiple ≥ 1.25 × ceiling). The executor
  computes the real ceiling and adjusts the number; the rule is fixed.
- The cron jobname `reap_strategy_analytics_stuck_computing` appears ONLY in the migration,
  the SQL gate, and the JOB-07 forbidden-token list — never as a Python constant. Nothing in
  Python may reference it; that absence IS the JOB-07 property.
</context>

<interfaces>
Verified current state (direct reads, 2026-08-02):

```python
# analytics-service/services/ingestion/long_fetch.py:583
tail_kind = "derive_broker_dailies" if is_ledger_backed else "sync_trades"

# analytics-service/services/job_worker.py:1857
_follow_on_kind = "derive_broker_dailies"

# analytics-service/services/job_worker.py:4886 (the args dict — .rpc( is at :4884,
# "enqueue_compute_job" at :4885 — inside _enqueue_csv_analytics)
{"p_strategy_id": strategy_id, "p_kind": "compute_analytics_from_csv"},

# analytics-service/tests/test_main_worker.py:978 (existing parser — REUSE, do not fork;
# test_job_worker_csv_kind.py:118's local _parse_minutes is the drift to avoid, not to copy)
def _watchdog_seconds(interval: str) -> float: ...
```

Target constant shape (goes beside `TIMEOUT_PER_KIND`, `job_worker.py` ~:492):

```python
JOB_CHAIN_FOLLOW_ON: Final[dict[str, tuple[str, ...]]] = {
    # JOB-03: canonical chain topology. Enqueue sites READ this map — see
    # long_fetch.py tail selection and the sync_trades/_enqueue_csv_analytics
    # follow-ons. Tuple order for process_key_long is load-bearing:
    # (ledger-backed tail, trade-backed tail).
    "process_key_long": ("derive_broker_dailies", "sync_trades"),
    "sync_trades": ("derive_broker_dailies",),
    "derive_broker_dailies": ("compute_analytics_from_csv",),
    "compute_analytics_from_csv": (),
    "stitch_composite": (),
}

STRATEGY_ANALYTICS_REAP_THRESHOLD: Final[str] = "16 hours"  # value derived in Task 2
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Declare JOB_CHAIN_FOLLOW_ON + STRATEGY_ANALYTICS_REAP_THRESHOLD and rewire the three enqueue sites to read the topology</name>
  <files>analytics-service/services/job_worker.py, analytics-service/services/ingestion/long_fetch.py</files>
  <read_first>
    - analytics-service/services/job_worker.py:468-500 (TIMEOUT_PER_KIND block + the _DERIVE_BUDGET_DEFAULT_S single-sourcing comment idiom to mirror)
    - analytics-service/services/job_worker.py:1850-1870 (sync_trades follow-on enqueue — current literal)
    - analytics-service/services/job_worker.py:4880-4892 (_enqueue_csv_analytics — current literal)
    - analytics-service/services/ingestion/long_fetch.py:40-80,575-607 (existing function-scoped import style from services.job_worker + the tail_kind selection at :583)
    - analytics-service/main_worker.py:121-124 (BACKFILL_KINDS — the Final-typed constant analog; Final imported from typing)
    - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-PATTERNS.md (item 7 — placement, per-entry trailing-comment house style, and the "decorative constant" risk)
  </read_first>
  <action>
    In `services/job_worker.py`, immediately after the `TIMEOUT_PER_KIND` /
    `_DERIVE_BUDGET_DEFAULT_S` block (~:492-497), add two `Final`-typed module constants per
    the interfaces shape: `JOB_CHAIN_FOLLOW_ON` (entries exactly as shown — verified against
    long_fetch.py:583, job_worker.py:1857, job_worker.py:4885, plus the two chain-terminal
    kinds with empty tuples) and `STRATEGY_ANALYTICS_REAP_THRESHOLD` (Postgres interval
    string; provisional "16 hours", finalized in Task 2). Each topology entry carries a
    trailing comment naming the enqueue site it mirrors (TIMEOUT_PER_KIND house style). The
    threshold constant's comment block must state three things: (1) it is the CANONICAL
    source — migration 20260802120000_strategy_analytics_stuck_computing_reaper.sql embeds
    this literal and a drift test (plan 142-04) fails CI if they diverge; (2) the derivation
    is the chain-inclusive ceiling computed by TestReaperThresholdInvariant, NOT
    batch_size × max_per_kind_timeout (C-6 — that is the compute_jobs formula from migration
    20260720120000 and under-counts a multi-hop chain by ~4×); (3) reap SAFETY lives in the
    reaper's NOT EXISTS(active compute_jobs) conjunct — this interval is defense-in-depth
    debounce; do not shrink it without the invariant.

    Rewire the three enqueue sites to READ the topology (each edit gets a `JOB-03:` tag
    comment, per the S-4 comment-tagged-invariant house style):
    - `job_worker.py` ~:1857: `_follow_on_kind = JOB_CHAIN_FOLLOW_ON["sync_trades"][0]`
      (module-local, no import needed).
    - `job_worker.py` ~:4885: replace the `"compute_analytics_from_csv"` p_kind literal with
      `JOB_CHAIN_FOLLOW_ON["derive_broker_dailies"][0]`.
    - `long_fetch.py` :583: unpack `ledger_tail, trade_tail = JOB_CHAIN_FOLLOW_ON["process_key_long"]`
      then `tail_kind = ledger_tail if is_ledger_backed else trade_tail`, importing
      JOB_CHAIN_FOLLOW_ON function-scoped exactly like the existing
      `from services.job_worker import DispatchResult` at :43 (do NOT add a module-level
      import — the deferred-import convention in this file avoids import cycles). Comment
      noting tuple order is (ledger-backed, trade-backed).

    Touch NOTHING else. The routers/cron.py daily re-entry and WATCHDOG_PER_KIND_OVERRIDES
    are NOT chain edges (PATTERNS correction #2: main_worker.py:200-230 is the watchdog map).
    Do not touch the partial upserts at job_worker.py:1702/:4875 or analytics_runner.py:1555.
  </action>
  <verify>
    <automated>cd analytics-service && python -c "from services.job_worker import JOB_CHAIN_FOLLOW_ON, STRATEGY_ANALYTICS_REAP_THRESHOLD; assert JOB_CHAIN_FOLLOW_ON['process_key_long'] == ('derive_broker_dailies','sync_trades'); assert JOB_CHAIN_FOLLOW_ON['sync_trades'] == ('derive_broker_dailies',); print('ok')" && pytest tests/test_main_worker.py tests/test_job_worker_csv_kind.py -x -q && mypy --strict services/job_worker.py services/ingestion/long_fetch.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "JOB_CHAIN_FOLLOW_ON" analytics-service/services/job_worker.py` shows the declaration plus reads at the ~:1857 and ~:4885 enqueue sites (declaration + ≥2 read sites in this file).
    - `grep -n "JOB_CHAIN_FOLLOW_ON" analytics-service/services/ingestion/long_fetch.py` shows a function-scoped import and the tail selection read (≥2 hits).
    - `grep -v '^\s*#' analytics-service/services/ingestion/long_fetch.py | grep -c '"sync_trades" if\|if is_ledger_backed else "sync_trades"'` returns 0 — the inline conditional literal form is gone from live code (comment lines excluded).
    - `grep -v '^\s*#' analytics-service/services/job_worker.py | grep -c '_follow_on_kind = "derive_broker_dailies"'` returns 0.
    - Existing job-flow suites green: `cd analytics-service && pytest tests/test_main_worker.py tests/test_job_worker_csv_kind.py -x -q` exits 0 (proves the rewiring is behavior-preserving).
    - `mypy --strict services/job_worker.py services/ingestion/long_fetch.py` reports 0 errors and 0 new `# type: ignore`.
  </acceptance_criteria>
  <done>Both constants exist beside TIMEOUT_PER_KIND, all three enqueue sites read JOB_CHAIN_FOLLOW_ON, no inline chain-edge literal survives in live code, existing job-flow tests and mypy --strict are green.</done>
</task>

<task type="auto">
  <name>Task 2: TestReaperThresholdInvariant — chain-inclusive headroom, sane upper bound, and the SC-3 mutation observation</name>
  <files>analytics-service/tests/test_main_worker.py, analytics-service/services/job_worker.py</files>
  <read_first>
    - analytics-service/tests/test_main_worker.py:970-1084 (the _INTERVAL_UNIT_SECONDS/_watchdog_seconds helpers and all three TestWatchdogInvariant methods — the structure to mirror; note the class docstring states CONSEQUENCE, not mechanic)
    - analytics-service/services/job_worker.py:476-500 (TIMEOUT_PER_KIND values + the new constants from Task 1)
    - analytics-service/main_worker.py:470,511 (p_batch_size = 5 — the batch-claim literal the test pins as a LOCAL literal, mirroring how the existing test pins DEFAULT_WATCHDOG_INTERVAL)
    - supabase/migrations/20260505115047_mark_compute_job_atomic_status_bridge.sql:209 (retry backoff COMMENT: attempt 1 → +30s, 2 → +2min, else → +8min; max_attempts default 3 at job_worker.py:8171)
    - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md (Oracle Independence section — the P-8 rule this test must satisfy)
  </read_first>
  <action>
    Append `class TestReaperThresholdInvariant` to `tests/test_main_worker.py` directly after
    `TestWatchdogInvariant` (the class name MUST contain "Reaper" so the VALIDATION.md command
    `pytest tests/test_main_worker.py -k Reaper -x` selects it). Docstring states the
    consequence: if the reaper threshold is below the chain-inclusive worst case, the pg_cron
    reaper flips a live, healthy multi-hop chain's strategy_analytics row to failed mid-compute
    — a false failure on a money surface. Three methods:

    1. `test_threshold_exceeds_chain_inclusive_ceiling` — computes the ceiling INDEPENDENTLY
       of the constant under test: for every simple path through JOB_CHAIN_FOLLOW_ON (walk the
       map from each root kind; the map is imported from services.job_worker — a DELIBERATE
       oracle exception, mitigated because Task 1 made the enqueue sites read it, so a wrong
       map changes production behavior and the job-flow suites catch it; record this in a
       comment citing VALIDATION.md §Oracle Independence), sum per hop:
       (P_BATCH_SIZE - 1) × max(TIMEOUT_PER_KIND.values())   [batch-tail exposure]
       + TIMEOUT_PER_KIND[hop_kind] × MAX_ATTEMPTS            [own handler, retried]
       + RETRY_BACKOFF_TOTAL_S                                [30 + 120 + 480 = 630]
       with P_BATCH_SIZE = 5, MAX_ATTEMPTS = 3, RETRY_BACKOFF_TOTAL_S = 630 all declared as
       LOCAL literals in the test with comments naming their production sources
       (main_worker.py:470/:511, job_worker.py:8171, migration 20260505115047:209) — never
       imported. Ceiling = max over paths. Assert
       `_watchdog_seconds(STRATEGY_ANALYTICS_REAP_THRESHOLD) > ceiling` with a failure message
       that prints both numbers and names C-6. Also assert the computed ceiling itself is
       within a sanity band (between 6 h and 24 h, literals) so a topology edit that zeroes the
       map cannot vacuously pass the headroom assert. Two topology-coverage asserts close the
       blind spot where the walk sees only the 5 topology kinds while bridge branch (a) holds
       a row `computing` for ANY non-terminal sibling of ANY TIMEOUT_PER_KIND kind — a kind
       added to TIMEOUT_PER_KIND but never to the topology could otherwise never raise the
       ceiling: (i) every JOB_CHAIN_FOLLOW_ON key AND every follow-on kind it names is a key
       of TIMEOUT_PER_KIND; (ii) a literal-pinned registry count —
       `assert len(TIMEOUT_PER_KIND) == <N>` where <N> is the REAL key count read while
       writing the test (expected 15; verify by reading, never trust this plan's number),
       failure message: "a job kind was added/removed — decide consciously whether it joins
       JOB_CHAIN_FOLLOW_ON (a chain edge) or is chain-terminal, then bump this literal".
       Checker-verified rationale (W-1) — record it in a test comment: the previously
       drafted dominance form `chain_ceiling >= all-kinds single_hop ceiling` is
       mathematically INCAPABLE of failing — both sides share
       M = max(TIMEOUT_PER_KIND.values()), so any >=2-hop path exceeds the single-hop
       ceiling by M + RETRY_BACKOFF_TOTAL_S + MAX_ATTEMPTS·ΣT > 0 unconditionally, and a
       new kind raises M on BOTH sides (real numbers: M = 1800 s ⇒ 4-hop ceiling 43,920 s
       vs single_hop 13,230 s). A dead assert pinned as coverage is the Rule-9 defect; the
       literal count converts the blind spot into a forced conscious decision (VALIDATION
       §Oracle Independence: registry sizes pinned to a literal).
    2. `test_threshold_has_sane_upper_bound` — mirror test_watchdog_threshold_has_sane_upper_bound:
       `MAX_RATIO = 2.0` against the computed ceiling; a '160 hours'-for-'16 hours' typo must
       fail; docstring carries the same "a deliberate larger window must raise MAX_RATIO and
       justify why" escape hatch.
    3. `test_threshold_is_parseable_interval` — `_watchdog_seconds` accepts the constant and
       returns > 0 (guards against a malformed interval string reaching the migration).

    Reuse the module-level `_watchdog_seconds` helper — do NOT write a second parser
    (test_job_worker_csv_kind.py:118's local _parse_minutes is the existing drift to avoid).

    Then FINALIZE the threshold value in `services/job_worker.py`: run the new test, read the
    ceiling it computes, and set STRATEGY_ANALYTICS_REAP_THRESHOLD to the smallest whole
    4-hour multiple ≥ 1.25 × ceiling (expected: '16 hours' for a ~43,920 s ceiling). Record
    the computed ceiling and the chosen value in the constant's comment block and in the
    SUMMARY (plan 142-04's migration header will restate both).

    SC-3 mutation observation (Falsifiability Ledger — must be OBSERVED, not asserted): edit
    STRATEGY_ANALYTICS_REAP_THRESHOLD to one tenth of its value (e.g. '96 minutes'), run
    `pytest tests/test_main_worker.py -k Reaper -x`, observe test 1 RED and paste the failing
    assertion into the SUMMARY; restore the constant from the pre-mutation text (never from
    memory), re-run GREEN, and verify `grep -rn MUTANT analytics-service/` returns 0.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_main_worker.py -k Reaper -x -q && pytest tests/test_main_worker.py tests/test_job_worker_csv_kind.py -x -q && mypy --strict services/job_worker.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_main_worker.py -k Reaper -x -q` collects ≥ 3 tests and exits 0.
    - The SC-3 mutation was RUN and OBSERVED: SUMMARY contains the pasted failing-assertion output from the divide-by-10 mutation, plus the restore-and-green re-run.
    - `grep -n "import" analytics-service/tests/test_main_worker.py | grep -c "P_BATCH_SIZE\|MAX_ATTEMPTS\|RETRY_BACKOFF"` returns 0 — batch/retry inputs are local literals, not imports (P-8).
    - The test does NOT contain the expression `batch_size * max(` applied as the ceiling (C-6): the ceiling sums per-hop terms over JOB_CHAIN_FOLLOW_ON paths.
    - Topology-coverage asserts present: (i) every JOB_CHAIN_FOLLOW_ON key and named follow-on kind exists in TIMEOUT_PER_KIND; (ii) `len(TIMEOUT_PER_KIND)` equals a pinned literal (the real key count, expected 15) with the conscious-update failure message. The dead dominance assert (chain ceiling ≥ all-kinds single-hop ceiling, both sides sharing max(TIMEOUT_PER_KIND.values())) is NOT present anywhere in the class — checker-verified unfalsifiable (W-1).
    - STRATEGY_ANALYTICS_REAP_THRESHOLD's final value satisfies ceiling < value ≤ 2 × ceiling, and both numbers are recorded in the constant's comment and the SUMMARY.
    - `grep -rn MUTANT analytics-service/` returns 0 after the mutation cycle.
  </acceptance_criteria>
  <done>TestReaperThresholdInvariant green with an independently computed chain-inclusive ceiling; threshold finalized above it with documented margin; SC-3 mutation observed RED first-hand and restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → compute_jobs enqueue RPC | The rewired enqueue sites feed `p_kind` into `enqueue_compute_job`; a wrong topology value changes which handler runs against tenant data |
| Python constant → migration literal | The threshold constant crosses into SQL (plan 142-04); drift between them silently mis-reaps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-01 | Tampering | JOB_CHAIN_FOLLOW_ON entries | mitigate | Enqueue sites READ the constant, so a tampered/wrong entry changes production enqueue behavior and reddens the existing job-flow suites (test_main_worker.py, test_job_worker_csv_kind.py) run in this plan's verify; per-entry comments name the mirrored site |
| T-142-02 | DoS | STRATEGY_ANALYTICS_REAP_THRESHOLD too small → mass false-failure reap of healthy chains | mitigate | TestReaperThresholdInvariant headroom assert (SC-3, mutation observed); reap safety additionally carried by the NOT EXISTS conjunct in plan 142-04 |
| T-142-03 | DoS | Threshold absurdly large (unit typo) → reaper never fires, spinners persist | mitigate | test_threshold_has_sane_upper_bound MAX_RATIO = 2.0 |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero packages (research §Package Legitimacy Audit: phase-wide zero new dependencies); no install tasks exist |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_main_worker.py -k Reaper -x -q` — new invariants green.
- `cd analytics-service && pytest tests/test_main_worker.py tests/test_job_worker_csv_kind.py -x -q` — no regression from the rewiring.
- `cd analytics-service && mypy --strict services/job_worker.py services/ingestion/long_fetch.py` — clean.
- SC-3 mutation observed RED and restored (SUMMARY evidence).
</verification>

<success_criteria>
- JOB_CHAIN_FOLLOW_ON is declared once and read by all three enqueue sites; no inline chain-edge literal remains in live code.
- STRATEGY_ANALYTICS_REAP_THRESHOLD finalized above the computed chain-inclusive ceiling with ≤ 2× margin, both numbers documented.
- TestReaperThresholdInvariant exists beside TestWatchdogInvariant, selects under `-k Reaper`, and its headroom assert was observed to fail under the SC-3 mutation.
</success_criteria>

<output>
Create `.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-01-SUMMARY.md` when done. It MUST record: the computed ceiling, the final threshold value, the SC-3 mutation evidence (pasted failing assertion), and the topology single-source choice rationale (option (a)).
</output>
