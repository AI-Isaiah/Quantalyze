---
phase: 142-job-strategy-analytics-stuck-computing-reaper-computing-star
plan: 04
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql
  - analytics-service/tests/test_main_worker.py
autonomous: true
requirements: [JOB-01, JOB-02, JOB-03]
must_haves:
  truths:
    - "strategy_analytics.computing_started_at exists: timestamptz, nullable, NO default (a NOT NULL DEFAULT now() is a 23502 timebomb), with a one-shot backfill from computed_at BEFORE cron.schedule — computed_at is acceptable ONLY as the one-shot anchor, never the ongoing key (C-1/C-2)"
    - "sync_strategy_analytics_status is re-based on the 20260710150000 definition and branch (a) stamps CONDITIONALLY on the transition into computing keyed off the RESOLVED status; branches (b)/(c) and the branch-(a) complete_with_warnings arm clear the stamp (C-3 — an unconditional now() is the 106 bug in a new column)"
    - "A pg_cron job reap_strategy_analytics_stuck_computing runs */15 * * * * with an INLINE literal body: bounded UPDATE (LIMIT + FOR UPDATE SKIP LOCKED + deterministic ordering) setting exactly four columns — failed, computation_warned=FALSE, the fixed computation_error literal, computing_started_at=NULL — gated on the NOT EXISTS(active compute_jobs) conjunct and the stamp threshold; the body never mentions computed_at or updated_at (P-1)"
    - "The migration's interval literal equals the Python STRATEGY_ANALYTICS_REAP_THRESHOLD constant, enforced by a CI drift test (SC-3b)"
  artifacts:
    - path: "supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql"
      provides: "DDL + backfill + partial index + re-based bridge + cron.schedule + self-verify blocks"
      contains: "reap_strategy_analytics_stuck_computing"
    - path: "analytics-service/tests/test_main_worker.py"
      provides: "SQL↔Python threshold drift gate (reads the migration text, compares to the canonical constant)"
      contains: "20260802120000"
  key_links:
    - from: "supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql"
      to: "services.job_worker.STRATEGY_ANALYTICS_REAP_THRESHOLD"
      via: "embedded interval literal + header comment naming the source; drift test pins equality"
      pattern: "interval '16 hours'"
    - from: "cron body"
      to: "public.compute_jobs non-terminal set"
      via: "NOT EXISTS with status IN ('pending','running','done_pending_children','failed_retry') — byte-identical to the bridge's branch-(a) list at 20260710150000:111-112"
      pattern: "done_pending_children"
---

<objective>
Author the phase's single migration: `computing_started_at` DDL + one-shot backfill, the
re-based `sync_strategy_analytics_status` with conditional stamping and exit-clears, the
partial index, and the pg_cron reaper with an inline literal body — plus the SQL↔Python
threshold drift gate in pytest.

Purpose: this is the SQL half of JOB-01 and the whole of JOB-02's mechanism. It is the
highest-risk artifact of the phase: merging `supabase/migrations/**` to main AUTO-APPLIES to
PROD, the bridge re-base has a documented history of silent-revert bugs (P-9), and the
bounded reaping UPDATE has NO in-repo precedent (PATTERNS §No Analog Found) — it is written
from first principles and flagged for extra review attention.

Output: `supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql`,
drift-gate test appended to `test_main_worker.py`. Plan 142-05 applies it to TEST via MCP and
proves it with the SQL gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-CONTEXT.md
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-PATTERNS.md
@.claude/agents/migration-reviewer.md
@.claude/agents/rls-policy-auditor.md

Fixed decisions (planner-resolved, shared across the plan set):
- Filename: `20260802120000_strategy_analytics_stuck_computing_reaper.sql` — sorts after the
  repo tip 20260728120000 (migration-policy.yml check), round-time style matching the pg_cron
  family.
- Jobname `reap_strategy_analytics_stuck_computing`; cadence `*/15 * * * *` (CONTEXT locked);
  LIMIT 25 (bounded run: drains 100 rows/hour at this cadence — Claude's-discretion value,
  recorded here); INLINE literal cron body — NO named function, therefore NO SECDEF surface,
  NO EXECUTE grants, NO caller-suppliable threshold (P-5: the parameter IS the attack surface;
  the 20260516170100 incident is the named precedent).
- Threshold literal: the value of `STRATEGY_ANALYTICS_REAP_THRESHOLD` finalized by plan
  142-01 (expected `16 hours` — READ the constant from services/job_worker.py, do not assume).
- computation_error literal (CONTEXT-constrained wording, lands under the shipped
  GATE_ANALYTICS_FAILED copy as a Details line — must not re-attribute fault, not claim how
  much prior work completed, not mention "platform upgrade"):
  `Analytics was interrupted before it could finish and did not recover. Retry the sync.`
- NULL-stamp rows are SKIPPED (predicate requires computing_started_at IS NOT NULL); detection
  of such writer bugs is the static CI gate (plans 142-03/142-05), NOT a runtime alert — no
  cron→Sentry bridge exists and none is claimed (SEAMUX-08 class). No skip-count reporting:
  the body stays a single bounded UPDATE; the header documents the operator query against
  cron.job_run_details instead.
- BEGIN;/COMMIT; + SET lock_timeout='5s' per repo convention — a PRE-DOCUMENTED deviation
  from migration-reviewer invariant #14 (non-negotiable 9); every other reviewer invariant
  applies, notably #3 (search_path public, pg_catalog), #5 (no CONCURRENTLY in tx), #16
  (template-artifact scan), #21 (RAISE format = single literal, no ||).
- Plain non-concurrent partial CREATE INDEX inside the transaction (research §Migration
  Shape: one row per strategy + tiny partial predicate ⇒ brief ACCESS EXCLUSIVE acceptable).
- No down/ rollback file (PATTERNS correction #5). Do NOT add the cron to
  test_retention_crons_safe.sql (correction #4 — its loop requires %where%created_at%, the
  opposite of P-1).
</context>

<interfaces>
The ONLY line-region of the bridge that changes semantics (current branch (a),
20260710150000:113-125) and the two exit branches gaining clears:

```sql
-- branch (a) today ends with:  computed_at = now();   ← why computed_at is the wrong key
-- target: add a computing_started_at expression keyed off the RESOLVED status:
--   CASE
--     WHEN <resolved> = 'computing' AND strategy_analytics.computation_status IS DISTINCT FROM 'computing'
--       THEN now()                                        -- genuine transition in
--     WHEN <resolved> = 'computing'
--       THEN strategy_analytics.computing_started_at      -- already computing: KEEP (SC-2b)
--     ELSE NULL                                           -- resolved complete_with_warnings: exit ⇒ clear
--   END
-- where <resolved> repeats branch (a)'s own status CASE (complete_with_warnings vs computing).
-- branches (b) (:174-181, failed) and (c) (:189-200, complete/cww): add computing_started_at = NULL.
```

Reap body shape (from first principles — P-6; the bridge's non-terminal list is the
definition of "active" everywhere else, byte-identical per research §Superseded One-Off):

```sql
UPDATE public.strategy_analytics sa
   SET computation_status   = 'failed',
       computation_warned   = FALSE,   -- SI-02: block the cww launder (P-4)
       computation_error    = '<fixed literal above>',
       computing_started_at = NULL     -- the reaper is itself an exit transition
 WHERE sa.strategy_id IN (
         SELECT s.strategy_id FROM public.strategy_analytics s
          WHERE s.computation_status = 'computing'
            AND s.computing_started_at IS NOT NULL
            AND s.computing_started_at < now() - interval '<threshold>'
            AND NOT EXISTS (SELECT 1 FROM public.compute_jobs cj
                             WHERE cj.strategy_id = s.strategy_id
                               AND cj.status IN ('pending','running','done_pending_children','failed_retry'))
          ORDER BY s.computing_started_at ASC
          LIMIT 25
          FOR UPDATE SKIP LOCKED)
   AND sa.computation_status = 'computing';  -- compare-and-set fence (one-off script :102)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-authoring verification — re-base source, writer census, TEST/PROD computing census, A-1 check</name>
  <files>(read-only / MCP queries — no repo files modified)</files>
  <read_first>
    - supabase/migrations/20260710150000_sync_status_supersede_failed_per_kind.sql (FULL — the definition to re-base on: signature, SECDEF posture, branches a/b/c/d, computation_warned reads, REVOKE at :206, COMMENT at :203-204, self-verify DO at :211+)
    - supabase/migrations/20260708120000_sync_status_failed_final_bounce.sql:40-63 (the re-base contract paragraph shape + the column+backfill precedent)
    - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-RESEARCH.md §Re-basing discipline, §Writer Census, Assumptions A-1/A-2, Open Question 2
  </read_first>
  <action>
    1. Re-base check (P-9, mandatory freshness re-verification): grep ALL of
       supabase/migrations/ for `sync_strategy_analytics_status` CREATE statements; confirm
       20260710150000 is still the LATEST definition and later migrations only CALL it. If a
       newer definition exists, STOP — the re-base target changes and the SUMMARY must record
       it before Task 2 proceeds.
    2. Writer-census freshness: re-run `grep -rn 'table("strategy_analytics")'
       analytics-service --include='*.py'` and confirm the 'computing' writer set is still
       exactly {analytics_runner._mark_computing, bridge branch (a)}. Any third writer → STOP
       and surface.
    3. Read-only computing census (Open Question 2): via Supabase MCP execute_sql on TEST
       (qmnijlgmdhviwzwfyzlc): `SELECT count(*), min(computed_at), max(computed_at) FROM
       strategy_analytics WHERE computation_status='computing';` — record the numbers; run the
       same on PROD (khslejtfbuezsmvmtsdn) if an MCP connection for it is available, else
       record "PROD census unavailable at planning-execution time" and carry it as a
       pre-merge operator step. The migration header states the REAL census number, not a
       hypothetical.
    4. A-1 empirical check (PostgREST merge-duplicates leaves omitted columns untouched):
       attempt against TEST's PostgREST REST endpoint (an upsert omitting computed_at on a
       throwaway seeded row; requires a service key). If credentials are not available to the
       executor, DO NOT fake it: record A-1 as verified-by-documentation only, and phrase the
       migration-header sentence about the runner-path false-positive as "per PostgREST
       merge-duplicates semantics" (cited), not as an observed fact. Note: the SQL gate's SC#2
       arms seed computed_at directly and do NOT depend on A-1 either way.
    5. Confirm pg_cron on TEST (A-2): `SELECT EXISTS(SELECT 1 FROM pg_extension WHERE
       extname='pg_cron');` via MCP on TEST. If FALSE, STOP — plan 142-05's gate would
       green-skip and the phase cannot be proven; surface to the orchestrator.
  </action>
  <verify>
    <automated>grep -c "CREATE OR REPLACE FUNCTION sync_strategy_analytics_status\|CREATE FUNCTION sync_strategy_analytics_status" supabase/migrations/20260710150000_sync_status_supersede_failed_per_kind.sql</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records: the re-base grep output (4 defining migrations, 20260710150000 latest), the census counts from TEST (and PROD or the explicit unavailability note), the pg_cron-on-TEST boolean (must be TRUE to proceed), and the A-1 outcome (observed or documented-fallback).
    - Zero repo files modified by this task (`git status --porcelain` unchanged).
  </acceptance_criteria>
  <done>All five pre-flight facts recorded with evidence; any STOP condition surfaced instead of worked around.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 20260802120000_strategy_analytics_stuck_computing_reaper.sql</name>
  <files>supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql</files>
  <read_first>
    - supabase/migrations/20260719120000_retention_orphaned_running_compute_jobs.sql (FULL — header sections, BEGIN/lock_timeout, fail-loud pg_cron DO, idempotent unschedule-then-schedule, $cron$ literal body, self-verify block)
    - supabase/migrations/20260720120000_retention_orphaned_running_window_4h.sql:92-97 (positive + NEGATIVE cron-body anchors)
    - supabase/migrations/20260710150000_sync_status_supersede_failed_per_kind.sql (FULL — the byte-for-byte re-base source)
    - supabase/migrations/20260710120000_strategy_keys.sql:131-145 (additive-nullable column + COMMENT ON COLUMN precedent)
    - supabase/migrations/20260708120000_sync_status_failed_final_bounce.sql:52-63 (column + same-file idempotent backfill precedent, IS DISTINCT FROM guard)
    - analytics-service/services/job_worker.py (the finalized STRATEGY_ANALYTICS_REAP_THRESHOLD value and its derivation comment — from plan 142-01)
    - .claude/agents/migration-reviewer.md (invariants #3, #5, #6, #16, #20, #21 — this file faces that reviewer)
  </read_first>
  <action>
    Author the migration in this exact internal order (research §Migration Shape): prose
    header → BEGIN; SET lock_timeout='5s'; → DDL → backfill → index → CREATE OR REPLACE
    bridge → cron DO block → self-verify DO blocks → COMMIT;.

    HEADER (prose, cited): sections mirroring 20260719120000 — "Why this migration exists"
    (the forever-spinner + the 106-janitor revert history; value = page-refresh /
    return-later + factsheet paths, explicitly NOT the live wizard poll which self-escalates
    at 15 min per SyncPreviewStep.tsx:112 — non-negotiable 11); "Threshold rationale" stating
    BOTH numbers (chain-inclusive ceiling from TestReaperThresholdInvariant and the deployed
    interval = STRATEGY_ANALYTICS_REAP_THRESHOLD, naming services/job_worker.py as canonical
    source and the drift test), and that the NOT EXISTS conjunct carries safety while the
    interval is debounce (C-6: this is NOT batch_size × max_per_kind_timeout — say so);
    "Backfill anchor" (computed_at is a ONE-SHOT anchor only; ongoing use is broken in both
    directions — bridge re-stamps now() ⇒ never reap, runner omits it ⇒ reap instantly —
    C-1/C-2, with the Task-1 census number); "Scope discipline"; "Idempotency"; "Convention
    deviation" (BEGIN/COMMIT + session SET lock_timeout used deliberately per repo convention
    — 150/231 migrations incl. the tip — overriding migration-reviewer #14; pre-answering
    review); "Operator observability" (query cron.job_run_details for the jobname; no Sentry
    path exists and none is claimed); a clock-skew sentence (the Python stamp is client-side
    UTC; skew is bounded and dwarfed by the hours-scale threshold).

    DDL: `ALTER TABLE public.strategy_analytics ADD COLUMN IF NOT EXISTS computing_started_at
    timestamptz;` — no DEFAULT, no NOT NULL (23502 timebomb, reviewer #6). `COMMENT ON COLUMN`
    stating NULL = "not currently computing", writer-stamped in the same statement as
    computation_status='computing' (JOB-01), and that computed_at is not a substitute.

    BACKFILL (BEFORE cron.schedule — ordering is load-bearing): `UPDATE
    public.strategy_analytics SET computing_started_at = computed_at WHERE
    computation_status = 'computing' AND computing_started_at IS NULL;` (idempotent via the
    IS NULL guard, mirroring the 20260708120000 IS DISTINCT FROM idiom).

    INDEX: plain (non-CONCURRENT — reviewer #5 satisfied by NOT using CONCURRENTLY inside the
    tx) partial index `idx_strategy_analytics_computing_started` ON
    public.strategy_analytics (computing_started_at) WHERE computation_status = 'computing'.

    BRIDGE RE-BASE (the highest-risk edit — P-9): CREATE OR REPLACE
    sync_strategy_analytics_status copied byte-for-byte from 20260710150000 with ONLY these
    deltas, and a header paragraph mirroring 20260710150000:45-51 naming the re-base source
    and the byte-identical guarantees (branch (b) per-kind NOT EXISTS supersession, BOTH
    computation_warned reads in branches (a)/(c), SECURITY DEFINER, SET search_path = public,
    pg_catalog, the REVOKE ALL ... FROM PUBLIC, anon, authenticated):
      - branch (a) ON CONFLICT DO UPDATE gains the three-arm computing_started_at CASE from
        the interfaces block (transition-in ⇒ now(); already-computing ⇒ KEEP prior stamp —
        the SC-2b invariant; resolved complete_with_warnings ⇒ NULL, honoring clear-on-exit
        even on the cww arm — an extension of CONTEXT's two-arm preferred form, documented in
        an inline comment). Also add the stamp to the INSERT column list (VALUES arm: now(),
        since a fresh INSERT with computing IS the transition). The resolved-status
        condition must repeat branch (a)'s own status CASE — keyed off the RESOLVED status,
        never the branch (C-3: branch (a) can resolve to complete_with_warnings).
      - branches (b) and (c) each gain `computing_started_at = NULL` in their SET lists
        (and the INSERT arms where present set it NULL).
      - COMMENT ON FUNCTION gains a computing_started_at clause.
      - The existing self-verify DO block is retained and EXTENDED (see below).

    CRON (mirroring 20260719120000 exactly in structure): DO block with fail-loud pg_cron
    existence check (RAISE EXCEPTION, single-literal format string — reviewer #21, ERRCODE
    feature_not_supported), idempotent unschedule-if-exists, then PERFORM cron.schedule(
    'reap_strategy_analytics_stuck_computing', '*/15 * * * *', $cron$ ... $cron$) with the
    inline body from the interfaces block: fixed literal, no interpolation, schema-qualified
    public.* throughout, the bridge's byte-identical non-terminal status list, computing_
    started_at IS NOT NULL (NULL-stamp skip), the threshold interval literal (must equal the
    Python constant), ORDER BY computing_started_at ASC, LIMIT 25, FOR UPDATE SKIP LOCKED,
    and the outer compare-and-set fence AND computation_status = 'computing'. Closing RAISE
    NOTICE. The body must NEVER contain computed_at, updated_at, or a re-enqueue (JOB-04 is
    Phase 143 — terminalize only).

    SELF-VERIFY (separate DO blocks, fail-without-fix anchors — positive AND negative):
      (1) column shape via pg_attribute: exists, atttypid = 'timestamptz'::regtype,
          attnotnull = FALSE, atthasdef = FALSE (extending the 20260710150000:254-261
          template per PATTERNS 1e);
      (2) function body via pg_get_functiondef: positive anchors — the conditional CASE
          (regex for computing_started_at followed by CASE), IS DISTINCT FROM, SECURITY
          DEFINER, search_path; NEGATIVE anchor — must NOT match the unconditional form
          `computing_started_at\s*=\s*now\(\)` (the C-3 trap, expressible because the
          conditional form never produces that exact byte sequence); plus the retained
          20260710150000 anchors (d.kind = f.kind etc.) so the re-base cannot silently
          revert F-3/PUB-02;
      (3) cron job: exists under the jobname; schedule = '*/15 * * * *' by STRING EQUALITY
          (P-10 — never the hour-band ::INT cast, which errors on '*'); body positive
          anchors (public.strategy_analytics, computing_started_at, SKIP LOCKED, LIMIT,
          computation_warned, the interval literal) and NEGATIVE anchors (NOT ILIKE
          '%computed_at%', NOT ILIKE '%updated_at%', NOT ILIKE '%interval ''4 hours''%' —
          the P-3 anti-copy anchor).

    Run the reviewer #16 template-artifact scan on the finished file:
    `grep -cE '</?(content|invoke|function_calls|antml:|parameter)'` must be 0. Also confirm
    no `ROLLBACK` token and no psql meta-commands.
  </action>
  <verify>
    <automated>bash -c "f=supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql; test -f \$f && grep -cE '</?(content|invoke|function_calls|antml:|parameter)' \$f | grep -qx 0 && ! grep -qw ROLLBACK \$f && grep -q \"reap_strategy_analytics_stuck_computing\" \$f && grep -q 'FOR UPDATE SKIP LOCKED' \$f && grep -q 'computation_warned' \$f && ! grep -Eq 'computing_started_at[[:space:]]*=[[:space:]]*now\(\)' \$f && echo migration-shape-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Filename is exactly `20260802120000_strategy_analytics_stuck_computing_reaper.sql`, sorting after 20260728120000.
    - Internal order verified by reading: DDL → backfill → index → bridge → cron.schedule → self-verify; the backfill UPDATE appears textually BEFORE the cron.schedule call.
    - The reap body's SET list has exactly FOUR columns (failed, computation_warned FALSE, computation_error literal, computing_started_at NULL) — P-4's warning sign is a two-column SET.
    - The non-terminal status list in the body is byte-identical to `('pending','running','done_pending_children','failed_retry')`.
    - `grep -Eq "computing_started_at\s*=\s*now\(\)" <file>` finds nothing (conditional CASE only); `grep -c "IS DISTINCT FROM" <file>` ≥ 1 in the branch-(a) stamp expression.
    - The interval literal in the cron body equals the current value of STRATEGY_ANALYTICS_REAP_THRESHOLD in services/job_worker.py (manual read + the Task 3 drift test).
    - The bridge REVOKE ALL ... FROM PUBLIC, anon, authenticated line survives byte-identically; no new GRANT appears (ACL drift check vs 20260710150000 — reviewer #20).
    - Every RAISE format string is a single literal (no || in any RAISE — reviewer #21 awk check clean).
    - The header contains the invariant-#14 deviation paragraph, both threshold numbers, the census figure from Task 1, and the no-Sentry operator-query paragraph.
  </acceptance_criteria>
  <done>Migration file authored to house shape with all self-verify anchors, no unconditional stamp anywhere, ACLs preserved, template-artifact scan clean.</done>
</task>

<task type="auto">
  <name>Task 3: SQL↔Python threshold drift gate (SC-3b) in test_main_worker.py</name>
  <files>analytics-service/tests/test_main_worker.py</files>
  <read_first>
    - analytics-service/tests/test_migration_132.py:20-73 (the read-migration-text-and-pin pattern: parents[2] path resolution, regex assertions)
    - analytics-service/tests/test_main_worker.py (the TestReaperThresholdInvariant class added by plan 142-01 — append beside it)
    - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md (§Oracle Independence — the deliberate-exception rule this test must document)
  </read_first>
  <action>
    Append `class TestReaperThresholdDriftGate` (name contains "Reaper" so `-k Reaper`
    selects it) to tests/test_main_worker.py:
    1. `test_migration_file_exists` — the 20260802120000 file is on disk (parents[2] path,
       catching silent rebase-drops, per the analog).
    2. `test_sql_interval_literal_equals_python_constant` — import
       STRATEGY_ANALYTICS_REAP_THRESHOLD from services.job_worker, assert the migration text
       contains `interval '<constant>'` inside the $cron$ body (regex anchored to the cron
       body region, case-insensitive). Comment: importing the constant from the module under
       pin is the DELIBERATE drift-gate exception (the Python side is declared canonical) —
       VALUE-correctness is owned separately by TestReaperThresholdInvariant's literal-pinned
       ceiling (per VALIDATION §Oracle Independence).
    3. `test_cron_body_never_keys_on_computed_at` — the $cron$ body substring contains
       neither `computed_at` nor `updated_at` (P-1 pinned at the source-text level too, so
       the trap reddens in CI without a DB).
    SC-3b mutation observation: edit the migration's interval literal to a different value
    (e.g. '4 hours' — the exact anti-pattern number), run `pytest tests/test_main_worker.py
    -k Reaper -x`, observe test 2 RED, paste output, restore from pre-mutation text, re-run
    GREEN, `grep -rn MUTANT` → 0.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_main_worker.py -k Reaper -x -q && mypy --strict .</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_main_worker.py -k Reaper -x -q` collects the plan-01 invariants PLUS ≥ 3 drift-gate tests, all green.
    - SC-3b mutation OBSERVED: SUMMARY carries the pasted RED from the '4 hours' literal mutation and the restore-green evidence.
    - The drift test's regex targets the $cron$ body region, not the whole file (the header legitimately mentions computed_at in prose — grep-gate hygiene: prose must not satisfy or trip the gate).
  </acceptance_criteria>
  <done>Drift gate green, SC-3b observed RED first-hand, P-1 pinned at source-text level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron (postgres role) → all tenants' strategy_analytics rows | The reaper writes terminal state on rows it does not own — a mis-predicated or unbounded UPDATE is a cross-tenant data-integrity event |
| authenticated/anon callers → bridge function | sync_strategy_analytics_status is SECURITY DEFINER; its ACL posture must survive the re-base byte-identically |
| computation_error → user render | The string lands in the wizard's Details line for the row owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-09 | Tampering/DoS | caller-controlled reap threshold flips every tenant's in-flight rows (the 20260516170100 incident class) | mitigate | INLINE literal cron body — no function, no parameter, no EXECUTE surface; threshold is a fixed literal pinned by the drift gate |
| T-142-10 | Elevation of Privilege | bridge re-base drops SECDEF hardening or the REVOKE (leak window — reviewer #20) | mitigate | Byte-for-byte re-base on 20260710150000 with the REVOKE + search_path = public, pg_catalog retained; self-verify DO re-asserts SECDEF + search_path + retained anchors |
| T-142-11 | Tampering | Unconditional branch-(a) stamp resets on every hop → reaper never fires while gates stay green (C-3 / the 106 bug) | mitigate | Three-arm CASE keyed off the RESOLVED status; NEGATIVE self-verify anchor on `computing_started_at = now()`; behaviorally pinned by 142-05's sentinel two-RPC test (SC-2b) |
| T-142-12 | Tampering | Reap laundered to complete_with_warnings → false success on a money surface (P-4) | mitigate | computation_warned = FALSE in the four-column SET; SET-list arity is an acceptance criterion; behaviorally pinned by 142-05 (SC-1b) |
| T-142-13 | DoS | Unbounded UPDATE holds locks across the table during a backlog (no in-repo precedent for this shape — extra review flagged) | mitigate | LIMIT 25 + ORDER BY computing_started_at ASC + FOR UPDATE SKIP LOCKED + partial index; N+1 bound proven by 142-05's LIMIT arm |
| T-142-14 | Information Disclosure | computation_error leaks internals/tenant data | mitigate | Fixed literal string; echoes no identifiers, no row data, no internal names; wording pre-fixed in this plan |
| T-142-15 | Tampering | SQL injection via dynamic cron body | mitigate | $cron$ fixed literal, zero interpolation, schema-qualified public.* (the 20260719120000:88-90 rationale) |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed in this plan |
</threat_model>

<verification>
- Migration file passes the shape greps (template-artifact 0, no ROLLBACK, no unconditional stamp, four-column SET, SKIP LOCKED present).
- `cd analytics-service && pytest tests/test_main_worker.py -k Reaper -x -q` green (invariants + drift gate).
- SC-3b mutation observed RED and restored.
- Task 1 pre-flight facts recorded (re-base freshness, census, pg_cron-on-TEST TRUE, A-1 disposition).
</verification>

<success_criteria>
- One migration delivering: DDL + one-shot backfill (before cron.schedule) + partial index + conditionally-stamping re-based bridge with exit-clears + bounded inline pg_cron reaper + fail-without-fix self-verification.
- Threshold literal single-sourced from Python with a CI drift gate that was observed to fail.
- Zero new callable SQL surface; ACLs byte-preserved.
</success_criteria>

<output>
Create `.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-04-SUMMARY.md` when done, carrying: Task 1's five pre-flight facts, the final threshold/ceiling pair, the SC-3b evidence, and an explicit review note that the bounded reap UPDATE has no in-repo precedent (first-principles construction — reviewers should weight it accordingly).
</output>
