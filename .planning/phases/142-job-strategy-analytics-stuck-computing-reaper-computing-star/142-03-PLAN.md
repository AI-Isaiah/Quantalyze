---
phase: 142-job-strategy-analytics-stuck-computing-reaper-computing-star
plan: 03
type: execute
wave: 2
depends_on: ["142-01", "142-02"]
files_modified:
  - analytics-service/services/analytics_runner.py
  - analytics-service/services/job_worker.py
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/strategies/csv-finalize/route.ts
  - src/app/api/keys/sync/route.ts
  - analytics-service/tests/test_computing_started_at_stamp.py
  - TODOS.md
autonomous: true
requirements: [JOB-01]
must_haves:
  truths:
    - "The single Python computing writer (analytics_runner._mark_computing) stamps computing_started_at with a client-side UTC ISO timestamp in the SAME upsert payload that sets computation_status='computing'"
    - "Every Python exit writer (all 11 sites across analytics_runner.py and job_worker.py) and every Next.js failed-placeholder writer (finalize-wizard ×2, csv-finalize, keys/sync) clears computing_started_at to NULL in the same payload"
    - "A CI stamp invariant covers the Python AND TypeScript app runtimes and fails when any strategy_analytics status write omits the stamp key — a Python-only gate is a false pass (P-11); the SQL runtime half is owned by plan 142-05's gate on the deployed bridge"
    - "The stamp gate is GREEN on the post-Task-1/2 tree without touching the three partial upserts — including the parameter-default write site job_worker.py:4874-4878, which the payload resolver classifies and correctly DROPS (no status key), never fails loud on"
    - "The already-shipped retry affordance is VERIFIED, not rebuilt — zero frontend behavior changes; the StrategyAnalytics row-type line and its checker-measured 8-file fixture blast radius are owned by plan 142-06"
  artifacts:
    - path: "analytics-service/services/analytics_runner.py"
      provides: "stamp at _mark_computing (~:1227); computing_started_at: None at the 5 exit writers"
      contains: "computing_started_at"
    - path: "analytics-service/services/job_worker.py"
      provides: "computing_started_at: None at the 6 exit writers"
      contains: "computing_started_at"
    - path: "analytics-service/tests/test_computing_started_at_stamp.py"
      provides: "two-app-runtime stamp/clear invariant (Python AST + TS textual), with anti-vacuity counts"
      min_lines: 80
  key_links:
    - from: "analytics-service/services/analytics_runner.py:1227"
      to: "strategy_analytics.computing_started_at"
      via: "upsert payload key beside computation_status: computing"
      pattern: "computing_started_at"
    - from: "analytics-service/tests/test_computing_started_at_stamp.py"
      to: "src/app/api/**/route.ts strategy_analytics writes"
      via: "_repo_root()-based cross-language scan, comment-stripped, anchored on computation_status object-literal payload keys (NOT from() statement windows — the keys/sync payload is built away from its from() call)"
      pattern: "computation_status"
---

<objective>
Stamp and clear `computing_started_at` at every application-runtime writer of
`strategy_analytics.computation_status`, update the live D.10 writer-census comment, and ship
the CI stamp invariant covering both app runtimes (Python AST + TypeScript textual).

Purpose: JOB-01 — the reaper (plan 142-04) keys exclusively on `computing_started_at`; a
writer that sets `computing` without the stamp creates rows the reaper must skip forever
(NULL-stamp skip rule), and an exit writer that fails to clear leaves stale stamps.
Enforcement is static (CI), not runtime — a missed writer must be a red build, not a 23514 on
the money path (CONTEXT locked decision).

Scope guards (non-negotiables 2, 5): the writer set for 'computing' is EXACTLY TWO — the
Python upsert here and the SQL bridge branch (a), which is owned by plan 142-04's migration.
The three Next.js placeholder writers are server API-route DATA writes (exit-clears), not
frontend behavior. The `StrategyAnalytics` row-type line (`src/lib/types.ts`) and its
compile-forced fixture blast radius are owned by plan 142-06 — checker-measured (BLOCKER-2):
the required field breaks `npm run typecheck` in 8 files, which would push this plan past
scope thresholds. The four payload edits here compile WITHOUT the type line because all four
sites go through the deliberately-untyped `createAdminClient()` (`src/lib/supabase/admin.ts:4-9`,
"intentionally returns the untyped client") or a `Record<string, unknown>` payload parameter
(`keys/sync/route.ts:452`) — verified 2026-08-02. The retry affordance is already shipped —
verify, do not rebuild.

Dependency note (checker W-2): `depends_on` carries `142-02` because the stamp gate's Python
scan surface includes `scripts/`, and the pre-142-02 tree still holds
`scripts/reset_stuck_computing_rows.py:97` — a `computation_status: 'failed'` dict WITHOUT
the clear, which this gate would rightly flag (13 sites, not 12). Plan 142-02 deletes that
script; the dependency makes the ordering explicit rather than incidental.

Output: stamped/cleared writers across both app runtimes, `test_computing_started_at_stamp.py`,
updated D.10 census, two TODOS.md entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-CONTEXT.md
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-PATTERNS.md

Writer census (verified 2026-08-02 — re-confirm each line number on read):
- ENTRY (stamp, client-side UTC ISO — PostgREST cannot express SQL now() in a payload):
  W1 `analytics_runner.py:1227-1232` `_mark_computing` upsert. Unconditional stamp is CORRECT
  here (every invocation genuinely transitions into computing).
- EXIT (clear to None/null in the same payload):
  analytics_runner.py `:1270-1281` (failed, insufficient history), `:1490-1508` (the
  csv_status success payload dict), `:1589-1604` (failed, pagination truncation),
  `:1630-1642` (failed, malformed denominator config), `:1680-1690` (failed, catch-all);
  job_worker.py `:1890-1905`, `:2326-2341`, `:2440-2453`, `:4317-4333`, `:5132-5141`
  (all failed), `:6633-6746` (composite_status success write);
  TypeScript: finalize-wizard/route.ts `:1479` and `:1541`, csv-finalize/route.ts `:767`,
  keys/sync/route.ts `:532` (all failed placeholders).
- DO NOT TOUCH: partial upserts job_worker.py:1702, :4875, analytics_runner.py:1555 (they
  write data_quality_flags only; PostgREST merge-duplicates leaves other columns alone — a
  blanket sweep would break them). NOTE the :4875 site's shape (BLOCKER-1): it is a
  parameter-default write — `def _prestamp_dq_flags(payload: dict[str, Any] = _prestamp_payload)`
  at :4874-4878, with `_prestamp_payload` an AnnAssign dict at :4675 whose keys are
  {strategy_id, data_quality_flags} (the :4872 subscript-assignment adds metrics_json_by_basis
  to the same name; no status key anywhere). The Task 3 gate MUST resolve this via its
  parameter-default arm and drop it — not fail loud on it. set_wizard_composite_members needs
  no handling (never touches a computing row).

types.ts decision (PATTERNS correction #1 + checker BLOCKER-2, resolved): the
`computing_started_at: string | null` interface line, the `EMPTY_ANALYTICS` literal in
`src/lib/utils.ts:178`, and the 7 compile-broken test fixtures are ALL owned by plan 142-06
(the checker applied the type edit and measured an 8-file `tsc` blast radius none of which
were in this plan's files_modified). This plan touches NEITHER `src/lib/types.ts` NOR
`src/lib/database.types.ts`. The pre-existing 2-column drift (computation_warned,
metrics_json_by_basis in BOTH type files) still goes to TODOS.md here (Task 2).
</context>

<interfaces>
The two payload shapes to mirror (S-4 comment-tag house style):

```python
# ENTRY — analytics-service/services/analytics_runner.py:1227 (current):
{"strategy_id": strategy_id, "computation_status": "computing"}
# becomes (JOB-01 tag; datetime/timezone already imported in this module — verify, else add):
{"strategy_id": strategy_id, "computation_status": "computing",
 # JOB-01: writer-stamped transition timestamp — the reaper keys on this, never computed_at.
 "computing_started_at": datetime.now(timezone.utc).isoformat()}

# EXIT — the canonical terminal writer job_worker.py:1890-1905 gains one key:
 # JOB-01: clear on exit so a stale stamp can never re-trigger the reaper.
 "computing_started_at": None,
```

```typescript
// EXIT (TS) — e.g. src/app/api/keys/sync/route.ts:532 payload gains:
computing_started_at: null, // JOB-01: clear on exit from computing (reaper key)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Stamp the Python entry writer; clear all 11 Python exit writers</name>
  <files>analytics-service/services/analytics_runner.py, analytics-service/services/job_worker.py</files>
  <read_first>
    - analytics-service/services/analytics_runner.py:1220-1290,1480-1512,1585-1695 (the entry writer and all 5 exit payloads — read each before editing; :1490-1508 builds a payload variable, edit the dict literal not the upsert call)
    - analytics-service/services/job_worker.py:1885-1910,2320-2345,2435-2458,4310-4335,5125-5145,6625-6750 (the 6 exit payloads; :6633-6746 holds the ONE composite success write — the "second member of the class" the Falsifiability Ledger says to treat as first-class)
    - analytics-service/services/job_worker.py:1700-1705,4870-4880 and analytics-service/services/analytics_runner.py:1550-1560 (the partial upserts that MUST stay untouched)
    - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-PATTERNS.md (item 5 — sync-closure-to-db_execute rule, exploded-dict + tagged-comment style, client-side timestamp rule)
  </read_first>
  <action>
    Entry: add `"computing_started_at": datetime.now(timezone.utc).isoformat()` to the
    `_mark_computing` upsert dict at analytics_runner.py:1227-1232 with the JOB-01 tag comment
    (see interfaces). Confirm `datetime`/`timezone` are imported at module top; add to the
    existing import line if not. Client-side timestamp is mandatory — PostgREST payloads are
    literals, never SQL expressions (the trap the deleted one-off documents).

    Exit: add `"computing_started_at": None` with the JOB-01 clear-on-exit tag comment to all
    11 Python exit payload dicts enumerated in the context census — the 5 analytics_runner
    sites (including the success payload at ~:1490-1508: add the key to the payload dict where
    csv_status is set) and the 6 job_worker sites, including the ONE composite success write
    in the :6633-6746 region: the `headline_payload` dict bound at :6635 and upserted by the
    nested `_write_headline_and_by_basis` at :6744. There is exactly ONE such dict in that
    region (checker W-3) — do not hunt for a second; if you believe you found another
    computation_status-bearing dict there, that is census drift: STOP and record it. Keep
    each existing `computation_warned` line byte-unchanged; the new key sits beside it. Do
    NOT touch the three partial upserts.

    If any enumerated line number has drifted, re-locate by content (`computation_status`
    inside a `.table("strategy_analytics")` call chain) — the invariant is the census, not the
    line numbers. If a NEW status-writing site is found that the census misses, STOP and record
    it in the SUMMARY before proceeding (fail loud, never silently widen).
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/ -x -q -k "analytics_runner or job_worker or csv_kind" && mypy --strict services/analytics_runner.py services/job_worker.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*#' analytics-service/services/analytics_runner.py | grep -c 'computing_started_at'` ≥ 6 (1 stamp + 5 clears, comments excluded).
    - `grep -v '^\s*#' analytics-service/services/job_worker.py | grep -c '"computing_started_at": None'` ≥ 6.
    - The three partial upserts (job_worker.py:1702, :4875, analytics_runner.py:1555) are byte-unchanged (`git diff` shows no hunk touching them).
    - Every touched payload retains its existing computation_warned handling byte-unchanged.
    - Targeted pytest selection and `mypy --strict` on both files exit 0; zero new `# type: ignore`.
  </acceptance_criteria>
  <done>Entry writer stamps client-side UTC; all 11 Python exit writers clear; partial upserts untouched; types and targeted tests green.</done>
</task>

<task type="auto">
  <name>Task 2: TS placeholder clears, D.10 census update, TODOS entries, retry-affordance verification</name>
  <files>src/app/api/strategies/finalize-wizard/route.ts, src/app/api/strategies/csv-finalize/route.ts, src/app/api/keys/sync/route.ts, TODOS.md</files>
  <read_first>
    - src/app/api/strategies/finalize-wizard/route.ts:1470-1550 (both failed-placeholder payloads at :1479 and :1541)
    - src/app/api/strategies/csv-finalize/route.ts:755-775 (the :767 payload)
    - src/app/api/keys/sync/route.ts:30-60,440-460,520-540 (the D.10 "Direct-writes audit" census comment at :41-59, the `stampCompositeFailedUnlessComplete` signature at :449-453 with its `Record<string, unknown>` payload param, AND the :532 payload)
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx:1640-1690 and src/lib/wizardErrors.ts:663-673,1457-1463 (READ-ONLY — the shipped retry affordance to verify, not rebuild)
    - TODOS.md (append-only; the ONE backlog ground truth per project memory)
  </read_first>
  <action>
    1. Add `computing_started_at: null,` with a `// JOB-01: clear on exit from computing
       (reaper key — migration 20260802120000)` comment to the four failed-placeholder
       payloads (finalize-wizard :1479 and :1541, csv-finalize :767, keys/sync :532). These
       are server API-route data writes; no component, hook, or copy changes anywhere. These
       keys compile WITHOUT any type change: all four sites use the deliberately-untyped
       `createAdminClient()` (src/lib/supabase/admin.ts:4-9) or the `Record<string, unknown>`
       payload param (keys/sync:452) — the StrategyAnalytics row-type line is plan 142-06's,
       and this plan must NOT touch src/lib/types.ts or src/lib/database.types.ts.
    2. Update the D.10 census comment (keys/sync/route.ts:41-59) so it stops being false:
       keep the lettered-entry format; correct/extend it to name (a) the SQL bridge
       sync_strategy_analytics_status as a computing writer that stamps conditionally on the
       transition, (c) analytics_runner._mark_computing as the stamping Python writer, the
       job_worker/analytics_runner terminal writers and THESE four Next.js placeholder writers
       as stamp-CLEARING exit sites, and (g, new) the pg_cron reaper
       reap_strategy_analytics_stuck_computing as a terminal-failed writer. One line each;
       cite migration 20260802120000.
    3. TODOS.md — append two items: (i) `src/lib/types.ts` + `src/lib/database.types.ts` are
       both stale by computation_warned + metrics_json_by_basis (found in Phase 142; only
       computing_started_at was added — types.ts plus its 8-file fixture blast radius via
       plan 142-06 — by deliberate scope containment);
       (ii) `.claude/agents/migration-reviewer.md` invariant #14 contradicts the repo's actual
       BEGIN/COMMIT convention (150/231 migrations) — reviewer doc needs updating (Phase 142
       pre-documented the deviation in its migration header instead).
    4. VERIFY (read-only, record in SUMMARY with file:line quotes): GATE_ANALYTICS_FAILED in
       wizardErrors.ts:663-673 still carries actions ["clear_and_retry","request_call"] and
       our-fault attribution; wizardErrors.ts:1457-1463 still appends `Details:
       ${computationError}.`; SyncPreviewStep.tsx:1655-1680 still passes onRetry when
       clear_and_retry is present. Zero edits to these files — if any of the three anchors is
       missing, STOP and surface it (the phase premise "retry already shipped" would be false).
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/__tests__/contracts/check-zod-db-check-parity.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "computing_started_at: null" src/app/api/strategies/finalize-wizard/route.ts` returns 2; same grep returns 1 each in csv-finalize/route.ts and keys/sync/route.ts.
    - The D.10 comment names the reaper cron jobname and the stamp/clear obligation (`grep -n "reap_strategy_analytics_stuck_computing" src/app/api/keys/sync/route.ts` ≥ 1, in comment only).
    - TODOS.md gained exactly the two enumerated items.
    - SUMMARY quotes the three retry-affordance anchors verbatim with file:line; `git diff --name-only` contains NO SyncPreviewStep.tsx, wizardErrors.ts, useStrategySyncPoller.ts, database.types.ts, or src/lib/types.ts (the row-type line is plan 142-06's).
    - `npm run typecheck` exits 0; the parity vitest passes untouched (this phase adds no enum value).
  </acceptance_criteria>
  <done>Four TS clears in place, census truthful, TODOS updated, retry affordance verified-not-rebuilt with quoted evidence.</done>
</task>

<task type="auto">
  <name>Task 3: test_computing_started_at_stamp.py — the two-app-runtime CI stamp invariant</name>
  <files>analytics-service/tests/test_computing_started_at_stamp.py</files>
  <read_first>
    - analytics-service/tests/test_verify_strategy_no_legacy_writes.py:34-98 (the AST chain-unwind that resolves supabase.table("&lt;name&gt;") call chains — the Python-half mechanism)
    - analytics-service/routers/portfolio.py:628-740 (the trap the scan design must survive: _compute_portfolio_analytics carries portfolio_analytics status dicts at :651-652 and :695-697 — using ComputationStatus.*.value Attribute forms — AND a strategy_analytics .select() at :734 in the SAME function; write-method chain scoping, not function scoping, is what keeps this file at zero findings)
    - analytics-service/services/analytics_runner.py:1487-1508 and analytics-service/services/job_worker.py:6635-6748 (the two Name-passed payloads the single-assignment arm exists for: `payload` bound at :1488 and upserted by name at :1506; `headline_payload` bound at :6635 in the OUTER function and upserted at :6744 inside the NESTED def _write_headline_and_by_basis)
    - analytics-service/services/job_worker.py:4660-4680,4860-4890 (the parameter-default site the n2 arm exists for — BLOCKER-1: `_prestamp_payload` AnnAssign Dict at :4675 with keys {strategy_id, data_quality_flags}; the :4872 subscript-assignment adding metrics_json_by_basis to the same name; and `def _prestamp_dq_flags(payload: dict[str, Any] = _prestamp_payload)` at :4874-4878 whose upsert the gate must resolve via the parameter DEFAULT and then DROP — no status key — never fail loud on)
    - analytics-service/tests/test_dark_path_deleted.py:37-118 (_repo_root for reaching src/ from the analytics-service pytest cwd, _strip_comment for both languages, anti-vacuity asserts)
    - analytics-service/services/analytics_runner.py + services/job_worker.py (post-Task-1 state — the gate must pass against it)
    - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md (rows: "Python computing writer co-locates the stamp" and "stamp cleared on every exit — SQL gate + pytest")
  </read_first>
  <action>
    Create `analytics-service/tests/test_computing_started_at_stamp.py`. Module docstring:
    the JOB-01 static enforcement — a missed writer is a red build, not a 23514 on the money
    path; the SQL-runtime half (bridge branch (a) conditional stamp) is enforced by
    supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql on the DEPLOYED function
    body (P-11: this file alone is NOT the whole invariant). The docstring must ALSO state
    the TypeScript scan boundary honestly (checker W-8): the TS surface is
    src/app/api/**/*.ts route handlers ONLY — a future strategy_analytics computation_status
    writer in a server action, src/lib, or a page component is OUTSIDE this gate's coverage,
    and the D.10 census comment (keys/sync/route.ts) is the discoverability pointer for the
    class. Name the known out-of-surface literals and why widening was rejected:
    src/lib/utils.ts EMPTY_ANALYTICS (an object literal, not a DB write — and it DOES carry
    computing_started_at: null via plan 142-06); src/app/(dashboard)/portfolios/[id]/page.tsx:503
    (a portfolio_analytics payload — the TS twin of the portfolio.py trap; a naive widen
    would flag the wrong table); and type-annotation members (src/lib/queries.ts:414,452,
    src/components/admin/AdminTabs.tsx:82) that the textual key-colon regex cannot
    distinguish from payload keys without a real parser. The coverage claim is therefore
    scoped: this file covers the API-route writer surface, where all 4 census sites live.

    Python half (AST — chain-scoped to the WRITE call with payload resolution. Function
    scoping is WRONG here and MUST NOT be used: routers/portfolio.py::
    _compute_portfolio_analytics contains portfolio_analytics status dicts AND a
    strategy_analytics .select() in the SAME function, so a function-scope rule flags the
    wrong table's payloads and can never satisfy the portfolio-zero-findings criterion):
    for every *.py under analytics-service/ services|routers|scripts + main_worker.py +
    main.py, parse the AST and walk every ast.Call. A WRITE SITE is a Call whose func is an
    ast.Attribute with attr in {"insert", "upsert", "update"} AND whose attribute chain
    bottoms out at a `.table("strategy_analytics")` call (chain-unwind exactly per the
    test_verify_strategy_no_legacy_writes.py:34-98 analog). Read chains (.select/.eq/…) are
    NOT write sites and are ignored — that is what keeps portfolio.py at zero findings.
    Resolve each write site's payload (args[0]):
      - ast.Dict literal → use it directly.
      - ast.Name → resolution in TWO sub-arms, tried per enclosing scope walking the
        (Async)FunctionDef stack from innermost to OUTERMOST:
        (n1) single-assignment: collect Assign/AnnAssign nodes binding that name anywhere
             in the scope's subtree; exactly one Dict-valued binding in the first scope
             that yields any → use that dict (later .update() calls or Subscript
             assignments on the name are irrelevant: the status key is in the literal or
             nowhere).
        (n2) parameter-default (BLOCKER-1 — this arm is REQUIRED, the checker ran the
             n1-only rule and it fails loud on a correct site): if the name is a PARAMETER
             of the current scope's own (Async)FunctionDef (args.posonlyargs / args.args /
             args.kwonlyargs), resolve via that parameter's DEFAULT expression,
             positionally aligned from args.defaults / kw_defaults. A default that is an
             ast.Dict → use it. A default that is an ast.Name → resume the n1 walk for
             THAT name starting at the scope ENCLOSING the FunctionDef (where defaults are
             evaluated). A matching parameter with NO default, or a default of any other
             form → FAIL LOUD naming file:line + "extend the gate".
        Zero bindings across BOTH sub-arms in every enclosing scope, multiple Dict
        bindings within the resolving scope, or a binding whose value is not an ast.Dict →
        FAIL LOUD naming file:line + "extend the gate" — never silently skip a write site.
        This resolves ALL THREE census variable sites: analytics_runner.py:1506 upserts
        `payload` bound at :1488 inside the same nested _mark_complete (n1);
        job_worker.py:6744 upserts `headline_payload` bound at :6635 in the OUTER function
        body while the .upsert() sits inside the NESTED def _write_headline_and_by_basis
        (n1, outer scope); and job_worker.py:4875-4878 upserts `payload`, a PARAMETER of
        _prestamp_dq_flags whose default is the Name `_prestamp_payload`, itself an
        AnnAssign Dict at :4675 with keys {strategy_id, data_quality_flags} (n2 → n1) —
        which correctly DROPS out of the kept set below because it has no
        computation_status key. The gate must be green on the post-Task-1/2 tree with
        job_worker.py:4874-4878 byte-untouched.
      - any other arg form (call result, starred, missing args) → FAIL LOUD, same message.
    From the resolved dicts keep those whose keys include the constant "computation_status"
    (the three partial data_quality_flags upserts resolve to dicts WITHOUT that key and drop
    out here — untouched, exactly as Task 1 requires). Assert each kept dict ALSO carries
    "computing_started_at". Value rules on the status value:
      - ast.Constant "computing" → stamp value must NOT be the constant None (it must be an
        expression — the client-side timestamp);
      - ast.Constant terminal ("failed", "complete", "complete_with_warnings") → stamp value
        MUST be the constant None;
      - ast.Name (csv_status, composite_status) → require key presence with value None
        (both variable-status census sites are exit writers);
      - ast.Attribute or ANY other expression form (e.g. ComputationStatus.FAILED.value —
        the form portfolio.py uses today for its OWN table) → FAIL LOUD naming file:line +
        "unrecognized computation_status value form — extend the gate". A new writer style
        must redden the gate, never pass unclassified.
    Anti-vacuity (typed literals with a comment naming the census — EXACT counts so a new
    writer forces a conscious update; all four counts checker-verified against the
    post-142-01/142-02 tree): exactly 12 status-writing dicts total (6 in
    analytics_runner.py: 1 entry + 5 exits; 6 in job_worker.py exits — the scripts/ 13th
    site is gone because 142-02 deleted the one-off, which is why this plan depends_on
    142-02), exactly 2 of the 12 reached via the n1 Name arm (the :1506 `payload` and :6744
    `headline_payload` — this count is what proves n1 is LIVE; chain-scoping alone would
    silently drop both and still show a plausible-looking total of 10), EXACTLY 1 write
    site classified via the n2 parameter-default arm (the :4875 _prestamp_dq_flags upsert —
    it resolves to a dict WITHOUT the status key and drops OUT of the kept set, so this
    resolutions-by-arm counter — tracked across ALL write sites, not just kept dicts — is
    the ONLY liveness proof for n2), and EXACTLY 1 kept dict with the literal "computing"
    (a second entry writer must be a conscious update, never an accident).

    TypeScript half (textual, comment-stripped via the dark-path _strip_comment idiom for
    ts — comment stripping is LOAD-BEARING: keys/sync/route.ts:435 mentions
    `computation_status:'failed'` inside a block comment). Do NOT anchor on
    `from("strategy_analytics")` statement windows: the actual keys/sync failed payload
    (:532) is built inside compositeMemberCount and passed as an ARGUMENT to
    stampCompositeFailedUnlessComplete — no window around a from() call contains it (so the
    required m3 mutation could never go RED) — and three read-only .select sites
    (csv-finalize:724, keys/sync:456, admin/strategy-review:336) would false-positive as
    write windows. Instead: from `_repo_root()`, scan src/app/api/**/*.ts (skip *.test.ts)
    for non-comment lines matching the object-literal property key `computation_status\s*:`
    (the regex must NOT match the optional-property TYPE form `computation_status?:` at
    csv-finalize:746; `.select(` string arguments carry no key-colon form after comment
    stripping — exclude them anyway for hygiene). For each hit, brace-match the ENCLOSING
    OBJECT LITERAL (scan back to the unbalanced `{`, forward to its matching `}`) and
    require a `computing_started_at` key inside that same literal. Census pin (typed
    literals, checker-verified exactly 4 in non-test code): per-file counts
    {finalize-wizard/route.ts: 2 (:1479, :1541), csv-finalize/route.ts: 1 (:767),
    keys/sync/route.ts: 1 (:532)} plus total == 4 — per-file counts make the pin robust to
    line drift. A hit in any other file, a 5th hit, or a missing expected hit → FAIL LOUD
    naming file:line + "extend the gate".

    Mutation observations (Falsifiability Ledger rows SC-5/SC-5b/SC-5c — "prefer the second
    member of the class"): (m1 = SC-5) remove the stamp key from _mark_computing → run this
    file → RED (paste); (m2 = SC-5b) remove the `computing_started_at: None` clear from the
    `headline_payload` dict literal at job_worker.py:6635 (the composite success write — a
    site an author fixing the obvious writers would not have in mind, AND the payload the
    gate reaches only through the n1 Name-resolution arm, so this RED is the end-to-end
    proof that arm is live) → RED (paste); (m3 = SC-5c) remove `computing_started_at: null`
    from keys/sync/route.ts:532 → RED (paste — only the object-literal anchor can see this
    site). Restore each from the pre-mutation text, re-run GREEN, `grep -rn MUTANT` → 0,
    `git status` clean of unintended files.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_computing_started_at_stamp.py -x -q && mypy --strict tests/test_computing_started_at_stamp.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_computing_started_at_stamp.py -x -q` green on the post-Task-1/2 tree, with job_worker.py:4874-4878 (the parameter-default prestamp site) byte-untouched — the n2 arm resolves it and drops it; the gate does NOT fail loud there (BLOCKER-1).
    - All three mutations (m1/SC-5 entry-stamp, m2/SC-5b the Name-resolved headline_payload clear, m3/SC-5c TS clear) OBSERVED RED with pasted failing assertions in the SUMMARY, then restored and re-run GREEN.
    - The Python half does NOT flag routers/portfolio.py (its computation_status dicts write portfolio_analytics; its strategy_analytics chains are .select() reads — the write-method chain-scope rule excludes both); explicitly asserted by a companion test that scans portfolio.py and expects zero findings there.
    - Anti-vacuity counts present as typed literals: exactly 12 Python status dicts (6 runner + 6 worker), exactly 2 of them via the n1 ast.Name arm, EXACTLY 1 write site classified via the n2 parameter-default arm (the :4875 prestamp — counted across all resolutions, not just kept dicts; the n2 liveness proof), exactly 1 kept dict with the literal "computing", and exactly 4 TS payload sites with the per-file counts {finalize-wizard: 2, csv-finalize: 1, keys/sync: 1}.
    - Fail-loud paths exist (asserted or code-reviewed in the SUMMARY): unresolvable/multiply-bound Name payloads, parameters without defaults or with non-Dict/non-Name defaults, non-Dict/non-Name write args, and unrecognized status value forms (ast.Attribute) each raise naming file:line — no silent skip arm anywhere in the scanner.
    - The docstring names the SQL half's owner (the supabase/tests gate) AND states the TS scan boundary (src/app/api route handlers only) with the named out-of-surface literals (utils.ts EMPTY_ANALYTICS; portfolios/[id]/page.tsx portfolio_analytics payload; the queries.ts/AdminTabs.tsx type-annotation members) — no unscoped coverage claim (W-8).
  </acceptance_criteria>
  <done>Two-app-runtime stamp invariant green on the untouched-partial-upsert tree, three mutations observed RED first-hand, false-positive exclusion proven, coverage boundaries stated honestly, SQL half handed off explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service_role writers → strategy_analytics | All writers here run as service_role (BYPASSRLS) — RLS is NOT the boundary; correctness is carried by the static CI invariant |
| DB row → wizard/factsheet render | computation_error / computation_status flow to authed users; this plan writes no new user-visible strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-06 | Tampering | A future writer sets 'computing' without the stamp → row unreapable forever (NULL-skip rule) | mitigate | AST gate chain-scoped on the strategy_analytics table literal with the n1/n2 payload-resolution arms; anti-vacuity counts force conscious updates; SQL-runtime half owned by 142-05 |
| T-142-07 | Tampering | An exit writer misses the clear → stale stamp survives on a terminal row | mitigate | Same gate's exit-value rule (terminal literal ⇒ None); second-member mutation m2 observed |
| T-142-08 | Information Disclosure | types.ts / census comment drift misleads future agents about the writer set | mitigate | D.10 census corrected in-code; residual 2-column type drift logged to TODOS.md instead of silently widening; row-type line + fixture blast radius owned by plan 142-06 |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (stdlib + existing deps only) |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_computing_started_at_stamp.py -x -q` + targeted runner/worker suites green.
- `cd analytics-service && mypy --strict .` clean (run before hand-off — memory: GSD milestone path runs pytest only).
- `npm run typecheck` + parity vitest green; `git diff --name-only` limited to this plan's files_modified.
- Mutations m1–m3 observed RED and restored.
</verification>

<success_criteria>
- Both app runtimes stamp/clear correctly at every census site; partial upserts (including the :4875 parameter-default site) untouched and correctly classified by the gate.
- The stamp invariant demonstrably fails for entry-stamp omission, a non-obvious Python clear omission, and a TS clear omission — and demonstrably passes the correct tree without fail-loud noise.
- No frontend behavior changed; retry affordance verified with quoted evidence; the row-type line is plan 142-06's, not this plan's.
</success_criteria>

<output>
Create `.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-03-SUMMARY.md` when done, carrying: the m1–m3 mutation evidence, the resolutions-by-arm counts (n1 = 2 kept, n2 = 1 dropped), the retry-affordance verification quotes, and any census drift found (fail loud).
</output>
