---
phase: 148-own-owner-factsheet-without-cache-disclosure
plan: 03
type: execute
wave: 2
depends_on: [148-01, 148-02]
files_modified:
  - src/app/factsheet/[id]/v2/page.tsx
  - src/app/factsheet/[id]/v2/page.smoothed-wiring.test.tsx
  - src/app/factsheet/[id]/v2/page.owner-lane.test.tsx
  - .planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-VALIDATION.md
autonomous: true
requirements: [OWN-02]

must_haves:
  truths:
    - "The owner, from the uploading account, renders the FULL factsheet of their own unpublished strategy (real payload, not the still-computing placeholder) — SC1"
    - "AFTER an owner renders a draft, an anon request for the same id still 404s and the shared cache holds no owner-populated entry — unstable_cache invoked 0 times on the owner lane, 1 time on the public lane — SC2 behavior"
    - "Anon and non-owner authed requests for an unpublished id collapse to the SAME notFound(); the owner probe is session-keyed (auth.getUser), never param-keyed — SC4"
    - "A draft id whose build returns null never writes null into the shared cache from the owner lane (null-is-cached trap covered by the spy test)"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/page.tsx"
      provides: "two-lane branch: Lane A byte-identical; Lane B = published-miss → session probe via withPublishedOrOwner → direct fetchAndBuildPayload, no cache read/write → viewerNotice"
      contains: "withPublishedOrOwner"
    - path: "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx"
      provides: "SC1 / SC2-A / SC4 behavior proofs with unstable_cache as a SPY"
      min_lines: 150
    - path: "src/app/factsheet/[id]/v2/page.smoothed-wiring.test.tsx"
      provides: "visibility mock factory extended with withPublishedOrOwner (same commit as the page import)"
      contains: "withPublishedOrOwner"
  key_links:
    - from: "page.tsx owner lane"
      to: "fetchAndBuildPayload"
      via: "direct uncached call with owner predicate"
      pattern: "fetchAndBuildPayload\\(id, \\(q"
    - from: "page.tsx owner lane"
      to: "FactsheetView"
      via: "viewerNotice prop derived from lane, never from payload"
      pattern: "viewerNotice="
---

<objective>
Build Lane B: on a published-lane miss with an authed session, probe ownership on the
request-scoped client via withPublishedOrOwner, then build the payload DIRECTLY through the
148-02 seam — no cache read, no cache write — and thread the 148-01 viewerNotice. Prove SC1,
SC2-A (behavior layer) and SC4 in a new page-level RSC test with unstable_cache as a SPY.

Purpose: this is OWN-02 itself. CONTEXT locks the two-lane design and lane order
(published/cached FIRST; owner probe only on its miss — no extra query on public authed views).
Output: two-lane page.tsx; extended smoothed-wiring mock (same commit — guaranteed break
otherwise, research finding 6); page.owner-lane.test.tsx; VALIDATION ledger observations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-CONTEXT.md
@.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-PATTERNS.md
@.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-RESEARCH.md

Key sections: RESEARCH "Code Examples → Lane selection in the page body" (the recommended branch
shape), Pitfalls 1/2/5; PATTERNS §1d (Lane B probe MUST select the same column list as Lane A —
signature is consumed at :366-369 and by the :385-393 payload-pending fallback), §1e (structured
console.warn + hint idiom; update the hint to name which lane fell through), §1f (returns-route
probe verbatim analog + its three load-bearing properties), §4 (the exact smoothed-wiring mock
block to edit), §5 (owner-lane test harness: mock block with the two mandatory changes, findPayload
DFS, R8 predicate capture, neuter-check header convention), S1/S2/S6.
</context>

<interfaces>
From 148-02 (already landed):
- `fetchAndBuildPayload(id: string, visibility: StrategyVisibility): Promise<FactsheetPayload | null>` — visibility required
- `buildFactsheetPayloadCached(cacheKey: string)` — visibility-free, public literal inside

From 148-01 (already landed):
- `FactsheetView({ payload, viewerNotice }: { payload: FactsheetPayload; viewerNotice?: "owner_unpublished" })`

Existing helpers:
- `withPublishedOrOwner<Q>(query: Q, authUserId: string): Q` — src/lib/visibility.ts:115; appends
  `.or("status.eq.published,user_id.eq.${authUserId}")`
- `captureToSentry` — "@/lib/sentry-capture" (NOT yet imported by page.tsx; the smoothed-wiring
  test already mocks the module, so the new import survives that harness)
- `supabase.auth.getUser()` — server-side JWT validation (never getSession(); tearsheet precedent :104-106)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write page.owner-lane.test.tsx — RED against the current published-only page</name>
  <files>src/app/factsheet/[id]/v2/page.owner-lane.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/page.smoothed-wiring.test.tsx (FULL — harness to clone: mock block :17-41, request/admin client stubs :154-193, findPayload :173-186, RSC invocation :210-213, neuter-check header :13-15)
    - src/app/api/strategies/[id]/returns/route.test.ts (:127-147 observer state shape, :639-660 R8 session-keyed predicate assertion — the style to copy)
    - 148-PATTERNS.md §5 (the two mandatory harness changes: unstable_cache as vi.fn((fn) => fn) SPY not identity stub — Pitfall 5; @/lib/visibility via vi.importActual + spread so the REAL predicates run against a recording builder stub — option (a), the strictly stronger form)
  </read_first>
  <action>
    Create the new test file (do NOT edit the smoothed-wiring file in this task). Harness: clone
    the smoothed-wiring mock set with two changes — (1) `vi.mock("next/cache")` exposes
    `unstable_cache: vi.fn((fn) => fn)` so invocations are COUNTABLE while behavior stays
    identity; (2) `vi.mock("@/lib/visibility")` uses `vi.importActual` + spread so the real
    withPublishedOnly/withPublishedOrOwner run against a recording query-builder stub that
    captures .eq("status","published") and .or(filter) strings (returns-route observer shape).
    Request-client stub must be switchable per test: published-hit row, published-miss + owner
    row on the .or probe, published-miss + no row, and auth.getUser() returning a fixed literal
    UUID user / null. Admin-client stub cloned from smoothed-wiring so fetchAndBuildPayload can
    assemble a real payload. Add a `findViewProps()` sibling helper (DFS returning the
    FactsheetView element's whole props) — do not mutate findPayload.

    Tests (expected values are LITERALS in this file, never imports — oracle independence):
    1. SC1: owner + unpublished id → resolved tree contains a real FactsheetPayload (findPayload
       non-null, not the placeholder) and findViewProps().viewerNotice === "owner_unpublished".
    2. SC1: published id (Lane A) → payload present, viewerNotice undefined.
    3. SC1: owner lane trust tier — readPublicVerificationSignals mock returns empty map → the
       rendered payload/props carry null trust tier (badge hides; never fabricated).
    4. SC2-A: owner render → expect(unstable_cache).toHaveBeenCalledTimes(0). Public render →
       exactly 1.
    5. SC2-A sequence: owner render for id X, then anon render for same id X with shared spy →
       second render throws notFound; unstable_cache never invoked during the owner pass.
    6. SC2-A draft-null (orchestrator ruling — null-is-cached trap): owner + draft id whose admin
       build returns null → page falls to the payload-pending placeholder arm, and
       unstable_cache count is STILL 0 (no null ever offered to the shared cache from Lane B).
    7. SC4: anon (auth.getUser → null user) + unpublished id → rejects with /notFound/ BEFORE any
       .or probe is recorded (published-first lane order, no extra query for anon).
    8. SC4: non-owner authed + unpublished id (probe returns no row) → rejects with /notFound/ —
       same notFound as anon (no existence oracle).
    9. SC4 R8-style: the recorded .or filter string equals
       "status.eq.published,user_id.eq.<the fixed session UUID literal>" — proves session-keying;
       a param-keyed predicate cannot produce this string.

    Header must carry the neuter check (repo convention): "point Lane B at
    buildFactsheetPayloadCached → the count-0 assertion of tests 4/5/6 reddens" — leave the
    sentence in the header now; 148-04's mutation run records the observed output.
    Run the file: tests 1, 4(owner half), 5, 6, 8, 9 MUST be RED (Lane B does not exist yet);
    tests 2, 7 and the public halves pass. Record the RED output.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" --no-file-parallelism; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - File exists with ≥9 tests; RED observed with failures in the owner-lane tests specifically (paste 2 failing assertion lines into the SUMMARY)
    - unstable_cache mock is vi.fn((fn) => fn) — grep the file for "vi.fn((fn" ; NO bare identity stub
    - visibility mock uses importActual + spread (grep "importActual")
    - The expected .or predicate string and the session UUID are literals typed in the test (no import from page.tsx or visibility.ts for expected values)
  </acceptance_criteria>
  <done>Failing behavior spec exists; every SC1/SC2-A/SC4 clause has a named test.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Lane B in page.tsx + extend the smoothed-wiring visibility mock (SAME COMMIT)</name>
  <files>src/app/factsheet/[id]/v2/page.tsx, src/app/factsheet/[id]/v2/page.smoothed-wiring.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/page.tsx (:322-466 — the page body: Promise.all signature probe :341-350, 404 arm :352-365, signAnalytics/computedAt :366-369, cached call :371, placeholder arm :372-416, FactsheetView mount :463)
    - src/app/api/strategies/[id]/returns/route.ts (:190-233 — the probe triad to copy: request-client probe, session-only user.id, fail-loud-but-non-oracular error arm)
    - 148-PATTERNS.md §1d/§1e/§1f + RESEARCH Code Example "Lane selection in the page body"
    - src/app/factsheet/[id]/v2/page.smoothed-wiring.test.tsx (:32-36 — the exact visibility mock factory block to extend)
  </read_first>
  <action>
    In page.tsx (ONE commit together with the smoothed-wiring mock edit — research finding 6:
    the partial vi.mock factory returns undefined for the new import, guaranteed TypeError):

    1. Imports: add withPublishedOrOwner to the existing "@/lib/visibility" import; add
       `import { captureToSentry } from "@/lib/sentry-capture";`.
    2. Keep Lane A byte-identical: the Promise.all + withPublishedOnly signature probe at :341-350
       stays as-is. Introduce `let lane: "public" | "owner" = "public";` and let `signature` be
       reassignable.
    3. On the published miss (signRes.error || !signature): call
       `const { data: { user } } = await supabase.auth.getUser();` — if no user, keep the existing
       structured console.warn + notFound() (anon path unchanged, no probe query). If a user is
       present, run the Lane B probe on the SAME request-scoped client:
       `withPublishedOrOwner(supabase.from("strategies").select(<the EXACT Lane A column list —
       "id, name, codename, disclosure_tier, strategy_analytics ( computed_at )">).eq("id", id), user.id).maybeSingle()`.
       user.id is session-only — from this auth.getUser() call in this function, NEVER from
       params/searchParams (Pitfall 1 / S1). On probeError: console.error with the structured
       shape + captureToSentry({ tags: { route: "factsheet/v2/page", stage: "owner-probe" } }),
       then STILL fall through to the row check (fail loud, non-oracular — S2). No row →
       notFound() — the same notFound as anon. Row → signature = ownRow; lane = "owner".
       Update the existing 404-arm hint text so it no longer claims only
       "(id, status='published')" — it must name which lane fell through (S6).
    4. Payload: `lane === "owner" ? await fetchAndBuildPayload(id, (q) => withPublishedOrOwner(q, user.id)) : await buildFactsheetPayloadCached(\`${id}::${computedAt}\`)`.
       The owner arm calls the builder DIRECTLY — no cache read, no cache write (CONTEXT lock).
       Capture the owner's uid into a narrowly-scoped const inside the miss branch for the lambda.
    5. FactsheetView mount gains `viewerNotice={lane === "owner" ? "owner_unpublished" : undefined}`
       — derived from the lane decision, never from any payload field (UI-SPEC:112).
       The placeholder arm (:372-416) needs no change — Lane B's probe selects the identical
       column list, so signature.name/codename/disclosure_tier resolve on both lanes.
    ⛔ Do NOT touch generateMetadata, the cached wrapper, the cache key/tags, or the select
    string inside fetchAndBuildPayload.

    In page.smoothed-wiring.test.tsx (same commit): extend the "@/lib/visibility" factory with
    `withPublishedOrOwner: (qb: unknown) => qb` (or importActual + spread). No other edit — that
    file's request-client stub already satisfies both lanes (chain reusable, always Lane A).
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" "src/app/factsheet/[id]/v2/page.smoothed-wiring.test.tsx" src/__tests__/phase-147-series-resolution-guards.test.ts --no-file-parallelism && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - All page.owner-lane tests GREEN (9/9); smoothed-wiring GREEN; 147 guards GREEN; typecheck 0; lint 0 errors (P12/P13 clean — both predicates via helpers only)
    - git log shows the page.tsx Lane B import and the smoothed-wiring mock extension in the SAME commit
    - grep -c "buildFactsheetPayloadCached" page.tsx == 2 exactly (declaration + the single Lane A call) — Lane B never references it
    - grep -n "searchParams" on the Lane B block returns nothing; the probe's second arg is user.id from auth.getUser() in the same function
    - Lane B probe select string is character-identical to Lane A's (paste both in SUMMARY)
  </acceptance_criteria>
  <done>Two-lane page live; SC1 + SC2-A + SC4 behavior tests green; zero collateral edits.</done>
</task>

<task type="auto">
  <name>Task 3: Falsifiability mutations for SC1 / SC2-A(behavior) / SC4 + VALIDATION.md records</name>
  <files>src/app/factsheet/[id]/v2/page.tsx, .planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-VALIDATION.md</files>
  <read_first>
    - .planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-VALIDATION.md (Falsifiability Ledger rows SC-1, SC-2A, SC-4 — the exact mutations are specified there)
    - 148-PATTERNS.md §6 (Rule-9 ledger convention: run the mutation, record the exact failure output, revert by RE-EDITING the mutated line — never git checkout --)
  </read_first>
  <action>
    Run three semantic production mutations, one at a time, each observed RED then reverted by
    re-edit (tree byte-identical after; verify with git diff):
    1. SC-1: at the Lane B probe, replace withPublishedOrOwner(q, user.id) with withPublishedOnly(q)
       → owner-lane tests 1/9 must redden.
    2. SC-2A: point the owner payload arm at buildFactsheetPayloadCached(`${id}::${computedAt}`)
       → the count-0 assertions (tests 4/5/6) must redden. Record this output in the
       page.owner-lane.test.tsx neuter-check header too (it is the header's named regression).
    3. SC-4: replace user.id with id (the route param) in the probe's second argument
       → the R8-style predicate-literal assertion (test 9) must redden.
    Update 148-VALIDATION.md: flip these ledger rows to ✅ Observed with one pasted failing
    assertion line each; update the Per-Task Verification Map statuses for 148-03 tasks.
    Re-run the full targeted battery after reverts to prove green restoration.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" "src/app/factsheet/[id]/v2/page.smoothed-wiring.test.tsx" --no-file-parallelism && git diff --quiet -- "src/app/factsheet/[id]/v2/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Three mutations OBSERVED red (pasted failure lines in VALIDATION.md, none "asserted — NOT observed")
    - page.tsx net diff after this task: only the neuter-check header note lives in the TEST file; production file byte-identical to Task 2's commit (git diff --quiet passes)
    - VALIDATION ledger rows SC-1, SC-2A, SC-4 show ✅ Observed
  </acceptance_criteria>
  <done>OWN-02's behavior proofs are falsifiable and the evidence is on disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/non-owner ↔ draft factsheet | the route is PUBLIC; the only thing between a draft and the world is this page's lane logic |
| session cookie ↔ owner predicate | user.id must originate from auth.getUser() in-function, never a request param |
| owner render ↔ shared data cache | one wrong call and an owner-built payload (or a draft-id null) serves anon for 3600s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-01 | Information Disclosure | owner-populated cache entry served to anon (SC2 — the phase's reason to exist) | mitigate | Lane B calls fetchAndBuildPayload directly; spy tests pin unstable_cache count 0 on owner renders incl. the draft-null case; sequence test owner→anon→404 |
| T-148-02 | Elevation of Privilege / Info Disclosure | admin-client builder with injected predicate — wrong-predicate call = full RLS bypass | mitigate | Probe-first ordering on the RLS request client (S1) so the admin-side predicate is the SECOND gate; owner lambda closes over session uid; R8 predicate-literal test; ⚠️ the no-owner-or-on-admin-client lint rule cannot catch withPublishedOrOwner(createAdminClient()…) — the probe-first ordering + review is the control, stated in-code |
| T-148-04 | Information Disclosure | existence oracle via differentiated 404 | mitigate | anon, non-owner-authed, and genuinely-missing all collapse to the same notFound(); probe errors log + Sentry server-side only, never change the status |
| T-148-03 | Information Disclosure | draft name/description via generateMetadata/OG | mitigate | generateMetadata untouched (published-only + "Strategy" fallback + unconditional noindex :301); pinned structurally in 148-04 |
| T-148-SC | Tampering | package installs | accept (vacuous) | Zero packages installed this phase |
</threat_model>

<verification>
- 9/9 owner-lane tests, smoothed-wiring, 147 guards, GUARD-02, typecheck, lint — all green.
- Three mutations observed RED and reverted; VALIDATION.md updated.
</verification>

<success_criteria>
- ROADMAP SC1 fully met; SC2 behavior layer met; SC4 met on every touched surface (Lane A
  byte-identical; publication writes untouched — DB trigger 20260716131000 stands).
</success_criteria>

<output>
Create `.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-03-SUMMARY.md` when done.
</output>
