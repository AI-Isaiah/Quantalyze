---
phase: 148-own-owner-factsheet-without-cache-disclosure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.owner-notice.test.tsx
autonomous: true
requirements: [OWN-02]

must_haves:
  truths:
    - "FactsheetBody with viewerNotice='owner_unpublished' renders the UI-SPEC banner as the FIRST child of <article id='factsheet-main'>, above FactsheetHeader"
    - "Every existing FactsheetBody call site (viewerNotice absent) renders byte-identically to today — zero new nodes"
    - "The banner carries role='note' aria-label='Visibility notice', prints (no print:hidden), and has no dismiss control"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "viewerNotice?: 'owner_unpublished' on FactsheetBodyOptions, threaded FactsheetView → FactsheetShell → FactsheetBody, plus OwnerUnpublishedNotice"
      contains: "viewerNotice"
    - path: "src/app/factsheet/[id]/v2/FactsheetView.owner-notice.test.tsx"
      provides: "banner render + absence + DOM-order proof"
      min_lines: 60
  key_links:
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "OwnerUnpublishedNotice"
      via: "conditional render before {!hideHeader && <FactsheetHeader …>}"
      pattern: "viewerNotice === \"owner_unpublished\""
---

<objective>
Give the factsheet body the owner-lane banner capability as an additive, default-off prop —
WITHOUT any consumer yet. The page (plan 148-03) wires it in Wave 2.

Purpose: OWN-02's owner lane needs a lane-derived "Unpublished — only you can see this" notice
(CONTEXT Area 2). The lane signal must live on FactsheetBodyOptions, NEVER on the cached
FactsheetPayload (UI-SPEC:112) — so no v6→v7 shape bump and no lane state in the shared cache.
Output: FactsheetView.tsx with the threaded prop + banner component; a new component test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-UI-SPEC.md
@.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-PATTERNS.md

Key pattern sections: PATTERNS §2 (FactsheetView analog — scenarioMode additive-prop precedent,
options interface :143-161, render position :199-211, three-hop threading chain :87-283,
NotEnoughDataPanel primitive :555-564 with the three UI-SPEC deltas), PATTERNS §7 (owner-notice
test analog — GUARD-02 harness, omitProp trick, per-test vi.stubGlobal block).
</context>

<interfaces>
Current signatures (from FactsheetView.tsx, verified by pattern map):

- `export function FactsheetView({ payload }: { payload: FactsheetPayload })` — :87, one prop today
- `FactsheetShell` — :100, takes only `payload`, returns `<FactsheetBody payload={payload} />` at :140
- `export interface FactsheetBodyOptions` — :143-161, fields hideHeader / hideAllocatorSection /
  hideFooter / topSlot / scenarioMode, each with a doc-comment naming who passes it and why the
  default keeps other call sites byte-identical
- `FactsheetBody({ payload, hideHeader = false, … }: { payload: FactsheetPayload } & FactsheetBodyOptions)` — :171-178
- Render target: `<article id="factsheet-main" …>` :199-206; `{!hideHeader && <FactsheetHeader payload={payload} />}` :207; `{topSlot}` :208

New contract this plan creates (consumed by 148-03):

- `FactsheetBodyOptions.viewerNotice?: "owner_unpublished"` — discriminated string union, NOT a
  boolean (Phase 149/152 extend it without a second prop)
- `FactsheetView({ payload, viewerNotice }: { payload: FactsheetPayload; viewerNotice?: "owner_unpublished" })`
- `OwnerUnpublishedNotice` — private component in FactsheetView.tsx (not exported beyond the file)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add viewerNotice prop + OwnerUnpublishedNotice banner (test-first)</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.owner-notice.test.tsx, src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <read_first>
    - DESIGN.md (CLAUDE.md mandate: read before ANY visual/UI decision — fonts, colors, spacing, aesthetic direction; the banner's tokens must be checked against it, not just against the UI-SPEC excerpts)
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (:80-229 prop chain + article structure, :540-569 NotEnoughDataPanel)
    - src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx (FULL — harness :98-110, byte-identity :112-120, stub block :47-74 incl. the per-test beforeEach vi.stubGlobal requirement, DEF-16-1)
    - 148-PATTERNS.md §2 and §7 (the three UI-SPEC deltas table: text-caption not text-micro, role="note" + aria-label, mb-6, h2 not h3)
    - 148-UI-SPEC.md Component Contract 1 + Copywriting Contract (copy is VERBATIM)
  </read_first>
  <behavior>
    - Test 1: FactsheetBody with viewerNotice="owner_unpublished" and hideHeader={false} renders a
      section[role="note"][aria-label="Visibility notice"] containing heading text
      "Unpublished — only you can see this" and body text "This factsheet is visible only from the
      account that uploaded the strategy. Anyone else who opens this link sees a 404 until
      Quantalyze review publishes it." (both LITERALS typed into the test, never imported)
    - Test 2: DOM order — the note section precedes the FactsheetHeader masthead inside
      article#factsheet-main (compareDocumentPosition or index-of on container.innerHTML; requires
      hideHeader={false})
    - Test 3: viewerNotice absent AND viewerNotice={undefined} → querySelector('[role="note"]')
      is null — zero nodes, not a hidden node
    - Test 4: the section's className contains "mb-6", "border-border", "bg-surface-subtle" and
      does NOT contain "print:hidden"; heading tag is H2 with classes text-caption font-semibold
      uppercase tracking-[0.18em]; body p is text-caption (NOT text-micro) text-text-muted
  </behavior>
  <action>
    RED first: create src/app/factsheet/[id]/v2/FactsheetView.owner-notice.test.tsx as a sibling
    of the GUARD-02 file — clone its mount harness (FactsheetProvider with persist={false},
    populatedPayload fixture, the omitProp spread trick) and its per-test vi.stubGlobal block
    verbatim (vitest.config.ts sets unstubGlobals: true, so module-scope stubs leak — DEF-16-1).
    Run it; record the RED (banner assertions fail — prop does not exist yet).

    GREEN: in FactsheetView.tsx —
    1. Add `viewerNotice?: "owner_unpublished"` to FactsheetBodyOptions with a doc-comment in the
       scenarioMode convention: names that only the v2 page's owner lane passes it, and that
       undefined on every existing call site (page.tsx:463, AllocationDashboardV2.tsx:162,
       ScenarioFactsheetChart.tsx:237) renders zero nodes so GUARD-02 byte-identity holds.
    2. Thread through all three hops: FactsheetView signature gains optional viewerNotice;
       FactsheetShell gains and forwards it; FactsheetBody destructures it.
    3. Add private function OwnerUnpublishedNotice (no props): section with role="note",
       aria-label="Visibility notice", className "mb-6 border border-border bg-surface-subtle
       px-4 py-3"; h2 "text-caption font-semibold uppercase tracking-[0.18em] text-text-primary"
       with the heading copy; p "mt-1 text-caption text-text-muted" with the body copy. Copy
       strings VERBATIM from UI-SPEC Copywriting Contract. Reuse the NotEnoughDataPanel treatment
       but apply the three UI-SPEC deltas (text-caption body per UI-SPEC:66, role/aria per :110,
       mb-6 per :97, h2 because it precedes the masthead). No icon, no dismiss, no print:hidden.
    4. Render `{viewerNotice === "owner_unpublished" && <OwnerUnpublishedNotice />}` as the FIRST
       child inside article#factsheet-main, BEFORE the `{!hideHeader && <FactsheetHeader …>}` line
       (:207). ⛔ Do NOT reuse topSlot — it renders below the masthead (UI-SPEC:97 violation).
    Do not touch any other consumer file. Do not add any field to FactsheetPayload (no v6→v7 bump).
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetView.owner-notice.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - RED observed and recorded before implementation (paste one failing assertion line in the SUMMARY)
    - All 4 behavior tests green; exit code 0
    - grep -c "viewerNotice" "src/app/factsheet/[id]/v2/FactsheetView.tsx" ≥ 4 (interface field, three hop signatures/forwards, render condition)
    - grep -n "print:hidden" on the new section returns nothing; grep -n "text-micro" inside OwnerUnpublishedNotice returns nothing
    - Copy literals in the test file are typed strings, not imports from FactsheetView.tsx (Oracle Independence)
  </acceptance_criteria>
  <done>Banner renders exactly per UI-SPEC on the prop, renders nothing without it, test file green.</done>
</task>

<task type="auto">
  <name>Task 2: Byte-identity + regression battery</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx (:112-141 — GUARD-02 byte-identity + Overview scan, PERMANENT gate; do NOT edit it)
    - 148-PATTERNS.md Pinned Literals P5 (GUARD-02), P11 (type-scale glob — factsheet v2 NOT in scope, do not widen), P14 (no-raw-font-px — named tokens only)
  </read_first>
  <action>
    Run the full regression battery for the additive prop. If GUARD-02 reddens, the prop default
    is wrong (a node renders when unset) — fix FactsheetView.tsx, never the test. Confirm the
    three existing FactsheetBody consumers are untouched: git diff must show changes ONLY in
    FactsheetView.tsx and the new test file. Do not "harmonize" NotEnoughDataPanel's text-micro
    (Rule 3 — surgical).
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx" "src/app/factsheet/[id]/v2/FactsheetView.owner-notice.test.tsx" --no-file-parallelism && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - GUARD-02 "renders byte-identically with default props vs scenarioMode={false}" passes UNMODIFIED (git diff on FactsheetBody.scenario-mode.test.tsx is empty)
    - typecheck exit 0; lint 0 errors (no-raw-font-px clean — banner uses text-caption/text-text-muted named tokens)
    - git status shows exactly 2 changed files: FactsheetView.tsx + FactsheetView.owner-notice.test.tsx
  </acceptance_criteria>
  <done>Additive prop proven byte-neutral on every existing call site; suite, types, lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared cache ↔ render props | lane state must never enter the object the public cache serves |
| component API ↔ 3 external consumers | an accidental default-on banner would brand published factsheets as unpublished |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-05 | Information Disclosure | viewerNotice channel | mitigate | Prop on FactsheetBodyOptions, structurally NOT on FactsheetPayload (UI-SPEC:112); no v6→v7 bump; GUARD-02 byte-identity proves default-off; 148-04's gate later pins that the cached payload shape is untouched |
| T-148-05b | Tampering (integrity of published render) | OwnerUnpublishedNotice default | mitigate | Test 3 asserts zero nodes when unset; GUARD-02 permanent gate reddens on any default-render drift |
| T-148-SC | Tampering | package installs | accept (vacuous) | Zero packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- New test file green; GUARD-02 green unmodified; typecheck + lint clean.
- Diff surface: exactly FactsheetView.tsx + the new test file.
</verification>

<success_criteria>
- viewerNotice capability exists, default-off, byte-neutral — ready for 148-03 to consume.
- UI-SPEC Component Contract 1 satisfied verbatim (copy, tokens, placement, a11y, print).
</success_criteria>

<output>
Create `.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-01-SUMMARY.md` when done.
</output>
