---
phase: 148-own-owner-factsheet-without-cache-disclosure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/factsheet/[id]/v2/page.tsx
  - TODOS.md
autonomous: true
requirements: [OWN-02]

must_haves:
  truths:
    - "fetchAndBuildPayload requires a visibility parameter — a call site that makes no visibility decision does not compile"
    - "buildFactsheetPayloadCached keeps a visibility-free signature and hard-codes the withPublishedOnly LITERAL inside the unstable_cache callback — an owner predicate is unrepresentable there (type-level SC2-B)"
    - "The page's cache-key header comment describes reality (id-only key), not the false ${id}::${computedAt} claim"
    - "The route is pinned dynamic (force-dynamic) so an authed-rendered HTML response can never be response-cached and served to anon"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/page.tsx"
      provides: "DI seam: fetchAndBuildPayload(id, visibility) + literal-wired cached wrapper + corrected comment + force-dynamic"
      contains: "export const dynamic = \"force-dynamic\""
    - path: "TODOS.md"
      provides: "logged §3a behavioral staleness finding (log-only per blast-radius bar)"
  key_links:
    - from: "buildFactsheetPayloadCached"
      to: "fetchAndBuildPayload"
      via: "unstable_cache callback"
      pattern: "fetchAndBuildPayload\\(id, withPublishedOnly\\)"
---

<objective>
Install the DI seam that IS this phase (orchestrator ruling): parameterize the payload builder by
a required visibility predicate while the cached wrapper stays visibility-free with the public
predicate as a literal. Correct the false cache-key comment. Pin the route dynamic. No owner lane
yet — `withPublishedOrOwner` is deliberately NOT imported in this plan, so the existing
page.smoothed-wiring.test.tsx mock survives untouched (Pitfall 2 fires only in 148-03).

Purpose: G2 (page.tsx:37, inside fetchAndBuildPayload on the service-role admin client) is the
gate the ROADMAP never names — the root cause of OWN-02 (Rule 6). Leaving it published-only while
flipping G1 would ship the "still computing" placeholder to owners AND cache a draft-id null for
3600s (RESEARCH §3b). This plan makes the seam exist so 148-03's lane branch is a wiring change.
Output: refactored page.tsx (behavior-identical for every current caller), TODOS.md entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-RESEARCH.md
@.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-PATTERNS.md

Key sections: RESEARCH §1 (G2/G4 census), §3a (⛔ computedAt is split off and DISCARDED at
page.tsx:229; effective unstable_cache key is cb.toString() + "factsheet-v2-payload-v6," + id —
verified against node_modules/next/dist/server/web/spec-extension/unstable-cache.js:55,82),
§3e (force-dynamic recommendation + tearsheet precedent). PATTERNS §1b (builder head excerpt),
§1c (cached wrapper excerpt), §1g (the tearsheet force-dynamic comment to copy).
</context>

<interfaces>
Current (page.tsx, verified in pattern map):

- `async function fetchAndBuildPayload(id: string): Promise<FactsheetPayload | null>` — :35; its
  FIRST statement set: createAdminClient() then `withPublishedOnly(supabase.from("strategies").select(\`…\`).eq("id", id)).maybeSingle()`
- `function buildFactsheetPayloadCached(cacheKey: string)` — :226-266; `const [id] = cacheKey.split("::")` at :229;
  keyParts `["factsheet-v2-payload-v6", id]` :260; `{ revalidate: 3600, tags: ["factsheet-v2", \`factsheet-v2:${id}\`] }` :261-264
- Visibility helpers (src/lib/visibility.ts): `withPublishedOnly<Q>(query: Q): Q` (:74),
  `withPublishedOrOwner<Q>(query: Q, authUserId: string): Q` (:115) — both `<Q>(q: Q) => Q` shaped

Contract this plan creates (consumed by 148-03 and pinned by 148-04):

- `type StrategyVisibility = <Q>(query: Q) => Q;` — local to page.tsx
- `async function fetchAndBuildPayload(id: string, visibility: StrategyVisibility): Promise<FactsheetPayload | null>`
  — visibility REQUIRED, no default (a defaulted param would let a call site silently make no decision)
- `buildFactsheetPayloadCached(cacheKey: string)` — signature UNCHANGED; callback becomes
  `async () => fetchAndBuildPayload(id, withPublishedOnly)` with withPublishedOnly as a LITERAL, never a variable
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize the builder; literal-wire the cached wrapper; fix the comment; pin dynamic</name>
  <files>src/app/factsheet/[id]/v2/page.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/page.tsx (FULL, 466 lines — all four gate sites, the :236-259 shape-version bump ledger, the :32-33 false comment)
    - 148-PATTERNS.md §1b (⛔ do NOT touch the .select(...) string — it names daily_returns AND returns_series; 147 Layer-A gate P1 reddens if either drops; P2 pins the literal resolveDailyReturnSeries( in this file)
    - src/app/factsheet/[id]/tearsheet/page.tsx (:17-25 — the force-dynamic comment to copy, adapted)
    - src/lib/visibility.ts (:74-125 — both helper contracts)
  </read_first>
  <action>
    Four surgical edits to page.tsx, nothing else (D-lane design per CONTEXT locked decisions +
    orchestrator ruling "the DI seam is the phase"):

    1. Declare `type StrategyVisibility = <Q>(query: Q) => Q;` near the top (after imports).
       Change the signature to `fetchAndBuildPayload(id: string, visibility: StrategyVisibility)`
       — required second parameter, NO default. Inside, replace the single `withPublishedOnly(`
       wrapping call with `visibility(`. ⛔ The .select(...) template string is byte-untouched.
       No other interior change — grep confirms G2 was the only gate inside the builder
       (RESEARCH §1: src/lib/factsheet/ has zero visibility predicates).

    2. In buildFactsheetPayloadCached, change the unstable_cache callback to
       `async () => fetchAndBuildPayload(id, withPublishedOnly)` — the literal identifier, never
       a variable or parameter. Signature of buildFactsheetPayloadCached stays exactly
       `(cacheKey: string)` (owner predicate = type error there; this is SC2-B's type-level half).
       keyParts, revalidate, tags: byte-unchanged (P15 — the admin strategy-review revalidateTag
       at route.ts:501 keeps working; no v6→v7 bump anywhere in this phase).

    3. Correct the FALSE header comment at :32-33 (orchestrator ruling: fix in-phase). Replace the
       "Cache key = ${id}::${computedAt} so a new analytics row automatically misses cache" claim
       with the truth: the callback's cacheKey string is split at "::" and everything after the id
       is DISCARDED (:229); the effective unstable_cache key is id-only
       (cb.toString() + keyParts ["factsheet-v2-payload-v6", id], args []); entries live the full
       revalidate=3600s or until the admin publish flow's revalidateTag("factsheet-v2:${id}").
       State explicitly that a fresh computed_at does NOT bust the cache (see TODOS.md entry,
       Task 2) and that lane separation can therefore never be done via the cacheKey string.

    4. Add `export const dynamic = "force-dynamic";` with the tearsheet precedent's comment
       adapted to this route (RESEARCH §3e / A3, taken deliberately): once render output varies by
       session identity (Lane B lands in 148-03), implicit dynamism via cookies() is a property a
       future refactor can silently remove; the failure mode is an owner-rendered HTML response
       cached and served to anon. Copy the comment's reasoning per PATTERNS §1g — comment parity
       is what makes the pin survive refactors.

    ⛔ Do NOT import withPublishedOrOwner in this plan (keeps page.smoothed-wiring.test.tsx's
    partial visibility mock green — Pitfall 2 is 148-03's same-commit obligation).
    ⛔ Do NOT touch generateMetadata (G3 stays published-only — orchestrator ruling; the existing
    "Strategy" fallback + unconditional robots noindex at :301 already satisfy "minimal + noindex").
    ⛔ Do NOT touch the G1 signature gate at :342 (Lane A stays byte-identical; 148-03 adds the
    miss-branch beside it).
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run "src/app/factsheet/[id]/v2/page.smoothed-wiring.test.tsx" src/__tests__/phase-147-series-resolution-guards.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - typecheck exit 0 — proves the required param propagated to the ONLY call site (the cached wrapper)
    - page.smoothed-wiring.test.tsx green UNMODIFIED (its withPublishedOnly passthrough mock + unstable_cache identity stub still satisfy the refactor; git diff on that file is empty)
    - phase-147-series-resolution-guards green (P1/P2: select string + resolveDailyReturnSeries( literal untouched)
    - Source assertions: grep -c "fetchAndBuildPayload(id, withPublishedOnly)" page.tsx == 1; grep -c "visibility: StrategyVisibility" page.tsx == 1; grep -c "export const dynamic" page.tsx == 1; grep -n "::\${computedAt}\` so a new analytics row" page.tsx returns nothing (false claim gone)
    - grep -c "withPublishedOrOwner" page.tsx == 0 (import deferred to 148-03 by design)
    - buildFactsheetPayloadCached signature still exactly "(cacheKey: string)" (grep the declaration line)
  </acceptance_criteria>
  <done>Seam installed; every current caller behavior-identical; comment tells the truth; route pinned dynamic.</done>
</task>

<task type="auto">
  <name>Task 2: Log the staleness finding to TODOS.md + lint battery</name>
  <files>TODOS.md</files>
  <read_first>
    - TODOS.md (top ~80 lines — match the existing entry format; backlog = ONE ground truth, root TODOS.md only)
    - 148-RESEARCH.md §3a consequence 3 (the pre-existing latent defect being logged)
  </read_first>
  <action>
    Append ONE entry to TODOS.md (orchestrator ruling: log-only, blast-radius bar): the factsheet
    v2 payload cache is id-only-keyed, so a re-derive that stamps a fresh
    strategy_analytics.computed_at does NOT bust the factsheet-v2-payload-v6 entry — the factsheet
    can serve metrics up to 3600s stale despite the (now-corrected) header comment having claimed
    otherwise. Reference page.tsx:229 (split discards computedAt) and
    node_modules/next unstable-cache.js:55,82 key derivation; note the existing mitigations
    (1h TTL ceiling; revalidateTag on admin publish at api/admin/strategy-review/route.ts:501)
    and that the founder stopping rule classifies this as staleness, not user-facing/data-integrity
    — do NOT fix in-phase. Then run lint to prove P12/P13 (no-raw-published-predicate /
    no-owner-or-on-admin-client, both repo-wide "error") are clean on the refactor.
  </action>
  <verify>
    <automated>npm run lint && grep -c "factsheet-v2-payload-v6" TODOS.md</automated>
  </verify>
  <acceptance_criteria>
    - TODOS.md gains exactly one new entry naming: id-only cache key, computed_at not key-participating, 3600s ceiling, revalidateTag-on-publish, log-only rationale
    - lint exit 0, 0 errors; grep exit 0 (entry present)
    - No other file modified in this task
  </acceptance_criteria>
  <done>Finding durably logged in the single backlog; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request client (RLS on) ↔ admin client (BYPASSRLS) | inside fetchAndBuildPayload the injected predicate is the ONLY gate — service_role bypasses strategies_read |
| shared Next data cache ↔ anon readers | whatever the cached callback builds is served to every visitor for ≤3600s |
| Next response cache ↔ session-varying render | an authed-rendered HTML response must never be statically cached |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-02 | Elevation of Privilege / Info Disclosure | fetchAndBuildPayload on createAdminClient (predicate omission or wrong predicate = full RLS bypass, EVERY user's drafts) | mitigate | visibility is a REQUIRED param — omission is a compile error, so no call site can silently make no visibility decision; the cached wrapper hard-codes the withPublishedOnly literal; 148-04 pins the literal with a source-scan gate |
| T-148-01 | Information Disclosure | shared unstable_cache entry (owner-built payload served to anon — SC2) | mitigate | This plan makes an owner predicate UNREPRESENTABLE in the cached wrapper (visibility-free signature, type error); behavioral proof lands in 148-03, structural pin in 148-04 |
| T-148-07 | Information Disclosure | response-level caching of a session-varying render | mitigate | `export const dynamic = "force-dynamic"` + tearsheet-precedent comment (RESEARCH §3e) |
| T-148-03 | Information Disclosure | generateMetadata / OG draft leakage | mitigate | G3 deliberately untouched: published-only query + "Strategy" fallback + unconditional robots noindex (:301); 148-04's gate asserts generateMetadata never contains withPublishedOrOwner |
| T-148-SC | Tampering | package installs | accept (vacuous) | Zero packages installed this phase |
</threat_model>

<verification>
- typecheck + lint + smoothed-wiring + 147 guards all green with ZERO test-file edits.
- Diff surface: page.tsx + TODOS.md only.
</verification>

<success_criteria>
- The DI seam exists exactly as ruled: required visibility on the builder, visibility-free cached
  wrapper with the public literal. Anon path byte-identical (Lane A untouched: G1 :342, cache key,
  tags, TTL all unchanged).
- False comment corrected; behavioral staleness logged, not fixed.
</success_criteria>

<output>
Create `.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-02-SUMMARY.md` when done.
</output>
