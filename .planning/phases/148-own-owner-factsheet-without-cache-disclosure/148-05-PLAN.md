---
phase: 148-own-owner-factsheet-without-cache-disclosure
plan: 05
type: execute
wave: 4
depends_on: [148-03, 148-04]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.own04-link.test.tsx
  - TODOS.md
  - .planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-VALIDATION.md
autonomous: true
requirements: [OWN-04]

must_haves:
  truths:
    - "Both wizard success branches (composite :1917, single-key :2194) render the 'View full factsheet →' text link to /factsheet/{strategyId}/v2 — same element, same copy, both sites (SC3)"
    - "The link opens a new tab (target=_blank rel='noopener noreferrer') with a persistent underline (WCAG 1.4.1 per UI-SPEC — NOT the hover-only precedent)"
    - "Pre-success states (kicking_off / waiting_for_complete / gate_failed) render NO link node — structurally absent, never disabled/greyed"
    - "No link shipped in this phase can land on notFound(): OWN-02's owner lane (148-03) is already merged before this plan starts (wave ordering)"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      provides: "one local link component rendered at both success sites (copy cannot drift)"
      contains: "wizard-view-full-factsheet"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.own04-link.test.tsx"
      provides: "SC3 proofs — both branches + structural absence"
      min_lines: 80
  key_links:
    - from: "SyncPreviewStep.tsx"
      to: "/factsheet/${strategyId}/v2"
      via: "next/link, both success branches"
      pattern: "factsheet/\\$\\{strategyId\\}/v2"
---

<objective>
OWN-04: the wizard preview links to the real factsheet — strictly AFTER OWN-02 (this plan is
wave 4; the owner lane landed in wave 2), so the link can never dead-end on notFound(). Then run
the phase-final gate and close the VALIDATION sign-off.

Purpose: after finalize, the founder's next question is "where is my strategy?" — today the
wizard shows a preview panel with no route to the full factsheet. CONTEXT locks reuse of
/factsheet/{id}/v2 (no second preview route — the drift class the reuse rules forbid).
Output: link at both success sites, sibling test file, TODOS entries, phase gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-UI-SPEC.md
@.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-PATTERNS.md

Key sections: UI-SPEC Component Contract 2 + Copywriting Contract (copy VERBATIM; text link not
button; mt-3 under the FactsheetPreview panel, ABOVE the CTA row; no finalize-step repetition —
UI-SPEC decided DO NOT repeat). PATTERNS §3 (both insertion sites with surrounding code, the
:1781 structural-presence idiom, WizardChrome :250-260 target=_blank precedent, the two Rule-7
style conflicts UI-SPEC wins, data-testid convention wizard-<kebab-action>), §8 (test analogs:
single-key passed driver :855-900, composite fixture builder, THIRD-sibling-file convention —
the two existing render test files stay FROZEN).
</context>

<interfaces>
- `SyncPreviewStep` props: `strategyId: string` — REQUIRED, non-optional (:274, destructured :406);
  non-null in both success branches by construction (:142-143) — no null-guard needed
- `Link` from next/link — already imported at SyncPreviewStep.tsx:4
- `/factsheet` is in PUBLIC_ROUTES (src/proxy.ts:17, allow-listed :109) — no proxy bounce
- `FactsheetPreview` — shared component; do NOT modify it; the link is a SIBLING node below it
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write SyncPreviewStep.own04-link.test.tsx — RED (link absent today)</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.own04-link.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx (:37-68 base props + mock set, :855-900 the single-key passed driver: fake timers, installDeribitPassMock(30), advanceTimersByTimeAsync — clone the driver, do NOT edit the file)
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx (:1-120 — CompositeMockOpts fixture builder + the header convention: existing files stay FROZEN, new pins get a sibling file)
    - 148-PATTERNS.md §8 (structural-absence assertion style: queryByTestId .not.toBeInTheDocument() AND queryByRole("link", { name: /view full factsheet/i }) null — the no-disabled-buttons UAT direction)
  </read_first>
  <action>
    Create a THIRD sibling test file (never edit the two frozen render files). Drive four states
    using the cloned fixtures:
    1. Single-key `passed`: getByTestId("wizard-view-full-factsheet") is an <a> with
       href "/factsheet/strat-1/v2" (base-prop strategyId literal), target "_blank",
       rel "noopener noreferrer"; accessible name matches /view full factsheet/i; className
       contains "underline" and "underline-offset-4" (persistent underline, not hover-only);
       caption "Visible only to you until the strategy is published." present; the link node
       precedes the "wizard-use-this-key" CTA in DOM order.
    2. Composite `passed` (composite fixture): same testid present with the same href shape —
       same element, same copy, both sites (UI-SPEC:118).
    3. Structural absence: in kicking_off / waiting_for_complete / gate_failed renders,
       queryByTestId("wizard-view-full-factsheet") is null AND
       queryByRole("link", { name: /view full factsheet/i }) is null — no disabled/greyed variant
       exists anywhere.
    Expected strings (href, rel, copy) are LITERALS typed into this file. Run → record RED
    (link does not exist yet).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.own04-link.test.tsx" --no-file-parallelism; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - RED observed (paste one failing line in SUMMARY); the two frozen render test files show empty git diff
    - Both branches driven through REAL state machines (fixtures), not by rendering a fragment
    - All expected values literal in-file (oracle independence)
  </acceptance_criteria>
  <done>SC3's spec exists and fails against current code.</done>
</task>

<task type="auto">
  <name>Task 2: Add the link at both success sites + log the two precedent divergences</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, TODOS.md</files>
  <read_first>
    - DESIGN.md (CLAUDE.md mandate: read before ANY visual/UI decision — the link treatment, color tokens, underline affordance and spacing must be checked against it, not just against the UI-SPEC excerpts)
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx (:1895-1939 composite site, :2178-2227 single-key site incl. the CTA row :2208-2219; :1775-1785 the structural-presence Link idiom)
    - 148-UI-SPEC.md Component Contract 2 (placement mt-3 under the panel inside/below its mt-6 block, above the CTA row; affordance: text-small font-medium text-accent underline underline-offset-4, trailing "→" with preceding space, hover text-accent-hover 150ms ease-out)
    - 148-PATTERNS.md §3 (Rule-7 conflicts: WizardChrome hover:underline and ConnectKeyStep rel="noopener" — UI-SPEC WINS both; log divergences, do not fix them)
  </read_first>
  <action>
    In SyncPreviewStep.tsx define ONE local component (e.g. `ViewFullFactsheetLink({ strategyId }:
    { strategyId: string })`) so the copy cannot drift between sites (UI-SPEC:118): a div mt-3
    containing (1) Link href={`/factsheet/${strategyId}/v2`} target="_blank"
    rel="noopener noreferrer" data-testid="wizard-view-full-factsheet" className per UI-SPEC
    ("text-small font-medium text-accent underline underline-offset-4 transition-colors
    duration-150 ease-out hover:text-accent-hover") with children "View full factsheet →" plus a
    sr-only " (opens in new tab)" span (cheap a11y, axe-covered surface); (2) p.mt-1 text-caption
    text-text-muted with "Visible only to you until the strategy is published." Copy VERBATIM
    from the UI-SPEC Copywriting Contract.

    Render it directly below the FactsheetPreview wrapper div at BOTH sites — composite (:1917
    area) and single-key (:2194 area, ABOVE the mt-6 CTA row). No conditional beyond the branch
    itself: structurally present in success branches, structurally absent elsewhere — never a
    disabled state. Do not modify FactsheetPreview, WizardChrome, or ConnectKeyStep.

    TODOS.md: append ONE log-only entry naming the two in-wizard link-style divergences from the
    now-authoritative UI-SPEC treatment — WizardChrome.tsx:255 (hover-only underline) and
    ConnectKeyStep.tsx:661 (rel="noopener" without noreferrer) — flagged for a future cleanup
    pass, explicitly NOT fixed here (Rule 3 / orchestrator ruling).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.own04-link.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx" --no-file-parallelism && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - own04-link tests GREEN; both frozen sibling render files GREEN and UNMODIFIED
    - grep -c "wizard-view-full-factsheet" SyncPreviewStep.tsx == 1 (single definition) and grep -c "ViewFullFactsheetLink" ≥ 3 (definition + two usages)
    - grep -c "factsheet/\${strategyId}/v2" SyncPreviewStep.tsx == 1 (href lives in one place)
    - TODOS.md names both divergent files:lines; typecheck 0; lint 0 errors
  </acceptance_criteria>
  <done>SC3 shipped: one element, verbatim copy, two sites, zero disabled states.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate + SC3 mutation + VALIDATION sign-off</name>
  <files>.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-VALIDATION.md, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx</files>
  <read_first>
    - 148-VALIDATION.md (ledger row SC-3; the sign-off checklist)
  </read_first>
  <action>
    1. SC-3 mutation (second member of the class — the site an author forgets): delete the
       ViewFullFactsheetLink usage from the COMPOSITE branch only → the composite test must
       redden while single-key stays green (proves per-site coverage, not one shared assertion).
       Observe, record the failing line in VALIDATION.md, revert by re-edit (git diff --quiet).
    2. Phase-final gate: full suite via `npm run test:coverage` (this is the command that
       enforces the blocking coverage thresholds locally — plain `npm test` runs without
       `--coverage` and proves nothing about the gate), `npm run typecheck`, `npm run lint`,
       plus the targeted set — GUARD-02 (FactsheetBody.scenario-mode),
       phase-147-series-resolution-guards, phase-148-owner-lane-cache-isolation, page.owner-lane,
       page.smoothed-wiring, FactsheetView.owner-notice, SyncPreviewStep.own04-link. ⚠️ CI is
       Node 22 vs local 25 — if the full suite reddens only in CI later, reproduce with
       PATH=/opt/homebrew/opt/node@22/bin before blaming a flake.
    3. 148-VALIDATION.md: complete the Per-Task Verification Map statuses, confirm every ledger
       row is ✅ Observed (or skipped-with-reason), tick the Oracle Independence checklist and the
       Validation Sign-Off, set frontmatter wave_0_complete: true and status: executed, and stamp
       Approval with the date. Note SC-4's publication-remains-admin-only clause as
       satisfied-by-absence: zero writes in the phase diff; DB trigger 20260716131000 stands.
  </action>
  <verify>
    <automated>npm run test:coverage && npm run typecheck && npm run lint && git diff --quiet -- "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Full vitest suite green under `npm run test:coverage` (record test counts AND the four coverage numbers in SUMMARY); coverage thresholds clear (blocking gate 82/80/74/72 — enforced by this command, exit non-zero on breach); typecheck 0; lint 0 errors
    - SC-3 mutation observed RED at the composite site with single-key green, then reverted
    - 148-VALIDATION.md: no ⬜ pending ledger rows; sign-off boxes ticked; Approval stamped
  </acceptance_criteria>
  <done>Phase 148 fully gated: all 4 ROADMAP SCs proven, ledger closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner's wizard session ↔ draft URL | the link renders only inside the uploading account's authed wizard flow |
| shared URL ↔ non-owner recipients | anyone else opening the link must hit the SC4 404, non-oracularly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-06 | Information Disclosure | wizard link exposing a draft URL to a non-owner session | mitigate (by construction — assert it) | The link renders ONLY in SyncPreviewStep's success branches, which exist only in the owner's authed wizard session (strategyId is the owner's own draft); structural-absence tests pin that no other state renders it. The URL itself is not a secret: a non-owner opening it hits 148-03's uniform notFound() (no existence oracle), proven by SC4 tests. rel="noopener noreferrer" prevents opener tampering/referrer leakage from the new tab |
| T-148-06b | Spoofing/Tampering | reverse-tabnabbing via target="_blank" | mitigate | rel="noopener noreferrer" asserted literally in the test |
| T-148-01 | Information Disclosure | link driving an owner render that populates the shared cache | mitigate | Already closed by 148-03/148-04 (link lands on the owner lane — cache untouched); wave ordering makes a dead-end or cache-touching link unshippable in this phase |
| T-148-SC | Tampering | package installs | accept (vacuous) | Zero packages installed this phase |
</threat_model>

<verification>
- Full suite (with coverage thresholds) + typecheck + lint green; all frozen sibling tests unmodified; ledger closed.
</verification>

<success_criteria>
- ROADMAP SC3 met: the wizard links to the full factsheet at both success sites and no link
  shipped in this phase can land on notFound() (OWN-02 merged first — structural, wave-enforced).
- Phase-wide: all 4 SCs hold with observed falsifiability evidence.
</success_criteria>

<output>
Create `.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-05-SUMMARY.md` when done.
</output>
