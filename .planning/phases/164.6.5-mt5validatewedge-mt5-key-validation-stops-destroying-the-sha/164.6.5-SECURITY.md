---
phase: "164.6.5"
slug: "mt5validatewedge-mt5-key-validation-stops-destroying-the-sha"
status: open_threats
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 5
asvs_level: 1
block_on: high
created: "2026-09-26"
audited_at_sha: 4c0c10febe4def04d3d6df979bef04678ca8688b
---

# Phase 164.6.5 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

**Verdict: OPEN_THREATS.** 38 of 43 threats are closed. The 5 open threats are all from
**plan 08** (the release and phase-close plan), which has **not been executed**: there is no
`164.6.5-08-SUMMARY.md`, and `VERSION`, `package.json` and `CHANGELOG.md` have not moved since
the merge base. None of the five is a gap in shipped code. Each closes by executing plan 08 as
written, followed by a re-run of this audit (see "Open threats" below).

`asvs_level` and `block_on` are not set in `.planning/config.json`, so the workflow defaults
(level 1, block on high) apply. The checks below went further than level 1: each mitigation
was traced to the boundary where it has to hold, not just grep-matched.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| analytics-service → rpyc bridge (gateway) | unauthenticated arbitrary-remote-code channel (T-134-03), private mesh only | two committed remote literals; one by-value int argument |
| heal cadence → the ONE shared terminal | a periodic loop can now end the terminal process every client depends on | a process terminate, then a bare `initialize()` |
| browser → wizard / key API routes | client-supplied credentials and correlation headers | credentials (untrusted), `X-Correlation-Id`, `X-Wizard-Page-Load-Id` |
| analytics-service error → wizard copy / Sentry | a remote error can echo a submitted account or server | wire codes and static authored detail |
| prober arm → public Actions log | remedy and detail strings are world-readable | authored strings only |
| repo → public | the repo is PUBLIC and `.planning/`, `docs/` and `deploy/` are tracked | prose, fixtures, identifiers |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Evidence | Status |
|-----------|----------|-----------|----------|-------------|----------|--------|
| T-164.6.5-01 | Information Disclosure | runbook transcript (`docs/runbooks/mt5-go-live.md`) | high | mitigate | Every added line on the branch was swept for account-shaped runs of 7–12 digits and for machine paths. The only hits were synthetic repeated-digit test values plus one synthetic planted path in a test. Plan 01 recorded `check-planning-hygiene.ts` as OK. | closed |
| T-164.6.5-02 | Information Disclosure | proof-of-absence publishing its subject | medium | mitigate | The runbook's Step 2b verdicts are recorded as verdicts and counts only ("Verdict — criterion 1 is EXPLICITLY OPEN"). No specimen and no search pattern is quoted. | closed |
| T-164.6.5-03 | Repudiation | criterion 1 falsely closed | high | mitigate | The runbook section "Verdict — criterion 1 is EXPLICITLY OPEN" records D-03a as UNDECIDED. `TODOS.md` carries the routed residual `MT5-SWITCH-WEDGE-CAUSE-01`. | closed |
| T-164.6.5-04 | Tampering | live gateway edited during evidence capture | medium | mitigate | The capture was a founder act, read-only by instruction. Agents made no gateway command. The only agent-made remote actions are the two committed read/terminate literals below. | closed |
| T-164.6.5-05 | Elevation of Privilege | recycle verb over rpyc classic | critical | mitigate | `Mt5Client.recycle_terminal_process` is ONE method. It has no parameters (pinned by `test_TERMINAL_RECYCLE_the_verb_structurally_cannot_accept_a_credential`), sends the committed `_REMOTE_TERMINAL_RECYCLE_SRC`, and has no general exec helper or `__getattr__` passthrough. The only production caller is `_escalate_ipc_fault`. | closed |
| T-164.6.5-06 | Tampering | run-time assembly of remote source | high | mitigate | `test_TERMINAL_RECYCLE_the_remote_source_is_a_committed_literal_with_no_interpolation` asserts an AST `Constant` with no `{`, `}`, `%(`, `%s` or `%d`. `test_SESSION_SNAPSHOT_the_remote_source_is_a_committed_literal_that_can_only_read` asserts the same for `_REMOTE_SESSION_SNAPSHOT_SRC`, and also that it has no `__` and no `import`. Its only `mt5` calls are `terminal_info`, `account_info` and `last_error`, and it takes no argument. | closed |
| T-164.6.5-07 | Denial of Service | recycle fired repeatedly | high | mitigate | The verb is never looped: there is one call site, in `_escalate_ipc_fault`, gated as described under T-22. | closed |
| T-164.6.5-08 | Information Disclosure | remote traceback reaching logs or Sentry | high | mitigate | The recycle and the snapshot both cross through `_guarded_read`, which re-raises as `Mt5ClientError(0, scrub_freeform_string(...))`. Pinned by `test_TERMINAL_RECYCLE_a_remote_traceback_is_scrubbed_typed_and_timed` and `test_SESSION_SNAPSHOT_a_transport_raise_is_scrubbed_and_typed`. The failure arms of the heal log only `exc_class=` and `code=` (`_describe_capture_failure`). Class-name diagnostics must match a Python-identifier regex (`_RECYCLE_CLASS_NAME_RE`). | closed |
| T-164.6.5-09 | Tampering | Wine prefix / volume reset (D-07) | critical | mitigate | The comment on `_REMOTE_TERMINAL_RECYCLE_SRC` states the one-way rule. `test_TERMINAL_RECYCLE_the_remote_source_can_only_end_a_process` forbids filesystem, launch, credential and MT5 tokens, and has a positive control that the source still calls `TerminateProcess`. The image path read for the file version is never returned. | closed |
| T-164.6.5-10 | Information Disclosure | spike paths / ids in tracked files | medium | mitigate | The same added-line sweep as T-01 found no path and no identifier. The recycle verdict carries ints, four-int versions and class names only. | closed |
| T-164.6.5-11 | Information Disclosure | real-wedge transcript committed as fixture | high | mitigate | No `.txt` fixture was added on the branch. The backstop scan (`no prober .txt transcript carries an account-shaped digit run or an absolute machine path`, with its CALIBRATION twin) is present and green. | closed |
| T-164.6.5-12 | Information Disclosure | new remedy / detail text in Actions log | medium | mitigate | The `REMEDIES["mt5-ipc-timeout"]` text and the `-10005` detail contain only causes, codes, durations and pointers to symbols and the runbook. `MT5_PROBE_PY` is unchanged on the branch. | closed |
| T-164.6.5-13 | Repudiation | gate reporting a verdict having measured nothing | high | mitigate | The D-10 set-equality gate (`ARM.requiredEnv and the Probe production step's supplied RAILWAY_ names are the SAME SET`) is present and green. An unmeasured run classifies as `mt5-ssh-transport`, whose remedy says nothing was measured. ⚠️ Part A of task 3 (bind a live verdict to a run id and head sha) is a pending founder act. Plan 03's SUMMARY records it as NOT executed, and no document claims it was done. That makes it a verification item, not a false claim. | closed |
| T-164.6.5-14 | Spoofing | secret rotation breaking other consumers | medium | mitigate | No rotation happened. The checkpoint in plan 03's SUMMARY carries the instruction to re-check other consumers before and after any re-scope. | closed |
| T-164.6.5-15 | Tampering | run-time assembly via the probe body | high | mitigate | `MT5_PROBE_PY` has no hunk in the branch diff. Its shipped byte-diff self-test runs in `prod-prober-wiring.test.ts` (green). | closed |
| T-164.6.5-16 | Information Disclosure | new arm log line and raised detail | high | mitigate | The `is_ipc_transport_fault` arm of `routers/exchange.py` logs `code=%s` only. Its `detail` is a static sentence with no submitted value (`test_mt5_ipc_transport_fault_maps_to_terminal_unresponsive`). | closed |
| T-164.6.5-17 | Repudiation | terminal fault misattributed to credential or server | high | mitigate | The arm sits inside the not-a-login-refusal branch, and its code gate (`_IPC_TRANSPORT_CODES`) runs before any text matching. The test asserts the detail is neither the auth copy nor the wrong-server copy. The D-12 fence test pins non-IPC errors to 424. | closed |
| T-164.6.5-18 | Repudiation | copy promising an unshipped automatic recovery | medium | mitigate | The `KEY_MT5_TERMINAL_UNRESPONSIVE` copy says "It can come back without anything from you" and "A later attempt can succeed". That is hedged, and true, because the plan 05 heal shipped. There is no "will" promise. The draft sentence is gated to the connect surface (`REQUIRES_CONNECT_SURFACE`). | closed |
| T-164.6.5-19 | Spoofing | failure path returning valid or persisting | critical | mitigate | The arm raises `service_error(500, …)` and never returns `{"valid": true}`, and nothing is persisted on any arm. On the TS rotate route, the test asserts `ADMIN_STATE.updates` has length 0 on this code. | closed |
| T-164.6.5-20 | Information Disclosure | credential-shaped literal as test subject | high | mitigate | The new-arm tests assert properties (code, status, flags, copy identity). Only synthetic short placeholders appear; none is credential-shaped. | closed |
| T-164.6.5-21 | Tampering | new wire code with no TS disposition | high | mitigate | `VENUE_WIRE_CODE_TO_VERDICT` has the row `MT5_TERMINAL_UNRESPONSIVE → KEY_MT5_TERMINAL_UNRESPONSIVE/500`. The dialog route set lists it. The invariant suites (`seam-venue-vocabulary`, `dialog-envelope`) are green. | closed |
| T-164.6.5-22 | Denial of Service | un-debounced escalation (unbounded kill loop) | critical | mitigate | There is a once-per-run gate: `ipc_fault_escalation_armed` peeks and `claim_ipc_fault_escalation` disarms. It re-arms only when the terminal is measured answering, never on `0`, `-10003` or `-10004` (WR-08). There is also a rolling cap, `IPC_FAULT_RECYCLE_CAP = 2` per `_IPC_FAULT_RECYCLE_WINDOW_S`, which `end_ipc_fault_run` never clears, and a capped reading recycles nothing. Pinned by `test_ESCALATION_five_consecutive_ipc_timeouts_produce_exactly_ONE_recycle`, `test_ESCALATION_WR08_…does_NOT_re_arm` and `test_R2_CR01_…is_CAPPED_and_raised_at_ERROR`. The recycle is also refused when the budget cannot finish it (`KIND_IPC_FAULT_RECYCLE_SKIPPED_BUDGET`). | closed |
| T-164.6.5-23 | Denial of Service | recycle while another caller holds the terminal | high | mitigate | `_escalate_ipc_fault` is called only from `_heal_blocking`, which runs inside the single `mt5_terminal_lease` in `heal_mt5_terminal_session`. The verb's first statement is `_assert_live("terminal_recycle")`, so a zombie thread is refused before anything crosses (`test_TERMINAL_RECYCLE_an_abandoned_session_is_refused_before_anything_crosses`). | closed |
| T-164.6.5-24 | Denial of Service | heal raise reaching the crash handler | critical | mitigate | `except Exception` arms in `_escalate_ipc_fault` let only `Mt5SessionAbandoned` through, and the entry keeps its one-handler outer guard. Pinned by `test_ESCALATION_a_raising_recycle_cannot_escape_the_heal`, `test_no_failure_mode_escapes_the_heal` and `test_the_never_raises_predicate_REDS_on_every_escape_route`. | closed |
| T-164.6.5-25 | Elevation of Privilege | escalation becoming general remote exec | high | mitigate | The escalation calls only `recycle_terminal_process`, `session_snapshot` and `assert_session_authorized`. None takes a credential, and both remote payloads are committed literals (T-05, T-06). `test_ESCALATION_no_credential_reaches_the_recycle` is green. | closed |
| T-164.6.5-26 | Tampering | recycle fired on a detached bridge | medium | mitigate | `_RECYCLE_REACHABLE_IPC_CODES = _IPC_TRANSPORT_CODES - {-10004}` is derived, not re-spelled. Pinned by `test_ESCALATION_never_fires_where_the_recycle_cannot_reach` and `test_the_escalation_gate_is_exactly_the_ipc_timeout_code`. | closed |
| T-164.6.5-27 | Repudiation | verdict meaning changed for prefix consumers | high | mitigate | The escalation result rides `HealOutcome.escalation_kind` and its own log line. `test_ESCALATION_the_verdict_string_did_not_move` is green. | closed |
| T-164.6.5-28 | Tampering | ticking the account-change algo-disable option (D-15) | critical | mitigate | `test_d15_no_analytics_service_path_writes_a_terminal_option` does a comment- and docstring-stripped token scan over more than 50 service files. `test_d15_the_write_scan_ignores_prose_and_catches_code` proves both halves of that scan. `ACCOUNT_CHANGE_ALGO_DISABLE_OPTION` is a named constant, not a setter. | closed |
| T-164.6.5-29 | Repudiation | false recurrence cause sentence | high | mitigate | `MT5_GATEWAY_TRADE_PERMISSION_OFF_DETAIL` was reworded and the recurrence claim removed, with a dated correction in `mt5_probe.py`, `job_worker.py` and the prober `-6` remedy. The curated-family fence is green. | closed |
| T-164.6.5-30 | Denial of Service | loud check false-alarming on a blip | high | mitigate | The loud check builds on the existing `terminal_trade_permission_off` predicate, with no second judge. `test_d15_the_loud_check_never_false_alarms_on_a_bridge_blip` is green. | closed |
| T-164.6.5-31 | Repudiation | severity inversion | medium | mitigate | `test_d15_lost_terminal_permission_logs_above_an_ordinary_verdict` asserts the level relative to the router's sibling arm. | closed |
| T-164.6.5-32 | Information Disclosure | cause constant carrying path / identifier | medium | mitigate | `test_every_builder_emittable_constant_is_curated_and_credential_free` and `test_the_curated_family_is_the_measured_three_and_they_are_distinct` are green. The reworded sentence names a UI label only. | closed |
| T-164.6.5-33 | Tampering | finalize dedupe on a client value | critical | mitigate | `finalize-wizard/route.ts` still selects `wizard_session_id` from the owner-scoped `strategies` row. The only phase hunk there is a dated comment saying never to key the dedupe on `X-Correlation-Id`. | closed |
| T-164.6.5-34 | Information Disclosure | identifier carrying user data | medium | mitigate | Both ids are `wizard:<uuid>` from `crypto.randomUUID()` (`mintWizardId`) and neither is derived from input. A caller-supplied empty id mints a fresh one (IN-08). | closed |
| T-164.6.5-35 | Spoofing | caller correlation value respected | low | accept | See the Accepted Risks Log. The rationale was re-measured: the id is used only for log correlation and the displayed support id, never for authz, ownership or dedupe (T-33). | closed |
| T-164.6.5-36 | Repudiation | support id naming the page load, not the failure | high | mitigate | Six step surfaces pass `onCorrelationId`. The enumeration table in commit `34a1750b` lists 8 changed and 5 left-alone sites, each with a reason. `WizardClient.tsx`'s one `wizardFetch` (draft DELETE) renders no envelope. | closed |
| T-164.6.5-37 | Repudiation | docblock asserting the reversed rule | medium | mitigate | The `wizard-correlation.ts` docblock now states that a caller-supplied `X-Correlation-Id` "is now RESPECTED". | closed |
| T-164.6.5-38 | Tampering | CI skip trailer in a commit message | critical | mitigate | ⚠️ Plan 08 has not executed. The 85 existing branch commits (merge base `9fd60115`..HEAD) were swept with the token regex and 0 matched. The release commit and the squash body do not exist yet. | **open** |
| T-164.6.5-39 | Information Disclosure | credential-shaped literal in CHANGELOG prose | high | mitigate | ⚠️ Plan 08 has not executed. There is no CHANGELOG entry yet, so nothing can be checked. | **open** |
| T-164.6.5-40 | Tampering | ledger handler clobber | high | mitigate | ⚠️ Plan 08 has not executed. No ledger edits have been made yet. | **open** |
| T-164.6.5-41 | Tampering | staging other phases' working-tree files | high | mitigate | ⚠️ Plan 08 has not executed. At HEAD, none of `.planning/milestone.lock`, `.planning/state.json` or the 164.5.3 VERIFICATION report has been touched by any branch commit, but the guard that matters runs at plan 08's commit. | **open** |
| T-164.6.5-42 | Repudiation | phase closed with a criterion restated as met | high | mitigate | ⚠️ Plan 08 has not executed, and ROADMAP/STATE are not yet closed. Plan 01 (criterion 1) and plan 03 (D-10/D-11) already record their open items honestly. | **open** |
| T-164.6.5-SC | Tampering | package installs | low | accept | See the Accepted Risks Log. Re-measured: no manifest or lockfile hunk on the branch. | closed |

*Status: open · closed. Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party).*

---

## Open threats (blocking — severity ≥ high)

All five belong to **plan 08** (release commit, CHANGELOG, ledger close), which has not been
executed. Their mitigations are instructions inside that plan's tasks, and the things they
protect (the release commit, the CHANGELOG entry, the ROADMAP/STATE close) do not exist yet.
**No shipped source file has an open threat.**

To close them: execute plan 08 as written, then re-run `/gsd-secure-phase 164.6.5`. The
re-audit must check:
- **T-38:** every message file for the release commit, and the squash body, sweep 0 for the
  CI skip-token regex. The run count is bound to the head sha, not eyeballed.
- **T-39:** the CHANGELOG entry quotes no fixture literal verbatim. A range-form gitleaks scan
  over the release commit is clean.
- **T-40:** a byte backup was taken before any gsd-tools handler ran, and `git diff` of
  ROADMAP/STATE shows no collateral edits.
- **T-41:** the three foreign working-tree files are absent from every plan-08 commit.
- **T-42:** the ROADMAP records MET or OPEN per criterion. Criterion 1 must stay OPEN, routed
  to `MT5-SWITCH-WEDGE-CAUSE-01`. D-10/D-11 stay OPEN unless the founder's Part A run is
  recorded with a run id and head sha.

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-164.6.5-01 | T-164.6.5-SC | The phase adds no npm, pip or cargo dependency, and `rpyc` / `mt5linux` pins are untouched. There is nothing for a package-legitimacy gate to audit. Re-measured: no manifest or lockfile in the branch diff. | plan threat models 01–08 (planner), re-verified by auditor | 2026-09-26 |
| AR-164.6.5-02 | T-164.6.5-35 | `X-Correlation-Id` was already client-controlled end to end, and it is used for log correlation and the displayed support id only. It is never used for authorization, ownership or dedupe (the finalize dedupe keys on the owner-scoped `strategies.wizard_session_id`). The server re-shapes the value through `CORRELATION_ID_SHAPE` before use. Respecting a caller's value changes no trust decision. | plan 07 threat model (planner), re-verified by auditor | 2026-09-26 |

---

## Unregistered Flags

None. Plan 02 and plan 05 SUMMARY `## Threat Flags` both read "None beyond the plan's
register". Plans 01, 03, 04, 06 and 07 have no such section.

Two surfaces were added in review rounds after the threat models were written. Both map to
existing threat IDs, so they are recorded here as informational rather than as unregistered
flags:
- **`_REMOTE_SESSION_SNAPSHOT_SRC` / `Mt5Client.session_snapshot`** (R2 WR-03 root cause). This
  is a second remote literal on the T-134-03 channel, and it is covered by T-05, T-06, T-08 and
  T-25. It is an AST constant with no brace, no dunder and no import. Its only `mt5` calls are
  `terminal_info`, `account_info` and `last_error`, and it takes no argument, so the environment's
  login and server never cross. `_parse_session_snapshot` reduces the result to
  `login_matches` / `server_matches` booleans, and
  `test_SESSION_SNAPSHOT_returns_verdicts_never_values` asserts that no account number or server
  name comes back. The pre-recycle and post-relaunch log lines carry
  `session_*_matches_env=<bool>` verdicts only.
- **Sentry paging of `KEY_MT5_TERMINAL_UNRESPONSIVE`** (WR-06/SFH-08, R2-SFH-10). This is
  covered by T-16. `OUR_DEFECT_KEY_ERROR_CODES` gates `captureToSentry` on all three key routes.
  The rotate-secret capture passes `secrets: perRequestSecrets` (the new secret), which
  `captureToSentry` scrubs from the payload, tags and extra (`scrubCaptureInput` / `scrubRecord`).
  The test asserts `options.secrets` contains the synthetic new secret. The `console.error`
  beside it also goes through `scrubSeamError(err, perRequestSecrets)`.
- **`wizard_page_load_id` Sentry tag** (WR-07, IN-03, R2-SFH-11). This is covered by T-34.
  `wizardPageLoadIdTag` returns `null` when the header is absent. A present header that fails
  `/^[A-Za-z0-9._:-]{1,128}$/`, or an array, returns the fixed sentinel `"<malformed>"`, and the
  client value is never echoed. The sentinel's angle brackets sit outside the allowlist, so no
  client value can read as it. `src/instrumentation.test.ts` covers the absent, empty, CR/LF and
  array cases.

⚠️ This one is observed, not in scope, and pre-existing: the neighbouring `correlation_id` tag
in `onRequestError` passes any string `x-correlation-id` header through unshaped. That predates
this phase (Phase 16 / OBSERV-04), and this phase did not widen it. The wizard now sends a
server-conformant `wizard:<uuid>`. It is recorded for visibility only and is not counted.

---

## Scope note: the phase goal vs. its threats

"Key validation stops destroying the shared session" is **mitigated, not root-caused**.
Validation now answers an honest, non-retryable `MT5_TERMINAL_UNRESPONSIVE`, so it no longer
invites the harmful retry. The heal recycles a wedged terminal under a debounce and a cap. No
code path writes a terminal option. The **cause** of the account-switch wedge is still
criterion 1, recorded OPEN and owned by Phase 164.6.6 (`MT5-SWITCH-WEDGE-CAUSE-01`). Every
recycle-path mitigation above has been exercised only against fakes. Live Win32 marshalling
under Wine and the live bridge snapshot are unmeasured (`.planning/WINDOWS.md` entry 68).

---

## Gate output (local, read-only, at audit sha `4c0c10feb`)

No live MT5, gateway, Railway or database command was run.

- `analytics-service/` pytest (`test_mt5_client_contract`, `test_mt5_relogin`,
  `test_mt5_session_episodes`, `test_mt5_validate`, `test_mt5_validate_parity`,
  `test_ingestion_mt5`, `test_validate_key_venue_transient`):
  `709 passed, 1 skipped, 1 warning`. The one skip is
  `tests/test_mt5_validate.py:685: got empty parameter set for (observation)`. It predates this
  phase: the phase diff has no hunk in that test.
- vitest (rotate-secret route, instrumentation, wizard-correlation, wizardErrors,
  prod-prober-wiring, dialog-envelope invariant, seam-venue-vocabulary invariant,
  create-with-key route, composite/add-key route): `Test Files  9 passed (9)` /
  `Tests  725 passed (725)`.
- Commit-message skip-token sweep over 85 branch commits: 0 matches.

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-09-26 | 43 | 38 | 5 (all plan 08, unexecuted) | gsd-security-auditor (read-only + local gates) |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [ ] `threats_open: 0` confirmed. **Not yet: 5, pending plan 08.**
- [ ] `status: verified` set in frontmatter. **Not yet.**

**Approval:** pending plan 08 execution and re-audit
