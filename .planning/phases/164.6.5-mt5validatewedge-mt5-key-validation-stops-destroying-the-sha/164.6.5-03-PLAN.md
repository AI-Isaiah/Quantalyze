---
phase: 164.6.5
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/prod-prober/arms/mt5.mjs
  - src/__tests__/prod-prober-wiring.test.ts
  - scripts/prod-prober/fixtures/mt5/
autonomous: false
requirements: ["164.6.5-C4"]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `-10005` remedy no longer asserts ONE cause. It names BOTH known causes and gives an ordered procedure that is correct under both, so an operator reading it during the second cause is not sent to a VNC console with no dialog in it (D-09)."
    - "The D-09 choice — broadened remedy text rather than a classifier split — is recorded in the arm's own header with the three measured reasons the split was refused, so the next reader does not re-litigate it as an oversight."
    - "A workflow edit that drops, renames or mis-scopes any of the four environment names the mt5 arm declares reds BY NAME in CI, instead of degrading every hourly run to a transport verdict that measured nothing (D-10)."
    - "The arm stays IMPORT-FREE: the mutation harness builds a standalone copy of it in a temp directory, and an added import breaks that idiom."
    - "The committed probe body stays non-interpolated: any field added goes into the source constant, never assembled at run time."
    - "D-11 is answered against a REAL wedge, or recorded explicitly open with a named routed residual and its trigger — never quietly satisfied by a fixture alone."
  artifacts:
    - path: "scripts/prod-prober/arms/mt5.mjs"
      provides: "a `-10005` remedy and branch detail that name both causes and assert neither, plus a header block recording the D-09 decision and why the classifier split was refused"
      contains: "-10005"
    - path: "src/__tests__/prod-prober-wiring.test.ts"
      provides: "the D-09 remedy-honesty gate and the D-10 arm-declares-to-workflow-passes agreement gate"
      contains: "WORKFLOW_PATH"
  key_links:
    - from: "scripts/prod-prober/arms/mt5.mjs :: ARM.requiredEnv"
      to: ".github/workflows/prod-prober.yml :: the mt5 run step's env block"
      via: "a set-equality gate in prod-prober-wiring.test.ts reading both sides, so a silent workflow edit cannot make every run measure nothing"
      pattern: "requiredEnv"
    - from: "scripts/prod-prober/arms/mt5.mjs :: REMEDIES['mt5-ipc-timeout']"
      to: "docs/runbooks/mt5-go-live.md :: the -10005 differential section"
      via: "the remedy is the short form of the runbook procedure; both name BOTH causes"
      pattern: "mt5-ipc-timeout"
---

<objective>
Criterion 4 — **the prod-prober's MT5 arm actually measures the terminal**, and the `-10005`
verdict stops telling an operator a story that is false for one of its two causes.

Purpose, and it is the vacuous-gate class this milestone exists to close: `mt5-ipc-timeout` has
**NEVER ONCE FIRED IN PRODUCTION**. The arm reported `mt5-ssh-transport`, whose own remedy text
concedes *"the terminal was never reached and nothing about it was measured"* — while `railway ssh`
into the same container worked fine with a WORKSPACE-scoped token. ⇒ The classification
`MT5-WEDGE-OBS-01` was closed on is UNPROVEN, and without fixing this the phase cannot honestly
prove criterion 1 held, because it would have no instrument that measures the subject (D-10).

⛔ And the shipped remedy is KNOWN-WRONG for the 2026-09-21 cause: it instructs clearing a MODAL
LOGIN DIALOG and asserts a redeploy will not help. Both were measured FALSE — the VNC console was
clean, the Alerts tab empty, and a restart cleared it in 2.0 s (D-09).

⛔ **Cross-link and REFINE `MT5-WEDGE-OBS-01`; do NOT reopen it.** ⚠️ Plan 01 owns `TODOS.md` and
`docs/runbooks/mt5-go-live.md` for this phase — this plan edits NEITHER and cites the runbook
section by symbol.

⛔ **`[MT5-VERDICT-SINK-01]` is NOT in scope.** It is ADJACENT — a CAPABILITY verdict — while this
plan is a LIVENESS instrument. It is a founder call; leave it deferred and named.

Output: an honest `-10005` remedy, a CI gate that stops the arm being silently de-configured, and
a live proof that the arm reaches the terminal.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-CONTEXT.md
@.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-RESEARCH.md
@.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-PATTERNS.md
@scripts/prod-prober/arms/mt5.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-09 — the `-10005` verdict stops asserting one cause, and the decision is recorded</name>
  <files>scripts/prod-prober/arms/mt5.mjs, src/__tests__/prod-prober-wiring.test.ts</files>
  <read_first>
    - `scripts/prod-prober/arms/mt5.mjs` in full. Note three shipped properties you must not break: the file has ZERO imports (the mutation harness builds a standalone copy of it in a temp directory and an added import breaks that idiom); `MT5_PROBE_PY` is a committed constant the self-test decodes from argv and diffs byte-for-byte; and `classifyProbe` is PURE, which is the whole reason fixtures can drive it.
    - The same file's header paragraph stating that `connected` and `trade_allowed` are printed and NEVER JUDGED, and the comment above `REMEDIES` stating the two IPC remedies say opposite things.
    - `scripts/prod-prober/run.mjs` — the self-test's remedy assertions (every kind's remedy is at least 40 chars, every key is a registered defect kind, no two remedies are the same string) and the set-equality assertion that every declared kind has a red fixture and every red fixture names a declared kind. These constrain what a new kind would cost.
    - `src/__tests__/prod-prober-wiring.test.ts` — the existing describe-block naming convention and the fixture-driven `probeResultFor()` helper.
  </read_first>
  <behavior>
    - The `-10005` remedy names both known causes and asserts neither as the cause of the reading in hand.
    - The `-10005` remedy no longer claims that a redeploy or a restart cannot help, because for one of its two causes that claim is measured false.
    - The `-10005` remedy remains distinguishable from the `-10004` remedy and from every other remedy in the table — the self-test's no-two-identical and per-kind properties still hold.
    - The `-10004` remedy is unchanged: its single cause is proven and its instruction is correct.
    - `classifyProbe` still returns exactly one kind for a `-10005` transcript, the existing fixture still drives it, and the arm still has zero imports.
  </behavior>
  <action>
Rewrite the `-10005` remedy string and the matching `classifyProbe` branch detail so that what the
operator reads is true under BOTH causes.

The new text must do four things. Name the two causes: the modal-login-dialog wedge, which is
PERSISTED state living with the Wine prefix on the named volume; and the account-switch wedge
measured 2026-09-21, which is PROCESS state, where the console was clean, the Alerts tab empty, and
the terminal's UI answered a human while its Journal wrote nothing. Say that this reading alone
cannot tell them apart. Give the ordered procedure that is correct under both — the process
recycle first, because it is cheap, unattended, measured at 2.0 s and cannot make the dialog cause
worse; then the VNC console for the dialog if the fault returns. And point at the runbook's
differential section by symbol for the long form. ⛔ Drop the assertion that a redeploy does not
fix this; it is true for one cause and false for the other, and asserting it is the defect.

Apply the same correction to branch (6)'s `detail` string: it currently names the modal-dialog
wedge as THE state and says the reading is not fixed by a redeploy. It must describe what was
actually READ — the bridge is attached and the terminal is not answering — and stop naming a cause
it did not measure. ⛔ A row that narrates a state it did not read is this arm's own named defect
class; the `-6` branch's comments already say so in this file's voice.

**Record the D-09 decision in the arm's header, with the three measured reasons the classifier
split was REFUSED**, so the next reader does not read a missing kind as an oversight:
  1. Both causes produce the IDENTICAL `initialize()` failure code, and the probe is READ-ONLY by
     hard constraint — a `login()` call is itself an account change, which would re-trip the very
     landmine this phase pins, so the probe cannot be widened to disambiguate.
  2. The alternative, correlating against external state such as a recent validate from the
     session monitor's own episode log, would require this arm to read something. The arm is
     pinned IMPORT-FREE by the mutation harness idiom, so that correlation cannot live here.
  3. The runner's own self-test requires every declared kind to have a RED FIXTURE that fires it.
     With no mechanical discriminator, no honest fixture exists, and a fabricated one would be a
     vacuous gate — the exact class this milestone closes.
Then state what WOULD reopen the split: a read-only signal, in the committed probe body, that
differs between the two causes.

⛔ Do NOT add an import. ⛔ Do NOT assemble any part of the probe body at run time; if a field is
ever needed it goes into the committed constant. ⛔ Do NOT start judging `connected` or
`trade_allowed` in the informational branch — that is deliberately unjudged and plan 06 owns the
judged version in its correct host.

**The gate**, added to `src/__tests__/prod-prober-wiring.test.ts` in that file's existing style:
assert the `-10005` remedy carries a marker for EACH of the two causes and carries the ordered
procedure, and assert it does not assert a single cause. Express the second half as a property of
the remedy's own content rather than as a bare absence — a gate that only checks a word is gone
goes green the moment someone rewords it.

⛔ **PROVE THE NEW GATE CAN FAIL.** `cp` a byte backup of the arm, revert the remedy to a
single-cause form, run the gate, OBSERVE IT RED, restore from the backup and verify with `cmp`.
⛔ Never `git checkout -- <path>`, never `git restore`, never `git stash`. ⚠️ Re-take the byte
backup after every edit. Record the RED observation in the SUMMARY.

⛔ PUBLIC REPO: the arm's strings reach a PUBLIC Actions log. No account number, no broker server
name, no machine path, no credential — this is the surface that already held a production
filesystem path once.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test</automated>
    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
  </verify>
  <done>The `-10005` remedy and branch detail name both causes, assert neither, and carry the ordered procedure; the `-10004` remedy is byte-unchanged; the arm still has zero imports and the committed probe body is unchanged; the D-09 decision and its three refusal reasons are recorded in the arm's header; the new gate was observed RED under a reverted remedy and restored via a re-taken byte backup verified with `cmp`. Both verify commands pass.</done>
</task>

<task type="auto">
  <name>Task 2: D-10 — the arm's declared environment and the workflow's supplied environment must agree, by gate</name>
  <files>src/__tests__/prod-prober-wiring.test.ts</files>
  <read_first>
    - `src/__tests__/prod-prober-wiring.test.ts` — `WORKFLOW_PATH` already exists in this file and already resolves the prober workflow, so reading it is an established idiom here, not a new one. Read how the existing workflow-reading assertions are written and match them.
    - `scripts/prod-prober/arms/mt5.mjs` — `ARM.requiredEnv` and the comment above it stating that the token is the WORKSPACE/account slot and that the CLI's other, shorter credential variable is the PROJECT slot, which `railway ssh` refuses.
    - `.github/workflows/prod-prober.yml` — the step that runs the prober and the env block it passes; also the configuration pre-flight that names the same variables. ⚠️ Read it; do not assume the names from this plan.
    - `scripts/prod-prober/run.mjs` — how a credential-blocked arm is counted, so the gate's failure message can name the consequence accurately.
  </read_first>
  <action>
Close the self-inflicted half of D-10 with a gate, because the measured failure was an
instrument that reported a verdict having measured nothing and nothing in CI noticed for weeks.

Add ONE assertion to `src/__tests__/prod-prober-wiring.test.ts`: the set of environment names the
mt5 arm DECLARES it requires, and the set of environment names the prober workflow SUPPLIES to the
step that runs it, must agree member for member. Read both sides from their real sources — the
arm's exported `requiredEnv`, and the workflow file already resolved by `WORKFLOW_PATH` — never
from a list typed into the test. A list typed into the test is a third source that drifts from
both.

Write the failure message to name the CONSEQUENCE, not just the mismatch: a dropped or renamed
name does not fail loudly at run time — it makes the arm credential-blocked or makes every hourly
run report a transport verdict that measured nothing about the terminal, which is exactly the
state this phase found and exactly the state that let an unproven classification be treated as
proven. Name the moved member in the message so the failure is actionable.

⚠️ Assert as a SET, sorted, so the message names what moved — not as a count. A count-based gate
goes green on a simultaneous add and drop, and this repo has a dated record of that class.

⛔ The gate is about NAMES, never VALUES. It must not read, log or echo any secret, and it must not
assert anything about a token's scope — scope is not observable from the repo and claiming it here
would be the fabrication this milestone exists to stop. The scope is task 3's subject.

⛔ **PROVE IT CAN FAIL.** Take a byte backup of the workflow file, remove one supplied name, run
the gate, OBSERVE IT RED and confirm the message NAMES the removed member, then restore from the
backup and verify with `cmp`. ⛔ Never `git checkout -- <path>`, never `git restore`, never
`git stash`. ⚠️ Re-take the backup after every edit. Record the RED observation in the SUMMARY.
⚠️ Restore the workflow before committing: this task's `files_modified` is the test file only.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
  </verify>
  <done>A set-equality gate ties the mt5 arm's declared environment names to the names the prober workflow supplies, reading both from their real sources; its failure message names the moved member and the consequence; it was observed RED with one name removed and the workflow was restored byte-identically (`cmp`). `.github/workflows/prod-prober.yml` is unchanged in the commit.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: FOUNDER — prove the arm reaches the terminal, and calibrate against a REAL wedge</name>
  <files>scripts/prod-prober/fixtures/mt5/</files>
  <precondition>The founder can dispatch GitHub Actions workflows on this repository and can read the prod-prober run log.</precondition>
  <action>Prove the mt5 arm produces a verdict that MEASURED the terminal against production, and capture a real-wedge transcript to calibrate the arm against.</action>
  <instructions>
⛔ FOUNDER ACT. No agent dispatches this workflow, touches the live gateway, or reads or rotates any
credential. ⛔ No agent enters, reads, decrypts, logs or echoes an MT5 credential.

**PART A — prove the arm measures the terminal (D-10).**

The prober workflow accepts a manual dispatch that can target a single arm. Dispatch it for the
mt5 arm and read the verdict line from the run log.

  · If the verdict MEASURES THE TERMINAL — the informational line reporting a successful
    initialize, or any of the terminal-attributed kinds — **D-10 is answered.** Record the run id,
    the head sha and the verdict line verbatim.
  · If the verdict is the TRANSPORT kind — the one whose own remedy says the terminal was never
    reached and nothing about it was measured — then the arm still measures nothing. Follow that
    remedy's own ordered checklist: the token's SLOT first (it must be the workspace/account slot;
    the CLI's project-slot token is refused), then the project / environment / service variables,
    then the gateway container's own state. Re-dispatch after each change until a
    terminal-measuring verdict appears.
    ⚠️ Rotating or re-scoping the Actions secret is a founder act and it is the ONE change in this
    phase that can break other workflows. Re-check anything else that consumes the same secret
    before and after.

⛔ **BIND THE PROOF TO A SHA AND COUNT THE CHECKS.** A green board is not a green run: read the run
by its head sha and read the verdict line itself. An empty result set passes a naive zero-check.

**PART B — calibrate against a REAL wedge (D-11).**

A fixture proves the classifier can emit a string. Only a real wedge proves the arm reaches the
terminal in the state that matters. Capture the transcript in whichever of these windows arrives
first:

  · **Induced, founder-supervised.** Plan 02's spike deliberately recycles the terminal process.
    While the terminal is down and before it re-authorises, dispatch the mt5 arm and capture the
    `railway ssh` transcript. ⚠️ Record what the arm ACTUALLY reported; do not assume which code a
    recycled terminal produces — establishing that is the measurement.
  · **Opportunistic.** On the next natural wedge, capture the transcript before recovering.

**HOW TO REPORT AND WHAT GETS COMMITTED.** Paste the transcript into this session. An agent then
commits it as a new fixture file under the mt5 fixtures directory, shaped exactly like the existing
fixtures there — a plain text file carrying the probe line plus any CLI banner the scanning logic
already tolerates — and registers it in the runner's fixture table for the kind it actually
produced. ⚠️ If it produces a kind that already has a fixture, that is fine: the runner's own
comment records that one kind may legitimately need two red fixtures for two different causes.

⛔ **PUBLIC REPO, AND THIS IS THE SURFACE THAT ALREADY HELD A PRODUCTION ARTIFACT.** A fixture is
copied out of a live production container. Before pasting: remove any account-shaped digit run, any
absolute machine path, any broker server name, any credential. The wiring test already scans every
committed transcript for the first two and will red — treat that as the backstop, not the control.

⚠️ **IF NEITHER WINDOW ARRIVES INSIDE THE PHASE** — say so plainly. Record D-11 as EXPLICITLY OPEN
with a named routed residual carrying an owner and a trigger, per the founder's standing rule that
a deferral names its phase. ⛔ Do NOT close D-11 on a hand-written fixture and call it calibrated;
that is the vacuous gate this phase exists to remove.
  </instructions>
  <verification>
`node scripts/prod-prober/run.mjs --self-test` exits 0 and `npx vitest run src/__tests__/prod-prober-wiring.test.ts` passes after any fixture is registered — the latter also scans every committed transcript for account-shaped digit runs and absolute machine paths, which is the redaction backstop. Part A's proof is the verdict line read from the run identified by its run id and head sha; ⛔ count the checks against that sha rather than reading the board as clean.
  </verification>
  <resume-signal>Reply with the Part A verdict line plus run id and head sha, and either the Part B transcript or `D-11 OPEN` with the reason.</resume-signal>
  <done>The mt5 arm has produced a verdict that MEASURED THE TERMINAL against production, bound to a run id and head sha; and a real-wedge transcript is committed as a registered fixture, or D-11 is recorded explicitly open with a named routed residual, owner and trigger. Both automated commands still pass after any fixture registration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live gateway container → a committed `.txt` fixture → a PUBLIC repository | fixtures are copied out of production by nature; one already carried a production filesystem path |
| prober arm strings → a PUBLIC GitHub Actions log | every remedy and detail string is printed into a world-readable log |
| GitHub Actions secret store → `railway ssh` | the workspace-scoped token; its scope is the D-10 subject |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.5-11 | Information Disclosure | a real-wedge transcript committed as a fixture | high | mitigate | Task 3 instructs redaction before pasting; the wiring test's existing transcript scan for account-shaped digit runs and absolute machine paths is the backstop and runs in the task's verify |
| T-164.6.5-12 | Information Disclosure | new remedy / detail text reaching a public Actions log | medium | mitigate | Task 1 forbids identifiers in the strings; the text carries only causes, codes, durations and a runbook pointer |
| T-164.6.5-13 | Repudiation | a gate that reports a verdict having measured nothing | high | mitigate | Task 2's arm-to-workflow set-equality gate closes the self-inflicted de-configuration path; task 3's Part A binds the proof to a run id and head sha |
| T-164.6.5-14 | Spoofing | a secret rotation that silently breaks other consumers | medium | mitigate | Task 3 instructs re-checking every other consumer of the same secret before and after any re-scope |
| T-164.6.5-15 | Tampering | run-time assembly reaching the container through the probe body | high | mitigate | The committed non-interpolated probe body is unchanged by this plan and its shipped byte-diff self-test still runs |
| T-164.6.5-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; RESEARCH.md records the phase introduces no new external dependency |
</threat_model>

<verification>
- `node scripts/prod-prober/run.mjs --self-test` exits 0.
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts` passes.
- The arm still has zero `import`/`require` lines (the mutation harness depends on it).
- `.github/workflows/prod-prober.yml` is byte-unchanged by tasks 1 and 2.
- ⛔ NEVER write a CI skip token into any commit message, not even to deny it — the token fires from inside a sentence denying it and rides a squash into the default branch. Sweep every message file before committing.
</verification>

<success_criteria>
- The `-10005` verdict names both causes and asserts neither; the decision and its refusal reasons are recorded in the arm.
- A CI gate ties the arm's declared environment to the workflow's supplied environment, reading both from source.
- The arm has produced a terminal-measuring verdict against production, bound to a sha.
- D-11 is calibrated against a real wedge, or recorded explicitly open with a routed residual.
</success_criteria>

<output>
Create `.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-03-SUMMARY.md` when done.
</output>
