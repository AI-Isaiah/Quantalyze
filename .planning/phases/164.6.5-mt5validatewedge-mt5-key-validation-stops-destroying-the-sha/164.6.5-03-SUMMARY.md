---
phase: 164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha
plan: 03
subsystem: infra
tags: [prod-prober, mt5, vitest, observability, ci-gates]

# Dependency graph
requires:
  - phase: 164.1
    provides: the mt5 prod-prober arm (classifyProbe, REMEDIES) and its wiring
      test harness (prod-prober-wiring.test.ts, self-test idiom)
provides:
  - "The `-10005` remedy names both known causes (persisted modal-login-dialog;
    process-state account-switch measured 2026-09-21), asserts neither, and
    gives the ordered procedure correct under both (recycle, then VNC)."
  - "The D-09 decision record (broadened remedy, classifier split refused, three
    measured reasons) recorded in the arm's own header."
  - "A CI gate (D-10) tying the mt5 arm's declared requiredEnv to the workflow's
    supplied env names by set equality, reading both from their real sources."
affects: [164.6.5-01 (runbook -10005 differential section, TODOS.md refinement), 164.6.5-06 (judged trade_allowed host), 164.6.5-08 (release/CHANGELOG)]

# Actuals (#2632) — pairs with the plan's estimate to calibrate future estimates.
actuals:
  tokens: 4006     # chars/4 over the realized diff (16022 chars / 4) for the two files touched by tasks 1-2
  tasks: 2          # of 3 — task 3 is a founder checkpoint, not executed by this agent
  commits: 2        # MEASURED: git rev-list --count 3925a55e..HEAD
  plan_head_before: 3925a55e534137bc8b67dddd6323d5859ecfe9d3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Set-equality env-wiring gate reading both sides from source (arm export vs. workflow text slice), never a typed third list"
    - "Positive ambiguity-property assertion (presence of a disclaiming phrase) instead of a bare-absence check on a reworded sentence"

key-files:
  created: []
  modified:
    - scripts/prod-prober/arms/mt5.mjs
    - src/__tests__/prod-prober-wiring.test.ts

key-decisions:
  - "D-09 implemented as plan directed: broadened remedy text, NOT a classifier split — the three refusal reasons (identical initialize() code + read-only probe constraint; import-free arm pinned by the mutation harness; no honest fixture possible for a mechanically indistinguishable second kind) are recorded in the arm's header, beside REMEDIES."
  - "D-10's gate is scoped to the RAILWAY_-prefixed subset of the `Probe production` step's env block, because that step aggregates env for all four prober arms and only mt5 consumes a RAILWAY_ var — this lets the gate compare a real 4-member set to a real 4-member set without typing an allowlist that could itself drift."
  - "-10004's remedy (mt5-no-ipc) is untouched; only mt5-ipc-timeout's remedy and detail changed."

# ⛔ CORRECTED 2026-09-26 (plan 164.6.5-08): this said requirements-completed: ["164.6.5-C4"].
# C4 is NOT met. Its measuring half (D-10) is answered by production runs; its calibration half
# (D-11) is OPEN and routed. The earlier value claimed a criterion this plan did not close.
requirements-completed: []
requirements-open: ["164.6.5-C4"]
open_residuals:
  - "D-11 (real-wedge calibration) -> TODOS.md MT5-PROBER-WEDGE-CALIBRATION-01, owner Phase 164.6.6, trigger: the next live -10005 captured before the heal recycles it"

coverage:
  - id: D1
    description: "The -10005 remedy names both known causes, asserts neither, and gives the ordered procedure (recycle first, then VNC) correct under both."
    requirement: "164.6.5-C4"
    verification:
      - kind: unit
        ref: "src/__tests__/prod-prober-wiring.test.ts#[164.6.5-03] D-09: the -10005 remedy names both causes and asserts neither"
        status: pass
      - kind: unit
        ref: "scripts/prod-prober/run.mjs --self-test (83/83)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The D-09 decision (broadened remedy, classifier split refused) is recorded in the arm's header with the three measured refusal reasons."
    verification: []
    human_judgment: true
    rationale: "A doc-comment's presence and content quality is not asserted by an automated test in this plan; a human should read scripts/prod-prober/arms/mt5.mjs's D-09 decision-record comment (immediately above REMEDIES) to confirm it reads as intended."
  - id: D3
    description: "A CI gate ties the mt5 arm's declared requiredEnv to the prod-prober workflow's supplied env names, reading both from source, naming the moved member on failure."
    requirement: "164.6.5-C4"
    verification:
      - kind: unit
        ref: "src/__tests__/prod-prober-wiring.test.ts#[164.6.5-03] D-10: the mt5 arm's declared environment and the workflow's supplied environment agree"
        status: pass
    human_judgment: false
  - id: D4
    description: "D-10: the arm has produced a terminal-measuring verdict against production, bound to a run id and head sha."
    requirement: "164.6.5-C4"
    verification:
      - kind: live-read
        ref: "prod-prober scheduled run 36134914962 (2026-09-25T12:26Z, head 01dcf1cc): first terminal-measuring mt5 line; still current at run 36212376262 (2026-09-26T02:39Z, head ecf1ef4e). Read-only via gh run view --log on 2026-09-26."
        status: pass
    human_judgment: false
  - id: D5
    description: "D-11: the arm is calibrated against a REAL wedge transcript, not only a fixture."
    requirement: "164.6.5-C4"
    verification: []
    human_judgment: true
    status: open
    rationale: "No real wedge occurred inside the phase and none was induced. scripts/prod-prober/fixtures/mt5/ is unchanged since Phase 164.8.3 and mt5-ipc-timeout has never fired in production. Recorded OPEN and routed per task 3's own step 6: TODOS.md MT5-PROBER-WEDGE-CALIBRATION-01, owner Phase 164.6.6."

# Metrics
duration: ~unknown (PLAN_START_TIME was not separately captured at dispatch; commits 3cb4f062 and 93a2cd78 landed 2026-09-22T02:52:50+02:00 and 02:54:12+02:00 respectively)
completed: 2026-09-22
# ⛔ CORRECTED 2026-09-26 (plan 164.6.5-08): this read `status: halted`. Task 3 is now resolved
# by its own contract: Part A (D-10) answered by production evidence, Part B (D-11) recorded
# OPEN with a routed residual (task 3 step 6). The plan is complete; criterion C4 is NOT met.
status: complete
---

# Phase 164.6.5 Plan 03: Prod-prober MT5 arm — honest -10005 remedy, env-wiring gate; live proof deferred to founder Summary

**The `-10005` verdict stops asserting one cause: it now names the persisted modal-dialog wedge AND the 2026-09-21 process-state wedge, asserts neither, and gives the ordered recycle-then-VNC procedure — backed by a new set-equality CI gate tying the arm's declared env to what the workflow actually supplies. Task 3 (live proof + real-wedge calibration) is a founder-only checkpoint and was not executed.**

## Performance

- **Tasks completed:** 2 of 3 (task 3 is `checkpoint:human-action`, `gate="blocking-human"`)
- **Files modified:** 2 (`scripts/prod-prober/arms/mt5.mjs`, `src/__tests__/prod-prober-wiring.test.ts`)
- **Commits:** 2 (measured via `git rev-list --count`)

## Accomplishments

- **D-09 (task 1).** `REMEDIES["mt5-ipc-timeout"]` and `classifyProbe`'s `-10005` branch `detail` no longer assert a single cause. The remedy now names the persisted modal-login-dialog cause and the process-state account-switch cause (measured 2026-09-21), states plainly that the reading alone cannot distinguish them, and gives an ordered procedure — recycle the terminal process first (cheap, unattended, 2.0 s, cannot make the dialog cause worse), then the VNC console if `-10005` returns — pointing at the runbook's `-10005` differential-diagnosis section by symbol. The `-10004` remedy (`mt5-no-ipc`) is untouched. A D-09 decision-record comment above `REMEDIES` documents why a classifier split was refused, with the three measured reasons from the plan (identical failure code + read-only-probe constraint; import-free-arm constraint; no honest fixture possible for a mechanically indistinguishable second kind).
- **D-10 (task 2).** A new set-equality gate in `prod-prober-wiring.test.ts` reads `ARM.requiredEnv` from `mt5.mjs` and the RAILWAY_-prefixed env names the `Probe production` step in `.github/workflows/prod-prober.yml` actually supplies (scoped to that step specifically, distinct from the credential-assert pre-flight step which shares the same var names), and asserts they agree member for member. The failure message names the missing/extra members and the consequence (credential-blocked arm, or every hourly run reporting `mt5-ssh-transport` — a transport verdict that measures nothing about the terminal). `.github/workflows/prod-prober.yml` is byte-unchanged.
- **Both new gates were proven able to fail**, on-disk, with byte-backup + `cmp` restoration (see "Gate RED Observations" below), in addition to in-file `CALIBRATION` tests that reproduce the same proof durably for CI.
- `node scripts/prod-prober/run.mjs --self-test` (83/83) and `npx vitest run src/__tests__/prod-prober-wiring.test.ts` (111/111) both pass after every change; the arm retains zero `import`/`require` lines and the committed probe body (`MT5_PROBE_PY`) is unchanged.

## Task Commits

Each task was committed atomically:

1. **Task 1: D-09 — the `-10005` verdict stops asserting one cause, and the decision is recorded** - `3cb4f062` (feat)
2. **Task 2: D-10 — the arm's declared environment and the workflow's supplied environment must agree, by gate** - `93a2cd78` (test)

**Task 3: FOUNDER — prove the arm reaches the terminal, and calibrate against a REAL wedge** — NOT STARTED. `checkpoint:human-action`, `gate="blocking-human"`. See "Checkpoint — Founder Action Required" below.

## Files Created/Modified

- `scripts/prod-prober/arms/mt5.mjs` — D-09 decision record; broadened `mt5-ipc-timeout` remedy and `-10005` branch detail; `-10004` and all other remedies unchanged; zero imports preserved.
- `src/__tests__/prod-prober-wiring.test.ts` — new `[164.6.5-03] D-09` describe block (both-cause markers, ordered-procedure check, positive ambiguity-property assertion, pre-D-09-revert calibration) and new `[164.6.5-03] D-10` describe block (env set-equality gate + in-memory calibration); `ARM` added to the existing `mt5.mjs` import.

## Decisions Made

- **D-09 implemented as the plan directed: broadened remedy, not a classifier split.** The three refusal reasons are recorded in the arm's own header (see `scripts/prod-prober/arms/mt5.mjs`, the comment block immediately above `REMEDIES`) rather than argued only here, so a future reader finds the reasoning beside the code it governs.
- **D-10's gate scope.** The `Probe production` step aggregates env for all four prober arms in one `env:` block, so a naive full-block set-equality against `ARM.requiredEnv` (mt5's four names) would never hold — the block also carries `ANALYTICS_*`/`SUPABASE_*` names for the other three arms. Scoping the comparison to the `RAILWAY_`-prefixed subset works because mt5 is the only arm in this workflow that consumes a `RAILWAY_`-prefixed variable; this reads a real 4-member set on each side without typing a third, driftable list.
- **The manual on-disk RED proof used the `Probe production` step's own occurrence**, not the credential-assert pre-flight step's, even though both steps repeat the same four `RAILWAY_*: value` lines — the pre-flight step is a different, non-run-feeding source and mutating it would not exercise the actual defect D-10 closes (a name dropped from the step that RUNS the arm).

## Deviations from Plan

None — plan executed exactly as written for tasks 1 and 2. Task 3 was correctly NOT attempted, per its `gate="blocking-human"` designation and the orchestrator's explicit instruction to stop there.

## Gate RED Observations (task-required manual proof)

**Task 1 (D-09 gate).** `scripts/prod-prober/arms/mt5.mjs` was byte-backed-up (`cp`), the `mt5-ipc-timeout` remedy was reverted in-place to its pre-D-09 single-cause string, and `npx vitest run src/__tests__/prod-prober-wiring.test.ts -t "164.6.5-03"` was run:

```
× names both causes and the ordered procedure (recycle before VNC)
× ⛔ EXPRESSED AS A PROPERTY OF THE REMEDY'S OWN CONTENT: ...
× points at the runbook's -10005 differential section BY SYMBOL, ...
× CALIBRATION: a single-cause revert (the pre-D-09 shape) FAILS the ambiguity-property assertion
```

4 of the 4 D-09 tests went RED, each naming the missing marker or property explicitly (see full assertion text above). The file was then restored from the backup and verified: `cmp` printed nothing (`RESTORE-CMP-OK`). Full green re-confirmed afterward: 111/111 vitest, 83/83 self-test.

**Task 2 (D-10 gate).** `.github/workflows/prod-prober.yml` was byte-backed-up, `RAILWAY_MT5_SERVICE` was deleted from the `Probe production` step's own `env:` block (line 260, distinct from the identically-named line in the credential-assert pre-flight step), and `npx vitest run src/__tests__/prod-prober-wiring.test.ts -t "D-10"` was run:

```
× ARM.requiredEnv and the Probe production step's supplied RAILWAY_ names are the SAME SET
  AssertionError: ... missing from the workflow: [RAILWAY_MT5_SERVICE], supplied but not declared by the arm: []
```

The live gate went RED and its message named the removed member, `RAILWAY_MT5_SERVICE`, exactly. (The in-memory `CALIBRATION` test in the same run also failed as a compounding effect of stacking two live mutations at once — expected, not a defect.) The workflow file was then restored from the backup and verified: `cmp` printed nothing (`RESTORE-CMP-OK`), and `git status --short` confirmed zero diff on `.github/workflows/prod-prober.yml`.

## Issues Encountered

None that blocked tasks 1-2. One implementation nuance worth recording: an early draft of the `mt5-ipc-timeout` remedy described the process-state cause using the phrase "the VNC console was clean," which put a second occurrence of the word "VNC" *before* the word "recycle" in the string — failing the new ordered-procedure assertion (recycle must be named before VNC). Reworded to "the terminal's own console was clean" for the symptom description, reserving "VNC" for the actual remedy instruction. This is a normal within-task implementation fix, not a deviation from plan scope.

## User Setup Required

None — no external service configuration required by tasks 1-2.

## Checkpoint — Founder Action Required (Task 3)

**Type:** human-action
**Gate:** blocking-human — never auto-approved, surfaces in every mode including auto/chain runs.

### What was automated (tasks 1-2, done)

- The `-10005` remedy operators will read is now honest under both known causes.
- A CI gate now fails loudly, by name, if the mt5 arm's required credentials and the workflow's supplied credentials ever diverge again (the exact self-inflicted failure mode D-10 found).

### What the founder must do (Part A — prove the arm measures the terminal, D-10)

1. Dispatch the `prod-prober` GitHub Actions workflow with `mode=probe`, `arm=mt5` (manual `workflow_dispatch`, narrowed to the mt5 arm).
2. Read the verdict line from the run log, bound to that run's id and head sha (count the checks against the sha — a green board is not a green run).
   - **If the verdict measures the terminal** (an informational success line, or any terminal-attributed kind): D-10 is answered. Record the run id, head sha, and verdict line verbatim.
   - **If the verdict is `mt5-ssh-transport`** (the transport kind whose own remedy says nothing about the terminal was measured): the arm still measures nothing. Follow that remedy's own ordered checklist — the `RAILWAY_API_TOKEN`'s slot first (must be the WORKSPACE/account slot, never the CLI's project-slot token), then the project/environment/service variables, then the gateway container's own state — re-dispatching after each change until a terminal-measuring verdict appears.
   - ⚠️ Rotating or re-scoping the Actions secret is a founder act with blast radius: re-check every other workflow that consumes the same secret before and after.

### What the founder must do (Part B — calibrate against a real wedge, D-11)

3. Capture a real-wedge transcript in whichever window arrives first:
   - **Induced, founder-supervised:** during plan 02's spike (which deliberately recycles the terminal process), dispatch the mt5 arm while the terminal is down and capture the `railway ssh` transcript — record what the arm actually reported, do not assume the code.
   - **Opportunistic:** on the next natural wedge, capture the transcript before recovering.
4. **Before pasting the transcript anywhere:** strip any account-shaped digit run, absolute machine path, broker server name, or credential. The wiring test's transcript scan is the backstop, not the control.
5. Paste the transcript into the session; an agent will commit it as a new fixture under `scripts/prod-prober/fixtures/mt5/`, shaped like the existing fixtures, and register it in the runner's fixture table for the kind it actually produced.
6. **If neither window arrives inside this phase**, say so plainly — D-11 is then recorded explicitly OPEN with a named routed residual (owner + trigger), never quietly closed on a hand-written fixture.

### Verification (after either path)

`node scripts/prod-prober/run.mjs --self-test` must exit 0, and `npx vitest run src/__tests__/prod-prober-wiring.test.ts` must pass after any fixture registration (its transcript scan also re-checks every committed `.txt` fixture for account-shaped digit runs and absolute machine paths).

### Resume signal

Reply with the Part A verdict line plus run id and head sha, and either the Part B transcript or `D-11 OPEN` with the reason.

## Next Phase Readiness

- Tasks 1-2 are complete, committed, and green (`git log --oneline`: `93a2cd78`, `3cb4f062` on top of `3925a55e`).
- Plan 03 **cannot be marked complete** until task 3 resolves. This is the intended stop — `status: halted` above, not `complete`.
- Once the founder completes Part A/B (or explicitly records D-11 OPEN), a continuation agent should: (a) commit any real-wedge fixture per the checkpoint instructions, (b) re-run both verification commands, (c) write the final plan-level `<verification>`/`<success_criteria>` confirmation into this SUMMARY (or a follow-on note), and (d) flip `status` to `complete`.

---
*Phase: 164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha*
*Plan: 03*
*Completed (tasks 1-2 only): 2026-09-22*

## Self-Check: PASSED

- FOUND: `scripts/prod-prober/arms/mt5.mjs`
- FOUND: `src/__tests__/prod-prober-wiring.test.ts`
- FOUND: commit `3cb4f062` (`git log --oneline --all`)
- FOUND: commit `93a2cd78` (`git log --oneline --all`)
- `npx tsx scripts/check-planning-hygiene.ts` — OK, 6834 tracked files scanned, no local username or absolute home path.

## Task 3 resolution — recorded 2026-09-26 by plan 164.6.5-08 (D-10 answered, D-11 OPEN)

⛔ **This section supersedes "Task 3 ... NOT STARTED" above, which is kept as lineage.** The
phase verifier found that D-10 had quietly become true in production and that the repo still
said the opposite. Plan 08 re-read the evidence itself, read-only, before writing this.

**Part A — D-10: ANSWERED by production evidence, without a founder dispatch.** The scheduled
`prod-prober` runs were read with `gh run view <id> --log`, filtered to the mt5 verdict line
only (no account or server value was copied):

| run | created (UTC) | head | mt5 verdict line |
|---|---|---|---|
| 36104820751 | 2026-09-25T06:52Z | `bb31564f` | `mt5-ssh-transport` (the transport kind: nothing about the terminal was measured) |
| **36134914962** | 2026-09-25T12:26Z | `01dcf1cc` | `mt5: initialize=true last_error=1 connected=true trade_allowed=true` — **the first terminal-measuring line** |
| 36212376262 | 2026-09-26T02:39Z | `ecf1ef4e` | `mt5: initialize=true last_error=1 connected=true trade_allowed=true` — the most recent completed run at the time of reading |

The verifier also read runs 36168373432 and 36202021941 with the same line. The arm has
measured the terminal on every scheduled run since 2026-09-25T12:26Z. What changed between
06:52Z and 12:26Z is not identified here: this plan did not change the workflow or its
secrets, and it makes no claim about which founder act fixed the transport.

⚠️ **The D-10 gate (task 2) and the D-10 live proof are different things.** The gate ties the
arm's declared environment to the workflow's supplied names. The proof is the line above. The
gate would not have caught the transport failure that preceded run 36134914962, because the
names already agreed: the gate passes against a workflow file plan 03 left byte-unchanged.

**Part B — D-11: OPEN, routed, never closed on a hand-written fixture.** No real wedge arrived
inside the phase and none was induced. `scripts/prod-prober/fixtures/mt5/` is unchanged since
Phase 164.8.3, and `mt5-ipc-timeout` has never fired in production. Per task 3 step 6 it is
booked as **`TODOS.md` `MT5-PROBER-WEDGE-CALIBRATION-01`**:

- **Owner:** Phase 164.6.6 (MT5TERMINALISOLATION), which already owns `MT5-SWITCH-WEDGE-CAUSE-01`
  and the next-wedge evidence capture it depends on.
- **Trigger:** the next live `-10005`, captured before the heal recycles the terminal.
- **Gate:** a scrubbed real-wedge transcript committed under `scripts/prod-prober/fixtures/mt5/`
  and registered for the kind it actually produced, with the self-test and the wiring suite green.

⚠️ **The heal this phase ships makes the trigger harder to meet.** Plan 05 recycles the terminal
on `ipc_fault` at the session monitor's cadence, so a future wedge may heal before a prober run
reads it. That is recorded in the TODOS entry, not solved here.

**Criterion C4 is therefore NOT met.** The measuring half is true in production; the calibration
half is open. `requirements-completed` above was corrected from `["164.6.5-C4"]` to `[]`.

