---
phase: 164.6.5-mt5validatewedge
verified: 2026-09-26T07:26:55Z
status: human_needed
score: 4/9 must-haves verified (2 deferred to Phase 164.6.6, 1 present behavior-unverified, 2 founder UAT)
verified_at_sha: 120c1e065108867868ae14664c11a580eb3ffbdb
drift_subjects:
  - analytics-service/services/mt5_client.py
  - analytics-service/services/mt5_relogin.py
  - analytics-service/services/mt5_session_episodes.py
  - analytics-service/services/mt5_session_monitor.py
  - analytics-service/services/mt5_validation.py
  - analytics-service/services/mt5_probe.py
  - analytics-service/services/job_worker.py
  - analytics-service/routers/exchange.py
  - scripts/prod-prober/arms/mt5.mjs
  - scripts/prod-prober/fixtures/mt5/10005.txt
  - src/lib/wizardErrors.ts
  - src/lib/wizard/wizard-correlation.ts
  - src/instrumentation.ts
  - src/app/api/keys/[id]/rotate-secret/route.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - docs/runbooks/mt5-go-live.md
  - deploy/mt5-gateway/railway-gateway.md
behavior_unverified: 1
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/7
  previous_report_sha: 38647f8b5
  gaps_closed:
    - "Criterion 4 ledger gap: D-10 production evidence is now recorded in plan 03's SUMMARY (runs 36104820751, 36134914962, 36212376262), plan 03 flipped halted -> complete with requirements-completed corrected from [C4] to [], and D-11 is booked OPEN by id as TODOS.md MT5-PROBER-WEDGE-CALIBRATION-01, owned by Phase 164.6.6 with trigger and gate. C4 itself is NOT met; it moves from gaps to deferred."
    - "WINDOWS.md entry 68 anti-pattern: the entry now names the session_snapshot conn.eval as well as the terminate step, in both the table row and the JSON description, and says a terminate-only reading does not close it."
  gaps_remaining: []
  regressions: []
  scope_correction: "The previous report scored 7 truths and omitted the ROADMAP's two inherited success criteria (7 from Phase 161, 8 from Phase 164.5.3). They are roadmap truths and are scored here, so the denominator moves 7 -> 9. The previous report also counted C1 as VERIFIED 'as explicitly open'; under this run's rule an honestly-OPEN criterion routed to a later phase is deferred and not counted."
deferred:
  - truth: "Criterion 1 — the root cause of the switch wedge is found and named"
    addressed_in: "Phase 164.6.6"
    evidence: "ROADMAP 164.6.6: 'Owns (2026-09-25, from 164.6.5 plan 01): TODOS.md MT5-SWITCH-WEDGE-CAUSE-01 ... Closes only on evidence captured at the next wedge BEFORE any restart'"
  - truth: "Criterion 4, calibration half (D-11) — the prober's -10005 kind is calibrated against a REAL wedge, not only a fixture"
    addressed_in: "Phase 164.6.6"
    evidence: "ROADMAP 164.6.6: 'Owns (2026-09-26, from 164.6.5 plan 08): TODOS.md MT5-PROBER-WEDGE-CALIBRATION-01 ... Trigger: the next live -10005, captured BEFORE the heal recycles the terminal ... Gate: a scrubbed real-wedge transcript committed under scripts/prod-prober/fixtures/mt5/ ... A hand-written fixture is not a close.' ⚠️ ROADMAP criterion 4 itself says 'REQUIRED HERE, not deferred'; see human_verification item 4."
behavior_unverified_items:
  - truth: "Criterion 2 — an unresponsive terminal restarts without a human, preserving the unattended auto-login"
    test: "At the first live -10005 after deploy (or a founder-supervised induced wedge), read the analytics log: the `pre-recycle wedge evidence` line, the escalation line with matched/terminated/exited/authorized/relaunch_code, then the post-relaunch session fragment. Then read the next prod-prober mt5 line."
    expected: "terminated == matched >= 1, exit confirmed, relaunch answers authorized on the house session (kind ipc_fault_recycled at INFO), the snapshot line carries its verdict fields (not an error), and the next prober line reads initialize=true connected=true trade_allowed=true. No human touched VNC."
    why_human: "The Win32 TerminateProcess half and the session_snapshot conn.eval have run only against a faked WinDLL / a local double. The 2026-09-25 spike proved kill + initialize() relaunch with a Linux-side kill, not the over-the-bridge terminate. Owner: .planning/WINDOWS.md entry 68 (post-deploy; now names both paths)."
human_verification:
  - test: "First live recycle AND first live session_snapshot over the rpyc bridge (WINDOWS.md entry 68) — see behavior_unverified_items"
    expected: "Recycle ends every terminal64.exe, relaunches authorized unattended, verdict ipc_fault_recycled; the snapshot's conn.eval resolves mt5 in the bridge namespace and returns a by-value tuple"
    why_human: "Needs the live gateway after deploy; this verifier ran no gateway command"
  - test: "Inherited criterion 7 (from Phase 161): read one live MT5 validate that lands an `undetermined` capability verdict"
    expected: "It names 'Allow algorithmic trading' when Experts is off, and names the external-Python-API option only when terminal_info reports tradeapi_disabled"
    why_human: "Live-only; the verdict has no durable sink (MT5-VERDICT-SINK-01 stays deferred), so it must be captured as it happens. ROADMAP routes it to founder UAT."
  - test: "Inherited criterion 8 (from Phase 164.5.3): end-to-end live MT5 credential update on a key the worker marked revoked or error"
    expected: "PATCH 200, the revoked pill drops at once, and the next ledger-refresh or allocator-poll tick fans the key out again (sync_status = 'idle' alone does not close it)"
    why_human: "Founder-only: no agent enters or drives a real credential"
  - test: "Founder ratifies routing D-11 out of this phase"
    expected: "Founder accepts (or reverses) moving the real-wedge calibration to Phase 164.6.6, given ROADMAP criterion 4's own wording 'REQUIRED HERE, not deferred', and given that this phase's heal now recycles a wedge before a scheduled prober run is likely to read it"
    why_human: "The deferral contradicts the criterion's text. The verifier accepts the routing as honest (owner, trigger and gate are all named) but cannot waive a criterion that forbade exactly this."
  - test: "Founder confirms D-16 and D-17 (CONTEXT, 2026-09-23, 'founder to CONFIRM')"
    expected: "Ratified or reversed: login-stage -10005 stays SIGN_IN_FAILED; other IPC faults move 424 NETWORK_UNAVAILABLE+Retry -> 500 MT5_TERMINAL_UNRESPONSIVE without Retry; the code joins DASHBOARD_DIALOG_ROUTE_CODES"
    why_human: "Orchestrator decision still recorded as awaiting founder confirmation; it changes PROD answers. Under D-16 the FIRST, wedge-causing validate still answers SIGN_IN_FAILED."
  - test: "Operator check (R2-SFH-05): a Sentry alert rule routes the heal's hourly ERROR re-raise and the capped-recycle ERROR to a human"
    expected: "A rule exists and fires on those events"
    why_human: "Live alert-rule configuration is not in the repo"
  - test: "Copy read-through of KEY_MT5_TERMINAL_UNRESPONSIVE in the connect step and in UpdateMt5SecretDialog (320px and 200% zoom)"
    expected: "No Retry control; 'Your draft is saved' only on the connect step; no promise of a self-heal beyond 'can come back'"
    why_human: "Visual and copy judgement (D-13)"
---

# Phase 164.6.5: MT5VALIDATEWEDGE Verification Report

**Phase Goal:** A client can add an MT5 key, repeatedly, without taking MT5 validation down for
everyone. If the terminal does wedge, it recovers WITHOUT a human.
**Verified:** 2026-09-26T07:26:55Z at `120c1e065` (branch `feat/164.6.5-mt5validatewedge`, plans
01–08 executed, `origin/main` merged in at `ef9d43681`, release v0.94.0.0)
**Status:** human_needed
**Re-verification:** Yes. The previous report (`38647f8b5`) was `gaps_found` 5/7, with one gap on
criterion 4.

## Goal Achievement

The code that makes the goal reachable is present, wired and green offline. Every gate was re-run
at HEAD after the merge. The goal's live half has not been observed: a terminal recovering without
a human in production. The phase cannot observe it until a real wedge happens after deploy. Nothing
below is counted as passed on the strength of a ledger entry.

### Observable Truths

| # | Truth (ROADMAP criterion) | Status | Evidence |
|---|---|---|---|
| 1 | C1: root cause of the switch wedge found and named | ⏭ DEFERRED (Phase 164.6.6), not counted | Recorded honestly OPEN (D-03): runbook Step 2b verdicts per candidate; `TODOS.md` `MT5-SWITCH-WEDGE-CAUSE-01`; ROADMAP 164.6.6 "Owns" it. Nothing restates the restart mitigation as a diagnosis. Regression check: unchanged since the last report. |
| 2 | C2: supervised terminal with an IPC liveness check, restarted without a human, auto-login preserved | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Unchanged since the last report: no phase source file moved between `4c0c10feb` and HEAD (only main's unrelated `wizardErrors.ts` addition). The recycle verb is wired from the monitor loop, and its 9 offline contract tests pass inside the `-k mt5` run. Never run live over the bridge. WINDOWS.md 68 now names both remote paths, so a terminate-only reading can no longer close it. |
| 3 | C3: the heal ACTS on `ipc_fault` | ✓ VERIFIED | The state-transition tests named in the last report pass at HEAD, inside `pytest -k mt5`: one recycle per five readings, the cap at 2/window, a raising recycle that cannot escape, and the budget covering escalation. |
| 4 | C4: the prober's MT5 arm actually measures the terminal, calibrated against a REAL wedge | ⏭ DEFERRED on its calibration half, not counted | **Measuring half (D-10): true.** This is now recorded in plan 03's SUMMARY, the TODOS entry and the ROADMAP outcome table, and it matches what this verifier read from `gh run list`. The most recent completed scheduled run is still 36212376262, at head `ecf1ef4e`. **Calibration half (D-11): not done.** `scripts/prod-prober/fixtures/mt5/10005.txt` was last touched by Phase 164.8.3 (`5dead88ab`) and is constructed, not captured. D-11 is booked by id as `MT5-PROBER-WEDGE-CALIBRATION-01`, which names an owner (164.6.6), a trigger and a gate, and says a hand-written fixture does not close it. Plan 03's false `requirements-completed: [C4]` was corrected to `[]`. ⚠️ The criterion's own text reads "REQUIRED HERE, not deferred", so the routing needs founder ratification (human item 4). |
| 5 | C5: the wizard stops promising a retry that cannot work | ✓ VERIFIED | Unchanged. `MT5_TERMINAL_UNRESPONSIVE` → `KEY_MT5_TERMINAL_UNRESPONSIVE`, whose actions offer no recoverable option, so Retry is suppressed. It is admitted to the three route rosters and the dialog roster. After the merge, `wizardErrors.test.ts` moved `EXPECTED_TABLE_SIZE` 96 → 97 to absorb main's `SUBMITTED_ANALYTICS_NOT_QUEUED`, and the suite passes. D-16/D-17 still await the founder (human item 5). |
| 6 | C6: `correlation_id` per request | ✓ VERIFIED | `wizardFetch` still mints `wizard:${crypto.randomUUID()}` per call. The merge resolution in `SyncPreviewStep.tsx` kept both sides: main's `probeExistingChain` guard runs first, the kickoff and retry paths still pass `onCorrelationId: setRequestCorrelationId`, the retry resets `requestCorrelationId`, and the envelope renders `upstream ?? request ?? page-load`. The wizard steps suites and `wizard-correlation.test.ts` pass. |
| 7 | C7 (pinned landmine): the algo-trading setting is PINNED, not ticked | ✓ VERIFIED | Unchanged. The six D-15 gates pass inside `-k mt5`. Nothing writes the option. |
| 8 | Inherited 7 (Phase 161): a live `undetermined` verdict names the right option | ? UNCERTAIN, founder UAT, not counted | Live-only with no durable sink. The ROADMAP outcome table records it OPEN, routed to founder UAT. The previous report omitted this criterion. |
| 9 | Inherited 8 (Phase 164.5.3): end-to-end live MT5 credential update | ? UNCERTAIN, founder UAT, not counted | Founder-only. The ROADMAP outcome table records it OPEN. The previous report omitted this criterion. |

**Score:** 4/9 truths verified. 2 are deferred to Phase 164.6.6, 1 is present but behavior-unverified, and 2 need founder UAT.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|---|---|---|
| 1 | C1: the mechanism behind the switch wedge | Phase 164.6.6 | ROADMAP 164.6.6 "Owns … `MT5-SWITCH-WEDGE-CAUSE-01`" |
| 2 | C4 calibration half (D-11) | Phase 164.6.6 | ROADMAP 164.6.6 "Owns … `MT5-PROBER-WEDGE-CALIBRATION-01`", with trigger and gate; mirrored in `TODOS.md` |

### Advisory (New Scope, Unevidenced)

| # | Finding | Category | Why Advisory |
|---|---|---|---|
| 1 | The CHANGELOG 0.94.0.0 headline says "a wedged MT5 terminal heals without a human". That has not yet been observed live. The body does disclose it: C2 is OPEN on its live half, and WINDOWS 68 is named. | other | The headline outruns the evidence. No gate fails on it, and the disclosure is in the body. |

### Key Link Verification (regression check)

All links from the previous report still hold. No phase-owned source file changed between
`4c0c10feb` and HEAD except `src/lib/wizardErrors.ts`, where main added
`SUBMITTED_ANALYTICS_NOT_QUEUED` (+24 lines, unrelated to MT5). The merge-resolved
`SyncPreviewStep.tsx` was traced by hand (C6 above).

### Behavioral Spot-Checks and Gates (run by this verifier at `120c1e065`)

The Python gates ran from `analytics-service/` under `env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY`. They used the main checkout's
`analytics-service/.venv`, because the briefed repo-root `.venv` does not exist.

| Gate | Result |
|---|---|
| `pytest -k mt5 -q` | `1120 passed, 3 skipped, 5443 deselected, 6 warnings in 20.08s` (EXIT 0) |
| `pytest -q -n auto` (full suite, once) | `6476 passed, 90 skipped, 479 warnings in 43.78s` (EXIT 0) |
| CI mypy, as widened by main's 164.6.1: `mypy --strict --follow-imports=silent --config-file=pyproject.toml services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py` | `Success: no issues found in 100 source files` |
| `npx tsc --noEmit` | EXIT 0 |
| `npx vitest run` on 13 paths: prod-prober-wiring, the wizard steps dir, rotate-secret, add-key, create-with-key, UpdateMt5SecretDialog, dialog-envelope invariant, seam-venue-vocabulary invariant, wizard-correlation, wizardErrors, wizardErrors.invariant, instrumentation, critical-regressions | `Test Files 36 passed (36)` / `Tests 1554 passed (1554)` |
| `node scripts/prod-prober/run.mjs --self-test` | `=== SELF-TEST PASSED: 91/91 scenarios, every arm's kinds fired on their own fixtures and nowhere else ===` (83 → 91 comes from main's cron-obs fixtures) |
| Release files | `VERSION` = `0.94.0.0`, `package.json` `"version": "0.94.0.0"`, `CHANGELOG.md` `## [0.94.0.0]` at line 3 |

### Probe Execution

| Probe | Result | Status |
|---|---|---|
| prod-prober self-test | 91/91 | PASS |
| PROD mt5 arm, `gh run list --workflow prod-prober.yml` (read-only) | The latest three completed runs, 36212376262, 36202021941 and 36188440940, all concluded `success` at head `ecf1ef4e`. This agrees with the recorded D-10 evidence. | D-10 answered; D-11 deferred |

No gateway, Railway or database command was run.

### Requirements Coverage

| Requirement | Plan | Status |
|---|---|---|
| 164.6.5-C1 | 01 | DEFERRED: honestly OPEN, owned by 164.6.6 |
| 164.6.5-C2 | 02 | NEEDS HUMAN: live recycle and snapshot, WINDOWS.md 68 |
| 164.6.5-C3 | 05 | SATISFIED (offline behaviour tests) |
| 164.6.5-C4 | 03, 08 | DEFERRED on its calibration half (D-11 → 164.6.6). The measuring half is satisfied and recorded. |
| 164.6.5-C5 | 04 | SATISFIED (D-16/D-17 founder confirmation pending) |
| 164.6.5-C6 | 07 | SATISFIED |
| 164.6.5-C7 | 06 | SATISFIED |

The requirement IDs appear only in the ROADMAP, so none are orphaned. The plan 08 SUMMARY's
`requirements-completed` [C3, C5, C6, C7] and `requirements-open` [C1, C2, C4] agree with this
table.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|---|---|---|---|
| Branch non-planning diff vs `origin/main` | TBD / FIXME / XXX on added lines | none found | none |
| `CHANGELOG.md` 0.94.0.0 headline | Claims live self-heal, which has not been observed | ℹ️ Info (advisory above) | Disclosed in the body |
| Plan 08 commit `196dea9a1` message says "Plans 01-07 ticked, 7/8" | The ROADMAP now reads 8/8 after the release commit | ℹ️ Info | Consistent at HEAD |

### Human Verification Required

1. **First live recycle and first live `session_snapshot` (WINDOWS.md 68).** Trigger: the first live `-10005` after deploy, or a founder-supervised induced wedge. Expected: terminated == matched >= 1, exit confirmed, the relaunch authorized unattended, kind `ipc_fault_recycled`, the snapshot fields present, and no human touching VNC.
2. **Inherited criterion 7.** Read a live `undetermined` verdict and confirm it names the right option.
3. **Inherited criterion 8.** The founder runs "Update password" on a revoked MT5 key end to end.
4. **Ratify the D-11 routing.** Criterion 4 says "REQUIRED HERE, not deferred". The routing is honest, with owner, trigger and gate all named, but only the founder can accept a deferral the criterion forbade. Note that this phase's own heal makes the trigger harder to meet.
5. **Confirm D-16 and D-17.**
6. **Sentry alert rule** for the hourly ERROR re-raise and the capped-recycle ERROR (R2-SFH-05).
7. **Copy read-through** of `KEY_MT5_TERMINAL_UNRESPONSIVE` at 320px and 200% zoom.

### Gaps Summary

No blocking gap remains. The previous report's only gap was a ledger gap on criterion 4, and plan 08
closed it the honest way:
- the D-10 production evidence is recorded;
- plan 03's false claim that C4 was complete is corrected;
- D-11 is booked by id with an owner phase, a trigger and a gate that refuses a hand-written fixture.

C4 is **not** met, and it is not counted. It is deferred, and so is C1.

The phase goal's defining behavior, recovery without a human, has not happened in production yet.
It cannot be observed until a real wedge occurs after deploy, so the phase is `human_needed`, not
`passed`.

This report also corrects its predecessor's denominator: it adds the two inherited ROADMAP criteria,
and it stops counting the honestly-open C1 as verified.

`164.6.5-SECURITY.md` is being re-audited in parallel and was not assessed or touched here.

---

_Verified: 2026-09-26T07:26:55Z_
_Verifier: Claude (gsd-verifier)_
