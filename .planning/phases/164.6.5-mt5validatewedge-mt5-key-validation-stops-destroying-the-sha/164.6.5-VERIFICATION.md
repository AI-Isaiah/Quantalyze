---
phase: 164.6.5-mt5validatewedge
verified: 2026-09-26T05:58:37Z
status: gaps_found
score: 5/7 must-haves verified
verified_at_sha: 4c0c10febe4def04d3d6df979bef04678ca8688b
drift_subjects:
  - analytics-service/services/mt5_client.py
  - analytics-service/services/mt5_relogin.py
  - analytics-service/services/mt5_session_episodes.py
  - analytics-service/services/mt5_session_monitor.py
  - analytics-service/services/mt5_validation.py
  - analytics-service/services/mt5_probe.py
  - analytics-service/services/job_worker.py
  - analytics-service/routers/exchange.py
  - scripts/prod-prober/arms/mt5.mjs
  - src/lib/wizardErrors.ts
  - src/lib/wizard/wizard-correlation.ts
  - src/instrumentation.ts
  - src/app/api/keys/[id]/rotate-secret/route.ts
  - docs/runbooks/mt5-go-live.md
  - deploy/mt5-gateway/railway-gateway.md
behavior_unverified: 1
overrides_applied: 0
gaps:
  - truth: "Criterion 4 — the prod-prober's MT5 arm actually measures the terminal, calibrated against a REAL wedge, not only a fixture (ROADMAP: 'REQUIRED HERE, not deferred')"
    status: partial
    reason: >-
      Half is now true in production and recorded nowhere; the other half is neither done nor
      routed. (a) D-10 IS answered by evidence this verifier read from GitHub Actions: scheduled
      prod-prober runs reported `mt5-ssh-transport` up to run 36104820751 (2026-09-25T06:53Z) and
      a terminal-measuring line `mt5: initialize=true last_error=1 connected=true
      trade_allowed=true` from run 36134914962 (2026-09-25T12:26Z, head 01dcf1cc) onward, through
      run 36212376262 (2026-09-26T02:40Z, head ecf1ef4e). No file in the repo records this, and
      plan 03's SUMMARY still reads `status: halted` with task 3 "NOT STARTED". (b) D-11 (real-wedge
      calibration) was not done: `scripts/prod-prober/fixtures/mt5/` is unchanged since Phase
      164.8.3, and `mt5-ipc-timeout` has still never fired in production. Plan 03's own contract
      says that in that case "D-11 is then recorded explicitly OPEN with a named routed residual
      (owner + trigger), never quietly closed". No such residual exists in TODOS.md or in the
      ROADMAP, and no later phase's criteria cover it.
    artifacts:
      - path: ".planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-03-SUMMARY.md"
        issue: "status: halted; task 3 (D-10 live proof, D-11 real-wedge calibration) unrecorded"
      - path: "scripts/prod-prober/fixtures/mt5/"
        issue: "no real-wedge transcript; calibration is fixture-only"
    missing:
      - "Record the D-10 evidence (first terminal-measuring run 36134914962 at head 01dcf1cc, and a current one) in plan 03's SUMMARY and flip it from halted"
      - "EITHER commit a real-wedge transcript fixture (founder capture, scrubbed) OR book D-11 explicitly OPEN as a routed residual in TODOS.md + the ROADMAP with an owner phase and a trigger (the next natural -10005, captured before the heal recycles it). Plan 08 task 1 is the natural place, but it must name D-11 by id, not fold it into 'criterion 4 MET'"
deferred:
  - truth: "Criterion 1 — the root cause of the switch wedge is found and named"
    addressed_in: "Phase 164.6.6"
    evidence: "ROADMAP 164.6.6: 'Owns (2026-09-25, from 164.6.5 plan 01): TODOS.md MT5-SWITCH-WEDGE-CAUSE-01 — why some account switches on the shared terminal wedge it ... Closes only on evidence captured at the next wedge BEFORE any restart'"
behavior_unverified_items:
  - truth: "Criterion 2 — an unresponsive terminal restarts without a human, preserving the unattended auto-login"
    test: "At the first live -10005 after deploy (or a founder-supervised induced wedge), read the analytics log: the `pre-recycle wedge evidence` line, then the escalation line with matched/terminated/exited/authorized/relaunch_code, then the post-relaunch session fragment. Then read the next prod-prober mt5 line."
    expected: "terminated == matched >= 1, exited confirmed, relaunch answers authorized on the house session (kind ipc_fault_recycled at INFO), the snapshot line carries build/connected/session_server_matches_env (not an error), and the next prober line reads initialize=true connected=true trade_allowed=true. No human touched VNC."
    why_human: "The Win32 TerminateProcess half and the `session_snapshot` conn.eval have run only against a faked WinDLL / a local double. The 2026-09-25 spike proved kill + initialize() relaunch with a Linux-side kill, not the over-the-bridge terminate. Owner: .planning/WINDOWS.md entry 68 (post-deploy)."
human_verification:
  - test: "First live recycle over the rpyc bridge (WINDOWS.md entry 68) — see behavior_unverified_items"
    expected: "Recycle ends every terminal64.exe, relaunches authorized unattended, verdict ipc_fault_recycled"
    why_human: "Needs the live gateway; this verifier ran no gateway command"
  - test: "First live `Mt5Client.session_snapshot` crossing: confirm `conn.eval` resolves `mt5` in the bridge namespace and brine returns the tuple by value"
    expected: "Evidence line carries build/connected/equality verdicts, never `Mt5ClientError` for a netref"
    why_human: "Never run against mt5linux on the gateway. ⚠️ WINDOWS.md entry 68's description names only the TerminateProcess half; CONTEXT and REVIEW-FIX cite entry 68 for the snapshot too"
  - test: "Founder confirms D-16 and D-17 (CONTEXT, 2026-09-23, 'founder to CONFIRM')"
    expected: "Ratified or reversed: login-stage -10005 stays SIGN_IN_FAILED; other IPC faults move 424 NETWORK_UNAVAILABLE+Retry -> 500 MT5_TERMINAL_UNRESPONSIVE without Retry; the code joins DASHBOARD_DIALOG_ROUTE_CODES"
    why_human: "Orchestrator decision recorded as awaiting founder confirmation; it changes PROD answers"
  - test: "Operator check (R2-SFH-05): a Sentry alert rule routes the heal's hourly ERROR re-raise and the capped-recycle ERROR to a human"
    expected: "A rule exists and fires on those events"
    why_human: "Live alert-rule configuration is not in the repo"
  - test: "Copy read-through of KEY_MT5_TERMINAL_UNRESPONSIVE in the connect step and in UpdateMt5SecretDialog (320px and 200% zoom)"
    expected: "No Retry control; 'Your draft is saved' only on the connect step; no promise of a self-heal beyond 'can come back'"
    why_human: "Visual and copy judgement (D-13)"
---

# Phase 164.6.5: MT5VALIDATEWEDGE Verification Report

**Phase Goal:** A client can add an MT5 key, repeatedly, without taking MT5 validation down for
everyone. If the terminal does wedge, it recovers WITHOUT a human.
**Verified:** 2026-09-26T05:58:37Z at `4c0c10feb` (branch `feat/164.6.5-mt5validatewedge` worktree, plans 01–07 executed; plan 08 not run)
**Status:** gaps_found
**Re-verification:** No. This is the initial verification. The previous attempt died in an outage and wrote no report.

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP criterion) | Status | Evidence |
|---|---|---|---|
| 1 | C1: the root cause is named, or recorded as honestly open (D-03) | ✓ VERIFIED as EXPLICITLY OPEN, not claimed (goal half deferred to 164.6.6) | `docs/runbooks/mt5-go-live.md` Step 2b gives a dated verdict per candidate: (a) self-update UNDECIDED, (b) same/different account REJECTED, (c) same/different broker server UNDECIDED. `TODOS.md` has `MT5-SWITCH-WEDGE-CAUSE-01`, which names the three readings that close it and carries a dated SFH-04 correction saying the heal's capture is narrower than "the build and the servers". `MT5-WEDGE-OBS-01` is refined, not reopened. ROADMAP `### Phase 164.6.6` "Owns" the residual. Nothing restates a mitigation as a diagnosis: the restart remedy is recorded under D-02 as a MITIGATION finding. |
| 2 | C2: supervision with an IPC liveness check, restarted without a human, auto-login preserved | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | **Present:** `Mt5Client.recycle_terminal_process` pushes the committed literal `_REMOTE_TERMINAL_RECYCLE_SRC`, fenced by `_assert_live` and `_guarded_read`, then relaunches through `assert_session_authorized`. The liveness decision is the credential-free IPC detector (D-06). **Wired:** `main.py` starts `mt5_session_monitor_loop`, which calls `heal_mt5_terminal_session`, then `_heal_blocking`, then `_escalate_ipc_fault`, then the verb. **Tested offline:** 9 contract tests exec the real source against a stubbed Win32 body. **Not exercised live:** the over-the-bridge terminate (WINDOWS.md 68). The auto-login-preserving relaunch was measured only for a Linux-side kill (plan 02 spike). The prober reading `initialize=true connected=true trade_allowed=true` after that spike's `/portable` relaunch corroborates D-07 for the spike mechanism, not for the verb. |
| 3 | C3: the heal ACTS on `ipc_fault` | ✓ VERIFIED | `_heal_blocking`'s non-`-6` branch composes the byte-identical `not_healed:ipc_fault:` verdict and then sets `escalation_kind=_escalate_ipc_fault(...)`. The code gate is exactly `{-10005}`. There is one recycle per run, capped at `IPC_FAULT_RECYCLE_CAP`=2 per `_IPC_FAULT_RECYCLE_WINDOW_S` (D-18). It never raises except `Mt5SessionAbandoned`. The state transitions are exercised by passing tests: `test_ESCALATION_five_consecutive_ipc_timeouts_produce_exactly_ONE_recycle`, `test_R2_CR01_a_wedge_that_RECURS_after_each_working_recycle_is_CAPPED_and_raised_at_ERROR`, `test_ESCALATION_a_raising_recycle_cannot_escape_the_heal`, `test_the_budget_covers_the_ESCALATION_path_too` and `test_R2_WR03_RC_the_capture_and_the_post_relaunch_check_RUN_at_the_default_budget`, all inside the `-k mt5` run below. |
| 4 | C4: the prober's MT5 arm actually measures the terminal, calibrated against a REAL wedge | ✗ FAILED (partial) | **Code half done:** the `-10005` remedy names both causes and asserts neither. A D-10 set-equality gate ties `ARM.requiredEnv` to the workflow env, and it passes. **Live half:** the arm DOES measure the terminal in PROD. Scheduled run 36134914962 (2026-09-25T12:26Z, head 01dcf1cc) is the first `mt5: initialize=true ...` line; run 36104820751 (06:53Z) was still `mt5-ssh-transport`. That evidence is recorded nowhere in the repo. **D-11 (real-wedge calibration)** is neither done nor routed: fixtures are unchanged, plan 03 is `status: halted`, and there is no residual. See Gaps. |
| 5 | C5: the wizard stops promising a retry that cannot work | ✓ VERIFIED | `routers/exchange.py` `_validate_mt5_key_probe`: `is_ipc_transport_fault(e)` raises `service_error(500, "MT5_TERMINAL_UNRESPONSIVE", retryable=False)` and logs at ERROR. It sits inside 167's not-a-login-refusal branch (D-16). `VENUE_WIRE_CODE_TO_VERDICT` maps it to `KEY_MT5_TERMINAL_UNRESPONSIVE`, whose `actions: ["request_call"]` holds no RECOVERABLE action, so `buildEnvelope` suppresses Retry. The code is admitted to `KNOWN_CREATE_WITH_KEY_CODES`, `KNOWN_ADD_KEY_CODES` and `DASHBOARD_DIALOG_ROUTE_CODES` (rotate-secret, D-17). `KEY_NETWORK_TIMEOUT` is intact and was not widened, and the D-12 fence tests execute. |
| 6 | C6: `correlation_id` per request | ✓ VERIFIED | `wizardFetch` mints `wizard:${crypto.randomUUID()}` per call on `X-Correlation-Id`, respects a caller value, and keeps `getWizardCorrelationId()` on `X-Wizard-Page-Load-Id`, which `src/instrumentation.ts` reads (WR-07). All six wizard envelope surfaces render the failed attempt's id. `wizard-correlation.test.ts` and the step suites pass. |
| 7 | C7: the algo-trading landmine is PINNED, not ticked | ✓ VERIFIED | `mt5_validation.ACCOUNT_CHANGE_ALGO_DISABLE_OPTION` is named, and there is no second judge. The operator arm logs at ERROR and names the option. The six D-15 gates include a comment-stripping no-terminal-option-write scan. The stale "every account change" sentences are corrected with lineage in `mt5_client.py`, `job_worker.py` and `mt5.mjs`. Nothing writes the setting. |

**Score:** 5/7 truths verified (1 present, behavior-unverified; 1 failed)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|---|---|---|
| 1 | C1's "found and named" half (the mechanism behind the switch wedge) | Phase 164.6.6 | ROADMAP 164.6.6 "Owns ... `MT5-SWITCH-WEDGE-CAUSE-01` ... Closes only on evidence captured at the next wedge BEFORE any restart" |

### Required Artifacts

`gsd-tools verify.artifacts` returned all-pass for plans 01–07: 2/2, 3/3, 2/2, 4/4, 3/3, 3/3 and 2/2. The key-link tool could not parse this phase's `file :: symbol` link format ("Source file not found"), so every link below was traced by hand.

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `main.py` lifespan | `mt5_session_monitor_loop` | `asyncio.create_task` | WIRED |
| monitor tick | `heal_mt5_terminal_session` → `_heal_blocking` | lease + `to_thread` + `wait_for(budget)` | WIRED |
| `_heal_blocking` ipc branch | `_escalate_ipc_fault` | `escalation_kind=` | WIRED |
| `_escalate_ipc_fault` | `Mt5Client.recycle_terminal_process` / `session_snapshot` | direct call | WIRED |
| `recycle_terminal_process` | `_assert_live`, `_guarded_read`, `assert_session_authorized` | in-method | WIRED |
| `_validate_mt5_key_probe` | `MT5_TERMINAL_UNRESPONSIVE` → `KEY_MT5_TERMINAL_UNRESPONSIVE` | `VENUE_WIRE_CODE_TO_VERDICT` | WIRED |
| copy `actions` | Retry suppression | `buildEnvelope` / `RECOVERABLE_ACTIONS` | WIRED |
| `wizardFetch` | server inbound id | `X-Correlation-Id` / `correlation-id.ts` | WIRED |
| `ARM.requiredEnv` | `prod-prober.yml` Probe production env | D-10 set-equality test | WIRED (test passes) |

### Behavioral Spot-Checks and Gates (run by this verifier, at `4c0c10feb`)

The Python gates ran from `analytics-service/` with the main checkout's `.venv`, under `env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY`.

| Gate | Result |
|---|---|
| `pytest -k mt5 -q` | `1120 passed, 3 skipped, 5212 deselected, 33 warnings in 19.32s` (EXIT 0) |
| `pytest -q -n auto` | `6245 passed, 90 skipped, 928 warnings in 38.97s` (EXIT 0) |
| `mypy --strict --follow-imports=silent services/ routers/ models/` (CI's command) | `Success: no issues found in 96 source files` |
| `npx tsc --noEmit` | EXIT 0 |
| `npx vitest run` over 12 touched TS suites/dirs (prod-prober-wiring, wizard steps dir, rotate-secret, add-key, create-with-key, UpdateMt5SecretDialog, instrumentation, dialog-envelope invariant, seam-venue-vocabulary invariant, wizard-correlation, wizardErrors, wizardErrors.invariant) | `Test Files 34 passed (34)` / `Tests 1360 passed (1360)` |
| `node scripts/prod-prober/run.mjs --self-test` | `=== SELF-TEST PASSED: 83/83 scenarios, every arm's kinds fired on their own fixtures and nowhere else ===` |

ℹ️ A scratch file left by the pre-outage verifier attempt shows one failure in the full suite, `tests/test_feedback_engine.py::test_lazy_import_not_triggered_at_module_load`. This run did not reproduce it: the full suite was green, and the test passes on its own (`1 passed`). The file is not touched by this phase. It is an order-dependent flake under xdist.

### Probe Execution

| Probe | Result | Status |
|---|---|---|
| prod-prober self-test (above) | 83/83 | PASS |
| PROD mt5 arm, scheduled runs (read-only `gh run view --log`) | 36104820751: `mt5-ssh-transport`. 36134914962, 36168373432, 36202021941 and 36212376262: `mt5: initialize=true last_error=1 connected=true trade_allowed=true` | D-10 answered; D-11 open |

No gateway, Railway or database command was run.

### Requirements Coverage

| Requirement | Plan | Status |
|---|---|---|
| 164.6.5-C1 | 01 | SATISFIED as honestly OPEN (residual owned by 164.6.6) |
| 164.6.5-C2 | 02 | NEEDS HUMAN (live recycle, WINDOWS.md 68) |
| 164.6.5-C3 | 05 | SATISFIED (offline behaviour tests) |
| 164.6.5-C4 | 03 | BLOCKED (partial): D-10 unrecorded, D-11 unrouted |
| 164.6.5-C5 | 04 | SATISFIED |
| 164.6.5-C6 | 07 | SATISFIED |
| 164.6.5-C7 | 06 | SATISFIED |

The IDs exist only in the ROADMAP; `.planning/REQUIREMENTS.md` carries none of them, so none are orphaned.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|---|---|---|---|
| (all added non-planning lines) | TBD / FIXME / XXX / TODO / HACK | none found | none |
| `.planning/WINDOWS.md` entry 68 | Its description names only the TerminateProcess half, while CONTEXT (WR-03 root-cause entry) and REVIEW-FIX cite it for the `session_snapshot` `conn.eval` and the `file_versions` read too | ⚠️ Warning | A later close of entry 68 on a terminate-only reading could leave the snapshot path unmeasured. Widen the entry's text in plan 08. |
| ROADMAP `### Phase 164.6.5` | "Plans: 2/8 plans executed" and unticked boxes are stale | ℹ️ Info | Plan 08 task 1 owns it |
| `analytics-service/tests/test_mt5_relogin.py` | pre-existing F401 (`import asyncio`) | ℹ️ Info | Not a CI step; unchanged |

### Human Verification Required

1. **First live recycle over the bridge (WINDOWS.md 68).** Trigger: the first live `-10005` after deploy, or a founder-supervised induced wedge. Expected: terminated == matched >= 1, exit confirmed, relaunch authorized on the house session, kind `ipc_fault_recycled` at INFO, and no human touching VNC. Why human: only faked Win32 has run it.
2. **First live `session_snapshot`.** Expected: `conn.eval` resolves `mt5`, a by-value tuple, and verdict-only fields. Why human: this path has never crossed mt5linux.
3. **Founder confirmation of D-16 and D-17.** These change PROD answers for non-sign-in IPC faults. Note that under D-16 the FIRST, wedge-causing validate (a login-stage `-10005`, like incident #1) still answers `SIGN_IN_FAILED`. Only the retries against an already-wedged terminal get the new honest code.
4. **Sentry alert rule** for the hourly ERROR re-raise and the capped-recycle ERROR (R2-SFH-05).
5. **Copy read-through** of `KEY_MT5_TERMINAL_UNRESPONSIVE` on the connect step and the rotate dialog.

### Gaps Summary

One gap, and it is about ledgers, not code. Criterion 4 required the prober to measure the terminal and to be calibrated against a real wedge, "REQUIRED HERE, not deferred".

The measuring half has quietly become true in production: scheduled runs have read the terminal since 2026-09-25T12:26Z. The repo still says the opposite, because plan 03 is `halted` with task 3 "NOT STARTED".

The calibration half (D-11) was never done. Plan 03 required it to be booked explicitly OPEN with an owner and a trigger if it could not be done, and that booking was never written.

Closing it takes no code:
- record the D-10 run ids and head sha in plan 03's SUMMARY;
- either commit a scrubbed real-wedge fixture, or book D-11 as a routed residual in TODOS.md and the ROADMAP.

Plan 08's per-criterion ledger task is where the second half belongs. It must name D-11 and not fold it into "criterion 4 MET".

Everything else that is not green needs the live gateway or a founder, and is routed above rather than counted as passed:
- the live recycle and snapshot (C2, WINDOWS.md 68);
- D-16/D-17 confirmation;
- the alert rule;
- the copy read-through.

Criterion 1 is recorded honestly OPEN and owned by 164.6.6. That is the D-03 success path, and nothing claims it as met.

`/gsd-secure-phase` for 164.6.5 was owed by the D-05 ratification. It is running in parallel and writes SECURITY.md, so it is not assessed here.

---

_Verified: 2026-09-26T05:58:37Z_
_Verifier: Claude (gsd-verifier)_
