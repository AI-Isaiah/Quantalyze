---
phase: 164.6.5
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/mt5_client.py
  - analytics-service/tests/test_mt5_client_contract.py
  - deploy/mt5-gateway/railway-gateway.md
autonomous: false
requirements: ["164.6.5-C2"]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-05 is CLOSED with a recorded choice and the reason it beat the other three candidates, inside the D-06/D-07 fence (D-05)."
    - "The liveness DECISION is taken by the credential-free IPC detector that already exists — `Mt5Client.assert_session_authorized` — and never by process liveness, window liveness or UI responsiveness, all three of which were MEASURED to pass for the entire 1h39m outage (D-06)."
    - "The recovery restarts the terminal PROCESS inside the same container, under the same Wine prefix and the same named volume. Nothing in this plan can reset, rebuild or discard that prefix, because the saved login that makes the 2.0 s unattended recovery possible lives in it (D-07)."
    - "The remote command is a COMMITTED, NON-INTERPOLATED source constant executed through ONE narrow method — never a general run-arbitrary-code helper — mirroring the shipped `_REMOTE_MATERIALIZE_SRC` discipline in the same file."
    - "The new verb does NOT reuse the name `restart`: `Mt5Client.restart` already exists and means `reconnect the rpyc transport`, which is a different thing entirely."
    - "A recovery attempt NEVER carries a credential: the verb re-launches a process and the terminal's own saved login does the rest."
  artifacts:
    - path: "analytics-service/services/mt5_client.py"
      provides: "a committed remote-source constant pair and ONE narrow client method that recycles the Wine-hosted terminal process over the existing rpyc channel, fenced by `_assert_live` and bracketed by `_guarded_read`"
      contains: "_REMOTE_MATERIALIZE_SRC"
    - path: "analytics-service/tests/test_mt5_client_contract.py"
      provides: "contract gates: the remote source carries no interpolation marker, the verb passes no credential, the verb is fenced by `_assert_live`, and a transport fault raises the typed error rather than returning a false success"
      contains: "TERMINAL_RECYCLE"
    - path: "deploy/mt5-gateway/railway-gateway.md"
      provides: "the D-05 decision record, the security posture change against T-134-03, and the exact command the spike proved"
      contains: "T-134-03"
  key_links:
    - from: "analytics-service/services/mt5_client.py :: the new recycle verb"
      to: "analytics-service/services/mt5_client.py :: _guarded_read"
      via: "the same scrub-and-time bracket every other remote crossing uses — a remote traceback carries the interpolated source line"
      pattern: "_guarded_read"
    - from: "analytics-service/services/mt5_client.py :: the new recycle verb"
      to: "analytics-service/services/mt5_client.py :: _assert_live"
      via: "the D-36 abandonment fence, as the first executable statement, exactly as every other session touch does it"
      pattern: "_assert_live"
    - from: "deploy/mt5-gateway/railway-gateway.md :: the D-05 decision record"
      to: "analytics-service/services/mt5_client.py :: the committed remote-source constant"
      via: "the document names the verb and the constant by symbol so the posture change and the code cannot drift apart"
      pattern: "mt5_client"
---

<objective>
**THE PHASE'S TRACER.** One path, every layer, wired end to end and proven against the real
terminal before anything expands from it: analytics-service → the existing rpyc channel → the
gateway container → the Wine-hosted `terminal64.exe` → the broker re-auth.

Criterion 2 — **`terminal64.exe` runs under supervision with a liveness check, so an unresponsive
terminal restarts without a human, and the restart preserves the auto-login proven today.**

Purpose: the 2026-09-21 outage lasted **1h39m** for a fault a **2.0 s** unattended process restart
cleared. ⭐ Nothing supervises the terminal: s6 supervises the desktop, VNC, nginx, cron and audio;
the terminal is started ONCE from the openbox autostart by the vendor's `start.sh` — verified
against the upstream source, which backgrounds it with a bare `&` and never restarts it — and
NOTHING watches it.

⛔ **D-04, the phase's biggest surprise, decides where a fix can live.** There is NO Dockerfile for
this terminal in this repository. The gateway runs the third-party prebuilt image
`gmag11/metatrader5_vnc:2.3`, deployed via Railway's *Deploy from Docker Image*. A plan that
assumes an editable in-repo s6 tree produces tasks that cannot be executed.

Output: D-05 closed with its reason; a live spike that proves the mechanism or kills it; and ONE
narrow, committed, credential-free verb that plan 05 will call.

⚠️ **If the task-2 spike DISPROVES the mechanism, STOP and REPLAN — do not hand-patch.** The
downstream plan (05) is built on this verb's existence; patching around a disproved spike
silently breaks it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-CONTEXT.md
@.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-RESEARCH.md
@.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-PATTERNS.md
@deploy/mt5-gateway/railway-gateway.md
@deploy/mt5-gateway/docker-compose.yml
@analytics-service/services/mt5_client.py
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: D-05 — ratify the supervision remedy, and the security posture it changes</name>
  <files>none (decision gate)</files>
  <reversibility rating="costly">D-05 makes a documented security liability load-bearing for production recovery; unwinding it means removing the verb and re-establishing a different supervision path, but no data and no deploy topology changes.</reversibility>
  <action>Ratify the D-05 supervision remedy and the security posture change it carries, before any code is written.</action>
  <decision>Which supervision remedy closes D-05, given that CONTEXT.md deliberately left it open and fenced it with D-06 (the probe must exercise the IPC bridge, not the process) and D-07 (the restart must preserve the unattended auto-login).</decision>
  <context>
The planner's recommendation is **Option 2**, and this gate exists for ONE reason: Option 2 makes a
documented security liability load-bearing. That is a founder call, not an engineering preference.

⛔ **THE COST, STATED PLAINLY.** The rpyc channel is documented in this repo as an
**unauthenticated arbitrary-remote-code channel** (Phase-134 finding T-134-03), private-mesh-only,
never to be publicly exposed. Choosing Option 2 means deliberately making it a production recovery
path. It creates NO new network exposure — the channel is already dialled on every validate and
every heal tick — but it does create a NEW CLASS of command sent over it.

⭐ **ONE CORRECTION TO PATTERNS.md, found by reading `mt5_client.py` directly, and it lowers the
cost.** PATTERNS.md records "no analog for the ACTION (executing code over rpyc classic)". That is
wrong at HEAD: `_materialize_rows` ALREADY executes a COMMITTED, NON-INTERPOLATED remote source
constant (`_REMOTE_MATERIALIZE_SRC`) via `conn.execute(...)` and reads the result back through
`_guarded_read`. The mechanism, the discipline AND the in-repo precedent all already exist. Option
2 is one more constant in a shape this file already ships and already tests.

**RATIFYING OPTION 2 ALSO RATIFIES:** that the capability ships as ONE narrow verb over a
committed, non-interpolated source constant — never a general run-arbitrary-code helper and never
an interpolated string; and that this plan's threat model records the posture change and the phase
runs `/gsd-secure-phase` before close.

⛔ **IF OPTION 2 IS REFUSED:** stop and return to planning. Option 1 is a materially bigger phase —
a new image, a build pipeline, a re-pointed deploy, and a standing maintenance obligation for the
terminal self-update. That is a PHASE SPLIT conversation, not an in-flight substitution.
  </context>
  <options>
    <option id="option-2-rpyc-supervision">
      <name>Option 2 — external supervision over the rpyc channel we already hold (PLANNER'S PICK)</name>
      <pros>Satisfies D-06 directly: the decision is taken by `assert_session_authorized()`, the credential-free IPC detector that produced `not_healed:ipc_fault` five consecutive times during the real outage — the ONE instrument MEASURED to have detected this exact fault. Satisfies D-07 directly: same container, same Wine prefix, same named volume, process-only recycle — literally the mechanism that reached `authorized` in 2.0 s unattended. No new image, no registry, no build pipeline, no re-pointed deploy; it ships inside this repo's existing test and typecheck gates. The remote-execution discipline it needs is already shipped and already tested in the same file.</pros>
      <cons>Deliberately makes a documented unauthenticated arbitrary-remote-code channel a load-bearing production recovery path. Requires an explicit threat model and must survive `/gsd-secure-phase`. Rests on assumption A1 — that the Wine process can be recycled over that channel without a credential — which is UNVERIFIED and is why task 2 spikes it before task 3 writes code.</cons>
    </option>
    <option id="option-1-wrap-the-image">
      <name>Option 1 — wrap the image with our own Dockerfile and an s6-rc.d longrun for the terminal</name>
      <pros>Most literal reading of criterion 2. Architecturally standard for this image family: the base ships s6-overlay v3 and the vendor documents a supported pattern for adding services on top of it.</pros>
      <cons>⛔ An s6 longrun supervises EXIT, and there was NO exit — throughout the 1h39m outage the process ran and its UI answered a human clicking its own Login dialog three ways while the Journal wrote nothing. A bare longrun would have sat quiet for the entire outage, so it is disqualified by D-06 by construction UNLESS it also builds an in-container IPC liveness checker — which is Option 2's detector, inside a new image we would then own. Option 1 is therefore Option 2 PLUS an image-ownership obligation: a build/push/digest-pin workflow, an amd64-only build, a re-pointed Railway deploy, and the broker's un-freezable terminal self-update becoming ours.</cons>
    </option>
    <option id="option-3-container-restart-policy">
      <name>Option 3 — Railway container healthcheck or restart policy</name>
      <pros>Cheapest to configure; the pattern already exists elsewhere in this repo for a different service.</pros>
      <cons>⛔ Cannot satisfy D-06 at all. The image exposes only noVNC and the rpyc bridge, and the bridge is under a HARD CONSTRAINT never to be publicly exposed — so there is no HTTP surface to healthcheck, and building one that exercises the IPC bridge means doing Option 1's work first. There is also no `railway.toml` for this gateway anywhere in the repo, so the setting could not be committed as code. And it restarts the WHOLE container, which is NOT the mechanism the 2.0 s unattended recovery was measured against.</cons>
    </option>
  </options>
  <resume-signal>Reply `D-05 = Option 2, ratified` — or name a different option id and halt the plan for replanning.</resume-signal>
  <done>D-05 is closed with a recorded decision and the reason it beat the other candidates, or the plan is halted for replanning.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: FOUNDER — the live spike: can the terminal process be recycled over the existing channel, and does the auto-login come back unattended?</name>
  <files>none (live spike)</files>
  <precondition>Task 1 ratified Option 2, and the founder can reach the `mt5-gateway` service and its VNC console.</precondition>
  <action>Spike assumption A1 against the live gateway: establish whether the Wine terminal process can be recycled over the existing channel and whether the auto-login returns with no human.</action>
  <instructions>
⛔ FOUNDER ACT. No agent runs any command against the live `mt5-gateway` — not `railway redeploy`,
not `railway ssh`, not a deliberate wedge.
⛔ No agent enters, reads, decrypts, logs or echoes an MT5 credential at any point.

RESEARCH.md's assumption **A1** is the single highest-risk claim under Option 2: that the Wine
process can be killed and relaunched from the Linux side of the container without corrupting the
Wine prefix or requiring a credential. It is ASSUMED, not verified. This task verifies it before a
line of code is written, because discovering it false during implementation wastes a wave and —
worse — would be discovered against production.

**WHAT TO ESTABLISH, in order. Steps 1–3 are READ-ONLY; step 4 is the only destructive one.**

1. **The process tree.** From inside the container, list the running processes and identify the
   Wine-hosted terminal and the rpyc bridge process. Record the exact process names and the exact
   command lines that launched them. ⛔ Redact any path that contains a home directory or a
   username before reporting.
2. **The launch sequence.** Read the vendor's `start.sh` inside the RUNNING container — not the
   version on GitHub, which may have moved. Record verbatim how it launches the terminal and how
   it launches the bridge. This is what the committed constant will mirror.
3. **The reachability question.** Establish whether the rpyc channel the analytics-service already
   dials can, from its own side, see and act on that process tree. This is A1's core. A read-only
   confirmation is enough at this step.
4. **THE DESTRUCTIVE STEP — and the whole point of it.** Recycle the terminal process (kill it,
   relaunch it the way `start.sh` does) and MEASURE, with timestamps:
     · how long until the terminal's own Journal writes `started`;
     · how long until it writes `authorized`;
     · whether ANY human input was required — ⛔ if a credential prompt appears, STOP, record it,
       and report; that outcome kills Option 2 as specified and is exactly what this spike is for;
     · whether `assert_session_authorized()` answers cleanly afterwards (the analytics-service's
       own session monitor tick is the cheapest way to read this).
   ⛔ **DO NOT** delete, reset, move or rebuild the Wine prefix or the named volume. D-07 is
   ONE-WAY: the saved login lives there and is the ONLY reason a restart is unattended. Restart the
   PROCESS; do not reset the PREFIX.
   ⚠️ Pick a quiet window: the terminal is shared, and a recycle interrupts anything using it.

**HOW TO REPORT BACK** — paste into this session:
  · the exact process names and launch command lines (paths redacted);
  · the relaunch command that WORKED, verbatim — this becomes the committed constant in task 3;
  · the measured `started` → `authorized` timings;
  · whether any human input was required;
  · anything that went wrong.

⚠️ **IF THE SPIKE DISPROVES A1** — the channel cannot reach the process tree, the relaunch needs a
display/session the channel does not have, or the terminal comes back demanding a credential —
⛔ **STOP THE PHASE HERE AND REPLAN. Do not hand-patch around it.** Plan 05 is built on this verb
existing; a patched-around spike silently breaks it. Say so plainly and hand back.
  </instructions>
  <verification>The session carries the process names, the verbatim relaunch command that worked, the measured started→authorized timings and whether any human input was required — or an explicit `A1 DISPROVED`. Afterwards, the analytics-service session monitor's next tick answering cleanly is the independent confirmation that the terminal came back; task 3 then encodes the proven command as a committed constant and its contract gates run in CI.</verification>
  <resume-signal>Reply with the transcript, or with `A1 DISPROVED` and what failed.</resume-signal>
  <done>A1 is proven with a verbatim working relaunch command and measured unattended timings, or A1 is disproved and the phase is handed back for replanning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The narrow recycle verb — one committed, non-interpolated constant, one fenced method</name>
  <files>analytics-service/services/mt5_client.py, analytics-service/tests/test_mt5_client_contract.py, deploy/mt5-gateway/railway-gateway.md</files>
  <precondition>Task 2 reported a verbatim relaunch command that worked and confirmed the auto-login returned with no human input.</precondition>
  <read_first>
    - `analytics-service/services/mt5_client.py` — read `_REMOTE_MATERIALIZE_FN` / `_REMOTE_MATERIALIZE_SRC` and the `_materialize_rows` method that executes them. ⭐ This pair is the exact shape to copy: a module-level committed source constant that cannot import from this module, executed via `conn.execute(...)` with the result read back through `_guarded_read(..., stage=...)`.
    - The same file's `_assert_live` and `_timed`, and the comment stating every session touch puts the `_assert_live` fence AHEAD of the `_timed` bracket.
    - ⛔ The same file's existing `restart` method — read it before naming anything. It means "replace the rpyc CONNECTION and dispose the stale transport". The new verb MUST NOT reuse that name; a reader who conflates the two will believe the terminal is being recycled when only the socket is.
    - The same file's `assert_session_authorized` docstring — it is the credential-free detector and states why a bare `initialize()` is the one probe that separates faults whose remedies are opposite. This is D-06's decision half; the new verb is only the action half.
    - `analytics-service/tests/test_mt5_client_contract.py` — find the existing contract-gate style (the credential-redaction gates and the abandonment-fence gates) and extend it; do not start a second convention.
  </read_first>
  <behavior>
    - A recycle issued on a live session executes the committed remote source and returns a structured verdict; nothing about the call carries a login, a password or a broker server.
    - A recycle issued on a session the lease has already abandoned is REFUSED by the `_assert_live` fence before any remote crossing happens.
    - When the rpyc transport seam is unreachable, the verb raises the module's typed error — it never returns a success-shaped verdict it did not measure.
    - The committed remote-source constant contains no interpolation marker, so nothing assembled at run time can reach the container.
    - A remote traceback from the recycle is scrubbed on the way out, exactly as every other remote crossing in this file is.
  </behavior>
  <action>
Add the recycle capability to `Mt5Client`, in the shape this file already ships for remote
execution, and nothing wider.

**The constant pair.** Mirror `_REMOTE_MATERIALIZE_FN` / `_REMOTE_MATERIALIZE_SRC`: a module-level
name the remote function is bound to, and a module-level source string defining it. The body is
the relaunch sequence task 2 proved, transcribed verbatim from the spike transcript. Write the
same warnings the materializer carries: it is a STRING executed in a REMOTE interpreter, it cannot
import from this module, it cannot be type-checked, and it must stay self-contained. It returns a
by-value JSON string, never a proxy. ⛔ The constant is written out in full in the source; nothing
about it is assembled from a variable at run time.

**The method.** ONE new public method on `Mt5Client`. ⛔ Name it for what it does to the TERMINAL
PROCESS — the name `restart` is taken and means the rpyc reconnect. Its shape follows every other
remote crossing in the file, in this order: the `_assert_live` fence as the FIRST executable
statement with its own stage name; then the transport-seam lookup with the same fail-loud posture
`_materialize_rows` uses when the seam is absent; then `conn.execute(<the constant>)` and the
namespace call, wrapped by `_guarded_read(..., stage=...)` so the timing event is emitted and a
remote traceback is scrubbed.

⛔ **THE VERB TAKES NO CREDENTIAL AND MUST NOT BE ABLE TO.** Its signature carries no login, no
password and no server. It relaunches a process; the terminal's own saved login in the persistent
Wine prefix is what re-authorises, and that is exactly the property D-07 protects. A signature
that could accept a credential would invite a future caller to pass one into a remote-executed
source string on a public repo.

⛔ **IT MUST NOT TOUCH THE PREFIX OR THE VOLUME.** The remote source relaunches a process. It does
not delete, move, reset or rebuild anything under the persistent config directory. Write that
prohibition into the constant's own comment, with the reason: the saved login is what makes the
2.0 s unattended recovery possible, and restoring it after a wipe requires a human at the VNC
console — precisely the 1h39m manual step this phase exists to remove.

**The tests**, in `test_mt5_client_contract.py`, following that file's existing gate style:
  · the committed source constant carries no interpolation marker — the same property the prober's
    own no-interpolation gate asserts one layer over, applied here;
  · the verb's signature accepts no credential parameter, asserted structurally (inspect the
    signature) rather than by reading the body;
  · the `_assert_live` fence refuses a recycle on an abandoned session BEFORE any remote crossing
    is attempted;
  · an absent transport seam raises the typed error rather than returning a success-shaped verdict;
  · a remote traceback is scrubbed on the way out.

⛔ **EVERY ASSERTION ADDED HERE MUST BE PROVEN ABLE TO FAIL.** For each: `cp` a byte backup of the
file, neuter the production behaviour the assertion targets, run the test, OBSERVE IT RED, then
restore from the backup and verify with `cmp`. ⛔ Never `git checkout -- <path>`, never
`git restore`, never `git stash` — this repo has a dated record of `git checkout` silently
destroying uncommitted work in exactly this neuter/restore shape. ⚠️ RE-TAKE the byte backup after
every edit; a stale backup makes `cmp` pass on a corrupt restore. Record the RED observation in
the SUMMARY.

**The decision record**, appended to `deploy/mt5-gateway/railway-gateway.md` beside its existing
HARD CONSTRAINT header: the D-05 choice and the reason it beat Options 1 and 3; the T-134-03
posture change stated plainly (an already-dialled channel now carries a new class of command, and
no new network exposure is created); the narrowing that pays for it (one verb, one committed
constant, no general execution helper); and the verb and constant named BY SYMBOL so the document
and the code cannot drift apart. ⛔ Cite by symbol, never by line number.

⛔ PUBLIC REPO: no account number, no broker server name, no credential, no machine path, no local
username in the constant, the comments, the tests or the document. Redact the spike's paths.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_client_contract.py -q</automated>
    <automated>cd analytics-service && .venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <done>`Mt5Client` carries ONE new credential-free method, named distinctly from the existing `restart`, that executes a committed non-interpolated remote-source constant through `_assert_live` + `_guarded_read`. `test_mt5_client_contract.py` gates the no-interpolation, no-credential, fence-refusal, absent-seam and scrub properties, and each new assertion was observed RED under a neuter before being restored via a re-taken byte backup verified with `cmp`. `railway-gateway.md` records the D-05 decision and the T-134-03 posture change. Both verify commands pass with a non-zero collected count (⛔ `0 collected` is a loud failure, not a pass).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analytics-service → rpyc bridge `:8001` | an unauthenticated arbitrary-remote-code channel (T-134-03), private WireGuard mesh only, never publicly exposed |
| the committed remote-source constant → a remote Python interpreter inside the gateway | code that leaves this repo and runs with the container's privileges |
| the gateway's persistent volume (Wine prefix + saved login) | the single artifact that makes unattended recovery possible; D-07 is one-way |
| this repo → the public internet | `.planning/`, `docs/` and `deploy/` are tracked in a PUBLIC repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.5-05 | Elevation of Privilege | the new recycle verb over the rpyc classic channel | critical | mitigate | ONE narrow method, no general execution helper, no `__getattr__` passthrough; the payload is a COMMITTED module-level source constant asserted to carry no interpolation marker; the signature structurally cannot accept a credential. Ratified at the task-1 decision gate and routed to `/gsd-secure-phase` |
| T-164.6.5-06 | Tampering | run-time assembly of the remote source | high | mitigate | The no-interpolation contract gate in `test_mt5_client_contract.py`, mirroring the prober arm's shipped gate on its own probe body |
| T-164.6.5-07 | Denial of Service | a recycle firing repeatedly and holding the shared terminal down | high | mitigate | The verb itself is dumb and callable once per call site; the debounce that makes it fire at most once per episode is plan 05's contract and is tested there. This plan's verb is never wired to a loop |
| T-164.6.5-08 | Information Disclosure | a remote traceback carrying the interpolated source line to a public Actions log or Sentry | high | mitigate | `_guarded_read` is the shipped scrub-and-time bracket for exactly this and the verb goes through it; a contract gate asserts the scrub |
| T-164.6.5-09 | Tampering | a remedy that resets or rebuilds the Wine prefix, destroying the saved login | critical | mitigate | D-07 is one-way. The constant relaunches a process only; the prohibition and its reason are written into the constant's own comment; the task-2 spike is instructed never to touch the volume |
| T-164.6.5-10 | Information Disclosure | the spike transcript carrying machine paths or account identifiers into a tracked file | medium | mitigate | Task 2 instructs redaction before reporting; task 3 repeats the prohibition for the constant, comments, tests and document |
| T-164.6.5-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. RESEARCH.md records the phase introduces no new external dependency, and `rpyc` / `mt5linux` pins are explicitly not to be touched |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_client_contract.py -q` passes with a non-zero collected count.
- `cd analytics-service && .venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/` passes.
- ⚠️ pytest and mypy run ONLY from `analytics-service/` via that directory's own `.venv/bin/python`; a repo-root invocation reaches the wrong interpreter.
- The SUMMARY records, per new assertion, the neuter that made it RED and the `cmp`-verified restore.
</verification>

<success_criteria>
- D-05 is closed with the reason it beat the other candidates, inside the D-06/D-07 fence.
- A1 is proven live, or the phase halts for replanning.
- One credential-free, committed-source, fenced verb exists and is named distinctly from `restart`.
- The T-134-03 posture change is recorded where the constraint itself lives.
</success_criteria>

<output>
Create `.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-02-SUMMARY.md` when done.
</output>
