---
phase: 164.6.5
plan: 08
type: execute
wave: 3
depends_on: ["164.6.5-01", "164.6.5-02", "164.6.5-03", "164.6.5-04", "164.6.5-05", "164.6.5-06", "164.6.5-07"]
files_modified:
  - VERSION
  - package.json
  - CHANGELOG.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: ["164.6.5-C1", "164.6.5-C2", "164.6.5-C3", "164.6.5-C4", "164.6.5-C5", "164.6.5-C6", "164.6.5-C7"]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ONE release commit carries the version bump and the CHANGELOG entry together, so a filtered PR branch cherry-picks a coherent release (founder instruction, 2026-09-09)."
    - "EVERY commit on the branch maps to at least one CHANGELOG bullet. An unrepresented commit is not minor; it is an unshipped fact."
    - "The version files are BYTE-EQUAL four-digit strings and the version file carries NO trailing newline — a critical-regression test asserts it."
    - "The ROADMAP records the honest per-criterion outcome, including any criterion recorded OPEN, with its routed residual named."
    - "The three files this working tree deliberately carries for OTHER phases are untouched: the milestone lock, the planning state json, and the 164.5.3 verification report."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "one unified entry for this version, newest first, grouped by this repo's own section vocabulary"
      contains: "164.6.5"
    - path: ".planning/ROADMAP.md"
      provides: "the phase's per-criterion outcome and the plan list marked through"
      contains: "Phase 164.6.5"
  key_links:
    - from: "VERSION"
      to: "package.json"
      via: "byte-equal four-digit strings, asserted by the critical-regressions suite"
      pattern: "version"
    - from: "CHANGELOG.md :: the new entry"
      to: ".planning/ROADMAP.md :: ### Phase 164.6.5"
      via: "the entry names the phase, its criteria outcomes and any routed residual, so release notes and ledger agree"
      pattern: "164\\.6\\.5"
---

<objective>
Close the phase: ONE release commit, an honest ledger, and a CHANGELOG entry produced by a
mechanism rather than by attention.

Purpose: the repo's own record shows a ship that changed 672 lines, rebuilt two gates and routed a
new deferral with NO CHANGELOG entry and NO version bump, while a smaller commit the same day got
both. A step that cannot fail did not fail; it was skipped. This plan carries the mechanism that
replaces it.

⚠️ **The version gate fails any PR touching a non-planning path without moving the version.** Every
other plan in this phase touches source, so this plan is not optional.

Output: a coherent release commit and a ledger that says what actually happened, including what
did not.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The ledger — per-criterion outcomes, honestly, including what stayed open</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - Every `164.6.5-NN-SUMMARY.md` in this phase directory. The outcomes are there, including any criterion a plan recorded OPEN.
    - `.planning/ROADMAP.md` § `### Phase 164.6.5` — the six measured subjects and the pin, as booked.
    - `.planning/STATE.md` — the frontmatter shape and the `## ⭐ STATE lineage` note pointing at the separate lineage file.
  </read_first>
  <action>
Write the phase's outcome into the two ledgers, per criterion, from the SUMMARYs rather than from
intent.

For EACH of the seven criteria — root cause, supervision, the heal acting, the prober measuring, the
wizard copy, the correlation id, the settings pin — record MET or OPEN. For every OPEN one, name the
routed residual with an owner and a trigger. ⛔ Do not soften a criterion's wording to make it read
as met, and ⛔ do not record a mitigation as a diagnosis. D-03 makes an honest open criterion the
success path; a quietly-restated one is the failure this phase exists to prevent.

⛔ **Restate the scope fence in the ROADMAP entry so it survives the close:** this phase did NOT fix
the eviction — that is Phase 164.6.6. "MT5 is back" must not read as "the finding is closed": the
outage was the symptom, the shared mutable terminal is the defect. And `[MT5-VERDICT-SINK-01]`
stays deferred and named; it was not absorbed.

Update the plan list to reflect the eight plans and their state.

⚠️ **The two ledger handlers have a measured clobber history in this repo.** Before ANY handler call,
`cp` a byte backup of both files. Run the handler, then `git diff` the result and revert every
collateral change it caused — stray bullets injected into unrelated phase sections, comment lines
dropped from the frontmatter, a recomputed progress block that under-reports. ⛔ Never
`git checkout -- <path>`, never `git restore`, never `git stash`; revert by editing back from the
byte backup. ⚠️ Re-take the backup after every edit, and verify a restore with `cmp` rather than by
eyeballing a hash.

⛔ **THREE FILES IN THIS WORKING TREE BELONG TO OTHER PHASES AND ARE MODIFIED ON PURPOSE:** the
milestone lock, the planning state json, and the 164.5.3 verification report. ⛔ Do NOT stage,
commit, move, revert or check out any of them.

⛔ PUBLIC REPO: no account number, no broker server name, no credential, no machine path, no local
username in either ledger.
  </action>
  <verify>
    <automated>npm run check:planning-hygiene</automated>
    <automated>git -c core.pager=cat diff --stat -- .planning/milestone.lock .planning/state.json .planning/phases/164.5.3-mt5creds-show-the-mt5-account-number-on-the-key-card-and-add/164.5.3-VERIFICATION.md</automated>
  </verify>
  <done>Both ledgers record all seven criteria as MET or OPEN with routed residuals named for the open ones; the scope fence and the untouched deferral are restated in the ROADMAP entry; the plan list is current; any handler collateral was reverted from a byte backup and verified with `cmp`; the three other-phase files show exactly the modifications they had before this plan ran. The hygiene check passes (⚠️ the count line and the OK line are DIFFERENT lines — read the OK line, never `tail -1`).</done>
</task>

<task type="auto">
  <name>Task 2: The release commit — version bump and CHANGELOG, together, from the commit list</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <read_first>
    - `CHANGELOG.md` — the most recent entries, for this repo's own voice and its own section vocabulary. ⚠️ It is NOT the four keep-a-changelog sections; read what is actually used and pick the one that fits. Do not flatten a root-cause note into a fix.
    - `VERSION` and `package.json` — the current four-digit string, byte for byte.
    - Every `164.6.5-NN-SUMMARY.md`, for what each plan actually delivered and what it recorded open.
  </read_first>
  <action>
Produce the entry by the MECHANISM, not by recollection. Run it in this order and treat each step's
output as a checklist, not as background reading.

  1. `git log <base>..HEAD --oneline` — enumerate EVERY commit on the branch. **Count them.**
  2. `git -c core.pager=cat diff <base>...HEAD` — read the full diff. What a commit SAYS and what it
     CHANGED diverge, and this repo has a dated record of that class.
  3. Group by theme BEFORE writing a line: new capability, behaviour change, bug fix, removal,
     infrastructure and tooling and tests, refactor.
  4. Write ONE unified entry for this version, newest first, under this repo's own section
     vocabulary. If entries for earlier commits on this branch already exist, REPLACE them with the
     unified one.
  5. ⛔ **CROSS-CHECK, and treat a miss as a defect:** every commit from step 1 maps to at least one
     bullet. N commits over K themes means all K themes appear. An unrepresented commit is not
     "minor", it is an unshipped fact.
  6. ⛔ Never ask the founder to describe the changes. Infer them from the diff and the history.

**Voice, and it diverges from the generic advice on purpose.** This repo's entries are largely
infrastructure with no end-user verb, and they deliberately name phases, backlog ids and routed
deferrals so the release notes and the ledger agree. Say what changed, what it means for whoever
reads it next, and carry the known limits and anything recorded-rather-than-fixed — including any
criterion this phase left OPEN.

**The bump.** Decide it on MEANING, never generically. This phase ships a new user-facing error code
and copy, a new recovery mechanism on a shared production dependency, a behaviour change in the
periodic heal, and a corrected operator-facing cause sentence — weigh those against this repo's own
recent bump practice and choose. ⛔ Never run the package manager's version command: it rewrites the
lockfile and can normalise away the fourth digit. Edit both files by hand.
⚠️ The two version strings must be BYTE-EQUAL four-digit strings and the version file must carry NO
trailing newline — a critical-regressions test asserts it. Verify with a byte-level comparison, not
by eye.

**The commit.** The version bump and the CHANGELOG entry land in ONE release commit, so a filtered
branch cherry-picks a coherent release: those three paths are non-planning and always survive the
filter.

⛔ **NEVER WRITE A CI SKIP TOKEN INTO ANY COMMIT MESSAGE, NOT EVEN TO DENY IT.** The token is honoured
anywhere in the message — including inside a sentence explaining that you are not using one — it
suppresses every workflow for that sha, and a squash merge carries it into the default branch. Refer
to it as "the skip trailer" in prose. ⚠️ Sweep every commit-message file BEFORE committing.
⚠️ Do not quote a credential-shaped fixture literal verbatim in CHANGELOG prose: the push-scope
secret scan sees one squash commit whose diff includes the changelog, and the branch-scoped
allowlist does not cover it.
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const v=fs.readFileSync("VERSION","utf8");const p=JSON.parse(fs.readFileSync("package.json","utf8")).version;if(/\n|\r/.test(v)){console.error("VERSION carries a newline");process.exit(1)}if(!/^\d+\.\d+\.\d+\.\d+$/.test(v)){console.error("VERSION is not a four-digit string: "+JSON.stringify(v));process.exit(1)}if(v!==p){console.error("VERSION and package.json differ: "+JSON.stringify(v)+" vs "+JSON.stringify(p));process.exit(1)}const c=fs.readFileSync("CHANGELOG.md","utf8");if(!c.includes(v)){console.error("CHANGELOG has no entry for "+v);process.exit(1)}console.log("release files agree at "+v)'</automated>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts</automated>
  </verify>
  <done>Every commit on the branch is enumerated and mapped to at least one CHANGELOG bullet, with the mapping recorded in the SUMMARY; one unified entry exists for the new version in this repo's own section vocabulary, naming the phase, any routed residual and any criterion left open; the two version strings are byte-equal four-digit values with no trailing newline; the bump was chosen on meaning and the reason is recorded; the bump and the entry are in ONE commit; no commit message on the branch contains a CI skip trailer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit messages → the default branch via a squash merge | a skip trailer anywhere in any branch commit rides into the merge body and suppresses every workflow |
| CHANGELOG prose → a full-history secret scan | a credential-shaped literal quoted in prose is seen by the push-scope scan, which the branch-scoped allowlist does not cover |
| the ledger files → a PUBLIC repository | planning files are tracked and world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.5-38 | Tampering | a CI skip trailer in a commit message suppressing every gate for the sha | critical | mitigate | Task 2 forbids writing the token at all, including inside a denial, and requires sweeping every message file before committing; the board reads "clean" rather than "absent" when it fires, so absence must be counted against the head sha, never eyeballed |
| T-164.6.5-39 | Information Disclosure | a credential-shaped literal quoted in CHANGELOG prose | high | mitigate | Task 2 forbids verbatim quotation of fixture literals in the entry |
| T-164.6.5-40 | Tampering | a ledger handler clobbering unrelated phase sections or dropping frontmatter comments | high | mitigate | Task 1 requires a byte backup before any handler call, a diff review after, and reverting collateral by editing back from the backup — never by a checkout |
| T-164.6.5-41 | Tampering | staging or reverting the three working-tree files that belong to other phases | high | mitigate | Named explicitly in task 1 with an explicit prohibition, and asserted in the task's verify |
| T-164.6.5-42 | Repudiation | a phase closed with a criterion quietly restated as met | high | mitigate | Task 1 requires MET or OPEN per criterion, with a routed residual for every open one, and forbids softening the wording |
| T-164.6.5-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and must not run the package manager's version command, which would rewrite the lockfile |
</threat_model>

<verification>
- The two version strings are byte-equal four-digit values; the version file has no trailing newline.
- `npx vitest run src/__tests__/critical-regressions.test.ts` passes.
- `npm run check:planning-hygiene` passes (⚠️ read the OK line, not the count line; never `tail -1`; `git add` new files first).
- `git log <base>..HEAD --oneline` shows no commit message containing a CI skip trailer.
- The milestone lock, the planning state json and the 164.5.3 verification report carry exactly the modifications they had before this phase ran.
</verification>

<success_criteria>
- One release commit carries the bump and the entry.
- Every branch commit maps to a bullet, and the mapping is recorded.
- Both ledgers record seven criteria as MET or OPEN, with residuals named.
- No skip trailer anywhere on the branch.
</success_criteria>

<output>
Create `.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-08-SUMMARY.md` when done.
</output>
