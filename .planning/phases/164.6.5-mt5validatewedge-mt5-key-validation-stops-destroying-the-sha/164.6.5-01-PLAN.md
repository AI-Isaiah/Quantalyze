---
phase: 164.6.5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbooks/mt5-go-live.md
  - TODOS.md
autonomous: false
requirements: ["164.6.5-C1"]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The runbook carries a written, runnable procedure that separates the TWO known causes of `-10005` from evidence a human can collect at the gateway, so the next operator does not re-run the 2026-09-21 investigation from scratch (D-09, criterion 1)."
    - "D-03a — the terminal-self-update hypothesis — is recorded as CONFIRMED or REJECTED against evidence, with the evidence named. It is never left as a standing hypothesis (D-03a)."
    - "The asymmetry (a clean ~1 s switch the same day vs a switch that wrote `disconnected` and nothing after) is either explained by a NAMED mechanism that PREDICTS it, or criterion 1 is recorded EXPLICITLY OPEN with a named routed residual — and nothing in this repo restates a mitigation as a diagnosis (D-01/D-02/D-03)."
    - "`MT5-WEDGE-OBS-01` is REFINED and cross-linked, never reopened: its modal-dialog mechanism stays recorded as proven for its own cause (D-09)."
    - "No MT5 account number, broker server name, credential or machine path is written into either file."
  artifacts:
    - path: "docs/runbooks/mt5-go-live.md"
      provides: "a `-10005` differential-diagnosis section: the two causes, the evidence that separates them, and the ordered remedy that is correct under both"
      contains: "-10005"
    - path: "TODOS.md"
      provides: "the refined `MT5-WEDGE-OBS-01` entry, cross-linked to this phase, stating that `-10005` has at least two causes with different remedies"
      contains: "MT5-WEDGE-OBS-01"
  key_links:
    - from: "docs/runbooks/mt5-go-live.md :: the -10005 differential section"
      to: "scripts/prod-prober/arms/mt5.mjs :: REMEDIES['mt5-ipc-timeout']"
      via: "the runbook is the long form of the remedy string plan 03 rewrites; both name BOTH causes and neither asserts a single one"
      pattern: "mt5-ipc-timeout"
    - from: "TODOS.md :: MT5-WEDGE-OBS-01"
      to: ".planning/ROADMAP.md :: ### Phase 164.6.5"
      via: "a cross-link naming this phase as the second cause's owner"
      pattern: "164\\.6\\.5"
---

<objective>
Criterion 1 — **the root cause of the switch wedge is found and NAMED, or honestly recorded
open.**

Purpose: on 2026-09-21 an MT5 key validation tore down the shared terminal's broker session and
the terminal did not come back for **1h39m**, while a restart of the same process under the same
Wine prefix reached `authorized` in **2.0 s, unattended**. The binding fact is the ASYMMETRY: a
switch succeeded cleanly the same day (`04:08:06 disconnected` → `04:08:07 authorized`, ~1 s)
while the 11:02 and ~12:52 switches wrote `disconnected` and nothing after. ⛔ A candidate that
cannot say why those runs differ is NOT the root cause (D-01).

⛔ **THE EVIDENCE BAR, and it is not negotiable.** Sleeping, retrying or serialising around the
switch is a FINDING TO RECORD, never a close for this criterion (D-02). ⭐ The phase is NOT
blocked on this criterion: if it resists, ship it EXPLICITLY OPEN with a named routed residual
and ⛔ never redefine "root cause" downward to make it closeable (D-03). Recording an honest open
criterion is the success path here.

⚠️ **This plan owns `TODOS.md` and `docs/runbooks/mt5-go-live.md` for the whole phase.** No other
plan in 164.6.5 edits either file. Plan 03 rewrites the prober's remedy STRING and cites this
runbook section by symbol; it does not edit the runbook.

Output: a runbook differential-diagnosis section, a refined `MT5-WEDGE-OBS-01`, and a recorded
verdict on D-03a.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-CONTEXT.md
@.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-RESEARCH.md
@docs/runbooks/mt5-go-live.md
@deploy/mt5-gateway/railway-gateway.md
@scripts/mt5-diag.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the `-10005` differential-diagnosis section — the two causes, and what separates them</name>
  <files>docs/runbooks/mt5-go-live.md</files>
  <read_first>
    - `deploy/mt5-gateway/railway-gateway.md` — the HARD CONSTRAINT header, the digest-pin paragraph, and the sentence recording that the MetaTrader terminal binary self-updates from the broker independently of the image and cannot be frozen (this is D-03a's source).
    - `scripts/mt5-diag.sh` — the only in-repo gateway diagnostic, and its warning that `Config/terminal.ini` is only rewritten on a CLEAN terminal exit.
    - `analytics-service/services/mt5_validation.py` — `classify_mt5_login_error` and `_IPC_TRANSPORT_CODES`; read the comment block above the code gate, which already records the 2026-08-12 wedged-gateway incident.
    - `analytics-service/routers/exchange.py` — `_validate_mt5_key_probe`, and in particular the fact that a validate calls the SAME verb the session monitor calls but against a DIFFERENT account.
    - `docs/runbooks/mt5-go-live.md` — find the existing step structure first and append a new section in the file's own voice; do not restructure the runbook.
  </read_first>
  <action>
Add ONE new section to the runbook whose subject is: an operator is looking at a terminal that
answers `-10005` and must decide which of the two known causes they have.

Write it from the repo's own measured record, not from recollection. Three things must be in it.

FIRST — the two causes, named and separated. Cause A is the modal-login-dialog wedge, the one
`MT5-WEDGE-OBS-01` records: it is PERSISTED state, it lives with the Wine prefix on the named
volume, and a redeploy brings it straight back. Cause B is the account-switch wedge measured
2026-09-21: it is PROCESS state, there was no dialog (the live VNC console was clean and the
Alerts tab empty), the UI answered a human clicking its own Login dialog three ways while the
Journal wrote nothing, and a process restart under the same prefix reached `authorized` in 2.0 s
unattended. State plainly that the two have OPPOSITE remedies and that the shipped prober remedy
string described only cause A until this phase.

SECOND — the evidence that separates them, as a numbered procedure an operator can run at the
gateway. It must reach for evidence, not for a guess: the terminal's own Journal tab (a login
attempt writes a line EVEN WHEN IT FAILS, so silence after a `disconnected` is itself the
reading), the Alerts tab, whether the VNC console shows a modal window at all, and the terminal's
reported build number. Name `scripts/mt5-diag.sh` as the read-only diagnostic and carry its
warning forward: do not read `Config/terminal.ini` for live state, because MT5 only rewrites it on
a clean exit. Cite every file BY SYMBOL, never by line number.

THIRD — the ordered remedy that is correct under BOTH causes, and say why the order is safe: try
the PROCESS restart first (cheap, unattended, measured at 2.0 s, and it cannot make cause A
worse); if `-10005` returns, then open the VNC console and clear the modal dialog, which is the
step a restart cannot do for you.

Also record, as a short subsection, the CANDIDATE MECHANISMS for cause B and the evidence that
would decide between them, so task 2's capture has a target list rather than a fishing licence.
Derive the candidates from what the repo actually establishes, at minimum: (a) the terminal binary
self-updated between the clean switch and the wedged one, per the self-update sentence in
`railway-gateway.md` — the hypothesis CONTEXT.md D-03a asks to be confirmed or rejected; (b) the
clean `04:08` event was a re-auth to the SAME account while the wedged events were switches to a
DIFFERENT account, which the monitor-vs-validate call shapes in `mt5_relogin.py` and
`routers/exchange.py` make mechanically possible; (c) any further candidate the read establishes.
For each, write the ONE observation that would confirm it and the ONE that would reject it.

⛔ PUBLIC REPO. No MT5 account number, no broker server name, no credential, no connection string,
no absolute machine path, no local username anywhere in this section. Refer to accounts as "the
house account" and "a client account". Timestamps, durations and MT5 error codes are safe and
should be kept — they are the evidence.
  </action>
  <verify>
    <automated>node -e 'const s=require("fs").readFileSync("docs/runbooks/mt5-go-live.md","utf8"); const bad=[/\b[0-9]{6,}\b/,/\/(?:Users|home)\/[a-z]/i,/[A-Za-z]:\\\\/]; const hits=bad.filter(r=>r.test(s)); if(hits.length){console.error("hygiene: runbook carries an account-shaped run or a machine path");process.exit(1)} if(!s.includes("-10005")){console.error("the new section did not land");process.exit(1)} console.log("runbook OK")'</automated>
    <human-check>The section names BOTH causes, gives a numbered evidence procedure, and its remedy ordering puts the process restart first with the reason it is safe under both causes.</human-check>
  </verify>
  <done>`docs/runbooks/mt5-go-live.md` carries a `-10005` differential-diagnosis section with the two causes, a numbered evidence procedure, an ordered remedy safe under both, and a candidate-mechanism list with a confirming and a rejecting observation for each. The hygiene check above passes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: FOUNDER — capture the terminal-side evidence the repo cannot reach</name>
  <files>none (evidence capture only)</files>
  <precondition>The gateway service is reachable from the founder's Railway account and its VNC console opens.</precondition>
  <action>Capture the terminal-side evidence criterion 1 needs, following the procedure task 1 committed to the runbook.</action>
  <instructions>
⛔ STOP. This task touches the live `mt5-gateway` and is a FOUNDER ACT. No agent runs any of it.
⛔ No agent enters, reads, decrypts, logs or echoes an MT5 credential at any point.

The repo cannot decide criterion 1 on its own: the deciding evidence is inside the terminal's own
Journal and its build number, and neither is in this repository. Task 1 committed the procedure;
this task runs it.

WHAT TO CAPTURE — the list task 1 wrote into `docs/runbooks/mt5-go-live.md`, plus these three:

1. The Journal lines bracketing the CLEAN switch on 2026-09-21 (~04:08:06 → 04:08:07) and the
   WEDGED one (~11:02:38 onward). For each, note only whether the event was a re-auth to the HOUSE
   account or a switch to a CLIENT account — ⛔ write "house" or "client", NEVER the number.
2. The terminal's reported build number as it stands now, and — if the Journal or the terminal's
   own update log records one — whether a terminal self-update landed between 04:08 and 11:02 on
   that day. This is the observation that confirms or rejects D-03a.
3. Whether the Alerts tab was empty and whether any modal window was present during the wedge.

HOW TO REPORT BACK — paste a plain-text transcript into this session with:
  · timestamps, durations and MT5 error codes VERBATIM (these are safe);
  · every account number replaced by `house` or `client`;
  · every broker server name replaced by `<server>`;
  · any file path outside the container replaced by `<path>`.

⚠️ If the evidence is unavailable — the Journal has rolled, the build history is gone, the console
will not open — SAY SO PLAINLY. That answer is acceptable and it is what D-03 exists for. Do not
reconstruct it from memory: a remembered Journal line is not evidence, and criterion 1 recorded
OPEN is a better outcome than criterion 1 closed on a story.

  </instructions>
  <verification>A transcript — or an explicit "evidence unavailable" — is in the session, and it carries no account number, no broker server name and no external machine path. Task 3 re-checks the same properties automatically once the evidence is written into a tracked file.</verification>
  <resume-signal>Reply with the transcript, or with the words "evidence unavailable" and which parts are missing.</resume-signal>
  <done>The founder has reported the captured evidence or explicitly reported it unavailable, and the report is free of identifiers.</done>
</task>

<task type="auto">
  <name>Task 3: Record the verdict — named, or explicitly open — and refine MT5-WEDGE-OBS-01</name>
  <files>docs/runbooks/mt5-go-live.md, TODOS.md</files>
  <read_first>
    - The founder's transcript from task 2, in this session.
    - `TODOS.md` — read the whole `MT5-WEDGE-OBS-01` entry, including its status line and its dated history, before touching it. ⚠️ `TODOS.md` is large; locate the entry with `grep -n -a 'MT5-WEDGE-OBS-01' TODOS.md` and read only the surrounding range.
    - `.planning/ROADMAP.md` § `### Phase 164.6.5` — the six measured subjects, so the verdict is written against criterion 1 as booked.
  </read_first>
  <action>
Turn the task-2 evidence into a recorded verdict. There are exactly two acceptable shapes and the
choice is decided by the evidence, not by preference.

SHAPE ONE — the mechanism is NAMED. Write it into the runbook's candidate-mechanism subsection as
the confirmed cause, state the observation that confirmed it, and state HOW IT PREDICTS THE
ASYMMETRY — why the ~1 s clean switch and the wedged switch differ under this mechanism. ⛔ If it
cannot predict the asymmetry, it is not the root cause and shape two applies instead (D-01).

SHAPE TWO — the mechanism RESISTED. Write criterion 1 into the runbook as EXPLICITLY OPEN. Name
what is still unknown, name the evidence that would close it, and record a routed residual with an
owner and a trigger in `TODOS.md` under the existing backlog conventions (this repo's ONE ground
truth for backlog items is root `TODOS.md`). ⛔ Do not soften the criterion's wording to make it
look met, and ⛔ do not record a mitigation as a diagnosis.

In BOTH shapes, record the D-03a verdict explicitly, in one sentence: the terminal-self-update
hypothesis is CONFIRMED by <observation>, or REJECTED by <observation>, or UNDECIDED because
<the evidence that is missing>. It must not survive this plan as a standing hypothesis.

Then REFINE `MT5-WEDGE-OBS-01` in `TODOS.md`. ⛔ Do NOT reopen it — its modal-dialog mechanism is
proven and still correct for its own cause. Add to it, in the entry's own voice: that `-10005` has
at least TWO causes with different remedies; that the second was measured 2026-09-21; that the
entry's own claim "a redeploy cannot fix this because the Wine prefix lives on the persistent
volume" is TRUE for its cause and FALSE for the second; and a cross-link to Phase 164.6.5 as the
second cause's owner. Keep the entry's id — it is cited by name elsewhere and retiring it would
orphan those citations.

⛔ PUBLIC REPO, same rule as task 1: verdicts and counts, never specimens. No account number, no
broker server name, no machine path. ⛔ A proof-of-absence must not publish what it proves absent —
record that the Alerts tab was empty, never a quoted specimen of what would have been in it.
  </action>
  <verify>
    <automated>npm run check:planning-hygiene</automated>
    <automated>node -e 'const fs=require("fs");const t=fs.readFileSync("TODOS.md","utf8");const r=fs.readFileSync("docs/runbooks/mt5-go-live.md","utf8");if(!t.includes("MT5-WEDGE-OBS-01")){console.error("the entry id was retired — it is cited by name elsewhere");process.exit(1)}if(!t.includes("164.6.5")){console.error("MT5-WEDGE-OBS-01 was not cross-linked to this phase");process.exit(1)}if(!/D-03a/.test(r)){console.error("the D-03a verdict was not recorded in the runbook");process.exit(1)}for(const re of [/\b[0-9]{6,}\b/,/\/(?:Users|home)\/[a-z]/i]){if(re.test(r)){console.error("hygiene: runbook carries an account-shaped run or a machine path");process.exit(1)}}console.log("verdict recorded")'</automated>
  </verify>
  <done>The runbook records either a NAMED mechanism that predicts the asymmetry, or criterion 1 explicitly open with a named routed residual; the D-03a verdict is recorded as confirmed, rejected or undecided-with-the-missing-evidence; `MT5-WEDGE-OBS-01` keeps its id, is refined rather than reopened, and cross-links this phase. `npm run check:planning-hygiene` passes (⚠️ read the OK line, not the count line — they are different lines; never `tail -1`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live MT5 terminal → a human's transcript → a PUBLIC git repository | the evidence carries MT5 account numbers and broker server names by nature; this plan's whole output is that evidence, transcribed |
| `TODOS.md` / runbook → world-readable on push | `.planning/` and `docs/` are tracked in a public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.5-01 | Information Disclosure | task 2's transcript → `docs/runbooks/mt5-go-live.md` | high | mitigate | The checkpoint instructs the founder to substitute `house`/`client`/`<server>`/`<path>` before pasting; tasks 1 and 3 carry an automated account-shaped-digit-run and machine-path scan; `npm run check:planning-hygiene` runs at task 3 |
| T-164.6.5-02 | Information Disclosure | a proof-of-absence that publishes what it proves absent | medium | mitigate | Task 3 records VERDICTS and COUNTS only — "the Alerts tab was empty" — and never a specimen or the pattern searched for |
| T-164.6.5-03 | Repudiation | recording a mitigation as a diagnosis, closing criterion 1 falsely | high | mitigate | Task 3's two acceptable shapes are enumerated and the choice is bound to the evidence; the asymmetry-prediction test (D-01) is the gate on shape one |
| T-164.6.5-04 | Tampering | editing the live gateway during evidence capture | medium | mitigate | Task 2 is read-only by instruction and is a founder act; no agent touches the gateway; `scripts/mt5-diag.sh` is the named read-only diagnostic |
| T-164.6.5-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. RESEARCH.md records the phase introduces no new external dependency, so the Package Legitimacy Gate has nothing to audit here |
</threat_model>

<verification>
- `npm run check:planning-hygiene` passes (⚠️ the count line and the OK line are DIFFERENT lines — read the OK line, never `tail -1`; `git add` any new file first).
- `grep -n -a 'MT5-WEDGE-OBS-01' TODOS.md` still returns the entry — it was refined, not retired (⚠️ `grep -a`: this repo has NUL-bearing text files that `grep` is silently blind to; ⚠️ `grep -c` exits 1 on a zero count).
- No account number, broker server name, credential, connection string, machine path or local username appears in either modified file.
</verification>

<success_criteria>
- Criterion 1 is recorded as either NAMED-with-a-mechanism-that-predicts-the-asymmetry or EXPLICITLY OPEN with a named routed residual.
- D-03a is confirmed, rejected or recorded undecided with the missing evidence named.
- `MT5-WEDGE-OBS-01` is refined and cross-linked, never reopened, and keeps its id.
- The runbook carries a `-10005` differential procedure the next operator can run.
</success_criteria>

<output>
Create `.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-01-SUMMARY.md` when done.
</output>
