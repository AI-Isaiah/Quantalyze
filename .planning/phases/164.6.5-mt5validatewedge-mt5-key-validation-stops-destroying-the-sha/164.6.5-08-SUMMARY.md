---
phase: 164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha
plan: 08
subsystem: release
tags: [release, changelog, ledger, mt5, prod-prober]

requires:
  - phase: 164.6.5
    provides: plans 01-07, VERIFICATION.md (gaps_found, 5/7), SECURITY.md (5 open threats, all plan 08)
provides:
  - "v0.94.0.0 release commit: VERSION, package.json and ONE unified CHANGELOG entry"
  - "Per-criterion outcome table in the ROADMAP (MET or OPEN, with a routed residual for each OPEN)"
  - "D-11 booked OPEN as TODOS.md MT5-PROBER-WEDGE-CALIBRATION-01, owned by Phase 164.6.6"
  - "Plan 03 SUMMARY records the D-10 production evidence and flips halted to complete; C4 not met"
  - "WINDOWS.md entry 68 names the first live Mt5Client.session_snapshot read beside the terminate step"
affects: [164.6.6 (now owns D-11), /gsd-secure-phase re-audit, /gsd-ship]

actuals:
  tokens: 7327     # chars/4 over the added lines of the two authored commits (196dea9a1, feaf79d95); the merge's conflict resolution is not separable from main's content and is excluded
  tasks: 2
  commits: 18      # MEASURED: git rev-list --count 96ba93be7..HEAD before this SUMMARY's commit. 15 of the 18 are origin/main's commits, brought in by the merge ef9d43681; 3 are this plan's (first-parent)
plan_head_before: 96ba93be78610301b115af9c8ba7ff5476b7ad0c

tech-stack:
  added: []
  patterns:
    - "CHANGELOG by script cross-check: every branch commit's short sha must appear in the entry (89/89)"

key-files:
  created:
    - .planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-08-SUMMARY.md
  modified:
    - VERSION
    - package.json
    - CHANGELOG.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/WINDOWS.md
    - TODOS.md
    - .planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-03-SUMMARY.md
    - src/lib/wizardErrors.test.ts
    - src/__tests__/prod-prober-wiring.test.ts
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx

key-decisions:
  - "Version 0.94.0.0: a minor bump over origin/main's 0.93.0.0. The phase ships a new user-facing wizard code and copy, moves an MT5 validate IPC fault from a retryable 424 to a non-retryable 500, adds a production loop that can end the shared terminal process, and changes X-Correlation-Id to one value per request. JOBRPCTRUTH, GATEHYGIENE and KEYCARDSYNC set the precedent."
  - "origin/main was merged into the branch BEFORE the bump, so the PR carries no VERSION or CHANGELOG conflict. The four conflicts were resolved by keeping both sides."
  - "Plan 03 status moved from halted to complete through task 3's own step 6: D-10 answered, D-11 recorded OPEN and routed. Its requirements-completed was corrected from [164.6.5-C4] to [], because C4 is not met."
  - "D-11 is owned by Phase 164.6.6, beside MT5-SWITCH-WEDGE-CAUSE-01, because both close on the same next-wedge capture."

requirements-completed: ["164.6.5-C3", "164.6.5-C5", "164.6.5-C6", "164.6.5-C7"]
requirements-open: ["164.6.5-C1", "164.6.5-C2", "164.6.5-C4"]

duration: ~40 min
completed: 2026-09-26
status: complete
---

# Phase 164.6.5 Plan 08: Release and honest ledger close Summary

**v0.94.0.0 ships as ONE release commit. Its CHANGELOG entry maps all 89 branch commits by sha. The ROADMAP records all seven criteria plus the two inherited ones as MET or OPEN. D-11 is booked by name to Phase 164.6.6, and C4 is not claimed as met.**

## Task Commits

0. **Merge origin/main (0.93.0.0) into the branch** — `ef9d43681` (merge; four conflicts, both sides kept)
1. **Task 1: the ledger** — `196dea9a1` (docs)
2. **Task 2: the release commit** — `feaf79d95` (chore(release))

## What was done

### Merge first (instruction: avoid a VERSION or CHANGELOG conflict)

`origin/main` was at 0.93.0.0, 15 commits ahead. A `git merge-tree` dry run showed four conflicts.
Each was resolved by keeping both sides:

- `SyncPreviewStep.tsx`: main's `probeExistingChain` guard runs first. Then the kickoff POST keeps
  164.6.5-07's `onCorrelationId` capture. The retry handler resets main's in-flight evidence AND the
  request correlation id.
- `wizardErrors.test.ts` (a NUL-bearing file, edited byte-safe in Python): both "95 → 96" notes are
  kept as lineage, and a merge note is added. `EXPECTED_TABLE_SIZE` moves 96 → 97 at both pins. The
  value was READ OFF the guard's own failure ("expected 97 to be 96"), and the NUL count stayed 1.
- `prod-prober-wiring.test.ts`: both imports, and both describe blocks.
- `.planning/WINDOWS.md`: main's fixed row 66, plus the branch's rows 67 and 68. The header is
  recounted to open 50, fixed 18, total 68. The embedded JSON block agrees (`windows status`).

After the merge: `tsc --noEmit` was clean. The wizard steps and prober wiring suites passed (25
files, 701 tests), and so did `wizardErrors.test.ts` (252).

⚠️ The merge commit carries main's version of
`.planning/phases/164.5.3-*/164.5.3-VERIFICATION.md`, because main changed that file. This plan did
not edit, stage or revert it, and the PR diff against main shows nothing for it. This worktree had
no local modifications to any of the three other-phase files.

### Task 1 — the ledger

The per-criterion outcome is taken from the SUMMARYs and the VERIFICATION, not from intent:

| req | outcome | residual |
|---|---|---|
| C1 root cause | OPEN (D-03 path) | `MT5-SWITCH-WEDGE-CAUSE-01`, Phase 164.6.6 |
| C2 supervision / restart | OPEN, live half | WINDOWS 68, founder, first live recycle |
| C3 heal acts | MET | none |
| C4 prober measures + real-wedge calibration | OPEN, calibration half | `MT5-PROBER-WEDGE-CALIBRATION-01` (D-11), Phase 164.6.6 |
| C5 wizard copy | MET | D-16/D-17 founder confirmation noted |
| C6 per-request id | MET | none |
| C7 landmine pinned | MET | none |
| inherited 7 (161) / 8 (164.5.3) | OPEN | founder UAT |

**D-10 evidence was re-read by this plan, read-only** (`gh run view <id> --log`, mt5 verdict line
only, digit runs redacted in the terminal, nothing copied):

- 36104820751 (2026-09-25T06:52Z, `bb31564f`): `mt5-ssh-transport`.
- 36134914962 (2026-09-25T12:26Z, `01dcf1cc`): `mt5: initialize=true last_error=1 connected=true trade_allowed=true`.
- 36212376262 (2026-09-26T02:39Z, `ecf1ef4e`, the latest completed run when read): the same line.

⚠️ A naive grep of these logs over-counts both kinds: the prober's self-test prints fixture and
remedy text naming them. Only lines after `SELF-TEST PASSED` are the live verdict.

The following were also updated: the scope fence and `[MT5-VERDICT-SINK-01]` restated, plans 01-08
ticked, 8/8, Phase 164.6.6's "Owns" line for D-11, a TODOS entry with owner, trigger and gate, WINDOWS
68 widened (table row AND JSON description), and one STATE roadmap-evolution line plus a metrics row.

### Task 2 — the release

Every step of the mechanism was run:

1. `git log origin/main..HEAD` gave **89 commits** (83 plus 6 merges).
2. The source diff was read (47 non-planning files, +8882/−501).
3. The commits were grouped into Added, Changed, Fixed (two review rounds), Tests and Notes.
4. ONE entry was written.
5. A script cross-check confirmed that every commit's 9-char sha appears in the entry: `commits=89 missing=0`.

## Commit → CHANGELOG mapping (cross-check, by script)

All 89 short shas appear in the entry. The groups are:

- **Added:** b0a537b98, be67187cd, 7037055cf, 87123dbfc, 3222bf83c, 81af07dce, de4c0720d, d21696fe4, 34a1750be, fec4397e4, ee224d576.
- **Changed:** 3cb4f0624, 93fab8e3f, 7a7a310af, c6fb7d23a, 40e7606fd, 7fb575d08, 6406087ba.
- **Fixed, round 1:** 45aa1d5d8, b571e727b, 6a0823d6f, 85cebe5d5, 49d26446b, fc77d3e8a, f62a5ebec, 61d4350c0, 91640498c, 0b591e9a1, b7adc2cd4, 8393aa73b, dad768a48, 0663229a4, 3c0139e0e, 1a3541814, 8a7c35149, 23f44cc53, 35f80c0a0, 72bcea3d7.
- **Fixed, round 2:** e42d1f551, 7cc7e4eb9, 6beb13ff6, 9abd42fa7, dd2ceb0e3, 68ceee3cc, 3d02f846e, 7662d7d8b, c23221cde, a53275f0e, 9b4041dc8, 3a8212c2f, 9b1ea9825.
- **Tests:** 38c1ad007, 93a2cd782, 3b313a94e, a50569376, 16fb1c655, 2c347f7f4, ef9d43681.
- **Notes:** 2d176b924, 754a02152, 4e22f73f2, 337f2827f, 42b296b8f, 8288ad458, 3925a55e5, 65b7b986f, 47bb479d5, 64c318cdd, d671e1eb9, 22e2fbb00, 031891432, 2e2033dad, 20422b71f, 29e010f1d, 71a8b187f, 6c515b5b0, a91675aa8, e50da1dd3, 6e4a9aab3, 7400392f8, 0ecf94557, 716b994c2, ebfe94dab, 35e63761e, 4c0c10feb, 38647f8b5, 96ba93be7, 196dea9a1, 2fa71d3e2.

## Gates (verbatim)

- Release files: `release files agree at 0.94.0.0`
- `npx vitest run src/__tests__/critical-regressions.test.ts`: `Test Files  1 passed (1)` / `Tests  166 passed (166)`
- Phase-touched vitest suites: `Test Files  16 passed (16)` / `Tests  935 passed (935)`
- `node scripts/prod-prober/run.mjs --self-test`: `=== SELF-TEST PASSED: 91/91 scenarios, every arm's kinds fired on their own fixtures and nowhere else ===`
- MT5 pytest (from `analytics-service/`, main checkout's .venv): `709 passed, 1 skipped, 1 warning in 9.78s`
- Planning hygiene (after each `git add`): `[check-planning-hygiene] OK — 7135 tracked files scanned, none carry the local username or an absolute home path`
- `gitleaks git . --config .gitleaks.toml --log-opts=origin/main..HEAD`: `no leaks found`
- Skip-trailer sweep: each message file printed `skip-sweep=0`, and the branch-wide sweep of every commit body over `origin/main..HEAD` found none.

## Security threats T-38..T-42 (closed by construction; re-audit owed)

- **T-38:** no skip trailer in any message file or in any branch commit body.
- **T-39:** no fixture literal is quoted in the entry, and the range gitleaks scan is clean.
- **T-40:** no gsd-tools state handler was run. ROADMAP and STATE were hand-edited after a `cp` byte backup, and each `git diff` was reviewed. There was no collateral to revert.
- **T-41:** `.planning/milestone.lock`, `.planning/state.json` and the 164.5.3 VERIFICATION were never staged by this plan.
- **T-42:** C4 is recorded OPEN, and plan 03's false `requirements-completed: [C4]` was corrected.

`/gsd-secure-phase 164.6.5` must re-run to flip SECURITY.md.

## Deviations from Plan

1. **[Rule 3 - Blocking] Merged origin/main before the bump** (orchestrator instruction). This
   required conflict resolution in three source/test files and in WINDOWS.md. The pin move 96 → 97
   was read off the failing guard. Commit `ef9d43681`.
2. **[Orchestrator scope] Additional C4 ledger work.** Plan 03's SUMMARY was corrected, D-11 was
   booked in TODOS and the ROADMAP, and WINDOWS 68 was widened. None of this was in the written plan.
3. **The plan's `npm run check:planning-hygiene`** was run as the tsx invocation the orchestrator
   specified (`env -u TMPDIR .../tsx scripts/check-planning-hygiene.ts`).
4. **The worktree allow-list in the executor protocol** (`agent-*` branches) does not match this
   branch. The orchestrator set it (`feat/164.6.5-mt5validatewedge`), and it is not a protected
   branch, so the commits went there.

## Known Stubs

None.

## Next

- `/gsd-secure-phase 164.6.5` (re-audit T-38..T-42), then a verifier re-run. C4 stays a gap by
  design until D-11 closes, so expect `gaps_found` to remain, now with the residual routed.
- Do NOT transition the phase to complete (orchestrator instruction). Not pushed.

## Self-Check: PASSED

- FOUND: VERSION (`0.94.0.0`, no newline), package.json, CHANGELOG.md entry `## [0.94.0.0]`
- FOUND: commits ef9d43681, 196dea9a1, feaf79d95 (`git log --oneline`)
- FOUND: `MT5-PROBER-WEDGE-CALIBRATION-01` in TODOS.md and ROADMAP.md
