---
phase: 164.6.5-mt5validatewedge
fixed_at: 2026-09-25T20:30:00Z
review_path: .planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-REVIEW.md
iteration: 1
topic_b:
  findings_in_scope: 7
  fixed: 7
  skipped: 0
  status: all_fixed
topic_a:
  findings_in_scope: 15
  fixed: 15
  skipped: 0
  status: all_fixed
  requires_human_verification: [CR-01, WR-01, WR-02, WR-03, WR-04, WR-08, SFH-03, SFH-04, SFH-05, SFH-09]
---

# Phase 164.6.5: Code Review Fix Report (round 1)

**Source reviews:** `164.6.5-REVIEW.md` (round 1) and `164.6.5-REVIEW-SFH.md` (round 1).
**Iteration:** 1

Two parallel fixers split this round by topic. Topic A owns the escalation state machine in
`mt5_relogin.py`, `mt5_client.py`, `mt5_session_monitor.py`, `mt5_session_episodes.py` and
their tests. Topic B owns user-facing codes, copy and ops wording. Each section is written by
its own fixer. Topic A appends `## Topic A`. The frontmatter carries one block per topic.

## Topic B: user-facing codes, copy and ops wording

**Summary:**
- Findings in scope: 7. They are CR-02, WR-05, WR-06 + SFH-08, WR-07, IN-02, IN-04 and IN-08.
  The known stale account-change sentence is counted with IN-04, since the brief grouped the
  prober's operator sentence with it.
- Fixed: 7
- Skipped: 0

**Where verification ran.** Everything ran in the pinned `feat/164.6.5-mt5validatewedge`
checkout, on its own working tree. No isolated review-fix worktree was created. `node_modules`
is a symlink to the main checkout's copy. It was removed before each commit and re-linked
after. pytest, mypy and ruff ran from `analytics-service/` with the main checkout's `.venv`.
⚠️ Topic A's uncommitted edits were in the same working tree while these gates ran. The
figures below were read on that combined tree, at HEAD `c6fb7d23a` plus Topic A's in-flight
changes.

### CR-02: D-17 was never implemented, so the rotate-secret dialog showed a Retry for a wedged terminal

**Files modified:** `src/lib/wizardErrors.ts`, `src/components/strategy/UpdateMt5SecretDialog.test.tsx`, `src/lib/dialog-envelope.invariant.test.ts`
**Commit:** fc77d3e8a
**Applied fix:** `KEY_MT5_TERMINAL_UNRESPONSIVE` joins the `"keys/[id]/rotate-secret"` roster in
`DASHBOARD_DIALOG_ROUTE_CODES`, beside `KEY_SIGN_IN_FAILED`, with its reasoning. A new dialog
test feeds the code and asserts three things: the honest card renders, "Try the last action
again." (UNKNOWN's retry instruction) is absent, and no Retry button renders.
The rostered-code census in `dialog-envelope.invariant.test.ts` moved 33 -> 34. That was
measured, not assumed: it read `expected 34 to be 33` with the row added. No other census
moved, because the copy entry and the `VENUE_WIRE_CODE_TO_VERDICT` row already existed.
**Guard:** row neutered -> RED (`1 failed | 10 passed`) -> restored (cp + cmp) -> GREEN.

### WR-06 + SFH-08: a fault whose card says "tell us" paged nobody

**Files modified:** `src/lib/wizardErrors.ts`, `src/app/api/strategies/create-with-key/route.test.ts`, `src/app/api/strategies/composite/add-key/route.test.ts`, `analytics-service/routers/exchange.py`, `analytics-service/tests/test_mt5_validate.py`
**Commit:** f62a5ebec
**Applied fix:** both halves were fixed.
- `KEY_MT5_TERMINAL_UNRESPONSIVE` joins `OUR_DEFECT_KEY_ERROR_CODES`, so both key routes page
  it. The set's rule excludes "a timeout". The comment explains why this case is different:
  that exclusion is for an upstream we do not own being slow, while this is our own terminal,
  and one wedge stops every client.
- The router's `is_ipc_transport_fault` arm now logs at ERROR, on the D-15 trade-permission
  arm's reasoning. It still logs codes only.
- Tests: twin route tests assert the honest code at 500 AND a capture carrying each route's
  tags. A new pytest gate, `test_ipc_transport_fault_logs_at_error_like_the_d15_arm`, pins the
  level and the code for both IPC codes.
- The existing scrubbed-code sweep's non-vacuity check read `mock_logger.warning` and went red
  on the level change. It now reads `mock_logger.error`, with a comment saying why.

**Guards:**
- The set member was neutered -> RED on both route tests (`2 failed | 239 passed`) ->
  restored -> GREEN.
- The router level was neutered to WARNING -> RED (`3 failed`) -> restored -> GREEN.

### WR-05: the copy claimed permanence for faults the phase itself makes transient

**Files modified:** `src/lib/wizardErrors.ts`, `src/lib/wizardErrors.test.ts`, `src/components/strategy/UpdateMt5SecretDialog.test.tsx`, `analytics-service/routers/exchange.py`, `analytics-service/tests/test_mt5_validate.py`
**Commit:** 61d4350c0
**Applied fix:**
- **Card copy.** The card no longer says "it will not clear on a retry" or "nothing you do
  from this screen can clear it". It now says the fault is ours, nothing was stored, an
  immediate retry will not help, and a later attempt can succeed. It does not promise the
  heal, because a persisted modal dialog still needs an operator.
- **Draft sentence.** "Your draft is saved" is now gated with `REQUIRES_CONNECT_SURFACE`.
  CR-02 put the same code on the rotate-secret dialog, which has no draft and names no
  surface. Without the gate that dialog would have shown a false claim.
- **Router detail.** It changed from "This needs an operator, not a retry" to the same not
  now / not never wording. `retryable=False` stands, because it describes an IMMEDIATE retry
  (167 D-08).
- **Existing test.** The copy test's positive half used to be "the copy must say the draft
  is safe". It now requires "a later attempt can succeed" and bans three permanence phrases.
  A new case pins the gated draft sentence.

**Guard:** the old copy and the old detail were swapped back in. That turned 3 vitest cases
and 2 pytest cases RED. Both files were restored (cp + cmp) -> GREEN.
**Not done:** the review also suggested minting only on `-10005` and leaving `-10004` on the
transient arm. That change lives in `services/mt5_validation.py` (`is_ipc_transport_fault`),
which is outside Topic B's file list, and it would change an emitted verdict. It was not
done. The copy fix makes the current `-10004` mapping honest either way.

### WR-07: `X-Wizard-Page-Load-Id` was read by no server code

**Files modified:** `src/instrumentation.ts`, `src/instrumentation.test.ts`, `src/lib/wizard/wizard-correlation.ts`
**Commit:** 91640498c
**Applied fix:** the join is partly restored, and the docblock now tells the truth about the
rest.
- **Restored.** `onRequestError` is the one server sink that sees every request's headers.
  It now tags `wizard_page_load_id` beside the per-request `correlation_id`. The value is
  client-supplied, so it is shape-checked with the allowlist `correlation-id.ts` uses. An
  absent, empty or CR/LF-bearing value gives null and is never echoed.
- **Corrected.** The docblock now states what changed. Server logs and
  `compute_jobs.metadata.correlation_id` carry a per-request id from this release and a
  per-page-load id before it. Handled captures (`captureToSentry` in the key routes) and the
  Python seam do not carry the page-load id, and the docblock says what restoring them would
  take.

**Guard:** the shape check was neutered to return the raw value -> RED -> restored -> GREEN.
**Remaining, recorded rather than done:** a full restoration of the join, which would mean
reading the header in each wizard route and forwarding it to the analytics service. That is
a cross-cutting change touching the Python seam. The plan-07 SUMMARY's claim was not edited.

### IN-08: a caller-supplied empty `X-Correlation-Id` was respected verbatim

**Files modified:** `src/lib/wizard/wizard-correlation.ts`, `src/lib/wizard/wizard-correlation.test.ts`
**Commit:** 0b591e9a1
**Applied fix:** `headers.get(...) ?? mint` became `|| mint`. `Headers.get` answers "" for an
empty or whitespace-only value, and "" now mints like an absent value.
**Tests:** empty and whitespace-only cases each assert a minted id on the wire and the same
id reported. RED on the `??` line (`2 failed | 15 passed`), GREEN on `||`.

### IN-02: the finalize route's dedupe comment said `X-Correlation-Id` is memoized per page load

**Files modified:** `src/app/api/strategies/finalize-wizard/route.ts`
**Commit:** b7adc2cd4
**Applied fix:** a dated correction was added beside the stale sentence, which is kept as
lineage. The header is per-request since plan 07, and the conclusion is stronger for it:
never key the dedupe on it. Comment only.

### IN-04 and the account-change sentence: the prober's operator wording

**Files modified:** `scripts/prod-prober/arms/mt5.mjs`, `src/__tests__/prod-prober-wiring.test.ts`, `analytics-service/services/job_worker.py`
**Commits:** 93fab8e3f (IN-04), c6fb7d23a (account-change sentence)
**Applied fix:**
- **IN-04.** The `mt5-ipc-timeout` remedy now cites all three measured figures, from
  `deploy/mt5-gateway/railway-gateway.md` and CONTEXT D-07:
  - 2.0 s, the 2026-09-21 manual restart
  - 86 s from kill to logged in, spike run 1
  - about 4m45s to a relaunched terminal when the relaunch waited for the next caller, run 2

  It says the session monitor attempts the recycle automatically, cited by symbol
  (`_escalate_ipc_fault`, `MT5_SESSION_POLL_INTERVAL_S`), and tells the operator to read
  that verdict before acting by hand. The ordered procedure and the ambiguity statement are
  unchanged.
- **Account-change sentence.** The `mt5-not-authorized` remedy and job_worker's
  `Mt5GatewayMisconfigured` comment now state the account-change re-clear as conditional on
  "Disable algorithmic trading when the account has been changed" being ticked (founder-read
  UNCHECKED 2026-09-24). The 2026-08-13 observation is recorded as an attribution, not as a
  measured fact about the option's state at that time.

**Guard:** the pre-fix prober file was swapped in -> both new wiring tests RED -> restored
(cp + cmp) -> GREEN. `node scripts/prod-prober/run.mjs --self-test` passed after each commit.

### Gate output (Topic B, final, verbatim)

- `npx tsc --noEmit`: `tsc exit 0`
- vitest over the 13 touched suites (UpdateMt5SecretDialog, dialog-envelope invariant,
  seam-venue-vocabulary invariant, wizardErrors, wizardErrors invariant, both key-route
  tests, instrumentation, wizard-correlation, prod-prober-wiring, ConnectKeyStep,
  MultiKeyConnectStep, finalize-wizard): `Test Files  13 passed (13)` / `Tests  1061 passed (1061)`
- eslint on every touched TS/MJS file: `eslint exit 0`
- `pytest tests/test_mt5_validate.py tests/test_validate_key_venue_transient.py tests/test_mt5_validate_parity.py tests/test_ingestion_mt5.py -q`: `191 passed, 1 skipped, 1 warning in 5.54s`
- `mypy --strict --follow-imports=silent services/ routers/ models/`: `Success: no issues found in 96 source files`
- `ruff check tests/test_mt5_validate.py`: `All checks passed!`
  ⚠️ `ruff check routers/exchange.py` reports 2 F401 (unused imports), and
  `services/job_worker.py` reports 5 (F401 and F841). Both sets are PRE-EXISTING on
  `origin/main`, measured with `git show origin/main:analytics-service/routers/exchange.py | ruff check -`.
  They are untouched here, and ruff is not a CI step.
- `node scripts/prod-prober/run.mjs --self-test`: `=== SELF-TEST PASSED: 83/83 scenarios, every arm's kinds fired on their own fixtures and nowhere else ===`

---

_Fixed: 2026-09-25_
_Fixer: Claude (gsd-code-fixer), Topic B_
_Iteration: 1_

## Topic A: the MT5 heal core (escalation state machine, recycle verb, budget)

**Summary:**
- Findings in scope: 15. They are CR-01, WR-01, WR-02 + SFH-02, WR-03 + SFH-01, WR-04 + IN-03,
  WR-08 + SFH-05, SFH-03, SFH-04 (+ the TODOS half of IN-05), SFH-06, SFH-07, SFH-09,
  SFH-10 + IN-01, the stale account-change wording in `Mt5Client.terminal_info`, IN-06 and IN-07.
- Fixed: 15
- Skipped: 0
- ⚠️ **Fixed, but a human must verify the logic:** CR-01, WR-01, WR-02, WR-03, WR-04, WR-08,
  SFH-03, SFH-04, SFH-05 and SFH-09. These change the escalation's state machine, its budget, or
  code that runs inside the Wine bridge. The tests prove the logic behaves as written. They
  cannot prove it matches the live terminal. ⛔ **The new remote Win32 code has never run
  live.** That covers the `GetLastError` codes and the file-version read. It was run offline
  against real `ctypes` structures with a faked `WinDLL`, the same caveat D-05 records for the
  terminate itself.

**Where verification ran.** Everything ran in the pinned `feat/164.6.5-mt5validatewedge`
checkout, on its own working tree. File paths below are relative to `analytics-service/` unless
they say otherwise. No isolated review-fix worktree was
created. `node_modules` is a symlink to the main checkout's copy, removed before each commit and
re-linked after. pytest, mypy and ruff ran from `analytics-service/` with the main checkout's
`.venv`. Topic B's edits were in the same tree while Topic A's gates ran. Nothing was run against
the live mt5-gateway or any database.

### CR-01: a successful recycle never re-armed the escalation gate

**Files modified:** `services/mt5_relogin.py`, `tests/test_mt5_relogin.py`
**Commit:** 6a0823d6f
**Applied fix:** the relaunch probe counts as a reading. When it measured the terminal answering
(authorized, or `-6`), the run is over and the gate re-arms.
**Test:** wedge, then a recycle that works, then a wedge on the very next tick gives two
recycles. It is parametrized over authorized and `-6`.
**Guard:** re-arm neutered -> RED (2 cases) -> restored (cp + cmp) -> GREEN.

### WR-08 + SFH-05: re-arm only when the terminal is seen answering, by any probe

**Files modified:** `services/mt5_relogin.py`, `services/mt5_client.py`,
`services/mt5_session_episodes.py`, `tests/test_mt5_relogin.py`, `tests/test_mt5_session_episodes.py`
**Commit:** 85cebe5d5
**Applied fix:**
- **Neutral readings.** `0`, `-10003` and `-10004` are now neutral: they neither arm nor disarm.
- **Answered count.** `mt5_client` keeps a per-terminal count of bare `initialize()` calls that
  returned truthy, from any caller, the job path's `login()` included
  (`_MT5_TERMINAL_ANSWERS`, `mt5_terminal_answer_count`). The claim records the count, and a
  count that has moved re-arms the gate. It is cleared by the one shared reset helper.
- **Structural test.** The new module global is declared in the no-episode-state test's list,
  with its reason.

**Tests:**
- `-10005`, then one of {0, -10003, -10004}, then `-10005` gives ONE recycle.
- A job's `login()` on the same terminal between two wedges gives TWO recycles.

**Guards (each RED, then restored cp + cmp, then GREEN):**
- re-arm on the neutral codes
- the login note removed
- the claim ignoring the count
- the note taken before the falsy check

### WR-01: the attempt was spent before anything crossed

**Files modified:** `services/mt5_relogin.py`, `services/mt5_session_episodes.py`, `tests/test_mt5_relogin.py`
**Commit:** 49d26446b
**Applied fix:**
- **Peek, then claim.** The gate is now a peek (`ipc_fault_escalation_armed`) plus a claim that
  is made immediately before the recycle, no longer before the capture.
- **Fence stage.** An `Mt5SessionAbandoned` whose `stage` is the verb's own fence
  (`_RECYCLE_FENCE_STAGE`, `terminal_recycle`) was refused before crossing, so it re-arms. Any
  other stage means the terminate had crossed, and the attempt stands.

**Tests:** driven through the real verb:
- The lease is released during the capture, or after it: the next escalation still recycles.
- The lease is released while the remote call is in flight: the attempt is spent.

**Guards (each RED, restored, GREEN):**
- no stage re-arm
- a re-arm on any abandonment
- the claim moved back ahead of the capture

### WR-03 + SFH-01: a recycle that did not end every terminal is never "recycled"

**Files modified:** `services/mt5_relogin.py`, `services/mt5_session_episodes.py`, `tests/test_mt5_relogin.py`
**Commit:** 8393aa73b
**Applied fix:**
- **Counts decide first.** `terminated < matched`, or `matched == 0`, is the new
  `ipc_fault_recycle_not_landed`, at ERROR, whatever the relaunch said.
- **Exit qualifier.** `exited < terminated` raises the line to at least WARNING and appends
  `exit_unconfirmed=N`.
- **Double.** The relogin double's counts are a scenario knob now. Before, it always answered
  1/1/1.

**Tests:** these count shapes are covered:
- `0/0/0` with an answering probe
- `1/0/0` still wedged
- a partial `2/1/1`
- `1/1/0`

**Guards (each RED, restored, GREEN):**
- the classifier ignoring the counts
- a partial landing counted as landed
- the exit qualifier dropped

### SFH-09: the recycle source names each refusal

**Files modified:** `services/mt5_client.py`, `services/mt5_relogin.py`, `tests/test_mt5_client_contract.py`, `tests/test_mt5_relogin.py`
**Commit:** dad768a48
**Applied fix:**
- **Codes.** The committed remote source appends each refused `OpenProcess` /
  `TerminateProcess` call's `GetLastError()` int to `open_errors` / `terminate_errors`.
- **Still a plain literal.** It has no interpolation and no brace; only its return dict grew.
- **Carried through.** The verb parses the codes as ints, and the escalation line names them.

**Tests:** these gates EXECUTE the committed remote source offline for the first time, with real
`ctypes` structures. Only the Windows-only `WinDLL` and `get_last_error` are supplied. They
cover:
- case-insensitive `terminal64.exe` matching
- the counts
- the codes
- an exit wait that times out
- `matched=0`

**Guards (each RED, restored, GREEN):**
- each append dropped
- case-sensitive matching
- the verb dropping the codes
- the line dropping the codes

### SFH-06: the terminate counts are logged before the relaunch

**Files modified:** `services/mt5_client.py`, `tests/test_mt5_client_contract.py`
**Commit:** 0663229a4
**Applied fix:** the verb logs the counts, codes and (since SFH-04) file versions at WARNING the
moment they are parsed, before the relaunch probe. An abandonment during the relaunch can no
longer discard the only evidence that a shared terminal was ended.
**Test:** the lease releases while the remote call is in flight. The verb raises
`Mt5SessionAbandoned` and the counts line is on record.
**Guard:** line demoted to DEBUG -> RED -> restored -> GREEN.

### WR-04 + IN-03: every rpyc crossing is counted, optional reads are budget-gated, and the budget is pinned by equality

**Files modified:** `services/mt5_relogin.py`, `tests/test_mt5_relogin.py`
**Commit:** 3c0139e0e
**Applied fix:**
- **The unit.** The rpyc crossing is now the unit throughout.
- **The budget.** `_MT5_RELOGIN_ROUND_TRIPS` is built from named parts:
  - first probe: 2
  - `terminal_info` with its `last_error`: 2
  - the recycle, meaning execute + namespace lookup + call: 3
  - the verb's relaunch probe: 2

  That is 9 crossings, and 9 x 30 + 10 = 280 s, under the unchanged 300 s ceiling. It covers
  exactly the unconditional path.
- **Optional reads.** Every optional read goes through `_affordable(deadline, crossings)`.
  - The capture's `account_info()` is now budget-gated. That was plan 05's own fallback.
  - A recycle that the time left cannot finish is never started, and the attempt is not
    spent. That can only happen with `MT5_RELOGIN_BUDGET_S` set below its derived default.
- **Deadline.** The blocking body gets the same deadline as the `wait_for`, through
  `_heal_blocking(..., deadline=...)`.

**Tests:**
- **Double.** It records `execute` and the namespace lookup as crossings, and charges every
  crossing against a fake clock (`_FakeClock`, `mt5_relogin._clock`).
- **Budget gate.** It drives both capture shapes with every crossing at its 30 s ceiling. It
  asserts EQUALITY with the worst path, and asserts that the elapsed time stays inside the
  budget.

**Guards (each RED, restored, GREEN):**
- the count +1
- the count -1
- `account_info` ungated
- the recycle ungated
- the double not counting `execute`

**Deviation from the review's options.** The review offered two fixes:
- (a) drop `account_info`
- (b) merge the three crossings

Neither was taken as written:
- (b) changes the committed-constant shape, which the review itself calls a plan-level
  decision.
- (a) would have thrown away evidence that is cheap whenever the bridge is fast.

Gating the read keeps the evidence when the budget allows it, and it never costs the recovery.

### WR-02 + SFH-02: the relaunch is watched, so a working recycle is not logged as "human needed"

**Files modified:** `services/mt5_relogin.py`, `services/mt5_session_episodes.py`, `tests/test_mt5_relogin.py`
**Commit:** 1a3541814
**Applied fix:** a recycle that landed is now watched (`_watch_relaunch`).
- **Polling.** The credential-free detector is polled every `_RELAUNCH_POLL_INTERVAL_S` (10 s)
  until the terminal answers, or until `_RELAUNCH_SETTLE_S` (90 s) has passed since the verb
  returned. The verb only returns after the kill, so the window always exceeds the measured
  86 s from kill to authorized.
- **`still_faulted`** is claimed only after the whole window was watched.
- **Budget-gated.** Each poll is gated by the budget. When the budget runs out first, the new
  kind `ipc_fault_recycled_relaunch_pending` is logged at WARNING. It means "not yet known", and
  the next reading decides.
- **Log line.** The line carries `relaunch_polls`.

**Tests:**
- On the measured timeline, with 20 s per unanswered `initialize()` and authorized at 86 s, the
  result is `recycled` with no ERROR line.
- With every crossing at its ceiling, the result is `relaunch_pending` at WARNING.
- `still_faulted` appears only after the whole window was slept.

**Guards (each RED, restored, GREEN):**
- window set to 0
- half the window
- pending collapsed into `still_faulted`
- polls ungated by the budget

### SFH-03: a persisting IPC fault is re-raised at ERROR hourly

**Files modified:** `services/mt5_relogin.py`, `services/mt5_session_episodes.py`,
`tests/test_mt5_relogin.py`, `tests/test_mt5_session_episodes.py`
**Commit:** 8a7c35149
**Applied fix:** the debounce now applies to the ACTION, and no longer to the ALARM.
- **The alarm.** Two monotonic stamps (`_IPC_FAULT_RUN_SINCE`, `_IPC_FAULT_LAST_ALARM_AT`) let
  a run of `-10003` / `-10004` / `-10005` readings re-raise at ERROR at most every
  `_IPC_FAULT_ALARM_INTERVAL_S`. That is one hour, the prober's cadence. Each re-raise names
  the elapsed minutes. The `0` sentinel is never part of a run.
- **After a WARNING attempt.** A `-10005` whose one attempt did not itself alarm (for example
  `relaunch_pending`) is re-raised on the next persisting reading.
- **Transient faults.** A `-10004` that lasts less than an hour stays quiet.
- **End of a run.** The run ends only when the terminal is measured answering.
- **Unchanged:** one recycle per run.
- **Structural test.** Both stamps are declared in the no-episode-state test's list, with
  their reasons.

**Tests:**
- Four hours of ten-minute ticks give 1 recycle and 4 ERRORs at least 60 min apart.
- A persisting `-10004` / `-10003` reaches ERROR after an hour, then hourly.
- A recovery in the middle restarts the clock.

**Guards (each RED, restored, GREEN):**
- the debounced alarm
- the never-escalated alarm
- the hourly throttle
- the run not ended by an answer
- `-10003` dropped

### SFH-07: "authorized" after a relaunch now says which account and whether it is connected

**Files modified:** `services/mt5_relogin.py`, `tests/test_mt5_relogin.py`
**Commit:** 23f44cc53
**Applied fix:**
- **The reads.** After a relaunch that reads authorized, the escalation reads
  `terminal_info().connected` and `account_info()`. Both reads are credential-free.
- **Verdicts only.** The account and the server are recorded ONLY as equality verdicts against
  the environment's values. No account number reaches the line.
- **Level.** A terminal that is disconnected, or on another account, raises the line to
  WARNING.
- **Budget.** Both reads are budget-gated. A read the budget cannot cover is recorded as
  `not_read`.

**Tests:**
- house account and connected: INFO
- another account: WARNING
- disconnected: WARNING
- no account number on the line
- with every crossing at its ceiling: `not_read`, inside the budget

**Guards (each RED, restored, GREEN):**
- a mismatch not degrading the level
- a disconnect not degrading the level
- the login value appended to the line
- the reads removed
- the reads ungated

### SFH-04 (+ the TODOS half of IN-05): the build is read bridge-side, and the capture's limits are stated

**Files modified:** `services/mt5_client.py`, `services/mt5_relogin.py`,
`tests/test_mt5_client_contract.py`, `tests/test_mt5_relogin.py`, `TODOS.md`
**Commit:** 35f80c0a0
**Applied fix:**
- **Bridge-side build.** The committed recycle source reads each `terminal64.exe` image's file
  version before ending it (`QueryFullProcessImageNameW` + `GetFileVersionInfoW`). It returns
  four ints per process as `file_versions`, and never the image path. A failed read is an int
  in `file_version_errors` and never blocks the terminate.
- **Capture docstring and evidence line.** They now say the capture is EXPECTED to be
  `not_captured` on a true `-10005`, and they point at the recycle's line.
- **TODOS.** `MT5-SWITCH-WEDGE-CAUSE-01` gets a dated correction, with the original kept as
  lineage:
  - The in-process build is expected to be `not_captured`.
  - The bridge-side build is recorded but has never run live. It equals the running build only
    when no self-update replaced the file.
  - The broker servers and the Alerts/Journal state are recorded by nothing automatic.

**Test:** the executed source returns `[5, 0, 0, 6182]` for one process and a code for a refused
query, still terminates both, and puts no path in the verdict.

**Guards (each RED, restored, GREEN):**
- the read removed
- a refusal that raises
- a failed read blocking the terminate

**Not done here (Topic B's file):** `docs/runbooks/mt5-go-live.md` still says "Unless the heal
records them first". IN-05 asks for that sentence to be narrowed to what the line records.

### SFH-10 + IN-01: the monitor's cadence derivation cites symbols

**Files modified:** `services/mt5_session_monitor.py`
**Commit:** 72bcea3d7
**Applied fix:** these stale figures are now stated by symbol, each with a dated correction:
- "(600 / 2 / ~35) leave ~560 s of headroom"
- "about 27%"
- "five-round-trip budget"

Comment only.

### Account-change wording, IN-06, IN-07: dated corrections and two 164.6.6 constraints

**Files modified:** `services/mt5_client.py`, `services/mt5_session_episodes.py`
**Commit:** 40e7606fd
**Applied fix:**
- **Account-change wording.** `Mt5Client.terminal_info`'s "re-sets `Enabled` off on every
  account change" now carries a dated correction. The re-clear is conditional on the
  account-change option. The founder read it UNCHECKED on 2026-09-24.
- **IN-06 and IN-07.** These are recorded at the code they bind, as 164.6.6 constraints:
  - the gate and the alarm are process-global, not keyed by terminal
  - the recycle ends every `terminal64.exe`

Comment only.

### Recorded rather than done

- **No rate cap on recycles.** CR-01 now allows a recycle per wedge whenever the terminal
  answered in between. There is no "N per rolling hour" cap. Each such recycle followed a
  measured recovery, and a cap would withhold a working remedy. If the live logs show
  wedge -> recycle -> wedge every tick, that pattern is the signal for Cause B. It is not a
  reason to throttle the recycle.
- **Runbook sentence.** See SFH-04 above.
- **Plan-05 SUMMARY.** Its "8 round-trips" and "re-arm on every non--10005 reading" statements
  were not edited. They are historical records, and this section supersedes them.
- **Pre-existing lint.** `tests/test_mt5_relogin.py` carries an F401 (`import asyncio` unused).
  It is PRE-EXISTING: `git show HEAD:... | ruff check -` reported it before this round. It is
  left as it was, and ruff is not a CI step.

### Gate output (Topic A, final, verbatim)

- `mypy --strict --follow-imports=silent services/ routers/ models/`: `Success: no issues found in 96 source files`
- `pytest -k mt5 -q` (from `analytics-service/`): `1069 passed, 3 skipped, 5212 deselected, 33 warnings in 16.64s`
- `pytest -q -n auto` (whole analytics suite): `6194 passed, 90 skipped, 928 warnings in 19.36s`
- `ruff check` on the eight Topic A files: `Found 1 error.` This is the pre-existing F401 above.
- `tsx scripts/check-planning-hygiene.ts`: `[check-planning-hygiene] OK — 6878 tracked files scanned, none carry the local username or an absolute home path`

_Fixed: 2026-09-25_
_Fixer: Claude (gsd-code-fixer), Topic A_
_Iteration: 1_

---

## Round 2 — Topic C

**Sources:** `164.6.5-REVIEW-R2.md` and `164.6.5-REVIEW-SFH-R2.md`.
**Scope:** docs, Next routes and comments. That covers `docs/runbooks/mt5-go-live.md`, the
rotate-secret key route and its test, `src/instrumentation.ts` and its test, and one comment in
`analytics-service/routers/exchange.py`. Parallel fixers own `mt5_relogin` / `mt5_session_monitor`
/ `mt5_session_episodes` / `mt5_client`, and nothing here touches them.
**Where verification ran:** in the MAIN checkout of this branch (no separate worktree), with
`node_modules` symlinked from the primary checkout. The symlink was removed before each commit and
restored after it.

**Summary:** 5 findings in scope, 5 fixed, 0 skipped. There are 3 commits, because IN-03 and
R2-SFH-11 change the same function.

### R2-SFH-10: the rotate-secret route did not page `KEY_MT5_TERMINAL_UNRESPONSIVE`

**Files modified:** `src/app/api/keys/[id]/rotate-secret/route.ts`, `src/app/api/keys/[id]/rotate-secret/route.test.ts`, `analytics-service/routers/exchange.py` (comment only)
**Commit:** 3a8212c2f
**Applied fix:** the route's validation catch now captures to Sentry when the classified code is in
`OUR_DEFECT_KEY_ERROR_CODES`. It uses the same shared set and the same `surface` / `step:
"unclassified-key-error"` tag shape as `create-with-key` and `composite/add-key`, and names
`new_secret` in `secrets`. That makes the `exchange.py` comment ("the Next-side key routes also
page it") true for all three routes. The comment now carries a dated correction.
**Recorded, not changed:** one wedged validate now produces TWO Sentry events, the Python probe's
`logger.error` and the Next capture. Both are kept, because each carries context the other lacks
(the IPC code on the Python side, the key route on the Next side). This is recorded in the route
comment and the `exchange.py` comment.
**Tests:** a twin of the sibling routes' `[164.6.5 WR-06]` case (500, `KEY_MT5_TERMINAL_UNRESPONSIVE`,
no admin UPDATE, captured once with the route's tags and the secret named), plus a negative case
(`KEY_AUTH_FAILED` is not captured). The positive case was RED before the fix ("expected vi.fn() to
be called 1 times, but got 0 times"). Neutered both ways (`false &&` gave the positive case RED;
`true ||` gave the negative case RED), then restored from a `cp` backup verified with `cmp`.
**⚠️ Stale docblock, NOT edited because the file is outside this topic's list:** the
`OUR_DEFECT_KEY_ERROR_CODES` docblock in `src/lib/wizardErrors.ts` still says "Both key routes
page Sentry" and "⚠️ ONE SET, BOTH ROUTES". There are three consumers now. It needs a one-line
comment correction by whoever owns that file.

### IN-03: `wizardPageLoadIdTag` trusted the repo's narrowed header type

**Files modified:** `src/instrumentation.ts`, `src/instrumentation.test.ts`
**Commit:** 9b1ea9825
**Applied fix:** read Next's own documentation first. `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation.md`
types `request.headers` as `{ [key: string]: string | string[] }`, and
`next/dist/server/instrumentation/types.d.ts` types it as `NodeJS.Dict<string | string[]>`. The
changes:
- `onRequestError` now declares `NodeJS.Dict<string | string[]>`.
- `wizardPageLoadIdTag` takes `string | string[] | undefined`, and an array is malformed.
- `correlation_id` becomes a tag only when the header is a string. A Sentry tag must be a
  primitive, and tsc enforces this once the type is widened.

**Measured:** before the fix, the new array-header test failed with `TypeError: raw.trim is not a
function` inside the hook, which is the lost-capture failure the finding predicted.
**Tests:** an array-valued header produces one capture, `wizard_page_load_id` is `<malformed>`, and
`correlation_id` is `null`. Two neuters were each observed RED, then restored with `cp` + `cmp`:
removing the array guard (the `raw.trim` TypeError returns) and reverting the correlation check to
`?? null`.

### R2-SFH-11: a malformed `X-Wizard-Page-Load-Id` was tagged the same as an absent one

**Files modified:** `src/instrumentation.ts`, `src/instrumentation.test.ts`
**Commit:** 9b1ea9825 (with IN-03, same function)
**Applied fix:** an absent header is still `null`, meaning an old bundle. A present header that
fails the shape check is now the fixed sentinel `<malformed>`, meaning a client bug. "Present but
failing" covers empty, whitespace, CR/LF, too long, or an array. The angle brackets are outside
`WIZARD_PAGE_LOAD_ID_SHAPE`, so no client value can read as the sentinel, and the client's value
is still never echoed. The brief said `"malformed"`, but that bare word PASSES the shape allowlist,
so a client could forge it. That is why the sentinel has angle brackets.
**Changed test, with reason:** the round-1 case "an absent, empty or malformed page-load id is null"
now asserts `<malformed>` for empty and CR/LF, and it carries a dated correction note. An empty
header counts as malformed rather than absent, because the client did send the header.
**Tests:** RED before the fix ("expected null to be '<malformed>'"). The neuter set the sentinel to
`null`, the tests went RED, and the file was restored with `cp` + `cmp`.

### WR-05 / IN-05: the runbook said "Unless the heal records them first"

**Files modified:** `docs/runbooks/mt5-go-live.md`
**Commit:** 754a02152
**Applied fix:** added a dated correction under the criterion-1 block, and kept the original
paragraph as lineage. It now agrees with the corrected `MT5-SWITCH-WEDGE-CAUSE-01` entry in
`TODOS.md`. The heal records only two things: (a) a best-effort in-process build and connection
state, expected to be `not_captured` on a true `-10005`, and (b) the bridge-side `file_versions`,
which has never run live and matches the running build only if no self-update replaced the file.
Items 2 and 3 are recorded by nothing automatic, so they need an operator reading before the next
monitor tick (`MT5_SESSION_POLL_INTERVAL_S`). `TODOS.md` already said this, so it was not edited.
The prober's `mt5-ipc-timeout` remedy makes no claim about what gets recorded, so it was not
edited either.

### R2-SFH-05 (runbook half): the hourly re-raise lands on one Sentry issue

**Files modified:** `docs/runbooks/mt5-go-live.md`
**Commit:** 754a02152
**Applied fix:** added a new Step 2b subsection. Every hourly ERROR re-raise (`_escalate_ipc_fault`,
lines beginning `mt5 session heal: ipc_fault`) uses a fixed template and lands on ONE issue. That
has two consequences: a "new issue" alert fires only once, and an ignored or archived issue can
notify nobody. So an event-frequency issue-alert rule must exist, and the issue must not be
ignored. The Python service keeps Sentry's default integrations: `sentry_init.py`'s
`sentry_sdk.init` does not disable them, so the logging integration turns ERROR records into
events. **Not measured here:** whether such a rule exists. That is the orchestrator's separate,
read-only check of the live alert rules, and the runbook says so.

### Observed but not changed (fail loud)

- `src/lib/wizardErrors.ts`: the stale "both key routes" docblock described under R2-SFH-10.
- `docs/runbooks/mt5-go-live.md`, "The remedy that is correct under both causes", says trying the
  restart first "never destroys evidence". For Cause A that holds. For Cause B, the criterion-1
  block now states the opposite: the restart destroys the process-state readings. No finding
  named this sentence, so it was left alone. It should be narrowed to Cause A.

### Gate output (Topic C, final, verbatim)

- `npx tsc --noEmit`: exit 0, no output
- `npx eslint` on the four touched TS files: exit 0
- `npx vitest run` over instrumentation, the rotate-secret route, `UpdateMt5SecretDialog`,
  create-with-key, composite/add-key, the dialog-envelope invariant, `wizardErrors`,
  seam-log-coverage, wizard-correlation and prod-prober-wiring: `Test Files  10 passed (10)` /
  `Tests  793 passed (793)`
- The rotate-secret touch set (resilient-fetch wiring, the seam-ratelimit / seam-budgets /
  seam-constants / seam-retry-registry / limiter-ordering suites, and the key-route and dialog
  suites): `Test Files  13 passed (13)` / `Tests  933 passed (933)`
- `pytest tests/test_mt5_validate.py -q` (from `analytics-service/`, for the `exchange.py`
  comment): `104 passed, 1 skipped in 4.77s`
- `node scripts/prod-prober/run.mjs --self-test` (the prober was NOT edited, and this run
  confirms its runbook reference still holds): `=== SELF-TEST PASSED: 83/83 scenarios, every arm's kinds fired on their own fixtures and nowhere else ===`
- `tsx scripts/check-planning-hygiene.ts`: `[check-planning-hygiene] OK — 6880 tracked files scanned, none carry the local username or an absolute home path`

_Fixed: 2026-09-25_
_Fixer: Claude (gsd-code-fixer), Round 2 Topic C_
_Iteration: 2_

---

## Round 2 — Topic B

**Sources:** `164.6.5-REVIEW-R2.md` (WR-06, IN-01) and `164.6.5-REVIEW-SFH-R2.md` (R2-SFH-04,
R2-SFH-06, R2-SFH-07).
**Scope:** the remote recycle source. That covers `Mt5Client.recycle_terminal_process` and
`_REMOTE_TERMINAL_RECYCLE_SRC` in `analytics-service/services/mt5_client.py`, and
`analytics-service/tests/test_mt5_client_contract.py`. Parallel fixers own `mt5_relogin` /
`mt5_session_monitor` / `mt5_session_episodes` and docs/src, and nothing here touches them.
**Where verification ran:** in the pinned phase worktree (no separate hand-rolled worktree; the
orchestrator pinned this checkout and runs sibling fixers in it), with the primary checkout's
`analytics-service/.venv`. The final gates ran on a `git archive` export of committed HEAD, so the
sibling fixers' uncommitted edits could not colour them.
**No live MT5 or gateway command was run.**

**Summary:** 5 findings in scope, 5 fixed, 0 skipped, in ONE commit (`dd2ceb0e3`), because all
five change the same remote literal and its one parse. One PART of R2-SFH-04 is outside this
topic's files and is recorded below, not done.

**Return shape:** backward-compatible. Every existing key keeps its name and type. Added keys:
`attempted`, `unprocessed`, `enumerated`, `enumerate_error`, `pid_errors`, `file_version_exc`.
The remote function still takes ONE by-value int, so the call contract that
`tests/test_mt5_relogin.py` pins is unchanged.

### R2-SFH-04: a parse failure or remote raise after the terminate lost the counts

**Files modified:** `analytics-service/services/mt5_client.py`, `analytics-service/tests/test_mt5_client_contract.py`
**Commit:** dd2ceb0e3
**Status:** fixed: requires human verification (state-handling logic)
**Applied fix:**
- **Client.** The three counts (`matched` / `terminated` / `exited`) are parsed first, in their
  own `try`, as strict ints. They are the verdict, so an unreadable count still raises
  "malformed verdict". Every other field goes through `_parse_recycle_diagnostics`, which cannot
  raise: a missing or mis-shaped field becomes `"unparsed"` and the other fields are kept. The
  WARNING counts line and the relaunch therefore survive any diagnostic surprise.
- **Bridge.** Each process's `OpenProcess` + `TerminateProcess` is wrapped in a per-process
  `try`. A raise on a later pid (a ctypes marshalling error, say) is recorded as its exception
  CLASS name in `pid_errors` and the loop continues. `attempted` counts every `TerminateProcess`
  issued, a raising one included, so "terminate attempted" survives. A snapshot failure still
  raises, and that is correct, because nothing has been terminated at that point.
**⚠️ NOT done here, and outside this topic's files:** the review's step 3, a budget-gated
detector reading in `_escalate_ipc_fault`'s `RECYCLE_FAILED` arm so a terminal the failed verb
may have ended gets relaunched. That lives in `services/mt5_relogin.py`, which the relogin fixer
owns. After this commit, the verb reaches `RECYCLE_FAILED` only on a transport failure, a snapshot
failure or an unreadable count, so the arm is narrower than it was. It is not closed.
**Also recorded:** `mt5_relogin`'s escalation line does not print the six new fields. The verb's
own WARNING line does. Adding them to the escalation line is the relogin owner's call.

### WR-06: several stray terminals could overrun the one 30 s rpyc request

**Files modified:** `analytics-service/services/mt5_client.py`, `analytics-service/tests/test_mt5_client_contract.py`
**Commit:** dd2ceb0e3
**Status:** fixed: requires human verification (timing logic)
**Applied fix:** the body now ends EVERY matched process first and only then waits. Each wait is
shrunk to what is left of a total budget of three exit waits (`budget_ms = 3 * exit_wait_ms`). It
stops STARTING new processes once the budget is spent, and reports them as `unprocessed`, which
makes `matched != terminated` and so reads as not landed. The verb now sends
`min(_TERMINAL_EXIT_WAIT_MS, request_timeout_ms // _TERMINAL_EXIT_WAIT_DIVISOR)` with a divisor of
6, so three waits fit in HALF of this client's own `sync_request_timeout`. The other half covers
the Win32 calls, whose cost under Wine is unmeasured. At the default 30 s bound that is still 5000
ms, the value `test_mt5_relogin.py` pins.
**Behaviour change:** the exit waits used to follow each terminate. They now follow all of them.
A process that has already exited answers its wait at once, so this only shortens the total.
**Tests:** eight never-exiting strays model at most 15 000 ms and are all terminated (before the
fix: 40 000 ms). Slow terminates (6000 ms modelled each) stop after three and report
`unprocessed=3` with zero-length waits. A client built with `request_timeout_s=24` sends 4000.

### R2-SFH-06: a failed enumeration read like "no terminal running"

**Files modified:** `analytics-service/services/mt5_client.py`, `analytics-service/tests/test_mt5_client_contract.py`
**Commit:** dd2ceb0e3
**Applied fix:** the body returns `enumerated` (every process the snapshot walked) and
`enumerate_error` (`GetLastError()` when `Process32FirstW` returns false, else 0), both as ints.
⚠️ A snapshot with genuinely no processes would also make `Process32FirstW` fail (Win32 then
reports `ERROR_NO_MORE_FILES`, 18). The snapshot should always hold at least the bridge's own
interpreter, so `enumerated=0` with a non-zero code should be the failed-enumeration signature.
That is reasoned, not measured under Wine.

### R2-SFH-07: the version read's `except Exception` kept only `-1`

**Files modified:** `analytics-service/services/mt5_client.py`, `analytics-service/tests/test_mt5_client_contract.py`
**Commit:** dd2ceb0e3
**Applied fix:** `file_version_errors` keeps its `-1` (unchanged type). A new
`file_version_exc` carries the exception's CLASS name per process (`None` when the read did not
raise). Both class-name fields are admitted client-side only if they are a Python identifier
(`_RECYCLE_CLASS_NAME_RE`, at most 64 characters). Anything else, such as a message or a path,
becomes `"unparsed"`, so remote free text cannot reach a log line.

### IN-01: the Win32 half has run only against a faked `WinDLL`

**Files modified:** `analytics-service/services/mt5_client.py` (comment), `analytics-service/tests/test_mt5_client_contract.py`
**Commit:** dd2ceb0e3
**Applied fix:** a test scans the committed source for every `kernel32.X(` / `ver.X(` call. It
then EXECUTES the body and asserts that each called function had `argtypes` set, and that the
non-BOOL returners (`CreateToolhelp32Snapshot`, `OpenProcess`, `WaitForSingleObject`,
`GetFileVersionInfoSizeW`) had `restype` set. The source already met this, so the test was GREEN
on arrival. Its bite was shown by neutering (see below).
**⚠️ Still unmeasured, and this test cannot measure it:** whether the declarations are RIGHT under
Wine. That covers argument conversion, `restype` truncation, and Wine's answers to
`PROCESS_QUERY_LIMITED_INFORMATION` and `GetFileVersionInfoW`. It also covers the relaunch-poll
question the review raised (whether a poll during startup can launch a second terminal). The
owner is `.planning/WINDOWS.md` entry 68, closed by the first post-deploy live run. That run
should read the new `enumerated` / `enumerate_error` / `file_version_exc` / `pid_errors` fields,
and the process count after a watched relaunch.

### Proven able to fail (neuter, observe RED, restore by `cp` + `cmp`)

The eight behavioural tests were RED before the fix (`8 failed, 16 passed`). Each guard was then
neutered one at a time in the fixed file, the named test run, and the file restored from a byte
backup verified with `cmp`:

| neuter | test | result |
|---|---|---|
| diagnostic `except` re-raises | `R2SFH04_a_malformed_DIAGNOSTIC` | 1 failed |
| essential parse made tolerant | `R2SFH04_a_malformed_ESSENTIAL` + `a_malformed_remote_verdict` | 2 failed |
| per-pid `except Exception` narrowed | `R2SFH04_a_raise_after` | 1 failed |
| wait not capped by the budget | `WR06_many_stray` | 1 failed |
| budget stop disabled | `WR06_slow_win32` | 1 failed |
| divisor 6 changed to 1 | `WR06_the_exit_wait_shrinks` | 1 failed |
| `enumerate_error` forced to 0 | `R2SFH06` | 1 failed |
| version class name dropped | `R2SFH07_a_raising` | 1 failed |
| identifier check disabled | `R2SFH07_a_free_text` | 1 failed |
| `CloseHandle.argtypes` deleted | `IN01` | 1 failed |
| `OpenProcess.restype` deleted | `IN01` | 1 failed |

Every restore printed `restored cmp OK`. The first attempt at the essential neuter replaced the
parse rather than relaxing it and passed. It was redone as the row above.

### Gate output (Topic B, final, verbatim, on a `git archive` export of HEAD `68ceee3cc`)

- `pytest -k mt5 -q` (from `analytics-service/`, `env -u` on the four test-DB variables): `1086 passed, 3 skipped, 5212 deselected, 33 warnings in 27.99s`
- `mypy --strict --follow-imports=silent services/ routers/ models/`: `Success: no issues found in 96 source files`
- `pytest tests/test_mt5_client_contract.py -k TERMINAL_RECYCLE -q`: `24 passed, 149 deselected in 0.93s`
- The 3 skips are environment-gated and not from this topic: two integration/live-ccxt opt-ins,
  and `test_mt5_validate.py` `got empty parameter set for (observation)`.
- ⚠️ One mid-round run in the SHARED checkout read `11 failed` and later `2 failed`, all in
  `tests/test_mt5_session_episodes.py`, plus 4 mypy errors in one file. Those came from a sibling
  fixer's uncommitted, in-flight edits. The export-of-HEAD run above is the one that counts.
- `tsx scripts/check-planning-hygiene.ts`: `[check-planning-hygiene] OK — 6880 tracked files scanned, none carry the local username or an absolute home path (rule 1 needle: derived at runtime from the home-directory basename).`

_Fixed: 2026-09-25_
_Fixer: Claude (gsd-code-fixer), Round 2 Topic B_
_Iteration: 2_


## Round 2 — Topic A

**Sources:** `164.6.5-REVIEW-R2.md` (CR-01, WR-01, WR-02, WR-03, WR-04, IN-02) and
`164.6.5-REVIEW-SFH-R2.md` (R2-SFH-01, R2-SFH-02, R2-SFH-03, R2-SFH-04 part 3, R2-SFH-08, R2-SFH-09).
**Scope:** the escalation logic. The files are `analytics-service/services/mt5_relogin.py` and
`analytics-service/services/mt5_session_episodes.py`, with `tests/test_mt5_relogin.py` and
`tests/test_mt5_session_episodes.py`. `mt5_session_monitor.py` needed no change. `mt5_client.py`
was touched by interface only: Topic B's `dd2ceb0e3` return shape.
**Where verification ran:** in the pinned phase worktree, with the primary checkout's
`analytics-service/.venv`. There is no separate hand-rolled worktree; the orchestrator pinned this
checkout. The final gates ran after the last Topic A commit, with `git status` clean, so they
measure committed HEAD.
**No live MT5, gateway, Railway or database command was run.**

**Summary:** 12 findings in scope, 12 fixed, 0 skipped, in 7 commits. Each fix was written test
first, and the test was observed RED against the pre-fix code. The guard was then neutered
(`cp` byte backup, edit), observed RED, restored by `cp`, and checked with `cmp`.

**Orchestrator decision recorded:** D-18 in `164.6.5-CONTEXT.md` (the recycle cap, below). It
supersedes round 1's "Recorded rather than done: no rate cap on recycles".

**Measured by the orchestrator 2026-09-25 (read-only), recorded here and in `_relogin_budget_s`'s
docstring:** `MT5_RELOGIN_BUDGET_S` is NOT set on the production analytics service, so the derived
default applies there. WR-01 / R2-SFH-01 were fixed anyway.

### WR-02 + R2-SFH-03: "undo the claim" and "the terminal answered" were one function

**Files modified:** `services/mt5_relogin.py`, `services/mt5_session_episodes.py`, both test files
**Commit:** 9abd42fa7
**Status:** fixed: requires human verification (state handling)
**Applied fix:**
- `rearm_ipc_fault_escalation` is split in two. `restore_ipc_fault_attempt()` only gives the
  attempt back, and the recycle fence refusal uses it. `end_ipc_fault_run()` re-arms the gate AND
  clears both alarm stamps.
- The answered-count is now compared against the count the RUN started at
  (`_IPC_FAULT_RUN_ANSWERS`, which replaces `_IPC_FAULT_ESCALATION_CLAIMED_AT_ANSWER`). The check
  runs for EVERY first-probe code, the `0` sentinel included. A moved count ends the whole run.
- The escalation no longer stamps an alarm onto a run it has just ended. `answered` is decided
  after the verdict and applied after the level ladder.
**Tests:** a `-10004` hours after a job-path answer does not page on its first reading. Two shapes:
after a `-10005` recycle, and after a `-10004` blip. A fence refusal leaves `_IPC_FAULT_RUN_SINCE`
set and gives the attempt back.

### R2-SFH-02: the persistence alarm was an allowlist

**Files modified:** `services/mt5_relogin.py`, `tests/test_mt5_relogin.py`
**Commit:** 68ceee3cc
**Status:** fixed
**Applied fix:** the allowlist is replaced by `_IPC_FAULT_ALARM_EXEMPT_CODES = {-6, 0}`. Every other
first-probe code's persistence is alarmed. The ACTION gate, `_RECYCLE_REACHABLE_IPC_CODES`, is
unchanged.
**Tests:** `-10001`, `-10002` and `-1` each reach ERROR hourly; before the fix, 0 ERROR. Three hours
of the `0` sentinel never page.

### WR-01 + R2-SFH-01 + SFH-09: a budget-skipped recycle never reached the alarm

**Files modified:** `services/mt5_relogin.py`, `services/mt5_session_episodes.py`, `tests/test_mt5_relogin.py`
**Commit:** 3d02f846e
**Status:** fixed: requires human verification (state handling)
**Applied fix:**
- The "budget cannot cover the recycle" exit goes through
  `ipc_fault_alarm_due(..., attempted=False)`. The first due reading is an ERROR that names
  `MT5_RELOGIN_BUDGET_S` and the elapsed time.
- It returns its own kind, `ipc_fault_recycle_skipped_budget` (SFH-09). `HealOutcome`'s docstring
  now says `None` covers only "the recycle step was not entered".
- `_relogin_budget_s` names a value below the derived default once per process at WARNING, by NAME
  only.
**Tests:**
- 80 minutes of ticks at a 160 s budget give exactly one ERROR naming the variable, and no
  recycle.
- The read-time WARNING fires once, never carries the value, and never fires for the default.
- The abandoned-budget test filters that configuration note, because its own 0.05 s budget now
  correctly triggers it.

### CR-01: a wedge that recurred every tick was recycled every tick, never at ERROR

**Files modified:** `services/mt5_relogin.py`, `services/mt5_session_episodes.py`, both test files, `164.6.5-CONTEXT.md`
**Commit:** 6beb13ff6
**Status:** fixed: requires human verification (state handling)
**Applied fix (D-18):**
- A rolling one-hour window (`_IPC_FAULT_RECYCLE_WINDOW_S`) caps recycles at
  `IPC_FAULT_RECYCLE_CAP = 2`.
- The 2nd recycle inside the hour is an ERROR, "REPEATED WEDGE", naming the Cause B signal
  (`MT5-SWITCH-WEDGE-CAUSE-01`).
- Once capped, nothing is read or recycled until the oldest recycle leaves the window. The heal
  returns `ipc_fault_recycle_capped` and logs the cap at ERROR at most hourly, WARNING otherwise.
- The window is two monotonic stamps, because the cap is 2 and a list would be unbounded module
  state.
- `end_ipc_fault_run` never clears the window. A claim the fence refused takes its entry back.
**Tests:**
- Four ticks of wedge, working recycle, re-wedge give 2 recycles, 1 ERROR naming Cause B, and 2
  capped readings.
- At the hour the remedy frees and the recurrence is raised again.
- A unit pins the two-stamp window, the cap value beside its storage, and the refused-claim
  rollback.
**⚠️ In-process, like the gate:** a redeploy clears the window, so it can free the cap early, at
most once per deploy.

### WR-03: an answering `terminal_info()` / `account_info()` was charged one crossing

**Files modified:** `services/mt5_relogin.py`, `tests/test_mt5_relogin.py`
**Commit:** 7662d7d8b
**Status:** fixed: requires human verification (budget arithmetic)
**Applied fix:** the charge is named and derived. An answering read costs the read (1), plus
`_materialize`'s fixed crossings (6), plus one per field. The field counts are the MetaTrader5
package's documented `TerminalInfo` (22) and `AccountInfo` (28); ⚠️ they were NOT measured on this
gateway. The capture's `terminal_info()` is now budget-gated on that cost, like every other
optional read. So the unconditional path is 7 crossings, and the derived default budget drops from
280 s to **220 s**. A lower default also shortens the unbounded lease wait batch jobs pay (IN-04).
The test double returns a netref-shaped object that records every attribute access, call,
`__iter__` and `next()` as a crossing.
**⚠️ THE CONSEQUENCE, for the orchestrator:** charged honestly, any answering read is at least 8
crossings. Beside the recycle, that exceeds the 300 s budget CEILING at the rpyc bound. So at
every budget the window admits, these are now **SKIPPED on every run**:
- the in-process pre-recycle capture (`terminal_info` / `account_info`)
- the SFH-07 post-relaunch house-session check

Their lines say so ("terminal_info and account_info skipped", `post_relaunch=not_read`). The
bridge-side `file_versions` on the recycle line is unaffected. Consequences:
- A working recycle now reports `ipc_fault_recycled_unverified` at WARNING, see WR-04.
- `recycled` at INFO is reachable only with a verified check.
- These reads come back only if they are materialized bridge-side, the way
  `Mt5Client._materialize_rows` does deals. That is the review's other option, and it is
  `mt5_client`'s to take. It is not done here.
- Note that even a 3-crossing bridge-side read would not fit the capture beside the 5-crossing
  reserve at 220 s.
**Tests:**
- A unit drives the REAL client read against the netref double and requires the declared charge
  to equal what it crossed.
- The budget equality gate is restated over the netref double: 7 crossings, both capture shapes.
- A new test pins that neither the capture nor the post-relaunch reads start, even on an
  instantly answering terminal.
- The tests about what those reads SAY lower the charge explicitly, via
  `_make_the_optional_reads_affordable`.

### WR-04 + R2-SFH-08 + IN-02: the kind said "recycled" when the house session was not shown

**Files modified:** `services/mt5_relogin.py`, `services/mt5_session_episodes.py`, `tests/test_mt5_relogin.py`
**Commit:** a53275f0e
**Status:** fixed: requires human verification (classification logic)
**Applied fix:**
- Two new kinds:
  - `ipc_fault_recycled_degraded`: MEASURED disconnected, another login, or another broker server.
    A login is only unique per server.
  - `ipc_fault_recycled_unverified`: the check could not complete (`not_read`, a failed read, a
    field not captured).
  Both are WARNING. Only a verified house session is `recycled` at INFO.
- IN-02: `matched=0` with an answering relaunch is WARNING, qualified
  `no_process_matched_relaunch_answered`. Every other `recycle_not_landed` stays ERROR.
**Tests:**
- The SFH-07 parametrization pins the KIND for six shapes.
- The tests that reach a working recycle now expect `unverified`.
- The not-landed table pins the IN-02 level and qualifier.

### R2-SFH-04 part 3 + Topic B's new fields

**Files modified:** `services/mt5_relogin.py`, `services/mt5_session_episodes.py`, `tests/test_mt5_relogin.py`
**Commit:** 9b4041dc8
**Status:** fixed: requires human verification (state handling)
**Applied fix:**
- The `recycle_failed` arm takes ONE budget-gated, credential-free detector reading. It relaunches
  a terminal the failed call may have ended, and records
  `post_failure_probe=authorized|code=N|not_read`.
- An answer, `-6` included, ends the run. The kind stays `recycle_failed` at ERROR.
- `Mt5SessionAbandoned` propagates.
- The escalation line now carries Topic B's six fields: `attempted`, `unprocessed`, `enumerated`,
  `enumerate_error`, `pid_errors`, `file_version_exc`. They are ints, Win32 codes, class names the
  client admits only as Python identifiers, or `"unparsed"`. The double emits them.
**Tests:**
- Four post-failure shapes: answered; answered with no account (`-6`, where the answered-count does
  not move); still wedged; budget spent by the failed call.
- The refusal-codes test pins the six fields.

### Neuters (each observed RED, restored by `cp`, verified by `cmp`)

- WR-02: the answered check in `note_ipc_fault_reading` removed; `restore_ipc_fault_attempt` ending
  the run.
- R2-SFH-02: the allowlist restored; `0` made alarmable.
- WR-01: the skip branch's alarm removed; the read-time warning removed; the kind reverted to
  `None`.
- CR-01: the cap check off; the recurrence ERROR removed; restore keeping its window entry; the
  run end clearing the window.
- WR-03: the capture ungated; the materialize overhead under-counted; the post-relaunch read
  charged 2.
- WR-04: the kind mapping removed; a server mismatch ignored; the IN-02 branch off; an incomplete
  check counted verified.
- R2-SFH-04: the probe removed; the probe's answer not ending the run (the first try at this
  neuter stayed GREEN, because an authorized probe also moves the answered-count, so the `-6` case
  was added and then went RED); a field dropped.

### Recorded rather than done

- **WR-03's consequence** (above). The founder or orchestrator should decide whether to take
  bridge-side materialization of these two reads in `mt5_client`. Until then, the in-process
  capture and the SFH-07 check are inert by design, and they say so.
- **The documented field counts** (22 / 28) are unmeasured on this gateway. Add them to the
  first-live-run checklist beside IN-01.
- **R2-SFH-05** (whether the hourly ERROR reaches a human through a Sentry alert rule) is an
  operator check and not this topic's.
- **Pre-existing lint:** `tests/test_mt5_relogin.py` F401 (`import asyncio`) is unchanged from
  round 1.

### Gate output (Topic A round 2, final, verbatim)

- `mypy --strict --follow-imports=silent services/ routers/ models/`: `Success: no issues found in 96 source files`
- `pytest -k mt5 -q` (from `analytics-service/`): `1104 passed, 3 skipped, 5212 deselected, 33 warnings in 21.38s`
- `pytest -q -n auto` (whole analytics suite): `6229 passed, 90 skipped, 928 warnings in 41.36s`
- `ruff check` on the four Topic A files: `Found 1 error.` This is the pre-existing F401 above.
- `tsx scripts/check-planning-hygiene.ts`: `[check-planning-hygiene] OK — 6880 tracked files scanned, none carry the local username or an absolute home path (rule 1 needle: derived at runtime from the home-directory basename).`

_Fixed: 2026-09-25_
_Fixer: Claude (gsd-code-fixer), Round 2 Topic A_
_Iteration: 2_

## Round 2 — WR-03 root cause

**Scope:** a follow-up the orchestrator directed to round 2's WR-03 (commit `7662d7d8b`, Topic A
above). That fix charged `terminal_info()` / `account_info()` honestly, at one rpyc crossing per
field. As a result, the pre-recycle evidence capture (criterion 1's evidence) and the SFH-07
post-relaunch house-session check were SKIPPED on every run. This round fixes the cause rather than
the charge.

### WR-03 (root cause): the heal's two session reads are now ONE bridge-side crossing

**Files modified:** `services/mt5_client.py`, `services/mt5_relogin.py`,
`tests/test_mt5_client_contract.py`, `tests/test_mt5_relogin.py`, `164.6.5-CONTEXT.md`
**Commit:** c23221cde
**Status:** fixed: requires human verification (budget arithmetic; live behaviour unmeasured)
**Applied fix:**
- **`Mt5Client.session_snapshot(*, expected_login, expected_server)`** makes ONE `conn.eval` of a
  committed literal expression, `_REMOTE_SESSION_SNAPSHOT_SRC`:
  - The expression is an AST string constant with no brace, no interpolation marker, no dunder and
    no import. It takes NO argument.
  - Its only calls on `mt5` are `terminal_info`, `account_info` and `last_error`.
  - It reads both on the far side and returns a tagged tuple of scalars, which rpyc copies by
    value. The three tags are `terminal_unanswered`, `account_unanswered` and `answered`.
  - A read that does not answer takes its `last_error()` CODE in the same crossing. Its text never
    crosses.
  - A terminal that does not answer is not asked `account_info`.
  - The client compares the session's login and server with the expected values and returns
    `Mt5SessionSnapshot`. That holds `build`, `connected`, the unanswered codes and the equality
    verdicts only. No account number or server name is returned or logged, and the expected values
    never cross the wire.
  - A result that is not a real tuple (a netref) is refused WITHOUT being indexed. Malformed
    shapes, far-side raises and a missing transport raise a typed, scrubbed `Mt5ClientError`.
  - `terminal_info()` / `account_info()` are unchanged, because the validate path needs the full
    dicts.
- **`mt5_relogin`**: the capture and the post-relaunch check use the snapshot, at
  `_SESSION_SNAPSHOT_CROSSINGS = 1` each. `_MT5_RELOGIN_ROUND_TRIPS` is re-derived:

  | step | crossings |
  |---|---|
  | first probe | 2 |
  | capture | 1 |
  | recycle | 3 |
  | relaunch reading: a failing probe + `last_error()`, OR an answering probe + the check | 2 |

  - That is 8 crossings, so the derived default budget is **8 x 30 + 10 = 250 s**. It was 220 s
    with both reads skipped, and 280 s before that.
  - The check adds no crossing: it follows only an answering probe, which uses one of that
    reading's two crossings. For the same reason the recycle's reserve
    (`_ESCALATION_RESERVE_CROSSINGS`) already covers the check.
  - The capture is gated on 1 + that reserve.
  - The two reads are one crossing on purpose. At one crossing per read the path would be
    10 crossings = 310 s, over the ceiling.
  - `ipc_fault_recycled` at INFO comes back when the check verifies the house session. That branch
    already existed; it was unreachable.
  - The below-default warning now says that the capture is dropped first, and the recycle only
    further below the default.
- **CONTEXT:** a dated entry records that WR-03's "skipped on every run" state is superseded.
  D-18 (the recycle cap) is untouched.

**Tests:**
- The relogin double gains `conn.eval`, charged as ONE crossing. It evaluates the REAL committed
  source against a bridge-side view whose MT5 calls cross nothing, and wraps the result the way
  brine would.
- `test_R2_WR03_RC_a_session_snapshot_is_ONE_crossing_on_every_shape`: one crossing on all three
  shapes. A positive control shows the same double charges `terminal_info()` its per-field cost.
- `test_the_budget_covers_the_ESCALATION_path_too` drives four shapes (capture answers or not,
  relaunch answers or reads `-6`). Each makes exactly `_MT5_RELOGIN_ROUND_TRIPS` crossings within
  the budget.
- `test_R2_WR03_RC_the_capture_and_the_post_relaunch_check_RUN_at_the_default_budget`: every
  crossing is at its ceiling and the budget is the default. The evidence line carries
  `build=6182 connected=True session_server_matches_env=True`, the check verifies, and the
  outcome is `ipc_fault_recycled` at INFO.
- The SFH-07 not-read gate is pinned at its boundary, as a unit on `_read_post_relaunch_state`.
  Through the heal it can no longer bite at a realistic cost.
- The not-landed table gains a `back-on-the-HOUSE-session-VERIFIED` case (INFO). The WR-02
  slow-relaunch case now ends `recycled` at INFO on the measured 86 s timeline.
- Six contract tests cover:
  - the literal and its read-only call set
  - scalars by value
  - verdicts, never values
  - malformed results
  - the scrubbed transport raise
  - the missing transport and the fence
- `_make_the_optional_reads_affordable` is deleted. The reads are affordable at the default now,
  so no test lowers a charge.

### Neuters (each observed RED, restored by `cp`, verified by `cmp`)

- N1, the budget drops the capture term: `1 failed` (the budget equality gate).
- N2, the capture over-reserved so it is skipped at the default: `1 failed` (the RUN test).
- N3, the far side stops coercing non-scalars: `1 failed` (the scalars-by-value test).
- N4, the client returns the login value instead of the verdict: `2 failed, 3 passed` (the
  verdicts test; the three passing cases carry no comparable login).
- N5, an extra per-field crossing before the snapshot: `3 failed` (the one-crossing test).
- N6, a verified recycle logged at WARNING: `1 failed` (the RUN test).
- N7, the remote source calls `mt5.login`: `1 failed` (the literal/read-only test).

Both service files were byte-identical to the pre-neuter copies afterwards (`cmp`).

### Recorded rather than done

- **Live behaviour is unmeasured** (`.planning/WINDOWS.md` entry 68). Not measured:
  - that `conn.eval` resolves `mt5` in the bridge's classic namespace. `mt5linux` 0.1.9 binds it
    there for its own calls.
  - that brine returns the tuple by value.
  - the real cost of the far-side reads during a wedge.
  The first live run should read one `pre-recycle wedge evidence` line and one post-relaunch
  fragment. This agent ran no gateway command.
- **The documented field counts (22 / 28)** no longer set any budget. They survive only as the
  test double's shape.
- **Pre-existing lint:** `tests/test_mt5_relogin.py` F401 (`import asyncio`) is unchanged.

### Gate output (WR-03 root cause, final, verbatim)

All three were run in the pinned worktree at the fix commit, from `analytics-service/`, with the
main checkout's `.venv` and under `env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_DB_URL
-u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY`:

- `mypy --strict --follow-imports=silent services/ routers/ models/`: `Success: no issues found in 96 source files`
- `pytest -k mt5 -q`: `1120 passed, 3 skipped, 5212 deselected, 33 warnings in 19.42s`
- `pytest -q -n auto`: `6245 passed, 90 skipped, 928 warnings in 40.80s`
- `ruff check` on the four files: `Found 1 error.` This is the pre-existing F401 above.
- `tsx scripts/check-planning-hygiene.ts`: `[check-planning-hygiene] OK — 6880 tracked files scanned, none carry the local username or an absolute home path (rule 1 needle: derived at runtime from the home-directory basename).`

_Fixed: 2026-09-25_
_Fixer: Claude (gsd-code-fixer), Round 2 WR-03 root cause_
_Iteration: 2_
