---
phase: 164.6.5
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/wizardErrors.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx
  - analytics-service/services/mt5_validation.py
  - analytics-service/routers/exchange.py
  - src/lib/seam-venue-vocabulary.invariant.test.ts
  - src/lib/wizardErrors.test.ts
  - analytics-service/tests/test_mt5_validate.py
  - analytics-service/tests/test_validate_key_venue_transient.py
autonomous: true
requirements: ["164.6.5-C5"]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A wedged terminal no longer renders as a temporary exchange issue with a Try-again instruction. It renders honest copy with NO Retry control, and the control is suppressed STRUCTURALLY by the copy's own `actions` list rather than by a component-level condition (D-12/D-13)."
    - "The honest `KEY_NETWORK_TIMEOUT` arm is neither deleted nor widened — it stays correct for a genuine transport failure, where a retry really can succeed (D-12)."
    - "The server CARRIES the class through machine-readably. The Python side mints a wire code and the TypeScript side reads it via the `seamCode`-first check, ahead of the substring cascade — the class is never re-sniffed from message text (D-13)."
    - "The copy does not promise an automatic recovery, because D-05's self-heal may not have shipped when this renders (D-13)."
    - "The validate path still fails CLOSED on this arm: it returns no valid verdict and reaches no persistence."
    - "The new outcome is counted on its own, not folded into the existing transient category, so the histogram the parity work is built from is not corrupted."
  artifacts:
    - path: "src/lib/wizardErrors.ts"
      provides: "a new MT5-specific wizard error code, its copy entry with a non-recoverable `actions` list, and its `VENUE_WIRE_CODE_TO_VERDICT` row"
      contains: "VENUE_WIRE_CODE_TO_VERDICT"
    - path: "analytics-service/routers/exchange.py"
      provides: "an IPC-fault arm inside `_validate_mt5_key_probe`'s `Mt5ClientError` handler, positioned by specificity ahead of the generic transient tail"
      contains: "_validate_mt5_key_probe"
    - path: "analytics-service/services/mt5_validation.py"
      provides: "the narrow code-gated predicate the new arm branches on, beside the existing login-error classifier rather than inside it"
      contains: "_IPC_TRANSPORT_CODES"
    - path: "src/lib/seam-venue-vocabulary.invariant.test.ts"
      provides: "the new wire code admitted to the hand-typed arrival roster, so the derived population still matches member for member"
      contains: "EXPECTED_EMITTED_CODES"
  key_links:
    - from: "analytics-service/routers/exchange.py :: _validate_mt5_key_probe's Mt5ClientError arm"
      to: "src/lib/wizardErrors.ts :: VENUE_WIRE_CODE_TO_VERDICT"
      via: "the Python-minted wire code arrives as `AnalyticsUpstreamError.seamCode` and is read by `classifyKeyValidationError` BEFORE the substring cascade"
      pattern: "classifyKeyValidationError"
    - from: "src/lib/wizardErrors.ts :: the new code's copy entry"
      to: "src/lib/envelope.ts :: buildEnvelope"
      via: "`actions` holds no member of `RECOVERABLE_ACTIONS`, so `recoverable` derives false and the Retry control never renders"
      pattern: "RECOVERABLE_ACTIONS"
---

<objective>
Criterion 5 — **the wizard stops telling the user to retry when retry cannot work.**

Purpose: on 2026-09-21 the shipped copy told a client this was *"a temporary exchange issue or a
network blip"* and to *"Try again in a moment"*. MEASURED FALSE across two retries 45 s and 55 s
apart, one of them with CORRECT credentials, against a terminal that stayed wedged for 1h39m. ⭐
The server already knew — the failure is classified server-side as an IPC fault — and the wizard's
job is to stop instructing a retry that cannot work (D-13).

⛔ **THE STAGE IS THE INITIAL VALIDATE, and this is a deliberate pick with evidence behind it.**
CONTEXT.md D-12 originally pointed at the finalize-time scope re-probe's precedent, and its own
dated correction says that precedent governs the wrong stage. The measured incident answered
**HTTP 424** — and on this seam a 424 is emitted only by the flat venue-transient shape raised
inside `_validate_mt5_key_probe`, while the finalize-time scope arm answers 502 from the Next route
and never reaches the Python seam. ⇒ The new arm lands on the INITIAL VALIDATE path.
`finalize-wizard/route.ts` is left untouched on purpose; both consuming routes inherit the new
mapping automatically because both classify through the same shared function.

⛔ **MINT A DISTINCT ARM. Do NOT delete or widen the honest transport arm** — it is correct for a
genuine transport failure and its honest tail is deliberate (D-12).

⛔ **Read `DESIGN.md` before ANY copy decision.** ⚠️ The copy must not promise a self-heal that has
not shipped (D-13).

Output: one wire code, one wizard code, honest non-recoverable copy, and the gates that keep the
two vocabularies agreeing.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-CONTEXT.md
@.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-PATTERNS.md
@DESIGN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The TypeScript half — a new MT5 code, honest non-recoverable copy, its verdict row, and the three step rosters</name>
  <files>src/lib/wizardErrors.ts, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx</files>
  <read_first>
    - ⛔ `DESIGN.md` § `Error Envelope` and § `Voice / microcopy` — MANDATORY before writing a word of copy. The envelope's visual contract, the rule that the Retry CTA renders iff the envelope is recoverable, and the voice rules (declarative sentence-case, active voice, state limitations with the threshold attached, no adjectives where a number exists).
    - `src/lib/wizardErrors.ts` — the MT5 sibling pair for COPY SHAPE (`KEY_MT5_MASTER_PASSWORD` / `KEY_MT5_WRONG_SERVER`, which carry the `{title, cause, fix[], docsHref, actions}` shape); the non-recoverable sibling `KEY_SCOPE_CHECK_UNAVAILABLE`, whose comment explains that its `actions` holds no member of `RECOVERABLE_ACTIONS` and that a Retry button which can only fail again is worse than no button; `VENUE_WIRE_CODE_TO_VERDICT` and `classifyKeyValidationError`'s `seamCode`-first check.
    - `src/lib/envelope.ts` — `buildEnvelope`, and how `recoverable` is DERIVED from `actions` rather than passed.
    - `ConnectKeyStep.tsx` — the roster set and, above it, the long comment recording that this list MUST grow in the same commit the classifier starts returning a new verdict, and that a derive guard reds BY NAME when it does not. `MultiKeyConnectStep.tsx` and `SubmitStep.tsx` carry their own roster sets; read each and decide per-roster from its own stated contract, not by symmetry.
    - `src/lib/wizardErrors.invariant.test.ts` — the derive guard that names the rosters. Run it after the edit; it tells you which rosters are owed.
  </read_first>
  <behavior>
    - The classifier, given an error carrying the new wire code as its seam code, returns the new wizard code — matched on the machine code, before any message text is looked at.
    - An envelope built for the new code derives `recoverable: false`, so the Retry control does not render.
    - The existing generic transport code's copy and its verdict row are byte-unchanged.
    - Every roster whose own contract admits classifier-returned verdicts admits the new code, and the derive guard is green.
  </behavior>
  <action>
Mint the wizard-facing half first, because it is additive and green on its own: a verdict row for a
wire code no producer emits yet changes none of the derived-population assertions, while the
reverse order would red the arrival roster.

**The code.** Add ONE new member to the wizard error code union, named as an MT5-specific sibling
of the existing MT5 pair — its subject is our terminal not answering, not the user's credential and
not their broker server. Suggested name: `KEY_MT5_TERMINAL_UNRESPONSIVE`. ⚠️ Before committing to
it, grep the union and the copy registry to confirm the name is unused and does not read like a
near-duplicate of an existing member.

**The copy**, in the `{title, cause, fix[], docsHref, actions}` shape, written against `DESIGN.md`'s
voice rules:
  · It says what is true: the MetaTrader terminal we use to check the key stopped answering, and
    this is ours to fix — not the user's key, not their password, not their broker server.
  · It does NOT instruct a retry, and it does NOT say "try again in a moment". That sentence is the
    measured defect.
  · ⛔ It does NOT promise that the terminal will recover on its own. D-05's self-heal is a
    separate, concurrent plan and may not have shipped when this renders; copy that promises an
    automatic recovery which has not shipped is the same class of false statement, pointed the
    other way.
  · It tells the user what IS true about their position: their draft is saved, and nothing they do
    from this screen can clear it.
  · ⛔ `actions` holds NO member of `RECOVERABLE_ACTIONS`, following the non-recoverable sibling's
    shape exactly. That is the mechanism — `buildEnvelope` derives `recoverable` from `actions`, so
    the Retry control is suppressed STRUCTURALLY. Never by a component-level condition.
  · `docsHref` follows the sibling MT5 entries.

**The verdict row.** Add one row to `VENUE_WIRE_CODE_TO_VERDICT` mapping the new WIRE code (the
Python-side string, minted in task 2 — suggested: `MT5_TERMINAL_UNRESPONSIVE`) to the new wizard
code and the status task 2 will actually raise. ⚠️ The status here must equal what task 2 raises;
write task 2's status decision down before finishing this task so the two cannot disagree.

**The rosters.** Run `npx vitest run src/lib/wizardErrors.invariant.test.ts` after the edit. Its
derive guard reds BY NAME for every roster that must admit a classifier-returned verdict and does
not. Add the member to exactly those rosters, each with a short comment in that file's own voice
saying it was admitted in the same commit the classifier started returning it and what the
omission would have cost — a rejected honest code rendering as the unknown verdict, whose copy IS
recoverable, giving the user a Retry control for a fault the service marked non-retryable. ⛔ Do
not add it to a roster the guard does not name; each roster's contract is its own.

⛔ **PROVE THE SUPPRESSION CAN FAIL.** `cp` byte backups; add a recoverable action to the new
entry's `actions`; assert the envelope's derived recoverable flag; OBSERVE THE ASSERTION RED;
restore from the backups and verify with `cmp`. ⛔ Never `git checkout -- <path>`, never
`git restore`, never `git stash`. ⚠️ Re-take backups after every edit. Record the RED in the
SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.test.ts src/lib/wizardErrors.invariant.test.ts src/lib/seam-venue-vocabulary.invariant.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>A new MT5-specific wizard code exists with honest, non-promising copy whose `actions` holds no recoverable member; its `VENUE_WIRE_CODE_TO_VERDICT` row is present; every roster the derive guard named admits it with a reason comment; the generic transport code is byte-unchanged; the recoverable-derivation was observed RED under a neuter and restored via `cmp`-verified byte backups. Both verify commands pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Python half — mint the wire code at the initial-validate IPC arm, and admit it to the arrival roster</name>
  <files>analytics-service/services/mt5_validation.py, analytics-service/routers/exchange.py, src/lib/seam-venue-vocabulary.invariant.test.ts</files>
  <read_first>
    - `analytics-service/services/mt5_validation.py` — `_IPC_TRANSPORT_CODES` and the whole comment block above the code gate inside `classify_mt5_login_error`, which records the 2026-08-12 wedged-gateway incident and states why the IPC codes are gated BEFORE any text matching. Also read `classify_mt5_login_error`'s REFUSAL RULE docstring: exactly two of its three classes become a permanent user-attributed verdict, and an ambiguous message must never reach either.
    - `analytics-service/routers/exchange.py` — the `except Mt5ClientError as e:` arm inside `_validate_mt5_key_probe`'s nested probe, and the three arms above it; the `service_error(...)` call for the gateway-unconfigured fault, which is the shipped precedent for an MT5-specific machine code that already has a verdict row; and the `_Mt5ValidateTrace` dataclass docstring, which explains why every arm writes its own outcome category.
    - `analytics-service/services/error_contract.py` — `service_error` and its `_validate` rules, and the flat venue-transient class's own docstring. ⚠️ Read `_validate` before choosing a status: it refuses combinations, and a status it refuses raises at the call site. Do not invent one.
    - `src/lib/seam-venue-vocabulary.invariant.test.ts` — `EXPECTED_EMITTED_CODES` and the assertion that the derived population matches it member for member, plus the assertion that every derived code has an explicit disposition. These are the arrival gates this task must satisfy in the same commit.
  </read_first>
  <behavior>
    - A validate whose login raises the client error with an IPC transport code fails with the NEW wire code, not the generic transport one.
    - A validate whose login raises the client error with a NON-IPC code still classifies exactly as it does today — auth, wrong server, or the generic transport tail.
    - The new arm reaches no persistence and returns no valid verdict: it fails CLOSED, like every other arm of this function.
    - The outcome recorded for the stage event is the new category, distinct from the existing transient one.
    - The raised body carries the new code as its machine code and marks the failure non-retryable.
  </behavior>
  <action>
Mint the machine code at the site the measurement points to, and admit it to the arrival gate in
the same commit — the derived population and the hand-typed roster are compared member for member,
so a code that exists on one side only is red.

**The predicate**, in `mt5_validation.py`, beside `classify_mt5_login_error` rather than inside it.
A narrow function answering ONE question from the CODE alone: is this client error one of the IPC
transport codes? Reuse the existing `_IPC_TRANSPORT_CODES` tuple — ⛔ do not re-spell the codes and
⛔ do not duplicate the tuple; this module's own comments say a shape test copied twice drifts and
the drift is silent.
⛔ **Do NOT add a fourth class to `classify_mt5_login_error`.** Its three-way contract is pinned by
executing tests at two call sites and its refusal rule is load-bearing; widening it to carry a
disposition only one caller needs is how a shared classifier acquires a caller's concerns.

**The arm**, inside `_validate_mt5_key_probe`'s `Mt5ClientError` handler. Order matters and this
file orders by specificity: the auth and wrong-server branches stay exactly where they are and
exactly as they are; the new branch sits AFTER them and BEFORE the generic transient tail. Under
it: write the new outcome category onto the trace; log at WARNING carrying the SCRUBBED code only
(⛔ never the interpolated remote text, ⛔ never a login, password or broker server value — the
handler's existing comment says exactly this); and raise the envelope carrying the new machine code
with the dependency named and the failure marked non-retryable, following the gateway-unconfigured
arm's shape in this same function.
⚠️ Pick the STATUS from `error_contract._validate`'s own admissible rules and from what the fault
actually is — our named dependency answered unusably, which is not a caller-class fault. Record the
choice and its reason at the site, and make it equal the status task 1 wrote into the verdict row.
⛔ It must NOT reuse the flat caller-class shape reserved for caller-exchange faults if `_validate`
and that class's own docstring say otherwise; read both before deciding.

**The trace outcome.** Mint a NEW category rather than reusing the existing transient one, for the
reason the undetermined arm's own comment gives: the parity histogram groups by this field, and
folding a distinct, operator-actionable state into the transient bucket corrupts the counts. ⚠️
Before choosing the string, grep for any place that enumerates outcome categories and confirm a new
one is admitted rather than refused.

**The arrival roster.** Add the new wire code to `EXPECTED_EMITTED_CODES` in
`src/lib/seam-venue-vocabulary.invariant.test.ts`, with a short comment in that file's own style
naming the phase, the emitter and the disposition it takes — matching the existing MT5 entries.
⛔ This is not optional bookkeeping: that roster is the ARRIVAL gate, and a new Python code with no
TypeScript disposition falls through the substring cascade to whatever an English sentence happens
to earn, which is the exact failure the file exists to prevent.

⛔ **PROVE THE ARM CAN FAIL.** `cp` byte backups; neuter the new branch so an IPC-coded error falls
to the generic tail; run the task-3 tests; OBSERVE RED; restore and verify with `cmp`. ⛔ Never
`git checkout -- <path>`, never `git restore`, never `git stash`. ⚠️ Re-take backups after every
edit.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <automated>npx vitest run src/lib/seam-venue-vocabulary.invariant.test.ts</automated>
  </verify>
  <done>A narrow code-gated predicate exists beside the login classifier and reuses the shipped IPC-code tuple; `classify_mt5_login_error`'s three-way contract is unchanged; the new arm sits between the wrong-server branch and the generic tail, fails CLOSED, logs the scrubbed code only, raises the new machine code non-retryable with the dependency named, and writes its own trace outcome; the wire code is admitted to `EXPECTED_EMITTED_CODES` with a reason comment. mypy strict passes and the arrival-gate suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: The gates — both halves, and the honest transport arm proven untouched</name>
  <files>analytics-service/tests/test_mt5_validate.py, analytics-service/tests/test_validate_key_venue_transient.py, src/lib/wizardErrors.test.ts</files>
  <read_first>
    - `analytics-service/tests/test_mt5_validate.py` and `analytics-service/tests/test_validate_key_venue_transient.py` — find the existing style for driving `_validate_mt5_key_probe`'s arms and for asserting the raised wire shape. Extend it; do not start a second convention.
    - `src/lib/wizardErrors.test.ts` — the existing per-code copy assertions and the classifier assertions.
    - `src/app/api/strategies/create-with-key/route.test.ts` — find the case that exercises a seam-code-carried classification end to end (the file already has one for the gateway-unconfigured code). ⚠️ If a route-level case is owed there, note it in the SUMMARY; that route file itself needs NO code change, because it inherits the mapping through the shared classifier.
  </read_first>
  <action>
Write the gates that make both halves of this criterion falsifiable.

PYTHON side:
  · A validate whose login raises the client error with an IPC transport code produces the NEW wire
    code, marked non-retryable, with the dependency named — and reaches no persistence.
  · A validate whose login raises the client error with a NON-IPC code is UNCHANGED: the auth
    branch, the wrong-server branch and the generic transport tail all classify exactly as before.
    ⭐ This is the D-12 fence as an executing assertion — the honest transport arm must be proven
    intact, not merely left alone.
  · The stage event's recorded outcome for the new arm is the new category and not the transient
    one.
  · The log line for the new arm carries the scrubbed code and no credential value. Assert it as a
    PROPERTY of what was logged. ⛔ Never construct a credential-shaped literal in the test file as
    the thing you search for — this repo has a dated case of a proof-of-absence publishing what it
    proved absent, and the full-history secret scan reads every file.

TYPESCRIPT side:
  · The classifier, given an error whose seam code is the new wire code, returns the new wizard
    code and the agreed status — and it matches on the MACHINE code, proven by giving the error a
    message that would classify differently under the substring cascade.
  · An envelope built for the new code derives a non-recoverable verdict, so the Retry control does
    not render.
  · The new copy does not instruct a retry and does not promise an automatic recovery. Express this
    as a property of what the copy DOES say plus the structural non-recoverable derivation — ⛔ a
    gate that only greps for an absent phrase goes green the moment someone rewords it.

⛔ **EVERY ASSERTION HERE MUST BE PROVEN ABLE TO FAIL**, one at a time: `cp` a byte backup of the
production file the assertion targets, neuter that behaviour, run the test, OBSERVE IT RED, restore
from the backup and verify with `cmp`. ⛔ Never `git checkout -- <path>`, never `git restore`, never
`git stash` — this repo has a dated record of `git checkout` silently destroying uncommitted work in
exactly this neuter/restore shape. ⚠️ RE-TAKE the byte backup after every edit; a stale backup makes
`cmp` pass on a corrupt restore. ⚠️ A test that CANNOT FAIL is worse than none. Record every RED
observation in the SUMMARY.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_validate.py tests/test_validate_key_venue_transient.py -q</automated>
    <automated>npx vitest run src/lib/wizardErrors.test.ts src/lib/wizardErrors.invariant.test.ts src/lib/seam-venue-vocabulary.invariant.test.ts</automated>
  </verify>
  <done>Both suites gate the new arm and prove the honest transport arm unchanged; the classifier is proven to match on the machine code rather than the message; the non-recoverable derivation is gated; every new assertion was observed RED under a neuter and restored via a re-taken byte backup verified with `cmp`; both commands pass with a non-zero collected count (⛔ `0 collected` is a loud failure, not a pass).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a client's browser → the wizard API routes → analytics-service validate | untrusted credentials cross here; the arm added is on the failure path |
| analytics-service error text → the wizard's rendered copy and its copy-diagnostics payload | a remote error message can echo a submitted account or broker server |
| this repo → the public internet | test fixtures and copy strings are world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.5-16 | Information Disclosure | the new arm's log line and raised detail | high | mitigate | The arm logs the SCRUBBED code only and never the interpolated remote text, following the handler's existing rule; the raised detail is a static operator-facing sentence carrying no submitted value; task 3 gates it as a property |
| T-164.6.5-17 | Repudiation | copy that misattributes our terminal fault to the user's credential or broker server | high | mitigate | The new code is a distinct sibling; the auth and wrong-server branches are byte-unchanged and task 3 proves them intact; the code gate runs ahead of any text matching |
| T-164.6.5-18 | Repudiation | copy that promises an automatic recovery that has not shipped | medium | mitigate | Task 1 forbids it explicitly; D-05 is a concurrent plan and this plan assumes it has not shipped |
| T-164.6.5-19 | Spoofing | a failure path that returns a valid verdict or reaches persistence | critical | mitigate | The new arm raises before any persistence, mirroring every other arm of the function; task 3 gates the fail-closed property |
| T-164.6.5-20 | Information Disclosure | a test that constructs a credential-shaped literal as its search subject | high | mitigate | Task 3 forbids it and requires the assertion be written as a property of what was logged; a full-history secret scan reads every file in this repo |
| T-164.6.5-21 | Tampering | a new wire code with no TypeScript disposition, falling through the substring cascade | high | mitigate | The arrival roster and the verdict row are both updated; the member-for-member derived-population gate is in both tasks' verify |
| T-164.6.5-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; RESEARCH.md records the phase introduces no new external dependency |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_validate.py tests/test_validate_key_venue_transient.py -q` passes with a non-zero collected count.
- `cd analytics-service && .venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/` passes.
- `npx vitest run src/lib/wizardErrors.test.ts src/lib/wizardErrors.invariant.test.ts src/lib/seam-venue-vocabulary.invariant.test.ts` passes.
- `npx tsc --noEmit` passes.
- ⚠️ pytest and mypy run ONLY from `analytics-service/` via that directory's own `.venv/bin/python`.
- `src/app/api/strategies/finalize-wizard/route.ts` is unchanged, and the SUMMARY records why (the measured 424 pins the initial-validate stage).
</verification>

<success_criteria>
- A wedged terminal renders honest, non-retry copy with no Retry control, suppressed structurally.
- The honest transport arm is intact and proven so by an executing assertion.
- The class travels machine-readably from the Python raise site to the rendered copy.
- Both vocabularies agree member for member and the arrival gate is green.
</success_criteria>

<output>
Create `.planning/phases/164.6.5-mt5validatewedge-mt5-key-validation-stops-destroying-the-sha/164.6.5-04-SUMMARY.md` when done.
</output>
