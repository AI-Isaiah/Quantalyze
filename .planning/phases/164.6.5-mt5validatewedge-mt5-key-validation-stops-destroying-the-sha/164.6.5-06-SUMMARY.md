---
phase: 164.6.5
plan: 06
subsystem: mt5-validate
tags: [mt5, d-15, settings-landmine, severity, curated-copy, pytest, structural-scan]

requires:
  - phase: 164.6.5-04
    provides: "the undetermined arm's current shape in _validate_mt5_key_probe and the neuter/cp/cmp proof discipline"
  - phase: 164.6.4
    provides: "164.6.4-UAT R2 / question THREE: a real login on 2026-09-16 left the Expert-Advisors options byte-identical"
provides:
  - "mt5_validation.ACCOUNT_CHANGE_ALGO_DISABLE_OPTION: the D-15 landmine named by its on-screen label, documented (what it is, why validation trips it, why it cannot be read directly), with NO second judge"
  - "A reconciled, dated record: the 2026-08-13 're-clears on EVERY account change' belief corrected in every restatement inside this plan's file set, old sentence kept as lineage"
  - "A true user-facing trade-permission cause sentence (the false recurrence clause removed; the landmine named as what must stay unticked)"
  - "The operator arm in routers/exchange.py logs at ERROR and names the setting and validation-as-account-change; raised fault unchanged"
  - "Six D-15 gates: severity, cause-naming, fail-safe direction, fault shape, no-terminal-option-write scan, curated-family label"
affects: [164.6.5-07, 164.6.5-08, 164.6.6]

tech-stack:
  added: []
  patterns:
    - "A structural 'no write path' scan built on tokenize + ast that drops COMMENT tokens and docstrings before matching, paired with a synthetic-source test proving prose is ignored and code-shaped writes are caught"
    - "Severity asserted RELATIVE to a sibling arm measured from the same router (never below the unset-env arm), not only against an absolute level"

key-files:
  created: []
  modified:
    - analytics-service/services/mt5_validation.py
    - analytics-service/services/mt5_probe.py
    - analytics-service/routers/exchange.py
    - analytics-service/tests/test_mt5_validate.py
    - analytics-service/tests/test_mt5_validate_parity.py
    - analytics-service/tests/test_ingestion_mt5.py

key-decisions:
  - "The recorded belief is STALE as an unconditional claim, not averaged. The founder read the Experts tab over VNC on 2026-09-24: 'Allow algo trading' CHECKED, all four 'Disable ...' options UNCHECKED, including 'Disable algorithmic trading when the account has been changed'. With the 2026-09-16 164.6.4-UAT login (options byte-identical across a real authorization), the re-clear is GATED by that box. The 'every account change' sentences were corrected, and the old wording is kept as lineage."
  - "The user-facing sentence now names the account-change option as what must stay unticked, because every key check changes the account. That is true whichever way the box sits, and it is the first thing an operator should check when this fault fires."
  - "Operator arm logged at ERROR, level with the router's own unset-env arms (which also log at ERROR), not at CRITICAL. The plan's requirement is 'above an ordinary verdict' (WARNING) and never below an unset env var. That mirrors mt5_relogin WR-01's parity argument."
  - "The landmine is named as a constant (ACCOUNT_CHANGE_ALGO_DISABLE_OPTION), not as a second predicate. terminal_trade_permission_off stays the ONE cause predicate. The prober's unjudged branch is untouched."
  - "The founder reading is recorded as a POINT-IN-TIME measurement, not a config guarantee. The bridge has since relaunched the terminal with /portable (164.6.5-02), whose settings live in the install folder, so the live state is only what terminal_info() reports. That is the reason the consequence check exists."

requirements-completed: ["164.6.5-C7"]

actuals:
  tokens: 8000
  tasks: 3
  commits: 5
  plan_head_before: 2e2033dade2b9fe307cf7f8e5cd252ebb66272f0

duration: 9 min
completed: 2026-09-25
status: complete
---

# Phase 164.6.5 Plan 06: The Settings Landmine Is Pinned, Not Ticked. Summary

**The "re-clears on every account change" record is reconciled against a founder-read Experts tab. The false recurrence clause is gone from the user-facing cause sentence. The landmine now has a name (`ACCOUNT_CHANGE_ALGO_DISABLE_OPTION`) built on the one existing predicate. A shared terminal losing algo permission logs at ERROR, naming the setting and validation-as-account-change. Six gates keep that loud, false-alarm-free and write-free.**

## Performance

- **Started:** 2026-09-25T18:52:53Z
- **Completed:** 2026-09-25T19:01:35Z
- **Tasks:** 3 (1 founder reading, 2 auto)
- **Files modified:** 6

## Task 1: the founder reading (recorded, dated)

**Founder-read over the gateway's VNC console on 2026-09-24.** The dialog was Tools → Options → Expert Advisors. Nothing on the screen was changed.

| Option (on-screen label) | State |
|---|---|
| Allow algorithmic trading | **CHECKED** |
| Disable algorithmic trading when the account has been changed | **UNCHECKED** |
| Disable algorithmic trading when the profile has been changed | **UNCHECKED** |
| Disable automatic trading through the external Python API | **UNCHECKED** |
| (the fourth "Disable ..." option on the tab) | **UNCHECKED** |

The founder's report reads: "all four 'disable ...' options UNCHECKED".

⚠️ **This is a point-in-time measurement, not a config guarantee.** After that reading the bridge relaunched the terminal with `/portable` (164.6.5-02 SUMMARY). A portable terminal keeps its settings in the install folder, so the live state is only what `terminal_info()` reports. D-15 already required that, and it is why the deliverable is a check on the observable consequence.

**Reconciliation verdict.** The five-place belief was dated 2026-08-13 and said the gateway re-clears algo trading on EVERY account change. As an unconditional claim it is **stale**. The re-clear is gated by "Disable algorithmic trading when the account has been changed", which reads UNCHECKED. That agrees with 164.6.4-UAT, where a real login on 2026-09-16 left the options byte-identical, and with D-15's 2026-09-22 reading.

Only one side had to be corrected, so nothing was averaged. The conditional form ("while `Account=1` is armed") in `scripts/mt5-diag.sh` and the header of `scripts/prod-prober/arms/mt5.mjs` was already right.

## Accomplishments

- **Reconciled, dated corrections.** Every restatement in this plan's file set now carries a `⛔ CORRECTED 2026-09-25 (164.6.5-06)` note, with the old sentence kept as lineage:
  - the cause-constant comment and the `Mt5GatewayMisconfigured` docstring in `mt5_probe.py`;
  - the module docstring and the `terminal_trade_permission_off` docstring in `mt5_validation.py`;
  - the operator-arm comment in `routers/exchange.py`.
- **The user-facing sentence is true at HEAD.** `MT5_GATEWAY_TRADE_PERMISSION_OFF_DETAIL` no longer says the gateway "switches it off again whenever it changes users". It now says:
  > …Turning it back on needs an operator, not a retry, and the gateway's 'Disable algorithmic trading when the account has been changed' option must stay unticked, because every key check changes the account — see docs/runbooks/mt5-go-live.md.

  The sentence is still a member of `MT5_GATEWAY_MISCONFIGURED_DETAILS`, still credential-free, and still classifies `transient`. The whole-family fence passes unchanged.
- **The landmine is nameable without a second judge.** `ACCOUNT_CHANGE_ALGO_DISABLE_OPTION` in `mt5_validation.py` has a doc-comment that covers what the setting is, why validation trips it (a validation logs the shared terminal in), and why it cannot be read directly (`[Experts] Account=1` reaches disk only on a clean exit). `terminal_trade_permission_off` is still the one cause predicate. The prober's unjudged branch was not touched.
- **The lost-permission arm is loud.** `logger.warning` became `logger.error`, which is level with the router's unset-env arms. The line names 'Allow algorithmic trading', says the terminal is shared by every client, says "Validation is an account change", and names the option label through the constant. The raised `service_error(500, "MT5_GATEWAY_UNCONFIGURED", dependency="mt5-gateway", retryable=False, detail=…)` is byte-for-byte unchanged.
- **Nothing writes a terminal option.** No setter call was added, and a structural scan now asserts it.

## Task Commits

1. **Task 1: founder reading.** No commit. It is a reconciliation gate, and the reading is recorded above.
2. **Task 2: reconcile the record and make the consequence loud.** `7a7a310af` (fix)
3. **Task 3: the gates.** `3b313a94e` (test)

`commits: 5` is MEASURED as `git rev-list --count 2e2033da..HEAD`. It includes three commits from sibling plans that share this checkout: `ee224d576` and `20422b71f` (plan 01), and `2d176b924` (roadmap). This plan's own commits are the two above.

## The six gates (all in `tests/test_mt5_validate.py` except the last)

1. `test_d15_lost_terminal_permission_logs_above_an_ordinary_verdict`: the level is above WARNING and at least the unset-env arm's level, measured from the same router.
2. `test_d15_the_line_names_the_setting_and_why_validation_trips_it`: positive assertions on the hand-typed label, "Validation is an account change", "Allow algorithmic trading", and every/client.
3. `test_d15_the_loud_check_never_false_alarms_on_a_bridge_blip[5 cases]`: covers unreadable, disconnected+permission-off, disconnected, and both malformed shapes. Each must answer 424 `NETWORK_ERROR_DETAIL`, emit nothing above WARNING, and never name the landmine.
4. `test_d15_the_raised_fault_is_unchanged_only_the_log_got_louder`: 500, `MT5_GATEWAY_UNCONFIGURED`, `mt5-gateway`, `retryable is False`, exact key set, no Retry-After.
5. `test_d15_no_analytics_service_path_writes_a_terminal_option`, plus its proof `test_d15_the_write_scan_ignores_prose_and_catches_code`. The scan drops comments and docstrings with tokenize and ast, matches terminal/common/origin `.ini`, `[Experts]`, a bare `"Experts"` key, and `Account|Profile|Enabled|Api|AllowLiveTrading|AllowDllImport=0|1`, and names file:line on failure. The paired test also proves the real corpus carries `[Experts]` in prose, so stripping it is load-bearing.
6. `tests/test_mt5_validate_parity.py::test_d15_the_trade_permission_sentence_names_the_landmine_and_no_recurrence`: the sentence is a family member and names the label, which must equal both a hand-typed copy and the named constant.

## Proven-Able-to-Fail Ledger (neuter → RED → cp restore → cmp OK)

Backups were taken with `cp` from the committed task-2 state. After the run, all three production files were `cmp` OK and `git diff` against HEAD was empty. No `git checkout`, `git restore` or `git stash` was used.

| # | Neuter | Observed |
|---|---|---|
| N1 | operator arm `logger.error` → `logger.warning` | RED: gate 1 |
| N2 | log arg `ACCOUNT_CHANGE_ALGO_DISABLE_OPTION` → `'the relevant option'` | RED: gate 2 |
| N3 | "Validation is an account change: " removed from the line | RED: gate 2 |
| N4 | `terminal_trade_permission_off` loses its `connected` check | RED: gate 3 [disconnected AND permission off], gate 3 [malformed, no connected field], and the existing builder-selection DETACHED case |
| N5 | raised fault `dependency="mt5-gateway"` → `"analytics"` | RED: gate 4, the existing `test_mt5_terminal_trade_disabled_never_returns_readonly`, `test_mt5_gateway_misconfigured_operator_fault_on_both_paths`, and gates 1 and 2. The last two fail because `service_error` rejects the dependency, so the arm no longer raises an HTTPException |
| N6 | code line `_NEUTER_INI = "Config/terminal.ini"` added to `mt5_validation.py` | RED: gate 5 |
| N7 | **CONTROL:** comment `# [Experts] Account=1 in Config/terminal.ini` added | **GREEN**, as intended: prose cannot satisfy or defeat the scan |
| N8 | `ACCOUNT_CHANGE_ALGO_DISABLE_OPTION` label drifted | RED: gate 2 and gate 6 |
| N9 | sentence regains the "switches it off again whenever it changes users" clause | RED: gate 6, both builder-selection trade-permission cases, `test_mt5_gateway_misconfigured_operator_fault_on_both_paths`, and `test_ingestion_mt5.py::test_validate_terminal_trade_disabled_never_returns_readonly` |

## Verification (verbatim tails)

- `cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_validate.py tests/test_mt5_validate_parity.py -q` → `127 passed, 1 skipped in 4.21s`
- `cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_probe_parity_roster.py -q` → `12 passed in 1.00s`
- `cd analytics-service && .venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/` (CI's exact command, `ci.yml`'s python job) → `Success: no issues found in 96 source files`
- The broader sweep over every `tests/*.py` that mentions mt5 gave `1 failed, 1691 passed, 2 skipped`. The one failure was `test_mt5_session_episodes.py::test_the_module_holds_NO_EPISODE_STATE_AT_ALL_by_construction`, a file that belongs to plan 05, which was mid-edit uncommitted in the same checkout. It passed when re-run moments later, and this plan touches neither that file nor its module.
- `scripts/mt5-diag.sh` is byte-unchanged (`git diff --quiet 2e2033da..HEAD -- scripts/mt5-diag.sh`).
- `check-planning-hygiene`: `OK — 6873 tracked files scanned`.

Python was the main checkout's `analytics-service/.venv`, run from this worktree's `analytics-service/`. Imports were confirmed to resolve to the worktree's `services/mt5_probe.py`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] The reworded sentence is also hand-pinned in `tests/test_ingestion_mt5.py`, which is outside `files_modified`.**
- **Found during:** Task 2
- **Issue:** `test_validate_terminal_trade_disabled_never_returns_readonly` hand-types the old sentence verbatim, so correcting the false sentence red it.
- **Fix:** Re-typed the expectation to the corrected sentence, still hand-typed and never imported, with a dated note. No sibling plan declares this file.
- **Commit:** `7a7a310af`

**2. [Ordering] The existing hand-typed pins moved in task 2's commit, not task 3's.**
- The plan says to "update the parity expectations in task 3". Leaving them for task 3 would have made the task-2 commit red. So the minimal pin updates went in with the change that forced them: `_EXPECTED_TRADE_PERMISSION_OFF`, the ingestion pin, and the non-vacuity line in `test_mt5_capability_refusal_logs_carry_no_credentials`, which read `warning` and now reads `error` because that fixture is the operator arm. Every NEW gate is in task 3. No fence was loosened.

**3. [Process] Hygiene before the task-3 commit did not run.**
- A sibling agent had already linked `node_modules`, so my `ln -s` failed and the chained hygiene step was skipped. My unconditional `rm -f node_modules` then removed the sibling's link. I re-created the link right away and ran hygiene after the commit: OK, 6873 files. The task-3 commit adds only test code, with no paths or identifiers.

## Named residuals (stale restatements OUTSIDE this plan's file set, not edited)

- `analytics-service/services/mt5_client.py`, around the `trade_allowed` field docs: "The gateway re-sets `Enabled` off on every account change (`Account=1`/`Profile=1`), which is why the fault recurs." This is unconditionally stated and stale.
- `analytics-service/services/job_worker.py`, in the comment above the `Mt5GatewayMisconfigured` classify arm: "which the gateway re-sets off on every account change while THIS worker logs in on every job". This is stale in the same way.
- `scripts/prod-prober/arms/mt5.mjs`, in the no-account-authorized operator sentence: "MT5 re-clears those options on every account change". This is an operator-facing string and unconditional. The file's header is conditional ("while `[Experts] Account=1` is armed") and correct.
- `scripts/mt5-diag.sh` header: conditional ("while `[Experts] Account=1` is armed") and **correct**. It is fenced read-only and was not edited.
- `161-UI-SPEC § Copy Spec WIZERR-01`, the origin of the old sentence, is an archived phase artifact and was not edited. This SUMMARY records the supersession.

## Threat Model Coverage

| Threat | Disposition | How met |
|---|---|---|
| T-164.6.5-28 | mitigate | Task 1 was read-only. No option-setter call was added. Gate 5 scans structurally with comments and docstrings stripped (N6 RED, N7 control GREEN). |
| T-164.6.5-29 | mitigate | The sentence was reconciled against a dated reading and the false clause removed. Gate 6 and the existing hand-typed pins go RED if it returns (N9). |
| T-164.6.5-30 | mitigate | Built on the existing predicate. Gate 3 covers 5 blip shapes (N4 RED). |
| T-164.6.5-31 | mitigate | Raised to ERROR. Gate 1 checks the level both absolutely and relative to the env arm (N1 RED). |
| T-164.6.5-32 | mitigate | The whole-family credential-free and blame-free fence passes over the reworded constant. It was not loosened. |
| T-164.6.5-SC | accept | No package installed. |

## Known Stubs

None.

## Self-Check: PASSED

- All six modified files exist, and all were committed in `7a7a310af` or `3b313a94e`.
- `git log --oneline --all` shows both `7a7a310af` and `3b313a94e`.
- Both verify commands pass with non-zero collected counts: 127 passed + 1 skipped, and 12 passed.
