---
phase: 164.5-baseline-snapshot
plan: 07
type: execute
wave: 2
depends_on: ["164.5-01"]
files_modified:
  - src/__tests__/csv-finalize-concurrent-never-classified.test.ts
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CRIT-6, VAC-07, VAC-07-DEFER]

estimate:
  tokens: 75000
  raw_tokens: 37500
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Two concurrent `csv-finalize` POSTs on ONE never-classified `wizard_session_id`, driven from TWO clients with SEPARATE access tokens, produce exactly one 2xx applied receipt and one honest raced refusal, with `category_id` holding the winner."
    - "The spec was OBSERVED RED with the serialization fence removed and GREEN with it restored, and both observations are pasted into the SUMMARY."
    - "The spec EXECUTES in CI — it is not env-gated into a permanent silent skip."
    - "REQUIREMENTS flips VAC-07 to Complete only AFTER both observations exist."
  artifacts:
    - src/__tests__/csv-finalize-concurrent-never-classified.test.ts
    - .github/workflows/ci.yml
    - .planning/REQUIREMENTS.md
  key_links:
    - "plan 01's booted local-stack lane -> this spec's substrate. The pg-lane is bare Postgres and structurally cannot host it."
    - "two supabase-js clients with SEPARATE access tokens -> two PostgREST connections -> the race window can actually open."
---

<objective>
⚠️ **CONTINGENCY — this plan rests on an UNPROVEN precondition.** `scripts/local-stack/run.sh up`
had never been run as of planning time (research declined, to avoid Docker side effects), and plan
01 is instructed to HALT rather than weaken `load_baseline()`'s FATAL if it fails on Docker or the
2.84.2-vs-2.98.2 CLI skew. If plan 01 halts, THIS PLAN IS STRUCTURALLY IMPOSSIBLE: criterion 6 then
DEFERS to its own phase, VAC-07 stays `Pending` in REQUIREMENTS under `[VAC-07-DEFER]` (which
already forbids scoring it early), and the phase reports that plainly rather than substituting a
weaker substrate. Do NOT fall back to the pg-lane — it has no PostgREST or GoTrue and structurally
cannot host this spec.

Criterion 6 / VAC-07: Phase 159's two blocked items close as ONE spec — two concurrent
`csv-finalize` POSTs on one never-classified `wizard_session_id`; exactly one 2xx applied receipt,
one honest raced refusal, `category_id` holds the winner. Bounded to one spec, no production code
changes.

⛔ **SUBSTRATE CORRECTION — the ROADMAP's "on the pg-lane" is wrong for this half, and this is an
ordering dependency, not a wording quibble.** `csv-finalize` and `withAuth` reach the database
through supabase-js, i.e. PostgREST + GoTrue over HTTP. `scripts/local-stack/run.sh` states it as a
hard boundary: *"Lane 2 cannot be lane 1 … A bare Postgres cluster serves none of those, so it
structurally cannot host this spec."* The correct substrate is the **local-stack lane**, which is
exactly what plan 01 makes bootable. Hence `depends_on: 164.5-01`.

⛔ **THE ANTI-VACUITY TRAP THIS SPEC WOULD OTHERWISE FALL INTO.** A single-client `Promise.all` is
a test that CANNOT FAIL: two supabase-js calls on ONE client serialize over one pooled connection,
so the race window never opens and the spec passes with AND without the fence.
`src/__tests__/scenario-commit-batch-race.test.ts` records this as a measured lesson (H-0033 /
H-0036) and drives its two RPCs from two clients with SEPARATE access tokens — separate sessions,
separate PostgREST connections. Copy that.

⛔ **THE SECOND TRAP: a spec that cannot EXECUTE.** `src/__tests__/csv-finalize-rpc.test.ts` is a
tombstone for exactly this — six live-DB cases that skipped silently in every CI shard over a
function that had been DROPped, because the live-DB flag is false in every CI vitest shard by
explicit instruction. *"Coverage that cannot execute is worse than absent coverage."* If this spec
is env-gated, this plan must say what makes it EXECUTE in CI, or it ships the same defect.

**D-05 / `[VAC-07-DEFER]` scoring fence:** REQUIREMENTS flips VAC-07 to Complete ONLY after the
spec is observed RED with the fence removed and GREEN with it restored. Both observations must be
pasted into the SUMMARY. An assertion that they were run does not satisfy this project's
anti-vacuity rule.

⚠️ **MEASURED: there is NO advisory lock in the csv-finalize path.** The serialization this spec
tests is the existing double-submit idempotency fence, not an advisory lock. The advisory-lock item
routed here is `161.1-D1` and concerns the `compute_jobs` mark RPCs — different objects, different
substrate. It is plan 08, not this plan.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-CONTEXT.md
@.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-PATTERNS.md
@.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-01-SUMMARY.md
@src/__tests__/scenario-commit-batch-race.test.ts
@src/__tests__/csv-finalize-rpc.test.ts
</context>

## Artifacts this phase produces

This plan produces: `src/__tests__/csv-finalize-concurrent-never-classified.test.ts` (new), a CI
wiring change in `ci.yml` that makes it execute, and the VAC-07 row in
`.planning/REQUIREMENTS.md` flipped to Complete.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the two-client concurrent csv-finalize spec on the local-stack lane</name>
  <read_first>
    - `src/__tests__/scenario-commit-batch-race.test.ts` — the whole file: its H-0033 / H-0036 comment explaining WHY two independent user-scoped sessions are required, its two-client setup, and its `Promise.all` firing site.
    - `src/app/api/strategies/csv-finalize/route.ts` — the never-classified branch the race targets (the "NEVER CLASSIFIED -> FILL — and never before clock safety has been…" comment), and the double-submit idempotency fence the route relies on. Read enough to name the exact refusal shape the loser receives.
    - `supabase/migrations/20260728120000_csv_finalize_double_submit_idempotency.sql` — the fence itself, since removing it is the RED arm.
    - `scripts/local-stack/run.sh` — its `.stack-env` output and its lifecycle commands. ⚠️ Nothing in the tree consumes `.stack-env` yet; this spec is the first consumer, so its wiring is NEW work.
    - `src/__tests__/csv-finalize-rpc.test.ts` — its tombstone header, as the shape to avoid.
  </read_first>
  <files>src/__tests__/csv-finalize-concurrent-never-classified.test.ts</files>
  <behavior>
    - Given ONE `wizard_session_id` that has never been classified, two concurrent POSTs to `csv-finalize` from two clients with SEPARATE access tokens produce exactly ONE 2xx applied receipt.
    - The other POST returns an honest raced refusal — a named, specific status and error shape, asserted exactly, never merely "not 2xx".
    - `category_id` afterwards holds the WINNER's value, and it is the value from the response that returned 2xx, not a hardcoded expectation.
    - Exactly one row was written: assert the applied-receipt count is 1, so a double-apply is caught as well as a double-refusal.
  </behavior>
  <action>
    Write the spec against the local-stack lane, reading its connection details from
    `scripts/local-stack/.stack-env`. Create two supabase-js clients, each with its OWN session and
    its OWN access token for the SAME user, following `scenario-commit-batch-race.test.ts`'s setup.
    ⛔ Do not use one client and `Promise.all`; carry that file's H-0033 comment across in your own
    words so the next reader knows why.

    Seed a wizard session in the never-classified state, fire both POSTs with `Promise.all`, and
    assert the four behaviors above.

    Assert the loser's refusal by its SPECIFIC status and error shape, taken from the route's own
    code. A `expect(res.status).not.toBe(200)` assertion would pass for a crash, a timeout or an
    auth failure and is therefore not a test of the race.

    The spec must assert its OWN seeded invariant, never a global empty-state — the lane may be
    reused and a global assertion is unreliable.

    If the spec is env-gated on the lane being up, the gate must FAIL LOUD when the lane is absent
    in CI rather than silently skipping. Name the environment variable and state in a header comment
    exactly what makes the spec execute — task 2 wires that.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/csv-finalize-concurrent-never-classified.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/csv-finalize-concurrent-never-classified.test.ts` passes against a booted local-stack lane, and its output shows the test EXECUTED rather than being skipped.
    - The file creates two distinct clients with two distinct access tokens — `grep -c 'createClient' src/__tests__/csv-finalize-concurrent-never-classified.test.ts` is at least 2 and the two sessions are visibly separate.
    - The loser assertion names a specific status and error shape; `grep -c 'not.toBe(200)' <file>` is 0.
    - The spec asserts its own seeded rows, not a global count.
  </acceptance_criteria>
  <done>The race spec exists, drives two genuinely concurrent sessions, and asserts one winner, one named refusal and the winner's `category_id`.</done>
</task>

<task type="auto">
  <name>Task 2: Make it EXECUTE in CI, and prove the gate fires when the lane is absent</name>
  <read_first>
    - `.github/workflows/ci.yml` — the vitest shard jobs and the explicit instruction that sets the live-DB flag false in every shard; the `frontend` aggregator's `needs:` list AND its result loop. ⚠️ A job in only one of those two lists is a check that cannot block a merge.
    - `scripts/local-stack/run.sh` — its `up` / down lifecycle and its Docker requirement, so the CI job can boot and tear down the lane.
    - `.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-01-SUMMARY.md` — the measured boot time of `run.sh up`, which sets this job's `timeout-minutes`.
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Wire the spec so it runs in CI. Add a job that boots the local-stack lane, runs this spec (and
    only this spec plus any sibling lane specs), and tears the lane down. Do NOT enable the live-DB
    flag in the existing vitest shards — those are deliberately off and widening them is out of
    scope.

    Add the new job to BOTH the `frontend` aggregator's `needs:` list AND its result loop, or it
    cannot block a merge. Paste both greps into the SUMMARY.

    ⛔ DECIDE AND RECORD THE RESULT-LOOP ARM — do not leave it to the default by omission.
    ⚠️ LINE CITES RE-RESOLVED 2026-09-07 by plan 164.5-03, which inserted 62 lines into the
    `sql-gate-lint` job and shifted everything below it: the ranges below read `1877-1882` and
    `1867-1882` before that commit. Re-resolve them again before use — cite the PROSE
    (`⛔ The DB-touching jobs' ban on new `needs:` edges does NOT apply to this one`), never the
    number, since plan 06 also edits this file.
    ⚠️ RE-RESOLVED AGAIN 2026-09-07 AT EXECUTE TIME, exactly as the paragraph above instructs:
    plan 06 landed and this plan's own `frontend-local-stack` job then inserted ~160 more lines,
    so the ranges below — which read `1939-1944` and `1929-1944` while this plan was pending —
    now resolve to `2078-2083` and `2068-2083`. The PROSE quoted above is the durable anchor;
    the numbers are a dated reading and were corrected here rather than left to send the next
    reader at the wrong lines.
    `ci.yml:2078-2083` carries an explicit BAN on new `needs:` edges for DB-touching jobs (mutex /
    `if:` divergence), with per-job carve-outs reasoned in place for `sql-gate-lint` and
    `sql-mutation`. This job is DB-touching, so it needs the same treatment: write a carve-out
    reasoning paragraph in the shape of `ci.yml:2068-2083` naming why this job is safe to add,
    and state whether it takes the loop's STRICT default or a tolerance arm. Undecided, this job
    either ships advisory (defeating this task's purpose) or reddens events where Docker/lane
    setup legitimately cannot run.

    Set `timeout-minutes` from plan 01's measured `run.sh up` wall clock plus the spec's runtime,
    with headroom, and record the derivation in a comment beside the value.

    ⛔ The spec must FAIL, not skip, when the lane is unavailable. Prove it: run the job's command
    with the lane deliberately down and observe a non-zero exit naming the missing lane. Paste that
    run. A silent skip here reproduces the `csv-finalize-rpc.test.ts` tombstone exactly.

    Add a step comment recording that this is the first consumer of `scripts/local-stack/.stack-env`.
  </action>
  <verify>
    <automated>grep -c 'csv-finalize-concurrent' .github/workflows/ci.yml && grep -n 'local-stack' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - The new job appears in `ci.yml`, in the `frontend` aggregator's `needs:` list, and in its result loop — all three greps pasted into the SUMMARY.
    - Running the spec with the lane down exits non-zero and names the missing lane — pasted, with its exit code. It must not report a skip.
    - The job's `timeout-minutes` carries a comment deriving it from plan 01's measured boot time.
    - The existing vitest shards' live-DB flag is unchanged: `git diff .github/workflows/ci.yml` shows no edit to it.
  </acceptance_criteria>
  <done>The spec executes on every relevant CI run, blocks a merge through the aggregator, and fails loud rather than skipping when its substrate is missing.</done>
</task>

<task type="auto">
  <name>Task 3: Observe RED with the fence removed, GREEN with it restored, then flip VAC-07</name>
  <read_first>
    - `supabase/migrations/20260728120000_csv_finalize_double_submit_idempotency.sql` — the fence to neuter.
    - `.planning/REQUIREMENTS.md` — the VAC-07 row and its current `Pending` status with the `[VAC-07-DEFER]` note.
    - `TODOS.md` — the `[VAC-07-DEFER]` entry, which forbids scoring VAC-07 earlier.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    MUTATION PROOF, on the lane only — this never touches shared TEST or PROD.

    Take a byte backup with `cp` first. ⛔ `git checkout --` is BANNED as the restore mechanism: it
    restores to HEAD and silently destroys the harness's own uncommitted edits. Re-take the backup
    after every edit and verify each restore by grepping for the newest change, not by hash alone.

    RED: remove the serialization fence from the lane's schema (drop or neuter the idempotency
    constraint/guard the route relies on), re-run the spec, and observe it FAIL with a named
    assertion — two applied receipts, or a `category_id` that is neither winner's. Record the exact
    failing assertion text. ⚠️ A bare non-zero exit is not enough: a broken environment also exits
    1, so the RED observation must name the failing assertion.

    GREEN: restore the fence from the byte backup, re-run, observe pass.

    Paste BOTH runs verbatim with exit codes and the named assertion into the SUMMARY.

    ONLY THEN flip VAC-07 in `.planning/REQUIREMENTS.md` from `Pending` to `Complete`, with a dated
    note citing both observations and where they are recorded. Use a SCOPED edit of that row only.

    Also correct the requirement's own text if it still says the spec runs "on the pg-lane" — the
    substrate is the local-stack lane, and leaving the wrong substrate in REQUIREMENTS would send
    the next reader to a lane that structurally cannot host it.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/csv-finalize-concurrent-never-classified.test.ts && grep -n 'VAC-07' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains the RED run with its NAMED failing assertion and its exit code, and the GREEN run with its exit code.
    - The RED failure is an assertion failure, not an environment error — the named assertion text distinguishes them.
    - `.planning/REQUIREMENTS.md` shows VAC-07 as Complete with a dated citation of both observations.
    - The REQUIREMENTS text for VAC-07 names the local-stack lane, not the pg-lane.
    - `git status --porcelain supabase/` is empty — the fence was restored.
  </acceptance_criteria>
  <done>The scoring fence in `[VAC-07-DEFER]` has been satisfied by observation, and VAC-07 is Complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user -> `csv-finalize` route | `withAuth`-guarded HTTP surface writing wizard-session state. |
| concurrent sessions -> one wizard session row | Two simultaneous requests contend for one never-classified row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5-25 | Tampering | A double-apply on one never-classified wizard session | high | mitigate | The spec asserts exactly one applied receipt and a `category_id` equal to the winner's, and was observed RED with the fence removed. |
| T-164.5-26 | Repudiation | A concurrency test that cannot fail (single-client `Promise.all`) | critical | mitigate | Two clients with separate access tokens, per the measured H-0033 / H-0036 lesson; and the RED arm must name its failing assertion. |
| T-164.5-27 | Repudiation | A live-DB spec that skips silently in every CI shard | critical | mitigate | Task 2 boots the lane in its own job, wires it into the aggregator in both lists, and proves a lane-down run exits non-zero naming the lane. |
| T-164.5-28 | Tampering | A mutation harness destroying its own uncommitted work | high | mitigate | Byte backups via `cp`; `git checkout --` explicitly banned; restore verified by grepping the newest change. |
| T-164.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan. Nothing to audit. |
</threat_model>

<verification>
- `npx vitest run src/__tests__/csv-finalize-concurrent-never-classified.test.ts` green against a booted lane, EXECUTED not skipped.
- Lane-down run exits non-zero naming the lane.
- RED (fence removed) and GREEN (fence restored) both pasted with exit codes; RED names its assertion.
- The new CI job appears in the aggregator's `needs:` list AND its result loop.
- Full vitest suite serialized and green; `npm run lint`, `npm run typecheck` clean.
- `npm run check:planning-hygiene` exit 0 after the REQUIREMENTS edit.
</verification>

<success_criteria>
Criterion 6 is met: the concurrent csv-finalize spec exists on its correct substrate, was observed
RED with the serialization removed and GREEN with it restored, executes in CI rather than skipping,
and only then did REQUIREMENTS flip VAC-07 to Complete.
</success_criteria>

<output>
Create `.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-07-SUMMARY.md` when done.
</output>
