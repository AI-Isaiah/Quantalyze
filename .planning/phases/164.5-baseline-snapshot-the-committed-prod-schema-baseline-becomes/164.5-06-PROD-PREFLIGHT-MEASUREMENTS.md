# DRIFT-04 — PROD pre-flight measurements, 2026-09-07

⛔ **READ THIS FIRST — WHAT DID AND DID NOT RUN.**
`scripts/preflight-drop-allocator-fn.sh` **DID NOT RUN** against PROD when these measurements were
taken. What follows are the SAME four measurements, taken by hand, read-only, through the
authenticated PROD connection. They are evidence for the decision; they are **not** a substitute
for the gate. **The SHA-bound gate run is still OWED.** No write of any kind was issued.
Founder decision 2026-09-07: pre-flight only, then report.

⚠️ **CORRECTION 2026-09-07 — this section previously gave the operator WRONG instructions and is
the reason it is being rewritten rather than quietly edited.** It said the gate needs
`SUPABASE_PROJECT_REF`, `SUPABASE_DB_HOST`, `SUPABASE_DB_PASSWORD` and `SUPABASE_ACCESS_TOKEN`, and
quoted `SUPABASE_PROJECT_REF is not configured` as the refusal. **That is no longer true.** The
founder took the root-cause fix for reviewer finding HIGH-2: `assert_credentials` now demands only
what the run actually consumes, and all three Supabase variables were DROPPED — they were asserted
non-empty and never read, so they constrained nothing about which database answered. A document
telling an operator to set a variable the gate ignores is this phase's own defect class.

## HOW TO RUN THE GATE (current, 2026-09-07)

**No database password is required.** The Supabase CLI is authenticated and LINKED to prod
(`khslejtfbuezsmvmtsdn`, "quantalyze") and reaches it through the Management API on its stored
keychain token. Verified working: `supabase db query --linked` returns the PRODUCTION marker.

What the gate now requires:

| Variable | Required? | Why |
|---|---|---|
| `ALLOC_EXPECT_DB_MARKER` | **YES, always** | Genuinely consumed by IDENTITY. Set it to `PRODUCTION`. ⛔ An EMPTY pin makes the containment test match everything — a vacuous pass, which is why absence is a hard failure. |
| `ALLOC_DB_IDENTITY_CMD` | **YES, always** | The positive control. Without it `--expect-absent` would certify an absence it never measured. |
| `ALLOC_SIGNATURE_CMD` | **YES, always** | The measurement both modes turn on. |
| `ALLOC_BODY_FETCH_CMD`, `ALLOC_DEPENDENTS_CMD`, `ALLOC_PLPGSQL_CALLERS_CMD`, `ALLOC_EXTENSION_CMD` | `--expect-present` only | Assertions (a), (b.1), (b.2) and (c) have no subject once the object is gone. |
| `ALLOC_CALLS_CMD` | when the probe reports PRESENT | Refused lazily at `run_reader`, deliberately — hoisting it would be theatre one variable later. |
| `SUPABASE_PROJECT_REF` / `SUPABASE_ACCESS_TOKEN` / `SUPABASE_DB_PASSWORD` | **NO — dropped** | Never read. A ref naming PROD beside readers pointed at TEST used to pass this check. IDENTITY does that job by measurement instead. |

Each reader must emit `rows=<N>` then N payload lines. Point them at
`supabase db query --linked -o csv "…"` (add `-v ON_ERROR_STOP=1` for any psql-based recipe).

## Database identity (the new positive control, HIGH-1's fix)

```
which_database : ⛔ PRODUCTION — real customer data. No seeds, no fixtures, no DELETE without a scoped id.
current_database : postgres          ← proves nothing on its own; the MARKER is the control
project ref      : khslejtfbuezsmvmtsdn   (== PROD_PROJECT_REFS, src/lib/test-safety.ts:26)
```
⭐ `ALLOC_EXPECT_DB_MARKER` now has a real value to pin: the substring **`PRODUCTION`**. The
pre-flight refuses without it, so this must be set before the credentialed run.

## The object, live

```
oid              : 21269
pronargs         : 11
identity_args    : p_user_id uuid, p_portfolio_id uuid, p_exchange text, p_label text,
                   p_strategy_name text, p_api_key_encrypted text, p_api_secret_encrypted text,
                   p_passphrase_encrypted text, p_dek_encrypted text, p_nonce text,
                   p_kek_version integer
prosecdef        : true     (SECURITY DEFINER)
owner            : postgres
live ACL         : postgres=X/postgres | authenticated=X/postgres | service_role=X/postgres
```
✅ **Signature MATCHES the migration's DROP exactly** — 11 args, same order, same types.
✅ **BOTH grantees confirmed live.** For a function `GRANT ALL` IS `EXECUTE`, so `X` for
`authenticated` and `service_role` is exactly what `baseline.sql:13755-13756` records. TODOS named
only `authenticated`; the correction stands and is now measured against PROD, not inferred.

## Assertion (a) — live body vs the committed baseline: **PASS**

```
PROD     md5(prosrc) = 325b9b523fa40ef0021fa716766e7d0c   len 2271
baseline md5(body)   = 325b9b523fa40ef0021fa716766e7d0c   len 2271
```
Byte-identical. (`pg_proc.prosrc` is exactly the text between the dollar-quote delimiters, which is
what makes this comparison exact rather than approximate.)

## Assertions (b.1) / (b.2) — dependents: **ZERO, both ways**

```
b.1 pg_depend rows (deptype<>'i', classid<>0) : 0
b.2 pg_proc bodies naming it (oid <> 21269)   : 0
    views naming it                            : 0
```
⭐ **(b.2) is the reader added for MED-1, and it closes that gap on real PROD.** `pg_depend`
records nothing for a plpgsql body calling another function, so (b.1) alone could never have seen
an in-database caller — and a bare `DROP` would not have aborted on one either. Measured: there is
no such caller.

## Assertion (c) — call evidence: **installed, and ZERO invocations**

```
pg_stat_statements installed : 1   ← so the ABSENT/abort path (exit 6) does not fire
matching statements          : 5
total calls                  : 5
window since                 : 2026-04-05 04:34:10+00
track mode                   : top
```
⛔ **The 5 are NOT invocations.** Every one is DDL from the original hand-creation:

| calls | statement |
|---|---|
| 1 | `COMMENT ON FUNCTION create_allocator_connected_strategy IS 'Atomic api_keys + strategies…` |
| 1 | `REVOKE ALL ON FUNCTION create_allocator_connected_strategy FROM PUBLIC, anon` |
| 1 | `GRANT EXECUTE ON FUNCTION create_allocator_connected_strategy TO authenticated` |
| 1 | `-- STEP 2: create_allocator_connected_strategy RPC (SECURITY DEFINER)` |
| 1 | `-- STEP 3: self-verifying DO block` |

**Zero `SELECT create_allocator_connected_strategy(...)` entries.** So (c) reads zero invocations
over a window opening 2026-04-05.

⭐ Unplanned finding: `pg_stat_statements` has captured the HAND-CREATION ITSELF — the `COMMENT`,
the `REVOKE`, the `GRANT` and two comment banners of a script that exists in no migration. That is
direct positive evidence for DRIFT-04's core claim, not an inference from absence.

⚠️ **(c) remains NECESSARY BUT NOT SUFFICIENT** and must not be read as proof of never: the store
is bounded, resettable and evicting, and `track=top` records no nested calls. It is not the
authorising basis — see below.

## The authorising basis is NOT (c)

`create_allocator_connected_strategy` has been **NON-COMPLETABLE for every caller since
2026-08-06**. It never sets `strategies.capital_ownership`, and the BEFORE-INSERT trigger
`trg_portfolio_strategies_own_capital_only` → `guard_allocation_requires_own_capital`
(`baseline.sql:5513`, ARM 2: `v_strategy_owner = v_portfolio_owner AND v_mark IS DISTINCT FROM
'own_capital'`) raises on its third INSERT and rolls back all three. Measured on the pg-lane with
the real function and trigger bodies, with a control arm (trigger detached → completes) proving the
fixture faithful. That argument is role-independent and window-independent, which (c) can never be.

TODOS' *"MEASURED 2026-08-29: the function still WORKS"* checked only the FIRST insert's column
completeness — 23 days after the blocking trigger shipped. Corrected.

## TEST — the migration reviewer's HIGH, now measured

```
overloads on TEST : 0        (marker: ✅ TEST (shared with other CI))
```
✅ **Confirms the permanent-TEST-exception disposition.** The function was hand-created on PROD and
does not exist on TEST, so `20260908120000` would raise `42883` there. Under Phase 164.8
TESTPREPROD's apply-on-merge, `supabase db push --include-all` would ABORT THE BATCH and wedge every
later migration to TEST. Booked as `[164.5-TEST-EXCEPT-DRIFT04]`, not as apply-debt.

## Verdict on the measurements

All four assertions are satisfied and none aborted. Nothing here authorises the apply by itself —
the gate script has still never run against PROD, and that is the remaining precondition.
