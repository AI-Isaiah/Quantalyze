---
phase: 164.5-baseline-snapshot
plan: 05
subsystem: ci-gates
tags: [vac-08, ledger-drift, ratchet, anti-vacuity, migrations]
status: partial
requires:
  - scripts/test-ledger-drift-check.sh (the RATCHET block and its LEDGER_BASELINE_FILE seam)
  - TODOS.md [VAC08-LEDGER-32] (reserves the TEST-apply decision to the founder)
provides:
  - 31 per-name dispositions in scripts/vac08-ledger-baseline.txt
  - src/__tests__/vac08-ledger-dispositions.test.ts (independent re-derivation + AIM)
affects:
  - VAC-08's next credentialed run (reading only; no measurement changed)
tech-stack:
  added: []
  patterns:
    - floor-plus-contract-test (as in src/__tests__/mutation-runner-floors.test.ts)
    - neuter/observe-RED/restore-from-byte-backup (cp, never `git checkout --`)
key-files:
  created:
    - src/__tests__/vac08-ledger-dispositions.test.ts
  modified:
    - scripts/vac08-ledger-baseline.txt
decisions:
  - "The 'why' half of all 31 dispositions is UNIFORM and honest — no automated mechanism applies migrations to TEST at all — rather than inventing per-entry categories that would look more resolved than the evidence supports."
  - "The RED proof mutates the REAL baseline file and restores from a `cp` byte backup, instead of adding an env seam to the test. An env override would let CI point the pin at a file that is not the one shipped."
metrics:
  duration: ~35 min
  completed: 2026-09-07
actuals:
  tokens: 21000
  tasks: 2
  commits: 2
---

# Phase 164.5 Plan 05: VAC08-LEDGER dispositions Summary

All 31 unledgered repo migrations are now dispositioned by name in
`scripts/vac08-ledger-baseline.txt`, and a vitest re-derives that fact
independently of the gate that cannot see it. **Task 3 was NOT executed** — it is
a `checkpoint:decision` with `gate="blocking-human"` and the founder has not
answered it, so `.planning/ROADMAP.md` and `TODOS.md` are UNTOUCHED by this plan.

## Status

| Task | What | Status | Commit |
|---|---|---|---|
| 1 | Per-name disposition for each of the 31 entries | DONE | `2572c5cc` |
| 2 | Independent re-derivation + both ratchet edges observed | DONE | `b8c3cc9d` |
| 3 | CHECKPOINT — amend ROADMAP criterion 5 | **BLOCKED ON FOUNDER GATE** | — |
| 4 | Apply the criterion-5 amendment + TODOS update | NOT STARTED (gated behind task 3) | — |

## Task 1 — 31 dispositions

Every non-comment entry now carries a trailing `# <reason>` stating what the
migration does and why it has no TEST ledger row. A new dated header paragraph
records the count (31), the disposition seam, and the honest reported wording.

**The "why" half is uniform, and that is a finding, not laziness.** The plan
offered four honest categories (predates conventions / superseded / PROD-only /
un-applied). Measured in this tree, only the fourth is true, and it is true for
all 31: no automated mechanism applies migrations to TEST at all.
`.github/workflows/supabase-migrate.yml:3` — *"Applies Supabase migrations to
production automatically on push to main"* — is the only apply path, and CI's
`sql-tests` job has no apply step. So each line says, exactly as the plan
instructed, `UNAPPLIED to TEST, awaiting the founder decision [VAC08-LEDGER-32]
reserves.` No line invents a tidier category to look resolved. The per-entry
prose that differs is the *what*, drawn from each migration's own header.

**`[SEC]` marks the three security migrations still listed** —
`20260529150000_lock_profile_privileged_columns`,
`20260715120000_grant_anon_execute_current_user_has_app_role`,
`20260814120000_wizard_rpcs_revoke_authenticated`. (The plan named four; the
fourth, `20260823120000`, is the applied line already removed — noted in the
header so a future reader does not go hunting.) Each of the three states on its
own line that *a TEST-side test asserting these grants may be asserting them
against a schema that never received it*, and that the object-level probe is
owed and out of scope here.

### Acceptance criteria — measured

```
$ test "$(grep -vc '^#' scripts/vac08-ledger-baseline.txt)" = "31"
count-is-31 exit=0
$ test "$(grep -v '^#' scripts/vac08-ledger-baseline.txt | sed '/^$/d' | grep -c '#')" = "31"
all-31-dispositioned exit=0
```

**The gate's own name parse is byte-identical before and after** — this is the
criterion that matters, and it was verified rather than assumed from reading the
`sed`:

```
$ sed 's/#.*//' scripts/vac08-ledger-baseline.txt | sed 's/[[:space:]]*$//' | sed '/^$/d' > parse.AFTER.final.txt
$ diff parse.BEFORE.txt parse.AFTER.final.txt
PARSE DIFF exit=0
$ shasum parse.BEFORE.txt parse.AFTER.final.txt
087de90982b88a834ece166efd7a00969e95de06  parse.BEFORE.txt
087de90982b88a834ece166efd7a00969e95de06  parse.AFTER.final.txt
```

(The diff produced no output. `parse.BEFORE.txt` was captured from the committed
file before any edit.)

```
$ bash scripts/test-ledger-drift-check.sh --self-test
  ok   missing-ledger-row RED (exit 1)
  ok   body-mismatch RED (exit 1)
  ok   empty-body-check-list RED (exit 1)
  ok   zero-comparisons RED (exit 1)
  ok   green path (exit 0)
VAC-08 repo-vs-TEST ledger and body drift gate: self-test OK (5/5 arms — all four red modes fire and the green path passes).
SELF-TEST exit=0
```

No line says or implies TEST is missing 31 features. The single tree match for
`TEST is missing` is the file's own pre-existing **prohibition** at line 18-19
("Do not read this list as …"), which was left untouched as instructed.

## Task 2 — independent re-derivation and both ratchet edges

`src/__tests__/vac08-ledger-dispositions.test.ts` reads the baseline with its
own regexes and its own line split. It does **not** import or shell out to
`test-ledger-drift-check.sh` — a test that agreed with the gate by construction
would prove nothing about the file, and the gate is structurally blind to this
property anyway.

```
$ npx vitest run src/__tests__/vac08-ledger-dispositions.test.ts
 Test Files  1 passed (1)
      Tests  10 passed (10)
```

Ten arms: the count pin (both directions, with a message naming which way it
moved), the disposition pin, a name-hygiene pin, the `[SEC]` disclosure pin with
a **closed** marker set, the positive `0 NEW drift` pin, the negative
strong-claim pin, and **four AIM arms** that drive the same functions over
synthetic corpora whose answers are known and are NOT the empty set.

### The pin can fail — observed, not asserted

Neutered by appending one undispositioned entry to the **real** file, then
restored from a `cp` byte backup (`git checkout --` is banned as a restore
mechanism here). Both failures name their own assertion:

```
AssertionError: ENTRY_COUNT pin is 31 but the file GREW to 32: new drift was
baselined — that is a decision, record it in the file header and move the pin
deliberately: expected 32 to be 31

AssertionError: an entry was added to the VAC-08 ledger baseline with no
disposition. The gate CANNOT see this — it strips comments before comparing —
so this test is the only thing that can. […]: expected [ Array(1) ] to deeply equal []
+ [ "line 106: 20261231000000_synthetic_no_disposition" ]

 Tests  2 failed | 8 passed (10)
```

And the other direction, by deleting one entry:

```
AssertionError: ENTRY_COUNT pin is 31 but the file SHRANK to 30: a migration
reached TEST — record the apply in the file header, then lower the pin:
expected 30 to be 31
```

Restored and re-verified by hash both times:
`3860991b83752e81993775a504ab8df927baf567` == the committed file;
`git status --porcelain scripts/` empty.

⚠️ **Deviation from the plan's wording (Rule 3).** The plan asked for the RED
proof to run against *a copy of the file with the test pointed at it*. The test
has no path seam, and adding an env override would let CI point the pin at a
file that is not the one shipped — the pin's whole value is that it is nailed to
the real artifact. The repo's own neuter/observe-RED/restore convention was used
instead, which is a stronger proof of the same property.

### Both ratchet edges, driven through `LEDGER_BASELINE_FILE`

The real `scripts/vac08-ledger-baseline.txt` was never read or written by this
proof — every arm points `LEDGER_BASELINE_FILE` at a throwaway fixture in
`mktemp -d`, with stubbed ledger and body-fetch commands.

**Arm 1 — NEW drift, ISOLATED** (the baselined name is also measured-missing, so
the stale set is empty and the only defect the gate can report is the one this
arm set up — a RED must name its own failing assertion):

```
ARM: NEW drift ISOLATED — stale set empty, one unbaselined measured-missing name
::error::VAC-08 …: 1 repo migration(s) are not present in the TEST ledger and are NOT baselined:
::error::  20260829120000_demo
::error::--- TEST ledger shape (diagnostic, does not decide pass/fail) ---
::error::Do not apply migrations to TEST to make this gate green — TEST is shared.
  >>> exit=1
```

**Arm 2 — STALE:**

```
ARM: STALE — baselined name is no longer measured-missing
::error::VAC-08 …: 1 baseline entr(y/ies) are no longer MEASURED absent.
::error::Either the migration reached TEST, or its file left the repo. Both mean the
::error::same thing: the line is stale. The baseline may only shrink — delete these
::error::  20260829120000_demo
  >>> exit=1
```

**Arm 3 — CONTROL GREEN** (load-bearing: without it, a gate that failed
unconditionally would also pass arms 1 and 2):

```
ARM: CONTROL GREEN — same name, baselined AND measured-missing
  ledger presence: 1 absent, all 1 baselined (see …/baseline.fixture.txt); 0 NEW drift.
::notice::VAC-08 …: ledger and body checks clean.
  >>> exit=0
```

A fourth arm was also run showing both defects reported together in one run.

```
$ git status --porcelain scripts/
(empty — the real baseline was never modified during the proof)
```

## Verification

| Check | Result |
|---|---|
| `bash scripts/test-ledger-drift-check.sh --self-test` | exit 0, 5/5 arms |
| gate name-parse before/after diff | EMPTY, same sha `087de909…` |
| `npx vitest run src/__tests__/vac08-ledger-dispositions.test.ts` | 10 passed |
| `npx vitest run src/__tests__/drift-check-scripts.test.ts` (pre-existing) | 396 passed |
| `npm run typecheck` | clean |
| `npm run lint` | 0 errors (2 pre-existing warnings in unrelated UI files — out of scope) |
| `npm run check:planning-hygiene` | run as part of `npm run lint`: OK, 6106 files |

## ⛔ Task 3 — BLOCKED ON FOUNDER GATE

**Type:** `checkpoint:decision` — **Gate: `blocking-human`.** Not auto-selectable
under any mode. Tasks 1 and 2 needed no gate and are complete; only the
ROADMAP/TODOS edit is gated, and it was NOT performed.

**Untouched by this plan, deliberately:** `.planning/ROADMAP.md` and `TODOS.md`.

**Criterion 5 as it stands today** (`.planning/ROADMAP.md:1299`, verbatim):

> 5. VAC08-LEDGER-32: every one of the 32 unledgered migrations is applied to TEST or dispositioned by name; VAC-08 reports zero unledgered migrations on its next credentialed run.

**The measured facts the decision rests on** (re-verified here, not inherited):

- The file holds **31** entries, not 32. `20260823120000_revoke_api_keys_insert`
  was applied to TEST on 2026-09-07 and is the only line ever removed by an apply.
- All 31 now carry a disposition; **0 did before this plan**.
- The seam is `sed 's/#.*//'` in `scripts/test-ledger-drift-check.sh`'s RATCHET
  block, which strips comments **before** comparing. Dispositions are therefore
  invisible to the gate, which is exactly why they change no measurement — and
  exactly why they cannot make the gate print anything new.
- So the disposition route yields **`0 NEW drift` with 31 named dispositions**.
  It cannot yield "zero unledgered migrations". Only applying the 31 to shared
  TEST would, and `TODOS.md [VAC08-LEDGER-32]` explicitly forbids an agent doing
  that. No migration was applied to TEST by this plan.

**The two options, unchanged from the plan:** `amend` (restate criterion 5's
second clause as the `0 NEW drift` reading and fix 32 → 31) or `hold-open` (leave
criterion 5 open pending the founder's TEST-apply decision; still correct the
standalone 32 → 31 count error).

**Criterion 5 is therefore recorded OPEN.** Its first clause ("dispositioned by
name") is now satisfied for all 31 by name. Its second clause ("VAC-08 reports
zero unledgered migrations") is NOT satisfied and cannot be satisfied on this
route — that disagreement is recorded rather than averaged, per the founder gate.

## Deviations from Plan

**1. [Rule 3 — blocking] The RED proof of the vitest pin mutates the real file
and restores from a byte backup, instead of pointing the test at a copy.**
- **Found during:** Task 2.
- **Issue:** The test hard-codes `BASELINE_PATH`; there is no seam to point it at
  a copy, and adding an env override would let the pin be aimed at a file other
  than the one shipped.
- **Fix:** Used the repo's neuter → observe RED → restore-from-`cp`-backup
  convention. Both directions observed, both naming their own assertion, restore
  verified by sha and by an empty `git status --porcelain scripts/`.
- **Files modified:** none beyond the plan's own (the mutation was reverted).
- **Commit:** `b8c3cc9d`.

**2. [Rule 1 — bug] The strong-claim scan's first cut was a one-line regex and
went red on the file's own prohibition.**
- **Found during:** Task 2.
- **Issue:** The file forbids the phrase "zero unledgered migrations" in a
  sentence whose "It is NOT" wraps onto the previous line. A single-line scan
  cannot read a wrapped sentence, so it flagged the prohibition as an assertion.
- **Fix:** Widened to a three-line window and documented in the source that the
  window was forced by measurement, not chosen for taste.
- **Commit:** `b8c3cc9d`.

**3. [cosmetic] One duplicated bare `#` line** was collapsed where the new header
paragraph met the existing one. No effect on the gate parse (verified after).

## Known Stubs

None. No stub values, placeholder text, skipped tests, or unrun `<verify>` blocks
were introduced. Every `<verify>` command in tasks 1 and 2 was run and its output
pasted above.

## What could NOT be established

- **VAC-08's actual credentialed reading was NOT observed.** The gate needs
  `TEST_SUPABASE_DB_URL`; every run here used stubs and fixtures. The claim that
  a real run prints `ledger presence: 31 absent, all 31 baselined …; 0 NEW drift`
  is a derivation from the RATCHET block plus the fixture control arm, not a
  measurement against TEST. It stays owed to VAC-08's next credentialed run.
- **Whether the 31 migrations' OBJECTS exist in TEST is unknown and unaddressed.**
  Ledger absence is not object absence. Nothing here probes for objects, and the
  three `[SEC]` entries say so on their own lines.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern, or schema change
at a trust boundary. `T-164.5-15` (an agent hand-applying migrations to shared
TEST) is mitigated by construction: this plan performed no apply, and every
ratchet arm ran against `mktemp -d` fixtures.

## Self-Check: PASSED

- Both created/modified files exist on disk (verified with `[ -f ]`).
- Both commits resolve to real objects: `2572c5cc` and `b8c3cc9d` (`git cat-file -t` → commit).
- The ROADMAP and TODOS were verified UNTOUCHED: `git status --porcelain` is clean and neither file appears in either commit.
