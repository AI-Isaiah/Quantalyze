# Phase 164.5: BASELINE-SNAPSHOT — Research

**Researched:** 2026-09-07
**Domain:** schema-drift gating, hermetic SQL text tooling, production DDL under review, pg-lane concurrency specs
**Confidence:** HIGH for criteria 1, 2, 4, 5, 8 (all measured in-session at HEAD). MEDIUM for 3 and 7 (their evidence lives in PROD, which this research deliberately did not query). MEDIUM for 6 (scope is larger than the criterion's wording implies).

⛔ **Every measurement below was taken this session on this checkout.** Where I could not measure, I say so explicitly rather than reporting a zero. Two important claims in the ROADMAP were FALSIFIED here (criterion 7's `grep` clause, and the availability of a full migration replay); one was CONFIRMED-and-extended (criterion 4a is RED today).

---

<user_constraints>

## User Constraints (from CONTEXT.md)

### Locked decisions (verbatim from `164.5-CONTEXT.md`)

- **DRIFT-04**: `create_allocator_connected_strategy` is DROPPED, not adopted. Founder
  decision 2026-08-29. `DROP FUNCTION` with the exact 11 arg types, **no `IF EXISTS`,
  no `CASCADE`**. Three pre-flight assertions must PRINT their measurements.
- **CRON-DRIFT-01-REPAIR**: ONE forward migration re-scheduling `match_engine_cron` onto
  the Vault-backed command. Pre-flight ABORTS on a live-command mismatch rather than
  overwriting an unknown command.
- **Never infer from an absent measurement.** Both prod-DDL items abort when their
  evidence source is unavailable (`pg_stat_statements` missing → abort; live command
  unreadable → abort). An absent measurement is never read as a zero.
- **Three reviewers before any apply** (migration-reviewer, rls-policy-auditor,
  silent-failure-hunter) — both prod-DDL items are on credential-bearing surfaces.
- **Ordering**: criterion 8 lands BEFORE any migration squash. A squash onto an
  unpinned, already-drifted baseline freezes the drift into the new floor permanently.
- **VAC-07 scoring fence** (`[VAC-07-DEFER]`): REQUIREMENTS flips VAC-07 to Complete only
  after its spec is observed RED with the fence removed and GREEN with it restored.

### Claude's Discretion

CONTEXT.md is INFRASTRUCTURE-classified and put no grey-area proposals to the founder. Everything not
listed as a locked decision above is planner discretion — in particular: gate placement in CI, whether
criterion 8 is a new script or an extension of an existing one, plan/wave decomposition, and the fixture
shapes for each mutation proof.

### Deferred Ideas (OUT OF SCOPE)

- The migration **squash** itself. This phase only establishes the ordering constraint that must precede it.
- Applying the remaining 31 unledgered migrations to shared TEST — `[VAC08-LEDGER-32]` states this
  "stays a founder decision, not an agent one".
- 164.1's cron-drift DETECT half (already shipped); only the REPAIR half is here.

### Fences (verbatim)

- Nothing in this phase touches shared TEST or PROD except the two reviewed migrations
  (DRIFT-04's DROP and CRON-DRIFT-01-REPAIR). Substrate for everything else is the
  pg-lane with pg_cron (Phase 164.4.1).
- Criterion 7 must consume Phase 164.7's settled `app.*` replacement mechanism
  (`system_settings` + Vault), NOT invent a second answer.
- Every gate here is proven by MUTATION in both directions, never by inspection.

</user_constraints>

<phase_requirements>

## Phase Requirements

| ID | Description | Research Support |
|----|-------------|------------------|
| **Criterion 1** | Repoint local-stack at the committed baseline | §1 — exact edit sites measured, `--print-baseline-path` seam already exists, one downstream test already pins both directions |
| **Criterion 2** | Baseline staleness (co-edit) gate | §2 — the sha256 already exists AND is already asserted by a vitest; the gap is CI placement + mutation proof |
| **Criterion 3 / DRIFT-04** | DROP the unowned PROD function | §4 — exact 11 arg types quoted verbatim from `baseline.sql:2618` |
| **Criterion 4 / DRIFT-05** | Two gates, not conflated | §5 — 4(a) measured RED today with a 3-name diff; slot in `check()` located |
| **Criterion 5 / VAC08-LEDGER-32** | Disposition the unledgered migrations | §6 — "dispositioned by name" is mechanically a line in `scripts/vac08-ledger-baseline.txt`; the ratchet's two directions read |
| **Criterion 6 / VAC-07** | Concurrent csv-finalize spec + advisory lock | §7 — ⚠️ the advisory-lock item is 161.1-D1 and is BIGGER than the criterion's one clause suggests |
| **Criterion 7 / CRON-DRIFT-01-REPAIR** | Re-schedule jobid 1 | §8 — manifest command quoted verbatim; ⛔ **two ROADMAP clauses falsified** |
| **Criterion 8 / BASELINE-CONTENT-DRIFT** | Baseline pinned to the migration chain | §3 — **implementable hermetically in 0.12 s with an existing tool; measured 6 DRIFT + 2 SNAPSHOT_MISSING today** |
| `[VAC-07-DEFER]` | Scoring fence | §7 |

</phase_requirements>

## Summary

This phase is unusual: **most of its tooling already exists and most of its gates are already RED**. The
expensive-looking criterion (8) turned out to be the cheapest, because `scripts/sql-body-normalize.mjs`
already implements exactly the comparison it needs and the hermetic migration-chain rendering already
exists as `supabase/schema/functions/*.sql`. I ran that comparison in-session: it completes in **0.12 s**
and reports **6 DRIFT, 2 SNAPSHOT_MISSING, 114 MATCH** against today's committed baseline. There is no
need to replay migrations on the pg-lane for criterion 8 — and, critically, **there is no option to**:
the chain measurably does not replay from empty (69 of 262 migrations fail, ≥6 independent root causes,
one refusing BY DESIGN).

Criterion 1 is a two-line edit with a pre-built seam (`--print-baseline-path`) and a pre-built
bidirectional pin (`src/__tests__/baseline-wiring-claim.test.ts`) that will fail the moment the repoint
lands until `BASELINE.md`'s prose is updated in the same commit. Criterion 2's sha256 already exists and is
already asserted by that same vitest — the true gap is that **zero `.github/workflows/` files reference the
baseline at all**, so the work is CI placement plus a mutation proof, not inventing a hash.

The two production-DDL criteria (3 and 7) are where the risk is. For criterion 3 I extracted the exact
11-type signature from the committed baseline without touching PROD. For criterion 7 I found **two
falsified ROADMAP clauses** that would have made the criterion unsatisfiable-as-written, plus a
manifest-oracle collision the plan must resolve explicitly.

**Primary recommendation:** plan criterion 8 FIRST as an extension of the existing
`sql-body-normalize.mjs --diff-bodies` seam driven by a small runner + allowlist, land criteria 1/2/4/5
around it as one hermetic wave, and hold criteria 3, 6 and 7 as separate later plans — 3 and 7 behind the
three-reviewer gate, 6 because it is materially larger than its one-line description.

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Baseline→chain body diff (crit 8) | Build tooling (node script) | CI job | Hermetic text comparison; no DB, no credential — belongs where `sql-body-normalize.mjs` already lives |
| Baseline staleness / co-edit (crit 2) | CI job | vitest | Already half-implemented in vitest; CI is where a PR is blocked |
| Name-set diff (crit 4a) | Build tooling (`dump-sql-functions.ts --check`) | CI (`sql-function-snapshot.yml`) | The ROADMAP names the file; it is hermetic and already CI-wired |
| Baseline-vs-LIVE (crit 4b) | Credentialed CI job | — | Needs PROD credential; rides the VAC-04 job |
| Local-stack schema load (crit 1) | Dev/CI script (`scripts/local-stack/run.sh`) | — | Lane-owned |
| DROP FUNCTION (crit 3) | Migration (PROD DDL) | Pre-flight script | Reviewed production DDL |
| Cron re-schedule (crit 7) | ⚠️ Migration **or** runbook — CONTESTED, see §8 | Pre-flight script | `20260907120000` says a migration must never schedule; the ROADMAP says write one |
| csv-finalize race spec (crit 6) | pg-lane / local-stack | Migration (advisory lock) | App-level spec needs PostgREST+GoTrue ⇒ local-stack, not bare pg-lane |
| Ledger disposition (crit 5) | Baseline text file + credentialed CI | — | `scripts/vac08-ledger-baseline.txt` is the ratchet |

---

## 1. Criterion 1 — the local-stack repoint

**Exact state, measured at HEAD.**

- `scripts/local-stack/run.sh:57` — `BASELINE_FILE="${LANE_DIR}/baseline.sql"` where `LANE_DIR="${REPO_ROOT}/scripts/local-stack"` (line 55). [VERIFIED: scripts/local-stack/run.sh:55-57]
  ⚠️ The ROADMAP and `BASELINE.md` both cite **line 50**. Both are stale; CONTEXT.md already re-measured this. Do not re-derive.
- `.gitignore:138` — `scripts/local-stack/baseline.sql`, under a comment block at lines 134-137 explaining that a dump can carry a DSN and that committing one is "a deliberate, reviewed act". [VERIFIED: .gitignore:134-138]
- The committed artifact is `supabase/schema/baseline.sql`, **701,524 bytes**, mtime 2026-09-01 12:08. [VERIFIED: `ls -la supabase/schema/`]

**Who else consumes `LANE_DIR/baseline.sql`?** Only `run.sh` itself. Every reference is inside that one file:
the header docs (lines 18-41), `load_baseline()` (lines 192-223), the `up` path (line 250), the self-test note
(line 317) and the `--print-baseline-path` case (lines 382-388). [VERIFIED: `grep -n baseline scripts/local-stack/run.sh`]
Nothing in `.github/workflows/`, `package.json` or `src/` names either path except the documentation and the
wiring test. [VERIFIED: `grep -rln 'baseline.sql' .github/ scripts/ src/ package.json supabase/`]

**What `--print-baseline-path` must report.** It already does the right thing: it prints the *resolved*
`$BASELINE_FILE` through bash (`run.sh:388`), and `src/__tests__/baseline-wiring-claim.test.ts` compares
`resolve()`d absolute paths on both sides — deliberately, because an earlier substring-matching version
would have read a perfectly ordinary repoint as UNWIRED. [VERIFIED: src/__tests__/baseline-wiring-claim.test.ts,
`laneReadsCommittedBaseline()` docblock] **So the repoint may be spelled any way you like**; no change to
`--print-baseline-path` is needed. What IS needed: after the repoint the same test flips branch and will
FAIL until `BASELINE.md`'s `## ⛔ NOT WIRED YET` section is deleted and the wiring stated. That is a
built-in, pre-existing co-edit gate for criterion 1 — the planner should treat it as the criterion's proof,
not as a surprise red.

⚠️ **A caution on "drop the `.gitignore` exclusion".** Line 138 ignores the *lane-local* path, not the
committed one — `supabase/schema/baseline.sql` is tracked and was never covered by it. Deleting line 138
therefore buys nothing mechanically once `run.sh` is repointed, and it *removes* the accidental-dump-commit
default that lines 134-137 exist to provide. The test's `else` branch asserts line 138's presence only while
UNWIRED, so keeping it is green either way. **Recommendation:** repoint first; treat the `.gitignore` deletion
as a separate, argued decision rather than a mechanical consequence. If it is deleted, the comment block above it
should move to `BASELINE.md` so the reasoning is not lost. [ASSUMED — this is my judgement, not a founder decision]

**Also note:** `run.sh` refuses to load into a non-local database (line 211) and derives a throwaway
Supabase workdir with `[db.migrations] enabled = false` (lines 60-108). None of that changes.

---

## 2. Criterion 2 — the staleness (co-edit) gate

**`BASELINE.md` already records a sha256:** `514ba9bccd3181d925860576479e1d9ed623e429b3cb8d135f70a031e24a37fb`,
in a provenance table alongside `Taken 2026-08-29`, `Supabase CLI 2.84.2` and
`Shape: 61 tables, 152 policies, 121 function statements (119 distinct names), 0 data statements`.
[VERIFIED: supabase/schema/BASELINE.md, "## Provenance" table]

**And it is already asserted** — `src/__tests__/baseline-wiring-claim.test.ts` has a second `it()`,
*"the sha256 BASELINE.md records is the sha256 of the file it records"*, which regex-extracts
`` |\s*sha256\s*|\s*`([0-9a-f]{64})`\s*| `` and compares against `createHash("sha256")` of the file. Its own
message says: *"⚠️ This is NOT the staleness gate — it says nothing about whether PROD has moved on. That is
Phase 164.5 / WINDOWS 29."* [VERIFIED: src/__tests__/baseline-wiring-claim.test.ts, second `it`]

**So criterion 2's real gap is narrower than it reads.** The hash assertion exists in vitest. What does not
exist: any `.github/workflows/` reference to the baseline at all (**0 hits**, confirmed [VERIFIED:
`grep -rln baseline.sql .github/`]), so the assertion only runs as part of the general frontend suite.

**Closest existing pattern to copy:** `sql-function-snapshot.yml` — it gates `supabase/schema/functions/`
by running `dump-sql-functions.ts --check`. `BASELINE.md`'s own UNGATED section names it as the precedent:
*"`sql-function-snapshot.yml` gates `supabase/schema/functions/`; nothing yet gates this."* [CITED:
supabase/schema/BASELINE.md, "## ⚠️ UNGATED as of this commit"]

**RED / GREEN fixtures for the mutation proof (criterion 2 says "proven by mutation, not inspection"):**

| | Fixture | Expected |
|---|---|---|
| RED | append one byte to `supabase/schema/baseline.sql`, leave `BASELINE.md` untouched | gate exits non-zero naming the sha mismatch |
| RED (reverse) | change the sha256 in `BASELINE.md`, leave the file untouched | gate exits non-zero |
| GREEN | change the byte AND update the recorded sha256 | gate exits 0 |

⚠️ Restore by **byte backup**, not `git checkout --` — MEMORY records that `git checkout --` has destroyed
uncommitted work in exactly this kind of neuter/restore harness.

---

## 3. ⭐ Criterion 8 — BASELINE-CONTENT-DRIFT (the one I was asked to establish hardest)

### The tool already exists and I ran it

`scripts/sql-body-normalize.mjs` is *"The SINGLE shared SQL-body normalizer + comparator (Phase 164.3, D-05)"*.
Its documented modes, verbatim from the header: [VERIFIED: scripts/sql-body-normalize.mjs:48-54]

```
node scripts/sql-body-normalize.mjs --normalize [file]        # stdin if no file
node scripts/sql-body-normalize.mjs --hash [file]
node scripts/sql-body-normalize.mjs --function-names <file...>
node scripts/sql-body-normalize.mjs --extract-fn <file> <name>
node scripts/sql-body-normalize.mjs --diff-bodies <snapshot.sql> <live.sql>
node scripts/sql-body-normalize.mjs --self-test
```

`--diff-bodies` takes **two SQL files** — it opens no database. Its scope boundary is explicit:
*"This module does TEXT only. It opens no database, reads no secret, and prints no body text in any reporting
mode (`--diff-bodies` emits names, argument counts, sha256 hashes and differing-line counts — nothing else)."*
[VERIFIED: scripts/sql-body-normalize.mjs:25-31] That last clause matters: this repo is PUBLIC.

Its five statuses are `MATCH`, `DRIFT`, `SNAPSHOT_MISSING`, `SNAPSHOT_ONLY`, `UNCOMPARABLE`, and
`UNCOMPARABLE` is documented as *"callers MUST fail closed on it… Reporting a pass for something that was
not compared is the precise defect this phase exists to make impossible."*
[VERIFIED: scripts/sql-body-normalize.mjs:440-447 header block]

### MEASURED IN-SESSION — the gate is already RED, and it takes 0.12 s

```
cat supabase/schema/functions/*.sql > /tmp/chain.sql
node scripts/sql-body-normalize.mjs --diff-bodies supabase/schema/baseline.sql /tmp/chain.sql
```
→ **0.118 s wall**, exit 0, 122 rows: `114 MATCH`, `6 DRIFT`, `2 SNAPSHOT_MISSING`.
[VERIFIED: run in this session, 2026-09-07, at the current working tree]

The six DRIFT rows (name, nargs, hunks):

| function | nargs | hunks |
|---|---|---|
| `check_fan_in_ready` | 1 | 5 |
| `enqueue_ledger_composite_refresh` | 0 | 15 |
| `enqueue_ledger_refresh_for_strategies` | 0 | 15 |
| `reject_sentinel_writes` | 0 | 9 |
| `retention_delete_guard` | 0 | 2 |
| `sync_strategy_analytics_status` | 1 | 38 |

The two `enqueue_ledger_*` rows are the CONTEXT.md-confirmed **superseded Lock B**
(`grep -c ledger_refresh_enabled` = 4 in the baseline) — i.e. this comparison independently reproduces
the exact drift CONTEXT.md predicts, from a different direction. That is a strong signal the comparison is
measuring the right thing.

### The name-set direction, measured the same way

```
node scripts/sql-body-normalize.mjs --function-names supabase/schema/baseline.sql   # 119 names
node scripts/sql-body-normalize.mjs --function-names supabase/schema/functions/*.sql # 120 names
```
[VERIFIED: run in this session]

- **baseline-only (1):** `create_allocator_connected_strategy` — this is DRIFT-04, reproduced mechanically.
- **chain-only (2):** `match_engine_cron_tick` (164.7's `20260907120000`), `strategy_analytics_drop_stale_error_provenance` (164.2's `20260906120000`).

119 matches `BASELINE.md`'s recorded shape ("119 distinct names") exactly — the tool and the provenance table agree.

### ⛔ The full-replay route is CLOSED, not merely expensive

I was asked to report the measured cost of a full pg-lane migration replay. **I did not run one, and it
cannot be run**: the chain does not replay from empty. `scripts/local-stack/run.sh:18-31` records the
measurement: *"262 migrations, 69 fail, 193 apply. `supabase db reset` dies at 20260416125432 (CREATE INDEX
CONCURRENTLY inside the CLI's libpq pipeline, SQLSTATE 25001), and removing the pipeline does not save it —
>=6 independent root causes remain under psql, including 20260823120000_revoke_api_keys_insert.sql which
REFUSES BY DESIGN to run against a database it cannot identify."* [VERIFIED: scripts/local-stack/run.sh:18-31,
corroborated by supabase/schema/BASELINE.md "## Why it exists rather than a migration replay"]

`scripts/pg-lane/run.sh` is not a chain replayer either — its CLI contract is
`--apply <file.sql> [<file.sql> ...] --gate <gate.sql>`, i.e. a hand-curated APPLY LIST per gate, which is
exactly why each gate carries a `RED-UNDER-SETUP` apply list. [VERIFIED: scripts/pg-lane/run.sh:17-25]
**I therefore have NO measurement of a full-chain pg-lane replay cost and must not report one.** The honest
statement is: it is not an available substrate at all.

### Recommended shape for criterion 8

Use the hermetic text-replay chain (`dump-sql-functions.ts`'s own output, which *is* "the applied migration
chain" as far as function bodies are concerned) as the `<live.sql>` side, and `baseline.sql` as `<snapshot.sql>`.
Concretely: a small runner that generates the chain in-memory (or `cat`s `supabase/schema/functions/*.sql`,
whose currency criterion 4a's sibling gate already enforces), calls `diffFunctionBodies()`, fails on any
`DRIFT` / `UNCOMPARABLE`, and consults a **dated, name-pinned allowlist** for the known-stale rows.

| | Fixture | Expected |
|---|---|---|
| GREEN | regenerate `baseline.sql` from PROD (or allowlist the 6+2 with dated reasons) | gate exits 0 |
| RED | revert one function body in `supabase/schema/functions/<fn>.sql` (or in its migration) | gate exits non-zero **naming that function** |
| RED (reverse, criterion 8's "both directions") | edit that function's body in `baseline.sql` only | gate exits non-zero naming it |

⚠️ **Scope boundary the plan must state out loud.** This covers **function bodies only**. Tables, columns,
policies and triggers are NOT compared — `dump-sql-functions.ts`'s own header records that boundary
(incremental `ALTER`s cannot be reconstructed by text-replay). A gate that reads green while blind is this
project's named defect class, so criterion 8's gate must SAY what it does not compare, in its own failure
and success output. [VERIFIED: supabase/schema/BASELINE.md "## Why it exists rather than a migration replay"]

⚠️ **Version-skew caveat inherited from `BASELINE.md`:** local CLI 2.84.2 vs CI-pinned 2.98.2. Comparing
extracted+normalized bodies (which is what `--diff-bodies` does) is precisely the documented mitigation, so
this route sidesteps the trap. Comparing whole rendered statements would not. [CITED: supabase/schema/BASELINE.md
"Version-skew caveat for whoever writes that gate"]

**Ordering:** criterion 8 before any squash — locked. The ROADMAP's stated weight (44 function names in
`supabase/migrations/**` with more than one definition, `mark_compute_job_done` × 9) is the squash's prize
and the reason the ordering is not negotiable. I did not independently re-count the 44. [ASSUMED — ROADMAP figure, not re-measured]

---

## 4. Criterion 3 / DRIFT-04 — the exact signature

**Read from the committed baseline; PROD was NOT queried.** [VERIFIED: supabase/schema/baseline.sql:2618]

```
CREATE OR REPLACE FUNCTION "public"."create_allocator_connected_strategy"(
  "p_user_id" "uuid", "p_portfolio_id" "uuid", "p_exchange" "text", "p_label" "text",
  "p_strategy_name" "text", "p_api_key_encrypted" "text", "p_api_secret_encrypted" "text",
  "p_passphrase_encrypted" "text", "p_dek_encrypted" "text", "p_nonce" "text",
  "p_kek_version" integer
) RETURNS TABLE("strategy_id" "uuid", "api_key_id" "uuid")
```

**The 11 argument types, in order:** `uuid, uuid, text, text, text, text, text, text, text, text, integer`.

So the DROP statement is:

```sql
DROP FUNCTION public.create_allocator_connected_strategy(
  uuid, uuid, text, text, text, text, text, text, text, text, integer
);
```

with **no `IF EXISTS`** (it would silently no-op on a signature mismatch — a vacuous pass) and **no
`CASCADE`** (it would silently remove dependents). [CITED: TODOS.md `[DRIFT-04]`]

Corroborating lines in the same file, all carrying the identical 11-type list:
`baseline.sql:2696` (`ALTER FUNCTION … OWNER TO "postgres"`), `:2699` (`COMMENT … 'See migration 043.'`),
`:13754` (`REVOKE ALL … FROM PUBLIC`), `:13755` (`GRANT ALL … TO "authenticated"`),
`:13756` (`GRANT ALL … TO "service_role"`). [VERIFIED: supabase/schema/baseline.sql:2696,2699,13754-13756]

⚠️ Note `GRANT ALL … TO "service_role"` as well as `authenticated` — TODOS' `[DRIFT-04]` names only
`authenticated`. Both grants disappear with the function; worth stating in the migration's own comment so
the reviewers see the full reachability picture.

**Guards present in the body** (so the reviewers are not surprised into treating this as an incident):
`v_auth_uid IS NULL → RAISE` (:2630), `v_auth_uid <> p_user_id → RAISE` (:2635), portfolio-not-found →
RAISE (:2646), portfolio-not-owned → RAISE (:2652), plus `SET search_path TO 'public','pg_catalog'`.
`BASELINE.md` states explicitly: *"It is not exploitable, and triaging DRIFT-04 as a security incident
would spend the response budget in the wrong place."* [CITED: supabase/schema/BASELINE.md "## What the first use of this file found"]

### `pg_stat_statements` availability — what I could and could not establish

**I could NOT establish how this codebase probes `pg_stat_statements`.** I found no existing probe to copy.
The standard idiom is a catalogue read:

```sql
SELECT count(*) FROM pg_extension WHERE extname = 'pg_stat_statements';
```

— the same shape `scripts/mutation-runner/run.mjs`'s `lane-probe` uses for pg_cron (`gateNeedsPgCron` /
`pg_extension` probing is described in CLAUDE.md's SQL-gate section). [ASSUMED — the pg_cron probe idiom is
documented in CLAUDE.md, but I did not read `run.mjs`'s probe code this session, and I found no
`pg_stat_statements` reference anywhere in the repo.]

⛔ **The locked decision is the important part and it is unambiguous:** an unreadable or absent
`pg_stat_statements` must **ABORT**, printing that it aborted because the measurement was unavailable. It
must never be rendered as "zero calls". Two distinct states must be distinguishable in the pre-flight's
printed output: *extension absent* vs *extension present, zero rows for this function*. Only the second is
evidence.

### Asserting zero dependents — what the codebase already does

**I could NOT find an existing zero-dependents assertion to copy.** The nearest established pattern is the
self-verifying `DO` block idiom used throughout `supabase/migrations/**` (see `20260907120000` STEP 3, which
asserts SHAPE via `pg_proc`/`pg_namespace` and RAISEs rather than NOTICEs). [VERIFIED:
supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql:391-436]
The canonical Postgres read for dependents is `pg_depend` joined to `pg_proc` on the function's `oid`; the
plan should specify it explicitly and PRINT the row count, per the locked "assertions must PRINT their
measurements" rule. [ASSUMED — standard Postgres, not measured here]

**Post-drop:** `database.types.ts` must be regenerated without the function. TODOS notes the file is
generated FROM the DB, so it proves existence, not use. [CITED: TODOS.md `[DRIFT-04]`]

---

## 5. Criterion 4 / DRIFT-05 — the two gates

### 4(a) — hermetic name-set diff, and where the third assertion slots in

`scripts/dump-sql-functions.ts` `check()` is at **line 295**. Its current shape: [VERIFIED:
scripts/dump-sql-functions.ts:295-317]

```ts
function check(): number {
  const want = generate();          // text-replay of supabase/migrations/**
  const have = readCommitted();     // supabase/schema/functions/*.sql
  const drift: string[] = [];
  for (const [name, content] of want) { /* missing: / stale: */ }
  for (const name of have.keys())    { /* orphaned: */ }
  if (drift.length) { console.error(...); return 1; }
  console.log(`SQL function snapshot is current (${want.size} functions).`);
  return 0;
}
```

**The third assertion slots between the second `for` loop and the `if (drift.length)` block** — pushing
`baseline-only:` / `snapshot-only:` entries into the same `drift[]` array reuses the existing failure
rendering and the existing `sql-function-snapshot.yml` wiring for free. The names come from
`extractFunctionDefs` / `--function-names` in `sql-body-normalize.mjs` (which I confirmed parses
`baseline.sql`'s **quoted** identifiers correctly — 119 names, matching `BASELINE.md`'s recorded shape).

⛔ **MEASURED: this assertion is RED today, in both directions.**
baseline-only = `create_allocator_connected_strategy`;
chain-only = `match_engine_cron_tick`, `strategy_analytics_drop_stale_error_provenance`.
The plan MUST decide how the gate goes green — regenerate `baseline.sql` (which needs PROD and moves
criterion 2's sha), or carry a dated named allowlist. It cannot be shipped as "add assertion, observe green".

⚠️ Note the interaction with criterion 3: `create_allocator_connected_strategy` leaves the baseline-only
set only when `baseline.sql` is regenerated *after* the DROP applies. Until then it must be allowlisted with
"pending DRIFT-04" as its reason, or 4(a) blocks the phase's own other plans.

`check()`'s existing self-test (`--self-test`, 11 assertions, line 319) is the natural home for the red
fixtures: two synthetic corpora, one with a name only on the baseline side and one with a name only on the
snapshot side, each asserted to produce a non-zero return.

### 4(b) — baseline-vs-LIVE on the credentialed job

Rides VAC-04's PR-triggered credentialed job. CLAUDE.md records the invariant to preserve:
*"VAC-04 (repo-vs-PROD function body diff) is a step in `migration-drift-check.yml` on migration PRs …
Both exit 1 when their credential is absent — neither ever skips."* [CITED: CLAUDE.md, "SQL gate integrity jobs"]
Criterion 4(b) inherits that contract verbatim: **exit 1 on an absent credential, never skip.**

---

## 6. Criterion 5 / VAC08-LEDGER-32 — what "dispositioned by name" means mechanically

**Do not re-derive the count.** CONTEXT.md re-measured it: **31**, not 32; the join was checked under both a
full-name join and a version-stripped-description join and all 31 are absent under either.
I confirmed the file's own shape only: `scripts/vac08-ledger-baseline.txt` is 70 non-blank lines of which
**31 are non-comment entries**. [VERIFIED: `grep -vc '^#' scripts/vac08-ledger-baseline.txt` → 31]

**Mechanically, "dispositioned by name" = one line in `scripts/vac08-ledger-baseline.txt`.**
The gate's RATCHET block reads: [VERIFIED: scripts/test-ledger-drift-check.sh:350-400]

- `baseline="${LEDGER_BASELINE_FILE:-$(dirname "$0")/vac08-ledger-baseline.txt}"` — overridable, which is how a
  red/green fixture is driven without touching the real file.
- Names are parsed as `sed 's/#.*//'` then whitespace-strip then drop-blank — **so a `# reason` comment on the
  same line is the disposition prose, and it is machine-ignored.** That is the seam for "dispositioned by name":
  the name is the key, the trailing comment is the human disposition.
- `new = measured-missing MINUS baselined` → any non-zero is a hard failure ("not present in the TEST ledger and
  are NOT baselined").
- `stale = baselined MINUS measured-missing` → also a hard failure: *"The baseline may only shrink — delete these
  lines"*. So the file is a one-way ratchet in both senses.

**What VAC-08 reads to decide it reports zero:** it reports zero **NEW** drift when `new_count == 0`, i.e. when
every measured absence has a baseline line. It does **not** report "zero unledgered migrations" — the
unprefixed diagnostic line reads e.g. `ledger presence: 32 absent, all 32 baselined …; 0 NEW drift`.
TODOS records that exact wording and the lesson that grepping a CI log by a token tests the token, not the gate.
[CITED: TODOS.md:6748]

⚠️ **So criterion 5's phrase "VAC-08 reports zero unledgered migrations" is satisfiable in TWO ways** and the
plan must pick one explicitly: (a) apply all 31 to shared TEST — **explicitly forbidden to an agent**
(*"⛔ Do NOT hand-apply the remaining 31 to TEST to shorten the list — TEST is shared with other people's CI;
that stays a founder decision, not an agent one"* [CITED: TODOS.md `[VAC08-LEDGER-32]`]); or (b) confirm each of
the 31 carries a per-name disposition comment and that `0 NEW drift` is the reported reading. **(b) is the only
agent-available route**, and the honest success wording is "zero NEW drift with 31 named dispositions", not
"zero unledgered migrations". The precedent for a real shrink exists and is instructive: `20260823120000_revoke_api_keys_insert`
was applied on founder instruction on 2026-09-07 and its line deleted — *"it is the only line ever removed by an apply."*

⚠️ Also inherit the caveat: **ledger absence is not object absence.** Do not let the plan's prose slide into
"TEST is missing 31 features".

---

## 7. Criterion 6 / VAC-07 — and the advisory lock (⚠️ bigger than one clause)

### The lane

VAC-07 is an **app-level** spec: `csv-finalize` and `withAuth` reach the database through supabase-js, i.e.
PostgREST + GoTrue over HTTP. `scripts/local-stack/run.sh:10-20` states this as a hard boundary:
*"Lane 2 cannot be lane 1 … A bare Postgres cluster serves none of those, so it structurally cannot host this
spec."* [VERIFIED: scripts/local-stack/run.sh:10-20]

⛔ **This contradicts the ROADMAP's phrasing "two concurrent csv-finalize POSTs on one never-classified
`wizard_session_id`, on the pg-lane".** The pg-lane is bare Postgres. The correct substrate is the
**local-stack lane** (criterion 1's lane) — which is precisely why criteria 1 and 6 are in the same phase and
why criterion 6 cannot start before criterion 1 lands. The planner should surface this as an ordering
dependency, not a wording quibble.

### The existing csv-finalize surface

- Route: `src/app/api/strategies/csv-finalize/route.ts` (~1,600 lines).
- The never-classified branch the race targets is at `route.ts:1504` — `// (a) NEVER CLASSIFIED → FILL — and never before clock safety has been…`. [VERIFIED: src/app/api/strategies/csv-finalize/route.ts:1504]
- Existing SQL gates: `supabase/tests/test_csv_finalize_atomic_fold.sql`, `test_csv_finalize_double_submit.sql`, `test_csv_finalize_auth_guard.sql`. [VERIFIED: `ls supabase/tests | grep -i finalize`]
- Existing vitest specs: `src/__tests__/csv-finalize-rpc.test.ts`, `csv-finalize-after-failloud.test.ts`, `csv-finalize-c14-regression.test.ts`, `csv-finalize-cross-submission-merge.test.ts`.
- Existing migrations: `20260728120000_csv_finalize_double_submit_idempotency.sql`, `20260819120000_csv_finalize_atomic_fold.sql`, `20260819130000_..._fold_input_guards.sql`, `20260819151000_..._fold_guard1_null_safe.sql`.

⛔ **MEASURED: there is NO advisory lock in the csv-finalize path.** `grep -rn 'advisory'` over
`src/app/api/strategies/csv-finalize/route.ts` returns zero hits for a lock (only unrelated prose matches for
"block"). [VERIFIED: `grep -rn advisory src/app/api/strategies/csv-finalize/route.ts` → no lock]

### ⚠️ The advisory lock that "left 164.1" is **161.1-D1**, and it is a separate, larger piece of work

`.planning/phases/164.1-…/164.1-CONTEXT.md:191` routes *"The advisory-lock concurrency test → Phase 164.5
BASELINE-SNAPSHOT (VAC-07's spec on the …)"*. [VERIFIED: 164.1-CONTEXT.md:24,191] Following it back:

**TODOS 161.1-D1** — *"the bridge's read window is fail-SAFE, not CLOSED. The real closure is a per-strategy
advisory lock in the two mark RPCs."* [VERIFIED: TODOS.md:6469-6489]
- `mark_compute_job_done` / `mark_compute_job_failed` take `FOR UPDATE` on the **job** row only; neither takes a
  per-strategy lock before `PERFORM sync_strategy_analytics_status`. Bridge calls for the same strategy are
  **not serialized at all**.
- The repo already has the right pattern twice: `positions_atomic_rebuild` and `sync_trades` both take
  `pg_advisory_xact_lock(hashtext(p_strategy_id::text))`.
- **Fix shape (quoted):** take the same advisory lock in `mark_compute_job_done` (`20260603120000`) and
  `mark_compute_job_failed` (`20260529180000`) before the bridge call.
- **Why it was deferred:** *"a half-applied lock discipline (one RPC locking, the other not) is worse than a
  documented window — it reads as protection while providing none. Wants its own phase and its own concurrency test."*
- `TODOS.md:6682` carries **DEC-4**: *"the D1 advisory lock gets its OWN phase, with a real concurrency test."*

⛔ **This is a second migration touching two RPCs, plus a real concurrency test — NOT the csv-finalize race.**
They are different objects (`compute_jobs` mark RPCs vs `wizard_session` csv-finalize) on different substrates
(bare pg-lane suffices for the mark RPCs; the local-stack is required for csv-finalize). The planner should
budget them as **two plans**, and should surface to the founder that DEC-4 asked for a phase of its own —
folding it into criterion 6 as one clause is exactly the compression DEC-4 refused.

### The scoring fence

`[VAC-07-DEFER]` (TODOS.md:2112) forbids flipping VAC-07 to Complete in REQUIREMENTS until its spec is
observed RED with the fence removed and GREEN with it restored. [VERIFIED: TODOS.md:2112, 2128]
So criterion 6's RED fixture is: remove the serialization (the advisory lock / idempotency fence), run two
concurrent POSTs, observe a named failure. GREEN: restore, observe pass. **Both observations must be pasted
into the SUMMARY**; an assertion that they were run does not satisfy this project's anti-vacuity rule.

---

## 8. Criterion 7 / CRON-DRIFT-01-REPAIR — ⛔ two ROADMAP clauses falsified

### The oracle, read

`scripts/prod-prober/cron-manifest.json`: `schema_version: 1`, `normalization: "ws-collapse-v1"`,
`captured_at: "2026-09-06T07:15:05.335Z"`, `database_marker: "⛔ PRODUCTION — real customer data. …"`,
`jobs: [...]`. [VERIFIED: scripts/prod-prober/cron-manifest.json]

Each job row carries `jobid, jobname, schedule, active, database, username, command_sha256, command`, and
`command` is documented as **OPTIONAL** — a row may be published SHA-ONLY by deleting its `command`.
[VERIFIED: scripts/prod-prober/arms/cron-drift.mjs:50-59]

**jobid 1, verbatim** [VERIFIED: scripts/prod-prober/cron-manifest.json, jobs[] entry jobid 1]:

```
jobid 1 | jobname match_engine_cron | schedule "0 * * * *" | active true
database postgres | username postgres
command_sha256 225c37bd9650653bc5c58ecda47fdacc3b1fa8ff443bcb7476733c11c19c9730
command:
DO $body$ DECLARE v_key text; BEGIN
  SELECT decrypted_secret INTO v_key FROM vault.decrypted_secrets WHERE name = 'analytics_service_key';
  IF v_key IS NULL OR v_key = '' THEN
    RAISE EXCEPTION 'analytics_service_key missing from vault — refusing to send a null header';
  END IF;
  PERFORM net.http_post(
    url := 'https://quantalyze-analytics-production.up.railway.app/api/match/cron-recompute',
    headers := jsonb_build_object('Content-Type', 'application/json', 'X-Service-Key', v_key),
    body := '{}'::jsonb, timeout_milliseconds := 60000 );
END $body$;
```

The arm's comparison is over `normalizeCommand()` (`cron-drift.mjs:146`), declared `ws-collapse-v1`, and it
refuses to compare at all on a `schema_version` or `normalization` mismatch — reporting `manifest-invalid`
rather than a pass, because *"An absent oracle is not the absence of drift."* [VERIFIED: cron-drift.mjs:444-464]

### ⛔ FALSIFIED CLAIM 1 — `grep -rn decrypted_secrets supabase/migrations/` is NOT zero

The ROADMAP says the command text *"exists NOWHERE in the repo — `grep -rn decrypted_secrets supabase/ scripts/ src/`
returns ZERO hits"*, and criterion 7 is scored partly on that grep returning the new migration.
**MEASURED at HEAD: it returns three migration files already.** [VERIFIED: `grep -rln decrypted_secrets supabase/migrations/`]

| file | nature of the hit |
|---|---|
| `20260408215026_schedule_match_cron_hourly.sql:1` | an `APP-GUC-LINEAGE:` header comment (164.7 plan 05) |
| `20260408113029_cron_heartbeat.sql:2` | a comment naming the successor |
| `20260907120000_analytics_service_settings_and_vault_tick.sql:322` | **real code** — inside `match_engine_cron_tick()`'s body |

So the grep-based half of criterion 7 is already satisfied by 164.7 and no longer discriminates. **The plan
must restate that clause as something that can still fail** — e.g. "a scheduling statement for
`match_engine_cron` exists in `supabase/migrations/**` and its command normalizes to the manifest's jobid-1
text", proven by mutation.

### ⛔ FALSIFIED / CONTESTED CLAIM 2 — a migration must never schedule

`20260907120000` states, in its own ⛔ block: *"It REGISTERS NO SCHEDULE and REMOVES NONE… Repointing the LIVE
job row is Phase 164.5 item (7), which owns the one production row; this phase owns what the MIGRATIONS
describe. **A migration that schedules is a scope violation, and the runbook rule it would break is stated in
`docs/runbooks/ledger-refresh-go-live.md`: the registration statement lives THERE and never in a migration.**"*
[VERIFIED: supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql:14-24]

The ROADMAP says *"Write ONE forward migration re-scheduling `match_engine_cron`."* **These are in direct
conflict** and it is not the researcher's call. The plan must put this to the founder as a decision with two
named options:
1. a forward migration that schedules (satisfies the ROADMAP; violates the runbook rule 164.7 just re-asserted);
2. a reviewed **runbook registration statement** plus a repo-committed, gate-checked artifact recording it
   (honours the runbook rule; needs a different scoring clause than "the grep returns the new migration").

Per this project's Rule 7 — surface conflicts, do not average them.

### ⚠️ The manifest-oracle collision — criterion 7 as written may be self-defeating

Criterion 7 requires that after the repair *"Phase 164.1's cron-drift arm reports ZERO drift for
`match_engine_cron` against the committed manifest."*

But **PROD already matches the manifest today** — the manifest was captured from PROD on 2026-09-06 and jobid 1
holds the DO-block command above. TODOS/ROADMAP also record PROD as CORRECT (verified 2026-09-01, `net._http_response`
id 3485 returned 200). So the arm reads zero drift *now*.

Therefore:
- If the repair re-schedules jobid 1 onto `SELECT public.match_engine_cron_tick();` (which is what "consume
  164.7's settled mechanism" most naturally means, and what `match_engine_cron_tick`'s own COMMENT anticipates),
  the live command **stops** matching the manifest and the arm goes RED — unless the manifest is re-captured and
  re-committed in the same phase.
- If the repair re-schedules onto the byte-identical DO block, nothing changes in PROD and the arm stays green,
  but the "consume 164.7's mechanism" fence is only honoured in spirit.

⛔ **The plan must state which, and must include the manifest re-capture as an explicit step if the first.**
`match_engine_cron_tick`'s COMMENT flags this precise problem as unresolved: *"repointing the live job row at it is
Phase 164.5 item (7), which must also settle the manifest hygiene rule requiring the literal
`vault.decrypted_secrets` to appear in that row's own command text (164.7-RESEARCH Open Question 2)."*
[VERIFIED: supabase/migrations/20260907120000_…:370-384] Note the hygiene rule cuts against
`SELECT match_engine_cron_tick();`, whose command text contains no `vault.decrypted_secrets` at all — the arm's
`secret-in-command` remedy prose assumes a command that resolves the secret at execution time in its own text.
**I could not locate `164.7-RESEARCH.md`'s Open Question 2 text in this session and did not read it; the plan
must.**

### The idiom criterion 7 must reuse (from `20260907120000`)

- `public.system_settings` — TEXT-valued operator config, `key`/`value`, RLS enabled, `REVOKE ALL … FROM anon`
  (:244), `REVOKE TRUNCATE, REFERENCES, TRIGGER … FROM authenticated` (:254, because TRUNCATE is not subject to
  row security), policies `system_settings_admin_all` (:270) and `system_settings_service_all` (:282), and a CHECK
  constraint `system_settings_analytics_service_url_allowed` (:214) pinning the URL value.
- `public.match_engine_cron_tick()` (:301) — `SECURITY DEFINER` (:304), reads the key from
  `vault.decrypted_secrets` (:322), RAISEs on a missing key (:325), missing URL (:332) and a non-allowed URL
  (:348, deliberately not echoing the offending value), then one ASYNC `net.http_post` returning a request id
  that is **not** an HTTP success. `REVOKE ALL ON FUNCTION … FROM PUBLIC, anon, authenticated` (:385); the
  scheduler runs as superuser so no GRANT is needed.
- Self-verifying `DO` block (STEP 3, :391+) asserting SHAPE only — ⛔ *"THIS BLOCK MUST NEVER CALL
  `match_engine_cron_tick()`"* (an apply-time invocation would fire a real HTTP POST), and ⛔ it must never name
  `vault.*` or `net.*` as objects, because neither exists on the pg-lane.

**These last two ⛔s transfer directly to criterion 7's migration** and are the most likely way to break the
pg-lane or to POST at production on merge.

### Pre-flight and fixtures

Locked: the pre-flight must read the **live** `cron.job.command` for jobid 1, compare it (normalized,
`ws-collapse-v1`) against the manifest, and **ABORT non-zero WITHOUT writing** on mismatch. Criterion 7 requires
this proven *"by running it against a deliberately mismatched manifest, not by inspection."*

| | Fixture | Expected |
|---|---|---|
| RED | a deliberately mismatched manifest copy (the arm already ships fixtures of exactly this shape: `scripts/prod-prober/fixtures/cron-drift/prod-missing-job.json`, `prod-extra-job.json`, `prod-active-flipped.json`, `prod-ok-shuffled-crlf.json`, `prod-ok.json`) | pre-flight exits non-zero, writes nothing |
| RED | live command unreadable / no credential | exits non-zero saying the measurement was unavailable — never "no drift" |
| GREEN | manifest matching live | proceeds |

Those five fixtures are a ready-made harness — reuse them rather than authoring new ones.
[VERIFIED: `ls scripts/prod-prober/fixtures/cron-drift/`]

---

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---|---|---|---|
| Normalize/compare SQL bodies (crit 8, 4a) | a second normalizer | `scripts/sql-body-normalize.mjs` (`--diff-bodies`, `--function-names`, `--hash`, `--extract-fn`) | It is the *declared single shared* normalizer (D-05); a second one would produce a second answer for the same text. Handles quoted identifiers, comment-vs-code, overload pairing, and refuses to pass `UNCOMPARABLE`. Measured 0.12 s on the full corpus. |
| Render "the applied migration chain" | a pg-lane full replay | `dump-sql-functions.ts` text-replay / `supabase/schema/functions/*.sql` | The chain measurably does not replay (69/262 fail). The pg-lane takes curated apply lists, not the chain. |
| Cron drift comparison | a new comparator | `scripts/prod-prober/arms/cron-drift.mjs` + its 5 fixtures | Already has normalization identity, schema-version refusal, and a manifest-invalid state that refuses to read as "no drift". |
| Ledger disposition bookkeeping | a new tracker | `scripts/vac08-ledger-baseline.txt` + `test-ledger-drift-check.sh` ratchet | Already fails in BOTH directions (new drift AND stale lines). `LEDGER_BASELINE_FILE` is the fixture seam. |
| Baseline sha assertion | a new hash check | the existing vitest in `baseline-wiring-claim.test.ts` | Already extracts and compares; only CI placement is missing. |
| Per-strategy serialization (crit 6) | a bespoke lock scheme | `pg_advisory_xact_lock(hashtext(p_strategy_id::text))` | The repo uses this exact call twice already (`positions_atomic_rebuild`, `sync_trades`). |

**Key insight:** almost every "build a gate" in this phase is really "wire an existing gate into CI and prove it
by mutation". The novel work is concentrated in the two production-DDL items and in criterion 6's advisory lock.

---

## Common Pitfalls

**P1 — Shipping criterion 4(a) or 8 as "add assertion, observe green."** Both are RED at HEAD (3 names / 6 DRIFT +
2 SNAPSHOT_MISSING). The plan must carry an explicit go-green decision (regenerate the baseline, or a dated named
allowlist). Warning sign: a plan task that says "add the assertion" with no companion task about the existing drift.

**P2 — Restoring mutation fixtures with `git checkout --`.** It restores to HEAD and silently destroys the
harness's own uncommitted edits. Use byte backups, re-taken after every edit; verify the restore by grepping the
newest change, not by hash alone. [CITED: MEMORY, two entries]

**P3 — Reading an absent measurement as a zero.** Applies to `pg_stat_statements` (crit 3), the live cron command
(crit 7), and 4(b)'s credential. Each must ABORT/exit 1 and *say which state it is in*.

**P4 — A migration that schedules, or a self-verify block that CALLs the tick.** Would fire a real HTTP POST at
the analytics service on merge, and would break the pg-lane (no `vault.*`, no `net.*`).

**P5 — Believing 4(a) covers 4(b).** TODOS `[DRIFT-05]` names this precisely: *"Shipping (a) while believing it
covers (b) would be a control that reads green while blind — the exact defect class Phase 164.3 exists to eliminate."*

**P6 — Running criterion 6's spec on the pg-lane.** Structurally impossible (no PostgREST/GoTrue). Ordering: crit 1 → crit 6.

**P7 — Grepping a CI log by a token to decide a gate passed.** TODOS.md:6748 records this exact misreading of VAC-08.

**P8 — `.planning`/PR hygiene.** `.planning/` is tracked; the repo is PUBLIC and this RESEARCH names PROD object
signatures. Nothing here is a secret (all of it is already in the committed baseline / manifest), but the PR-branch
deletion guard in CLAUDE.md still applies.

---

## Project Constraints (from CLAUDE.md / AGENTS.md)

- ⛔ **This checkout's Supabase CLI is linked to PRODUCTION.** `supabase db push`, `db reset --linked`,
  `--project-ref`, `--db-url` all target prod from this directory. The link is deliberate; do not "fix" it.
- ⛔ Before any writing statement, run the `shobj_description(oid,'pg_database')` which-database probe. `current_database()`
  is `postgres` on both projects and proves nothing. NULL marker ⇒ stop.
- ⛔ Merging `supabase/migrations/**` to main **auto-applies to PROD**. Both of this phase's migrations are live on merge.
- ⛔ Three reviewers before any migration apply: migration-reviewer, rls-policy-auditor, silent-failure-hunter.
- Floors: read `FILES_FLOOR` / `ARMS_FLOOR` off `scripts/mutation-runner/run.mjs` **by symbol**, never from prose.
  Any new annotated SQL gate file moves both, and the *stale-low* direction is caught by
  `src/__tests__/mutation-runner-floors.test.ts`, not by the runner.
- `sql-gate-lint` now also runs `scripts/lint-app-guc.mjs`; its corpus step is at **0 findings** and a red corpus step
  is a regression, never cleared by widening `LINEAGE_ALLOWLIST`.
- ⛔ `sql-tests` / VAC-08 are expected RED on open PRs for the arms named in `[164.7-TEST-APPLY-APPSETTINGS]` and
  `[164.2-TEST-APPLY-PROVENANCE]` until the founder hand-applies to TEST. Missing apply, not a regression.
- Full vitest must not share the box; serialize vitest → pytest → mypy.
- AGENTS.md: this is not the Next.js you know — read `node_modules/next/dist/docs/` before writing app code.
  (Not expected to bind in this phase; criterion 6 touches an existing route, not routing conventions.)

---

## Runtime State Inventory

This phase changes production runtime state, so this section is mandatory.

| Category | Items Found | Action Required |
|---|---|---|
| Stored data | None. No table rows are rewritten by this phase. `system_settings` already carries its seeded row from `20260907120000`. | none |
| Live service config | ⛔ **PROD `cron.job` jobid 1** (`match_engine_cron`, `0 * * * *`, active). Its command lives in the DB, not git; the only committed copy is `cron-manifest.json`. Repointing it is criterion 7. | pre-flight + reviewed change + **manifest re-capture if the command changes** |
| OS-registered state | None found — no Task Scheduler / launchd / pm2 surface is touched by this phase. (Verified by scope: all edits are repo files, two migrations, one cron row.) | none |
| Secrets / env vars | `analytics_service_key` in PROD Vault (exists since 2026-08-25). ⚠️ `cron-drift.mjs`'s `secret-in-command` remedy treats a credential-shaped command as EXPOSED. This phase must not move the secret into any command text. `TEST_SUPABASE_DB_URL` and the VAC-04 PROD credential gate criteria 4(b)/5. | no rotation; do not embed |
| Build artifacts | `src/lib/database.types.ts` — generated FROM the DB, so it will still declare `create_allocator_connected_strategy` until regenerated after the DROP (criterion 3 says so explicitly). `supabase/schema/functions/*.sql` and `supabase/schema/baseline.sql` are both generated artifacts whose currency this phase makes load-bearing. | regenerate `database.types.ts` post-DROP; decide on a `baseline.sql` regeneration |

---

## Ordering Constraints (surface these to the planner)

1. **Criterion 8 BEFORE any migration squash** — locked. A squash onto an unpinned, already-drifted baseline
   freezes the drift into the new floor permanently.
2. **Criterion 1 BEFORE criterion 6** — the csv-finalize spec needs the local-stack lane, which cannot boot until
   the baseline is repointed (`run.sh up` currently exits FATAL).
3. **Criterion 1 and the `BASELINE.md` prose edit in the SAME commit** — `baseline-wiring-claim.test.ts` flips
   branch and fails otherwise. This is a feature, not a hazard.
4. **Criterion 3's DROP BEFORE a `baseline.sql` regeneration** if the regeneration is used to green 4(a)/8 —
   otherwise the regenerated baseline re-imports the function name.
5. **Criterion 7's manifest re-capture AFTER the cron row changes**, in the same phase, or 164.1's arm goes RED.
6. Criteria 3 and 7 each behind the three-reviewer gate; neither can be batched with hermetic work.

---

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|---|---|---|
| A1 | `pg_extension`-style probe is the right idiom for `pg_stat_statements` availability | §4 | Pre-flight probes the wrong catalogue and cannot distinguish "absent" from "zero rows" — the exact failure the locked rule forbids |
| A2 | `pg_depend` joined to `pg_proc.oid` is the right zero-dependents read | §4 | A dependent is missed and the no-CASCADE DROP fails at apply (loud, so low harm) or a false abort |
| A3 | Keeping `.gitignore:138` is safe and preferable to deleting it | §1 | Contradicts the ROADMAP's literal instruction; needs a founder line |
| A4 | The ROADMAP's "44 function names with >1 definition / `mark_compute_job_done` × 9" | §3 | Only affects squash-sizing prose, not this phase's gates. Not re-measured. |
| A5 | "consume 164.7's mechanism" most naturally means scheduling `SELECT public.match_engine_cron_tick();` | §8 | If the founder means the byte-identical DO block instead, the manifest re-capture step is unnecessary |

---

## Open Questions

1. **Does criterion 7's repair schedule the tick function or the DO block?** — determines whether a manifest
   re-capture is in scope and whether the arm reads zero. *Recommendation:* founder decision, framed with the
   `vault.decrypted_secrets`-in-command hygiene rule that `match_engine_cron_tick`'s COMMENT flags as unsettled.
2. **Migration vs runbook for the cron registration.** ROADMAP and `20260907120000` are in explicit conflict.
   *Recommendation:* surface both options; do not average.
3. **164.7-RESEARCH Open Question 2** — named by the migration's COMMENT as the thing criterion 7 must settle.
   ⛔ **I did not read it.** The planner must.
4. **How does 4(a)/8 go green?** Regenerate `baseline.sql` from PROD (moves criterion 2's sha, needs the PROD
   credential and the secret re-scan) vs a dated allowlist. *Recommendation:* allowlist first (hermetic, ships now),
   regeneration as a separate reviewed act after criterion 3's DROP.
5. **Is 161.1-D1 in scope, or does DEC-4 still hold that it wants its own phase?** *Recommendation:* plan it as its
   own plan inside 164.5 and flag DEC-4 to the founder.
6. **Cost of a criterion-8 CI job.** Measured 0.12 s for the diff itself; I did **not** measure job setup overhead.
   No pg-lane cost applies because no replay is possible.

---

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|---|---|---|---|---|
| `node` (running `sql-body-normalize.mjs`) | crit 8, 4a | ✓ | ran successfully in-session | — |
| `supabase/schema/baseline.sql` | crit 1, 2, 4a, 8 | ✓ | 701,524 B, 2026-09-01 | — |
| `supabase/schema/functions/*.sql` | crit 4a, 8 | ✓ | 120 distinct names | — |
| `scripts/prod-prober/cron-manifest.json` | crit 7 | ✓ | schema_version 1, captured 2026-09-06 | — |
| cron-drift fixtures (5) | crit 7 RED proof | ✓ | — | — |
| pg-lane with pg_cron | crit 6 (mark-RPC half) | ✓ | per CLAUDE.md 164.4.1 | — |
| Supabase CLI local stack (Docker) | crit 1, 6 | **NOT VERIFIED** — I did not run `run.sh up` | CLI 2.84.2 local / 2.98.2 CI | none; a lane that cannot boot blocks crit 1's proof |
| PROD credential (`pg_stat_statements`, live `cron.job`) | crit 3, 4b, 7 | **NOT VERIFIED** — deliberately not queried | — | none — ABORT is the required behaviour |
| `TEST_SUPABASE_DB_URL` | crit 5 | **NOT VERIFIED** | — | none — VAC-08 exits 1 on absence by design |

**Missing with no fallback:** the PROD and TEST credentials. That is by design — both gates must exit 1 rather than skip.
**⛔ I did not run `bash scripts/local-stack/run.sh up`** (it starts Docker containers and would have been a side effect
outside a research pass). Criterion 1's "proven by running it" remains unproven at research time.

---

## Validation Architecture

### Test Framework
| Property | Value |
|---|---|
| Framework | vitest (TS) + `supabase/tests/*.sql` gates driven by `scripts/pg-lane/run.sh` + `scripts/mutation-runner/run.mjs` |
| Config file | `vitest.config.ts` |
| Quick run command | `npx vitest run src/__tests__/baseline-wiring-claim.test.ts` |
| Full suite command | `npm run test` / `node scripts/mutation-runner/run.mjs` for the SQL corpus |

### Phase Requirements → Test Map
| Req | Behavior | Type | Automated command | Exists? |
|---|---|---|---|---|
| crit 1 | lane resolves the committed baseline | unit | `npx vitest run src/__tests__/baseline-wiring-claim.test.ts` | ✅ (flips branch on the repoint) |
| crit 2 | sha co-edit | unit + CI | same file, second `it` | ✅ test / ❌ CI job — Wave 0 |
| crit 4a | name-set diff both directions | unit | `npx tsx scripts/dump-sql-functions.ts --check` + `--self-test` | ❌ Wave 0 (assertion + 2 fixtures) |
| crit 4b | baseline-vs-LIVE, exit 1 on absent credential | credentialed CI | rides VAC-04 in `migration-drift-check.yml` | ❌ Wave 0 |
| crit 5 | 0 NEW drift, 31 named | credentialed CI | `bash scripts/test-ledger-drift-check.sh` (+ `--self-test`) | ✅ gate / ❌ dispositions |
| crit 6 | concurrent csv-finalize RED/GREEN | integration (local-stack) | new spec | ❌ Wave 0 |
| crit 6b | mark-RPC advisory lock | SQL gate (pg-lane) | new `supabase/tests/test_*.sql` + mutation arms | ❌ Wave 0 |
| crit 7 | pre-flight aborts on mismatched manifest | unit | fixture-driven, reuse `fixtures/cron-drift/*` | ❌ Wave 0 |
| crit 8 | baseline↔chain body drift, both directions | unit | new runner over `diffFunctionBodies()` | ❌ Wave 0 |

### Sampling Rate
- Per task commit: the file-scoped vitest above. ⛔ **File-scoped runs cannot clear contract tests** — `src/__tests__/contracts/` scan all of `src/`.
- Per wave merge: full vitest (serialized, never sharing the box).
- Phase gate: full suite + `node scripts/mutation-runner/run.mjs` exit 0 with floors read by symbol.

### Wave 0 Gaps
- [ ] A CI job (or an existing job's step) that runs the baseline gates — currently **zero** workflow references.
- [ ] Red/green fixture pairs for crit 2, 4a (×2 directions), 7, 8 (×2 directions).
- [ ] Any new `supabase/tests/test_*.sql` gate must carry `RED-UNDER` annotations and move `FILES_FLOOR`/`ARMS_FLOOR`.

---

## Security Domain

| ASVS Category | Applies | Standard control |
|---|---|---|
| V2 Authentication | yes (crit 3, 6) | The dropped function's own `auth.uid()` guards; csv-finalize `withAuth` |
| V3 Session Management | no | — |
| V4 Access Control | **yes** | crit 3 removes `GRANT ALL … TO authenticated` + `service_role` on a SECDEF credential-writing RPC; crit 7's tick is `REVOKE ALL … FROM PUBLIC, anon, authenticated` |
| V5 Input Validation | yes (crit 7) | `system_settings_analytics_service_url_allowed` CHECK + the in-function re-test that survives the constraint being dropped |
| V6 Cryptography | yes (indirect) | `vault.decrypted_secrets` read at execution time — never embedded in a cron command |

| Pattern | STRIDE | Mitigation |
|---|---|---|
| Credential-shaped text in a public artifact (cron command, PR log, this repo) | Information Disclosure | `cron-drift.mjs` `secret-in-command` defect: treat as EXPOSED and rotate FIRST; `--diff-bodies` prints hashes and line counts, never body text |
| Unreviewed SECDEF RPC reachable by `authenticated` | Elevation of Privilege | crit 3's DROP; crit 4a makes the class visible going forward |
| Dropping the wrong overload / silently no-op DROP | Tampering | no `IF EXISTS`, no `CASCADE`, exact 11-type signature |
| An apply-time HTTP POST from a self-verify block | (operational) | assert SHAPE only; never CALL the tick |
| A gate that reads green while blind | (this project's #1 class) | mutation proof in both directions for every gate; `UNCOMPARABLE` fails closed |

---

## Sources

### Primary (HIGH confidence — read and/or executed this session)
- `supabase/schema/baseline.sql` (:2618, :2696, :2699, :13754-13756)
- `supabase/schema/BASELINE.md` (full)
- `src/__tests__/baseline-wiring-claim.test.ts` (full)
- `scripts/local-stack/run.sh` (:10-31, :55-57, :192-223, :382-388)
- `scripts/sql-body-normalize.mjs` (:1-90, :440-545) + executed `--function-names`, `--diff-bodies`
- `scripts/dump-sql-functions.ts` (:286-320, :386-391)
- `scripts/test-ledger-drift-check.sh` (:1-120, :350-400)
- `scripts/vac08-ledger-baseline.txt` (31 non-comment entries)
- `scripts/prod-prober/cron-manifest.json`, `scripts/prod-prober/arms/cron-drift.mjs`, `fixtures/cron-drift/`
- `scripts/pg-lane/run.sh` (:1-40)
- `supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql`
- `supabase/migrations/20260408215026_schedule_match_cron_hourly.sql:1` (lineage header)
- `.gitignore:128-142`
- `TODOS.md` — `[DRIFT-04]`:6696, `[DRIFT-05]`:6697, `[VAC08-LEDGER-32]`:6741-6748, `[VAC-07-DEFER]`:2112, 161.1-D1:6469-6489, DEC-4:6682, CRON-DRIFT-01:2221-2283
- `.planning/ROADMAP.md` Phase 164.5, `164.5-CONTEXT.md`, `164.1-CONTEXT.md:24,191`
- `CLAUDE.md`, `AGENTS.md`

### Secondary (MEDIUM)
- `.planning/phases/164.1-…/164.1-RESEARCH.md:73` (routing of the advisory-lock item)

### Not consulted (stated so it is not mistaken for coverage)
- PROD or TEST databases — deliberately not queried.
- `164.7-RESEARCH.md` Open Question 2 — **named as load-bearing by criterion 7 and NOT read.**
- `docs/runbooks/ledger-refresh-go-live.md` — cited by `20260907120000` as the source of the "never schedule in a migration" rule; **not read.**
- `scripts/local-stack/REPLAY-SPIKE.md` — its measurements are quoted second-hand via `run.sh` and `BASELINE.md`, which agree with each other.
- `scripts/mutation-runner/run.mjs` — floors and probe idiom taken from CLAUDE.md, not re-read.

## Package Legitimacy Audit

**Not applicable — this phase installs no external packages.** Every tool it needs (`sql-body-normalize.mjs`,
`dump-sql-functions.ts`, `pg-lane`, `prod-prober`, vitest) is already in the repo. If a plan introduces a
dependency, run the legitimacy gate then.

## Metadata

**Confidence breakdown:**
- Criteria 1, 2, 4, 5, 8 — **HIGH**. Every claim executed or read at HEAD this session; criterion 8's comparison
  was actually run and its numbers are reproducible.
- Criterion 3 — **MEDIUM**. Signature is HIGH (verbatim from the committed baseline). The two probe idioms
  (`pg_stat_statements`, `pg_depend`) are ASSUMED; no existing precedent found in the repo.
- Criterion 6 — **MEDIUM**. Lane and file inventory are HIGH; the true size of the advisory-lock work is HIGH from
  TODOS but its interaction with the csv-finalize spec is unresolved.
- Criterion 7 — **MEDIUM**. Manifest and idioms are HIGH; two ROADMAP clauses are falsified and three questions
  (migration-vs-runbook, tick-vs-DO-block, OQ2) remain open.

**Research date:** 2026-09-07
**Valid until:** ~2026-09-14 — this tree is moving fast (164.2 and 164.7 both landed within 48 h and both changed
the numbers in §3 and §5). Re-run the two commands in §3 before planning if more than a few days pass.
