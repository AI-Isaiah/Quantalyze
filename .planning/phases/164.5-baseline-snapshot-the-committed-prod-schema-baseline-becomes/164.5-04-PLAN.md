---
phase: 164.5-baseline-snapshot
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dump-sql-functions.ts
  - scripts/prod-body-drift-check.sh
  - .github/workflows/migration-drift-check.yml
autonomous: true
requirements: [CRIT-4, DRIFT-05]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`npx tsx scripts/dump-sql-functions.ts --check` fails when a function name is present in `supabase/schema/baseline.sql` and absent from `supabase/schema/functions/*.sql`, and fails in the opposite direction too."
    - "Each direction has its own RED fixture inside `--self-test`, and the printed assertion count moved."
    - "The three name-set disagreements measured at HEAD are carried as a dated named ratchet, not muted and not left red."
    - "The credentialed baseline-vs-LIVE leg exits 1 when its credential is absent — it NEVER skips."
    - "4(a) and 4(b) are two gates and the tree says so; nowhere does prose claim (a) covers (b)."
  artifacts:
    - scripts/dump-sql-functions.ts
    - scripts/prod-body-drift-check.sh
    - .github/workflows/migration-drift-check.yml
  key_links:
    - "the new assertion pushes into `check()`'s EXISTING `drift[]` array -> it inherits the existing failure rendering and the existing `sql-function-snapshot.yml` wiring for free."
    - "names come from `sql-body-normalize.mjs --function-names` / `extractFunctionDefs` -> the declared single parser, never a second one."
    - "4(b) rides VAC-04's EXISTING job, credential and `supabase db dump` -> the phase mints no second copy of production access."
---

<objective>
Criterion 4 (DRIFT-05) as TWO gates that are never conflated:

**(a) HERMETIC name-set diff, both directions** — `baseline.sql`'s function names against
`supabase/schema/functions/*.sql`, as a third assertion inside `dump-sql-functions.ts --check`. No
credentials, no Docker, no network, cannot flake. This is how DRIFT-04 would have been caught
years ago, but it catches that class only AS OF THE LAST BASELINE REFRESH.

**(b) baseline-vs-LIVE staleness** — needs the PROD credential, so it rides the PR-triggered
credentialed job VAC-04 already uses.

⛔ **Shipping (a) while believing it covers (b) would be a control that reads green while blind** —
the exact defect class Phase 164.3 exists to eliminate. TODOS `[DRIFT-05]` names this precisely.
Neither this plan's code comments nor its SUMMARY may imply (a) subsumes (b).

⛔ **GATE (a) IS RED AT HEAD.** Measured: baseline-only = `create_allocator_connected_strategy`;
chain-only = `match_engine_cron_tick`, `strategy_analytics_drop_stale_error_provenance`.

**P-02 — go-green route, decided here: PRECEDENT B (a dated named ratchet).** Same reasoning as
P-03 in plan 03: the alternative is regenerating `baseline.sql` from PROD, which needs the
credential, a secret re-scan and moves plan 02's recorded sha — a separate reviewed act. And a
declared red interval would leave this phase's OWN other plans blocked by a gate the phase just
added.

⚠️ **Interaction with plan 06 (D-01), stated so it is not a surprise:**
`create_allocator_connected_strategy` leaves the baseline-only set ONLY when `baseline.sql` is
regenerated AFTER the DROP applies. Until then its ratchet row carries `reason: pending DRIFT-04`
and `clearedBy` naming that regeneration. Plan 06 does NOT delete the row; the regeneration does.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-CONTEXT.md
@.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-PATTERNS.md
@scripts/dump-sql-functions.ts
@scripts/prod-body-drift-check.sh
@.github/workflows/migration-drift-check.yml
</context>

## Artifacts this phase produces

This plan produces: a third assertion plus a dated `NAME_SET_RATCHET` in
`scripts/dump-sql-functions.ts` with two new `--self-test` arms; a baseline-vs-LIVE leg in
`scripts/prod-body-drift-check.sh` with its own self-test arms; and a step in
`.github/workflows/migration-drift-check.yml` that rides VAC-04's existing job and credential.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 4(a) — the third assertion in check(), its ratchet, and a RED fixture in EACH direction</name>
  <read_first>
    - `scripts/dump-sql-functions.ts` — the whole file, and in particular the `check()` function (its `want` text-replay map, its `have` committed-snapshot map, the two existing `for` loops pushing `missing:` / `stale:` / `orphaned:` rows into `drift[]`, and the `if (drift.length)` rendering); and `selfTest()`, with its local `assert` helper and its printed assertion COUNT.
    - `scripts/sql-body-normalize.mjs` — `--function-names` and `extractFunctionDefs`, the declared single parser. It was confirmed to parse `baseline.sql`'s QUOTED identifiers correctly, yielding 119 names matching `BASELINE.md`'s recorded shape.
    - `scripts/lint-app-guc.mjs` — `LINEAGE_ALLOWLIST`'s three properties (exact count, successor, reason), the shape the ratchet copies.
  </read_first>
  <files>scripts/dump-sql-functions.ts</files>
  <behavior>
    - A name present in `baseline.sql` and absent from `supabase/schema/functions/*.sql`, not in the ratchet -> a `baseline-only:` row in `drift[]`, `check()` returns 1.
    - A name present in the chain snapshot and absent from `baseline.sql`, not in the ratchet -> a `snapshot-only:` row, returns 1.
    - A name in the ratchet -> no row ONLY when the ratchet row's recorded SIDE matches the side
      the name is currently only on. ⛔ A ratcheted name that has FLIPPED SIDES (was `baseline-only`,
      now `snapshot-only`, or the reverse) is a genuinely NEW drift and MUST produce a row and
      return 1 — a name-only match would silently absorb it. This mirrors plan 03, which pins the
      hash PAIR for the same reason.
    - A ratchet name that is now present on BOTH sides -> a STALE row, returns 1, saying to delete the entry. The ratchet may only shrink.
    - `--self-test` gains one arm per direction, each driving a synthetic corpus and asserting a non-zero return.
  </behavior>
  <action>
    Insert the third assertion into `check()` BETWEEN the existing orphaned-name loop and the
    `if (drift.length)` block, pushing `baseline-only:` and `snapshot-only:` rows into the SAME
    `drift[]` array. That reuses the existing failure rendering and the existing
    `sql-function-snapshot.yml` wiring at no cost — do not add a parallel reporting path and do not
    add a new CI step for it.

    Source the names from `sql-body-normalize.mjs`'s `--function-names` / `extractFunctionDefs`.
    Do not write a second parser.

    Add an exported dated `NAME_SET_RATCHET` next to `check()`, one row per known disagreement,
    each with: the name, which SIDE it is only on, a `capturedAt` date, a `clearedBy` naming
    exactly what removes it, and a `reason`. Populate it by RE-RUNNING the measurement at execution
    time rather than transcribing the three names above — the readings are dated 2026-09-07 and are
    warned valid until roughly 2026-09-14. Record any difference in the SUMMARY.
    `create_allocator_connected_strategy`'s row carries `reason: pending DRIFT-04` and a
    `clearedBy` of "a regeneration of supabase/schema/baseline.sql from PROD, taken as a separate
    reviewed act AFTER plan 06's DROP applies".

    Extend `selfTest()` with the two directional arms and UPDATE the printed assertion count. ⛔
    Leaving the count at its current value is the stale-claim shape this repo names as a defect.

    In the header, state that this is gate (a) of two, that it is hermetic, and that it catches the
    DRIFT-04 class only as of the last baseline refresh — and that gate (b), which closes the live
    direction, lives in `scripts/prod-body-drift-check.sh`. Do not let the header imply (a)
    subsumes (b).
  </action>
  <verify>
    <automated>npx tsx scripts/dump-sql-functions.ts --self-test && npx tsx scripts/dump-sql-functions.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/dump-sql-functions.ts --self-test` exits 0 and its printed assertion count is strictly greater than the count present before this task.
    - `npx tsx scripts/dump-sql-functions.ts --check` exits 0 on the tree with the ratchet populated.
    - Temporarily emptying the ratchet makes `--check` exit 1 and print all three names — pasted into the SUMMARY, then restored from a byte backup.
    - `grep -c 'sql-body-normalize' scripts/dump-sql-functions.ts` is at least 1; no second identifier parser is introduced.
    - The header names gate (b) and its file, and contains no sentence claiming (a) covers the live direction.
  </acceptance_criteria>
  <done>The hermetic name-set diff fires in both directions, has a red fixture for each inside the self-test, and ships green on a dated ratchet that can only shrink.</done>
</task>

<task type="auto">
  <name>Task 2: 4(b) — the baseline-vs-LIVE leg on the credentialed script, exit 1 when the credential is absent</name>
  <read_first>
    - `scripts/prod-body-drift-check.sh` — the whole file: its three NON-NEGOTIABLES header block (never skip; never print PROD text; never pass something it did not compare), its `*_CMD` injectable-seam env vars including `BODY_FETCH_CMD` and `BODY_NAME_INDEX_CMD` and the recorded reason `BODY_NAME_INDEX_CMD` is required and not optional, and its self-test harness.
    - `scripts/test-ledger-drift-check.sh` — its self-test harness shape: stub scripts written into a `mktemp -d`, driven through the REAL check path, each arm declaring its expected exit code; and its trap discipline (split `RETURN` / `EXIT` / `INT` / `TERM`, because a single combined trap RESUMES the script).
    - `scripts/sql-function-names-naive.mjs` — the SECOND, independent name index. The two-independent-indexes lesson: a name index and a fetcher sharing an implementation agree by construction, so an absence check that reuses `extractFunctionDefs` must be UNIONED with the naive one.
  </read_first>
  <files>scripts/prod-body-drift-check.sh</files>
  <action>
    Add a baseline-vs-LIVE name-set leg to the existing script: compare `supabase/schema/baseline.sql`'s
    function names against the LIVE dump's names, in both directions, and report a name present live
    but absent from the committed baseline as drift — this is the direction that would have caught
    DRIFT-04 the day it appeared rather than at the next refresh.

    Reuse the SAME live dump the VAC-04 leg already takes. Do not take a second dump and do not mint
    a second credential path. Take the names through the union of `sql-body-normalize.mjs` and
    `scripts/sql-function-names-naive.mjs`, exactly as the existing VAC-04 leg does, so the index
    and the fetcher cannot agree by construction.

    Inherit the three non-negotiables verbatim in behavior: an absent credential is `exit 1` and
    never a `::notice::` skip (D-02); output carries names, argument counts, hashes and differing
    line counts and NOTHING else, because the repository is PUBLIC; and a fetch that errors, a name
    index that cannot be built, or a missing baseline are all `exit 1` — "could not measure" and
    "measured zero problems" must not share a code path.

    Route every new I/O through an injectable `*_CMD` env var in the file's existing style, so the
    self-test can drive red arms with no credential at all.

    Extend the script's self-test with arms for: live-only name -> exit 1; baseline-only name ->
    exit 1; both sides equal -> exit 0; absent credential -> exit 1 naming the credential; name
    index unbuildable -> exit 1. Copy the harness's trap discipline.

    In the header, add the sentence that gate (a) is hermetic and as-of-last-refresh, this leg is
    the live direction, and neither covers the other.
  </action>
  <verify>
    <automated>bash scripts/prod-body-drift-check.sh --self-test</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/prod-body-drift-check.sh --self-test` exits 0 and its output names every new arm, including the absent-credential arm and its expected exit code 1.
    - Running the script with its credential env var unset exits 1 and prints the credential's NAME — pasted into the SUMMARY. It must not exit 0 and must not print a skip notice.
    - `grep -c 'sql-function-names-naive' scripts/prod-body-drift-check.sh` is at least 1 — the new leg is unioned with the independent index.
    - The script prints no body text in any arm: the self-test output contains no dollar-quoted SQL.
  </acceptance_criteria>
  <done>The live direction is measured on the credential VAC-04 already carries, it exits 1 rather than skipping when that credential is absent, and every red arm has been observed without touching PROD.</done>
</task>

<task type="auto">
  <name>Task 3: Wire 4(b) into VAC-04's existing job, with no `if:` of its own</name>
  <read_first>
    - `.github/workflows/migration-drift-check.yml` — the whole VAC-04 job: its D-12 comment (this workflow hosts the body-drift gate and it rides THIS job and THIS credential rather than minting a second copy of production access), its D-17a comment (the step deliberately carries NO `if:` of its own because its FIRST act is its own credential assertion), the hard-failure "secrets are configured" step, and the `supabase db dump` step that writes to `$RUNNER_TEMP` and is never `cat`ted.
  </read_first>
  <files>.github/workflows/migration-drift-check.yml</files>
  <action>
    Add the baseline-vs-LIVE step to the SAME job, ordered AFTER the existing `supabase db dump`
    step so it consumes that dump rather than taking another.

    Give it NO `if:` of its own — the script's first act is its own credential assertion, and a step
    that could inherit a condition making it silently not run is the forbidden shape. Add the
    comment saying so, pointing at the existing D-17a comment as the precedent.

    Do not widen the job's secret exposure: it already carries the credential on every migrations
    PR, so this step widens nothing, and the comment should say that explicitly the way D-12 does.

    Do not print the dump. Do not add the dump path to any log line.

    Do NOT alter the existing VAC-04 step, its `prod-body-ack:` pragma handling, or the credential
    assertion step.
  </action>
  <verify>
    <automated>grep -n 'prod-body-drift-check' .github/workflows/migration-drift-check.yml && grep -c 'if:' .github/workflows/migration-drift-check.yml</automated>
  </verify>
  <acceptance_criteria>
    - The new step appears in `migration-drift-check.yml` at a line number GREATER than the `supabase db dump` step's.
    - The new step block contains no `if:` key — verified by reading the step's own YAML block and pasted into the SUMMARY.
    - The count of `if:` occurrences in the file is unchanged from before the edit.
    - The existing VAC-04 step's command line is byte-identical to before — `git diff` shows no change to it.
  </acceptance_criteria>
  <done>4(b) runs on every migrations PR, on VAC-04's credential, and cannot be conditioned out of running.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD -> CI job | A credentialed `supabase db dump` crosses into a job whose logs are public. |
| unowned PROD object -> repo | A function existing in PROD under no migration is invisible to a hermetic gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5-11 | Elevation of Privilege | An unreviewed SECDEF RPC reachable by `authenticated` appearing in PROD under no migration | critical | mitigate | 4(b) measures the live direction on every migrations PR; 4(a) makes the class visible hermetically going forward. |
| T-164.5-12 | Repudiation | Believing (a) covers (b) | critical | mitigate | Both files' headers state the split; no prose in this plan or its SUMMARY may claim subsumption; task 1's acceptance criteria greps for the absence of such a sentence. |
| T-164.5-13 | Denial of Service / Repudiation | A credentialed gate that SKIPS on an absent secret | critical | mitigate | D-02: exit 1 naming the credential, proven by a self-test arm and by an unset-env run pasted into the SUMMARY. |
| T-164.5-14 | Information Disclosure | PROD function text in a public CI log | high | mitigate | Names, nargs, hashes and differing-line counts only; the dump goes to `$RUNNER_TEMP` and is never `cat`ted. |
| T-164.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan. Nothing to audit. |
</threat_model>

<verification>
- `npx tsx scripts/dump-sql-functions.ts --self-test` and `--check` exit 0; assertion count moved.
- Emptied-ratchet run pasted showing all three names, then restored.
- `bash scripts/prod-body-drift-check.sh --self-test` exit 0; unset-credential run exits 1 naming it.
- `npm run lint`, `npm run typecheck` clean.
- `npm run schema:functions:check` (the packaged spelling of the hermetic check) exit 0.
</verification>

<success_criteria>
Criterion 4 is met: (a) is a hermetic assertion inside `dump-sql-functions.ts --check` that fails
when a name is present on one side and not the other, with a red fixture in EACH direction; and
(b) runs on the credentialed job and exits 1 when its credential is absent, never skipping. The
tree nowhere claims either covers the other.
</success_criteria>

<output>
Create `.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-04-SUMMARY.md` when done.
</output>
