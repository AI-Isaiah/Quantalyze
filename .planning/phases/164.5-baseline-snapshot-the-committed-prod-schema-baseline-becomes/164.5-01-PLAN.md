---
phase: 164.5-baseline-snapshot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/local-stack/run.sh
  - .gitignore
  - supabase/schema/BASELINE.md
autonomous: true
requirements: [CRIT-1]

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`bash scripts/local-stack/run.sh --print-baseline-path` prints an absolute path resolving to the tracked `supabase/schema/baseline.sql`."
    - "`bash scripts/local-stack/run.sh up` boots and loads that schema instead of exiting FATAL."
    - "`src/__tests__/baseline-wiring-claim.test.ts` passes on its WIRED branch — i.e. `BASELINE.md` carries no `NOT WIRED YET` section."
    - "No spelling of the lane-local gitignored path `scripts/local-stack/baseline.sql` remains as the lane's schema source."
  artifacts:
    - scripts/local-stack/run.sh
    - .gitignore
    - supabase/schema/BASELINE.md
  key_links:
    - "`run.sh --print-baseline-path` -> `src/__tests__/baseline-wiring-claim.test.ts` (the pre-built seam that pins the repoint in both directions)."
    - "`run.sh` repoint <-> `BASELINE.md` prose edit: ONE commit, because the vitest flips branch the instant the path resolves to the committed file."
---

<objective>
Criterion 1: make `scripts/local-stack/run.sh` read the COMMITTED `supabase/schema/baseline.sql`
instead of the gitignored `scripts/local-stack/baseline.sql`, and record the wiring in
`BASELINE.md` in the same commit.

Purpose: today the lane's consumer (`run.sh`) and `.gitignore`'s exclusion point at the SAME
gitignored file, so `run.sh up` exits FATAL and the committed baseline has no consumer at all
(measured: **0** references in `.github/workflows/`). This is the phase's tracer — it wires one
path end-to-end (script -> schema file -> a booted lane -> the vitest that pins it) and it is a
hard ordering dependency for plan 07, whose csv-finalize race spec needs PostgREST + GoTrue and
therefore this lane.

Output: a lane that boots from the committed baseline, proven by RUNNING it, not by inspection.

**Decision IDs used across this phase's plans** (from `164.5-CONTEXT.md`'s locked decisions; the
CONTEXT file states them as prose bullets, so they are numbered here once and cited by number
everywhere else):

| ID | Locked decision |
|----|-----------------|
| D-01 | DRIFT-04's `create_allocator_connected_strategy` is DROPPED, exact 11 arg types, **no `IF EXISTS`, no `CASCADE`**; the three pre-flight assertions must PRINT their measurements. |
| D-02 | **Never infer from an absent measurement.** An unavailable evidence source ABORTS; it is never rendered as a zero. |
| D-03 | **Three reviewers before any apply** (migration-reviewer, rls-policy-auditor, silent-failure-hunter) on credential-bearing production DDL. |
| D-04 | BASELINE-CONTENT-DRIFT (criterion 7) lands BEFORE any migration squash. |
| D-05 | VAC-07 scoring fence (`[VAC-07-DEFER]`): REQUIREMENTS flips VAC-07 to Complete only after RED-with-fence-removed and GREEN-with-fence-restored are both OBSERVED. |

**Planner decisions taken in this plan set** (stated so no executor picks them):

| ID | Decision | Reasoning |
|----|----------|-----------|
| P-01 | Criterion 5 takes the **disposition** path, and the criterion's wording is pinned to what the tool PRINTS. | See plan 05. |
| P-02 | Criterion 4(a) goes green by **Precedent B (dated name-pinned ratchet)**, not a declared red interval. | See plan 04. |
| P-03 | Criterion 7 goes green by **Precedent B**, with hash-pinned allowlist rows. | See plan 03. |
| P-04 | Criterion 7's gate is wired into `ci.yml`'s `sql-gate-lint` job (aggregator-blocking); criterion 2's is wired into `sql-function-snapshot.yml`. | Splits the two workflow edits across plans so waves stay file-disjoint, and puts the merge-blocking gate on the merge-blocking job. |
| P-05 | `.gitignore:138` is DELETED and its reasoning is MOVED into `BASELINE.md`. | The ROADMAP says the exclusion goes; the reasoning (a dump can carry a DSN/host/project ref, so committing one is a deliberate reviewed act preceded by a secret scan) is the asset and must survive the line. |
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-CONTEXT.md
@.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-PATTERNS.md
@scripts/local-stack/run.sh
@src/__tests__/baseline-wiring-claim.test.ts
@supabase/schema/BASELINE.md
</context>

## Artifacts this phase produces

This plan produces: a repointed `scripts/local-stack/run.sh`, a `.gitignore` without the
lane-local baseline exclusion, and a `BASELINE.md` whose `NOT WIRED YET` section is gone and
whose new prose carries the secret-scan reasoning moved out of `.gitignore`.

<tasks>

<task type="tracer">
  <name>Task 1: Repoint the lane at the committed baseline and retire the NOT WIRED YET claim — one commit</name>
  <read_first>
    - `scripts/local-stack/run.sh` — the whole file. The assignment to change is `BASELINE_FILE=` in the path block near the top (the ROADMAP's `:50` cite has DRIFTED; CONTEXT measured it at line 57, and it may drift again — locate it by the `BASELINE_FILE=` token, never by line number). Also read `load_baseline()`'s FATAL block, the non-local DSN refusal, and the `--print-baseline-path` case arm.
    - `src/__tests__/baseline-wiring-claim.test.ts` — the whole file. It is the pre-built bidirectional pin: it resolves BOTH sides to absolute paths, flips branch on the repoint, and on the WIRED branch requires `BASELINE.md` to have NO `NOT WIRED YET` section. It asserts `.gitignore`'s exclusion line only on the UNWIRED branch.
    - `supabase/schema/BASELINE.md` — the `NOT WIRED YET` section and the `sha256` table row.
    - `.gitignore` — the comment block above `scripts/local-stack/baseline.sql`.
  </read_first>
  <files>scripts/local-stack/run.sh, .gitignore, supabase/schema/BASELINE.md</files>
  <action>
    Change the `BASELINE_FILE` assignment in `scripts/local-stack/run.sh` so it resolves to the
    tracked `supabase/schema/baseline.sql` under `REPO_ROOT`. Any spelling works because the
    downstream test resolves both sides to absolute paths — prefer the direct
    `REPO_ROOT`-relative form over introducing a new variable.

    Leave `load_baseline()`'s FATAL shape intact: a missing or empty baseline stays `exit 1` with
    the "refusing to continue / a partially-applied schema is a silently-wrong baseline" reasoning.
    Leave the non-local DSN refusal untouched. Leave the `--print-baseline-path` case arm untouched
    — it is the seam the test asks through, and it must keep printing the RESOLVED value.

    Delete the `scripts/local-stack/baseline.sql` line from `.gitignore` together with its comment
    block, per P-05, and MOVE that comment block's reasoning into `BASELINE.md` — the point that a
    schema dump can carry a DSN, host or project ref, that committing one is a deliberate reviewed
    act, and that the secret scan in `scripts/local-stack/REPLAY-SPIKE.md` runs first.

    In `BASELINE.md`, remove the `NOT WIRED YET` section entirely and replace it with a section
    stating what now consumes the file (the local-stack lane, resolved through
    `--print-baseline-path`) plus the relocated secret-scan reasoning. Do NOT touch the `sha256`
    table row — plan 02's gate reads it and the file's bytes have not changed.

    All three edits go in ONE commit: the vitest flips branch the instant the path resolves, so a
    split commit is a knowingly-red tree.
  </action>
  <verify>
    <automated>bash scripts/local-stack/run.sh --print-baseline-path && npx vitest run src/__tests__/baseline-wiring-claim.test.ts && ! grep -q '^scripts/local-stack/baseline\.sql$' .gitignore && ! grep -q 'NOT WIRED YET' supabase/schema/BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/local-stack/run.sh --print-baseline-path` prints a path whose realpath equals the realpath of `supabase/schema/baseline.sql`.
    - `npx vitest run src/__tests__/baseline-wiring-claim.test.ts` passes, and its output shows the WIRED branch was taken (not the UNWIRED one).
    - `grep -rn 'local-stack/baseline\.sql' scripts/ .gitignore` returns no line that is still the lane's schema SOURCE. Prose references in `REPLAY-SPIKE.md` describing history are acceptable and must be left alone.
    - `grep -c 'REPLAY-SPIKE' supabase/schema/BASELINE.md` is >= 1 — the relocated secret-scan reasoning survived the `.gitignore` deletion.
  </acceptance_criteria>
  <done>The lane's schema source is the committed baseline, the gitignore exclusion is gone, its reasoning lives in `BASELINE.md`, and the pre-built vitest pins all of it on its WIRED branch.</done>
</task>

<task type="auto">
  <name>Task 2: Boot the lane and PASTE the evidence — "proven by running it"</name>
  <read_first>
    - `scripts/local-stack/run.sh` — the `up` path, the schema-load step, and `--self-test`. Note its own declared limit: a green `--self-test` does NOT mean the lane can run app specs, because schema load is not covered there. That is exactly why this task boots for real.
    - `.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-RESEARCH.md` — the Environment Availability table: `run.sh up` was deliberately NOT run at research time, so its Docker/CLI dependency is UNVERIFIED here and may fail.
  </read_first>
  <files>.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-01-SUMMARY.md</files>
  <action>
    Run `bash scripts/local-stack/run.sh --self-test`, then `bash scripts/local-stack/run.sh up`,
    then tear down with the script's own down/stop path. Capture the wall-clock time and the
    schema-load output.

    Paste BOTH runs' real terminal output into the SUMMARY, including exit codes. An assertion
    that the lane booted does not satisfy this project's anti-vacuity rule; criterion 1 says
    "proven by running it".

    If `up` fails for an environmental reason (Docker absent, Supabase CLI version skew — local is
    2.84.2 against a CI pin of 2.98.2), do NOT paper over it and do NOT weaken `load_baseline()`'s
    FATAL. HALT and report the exact failure with its output. A lane that cannot boot blocks
    plan 07, and that is information the phase needs, not a defect to route around.

    Record in the SUMMARY, as a stated limitation, that nothing in `.github/workflows/` runs
    `run.sh up` — so this boot is a developer-box measurement, dated, not a CI-enforced fact.
  </action>
  <verify>
    <automated>bash scripts/local-stack/run.sh --self-test && bash scripts/local-stack/run.sh up</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains verbatim pasted output of `run.sh --self-test` and `run.sh up` with their exit codes and the wall-clock duration of `up`.
    - The `up` output shows the schema being loaded from the committed `supabase/schema/baseline.sql` path.
    - The SUMMARY states plainly that no workflow invokes `run.sh up`, so this is a dated local measurement.
  </acceptance_criteria>
  <done>The lane has been observed booting from the committed baseline on a real box, with the output in the SUMMARY, or the phase has HALTED with the exact environmental failure named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> local Docker lane | A committed schema dump is loaded into a locally-reachable Postgres. |
| repo -> public GitHub | This repository is PUBLIC; the baseline is a pg_dump. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5-01 | Information Disclosure | `.gitignore` deletion normalises committing schema dumps | medium | mitigate | P-05: the secret-scan reasoning is MOVED to `BASELINE.md`, not deleted; task 1's acceptance criterion greps for its survival. |
| T-164.5-02 | Tampering | `run.sh` pointed at a non-local DSN | high | mitigate | The `*@127.0.0.1:*|*@localhost:*` refusal is explicitly left untouched by task 1. |
| T-164.5-03 | Denial of Service | A silently-degraded schema load | high | mitigate | `load_baseline()`'s FATAL keeps its shape; a missing/empty baseline stays `exit 1`, never a partial apply. |
| T-164.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan. Nothing to audit. |
</threat_model>

<verification>
- `bash scripts/local-stack/run.sh --print-baseline-path` resolves to `supabase/schema/baseline.sql`.
- `npx vitest run src/__tests__/baseline-wiring-claim.test.ts` — WIRED branch, green.
- `run.sh --self-test` and `run.sh up` output pasted in the SUMMARY.
- `npm run lint` and `npm run typecheck` clean (no TS touched, but the tree must stay green).
</verification>

<success_criteria>
Criterion 1 is met: the lane boots from the committed baseline, the old path is gone from the
script, `.gitignore` no longer excludes it, and the boot was RUN rather than reasoned about.
</success_criteria>

<output>
Create `.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-01-SUMMARY.md` when done.
</output>
