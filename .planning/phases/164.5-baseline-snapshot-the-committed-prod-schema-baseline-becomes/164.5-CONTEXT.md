# Phase 164.5 BASELINE-SNAPSHOT — CONTEXT

**Classification: INFRASTRUCTURE.** All 8 success criteria are technical (a script boots,
a gate fires, a function is absent, a probe reports zero). No user-facing behavior, no
product surface. Per `autonomous-smart-discuss.md`'s infrastructure rule, no grey-area
proposals were put to the founder: the ROADMAP already carries the decisions that matter
(DRIFT-04's DROP verdict, the three-reviewer rule, the never-infer-from-absence rule).

## Locked decisions (from ROADMAP — do NOT re-litigate)

- **DRIFT-04**: `create_allocator_connected_strategy` is DROPPED, not adopted. Founder
  decision 2026-08-29. `DROP FUNCTION` with the exact 11 arg types, **no `IF EXISTS`,
  no `CASCADE`**. Three pre-flight assertions must PRINT their measurements.
- **Never infer from an absent measurement.** The DROP aborts when its evidence source
  is unavailable (`pg_stat_statements` missing → abort). An absent measurement is never
  read as a zero.
- **Three reviewers before any apply** (migration-reviewer, rls-policy-auditor,
  silent-failure-hunter) — the DROP is on a credential-bearing surface.
- **Ordering**: criterion 8 lands BEFORE any migration squash. A squash onto an
  unpinned, already-drifted baseline freezes the drift into the new floor permanently.
- **VAC-07 scoring fence** (`[VAC-07-DEFER]`): REQUIREMENTS flips VAC-07 to Complete only
  after its spec is observed RED with the fence removed and GREEN with it restored.

## Measured at HEAD `14b3b6c3` (2026-09-07) — corrections to the ROADMAP's own citations

The ROADMAP text for this phase was written earlier; these were re-measured, not inherited.

| ROADMAP claim | Measured | Verdict |
|---|---|---|
| `scripts/local-stack/run.sh:50` | `BASELINE_FILE` is set at **line 57** | line cite DRIFTED, defect REAL |
| `.gitignore:138` excludes the baseline | line 138 = `scripts/local-stack/baseline.sql` | ACCURATE, and it IS the path `run.sh:57` resolves to — the exclusion and the consumer point at the same gitignored file, which is exactly criterion 1's defect |
| `baseline.sql` carries superseded Lock B | `grep -c ledger_refresh_enabled` = **4** | CONFIRMED |
| zero workflows reference `baseline.sql` | **0** in `.github/workflows/` | CONFIRMED — committed with no consumer and no gate |
| VAC08-LEDGER-**32** unledgered migrations | baseline file now holds **31** | 32 is STALE; `20260823120000_revoke_api_keys_insert` was applied to TEST 2026-09-07 |
| Phase 164.1's cron manifest is (7)'s oracle | `scripts/prod-prober/cron-manifest.json` exists and carries `match_engine_cron` | dependency SATISFIED |

### The 31 are genuinely absent — the name-join was checked, not assumed

On 2026-09-07 a related investigation established that reading migration drift by
**version** is wrong: repo migrations can be applied to TEST under re-stamped versions, so
a version-diff overstates what is missing. That finding does NOT shrink this criterion.
Measured directly: of the 31 baseline entries, **zero** appear in TEST's
`supabase_migrations.schema_migrations` by full name, and **zero** appear by description
with the version prefix stripped. The 31 are absent under either join. Criterion 5 is real
work at 31 entries, not an artifact of a bad comparison.

## Discharged obligation

`VAC04-ARMS-OBSERVE` — the ROADMAP said this rolls forward to 164.5 if Phase 164.7's
migration did not change a function body. It DID: VAC-04's DRIFT branch fired on both
ledger arms against the real PROD credential, with real hashes and 15-line counts, and
both were acknowledged by earned `prod-body-ack:` pragmas. **Nothing to carry here.**

## Fences

- Nothing in this phase touches shared TEST or PROD except the two reviewed migrations
  (DRIFT-04's DROP and CRON-DRIFT-01-REPAIR). Substrate for everything else is the
  pg-lane with pg_cron (Phase 164.4.1).
- Criterion 7 must consume Phase 164.7's settled `app.*` replacement mechanism
  (`system_settings` + Vault), NOT invent a second answer. The `20260408215026` GUC design
  is exactly what 164.7 retired.
- Every gate here is proven by MUTATION in both directions, never by inspection —
  criteria 2, 4, 7 and 8 all say so explicitly.

## ⛔ SCOPE AMENDMENT 2026-09-07 (founder decision) — criterion 7 is GONE from this phase

**CRON-DRIFT-01-REPAIR (old criterion 7) was SPLIT OUT into new Phase 164.5.1 CRONREPOINT.**
This phase now has **SEVEN** criteria: old 8 (BASELINE-CONTENT-DRIFT) is renumbered to 7.

**Why.** Research surfaced a conflict that must not be averaged. The ROADMAP said "write ONE
forward migration re-scheduling `match_engine_cron`". Migration
`20260907120000_analytics_service_settings_and_vault_tick.sql:14-21`, merged the SAME day,
says a migration that schedules is "a scope violation" and the registration statement "lives
THERE [the runbook] and never in a migration". Item (7) was the only item in this phase
resting on that conflict, so it left rather than being built on an unsettled rule.

**Two clauses of the old criterion 7 were also measured FALSIFIED** (verified in-session, not
taken from the research summary):

- `grep -rn decrypted_secrets supabase/ scripts/ src/` returns **3** migration files, not the
  ZERO the ROADMAP claimed. Two are `-- APP-GUC-LINEAGE:` comments **Phase 164.7 itself added**
  in commit `14b3b6c3` — the claim was true when written and our own work falsified it.
- `cron-manifest.json` jobid 1 carries `decrypted_secrets` and NOT `match_engine_cron_tick`,
  and PROD matches it today. Repointing the row turns Phase 164.1's cron-drift arm RED unless
  the manifest is re-captured in the same phase.

Both are recorded in Phase 164.5.1 so they are not re-derived there.

**Also corrected here:** the DRIFT-04 function carries `GRANT ALL` to **both** `authenticated`
AND `service_role` (`baseline.sql:13755-13756`). TODOS records only `authenticated`. Its exact
11-arg signature, read from `baseline.sql:2618` without touching PROD, is
`uuid, uuid, text, text, text, text, text, text, text, text, integer`.


---

## ⭐ FOUNDER DECISIONS 2026-09-08 (ship gate, AskUserQuestion)

### D-08 — DRIFT-04 DROP: **AUTHORISED**, sequenced as TWO PRs

Criterion 3 is no longer deferred. The founder authorised removing
`public.create_allocator_connected_strategy` from PRODUCTION, **in the two-PR sequence the
ROADMAP already records**, not folded into PR #757:

- **PR 1** — the DROP migration ONLY
  (`supabase/migrations/20260908120000_drop_create_allocator_connected_strategy.sql`).
- **PR 2** — regenerate `supabase/schema/baseline.sql` AND delete the `NAME_SET_RATCHET` row,
  **together**, immediately after. Gate (a) reads red in the window between them, which is why
  they must not be separated further.

⛔ **Merging `supabase/migrations/**` to `main` AUTO-APPLIES to PROD.** This is not revertible
by a revert commit — undoing it would mean re-creating the function from `baseline.sql`.

**The evidence the authorisation rests on, as presented at the gate:**
identity CONFIRMED by the hand-set prod database marker; SIGNATURE MATCH on all 11 argument
types; (a) live body sha256 == the committed baseline, 0 differing lines; (b.1) 0 `pg_depend`
rows; (b.2) 0 plpgsql callers; (c) `pg_stat_statements` PRESENT with **0 genuine invocations**
(the 5 observed entries were all DDL from the hand-creation, classified individually); three
reviewers (migration-reviewer, rls-policy-auditor, silent-failure-hunter) passed the migration.

⚠️ **STATED AT THE GATE AND NOT GLOSSED:** callers OUTSIDE the database are NOT proven absent —
the pre-flight script says so itself. The authorising basis is that the function has been
**NON-COMPLETABLE for every caller since 2026-08-06**, which is role- and window-independent.

### D-09 — VAC-08 `0 NEW drift`: read it on THIS PR, before merging

Criterion 5's pinned sentence is closed by MEASUREMENT rather than by decision. `sql-tests` runs
on PR #757 at `3bb3e699` and emits the ledger line. The founder chose to **wait for it, read the
actual line, and merge only if it matches** — explicitly rejecting "merge on a green board and
read it afterwards", because a passing job is not the same claim as the printed sentence, and
this project has been bitten by green badges over unread output.
⛔ If it prints anything other than what criterion 5 requires, that is a finding and the merge
stops.
