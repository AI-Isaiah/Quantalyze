---
phase: 164.5-baseline-snapshot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-baseline-staleness.mjs
  - .github/workflows/sql-function-snapshot.yml
autonomous: true
requirements: [CRIT-2]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node scripts/check-baseline-staleness.mjs` exits 0 on today's tree and exits 1, naming the defect kind, when `baseline.sql`'s bytes move without `BASELINE.md`'s recorded sha256 moving with them."
    - "`node scripts/check-baseline-staleness.mjs --self-test` proves EVERY named defect kind fires against synthetic inputs."
    - "A missing or unparseable sha256 row in `BASELINE.md` is its own named defect, not a pass."
    - "The gate runs in CI on any PR touching `supabase/schema/**` — the measured gap was ZERO workflow references to the baseline."
  artifacts:
    - scripts/check-baseline-staleness.mjs
    - .github/workflows/sql-function-snapshot.yml
  key_links:
    - "`BASELINE.md`'s sha256 table row -> the gate's recorded-hash reader (the SAME regex `baseline-wiring-claim.test.ts` already uses — one spelling, not two)."
    - "`sql-function-snapshot.yml`'s existing `supabase/schema/**` path trigger -> the new step (no new trigger is needed)."
---

<objective>
Criterion 2: a staleness (CO-EDIT) gate that FAILS when `supabase/schema/baseline.sql` changes by
one byte without a matching `BASELINE.md` update, and passes when both move — proven by MUTATION,
not by inspection.

Purpose: the sha256 itself already exists in `BASELINE.md` and is already asserted by
`src/__tests__/baseline-wiring-claim.test.ts`. The measured gap is CI placement: **zero** files in
`.github/workflows/` reference the baseline at all, so today nothing blocks a PR that edits the
dump and leaves its changelog behind.

Output: `scripts/check-baseline-staleness.mjs` (hermetic, pure-judge, `--self-test`-first) wired
into the existing hermetic `sql-function-snapshot.yml` workflow.

⚠️ **Stated overlap, not silently duplicated.** The vitest assertion in
`baseline-wiring-claim.test.ts` STAYS. It and this gate answer the same question at different
sampling rates — the vitest fires on every full-suite run, the CI step fires on every PR touching
`supabase/schema/**` including forks. The plan's rule: the vitest keeps its assertion, this gate
does not re-implement the hash extraction, and BOTH read the recorded hash through the same
regex spelling so they can never disagree by construction.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-CONTEXT.md
@.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-PATTERNS.md
@scripts/check-version-bump.mjs
@src/__tests__/baseline-wiring-claim.test.ts
@.github/workflows/sql-function-snapshot.yml
</context>

## Artifacts this phase produces

This plan produces: `scripts/check-baseline-staleness.mjs` (new) and two new steps in
`.github/workflows/sql-function-snapshot.yml` (self-test, then the real check).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the hermetic co-edit gate with a pure judge and named defect kinds</name>
  <read_first>
    - `scripts/check-version-bump.mjs` — the whole file. This is the EXACT species analog: a hermetic node gate over two committed artifacts, exporting a PURE `judge()` so the self-test drives real logic rather than a parallel copy, with a `DEFECTS` array of named kinds and a header that says why a never-observed red path is the defect this repo removes.
    - `src/__tests__/baseline-wiring-claim.test.ts` — specifically the two lines that already do this work: the regex that extracts the `sha256` row from `BASELINE.md`, and the `createHash("sha256")` over `readFileSync(BASELINE_SQL)`. Copy those spellings; do not invent a second parser.
    - `scripts/lint-app-guc.mjs` — its `invokedDirectly()` realpath-safe main-module guard, to be copied verbatim.
  </read_first>
  <files>scripts/check-baseline-staleness.mjs</files>
  <behavior>
    - Recorded hash present and equal to the actual sha256 of `baseline.sql` -> zero defects, exit 0.
    - Recorded hash present but different -> one defect, kind `baseline-sha-mismatch`, exit 1, printing BOTH hashes.
    - `BASELINE.md` has no parseable sha256 row -> one defect, kind `baseline-sha-absent`, exit 1. ⛔ This is D-02 applied here: a hash that could not be READ is never a pass.
    - `baseline.sql` itself missing or empty -> its own named defect, exit 1. "Could not measure" and "measured zero problems" do not share a code path.
    - `--self-test` drives `judge()` directly on synthetic inputs and asserts each kind fires; it prints an assertion COUNT that must be kept true.
  </behavior>
  <action>
    Create the gate with a header in the house style: what defect class it exists for, the exact
    commands CI will paste verbatim (`--self-test` and the bare invocation), and the stated overlap
    with `baseline-wiring-claim.test.ts` plus why both are kept.

    Export a PURE `judge({ recordedSha, actualSha })` returning an array of named defects, and an
    exported `DEFECTS` list naming every kind. Keep all file I/O out of `judge()` so the self-test
    exercises the real decision logic.

    Reporting must never let clean and did-not-run look alike: print a summary line at zero as well
    as non-zero, naming the file scanned and the count. Use the `::error::` annotation form the
    sibling gates use.

    Use the realpath-safe `invokedDirectly()` guard so `process.exit(main(...))` runs only under
    direct invocation and the module stays importable by a test.

    Do NOT print the baseline's contents in any mode — this repository is PUBLIC and the file is a
    pg_dump. Hashes and a byte count only.
  </action>
  <verify>
    <automated>node scripts/check-baseline-staleness.mjs --self-test && node scripts/check-baseline-staleness.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-baseline-staleness.mjs --self-test` exits 0 and prints an assertion count.
    - `node scripts/check-baseline-staleness.mjs` exits 0 on the unmodified tree and prints a summary line naming the file and a zero count.
    - `node -e "import('./scripts/check-baseline-staleness.mjs').then(m=>{if(m.DEFECTS.length<3)process.exit(1)})"` exits 0 — at least three named defect kinds are exported.
    - The extraction of the recorded hash uses the same regex shape as `baseline-wiring-claim.test.ts`; a diff of the two spellings shows no semantic difference.
  </acceptance_criteria>
  <done>The gate exists, its self-test proves each red path fires, and it is green on today's tree.</done>
</task>

<task type="auto">
  <name>Task 2: MUTATION-prove the gate in both directions, restoring from a BYTE BACKUP</name>
  <read_first>
    - `scripts/check-baseline-staleness.mjs` — the file just written.
    - `supabase/schema/BASELINE.md` — the sha256 row.
  </read_first>
  <files>.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-02-SUMMARY.md</files>
  <action>
    Take byte backups of `supabase/schema/baseline.sql` and `supabase/schema/BASELINE.md` with `cp`,
    naming them `164.5-02-baseline.sql` and `164.5-02-BASELINE.md`. ⛔ PREFIX THE PLAN ID: the
    scratchpad is NOT per-worktree even though `use_worktrees` is true, and plan 03 backs up the
    SAME file in the same wave — an unprefixed basename lets a restore pull the sibling's copy
    to a scratch directory FIRST. ⛔ `git checkout --` is BANNED as the restore mechanism here: it
    restores to HEAD and silently destroys the harness's own uncommitted edits.

    Arm 1 (RED): append one byte to `baseline.sql`, leave `BASELINE.md` alone, run the gate, record
    the non-zero exit and the printed defect kind. Restore from the byte backup and verify the
    restore by re-running the gate to green AND by comparing the sha to the recorded one.

    Arm 2 (GREEN, the co-move): append the same byte, recompute the sha, update `BASELINE.md`'s
    recorded row to match, run the gate, record the zero exit. Restore BOTH files from backup.

    Arm 3 (RED, the absent-measurement direction): delete the sha256 row from `BASELINE.md` only,
    run the gate, record the non-zero exit and that the kind printed is the ABSENT kind, not the
    mismatch kind — the two states must be distinguishable in the output. Restore.

    Paste all three arms' real commands, output and exit codes into the SUMMARY. After the last
    restore, confirm the tree is clean with `git status --porcelain supabase/schema/`.
  </action>
  <verify>
    <automated>node scripts/check-baseline-staleness.mjs && test -z "$(git status --porcelain supabase/schema/)"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains three arms, each with its command, its verbatim output and its exit code.
    - Arm 1 and arm 3 exit non-zero and print DIFFERENT defect kinds.
    - Arm 2 exits 0.
    - `git status --porcelain supabase/schema/` is empty after the harness finishes.
  </acceptance_criteria>
  <done>The gate has been observed failing on a one-byte change without a co-edit, passing when both move, and failing distinguishably when the recorded hash cannot be read.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the gate into the hermetic CI workflow, self-test first</name>
  <read_first>
    - `.github/workflows/sql-function-snapshot.yml` — the whole file: its `on.pull_request.paths` list (which already includes `supabase/schema/**`, so no new trigger is needed), its hermetic-by-design header comment, and its existing two-step self-test-then-corpus ordering.
    - `.github/workflows/ci.yml` — the `sql-gate-lint` job's step comments, for the wording that explains WHY the self-test runs first.
  </read_first>
  <files>.github/workflows/sql-function-snapshot.yml</files>
  <action>
    Add two steps to the existing job: the gate's `--self-test` first, then the bare invocation.
    Both commands pasted VERBATIM from the script's own header — a wrapped or re-spelled run is a
    different run, and mode identity is a named pitfall in this repo.

    Add the step comment explaining the ordering: a corpus step reporting "0 findings" says nothing
    unless the rules can still fire, and the self-test is what distinguishes a clean corpus from a
    defanged gate.

    Add `scripts/check-baseline-staleness.mjs` to the workflow's `paths:` trigger list so a change
    to the gate itself re-runs it.

    State in a comment that this workflow is a STANDALONE check and is NOT in `ci.yml`'s `frontend`
    aggregator, so it blocks a merge only through its own required-check status — and say so
    plainly rather than implying it is aggregator-gated.

    Do not touch the existing `dump-sql-functions.ts` steps; plan 04 owns that script.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/sql-function-snapshot.yml','utf8'); const n=(y.match(/check-baseline-staleness\.mjs/g)||[]).length; if(n<3){console.error('expected the gate in paths + 2 steps, saw '+n);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'check-baseline-staleness' .github/workflows/sql-function-snapshot.yml` is at least 3 (paths entry, self-test step, corpus step).
    - The self-test step appears at a LOWER line number than the bare-invocation step.
    - `grep -n 'supabase/schema/\*\*' .github/workflows/sql-function-snapshot.yml` still matches — the pre-existing trigger was not disturbed.
    - The workflow file still parses and lints: `actionlint .github/workflows/sql-function-snapshot.yml` exits 0. ⚠️ MEASURED 2026-09-07: `actionlint` IS installed (`/opt/homebrew/bin/actionlint`) and `node -e "require('js-yaml')"` DOES succeed on this box — an earlier draft of this criterion asserted both were unavailable and prescribed an indentation eyeball, which is not a runnable check. If `actionlint` is genuinely absent in the execution environment, fall back to `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/sql-function-snapshot.yml','utf8'))"` and record which was used.
  </acceptance_criteria>
  <done>Every PR touching `supabase/schema/**` runs the co-edit gate's self-test and then the gate, and the workflow says out loud that it is standalone rather than aggregator-gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author -> committed schema dump | An untrusted contributor can edit `baseline.sql`. |
| gate output -> public CI log | This repository is PUBLIC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5-04 | Tampering | A silent edit to `baseline.sql` | high | mitigate | The co-edit gate fails on a one-byte change without a `BASELINE.md` update, proven by task 2 arm 1. |
| T-164.5-05 | Information Disclosure | Gate printing dump contents into a public log | high | mitigate | Task 1: hashes and a byte count only, never file contents, in any mode. |
| T-164.5-06 | Repudiation | A gate that reads green while blind | critical | mitigate | `--self-test` runs BEFORE the corpus step and proves each named defect kind can still fire; an absent/unparseable hash is its own defect kind (D-02). |
| T-164.5-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan. Nothing to audit. |
</threat_model>

<verification>
- `node scripts/check-baseline-staleness.mjs --self-test` exit 0.
- `node scripts/check-baseline-staleness.mjs` exit 0.
- Three mutation arms pasted in the SUMMARY with exit codes.
- `npx vitest run src/__tests__/baseline-wiring-claim.test.ts` still green (the vitest assertion was kept, not replaced).
- `npm run lint` clean.
</verification>

<success_criteria>
Criterion 2 is met: the staleness gate FAILS on a one-byte change to `baseline.sql` without a
matching `BASELINE.md` update and PASSES when both move — observed by mutation, restored from byte
backups, and running in CI on the path that can trigger it.
</success_criteria>

<output>
Create `.planning/phases/164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes/164.5-02-SUMMARY.md` when done.
</output>
