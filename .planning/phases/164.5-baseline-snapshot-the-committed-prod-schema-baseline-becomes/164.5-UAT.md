---
status: partial
phase: 164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes
source: [164.5-VERIFICATION.md]
started: 2026-09-08T02:33:00Z
updated: 2026-09-08T02:35:00Z
---

## Current Test

[testing paused — 3 items outstanding: 1 fixed by the ship step, 1 founder decision, 1 needs a credentialed CI run]

## Tests

### 1. Rebuild the stale PR branch before merge
expected: |
  The rebuilt `-pr` branch carries the CR-01 zero-comparison guard (`COMPARED_FLOOR`)
  and the CR-02 step (`prod-body-drift-check.sh --self-test` in ci.yml), and
  `git diff --diff-filter=D --name-only main..<pr-branch> -- .planning/` is EMPTY.
result: [pending]
reason: |
  ⛔ BLOCKER, independently re-measured by the orchestrator at d6d929cb. All four
  review-fix commits are ABSENT from `phase-164.5-baseline-snapshot-pr` (ac68bed2).
  On that branch: COMPARED_FLOOR 0 hits (HEAD 5), the CR-01 floor 0 hits (HEAD 2),
  the CR-02 ci.yml step 0 hits (HEAD 2). Merging #757 as it stands ships BOTH
  Critical findings unfixed.
  Not a codebase defect — a ship-path defect, and `/gsd-ship`'s
  `filter_planning_artifacts` is the step that fixes it. Deferred to that step
  DELIBERATELY rather than hand-patched here.

### 2. Founder authorisation for the DRIFT-04 DROP (criterion 3)
expected: Either the apply proceeds under the recorded two-PR sequence, or the deferral is re-affirmed and criterion 3 stays open.
result: blocked
blocked_by: founder-decision
reason: |
  One-way production DDL on a SECURITY DEFINER function that writes encrypted
  credential material. Authorisation has not been given, and the phase is
  explicitly 6 of 7. Better supported than at plan time: the pre-flight SCRIPT ran
  against real PROD in the --expect-present direction and exited 0 with all four
  measurements printed (164.5-06-PROD-PREFLIGHT-RUN.md).

### 3. Lane boot + VAC-07 spec on the REGENERATED baseline bytes
expected: |
  `up` exits 0 and logs `baseline loaded from …/supabase/schema/baseline.sql`;
  the VAC-07 spec is 2 passed / 0 skipped.
result: pass
reported: |
  MEASURED 2026-09-08 by the orchestrator, not handed back as an open item.
  This was the verifier's one item that was testable on this box, because Docker
  works here. Criteria 1 and 6 had BOTH been observed against baseline.sql sha
  514ba9bc…, and 5c8d0fef replaced it with 9fad9a1b… (449 changed lines) — so no
  lane boot or VAC-07 run existed on the shipping bytes.
    - bytes under test: baseline.sql sha256 9fad9a1b4c3cedac1788…c99168f, which is
      byte-for-byte what BASELINE.md records (checked, not assumed)
    - `run.sh up` -> UP_EXIT=0 in 28s, logging
      `[local-stack] baseline loaded from <repo>/supabase/schema/baseline.sql`
    - `vitest run --config vitest.local-stack.config.ts` -> Test Files 1 passed (1),
      **Tests 2 passed (2)**, exit 0
    - ⛔ the SHAPE was checked, not just the exit code: 2 passed / 0 SKIPPED. A
      `2 skipped` result is how the csv-finalize-rpc.test.ts tombstone is
      remembered and is what a silently-absent lane looks like.
    - teardown: `run.sh down` complete; `--assert-teardown` ->
      `surviving quantalyze containers -> 0 (measured; docker ps exited 0)`, exit 0;
      independent `docker ps` also 0.
  ⚠️ One honest note on method: the first grep over the spec log matched nothing and
  the run looked like a 1-second no-op. It was a too-narrow grep, not a silent
  green — the full log reads `Tests 2 passed (2)`. Recorded because the near-miss
  is the exact failure mode this criterion exists to catch.

### 4. VAC-08 prints `0 NEW drift` on its next CREDENTIALED run
expected: "`ledger presence: 31 absent, all 31 baselined …; 0 NEW drift`"
result: blocked
blocked_by: server
reason: |
  Needs a credentialed CI run against shared TEST; cannot be produced locally, and
  fabricating it would be the "never infer from an absent measurement" failure this
  phase is largely about. Read it off the PR's `sql-tests` job.

## Summary

total: 4
passed: 1
issues: 0
pending: 1
skipped: 0
blocked: 2

## Gaps

<!-- No behavioural gaps. Test 1 is a ship-path defect with a named owner (/gsd-ship);
     tests 2 and 4 are blocked on a human decision and on a credentialed run. None of
     these is a defect in the phase's delivered code. -->
