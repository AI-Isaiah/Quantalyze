---
phase: 164.5-baseline-snapshot-the-committed-prod-schema-baseline-becomes
verified: 2026-09-08T02:35:00Z
status: human_needed
score: 6/6 in-scope must-haves verified (criterion 3 DEFERRED-BY-DECISION, excluded by founder)
behavior_unverified: 0
overrides_applied: 0
verified_at_commit: d6d929cba5e59cdc0826c5dbd9f84c965039754f
verified_on_branch: phase-164.5-baseline-snapshot
deferred_by_decision:
  - criterion: 3
    item: "DRIFT-04 — DROP FUNCTION public.create_allocator_connected_strategy(11 args) applied to PROD"
    reason: "One-way production DDL on a SECURITY DEFINER function that writes encrypted credential material. Founder authorisation not given. Phase is explicitly 6 of 7."
    evidence:
      - "supabase/migrations/20260908120000_drop_create_allocator_connected_strategy.sql exists on the working branch (251 lines); DELIBERATELY absent from phase-164.5-baseline-snapshot-pr (git ls-tree measured: 0 hits)"
      - "scripts/preflight-drop-allocator-fn.sh exists on the working branch (1732 lines); likewise absent from the PR branch"
      - "The pre-flight SCRIPT was RUN against real PROD in the --expect-present direction and exited 0 with all four measurements PRINTED — 164.5-06-PROD-PREFLIGHT-RUN.md (IDENTITY CONFIRMED via ALLOC_EXPECT_DB_MARKER=PRODUCTION, SIGNATURE MATCH 11 args, (a) body sha256 equal / 0 differing lines, (b.1) 0 pg_depend rows, (b.2) 0 prosrc callers, (c) pg_stat_statements PRESENT with 5 observed calls)"
human_verification:
  - test: "⛔ BLOCKER-SEVERITY, PRE-MERGE: rebuild the PR branch. `phase-164.5-baseline-snapshot-pr` (local AND origin) is stale at ac68bed2 and carries NONE of the four review-fix commits (ab8c5623, 4251c63c, f181a2b2, d6d929cb). Re-run /gsd-pr-branch from HEAD (d6d929cb) with the `git rm` scoped to each commit's own paths, then re-run the deletion guard."
    expected: "The rebuilt -pr branch contains the CR-01 zero-comparison guard (COMPARED_FLOOR in scripts/baseline-content-drift-check.mjs) and the CR-02 step (`prod-body-drift-check.sh --self-test` in .github/workflows/ci.yml), and `git diff --diff-filter=D --name-only main..<pr-branch> -- .planning/` is EMPTY."
    why_human: "MEASURED on the current -pr branch: 0 occurrences of `compared === 0|comparedFloor|COMPARED_FLOOR` and 0 occurrences of `prod-body-drift-check.sh --self-test`. Merging as-is ships BOTH Critical review findings unfixed — criterion 7's gate without its zero-comparison guard, and criterion 4(b)'s only falsifiability control still invoked by nothing. Rebuilding a PR branch is a ship-step decision, not a code change."
  - test: "Founder authorisation for the DRIFT-04 DROP (criterion 3). The pre-flight has now RUN against PROD (exit 0). Decide whether to apply supabase/migrations/20260908120000, and in the two-PR sequence the ROADMAP records (PR 1 = DROP only; PR 2 = regenerate baseline.sql AND delete the NAME_SET_RATCHET row, together, immediately after)."
    expected: "Either the apply proceeds under the recorded sequence, or the deferral is re-affirmed and criterion 3 stays open."
    why_human: "One-way production DDL on a credential-bearing SECURITY DEFINER surface. Not an agent decision, by explicit founder instruction."
  - test: "Re-run `bash scripts/local-stack/run.sh up` and then `npx vitest run --config vitest.local-stack.config.ts` on the CURRENT baseline.sql (sha256 9fad9a1b…)."
    expected: "`up` exits 0 and logs `baseline loaded from …/supabase/schema/baseline.sql`; the VAC-07 spec is 2 passed / 0 skipped."
    why_human: "Criterion 1's boot and criterion 6's GREEN were both measured against baseline sha256 514ba9bc…. Commit 5c8d0fef then REGENERATED the file from PROD (449 changed lines: system_settings + policies, match_engine_cron_tick, two ledger bodies, provenance columns) and no lane boot has been recorded on the new bytes. The load runs under psql ON_ERROR_STOP=1, so a statement the local image cannot execute would break the lane silently for the next reader. Verified as far as inspection allows: the fence index `strategies_user_wizard_session_source_uniq` survives the regeneration (baseline.sql:12075), and the 6 CREATE EXTENSION lines are in the unchanged header region. Requires Docker; out of a verifier's hermetic remit."
  - test: "Read `sql-tests` / VAC-08's next CREDENTIALED CI run and confirm it prints `0 NEW drift` with 31 baselined entries."
    expected: "`ledger presence: 31 absent, all 31 baselined …; 0 NEW drift`"
    why_human: "Criterion 5's second clause is about what a credentialed job PRINTS against shared TEST. No credential is available to a verifier, and the repo forbids running against TEST/PROD from this checkout."
info_findings:
  - file: CHANGELOG.md
    line: 12
    issue: "The 0.77.19.0 entry says criterion 2's self-test is `10/10`. MEASURED at HEAD: `12/12 declared assertions across 9 sections` — arms IN-02/IN-03 were added by 4251c63c/f181a2b2 after the entry was written. Prose stale-integer only; the gate itself refuses to pass when the number that RAN disagrees with the number it declares."
    severity: info
  - file: .planning/phases/164.5-.../164.5-REVIEW.md
    line: 26
    issue: "Frontmatter `status: issues_found` is STALE. Independently re-verified: every iteration-2 finding is closed or booked at HEAD."
    severity: info
---

# Phase 164.5: BASELINE-SNAPSHOT Verification Report

**Phase Goal:** `supabase/schema/baseline.sql` (committed 2026-08-29) becomes load-bearing instead of decorative — repointed as the local-stack lane's schema source, pinned by a staleness gate and a content-drift gate, with DRIFT-05 split into two gates rather than conflated, the 31 unledgered migrations dispositioned by name, and VAC-07's concurrent csv-finalize spec proven RED-then-GREEN.

**Verified:** 2026-09-08T02:35:00Z at `d6d929cb` on `phase-164.5-baseline-snapshot`
**Status:** human_needed
**Re-verification:** No — initial verification

⚠️ **Scope note honoured:** criterion 3 (the DRIFT-04 `DROP FUNCTION`) is DELIBERATELY NOT DELIVERED by founder decision. It is recorded below as DEFERRED-BY-DECISION with its evidence, NOT as a gap. The phase is explicitly 6 of 7.

⛔ **Every mutation below was applied to a `cp` byte backup and restored with `cp`. `git checkout --` was used NOWHERE. Every restore was confirmed with `shasum -a 256` and `git status --porcelain` (empty at the end of every block, and at the end of this verification).**

---

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP success criterion) | Status | Evidence — MEASURED, not read |
|---|---|---|---|
| 1 | `run.sh up` boots from `supabase/schema/baseline.sql`; the old path is gone from the script and `.gitignore` no longer excludes the file. Proven by running it. | ✓ VERIFIED | `bash scripts/local-stack/run.sh --print-baseline-path` → `…/supabase/schema/baseline.sql`, exit 0 (run by me). `run.sh:57` sets `BASELINE_FILE="${REPO_ROOT}/supabase/schema/baseline.sql"`; no spelling of `${LANE_DIR}/baseline.sql` survives as the lane's source. `git check-ignore supabase/schema/baseline.sql` → exit 1 (NOT ignored); `git ls-files --error-unmatch` → tracked. `grep "NOT WIRED YET" supabase/schema/BASELINE.md` → exit 1 (claim retired). Real boot with an INDEPENDENT live-DB oracle recorded in `164.5-01-SUMMARY.md` (LEG 2, `UP_EXIT=0`, 27 s, `baseline loaded from …/supabase/schema/baseline.sql`, then a `pg_proc` query proving the DRIFT-04 baseline-only function is present in the lane DB). See caveat ⚠️1 below. |
| 2 | The staleness gate FAILS on a one-byte change to `baseline.sql` without a matching `BASELINE.md` update — proven by mutation, not inspection. | ✓ VERIFIED | **Mutation run by me.** Appended 2 lines to `supabase/schema/baseline.sql` → `node scripts/check-baseline-staleness.mjs` exit **1**, `baseline-sha-mismatch`, printing BOTH hashes (`recorded 9fad9a1b…` vs `actual 642d8ade…`) and the new byte count. Restored by `cp`; `9fad9a1b…` before == after. Clean tree: exit 0, `defects=0`. `--self-test` **12/12 across 9 sections**, exit 0, and section 9 asserts `DEFECTS` names exactly the kinds `judge()` can emit. |
| 3 | DRIFT-04: the DROP reaches PROD only after all three pre-flight assertions pass and PRINT their measurements; afterwards the function is absent and `database.types.ts` is regenerated. | ⏸ DEFERRED-BY-DECISION | Not a gap. See the `deferred_by_decision` block above. What IS verifiable and was verified: the migration carries the exact 11 arg types, **no `IF EXISTS`**, **no `CASCADE`** (`grep` measured), is transaction-wrapped, and carries two `RAISE EXCEPTION` self-verify checks (`:218` overload-count, `:246` surviving-arity enumeration). The pre-flight SCRIPT ran against real PROD, `--expect-present`, **exit 0**, every measurement printed with its stated bound. Both files are on the working branch and DELIBERATELY absent from the PR branch (measured). |
| 4 | DRIFT-05 (a) hermetic assertion in `dump-sql-functions.ts --check`, RED fixture in each direction; (b) runs on the credentialed job and exits 1 when its credential is absent, never skips. | ✓ VERIFIED | **(a)** `--check` exit 0, printing `baseline 121 name(s) vs migration-replay 120 name(s) — agree, with 1 ratcheted disagreement(s)`. `--self-test` `PASS: 22 assertions`. **Both directions neutered independently by me**: `if (snapshotNames.has(name)) continue` → `if (true) continue` ⇒ `SELF-TEST FAIL: a baseline-only name must produce a drift row`, exit 1; `if (baselineNames.has(name))` → `if (true)` ⇒ `SELF-TEST FAIL: a snapshot-only name must produce a drift row`, exit 1. Distinct messages, so the two fixtures are not one fixture. **(b)** Run by me with the three vars unset: exit **1**, `SUPABASE_PROJECT_REF is not configured, so this gate CANNOT read PROD… This is a HARD FAILURE, not a skip`, zero `::notice::`. `--self-test` **8/8**; neutering `assert_credentials`' emptiness test to `if false` ⇒ **6/8**, exit 1, both credential arms named. Wired at `migration-drift-check.yml:298`, with `supabase/schema/baseline.sql` an ACTIVE entry in its `paths:` (`:56`). Both scripts' headers state that neither gate covers the other; `--check`'s success line repeats it. |
| 5 | VAC08-LEDGER: every one of the 31 unledgered migrations is dispositioned by name, and VAC-08's next credentialed run prints `0 NEW drift`. | ✓ VERIFIED (disposition half; the printed line is credentialed — human item) | 31 non-comment entries; **31 carry a trailing `#` disposition; 0 do not** (counted). `bash scripts/test-ledger-drift-check.sh --self-test` **5/5**, exit 0. `src/__tests__/vac08-ledger-dispositions.test.ts` **10/10**, and FALSIFIABLE — I appended `20260101000000_probe_undispositioned` and got **2 named failures** (`the entry count matches the pinned integer, in BOTH directions` and `every entry carries a trailing '#' disposition`), the second printing `line 106: 20260101000000_probe_undispositioned`. Restored by `cp`. **Byte-identity claim INDEPENDENTLY re-derived**: the gate's own parse (`sed 's/#.*//' | sed 's/[[:space:]]*$//' | sed '/^$/d'`, `test-ledger-drift-check.sh:361`) hashes to SHA-1 `087de90982b88a834ece166efd7a00969e95de06` at BOTH `14b3b6c3` (pre-disposition) and HEAD; `cmp` reports the two files identical. ROADMAP criterion 5's wording carries the founder AMENDMENT. |
| 6 | VAC-07: the concurrent csv-finalize spec exists, was observed RED with the fence removed and GREEN with it restored, and only then does REQUIREMENTS flip VAC-07 to Complete. | ✓ VERIFIED | The `[VAC-07-DEFER]` fence is satisfied BY OBSERVATION, not assertion. `164.5-07-SUMMARY.md:155-250` pastes both runs VERBATIM with exit codes: RED = `AssertionError: exactly ONE of the two concurrent csv-finalize POSTs may return a 2xx applied receipt… Both outcomes were: A:200:ok, B:200:ok`, `SPEC_EXIT=1` — an assertion failure that names itself, not a bare non-zero exit; GREEN = `Tests 2 passed (2)`, `SPEC_EXIT=0`. Byte backup taken FIRST and restored by `cp`, `514ba9bc…` before == after, plus two `grep -c … = 1` re-checks and an empty `git status --porcelain`. Ordering honoured: `REQUIREMENTS.md:163/207` flip VAC-07 to Complete citing that observation. **Measured by me:** spec is 529 lines, contains **0** `it.skip`/`describe.skip`/`test.skip`; run with no lane it FAILS loudly — `Tests 2 failed (2)`, `LOCAL-STACK LANE ABSENT`, **zero skipped**. CI: `frontend-local-stack` job at `ci.yml:495`, in the `frontend` aggregator's `needs:` (`:2199`) AND its result loop (`:2218`). See caveat ⚠️2 below. |
| 7 | BASELINE-CONTENT-DRIFT: a gate compares the committed baseline against the applied migration chain and FAILS on a body that disagrees, naming that function — proven by mutation in BOTH directions. | ✓ VERIFIED | Clean run: `chain files 120`, `functions compared 122 — MATCH 119, DRIFT 3`, `findings 0`, exit 0. `--self-test` `41 assertions across 4 finding kinds`. **Mutation run by me (revert-a-body direction):** injected `v_probe INTEGER := 42;` into `supabase/schema/functions/_assert_no_public_execute.sql` ⇒ exit **1**, `::error file=…::content-drift — DRIFT _assert_no_public_execute/1 snapshot=eb486559… chain=7058bfe1… differing-lines=2`. It NAMES the function, prints name/argcount/hashes/differing-lines and **no body text** — the D-05 output contract. Restored by `cp`, sha256 matched. The regenerate-and-observe-GREEN direction is recorded at `5c8d0fef` (five allowlist rows went MATCH and were deleted) with its own anti-vacuity mutation on `match_engine_cron_tick`. Wired into `sql-gate-lint` (`ci.yml:1039` self-test then `:1041` corpus, job boundary 913–1069), which is in the aggregator's `needs:` (`:2204`) and result loop (`:2223`). |

**Score: 6/6 in-scope criteria verified. Criterion 3 DEFERRED-BY-DECISION (excluded by founder, evidenced).**

#### ⚠️ Caveats attached to VERIFIED criteria (both routed to human verification)

- **⚠️1 / ⚠️2 — the substrate moved AFTER the two behavioural observations.** Criterion 1's boot (`UP_EXIT=0`) and criterion 6's RED/GREEN were both taken against `baseline.sql` sha256 `514ba9bc…`. Commit `5c8d0fef` then regenerated the file from PROD to `9fad9a1b…` (449 changed lines). Neither the lane boot nor the VAC-07 spec has been executed on the new bytes. The criteria themselves are historical observation predicates and were satisfied with strong, verbatim, byte-backed evidence — so they are VERIFIED, not downgraded. But the *current* substrate is unexercised, so a re-run is listed as a human item. Two things reduce the risk and were verified by inspection: the fence index survives at `baseline.sql:12075`, and all 6 `CREATE EXTENSION` lines sit in the region the regeneration did not touch (its first hunk starts at line 3766).

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `scripts/local-stack/run.sh` | lane reads the committed baseline | ✓ VERIFIED | 421 lines; `BASELINE_FILE` at `:57`; `--print-baseline-path` seam exercised |
| `.gitignore` | committed baseline not excluded | ✓ VERIFIED (documented deviation) | `supabase/schema/baseline.sql` NOT ignored. `scripts/local-stack/baseline.sql` (a DIFFERENT path, now consumer-less) IS still ignored at `:154` — deliberately RESTORED under review finding WR-05 because `REPLAY-SPIKE.md` still carries a runnable `supabase db dump -f` aimed at it and this repo is PUBLIC. Rationale is 17 comment lines at the line itself. Criterion 1's intent — the committed dump is readable, tracked and load-bearing — is met. |
| `supabase/schema/BASELINE.md` | NOT WIRED YET retired; sha256 provenance row | ✓ VERIFIED | 202 lines; zero `NOT WIRED YET`; exactly one sha256 row (the gate refuses ≥2) |
| `scripts/check-baseline-staleness.mjs` | co-edit gate + self-test | ✓ VERIFIED | 353 lines; 3 defect kinds, all fired on their own input |
| `.github/workflows/sql-function-snapshot.yml` | crit-2 CI placement | ✓ VERIFIED | `supabase/schema/**` on BOTH `pull_request` and `push`; self-test step BEFORE the check; both before `npm ci`; no `if:`, no `continue-on-error` |
| `scripts/baseline-content-drift-check.mjs` | crit-7 gate | ✓ VERIFIED | 915 lines; `COMPARED_FLOOR = 122`; `CONTENT_DRIFT_ALLOWLIST` 3 dated rows, each with both hashes, `capturedAt`, `clearedBy`, `reason` |
| `src/__tests__/baseline-content-drift-allowlist.test.ts` | independent allowlist re-derivation | ✓ VERIFIED | 448 lines, 18 arms; two independent derivations (module import vs regex over bytes) must agree; `PINNED_SIZE = 3` ratchets in BOTH directions with distinct GREW/STALE messages |
| `scripts/dump-sql-functions.ts` | DRIFT-05 (a) | ✓ VERIFIED | 699 lines; `diffNameSets` + `NAME_SET_RATCHET`; side-flip, present-on-both and present-on-neither are each their own named defect |
| `scripts/prod-body-drift-check.sh` | DRIFT-05 (b) | ✓ VERIFIED | 1372 lines; 8 self-test arms; `[WR-D-DUMP-SCOPE]` premise gap recorded at `:234` inside the refusal block rather than papered over |
| `.github/workflows/migration-drift-check.yml` | (b) on VAC-04's credentialed job | ✓ VERIFIED | `:298` invocation; `:56` `paths:` entry, ACTIVE (proven by mutation) |
| `scripts/vac08-ledger-baseline.txt` | 31 named dispositions | ✓ VERIFIED | 105 lines; 31/31 dispositioned |
| `src/__tests__/vac08-ledger-dispositions.test.ts` | independent pin | ✓ VERIFIED | 249 lines, 10 arms, falsifiable in both directions |
| `src/__tests__/csv-finalize-concurrent-never-classified.test.ts` | VAC-07 spec | ✓ VERIFIED | 529 lines; two clients, separate tokens, `AsyncLocalStorage` per-request binding; a dedicated arm asserts `tokenA !== tokenB` |
| `vitest.local-stack.config.ts` | lane config | ✓ VERIFIED | 74 lines; `setupFiles`/`unstubGlobals`/`unstubEnvs` restated and now pinned on NON-COMMENT lines |
| `scripts/preflight-drop-allocator-fn.sh` | DRIFT-04 pre-flight | ✓ VERIFIED (not applied) | 1732 lines; ran against real PROD, exit 0 |
| `supabase/migrations/20260908120000_…sql` | DRIFT-04 DROP | ⏸ DEFERRED-BY-DECISION | 251 lines; exact 11 args, no `IF EXISTS`, no `CASCADE`, transaction-wrapped, two `RAISE EXCEPTION` self-verifies |

---

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `run.sh --print-baseline-path` | `baseline-wiring-claim.test.ts` | pre-built seam | ✓ WIRED | 146-line spec green; WIRED branch taken |
| `BASELINE.md` sha256 row | `check-baseline-staleness.mjs` reader | shared `RECORDED_SHA_RE` | ✓ WIRED | one spelling; IN-C brought the sibling spec onto the same ambiguity contract |
| `sql-body-normalize.mjs` `diffFunctionBodies()` | content-drift gate | single declared normalizer | ✓ WIRED | no second normalizer exists (D-05 of 164.3 honoured) |
| content-drift gate | `frontend` aggregator | `sql-gate-lint` in `needs:` AND result loop | ✓ WIRED | `ci.yml:2204` + `:2223` — both lists carry it, so the check can block |
| VAC-07 spec | `frontend` aggregator | `frontend-local-stack` in `needs:` AND result loop | ✓ WIRED | `ci.yml:2199` + `:2218` |
| DRIFT-05 (b) | VAC-04's existing job + credential | `migration-drift-check.yml` | ✓ WIRED | rides the existing `supabase db dump`; no second copy of production access minted |
| `_pr` branch | `main` | `/gsd-pr-branch` filter | ✗ **NOT CURRENT** | see BLOCKER-severity finding below |

---

### Behavioural Spot-Checks (all run by me at `d6d929cb`)

| Behaviour | Command | Result | Status |
|---|---|---|---|
| lane resolves the committed baseline | `bash scripts/local-stack/run.sh --print-baseline-path` | `…/supabase/schema/baseline.sql`, exit 0 | ✓ PASS |
| staleness gate clean | `node scripts/check-baseline-staleness.mjs` | `defects=0`, exit 0 | ✓ PASS |
| staleness gate self-test | `… --self-test` | `12/12 across 9 sections`, exit 0 | ✓ PASS |
| staleness gate FIRES | mutate `baseline.sql`, re-run | exit 1, `baseline-sha-mismatch`, both hashes printed | ✓ PASS |
| content-drift gate clean | `node scripts/baseline-content-drift-check.mjs` | `compared 122 … findings 0`, exit 0 | ✓ PASS |
| content-drift self-test | `… --self-test` | `41 assertions across 4 finding kinds`, exit 0 | ✓ PASS |
| content-drift gate FIRES + names | mutate a chain body | exit 1, names `_assert_no_public_execute/1`, hashes + differing-lines, no body text | ✓ PASS |
| CR-01 floor, runner layer | `COMPARED_FLOOR` 122→200 | `MEASURE_FAIL — only 122 … below the pinned COMPARED_FLOOR of 200`, exit 1 | ✓ PASS |
| CR-01 floor, ratchet layer | `COMPARED_FLOOR` 122→100 | gate exit **0** (blind by construction); vitest RED `RATCHET: COMPARED_FLOOR equals what the REAL corpus compares` | ✓ PASS |
| DRIFT-05 (a) check | `npx tsx scripts/dump-sql-functions.ts --check` | agree, 1 ratcheted disagreement, exit 0 | ✓ PASS |
| DRIFT-05 (a) self-test | `… --self-test` | `PASS: 22 assertions`, exit 0 | ✓ PASS |
| (a) baseline-only direction FIRES | neuter that loop | `SELF-TEST FAIL: a baseline-only name must produce a drift row`, exit 1 | ✓ PASS |
| (a) snapshot-only direction FIRES | neuter that loop | `SELF-TEST FAIL: a snapshot-only name must produce a drift row`, exit 1 | ✓ PASS |
| (b) uncredentialed | `env -u … bash scripts/prod-body-drift-check.sh --baseline-live` | exit **1**, names `SUPABASE_PROJECT_REF`, `HARD FAILURE, not a skip`, no `::notice::` | ✓ PASS |
| (b) self-test | `… --self-test` | `8/8 arms`, exit 0 | ✓ PASS |
| (b) self-test FIRES | neuter `assert_credentials` | `6/8`, exit 1, both credential arms named | ✓ PASS |
| VAC-08 gate self-test | `bash scripts/test-ledger-drift-check.sh --self-test` | `5/5 arms`, exit 0 | ✓ PASS |
| disposition pin FIRES | append an undispositioned entry | 2 named failures, offending line printed | ✓ PASS |
| WR-A pin FIRES (was vacuous) | comment out the 3 lane fences | RED — `no longer restates … on a live (non-comment) line` | ✓ PASS |
| WR-B pin FIRES (was vacuous) | comment out the `paths:` entry | RED — `gate (b)'s SUBJECT re-runs gate (b)` | ✓ PASS |
| VAC-07 spec fails-not-skips with no lane | `npx vitest run --config vitest.local-stack.config.ts` | `Tests 2 failed (2)`, `LOCAL-STACK LANE ABSENT`, 0 skipped | ✓ PASS |
| lane boot on the CURRENT baseline | `run.sh up` | not run — Docker, out of hermetic remit | ? SKIP → human |
| VAC-07 GREEN on the CURRENT baseline | lane spec against a booted stack | not run — same reason | ? SKIP → human |
| VAC-08 credentialed `0 NEW drift` | credentialed CI job | not run — no credential; TEST/PROD forbidden here | ? SKIP → human |

**Databases touched: NONE.** Everything above is hermetic (`node`/`bash`/`tsx` self-tests, vitest, `git`). No `supabase` CLI command was issued, no DSN was opened.

---

### Regression Evidence (CI-shaped, per `ci.yml:292`)

| Run | Result |
|---|---|
| `npx vitest run --shard=1/2` | `1 failed | 422 passed | 11 skipped (434)` |
| `npx vitest run --shard=2/2` | `425 passed | 8 skipped (433)`, **exit 0** |
| reconciliation | 434 + 433 = **867** files — matches the count `d6d929cb` recorded, so sharding dropped nothing |
| the one failure | `ScenarioCommitDrawer.test.tsx > … focus moves to the error banner` — **49/49 GREEN in isolation** (re-run by me). Contention flake; the file is NOT in this phase's diff. Same instance `d6d929cb` already classified. |
| `src/__tests__/contracts/` | `5 files, 110 passed` — the globally-scanning specs a file-scoped run cannot clear |
| `gate-family-meta.test.ts` + `prod-prober-wiring.test.ts` | `45 passed`. **`deferred-items.md` D-1 (gate-family-meta RED) is CLOSED at HEAD** — the five bare conclusions were fixed in `b56fcefd`. D-18 green, confirming `d6d929cb`. |

---

### Requirements Coverage

| Requirement | Source | Status | Evidence |
|---|---|---|---|
| **VAC-07** | `REQUIREMENTS.md:163/207` | ✓ SATISFIED | Flipped to Complete only after the RED-then-GREEN observation; substrate corrected from "pg-lane" to the local-stack lane in the same edit. The `[VAC-07-DEFER]` fence was honoured, not bypassed. |
| **DRIFT-04** | `TODOS.md:6828` | ⏸ OPEN by design | Migration + pre-flight built, three-reviewer round done, pre-flight RUN against PROD. Apply is the founder's. |
| **DRIFT-05** | `TODOS.md:6829` | ✓ SATISFIED | Marked `[x]` with both gates named and their non-coverage of each other stated. Independently re-verified above. |
| **VAC08-LEDGER-32** | `TODOS.md:6933` | ✓ SATISFIED (disposition half) | 31/31 dispositioned + pinned. Entry deliberately stays OPEN for the apply, which the ROADMAP moves to Phase 164.8 TESTPREPROD. |
| **`[VAC-07-DEFER]`** | `TODOS.md:2231` | ✓ SATISFIED | Checkbox deliberately left unchecked with a stated reason (the entry's trailing ⭐ OWNER paragraph carries a separate obligation this plan does not own). The two now-false sentences are marked FALSE and kept as dated lineage rather than silently rewritten. |
| plan-local `CRIT-1…CRIT-7` | PLAN frontmatter | ✓ ACCOUNTED | Map 1:1 onto ROADMAP criteria 1–7. No orphans. |

**Orphaned requirements: NONE.** `grep "164.5" .planning/REQUIREMENTS.md` returns only VAC-07 and two summary lines; no requirement is mapped to this phase without a claiming plan.

---

### Code-Review Status — the stale `issues_found` claim, VERIFIED rather than accepted

`164.5-REVIEW.md` frontmatter still reads `status: issues_found` (iteration 2). I re-measured every finding at HEAD:

| Finding | Disposition | How I confirmed it |
|---|---|---|
| **WR-A** lane-config pin satisfied by a commented-out fence | ✓ CLOSED | Re-ran the exact mutation: now **RED** with `no longer restates … on a live (non-comment) line`. Was 7/7 GREEN before. |
| **WR-B** `paths:` pin satisfied by a commented-out entry | ✓ CLOSED | Re-ran the exact mutation: now **RED**. Was 405/405 GREEN before. |
| **WR-C** CR-01 floor caught only a TOTAL parse failure | ✓ CLOSED | `COMPARED_FLOOR = 122` at `:120`; proven in BOTH layers (table above). Note the honest asymmetry, matching the `CLAUDE.md` lesson: the runner catches floor-too-HIGH, the vitest ratchet catches floor-too-LOW. |
| **WR-D** dump-scope premise gap | ✓ DOCUMENTED, deliberately not "fixed" | `prod-body-drift-check.sh:234` records it inside the refusal block; `TODOS.md:1353` carries `[WR-D-DUMP-SCOPE]`. Correct call: answering it with the ratchet the refusal rejects would have been averaging a conflict. |
| **IN-A** `cmd_up` ignored `$2`+ | ✓ CLOSED at root cause | `run.sh:253` `if [ "$#" -gt 1 ]` runs ahead of anything that boots, and the dispatcher now forwards `"$@"`. The commit records that its own FIRST attempt could never fire and why (zsh does not word-split an unquoted var) — a correction I would otherwise have had to find. |
| **IN-B** `RECORDED_SHA_RE_ALL` a `.source` donor | ✓ CLOSED | `:305` now `md.match(RECORDED_SHA_RE_ALL)` |
| **IN-C** sibling spec bound to the FIRST sha row | ✓ CLOSED | `baseline-wiring-claim.test.ts:129-132` refuses ≥2 rows with a named message before comparing |

**Conclusion: the `issues_found` status field is stale. Zero iteration-2 findings are open.**

`deferred-items.md`: **D-1 is CLOSED** (measured green at HEAD, closed by `b56fcefd`). **D-2** (tsx absolute-path spawn in a worktree) and **D-3** (corpus arm timing out under full-suite contention) remain correctly classified as environmental/contention, not regressions — D-3's arm passed in both of my shard runs.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | `TBD` / `FIXME` / `XXX` in the phase's non-planning diff | — | **ZERO.** Scanned all 30 changed files with `awk` (not `grep`, per the NUL-byte blindness rule). The debt-marker gate passes. |
| — | — | `TODO` / `HACK` / `PLACEHOLDER` / `not yet implemented` | — | **ZERO** across the same set. |
| — | — | absolute `\/Users\/…` paths | — | **ZERO** in the entire `14b3b6c3..HEAD` diff, planning artifacts included. Public-repo rule honoured. |
| `CHANGELOG.md` | 12 | stale integer `10/10` (actual 12/12) | ℹ️ Info | Prose only. Per the repo stopping rule, TODOS material, never blocking. |

---

## ⛔ BLOCKER-SEVERITY FINDING (ship path, not the codebase)

**The PR branch is stale and would merge BOTH Critical review findings unfixed.**

`phase-164.5-baseline-snapshot-pr` — local and `origin/` — is at `ac68bed2` and contains **none** of `ab8c5623`, `4251c63c`, `f181a2b2`, `d6d929cb`.

Measured on that branch, not inferred:

```
git show phase-164.5-baseline-snapshot-pr:scripts/baseline-content-drift-check.mjs \
  | grep -c "compared === 0\|comparedFloor\|COMPARED_FLOOR"     → 0
git show phase-164.5-baseline-snapshot-pr:.github/workflows/ci.yml \
  | grep -c "prod-body-drift-check.sh --self-test"              → 0   (HEAD: 2)
```

So the tree that PR #757 currently proposes to merge carries:

- **CR-01 unfixed** — criterion 7's gate with no zero-comparison guard. It can report `findings 0` having compared nothing.
- **CR-02 unfixed** — criterion 4(b)'s `--self-test`, the only control that distinguishes a working gate (b) from a defanged one, invoked by nothing.
- the two vacuous pins (WR-A, WR-B) and the D-18 `prod-prober-wiring` regression.

This is not a code defect — the working branch at `d6d929cb` is correct and fully verified above — it is the ordering trap `CLAUDE.md` already documents, in its other direction: reviewers correctly ran on the working branch, and the derived `-pr` branch was never rebuilt afterwards.

**Remedy:** re-run `/gsd-pr-branch` from `d6d929cb`, keeping the DRIFT-04 exclusions, then re-run the deletion guard. The guard is currently clean and must stay clean: `git diff --diff-filter=D --name-only main..phase-164.5-baseline-snapshot-pr -- .planning/` returns **empty** today.

---

## Gaps Summary

**No gaps in the codebase.** Every in-scope criterion is met at `d6d929cb`, and every gate this phase added was proven able to FAIL by a mutation I applied and reverted myself — thirteen separate neuter-and-observe cycles, each restored from a `cp` byte backup and confirmed with `shasum -a 256`, with `git status --porcelain` empty at the end. `git checkout --` was used nowhere.

What is outstanding is four things a verifier structurally cannot close:

1. **The PR branch must be rebuilt before merge** (BLOCKER-severity, above). This is the one item that can turn a correct phase into a merged-vacuous one.
2. **Criterion 3 awaits founder authorisation.** Deliberate, evidenced, and now stronger than when the phase started — the pre-flight has RUN against PROD and exited 0.
3. **Criteria 1 and 6 should be re-observed on the regenerated baseline.** Their evidence is excellent but predates a 449-line change to the file they load.
4. **Criterion 5's `0 NEW drift` line needs a credentialed CI run.**

The phase's honesty is itself notable and worth recording: the SUMMARYs mark their own superseded sentences FALSE rather than editing them away; two of the fix commits open by naming their own earlier vacuous pins; `[VAC-07-DEFER]`'s checkbox is deliberately left unchecked with a stated reason; and the WR-D refusal was documented at the source rather than answered with the ratchet it rejects. None of that substitutes for measurement — but none of it was contradicted by measurement either.

---

_Verified: 2026-09-08T02:35:00Z_
_Verifier: Claude (gsd-verifier)_
_Working tree byte-identical to `d6d929cb` after every mutation. `git status --porcelain` empty. No database was contacted._
