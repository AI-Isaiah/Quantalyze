---
phase: 154-wizcont-stale-wizard-continuity-no-stale-screens
plan: 08
type: execute
wave: 3
depends_on: ["154-01", "154-04", "154-05", "154-06"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale.runtime.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale-refusal.runtime.test.tsx
  - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-VALIDATION.md
autonomous: true
requirements: [STALE-01]

must_haves:
  truths:
    - "T1, T1b, T2b, T3 are GREEN; T3b stays GREEN — the wizard never sits on 'Fetching trades…' unboundedly and never renders a refusal computed from a stale analytics row while a re-derive is in flight"
    - "The SF-1 stall backstop and the sync-progress in-flight datum are visible to SINGLE-KEY users — the isComposite gates at :2290 and :910 are gone as a class (TWIN-2 + TWIN-5)"
    - "Red renders only on a verdict that is CURRENT: mid-re-derive the screen is amber (role=status, warning tokens), never the red envelope (UI-SPEC semantic-color gate)"
    - "No timeout or threshold constant moved or added — the existing ladder is the only timing vocabulary"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      provides: "un-gated stall backstop, single-key sync-progress fetch, queued:false honesty, single-key R2-5 repoll guard, amber recomputing state"
      contains: "wizard-sync-interrupted"
  key_links:
    - from: "SyncPreviewStep.tsx single-key arm"
      to: "GET /api/strategies/[id]/sync-progress (widened in 154-04)"
      via: "the client fetch gate no longer conditioned on isComposite"
      pattern: "sync-progress"
    - from: "single-key terminal arm"
      to: "composite R2-5 guard :1092-1103"
      via: "the twin guard — empty series under a terminal status repolls"
      pattern: "repoll"
---

<objective>
Land the STALE-01 screen truths on the fix side: green every RED pin from 154-01 by making the
wizard's single-key arm honest — the backstop it computes but never shows, the in-flight datum it
never asks for, the queued flag it never reads, and the R2-5 guard its composite twin already has.
Then close the phase's falsifiability ledger.

⚠️ CONTINGENT design (GATE 1): read 154-INVESTIGATION.md "## Conclusion" first. This plan's
mechanism is what M1 supports — surface the existing affordances to the user class they were
hidden from, and stop treating a possibly-not-current reading as current. If the verdict demands a
NEW WizardErrorCode, it lands with KNOWN_FINALIZE_CODES + the EXPECTED_TABLE_SIZE copy-table pins
(75 → 76 at BOTH wizardErrors.test.ts sites) in the SAME commit — 153.x discipline; otherwise no
new code is minted.

⛔ No new timeout/threshold numbers anywhere. Existing ladder constants referenced, never moved.
⛔ All wizard error copy via buildEnvelope()/wizardErrors.ts only (grep-enforced).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (§ 5 — the three SyncPreviewStep sites with verbatim excerpts; Shared Patterns C/D/G)
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-UI-SPEC.md (§§ State Contracts 2 + 3, Copywriting — the screen truths, the amber block markup, the exact new copy)
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-VALIDATION.md

<interfaces>
From 154-04:
- useStrategySyncPoller ladder arm: zero-rows reads never fabricate "pending"; `missingRowGracePolls`
  now consumed in ladder mode (absent-row polls beyond the grace escalate via onError). The wizard
  may opt in by passing an EXISTING constant (e.g. MAX_CONSECUTIVE_POLL_ERRORS) — never a new number.
- GET /api/strategies/[id]/sync-progress for single-key: `{jobStatus: <status|null>, stalled: false, memberProgress: []}`;
  `jobStatus: null` MEANS zero compute_jobs rows (the branch-(d)/M4 tell); composite byte-identical.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-hide the exits — backstop gate, fetch gate, queued honesty (greens T1/T1b/T2b)</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale.runtime.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx (:775-782 kickoff arm; :830-856 stallBackstop + heavyFetchErrorsRef; :900-930 the isComposite fetch gate at :910; :2275-2323 the waiting render + status line; :2290-2291 the render gate; :2379-2408 the red prose + interrupted banner markup)
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale.runtime.test.tsx (154-01's RED pins — green them WITHOUT weakening their oracles)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-UI-SPEC.md (§ State Contract 2 — what each backend truth must render)
  </read_first>
  <action>
    Three gates, one class ("the in-flight datum exists and is owner-readable but never reaches
    the single-key screen"):
    1. Render gate (:2290-2291): delete the `isComposite &&` conjunct —
       `showInterruptedBanner = syncProgress?.stalled === true || stallBackstop` for every user
       class. The banner markup, testid `wizard-sync-interrupted`, role="status", and copy are
       UNCHANGED (UI-SPEC: no fourth banner, no re-copywriting).
    2. Client fetch gate (:910): remove `if (isComposite)` so single-key strategies also poll
       `/api/strategies/${strategyId}/sync-progress` (widened in 154-04). The fetch cadence and
       error handling stay exactly as the composite arm's today — no new interval, no new number.
    3. Kickoff honesty (:775-782): read `queued` from the kickoff body. When the response is OK
       but `queued === false` AND no draft-resume short-circuit applies, do NOT enter a
       waiting_for_complete that claims fresh work; consult the freshness probe/sync-progress
       datum the component already fetches — if nothing is in flight (`jobStatus: null`) and the
       analytics row is non-terminal/absent, surface the interrupted banner path instead of the
       eternal spinner. Use ONLY existing affordances (banner, existing retry contract, envelope
       via buildEnvelope) — invent no copy, no code, no threshold. Type the field additively on
       the kickoff response type if absent (check src/lib/process-key-onboard-contract.ts — the
       contract has a bidirectional pytest oracle; an ADDITIVE optional read must not change the
       accepted shape).
    Optionally (verdict-dependent): pass the poller's `missingRowGracePolls` from the wizard call
    site using an EXISTING constant so a persistent zero-rows read escalates to the existing
    SYNC_FAILED recoverable envelope. Record which constant and why in a comment.
    Re-examine A6 while in the file: `heavyFetchErrorsRef`'s "never needs a reset" invariant —
    Task 2 adds a non-throwing repoll path, exactly the case the invariant did not contemplate; if
    a stale ref could escalate a healthy run to SYNC_FAILED, reset it on the repoll path and pin
    it with a test case.
    Green T1, T1b, T2b WITHOUT touching their assertions (the oracles were reviewed at 154-01;
    changing them re-opens the gate). Coverage ratchet: every new branch in this 2448-line file
    ships with its test in the same commit (blocking frontend-coverage job, 82/80/74/72).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale.runtime.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - T1, T1b, T2b GREEN with their 154-01 oracles textually unchanged (git diff on the test file shows no assertion edits beyond what 154-01 committed — additions allowed, modifications not)
    - `grep -n "isComposite" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"` shows neither the :2290 render gate nor the :910 fetch gate keyed on it any longer (remaining isComposite uses are copy/memberProgress rendering only)
    - `grep -c "queued" <SyncPreviewStep.tsx>` >= 1 in the kickoff arm
    - No new numeric literal in production diff except an existing-constant reference (review the diff; SLOW_HINT/WARN/RETRY/backoff untouched)
    - readfailure + poll-disjointness runtime suites still green: `npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps" --no-file-parallelism`
  </acceptance_criteria>
  <done>The single-key user class has every exit the composite class had; the client no longer infers in-flight work from silence</done>
</task>

<task type="auto">
  <name>Task 2: The single-key R2-5 twin + amber recomputing state (greens T3, keeps T3b)</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale-refusal.runtime.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx (:1092-1103 the composite R2-5 guard — the donor; :1398-1447 the single-key arm incl. the null-count throws and the checkStrategyGate call; :1108-1116 why series_completeness survives stale)
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale-refusal.runtime.test.tsx (T3/T3b pins)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-UI-SPEC.md (§ State Contract 3 — the amber block: role=status, border-warning/40 bg-warning/5 px-4 py-3, the two NEW copy strings verbatim, Numbers Contract)
  </read_first>
  <action>
    Single-key R2-5 twin (TWIN-1): in the single-key terminal arm (~:1398-1447), clone the
    composite guard's READING, not just its line: a terminal status with ZERO series rows is
    NOT-yet-terminal while a re-derive may be in flight (`run_derive_broker_dailies_job` performs
    the same wholesale delete→re-upsert on the strategy-mode path — job_worker.py:2539-2560).
    PATTERNS § 5 pins the asymmetry precisely: the arm already distinguishes null count (throw)
    from zero count (a real measurement); the fix is a THIRD state — a real zero that may be
    mid-re-derive — expressed as `return "repoll"` before checkStrategyGate can refuse. It is not
    a fourth `??`. The existing null-count throws stay exactly as they are.
    Bound the honest amber, not the loop: while in this repoll state AND the sync-progress datum
    shows work in flight (jobStatus non-terminal), render the UI-SPEC state-3 amber block — an
    inline branch (NOT a new component file), cloning the wizard-sync-interrupted banner shape:
    role="status", `rounded-md border border-warning/40 bg-warning/5 px-4 py-3`; heading
    "Recomputing this strategy's analytics" (`text-body font-medium text-text-primary` — 500
    inherited from the slot, per the UI-SPEC weight clause); body "The previous result is out of
    date and is being recalculated. This screen updates when it finishes — your draft is saved."
    (`text-caption text-text-secondary`). ⛔ Never the red envelope, never "failed" copy, while
    the re-derive is in flight — red only on a verdict that is CURRENT. If NO work is in flight
    (jobStatus null) the repoll state falls back to the interrupted-banner path from Task 1 —
    the screen never claims recomputation it has no evidence for.
    Numbers Contract: any count the screen cannot currently know renders `—` (em-dash), never 0,
    never a stale number; a `—` cell carries no semantic color.
    A11y: amber block role="status"; terminal failures keep role="alert" via ErrorEnvelope.
    Green T3; T3b (composite symmetry) must remain green UNTOUCHED — run it explicitly. Extend the
    stale-refusal file with: the amber-state render case (role=status + hand-typed heading needle,
    never imported copy), the no-red-while-in-flight case, and the `—` rendering case (VALIDATION
    rows 7-8 of plan 02's block).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale-refusal.runtime.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - T3 GREEN, T3b GREEN with unchanged assertions; the amber, no-red, and em-dash cases all pass with hand-typed needles
    - The single-key guard is a repoll return placed BEFORE checkStrategyGate (source assertion); the null-count throws unchanged
    - `grep -c "Recomputing this strategy's analytics" <SyncPreviewStep.tsx>` = 1 and the string does NOT appear in wizardErrors.ts (it is a status line, not an error envelope)
    - No new component file created (git status shows no new .tsx outside tests)
  </acceptance_criteria>
  <done>Instance (b) is closed as a class twin, and the screen's colors tell the truth about verdict currency</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suites + falsifiability ledger closure</name>
  <files>.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-VALIDATION.md</files>
  <read_first>
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-VALIDATION.md (the Falsifiability Ledger + Oracle Independence + Sign-Off sections)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-05-SUMMARY.md (SC-1 mutation evidence to transcribe)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-06-SUMMARY.md (SC-3 mutation evidence to transcribe)
  </read_first>
  <action>
    Run the SC-2 mutations NOW (each: apply to production source, run the named test, observe RED,
    paste the failing assertion, revert, observe GREEN):
    - SC-2a (stall): re-add the `isComposite &&` gate at the former :2290-2291 site → T1b RED.
    - SC-2a (zero-rows): restore `statusRow?.computation_status ?? "pending"` in the poller → T2 RED.
    - SC-2b (stale refusal): remove the single-key repoll guard → T3 RED.
    - SC-2b (2nd member): remove the COMPOSITE arm's R2-5 guard (:1092-1103 region) → T3b RED —
      the second-member mutation is the one that detects instance-fixes wearing class-fix clothes.
    Then close 154-VALIDATION.md (Edit, never full-file Write; wc -l sanity):
    - Transcribe SC-1 rows from 154-05's SUMMARY and SC-3 rows from 154-06's SUMMARY (pasted
      failing assertions, marked Observed ✅; any skipped row marked skipped-with-reason, never
      "caught").
    - Fill the SC-2 rows from this task's runs.
    - Check every Oracle Independence box (verify by grep: no RETRY_THRESHOLD_MS/WARN_THRESHOLD_MS
      import in T1 files; no isComputedAnalytics in test expectations; the zero-rows double shape).
    - Confirm the T1/T2/T3 RED-at-pre-fix-commit evidence from 154-01 is present in the ledger
      (the CONTEXT/ROADMAP gate).
    - Complete the Validation Sign-Off checklist and set `nyquist_compliant: true` in frontmatter.
    Phase-gate runs (paste tails into SUMMARY):
    - `npm test` (full vitest) green; coverage thresholds 82/80/74/72 clear (`npm run test:coverage` if the full run lacks it locally)
    - `npx tsc --noEmit` and `npm run lint` clean
    - If 154-07 touched Python: `cd analytics-service && pytest -q && mypy --strict .`
    - `grep -rn MUTANT src/` → 0
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" ".planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-VALIDATION.md" && grep -c "Observed" ".planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-VALIDATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - All four SC-2 mutations observed RED with pasted assertions, all reverted (working tree clean of mutations)
    - Every Falsifiability Ledger row is Observed ✅ (with evidence) or skipped-with-reason — zero ⬜ pending rows remain
    - Full vitest green with coverage thresholds cleared; tsc + lint clean
    - nyquist_compliant: true set; the Sign-Off checklist fully checked
  </acceptance_criteria>
  <done>The phase's own proof is complete — no pending ledger rows, the exact failure mode Phase 142's close-out was caught on</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync-progress response → single-key render | in-flight evidence now drives user-visible claims |
| stale analytics row → gate verdict | the refusal must not be computed from a row mid-replacement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-08-A | DoS (self-inflicted: infinite spinner / unbounded repoll) | single-key repoll state | mitigate | repoll is bounded by evidence: in-flight → amber; no evidence → interrupted banner with the existing retry contract; grace escalation via existing SYNC_FAILED |
| T-154-08-B | Information Disclosure (stale number presented as fresh) | counts mid-re-derive | mitigate | Numbers Contract — unknowable counts render em-dash, no semantic color |
| T-154-08-C | Spoofing (screen claims a state the backend has left) | status line / colors | mitigate | red only on current verdicts; "Fetching trades..." only while the claim is true per the in-flight datum |
| T-154-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- All 154-01 pins green (T1/T1b/T2/T2b/T3/T3b), full vitest + coverage + lint + tsc green
- SC-2 mutation quartet observed RED and reverted; ledger fully closed; nyquist_compliant: true
</verification>

<success_criteria>
A wizard screen never shows a state the backend has already left: the stuck "Fetching trades…" has
honest, visible exits for every user class, and the stale-row refusal is structurally impossible
while a re-derive is in flight — proven by tests that were RED at the pre-fix commit and by
mutations that redden them again.
</success_criteria>

<output>
Create `.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-08-SUMMARY.md` when done
</output>
