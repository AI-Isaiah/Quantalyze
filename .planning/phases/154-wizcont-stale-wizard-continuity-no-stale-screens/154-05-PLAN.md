---
phase: 154-wizcont-stale-wizard-continuity-no-stale-screens
plan: 05
type: execute
wave: 2
depends_on: ["154-02"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/page.tsx
  - src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx
  - src/app/(dashboard)/allocations/components/ContributionWizardOverlay.test.tsx
  - src/app/(dashboard)/my-strategies/MyStrategiesEmptyState.test.tsx
  - src/app/(dashboard)/my-strategies/MyStrategiesSection.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.addalloc.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - e2e/wizard-resume.spec.ts
  - e2e/helpers/seed-test-project.ts
autonomous: true
requirements: [WIZCONT-01]

must_haves:
  truths:
    - "Opening the overlay (+ Strategy / My Strategies empty state) with an existing draft renders the resume banner and Resume ACTUALLY lands on the draft's step — sync_preview renders, not merely a prop passed (Pitfall 2)"
    - "Resume is explicit, never silent: the founder always chooses Resume vs Start fresh; Start fresh opens the confirm-delete dialog and never deletes directly (TRAP-4)"
    - "A CSV draft resumes on both paths — the WizardClient.tsx:198 short-circuit no longer runs before initialDraft is consulted (the class, not the overlay instance)"
    - "An API draft is never hijacked onto the CSV branch: a draft is offered only on the branch matching its kind"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx"
      provides: "fetch-on-open above the null gate, deferred WizardClient mount (undefined/null/row), close-reset, kind-following default tab"
    - path: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      provides: "draft-kind-aware step initializer (the CSV short-circuit fix)"
    - path: "e2e/wizard-resume.spec.ts"
      provides: "browser proof: seeded draft → overlay → banner → Resume → sync_preview, own-seed invariant"
  key_links:
    - from: "ContributionWizardOverlay.tsx"
      to: "/api/strategies/wizard-draft"
      via: "fetch on open"
      pattern: "api/strategies/wizard-draft"
    - from: "WizardClient.tsx step initializer"
      to: "draft-query.ts draftMatchesSource/deriveDraftKind"
      via: "kind decides the resume step before source decides the fresh step"
      pattern: "draft_kind|WizardDraftKind"
---

<objective>
Wire WIZCONT-01: the overlay supplies a real `initialDraft` (the Phase-110 deferral at :146), and
the WizardClient step initializer consults the draft before the CSV short-circuit — closing the
class "the draft is not consulted before the step is chosen" on BOTH entry paths and BOTH branches.

⛔ Do NOT rebuild the resume state machine — WizardClient already resumes (:198-201), already owns
the banner (:899-925), handleResume, and the TRAP-4-safe handleStartFresh. The overlay supplies
the draft; the initializer orders its checks correctly. Nothing more.

Purpose: CONTEXT.md locked decisions (entry point = ContributionWizardOverlay.tsx:146; explicit
choice via showResumeBanner; CSV short-circuit in scope as the same class).
Output: resume works from + Strategy / empty state; CSV drafts resume; e2e browser proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-CONTEXT.md
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (§§ 1, 13, Shared Patterns E/F — overlay discipline, e2e donor, hydration rule, banner markup)
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-UI-SPEC.md (§ State Contract 1 — the resume affordance contract, byte-identical reuse)

<interfaces>
From 154-02 (src/lib/wizard/draft-query.ts):
- readLatestWizardDraft(supabase, userId) → { draft: InitialDraft | null, kind: WizardDraftKind | null }
- type WizardDraftKind = "api" | "csv" | "composite"
- draftMatchesSource(kind, source): kind === "csv" ? source === "csv" : source !== "csv"
From 154-02 (GET /api/strategies/wizard-draft): 200 { draft: InitialDraft | null, kind: WizardDraftKind | null }, NO_STORE_HEADERS.

Resolution of Pitfall W-1 (PLANNER DECISION — do not re-decide): option (a) DEFER THE MOUNT.
The overlay does not render WizardClient until the draft read settles (undefined = loading,
null = none, row = draft). A late prop into a mounted WizardClient does nothing — its useState
initializers read initialDraft ONCE (:198, :204, :207, :228); deferring the mount means it mounts
once with the real value. The key is additionally extended to `${source}:${draft?.id ?? "new"}`
so Start-fresh/delete transitions force a clean remount. Option (b) alone was rejected: a
late-arriving keyed remount can discard a half-typed key — CONTEXT's stated reason resume is
explicit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft-kind-aware step initializer (the CSV short-circuit fix) + branch matching</name>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx, src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx, src/app/(dashboard)/strategies/new/wizard/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx (:178-266 the initializer block + hydration docblock; :803 the banner-raise effect; :815-846 TRAP-4 handleStartFresh; :899-925 banner markup incl. the CSV body copy)
    - src/lib/wizard/draft-query.ts (the kind/matching contracts from 154-02)
    - src/app/(dashboard)/strategies/new/wizard/page.tsx (as refactored by 154-02)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (Shared Pattern E — hydration discipline)
  </read_first>
  <action>
    WizardClient.tsx:
    - Thread the draft kind: `initialDraft` already flows in; add the kind alongside (either extend
      the InitialDraft object passed down to carry `draft_kind: WizardDraftKind`, or a sibling
      `initialDraftKind` prop — pick whichever keeps the existing prop contract additive; the
      overlay and page both have the kind from the helper/route).
    - Reorder the step initializer (:198-201) to consult the draft FIRST. Exact truth table:
      * initialDraft present AND kind === "csv" → "csv_upload" (resume: re-select file, metadata
        preserved — the UI-SPEC CSV banner body)
      * initialDraft present AND kind !== "csv" → "sync_preview" (api AND composite drafts — a
        composite draft must NOT be routed to the CSV step; this is open item A4, resolved by the
        kind discriminator)
      * no draft AND source === "csv" → "csv_upload" (unchanged)
      * no draft → first step (unchanged)
    - Hydration discipline holds: the initializer reads only props (`source`, `initialDraft`,
      the kind) — all SSR-deterministic on the page path, all settled-before-mount on the overlay
      path. ⛔ No localStorage, no fetch in any initializer (React #418).
    - TRAP-4 standing invariant untouched: handleStartFresh opens the confirm dialog and does
      nothing else — do not modify it; the test below re-pins it.
    WizardClient.test.tsx — extend (tdd-flavored: write these RED against the unmodified
    initializer first where practical):
    - CSV draft resumes: source="csv" + a CSV-kind initialDraft → banner renders
      (wizard-resume + wizard-start-fresh testids) and the step is csv_upload with the draft's
      strategyId bound (SC-1 second-member mutation target).
    - API draft not hijacked: source="csv" + NO draft → csv_upload fresh (unchanged); and a
      composite-kind draft → sync_preview, never csv_upload.
    - TRAP-4: Start fresh click opens the confirm dialog; no delete call fired (spy).
    page.tsx:
    - Apply `draftMatchesSource` from the helper: pass initialDraft (and its kind) only when the
      draft's kind matches the effective source branch; otherwise pass null. One rule, imported —
      not re-derived inline.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'source === "csv"' "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"` shows the source check AFTER the initialDraft consultation in the step initializer
    - CSV-draft resume test green; composite-draft → sync_preview test green; TRAP-4 pin green
    - page.tsx imports draftMatchesSource from @/lib/wizard/draft-query (grep)
    - Full wizard-directory suite green: `npx vitest run "src/app/(dashboard)/strategies/new/wizard" --no-file-parallelism`
  </acceptance_criteria>
  <done>The step is chosen from the draft first on every path; CSV and composite drafts route correctly; TRAP-4 intact</done>
</task>

<task type="auto">
  <name>Task 2: Overlay fetch-on-open with deferred mount</name>
  <files>src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx, src/app/(dashboard)/allocations/components/ContributionWizardOverlay.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx (FULL file — hooks-above-null-gate + close-reset :45-80; the defect site + comment :135-149)
    - src/app/(dashboard)/allocations/components/ContributionWizardOverlay.test.tsx (:21-59 the wholesale WizardClient mock — the Pitfall-2-shaped assertion this task replaces for the resume case)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (§ 1 — verbatim excerpts of both blocks)
  </read_first>
  <action>
    ContributionWizardOverlay.tsx:
    - Add draft state `useState<{draft, kind} | null | undefined>(undefined)` (undefined = not yet
      read — an SSR-safe initial value; the overlay is client-only but the rule is uniform).
    - Fetch effect ABOVE the `if (!isOpen) return null` gate (hooks-above-null-gate discipline,
      same as the Esc effect): on isOpen becoming true, `fetch("/api/strategies/wizard-draft")`;
      on settle, store `{draft, kind}` or null; non-OK/network error → treat as null (no draft) and
      console.warn — the overlay must never block a fresh start on a failed read. On close, reset
      draft state to undefined alongside the existing `setSource("api")` close-reset (⭐ a draft
      fetched on open must be cleared on close, or a reopen resumes a deleted draft).
    - When a draft arrives and `kind` is "csv", set the source tab to "csv" (the overlay opens on
      the draft's branch so the resume banner is visible); otherwise leave/set "api".
    - Defer the mount: while draft state is undefined AND isOpen, render the overlay chrome with
      an inline minimal pending body (reuse existing muted-text conventions — no new component
      file, no spinner invention; a single `text-caption text-text-muted` line). Mount
      WizardClient only once settled, passing `initialDraft`/kind when `draftMatchesSource(kind, source)`
      holds, else null (a tab switch offers a fresh flow — never a hijack).
    - Extend the key: `key={`${source}:${draft?.id ?? "new"}`}` (documented precedent :45-47).
    - Replace the defect comment at :135-149 — the new comment states the read seam, the deferred
      mount, and the matching rule; the Phase-110 deferral note goes away with the null.
    ContributionWizardOverlay.test.tsx:
    - KEEP the mocked-WizardClient cases for the portal/Esc/keying/close-reset contract.
    - ADD the resume case rendering the REAL WizardClient (unmock via importActual or a second
      describe without the mock): overlay open + route fetch mocked (vi.spyOn on fetch, resolving
      the 154-02 response shape) with an API-kind draft → the REAL banner testids
      (wizard-resume / wizard-start-fresh) render, and clicking Resume renders the sync_preview
      step. ⚠️ The existing `wizard-initial-draft` null/present testid assertion is EXACTLY
      RESEARCH Pitfall 2's warning shape — the resume case must not use it. Assert rendered
      behavior, not the prop.
    - Fetch-fail case: route 500 → WizardClient mounts fresh (no banner, no crash).
    - vi.spyOn + restoreAllMocks; ⛔ no vi.stubGlobal.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "initialDraft={null}" "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx"` returns 0
    - The fetch effect sits above the `if (!isOpen) return null` line (source assertion)
    - Resume test renders the REAL WizardClient and asserts wizard-resume testid + sync_preview render after click — not a prop-echo testid
    - Close-reset test: close then reopen re-fetches (draft state cleared on close)
    - Fetch-failure test: overlay still offers a fresh wizard (never blocks on the read)
  </acceptance_criteria>
  <done>The overlay supplies a real draft through a mount that can actually consume it; the test can fail if resume breaks</done>
</task>

<task type="auto">
  <name>Task 3: Consumer-test ripple + e2e browser proof + SC-1 mutations</name>
  <files>src/app/(dashboard)/my-strategies/MyStrategiesEmptyState.test.tsx, src/app/(dashboard)/my-strategies/MyStrategiesSection.test.tsx, src/app/(dashboard)/allocations/AllocationsTabs.addalloc.test.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx, e2e/wizard-resume.spec.ts, e2e/helpers/seed-test-project.ts</files>
  <read_first>
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (Twin Register consumer-ripple correction — the four REAL renderers; ScenarioComposer mock setup at ScenarioComposer.test.tsx:125-126 and :655; § 13 e2e donor)
    - e2e/composite-onboarding.spec.ts (:405-500 — seeded-draft resume donor: HAS_SEED_ENV skip, afterAll prefix GC, route SPIES not stubs, loginViaForm)
    - e2e/helpers/seed-test-project.ts (seedTestAllocator :76, seedStrategyWithHistory :380, cleanupStrategiesByNamePrefix :345 — adapt for a wizard draft seed)
  </read_first>
  <action>
    Consumer ripple (the overlay's public props did NOT change — the fetch is internal — so
    consumer SOURCE files are untouched; only test setup ripples): in each of the four renderer
    test files (MyStrategiesEmptyState, MyStrategiesSection, AllocationsTabs.addalloc,
    ScenarioComposer — ⚠️ ScenarioComposer is the one RESEARCH's list missed;
    StrategyBrowseDrawer is comment-only and gets NOTHING), add the wizard-draft fetch mock to the
    existing setup so overlay-opening cases resolve `{draft: null, kind: null}` deterministically.
    If a file's existing mocks already swallow fetch, verify and leave it — record which.

    e2e/wizard-resume.spec.ts (NEW, donor: composite-onboarding :405-500):
    - `test.skip(!HAS_SEED_ENV, ...)`; afterAll `cleanupStrategiesByNamePrefix("e2e-wizcont-")`.
    - Seed: seedTestAllocator + a wizard API draft owned by that user — add a
      `seedWizardDraft({ownerUserId, namePrefix: "e2e-wizcont-"})` helper to seed-test-project.ts
      (source='wizard', status='draft', an api_key row per existing helper conventions).
    - Drive: loginViaForm → the page hosting the overlay (allocations or my-strategies) → click
      the `+ Strategy` affordance → overlay opens → assert the resume banner
      (`wizard-resume` visible) → click Resume → assert the sync_preview step surface renders.
    - Route SPY (not stub) on `**/api/strategies/wizard-draft` counting calls >= 1 — proof the
      overlay actually asks.
    - ⚠️ Shared TEST DB: assert THIS spec's seeded draft id/name resumes (the banner context or a
      subsequent render carrying the seeded name) — NEVER a global "no draft exists" or row count.
    SC-1 falsifiability mutations (run now, record in SUMMARY — 154-08 transcribes to VALIDATION.md):
    - Mutation 1: revert overlay to literal `initialDraft={null}` → overlay resume test RED. Restore.
    - Mutation 2 (second member): restore `if (source === "csv") return "csv_upload";` ABOVE the
      draft consultation → CSV-draft resume test RED. Restore.
    - Mutation 3 (TRAP-4): make Start fresh call the delete directly → confirm-dialog test RED. Restore.
    Paste each failing assertion into the SUMMARY; `grep -rn MUTANT src/` → 0 afterwards.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/my-strategies" "src/app/(dashboard)/allocations" --no-file-parallelism && npx playwright test e2e/wizard-resume.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - All four consumer test files green with the new mock setup; StrategyBrowseDrawer.test.tsx untouched
    - e2e spec exists, lists cleanly, and (when HAS_SEED_ENV) asserts the OWN seeded draft resumes with a route spy count, never global empty-state
    - Three SC-1 mutation RED outputs pasted in SUMMARY, all restored, zero MUTANT markers
    - Full targeted suite green including WizardClient + overlay files
  </acceptance_criteria>
  <done>Resume proven at component, consumer, and browser levels; the falsifiability record exists for the ledger</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| overlay (client) → wizard-draft route | authenticated fetch of the caller's own draft |
| draft row → rendered banner | draft fields render in the founder's own session only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-05-A | Information Disclosure | overlay draft fetch | mitigate | route is id-less + RLS-bounded (154-02); overlay renders only the caller's own draft fields |
| T-154-05-B | Tampering (destructive one-click delete) | Start fresh | mitigate | TRAP-4 pin re-asserted: confirm dialog only, spy proves no direct delete |
| T-154-05-C | DoS (overlay blocked by failed read) | fetch-fail arm | mitigate | non-OK read degrades to fresh wizard, console.warn observable |
| T-154-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)" --no-file-parallelism` green; tsc clean
- e2e spec runs green under seed env (CI) — asserts own-seed invariant
- SC-1 mutation trio observed RED and restored
</verification>

<success_criteria>
Re-entering "add a strategy" with an existing draft continues where the founder left off, by
explicit choice, from every real entry point, for API and CSV drafts alike — and the tests that
say so have been shown able to fail.
</success_criteria>

<output>
Create `.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-05-SUMMARY.md` when done
</output>
