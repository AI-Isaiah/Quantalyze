---
phase: 154-wizcont-stale-wizard-continuity-no-stale-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useStrategySyncPoller.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale.runtime.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale-refusal.runtime.test.tsx
  - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md
  - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-VALIDATION.md
autonomous: false
requirements: [STALE-01]

must_haves:
  truths:
    - "The supplier mechanism (M2i / M2ii / M3 / M4) that put the founder's screen in pending/absent on 2026-08-04 is discriminated by PROD evidence, not asserted from code"
    - "T1, T1b, T2, T2b, T3 fail RED at HEAD with output pasted into the ledger; T3b passes GREEN at HEAD (the composite arm already guards)"
    - "The 'do (a) and (b) share one cause' question is answered explicitly in writing: one root idea, two distinct code sites, a single-site fix discharges neither alone"
  artifacts:
    - path: "src/hooks/useStrategySyncPoller.test.ts"
      provides: "T2 — the null→pending coercion pinned at hook level (no test file exists for this hook today)"
      min_lines: 80
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale.runtime.test.tsx"
      provides: "T1 (unbounded non-terminal), T1b (isComposite gate on the SF-1 backstop), T2b (queued:false ignored)"
      min_lines: 150
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale-refusal.runtime.test.tsx"
      provides: "T3 (single-key stale refusal) + T3b (composite symmetry, passes today)"
      min_lines: 120
    - path: ".planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md"
      provides: "Q0/Q1/Q2 PROD results, discriminator verdict, mechanism conclusion, shared-cause answer"
  key_links:
    - from: "154-INVESTIGATION.md verdict"
      to: "154-04 / 154-07 / 154-08 fix design"
      via: "depends_on gate — no STALE fix executes before this plan completes"
      pattern: "Mechanism verdict:"
---

<objective>
Close the STALE-01 investigation gate: discriminate which supplier mechanism fired on PROD on
2026-08-04 (read-only queries), and land the three failing regression tests that reproduce the
mechanism at HEAD. "Root cause established" closes ONLY on the failing tests plus the PROD
discriminator — a written trace alone is the self-referential-oracle shape this project has been
burned by.

⛔ THIS PLAN CONTAINS NO FIX. No production source file is modified. No timeout or threshold
number is invented anywhere — the existing ladder (SLOW_HINT_MS 15s / WARN_THRESHOLD_MS 60s /
RETRY_THRESHOLD_MS 900s / MAX_CONSECUTIVE_POLL_ERRORS 3) is referenced, never moved.

Purpose: ROADMAP criterion 2 and REQUIREMENTS say verbatim "Do not plan a fix before answering
it." Plans 154-04, 154-07 and 154-08 depend_on this plan and their fix design is contingent on
this plan's finding.
Output: three RED test files (committed failing), 154-INVESTIGATION.md with the PROD verdict,
154-VALIDATION.md rows flipped to red-observed with pasted output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-CONTEXT.md
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-RESEARCH.md
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-VALIDATION.md

RESEARCH.md § STALE-01 Steps 0-9 is the investigation's map: M1 (leading, pure code), M2 (null
coerced to "pending" — absent row OR RLS zero-rows), M3 (parent parked at done_pending_children),
M4 (200-but-nothing-queued), M5/M6/M7 ruled out. PATTERNS.md §§ 10-11 are the test donors.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run the Q0/Q1/Q2 read-only PROD discriminator queries</name>
  <files>.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md</files>
  <read_first>
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-RESEARCH.md (§ STALE-01 Step 6 — Q1/Q2 verbatim + the discriminator table)
  </read_first>
  <action>
    Supabase MCP tools are stripped from subagent executors (upstream anthropics/claude-code#13898),
    so this task is executed by the ORCHESTRATOR session (which has the Supabase MCP) or the
    operator. ⛔ READ-ONLY. No writes, no migrations, PROD project khslejtfbuezsmvmtsdn,
    execute_sql only.

    1. Q0 — find the MT5 strategy ids:
       SELECT s.id, s.name, s.status, s.source, s.created_at
         FROM strategies s JOIN api_keys k ON k.id = s.api_key_id
        WHERE k.exchange = 'mt5';
    2. Q1 and Q2 — run VERBATIM from 154-RESEARCH.md § STALE-01 Step 6, substituting the Q0 ids
       into the `IN (<the three MT5 strategy ids>)` clauses.
    3. Optionally corroborate via Railway logs (greps: process_key_long.enqueued_tail,
       process_key_long.enqueue_tail_failed, process_key.queued, "series heal-delete failed") —
       Q2 alone discriminates M3 without logs.
    4. Create 154-INVESTIGATION.md in the phase directory with: the raw Q0/Q1/Q2 result rows, the
       matched row of the RESEARCH Step-6 discriminator table, and a one-line
       `Mechanism verdict: <M2i | M2ii | M3 | M4 | combination>` plus whether the founder's quote
       was the strict ("Fetching trades...") or loose ("computing") reading (RESEARCH A3).
  </action>
  <verify>
    <automated>test -f ".planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md" && grep -c "Mechanism verdict:" ".planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - 154-INVESTIGATION.md exists and contains the literal string "Mechanism verdict:" followed by one or more of M2i/M2ii/M3/M4
    - Raw Q1 result includes analytics_row_exists, computation_status and computed_at for every Q0 strategy id
    - Raw Q2 result includes kind, status, created_at per compute_jobs row (or an explicit "zero rows" note per strategy)
    - No INSERT/UPDATE/DELETE/DDL was issued (queries are SELECT-only, copy-pasted)
  </acceptance_criteria>
  <resume-signal>Paste the Q0/Q1/Q2 results (or confirm 154-INVESTIGATION.md written) and type "verdict recorded"</resume-signal>
  <done>154-INVESTIGATION.md carries the PROD evidence and a named mechanism verdict</done>
</task>

<task type="auto">
  <name>Task 2: Land T2 (hook) and T1/T1b/T2b (component) RED</name>
  <files>src/hooks/useStrategySyncPoller.test.ts, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale.runtime.test.tsx</files>
  <read_first>
    - src/hooks/useStrategySyncPoller.ts (full file — the ladder arm :226-249 vs the interval arm :149-159 is the subject)
    - src/components/notes/useNoteAutoSave.test.ts (Donor A: renderHook + fake timers + async advance — ⛔ do NOT copy its vi.stubGlobal at :31)
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.readfailure.runtime.test.tsx (Donor B: chainable supabase double :146-199, module mocks :197-207, kickoff spy + baseProps :282-299, three-stage settle ladder :310-328, analytics dispatch keyed on selected columns :229-247, teardown :342-353, hand-typed needles :80-107, vacuity fence :511-514)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (§§ 10-11 — the composite-donor recipe and the oracle disciplines)
  </read_first>
  <action>
    Create the two Wave-0 test files. All of these tests are EXPECTED TO FAIL at HEAD — that is
    the deliverable. Commit them failing; plans 154-04 and 154-08 green them. Do NOT mark them
    `.fails`/`.skip` — a skipped repro is not a gate.

    `src/hooks/useStrategySyncPoller.test.ts` (NEW — no test exists for this hook):
    - Compose Donor A (renderHook + `vi.useFakeTimers()` + `await act(async () => { await vi.advanceTimersByTimeAsync(...) })`)
      with Donor B's chainable double for `@/lib/supabase/client`. The double must support BOTH
      await forms: `.maybeSingle()` (ladder arm) AND `.single()` (interval arm) — extend Donor B's
      `chain()` with a `single` method.
    - T2: drive LADDER mode (`schedule: [3000, 3000, 5000]` hand-typed literal array, NOT an
      import of POLL_BACKOFF_MS) with a read that always resolves the PostgREST zero-rows shape
      `{ data: null, error: null }` — exactly that shape, never a hand-invented `{data: undefined}`.
      Assert `onStatus` is NEVER called with the fabricated `"pending"`. At HEAD :228-229 fabricates
      it → RED.
    - Symmetry case (own `it()`, never a loop): drive INTERVAL mode through the same
      `{ data: null, error: null }` read and assert `onStatus` is not called — this arm passes
      today (:151-159) and makes the divergence the test's subject.
    - Terminal-set assertions (if any) pin the hand-typed literal set
      `["failed", "complete", "complete_with_warnings"]` with a comment citing the DB CHECK
      migration 20260602120000 as the independent authority — ⛔ never import `isComputedAnalytics`
      or anything from `closed-sets.ts` for an expected value.
    - Mocking discipline: `vi.spyOn` + `vi.restoreAllMocks()` in afterEach; `vi.clearAllTimers()`
      + `vi.useRealTimers()`; module-level client factory reset in afterEach (Donor B :342-353).
      ⛔ No `vi.stubGlobal` anywhere (DEF-16-1 CI-only Node 22 failure class).

    `SyncPreviewStep.stale.runtime.test.tsx` (NEW):
    - Copy Donor B's harness verbatim: the three module mocks, `installFetchMock` (kickoff resolves
      `{ ok: true, composite: false }`), baseProps, the three-stage settle ladder, the
      selected-columns dispatch for `strategy_analytics`.
    - T1: every status read resolves `{ data: { computation_status: "pending", computation_error: null }, error: null }`.
      Advance fake time by the hand-typed literal `1_000_000` ms (≈16.7 min — past the existing
      900 s patience window; ⛔ do NOT import RETRY_THRESHOLD_MS/WARN_THRESHOLD_MS from the module
      under test). Assert the rendered text no longer contains the hand-typed needle
      `"Fetching trades..."` OR an honest affordance is present. At HEAD it still renders the
      needle → RED.
    - T1b (the mechanism pin, not the symptom): same drive; assert
      `queryByTestId("wizard-sync-interrupted")` is non-null. At HEAD the `isComposite &&` gate at
      :2290-2291 keeps it null → RED.
    - T2b: kickoff fetch resolves 200 with body `{ ok: true, composite: false, queued: false }`;
      assert the component does NOT sit in the waiting spinner claiming work is in flight (needle:
      the container text does not equal the eternal "Fetching trades..." render after the settle
      ladder) — at HEAD the kickoff arm (:775-782) never inspects `queued` → RED.
    - Every negative assertion gets its positive counterpart + the vacuity fence
      `expect(text.length).toBeGreaterThan(0)` (Donor B :511-514) — a component that renders
      nothing must not pass.
    - Name every `it()` with its T-label as a literal prefix — `it("T1: ...")`, `it("T1b: ...")`,
      `it("T2: ...")`, `it("T2b: ...")`; the interval-arm symmetry case is `it("SYM-interval: ...")`.
      The RED-gate verify greps the failure list for these exact labels — an unlabeled test
      disarms the gate.
    - Run: `npx vitest run src/hooks/useStrategySyncPoller.test.ts "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale.runtime.test.tsx" --no-file-parallelism`
      and CAPTURE the failing output. Paste the failing assertion lines for T1, T1b, T2, T2b into
      154-INVESTIGATION.md under a "## RED evidence" heading.
  </action>
  <verify>
    <automated>OUT=$(npx vitest run src/hooks/useStrategySyncPoller.test.ts "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale.runtime.test.tsx" --no-file-parallelism 2>&1) && { echo "UNEXPECTED GREEN — RED gate not satisfied (the tests do not reproduce the defect)"; echo "$OUT" | tail -20; exit 1; }; for t in "T1:" "T1b:" "T2:" "T2b:"; do echo "$OUT" | grep -E "(✕|×|FAIL).*$t" >/dev/null || { echo "RED gate NOT satisfied: $t is not among the failures — a crash or import error is not the intended RED"; echo "$OUT" | tail -40; exit 1; }; done; echo "$OUT" | grep -E "(✕|×|FAIL).*SYM-interval:" >/dev/null && { echo "SYM-interval FAILED — the interval arm must stay GREEN"; exit 1; }; echo "RED gate satisfied: T1 T1b T2 T2b failing, interval symmetry passing"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; vitest run exits NON-zero with T1, T1b, T2, T2b listed as failures and the interval-arm symmetry case listed as passing
    - grep confirms neither file imports RETRY_THRESHOLD_MS, WARN_THRESHOLD_MS, POLL_BACKOFF_MS, or isComputedAnalytics: `grep -n "RETRY_THRESHOLD_MS\|WARN_THRESHOLD_MS\|POLL_BACKOFF_MS\|isComputedAnalytics" <both files>` returns 0 matches outside comments
    - `grep -n "stubGlobal" <both files>` returns 0 matches
    - The hook double resolves the exact shape `{ data: null, error: null }` (source assertion)
    - 154-INVESTIGATION.md "## RED evidence" section contains the pasted failing assertions
  </acceptance_criteria>
  <done>T1/T1b/T2/T2b demonstrably RED at HEAD, failing output captured in the ledger, files committed</done>
</task>

<task type="auto">
  <name>Task 3: Land T3/T3b RED and write the mechanism conclusion</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale-refusal.runtime.test.tsx, .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md, .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-VALIDATION.md</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.readfailure.runtime.test.tsx (harness donor — same five blocks as Task 2)
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx (:1092-1103 the composite R2-5 guard; :1398-1447 the guard-less single-key arm; :2315-2323 the status line)
    - src/lib/strategyGate.ts (:291-334 — the refusal codes the test must needle on)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md (Task 1's verdict + Task 2's RED evidence)
  </read_first>
  <action>
    `SyncPreviewStep.stale-refusal.runtime.test.tsx` (NEW), same harness as Task 2's component file:
    - T3 (single-key arm): kickoff `{ ok: true, composite: false }`; status read resolves
      `complete_with_warnings`; the heavy terminal fetch resolves trades count 0 AND
      csv_daily_returns count 0 (the heal-delete window), `series_completeness` null. Assert NO
      gate_failed render, NO ErrorEnvelope, NO wizard_error funnel event — the arm must repoll.
      Needle discipline: hand-typed `RENDERED_CODE("GATE_SERIES_PROVENANCE_UNVERIFIED")` shape
      (Donor `:80-107`) — ⛔ never the shared envelope title (it matches three states and reported
      a broken tree GREEN once). At HEAD the gate refuses → RED.
    - T3b (composite symmetry, own `it()`): identical empty-series state on the COMPOSITE arm
      (`composite: true`); assert it repolls — GREEN at HEAD (:1092-1103). The pair makes the
      divergence the subject.
    - Positive counterpart + vacuity fence as in Task 2. Label the cases `it("T3: ...")` and
      `it("T3b: ...")` — the verify greps the failure list for these labels to prove the
      RED/GREEN asymmetry, not merely a non-zero exit.
    - Capture the run output; append T3 RED / T3b GREEN evidence to 154-INVESTIGATION.md
      "## RED evidence".

    154-INVESTIGATION.md — append "## Conclusion":
    - Synthesize Task 1's PROD verdict with the code trace: name the confirmed supplier mechanism
      and state M1's independent standing (why the user had no exit regardless of supplier).
    - Answer the CONTEXT.md question explicitly: instances (a) and (b) share one ROOT IDEA (a
      possibly-not-current reading treated as current) but live at DISTINCT code sites
      (useStrategySyncPoller.ts:228 + SyncPreviewStep.tsx:2290 for (a); the missing single-key
      R2-5 guard at ~:1420 for (b)) — a single-site fix discharges neither alone.
    - Name which fix plans are activated: 154-04 and 154-08 always (M1/M2 client class, established
      from code); 154-07's arm selection per the verdict (M3/H-a → long_fetch tail; M4/H-b →
      process_key queued honesty; M2i bridge-gap → SQL arm; M2ii/M1-only → record 154-07 as NO-OP
      with rationale).

    154-VALIDATION.md — flip the T1/T1b/T2/T2b/T3/T3b rows from ⬜ pending to ❌ red (T3b: ✅
    green) with the run commands, and set `wave_0_complete: true` in frontmatter. Do NOT set
    nyquist_compliant (that is 154-08's ledger-closure call). Use Edit on the existing file, never
    a full rewrite (subagent Write truncation hazard — spot-check `wc -l` after).
  </action>
  <verify>
    <automated>OUT=$(npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.stale-refusal.runtime.test.tsx" --no-file-parallelism 2>&1) && { echo "UNEXPECTED GREEN — T3 RED gate not satisfied"; echo "$OUT" | tail -20; exit 1; }; echo "$OUT" | grep -E "(✕|×|FAIL).*T3:" >/dev/null || { echo "T3 is not among the failures — a crash or import error is not the intended RED"; echo "$OUT" | tail -40; exit 1; }; echo "$OUT" | grep -E "(✕|×|FAIL).*T3b:" >/dev/null && { echo "T3b FAILED — the composite symmetry must stay GREEN"; exit 1; }; grep -c "## Conclusion" ".planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - vitest run shows T3 failing and T3b passing (the asymmetry is the observed fact)
    - 154-INVESTIGATION.md contains "## Conclusion" with a named activated-plans list and the explicit two-sites/one-root answer
    - 154-VALIDATION.md frontmatter has `wave_0_complete: true`; T-rows show ❌/✅ statuses; `wc -l` on the file is >= its pre-edit line count minus 5
    - No production source file was modified in this plan: `git diff --name-only` contains only test files and .planning files
  </acceptance_criteria>
  <done>All five RED pins + one GREEN symmetry pin observed and recorded; investigation closed with a verdict the fix plans can consume</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → PROD DB | read-only SELECTs against khslejtfbuezsmvmtsdn via Supabase MCP |
| test double → poll loop | the fake must reproduce PostgREST's real zero-rows shape or T2 is meaningless |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-01-A | Information Disclosure | Q1/Q2 results pasted into tracked .planning files | mitigate | results contain strategy ids/statuses only — no credentials, no ciphertext columns selected; repo is PUBLIC, so paste only the columns the queries name |
| T-154-01-B | Tampering | PROD data during investigation | mitigate | SELECT-only task; acceptance criterion asserts no DML/DDL issued |
| T-154-SC | Tampering | npm/pip/cargo installs | accept | no packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npx vitest run <the three new files> --no-file-parallelism` exits non-zero with exactly T1, T1b, T2, T2b, T3 failing and the two symmetry cases passing
- 154-INVESTIGATION.md carries: raw PROD rows, "Mechanism verdict:", "## RED evidence", "## Conclusion"
- Zero production source diffs
</verification>

<success_criteria>
The STALE-01 gate is closed in the only form CONTEXT.md accepts: failing regression tests that
reproduce the mechanism, plus PROD evidence discriminating the supplier. Plans 154-04/07/08 can
read a single file (154-INVESTIGATION.md) and know exactly which arms to execute.
</success_criteria>

<output>
Create `.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-01-SUMMARY.md` when done
</output>
