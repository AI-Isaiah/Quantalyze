---
phase: 154-wizcont-stale-wizard-continuity-no-stale-screens
plan: 04
type: execute
wave: 2
depends_on: ["154-01"]
files_modified:
  - src/hooks/useStrategySyncPoller.ts
  - src/hooks/useStrategySyncPoller.test.ts
  - src/components/strategy/SyncProgress.poll.test.tsx
  - src/app/api/strategies/[id]/sync-progress/route.ts
  - src/app/api/strategies/[id]/sync-progress/route.test.ts
autonomous: true
requirements: [STALE-01]

must_haves:
  truths:
    - "A {data: null, error: null} zero-rows read is never reported to onStatus as 'pending' — absence is not a value, in BOTH poller arms (TWIN-3 closed)"
    - "GET /api/strategies/[id]/sync-progress answers with the latest job status for SINGLE-KEY strategies; composite responses stay byte-identical (pinned BEFORE the widening)"
    - "The route still never reads the analytics table (RT-1 invariant) and never emits a false stalled:true for a single-key job"
  artifacts:
    - path: "src/hooks/useStrategySyncPoller.ts"
      provides: "ladder arm mirrors the interval arm's absent-row handling; missingRowGracePolls consumed by both arms"
    - path: "src/app/api/strategies/[id]/sync-progress/route.ts"
      provides: "additive kind-filter widening with field-by-field projection preserved"
  key_links:
    - from: "src/hooks/useStrategySyncPoller.ts ladder arm"
      to: "interval arm :149-159"
      via: "same absent-row semantics (the in-file donor)"
      pattern: "missingRowGracePolls"
    - from: "src/app/api/strategies/[id]/sync-progress/route.test.ts"
      to: "composite response byte-identity"
      via: "pin written and observed green BEFORE the route diff"
      pattern: "stitch_composite"
---

<objective>
Fix the STALE-01a shared layer: the poller's null→"pending" fabrication (TWIN-3) and the
sync-progress route's composite-only filter (TWIN-5's server gate) — greening T2 from 154-01 and
making the in-flight datum available to single-key strategies for 154-08's honest screens.

⚠️ CONTINGENCY (GATE 1): this plan's design covers only what mechanism M1/M2 supports, which
RESEARCH established from code alone — the fabricated status and the projected-away in-flight
datum. It executes REGARDLESS of 154-01's supplier verdict. If 154-INVESTIGATION.md implicates a
backend supplier (M3/M4/bridge), that fix lands in 154-07, not here. Read 154-INVESTIGATION.md's
"## Conclusion" before starting and note the verdict in the SUMMARY.

⛔ No new timeout or threshold numbers. The existing ladder constants are referenced, never moved,
and no new numeric constant may be introduced in this plan.

Purpose: close the "absence is not a value" class at the read that nobody swept, and un-hide the
owner-readable in-flight evidence (get_user_compute_jobs) from the single-key user class.
Output: T2 green; interval-arm symmetry stays green; composite route behavior pinned byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (§§ 4, 6 — the poller's two arms and the route's contracts, quoted verbatim)
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close TWIN-3 — the ladder arm stops fabricating "pending"</name>
  <files>src/hooks/useStrategySyncPoller.ts, src/hooks/useStrategySyncPoller.test.ts, src/components/strategy/SyncProgress.poll.test.tsx</files>
  <read_first>
    - src/hooks/useStrategySyncPoller.ts (FULL file — the interval arm :149-159 is the in-file donor; the ladder arm :226-249 is the defect; the ref/deps discipline :89-104 and :275-283 must survive; the declared-but-unread missingRowGracePolls option :61)
    - src/hooks/useStrategySyncPoller.test.ts (154-01's RED T2 — the test this task greens; do not weaken its assertions)
    - src/components/strategy/SyncProgress.poll.test.tsx (the OTHER surface sharing this hook — RESEARCH State-of-the-Art row 4)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (§ 4)
  </read_first>
  <action>
    In the ladder arm of `useStrategySyncPoller.ts` (:226-249):
    - Replace the `statusRow?.computation_status ?? "pending"` coercion: when the read resolves
      `{data: null, error: null}` (PostgREST zero rows — absent row OR RLS-filtered), do NOT call
      `onStatusRef.current` with any fabricated value. Mirror the interval arm's semantics
      (:149-159): the absent-row poll consumes grace; when `missingRowGracePolls` is set and the
      count of consecutive absent-row polls exceeds it, escalate via `onErrorRef.current()` (the
      existing SYNC_FAILED escalation path); otherwise `scheduleNext()` silently. This makes the
      declared-but-unread `missingRowGracePolls` option (:61) real for the ladder arm — it is the
      existing affordance PATTERNS told the plan to reuse instead of re-inventing.
    - Rule 12 (fail loud): on the FIRST absent-row poll of a run, `console.warn` naming the hook,
      the strategyId, and the two possible facts ("no strategy_analytics row, or the session's RLS
      read matched nothing") — a clean-null read must be observable, today it logs nothing.
    - When a real row IS present, behavior is unchanged: `onStatus(row.computation_status, row.computation_error ?? null)`
      and the terminal check as today. ⛔ Do NOT touch the terminal set at :235 or
      `isComputedAnalytics` (Pitfall 1 — terminal detection is correct).
    - `consecutiveErrors = 0` may still reset on a clean read, but the absent-row counter is a
      SEPARATE counter (an error is not the same fact as an absent row — the file's own C-3 rule).
    - Preserve the ref/deps discipline exactly: no non-primitive enters the effect deps array; the
      absent-row counter lives alongside `consecutiveErrors` inside the effect closure.
    - Green T2 in useStrategySyncPoller.test.ts WITHOUT editing its oracle (the assertion that
      onStatus is never called with "pending" on zero-rows stands as written). Add one new case:
      ladder arm + missingRowGracePolls set + persistent zero-rows → onError fires after the
      grace boundary (pin the escalation with a hand-typed literal grace value in the TEST, e.g. 2).
    - Because the hook is shared, extend `SyncProgress.poll.test.tsx` in the SAME commit with a
      regression case: the interval-arm consumer's existing behavior under zero-rows is unchanged
      (no onStatus call, no escalation without the option) — the pinned asymmetry
      (no consecutive-error escalation in interval mode) must survive.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useStrategySyncPoller.test.ts src/components/strategy/SyncProgress.poll.test.tsx --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - T2 green; `grep -c '?? "pending"' src/hooks/useStrategySyncPoller.ts` returns 0
    - `grep -n "missingRowGracePolls" src/hooks/useStrategySyncPoller.ts` shows it consumed in BOTH arms
    - The effect deps array (:275-283 region) is unchanged in membership — no new dep added (source assertion)
    - No numeric literal was added to the hook's production code (the grace VALUE is caller-supplied; callers are wired in 154-08)
    - SyncProgress.poll.test.tsx passes with the new regression case present
  </acceptance_criteria>
  <done>Absence is not a value in either poller arm; the divergence 154-01 pinned is gone; the sibling surface is regression-covered</done>
</task>

<task type="auto">
  <name>Task 2: Widen the sync-progress kind filter — composite byte-identity pinned first</name>
  <files>src/app/api/strategies/[id]/sync-progress/route.ts, src/app/api/strategies/[id]/sync-progress/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/[id]/sync-progress/route.ts (FULL file — the kind filter :184-192, the field-by-field projection :198-218 (T-95-07: never spread an RPC row), the stitch-specific heartbeat :226-231, the RT-1 docblock :20-35)
    - src/app/api/strategies/[id]/sync-progress/route.test.ts (581 lines — extend, do not fork; this is where the byte-identity pin lives)
    - src/lib/sync-progress.ts (the shared response contract module — SyncProgressResponse shape)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (§ 6, incl. the false-stalled hazard)
  </read_first>
  <action>
    ORDER MATTERS — pin, then widen:
    1. FIRST commit: add route.test.ts cases pinning the CURRENT composite response byte-identically
       (jobStatus, stalled, memberProgress for a stitch_composite job set) and the current
       single-key IDLE body `{jobStatus: null, stalled: false, memberProgress: []}`. Observe green
       against the UNMODIFIED route.
    2. THEN widen `:184-192`: for the strategy's job rows, select the latest row across ALL kinds
       (by created_at, as today) instead of only `stitch_composite` — but derive `stalled` and
       `memberProgress` ONLY from stitch rows:
       - `jobStatus`: projected from the latest row regardless of kind (this is the in-flight
         datum 154-08 needs — jobStatus null now MEANS "zero compute_jobs rows", the branch-(d)/M4
         tell).
       - `stalled`: computed exactly as today and ONLY when the latest row is stitch_composite —
         the heartbeat (`metadata.member_progress_at ?? claimed_at`) is stitch-specific; for any
         other kind emit `stalled: false` ALWAYS, with a comment naming the false-positive hazard
         (claimed_at never refreshes on a long legitimate single-key job → a naive widening emits
         a FALSE stalled:true).
       - `memberProgress`: stitch rows only, unchanged.
    3. Update route.test.ts: composite pins still green UNMODIFIED (the diff to test expectations
       must be zero for composite cases); new single-key cases: running job → jobStatus "running",
       stalled false; zero rows → jobStatus null; done job → jobStatus "done". Assert the response
       never contains a `metadata` key (the redaction pin extends to the widened arm) and
       last_error stays redacted per the existing assertions.
    4. RT-1: the route still never reads strategy_analytics — assert via
       `grep -c "strategy_analytics" route.ts` = docblock-only (the docblock mentions it; no query
       does).
    Ownership scope is NOT widened: the RPC is auth.uid()-scoped; the route's existing isUuid +
    limiter ordering is untouched.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/strategies/[id]/sync-progress/route.test.ts" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The byte-identity pin commit precedes the widening commit in history (two commits, verifiable in git log)
    - Composite test expectations are UNCHANGED by the widening (git diff of the pin cases between the two commits is empty)
    - Single-key: latest running job → `{jobStatus: "running", stalled: false}`; zero job rows → `{jobStatus: null}` (test assertions)
    - `stalled: true` is unreachable for non-stitch kinds (test case: single-key running job with ancient claimed_at → stalled false)
    - No response arm spreads an RPC row; no `metadata` key in any response (existing + new assertions green)
  </acceptance_criteria>
  <done>The owner-readable in-flight datum reaches single-key callers additively; composite behavior provably unmoved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → sync-progress route | caller-supplied strategy id; RPC is auth.uid()-scoped |
| widened projection → browser | compute_jobs fields cross to the client — redaction must hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-04-A | Information Disclosure (redaction bypass via widened projection) | sync-progress route | mitigate | field-by-field projection preserved; test asserts no metadata key and redacted last_error on the widened arm (T-95-07) |
| T-154-04-B | Information Disclosure / self-DoS (zero-rows-under-RLS read as domain value) | useStrategySyncPoller | mitigate | absent row never becomes "pending"; observable console.warn; grace escalation via existing onError path |
| T-154-04-C | Elevation of Privilege (cross-tenant job read) | widened kind filter | mitigate | ownership scope untouched — get_user_compute_jobs is auth.uid()-scoped SECURITY DEFINER; route adds no new inputs |
| T-154-04-D | DoS (false stalled:true aborting healthy long jobs) | stalled derivation | mitigate | stalled pinned stitch-only with test proving ancient claimed_at on single-key ⇒ false |
| T-154-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- T2 green, interval symmetry green, SyncProgress regression green, route tests green, tsc clean
- `npx vitest run src/hooks src/components/strategy/SyncProgress.poll.test.tsx "src/app/api/strategies/[id]/sync-progress" --no-file-parallelism` exits 0
</verification>

<success_criteria>
The two shared-layer instances of "a possibly-not-current reading treated as current" are closed
class-wide (both poller arms; the projection un-hidden), without moving any composite behavior or
any timing constant. 154-08 can now build the honest screens on real inputs.
</success_criteria>

<output>
Create `.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-04-SUMMARY.md` when done
</output>
