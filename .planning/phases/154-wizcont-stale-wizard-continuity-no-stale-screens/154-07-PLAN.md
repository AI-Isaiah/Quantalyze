---
phase: 154-wizcont-stale-wizard-continuity-no-stale-screens
plan: 07
type: execute
wave: 2
depends_on: ["154-01"]
files_modified:
  - analytics-service/services/ingestion/long_fetch.py
  - analytics-service/routers/process_key.py
  - analytics-service/tests/test_long_fetch_follow_on_guard.py
  - supabase/tests/test_sync_status_bridge_branches.sql
autonomous: true
requirements: [STALE-01]

must_haves:
  truths:
    - "The supplier arm 154-01's verdict implicates is fixed at its root — in Python or SQL, not contained in the frontend (CONTEXT.md: no bandaids, no frontend-only containment of a backend cause)"
    - "Arms the verdict does NOT implicate are explicitly dispositioned NO-OP with the verdict quoted — never silently skipped"
  artifacts:
    - path: "analytics-service/tests/test_long_fetch_follow_on_guard.py"
      provides: "extended (never forked) guard pinning whichever backend behavior this plan changes"
  key_links:
    - from: "154-INVESTIGATION.md ## Conclusion"
      to: "this plan's executed arm selection"
      via: "decision matrix below — the verdict chooses the arm"
      pattern: "Mechanism verdict:"
---

<objective>
Execute the supplier-mechanism arm of the STALE-01 fix — the part whose design is CONTINGENT on
154-01's finding. This plan is a decision matrix, not a fixed diff: read
154-INVESTIGATION.md's "## Conclusion" FIRST and execute only the implicated arm(s).

⛔ GATE 1 discipline: no mechanism beyond what the verdict supports is implemented. No timeout or
threshold numbers are invented. The client-side class fixes (M1/M2) are NOT here — they are
154-04/154-08 and run regardless.

Purpose: CONTEXT.md locked decision — "Fix at the root cause wherever it lives, including
analytics-service/ (Python) if the job chain never wrote a terminal status, or the DB/RLS layer if
a read is being denied."
Output: the implicated backend hole closed with a regression test, or a recorded NO-OP with the
verdict quoted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-CONTEXT.md
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-RESEARCH.md (§ STALE-01 Steps 3-6 — H-a, H-b, bridge branch (d), the reaper's blind spot)

Decision matrix (the verdict selects; multiple arms may apply):
| Verdict names | Arm to execute |
|---|---|
| M3 / H-a (parent parked at done_pending_children; tail enqueue swallowed) | ARM A |
| M4 / H-b (200-with-queued:false against a strategy with no work) | ARM B |
| M2(i) with Q1 "analytics_row_exists=false while Q2 all-done" (bridge never invoked) | ARM C |
| M2(ii) (RLS/lost-session zero-rows) or M1-only | NO-OP here — the class fix is 154-04/154-08; record the disposition |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the verdict; execute the implicated arm(s)</name>
  <files>analytics-service/services/ingestion/long_fetch.py, analytics-service/routers/process_key.py, analytics-service/tests/test_long_fetch_follow_on_guard.py</files>
  <read_first>
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md (the verdict — this decides everything below)
    - analytics-service/services/ingestion/long_fetch.py (:540-660 — the best-effort tail enqueue inside try/except that only log.errors and returns DONE regardless)
    - analytics-service/routers/process_key.py (:640-760 _resume_duplicate_job + _wizard_duplicate_reply; :1600-1640 the long-fetch enqueue)
    - analytics-service/tests/test_long_fetch_follow_on_guard.py (exists — EXTEND, never fork)
    - analytics-service/services/job_worker.py (:495-530 JOB_CHAIN_FOLLOW_ON topology — context for either arm)
  </read_first>
  <action>
    ARM A (M3/H-a): make the swallowed tail-enqueue fail loud at the root. When
    `long_fetch.py`'s follow-on enqueue (:600-628) fails, the handler must NOT return
    `DispatchOutcome.DONE` as if the chain will continue — the parent otherwise parks at
    done_pending_children forever, invisible to the reaper (it requires `computing` AND no active
    job). Root-cause form: propagate the enqueue failure into the job outcome the worker records
    (the retryable-failure path that already exists for this handler — reuse the existing failure
    plumbing; invent no new state, no new timeout). Extend
    test_long_fetch_follow_on_guard.py: a failing child enqueue can no longer coexist with a DONE
    outcome — assert the exact outcome the existing plumbing produces, RED-first against the
    unmodified handler.

    ARM B (M4/H-b): make the duplicate-reply honest at the source. `_wizard_duplicate_reply`
    already answers `queued: False` — the client half (ignoring it) is 154-08's. The Python half
    here: ensure the `queued: False` reply is emitted ONLY when it is true that no new work exists
    AND the existing verification/job state is genuinely non-resumable
    (_RESUMABLE_VERIFICATION_STATUSES review); if the verdict shows a resumable state was refused
    re-enqueue, widen the resumable set to match the observed state — with the verdict row quoted
    in the test's docstring. Extend the process_key duplicate-arm pytest RED-first. ⛔ Do not
    change the HTTP status or the envelope contract (process-key-onboard-contract.ts has a
    bidirectional pytest/vitest oracle — if the reply SHAPE changes, both halves move in the same
    commit; prefer not changing the shape at all).

    ARM C (M2(i) bridge-invocation gap): the fix target is SQL — see Task 2.

    NO-OP arm: if the verdict implicates only M1/M2(ii), make NO Python change. Write the
    disposition into the SUMMARY quoting the verdict line, and state which plan closes the
    implicated mechanism (154-04/154-08).

    Regardless of arm: run from analytics-service/ — `cd analytics-service && pytest tests/test_long_fetch_follow_on_guard.py -x`
    (repo-root pytest misses VCR cassettes and fires live broker calls), and
    `cd analytics-service && mypy --strict .` before finishing (the milestone gate runs pytest
    only; mypy errors otherwise stay latent until PR CI). Fix typing with cast(), never
    `# type: ignore`.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_long_fetch_follow_on_guard.py -x -q && mypy --strict services/ingestion/long_fetch.py routers/process_key.py</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY quotes the verdict line and maps it to the executed arm(s) or the NO-OP disposition
    - If ARM A: the failing-enqueue test observed RED before the fix, GREEN after; no new constant introduced
    - If ARM B: the duplicate-arm test observed RED first; contract shape unchanged (vitest contract suite still green: npx vitest run src/lib/process-key-onboard-contract --no-file-parallelism)
    - pytest run from analytics-service/ (not repo root); mypy --strict clean on touched files
  </acceptance_criteria>
  <done>The implicated Python hole is closed at the root with a red-first regression, or the NO-OP is on the record with the verdict quoted</done>
</task>

<task type="auto">
  <name>Task 2: DB arm — bridge branch (d) pin (only if implicated)</name>
  <files>supabase/tests/test_sync_status_bridge_branches.sql</files>
  <read_first>
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-INVESTIGATION.md (is ARM C implicated?)
    - supabase/migrations/20260710150000_sync_status_supersede_failed_per_kind.sql (the LATEST deployed bridge body — ⛔ never migration 038/20260412094454, Pitfall 3)
    - supabase/tests/ (existing bridge-adjacent gates — extend the existing file if one covers the bridge; create the named file only if none does)
  </read_first>
  <action>
    ONLY if the verdict implicates M2(i)/branch-(d) (the bridge was never invoked for a strategy
    whose jobs completed, or zero-rows RETURN left the DEFAULT 'pending' standing):
    - Pin the implicated branch behavior in a SQL gate (supabase/tests/ house form — DO block,
      RAISE EXCEPTION, psql usage comment). If the fix requires changing the bridge or its
      invocation, the new migration re-bases on 20260710150000 (grep ALL migrations for the
      function name first — the B5b rule) and goes through the same TEST-first MCP checkpoint
      discipline as 154-03 (raise it to the orchestrator rather than merging unreviewed).
    - If ARM C is NOT implicated: create nothing; record the NO-OP in the SUMMARY. Do not commit
      an empty file.
  </action>
  <verify>
    <automated>test -f supabase/tests/test_sync_status_bridge_branches.sql && grep -c "RAISE EXCEPTION" supabase/tests/test_sync_status_bridge_branches.sql || echo "NO-OP arm — verify SUMMARY records the disposition"</automated>
  </verify>
  <acceptance_criteria>
    - Either the gate exists asserting the implicated branch (with the re-base rule honored for any bridge change), or the SUMMARY carries an explicit "ARM C: NO-OP — verdict was <X>" line
    - No bridge migration merged without the 154-03-style TEST/MCP/reviewer checkpoint
  </acceptance_criteria>
  <done>The DB arm is closed or explicitly dispositioned — never silently skipped</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → compute_jobs/strategy_analytics | job-outcome writes derive user-visible status |
| duplicate-reply → wizard client | queued flag crosses the seam; its honesty is the subject |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-07-A | Repudiation (DONE recorded while the chain died) | long_fetch tail enqueue | mitigate (if ARM A) | enqueue failure propagates to the recorded job outcome via existing failure plumbing |
| T-154-07-B | Information Disclosure | any new log lines | mitigate | scrub_freeform_string / existing redaction conventions on every new log |
| T-154-07-C | DoS (contract drift breaking the wizard kickoff) | duplicate-reply shape | mitigate | bidirectional pytest/vitest contract oracle kept green; shape preferred unchanged |
| T-154-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- `cd analytics-service && pytest -x -q` targeted files green; mypy --strict clean on touched files
- SUMMARY carries the verdict quote + per-arm disposition table (executed / NO-OP with reason)
</verification>

<success_criteria>
Whatever put the row in pending/absent on 2026-08-04 is fixed where it lives — or is proven to
live entirely in the client class already covered — with the reasoning on the record either way.
</success_criteria>

<output>
Create `.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-07-SUMMARY.md` when done
</output>
