---
phase: 154-wizcont-stale-wizard-continuity-no-stale-screens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/wizard/draft-query.ts
  - src/lib/wizard/draft-query.test.ts
  - src/app/api/strategies/wizard-draft/route.ts
  - src/app/api/strategies/wizard-draft/route.test.ts
  - src/app/(dashboard)/strategies/new/wizard/page.tsx
  - src/__tests__/wizard-draft-query-single-source.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [WIZCONT-01]

must_haves:
  truths:
    - "Exactly ONE draft-query shape exists in src/ — the helper — and both the wizard SSR page and the new route import it (the CONTEXT.md single-source criterion, grep-enforced)"
    - "An authenticated client can read its OWN latest wizard draft via GET /api/strategies/wizard-draft; the route exposes no id parameter and no other user's rows"
    - "REQUIREMENTS.md no longer claims /strategies/new is 'a branch chooser with no draft awareness' — the corrected diagnosis names ContributionWizardOverlay.tsx:146 as the defect site"
  artifacts:
    - path: "src/lib/wizard/draft-query.ts"
      provides: "readLatestWizardDraft(client, userId) — the verbatim page.tsx:79-89 query + InitialDraft type + WizardDraftKind discriminator + draftMatchesSource rule"
      exports: ["readLatestWizardDraft", "deriveDraftKind", "draftMatchesSource"]
    - path: "src/app/api/strategies/wizard-draft/route.ts"
      provides: "GET, withAuth, user-scoped client, NO_STORE_HEADERS, field-by-field projection"
      exports: ["GET"]
    - path: "src/__tests__/wizard-draft-query-single-source.test.ts"
      provides: "source-scan guard: the source='wizard' AND status='draft' query pair appears only in the helper"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/page.tsx"
      to: "src/lib/wizard/draft-query.ts"
      via: "import readLatestWizardDraft"
      pattern: "from \"@/lib/wizard/draft-query\""
    - from: "src/app/api/strategies/wizard-draft/route.ts"
      to: "src/lib/wizard/draft-query.ts"
      via: "import readLatestWizardDraft"
      pattern: "from \"@/lib/wizard/draft-query\""
---

<objective>
Build the WIZCONT-01 plumbing: single-source the canonical wizard-draft query into a shared
helper, expose it to the client-only overlay through a new authenticated GET route, and correct
the stale REQUIREMENTS.md entry-path diagnosis. No UI behavior changes in this plan — the wizard
page is refactored to import the helper with byte-identical query semantics; plan 154-05 wires the
overlay and the resume truth table.

Purpose: CONTEXT.md locked decision — "One query shape, two callers — a second divergent shape is
how the two paths drift apart." Option A (route handler) chosen per RESEARCH's comparison, on the
locked criterion.
Output: helper + route + contract test + corrected REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-CONTEXT.md
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (§§ 2, 3, 12 — route analog, helper analog, contract-test donor)

AGENTS.md gate DISCHARGED at planning (A1): `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md:669`
records "The default caching for GET handlers was changed from static to dynamic" in v15.0.0-RC —
GET route handlers are dynamic by default in this repo's Next version. No `export const dynamic`
opt-out is required; the repo's `NO_STORE_HEADERS` convention covers response caching. The route
also reads auth cookies via the server client, which is dynamic by construction.

<interfaces>
The query body to move VERBATIM (from wizard/page.tsx:79-89) and its return type
(InitialDraft, page.tsx:33-47) are quoted in full in 154-PATTERNS.md § 3. The route analog's
imports/wrapper/error arms are quoted in § 2. The contract-test donor skeleton is in § 12.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the single-sourced draft-query helper</name>
  <files>src/lib/wizard/draft-query.ts, src/lib/wizard/draft-query.test.ts, src/app/(dashboard)/strategies/new/wizard/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/page.tsx (full file — the query :79-89, the InitialDraft type :33-47, the Suspense/searchParams handling that must not change)
    - src/lib/sync-progress.ts (the analog: one contract module imported by both a route and a component)
    - src/lib/wizard/localStorage.ts and its colocated test (directory conventions for src/lib/wizard/)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (§ 3, incl. the A4/W-2 warning)
  </read_first>
  <action>
    Create `src/lib/wizard/draft-query.ts`:
    - Move the `InitialDraft` interface from page.tsx:33-47 here (export it), and move the query
      body from page.tsx:79-89 VERBATIM into an exported async
      `readLatestWizardDraft(supabase, userId)` — same 15 columns, same
      `.eq("user_id", userId).eq("source", "wizard").eq("status", "draft")` predicate, same
      `.order("created_at", { ascending: false }).limit(1).maybeSingle()`.
    - Resolve PATTERNS open item A4 (the CSV-vs-composite discriminator) HERE, single-sourced:
      export `type WizardDraftKind = "api" | "csv" | "composite"` and
      `deriveDraftKind(row): WizardDraftKind` — `api_key_id !== null` → "api"; else a
      `strategy_keys` member count > 0 → "composite"; else "csv". Obtain the member count inside
      the helper: preferred form is the PostgREST embedded aggregate `strategy_keys(count)`
      appended to the select; if the embed errors against the live schema, fall back to a second
      `select("strategy_id", { count: "exact", head: true }).eq("strategy_id", row.id)` INSIDE the
      helper — and per the absence-is-not-a-value class, a null count with a null error throws,
      never `?? 0` (donor comment: SyncPreviewStep.tsx:1400-1413). Either way the discriminator
      has exactly one definition. The helper's return couples row + kind:
      `{ draft: InitialDraft | null, kind: WizardDraftKind | null }`.
    - Export `draftMatchesSource(kind, source)`: `kind === "csv" ? source === "csv" : source !== "csv"`
      — the branch-matching rule 154-05 applies on BOTH paths (a draft is offered only on the
      branch matching its kind). One definition, two callers, same reason as the query.
    - Refactor page.tsx to import `InitialDraft` and call `readLatestWizardDraft`. Behavior must
      stay byte-equivalent this plan: page.tsx continues to pass the draft down exactly as today
      (the matching rule is APPLIED in 154-05, not here). The Suspense boundary and searchParams
      handling are untouched.
    - `draft-query.test.ts` (colocated, per src/lib/wizard/ convention): unit-test deriveDraftKind
      (three kinds + the composite-vs-csv boundary: api_key_id null + members>0 → "composite",
      NOT "csv") and draftMatchesSource (4-row truth table), plus the null-count-throws arm if the
      fallback form is used. Use a chainable double resolving PostgREST shapes
      ({data, error}); vi.spyOn/restoreAllMocks only, no stubGlobal.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizard/draft-query.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx no longer contains `.eq("source", "wizard")` (the query lives only in the helper): `grep -c 'eq("source", "wizard")' "src/app/(dashboard)/strategies/new/wizard/page.tsx"` returns 0
    - draft-query.ts contains the 15-column select string byte-identical to the pre-move page.tsx:79-89 select (diff the string)
    - deriveDraftKind returns "composite" for {api_key_id: null, member_count: 2} and "csv" for {api_key_id: null, member_count: 0} (test assertions)
    - tsc clean; existing WizardClient.test.tsx and page-adjacent tests still green: `npx vitest run "src/app/(dashboard)/strategies/new/wizard" --no-file-parallelism` passes
  </acceptance_criteria>
  <done>One helper owns the query, the type, the kind discriminator and the matching rule; the SSR page consumes it with unchanged behavior</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/strategies/wizard-draft route</name>
  <files>src/app/api/strategies/wizard-draft/route.ts, src/app/api/strategies/wizard-draft/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/composite/members/route.ts (the exact analog: withAuth wrapper, user-scoped createClient, field-by-field projection, NO_STORE_HEADERS on EVERY response, the no-rate-limiter rationale :36-39, never-forward-raw-message arms :105-115/:146-158)
    - src/app/api/strategies/composite/members/route.test.ts (route-test harness shape)
    - src/lib/wizard/draft-query.ts (Task 1's contract — import it, do not re-derive)
    - src/__tests__/no-store-coverage.test.ts (the repo-wide contract a new route must satisfy)
  </read_first>
  <action>
    Create `GET /api/strategies/wizard-draft` cloning the composite/members conventions:
    - `withAuth`; user-scoped `createClient()` from `@/lib/supabase/server` — ⛔ NEVER
      `createAdminClient()`. Call `readLatestWizardDraft(supabase, user.id)`.
    - NO id parameter (the route reads the caller's OWN latest draft — no enumeration surface, no
      isUuid check needed). NO rate limiter, with the members-route rationale adapted in a
      docblock: idempotent, RLS-bounded, fires on overlay open.
    - Response: 200 `{ draft: <field-by-field projection of InitialDraft> | null, kind: <WizardDraftKind> | null }`
      with NO_STORE_HEADERS. Project field-by-field from the helper's row — never spread. The
      projection is exactly the InitialDraft fields; no ciphertext/credential columns are in the
      select, keep it that way.
    - Error arms: DB error → correlation-id console.error + `{ code: "UNKNOWN" }` 500; caught
      exception → same, never forwarding the raw message (H-0305 discipline, copy the members
      route's arms).
    - route.test.ts: authed happy path (draft + kind returned), zero-rows path
      ({draft: null, kind: null} — a 200, not an error), DB-error path (500, code UNKNOWN, no raw
      message in body), and a header assertion that every arm carries Cache-Control from
      NO_STORE_HEADERS.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/wizard-draft/route.test.ts src/__tests__/no-store-coverage.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createAdminClient" src/app/api/strategies/wizard-draft/route.ts` returns 0
    - `grep -c "readLatestWizardDraft" src/app/api/strategies/wizard-draft/route.ts` returns >= 1 (the route imports the helper, not a second query)
    - no-store-coverage contract test passes with the new route present
    - Zero-rows arm returns 200 with `draft: null` — asserted in route.test.ts (absent draft is a fact, not an error)
  </acceptance_criteria>
  <done>The overlay has a sanctioned, RLS-bounded read for the caller's own draft, single-sourced through the helper</done>
</task>

<task type="auto">
  <name>Task 3: Single-source contract test + REQUIREMENTS.md correction</name>
  <files>src/__tests__/wizard-draft-query-single-source.test.ts, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/__tests__/strategies-published-sole-writer-guard.test.ts (the donor: walk/isTestPath helpers, the allow-list shape, and the falsifiability record at :62-68 — copy that discipline hardest)
    - .planning/REQUIREMENTS.md (the WIZCONT-01 entry at ~line 617 — the paragraph to correct)
    - src/app/(dashboard)/strategies/new/page.tsx (the 32-line pure redirect() — the evidence for the correction)
  </read_first>
  <action>
    Contract test (donor: strategies-published-sole-writer-guard):
    - Offender predicate: a non-test `src/` file whose source contains BOTH `.eq("source", "wizard")`
      and `.eq("status", "draft")` (the wizard-draft query pair). Allow-list: exactly
      `src/lib/wizard/draft-query.ts`. Walk src/ with the donor's walk/isTestPath helpers.
    - Falsifiability demonstration (REQUIRED, donor :62-68 discipline): temporarily add the query
      pair to a throwaway src/ file, run the test, OBSERVE RED naming that file, remove it, observe
      GREEN. Paste both outputs into the plan SUMMARY — "the test covers it" is not evidence.
    - Grep-gate hygiene: the predicate must match code, not its own allow-list string — assert on
      the query-pair regexes, never on the filename appearing in source.

    REQUIREMENTS.md correction (GATE: leaving the wrong diagnosis invites the next reader to
    re-fix a file that was never broken). Edit the WIZCONT-01 entry (~line 617):
    - Replace the sentence claiming `/strategies/new` "is a branch chooser with no draft awareness,
      so it re-asks API-vs-CSV" with the observed-at-HEAD fact: `src/app/(dashboard)/strategies/new/page.tsx`
      is a pure `redirect()` (32 lines, forwards `?source=csv`); the draft-blind entry point is
      `ContributionWizardOverlay.tsx:146` (`initialDraft={null}`, the Phase-110 deferral), reached
      from `+ Strategy` and the My Strategies empty state.
    - Add a dated correction note: "(corrected 2026-08-12 by Phase 154 observation — the earlier
      'branch chooser' diagnosis was inferred from stale code, per the requirement's own demand to
      establish the entry path by observation first)".
    - Use Edit (never a full-file Write) on REQUIREMENTS.md; verify surrounding entries are intact
      afterwards (`wc -l` within 5 lines of the pre-edit count).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/wizard-draft-query-single-source.test.ts --no-file-parallelism && grep -c "ContributionWizardOverlay.tsx:146" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Contract test green at HEAD with allow-list = the helper only; RED/GREEN falsifiability outputs pasted in SUMMARY
    - `grep -c "branch chooser with no draft awareness" .planning/REQUIREMENTS.md` returns 0
    - REQUIREMENTS.md WIZCONT-01 entry names ContributionWizardOverlay.tsx:146 and the pure redirect() fact, with the dated correction note
    - Full targeted suite green: `npx vitest run src/__tests__ src/lib/wizard "src/app/api/strategies/wizard-draft" --no-file-parallelism`
  </acceptance_criteria>
  <done>The single-source criterion is machine-enforced and the ledger's diagnosis matches HEAD</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/strategies/wizard-draft | authenticated client reads its own draft; untrusted caller |
| route → strategies table | RLS-bounded user-scoped client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-02-A | Information Disclosure (enumeration) | wizard-draft route | mitigate | no id parameter — route reads the caller's own latest draft only; withAuth; user-scoped client; RLS predicate user_id = auth.uid() |
| T-154-02-B | Information Disclosure (raw seam error) | route error arms | mitigate | never forward raw DB message; correlation-id log + `{code:"UNKNOWN"}` 500 (members-route pattern) |
| T-154-02-C | Elevation of Privilege | route client selection | mitigate | createAdminClient forbidden — grep acceptance criterion pins it |
| T-154-02-D | DoS (limiter-bucket abuse) | absent limiter | accept | read is idempotent, RLS-bounded, fires on overlay open; documented rationale matches composite/members precedent |
| T-154-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/wizard src/app/api/strategies/wizard-draft src/__tests__/wizard-draft-query-single-source.test.ts --no-file-parallelism` green
- `npx tsc --noEmit` clean
- grep gates: query pair only in helper; no createAdminClient in the route; REQUIREMENTS.md corrected
</verification>

<success_criteria>
The overlay (154-05) can obtain the caller's draft through one sanctioned seam whose query shape
cannot silently fork from the SSR page's — enforced by a contract test demonstrated falsifiable.
</success_criteria>

<output>
Create `.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-02-SUMMARY.md` when done
</output>
