---
phase: 154-wizcont-stale-wizard-continuity-no-stale-screens
plan: 06
type: execute
wave: 2
depends_on: ["154-03"]
files_modified:
  - src/app/api/strategies/create-with-key/route.ts
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/api/strategies/composite/add-key/route.ts
  - src/app/api/strategies/composite/add-key/route.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx
  - src/lib/api/pgConstraintName.ts # optional — created ONLY if Task 2 extracts the shared 23505 constraint-name helper
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [WIZCONT-02]

must_haves:
  truths:
    - "A second create-with-key with the same MT5 login and a DIFFERENT wizard_session_id returns the EXISTING row — no second strategy, no second api_keys row, strategy_keys membership untouched, ciphertext untouched (fail toward the existing row, never overwrite)"
    - "The 23505 arm distinguishes the venue-identity constraint from strategies_user_wizard_session_source_uniq by constraint name — the token-less path never reports the wrong fact"
    - "The dedup outcome renders as the neutral strip, never an ErrorEnvelope, and never echoes the credential or account id"
    - "The residual gap (ccxt venues expose no stable non-secret id — verified against ValidationResult) is WRITTEN DOWN in REQUIREMENTS.md, not papered over"
  artifacts:
    - path: "src/app/api/strategies/create-with-key/route.ts"
      provides: "one fence, two keys — venue-identity arm beside the wizard_session_id arm with identical failure posture"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      provides: "the UI-SPEC state-4 neutral dedup strip"
  key_links:
    - from: "create-with-key/route.ts fence"
      to: "api_keys.venue_account_id (154-03 column)"
      via: "SELECT eq(user_id, exchange, venue_account_id) + RPC p_venue_account_id"
      pattern: "venue_account_id"
    - from: "ConnectKeyStep.tsx"
      to: "create-with-key dedup response marker"
      via: "renders the neutral strip on deduped: true"
      pattern: "deduped"
---

<objective>
Wire the WIZCONT-02 application layer onto 154-03's DB backstop: the second fence key beside the
existing wizard_session_id fence (one fence, two keys — CONTEXT.md locked), constraint-name
discrimination on the 23505 arms of BOTH wizard-write routes (TWIN-8), and the UI-SPEC's
deliberately small dedup notice.

Scope is NARROW by locked decision: only venues with a venue-confirmed stable non-secret id —
MT5 only today (verified: ValidationResult carries no account-identity field for ccxt venues).
The residual is recorded, not papered over. Keep every change additive — Phase 156 relocates the
api_keys INSERT behind a service-role writer and must not have to unpick this.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-CONTEXT.md
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (§ 7 — the fence excerpt, the failure posture, the 23505 arm, the MT5 normalisation, the captureToSentry secrets array)
@.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-UI-SPEC.md (§ State Contract 4 + Copywriting — the exact strip markup pattern and copy)

<interfaces>
From 154-03: `api_keys.venue_account_id text`; partial UNIQUE
`api_keys_user_exchange_venue_account_uniq (user_id, exchange, venue_account_id) WHERE venue_account_id IS NOT NULL`;
`create_wizard_strategy(..., p_venue_account_id text DEFAULT NULL)` — single overload, stamps the
column on the api_keys INSERT; BEFORE INSERT scrub trigger nulls non-privileged writes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The second fence key + RPC threading in create-with-key</name>
  <files>src/app/api/strategies/create-with-key/route.ts, src/app/api/strategies/create-with-key/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/create-with-key/route.ts (FULL relevant spans — the fence :262-290 incl. its failure posture; the MT5 normalisation :103-172; the RPC call :409-421; the 23505 arm :428-440 incl. the captureToSentry secrets array)
    - src/app/api/strategies/create-with-key/route.test.ts (1784 lines — extend, do not fork; find the fence and 23505 describes)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-PATTERNS.md (§ 7)
  </read_first>
  <action>
    Fence (one fence, two keys — beside :262-290, NOT a separate block elsewhere):
    - Compute `const venueAccountId = isMt5 ? api_key.trim() : null;` (the MT5 login IS the
      api_key slot — validate-and-encrypt/route.ts:66 mapping; `isMt5` already exists :103-172).
      For every other venue it is null and the arm is inert — ⛔ no `exchange === 'mt5'` scattering
      beyond this single capture point; the arm is venue-neutral, gated on `venueAccountId != null`.
    - When venueAccountId is non-null, run the second fence SELECT beside the session fence:
      api_keys row by `.eq("user_id", user.id).eq("exchange", exchange).eq("venue_account_id", venueAccountId).maybeSingle()`,
      then (if hit) the strategy referencing it (`strategies.api_key_id = key.id`, same user).
      COPY THE POSTURE VERBATIM from the session arm: a failed fence read logs
      (scrubSeamError + code) and FALLS THROUGH to the RPC — the DB fence still dedups; it never
      500s. A hit returns `{ ok: true, strategy_id, api_key_id, deduped: true }` with
      NO_STORE_HEADERS — fail TOWARD the existing row, never write, never touch strategy_keys.
      The `deduped` field is ADDITIVE (absent on all other arms) — the session-fence arm stays
      byte-identical (the common case is already safe and gets no new UI).
    - Thread `p_venue_account_id: venueAccountId` into the create_wizard_strategy rpc() params.
    - ⛔ Response bodies and console logs must NEVER contain venueAccountId (V8/UI-SPEC). Add
      `api_key` / venueAccountId to the captureToSentry `secrets:` array on any arm that gains a
      capture (the login doubles as a credential slot even though the column value is non-secret
      by definition — err toward scrubbing in telemetry).
    23505 discrimination (:428-440):
    - Branch on the constraint name parsed from `error.message`/`error.details`:
      * contains `api_keys_user_exchange_venue_account_uniq` → the race arm of the new fence:
        re-run the fence SELECT and return the existing row (200, deduped: true); if unresolvable
        (orphaned key, no strategy), fall back to the existing 409 DRAFT_ALREADY_EXISTS — honest
        enough for a race, and no new WizardErrorCode is minted (the copy-table pins at
        EXPECTED_TABLE_SIZE 75 do not move).
      * contains `strategies_user_wizard_session_source_uniq` (or no name parseable) → the
        existing 409 DRAFT_ALREADY_EXISTS arm, unchanged.
      * any OTHER named constraint → log the constraint name + Sentry capture (existing tags
        shape), return the existing generic arm — fail loud, never mis-report.
    route.test.ts — extend per the VALIDATION rows (write RED-first where practical):
    - Same MT5 login + DIFFERENT wizard_session_id → existing {strategy_id, api_key_id} returned,
      deduped true; the RPC mock NOT called; no UPDATE issued (spy on the client double).
    - Fence SELECT error → falls through to RPC, console.error observed, no 500.
    - 23505 with the venue constraint name in details → 200 deduped (resolvable) / 409 (not).
    - 23505 with the session constraint name → DRAFT_ALREADY_EXISTS unchanged (byte-identical body).
    - Response bodies never contain the login string (assert on JSON.stringify of every arm).
    - Non-MT5 venue → venue arm inert: no api_keys fence SELECT beyond the session fence
      (call-count assertion), p_venue_account_id null in the rpc params.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/create-with-key/route.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "venue_account_id" src/app/api/strategies/create-with-key/route.ts` shows: one capture point, the fence eq, the rpc param — and `grep -c "=== 'mt5'"`-style venue literals do not exceed the single existing isMt5 site
    - Dedup arm returns the EXISTING ids and issues zero writes (test spy proves no insert/update/upsert on the dedup path)
    - Both 23505 branches asserted with distinct outcomes; unknown-constraint arm logs the name
    - No response body contains the login value (stringify assertion across arms)
  </acceptance_criteria>
  <done>One fence, two keys, identical posture; the DB race resolves toward the existing row; the wrong-fact 409 is impossible on the token-less path</done>
</task>

<task type="auto">
  <name>Task 2: TWIN-8 — constraint-aware 23505 on composite/add-key</name>
  <files>src/app/api/strategies/composite/add-key/route.ts, src/app/api/strategies/composite/add-key/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/composite/add-key/route.ts (:404 region — the undifferentiated 23505 arm, PATTERNS TWIN-8)
    - src/app/api/strategies/composite/add-key/route.test.ts (existing harness)
    - The Task 1 diff (mirror the discrimination helper — extract it if both routes can share one function without forcing a new module dance; if extracted, the shared util lives at src/lib/api/pgConstraintName.ts — the ONE such path declared in files_modified; no other new module)
  </read_first>
  <action>
    Close the twin, not just the instance: give add-key's 23505 arm the same constraint-name
    discrimination. The venue-identity constraint is UNREACHABLE here today (add_wizard_composite_key
    does not write venue_account_id — 154-03 deliberately did not touch that RPC; MT5 cannot be a
    composite member), so the arms are:
    * session/known constraints → existing DRAFT_ALREADY_EXISTS behavior byte-identical;
    * venue-identity constraint name → log loudly + Sentry (it appearing here means the composite
      path started writing identities — a fact worth an alarm, not a silent 409);
    * unknown constraint → log name + existing generic arm.
    State in a comment WHY the venue arm is alarm-only on this route (the recorded twin
    disposition GATE 2 requires). Extend route.test.ts: the three branches, with the existing
    23505 case's expected body unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/composite/add-key/route.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - add-key 23505 discrimination present; existing DRAFT_ALREADY_EXISTS body byte-identical for the session-constraint case (snapshot/equality assertion)
    - The comment names TWIN-8 and why venue-identity is alarm-only here
  </acceptance_criteria>
  <done>Both copies of the 23505 arm discriminate; the twin is closed with a recorded disposition</done>
</task>

<task type="auto">
  <name>Task 3: Dedup notice strip + residual record + SC-3 mutations</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx (:739 region — the create-with-key POST and its response handling)
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx (:886-897 the session-expired strip — the visual donor, byte-identical pattern)
    - .planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-UI-SPEC.md (§ State Contract 4 + Copywriting table — exact copy)
    - .planning/REQUIREMENTS.md (WIZCONT-02 entry ~line 627)
  </read_first>
  <action>
    ConnectKeyStep.tsx:
    - When the create-with-key response carries `deduped: true`, render ONE informational line in
      the flow the user is already in, cloning the session-expired strip pattern byte-identically:
      `rounded-md border border-border bg-page px-3 py-2 text-caption text-text-secondary`.
      Copy VERBATIM from the UI-SPEC: "These credentials are already connected. We continued with
      your existing strategy instead of creating a duplicate." NOT an ErrorEnvelope, NOT amber,
      NOT red — nothing failed. The wizard proceeds exactly as the fast fence path does today; the
      strip is the only UI delta, no confirmation, no fork.
    - ⛔ Never render the credential or account id anywhere in the strip or its aria text.
    - `data-testid="wizard-dedup-notice"` for the tests.
    ConnectKeyStep.test.tsx: deduped:true → strip renders with the verbatim copy (hand-typed
    needle, never imported), no ErrorEnvelope testid present, and the rendered HTML does not
    contain the submitted login string; deduped absent → no strip (positive + negative with the
    vacuity fence).
    REQUIREMENTS.md — WIZCONT-02 entry: mark the delivered scope and RECORD THE RESIDUAL with a
    dated note: "Residual (2026-08-12, Phase 154): only MT5 exposes a venue-confirmed stable
    non-secret account id at validation (ValidationResult in
    analytics-service/services/ingestion/adapter.py:98-123 carries no account-identity field for
    ccxt venues). Token-less re-connect dedup is live for MT5 only; ccxt venues and pre-existing
    api_keys rows (backfilled NULL) remain undeduplicable until a venue identity exists. Deferred
    by CONTEXT.md decision — cross-venue identity is explicitly out of scope." Edit, never
    full-file Write; wc -l sanity after.
    SC-3 falsifiability mutations (run now, record RED output in SUMMARY; 154-08 transcribes):
    - Drop the venue-identity arm from the fence (keep session key only) → the dedup route test RED. Restore.
    - Make the dedup path UPDATE the existing api_keys row → the membership/ciphertext-untouched
      test RED. Restore.
    (The third SC-3 mutation — index predicate made total — is a SQL-gate concern already pinned
    by 154-03's gate; note that in the SUMMARY rather than re-running DDL here.)
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx" --no-file-parallelism && grep -c "Residual (2026-08-12" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Strip renders only on deduped:true, with the verbatim UI-SPEC copy as a hand-typed test needle
    - No account id / login string in any rendered output (stringify assertion)
    - REQUIREMENTS.md carries the dated residual note inside the WIZCONT-02 entry
    - Two SC-3 mutation RED outputs pasted in SUMMARY, restored, `grep -rn MUTANT src/` → 0
  </acceptance_criteria>
  <done>The dedup outcome is visible, honest, small, and id-silent; the narrow scope is written down where the next reader will look</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → create-with-key | client-supplied login crosses; venue confirmation happens server-side in the same request |
| route → api_keys/strategies | user-scoped client + SECURITY DEFINER RPC |
| route → browser | dedup outcome crosses back; identity value must not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-154-06-A | Tampering (duplicate-row race) | dedup fence | mitigate | app fence + DB partial UNIQUE (154-03); 23505 race arm resolves toward the existing row |
| T-154-06-B | Tampering (silent overwrite orphaning strategy_keys) | dedup path | mitigate | dedup arm is read-only — test spy proves zero writes; ciphertext untouched |
| T-154-06-C | Information Disclosure (identity echo) | responses/logs/strip | mitigate | stringify assertions on every arm + the strip; venueAccountId in Sentry secrets array |
| T-154-06-D | Spoofing (fabricated identity evading dedup) | client-supplied login | mitigate | self-scoped only (user_id-led index); DB scrub trigger blocks direct writes; probe asserts the login venue-side during validate |
| T-154-06-E | Repudiation (wrong fact reported on 23505) | both routes' 23505 arms | mitigate | constraint-name discrimination on BOTH copies (TWIN-8), unknown names logged loudly |
| T-154-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- `npx vitest run src/app/api/strategies/create-with-key src/app/api/strategies/composite/add-key "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx" --no-file-parallelism` green; tsc clean
- SC-3 mutations observed RED and restored
</verification>

<success_criteria>
Re-connecting the same MT5 credentials with the session token lost resolves to the existing row at
both layers, reports the right fact on every arm, shows one honest neutral line, and the narrowness
of the win is recorded where it counts.
</success_criteria>

<output>
Create `.planning/phases/154-wizcont-stale-wizard-continuity-no-stale-screens/154-06-SUMMARY.md` when done
</output>
