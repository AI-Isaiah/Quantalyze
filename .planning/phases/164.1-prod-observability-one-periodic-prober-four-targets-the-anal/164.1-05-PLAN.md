---
phase: 164.1-prod-observability-one-periodic-prober-four-targets-the-anal
plan: 05
type: execute
wave: 4
depends_on: ["164.1-02", "164.1-04"]
files_modified:
  - .github/workflows/prod-prober.yml
  - src/__tests__/prod-prober-wiring.test.ts
autonomous: true
requirements: [PYAPI-06, CRON-OBS-01, CRON-DRIFT-01, MT5-WEDGE-OBS-01]
tags: [prod-prober, github-actions, workflow-pin, mode-identity]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-01 / D-02 / D-18: `.github/workflows/prod-prober.yml` exists as its OWN workflow with `schedule: cron: \"0 * * * *\"` and `workflow_dispatch:`; it is referenced by NOTHING in `ci.yml` (not in the `frontend` aggregator's needs list or result loop) and Railway's deploy gate is untouched."
    - "D-06 both halves: a workflow step with NO `if:` asserts every required secret and variable by NAME and exits 1 listing the missing ones (the migration-drift-check idiom), AND the script's own `credential-absent` defect is the half the self-test proves; the workflow contains no exit-0-on-unset shape and no step-level soft-fail key."
    - "Mode identity: the workflow runs the two bare commands from the script's own header — the self-test step is `run: node scripts/prod-prober/run.mjs --self-test` and the live step is `node scripts/prod-prober/run.mjs > \"$RUNNER_LOG\" 2>&1` with `status=$?` on the very next line — in the SAME job, self-test BEFORE live, nothing wrapping either."
    - "D-19: secrets reach the script through `env:` only (never `${{ }}` inside `run:`); `PROBER_POOLER_URL` is read from `supabase/.temp/pooler-url` after `supabase link` and masked with `::add-mask::` before it is exported; the password is `PGPASSWORD`-only inside the script."
    - "D-03: on a failing live step the workflow files or updates ONE dedup'd issue (label `prod-prober`) whose body is the defect table and remedy lines copied from the run log — the same text the summary printed — and the job's exit status is the script's (the LOCKED posture; plan 06 measures the deploy-skip hazard and may flip exactly one documented line)."
    - "`workflow_dispatch` inputs: `mode` (choice `probe` | `capture-manifest`, default `probe`) and `arm` (string, default empty); `capture-manifest` runs `--capture-manifest --out \"$RUNNER_TEMP/cron-manifest.json\"` and uploads it as artifact `cron-manifest` (never commits); a non-empty `arm` runs `--arm <name>` (exit 2 by design)."
    - "The vitest pin `src/__tests__/prod-prober-wiring.test.ts` asserts: the exact bare commands, self-test before live in one job, no soft-fail tokens, no exit-0-after-credential-test shape, `permissions: contents: read / issues: write`, `concurrency: group: prod-prober` with `cancel-in-progress: false`, an explicit `timeout-minutes`, the C-0293 SHA-pin + persist-credentials rules, `ARMS_FLOOR === 4`, a hand-typed `EXPECTED_DEFECT_KINDS` equal to the runner's list member for member, `SELF_TEST_SCENARIOS === 50` matching the counted headers in the source (13 + 9 + 17 + 11 — the enumeration is in plan 03 Task 2 and plan 04 Task 2), an EMPTY `uncovered` kind set, the issue step gated on `inputs.arm == ''`, and NO occurrence of the Railway PROJECT-token variable name anywhere under `scripts/prod-prober/` (the seam must hand the CLI the workspace token under `RAILWAY_API_TOKEN` — plan 04) — each predicate calibrated on a mutated copy of the text."
  artifacts:
    - ".github/workflows/prod-prober.yml"
    - "src/__tests__/prod-prober-wiring.test.ts"
  key_links:
    - "workflow: checkout (SHA-pinned, persist-credentials false) → assert credentials → setup-node 22 → apt psql → supabase link → export masked PROBER_POOLER_URL → install railway CLI (pinned release + checksum) → self-test → live run (log to RUNNER_TEMP) → on failure AND a full (non-narrowed) run: issue → exit status"
    - "wiring test reads the workflow text and the runner module; mutated copies calibrate every predicate"
  prohibitions:
    - { requirement_id: CRON-OBS-01, category: safety, status: resolved, verification: test, statement: "MUST NOT add prod-prober to ci.yml's frontend aggregator or to any job Railway's wait-for-CI consults for the analytics deploy — a red prober must never block the deploy that fixes it (D-18)" }
    - { requirement_id: PYAPI-06, category: safety, status: resolved, verification: test, statement: "MUST NOT wrap either run command (no npx, no npm run, no pipe deciding the exit, no soft-fail key, no OR-true) — local and CI invocations must be byte-identical in mode" }
    - { requirement_id: CRON-DRIFT-01, category: privacy, status: resolved, verification: test, statement: "MUST NOT interpolate a secret or the pooler URL into a run: line or print either; secrets go through env: and the pooler URL is masked before export" }
    - { requirement_id: MT5-WEDGE-OBS-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT install the Railway CLI from an unpinned installer or from npm without the legitimacy gate — the workflow downloads the v4.36.1 release asset and verifies its sha256 against a constant recorded in the workflow" }
    - { requirement_id: CRON-DRIFT-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT commit the captured manifest from the workflow — capture-manifest mode uploads an artifact for human review; the commit is a reviewed act in plan 06" }
---

<objective>
Ship the prober as its own hourly GitHub Actions workflow with the hard-fail credential step, the
self-test-then-live ordering, masked pooler URL, a pinned Railway CLI, the dedup'd auto-issue, and
a `capture-manifest` dispatch mode; and pin all of it from vitest so the wiring cannot soften.

Purpose: the script's contract is proven by its self-test; the workflow is where mode identity is
lost (a wrapper, a pipe, a soft-fail key, an exit 0 after a credential test — every one of those
has happened in this repo's other workflows). The vitest pin holds the workflow to the script.

Output: `.github/workflows/prod-prober.yml` and `src/__tests__/prod-prober-wiring.test.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-CONTEXT.md
@.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-RESEARCH.md
@.github/workflows/analytics-deploy-verify.yml
@.github/workflows/migration-drift-check.yml
@src/__tests__/mutation-runner-floors.test.ts
@src/__tests__/critical-regressions.test.ts

Also read before starting (created by earlier plans, so not `@`-referenced): the plan-04 SUMMARY
in this phase directory and `scripts/prod-prober/run.mjs` (its header is what the workflow pastes).

Decision register: plan 01's context block (D-01..D-20). This plan implements D-01, D-02, D-03,
D-18 (the locked posture, pending plan 06's measurement), and the workflow half of D-06 and D-19.

## Artifacts this phase produces (this plan's share)

| Artifact | Symbol |
|---|---|
| `.github/workflows/prod-prober.yml` | job `probe`; inputs `mode`, `arm`; env names `ANALYTICS_BASE_URL`, `ANALYTICS_SERVICE_KEY`, `SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD`, `SUPABASE_PROJECT_REF`, `PROBER_POOLER_URL`, `RAILWAY_API_TOKEN`, `RAILWAY_PROJECT_ID`, `RAILWAY_MT5_SERVICE`, `RAILWAY_ENVIRONMENT`; constants `RAILWAY_CLI_VERSION = 4.36.1`, `RAILWAY_CLI_SHA256`; artifact `cron-manifest`; issue label `prod-prober` |
| `src/__tests__/prod-prober-wiring.test.ts` | `EXPECTED_DEFECT_KINDS` (hand-typed, 20), `WORKFLOW_PATH`, `RUNNER_PATH`, the calibrated predicates |

The full phase artifact table is in plan 01's context block.
</context>

<tasks>

<task type="auto">
  <name>Task 1: `.github/workflows/prod-prober.yml` — hourly, own workflow, hard-fail credentials, self-test then live, masked DSN, pinned Railway CLI, dedup'd issue, capture-manifest mode</name>
  <reversibility rating="costly">Adding a scheduled workflow that reaches PROD hourly is a standing production touch; undoing it is deleting one file, but every downstream reader (the issue label, the wiring test, the runbook sentence in CLAUDE.md) references its name and step shape.</reversibility>
  <files>.github/workflows/prod-prober.yml</files>
  <read_first>
    - .github/workflows/analytics-deploy-verify.yml:17-24 (the deploy-skip hazard header — quote it; plan 06 measures it)
    - .github/workflows/analytics-deploy-verify.yml:38-51 (schedule / permissions / concurrency shape)
    - .github/workflows/analytics-deploy-verify.yml:56-72 (explicit timeout-minutes reasoning; SHA-pinned checkout with persist-credentials false)
    - .github/workflows/analytics-deploy-verify.yml:76 (the public analytics base URL default)
    - .github/workflows/analytics-deploy-verify.yml:137-192 (the dedup'd github-script issue step — env: not interpolation)
    - .github/workflows/migration-drift-check.yml:91-110 (the hard-fail secrets step with NO if:)
    - .github/workflows/migration-drift-check.yml:111-122 (supabase/setup-cli pinned to 2.98.2; supabase link with the three env names)
    - .github/workflows/ci.yml:1239 and .github/workflows/ci.yml:1263 (the two bare mutation-runner commands: mode identity, status captured on the next line)
    - .github/workflows/ci.yml:2156 (apt-get install postgresql-client)
    - .github/workflows/phase-19-stability.yml:54-58 (the shape that must NOT appear)
    - src/__tests__/critical-regressions.test.ts:540-600 (every workflow is discovered dynamically and held to SHA-pin / permissions / persist-credentials rules)
  </read_first>
  <action>
    Create `.github/workflows/prod-prober.yml`. Header comment (10-25 lines): what the four arms measure, that this is deliberately a SEPARATE workflow outside `ci.yml` (D-18), a verbatim quote of analytics-deploy-verify.yml lines 17-24 with the sentence `POSTURE LINE — measured by plan 164.1-06; see the final step` marking the one line plan 06 may flip, the credential inventory by NAME (two secrets new this phase: `ANALYTICS_SERVICE_KEY`, `RAILWAY_API_TOKEN` — workspace scope, a project token is refused by railway ssh; `RAILWAY_SSH_PRIVATE_KEY` was RETIRED by measurement 2026-09-05 and must not be added), and the injection-safety note (no `github.event.*` reaches a `run:`; every secret is `env:`).

    Triggers: `schedule: - cron: "0 * * * *"` and `workflow_dispatch:` with inputs `mode` (type choice, options `probe` and `capture-manifest`, default `probe`, description naming what each does) and `arm` (type string, default empty, description: narrowed diagnostic — exits 2 by design). `permissions: contents: read, issues: write`. `concurrency: group: prod-prober, cancel-in-progress: false`. One job `probe` on `ubuntu-latest`, `timeout-minutes: 20` with a comment deriving it (self-test seconds + four arms, the ssh arm capped at 120 s, apt + supabase link + CLI download ~2 min; the 360-min default is itself a hazard per analytics-deploy-verify.yml).

    Steps, in order, every action SHA-pinned by copying the exact pins already in the repo: (1) `actions/checkout` with `persist-credentials: false`; (2) `Assert credentials are configured (D-06 — a skip is not a pass)` with NO `if:`, env block naming `ANALYTICS_SERVICE_KEY`, `RAILWAY_API_TOKEN`, `SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD` from secrets and `SUPABASE_PROJECT_REF`, `RAILWAY_PROJECT_ID`, `RAILWAY_MT5_SERVICE`, `RAILWAY_ENVIRONMENT` from vars; the run body accumulates a `missing` list and exits 1 with `::error::` lines that say HARD FAILURE, not a skip, then prints one `credential: <NAME> present` line per name (names only) — copy the migration-drift-check idiom; (3) `actions/setup-node` at node-version 22; (4) `sudo apt-get install -y --no-install-recommends postgresql-client` (with `apt-get update -qq` first); (5) `supabase/setup-cli` pinned to 2.98.2, then `supabase link --project-ref "$SUPABASE_PROJECT_REF"` with the three Supabase env names, then read `supabase/.temp/pooler-url`, FAIL with exit 1 if the file is missing or empty (never fall back to a hand-built DSN), `echo "::add-mask::$url"`, and append `PROBER_POOLER_URL=<url>` to `$GITHUB_ENV`; (6) install the Railway CLI from the GitHub release: `RAILWAY_CLI_VERSION: "4.36.1"` and `RAILWAY_CLI_SHA256: "<sha256 of railway-v4.36.1-x86_64-unknown-linux-musl.tar.gz>"` as step env — the executor obtains the asset name list with `gh release view v4.36.1 -R railwayapp/cli --json assets`, downloads the x86_64 linux musl tarball once with `gh release download`, computes its sha256 locally with `shasum -a 256`, and pins that hex here; the step downloads with `curl -fsSL -o`, verifies `sha256sum -c`, exits 1 on mismatch, extracts to `$RUNNER_TEMP/railway`, adds it to `$GITHUB_PATH`, and prints `railway --version` (must print 4.36.1 — the version CONTEXT measured, where `--native` is opt-in and no SSH key is needed); (7) `Self-test (fixtures only, no network)` — `run: node scripts/prod-prober/run.mjs --self-test` bare, with a MODE IDENTITY comment pointing at the vitest pin; (8) `Probe production (all four arms, aggregate, exit at the end)` — `if: inputs.mode != 'capture-manifest'`, env block with all ten names (`ANALYTICS_BASE_URL: ${{ vars.ANALYTICS_BASE_URL || 'https://quantalyze-analytics-production.up.railway.app' }}`, the secrets, the vars, `PROBER_POOLER_URL` from `$GITHUB_ENV` automatically) plus `RUNNER_LOG: ${{ runner.temp }}/prod-prober.log`; run body `set -uo pipefail`, then EITHER `node scripts/prod-prober/run.mjs --arm "$ARM" > "$RUNNER_LOG" 2>&1` when `ARM` (from `inputs.arm` via env) is non-empty OR the bare `node scripts/prod-prober/run.mjs > "$RUNNER_LOG" 2>&1`, then `status=$?` on the very next line, `cat "$RUNNER_LOG"`, write the defect table (the lines from `❌` to the end of the table) into `$GITHUB_STEP_SUMMARY`, and `exit $status` — this `exit $status` is the POSTURE LINE; (9) `Capture the cron manifest (read-only; artifact, never a commit)` — `if: inputs.mode == 'capture-manifest'`, same env, `node scripts/prod-prober/run.mjs --capture-manifest --out "$RUNNER_TEMP/cron-manifest.json"`, `status=$?` next line, `exit $status`; then `actions/upload-artifact` (pinned) with name `cron-manifest`, path `${{ runner.temp }}/cron-manifest.json`, `if-no-files-found: error`, retention 7 days; (10) `Open or update the prod-prober issue` — `if: failure() && steps.<probe step id>.outcome == 'failure' && inputs.arm == ''` (the third clause is load-bearing: a narrowed `--arm` dispatch exits 2 BY DESIGN even when green, `failure()` would fire, and an auto-issue reading "0 defect(s)" would be filed; on a `schedule` trigger `inputs.arm` is empty so the guard is inert there — put this reasoning in a comment on the step), `actions/github-script` pinned, env `RUN_ID`, `REPO_NAME`, `HEAD_SHA`, `LOG_PATH`; the script reads the log file with `fs.readFileSync`, extracts the defect table (from the `❌` line through the last indented `remedy:` line) and the `arms:` line, dedups on label `prod-prober` (comment on the open issue if one exists, else create with title `prod-prober: <n> defect(s) on <date>`, labels `prod-prober`, `p1`), and the body is those verbatim lines plus the run URL — no other values. Every value the script needs arrives through `env:`; nothing is `${{ }}`-interpolated inside `script:` or `run:`.

    Do NOT add this workflow to `ci.yml` in any form. Do NOT add a step that pushes, commits, or writes to Supabase or Railway. Do NOT add any step-level soft-fail key or an OR-true after either node command.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts -t "C0293" 2>&1 | tail -12</automated>
    <fails_when>non-zero exit, a "FAIL" line naming prod-prober.yml (unpinned action, missing permissions, or a checkout without persist-credentials false), or "0 passed"</fails_when>
    <automated>set -o pipefail; f=.github/workflows/prod-prober.yml; grep -q '^    - cron: "0 \* \* \* \*"' "$f" && grep -c 'run: node scripts/prod-prober/run.mjs --self-test' "$f" | grep -qx 1 && grep -c 'node scripts/prod-prober/run.mjs > "$RUNNER_LOG" 2>&1' "$f" | grep -qx 1 && ! grep -q 'continue-on-error' "$f" && ! grep -q '|| true' "$f" && ! grep -q 'prod-prober' .github/workflows/ci.yml && grep -c 'add-mask' "$f" | grep -qx 1 && grep -c 'RAILWAY_SSH_PRIVATE_KEY' "$f" | grep -qx 1 && grep -c "inputs.arm == ''" "$f" | grep -qx 1</automated>
    <fails_when>non-zero exit: the schedule, either bare command, the mask, the retired-secret warning sentence, or the issue step's narrowed-dispatch guard is absent, a soft-fail token is present, or ci.yml mentions prod-prober. (The three zero-count clauses are spelled `! grep -q`, never as a `grep -c` count piped into a match for zero: under `set -o pipefail` a zero count exits 1 from `grep -c` and the pipeline fails exactly when the workflow is correct. Keep `pipefail` — it protects the `grep -qx 1` clauses beside them.)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'uses:' .github/workflows/prod-prober.yml` shows every `uses:` with a 40-hex SHA and a trailing version comment, and each SHA equals one already present in `ci.yml`, `migration-drift-check.yml` or `analytics-deploy-verify.yml`
    - `grep -n 'if:' .github/workflows/prod-prober.yml` shows NO `if:` on the credential-assert step, and `grep -c '::warning' .github/workflows/prod-prober.yml` prints `0`
    - `grep -n 'exit 0' .github/workflows/prod-prober.yml` prints nothing (no path exits 0 explicitly; the live step exits with `$status`)
    - `grep -n 'RAILWAY_CLI_SHA256' .github/workflows/prod-prober.yml` shows a 64-hex value, and the SUMMARY records the `shasum -a 256` command and output that produced it
    - `grep -c 'POSTURE LINE' .github/workflows/prod-prober.yml` prints `2` (the header sentence and the marker beside `exit $status`)
    - `grep -n '\${{' .github/workflows/prod-prober.yml` shows interpolation only in `env:`, `with:`, `if:` and the `path:` of upload-artifact — never inside a `run:` body or the github-script `script:`
    - the workflow parses: `node -e 'const y=require("js-yaml"); y.load(require("fs").readFileSync(".github/workflows/prod-prober.yml","utf8")); console.log("yaml ok")'` prints `yaml ok` (js-yaml is already in node_modules as a transitive dependency; if it is not, use `npx --no-install yaml`'s parse or python3 with the stdlib-free fallback of `ruby -ryaml`; do not install anything)
  </acceptance_criteria>
  <done>The workflow exists as its own hourly job with the hard-fail credential step, the two bare commands in order, a masked pooler URL, a checksum-pinned Railway CLI, the dedup'd issue, and a capture-manifest artifact mode; C0293 invariants pass; nothing in ci.yml references it.</done>
</task>

<!-- planner-discipline-allow: prod-prober -->

<task type="auto" tdd="true">
  <name>Task 2: `src/__tests__/prod-prober-wiring.test.ts` — pin the workflow to the script, calibrated on mutated copies</name>
  <files>src/__tests__/prod-prober-wiring.test.ts</files>
  <read_first>
    - src/__tests__/mutation-runner-floors.test.ts:596-725 (the four blocks to copy: bare-command predicate + calibration, ordering in one job, no softening, kind coverage with an EXACT uncovered list)
    - src/__tests__/mutation-runner-floors.test.ts:669-685 (the source extractor over selfTest's body)
    - .github/workflows/prod-prober.yml (Task 1)
    - scripts/prod-prober/run.mjs (exports ARMS_FLOOR, DEFECT_KINDS, SELF_TEST_SCENARIOS, ARMS)
    - .github/workflows/phase-19-stability.yml:54-58 (the shape the negative predicate must catch — build the calibration mutant from it)
  </read_first>
  <behavior>
    - bare commands: `bareRunLines(WORKFLOW_TEXT)` equals exactly `["run: node scripts/prod-prober/run.mjs --self-test"]` for the self-test form, and the live line `node scripts/prod-prober/run.mjs > "$RUNNER_LOG" 2>&1` appears exactly once with `status=$?` on the next non-empty line; CALIBRATION: removing the self-test line from a copy makes the predicate return `[]`.
    - ordering: the self-test line's index is less than the live line's index and both are inside the single `probe:` job block.
    - nothing softens: the job text contains no step-level soft-fail key, no OR-true after a node command, no `exit 0` anywhere, no `::warning` line; CALIBRATION: injecting the phase-19 rc-2 block into a copy makes the negative predicate fail by name.
    - credential step: has no `if:` line between its `- name:` and its `run:`; names all eight credential identifiers; CALIBRATION: inserting an `if:` line into a copy flips it.
    - workflow policy: `schedule` cron is `0 * * * *`; `permissions` has `contents: read` and `issues: write`; `concurrency.group` is `prod-prober` with `cancel-in-progress: false`; `timeout-minutes` is present and at most 30; `ci.yml` contains no `prod-prober`; the issue step's `if:` contains `inputs.arm == ''` (a narrowed dispatch exits 2 by design and must not file an issue); CALIBRATION: deleting that clause from a copy flips the predicate.
    - token slot: no file under `scripts/prod-prober/` (recursive, `.mjs` and `.json`) contains the Railway PROJECT-token variable name — spell it in the test as the string the CLI's own "Unauthorized" message names for a project token, i.e. `"RAILWAY_" + "TOKEN"` built by concatenation so the test file itself cannot be a false hit — while `seams.mjs` DOES contain `RAILWAY_API_TOKEN` inside `realSshRunner`; CALIBRATION: a copy of seams.mjs with the child-env key renamed to the project slot flips the predicate.
    - runner constants: `ARMS_FLOOR === 4`, `ARMS.length === 4`, `SELF_TEST_SCENARIOS === 50`, the source contains exactly 50 `=== SELF-TEST k/50:` headers numbered 1..50 contiguously.
    - kinds: hand-typed `EXPECTED_DEFECT_KINDS` (20 names, sorted) equals `[...DEFECT_KINDS].sort()`; the extractor over `selfTest()`'s source yields an `uncovered` array equal to `[]`; `DEFECT_KINDS.length` floor of 15 so an emptied list cannot pass.
    - masking: the workflow contains `::add-mask::` before the `PROBER_POOLER_URL` export, and no `run:` body contains `${{ secrets.`.
  </behavior>
  <action>
    Create `src/__tests__/prod-prober-wiring.test.ts` importing `readFileSync` and dynamically importing `../../scripts/prod-prober/run.mjs` (vitest resolves the `.mjs` the same way `mutation-runner-floors.test.ts` imports its runner — copy that import form). Define `WORKFLOW_PATH`, `RUNNER_PATH`, `CI_PATH` relative to `process.cwd()`. Implement every predicate as a function over ARBITRARY TEXT and give each a `CALIBRATION` `it` that mutates a copy (assert the copy differs from the original, then assert the predicate flips) — a predicate only ever applied to the passing input is not evidence. Structure the file as four `describe` blocks mirroring the analog: `[164.1-05] mode identity`, `[164.1-05] nothing softens a failure`, `[164.1-05] workflow policy`, `[164.1-05] kinds and floors`. In the kinds block, spell the hand-typed roster in full (never `[...DEFECT_KINDS]`), and use `noDefectOfKind`-style absence through the list — the extractor must scan `selfTest()`'s SOURCE from `function selfTest()` to the end of the file and collect `kind === "<k>"` matches. Add a header docblock stating the defect this file catches (a wrapper or a soft-fail key in CI turning the prober into `phase-19-stability.yml`'s rc-2 shape) and the standing rule that a change to the counted scenario set is a one-edit renumber across `run.mjs` and this file.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&1 | tail -12 && npx tsc --noEmit</automated>
    <fails_when>non-zero exit, a "FAIL" line, fewer than 12 passed, or any tsc diagnostic</fails_when>
    <automated>set -o pipefail; cp .github/workflows/prod-prober.yml /tmp/prod-prober.yml.bak && sed -i.orig 's#^\( *\)run: node scripts/prod-prober/run.mjs --self-test#\1run: node scripts/prod-prober/run.mjs --self-test || true#' .github/workflows/prod-prober.yml && (npx vitest run src/__tests__/prod-prober-wiring.test.ts >/tmp/wiring-red.log 2>&1; echo "exit=$?" | tee -a /tmp/wiring-red.log | grep -q 'exit=1'); r=$?; cp /tmp/prod-prober.yml.bak .github/workflows/prod-prober.yml; rm -f .github/workflows/prod-prober.yml.orig; shasum -a 256 /tmp/prod-prober.yml.bak .github/workflows/prod-prober.yml | awk '{print $1}' | uniq | wc -l | grep -qx 1 && test "$r" -eq 0</automated>
    <fails_when>non-zero exit: the wiring test stayed GREEN with a softened self-test line (the pin cannot fail), or the workflow was not restored byte-identically (two distinct hashes)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CALIBRATION' src/__tests__/prod-prober-wiring.test.ts` is at least 6
    - `grep -c "inputs.arm == ''" src/__tests__/prod-prober-wiring.test.ts` is at least 1 (the narrowed-dispatch guard is pinned, not just present)
    - `grep -c '"credential-absent"' src/__tests__/prod-prober-wiring.test.ts` prints `1` and the hand-typed roster contains all 20 names (`grep -c '^\s*"[a-z0-9-]*",\?$' src/__tests__/prod-prober-wiring.test.ts` is at least 20)
    - `grep -n 'toEqual(\[\])' src/__tests__/prod-prober-wiring.test.ts` shows the `uncovered` assertion
    - `npx vitest run src/__tests__/prod-prober-wiring.test.ts` reports 0 skipped
    - the SUMMARY records the RED line observed with the softened self-test line and the identical pre/post sha256 of the workflow
  </acceptance_criteria>
  <done>The wiring test pins the exact commands, the ordering, the absence of every softening shape, the policy fields, the runner constants and the full kind coverage — each predicate calibrated on a mutant — and it was observed RED when the self-test line was softened, then the workflow restored byte-identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → PROD Postgres / analytics-service / Railway relay | hourly, with three production credentials in the job env |
| workflow log / job summary / auto-issue → public | everything printed is world-readable |
| GitHub release asset → runner | a third-party binary executed with the workspace token in its env |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.1-21 | Information disclosure | pooler URL in `$GITHUB_ENV` / logs | high | mitigate | `::add-mask::` before export; the script never prints it; secrets only via `env:` |
| T-164.1-22 | Tampering | Railway CLI binary | high | mitigate | pinned release version + sha256 constant verified with `sha256sum -c`, exit 1 on mismatch; `railway --version` printed and asserted |
| T-164.1-23 | Elevation | workspace token + `issues: write` | medium | mitigate | `permissions` minimal (`contents: read`, `issues: write`); token only in the ssh step's child env; checkout with `persist-credentials: false` |
| T-164.1-24 | Denial of service | a red prober blocking Railway deploys (deploy-skip hazard) | high | mitigate | own workflow outside ci.yml (D-18); the POSTURE LINE is marked and plan 06 measures the hazard before the posture is final; the wiring test pins the current posture |
| T-164.1-25 | Repudiation | a softened CI invocation | high | mitigate | vitest pin on the exact bare commands, ordering, and absence of soft-fail shapes, calibrated on mutants |
| T-164.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install: `postgresql-client` via apt (the ci.yml idiom), Supabase CLI via the pinned setup-cli action, Railway CLI via a checksum-verified release asset |
</threat_model>

<verification>
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/critical-regressions.test.ts` green; `npx tsc --noEmit` clean; `npm run lint` clean (the planning-hygiene gate scans the new files for absolute home paths).
- `grep -c prod-prober .github/workflows/ci.yml` prints 0.
</verification>

<success_criteria>
- The prober is wired hourly as its own workflow with mode identity to the script, a hard-fail credential step, and a pin that goes red on any softening — success criterion 1's "never a SKIP" holds at the workflow layer as well as the script layer.
</success_criteria>

<output>
Create `.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-05-SUMMARY.md` when done. Record the pinned Railway CLI sha256 and how it was obtained, the wiring test's RED observation on the softened copy, and the exact `exit $status` POSTURE LINE number in the workflow.
</output>