---
phase: 164.1-prod-observability-one-periodic-prober-four-targets-the-anal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/analytics-client.ts
  - src/lib/analytics-client.test.ts
  - analytics-service/main.py
  - analytics-service/tests/test_verify_service_key_middleware.py
  - analytics-service/tests/test_service_key_log_companion.py
  - analytics-service/tests/test_secret_misconfig_signal.py
  - src/lib/seam-venue-vocabulary.invariant.test.ts
  - src/lib/wizardErrors.ts
autonomous: true
requirements: [PYAPI-06]
tags: [pyapi-06, service-key, seam-config-error, neuter-red-restore]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-09: with `ANALYTICS_SERVICE_KEY` unset, every analytics wrapper rejects with `SeamConfigError` whose message names `ANALYTICS_SERVICE_KEY` and never its value, and the fetch mock is NOT called — the request is refused before it leaves the process."
    - "D-09: the throw sits ABOVE the `try` beside `mintTenantClaim`, so it is never rewritten as an upstream outage; `X-Service-Key` is now emitted unconditionally (no `SERVICE_KEY &&` spread)."
    - "D-10: an absent `X-Service-Key` on a guarded route returns 401 with `detail.code === \"SERVICE_KEY_ABSENT\"`; a WRONG key still returns 401 with `{\"detail\": \"Unauthorized\"}`; `/health`, `/internal/*` and `/process-key` are untouched."
    - "D-11: the `if provided:` block is byte-identical; an absent header emits ZERO `service_key.*` log events and NO Sentry capture (`faults == [\"unset\", \"mismatched\"]` still holds)."
    - "D-12: BOTH halves were neutered, observed RED, and restored byte-identically (shasum before == shasum after), and the RED output lines are recorded in the SUMMARY."
    - "`SERVICE_KEY_ABSENT` has an explicit TypeScript disposition (an entry in `VENUE_WIRE_CODES_WITHOUT_VERDICT` with a substantive reason) and appears in the hand-typed `EXPECTED_EMITTED_CODES` roster, so `seam-venue-vocabulary.invariant.test.ts` stays green by design, not by accident."
    - "No test file in the repo still pins the absent-header body to `{\"detail\": \"Unauthorized\"}` (`grep -rn` over `analytics-service/tests` for that literal beside an absent-header request returns only the wrong-key cases)."
  artifacts:
    - "src/lib/analytics-client.ts — the SeamConfigError refusal above the try"
    - "src/lib/analytics-client.test.ts — the [164.1-02] refusal describe block + the key set in every beforeEach that drives the real seam"
    - "analytics-service/main.py — the `if not provided:` branch returning service_error_response(401, \"SERVICE_KEY_ABSENT\", ...)"
    - "analytics-service/tests/test_verify_service_key_middleware.py — the absent-vs-wrong distinction test"
  key_links:
    - "analytics-client.ts request path: mintTenantClaim (refuses on empty token) → NEW service-key refusal → try { resilientFetch(...) }"
    - "main.py verify_service_key: unset arm (500 SERVICE_KEY_UNCONFIGURED) → NEW absent arm (401 SERVICE_KEY_ABSENT, no log, no capture) → mismatch arm (401 plain, capture only if provided)"
    - "seam-venue-vocabulary.invariant.test.ts derives every emitted Python code and requires a disposition in wizardErrors.ts — SERVICE_KEY_ABSENT must appear in both the roster and the exemption map"
  prohibitions:
    - { requirement_id: PYAPI-06, category: safety, status: resolved, verification: test, statement: "MUST NOT dedent the Sentry capture or the mismatch log line out of `if provided:` — an absent header is internet background noise and capturing it buries the real signal (the zero-event pin in test_service_key_log_companion.py is the mechanical check)" }
    - { requirement_id: PYAPI-06, category: privacy, status: resolved, verification: test, statement: "MUST NOT include the service-key VALUE in the SeamConfigError message, any log line, or any test assertion — the error names the env var only" }
    - { requirement_id: PYAPI-06, category: safety, status: resolved, verification: judgment, statement: "MUST NOT change the public contract for unauthenticated callers: absent stays 401 (not 400), and no other status or path changes" }
    - { requirement_id: PYAPI-06, category: safety, status: resolved, verification: judgment, statement: "MUST NOT add a module-load assertion for ANALYTICS_SERVICE_KEY — local dev and any build step importing the module without the secret must keep working; the refusal is at call time only" }
---

<objective>
Close PYAPI-06 at BOTH halves so the client can no longer send an anonymous request and the server
can no longer discard the one absent-header case that is our own outage — each half guarded by a
test that goes RED when its guard is deleted.

Purpose: the two halves composed into a blind spot that let a production service-key mismatch run
with zero signal (TODOS 0.04). The prober's PYAPI-06 arm (plan 01) asserts the Python half is LIVE
in PROD by expecting `SERVICE_KEY_ABSENT` on an absent header; until this plan is deployed that arm
reports `pyapi06-absent-uncoded`, which is the honest reading.

Output: the `SeamConfigError` refusal in `src/lib/analytics-client.ts`, the `SERVICE_KEY_ABSENT`
branch in `analytics-service/main.py`, the moved test pins, the vocabulary roster/exemption rows,
and two recorded neuter → RED → restore proofs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-CONTEXT.md
@.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-PATTERNS.md
@src/lib/analytics-client.ts
@src/lib/resilient-fetch.ts
@analytics-service/main.py
@analytics-service/services/error_contract.py

Decision register: see plan 01's context block (D-01..D-20). This plan implements D-09, D-10,
D-11, D-12.

## Artifacts this phase produces (this plan's share)

| Artifact | Symbol |
|---|---|
| `src/lib/analytics-client.ts` | the `SeamConfigError` throw naming `ANALYTICS_SERVICE_KEY`, placed above the `try` |
| `analytics-service/main.py` | machine code string `SERVICE_KEY_ABSENT` (401, `retryable=False`, `detail="Unauthorized"`) |
| `src/lib/wizardErrors.ts` | `VENUE_WIRE_CODES_WITHOUT_VERDICT` entry for `SERVICE_KEY_ABSENT` |
| `src/lib/seam-venue-vocabulary.invariant.test.ts` | `EXPECTED_EMITTED_CODES` gains `SERVICE_KEY_ABSENT` |

The full phase artifact table is in plan 01's context block.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TS half — refuse before the fetch when ANALYTICS_SERVICE_KEY is absent (D-09), proven neuter → RED → restore (D-12)</name>
  <reversibility rating="reversible">One guard in one function; removing it is a one-line revert with no callers to update.</reversibility>
  <files>src/lib/analytics-client.ts, src/lib/analytics-client.test.ts</files>
  <read_first>
    - src/lib/analytics-client.ts:40-50 (SERVICE_KEY captured at module scope from ANALYTICS_SERVICE_KEY)
    - src/lib/analytics-client.ts:424-427 (why the tenant claim is minted BEFORE the try)
    - src/lib/analytics-client.ts:447-450 (the mintTenantClaim call — the refusal goes directly below it)
    - src/lib/analytics-client.ts:457-469 (the try, the header block, and the conditional spread at :466)
    - src/lib/analytics-client.ts:511-517 (the catch arm that already rethrows SeamConfigError unwrapped — do not touch)
    - src/lib/resilient-fetch.ts:1830-1835 (the SeamConfigError class; already imported at analytics-client.ts:13-19)
    - src/lib/analytics-client.test.ts:45-53 (the beforeEach shape shared by the eleven blocks that set only INTERNAL_API_TOKEN)
    - src/lib/analytics-client.test.ts:882-891 (the one block that already sets ANALYTICS_SERVICE_KEY BEFORE the dynamic import — the ordering rule)
  </read_first>
  <behavior>
    - Test A (the guard): with `ANALYTICS_SERVICE_KEY` deleted from `process.env`, `vi.resetModules()`, dynamic-import the module, call one wrapper (e.g. the simulator or bridge wrapper the file already exercises) with a stubbed global fetch → rejects with an error whose `name === "SeamConfigError"`, whose message contains `ANALYTICS_SERVICE_KEY` and does NOT contain the sentinel value used elsewhere in the file, and the fetch stub was called 0 times.
    - Test B (the header is unconditional): with the key set to a sentinel, the wrapper's outgoing headers carry `X-Service-Key` equal to the sentinel (this may already exist in the [140.2-09] block — reuse or extend it, do not duplicate).
    - Test C (blast radius): every describe block that drives the REAL seam still passes once its beforeEach sets the key.
  </behavior>
  <action>
    In `src/lib/analytics-client.ts`, directly after the `mintTenantClaim(...)` call and BEFORE `let res: SeamResponse;` / the `try`, add a call-time refusal: when `SERVICE_KEY` is empty, throw `new SeamConfigError(...)` with a message that (1) starts with `[analytics-client]`, (2) names `ANALYTICS_SERVICE_KEY`, (3) says the wrapper is refusing to send an unauthenticated guarded request and that this is a deployment misconfiguration rather than an analytics-service failure, and (4) never includes the value. Comment it per D-09: thrown ABOVE the try, deliberately, mirroring the tenant-claim reasoning at lines 424-427, so it is not rewritten as "not reachable"; and per CONTEXT no module-load assertion (a build or local dev importing the module without the secret must keep working — the refusal is at call time). Then replace the conditional header spread at line 466 with an unconditional `"X-Service-Key": SERVICE_KEY` entry (the spread was the silent omission; TODOS 0.04). Do not touch the catch arm at 511-517.

    In `src/lib/analytics-client.test.ts`: add `process.env.ANALYTICS_SERVICE_KEY = <a file-level sentinel constant>` beside the `INTERNAL_API_TOKEN` line in EACH of the eleven beforeEach blocks whose token line sits at 52, 245, 451, 580, 682, 1429, 1697, 1952, 2021, 2163 and 2392 — set BEFORE `vi.resetModules()` + the dynamic import per the ordering note at 882-891 (SERVICE_KEY is captured at module scope). Leave the [140.2-09] block alone. Add a new describe block titled `[164.1-02 / PYAPI-06] refuses before the fetch when ANALYTICS_SERVICE_KEY is absent` implementing Tests A and B above, with a comment naming the neuter proof: delete the throw → Test A goes RED because fetch is called and no error is thrown.

    Neuter → RED → restore (D-12), recorded in the SUMMARY: take a byte backup `cp src/lib/analytics-client.ts "$RUNNER_TMP_OR_SCRATCH/analytics-client.ts.bak"` and `shasum -a 256` it; delete ONLY the throw statement (keep the file otherwise intact); run `npx vitest run src/lib/analytics-client.test.ts -t "PYAPI-06"` and confirm it FAILS on Test A (record the failing assertion line); restore with `cp` from the backup (never `git checkout --`, which destroys uncommitted work); confirm `shasum -a 256` matches the pre-neuter hash and re-run the same command to green.
  </action>
  <verify>
    <automated>npx vitest run src/lib/analytics-client.test.ts 2>&1 | tail -15 && npx tsc --noEmit</automated>
    <fails_when>non-zero exit, a "FAIL" line in the vitest tail, "0 passed" in the summary, or any tsc diagnostic line</fails_when>
    <automated>set -o pipefail; grep -c 'SERVICE_KEY && {' src/lib/analytics-client.ts | grep -qx 0 && grep -c 'throw new SeamConfigError' src/lib/analytics-client.ts | grep -qx 1 && grep -c 'process.env.ANALYTICS_SERVICE_KEY = ' src/lib/analytics-client.test.ts | awk '$1 >= 12 {exit 0} {exit 1}'</automated>
    <fails_when>non-zero exit: the conditional spread survives, the throw count is not exactly 1, or fewer than 12 beforeEach blocks set the key (11 new + the existing [140.2-09] one)</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n 440,475p src/lib/analytics-client.ts` shows the `SeamConfigError` throw between the `mintTenantClaim(` call and the `try {` line, and the header block contains the literal `"X-Service-Key": SERVICE_KEY,` with no `SERVICE_KEY &&` spread
    - `grep -n 'ANALYTICS_SERVICE_KEY' src/lib/analytics-client.ts` shows the env read at line 46 AND the name inside the new error message
    - `npx vitest run src/lib/analytics-client.test.ts -t "PYAPI-06"` prints at least 2 passed and 0 failed
    - the SUMMARY contains the RED assertion line observed with the throw deleted, the pre- and post-restore sha256 (identical), and the command used to restore (a `cp` from the byte backup)
    - `npx vitest run src/lib/resilient-fetch.wiring.test.ts src/lib/resilient-fetch.retry.test.ts src/lib/seam-redaction.test.ts src/lib/sentry-capture.test.ts 2>&1 | tail -8` shows 0 failed (the sibling files RESEARCH F5 named as already setting or stubbing the key)
  </acceptance_criteria>
  <done>With the key unset every wrapper refuses with a named `SeamConfigError` before any fetch; with the key set the header is unconditional; the eleven describe blocks are green; the guard was observed RED when deleted and restored byte-identically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Python half — reject an ABSENT X-Service-Key with SERVICE_KEY_ABSENT (D-10), keep `if provided:` (D-11), move the pins, disposition the new code, prove neuter → RED → restore (D-12)</name>
  <reversibility rating="costly">`SERVICE_KEY_ABSENT` is a wire code: once deployed it is read by the prober's PYAPI-06 arm and dispositioned in the TypeScript vocabulary; renaming it later touches main.py, three Python tests, wizardErrors.ts, the vocabulary roster and the prober fixtures.</reversibility>
  <files>analytics-service/main.py, analytics-service/tests/test_verify_service_key_middleware.py, analytics-service/tests/test_service_key_log_companion.py, analytics-service/tests/test_secret_misconfig_signal.py, src/lib/seam-venue-vocabulary.invariant.test.ts, src/lib/wizardErrors.ts</files>
  <read_first>
    - analytics-service/main.py:797-812 (the unset arm — the envelope helper and its style)
    - analytics-service/main.py:814-834 (the header-read line and the mismatch block that must stay byte-identical, including the docblock explaining why the capture lives inside the provided-guard)
    - analytics-service/services/error_contract.py:303-312 (service_error_response signature; status >= 400 and a non-empty code are the only constraints for a 401)
    - analytics-service/tests/test_verify_service_key_middleware.py:27-44 (the absent-header test whose body pin moves)
    - analytics-service/tests/test_service_key_log_companion.py:122-148 (the zero-event pin that STAYS; only the body assertion moves)
    - analytics-service/tests/test_secret_misconfig_signal.py:296-299 (`faults == ["unset", "mismatched"]` — must stay EXACTLY that)
    - src/lib/seam-venue-vocabulary.invariant.test.ts:650-688 (the hand-typed EXPECTED_EMITTED_CODES roster — a new Python code reds the suite until it is added here by name)
    - src/lib/seam-venue-vocabulary.invariant.test.ts:773-791 (every derived code needs a verdict row OR an exemption with a reason longer than 80 chars and no "todo")
    - src/lib/wizardErrors.ts:4293-4320 (VENUE_WIRE_CODES_WITHOUT_VERDICT — the exemption map shape and the reason prose style)
    - src/lib/wizardErrors.ts:4272 (how SERVICE_KEY_UNCONFIGURED was dispositioned, and why a verdict row would be FALSE for the absent case)
  </read_first>
  <behavior>
    - Test D: absent header on a guarded POST → 401 and `resp.json()["detail"]["code"] == "SERVICE_KEY_ABSENT"`, `["detail"]["retryable"] is False`.
    - Test E: wrong header → 401 and `resp.json() == {"detail": "Unauthorized"}` (unchanged), i.e. the two bodies DIFFER.
    - Test F (stays): absent header logs zero `service_key.*` events; Test G (stays): captures are exactly `["unset", "mismatched"]`.
    - Test H (TS): `seam-venue-vocabulary.invariant.test.ts` is green with `SERVICE_KEY_ABSENT` in the roster and in the exemption map, and red if either is removed.
  </behavior>
  <action>
    In `analytics-service/main.py`, after the line that reads `provided = request.headers.get("X-Service-Key", "")` and BEFORE the `compare_digest` line, add an `if not provided:` branch that returns `service_error_response(401, "SERVICE_KEY_ABSENT", retryable=False, detail="Unauthorized")` — the same envelope helper the unset arm uses, nested under `detail` like every other middleware envelope. NO `_auth_log` call and NO `_capture_secret_misconfig` call in this branch: an absent header is still internet background noise; the point (TODOS 0.04) is that our OWN client with an empty key is now impossible after plan 02 Task 1, so absence can be rejected with its own code without paging anyone. Leave the mismatch block and the `if provided:` block byte-identical (D-11). Add a 4-6 line comment in the new branch citing PYAPI-06 both halves, why the status stays 401 (public contract for unauthenticated callers), and why there is no log/capture (the zero-event pin).

    Move the pins: `test_verify_service_key_middleware.py` `test_missing_service_key_returns_clean_401` — keep the status assertion; replace the body equality with `resp.json()["detail"]["code"] == "SERVICE_KEY_ABSENT"` and add a sibling assertion in the SAME test that a wrong key still yields exactly `{"detail": "Unauthorized"}` so the file itself proves the two bodies differ; add a comment naming the neuter proof (delete the `if not provided:` branch → this test reds on the code assertion while the wrong-key test stays green). `test_service_key_log_companion.py` `test_an_absent_service_key_header_logs_nothing_at_all` — replace the body equality with the code assertion; the `events == []` assertion STAYS. `test_secret_misconfig_signal.py` — no change to the `faults == ["unset", "mismatched"]` assertion; run it to prove absence adds no capture.

    TypeScript disposition: add `"SERVICE_KEY_ABSENT",` to `EXPECTED_EMITTED_CODES` in `src/lib/seam-venue-vocabulary.invariant.test.ts` in sorted position (the roster is hand-typed by design — do not derive it), and update the roster's measured-count prose in the docblock above it (37 → 38 distinct codes, 42 → 43 call sites) so the comment is not a stale integer. In `src/lib/wizardErrors.ts` add an entry to `VENUE_WIRE_CODES_WITHOUT_VERDICT` for `SERVICE_KEY_ABSENT` with a reason of more than 80 characters and no placeholder words, stating: it is emitted only to a caller that sent NO `X-Service-Key`; after PYAPI-06's TypeScript half `analytics-client.ts` refuses before the fetch, so this code cannot arrive from our own client and reaches no user-facing surface; a verdict row would manufacture a diagnosis for a request we never send, and `SEAM_MISCONFIGURED`'s "nothing was submitted" copy would be false-by-construction because a request WAS sent. Do NOT add a `VENUE_WIRE_CODE_TO_VERDICT` row (the two dispositions are exclusive).

    Neuter → RED → restore for this half, recorded in the SUMMARY: byte-backup `analytics-service/main.py` + sha256; delete ONLY the `if not provided:` branch; from `analytics-service/` run `python3 -m pytest tests/test_verify_service_key_middleware.py -x -q` and confirm the code assertion FAILS while a targeted run of the wrong-key test still passes; restore from the backup with `cp`; confirm the sha256 matches; re-run to green. Then run `python3 -m mypy --strict main.py` from `analytics-service/` (memory: mypy errors stay latent until PR CI otherwise).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_verify_service_key_middleware.py tests/test_service_key_log_companion.py tests/test_secret_misconfig_signal.py tests/test_status_contract_match_sim_portfolio.py -q 2>&1 | tail -8</automated>
    <fails_when>non-zero exit, any "failed" count above 0 in the summary line, or "no tests ran"</fails_when>
    <automated>npx vitest run src/lib/seam-venue-vocabulary.invariant.test.ts src/lib/seam-terminal-arm.invariant.test.ts 2>&1 | tail -8 && npx tsc --noEmit</automated>
    <fails_when>non-zero exit, a "FAIL" line, "0 passed", or any tsc diagnostic</fails_when>
    <automated>cd analytics-service && python3 -m mypy --strict main.py 2>&1 | tail -3</automated>
    <fails_when>the last line is not "Success: no issues found" (any error count line is a failure)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'SERVICE_KEY_ABSENT' analytics-service/main.py` shows exactly one emitter line inside `verify_service_key`, positioned after the `provided =` line and before the `compare_digest` line (`awk` the line numbers: provided < absent < compare_digest)
    - `sed -n 815,840p analytics-service/main.py` still contains the `if provided:` block with both the `_auth_log.warning("service_key.mismatch"` and the `_capture_secret_misconfig("mismatched"` lines INSIDE it, and the trailing `return JSONResponse({"detail": "Unauthorized"}, status_code=401)`
    - `grep -c '"SERVICE_KEY_ABSENT"' src/lib/seam-venue-vocabulary.invariant.test.ts` prints `1` and `grep -c 'SERVICE_KEY_ABSENT' src/lib/wizardErrors.ts` is at least 1, with the match inside `VENUE_WIRE_CODES_WITHOUT_VERDICT` and NOT inside `VENUE_WIRE_CODE_TO_VERDICT`
    - `grep -rn 'assert resp.json() == {"detail": "Unauthorized"}' analytics-service/tests/` returns only wrong-key cases (each hit's enclosing test sends a non-empty `X-Service-Key`)
    - `grep -n 'faults == \["unset", "mismatched"\]' analytics-service/tests/test_secret_misconfig_signal.py` still matches
    - the SUMMARY contains the RED pytest line observed with the branch deleted, the identical pre/post sha256, and the mypy `Success` line
  </acceptance_criteria>
  <done>An absent header is rejected with `SERVICE_KEY_ABSENT` (401), a wrong key is rejected with the unchanged plain body, nothing is logged or captured for absence, the vocabulary invariant and wizardErrors carry the new code's disposition, pytest + mypy --strict + the vitest invariants are green, and the branch was observed RED when deleted and restored byte-identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel (analytics-client.ts) → Railway analytics-service | the service-key header crosses the public internet |
| internet → analytics-service guarded routes | unauthenticated probers hit the middleware continuously |
| Python emitters → TypeScript vocabulary | every emitted machine code must be dispositioned or it falls through the substring cascade |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.1-06 | Information disclosure | SeamConfigError message / logs | high | mitigate | The message names `ANALYTICS_SERVICE_KEY` and never its value; Test A asserts the sentinel value is absent from the message; the existing catch arm scrubs via `scrubSeamString` |
| T-164.1-07 | Denial of service | Sentry / log volume from internet probers | medium | mitigate | The absent branch emits no log and no capture; the `if provided:` guard is byte-identical and pinned by the zero-event test and the exact `["unset", "mismatched"]` fault list |
| T-164.1-08 | Spoofing | a caller distinguishing absent vs wrong by the response code | low | accept | `SERVICE_KEY_ABSENT` tells an attacker only that they sent no header, which they already know; the status is unchanged (401) and the wrong-key body is unchanged |
| T-164.1-09 | Repudiation | a config fault reported as an upstream outage | medium | mitigate | The throw sits above the `try` so the existing catch arms cannot rewrite it as "not reachable" (the tenant-claim precedent at analytics-client.ts:424-427) |
| T-164.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing; no dependency manifest changes on either side |
</threat_model>

<verification>
- From `analytics-service/`: `python3 -m pytest -q` (whole suite, alone on the box) then `python3 -m mypy --strict main.py`.
- `npx vitest run src/lib/analytics-client.test.ts src/lib/seam-venue-vocabulary.invariant.test.ts` green; `npx tsc --noEmit` clean.
- Full vitest run once, ALONE on the box (memory: a contended run fakes regressions), before the wave is declared done.
</verification>

<success_criteria>
- Success criterion 2 is met: both halves are closed, each with a test that fails when its guard is removed, and the RED observations are recorded in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-02-SUMMARY.md` when done. Record both neuter → RED → restore proofs (RED line, pre/post sha256, restore command), the mypy result, and the exact roster/exemption edits.
</output>