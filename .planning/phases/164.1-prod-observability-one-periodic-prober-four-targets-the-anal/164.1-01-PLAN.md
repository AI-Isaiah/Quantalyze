---
phase: 164.1-prod-observability-one-periodic-prober-four-targets-the-anal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/prod-prober/run.mjs
  - scripts/prod-prober/seams.mjs
  - scripts/prod-prober/arms/pyapi06.mjs
  - scripts/prod-prober/fixtures/pyapi06/ok.json
  - scripts/prod-prober/fixtures/pyapi06/keyed-401.json
  - scripts/prod-prober/fixtures/pyapi06/absent-200.json
  - scripts/prod-prober/fixtures/pyapi06/absent-401-uncoded.json
  - scripts/prod-prober/fixtures/pyapi06/wrong-key-200.json
  - scripts/prod-prober/fixtures/pyapi06/health-degraded.json
autonomous: true
requirements: [PYAPI-06]
tags: [prod-prober, observability, self-test, anti-vacuity]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-04 / D-05: `node scripts/prod-prober/run.mjs --self-test` exits 0 and prints `=== SELF-TEST PASSED` after driving the PYAPI-06 arm through the REAL `runProber` verdict loop with fixture seams — every pyapi06 defect kind fires on its own red fixture, isolates exactly one kind, and the green fixture exits 0."
    - "D-06: a live invocation with NO credentials (`env -i PATH=\"$PATH\" node scripts/prod-prober/run.mjs`) exits 1 and prints one `credential-absent` defect row per required env NAME (`ANALYTICS_BASE_URL`, `ANALYTICS_SERVICE_KEY`) — never a skip, never exit 0, and never an env VALUE."
    - "D-08: `ARMS_FLOOR` is the literal export `4` in `scripts/prod-prober/run.mjs`; with only one arm registered the live run reports a `floor` defect (`1 < 4`) and exits 1 — the incomplete prober is loud until plans 03 and 04 register the other three arms."
    - "D-04: two faults injected in ONE run both appear in the defect table (the OPS-08-F8 first-failure shape is not reproduced) — self-test scenario 7."
    - "The absurdity floor holds: a seam stub that never increments the tally makes the run report `absurdity` and exit 1 (scenario 10), and a fetch that THROWS reports `measure-fail` on the arm, never a green arm and never a pyapi06 verdict (scenario 11)."
    - "No env VALUE ever reaches the log: self-test scenario 12 injects sentinel secret values and asserts no captured log line contains them."
    - "A narrowed `--arm pyapi06` diagnostic run exits 2 even when green (scenario 13) — a partial run is never mistakable for a passing gate."
    - { statement: "PYAPI-06 concurrency edge: two prober runs overlapping in time cannot corrupt each other — the arm is read-only (three GETs + one /health GET), keeps no state between runs, and the workflow (plan 05) serializes runs under `concurrency: group: prod-prober`; a mid-run interruption leaves nothing behind because the arm writes nothing", verification: backstop }
  artifacts:
    - "scripts/prod-prober/run.mjs — CLI entry; exports ARMS_FLOOR, DEFECT_KINDS, SELF_TEST_SCENARIOS, ARMS, runProber, selfTest, main"
    - "scripts/prod-prober/seams.mjs — createSeams({fetchImpl, sqlRunner, sshRunner, clock}) with per-seam and per-arm invocation tallies; realFetch"
    - "scripts/prod-prober/arms/pyapi06.mjs — ARM {name:'pyapi06', requiredEnv, run, REMEDIES}"
    - "scripts/prod-prober/fixtures/pyapi06/*.json — one green + five red fixtures"
  key_links:
    - "runProber → credential gate → arm.run(seams) → addDefect(kind, arm, subject, detail, remedy) → floors → absurdity → summary → exitCode (decided ONCE at the end)"
    - "selfTest → runProber with fixture seams and an explicit armsFloor per scenario (never the constant) → expect()/noDefectOfKind() chains"
    - "main(argv) is the only process.exit; the endsWith('run.mjs') guard lets vitest import the module"
  prohibitions:
    - { requirement_id: PYAPI-06, category: safety, status: resolved, verification: test, statement: "MUST NOT convert an absent credential into a skip, a warning, or exit 0 — the rc-2 warning-then-exit-0 shape in phase-19-stability.yml (lines 54-58) is forbidden; an absent credential is a credential-absent defect and exit 1" }
    - { requirement_id: PYAPI-06, category: privacy, status: resolved, verification: test, statement: "MUST NOT print any env VALUE, response header, or bearer/key material to stdout, stderr, a defect row or a remedy string — names only (the repo and its Actions logs are public)" }
    - { requirement_id: PYAPI-06, category: safety, status: resolved, verification: judgment, statement: "MUST NOT write to PROD from any arm — the PYAPI-06 arm is three GETs to a guarded read-only route plus one /health GET; no POST, no retry loop" }
    - { requirement_id: PYAPI-06, category: safety, status: resolved, verification: test, statement: "MUST NOT exit at the first failing arm — every registered arm runs every time and the exit code is decided once, at the end" }
---

<objective>
Build the tracer: the prod-prober skeleton (`scripts/prod-prober/run.mjs` + `seams.mjs`) with ONE
arm — PYAPI-06 — proven end to end through the real verdict loop by a `--self-test` mode, with the
pinned `ARMS_FLOOR = 4`, the per-name `credential-absent` gate, the absurdity cross-check, the
`measure-fail` distinction, aggregate-then-exit, and the never-print-a-value rule.

Purpose: prove the architecture (injectable seams → arm registry → named-defect table → floors →
single exit) on one arm before three more arms are stacked on it. Everything plans 03 and 04 add
is another `arms/*.mjs` module exporting the same `{ name, requiredEnv, run, REMEDIES }` contract
and its scenarios; nothing structural changes after this plan.

Output: `scripts/prod-prober/run.mjs`, `scripts/prod-prober/seams.mjs`,
`scripts/prod-prober/arms/pyapi06.mjs`, six fixtures, a passing `--self-test` (13 scenarios), and
a live no-credential run that exits 1 naming each absent credential and the floor.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-CONTEXT.md
@.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-RESEARCH.md
@.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-PATTERNS.md
@scripts/mutation-runner/run.mjs
@scripts/lint-sql-gates.mjs
@scripts/prod-body-drift-check.sh

## Decision register (CONTEXT.md decisions, numbered here for traceability)

- D-01 GitHub Actions scheduled workflow driving ONE script under `scripts/prod-prober/`.
- D-02 Hourly `0 * * * *` plus `workflow_dispatch`.
- D-03 A failing arm makes the job exit non-zero; the dedup'd auto-issue pattern is reused.
- D-04 ALL four arms run every time; aggregate and exit 1 at the END (OPS-08-F8 must not recur).
- D-05 `--self-test` injects each arm's fault against fixtures and asserts the non-zero exit NAMING that arm.
- D-06 An absent credential is a HARD FAILURE naming the credential — never a SKIP.
- D-07 Read-only against PROD; no MT5 log-in call.
- D-08 Pinned `ARMS_FLOOR` of 4.
- D-09 TS half: throw `SeamConfigError` BEFORE the fetch when the key is absent; no module-load assertion.
- D-10 Python half: absent header rejected with machine code `SERVICE_KEY_ABSENT`; status stays 401.
- D-11 The `if provided:` Sentry guard stays.
- D-12 Each PYAPI-06 half gets a test that FAILS when its guard is deleted (neuter → RED → restore).
- D-13 Cron-drift oracle = a COMMITTED manifest of the ACHIEVABLE Vault-backed command; not derived from migrations; not a migration.
- D-14 ALL PROD `cron.job` rows are in scope; an extra job is drift.
- D-15 The comparison result is RECORDED so the next reader knows which side moved.
- D-16 MT5 arm round-trips via initialize() + terminal_info(); never the log-in call.
- D-17 -10004 and -10005 are two DISTINCT kinds with two distinct remedies.
- D-18 Own workflow `prod-prober.yml`, outside the `frontend` aggregate and outside the run Railway consults; the deploy-skip hazard is MEASURED once before the exit posture is fixed.
- D-19 Credentials: `ANALYTICS_SERVICE_KEY` and `RAILWAY_API_TOKEN` exist as repo secrets; `RAILWAY_SSH_PRIVATE_KEY` is RETIRED; repo variables `RAILWAY_PROJECT_ID`, `RAILWAY_MT5_SERVICE`, `RAILWAY_ENVIRONMENT`, `SUPABASE_PROJECT_REF` exist; the SQL arms use `SUPABASE_ACCESS_TOKEN` + `SUPABASE_DB_PASSWORD` through the session pooler with the password as `PGPASSWORD`.
- D-20 Wave 0 owns measuring that a workspace token authenticates `railway ssh` from a hosted runner.

## Artifacts this phase produces (whole phase, all six plans)

| Artifact | Created by | Symbol(s) |
|---|---|---|
| `scripts/prod-prober/run.mjs` | plan 01 | `ARMS_FLOOR = 4`, `SELF_TEST_SCENARIOS`, `DEFECT_KINDS`, `ARMS`, `runProber`, `selfTest`, `captureManifest` (plan 03), `main`; flags `--self-test`, `--arm <name>`, `--capture-manifest --out <path>` |
| `scripts/prod-prober/seams.mjs` | plan 01 | `createSeams`, `realFetch`, `realSqlRunner` (plan 03), `realSshRunner` (plan 04), `tally` |
| `scripts/prod-prober/arms/pyapi06.mjs` | plan 01 | `ARM` (`name: "pyapi06"`), `REMEDIES` |
| `scripts/prod-prober/arms/cron-obs.mjs` | plan 03 | `ARM` (`name: "cron-obs"`), `CRON_OBS_SQL`, `judgeCronObs`, `REMEDIES` |
| `scripts/prod-prober/arms/cron-drift.mjs` | plan 03 | `ARM` (`name: "cron-drift"`), `MANIFEST_PATH`, `MANIFEST_SCHEMA_VERSION`, `NORMALIZATION`, `normalizeCommand`, `hygieneViolations`, `compareManifest`, `REMEDIES` |
| `scripts/prod-prober/arms/mt5.mjs` | plan 04 | `ARM` (`name: "mt5"`), `MT5_PROBE_PY`, `classifyProbe`, `REMEDIES` |
| `scripts/prod-prober/fixtures/**` | plans 01, 03, 04 | red + green fixtures per arm |
| `scripts/prod-prober/cron-manifest.json` | plan 06 (captured from PROD) | the committed oracle |
| `.github/workflows/prod-prober.yml` | plan 05 | hourly schedule, dispatch inputs `mode` and `arm`, self-test-then-live, dedup'd issue |
| `src/__tests__/prod-prober-wiring.test.ts` | plan 05 | `EXPECTED_DEFECT_KINDS`, workflow-text pins |
| `src/lib/analytics-client.ts` | plan 02 | the `SeamConfigError` throw above the try |
| `analytics-service/main.py` | plan 02 | machine code string `SERVICE_KEY_ABSENT` |
| `DEFECT_KINDS` (20) | plans 01/03/04 | `credential-absent`, `measure-fail`, `pyapi06-keyed-refused`, `pyapi06-absent-accepted`, `pyapi06-absent-uncoded`, `pyapi06-wrong-key-accepted`, `pyapi06-health-degraded`, `cron-no-observation`, `cron-non-2xx`, `cron-transport-error`, `cron-drift`, `cron-secret-in-command`, `manifest-invalid`, `mt5-no-ipc`, `mt5-ipc-timeout`, `mt5-ssh-transport`, `mt5-probe-timeout`, `mt5-terminal-error`, `floor`, `absurdity` |
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "one arm, one verdict loop" — run.mjs + seams.mjs + the PYAPI-06 arm, proven by --self-test and a live no-credential run</name>
  <reversibility rating="costly">The arm-module contract `{ name, requiredEnv, run(ctx), REMEDIES }`, the seam result shapes and the `DEFECT_KINDS` names are what plans 03-05 and the wiring test build on; changing them later touches every arm and the vitest pin.</reversibility>
  <files>scripts/prod-prober/run.mjs, scripts/prod-prober/seams.mjs, scripts/prod-prober/arms/pyapi06.mjs, scripts/prod-prober/fixtures/pyapi06/ok.json, scripts/prod-prober/fixtures/pyapi06/keyed-401.json, scripts/prod-prober/fixtures/pyapi06/absent-200.json, scripts/prod-prober/fixtures/pyapi06/absent-401-uncoded.json, scripts/prod-prober/fixtures/pyapi06/wrong-key-200.json, scripts/prod-prober/fixtures/pyapi06/health-degraded.json</files>
  <read_first>
    - scripts/mutation-runner/run.mjs:1-60 (the CLI-contract header and exit-code table to mirror)
    - scripts/mutation-runner/run.mjs:1702-1734 (DEFECT_KINDS export + addDefect throwing on an unknown kind)
    - scripts/mutation-runner/run.mjs:3722-3740 (the injectable runner docblock — only self-test and vitest pass it)
    - scripts/mutation-runner/run.mjs:3765-3772 (tally snapshot read as a delta)
    - scripts/mutation-runner/run.mjs:3268-3299 (absurdityViolations — an unmeasurable count is not zero)
    - scripts/mutation-runner/run.mjs:4255-4258 (summary lines, floors as defects, exit decided once)
    - scripts/mutation-runner/run.mjs:4613-4642 (expect, noDefectOfKind, the counted N/M header rule)
    - scripts/mutation-runner/run.mjs:5229-5287 (main dispatch; the endsWith guard)
    - scripts/lint-sql-gates.mjs:1307-1343 (red fixture must fire exactly ONE rule; green fires none; a missing fixture is a FAIL)
    - scripts/prod-body-drift-check.sh:24-35 (never skip / never print PROD text / never pass what was not compared)
    - .github/workflows/phase-19-stability.yml:54-58 (the anti-pattern: a warning plus exit 0 on unset secrets — do not reproduce)
    - analytics-service/main.py:742-777 (which paths the service-key middleware skips; /health is unauthenticated)
    - analytics-service/main.py:884-893 (the /health body: config_ok and config_degraded_secrets name secrets, never values)
    - analytics-service/services/error_contract.py:303-312 (service_error_response: the envelope is nested under detail)
    - analytics-service/routers/match.py:1855-1861 (GET /api/match/eval — the guarded read-only route, 30/min)
    - .github/workflows/analytics-deploy-verify.yml:76 (the public analytics base URL default)
  </read_first>
  <action>
    Create `scripts/prod-prober/run.mjs` as a port of the mutation-runner skeleton, one arm registered.

    Header docblock: the CLI contract the workflow (plan 05) pastes VERBATIM — `node scripts/prod-prober/run.mjs` (live, four arms), `node scripts/prod-prober/run.mjs --self-test` (fixtures only, no network), `node scripts/prod-prober/run.mjs --arm <name>` (narrowed DIAGNOSTIC, never exits 0), `node scripts/prod-prober/run.mjs --capture-manifest --out <path>` (reserved for plan 03; in this plan `main` returns 3 with a usage line for it) — and the exit-code table: 0 no defects and floors held, 1 at least one defect / floor / absurdity, 2 narrowed diagnostic with no defects, 3 usage or environment error. State D-04 and D-06 in the header in prose.

    Exports, all literal: `export const ARMS_FLOOR = 4;` (D-08 — the literal 4 now, while only one arm is registered; the live run MUST report a `floor` defect until plans 03/04 register the other three — that is the tripwire working, not a bug), `export const SELF_TEST_SCENARIOS = 13;`, `export const DEFECT_KINDS = [...]` carrying ALL twenty names listed in the context table above (the cron and mt5 kinds are registered now so plans 03/04 add scenarios, not kinds — the wiring test in plan 05 ranges over this list), `export const ARMS = [PYAPI06_ARM];` (plans 03/04 append), `export function runProber(opts)`, `export function selfTest()`, `export function main(argv)`.

    `runProber({ arms = ARMS, env = process.env, seams, armsFloor = ARMS_FLOOR, manifestPath = MANIFEST_PATH_PLACEHOLDER, log = console.log })` — NEVER reads process.env, fetch, or spawn directly; everything comes through `seams` (see seams.mjs) and `env`. Steps, in order: (1) credential gate — for every arm, for every name in `arm.requiredEnv`, if `env[name]` is empty or undefined then `addDefect("credential-absent", arm.name, name, "<NAME> is not set — this arm cannot measure, and a run that cannot measure is a failing run", REMEDY_TEXT)` and mark the arm BLOCKED (its `run` is not invoked); print `credential: <NAME> present` or `credential: <NAME> ABSENT` per name — names only, never values; (2) snapshot `seams.tally` before running; (3) for every non-blocked arm, wrap `arm.run({ env, seams, log, addDefect })` in try/catch — a throw becomes `addDefect("measure-fail", arm.name, null, "<arm> threw: <error.message with env values scrubbed>")`, never a silent pass; (4) floors: `arms.length < armsFloor` → `addDefect("floor", null, null, "ARMS_FLOOR regression: <n> registered arm(s) < floor <armsFloor>")`; (5) absurdity: compute the tally delta; `armsExecuted` must equal the number of arms whose per-arm tally delta is at least 1, and every executed arm must have at least one seam invocation — otherwise `addDefect("absurdity", ...)` with a `MEASURE_FAIL — this is the GATE failing, not production:` prefix; a non-integer count is an absurdity too; (6) print `arms: <executed>/<registered>/<blocked>   (executed/registered/credential-blocked)` and `seam-invocations: <total>   (fetch <f> / sql <s> / ssh <x>)`; (7) print the defect table — `KIND  ARM  SUBJECT` header, one row per defect plus an indented `detail` line and an indented `remedy:` line — or the exact line `✅ No defects. Every registered arm ran and measured.`; (8) return `{ armsRegistered, armsExecuted, armsBlocked, seamInvocations, tallyByArm, defects, exitCode }` where `exitCode` is 1 if any defect, else 2 if narrowed (an `onlyArm` option was set), else 0. `addDefect(kind, arm, subject, detail, remedy)` throws `unknown defect kind` on a name outside `DEFECT_KINDS`, and the log function is the ONLY output channel (self-test captures it). Scrub: before any detail/remedy string is logged, replace every non-empty env value of a name in any arm's `requiredEnv` with `<redacted>` (the secret-echo guard the self-test proves).

    `main(argv)`: `--self-test` → `return selfTest()` before anything else; `--arm <name>` → `runProber({ onlyArm: name, seams: createSeams({ realFetch ... }) })` and return its exitCode (2 when clean); `--capture-manifest` → print a usage line naming plan 03 and return 3 for now; default → live run with real seams, return exitCode. Guard the CLI with `if (process.argv[1] && process.argv[1].endsWith("run.mjs")) process.exit(main(process.argv.slice(2)))` so vitest can import the exports without running.

    Create `scripts/prod-prober/seams.mjs`: `export function createSeams({ fetchImpl = realFetch, sqlRunner = null, sshRunner = null, clock = () => new Date() })` returning `{ fetch(arm, url, init), sql(arm, query), ssh(arm, argv, { timeoutMs }), now(), tally }`. Each seam call increments `tally.<seam>` and `tally.byArm[arm]` BEFORE delegating (the two independent counts). Seam result shapes (fixed contract for plans 03/04): fetch → `{ status: number|null, json: any|null, text: string, measureFail: string|null }`; sql → `{ status: number|null, stdout: string, stderr: string, timedOut: boolean, measureFail: string|null }`; ssh → same shape as sql. `sql` and `ssh` throw a plain Error `seam <name> not wired` when their runner is null (plans 03/04 wire them). `realFetch(url, init)` uses global fetch with `AbortSignal.timeout(15000)`, parses JSON when the content-type is JSON, and returns `measureFail` on a network throw rather than throwing. Export `resetTallyForTests()` ONLY for the self-test to snapshot around scenarios — document that the CLI never calls it.

    Create `scripts/prod-prober/arms/pyapi06.mjs` exporting `ARM = { name: "pyapi06", requiredEnv: ["ANALYTICS_BASE_URL", "ANALYTICS_SERVICE_KEY"], run, REMEDIES }`. `run({ env, seams, log, addDefect })` issues, through `seams.fetch("pyapi06", ...)`, exactly FOUR requests to `env.ANALYTICS_BASE_URL`: (a) GET `/api/match/eval?lookback_days=1` with header `X-Service-Key: <env.ANALYTICS_SERVICE_KEY>` → expect status 200; a 401 is `pyapi06-keyed-refused`; a 5xx or a fetch `measureFail` is `measure-fail`; (b) the same GET with NO service-key header → expect 401 AND `json.detail.code === "SERVICE_KEY_ABSENT"`; a 2xx is `pyapi06-absent-accepted`; a 401 whose body lacks that code is `pyapi06-absent-uncoded` (the Python half of PYAPI-06 is not live in PROD yet — plan 02 lands it); (c) the same GET with `X-Service-Key: prod-prober-deliberately-wrong-key` → expect 401 WITHOUT `SERVICE_KEY_ABSENT`; a 2xx is `pyapi06-wrong-key-accepted`; (d) GET `/health` → if `json.config_degraded_secrets` is an array containing `SERVICE_KEY`, `pyapi06-health-degraded`. Print one line per request as `pyapi06: <label> → <status>` (status codes and the code string only; never the key, never response headers, never bodies beyond the first 200 chars of `text` and only on a defect). `REMEDIES` maps each of the five pyapi06 kinds to one sentence of operator remedy (e.g. keyed-refused: re-copy Railway SERVICE_KEY into the GitHub secret ANALYTICS_SERVICE_KEY through a trimming pipe and into Vercel, Railway → GitHub/Vercel never the reverse; absent-accepted / wrong-key-accepted: the middleware is not guarding — check the deployed analytics-service SHA and SERVICE_KEY on Railway; absent-uncoded: the deployed analytics-service predates PYAPI-06's Python half — deploy main; health-degraded: SERVICE_KEY is unset on Railway). Every defect row from this arm carries its REMEDIES entry.

    Fixtures under `scripts/prod-prober/fixtures/pyapi06/`: each is a JSON object keyed by request label (`keyed`, `absent`, `wrong`, `health`) → `{ status, json }`. `ok.json` = 200 / 401+SERVICE_KEY_ABSENT / 401 plain / health with `config_degraded_secrets: []`. `keyed-401.json` flips only `keyed` to 401. `absent-200.json` flips only `absent` to 200. `absent-401-uncoded.json` sets `absent` to 401 with body `{ "detail": "Unauthorized" }` (today's PROD shape). `wrong-key-200.json` flips only `wrong` to 200. `health-degraded.json` sets `health.json.config_degraded_secrets` to `["SERVICE_KEY"]`. Fixture values are obviously fake (no key material anywhere).

    `selfTest()` in run.mjs: `expect(condition, message)` prints `SELF-TEST FAIL: <message>` and returns false, else `  ok — <message>`; `noDefectOfKind(defects, kinds, where)` is the ONLY way an ABSENCE is spelled (the plan-05 extractor scans this function's source for `kind === "<k>"` and would credit a negated equality as coverage — say so in a comment); scenario headers are the counted set `=== SELF-TEST k/13: <title> ===`, `SELF_TEST_SCENARIOS` is asserted equal to the number of headers printed at the end; every whole-run scenario passes an EXPLICIT `armsFloor` and an explicit `arms` list so the mechanism, not the constant, is measured; `pass = expect(...) && expect(...) && pass` with `pass` LAST; a `quiet` log that captures lines into an array (scenario 12 reads it). A fixture-backed fetch seam `fixtureFetch(fixtureName)` maps the four labels to `{ status, json, text: JSON.stringify(json), measureFail: null }` and a missing fixture file is a SELF-TEST FAIL, not a skip. The thirteen scenarios:
    1/13 green — `ok.json`, arms `[PYAPI06_ARM]`, `armsFloor: 1`, env with all names set → exitCode 0, armsExecuted 1, tally fetch 4, byArm.pyapi06 4, and the ✅ line present.
    2/13 `keyed-401.json` → exitCode 1, exactly one defect, kind `pyapi06-keyed-refused`, arm `pyapi06`, and its `remedy` equals `REMEDIES["pyapi06-keyed-refused"]`.
    3/13 `absent-200.json` → exactly one defect, `pyapi06-absent-accepted`.
    4/13 `absent-401-uncoded.json` → exactly one defect, `pyapi06-absent-uncoded`.
    5/13 `wrong-key-200.json` → exactly one defect, `pyapi06-wrong-key-accepted`.
    6/13 `health-degraded.json` → exactly one defect, `pyapi06-health-degraded`.
    7/13 two faults in one run — a merged fixture (absent-200 + health-degraded) → exitCode 1 and BOTH kinds present (D-04, OPS-08-F8 not reproduced).
    8/13 credential-absent — for EACH name in `PYAPI06_ARM.requiredEnv`, run with that one name deleted from env → exitCode 1, a `credential-absent` defect whose `subject` is that name, its `detail` contains the name, the run's captured log contains `credential: <NAME> ABSENT`, `armsBlocked === 1`, `armsExecuted === 0`, and `noDefectOfKind(defects, ["pyapi06-keyed-refused", "pyapi06-absent-accepted", "pyapi06-absent-uncoded", "pyapi06-wrong-key-accepted", "pyapi06-health-degraded"])` (a blocked arm reports nothing else).
    9/13 floor — `ok.json`, arms `[PYAPI06_ARM]`, armsFloor left at the DEFAULT → exitCode 1 and a `floor` defect whose detail contains `1 registered arm(s) < floor 4`.
    10/13 absurdity — a fetch seam that answers `ok.json` but was constructed to bypass the tally (pass `createSeams` a fetchImpl and then overwrite `seams.fetch` with a wrapper that does not increment) → exitCode 1, an `absurdity` defect whose detail contains `MEASURE_FAIL`, and `noDefectOfKind(defects, ["floor"])` is irrelevant — assert armsExecuted is reported against a zero tally.
    11/13 measure-fail — a fetch seam whose `keyed` result carries `measureFail: "ECONNREFUSED"` → exitCode 1, one `measure-fail` defect on `pyapi06`, and `noDefectOfKind(defects, ["pyapi06-keyed-refused", "pyapi06-absent-accepted", "pyapi06-absent-uncoded", "pyapi06-wrong-key-accepted", "pyapi06-health-degraded"])`.
    12/13 secret-echo — env values `ANALYTICS_SERVICE_KEY: "SENTINEL-KEY-9f3a"`, `ANALYTICS_BASE_URL: "https://sentinel.invalid"` with `keyed-401.json` (a defect run, so detail lines are printed) → no captured log line contains `SENTINEL-KEY-9f3a`; the defect detail does not contain it either.
    13/13 narrowed — `main(["--arm", "pyapi06"])` with the seams factory injected through a module-level `setSeamsFactoryForTests` (documented self-test-only) answering `ok.json` → returns 2, never 0.
    Print `=== SELF-TEST PASSED: 13/13 scenarios, every pyapi06 kind fired on its own fixture and nowhere else ===` and return 0, or `=== SELF-TEST FAILED ===` and return 1.

    Node 22 compatibility: no `--experimental` flags, no top-level await outside modules, `AbortSignal.timeout` and global fetch only (CI is Node 22, local is Node 25 — a CI-only failure is usually this).
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&1 | tail -20 && node scripts/prod-prober/run.mjs --self-test >/dev/null 2>&1; test $? -eq 0</automated>
    <fails_when>non-zero exit of the second invocation, or the tail lacks the line beginning with "=== SELF-TEST PASSED: 13/13", or any "SELF-TEST FAIL:" line appears</fails_when>
    <automated>set -o pipefail; env -i PATH="$PATH" node scripts/prod-prober/run.mjs > /tmp/prober-live.log 2>&1; status=$?; cat /tmp/prober-live.log | tail -30; test "$status" -eq 1 && grep -c "credential-absent" /tmp/prober-live.log | grep -qx 2 && grep -q "credential: ANALYTICS_SERVICE_KEY ABSENT" /tmp/prober-live.log && grep -q "credential: ANALYTICS_BASE_URL ABSENT" /tmp/prober-live.log && grep -q "1 registered arm(s) < floor 4" /tmp/prober-live.log</automated>
    <fails_when>exit status is not exactly 1, or fewer/more than 2 credential-absent rows, or either "credential: ... ABSENT" line is missing, or the floor line "1 registered arm(s) < floor 4" is absent</fails_when>
  </verify>
  <acceptance_criteria>
    - `node -e 'import("./scripts/prod-prober/run.mjs").then(m => { if (m.ARMS_FLOOR !== 4 || m.SELF_TEST_SCENARIOS !== 13 || m.DEFECT_KINDS.length !== 20 || m.ARMS.length !== 1) process.exit(1); console.log("exports ok") })'` prints `exports ok`
    - `grep -c '^=== SELF-TEST' <(node scripts/prod-prober/run.mjs --self-test 2>&1)` prints `14` (13 scenario headers + the PASSED line)
    - `grep -n 'process.exit' scripts/prod-prober/run.mjs` shows exactly ONE occurrence, on the endsWith-guarded CLI line
    - `grep -c 'process.env' scripts/prod-prober/arms/pyapi06.mjs scripts/prod-prober/seams.mjs` prints `0` for both files (arms and seams read env only through the injected `env`)
    - `grep -c 'kind === "' scripts/prod-prober/run.mjs` is at least 12 and `grep -c '!defects.some' scripts/prod-prober/run.mjs` prints `0` (absences go through noDefectOfKind)
    - the live no-credential run's log contains no line matching the regex `^credential: [A-Z_]+ present` and no `=` after a credential name (values never printed)
    - `ls scripts/prod-prober/fixtures/pyapi06/ | wc -l` prints `6`
  </acceptance_criteria>
  <done>The prober skeleton runs one arm end to end: `--self-test` passes 13/13 through the real `runProber`, the live no-credential run exits 1 naming both absent credentials and the `1 < 4` floor, and every pyapi06 kind is proven to fire on its own fixture and nowhere else.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the harness — fixture/scenario isolation checks, the header contract, and the Node-22 smoke</name>
  <files>scripts/prod-prober/run.mjs, scripts/prod-prober/arms/pyapi06.mjs</files>
  <read_first>
    - scripts/prod-prober/run.mjs (the file Task 1 created — read it in full before editing)
    - scripts/prod-prober/arms/pyapi06.mjs
    - scripts/lint-sql-gates.mjs:1307-1343 (the per-rule red/green loop shape)
    - src/__tests__/mutation-runner-floors.test.ts:669-685 (how the plan-05 extractor will read selfTest's source — every kind must be asserted by a strict-equality comparison on the kind string somewhere in selfTest)
    - .github/workflows/ci.yml:1300-1309 (how CI reads a floor constant back off the module and MEASURE_FAILs on nothing)
  </read_first>
  <action>
    Add a generic per-arm isolation loop INSIDE `selfTest()` that plans 03 and 04 extend rather than re-invent: a table `ARM_FIXTURE_TABLE = [{ arm: PYAPI06_ARM, fixtureDir: "pyapi06", green: "ok.json", red: { "keyed-401.json": "pyapi06-keyed-refused", "absent-200.json": "pyapi06-absent-accepted", "absent-401-uncoded.json": "pyapi06-absent-uncoded", "wrong-key-200.json": "pyapi06-wrong-key-accepted", "health-degraded.json": "pyapi06-health-degraded" } }]`. The loop is what scenarios 2-6 already do by hand — refactor them to iterate the table so each red fixture must (a) exist on disk (missing → SELF-TEST FAIL, not skip), (b) produce EXACTLY ONE defect, (c) of the named kind, (d) on the named arm, (e) carrying a non-empty remedy equal to `ARM.REMEDIES[kind]`; and the green fixture must produce zero defects. Keep the scenario headers counted (still 13/13 — the loop prints one header per fixture so the count is unchanged; if you restructure the numbering, renumber every `k/13` and `SELF_TEST_SCENARIOS` in ONE edit).

    Add an ABSENCE calibration to the isolation loop: after a red fixture fires its kind, assert `noDefectOfKind(defects, DEFECT_KINDS.filter(k => k !== expectedKind))` — spelled through the kind LIST so the plan-05 extractor cannot credit it as coverage of the other kinds.

    Add `REMEDIES` sanity into scenario 1: every kind in `DEFECT_KINDS` that starts with `pyapi06-` has a `REMEDIES` entry of at least 40 characters, and no two pyapi06 remedies are identical strings.

    Header: add a `WHAT DEFECT CLASS THIS EXISTS FOR` block to run.mjs (mirror the mutation-runner header) naming the four measured outages in one paragraph each (PYAPI-06 five 401s with zero mismatch lines 2026-08-25; CRON-OBS-01 jobid 1 401 hourly for 7 days behind a green cron history; CRON-DRIFT-01 the inline key nobody compared; MT5-WEDGE-OBS-01 the modal dialog behind a healthy container) and the standing rule that `cron.job_run_details.status` is not evidence — read `net._http_response`.

    Node-22 smoke: if `/opt/homebrew/opt/node@22/bin/node` exists on the box, run the self-test with it too (`PATH=/opt/homebrew/opt/node@22/bin:$PATH node --version` must print v22) and record the result in the SUMMARY; if it does not exist, record `node@22 not installed locally — CI is the Node-22 proof` in the SUMMARY. Do not install anything.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test >/dev/null 2>&1; test $? -eq 0 && node -e 'import("./scripts/prod-prober/run.mjs").then(m => { const src = require("node:fs").readFileSync("scripts/prod-prober/run.mjs","utf8"); const body = src.slice(src.indexOf("function selfTest()")); const hit = new Set([...body.matchAll(/kind === "([a-z0-9-]+)"/g)].map(x => x[1])); const need = m.DEFECT_KINDS.filter(k => k.startsWith("pyapi06-") || ["credential-absent","measure-fail","floor","absurdity"].includes(k)); const miss = need.filter(k => !hit.has(k)); if (miss.length) { console.error("uncovered:", miss); process.exit(1) } console.log("coverage ok:", need.length) })'</automated>
    <fails_when>non-zero exit, or stderr prints "uncovered:" naming any kind, or "coverage ok:" is absent from stdout</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ARM_FIXTURE_TABLE' scripts/prod-prober/run.mjs` is at least 2 (declared and iterated)
    - `grep -c '^=== SELF-TEST' <(node scripts/prod-prober/run.mjs --self-test 2>&1)` still prints `14`
    - `grep -c 'WHAT DEFECT CLASS THIS EXISTS FOR' scripts/prod-prober/run.mjs` prints `1`
    - `grep -c 'net._http_response' scripts/prod-prober/run.mjs` is at least 1 (the standing rule is stated in the header)
    - the SUMMARY records the Node-22 smoke outcome as one of the two exact sentences named in the action
    - deleting one red fixture file (e.g. `mv scripts/prod-prober/fixtures/pyapi06/absent-200.json /tmp/`) makes `--self-test` exit 1 with a `SELF-TEST FAIL:` line naming the missing fixture; restore the file (`mv` it back) and confirm `git status --short scripts/prod-prober/fixtures` is clean — record both observations in the SUMMARY
  </acceptance_criteria>
  <done>The self-test harness is table-driven per arm (missing fixture = FAIL, red fires exactly one kind, green fires none, remedy attached), every pyapi06 and harness kind is asserted by name inside `selfTest()`, the header states the defect class, and the fixture-deletion RED was observed and restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prober → analytics-service (public Railway URL) | outbound HTTPS carrying the production service key in a header |
| prober stdout/stderr → public Actions log (plan 05) | everything the script prints is world-readable once the workflow lands |
| fixtures → git (public repo) | committed test inputs must contain no real key material |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.1-01 | Information disclosure | run.mjs log lines, defect details, remedies | high | mitigate | Names only; the scrubber replaces every requiredEnv VALUE before logging; self-test scenario 12 injects sentinel values and asserts they never appear; response headers are never printed and bodies are capped at 200 chars and printed only on a defect |
| T-164.1-02 | Spoofing | pyapi06 arm's own key wrong vs server broken | medium | mitigate | The three-request control (keyed / absent / wrong) separates "our key is wrong" (`pyapi06-keyed-refused`) from "the server accepts anything" (`-absent-accepted`, `-wrong-key-accepted`) — each is a distinct kind with a distinct remedy |
| T-164.1-03 | Tampering | fixtures/pyapi06/*.json | low | accept | Fixtures are obviously fake, contain no key material and drive only the self-test; a tampered fixture reds the self-test rather than the live run |
| T-164.1-04 | Denial of service | three GETs per hour to a 30/min guarded route | low | accept | `lookback_days=1`, no retry loop, four requests per run (RESEARCH Pitfall 7) |
| T-164.1-05 | Repudiation | an arm silently not running | high | mitigate | ARMS_FLOOR literal + absurdity cross-check (per-arm seam tally vs executed) + `credential-absent` per name; a blocked or vanished arm is a defect, never a quiet shrink |
| T-164.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs NO package; Node built-ins only (`node:fs`, `node:crypto`, global fetch). Any future install goes through the package-legitimacy gate |
</threat_model>

<verification>
- `node scripts/prod-prober/run.mjs --self-test` exits 0 with `=== SELF-TEST PASSED: 13/13`.
- `env -i PATH="$PATH" node scripts/prod-prober/run.mjs` exits 1 with two `credential-absent` rows and the `1 registered arm(s) < floor 4` line — the floor tripwire is live until plans 03/04 register the remaining arms.
- `npx vitest run src/__tests__/contracts 2>&1 | tail -20` stays green (contract tests scan all of `src/`; this plan adds nothing under `src/`, so any new red there is a pre-existing condition to report, not to fix here).
</verification>

<success_criteria>
- Success criterion 1 (partial, PYAPI-06 arm): the arm exits non-zero on a missing or mismatched analytics service key, proven by fixture injection and by the credential-removal scenario; a SKIP is structurally impossible (no code path returns 0 with an absent credential).
- The seam/arm/defect contract that plans 03-05 build on is fixed and documented in the run.mjs header.
</success_criteria>

<output>
Create `.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-01-SUMMARY.md` when done. Record: the exact `arms:` and `seam-invocations:` lines of scenario 1, the two `credential-absent` rows of the live run, the fixture-deletion RED line, and the Node-22 smoke sentence.
</output>