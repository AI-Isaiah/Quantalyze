---
phase: 164.1-prod-observability-one-periodic-prober-four-targets-the-anal
plan: 03
type: execute
wave: 2
depends_on: ["164.1-01"]
files_modified:
  - scripts/prod-prober/run.mjs
  - scripts/prod-prober/seams.mjs
  - scripts/prod-prober/arms/cron-obs.mjs
  - scripts/prod-prober/arms/cron-drift.mjs
  - scripts/prod-prober/fixtures/cron-obs/ok.json
  - scripts/prod-prober/fixtures/cron-obs/401.json
  - scripts/prod-prober/fixtures/cron-obs/no-response.json
  - scripts/prod-prober/fixtures/cron-obs/no-runs.json
  - scripts/prod-prober/fixtures/cron-obs/timed-out.json
  - scripts/prod-prober/fixtures/cron-obs/recent-run-pending.json
  - scripts/prod-prober/fixtures/cron-drift/manifest.json
  - scripts/prod-prober/fixtures/cron-drift/manifest-inline-key.json
  - scripts/prod-prober/fixtures/cron-drift/prod-ok.json
  - scripts/prod-prober/fixtures/cron-drift/prod-ok-shuffled-crlf.json
  - scripts/prod-prober/fixtures/cron-drift/prod-extra-job.json
  - scripts/prod-prober/fixtures/cron-drift/prod-missing-job.json
  - scripts/prod-prober/fixtures/cron-drift/prod-schedule-moved.json
  - scripts/prod-prober/fixtures/cron-drift/prod-active-flipped.json
  - scripts/prod-prober/fixtures/cron-drift/prod-inline-key.json
  - scripts/prod-prober/fixtures/cron-drift/prod-zero-rows.json
  - scripts/prod-prober/fixtures/cron-drift/prod-duplicate-jobname.json
  - scripts/prod-prober/fixtures/cron-drift/hygiene-red.json
  - scripts/prod-prober/fixtures/cron-drift/hygiene-green.json
  - scripts/prod-prober/fixtures/cron-drift/manifest-sha-only.json
autonomous: true
requirements: [CRON-OBS-01, CRON-DRIFT-01]
tags: [prod-prober, pg_net, cron, drift, manifest]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CRON-OBS-01: the arm reads `net._http_response` joined by TIME WINDOW to `cron.job_run_details` for the `match_engine_cron` job — never `cron.job_run_details.status` — and a `succeeded` run with ZERO response rows inside its window is a `cron-no-observation` defect, not a pass."
    - "CRON-OBS-01 boundary (resolved, explicit): the scan window is runs with `start_time` in `(now - 3h, now - 120s]`; a run younger than 120 s with no response yet is NOT a defect (fixture `recent-run-pending.json` is green); a run with no response at exactly 121 s IS (fixture `no-response.json`); the response match window is `[start_time, start_time + 90s]`."
    - "CRON-OBS-01 precision (resolved, explicit): `pg_net.ttl` is read via `current_setting('pg_net.ttl', true)` and PRINTED every run; a NULL reads as the documented default `6 hours`; if the TTL parses to less than the 3 h scan window the window shrinks to the TTL and the shrink is printed; timestamps are compared as ISO-8601 instants in UTC, never as local strings."
    - "CRON-OBS-01 non-2xx: any matched response with `status_code` outside 200-299 is `cron-non-2xx` printing `id`, `status_code`, `created` and the first 200 chars of `content`; `timed_out = true` or `error_msg IS NOT NULL` is `cron-transport-error`; `headers` is never selected."
    - "CRON-OBS-01 empty-table rule: ZERO runs in the window is `cron-no-observation` (the job is hourly; three hours with no run is itself the failure); a psql non-zero exit is `measure-fail`, never zero rows."
    - "D-13 / D-14: the drift arm compares PROD `cron.job` (ALL rows) against `scripts/prod-prober/cron-manifest.json` keyed by `jobname` — set equality first (extra job = drift, missing job = drift), then per-job `schedule`, `active`, `command_sha256` of the NORMALIZED command; `jobid` is informational only."
    - "CRON-DRIFT-01 ordering / encoding (resolved, explicit): comparison is by jobname map, so PROD row order is irrelevant; `normalizeCommand` = CRLF→LF, trim, collapse every whitespace run to one space, before sha256 over UTF-8 bytes; jobname and schedule compare byte-exact after trim (no case folding); fixture `prod-ok-shuffled-crlf.json` is green."
    - "CRON-DRIFT-01 adjacency / empty (resolved, explicit): two PROD rows with the same jobname is `cron-drift` (duplicate); PROD returning zero rows is `cron-drift` (detail names zero rows) — never 'no drift'; a missing, unparsable, empty, or `match_engine_cron`-less manifest is `manifest-invalid`."
    - "CRON-DRIFT-01 precision (resolved, explicit): `active` is compared as a boolean parsed from psql's `t`/`f`; `schedule` is a string; nothing is numeric except `jobid`, which is not compared."
    - "CRON-DRIFT-01 boundary (resolved, explicit): two commands whose NORMALIZED text is byte-equal are not drift (sha equal, fixture `prod-ok-shuffled-crlf.json`); one differing non-whitespace byte after normalization IS drift (fixture `prod-schedule-moved.json` for schedule, and the inline-key pair for command); a manifest with exactly ONE job (`match_engine_cron`) is valid, with ZERO jobs invalid; a PROD side with exactly one row equal to a one-job manifest is green and with one row plus any extra is drift."
    - "D-13 secret hygiene: `hygieneViolations(jobname, command)` flags TEN shapes — an `X-Service-Key` header carrying a quoted literal, any `current_setting('app.` reference, (for `match_engine_cron` only) the ABSENCE of `vault.decrypted_secrets`, `PGPASSWORD`/`password=`, an `Authorization` header carrying a quoted literal, a `Bearer` literal that continues inside its own quotes, an `apikey` header carrying a quoted literal, the token `service_role` anywhere, a JWT-shaped literal (`eyJ…`) anywhere, and ANY quoted literal of 32+ characters inside a `headers :=` / `jsonb_build_object(` argument list — every rule has a RED fixture row in `hygiene-red.json` proving it fires and a GREEN control in `hygiene-green.json` proving the achievable Vault shapes do not; it runs on BOTH sides — a PROD command failing it is `cron-secret-in-command` EVEN IF its sha matches the manifest (fixture `prod-inline-key.json` with `manifest-inline-key.json`), and `captureManifest` REFUSES to write a manifest whose rows fail it. The repo is PUBLIC and `.planning/` is tracked: the manifest and the Actions log are world-readable, and rows beyond jobid 1 have never been read by anyone (RESEARCH F3)."
    - "Manifest `command` is OPTIONAL per row: a row may carry `command_sha256` only (with `command_withheld: true` and a `withheld_reason`), a reviewer's act in plan 06 for any row whose text they choose not to publish; a sha-only row still detects drift (fixture `manifest-sha-only.json` is green against `prod-ok.json` and `cron-drift` against `prod-schedule-moved.json`), and the arm never prints a diff for it."
    - "D-15: on drift the arm prints `cron-drift: manifest captured <captured_at> (marker <db marker>) sha <a> — PROD now sha <b>` per differing job plus the two readings (PROD moved after the capture / the manifest was updated ahead of PROD) and the remedy for each; command TEXT (the unified diff) is printed only when BOTH sides pass hygiene AND the manifest row carries `command`, otherwise sha only with a `command text withheld: <why>` line."
    - "D-19: the SQL seam spawns `psql` with the pooler URL from `env.PROBER_POOLER_URL` and the password ONLY as `PGPASSWORD` in the child env, `-v ON_ERROR_STOP=1 -At -F <tab>`, a 60 s timeout, and never prints the URL, the DSN, or stderr beyond the first line with the URL redacted."
    - { statement: "CRON-OBS-01 concurrency edge: two overlapping prober runs read the same two tables and write nothing, so they cannot corrupt each other or PROD; the workflow serializes runs anyway (`concurrency: group: prod-prober`, plan 05)", verification: backstop }
  artifacts:
    - "scripts/prod-prober/arms/cron-obs.mjs — ARM {name:'cron-obs'}, CRON_OBS_SQL, judgeCronObs(rows, now, ttl), REMEDIES"
    - "scripts/prod-prober/arms/cron-drift.mjs — ARM {name:'cron-drift'}, MANIFEST_PATH, MANIFEST_SCHEMA_VERSION, NORMALIZATION, normalizeCommand, hygieneViolations, compareManifest, CRON_JOB_SQL, DB_MARKER_SQL, REMEDIES"
    - "scripts/prod-prober/seams.mjs — realSqlRunner(env)"
    - "scripts/prod-prober/run.mjs — captureManifest, the --capture-manifest branch, ARMS now [pyapi06, cron-obs, cron-drift], SELF_TEST_SCENARIOS = 39"
    - "scripts/prod-prober/fixtures/cron-obs/*.json and fixtures/cron-drift/*.json"
  key_links:
    - "seams.sql(arm, query) → realSqlRunner → spawnSync('psql', [url, '-v', 'ON_ERROR_STOP=1', '-At', '-F', tab, '-c', query], { env: { PGPASSWORD } }) → { status, stdout, stderr, timedOut, measureFail }"
    - "cron-obs: rows (tab-separated) → judgeCronObs(rows, seams.now(), ttl) → defects"
    - "cron-drift: manifest.json (LEFT) + PROD rows (RIGHT) → hygieneViolations on both → compareManifest → defects with both readings"
    - "captureManifest: DB_MARKER_SQL + CRON_JOB_SQL → hygiene → write JSON with captured_at, marker, normalization, rows"
  prohibitions:
    - { requirement_id: CRON-OBS-01, category: safety, status: resolved, verification: test, statement: "MUST NOT treat `cron.job_run_details.status = 'succeeded'` as evidence of anything — the arm never selects that column for judgement (the standing rule in TODOS)" }
    - { requirement_id: CRON-OBS-01, category: safety, status: resolved, verification: test, statement: "MUST NOT read an empty psql stdout as zero problems — a non-zero psql exit is `measure-fail` and zero runs in the window is `cron-no-observation`" }
    - { requirement_id: CRON-DRIFT-01, category: privacy, status: resolved, verification: test, statement: "MUST NOT print, log, or write into the manifest any `cron.job.command` text that fails the secret-hygiene assertions, and MUST NOT print the pooler URL, DSN, password, or `_http_response.headers` — the repo and the Actions log are public" }
    - { requirement_id: CRON-DRIFT-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT derive the oracle from `supabase/migrations/**` and MUST NOT add a migration or run `supabase db push` — the manifest is captured from PROD read-only and committed as a file" }
    - { requirement_id: CRON-DRIFT-01, category: safety, status: resolved, verification: test, statement: "MUST NOT issue any statement other than SELECT against PROD: `CRON_OBS_SQL`, `CRON_JOB_SQL` and `DB_MARKER_SQL` are single SELECT statements and the self-test asserts each begins with SELECT/WITH and contains no INSERT/UPDATE/DELETE/ALTER/CREATE/DROP token" }
---

<objective>
Add the two PROD-database arms to the prober: CRON-OBS-01 (the async `net._http_response` result
that `cron.job_run_details` cannot see) and CRON-DRIFT-01 (PROD `cron.job` versus a committed
manifest of the ACHIEVABLE Vault-backed command), plus the `--capture-manifest` mode that writes
that manifest from PROD after hygiene assertions, plus a `psql` seam that never leaks a DSN.

Purpose: PROD jobid 1 returned 401 hourly for seven days behind a green cron history because
`net.http_post` is async and nothing read the response table; and nothing compared PROD's cron
jobs to anything, so an inline key sat in `cron.job.command` for months. Both arms are proven to
bite on fixtures before they ever touch PROD.

Output: `arms/cron-obs.mjs`, `arms/cron-drift.mjs`, `realSqlRunner`, twenty fixtures (6 cron-obs +
14 cron-drift), `captureManifest`, and `--self-test` at 39/39.

Scenario arithmetic (the counting rule is plan 01 Task 2's: one counted header per
ARM_FIXTURE_TABLE fixture, green included — pyapi06's six fixtures are scenarios 1-6 — plus one per
hand-written scenario; loop EXTENSIONS such as scenario 8's per-arm credential iteration are not
new headers): plan 01 leaves 13; Task 1 adds 9 (13 → 22); Task 2 adds 17 (22 → 39). Each task
enumerates its own additions in its action. Plan 04 adds 11 (39 → 50); plans 05 and 06 pin 50.

Sizing note (plan-check W1, decided: NOT split). The estimate is uncalibrated (0 samples) and 17 of
the 24 files are fixture JSONs of under 40 lines each; the code is two arm modules, one seam and
the scenario table. A split would have to put Task 2 in its own plan touching `run.mjs`, which plan
04 also touches — so it would either share a wave with plan 04 (a file conflict) or shift plans
04-06 down a wave, and the checker asked for the wave structure to be preserved. The mitigation is
the natural one: Task 1 commits on its own (22/22) before Task 2 starts, so a context reset between
them loses nothing.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-CONTEXT.md
@.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-RESEARCH.md
@scripts/prod-body-drift-check.sh

Also read before starting (created by plan 01, so not `@`-referenced here): the plan-01 SUMMARY in
this phase directory, `scripts/prod-prober/run.mjs` and `scripts/prod-prober/seams.mjs`.

Decision register: plan 01's context block (D-01..D-20). This plan implements D-13, D-14, D-15
and the SQL half of D-19, and closes CRON-OBS-01 / CRON-DRIFT-01 at the script layer.

## Artifacts this phase produces (this plan's share)

| Artifact | Symbol |
|---|---|
| `scripts/prod-prober/arms/cron-obs.mjs` | `ARM` (`name: "cron-obs"`, `requiredEnv: ["PROBER_POOLER_URL", "SUPABASE_DB_PASSWORD"]`), `CRON_OBS_SQL`, `TTL_SQL`, `judgeCronObs`, `REMEDIES` |
| `scripts/prod-prober/arms/cron-drift.mjs` | `ARM` (`name: "cron-drift"`, same `requiredEnv`), `MANIFEST_PATH = "scripts/prod-prober/cron-manifest.json"`, `MANIFEST_SCHEMA_VERSION = 1`, `NORMALIZATION = "ws-collapse-v1"`, `CRON_JOB_SQL`, `DB_MARKER_SQL`, `normalizeCommand`, `hygieneViolations`, `compareManifest`, `REMEDIES` |
| `scripts/prod-prober/seams.mjs` | `realSqlRunner(env)` |
| `scripts/prod-prober/run.mjs` | `captureManifest`, `--capture-manifest --out <path>`, `ARMS` = three arms, `SELF_TEST_SCENARIOS = 39` |
| `scripts/prod-prober/cron-manifest.json` | NOT written by this plan — captured from PROD in plan 06 through the workflow's `capture-manifest` mode; until then the live drift arm reports `manifest-invalid` (missing file), which is the honest state |

The full phase artifact table is in plan 01's context block.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The psql seam and the CRON-OBS-01 arm — join `net._http_response` to the job's runs by time window, judge in Node, prove every kind on fixtures</name>
  <reversibility rating="reversible">Query text and judgement thresholds live in one module; changing the window or the join is a local edit with the fixtures as the contract.</reversibility>
  <files>scripts/prod-prober/seams.mjs, scripts/prod-prober/arms/cron-obs.mjs, scripts/prod-prober/run.mjs, scripts/prod-prober/fixtures/cron-obs/ok.json, scripts/prod-prober/fixtures/cron-obs/401.json, scripts/prod-prober/fixtures/cron-obs/no-response.json, scripts/prod-prober/fixtures/cron-obs/no-runs.json, scripts/prod-prober/fixtures/cron-obs/timed-out.json, scripts/prod-prober/fixtures/cron-obs/recent-run-pending.json</files>
  <read_first>
    - scripts/prod-prober/run.mjs (plan 01's file — the ARM_FIXTURE_TABLE loop, the scenario numbering rule, the credential gate)
    - scripts/prod-prober/seams.mjs (the sql seam stub that throws `seam sql not wired`, the result shape, the tally)
    - scripts/prod-body-drift-check.sh:24-35 (never skip / never print PROD text / never pass what was not compared)
    - .github/workflows/ci.yml:2156 (how CI installs psql on ubuntu — the workflow in plan 05 copies it; nothing to install here)
    - supabase/migrations/20260408215026_schedule_match_cron_hourly.sql:65-79 (the job's 60000 ms request timeout that sizes the 90 s match window)
  </read_first>
  <action>
    In `scripts/prod-prober/seams.mjs` add `export function realSqlRunner(env)` returning a function `(query) => result` that spawns `psql` via `spawnSync` with argv `[env.PROBER_POOLER_URL, "-v", "ON_ERROR_STOP=1", "-At", "-F", "\t", "-X", "-c", query]`, `timeout: 60000`, `encoding: "utf8"`, and a child env of `{ PATH: env.PATH, PGPASSWORD: env.SUPABASE_DB_PASSWORD, PGCONNECT_TIMEOUT: "15" }` — the password is never placed in the URL and the URL is never logged. Map the outcome: `error.code === "ENOENT"` → `measureFail: "psql not found on PATH"`; `signal` or `error.code === "ETIMEDOUT"` → `timedOut: true, measureFail: "psql timed out after 60s"`; non-zero status → `measureFail: "psql exited <status>: <first stderr line with env.PROBER_POOLER_URL replaced by <pooler-url>>"`; status 0 → `{ status: 0, stdout, stderr, timedOut: false, measureFail: null }`. Wire `createSeams({ sqlRunner })` so `seams.sql(arm, query)` increments the tallies then delegates. Document in the docblock that an empty stdout with status 0 is a legitimate zero-row answer and the ARM decides what zero rows means — the seam never does.

    Create `scripts/prod-prober/arms/cron-obs.mjs` exporting `ARM = { name: "cron-obs", requiredEnv: ["PROBER_POOLER_URL", "SUPABASE_DB_PASSWORD"], run, REMEDIES }`, `TTL_SQL` (a single SELECT of `current_setting('pg_net.ttl', true)`), `CRON_OBS_SQL` and `judgeCronObs`. `CRON_OBS_SQL` is ONE statement: a CTE `j` selecting the `jobid` of `cron.job` where `jobname = 'match_engine_cron'`; a CTE `runs` selecting `runid`, `start_time` from `cron.job_run_details` where `jobid IN (SELECT jobid FROM j)` and `start_time > now() - interval '3 hours'` (bounded scan — that table is never auto-cleaned); then a LEFT JOIN to `net._http_response h` on `h.created BETWEEN r.start_time AND r.start_time + interval '90 seconds'`, selecting `r.runid`, `r.start_time` (as `to_char(... AT TIME ZONE 'UTC', 'YYYY-MM-DD"T"HH24:MI:SS"Z"')`), `h.id`, `h.status_code`, `h.timed_out`, `h.error_msg`, `h.created` (same UTC format), `left(h.content, 200)`, plus a first row-less probe of `(SELECT count(*) FROM j)` so the arm can tell "job not scheduled" from "no runs" — never select `h.headers`, never select `job_run_details.status`. The self-test asserts every exported SQL constant starts with `SELECT` or `WITH` and contains none of the tokens INSERT, UPDATE, DELETE, ALTER, CREATE, DROP (word-bounded, case-insensitive).

    `run({ env, seams, log, addDefect })`: (1) `seams.sql("cron-obs", TTL_SQL)`; a `measureFail` → `addDefect("measure-fail", ...)` and return; parse the TTL (`NULL`/empty → the default `6 hours`; else Postgres interval text — support `N hours`, `HH:MM:SS`, `N minutes`); print `cron-obs: pg_net.ttl = <text> (<seconds> s)`; (2) `seams.sql("cron-obs", CRON_OBS_SQL)`; `measureFail` → `measure-fail`; (3) parse the tab-separated rows and hand them with `seams.now()` and the TTL seconds to `judgeCronObs(rows, now, ttlSeconds, log)`, which returns `[{ kind, subject, detail }]` and is a PURE function so fixtures can drive it. Judgement: the effective window is `min(3 h, ttl)` (print a `cron-obs: window shrunk to <ttl> because pg_net.ttl is shorter than 3h` line when it shrinks); consider only runs with `start_time <= now - 120 s` (younger runs may still be in flight: NOT a defect); zero eligible runs → one `cron-no-observation` with detail `no match_engine_cron run in the last <window>` (or `match_engine_cron is not scheduled in cron.job` when the job-count probe is 0 — the drift arm will also flag it); a run whose `id` is NULL → `cron-no-observation` naming the `runid` and `start_time`; a response with `timed_out = t` or non-empty `error_msg` → `cron-transport-error` with `id`, `created`, and the error text (error_msg only — never content); a response with `status_code` outside 200-299 → `cron-non-2xx` with `id`, `status_code`, `created` and the 200-char content head; otherwise print `cron-obs: run <runid> at <start_time> → <status_code> (id <id>)`. Every defect carries its `REMEDIES` entry: no-observation → check `cron.job` is scheduled and pg_net's worker is running (`SELECT net.worker_restart()` is the documented operator remedy, run by a human, never by this prober); non-2xx 401 → the job's key no longer matches Railway `SERVICE_KEY` — re-point the Vault secret `analytics_service_key` and verify the next `_http_response` row; transport-error → the analytics service did not answer within 60 s — read Railway logs for the tick.

    Fixtures under `scripts/prod-prober/fixtures/cron-obs/` as JSON `{ "now": "<ISO>", "ttl": "6 hours", "rows": [[runid, start_time, id, status_code, timed_out, error_msg, created, content_head], ...] }` with `now` fixed at `2026-09-05T12:00:00Z` so the fixtures are deterministic: `ok.json` three hourly runs each with a 200 row; `401.json` one run whose row is 401 with content `{"detail":"Unauthorized"}`; `no-response.json` a run at `11:57:59Z` (121 s old) with NULL response columns → `cron-no-observation`; `no-runs.json` zero rows and job-count 1 → `cron-no-observation`; `timed-out.json` a run whose row has `timed_out = t` → `cron-transport-error`; `recent-run-pending.json` one 200 run plus a run at `11:59:00Z` (60 s old) with NULL response columns → GREEN. The fixture sql seam for this arm answers `TTL_SQL` with the fixture's `ttl` and `CRON_OBS_SQL` with the rows rendered as tab-separated lines, and the fixture clock returns `now`.

    In `run.mjs`: import the arm, append it to `ARMS`, add a row to `ARM_FIXTURE_TABLE` (green `ok.json`; red `401.json` → `cron-non-2xx`, `no-response.json` → `cron-no-observation`, `no-runs.json` → `cron-no-observation`, `timed-out.json` → `cron-transport-error`), add scenario `recent-run-pending.json` → exit 0 (the boundary calibration), a scenario where the sql seam returns `measureFail: "psql exited 1: permission denied"` → exactly one `measure-fail` on `cron-obs` and `noDefectOfKind(defects, ["cron-no-observation", "cron-non-2xx", "cron-transport-error"])`, a scenario where the TTL answers `2 hours` and the fixture has a NULL-response run at 2.5 h → GREEN because the window shrank (and the shrink line was logged), and the SQL-is-read-only assertion scenario over every exported `*_SQL` constant of every arm. Enumerated, this task adds NINE counted headers: the cron-obs table row is 5 (green `ok.json` + red `401.json`, `no-response.json`, `no-runs.json`, `timed-out.json`), then `recent-run-pending` (1), sql `measureFail` (1), TTL-shrink (1), SQL-read-only (1). Renumber the counted headers and `SELF_TEST_SCENARIOS` in ONE edit (13 → 22 after this task; Task 2 takes it to 39). Extend scenario 8 (credential-absent) to iterate EVERY registered arm's `requiredEnv`, asserting for each deleted name that the OTHER arms still executed (`armsExecuted === registered - 1` for names unique to one arm; for the shared `PROBER_POOLER_URL`/`SUPABASE_DB_PASSWORD` names both SQL arms are blocked and `armsExecuted === registered - 2`).
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&1 | tail -12 && node scripts/prod-prober/run.mjs --self-test >/dev/null 2>&1; test $? -eq 0</automated>
    <fails_when>non-zero exit of the second invocation, or the tail lacks "=== SELF-TEST PASSED: 22/22", or any "SELF-TEST FAIL:" line</fails_when>
    <automated>node -e 'import("./scripts/prod-prober/arms/cron-obs.mjs").then(m => { const sqls = Object.entries(m).filter(([k]) => k.endsWith("_SQL")).map(([,v]) => v); if (sqls.length < 2) process.exit(1); for (const s of sqls) { if (!/^\s*(SELECT|WITH)\b/i.test(s) || /\b(INSERT|UPDATE|DELETE|ALTER|CREATE|DROP)\b/i.test(s) || /\bheaders\b/.test(s) || /job_run_details\.status\b/.test(s)) { console.error("bad sql"); process.exit(1) } } console.log("sql read-only ok", sqls.length) })'</automated>
    <fails_when>non-zero exit or "bad sql" on stderr — a non-SELECT statement, a write token, the headers column, or the status column reached the query text</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'process.env' scripts/prod-prober/arms/cron-obs.mjs` prints `0`
    - `grep -n 'PGPASSWORD' scripts/prod-prober/seams.mjs` shows it ONLY inside the child `env` object passed to spawnSync, and `grep -c 'PROBER_POOLER_URL' scripts/prod-prober/seams.mjs` shows it used as an argv element and in the stderr redaction, never in a log call
    - `ls scripts/prod-prober/fixtures/cron-obs | wc -l` prints `6`
    - `node -e 'import("./scripts/prod-prober/run.mjs").then(m => { if (m.ARMS.length !== 2 || m.SELF_TEST_SCENARIOS !== 22) process.exit(1); console.log("ok") })'` prints `ok`
    - `env -i PATH="$PATH" node scripts/prod-prober/run.mjs; test $? -eq 1` and its log contains `credential: PROBER_POOLER_URL ABSENT`, `credential: SUPABASE_DB_PASSWORD ABSENT`, and `2 registered arm(s) < floor 4`
    - `grep -c 'kind === "cron-no-observation"' scripts/prod-prober/run.mjs`, the same for `cron-non-2xx`, `cron-transport-error`, are each at least 1 (the plan-05 extractor will require them)
  </acceptance_criteria>
  <done>The psql seam returns a measure-fail rather than an empty answer on any failure, the CRON-OBS-01 arm judges the time-window join with the 120 s / 90 s / TTL boundaries proven by fixtures (a pending recent run is green, a 121 s-old unanswered run is red), every cron-obs kind fires on its own fixture only, and the self-test is 22/22.</done>
</task>

<task type="auto">
  <name>Task 2: The CRON-DRIFT-01 arm and `--capture-manifest` — a committed, hygiene-asserted manifest of the achievable Vault-backed cron configuration, compared by jobname with both readings recorded</name>
  <reversibility rating="costly">The manifest JSON schema (`MANIFEST_SCHEMA_VERSION = 1`, `NORMALIZATION = "ws-collapse-v1"`) is what plan 06 captures from PROD and what every future re-capture must produce; changing the schema or the normalization later invalidates the committed oracle and every sha in it.</reversibility>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs, scripts/prod-prober/fixtures/cron-drift/manifest.json, scripts/prod-prober/fixtures/cron-drift/manifest-inline-key.json, scripts/prod-prober/fixtures/cron-drift/manifest-sha-only.json, scripts/prod-prober/fixtures/cron-drift/prod-ok.json, scripts/prod-prober/fixtures/cron-drift/prod-ok-shuffled-crlf.json, scripts/prod-prober/fixtures/cron-drift/prod-extra-job.json, scripts/prod-prober/fixtures/cron-drift/prod-missing-job.json, scripts/prod-prober/fixtures/cron-drift/prod-schedule-moved.json, scripts/prod-prober/fixtures/cron-drift/prod-active-flipped.json, scripts/prod-prober/fixtures/cron-drift/prod-inline-key.json, scripts/prod-prober/fixtures/cron-drift/prod-zero-rows.json, scripts/prod-prober/fixtures/cron-drift/prod-duplicate-jobname.json, scripts/prod-prober/fixtures/cron-drift/hygiene-red.json, scripts/prod-prober/fixtures/cron-drift/hygiene-green.json</files>
  <read_first>
    - scripts/prod-prober/run.mjs (as left by Task 1)
    - scripts/prod-prober/arms/cron-obs.mjs (the SQL-arm shape to mirror)
    - scripts/prod-body-drift-check.sh:37-44 (LEFT/RIGHT with a hash-bound acknowledgment — the recording idea)
    - supabase/migrations/20260408215026_schedule_match_cron_hourly.sql:65-79 (the UNRUNNABLE GUC shape the hygiene assertion must reject; the oracle is NOT this text)
    - .planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-RESEARCH.md (Finding 3: manifest fields, the three hygiene assertions, the fifteen migration jobnames as a plausibility list — NOT the oracle)
  </read_first>
  <action>
    Create `scripts/prod-prober/arms/cron-drift.mjs` exporting `ARM = { name: "cron-drift", requiredEnv: ["PROBER_POOLER_URL", "SUPABASE_DB_PASSWORD"], run, REMEDIES }`, `MANIFEST_PATH = "scripts/prod-prober/cron-manifest.json"`, `MANIFEST_SCHEMA_VERSION = 1`, `NORMALIZATION = "ws-collapse-v1"`, `DB_MARKER_SQL` (single SELECT of `shobj_description(oid, 'pg_database')` from `pg_database` where `datname = current_database()` — the hand-set marker CLAUDE.md requires before any PROD interaction; a NULL marker is a `measure-fail` with the remedy "re-set the COMMENT ON DATABASE marker before trusting this reading"), `CRON_JOB_SQL` (single SELECT of `jobid`, `jobname`, `schedule`, `active`, `database`, `username`, `command` from `cron.job` ordered by `jobid` — no other columns), `normalizeCommand(text)` (CRLF→LF, trim, collapse every whitespace run to one space), `sha256Hex(text)` over UTF-8 via `node:crypto`, `hygieneViolations(jobname, command)` returning an array of violation strings, each beginning with a stable rule id in square brackets, over TEN rules: `[x-service-key-literal]` an `X-Service-Key` header carrying a quoted literal (`/X-Service-Key'\s*,\s*'/i`); `[app-guc]` any `current_setting('app.` reference (the GUC design that returns 42501 on Supabase); `[vault-absent]` for `jobname === "match_engine_cron"` only, the ABSENCE of the substring `vault.decrypted_secrets`; `[pg-password]` `PGPASSWORD` or `password=`; `[authorization-literal]` an `Authorization` header carrying a quoted literal (`/Authorization'\s*,\s*'/i`); `[bearer-literal]` a `Bearer` literal that continues inside its own quotes for 8+ token characters (`/'Bearer\s+[A-Za-z0-9._-]{8,}/i` — the achievable shape `'Bearer ' || <expr>` closes its quote right after the space and does NOT match); `[apikey-literal]` an `apikey` header carrying a quoted literal (`/apikey'\s*,\s*'/i`); `[service-role]` the token `service_role` anywhere (case-insensitive); `[jwt-shape]` `/eyJ[A-Za-z0-9_-]{20,}/` anywhere (a JWT header segment — the shape Supabase's own pg_cron → pg_net documentation puts in `headers`); `[long-literal-in-headers]` any single-quoted literal of 32 or more characters inside the argument list of a `headers :=` assignment or a `jsonb_build_object(` call (walk from the opener to its matching close paren, counting nesting; a short literal such as `'application/json'` must NOT fire). Each violation is a sentence naming the rule, never quoting the offending text. The four original rules were the only ones on the first draft of this plan; the six added ones exist because the repo is PUBLIC, the manifest is committed and the drift diff is printed to the public Actions log, and the rows beyond jobid 1 have never been read by anyone — a rule that cannot fire is the defect, so every rule has a RED row in `hygiene-red.json` (below). `compareManifest(manifest, prodRows)` (PURE) returning `{ defects: [{ kind, subject, detail }], lines: [] }`.

    `compareManifest` rules (D-13, D-14, D-15): validate the manifest first — missing/unparsable file, `schema_version !== 1`, `normalization !== "ws-collapse-v1"`, empty `jobs`, or no `match_engine_cron` entry → ONE `manifest-invalid` defect and return (do not compare against an invalid oracle). Then build a map by `jobname` on each side; a duplicate jobname on the PROD side → `cron-drift` (detail: `duplicate jobname <name> in PROD cron.job`); zero PROD rows → `cron-drift` (detail: `PROD cron.job returned zero rows; manifest has <n> jobs` — never "no drift"); names only in PROD → `cron-drift` per job (`unexpected job <name> in PROD`); names only in the manifest → `cron-drift` per job (`job <name> missing from PROD`); for each shared name compare `schedule` (byte-exact after trim), `active` (boolean, parsed from `t`/`f`), and `command_sha256` (of `normalizeCommand(command)`); any difference → one `cron-drift` per job whose detail is the recorded sentence `cron-drift: manifest captured <captured_at> (marker <marker>) sha <a> — PROD now sha <b>; changed: <schedule|active|command>` followed by the two readings verbatim: `reading 1: PROD moved after the capture — remedy: re-schedule PROD from the manifest`; `reading 2: the manifest was updated ahead of PROD — remedy: run the workflow in capture-manifest mode, review the artifact, commit it`; when `command` differs print a unified diff of the NORMALIZED text ONLY if `hygieneViolations` is empty on BOTH sides AND the manifest row carries `command`; otherwise print `command text withheld: <side> fails hygiene (<rule ids>)` or `command text withheld: manifest row is sha-only (<withheld_reason>)`. Manifest row schema: `command` is OPTIONAL — a row without it MUST carry `command_withheld: true` and a non-empty string `withheld_reason` (a row with neither `command` nor `command_withheld` is `manifest-invalid`); `command_sha256` is always required and is what the comparison uses, so a sha-only row detects drift exactly as a full row does. The hygiene pass over manifest rows runs only on rows that carry `command`; a withheld row was read by a human at capture time (plan 06). Independently of equality, run `hygieneViolations` on EVERY PROD row and every manifest row: any violation → `cron-secret-in-command` naming the jobname and the rule (a PROD command that equals the manifest byte for byte but carries an inline key is STILL a defect — that is what makes the oracle "achievable" rather than "whatever PROD has"). `jobid` is printed (`cron-drift: <jobname> jobid <prod> (manifest <manifest>)`) but never compared.

    `run({ env, seams, log, addDefect })`: read `MANIFEST_PATH` through `node:fs` (an absent file is `manifest-invalid`, never a skip); `seams.sql("cron-drift", DB_MARKER_SQL)` → print `cron-drift: database marker = <marker>` (the marker is a hand-set name, safe to print); `seams.sql("cron-drift", CRON_JOB_SQL)` → `measureFail` is `measure-fail`; parse tab-separated rows (command text may contain tabs/newlines — use psql's `-F` with the unit-separator character `\x1f` and `-R` with the record-separator `\x1e` for THIS query so multi-line commands survive; document why); hand `compareManifest` the parsed rows; emit its defects with `REMEDIES` (cron-drift → the two readings above; cron-secret-in-command → rotate the secret named in the rule, re-schedule the job onto the Vault-backed body, and re-capture; manifest-invalid → run capture-manifest mode and commit the artifact). Print `cron-drift: <n> PROD job(s) vs <m> manifest job(s), <k> differing`.

    `captureManifest({ seams, env, outPath, log })` in `run.mjs` (exported; wired to `--capture-manifest --out <path>`; `--out` is REQUIRED — refuse to write into the repo path implicitly, so a capture is always a reviewed file move): run `DB_MARKER_SQL` and REFUSE (return 3, write nothing) when the marker is NULL or empty; run `CRON_JOB_SQL`; run `hygieneViolations` on every row and REFUSE (return 1, write nothing, print the jobname and rule names) on any violation — the manifest can never be captured into a non-achievable state; otherwise write JSON `{ schema_version: 1, normalization: "ws-collapse-v1", captured_at: <ISO UTC from seams.now()>, database_marker: <marker>, jobs: [{ jobid, jobname, schedule, active, database, username, command_sha256, command: <normalized text> }] }` sorted by jobname, print `captured <n> job(s) from database "<marker>" at <captured_at>` and one `  <jobname> <schedule> active=<bool> sha <first 12 hex>` line per job, and return 0. The command text is written because every row passed the ten hygiene rules; say so in a comment and in the README-style docblock at the top of `cron-drift.mjs`, together with the plausibility list of the fifteen migration jobnames from RESEARCH F3 (a reviewer's checklist, explicitly NOT the oracle), and document the reviewer's withhold procedure there: to publish a row sha-only, delete its `command` key and add `command_withheld: true` plus `withheld_reason: "<why>"` — the sha stays machine-produced, so this is a redaction, not authorship, and the self-test proves such a row still compares (`manifest-sha-only.json`).

    Fixtures under `scripts/prod-prober/fixtures/cron-drift/`: `manifest.json` (schema 1, three jobs: `match_engine_cron` hourly with a Vault-backed DO block command in the achievable shape, `audit_log_cold_purge`, `retention_compute_jobs_done`, with correct `command_sha256` values computed by `normalizeCommand` + `sha256Hex` — compute them with a one-off node -e and paste); `manifest-inline-key.json` (same but `match_engine_cron`'s command carries the pre-2026-09-01 inline-literal header shape with an obviously fake literal); PROD fixtures as JSON arrays of row objects `{ jobid, jobname, schedule, active, database, username, command }`: `prod-ok.json` equals the manifest; `prod-ok-shuffled-crlf.json` same rows in reverse order with CRLF line endings and doubled spaces inside commands (must be GREEN); `prod-extra-job.json` adds `poll-allocator-positions`; `prod-missing-job.json` drops `audit_log_cold_purge`; `prod-schedule-moved.json` changes the hourly schedule to `5 * * * *`; `prod-active-flipped.json` sets `match_engine_cron.active` to `f`; `prod-inline-key.json` uses the inline-literal command (paired with `manifest-inline-key.json` in its scenario so the shas MATCH and the defect must still fire); `prod-zero-rows.json` is `[]`; `prod-duplicate-jobname.json` repeats `match_engine_cron` twice; `manifest-sha-only.json` equals `manifest.json` except that the `audit_log_cold_purge` row has no `command` and carries `command_withheld: true`, `withheld_reason: "fixture: reviewer withheld"`; `hygiene-red.json` is an array of `{ "rule": "<rule id>", "jobname": "<name>", "command": "<text>" }` with EXACTLY one row per rule id (ten rows), each command built around an obviously fake literal (`Bearer eyJfakeHeader.fakePayload.fakeSig` and the like — nothing that could ever be real), and `hygiene-green.json` an array of `{ "jobname", "command" }` controls that must produce ZERO violations: the Vault-backed `match_engine_cron` DO block, an `'Authorization', 'Bearer ' || (SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'x')` concatenation, an `'apikey', v_key` variable reference, a `'Content-Type', 'application/json'` pair (a short literal inside `jsonb_build_object(`), and a plain `SELECT retention_sweep()` with no headers at all. The fixture sql seam answers `DB_MARKER_SQL` with `quantalyze-fixture-db` and `CRON_JOB_SQL` with the rows rendered with the same separators the real seam uses; the fixture arm run reads the manifest from a per-scenario `manifestPath` option threaded through `runProber` (the live default is `MANIFEST_PATH`).

    Scenarios — SEVENTEEN new counted headers, enumerated (renumber 22 → 39 in ONE edit): (i-vii) the ARM_FIXTURE_TABLE row for cron-drift, 7 headers: green `prod-ok.json` with `manifest.json`; red `prod-extra-job.json`, `prod-missing-job.json`, `prod-schedule-moved.json`, `prod-active-flipped.json`, `prod-zero-rows.json`, `prod-duplicate-jobname.json` → each `cron-drift` exactly once — for the extra/missing pair assert the detail names the job; (viii) `prod-inline-key.json` + `manifest-inline-key.json` → `cron-secret-in-command` (and assert the shas are EQUAL in that scenario so the assertion cannot be satisfied by sha inequality); (ix) `prod-ok-shuffled-crlf.json` → GREEN; (x) `manifestPath` pointing at a nonexistent file → exactly one `manifest-invalid` and `noDefectOfKind(defects, ["cron-drift", "cron-secret-in-command"])`; (xi) the drift detail contains BOTH reading sentences and the `manifest captured` / `PROD now sha` fragments; (xii) hygiene RED — for every row of `hygiene-red.json`, `hygieneViolations(row.jobname, row.command)` is non-empty and contains a violation beginning with `[<row.rule>]`, AND the set of rule ids across the fixture equals the arm's exported `HYGIENE_RULE_IDS` (ten) member for member, so a rule added without a red row, or a red row whose rule never fires, is a SELF-TEST FAIL; (xiii) hygiene GREEN — every `hygiene-green.json` control returns `[]` (the false-positive calibration: the achievable shapes are not flagged); (xiv) sha-only manifest — `manifest-sha-only.json` against `prod-ok.json` → GREEN, and against `prod-schedule-moved.json` → exactly one `cron-drift` whose detail contains `command text withheld: manifest row is sha-only`, and no diff line (assert no captured log line starts with `+` or `-` followed by SQL); (xv) `captureManifest` with the inline-key PROD rows → returns 1 and the out path does not exist afterwards; (xvi) `captureManifest` with `prod-ok.json` rows and a fixed clock → returns 0 and the written file parses to schema 1 with three jobs, the marker `quantalyze-fixture-db`, and shas equal to the manifest fixture's; (xvii) `captureManifest` with a NULL marker → returns 3 and writes nothing. Run every capture scenario against a `mkdtemp` directory, never the repo path. Export `HYGIENE_RULE_IDS` (the ten ids, a literal array) from `cron-drift.mjs` so scenario (xii) and plan 06's review can range over it.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&1 | tail -12 && node scripts/prod-prober/run.mjs --self-test >/dev/null 2>&1; test $? -eq 0</automated>
    <fails_when>non-zero exit of the second invocation, or the tail lacks "=== SELF-TEST PASSED: 39/39", or any "SELF-TEST FAIL:" line</fails_when>
    <automated>node -e 'import("./scripts/prod-prober/arms/cron-drift.mjs").then(m => { const h = m.hygieneViolations; const red = JSON.parse(require("fs").readFileSync("scripts/prod-prober/fixtures/cron-drift/hygiene-red.json","utf8")); const green = JSON.parse(require("fs").readFileSync("scripts/prod-prober/fixtures/cron-drift/hygiene-green.json","utf8")); const missed = red.filter(r => !h(r.jobname, r.command).some(v => v.startsWith("[" + r.rule + "]"))).map(r => r.rule); const falsePos = green.filter(g => h(g.jobname, g.command).length).map(g => g.jobname); const ids = [...new Set(red.map(r => r.rule))].sort(); const want = [...m.HYGIENE_RULE_IDS].sort(); if (missed.length || falsePos.length || ids.length !== 10 || JSON.stringify(ids) !== JSON.stringify(want)) { console.error("hygiene", { missed, falsePos, ids, want }); process.exit(1) } console.log("hygiene ok", ids.length, "rules, each fired by its red row, no green control flagged") })'</automated>
    <fails_when>non-zero exit or "hygiene" on stderr: a red row failed to fire its named rule (`missed`), a green control was flagged (`falsePos`), or the fixture's rule ids are not exactly the arm's ten `HYGIENE_RULE_IDS`</fails_when>
  </verify>
  <acceptance_criteria>
    - `node -e 'import("./scripts/prod-prober/run.mjs").then(m => { if (m.ARMS.length !== 3 || m.SELF_TEST_SCENARIOS !== 39 || typeof m.captureManifest !== "function") process.exit(1); console.log("ok") })'` prints `ok`
    - `node scripts/prod-prober/run.mjs --capture-manifest; test $? -eq 3` (no `--out` → usage error, nothing written) and `git status --short scripts/prod-prober/` shows no `cron-manifest.json`
    - `ls scripts/prod-prober/fixtures/cron-drift | wc -l` prints `14`
    - `node -e 'import("./scripts/prod-prober/arms/cron-drift.mjs").then(m => { const ids = m.HYGIENE_RULE_IDS; if (ids.length !== 10 || new Set(ids).size !== 10) process.exit(1); console.log(ids.join(" ")) })'` prints ten distinct ids including `authorization-literal`, `bearer-literal`, `apikey-literal`, `service-role`, `jwt-shape`, `long-literal-in-headers`
    - `grep -c 'command_withheld' scripts/prod-prober/arms/cron-drift.mjs` is at least 2 (the schema check and the withheld-diff branch), and `grep -n 'sha-only' scripts/prod-prober/run.mjs` shows the scenario (xiv) header
    - `grep -c 'kind === "cron-drift"' scripts/prod-prober/run.mjs`, `kind === "cron-secret-in-command"`, `kind === "manifest-invalid"` are each at least 1
    - `grep -n 'reading 1:' scripts/prod-prober/arms/cron-drift.mjs` and `grep -n 'reading 2:' scripts/prod-prober/arms/cron-drift.mjs` each match (D-15: both readings are literal sentences in the arm, so the summary and the issue body cannot drift)
    - `test ! -e scripts/prod-prober/cron-manifest.json` (this plan does not capture from PROD; plan 06 does through the workflow)
    - `env -i PATH="$PATH" node scripts/prod-prober/run.mjs; test $? -eq 1` and its log contains `3 registered arm(s) < floor 4`
  </acceptance_criteria>
  <done>The drift arm compares PROD `cron.job` to a committed manifest by jobname with schedule/active/normalized-command sha, records both readings on drift, refuses secrets in either direction, treats an empty PROD or an invalid manifest as loud, flags every one of ten credential shapes with a red fixture row proving each fires and green controls proving the achievable shapes do not, compares a sha-only manifest row without ever printing its text, `--capture-manifest` writes only a hygiene-clean manifest to an explicit `--out` path, and the self-test is 39/39.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prober → PROD Postgres (session pooler, IPv4) | read-only SELECTs as the `postgres` role over TLS; the password travels only as PGPASSWORD in a child env |
| PROD `cron.job.command` → prober log / manifest / public repo | command text may carry secrets (it did, until 2026-09-01) |
| `net._http_response.content` → prober log | response bodies are printed (200 chars) only on a defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.1-10 | Information disclosure | `cron.job.command` text in logs / manifest / PUBLIC repo | critical | mitigate | ten `hygieneViolations` rules (including Authorization/Bearer/apikey/service_role/JWT-shape/long-literal) run on both sides before any text is printed or written, each proven to fire by a red fixture row; `captureManifest` refuses on any violation; the diff is printed only when both sides are clean AND the manifest row carries text; a reviewer may withhold any row to sha-only (plan 06's per-row human read); otherwise sha only |
| T-164.1-11 | Information disclosure | pooler URL / PGPASSWORD / stderr | high | mitigate | URL is an argv element never logged; password only in the child env; stderr is redacted (URL replaced) and truncated to one line; the workflow (plan 05) masks the URL with add-mask |
| T-164.1-12 | Information disclosure | `_http_response.headers` and `content` | medium | mitigate | `headers` is never selected (the self-test rejects the token in any `*_SQL`); `content` is capped at 200 chars and printed only on a defect |
| T-164.1-13 | Tampering | writes to PROD | critical | mitigate | Every `*_SQL` constant is a single SELECT/WITH; the self-test rejects write tokens; `captureManifest` writes a LOCAL file only; no migration, no `supabase db push`, no `net.worker_restart()` — that remedy is printed for a human, never executed |
| T-164.1-14 | Repudiation | which side moved | medium | mitigate | `captured_at` + database marker in the manifest, both shas and both readings in the drift detail (D-15) |
| T-164.1-15 | Denial of service | unbounded `job_run_details` scan | low | mitigate | bounded by `start_time > now() - 3 hours`; one row-limited query per arm per hour |
| T-164.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installed; `psql` is provisioned by the workflow via apt (plan 05), identical to ci.yml:2156 |
</threat_model>

<verification>
- `node scripts/prod-prober/run.mjs --self-test` → `=== SELF-TEST PASSED: 39/39`.
- The live no-credential run still exits 1 with a `3 registered arm(s) < floor 4` line — plan 04 clears the floor.
- No file named `cron-manifest.json` exists under `scripts/prod-prober/` yet.
</verification>

<success_criteria>
- Success criterion 4 at the script layer: the oracle is a committed, hygiene-asserted manifest of the achievable Vault-backed configuration; drift fails loud; the comparison result records which side moved.
- Success criterion 1 (CRON-OBS-01 and CRON-DRIFT-01 arms): each exits non-zero on its injected fault and on its removed credential, proven by fixtures.
</success_criteria>

<output>
Create `.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-03-SUMMARY.md` when done. Record the 39/39 line, the fixture-pair proving `cron-secret-in-command` fires with EQUAL shas, the ten hygiene rule ids each beside the red row that fired it, and the exact separators chosen for the multi-line command query.
</output>