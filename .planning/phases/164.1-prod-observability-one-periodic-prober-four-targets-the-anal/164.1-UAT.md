---
status: partial
phase: 164.1-prod-observability-one-periodic-prober-four-targets-the-anal
source: 164.1-01-SUMMARY.md, 164.1-02-SUMMARY.md, 164.1-03-SUMMARY.md, 164.1-04-SUMMARY.md, 164.1-05-SUMMARY.md, 164.1-06-SUMMARY.md
started: 2026-09-06T11:45:00Z
updated: 2026-09-18T19:30:00Z
---

## Current Test

[testing paused — 1 item outstanding (test 9), BLOCKED on a production state that cannot be
manufactured on demand. Test 8 was unblocked and PASSED on 2026-09-18 once the scheduled path
had accumulated 80 real runs; see its evidence block.]

⚠️ This phase shipped and merged (PR #746 + PR #748, v0.77.15.0) with NO VERIFICATION.md.
This UAT is the verification pass that was owed. Every PASS below was RE-MEASURED in this
session — none is quoted from a SUMMARY. Where a SUMMARY recorded a runnable proof, the
command was re-run rather than cited.

## Tests

### 1. Absent credentials FAIL the run — never SKIP (SC1)
expected: With every credential stripped, the prober exits NON-ZERO, reports each arm as
  credential-blocked, and never emits a SKIP. A SKIP on an absent credential fails SC1 outright.
result: pass
evidence: |
  Re-ran the proof recorded in 164.1-04-SUMMARY:
    env -i PATH="$PATH" HOME="$HOME" node scripts/prod-prober/run.mjs
  → exit 1; `arms: 0/4/4 (executed/registered/credential-blocked)`; 10 credential-absent rows;
  grep -ci 'skip' → 0.

### 2. Each arm's fault fires on its own fixture and nowhere else (SC1)
expected: The self-test isolates every defect kind to its own fixture, and the counted
  scenario set is complete (no scenario silently dropped from the tally).
result: pass
evidence: "node scripts/prod-prober/run.mjs --self-test → 52/52 scenarios, exit 0, including the anti-vacuity tally arm: '52 header(s) printed, SELF_TEST_SCENARIOS is 52'."

### 3. PYAPI-06 closed at BOTH halves (SC2)
expected: analytics-client.ts refuses a guarded request without the header (loud, not a silent
  omission); main.py rejects an absent header with a code distinct from a wrong header.
result: pass
evidence: |
  TS:  npx vitest run src/lib/analytics-client.test.ts → 106 passed.
  PY:  (from analytics-service/) python3 -m pytest tests/test_verify_service_key_middleware.py
       tests/test_secret_misconfig_signal.py -q → 21 passed.

### 4. -10004 and -10005 are DIFFERENT failures with different remedies (SC3)
expected: The two MT5 states never collapse into one failure, and each carries its own remedy.
result: pass
evidence: |
  Self-test 20/52 — mt5-no-ipc fires on 10004.txt and ISOLATES exactly one defect.
  Self-test 21/52 — mt5-ipc-timeout fires on 10005.txt and ISOLATES exactly one defect.
  Self-test 46/52 — THE BOUNDARY: the prober's own transport timeout is mt5-probe-timeout and
  is never read as the terminal's -10005.
  5 defect kinds, 5 distinct remedy strings (no two arms share a remedy).

### 5. CRON-DRIFT-01's oracle is the ACHIEVABLE config, not the 42501 GUC design (SC4)
expected: The committed manifest describes what Supabase can actually run — a Vault-backed
  cron.job command — and carries no trace of the rejected GUC design.
result: pass
evidence: |
  scripts/prod-prober/cron-manifest.json: 1 occurrence of `vault.decrypted_secrets`
  (jobid 1 reads the key BY NAME), 0 occurrences of `42501` or `current_setting`.
  Independently re-scanned for secret material: 14 jobs, zero JWT / sk- / password / apikey
  literals; the only 32+ char strings are SQL function names.

### 6. No pre-2026-09-05 item silently dropped (SC5)
expected: Every item the phase carried before the re-partition is either closed here with
  evidence, or named with its new owning phase, or given a DROPPED reason.
result: pass
evidence: |
  164.1-06-SUMMARY.md carries the criterion-5 ledger with per-item dispositions:
  CRON-DRIFT-01 repair → 164.5(7); advisory-lock concurrency → 164.5; composite-stamp twin,
  OPS-08-TS/F2/F8/F9, PROC-02, PROC-03, H-0001 → 164.6; 161-ERRPREFIX → 164.2;
  MT5-VERDICT-SINK-01 → 164.6; MYPY-MAINPY-01 → 164.6; VAC04-ARMS-OBSERVE → 164.7;
  frozen-spine gate retirement → DROPPED with its reason.

### 7. The prober runs against real production and reports truthfully
expected: A live dispatch resolves all credentials, masks every secret, and reports each arm's
  real state rather than a convenient one.
result: pass
evidence: |
  Live run (job 101447647049): all 8 credentials `present`, every secret rendered `***`,
  `arms: 4/4/0`. Three arms green; the MT5 arm returned a REAL production fault,
  `mt5-terminal-error -6` (authorization failed — terminal up, answering IPC, not logged in).
  ⭐ The instrument found a fault on its first live outing, which is the thesis of the phase.
  ⛔ The fault itself is OPEN as issue #747 and is NOT a phase defect.

### 8. The hourly SCHEDULED path executes
expected: The `schedule:` trigger fires on the default branch and the run behaves as the
  posture line intends (defect → warn + exit 0, so Railway's wait-for-CI does not skip a deploy).
result: pass
unblocked: 2026-09-18
evidence: |
  ⭐ UNBLOCKED BY TIME, then MEASURED — not re-argued. When this test was written (2026-09-06)
  the workflow had reached the default branch hours earlier and the scheduled path had never
  fired, so nothing could be claimed. Twelve days later it has fired **80** times.

  Both halves of the expectation were measured separately:

  1. THE TRIGGER FIRES. `gh run list --workflow=prod-prober.yml --branch main --limit 100`
     filtered to `event == "schedule"` returns **80** runs, most recently
     2026-09-18T16:49:42Z.

  2. THE POSTURE HOLDS. Of those, the failures are a single contiguous window,
     2026-09-07 → 2026-09-11, and they are NOT the posture failing:

       2026-09-06  ok=6 fail=0      2026-09-13  ok=7 fail=0
       2026-09-07  ok=1 fail=5      2026-09-14  ok=5 fail=0
       2026-09-08  ok=0 fail=6      2026-09-15  ok=6 fail=0
       2026-09-09  ok=0 fail=7      2026-09-16  ok=6 fail=0
       2026-09-10  ok=1 fail=5      2026-09-17  ok=6 fail=0
       2026-09-11  ok=4 fail=3      2026-09-18  ok=4 fail=0
       2026-09-12  ok=8 fail=0

     The window ends at PR #774 (2026-09-11 13:18Z), which fixed the step dying early on the
     shell's `-e`. `prod-prober.yml` records this in its own POSTURE LINE comment block rather
     than only in planning prose. **Since that fix: 43 scheduled runs, 0 failures**, while the
     prober was reporting real PROD defects on most of them — which is exactly the posture the
     test names: defect → warn + exit 0, so Railway's wait-for-CI does not skip a deploy.

  ⚠️ RECORDED, NOT GLOSSED — the posture has a cost this test should not hide. Because the
  scheduled path exits 0, a real defect never shows in the run's conclusion. The signal is the
  auto-filed issue, and that guard is a SEPARATE control which was itself broken for a while:
  it keyed off `failure()`, which is false on the very path that runs hourly. It now keys off
  `steps.probe.outputs.status != '0'` and is observed firing — issue #773 was updated at
  2026-09-18T16:50Z and again at 18:56Z. A test that only checked the exit code would have
  called this green throughout the period the alerting was dead.

### 9. An MT5 WEDGE (-10004 / -10005) is observed in production
expected: A real wedge produces the right kind and remedy against the live gateway.
result: blocked
blocked_by: server
reason: |
  Production is currently in a DIFFERENT fault state (-6, authorization). The wedge states are
  proven on fixtures (tests 2 and 4) but have not been observed live, and a wedge cannot be
  manufactured on demand without deliberately breaking the production gateway — which is not
  worth doing to satisfy a test. Re-check opportunistically the next time #747 or a wedge recurs.

  ⭐ RE-CHECKED 2026-09-18, twelve days on. STILL BLOCKED, and now with a measurement rather
  than an assumption. Every occurrence of `-10004` / `-10005` across the three auto-filed
  prober issues (#747, #753, #773 — 66 hits in total) is the REMEDY SENTENCE saying the code is
  neither, e.g.:

    "MT5 initialize() failed with code -6, which is neither -10004 nor -10005 — the IPC
     transport is not implicated, so neither IPC remedy applies."

  Production has only ever shown `-6`. The observed MT5 defect kinds are `mt5-not-authorized`,
  `mt5-gateway`, `mt5-diag` and `mt5-terminal-error` — no wedge among them.

  ⭐ What DID move: the arm has now fired against live production repeatedly and its
  DISCRIMINATION is demonstrably live — the remedy text above is the arm correctly refusing to
  apply an IPC remedy to a non-IPC fault, in production, unprompted. That is most of what this
  test wanted. What remains unobserved is the specific wedge STATE, and the honest record is
  that it has not occurred, not that it cannot be detected.

## Summary

total: 9
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

<!-- No functional gaps found. The two blocked items are unobserved production states, not
     defects: neither can be resolved by changing code in this phase. They are recorded so a
     later reader cannot mistake "not observed" for "verified". -->

## Verdict

**Phase 164.1's five success criteria are all met, each re-measured in this session.**

⚠️ Two properties remain UNOBSERVED and must not be restated as verified:
  1. the hourly scheduled run (test 8) — the trigger only became registrable today;
  2. live MT5 wedge behaviour (test 9) — production is in a different fault state.

⛔ Issue #747 (`mt5-terminal-error -6`) is an OPEN PRODUCTION FAULT found BY this phase's
instrument. It is founder-owned and is not a defect of the phase.
