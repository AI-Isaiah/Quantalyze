---
phase: 164.1-prod-observability-one-periodic-prober-four-targets-the-anal
plan: 06
type: execute
wave: 5
depends_on: ["164.1-05"]
files_modified:
  - scripts/prod-prober/cron-manifest.json
  - .github/workflows/prod-prober.yml
  - TODOS.md
autonomous: false
requirements: [PYAPI-06, CRON-OBS-01, CRON-DRIFT-01, MT5-WEDGE-OBS-01]
tags: [prod-prober, dispatch, manifest-capture, measurement, checkpoint]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "D-13: `scripts/prod-prober/cron-manifest.json` is COMMITTED, captured from PROD by the workflow's `capture-manifest` mode (never derived, never hand-written), schema 1, normalization `ws-collapse-v1`, with a non-empty `database_marker`, a `captured_at`, and every row hygiene-clean — and the review that preceded the commit is recorded (jobnames vs the plausibility list, the marker, the sha of `match_engine_cron`)."
    - "SHA-bound live read: ONE `workflow_dispatch` in `probe` mode ran on the finished tree (head sha recorded and equal to the branch HEAD at dispatch time), its self-test step passed on ubuntu / Node 22, and its live step's `arms:`, `seam-invocations:`, per-arm lines and defect table are copied verbatim into the SUMMARY — read from `gh api .../jobs/<id>/logs`, never from `gh run view --log` (which truncates)."
    - "D-20 measured: the MT5 arm's line from that dispatch states whether a workspace-scoped `RAILWAY_API_TOKEN` authenticated `railway ssh` non-interactively from a hosted runner (a PROBE line = yes; `mt5-ssh-transport` with an auth message = no) — recorded as a fact, not assumed."
    - "D-18 measured: after the dispatch, `gh api repos/AI-Isaiah/Quantalyze/commits/<sha>/check-runs` was read and the SUMMARY records whether the prober's check-run appears on the commit and with what conclusion, together with the Railway analytics service's wait-for-CI setting as read from the Railway CLI/MCP — the two facts that decide the posture."
    - "Expected first reading, stated so it is not mistaken for a regression: the PYAPI-06 arm reports `pyapi06-absent-uncoded` until plan 02's Python half is DEPLOYED to Railway (which happens when this phase merges to main); a green PYAPI-06 arm before merge would itself be suspicious."
    - "Criterion 5 ledger: the SUMMARY lists every item that was listed in this phase before 2026-09-05 with its disposition (closed here with evidence / moved to 164.2, 164.5 or 164.6 / DROPPED with reason) — nothing silently dropped."
    - "TODOS.md: each of the four entries (PYAPI-06 at item 0.04, CRON-OBS-01, CRON-DRIFT-01, MT5-WEDGE-OBS-01) carries a dated `SHIPPED in Phase 164.1` line naming the arm/kind that now covers it; no entry is deleted."
  artifacts:
    - "scripts/prod-prober/cron-manifest.json"
    - ".github/workflows/prod-prober.yml — header measurement paragraph filled in with the D-18 reading and the chosen posture"
    - "TODOS.md — four dated SHIPPED lines"
  key_links:
    - "gh workflow run prod-prober.yml --ref <branch> -f mode=capture-manifest → artifact cron-manifest → local hygiene re-check → commit"
    - "gh workflow run prod-prober.yml --ref <branch> → SHA-bound run → gh api jobs/<id>/logs → SUMMARY"
    - "check-runs on the dispatched sha + Railway wait-for-CI setting → checkpoint:decision → POSTURE LINE"
  prohibitions:
    - { requirement_id: CRON-DRIFT-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT push to main, run `supabase db push`, apply a migration, or write to PROD in any form during this plan — every dispatch targets the feature branch and every arm is read-only" }
    - { requirement_id: CRON-DRIFT-01, category: privacy, status: resolved, verification: test, statement: "MUST NOT commit a manifest containing a row that fails `hygieneViolations`, and MUST NOT paste any log line containing a URL with credentials, a token, or a key into the SUMMARY — the local re-check runs before the commit" }
    - { requirement_id: MT5-WEDGE-OBS-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT use `gh run view --log` as the source of a reading — it truncates and a clean failure reads as a hang; use the jobs/<id>/logs API and compare the last log timestamp to the job's completedAt" }
    - { requirement_id: PYAPI-06, category: safety, status: resolved, verification: judgment, statement: "MUST NOT write any commit message containing the CI-skip bracket token in any sentence — GitHub honors it anywhere in the body and the SHA-bound run would have zero checks" }
---

<objective>
Capture the cron manifest from PROD through the workflow, commit it after review, run the finished
prober once SHA-bound on the feature branch, read all four arms off the job log, measure the two
things CONTEXT left open (D-20: token-auth `railway ssh` from a hosted runner; D-18: whether the
prober's check-run lands where Railway's wait-for-CI reads), close the four TODOS entries, write
the criterion-5 ledger, and put the posture decision in front of the founder with the measurements
beside it.

Purpose: a prober that has never run against production is a claim, not a control. This plan
turns the phase into measured evidence and settles the one workflow-layer question that could
make the prober harmful (blocking the deploy that fixes an outage).

Output: the committed manifest, the SUMMARY with the SHA-bound reading, the workflow header
measurement paragraph, four TODOS lines, and the founder's posture decision applied.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-CONTEXT.md
@.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-RESEARCH.md
@.github/workflows/analytics-deploy-verify.yml
@TODOS.md

Also read before starting (created by earlier plans, so not `@`-referenced): every SUMMARY in this
phase directory, `.github/workflows/prod-prober.yml`, `scripts/prod-prober/arms/cron-drift.mjs`.

Decision register: plan 01's context block (D-01..D-20). This plan closes D-13 (the committed
manifest), D-15 (recorded), D-18 (measured, decided), D-20 (measured), and success criterion 5.

## Artifacts this phase produces (this plan's share)

| Artifact | Symbol |
|---|---|
| `scripts/prod-prober/cron-manifest.json` | the committed oracle (schema 1, `ws-collapse-v1`) |
| `.github/workflows/prod-prober.yml` | header paragraph `MEASURED <date>:` with the D-18 reading; the POSTURE LINE as decided |
| `TODOS.md` | four `SHIPPED in Phase 164.1 (<date>)` lines |

The full phase artifact table is in plan 01's context block.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the cron manifest from PROD through the workflow, review it, commit it</name>
  <precondition>Repo secrets `ANALYTICS_SERVICE_KEY`, `RAILWAY_API_TOKEN`, `SUPABASE_ACCESS_TOKEN`, `SUPABASE_DB_PASSWORD` and repo variables `SUPABASE_PROJECT_REF`, `RAILWAY_PROJECT_ID`, `RAILWAY_MT5_SERVICE`, `RAILWAY_ENVIRONMENT` exist (`gh secret list` / `gh variable list` — measured present 2026-09-05), and `gh auth status` is logged in to the repo.</precondition>
  <reversibility rating="costly">The committed manifest is the drift oracle every future hourly run compares PROD against; a wrong capture (e.g. from TEST, or of a PROD state mid-change) makes every run red until re-captured. The database marker and the plausibility review are the guard.</reversibility>
  <files>scripts/prod-prober/cron-manifest.json</files>
  <read_first>
    - .github/workflows/prod-prober.yml (the capture-manifest input and artifact name)
    - scripts/prod-prober/arms/cron-drift.mjs (MANIFEST_PATH, hygieneViolations, the plausibility list in the docblock)
    - .planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-RESEARCH.md (Finding 3 — the fifteen migration jobnames; the reviewer's checklist)
  </read_first>
  <action>
    Confirm the working branch is a feature branch (`git branch --show-current` is not `main`) and that every prior plan's commits are on it. Push it: `git push -u origin HEAD`. Dispatch the capture: `gh workflow run prod-prober.yml --ref "$(git branch --show-current)" -f mode=capture-manifest`. Poll `gh run list --workflow prod-prober.yml --branch "$(git branch --show-current)" --limit 1 --json databaseId,status,conclusion,headSha` until `status` is `completed`, and REQUIRE `headSha` to equal `git rev-parse HEAD` (a settled green board that belongs to an older commit is not evidence — re-read `headSha` on every poll, and emit `MEASURE_FAIL` rather than proceeding if it differs). Read the job log through `gh api repos/AI-Isaiah/Quantalyze/actions/jobs/<job_id>/logs` (list job ids with `gh run view <run_id> --json jobs`): the self-test step must show `=== SELF-TEST PASSED: 42/42` on ubuntu (record it — this is also the Node-22 proof), the credential step must show eight `credential: ... present` lines, and the capture step must print `captured <n> job(s) from database "<marker>"`. If the run failed, stop and record the failing step's lines — do not retry blindly.

    Download the artifact into the scratchpad directory: `gh run download <run_id> -n cron-manifest -D <scratch>/manifest`. Review BEFORE moving it into the repo: (1) parse it and print `schema_version`, `normalization`, `captured_at`, `database_marker`, and one line per job (`jobname schedule active sha[0:12]`); (2) the marker must be non-empty and must read as the PRODUCTION project's hand-set name (CLAUDE.md "Which database am I on?") — if it names TEST or is absent, STOP: the workflow linked the wrong project and nothing may be committed; (3) compare the jobnames to the fifteen-name plausibility list from RESEARCH F3 and record every difference in the SUMMARY (a difference is INFORMATION, not a failure — CONTEXT: PROD can be older than the repo); (4) re-run `hygieneViolations` locally over every row with a `node -e` import of `arms/cron-drift.mjs` — any violation means the workflow's refusal failed and nothing may be committed; (5) confirm `match_engine_cron` is present, `active` is true, its schedule is `0 * * * *`, and its command contains `vault.decrypted_secrets` (the achievable shape — D-13). Then `cp` the file to `scripts/prod-prober/cron-manifest.json`, run `node scripts/prod-prober/run.mjs --self-test` once more (the live manifest path must not disturb the fixture-driven scenarios), and commit with message `docs(164.1): commit the PROD cron manifest captured by prod-prober run <run_id> at <sha>` — a normal commit message with no bracketed CI-control token anywhere in it.
  </action>
  <verify>
    <automated>node -e 'const m=JSON.parse(require("fs").readFileSync("scripts/prod-prober/cron-manifest.json","utf8")); import("./scripts/prod-prober/arms/cron-drift.mjs").then(a => { const bad = m.jobs.flatMap(j => a.hygieneViolations(j.jobname, j.command).map(v => j.jobname+": "+v)); const mec = m.jobs.find(j => j.jobname === "match_engine_cron"); if (m.schema_version !== 1 || m.normalization !== "ws-collapse-v1" || !m.database_marker || !m.captured_at || m.jobs.length < 1 || bad.length || !mec || !mec.command.includes("vault.decrypted_secrets") || m.jobs.some(j => a.sha256Hex(a.normalizeCommand(j.command)) !== j.command_sha256)) { console.error("manifest", bad); process.exit(1) } console.log("manifest ok:", m.jobs.length, "jobs, marker", m.database_marker) })'</automated>
    <fails_when>non-zero exit or "manifest" on stderr: schema/normalization/marker/captured_at missing, a hygiene violation, no Vault-backed match_engine_cron, or a sha that does not recompute from the stored normalized command</fails_when>
    <automated>node scripts/prod-prober/run.mjs --self-test >/dev/null 2>&1; test $? -eq 0 && git log -1 --format=%B | grep -ciE 'ci[[:space:]]+skip|skip[[:space:]]+ci' | grep -qx 0</automated>
    <fails_when>self-test non-zero with the real manifest present, or the last commit message contains a CI-skip token</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1 -- scripts/prod-prober/cron-manifest.json` shows the commit and its message names the run id and the sha
    - the SUMMARY records: the dispatch run id and head sha (equal to the branch HEAD at that time), the ubuntu self-test line, the `captured <n> job(s)` line with the marker, the per-job table, and the plausibility differences
    - `gh run view <run_id> --json headSha --jq .headSha` equals the sha recorded in the SUMMARY
  </acceptance_criteria>
  <done>The manifest was captured from the production database by the workflow, reviewed against the marker, the plausibility list and the hygiene rules, and committed; the self-test still passes with it in place.</done>
</task>

<task type="auto">
  <name>Task 2: The SHA-bound live read — dispatch the finished prober, read all four arms off the job log, measure D-20 and D-18, close the TODOS entries, write the criterion-5 ledger</name>
  <precondition>Task 1's manifest commit is pushed to the feature branch (`git status` clean, `git log origin/$(git branch --show-current)..HEAD` empty).</precondition>
  <files>TODOS.md, .github/workflows/prod-prober.yml</files>
  <read_first>
    - .github/workflows/prod-prober.yml (the header's measurement paragraph placeholder and the POSTURE LINE)
    - .github/workflows/analytics-deploy-verify.yml:17-24 (the 2026-06-21 measurement being re-measured)
    - TODOS.md (the four entries: item 0.04 near line 186; CRON-DRIFT-01 / CRON-OBS-01 near line 1731; MT5-WEDGE-OBS-01 near line 1779)
    - .planning/ROADMAP.md (the Phase 164.1 section: the RE-PARTITION block and the two DEDUP/carried-in lists — the criterion-5 source)
  </read_first>
  <action>
    Dispatch the live probe: `gh workflow run prod-prober.yml --ref "$(git branch --show-current)"` (mode defaults to `probe`). Poll as in Task 1, requiring `headSha == git rev-parse HEAD` on every poll. Whatever the conclusion, read the `probe` job's log via `gh api repos/AI-Isaiah/Quantalyze/actions/jobs/<job_id>/logs` and compare the last log timestamp with the job's `completed_at` (a truncated log reads as a hang — this comparison is the check). Copy VERBATIM into the SUMMARY: the eight `credential: ... present` lines, `railway --version`, the self-test PASSED line, every `pyapi06:`, `cron-obs:`, `cron-drift:`, `mt5:` line, the `arms:` and `seam-invocations:` lines, and the whole defect table with remedies (these lines contain names, statuses, shas and codes only — if any line contains a URL with a credential segment, a token or a key, the redaction failed: STOP, do not paste it, and record the failure as a blocking finding). State the expected reading explicitly beside the actual: `pyapi06-absent-uncoded` is EXPECTED until plan 02's Python half is deployed to Railway (it deploys when this phase merges to main); any other pyapi06 kind, or a green pyapi06 arm, needs an explanation. Check the auto-issue: if the run failed, `gh issue list --label prod-prober --state open` shows exactly one issue whose body contains the same defect table; record its number.

    D-20 (token-auth ssh from a hosted runner): the `mt5:` line or the mt5 defect row IS the measurement. A PROBE-derived line (`mt5: initialize=...` or an `mt5-no-ipc` / `mt5-ipc-timeout` / `mt5-terminal-error` row) means the workspace token authenticated `railway ssh` non-interactively — record `D-20: MEASURED YES, run <id>`. An `mt5-ssh-transport` row means it did not (or the relay refused) — record `D-20: MEASURED NO, stderr: <redacted first line>`; this is a finding for the checkpoint, not something to work around in this plan (an account-scoped token is the documented fallback and is the founder's to create).

    D-18 (deploy-skip hazard): run `gh api repos/AI-Isaiah/Quantalyze/commits/<sha>/check-runs --jq '.check_runs[] | {name, conclusion, app: .app.slug}'` and record whether the prober's job appears as a check-run on the dispatched commit and with what conclusion. Then read the Railway analytics service's wait-for-CI setting — via the Railway MCP service-settings read if available, else `railway service` / `railway status` in the linked project, else the Railway dashboard, recording which — and record its value verbatim. Do NOT push to main and do NOT create a deploy to test it; the composition (check-run present on the commit) + (wait-for-CI enabled) + (the 2026-06-21 measured skip) is the evidence set the checkpoint decides on. Write the result into the workflow header as a paragraph beginning `MEASURED <date> (plan 164.1-06, run <id>):` with the three facts and the sentence `posture below chosen at checkpoint 164.1-06 Task 4` — header text only; the POSTURE LINE itself is not touched in this task.

    TODOS.md: under item 0.04 (PYAPI-06), under the CRON-DRIFT-01 / CRON-OBS-01 section and under MT5-WEDGE-OBS-01, append a dated line `✅ SHIPPED in Phase 164.1 (<date>): <arm> / kinds <...>; prober workflow prod-prober.yml hourly; first SHA-bound read run <id>` — do not delete or rewrite the entries (they are the evidence trail). Add no new TODOS item unless the dispatch surfaced a genuine open finding (D-20 NO, a redaction failure, a marker mismatch); if it did, book it under a new ID with the measurement.

    Criterion-5 ledger in the SUMMARY: a table with one row per item that the ROADMAP's Phase 164.1 section lists as ever having been in this phase — SKIP-01, DRIFT-01, OPS-08-F9, OPS-08-F8, OPS-08-TS, OPS-08-F2, H-0001, WR-06-UTC, HONEST-08-RESIDUAL, WIZFORM-02, PII-01, 161-ERRPREFIX, PROC-01, PROC-02, PROC-03, composite-stamp twin (TS half), the advisory-lock concurrency test, frozen-spine gate retirement, PYAPI-06, CRON-OBS-01, CRON-DRIFT-01, MT5-WEDGE-OBS-01 — with columns `Item | Disposition | Owner / evidence`, where the four this phase owns read `closed here` with the run id and the others carry the owner the RE-PARTITION block names (164.2 / 164.5 / 164.6 / DROPPED with reason / closed-decided earlier). Every row filled; none blank.
  </action>
  <verify>
    <automated>set -o pipefail; grep -c 'SHIPPED in Phase 164.1' TODOS.md | grep -qx 3 && grep -c '^# MEASURED 20' .github/workflows/prod-prober.yml | grep -qx 1 && npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/critical-regressions.test.ts 2>&1 | tail -6</automated>
    <fails_when>non-zero exit: fewer or more than 3 SHIPPED lines (0.04, the joint CRON section, MT5), no MEASURED header paragraph, or a red wiring/critical-regressions test after the header edit</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY contains a section `## SHA-bound live read` with the run id, the head sha, `arms: <e>/4/<b>`, `seam-invocations:`, four per-arm readings, and the defect table verbatim
    - the SUMMARY contains `D-20: MEASURED YES` or `D-20: MEASURED NO` with the evidence line, and a `D-18` section with the check-runs output and the wait-for-CI value and its source
    - the SUMMARY contains the criterion-5 ledger table with 22 rows and no empty cells
    - `gh issue list --label prod-prober --state open --json number | jq length` is `1` if the run failed and `0` if it passed, and the SUMMARY says which
    - `git diff --stat HEAD~1 -- .github/workflows/prod-prober.yml` shows header-only changes (no change to the `exit $status` line)
  </acceptance_criteria>
  <done>The finished prober ran once against production SHA-bound, every arm's reading is on record, D-20 and D-18 are measured facts in the SUMMARY and the workflow header, the four TODOS entries carry their SHIPPED lines, and the criterion-5 ledger accounts for every item this phase ever listed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Founder reads the first live run arm by arm</name>
  <what-built>The four-arm prober ran once against production from a GitHub-hosted runner on the finished tree. The SUMMARY's `## SHA-bound live read` section holds the verbatim per-arm lines and the defect table; the auto-issue (if any) holds the same table.</what-built>
  <how-to-verify>
    1. Open the run: `gh run view <run_id> --web` (id in the SUMMARY). Confirm the head sha shown equals the branch HEAD you are reading.
    2. Read the `probe` job's steps in order: the credential step shows eight `present` lines; the self-test step ends in `=== SELF-TEST PASSED: 42/42`; the live step prints one block per arm.
    3. PYAPI-06 arm: expect `pyapi06-absent-uncoded` (the Python half is not deployed until merge). Anything else needs the executor's explanation in the SUMMARY.
    4. CRON-OBS-01 arm: read `pg_net.ttl` and the last runs' status codes; a 200 per hourly run is the healthy reading.
    5. CRON-DRIFT-01 arm: the manifest was captured minutes earlier, so `0 differing` is expected; if drift is reported, the two readings are printed — decide which side moved.
    6. MT5 arm: this is the D-20 measurement. A `mt5:` line with `initialize=` or an IPC-code defect means the token path works; `mt5-ssh-transport` means it does not and the token scope is yours to change.
    7. Confirm no line in the log shows a URL with a password segment, a token, or a key value. If one does, say so — that is a blocking finding.
  </how-to-verify>
  <resume-signal>Reply `approved` to proceed to the posture decision, or describe what does not match.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: The workflow-layer exit posture (D-18) — decided on the measurement, not averaged</name>
  <decision>How a failing hourly run surfaces at the WORKFLOW layer, given the measured deploy-skip hazard. The SCRIPT contract (exit 1 on any defect, self-tested) is not in question and does not change either way.</decision>
  <context>CONTEXT locked "the job exits non-zero (red workflow run)" AND put the prober in its own workflow because a red check on main HEAD made Railway skip the analytics deploy on 2026-06-21. Task 2 measured: (a) whether the prober's check-run lands on the commit; (b) Railway's wait-for-CI setting; (c) the 2026-06-21 precedent. If (a) and (b) are both true, an hourly red prober during a persistent outage will skip any analytics deploy whose CI wait overlaps a red run — including the deploy that fixes the outage.</context>
  <options>
    <option id="A" default="true">Keep the locked posture: `exit $status` (red run on any defect). The loud signal is the red check + the dedup'd issue. Accept the measured hazard and document the operator rule in the header: during a persistent outage, re-run main CI for the merge sha after the fix lands, or temporarily disable the schedule from the Actions UI. No file change beyond the header sentence.</option>
    <option id="B">Adopt the analytics-deploy-verify posture at the workflow layer only: the POSTURE LINE becomes `exit 0` after the issue step, with `::error::` annotations and the job summary carrying the defect table, and the dedup'd issue as the loud signal. Required edits, all in one commit: the POSTURE LINE and its comment; the wiring test's "no exit 0" predicate narrowed to "exit 0 only on the POSTURE LINE, preceded by the issue step" with a new calibration; CONTEXT's locked sentence amended in the SUMMARY with the founder's ruling and the measurement that justified it (a scope amendment touches more than one file — record it in STATE.md and ROADMAP.md too).</option>
  </options>
  <resume-signal>Reply `A` or `B`. The executor applies the chosen edits, re-runs `npx vitest run src/__tests__/prod-prober-wiring.test.ts`, and records the ruling and the measurement in the SUMMARY, the workflow header, and (for B) STATE.md and the ROADMAP phase section.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature branch dispatch → production reads | the first real runs of the prober against PROD, from a hosted runner |
| workflow artifact → reviewer's checkout → git | the manifest crosses from PROD into a public repo |
| SUMMARY / TODOS / workflow header → public | pasted log lines become world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.1-26 | Information disclosure | manifest committed with a secret | critical | mitigate | the workflow refuses to write on any hygiene violation; the local re-check repeats `hygieneViolations` on every row before `cp`; the reviewer confirms the Vault shape |
| T-164.1-27 | Information disclosure | log lines pasted into the SUMMARY | high | mitigate | explicit STOP rule: any line with a credential segment, token or key is a blocking finding, never pasted |
| T-164.1-28 | Tampering | committing a manifest captured from the wrong project | high | mitigate | the database marker is printed by the capture and checked by the reviewer against the PROD name; TEST or empty → nothing committed |
| T-164.1-29 | Repudiation | a reading taken from an older commit | medium | mitigate | `headSha` re-read on every poll and required equal to HEAD; the run id and sha are in the SUMMARY and the commit message |
| T-164.1-30 | Denial of service | the posture decision blocking deploys | high | mitigate | measured (check-runs + wait-for-CI + precedent) and decided at a checkpoint with both options' edits spelled out |
| T-164.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | nothing installed in this plan |
</threat_model>

<verification>
- The manifest verify script (Task 1) passes; `node scripts/prod-prober/run.mjs --self-test` passes with the real manifest present.
- Two SHA-bound dispatches recorded (capture, probe) with run ids and equal head shas.
- Full vitest once, ALONE on the box; from `analytics-service/`: `python3 -m pytest -q` then `python3 -m mypy --strict main.py`.
</verification>

<success_criteria>
- Success criterion 1: proven on production — all four arms ran in one job, aggregate-then-exit, each arm's credential-removal and fault-injection proofs already on record from plans 01-04, no SKIP path exists.
- Success criterion 4: the committed manifest is the achievable Vault-backed configuration captured from PROD; the comparison is recorded with both readings.
- Success criterion 5: the ledger accounts for every item ever listed in this phase.
- D-18 and D-20 are measured facts, and the posture is a founder decision recorded in three places.
</success_criteria>

<output>
Create `.planning/phases/164.1-prod-observability-one-periodic-prober-four-targets-the-anal/164.1-06-SUMMARY.md` when done. It must contain the `## SHA-bound live read` section, the D-20 and D-18 measurements, the criterion-5 ledger, the founder's posture ruling, and the run ids.
</output>