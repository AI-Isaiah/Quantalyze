---
phase: 164.5.1.2-fanoutsiblings-the-daily-position-poll-never-runs-for-a
plan: 02
subsystem: api
tags: [python, pytest, fastapi, cron, cursor-advance]

# Dependency graph
requires: []
provides:
  - "A third, calibrated member of the `TestC0198CursorOnlyAdvancesWhenStored` class — `test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor` — proven RED against unmodified `cron.py`, then GREEN after the fix."
  - "A corrected `should_advance_cursor` formula in `analytics-service/routers/cron.py::_sync_single_key`: `(not trades) or synced_count > 0`, dropping the `bool(strategy_ids)` term that previously made an idle-tick and a zero-eligible-strategies tick indistinguishable."
  - "A split warning-log reason distinguishing 'no strategy was eligible' from 'all per-strategy RPCs failed'."
affects: [164.5.1.2-03]

# Actuals (#2632)
actuals:
  tokens: 1910
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cursor-advance formula distinguishes 'no trades fetched at all' (idle tick) from 'trades fetched but nothing stored' (either because strategy_ids was empty or every per-strategy RPC failed) via a single `synced_count > 0` disjunct, reused rather than duplicated."

key-files:
  created: []
  modified:
    - analytics-service/routers/cron.py
    - analytics-service/tests/test_cron_router.py

key-decisions:
  - "Dropped `bool(strategy_ids)` from the `should_advance_cursor` gate entirely rather than adding a parallel condition, per CONTEXT.md D-03's instruction to reuse the same `synced_count > 0` disjunct C-0198 already established — no new gate shape was invented."
  - "Split the single warning-log message into two branches (no eligible strategy vs. all RPCs failed) rather than writing one message that would only be accurate for one of the two causes."
  - "Did not touch `ALLOWED_STRATEGY_STATUSES`, its filter in `cron_sync`, or any SQL file — D-03 is a cursor-advance defect independent of the widening verdict CONTEXT.md D-03 assigns to Plan 01."

patterns-established:
  - "A third member added to an existing calibrated test class (`TestC0198CursorOnlyAdvancesWhenStored`) rather than a new, parallel, potentially-divergent test class for a narrower sub-case of the same defect family."

requirements-completed:
  - FANOUT-COHORT-SYNC-CONSTANT-01

coverage:
  - id: D1
    description: "A new pytest, run against unmodified code, FAILS — proving the last_sync_at LIES defect exists on the strategy_ids=[] path before any fix is applied."
    requirement: FANOUT-COHORT-SYNC-CONSTANT-01
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_cron_router.py::TestC0198CursorOnlyAdvancesWhenStored::test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor (RED, quoted below)"
        status: pass
    human_judgment: false
  - id: D2
    description: "After the fix, the same test PASSES, and both existing TestC0198CursorOnlyAdvancesWhenStored tests still pass unchanged."
    requirement: FANOUT-COHORT-SYNC-CONSTANT-01
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_cron_router.py::TestC0198CursorOnlyAdvancesWhenStored (3 passed, 0 failed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The last_sync_at cursor no longer advances on a tick where trades were fetched but zero strategies were eligible to receive them, independent of the ALLOWED_STRATEGY_STATUSES verdict."
    requirement: FANOUT-COHORT-SYNC-CONSTANT-01
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_cron_router.py::TestC0198CursorOnlyAdvancesWhenStored::test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor"
        status: pass
    human_judgment: false
  - id: D4
    description: "The full analytics-service pytest suite is green after the change, run from the analytics-service/ directory."
    verification:
      - kind: unit
        ref: "pytest (full suite, analytics-service/) — 5880 passed, 89 skipped, exit 0"
        status: pass
    human_judgment: false

duration: 12min
completed: 2026-09-19
status: complete
---

# Phase 164.5.1.2 Plan 02: D-03 cursor-advance fix Summary

**Fixed the `last_sync_at` cursor advancing on a tick where trades were fetched but zero strategies were eligible to receive them, by dropping `bool(strategy_ids)` from `should_advance_cursor`'s gate and reusing the existing `synced_count > 0` disjunct — proven by a test observed RED against unmodified code before the fix, then GREEN after.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-09-19T12:12:00Z (approx.)
- **Completed:** 2026-09-19T12:24:22Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added `test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor` to `TestC0198CursorOnlyAdvancesWhenStored` in `analytics-service/tests/test_cron_router.py`, covering the narrower case (no RPC ever attempted) that both RESEARCH-identified sub-causes (all siblings filtered out; zero linked strategies) collapse into.
- Confirmed the test FAILS against unmodified `cron.py` and quoted the actual failure output (below) before touching the formula.
- Fixed `_sync_single_key`'s `should_advance_cursor` formula in `analytics-service/routers/cron.py`: it now reads `(not trades) or synced_count > 0`, no longer conflating "no trades fetched" (a true idle tick) with "trades fetched but nothing stored because no strategy was eligible."
- Split the adjacent `logger.warning` call so it names the correct one of the two causes ("no strategy on this key was currently eligible" vs. "all per-strategy RPCs failed") instead of one message that was only accurate for the original C-0198 case.
- Verified the new test now passes, both original C-0198 tests still pass unchanged, and the full `analytics-service` pytest suite is green.

## Evidence: RED before the fix (Task 1)

Command: `pytest tests/test_cron_router.py::TestC0198CursorOnlyAdvancesWhenStored::test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor -v`, run from `analytics-service/` against the unmodified `cron.py`.

```
collected 1 item
tests/test_cron_router.py::TestC0198CursorOnlyAdvancesWhenStored::test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor FAILED [100%]

...
>       assert all(
            "last_sync_at" not in p for p in api_keys_update_payloads
        ), (
            "Expected no last_sync_at cursor advance when strategy_ids=[] "
            f"despite fetched trades; got {api_keys_update_payloads!r}"
        )
E       AssertionError: Expected no last_sync_at cursor advance when strategy_ids=[] despite fetched trades; got [{'last_sync_at': '2026-09-19T12:19:08.764981+00:00'}]
E       assert False
E        +  where False = all(<generator object TestC0198CursorOnlyAdvancesWhenStored.test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor.<locals>.<genexpr> at 0x11a75e8e0>)

1 failed in 3.28s
```

`collected=1`, `1 failed` — RED confirmed and evidenced, not asserted.

## Evidence: GREEN after the fix (Task 2)

Command: `pytest tests/test_cron_router.py::TestC0198CursorOnlyAdvancesWhenStored -v`

```
collected 3 items
tests/test_cron_router.py::TestC0198CursorOnlyAdvancesWhenStored::test_C0198_zero_stored_does_not_advance_last_sync_at PASSED [ 33%]
tests/test_cron_router.py::TestC0198CursorOnlyAdvancesWhenStored::test_C0198_partial_success_does_advance_last_sync_at PASSED [ 66%]
tests/test_cron_router.py::TestC0198CursorOnlyAdvancesWhenStored::test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor PASSED [100%]

3 passed in 2.98s
```

Full suite (`pytest`, run from `analytics-service/`):

```
========== 5880 passed, 89 skipped, 919 warnings in 144.42s (0:02:24) ==========
[exited with code 0]
```

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — a calibrated test proving the cursor lies when no strategy is eligible** - `d1157059` (test)
2. **Task 2: GREEN — correct the cursor-advance formula and its warning-log reason** - `ca432660` (fix)

**Plan metadata:** pending (this SUMMARY + final metadata commit)

## Files Created/Modified

- `analytics-service/tests/test_cron_router.py` - Added `test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor` as a third member of `TestC0198CursorOnlyAdvancesWhenStored`, covering the `strategy_ids=[]` path where no RPC is ever attempted.
- `analytics-service/routers/cron.py` - Corrected `should_advance_cursor` to `(not trades) or synced_count > 0` (dropped `bool(strategy_ids)`), added a dated D-03 comment beside the existing C-0198 audit block, and split the warning-log reason into two accurate branches.

## Decisions Made

- Dropped `bool(strategy_ids)` from the gate rather than inventing a new gate shape, per CONTEXT.md D-03's explicit instruction to reuse the `synced_count > 0` disjunct C-0198 already established — `synced_count` is already structurally zero whenever `strategy_ids` is empty (the per-strategy RPC loop is gated on `if trades and strategy_ids:`), so no new logic was needed to detect the case.
- Split the warning-log message into two branches so it names the correct cause per case, rather than leaving one message that would misdescribe half its callers.
- Did not widen `ALLOWED_STRATEGY_STATUSES` or touch its filter/any SQL file, per plan scope and CONTEXT.md D-03 — that verdict belongs to Plan 01.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The `analytics-service/.venv` absence in this worktree was anticipated per the plan's precondition; the main checkout's interpreter was used by absolute path (`/Users/helios-mammut/claude-projects/quantalyze/analytics-service/.venv/bin/python`), and every pytest run's "collected N items" / PASSED-FAILED counts were checked explicitly rather than trusting a bare exit code, per the plan's verify commands.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 03 (per this plan's `<output>` instruction) can rely on: the new test's exact name
  (`test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor`), the RED failure output
  quoted above, the GREEN confirmation, the full-suite result, and the exact commits
  (`d1157059`, `ca432660`) this shipped in.
- No blockers. This plan's scope (D-03 cursor-advance defect) is fully closed, independent of
  the `ALLOWED_STRATEGY_STATUSES` widening verdict Plan 01 owns.

---
*Phase: 164.5.1.2-fanoutsiblings-the-daily-position-poll-never-runs-for-a*
*Completed: 2026-09-19*

## Self-Check: PASSED

- FOUND: analytics-service/routers/cron.py
- FOUND: analytics-service/tests/test_cron_router.py
- FOUND: .planning/phases/164.5.1.2-fanoutsiblings-the-daily-position-poll-never-runs-for-a/164.5.1.2-02-SUMMARY.md
- FOUND commit: d1157059
- FOUND commit: ca432660
