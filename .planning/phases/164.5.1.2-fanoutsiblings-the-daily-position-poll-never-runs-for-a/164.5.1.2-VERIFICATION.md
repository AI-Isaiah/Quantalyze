---
phase: 164.5.1.2-fanoutsiblings-the-daily-position-poll-never-runs-for-a
verified: 2026-09-19T16:20:00Z
status: passed
score: 5/5 must-have claims verified
verified_at_sha: 8d90772ec573a907f575a48f159f262d338e3469
drift_subjects:
  - analytics-service/routers/cron.py
  - analytics-service/tests/test_cron_router.py
  - supabase/schema/functions/enqueue_poll_positions_for_all_strategies.sql
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 4/4
  previous_sha: ed1b7d92caf8435051aef95686491fccae208a65
  gaps_closed:
    - "The sole human_verification item — the descoped third requirement still naming this phase as its owner — is RESOLVED: the ledger entry now carries a dated descope annotation recording that it deliberately has NO owner phase and stays trigger-gated"
  gaps_remaining: []
  regressions: []
  newly_verified:
    - "The `held` terminal SyncStatus, its summary bucket, its non-OK triage membership and its WARNING-level per-key line"
    - "The sync_trades contract-drift fallback counting 0 rather than fabricating a count, AND recording the drift so the status classifier can see it"
    - "The round-2 comment correction: no current comment claims a safety property the code does not deliver, and both comments cross-reference the root-cause phase"
evidence_provenance:
  - claim: "The poll-positions widening is refused as inert"
    machine_verified: "the predicate is byte-unchanged at the ship SHA (identical blob hash on both sides of the branch point); zero SQL files touched; zero new migrations; the sync constant that starves the second conjunct is likewise unchanged"
    founder_attested: "the 2026-09-17 and 2026-09-19 production readings — not reproducible by this verifier, which holds no production credentials and is forbidden from acquiring them"
---

# Phase 164.5.1.2 FANOUTSIBLINGS — Verification Report (RE-VERIFICATION, supersedes the `ed1b7d92` report)

**Phase Goal:** For each of the two sites Phase 164.5.1.1 deliberately left alone, decide on this
phase's own evidence whether leaving it alone is still right — and record each verdict with the
measurement that justifies it.

**Verified:** 2026-09-19
**Verified at SHA:** `8d90772e` (the ship SHA)
**Status:** passed
**Re-verification:** YES — supersedes the `ed1b7d92` report (`human_needed`, 4/4)

⭐ **This phase's deliverable is VERDICTS, not diffs.** It is verified on that basis: a refusal
backed by evidence is held to the same standard a change would be, and is never marked incomplete
for lack of a code diff.

⚠️ **HEAD MOVED DURING THIS VERIFICATION, and the movement is enumerated rather than ignored.** The
run began at the ship SHA `8d90772e`; a docs-only commit landed on the branch mid-run, advancing
HEAD to `03d30b89` ("security audit — SECURED, 13/13 threats closed"). Its entire delta is two
planning/ledger files — a new phase SECURITY document and one additive backlog entry booking a
latent contract-drift logging concern to Phase 164.5.1.4. **No code, no test and no SQL file
differs between the two commits**, so every code measurement below is equally true at both. The
frontmatter records the ship SHA, as instructed.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The poll-positions lifecycle conjunct is refused as inert and stands exactly as measured — never widened, never repaired, never claimed as addressed | ✓ VERIFIED | The canonical function's blob hash is IDENTICAL on both sides of the branch point; the branch's SQL diffstat is EMPTY; zero new migration files; the backlog entry is CLOSED as a refusal |
| 2 | The D-03 cursor-advance defect is fixed, and the gate proving it BITES | ✓ VERIFIED | `should_advance_cursor = (not trades) or synced_count > 0` by symbol; neuter → RED on 3 tests → restore → `cmp` byte-identical, performed by this verifier on a throwaway copy |
| 3 | `FANOUT-COHORT-SYNC-CONSTANT-01` is FULLY CLOSED in two DISTINCT paragraphs with different causes, routed to exactly one destination | ✓ VERIFIED | Two separately-headed closures with separately-sharpened TRIGGERs; `FULLY CLOSED` asserted only after both; destination resolves to exactly ONE roadmap heading and exactly ONE phase directory |
| 4 | The advisory-lock false claim carries a recorded DROP decision with a named TRIGGER, and the lock is genuinely absent | ✓ VERIFIED | The lock name occurs in exactly 2 non-planning files, both as `--` comment text; 0 advisory-lock calls of any name in the service's Python; the real guard exists and is referenced 3× in the worker |
| 5 | The two review rounds' additions are real, wired, and gated — the `held` bucket and the contract-drift fix | ✓ VERIFIED | Both calibrated by this verifier with independent neuters; both bite; both directions of the registry gate pinned by their own test class |

**Score:** 5/5 truths verified (0 present-but-behavior-unverified, 0 overrides applied)

### The prior report's one human item — RESOLVED

The `ed1b7d92` report was `human_needed` on exactly one ledger annotation: a requirement descoped
by founder decision still named this phase as its owner. **That item is now closed in the ledger,
and this verifier confirmed the annotation exists and is coherent rather than re-raising it.**

| check | result |
|---|---|
| annotation present on the entry | ✓ — a dated `DESTINATION CORRECTION` block appended beneath the stale Destination line |
| does it supersede rather than silently rewrite? | ✓ — it states the original line is stale and supersedes it, per this repo's append-and-annotate convention; no existing line was rewritten |
| does it name the decision, not just the outcome? | ✓ — names the founder's CONTEXT-time descope, its three grounds (unscheduled, called by nothing, observed by no user), and that the phase's success criteria covered TWO sites by design |
| does it re-point at a live phase? | ✗ — deliberately NOT, and it says so: re-pointing would fabricate scheduled work for a fan-out nobody has scheduled |
| does it leave a dangling pointer? | ✗ — it records an explicit "no owner phase, by design" standing decision and keeps the entry TRIGGER-GATED |
| is the earlier founder-locked prohibition weakened? | ✗ — the annotation states in its own text that the prohibition stands in full and is not weakened |

⭐ **This is the honest answer to the founder rule that every deferral names an owner phase:** a
reasoned standing decision, not a phase number invented to satisfy a checklist. The scope decision
was NOT re-opened by this verifier and is NOT re-raised as a fresh human item.

---

## Claim 1 — the widening was REFUSED, and the refusal is real

**Verified by this verifier, independently, at the ship SHA.**

| check | result |
|---|---|
| branch diffstat over the SQL tree | **EMPTY** — no SQL file touched on this branch at all |
| canonical poll function, blob hash at branch point vs ship SHA | **IDENTICAL** — byte-unchanged, which is the CORRECT outcome for a refusal |
| new migration files on the branch | **0** |
| `ALLOWED_STRATEGY_STATUSES` declaration | **unchanged** — the only branch-added lines naming it are explanatory comments; its three-value set still omits the owner-only status |

**The inert-ness argument is checkable in code, and this verifier checked it rather than taking the
plan's word.** The lifecycle conjunct is only one of two the poll predicate requires; the second
demands a recent trade-sync job, which is issued only by the cron sync path, whose own constant
excludes the owner-only status. So the conjunct a lifecycle widening would relax is starved one
level upstream — a lifecycle-only change would move nothing observable. That is why refusing it was
right, and why the byte-unchanged file is evidence of the verdict rather than evidence of inaction.

**Ledger closure** — `FANOUT-COHORT-SIBLING-POLL-01` is CLOSED, and its closure text states the
verdict as a refusal, keeps the harm explicitly booked ("this is a refusal of the wrong fix, never a
finding that nothing is wrong"), and restates the two-conjunct coupling as the condition on any
future revisit. It does not claim the predicate was repaired.

⚠️ **Taken on report, by necessity:** the production readings (2026-09-17 poll/snapshot census;
2026-09-19 sibling-key counts) are founder-run observations against the live database. This verifier
holds no production credentials and is forbidden from acquiring them, so it cannot re-run them and
does not pretend to have. What it *can* verify, and did: the session record puts the database-identity
marker FIRST, ahead of both read statements, records every reading as OUTPUT rather than as the query
that would produce it, registers its limit BEFORE the read, and contains no identifier longer than 8
characters.

---

## Claim 2 — the D-03 fix, and the anti-vacuity calibration

**Verified by this verifier, independently, including the neuter.**

**Fix, by symbol:** in the cron router's `_sync_single_key`,
`should_advance_cursor = (not trades) or synced_count > 0`. The pre-fix `bool(strategy_ids)`
disjunct is absent from executable code — its surviving occurrences in the file are inside the dated
audit comment explaining why it was dropped, which is documentation, not logic.

**Test, by symbol:** `test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor`, the third
member of `TestC0198CursorOnlyAdvancesWhenStored`.

### Anti-vacuity: neuter → RED → restore → `cmp`

⛔ The test's existence was NOT taken as proof it bites. This is data-integrity, so the calibration
was performed — **on a throwaway copy of the repository under the session scratchpad, never on the
checkout**, because another agent may be reading this checkout concurrently and a transiently
mutated file would hand it a phantom finding.

| step | what was done | result |
|---|---|---|
| build harness | a tracked-files-only export of the ship SHA extracted to scratch; the service's own interpreter invoked from there by absolute path | built |
| prove the copy faithful | `cmp` of the router and its test file against the checkout | **BYTE-IDENTICAL** |
| prove the harness faithful | full service suite on the PRISTINE copy | **5886 passed, 89 skipped** — reproduces the orchestrator's figure exactly, so the scratch harness is not a different world |
| neuter | the pre-fix disjunct restored into the formula, replacement count asserted `== 1` before writing; fixed formula then confirmed ABSENT | **applied** — a neuter that does not apply reads GREEN and measures nothing |
| observe | full service suite under the neuter | **3 failed, 5883 passed, 89 skipped** |
| restore | re-extracted from the SHA; `cmp` against the checkout | **BYTE-IDENTICAL** |
| clean up | scratch copy deleted; checkout status re-checked | `git status --porcelain` **EMPTY** |

**The 3 reds, named:**

1. `TestC0198CursorOnlyAdvancesWhenStored::test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor`
2. `TestHeldStatusBucketForStalledKeys::test_stalled_key_reports_held_not_ok`
3. `TestHeldStatusBucketForStalledKeys::test_stalled_key_per_key_line_logs_at_warning`

⭐ **This independently reproduces the orchestrator's own measurement — same count, same three
members.** Two measurements taken separately agreeing on which tests move is stronger than either
alone.

**OBSERVED failure text, quoted (file paths redacted — a proof-of-absence must not publish what it
proves absent):**

```
E  AssertionError: Expected no last_sync_at cursor advance when strategy_ids=[]
E     despite fetched trades; got {'last_sync_at': '2026-09-19T...', 'account_balance_usdt': 4321.0}
E  assert 'last_sync_at' not in {'last_sync_at': '2026-09-19T...', ...}

E  AssertionError: {'synced': 1, 'partial': 0, 'failed': 0, 'timed_out': 0, ...}
E  assert 0 == 1

E  AssertionError: The stalled key's per-key summary line must log at WARNING; a held key
E     logging at INFO is invisible in the same log stream an operator scans for trouble.
E  assert 20 == 30
```

⭐ **The spread of the three reds is the load-bearing detail.** The neuter is a one-line change to a
cursor formula, yet it also reds both stalled-key tests — because with the pre-fix disjunct restored
the tick advances its cursor, so it is never classified `held` and never logs at WARNING. The `held`
bucket and the cursor gate are therefore genuinely coupled in the code, not two independent features
that happen to sit in the same file. Nothing else in 5886 tests moved.

**The checkout was never touched.** No `git checkout --`, `git restore` or `git stash` was used at
any point; the working tree was clean at entry and is clean at exit, and that is measured, not
asserted.

---

## Claim 3 — two distinct closures, one destination

**Verified by this verifier, independently.**

| check | result |
|---|---|
| widening-verdict closure | present, headed as closing THAT HALF ONLY, and stating in its own text that the cursor-advance half is explicitly not closed by it |
| cursor-advance (D-03) closure | present, separately headed, citing the shipped formula by symbol, the test by exact name, and both shipping commits |
| causes kept distinct | ✓ — the first rests on a production sibling-key read; the second on a code-traced defect that is wrong regardless of how that read came out, and says so |
| TRIGGERs kept distinct | ✓ — each half carries its own separately-sharpened TRIGGER; neither restates the other's |
| `FULLY CLOSED` marker | asserted ONCE, after both halves, naming them as distinct per the locked decision |
| destination roadmap heading (`164.5.1.3 SYNCADMIT`) | **exactly 1** |
| destination phase directory | **exactly 1** |
| blank or placeholder destination | none |

The destination directory holds only a keep-file, which is the expected state of a booked-but-unplanned
phase and not a blank destination.

---

## Claim 4 — the advisory-lock decision

**Verified by this verifier; the zero-counts were re-derived rather than accepted.**

| check | result |
|---|---|
| non-planning files naming the lock | **2** — the canonical function and its source migration |
| are those occurrences executable? | **No** — both lines begin `--`, i.e. SQL comment text asserting a guard at the worker layer |
| advisory-lock calls of ANY name in the service's Python | **0** |
| the guard the decision relies on instead | present in the worker module and referenced 3× — a UTC-day check gating the daily tick, plus the enqueue path's own idempotent dedup |

**The decision is recorded, not silence:** a DROP of a same-phase migration fix, with the cost stated
(a comment-only change with zero functional effect does not justify a three-reviewer plus test-apply
plus human-gated production-apply pipeline) and an **opportunistic TRIGGER** named — correct the
comment the next time that function's migration is genuinely touched for another reason, never as a
standalone migration. That is a DROP-with-a-named-TRIGGER, exactly as required.

⚠️ **One consequence, stated so it is not mistaken for a gap:** the older, pre-existing part of the
poll entry still repeats the false lock sentence, because this repo's ledger convention is
append-and-annotate and existing lines are never rewritten. The correction lives in the appended
note and in the roadmap. That is the convention working, not a missed edit.

---

## Claim 5 — what the two review rounds added

**Verified by this verifier, independently, and calibrated in both directions.**

### 5a. The `held` terminal status

| requirement | evidence |
|---|---|
| a terminal `SyncStatus` member `held` | present in the status literal, with a dated comment explaining why it is neither `error` nor `partial` nor `ok` |
| its own summary bucket, OUTSIDE `synced` | present — counters are DERIVED from a single status→bucket registry, so registering a status is the only step needed for it to reach the response body |
| its own counter | present and named in the completion log line alongside the other seven |
| `_NON_OK_STATUSES` membership | present — so `held` wins a slot when the response results list is capped, i.e. it survives triage as a diagnostic |
| WARNING-level per-key line | present — the per-key log level is INFO only for `ok`, WARNING for everything else, and a test asserts the level explicitly rather than inferring it |
| reaches the response body | asserted directly: the stalled-key test reads `response["held"] == 1` and finds the key in `response["results"]` |

**The registry gate bites in BOTH directions** — `TestSyncStatusSummaryBucketCompleteness` has two
arms: a new status with no registry entry fails arm 1, and a registry entry whose counter never
reaches the response body fails arm 2. A ninth status therefore cannot ship without an alarmable
bucket.

⭐ **A key that fetches trades, stores none and holds its cursor is no longer counted as healthy —
and the proof is a test that used to assert the opposite.** One pre-existing test asserted
`status == "ok"` on exactly that tick; the phase changed it to `held` and its comment names that
former expectation as having been the defect itself. That is the strongest available evidence that
the bucket is not decorative: it changed an existing green assertion rather than only adding a new one.

### 5b. The contract-drift fallback

The drift branch now sets `stored = 0` (previously it fabricated a count from the fetch size) AND
writes a type-name-only entry into the per-strategy error map, which is the only input the status
classifier has for "something went wrong."

**Calibrated by this verifier with TWO independent neuters, both on the throwaway copy:**

| neuter | what was reverted | observed |
|---|---|---|
| A | the whole round-1+round-2 fix — fabricated count restored, drift record removed | **3 failed** of the 3 named drift tests; the fabricated count propagated visibly (`trades_stored: 2` on a drift where nothing was stored; `{'strat-B': 3}` where 0 was correct) and the key reported `ok` |
| B | **only** the round-2 half — `stored = 0` KEPT, drift record removed | **2 failed, 1 passed** — the surviving arms are precisely the classifier ones |

⭐ **Neuter B is the sharper of the two, because it isolates the round-2 fix from the round-1 fix.**
Its observed assertions are the routing contract stated verbatim by the code under test:

```
E  AssertionError: ... assert 'held' == 'error'
E  AssertionError: Contract drift on a fan-out key produced no strategy_errors, so the
E     classifier cannot see it at all; got {... 'status': 'ok'}
```

Single-strategy drift falls back to `held` without the record and is `error` with it; fan-out drift
falls back to `ok` with nothing recorded and is `partial` with it. **That is exactly the claimed
routing — single-strategy drift → `error`, fan-out drift → `partial` — proven by observing the
failure, not by reading the code.** And the cursor behaviour is untouched either way: `stored` stays
0 in both, so the sum the cursor gate keys off is unchanged.

### 5c. The comments — checked for false claims, not just the code

⛔ The task was to check what the comments CLAIM, because round 2 caught a comment asserting a safety
property the code does not deliver.

| check | result |
|---|---|
| the round-2 defect — a comment claiming the cursor holds and the next tick retries after drift | **CORRECTED, and corrected in the strongest available form**: the comment now states the claim is true ONLY when every strategy on the key drifts, explains that the gate keys off a SUM over the whole key, and states that a sibling storing successfully advances the shared cursor past the drifted strategy's window |
| does it name the root-cause remedy? | ✓ — it names one shared per-key cursor as the structural cause and books the per-strategy cursor as **Phase 164.5.1.4 SYNCCURSOR** |
| does `164.5.1.4 SYNCCURSOR` resolve to exactly one roadmap entry? | ✓ — **exactly 1** heading, and **exactly 1** phase directory |
| does the log message carry the same caveat, not just the comment? | ✓ — the emitted error string itself says a sibling storing successfully still advances the shared cursor past this window, so an operator reading only the log is not misled |
| the held-cursor warning that "claimed every RPC failed" | **CORRECTED** — the warning now branches: one message for "no strategy on this key was eligible", another for "0 stored across every eligible strategy, each RPC either raised, returned an unreadable shape, or legitimately stored 0 rows." Both are true on the path that emits them |
| does any current comment overclaim the retry guarantee? | **No** — where a comment says the next tick retries the same window, the cursor genuinely did not advance on that path |

ℹ️ **One INFO-level imprecision found, reported rather than passed over — see Findings.** It concerns
a remedy sentence, not a safety property, and errs pessimistic.

---

## Behavioural, Static and Hygiene Checks

| check | how | result | status |
|---|---|---|---|
| full service suite, pristine | run once on the faithful scratch copy of the ship SHA | **5886 passed, 89 skipped**, exit 0 | ✓ PASS — reproduces the orchestrator's figure exactly |
| full service suite, D-03 neutered | same harness | **3 failed, 5883 passed, 89 skipped** | ✓ PASS — the gate bites |
| drift tests, pristine | named classes only | 3 passed | ✓ PASS |
| drift tests, neuter A | named classes only | 3 failed | ✓ PASS — bites |
| drift tests, neuter B | named classes only | 2 failed, 1 passed | ✓ PASS — the round-2 half bites on its own |
| type check | the service's own checker over the routers package | **Success: no issues found in 11 source files** | ✓ PASS |
| planning hygiene | the repo's own checker | **OK — 6660 tracked files scanned**, none carry the local username or an absolute home path | ✓ PASS |
| falsifiability guard, independently re-implemented | polarity-aware scan over all 14 phase artifacts + the ledger + the roadmap | **0 substantive unnegated claims** (see below) | ✓ PASS |
| guard calibration | claims injected then removed, on a scratch copy only | **8 → 9 → 10 → 8** | ✓ PASS — it bites |
| full identifiers added by the branch | scan of every added line, whole branch | **0** | ✓ PASS |
| debt markers on added service lines | scan of added lines under the service tree | **0** of any kind | ✓ PASS |
| skip-trailer tokens across all 39 commit messages | case-insensitive scan of every token form | **0** | ✓ PASS |
| release identity | the two version files compared byte-for-byte | **BYTE-EQUAL**, four-digit | ✓ PASS |
| changelog entry for this release | present, dated, and named for this phase | present | ✓ PASS |

⚠️ **The hygiene count differs by one from the orchestrator's reading (6660 vs 6659), and the
difference is explained rather than waved off:** the docs-only commit that landed mid-run added
exactly one tracked file. Both readings are OK verdicts. ⛔ The count line and the OK line are
different lines in that tool's output and were read separately, never via a last-line shortcut.

### On the falsifiability guard's raw number

My re-implementation is deliberately BROADER than the phase's own: it scans all 14 phase artifacts
(the phase's own guard scanned four named files) and uses a wider verb set. It reports **8**
line-scoped hits, and every one was individually adjudicated by reading the line:

- **6** are the guard DESCRIBING ITS OWN PREDICATE — sentences of the form "finds zero unnegated
  claims that the predicate was widened, fixed, or completed." A scanner cannot scan the sentence
  that defines it. That is a self-reference limit, not a defect.
- **2** are negations split across a physical line break by the phase's own documented
  line-break-controlled prose — read as sentences, both are negative.
- **0 are substantive.** No artifact this phase produced asserts, unnegated, that the predicate was
  repaired.

⭐ **The calibration matters more than the number.** A guard returning a stable figure proves nothing
unless it can move; mine was driven 8 → 9 → 10 → 8 on a scratch copy before its baseline was
believed. The repo files were never touched by that calibration.

---

## Requirements Coverage

| Requirement | Declared in | Status | Evidence |
|---|---|---|---|
| `FANOUT-COHORT-SIBLING-POLL-01` | roadmap Requirements line; plan 01 | ✓ SATISFIED | CLOSED as a refusal; harm kept booked; two-conjunct coupling restated as the revisit condition |
| `FANOUT-COHORT-SYNC-CONSTANT-01` | roadmap Requirements line; plans 01, 02, 03 | ✓ SATISFIED | FULLY CLOSED across two distinct halves; the cursor half backed by a shipped, calibrated fix |
| `FANOUT-COHORT-SIBLING-COMPOSITE-01` | roadmap Requirements line | ✓ SATISFIED — as a recorded DESCOPE | Descoped by founder decision at CONTEXT time; the ledger entry now carries the dated annotation recording that it deliberately has no owner phase and stays trigger-gated. **This is what closed the prior report's one human item.** |

No structured requirement ids are mapped to this phase; the obligation set is the three ledger
entries, as the roadmap entry itself states. No orphaned requirement ids.

### Roadmap Success Criteria

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | For each of the TWO sites, a recorded verdict with the measurement that justifies it; "leave" is valid and must be recorded as explicitly as a change | ✓ SATISFIED | Poll site: refused as inert, closed with the production measurement. Sync-constant site: `leave-book-future-phase`, routed to one named phase, with the 5-of-5 falsified-hypothesis reading stated |
| 2 | If the poll conjunct is widened, the second conjunct is addressed in the SAME decision, or the widening is refused as inert | ✓ SATISFIED | Refused as inert; the file is byte-unchanged, so the antecedent never fires and the refusal is the recorded branch |
| 3 | Any change to the sync constant names what it does to the cursor advance | ✓ SATISFIED (vacuously, and explicitly) | The constant is unchanged; the cursor-advance question was nonetheless answered on its own evidence and fixed independently, and the closure text says the two do not depend on each other |
| 4 | Any SQL change follows the three-reviewer + test-apply + human-gated production-apply path | ✓ SATISFIED (vacuously) | **Zero** SQL changes on the branch; zero new migrations. The one SQL edit that was considered — the false comment — was explicitly DROPPED rather than smuggled through |

---

## Findings

### ℹ️ INFO — one comment sentence names a narrower remedy than the code's own reachable paths

**Not a blocker, not routed to a phase, and no human decision is requested.** Recorded because the
task asked for the comments to be checked, and passing over a real observation would be the failure
mode this report exists to avoid.

Two comments describing the `held` status say the stalled key clears **only** when a strategy on that
key re-enters the allowed lifecycle set. That is true of the path the phase set out to fix — the
empty-eligible-strategies path — but `held` is also reachable with a NON-EMPTY eligible list, when
every per-strategy call returns a legitimate zero with no error recorded. **The phase itself knows
that path exists**: the warning message has a dedicated second branch for it, and a pre-existing test
pins it. On that path the key clears as soon as a store succeeds, with no lifecycle change.

**Why this is INFO and not a gap:**

- The **safety property** the comments assert IS delivered on every path: the cursor genuinely holds,
  the window genuinely grows, and the tick genuinely does not land in the healthy bucket. Nothing is
  lost, and no reader is invited to rely on a guarantee that does not exist.
- The imprecision errs **pessimistic** — it names a harder clearing condition than one reachable path
  requires. That is the opposite polarity from the round-2 defect, which claimed a protection the
  code did not provide.
- Under this repo's threshold it is neither user-facing nor data-integrity, so it is **fix-or-drop,
  never a phase**. The natural moment is whenever Phase 164.5.1.4 rewrites this block, which it will.

### ℹ️ INFO — debt markers on the branch are the planning idiom, not debt

Debt markers appear only in planning artifacts (a roadmap placeholder for a newly booked phase and
one line of instructional prose inside a plan). **Zero** debt markers of any kind were added under the
service tree. Not blocking.

### ℹ️ INFO — HEAD advanced mid-verification, docs-only

Enumerated in full at the top of this report. No code, test or SQL file differs between the ship SHA
and the new HEAD, so no measurement here is affected.

---

## Human Verification Required

**None.** The prior report's sole item is resolved in the ledger and was confirmed, not re-raised.
No truth is present-but-behaviour-unverified: every behavioural claim in this phase was exercised by
a test that this verifier drove RED under a real neuter and watched return GREEN on restore.

---

## Verdict

**The phase goal is achieved, and the prior report's one open item is closed.** Two sites, two
recorded verdicts, each carrying the evidence that justifies it. The refusal was held to a change's
standard and survived it: the predicate stands byte-unchanged, and the reason it stands unchanged is
checkable in the code rather than only in the prose. The one code change in scope shipped, and the
two review rounds hardened it in the direction that matters — a key that fetches trades and stores
none can no longer report itself healthy, and a contract drift can no longer fabricate a success
count nor hide from the classifier.

**What this verifier proved itself, at the ship SHA:** the untouched SQL and the identical blob hash;
the unchanged sync constant; the cursor formula and the vanished disjunct; the neuter → RED-on-3 →
restore → `cmp` cycle, independently reproducing the orchestrator's count; the pristine full suite at
5886/89 from a harness first proven faithful; two further independent neuters isolating the round-1
and round-2 halves of the drift fix and observing the exact routing contract fail; the `held` bucket's
registry, triage membership, log level and response-body presence, plus its two-armed completeness
gate; the two distinct closures and their single destination; the single roadmap entry and directory
for both destination phases; the zero-counts on the advisory lock; the descope annotation's existence
and coherence; a re-implemented and separately calibrated falsifiability guard; and the clean type
check, hygiene, identifier, debt-marker, skip-trailer and release-identity scans.

**What it took on report, and could not have taken any other way:** the production readings. They are
founder-run observations against a live database this verifier is forbidden to touch, holding no
credentials and acquiring none. Their *record* was verified — marker first, output-only, limits
pre-registered, no long identifiers — but their *content* rests on the founder's attestation. Stated
plainly rather than folded silently into a green verdict.

**Nothing is outstanding.** One INFO-level comment imprecision is recorded above; it is fix-or-drop
by this repo's own threshold, and it is deliberately not dressed up as a gap.

---

_Verified: 2026-09-19 at `8d90772e`_
_Verifier: Claude (gsd-verifier) — re-verification, supersedes the `ed1b7d92` report_
