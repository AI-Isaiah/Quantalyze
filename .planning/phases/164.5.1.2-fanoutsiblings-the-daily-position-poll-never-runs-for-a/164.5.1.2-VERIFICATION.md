---
phase: 164.5.1.2-fanoutsiblings-the-daily-position-poll-never-runs-for-a
verified: 2026-09-19T13:30:00Z
status: human_needed
score: 4/4 must-have claims verified
verified_at_sha: ed1b7d92caf8435051aef95686491fccae208a65
drift_subjects:
  - analytics-service/routers/cron.py
  - analytics-service/tests/test_cron_router.py
  - supabase/schema/functions/enqueue_poll_positions_for_all_strategies.sql
behavior_unverified: 0
overrides_applied: 0
evidence_provenance:
  - claim: "The poll-positions widening is refused as inert"
    machine_verified: "the predicate is byte-unchanged at HEAD; the second conjunct and the one-declaration/one-reader constant chain that starves it"
    founder_attested: "the 2026-09-17 and 2026-09-19 production readings — not reproducible by this verifier, which holds no credentials and is forbidden from acquiring them"
human_verification:
  - test: "Decide where FANOUT-COHORT-SIBLING-COMPOSITE-01 now belongs, and annotate its Destination line"
    expected: "Its Destination/Owner names a phase that has not already closed, or the entry carries a dated descope annotation naming the founder decision of 2026-09-17 that cut it from this phase's scope"
    why_human: "The entry is declared in this phase's ROADMAP Requirements line but was deliberately descoped by a founder decision. Which phase inherits it, or whether it simply rests trigger-gated with no owner phase, is a scope decision this verifier must not take. It is a ledger annotation, not code."
---

# Phase 164.5.1.2 FANOUTSIBLINGS Verification Report

**Phase Goal:** For each of the two remaining sites Phase 164.5.1.1 left alone, decide on this phase's own evidence whether leaving it alone is still right.
**Verified:** 2026-09-19
**Status:** human_needed — all four must-have claims VERIFIED; one ledger annotation awaits a scope decision
**Re-verification:** No — initial verification

⭐ **This phase's deliverable is VERDICTS, not diffs.** It was verified on that basis: a refusal
backed by evidence was held to the same standard a change would be, and was never marked
incomplete for lack of a code diff. Exactly one code change was in scope (D-03) and exactly one
shipped.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The poll-positions lifecycle conjunct is refused as inert and stands unchanged — not widened, not fixed, not addressed | ✓ VERIFIED | `git diff main...HEAD` over `supabase/` returns EMPTY; the canonical function still carries the two-value lifecycle literal exactly once; the owner-only status string appears 0 times in it; 0 new migrations on the branch |
| 2 | The D-03 cursor-advance defect is fixed, and the test proving it BITES | ✓ VERIFIED | `should_advance_cursor` reads `(not trades) or synced_count > 0` by symbol; the old disjunct survives only inside an explanatory comment, zero times in executable code; neuter→RED→restore→`cmp` performed by this verifier (below) |
| 3 | `FANOUT-COHORT-SYNC-CONSTANT-01` is fully closed in two DISTINCT paragraphs with different causes, routed to exactly one destination | ✓ VERIFIED | Two separately-headed closures with distinct, separately-sharpened TRIGGERs; `FULLY CLOSED` asserted only after both; destination resolves to exactly ONE roadmap heading and exactly ONE phase directory |
| 4 | The advisory-lock false claim carries a recorded DROP decision with a named TRIGGER, and the lock is genuinely absent | ✓ VERIFIED | Lock name occurs in exactly 2 files, both as `--` comment text; 0 calls of `advisory_lock` under any name in `analytics-service/`; the real guard exists and is wired; DROP decision + opportunistic TRIGGER recorded, never silence |

**Score:** 4/4 truths verified (0 present-but-behavior-unverified)

---

## Claim 1 — the widening was REFUSED, and the refusal is real

**Verified by this verifier, independently.**

| check | result |
|---|---|
| `git diff --stat main...HEAD -- supabase/` | EMPTY — no SQL touched on this branch at all |
| new files under `supabase/migrations/` | 0 |
| lifecycle literal in the canonical poll function | present, once, unchanged |
| owner-only status string in that file | 0 occurrences |
| second conjunct intact | `EXISTS` on `compute_jobs`, keyed `cj.strategy_id = s.id`, `kind = 'sync_trades'`, `status = 'done'`, 30-day window — all present |

**The inert-ness argument is not merely asserted in prose; its backbone is checkable in code, and
this verifier checked it rather than taking D-02's word.** `ALLOWED_STRATEGY_STATUSES` has exactly
ONE declaration and exactly ONE reader in `analytics-service/routers/cron.py` (cited by symbol; the
reader is the lifecycle filter inside the `/cron-sync` handler that builds `strategy_ids`), and its
three-value set omits the owner-only status. So the conjunct that would have to be satisfied for a
lifecycle widening to change anything is starved one level upstream. **A widening of the lifecycle
conjunct alone therefore changes nothing observable — which is why it was correctly refused rather
than shipped.**

**TODOS closure** — `FANOUT-COHORT-SIBLING-POLL-01` is CLOSED, and its closure text states the
verdict as a refusal, keeps the harm booked, and restates the two-conjunct coupling as the
condition on any future revisit. It does not claim the predicate was repaired.

ℹ️ **INFO, strengthening the verdict rather than qualifying it.** Searching for the producer of a
`compute_jobs` row of the `sync_trades` kind, this verifier found none in the Python or TypeScript
at HEAD outside test fixtures; a schema comment describes such a poller in the past tense. This is
recorded as an observation only — it makes the second conjunct *harder* to satisfy, never easier,
so it cannot weaken a refusal premised on that conjunct binding. It is out of this phase's scope
and no action is proposed here.

⚠️ **Taken on report, by necessity:** the production readings (2026-09-17 poll/snapshot table;
2026-09-19 sibling-key counts) are founder-run SQL-editor observations. This verifier holds no
production credentials and is forbidden from acquiring them, so it cannot re-run them. What it
*can* verify, and did: `164.5.1.2-PROD-SESSION.md` records the database-identity marker FIRST, ahead
of both read statements, records every reading as OUTPUT rather than as the query that would
produce it, carries a limit registered BEFORE the read, and contains no identifier longer than 8
characters.

---

## Claim 2 — the D-03 fix, and the anti-vacuity calibration

**Verified by this verifier, independently, including the neuter.**

**Fix, by symbol:** in `analytics-service/routers/cron.py::_sync_single_key`,
`should_advance_cursor = (not trades) or synced_count > 0`. The `bool(strategy_ids)` disjunct is
gone from executable code — its single surviving occurrence in the file is inside the dated audit
comment that explains why it was dropped, which is documentation, not logic.

**Test, by symbol:** `test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor`, the third
member of `TestC0198CursorOnlyAdvancesWhenStored` in
`analytics-service/tests/test_cron_router.py`. It drives `_sync_single_key` end-to-end with real
trades fetched and an empty eligible-strategy list, and asserts no cursor payload is written. It
deliberately leaves the RPC unstubbed so a regression that calls it fails loudly instead of passing
quietly.

### Anti-vacuity: neuter → RED → restore → `cmp`

⛔ The test's existence was NOT taken as proof it bites. This is data-integrity, so the calibration
was performed.

| step | what was done | result |
|---|---|---|
| backup | `cp` byte backup taken; sha256 recorded | taken |
| neuter | the pre-fix disjunct restored into the formula, replacement count asserted `== 1` before writing | **applied** — verified present in the tree, fixed line absent (a neuter that does not apply would read GREEN and measure nothing) |
| observe | named class run from `analytics-service/` | **RED** |
| restore | `cp` from the byte backup | `cmp` reports BYTE-IDENTICAL; sha256 matches the pre-neuter value; `git status --porcelain` empty |
| re-confirm | named class re-run after restore | 3 passed |

**OBSERVED failure text under the neuter, quoted:**

```
E       AssertionError: Expected no last_sync_at cursor advance when strategy_ids=[] despite fetched trades; got [{'last_sync_at': '...'}]
E       assert False
tests/test_cron_router.py:<assert> AssertionError
=========== 1 failed, 2 passed in 2.58s ===========
```

⭐ **`1 failed, 2 passed` is the load-bearing detail.** Only the D-03 member went red; both
pre-existing C-0198 members stayed green under the same neuter. The new test therefore measures a
condition neither older test covers — it is not a duplicate riding their coverage. `collected 3`
throughout; no run reported 0 collected.

**The tree was left byte-identical to how it was found, and that is proven, not asserted:** `cmp`
clean, sha256 match, `git status --porcelain` empty. No `git checkout`, `git restore` or `git stash`
was used at any point.

---

## Claim 3 — two distinct closures, one destination

**Verified by this verifier, independently.**

| check | result |
|---|---|
| widening-verdict closure | present, headed as closing that half ONLY, and stating in its own text that the D-03 half is explicitly not closed by it |
| D-03 cursor-advance closure | present, separately headed, citing the shipped formula by symbol, the test by exact name, and both shipping commits |
| causes kept distinct | ✓ — each half carries its own separately-sharpened TRIGGER; neither restates the other's |
| `FULLY CLOSED` marker | asserted once, after both halves, explicitly naming them as distinct per the locked decision |
| destination heading in ROADMAP | **exactly 1** |
| destination phase directory | **exactly 1** |
| blank/placeholder destination | none — the destination is named directly, a generic placeholder having been corrected to the booked name |

The destination directory currently holds only a `.gitkeep`, which is the expected state of a
booked-but-unplanned phase and not a blank destination.

---

## Claim 4 — the advisory-lock decision

**Verified by this verifier, independently; the 0-count was re-derived rather than accepted.**

| check | result |
|---|---|
| occurrences of the named lock, repo-wide (code file types) | **2 files** — the canonical function and its source migration |
| are those occurrences executable? | **No** — both are `--` comment lines asserting the guard exists at the worker layer |
| `advisory_lock` calls of ANY name in `analytics-service/` | **0** |
| ...specifically in the worker module | **0** |
| the guard the decision relies on instead | `_daily_enqueue_already_ran_today` exists in the worker module and is called at the daily tick; the tick itself calls the poll RPC once per UTC day |

**The decision is recorded, not silence:** a DROP of a same-phase migration fix, with the cost
rationale stated (a comment-only change with no functional effect does not justify the three-
reviewer plus test-apply plus human-gated production-apply pipeline) and an **opportunistic TRIGGER**
named — correct the comment the next time that function's migration is genuinely touched for
another reason, never as a standalone migration. That is a DROP-with-a-named-TRIGGER, exactly as
required.

⚠️ **One consequence worth stating so it is not mistaken for a gap:** the older, pre-existing part
of the `FANOUT-COHORT-SIBLING-POLL-01` entry still repeats the false lock sentence, because this
repo's TODOS convention is append-and-annotate and existing lines are never rewritten. The
correction lives in the appended note, in the roadmap, and in this phase's research artifacts. This
is the convention working as designed, not a missed edit.

---

## Behavioral and Regression Checks

| check | command | result | status |
|---|---|---|---|
| named test class, restored tree | pytest on `TestC0198CursorOnlyAdvancesWhenStored` | collected 3, **3 passed** | ✓ PASS |
| named test class, neutered tree | same | collected 3, **1 failed, 2 passed** | ✓ PASS (RED as designed) |
| full service suite | pytest from the service directory, main checkout interpreter | **5880 passed, 89 skipped**, exit 0 | ✓ PASS |
| falsifiability guard, re-derived | independent polarity-aware scan of the produced artifacts | **0** unnegated claims | ✓ PASS |
| guard calibration | scratch copy only, claims injected then removed | **0 → 1 → 2 → 0** | ✓ PASS — it bites |
| full identifiers added by this branch | diff-scoped scan | **0** | ✓ PASS |
| debt markers in added code lines | scan of added lines under the service directory | **0** | ✓ PASS |

**The full suite was run ONCE and matches the orchestrator's figure exactly (5880 / 89).** The
guard's own result was re-derived rather than accepted: my independent scan reproduced zero
unnegated claims across the four scanned artifacts. Three further lines surfaced in the plan-03
summary, and all three are the guard **describing itself** — it cannot scan the summary that
reports it. That is a self-reference limit, not a defect; this verifier's own scan covered that file
and found no substantive claim.

⭐ **The calibration matters more than the result.** A guard returning zero proves nothing unless it
can return non-zero; mine was driven 0 → 1 → 2 → 0 on a scratch copy before its zero was believed.
The repo file was never touched by that calibration and the working tree is clean.

---

## Requirements Coverage

| Requirement | Declared in | Status | Evidence |
|---|---|---|---|
| `FANOUT-COHORT-SIBLING-POLL-01` | plan 01 | ✓ SATISFIED | CLOSED as a refusal, harm kept booked, coupling restated as the revisit condition |
| `FANOUT-COHORT-SYNC-CONSTANT-01` | plans 01, 02, 03 | ✓ SATISFIED | FULLY CLOSED across two distinct halves; the D-03 half backed by a shipped, calibrated fix |
| `FANOUT-COHORT-SIBLING-COMPOSITE-01` | ROADMAP Requirements line for this phase | ⚠️ DESCOPED, DESTINATION NOW STALE | See finding below |

No REQUIREMENTS.md ids are mapped to this phase; the obligation set is the TODOS entries, as the
roadmap entry itself states. No orphaned REQUIREMENTS.md ids.

---

## Findings

### ⚠️ WARNING — a descoped requirement still names this phase as its owner

`FANOUT-COHORT-SIBLING-COMPOSITE-01` is listed in this phase's ROADMAP **Requirements** line. It was
then cut from scope by a dated founder decision (2026-09-17) on the ground that the function is
unscheduled, uncalled, and observed by no user — a decision recorded in both the phase context and
the roadmap goal, and this verifier has no quarrel with it.

**But the ledger entry itself was not annotated.** Measured: the entry was untouched by this
branch, carries no descope note, and still reads `Destination: Phase 164.5.1.2 FANOUTSIBLINGS —
Owner: that phase`. The moment this phase closes, that entry names a finished phase as its owner —
the "dangling condition with no owner" failure mode this phase's own context document warns about
in a different setting.

**Why this is a WARNING and not a BLOCKER:** the phase's success criteria cover TWO sites, not
three, deliberately and on the record; the phase delivered verdicts for both. The entry is
trigger-gated on someone proposing the widening, so nothing silently rots in the meantime. Nothing
in the four must-have claims depends on it.

**Remedy (one line, ledger only):** append a dated note to that entry either re-pointing its
Destination at a phase that has not closed, or recording that the 2026-09-17 founder decision left
it trigger-gated with no owner phase by design. Routed to the human because choosing between those
two is a scope decision.

### ℹ️ INFO — `TBD` markers on the branch are the planning idiom, not debt

Two `TBD` occurrences appear in added lines, both in planning artifacts: the roadmap's standard
plan placeholder for the newly booked destination phase, and one line of instructional prose inside
a plan. Zero debt markers of any kind were added under the service directory. Not blocking.

---

## Human Verification Required

### 1. Re-point or annotate the descoped third requirement

**Test:** Read `FANOUT-COHORT-SIBLING-COMPOSITE-01` in the backlog ledger and decide its owner.
**Expected:** Its Destination/Owner names a phase that has not already closed, OR the entry carries
a dated annotation recording that the 2026-09-17 founder decision removed it from this phase's
scope and left it trigger-gated with no owner phase.
**Why human:** A scope decision, not a fact to measure. This verifier can observe that the
destination is stale; it must not choose the new one.

---

## Verdict

**The phase goal is achieved.** Two sites, two recorded verdicts, each carrying the evidence that
justifies it. The one code change in scope shipped and its gate was proven to bite under a real
neuter. The refusal was held to a change's standard and survived it: the predicate stands
unchanged, and the reason it stands unchanged is checkable in the code, not only in the prose.

**What this verifier proved itself:** every code-level fact above — the untouched SQL, the formula
and the vanished disjunct, the neuter-RED-restore-`cmp` cycle, the full suite, the two distinct
closures, the single destination, the zero-count on the lock, the re-derived and separately
calibrated falsifiability guard, the zero added identifiers.

**What it took on report, and could not have taken any other way:** the production readings. They
are founder-run observations against a live database this verifier is forbidden to touch. Their
*record* was verified — marker first, output-only, limits pre-registered, no long identifiers — but
their *content* rests on the founder's attestation. Stated plainly rather than folded silently into
a green verdict.

**One loose end**, and it is a ledger annotation rather than code: a requirement descoped by
decision still names this phase as its owner.

---

_Verified: 2026-09-19_
_Verifier: Claude (gsd-verifier)_
