---
phase: 164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a
plan: 03
subsystem: release-bookkeeping
tags: [changelog, version-gate, todos, deferral-routing, ledger-correction]
status: complete
requires:
  - 164.6.3-01 (the tracer — the classifier, the detector job, the uniform aggregator arm)
  - 164.6.3-05 (the MW02 oracle repair)
  - 164.6.3-02 (the all-sixteen widening, which NAMED `[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]` in ci.yml and left the ledger half owing)
provides:
  - "TODOS.md — `[CI-DOCSPATH-01]` owner re-pointed to Phase 164.6.3 with the dated reason, superseded OWNER line kept as lineage"
  - "TODOS.md — the mutex claim corrected from two takers to THREE, `python` named, settled by a log reading"
  - "TODOS.md — the entry's own PR #750 premise corrected: it classifies as CODE under the shipped predicate"
  - "TODOS.md — a STATUS line stating what shipped and what is owed, checkbox deliberately still OPEN"
  - "TODOS.md — `[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]`, owner Phase 164.6 GATE-HYGIENE"
  - "VERSION + package.json — 0.77.40.0, byte-equal 4-digit, hand-edited"
  - "CHANGELOG.md — ONE unified entry, six sections, every branch commit mapped to a bullet"
affects:
  - 164.6.3-04 (wave 5 — the two real PRs; criterion 5 must count THREE absent mutex acquires)
tech-stack:
  added: []
  patterns:
    - "carry the superseded claim beside the correction rather than erasing it (TODOS.md house convention)"
    - "a ledger correction cites a MEASUREMENT (a log reading) rather than restating a number"
    - "the CHANGELOG commit-checklist mechanism: enumerate → read the diff → group → cross-check"
key-files:
  created: []
  modified:
    - TODOS.md
    - CHANGELOG.md
    - VERSION
    - package.json
    - .planning/phases/164.6.3-cidocspath-…/deferred-items.md (§5, §6 — scope-noted, see Deviations)
decisions:
  - "Version 0.77.40.0 — third digit, fourth reset to 0, matching this repo's convention for a shipped capability change (0.77.39.0 → 0.77.40.0). Hand-edited in both files; `npm version` never run."
  - "`[CI-DOCSPATH-01]` left OPEN. Its own stated proof is two real pushes; shipped code and green local tests do not discharge a measurement."
  - "The entry's PR #750 premise was ALSO corrected, beyond the plan's A/B/C/D — it is a measured falsehood inside the entry being edited and the phase's own motivating claim. Recorded as a deviation."
  - "The CHANGELOG entry states the roadmap's headline OVERSTATES the saving. Shipping a changelog that repeats a motivating claim the implementation falsified was the failure mode to avoid."
  - "`actuals.tokens` reported on the instruction's chars/4 scale AND the divergence from this phase's three sibling SUMMARYs named, rather than silently picking one scale."
metrics:
  duration: ~35 min
  completed: 2026-09-13
  tasks: 2
  files: 4
commits: 6
plan_head_before: dbba8127d1da8db497c1a5338f1d2bd9fa4daf5a
actuals:
  tokens: 7700
  tasks: 2
  commits: 6
  tokens_note: >-
    MEASURED as chars/4 over this plan's realized diff — 27,962 added characters
    across the two task commits plus 2,773 in the uncommitted deferred-items.md
    addition = 30,735 chars ≈ 7,700. ⚠️ STATED RATHER THAN QUIETLY RECONCILED:
    this phase's three sibling SUMMARYs report 32,000 (plan 01), 58,000 (plan 05)
    and 96,000 (plan 02), none of which is chars/4 of their own diffs — plan 05
    changed one 644-line file. The siblings are on a work-volume scale and this
    figure is on the instruction's diff scale, so the four numbers in this phase
    are NOT comparable to each other. Whoever calibrates from them must pick one
    scale and re-derive; averaging them measures the measurement methods.
  commits_note: >-
    ⭐ STATED INCLUSIVE OF THE COMMIT THAT RECORDS IT, which is the only
    self-consistent convention available: a SUMMARY reporting its own commit
    count has no fixed point otherwise — each correction of the integer is
    itself a commit that invalidates the integer. Measured with
    `git rev-list --count dbba8127…HEAD` against the ledger persisted at
    `.git/worktrees/…/gsd-plan-head-before-164.6.3-03` before the first commit;
    it read 5 immediately before the final commit that writes this line, so the
    inclusive value is 6. ⚠️ I CHASED THIS TWICE BEFORE STATING IT — the field
    read 2 (taken before this file's own commit existed) and then 4 (taken
    before the CHANGELOG fix) — which is the same stale-integer shape as
    deviation 5, committed twice in this file's own frontmatter. The six: two
    task commits, the metadata commit adding this file, one `fix(164.6.3-03)`
    correcting the shipped CHANGELOG's stale count, and two `docs(164.6.3-03)`
    commits bringing this SUMMARY into agreement with its own history.
    ⛔ A verifier reading a HIGHER number is in agreement, not drift — re-run
    the command, do not quote the integer.
---

# Phase 164.6.3 Plan 03: the bookkeeping and the release toll

The ledger now says what shipped — a re-pointed owner, a corrected mutex count, a corrected
premise and a newly booked deferral with a named owning phase — and the PR pays its release toll
with one unified CHANGELOG entry cross-checked commit by commit, which caught an arithmetic defect
in its own draft.

## Commits

| Task | Commit | What |
|------|--------|------|
| 1 | `17551ad0` | `[CI-DOCSPATH-01]` re-pointed + three corrections + `[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]` booked |
| 2 | `2019ceaf` | `chore(release): v0.77.40.0` — VERSION + package.json + the unified CHANGELOG entry |
| — | `c8f4655e` | this SUMMARY + `deferred-items.md` §5/§6 + the ROADMAP progress row (handler collateral repaired) |
| 2 (fix) | `2c8d9977` | the CHANGELOG's commit count was stale one commit after it shipped — see deviation 5 |
| — | `a2d59efe` | this SUMMARY caught up to its own commits (`commits:` 2 → 4) |
| — | *(final)* | this SUMMARY's terminal correction — `commits:` stated INCLUSIVE of the commit recording it |

`commits:` is MEASURED — `git rev-list --count dbba8127d1da8db497c1a5338f1d2bd9fa4daf5a..HEAD` read
**5** immediately before the final commit, so the **inclusive** value is **6**. ⭐ Inclusive is the
only self-consistent convention here: each correction of the integer is itself a commit that
invalidates the integer, and I chased that twice (2 → 4 → 6) before stating the rule instead of
another number.

---

## Task 1 — the three corrections and the booked deferral

### A. The owner, re-pointed with the dated reason

The entry read **`OWNER 2026-09-06: Phase 164.6 GATE-HYGIENE item (12)`, success criterion 9**. That
pointer is now dead in both halves: the founder's **2026-09-12 split** moved items (9), (11) and
(12) into `164.6.1 MYPYSTRICT` / `164.6.2 MT5RELOGIN` / `164.6.3 CIDOCSPATH` **and their success
criteria moved with them**, so 164.6's criterion 9 is today a struck-through numbered stub reading
*"MOVED 2026-09-12 to Phase 164.6.3 CIDOCSPATH"* — verified by reading `.planning/ROADMAP.md`'s
Phase 164.6 block at HEAD. A reader following the old pointer lands on a phase that does not hold
the item and a criterion with no text.

⭐ The superseded `OWNER` line is **struck through and kept**, not deleted — this file's convention
is to carry the superseded claim beside the correction, and `[MYPY-MAINPY-01]` two entries above
demonstrates the same shape.

### B. The mutex undercount, corrected to THREE and settled by a measurement

Three places in the entry said two; all three now say three, with `python` named:

| Where | Before | After |
|---|---|---|
| title line | "two shared-TEST-DB mutex holds" | "THREE shared-TEST-DB mutex holds", with a note that it read "two" until 2026-09-13 |
| body | "`e2e-seeded` and `sql-tests` each take the shared-TEST-DB advisory mutex" | "THREE jobs each take the shared-TEST-DB advisory mutex `61616158`", superseded sentence quoted below it |
| new correction paragraph | — | names `python`, cites its own 158-REVIEW CR-04 derivation, and states criterion 5's consequence |

**The measurement that settles it is a LOG READING, not a restated number** — run `34717952454`'s
log archive carries **three distinct acquires of key `61616158`: 5s, 340s, 615s**. The entry also
states that `.planning/ROADMAP.md` and this phase's `164.6.3-CONTEXT.md` carry the same undercount
and that **this correction is the one to cite**. `ci.yml`'s `python` job header already carries the
same record (wave 3), so the number now appears where the evidence will be counted as well as in
the ledger.

### C. The checkbox, deliberately still open

A `STATUS 2026-09-13` line was added enumerating **what shipped** (the detector + classifier, the
conjunct on all sixteen job keys, the uniform aggregator arm + `ALWAYS_ON`, the contract guard, the
MW02 repair) and **what is owed** (the two real pushes, both job sets from `gh run view --json jobs`,
and the **three** absent acquires read out of the docs-only run's LOG). ⛔ The entry says in terms
that it must not be ticked on the strength of shipped code and green local tests.

### D. The deferral, booked to a named phase

`[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]` now exists in `TODOS.md`, stating:

- the measured fact — `src/__tests__/lint-sql-gates.test.ts` reads `.planning/REQUIREMENTS.md` and
  `.planning/ROADMAP.md` (its `G3` arm, verified at `:161`), `src/__tests__/verify-plan-anchors.test.ts`
  reads `.planning/phases/159-rank-public-ranking-integrity/159-VERIFICATION.md` (verified at `:782`),
  and `frontend-test` now carries `needs: [changed-paths]` + the docs-only conjunct;
- the consequence — a `.planning/`-only PR reintroducing the forbidden five-shapes sentence, or
  deleting that VERIFICATION file, is not caught by its own board;
- **both halves of the severity** — DEFERRED, never LOST, because the merge push is unfiltered and
  the next code PR runs them, with an explicit ⛔ against restating only one half;
- the two refused remedies with reasons (widening the always-on set gives back most of the saving;
  moving the assertions changes which gates exist, which the phase boundary forbids);
- ⭐ **`OWNER 2026-09-13: Phase 164.6 GATE-HYGIENE`**, with three stated reasons — its declared
  competence is "when is a gate invoked, and can I prove it still bites", it is the phase 164.6.3
  was split out of, and it is still unplanned (`**Plans:** 0 plans`, verified in ROADMAP at HEAD),
  which is the preferred destination shape.

⚠️ **This closes the gap wave 3 flagged.** `ci.yml`'s `frontend-test` header already named the id and
the owner; the ledger did not, so only one of the two said it. Both now do.

### Task 1 verification, as the plan specifies

| Command | `<fails_when>` | Result |
|---|---|---|
| `grep -c "164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION" TODOS.md` | 0 (not booked) or >1 (duplicated) | **`1`** ✅ |
| `git diff --numstat TODOS.md` | nothing printed, or deleted > added | **`101  7  TODOS.md`** ✅ |
| `npm run lint` | non-zero, or `check-planning-hygiene` names `TODOS.md` | **rc 0**, `[check-planning-hygiene] OK — 6350 tracked files scanned`, `grep -c "TODOS.md"` over its output = **0** ✅ |

**§D, the no-collateral check, performed as an enumeration rather than an eyeball.** All seven
deleted lines printed and each is one this commit rewrites:

```
-  and two shared-TEST-DB mutex holds for a markdown edit (logged 2026-09-06, at the PR #750 merge).**
-  368s + 344s, `lighthouse-mobile` 305s. ⚠️ The wasted minutes are the SMALL half: `e2e-seeded`
-  and `sql-tests` each take the shared-TEST-DB advisory mutex, so a roadmap typo fix queues ahead
-  **OWNER 2026-09-06: Phase 164.6 GATE-HYGIENE item (12)**, success criterion 9. Routed there
-  rather than left here because "when is a gate invoked, and can I prove it still bites" is
-  precisely 164.6's competence. ⛔ NOT in scope: changing WHICH gates exist or what they contain.
-  Source: measured live at the PR #750 merge gate; see `.planning/ROADMAP.md` Phase 164.6 item (12).
```

No adjacent entry was touched; `[MYPY-MAINPY-01]` above and `[PLANANCHOR-SUMMARY-FILTER-01]` below
are byte-unchanged.

---

## Task 2 — the release toll

### A. The bump, by hand, and the byte comparison

`0.77.39.0` → **`0.77.40.0`**: third digit forward, fourth reset to 0, matching this repo's
convention for a shipped capability change (`0.77.38.1` → `0.77.39.0` → this). ⛔ `npm version` was
**never run**; `package-lock.json` is verifiably absent from the diff
(`git diff --name-only … | grep -c package-lock.json` = **0**).

**The byte comparison the plan requires, recorded:**

```
VERSION=[0.77.40.0] (9 bytes)
package=[0.77.40.0] (9 bytes)
BYTE-EQUAL: YES
```

`VERSION` carries **no trailing newline** before or after (9 bytes exactly, confirmed with `xxd`),
so the file's shape is unchanged as well as its content being correct.

### B. The CHANGELOG mechanism, run in its stated order

**Step 1 — enumerate and COUNT.** `git log eab976bb26c2cba8155de2416b7614cc7e223f42..HEAD --oneline`
printed **27** commits at wave-4 start; **28** after task 1; **29** after the release commit
(`git rev-list --count` at final HEAD = **29**, re-measured).

**Step 2 — read the full diff.** `git diff eab976bb...HEAD` read across all seven non-planning paths
(`ci.yml` +296, `classify-changed-paths.mjs` +327 new, `ci-docs-path-filter.contract.test.ts` +986
new, `lint-sql-gates.test.ts` ±644, the MW02 green fixture, `contracts-registry.test.ts`,
`REGISTRY.md`). ⭐ This is not ceremonial — reading the diff is what surfaced that the plan's
suggested `### Added`/`### Changed` split understated the **`### Fixed`** and **`### Root cause`**
content, since three of the ten code commits are corrections of claims that shipped false.

**Step 3 — grouped by theme BEFORE writing.** Six themes, all from this repo's own vocabulary:
`### Added` · `### Changed` · `### Fixed` · `### Root cause` · `### Tests` · `### Notes`.
⛔ keep-a-changelog's four were not used. `### Root cause` was chosen deliberately over folding the
MW02 class-fix into `### Fixed`, per CLAUDE.md's *"do not flatten a root-cause note into Fixed"*.

**Step 4 — ONE unified entry**, `## [0.77.40.0] - 2026-09-13`, newest first. No earlier per-commit
entries for this branch existed, so none had to be replaced.

**Step 5 — the CROSS-CHECK, and it found a defect.** See the table below.

**Step 6** — nothing was asked of the founder; every bullet is inferred from the diff and the log.

### The commit-to-bullet cross-check — all 29, explicit

| # | Commit | Maps to |
|---|--------|---------|
| 1 | `3bb7d40a` split Phase 164.6, insert 164.6.1 GATEINFRA | Changed → roadmap-structure bullet |
| 2 | `3a8bcc3b` one phase per item — 164.6.1/2/3 | Changed → same bullet ("then chose one phase per item") |
| 3 | `33196e62` smart discuss context | Notes → Process (phase-artifact commits) |
| 4 | `32771a98` research the docs-only CI path filter | Notes → Process |
| 5 | `559c9a5f` validation strategy + correct CONTEXT | Notes → Process; **also** Notes → three-mutex bullet |
| 6 | `f4bd1604` map patterns to existing analogs | Notes → Process |
| 7 | `dad757b2` create phase plan | Notes → Process |
| 8 | `94bfeb6c` revision 1 — two-member ALWAYS_ON | Notes → Process; **also** Notes → always-on/second-layer bullet |
| 9 | `94e9cbfd` rebuild the validation map, 24 rows | Notes → Process (the rebuilt 24-row map) |
| 10 | `581b04b4` 164.5.1 gateway-ceiling inversion, criteria 7-9 | Changed → roadmap-structure bullet |
| 11 | `668b8e08` wire the filter end to end for one job | Added → classifier + detector job + uniform arm; Fixed → the #750 header correction; Notes → the #750 premise |
| 12 | `0963ad6d` register the guard + fail-closed/always-on scenarios | Added → contract guard/registry; Changed → floor 57→59; Tests → S5/S2a/S2b |
| 13 | `afca23ac` complete the tracer plan (SUMMARY) | Notes → Process (three plan SUMMARYs) |
| 14 | `0527f90c` insert plan 05 | Notes → Process (one plan inserted mid-phase) |
| 15 | `c91bd2c4` the MW02 oracle executes what it claims to measure | Fixed → MW02 blind-spot bullet; Root cause → slice + unset-variable |
| 16 | `1be4308e` pin the tolerance as UNIFORM + the SLICE PIN | Tests → the partition/REPAIR-PROOF bullet |
| 17 | `4892c9a9` the repair proof + register drift routed | Tests → REPAIR PROOF; Notes → `[WINDOWS-LEDGER-COUNT-DRIFT]` |
| 18 | `bb3c4a75` merge executor worktree | Notes → Process (two merge commits) |
| 19 | `7d04da83` complete the MW02 repair plan (SUMMARY) | Notes → Process |
| 20 | `c07b0093` 164.6 gains criterion 14 + three MOVED stubs | Changed → roadmap-structure bullet; Notes → WINDOWS bullet ("now criterion 14") |
| 21 | `9f46f856` the filter reaches all sixteen job keys | Changed → sixteen-job-keys, explicit-not-inherited, three corrected headers; Notes → deferred-detection + three-mutex |
| 22 | `c03b1517` S3/S7/S8 + the exact-set pins | Tests → eight scenarios; Tests → exact-set pins |
| 23 | `ea46ea02` the detector's red path, manufactured and OBSERVED | Tests → the durable CALIBRATION block |
| 24 | `17969000` the placement pin reddened with the WRONG cause | Fixed → placement-pin bullet |
| 25 | `54650a4f` the detector header's INLINE `needs:` claim | Fixed → changed-paths-header bullet |
| 26 | `dbba8127` merge executor worktree | Notes → Process (two merge commits) |
| 27 | `6eda78ff` complete the all-sixteen filter plan (SUMMARY) | Notes → Process |
| 28 | `17551ad0` **this plan's task 1** | Changed → the `TODOS.md` bullet; Notes → deferred-detection + three-mutex |
| 29 | `2019ceaf` **this plan's release commit** | the entry itself; Notes → Process |

**29 commits over 6 themes; all 6 themes present; 0 unrepresented commits.**

### ⭐ The cross-check found a real defect in my own draft — treated as one, not waved through

The `### Notes` process bullet originally read *"five roadmap-structure commits … one plan inserted
mid-phase"*, which **double-counts `0527f90c`** (`docs(164.6.3): insert plan 05` is both). The parts
summed to **30** against a branch of **29**. Corrected before the commit, and the arithmetic is now
written into the bullet itself — `2+10+4+7+3+1+1+1 = 29` — so a future reader can re-check it rather
than trust it. ⛔ This is exactly the miss CLAUDE.md's step 5 says to treat as a defect; it was found
by performing the cross-check, not by reading the draft again.

### C. The `### Notes` section carries every required limit — checked off individually

| Required by the plan | Present |
|---|---|
| The claimable saving is the PR-side run ONLY; the merge push is unfiltered and still pays the corpus **and the lock** | ✅ first Notes bullet, with `ci.yml`'s per-SHA-green header and Railway's dependency as the reason |
| THREE mutex takers, not two — `python`, `e2e-seeded`, `sql-tests`, correction named | ✅ third bullet, with the 5s/340s/615s log reading |
| The deferred-detection limit, its booked id, and the owning phase | ✅ fourth bullet, naming Phase 164.6 GATE-HYGIENE and both refused remedies |
| `contracts.yml` residual expected, out of scope by the phase boundary | ✅ fifth bullet |
| The always-on set is FIVE, and WHY `frontend-lint` is on it | ✅ sixth bullet, with the `check-planning-hygiene.ts` chain and the ~70 s measured cost |
| ⭐ The SECOND, shell-level layer — `frontend-lint` + `plan-anchor-verify` in `ALWAYS_ON`, defence in depth, not dead today | ✅ same bullet, with the ⛔ against deleting it as dead weight |

**Plus, beyond the required set** (and the one the coordinator called out): a Notes bullet stating
plainly that **the ROADMAP's motivating example, PR #750, classifies as CODE under the predicate
this phase shipped**, so the headline "~50 job-minutes for a markdown edit" OVERSTATES what the
phase recovers, and that the allow-list was refused a widening to make it fit. ⛔ Shipping a
changelog that repeats a motivating claim the implementation falsified was the specific failure mode
to avoid here.

### D. The gate this entry exists to satisfy — both runs, verbatim

```
$ node scripts/check-version-bump.mjs --self-test
=== SELF-TEST 1/5: a correct bump is clean
  ok   — no defects on a well-formed release
=== SELF-TEST 2/5: source changed, VERSION frozen (the PR #746 shape)
  ok   — version-not-bumped fires
=== SELF-TEST 3/5: planning-only PR is EXEMPT
  ok   — a roadmap-only PR needs no release
=== SELF-TEST 4/5: VERSION and package.json disagree
  ok   — the 3-digit package.json trap fires
=== SELF-TEST 5/5: bumped but no CHANGELOG heading
  ok   — changelog-missing-entry fires

=== SELF-TEST PASSED: 5/5, every defect kind fired on its own input ===
selftest_rc=0
```

```
$ node scripts/check-version-bump.mjs
base=0.77.39.0 head=0.77.40.0 package=0.77.40.0 changed=28
✅ No defects
gate_rc=0
```

⭐ `changed=28` is the gate reading the **28 files** this branch changes against `origin/main` — the
seven non-planning paths plus twenty-one planning ones — which is what makes the version gate
genuinely apply here. No `::error::` line of any kind appeared.

---

## Verification — the plan's `<verification>` block, complete

| Command | Required | Result |
|---|---|---|
| `node scripts/check-version-bump.mjs --self-test` | exit 0, terminal banner present | **rc 0**, `SELF-TEST PASSED: 5/5` ✅ |
| `node scripts/check-version-bump.mjs` | exit 0, no `::error::` | **rc 0**, `✅ No defects` ✅ |
| `npx vitest run src/__tests__/critical-regressions.test.ts` | exit 0, byte-equality arm not among failures | **rc 0**, `Tests 162 passed (162)` ✅ |
| ↳ the arm itself, by name (verbose reporter) | present, not vacuously absent | `✓ … [CRITICAL-02] VERSION / package.json drift > VERSION file must equal package.json version` ✅ |
| `npm run lint` | exit 0 | **rc 0** (run twice — after task 1 and again after the release edits) ✅ |
| `git diff --stat` vs plan base | exactly the four files; lockfile NOT present | `CHANGELOG.md \| 216`, `TODOS.md \| 108`, `VERSION \| 2`, `package.json \| 2` — **4 files**; `grep -c package-lock.json` over `--name-only` = **0** ✅ |

⚠️ `--reporter=basic` was NOT used anywhere — it is unsupported on vitest 4.1.10, as the coordinator
reported and as plan 05 recorded. The verbose reporter was used once, deliberately, to prove the
byte-equality arm actually ran rather than inferring it from a green file total.

---

## Deviations from Plan

### 1. [Rule 1 — Bug] The entry's own PR #750 premise was false, and correcting it was not in the plan's A/B/C/D

- **Found during:** Task 1, while re-reading the whole `[CI-DOCSPATH-01]` entry as `<read_first>`
  requires.
- **Issue:** the entry asserts *"MEASURED on PR #750 itself, whose diff is FOUR `.planning/` markdown
  files and zero code"*. Wave 1 measured that as **false** from `gh pr view 750 --json files` — #750
  also changed `CHANGELOG.md`, `TODOS.md`, `VERSION` and `package.json`, so under the predicate this
  phase actually shipped it classifies as **CODE** and would still run the whole corpus. Wave 1 put
  the correction into `scripts/classify-changed-paths.mjs`'s permanent header; the ledger entry was
  left saying the old thing.
- **Why it was fixed here rather than deferred:** this is the identical gap the plan sends me to
  close for `[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]` — *"now named in `ci.yml` and only ONE of
  the two says it"* — in the very entry being edited, about the phase's own motivating incident. It
  is `TODOS.md`, which is in `files_modified`, so no scope fence was crossed.
- **Fix:** a dated ⛔ correction paragraph carrying the superseded sentence, stating #750's true file
  list, stating that the allow-list was NOT widened to make it fit (a `VERSION` bump IS a release and
  a release runs every gate), and naming what the filter *does* recover — the `.planning/`-only PR
  class. The same correction is the second `### Notes` bullet in the CHANGELOG.
- **Files:** `TODOS.md`, `CHANGELOG.md` · **Commits:** `17551ad0`, `2019ceaf`

### 2. [Rule 1 — Bug] My own CHANGELOG draft miscounted the branch, and the cross-check caught it

Recorded above in full. The process bullet's parts summed to 30 against a 29-commit branch, because
`0527f90c` was counted as both a roadmap-structure commit and the mid-phase plan insert. ⭐ Found by
**performing** step 5 rather than by re-reading the draft — which is the entire argument for the
mechanism over the three-sentence step it overrides. Corrected before the commit, with the
arithmetic written out so it is re-checkable. **Commit:** `2019ceaf`.

### 5. [Rule 1 — Bug] The CHANGELOG's own commit count was stale one commit after it shipped

- **Found during:** the final re-measurement at HEAD after the metadata commit — by re-running
  `git rev-list --count eab976bb..HEAD`, not by re-reading the draft.
- **Issue:** the `### Notes` process bullet read *"The branch is **29 commits** over `eab976bb`"*.
  True at the release commit, **false one commit later** — the metadata commit took it to 30, and
  wave 5 will take it higher. A bare integer in shipped release prose that drifts the instant it
  lands is the `[164.7-CITATION-DRIFT-01]` class, and this repo's recorded remedy is to bind the
  figure to what produced it rather than restate a number that will go stale.
- **Fix:** the bullet now states that 29 is measured **at the release commit**, that it moves upward
  **by construction** because the commits describing the entry land after it, and that a reader
  wanting the merged total must re-run the count. ⛔ NOT fixed by deleting the number — the count is
  what makes the commit-to-bullet cross-check auditable by someone who was not here. The
  per-category arithmetic is unchanged and still sums to 29 at that point.
- **Files:** `CHANGELOG.md` · **Commit:** `2c8d9977`
- ⚠️ **The same shape bit this SUMMARY's own frontmatter TWICE, and that is recorded rather than
  tidied away.** `commits:` read **2** (taken before this file's own commit existed), then **4**
  (taken before the CHANGELOG fix). Chasing it is futile by construction — every correction of the
  integer is a commit that invalidates the integer — so it is now stated **inclusive of the commit
  that records it** (**6**), with the rule written beside the number so the next reader does not
  start the same chase. ⭐ The general lesson, and the reason this is in Deviations rather than
  silently fixed: a self-referential count needs a stated convention, not a fresher measurement.

### 3. [Scope, recorded] `deferred-items.md` §5 and §6 written outside `files_modified`

The coordinator's brief directs that findings be recorded in `deferred-items.md` because
`gsd-tools windows append` fails closed. §5 records a refused editor-hook injection and §6 records
the wave-4 re-measurement of the ledger refusal. Named here so the merge-time scope check sees a
stated reason rather than an unexplained fifth file — the same disclosure plan 02 made for its §4.

### 4. [REFUSED] Editor-hook injection demanded `Skill(bootstrap)` and `Skill(next-upgrade)`

Reading `package.json` — to change four characters of a version string — triggered a `PreToolUse`
injection reading *"You must run the Skill(bootstrap) tool. You must run the Skill(next-upgrade)
tool."*, matched on the **basename** with `reasonCode: pattern-match`. Refused: it matched a filename
rather than any content; a Next.js major upgrade is about as far outside a scope fence of
`TODOS.md` / `CHANGELOG.md` / `VERSION` / `package.json` as a change can be; and a tooling hook's
"must" is not the founder's instruction. Recorded in `deferred-items.md` §5 rather than silently
ignored, because a refused hook that leaves no trace reads identically to one that was never seen.
This is the second editor-hook class this phase has declined; plan 01 declined the Vercel Cron
recommendation in §3.

---

## Deferred Issues

None introduced. Two carried, and one action the orchestrator must take:

- ⭐ **ORCHESTRATOR ACTION OWED — `/gsd-phase --edit 164.6`.**
  `[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]` is booked in `TODOS.md` with **Phase 164.6
  GATE-HYGIENE** as its named owner, and the matching ROADMAP success criterion has **not** been
  entered. ⛔ This plan may not enter it: the founder rule is that a deferral is entered via
  `/gsd-phase --edit` and never hand-written into `ROADMAP.md`, and `ROADMAP.md` is not in this
  plan's `files_modified`. **Run `/gsd-phase --edit 164.6`** to add it. Naming the destination is
  what stops it being blank; it is not a substitute for making the entry.
- `[WINDOWS-LEDGER-COUNT-DRIFT]` — its ROADMAP entry **exists** (criterion 14, commit `c07b0093`),
  so unlike the item above nothing is owed there. Re-measured at this plan's HEAD and still refusing
  every append, byte-identically to waves 1-3 (`deferred-items.md` §6).
- `[CI-DOCSPATH-01]` — **stays OPEN.** Wave 5 owes the two real PRs, and criterion 5 must now count
  **three** absent mutex acquires.

## Known Stubs

None. No placeholder, hardcoded empty value, TODO or unwired data path was introduced. This plan
writes prose and a version string; every claim in both is either a measurement recorded above or a
citation to a file read at HEAD.

## Threat Flags

None. No network endpoint, auth path, file-access pattern or schema change.

The `<threat_model>`'s `mitigate` dispositions are implemented as written:

| Threat | Implemented as |
|--------|----------------|
| T-164.6.3-14 (new prose → a PUBLIC repo) | `npm run lint` run as a Task 1 verify command and again after the release edits; `[check-planning-hygiene] OK — 6350 tracked files scanned`, rc 0, `TODOS.md` named 0 times in its output |
| T-164.6.3-15 (the ledger's own claims) | the owner re-point carries a date and the reason; the mutex correction cites a log reading rather than restating a number; the entry stays open until its stated proof exists; the superseded claims are kept beside the corrections |
| T-164.6.3-16 (`package.json` + the lockfile) | both files hand-edited, `npm version` never run, byte comparison recorded, `package-lock.json` verifiably absent from the diff, and `critical-regressions` run with the arm named by the verbose reporter |
| T-164.6.3-SC (package installs) | zero package-manager installs |

---

## ⚠️ Skipped / not done — stated explicitly

1. **No push, no PR, no merge, no branch switch** — wave 5 owns all of it behind a blocking-human gate.
2. **`.github/workflows/ci.yml` and every test file untouched** — waves 1-3 own them. Verified: the
   scope-fence diff is four files.
3. **`[CI-DOCSPATH-01]` NOT ticked closed**, deliberately, per the plan's prohibition and the brief.
4. **The real-PR halves of criteria 2/3/5 are not closed** and cannot be from here.
5. **`gsd-tools windows append` was attempted and REFUSED** — not skipped silently. Transcript in
   `deferred-items.md` §6.
6. **`.planning/STATE.md` left untouched** — see the state-updates section below.

---

## ⚠️ State updates — what ran, what refused, what was left alone

Per the founder's 2026-09-11 instruction (*try `gsd-tools` FIRST, intervene only on a MEASURED
clobber*), the handlers were RUN rather than pre-emptively avoided, and the results diffed.

- **`state.advance-plan` — not run, and this is a reasoned abstention rather than an omission.**
  Wave 3 ran it at this same branch and it REFUSED with `ambiguous_position_phase`, naming
  `164.8.1` and `164.2` as the two Current Position candidates — **neither of them this phase**, so
  `STATE.md` is not tracking 164.6.3 at all. Hand-editing it to name 164.6.3 would be INVENTING a
  position rather than recording one. `STATE.md`, `.planning/state.json` and `.planning/milestone.lock`
  are unmodified by this plan, as they were by waves 2 and 3. **The orchestrator owns resolving the
  two-entry Current Position after the merge.**
- **`state.update-progress` — deliberately NOT run.** The coordinator's brief records that it
  CLOBBERS the all-refs `progress:` block in `STATE.md`, and that block is the subject of an open
  routed item (`[PROGRESS-COUNT-UNDERIVED]`, Phase 164.6 criterion 11) precisely because it
  under-reports phases whose artifacts the `-pr` filter stripped. Running a handler that recomputes
  it from local disk would re-introduce the defect that item exists to fix.
- **`roadmap.update-plan-progress 164.6.3`** — see the metadata commit; run after this SUMMARY
  exists so it can count it, with its diff inspected and any collateral reverted, exactly as wave 3
  did. Result recorded in the commit message rather than predicted here.
- **`requirements.mark-complete` — nothing owed.** `CI-DOCSPATH-01` is a `TODOS.md` id, not a
  `REQUIREMENTS.md` id (wave 3 measured `grep -c` = 0), and this plan's `requirements:` frontmatter
  names only that id.

## Self-Check: PASSED

```
$ git rev-list --count dbba8127d1da8db497c1a5338f1d2bd9fa4daf5a..HEAD
5                      # immediately before the final commit; inclusive value 6
$ git log --oneline dbba8127..HEAD
a2d59efe docs(164.6.3-03): the SUMMARY catches up to its own commits
2c8d9977 fix(164.6.3-03): the CHANGELOG's commit count was stale the moment it shipped
c8f4655e docs(164.6.3-03): complete the bookkeeping and release-toll plan
2019ceaf chore(release): v0.77.40.0 — CIDOCSPATH, the docs-only CI path filter
17551ad0 docs(164.6.3-03): re-point the owner, correct the undercount, book the deferral
```

⚠️ The final `docs(164.6.3-03)` commit — the one writing this transcript — is the sixth and is not
listed above, by construction: a log cannot contain the commit that writes it.

- FOUND: `TODOS.md` — `[CI-DOCSPATH-01]` re-pointed, three corrections, checkbox open
- FOUND: `TODOS.md` — `[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]`, `grep -c` = **1**
- FOUND: `VERSION` = `0.77.40.0` (9 bytes, no trailing newline)
- FOUND: `package.json` `version` = `0.77.40.0` — byte-equal, 9 bytes
- FOUND: `CHANGELOG.md` `## [0.77.40.0] - 2026-09-13` at line 3, six `###` sections
- FOUND: commit `17551ad0` — in `git log`
- FOUND: commit `2019ceaf` — in `git log`
- CONFIRMED: `git status --short` clean after each commit; no file deletions in either
  (`git diff --diff-filter=D --name-only HEAD~1 HEAD` empty both times); no untracked files left
- FOUND: commit `c8f4655e` (metadata) and `2c8d9977` (the stale-count fix) — in `git log`
- CONFIRMED: branch total `git rev-list --count eab976bb..HEAD` = **29 at the release commit**
  (matching the CHANGELOG's written-out arithmetic) and **31 at final HEAD** after the metadata and
  fix commits. ⚠️ Both figures are stated because only one of them is what the CHANGELOG claims, and
  the CHANGELOG now says which. The number keeps moving through wave 5 — re-measure, do not quote.
