---
phase: 164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a
plan: 04
type: execute
wave: 5
depends_on:
  - 164.6.3-05
  - 164.6.3-01
  - 164.6.3-02
  - 164.6.3-03
files_modified: []
autonomous: false
requirements:
  - CI-DOCSPATH-01
security_asvs_level: 1
security_block_on: high

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ROADMAP criterion 2 is closed on TWO REAL PRs, never on a dry run and never on a workflow-syntax argument: this phase's OWN pull request is the code PR (its diff touches `.github/`, `scripts/`, `src/`, `TODOS.md`, `VERSION`, `package.json` and `CHANGELOG.md`), and a separate `.planning/`-only pull request opened from a main that already carries the filter is the docs-only PR."
    - "ROADMAP criterion 3 is closed by an explicit before/after comparison in which EVERY difference is named, taken by check ROW and not by job key — the baseline is 23 rows because `frontend-test` is a two-shard matrix, and the post-change code board is 24 rows because `changed-paths` is new. A code PR running FEWER rows than the pre-change corpus is a regression and closes nothing."
    - "ROADMAP criterion 4 is closed by TWO independent arguments, both recorded: branch protection and rulesets are re-measured at close and return not-protected and empty, so there is no required check to wedge; and a job skipped by a conditional reports success to a required check whereas a workflow skipped by a paths filter reports nothing, which is why the locked mechanism is the job-level condition."
    - "ROADMAP criterion 5 is closed by an ABSENCE paired with a POSITIVE CONTROL: the code PR's log archive carries THREE distinct advisory-lock acquires — `python`, `e2e-seeded`, `sql-tests` — and the docs-only PR's carries zero. A zero on its own cannot distinguish a lock not taken from a log fetch that failed."
    - "Every census is SHA-BOUND: each run id is recorded beside the head SHA it ran against, because a settled check board can belong to an ancestor."
    - statement: "The `supabase/migrations/**` half of the TODOS proof is addressed by a BYTE ARGUMENT, not by a probe migration PR: a diff of the four `paths:`-filtered workflows against main returns EMPTY. ⚠️ That is a DIFFERENT statement from the one the TODOS entry asks for, not a stronger one, and the SUMMARY must say so. It soundly proves this phase COULD NOT have narrowed those four workflows — GitHub evaluates each workflow's `paths:` independently, so a job-level condition inside `ci.yml` cannot reach them. It does NOT observe `migration-drift-check` firing. The observational half stays UNTESTED here, by the recorded decision below, and is outside this phase's boundary."
      verification: judgment
    - "The residual checks that still run on a docs-only PR are enumerated rather than discovered: `contracts.yml` has no `paths:` and is out of this phase's boundary, and the five always-on jobs run by design."
  prohibitions:
    - statement: "A throwaway probe pull request carrying a file under `supabase/migrations/**` must not be opened in this phase. The basis is SAFETY and nothing else: merging that path auto-applies to shared TEST and then to PROD. ⛔ The byte diff does not REPLACE that evidence — it answers a different question — so the residual it leaves must be named as untested rather than described as covered."
      status: resolved
      verification: judgment
    - statement: "`gh run view --log` must never be used to prove the absence of an advisory-lock acquire — it truncates, and a truncated log makes an absence unfalsifiable. Use the log archive endpoint and stream it."
      status: resolved
      verification: judgment
    - statement: "The docs-only proof PR must not be produced by the `-pr` branch filter, which strips `.planning/` from a PR head by design and would filter the intended diff to empty."
      status: resolved
      verification: judgment
    - statement: "A shorter check board on the CODE PR must never be reported as a success. It is a regression and it closes nothing."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-04-SUMMARY.md"
      provides: "both job censuses with run ids and head SHAs, the named before/after difference table, the three-acquire positive control beside the zero-acquire absence, the re-measured protection and ruleset readings, and the byte argument for the four paths-filtered workflows"
  key_links:
    - from: "this phase's own pull request"
      to: "ROADMAP criterion 2 (code half)"
      via: "gh run view --json jobs,headSha over the PR's ci.yml run, enumerated by check row"
      pattern: "conclusion"
    - from: "a .planning-only pull request opened from post-merge main"
      to: "ROADMAP criteria 1, 4 and 5"
      via: "the same census plus the log-archive acquire count, compared against the code PR's positive control"
      pattern: "61616158"
---

<objective>
Close the four criteria that no local harness can close, on real runs, with SHA-bound evidence.

Purpose: the ROADMAP is explicit that this phase is NOT shippable on the evidence that a docs PR
went fast — it is shippable only on evidence that a CODE PR still runs everything, proven on two
real PRs rather than on a workflow-syntax argument. Plans 01 to 03 built and pinned the
mechanism; scenario S5 and the neuter calibration prove the aggregator's shell behaves under
injected inputs, and RESEARCH states plainly what that cannot prove — that GitHub's scheduler
produces those inputs. This plan is the half that only a real run can supply.

Output: no source file changes. Two job censuses, a named difference table, an absence paired
with its positive control, two re-measured remote readings, and the byte argument that discharges
the migrations half of the TODOS proof — all written into the plan SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-CONTEXT.md
@.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-RESEARCH.md
@.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The CODE-PR census, the positive control, the byte argument, and the two remote re-measurements</name>
  <files>none — this task produces evidence, and its only durable artifact is the plan SUMMARY</files>
  <precondition>This phase's own pull request is OPEN against `main` and its check board has COMPLETED (no queued or in-progress rows): confirm with `gh pr view --json number,state,headRefOid` and `gh run list --branch <head> --workflow ci.yml --limit 1 --json status,conclusion,headSha`. If the PR does not exist yet, HALT — this evidence cannot be manufactured locally and the phase ships through `/gsd-ship`.</precondition>
  <read_first>
    - `.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-RESEARCH.md` — the Evidence Collection section IN FULL. All four recipes there were executed against a real run and their outputs are quoted; use them verbatim rather than re-deriving. Note the three warnings: the repo flag is required when the cwd is not the repo, streaming the archive is required rather than extracting it, and the binary-safe grep flag is mandatory because this repo has a dated record of a grep exit 1 reading as clean.
    - The same file's Before/After Job Census table — it is the measured baseline, bound to its run id, and it is the thing the new census is compared against.
    - `.planning/ROADMAP.md` — this phase's five success criteria, verbatim, so the evidence is written against the words rather than a paraphrase.
    - `.github/workflows/contracts.yml` — its trigger block, to confirm it has no `paths:` and therefore still runs on a docs-only PR.
  </read_first>
  <action>
**A. DECISION, recorded here rather than left to the executor — the migrations half of the TODOS
proof is addressed by a BYTE ARGUMENT, and the probe migration PR is REFUSED.**

The `[CI-DOCSPATH-01]` entry asks the code-PR proof to show `migration-drift-check` executing
alongside the `ci.yml` gates. Three routes existed. The chosen one is the byte argument, and the
SUMMARY must state its SCOPE with the same precision it states its result — this is a phase whose
entire thesis is that an argument is not evidence, so it cannot close out on a claim its own
instrument does not support.

1. **What the byte argument proves, soundly.** This phase's locked mechanism is a job-level
   condition inside `ci.yml`, and GitHub evaluates every workflow's `paths:` filter
   independently — a condition inside `ci.yml` cannot reach `migration-drift-check.yml`. An empty
   diff of those four workflow files against main proves they are byte-identical, so this phase
   PROVABLY COULD NOT have narrowed them. That is a complete answer to "did this change break
   them".
   ⚠️ **What it does NOT prove, and must be named rather than implied:** it does not OBSERVE
   `migration-drift-check` firing. It is a DIFFERENT statement from the observational one the
   TODOS entry asks for, not a stronger one, and it does not on its own discharge the
   observational half of proof (b). ⛔ Do not write "strictly stronger", "subsumes" or any wording
   that implies the observation was made. The correct sentence, and the one to carry into the
   SUMMARY, is: *this phase provably could not have affected those four workflows; whether their
   own `paths:` filters still fire is UNTESTED here and outside this phase's boundary.*
2. **Why the observational half is refused rather than attempted — a safety constraint, not a
   judgment about difficulty.** Merging anything under `supabase/migrations/**` auto-applies to
   shared TEST and then to PROD. Opening such a PR is safe in itself — the apply path is the push
   trigger, not the PR trigger — but an accidentally merged probe migration is a production
   event, and this repository already carries a dated record of a comment-only edit to an applied
   migration being a review blocker. The refusal STANDS; the fix here is to the RECORD, not to
   the decision.

⛔ Therefore: do NOT create a branch carrying a file under `supabase/migrations/**` in this phase.
Record the refusal, its safety basis, AND the named untested residual in the SUMMARY, so it reads
as a bounded decision rather than either an omission or an overclaim.

**B. The code-PR census.** Take the run id for this phase's PR head. Enumerate every check row
with the conclusion-and-name query from the Evidence Collection section, sorted, and record the
head SHA beside it — a census that is not SHA-bound proves nothing, because a settled board can
belong to an ancestor. Then difference the per-job start and completion timestamps to get the
per-row seconds.

Build the before/after table and NAME EVERY DIFFERENCE. Expected shape:
- The baseline is 23 check ROWS, not 21 job keys, because `frontend-test` is a two-shard matrix
  and reports two rows. State that difference explicitly or the comparison will read as a
  regression.
- The post-change code board should be 24 rows: the same 23 plus `changed-paths`.
- ⛔ ANY row present in the baseline and absent here is a REGRESSION. Do not report it as a
  success and do not explain it away. Name it, stop, and surface it.
- A row whose conclusion is `skipped` on the CODE PR is equally a finding: the whole claim of
  criterion 2 is that a code PR still runs every one of them.

**C. The byte argument for the four `paths:`-filtered workflows.** Diff this branch against main
restricted to `migration-drift-check.yml`, `migration-policy.yml`, `sql-function-snapshot.yml`
and `migration-policy-self-test.yml`. The result must be EMPTY. Paste the empty result, then
quote each of those four workflows' `paths:` allow-lists from the file so the argument is
self-contained: they are byte-untouched by this phase, and their `paths:` filters are evaluated
by GitHub independently of anything inside `ci.yml`. ⛔ Stop the claim there. Write the scope
sentence from step A beside it — that whether those filters still FIRE is untested here and
outside this phase's boundary — rather than letting the empty diff imply an observation nobody
made.

**D. The positive control for criterion 5.** Fetch the code PR's run log ARCHIVE — the archive
endpoint, never the truncating log view — stream it rather than extracting it, and count the
advisory-key occurrences with the binary-safe flag. Then extract the distinct acquire lines and
collapse them. ⚠️ Expect THREE distinct acquires, not two: `python`, `e2e-seeded` and `sql-tests`.
⚠️ Each acquire appears TWICE in the archive, once in the per-job file and once in the per-step
file, so count DISTINCT and say so. Record the archive's entry count as a sanity floor — a
truncated archive makes any later zero meaningless. This output is the control that the docs-only
PR's zero will be read against.

**E. Criterion 4, re-measured at close.** Query branch protection on `main` and query rulesets.
Record both responses verbatim with the date. State the second, independent argument beside the
measurement: a job skipped by a conditional reports success to a required check, whereas a
workflow skipped by a paths filter reports nothing and stays pending — which is precisely why the
locked mechanism is the job-level condition and why the rejected workflow-level filter would have
been the wedging one. ⛔ Record the conditional: if protection is ever enabled later,
`changed-paths` must be added to the required list or it becomes an ungated single point of trust.

**F. Enumerate the residuals** that will still run on a docs-only PR, before seeing them, so the
next task's census is a confirmation and not a discovery: the five always-on jobs, the aggregator,
`changed-paths` itself, and every check row contributed by `contracts.yml`, which has no `paths:`
filter and is outside this phase's boundary.
  </action>
  <verify>
    <automated>gh pr view --json number,state,headRefOid</automated>
    <fails_when>non-zero exit, or the state is not OPEN, or the head SHA does not match the SHA the census below is recorded against</fails_when>
    <automated>git diff --stat origin/main...HEAD -- .github/workflows/migration-drift-check.yml .github/workflows/migration-policy.yml .github/workflows/sql-function-snapshot.yml .github/workflows/migration-policy-self-test.yml</automated>
    <fails_when>the command prints ANY line — a non-empty diff means this phase touched one of the four `paths:`-filtered workflows, and the byte argument that addresses the migrations half of the TODOS proof does not hold</fails_when>
    <automated>git log --oneline origin/main..HEAD -- supabase/migrations/</automated>
    <fails_when>the command prints ANY line — this phase must carry no commit touching a migrations path, and a non-empty result means the refused probe route was taken</fails_when>
    <human-check>The founder reads the before/after difference table and confirms every difference between the 23-row baseline and this board is NAMED — not merely that the counts reconcile. A row present in the baseline and absent here, or present here with a skipped conclusion, is a regression that closes nothing, and only a human reading the two lists side by side will catch a difference the row count happens to hide.</human-check>
  </verify>
  <done>The code PR's census is recorded with its run id and head SHA, every difference from the 23-row baseline is named and none is a missing row, the four `paths:`-filtered workflows are proven byte-identical to main WITH the scope sentence beside the result naming their continued firing as untested here and outside this phase's boundary, the three-acquire positive control is captured from the log archive with the entry count beside it, branch protection and rulesets are re-measured with both criterion-4 arguments written out, and the probe-migration refusal is recorded with its safety basis and its named untested residual.</done>
  <reversibility rating="costly">Recorded against the REJECTED alternative, not the chosen one: opening a throwaway pull request carrying a file under `supabase/migrations/**` is reversible only while it stays unmerged. An accidental merge auto-applies to shared TEST and then to PROD and cannot be undone by closing anything, which is why the byte argument was taken instead.</reversibility>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: MERGE GATE — the docs-only half cannot be measured until the filter is on `main`</name>
  <files>none</files>
  <read_first>
    - `.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-04-SUMMARY.md` as far as Task 1 has written it — the code-PR census, the named difference table and the three-acquire positive control are what the founder is being asked to accept before the merge.
  </read_first>
  <action>The founder merges this phase's pull request into `main`. Claude has already produced every piece of evidence that can be produced before the merge; this is the one step it cannot take, and `gate="blocking-human"` is set deliberately so `auto_advance` cannot approve a merge on a human's behalf.</action>
  <instructions>
**Why the pause is structural rather than procedural.** The docs-only proof PR's verdict is
computed as a diff against `main`. Branched from this phase's own branch, its diff would include
the filter's own code and it would correctly classify as CODE — so the measurement is impossible
until the filter is on `main`. There is no ordering in which the docs-only census can be taken
first, and no local harness can substitute for it.

**Already automated by Claude (Task 1), and presented here in this order:**
1. The code PR's board: run id, head SHA, the row count, and the named before/after difference
   table — in particular that no baseline row went missing and that no row is `skipped`.
2. The three-acquire positive control with its archive entry count.
3. The two criterion-4 readings, freshly dated.
4. The predicted residuals for the docs-only PR to come, so the next census confirms a
   prediction rather than reporting a surprise.
5. The probe-migration refusal and its two reasons, so the decision is accepted rather than
   discovered later.

**The ONE thing requiring human action:** merge this phase's PR to `main`, one PR at a time, per
the standing rule. Nothing in this task merges anything on the founder's behalf.
  </instructions>
  <verification>After the merge, Claude confirms the merge commit is on `main` and that `main`'s `ci.yml` contains the `changed-paths` job key — which is also Task 3's precondition, asserted there as a command.</verification>
  <resume-signal>Type "merged" once the PR is merged, or describe what blocked it.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: The docs-only PR — the skipped set, the GREEN aggregator, and the absent mutex read against its control</name>
  <files>none — evidence only; the proof PR's own diff is `.planning/` content produced by the close-out</files>
  <precondition>`main` carries the filter — fetch `origin main`, write `origin/main:.github/workflows/ci.yml` to a `mktemp` file capturing that command's own status, then count `changed-paths` job-key lines in the file and require 1. If it is 0, HALT: the docs-only verdict is computed against `main` and would classify as CODE until then.</precondition>
  <read_first>
    - `.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-RESEARCH.md` — the Evidence Collection section again, for the archive recipe and its three warnings.
    - `.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-04-SUMMARY.md` — Task 1's positive control and residual prediction, which this task is read against.
    - `.planning/ROADMAP.md` — criteria 1 and 5, verbatim.
  </read_first>
  <action>
**A. Build the docs-only PR BY HAND.** Branch from freshly-fetched `origin/main`, make a diff that
is entirely under `.planning/` — the natural content is this phase's close-out: the ROADMAP status
line for Phase 164.6.3 and the phase SUMMARY files. Commit, push, and open the PR with `gh pr
create`.

⛔ Do NOT produce this branch with the `-pr` branch filter. That tooling strips `.planning/` from
a PR head BY DESIGN, which would filter the intended diff to empty and leave nothing to classify.
⛔ Do NOT include `TODOS.md`, `VERSION`, `package.json` or `CHANGELOG.md` — none of them is under
the allow-list and any one of them makes this a CODE PR. Before pushing, run the classifier
locally against this branch with the pull-request event and the base ref set, and confirm it
reports a docs-only verdict; if it does not, the branch is wrong and pushing it wastes a run.

**B. The census.** Once the board completes, enumerate every check row with its conclusion, and
record the run id and the head SHA. Compare against Task 1's residual prediction and name every
difference in BOTH directions:
- Present and running, expected: the five always-on jobs, the aggregator, `changed-paths`, and
  the `contracts.yml` rows.
- Present and `skipped`, expected: the sixteen filtered job keys, which appear in the list with a
  skipped conclusion rather than being absent.
- ⭐ The aggregator's conclusion must be SUCCESS. A RED `frontend` on a docs-only PR is the exact
  failure Pitfall 1 predicts — the strict default arm treating `skipped` as failure — and it means
  the uniform arm did not fire. If that happens, do not retry and do not adjust the arm's
  placement blind: capture the aggregator's own echo line naming the detector value, because the
  empty-string case and the false case are distinguishable there and nowhere else.
- Anything present that the prediction did not name is a finding; write it down rather than
  absorbing it.

**C. Confirm the detector's value was read as expected.** Pull the aggregator's step log and quote
its classification echo line verbatim. This is the direct discharge of the research assumption
about what a `needs:` output holds — it is the only place the real value is observable, and it is
why that echo was added.

**D. The absence, read against the control.** Fetch this run's log archive by the archive
endpoint, stream it, and count advisory-key occurrences with the binary-safe flag. Expect ZERO.
Paste it BESIDE Task 1's non-zero count from the code PR — an absence without its positive
control cannot distinguish a lock not taken from a log fetch that failed. Record the archive's
entry count for this run too, so the zero is known to come from a complete archive.

**E. State the saving honestly.** Compute the docs-only board's job-seconds and set it against the
baseline, then state the limit in the same breath: the `push: branches: [main]` trigger is
unfiltered by a locked decision, so this change still pays the full corpus once on merge and still
takes the advisory lock three times there. The claimable saving is the PR-side run and the
PR-side mutex holds. ⛔ A claim of the whole corpus's job-seconds would be false.

**F. Close the criteria explicitly,** one paragraph each, quoting the ROADMAP wording and naming
the evidence that closes it — and naming, for criterion 2, the one thing none of this proves
locally and which these two runs are what close: that GitHub's scheduler produces the skip set the
contract test injects.
  </action>
  <verify>
    <automated>gh pr view --json number,files --jq '.files[].path'</automated>
    <fails_when>the printed list contains any path that does not begin with `.planning/` — the proof PR would then be a code PR and would classify as such, and the docs-only census would measure the wrong thing</fails_when>
    <automated>git fetch origin main &amp;&amp; CI=$(mktemp) &amp;&amp; git show origin/main:.github/workflows/ci.yml &gt; "$CI" &amp;&amp; grep -c '^  changed-paths:' "$CI"</automated>
    <fails_when>non-zero exit from any stage (each is chained, never piped, so a failed fetch or show cannot be masked by the grep's status), or the printed count is not 1 — `main` does not carry the filter, so any verdict taken now is meaningless</fails_when>
    <automated>node scripts/classify-changed-paths.mjs --self-test</automated>
    <fails_when>non-zero exit, or the terminal PASSED banner is absent — the instrument producing the verdict under test must itself be green when the evidence is taken</fails_when>
    <human-check>The founder reads the five closed-criteria paragraphs against the ROADMAP's own wording and confirms each names evidence rather than an argument — in particular that criterion 2 cites two real runs and that criterion 5's zero is printed beside its non-zero control.</human-check>
  </verify>
  <done>A `.planning/`-only PR opened from post-merge `main` has a completed board in which the sixteen filtered rows are `skipped`, the five always-on jobs plus the aggregator plus `changed-paths` plus the `contracts.yml` rows ran, and the aggregator's conclusion is SUCCESS. Its aggregator echo line is quoted verbatim. Its log archive carries zero advisory-key occurrences beside the code PR's non-zero control, both with archive entry counts. The saving is stated as PR-side only, and all five ROADMAP criteria are closed in writing against their own wording.</done>
  <reversibility rating="reversible">The proof PR is a legitimate `.planning/` close-out and can be merged or closed; either outcome is recorded.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the Actions API → this phase's claims | Every criterion is closed by data fetched from a remote; a truncated or mis-scoped fetch silently weakens the claim it supports |
| a proof branch → `main` | A branch created for evidence sits one click away from the merge button that auto-applies migrations |
| the docs-only proof PR → the filter under test | The proof PR is classified by the very instrument it is meant to test, so its own diff composition is load-bearing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.3-17 | Tampering | a probe branch carrying `supabase/migrations/**` | high | mitigate | The probe route is refused outright; a verify command asserts this branch carries no commit touching a migrations path, so the refusal is enforced rather than intended |
| T-164.6.3-18 | Repudiation | the criterion-5 absence claim | high | mitigate | The archive endpoint is used rather than the truncating log view, the archive is streamed rather than extracted, the binary-safe grep flag is mandatory, the entry count is recorded as a sanity floor, and the zero is only ever reported beside a non-zero positive control from the code PR |
| T-164.6.3-19 | Repudiation | a census bound to the wrong commit | medium | mitigate | Every run id is recorded beside its head SHA and a verify command asserts the PR head matches the SHA the census is recorded against |
| T-164.6.3-20 | Tampering | the docs-only proof PR's diff | medium | mitigate | A verify command asserts every file in the PR is under `.planning/`; the classifier is also run locally against the branch before pushing |
| T-164.6.3-21 | Elevation | `changed-paths` as an unregistered required check | medium | accept | Branch protection and rulesets are measured absent at close, so there is nothing to register. The conditional is recorded: enabling protection later obliges adding `changed-paths` to the required list |
| T-164.6.3-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs in this plan; no `[ASSUMED]` or `[SUS]` package exists, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
- Two run ids recorded, each beside its head SHA.
- The code board carries no missing row against the 23-row baseline and no `skipped` row.
- The docs-only board's aggregator conclusion is success, and its echo line is quoted.
- Three distinct advisory-lock acquires on the code run; zero on the docs-only run; both archive entry counts recorded.
- Branch protection and rulesets re-measured and dated.
- The four `paths:`-filtered workflows diff empty against main, and this branch carries no commit touching a migrations path.
</verification>

<success_criteria>
- ROADMAP criteria 1, 2, 3, 4 and 5 are each closed in writing, quoting the criterion and naming the evidence.
- Every difference between the before and after boards is named, in both directions.
- The saving is stated as PR-side only, with the unfiltered merge push named as the reason.
- The probe-migration refusal is recorded as a decision with its safety basis, and the byte argument is recorded with its SCOPE — what it proves, and the observational half it leaves untested and outside this phase's boundary. ⛔ No sentence in the SUMMARY may describe it as "strictly stronger" or as subsuming the observation.
</success_criteria>

<output>
Create `.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-04-SUMMARY.md` when done. It must carry both censuses with run ids and head SHAs, the named difference table, the aggregator echo line verbatim, the three-acquire control beside the zero-acquire absence with both archive entry counts, the two criterion-4 readings with their date, the empty four-workflow diff WITH its scope sentence naming the untested observational half, and the recorded probe-migration refusal with its safety basis.
</output>
