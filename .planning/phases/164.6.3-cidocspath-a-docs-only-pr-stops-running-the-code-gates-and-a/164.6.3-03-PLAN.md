---
phase: 164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a
plan: 03
type: execute
wave: 3
depends_on:
  - 164.6.3-01
  - 164.6.3-02
files_modified:
  - TODOS.md
  - CHANGELOG.md
  - VERSION
  - package.json
autonomous: true
requirements:
  - CI-DOCSPATH-01
security_asvs_level: 1
security_block_on: high

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The `[CI-DOCSPATH-01]` entry's owner line names THIS phase. It reads as owned by Phase 164.6 GATE-HYGIENE item (12) today, and that ownership moved in the 2026-09-12 split; leaving it would send a future reader to a phase that no longer holds the item."
    - "The same entry's `two shared-TEST-DB mutex holds` claim is corrected to THREE, naming `python` as the third taker, because the ROADMAP, the TODOS entry and CONTEXT.md all undercount and criterion 5's evidence must account for three absences."
    - "The deferred-detection consequence of filtering `frontend-test` is BOOKED as its own TODOS entry with a named owning phase — Phase 164.6 GATE-HYGIENE, whose stated competence is exactly 'when is a gate invoked, and can I prove it still bites'. ⛔ No deferral in this phase is left without a phase, and none is hand-written into ROADMAP.md."
    - "`VERSION` and `package.json` carry the SAME four-digit string, byte-for-byte, one patch step above the value at the start of this phase — the version-gate exists because a phase once shipped with them identical to main."
    - "`CHANGELOG.md` carries ONE unified entry for this phase's version, produced by the commit-checklist mechanism: every commit on the branch enumerated, the full diff read, grouped by theme, and every commit mapped to at least one bullet."
    - "The CHANGELOG entry states the claimable saving honestly — the PR-side run only. The merge push is unfiltered by locked decision and still pays the full corpus and all three mutex holds."
  prohibitions:
    - statement: "The CHANGELOG entry must never claim the whole-corpus job-seconds as saved. The `push: branches: [main]` trigger is unfiltered by a locked decision, so a docs-only change still pays the full corpus once on merge."
      status: resolved
      verification: judgment
    - statement: "`npm version` must never be run — it rewrites the lockfile and can normalise the four-digit string to three, which reddens the byte-equality assertion."
      status: resolved
      verification: test
    - statement: "A TODOS entry must never be closed on the strength of shipped code alone when its own stated proof is a measurement; `[CI-DOCSPATH-01]` stays OPEN until plan 04's two real-PR censuses exist."
      status: resolved
      verification: judgment
    - statement: "This phase's entries must never be hand-written into ROADMAP.md. The roadmap is mutated by the roadmap subcommands or by `/gsd-phase --edit`; a hand edit injects collateral into unrelated phase sections."
      status: resolved
      verification: judgment
  artifacts:
    - path: "TODOS.md"
      provides: "the re-pointed owner line on [CI-DOCSPATH-01], its corrected three-mutex claim, and the new deferred-detection entry naming Phase 164.6 as owner"
      contains: "CI-DOCSPATH-01"
    - path: "CHANGELOG.md"
      provides: "one unified versioned entry using this repo's section vocabulary"
    - path: "VERSION"
      provides: "the bumped four-digit string"
    - path: "package.json"
      provides: "the byte-equal version field"
  key_links:
    - from: "VERSION"
      to: "package.json"
      via: "byte-equal four-digit strings, asserted by src/__tests__/critical-regressions.test.ts"
      pattern: "[0-9]+\\.[0-9]+\\.[0-9]+\\.[0-9]+"
    - from: "CHANGELOG.md"
      to: "the branch's commit list"
      via: "every commit from `git log <base>..HEAD --oneline` maps to at least one bullet; an unrepresented commit is a defect, not a minor omission"
      pattern: "## \\[[0-9]"
---

<objective>
Make the ledger agree with what shipped, and pay the release toll this PR owes.

Purpose: two bookkeeping facts are wrong on disk right now and both mislead a future reader. The
`[CI-DOCSPATH-01]` entry still routes its owner to Phase 164.6 item (12), an ownership the
2026-09-12 split moved to this phase, and the same entry undercounts the shared-TEST mutex at
two when the measurement is three. Separately, this PR touches non-planning paths — `.github/`,
`scripts/`, `src/`, and `TODOS.md` itself — so `version-gate` will fail it without a bump, and
the founder's CHANGELOG instruction has a mechanism that must be run rather than a step that can
be skipped.

Output: a corrected and re-pointed TODOS entry, a newly booked deferral with a named owning
phase, a byte-equal version bump, and one unified CHANGELOG entry cross-checked against every
commit on the branch.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-CONTEXT.md
@.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point the owner, correct the undercount, and book the deferral to a named phase</name>
  <files>TODOS.md</files>
  <read_first>
    - `TODOS.md` — the whole `[CI-DOCSPATH-01]` entry. Locate it by symbol rather than by line number, because this file's line numbers drift and this repo has a dated record of that class: `grep -n 'CI-DOCSPATH-01' TODOS.md`. Read the entry end to end including the `OWNER` line and the `Source:` line.
    - `TODOS.md` — one neighbouring recently-added entry, to copy the entry shape exactly: the bolded bracketed id and title line, the measured body, the bolded `OWNER` line, and the `Source:` line.
    - `.planning/ROADMAP.md` — the `### Phase 164.6: GATE-HYGIENE` entry (`grep -n '### Phase 164.6:' .planning/ROADMAP.md`). Read enough of it to confirm it is the right owner for a "when is a gate invoked, and can I prove it still bites" item and that it is still unplanned.
    - `src/__tests__/lint-sql-gates.test.ts` and `src/__tests__/verify-plan-anchors.test.ts` — the two assertions whose subject is real `.planning/**` content, so the new entry names them precisely rather than gesturing at them.
    - `CLAUDE.md` — the GSD orchestration rules section, for the standing instruction that the roadmap is not hand-edited.
  </read_first>
  <action>
**A. Re-point the owner of `[CI-DOCSPATH-01]`.** Replace the `OWNER` line so it names Phase
164.6.3 CIDOCSPATH and its own success criteria, and add one dated sentence recording WHY it
moved — the 2026-09-12 founder split took items (9), (11) and (12) out of Phase 164.6 into three
phases of their own and their success criteria moved with them, so the old pointer now sends a
reader to a phase that does not hold the item. Keep the original routing sentence as lineage
rather than deleting it; this file's convention is to carry the superseded claim beside the
correction, not to erase it.

⛔ Leave the entry's checkbox OPEN. Its own stated proof is two real pushes, and those are plan
04's work. Add one line saying what is shipped and what is still owed, so the entry is not read
as untouched.

**B. Correct the mutex undercount in the same entry.** The body says two shared-TEST-DB mutex
holds. It is THREE: `python` takes the advisory key as well as `e2e-seeded` and `sql-tests`, and
`python`'s own job header states it in its timeout derivation. Correct the number where it
appears in the entry, name the third taker, and add the measurement that settles it — the log
archive for the baseline run carries three distinct acquire durations, not two. Add one sentence
stating that CONTEXT.md and the ROADMAP entry carry the same undercount and that this correction
is the one to cite.

**C. Book the deferred-detection item as its own entry.** Add a new `TODOS.md` entry with a
bracketed id in this file's style — `[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]` — stating
the measured fact: two assertions hosted by `frontend-test` read real `.planning/**` content, one
reading `REQUIREMENTS.md` and `ROADMAP.md` and one reading a real phase VERIFICATION file, and
`frontend-test` is now filtered on a docs-only PR. Consequence: a docs-only PR that reintroduces
the forbidden sentence, or deletes that VERIFICATION file, is not caught until the next code PR
or the unfiltered merge push. Severity: deferred detection, never lost detection — say both
halves. State the two refused remedies and why: widening the always-on set to include
`frontend-test` gives back most of the saving, and moving the assertions changes which gates
exist, which the phase boundary forbids.

⭐ **`OWNER: Phase 164.6 GATE-HYGIENE.`** That phase's stated competence is exactly "when is a
gate invoked, and can I prove it still bites", it is the phase this one was split out of, and it
is still unplanned — which is the preferred destination shape. ⛔ Do NOT leave the destination
blank and ⛔ do NOT hand-write a ROADMAP success criterion for it: record in this plan's SUMMARY
that the orchestrator should enter the matching criterion on Phase 164.6 via `/gsd-phase --edit`,
because hand-editing that file injects collateral into unrelated phase sections.

**D.** Re-read both edited regions after writing and confirm no adjacent entry was disturbed —
`git diff TODOS.md` must show only the two entries you touched.
  </action>
  <verify>
    <automated>grep -c "164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION" TODOS.md</automated>
    <fails_when>the printed count is 0 — the deferral was not booked — or is greater than 1, which means the entry was duplicated rather than added once</fails_when>
    <automated>git diff --numstat TODOS.md</automated>
    <fails_when>no line is printed (nothing was edited), or the deleted-line count exceeds the added-line count, which would mean an adjacent entry was removed rather than an owner line rewritten</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or `check-planning-hygiene.ts` names `TODOS.md` — an absolute home path or the macOS username reached a tracked file on a public repo</fails_when>
  </verify>
  <done>`[CI-DOCSPATH-01]`'s owner line names Phase 164.6.3 with the dated reason for the move, its mutex count reads three with `python` named, its checkbox is still open with what-is-owed stated, and `[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]` exists with Phase 164.6 GATE-HYGIENE as its named owner.</done>
  <reversibility rating="reversible">Ledger prose in one file.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The release toll — a byte-equal four-digit bump and ONE unified CHANGELOG entry, cross-checked commit by commit</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <read_first>
    - The plan-01 and plan-02 SUMMARYs in this phase directory, if they are on disk (deliberately NOT `@` context references — a forward reference to a file that does not exist yet is read as nothing and is a stale claim the anchor gate reddens on). ⚠️ They are context for the entry, never the source of it: step 2 of the mechanism below requires the full diff, because what a commit says and what it changed diverge.
    - `CLAUDE.md` — the CHANGELOG discipline section IN FULL. It is a founder instruction with a mechanism and it overrides the three-sentence CHANGELOG step in the global ship workflow. Read the five numbered mechanism steps and the section-vocabulary paragraph; note that this repo deliberately does NOT adopt the product-changelog voice and keeps phase ids, TODOS ids and routed deferrals in its entries.
    - `CHANGELOG.md` — the most recent two entries, for the heading form and the section vocabulary actually in use.
    - `VERSION` and the `version` field in `package.json` — read both and confirm they are byte-equal four-digit strings before touching either.
    - `src/__tests__/critical-regressions.test.ts` — the assertion that pins those two to each other (`grep -n 'VERSION' src/__tests__/critical-regressions.test.ts | head`).
    - `scripts/check-version-bump.mjs` — what the gate actually requires of this PR: a changed non-planning path obliges a bumped VERSION, a matching package version, and a CHANGELOG entry for it.
  </read_first>
  <action>
**A. Bump, by hand.** Decide the four-digit value yourself: this is a shipped capability change to
CI invocation, so take the patch position one step above the current value and keep the fourth
digit's existing convention. Write the identical string to `VERSION` and to `package.json`'s
`version` field. ⛔ Never run `npm version` — it rewrites the lockfile and can normalise a
four-digit string to three, which reddens the byte-equality assertion. After writing, compare the
two strings directly and record the comparison in the SUMMARY.

**B. Run the CHANGELOG mechanism in its stated order — it is a checklist, not background reading.**

1. `git log <base>..HEAD --oneline` where `<base>` is the merge base with `main`. Enumerate EVERY
   commit and COUNT them. This list is the checklist.
2. `git diff <base>...HEAD` and read the full diff. What a commit message says and what it
   changed diverge, and this repo has a dated record of that class.
3. Group by theme BEFORE writing a line: new capability, behaviour change, bug fix, removal,
   infrastructure/tooling/tests, refactor.
4. Write ONE unified entry under a `## [X.Y.Z.B] - YYYY-MM-DD` heading, newest first, using this
   repo's own section vocabulary rather than the four keep-a-changelog sections. For this phase
   the fitting sections are `### Added` (the classifier, the detector job, the contract guard),
   `### Changed` (when the sixteen jobs are invoked; the aggregator's single uniform arm),
   `### Tests` (the eight executed scenarios, the exact-set pins, the neuter calibration) and
   `### Notes` (the known limits). If entries for earlier commits on this branch already exist,
   REPLACE them with the unified one.
5. CROSS-CHECK and treat a miss as a defect: every commit from step 1 must map to at least one
   bullet. Record the commit count and the theme count in the SUMMARY so the check is auditable.

**C. The `### Notes` section must carry, explicitly:**
- The claimable saving is the PR-side run ONLY. The `push: branches: [main]` trigger is unfiltered
  by a locked decision — `ci.yml`'s own header records that every commit must produce its own
  recorded green run and Railway waits on main CI — so a docs-only change still pays the full
  corpus once, on merge, and still takes the shared-TEST advisory lock there. ⛔ A claim of the
  whole corpus's job-seconds would be false.
- THREE jobs take that lock per run, not two: `python`, `e2e-seeded` and `sql-tests`. Name the
  correction, because the ROADMAP and the TODOS entry both undercount.
- The deferred-detection limit and its booked id with the owning phase.
- `contracts.yml` still runs on a docs-only PR and is out of scope by the phase boundary, which
  names `ci.yml` as the only file that must change — so a residual check board on a docs-only PR
  is expected and is not a filter failure.
- The always-on set is FIVE, and `frontend-lint` is on it because `npm run lint` also runs the
  planning-hygiene leak gate whose subject is exactly a `.planning/`-only agent-written diff.
  ⭐ Name the SECOND, shell-level layer too: `frontend-lint` and `plan-anchor-verify` — the two
  always-on keys that are also aggregator rows — are both members of the aggregator's `ALWAYS_ON`
  list, so a `skipped` result for either under a docs-only classification still reddens the board.
  That layer is defence in depth against a future edit, not a live path today, and saying so is
  what stops a later reader deleting it as dead.

**D.** Confirm the gate this entry exists to satisfy would pass: run the version gate's own
self-test and then the gate itself, and paste both outputs into the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/check-version-bump.mjs --self-test</automated>
    <fails_when>non-zero exit, or the terminal self-test banner is absent from stdout</fails_when>
    <automated>node scripts/check-version-bump.mjs</automated>
    <fails_when>non-zero exit, or stdout carries any `::error::` line naming a version, package-mismatch or changelog defect</fails_when>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts</automated>
    <fails_when>non-zero exit, or the reporter names the VERSION / package.json byte-equality assertion among the failures</fails_when>
  </verify>
  <done>`VERSION` and `package.json` carry the same bumped four-digit string; `CHANGELOG.md` carries one unified entry for it in this repo's section vocabulary; every commit on the branch maps to at least one bullet and the counts are recorded; the `### Notes` section carries the PR-side-only saving, the three-mutex correction, the booked deferral, the `contracts.yml` residual and the five-member always-on set.</done>
  <reversibility rating="reversible">A version string and a changelog entry; both revert cleanly.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the working tree → a PUBLIC repository | `.planning/` is tracked and the repo is public, so every byte written here is world-readable |
| the ledger (`TODOS.md`, `CHANGELOG.md`) → a future reader | A wrong owner or a wrong measurement in these files is acted on later as if it were a fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.3-14 | Information Disclosure | new prose in `TODOS.md` and `CHANGELOG.md` | high | mitigate | `npm run lint` runs `check-planning-hygiene.ts` over every tracked file with no path allowlist; it is a verify command on Task 1 precisely because this task writes new prose to a public repo |
| T-164.6.3-15 | Repudiation | the ledger's own claims | medium | mitigate | The owner re-point carries a date and the reason for the move; the mutex correction cites the measurement rather than restating a number; the entry stays open until its stated proof exists |
| T-164.6.3-16 | Tampering | `package.json` and the lockfile | medium | mitigate | The version field is edited by hand; `npm version` is prohibited because it rewrites the lockfile and can normalise the four-digit string, and the byte-equality assertion is a verify command |
| T-164.6.3-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs in this plan; the lockfile is deliberately not touched and no `[ASSUMED]` or `[SUS]` package exists, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
- `node scripts/check-version-bump.mjs --self-test` and `node scripts/check-version-bump.mjs` both exit 0.
- `npx vitest run src/__tests__/critical-regressions.test.ts` exits 0.
- `npm run lint` exits 0.
- `git diff --stat` shows exactly `TODOS.md`, `CHANGELOG.md`, `VERSION` and `package.json`; the lockfile is NOT in the diff.
</verification>

<success_criteria>
- The `[CI-DOCSPATH-01]` owner names this phase, with the dated reason for the move.
- The mutex count reads three, with the third taker named and the measurement cited.
- The deferred-detection item is booked with Phase 164.6 GATE-HYGIENE as its named owner.
- `VERSION` and `package.json` are byte-equal.
- One unified CHANGELOG entry exists and every commit on the branch maps to at least one bullet.
</success_criteria>

<output>
Create `.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-03-SUMMARY.md` when done. It must carry the commit count from step 1 of the CHANGELOG mechanism, the theme grouping, the explicit commit-to-bullet cross-check, the byte comparison of `VERSION` and `package.json`, and the line reminding the orchestrator to enter the deferral's criterion on Phase 164.6 via `/gsd-phase --edit`.
</output>
