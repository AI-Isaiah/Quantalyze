---
phase: 164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a
plan: 02
type: execute
wave: 2
depends_on:
  - 164.6.3-01
files_modified:
  - .github/workflows/ci.yml
  - src/__tests__/contracts/ci-docs-path-filter.contract.test.ts
autonomous: true
requirements:
  - CI-DOCSPATH-01
security_asvs_level: 1
security_block_on: high

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Exactly SIXTEEN job keys carry the docs-only conjunct — `deps-cache`, `frontend-typecheck`, `frontend-test`, `frontend-coverage`, `frontend-seam-redis`, `frontend-local-stack`, `frontend-policy`, `knip`, `frontend-build`, `sql-gate-lint`, `sql-mutation`, `python`, `e2e`, `sql-tests`, `e2e-seeded`, `lighthouse-mobile` — and every one of them also carries `needs: [changed-paths]`, including the five that would inherit the skip anyway, so the run log shows each job's own classification rather than a chain of consequences."
    - "Exactly FIVE job keys are always-on and carry NEITHER `needs: changed-paths` NOR a docs-only conjunct: `plan-anchor-verify`, `secret-scan`, `version-gate`, `docs-link-check` and `frontend-lint`. The aggregator and the detector itself are the two remaining keys and are classified explicitly rather than left over."
    - "The filtered set and the always-on set are asserted as EXACT SETS in BOTH directions against the job keys re-derived from `ci.yml` text — a job added to the file and not classified reddens by name, and a job removed from either set reddens by name."
    - "Every added conjunct is on ONE physical line and uses the not-equals-true form; the contract test asserts the equals-false form appears in no filtered job's condition, because an empty detector output would make that form skip the gate."
    - "ORDERING EDGE, aggregator half: the loop's verdict does not depend on the order of the rows — only on the uniform arm being the FIRST branch of the chain. The contract test pins the placement structurally and proves the consequence by executing a fork-PR docs-only scenario in which the two pre-existing tolerance arms would otherwise have set the failure flag first."
    - "The pre-existing `e2e-seeded` arm still bites: a same-repo code PR with that row skipped still exits 1, so this phase demonstrably did not weaken it."
    - "ANTI-VACUITY, OBSERVED: neutering the classifier's allow-list makes its `--self-test` exit non-zero, and the neuter is proven to have APPLIED before the red is believed. The mutation is applied to a tempdir copy and never to the file on disk."
    - "The anti-vacuity observation is DURABLE, not a one-time ceremony: it is encoded as a calibration arm inside the contract test that rebuilds the mutated copy every run."
    - "The deferred-detection consequence is named in `ci.yml` itself: filtering `frontend-test` defers — never loses — the two assertions whose subject is real `.planning/**` content, because the unfiltered merge push and the next code PR both run them."
  prohibitions:
    - statement: "The `push: branches: [main]` trigger must never be filtered. `ci.yml`'s header records that every commit must produce its own recorded green run, and Railway waits on main CI and skips the analytics deploy when it is red."
      status: resolved
      verification: test
    - statement: "No job may be moved between the filtered and always-on sets without the exact-set assertion being updated in the same commit — a silent move is the gate-silently-not-running shape this milestone exists to remove."
      status: resolved
      verification: test
    - statement: "The uniform arm must never be widened past the exactly-`skipped` result; a `failure` or `cancelled` row must keep falling through to its pre-existing arm."
      status: resolved
      verification: test
    - statement: "A red gate under this filter must never be cleared by widening the allow-list or by adding a row to ALWAYS_ON as a workaround."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "the docs-only conjunct and needs: edge on all sixteen filtered job keys; the three prose records (needs-edge carve-out, deferred-detection consequence, three-mutex correction)"
      contains: "needs.changed-paths.outputs.docs_only"
    - path: "src/__tests__/contracts/ci-docs-path-filter.contract.test.ts"
      provides: "scenarios S3, S7, S8; the exact-set structural pins in both directions; the one-physical-line and not-equals-true pins; the no-if-on-changed-paths pin; the vacuity fences; the neuter calibration arm"
  key_links:
    - from: ".github/workflows/ci.yml (job keys)"
      to: "src/__tests__/contracts/ci-docs-path-filter.contract.test.ts (structural pins)"
      via: "the test re-derives the job-key list from ci.yml text independently rather than importing any list, so agreement is evidence and not construction"
      pattern: "^  [a-z0-9-]+:$"
    - from: "scripts/classify-changed-paths.mjs"
      to: "src/__tests__/contracts/ci-docs-path-filter.contract.test.ts (neuter arm)"
      via: "the arm copies the script into a tempdir, rewrites the allow-list literal there, asserts the rewrite applied, then spawns the copy's --self-test and requires a non-zero exit"
      pattern: "DOCS_ONLY_PREFIXES"
---

<objective>
Expand the proven slice to the whole filtered set, pin the partition as an exact set in both
directions, and close the anti-vacuity loop with an observation rather than an assertion.

Purpose: plan 01 proved the architecture on one row. The remaining risk is not architectural, it
is bookkeeping — a job that belongs in neither set, a conjunct that fails open, an arm that was
never shown able to fire. Each of those is the gate-silently-not-running shape this milestone is
named for, so each gets a structural pin rather than a comment. The deferred-detection
consequence of filtering `frontend-test` is written into `ci.yml` here rather than discovered
later; naming a known limit is this repo's house style, discovering it later is the defect.

Output: sixteen one-line conjuncts and their `needs:` edges, three prose records in `ci.yml`, the
remaining three execution scenarios, the exact-set and form pins, and the durable neuter
calibration with its observed transcript.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-CONTEXT.md
@.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-RESEARCH.md
@.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-PATTERNS.md
@src/__tests__/mutation-runner-floors.test.ts
@src/__tests__/supabase-migrate-test-first.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: The remaining fifteen job keys, and the three things `ci.yml` must say in prose</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - The plan-01 SUMMARY in this phase directory, if it is on disk (it is deliberately NOT an `@` context reference, because a forward reference to a file that does not exist yet is read as nothing and is a stale claim the anchor gate reddens on). It carries the self-test stdout, the vitest summary and the registry row counts this plan builds on.
    - `.github/workflows/ci.yml` — re-derive the job-key list at HEAD rather than trusting any document: `grep -n '^  [a-z0-9-]*:$' .github/workflows/ci.yml`. Twenty-two keys are expected before this phase's addition; reconcile the list you get against the sixteen-plus-five-plus-aggregator partition below and STOP if it does not reconcile exactly.
    - `.github/workflows/ci.yml` — the three job-level `if:` lines that already exist on filtered jobs: `sql-tests` (a parenthesised fork/push disjunction conjoined with a repository-variable check) and `e2e-seeded` (the same shape wrapped in a `${{ }}` expression). Read both in full before conjoining.
    - `src/__tests__/critical-regressions.test.ts` — the single-line regex that matches `e2e-seeded`'s `if:` (`grep -n 'E2E_TEST_DB_CONFIGURED' src/__tests__/critical-regressions.test.ts`). ⛔ Folding that condition onto multiple lines reddens that test with a message about a lost job-level gate, which reads as a security regression and is not one.
    - `.github/workflows/ci.yml` — the `knip` header (it states it is intentionally NOT in the aggregator's `needs:`), the `python` header's advisory-lock derivation paragraph (it states the lock is taken THREE times per run and names the three takers), and the `deps-cache` header sentence stating that no downstream job `needs:` it.
    - `src/__tests__/lint-sql-gates.test.ts` and `src/__tests__/verify-plan-anchors.test.ts` — read only the two assertions whose subject is real `.planning/**` content (`grep -n '\.planning/' src/__tests__/lint-sql-gates.test.ts src/__tests__/verify-plan-anchors.test.ts`). You are writing a `ci.yml` comment about exactly these two.
  </read_first>
  <action>
**A. Fifteen conjuncts, each on ONE physical line.**

`sql-gate-lint` already carries its conjunct from plan 01. Add `needs: [changed-paths]` and the
docs-only conjunct to the other fifteen filtered keys: `deps-cache`, `frontend-typecheck`,
`frontend-test`, `frontend-coverage`, `frontend-seam-redis`, `frontend-local-stack`,
`frontend-policy`, `knip`, `frontend-build`, `sql-mutation`, `python`, `e2e`, `sql-tests`,
`e2e-seeded`, `lighthouse-mobile`.

Three shapes, and which one applies is decided by what the job already has:
- A job with NO `if:` today gains a bare job-level `if:` on the not-equals-true form alone.
- A job that already has an unwrapped `if:` (`sql-tests`) keeps it unwrapped: put the docs-only
  conjunct FIRST and conjoin the existing condition behind it, on ONE physical line.
- A job that already has a `${{ }}`-wrapped `if:` (`e2e-seeded`) keeps the wrapper: conjoin
  INSIDE it, docs-only conjunct first, on ONE physical line.

A job that already has a `needs:` list (`frontend-coverage`, `sql-tests`, `e2e`, `e2e-seeded`,
`lighthouse-mobile`) gains `changed-paths` as an additional entry in its existing list rather
than a second `needs:` key. ⭐ Give these five the explicit conjunct even though they would
inherit the skip from their parents: the aggregator's `sql-tests` arm already warns that the
loop cannot tell two skip causes apart, and relying on an inherited skip would make a third
indistinguishable cause — and CONTEXT.md requires the classification to be readable in the run
log per job.

⚠️ Use the not-equals-true form everywhere and never the equals-false form; an empty detector
output must run the gate, not skip it. Every one of the fifteen gets a one- or two-line comment
above it naming the phase and `CI-DOCSPATH-01` and stating that this changes WHEN the job is
invoked and nothing about what it asserts.

⛔ Do NOT add the conjunct or the `needs:` edge to `plan-anchor-verify`, `secret-scan`,
`version-gate`, `docs-link-check` or `frontend-lint`. ⛔ Do NOT touch the `push:` trigger.
⛔ Do NOT add a `paths:` or `paths-ignore:` key anywhere in the file.

**B. Three prose records, each where a reader will hit it.**

(i) On `frontend-lint`: a comment stating it is deliberately NOT filtered, because `npm run lint`
also runs `check-planning-hygiene.ts` — a gate whose stated subject is agents writing absolute
worktree and scratchpad paths into new planning artifacts on a PUBLIC repo with `.planning/`
tracked, carrying no path allowlist by construction. A `.planning/`-only PR is exactly the PR
class that gate exists to catch. Give the measured cost (70 s) so the trade is legible.

(ii) On `frontend-test`: the deferred-detection record. Two assertions hosted by this job read
real `.planning/**` content — one in `lint-sql-gates.test.ts` that reads `REQUIREMENTS.md` and
`ROADMAP.md`, one in `verify-plan-anchors.test.ts` that reads a real phase VERIFICATION file. On
a docs-only PR they no longer run, so their detection is DEFERRED to the unfiltered merge push
and to the next code PR — deferred, not lost. State the accepted consequence, state that widening
the always-on set to include `frontend-test` is refused because that job is most of the saving,
and name the booked item and its owning phase so the record and the backlog agree.

(iii) On `python`: correct the mutex undercount in a comment. THREE jobs take the shared-TEST
advisory lock per run, not two — `python`, `e2e-seeded` and `sql-tests`, as `python`'s own
header derivation already states; the ROADMAP, the TODOS entry and CONTEXT.md all name only two,
and this phase's criterion-5 evidence must account for three absences.

**C.** Re-run the job-key derivation and assert the partition closes: sixteen filtered, five
always-on, plus `frontend` and `changed-paths` classified explicitly. Paste the derived list into
the SUMMARY. If a key falls outside all four buckets, STOP and surface it rather than guessing.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts</automated>
    <fails_when>non-zero exit, or the reporter names the `e2e-seeded` job-level gate assertion or any `persist-credentials` assertion among the failures</fails_when>
    <automated>grep -v '^ *#' .github/workflows/ci.yml | grep -c "needs.changed-paths.outputs.docs_only"</automated>
    <fails_when>the printed count is not exactly 17 — sixteen job conditions plus the single aggregator hoist. Comment lines are stripped first so header prose cannot inflate the tally</fails_when>
    <automated>npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts</automated>
    <fails_when>non-zero exit, or the reporter reports 0 test files, or the extractor throws with the missing-step-name message (which means the aggregator step was renamed or its body reflowed)</fails_when>
  </verify>
  <done>Sixteen job keys carry the conjunct and the `needs:` edge, five carry neither, every added condition is one physical line in the not-equals-true form, the `push:` trigger is untouched, and `ci.yml` carries the three prose records.</done>
  <reversibility rating="reversible">Sixteen additive conditions in one file; reverting restores the pre-change invocation set byte-for-byte.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The rest of the scenario matrix, and the exact-set pins that make a silent reclassification impossible</name>
  <files>src/__tests__/contracts/ci-docs-path-filter.contract.test.ts</files>
  <read_first>
    - `src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` — the file as plan 01 left it: the extractor, the substitution helper and its applied-assertion, the tempdir harness, and S1, S2, S4, S5, S6.
    - `src/__tests__/mutation-runner-floors.test.ts` — read the header (the two-floors-two-layers statement and the stale-direction message that names its own fix), the independent-re-derivation comment explaining why the parser's own constants are deliberately NOT imported, and the vacuity fences placed before each derived assertion. All three idioms are used here.
    - `src/__tests__/supabase-migrate-test-first.test.ts` — the line-exact workflow-pinning block and its CALIBRATION arm.
    - `.github/workflows/ci.yml` — the aggregator's `needs:` list as it now stands, and the two pre-existing tolerance arms' fork-PR computation, so the S7 and S8 scenarios inject the right event shape.
  </read_first>
  <action>
**A. Three more execution scenarios.**

- **S3** — `docs_only` is `true`, the filterable rows are `skipped`, but `sql-mutation` is
  `failure`. Expect exit 1. This pins that the tolerance is scoped to exactly the `skipped`
  result and never to a failure, which is one of the three conjuncts that keep the arm from
  greening a code PR.
- **S7** — `docs_only` is `true` on a FORK pull request: `e2e-seeded` and `sql-tests` are
  `skipped` along with the rest of the filterable rows, `plan-anchor-verify` is `success`.
  Expect exit 0. This proves the uniform arm and the two pre-existing tolerance arms COMPOSE
  rather than collide, and it is the scenario that catches the arm having been appended at the
  bottom of the chain instead of placed at the top.
- **S8** — `docs_only` is `false` on a SAME-REPO pull request with `e2e-seeded` `skipped`.
  Expect exit 1, and assert the output carries the pre-existing `e2e-seeded` error text. ⛔ Label
  this CALIBRATION in the file's own vocabulary: it is the proof that this phase did not weaken
  the arm it had to reach past, and it is the only scenario that would notice if it had.

**B. Structural pins, every population independently re-derived from `ci.yml` text.**

⭐ Derive the job-key list inside the test by scanning the workflow text, and derive the filtered
set by scanning for the conjunct. ⛔ Do NOT import a list from the classifier or from any
constant — the re-derivation is what makes agreement evidence rather than construction, exactly
as `mutation-runner-floors.test.ts` states for its own regexes.

Pin, each failing BY NAME and in BOTH directions:
- filtered ∪ always-on ∪ {`frontend`, `changed-paths`} equals the full derived job-key set, with
  a message naming any key that belongs to none of them and telling the reader to classify it.
- the filtered set equals a hand-typed roster of the sixteen; a key that gained or lost the
  conjunct reddens naming itself.
- the always-on roster of five each carry NO `needs: changed-paths` and NO docs-only conjunct.
- `changed-paths` has NO job-level `if:` line at all, with a message repeating why: a conditional
  detector silently skips every dependent.
- every filtered job's condition is a SINGLE physical line containing the not-equals-true form,
  and the equals-false form is absent from all of them, with a message naming the fail-open
  consequence.
- the aggregator's `needs:` list contains `changed-paths`.
- the uniform arm's branch appears BEFORE the first per-row branch in the loop body — assert on
  relative position within the extracted script, so a future edit that moves it to the bottom
  reddens even though every string is still present.
- the `push:` trigger block carries no `paths:` or `paths-ignore:` key, and neither does the
  workflow's `pull_request:` trigger — the mechanism is job-level and must stay job-level.

**C. Vacuity fences before every derived assertion,** in the sibling file's exact form: assert
the derived job-key set is non-empty and that the derived filtered set is non-empty, each with a
message saying the assertions below would otherwise be vacuous. A set-equality assertion over two
empty sets passes and proves nothing; that is the defect class this whole file exists inside.

**D.** Run the file and paste the per-scenario reporter lines into the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts</automated>
    <fails_when>non-zero exit, or fewer than 8 scenario rows appear in the reporter output, or any structural pin reports an unclassified job key</fails_when>
    <automated>npx vitest run src/__tests__/contracts/ src/__tests__/critical-regressions.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports 0 test files</fails_when>
  </verify>
  <done>S1 through S8 all present and passing; the filtered and always-on sets are pinned as exact sets in both directions against a population re-derived from `ci.yml`; the arm's FIRST placement is pinned by relative position; every derived assertion sits behind a vacuity fence.</done>
  <reversibility rating="reversible">Test-only additions.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Neuter the detector, OBSERVE the red, restore — and leave the observation running on every CI run</name>
  <files>src/__tests__/contracts/ci-docs-path-filter.contract.test.ts</files>
  <read_first>
    - `src/__tests__/mutation-runner-floors.test.ts` and `scripts/mutation-runner/run.mjs` — read how a mutated subject is exercised without ever touching the real tree: a throwaway corpus built with `mkdtempSync` and `writeFileSync`, and the mutation asserted to have landed before the flip is believed.
    - `src/__tests__/supabase-migrate-test-first.test.ts` — the calibration comment stating that the mutation is asserted APPLIED before the flip is asserted, because a subject that still held the anchor would make the assertion vacuous.
    - `scripts/classify-changed-paths.mjs` — the exact text of the allow-list declaration, which is the mutation target.
    - `src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` — the tempdir harness you are extending.
  </read_first>
  <action>
⭐ **The direction matters and it is the reason this task exists.** Neutering the detector
produces a GREEN, SHORT board, not a red one. A control that waits for red to appear on its own
will never fire here, so the red has to be manufactured and then OBSERVED.

**A. The durable calibration arm, inside the contract test.**

Add an arm named as a CALIBRATION in the file's own vocabulary. It must:

1. Read `scripts/classify-changed-paths.mjs` into memory and copy it into the module's tempdir.
   ⛔ Never write to the file on disk and ⛔ never restore with a checkout — this repo has a
   dated record of a checkout in a neuter/restore harness silently destroying uncommitted work,
   and the byte-backup remedy goes stale mid-edit. A tempdir copy has neither failure mode.
2. Apply the neuter to the COPY: replace the allow-list declaration so the predicate accepts
   everything (the empty-string prefix is the minimal mutation, and the file's own comment names
   it as the neuter).
3. ⛔ Assert the mutation APPLIED before believing anything: assert the mutated text differs from
   the original AND that the original allow-list literal is absent from the mutated text, with a
   CALIBRATION message saying a neuter that does not apply reads as GREEN. This assertion is the
   whole point — without it the arm certifies nothing.
4. Spawn the COPY with `--self-test` and require a NON-ZERO exit, and require the stderr to carry
   the terminal failed banner. State in the message which fixture rows the neuter is expected to
   break (every row whose expected verdict is CODE).
5. Add a NON-VACUITY CONTROL beside it: spawn the UNMUTATED copy from the same tempdir with the
   same spawn call and require exit 0. Without this control, an arm that always saw a non-zero
   exit — a broken spawn, a missing interpreter, a bad cwd — would pass while proving nothing.
6. Add a second neuter leg on the OTHER polarity: mutate the copy so the every-match becomes a
   some-match, assert that mutation applied, and require a non-zero self-test exit. The two legs
   together show both the widening and the loosening neuter are caught.

Add a comment recording the placement argument once more, because this arm is the one a reader
will question: this test runs in `frontend-test`, which this phase filters — and that is sound,
because a docs-only PR cannot change `scripts/classify-changed-paths.mjs`, so every PR that can
break the subject of this arm is a code PR on which `frontend-test` runs.

**B. The one-time end-to-end observation, recorded rather than assumed.**

Run the neuter by hand once, outside the test, and record the transcript verbatim in the SUMMARY:
copy the classifier to the scratchpad directory, apply the same widening mutation there, show
the mutated line so the neuter is visibly APPLIED, run the copy's `--self-test`, and capture the
failed banner and the non-zero exit code. Then run the unmutated script's `--self-test` and
capture the passing banner. ⛔ Do not mutate the checked-out file at any point.

Then state, in the SUMMARY, the CHAIN the observation stands for and why it terminates in a RED
board rather than a short one: a failing classifier makes the `changed-paths` job red; every job
carrying `needs: [changed-paths]` skips; the detector output is the empty string so the uniform
arm's first conjunct is false; every skipped row falls through to the strict default or to a
pre-existing non-tolerant branch; the aggregator exits 1. Cite scenario S5 as the executed proof
of the last three links, and state plainly which link is NOT proven locally — that GitHub's
scheduler produces those results — and that it is closed by the real PRs in plan 04.

**C.** Re-run the whole contracts directory once and paste the summary line into the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts</automated>
    <fails_when>non-zero exit, or the CALIBRATION arm and its non-vacuity control do not both appear as passing rows in the reporter output</fails_when>
    <automated>git status --porcelain scripts/classify-changed-paths.mjs</automated>
    <fails_when>the command prints any line — the classifier must be byte-unchanged after the neuter work, and a non-empty result means the mutation was applied to the tree instead of to a copy</fails_when>
    <automated>node scripts/classify-changed-paths.mjs --self-test</automated>
    <fails_when>non-zero exit, or the terminal PASSED banner is absent — proving the restore is real rather than asserted</fails_when>
  </verify>
  <done>The contract test carries a calibration arm that manufactures the neuter in a tempdir, proves the neuter applied, observes the non-zero self-test exit on two independent mutations, and carries a non-vacuity control that requires the unmutated copy to exit 0. The one-time transcript is in the SUMMARY with the mutated line visible, and the working tree's classifier is byte-unchanged.</done>
  <reversibility rating="reversible">Test-only; the subject is never mutated on disk.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR head branch → the sixteen filtered jobs | One boolean now decides whether sixteen gates run; a misclassification is a gate-disable primitive |
| `ci.yml` job-key set → the filter's partition | A job added tomorrow that nobody classifies silently defaults to always-on or to filtered depending on where it is written |
| the neuter harness → the working tree | A mutation harness that writes to the tree can destroy concurrent uncommitted work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.3-08 | Tampering | a filtered job's condition | high | mitigate | The contract test pins every filtered condition to a single physical line carrying the not-equals-true form and asserts the fail-open equals-false form is absent from all of them; an empty detector output therefore runs the gate |
| T-164.6.3-09 | Tampering / Elevation | an unclassified new job key | high | mitigate | The exact-set pin asserts filtered ∪ always-on ∪ {aggregator, detector} equals the population re-derived from `ci.yml` text, failing by name on any key that belongs to none |
| T-164.6.3-10 | Information Disclosure | the five always-on leak gates | high | mitigate | A structural pin asserts each of the five carries neither the `needs:` edge nor the conjunct, so a later edit that filters `secret-scan` or `frontend-lint` reddens rather than shipping |
| T-164.6.3-11 | Tampering | the uniform arm's placement | high | mitigate | Relative-position pin plus scenario S7: moving the arm to the bottom of the chain flips S7 from 0 to 1 even though every string is still present |
| T-164.6.3-12 | Denial of Service | the neuter/restore harness | medium | mitigate | The mutation is applied to a `mkdtempSync` copy only; a `git status --porcelain` verify proves the checked-out classifier is byte-unchanged, and no checkout is used to restore |
| T-164.6.3-13 | Repudiation | the deferred-detection consequence | low | mitigate | Recorded in a `ci.yml` comment beside the filtered job, booked as a TODOS item with a named owning phase in plan 03, and required in the SUMMARY — three places that must agree |
| T-164.6.3-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs in this plan; no `[ASSUMED]` or `[SUS]` package exists, so no legitimacy checkpoint is owed |
</threat_model>

<verification>
- `npx vitest run src/__tests__/contracts/ src/__tests__/critical-regressions.test.ts` exits 0.
- `node scripts/classify-changed-paths.mjs --self-test` exits 0 after the neuter work.
- `git status --porcelain scripts/classify-changed-paths.mjs` prints nothing.
- The job-key derivation run in Task 1 reconciles exactly against the sixteen-plus-five-plus-two partition, and the derived list is in the SUMMARY.
- `npm run lint` exits 0.
</verification>

<success_criteria>
- Sixteen jobs filtered, five always-on, the partition pinned as an exact set in both directions.
- Every added condition fails closed and stays on one physical line.
- S1 through S8 all execute the aggregator's real shell and assert exit codes.
- The neuter was APPLIED, the red was OBSERVED on two independent mutations, the non-vacuity control passes, and the tree is unchanged.
- The deferred-detection consequence is named in `ci.yml`.
</success_criteria>

<output>
Create `.planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-02-SUMMARY.md` when done. It must carry the derived job-key list, the eight scenario reporter rows, and the verbatim neuter transcript with the mutated line visible and both exit codes.
</output>
