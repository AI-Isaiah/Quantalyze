---
phase: 164.6.3-cidocspath
verified: 2026-09-13T14:15:00Z
status: passed
score: 5/5 must-haves verified
covered_files:
  - ".github/workflows/ci.yml"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-01-PLAN.md"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-01-SUMMARY.md"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-02-PLAN.md"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-02-SUMMARY.md"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-03-PLAN.md"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-03-SUMMARY.md"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-04-PLAN.md"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-04-SUMMARY.md"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-05-PLAN.md"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-05-SUMMARY.md"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/164.6.3-CONTEXT.md"
  - ".planning/phases/164.6.3-cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a/deferred-items.md"
  - "scripts/classify-changed-paths.mjs"
  - "src/__tests__/contracts/ci-docs-path-filter.contract.test.ts"
  - "src/__tests__/lint-sql-gates.test.ts"
covered_digest: "v1:sha256:56f641ac2ed811d3b8ace43e35924cf387e39a9b30301c5010462d3da07f774d"
behavior_unverified: 0
overrides_applied: 0
deferred:
  - truth: "The five `paths:`-filtered workflows are OBSERVED firing on a migration PR (`migration-drift-check` executing alongside the `ci.yml` gates)"
    addressed_in: "whichever phase next opens a legitimate migration PR"
    evidence: "164.6.3-04-SUMMARY.md §6.3 records this explicitly as UNTESTED and outside the phase boundary; the probe-migration PR is REFUSED on a production-safety basis (merging `supabase/migrations/**` auto-applies to shared TEST then PROD). The byte argument is stated as NOT subsuming the observational half."
  - truth: "Two assertions whose SUBJECT is `.planning/**` are hosted by a job the docs-only filter skips"
    addressed_in: "Phase 164.6 criterion 15"
    evidence: "ROADMAP.md:1733 `[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]`, with both candidate remedies recorded as REFUSED so neither returns as a new idea"
  - truth: "`gsd-tools windows append` stops failing closed on `.planning/WINDOWS.md` and the counts are re-derived"
    addressed_in: "Phase 164.6 criterion 14"
    evidence: "ROADMAP.md:1731 `[WINDOWS-LEDGER-COUNT-DRIFT]`; the drift was PRESERVED, not absorbed — verified below"
---

# Phase 164.6.3: CIDOCSPATH Verification Report

**Phase Goal:** A PR that changes NO code must stop running the entire gate corpus, AND a code PR must be PROVEN to still run every one of them.
**Verified:** 2026-09-13T14:15:00Z at worktree HEAD `d21b6c99`
**Status:** passed — 5/5 success criteria
**Re-verification:** No — initial verification

> **Method note.** Every figure below was RE-MEASURED by this verifier against the GitHub API and
> the local tree. Where a SUMMARY claim and my own reading are both present, my reading is the one
> reported. Two independent neuters were applied to the shipped gates, OBSERVED red, and restored;
> the tree is byte-clean afterwards (`ci.yml` sha256 `e4a7d563…`, restored and confirmed).

---

## Goal Achievement

### Observable Truths (the ROADMAP's five Success Criteria)

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | A docs-only PR no longer runs the code-gate jobs | ✓ VERIFIED | Run `34754223975` @ `d8afbe3d` (PR #792), `event: pull_request`, `conclusion: success`. My own `gh run view --json jobs`: **23 rows — 16 `skipped`, 7 `success`**. The 7 that ran are exactly `changed-paths`, `frontend`, `frontend-lint`, `plan-anchor-verify`, `secret-scan`, `version-gate`, `docs-link-check` — the declared always-on five plus the detector and the aggregator. |
| 2 | A code PR still runs EVERY one of them, on TWO REAL PRs | ✓ VERIFIED | Run `34748659917` @ `6a17adae` (PR #791), `event: pull_request`: **24 rows, ALL `success`** — zero skipped, zero failed. Both PRs are real and merged (#791 code, #792 docs). Structurally reinforced by the EXECUTED tolerance posture (below), which shows every filtered row tolerated in **0 of 8** non-docs-only combinations. |
| 3 | Before/after job set compared explicitly, every difference named; a shorter code-PR list is a REGRESSION | ✓ VERIFIED | I diffed the row sets myself. Baseline run `34717952454` @ `c1262aff` = 23 rows. Code PR = 24 rows. `comm -23` (lost) = **EMPTY**. `comm -13` (gained) = **`changed-paths`**. The code PR is a strict SUPERSET of the pre-change corpus. |
| 4 | The `frontend` aggregator demonstrated NOT to wedge — no required check pending forever | ✓ VERIFIED | On the docs-only run the aggregator `frontend` reports **`success`** — not pending, not red. That is the real hazard CONTEXT.md identified (the `if: always()` default arm would have turned it RED) and it was observed GREEN on a live board. Independently re-confirmed: `branches/main/protection` → **404 Branch not protected**, `rulesets` → **[]**, so no required check exists to wedge. Both halves of the wedge asymmetry are visible in the phase's own data. |
| 5 | The shared-TEST mutex no longer taken by a docs-only PR, measured by ABSENCE of a `61616158` acquire in that PR's run | ✓ VERIFIED | Reproduced byte-for-byte from the log ARCHIVE by this verifier — see §5 below. Docs run: **0 occurrences across a 64-entry archive**. Code run: **3 distinct acquires**, attributed by archive entry to `1_sql-tests.txt`, `5_e2e-seeded.txt`, `17_python.txt`. |

**Score: 5/5 truths verified (0 present, behavior-unverified).**

---

## 1. Anti-vacuity — the shipped gates were OBSERVED red

This is the premise the phase must be held to, so it is verified first and by neuter, not by reading.

### 1a. `ci-docs-path-filter.contract.test.ts` — CAN it fail?

Located at `src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` (986 lines, 27 tests).
Baseline: **27 passed**. Two independent neuters applied to the real `ci.yml`:

| Neuter | What was changed | Result |
|--------|------------------|--------|
| N1 | `ALWAYS_ON="plan-anchor-verify frontend-lint"` → `ALWAYS_ON="plan-anchor-verify"` (drop the leak-gate from the always-on list) | 🔴 **3 failed / 23 passed.** Named diff printed: expected `["frontend-lint","plan-anchor-verify"]`, received `["plan-anchor-verify"]` |
| N2 | The uniform arm's guard `[ "$docs_only" = "true" ]` → `[ "$docs_only" != "false" ]` (fail-OPEN on an empty detector output) | 🔴 **2 failed / 25 passed.** `S5 — a failed/cancelled detector (empty output) reddens the board` failed with `an empty detector output must FAIL CLOSED`, plus the arm-position test |

⭐ **N2 is the important one.** `S5` did not grep for a string — it **spawned bash, executed the
aggregator's own extracted shell, and read the exit code**. That is a behavioural oracle, so the
contract test cannot be re-spelled around. Both neuters restored; `ci.yml` sha256 confirmed
identical to pre-neuter.

**Verdict: the contract test is able to fail, on both its static and its executed arms.**

### 1b. The MW02 executed-tolerance oracle (plan 05) — genuinely RESTORED, not merely edited

Plan 05 exists because wave 1 blinded this oracle (`[164.6.3-MW02-DOCSONLY-BLIND]`): hoisting
`docs_only` / `ALWAYS_ON` above the `for r in \` anchor left the slice MENTIONING both names and
DEFINING neither, so bash expanded them to empty, the uniform arm was dead in simulation, eleven
rows silently gained tolerance, and the oracle printed the pre-change answer and passed.

All three claimed repairs are present in `src/__tests__/lint-sql-gates.test.ts`:

1. **`extractResultLoopScript`** — slices from a whole-line `fail=0` anchor (whole-line
   deliberately, because the case-spelling fixture carries `fail=0` inside prose).
2. **The unset-variable check** — `spawnSync("bash", ["-euo", "pipefail"])`, i.e. `-u` added to
   GitHub's default flags, with a `did not run to its verdict` guard converting an abort into a
   MEASURE_FAIL.
3. **The 3/11/1 partition** — `EVENT_TOLERANT_JOBS` (3), `DOCS_ONLY_TOLERANT_JOBS` (11),
   `NEVER_TOLERANT_JOBS` (1, `frontend-lint`), replacing the flat `TOLERANCE_BEARING_JOBS`.

Baseline: **74 passed**. The file prints its measured posture:

```
docs_only=false — 8 combination(s); tolerated skips: frontend-typecheck=0, frontend-lint=0,
  frontend-test=0, frontend-coverage=0, frontend-seam-redis=0, frontend-local-stack=0,
  frontend-policy=0, frontend-build=0, e2e-seeded=4, sql-tests=6, sql-gate-lint=0,
  sql-mutation=0, plan-anchor-verify=4
docs_only=true  — 8 combination(s); tolerated skips: frontend-typecheck=8, frontend-lint=0,
  frontend-test=8, ... sql-mutation=8, plan-anchor-verify=4
```

**NEUTER — I reintroduced the exact original defect** by reverting `extractResultLoopScript` to the
pre-repair narrow slice (`const init = start`):

```
🔴 Tests  8 failed | 66 passed (74)
Error: the result loop did not run to its verdict (status 1) — nothing below may be read as a pass:
bash: line 18: ALWAYS_ON: unbound variable
AssertionError: the executed script no longer assigns `docs_only` — the partition has nothing to
  split on, and a posture that cannot distinguish the halves is the blindness this oracle was
  repaired for
```

⭐ **Both repair layers bit independently** — the instance fix (`docs_only` assignment missing) and
the root-cause fix (`ALWAYS_ON: unbound variable` abort). Restored; file sha256 confirmed.

**Verdict: the MW02 oracle is genuinely restored. The blindness is now a named RED, not a green run
reporting yesterday's world.**

⭐ Note also what the posture PROVES about criterion 2 as a structural property: every filtered row
is tolerated in **0 of 8** non-docs-only combinations and **8 of 8** docs-only ones — never an
intermediate count. An intermediate count is the signature a smuggled per-row tolerance would leave.
There is none, so CONTEXT.md's "ONE uniform arm, never a per-row tolerance" is verified **by
execution**, not by inspection. `frontend-lint` is 0 in BOTH halves, as its `NEVER_TOLERANT` status
requires.

### 1c. `scripts/classify-changed-paths.mjs` — self-test and fail-closed

```
$ node scripts/classify-changed-paths.mjs --self-test
=== SELF-TEST PASSED: 11/11, every verdict fired on its own input ===   exit=0
```

**11/11 as specified.** Fail-closed behaviour verified by direct execution, all three forms:

```
$ GITHUB_EVENT_NAME=push               → docs_only=false — event is 'push', not pull_request
$ (unset)                              → docs_only=false — event is '(unset)', not pull_request
$ GITHUB_EVENT_NAME=workflow_dispatch  → docs_only=false — event is 'workflow_dispatch', ...
```

A non-`pull_request` event yields `docs_only=false`. Confirmed. The allow-list is a single literal
`".planning/"` with the trailing separator INSIDE the literal (which is what makes the
`.planningfake/` / bare `.planning` / `.planning-notes/` boundary rows fail closed), an empty diff
returns `false` by explicit decision, and a `git diff` read failure is a `MEASURE_FAIL` with a
non-zero exit. `--no-renames` is present with its reasoning. The contract test also carries three
calibration arms that mutate a COPY of the classifier (widen the allow-list; `every` → `some`),
observe the self-test go RED, run an unmutated non-vacuity control, and then assert the
checked-out file is byte-unchanged.

---

## 2. Criterion 3 — the before/after census, measured by this verifier

⛔ Compared by ROW, not by job key, per the standing correction (`frontend-test` is a 2-shard
matrix reporting two rows for one key).

| Board | Run | Event | Rows | Composition |
|-------|-----|-------|------|-------------|
| Baseline (pre-change) | `34717952454` @ `c1262aff` | pull_request | **23** | all success |
| Code PR (post-change) | `34748659917` @ `6a17adae` | pull_request | **24** | **all 24 success** |
| Docs PR (post-change) | `34754223975` @ `d8afbe3d` | pull_request | **23** | 16 skipped, 7 success |

```
$ comm -23 before.txt after.txt     # rows LOST on the code PR — a REGRESSION if non-empty
(empty)
$ comm -13 before.txt after.txt     # rows GAINED
changed-paths
```

**Zero rows lost. One row gained (the detector).** The code PR is a strict superset of the
pre-change corpus, which is the strongest possible answer to criterion 3 and to the ROADMAP's
"a shorter job list on a CODE PR is a REGRESSION, not an optimisation".

⚠️ **The docs board's 23 vs the code board's 24 is NOT a lost gate.** A SKIPPED matrix job reports
ONE row, so `frontend-test (1)`/`(2)` collapses to a single `frontend-test` row. Verified in my own
census output: the docs board shows exactly one `frontend-test` entry, the code board shows two.

---

## 3. Criterion 1 — the docs-only skip set

16 skipped rows on run `34754223975`, enumerated by me: `deps-cache`, `e2e`, `e2e-seeded`,
`frontend-build`, `frontend-coverage`, `frontend-local-stack`, `frontend-policy`,
`frontend-seam-redis`, `frontend-test`, `frontend-typecheck`, `knip`, `lighthouse-mobile`,
`python`, `sql-gate-lint`, `sql-mutation`, `sql-tests`.

That set is exactly the sixteen the contract test pins as the FILTERED roster in both directions,
and its complement on the board is exactly the five always-on jobs plus detector plus aggregator.
The always-on five survive as CONTEXT.md locked them — including `frontend-lint`, which the
orchestrator's original scout had missed and this phase's own research corrected (it hosts
`check-planning-hygiene.ts`, the leak gate whose subject IS a `.planning/`-only diff on a PUBLIC
repo). **Wiring confirmed at the source:** `grep -c '^  changed-paths:' .github/workflows/ci.yml`
→ 1, with `needs: [changed-paths]` + `if: needs.changed-paths.outputs.docs_only != 'true'` on the
filtered jobs and the detector itself carrying no `if:` of its own.

---

## 4. Criterion 4 — the aggregator does not wedge

| Check | Reading |
|-------|---------|
| `frontend` aggregator on the docs board | **`success`** (observed, live) |
| `gh api .../branches/main/protection` | **404 Branch not protected** |
| `gh api .../rulesets` | **`[]`** |

The 404 means there is no required check to wedge *today*, so half of this criterion has no subject
to test against. That would be a weak close on its own — but it is not what the phase rests on. The
substantive hazard CONTEXT.md identified is different and worse than the ROADMAP's: the aggregator
carries `if: always()` and a default arm setting `fail=1` on any non-`success`, `skipped` included,
so filtering without teaching the loop would turn a docs-only board **RED**. That hazard was
**observed cleared on a live board** — `frontend` = `success` with 16 skipped rows beneath it.

The mechanism argument is sound and, unusually, both of its halves are OBSERVED in this phase's own
data rather than asserted: a job skipped by a conditional still reports a `skipped` row (16 of them
are on the docs board), whereas a workflow skipped by a `paths:` filter reports nothing at all (the
five `paths:`-filtered workflows contribute zero rows). The forward conditional is booked: if
protection is ever enabled, `changed-paths` MUST join the required list, because it is the single
point of trust for sixteen gates.

**Verdict: PASS.** Scope noted honestly in the SUMMARY rather than glossed.

---

## 5. Criterion 5 — the crux, re-measured independently

The task brief flagged this as the place the phase could be hollow: does the absence rest on an
observation or on an argument? **I re-ran the measurement myself from the log archive endpoint.**

### 5a. The absence (docs run `34754223975`)

```
$ gh api repos/AI-Isaiah/Quantalyze/actions/runs/34754223975/logs > docs.zip    # fetch_exit=0
$ unzip -l docs.zip | tail -1
   365050                     64 files
$ unzip -p docs.zip | grep -ac '61616158'
0                                                                  grep_exit=1
$ unzip -Z1 docs.zip | grep -v '/' | sort
0_frontend.txt  17_version-gate.txt  18_plan-anchor-verify.txt  19_frontend-lint.txt
20_changed-paths.txt  21_docs-link-check.txt  22_secret-scan.txt
```

**Zero — and the zero is falsifiable, on three independent legs:**

1. **My own readability control (not in the SUMMARY).** `unzip -p docs.zip | grep -ac 'docs_only'`
   → **38, grep_exit=0**, on the same stream and the same instrument. The archive genuinely reads
   and grep is not declining the input. A `grep` exit 1 reading as "clean" is a dated defect class
   in this repo; that possibility is now excluded by measurement rather than by reasoning.
2. **Job coverage.** The 7 top-level per-job logs match EXACTLY the 7 jobs my `gh run view --json
   jobs` census says ran. Indices 1–16 are absent — the sixteen skipped jobs contribute no log file.
   No job's log is missing from the reading, so the zero is over a complete archive.
3. **The positive control on the same instrument** — below.

### 5b. The positive control (code run `34748659917`), attributed by entry

```
1_sql-tests.txt:  key x9, REAL acquires=1  ["... after 5s"]
5_e2e-seeded.txt: key x9, REAL acquires=1  ["... after 5s"]
17_python.txt:    key x9, REAL acquires=1  ["... after 540s"]
entries=48  entries_with_key=3  total_real_acquires=3     (48/48 entries readable, 0 unreadable)
```

**Three distinct acquires, three distinct jobs — reproduced byte-for-byte from the SUMMARY,
including `python`, the taker everything upstream forgets.** Raw occurrence count 27, matching.

### 5c. Adjudicating the `sort | uniq -c` under-count

The SUMMARY discloses that the plan's own recipe collapses to TWO rows for THREE acquires because
`sql-tests` and `e2e-seeded` both waited 5s. I reproduced that collapse exactly:

```
   1 Acquired the shared-test-db advisory lock (key 61616158) after 540s
   2 Acquired the shared-test-db advisory lock (key 61616158) after 5s
```

⭐ **I probed this further than the SUMMARY did and it holds up.** A bare
`grep -c 'Acquired the shared-test-db advisory lock'` returns **6**, not 3 — which looked at first
like an undercount in their favour. It is not. Three of those six are the shell's own command trace
(ANSI-coloured, containing the literal `${waited}s`), not acquisitions:

```
   3 [36;1m  echo "Acquired ... after ${waited}s (attempt ${attempt}/3)."[0m   ← command echo
   1 Acquired ... after 540s (attempt 1/3).                                    ← real
   2 Acquired ... after 5s (attempt 1/3).                                      ← real
```

Their `after [0-9]*s` pattern correctly excludes the trace lines; my looser phrase match did not.
**The instrument defect was on my side, not theirs.** Their count of three is right.

**Judgment on the crux: criterion 5 is closed by OBSERVATION, not by argument.** The under-count is
a disclosed defect in the PLAN's recipe on the *positive-control* side, remediated by switching to
entry attribution, and it does not touch the absence at all — an absence cannot be under-counted by
a collapsing recipe. The absence itself is a hard zero over a complete, demonstrably-readable
archive with a working positive control on the same instrument.

---

## 6. The specific traps checked

### 6a. Run `34749517490` is censused NOWHERE ✓

Independently confirmed: `conclusion=""`, `status=queued`, `jobs=0`. It appears in exactly two
lines of the corpus, both in a §-headed process note titled *"run `34749517490` is NOT evidence"*,
with the reasoning I would myself demand:

> ⛔ **It is censused NOWHERE in this SUMMARY and must never be.** A 0-job run against the docs head
> SHA looks exactly like "every job was filtered away" and is nothing of the kind — no job was ever
> scheduled.

**No criterion leans on it.** This is the correct handling of a run that could have been passed off
as the strongest possible evidence.

### 6b. The byte argument's SCOPE ✓

I re-derived the filtered-workflow set myself:

```
$ grep -l -E "^\s+paths:" .github/workflows/*.yml
migration-drift-check.yml   migration-policy-self-test.yml   migration-policy.yml
sql-function-snapshot.yml   supabase-migrate.yml
```

**Five, matching** — including `supabase-migrate.yml`, which the plan's four-item list omitted and
which is the highest-consequence file of the set (it is what auto-applies to TEST then PROD). The
SUMMARY widened four→five by derivation and booked it as a Rule-2 finding against its own plan.

Byte-identity independently re-run: `git diff --stat 0b9f0699^ 0b9f0699 -- <all five>` → **empty**.

The scope sentence is present, plain, and correctly non-subsuming:

> **What it does NOT prove.** It does **not** observe `migration-drift-check` firing. It is a
> DIFFERENT statement from the observational one `[CI-DOCSPATH-01]` asks for — **not a stronger one,
> and it does not subsume it.**

**The SUMMARY never claims it is stronger.** The observational half is explicitly recorded as owed.
Logged in `deferred:` above — a deferred observation, not a gap: the phase's mechanism is a
job-level condition inside `ci.yml`, and GitHub evaluates each workflow's `paths:` independently,
so this phase provably could not have narrowed those five. The refusal to open a probe migration PR
is the correct call on a repo where merging `supabase/migrations/**` is a production event.

### 6c. Criterion 3 by ROW ✓ — see §2. No row lost; one gained.

---

## 7. Known limits — confirmed RECORDED, not absorbed

| Limit | Routed to | Confirmed |
|-------|-----------|-----------|
| `[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]` | Phase 164.6 criterion 15 | ✓ `ROADMAP.md:1733`, with BOTH candidate remedies recorded as REFUSED and reasoned so neither returns as a new idea |
| `[WINDOWS-LEDGER-COUNT-DRIFT]` | Phase 164.6 criterion 14 | ✓ `ROADMAP.md:1731` — **and the counts were NOT hand-forced** (verified below) |
| PR-side-only saving | stated in-SUMMARY | ✓ stated as a measurement, not buried (verified below) |

### 7a. The WINDOWS counts were NOT hand-forced — the silent absorption did not occur

```
$ head -8 .planning/WINDOWS.md
open_count: 36   waived_count: 0   fixed_count: 11   total_count: 47
$ git log --oneline -1 -- .planning/WINDOWS.md
604d655f Phase 164.8.5: PROBERPARSE ...
```

The frontmatter is **untouched since `604d655f` (Phase 164.8.5)** — this phase did not edit it. And
the drift is real: the table carries **50 entry rows** against a declared `total_count: 47`. The
refusal stands, the discrepancy is preserved intact for Phase 164.6 criterion 14 to establish which
of the two numbers is wrong, and this phase's own `[164.6.3-MW02-DOCSONLY-BLIND]` went into
`deferred-items.md` instead. **This is exactly the right behaviour** — hand-editing the integers to
make the verb accept is the silent absorption that criterion exists to prevent.

### 7b. The PR-side-only limit is stated, not buried ✓

I re-measured the merge push myself: run `34755121736` @ `f10b0e23`, `event: push`,
`conclusion: success`, **24 rows** — only `version-gate` and `plan-anchor-verify` skipped, and both
from their own pre-existing push-event conditions, not from this filter. The SUMMARY states it in
bold, in its own section, with the measured acquire count (three, at 330s/475s/5s):

> **So: this change buys back the PR-side run and the PR-side mutex holds. It does not reduce what
> `main` pays on merge, and the shared-TEST mutex is still taken three times there.** That is the
> claimable saving and the whole of it.

⭐ It even opens the section with *"A claim of the whole corpus's job-seconds would be FALSE"*. The
honest number is `163 PR-side job-seconds + 0 mutex holds`, then `4,481 job-seconds + 3 mutex holds`
on merge. **Stated as a limit, prominently.**

---

## Behavioural Spot-Checks

| Behaviour | Command | Result | Status |
|-----------|---------|--------|--------|
| Classifier self-test | `node scripts/classify-changed-paths.mjs --self-test` | `SELF-TEST PASSED: 11/11`, exit 0 | ✓ PASS |
| Fail-closed on non-PR event | `GITHUB_EVENT_NAME=push node scripts/classify-changed-paths.mjs` | `docs_only=false` | ✓ PASS |
| Fail-closed on unset event | `node scripts/classify-changed-paths.mjs` | `docs_only=false` | ✓ PASS |
| Contract test green | `npx vitest run …/ci-docs-path-filter.contract.test.ts` | 27 passed | ✓ PASS |
| Contract test RED under N1 | ALWAYS_ON shrunk | 3 failed / 23 passed | ✓ PASS (bites) |
| Contract test RED under N2 | uniform arm made fail-open | 2 failed / 25 passed, S5 executed-shell arm fired | ✓ PASS (bites) |
| MW02 oracle green | `npx vitest run src/__tests__/lint-sql-gates.test.ts` | 74 passed, posture printed | ✓ PASS |
| MW02 oracle RED under original defect | narrow slice restored | 8 failed / 66 passed, `ALWAYS_ON: unbound variable` | ✓ PASS (bites) |
| Detector wired in `ci.yml` | `grep -c '^  changed-paths:'` | 1 | ✓ PASS |
| Byte-identity of 5 filtered workflows | `git diff --stat 0b9f0699^ 0b9f0699 -- <5>` | empty | ✓ PASS |

---

## Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `scripts/classify-changed-paths.mjs` | `TBD\|FIXME\|XXX` | — | **0 markers** |
| `src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` | `TBD\|FIXME\|XXX` | — | **0 markers** |
| `.github/workflows/ci.yml` | `TBD\|FIXME\|XXX` | — | **0 markers** |
| `src/__tests__/lint-sql-gates.test.ts` | `TBD\|FIXME\|XXX` | — | **0 markers** |

No unreferenced debt markers in any file this phase modified. Debt-marker gate: **clean**.

Release discipline (plan 03): `VERSION` and `package.json` are byte-equal 4-digit `0.77.40.0`;
`CHANGELOG.md:3` carries the unified entry `## [0.77.40.0] - 2026-09-13 — CIDOCSPATH: …`.
`TODOS.md`'s `[CI-DOCSPATH-01]` is re-pointed, its two-mutex claim corrected to THREE with `python`
named, and — notably — it carries a self-correction that the *motivating* PR #750 would NOT be
filtered by the shipped gate, with an explicit refusal to widen the allow-list to make it fit
(*"`VERSION`/`CHANGELOG.md` moving IS a release, and a release must run every gate"*).

---

## Gaps Summary

**None.** All five ROADMAP success criteria are verified against live, SHA-bound evidence that this
verifier re-measured independently rather than accepted from the SUMMARY.

**Where I pushed hardest and what I found:**

- **The contract test and the MW02 oracle both bite.** Four neuters across two files, all four
  observed RED with named diagnostics, all four restored to byte-identical state. The MW02 repair
  in particular is genuine: reintroducing the exact `[164.6.3-MW02-DOCSONLY-BLIND]` defect produces
  8 RED including the root-cause `ALWAYS_ON: unbound variable` abort, so both repair layers are
  load-bearing.
- **Criterion 5's absence is an observation, not an argument.** I added a readability positive
  control the SUMMARY did not have (`docs_only` × 38 in the same archive stream) and it holds. The
  disclosed `sort | uniq -c` under-count affects only the positive control and was remediated; when
  I probed the acquire count further (6 phrase-matches vs 3 real acquires) the discrepancy resolved
  in the phase's favour — the extra three are ANSI-coloured command-trace lines, and their
  `after [0-9]*s` pattern correctly excludes them.
- **Criterion 3 is stronger than "no regression".** The code PR is a strict SUPERSET of the
  pre-change corpus: zero rows lost, one gained.
- **The two places a phase like this usually cheats, it did not.** The wedged 0-job run
  `34749517490` — which would have been the most flattering possible evidence — is quarantined by
  name with the reasoning for its exclusion. And the WINDOWS ledger counts were left in their
  refusing state rather than hand-forced to let `/gsd-ship` through.
- **The byte argument does not overreach.** It is scoped in the SUMMARY's own words as "not a
  stronger one, and it does not subsume it", with the observational half recorded as owed. I
  re-derived the five-workflow set and the empty diff myself; both check out.

**Deferred (not gaps):** three items, all already booked to named phases with dated reasons — the
observational half of the `paths:`-workflow check, `[164.6.3-PLANNING-SUBJECT-DEFERRED-DETECTION]`
(Phase 164.6 criterion 15), and `[WINDOWS-LEDGER-COUNT-DRIFT]` (Phase 164.6 criterion 14).

---

_Verified: 2026-09-13T14:15:00Z_
_Verifier: Claude (gsd-verifier) — all readings re-measured at `d21b6c99`; neuter artifacts restored, tree byte-clean_
