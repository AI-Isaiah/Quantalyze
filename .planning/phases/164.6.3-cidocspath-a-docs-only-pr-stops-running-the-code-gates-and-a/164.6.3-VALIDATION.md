---
phase: "164.6.3"
slug: "cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-12"
---

# Phase 164.6.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (`vitest.config.ts` at repo root — carries the coverage RATCHET thresholds) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `node scripts/classify-changed-paths.mjs --self-test && npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` |
| **Full suite command** | `npm run lint && npm run test` |
| **Estimated runtime** | ~15s quick · full suite sharded 2× in CI (`ci.yml:215-217`) |

⚠️ The classifier's `--self-test` is a FIRST-CLASS gate here, not a convenience: the classifier is
this phase's single point of trust, and a wrong verdict from it greens sixteen jobs at once.

---

## Sampling Rate

- **After every task commit:** `node scripts/classify-changed-paths.mjs --self-test && npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts src/__tests__/critical-regressions.test.ts`
- **After every plan wave:** `npm run lint && npm run test`
- **Before `/gsd-verify-work`:** full suite green, PLUS both real-PR job censuses collected
- **Max feedback latency:** 20 seconds for the quick arm

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 164.6.3-01-xx | 01 | 0 | `CI-DOCSPATH-01` | — | Classifier verdict on every fixture row; fails CLOSED (unknown ⇒ treat as code) | unit | `node scripts/classify-changed-paths.mjs --self-test` | ❌ W0 | ⬜ pending |
| 164.6.3-01-xx | 01 | 0 | Criterion 1 | — | Docs-only PR greens the aggregator with the filtered rows skipped | contract S1 | `npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` | ❌ W0 | ⬜ pending |
| 164.6.3-01-xx | 01 | 0 | ⭐ Criterion 2 | — | Code-PR path is BYTE-IDENTICAL to pre-change; the new arm is inert | contract S4/S6/S8 | same | ❌ W0 | ⬜ pending |
| 164.6.3-01-xx | 01 | 0 | Criterion 3 | — | filtered ∪ always-on = every job key in `ci.yml`, exact in BOTH directions | contract (structural) | same | ❌ W0 | ⬜ pending |
| 164.6.3-01-xx | 01 | 0 | Fail-closed | T-164.6.3-01 | Detector failure ⇒ RED board, never a green skip | contract S5 | same | ❌ W0 | ⬜ pending |
| 164.6.3-01-xx | 01 | 0 | Always-on integrity | T-164.6.3-01 | `frontend-lint`, `plan-anchor-verify`, `secret-scan`, `version-gate`, `docs-link-check` carry NO docs-only `if:` | contract (structural) | same | ❌ W0 | ⬜ pending |
| 164.6.3-0x-xx | 0x | 1 | ⭐ Anti-vacuity | — | Neutered classifier ⇒ observed RED, then restored | manual-observed + recorded | neuter → run → restore, transcript in SUMMARY | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `scripts/classify-changed-paths.mjs` — the classifier plus its `--self-test`
- [ ] `src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` — scenarios S1–S8, the structural pins, and the CALIBRATION arms
- [ ] ⚠️ **Read `src/__tests__/contracts/REGISTRY.md` and `contracts-registry.test.ts` BEFORE creating that file.** `contracts-registry.test.ts` asserts every named invariant test still exists; an unregistered file in that directory may redden it. Register the new test in the same commit that creates it.
- [ ] Framework install: none needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| ⭐ TWO REAL PRs — one docs-only, one code | Criterion 2 | No local harness can make GitHub's scheduler produce a real skip set; the ROADMAP forbids closing on a dry run or a workflow-syntax argument | For each PR: `gh run view <id> -R AI-Isaiah/Quantalyze --json jobs`. Expect **23 check rows** on the code PR, not 21 — `frontend-test` is a 2-shard matrix. Enumerate before and after and name every difference. |
| Absence of the shared-TEST mutex acquire | Criterion 5 | Only a real run's log archive proves an absence | `gh api repos/AI-Isaiah/Quantalyze/actions/runs/<id>/logs > /tmp/logs.zip && unzip -p /tmp/logs.zip \| grep -ac 61616158`. ⚠️ `gh run view --log` TRUNCATES — do not use it. ⚠️ Expect **THREE** acquires on the baseline run (`python`, `e2e-seeded`, `sql-tests`), not two. |
| No wedge | Criterion 4 | State of the remote, not of the tree | `gh api repos/:owner/:repo/branches/main/protection` and `gh api .../rulesets`. Measured 2026-09-12: 404 "Branch not protected" and `[]`. Re-measure at close — a green that is not SHA-bound proves nothing. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without an automated verify
- [ ] Wave 0 covers every MISSING reference above
- [ ] No watch-mode flags
- [ ] Feedback latency < 20s on the quick arm
- [ ] The neuter was APPLIED, observed RED, and restored — a neuter that does not apply reads as GREEN
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
