---
phase: "164.6.3"
slug: "cidocspath-a-docs-only-pr-stops-running-the-code-gates-and-a"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-12"
---

# Phase 164.6.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (`vitest.config.ts` at repo root — carries the coverage RATCHET thresholds) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `node scripts/classify-changed-paths.mjs --self-test && npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` |
| **Full suite command** | `npm run lint && npm run test` |
| **Estimated runtime** | ~15s quick · full suite sharded 2× in CI (the `frontend-test:` job key and its `matrix: shard:` entry — cited by symbol, not by line: `[164.7-CITATION-DRIFT-01]`) |

⚠️ The classifier's `--self-test` is a FIRST-CLASS gate here, not a convenience: the classifier is
this phase's single point of trust, and a wrong verdict from it greens sixteen jobs at once.

---

## Sampling Rate

- **After every task commit:** `node scripts/classify-changed-paths.mjs --self-test && npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts src/__tests__/critical-regressions.test.ts`
- **After every plan wave:** `npm run lint && npm run test`
- **Before `/gsd-verify-work`:** full suite green, PLUS both real-PR job censuses collected
- **Max feedback latency:** 20 seconds for the quick arm

---

## Per-Task Verification Map

Rebuilt 2026-09-12 against the FINAL plan set (post revision 1). Task names are verbatim.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 164.6.3-01-01 | 01 | 1 | `CI-DOCSPATH-01` · crit 1 | T-164.6.3-01 | **TRACER** — classifier fails CLOSED; detector→output→job `if:`→FIRST-placed uniform arm proven by EXECUTING the aggregator shell | unit + contract | `node scripts/classify-changed-paths.mjs --self-test` · `GITHUB_EVENT_NAME=push node scripts/classify-changed-paths.mjs` · `npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` | ❌ W0 | ⬜ pending |
| 164.6.3-01-02 | 01 | 1 | `CI-DOCSPATH-01` | T-164.6.3-05 | Guard registered so it cannot be silently deleted; fail-closed + always-on scenarios | contract + registry | `npx vitest run src/__tests__/contracts/` · `grep -c '^  { path:' src/__tests__/contracts/contracts-registry.test.ts` | ❌ W0 | ⬜ pending |
| 164.6.3-02-01 | 02 | 2 | crit 1 · crit 3 | — | The remaining fifteen job keys carry the `if:`; `ci.yml` records the three prose facts (always-on rationale, Pitfall-5 residual, merge-push honesty) | unit + static | `npx vitest run src/__tests__/critical-regressions.test.ts` | ✅ | ⬜ pending |
| 164.6.3-02-02 | 02 | 2 | crit 3 | T-164.6.3-10 | Exact-set pins in BOTH directions make a silent reclassification impossible; `ALWAYS_ON` pinned at two members | contract S1–S8 (**S2 split into S2a/S2b ⇒ 9 scenario rows**) | `npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` | ❌ W0 | ⬜ pending |
| 164.6.3-02-03 | 02 | 2 | ⭐ anti-vacuity | — | Neuter the detector, **OBSERVE the red**, restore — mutation applied to a `mkdtempSync` copy, mutation asserted to have APPLIED before the red is believed, non-vacuity control (unmutated copy exits 0), `git status --porcelain` clean after | observed neuter + CI-resident control | `npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` | ❌ W0 | ⬜ pending |
| 164.6.3-03-01 | 03 | 3 | bookkeeping | — | `TODOS.md` owner re-pointed from Phase 164.6 item (12) to this phase; two→three mutex-taker undercount corrected; Pitfall-5 deferral booked to a NAMED phase (164.6 GATE-HYGIENE) | static | `grep` assertions named in the plan | ✅ | ⬜ pending |
| 164.6.3-03-02 | 03 | 3 | release toll | — | Byte-equal 4-digit `VERSION`/`package.json`; ONE unified `[version]` CHANGELOG entry cross-checked commit by commit per CLAUDE.md's mechanism | unit | `npx vitest run src/__tests__/critical-regressions.test.ts` | ✅ | ⬜ pending |
| 164.6.3-04-01 | 04 | 4 | ⭐ crit 2 · crit 3 · crit 4 · crit 5 | — | The CODE-PR census (**24** check rows post-change — the 23-row baseline plus `changed-paths`), the **three**-acquire mutex positive control, the byte argument stated at its true scope, two remote re-measurements | manual-observed (real run) | `gh run view <id> -R AI-Isaiah/Quantalyze --json jobs` | n/a | ⬜ pending |
| 164.6.3-04-02 | 04 | 4 | — | — | **MERGE GATE** — `checkpoint:human-action` `gate="blocking-human"`; no merge on the founder's behalf, and the docs-only half is structurally unmeasurable before it | human checkpoint | none — blocking | n/a | ⬜ pending |
| 164.6.3-04-03 | 04 | 4 | ⭐ crit 1 · crit 5 | — | The docs-only PR: the skipped set, a **GREEN** aggregator, and the absent mutex read against its control | manual-observed (real run) | `gh run view … --json jobs` · `gh api …/actions/runs/<id>/logs` → `unzip -p \| grep -ac 61616158` | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

⛔ Plan 04 is `autonomous: false`. Waves 1–3 are fully autonomous; wave 4 is the gate.

---

## Wave 0 Requirements

- [ ] `scripts/classify-changed-paths.mjs` — the classifier plus its `--self-test`
- [ ] `src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` — scenarios S1–S8, the structural pins, and the CALIBRATION arms
- [ ] ⚠️ **Read `src/__tests__/contracts/REGISTRY.md` and `contracts-registry.test.ts` BEFORE creating that file.** `contracts-registry.test.ts` asserts every named invariant test still exists; an unregistered file in that directory may redden it. Register the new test in the same commit that creates it.
- [ ] Framework install: none needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| ⭐ TWO REAL PRs — one docs-only, one code | Criterion 2 | No local harness can make GitHub's scheduler produce a real skip set; the ROADMAP forbids closing on a dry run or a workflow-syntax argument | For each PR: `gh run view <id> -R AI-Isaiah/Quantalyze --json jobs`. ⚠️ **Two different numbers, do not conflate them.** The BASELINE (pre-change) board is **23** check rows, not 21 — 22 job keys with `frontend-test` counting twice as a 2-shard matrix. The POST-CHANGE code-PR board must be **24**: the same 23 plus the new `changed-paths` job. A code PR showing 23 has LOST a gate. Enumerate before and after and name every difference. |
| Absence of the shared-TEST mutex acquire | Criterion 5 | Only a real run's log archive proves an absence | `gh api repos/AI-Isaiah/Quantalyze/actions/runs/<id>/logs > /tmp/logs.zip && unzip -p /tmp/logs.zip \| grep -ac 61616158`. ⚠️ `gh run view --log` TRUNCATES — do not use it. ⚠️ Expect **THREE** acquires on the baseline run (`python`, `e2e-seeded`, `sql-tests`), not two. |
| No wedge | Criterion 4 | State of the remote, not of the tree | `gh api repos/:owner/:repo/branches/main/protection` and `gh api .../rulesets`. Measured 2026-09-12: 404 "Branch not protected" and `[]`. Re-measure at close — a green that is not SHA-bound proves nothing. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without an automated verify
- [ ] Wave 0 covers every MISSING reference above
- [ ] No watch-mode flags
- [ ] Feedback latency < 20s on the quick arm
- [ ] The contract reporter shows NINE scenario rows (S2 split into S2a/S2b), not eight
- [ ] The neuter was APPLIED, observed RED, and restored — a neuter that does not apply reads as GREEN
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
