---
phase: 140.5-seamprose
plan: 03
type: execute
wave: 2
depends_on: ["140.5-01"]
files_modified:
  - src/lib/process-key-client.ts
  - src/lib/process-key-client.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.poll-disjointness.runtime.test.tsx
  - src/lib/seam-transport-attribution.invariant.test.ts
autonomous: true
requirements: [SEAMPROSE-02, SEAMPROSE-03]
must_haves:
  truths:
    - "A Python-side 429's Retry-After header survives postProcessKey to the browser on all five caller routes (⭐ HARD PREREQUISITE for phase 141)"
    - "Every actionable buildEnvelope thread passes the advertised wait to the envelope, and clears it on a fresh attempt (TRAP-3)"
    - "The SIXTH thread — SyncPreviewStep.handleRetrySync — reads the header and renders an envelope on a non-2xx retry (⭐ the exact path 141 builds on)"
    - "No wizard transport catch asserts a venue fault (KEY_NETWORK_TIMEOUT) for a failure on our own hop — EVERY occurrence in every catch body, including the telemetry payloads, at all five catches"
    - "No docblock asserts roster/wire-table disjointness; the stated property is AGREEMENT"
  artifacts:
    - path: "src/lib/process-key-client.ts"
      provides: "Retry-After re-stamped on the forwarded non-2xx response (headers currently die at :643-648)"
    - path: "src/lib/seam-transport-attribution.invariant.test.ts"
      provides: "derived-population guard: no KEY_NETWORK_TIMEOUT occurrence inside any wizard-step catch body"
  key_links:
    - from: "analytics-service/main.py RateLimitExceeded handler"
      to: "browser"
      via: "postProcessKey forwarding the Retry-After header verbatim (relay, never parse server-side)"
    - from: "wizard step responses"
      to: "src/components/error/ErrorEnvelope.tsx waitSeconds"
      via: "parseRetryAfterSeconds → state → buildEnvelope({retryAfterSeconds}) → envelope.retry_after_seconds"
---

<objective>
Make `Retry-After` ARRIVE (this phase must not add retry — 141 owns retry; CONTEXT §7 OUT-fence),
and stop the five wizard transport catches blaming the exchange for our own hop.

Coverage-law rows, stated per CONTEXT §2: the chokepoint re-stamp is **row 1** (one edit at
`process-key-client.ts:643-648` restores the header for all five caller routes — RESEARCH §4.4).
The five client threads are **per-site edits — row 3, partial by construction in those words** for
the threading itself; the compensating artefact is the derived-population transport-attribution
guard plus per-surface arrival tests, and BOTH halves are required: the chokepoint alone reaches
zero wizard surfaces, the threads alone reach zero `/process-key` 429s (RESEARCH §4.4 — state this
in the SUMMARY too).

Purpose: SEAMPROSE-02 (WP-3, ⭐ hard prerequisite for 141) + the transport half of SEAMPROSE-03 (WP-10).
Output: header travels end-to-end; SERVICE_UNREACHABLE at all five catches; agreement docblocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-CONTEXT.md   (§6e sixth thread, §7.2/7.3)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-RESEARCH.md   (§4 whole section, §5, §13.1, §19)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-PATTERNS.md   (§5 envelope construction — the working thread end to end)
@src/lib/process-key-client.ts
@src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
@src/lib/retry/retry-after.ts
@src/components/error/ErrorEnvelope.tsx
</context>

<interfaces>
The pattern to copy is IN THE REPO and measured working — `SyncPreviewStep.tsx`, end to end
(PATTERNS §5): `parseRetryAfterSeconds(res.headers)` at the response (`:709-717`) → `useState` →
`buildEnvelope(code, correlationId, { retryAfterSeconds: retryAfterSeconds ?? undefined })`
(`:1449-1456`) → **clear the state on a fresh attempt (`:1480`)** — the half a copy-paste drops;
a stale wait rendered on the NEXT failure names a duration nobody advertised (TRAP-3).

Hard constraints:
- `parseRetryAfterSeconds` is the ONE parser — a raw `Number(res.headers.get(…))` is a repo-wide
  ESLint error by design (`quantalyze/no-raw-retry-after-parse`).
- SERVER side (chokepoint): do NOT parse at all — relay the raw header string verbatim. Parsing
  belongs to the read side; the body's `retry_after_seconds` field must NOT become a second
  extraction path (RESEARCH §4.4 warns exactly this).
- `ErrorEnvelope.tsx:128-130` renders the wait only when `showRetry` (derived from `recoverable`)
  — threading a wait onto a non-recoverable code reaches nothing; re-derive the polarity per site.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chokepoint — re-stamp Retry-After on the forwarded non-2xx response</name>
  <files>src/lib/process-key-client.ts, src/lib/process-key-client.test.ts</files>
  <behavior>
    - A mocked upstream 429 `Response` with `Retry-After: 17` returns through `postProcessKey` as a
      NextResponse whose `Retry-After` header is `"17"` (string-identical relay).
    - An HTTP-date `Retry-After` value is relayed byte-identically (no parsing server-side).
    - An upstream response WITHOUT the header produces a response WITHOUT it (absence is not zero —
      TRAP-3).
    - Body and status remain forwarded verbatim exactly as today (positive control: the 403
      write-capable-key verdict passes through untouched).
  </behavior>
  <action>
    At `process-key-client.ts:643-648` (line-exact at `4c60518b`: `if (!res.ok) {` …
    `NextResponse.json(body, { status: res.status })`), copy the upstream `Retry-After` header onto
    the returned NextResponse when present. ⚠️ The 45-line docblock at `:598-642` FORBIDS branching
    on status — reconciliation, to be argued in the docblock itself (PATTERNS §11.7 demands prose):
    relaying a header is not a status branch — no code path selects behaviour by status; the
    body-verbatim/`body.ok`-is-the-verdict contract is untouched; the header is metadata the
    docblock's contract never governed, and dropping it silently was the defect (a Python 429
    reaches the browser with `retry_after_seconds` in the body but no header — RESEARCH §4.4).
    Extend the docblock with this reconciliation; do not weaken its status-branching prohibition.
    Tests go in `src/lib/process-key-client.test.ts` — ⚠️ FENCE: `tests/lib/process-key-client.test.ts`
    also exists and is OWNED BY 140.5-04 in this same wave (its `:8` citation is past-EOF); do not
    touch it. Use `vi.spyOn` + `restoreAllMocks`, NEVER `vi.stubGlobal` (CONTEXT §9 / DEF-16-1).
    Prose hygiene in owned file: "sixteen" at `process-key-client.ts:198` → delete the integer, name
    the predicate; convert bare `file:line` citations reported by
    `MODE=full node .planning/phases/140.5-*/140.5-citation-census.mjs src/lib/process-key-client.ts`
    to symbol-anchored references.
  </action>
  <verify>
    <automated>SC-RA-3 (planner-added ledger row): remove the header relay → the chokepoint test reds (paste, revert). Full `npx vitest run` + `npx tsc --noEmit`</automated>
  </verify>
  <done>Header relayed verbatim on all five caller routes via the one chokepoint; docblock reconciliation written; ledger observed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the wait at the five actionable silent sites + SERVICE_UNREACHABLE at the five catches (every occurrence)</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx</files>
  <behavior>
    Per-surface arrival (population + classification published, RESEARCH §4.2: 8 buildEnvelope call
    sites; 1 already threaded; 2 structurally excluded — #2 has no Response, #5's route has no
    Retry-After producer; ⇒ 5 actionable across 4 handler threads):
    - SubmitStep (`:291`), ConnectKeyStep (`:479`), MultiKeyConnectStep continue-handler
      (`:886`/`:891` — ONE handler, one edit covers both) and add-key panel (`:1178`): a 429
      response with `Retry-After: 30` renders the envelope wait; a response WITHOUT the header
      renders NO wait; a SECOND failure without the header after a first WITH it renders NO stale
      wait (the clear-on-fresh-attempt half).
    - SERVICE_UNREACHABLE at the transport catches in these three files (population predicate
      RESEARCH §5: `ConnectKeyStep.tsx:467`, `SubmitStep.tsx:278`, `MultiKeyConnectStep.tsx:806`,
      `:936` — the synthesis named three and missed MultiKey entirely): a rejected `wizardFetch`
      renders "We could not reach our own service", never the exchange-fault KEY_NETWORK_TIMEOUT
      copy. ⚠️ W5 — REPLACE EVERY OCCURRENCE WITHIN EACH CATCH BODY, not just the state
      assignment: each of these catches carries a SECOND `KEY_NETWORK_TIMEOUT` in its telemetry
      payload (`ConnectKeyStep.tsx:471`, `MultiKeyConnectStep.tsx:811`, `:940`) — a telemetry
      stream still reporting the venue-fault code is the same false attribution, machine-readable.
      (SyncPreviewStep's catch is edited in Task 3 with its file.)
  </behavior>
  <action>
    Copy the SyncPreviewStep pattern end to end per the interfaces block, INCLUDING the state-clear
    on every fresh attempt. Where a handler already passes a context object
    (`MultiKeyConnectStep.tsx:863`), extend it; never add a second buildEnvelope call. For the
    catches: replace ALL `KEY_NETWORK_TIMEOUT` occurrences per catch body with
    `SERVICE_UNREACHABLE` (copy already final at `wizardErrors.ts:1097-1109`; the rule is stated
    by name at `:196-200`). TRAP-1: every edited catch must keep (or gain) its scrub — a transport
    error can embed outgoing headers including the user's live JWT; check each catch's logging
    before and after. Do NOT add any retry/backoff logic — 141 owns retry (CONTEXT §7 OUT).
    Docblock truth in owned files: rewrite `ConnectKeyStep.tsx:430` and `MultiKeyConnectStep.tsx:757`
    — both still assert unqualified roster/wire-table disjointness, contradicting their own headers
    200 lines above (RESEARCH §13.1). State AGREEMENT as the property (disjointness sufficient,
    agreement necessary; two measured overlaps: KICKOFF∩={RATE_LIMITED}, FINALIZE∩={SEAM_MISCONFIGURED}).
    Do not touch `seam-discriminator.ts:242`'s "disjoint by CONTRACT" — different pair, correct.
    Prose hygiene: convert census-reported bare citations in these three step files.
  </action>
  <verify>
    <automated>SC-RA-1 mutation: drop the wait from the SECOND thread (ConnectKeyStep — not the first one an author checks) → its arrival test reds. Full `npx vitest run`</automated>
  </verify>
  <done>4 of 4 non-CSV surfaces thread the wait (receipt = the arrival tests, not a grep); catches honest in three files with telemetry payloads included; ledger observed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The sixth thread (handleRetrySync) + the transport-attribution class guard</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.test.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.poll-disjointness.runtime.test.tsx, src/lib/seam-transport-attribution.invariant.test.ts</files>
  <behavior>
    - `handleRetrySync` (`SyncPreviewStep.tsx:1406-1440`): on a non-2xx it currently discards the
      header AND renders nothing (`if (res.ok && …)` with no else — CONTEXT §6e names this the exact
      path 141 builds on). After: non-2xx → classify via the existing kickoff path, set the error
      state so the envelope renders, read `parseRetryAfterSeconds(res.headers)` into the SAME state
      the first-attempt thread uses, and clear it on the next attempt. No automatic retry added.
    - SyncPreviewStep's transport catch (`:740`) renders SERVICE_UNREACHABLE (fifth catch, with
      Task 2's four) — every occurrence in the catch body, per W5.
    - Class guard `seam-transport-attribution.invariant.test.ts`: derives the population (all
      non-test `*.tsx` under the wizard steps directory, from disk), comment-strips via
      `stripCommentsPreserveLines` (140.5-01), extracts `catch` block bodies by balanced-brace walk,
      and asserts NO catch body contains `"KEY_NETWORK_TIMEOUT"` — ANY occurrence, so the telemetry
      second-occurrences are in scope. ⚠️ W4 — vacuity fence re-derived: the measured population is
      **11 catch bodies** across the 6 non-test step files (predicate: the balanced-brace catch
      extraction over the comment-stripped sources; only 5 of the 11 classify transport failures —
      the assertion covers all 11, the floor counts what the WALK finds). Floor: ≥8 catch bodies
      (~70% — a degraded walk finding 6 must FAIL, which the old ≥5 floor let pass silently) AND
      ≥4 step files scanned. Failure message: fix-the-walk-never-lower-the-floor (seam-log-coverage
      idiom). Both-polarity needle self-test on synthetic source strings.
      CSV note in the guard docblock: `CsvUploadStep`'s catch sets CSV_NETWORK_TIMEOUT — its copy
      ("the server did not respond") does NOT assert a venue fault, so it is deliberately allowed;
      the guard bans only the venue-fault code (decision per RESEARCH §5's sixth-site note).
  </behavior>
  <action>
    Implement per the behavior block. The retry-button test simulates: first attempt 429-with-header
    (wait renders), retry non-2xx without header (envelope renders, NO stale wait), retry 401
    (envelope renders — today it silently leaves the stale banner). Guard goes in
    `src/lib/seam-transport-attribution.invariant.test.ts`; registration in CONTRACT_GUARDS owed by
    140.5-08 (name it in `## OPEN`).
    Prose hygiene in owned files: ⛔ B-4 — `SyncPreviewStep.poll-disjointness.runtime.test.tsx:105`
    reads "a sixteenth seam route added next month": this is an ORDINAL about a hypothetical NEW
    route, NOT a count of route tests — deleting it as a count (the treatment the other "sixteen"
    sites get) would make it FALSE. REWORD the ordinal instead: "a new seam route added next month"
    (keyed to the derived roster, no integer). Census-reported bare citations in
    `SyncPreviewStep.tsx` converted. `SyncPreviewStep.tsx:664-668`'s corrected non-disjointness
    text is already right — do not touch.
  </action>
  <verify>
    <automated>SC-RA-2 mutation: re-drop the header read in `handleRetrySync` → sixth-thread test reds. SC-B02-2 / SC-SU-1 mutations: revert `MultiKeyConnectStep.tsx:806` (the 4th catch nobody counted) and `SubmitStep.tsx`'s catch to KEY_NETWORK_TIMEOUT → the class guard reds naming each file AND the per-surface test reds. Paste all, revert all, porcelain clean. Full `npx vitest run` + Node 22 parity + `npx tsc --noEmit` + `npm run lint` (wave-merge gate)</automated>
  </verify>
  <done>Sixth thread wired; class guard green with the ≥8-of-11 floor (predicate published); poll-disjointness ordinal reworded not deleted; SC-RA-2, SC-B02-2, SC-SU-1 observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python service → Next server → browser | Retry-After relayed verbatim across two hops; body verdict contract untouched |
| transport error → log/render/telemetry | undici errors embed outgoing headers (INTERNAL_API_TOKEN, live JWT) — TRAP-1; telemetry carries the attribution code too |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.5-06 | Info Disclosure | edited catch blocks | mitigate | scrubSeamError kept/added at every edited log site; verified per catch |
| T-140.5-07 | DoS (self-inflicted herd) | Retry-After plumbing | mitigate | ONE parser (`parseRetryAfterSeconds`, HTTP-date safe, never NaN/0/negative); no second extraction path from the body |
| T-140.5-08 | Tampering | header relay | accept | value originates from our own Python service, not the venue; relayed not interpreted |
</threat_model>

<verification>
Full suite (never scoped) after every task commit; Node 22 parity + lint + tsc at wave merge.
`composite/members` remains the one buildEnvelope-backed route with NO Retry-After producer — site
#5 stays structurally excluded; record it in the class-guard docblock so it cannot become a silent
hole the day a limiter lands (RESEARCH Open Question 4).
</verification>

<success_criteria>
- A Python-side 429 reaches the browser WITH its Retry-After header (chokepoint test) AND renders a
  wait on every actionable wizard surface (arrival tests) — both halves, stated because each alone
  reaches nothing.
- handleRetrySync renders an envelope + fresh wait state on non-2xx retry (⭐ 141's path).
- Zero KEY_NETWORK_TIMEOUT occurrences inside wizard catch bodies — state assignments AND telemetry
  payloads — held by a derived-population guard with an ≥8-of-11 floor.
- SC-RA-1, SC-RA-2, SC-RA-3, SC-SU-1, SC-B02-2 all Observed with pasted evidence.
</success_criteria>

<output>
Create `.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-03-SUMMARY.md`.
`## OPEN` must name: (a) CONTRACT_GUARDS registration of `seam-transport-attribution.invariant.test.ts`
owed by 140.5-08; (b) the CSV surface's Retry-After arrival owed by 140.5-05 (this plan deliberately
does not touch CSV files); (c) `composite/members` limiter absence recorded. Do NOT touch STATE.md,
ROADMAP.md, DESIGN.md.
</output>
