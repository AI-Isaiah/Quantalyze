---
phase: 140.5-seamprose
plan: 07
type: execute
wave: 3
depends_on: ["140.5-02", "140.5-03"]
files_modified:
  - src/lib/tenant-claim.ts
  - src/lib/tenant-claim.test.ts
  - src/lib/seam-discriminator.ts
  - src/lib/resilient-fetch.ts
  - src/lib/process-key-client.ts
  - src/lib/analytics-client.ts
autonomous: true
requirements: [SEAMPROSE-05, SEAMPROSE-01]
must_haves:
  truths:
    - "mintTenantClaim cannot be called with its two same-typed strings transposed without a compile error (latent type hazard closed at the signature)"
    - "The SeamBreakerVerdict invariant ('breakerKey non-null exactly when counts is true') lives in the TYPE, not in prose at :125 — invalid states unrepresentable, AND the deleted silent fail-open is pinned by a runtime regression test that can fail (SC-TYPE-2)"
    - "resilient-fetch's breaker-evidence table cites symbols, not coordinates drifted +21/+72 (4 of its 5 rows are provably wrong today, repeated at three sites)"
    - "probe_error stays optional — the schema's own reasoned docblock beats the stale scope item (locked D-6d.2: correct the claim, not the schema)"
  artifacts:
    - path: "src/lib/tenant-claim.ts"
      provides: "TenantIdentity-shaped mint signature (the :88 type, currently defined and unused, applied)"
    - path: "src/lib/seam-discriminator.ts"
      provides: "SeamBreakerVerdict as a discriminated union"
  key_links:
    - from: "src/lib/process-key-client.ts + src/lib/analytics-client.ts"
      to: "src/lib/tenant-claim.ts"
      via: "the two mint call sites (:339 / :321), updated in the same commit as the signature"
---

<objective>
Close the WP-13 type invariants CONTEXT §7.5 keeps in scope, honour the locked probe_error decision,
and fix the resilient-fetch evidence table this plan's files own.

Coverage-law row, stated per CONTEXT §2: type-level fixes are **row 1 by construction** — the
compiler enforces them at every present and future call site without an edit. The evidence-table
citation fix is **row 3 (per-site prose)**, partial by construction in those words; the seam-surface
class guard arrives in 140.5-08.

Scope notes locked upstream: `mintTenantClaim` is a LATENT type hazard, NOT a live attacker path —
both mint sites pass server-derived values (RESEARCH §11) — plan at that severity, no security
theater. `probe_error` requires NO schema change (CONTEXT §6d.2: "the §7.5 scope item demanding
z.boolean() is itself a false claim — correct the claim, not the schema").

Purpose: SEAMPROSE-05 (WP-13) + the resilient-fetch slice of SEAMPROSE-01.
Output: two type invariants compiled-in; truthful evidence table; probe_error decision recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-CONTEXT.md   (§6d.2, §7.5)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-RESEARCH.md   (§3.8, §11)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-PATTERNS.md   (§1 rows for these artefacts — PostProcessKeyArgs / PostProcessKeyResult analogs)
@src/lib/tenant-claim.ts
@src/lib/seam-discriminator.ts
@src/lib/resilient-fetch.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mintTenantClaim — apply the TenantIdentity type that already exists</name>
  <files>src/lib/tenant-claim.ts, src/lib/tenant-claim.test.ts, src/lib/process-key-client.ts, src/lib/analytics-client.ts</files>
  <behavior>
    - The mint signature no longer accepts two adjacent bare strings: it takes the `TenantIdentity`
      shape defined (and currently unapplied) at `tenant-claim.ts:88`, following the
      `PostProcessKeyArgs` named-object analog (`process-key-client.ts:247-280` — PATTERNS §1's
      role-match).
    - Claims minted before and after are byte-identical for identical inputs (a
      compatibility test pins one known input → known HMAC output as literals — the refactor must
      be shape-only; a changed claim would invalidate live verification).
  </behavior>
  <action>
    ⚠️ PRE-FLIGHT (fence protection): `grep -rn 'mintTenantClaim' src/ tests/ --include='*.ts' --include='*.tsx'`
    and enumerate every reference. Expected callers: `process-key-client.ts:339`,
    `analytics-client.ts:321`, plus tenant-claim's own tests. If `analytics-client.test.ts` (owned
    by 140.5-06 THIS WAVE) or any other same-wave-owned file references it, STOP and report to the
    orchestrator instead of editing across the fence — that is the worktree-merge conflict this
    plan's file list exists to prevent. Otherwise: apply the signature, update both call sites in
    the same commit, keep V6-adjacent code untouched (`secrets.compare_digest`/KEK — do-not-touch
    per RESEARCH security domain).
  </action>
  <verify>
    <automated>npx tsc --noEmit exit 0; the compatibility pin green; FULL `npx vitest run`. Ledger note: a transposed-arguments call is now a COMPILE error — the runtime-mutation rule is inadmissible for a type fix (tsc must exit 0 under any admissible mutation); record SC-TYPE-1 as compile-receipt with this reason</automated>
  </verify>
  <done>Signature typed, both call sites updated, claims byte-identical, pre-flight enumeration pasted in SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SeamBreakerVerdict — discriminated union, fail-open deleted AND pinned</name>
  <files>src/lib/seam-discriminator.ts, src/lib/resilient-fetch.ts</files>
  <behavior>
    - `SeamBreakerVerdict` (`seam-discriminator.ts:126-135`, flat interface) becomes a two-member
      union on `counts`: `{ counts: true; breakerKey: string; … } | { counts: false; breakerKey: null; … }`
      — the invariant currently stated only in prose at `:125`. Analog: `PostProcessKeyResult`
      (`process-key-client.ts:282-284`).
    - WP-13's payoff realized, not just declared: the redundant null-tests in consumers become
      dead under the narrowed type and are DELETED (WP-13 counts three plus one silent fail-open —
      re-derive the actual sites from the compiler, do not trust the count: run tsc after narrowing
      and let it name them; publish the found list in the SUMMARY beside WP-13's claim).
    - The silent fail-open (a path that proceeded when `breakerKey` was unexpectedly null) is
      replaced by exhaustive narrowing — AND the corrected behaviour gets a RUNTIME regression
      test (SC-TYPE-2's subject): the compile receipt alone cannot prove the runtime consequence
      of deleting the fail-open (checker B-5).
  </behavior>
  <action>
    Implement the union; update `seamBreakerVerdict`'s construction sites; chase the compiler
    through consumers (expected: `resilient-fetch.ts`; enumerate whatever tsc actually names).
    Write the fail-open regression test against the consumer's observable behaviour (what happens
    on the `counts:false` path — before: silently proceeded as countable; after: the narrowed
    branch's explicit behaviour). If deleting the fail-open CHANGES observable behaviour, that is
    a finding — report it in the SUMMARY, never silently.
    ⚠️ `seam-discriminator.purity.test.ts` is NOT in this plan's files (140.5-01 touched it in
    wave 1): the union adds a TYPE export, not a function — `EXPECTED_EXPORTS` (five functions)
    must not change. If the purity guard reds, the change leaked a runtime export — fix the change,
    never the guard.
    Prose in owned file: `seam-discriminator.ts:137-139` still says "the exported surface stays the
    three predicates" — FIVE functions exist and the guard's own EXPECTED_EXPORTS lists five
    (RESEARCH §3.5: "the file lies about itself in its own body"). Reword to reference
    EXPECTED_EXPORTS with no bare integer. ⚠️ Do NOT touch `:242`'s "disjoint by CONTRACT" — a
    different pair (breaker keys vs venue names), correct and correctly sourced.
  </action>
  <verify>
    <automated>npx tsc --noEmit exit 0 (the union's compile receipt — SC-TYPE-1). SC-TYPE-2 (planner-added row): re-introduce the fail-open (a production edit tolerating the null-key path as countable — write it tsc-clean, e.g. a defensive `?? ""` branch) → the new runtime regression test reds; paste, revert, porcelain clean. FULL `npx vitest run`</automated>
  </verify>
  <done>Union landed; compiler-named redundant checks deleted with the list published; fail-open pinned at runtime (SC-TYPE-2 observed); :137 truthful; purity guard untouched and green.</done>
</task>

<task type="auto">
  <name>Task 3: resilient-fetch evidence table + probe_error decision record</name>
  <files>src/lib/resilient-fetch.ts</files>
  <action>
    The breaker-key evidence table (`resilient-fetch.ts:355-370`, REPEATED at `:396` and `:407`)
    cites `routers/exchange.py` coordinates of which 4 of 5 are wrong — three by +21, one by +72
    (RESEARCH §3.8's per-row table: cited :314,:324/:626/:276,:287/:547 → real :335,:345/:647/
    :297,:308/:619-620; the one correct row is correct by luck). ⚠️ A single global offset would be
    WRONG (mixed +21/+72) — that asymmetry is the argument for symbols: re-derive each row from
    `routers/exchange.py` at HEAD and anchor to function/branch symbols
    (`_validate_mt5_key`, `validate_key`'s return, the VenueTransientHTTPException raise), not
    fresh line numbers. Fix all THREE occurrences — the same wrong coordinates appear twice more in
    the budget entries; fixing one of three is the 5-of-7 shape.
    `:761`'s `dist/index.mjs:757-761` cites a path inside `@upstash/ratelimit` — LEGITIMATE
    out-of-repo evidence (RESEARCH §3.1 bug 2); annotate it as an out-of-repo reference in the
    form 140.5-08's guard will allowlist (coordinate with 08's `## OPEN` hand-off: this file is
    the allowlist's one expected member).
    Convert any remaining census-reported bare citations in this file
    (`MODE=full node .planning/phases/140.5-*/140.5-citation-census.mjs src/lib/resilient-fetch.ts`).
    probe_error: NO code change (locked D-6d.2). `analytics-schemas.ts:354-357`'s reasoning stands
    as-is and is not edited. Record the decision + rationale in this plan's SUMMARY so the
    REQUIREMENTS trace shows the §7.5 item RESOLVED-BY-DECISION, not dropped (the founder-flag
    RESEARCH raised — A4 — is answered by CONTEXT §6d.2, which post-dates the schema docblock and
    endorses it).
  </action>
  <verify>
    <automated>Census MODE=full on src/lib/resilient-fetch.ts → zero bare in-repo `file:line` citations remain (out-of-repo reference annotated, not converted); FULL `npx vitest run` + `npx tsc --noEmit` + `npm run lint` + Node 22 parity (wave-merge gate)</automated>
  </verify>
  <done>All three table occurrences symbol-anchored; out-of-repo reference annotated for 08's allowlist; probe_error decision recorded with the D-6d.2 trace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant claim mint → verify | HMAC claim crosses the Next↔Python hop; transposition = wrong-tenant claim (latent, server-derived inputs today) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.5-16 | Spoofing | mintTenantClaim | mitigate | TenantIdentity signature — transposition becomes a compile error; claim bytes pinned unchanged |
| T-140.5-17 | Elevation | SeamBreakerVerdict fail-open | mitigate | union narrowing deletes the silent proceed-on-null path; runtime regression test pins the corrected behaviour |
| T-140.5-18 | Tampering | V6 crypto adjacency | accept | compare_digest/KEK untouched by fence; no crypto code edited |
</threat_model>

<verification>
Full suite + tsc + lint after each task commit; Node 22 parity at wave merge. SC-TYPE-1 is
COMPILE-TIME by nature (recorded as such — the runtime-mutation rule's tsc-must-pass clause makes
runtime mutations inadmissible for it); SC-TYPE-2 is the RUNTIME half and is a real mutation with
pasted evidence — the pair together is the honest recording.
</verification>

<success_criteria>
- Transposed mint args and null-key-while-counting are compile errors (SC-TYPE-1, ledger-noted).
- The deleted fail-open is pinned by a runtime test that reds under re-introduction (SC-TYPE-2
  observed).
- Compiler-derived list of deleted redundant checks published beside WP-13's claimed count.
- Evidence table truthful at all three occurrences; single-offset correction explicitly rejected.
- probe_error resolved-by-decision with the D-6d.2 trace; schema untouched.
</success_criteria>

<output>
Create `.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-07-SUMMARY.md`.
`## OPEN` must name: (a) resilient-fetch.ts's out-of-repo `@upstash/ratelimit` reference as the
expected sole allowlist member for 140.5-08's citation guard; (b) `AnalyticsUpstreamError`'s four
positional params — OBSERVED, grown-by-one since the synthesis (RESEARCH §11), NOT in CONTEXT §7.5's
fence, deliberately unplanned — named so it is a decision, not an omission.
Do NOT touch STATE.md, ROADMAP.md, DESIGN.md, analytics-schemas.ts.
</output>
