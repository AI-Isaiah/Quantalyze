---
phase: 140.5-seamprose
plan: 06
type: execute
wave: 3
depends_on: ["140.5-01", "140.5-02"]
files_modified:
  - src/app/api/keys/[id]/permissions/route.test.ts
  - src/components/portfolio/PortfolioImpactPanel.test.tsx
  - src/lib/analytics-client.test.ts
  - src/app/api/admin/match/eval/route.test.ts
  - src/app/api/admin/match/recompute/route.test.ts
  - src/app/api/admin/match/recompute/route.seam.test.ts
  - src/components/admin/MatchEvalDashboard.test.tsx
  - src/app/api/scenario/optimize/route.test.ts
  - src/app/api/keys/validate-and-encrypt/route.test.ts
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/api/strategies/composite/add-key/route.test.ts
  - src/app/api/verify-strategy/route.test.ts
  - src/app/api/keys/sync/route.test.ts
  - src/app/api/allocator/scenario/commit/route.test.ts
  - src/__tests__/audit-coverage.test.ts
  - e2e/api-key-flow.spec.ts
  - e2e/sync-flow-queue.spec.ts
  - e2e/wizard-sync-regression.spec.ts
  - src/__tests__/contracts/spec-disabling.invariant.test.ts
autonomous: true
requirements: [SEAMPROSE-06, SEAMPROSE-04]
must_haves:
  truths:
    - "No test fixture asserts a body production refuses to construct (the 500+retryable:true body — REFUSED by the real constructor, R-1) — and R-1 itself is pinned by a Python test that reds if it is weakened"
    - "The 429 fixtures use the shapes production actually emits — including the NESTED shape the route's own throttle emits, which no test in the repo exercises at 429 today"
    - "424 is tested where it CAN arrive; where it cannot, the suites' real forwarding assertions survive under an arriving status (re-homed, not deleted)"
    - "A disabled test (skip/only/fixme/todo) anywhere in the spec population reddens a guard — comment-stripped, with all six formerly-unconditional skips explicitly dispositioned per-site"
  artifacts:
    - path: "src/__tests__/contracts/spec-disabling.invariant.test.ts"
      provides: "row-1 replacement for ci.yml:1633's single-file, comment-blind bash grep"
  key_links:
    - from: "fixtures in the three 500-suites"
      to: "analytics-service/services/error_contract.py _validate (R-1)"
      via: "fixture literals matching what the executed constructor actually returns"
---

<objective>
Make the tests standing in for the contract tell the truth (WP-14), and replace the skip-regex's
row-3 bash grep with a comment-stripped, derived-population guard (the §7.4 harness item).

Coverage-law rows, stated per CONTEXT §2: fixture corrections are **per-site — row 3, partial by
construction in those words** (corrections of specific measured lies; enumerations exhaustive at
`4c60518b` by published predicate). The spec-disabling guard is **row 1** (population derived from
disk every run). The 424 re-home is the JUDGEMENT call CONTEXT §6g.3 left to planning — decision:
RE-HOME, do not delete (RESEARCH pitfall 7: those suites also assert real forwarding behaviour).

⛔ CHECKER B-3 — the guard's population contains SIX genuine unconditional skips, not one. Every
one is named below with a per-site decision made HERE, not left to the executor: five convert to
env-conditional skips or stay allowlisted; the pin is set to the resulting number. A silent
allowlist widening would convert a row-1 guard to row-2 — that exit is closed by freezing the
allowlist at the planner-decided length.

Purpose: SEAMPROSE-06 (WP-14) + the ci.yml half of SEAMPROSE-04 (WP-12).
Output: fixtures production can construct; 424 coverage where it arrives; spec-disabling guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-CONTEXT.md   (§6b: SEVEN not six; §6f; §6g.3)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-RESEARCH.md   (§8.3, §9 whole section)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-PATTERNS.md   (§7 — why the bash grep is the WRONG analog; the vacuity-fence pattern to copy)
@analytics-service/services/error_contract.py
@analytics-service/routers/internal.py
@analytics-service/main.py
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 429 shapes (SEVEN fixtures) + the unconstructable 500 body (three suites) + the R-1 pin</name>
  <files>src/app/api/keys/[id]/permissions/route.test.ts, src/components/portfolio/PortfolioImpactPanel.test.tsx, src/lib/analytics-client.test.ts</files>
  <action>
    429 half — predicate published (CONTEXT §6b: SEVEN, not the six the scope line says):
    `grep -n 'STATE.upstreamStatus = 429' 'src/app/api/keys/[id]/permissions/route.test.ts'` →
    lines 723, 777, 813, 840, 870, 888, 1280 at `4c60518b`. Production emits THREE 429 shapes
    (RESEARCH §9.1, named by `internal.py:238-243` itself): flat app-global (`main.py:516-530`),
    nested `service_error` (`internal.py:246-250` — this route's OWN per-key throttle),
    bare-scalar detail. All seven fixtures use the FLAT shape and their literals diverge from
    `main.py`'s. Fix: (a) convert the fixtures modelling the route's own throttle to the NESTED
    shape; keep genuinely-app-global cases flat with `main.py`'s REAL literals (read
    `main.py:516-530`, copy them; decide per fixture from what each test claims to model — the
    file's own `:720-722` note admits the nested gap); (b) add the missing contract case: the
    NESTED shape AT STATUS 429 ("Contract A" covers the nested body only at 500 — `:681-684`).
    500 half — the three suites carrying the byte-identical `retryable: true` fixture (predicate +
    list in RESEARCH §9.2: `permissions/route.test.ts:682`, `PortfolioImpactPanel.test.tsx:1488`,
    `analytics-client.test.ts:1734`). The REAL constructor REFUSES it (R-1, proven by execution).
    Fix all three to `retryable: false` with a `detail` a real raise site passes (the executed
    real body is quoted in RESEARCH §9.2). Where a downstream assertion depended on the impossible
    body, re-derive actual production behaviour and assert that; report any behavioural hole in
    the SUMMARY, never paper over it.
    R-1 pin (checker B-5, the 500-half ledger row): locate the Python test pinning R-1
    (`_validate` refusing retryable=True at 500) in the analytics suite; if none exists, ADD one
    beside `error_contract.py`'s existing tests — it is the oracle these three fixtures now lean
    on, and it must be able to fail.
    Prose hygiene in owned file: "sixteen" at `analytics-client.test.ts:1622` and `:1849` →
    delete the integer, name the predicate (RESEARCH §3.10). ⚠️ FENCE: do not touch
    `analytics-client.ts` (140.5-07 owns it this wave).
  </action>
  <verify>
    <automated>Oracle re-run: `cd analytics-service && python3 -c "from services.error_contract import service_error; service_error(500,'SEAM_DEGRADED',dependency='supabase',retryable=False)"` succeeds and matches the fixture literals. SC-429-1 (production-side, a fixture flip is a test edit and inadmissible): make `internal.py`'s throttle emit the flat shape → the new nested-at-429 contract case reds. SC-R1-1: weaken R-1 in `error_contract.py` (permit retryable=True at 500) → the Python R-1 pin reds. ⚠️ W10: BOTH mutations edit production source OUTSIDE this plan's fence BY DESIGN; under use_worktrees the revert-before-commit discipline is the only protection — run with no other uncommitted work, capture, revert, porcelain clean, then `python3 -m pytest -q` to prove baseline restored. FULL `npx vitest run`</automated>
  </verify>
  <done>Zero `retryable: true` within a 500 fixture in src/+tests/ (RESEARCH §9.2 proximity predicate re-run and pasted); nested-shape-at-429 exercised; SC-429-1 + SC-R1-1 observed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 424 — re-home the four cannot-arrive suites, add coverage where it CAN arrive</name>
  <files>src/app/api/admin/match/eval/route.test.ts, src/app/api/admin/match/recompute/route.test.ts, src/app/api/admin/match/recompute/route.seam.test.ts, src/components/admin/MatchEvalDashboard.test.tsx, src/app/api/scenario/optimize/route.test.ts, src/app/api/keys/validate-and-encrypt/route.test.ts, src/app/api/strategies/create-with-key/route.test.ts, src/app/api/strategies/composite/add-key/route.test.ts, src/app/api/verify-strategy/route.test.ts, src/app/api/keys/sync/route.test.ts</files>
  <behavior>
    Producer/consumer populations measured on both sides (RESEARCH §9.3): 424 is raised ONLY by
    `routers/exchange.py` (validate_key + its two private helpers), `portfolio.py:2355`
    (verify-strategy) and `process_key.py:1602`; NO 424 in match.py/simulator.py/optimizer.py.
    - Five cannot-arrive suites (4 match/optimize + MatchEvalDashboard): each 424 test keeps its
      REAL assertion — an upstream error status survives forwarding — but the fixture status
      becomes one the upstream CAN emit (the bare-scalar `{"detail": str}` shape those routers
      produce). A comment states the re-home + the producer-audit predicate.
    - Five can-arrive-but-untested route suites (validate-and-encrypt, create-with-key,
      composite/add-key, verify-strategy, keys/sync — all measured ZERO 424 mentions): one new
      case each with the upstream's real nested `VenueTransientHTTPException` 424 body (shape from
      `exchange.py:619-624`) → assert the route's actual contract.
  </behavior>
  <action>
    Implement per the behavior block. All five additions in one task — covering 3 of 5 arrival
    routes would be the 5-of-7 shape again; say so in the commit message.
  </action>
  <verify>
    <automated>SC-424-2: production mutation — make keys/sync (the SECOND-most-obvious route) collapse the forwarded upstream status to 500 → its new 424 case reds. Paste, revert. FULL `npx vitest run`</automated>
  </verify>
  <done>Five new arrival cases green, five re-homed suites green with re-home comments; SC-424-2 observed. (A `grep -c '424'` is not the receipt.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The spec-disabling guard (row 1) — six named skips dispositioned, ci.yml:1633 stays narrow</name>
  <files>src/__tests__/contracts/spec-disabling.invariant.test.ts, src/app/api/allocator/scenario/commit/route.test.ts, src/__tests__/audit-coverage.test.ts, e2e/api-key-flow.spec.ts, e2e/sync-flow-queue.spec.ts, e2e/wizard-sync-regression.spec.ts, src/app/api/admin/match/recompute/route.seam.test.ts</files>
  <behavior>
    - Population derived from disk each run: `e2e/**/*.spec.ts` + `src/**/*.test.ts{,x}` +
      `tests/**/*.test.ts{,x}`. Vacuity floor ≥400 files (measured 740 vitest files + the e2e
      specs at baseline; predicate stated in the floor's failure message — re-measure at edit time).
    - Needles, on COMMENT-STRIPPED source (source-scan from 140.5-01): the ci.yml:1633 set PLUS
      the measured misses `it.skip(`, `it.only(`, `test.todo(`, `describe.todo(`, `xit(`,
      `xdescribe(`, `test.describe.skip(` — leaving `skipIf(` and CONDITIONAL `test.skip(<expr>,…)`
      alone (the needle targets the literal-`true`/empty-arg forms). ⛔ W7: `test.slow(` is NOT a
      needle — it triples a timeout, it does not disable; including it was a category error.
    - Both-polarity needle self-tests incl. the DEF-16-2 specimen: a COMMENT containing
      `test.skip(true)` must NOT trip (that exact hit — `ResponsiveChartFrame.test.tsx:6` —
      reddens the widened bash grep on a clean tree TODAY; quote it in the docblock as the reason
      the mechanism is a vitest guard, not bash).
    - ⛔ THE SIX UNCONDITIONAL SKIPS IN THE POPULATION, dispositioned per-site (checker B-3 —
      these are LIVE CODE, comment-strip does not help, and every one is decided HERE):
      1. `e2e/api-key-flow.spec.ts:74` ("Requires authenticated session with a valid strategy ID")
         → CONVERT to `test.skip(!process.env.PLAYWRIGHT_TEST_STRATEGY_ID, <same reason>)` —
         CI-behavior-preserving (the env var is absent in CI, so exactly the same tests skip;
         locally it becomes opt-in-runnable, which is what its reason string already instructs).
      2. `e2e/api-key-flow.spec.ts:178` (names `PLAYWRIGHT_TEST_EXCHANGE_KEY/SECRET` itself)
         → CONVERT to `test.skip(!process.env.PLAYWRIGHT_TEST_EXCHANGE_KEY || !process.env.PLAYWRIGHT_TEST_EXCHANGE_SECRET, <same reason>)`.
      3. `e2e/sync-flow-queue.spec.ts:43` (names `PLAYWRIGHT_TEST_STRATEGY_ID`)
         → CONVERT to `test.skip(!process.env.PLAYWRIGHT_TEST_STRATEGY_ID, <same reason>)`.
      4. `e2e/wizard-sync-regression.spec.ts:18` (names `PLAYWRIGHT_TEST_STRATEGY_ID`)
         → CONVERT identically.
      5. `e2e/strategy-v2-keyboard.spec.ts:53` → ALLOWLIST, do not convert: a TODO-rewrite skip
         (PR #108 deferred follow-ups) whose class of regression is held by
         `tests/visual/chart-accessibility-layer.test.ts` per its own comment; un-skipping a spec
         known to need a locator rewrite would redden CI on a non-defect. File NOT edited.
      6. `src/__tests__/audit-coverage.test.ts:977` ("H-0001 (intended behavior)") → ALLOWLIST:
         a documentation skip, deliberate.
      Plus: `src/app/api/allocator/scenario/commit/route.test.ts:661` ("[live-DB] … requires
      seeded Supabase") → CONVERT to the repo's live-DB `skipIf` convention (read how sibling
      live-DB suites gate; reuse their exact condition, do not invent a new env flag).
    - Exemption ledger: hand-typed allowlist FROZEN at exactly TWO entries
      (`e2e/strategy-v2-keyboard.spec.ts`, `src/__tests__/audit-coverage.test.ts`), each with its
      reason inline, length pinned `.toBe(2)` (the SEAM_ALLOWLIST_EXEMPT idiom — a row-2 list run
      the right way: its length is a DECISION). Failure message forbids widening without a reason
      row and names this plan as the decision record.
  </behavior>
  <action>
    First perform the five conversions per the disposition table (each preserves the original
    reason string; each is CI-behavior-preserving because the gating env vars are absent in CI).
    Then build the guard per the behavior block. `ci.yml:1633` itself: LEAVE UNCHANGED, and
    document why in the guard docblock — the line-based single-file bash grep is green and
    harmless where it sits; widening IT reddens a clean tree on a comment (measured, RESEARCH
    §8.3) and bash cannot comment-strip maintainably. The guard subsumes its intent at row 1.
    Branch protection is OFF — write "would have caught", never "did stop" (CONTEXT §9).
    Prose hygiene in owned file: "sixteen" at `admin/match/recompute/route.seam.test.ts:16` →
    predicate wording.
  </action>
  <verify>
    <automated>SC-SPEC-1: the guard's both-polarity needle self-tests ARE the ledger evidence — the production-mutation rule cannot map to a guard whose SUBJECT is test files (any tripping edit is definitionally a test edit; recorded with this reason per the VALIDATION Wave-0 sanction). Paste the self-test assertions AND the guard's green run over the real population (proving the six dispositions actually cleared it). FULL `npx vitest run` + Node 22 parity + `npm run lint` (wave-merge gate)</automated>
  </verify>
  <done>Guard green on the clean tree with non-vacuous floors; five conversions landed CI-behavior-preserving; allowlist frozen at 2 with reasons; SC-SPEC-1 recorded with the sanctioned-substitute note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures → contract confidence | a fixture production cannot construct makes the suite vouch for impossible behaviour |
| spec files → CI verdict | a quietly-disabled spec ships a hole while CI reads green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.5-14 | Repudiation | 429/500 fixtures | mitigate | fixture literals pinned to the EXECUTED constructor's output; R-1 pinned by its own Python test |
| T-140.5-15 | Repudiation | spec-disabling guard | mitigate | comment-strip + vacuity floor + allowlist frozen at 2 with per-entry reasons |
</threat_model>

<verification>
Full `npx vitest run` + `pytest -q` after each task commit (Task 1's mutations touch Python —
prove baseline restored). Node 22 parity + lint + tsc at wave merge. Never a scoped run as a
commit receipt.
</verification>

<success_criteria>
- No suite asserts the R-1-refused body; R-1 itself pinned in Python; nested-shape-at-429
  exercised; the SEVEN-fixture predicate published wherever the count appears.
- 424 arrival coverage at all five can-arrive routes; forwarding coverage preserved in all five
  re-homed suites (the §6g.3 judgement made and stated).
- Spec-disabling guard green with all six unconditional skips dispositioned per the planner's
  table; allowlist pinned at 2; `test.slow(` excluded as a non-disabling token.
- SC-429-1, SC-R1-1, SC-424-2, SC-SPEC-1 recorded (with mechanism notes where adjusted).
</success_criteria>

<output>
Create `.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-06-SUMMARY.md`.
`## OPEN` must name: (a) CONTRACT_GUARDS registration of `spec-disabling.invariant.test.ts` owed
by 140.5-08; (b) any behavioural hole exposed by correcting the impossible 500 fixture (if none,
say none found); (c) `ci.yml:1633` deliberately left narrow, with the guard as the class
mechanism; (d) `e2e/strategy-v2-keyboard.spec.ts`'s allowlisted skip and its PR #108 follow-up.
Do NOT touch STATE.md, ROADMAP.md, DESIGN.md, and do not edit ci.yml or
e2e/strategy-v2-keyboard.spec.ts.
</output>
