---
phase: 140.5-seamprose
plan: 06
type: execute
wave: 3
depends_on: ["140.5-01", "140.5-02"]
files_modified:
  - src/app/api/keys/[id]/permissions/route.test.ts
  - src/components/portfolio/PortfolioImpactPanel.test.tsx
  - src/lib/analytics-client.test.ts
  - src/app/api/admin/match/eval/route.test.ts
  - src/app/api/admin/match/recompute/route.test.ts
  - src/app/api/admin/match/recompute/route.seam.test.ts
  - src/components/admin/MatchEvalDashboard.test.tsx
  - src/app/api/scenario/optimize/route.test.ts
  - src/app/api/keys/validate-and-encrypt/route.test.ts
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/api/strategies/composite/add-key/route.test.ts
  - src/app/api/verify-strategy/route.test.ts
  - src/app/api/keys/sync/route.test.ts
  - src/app/api/allocator/scenario/commit/route.test.ts
  - src/__tests__/audit-coverage.test.ts
  - src/__tests__/contracts/spec-disabling.invariant.test.ts
autonomous: true
requirements: [SEAMPROSE-06, SEAMPROSE-04]
must_haves:
  truths:
    - "No test fixture asserts a body production refuses to construct (the 500+retryable:true body — REFUSED by the real constructor, R-1)"
    - "The 429 fixtures use the shapes production actually emits — including the NESTED shape the route's own throttle emits, which no test in the repo exercises at 429 today"
    - "424 is tested where it CAN arrive; where it cannot, the suites' real forwarding assertions survive under an arriving status (re-homed, not deleted)"
    - "A disabled test (skip/only/fixme/todo) anywhere in the spec population reddens a guard — comment-stripped, so prose cannot trip or defeat it"
  artifacts:
    - path: "src/__tests__/contracts/spec-disabling.invariant.test.ts"
      provides: "row-1 replacement for ci.yml:1633's single-file, comment-blind bash grep"
  key_links:
    - from: "fixtures in the three 500-suites"
      to: "analytics-service/services/error_contract.py _validate (R-1)"
      via: "fixture literals matching what the executed constructor actually returns"
---

<objective>
Make the tests standing in for the contract tell the truth (WP-14), and replace the skip-regex's
row-3 bash grep with a comment-stripped, derived-population guard (the §7.4 harness item).

Coverage-law rows, stated per CONTEXT §2: fixture corrections are **per-site — row 3, partial by
construction in those words** (they are corrections of specific measured lies; the enumerations
below are exhaustive at `4c60518b` by published predicate). The spec-disabling guard is **row 1**
(population derived from disk every run). The 424 re-home is a JUDGEMENT call CONTEXT §6g.3
explicitly left to planning — the decision, made here: RE-HOME, do not delete (RESEARCH pitfall 7:
those suites also assert real forwarding behaviour; deleting them silently removes it).

Purpose: SEAMPROSE-06 (WP-14) + the ci.yml half of SEAMPROSE-04 (WP-12).
Output: fixtures production can construct; 424 coverage where it arrives; spec-disabling guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-CONTEXT.md   (§6b: SEVEN not six; §6f; §6g.3)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-RESEARCH.md   (§8.3, §9 whole section)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-PATTERNS.md   (§7 — why the bash grep is the WRONG analog; the vacuity-fence pattern to copy)
@analytics-service/services/error_contract.py
@analytics-service/routers/internal.py
@analytics-service/main.py
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: 429 shapes (SEVEN fixtures) + the unconstructable 500 body (three suites)</name>
  <files>src/app/api/keys/[id]/permissions/route.test.ts, src/components/portfolio/PortfolioImpactPanel.test.tsx, src/lib/analytics-client.test.ts</files>
  <action>
    429 half — predicate published (CONTEXT §6b: SEVEN, not the six the scope line says):
    `grep -n 'STATE.upstreamStatus = 429' 'src/app/api/keys/[id]/permissions/route.test.ts'` →
    lines 723, 777, 813, 840, 870, 888, 1280 at `4c60518b`. Production emits THREE 429 shapes
    (RESEARCH §9.1, named by `internal.py:238-243` itself): flat app-global (`main.py:516-530`),
    nested `service_error` (`internal.py:246-250` — this route's OWN per-key throttle), bare-scalar
    detail. All seven fixtures use the FLAT shape, and their literals don't even match `main.py`'s
    (`human_message` and `detail` diverge — table in RESEARCH §9.1). Fix: (a) convert the fixtures
    that model the route's own throttle to the NESTED shape; keep genuinely-app-global cases flat
    with `main.py`'s REAL literals (read `main.py:516-530` and copy them; decide per fixture from
    what each test claims to model — the test file's own `:720-722` note admits the nested gap);
    (b) add the missing contract case: the NESTED shape AT STATUS 429 (the file's "Contract A"
    covers the nested body only at 500 — `:681-684`); assert it classifies/propagates correctly,
    naming the fixture in the assertion message.
    500 half — the three suites carrying the byte-identical `retryable: true` fixture
    (predicate + list in RESEARCH §9.2: `permissions/route.test.ts:682`,
    `PortfolioImpactPanel.test.tsx:1488`, `analytics-client.test.ts:1734`). The REAL constructor
    REFUSES it: `service_error(500,…,retryable=True)` → "REFUSED: a 500 is SERVICE-PERMANENT and
    must not be retryable (R-1)" — proven by execution. Fix all three to `retryable: false` with a
    `detail` a real raise site passes (the executed real body is quoted in RESEARCH §9.2). Where a
    downstream assertion depended on `retryable: true` at 500, the ASSERTION was encoding the
    impossible body — re-derive what production behaviour actually is and assert that; if any
    behavioural hole opens (something only worked because the fixture was impossible), report it in
    the SUMMARY rather than papering over it.
    Prose hygiene in owned file: "sixteen" at `analytics-client.test.ts:1622` and `:1849` → delete
    the integer, name the predicate (RESEARCH §3.10). ⚠️ FENCE: do not touch `analytics-client.ts`
    (140.5-07 owns it this wave).
  </action>
  <verify>
    <automated>Re-run the Python constructor as the oracle: `cd analytics-service && python3 -c "from services.error_contract import service_error; service_error(500,'SEAM_DEGRADED',dependency='supabase',retryable=False)"` succeeds and its output matches the fixture literals. SC-429-1 mutation: flip ONE nested fixture back to flat (a test-side seed for the ledger is inadmissible as a production mutation — instead run the PRODUCTION mutation: make `internal.py`'s throttle emit the flat shape → the new nested-429 contract case reds; tsc unaffected, pytest baseline re-proven after revert). Paste, revert, porcelain clean. FULL `npx vitest run` + `cd analytics-service && python3 -m pytest -q`</automated>
  </verify>
  <done>Zero `retryable: true` within a 500 fixture in src/+tests/ (predicate: the RESEARCH §9.2 proximity scan, re-run and pasted); nested-shape-at-429 exercised; SC-429-1 observed with the production-side mutation note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 424 — re-home the four cannot-arrive suites, add coverage where it CAN arrive</name>
  <files>src/app/api/admin/match/eval/route.test.ts, src/app/api/admin/match/recompute/route.test.ts, src/app/api/admin/match/recompute/route.seam.test.ts, src/components/admin/MatchEvalDashboard.test.tsx, src/app/api/scenario/optimize/route.test.ts, src/app/api/keys/validate-and-encrypt/route.test.ts, src/app/api/strategies/create-with-key/route.test.ts, src/app/api/strategies/composite/add-key/route.test.ts, src/app/api/verify-strategy/route.test.ts, src/app/api/keys/sync/route.test.ts</files>
  <behavior>
    Producer/consumer populations are measured, both sides (RESEARCH §9.3): 424 is raised ONLY by
    `routers/exchange.py` (validate_key + its two private helpers), `portfolio.py:2355`
    (verify-strategy) and `process_key.py:1602`; NO 424 exists in match.py/simulator.py/optimizer.py.
    - In the five cannot-arrive suites (4 match/optimize + MatchEvalDashboard): each 424 test keeps
      its REAL assertion — an upstream error status survives forwarding with its own status/body —
      but the fixture status becomes one the upstream CAN emit (the bare-scalar `{"detail": str}`
      shape those routers actually produce). A comment states the re-home and the producer audit
      predicate, so the next reader knows 424 was not "lost" but never arrivable.
    - In the five can-arrive-but-untested route suites (validate-and-encrypt, create-with-key,
      composite/add-key, verify-strategy, keys/sync — all measured ZERO 424 mentions): one new case
      each: the upstream's real nested `VenueTransientHTTPException` 424 body (shape from
      `exchange.py:619-624`: verbatim `code`, `recoverable` derived) → assert the route's actual
      contract (classification/forwarding/envelope per that route's existing patterns).
  </behavior>
  <action>
    Implement per the behavior block. This is the judgement call CONTEXT §6g.3 reserved — decision:
    RE-HOME + ADD, never delete (forwarding coverage is real; RESEARCH A3/pitfall 7). All five
    additions in one task because the milestone's signature defect is instance-not-class: covering
    3 of 5 arrival routes would be the 5-of-7 shape again — say so in the commit message.
  </action>
  <verify>
    <automated>SC-424-2 (planner-added row): production mutation — make ONE arrival route swallow the upstream status (e.g. collapse the forwarded status to 500 at its non-2xx arm; pick the SECOND-most-obvious route, keys/sync, not validate-and-encrypt) → that route's new 424 case reds. Paste, revert. FULL `npx vitest run`</automated>
  </verify>
  <done>grep -c '424' is not the receipt — the receipt: five new arrival cases green, five re-homed suites green with re-home comments, SC-424-2 observed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The spec-disabling guard (row 1) — and why ci.yml:1633 stays narrow</name>
  <files>src/__tests__/contracts/spec-disabling.invariant.test.ts, src/app/api/allocator/scenario/commit/route.test.ts, src/__tests__/audit-coverage.test.ts, src/app/api/admin/match/recompute/route.seam.test.ts</files>
  <behavior>
    - Population derived from disk each run: `e2e/**/*.spec.ts` + `src/**/*.test.ts{,x}` +
      `tests/**/*.test.ts{,x}`. Vacuity floor: ≥400 files scanned (measured 740 test files run by
      vitest at baseline; floor ~60% per the seam-log-coverage idiom — re-measure at edit time and
      state the predicate in the floor's failure message).
    - Needles, on COMMENT-STRIPPED source (source-scan from 140.5-01): the ci.yml:1633 set PLUS the
      measured misses `it.skip(`, `it.only(`, `test.todo(`, `describe.todo(`, `xit(`, `xdescribe(`,
      `test.slow(`, `test.describe.skip(` — while leaving `skipIf(` alone (conditional; 244+5
      legitimate uses measured, RESEARCH §8.3).
    - Both-polarity needle self-tests on synthetic strings, including the DEF-16-2 specimen: a
      COMMENT containing `test.skip(true)` must NOT trip (this exact hit —
      `ResponsiveChartFrame.test.tsx:6` — reddens the widened bash grep on a clean tree TODAY;
      quote that in the guard's docblock as the reason the mechanism is a vitest guard, not bash).
    - Exemption ledger: a hand-typed allow-list FROZEN with a length pin (`.toBe(n)` — the
      SEAM_ALLOWLIST_EXEMPT idiom, "the right way to run a row-2 list"), one reasoned entry:
      `audit-coverage.test.ts` (`:977` — "H-0001 (intended behavior)" documentation skip).
  </behavior>
  <action>
    First convert the OTHER real unconditional skip to the repo's own conditional convention:
    `allocator/scenario/commit/route.test.ts:661` ("[live-DB] … requires seeded Supabase") →
    `it.skipIf(<the live-DB availability check used by sibling live-DB suites>)` — read how the
    repo's existing live-DB suites gate (the skipIf convention is established; find it in the
    sibling suites, do not invent a new env flag). Then build the guard per the behavior block.
    `ci.yml:1633` itself: LEAVE UNCHANGED, and document why in the guard docblock — the line-based,
    single-file bash grep is green and harmless where it sits; widening IT reddens a clean tree on
    a comment (measured, RESEARCH §8.3) and bash cannot comment-strip maintainably. The guard
    subsumes its intent at row 1. This satisfies the §7.4 scope item by closing the CLASS the bash
    line was a row-3 instance of; branch protection is OFF, so write "would have caught", never
    "did stop" (CONTEXT §9).
    Prose hygiene in owned file: "sixteen" at `admin/match/recompute/route.seam.test.ts:16` →
    predicate wording.
  </action>
  <verify>
    <automated>SC-SPEC-1 (planner-added row): the guard's own both-polarity needle self-test IS the ledger evidence — the production-source mutation rule cannot map to a guard whose SUBJECT is test files (any tripping edit is definitionally a test edit; recorded in the ledger with this reason per the VALIDATION Wave-0 needle-self-test sanction). Paste the self-test assertions. FULL `npx vitest run` + Node 22 parity + `npm run lint` (wave-merge gate)</automated>
  </verify>
  <done>Guard green on the clean tree with a non-vacuous floor; exemption list frozen at 1 with reason; live-DB skip converted to skipIf; ledger row recorded with the sanctioned-substitute note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures → contract confidence | a fixture production cannot construct makes the suite vouch for impossible behaviour |
| spec files → CI verdict | a quietly-disabled spec ships a hole while CI reads green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.5-14 | Repudiation | 429/500 fixtures | mitigate | fixture literals pinned to the EXECUTED constructor's output, not hand-imagined bodies |
| T-140.5-15 | Repudiation | spec-disabling guard | mitigate | comment-strip + vacuity floor + frozen exemption ledger with length pin |
</threat_model>

<verification>
Full `npx vitest run` + `pytest -q` after each task commit (Task 1's ledger mutation touches
Python — prove baseline restored). Node 22 parity + lint + tsc at wave merge. Never a scoped run
as a commit receipt.
</verification>

<success_criteria>
- No suite asserts the R-1-refused body; nested-shape-at-429 exercised; SEVEN-fixture predicate
  published wherever the count appears.
- 424 arrival coverage at all five can-arrive routes; forwarding coverage preserved in all five
  re-homed suites (the §6g.3 judgement made and stated).
- Spec-disabling guard green, floors non-vacuous, DEF-16-2 specimen in its self-tests.
- SC-429-1, SC-424-2, SC-SPEC-1 recorded (with their mechanism notes where adjusted).
</success_criteria>

<output>
Create `.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-06-SUMMARY.md`.
`## OPEN` must name: (a) CONTRACT_GUARDS registration of `spec-disabling.invariant.test.ts` owed by
140.5-08; (b) any behavioural hole exposed by correcting the impossible 500 fixture (if none, say
none found); (c) `ci.yml:1633` deliberately left narrow, with the guard as the class mechanism.
Do NOT touch STATE.md, ROADMAP.md, DESIGN.md, and do not edit ci.yml.
</output>
