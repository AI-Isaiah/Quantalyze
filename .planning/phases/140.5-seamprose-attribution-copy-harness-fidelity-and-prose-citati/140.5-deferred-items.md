# Phase 140.5 — consolidated deferred items (carry-forward ledger)

The phase's terminal record of every `## OPEN` hand-off. Assembled by 140.5-08
Task 3 from each plan's SUMMARY, so the §6 "plan-to-plan hand-off hole" class is
closed **procedurally** (a single durable ledger) as well as structurally (the
wire-vocabulary guard). Nothing here was fixed by 140.5-08; each item names its
owner-plan and why it is out of scope for this phase.

⚠️ **Two machine-measurement hazards (`DEF-140.5-A`, `DEF-140.5-B`) already live
in `deferred-items.md` in this directory, with full reproduction detail.** They
are NOT restated here — see that file. Both are measurement hazards, not code
defects; the tree is green on a quiet host on both Nodes.

---

## 1. Data / product surface (owner: a future phase)

**1a. The per-row CSV breakdown NEVER renders, on either arm — DATA half open, PII
fence intact.** (140.5-02 (b), 140.5-05 (b); CONTEXT §6e, §7 / QA ISSUE-005.)
Only the COPY half is done (140.5-02 stopped the false "see per-row breakdown"
promise). `csv_adapter.py` emits `debug_context.violations`; the client reads
`pandera_errors` (zero Python hits); and `_envelope_error` discards
`val.debug_context` before the wire. The `'nan'` a user reads (ISSUE-005) comes
through the same hole: `csv_validator.py`'s `row.get('column')` is NaN for
`column_in_dataframe` while the real name sits in a `failure_case` field the PII
rule never echoes. ⚠️ **Fixing the data half forwards raw cell values — the PII
surface.** Admit only if closeable WITHOUT echoing untrusted cell values (CONTEXT
§7 condition). Three things must move together: `_envelope_error` must stop
discarding `debug_context`; the client's `pandera_errors` key must reconcile with
Python's `violations`; and the PII rule must not be weakened.

**1b. `UNSUPPORTED_EXCHANGE` deserves its own wizard member.** (140.5-02 (f).)
Exempted as `UNKNOWN`/500 today on the honest ground that no `KEY_*` copy is true
of it — it is our own configuration gap. A dedicated member is a real
improvement; not this phase's.

**1c. The `503` config-missing arm of `csv-validate` renders "Service
unavailable."** (140.5-05 (f).) Under branch 1 it is a bare heading with no
second line — markedly weaker than the §4a sentence the sibling 502 arm sends. It
is the route's own copy; aligning them is a future plan's.

**1d. KEK "not configured" upstream fault is mis-attributed as "Could not reach
the permissions service".** (140.5-06 (b), pinned as `FINDING (140.5-06)`, observed
NOT endorsed.) `permissions/route.ts`'s `isConfigError` keys on
`rawMessage.includes("not configured")`, which now also matches the UPSTREAM's
missing-KEK prose — so a reached-and-answered operator-actionable gap is reported
to the user as our layer failing. The machine `cause` code is already carried and
would make it exact. Fixing it must REWRITE the pinned case, not delete it.

---

## 2. Coverage-law row-2/row-3 residuals (guard-shaped, not built)

**2a. The `.tsx` log-roster class is OPEN — two INSTANCES scrubbed.** (140.5-05
(c); PATTERNS §11.6.) `CsvUploadStep.tsx`/`CsvSubmitStep.tsx`'s bare caught-error
logs now go through `scrubSeamError`, but `seam-log-coverage.test.ts`'s roster
does not cover `.tsx` files at all, so every sibling `console.error(…, err)` in a
client component is still invisible. This is the `ratelimit.ts` shape recurring
(behaviour vs topology). Closing it means widening that roster's file population.

**2b. The wait-threading remedy is row 3 and NOTHING guards its completeness.**
(140.5-03 (4).) `seam-transport-attribution.invariant.test.ts` covers the CATCH
half only; a sixth `buildEnvelope` surface that forgets to read the header gets no
wait and no test fails. Widening to row 1 means one shared hook owning both the
code and the wait.

**2c. `composite/members` is the one `buildEnvelope`-backed route with NO
`Retry-After` producer.** (140.5-03 (3).) `MultiKeyConnectStep`'s
`WIZARD_KEYS_LOAD_FAILED` site is structurally excluded from wait-threading —
recorded in the attribution guard's docblock so it cannot become a silent hole
the day a limiter lands there (RESEARCH Open Q4).

**2d. The docblock rewrites have NO guard behind them.** (140.5-03 (5); CONTEXT
§3.) The AGREEMENT corrections, the citation conversions and the ordinal rewords
are PROSE. `seam-ratelimit-posture` / `seam-wire-vocabulary` guard the PROPERTY,
but nothing stops someone re-writing "the two vocabularies are disjoint" back in.
A grep is not a receipt; no guard exists; stated rather than implied.

**2e. The purity-needle remedy is row 2, partial by construction.** (140.5-01
(2).) Four hand-listed pattern sets, no derived population; a fifth
dependency-free leaf gets no needle. Widening to row 1 = derive the leaf
population from disk (`deriveSeamRouteFiles` shape).

**2f. `wire-vocabulary` guard's declared blindness: `checkLimit` /
`withAuthLimited`-only routes.** (140.5-05 (d).) The population is defined by a
CALL to `rateLimitDenyJson`; `strategies/finalize-wizard` (client `SubmitStep`) is
outside it. Widening is a one-line change to the DENY_CALL needle plus a
re-measured floor.

---

## 3. Citation / prose harness residuals (this phase's own subject matter)

**3a. Variants D/E/F (self-relative citations) are NOT covered by the citation
guard.** (140.5-08 by construction; CONTEXT §6d.1, PATTERNS §6.) `line 55`,
`(:1027)`, `at line 67` — 69 repo-wide — name a line in the file they sit in,
carry no path, and need a SECOND, containing-file-scoped predicate. The guard is
row 1 for the path-qualified forms and silent for these.

**3b. The census/guard cannot see citations inside STRING LITERALS.** (140.5-07
(c).) The predicate is comment-scoped by design. `resilient-fetch.ts` carried four
in `notes:` strings, three wrong — all invisible to `MODE=full`, fixed there by
hand. The class is open on the rest of the surface; closing it needs a second
predicate over string literals (population unmeasured).

**3c. A prose absence check must EXCLUDE marked quotations; nothing in the repo
does that yet.** (140.5-04 SC-PROSE-3, named for 08.) A file that quotes a
now-false paragraph and marks it "IS NOW FALSE" (140.4's record of refuted
reasoning) legitimately still contains the false phrase. Editing a marked
quotation to satisfy a grep falsifies a historical record. A guard over prose
needs to skip marked-quotation spans — unbuilt.

**3d. Widening the citation guard repo-wide** requires, in order: `.planning/**`
stays excluded (frozen record); human triage of hundreds of pre-existing stale
citations; the D/E/F second predicate (3a); THEN a one-line glob change (§4b).

**3e. RESEARCH §3.8's per-row offset table has an error.** (140.5-07 (d).) The
`+72` row does not reproduce. Anything inheriting "three by +21, one by +72"
(incl. this phase's narrative) should read "four by +21." Flagged because §3.8 is
cited by 08's own context.

**3f. `WP-13`'s "three redundant checks plus one silent fail-open" is a
mis-shaped count.** (140.5-07 (e).) Measured: three sites, where the null-conjunct
IS the fail-open. Anything citing 3+1 as four distinct remediations should be
re-read.

---

## 4. Type / constructor hazards (own scoped plan wanted)

**4a. `AnalyticsUpstreamError`'s positional parameters — the same
adjacent-same-typed-argument class as `mintTenantClaim`.** (140.5-07 (b); RESEARCH
§11 recorded three, since grown by one.) Not in CONTEXT §7.5's fence, deliberately
unplanned. It has more call sites than `mintTenantClaim` and wants its own scoped
plan rather than a drive-by widening.

---

## 5. Harness / CI residuals

**5a. The 17 in-repo `stripComments` copies are NOT rewired.** (140.5-01 (1);
RESEARCH §8.2, WP-12.) `source-scan.ts` is row 1 for its IMPORTERS; it does not
touch the copies at `seam-log-coverage.test.ts`, the three `*.purity.test.ts`,
`seam-redaction.test.ts`, `scripts/check-route-contract.ts`,
`scripts/check-admin-route-manifest.ts` and the rest. Not a pure mechanical swap:
`check-route-contract.ts` also erases STRING bodies, which `source-scan.ts`
deliberately does not, so that migration needs a third mode.

**5b. `ci.yml:1633` deliberately left narrow.** (140.5-06 (c).) Widening its
line-based, comment-blind `grep -Eq` to `src/**`+`tests/**` reddens a clean tree
today on one hit that is a COMMENT, and bash cannot comment-strip maintainably.
`spec-disabling.invariant.test.ts` subsumes its intent at row 1; the reasoning
lives in that guard's docblock so the next reader does not "finish the job."

**5c. `e2e/strategy-v2-keyboard.spec.ts`'s skip is allow-listed and stays
skipped.** (140.5-06 (d).) A PR #108 follow-up needing role-based locators + a
chart-mount guard; the class is held meanwhile by
`tests/visual/chart-accessibility-layer.test.ts`. `e2e/strategy-v2-chart-parity.spec.ts`
is the SECOND member (goldens never baselined, red continuously when un-skipped) —
converting it would re-enable a known-red spec. Both are PR #108 follow-ups.

**5d. `handleRetrySync`'s clear-on-fresh-attempt reset is NOT covered by a
test.** (140.5-03 (6).) The only writer of that state before this point routes
straight to `gate_failed` and never shows the banner, so the stale-wait scenario
is unreachable today. The reset is defence for the path 141 adds; claiming it
guarded would be this milestone's own defect.

**5e. The Node-22 BASELINE at `4c60518b` remains unre-run.** (140.5-01 (5).) What
is DISCHARGED is the property that matters — Node 22 and Node 25 agree exactly at
HEAD. The historical 721/10052 figure was never re-measured on Node 22.

**5f. `SC-429-1`'s ledger row receipt is unreachable by its stated mutation.**
(140.5-06 (f).) A Python edit cannot red a vitest case with a mocked upstream.
Recorded with what actually reds; re-wording the row is owned by whoever owns
VALIDATION.md. (5g) The `mypy --strict` run UNDER the SC-429-1 mutation was
blocked by the sandbox classifier and is argued, not executed (140.5-06 (g)).

---

## 6. Founder-owed (not code — do not plan around)

**6a. Copy vs `DESIGN.md` §Voice — founder review owed for Claude-drafted copy**
(CONTEXT §8.1). From 140.5-02: `CSV_VALIDATION_FAILED` (replacement title/cause/fix),
`KEY_MISSING_READ_SCOPE` (new), `KEY_PERMISSION_DENIED` (new). From 140.5-05: NONE
(only a removal). ⭐ `CSV_UPSTREAM_FAIL`'s §4a copy is EXEMPT — founder-authored,
pre-approved, reproduced verbatim.

**6b. TRAP-4 five clicks in a real browser (C-4); Sentry ingestion (C-5, force a
terminal-arm failure via an unroutable `ANALYTICS_SERVICE_URL` in preview).**
Carried unchanged from 140.4 §7.

**6c. ⛔ D-01 — the live-Redis lane is STILL UNVERIFIED.** (CONTEXT §6g.4, §7
KNOWN BLIND SPOT; restated because no plan claimed it closed and none may.)
`tests/redis/**` needs a live store and is not in the default run;
`seam-breaker.redis.test.ts`'s registration pins its EXISTENCE, not its execution.

---

## 7. Owned by 140.5-08, discharged here

- ⛔ **Task 2 (140.5-06) is 1 of 5 424-arrival routes and 0 of 5 re-homes.**
  Still owed to a future plan: 424 arrival cases at `keys/validate-and-encrypt`,
  `strategies/create-with-key`, `strategies/composite/add-key` (flat shape) and
  `verify-strategy` (`_envelope_error` shape); and RE-HOMING the five cannot-arrive
  suites (`admin/match/eval`, `admin/match/recompute` ×2, `MatchEvalDashboard`,
  `scenario/optimize`) onto an emittable status while keeping their real forwarding
  assertions (§6g.3: RE-HOME, do not delete). **NOT this plan's** — 08 owns
  registration + the citation guard + the gate, not the 424 arrival expansion.
- ✅ CONTRACT_GUARDS registration of all five phase guards — **done** (this plan).
- ✅ Registry prose corrected ("three predicates"→five/named set; "six seam
  files"→derived roster; "sixteen route tests"→predicate) — **done** (this plan).
