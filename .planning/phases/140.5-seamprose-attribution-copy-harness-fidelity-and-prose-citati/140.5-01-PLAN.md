---
phase: 140.5-seamprose
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/test-setup.ts
  - src/test-setup.leak-canary.test.ts
  - src/lib/source-scan.ts
  - src/lib/source-scan.test.ts
  - src/lib/seam-copy.purity.test.ts
  - src/lib/seam-errors.purity.test.ts
  - src/lib/seam-discriminator.purity.test.ts
  - src/lib/seam-redaction.test.ts
autonomous: true
requirements: [SEAMPROSE-04]
must_haves:
  truths:
    - "A global stubbed in one test cannot leak into the next test (config-enforced, not per-file discipline)"
    - "A direct process.env assignment in one test file cannot leak into later tests in the same worker"
    - "A dynamic import(\"pkg\") in any of the four dependency-free seam leaves reddens that leaf's purity guard"
    - "One shared comment-handling module exists for every structural guard this phase writes — and it can itself FAIL (SC-SCAN-1)"
  artifacts:
    - path: "src/lib/source-scan.ts"
      provides: "stripCommentsPreserveLines + extractComments (line-preserving, quote-aware; TS + minimal Python)"
      exports: ["stripCommentsPreserveLines", "extractComments"]
    - path: "src/test-setup.leak-canary.test.ts"
      provides: "in-file ordered canary proving global + env restoration between tests"
    - path: "vitest.config.ts"
      contains: "unstubGlobals: true"
  key_links:
    - from: "vitest.config.ts"
      to: "every test file"
      via: "unstubGlobals/unstubEnvs restore before each test"
    - from: "src/test-setup.ts"
      to: "process.env"
      via: "snapshot in setup, restore in the EXISTING afterEach (extend it, do not add a second)"
---

<objective>
Close the harness-fidelity holes (DEF-16-1's mechanism) and land the shared source-scanning
module every later guard in this phase depends on. This plan lands ALONE in wave 1 because the
leak closure is sequence-sensitive (TRAP-8): suites silently depending on a leaked global/env may
redden, and the fallout must be attributable to this change and nothing else.

Coverage-law row: **row 1** for both remedies — a vitest config flag and the single setup-file
`afterEach` are inherited by every test file without an edit. (CONTEXT §2.)

Purpose: 81 files call `vi.stubGlobal` (predicate: `grep -rln 'vi\.stubGlobal' src/ tests/`), 38 with
no local cleanup; 54 test files assign `process.env.X =` directly, 38 with no restore (RESEARCH §8.1).
Output: leak-proof harness + `src/lib/source-scan.ts` + the dynamic-import purity needle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-CONTEXT.md
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-RESEARCH.md   (§2, §8, §17 pitfalls 2+5, §18)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-PATTERNS.md   (§2 stripComments variants, §7)
@src/test-setup.ts
@vitest.config.ts
@src/lib/seam-log-coverage.test.ts
@src/lib/seam-copy.purity.test.ts
</context>

<interfaces>
Contracts this plan CREATES (wave-2+ plans build against them — treat as frozen once landed):

```ts
// src/lib/source-scan.ts
/** Blank comment contents char-for-char so reported line/column numbers stay real.
    Handles // line, block comments, string literals (must NOT blank string contents for
    the strip variant — a "//" inside a string is not a comment), template literals. */
export function stripCommentsPreserveLines(source: string, lang?: "ts" | "py"): string;
/** The inverse: return ONLY the comment spans, each with its 1-based start line.
    This is what a citation guard consumes — a citation lives INSIDE a comment. */
export function extractComments(source: string, lang?: "ts" | "py"): Array<{ line: number; text: string }>;
```

Semantics to copy: `seam-log-coverage.test.ts:299-305` (line-preserving char blanking — the variant
PATTERNS §2 mandates when a guard must report a location). Python: `#` comments + triple-quoted
docstrings (see the committed `140.5-citation-census.mjs` for the reference implementation).
The 17 existing in-repo `stripComments` copies are NOT rewired in this phase — record that
explicitly in the SUMMARY `## OPEN` as a deferred consolidation (RESEARCH §8.2).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared source-scan module (inert — nothing imports it yet)</name>
  <files>src/lib/source-scan.ts, src/lib/source-scan.test.ts</files>
  <behavior>
    - stripCommentsPreserveLines: a `// comment` is blanked; `wc -l` of output equals input; a
      string literal containing `"// not a comment"` survives intact; a block comment spanning
      3 lines leaves 3 lines; template-literal contents with `${}` interpolation survive.
    - extractComments: returns the comment text with the correct 1-based line; a citation-shaped
      token inside a STRING literal is NOT returned (both polarities); Python `#` and `"""docstring"""`
      spans are returned exactly once (dedup — the census hit double-extraction, RESEARCH §3.1 bug 3).
    - Expected values in the test are hand-typed literals, never derived from the module (Oracle
      Independence hazard #3/#5).
  </behavior>
  <action>
    Create `src/lib/source-scan.ts` implementing the two exports above. Start from the semantics of
    `seam-log-coverage.test.ts:299-305` (line-preserving blanking) — do NOT copy the purity-test
    variant (`seam-copy.purity.test.ts:55-61`), which discards line numbers (PATTERNS §2 names this
    exact wrong-pick hazard). For Python handling, port the comment-extraction logic from
    `.planning/phases/140.5-*/140.5-citation-census.mjs` (it already survived the paren-eating,
    third-party-path and docstring-dedup bugs — RESEARCH §3.1).
    Do NOT modify any existing file in this task.
  </action>
  <verify>
    <automated>SC-SCAN-1 (planner-added ledger row — this module is PRODUCTION source four later guards import, so a semantic mutation IS admissible): make `stripCommentsPreserveLines` stop blanking BLOCK comments (a branch-direction edit, tsc-clean) → its unit tests red; paste the failing assertion, revert, porcelain clean. Then FULL `npx vitest run` before commit — scoped runs are never a commit receipt (CONTEXT §9)</automated>
  </verify>
  <done>Both exports exist with both-polarity tests; SC-SCAN-1 observed; full suite still 0 failed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the global/env leak at the harness chokepoint, measure the fallout, fix forward</name>
  <files>vitest.config.ts, src/test-setup.ts, src/test-setup.leak-canary.test.ts</files>
  <behavior>
    Canary (single file, ordered tests — vitest runs tests within a file in order):
    - Test 1 stubs a global (`vi.stubGlobal("__seamproseCanary__", …)`) and assigns
      `process.env.__SEAMPROSE_CANARY__ = "leaked"` directly.
    - Test 2 asserts the global is gone AND the env key is gone.
    - This test FAILS if either restore mechanism is removed (SC-HARNESS-1 + SC-ENV-1 in the
      Falsifiability Ledger — run both mutations, paste the failing assertion, revert).
  </behavior>
  <action>
    1. `vitest.config.ts`: add `unstubGlobals: true` and `unstubEnvs: true` to the `test` config
       (vitest 4.1.10 options verified in RESEARCH §2 from the installed .d.ts). Config beats a
       setup-file hook for stub-based restores: it runs BEFORE each test and cannot be shadowed by
       a file-local `afterEach`.
    2. `src/test-setup.ts`: snapshot `{ ...process.env }` at module scope; EXTEND the existing
       `afterEach` at :44-49 (do not add a second one — PATTERNS §7) to restore `process.env` to
       the snapshot (delete keys added, restore keys mutated/removed). This is the only mechanism
       that covers DIRECT `process.env.X =` assignments — `unstubEnvs` only tracks `vi.stubEnv`
       (RESEARCH §2, measured).
    3. Add the canary file per the behavior block.
    4. Run the FULL suite on Node 25. Baseline to preserve: 721 files / 10052 passed / 287 skipped /
       0 failed (predicate: `npx vitest run` at `4c60518b`). Every newly-red file was silently
       depending on a leak — fix each FORWARD at root cause (give the test its own properly-scoped
       setup via `vi.spyOn` + the config-level restore; NEVER `vi.stubGlobal` in new code, never
       weaken the harness). List every fallout file in the SUMMARY and append it to files_modified.
    5. Run Node 22 parity: `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run`. DEF-140.4-A
       caveat: a red parity run on a loaded machine may be contention (four known load-dependent 5s
       timeouts) — re-run before concluding regression.
  </action>
  <verify>
    <automated>npx vitest run → 0 failed on Node 25 AND Node 22; canary present and passing; mutations SC-HARNESS-1 (flip `unstubGlobals` to `false` — mechanism note: the landed restore is the config flag, not a setup-file `vi.unstubAllGlobals()`) and SC-ENV-1 (remove the env snapshot-restore) each redden the canary — paste evidence into VALIDATION.md ledger, revert, prove `git status --porcelain` clean of the mutation</automated>
  </verify>
  <done>Full suite green both Nodes; canary observably falsifiable in both polarities; fallout files enumerated in SUMMARY with root-cause notes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the dynamic-import needle to the four purity pattern sets + prose hygiene in owned files</name>
  <files>src/lib/seam-copy.purity.test.ts, src/lib/seam-errors.purity.test.ts, src/lib/seam-discriminator.purity.test.ts, src/lib/seam-redaction.test.ts</files>
  <behavior>
    - A synthetic source string `const m = await import("@upstash/redis");` (indented, in a
      function) is CAUGHT by each widened pattern set; the same file without it stays silent.
      RESEARCH §8.2 proved by execution that all three existing needles miss `import(` in every
      position, and `sentry-capture.ts` already uses that exact idiom in-repo.
  </behavior>
  <action>
    Add `/\bimport\s*\(/` to the pattern set in each of the four files (three `*.purity.test.ts`
    plus the set EMBEDDED in `seam-redaction.test.ts:538` — a `*.purity.test.ts` glob finds only
    three; RESEARCH §8.2). ⚠️ `seam-redaction.test.ts` is the odd one out and must NOT be edited by
    copy-paste: at `:565-575` it asserts `process.env` IS present (call-time read, not module-scope
    capture) — the opposite polarity of the other three; read `:560-575` first (PATTERNS §7). Add a
    positive-control case (the behavior block's synthetic string) beside each needle so the
    widening is provably not vacuous.
    Prose hygiene in files this task owns: the word "sixteen" at `seam-copy.purity.test.ts:86`,
    `seam-errors.purity.test.ts:105`, `seam-discriminator.purity.test.ts:116` is defensible under
    exactly one of five predicates (RESEARCH §3.10) — DELETE the integer and name the predicate
    instead ("every route test that mocks a seam client wholesale"). Never replace it with a
    different integer.
  </action>
  <verify>
    <automated>SC-HARNESS-2 mutation for the ledger: add `const p = import("@upstash/redis")` (dead, unexecuted — tsc must stay clean) to `src/lib/seam-copy.ts`, run the purity suite, paste the failing assertion, revert. ⚠️ W10: this mutation edits production source OUTSIDE this plan's files_modified fence BY DESIGN (seam-copy.ts is not this plan's file); under use_worktrees the revert-before-commit discipline is the ONLY protection against cross-worktree contamination — run it with no other uncommitted work present, capture evidence, revert, and prove `git status --porcelain` is clean BEFORE any commit. Then FULL `npx vitest run` + `npx tsc --noEmit`</automated>
  </verify>
  <done>All four sets carry the needle with a positive control; no "sixteen" integer survives in the three purity files; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → CI verdict | a leaked stub makes CI pass/fail depend on file execution order (Node 22 vs 25 divergence — DEF-16-1) |
| purity leaves → client bundle | a dynamic import smuggles a dependency into leaves whose value IS having none |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.5-01 | Repudiation | vitest harness | mitigate | config-level `unstubGlobals`/`unstubEnvs` + env snapshot; canary falsifiable both polarities |
| T-140.5-02 | Elevation (bundle) | seam leaves | mitigate | `/\bimport\s*\(/` needle in all four pattern sets, positive control beside each |
| T-140.5-SC | Tampering | npm/pip installs | mitigate | ZERO installs this phase; gate = `git diff 4c60518b..HEAD -- package.json package-lock.json` EMPTY (verified at plan 08) |
</threat_model>

<verification>
- FULL `npx vitest run` (Node 25 + Node 22) → 0 failed. Baseline predicate + values in CONTEXT §9.
- `npx tsc --noEmit` exit 0; `npm run lint` 0 errors (1 pre-existing warning `EquityChart.tsx:1119:6` allowed).
- Owed baseline verification (CONTEXT §6g.2 — a plan must verify): run `cd analytics-service && python3 -m pytest -q`
  and `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/`; record the actual
  numbers in the SUMMARY beside their commands (expected ~4803 passed / 96 skipped and clean/89 files —
  if they differ, report, do not silently accept).
</verification>

<success_criteria>
- Canary reddens under removal of either restore mechanism (evidence pasted in VALIDATION.md ledger rows SC-HARNESS-1, SC-ENV-1).
- source-scan provably falsifiable (SC-SCAN-1 observed) — the contract wave-2+ guards import.
- A dynamic import reddens each of the four purity sets (SC-HARNESS-2 observed, cross-fence discipline recorded).
- Full suite green on both Nodes; Python baselines re-measured and recorded.
</success_criteria>

<output>
Create `.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-01-SUMMARY.md`.
`## OPEN` must name: (a) the 17 unmigrated `stripComments` copies (deferred consolidation);
(b) any fallout suites fixed forward, with root causes; (c) that the harness canary is NOT registered
in CONTRACT_GUARDS (it is an ordinary always-running test, not a structural contract — registration
of this phase's structural guards is owed by 140.5-08, named there).
Do NOT touch STATE.md, ROADMAP.md, or DESIGN.md.
</output>
