---
phase: 140.5-seamprose
plan: 07
subsystem: seam-type-invariants
tags: [SEAMPROSE-05, SEAMPROSE-01, WP-13, F5, discriminated-union, fail-open, citation-rot, DEF-16-2, probe_error, D-6d.2]
requires:
  - "140.5-02 / 140.5-03 — wave-2 seam corrections; no artefact of theirs is imported here, the dependency is ordering only"
provides:
  - "mintTenantClaim(identity: TenantIdentity, secret: string) — the transposition of the two adjacent same-typed strings is now a COMPILE error at every call site, present and future"
  - "SeamBreakerVerdict as a two-member discriminated union on `counts` — `{counts:true, breakerKey:string}` | `{counts:false, breakerKey:null, dependency:null}`; the keyless-counting state is unrepresentable"
  - "src/lib/seam-breaker-failopen.regression.test.ts — the RUNTIME pin that a counting verdict always lands on a real, non-empty breaker key (all three recording sites + a 424 positive control + a vacuity floor)"
  - "resilient-fetch's breaker-evidence table, symbol-anchored and POPULATION-COMPLETE (13 of 13 `dependency=` sites in routers/**)"
  - "The annotated out-of-repo `@upstash/ratelimit` reference — 140.5-08's sole expected citation-guard allowlist entry"
affects:
  - "140.5-08 — inherits the allowlist entry (exactly one), plus a named census blind spot (string-literal citations) and the AnalyticsUpstreamError carry-forward"
  - "141 — the breaker now records every counting verdict unconditionally; retry work builds on that being true rather than prose-asserted"
tech-stack:
  added: []
  patterns:
    - "One-field object parameter as a transposition fence for adjacent same-typed arguments (the PostProcessKeyArgs analog)"
    - "@ts-expect-error as a FALSIFIABLE compile receipt — reverting the property makes the directive unused and tsc red (TS2578)"
    - "Discriminated union replacing a docblock invariant, so consumers' defensive null-tests become dead and are deleted rather than kept"
    - "Citations anchored to function + machine code rather than line numbers"
key-files:
  created:
    - src/lib/seam-breaker-failopen.regression.test.ts
  modified:
    - src/lib/tenant-claim.ts
    - src/lib/tenant-claim.test.ts
    - src/lib/process-key-client.ts
    - src/lib/analytics-client.ts
    - src/lib/seam-discriminator.ts
    - src/lib/resilient-fetch.ts
    - .planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-VALIDATION.md
decisions:
  - "The mint takes TenantIdentity, NOT a branded InternalApiToken. The SPEC-types review proposed branding the secret as well; that needs a new reader for process.env and a brand ceremony at two sites to close a hazard the object already closes. The object makes the ORDER mistake uncompilable, which is the stated hazard. Recorded, with the residual named in the docblock: `mintTenantClaim({userId: theSecret}, theUserId)` still compiles and no type prevents it — the mistake now has to be written out rather than committed by getting two bare arguments the wrong way round."
  - "analyticsRequest's `options.tenantId` stays `string`. Only the mint boundary is wrapped. Widening options.tenantId to an object would have reached analytics-client.test.ts, which is 140.5-06's file THIS WAVE — the fence this plan's file list exists to protect."
  - "probe_error: NO schema change (locked D-6d.2). Recorded as RESOLVED-BY-DECISION, not dropped."
  - "The SC-TYPE-2 mutation is the EMPTY STRING, not null. Post-union `null` is not constructible, so the ledger's `?? \"\"` shape IS the only tsc-clean spelling of the fail-open — which is why the row is a real runtime mutation and not another compile receipt."
metrics:
  tasks: 3
  commits: 3
  duration: "~2h wall (two interruptions: a laptop suspend and an API error; zero work lost, zero commits rolled back)"
  completed: 2026-07-30
---

# Phase 140.5 Plan 07: Seam Type Invariants + resilient-fetch Citation Truth — Summary

Two seam invariants moved out of prose and into the type system — `mintTenantClaim`'s
argument order and `SeamBreakerVerdict`'s counts/key pairing — with the second one's deleted
fail-open pinned by a runtime guard that reds under re-introduction; plus `resilient-fetch`'s
breaker-evidence table re-derived from source, symbol-anchored, and proven population-complete.

**Coverage-law row, stated per CONTEXT §2:** the two type fixes are **row 1 by construction** —
the compiler enforces them at every present and future call site with no per-site edit. The
citation work is **row 3 (per-site prose), partial by construction, in those words**: it closes
`resilient-fetch.ts` only. The seam-surface class guard is 140.5-08's.

---

## Commits

| Commit | Task | What |
|---|---|---|
| `8909b142` | 1 | `mintTenantClaim` takes `TenantIdentity`; both call sites wrapped in the same commit |
| `ee7986dd` | 2 | `SeamBreakerVerdict` discriminated union; three fail-open conjuncts deleted; runtime pin added |
| `05698403` | 3 | Evidence table symbol-anchored ×3 occurrences; out-of-repo reference annotated |

---

## Task 1 — the mint signature

### Pre-flight enumeration (pasted, as the plan requires)

Predicate: `grep -rn 'mintTenantClaim' src/ tests/ e2e/ analytics-service/ scripts/ tools/` plus a
repo-wide sweep excluding `node_modules`/`.git`/`.next`. Outside `.planning/**` (planning ledgers
and review documents, which are frozen records and were not touched), **exactly four source files**
reference it:

| File | Role | In this plan's fence? |
|---|---|---|
| `src/lib/tenant-claim.ts` | the definition | ✅ |
| `src/lib/tenant-claim.test.ts` | its own tests (18 call sites) | ✅ |
| `src/lib/process-key-client.ts` | call site | ✅ |
| `src/lib/analytics-client.ts` | call site | ✅ |

**Fence check — the one the plan told me to stop on:** `src/lib/analytics-client.test.ts` is owned
by **140.5-06, running in a parallel worktree this same wave**. It references **neither**
`mintTenantClaim` **nor** `TenantIdentity` (`grep` exit 1, zero hits). ⭐ It *does* reference
`tenantId`, which is why `analyticsRequest`'s `options.tenantId` was deliberately left as `string`
and only the mint boundary wrapped. Widening that option would have reached 06's file. **No
cross-fence edit was made.**

### Two plan/RESEARCH premises measured FALSE

1. **"the `:88` type, currently defined and unused"** (plan artifact line; RESEARCH §11 says
   "exists at `:88` and is **not applied**"). Measured: `TenantIdentity` is applied at **four**
   sites in `analytics-client.ts` — `validateKey`, `encryptKey`, `optimizeScenarioWeights`,
   `evalMatch`. The claim is true *only* of `mintTenantClaim` specifically. Corrected in the file's
   own docblock rather than left to rot.
2. **`tenant-claim.ts`'s own prose said "The THREE wrappers … " and then listed FIVE names.**
   All five still take bare strings (each signature read at HEAD), so the *list* was right and the
   *integer* was wrong. Reworded to enumerate, integer deleted — per Oracle-Independence hazard 4,
   correcting 3→5 would have replaced one unpinned answer with another.

### Call-site coordinate drift

The plan cites `process-key-client.ts:339` and `analytics-client.ts:321`. Real at my base:
**`:345`** and **`:388`**. Recorded as an instance of this phase's own subject matter — a plan
carrying coordinates it did not re-measure.

### Claims are byte-identical

The refactor is shape-only and the receipt already existed: `tenant-claim.test.ts`'s
*"mints an EXACT claim string under a fixed clock"* pins
`11111111-…-555555555555.1900000000.b6c8871017dcf8dd…323b5` as a literal, and the 4-case
`tests/fixtures/tenant-claim-parity.json` round-trips through the shipped mint. **Both literals
were left untouched and both pass** — that is what proves the wire bytes did not move.

---

## Task 2 — the union, and what the compiler actually named

### WP-13's count re-derived, and it is a different shape

WP-13 claims *"three redundant null-tests plus one silent fail-open."* Measured:

> **Predicate:** `grep -n 'counts && ' src/lib/resilient-fetch.ts`, then each hit read.
> **Result: THREE sites** — `:1610`, `:1906`, `:1930`, all in `resilient-fetch.ts`.
> (A repo-wide run returns a fourth hit, which is *my own new docblock in
> `seam-discriminator.ts` quoting the pattern* — DEF-16-2 live, a grep matching a file's own prose.
> Excluded by reading, not by luck.)

**There is no separate fourth site. The null-conjunct IS the fail-open.** `if (counts && breakerKey
!== null)` reads as defence, but its false branch has **no else** — a countable failure with no key
was dropped with no log and no counter. So WP-13's "3 + 1" is really **one property at three
occurrences**. Published beside the claim, as the plan required.

### The compiler names them — but only in the opposite direction

The plan says *"run tsc after narrowing and let it name them."* **It does not.** After narrowing,
`verdict.breakerKey !== null` on a `string` is legal TypeScript (no TS2367 for null comparisons
against non-nullable types), and `@typescript-eslint/no-unnecessary-condition` is **not enabled**
in `eslint.config.mjs` — verified by grep. `tsc --noEmit` exited **0** with zero diagnostics.

The enumeration the plan wanted is available by **inverting** the probe: flatten the union back and
the compiler names every consumer that depended on the invariant.

```
src/lib/resilient-fetch.ts:1617:26 - error TS2345: Argument of type 'string | null' is not
  assignable to parameter of type 'string'.   await recordOnce(bodyVerdict.breakerKey);
src/lib/resilient-fetch.ts:1916:24 - error TS2345: …                await recordOnce(transportVerdict.breakerKey);
src/lib/resilient-fetch.ts:1949:22 - error TS2345: …                await recordOnce(verdict.breakerKey);

Found 3 errors in the same file, starting at: src/lib/resilient-fetch.ts:1617
```

⭐ **"in the same file" is the fence receipt.** The prompt flagged that narrowing might reach
`seam-discriminator.test.ts` or `seam-constants.pin.test.ts`, which are in **no plan's**
`files_modified`. It did not: the former declares its own local `VerdictCase` interface and never
imports the type name; the latter only ever passes `verdict.breakerKey` into `expect(...)`, which is
untyped. **No file outside this plan's fence was modified.** Both were run explicitly and are green.

⚠️ That flattening probe is a **TYPE edit** and is therefore **inadmissible as a ledger mutation**.
It is recorded as an enumeration probe and is *not* counted as a caught mutation anywhere.

### The purity guard

`seam-discriminator.purity.test.ts` is 140.5-01's file and was **not touched**. The union is a
**type** export, so `EXPECTED_EXPORTS` (five functions) is unchanged and the guard is green — which
is the plan's stated tell that the change did not leak a runtime export.

### Prose corrected in the owned file

`:137`'s *"the exported surface stays the three predicates"* was false when written and drifted
twice further (`seamDependencyName` 140.3-11, `seamCorrelationId` 140.3-15, each with
`EXPECTED_EXPORTS` edited in the same commit). Reworded to point at the **pinned** `EXPECTED_EXPORTS`
with **no replacement integer**. `:242`'s *"disjoint by CONTRACT"* was left alone as instructed — it
is a different pair (breaker keys vs venue names) and is correct.

---

## Task 3 — the evidence table

### Re-derived at HEAD, per row, by reading `analytics-service/routers/**`

> **Predicate:** every `dependency=` argument in `routers/**` (13 total), each read for its status,
> machine code and enclosing function. Citation count in `resilient-fetch.ts` under the committed
> census predicate (comment-scoped): **18 before**.

| Cited | Real at HEAD | Verdict |
|---|---|---|
| `exchange.py:314,324` (503 mt5-gateway) | raise `:332,:342`, `dependency=` `:335,:345` | ❌ **+21** |
| `exchange.py:276,287` (500 mt5-gateway) | raise `:294,:305`, `dependency=` `:297,:308` | ❌ **+21** |
| `exchange.py:626` (500 kek) | raise `:644`, `dependency=` `:647` | ❌ **+21** |
| `exchange.py:547` (424 venue) | raise `:565`, `dependency=` `:568` | ❌ **+21** |
| `exchange.py:132` (500 egress-proxy) | `dependency=` `:132` | ✅ correct |
| `internal.py:303,319` (500 kek) | `:303,:319` | ✅ correct |
| `internal.py:498` (424 venue) | `:498` | ✅ correct |
| `portfolio.py:684` (503 supabase) | `:684` | ✅ correct |
| `match.py:1657,1691` (503 supabase) | `:1657,:1691` | ✅ correct |

**4 of 9 wrong, all four in `exchange.py`, all four low by exactly 21.**

### ⚠️ RESEARCH §3.8's "+72" row does NOT reproduce — and the plan's rationale rests on it

The plan states the offsets are *"mixed +21/+72"* and that **"a single global offset would be
WRONG — that asymmetry is the argument for symbols."* Measured: **the offsets are uniform.**
RESEARCH mapped the cited `:547` onto `exchange.py:619-620`, which is a
`VenueTransientHTTPException(status_code=424, …)` carrying **no `dependency=` field at all** — so it
is not a member of a table whose subject is *"every `dependency=` argument"*. The correct target is
`:565/:568`, +21 like the rest. And `git diff 4c60518b 49c8353e -- analytics-service/routers/exchange.py`
is **empty**, so this is a measurement error in RESEARCH, not drift between bases.

**Symbols were still used, on the surviving argument:** line numbers rot on any edit above them —
these four had already rotted by 21 — and the four wrong rows sat beside five correct ones, so a
reader spot-checking a correct row would have concluded the table was sound. That is the durable
case, and it does not need the asymmetry.

### The population was incomplete-checkable, and is now checked

13 `dependency=` sites exist in `routers/**`; **all 13 are now accounted for** in the table. A
*missing* row silently under-declares a real breaker key, which is a worse failure than a wrong
coordinate — and nothing in the previous table let a reader tell the difference. Rows are anchored
to **function + machine code** (`MT5_GATEWAY_UNREACHABLE`, `ANALYTICS_ROW_NOT_CREATED`, …), which
are unique, greppable, and the same vocabulary `error_contract.py` validates.

**All three occurrences fixed**, not one of three: the table itself, the `validate-key` budget
comment, and the `encrypt-key` budget comment — plus the `match` / `portfolio-analytics` /
`keys-permissions` entries carrying the same coordinates.

### ⚠️ A census blind spot, found by measuring instead of trusting

The census is **comment-scoped by predicate**. Four citations in this file live inside `notes:`
**string literals** and are invisible to it — and **three of the four were wrong**:

| Site | Cited | Real |
|---|---|---|
| `bridge.notes` | `analytics-client.ts:260` | `findReplacementCandidates` (`:768`) |
| `simulator.notes` | `analytics-client.ts:285` | `simulateAddCandidate` (`:794`) |
| `portfolio-analytics.notes` | `analytics-client.ts:224` | `computePortfolioAnalytics` (`:735`) |
| `process-key-unified-dormant.notes` | `…/route.ts:158` | `_unifiedValidateAndEncryptHandler` (`:170`), its fetch at `:195` |

All four converted. **Named in `## OPEN` for 140.5-08** — a comment-scoped guard will not see this
class, and these strings ship in the bundle.

### Census result

```
BEFORE (at 49c8353e):  citations total: 18  | resolved 10 | ambiguous 7 | missing 1 | PAST-EOF 0
AFTER  (at 05698403):  citations total:  1  | resolved  0 | ambiguous 0 | missing 1 | PAST-EOF 0
   --- MISSING FILE ---  src/lib/resilient-fetch.ts:811  ->  dist/index.mjs:757-761
```

A raw grep (`[A-Za-z0-9_/.-]+\.(ts|tsx|py|mjs|js|sql|yml|md|json):[0-9]`), which *also* sees string
literals, returns **the same single line**. Two independent predicates agree: **zero bare in-repo
citations remain.**

### The out-of-repo reference (140.5-08's allowlist entry)

`dist/index.mjs:757-761` is **kept, not converted**, and now annotated with the reasoning:
`node_modules` is not ours to anchor into, the file is a **build artefact** with no stable symbol
names, and the **version pin (`@upstash/ratelimit@2.0.8`) is what makes the coordinates meaningful**
— they are expected to be wrong for any other version, which is the opposite of in-repo rot. The
annotation says explicitly: if the pin moves, re-read the artefact; do not "fix" the numbers.

Its two self-relative siblings (`:1580-1585`, `:1560-1569` — census variants D/E/F, which 08's guard
does not cover) were qualified as *"same file, lines …"* so they can no longer be misread as
in-repo.

---

## probe_error — RESOLVED BY DECISION, not dropped

**No code change. `analytics-schemas.ts` was not edited** (confirmed absent from all three commits).

CONTEXT §6d.2 locks it, and the schema's own docblock at `:354-357` argues it correctly and
verbatim:

> *"WHY `probe_error` STAYS OPTIONAL. Its absence already carries a safe meaning — 'no probe error'
> — and the field is the service's own fail-CLOSED signal (`services/key_permissions.py::_FAIL_CLOSED`),
> not part of the scope verdict. Requiring it would fail closed on nothing that matters."*

The hazard the §7.5 scope item was reaching for — *"absence publishes a write-capable key"* — is
real, and it lives in `read`/`trade`/`withdraw`, which are **already required** (`z.boolean()`,
verified at `:364-366`). **The scope item demanding `z.boolean()` for `probe_error` is itself the
false claim; the claim is corrected here, not the schema.** RESEARCH's founder-flag A4 is answered
by §6d.2, which post-dates the docblock and endorses it.

---

## Falsifiability Ledger — both rows observed, and they are DIFFERENT KINDS

Full evidence pasted into `140.5-VALIDATION.md`. Summarised:

**SC-TYPE-1 — COMPILE receipt.** A bare `tsc` exit 0 is vacuous as a receipt, so both halves were
made falsifiable. The mint half uses two `@ts-expect-error` cases: reverting the signature to
`(identity: string, secret: string)` makes the guarded lines legal and `tsc` exits non-zero with
**`TS2578: Unused '@ts-expect-error' directive`** at `tenant-claim.test.ts:168:9` and `:184:9`
(observed, reverted by edit). The verdict half's receipt is that `recordOnce(verdict.breakerKey)`
compiles at all — flattening the union reds it with **TS2345 ×3**. It does **not** reach runtime.

**SC-TYPE-2 — RUNTIME regression, a real admissible mutation.** `dependency === null ?
GLOBAL_BREAKER_KEY : …` → `dependency === null ? "" : …` in `seam-discriminator.ts`. **`tsc --noEmit`
exit 0 under the mutation.** **2 failed | 3 passed:** the counter landed on `':failures'` instead of
`'breaker:railway:failures'`, and the negative-form case reddened independently. ⭐ The
`breaker:supabase` case, the transport case and the 424 positive control **all still passed**, so
the mutation is isolated and the five cases are independent oracles. Reverted by edit; porcelain
empty; 5/5 green.

**Neither stands in for the other**, which is exactly why the checker added the second row: SC-TYPE-1
is satisfied by a tree in which all three recording sites have been deleted outright.

### Two harness bugs recorded rather than papered over

1. The negative-form case first looped over a store `beforeEach` had just cleared → it passed
   **vacuously**. It now drives its own request and carries a **population floor** whose failure
   message says the guard is *blind, not satisfied*.
2. A `{ok, status, json}` object literal is silently skipped by `readDependencyBody`'s
   `typeof res.clone !== "function"` guard, so the body was never read and the `supabase` case came
   back as `breaker:railway`. **A harness bug that read exactly like a production bug.** Real
   `Response` objects are now used, and the reason is written into the file.

---

## Deviations from Plan

**1. [Rule 3 — blocking] Worktree was on the wrong base.** At spawn, HEAD was `dfeeeacc` (a
main-line commit), **not** the specified `49c8353e`, and `git merge-base --is-ancestor` confirmed it
was **not a descendant**. That is also why `.planning/` appeared to contain only `phase-19`. Working
tree was clean (`git status --porcelain` empty), so the sanctioned startup `worktree_branch_check`
remedy applied: `git reset --hard 49c8353e`. `.planning/` then resolved with all 2203 tracked files.
No symlink was created; nothing was deleted.

**2. [Rule 2 — missing artefact] The plan specified no test file for Task 2's runtime pin.** Task 2's
`<files>` lists only the two production modules, and the frontmatter's only test file is
`tenant-claim.test.ts` — yet the task requires a runtime regression test. Created
`src/lib/seam-breaker-failopen.regression.test.ts`, a **new** file in no plan's `files_modified`,
which is fence-safe under parallel worktrees (a new path cannot collide on merge-back). Flagged
here so it is a decision, not a silent addition.

**3. [Rule 1 — correctness] Two new lint warnings introduced and fixed before commit.** The first
draft of the new test imported the fake doubles at top level while the `vi.mock` factories import
their own, leaving two `no-unused-vars` warnings. Removed; lint returned to baseline (0 errors,
1 pre-existing warning) before Task 2 was committed.

**No file outside this plan's `files_modified` was modified**, other than the plan-sanctioned
`140.5-VALIDATION.md` ledger rows and this SUMMARY.

---

## Verification

| Gate | Baseline at `49c8353e` | At `05698403` |
|---|---|---|
| `npx vitest run` (Node 25) | 729 passed / 19 skipped (748) · 10193 / 287 / **0 failed** | **730 / 19 (749) · 10200 / 287 / 0** |
| same, **Node 22** (`v22.22.1`) | identical | **identical — 730 / 19 (749) · 10200 / 287 / 0** |
| `npx tsc --noEmit` | exit 0 | **exit 0** |
| `npm run lint` | 0 errors, 1 warning (`EquityChart.tsx:1119:6`) | **0 errors, same 1 warning** |
| `git diff 49c8353e..HEAD -- package.json package-lock.json` | — | **EMPTY** (zero installs) |

**+1 test file** (the new regression guard) and **+7 tests** (2 SC-TYPE-1 compile-receipt cases,
5 SC-TYPE-2 runtime cases) — 10193 + 7 = 10200, reconciled.

**Wall time (DEF-140.5-B discriminator):** 88.0 s / 89.5 s / 92.0 s / 94.1 s across runs — all in
the **quiet lane** (85–190 s), so no red was triaged as contention and none needed to be. Node 22 at
107.0 s, also quiet.

**Python untouched:** no `analytics-service/**` file is in any of the three commits, so the
`pytest 4803/96` and `mypy 89 clean` baselines are unaffected and were not re-run. Python files were
**read** for the citation re-derivation only.

⚠️ **Branch protection is OFF (CONTEXT §9, settled).** Every gate above is **advisory at merge** —
these guards **would have caught** the regressions described; none of them *did stop* anything.

---

## OPEN

**(a) `resilient-fetch.ts:811`'s out-of-repo `@upstash/ratelimit` reference is 140.5-08's sole
expected citation-guard allowlist entry.** Annotated in place per that plan's Task 1
(*"was annotated by 140.5-07 and is NOT converted"*). 08 pins the allowlist length to `.toBe(1)` —
this file now contains **exactly one** census-matching citation, so that literal is correct as
written.

**(b) `AnalyticsUpstreamError`'s positional parameters — OBSERVED, deliberately unplanned.**
RESEARCH §11 recorded three; it has since **grown by one**. It is **not** in CONTEXT §7.5's fence
and was **not** planned here. Named so it is a decision rather than an omission: the same
adjacent-same-typed-argument class as `mintTenantClaim`, at a constructor with more call sites, and
it wants its own scoped plan rather than a drive-by widening of this one.

**(c) NEW — the census cannot see citations inside string literals.** Its predicate is
comment-scoped by design. `resilient-fetch.ts` carried **four** in `notes:` strings, **three wrong**,
all invisible to `MODE=full`. Fixed here, but the *class* is open: 140.5-08's guard reuses the same
comment-scoped extraction, so it will not catch this shape anywhere else on the seam surface.
Closing it needs a second predicate over string literals — cheap to state, and the population is
unmeasured.

**(d) NEW — RESEARCH §3.8's per-row offset table has an error.** The `+72` row does not reproduce
(see Task 3). Anything downstream that inherited *"three by +21, one by +72"* — including this
plan's own `must_haves` and the phase-level narrative — should read **"four by +21"**. Flagged
because §3.8 is cited by 140.5-08's context.

**(e) `WP-13`'s "three redundant checks plus one silent fail-open" is a mis-shaped count.**
Measured: three sites, where the null-conjunct *is* the fail-open. Anything citing 3+1 as four
distinct remediations should be re-read.

---

## Self-Check: PASSED

- `src/lib/seam-breaker-failopen.regression.test.ts` — FOUND
- `src/lib/tenant-claim.ts`, `tenant-claim.test.ts`, `process-key-client.ts`,
  `analytics-client.ts`, `seam-discriminator.ts`, `resilient-fetch.ts` — FOUND, all modified
- `8909b142`, `ee7986dd`, `05698403` — all FOUND in `git log`
- `git status --porcelain` — empty after every commit
- `analytics-schemas.ts` — confirmed ABSENT from all three commits (probe_error untouched)
- STATE.md / ROADMAP.md / DESIGN.md — not touched
