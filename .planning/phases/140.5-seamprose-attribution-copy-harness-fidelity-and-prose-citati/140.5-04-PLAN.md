---
phase: 140.5-seamprose
plan: 04
type: execute
wave: 2
depends_on: ["140.5-01"]
files_modified:
  - src/app/factsheet/[id]/v2/basis-context.tsx
  - src/lib/api/simulatorSchema.ts
  - src/lib/closed-sets.ts
  - src/lib/factsheet/composite-read-path.ts
  - src/lib/factsheet/composite-read-path.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioCompareTable.all-columns.test.tsx
  - e2e/discovery-watchlist.spec.ts
  - tests/lib/process-key-client.test.ts
  - analytics-service/routers/process_key.py
  - analytics-service/routers/simulator.py
  - analytics-service/services/basis_series.py
  - analytics-service/services/job_worker.py
  - analytics-service/services/metrics.py
  - analytics-service/services/exchange.py
  - analytics-service/tests/test_cash_basis_series_sc4.py
  - analytics-service/tests/test_simulator_router.py
  - src/lib/sentry-capture.ts
  - src/lib/sentry-capture.test.ts
  - src/app/api/keys/[id]/permissions/route.ts
  - src/lib/seam-log-coverage.test.ts
  - src/lib/seam-budgets.invariant.test.ts
  - src/lib/resilient-fetch.wiring.test.ts
autonomous: true
requirements: [SEAMPROSE-01]
must_haves:
  truths:
    - "Zero past-EOF citations remain in tracked source under the committed census predicate (18 at base; predicate = 140.5-citation-census.mjs over src analytics-service e2e scripts supabase tests tools, comment-scoped, full-suffix resolution)"
    - "sentry-capture.ts no longer claims the seam captures nothing — the replacement states the true mechanism WITHOUT a bare integer, and quotes the true-grep-false-claim lesson"
    - "permissions/route.ts no longer documents a 5-minute cache for a 60-second revalidate — and the fix reintroduces NO integer a test could derive"
    - "seam-log-coverage.test.ts:177 no longer says EIGHT for a roster whose derivation made it nineteen — and is NOT 'corrected' to CONTEXT's stale eight"
  artifacts:
    - path: "src/lib/sentry-capture.ts"
      provides: "truthful capture-mechanism docblock (claim about a POPULATION → predicate named beside any count)"
    - path: "src/app/api/keys/[id]/permissions/route.ts"
      provides: "cache docblock referencing the revalidate constant instead of restating a duration"
  key_links:
    - from: "every corrected citation"
      to: "its target"
      via: "symbol-anchored reference (function/const name) that survives line drift — never a fresh line number"
---

<objective>
Correct the known-bad prose repo-wide: the 18 past-EOF citations, the false population claims, and
the value claims that contradict adjacent code — per the founder's §4b decision (fix repo-wide,
guard seam-only). This plan owns every affected file that no other 140.5 plan owns.

Coverage-law row, stated per CONTEXT §2: corrections are per-site — **row 3, partial by
construction in those words** — and that is the DECIDED shape: the founder rejected a repo-wide
guard (§4b), so beyond the seam surface the receipt is the committed, re-runnable census predicate,
not a guard. The seam-surface CLASS closure (guard) is 140.5-08's; this plan feeds it.

⚠️ CONTEXT §3 / DEF-16-2 is at maximum hazard here: this plan edits PROSE. Never write an
acceptance criterion whose grep would match the file's own docblock; comment-strip before counting;
never replace a false integer with a fresh integer whose predicate is unpublished.

Purpose: SEAMPROSE-01 (WP-15) corrections half.
Output: truthful prose + 0 past-EOF under the published predicate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-CONTEXT.md   (§3, §6b table, §6e)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-RESEARCH.md   (§3 whole section, §10.4, §17 pitfall 3)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-PATTERNS.md   (§6 — the self-correcting-docblock shape to copy)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-citation-census.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 18 past-EOF citations → symbol-anchored references</name>
  <files>src/app/factsheet/[id]/v2/basis-context.tsx, src/lib/api/simulatorSchema.ts, src/lib/closed-sets.ts, src/lib/factsheet/composite-read-path.ts, src/lib/factsheet/composite-read-path.test.ts, src/app/(dashboard)/allocations/components/ScenarioCompareTable.all-columns.test.tsx, e2e/discovery-watchlist.spec.ts, tests/lib/process-key-client.test.ts, analytics-service/routers/process_key.py, analytics-service/routers/simulator.py, analytics-service/services/basis_series.py, analytics-service/services/job_worker.py, analytics-service/services/metrics.py, analytics-service/tests/test_cash_basis_series_sc4.py, analytics-service/tests/test_simulator_router.py</files>
  <action>
    The full 18-row list with citers, targets and EOFs is in RESEARCH §3.2 — work from it verbatim.
    15 of 18 are ONE event, not fifteen rots: `analytics_runner.py` (EOF 1725) and
    `simulator_scoring.py` (EOF 312) both SHRANK under their citers in a refactor — say this in the
    correction commits so the next reader sees one cause. For each: READ the target file, find where
    the cited content now lives, and convert the citation to a SYMBOL reference (function/const/
    class name — WP-15's class fix; line numbers rot on the next edit, RESEARCH §2 alternatives
    table). Do not write a corrected line number unless the referenced thing has no nameable symbol;
    if you must, the comment names what the reader should grep for beside it.
    ⚠️ FENCE: `tests/lib/process-key-client.test.ts` is THIS plan's file (its `:8` cites
    `process-key-thin-adapters.test.ts:854`, EOF 833); `src/lib/process-key-client.test.ts` is
    140.5-03's — same wave, do not touch it.
    Keep these edits comment-only in the Python files; if any test content (not comments) must
    change, stop and report — that would mean the citation was load-bearing, which is a different
    defect.
  </action>
  <verify>
    <automated>node .planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-citation-census.mjs src analytics-service e2e scripts supabase tests tools → PAST-EOF = 0 (predicate committed beside the phase; 18 at base). Then FULL `npx vitest run` + `cd analytics-service && python3 -m pytest -q` (comment-only edits must not move either suite)</automated>
  </verify>
  <done>Census reports 0 past-EOF; both suites at baseline; the one-event-not-fifteen framing is in the commit message.</done>
</task>

<task type="auto">
  <name>Task 2: The false population and value claims</name>
  <files>src/lib/sentry-capture.ts, src/lib/sentry-capture.test.ts, src/app/api/keys/[id]/permissions/route.ts, src/lib/seam-log-coverage.test.ts, analytics-service/services/exchange.py, src/lib/seam-budgets.invariant.test.ts, src/lib/resilient-fetch.wiring.test.ts</files>
  <action>
    Follow the self-correcting-docblock shape from PATTERNS §6 (the WR-05/WR-01 analogs): state what
    the text USED to say · why it was wrong · quote the command and its output · what it says now ·
    what the correction does NOT close.
    1. `sentry-capture.ts:24-28` + `sentry-capture.test.ts:9`: the claim "the seam captures nothing
       to Sentry" is FALSE — capture happens via `captureToSentry` (predicate: comment-stripped
       `captureToSentry(` count over `EXPECTED_SEAM_FILES` = 41 route-file sites / 45 across the
       19-file roster at `4c60518b`; repo-wide 107 sites / 32 route files under CONTEXT §6b's wider
       predicate — three numbers, three predicates, so the REPLACEMENT TEXT must either name its
       predicate beside any count or carry no count at all; prefer no count). ⭐ Quote the lesson in
       the replacement: the old sentence was defended by a grep for `captureException` that is
       LITERALLY TRUE while the sentence it supported was false — CONTEXT §3's purest specimen,
       recorded here per §6e.
    2. `permissions/route.ts:166`: "…inside 5 minutes" vs `revalidate: 60` at `:344` — a 5×
       overstatement. Reword to reference the `revalidate` value below WITHOUT restating a duration
       (reintroducing "60 seconds" recreates the class; RESEARCH §10.4's durable rule: never write
       an integer in prose a test can derive). ⚠️ Leave `:43`'s "15 minutes" — it is TRUE
       (`key_permissions.py:70` reads `KEY_PERMISSION_CACHE_TTL` default 900).
    3. `seam-log-coverage.test.ts:177` "⚠️ EIGHT SINCE HI-02, AND THE LAST TWO WERE THE HOLE":
       reword to reference the derived roster (`EXPECTED_SEAM_FILES` / `deriveSeamRouteFiles`) with
       NO bare integer. ⚠️ Do NOT correct to CONTEXT's "eight" — CONTEXT's own figure is stale
       (truth is the nineteen-file derived roster; RESEARCH §3.6, pitfall 3). Leave `:962`'s
       "eight-file roster" — correctly past-tensed history. Leave `:963`/`:1013` — already correct.
    4. `exchange.py:1003-1004` self-citation "the deribit scope_detail arm at `:1043-1059` below"
       drifted +47 (real site `:1090-1104`): anchor to the symbol/branch name instead of a
       coordinate — a self-relative citation is invisible to any path-based guard (variant E;
       RESEARCH §3.8), so the only durable fix is removing the coordinate.
    5. "sixteen" at `seam-budgets.invariant.test.ts:125` and `resilient-fetch.wiring.test.ts:19`:
       delete the integer, name the predicate ("every route test that mocks a seam client
       wholesale" — RESEARCH §3.10: the number is defensible under exactly one of five readings, so
       WP-15 says delete, not correct).
  </action>
  <verify>
    <automated>Comment-stripped absence checks (DEF-16-2-safe — never bare grep): `node -e` scripts using src/lib/source-scan.ts to strip each edited file, then assert: "captures nothing" absent from sentry-capture.ts; "5 minutes" absent from permissions/route.ts while "15 minutes" (the TRUE :43 claim) is still present; "EIGHT SINCE" absent from seam-log-coverage.test.ts; "sixteen" absent from both invariant/wiring test files. Then FULL `npx vitest run` + `cd analytics-service && python3 -m pytest -q` + `npx tsc --noEmit`</automated>
  </verify>
  <done>All five claim clusters truthful, integer-free or predicate-carrying; both suites at baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Census receipt + seam feed-forward</name>
  <files>tests/lib/process-key-client.test.ts</files>
  <action>
    Re-run the census in all three committed modes and paste outputs into the SUMMARY:
    summary (repo roots), detail (past-EOF rows — must be empty), and
    `MODE=full` over `140.5-seam-surface.txt` (the seam-surface remainder that 140.5-08 will convert
    and then guard — enumerate the files still carrying bare citations so 08 receives a measured
    hand-off, not a guess; the hand-off-hole class, CONTEXT §6, is closed by NAMING it).
    ⚠️ Never cite "881" anywhere — no predicate reproduces it (CONTEXT §6b, RESEARCH §14.1); every
    count this plan writes carries its predicate in the same paragraph.
    This task's file entry is a placeholder for any residual fix-ups Task 1 deferred in
    tests/lib/process-key-client.test.ts; if none, touch nothing and say so.
  </action>
  <verify>
    <automated>MODE=detail node .planning/phases/140.5-*/140.5-citation-census.mjs src analytics-service e2e scripts supabase tests tools → zero rows; FULL `npx vitest run`</automated>
  </verify>
  <done>SUMMARY carries all three census outputs with predicates; seam remainder enumerated for 140.5-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comments → future engineers/agents | false prose steers the next phase's decisions (this phase's entire thesis) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.5-09 | Repudiation | corrected docblocks | mitigate | self-correcting-docblock shape: quote the predicate and output beside every surviving count |
| T-140.5-10 | Tampering | comment-only Python edits | mitigate | pytest at baseline proves no behavioural drift from prose edits |
</threat_model>

<verification>
Full `npx vitest run`, `pytest -q`, `tsc --noEmit`, `npm run lint` after each task commit (comment
edits CAN break suites — several guards scan prose, which is why every absence check above
comment-strips first). Node 22 parity at wave merge.
</verification>

<success_criteria>
- Census (committed predicate) reports 0 past-EOF repo-wide; output pasted.
- No corrected text carries a bare integer; every surviving count names its predicate inline.
- The seam-surface bare-citation remainder is enumerated by file for 140.5-08.
- The receipt for the repo-wide class is EXPLICITLY the measured census, not a guard — partial by
  construction beyond the seam surface, per founder decision §4b, said in those words in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-04-SUMMARY.md`.
`## OPEN` must name: (a) the seam-surface conversion remainder handed to 140.5-08 (file list from
Task 3); (b) variants D/E/F self-relative citations (~69 repo-wide under the pattern-mapper
predicate) NOT covered by any correction pass here except exchange.py's — a named residual.
Do NOT touch STATE.md, ROADMAP.md, DESIGN.md.
</output>
