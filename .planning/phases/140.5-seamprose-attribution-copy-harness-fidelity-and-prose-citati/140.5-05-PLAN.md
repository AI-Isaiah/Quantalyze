---
phase: 140.5-seamprose
plan: 05
type: execute
wave: 3
depends_on: ["140.5-01", "140.5-02", "140.5-03"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.test.tsx
  - src/lib/seam-wire-vocabulary.invariant.test.ts
autonomous: true
requirements: [SEAMPROSE-07, SEAMPROSE-08, SEAMPROSE-02]
must_haves:
  truths:
    - "A forwarded/codeless upstream failure on the CSV surface renders the §4a founder copy with a rendered correlation_id, exactly once — never 'Validation failed. See per-row breakdown below.'"
    - "⛔ NO caller-fault route code loses its copy: CSV_FILE_TOO_LARGE still tells the user their file is too large, CSV_PERSIST_FAIL never asserts 'Nothing was saved', CSV_RATE_LIMIT still carries its stamped wait — replayed NEGATIVE CONTROLS, per corrected CONTEXT §6c"
    - "A genuine validation verdict (200 + ok:false) still renders the row-level path (positive control)"
    - "BOTH CSV wizard clients consult the shared wire→wizard hop — the class, not the instance (DEF-140.4-C and the §6 hand-off hole are ONE defect closed by ONE artefact)"
    - "A guard holds the class: every wizard client fetching a rateLimitDenyJson route consults recogniseSeamErrorCode; roster∩wire-table answers AGREE; each CSV client's roster covers every code its route emits"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx"
      provides: "THREE-WAY arm: seam hop → route caller-fault vocabulary → §4a fallback"
      contains: "recogniseSeamErrorCode"
    - path: "src/lib/seam-wire-vocabulary.invariant.test.ts"
      provides: "derived-population hop guard + agreement property + CSV roster-completeness (derived from route source)"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx"
      to: "src/lib/seam-discriminator.ts + src/lib/wizardErrors.ts"
      via: "seamErrorCode(body) → recogniseSeamErrorCode → KNOWN_CSV_* roster → CSV_UPSTREAM_FAIL fallback"
    - from: "csv upstream-error arm"
      to: "src/components/error/ErrorEnvelope.tsx"
      via: "buildEnvelope(code, correlationId, {retryAfterSeconds}) — the shared envelope, row 1"
---

<objective>
Close DEF-140.4-C and the §6 hand-off-hole class as the ONE defect they are (CONTEXT §6c):
`CsvUploadStep` and `CsvSubmitStep` are the only two wizard clients that never call
`recogniseSeamErrorCode` — that missing hop is mechanically why a forwarded 401 renders
"Validation failed. See per-row breakdown below." with no breakdown and `correlation_id: —`.

⛔ **THE ARM IS THREE-WAY, NOT TWO-WAY — read corrected CONTEXT §6c before writing any code.**
A bare `!res.ok → §4a` discriminator would broaden the milestone's signature defect to ~30 sites:
the routes emit their OWN caller-fault non-2xx with real top-level codes — `CSV_FILE_TOO_LARGE`
(`csv-validate/route.ts:144,164`), `CSV_INVALID_FORMAT` (`:155,160,176,191` and ×20 in
`csv-finalize`), `CSV_RATE_LIMIT` (429, `Retry-After` stamped, `:218`), `CSV_SESSION_REUSED`,
`CSV_PERSIST_FAIL`, `CSV_FINALIZE_FAIL` — none in the 6-member seam table, so all would resolve
`"UNKNOWN"` and read *"this is on our side, not your data"* for an 11 MB file, and
`CSV_PERSIST_FAIL` would assert *"Nothing was saved"* where that may be affirmatively false.
RESEARCH §12.2's `!res.ok` measurement was scoped to *"bodies with NO top-level `code`"* — the
upstream-forwarded population only.

**The three-way arm on `!res.ok`** (order matches the repo's proven hop-then-roster shape,
`ConnectKeyStep.tsx:443-449` — semantically equivalent to corrected §6c's (1)/(2)/(3) listing for
every population, argued below):
1. `recogniseSeamErrorCode(seamErrorCode(data)) !== "UNKNOWN"` → the seam hop: shared
   `ErrorEnvelope` with the advertised wait. (140.5-02 adds `CSV_RATE_LIMIT → RATE_LIMITED` to
   `SEAM_CODE_TO_WIZARD_CODE`, so the rate-limit deny renders rate-limit copy WITH its stamped
   wait — the only route-vocabulary member deliberately routed here, because
   `CsvValidationEnvelope` cannot carry a wait.)
2. else if top-level `data.code` ∈ the client's hand-typed `KNOWN_CSV_*` roster → **keep today's
   path**: `CsvValidationEnvelope` with the route's own `human_message` (the caller-fault copy is
   correct TODAY; this plan must not touch it).
3. else (unrecognised or codeless) → §4a: `CSV_UPSTREAM_FAIL` founder copy via shared envelope.

Ordering equivalence: hop-first differs from §6c's roster-first only for codes present in BOTH
vocabularies — after 140.5-02's table row that set is exactly `{CSV_RATE_LIMIT}`, where hop-first
is the REQUIRED outcome (the wait must render). Every other route code is absent from the seam
table and lands in branch 2 identically.

Coverage-law rows, stated per CONTEXT §2: the hop + shared envelope are **row 1**; the
`KNOWN_CSV_*` rosters are **row 2 — hand-typed, partial by construction in those words** (the same
shape as the four existing clients' rosters), fail-louded by the guard's roster-completeness
assertion, whose population is DERIVED from the route source.

Purpose: SEAMPROSE-07 (D-§4a), SEAMPROSE-08 (§6 class), SEAMPROSE-02 (fifth surface).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-CONTEXT.md   (§4a, ⛔ CORRECTED §6c, §6e)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-RESEARCH.md   (§12 — note its §12.2 population scoping, §13, §19)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-PATTERNS.md   (§3, §5 CSV-envelope incompatibilities, §8 hop-guard analog)
@src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx
@src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx
@src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx
@src/app/api/strategies/csv-validate/route.ts
@src/app/api/strategies/csv-finalize/route.ts
@src/lib/seam-discriminator.ts
@src/lib/envelope.ts
</context>

<interfaces>
Consumed contracts (landed in wave 2 — use directly):
```ts
// 140.5-02 landed: WIZARD_ERROR_COPY.CSV_UPSTREAM_FAIL = the §4a founder copy, verbatim,
// AND the SEAM_CODE_TO_WIZARD_CODE row ["CSV_RATE_LIMIT", "RATE_LIMITED"].
// Existing, from src/lib/seam-discriminator.ts (reads flat AND nested service_error shapes):
export function seamErrorCode(body: unknown): string | null;      // :206
export function seamCorrelationId(body: unknown): string | null;  // :321
// Existing: recogniseSeamErrorCode (wizardErrors.ts), buildEnvelope (envelope.ts:75-91),
// parseRetryAfterSeconds (retry-after.ts — the ONE parser, lint-enforced).
// The hop-then-roster pattern to copy end-to-end: ConnectKeyStep.tsx:443-449 (RESEARCH §19).
```
New in this plan: `KNOWN_CSV_VALIDATE_CODES` (CsvUploadStep) and `KNOWN_CSV_FINALIZE_CODES`
(CsvSubmitStep) — `ReadonlySet<string>` (route codes are NOT WizardErrorCode; the envelope's
`code: string` prop is load-bearing, `CsvValidationEnvelope.tsx:56-58`). Members enumerated from
the two routes' comment-stripped source at edit time (`csvErrorEnvelope("…")` and sibling literal
emitters); publish the enumeration predicate + member list in the plan SUMMARY. `CSV_RATE_LIMIT`
is deliberately EXCLUDED from the rosters (branch 1 owns it — see objective).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CsvUploadStep — the three-way arm, with negative controls</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.test.tsx</files>
  <behavior>
    Upstream population (RESEARCH §12.1 — bodies with NO top-level code):
    - `withAuth` 401 `{error:"Unauthorized"}` → §4a title "We couldn't check your file just now.",
      correlation_id RENDERED where present, heading exactly ONCE, no breakdown region.
    - FastAPI 403 `{"detail":"Forbidden"}` → same §4a copy.
    - Nested `service_error` 429 `{"detail":{code:"RATE_LIMITED",…}}` + `Retry-After: 30` →
      RATE_LIMITED shared envelope WITH the wait (the hop reads the nested shape).
    - Non-JSON body (res.json() throws) → §4a copy, no crash.
    ⛔ NEGATIVE CONTROLS (corrected §6c — without these, branch 3 swallowing branch 2 is invisible
    to the suite and reachable only by live QA, which is exactly how the original defect survived):
    - `CSV_FILE_TOO_LARGE` with the route's real human_message → THAT message renders; the §4a
      title and the "not your data" sentence are ABSENT from the DOM.
    - `CSV_INVALID_FORMAT` with its route human_message → same assertions.
    - `CSV_RATE_LIMIT` 429 + `Retry-After: 30` (the rateLimitDenyJson deny) → rate-limit copy with
      the ADVERTISED WAIT rendered; §4a copy absent (the one CSV wait the route stamps is not
      dropped — must-have).
    - POSITIVE CONTROL: 200 + `ok:false` with row errors → the validation path renders.
    - Second attempt after a waited failure → no stale wait (TRAP-3 clear).
  </behavior>
  <action>
    Rewrite the `:275-284` arm as the three-way per the objective: split `if (!res.ok)` from
    `else if (data.ok === false)`; inside `!res.ok`, hop → `KNOWN_CSV_VALIDATE_CODES` roster →
    `CSV_UPSTREAM_FAIL`. Branches 1 and 3 render via `buildEnvelope` + shared `ErrorEnvelope`
    (correlation via `seamCorrelationId(data) ?? data.correlation_id ?? null`;
    `parseRetryAfterSeconds(res.headers)` into state, cleared on each fresh attempt). Branch 2
    renders today's `CsvValidationEnvelope` with the route body UNCHANGED. Enumerate the roster
    from `csv-validate/route.ts`'s comment-stripped literal emitters; publish predicate + members.
    Keep the wizard_error telemetry event firing on all arms (RESEARCH §12.3 caveat).
    TRAP-1: route the `:321` bare `console.error("…submit threw:", err)` through `scrubSeamError`
    (dependency-free leaf, importable client-side) — an INSTANCE fix; do not claim the log class.
    Prose hygiene: `:93`'s `route.ts:748` citation is wrong + 88-way-ambiguous (RESEARCH §3.9) —
    convert to a symbol-anchored reference; census MODE=full on this file → convert all reported hits.
  </action>
  <verify>
    <automated>SC-CSV-2 mutation: make branch 3 swallow `200 + ok:false` → positive control reds. SC-CSV-3 mutation: drop correlation_id from the §4a render → render assertion reds. NEGATIVE-CONTROL falsification (the B-1 shape itself): temporarily re-collapse the arm to two-way (`!res.ok` → §4a) → the CSV_FILE_TOO_LARGE and CSV_RATE_LIMIT controls MUST red; paste that output — it is the proof the checker's regression cannot ship silently. Revert all. FULL `npx vitest run` + `npx tsc --noEmit`</automated>
  </verify>
  <done>All behaviors incl. three negative controls green; two-way collapse provably reddens the suite; census reports zero bare citations in this file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CsvSubmitStep — the SECOND client, same three-way arm</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx</files>
  <behavior>
    Same three arms against `csv-finalize` (its `!res.ok` population includes the nested 424 at
    `process_key.py:1602` and nested 429 at `internal.py:246` — code at `detail.code`, which the
    old top-level read missed). ⛔ NEGATIVE CONTROLS per corrected §6c:
    - `CSV_SESSION_REUSED` with its route human_message → that message renders, §4a absent.
    - `CSV_PERSIST_FAIL` with its route human_message → that message renders, AND the sentence
      "Nothing was saved" is ABSENT from the DOM (on this code it may be affirmatively false —
      the §4a copy must be provably unreachable here).
    - `CSV_INVALID_FORMAT` (the ×20 emitter family in this route) → route message renders.
    - Codeless/nested bodies → hop or §4a as in Task 1. SC-CSV-4 subject: a 429 with
      `Retry-After` on THIS client renders the advertised wait.
  </behavior>
  <action>
    Apply Task 1's exact three-way pattern with `KNOWN_CSV_FINALIZE_CODES` (enumerate from
    `csv-finalize/route.ts`, comment-stripped; publish predicate + members). Deliberately a second
    application, not a shared two-client abstraction — the shared artefacts are the hop, the
    envelope, the copy table, and the guard; a bespoke "shared CSV error handler" would be a third
    vocabulary surface. Prose hygiene: `:56` and `:138` citations (`route.ts:748`, wrong +
    ambiguous) → symbol-anchored.
  </action>
  <verify>
    <automated>SC-CSV-1 mutation (⭐ the SECOND client): remove the hop, leaving a top-level read → Task 3's class guard reds NAMING CsvSubmitStep.tsx AND this file's nested-body tests red (run after Task 3 lands; paste then). SC-CSV-4 mutation: drop `retryAfterSeconds` from THIS client's buildEnvelope context → its wait-render test reds. FULL `npx vitest run`</automated>
  </verify>
  <done>Both clients hop; CSV_PERSIST_FAIL provably never renders "Nothing was saved"; SC-CSV-1 + SC-CSV-4 observed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The class guard — hop coverage + agreement + roster-completeness, populations derived</name>
  <files>src/lib/seam-wire-vocabulary.invariant.test.ts, src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx, src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.test.tsx</files>
  <behavior>
    Guard (analog: `seam-ratelimit-posture.invariant.test.ts` — PATTERNS §8; comment-strip is
    load-bearing, its own registry note records why):
    - Route side: derive from disk the `src/app/api/**/route.ts` set whose COMMENT-STRIPPED source
      calls `rateLimitDenyJson`. Population measured **19 files** at HEAD (predicate: the
      comment-stripped call-site scan — a raw grep reads higher because admin/match routes mention
      the symbol in prose; publish the predicate beside the count). Floor ≥12 (~60%).
    - Client side: derive the non-test wizard step files whose comment-stripped source fetches one
      of those routes (URL literals via `wizardFetch` — needle-choice lesson at
      `contracts-registry.test.ts:188`). Population measured **5 clients** at HEAD: ConnectKey,
      SyncPreview, MultiKey, CsvUpload, CsvSubmit — ⚠️ `SubmitStep` is EXCLUDED because
      `finalize-wizard` throttles via `checkLimit`, not `rateLimitDenyJson`. Floor ≥4.
    - ⚠️ DOCBLOCK-DECLARED BLINDNESS: routes throttling via `checkLimit`/`withAuthLimited` only
      (live example: `finalize-wizard`) are OUTSIDE the derived population — this guard says
      nothing about their clients. Partial for that family, in those words.
    - Assert: every derived client references `recogniseSeamErrorCode` (comment-stripped — four
      step files mention it in docblocks; unstripped = DEF-16-2). Failure message NAMES the file.
    - Agreement (§13.1): parse the KNOWN_* roster literals from step-file source (textual, via
      source-scan; do NOT export module-local rosters just for a test), intersect with
      `SEAM_CODE_TO_WIZARD_CODE`'s domain, assert each overlapping code answers identically on
      both sides. Hand-typed oracle: expected overlap exactly {RATE_LIMITED, SEAM_MISCONFIGURED}
      for the pre-existing rosters; the CSV rosters expect ∅ overlap (CSV_RATE_LIMIT deliberately
      excluded from them). A new overlap → fail with "extend the hand-typed oracle deliberately".
    - CSV roster-completeness (the row-2 rosters' fail-loud): derive each CSV route's emitted code
      literals from its comment-stripped source and assert the corresponding KNOWN_CSV_* roster
      contains every one (CSV_RATE_LIMIT exempted by name, reason inline). Floor: ≥4 distinct
      codes per route (measure at edit time; publish the predicate in the failure message).
    - Both-polarity needle self-tests for every needle.
    Double-render pin (D-§4a bullet 3): CsvValidationEnvelope with `errors.length === 0` and a
    code absent from the copy table renders the heading EXACTLY once; positive counterpart with
    row errors renders heading once + breakdown region. Analog: `LiveRegion.test.tsx`.
  </behavior>
  <action>
    Build using `stripCommentsPreserveLines` (140.5-01). Land AFTER Tasks 1-2 (fix before guard —
    a guard landed first is red on arrival). Add the pin to `CsvValidationEnvelope.test.tsx`;
    touch the component only if the pin exposes a live defect. Then run SC-CSV-1 (Task 2) and
    paste the guard's failure output. Registration in CONTRACT_GUARDS owed by 140.5-08 (`## OPEN`).
  </action>
  <verify>
    <automated>SC-AGREE-1: edit a roster literal so an overlapping code disagrees with the wire table (production step-file edit, tsc-clean) → agreement assertion reds. SC-CSV-5: re-introduce the duplicate render in CsvValidationEnvelope.tsx (heading also rendered as cause when errors.length===0 — the 140.4 regression shape) → pin reds. Paste both, revert both, porcelain clean. FULL `npx vitest run` + Node 22 parity + `npm run lint` (wave-merge gate)</automated>
  </verify>
  <done>Guard green with published populations (19 routes / 5 clients) and floors; blindness declared; SC-AGREE-1 + SC-CSV-5 observed; pin green both polarities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream body → wizard UI | untrusted wire data; code read via discriminator functions, never trusted casts |
| caught error → console | transport errors can embed the user's live JWT (TRAP-1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.5-11 | Spoofing | CSV three-way arm | mitigate | wire code via seamErrorCode + Map lookup + hand-typed roster; unknown → CSV_UPSTREAM_FAIL, never cast |
| T-140.5-12 | Info Disclosure | CsvUploadStep :321 log | mitigate | scrubSeamError wraps the caught error |
| T-140.5-13 | Info Disclosure | per-row breakdown | transfer | data half NOT forwarded (PII fence, QA ISSUE-005); copy fixed in 140.5-02; carry-forward in OPEN |
| T-140.5-21 | Repudiation | fault attribution | mitigate | negative controls pin caller-fault copy per code; "Nothing was saved" provably unreachable on CSV_PERSIST_FAIL |
</threat_model>

<verification>
Full suite after every task commit; Node 22 parity + lint + tsc at wave merge. "Nothing was saved"
truth at the §4a arm: receipt is RESEARCH §12.3's three-layer verification — cite it; the honest
caveats (PostHog telemetry, strategy name in localStorage) go in the SUMMARY verbatim. The §4a copy
is reachable ONLY from branch 3 — the negative controls are what make that a tested property
instead of an intention.
</verification>

<success_criteria>
- The live-QA symptom is unreproducible AND no caller-fault regression shipped in its place: all
  upstream behaviors + all five negative controls (TOO_LARGE, INVALID_FORMAT, RATE_LIMIT,
  SESSION_REUSED, PERSIST_FAIL) green; two-way collapse provably reddens the suite.
- BOTH clients hop; guard holds hop + agreement + roster-completeness over derived populations
  with published predicates (19 routes / 5 clients / ≥4 codes per CSV route).
- CSV surface carries the advertised wait (SC-CSV-4) via the shared envelope.
- SC-CSV-1, SC-CSV-2, SC-CSV-3, SC-CSV-4, SC-CSV-5, SC-AGREE-1 all Observed with pasted evidence.
</success_criteria>

<output>
Create `.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-05-SUMMARY.md`.
`## OPEN` must name: (a) CONTRACT_GUARDS registration owed by 140.5-08; (b) per-row-breakdown DATA
half + QA ISSUE-005 carried forward (PII fence); (c) the .tsx log-roster class open (instance
scrubbed only); (d) the guard's declared blindness to checkLimit/withAuthLimited-only routes.
Do NOT touch STATE.md, ROADMAP.md, DESIGN.md.
</output>
