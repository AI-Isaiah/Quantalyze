---
phase: 140.5-seamprose
plan: 05
type: execute
wave: 3
depends_on: ["140.5-01", "140.5-02", "140.5-03"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.test.tsx
  - src/lib/seam-wire-vocabulary.invariant.test.ts
autonomous: true
requirements: [SEAMPROSE-07, SEAMPROSE-08, SEAMPROSE-02]
must_haves:
  truths:
    - "A forwarded 401/403/404/409/5xx on the CSV surface renders the §4a founder copy with a rendered correlation_id, exactly once — never 'Validation failed. See per-row breakdown below.'"
    - "A genuine validation verdict (200 + ok:false) still renders the row-level path (positive control)"
    - "BOTH CSV wizard clients consult the shared wire→wizard hop — the class, not the instance (DEF-140.4-C and the §6 hand-off hole are ONE defect closed by ONE artefact)"
    - "A 429 with Retry-After on the CSV surface renders the advertised wait (the fifth Retry-After surface, closed by routing onto the shared ErrorEnvelope)"
    - "A guard holds the class: every wizard client fetching a rateLimitDenyJson route consults recogniseSeamErrorCode, and roster∩wire-table answers AGREE"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx"
      provides: "res.ok discriminator + seam hop + shared ErrorEnvelope on the error arm"
      contains: "recogniseSeamErrorCode"
    - path: "src/lib/seam-wire-vocabulary.invariant.test.ts"
      provides: "derived-population hop guard + roster/wire-table agreement property"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx"
      to: "src/lib/seam-discriminator.ts + src/lib/wizardErrors.ts"
      via: "seamErrorCode(body) → recogniseSeamErrorCode → CSV_UPSTREAM_FAIL fallback (reads flat AND nested shapes)"
    - from: "csv error arm"
      to: "src/components/error/ErrorEnvelope.tsx"
      via: "buildEnvelope(code, correlationId, {retryAfterSeconds}) — the shared envelope, row 1"
---

<objective>
Close DEF-140.4-C and the §6 hand-off-hole class as the ONE defect they are (CONTEXT §6c, reached
independently by both analysis agents): `CsvUploadStep` and `CsvSubmitStep` are the only two wizard
clients that never call `recogniseSeamErrorCode` — that missing hop is mechanically why a forwarded
401 renders "Validation failed. See per-row breakdown below." with no breakdown and `correlation_id: —`.

Coverage-law row, stated per CONTEXT §2: **row 1** — the fix routes BOTH clients through the shared
hop and the error arm onto the shared `ErrorEnvelope` (which closes the CSV `Retry-After` gap in the
same edit — CONTEXT §6c names extending `CsvValidationEnvelope`'s props as the row-2 alternative,
rejected). The class is then HELD by a derived-population guard (§3: a grep proves a state; only a
guard proves it is held). ⚠️ A plan editing only `CsvUploadStep` closes 1 of 2 — the exact
instance-not-class shape this milestone exists to stop; this plan edits both, and the guard derives
the population so client #7 is covered on arrival.

The discriminator needs NO new protocol — it is already on the wire (RESEARCH §12.2): a genuine
validation verdict arrives **200 with `ok:false`**; every non-validation upstream status arrives
**`!res.ok`**. No status roster.

Purpose: SEAMPROSE-07 (D-§4a), SEAMPROSE-08 (§6 class), SEAMPROSE-02 (fifth surface).
Output: both CSV clients honest + hop/agreement guard (registration owed to 140.5-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-CONTEXT.md   (§4a, §6c, §6e)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-RESEARCH.md   (§12 whole section, §13, §19)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-PATTERNS.md   (§3, §5 CSV-envelope incompatibilities, §8 hop-guard analog)
@src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx
@src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx
@src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx
@src/lib/seam-discriminator.ts
@src/lib/envelope.ts
</context>

<interfaces>
Consumed contracts (landed in wave 2 — use directly, no exploration needed):
```ts
// 140.5-02 landed: WIZARD_ERROR_COPY.CSV_UPSTREAM_FAIL = the §4a founder copy, verbatim.
// Existing, from src/lib/seam-discriminator.ts (reads flat AND nested service_error shapes):
export function seamErrorCode(body: unknown): string | null;      // :206
export function seamCorrelationId(body: unknown): string | null;  // :321
// Existing, from src/lib/wizardErrors.ts:
export function recogniseSeamErrorCode(code: string | null | undefined): WizardErrorCode;
// Existing, from src/lib/envelope.ts (:75-91): buildEnvelope(code, correlationId, {retryAfterSeconds})
// The hop pattern to copy end-to-end: ConnectKeyStep.tsx:443-449 (quoted in RESEARCH §19).
```
The error arm renders `src/components/error/ErrorEnvelope.tsx` (shared, carries wait + cause);
the validation arm keeps `CsvValidationEnvelope` (whose props CANNOT carry a wait — PATTERNS §5
lists the three load-bearing incompatibilities; that is WHY the arm split is the design).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CsvUploadStep — discriminate, hop, render the shared envelope</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.test.tsx</files>
  <behavior>
    Replay the measured fall-through population (RESEARCH §12.1) as component tests:
    - `withAuth` 401 `{error:"Unauthorized"}` → §4a title "We couldn't check your file just now.",
      correlation_id RENDERED (from the response when present; the §4a defect included `— `), the
      heading appears EXACTLY ONCE in the DOM, and NO per-row breakdown region renders.
    - FastAPI 403 `{"detail":"Forbidden"}` → same copy (ONE message for every status — D-§4a).
    - Nested `service_error` 429 `{"detail":{code:"RATE_LIMITED",…}}` + `Retry-After: 30` →
      RATE_LIMITED envelope WITH the advertised wait (the hop reads the nested shape; `data.code`
      cannot).
    - Non-JSON body (res.json() throws) → §4a copy, no crash.
    - POSITIVE CONTROL (SC-CSV-2's subject): 200 + `ok:false` with row errors → the validation path
      renders, NOT the upstream sentence.
    - Second attempt after a waited failure → no stale wait (TRAP-3 clear).
  </behavior>
  <action>
    Rewrite the `:275-284` arm: split `if (!res.ok)` (upstream population) from
    `else if (data.ok === false)` (validation population). On `!res.ok`:
    `recogniseSeamErrorCode(seamErrorCode(data))`, falling back to `CSV_UPSTREAM_FAIL` on
    `"UNKNOWN"`; correlation via `seamCorrelationId(data)`; `parseRetryAfterSeconds(res.headers)`
    into state, cleared on each fresh attempt; render via `buildEnvelope` + shared `ErrorEnvelope`.
    The validation arm keeps `CsvValidationEnvelope` unchanged in shape. Keep the wizard_error
    telemetry event firing on both arms (RESEARCH §12.3 caveat — telemetry, not user data).
    TRAP-1: route the `:321` bare `console.error("…submit threw:", err)` through `scrubSeamError`
    (seam-redaction is a dependency-free leaf, importable client-side) — CONTEXT §6e names this log
    site as invisible to the log-coverage roster; scrubbing it here closes the leak without
    widening any roster (do NOT claim the log CLASS closed — say so in SUMMARY).
    Prose hygiene: `:93`'s citation `route.ts:748` is WRONG (88-way-ambiguous basename; real
    CSV_INVALID_FORMAT sites enumerated in RESEARCH §3.9) — convert to a symbol-anchored reference
    into `csv-finalize/route.ts`; run the census MODE=full on this file and convert every reported
    bare citation.
  </action>
  <verify>
    <automated>SC-CSV-2 mutation: widen the discriminator to also swallow `200 + ok:false` → the positive control reds. SC-CSV-3 mutation: drop correlation_id from the rendered §4a fields → the render assertion reds. Paste both, revert both. FULL `npx vitest run` + `npx tsc --noEmit`</automated>
  </verify>
  <done>All six behaviors green; both mutations observed; census reports zero bare citations in this file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CsvSubmitStep — the SECOND client (the one the hand-off hole eats)</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx</files>
  <behavior>
    Same discriminator/hop/envelope behaviors as Task 1, against `csv-finalize` (its non-2xx
    population includes the nested 424 at `process_key.py:1602` and nested 429 at
    `internal.py:246` — both carry `code` at `detail.code`, which the old top-level read missed).
    Positive control: a genuine finalize verdict body still takes its existing path.
  </behavior>
  <action>
    Apply Task 1's exact pattern to `CsvSubmitStep.tsx`'s fetch-error arm. This is deliberately a
    SECOND application of the same edit, not a shared abstraction-for-two — the shared artefacts
    are the hop + envelope + guard; the guard (Task 3) is what makes the class hold, per the
    coverage law (a hand-rolled "shared CSV error handler" would be a third vocabulary surface).
    Prose hygiene: `:56` and `:138` both cite `route.ts:748` (wrong + ambiguous — RESEARCH §3.9);
    convert as in Task 1.
  </action>
  <verify>
    <automated>SC-CSV-1 mutation (⭐ the SECOND client — the ledger's chosen member): remove the hop from CsvSubmitStep, leaving a top-level `data.code` read → Task 3's class guard reds NAMING CsvSubmitStep.tsx AND this file's replay tests red. (Run after Task 3 lands; sequence the ledger paste then.) FULL `npx vitest run`</automated>
  </verify>
  <done>Both clients hop; SC-CSV-1 observed post-Task-3 with the guard's failure message naming the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The class guard — hop coverage + agreement, populations derived</name>
  <files>src/lib/seam-wire-vocabulary.invariant.test.ts, src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx, src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.test.tsx</files>
  <behavior>
    Guard (analog: `seam-ratelimit-posture.invariant.test.ts` — PATTERNS §8, copy its per-arm
    honesty and comment-strip):
    - Route side: derive from disk the set of `src/app/api/**/route.ts` whose COMMENT-STRIPPED
      source calls `rateLimitDenyJson` (measured 23 sites / 22 files at `4c60518b`; floor ≥15 —
      the posture guard's registry note records why comment-strip is load-bearing here:
      admin/match routes mention the symbol in PROSE).
    - Client side: derive the set of non-test wizard step files whose comment-stripped source
      fetches one of those routes (URL literals via wizardFetch — the inverted-allow-list idiom;
      ⚠️ read `contracts-registry.test.ts:188`'s re-tiering note first: a `/\bfetch\s*\(/` needle
      was once blind against `wizardFetch(` — needle choice is the lesson).
    - Assert: every derived client's comment-stripped source references `recogniseSeamErrorCode`
      (four files mention it in DOCBLOCKS — an unstripped scan is DEF-16-2-vulnerable and the
      posture guard's history proves it). Floors: ≥5 routes matched to clients, ≥6 client files
      scanned. Failure message NAMES the offending file and the required hop.
    - Agreement (§13.1, the property stated precisely in RESEARCH): parse the KNOWN_* roster
      literals from the step files' source (textual parse via source-scan — the rosters are
      module-local consts; do NOT export them just for the test), intersect with
      `SEAM_CODE_TO_WIZARD_CODE`'s domain, and assert each overlapping code maps to the SAME
      wizard code on both sides. Hand-typed oracle: the expected overlap is exactly
      {RATE_LIMITED, SEAM_MISCONFIGURED} with their literal expected answers (TWO overlaps —
      CONTEXT names one; RESEARCH §13.1 measured both). If a THIRD overlap appears the guard fails
      with "extend the hand-typed oracle deliberately" — never derive both sides (Oracle hazard #5).
    - Both-polarity needle self-tests for every needle above.
    Double-render pin (D-§4a bullet 3, RESEARCH §12.5): `CsvValidationEnvelope` with
    `errors.length === 0` and a code absent from the copy table renders the heading text EXACTLY
    once (the 140.4-16/CR-02 fix exists; the PIN is what is missing); positive counterpart: with
    row errors, heading once AND the breakdown region renders. Analog: `LiveRegion.test.tsx`
    (registered with exactly this hazard in its registry note).
  </behavior>
  <action>
    Build the guard per the behavior block using `stripCommentsPreserveLines` from 140.5-01.
    Land it AFTER Tasks 1-2 in this plan (PATTERNS §8: land the FIX before the GUARD, or both —
    a guard landed first is red on arrival). Add the double-render pin to
    `CsvValidationEnvelope.test.tsx`; touch `CsvValidationEnvelope.tsx` itself only if the pin
    exposes a live defect (none expected — the fix landed in 140.4-16).
    Then run the SC-CSV-1 mutation from Task 2 and paste the guard's failure output.
    Registration in CONTRACT_GUARDS owed by 140.5-08 — name it in `## OPEN`.
  </action>
  <verify>
    <automated>SC-AGREE-1 (planner-added row): edit a roster literal so an overlapping code disagrees with the wire table (production edit in a step file; tsc-clean) → agreement assertion reds. SC-CSV-1 → hop assertion reds naming CsvSubmitStep.tsx. Paste both, revert both, porcelain clean. FULL `npx vitest run` + Node 22 parity + `npm run lint` (wave-merge gate)</automated>
  </verify>
  <done>Guard green with non-vacuous floors; both mutations observed; double-render pin green in both polarities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream body → wizard UI | body is untrusted wire data; code read via discriminator functions, never trusted casts |
| caught error → console | transport errors can embed the user's live JWT (TRAP-1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.5-11 | Spoofing | CSV error arm | mitigate | wire code only via seamErrorCode + Map lookup; unknown → CSV_UPSTREAM_FAIL, never cast to WizardErrorCode |
| T-140.5-12 | Info Disclosure | CsvUploadStep :321 log | mitigate | scrubSeamError wraps the caught error before logging |
| T-140.5-13 | Info Disclosure | per-row breakdown | transfer | data half NOT forwarded (PII fence, QA ISSUE-005) — copy fixed in 140.5-02; carry-forward named in OPEN |
</threat_model>

<verification>
Full suite after every task commit; Node 22 parity + lint + tsc at wave merge. "Nothing was saved"
at this arm: receipt is RESEARCH §12.3's three-layer verification (route: zero writers; Python:
`_run_validate_only` docstring verified against its body; row creation only at finalize) — cite it;
the honest caveats (PostHog telemetry event, strategy name in browser localStorage) go in the
SUMMARY verbatim so a reviewer finds them named, not discovered.
</verification>

<success_criteria>
- The live-QA symptom is unreproducible: a 401 upload renders founder copy + correlation_id, once,
  no phantom breakdown (component replay tests, all six behaviors).
- BOTH clients hop; the guard holds the class over a derived population with published floors.
- CSV surface carries the advertised wait via the shared envelope (row 1 — the row-2 props-extension
  alternative rejected, stated).
- SC-CSV-1, SC-CSV-2, SC-CSV-3, SC-AGREE-1 all Observed with pasted evidence.
</success_criteria>

<output>
Create `.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-05-SUMMARY.md`.
`## OPEN` must name: (a) CONTRACT_GUARDS registration of `seam-wire-vocabulary.invariant.test.ts`
owed by 140.5-08; (b) the per-row-breakdown DATA half + QA ISSUE-005 carried forward (PII fence) —
for 140.5-08's deferred-items consolidation; (c) the CsvUploadStep log site scrubbed as an INSTANCE
— the .tsx-files-not-on-the-log-roster class remains open (CONTEXT §6e), named not claimed.
Do NOT touch STATE.md, ROADMAP.md, DESIGN.md.
</output>
