---
phase: 140.5-seamprose
plan: 02
type: execute
wave: 2
depends_on: ["140.5-01"]
files_modified:
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.test.ts
  - src/lib/analytics-client.ts
  - src/lib/analytics-client.test.ts
  - src/lib/seam-venue-vocabulary.invariant.test.ts
autonomous: true
requirements: [SEAMPROSE-03, SEAMPROSE-07]
must_haves:
  truths:
    - "The founder-approved §4a copy exists verbatim under CSV_UPSTREAM_FAIL and is the ONE message for every unrecognised/codeless upstream failure (D-§4a; the caller-fault route vocabulary keeps its own copy — corrected §6c)"
    - "CSV_VALIDATION_FAILED no longer promises a per-row breakdown that never renders (D-§4a bullet 4, RESEARCH §12.4)"
    - "CSV_RATE_LIMIT maps to RATE_LIMITED in the seam table, so the CSV rate-limit deny can carry its stamped wait through the shared envelope (interface for 140.5-05)"
    - "An AnalyticsTimeoutError classifies as SERVICE_UNREACHABLE, not UNKNOWN/500 (B-02 closed by TYPE, not substring)"
    - "MISSING_SCOPE / PERMISSION_DENIED / WITHDRAW_SCOPE / TRADE_SCOPE reach honest caller-fault verdicts instead of UNKNOWN/500 or a false IP-allowlist claim"
    - "Every code the Python services actually EMIT has an explicit TS disposition, guarded against the real emitters — not against a prose enumeration"
  artifacts:
    - path: "src/lib/wizardErrors.ts"
      provides: "§4a copy entry, CSV_RATE_LIMIT table row, B-02 type-first branch, four venue-table rows, two honest new copy entries"
      contains: "We couldn't check your file just now."
    - path: "src/lib/seam-venue-vocabulary.invariant.test.ts"
      provides: "Python EMITTER-derived ↔ TS disposition parity guard"
  key_links:
    - from: "src/lib/analytics-client.ts"
      to: "src/lib/wizardErrors.ts classifyKeyValidationError"
      via: "own-property marker on AnalyticsTimeoutError read by the type-first branch (mock-survival idiom, never instanceof)"
    - from: "analytics-service/services/*.py error_code literal EMITTERS"
      to: "VENUE_WIRE_CODE_TO_VERDICT"
      via: "parity guard parsing the real assignments from disk via source-scan (NOT adapter.py's comment enumeration)"
---

<objective>
Own `wizardErrors.ts` for the phase: land the §4a vocabulary (the interface plan 140.5-05
consumes), stop the CSV copy lying about a breakdown, close B-02 by type, and give the orphaned
venue wire codes honest verdicts — with an EMITTER-derived guard so a newly-emitted code cannot
arrive without a disposition.

Coverage-law rows, stated per CONTEXT §2: the §4a copy entry is **row 1** (one chokepoint code for
every unrecognised/codeless upstream failure — D-§4a as scoped by corrected §6c; the route's own
caller-fault vocabulary is NOT this entry's population). B-02's type-first branch is **row 1**
(shared classifier artefact; RESEARCH §6.2 option b). The venue-table rows are **row 2 — a
hand-typed roster, partial by construction in those words**; the parity guard does NOT make it
row 1 — what it adds is fail-loud arrival: its population is derived from the REAL Python emitters,
so a new emitted code reddens CI until someone writes its disposition. (Checker B-2: the prior
"effective row 1" claim was false and is withdrawn; deriving from `adapter.py`'s COMMENT would
have been an empty set under comment-strip AND misses `csv_adapter.py`'s `CSV_TOO_LARGE` /
`CSV_FORMAT_UNSUPPORTED` — a prose roster is row 2.)

Purpose: SEAMPROSE-03 (WP-10) + the copy half of SEAMPROSE-07 (DEF-140.4-C).
Output: wizardErrors vocabulary complete + emitter parity guard (registration owed to 140.5-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-CONTEXT.md   (§4a verbatim copy, ⛔ corrected §6c, §6d, §6e)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-RESEARCH.md   (§6, §7.2, §12.4, §19)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-PATTERNS.md   (§4 copy-home rule, §3)
@src/lib/wizardErrors.ts
@src/lib/analytics-client.ts
@analytics-service/services/exchange.py
@analytics-service/services/ingestion/csv_adapter.py
</context>

<interfaces>
Contract this plan PUBLISHES for 140.5-05 (CSV clients) — frozen once landed:

```ts
// src/lib/wizardErrors.ts — CSV_UPSTREAM_FAIL becomes THE unrecognised-upstream code.
// Its WIZARD_ERROR_COPY entry carries the §4a founder copy VERBATIM.
// 140.5-05 routes every unrecognised-or-codeless `!res.ok` outcome onto it (three-way arm).
CSV_UPSTREAM_FAIL: { title: "We couldn't check your file just now.", cause: …, fix: […], docsHref: …, actions: ["clear_and_retry", "request_call"] }
// AND one new seam-table row (the CSV surface's rate-limit deny is the RATE_LIMITED fact
// under its surface-local name — explicit row, never an identity rule):
SEAM_CODE_TO_WIZARD_CODE: + ["CSV_RATE_LIMIT", "RATE_LIMITED"]
```

Decision, argued (planner discretion under D-§4a): REUSE `CSV_UPSTREAM_FAIL` rather than mint a new
code. It already exists, already means "upstream failure on this surface", and already has emitters
at `csv-validate/route.ts:291,:353` — a second code for the same fact is exactly the
two-names-one-fact drift `seam-copy.ts` exists to prevent. PATTERNS §4's "avoid the CSV_ prefix"
advice loses to reuse-over-rename (Rule 3, surgical changes); recorded deliberately. The copy home
is `WIZARD_ERROR_COPY`, NOT `seam-copy.ts` — locked at CONTEXT §6d.3 (the `EXPECTED_EXPORTS` pin in
`seam-copy.purity.test.ts` freezes that leaf at exactly one export, and a string leaf cannot supply
the `cause` field `CsvValidationEnvelope.tsx:59-76` renders).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: §4a founder copy verbatim + CSV_RATE_LIMIT table row + stop the per-row-breakdown lie</name>
  <files>src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts</files>
  <behavior>
    - `WIZARD_ERROR_COPY.CSV_UPSTREAM_FAIL.title === "We couldn't check your file just now."` (literal in the test, not imported — Oracle Independence).
    - Its cause/fix carry the founder sentences: "This is on our side, not your data. Nothing was
      saved." and "Try again in a moment — if it keeps happening, send us this reference." — split
      across cause/fix WITHOUT rewording (D-§4a: verbatim).
    - `actions` includes `"clear_and_retry"` (the copy says "try again in a moment" — polarity
      re-derived per PATTERNS §4) and keeps `"request_call"` as the "send us this reference"
      affordance.
    - `recogniseSeamErrorCode("CSV_RATE_LIMIT") === "RATE_LIMITED"` (new explicit table row — the
      docblock rule at `wizardErrors.ts:1599-1608` stands: explicit rows, never identity casts).
    - `WIZARD_ERROR_COPY.CSV_VALIDATION_FAILED.title` no longer contains "per-row breakdown" (a
      promise-pin test asserts the ABSENCE of that phrase and the presence of the replacement).
  </behavior>
  <action>
    Replace `CSV_UPSTREAM_FAIL`'s entry (`wizardErrors.ts:829-838`) with the §4a founder copy,
    mapped: title = heading sentence; cause = "This is on our side, not your data. Nothing was
    saved."; fix = ["Try again in a moment — if it keeps happening, send us this reference."].
    `correlation_id` is rendered by the envelope component, not embedded in copy — keep the copy's
    dynamic-value count at ONE (PATTERNS §9 static-copy rule). ⚠️ Scope note from corrected §6c:
    this entry's population is the UNRECOGNISED/CODELESS upstream failure only — the route's own
    caller-fault codes (CSV_FILE_TOO_LARGE, CSV_INVALID_FORMAT, CSV_PERSIST_FAIL, …) keep their
    own copy and never reach it; 140.5-05 enforces that with negative controls. "Nothing was
    saved" is TRUE at the csv-validate arm — verified at three layers in RESEARCH §12.3; cite that
    in the entry's docblock (telemetry caveat included).
    Add the `["CSV_RATE_LIMIT", "RATE_LIMITED"]` row to `SEAM_CODE_TO_WIZARD_CODE` with a docblock
    note in the table's existing style (an alias: surface-local name for the same fact — the
    UPSTREAM_TIMEOUT precedent).
    Then fix `CSV_VALIDATION_FAILED` (`:817-828`): the per-row breakdown NEVER renders on either
    arm (RESEARCH §12.4). Per the locked recommendation: fix the COPY — new title/fix describe
    what the user actually sees without promising a breakdown. Claude-drafted per DESIGN.md §Voice
    (read it first; do not edit it); founder review owed per CONTEXT §8.1 — say so in SUMMARY.
    The DATA half (forwarding violations = the QA ISSUE-005 PII surface) is NOT done here — record
    in `## OPEN` for 140.5-08's deferred-items consolidation.
    Prose hygiene in owned files: "sixteen" at `wizardErrors.ts:21` and `analytics-client.ts:42,:187`
    — delete the integer, name the predicate ("every route test that mocks a seam client
    wholesale", RESEARCH §3.10). Convert bare `file:line` citations reported by
    `MODE=full node .planning/phases/140.5-*/140.5-citation-census.mjs src/lib/wizardErrors.ts src/lib/analytics-client.ts`
    to symbol-anchored references.
  </action>
  <verify>
    <automated>SC-COPY-1 mutation: restore "Validation failed. See per-row breakdown below." as the title → promise-pin test reds (paste evidence, revert). Full `npx vitest run` + `npx tsc --noEmit`</automated>
  </verify>
  <done>Founder copy verbatim under CSV_UPSTREAM_FAIL; CSV_RATE_LIMIT row landed; no copy on this surface promises a breakdown; ledger row observed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: B-02 — close the dead timeout branch by TYPE, not substring</name>
  <files>src/lib/analytics-client.ts, src/lib/analytics-client.test.ts, src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts</files>
  <behavior>
    - Replay of the three real analytics-client messages (RESEARCH §6.1, executed): an
      `AnalyticsTimeoutError` ("Analytics service timed out after 15000ms on /process-key") and the
      not-reachable error now classify to `SERVICE_UNREACHABLE` (copy final at
      `wizardErrors.ts:1097-1109`) instead of `UNKNOWN`/500.
    - The classification survives `vi.mock("@/lib/analytics-client")` wholesale (the mock-survival
      constraint documented at `wizardErrors.ts:1500-1509`).
    - `wizardErrors.test.ts:611`'s pinned string `"connect ETIMEDOUT 10.0.0.1:443"` is RE-POINTED
      in the same commit — it pins a raw undici string that can never reach the classifier
      (RESEARCH §6.2); leaving it keeps the suite reading as covering a dead arm.
  </behavior>
  <action>
    In `analytics-client.ts`: assign an own property in `AnalyticsTimeoutError`'s constructor (and
    the not-reachable error class if distinct) exactly as `seamCode` is assigned — copy the idiom,
    do not invent a second marker convention. In `wizardErrors.ts`: add a type-first read of that
    own property in `classifyKeyValidationError` BEFORE the substring cascade, alongside the
    existing `seamCode` read at `:1507-1516` (NEVER `instanceof` — the docblock there explains
    why). Map to `SERVICE_UNREACHABLE`. Do NOT copy `SERVICE_UNAVAILABLE_RETRY`'s "Nothing was
    submitted" — knowable for a declined breaker request, FALSE for a timed-out one
    (`wizardErrors.ts:1085-1096`; 140.3-12 fixed this once). Keep the `timeout|etimedout`
    substring branch as last-resort for raw syscall strings; re-run the collision-invariant list
    at `:1545-1555` for the new branch (re-derive, don't copy — RESEARCH §11.5).
  </action>
  <verify>
    <automated>SC-B02-1 (mechanism adjusted, spirit kept — note in ledger): remove the type-first branch from production `wizardErrors.ts` → the replay test reds; the seeded "re-narrow the substring" wording no longer applies because the fix is not substring-carried. Paste evidence, revert. Full `npx vitest run`</automated>
  </verify>
  <done>All three real messages classify to SERVICE_UNREACHABLE (replay test green); :611 re-pointed; ledger row observed with the mechanism note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Venue table — four honest rows + the EMITTER-derived parity guard</name>
  <files>src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts, src/lib/seam-venue-vocabulary.invariant.test.ts</files>
  <behavior>
    - `MISSING_SCOPE` → NEW `KEY_MISSING_READ_SCOPE` (caller fault, 400): the key lacks a required
      read scope and how to grant it — never "our team has been notified" (renders UNKNOWN/500
      today for a fixable Deribit scope; RESEARCH §7.2, the DOGFOOD-3 dead end).
    - `PERMISSION_DENIED` → NEW `KEY_PERMISSION_DENIED` (caller fault, 400): copy names BOTH
      possible causes (scope OR IP allowlist) without asserting either — today it renders 502
      `KEY_IP_ALLOWLIST`, a server status asserting one of two causes (TRAP-3).
    - `WITHDRAW_SCOPE` → existing `KEY_HAS_WITHDRAW_PERMS` / 400 (today: copy says *trading*).
    - `TRADE_SCOPE` → existing `KEY_HAS_TRADING_PERMS` / 400 (today correct only by
      substring-order accident — make it correct by table).
    - Parity guard, ⛔ derived from the REAL EMITTERS (checker B-2 — `adapter.py:108-112` is a
      COMMENT; comment-stripping it yields an EMPTY set and the guard would be born red on its own
      vacuity fence): parse, comment-stripped via source-scan, every literal `error_code`
      assignment across `analytics-service/services/**/*.py` in its three measured forms —
      `error_code = "X"`, `result["error_code"] = "X"`, and `error_code="X"` kwargs. Verified at
      HEAD this yields a genuine multi-form set (11 distinct subscript-assignments in
      `exchange.py` alone, plus `MISSING_SCOPE`/`MT5_*`/`CSV_TOO_LARGE`/`CSV_FORMAT_UNSUPPORTED`
      elsewhere — executor publishes the exact predicate + full member list in the SUMMARY).
      Every derived code must have a disposition: a `VENUE_WIRE_CODE_TO_VERDICT` row OR membership
      in a hand-typed exemption set with a per-entry reason. Vacuity fence: derived set size ≥ 10
      (~60% of the measured population; state the predicate in the failure message). ⚠️ DECLARED
      BLIND SPOT in the guard docblock: `csv_adapter.py:172`'s `error_code = first_rule.upper()`
      is DYNAMIC — an open code family no static derivation can enumerate; its disposition is
      by-family (the CSV surface renders those via its route vocabulary), stated with the reason.
  </behavior>
  <action>
    Add the four rows to `VENUE_WIRE_CODE_TO_VERDICT` (`wizardErrors.ts:1422-1438`) and the two
    new `WizardErrorCode` members + copy entries (all `WizardErrorCopy` fields required; re-derive
    the `actions` polarity per entry — a scope fix is `try_another_key`-shaped). Unit tests replay
    the REAL Python detail strings from `exchange.py` (quoted in RESEARCH §7.2) with the
    own-property seamCode set — assert the table answers, not the cascade. Codes derived without a
    verdict row (`UNSUPPORTED_EXCHANGE`, `VALIDATION_UNEXPECTED`, MT5 codes, csv_adapter statics,
    and whatever else the derivation reports) go in the exemption set — for each, VERIFY what the
    classifier actually does with its real detail string before writing the reason (measure, do
    not guess; RESEARCH §7.2 covers four). New copy entries are Claude-drafted → founder review
    owed (CONTEXT §8.1); say so in SUMMARY. Guard registration owed by 140.5-08 (`## OPEN`).
  </action>
  <verify>
    <automated>SC-VENUE-1 mutation: delete the WITHDRAW_SCOPE row (the SECOND scope member) → its replay test reds. SC-VENUE-2 mutation (RE-SEATED per checker B-2 — the prior comment-mutation was inadmissible under VALIDATION rules): change ONE real emitter literal in `exchange.py` (e.g. `result["error_code"] = "DDOS_PROTECTION"` → a novel code) → parity guard reds naming the code. ⚠️ W10: this mutation edits production source OUTSIDE this plan's files_modified fence BY DESIGN; under use_worktrees the revert-before-commit discipline is the ONLY protection against cross-worktree contamination — run it with no other uncommitted work present, capture evidence, revert, and prove `git status --porcelain` is clean BEFORE any commit. Re-run `cd analytics-service && python3 -m pytest -q` after revert to prove the Python baseline restored. Full `npx vitest run`</automated>
  </verify>
  <done>Four wire codes classify by table with honest verdicts; parity guard green against the real emitters with a non-vacuous floor and the dynamic-emitter blind spot declared; both ledger rows observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python wire → TS classifier | `error_code` arrives over the wire; table is `Map`, key never trusted as type |
| copy → anonymous/authed user | attribution copy must not assert unobserved causes (TRAP-3) or leak internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.5-03 | Spoofing | VENUE_WIRE_CODE_TO_VERDICT | mitigate | keep `Map` lookup (never `Record`/identity-cast — `wizardErrors.ts:1599-1608` rule); explicit rows only |
| T-140.5-04 | Info Disclosure | new copy entries | mitigate | static copy, no interpolation of URL/status/hostname; `correlation_id` is the single dynamic value |
| T-140.5-05 | Repudiation | parity guard | mitigate | population from REAL emitters (comment-stripped), floor ≥10, dynamic-emitter blind spot declared |
</threat_model>

<verification>
Full `npx vitest run` (Node 25; Node 22 parity at wave merge) + `npx tsc --noEmit` + `npm run lint`
after every task commit. Never a scoped run as a commit receipt (CONTEXT §9). Mutations: semantic,
production-source, tsc-clean, reverted before commit; the SC-VENUE-2 cross-fence discipline above.
</verification>

<success_criteria>
- §4a copy verbatim, scoped to the unrecognised/codeless population (corrected §6c honored);
  CSV_RATE_LIMIT table row published for 140.5-05.
- No CSV copy promises a breakdown; SC-COPY-1 observed.
- B-02 replay green by type; SC-B02-1 observed (mechanism note recorded).
- MISSING_SCOPE/PERMISSION_DENIED/WITHDRAW_SCOPE/TRADE_SCOPE honest by table; SC-VENUE-1/2
  observed; parity guard derives from real emitters with published predicate — the roster is
  row 2 in those words, with fail-loud arrival, and no "effective row 1" claim.
</success_criteria>

<output>
Create `.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-02-SUMMARY.md`.
`## OPEN` must name: (a) CONTRACT_GUARDS registration of `seam-venue-vocabulary.invariant.test.ts`
owed by 140.5-08; (b) the per-row-breakdown DATA half carried forward (PII fence, QA ISSUE-005);
(c) founder review owed for all Claude-drafted copy (CSV_VALIDATION_FAILED replacement,
KEY_MISSING_READ_SCOPE, KEY_PERMISSION_DENIED) — §4a's copy itself is exempt (pre-approved);
(d) the emitter derivation's published predicate + member list. Do NOT touch STATE.md, ROADMAP.md,
DESIGN.md.
</output>
