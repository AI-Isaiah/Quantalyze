---
phase: 140.5-seamprose
plan: 02
type: execute
wave: 2
depends_on: ["140.5-01"]
files_modified:
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.test.ts
  - src/lib/analytics-client.ts
  - src/lib/analytics-client.test.ts
  - src/lib/seam-venue-vocabulary.invariant.test.ts
autonomous: true
requirements: [SEAMPROSE-03, SEAMPROSE-07]
must_haves:
  truths:
    - "The founder-approved §4a copy exists verbatim under CSV_UPSTREAM_FAIL and is the ONE message for every non-validation upstream status (D-§4a)"
    - "CSV_VALIDATION_FAILED no longer promises a per-row breakdown that never renders (D-§4a bullet 4, RESEARCH §12.4)"
    - "An AnalyticsTimeoutError classifies as SERVICE_UNREACHABLE, not UNKNOWN/500 (B-02 closed by TYPE, not substring)"
    - "MISSING_SCOPE / PERMISSION_DENIED / WITHDRAW_SCOPE / TRADE_SCOPE reach honest caller-fault verdicts instead of UNKNOWN/500 or a false IP-allowlist claim"
    - "Every code in the Python venue closed set has an explicit TS disposition, guarded"
  artifacts:
    - path: "src/lib/wizardErrors.ts"
      provides: "§4a copy entry, B-02 type-first branch, four venue-table rows, two honest new copy entries"
      contains: "We couldn't check your file just now."
    - path: "src/lib/seam-venue-vocabulary.invariant.test.ts"
      provides: "Python closed set ↔ TS disposition parity guard (derived population)"
  key_links:
    - from: "src/lib/analytics-client.ts"
      to: "src/lib/wizardErrors.ts classifyKeyValidationError"
      via: "own-property marker on AnalyticsTimeoutError read by the type-first branch (mock-survival idiom, never instanceof)"
    - from: "analytics-service/services/ingestion/adapter.py"
      to: "VENUE_WIRE_CODE_TO_VERDICT"
      via: "parity guard parsing the Python closed set from disk via source-scan"
---

<objective>
Own `wizardErrors.ts` for the phase: land the §4a vocabulary (the interface plan 140.5-05 consumes),
stop the CSV copy lying about a breakdown, close B-02 by type, and give the four orphaned venue wire
codes honest verdicts — with a derived-population guard so the table can never silently under-cover
the Python vocabulary again.

Coverage-law rows, stated per CONTEXT §2: the §4a copy entry is **row 1** (one chokepoint code for
every non-validation upstream status — D-§4a locked). B-02's type-first branch is **row 1** (shared
classifier artefact; RESEARCH §6.2 option b). The four venue-table rows are **row 2 — a hand-typed
roster, partial by construction in those words** — converted to effective row 1 by the parity guard,
which derives the population from Python source so a new wire code cannot arrive unmapped.

Purpose: SEAMPROSE-03 (WP-10) + the copy half of SEAMPROSE-07 (DEF-140.4-C).
Output: wizardErrors vocabulary complete + venue parity guard (registration owed to 140.5-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-CONTEXT.md   (§4a verbatim copy, §6d, §6e)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-RESEARCH.md   (§6, §7.2, §12.4, §19)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-PATTERNS.md   (§4 copy-home rule, §3)
@src/lib/wizardErrors.ts
@src/lib/analytics-client.ts
@analytics-service/services/ingestion/adapter.py
@analytics-service/services/exchange.py
</context>

<interfaces>
Contract this plan PUBLISHES for 140.5-05 (CSV clients) — frozen once landed:

```ts
// src/lib/wizardErrors.ts — CSV_UPSTREAM_FAIL becomes THE non-validation upstream code.
// Its WIZARD_ERROR_COPY entry carries the §4a founder copy VERBATIM (title/cause/fix mapping below).
// 140.5-05 routes every `!res.ok` outcome on the CSV surface onto it (after the seam hop).
CSV_UPSTREAM_FAIL: { title: "We couldn't check your file just now.", cause: …, fix: […], docsHref: …, actions: ["clear_and_retry", "request_call"] }
```

Decision, argued (planner discretion under D-§4a): REUSE `CSV_UPSTREAM_FAIL` rather than mint a new
code. It already exists, already means "upstream failure on this surface", and already has emitters at
`csv-validate/route.ts:291,:353` — a second code for the same fact is exactly the
two-names-one-fact drift `seam-copy.ts` exists to prevent. PATTERNS §4's "avoid the CSV_ prefix"
advice loses to reuse-over-rename here (Rule 3, surgical changes); the deviation is recorded here
deliberately. The copy home is `WIZARD_ERROR_COPY`, NOT `seam-copy.ts` — locked at CONTEXT §6d.3
(`seam-copy.purity.test.ts:79` pins that leaf to exactly one export and a string leaf cannot supply
the `cause` field `CsvValidationEnvelope.tsx:59-76` renders).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: §4a founder copy verbatim + stop the per-row-breakdown lie</name>
  <files>src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts</files>
  <behavior>
    - `WIZARD_ERROR_COPY.CSV_UPSTREAM_FAIL.title === "We couldn't check your file just now."` (literal in the test, not imported — Oracle Independence).
    - Its cause/fix carry the founder sentences: "This is on our side, not your data. Nothing was
      saved." and "Try again in a moment — if it keeps happening, send us this reference." — split
      across cause/fix WITHOUT rewording (D-§4a: verbatim).
    - `actions` includes `"clear_and_retry"` (the copy says "try again in a moment" — polarity
      re-derived per PATTERNS §4: an action absent from RECOVERABLE_ACTIONS renders no retry control
      and no wait) and keeps `"request_call"` as the "send us this reference" affordance.
    - `WIZARD_ERROR_COPY.CSV_VALIDATION_FAILED.title` no longer contains "per-row breakdown" (a
      promise-pin test asserts the ABSENCE of that phrase and the presence of the replacement).
  </behavior>
  <action>
    Replace `CSV_UPSTREAM_FAIL`'s entry (`wizardErrors.ts:829-838`) with the §4a founder copy,
    mapped: title = heading sentence; cause = "This is on our side, not your data. Nothing was
    saved."; fix = ["Try again in a moment — if it keeps happening, send us this reference."].
    `correlation_id` is rendered by the envelope component, not embedded in copy — keep the copy's
    dynamic-value count at ONE (PATTERNS §9 static-copy rule). "Nothing was saved" is TRUE at this
    arm — verified at three layers in RESEARCH §12.3; cite that in the entry's docblock (telemetry
    caveat included) rather than re-deriving.
    Then fix `CSV_VALIDATION_FAILED` (`:817-828`): the per-row breakdown NEVER renders on either arm
    (RESEARCH §12.4: Python emits `debug_context.violations`, the client reads `pandera_errors` —
    zero Python hits — and `_envelope_error` discards `debug_context` before the wire). Per the
    orchestrator's locked recommendation: fix the COPY — new title/fix must describe what the user
    actually sees (the human_message sentence) without promising a breakdown. Claude-drafted per
    DESIGN.md §Voice (read it first; do not edit it); founder review of replacements is owed per
    CONTEXT §8.1 — say so in the SUMMARY. The DATA half (forwarding violations = the QA ISSUE-005
    PII surface) is explicitly NOT done here — record it in `## OPEN` for 140.5-08's deferred-items
    consolidation.
    Prose hygiene in owned files: "sixteen" at `wizardErrors.ts:21` and `analytics-client.ts:42,:187`
    — delete the integer, name the predicate ("every route test that mocks a seam client wholesale",
    RESEARCH §3.10). Convert any bare `file:line` citations these two files carry: run
    `MODE=full node .planning/phases/140.5-*/140.5-citation-census.mjs src/lib/wizardErrors.ts src/lib/analytics-client.ts`
    and convert every reported hit to a symbol-anchored reference (the 140.5-08 guard will forbid
    the bare form on the seam surface).
  </action>
  <verify>
    <automated>SC-COPY-1 mutation: restore "Validation failed. See per-row breakdown below." as the title → promise-pin test reds (paste evidence, revert). Full `npx vitest run` + `npx tsc --noEmit`</automated>
  </verify>
  <done>Founder copy verbatim under CSV_UPSTREAM_FAIL; no copy on this surface promises a breakdown; ledger row observed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: B-02 — close the dead timeout branch by TYPE, not substring</name>
  <files>src/lib/analytics-client.ts, src/lib/analytics-client.test.ts, src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts</files>
  <behavior>
    - Replay of the three real analytics-client messages (RESEARCH §6.1, executed): an
      `AnalyticsTimeoutError` ("Analytics service timed out after 15000ms on /process-key") and the
      not-reachable error now classify to `SERVICE_UNREACHABLE` (whose copy at `wizardErrors.ts:1097-1109`
      is final — "We could not reach our own service") instead of `UNKNOWN`/500.
    - The classification survives `vi.mock("@/lib/analytics-client")` wholesale (the mock-survival
      constraint documented at `wizardErrors.ts:1500-1509`).
    - `wizardErrors.test.ts:611`'s pinned string `"connect ETIMEDOUT 10.0.0.1:443"` is RE-POINTED in
      the same commit — it pins a raw undici string that can never reach the classifier (RESEARCH
      §6.2); leaving it keeps the suite reading as covering a dead arm.
  </behavior>
  <action>
    In `analytics-client.ts`: assign an own property in `AnalyticsTimeoutError`'s constructor (and
    the not-reachable error class if distinct) exactly as `seamCode` is assigned — copy the idiom at
    the existing class, do not invent a second marker convention. In `wizardErrors.ts`: add a
    type-first read of that own property in `classifyKeyValidationError` BEFORE the substring
    cascade, alongside the existing `seamCode` read at `:1507-1516` (NEVER `instanceof` — the
    docblock there explains why). Map to `SERVICE_UNREACHABLE`. Do NOT copy
    `SERVICE_UNAVAILABLE_RETRY`'s "Nothing was submitted" — knowable for a declined breaker request,
    FALSE for a timed-out one (`wizardErrors.ts:1085-1096`; 140.3-12 fixed this once). Keep the
    `timeout|etimedout` substring branch as last-resort for raw syscall strings; re-run the
    collision-invariant list at `:1545-1555` for the new branch (re-derive, don't copy — RESEARCH §11.5).
  </action>
  <verify>
    <automated>SC-B02-1 (mechanism adjusted, spirit kept — note in ledger): remove the type-first branch from production `wizardErrors.ts` → the replay test reds; SC not satisfiable by the seeded "re-narrow the substring" wording because the fix is no longer substring-carried. Paste evidence, revert. Full `npx vitest run`</automated>
  </verify>
  <done>All three real messages classify to SERVICE_UNREACHABLE (replay test green); :611 re-pointed; ledger row observed with the mechanism note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Venue table — four honest rows + the Python↔TS parity guard</name>
  <files>src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts, src/lib/seam-venue-vocabulary.invariant.test.ts</files>
  <behavior>
    - `MISSING_SCOPE` → a NEW `KEY_MISSING_READ_SCOPE` (caller fault, 400): copy says the key lacks a
      required read scope and how to grant it — never "our team has been notified" (it renders
      UNKNOWN/500 today for a fixable Deribit scope; RESEARCH §7.2, the DOGFOOD-3 dead end).
    - `PERMISSION_DENIED` → a NEW `KEY_PERMISSION_DENIED` (caller fault, 400): copy names BOTH
      possible causes (scope OR IP allowlist) without asserting either — today it renders 502
      `KEY_IP_ALLOWLIST`, a server status asserting one of two causes (TRAP-3 discipline: never
      state unobserved specifics).
    - `WITHDRAW_SCOPE` → existing `KEY_HAS_WITHDRAW_PERMS` / 400 (today: copy says *trading*).
    - `TRADE_SCOPE` → existing `KEY_HAS_TRADING_PERMS` / 400 (today correct only by substring-order
      accident — make it correct by table).
    - Parity guard: every member of the Python closed set (parsed at run time from
      `analytics-service/services/ingestion/adapter.py`'s `error_code` enumeration via
      `source-scan`) has an explicit disposition: a `VENUE_WIRE_CODE_TO_VERDICT` row OR membership
      in a hand-typed exemption set with a reason string per entry. Vacuity fence: parsed set size
      ≥ 8 (measured 13 at `4c60518b`; predicate = the adapter.py enumeration at `:107-112`; floor
      ~60% per the seam-log-coverage idiom). Failure message forbids fixing a red by widening the
      exemption set without a reason.
  </behavior>
  <action>
    Add the four rows to `VENUE_WIRE_CODE_TO_VERDICT` (`wizardErrors.ts:1422-1438`) and the two new
    `WizardErrorCode` members + copy entries (all `WizardErrorCopy` fields are required — re-derive
    the `actions` polarity per entry; a scope fix is `try_another_key`-shaped, not `clear_and_retry`).
    Write unit tests replaying the REAL Python detail strings from `exchange.py` (quoted in RESEARCH
    §7.2) through `classifyKeyValidationError` with the own-property seamCode set — assert the table
    answers, not the cascade. Build the parity guard as a new invariant test using
    `stripCommentsPreserveLines` (140.5-01) on the Python source before parsing (a code named in a
    Python comment must not count). Codes left WITHOUT a verdict row (`UNSUPPORTED_EXCHANGE`,
    `VALIDATION_UNEXPECTED`, `CSV_VALIDATION_FAILED`, and any other measured member) go in the
    exemption set — for each, VERIFY what the cascade actually does with its real detail string
    before writing the reason (do not guess; RESEARCH §7.2's table covers four, measure the rest).
    New copy entries are Claude-drafted → founder review owed (CONTEXT §8.1); say so in SUMMARY.
    Guard registration in CONTRACT_GUARDS is owed by 140.5-08 — name that in `## OPEN`.
  </action>
  <verify>
    <automated>SC-VENUE-1 mutation: delete the WITHDRAW_SCOPE row (the SECOND member of the class — TRADE_SCOPE is the one an author has in mind) → unit test reds. SC-VENUE-2 mutation: append a fake code to adapter.py's enumeration (Python production source; tsc unaffected) → parity guard reds naming the code. Paste both, revert both, `git status --porcelain` clean. Full `npx vitest run` + `cd analytics-service && python3 -m pytest -q` (Python file was touched under mutation — prove restored)</automated>
  </verify>
  <done>Four wire codes classify by table with honest verdicts; parity guard green with a non-vacuous floor; both ledger rows observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python wire → TS classifier | `error_code` arrives over the wire; table is `Map`, key never trusted as type |
| copy → anonymous/authed user | attribution copy must not assert unobserved causes (TRAP-3) or leak internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.5-03 | Spoofing | VENUE_WIRE_CODE_TO_VERDICT | mitigate | keep `Map` lookup (never `Record`/identity-cast — `wizardErrors.ts:1599-1608` rule); explicit rows only |
| T-140.5-04 | Info Disclosure | new copy entries | mitigate | static copy, no interpolation of URL/status/hostname; `correlation_id` is the single dynamic value |
| T-140.5-05 | Repudiation | parity guard | mitigate | vacuity floor ≥8 on the parsed Python set; comment-strip before parse (DEF-16-2) |
</threat_model>

<verification>
Full `npx vitest run` (Node 25; Node 22 parity at wave merge) + `npx tsc --noEmit` + `npm run lint`
after every task commit. Never a scoped run as a commit receipt (CONTEXT §9 — violated six times in
this programme). Mutations: semantic, production-source, tsc-clean, reverted before commit.
</verification>

<success_criteria>
- §4a copy verbatim, ONE code for every non-validation upstream status (D-§4a honored; coverage row 1).
- No CSV copy promises a breakdown; SC-COPY-1 observed.
- B-02 replay green by type; SC-B02-1 observed (mechanism note recorded).
- MISSING_SCOPE/PERMISSION_DENIED/WITHDRAW_SCOPE/TRADE_SCOPE honest by table; SC-VENUE-1/2 observed.
- Parity guard green, floor non-vacuous, exemptions reasoned.
</success_criteria>

<output>
Create `.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-02-SUMMARY.md`.
`## OPEN` must name: (a) CONTRACT_GUARDS registration of `seam-venue-vocabulary.invariant.test.ts`
owed by 140.5-08; (b) the per-row-breakdown DATA half carried forward (PII fence, QA ISSUE-005) for
140.5-08's deferred-items consolidation; (c) founder review owed for all Claude-drafted copy
(CSV_VALIDATION_FAILED replacement, KEY_MISSING_READ_SCOPE, KEY_PERMISSION_DENIED) — §4a's copy
itself is exempt (pre-approved). Do NOT touch STATE.md, ROADMAP.md, DESIGN.md.
</output>
