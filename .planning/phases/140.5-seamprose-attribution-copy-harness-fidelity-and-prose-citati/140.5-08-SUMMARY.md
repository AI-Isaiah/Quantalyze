---
phase: 140.5-seamprose
plan: 08
subsystem: seam / prose-attribution / contracts-registry
tags: [SEAMPROSE-01, SEAMPROSE-08, SEAMPROSE-04, citation-guard, contracts-registry, phase-gate]
requires:
  - "140.5-01 (source-scan.ts extractComments — the citation guard's engine)"
  - "140.5-02/03/05/06 (the four sibling guards this plan registers)"
  - "140.5-04/07 (the surface conversions this plan's remainder completes)"
provides:
  - "src/lib/seam-citations.invariant.test.ts (SEAM_CITATION_SURFACE) — the §4b bare-citation guard"
  - "CONTRACT_GUARDS at 57 rows (all 5 phase guards registered, floor bumped same-commit)"
  - "140.5-deferred-items.md — the consolidated carry-forward ledger (§6 hand-off terminus)"
  - "140.5-VALIDATION.md signed (nyquist_compliant: true) — the phase's falsifiability record"
affects:
  - "phase 141 (retry work) inherits a truthful registry + a signed gate to build on"
tech-stack:
  added: []          # ZERO package installs — gate contract
  patterns:
    - "citation guard = seam-log-coverage's six-part shape INVERTED (extractComments, not stripComments)"
    - "form-ban over past-EOF (0 of 51 past-EOF on the surface makes past-EOF vacuous)"
key-files:
  created:
    - src/lib/seam-citations.invariant.test.ts
    - .planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-deferred-items.md
  modified:
    - src/__tests__/contracts/contracts-registry.test.ts
    - src/__tests__/contracts/REGISTRY.md
    - .planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-VALIDATION.md
    - "14 seam-surface files (comment-only citation conversions — see Task 1)"
decisions:
  - "Citation guard bans the FORM, does not resolve — ambiguity (20% of the surface) is irrelevant to the verdict"
  - "Registry false integers corrected to DERIVED/PINNED-SET references, never to another bare integer"
  - "SC-429-1 ledger row re-worded (its stated receipt was structurally unreachable)"
metrics:
  duration: "~75 min"
  completed: "2026-07-30"
  tasks: 3
  commits: 3
  files_changed: 20
---

# Phase 140.5 Plan 08: SEAMPROSE citation guard, guard registration & phase gate — Summary

**The §4b bare-`file:line` citation guard landed over the 34-file seam surface (form-ban, not past-EOF — 0 of 51 measured past-EOF here); all five guards this phase created are registered in `CONTRACT_GUARDS` with the floor bumped 52→57 in the same commit; the registry's false integers ("three predicates"→five, "six seam files"→nineteen-member derived roster, "sixteen route tests") are corrected to derived/pinned-set references rather than swapped for other integers; and the phase gate is green on both Nodes and both languages with zero package installs — `140.5-VALIDATION.md` is signed `nyquist_compliant: true`.**

This is the phase's terminal wave: every earlier plan's `## OPEN` hand-off terminates in `140.5-deferred-items.md`, closing the §6 plan-to-plan hand-off-hole class **procedurally** as well as structurally.

---

## What was built

### Task 1 — the seam-surface citation remainder (commit `c449aa3c`)

Re-measured the surface at HEAD (`MODE=full` census over `140.5-seam-surface.txt`): **26 resolved bare in-repo citations remained** across 14 files (waves 02–07 had converted the rest). Converted every one to a **symbol-anchored reference** by reading each target — e.g. `queries.ts:974` → `assertPortfolioOwnership in src/lib/queries.ts` (the `:974` had drifted onto an unrelated type assertion), `analytics-client.ts:66` → `AnalyticsUpstreamError` / its `seamCode`, `simulator/route.ts:201` → "the 4xx-forward arm". **Comment-only**: `git diff` proved every changed line is a comment. Post-conversion census: **0 bare in-repo, 1 out-of-repo** (the `resilient-fetch.ts → @upstash/ratelimit dist/index.mjs` reference, the guard's sole allowlist entry).

### Task 2 — the citation guard (commits `ee42f87d` + gate-fix in `7200b5cf`)

`src/lib/seam-citations.invariant.test.ts`, copying `seam-log-coverage.test.ts`'s six-part shape with the mandated **inversion** — it EXTRACTS comments (`extractComments`, line-preserving) where the log guard strips them, because a citation lives *inside* a comment. Parts: exported `SEAM_CITATION_SURFACE` (34 hand-typed paths, each existence-checked); the census's variant-A/B/C needle (ported with the paren-un-eating); a length-pinned allowlist (`.toBe(1)`) with a reason; vacuity fences (file count `.toBe(34)`, comment-line floor `>=6500` of 10863 measured); and **both-polarity needle self-tests end-to-end** — a planted `foo.ts:123` caught with the right line, the same token in a string literal NOT caught, a symbol reference NOT caught, a `(route.ts:172)` paren form caught. The mechanism is a **form-ban, not past-EOF**, because the founder-scoped surface has **0 of 51 past-EOF** (a past-EOF guard would be green forever here). The docblock carries the `.planning/**` permanent exclusion, the D/E/F self-relative residual, the four widening prerequisites, and §10.4's durable sentence (never write an integer a test can derive).

### Task 3 — registration, prose correction, gate, sign-off (commit `7200b5cf`)

- **Registration:** 5 rows added to `CONTRACT_GUARDS` (venue-vocabulary 02, transport-attribution 03, wire-vocabulary 05, spec-disabling 06, citations 08), floor bumped **52→57** with the predicate quoted, in the SAME commit — never `CONTRACT_GUARDS.length`. Same 5 rows mirrored into REGISTRY.md's prose table; the subset-count note updated to "29 against 57".
- **Prose correction:** the registry's false integers fixed in BOTH the array notes and REGISTRY.md, and NOT swapped for other integers — "exactly three predicates" → the pinned `EXPECTED_EXPORTS` set (five `seam*` readers, `grep -c '^export function' src/lib/seam-discriminator.ts` = 5); "the six seam files" → the DERIVED roster (`SEAM_FILES`/`EXPECTED_SEAM_FILES`, nineteen — explicitly not "eight", CONTEXT §6b); "sixteen route tests" → the predicate.
- **Deferred-items:** `140.5-deferred-items.md` consolidates every plan's `## OPEN`.
- **Gate + sign-off:** full gate run and pasted below; `140.5-VALIDATION.md` signed.

---

## Phase gate evidence (advisory at merge — branch protection OFF, so "would have caught")

| gate | result |
|---|---|
| **zero-install** `git diff 4c60518b..HEAD -- package.json package-lock.json` | **EMPTY** (also empty vs `453b4291`) |
| **vitest Node 25** | **733 files / 10265 passed / 287 skipped / 0 failed** |
| **vitest Node 22** (v22.22.1) | **IDENTICAL — 733 / 10265 / 287 / 0** |
| `tsc --noEmit` | **exit 0** |
| `npm run lint` | **0 errors, 1 pre-existing warning** (`EquityChart.tsx:1119`) |
| `pytest -q` | **4817 passed / 96 skipped / 0 failed** |
| `mypy --strict --follow-imports=silent services/ routers/ models/` | **89 files, no issues** |
| **census — seam surface** (`MODE=full`, 34 files) | `citations total: 1 / PAST-EOF: 0` — the one hit is the out-of-repo allowlist entry |
| **census — repo-wide** (`src analytics-service e2e scripts supabase tests tools`) | `1852 files / PAST-EOF: 0` |

⚠️ Branch protection on `main` is OFF (CONTEXT §9, settled): every gate above **would have caught** its failure at merge — none can be said to have "stopped" anything.

### SC-CITE-1 / SC-CITE-2 (this plan's ledger rows, W10 discipline)

- **SC-CITE-1 (observed):** on a CLEAN committed tree, planted `see route.ts:999 for the shape` in a production comment at `keys/sync/route.ts:222` (`tsc` exit 0 under it). Guard → **1 failed | 45 passed**, naming *"src/app/api/keys/sync/route.ts:222 cites `route.ts:999`"*. The other 45 file cases stayed GREEN (per-file discrimination). Reverted by edit; `git status --porcelain` empty BEFORE any commit.
- **SC-CITE-2 (observed):** clean tree → **46 passed**, zero offences across 34 files, all three fences hold (`.toBe(34)`, floor `>=6500`, allowlist `.toBe(1)`).

---

## Deviations from Plan

### Auto-fixed issues (Rule 1 / Rule 3)

**1. [Rule 1 — Bug] The citation guard's own docblock planted the banned form, creating a repo-wide past-EOF and failing the gate.**
- **Found during:** Task 3, running the repo-wide census.
- **Issue:** the guard illustrated the rot with a live `analytics_runner.py:2387` (past-EOF) plus `route.ts:748`/`queries.ts:974` in its docblock, and a `(route.ts:172)` in a real comment — so `PAST-EOF` was **1** repo-wide, and the guard was "a guard that plants the string it bans".
- **Fix:** reworded the docblock to describe the forms without writing them; changed the one live comment `route.ts:172` → `route.ts:NN`. Repo-wide past-EOF → **0**; the guard file now carries **0** census-matching comment citations.
- **Files:** `src/lib/seam-citations.invariant.test.ts`. **Commit:** `7200b5cf`.

**2. [Rule 1 — Bug] The `spec-disabling` guard reddened on my own registration note.**
- **Found during:** Task 3, the first full Node-25 run (`1 failed`).
- **Issue:** `spec-disabling.invariant.test.ts` scans **string literals** (it only comment-strips), and my registration note in `contracts-registry.test.ts` quoted the literal token `test.skip(true)` twice → 2 offences at that line. (The guard exempts only its own `SELF` file.)
- **Fix:** reworded the note to describe the disabling forms without writing the exact needle tokens. Full suite → 0 failed.
- **Files:** `src/__tests__/contracts/contracts-registry.test.ts`. **Commit:** `7200b5cf`.

**3. [Rule 3 — Blocking] Task 1's file list named 21 files, but the guard's green requires all 34 clean.**
- One resolved bare citation sat in `CsvValidationEnvelope.tsx` (a 05-owned file, not in Task 1's list). Since the guard scans all 34 and must be green, it was converted too. All waves are merged into this terminal base, so there is no cross-worktree contention. Comment-only.

### Ledger correction (recorded, not silently absorbed)

**SC-429-1's stated receipt was structurally unreachable** (a fixture flip is a test edit; the "nested-at-429 contract case" is a vitest test with a mocked upstream that no Python edit can reach). Per 06's `## OPEN(f)` — "re-worded by whoever owns VALIDATION.md" — the row was re-worded to record the admissible Python-pin mutation 06 actually ran. Also transcribed 06's evidence for SC-424-2, SC-R1-1, SC-SPEC-1 (observed in 06's SUMMARY, never written into VALIDATION.md because that file was 08's to own).

### Scope boundary honoured

- Ignored three vercel-plugin skill injections (next-cache-components / react-best-practices / Workflow DevKit) — all fired on comment-only edits, and Workflow DevKit would violate the hard zero-install gate (DEF-140.4-B already declined it).
- Did NOT touch STATE.md / ROADMAP.md / DESIGN.md (orchestrator-owned).

---

## Known Stubs

None. The citation guard is fully wired (real `extractComments` pipeline, hand-typed population, both-polarity self-tests). No placeholder values, no unwired assertions.

---

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema change at a trust boundary. The three threat-register mitigations hold: T-140.5-19 (both-polarity end-to-end self-tests + 34-file population + comment-line floor); T-140.5-20 (literal floor bumped same-commit, `.length` comparison forbidden); T-140.5-SC (zero-install diff EMPTY, pasted).

---

## Self-Check: PASSED

Files claimed created — verified on disk:
- `src/lib/seam-citations.invariant.test.ts` — FOUND
- `.planning/phases/140.5-.../140.5-deferred-items.md` — FOUND
- `.planning/phases/140.5-.../140.5-08-SUMMARY.md` — FOUND

Commits claimed — verified in `git log`:
- `c449aa3c` — Task 1 (citation conversions) — FOUND
- `ee42f87d` — Task 2 (citation guard) — FOUND
- `7200b5cf` — Task 3 (registration + prose + gate + sign-off) — FOUND

TDD gate (Task 2, `tdd="true"`): the guard is an ABSENCE assertion, green on a clean tree by construction, so its RED is the SC-CITE-1 mutation (observed, reverted) rather than a separate red commit — the 140.5-01 precedent. Registered atomically with the floor bump in `7200b5cf`.

Tree state: `git status --porcelain` empty after each commit; every mutation reverted BY EDIT (never `git checkout --` on a file holding uncommitted work). STATE.md / ROADMAP.md / DESIGN.md NOT touched.
