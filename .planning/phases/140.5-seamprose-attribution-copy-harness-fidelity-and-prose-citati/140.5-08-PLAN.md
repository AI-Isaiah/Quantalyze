---
phase: 140.5-seamprose
plan: 08
type: execute
wave: 4
depends_on: ["140.5-01", "140.5-02", "140.5-03", "140.5-04", "140.5-05", "140.5-06", "140.5-07"]
files_modified:
  - src/lib/seam-citations.invariant.test.ts
  - src/lib/ratelimit.ts
  - src/lib/seam-errors.ts
  - src/lib/seam-copy.ts
  - src/lib/seam-redaction.ts
  - src/lib/envelope.ts
  - src/components/error/ErrorEnvelope.tsx
  - src/app/api/admin/match/eval/route.ts
  - src/app/api/admin/match/recompute/route.ts
  - src/app/api/bridge/route.ts
  - src/app/api/keys/[id]/permissions/route.ts
  - src/app/api/keys/sync/route.ts
  - src/app/api/keys/validate-and-encrypt/route.ts
  - src/app/api/portfolio-optimizer/route.ts
  - src/app/api/scenario/optimize/route.ts
  - src/app/api/simulator/route.ts
  - src/app/api/strategies/composite/add-key/route.ts
  - src/app/api/strategies/create-with-key/route.ts
  - src/app/api/strategies/csv-finalize/route.ts
  - src/app/api/strategies/csv-validate/route.ts
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/verify-strategy/route.ts
  - src/__tests__/contracts/contracts-registry.test.ts
  - src/__tests__/contracts/REGISTRY.md
  - .planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-VALIDATION.md
  - .planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-deferred-items.md
autonomous: true
requirements: [SEAMPROSE-01, SEAMPROSE-08, SEAMPROSE-04]
must_haves:
  truths:
    - "A bare in-repo file:line citation in any of the 34 seam-surface files reddens a registered vitest guard — coverage-law row 1 ON THE SEAM SURFACE, explicitly NOT a repo-wide closure (founder decision §4b, in those words)"
    - "The guard is provably non-vacuous: it catches a planted bad citation, stays silent on a clean tree, and its scanner has a population floor (a past-EOF guard here would be green forever — 0 of 51 seam citations are past-EOF, measured)"
    - "Every structural guard this phase created is registered in CONTRACT_GUARDS with the floor bumped in the same commit"
    - "The registry's own prose is true: no 'exactly three predicates' (five exist), no 'six seam files' (the derived roster is the truth) — and NOT corrected to another bare integer"
    - "Phase gate green: zero package installs, both Nodes, both language suites, ledger fully observed"
  artifacts:
    - path: "src/lib/seam-citations.invariant.test.ts"
      provides: "the §4b guard: bare-citation ban over the ratified 34-file surface, one-line-widenable"
      exports: ["SEAM_CITATION_SURFACE"]
    - path: ".planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-deferred-items.md"
      provides: "consolidated carry-forwards from every plan's OPEN section"
  key_links:
    - from: "src/lib/seam-citations.invariant.test.ts"
      to: "src/lib/source-scan.ts"
      via: "extractComments (citations live INSIDE comments — the guard extracts, the opposite of stripping)"
    - from: "contracts-registry.test.ts CONTRACT_GUARDS"
      to: "the four wave-2/3 guards + this one"
      via: "registration rows + floor literal bumped in the same commit (the self-comparing-floor hazard rule)"
---

<objective>
Land the citation guard the founder scoped (§4b), register every guard this phase created, correct
the registry's own false prose, and run the phase gate. This is the last wave: every earlier plan's
`## OPEN` hand-offs terminate here — closing the §6 hand-off-hole class procedurally as well as
structurally.

Coverage-law row, stated per CONTEXT §2: **row 1 on the seam surface and explicitly NOT a repo-wide
closure** (founder decision §4b, said in those words). Mechanism locked by CONTEXT §6d.1: a
past-EOF check here is VACUOUS (51 citations, ZERO past-EOF, while 9 are provably wrong in-range —
measured) — the guard instead FORBIDS the bare `file:line` form (zero false positives after the
conversions; one-line glob widening).

THE AMBIGUITY POLICY — the design decision with no repo analog (PATTERNS §10), decided here, not
inherited: the guard does NOT resolve citations at all. It bans the path-qualified coordinate FORMS
(census variants A/B/C) outright, so resolvability and basename ambiguity (10 of 51 on this surface,
20%) are irrelevant to the guard's verdict — ambiguity mattered only during the one-time
conversions, where each citation was resolved by a human reading the target (waves 2-3 + Task 1).
Self-relative variants D/E/F (`line 55`, `(:NNN)`, `at line 67` — 69 repo-wide under the
pattern-mapper's predicate) are NOT covered: **partial by construction** for those forms, in those
words, with the second-predicate widening named in the guard docblock.

Purpose: SEAMPROSE-01 (guard half), SEAMPROSE-08 (hand-off terminus), SEAMPROSE-04 (gate).
Output: registered guard suite, truthful registry, phase gate evidence, deferred-items ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-CONTEXT.md   (§4b, §6d.1, §6g)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-RESEARCH.md   (§3.7, §10 whole section)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-PATTERNS.md   (§8 — the six parts of the analog to copy, in order)
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-seam-surface.txt
@.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-citation-census.mjs
@src/lib/seam-log-coverage.test.ts
@src/__tests__/contracts/contracts-registry.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the seam-surface remainder (the files no earlier plan owned)</name>
  <files>src/lib/ratelimit.ts, src/lib/seam-errors.ts, src/lib/seam-copy.ts, src/lib/seam-redaction.ts, src/lib/envelope.ts, src/components/error/ErrorEnvelope.tsx, src/app/api/admin/match/eval/route.ts, src/app/api/admin/match/recompute/route.ts, src/app/api/bridge/route.ts, src/app/api/keys/[id]/permissions/route.ts, src/app/api/keys/sync/route.ts, src/app/api/keys/validate-and-encrypt/route.ts, src/app/api/portfolio-optimizer/route.ts, src/app/api/scenario/optimize/route.ts, src/app/api/simulator/route.ts, src/app/api/strategies/composite/add-key/route.ts, src/app/api/strategies/create-with-key/route.ts, src/app/api/strategies/csv-finalize/route.ts, src/app/api/strategies/csv-validate/route.ts, src/app/api/strategies/finalize-wizard/route.ts, src/app/api/verify-strategy/route.ts</files>
  <action>
    Start from 140.5-04's Task-3 hand-off (the measured per-file remainder list in its SUMMARY),
    re-measure at HEAD:
    `MODE=full node .planning/phases/140.5-*/140.5-citation-census.mjs $(tr '\n' ' ' < .planning/phases/140.5-*/140.5-seam-surface.txt)`.
    Convert every reported bare in-repo citation in the files above to a symbol-anchored reference,
    resolving each by READING the target (the three-site `route.ts:748` class is already gone —
    140.5-05 owned those files; the `exchange.py` table is gone — 140.5-07). Comment-only edits;
    if a conversion would require touching executable code, stop and report. The known legitimate
    out-of-repo reference (`resilient-fetch.ts:761`, `@upstash/ratelimit`'s dist file) was
    annotated by 140.5-07 and is NOT converted — it becomes the guard's sole allowlist entry.
  </action>
  <verify>
    <automated>Census MODE=full over the full 34-file surface → zero bare in-repo citations, one annotated out-of-repo reference. FULL `npx vitest run` + `npx tsc --noEmit` + `npm run lint` (comment edits can trip prose-scanning guards — that is why the earlier guards comment-strip; a red here is signal, not noise)</automated>
  </verify>
  <done>Surface clean under the committed predicate; suites at baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The citation guard — needle self-tested in BOTH polarities before it is trusted</name>
  <files>src/lib/seam-citations.invariant.test.ts</files>
  <behavior>
    Copy `seam-log-coverage.test.ts`'s six parts in PATTERNS §8's order, with the stated inversion:
    1. Population: `SEAM_CITATION_SURFACE` — an exported hand-typed constant of the 34 ratified
       paths (A6 ratified: the 19-file derived roster + 5 leaves + envelope/wizardErrors/
       tenant-claim + 6 wizard steps + CsvValidationEnvelope + ErrorEnvelope, exactly
       `140.5-seam-surface.txt`). Every listed file must exist on disk (fail loud on drift).
       Widening later = appending to this ONE array (§4b's one-line ratchet requirement, satisfied
       literally).
    2. Extraction: `extractComments` from source-scan — the guard needs comments, the OPPOSITE of
       stripping (PATTERNS §2's rule for this artefact), line-preserving so offences carry real
       coordinates.
    3. Needle: the census's variant-A/B/C matcher (port from `140.5-citation-census.mjs`, which
       already survived paren-eating and docstring dedup) — any `<name>.<ext>:<digits>` with
       optional range/list tail, in a comment, in a surface file, is an offence.
    4. Allowlist: hand-typed, frozen with a length pin (`.toBe(1)`) and a reason string —
       sole entry: resilient-fetch's out-of-repo `@upstash/ratelimit` evidence reference. Failure
       message forbids widening without a reason row.
    5. Vacuity fences: exactly 34 files scanned; total extracted comment lines ≥ a hand-typed floor
       (~60% of measured — measure at edit time, state the predicate in the failure message:
       "the extractor is BLIND, not satisfied; fix the extractor, never lower the floor").
    6. Both-polarity needle self-tests, END-TO-END through the real pipeline on synthetic source
       strings (VALIDATION Wave-0 requirement — "a scanner that matches nothing reports agreement
       forever"): (a) a planted `foo.ts:123` in a comment → caught with the right line; (b) the
       same token inside a STRING literal → NOT caught; (c) a symbol-anchored reference → NOT
       caught; (d) a `(route.ts:172)` paren form → caught (the paren-eating regression).
    Offences reported as a collected array (the `offences` idiom — one file with three rots is
    three lines), failure message naming the conversion protocol (symbol references) and the
    docblock carrying: the `.planning/**` permanent exclusion + rationale (a planning ledger is a
    frozen record — correcting it would falsify the history this milestone depends on); the D/E/F
    residual + second predicate needed; the four §10.3 widening prerequisites; and the vacuity
    finding that made past-EOF the WRONG mechanism here (0 of 51, predicate cited).
  </behavior>
  <action>
    Build per the behavior block. The claims a citation guard structurally cannot reach (value/
    set-size claims — the "5 minutes" class, RESEARCH §10.4) are NOT this guard's job: they were
    fixed by deleting the derivable integers at source (waves 2-3); state that boundary in the
    docblock with §10.4's one durable sentence: never write an integer or duration in prose that a
    test can derive.
  </action>
  <verify>
    <automated>SC-CITE-1 mutation: plant a bare `file:line` citation in a seam-surface ROUTE file (production comment — tsc-clean; pick a route, not the guard's own fixtures) → guard reds naming file+line. SC-CITE-2 vacuity control: clean tree → zero offences AND the population floors hold (paste both outputs). ⚠️ W10 discipline applies even though the plant target is inside this plan's fence: run the mutation with no other uncommitted work present, capture evidence, revert, and prove `git status --porcelain` is clean BEFORE any commit — under use_worktrees the revert-before-commit discipline is what prevents a stray planted citation surviving into the merge. FULL `npx vitest run`</automated>
  </verify>
  <done>Guard green on clean tree, red on planted rot, floors non-vacuous; SC-CITE-1/2 observed.</done>
</task>

<task type="auto">
  <name>Task 3: Register everything, correct the registry's prose, run the phase gate</name>
  <files>src/__tests__/contracts/contracts-registry.test.ts, src/__tests__/contracts/REGISTRY.md, .planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-VALIDATION.md, .planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-deferred-items.md</files>
  <action>
    1. REGISTRATION (the hand-off every wave-2/3 plan named in `## OPEN`): add CONTRACT_GUARDS rows
       for the five guards this phase created — `seam-venue-vocabulary.invariant.test.ts` (02),
       `seam-transport-attribution.invariant.test.ts` (03), `seam-wire-vocabulary.invariant.test.ts`
       (05), `spec-disabling.invariant.test.ts` (06), `seam-citations.invariant.test.ts` (this
       plan) — and bump the floor literal at `contracts-registry.test.ts:259` by EXACTLY the number
       of rows added, in the same commit (read the current literal first — it was 52 at `4c60518b`;
       never copy that number blindly, and never write `CONTRACT_GUARDS.length` — the
       self-comparing-floor hazard the file itself forbids at `:251-255`). Each row's note names
       what the guard closes AND its blindness, in the registry's own style.
    2. REGISTRY.md prose (04 fenced these off for this plan): `:102`'s "sixteen" → predicate
       wording; `:103` "exactly three predicates" → reference the pinned `EXPECTED_EXPORTS` set
       (five functions — if an integer is written, quote the predicate beside it per the file's own
       `:243-244` rule); `:107` "the six seam files" → reference the DERIVED roster
       (`EXPECTED_SEAM_FILES` — ⚠️ NOT "eight": CONTEXT's own figure is stale, the derivation made
       it nineteen; correcting to eight would replace one false integer with another — CONTEXT §6b).
    3. DEFERRED-ITEMS consolidation: create `140.5-deferred-items.md` gathering every plan
       SUMMARY's `## OPEN` carry-forwards — expected set (verify against the actual SUMMARYs):
       per-row-breakdown DATA half + QA ISSUE-005 (PII fence); the .tsx log-roster class;
       17 stripComments copies unmigrated; D/E/F self-relative citation forms; composite/members
       limiter absence; AnalyticsUpstreamError positional params; founder copy reviews owed
       (CONTEXT §8.1); D-01 live-Redis lane STILL UNVERIFIED (no plan claimed it closed — restate);
       ci.yml:1633 left narrow by decision; `e2e/strategy-v2-keyboard.spec.ts`'s allowlisted skip
       + its PR #108 follow-up (140.5-06's disposition table).
    4. PHASE GATE — run and paste ALL of: `git diff 4c60518b..HEAD -- package.json package-lock.json`
       → EMPTY (zero-install contract); FULL `npx vitest run` on Node 25 AND
       `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run`; `npx tsc --noEmit`;
       `npm run lint`; `cd analytics-service && python3 -m pytest -q` and
       `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/`; the census
       (0 past-EOF repo-wide, 0 bare on the surface).
    5. VALIDATION.md sign-off: verify every Falsifiability Ledger row — seeded AND planner
       additions (SC-SCAN-1, SC-ENV-1, SC-COPY-1, SC-VENUE-1/2, SC-RA-3, SC-SU-1, SC-AGREE-1,
       SC-424-2, SC-R1-1, SC-SPEC-1, SC-TYPE-1/2, SC-CSV-4/5, SC-PROSE-1..4) — is `Observed ✅`
       with pasted evidence or explicitly skipped-with-reason (the compile-receipt,
       needle-self-test, and census-receipt notes are the sanctioned reasoned recordings);
       complete the Oracle Independence checklist; then set `nyquist_compliant: true` in its
       frontmatter. If ANY row is unobserved, the gate FAILS — report which, do not sign.
  </action>
  <verify>
    <automated>All gate commands above green with outputs pasted in the SUMMARY; registry suite green with the new rows and floor; VALIDATION.md sign-off checklist fully checked</automated>
  </verify>
  <done>Five guards registered atomically with the floor; registry prose truthful without fresh bare integers; gate evidence pasted; ledger signed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guard registry → phase-141 trust | an unregistered guard is invisible slack; a vacuous guard is a green light with no bulb |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.5-19 | Repudiation | citation guard | mitigate | both-polarity end-to-end needle self-tests + 34-file exact population + comment-line floor |
| T-140.5-20 | Tampering | CONTRACT_GUARDS floor | mitigate | literal floor bumped same-commit; `CONTRACT_GUARDS.length` comparison forbidden |
| T-140.5-SC | Tampering | package installs | mitigate | gate diff of package.json/package-lock.json vs 4c60518b → EMPTY, pasted |
</threat_model>

<verification>
The phase gate in Task 3 IS this plan's verification. Additionally: branch protection is OFF —
every gate is advisory at merge; all SUMMARY language says "would have caught", never "did stop"
(CONTEXT §9, settled founder call — do not re-raise).
</verification>

<success_criteria>
- SC-CITE-1 and SC-CITE-2 observed; the guard's mechanism is bare-form-ban, its vacuity finding and
  ambiguity policy stated in its own docblock.
- Five guards registered + floor bumped atomically; registry prose truthful, integer-free or
  predicate-carrying.
- Phase gate fully green on both Nodes and both languages, zero installs, evidence pasted.
- Deferred-items consolidated; VALIDATION.md signed with `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati/140.5-08-SUMMARY.md`.
It is the phase's terminal record: include the gate outputs, the registered-guard table, and the
deferred-items pointer. Do NOT touch STATE.md, ROADMAP.md, DESIGN.md.
</output>
