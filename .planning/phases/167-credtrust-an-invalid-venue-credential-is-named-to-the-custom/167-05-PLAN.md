---
phase: 167-credtrust
plan: 05
type: execute
wave: 4
depends_on: ["167-01", "167-02", "167-03", "167-04"]
files_modified:
  - VERSION
  - package.json
  - CHANGELOG.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [D-01, D-14]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low   # DERIVED: zero calibration samples available in this checkout
must_haves:
  truths:
    - "Both full suites are green before anything is released."
    - "The release carries a VERSION bump and a CHANGELOG entry in ONE commit, so a filtered PR branch cherry-picks them together."
    - "The ROADMAP's Depends on: attribution names the phase that actually activated the ledger refresh."
  artifacts:
    - "VERSION and package.json byte-equal 4-digit strings"
    - "CHANGELOG.md — one unified entry for this version, cross-checked against every commit on the branch"
  key_links:
    - "every commit on this branch maps to at least one CHANGELOG bullet"
---

<objective>
Gate the phase and release it: full suites, planning hygiene, one release commit, and the one
ROADMAP correction 167-CONTEXT deferred to ship time.

Purpose: `version-gate` fails any PR touching a non-`.planning/` path without moving `VERSION`, and
this repo's CHANGELOG rule is a founder instruction with a MECHANISM — a step that cannot fail has
already been measured failing here.

Output: a green phase and a release commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-CONTEXT.md
</context>

<repo_guardrails>
⛔ **THREE FILES IN THE WORKING TREE ARE MODIFIED ON PURPOSE AND BELONG TO OTHER PHASES:**
`.planning/milestone.lock`, `.planning/state.json`, and
`.planning/phases/164.5.3-*/164.5.3-VERIFICATION.md`.
⛔ NEVER stage, commit, move, revert or `git checkout` any of them, and do not report them as a
dirty tree. ⛔ **Never `git add -A` / `git add .` in this phase** — stage files by explicit path.

⛔ **NEVER write a CI skip trailer in a commit message or a PR body, not even inside a sentence
denying it.** GitHub honours those tokens anywhere in the message, they survive a squash into
`main`, and the board then reads "clean" rather than "absent". Name the concept in prose.
⛔ **Never quote a credential-shaped fixture literal verbatim in CHANGELOG prose** — the
push-to-`main` gitleaks scan sees one squash commit whose diff includes `CHANGELOG.md`, which the
branch-scoped allowlist does not cover.
⛔ **Never run `npm version`** — it rewrites the lockfile and can normalise to 3 digits.
</repo_guardrails>

<tasks>

<task type="auto">
  <name>Task 1: The phase gate — both full suites, both lint chains, planning hygiene</name>
  <files>(none — a verification task)</files>
  <action>
    Run, and read, every one of these. ⛔ A skipped suite is not a pass, and "tests pass" is wrong
    if any were skipped.

    - `npm run test:coverage` — the full TS suite with the ratcheted thresholds. ⛔ Read the live
      thresholds from `vitest.config.ts` by symbol; never from a number restated in `CLAUDE.md`.
      ⛔ If coverage falls, fix the cause; do NOT lower a threshold.
    - `npm run typecheck` and `npm run lint`.
    - The full Python suite:
      `cd "$(git rev-parse --show-toplevel)/analytics-service" && "$PWD/.venv/bin/python" -m pytest -q`
      ⛔ ONLY from that directory, ONLY via that venv — a repo-root pytest misses the cassettes and
      makes LIVE broker calls.
    - `node scripts/mutation-runner/run.mjs` — must exit 0. ⛔ A non-zero exit is a regression;
      `WAIVED_CEILING` is 0 and stays 0.
    - `node scripts/lint-sql-gates.mjs --self-test` then the corpus scan; same for
      `scripts/lint-app-guc.mjs`.
    - `npm run check:planning-hygiene`. ⚠️ It scans TRACKED files only — `git add` the new plan and
      summary files first. ⚠️ The count line and the OK line are DIFFERENT lines; ⛔ never read it
      with `tail -1`.

    ⛔ Run NO database command against any remote. ⛔ Never start a local `uvicorn main:app`.
  </action>
  <verify>
    <automated>npm run typecheck > /tmp/167-05-t1.log 2>&1 && npm run test:coverage >> /tmp/167-05-t1.log 2>&1 && npm run check:planning-hygiene >> /tmp/167-05-t1.log 2>&1; RC=$?; tail -60 /tmp/167-05-t1.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167-05-t1.log && echo OK</automated>
  </verify>
  <done>Every command above ran and passed; nothing was skipped; the SUMMARY names each and its result.</done>
</task>

<task type="auto">
  <name>Task 2: The release commit — VERSION, package.json and CHANGELOG together</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <action>
    ⛔ **FOLLOW THE MECHANISM, NOT THE INTENTION.** This repo's CHANGELOG rule exists because a
    taste-guidance step with no mechanism was measured being skipped on a 672-line ship.

    1. `git log <base>..HEAD --oneline` — enumerate EVERY commit on the branch and **COUNT them**.
       This list is a CHECKLIST, not background reading.
    2. `git diff <base>...HEAD` — read the full diff. What a commit SAYS and what it CHANGED
       diverge, and this repo has a dated record of that class.
    3. Group by theme BEFORE writing a line: new capability · behaviour change · bug fix · removal ·
       infrastructure/tooling/tests · refactor.
    4. Write ONE unified entry, newest first, `## [X.Y.Z.B] - YYYY-MM-DD`. Replace any earlier
       per-commit entries for this branch with the unified one.
    5. ⛔ **CROSS-CHECK AND TREAT A MISS AS A DEFECT:** every commit from step 1 maps to at least
       one bullet. An unrepresented commit is not "minor", it is an unshipped fact.
    6. Use this repo's own section vocabulary (`### Fixed`, `### Added`, `### Changed`,
       `### Removed`, `### Tests`, `### Notes`, `### Security`, `### Root cause`, `### Why`).
       ⛔ Do not flatten a root-cause note into "Fixed". Keep this repo's voice: name the phase, the
       routed deferrals and the known limits — ⛔ do NOT import a product-changelog audience rule
       that says never to mention internal tracking.

    **The version bump is decided on MEANING.** Current `VERSION` is `0.85.0.1`. What this phase
    ships: a new customer-facing state on the keys surface, a new wizard error code, a new wire
    code, and a `sync_status` CHECK-constraint change that auto-applies to PROD. That is a new
    capability plus a schema change, not a patch — so the MINOR digit moves and the two below it
    reset. Confirm the shape against the most recent `## [` headings in `CHANGELOG.md` before
    writing it, and record the reasoning in the SUMMARY.

    ⚠️ `VERSION` and `package.json` must be BYTE-EQUAL 4-digit strings; `VERSION` carries NO
    trailing newline. `src/__tests__/critical-regressions.test.ts` asserts it.
    ⛔ Never `npm version`.

    All three files go in ONE commit, `chore(release): vX.Y.Z.B — <name>`, so a filtered `-pr`
    branch cherry-picks them together — they are non-planning paths and always survive the filter.

    ⚠️ **Record in the CHANGELOG, because they are known limits and not oversights:**
    the strategy→key attribution residual from plan 04's scoping decision; the arms of the retry
    copy family that could not take the classifier guard (plan 02); the pre-existing amber-contrast
    and red-vs-amber findings surfaced but not fixed (plan 03); and that proactive notification
    remains OUT of scope (D-14, deferred to its own phase).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts --reporter=dot > /tmp/167-05-t2.log 2>&1; RC=$?; cat /tmp/167-05-t2.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167-05-t2.log && echo OK</automated>
  </verify>
  <done>
    VERSION and package.json are byte-equal 4-digit strings in one commit with the CHANGELOG entry;
    the SUMMARY records the commit COUNT from step 1 and confirms every one maps to a bullet.
  </done>
</task>

<task type="auto">
  <name>Task 3: The one ROADMAP correction this phase owns (D-01)</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    167-CONTEXT D-01 measured that `### Phase 167`'s `Depends on:` line attributes the 161.1 ledger
    refresh activation to **Phase 164.7 plan 07**, and that this is WRONG: 164.7 plan 07 recorded
    the DEFERRED path (the founder declined on a failed pre-flight; the migration applied DORMANT
    with nothing scheduled). The activation was taken by **Phase 164.5.1 plan 09** on 2026-09-17,
    and the live evidence in this repo is `scripts/prod-prober/cron-manifest.json`: jobid 40,
    `ledger_refresh_fanout`, `25 * * * *`, active, captured from PRODUCTION.

    ⚠️ Why the correction matters and is not cosmetic: a reader who checks 164.7's status and finds
    it Complete concludes the dependency is met for the wrong reason; a reader who reads 164.7 plan
    07 in full concludes it is NOT met and stalls the phase. Both readings are wrong today.

    ⛔ Use `Edit` with a SCOPED replacement of the `**Depends on:**` sentence only. NEVER `Write`
    the whole ROADMAP — a whole-file write destroys every phase entry outside the diff window.
    ⛔ Do not touch any other phase's section. Keep the correction to the attribution and cite the
    manifest as the live evidence; leave the rest of the sentence, including the 164.8.3 and
    164.5.4 notes, byte-unchanged.

    Then re-run `npm run check:planning-hygiene` (⛔ the count line and the OK line are different
    lines) and commit `.planning/ROADMAP.md` on its own, by explicit path.
  </action>
  <verify>
    <automated>npm run check:planning-hygiene > /tmp/167-05-t3.log 2>&1; RC=$?; cat /tmp/167-05-t3.log; test "$RC" = 0 && echo OK</automated>
  </verify>
  <done>
    The `Depends on:` line names 164.5.1 plan 09 with the manifest as evidence; no other ROADMAP
    section moved (confirmed with `git diff --stat` and by reading the diff); hygiene is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branch -> `main` | a squash merge concatenates every branch commit message into the merge body |
| repo -> public | this repo is PUBLIC and `.planning/` is TRACKED |
| merge -> PROD | `supabase/migrations/**` auto-applies on merge (plan 03's migration) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-18 | Tampering | commit messages | high | mitigate | ⛔ No CI skip trailer, ever — it fires from inside a sentence denying it and rides into `main` through the squash body, after which the board reads "clean" rather than "absent". Verify CI by COUNTING checks bound to the head SHA, never by "no red". |
| T-167-19 | Information Disclosure | `CHANGELOG.md` prose | high | mitigate | ⛔ No credential-shaped fixture literal is quoted verbatim; the push-to-`main` gitleaks scan covers the squash diff including `CHANGELOG.md`, which the branch-scoped allowlist does not. ⛔ No key identifier, strategy name, account number or broker server name — placeholders only. |
| T-167-20 | Tampering | `.planning/` writes by agents | medium | mitigate | `npm run check:planning-hygiene` runs after any `.planning/` write. ⛔ A proof-of-absence must record a VERDICT and a COUNT, never a specimen and never the pattern searched for. |
| T-167-21 | Denial of Service | version gate | medium | mitigate | `version-gate` fails any PR touching a non-`.planning/` path without moving `VERSION`; the bump and the CHANGELOG land in ONE commit so a filtered `-pr` branch carries both. |
| T-167-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No packages are installed by this plan.** ⛔ `npm version` is never run. |
</threat_model>

<verification>
- Both full suites green; both lint chains clean; mutation runner exits 0; planning hygiene green.
- `git diff --stat` on the ROADMAP commit shows exactly one file and a scoped change.
- ⛔ The three cross-phase working-tree files are untouched — confirm by reading `git status`, not
  by staging anything.
</verification>

<success_criteria>
- Nothing was skipped, and the SUMMARY says so command by command.
- Every branch commit maps to a CHANGELOG bullet, with the count stated.
- The ROADMAP attribution is corrected in a scoped edit and nothing else moved.
</success_criteria>

<output>
Create `.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-05-SUMMARY.md` when done
</output>
