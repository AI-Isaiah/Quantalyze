---
phase: 167-credtrust
plan: 03
type: execute
wave: 2
depends_on: ["167-01"]
files_modified:
  - supabase/migrations/20260922120000_api_keys_sync_status_sign_in_failed.sql
  - supabase/tests/test_api_keys_sync_status_sign_in_failed.sql
  - scripts/mutation-runner/run.mjs
  - src/components/exchanges/AllocatorSyncStatus.tsx
  - src/components/exchanges/AllocatorSyncStatus.test.tsx
autonomous: false
requirements: [D-03, D-04, D-05, D-11, D-12, D-13]
estimate:
  tokens: 130000
  raw_tokens: 65000
  tasks: 3
  confidence: low   # DERIVED: zero calibration samples available in this checkout
must_haves:
  truths:
    - "A key whose sign-in failed renders a pill that says so and an AUTHORED helper line that ignores api_keys.sync_error (D-05)."
    - "The helper names the remedy and promises no retry."
    - "The shared error / complete_with_warnings helper branch is byte-unchanged, so complete_with_warnings behaves exactly as before."
    - "The new sync_status value can never render as a neutral idle pill, even in a partial rollout."
    - "Nothing about the public factsheet payload changes; no cause field enters the id-keyed cache (D-04)."
  artifacts:
    - "supabase/migrations/20260922120000_api_keys_sync_status_sign_in_failed.sql — a CHECK widening with a SCHEMA-reading self-verify block"
    - "supabase/tests/test_api_keys_sync_status_sign_in_failed.sql — a CI gate over the widened constraint"
    - "src/components/exchanges/AllocatorSyncStatus.tsx — a PILL_STYLES row, a pill label, an authored helper constant and its own branch"
  key_links:
    - "api_keys.sync_status = sign_in_failed -> PILL_STYLES lookup -> amber pill, never the idle fallback"
    - "the helper-resolution chain's new branch sits BELOW helperOverride, so the optimistic cell keeps its existing behaviour"
---

<objective>
Close D-11 and ship the owner-facing half of the phase: an owner looking at their keys sees
"Sign-in failed" and a sentence naming the remedy, instead of a raw database string that promises a
retry which cannot succeed.

Purpose: D-05's owner half — `AllocatorSyncStatus`'s `error` arm renders
`api_keys.sync_error` verbatim, which is how "MT5 terminal unreachable — sync will retry
automatically." reached a customer and sat there for 17 days.

Output: one new `sync_status` value, its CHECK-constraint migration, its CI gate, and the pill +
authored helper — the style map and the migration in the SAME commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
@DESIGN.md

@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-CONTEXT.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-UI-SPEC.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-PATTERNS.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-VALIDATION.md

@src/components/exchanges/AllocatorSyncStatus.tsx
</context>

<repo_guardrails>
⛔ **THREE WORKING-TREE FILES ARE MODIFIED ON PURPOSE AND BELONG TO OTHER PHASES:**
`.planning/milestone.lock`, `.planning/state.json`, and
`.planning/phases/164.5.3-*/164.5.3-VERIFICATION.md`.
⛔ NEVER stage, commit, move, revert or `git checkout` any of them, and do not report them as a
dirty tree. ⛔ **Never `git add -A` / `git add .`** — stage files by explicit path.

⛔ **Never write a CI skip trailer in a commit message or a PR body, not even to deny it.** GitHub
honours those tokens anywhere in the message and they survive a squash into `main`. Name the
concept in prose.
⛔ **No credential, connection string, project ref, key, absolute home path, local username, venue
API key, MT5 account number, broker server name, real key identifier or real strategy name in any
committed artifact.** Placeholders only; truncate UUIDs to 8 characters. Error codes, timestamps,
durations and day-counts are safe; identifiers are not.
⛔ **A proof-of-absence can publish what it proves absent** — record VERDICTS and COUNTS, never
specimens and never the pattern searched for.
⛔ **Restore only from a `cp` byte backup verified with `cmp`, re-taken after every edit.** Never
`git checkout -- <path>`, `git restore` or `git stash` — the stash stack is SHARED across worktrees
and `git checkout --` silently destroys uncommitted work in a neuter/restore harness.
⛔ **Run NO database command against any remote** — this checkout's Supabase CLI is linked to
PRODUCTION. ⛔ Never start a local `uvicorn main:app`. ⛔ No `railway redeploy`, restart or `ssh`
into the live MT5 gateway. ⛔ The founder enters credentials; no agent enters, reads, decrypts, logs
or echoes one, ever.
⛔ **Nothing lowers a floor, raises a ceiling, relaxes a timeout or adds a waiver.** ⛔ Never clear
a red by widening an allowlist.
⛔ **Cite by SYMBOL, never by line number** — line numbers in this repo rot within a commit.
⛔ **No Grok. No Codex.**
⚠️ `grep` is SILENTLY BLIND to NUL-bearing files — always `grep -a`. ⚠️ `grep -c` EXITS 1 on a zero
count. ⚠️ `$?` after a PIPELINE measures the LAST command. ⚠️ A leading-dash pattern is eaten as a
flag — use `-e` or python.
⚠️ Run `npm run check:planning-hygiene` after any `.planning/` write; ⛔ never read its result with
`tail -1` (the count line and the OK line are different lines).
</repo_guardrails>

<tasks>

<task type="checkpoint:decision">
  <name>Checkpoint: D-11 — the credential failure gets a NEW `sync_status` value (arm B). Confirm before the one-way act.</name>
  <reversibility rating="one-way">A CHECK-constraint migration on `api_keys`; `supabase/migrations/**` auto-applies to TEST and then PROD on merge. Once a PROD row carries the value, removing it means a second migration plus a data migration.</reversibility>
  <action>
    Present the decision below, with its evidence, and WAIT. ⛔ Do not author the migration before
    this gate clears. The planner has already CLOSED D-11 on measurement; this checkpoint exists so
    a human can veto before an irreversible act, not to re-open the analysis.

    ## The decision: arm B — mint `sync_status = 'sign_in_failed'`

    ## ⛔ Arm A (reuse `revoked`) is REJECTED — and the usual "arm A is cheapest" framing is
    ## MEASURED WRONG in three independent ways

    1. ⛔⛔ **IT SELF-LATCHES, AND NOTHING IN CONTEXT.md OR RESEARCH.md SAW THIS.** Measured at
       HEAD `66466242` in `supabase/migrations/20260420073003_allocator_holdings.sql`:
       `enqueue_poll_allocator_positions_for_all_keys` loops
       `WHERE is_active = true AND sync_status IS DISTINCT FROM 'revoked'`, and PROD cron jobid 15
       (`poll-allocator-positions`, `0 4 * * *`, `active: true` in
       `scripts/prod-prober/cron-manifest.json`) is what drives it. ⇒ routing a failure onto
       `revoked` removes the key from **the very daily job that writes its status**. The state then
       has NO automatic exit: if the customer fixes their credential, the product never notices.
       CONTEXT.md D-11 said arm A "stops enqueueing its refresh"; the measurement is worse — it
       stops the poll that writes the signal this phase is about.
    2. It **REWORDS A LOCKED CONSTANT.** The helper chain reaches `REVOKED_HELPER` on the `revoked`
       branch before the `error` branch, and that text ("Re-add a read-only key from your
       exchange.") is venue-WRONG for MT5, which has a password and no key to re-add.
    3. Its blast radius is the largest, not the smallest: `IS DISTINCT FROM 'revoked'` appears in
       **eight** tracked migrations plus two Python services plus `queries.ts`'s connected-key
       predicate, `HoldingsTable` (×4), `OpenPositionsTable` and `AllocationDashboardV2` — every one
       changing meaning silently.

    ## ⛔ Arm C (a separate additive signal) is REJECTED — it does not avoid the migration

    - An additive column is STILL a migration that auto-applies to PROD, so arm C buys no safety on
      the axis it was supposed to buy it on. It ADDS a column-level `GRANT SELECT` and a query
      projection change on top.
    - ⛔ It forces a `src/lib/database.types.ts` change, and there is **no safe way to regenerate
      that file from this checkout** — the Supabase CLI here is linked to PRODUCTION and no database
      command may be run. A hand edit is drift by construction.
    - ⛔ It creates a SECOND SOURCE OF TRUTH whose clear-on-success is a separate write.
      167-UI-SPEC's 9-state *success* cell is binding: "the write that moves a key into `complete`
      must leave no credential text behind … in the SAME write." With one column that clear is
      structurally free; with two it is a write that can drift, producing exactly the "stale cause
      beside a fresh success" the spec calls the inverse of this phase's own defect.
    - Its "narrow the `error` branch" edit lands on the SHARED condition
      `normalized === "error" || normalized === "complete_with_warnings"` — one branch serving two
      states (the UI-checker's finding 1).

    ## ✅ Why arm B, measured

    - `sync_status` is typed `string | null` in `src/lib/database.types.ts` (measured) ⇒ **the
      generated types do not move at all.** No regeneration, no hand edit, no drift.
    - The column is already projected, already granted to `authenticated`, already rendered ⇒ zero
      plumbing.
    - The new value is in NO existing exclusion predicate, so every current reader treats it exactly
      as `error` is treated today — and the daily poll keeps running, so the state **self-heals**
      the moment a successful sync writes `complete`.
    - It is a pure EXTENSION of the locked copy table, never a reword — so the UI-checker's
      finding 1 is moot: the shared `error || complete_with_warnings` branch is not touched at all.
    - 167-UI-SPEC calls it "the one arm where the tone is actually chosen", and DESIGN.md's
      2026-07-02 semantic gate says a state a disclosed one-click user action reverses is **amber**,
      and that red is FORBIDDEN for it.
    - It gives the phase ONE vocabulary end to end: wire `SIGN_IN_FAILED` → `KEY_SIGN_IN_FAILED` →
      `sync_status = 'sign_in_failed'` → pill "Sign-in failed".

    ## What arm B costs, named rather than hidden

    - ⛔ A `DROP CONSTRAINT` / `ADD CONSTRAINT` migration that **auto-applies to TEST and then PROD
      on merge.** D-13's three reviewers run before it is proposed (Task 3).
    - ⛔ `PILL_STYLES` normalises an unknown key to `"idle"` — a NEUTRAL pill. A partial rollout
      would render a BROKEN key as HEALTHY. Mitigated by landing the style map and the migration in
      the SAME commit, plus a test that fails if the value ever normalises to idle.
    - ⛔ `sync_error_copy` falls back to `SYNC_ERROR_COPY_BY_STATUS["error"]` for an unknown status
      (measured) — a second silent fallback. Plan 04 adds the row; this plan records the dependency.

    ## The boundary this decision does NOT cross

    ⛔ Nothing here touches `fetchAndBuildPayload`, the cached factsheet wrapper or the share route.
    No field is added to `FactsheetPayload`. D-04 is honoured by NOT DOING THE THING — and that is
    the only way to honour it, because the existing guard
    (`src/__tests__/phase-148-owner-lane-cache-isolation.test.ts`) is a structural source-text
    guard with ZERO field-level assertions (measured — this CORRECTS 167-RESEARCH's claim that it
    "covers any new field"). A cause field added to the payload would be cached by id and served to
    anonymous readers for the full 3600 s TTL **with that guard green**.

    **Ask:** confirm arm B, or name the arm to take instead.
  </action>
  <done>A human has confirmed the arm. The chosen arm and the confirmation are recorded in the SUMMARY.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a signed-out key renders as Sign-in failed with an authored helper" — one value, one commit</name>
  <files>supabase/migrations/20260922120000_api_keys_sync_status_sign_in_failed.sql, src/components/exchanges/AllocatorSyncStatus.tsx, src/components/exchanges/AllocatorSyncStatus.test.tsx</files>
  <precondition>The D-11 checkpoint above has been confirmed.</precondition>
  <read_first>
    167-PATTERNS Pattern Assignment 8 — `20260420073003_allocator_holdings.sql` STEP 5 (the DROP+ADD
    widening, verbatim) and its self-verifying DO block arm (h), verbatim.
    167-PATTERNS Pattern Assignment 6 — `PILL_STYLES`, `REVOKED_HELPER`, the `EM_DASH` constant, the
    helper-resolution chain verbatim, and the three chain-order consequences.
    167-UI-SPEC § Copywriting Contract §1 (the helper string and every property of it), § Color (the
    amber trio and the contrast argument), § D-11 Arm Variance (the arm-B column), § 9-State Matrix
    S1, § Accessibility.
    `src/components/exchanges/AllocatorSyncStatus.test.tsx` — the existing `renders 'Key revoked' …`
    case and the `syncError` non-vacuity control.
  </read_first>
  <behavior>
    - Rendering the component with the new status produces pill text "Sign-in failed".
    - The helper line renders the authored sentence from 167-UI-SPEC §1 VERBATIM, including the
      U+2014 em dash and the terminating period, composed through the file's existing `EM_DASH`
      constant in the source and asserted with an escape in the test.
    - ⭐ **The non-vacuity control that makes the case able to fail the way the defect fails:**
      render the new status WITH a non-empty `syncError` and assert the helper is the authored
      constant and does NOT contain the `syncError` text. Without that control the case proves
      nothing.
    - The pill is amber, not neutral: assert the rendered class list carries the warning tokens and
      NOT the idle tokens. ⛔ This is the guard against the `PILL_STYLES` unknown-key fallback and
      it is the phase's worst silent-failure mode.
    - `role="status"` and `aria-live="polite"` on the helper are unchanged; the pill still carries
      no `aria-live`.
    - Regression control: rendering with `complete_with_warnings` and with `error` produces exactly
      what it produces today, `syncError` pass-through included.
  </behavior>
  <action>
    ⛔ **THE MIGRATION AND THE STYLE MAP LAND IN THE SAME COMMIT.** Never a follow-up. This is the
    UI-checker's finding 3 and the phase's worst silent-failure mode.

    (a) The migration. Copy `20260420073003_allocator_holdings.sql` STEP 5's shape: `DROP CONSTRAINT
    IF EXISTS` then `ADD CONSTRAINT` re-typing the FULL value list. ⛔ **Re-run the analog's own
    grep first and record the answer in a comment** — "no migration in between touched this CHECK
    (verified via grep)" is part of the pattern, not decoration, because a DROP+ADD that re-types a
    stale list silently REMOVES whatever was added since. The list as measured today is the eight
    values `idle`, `syncing`, `computing`, `complete`, `complete_with_warnings`, `error`, `revoked`,
    `rate_limited`; re-derive it from `pg_constraint`-shaped evidence in the migration history
    rather than trusting this sentence, and add the ninth.

    Add the self-verifying DO block on the analog's arm-(h) shape: read
    `pg_get_constraintdef(oid)` for `api_keys_sync_status_check` and `RAISE EXCEPTION` if it is
    NULL or does not carry the new value.
    ⛔⛔ **THE DO BLOCK MUST READ THE SCHEMA, NEVER THE DATA.** A data-reading `DO` block that
    `RAISE EXCEPTION`s on an unexpected count can apply cleanly to PROD and REFUSE on shared TEST —
    whose `public` tables are a schema-only restore and hold only what CI wrote — and a failed TEST
    apply BLOCKS the PROD apply. That is the recorded `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]`
    class, whose interim remedy is to REVERT THE MERGE. ⛔ Never edit `supabase-migrate.yml` to get
    a deploy out.
    Pick a filename timestamp strictly greater than the highest existing migration — measured at
    HEAD the highest is `20260920120000`.
    ⛔ **Do NOT run `supabase db push`, `psql`, or any other database command from this checkout.
    Its CLI is linked to PRODUCTION.** The migration is verified by the local throwaway cluster in
    Task 2 and by CI, never by hand against a remote.

    (b) `PILL_STYLES` gains one row for the new value, using the DESIGN.md-pinned OPAQUE amber trio
    that 167-UI-SPEC § Color specifies: `--color-warning` text on `--color-warning-bg` with a
    `--color-warning-border` hairline. The map's value type today is `{ bg; text }` with no border
    slot — widen it with an OPTIONAL `border` field and append that class only when present, so no
    existing entry changes. ⚠️ 167-UI-SPEC chose the opaque trio over the `bg-warning/10` alpha fill
    the neighbouring pills use for a measured contrast reason; do NOT "match the neighbours".
    ⛔ Do NOT restyle any existing pill. The red-vs-amber divergence between `AllocatorSyncStatus`
    and `HoldingsTable`, and the existing amber pills measuring below 4.5:1, are BOTH recorded in
    167-UI-SPEC § Open Questions as out of scope — surface them in the SUMMARY, fix neither.

    (c) The pill label: a new `case` producing "Sign-in failed" (167-UI-SPEC's Arm Variance table).

    (d) The authored helper: a new `const` beside `REVOKED_HELPER`, carrying the 167-UI-SPEC §1
    string composed through `EM_DASH`, with a `// LOCKED helper copy` comment matching the file's
    own convention. Add ONE new `else if` branch to the helper-resolution chain for the new status.
    ⛔ It goes **below** the `helperOverride` branch — the chain order is what enforces
    167-UI-SPEC's *optimistic* cell for free, and only while it stays below.
    ⛔ The shared `normalized === "error" || normalized === "complete_with_warnings"` branch is
    BYTE-UNCHANGED. Arm B needs no narrowing and no split; every non-credential failure keeps
    rendering the curated `sync_error` exactly as today.

    (e) The tests, in the existing `AllocatorSyncStatus.test.tsx` harness, covering every behaviour
    above. Prove each can fail: delete the `PILL_STYLES` row and observe the amber assertion RED
    (this is the fallback-to-idle drill and it must be run); replace the authored helper with a
    `syncError` pass-through and observe the non-vacuity control RED. Restore from a `cp` byte
    backup verified with `cmp` AND a sha256 match, re-taken after every edit. ⛔ Never
    `git checkout -- <path>`, `git restore` or `git stash`.
  </action>
  <verify>
    <automated>npx vitest run src/components/exchanges/AllocatorSyncStatus.test.tsx --reporter=dot > /tmp/167-03-t1.log 2>&1; RC=$?; cat /tmp/167-03-t1.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167-03-t1.log && echo OK</automated>
  </verify>
  <done>
    The migration, the style row, the label and the authored helper are in ONE commit; the amber
    assertion and the `syncError` non-vacuity control were each observed RED under their own neuter;
    the `error` and `complete_with_warnings` regression cases pass unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: The SQL gate over the widened constraint — and every corpus census it moves</name>
  <files>supabase/tests/test_api_keys_sync_status_sign_in_failed.sql, scripts/mutation-runner/run.mjs</files>
  <read_first>
    A neighbouring `api_keys` gate as the house shape — `supabase/tests/test_api_keys_venue_identity_uniq.sql`.
    `scripts/lint-sql-gates.mjs` (the seven static rules every gate must satisfy) and
    `scripts/pg-lane/README.md` (how to run a gate on a throwaway local cluster).
  </read_first>
  <action>
    Write the gate. It asserts that `api_keys_sync_status_check` ADMITS the new value AND still
    admits every prior value — the second half is the guard against a DROP+ADD that re-typed a
    stale list, which is the analog's own named hazard.

    ⛔ Run it on the LOCAL THROWAWAY cluster only: `scripts/pg-lane/run.sh`. ⛔ Never against any
    remote — this checkout's Supabase CLI is linked to PRODUCTION.

    ⛔ **ADDING A FILE TO `supabase/tests/` MOVES SEVERAL INDEPENDENT CENSUSES, AND A BLANKET
    N→N+1 ACROSS THEM HAS A DATED RECORD IN THIS REPO OF KILLING DELIBERATE MISMATCH
    CALIBRATIONS.** So:
    - Read `FILES_FLOOR`, `ARMS_FLOOR` and `WAIVED_CEILING` **BY SYMBOL**:
      `grep -nE '^export const (FILES_FLOOR|ARMS_FLOOR|WAIVED_CEILING)' scripts/mutation-runner/run.mjs`.
      ⛔ Never from a number restated in `CLAUDE.md` or in this plan — that file's own record shows
      its restated values going stale four times.
    - Then RUN `node scripts/mutation-runner/run.mjs` and read the real figures off the run: the
      coverage line, the two independent arm tallies (`arms:` vs `lane-invocations:`, which must
      AGREE), `lane-blocked:`, `lane-probe:` and the `unreachable:` class. ⛔ Do not predict them.
    - ⛔ A run that exits NON-ZERO is a regression. `WAIVED_CEILING` is 0 and stays 0 — do NOT add a
      waiver, fix the cause. ⛔ Nothing lowers a floor, raises a ceiling or relaxes a timeout.
    - Move only the floors the RUN's own failure messages name, each with its own re-run argument.
      Note that `scripts/mutation-runner/run.mjs` gates on `annotatedFiles < FILES_FLOOR`, so a
      floor set BELOW the corpus is invisible to it; the stale-low direction is caught one layer up
      by `src/__tests__/mutation-runner-floors.test.ts`.

    Run `node scripts/lint-sql-gates.mjs --self-test` then `node scripts/lint-sql-gates.mjs`, and
    `node scripts/lint-app-guc.mjs --self-test` then `node scripts/lint-app-guc.mjs`. ⛔ A red corpus
    step is a regression and is NEVER cleared by widening an allowlist.
  </action>
  <verify>
    <automated>node scripts/lint-sql-gates.mjs --self-test > /tmp/167-03-t2.log 2>&1 && node scripts/lint-sql-gates.mjs >> /tmp/167-03-t2.log 2>&1 && node scripts/mutation-runner/run.mjs >> /tmp/167-03-t2.log 2>&1; RC=$?; tail -60 /tmp/167-03-t2.log; test "$RC" = 0 && echo OK</automated>
  </verify>
  <done>
    The gate passes on the local throwaway cluster, the gate linters are green, the mutation runner
    exits 0, and every floor that moved did so against its own run-reported failure message with its
    own argument recorded.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: three reviewers on the migration (D-13), then a human sign-off before it can reach a branch that merges</name>
  <action>
    ⛔ **STANDING REPO RULE, RESTATED BECAUSE THIS PHASE PRODUCES A MIGRATION.** Before the
    migration is proposed for apply — and on this repo the APPLY IS THE MERGE, because
    `supabase/migrations/**` auto-applies to TEST and then PROD — run all three reviewers over it:
    `migration-reviewer`, `rls-policy-auditor`, `silent-failure-hunter`. Fix every finding. ⚠️ A FIX
    ROUND IS WHERE REGRESSIONS ENTER — re-run the reviewers after fixing.

    Then present to the human:
    - the migration diff,
    - each reviewer's findings and what was done about them,
    - the mutation-runner and gate-linter output from Task 2,
    - the measured statement that the self-verify DO block reads the SCHEMA and not the DATA, and
      why that distinction blocks a PROD deploy if it is got wrong,
    - a plain statement that merging this branch applies the constraint change to PROD.

    ⛔ This gate is NEVER auto-approvable, regardless of `workflow.auto_advance`.
  </action>
  <verify>
    <human-check>The human has seen all three reviewers' findings and the migration diff, and has approved the migration for merge.</human-check>
  </verify>
  <done>Three reviewer runs recorded in the SUMMARY with their findings and resolutions; human approval recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> PROD database | `supabase/migrations/**` auto-applies to TEST then PROD on merge |
| `api_keys` row -> owner browser | `sync_status` / `sync_error` cross into rendered copy |
| owner lane -> public lane | the id-keyed factsheet cache, 3600 s TTL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-08 | Tampering | `PILL_STYLES` unknown-key fallback | **critical** | mitigate | The fallback normalises an unknown value to `idle`, a NEUTRAL pill — a partial rollout renders a BROKEN key as HEALTHY. Mitigated by landing the style row in the SAME commit as the migration AND by a test that fails if the value ever resolves to the idle style. |
| T-167-09 | Denial of Service | the CHECK-constraint migration | high | mitigate | A DROP+ADD that re-types a stale value list silently removes values added since. Mitigated by re-running the analog's grep and recording the answer inline, plus a schema-reading self-verify block that RAISEs on a wrong constraint definition. |
| T-167-10 | Denial of Service | shared TEST apply | high | mitigate | A DATA-reading `DO` block can apply on PROD and REFUSE on TEST, and a failed TEST apply blocks the PROD apply. Mitigated by making the self-verify block read `pg_constraint` only. ⛔ Interim remedy for such a block is REVERT THE MERGE, never an edit to `supabase-migrate.yml`. |
| T-167-11 | Information Disclosure | factsheet payload | **critical** | mitigate | ⛔ No field is added to `FactsheetPayload`; `fetchAndBuildPayload`, the cached wrapper and the share route are untouched. MEASURED: the existing owner-lane guard is structural source-text only, with ZERO field-level assertions — it would stay GREEN while a new field leaked for the full TTL. The mitigation is not doing it; if a future checkpoint ever approves a field, a NEW field-level guard is owed. |
| T-167-12 | Elevation of Privilege | column grants | low | accept | No new column and no new grant. `sync_status` is already granted; widening its value set changes no privilege. `public.ledger_refresh_staleness` is not consumed by this plan, so D-12's grant posture is untouched — the phase does not build the SECDEF wrapper that has no in-repo precedent. |
| T-167-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No packages are installed by this plan.** An install would require 167-RESEARCH's Package Legitimacy Audit to be run first; it currently records "not applicable". |
</threat_model>

<verification>
- `npx vitest run src/components/exchanges/ --reporter=dot` green.
- `npm run typecheck` clean; `npm run lint` clean.
- `node scripts/lint-sql-gates.mjs --self-test` and the corpus scan green; `node scripts/mutation-runner/run.mjs` exits 0.
- ⛔ Run NO database command against any remote. The SQL gate is verified on `scripts/pg-lane/run.sh` only.
</verification>

<success_criteria>
- The new `sync_status` value renders an amber "Sign-in failed" pill and the authored helper, and
  can never resolve to the neutral idle style.
- The shared `error` / `complete_with_warnings` helper branch is byte-unchanged.
- Three reviewers ran on the migration and a human approved it.
- The SUMMARY records: the D-11 arm and its confirmation; the pre-existing amber-contrast and
  red-vs-amber findings surfaced and NOT fixed; and the `sync_error_copy` fallback dependency that
  plan 04 closes.
</success_criteria>

<output>
Create `.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-03-SUMMARY.md` when done
</output>
