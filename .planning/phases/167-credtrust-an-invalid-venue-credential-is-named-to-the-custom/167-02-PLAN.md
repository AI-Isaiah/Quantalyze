---
phase: 167-credtrust
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/allocator_positions.py
  - analytics-service/tests/test_allocator_positions.py
autonomous: true
requirements: [D-09, D-10]
estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: low   # DERIVED: zero calibration samples available in this checkout
must_haves:
  truths:
    - "No end-user note in the allocator holdings path promises a retry for a failure classify_exception calls permanent (D-10)."
    - "The fix covers the whole venue-agnostic copy family, not the MT5 string alone (D-09)."
    - "The rate-limit note keeps its retry promise, because rate limits ARE transient by construction."
    - "classify_exception stays the sole authority: it is CONSULTED via _must_reach_handler_unwrapped, never mirrored."
  artifacts:
    - "analytics-service/services/allocator_positions.py — every exception-catching arm that raises a retry-promising note carries the classifier guard ahead of the note"
    - "analytics-service/tests/test_allocator_positions.py — an equivalence-oracle case per newly guarded arm, parametrized over real exception shapes"
  key_links:
    - "fetch_allocator_holdings MT5/sFOX arms -> _must_reach_handler_unwrapped -> job_worker.classify_exception"
    - "a permanent-classified failure now reaches run_poll_allocator_positions_job's generic arm unwrapped, where sync_error_copy writes authored copy that promises nothing"
---

<objective>
Finish a fix this repo already half-shipped: make the retry PROMISE a function of the retry
DISPOSITION across the whole allocator copy family, not just the two ccxt arms that already do it.

Purpose: D-09/D-10. `analytics-service/services/allocator_positions.py` raises
`MT5_UNREACHABLE_NOTE` / `MT5_MISSING_ACCOUNT_REF_NOTE` / `SFOX_FETCH_FAILED_NOTE`
UNCONDITIONALLY across its MT5 and sFOX arms, while its two ccxt arms consult the classifier first.
The measured wrong-password path — `except Mt5ClientError` — is one of the unguarded ones.

Output: the two-line guard `if _must_reach_handler_unwrapped(exc): raise` placed ahead of the note
in every arm that has an exception to classify, each proved by the equivalence oracle already
written for the ccxt arms.

⭐ **REUSE, NOT INVENTION.** The guard symbol, its module, its import and its unit tests all
already exist two call sites away. 167-PATTERNS Pattern Assignment 7 calls this "A PRECEDENT
EXISTS, and it is only HALF applied. That gap IS the phase's Python work."
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md

@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-CONTEXT.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-PATTERNS.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-VALIDATION.md
</context>

<repo_guardrails>
⛔ **THREE WORKING-TREE FILES ARE MODIFIED ON PURPOSE AND BELONG TO OTHER PHASES:**
`.planning/milestone.lock`, `.planning/state.json`, and
`.planning/phases/164.5.3-*/164.5.3-VERIFICATION.md`.
⛔ NEVER stage, commit, move, revert or `git checkout` any of them, and do not report them as a
dirty tree. ⛔ **Never `git add -A` / `git add .`** — stage files by explicit path.

⛔ **Never write a CI skip trailer in a commit message or a PR body, not even to deny it.** GitHub
honours those tokens anywhere in the message and they survive a squash into `main`. Name the
concept in prose.
⛔ **No credential, connection string, project ref, key, absolute home path, local username, venue
API key, MT5 account number, broker server name, real key identifier or real strategy name in any
committed artifact.** Placeholders only; truncate UUIDs to 8 characters. Error codes, timestamps,
durations and day-counts are safe; identifiers are not.
⛔ **A proof-of-absence can publish what it proves absent** — record VERDICTS and COUNTS, never
specimens and never the pattern searched for.
⛔ **Restore only from a `cp` byte backup verified with `cmp`, re-taken after every edit.** Never
`git checkout -- <path>`, `git restore` or `git stash` — the stash stack is SHARED across worktrees
and `git checkout --` silently destroys uncommitted work in a neuter/restore harness.
⛔ **Run NO database command against any remote** — this checkout's Supabase CLI is linked to
PRODUCTION. ⛔ Never start a local `uvicorn main:app`. ⛔ No `railway redeploy`, restart or `ssh`
into the live MT5 gateway. ⛔ The founder enters credentials; no agent enters, reads, decrypts, logs
or echoes one, ever.
⛔ **Nothing lowers a floor, raises a ceiling, relaxes a timeout or adds a waiver.** ⛔ Never clear
a red by widening an allowlist.
⛔ **Cite by SYMBOL, never by line number** — line numbers in this repo rot within a commit.
⛔ **No Grok. No Codex.**
⚠️ `grep` is SILENTLY BLIND to NUL-bearing files — always `grep -a`. ⚠️ `grep -c` EXITS 1 on a zero
count. ⚠️ `$?` after a PIPELINE measures the LAST command. ⚠️ A leading-dash pattern is eaten as a
flag — use `-e` or python.
⚠️ Run `npm run check:planning-hygiene` after any `.planning/` write; ⛔ never read its result with
`tail -1` (the count line and the OK line are different lines).
</repo_guardrails>

<scope_note>
⛔ **WHAT THIS PLAN DOES NOT DO, stated so it is not drifted into.**

- It does **not** invent verdict-SELECTED wording. 167-PATTERNS' "No Analog Found" section is
  explicit: there is a precedent for the CONTROL-FLOW shape (`if _must_reach_handler_unwrapped(exc):
  raise` ahead of the note) and **no precedent anywhere for a string template whose TEXT is chosen
  by the verdict**. No dict keyed on permanent/transient exists in the copy path, and the module's
  own `SYNC_ERROR_COPY_BY_STATUS` docblock argues against minting one. ⛔ Plan the GUARD, not a new
  copy-selection mechanism.
- It does **not** touch `SYNC_ERROR_COPY_BY_STATUS["rate_limited"]`. That note's retry promise is
  LEGITIMATE — rate limits are transient by construction — and removing it would be a NEW
  dishonesty (167-RESEARCH Pitfall 2 names this by name).
- It does **not** touch the second, byte-identical `_map_exception_to_sync_status` in
  `equity_reconstruction.py`. 167-CONTEXT defers that de-duplication to its own phase.
- It does **not** close the phase on its own. ⚠️ D-10 says so in its own text: the measured MT5
  wrong-password case arrives as a TRANSPORT failure and classifies **transient**, so this plan
  leaves that note truthful-but-useless. Naming the credential is plan 03 + plan 04's job.
</scope_note>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a permanent MT5 failure stops being wrapped in a retry promise" — one arm only</name>
  <files>analytics-service/services/allocator_positions.py, analytics-service/tests/test_allocator_positions.py</files>
  <read_first>
    167-PATTERNS Pattern Assignment 7 — the verbatim ccxt analog, `_must_reach_handler_unwrapped`'s
    docblock (why the authority is CONSULTED and not mirrored, and what a hand-copied allow-list
    already cost), and the measured table of which arms are guarded.
    `analytics-service/tests/test_allocator_positions.py` — the equivalence-oracle case
    parametrized over `exc_factory`, and its docblock stating the oracle rule.
  </read_first>
  <behavior>
    - For an exception `classify_exception` calls permanent, the `Mt5ClientError` arm re-raises it
      UNWRAPPED — the handler receives the identical object (asserted by identity, `is`, as the
      existing oracle does).
    - For everything else, the arm still wraps into the existing fixed end-user copy and preserves
      `__cause__`.
    - The expectation is DERIVED from `classify_exception` inside the test, never hard-coded as a
      list, so the case cannot go stale when the classifier grows a family.
    - Non-vacuity: the case is observed RED with the guard removed.
  </behavior>
  <action>
    Add `if _must_reach_handler_unwrapped(exc): raise` as the FIRST statement inside the
    `except Mt5ClientError` arm of `fetch_allocator_holdings`, ahead of the log line and ahead of
    the `raise AllocatorHoldingsSyncTransientError(MT5_UNREACHABLE_NOTE)`. Copy the placement and
    the surrounding comment discipline from the two ccxt arms in the same function verbatim — same
    symbol, same module, already imported.

    ⭐ Why this arm first: it is the MEASURED wrong-password path (167-PATTERNS' table marks it
    "this is the measured wrong-password path"), so proving the guard here proves the slice that
    matters on the arm the phase exists for.

    Extend the existing equivalence-oracle test rather than writing a new harness. ⚠️ The
    `exc_factory` params must use the REAL exception shapes this arm actually catches — the
    existing oracle's docblock warns that `is_geo_blocked` is SIGNATURE-based, so a synthetic
    message tests nothing. Read how `Mt5ClientError` is constructed in
    `analytics-service/tests/test_mt5_client_contract.py` and build the params from that.

    Prove it can fail: remove the guard line, run the case, observe RED, restore from a `cp` byte
    backup verified with `cmp` AND a sha256 match. ⛔ Never `git checkout -- <path>`,
    `git restore` or `git stash` — the stash stack is SHARED across worktrees. ⚠️ Re-take the byte
    backup after every edit; a stale backup makes `cmp` pass on a corrupt restore.

    ⚠️ Run pytest ONLY from `analytics-service/` via that directory's `.venv/bin/python`. A
    repo-root pytest misses the cassettes and makes LIVE broker calls.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && "$PWD/.venv/bin/python" -m pytest tests/test_allocator_positions.py -x -q > /tmp/167-02-t1.log 2>&1; RC=$?; cat /tmp/167-02-t1.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167-02-t1.log && echo OK</automated>
  </verify>
  <done>
    The `Mt5ClientError` arm consults the authority; the oracle case passes, was observed RED
    without the guard, and derives its expectation from `classify_exception` rather than a list.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The remaining unguarded arms — enumerated by grep, each with its own oracle case</name>
  <files>analytics-service/services/allocator_positions.py, analytics-service/tests/test_allocator_positions.py</files>
  <behavior>
    - Every arm that catches an exception and raises a retry-promising note re-raises a
      permanent-classified exception unwrapped.
    - Each newly guarded arm gets its OWN oracle case with its OWN real exception shape — ⛔ not one
      parametrized case standing in for all of them, because the arms catch different types and a
      shared case cannot fail for the right arm.
  </behavior>
  <action>
    ⛔ **ENUMERATE BY MEASUREMENT, NOT FROM THIS PLAN.** Run
    `grep -an '_must_reach_handler_unwrapped' analytics-service/services/allocator_positions.py`
    (measured at HEAD `66466242`: exactly TWO call sites, both ccxt) and
    `grep -an 'MT5_UNREACHABLE_NOTE\|MT5_MISSING_ACCOUNT_REF_NOTE\|SFOX_FETCH_FAILED_NOTE\|DERIVATIVE_FETCH_FAILED_NOTE\|SPOT_FETCH_FAILED_NOTE' analytics-service/services/allocator_positions.py`.
    The set to guard is: every site the second grep reports that the first grep does NOT already
    cover, MINUS the one Task 1 did.

    167-PATTERNS measured six unguarded arms (MT5 read timeout, MT5 abandoned-session fence, MT5
    account mismatch, MT5 client error, MT5 missing account ref, sFOX balances). Re-measure; if
    your count differs, the RUN is right and the document is stale.

    ⚠️ **ONE OF THOSE SITES IS A GUARD, NOT AN `except` — it has no exception in scope, so it has
    no disposition to consult and the analog's mechanism structurally does not apply to it.**
    Do NOT force the guard onto it and do NOT invent a classifier call for it. Instead: record it
    in the SUMMARY BY NAME, with the measured reason, and state plainly whether its note still
    promises a retry the condition cannot deliver. That residual is then a named, routed item —
    not a silent omission.

    For each arm that DOES catch an exception: insert the guard as the first statement, then add
    its own oracle case built from a real exception shape for that arm's type.

    ⭐ **A side effect worth stating in the SUMMARY because it is the point, not an accident:**
    once guarded, a permanent-classified MT5 or sFOX failure reaches
    `run_poll_allocator_positions_job`'s generic `except Exception` arm, where the AUM-02 write
    boundary derives `sync_error` from the STATUS via `sync_error_copy`, never from the exception.
    That arm's `error` copy deliberately promises nothing about retrying. So the guard does not
    just stop a false promise — it routes those failures onto copy that is already honest.

    Anti-vacuity per arm: remove that arm's guard, observe ITS case red (and confirm the other
    arms' cases stay green — that is what proves the cases are arm-specific), restore from a
    `cmp`-verified byte backup, re-taken after every edit.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && "$PWD/.venv/bin/python" -m pytest tests/test_allocator_positions.py tests/test_job_worker.py -q > /tmp/167-02-t2.log 2>&1; RC=$?; tail -40 /tmp/167-02-t2.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167-02-t2.log && echo OK</automated>
  </verify>
  <done>
    Every exception-catching arm in the copy family consults the authority; each has its own oracle
    case observed red under its own neuter; the guard-shaped site is named as a residual with its
    measured reason.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove the venue-agnostic family is closed, and the rate-limit promise survived</name>
  <files>analytics-service/tests/test_allocator_positions.py</files>
  <action>
    Add a ROSTER case that closes D-09 as a CLASS rather than as a list of point fixes — the repo's
    own fix-campaign discipline. It derives the set of retry-promising end-user notes from the
    module source and asserts that every arm raising one is preceded by the classifier guard, so a
    SEVENTH arm added later cannot ship unguarded.

    ⛔ Do NOT build that assertion with a bare `grep -c` over the file: this repo has a dated
    self-invalidation class where header prose and comments are counted as code. Strip comments
    before counting, and give the case an anti-vacuity control proving the extractor really found
    the arms (a non-zero count, asserted).

    Add a companion case pinning that `SYNC_ERROR_COPY_BY_STATUS["rate_limited"]` still promises a
    retry — ⛔ the ONE note in the family whose promise is legitimate. Without this pin, a future
    "clean sweep" of the family removes it and creates a new dishonesty.

    Anti-vacuity for both: neuter one arm's guard and observe the roster case RED by that arm's
    NAME; neuter the rate-limit template and observe the companion case red. Restore from
    `cmp`-verified byte backups.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && "$PWD/.venv/bin/python" -m pytest tests/test_allocator_positions.py -q > /tmp/167-02-t3.log 2>&1; RC=$?; cat /tmp/167-02-t3.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167-02-t3.log && echo OK</automated>
  </verify>
  <done>
    The roster case fails by name when any arm loses its guard; the rate-limit promise is pinned;
    both controls were observed red and restored.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| venue -> analytics-service | raw exception text arrives from MT5 / sFOX / ccxt |
| analytics-service -> `api_keys.sync_error` | the ONE column the browser renders verbatim (AUM-02) |
| analytics-service -> operator surfaces | `compute_jobs.last_error`, audit metadata, logs, Sentry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-05 | Information Disclosure | the newly unwrapped permanent exceptions | high | mitigate | Unwrapping routes those exceptions to `run_poll_allocator_positions_job`'s generic arm, whose AUM-02 boundary derives `sync_error` from the STATUS via `sync_error_copy` and never from the exception. Task 2 must CONFIRM by reading that arm that raw `str(exc)` reaches only the operator surfaces. The PROD defect this boundary replaces literally showed a Python attribute error to a user. |
| T-167-06 | Repudiation | `classify_exception` as sole authority | medium | mitigate | The guard CONSULTS the authority; ⛔ no allow-list is mirrored. A hand-copied mirror is the recorded cause of two permanent failures being downgraded to transient under a promise that could not be kept. |
| T-167-07 | Denial of Service | retry behaviour change | medium | accept | A permanent failure that previously retried under a false promise now stops retrying. That is the INTENDED behaviour change and it reduces load; the rate-limit arm's transient promise is explicitly preserved so genuine backoff is unaffected. |
| T-167-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No packages are installed by this plan.** If any task turns out to need one, STOP — 167-RESEARCH's Package Legitimacy Audit records "not applicable", so an install would require the audit to be run first. |
</threat_model>

<verification>
- `cd "$(git rev-parse --show-toplevel)/analytics-service" && "$PWD/.venv/bin/python" -m pytest -q` green (full Python suite).
- `cd "$(git rev-parse --show-toplevel)/analytics-service" && "$PWD/.venv/bin/python" -m mypy .` clean, if the repo's lint chain runs it.
- ⛔ Run NO database command. ⛔ Never start a local `uvicorn main:app` — it claims REAL prod compute jobs.
- ⛔ No `railway redeploy`, restart or `ssh` into the live MT5 gateway.
</verification>

<success_criteria>
- Every exception-catching arm in the retry-promising copy family consults
  `_must_reach_handler_unwrapped` before raising its note.
- A roster case fails BY NAME if a future arm ships unguarded.
- The rate-limit note is unchanged and pinned.
- The one guard-shaped site that cannot take the mechanism is named in the SUMMARY with its reason.
</success_criteria>

<output>
Create `.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-02-SUMMARY.md` when done
</output>
