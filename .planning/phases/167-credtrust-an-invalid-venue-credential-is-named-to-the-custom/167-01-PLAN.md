---
phase: 167-credtrust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/exchange.py
  - analytics-service/routers/exchange.py
  - analytics-service/docs/STATUS_CONTRACT.md
  - analytics-service/tests/test_validate_key_venue_transient.py
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.test.ts
  - src/lib/dialog-envelope.invariant.test.ts
  - src/lib/seam-venue-vocabulary.invariant.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
autonomous: true
requirements: [D-05, D-06, D-07, D-08, D-09]
estimate:
  tokens: 120000
  raw_tokens: 60000
  tasks: 3
  confidence: low   # DERIVED: zero calibration samples available in this checkout
must_haves:
  truths:
    - "A customer whose MT5 sign-in fails at the wizard validate step reads copy that names a credential as one possible reason and does NOT promise a retry (D-07)."
    - "No Retry control renders for that failure, and its absence is DERIVED from the action set rather than asserted (D-08)."
    - "The nine other NETWORK_UNAVAILABLE raise sites in routers/exchange.py keep their existing wire code, detail and status — measured 9 at HEAD 66466242 (D-09 scope discipline)."
    - "The minted code is reachable ONLY from a server-emitted wire code; no client-side inference path exists (UI-SPEC S2 offline cell)."
  artifacts:
    - "src/lib/wizardErrors.ts carries a KEY_SIGN_IN_FAILED union member, a WIZARD_ERROR_COPY entry, a VENUE_WIRE_CODE_TO_VERDICT row and a REQUIRES_MT5 FixRequirement"
    - "analytics-service/services/exchange.py carries SIGN_IN_FAILED_DETAIL"
    - "analytics-service/docs/STATUS_CONTRACT.md carries an S-27 row in section 7 and a raise-site entry in section 2.1"
  key_links:
    - "routers/exchange.py Mt5ClientError transient arm -> wire SIGN_IN_FAILED -> VENUE_WIRE_CODE_TO_VERDICT -> KEY_SIGN_IN_FAILED -> buildEnvelope -> ErrorEnvelope with showRetry false"
    - "both ReadonlySet wizard rosters admit KEY_SIGN_IN_FAILED so neither connect surface falls to the UNKNOWN terminal"
---

<objective>
Close the wizard half of the phase: the validate surface stops answering a genuine credential
rejection with "a network blip" plus a Retry button, and answers it with honest copy and no Retry.

Purpose: D-05 measured that the Retry is rendered by TypeScript, not by the Python detail string,
so a Python-only change is user-invisible. This plan does the full cross-language treatment
164.5.4 plan 02 proved out (167-PATTERNS Pattern Assignment 1).

Output: a new Python wire code emitted at exactly one narrowed arm, a new WizardErrorCode with the
copy specified verbatim in 167-UI-SPEC.md, every census pin the mint moves, and the
STATUS_CONTRACT.md rows no automated gate will ever remind anyone about.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
@DESIGN.md

@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-CONTEXT.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-UI-SPEC.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-PATTERNS.md
@.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-VALIDATION.md

@analytics-service/docs/STATUS_CONTRACT.md
</context>

<repo_guardrails>
⛔ **THREE WORKING-TREE FILES ARE MODIFIED ON PURPOSE AND BELONG TO OTHER PHASES:**
`.planning/milestone.lock`, `.planning/state.json`, and
`.planning/phases/164.5.3-*/164.5.3-VERIFICATION.md`.
⛔ NEVER stage, commit, move, revert or `git checkout` any of them, and do not report them as a
dirty tree. ⛔ **Never `git add -A` / `git add .`** — stage files by explicit path.

⛔ **Never write a CI skip trailer in a commit message or a PR body, not even to deny it.** GitHub
honours those tokens anywhere in the message and they survive a squash into `main`. Name the
concept in prose.
⛔ **No credential, connection string, project ref, key, absolute home path, local username, venue
API key, MT5 account number, broker server name, real key identifier or real strategy name in any
committed artifact.** Placeholders only; truncate UUIDs to 8 characters. Error codes, timestamps,
durations and day-counts are safe; identifiers are not.
⛔ **A proof-of-absence can publish what it proves absent** — record VERDICTS and COUNTS, never
specimens and never the pattern searched for.
⛔ **Restore only from a `cp` byte backup verified with `cmp`, re-taken after every edit.** Never
`git checkout -- <path>`, `git restore` or `git stash` — the stash stack is SHARED across worktrees
and `git checkout --` silently destroys uncommitted work in a neuter/restore harness.
⛔ **Run NO database command against any remote** — this checkout's Supabase CLI is linked to
PRODUCTION. ⛔ Never start a local `uvicorn main:app`. ⛔ No `railway redeploy`, restart or `ssh`
into the live MT5 gateway. ⛔ The founder enters credentials; no agent enters, reads, decrypts, logs
or echoes one, ever.
⛔ **Nothing lowers a floor, raises a ceiling, relaxes a timeout or adds a waiver.** ⛔ Never clear
a red by widening an allowlist.
⛔ **Cite by SYMBOL, never by line number** — line numbers in this repo rot within a commit.
⛔ **No Grok. No Codex.**
⚠️ `grep` is SILENTLY BLIND to NUL-bearing files — always `grep -a`. ⚠️ `grep -c` EXITS 1 on a zero
count. ⚠️ `$?` after a PIPELINE measures the LAST command. ⚠️ A leading-dash pattern is eaten as a
flag — use `-e` or python.
⚠️ Run `npm run check:planning-hygiene` after any `.planning/` write; ⛔ never read its result with
`tail -1` (the count line and the OK line are different lines).
</repo_guardrails>

<naming_contract>
⭐ **This plan MINTS the phase's vocabulary. Plans 03 and 04 consume it byte-for-byte.**

| Layer | Identifier | Why this one |
|---|---|---|
| Python wire code | `SIGN_IN_FAILED` | Names the OBSERVATION ("the sign-in did not succeed"), asserts no cause. |
| Python detail constant | `SIGN_IN_FAILED_DETAIL` | Sibling of `AUTH_FAILED_DETAIL` / `NETWORK_ERROR_DETAIL` in `services/exchange.py`. |
| TypeScript member | `KEY_SIGN_IN_FAILED` | 167-UI-SPEC Open Question 1 recommends it; this plan ACCEPTS the recommendation. |
| `FixRequirement` | `REQUIRES_MT5` | Sibling of `REQUIRES_DERIBIT`, the only `venueIs` constant in the repo. |
| Wire status | `424` | The arm's existing status. `VENUE_WIRE_CODE_TO_VERDICT`'s stated discipline is that the wire answer stays the one the service chose. |

⛔ **The id is a stable wire contract and a PostHog `wizard_error { code }` key.** It is accepted
here on the record so nothing downstream re-opens it: the three near-miss members are all ruled out
by 167-CONTEXT D-07 on CAUSE grounds, `KEY_SIGN_IN_FAILED` claims only what was observed, and
naming it the same across the wire, the union and (plan 03) the `sync_status` value gives the phase
ONE vocabulary a reader can follow end to end.
</naming_contract>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a failed MT5 sign-in renders honest copy with no Retry" — one path only</name>
  <files>analytics-service/services/exchange.py, analytics-service/routers/exchange.py, src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts</files>
  <read_first>
    167-UI-SPEC.md § Copywriting Contract §2 — the `title`, `cause`, four `fix[]` bullets,
    `fixRequires[]`, `docsHref` and `actions` are SPECIFIED VERBATIM there and are used verbatim
    (D-07 / 167-PATTERNS Pitfall 3: copy the SHAPE of `KEY_MUST_BE_RECONNECTED`, not one word of
    its text).
    167-PATTERNS Pattern Assignment 1 (b)(c) — the copy-entry and routing-row shape, and the
    "⭐ WHY THE ROW IS NOT OPTIONAL BESIDE THE MINT" paragraph.
    167-PATTERNS Pattern Assignment 2 — `REQUIRES_DERIBIT`'s docblock, the template for `REQUIRES_MT5`.
    167-PATTERNS Pattern Assignment 4 — the `except Mt5ClientError` arm in `validate_key`, verbatim.
    167-PATTERNS Pattern Assignment 5 — `AUTH_FAILED_DETAIL`'s docblock and the substring-collision rule.
    `src/lib/envelope.ts` — `RECOVERABLE_ACTIONS` and `buildEnvelope`'s `recoverable` derivation.
  </read_first>
  <behavior>
    - Given the wire body a `VenueTransientHTTPException` with code `SIGN_IN_FAILED` produces,
      `classifyKeyValidationError` returns `{ code: "KEY_SIGN_IN_FAILED", status: 424 }`.
    - `buildEnvelope` over that code derives `recoverable: false`, so `ErrorEnvelope`'s `showRetry`
      is false. The assertion runs THROUGH `buildEnvelope`, never by reading `actions` back.
    - Non-vacuity control: the same human sentence carrying NO wire code still lands on the
      `UNKNOWN` terminal, proving it was the routing ROW that moved the verdict and not a reworded
      substring cascade.
    - Venue gating: with `venue: "mt5"` the entry renders four `fix` bullets; with any other venue,
      and with no venue named, it renders three. No bullet depends on a sibling being present.
  </behavior>
  <action>
    Wire ONE path from the Python emitter to the rendered envelope. No other call sites.

    (a) In `analytics-service/services/exchange.py`, add `SIGN_IN_FAILED_DETAIL` beside
    `AUTH_FAILED_DETAIL` / `NETWORK_ERROR_DETAIL`, with a docblock stating the cross-language
    contract in `AUTH_FAILED_DETAIL`'s own voice: the string is the classifier's live input, a
    reword changes behaviour, and it is the single source of truth for this failure's detail.
    ⛔ Before the string lands, run the substring-collision sweep 167-PATTERNS Pattern Assignment 5
    names: read `classifyKeyValidationError`'s substring cascade in `src/lib/wizardErrors.ts` and
    confirm the new detail's lowercase form contains NONE of the needles the cascade matches on
    (the MT5 detail block in `services/closed_sets.py` enumerates them). Record the verdict and the
    needle COUNT checked in the task commit message — never the needle list itself.
    ⛔ Do NOT add the new code to `PERMANENT_VALIDATION_ERROR_CODES`: that set means the venue
    asserted a permanent rejection, which is exactly the claim this arm refuses to make. Say so in
    a comment at the set's definition site so the next reader does not "fix" the omission.

    (b) In `analytics-service/routers/exchange.py`, narrow ONLY the `except Mt5ClientError` arm's
    TRANSIENT tail inside `_validate_mt5_key` — the arm that already calls
    `classify_mt5_login_error` and falls through past its `auth` and `wrong_server` branches. Raise
    `VenueTransientHTTPException` with `status_code=424`, `code="SIGN_IN_FAILED"`,
    `detail=SIGN_IN_FAILED_DETAIL`, `recoverable=False`. Keep `trace.outcome = "transient"`
    unchanged — the A-04 classification is LOCKED and is not reverted (D-05).
    ⛔ ADD AN ARM; DO NOT REWORD THE SHARED CONSTANT. MEASURED at HEAD `66466242`:
    `code="NETWORK_UNAVAILABLE"` occurs 9 times in this file (⚠️ 167-PATTERNS says ten — re-measure
    with a script, not by eye, and record what you get). The other eight raise sites — the MT5
    stage timeout, the `Mt5SessionAbandoned` fence, the `Mt5AccountMismatchError` concurrency arm,
    and the sFOX/ccxt/portfolio arms — stay byte-unchanged. Why the `Mt5ClientError` arm and only
    it: it is the arm where a LOGIN was attempted and did not succeed, which is what the copy
    claims. The other three MT5 arms are transport, lease and concurrency faults where no sign-in
    is implicated, and telling their user their credentials may have changed would be the same
    false blame this phase removes.
    ⚠️ `recoverable=False` on the wire is deliberate and DIVERGES from the sibling MT5 arms' hardcoded
    `recoverable=True`. 167-PATTERNS Pattern Assignment 5 names the hazard that the wire flag and
    the TypeScript-derived Retry can disagree invisibly; here they are made to AGREE. Record that
    reason at the raise site.

    (c) In `src/lib/wizardErrors.ts`, mint the member exactly as 167-PATTERNS Pattern Assignment 1
    (a) shows: a union member docblock naming WHERE IT COMES FROM, then one "⛔ NOT <member>"
    paragraph each for `KEY_MUST_BE_RECONNECTED`, `KEY_AUTH_FAILED` and `KEY_NETWORK_TIMEOUT`
    (the arguments are already written in 167-CONTEXT D-07 and 167-UI-SPEC §2's constraint table —
    reuse them), then the non-recoverability derivation paragraph. Add the `WIZARD_ERROR_COPY`
    entry with the UI-SPEC copy verbatim and `actions: ["request_call", "expand_log"]`. Add
    `REQUIRES_MT5` beside `REQUIRES_DERIBIT`, docblocked on the UI-SPEC §3 move-2 argument that an
    absent venue SUPPRESSES the bullet and the unconditional bullet still leaves a complete remedy.
    Reuse the existing `NOT_ON_PRESELECT_SURFACE` for the third slot — ⛔ do not mint a second.
    `fixRequires` is index-aligned: four bullets, four slots.
    Add the `VENUE_WIRE_CODE_TO_VERDICT` row keyed on `SIGN_IN_FAILED` with `status: 424`.
    ⛔ `fix` stays `string[]` — `buildEnvelope` forwards it verbatim as `debug_context`.

    (d) Add the behavioural cases in `src/lib/wizardErrors.test.ts` covering the four behaviours
    above. Prove each can fail by the repo's neuter drill: `cp` a byte backup, verify the restore
    with `cmp` AND a sha256 match, RE-TAKE the backup after every edit.
    ⛔ Never `git checkout -- <path>`, `git restore` or `git stash`. The three neuters that map
    one-to-one from the analog: add a `RECOVERABLE_ACTIONS` member to the new entry and observe
    `recoverable` flip true; delete the `VENUE_WIRE_CODE_TO_VERDICT` row and observe the UNKNOWN
    terminal; remove the wire code at its Python emission site and observe the Python case red.

    ⛔ The two `EXPECTED_TABLE_SIZE` pins will red in this task's run. Do NOT move them here —
    Task 2 owns every census pin, with its own re-run argument per pin. Leaving them red at the end
    of this task is EXPECTED and is what proves the gates bite.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.test.ts src/lib/envelope.test.ts --reporter=dot -t "KEY_SIGN_IN_FAILED" > /tmp/167-01-t1.log 2>&1; RC=$?; cat /tmp/167-01-t1.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167-01-t1.log && echo OK</automated>
  </verify>
  <done>
    The new cases pass in isolation; each was observed RED under its own neuter and GREEN after a
    `cmp`-verified restore; the census pins are knowingly left red for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move every census pin the mint reddened — found by RUNNING, not by reading this plan</name>
  <files>src/lib/wizardErrors.test.ts, src/lib/dialog-envelope.invariant.test.ts, src/lib/seam-venue-vocabulary.invariant.test.ts, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx</files>
  <read_first>
    167-PATTERNS "⭐ THE PINS THE PLAN NAMED vs. THE PINS RUNNING THE TESTS FOUND" and
    "⛔ THE ONE PLACE 167 IS BIGGER THAN ITS ANALOG".
  </read_first>
  <action>
    ⛔ **THE ENUMERATION BELOW IS A STARTING POINT, NOT A COMPLETE LIST, AND TREATING IT AS COMPLETE
    IS THE ANALOG'S OWN NAMED FAILURE.** 164.5.4 plan 02's summary: "the two pins the plan named
    would have gone green while three other gates stayed red. Clearing the named subset would have
    read as a complete fix." Run all six gate files first, read EVERY failure, and fix what the
    RUN reports — including anything not named here.

    Run: `npx vitest run src/lib/wizardErrors.test.ts src/lib/wizardErrors.invariant.test.ts
    src/lib/wizardErrors.roster-render.test.tsx src/lib/dialog-envelope.invariant.test.ts
    src/lib/seam-venue-vocabulary.invariant.test.ts src/lib/envelope.test.ts --reporter=dot`

    Pins measured at HEAD `66466242` and expected to move:
    - Two `EXPECTED_TABLE_SIZE` declarations in `src/lib/wizardErrors.test.ts`, both currently 94.
      ⛔ GREP them (`grep -an 'const EXPECTED_TABLE_SIZE' src/lib/wizardErrors.test.ts`), never
      count the table. ⭐ READ THE NEW INTEGER OFF THE GUARD'S OWN FAILURE MESSAGE — an expectation
      built by reading the subject is an oracle that cannot fail. The two pins front DIFFERENT
      scans (a destructive-action scan and a banned-claims honesty scan); each gets its OWN re-run
      argument written out before its integer moves. ⛔ A blanket increment across this corpus has
      a dated record in this repo of killing deliberate mismatch calibrations.
    - The roster non-vacuity pin `expect(checked).toBe(32)` in
      `src/lib/dialog-envelope.invariant.test.ts`. ⚠️ That file has TWO `expect(checked).toBe(`
      hits; the other one is 5 and is a different roster — grep, then move only the one the RUN
      names.
    - `EXPECTED_EMITTED_CODES` in `src/lib/seam-venue-vocabulary.invariant.test.ts`. Measured 41
      members by script at HEAD. ⭐ **THIS IS WHERE 167 IS BIGGER THAN ITS ANALOG**: 164.5.4 routed
      an EXISTING wire code and never touched this file; this phase MINTS a Python code, so the
      set comparison reds BY NAME. Add `SIGN_IN_FAILED`.
    - `DERIVED_FLOOR` in the same file, currently 24. ⚠️ **NOTHING WILL TELL YOU IF YOU FORGET.**
      The assertion is `toBeGreaterThanOrEqual`, so 24 still passes at 42 members. The file's own
      stated rule is `floor(0.6 × N)`; 0.6 × 42 floors to 25. Move it to 25 in this same commit and
      state in the commit message that no gate enforces the rule and that the file's lineage prose
      already drifted (it says 40 while the roster holds 41 — correct the prose too).
    - `KNOWN_CREATE_WITH_KEY_CODES` in `ConnectKeyStep.tsx` and `KNOWN_ADD_KEY_CODES` in
      `MultiKeyConnectStep.tsx`. Both reddened BY NAME in the analog via the
      `[153.7 review W-153.7-1]` gate. Each new line carries the analog's anti-deletion comment:
      whether this route's own seam can raise that wire code is NOT what the roster turns on.
      ⛔ `SyncPreviewStep.tsx`'s `KNOWN_KICKOFF_CODES` is a differently-typed, wire-code-keyed
      Record governing post-connect job kickoff. It is in scope ONLY if `SIGN_IN_FAILED` can be
      emitted at kickoff time. ANSWER THAT BY GREPPING the new code's emission sites under
      `analytics-service/routers/` — Task 1 creates exactly one, inside `validate_key` — and record
      the measured answer in the SUMMARY either way. Do not assume in either direction.

    ⚠️ Three floors will NOT red and must NOT be moved blindly, measured at HEAD:
    `EXPECTED_POPULATION_MIN = 78` (`wizardErrors.roster-render.test.tsx`) and
    `DERIVED_FLOOR = 36` (`wizardErrors.invariant.test.ts`) are both
    `toBeGreaterThanOrEqual` and stay put unless their own stated rule says otherwise. Exact-value
    pins that do NOT move for this phase: `EXPECTED_DIALOG_COUNT`, `EXPECTED_FINALIZE_REJECTION_SITES`,
    `EXPECTED_FORMAT_EMITTERS_PER_ROUTE`.

    ⚠️ `wizardErrors.test.ts` carries a THIRD, self-referential assertion that reads both
    `EXPECTED_TABLE_SIZE` declarations back out of the source with a regex and asserts exactly two
    that AGREE. Moving one and not the other reds there, not at the pin.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ --reporter=dot > /tmp/167-01-t2.log 2>&1; RC=$?; tail -40 /tmp/167-01-t2.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167-01-t2.log && echo OK</automated>
  </verify>
  <done>
    The whole of `src/lib/` is green, every moved pin carries its own re-run argument in the commit
    message, and the SUMMARY records the measured answer to the `KNOWN_KICKOFF_CODES` question and
    the total number of pins the RUN reported versus the number this plan named.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The STATUS_CONTRACT rows no gate will ever ask for, and the 3b wire-body test</name>
  <files>analytics-service/docs/STATUS_CONTRACT.md, analytics-service/tests/test_validate_key_venue_transient.py</files>
  <read_first>
    `analytics-service/docs/STATUS_CONTRACT.md` section 8 "How to add a new error site" (the
    authoritative procedure) and its section 7 rows `S-25` / `S-26` (the row template), plus
    section 2.1 "The two FLAT exceptions".
    `analytics-service/tests/test_validate_key_venue_transient.py` — the existing harness for this
    exact arm.
  </read_first>
  <behavior>
    - `validate_key` against an MT5 credential whose `Mt5ClientError` classifies transient answers
      HTTP 424 with wire `code` `SIGN_IN_FAILED` and `SIGN_IN_FAILED_DETAIL` as `detail`.
    - The wire body's key set is exactly the three-member FLAT set section 8 mandates. That
      assertion is what catches a fall-through to the default exception handler.
    - Every literal in the test is written out; ⛔ nothing is imported from `error_contract`.
    - The sibling arms still answer with their existing code: at least one case pins the MT5 stage
      timeout arm unchanged, so the narrowing is proven NARROW.
  </behavior>
  <action>
    ⚠️ **NO AUTOMATED GATE READS `STATUS_CONTRACT.md`** — measured: every reference to it under
    `src/` and `analytics-service/` is prose or a comment, none reads the file. It is missed
    SILENTLY, which is why it is a task and not a bullet.

    Add the section 7 row using `S-25` / `S-26` as the template — ⛔ do not assume the next id is
    `S-27`; grep the section 7 rows and take the successor of the highest id actually present.
    Then add the raise-site entry to section 2.1, which section 8 step 5 requires for a 3b FLAT
    site specifically.

    Write the cases in the existing `test_validate_key_venue_transient.py` harness rather than a
    new file. Follow section 8 step 4: literal expected values, and the exact-key-set assertion on
    the wire body.

    Prove the cases can fail: neuter the emitter's `code=` back to the shared constant, observe the
    new cases RED and the sibling-arm case still GREEN, then restore from a `cmp`-verified byte
    backup. ⛔ Never `git checkout --`, `git restore` or `git stash`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && "$PWD/.venv/bin/python" -m pytest tests/test_validate_key_venue_transient.py tests/test_envelope_recoverable.py -x -q > /tmp/167-01-t3.log 2>&1; RC=$?; cat /tmp/167-01-t3.log; test "$RC" = 0 && ! grep -qE '\b0 (passed|tests)\b' /tmp/167-01-t3.log && echo OK</automated>
  </verify>
  <done>
    Both STATUS_CONTRACT sections carry the new site; the Python cases pass, were observed red under
    neuter, and the sibling-arm case proves the narrowing did not widen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| venue (MT5 terminal) -> analytics-service | untrusted remote text arrives as `Mt5ClientError`; it is NEVER interpolated into a user-facing string |
| analytics-service -> browser | the wire body (`detail`, `code`, `recoverable`) crosses into rendered copy |
| repo -> public | this repo is PUBLIC and `.planning/` is TRACKED |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-01 | Information Disclosure | `routers/exchange.py` new raise site | high | mitigate | The arm logs the scrubbed `e.code` ONLY, exactly as the existing sibling arms do. ⛔ Never log or interpolate the remote text, the login, the password or the server value. `detail` is a fixed authored constant with no slot, so no venue-supplied text can travel through it. |
| T-167-02 | Information Disclosure | `SIGN_IN_FAILED_DETAIL` / the copy entry | medium | mitigate | Copy is authored, static and interpolates nothing. Task 1's substring-collision sweep records a VERDICT and a COUNT, never the needles searched for — a proof-of-absence must not publish what it proves absent. |
| T-167-03 | Tampering | the substring cascade in `classifyKeyValidationError` | medium | mitigate | The new detail is swept against the live cascade before landing. A collision would be LATENT because the `VENUE_WIRE_CODE_TO_VERDICT` row resolves before the cascade, so it must be caught by measurement now, not by behaviour later. |
| T-167-04 | Spoofing | client-side inference of the new code | medium | mitigate | The code is reachable ONLY from a server-emitted wire code. A browser-side fetch failure keeps routing to the existing network path (UI-SPEC S2 *offline* cell), so a customer's own Wi-Fi drop cannot print a sentence about their credentials. |
| T-167-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No packages are installed by this plan.** 167-RESEARCH's Package Legitimacy Audit records "not applicable — nothing to check". If any task turns out to need an install, STOP: the audit table must exist before the install runs. |
</threat_model>

<verification>
- `npx vitest run src/lib/ src/app/api/keys/ --reporter=dot` green (the analog's own reported scope).
- `npm run typecheck` clean.
- `cd "$(git rev-parse --show-toplevel)/analytics-service" && "$PWD/.venv/bin/python" -m pytest tests/test_validate_key_venue_transient.py tests/test_envelope_recoverable.py tests/test_mt5_validate.py -q` green.
- ⛔ Run NO database command. ⛔ Never start a local `uvicorn`. ⛔ No live MT5 operation.
</verification>

<success_criteria>
- A `SIGN_IN_FAILED` wire body resolves to `KEY_SIGN_IN_FAILED` and `buildEnvelope` derives
  `recoverable: false`, proven through `buildEnvelope` rather than by reading `actions`.
- The nine (re-measure) other `NETWORK_UNAVAILABLE` raise sites are byte-unchanged.
- Every census pin the RUN reported is moved, each with its own re-run argument.
- The SUMMARY states how many pins the RUN found versus how many this plan named.
</success_criteria>

<output>
Create `.planning/phases/167-credtrust-an-invalid-venue-credential-is-named-to-the-custom/167-01-SUMMARY.md` when done
</output>
