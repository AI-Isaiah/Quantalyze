---
status: complete
phase: 167-credtrust
source: 167-01-SUMMARY.md, 167-02-SUMMARY.md, 167-03-SUMMARY.md, 167-04-SUMMARY.md, 167-05-SUMMARY.md, 167-06-SUMMARY.md, 167-VERIFICATION.md
started: 2026-09-23T00:48:26Z
updated: 2026-09-23T09:00:00Z
---

## Current Test

[testing complete]

## Tests

Every behaviour an agent can exercise before merge is already verified in `167-VERIFICATION.md` (38/40 must-haves, each backed by a passing behavioural test at `verified_at_sha`). The four items below are the verifier's human items. Each is POST-MERGE or FOUNDER-ONLY BY CONSTRUCTION, and each is recorded as a skip with its reason, owner and trigger — ⛔ none is marked passed, and none was attempted by an agent.

### 1. Migration `20260922120000` applies to TEST, then PROD
expected: `supabase-migrate.yml`'s `apply-test` succeeds on merge, then `apply` succeeds after the founder approves the `Production` reviewer gate; `api_keys_sync_status_check` admits `sign_in_failed` on both.
result: pass
closed: |
  2026-09-23, SHA-bound. PR #841 merged as `2091fea6`; Supabase Migrate run `35819065230` on that
  SHA concluded success with `apply-test`, `apply-test-verdict`, `prod-credential-divergence-verdict`
  and `apply` all success (the founder approved the Production gate). The founder's schema dump
  taken afterwards differs from the prior capture by exactly the widened
  `api_keys_sync_status_check`, now admitting `sign_in_failed` (committed on the 164.4.2 branch).
reason: |
  NOT RUNNABLE BEFORE MERGE, BY CONSTRUCTION: `supabase/migrations/**` applies only on merge,
  TEST first, PROD behind the human `Production` reviewer gate. The migration passed two full
  rounds of migration-reviewer + rls-policy-auditor + silent-failure-hunter and the pg-lane gate
  (RED-UNDER-M arms), and the founder approved it on 2026-09-22.
  Owner: this session's `/land-and-deploy` (watches both jobs, SHA-bound) + THE FOUNDER (clicks
  the Production gate). Trigger: the 167 PR merging. Booked: TODOS `[167-POSTMERGE-CHECKS]`.
  ⚠️ Until PROD applies it, the worker's `sign_in_failed` write is refused and falls back to
  `error` with the sign-in copy (SFH-H2 fallback, pinned by test) — no key reads healthy.

### 2. The two measured keys read their new status after the first daily poll
expected: the MT5 key measured in the ROADMAP reads `sync_status = 'sign_in_failed'`; the venue key with the expired credential reads `revoked`; both owners see the amber pill + authored helper.
result: skipped
reason: |
  NOT RUNNABLE BEFORE MERGE, and a PROD read is FOUNDER-ONLY here: this checkout's Supabase CLI is
  linked to PRODUCTION, so no agent runs a database command against any remote. Code-level truth
  is verified (the pinned ccxt maps that venue's expired-key code to `AuthenticationError` →
  `revoked`; the MT5 login-stage refusal → `sign_in_failed`, D-17).
  Owner: THE FOUNDER. Trigger: the first daily holdings poll after the PROD apply. Booked: TODOS
  `[167-POSTMERGE-CHECKS]`.

### 3. The live MT5 gateway's real code for a wrong investor password (RESEARCH A1)
expected: the code the live gateway returns is recorded, and D-17's refusal set is confirmed or amended against it.
result: skipped
reason: |
  FOUNDER-ONLY BY CONSTRUCTION: a live MT5 op against the shared terminal, and entering a
  credential, are founder acts; no agent restarts, redeploys or logs into the gateway. D-17 records
  the decision taken without the measurement and its accepted cost.
  Owner: THE FOUNDER. Trigger: none required for ship — a measurement that informs D-17. Booked:
  TODOS `[167-POSTMERGE-CHECKS]`.

### 4. Visual check at 320px width and 200% zoom
expected: the owner pill + helper (`AllocatorSyncStatus`) and the manager key card (`ApiKeyManager`) wrap cleanly with no overflow or clipped text; the wizard's error envelope for `KEY_SIGN_IN_FAILED` does not overflow.
result: skipped
reason: |
  NOT RUNNABLE BY AN AGENT HERE: rendering the real surfaces needs a running app against real data,
  and a local server in this checkout would read PRODUCTION. Structural a11y (one live region per
  card, authored copy, the ≤60-char helper budget) is pinned by tests.
  Owner: THE FOUNDER (or a UI review once deployed). Trigger: the 167 deploy reaching production.
  Booked: TODOS `[167-POSTMERGE-CHECKS]`.

## Summary

total: 4
passed: 1
issues: 0
pending: 0
skipped: 3
blocked: 0

## Gaps

None. No test reported an issue; all four skips carry a reason, an owner and a trigger.
