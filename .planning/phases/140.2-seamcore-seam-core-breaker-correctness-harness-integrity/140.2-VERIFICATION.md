---
phase: 140.2-seamcore-seam-core-breaker-correctness-harness-integrity
verified: 2026-07-27T05:23:02Z
status: passed
score: 7/7 success criteria · 11/11 requirements
overrides_applied: 0
verifier_evidence:
  mutations_run_by_verifier: 7
  suites_run_by_verifier:
    - "npx vitest run (full): 9303 passed | 287 skipped (724 files)"
    - "npx tsc --noEmit: exit 0"
    - "npm run lint: 0 errors, 1 pre-existing warning (EquityChart.tsx, not a phase file)"
    - "npm run test:coverage: 84.57 / 78.52 / 81.78 / 86.68 (stmts/branches/funcs/lines) — exit 0"
    - "npm run test:redis (docker, digest-pinned): 7/7"
warnings:
  - id: W-1
    severity: warning
    concern: >-
      ROADMAP SC4's evidence column calls the `nx` residual "a redundant second layer".
      The phase's OWN 140.2-VALIDATION.md §8 calls the same thing "a real behavioural
      difference and a real open falsifier gap". The ledger is right and the ROADMAP is
      soft: `nx: true` guards a case the wave-7 early return structurally cannot reach —
      two Fluid Compute instances that both read `null` concurrently. Not redundant.
    location: ".planning/ROADMAP.md SC4 row vs 140.2-VALIDATION.md §8"
    action: "Align the ROADMAP wording to the ledger's. Documentation defect, not a code gap."
  - id: W-2
    severity: warning
    concern: >-
      A THIRD unfalsified property of `nx` is named in neither document. `written` gates
      `emitBreakerTransition("open", ...)`; with `nx: false` both racing instances receive
      "OK" and BOTH emit an open event, double-counting the transition across the fleet —
      which the code's own comment says must not happen. Also unfalsified by any test.
    location: "src/lib/resilient-fetch.ts:1135-1160"
    action: "Add to the Phase 141 handoff alongside the concurrency residual."
  - id: W-3
    severity: warning
    human_decision: true
    concern: >-
      Independently confirmed by the verifier: `gh api repos/:owner/:repo/rulesets` returns
      `[]` AND `gh api repos/:owner/:repo/branches/main/protection` returns 404 "Branch not
      protected". `frontend-seam-redis` is correctly wired STRICT in both `needs:` and the
      aggregator result loop, but NOTHING enforces the aggregator at merge. Every CI gate
      in this repo — the coverage ratchet, the full suite, and the real-Redis lane this
      phase built — is advisory in practice.
    location: ".github/workflows/ci.yml:359,782,796 (correct) · GitHub repo settings (absent)"
    action: "Founder/repo-admin action. Outside this phase's control; the phase recorded it honestly and did not act on it."
  - id: W-4
    severity: info
    concern: >-
      The verification scope named base `a12b9eaa` as "the 140.1.2 close". It is not —
      it sits SIX commits before the last 140.1.2 commit `b70ad840`, so `a12b9eaa..HEAD`
      contains 65 commits and 6 Python files. At the CORRECT base the ROADMAP's claims are
      exact: `b70ad840..48e6e3e2` = 55 commits, ZERO `.py`. No scope violation occurred.
    location: "phase scope statement"
    action: "Correct the recorded base so a future reader does not read a fence breach into it."
  - id: W-5
    severity: info
    concern: >-
      Second disclosed falsifier gap: M25's `bare-scalar-429` TOLERANCE clause cannot fail
      under its own mutation. Disclosed in VALIDATION.md §8, the row was re-expressed rather
      than left standing, and the property was handed to Phase 141. Disclosure verified
      present; the verifier did not independently re-run M25.
    location: "140.2-VALIDATION.md row M25 + §8"
    action: "Already handed forward. No action for this phase."
deferred:
  - truth: "`nx: true` trip idempotency is itself falsified by a test"
    addressed_in: "Phase 141"
    evidence: "VALIDATION.md §8 — 'Handed to Phase 141, which owns the breaker's timing behaviour next.' Requirement table records the same residual against SEAMCORE-09."
  - truth: "The bare-scalar-429 tolerance property has a falsifier"
    addressed_in: "Phase 141"
    evidence: "VALIDATION.md §8 'The second falsifier gap, from M25' — 'Also handed forward.'"
---

# Phase 140.2: SEAMCORE — Seam core & breaker correctness + harness integrity — Verification Report

**Phase Goal:** The breaker counts the failures it exists for, ignores the ones it must not, and every constant governing it is falsifiable
**Verified:** 2026-07-27T05:23:02Z
**Status:** passed (7/7 SC · 11/11 requirements) — with 3 warnings, none blocking
**Re-verification:** No — initial verification
**Tree verified at:** `48e6e3e2` (working tree confirmed clean before and after every mutation)

---

## Verdict

**The phase goal is achieved.** This phase existed because Phase 140 certified "5/5 success criteria
mutation-tested" while ten simultaneous semantic mutations produced a byte-identical pass. I did not
take the 55-RED table on trust: I ran **seven mutations myself**, including a counterfactual that
isolates the wave-2 fix as the load-bearing cause. The harness can now fail.

The single most important result: **I reproduced the exact failure Phase 140 could not see, and then
I reproduced its absence.** With the wave-2 cut in place, `BREAKER_FAILURE_THRESHOLD 5 → 30` reddens a
BEHAVIOURAL breaker test. When I re-added the one line the phase deleted (`opts.limiter.tokens`), that
same test went GREEN under the same mutation. That is the defect and the fix, measured in both
directions, by me.

---

## Mutations run by the verifier

Each applied to a pristine tree, run, then restored from a `/tmp` copy. `git status` verified clean
between rows. No `git checkout` / `stash` / `clean` was used (the `.planning` ledgers are gitignored).

| # | Row | Mutation | Command | Result | Verdict |
|---|-----|----------|---------|--------|---------|
| 1 | **M14b** | `BREAKER_FAILURE_THRESHOLD` `5 → 30` (`resilient-fetch.ts:146`) | `vitest run seam-constants.pin.test.ts resilient-fetch.test.ts` | **8 failed / 145 passed** — incl. `trips the breaker once the failure allowance is exhausted`, `cross-context … short-circuits`, `trips on HTTP 5xx`, `body-read failures arm the breaker` | ✓ RED — pin **and** behavioural, as claimed |
| 2 | **M14b counterfactual** | same mutation + restore `fakeRatelimitFor(store, opts.limiter.tokens)` at `resilient-fetch.test.ts:211` | `vitest run resilient-fetch.test.ts` | failing set **loses** `trips the breaker once the failure allowance is exhausted` and `trips on HTTP 5xx` | ✓ The wave-2 cut is the cause. Harvest restored ⇒ oracle goes blind again |
| 3 | **M28** | claim payload made constant across users (`analytics-client.ts:271`) | `vitest run analytics-client.test.ts` | **16 failed / 54 passed** — incl. `two different tenants land in two DIFFERENT per-tenant buckets` | ✓ RED — matches ledger's "M28 (16 RED)" exactly |
| 4 | **M35** | `breakerKeysFor` → `[BREAKER_KEY]` only (option C, `resilient-fetch.ts:779`) | `vitest run resilient-fetch.test.ts resilient-fetch.wiring.test.ts` | **3 failed** — both OB-8 negative controls | ✓ RED — matches ledger's "M35 (3 RED)" |
| 5 | **M19** | `nx: true → nx: false` (`resilient-fetch.ts:1135`) | `npm run test:redis` (real Redis) **and** `vitest run src/lib src/app/api/keys src/app/api/admin` | redis **7/7 PASS**; unit **3271 passed / 0 failed** | ⚠️ **GREEN — the phase's disclosure is accurate.** Confirmed unfalsified in BOTH lanes |
| 6 | **M19R** | delete `if (existing.expiresAtMs > now) return;` (`resilient-fetch.ts:1107`) | `npm run test:redis` | **1 failed / 6 passed** — `R-3 trip idempotency`, `expected 'open:…105185' to be 'open:…101170'`, plus the anti-vacuity fence firing (`expected 6 to be 7`) | ✓ RED — the replacement row has real teeth against the deployed Lua |
| 7 | **M22b** | add a 14th binding carrying an already-existing budget key (`analytics-client.ts`) | `vitest run resilient-fetch.wiring.test.ts` | **1 failed / 21 passed** — `every discovered binding is classified in the roster (a 14th binding FAILS)` | ✓ RED on **exactly one** assertion. The class is closed by a MECHANISM, not a census |

---

## Observable truths — the seven success criteria

| # | Criterion | Status | Evidence gathered by the verifier |
|---|-----------|--------|-----------------------------------|
| **SC1** | Stalling upstream (headers fast, body slow) **records a failure**; deadline surfaces as a typed seam error | ✓ VERIFIED | `resilient-fetch.ts:1287-1375` — `instrumentBody` wraps `json()`/`text()`; the catch arm calls `seamBreakerVerdict(null)` → `recordOnce(...)` **awaited inline** (not an orphanable IIFE), then throws `SeamBodyReadError`. `SyntaxError` correctly rethrown raw (a parse fault is not a transport fault). Full suite green. |
| **SC2** | Caller fault and exchange-attributable error record **zero**; genuine service fault records one, incl. `text/plain` | ✓ VERIFIED | `seam-discriminator.ts` (321 lines) decides on **STATUS BEFORE any body read** — 400/401/403/404/422/429 → caller, 424 → caller's exchange (its `dependency` is a venue slug and is **never read**), 500 → service-permanent, **never counts** (this covers the bodyless `text/plain` unhandled-exception shape), 503 → counts on `breaker:<dependency>`. `dependencyFor` validates against a closed `SERVICE_DEPENDENCIES` set and falls back to the global key with a fail-loud console otherwise — so a venue name on a 500 cannot poison a breaker key **regardless of what the Python emits** (closes the CONTEXT's M-2 carry-caveat by construction, not by trust). **Mutation 4 (M35) RED.** |
| **SC3** | Mutating any breaker constant or per-route budget turns a test **RED** — the direct inversion of Phase 140 | ✓ VERIFIED | Verified at source AND by measurement. The self-referential fake is cut at **all four** `fakeRatelimitFor` sites — `grep -rn "opts\.limiter\.tokens"` returns **zero** consuming call sites; the sole surviving `opts.limiter` reference (`resilient-fetch.test.ts:208`) **records** into `shared.constructed` and is asserted against hand-typed literals `{ tokens: 5, window: "30 s" }` (observe-not-consume). At base `b70ad840` all four sites read `fakeRatelimitFor(store, opts.limiter.tokens)` — the cut is real. `seam-constants.pin.test.ts` asserts against 22 hand-typed literals. **Mutations 1 + 2 (M14b + counterfactual) prove the oracle recovered its teeth.** |
| **SC4** | Redis-side semantics verified against **real Redis** | ✓ VERIFIED — **with the phase's named residual, accepted** | I ran the lane myself: digest-pinned `redis@sha256:e772…` + `hiett/serverless-redis-http@sha256:5b0b…`, **7/7 PASS**. The anti-skip property works — without env it throws `SeamRedisLaneUnavailableError` ("This lane is a GATE and must never skip") rather than skipping. **Mutation 6 (M19R) RED against the deployed Lua**, and its `EXPECTED_CASES = 7` fence fired. Decay + weighted carry-over are falsified. **Mutation 5 (M19) GREEN — confirmed by me in both lanes.** See W-1/W-2. |
| **SC5** | Degraded/hung store cannot hold a lambda past budget; invariant accounts for store round trips in all three states | ✓ VERIFIED | `Redis.fromEnv({ retry: { retries: BREAKER_STORE_RETRIES, backoff: () => BREAKER_STORE_BACKOFF_MS }, signal: () => AbortSignal.timeout(BREAKER_STORE_TIMEOUT_MS) })` (`resilient-fetch.ts:691-698`) — the **factory** form, correctly (the plain form fabricates a 200 on abort, which would report an unarmed breaker as armed). `seam-budgets.invariant.test.ts` SC-4b computes per route × 3 breaker states, **reading `maxDuration` from DISK** (independent oracle, never the table against itself), and carries an explicit anti-vacuity fence (`STORE_COMMAND_WORST_CASE_MS` pinned to `4_250`; every state must charge ≥1 round trip). A-09 sentinel guard present and discriminates on `reason === "timeout"` **not** `limit === 0` — correctly, because `"cacheBlock"` is a genuine denial that must still trip. |
| **SC6** | Leaf import, budget-key swap, or warmer routed through the core each fail a test | ✓ VERIFIED | (a) `seam-errors.ts`, `seam-discriminator.ts`, `seam-redaction.ts` all have **zero** `import` statements; purity guards comment-strip before matching. (b) warmer exclusion: `seam-budgets.invariant.test.ts:307-330` pins both `/health` warmers as **MEMBERS** of the exclusion set (anti-vacuity — deleting a warmer's row cannot silently shrink the guard). (c) **Mutation 7 (M22b) RED on exactly one assertion.** The ROADMAP's own disclosure that clause (b) was ADDITIVE (ESLint had `no-raw-analytics-fetch` `"off"` on both warmer paths, so the guard was BUILT not tightened) is accurate and correctly refuses credit. |
| **SC7** | `analytics-client.ts` mints `X-Tenant-Claim`, flipping the SIX reachable routes — five live | ✓ VERIFIED at the corrected scope | **I re-derived the reachable set independently.** `grep -rn "tenant_or_platform_key" analytics-service/routers/` → **9** sites. Cross-referencing the paths `analytics-client.ts` actually calls: validate-key, encrypt-key, optimize-weights, portfolio-analytics, portfolio-optimizer, portfolio-bridge = **6 reachable**. `fetch-trades` is absent from the client; `csv/validate` and `verify-strategy` TS routes both `import { postProcessKey }` and re-target to `/process-key` — **3 unreachable, confirmed**. `computePortfolioAnalytics` has **zero non-test callers** ⇒ 1 of the 6 is dead ⇒ **5 live**. The corrected wording is right and the original "all nine" was false. **Mutation 3 (M28) RED.** |

**Score: 7/7.**

### SC7 — server-derived identity, traced to the session

The stance asked specifically whether the header is minted from a **server-derived** identity (a
client-suppliable tenant claim would be worse than none). Traced at source:

- `TenantIdentity` is a **one-field object**, deliberately not a bare `string`, so a transposition
  with the adjacent `passphrase` parameter on `validateKey`/`encryptKey` becomes a type error rather
  than a credential minted into an outbound header.
- `src/app/api/keys/validate-and-encrypt/route.ts:166` passes `userId: user.id` from the route's
  `withAuth` session and **threads** it into `legacyValidateAndEncryptHandler` rather than
  re-deriving it — so it provably cannot drift to a body field.
- `create-with-key/route.ts:276,314` and `composite/add-key/route.ts:261,296` both pass
  `{ userId: user.id }`.
- `tenant-claim.ts:125-157` refuses to mint on an empty payload, a dotted payload, or an absent
  secret — the empty-secret guard is the one standing between a missing `INTERNAL_API_TOKEN` and a
  silent production no-op.
- No `X-User-Id` is sent anywhere (`analytics-client.test.ts:1007`).

### SC7 — the isolation test genuinely proves it

`analytics-client.test.ts:1104-1140` defines `pythonBucketKey`, a **hand-transcribed mirror** of
`rate_limit.py`'s `verify_tenant_claim` + `tenant_or_platform_key` — not an import, and not an
assertion that "two claim strings differ" (which would pass for two claims that BOTH fail
`compare_digest` and BOTH land in the same `platform:<path>` bucket — the exact regression). I diffed
the transcription against `analytics-service/services/rate_limit.py:252-302` line by line: the 512-char
cap, the `rsplit(".", 2)` parse, the empty-payload refusal, the `exp` check, the constant-time compare,
and the `ANON_PAYLOAD` arm are all faithful. The test asserts
`bucketA === "optimize_weights:t:tenant-a"`, `bucketB === "optimize_weights:t:tenant-b"`,
`bucketA !== bucketB`, and that **neither** equals `platform:/api/optimize-weights`. It also splices
tenant-b's payload onto tenant-a's MAC and asserts the forgery degrades to the platform bucket rather
than becoming tenant-b.

---

## Requirements coverage — SEAMCORE-01..11

| Req | Status | Source evidence gathered by the verifier |
|-----|--------|------------------------------------------|
| **SEAMCORE-01** | ✓ SATISFIED | `seam-discriminator.ts` — attributability decided on status before any body read; `text/plain` 500 handled; `dependencyFor` closed-set validated. Mutation 4 (M35) RED. |
| **SEAMCORE-02** | ✓ SATISFIED | `resilient-fetch.ts:1287-1375` — recording window covers the body read; `SeamBodyReadError` typed; `recordOnce` latch prevents double-recording a 503-with-stalling-body. |
| **SEAMCORE-03** | ✓ SATISFIED | Bounded store config at `:691-698` (retries + backoff + `AbortSignal.timeout` **factory**); SC-4b charges store round trips in all three states with an anti-vacuity fence. |
| **SEAMCORE-04** | ✓ SATISFIED | Single-read breaker state — the expiry travels **inside** the lock value (`open:<armedAt>:<expiresAt>`), so OPEN/CLOSED and `retryAfterS` come from ONE read. A healed circuit is never reported open. Mutation 4's negative controls exercise the per-key path. |
| **SEAMCORE-05** | ✓ SATISFIED | A-09 sentinel at `:1069` discriminating on `reason`; A-25 no-re-arm at `:1124` (`admittedAtMs <= existing.armedAtMs`) with `armedAt` **stored not derived**; cooldown-vs-window ordering asserted. |
| **SEAMCORE-06** | ✓ SATISFIED | `seam-redaction.ts` (318 lines) + `sentry-capture.ts` (162 lines). Scrub folded in at the **chokepoint** — `scrubCaptureInput` runs inside `captureToSentry`, so all ~143 call sites repo-wide are covered, not just the 10 seam ones. Fail-safe: the scrub has its own try/catch and withholds rather than throwing into the caller's catch arm. `seam-log-coverage.test.ts` scans real source **comment-stripped**, with a positive control, a negative control, a prose-in-a-string case, a template-interpolation case, and a `)`-inside-a-string case — the scanner itself is tested. The ROADMAP's disclosure that this clause is ADDITIVE ("no leak was plugged; a mechanism was built") is accurate. |
| **SEAMCORE-07** | ✓ SATISFIED | The phase's spine. See SC3 + mutations 1 and 2. |
| **SEAMCORE-08** | ✓ SATISFIED | Leaf purity (zero imports in all three leaves); 13/13 budget-key pins **+ the roster mechanism** (mutation 7); warmer exclusion with both warmers pinned as members; `no-raw-analytics-fetch.mjs:236-250` closes alias taint to a genuine **fixed point** with structural (not magic-cap) termination — 22/22 RuleTester fixtures pass. |
| **SEAMCORE-09** | ✓ SATISFIED — **one named residual** | Real-Redis lane 7/7 run by the verifier; fails loud rather than skipping. Mutation 6 RED. Residual (`nx` itself unfalsified) recorded, correctly diagnosed, handed to Phase 141. See W-1/W-2. |
| **SEAMCORE-10** | ✓ SATISFIED | `finalize-wizard/route.ts:123` `MAX_COMPOSITE_MEMBERS = 10`, enforced **at the query** (`.limit(MAX_COMPOSITE_MEMBERS)` at `:806`) with a fail-loud at-the-cap arm at `:845`. SC-4e binds the declared `calls` to the cap read from the **route file on disk** (a Next route cannot export it), so raising the cap without raising the declaration reddens. SC-4b **maximises across mutually-exclusive branches** rather than summing them. |
| **SEAMCORE-11** | ✓ SATISFIED — **both halves verified** | The stance flagged this as half-delivered by wave 5 and closed by wave 11. Both halves are present at source: **wave 5** — `redirect: "error"` (`resilient-fetch.ts:1577`) and `timeoutMsOverride` bounds validation raising at entry (`:1472-1482`); **wave 11** — `NULL_BODY_STATUSES = {204,205,304}` + `UNUSABLE_RESPONSE_STATUS = 502` + `unusableSeamResponseMessage` in **both** clients (`analytics-client.ts:127-154,348,398`; `process-key-client.ts:135-154,474-495`), and `CircuitOpenError`'s `retryAfterS` constructor validation (`seam-errors.ts:75-80`). Both clients route through `resilientFetch`, so the one `redirect: "error"` covers both. |

**11/11. No requirement was ticked on a partial delivery.**

---

## Scope fence

| Fence | Claimed | Verified | Status |
|-------|---------|----------|--------|
| TypeScript only, **zero Python** | 0 `.py` | `git diff --name-only b70ad840..HEAD \| grep -c '\.py$'` → **0** | ✓ (see W-4 on the base) |
| Commit count | 55 | `git log --oneline b70ad840..HEAD \| wc -l` → **55** | ✓ |
| Retry **not** enabled | `SEAM_RETRIES` 0 | `resilient-fetch.ts:249` `export const SEAM_RETRIES = 0;`, consumed by all 13 rows | ✓ Expressible per-call-site, not enabled |
| No new type suppressions | 0 | `git diff … \| grep '^\+.*@ts-ignore\|@ts-expect-error\|@ts-nocheck\|eslint-disable'` → **none** | ✓ |

---

## Gate verification — every number re-run by the verifier

| Gate | Claimed in ROADMAP | Measured by verifier | Match |
|------|--------------------|-----------------------|-------|
| Full suite | 9303 passed / 287 skipped / 724 files | **9303 passed \| 287 skipped (724 files)**, 705 files passed + 19 skipped | ✓ exact |
| `tsc` | 0 | **exit 0** | ✓ |
| `lint` | 0 errors | **0 errors, 1 warning** (`EquityChart.tsx:1119` `react-hooks/exhaustive-deps` — pre-existing, not a phase file) | ✓ |
| Coverage | 84.57 / 78.52 / 81.78 / 86.68 vs 80 / 72 / 74 / 82 | **Statements 84.57 · Branches 78.52 · Functions 81.78 · Lines 86.68**, exit 0 vs thresholds statements 80 / branches 72 / functions 74 / lines 82 | ✓ exact |
| Real-Redis lane | 7/7, fence matching | **7/7**, `EXPECTED_CASES = 7` fence observed firing under M19R | ✓ |
| CI gate wiring | `frontend-seam-redis` blocking | `ci.yml:359` job · `:782` in `needs:` · `:796` in the result loop taking the **strict** `elif [ "$result" != "success" ]` arm (no fork-skip tolerance, unlike `e2e-seeded`) | ✓ — but see **W-3** |

---

## Anti-pattern scan (42 phase-touched `.ts`/`.tsx`/`.mjs` files)

| Pattern | Hits | Severity | Disposition |
|---------|------|----------|-------------|
| `TBD` / `FIXME` / `XXX` | **0** | — | Debt-marker gate PASSES |
| `TODO` / `HACK` | 1 — `keys/validate-and-encrypt/route.ts:159` `TODO(phase-19+)` | info | **Pre-existing** — confirmed present at base `b70ad840:158`. Not introduced by this phase, and it carries a phase reference. |
| Self-referential test oracles | **0 consuming sites** | — | All four `fakeRatelimitFor` sites take the hand-typed default; the sole `opts.limiter` read is observe-not-consume, asserted against literals |
| Comment prose defeating grep gates | **0** | — | Every source-text guard comment-strips **before** matching (`seam-log-coverage.test.ts:79`, `seam-errors.purity.test.ts:56`, `seam-discriminator.purity.test.ts:57`), and the strippers themselves are unit-tested with positive **and** negative controls |
| Vacuous assertions | **0 found** | — | Anti-vacuity fences found in three independent places: `EXPECTED_CASES = 7` (redis lane), `STORE_COMMAND_WORST_CASE_MS` pin (SC-4b), warmers-as-MEMBERS (exclusion set) |

---

## Warnings

### W-1 — the ROADMAP softens a residual its own ledger states correctly (documentation)

ROADMAP SC4 says *"the `nx` mechanism is a redundant second layer since wave 7"*.
`140.2-VALIDATION.md` §8 says *"That is a real behavioural difference and a real open falsifier gap."*

**The ledger is right.** At `resilient-fetch.ts:1102-1140`, `nx: true` fires **only** when
`existing === null` at read time. The wave-7 early return requires the read to have **seen** a live
lock. The case `nx` guards is therefore the one the early return structurally cannot reach: two
instances that both read `null` and both write. Not redundant — orthogonal. Recommend adopting the
ledger's wording in the ROADMAP.

### W-2 — a third unfalsified `nx` property, named nowhere

`written` gates `emitBreakerTransition("open", …)` at `resilient-fetch.ts:1152-1160`. The code's own
comment states the requirement: *"`nx` answers `null` when a concurrent instance won the race … claiming
a transition here would double-count every trip across a Fluid Compute fleet."* With `nx: false` both
racers receive `"OK"` and both emit. That is an **observability** property riding on the same flag, it
is equally unfalsified, and neither the ROADMAP nor VALIDATION.md names it. It should join the Phase
141 handoff.

### W-3 — the gates this phase built are not enforced at merge (human decision)

Confirmed independently:

```
gh api repos/:owner/:repo/rulesets                    → []
gh api repos/:owner/:repo/branches/main/protection    → 404 "Branch not protected"
```

The workflow wiring is correct and strict. The repo setting that would make it binding does not exist.
This is a founder/repo-admin action, outside the phase's control, and the phase recorded it honestly
in SC4's evidence column rather than absorbing it.

### W-4 — the recorded phase base is off by six commits (info)

The scope statement names `a12b9eaa` as "the 140.1.2 close". It is not: `b70ad840` is. `a12b9eaa..HEAD`
spans 65 commits and includes six 140.1.2 Python files (`error_contract.py`, `adapter.py`,
`long_fetch.py`, `main.py`, and two tests) — all authored by 140.1.2 commits, none by 140.2. At the
correct base the fence is exact. **No violation occurred**; only the recorded coordinate is wrong.

### W-5 — second disclosed falsifier gap (info)

M25's `bare-scalar-429` **tolerance** clause cannot fail under its own mutation. The phase found this,
re-expressed the row rather than leaving the clause standing as if it were caught, and handed the
property to Phase 141. Disclosure verified present; not independently re-run.

---

## Adjudication of the stance's five specific challenges

| # | Challenge | Finding |
|---|-----------|---------|
| 1 | *The harness can now fail — verify the cut at source and RUN ≥2 ledger rows yourself* | **Upheld.** Cut verified at all four sites at source, with the base-commit diff proving it was real. **Seven** mutations run, including a counterfactual that isolates the cut as the cause. |
| 2 | *M19 is GREEN — verify the diagnosis; judge whether GREEN + M19R masks a real gap* | **Diagnosis correct at source** (`:1107` early return ahead of the `:1135` lock write). **GREEN reproduced by me** in both the redis lane (7/7) and the unit suite (3271 passed). Recording it GREEN was **right** — hiding it would have been the Phase-140 failure repeating. Adding M19R was **right** — it restores falsifiability of the property that moved. It does **not** mask the gap: VALIDATION.md §8 names it precisely. The only defect is the ROADMAP's softer "redundant" gloss (**W-1**) and one unnamed property (**W-2**). |
| 3 | *Re-derive SC7's reachable set; verify server-derived identity; verify cross-tenant isolation* | **All three upheld independently.** 9 sites → 6 reachable → 5 live, re-derived by me. Identity traced to `withAuth` sessions at every call site and threaded, not re-derived. The isolation oracle is a hand-transcribed Python mirror, verified faithful against `rate_limit.py:252-302`, and refuses the payload-splice forgery. |
| 4 | *Is SC4's ✅-with-residual honest, or should it be partial?* | **Honest.** SC4 names three properties. Sliding-window decay and weighted carry-over are falsified against real Redis. The third, *trip idempotency*, **is** falsified — by M19R, which I ran and which reddened against the deployed Lua. Only the `nx` **mechanism** is unfalsified, and the criterion's noun is the property. A stricter adjudicator could call it PARTIAL; I do not, because the phase found the gap itself, disclosed it in three places, diagnosed it at source, and built a replacement falsifier. That is the opposite of the behaviour this phase exists to end. **But the residual must be described as the ledger describes it, not as "redundant" (W-1).** |
| 5 | *Partial deliveries must not be ticked — check SEAMCORE-11's two halves and every 01..11 tick* | **No partial tick found.** SEAMCORE-11's wave-5 half (`redirect: "error"`, override validation) and wave-11 half (null-body/non-JSON outcome in **both** clients, `CircuitOpenError` validation) are both present at source. All 11 requirements carry independent source evidence. |
| 6 | *Scope fence* | **Holds** at the correct base — 55 commits, zero `.py`, `SEAM_RETRIES = 0`, zero new suppressions. See W-4. |
| 7 | *Toothless tests / comment prose defeating greps* | **None found.** Every source-text guard comment-strips before matching, and the strippers are themselves tested with positive and negative controls. Assertions are against hand-typed literals; three independent anti-vacuity fences found. |

---

## What the SUMMARYs claimed that I could NOT confirm at source

Nothing material. Every headline number I re-ran matched exactly (full suite, tsc, lint, coverage to
two decimals, redis lane). Two items were verified as **disclosures** rather than re-derived:

- **M25's re-expressed clause** (W-5) — I confirmed the disclosure text and the row re-expression are
  present in the ledger; I did not re-run M25 to independently confirm the bare-scalar-429 case cannot
  redden.
- **The remaining ~48 ledger rows** I did not personally execute. I ran 7 of 56 — chosen as the
  highest-leverage and most-contested (the headline harness claim plus its counterfactual, the GREEN
  row, its replacement, the class-closure mechanism, and one falsifier each for SC2 and SC7). All 7
  matched their recorded receipts exactly, including failure **counts** (M28: 16, M35: 3, M22b: 1).
  That correspondence is strong evidence the remaining table is sound, but it is inference, not
  measurement.

---

## Gaps Summary

**None blocking.** No must-have is FAILED. The phase goal — *the breaker counts the failures it exists
for, ignores the ones it must not, and every constant governing it is falsifiable* — is achieved and
independently demonstrated.

Two falsifier gaps remain **open and correctly handed to Phase 141**: the `nx` flag's concurrent-race
idempotency (plus, newly identified here, its transition-event de-duplication) and M25's bare-scalar-429
tolerance property. Neither is a regression in behaviour; both are regressions in *observability of*
behaviour, and both are named rather than absorbed.

Three warnings require attention: **W-1** (ROADMAP wording softer than its own ledger — fix the text),
**W-2** (add the third `nx` property to the Phase 141 handoff), and **W-3** (branch protection is absent,
so nothing enforces any CI gate at merge — founder action).

---

_Verified: 2026-07-27T05:23:02Z_
_Verifier: Claude (gsd-verifier) — 7 mutations, 5 suites and 2 GitHub API probes executed first-hand_
