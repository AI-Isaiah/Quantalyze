---
phase: 130-golive-railway-static-egress-whitelist-sfox-flag-flip-live-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/runbooks/sfox-go-live.md]
autonomous: false
requirements: [EGRESS-01, EGRESS-02, EGRESS-03, GOLIVE-01, GOLIVE-02]

must_haves:
  truths:
    - "The founder can execute the entire sFOX go-live from ONE runbook: read the Railway 3-IP static-outbound set, verify realized egress stays in-set (country NL), whitelist ALL 3 at sFOX, pass an explicit gate checklist, flip both flags, prove api_verified live across surfaces and roles, and roll back trivially"
    - "The GOLIVE-01 gate is an explicit written checklist (EGRESS 1-3 + FACTSHEET blocking e2e green via gh pr checks + E2GT two-part verdict exit 0 AND within_same_day_tolerance===true) - the flip is never assumed green"
    - "Rollback is one trivial documented op: both flags back to empty restores the proven-safe dormant v1.12 state, zero user impact"
    - "All 5 EGRESS/GOLIVE requirements are dispositioned human_needed-OPEN with the runbook named as the delivered execution path - none is ever claimed done without live evidence (observed NL IP set / authenticating whitelisted key / api_verified render)"
  artifacts:
    - path: "docs/runbooks/sfox-go-live.md"
      provides: "Founder step-by-step sFOX go-live execution path (Steps 0-5) with explicit GOLIVE-01 gate + trivial rollback"
      min_lines: 120
  key_links:
    - from: "docs/runbooks/sfox-go-live.md"
      to: "docs/runbooks/flipretry-derived-equity-go-live.md"
      via: "E2GT two-part gate cross-reference (the FLIP runbook Step 4 defines exit-0 AND anchor_consistency.within_same_day_tolerance===true)"
      pattern: "flipretry-derived-equity-go-live"
    - from: "docs/runbooks/sfox-go-live.md"
      to: "docs/runbooks/railway-worker.md"
      via: "worker redeploy + /health verification procedure (git_sha convergence, skipped-deploy gotcha)"
      pattern: "railway-worker"
    - from: "docs/runbooks/sfox-go-live.md"
      to: "Phase-126 blocking e2e-seeded gate in the frontend aggregator"
      via: "gh pr checks verification named in the Step 3 gate checklist"
      pattern: "gh pr checks"
---

<objective>
Deliver the milestone culmination HONESTLY: the milestone ships FLAG-OFF (as v1.12
did), and this phase delivers the founder's complete, gated, rollback-safe go-live
execution path — NOT the flip itself.

The ONE buildable deliverable is `docs/runbooks/sfox-go-live.md` (GOLIVE-RB): the
founder's step-by-step path for all 5 live ops in order — Railway 3-IP static-egress
read (EGRESS-01), realized-egress probe verification (EGRESS-02), sFOX whitelist +
native-egress key auth proof (EGRESS-03), the EXPLICIT flag-flip gate (GOLIVE-01),
and the live api_verified proof across surfaces and roles (GOLIVE-02) — plus a
trivial rollback. All 5 requirements are founder `human_needed` LIVE ops (they need
the Railway Pro dashboard, `railway ssh` into the prod worker, the sFOX dashboard /
security@ handoff, Railway + Vercel prod env toggles, and a real IP-whitelisted sFOX
key + multi-role session — none of which the autonomous run holds; all are also
gated on the EXTERNAL sFOX IP-bind turnaround). Disposition is PRE-DECIDED: DEFER —
each recorded human_needed-OPEN with the runbook as the delivered execution path.

Purpose: sFOX becomes offerable and LIVE on native Railway static egress the moment
the founder executes the runbook — with an explicit gate so the flip can never
happen on an assumed-green, and a rollback so trivial it removes all flip risk.
Output: `docs/runbooks/sfox-go-live.md` (new, grep-gate verified) + 5 requirements
honestly dispositioned human_needed-OPEN.

⭐ ZERO code/build work (founder 2026-07-19, locked): Railway Pro static outbound IS
the egress solution — NOT Fly, NOT a proxy. `WORKER_EGRESS_PROXY_URL` stays UNSET.
`fly-egress-proxy/` stays dormant and unwired. Nothing to install, wire, or migrate.
No task in this plan touches prod, source code, dependencies, or migrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/130-golive-railway-static-egress-whitelist-sfox-flag-flip-live-proof/130-CONTEXT.md
@docs/runbooks/flipretry-derived-equity-go-live.md
@docs/runbooks/railway-worker.md

Key facts the executor needs (no codebase exploration required):
- Flags: `SFOX_ENABLED` (Railway worker/server env) + `NEXT_PUBLIC_SFOX_ENABLED`
  (Vercel client env) — both EMPTY today (dormant, shipped v1.12 flag-off). The
  flip is a LIVE env op in BOTH dashboards, NOT a migration; each side needs a
  redeploy so the env takes (`NEXT_PUBLIC_*` is baked into the Vercel client
  bundle at build time — an env change without a redeploy is a silent no-op).
- Railway service: `quantalyze-analytics`, Pro plan (upgraded 2026-07-19), static
  outbound = a FIXED SET of 3 load-balanced Amsterdam/NL IPs read from service →
  Settings → Networking. Known current egress observation: `152.55.184.85`
  (AS400940, NL) — ONE observed IP, which is exactly why the whole-set rule exists.
- Probe pattern (from memory/railway-worker.md): `railway ssh "cd /app && curl -s ipinfo.io"`.
  Worker health: `curl -s https://quantalyze-analytics-production.up.railway.app/health`
  (returns `{status, version, git_sha, worker_last_tick_at, worker_age_s}`).
- Phase-126 FACTSHEET gate: the seed-gated `e2e-seeded` job (runs
  `e2e/sfox-badge.spec.ts`, owner/allocator/admin + axe) is wired BLOCKING into the
  `frontend` branch-protection aggregator (commit f1102ee5) — the gate check is
  `gh pr checks` on the milestone PR showing `e2e-seeded` green.
- Phase-127 E2GT gate: `e2_allocator_ground_truth.py` live run, TWO-PART verdict —
  exit 0 AND `anchor_consistency.within_same_day_tolerance === true` in the emitted
  JSON (exit 0 alone is NOT a pass; the drifted-curve failure mode exits 0 with the
  verdict in the JSON field). Fully specified in
  `docs/runbooks/flipretry-derived-equity-go-live.md` Step 4 — cross-reference it,
  do not restate its env/arg detail.
- sFOX whitelist channel: security@quantalyze.com / the sFOX dashboard; sFOX's
  per-key IP-restriction count is undocumented — if sFOX requires a SINGLE IP, the
  fallback is a proxy addon (QuotaGuard-type), NOT Fly (locked decision).
- Rollback: both flags back to empty (+ redeploy both sides) restores the
  proven-safe DORMANT v1.12 state — zero user impact.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/runbooks/sfox-go-live.md — the founder's Steps 0-5 go-live execution path (GOLIVE-RB)</name>
  <files>docs/runbooks/sfox-go-live.md</files>
  <read_first>docs/runbooks/flipretry-derived-equity-go-live.md (MIRROR its structure: title/Owner/Risk header, why-this-exists section, Step-N ordering, per-step Verify + Abort-path bullets, explicit gate, dedicated ROLLBACK step reachable from any step), docs/runbooks/railway-worker.md (health-check + redeploy + skipped-deploy-gotcha procedures to cross-reference, `railway ssh` one-off pattern)</read_first>
  <action>
    Create the NEW runbook `docs/runbooks/sfox-go-live.md` (no such file exists; the
    existing FLIP runbook covers only the derived-curve flip, not egress or the sFOX
    flags). It is the founder's step-by-step execution path for all 5 live ops, per
    the locked 130-CONTEXT decisions. Structure, mirroring the sibling FLIP runbook:

    HEADER + FRAMING: title "Runbook — sFOX Go-Live (GOLIVE, Phase 130)"; Owner:
    founder (all legs are LIVE ops); Risk: flipping the flags on an assumed-green
    gate / whitelisting a partial IP set → intermittent sFOX auth failures. A
    why-this-exists section stating the locked egress decision: Railway Pro static
    outbound is the egress path (founder 2026-07-19) — NO Fly, NO proxy,
    `WORKER_EGRESS_PROXY_URL` stays UNSET, `fly-egress-proxy/` dormant; native
    static egress covers BOTH sFOX AND ccxt with ZERO build work. State the
    standing rule: "Abort at any step → jump to Step 5 — ROLLBACK."

    Use EXACT step headings `## Step 0 — ...` through `## Step 5 — ...` (the
    acceptance grep gates key on `^## Step N `):

    ## Step 0 — EGRESS-01: read the FULL Railway static-outbound IP set.
    Railway dashboard → project `quantalyze-analytics` → the worker service →
    Settings → Networking. Confirm the service is on Pro with static outbound
    ACTIVE; if the dashboard says static outbound activates on next deploy,
    REDEPLOY the worker first (cross-reference `railway-worker.md` for the
    redeploy + `/health` git_sha convergence procedure and the silent
    skipped-deploy-on-red-CI gotcha). Record the FULL 3-IP set (Amsterdam/NL,
    load-balanced). ⭐ Whitelist rule stated here in bold: always the WHOLE
    dashboard set, NEVER one observed IP — Railway load-balances across all 3.
    Verify bullet: all 3 IPs recorded. Abort path: not on Pro / static outbound
    absent → stop, fix the plan state in the dashboard first.

    ## Step 1 — EGRESS-02: verify realized egress stays in-set (repeated probes).
    REPEATED probes via `railway ssh "cd /app && curl -s ipinfo.io"` — at least 5
    probes, spaced over time and ideally across a redeploy, because a SINGLE probe
    misses load-balanced sibling IPs. For every probe assert the payload's country
    is NL and collect the distinct observed `ip` values. Gate assertion (explicit):
    observed set ⊆ dashboard set from Step 0. Verify bullet: every probe NL; no
    observed IP outside the dashboard set. Abort path: any observed IP outside the
    set or non-NL → STOP, do NOT whitelist, re-read the dashboard set / contact
    Railway support — never proceed on a drifting egress.

    ## Step 2 — EGRESS-03: whitelist ALL 3 at sFOX + prove native-egress key auth.
    Hand the FULL 3-IP set to sFOX (security@quantalyze.com handoff / the sFOX
    dashboard). Note: sFOX's per-key IP-restriction count is undocumented — founder
    confirms 3 IPs are accepted; if sFOX requires a single IP, the fallback is a
    proxy addon (QuotaGuard-type), NOT Fly (locked decision). Then PROVE an
    IP-whitelisted sFOX key authenticates end-to-end from the worker's NATIVE
    Railway egress — no proxy in the path: verify `WORKER_EGRESS_PROXY_URL` is
    UNSET on the worker service env before the proof. This step GATES the flag
    flip. Verify bullet: sFOX confirms all 3 IPs bound; a whitelisted key
    authenticates from the worker. Abort path: auth fails from native egress →
    re-check the bound IP set vs Step 0/1 evidence before touching any flag.

    ## Step 3 — GOLIVE-01: the EXPLICIT flip gate (never assumed), then the flip.
    An explicit checklist table — ALL rows must be green before the flip:
    (1) EGRESS-01 dashboard set read; (2) EGRESS-02 observed ⊆ set, all NL;
    (3) EGRESS-03 whitelisted key authenticates from native egress;
    (4) FACTSHEET — the Phase-126 BLOCKING `e2e-seeded` job (sfox-badge.spec.ts,
    owner/allocator/admin + axe) is green, verified via `gh pr checks` on the
    milestone PR — never assumed from a local run; (5) E2GT — the Phase-127
    E2GT-01 live run is TWO-PART green: exit 0 AND
    `anchor_consistency.within_same_day_tolerance === true` in the evidence JSON
    (exit 0 alone is NOT a pass) — cross-reference
    `flipretry-derived-equity-go-live.md` Step 4 for the full procedure rather
    than restating env/args. ONLY when all 5 rows are checked: flip
    `SFOX_ENABLED` on the Railway worker service AND `NEXT_PUBLIC_SFOX_ENABLED`
    in Vercel prod env — LIVE env ops, NOT migrations — then REDEPLOY BOTH sides
    so the env takes (`NEXT_PUBLIC_*` is baked into the client bundle at build
    time; an un-redeployed Vercel env change is a silent no-op). Verify bullet:
    Railway `/health` fresh at the expected git_sha post-redeploy; the sFOX card
    appears in the add-key wizard exchange picker on prod. Abort path: any gate
    row not green → DO NOT FLIP; the gate is explicit, never assumed.

    ## Step 4 — GOLIVE-02: live proof — full flow, every surface, every role.
    With the flags on, a real user connects a LIVE IP-whitelisted sFOX key through
    the add-key wizard end-to-end; its `api_verified` strategy renders LIVE across
    ALL THREE surfaces — factsheet + discovery + edit — proven E2E across owner /
    allocator / admin roles. The full flow, not one surface (the v1.10 lesson:
    after any flag flip, test the WHOLE flow E2E). Verify bullet: the api_verified
    badge observed on all 3 surfaces for all 3 roles; the connect wizard completed
    without a fail-closed error. Abort path: any surface/role missing the render →
    Step 5 ROLLBACK, then root-cause with the flags safely off.

    ## Step 5 — ROLLBACK (trivial, executable at ANY step).
    Set BOTH flags back to empty (`SFOX_ENABLED` in Railway, `NEXT_PUBLIC_SFOX_ENABLED`
    in Vercel) and redeploy both sides → restores the proven-safe DORMANT v1.12
    state (tag `v1.12`), zero user impact — sFOX simply disappears from the picker;
    no data written by the flip needs cleanup (the flip is env-only). No migrations,
    no SQL, no code revert.

    Constraints: NO secrets/keys/tokens anywhere in the runbook (creds live only in
    the Railway/Vercel/sFOX dashboards — mirror the FLIP runbook's env-only, never
    argv/committed-file rule); do NOT invent Railway dashboard UI labels beyond
    "service → Settings → Networking" (the CONTEXT-given path); do NOT add a Fly or
    proxy step (locked decision — the fallback mention in Step 2 is the only
    permitted proxy reference, explicitly "NOT Fly"); do NOT restate the E2GT
    harness env/arg detail (cross-reference the FLIP runbook Step 4 — one source of
    truth); keep the E2GT gate wording exactly two-part (exit 0 AND
    within_same_day_tolerance===true).
  </action>
  <verify>
    <automated>RB=docs/runbooks/sfox-go-live.md; test -f "$RB" && [ "$(wc -l < "$RB")" -ge 120 ] && for s in 0 1 2 3 4 5; do grep -qE "^## Step $s " "$RB" || { echo "MISSING Step $s"; exit 1; }; done && grep -q 'railway ssh' "$RB" && grep -q 'ipinfo.io' "$RB" && grep -qi 'NL' "$RB" && grep -q 'security@quantalyze.com' "$RB" && grep -q 'WORKER_EGRESS_PROXY_URL' "$RB" && grep -q 'NEXT_PUBLIC_SFOX_ENABLED' "$RB" && grep -qE '[^_A-Z]SFOX_ENABLED' "$RB" && grep -q 'gh pr checks' "$RB" && grep -q 'within_same_day_tolerance' "$RB" && grep -q 'flipretry-derived-equity-go-live' "$RB" && grep -q 'railway-worker' "$RB" && grep -q 'api_verified' "$RB" && grep -qi 'discovery' "$RB" && grep -qi 'allocator' "$RB" && grep -qi 'admin' "$RB" && grep -qi 'rollback' "$RB" && ! grep -qiE 'fly deploy|flyctl' "$RB" && echo RUNBOOK-GATES-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `docs/runbooks/sfox-go-live.md` exists, >= 120 lines, with EXACT headings `## Step 0 ` through `## Step 5 ` (grep `^## Step N `).
    - Step 0 contains the whole-set rule (whitelist the WHOLE dashboard set, never one observed IP) and the Settings → Networking read path.
    - Step 1 contains the repeated-probe command (`railway ssh` + `curl -s ipinfo.io`), the NL country assertion, and the observed ⊆ dashboard-set gate.
    - Step 2 contains security@quantalyze.com, the 3-IP whitelist handoff, the native-egress key-auth proof, and `WORKER_EGRESS_PROXY_URL` UNSET verification; the only proxy mention is the single-IP fallback explicitly "NOT Fly".
    - Step 3 contains the 5-row explicit gate checklist including `gh pr checks` (FACTSHEET) and the two-part E2GT verdict (`within_same_day_tolerance`), both flag names, and the redeploy-both-sides instruction.
    - Step 4 names all three surfaces (factsheet + discovery + edit) and all three roles (owner / allocator / admin) with `api_verified`.
    - Step 5 rollback = both flags → empty + redeploy, dormant v1.12, no SQL/migration/code revert.
    - Cross-references to `flipretry-derived-equity-go-live` and `railway-worker` present; zero Fly-deploy content (`fly deploy`/`flyctl` absent); zero secret material.
    - The single chained automated command above prints RUNBOOK-GATES-GREEN.
  </acceptance_criteria>
  <done>The runbook exists, every grep gate is green, and a founder with prod access could execute Steps 0-5 without asking a single clarifying question. No prod system was touched to verify it.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: EGRESS-01/02/03 — founder LIVE egress ops (PRE-DECIDED: DEFER, human_needed-OPEN)</name>
  <read_first>docs/runbooks/sfox-go-live.md Steps 0-2 (written in Task 1 — the delivered execution path)</read_first>
  <action>
    The three EGRESS requirements are founder LIVE ops the autonomous run cannot
    perform (they need the Railway Pro dashboard, `railway ssh` into the running
    prod worker, and the sFOX dashboard / security@quantalyze.com handoff) and are
    additionally gated on the EXTERNAL sFOX IP-bind turnaround (outside our
    control; kicked off day 1 in parallel):
    - EGRESS-01: read the FULL 3-IP static-outbound set from the Railway dashboard
      (Settings → Networking; redeploy first if static outbound activates on next
      deploy). Whitelist rule: the WHOLE set, never one observed IP.
    - EGRESS-02: repeated `railway ssh "cd /app && curl -s ipinfo.io"` probes —
      every probe country NL, observed IP set ⊆ dashboard set (a single probe
      misses load-balanced siblings).
    - EGRESS-03: all 3 IPs whitelisted at sFOX + an IP-whitelisted sFOX key
      authenticates end-to-end from the worker's NATIVE egress
      (`WORKER_EGRESS_PROXY_URL` UNSET, no proxy in the path). Gates the flip.
  </action>
  <disposition>
    PRE-DECIDED: DEFER. Record EGRESS-01, EGRESS-02, EGRESS-03 as
    human_needed-OPEN in the SUMMARY and REQUIREMENTS traceability, each naming
    `docs/runbooks/sfox-go-live.md` (Steps 0-2) as the delivered execution path.
    NEVER claim any leg done without the live evidence: the recorded 3-IP
    dashboard set (EGRESS-01), the multi-probe NL/in-set observation log
    (EGRESS-02), the authenticating whitelisted key from native egress
    (EGRESS-03). No simulation, no CI-derived claim, no partial credit. If the
    founder happens to run any leg during this session and supplies the live
    evidence, that leg alone may be marked done with the evidence quoted.
  </disposition>
  <verify>
    <automated>N/A BY DESIGN — founder live op against prod Railway + sFOX; the 130-CONTEXT forbids any simulated or CI-derived stand-in. The deliverable verified here is the disposition itself: the SUMMARY records all three EGRESS reqs human_needed-OPEN with the runbook path named.</automated>
    <human-check>Live evidence contract when the founder executes: (1) all 3 dashboard IPs recorded; (2) >= 5 probes, all NL, observed ⊆ set; (3) sFOX confirms 3-IP bind AND a whitelisted key authenticates from the worker with WORKER_EGRESS_PROXY_URL unset.</human-check>
  </verify>
  <acceptance_criteria>
    - EGRESS-01, EGRESS-02, EGRESS-03 each recorded human_needed-OPEN (not done, not skipped-silently) with `docs/runbooks/sfox-go-live.md` named as the execution path.
    - No task output claims any EGRESS leg complete without the live evidence quoted verbatim.
    - No prod command was run against Railway or sFOX by the executor.
  </acceptance_criteria>
  <resume-signal>Founder: reply "egress done" WITH the evidence (3-IP set, probe log, key-auth proof) to mark legs done — or "defer" (default) to keep all three human_needed-OPEN and continue.</resume-signal>
  <done>EGRESS-01/02/03 each recorded human_needed-OPEN naming docs/runbooks/sfox-go-live.md Steps 0-2 as the execution path — or a leg marked done ONLY with founder-quoted live evidence. No prod command was run by the executor.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: GOLIVE-01/02 — founder flag flip + live proof (PRE-DECIDED: DEFER, human_needed-OPEN; milestone ships FLAG-OFF)</name>
  <read_first>docs/runbooks/sfox-go-live.md Steps 3-5 (written in Task 1 — the gate checklist, flip, live proof, rollback)</read_first>
  <action>
    The two GOLIVE requirements are founder LIVE ops gated on Task 2's EGRESS
    evidence plus the two green gates from prior phases:
    - GOLIVE-01: run the Step-3 EXPLICIT gate checklist — EGRESS 1-3 live-proven +
      FACTSHEET (Phase-126 blocking `e2e-seeded` green via `gh pr checks` on the
      milestone PR) + E2GT (Phase-127 E2GT-01, itself still human_needed-OPEN:
      exit 0 AND `anchor_consistency.within_same_day_tolerance === true`). Only on
      ALL green: flip `SFOX_ENABLED` (Railway) + `NEXT_PUBLIC_SFOX_ENABLED`
      (Vercel prod) — LIVE env ops, NOT migrations — and redeploy both sides.
    - GOLIVE-02: with the flags on, a real user connects a live IP-whitelisted
      sFOX key through the wizard; its `api_verified` strategy renders LIVE across
      factsheet + discovery + edit, proven E2E across owner / allocator / admin —
      the full flow, not one surface. Rollback stays trivial (Step 5: both flags
      → empty restores dormant v1.12).
  </action>
  <disposition>
    PRE-DECIDED: DEFER. Record GOLIVE-01 and GOLIVE-02 as human_needed-OPEN in
    the SUMMARY and REQUIREMENTS traceability, naming
    `docs/runbooks/sfox-go-live.md` (Steps 3-5) as the delivered execution path
    and the upstream open gates (EGRESS-01/02/03, E2GT-01) as the blockers. The
    milestone ships FLAG-OFF — this is the honest culmination: the go-live path
    is fully documented and explicitly gated; the flip awaits the external sFOX
    whitelist + the founder's live run. NEVER claim the flip or the live proof
    done without the live evidence: flags observed set + both redeploys verified
    (GOLIVE-01), the api_verified render observed on all 3 surfaces x 3 roles
    (GOLIVE-02). No simulation, no CI-derived claim, no partial credit.
  </disposition>
  <verify>
    <automated>N/A BY DESIGN — founder live op (prod env flips + a real whitelisted key + multi-role prod session); the 130-CONTEXT forbids any simulated or CI-derived stand-in. The deliverable verified here is the disposition itself: the SUMMARY records both GOLIVE reqs human_needed-OPEN with the runbook path + blocking gates named.</automated>
    <human-check>Live evidence contract when the founder executes: Step-3 checklist all green (incl. gh pr checks output + the E2GT evidence JSON field), both flags set + both sides redeployed and verified, then the api_verified badge observed live on factsheet + discovery + edit for owner + allocator + admin.</human-check>
  </verify>
  <acceptance_criteria>
    - GOLIVE-01 and GOLIVE-02 each recorded human_needed-OPEN with `docs/runbooks/sfox-go-live.md` Steps 3-5 named as the execution path and the open upstream gates (EGRESS 1-3, E2GT-01) named as blockers.
    - The phase/milestone close states plainly: ships FLAG-OFF; the flip is delivered as an execution path, not performed.
    - No flag was flipped, no prod env was written, no migration was created by the executor.
  </acceptance_criteria>
  <resume-signal>Founder: reply "golive done" WITH the evidence (gate checklist outputs, flag/redeploy proof, 3-surface x 3-role api_verified observations) — or "defer" (default) to record both human_needed-OPEN and close the phase honestly FLAG-OFF.</resume-signal>
  <done>GOLIVE-01/02 each recorded human_needed-OPEN naming runbook Steps 3-5 as the execution path + the open upstream gates (EGRESS 1-3, E2GT-01) as blockers; the phase closes honestly FLAG-OFF — or marked done ONLY with founder-quoted live evidence. No flag flipped, no prod env written by the executor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → sFOX API | prod egress crosses the public internet; the sFOX IP whitelist is the auth surface — a wrong/partial IP set becomes intermittent auth failure |
| founder → Railway/Vercel prod env | the privileged config plane where the flags flip; no code path guards it — only the runbook's explicit gate |
| runbook → repo readers | `docs/runbooks/` is committed and world-readable within the repo; it must never carry key material |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | Spoofing/DoS | sFOX IP whitelist | mitigate | Runbook Steps 0-1 hard-code the whole-set rule + the observed ⊆ dashboard-set gate with an explicit abort path; grep gate asserts the rule text exists |
| T-130-02 | Elevation | flag flip (GOLIVE-01) | mitigate | Step-3 explicit 5-row gate checklist (EGRESS 1-3 + `gh pr checks` + two-part E2GT verdict) — flip forbidden on any non-green row; "never assumed" wording grep-gated |
| T-130-03 | Info disclosure | runbook contents | mitigate | Task-1 constraint: zero secrets/keys/tokens in the runbook; creds live only in dashboards (mirrors the FLIP runbook's env-only rule) |
| T-130-04 | Repudiation | human_needed dispositions | mitigate | Evidence contracts in Tasks 2-3: no leg claimable without quoted live evidence (probe log / key auth / rendered badge); no simulation or CI-derived claim permitted |
| T-130-05 | Tampering | rollback path | accept | Step 5 is env-only (both flags → empty + redeploy); the flip writes no data, so rollback cannot corrupt state — the dormant v1.12 state is live-proven safe |
| T-130-SC | Tampering | package installs | accept | N/A — this plan installs NOTHING (zero build work, docs-only); no npm/pip/cargo activity exists to audit |
</threat_model>

<verification>
- Task 1's single chained grep command prints RUNBOOK-GATES-GREEN (structure + all required commands/rules/cross-refs present, zero Fly content).
- `git status` shows exactly ONE new tracked file (`docs/runbooks/sfox-go-live.md`) and ZERO modifications to source, migrations, package manifests, or `fly-egress-proxy/` — the zero-build-work invariant, checkable via `git status --porcelain | grep -v 'docs/runbooks/sfox-go-live.md'` returning nothing tracked.
- The SUMMARY records EGRESS-01/02/03 + GOLIVE-01/02 human_needed-OPEN with the runbook as the execution path (unless the founder supplied live evidence at a checkpoint, in which case that evidence is quoted verbatim).
- No command in the execution log touched prod Railway env, Vercel env, the sFOX dashboard, or Supabase.
</verification>

<success_criteria>
- `docs/runbooks/sfox-go-live.md` exists and passes every grep gate — a founder with prod access can execute the full go-live (Steps 0-5) from it alone, with the explicit GOLIVE-01 gate and the trivial Step-5 rollback.
- All 5 phase requirements (EGRESS-01/02/03, GOLIVE-01/02) are covered: dispositioned human_needed-OPEN with the delivered execution path named — or marked done ONLY with founder-supplied live evidence.
- Zero code/build/prod work performed: no installs, no migrations, no env writes, no proxy wiring, `WORKER_EGRESS_PROXY_URL` untouched (UNSET), `fly-egress-proxy/` untouched.
- The phase closes honestly: milestone ships FLAG-OFF; the culmination deliverable is the gated execution path, not the flip.
</success_criteria>

<output>
Create `.planning/phases/130-golive-railway-static-egress-whitelist-sfox-flag-flip-live-proof/130-01-SUMMARY.md` when done (use Edit if the file exists — never truncate .planning ledgers with Write).
</output>
