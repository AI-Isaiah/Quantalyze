---
phase: 163-harden-fail-safe-closed-and-loud
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ratelimit.ts
  - src/app/api/bridge/route.ts
  - src/app/api/portfolio-optimizer/route.ts
  - src/lib/seam-ratelimit-posture.invariant.test.ts
  - src/app/api/bridge/route.test.ts
  - src/app/api/portfolio-optimizer/route.test.ts
autonomous: true
requirements: [SEC-04]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The bridge and portfolio-optimizer front doors are rate-limited by a named `bridgeComputeLimiter` sized from a MEASURED PROD backend number, recorded before the number was chosen"
    - "`userActionLimiter` is byte-identical to HEAD — 5/60s, untouched"
    - "The seam-limiter roster pin moved in the same commit as the limiter swap; a neutered swap fails BY NAME"
    - "A 429 from either route carries the truthful Retry-After of the new bucket"
  artifacts:
    - "src/lib/ratelimit.ts — `bridgeComputeLimiter` via makeLimiter, family docblock with the measurement on record"
    - "src/lib/seam-ratelimit-posture.invariant.test.ts — roster updated at the hand-typed pin (:252)"
  key_links:
    - "bridge route :94 + portfolio-optimizer route :113 -> checkLimit(bridgeComputeLimiter, ...)"
    - "limiter docblock -> the PROD measurement task's recorded numbers"
---

<objective>
Close SEC-04: give the bridge and portfolio-optimizer compute flows a named
`bridgeComputeLimiter` sized from measured PROD backend reality — not from the inherited
"30x" figure, and never by resizing the shared `userActionLimiter`. The measurement is an
explicit early task because the number depends on it (CONTEXT locked decision).

Purpose: the front door stops advertising 300/h/user over a backend that budgets 10/h/tenant;
callers get a truthful Retry-After instead of burning requests into a backend 429.
Output: the measurement on record, the new named limiter, two rewired routes, moved roster
pin, behavioral deny-shape coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-CONTEXT.md
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-RESEARCH.md  (Section 5, SEC-04)
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-PATTERNS.md  (Section 1)
@src/lib/ratelimit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure PROD backend reality and derive the number</name>
  <files>none (read-only measurement; output lands in Task 2's docblock and the SUMMARY)</files>
  <action>
    Put a real measurement on the record BEFORE choosing the limiter size (CONTEXT: "do
    not default to 10/3600s without the measurement on record").

    Collect, in order:
    1. Backend budgets at HEAD (re-verify, they are the floor): slowapi `"10/hour"` per
       tenant on `/portfolio-bridge` (analytics-service/routers/portfolio.py:1947-1948)
       and `/portfolio-optimizer` (portfolio.py:1685-1686); in-handler per-user sliding
       window `_BRIDGE_USER_RATE_LIMIT = 30` / `_BRIDGE_USER_RATE_WINDOW_SEC = 3600`
       (portfolio.py:227-228).
    2. Replica count for the analytics service on Railway (slowapi storage is
       `memory://` PER REPLICA — backend figures are floors xN, order-of-magnitude only;
       recorded repo caveat). Read via the Railway MCP/service settings, do not guess.
    3. Observed PROD cadence and duration: Vercel logs grouped by requestPath for
       `/api/bridge` and `/api/portfolio-optimizer` over the widest available window, and
       Railway logs for the two Python endpoints' response times. If the window shows
       zero or near-zero traffic, RECORD THAT as the measurement (a measured "unused
       surface, budget = backend floor" is a valid, honest derivation).
    4. Derive the front-door number: it must be <= the effective backend budget the
       measurement supports (per-tenant slowapi floor x replicas, bounded by the
       in-handler 30/3600 user cap for bridge), so the front door 429s with a truthful
       Retry-After BEFORE the backend does. State the arithmetic explicitly.

    ⛔ Read-only: never invoke the PROD bridge/optimizer endpoints to "time" them — that
    consumes real tenant budget and PROD compute. Derive duration from logs only.
    Record every number, its source, and the timestamp in the SUMMARY (repo-relative
    references only; no dashboard URLs with project ids, no uids — the repo is public).
  </action>
  <verify>
    <automated>grep -qE 'MEASURED[^\n]*(replica|concurrenc|per-tenant|10/hour)' "$SUMMARY_PATH" && grep -qE '[0-9]+[[:space:]]*(req|/)' "$SUMMARY_PATH" || { echo "FAIL: no recorded derivation — the measurement was skipped"; exit 1; }</automated>
    <!-- ⛔ The previous verify was `test -n "$(git log --oneline -1)"`, which is
         TRUE in any git repo and therefore could not fail. This task is read-only,
         but its OUTPUT (the derivation) is a hard input to Task 2's limiter number,
         so an executor that skips the measurement must be observable. This asserts
         the derivation artifact exists and carries an actual number. -->
  </verify>
  <done>
    A written derivation exists: backend budgets, replica count, observed cadence/duration
    (or a recorded zero-traffic finding), and the chosen n/window with the arithmetic —
    dated, sourced, ready to paste into the Task 2 docblock.
  </done>
</task>

<task type="auto">
  <name>Task 2: bridgeComputeLimiter — new named export + two one-token route swaps</name>
  <files>src/lib/ratelimit.ts, src/app/api/bridge/route.ts, src/app/api/portfolio-optimizer/route.ts, src/lib/seam-ratelimit-posture.invariant.test.ts</files>
  <action>
    1. `src/lib/ratelimit.ts`: add `export const bridgeComputeLimiter = makeLimiter(n,
       "<seconds> s");` with the family convention EXACTLY (PATTERNS Section 1): a `//`
       comment block immediately above, first line the literal form
       `// <n>/<unit> per <bucket subject> — Phase 163 SEC-04 bridge compute.`, then
       prose answering (a) real user cadence — from Task 1's measurement, cite the
       numbers, (b) why not piggybacked on `userActionLimiter` (300/h/user front over a
       10/h/tenant backend = untruthful Retry-After; the shared bucket also collides
       ~9 surfaces — copy `csvValidateLimiter`'s rhetorical structure at :195-206),
       (c) what abuse it caps. Window as a second-scale template literal ("3600 s"
       style, never "1 h").
    2. Swap the limiter argument at exactly two call sites — a ONE-TOKEN change each:
       - src/app/api/bridge/route.ts:94 `checkLimit(bridgeComputeLimiter, \`bridge:${user.id}\`)`
       - src/app/api/portfolio-optimizer/route.ts:113 (same swap; note :141-143 also
         reference the limiter for resetUsedTokens — keep that reference consistent with
         the new limiter so a refunded token goes back to the bucket it came from).
       Preserve the B15 limiter-ordering comment (consume token only after input
       validation). ⛔ Do NOT converge the deny bodies onto `rateLimitDenyJson` where the
       route hand-rolls its 429 — the ratelimit.ts:279-295 docblock enumerates six live
       429 contracts that would regress.
    3. ⛔ PROHIBITIONS (CONTEXT locked): `userActionLimiter` (src/lib/ratelimit.ts:97,
       5/60s) stays byte-identical. `src/app/api/scenario/optimize/route.ts` stays on
       `userActionLimiter` — its backend (`/optimize-weights` per-tenant floor, L-9) is a
       separately-booked item; folding it in here is scope creep.
    4. Move the roster pin IN THE SAME COMMIT:
       src/lib/seam-ratelimit-posture.invariant.test.ts:252 — the hand-typed roster
       ("the derived limiter population matches the hand-typed roster (a new seam route
       fails BY NAME)"). Update any deny-shape pins for the two routes that name the old
       limiter. Repo rule: limiter literal pins move in the same commit as the behavior.
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-ratelimit-posture.invariant.test.ts src/lib/ratelimit.test.ts src/app/api/bridge/route.test.ts src/app/api/portfolio-optimizer/route.test.ts</automated>
  </verify>
  <done>
    Both routes consume `bridgeComputeLimiter`; the docblock carries the measured
    derivation; `userActionLimiter` and scenario/optimize are untouched (git diff
    confirms); roster pin green in the same commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Behavioral 429 coverage + RED demo</name>
  <files>src/app/api/bridge/route.test.ts, src/app/api/portfolio-optimizer/route.test.ts</files>
  <behavior>
    - Test 1: with the limiter denying, each route returns its established 429 contract
      (code RATE_LIMITED, retryAfter present) — through the NEW limiter identity.
    - Test 2: misconfigured-limiter arm still returns the 503 SEAM_MISCONFIGURED shape
      (isRateLimitMisconfigured branch) — unchanged by the swap.
  </behavior>
  <action>
    Add/extend the two route tests so the deny path is exercised against
    `bridgeComputeLimiter` by identity (mock the limiter module and assert `checkLimit`
    received the `bridgeComputeLimiter` instance, or spy on it — the identity assertion
    is what makes the swap falsifiable at the route level).

    RED demo (document in the test docstring): neuter by reverting one route to
    `userActionLimiter` — the roster pin fails BY NAME (structural gate) AND the identity
    assertion fails (behavioral). Observe both, restore. Full-suite note: contract tests
    scan all of src/ — clear any reds with a full run, not file-scoped runs.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/bridge/route.test.ts src/app/api/portfolio-optimizer/route.test.ts src/__tests__/audit-coverage.test.ts</automated>
  </verify>
  <done>
    Deny-shape and identity assertions green; RED demo recorded; full vitest suite green
    at plan end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed client -> compute-heavy Python backend | expensive weighted-covariance / bridge computation triggered per request |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-04 | Denial of Service | bridge + optimizer front doors on the 300/h shared bucket | high | mitigate | named bridgeComputeLimiter sized to measured backend budget |
| T-163-05 | Denial of Service | shared userActionLimiter collateral (9 surfaces) if resized | high | mitigate | prohibition enforced: new named limiter, shared bucket untouched, diff-verified |
</threat_model>

<verification>
- Full vitest suite green (CI is Node 22 vs local Node 25 — a CI-only red is reproducible with the Node 22 PATH, not a flake).
- The measurement derivation appears verbatim in the limiter docblock and the SUMMARY.
</verification>

<success_criteria>
- SC-5's limiter half closed: named limiter, measured size, shared bucket untouched, roster pin moved atomically, deny path behaviorally falsifiable.
</success_criteria>

<output>
Create `.planning/phases/163-harden-fail-safe-closed-and-loud/163-02-SUMMARY.md` when done.
⚠️ No local absolute paths, usernames, credentials, or PROD identifiers in the SUMMARY —
the 163-03 no-allowlist scanner will scan it, and the repo is public.
</output>
