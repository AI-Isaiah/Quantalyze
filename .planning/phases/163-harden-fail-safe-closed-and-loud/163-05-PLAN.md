---
phase: 163-harden-fail-safe-closed-and-loud
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/strategies/csv-finalize/route.ts
  - src/app/api/preferences/route.ts
  - src/app/api/account/deletion-request/route.ts
  - src/app/api/strategies/csv-finalize/route.test.ts
  - src/app/api/preferences/route.test.ts
  - src/app/api/account/deletion-request/route.test.ts
  - src/lib/observability.ts
  - src/__tests__/observ12-fixtures-presence.test.ts
  - src/app/api/cron/flag-monitor/route.ts
  - src/app/api/cron/flag-monitor/route.test.ts
  - tests/integration/cron-flag-monitor.test.ts
autonomous: true
requirements: [OPS-06, OPS-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No request handler constructs the admin client AFTER an irreversible commit — a missing-env throw is loud AND pre-commit (500 with nothing landed), never a 500 over landed work"
    - "checkStuckNotifications answers 'nothing stuck' and 'could not tell' with DIFFERENT values — zero no longer means both"
    - "A persistent denominator read failure in the flag-monitor cron produces a NON-200 (the run registers failed in Vercel cron history) plus a Sentry capture — it can no longer log success"
    - "The integration test falsifies both monitor behaviors — it exercises read-error, count:null and count:NaN, and goes RED when the fix is reverted"
  artifacts:
    - "Four hoisted call sites across three route files (research measured FOUR occurrences, not the roadmap's three)"
    - "src/lib/observability.ts — discriminated-union return, presence pin updated in the same commit"
    - "tests/integration/cron-flag-monitor.test.ts — falsifier cases added; the orphaned auditLogRows option exercised or removed"
  key_links:
    - "hoisted `const admin = createAdminClient()` above the commit -> logAuditEventAsUser(admin, ...) after"
    - "flag-monitor terminal arms -> non-200 NextResponse status -> Vercel cron failure signal"
---

<objective>
Close OPS-06 and OPS-07 (SC-2): failure paths are honest. `createAdminClient()` throws
synchronously on missing env (src/lib/supabase/admin.ts:11-17) and
`logAuditEventAsUser(createAdminClient(), ...)` evaluates the argument BEFORE the call —
outside every catch — so a post-commit throw 500s a request whose work already landed.
Research resolved the roadmap's "three known sites" to FOUR occurrences in THREE files:
plan against the measured four. The flag monitor's two dishonesty arms (indistinguishable
zero; 200-on-failed-denominator) get fixed AND made falsifiable — the current integration
test's `auditLogRows` option is passed by NO test (confirmed vacuous, the 141.2 record).

Purpose: committed work cannot 500; monitors cannot report false health.
Output: four hoists, a discriminated union, paging terminal arms, real falsifiers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-CONTEXT.md
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-RESEARCH.md  (Section 2)
@src/lib/supabase/admin.ts
@src/lib/observability.ts
@src/app/api/cron/flag-monitor/route.ts  (:294-333 getDenominator)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hoist createAdminClient above the irreversible commit — four occurrences, three files</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/app/api/preferences/route.ts, src/app/api/account/deletion-request/route.ts, src/app/api/strategies/csv-finalize/route.test.ts, src/app/api/preferences/route.test.ts, src/app/api/account/deletion-request/route.test.ts</files>
  <behavior>
    - Per route: when createAdminClient throws (env mocked absent), the handler 500s
      BEFORE the commit RPC/INSERT is invoked (spy asserts the commit was never called)
      — fail-safe: loud, nothing landed.
    - Per route: with a working client whose audit emission fails, the handler's response
      is unchanged (csv-finalize's own docblock promise at ~:790-793: "a failed emission
      ... must NOT change this response").
  </behavior>
  <action>
    At each site, hoist `const admin = createAdminClient()` ABOVE the irreversible commit
    in that handler and pass `admin` to the emit call(s). The four measured occurrences
    (anchor on the call shape `logAuditEventAsUser(createAdminClient(),` — the files
    move; line numbers are 2026-08-26 references):
    1. csv-finalize/route.ts:798 — after the fold committed a new strategy (the WR-01
       headline site). Note its two `after()`-epilogue admin usages (~:1851-1852,
       :1967-1968) are already try/catch-wrapped dynamic imports — OUT of scope, leave.
    2. preferences/route.ts:289 — after `update_allocator_mandates` RPC succeeded.
    3. preferences/route.ts:221 — the RPC ERROR branch (nothing committed, but a throw
       here converts a classified error response into an opaque 500) — same hoist,
       same pass.
    4. deletion-request/route.ts:141 — after the GDPR Art. 17 intake row INSERTed.

    ⛔ CONTEXT locks: NO `createAdminClientOrNull` variant (a quiet failure is the exact
    anti-pattern this phase closes); NO class-wide source-scan gate (deferred — 182 call
    sites, false-positive-prone; revisit if a fourth FILE appears). The defect is
    sequencing, not error handling — do not add try/catch around the constructor.

    Tests: add the behavior cases above to each route's existing test file. RED demo per
    route, named in the docstring: neuter by moving the constructor back below the commit
    -> the pre-commit assertion fails; restore.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/csv-finalize/route.test.ts src/app/api/preferences/route.test.ts src/app/api/account/deletion-request/route.test.ts</automated>
  </verify>
  <done>
    All four occurrences hoisted; env-throw is provably pre-commit at each route;
    responses unchanged on emit failure; RED demos recorded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: checkStuckNotifications — "nothing stuck" is not "could not tell"</name>
  <files>src/lib/observability.ts, src/__tests__/observ12-fixtures-presence.test.ts</files>
  <behavior>
    - Read error -> `{ kind: "indeterminate", error: <message> }` (never `{ stuck: 0 }`).
    - `count: null` (and non-finite counts) -> indeterminate — `count ?? 0` re-imported
      the same collapse and goes.
    - Successful read -> `{ kind: "ok", stuck: n }`.
  </behavior>
  <action>
    Rework `checkStuckNotifications` (src/lib/observability.ts:22-27) to return a
    discriminated union mirroring the flag-monitor's `DenominatorResult` shape, so the
    repo has ONE idiom for "zero vs unknown" (research discretion recommendation,
    accepted). No runtime caller exists at HEAD — the only reference is the
    export-presence pin at src/__tests__/observ12-fixtures-presence.test.ts:53-57 —
    so the return-type change is free; update that pin IN THE SAME COMMIT. Contract-only:
    do NOT wire a new cron/admin consumer (research open question 5 — wiring is new
    scope, declined). Add unit cases for all three arms in the same commit; RED demo:
    neuter by collapsing the error arm back to a zero-count success value -> the
    read-error case fails; restore, record in the docstring.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/observ12-fixtures-presence.test.ts src/lib/observability.test.ts</automated>
  </verify>
  <done>Union shape live, presence pin moved atomically, three arms covered, RED demo recorded. (If no observability unit test file exists yet, create it beside the lib file.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The denominator read PAGES — and the integration test can actually fail</name>
  <files>src/app/api/cron/flag-monitor/route.ts, src/app/api/cron/flag-monitor/route.test.ts, tests/integration/cron-flag-monitor.test.ts</files>
  <behavior>
    - Both `getDenominator` terminal arms (read error at :308-312; unusable count at
      :325-329) return a NON-200 status (503) so the Vercel cron run registers FAILED —
      that IS the page channel (Architectural Responsibility Map) — plus a Sentry capture
      on the terminal path (research open question 1 recommendation: non-200 + Sentry;
      email stays with the existing streak machinery).
    - Integration falsifiers: a denominator read ERROR run, a `count: null` run, and a
      `count: NaN` run each end non-200 with reason `denominator_read_failed` — and each
      is demonstrated RED by reverting the status to the default 200.
  </behavior>
  <action>
    1. Fix the two terminal arms: `NextResponse.json({ ok: false, reason:
       "denominator_read_failed" }, { status: 503 })` (plain NextResponse — this route is
       not on an exotic runtime), plus `captureToSentry`/equivalent per the repo's
       existing capture idiom. The Vercel-cron-history semantics change (green -> failed
       on persistent read failure) was priced in when the TODOS entry was filed — say so
       in the route comment, citing 141.2's lesson ("the remedy replaced a wrong page
       with no page").
    2. Make tests/integration/cron-flag-monitor.test.ts able to fail: the harness's
       `auditLogRows` option (:70-79) is currently passed by NO test — either exercise it
       in the new cases or delete it (an option no test passes is dead weight that reads
       as coverage). Add the three falsifier runs from <behavior>. Under BOTH prior
       denominator mutations the old file stayed green — the new cases must kill both.
    3. RED demos, named in docstrings: (a) revert terminal status to 200 -> paging cases
       fail; (b) restore. Note: file-scoped runs cannot clear the contract tests — finish
       with a full-suite run.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/cron/flag-monitor/route.test.ts tests/integration/cron-flag-monitor.test.ts</automated>
  </verify>
  <done>
    Terminal arms page (non-200 + Sentry); integration test exercises read-error /
    null / NaN and is proven RED under the revert; the vacuous option is exercised or
    removed; full vitest suite green at plan end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request handler -> DB commit -> response | work lands before the response is written; anything thrown in between lies to the caller |
| cron route -> Vercel cron history | the status code IS the monitoring signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-11 | Denial of Service (false 500) | post-commit createAdminClient throw at 4 sites | high | mitigate | hoist above commit; pre-commit throw proven by spy |
| T-163-12 | Repudiation (false health) | monitor collapsing indeterminate into zero / 200-on-failure | high | mitigate | discriminated union + non-200 terminal arms + Sentry |
| T-163-13 | Tampering | integration-test vacuity (option passed by no test) | medium | mitigate | falsifier cases with recorded RED demos |
</threat_model>

<verification>
- Full vitest suite green; the three RED demos recorded in docstrings.
- No `createAdminClientOrNull` symbol exists anywhere (git grep count 0 — it was never created).
</verification>

<success_criteria>
- SC-2 closed at the measured four occurrences; monitor distinguishes its states and pages on failure; the integration test can fail on exactly the arms it exists to guard.
</success_criteria>

<output>
Create `.planning/phases/163-harden-fail-safe-closed-and-loud/163-05-SUMMARY.md` when done.
⚠️ No local absolute paths or usernames in the SUMMARY (163-03's scanner will scan it).
</output>
