---
phase: 163-harden-fail-safe-closed-and-loud
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - "94 tracked files carrying the token — 87 under .planning/ (union of username- and \/Users\/-bearing), 5 under docs/, 2 applied Supabase migration headers. ⚠️ NOT 87: that is the .planning/-only figure. Re-measure live before scrubbing."
  - scripts/check-planning-hygiene.ts
  - package.json
  - .planning/REQUIREMENTS.md
  - supabase/migrations/20260517013000_revoke_probe_oracle_assert_strategy_visible_to_allocator.sql
  - supabase/migrations/20260517013100_sanitize_user_recipient_email_case_insensitive.sql
autonomous: true
requirements: [SEC-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Zero tracked files carry the macOS username or a local absolute home path; the scan that proves it has NO path allowlist"
    - "`npm run lint` fails on any new occurrence, locally and in CI (frontend-lint is in the `frontend` aggregator)"
    - "The gate reads files as TEXT (NUL-safe) — it is not blind to src/lib/wizardErrors.test.ts"
    - "The two applied-migration comment edits are a RECORDED exception, and the founder-locked limits (forward-only, no history rewrite, severity = metadata not credentials) are stated in the requirement"
  artifacts:
    - "scripts/check-planning-hygiene.ts — no-allowlist scanner with encoded needle + structural patterns, wired into `npm run lint`"
    - ".planning/REQUIREMENTS.md — SEC-02 entry carries the recorded decisions"
  key_links:
    - "package.json lint chain -> scripts/check-planning-hygiene.ts (exit 1 on violation) -> frontend-lint CI job -> frontend aggregator"
---

<objective>
Close SEC-02: scrub the macOS username and local absolute paths from every tracked file,
then stand up a NEW no-allowlist scan that fails the build on any recurrence. Forward-only
redaction (founder-locked — no history rewrite); replacement token `<user>`; severity is
metadata, not credentials, and the requirement must say so.

Research extended the scope beyond the ROADMAP: the token exists in 7 tracked files OUTSIDE
`.planning/` (5 under docs/, 2 in APPLIED Supabase migration header comments). A genuinely
no-allowlist gate must scan the whole tracked tree, so those 7 are scrubbed too — with the
applied-migration comment-edit exception explicitly recorded (CONTEXT recommendation 1).

Purpose: stop new leakage of local-identity metadata on a public repo, with a gate that is
structurally unable to reproduce the gitleaks path-allowlist blindness.
Output: clean tree, scanner script, lint wiring, recorded decisions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-CONTEXT.md
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-RESEARCH.md  (Section 4b)
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-PATTERNS.md  (Section 2)
@scripts/check-route-contract.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure, verify the migration-edit precondition, scrub all tracked files</name>
  <files>94 tracked files (live count; 87 .planning/ + 7 outside), incl. the two applied migration headers</files>
  <precondition>The Supabase CLI reconciles applied migrations by VERSION, not content hash (RESEARCH assumption A2) — verify via `supabase migration list` behavior/docs BEFORE editing the two applied migration files; if reconciliation is content-hashed, HALT this task and surface (the comment edit would break `migration list`).</precondition>
  <action>
    1. PRE-EDIT token counts (the anti-laundering record): re-measure at HEAD with a
       NUL-safe method (node fs + includes, or grep with binary-as-text semantics —
       plain grep silently skips src/lib/wizardErrors.test.ts after its deliberate NUL
       at line 1572 and exit 1 reads as clean). Reference counts from RESEARCH
       (2026-08-26): 94 username/path-bearing tracked files total (80 in .planning/ + 5 docs/
       + 2 migrations); 59 `.planning/` files with absolute home paths (drifted +2 from
       CONTEXT's 57 — plan against the LIVE number). Record the live counts in the
       SUMMARY before any edit.
    2. Scrub every occurrence:
       - The username itself -> the literal token `<user>` (founder-locked replacement —
         greppable, obviously a placeholder).
       - Local absolute home paths -> prefer rewriting to repo-relative paths where the
         path points into the repo; otherwise substitute the username segment with
         `<user>`. Also scrub the dash-mangled scratchpad form of the home path (the
         scanner in Task 2 matches it structurally).
       - The two applied migrations: comment line 5 of each (an absolute artifact path)
         — COMMENT-ONLY edits, zero SQL bytes change. In the same edit, append one
         header line to each recording the exception: edited 2026-08 under Phase 163
         SEC-02, comment-only scrub of local-path metadata; applied-migration content is
         otherwise inviolate (migration-reviewer rule 11).
       Use scripted replacement (node or sed per file) but review the diff — do not
       regex-replace inside binary-ish fixtures blindly; the NUL-carrying test file must
       be handled with a NUL-safe tool if it matches.
    3. Verify by re-running the same NUL-safe count: 0 files.
       ⛔ STAGE THE ENUMERATED SCRUB LIST ONLY — never `git add .planning/` or any
       directory. Seven sibling Wave-1 agents write their SUMMARYs into the same
       tracked `.planning/` tree through a SHARED GIT INDEX; a directory-wide add
       stages their in-flight work into this plan's commit and corrupts the wave.
       Write the measured paths to a file in step 1 and stage exactly those:
         `git add --pathspec-from-file=<the-enumerated-list>`
       `.planning/` stays tracked (standing rule) — this bounds WHAT you stage,
       not whether planning is committed.
  </action>
  <verify>
    <automated>node -e 'const{execSync}=require("child_process");const fs=require("fs");const N=String.fromCharCode(47,85,115,101,114,115,47);const U=require("path").basename(require("os").homedir());let bad=[];for(const f of execSync("git ls-files -z").toString().split("\0").filter(Boolean)){if(f==="scripts/check-planning-hygiene.ts")continue;let t;try{t=fs.readFileSync(f,"latin1")}catch{continue}if(t.includes(N)||t.includes(U))bad.push(f)}if(bad.length){console.error("LEAKS:",bad.length,bad.slice(0,10));process.exit(1)}console.log("clean: 0 of",execSync("git ls-files").toString().trim().split("\n").length,"tracked files")' && npx vitest run src/lib/wizardErrors.test.ts</automated>
    <!-- ⛔ NOT `git grep -I`. -I skips binary-classified files, and
         src/lib/wizardErrors.test.ts carries a deliberate NUL at line 1572 — the
         exact recorded blind spot. A missed occurrence there reads as a clean
         pass. This reads every tracked file as latin1 so NUL bytes cannot hide a
         match, and checks BOTH the path prefix and the username token (checking
         only the path prefix misses a bare username occurrence). Needle is
         encoded so the verify command is not itself a hit. -->
  </verify>
  <done>
    Live pre-edit counts recorded; zero occurrences remain by the NUL-safe count; the
    wizardErrors suite still passes (proving the NUL fixture survived the scrub
    untouched); the two migration headers carry the recorded exception.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The no-allowlist scanner, wired into `npm run lint`</name>
  <files>scripts/check-planning-hygiene.ts, package.json</files>
  <behavior>
    - Green run: prints a COUNT of files scanned (a walk that found 0 files must not read
      as OK — the check-route-contract.ts anti-vacuity habit).
    - RED demo: reintroducing one occurrence in a scratch tracked file makes
      `npm run lint` exit 1 with an uppercase violation code + fix instruction.
    - NUL-safety: a file containing a NUL byte is still scanned to its end.
  </behavior>
  <action>
    Build `scripts/check-planning-hygiene.ts` on the `scripts/check-route-contract.ts`
    skeleton (PATTERNS Section 2): shebang, docblock with numbered Rules enforced / Exit
    codes / Invocation, pure `node:fs` walk of `git ls-files` output (tracked files
    only), violations as `CODE: file — sentence` strings, `main()` with exit 1 and a
    success line printing the scanned-file count, import-guard tail so tests can import
    `runCheck`.

    Needles — three classes, ALL matched on every tracked file, NO path allowlist:
    1. The username literal: stored ENCODED (base64 or char codes) with a comment saying
       why — a scanner whose source contains the literal fails its own scan. Decode at
       runtime, match with `includes` case-insensitively.
    2. Structural absolute-home-path patterns, spelled ESCAPED in source (unescaped they
       self-match): the home-directory form `\/Users\/[^\/\s]+\/` and the dash-mangled
       scratchpad form `\-Users\-[A-Za-z0-9-]+` — so a future absolute path with a
       DIFFERENT username still fails. Exempt ONLY the placeholder segment `<user>` by
       VALUE (a placeholder is definitionally non-identifying); ⛔ never exempt by path —
       a path carve-out reproduces the gitleaks blindness this gate exists to fix
       (CONTEXT lock).
    3. Read files with `node:fs` `readFileSync` as utf8 text — NUL bytes must not
       truncate the scan (the wizardErrors.test.ts blind spot). Skip only
       content-addressed binaries by extension sniff on raw bytes if needed, never by
       directory.

    Wiring (package.json:11,18-19 shape): add `"check:planning-hygiene": "tsx
    scripts/check-planning-hygiene.ts"` AND chain `&& tsx
    scripts/check-planning-hygiene.ts` onto `lint` — lint runs in the `frontend-lint` CI
    job, which is already in the `frontend` aggregator's needs: list, so NO workflow-file
    change is needed (and the loop/needs two-place registration hazard is avoided
    entirely). ⛔ Do not attach anything to the `secret-scan` job — not in the
    aggregator, already red on workflow_dispatch.

    RED demo (record in the script docblock AND the SUMMARY): create a scratch tracked
    file containing one raw structural occurrence, run `npm run lint`, observe exit 1
    with the violation line, delete the scratch file, observe green. Name the exact
    mutation and direction.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    Lint chain runs the scanner; green prints the scanned count; the RED demo is recorded;
    the scanner's own source passes its own scan.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the decisions in the requirement</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Extend the SEC-02 entry (do not rewrite the file — scoped edit) so the requirement
    itself carries: (1) forward-only redaction, founder-declined history rewrite and WHY
    (published/cloneable history; force-push cost); (2) severity framing — the token is
    metadata, not credentials, do not inflate it (CONTEXT lock); (3) the scope extension
    to 7 files outside .planning/ with the applied-migration COMMENT-ONLY exception,
    recorded as the deliberate deviation from the no-edit rule, cross-referencing the
    exception lines added in the two migration headers; (4) the gate's no-allowlist
    construction (value-exemption for the placeholder only, never path).
    Check the box only if the verifier flow does so elsewhere — otherwise leave status
    to the phase verifier.
  </action>
  <verify>
    <automated>grep -c "forward-only" .planning/REQUIREMENTS.md | grep -qvx "0" && npm run lint</automated>
  </verify>
  <done>SEC-02's entry records all four decisions; lint (including the new scanner) is green over the edited file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked repo -> public GitHub | every committed byte is world-readable; .planning/ is tracked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-06 | Information Disclosure | local-identity metadata in tracked docs | medium | mitigate | full-tree scrub + no-allowlist lint gate |
| T-163-07 | Information Disclosure | git history still holds old occurrences | low | accept | founder-locked forward-only; recorded in the requirement |
| T-163-08 | Tampering | scanner blind spots (NUL byte, path allowlist, self-match) | medium | mitigate | NUL-safe text reads, value-only placeholder exemption, encoded needle |
</threat_model>

<verification>
- `npm run lint` green at HEAD; RED demo recorded with pre-edit token counts (87 / live re-measure) on the record BEFORE the scrub.
- ⚠️ Concurrent wave-1 plans write SUMMARYs while this plan runs; at phase gate, re-run the scanner over the merged tree and scrub any phase-163 artifact that trips it — that is the gate working, not a defect.
</verification>

<success_criteria>
- SC-4's scrub half closed: zero occurrences, no-allowlist gate live in lint + CI, decisions recorded where the CONTEXT demanded them.
</success_criteria>

<output>
Create `.planning/phases/163-harden-fail-safe-closed-and-loud/163-03-SUMMARY.md` when done.
⚠️ The SUMMARY must itself pass the new scanner: repo-relative paths only, placeholder
token only, structural patterns spelled escaped.
</output>
