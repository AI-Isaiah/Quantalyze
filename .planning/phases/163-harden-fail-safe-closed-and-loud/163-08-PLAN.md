---
phase: 163-harden-fail-safe-closed-and-loud
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resilient-fetch.ts
  - src/lib/resilient-fetch.retry.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx
autonomous: true
requirements: [OPS-10, SEC-06]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The seam retry loop's counting-status arm cancels the abandoned Response body before retrying — undici stops buffering it until the attempt signal fires"
    - "A fixture Response with no `body` member degrades silently — the cancel is capability-checked, never a throw inside the classification window"
    - "Removing a wizard panel mid-validate aborts that panel's in-flight credential-carrying POST, resolved by panel IDENTITY (never index), with abort reason 'user'"
    - "The comment that recorded the removal gap as deliberate is rewritten — it no longer states the opposite of the code"
  artifacts:
    - "src/lib/resilient-fetch.ts — capability-checked body cancel on the one abandoning continue"
    - "MultiKeyConnectStep.tsx — doRemove aborts via the id-keyed controller map and cleans both ref maps"
  key_links:
    - "counting-status retry arm (:2688-2692) -> res.body?.cancel() -> continue"
    - "doRemove -> panelsRef identity lookup -> abortReasonsRef 'user' BEFORE abortControllersRef abort -> map cleanup -> panel filter"
---

<objective>
Close OPS-10 and SEC-06 — the TS client/lib pair. The seam retry loop abandons exactly ONE
real Response without consuming it (the counting-status arm, resilient-fetch.ts:2688-2692;
`readDependencyBody` reads only a clone, and only for 503), so undici buffers the original
body until the attempt signal fires. And `doRemove` in the wizard's multi-key connect step
(MultiKeyConnectStep.tsx:1040-1043) drops a panel without aborting its in-flight
credential-carrying POST — an abort path that already exists for the deadline tick and
"Stop waiting" exits.

Purpose: bounded plumbing; a credential POST never outlives the UI context that owns it.
Output: capability-checked cancel + spy falsifier; identity-keyed abort in doRemove +
rewritten comment + falsifier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-CONTEXT.md
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-RESEARCH.md  (Sections 3 OPS-10, 4d)
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-PATTERNS.md  (Section 6)
@src/lib/resilient-fetch.ts  (:2450-2700)
@src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx  (:860-1250)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: body.cancel() on the one abandoning retry arm — capability-checked</name>
  <files>src/lib/resilient-fetch.ts, src/lib/resilient-fetch.retry.test.ts</files>
  <behavior>
    - A counting-status response driven through the retry arm has its ORIGINAL body
      cancelled (spy on `body.cancel`) before `continue`.
    - A bare `{ ok, status }` fixture literal with NO `body` member passes through the
      same arm without throwing.
    - MUTATION (docstring-named): deleting the cancel call -> the spy case RED.
  </behavior>
  <action>
    Add the cancel immediately before the counting-status `continue`
    (resilient-fetch.ts:2688-2692): shape `res.body?.cancel().catch(() => {})` — or an
    equivalent typeof guard — because `SeamResponse` is deliberately structural and
    several fixtures are bare `{ ok, status }` literals with no `body` at all (the same
    reason `hasContractualWait` checks `headers.get`, see its docblock ~:2178+). A missing
    or non-function `body.cancel` must degrade, never throw inside the classification
    window; a rejected cancel promise must be swallowed.

    Scope check (research-verified, re-confirm at HEAD): the transport-arm `continue`
    (~:2614) follows a THROWN fetch — no Response exists; the breaker pre-check
    continues (:1466, :1470) run before fetch. No other site abandons a real Response —
    this is a ONE-SITE fix; do not sprinkle cancels elsewhere.

    Test: extend src/lib/resilient-fetch.retry.test.ts with a stubbed Response whose
    `body.cancel` is a spy, driven through a counting-status retry, plus the bare-literal
    degradation case. Record the RED demo in the docstring.
  </action>
  <verify>
    <automated>npx vitest run src/lib/resilient-fetch.retry.test.ts src/lib/resilient-fetch.test.ts src/lib/resilient-fetch.wiring.test.ts</automated>
  </verify>
  <done>Cancel wired at the single abandoning site, capability-checked; spy + degradation cases green; RED demo recorded; all three resilient-fetch suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Panel removal aborts the in-flight credential POST — by identity, reason "user"</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx</files>
  <behavior>
    - Start a validate (mock fetch pending), remove that panel: the fetch's
      AbortSignal is aborted, and the recorded abort reason for that panel id is "user"
      (never "deadline" — an AbortError is ONE rejection with two opposite meanings, per
      the :869-875 invariant).
    - Removing a DIFFERENT panel while one validates leaves the validating panel's
      request un-aborted (the identity invariant: `onMove` reorders panels, so an
      index-keyed abort would cancel a sibling's credential-carrying POST — :860-868).
    - After removal, the removed panel's entries are deleted from both
      `abortControllersRef` and `abortReasonsRef` (the validate's `finally` at :1420
      never runs for an unmounted panel — today nothing cleans the maps).
    - MUTATION (docstring-named): removing the abort call from doRemove -> the first
      case RED.
  </behavior>
  <action>
    1. Rework `doRemove` (:1040-1043) on the `handleStopWaiting` analog (:1167-1173,
       SAME file): resolve `const p = panelsRef.current[idx]`, guard `if (!p) return;`,
       set `abortReasonsRef.current.set(p.id, "user")` BEFORE aborting, abort via
       `abortControllersRef.current.get(p.id)?.abort()`, then DELETE the panel's entries
       from both ref maps, then run the existing announcement + filter. ⛔ Never key the
       abort by index.
    2. Rewrite the :1104-1114 comment IN THE SAME EDIT — it currently records the gap as
       deliberate ("Left as-is on purpose ... Logged in TODOS.md") and after this fix it
       states the opposite of the code (the exact doc-drift class the repo tracks).
       House style: quote the superseded sentence and refute it, citing Phase 163
       SEC-06 as the override. Also update the :905-907 docblock sentence that names the
       deferral.
    3. Honest-copy caveat STAYS: aborting stops the browser listening, not the server
       working — the server may still store the key (the :1145-1165 CR-02 lesson), and
       neither connect route reads `request.signal` (re-verified; server-side
       cancellation is a recorded non-goal owned by another phase — restate that boundary
       in the comment, do not extend scope). Do not add any copy claiming nothing was
       saved.
    4. Extend MultiKeyConnectStep.test.tsx with the behavior cases. ⚠️ This suite has an
       order-sensitivity history (OPS-11: unrestored vi.stubGlobal/vi.mock) — restore all
       stubs/mocks in afterEach. Record the RED demo in the docstring.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx"</automated>
  </verify>
  <done>
    doRemove aborts by identity with reason "user" and cleans both maps; sibling panels
    unaffected; both comments truthful; RED demo recorded; full vitest suite green at
    plan end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser wizard -> connect API | POST body carries api_key / api_secret / passphrase |
| seam retry loop -> upstream response | abandoned bodies hold undici buffers on the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-20 | Information Disclosure | credential POST outliving its removed UI panel | medium | mitigate | identity-keyed client abort in doRemove (server request.signal handling stays a recorded non-goal) |
| T-163-21 | Denial of Service | unconsumed abandoned bodies buffering across retries | low | mitigate | capability-checked body cancel at the single abandoning site |
| T-163-22 | Repudiation | abort reason misclassification ("deadline" vs "user") polluting the seam funnel | low | mitigate | reason set to "user" BEFORE abort; asserted in the test |
</threat_model>

<verification>
- Full vitest suite green (file-scoped runs cannot clear the contract tests; finish with a full run).
- The two rewritten comments quote-and-refute their superseded claims.
</verification>

<success_criteria>
- OPS-10: the retry loop cancels abandoned bodies without breaking structural fixtures. SEC-06: removal aborts the in-flight credential POST by identity — both proven RED under their named neuters.
</success_criteria>

<output>
Create `.planning/phases/163-harden-fail-safe-closed-and-loud/163-08-SUMMARY.md` when done.
⚠️ No local absolute paths or usernames in the SUMMARY (163-03's scanner will scan it).
</output>
