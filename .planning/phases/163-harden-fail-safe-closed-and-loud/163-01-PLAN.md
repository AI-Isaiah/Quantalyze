---
phase: 163-harden-fail-safe-closed-and-loud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/main.py
  - analytics-service/main_worker.py
  - analytics-service/services/mt5_client.py
  - analytics-service/tests/test_structlog_frozen_proxy.py
autonomous: true
requirements: [OPS-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The worker process (`python -m main_worker`) emits every structlog and stdlib log line through the redacting chain — a denylisted value (HMAC signature, MT5 password) logged there renders as [REDACTED], not plaintext"
    - "The API process configures logging BEFORE any router import can emit, so no module-scope proxy can freeze the default (unredacted) chain"
    - "A module-scope `.bind()` on a `get_logger()` result anywhere in non-test analytics-service code fails pytest"
    - "Both gates have been observed RED under neutering and the mutation is named in each test's own docstring"
  artifacts:
    - "analytics-service/main_worker.py — calls `configure_logging()` at startup, before any loop or import-time emission"
    - "analytics-service/main.py — `configure_logging()` runs above the router imports"
    - "analytics-service/tests/test_structlog_frozen_proxy.py — Mode A source-scan (ast) + Mode B behavioral redaction test + negative control"
  key_links:
    - "main_worker.py startup -> services/logging_config.py configure_logging() -> _redact_processor + logging.setLogRecordFactory bridge"
    - "Mode B test -> subprocess/reset_defaults isolation -> real processor chain (never structlog.testing.capture_logs)"
---

<objective>
Close OPS-05: the structlog frozen-proxy class, at BOTH failure modes. This is the phase's
highest-risk item, planned first: research measured Mode B as LIVE ON THE WORKER —
`main_worker.py` never calls `configure_logging()`, so the process that actually runs ccxt
ingestion and MT5 sync emits through structlog's DEFAULT chain with no `_redact_processor`
and no stdlib `setLogRecordFactory` bridge. That is an active secret-exposure path for ccxt
HMAC signatures and MT5 passwords (mt5linux interpolates the password into remotely-eval'd
source, so exception TEXT is a credential surface — services/mt5_client.py:153-155).

Purpose: secrets cannot reach logs, in either process, ever again — and the gates that
guarantee it are proven able to fail.
Output: both entrypoints configured; one new pytest file with the Mode A scan, the Mode B
behavioral test, and a negative control; RED demos recorded in docstrings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-CONTEXT.md
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-RESEARCH.md  (Section 1)
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-PATTERNS.md  (Section 3)
@analytics-service/services/logging_config.py
@analytics-service/tests/test_logging_config.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Configure logging at BOTH entrypoints — the live worker leak first (Mode B)</name>
  <files>analytics-service/main_worker.py, analytics-service/main.py, analytics-service/services/mt5_client.py, analytics-service/tests/test_structlog_frozen_proxy.py</files>
  <action>
    Wire the ONE redaction chain end-to-end in both processes, then prove it behaviorally.

    1. `main_worker.py`: call `configure_logging()` (from `services.logging_config`) at
       startup, BEFORE any worker loop starts and before any module that logs at import
       time. Research verified this file currently has ZERO references to
       `configure_logging`/`structlog`, and the Railway worker service runs
       `python -m main_worker` (Dockerfile header, analytics-service/Dockerfile:1-8) —
       this is the process running ccxt long-fetch and MT5 sync. Place the call as early
       as possible in the entrypoint; mirror `main.py`'s call shape.
    2. `main.py`: move `configure_logging()` (currently at main.py:66) ABOVE the router
       imports at main.py:44-45. The API process is safe today only by the absence of
       import-time emission — nothing pins that. After this change, no import-time first
       use can freeze the default chain (structlog `cache_logger_on_first_use=True` makes
       first-use-before-configure permanent — the `BoundLoggerLazyProxy.bind`
       finalized-bind closure never sees later `structlog.configure()` calls).
    3. `services/mt5_client.py` `_stage_logger()` docstring (~:162-176): its sentence
       "main.py configures inside the lifespan, long after import" is STALE (configure
       runs at module scope). Rewrite it to state the current reality: config runs at
       module scope in main.py and at startup in main_worker.py; the per-call bind is
       kept as belt-and-braces. Per house style, quote-and-refute the wrong sentence
       rather than silently deleting it.
    4. Mode B behavioral test, in NEW file
       `analytics-service/tests/test_structlog_frozen_proxy.py`:
       - Follow the house idiom from tests/test_logging_config.py:60-123 —
         `configure_logging()` + `capsys`, parse the rendered JSON line, assert a
         denylisted key (e.g. `api_key`, or `signature` from services/redact.py
         DENYLIST_EXACT) renders as the redaction marker while a safe field survives.
       - The test must reproduce the FROZEN-PROXY sequence: obtain a module-scope-style
         lazy proxy and EMIT (or bind) through it BEFORE `configure_logging()` runs, then
         configure, then assert emission is redacted. Because structlog config is
         process-global and freezing is permanent within a process, run this in a
         SUBPROCESS (`sys.executable -c` or a pytest subprocess helper) or with
         `structlog.reset_defaults()` isolation so it cannot poison sibling tests. The
         subprocess variant is preferred: it can import `main_worker` (or execute the same
         configure-first ordering) and prove the entrypoint ordering itself, which is the
         thing this task changes.
       - Also assert the stdlib bridge: after configuration, a stdlib
         `logging.getLogger(...).warning("ccxt: %s", "<denylisted value>")` line is
         scrubbed (the LogRecord factory at logging_config.py:242-246 exists precisely
         for `routers/exchange.py:75`'s logger).
       - ⛔ Do NOT use `structlog.testing.capture_logs` — it replaces the processor chain
         so `_redact_processor` never runs and the assertion cannot fail
         (PATTERNS Section 3 records this exact vacuity trap).
    5. RED demo (record in the test docstring, house idiom PATTERNS "Shared Patterns (a)"):
       neuter by reverting the `main_worker.py` configure call (comment it out locally),
       observe the behavioral test go RED, restore. Name the exact mutation and the
       observed direction in the docstring. Take a byte backup before the neuter edit;
       confirm the restore by grepping for the configure call, not by hash alone.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_structlog_frozen_proxy.py tests/test_logging_config.py tests/test_stdlib_redact_bridge.py -x</automated>
  </verify>
  <done>
    main_worker.py configures logging at startup; main.py configures before router
    imports; the behavioral test proves a pre-configure emission is redacted in the fixed
    ordering and its docstring names the neuter mutation observed RED; the mt5_client
    docstring no longer contains the stale claim; existing logging/redaction suites stay
    green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mode A source-scan gate — module-scope .bind() is a pytest failure</name>
  <files>analytics-service/tests/test_structlog_frozen_proxy.py</files>
  <behavior>
    - Test 1: an ast walk over every non-test `.py` under analytics-service finds ZERO
      top-level assignments whose value is a `.bind(...)` call on a `get_logger(...)`
      result. Pre-edit gate token (measured 2026-08-26, recorded in RESEARCH Section 1
      BEFORE any edit): expected count = 0.
    - Test 2 (anti-vacuity floor): the walk visited a MINIMUM corpus size (e.g. >= 20
      files) so an empty walk cannot read as a pass — copy
      `test_the_walk_is_not_vacuous` from tests/test_limiter_route_coverage.py:362-390.
    - Test 3 (negative control): a sibling test in the same file, following
      tests/test_logging_config.py:94-123, monkeypatches the scrubber broken and asserts
      the UNREDACTED value survives — proving the Task 1 assertion is load-bearing.
  </behavior>
  <action>
    Add the Mode A gate to `tests/test_structlog_frozen_proxy.py`.

    - Use the ast idiom from tests/test_redact.py:266-300 (an anchored regex sanity check
      first, then `ast.parse` + walk as the real assertion) — ast, not grep, so docstring
      prose mentioning bind cannot false-positive. Walk `tree.body` (module top level
      only) for `ast.Assign`/`ast.AnnAssign` whose value is an `ast.Call` with `func` an
      `ast.Attribute` named `bind` — the shape that freezes the processor chain before
      `configure_logging()` (structlog docs: never call `new()`/`bind()` at module scope).
    - Scope: all non-test `.py` files under `analytics-service/` (exclude `tests/`).
      Known-safe module-scope lazy proxies (unbound) exist at main.py:161,380,473,658,
      routers/debug_key_flow.py:41, routers/process_key.py:67, services/rate_limit.py:139,
      services/ingestion/long_fetch.py:45 — the gate must PASS on all of these (they only
      become dangerous when bound or used pre-configure; Task 1 closed the latter).
    - This gate is PREVENTIVE (0 occurrences at HEAD). Per the hard rule, it MUST still
      be proven RED: temporarily introduce a scratch violation (a top-level
      `_frozen = get_logger("scratch").bind(x=1)` in a non-test module), observe the gate
      fail, remove the violation. Document the mutation and observed RED in the test's
      own docstring.
    - Run `mypy --strict --follow-imports=silent services/ routers/ models/` from
      analytics-service/ if any non-test source was touched (Task 1 touched main.py /
      main_worker.py — those are outside the mypy CI scope, but run it anyway to be safe
      before ship).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_structlog_frozen_proxy.py -x</automated>
  </verify>
  <done>
    The Mode A scan asserts zero module-scope binds with a corpus-size floor and a
    negative control; its docstring records the scratch-violation RED demo; full
    analytics-service pytest run is green from the analytics-service/ directory.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process -> log sink | exception text carrying ccxt HMAC signatures / MT5 passwords crosses into Railway logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-01 | Information Disclosure | main_worker.py default structlog chain (no _redact_processor, no LogRecord factory) | critical | mitigate | configure_logging() at worker startup (Task 1) + Mode B behavioral test |
| T-163-02 | Information Disclosure | future module-scope .bind() freezing the unredacted chain | high | mitigate | Mode A ast source-scan gate (Task 2), proven RED |
| T-163-03 | Tampering | test vacuity (capture_logs replacing the chain) | medium | mitigate | capsys+JSON idiom mandated; negative control shipped |
</threat_model>

<verification>
- `cd analytics-service && python3 -m pytest` — full suite green (pytest ONLY from analytics-service/, with python3; ⛔ never `uvicorn main:app` locally — it claims real PROD compute jobs).
- Both RED demos named in docstrings with the exact mutation and observed direction.
- No new dependencies (Phase 165 owns dependency churn).
</verification>

<success_criteria>
- SC-1 fully closed: Mode B fixed at both entrypoints with a behavioral test through the REAL chain; Mode A gated by source scan; each demonstrated RED when neutered.
- Worker process redaction is unconditional — the phase's highest-risk item is retired first.
</success_criteria>

<output>
Create `.planning/phases/163-harden-fail-safe-closed-and-loud/163-01-SUMMARY.md` when done.
⚠️ The SUMMARY must contain NO local absolute paths and NO usernames — this phase ships a
no-allowlist scanner (163-03) that will scan it. Repo-relative paths only.
</output>
