---
phase: 163-harden-fail-safe-closed-and-loud
plan: 09
type: execute
wave: 2
depends_on: ["163-03"]
files_modified:
  - src/components/auth/SignupForm.tsx
  - src/components/auth/ResetPasswordForm.tsx
  - src/lib/auth/password-policy.ts
  - src/lib/auth/password-policy.test.ts
  - src/__tests__/audit-coverage.test.ts
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [SEC-01, SEC-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The HOSTED Supabase project's minimum password length is READ (never assumed), is >= 6, and the reading is documented beside the client floor — client minLength={6} is backed, not decorative"
    - "The two independent client constants are unified into one exported MIN_PASSWORD_LENGTH so client copy cannot drift from the documented policy"
    - "`add_wizard_composite_key` is in MUTATING_RPC_NAMES — its @audit-skip pragma is now LIVE law, not decoration: deleting the pragma turns the audit-coverage gate RED"
    - "The pragma-vs-real-emission decision is RECORDED in the SEC-03 requirement entry, not just implemented"
  artifacts:
    - "src/lib/auth/password-policy.ts — the ONE exported MIN_PASSWORD_LENGTH + a docblock naming where the hosted policy lives and what was read"
    - "src/__tests__/audit-coverage.test.ts — allowlist extended by one name"
    - ".planning/REQUIREMENTS.md — SEC-01 policy documentation + SEC-03 recorded decision"
  key_links:
    - "SignupForm minLength + ResetPasswordForm validation -> the shared exported constant"
    - "MUTATING_RPC_NAMES entry -> the add-key route's @audit-skip pragma evaluated by the gate"
---

<objective>
Close SEC-01 and SEC-03 (SC-4's policy clause + SC-5's audit clause). The client password
floor is two independent constants (SignupForm.tsx:241 literal `minLength={6}`;
ResetPasswordForm.tsx:21 local `MIN_PASSWORD_LENGTH = 6`) with NO server hop to enforce
anything — signup goes browser -> Supabase Auth directly — and the HOSTED project's policy
lives outside the repo and has never been read (RESEARCH assumption A1 is unverified).
`add_wizard_composite_key` escapes the audit-coverage gate because the gate's RPC detection
is allowlist-driven and the name is absent — making the `@audit-skip` pragma at the call
site (composite/add-key/route.ts:477-480) DECORATIVE.

Wave 2 (after 163-03): both plans edit .planning/REQUIREMENTS.md; serialized to avoid a
same-file conflict.

Purpose: the security floor is measured, not assumed; the audit law reaches the RPC that
escaped it — and Phase 164's SHARE mint/revoke RPCs land in a gate that is known to work.
Output: hosted-policy reading, unified constant, extended allowlist, recorded decisions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-CONTEXT.md
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-RESEARCH.md  (Sections 4a, 5 SEC-03)
@src/components/auth/SignupForm.tsx
@src/components/auth/ResetPasswordForm.tsx
@src/__tests__/audit-coverage.test.ts  (:203-224 MUTATING_RPC_NAMES)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the hosted password policy, unify the client floor, document</name>
  <files>src/components/auth/SignupForm.tsx, src/components/auth/ResetPasswordForm.tsx, src/lib/auth/password-policy.ts, src/lib/auth/password-policy.test.ts</files>
  <precondition>Supabase access sufficient to read the HOSTED project's auth password policy (Management API or MCP auth-config read); if no agent-readable lane exists, this task pauses at the embedded checkpoint below rather than assuming the GoTrue default.</precondition>
  <action>
    1. READ the hosted project's minimum password length + character requirements:
       attempt the Supabase Management API / MCP auth-config lane first. ⛔ Never assume
       the GoTrue default of 6 (that is exactly assumption A1 this requirement exists to
       retire). ⚠️ `supabase/config.toml:175,178` governs only the LOCAL stack — do not
       cite it as the hosted policy.
       - If the read succeeds and the minimum is >= 6: record the value + where it is set
         (dashboard section name) + the read date.
       - If the read succeeds and the minimum is < 6 (or unset): SEC-01 is a live gap —
         raising it is a founder-visible live op; STOP and raise a checkpoint:human-verify
         describing exactly what to set and where, then resume once confirmed.
       - If no agent lane exists: checkpoint:human-verify — the founder reads
         Dashboard -> Authentication -> Policies and reports the value.
    2. Optional behavioral probe, only if cheap: a 5-char signUp against the TEST project
       expecting a 4xx. ⛔ Never against PROD (no test-account creation on the production
       auth surface).
    3. Unify the client floor: new `src/lib/auth/password-policy.ts` exporting
       `MIN_PASSWORD_LENGTH` with a docblock recording the hosted reading (value, where
       set, date, and the sentence "the client floor is UX only — enforcement is
       Supabase-side"; the repo is public, so no project refs/ids). Consume it at
       BOTH sites: SignupForm.tsx:241 `minLength={MIN_PASSWORD_LENGTH}` and
       ResetPasswordForm.tsx:21 (delete the local constant, import the shared one).
    4. Unit test: both forms reference the shared constant (import-identity or rendered
       minLength assertion) so a future hardcoded literal regression is caught; plus the
       constant equals the documented hosted value.
  </action>
  <verify>
    <automated>npx vitest run src/lib/auth/password-policy.test.ts src/components/auth</automated>
  </verify>
  <done>
    Hosted value read and recorded (not assumed); one exported constant consumed by both
    forms; documentation names where the policy lives; auth component suites green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Put add_wizard_composite_key under the audit law; record the decision</name>
  <files>src/__tests__/audit-coverage.test.ts, .planning/REQUIREMENTS.md</files>
  <behavior>
    - With `"add_wizard_composite_key"` added to MUTATING_RPC_NAMES
      (audit-coverage.test.ts:203-224), the gate is GREEN at HEAD (the pragma at
      composite/add-key/route.ts:477-480 satisfies it).
    - BUILT-IN FALSIFIER: temporarily deleting the pragma turns the gate RED — that IS
      the neuter demo; observe it, restore, record in the SUMMARY.
  </behavior>
  <action>
    1. Add `"add_wizard_composite_key"` to the MUTATING_RPC_NAMES allowlist. Decision
       (research-argued, adopt unless the falsifier reveals otherwise): KEEP the existing
       pragma rather than emitting at add-key time — its stated reason is coherent (the
       RPC writes draft strategies + api_keys not yet user-visible; the user-visible
       creation is audited at finalize time in finalize-wizard/route.ts, and sibling
       `create_wizard_strategy` follows the same draft-then-finalize audit shape).
    2. RECORD the decision in .planning/REQUIREMENTS.md's SEC-03 entry (scoped edit):
       pragma retained, WHY (drafts audited at finalize; double-emission would duplicate
       the finalize event), and the falsifier evidence (pragma-delete observed RED on
       date). The CONTEXT locks recording as part of the requirement — an implemented-but-
       unrecorded decision does not close SEC-03.
    3. Opportunistic (same file, only if touching that region): DEF-141.2-03-A — the
       stale it.skip comment at audit-coverage.test.ts:962-964 citing retired
       flag-monitor coordinates; correct it.
    4. Note for the SUMMARY: this allowlist is the ONE edit Phase 164's SHARE mint/revoke
       RPCs must land in — SEC-03 standing is a hard prerequisite of 164 (ROADMAP
       dependency note).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/audit-coverage.test.ts src/__tests__/contracts</automated>
  </verify>
  <done>
    Name listed; gate green at HEAD; pragma-delete observed RED and restored; decision
    recorded in the requirement entry; contract suites green (they scan all of src/ —
    finish with a full-suite run).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Supabase Auth (hosted GoTrue) | password acceptance is decided server-side by a policy the repo cannot see |
| route handlers -> mutating RPCs | unlisted RPC names mutate outside the audit law |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-23 | Spoofing (weak credentials) | hosted password minimum unverified behind a client-only floor | medium | mitigate | read the hosted value; raise via checkpoint if < 6; unified documented constant |
| T-163-24 | Repudiation | add_wizard_composite_key mutating invisibly to the audit gate | medium | mitigate | allowlist entry + live pragma law + recorded decision |
</threat_model>

<verification>
- Full vitest suite + `npm run lint` green (lint now includes the 163-03 scanner — this plan's REQUIREMENTS.md edit must pass it).
- If any checkpoint fired, its resolution is recorded in the SUMMARY.
</verification>

<success_criteria>
- SEC-01: policy measured, enforced (or escalated), documented, drift-proofed. SEC-03: the gate polices the RPC, the pragma is live law, and the decision is on the record — Phase 164's prerequisite stands.
</success_criteria>

<output>
Create `.planning/phases/163-harden-fail-safe-closed-and-loud/163-09-SUMMARY.md` when done.
⚠️ No local absolute paths, usernames, or hosted-project identifiers in the SUMMARY —
163-03's scanner scans it and the repo is public.
</output>
