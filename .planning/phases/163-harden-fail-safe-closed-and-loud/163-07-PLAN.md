---
phase: 163-harden-fail-safe-closed-and-loud
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/routers/process_key.py
  - analytics-service/routers/simulator.py
  - analytics-service/tests/test_limiter_identity.py
  - analytics-service/tests/test_limiter_route_coverage.py
  - analytics-service/tests/test_resync_precheck_determinism.py
autonomous: true
requirements: [OPS-09, SEC-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "When two resync drafts exist, the pre-check deterministically resumes the NEWEST (ORDER BY created_at DESC) and an ancient orphaned draft outside the bounded window is never resumed"
    - "The simulator route's slowapi bucket is keyed by tenant (tenant_or_platform_key), not by spoofable/NAT-collapsing client IP — the tenth and last IP-keyed route repaired"
    - "The wrapper-check test conceals nothing: the quarantine equality asserts an EMPTY set and the simulator carve-out is deleted — the class enumeration is total at 10"
    - "A tenant-claimed caller driving the repaired route past its limit receives a real 429 (behavioral, not just structural)"
  artifacts:
    - "analytics-service/routers/process_key.py — ordered + bounded draft pre-check, both prose debts settled"
    - "analytics-service/tests/test_limiter_identity.py — quarantine frozenset() empty, carve-out gone, EXPECTED_CLASS_SIZE moved deliberately"
  key_links:
    - "simulator.py key_func -> functools.partial(tenant_or_platform_key, scope=...) -> shared identity asserted by the equality test"
    - "resync pre-check -> .order(created_at desc) + .gte bounded window -> deterministic resume"
---

<objective>
Close OPS-09 and SEC-05 — the analytics-service plumbing pair. The resync draft pre-check
(routers/process_key.py:1404-1413) has no ORDER BY, so under the documented two-draft
residual which draft `.limit(1)` resumes is planner/PostgREST-order dependent. The tenth
IP-keyed route (routers/simulator.py:92) still keys its slowapi bucket on
`get_remote_address`, and the test meant to police the class carries BOTH a quarantine
entry and a carve-out `continue` that conceal it.

Pre-edit gate tokens on the record (RESEARCH, 2026-08-26): quarantine size = 1;
`get_remote_address`-referencing router files = exactly the simulator module;
EXPECTED_CLASS_SIZE = 9.

Purpose: deterministic request plumbing; the security floor's last IP-keyed bucket closed
with the concealment removed.
Output: ordered+bounded pre-check, tenant-keyed simulator bucket, equality-asserting tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-CONTEXT.md
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-RESEARCH.md  (Sections 3 OPS-09, 4c)
@analytics-service/routers/process_key.py  (:1382-1413)
@analytics-service/routers/simulator.py  (:85-130)
@analytics-service/tests/test_limiter_identity.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic resync draft pre-check — ordered, bounded, prose debts settled</name>
  <files>analytics-service/routers/process_key.py, analytics-service/tests/test_resync_precheck_determinism.py</files>
  <behavior>
    - Two drafts for the same strategy (the two-tab race residual): the pre-check resumes
      the one with the NEWEST created_at, deterministically, regardless of insertion or
      planner order.
    - A draft older than the bounded window is NOT resumed (the pre-check treats it as
      absent — an ancient orphan cannot be silently revived).
    - MUTATION (docstring-named): removing the order clause lets the fixture's OLD draft
      win under reversed insertion order -> RED.
  </behavior>
  <action>
    1. Extend the query chain at routers/process_key.py:1404-1413 with
       `.order("created_at", desc=True)` and a bounded window
       `.gte("created_at", <now - window>)`. Window length is discretion: anchor it on
       the mechanics the :1387-1402 comment records (the worker's 30 s tick and the 15 s
       timeout blip that mints the residual) — the window only needs to span the
       realistic lifetime of a legitimately-resumable draft; derive it, state the
       derivation in the comment, and expose it as a named module constant so the test
       pins the same value. The read already runs strictly after the ownership gate
       (:1382-1385), so reordering is tenant-safe.
    2. Settle the two adjacent prose debts IN THE SAME EDIT (recorded doc-drift items):
       (a) the :1387-1402 two-draft comment — update it to describe the now-deterministic
       resume and the bound; quote-and-refute rather than delete (house style);
       (b) DEF-141.1-02-A — the SCOPE BOUND comment that still over-claims closing "the
       SEQUENTIAL retry class only"; correct the claim to what is true after this change.
    3. New pytest file (tests are discovered from analytics-service/; run pytest ONLY
       from that directory, with python3). Build the fixture on the existing process_key
       test scaffolding for the resync flow (grep `-k resync` for the current cases and
       extend their harness rather than inventing a new stub). Record the RED demo in the
       docstring: name the neuter (drop the order clause) and the observed direction.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_resync_precheck_determinism.py -x && python3 -m pytest tests/ -k resync -x</automated>
  </verify>
  <done>
    Pre-check ordered + bounded with a pinned named constant; both comments truthful;
    determinism and bound cases green, RED demo recorded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tenant-key the simulator bucket; make the wrapper-check an equality that conceals nothing</name>
  <files>analytics-service/routers/simulator.py, analytics-service/tests/test_limiter_identity.py, analytics-service/tests/test_limiter_route_coverage.py</files>
  <behavior>
    - simulator's slowapi decorator keys on `functools.partial(tenant_or_platform_key,
      scope="simulator")` — same identity the nine repaired siblings share (pattern:
      routers/portfolio.py:1948, scope="portfolio_bridge").
    - `IP_KEYED_QUARANTINE == frozenset()` and the equality assert at
      test_limiter_identity.py:493 bites on ANY new offender.
    - The carve-out `continue` for the simulator route inside
      test_every_registered_router_limit_is_shared_or_quarantined (:609-610) is DELETED —
      the partial/identity assertions now apply to simulator too.
    - Behavioral: a tenant-claimed caller driving the repaired route past `20/hour` gets
      a 429 (pattern: test_match_recompute_actually_throttles, :614+).
  </behavior>
  <action>
    1. routers/simulator.py:92 — replace the IP key func with the sibling pattern. The
       in-handler per-user quota (`_check_simulator_user_rate`, ~:104-130) is the
       authoritative per-tenant check and STAYS — only the slowapi decorator key changes.
       Remove the now-dead `_simulator_rate_limit_key` helper (do not leave a decoy).
    2. test_limiter_identity.py, all in the SAME commit (repo rule: limiter literal pins
       move with the behavior):
       - `IP_KEYED_QUARANTINE` (:120) -> `frozenset()` — pre-edit token: size 1.
       - Delete the FINDING-10 carve-out (:609-610).
       - `EXPECTED_CLASS_SIZE` (:108) 9 -> 10, moved deliberately per the constant's own
         comment ordering.
    3. Sweep tests/test_limiter_route_coverage.py for the mirrored quarantine wording and
       any literal pin naming the simulator key; update in the same commit.
    4. Behavioral throttle test for the repaired route (copy the :614+ pattern). RED
       demos, docstring-named: (a) revert the key func to the IP form -> the equality
       assert reports it in `unexpected=` -> RED; (b) neuter the throttle by raising the
       decorator limit in a scratch edit -> behavioral case RED. Restore both; verify
       restores by grepping for the partial call, not by hash.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_limiter_identity.py tests/test_limiter_route_coverage.py -x</automated>
  </verify>
  <done>
    Simulator tenant-keyed; quarantine empty; carve-out gone; class size 10 asserted;
    429 behavioral case green; both RED demos recorded; full analytics-service pytest +
    `mypy --strict --follow-imports=silent services/ routers/ models/` green from
    analytics-service/.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client IP header -> rate-limit key | spoofable / NAT-collapsed identity chose the bucket on the simulator route |
| concurrent tabs -> draft resume | non-deterministic resume picks an arbitrary draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-17 | Denial of Service | IP-keyed simulator bucket (shared-NAT collapse starves tenants; spoofed IPs bypass) | medium | mitigate | tenant_or_platform_key partial + equality-asserted class membership |
| T-163-18 | Tampering | wrapper-check concealment (quarantine + carve-out) | medium | mitigate | quarantine to empty; carve-out deleted; behavioral 429 proof |
| T-163-19 | Elevation of Privilege | resuming another context's ancient orphaned draft | low | mitigate | ordered + bounded pre-check after the ownership gate |
</threat_model>

<verification>
- Full analytics-service pytest green from `analytics-service/` with python3 (⛔ never repo-root — VCR cassette misses make LIVE broker calls; ⛔ never `uvicorn main:app` locally).
- mypy strict scope clean before ship.
</verification>

<success_criteria>
- OPS-09: deterministic, bounded pre-check with truthful comments. SEC-05: tenth route repaired, concealment removed, quarantine at zero, class total at 10 — all falsifiable.
</success_criteria>

<output>
Create `.planning/phases/163-harden-fail-safe-closed-and-loud/163-07-SUMMARY.md` when done.
⚠️ No local absolute paths or usernames in the SUMMARY (163-03's scanner will scan it).
</output>
