---
phase: 163-harden-fail-safe-closed-and-loud
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries.ts
  - src/lib/queries.test.ts
  - src/lib/freshness.ts
  - src/components/strategy/SyncBadge.tsx
  - src/components/strategy/SyncBadge.staler-of-two.test.tsx
  - src/components/strategy/StrategyTable.tsx
  - src/components/strategy/StrategyGrid.tsx
  - src/app/factsheet/[id]/tearsheet/page.tsx
  - src/app/(dashboard)/portfolios/[id]/page.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
autonomous: true
requirements: [HONEST-08]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On /browse and /discovery, a published row whose return series ended months ago can NEVER render a fresh 'Synced Nh ago' claim — the badge buckets on the STALER of sync-recency and series-recency"
    - "When series-recency is the binding (staler) fact, the badge names that subject instead of presenting sync time as the only clock — mirroring FreshnessChip's subject-naming"
    - "The staler-of-two decision exists ONCE (shared helper), consumed by the badge — not a second implementation that can drift from the factsheet chip's"
    - "The regression test uses a REAL published fixture row (status published, is_example false) and goes RED when the staler-of-two comparison is neutered"
    - "The anon list response still never carries daily_returns, the metrics_json blob, data_quality_flags, or a raw returns_series array"
  artifacts:
    - "src/lib/freshness.ts — shared staler-of-two recency resolver (subject + binding date), with FreshnessChip's conservatism: an unresolvable series end caps 'fresh' but never erases a known-bad age"
    - "src/components/strategy/SyncBadge.tsx — required seriesEnd prop, buckets and labels on the staler fact"
    - "src/lib/queries.ts — series-end scalar plumbed into the ranked-row shape without shipping the series array to anon readers"
  key_links:
    - "CATEGORY_RANKING_ANALYTICS_COLUMNS (or server-side shaping) -> row analytics series_end -> StrategyTable/StrategyGrid -> SyncBadge"
    - "src/lib/freshness.ts resolver -> SyncBadge bucketing (the ONE derivation)"
---

<objective>
Close HONEST-08 (SC-6, added post-QA 2026-08-26): the public discovery table's "Synced Nd
ago" badge must bucket on the STALER of sync- and series-recency, exactly as `FreshnessChip`
already does on the factsheet. Measured on PROD: row 2 of /browse/crypto-sma renders
"Synced 7h ago" over a series that ended 2026-05-06 — 112 days stale — while its own
factsheet chip correctly reads Track record - old. Two public surfaces, one strategy,
contradicting each other.

⛔ Do NOT close by deleting the badge — sync recency is real information; the defect is
that it is presented as the only clock. ⛔ Do NOT test via the `is_example` gate —
HONEST-03 scoped that to example rows, all 15 were deleted from PROD, so it guards zero
rows here and a test written against it is vacuous by construction.

Purpose: a freshness claim is not surface-local; the most public surface in the product
stops advertising freshness a dead return series contradicts.
Output: series-end plumbing, staler-of-two badge, shared resolver, falsifiable regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-CONTEXT.md  (HONEST-08 section)
@src/app/factsheet/[id]/v2/FactsheetView.tsx  (FreshnessChip :860-1013 — the logic to mirror)
@src/components/strategy/SyncBadge.tsx
@src/lib/freshness.ts
@src/lib/queries.ts  (:220-352 — projection + shaping)
@src/lib/queries.test.ts  (:360-408 — projection pins)
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Plumb a series-end scalar to every ranked row — without shipping the array to anon</name>
  <files>src/lib/queries.ts, src/lib/queries.test.ts</files>
  <action>
    The ranked-list embed (`CATEGORY_RANKING_ANALYTICS_COLUMNS`, src/lib/queries.ts:269-270)
    is an explicit anon-safe projection and does not carry `returns_series`. The rows need
    ONE new scalar: the date of the last series point.

    1. PRIMARY approach — a JSONB alias, following the measured `three_month:metrics_json->three_month`
       precedent (docblock :260-267, MEASURED against the TEST project 2026-08-21): probe
       `series_end:returns_series->-1->>date` (negative array index into the JSONB array
       of {date,value} points) against the TEST project first — same measurement habit,
       record HTTP status and returned shape in the SUMMARY. If PostgREST accepts it and
       returns the date string scalar, add the alias to the projection constant.
    2. FALLBACK if the alias is rejected: select `returns_series` in the embed but REDUCE
       it server-side — in `shapeRankingRows`/`shapeRowAnalytics` (queries.ts:335-352 /
       :440-475), derive `series_end` from the last element and STRIP the raw array from
       the object handed to the client. ⚠️ `shapeRowAnalytics` currently passes rankable
       rows through UNTOUCHED (:450) — on this fallback path the strip must be explicit,
       or the whole array crosses the RSC boundary to anonymous readers (the exact class
       the :240-267 docblock exists to prevent).
    3. Type: extend the row-analytics type with `series_end: string | null`, add the field
       (null) to `EMPTY_ANALYTICS` (src/lib/utils.ts:178 region), and keep the STALE-01
       shaping arms consistent (a non-rankable row's substitute object carries
       `series_end: null` — a run that did not produce numbers cannot claim a track end).
    4. Extend the projection pins in src/lib/queries.test.ts:363-407 in the SAME commit:
       add `series_end` to the must-stay list, and add a never-the-blob pin mirroring the
       metrics_json one — `returns_series` may appear ONLY as the alias/arrow form, never
       as a bare projected column (regex-pin it as :398 does for metrics_json).
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.test.ts</automated>
  </verify>
  <done>
    Every ranked row carries `analytics.series_end` (ISO date or null); the TEST-project
    probe result is recorded; the pins prove the raw series array cannot silently join
    the anon projection; full queries suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SyncBadge buckets on the staler of two, via ONE shared resolver</name>
  <files>src/lib/freshness.ts, src/components/strategy/SyncBadge.tsx, src/components/strategy/StrategyTable.tsx, src/components/strategy/StrategyGrid.tsx, src/app/factsheet/[id]/tearsheet/page.tsx, src/app/(dashboard)/portfolios/[id]/page.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <behavior>
    - Test 1: computedAt 7h ago + seriesEnd 112d ago -> badge renders the STALE bucket
      (dot + copy), and the copy names the track record as the binding subject — it does
      not read as a bare fresh "Synced 7h ago".
    - Test 2: computedAt 3d ago + seriesEnd 1d ago -> sync is the staler fact; badge
      renders exactly today's sync-keyed form (no regression on healthy rows).
    - Test 3: seriesEnd null on a row with computed analytics -> the badge cannot render
      the FRESH bucket (unknown caps fresh) but a known-stale computedAt still binds
      (unknown never erases a known-bad age) — FreshnessChip's TONE_RANK semantics.
  </behavior>
  <action>
    1. `src/lib/freshness.ts` (the stated single source of truth for staleness): add an
       exported resolver, e.g. `resolveEffectiveRecency(computedAt, seriesEnd)` returning
       the binding date + which subject binds (`"sync" | "series" | "unknown"`). Semantics
       MIRROR FreshnessChip (FactsheetView.tsx:899-916 TONE_RANK ordering): the verdict is
       the WORST of what is known; an unresolvable series end sits above fresh (cannot
       support a freshness claim) but below a definite stale/old sync age. Because
       `computeFreshness` is monotone in age, "staler of two" here means: classify on the
       OLDER of the two dates when both resolve. Document in the docblock WHY the ladders
       deliberately differ across surfaces (UI-SPEC C-1 records computeFreshness 12h/48h
       vs the chip's 3d/7d — this helper resolves WHICH date binds; each surface keeps its
       own ladder) — that is the reuse the CONTEXT demands, with zero new thresholds.
    2. `SyncBadge.tsx`: add a REQUIRED prop `seriesEnd: string | null` (required, so every
       call site is compile-forced into an explicit decision — no silent sync-only
       mounts). Bucket the dot via `computeFreshness` on the binding date from the
       resolver. When the SERIES is the binding fact, the copy must name it (mirroring the
       chip's subject-naming — e.g. a track-record-keyed line instead of the sync-keyed
       "Synced X ago"); when sync binds, today's rendering is unchanged byte-for-byte.
       ⛔ The badge is not deleted, and the null-computedAt early return stays.
    3. Call sites: StrategyTable.tsx:1180-1182 and StrategyGrid.tsx:118-122 pass
       `seriesEnd={s.analytics.series_end}`. The `mayClaimSyncRecency` / `!s.is_example`
       gates stay exactly as they are (untouched, and NOT relied on by this fix). The
       three other SyncBadge mounts (tearsheet page, portfolios page, ScenarioComposer)
       must each make an explicit choice: pass real series data where the surface already
       holds it (the tearsheet has the payload's dates), else pass `null` and accept the
       conservative capping — document the choice at each call site in one comment line.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/StrategyTable.stale-analytics.test.tsx src/components/strategy/StrategyTable.test.tsx src/components/strategy/StrategyGrid.test.tsx src/app/factsheet/[id]/v2/FactsheetView.chip-honesty.test.tsx</automated>
  </verify>
  <done>
    Badge buckets and labels on the staler fact via the shared resolver; every mount made
    an explicit seriesEnd decision; existing table/grid/chip suites green (the factsheet
    chip's behavior is unchanged).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The falsifiable regression test — a real published row, RED under neutering</name>
  <files>src/components/strategy/SyncBadge.staler-of-two.test.tsx</files>
  <behavior>
    - The fixture models the MEASURED PROD case: a row with `status: "published"`,
      `is_example: false`, terminal-success analytics, computedAt a few hours old,
      series_end ~112 days old — rendered through StrategyTable (the real mount path,
      not the badge in isolation) it must show the stale bucket with the track-record
      subject, and must NOT contain a fresh sync-keyed claim.
    - A companion case through StrategyGrid covers the second render path (HONEST-03
      precedent: both paths covered so the class cannot re-open through whichever one a
      future page mounts).
  </behavior>
  <action>
    Write the regression test with the anti-vacuity discipline:
    - ⛔ The fixture must NOT be an example row and the assertions must NOT route through
      the `is_example` gate — that gate guards zero rows on this surface now (all 15
      examples deleted from PROD 2026-08-26); a test leaning on it is vacuous by
      construction (CONTEXT lock).
    - RED demo, named in the test docstring: neuter the staler-of-two comparison (make
      the badge classify on computedAt alone — e.g. bypass the resolver) -> the stale-series
      case renders the fresh sync claim -> test observed RED; restore. Take a byte backup
      before the neuter edit; verify the restore by grepping for the resolver call.
    - Keep the coverage ratchet in mind (lines 82 / stmts 80 / funcs 74 / branches 72):
      the new resolver and badge branches must be covered by these cases.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/SyncBadge.staler-of-two.test.tsx</automated>
  </verify>
  <done>
    Regression test green at HEAD, observed RED under the named neuter, both render paths
    covered, docstring records the mutation; full vitest suite green at plan end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server ranked-list query -> anonymous browser | analytics columns cross the RSC boundary to unauthenticated readers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-09 | Information Disclosure | raw returns_series joining the anon projection via the fallback path | high | mitigate | explicit strip in shaping + regex projection pin in queries.test.ts |
| T-163-10 | Repudiation (false health) | badge advertising freshness a dead series contradicts | medium | mitigate | staler-of-two bucketing via shared resolver, falsifiable regression test |
</threat_model>

<verification>
- Full vitest suite green; queries projection pins green; FactsheetView chip suites untouched and green.
- The TEST-project probe measurement recorded in the SUMMARY (status + shape, no project identifiers).
</verification>

<success_criteria>
- SC-6 closed: the discovery badge buckets on the staler of sync- and series-recency, mirroring FreshnessChip via one shared derivation; badge not deleted; is_example not relied upon; regression test proven able to fail.
</success_criteria>

<output>
Create `.planning/phases/163-harden-fail-safe-closed-and-loud/163-04-SUMMARY.md` when done.
⚠️ No local absolute paths or usernames in the SUMMARY (163-03's scanner will scan it).
</output>
