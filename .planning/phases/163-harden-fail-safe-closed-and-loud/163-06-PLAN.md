---
phase: 163-harden-fail-safe-closed-and-loud
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/<new-timestamp>_destrict_enqueue_internal_10param.sql
  - supabase/tests/test_enqueue_internal_destrict.sql
autonomous: false
requirements: [OPS-08]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The 10-param `_enqueue_compute_job_internal` re-reads its four lost-race branches with plain SELECT INTO and raises ERRCODE serialization_failure when the winner already advanced — parity with the 7-param overload in the same source migration"
    - "A lost race whose winner advanced past in-flight statuses surfaces as a retry-safe classified error, never an opaque 500 (NO_DATA_FOUND)"
    - "A SQL gate asserts the deployed function body via pg_get_functiondef — and its expected pre-apply RED is documented, not laundered"
    - "The migration passed the three-reviewer gate BEFORE any apply"
  artifacts:
    - "One NEW forward-only migration re-based on the LATEST definition (20260716090000 — verified the head by grep), never an edit to an applied file"
    - "supabase/tests/test_enqueue_internal_destrict.sql — auto-discovered by the sql-tests job"
  key_links:
    - "new migration CREATE OR REPLACE -> re-based on 20260716090000:181+ 10-param body -> self-verifying DO block extended"
    - "sql-tests CI job -> pg_get_functiondef assertions -> RED until the TEST-project hand-apply"
---

<objective>
Close OPS-08 (SC-3's SQL half): the 10-param `_enqueue_compute_job_internal` overload still
carries four strict re-reads on its lost-race branches (measured pre-edit token: 4
occurrences in the current 10-param body; 0 in the 7-param). Between the conflict and the
re-read the winner may legitimately advance past the in-flight statuses, so the strict form
raises NO_DATA_FOUND and surfaces as an opaque 500. The 7-param overload in the SAME file
already carries the target pattern — replicate it across all four target branches.

⚠️ Merging supabase/migrations/** to main AUTO-APPLIES to PROD. This plan carries a blocking
checkpoint: three reviewers before any apply.

Purpose: worker/request plumbing is deterministic — a lost race is a classified, retry-safe
outcome.
Output: one forward-only migration, one SQL gate, reviewer sign-off, TEST hand-apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-CONTEXT.md
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-RESEARCH.md  (Section 3, OPS-08)
@.planning/phases/163-harden-fail-safe-closed-and-loud/163-PATTERNS.md  (Sections 4-5)
@supabase/migrations/20260716090000_retire_compute_analytics_kind_rpc_guard.sql
@supabase/tests/test_wizard_composite_fence.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: The forward-only de-STRICT migration, re-based on the latest definition</name>
  <files>supabase/migrations/<new-timestamp>_destrict_enqueue_internal_10param.sql</files>
  <action>
    1. Re-base rule FIRST: `grep -rn "_enqueue_compute_job_internal" supabase/migrations/`
       and confirm 20260716090000 still holds the latest definitions of BOTH overloads
       (7-param at :49, 10-param at :181; later migrations reference it only in comments —
       research-verified 2026-08-26, re-verify at HEAD). ⛔ Never edit the applied file.
    2. New migration: CREATE OR REPLACE the 10-param overload with the body byte-based on
       the 20260716090000 definition, changing ONLY the four lost-race branches
       (p_strategy_id / p_portfolio_id / p_allocator_id / else p_api_key_id, currently at
       :283-311): each becomes a plain `SELECT id INTO v_new_id ... LIMIT 1;` (drop the
       strict keyword), followed after the IF-chain by the 7-param's explicit
       `IF v_new_id IS NULL THEN RAISE EXCEPTION ... USING ERRCODE =
       'serialization_failure'; END IF;` (copy the 7-param structure at :140-172
       verbatim; the RAISE message must name all four targets since this overload has
       four arms). Keep the retired-kind reject clause — the file's self-verifying DO
       block asserts both bodies retain it and the deploy fails (correctly) without it.
    3. Header: preserve the provenance-comment habit ("verbatim from 20260716090000:181
       with ONLY the four lost-race re-reads de-strict-ed and the serialization_failure
       raise added") — state what is byte-unchanged and what is not.
    4. Extend the migration's own self-verifying DO block (mirror :318-345) with an arm
       asserting the deployed 10-param body no longer contains the strict re-read form.
       ⚠️ GATE-TOKEN TRAP: `pg_get_functiondef` returns in-body COMMENTS too, and the
       7-param's honest comment quotes the old form in prose. Match the STATEMENT form
       token `INTO STRICT v_` (keyword pair + variable prefix), and ensure the new
       function body's comments never contain that exact token — phrase prose as "the
       strict re-read" instead. Count the expected occurrences PRE-EDIT: 4 in the current
       10-param body, 0 after.
  </action>
  <verify>
    <automated>grep -c "INTO STRICT v_" supabase/migrations/*destrict_enqueue_internal_10param.sql | grep -qx "0" && grep -c "serialization_failure" supabase/migrations/*destrict_enqueue_internal_10param.sql | grep -qvx "0"</automated>
  </verify>
  <done>
    Migration exists, re-based on the verified head, four branches de-strict-ed with the
    classified raise, retired-kind guard intact, DO block extended, provenance header
    present.
  </done>
</task>

<task type="auto">
  <name>Task 2: The SQL gate — pg_get_functiondef assertions with the expected RED documented</name>
  <files>supabase/tests/test_enqueue_internal_destrict.sql</files>
  <action>
    Author `supabase/tests/test_enqueue_internal_destrict.sql` in the house shape
    (analog: test_wizard_composite_fence.sql):
    - Long `--` header naming the migration under test and Phase 163 / OPS-08; a
      `-- This file asserts:` Part list; and — MANDATORY — the pre-apply RED behaviour
      stated explicitly ("Pre-apply (RED): Part 1 fails because the deployed 10-param
      body still carries the four strict re-reads; ON_ERROR_STOP aborts there").
    - Plain PL/pgSQL DO blocks (pgTAP is not installed), RAISE EXCEPTION on failure,
      RAISE NOTICE on pass. ⛔ No psql backslash meta-commands (the sql-tests preflight
      rejects them). ⛔ No SKIP path (the anti-SKIP gate makes a SKIP-printing file FAIL).
    - Assertions via `pg_get_functiondef` of the 10-param overload (select it by
      signature from pg_proc): (a) zero occurrences of the statement token
      `INTO STRICT v_`; (b) the serialization_failure raise present; (c) the retired-kind
      reject clause still present (the parity property the source file's DO block pins).
    - Hygiene paragraph: fixture-free (this gate only reads catalog state), but keep the
      transaction + ROLLBACK shell for uniformity with the suite.
    - ⚠️ EXPECTED RED IN CI: nothing applies migrations to the TEST project automatically
      — this gate is RED in `sql-tests` until the migration is hand-applied to TEST
      (the recorded phase-162 mechanism). The plan names it so the red is read as
      expected; the hand-apply happens at the Task 3 checkpoint, AFTER review.
  </action>
  <verify>
    <automated>grep -c "ON_ERROR_STOP\|DO \$\$" supabase/tests/test_enqueue_internal_destrict.sql | grep -qvx "0" && ! grep -q "^\\\\" supabase/tests/test_enqueue_internal_destrict.sql</automated>
  </verify>
  <done>
    Gate file exists, auto-discovered by filename glob, header documents the pre-apply
    RED, assertions target the deployed body via pg_get_functiondef with the
    comment-safe statement token.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Three-reviewer migration gate, then the TEST hand-apply</name>
  <what-built>
    A new forward-only migration de-strict-ing the 10-param `_enqueue_compute_job_internal`
    lost-race re-reads (Task 1) and a `supabase/tests/` gate asserting the deployed body
    via pg_get_functiondef (Task 2, currently EXPECTED RED — the TEST project has not
    received the migration).
  </what-built>
  <how-to-verify>
    ⛔ Hard project rule: three reviewers BEFORE any migration apply. In order:
    1. Run migration-reviewer, rls-policy-auditor and silent-failure-hunter over the new
       migration file; fix any findings within this plan's scope before proceeding.
    2. Hand-apply the migration to the TEST project only (psql against TEST — never PROD;
       PROD receives it automatically on merge to main).
    3. Re-run the sql-tests lane and observe test_enqueue_internal_destrict.sql flip
       RED -> GREEN. That observed flip IS the gate's anti-vacuity demonstration — record
       it in the SUMMARY with the pre-edit token counts (4 strict re-reads before, 0 after).
  </how-to-verify>
  <resume-signal>
    All three reviewer passes recorded with findings resolved; TEST hand-apply done; the
    SQL gate observed RED pre-apply and GREEN post-apply on the TEST project.
  </resume-signal>
  <verify>
    <automated>MISSING — the RED->GREEN flip is observed on the TEST project's sql-tests run at this checkpoint; record the run reference in the SUMMARY</automated>
  </verify>
  <done>Three reviewer passes recorded, TEST hand-apply done, gate observed RED pre-apply and GREEN post-apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase/migrations merge -> PROD database | merging to main auto-applies; a bad body ships to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-163-14 | Denial of Service (opaque 500) | strict lost-race re-read raising NO_DATA_FOUND on the request path | medium | mitigate | plain re-read + serialization_failure classified raise (retry-safe) |
| T-163-15 | Tampering | migration drift (re-basing on a stale definition) | high | mitigate | grep-all-migrations re-base rule + provenance header + extended DO block |
| T-163-16 | Repudiation | SQL gate green-washing via comment prose in the function body | medium | mitigate | statement-form token; in-body comments barred from carrying it |
</threat_model>

<verification>
- CI `sql-tests`: expected RED before the TEST hand-apply, GREEN after — both states observed and recorded.
- The migration file is NEW; `git diff` shows zero changes to any previously-applied migration.
</verification>

<success_criteria>
- SC-3's SQL clause closed: 10-param parity with the 7-param overload, gate proven able to fail by the apply-flip, reviewers before apply honored.
</success_criteria>

<output>
Create `.planning/phases/163-harden-fail-safe-closed-and-loud/163-06-SUMMARY.md` when done.
⚠️ No local absolute paths or usernames in the SUMMARY (163-03's scanner will scan it).
</output>
