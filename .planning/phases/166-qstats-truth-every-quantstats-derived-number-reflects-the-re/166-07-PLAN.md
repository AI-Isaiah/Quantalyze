---
phase: 166-qstats-truth-every-quantstats-derived-number-reflects-the-re
plan: 07
type: execute
wave: 6
depends_on: ["166-06"]
files_modified:
  - analytics-service/tests/qstats_gate.py
  - analytics-service/tests/test_qstats_gate.py
  - analytics-service/tests/conftest.py
  - analytics-service/tests/test_metrics.py
autonomous: true
requirements: [SC-2, SC-3, TODOS-0f, WINDOWS-9]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-14 / SC-3: the RANK-05 region gate is replaced by an AST walk over EVERY production .py module under analytics-service/ (tests/, virtualenvs, caches excluded); it sees direct qs.stats calls, the attribute dispatch over the stats namespace (resolving dispatched names from a module-level table), aliased references, from-imports of quantstats.stats / quantstats.utils members, preparer references, calls inside _rolling_alpha_beta, and a NEW module importing quantstats outside COVERED_MODULES"
    - "D-07 / SC-2: a census row per quantstats call node and per mirrored function (module, enclosing function, quantstats function, call shape, arm kwarg-proven / inline / exempt, reason) is PRINTED on every pytest run via pytest_terminal_summary, with the live 'qs.stats.' text-occurrence count beside the AST node count (the 30-vs-9 reconciliation); drawdown_details is the single exempt row"
    - "D-02: the census names the residual kwarg-closed quantstats sites, which is the evidence for 'quantstats stays, kwarg-closed plus parity oracle'"
    - "the old line-matching gate test is deleted in the SAME commit that lands the AST gate, so there is never a commit with no gate"
    - "anti-vacuity: scanned nodes > 0 and the covered modules found equal COVERED_MODULES; an empty scan is RED ('blind, not clean'); KWARG_PROVEN equals exactly the set of leaves the code calls, minus EXEMPT (`drawdown_details`) (a stale entry is RED)"
    - "the SUMMARY passed the planning-hygiene check while staged, before it was committed"
  artifacts:
    - path: analytics-service/tests/qstats_gate.py
      provides: "SERVICE_ROOT, scanned_files, COVERED_MODULES, KWARG_PROVEN, EXEMPT, MIRRORED, Violation, CensusRow, scan_source(module_name, source), scan_tree(root), census_lines()"
      contains: "def scan_source"
    - path: analytics-service/tests/test_qstats_gate.py
      provides: "real-corpus gate: clean, not-blind, census accounts for every node, stale-allowlist, printed census"
      contains: "def test_qstats_gate_real_corpus_is_not_blind"
    - path: analytics-service/tests/conftest.py
      provides: "pytest_terminal_summary writes the live-db line then the qstats census lines"
      contains: "census_lines"
  key_links:
    - from: analytics-service/tests/conftest.py
      to: analytics-service/tests/qstats_gate.py
      via: "lazy import inside the one existing pytest_terminal_summary (no second hook definition)"
      pattern: "qstats_gate"
    - from: analytics-service/tests/test_qstats_gate.py
      to: analytics-service/tests/qstats_gate.py
      via: "the SAME scan_source is exercised by the real-tree walk (and by plan 08's needles)"
      pattern: "scan_tree\\("
---

<objective>
Replace the RANK-05 line-matching region gate with an AST gate over every production module that
imports quantstats, allowlisted by named leaf functions, printing its census on every run (D-14, D-07,
SC-2, SC-3; the gate item of TODOS 0f; the gate-blindness half of WINDOWS.md entry 9).

Purpose: the old gate reported clean over the exact surface that was still open (it could not see the
attribute dispatch or `_rolling_alpha_beta`, and trusted keyword TEXT that `cvar` proves is a lie).
Fixing the metrics without fixing the gate leaves the next regression equally invisible. Plan 08 then
proves this gate able to fail at every call shape (needles, behavioural pins, real-file neuter drills);
the split exists for per-plan context budget only.
Output: a pure scanner helper module, the real-corpus gate tests, the conftest census print, and the old
gate deleted in the same commit that lands the new one.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-CONTEXT.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-PATTERNS.md
Also read `166-06-SUMMARY.md` in this phase directory once the plan it belongs to has executed (it records the final state of the mirrors; it does not exist at planning time, so it is not an @-reference).
@analytics-service/tests/test_raw_5xx_census.py
@analytics-service/tests/conftest.py

**Environment.** Python ONLY from the worktree's `analytics-service/` directory with the MAIN
checkout's venv interpreter resolved at run time (capture `git worktree list --porcelain` into `QZ_WT`,
first `worktree` path + `/analytics-service/.venv/bin/python` -> `QZ_VENV_PY`; never written into a file;
exit 127 = FAIL). No database/network/credential. No SPEC; spec-less probe fallback skipped.

**Expected census after plans 03-06 (re-derive from the code, do not trust):** quantstats call nodes in
`services/metrics.py`: `volatility`, `value_at_risk` (headline VaR, and serenity's drawdown VaR),
`tail_ratio`, `profit_factor` (compute_all_metrics and the common-sense / cpc mirrors), `win_rate`,
`avg_win`, `avg_loss` (kelly / cpc / payoff composition) - all with `prepare_returns=False` - plus
`drawdown_details` (exempt, pinned by `test_rank05_drawdown_details_is_heuristic_free`). Zero attribute
dispatch; zero `greeks` / `r_squared` / `rolling_greeks` / dispatched-scalar nodes. MIRRORED (arm inline):
recovery_factor -> `_recovery_factor`, ulcer_index -> `_ulcer_index`, ulcer_performance_index ->
`_ulcer_performance_index`, kelly_criterion -> `_kelly_criterion`, probabilistic_ratio ->
`_probabilistic_sharpe_ratio`, common_sense_ratio -> `_common_sense_ratio`, cpc_index -> `_cpc_index`,
serenity_index -> `_serenity_index`, r_squared -> `_r_squared`, greeks -> `_greeks_no_guess`,
rolling_greeks -> `_rolling_greeks`. At phase start: 30 text occurrences of `qs.stats.` and 9 AST nodes.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): the AST scanner, the real-corpus gate GREEN and not blind on services/metrics.py, and the old line gate deleted in the same commit</name>
  <files>analytics-service/tests/qstats_gate.py, analytics-service/tests/test_qstats_gate.py, analytics-service/tests/test_metrics.py</files>
  <read_first>
    - analytics-service/tests/test_raw_5xx_census.py - `scan_source`, `_enclosing_function_resolver` (copy it; keys on function name, not lineno), `scanned_files`, `TestVacuityFence`
    - analytics-service/tests/test_metrics.py - `test_rank05_no_unclosed_quantstats_call_survives_in_compute_all_metrics` (its ANTI-VACUITY docblock reasoning is carried into the new gate) and `test_rank05_drawdown_details_is_heuristic_free` (kept, pins the one exemption)
    - analytics-service/services/metrics.py - read-only here: every quantstats reference, `_QSTATS_SINGLE_ARG_SCALARS`, the mirror function names
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - "Pattern 3: The AST gate", Pitfall 5
  </read_first>
  <behavior>
    - test_qstats_gate_real_corpus_is_clean: `scan_tree(SERVICE_ROOT)` over every production module returns zero violations
    - test_qstats_gate_real_corpus_is_not_blind: scanned quantstats nodes > 0, and the set of modules found importing quantstats equals COVERED_MODULES (= {"services/metrics.py"}); failure message says "blind, not clean"
    - test_qstats_gate_kwarg_allowlist_is_not_stale: KWARG_PROVEN equals exactly the set of quantstats leaf names the covered modules call, minus EXEMPT (`drawdown_details` is called but exempt, never kwarg-proven)
  </behavior>
  <action>
    Per D-14, D-02:
    1. Create `analytics-service/tests/qstats_gate.py` (a helper, not collected by pytest; the conftest and the test files both import this single implementation - conftest never imports a test_* module). Contents:
       - `SERVICE_ROOT` (analytics-service/), `scanned_files()` = every `*.py` under SERVICE_ROOT whose relative path has no part in {"tests", ".venv", "venv", "__pycache__", "node_modules", "site-packages"} and no hidden-directory part.
       - `COVERED_MODULES = frozenset({"services/metrics.py"})`; `KWARG_PROVEN` = frozenset of the leaf names listed in context; `EXEMPT = {"drawdown_details": "consumes the drawdown curve, never a returns series; pinned by test_rank05_drawdown_details_is_heuristic_free"}`; `MIRRORED` = mapping quantstats name -> (mirror symbol, one-line reason) for the eleven names in context.
       - Named tuples `Violation(module, function, qs_name, shape, lineno, reason)` and `CensusRow(module, function, qs_name, shape, arm, reason)`.
       - `scan_source(module_name, source) -> tuple[list[Violation], list[CensusRow], int]` using stdlib `ast` only. First resolve local aliases: names bound by `import quantstats [as X]`, `import quantstats.stats [as X]`, `from quantstats import stats [as X]`. Rules (research Pattern 3): (A) importer coverage - a module importing quantstats in any form that is not in COVERED_MODULES -> Violation "uncovered importer"; (B1) a Call whose func is `<qs>.stats.<name>` (or `<stats-alias>.<name>`) is GREEN only if `<name>` is in KWARG_PROVEN AND it has a keyword `prepare_returns` whose value is `ast.Constant(False)` (a variable, `True`, or `**kwargs` is RED), or `<name>` is in EXEMPT; else Violation naming module:function:name:lineno; (B2) any `<qs>.stats.<name>` Attribute that is not a call's func (alias assignment, argument, tuple element) -> Violation "aliased reference"; (B3) a Call to the builtin attribute-getter whose first argument resolves to the stats namespace -> Violation "attribute dispatch", and resolve the dispatched names when the second argument is a loop variable over a module-level tuple literal (report them in the reason); (B4) any reference to `quantstats.utils`, `<qs>.utils`, `_utils`, or a name starting `_prepare_` bound from quantstats, and any `from quantstats.stats import ...` / `from quantstats.utils import ...` -> Violation. Comments and string literals are invisible to ast by construction.
       - `scan_tree(root) -> (violations, census, scanned_nodes, importers_found)`.
    2. Create `analytics-service/tests/test_qstats_gate.py` with the three behaviour tests.
    3. Delete `test_rank05_no_unclosed_quantstats_call_survives_in_compute_all_metrics` from test_metrics.py in this SAME commit (the replacement lands with it). Keep `test_rank05_drawdown_details_is_heuristic_free`.
    4. Run: the new test file GREEN; full suite green.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_qstats_gate.py -q -k "real_corpus or not_stale"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or " failed" in the summary, or fewer than 3 passed</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest -q -n auto</automated>
    <fails_when>non-zero exit, or " failed" / " error" in the final summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def scan_source(' analytics-service/tests/qstats_gate.py` prints 1
    - `grep -c 'def test_rank05_no_unclosed_quantstats_call_survives_in_compute_all_metrics' analytics-service/tests/test_metrics.py` prints 0, and for the task commit C (record its SHA) `git show --name-only --format= C` lists both test_metrics.py and the new analytics-service/tests/test_qstats_gate.py
    - `grep -c 'def test_rank05_drawdown_details_is_heuristic_free' analytics-service/tests/test_metrics.py` prints 1
    - both verify commands exit 0
  </acceptance_criteria>
  <done>The AST gate runs in the python CI job, is green on the real tree and not blind, its allowlist is exactly the called leaves, and the old line gate is gone in the same commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the printed census (every node and every mirror accounted for, the 30-vs-9 reconciliation) wired into the one existing pytest_terminal_summary, visible on a green run</name>
  <files>analytics-service/tests/qstats_gate.py, analytics-service/tests/test_qstats_gate.py, analytics-service/tests/conftest.py</files>
  <read_first>
    - analytics-service/tests/qstats_gate.py (Task 1)
    - analytics-service/tests/conftest.py - `live_db_retry_summary_line`, `pytest_terminal_summary` (extend THIS hook; a second definition would silently shadow it), the lazy `from tests.live_db_transport import ...` pattern
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - Pitfall 5 (census invisible on green)
  </read_first>
  <behavior>
    - test_qstats_gate_census_accounts_for_every_node: every AST quantstats node found has exactly one census row with an arm in {kwarg-proven, exempt}; every MIRRORED entry has a row with arm inline AND its mirror function is defined in services/metrics.py (a renamed or deleted mirror is RED); exactly one exempt row (drawdown_details)
    - test_qstats_gate_census_is_printed_on_a_real_run: a subprocess `python -m pytest` run of one trivial passing test in this repo prints the census header line and the reconciliation line in its stdout (the census must be visible on a GREEN run - Pitfall 5)
  </behavior>
  <action>
    Per D-07, D-14:
    1. In qstats_gate.py add `census_lines() -> list[str]`: a header line beginning `qstats-gate census:`, one line per CensusRow (MIRRORED entries emitted as arm `inline` rows after verifying the mirror symbol is defined), and a reconciliation line beginning `qstats-gate reconciliation:` stating the live count of `qs.stats.` text occurrences in services/metrics.py and the AST node count, with a note that the text count includes comments and docstrings.
    2. Add the two behaviour tests. For the printed-census test, run `sys.executable -m pytest <one existing fast test node> -q -p no:cacheprovider` with `cwd=SERVICE_ROOT` and assert both line prefixes appear in stdout.
    3. In `tests/conftest.py`, extend the ONE existing `pytest_terminal_summary`: keep `live_db_retry_summary_line()` first, then lazily import `census_lines` from `tests.qstats_gate` and `write_line` each. It runs on the controller only; the census is a pure function of source text, so no xdist aggregation is needed.
    4. Run: the whole test file GREEN with the census in its output; a full `pytest -q -n auto` run whose terminal output shows the census; full suite green.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && { "$QZ_VENV_PY" -m pytest tests/test_qstats_gate.py -q > "${TMPDIR:-/tmp}/q166-07-gate.log" 2>&1; QZ_RC=$?; cat "${TMPDIR:-/tmp}/q166-07-gate.log"; test "$QZ_RC" -eq 0; } && grep -q '^qstats-gate census:' "${TMPDIR:-/tmp}/q166-07-gate.log" && grep -q '^qstats-gate reconciliation:' "${TMPDIR:-/tmp}/q166-07-gate.log"</automated>
    <fails_when>non-zero pytest exit (exit 5 = nothing collected), or " failed" in the summary, or the log lacks a line starting "qstats-gate census:" or "qstats-gate reconciliation:"</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest -q -n auto</automated>
    <fails_when>non-zero exit, or " failed" / " error" in the final summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def pytest_terminal_summary' analytics-service/tests/conftest.py` prints 1 (extended, not duplicated)
    - `grep -c '^def census_lines(' analytics-service/tests/qstats_gate.py` prints 1
    - the census printed by the first verify command contains exactly one row with arm `exempt` (drawdown_details) and at least eleven rows with arm `inline`
    - both verify commands exit 0
  </acceptance_criteria>
  <done>The census accounts for every quantstats node and every mirror, prints on every green run through the single existing terminal-summary hook, and carries the 30-vs-9 reconciliation.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind |
|---|---|
| `analytics-service/tests/qstats_gate.py` | NEW helper: `SERVICE_ROOT`, `scanned_files`, `COVERED_MODULES`, `KWARG_PROVEN`, `EXEMPT`, `MIRRORED`, `Violation`, `CensusRow`, `scan_source`, `scan_tree`, `census_lines` |
| `analytics-service/tests/test_qstats_gate.py` | NEW tests: real-corpus clean / not-blind / stale-allowlist / census accounts / printed census |
| `pytest_terminal_summary` census print | conftest.py extension |
| deleted `test_rank05_no_unclosed_quantstats_call_survives_in_compute_all_metrics` | replaced in the same commit |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future code change -> persisted metrics | a reintroduced unclosed quantstats call would silently re-open the heuristic; the gate is the control |
| CI output -> reviewers | a gate whose census is invisible on green cannot be audited |
| planning docs -> public repo | the SUMMARY quotes the census and is world-readable once pushed |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-13 | Tampering (control that cannot fail) | the region gate | high | mitigate | AST scan of every production module; anti-vacuity fence ("blind, not clean"); stale-allowlist check; plan 08 adds permanent RED needles per shape and real-file neuter drills |
| T-166-15 | Repudiation (invisible census) | pytest output | medium | mitigate | pytest_terminal_summary prints the census on every run; a subprocess test asserts it appears |
| T-166-03 | Information disclosure | SUMMARY quoting output | medium | mitigate | repo-relative quoting; staged planning-hygiene check before the SUMMARY commit |
</threat_model>

<verification>
- `tests/test_qstats_gate.py` green; the census and reconciliation lines appear in a green run's output.
- Old line gate absent; drawdown_details exemption pin kept.
- Full suite green.
</verification>

<success_criteria>
SC-2 met: every quantstats node and mirrored function is in a printed census with an arm and a reason.
SC-3 structurally met: the gate AST-walks every quantstats-importing production module and sees the
dispatch shape and `_rolling_alpha_beta`; plan 08 records it failing at every shape.
</success_criteria>

<output>
Create `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-07-SUMMARY.md`
with: the full printed census (verbatim), the reconciliation (phase-start 30 text occurrences / 9 AST
nodes vs the live counts), and the allowlisted leaves.

**SUMMARY hygiene gate (mandatory, before the SUMMARY commit).** `.planning/` is public. Quote every
captured output repo-relative: an interpreter / venv / site-packages prefix becomes `<venv>/`, the
session scratchpad becomes `<scratchpad>/`, any other absolute path becomes its repo-relative form -
never paste a traceback's absolute file paths verbatim. Then, from the worktree root: `git add` the
SUMMARY (the checker enumerates `git ls-files`, so an unstaged new file is invisible to it), link
`node_modules` per D-19 if absent, run `./node_modules/.bin/tsx scripts/check-planning-hygiene.ts`,
then remove the link when it is a symlink. A non-zero exit or any violation line blocks the commit:
fix the text, re-stage, re-run until clean.
</output>
