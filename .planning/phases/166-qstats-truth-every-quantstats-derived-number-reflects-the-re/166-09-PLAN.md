---
phase: 166-qstats-truth-every-quantstats-derived-number-reflects-the-re
plan: 09
type: execute
wave: 8
depends_on: ["166-02", "166-08"]
files_modified:
  - .planning/WINDOWS.md
  - TODOS.md
autonomous: true
requirements: [SC-1, SC-5, TODOS-0f, WINDOWS-5, WINDOWS-9]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-10 / SC-5: the SUMMARY carries a before/after table - metric key, fixture, before, after, reason - MEASURED by running the pre-phase services/ tree and the current tree side by side, never copied from research: canonical trigger, non-monotone trigger, benchmark trigger, NaN-greeks class, PSR (benign + golden), rolling alpha, and every golden byte that moved across the phase. On a non-trigger fixture the only admitted movers are the keys D-15/D-16/D-17 change for that fixture's class: D-16 PSR on every non-zero-Sharpe fixture, D-17 rolling_alpha on every benchmarked fixture, and D-15 alpha/beta/treynor on the NaN-bearing benchmarked fixture. `treynor` (= cagr / beta) is recorded by VALUE and admitted as a DERIVED mover wherever beta moves (the benchmark trigger, where it changes value and sign while staying present, and the NaN-bearing class, where it goes absent -> present), each with its own row. Any other movement stops the plan"
    - "D-10: the golden FLATTENED key-path diff across the whole phase is exactly {metrics_json.metrics_json.probabilistic_sharpe_ratio, sibling.rolling_alpha}; any other moved key path is a defect, investigated, not re-baselined"
    - "D-11: the SUMMARY carries the read-only census SQL (guess-trigger population via daily_returns_grid; BTC >=100% day check; NaN-greeks fabricated-zero population; the rolling-alpha and PSR populations that D-16/D-17 change for every strategy) with its caveats, for the founder to run - this plan touches no remote database"
    - "D-01 / D-02 / SC-1: the SUMMARY answers 'does the library stay' explicitly: yes, pinned at 0.0.81, as a kwarg-closed dependency for the named leaf sites plus drawdown_details (exempt) and as the live parity oracle; PyPI latest re-measured at execution time; OPEN-1 moot (upstream main identical to v0.0.81, the only maintained fork carries both heuristics)"
    - "D-13: every surface reading an affected key is re-listed from a grep at HEAD and confirmed to render null as an absence; the eight dispatched scalars are persisted-only (no reader under src/)"
    - "D-08: findings RECORDED, not changed: F-1 (greeks alpha annualized on the frequency clock) and recovery_factor's arithmetic numerator vs upi's compounded one"
    - "WINDOWS.md entries 5 and 9 are marked fixed through gsd-tools and TODOS 0f carries a dated CLOSED note; both say that the 'kwarg-closable' claim was REFUTED by measurement (research Q2)"
    - "the gsd-tools windows handler leaves no collateral in ANY file: byte backups of .planning/WINDOWS.md, .planning/STATE.md, .planning/ROADMAP.md and .planning/state.json are taken before the call, `git status --porcelain` is read after it, and every file other than WINDOWS.md that it touched is restored from its backup (never git checkout / restore)"
    - "the SUMMARY passed the planning-hygiene check while staged, before it was committed"
  artifacts:
    - path: .planning/WINDOWS.md
      provides: "entries 5 and 9 status fixed"
    - path: TODOS.md
      provides: "0f [159-SIMPLIFY-DEFER] closed with a dated note naming what closed each of its three items"
  key_links:
    - from: "the phase's pre-metrics commit (BEFORE_SHA)"
      to: "the D-10 table"
      via: "git archive of analytics-service/services at BEFORE_SHA into the session scratchpad, measured in a separate python process from the current tree"
      pattern: "BEFORE_SHA"
---

<objective>
Produce the phase's disclosure of every value it changed (D-10, SC-5) by MEASURING the pre-phase and
post-phase code side by side, write the D-11 read-only census SQL for the founder, record the library
decision (D-01, D-02, SC-1) and the D-08 findings, re-verify the D-13 reader list, and close
WINDOWS.md entries 5 and 9 and TODOS 0f.

Purpose: these numbers are already in users' rows. A value that changes without a before/after row is
the defect SC-5 names. The table must be measured now, from the code, because research values were
taken before the mirrors existed and D-15/D-16/D-17 "after" values were never measured by research.
Output: the SUMMARY sections listed in `<output>`, plus two ledger edits in one commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-CONTEXT.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md
@.planning/WINDOWS.md
Also read `166-01-SUMMARY.md` .. `166-08-SUMMARY.md` in this phase directory (they exist once plans 01-08 have executed; they carry the RED-first evidence, the per-plan D-10 rows, the census (07) and the neuter drills (08) that this plan consolidates).

**Environment.** Python ONLY from an `analytics-service/` directory, with the MAIN checkout's venv
interpreter resolved at run time (capture `git worktree list --porcelain` into `QZ_WT`, first `worktree`
path + `/analytics-service/.venv/bin/python` -> `QZ_VENV_PY`; never written into a file; exit 127 = FAIL).
No remote database, no `uvicorn main:app`, no credential; the ONLY network call is the public PyPI JSON
read in Task 1. `.planning/` and `TODOS.md` are PUBLIC: no home path, username, credential, key id,
strategy name, connection string or project ref may be written. Cite code by symbol. No SPEC; spec-less
probe fallback skipped.

**gsd-tools handling (project rule):** run the handler first, then inspect EVERY file it may have
touched and hand-fix ONLY collateral it actually caused. The state handlers in this repo are measured
to clobber `.planning/STATE.md`, `.planning/ROADMAP.md` and `.planning/state.json` as side effects, so
diffing only the target file is not enough: take byte copies of `.planning/WINDOWS.md`,
`.planning/STATE.md`, `.planning/ROADMAP.md` and `.planning/state.json` into the session scratchpad
(plan-unique names) BEFORE the call, and read `git status --porcelain` AFTER it. Any file other than
WINDOWS.md that the handler changed is restored with `cp` from its byte backup and confirmed with
`cmp` - never `git checkout`, `git restore` or `git stash` (they would also destroy uncommitted work
that predates the call). Resolve the CLI as `~/.claude/gsd-core/bin/gsd-tools.cjs` (never write the
expanded home path into a file).

<assumption_delta_decision>
The assumption-delta detector fired `pluralization` on the ROADMAP sentence "Hand-writing inline mirrors
for the transitive four is a fallback, not the default." Primary noun: the quantstats-computed metric
(unchanged). Decision: `no-change`. Rationale: the sentence is prose about the library-shape decision
(D-01: upgrade vs mirror), not a second identity, tenant, platform or source of truth; the metric keys,
`metrics_json` shape and `strategy_analytics_series` kinds are unchanged, so no identity model is
promoted or added alongside.
</assumption_delta_decision>

**External API coverage:** the ai-integration detector was run at planning time over all seven PLAN
bodies plus the ROADMAP section and returned `detected: false` (quantstats is a local library, not an
external API), so no COVERAGE.md is required; the one PyPI read in Task 1 is a public metadata lookup,
not an integration.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): two-tree measurement harness producing the D-10 before/after table, plus the golden phase diff, the D-13 reader re-grep and the PyPI re-check</name>
  <files>none in the repository - the harness, the two extracted trees and the two JSON outputs live in the session scratchpad and are never committed</files>
  <read_first>
    - analytics-service/services/metrics.py - `compute_qstats_scalars`, `compute_all_metrics` (result keys `alpha`, `beta`, `r_squared`, and `result.sibling_kinds["rolling_alpha" / "rolling_beta"]`)
    - analytics-service/tests/test_metrics.py - the recipes of `_rank05_trigger_series`, `_rank05_benign_all_positive`, `_rank05_benign_mixed`, `_q166_trigger_nonmonotone`, `_q166_golden_with_nan_days`, `_q166_benchmark_trigger`, `_q166_calendar_mismatch`
    - analytics-service/tests/conftest.py - the `golden_returns` and `benchmark_returns` recipes
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - §Q4 tables, "Runtime State Inventory" (the census SQL and its caveats), §Q5 findings, §Q6 reader list
  </read_first>
  <action>
    Per D-10, D-11, D-13, D-01/D-02:
    1. Resolve `BEFORE_SHA` = the parent of the FIRST commit on this branch (since `git merge-base HEAD origin/main`) that touches `analytics-service/services/metrics.py` (the same phase base plans 03-06 diff their golden against). Record the SHA in the SUMMARY. Extract `analytics-service/services` at BEFORE_SHA with `git archive` into a plan-unique directory in the session scratchpad (e.g. `q166-09-before/`), never inside the repo; do the same for HEAD into `q166-09-after/` so both runs import identically-shaped trees.
    2. Write `q166_disclosure.py` in the scratchpad (never committed). It rebuilds each fixture from the SAME recipe the tests use (copy the recipe bodies verbatim), calls `compute_qstats_scalars` and `compute_all_metrics` from whichever `services` package is on `PYTHONPATH`, and writes one JSON object (keys = fixture names, e.g. `canonical_trigger`) to `$QZ_SCRATCH/q166-09-before.json` or `$QZ_SCRATCH/q166-09-after.json` (export `QZ_SCRATCH` = your session scratchpad directory): for each fixture, the eight scalars + r_squared, cagr, alpha, beta, treynor VALUE (JSON null when the key is absent - so presence and value are both diffed), rolling_alpha / rolling_beta point count and last point. Run it twice with `QZ_VENV_PY`, cwd = the scratchpad, `PYTHONPATH` pointing at each extracted `analytics-service` directory in turn. Fixtures: canonical trigger; non-monotone trigger; benign_all_positive; benign_mixed; golden_returns + benchmark_returns; its 3-NaN-day variant + benchmark_returns; benchmark trigger; calendar mismatch.
    3. Diff the two JSONs into the D-10 table (metric key | fixture | before | after | reason citing D-09/D-15/D-16/D-17, or the guess closure D-04/D-05/D-06 on a trigger fixture). The expected-movement rule is stated BY DECISION and per fixture class. It admits exactly these movements:
       - D-16 (PSR on the non-excess fourth moment): `probabilistic_sharpe_ratio` moves on EVERY fixture whose Sharpe is non-zero, because the fix changes the variance term by `(3/4)*SR**2`. Expected: all eight fixtures (benign_mixed; benign_all_positive, where the pre-fix value is None and the post-fix value is a number > 0.5; golden_returns; its 3-NaN-day variant; calendar mismatch; and the three triggers). Only a fixture whose measured Sharpe is exactly 0 keeps its PSR. A PSR that does NOT move on a non-zero-Sharpe fixture is itself a defect, because it means D-16 missed that path.
       - D-17 (windowed rolling intercept): the `rolling_alpha` last point moves on EVERY fixture run with a benchmark: golden_returns + benchmark_returns, its 3-NaN-day variant + benchmark_returns, calendar mismatch and the benchmark trigger. Its point count stays equal to `rolling_beta`'s count.
       - D-15 (pairwise-complete greeks, None and never 0.0): `alpha`, `beta` and `treynor` move under D-15 ONLY on a NaN-bearing benchmarked fixture. Here that is the 3-NaN-day variant + benchmark_returns: before 0.0 / 0.0 / treynor absent; after, the pairwise-complete values with treynor present at its measured value (or None / absent if beta is undefined). On a NaN-free benchmarked fixture (golden_returns, calendar mismatch) alpha, beta and treynor must be bit-identical.
       - Derived mover (D-10 / SC-5): `treynor` is `cagr / beta` (the M1 treynor guard), so its VALUE is admitted to move exactly where beta (or cagr) moves on a benchmarked fixture, and nowhere else: the benchmark trigger (beta moves under the guess closure; treynor changes value and sign while staying present) and the NaN-bearing class above. Each such movement is its own row with reason "derived: cagr / beta, beta moved per <decision>". A treynor that moves where beta and cagr are both bit-identical is a defect. No other persisted value derives from alpha/beta/r_squared/PSR/the rolling series: `correlation` and `info_ratio` are computed from the aligned pair directly (confirmed by reading the M1 block in `compute_all_metrics`), so they stay in the unchanged set.
       - Guess closure (D-04/D-05/D-06, with D-09's None rule): values move on the canonical trigger, the non-monotone trigger and the benchmark trigger, in the keys those fixtures were built to trip, and each moved key gets a row with its reason.
       EXPECTED UNCHANGED is everything else. On benign_mixed, benign_all_positive, golden_returns, the 3-NaN-day variant and calendar mismatch, that is every value except the keys the three decision bullets above name for that fixture's class. `r_squared` and `rolling_beta` are in that unchanged set on every non-trigger fixture. Any movement outside the admitted set is a defect: stop and report it, and do not table it as intended. Every movement inside the admitted set gets its own table row.
    4. Golden phase diff: FLATTENED key-path compare of `git show BEFORE_SHA:analytics-service/tests/fixtures/golden_252d_expected.json` with the working-tree file (the persisted scalars are nested one level down, at `metrics_json.metrics_json.<key>`); it must list exactly `metrics_json.metrics_json.probabilistic_sharpe_ratio` and `sibling.rolling_alpha`; table both with old/new values.
    5. D-13: grep `src/` (quote the include globs, e.g. `--include='*.ts' --include='*.tsx'`) for every affected key and its camelCase form, list each reader with how it renders null (research Q6: MetricsColumn reads the TS compute summary not metrics_json; BenchmarkGreeksTable em-dash; RollingAlphaBetaChart handles empty), and state that the eight dispatched scalars have no reader. If any surface renders null as 0 or crashes, STOP and report - that is a finding D-13 says to fix minimally, and it is not in this plan's file list.
    6. SC-1 / D-01: read the public PyPI JSON for quantstats and record `info.version`. If it is no longer 0.0.81, record it and state that per D-01 any bump is routed to Phase 165; do not change any dependency file.
    7. Assemble the D-11 census SQL from research "Runtime State Inventory" (guess-trigger population on the `daily_returns_grid` sibling kind; the BTC >= +100% day check) and ADD, read-only and table-level: (a) the NaN-greeks fabricated-zero population - rows whose `metrics_json` alpha and beta are both exactly 0; (b) the count of strategies with a `rolling_alpha` series row (every one of them renders a different rolling alpha after recompute, D-17); (c) the count of rows carrying `probabilistic_sharpe_ratio` (D-16, persisted-only). Print the caveats (5000-point cap, 6-dp rounding at the >= 1 boundary, ledger venues never re-compute on their own). No strategy name, id or project ref appears.
  </action>
  <verify>
    <automated>test -s "${QZ_SCRATCH:?set QZ_SCRATCH to your session scratchpad directory}/q166-09-before.json" && test -s "$QZ_SCRATCH/q166-09-after.json" && python3 -c "import json,sys; b=json.load(open(sys.argv[1])); a=json.load(open(sys.argv[2])); k='canonical_trigger'; print('before ulcer',b[k]['ulcer_index'],'after ulcer',a[k]['ulcer_index']); sys.exit(0 if (a[k]['ulcer_index']==0.0 and b[k]['ulcer_index'] not in (None,0.0) and a[k]['recovery_factor'] is None and a[k]['probabilistic_sharpe_ratio']>0.5) else 1)" "$QZ_SCRATCH/q166-09-before.json" "$QZ_SCRATCH/q166-09-after.json"</automated>
    <fails_when>non-zero exit, "QZ_SCRATCH" unset error, either JSON missing or empty, or the canonical-trigger row does not show before ulcer non-zero and after ulcer 0.0 / recovery None / PSR > 0.5</fails_when>
    <automated>QZ_BASE="$(git merge-base HEAD origin/main)" && QZ_LOG="$(git log --reverse --format=%H "$QZ_BASE"..HEAD -- analytics-service/services/metrics.py)" && QZ_FIRST="$(printf '%s\n' "$QZ_LOG" | head -1)" && test -n "$QZ_FIRST" && git show "$QZ_FIRST^:analytics-service/tests/fixtures/golden_252d_expected.json" > "${TMPDIR:-/tmp}/q166-09-golden-before.json" && python3 -c "import json,sys; fl=lambda x,p='': [kv for k,v in x.items() for kv in fl(v, p+'.'+k if p else k)] if isinstance(x,dict) else [(p,x)]; a=dict(fl(json.load(open(sys.argv[1])))); b=dict(fl(json.load(open('analytics-service/tests/fixtures/golden_252d_expected.json')))); moved=sorted(k for k in set(a)|set(b) if a.get(k)!=b.get(k)); print('phase golden key paths moved:',moved); sys.exit(0 if moved==['metrics_json.metrics_json.probabilistic_sharpe_ratio','sibling.rolling_alpha'] else 1)" "${TMPDIR:-/tmp}/q166-09-golden-before.json"</automated>
    <fails_when>non-zero exit (including an empty QZ_FIRST), or the printed "phase golden key paths moved:" lists anything other than exactly ['metrics_json.metrics_json.probabilistic_sharpe_ratio', 'sibling.rolling_alpha']</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY contains a markdown table with header `| metric key | fixture | before | after | reason |` and rows for at least: the eight scalars on the canonical trigger, kelly/cpc/csr on the non-monotone trigger, r_squared/alpha/beta/treynor/rolling beta/rolling alpha on the benchmark trigger, alpha/beta/treynor (measured value) on the NaN-greeks class, PSR on every non-zero-Sharpe fixture (benign_mixed / benign_all_positive / golden / 3-NaN-day variant / calendar mismatch / the three triggers), and rolling alpha on every benchmarked fixture (golden / 3-NaN-day variant / calendar mismatch / benchmark trigger)
    - the SUMMARY contains the D-11 SQL block and every statement in it begins with `SELECT` (read-only)
    - the SUMMARY records BEFORE_SHA, the PyPI version read, and the D-13 reader list
    - both verify commands exit 0 (run the first from the worktree root with `QZ_SCRATCH` exported to the session scratchpad)
  </acceptance_criteria>
  <done>Every value the phase changed is measured before/after from the code, the golden phase diff is exactly the two disclosed keys, the census SQL and reader list are ready for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: close WINDOWS.md entries 5 and 9 via gsd-tools and TODOS 0f with a dated note, recording the refuted kwarg-closable claim</name>
  <files>.planning/WINDOWS.md, TODOS.md</files>
  <read_first>
    - .planning/WINDOWS.md - header (how entries are closed) and rows 5 and 9
    - TODOS.md - entry 0f `[159-SIMPLIFY-DEFER]` and an existing `CLOSED` note elsewhere in the file for the house format (lineage kept, dated, names the closing phase)
  </read_first>
  <action>
    1. Before the handler: capture `git status --porcelain` to a scratchpad file (`q166-09-status-before.txt`), and byte-copy `.planning/WINDOWS.md`, `.planning/STATE.md`, `.planning/ROADMAP.md` and `.planning/state.json` into the session scratchpad under plan-unique names (`q166-09-bak-<file>`). From the worktree root run `node ~/.claude/gsd-core/bin/gsd-tools.cjs windows fixed 5` then `... windows fixed 9`. After the handler: capture `git status --porcelain` again and diff the two captures - every path that changed status or content, other than `.planning/WINDOWS.md`, is handler collateral. For each collateral file among the backed-up three, restore it with `cp` from its byte backup and confirm with `cmp`; for any OTHER file the handler touched, stop and report it (it has no backup, so it must be investigated, never reverted with git). `git diff -- .planning/WINDOWS.md` must show only rows 5 and 9 changing status to fixed with a resolved_at timestamp and the frontmatter counts moving by 2 (open -2, fixed +2); revert by hand (from the WINDOWS.md backup plus the two intended row edits) any other collateral inside WINDOWS.md. Never use `git checkout`, `git restore` or `git stash`.
    2. In TODOS.md, keep the 0f text as lineage and append a dated note in the house form: "CLOSED <execution date> by Phase 166 QSTATS-TRUTH", naming what closed each item - the primitives (plan 01: `_drawdown_series_from_wealth`, `_max_drawdown_from_wealth`, `_annualized_vol_sharpe`, `_downside_rms`, `_cvar_of_tail`), the AST gate (plans 07-08: `tests/qstats_gate.py`, `tests/test_qstats_gate.py`), the KPI array (plan 02: derived from `PERCENTILE_METRICS`) - and that the "kwarg-closable" wording of WINDOWS.md entry 9 / the 159-05 Residual table was REFUTED by the phase's preparer-spy measurement: recovery_factor, kelly_criterion, common_sense_ratio, cpc_index and the benchmark leg of r_squared / greeks / rolling_greeks all went to inline mirrors.
    3. Run the planning-hygiene check (link node_modules per D-19 first; remove the link before committing) and commit both files together as `docs(166): close WINDOWS 5 and 9 and TODOS 0f`.
  </action>
  <verify>
    <automated>test "$(grep -E '^\| (5|9) \| 159 \|' .planning/WINDOWS.md | grep -c '| fixed |')" -ge 2</automated>
    <fails_when>non-zero exit, i.e. fewer than two of rows 5 and 9 carry status "fixed" in .planning/WINDOWS.md</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsx scripts/check-planning-hygiene.ts</automated>
    <fails_when>non-zero exit (127 = toolchain not resolved = FAIL), or any line reporting a hygiene violation</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^\| (5|9) \| 159 \|' .planning/WINDOWS.md` shows both row 5 and row 9, each containing `| fixed |`
    - for the closure commit C (record its SHA), `git diff C^ C -- .planning/WINDOWS.md` touches only rows 5, 9 and the frontmatter counts / last_updated
    - the SUMMARY records the before/after `git status --porcelain` comparison around the handler call, names every collateral file found (or "none"), and for each restored file the `cmp` confirmation against its byte backup
    - `git show --name-only --format= C` lists exactly `.planning/WINDOWS.md` and `TODOS.md` (no STATE.md / ROADMAP.md / state.json collateral rides into the closure commit)
    - `grep -c 'CLOSED .* by Phase 166' TODOS.md` prints at least 1
    - `grep -c 'REFUTED' TODOS.md` increases by at least 1 relative to BEFORE_SHA
    - `test -L node_modules` is false at commit time
    - both verify commands exit 0
  </acceptance_criteria>
  <done>WINDOWS 5 and 9 are fixed with no collateral, TODOS 0f is closed with lineage kept, and hygiene is clean.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share, and the phase-wide list)

This plan produces no source symbol; it produces the disclosure (SUMMARY) and two ledger closures.
Phase-wide, the complete artifact list is:

| Plan | Artifacts |
|---|---|
| 01 | `_max_drawdown_from_wealth`, `_drawdown_series_from_wealth`, `_annualized_vol_sharpe`, `_downside_rms`, `_cvar_of_tail` |
| 02 | `src/lib/queries.percentile-columns.test.ts`; Test E in `csv-finalize-cross-submission-merge.test.ts`; `PERCENTILE_ANALYTICS_COLUMNS` and `CLOCK_SAFETY_KPI_COLUMNS` derived from `PERCENTILE_METRICS` |
| 03 | `_prepared_returns_no_guess`, `_recovery_factor`, `_ulcer_index`, `_ulcer_performance_index`, `_serenity_index`; retyped `_QSTATS_SINGLE_ARG_SCALARS` (attribute dispatch deleted); `_q166_trigger_nonmonotone`, `_q166_golden_with_nan_days`, `_Q166_PARITY_SITES`; drawdown-family `test_q166_*` |
| 04 | `_payoff_ratio_no_guess`, `_kelly_criterion`, `_probabilistic_sharpe_ratio`, `_common_sense_ratio`, `_cpc_index`; loss-family / shuffle / PSR-anchor `test_q166_*`; golden PSR key path |
| 05 | `_align_benchmark_like_qs`, `_r_squared`, `_greeks_no_guess`; `_q166_benchmark_trigger`, `_q166_calendar_mismatch`; r_squared / greeks tests |
| 06 | `_rolling_greeks`; rolling tests; golden rolling_alpha key path; deleted missing-columns test + branch |
| 07 | `tests/qstats_gate.py` (`scan_source`, `scan_tree`, `census_lines`, `COVERED_MODULES`, `KWARG_PROVEN`, `EXEMPT`, `MIRRORED`), real-corpus gate tests in `tests/test_qstats_gate.py`, conftest census print; old line gate deleted |
| 08 | needles per shape, behavioural kwarg pins + calibration rows, four neuter-drill records |
| 09 | D-10 table, D-11 census SQL, WINDOWS 5/9 fixed, TODOS 0f closed |
| 10 | VERSION / package.json bump, unified CHANGELOG entry, OPEN-2 decision record |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs / TODOS.md -> public repo | world-readable on push |
| founder -> PRODUCTION database | the census SQL is for the founder to run; this plan never connects |
| executor -> public PyPI | one unauthenticated read of public package metadata |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-17 | Information disclosure | SUMMARY / TODOS / WINDOWS text | high | mitigate | table-level SQL only; no strategy name/id, project ref, home path, username or credential; planning-hygiene check before commit |
| T-166-18 | Tampering (PROD data write) | census SQL | high | mitigate | every statement is SELECT; no recompute, backfill or enqueue is performed (D-11); OPEN-2 is a founder decision recorded in plan 10 |
| T-166-19 | Repudiation (undisclosed value change) | D-10 table | medium | mitigate | before/after MEASURED from two code trees; golden phase diff asserted to be exactly the two disclosed keys |
| T-166-20 | Tampering (ledger clobber) | gsd-tools windows handler | medium | mitigate | byte copy before the call; git diff after; collateral reverted by hand |
</threat_model>

<verification>
- D-10 table measured from two trees; canonical-trigger row asserted; golden phase diff exactly two keys.
- D-11 SQL present and read-only; caveats printed.
- WINDOWS 5 and 9 fixed; TODOS 0f closed; hygiene clean.
</verification>

<success_criteria>
SC-5 met: every persisted value the phase corrects is named with before and after, measured.
SC-1 met: the library decision and its evidence are stated. The two ledgers carrying this defect are closed.
</success_criteria>

<output>
Create `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-09-SUMMARY.md`
containing, in this order:
1. `## Library decision (SC-1, D-01, D-02)` - quantstats stays at 0.0.81 (PyPI version read at execution), kwarg-closed for the named leaves + drawdown_details exempt, and the live parity oracle; OPEN-1 moot with the research evidence; removal belongs to Phase 165.
2. `## Before / after (D-10)` - the measured table, BEFORE_SHA, and the golden phase diff.
3. `## Findings recorded, not changed (D-08)` - F-1 and the recovery_factor numerator note; F-2 / F-3 / F-4 fixed under D-16 / D-15 / D-17 with pointers to their rows; plus the research notes routed to Phase 165 and changing nothing here (scipy is now imported directly by services/metrics.py but pinned only transitively via quantstats; requirements.in pins pandas 2.2.3 while the lock carries 3.0.3).
4. `## Surfaces (D-13)` - the reader list.
5. `## Census SQL for the founder (D-11, read-only)` - the SQL block and caveats, stating that no PRODUCTION row was recomputed and that OPEN-2 is recorded in plan 10.
6. `## Gate evidence` - pointers to 166-07-SUMMARY.md's census and 166-08-SUMMARY.md's neuter drills, and the 30-vs-9 reconciliation.
7. `## Ledgers` - WINDOWS 5/9 and TODOS 0f closures, with the refuted kwarg-closable claim.
Verification frontmatter, if any, uses `verified_at_sha` + `drift_subjects` only.

**SUMMARY hygiene gate (mandatory, before the SUMMARY commit).** `.planning/` is public. Quote every
captured output repo-relative: an interpreter / venv / site-packages prefix becomes `<venv>/`, the
session scratchpad (where the two extracted trees and JSONs live) becomes `<scratchpad>/`, any other
absolute path becomes its repo-relative form. Then, from the worktree root: `git add` the SUMMARY (the
checker enumerates `git ls-files`, so an unstaged new file is invisible to it), link `node_modules`
per D-19 if absent, run `./node_modules/.bin/tsx scripts/check-planning-hygiene.ts`, then remove the
link when it is a symlink. A non-zero exit or any violation line blocks the commit: fix the text,
re-stage, re-run until clean.
</output>
