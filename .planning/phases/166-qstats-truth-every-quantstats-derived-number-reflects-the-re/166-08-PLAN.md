---
phase: 166-qstats-truth-every-quantstats-derived-number-reflects-the-re
plan: 08
type: execute
wave: 7
depends_on: ["166-07"]
files_modified:
  - analytics-service/tests/test_qstats_gate.py
autonomous: true
requirements: [SC-3, TODOS-0f, WINDOWS-9]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-14: the gate is PROVEN able to fail - permanently, by red/green needle fixtures per call shape that run on every CI run, AND once against the real services/metrics.py by a neuter drill per shape (attribute dispatch, bare rolling_greeks in _rolling_alpha_beta, a direct unclosed call, a new importer) observed RED naming the site, then restored from a byte backup"
    - "D-03 / research F-5: every KWARG_PROVEN leaf is pinned by a BEHAVIOURAL preparer-spy test on the trigger fixture - never the literal keyword text; payoff_ratio and cvar ship as expected-RED calibration rows proving the spy can see a non-honouring function"
    - "no neutered state is ever committed: services/ is clean at every commit, and every drill restore is confirmed by cmp against a byte backup (never git checkout / restore / stash)"
    - "the SUMMARY passed the planning-hygiene check while staged, before it was committed"
  artifacts:
    - path: analytics-service/tests/test_qstats_gate.py
      provides: "RED + GREEN needles per shape, importer needle, behavioural kwarg pins, calibration rows"
      contains: "def test_qstats_gate_calibration_non_honouring_functions_do_reach_a_preparer"
  key_links:
    - from: "the needle tests"
      to: "analytics-service/tests/qstats_gate.py scan_source / scan_tree"
      via: "the SAME scanner the real-corpus gate uses (plan 07), so a needle that passes proves the production gate's rule"
      pattern: "scan_source\\("
    - from: "the behavioural pins"
      to: "KWARG_PROVEN"
      via: "parametrized over sorted(KWARG_PROVEN), so an allowlisted leaf with no pin cannot exist"
      pattern: "KWARG_PROVEN"
---

<objective>
Prove the AST gate from plan 07 able to fail at every call shape it claims to see: permanent RED/GREEN
needle fixtures per shape, behavioural preparer-spy pins for every allowlisted leaf with calibration
rows proving the spy detects a non-honouring function, and a recorded neuter drill per shape against
the REAL `services/metrics.py` (D-14 "proven able to fail", D-03 / F-5 behavioural allowlist; SC-3).

Purpose: a gate whose red path has never been observed is the defect this repo's gate-integrity work
exists to remove; the old RANK-05 gate reported clean over an open surface.
Output: needle, pin and calibration tests in `test_qstats_gate.py`, and four neuter-drill records in
the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-CONTEXT.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md
Also read `166-07-SUMMARY.md` in this phase directory once the plan it belongs to has executed (it records the scanner API and census; it does not exist at planning time, so it is not an @-reference).
@analytics-service/tests/test_raw_5xx_census.py

**Environment.** Python ONLY from the worktree's `analytics-service/` directory with the MAIN
checkout's venv interpreter resolved at run time (capture `git worktree list --porcelain` into `QZ_WT`,
first `worktree` path + `/analytics-service/.venv/bin/python` -> `QZ_VENV_PY`; never written into a file;
exit 127 = FAIL). No database/network/credential. No SPEC; spec-less probe fallback skipped. Drills use
byte backups in the session scratchpad (plan-unique names), re-taken after every edit, verified with
`cmp` - never `git checkout --`, `git restore` or `git stash` (they destroy uncommitted work).
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): RED and GREEN needles per call shape through the production scan_source, plus the uncovered-importer needle through scan_tree</name>
  <files>analytics-service/tests/test_qstats_gate.py</files>
  <read_first>
    - analytics-service/tests/qstats_gate.py (plan 07) - `scan_source`, `scan_tree`, `Violation`, `KWARG_PROVEN`
    - analytics-service/tests/test_raw_5xx_census.py - the positive/negative needle pairs, incl. the commented-out and string-literal negatives
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - Pattern 3 "Proven able to fail"
  </read_first>
  <behavior>
    - RED needles (each: `scan_source` on a small source string returns a Violation naming module, function and quantstats name): a direct unclosed call; `prepare_returns=True`; `prepare_returns=<variable>`; `**kwargs` forwarding; `cvar(..., prepare_returns=False)` (not allowlisted); `payoff_ratio(..., prepare_returns=False)`; attribute dispatch over a module-level table (the reason lists the dispatched names); an alias `fn = qs.stats.greeks`; `from quantstats.stats import greeks`; a `qs.utils._prepare_returns` reference; a function named `_rolling_alpha_beta` calling `rolling_greeks`; `from quantstats import stats as S` then `S.sharpe(r)`
    - RED importer needle: `scan_tree` over a temporary directory (pytest `tmp_path`) holding `services/metrics.py` (clean) plus `services/other.py` containing `import quantstats` reports an "uncovered importer" violation naming services/other.py
    - GREEN needles: a clean module calling only KWARG_PROVEN leaves with `prepare_returns=False` and `drawdown_details(dd)`; a commented-out unclosed call; a string literal mentioning an unclosed call
  </behavior>
  <action>
    Per D-14 ("proven able to fail"):
    1. Add the needle tests, named with a `needle` token, using the house needle shape (one positive and one negative per shape against the SAME `scan_source` the real-corpus gate uses). Each needle is its OWN collected case: either its own test function or its own `pytest.mark.parametrize` id (one id per needle source string, never several needles asserted inside one case), so each RED needle, the importer needle and each GREEN needle passes or fails on its own and the verify floor of 16 (12 RED + importer + 3 GREEN) counts collected cases that exist by construction. Every case id or function name carries `needle` (the importer case may carry `importer`) so `-k "needle or importer"` selects all 16.
    2. Run the test file; full suite GREEN.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_qstats_gate.py -q -k "needle or importer"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or " failed" in the summary, or fewer than 16 passed (12 RED needles + importer + 3 GREEN)</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest -q -n auto</automated>
    <fails_when>non-zero exit, or " failed" / " error" in the final summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '_rolling_alpha_beta' analytics-service/tests/test_qstats_gate.py` prints at least 1
    - `grep -c 'payoff_ratio' analytics-service/tests/test_qstats_gate.py` prints at least 1
    - both verify commands exit 0
  </acceptance_criteria>
  <done>Every call shape the gate claims to see has a permanent RED needle and a GREEN counterpart running on every CI run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: behavioural kwarg pins with expected-RED calibration rows, and a recorded neuter drill per shape against the real services/metrics.py</name>
  <files>analytics-service/tests/test_qstats_gate.py</files>
  <read_first>
    - analytics-service/tests/qstats_gate.py - `KWARG_PROVEN`
    - analytics-service/tests/test_metrics.py - `_rank05_trigger_series` (import it for the pins) and `test_rank05_drawdown_details_is_heuristic_free`
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - "Code Examples: Behavioural kwarg pin"
  </read_first>
  <behavior>
    - test_qstats_gate_kwarg_proven_leaf_never_reaches_a_preparer (parametrized over sorted KWARG_PROVEN): spying `quantstats.utils._prepare_returns` / `_prepare_prices` via monkeypatch and calling the leaf on `_rank05_trigger_series()` with `prepare_returns=False` records zero preparer calls
    - test_qstats_gate_calibration_non_honouring_functions_do_reach_a_preparer (parametrized over payoff_ratio, cvar): the same spy records at least one call - proving the pin can see a function that drops the keyword
  </behavior>
  <action>
    Per D-14 and D-03 / F-5 (behavioural allowlist):
    1. Add the two parametrized behavioural tests to test_qstats_gate.py.
    2. Neuter drill against the REAL `services/metrics.py`, one shape at a time, recorded verbatim in the SUMMARY (the gate's failing line naming the site, then the restored GREEN line): (a) reintroduce an attribute dispatch over the stats namespace inside `compute_qstats_scalars`; (b) add a bare quantstats `rolling_greeks` call inside `_rolling_alpha_beta`; (c) add a direct call to the quantstats `sharpe` function without the keyword inside `compute_all_metrics`; (d) create `services/_q166_neuter_probe.py` containing only `import quantstats` (uncovered importer). Before each: `cp services/metrics.py` to a byte backup in the session scratchpad with a plan-unique name; after observing RED, `cp` the backup back and confirm with `cmp` (for (d) delete the probe file and confirm `git status --porcelain` shows it gone). Never use git checkout/restore/stash. Never commit a neutered state.
    3. Full test file and full suite GREEN; `git status --porcelain -- services` empty at commit time.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_qstats_gate.py -q -k "kwarg_proven or calibration"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or " failed" in the summary, or fewer than 9 passed (7 pins + 2 calibration rows)</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && test -z "$(git status --porcelain -- services)" && "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>non-zero exit (including a dirty services/ tree left by a drill), or " failed" in the pytest summary, or mypy lacks "Success: no issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY carries four neuter-drill records (a)-(d), each with the RED line naming the site and the restored GREEN line, and the `cmp` confirmation
    - `grep -c 'payoff_ratio' analytics-service/tests/test_qstats_gate.py` prints at least 2 (needle + calibration row)
    - `test -e analytics-service/services/_q166_neuter_probe.py` is false
    - both verify commands exit 0
  </acceptance_criteria>
  <done>Every allowlisted leaf is behaviourally proven with calibration rows showing the pin can fail, and four real-file neuter drills are recorded.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind |
|---|---|
| RED + GREEN needles per shape, importer needle | tests in `analytics-service/tests/test_qstats_gate.py` |
| behavioural kwarg pins + payoff_ratio / cvar calibration rows | tests in `analytics-service/tests/test_qstats_gate.py` |
| four neuter-drill records | SUMMARY evidence |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future code change -> persisted metrics | a reintroduced unclosed quantstats call would silently re-open the heuristic; the gate is the control |
| planning docs -> public repo | the SUMMARY quotes drill output and is world-readable once pushed |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-13 | Tampering (control that cannot fail) | the region gate | high | mitigate | permanent RED needles per shape; real-file neuter drills recorded RED then restored |
| T-166-14 | Spoofing (keyword that lies) | allowlist | high | mitigate | allowlist by function name with behavioural preparer-spy pins; payoff_ratio / cvar calibration rows prove the spy detects non-honouring functions |
| T-166-16 | Tampering (destructive drill) | neuter drills on metrics.py | medium | mitigate | byte backups + cmp; no git checkout/restore/stash; dirty-tree check before commit |
| T-166-03 | Information disclosure | SUMMARY quoting drill output | medium | mitigate | repo-relative quoting (scratchpad paths as `<scratchpad>/`); staged planning-hygiene check before the SUMMARY commit |
</threat_model>

<verification>
- Needles, pins and calibration rows green.
- Four neuter drills recorded RED then restored GREEN, services/ clean at commit.
- Full suite + mypy strict green.
</verification>

<success_criteria>
SC-3 met: the gate was observed RED naming a reintroduced site at every shape (dispatch, `_rolling_alpha_beta`,
direct call, new importer), and every allowlisted leaf is behaviourally proven.
</success_criteria>

<output>
Create `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-08-SUMMARY.md`
with: the four neuter-drill records, and the list of allowlisted leaves with the pin that proves each.

**SUMMARY hygiene gate (mandatory, before the SUMMARY commit).** `.planning/` is public. Quote every
captured output repo-relative: an interpreter / venv / site-packages prefix becomes `<venv>/`, the
session scratchpad (where the drill backups live) becomes `<scratchpad>/`, any other absolute path
becomes its repo-relative form - never paste a traceback's absolute file paths verbatim. Then, from the
worktree root: `git add` the SUMMARY (the checker enumerates `git ls-files`, so an unstaged new file is
invisible to it), link `node_modules` per D-19 if absent, run
`./node_modules/.bin/tsx scripts/check-planning-hygiene.ts`, then remove the link when it is a
symlink. A non-zero exit or any violation line blocks the commit: fix the text, re-stage, re-run until
clean.
</output>
