---
phase: 166-qstats-truth-every-quantstats-derived-number-reflects-the-re
plan: 06
type: execute
wave: 5
depends_on: ["166-05"]
files_modified:
  - analytics-service/services/metrics.py
  - analytics-service/tests/test_metrics.py
  - analytics-service/tests/fixtures/golden_252d_expected.json
autonomous: true
requirements: [SC-2, SC-4, SC-5, WINDOWS-5, WINDOWS-9]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-03 applied to the rolling leg: rolling_greeks is inline - the keyword is not forwarded to quantstats' benchmark preparer - and no quantstats greeks / r_squared / rolling_greeks reference remains in services/metrics.py"
    - "D-06 / D-17: rolling_greeks is replaced by one inline _rolling_greeks pass closing BOTH legs; rolling beta is unchanged in formula; rolling alpha is the WINDOWED intercept mean_w(r) - beta_t*mean_w(b), still unannualized (Phase 34 note kept); H-0711 one-pass and H-0726.3 WARNING + ([], []) contracts kept"
    - "SC-4 on the benchmark trigger: the rolling beta last point equals the rolling cov/var of the raw pair over the last 90 rows, observed RED against the pre-mirror code first (through _rolling_alpha_beta, the production entry)"
    - "D-10: the only golden key path this plan moves is sibling.rolling_alpha; the key-path diff against the phase-base golden is exactly {metrics_json.metrics_json.probabilistic_sharpe_ratio, sibling.rolling_alpha}; rolling_beta, alpha, beta and r_squared do not move"
    - "the SUMMARY passed the planning-hygiene check while staged, before it was committed"
  artifacts:
    - path: analytics-service/services/metrics.py
      provides: "_rolling_greeks; _rolling_alpha_beta re-pointed; missing-columns dead branch removed"
      contains: "def _rolling_greeks"
    - path: analytics-service/tests/test_metrics.py
      provides: "rolling beta / windowed-intercept / rolling parity tests, re-targeted rolling fault tests"
      contains: "test_q166_rolling_alpha_is_the_windowed_intercept"
    - path: analytics-service/tests/fixtures/golden_252d_expected.json
      provides: "sibling.rolling_alpha updated per D-17 (single key path)"
  key_links:
    - from: _rolling_alpha_beta
      to: _rolling_greeks
      via: "exactly one call per analytics run -> sibling_kinds rolling_alpha / rolling_beta -> strategy_analytics_series -> RollingAlphaBetaChart"
      pattern: "_rolling_greeks\\("
    - from: _rolling_greeks
      to: "_align_benchmark_like_qs / _prepared_returns_no_guess (plans 05 / 03)"
      via: "0.0.81's frame built without the guess on both legs"
      pattern: "_align_benchmark_like_qs\\("
---

<objective>
Close the heuristic on the ROLLING benchmark leg - `_rolling_alpha_beta`'s `rolling_greeks` call (D-06) -
and fix, with disclosure, the full-sample-mean "rolling alpha" research found on the same site (D-17,
F-4).

Purpose: rolling alpha/beta are RENDERED (`RollingAlphaBetaChart`) - the most user-facing numbers in
this phase. Research Q2: quantstats runs the benchmark through `_prepare_benchmark -> _prepare_returns`
unconditionally, so no keyword closes it. Built on plan 05's `_align_benchmark_like_qs` and plan 03's
`_prepared_returns_no_guess`.
Output: one module function, the call site re-pointed, rolling tests, re-targeted fault tests, one dead
branch + its test deleted together, one golden key path moved with a D-10 row.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-CONTEXT.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-PATTERNS.md
Also read `166-05-SUMMARY.md` in this phase directory once the plan it belongs to has executed (it records `_align_benchmark_like_qs` and the benchmark fixtures this plan reuses; it does not exist at planning time, so it is not an @-reference).
@analytics-service/services/metrics.py
@analytics-service/tests/test_metrics.py

**Environment.** Python ONLY from the worktree's `analytics-service/` directory, with the MAIN
checkout's venv interpreter resolved at run time (capture `git worktree list --porcelain` into `QZ_WT`,
first `worktree` path + `/analytics-service/.venv/bin/python` -> `QZ_VENV_PY`; never written into a
file; exit 127 = FAIL). No database, network, `uvicorn main:app` or credential. No SPEC; spec-less probe
fallback skipped. Drills use byte backups in the session scratchpad (plan-unique names) - never
`git checkout --`, `git restore` or `git stash`.

**0.0.81 source to mirror:** `quantstats.stats.rolling_greeks` (read with `inspect.getsource`; research
§Q3 rolling_greeks row, §Q5 F-4). Measured (research Q4, re-measure): on `_q166_benchmark_trigger()`
rolling beta last point -0.08887237598396791 -> -0.7554623350323423; rolling alpha last point
-0.0006752004065154814 -> (windowed intercept, measure).

**Golden layout and diff base.** Rolling series live at key paths `sibling.<kind>` in
`golden_252d_expected.json` (values rounded as the serializer writes them). Golden checks compare
FLATTENED key paths against the PHASE-BASE golden (the parent of the first commit since
`git merge-base HEAD origin/main` touching `analytics-service/services/metrics.py`), never HEAD, so
the verdict is the same before and after the golden change commits.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): RED-first rolling tests through _rolling_alpha_beta, then one inline _rolling_greeks pass wired into it, fault contracts re-targeted, dead branch removed with its test, and the single disclosed golden key-path move</name>
  <files>analytics-service/services/metrics.py, analytics-service/tests/test_metrics.py, analytics-service/tests/fixtures/golden_252d_expected.json</files>
  <read_first>
    - analytics-service/services/metrics.py - `_rolling_alpha_beta` (signature `(returns, benchmark, window=90) -> tuple[list[SeriesPoint], list[SeriesPoint]]`, guards, Phase 34 NOTE, H-0726.3 WARNING "rolling_greeks failed ...", the missing-columns branch), `_rolling_alpha`, `_rolling_beta` docstrings ("via qs.stats.rolling_greeks"), `_finalize_rolling`, `_rolling_sharpe` / `_rolling_correlation` (house rolling style), plan 05's `_align_benchmark_like_qs`
    - analytics-service/tests/test_metrics.py - `test_rolling_alpha_beta_single_rolling_greeks_call`, `test_rolling_alpha_beta_logs_warning_on_qs_failure`, `test_rolling_alpha_beta_missing_columns_returns_empty_and_logs`, and the guard tests that must stay unedited (`_aligns_misaligned_series`, `_short_benchmark_returns_empty`, `_none_benchmark_returns_empty`, `_zero_overlap_returns_empty`); plan 05's `_q166_benchmark_trigger`
    - analytics-service/tests/test_metrics_parity.py - `test_metrics_parity_full` (how `actual` and `result.sibling_kinds` are built)
    - analytics-service/tests/fixtures/regen_golden.py - serializer (do not run the script)
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - §Q3 rolling_greeks row, §Q5 F-4
  </read_first>
  <behavior>
    - test_q166_rolling_beta_is_the_rolling_regression_slope: on `_q166_benchmark_trigger()`, the last beta point `_rolling_alpha_beta(r, b)` returns equals in-test `cov/var` of the raw pair over the last 90 rows, compared at the precision `_finalize_rolling` writes (read its rounding and assert within half a unit of the last written digit) - RED on the pre-mirror code (live -0.0889 vs -0.755)
    - test_q166_rolling_alpha_is_the_windowed_intercept: every alpha point `_rolling_alpha_beta(r, b)` returns on the benchmark trigger equals `mean_w(r) - beta_t * mean_w(b)` over the same 90-row window of the prepared frame, at the written precision; the docstring states the pre-fix full-sample-mean form and that the value is unannualized (Phase 34) - RED on the pre-mirror code
    - re-targeted test_rolling_alpha_beta_single_rolling_greeks_call: spies `metrics_module._rolling_greeks`; exactly 1 call per `compute_all_metrics(golden_returns, benchmark_returns)`; both sibling kinds non-empty
    - re-targeted test_rolling_alpha_beta_logs_warning_on_qs_failure: `_rolling_greeks` monkeypatched to raise -> `([], [])` and a WARNING whose message contains `rolling_greeks`
  </behavior>
  <action>
    Per D-06, D-17, D-09 (RED first), D-10 (H-0711 / H-0726 contracts kept):
    1. Write the two trigger-based behaviour tests through the production entry `_rolling_alpha_beta`; observe both RED on the current code and record the observed values in the SUMMARY.
    2. Add `_rolling_greeks(returns, benchmark, window) -> pd.DataFrame` with columns `alpha`, `beta`: build `df = DataFrame({"returns": P(returns), "benchmark": _align_benchmark_like_qs(benchmark, returns.index)}).fillna(0)` (0.0.81's frame); `corr = df.rolling(window).corr().unstack()["returns"]["benchmark"]`; `std = df.rolling(window).std()`; `beta = corr * std["returns"] / std["benchmark"].replace(0, np.nan)` - 0.0.81's beta expression order (so benign beta stays bit-identical); `alpha = df["returns"].rolling(window).mean() - beta * df["benchmark"].rolling(window).mean()` (D-17 windowed intercept; replaces 0.0.81's full-sample means). Docblock: WHY INLINE (research Q2), MATH PARITY for beta, D-17 disclosure for alpha, the Phase 34 NOTE (unannualized) moved here.
    3. In `_rolling_alpha_beta`, replace the quantstats rolling call with `_rolling_greeks(aligned_returns, aligned_benchmark, window)` inside the existing try, keeping the WARNING text containing `rolling_greeks`. Delete the now-unreachable missing-columns branch AND `test_rolling_alpha_beta_missing_columns_returns_empty_and_logs` in the SAME commit, and record the deletion in the SUMMARY (159 Deviation-2 rule: never keep a test that cannot fail; the contract it guarded - quantstats column drift - no longer exists because the frame is built here). Update the `_rolling_alpha_beta`, `_rolling_alpha`, `_rolling_beta` docstrings so they name the inline helper.
    4. Re-target the two remaining rolling tests per the behaviour list. Leave the four guard tests unedited.
    5. Golden (D-10, one key path): produce the new `sibling.rolling_alpha` through the same path `test_metrics_parity_full` uses (`result.sibling_kinds["rolling_alpha"]`), write ONLY that key into golden_252d_expected.json with a throwaway scratchpad script using regen_golden.py's serializer (indent=2, sort_keys=True, same trailing newline); do not run regen_golden.py. Prove scope with the key-path diff verify below. Record old/new last point and point count in the SUMMARY as a D-10 row (every benchmarked strategy's rendered rolling alpha changes - say so plainly). If rolling_alpha does NOT move at the written precision, stop and report it - that contradicts research F-4's measurement and is investigated, not re-baselined.
    6. GREEN: new, re-targeted and guard tests, the parity file, the full suite and mypy strict.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py -q -k "q166_rolling or rolling_alpha_beta"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or " failed" in the summary, or fewer than 8 passed (2 new + 2 re-targeted + 4 guard tests)</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && QZ_BASE="$(git merge-base HEAD origin/main)" && QZ_LOG="$(git log --reverse --format=%H "$QZ_BASE"..HEAD -- services/metrics.py)" && QZ_FIRST="$(printf '%s\n' "$QZ_LOG" | head -1)" && test -n "$QZ_FIRST" && git show "$QZ_FIRST^:analytics-service/tests/fixtures/golden_252d_expected.json" > "${TMPDIR:-/tmp}/q166-06-golden-phase-base.json" && "$QZ_VENV_PY" -c "import json,sys; fl=lambda x,p='': [kv for k,v in x.items() for kv in fl(v, p+'.'+k if p else k)] if isinstance(x,dict) else [(p,x)]; a=dict(fl(json.load(open(sys.argv[1])))); b=dict(fl(json.load(open('tests/fixtures/golden_252d_expected.json')))); moved=sorted(k for k in set(a)|set(b) if a.get(k)!=b.get(k)); print('golden key paths moved since phase base:',moved); sys.exit(0 if moved==['metrics_json.metrics_json.probabilistic_sharpe_ratio','sibling.rolling_alpha'] else 1)" "${TMPDIR:-/tmp}/q166-06-golden-phase-base.json"</automated>
    <fails_when>non-zero exit (including an empty QZ_FIRST), or the printed "golden key paths moved since phase base:" list is anything other than exactly ['metrics_json.metrics_json.probabilistic_sharpe_ratio', 'sibling.rolling_alpha'] - same verdict before and after the commit</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -c "import ast,sys; t=ast.parse(open('services/metrics.py').read()); bad=[(n.attr,n.lineno) for n in ast.walk(t) if isinstance(n,ast.Attribute) and ast.unparse(n.value)=='qs.stats' and n.attr in {'greeks','r_squared','rolling_greeks'}]; print('open benchmark nodes:',bad); sys.exit(1 if bad else 0)" && "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>non-zero exit, or "open benchmark nodes:" non-empty, or " failed" in the pytest summary, or mypy lacks "Success: no issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY records RED then GREEN for both trigger-based rolling tests, the deletion of `test_rolling_alpha_beta_missing_columns_returns_empty_and_logs` with its dead branch, and the rolling_alpha D-10 row
    - `grep -c '^def _rolling_greeks(' analytics-service/services/metrics.py` prints 1
    - `grep -c 'def test_rolling_alpha_beta_missing_columns_returns_empty_and_logs' analytics-service/tests/test_metrics.py` prints 0
    - the key-path diff command printed `golden key paths moved since phase base: ['metrics_json.metrics_json.probabilistic_sharpe_ratio', 'sibling.rolling_alpha']`
    - all three verify commands exit 0
  </acceptance_criteria>
  <done>rolling alpha/beta come from one inline pass closed on both legs, the trigger tests went RED then GREEN, the fault contracts survive, and exactly one new golden key path moved, disclosed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: full-precision rolling parity and windowed-intercept proofs on _rolling_greeks for benign and calendar-mismatch fixtures</name>
  <files>analytics-service/tests/test_metrics.py</files>
  <read_first>
    - analytics-service/services/metrics.py - `_rolling_greeks` (Task 1), `_rolling_alpha_beta` (how it inner-joins before calling it)
    - analytics-service/tests/test_metrics.py - `_q166_calendar_mismatch`, `_q166_benchmark_trigger` (plan 05), the Task 1 rolling tests
  </read_first>
  <behavior>
    - test_q166_parity_rolling_beta_matches_live_quantstats: on (golden_returns, benchmark_returns) and `_q166_calendar_mismatch()` inner-joined as `_rolling_alpha_beta` does, every non-NaN point of `_rolling_greeks(r, b, 90)["beta"]` equals live `qs.stats.rolling_greeks(...)["beta"]` at rel 1e-12 (full precision, before any rounding)
    - test_q166_rolling_greeks_alpha_is_the_windowed_intercept_full_precision: on the benchmark trigger AND (golden_returns, benchmark_returns), every non-NaN point t of `_rolling_greeks(r, b, 90)["alpha"]` equals `mean_w(r) - beta_t * mean_w(b)` over the same 90-row window of the prepared frame at rel 1e-9
    - test_q166_rolling_greeks_alpha_differs_from_the_full_sample_form: on (golden_returns, benchmark_returns) at least one alpha point differs from live `qs.stats.rolling_greeks(...)["alpha"]` by more than 1e-12 (anti-vacuity: the D-17 change is real on the golden pair)
  </behavior>
  <action>
    Per D-06 (beta parity with 0.0.81), D-17 (windowed intercept), D-08:
    1. Add the three tests calling `_rolling_greeks` directly at full precision (Task 1's tests go through the rounded production output; these pin the unrounded math).
    2. Run them. A beta parity miss is a mirror defect: fix operation order in services/metrics.py only if a test proves it, record the change, never loosen 1e-12. If anything in services/metrics.py changes, re-run the golden key-path diff from Task 1.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py -q -k "q166_parity_rolling or q166_rolling_greeks"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or " failed" in the summary, or fewer than 3 passed</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py tests/test_metrics_parity.py -q</automated>
    <fails_when>non-zero exit, or " failed" / " error" in the summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def test_q166_parity_rolling_beta_matches_live_quantstats' analytics-service/tests/test_metrics.py` prints 1
    - `grep -c '^def test_q166_rolling_greeks_alpha_differs_from_the_full_sample_form' analytics-service/tests/test_metrics.py` prints 1
    - both verify commands exit 0
  </acceptance_criteria>
  <done>Rolling beta is bit-equal to live quantstats on benign and calendar-mismatch input at full precision, and rolling alpha is proven to be the windowed intercept and to differ from the full-sample form.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind |
|---|---|
| `_rolling_greeks(returns, benchmark, window)` | mirror function (D-06, D-17) |
| `_rolling_alpha_beta` re-pointed; missing-columns branch removed | changed function |
| `test_q166_rolling_*`, `test_q166_parity_rolling_beta_*`, `test_q166_rolling_greeks_*` | tests |
| re-targeted rolling x2 fault tests; deleted `test_rolling_alpha_beta_missing_columns_returns_empty_and_logs` | test changes |
| golden `sibling.rolling_alpha` | fixture bytes (D-10 row) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark feed (BTC daily) -> worker | the only benchmark; the benchmark-leg closure is correctness-by-construction |
| worker -> rendered surfaces | rolling alpha/beta (RollingAlphaBetaChart) are shown to users |
| planning docs -> public repo | the SUMMARY quotes test output and is world-readable once pushed |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-11 | Tampering (misleading rendered series) | rolling alpha | high | mitigate | D-17 windowed intercept anchored to an in-test formula at full and written precision; disclosed D-10 row; rolling beta parity kept at 1e-12 |
| T-166-02 | Denial of service (NaN/inf to JSONB) | rolling outputs | medium | mitigate | `_finalize_rolling` drops non-finite points |
| T-166-09 | Tampering (fault masking) | rolling failure envelope | low | mitigate | fault injected into the inline helper; WARNING + ([], []) asserted; the dead-branch test deleted only together with its branch |
| T-166-08 | Repudiation (silent value change) | golden rolling_alpha | medium | mitigate | flattened key-path diff against the phase-base golden proves exactly the two disclosed key paths moved |
| T-166-03 | Information disclosure | SUMMARY quoting tracebacks | medium | mitigate | repo-relative quoting; staged planning-hygiene check before the SUMMARY commit |
</threat_model>

<verification>
- Trigger-based rolling tests: RED recorded before, GREEN after.
- Rolling beta parity with live 0.0.81 at 1e-12, including the calendar-mismatch reindex branch; rolling alpha windowed intercept proven.
- AST probe: no quantstats greeks / r_squared / rolling_greeks reference remains.
- Golden: exactly the PSR and rolling_alpha key paths moved since the phase base.
- Full suite + mypy strict green.
</verification>

<success_criteria>
The rolling benchmark leg is closed on both legs; F-4 full-sample rolling alpha is fixed with a
before/after row; rolling beta is bit-identical to live quantstats.
</success_criteria>

<output>
Create `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-06-SUMMARY.md`
with RED-first evidence per new test, the D-10 rows (benchmark trigger rolling beta last / rolling alpha
last; golden rolling_alpha old/new last point and point count), the deleted test + branch, and
confirmation that the rolling_beta / alpha / beta / r_squared golden key paths did not move.

**SUMMARY hygiene gate (mandatory, before the SUMMARY commit).** `.planning/` is public. Quote every
captured output repo-relative: an interpreter / venv / site-packages prefix becomes `<venv>/`, the
session scratchpad becomes `<scratchpad>/`, any other absolute path becomes its repo-relative form -
never paste a traceback's absolute file paths verbatim. Then, from the worktree root: `git add` the
SUMMARY (the checker enumerates `git ls-files`, so an unstaged new file is invisible to it), link
`node_modules` per D-19 if absent, run `./node_modules/.bin/tsx scripts/check-planning-hygiene.ts`,
then remove the link when it is a symlink. A non-zero exit or any violation line blocks the commit:
fix the text, re-stage, re-run until clean.
</output>
