---
phase: 166-qstats-truth-every-quantstats-derived-number-reflects-the-re
plan: 10
type: execute
wave: 9
depends_on: ["166-09"]
files_modified:
  - VERSION
  - package.json
  - CHANGELOG.md
autonomous: false
requirements: [SC-5, SC-1]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every gate the phase touched is green at the release SHA, measured in one sweep: full analytics-service suite, mypy strict, the qstats AST gate with its census printed, the touched vitest files, tsc, eslint on touched TS, planning hygiene, and the PLAN anchor verifier"
    - "Release (repo CHANGELOG discipline): VERSION and package.json carry the SAME 4-digit string, bumped on MEANING - user-visible metric values change (rendered rolling alpha for every benchmarked strategy, alpha/beta for NaN-bearing strategies, persisted PSR and trigger-shaped scalars) - so a minor-level bump over the higher of this branch's and origin/main's VERSION; npm version is never run"
    - "ONE unified CHANGELOG entry covers EVERY commit on the branch: commits enumerated and counted, grouped by theme, each commit mapped to at least one bullet, using the repo's own section vocabulary, naming the D-10 value changes, the recorded-not-changed findings (F-1, recovery_factor numerator), the refuted kwarg-closable claim, and OPEN-2 as founder-owned"
    - "No commit message and no PR text contains a CI-skip trailer token, not even in a sentence denying it"
    - "OPEN-2 (recompute existing PRODUCTION rows) is presented as a founder-owned checkpoint:decision; this plan performs NO production write, recompute, backfill or enqueue regardless of the answer (D-11); unattended, it is recorded as PENDING with no option named as a default, so the record can never later read as a decision"
    - "the release commit passes the repo's CI version gate locally: `node scripts/check-version-bump.mjs --self-test` passes and `node scripts/check-version-bump.mjs` reports no defects against origin/main"
    - "the SUMMARY passed the planning-hygiene check while staged, before it was committed"
  artifacts:
    - path: VERSION
      provides: "next minor 4-digit version"
    - path: package.json
      provides: "version field byte-equal to VERSION"
    - path: CHANGELOG.md
      provides: "one unified `## [X.Y.Z.B] - YYYY-MM-DD` entry for Phase 166 at the top"
  key_links:
    - from: VERSION
      to: package.json
      via: "src/__tests__/critical-regressions.test.ts CRITICAL-02 asserts byte equality"
      pattern: "\"version\""
---

<objective>
Run the whole-phase gate sweep at one SHA, write the single `chore(release)` commit (VERSION +
package.json + one unified CHANGELOG entry covering every branch commit), and put OPEN-2 in front of
the founder as a decision this phase does not act on.

Purpose: the repo rule is that every ship writes a CHANGELOG entry in the same commit as the VERSION
bump, built from an enumerated commit checklist, because a step without a mechanism gets skipped.
OPEN-2 is a PRODUCTION data write and is the founder's call (CONTEXT OPEN-2, D-11).
Output: one release commit and a recorded OPEN-2 answer.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-CONTEXT.md
@VERSION
Also read `166-01-SUMMARY.md` .. `166-09-SUMMARY.md` in this phase directory (they exist once plans 01-09 have executed); 166-09-SUMMARY.md carries the D-10 table and the D-11 census SQL this plan cites.

**Environment.** Python from the worktree's `analytics-service/` directory only, with the MAIN
checkout's venv interpreter resolved at run time (`QZ_WT` / `QZ_VENV_PY` as in earlier plans; never
written into a file; exit 127 = FAIL). TS through the D-19 symlinked `node_modules` (created if absent,
removed before every commit when it is a symlink). No database, no credential. No SPEC; spec-less probe
fallback skipped.

**Version at planning time:** `VERSION` = `0.86.0.1` on this branch. Precedent: a phase ship moves the
second component (`0.85.0.1` -> `0.86.0.0` for Phase 167 CREDTRUST). Re-read both this branch's
`VERSION` and `git show origin/main:VERSION` at execution time; the new version is the higher of the two
with the second component incremented and the last two components zeroed (measured 2026-09-24: `origin/main` VERSION was already `0.87.0.0`, so the result is at least `0.88.0.0` — re-read both at execution).
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): whole-phase gate sweep at one SHA, recorded verbatim</name>
  <files>none - read-only sweep; results go to the SUMMARY</files>
  <read_first>
    - CLAUDE.md - "CHANGELOG discipline", "CI gate integrity", "Completion is HISTORICAL" (the banned verification fingerprint keys)
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-VALIDATION.md - the per-criterion map
  </read_first>
  <action>
    Record `git rev-parse HEAD` as SWEEP_SHA, then run and paste the final summary line of each into the SUMMARY: (1) full `pytest -q -n auto` from `analytics-service/` (the census and reconciliation lines must appear in its terminal output); (2) mypy strict; (3) `pytest tests/test_metrics_parity.py tests/test_qstats_gate.py -q`; (4) vitest on `src/lib/queries.percentile-columns.test.ts`, `src/__tests__/csv-finalize-cross-submission-merge.test.ts`, `src/__tests__/metrics-parity.test.ts` and `src/__tests__/critical-regressions.test.ts`; (5) `tsc --noEmit`; (6) `node scripts/verify-plan-anchors.mjs` over this phase's PLAN files; (7) the planning-hygiene check; (8) a grep over this phase directory for the two banned verification fingerprint keys named in CLAUDE.md "Completion is HISTORICAL" (expect zero hits; convert any the verifier later writes to `verified_at_sha` + `drift_subjects`). Any red result stops the plan - report it; do not release on red.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>non-zero exit, or " failed" / " error" in the pytest summary, or mypy lacks "Success: no issues found"</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/queries.percentile-columns.test.ts src/__tests__/csv-finalize-cross-submission-merge.test.ts src/__tests__/metrics-parity.test.ts src/__tests__/critical-regressions.test.ts && ./node_modules/.bin/tsc --noEmit && node scripts/verify-plan-anchors.mjs .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-[0-9][0-9]-PLAN.md && ./node_modules/.bin/tsx scripts/check-planning-hygiene.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), or vitest reports " failed" / "No test files found", or any "error TS" line, or the anchor verifier prints "FAIL:", or a hygiene violation line</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY records SWEEP_SHA and the summary line of each of the eight checks
    - the pytest terminal output quoted in the SUMMARY contains a line starting `qstats-gate census:`
    - both verify commands exit 0
  </acceptance_criteria>
  <done>Every phase gate is green at one recorded SHA.</done>
</task>

<task type="auto">
  <name>Task 2: the single chore(release) commit - VERSION and package.json byte-equal, one unified CHANGELOG entry mapped from an enumerated commit checklist</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <read_first>
    - CLAUDE.md - "CHANGELOG discipline (founder instruction, 2026-09-09)" (the 6-step mechanism, section vocabulary, the deliberately-not-adopted gstack rule) and "CI gate integrity" trap 1 (skip trailer tokens)
    - CHANGELOG.md - the top two entries (format, heading shape `## [X.Y.Z.B] - YYYY-MM-DD — title`: a hyphen between version and date, an EM-DASH before the title; copy the separators byte-for-byte from the top existing entry rather than from this plan, voice)
    - VERSION, package.json (`"version"` field)
    - src/__tests__/critical-regressions.test.ts - CRITICAL-02 (VERSION == package.json version)
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-09-SUMMARY.md - the D-10 table and findings to cite
  </read_first>
  <action>
    Follow CLAUDE.md's mechanism exactly:
    1. `git log "$(git merge-base HEAD origin/main)"..HEAD --oneline` - enumerate and COUNT every commit; keep the list as a checklist in the SUMMARY.
    2. `git diff "$(git merge-base HEAD origin/main)"...HEAD` - read what actually changed.
    3. Group by theme before writing: Fixed (the closed heuristic on the eight scalars, r_squared, scalar greeks, rolling greeks; D-15 fabricated zeros; D-16 PSR; D-17 rolling alpha), Changed (values users will see differ - say plainly which and for whom, citing the D-10 rows), Added (the AST gate + printed census; primitives; byte pins), Removed (the line gate; the dead rolling missing-columns branch and its test), Tests, Notes (recorded-not-changed F-1 and recovery_factor numerator; the refuted kwarg-closable claim; quantstats stays at 0.0.81 and why; no PRODUCTION row recomputed - OPEN-2 is the founder's; the D-11 census SQL lives in 166-09-SUMMARY.md; the SQL RPC's own KPI list out of scope), Root cause (quantstats' `_prepare_returns` / `_prepare_prices` price guess and why the keyword never reached the transitive preparers). Use only the repo's section vocabulary.
    4. Set the new version (context rule; re-read `origin/main:VERSION` and the branch VERSION at execution — at least `0.88.0.0` as measured 2026-09-24) in VERSION and in package.json's `"version"` by direct edit - never `npm version` (it rewrites the lockfile and can normalise to 3 digits). Write ONE entry at the top of CHANGELOG.md, `## [<version>] - <execution date> — QSTATS-TRUTH: every quantstats-derived number reflects the returns it was given`. If an earlier entry for this branch exists, REPLACE it with the unified one.
    5. Cross-check: every commit from step 1 maps to at least one bullet; list the mapping in the SUMMARY. An unmapped commit is a defect - add the bullet.
    6. No credential-shaped literal, home path, username, strategy name or project ref in the entry. No CI-skip trailer token anywhere in the commit message (refer to it only as "the skip trailer" if at all).
    7. Run the CRITICAL-02 test and the repo's CI version gate exactly as CI runs it - `node scripts/check-version-bump.mjs --self-test`, then `node scripts/check-version-bump.mjs` (it diffs `origin/main...HEAD` and reads VERSION, package.json and CHANGELOG.md from the working tree; confirm the script exists and `origin/main` resolves first, since an unreadable base is a MEASURE_FAIL, not a pass). Remove the node_modules link, commit exactly the three files as `chore(release): v<version> — QSTATS-TRUTH` with the attribution lines from the session instructions.
  </action>
  <verify>
    <automated>test "$(tr -d '\n' < VERSION)" = "$(node -p 'require("./package.json").version')" && tr -d '\n' < VERSION | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$' && head -40 CHANGELOG.md | grep -Eq "^## \[$(tr -d '\n' < VERSION | sed 's/\./\\./g')\] - "</automated>
    <fails_when>non-zero exit: VERSION and package.json differ, VERSION is not 4-digit, or the top of CHANGELOG.md has no heading for that version</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/__tests__/critical-regressions.test.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), or " failed" / "No test files found" in the output</fails_when>
    <automated>test -f scripts/check-version-bump.mjs && git rev-parse --verify --quiet origin/main > /dev/null && node scripts/check-version-bump.mjs --self-test && node scripts/check-version-bump.mjs</automated>
    <fails_when>non-zero exit: the script is missing, origin/main is not resolvable (the gate prints MEASURE_FAIL rather than passing), the self-test does not print "SELF-TEST PASSED", or the live run prints any "::error::" defect (version-not-bumped, version-package-mismatch, changelog-missing-entry) instead of "No defects"</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --name-only --format= <release sha>` lists exactly CHANGELOG.md, VERSION, package.json
    - the release commit's message, captured into a variable FIRST so a failing git read cannot be swallowed by a pipeline (`QZ_MSG="$(git log -1 --format=%B RELEASE_SHA)" && test -n "$QZ_MSG"`), contains none of the CI-skip trailer tokens listed in CLAUDE.md: a case-insensitive grep of `$QZ_MSG` for each of the tokens `skip ci`, `ci skip`, `no ci`, `skip actions`, `actions skip`, `skip-checks` finds none
    - the SUMMARY carries the commit checklist with its count and the commit-to-bullet mapping (every commit mapped)
    - `grep -c 'QSTATS-TRUTH' CHANGELOG.md` prints at least 1
    - `test -L node_modules` is false at commit time
    - the SUMMARY quotes the version gate's live output line (`base=... head=... package=... changed=...` followed by "No defects")
    - all three verify commands exit 0
  </acceptance_criteria>
  <done>One release commit carries a byte-equal 4-digit VERSION/package.json bump and a unified CHANGELOG entry that maps every branch commit.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: OPEN-2 - founder decides whether and when to recompute existing PRODUCTION rows (recorded; this phase performs no production write either way)</name>
  <decision>OPEN-2: after this phase merges, should the strategies whose persisted metrics were computed by the old code be recomputed, and when?</decision>
  <context>
    This phase changes CODE only (D-11). Values persisted before the merge stay until each strategy's next
    compute - and ledger venues never re-compute on their own. Per 166-09-SUMMARY.md: the eight dispatched
    scalars are persisted-only (no reader), trigger-shaped strategies carry wrong ratios; every benchmarked
    strategy's rendered rolling alpha changes (D-17); NaN-bearing benchmarked strategies show fabricated
    alpha/beta 0.000 (D-15); PSR changes for all (D-16, persisted-only). The read-only census SQL in
    166-09-SUMMARY.md measures each population. A recompute is a PRODUCTION data write visible in users'
    rows - the founder's call, and not a precondition for merging this code.
  </context>
  <options>
    <option id="no-recompute-now">
      <name>No recompute in this phase; values refresh on each strategy's next natural compute</name>
      <pros>No production write; zero operational risk; matches D-11 exactly</pros>
      <cons>Ledger venues never recompute on their own, so their old rolling alpha / greeks persist indefinitely</cons>
    </option>
    <option id="route-recompute-to-a-phase">
      <name>Founder runs the census SQL, then a NEW phase (added via /gsd-phase) owns an enqueue of recomputes for the measured populations</name>
      <pros>Corrects the user-facing rolling alpha and fabricated-zero greeks everywhere; a phase gives it a plan, review and a gate</pros>
      <cons>A production write campaign with its own review; needs the census numbers first</cons>
    </option>
  </options>
  <resume-signal>Reply "no-recompute-now" or "route-recompute-to-a-phase" (optionally with the census counts). The answer is recorded verbatim in 166-10-SUMMARY.md; neither answer causes this plan to write to any database. With no answer, the record is "pending" and names no default.</resume-signal>
  <acceptance_criteria>
    - 166-10-SUMMARY.md records the founder's answer verbatim, or, if running unattended, records exactly "OPEN-2: pending - founder decision not taken. Current state equals no recompute: no production write was performed." and names NO option as a default or as chosen, so the record cannot later be read as a decision
    - no command in this plan connects to a remote database
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind |
|---|---|
| `VERSION`, `package.json` version | release bump (next minor, 4-digit, byte-equal) |
| `CHANGELOG.md` top entry | one unified Phase 166 entry |
| OPEN-2 decision record | 166-10-SUMMARY.md |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit message / CHANGELOG -> public repo and CI | a skip token suppresses CI even when quoted; CHANGELOG is scanned by gitleaks on push to main |
| founder -> PRODUCTION | OPEN-2 is the only path to a production write, and it is not taken here |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-21 | Tampering (CI suppression) | release commit message | high | mitigate | no CI-skip trailer token in any message; acceptance greps the message for all six token forms |
| T-166-22 | Information disclosure | CHANGELOG entry | medium | mitigate | no credential-shaped literal, home path, username, strategy name or project ref; hygiene check in Task 1 |
| T-166-23 | Tampering (PROD data write) | OPEN-2 | high | mitigate | checkpoint:decision only; no database connection in this plan under either answer |
| T-166-24 | Repudiation (unshipped fact) | CHANGELOG | medium | mitigate | enumerated commit checklist with a commit-to-bullet mapping in the SUMMARY |
| T-166-SC | Tampering (supply chain) | package.json | low | mitigate | only the version field changes; npm version is never run (it rewrites the lockfile) |
</threat_model>

<verification>
- Gate sweep green at SWEEP_SHA.
- VERSION == package.json version, 4-digit; CHANGELOG top entry present; CRITICAL-02 green.
- Release commit touches exactly three files; no skip token.
- OPEN-2 answer recorded; no production write.
</verification>

<success_criteria>
The phase ships with a byte-equal version bump and a CHANGELOG entry that accounts for every commit and
every changed value, and the one production-data question is in front of the founder, not acted on.
</success_criteria>

<output>
Create `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-10-SUMMARY.md`
with: SWEEP_SHA and the eight gate lines; the commit checklist, count and commit-to-bullet mapping; the
release SHA and version; the version gate's output; the OPEN-2 answer verbatim, or the pending record (no default named). Note for any later review
fix round: new commits on the branch mean the CHANGELOG entry must be REPLACED by a re-unified one
(CHANGELOG step 4), never appended to.

**SUMMARY hygiene gate (mandatory, before the SUMMARY commit).** `.planning/` is public. Quote every
captured output repo-relative: interpreter / venv / `node_modules` prefixes become `<venv>/` /
`<node_modules>/`, the session scratchpad becomes `<scratchpad>/`, any other absolute path becomes its
repo-relative form. Then, from the worktree root: `git add` the SUMMARY (the checker enumerates
`git ls-files`, so an unstaged new file is invisible to it), link `node_modules` per D-19 if absent,
run `./node_modules/.bin/tsx scripts/check-planning-hygiene.ts`, then remove the link when it is a
symlink. A non-zero exit or any violation line blocks the commit: fix the text, re-stage, re-run until
clean.
</output>
