---
phase: 166-qstats-truth-every-quantstats-derived-number-reflects-the-re
plan: 05
type: execute
wave: 4
depends_on: ["166-04"]
files_modified:
  - analytics-service/services/metrics.py
  - analytics-service/tests/test_metrics.py
autonomous: true
requirements: [SC-2, SC-4, SC-5, WINDOWS-5, WINDOWS-9]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-03 applied to the scalar benchmark leg: r_squared and the scalar greeks call are inline - the keyword is not forwarded to quantstats' benchmark preparer, so neither is kwarg-closed"
    - "SC-4 on the benchmark trigger (all-non-negative benchmark with a +150% day): r_squared equals the squared Pearson correlation of the raw aligned pair; beta equals cov/var of the raw aligned pair; alpha equals (mean(r) - beta*mean(b))*periods - each observed RED against the pre-mirror code first"
    - "Pitfall 3 respected: r_squared keeps quantstats' unaligned-benchmark reindex/bfill behaviour (the benchmark is prepared TWICE through _align_benchmark_like_qs against the unaligned series), so r_squared does not move for any benign benchmarked strategy"
    - "D-05 / M1: the scalar greeks are inlined on the SAME inner-join aligned pair the correlation / info_ratio / treynor siblings use - the alignment contract is unchanged"
    - "D-15: alpha/beta are computed over pairwise-complete observations and are None (never 0.0) when beta is undefined (fewer than 2 complete pairs or zero benchmark variance); NaN-free series stay bit-identical to live 0.0.81; the NaN-bearing class gets a D-10 row"
    - "D-10 / SC-5 derived mover: persisted `treynor` = cagr / beta (the M1 treynor guard), so it moves in VALUE wherever beta moves - the benchmark trigger (beta -0.0154 -> positive, so treynor changes sign while staying present) and the NaN-bearing class (absent -> present). Each gets its own measured before/after treynor row in the SUMMARY. It is the only persisted value derived from alpha/beta: correlation and info_ratio read the aligned pair directly, not beta (read in the M1 block)"
    - "D-10: this plan moves NO golden key path - the key-path diff against the phase-base golden still lists exactly plan 04's PSR key path"
    - "the SUMMARY passed the planning-hygiene check while staged, before it was committed"
  artifacts:
    - path: analytics-service/services/metrics.py
      provides: "_align_benchmark_like_qs, _r_squared, _greeks_no_guess; scipy linregress import"
      contains: "def _align_benchmark_like_qs"
    - path: analytics-service/tests/test_metrics.py
      provides: "_q166_benchmark_trigger, _q166_calendar_mismatch, r_squared and greeks tests, re-targeted r_squared fault tests"
      contains: "_q166_benchmark_trigger"
  key_links:
    - from: compute_qstats_scalars
      to: _r_squared
      via: "replaces the quantstats r_squared call; r_squared_status three-state logic and WARNING kept"
      pattern: "_r_squared\\("
    - from: "compute_all_metrics benchmark block (M1 aligned pair)"
      to: _greeks_no_guess
      via: "alpha/beta written to metrics_json; treynor guard skips a None beta"
      pattern: "_greeks_no_guess\\("
---

<objective>
Close the heuristic on the SCALAR benchmark leg: `r_squared` in `compute_qstats_scalars` and the scalar
`greeks` call in `compute_all_metrics`' M1 block (D-05) - and fix, with disclosure, the fabricated
`alpha = 0.0 / beta = 0.0` on NaN-bearing strategies introduced by Phase 159 (D-15, F-3).

Purpose: alpha/beta are rendered in the Benchmark greeks table. Research Q2: quantstats runs the
benchmark through `_prepare_benchmark -> _prepare_returns` unconditionally, so no keyword closes it.
Plan 06 closes the rolling leg (`rolling_greeks`, D-06/D-17) on top of this plan's
`_align_benchmark_like_qs`; the split exists for per-plan context budget only.
Output: three module functions, two call sites re-pointed, benchmark test fixtures and tests,
re-targeted fault tests, no golden movement.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-CONTEXT.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-PATTERNS.md
Also read `166-04-SUMMARY.md` in this phase directory once the plan it belongs to has executed (it records what that plan actually built; it does not exist at planning time, so it is not an @-reference).
@analytics-service/services/metrics.py
@analytics-service/tests/test_metrics.py

**Environment.** Python ONLY from the worktree's `analytics-service/` directory, with the MAIN
checkout's venv interpreter resolved at run time (capture `git worktree list --porcelain` into `QZ_WT`,
first `worktree` path + `/analytics-service/.venv/bin/python` -> `QZ_VENV_PY`; never written into a
file; exit 127 = FAIL). No database, network, `uvicorn main:app` or credential. No SPEC; spec-less probe
fallback skipped. Drills use byte backups in the session scratchpad (plan-unique names) - never
`git checkout --`, `git restore` or `git stash`.

**0.0.81 sources to mirror** (research §Q3; read the installed source with `inspect.getsource`):
`quantstats.utils._prepare_benchmark` (INCLUDING its tz-normalisation step, copied verbatim),
`quantstats.stats.r_squared`, `quantstats.stats.greeks`.
`_prepare_benchmark` minus the guess: when `set(period) != set(benchmark.index)`, convert to prices
(`to_prices(base=1)`), reindex onto a daily `date_range(period[0], period[-1])` with bfill, reindex onto
`period`, `pct_change(fill_method=None).fillna(0)`, keep `index in period`; tz-normalise; then
`_prepared_returns_no_guess(benchmark.dropna())` (plan 03's P).

**Measured values (research Q4, re-measure):** benchmark trigger - strategy `normal(0.0006, 0.013, 250)`
seed 15905 over 250 bdays, benchmark `[1.5] + linspace(0.02, 0.001, 249)` on the same index:
r_squared 0.006670639650444322 -> 0.0037210240094842093; beta -0.015400848308443902 -> 0.007651507459018336;
alpha 0.12212418616146373 -> 0.1516558141912828.
NaN class (golden_returns with 3 NaN days + benchmark_returns): live alpha 0.0, beta 0.0 (fabricated).

**Golden diff base.** Golden checks compare FLATTENED key paths of `golden_252d_expected.json` against
the PHASE-BASE golden (the parent of the first commit since `git merge-base HEAD origin/main` touching
`analytics-service/services/metrics.py`), never HEAD. After plan 04 the expected set is exactly
`['metrics_json.metrics_json.probabilistic_sharpe_ratio']`, and this plan must not add to it.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): _align_benchmark_like_qs + _r_squared, RED-first on the benchmark trigger, wired through compute_qstats_scalars into metrics_json, with parity on benign and calendar-mismatch fixtures</name>
  <files>analytics-service/tests/test_metrics.py, analytics-service/services/metrics.py</files>
  <read_first>
    - analytics-service/services/metrics.py - `compute_qstats_scalars` r_squared block (the `r_squared_status` three-state logic and its WARNING), plan 03's `_prepared_returns_no_guess`, the module imports (plan 04's `from scipy.stats import norm`)
    - analytics-service/tests/test_metrics.py - `test_qstats_scalars_r_squared_status_error_on_qs_failure`, `test_qstats_scalars_r_squared_status_error_when_qs_returns_nan`, `test_qstats_scalars_r_squared_status_ok_when_benchmark_present`, the Phase 166 section from plans 03/04
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - §Q3 r_squared row and the `_prepare_benchmark` paragraph, Pattern 2 code sketch, Pitfall 3, "Alternatives Considered" (linregress, not corrcoef)
  </read_first>
  <behavior>
    - test_q166_benchmark_trigger_actually_trips_the_heuristic: the benchmark leg of `_q166_benchmark_trigger()` is all-non-negative with max > 1; the strategy leg has a negative day (anti-vacuity)
    - test_q166_benchmark_r_squared_is_the_squared_correlation: `compute_qstats_scalars(r, b)["r_squared"]` == `(P(r).corr(b))**2` computed in-test on the raw aligned pair, rel 1e-12 (R-squared of a one-regressor OLS is the squared Pearson correlation) - RED on the pre-mirror code (live 0.00667 vs 0.00372)
    - test_q166_benchmark_r_squared_reaches_metrics_json: the same value appears in `compute_all_metrics(r, b)["r_squared"]`
    - test_q166_benchmark_r_squared_is_pair_permutation_invariant: jointly permuting the (r, b) PAIRS (both re-indexed onto the original index) leaves r_squared unchanged at rel 1e-9
    - test_q166_parity_r_squared_matches_live_quantstats (`pytest.mark.parametrize` over its THREE fixture pairs, one collected case each, so each pair passes or fails on its own; resolve the conftest fixtures inside the test with `request.getfixturevalue`): on (golden_returns, benchmark_returns), `_q166_calendar_mismatch()` (weekday strategy vs 7-day benchmark - exercises the reindex/bfill branch) and (`_q166_golden_with_nan_days(golden_returns)`, benchmark_returns), the mirror equals live `qs.stats.r_squared` at rel 1e-12
  </behavior>
  <action>
    Per D-03 (inline, measured), D-08 (0.0.81 arithmetic, reindex branch kept), D-09 (RED first):
    1. Add test helpers `_q166_benchmark_trigger()` returning `(strategy, benchmark)` per the recipe in context, and `_q166_calendar_mismatch()` returning a weekday (bdate) strategy of ~160 rows and a daily (calendar-day) benchmark spanning a wider window, both seeded. Write the behaviour tests; run against the current code; the trigger tests MUST be RED (record observed values).
    2. In services/metrics.py extend the scipy import to `from scipy.stats import linregress, norm` (module scope, so tests can monkeypatch `metrics_module.linregress`). Add `_align_benchmark_like_qs(benchmark, period)` reproducing 0.0.81 `_prepare_benchmark(..., prepare_returns=True)` minus the guess, with the tz step copied verbatim and a P114 docblock (why a private quantstats symbol is NOT called instead - research "Alternatives Considered"). Add `_r_squared(returns, benchmark)`: p = P(returns); b = `_align_benchmark_like_qs(benchmark, p.index)`; return `linregress(p, _align_benchmark_like_qs(b, p.index))` r-value squared - benchmark prepared TWICE exactly as 0.0.81 does, against the UNALIGNED benchmark (never the M1 inner-join pair; Pitfall 3). Use `linregress`, not `np.corrcoef` (not bit-identical to the golden).
    3. In `compute_qstats_scalars` replace the quantstats r_squared call with `_r_squared(returns, benchmark)`; keep `_safe_float`, the `'ok' / 'error'` status logic and the WARNING text unchanged.
    4. Re-target the two r_squared fault tests: monkeypatch `metrics_module.linregress` to raise (status 'error', r_squared None) and to return a NaN r-value (status 'error'), keeping their docstrings' H-0718 / F7 reasoning.
    5. GREEN: new tests, re-targeted tests, `test_metrics_parity.py` (golden r_squared unchanged), full suite, mypy strict, and the golden key-path diff still exactly plan 04's PSR key path.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py -q -k "q166_benchmark or q166_parity_r_squared or r_squared_status"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or " failed" in the summary, or fewer than 10 passed (4 `q166_benchmark` tests + 3 parametrized parity cases + 3 existing `r_squared_status` tests)</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>non-zero exit, or " failed" / " error" in the pytest summary, or mypy lacks "Success: no issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY records the RED run (live r_squared ~0.00667) and the GREEN run
    - `grep -c '^def _align_benchmark_like_qs(' analytics-service/services/metrics.py` prints 1
    - `grep -c '^def _r_squared(' analytics-service/services/metrics.py` prints 1
    - `grep -c 'from scipy.stats import linregress, norm' analytics-service/services/metrics.py` prints 1
    - both verify commands exit 0
  </acceptance_criteria>
  <done>r_squared is closed on both legs, anchored to the squared correlation on the trigger, bit-equal to live quantstats on benign and calendar-mismatch fixtures, and its failure-state contract is re-targeted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: inline the scalar greeks on the M1 aligned pair (D-05) with pairwise-complete alpha/beta and None-not-zero when undefined (D-15)</name>
  <files>analytics-service/tests/test_metrics.py, analytics-service/services/metrics.py</files>
  <read_first>
    - analytics-service/services/metrics.py - `compute_all_metrics` "Benchmark metrics (single greeks() call ...)" block: the M1 comment, `aligned`, `aligned_returns`, `aligned_benchmark`, the quantstats greeks line and the "RESIDUAL, recorded rather than silently left" comment, the `.get("alpha", 0)` / `.get("beta", 0)` reads, `correlation`, `info_ratio`, the treynor guard, the `benchmark_metrics fan-out failed` WARNING envelope
    - analytics-service/tests/test_metrics_parity.py - `test_periods_param_rescales_365` (alpha rescales exactly x365/252, beta invariant) - must stay green unedited
    - analytics-service/tests/test_composite_headline_parity.py - alpha/beta equality across composite paths - must stay green unedited
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - §Q3 greeks row, §Q5 F-1 and F-3, §Q6 (alpha/beta readers render null as an em-dash)
  </read_first>
  <behavior>
    - test_q166_benchmark_greeks_are_the_raw_pair_regression: on `_q166_benchmark_trigger()`, `compute_all_metrics(r, b)` beta == in-test `np.cov(r, b)[0,1] / np.cov(r, b)[1,1]` on the raw aligned pair (rel 1e-12) and alpha == `(r.mean() - beta*b.mean()) * periods_per_year` (rel 1e-12) - RED on live code (beta -0.0154)
    - test_q166_greeks_nan_days_are_not_fabricated_zeros: on `_q166_golden_with_nan_days(golden_returns)` + benchmark_returns, alpha and beta are NOT 0.0, equal the in-test pairwise-complete regression over `pd.concat([r, b], axis=1).dropna()` (rel 1e-12), and treynor is present - RED on live code (alpha 0.0, beta 0.0)
    - test_q166_greeks_undefined_beta_is_none_not_zero: with a constant benchmark (zero variance) alpha and beta are None and treynor is absent; with fewer than 2 complete pairs alpha and beta are None
    - test_q166_parity_greeks_match_live_quantstats_on_nan_free_series: on (golden_returns, benchmark_returns) and `_q166_calendar_mismatch()`, alpha and beta equal live `qs.stats.greeks(aligned_r, aligned_b, periods=ppy, prepare_returns=False)` at rel 1e-12 (the inner-join pair, as compute_all_metrics builds it)
  </behavior>
  <action>
    Per D-05, D-15, D-09, D-10 (F-1 stays: D-08):
    1. Write the four behaviour tests; observe the trigger and NaN-class tests RED (record values).
    2. Add module function `_greeks_no_guess(aligned_returns, aligned_benchmark, periods) -> tuple[float | None, float | None]` returning (alpha, beta): benchmark leg = `_align_benchmark_like_qs(aligned_benchmark, aligned_returns.index)`; restrict both legs to pairwise-complete rows (the convention the sibling `aligned_returns.corr(aligned_benchmark)` on the next line already uses); if fewer than 2 complete rows or the benchmark variance from `np.cov` is 0 -> `(None, None)`; else `m = np.cov(r, b)`, `beta = m[0, 1] / m[1, 1]`, `alpha = (r.mean() - beta * b.mean()) * periods` - 0.0.81's expression order, so NaN-free series are bit-identical. No `fillna(0)` on the result (that is the fabrication). Docblock: WHY INLINE (research Q2: benchmark preparer unconditional), MATH PARITY (quote 0.0.81 greeks), D-15 disclosure, F-1 RECORDED (alpha is a return annualized on the frequency clock; not changed per D-08).
    3. In the M1 block, replace ONLY the quantstats greeks line and the two `.get(..., 0)` reads with a call to `_greeks_no_guess(aligned_returns, aligned_benchmark, periods_per_year)` whose outputs pass through `_safe_float` into `metrics_json["alpha"]` / `["beta"]`. Keep `aligned`, the `len(aligned_returns) > 1` guard, correlation, info_ratio, the rolling-correlation line and the WARNING envelope byte-for-byte. Confirm by reading that the treynor guard (`if beta and beta != 0 ...`) already skips a None beta; add no guard. Replace the "RESIDUAL, recorded rather than silently left" comment with the closure note citing D-05/D-15.
    4. GREEN: new tests, `test_metrics_parity.py` (golden alpha/beta unchanged; `test_periods_param_rescales_365` unedited), `test_composite_headline_parity.py`, full suite, mypy strict. Record the D-10 rows in the SUMMARY: the NaN class (alpha/beta before 0.0/0.0, after the measured pairwise values; treynor before absent, after its measured value), and the benchmark trigger's DERIVED treynor row (before = live cagr / beta with beta -0.0154, after = cagr / the new beta; measure both by running `compute_all_metrics` on the trigger before and after the M1 edit - never compute it by hand), per D-10.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py tests/test_composite_headline_parity.py tests/test_metrics_parity.py -q -k "q166_benchmark_greeks or q166_greeks or q166_parity_greeks or periods_param or composite or parity"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or " failed" in the summary, or no line reporting " passed"</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -c "import ast,sys; t=ast.parse(open('services/metrics.py').read()); bad=[(n.attr,n.lineno) for n in ast.walk(t) if isinstance(n,ast.Attribute) and ast.unparse(n.value)=='qs.stats' and n.attr in {'greeks','r_squared'}]; print('open scalar benchmark nodes:',bad); sys.exit(1 if bad else 0)" && "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>non-zero exit, or the printed "open scalar benchmark nodes:" list is non-empty, or " failed" in the pytest summary, or mypy lacks "Success: no issues found"</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && QZ_BASE="$(git merge-base HEAD origin/main)" && QZ_LOG="$(git log --reverse --format=%H "$QZ_BASE"..HEAD -- services/metrics.py)" && QZ_FIRST="$(printf '%s\n' "$QZ_LOG" | head -1)" && test -n "$QZ_FIRST" && git show "$QZ_FIRST^:analytics-service/tests/fixtures/golden_252d_expected.json" > "${TMPDIR:-/tmp}/q166-05-golden-phase-base.json" && "$QZ_VENV_PY" -c "import json,sys; fl=lambda x,p='': [kv for k,v in x.items() for kv in fl(v, p+'.'+k if p else k)] if isinstance(x,dict) else [(p,x)]; a=dict(fl(json.load(open(sys.argv[1])))); b=dict(fl(json.load(open('tests/fixtures/golden_252d_expected.json')))); moved=sorted(k for k in set(a)|set(b) if a.get(k)!=b.get(k)); print('golden key paths moved since phase base:',moved); sys.exit(0 if moved==['metrics_json.metrics_json.probabilistic_sharpe_ratio'] else 1)" "${TMPDIR:-/tmp}/q166-05-golden-phase-base.json"</automated>
    <fails_when>non-zero exit (including an empty QZ_FIRST), or the printed "golden key paths moved since phase base:" list is anything other than exactly ['metrics_json.metrics_json.probabilistic_sharpe_ratio'] (a moved alpha, beta or r_squared path is a defect of this plan)</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY records RED (beta -0.0154 on the trigger; alpha 0.0 / beta 0.0 on the NaN class) then GREEN, and the NaN-class D-10 row
    - the SUMMARY carries a measured before/after `treynor` VALUE row for the benchmark trigger and for the NaN class (D-10 derived mover)
    - `grep -c '^def _greeks_no_guess(' analytics-service/services/metrics.py` prints 1
    - `grep -c '_greeks_no_guess(aligned_returns, aligned_benchmark, periods_per_year)' analytics-service/services/metrics.py` prints 1
    - `git diff --quiet "$(git merge-base HEAD origin/main)" -- analytics-service/tests/test_metrics_parity.py analytics-service/tests/test_composite_headline_parity.py` exits 0 (both files unedited across the whole phase so far - the same verdict before and after this task commits)
    - all three verify commands exit 0
  </acceptance_criteria>
  <done>Scalar alpha/beta are inline on the M1 pair, closed on the benchmark leg, never a fabricated zero, bit-identical to 0.0.81 on NaN-free input, and no golden key path moved.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind |
|---|---|
| `_align_benchmark_like_qs(benchmark, period)` | function (quantstats `_prepare_benchmark` minus the guess; reused by plan 06) |
| `_r_squared(returns, benchmark)` | mirror function |
| `_greeks_no_guess(aligned_returns, aligned_benchmark, periods)` | mirror function (D-05, D-15) |
| `from scipy.stats import linregress, norm` | module import |
| `_q166_benchmark_trigger`, `_q166_calendar_mismatch` | test helpers (reused by plan 06) |
| `test_q166_benchmark_*`, `test_q166_greeks_*`, `test_q166_parity_r_squared_*`, `test_q166_parity_greeks_*` | tests |
| re-targeted r_squared x2 fault tests | test changes |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark feed (BTC daily) -> worker | the only benchmark; research A1 says a >=100% all-non-negative BTC window is not realistic, so the benchmark-leg closure is correctness-by-construction |
| worker -> rendered surfaces | alpha/beta (BenchmarkGreeksTable) are shown to users |
| planning docs -> public repo | the SUMMARY quotes test output and is world-readable once pushed |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-10 | Tampering (fabricated rendered value) | scalar greeks on NaN-bearing strategies | high | mitigate | D-15 pairwise-complete regression, None when undefined, RED-first test on the NaN class; the `.get(..., 0)` defaults removed |
| T-166-12 | Tampering (silent drift for all strategies) | r_squared alignment | medium | mitigate | 0.0.81 reindex/bfill reproduced (benchmark prepared twice, unaligned); calendar-mismatch parity fixture; golden key-path diff shows no r_squared movement |
| T-166-02 | Denial of service (NaN/inf to JSONB) | alpha/beta/r_squared outputs | medium | mitigate | `_safe_float` on every scalar |
| T-166-09 | Tampering (fault masking) | r_squared failure state | low | mitigate | fault injected into `linregress`; status 'error' + WARNING asserted |
| T-166-03 | Information disclosure | SUMMARY quoting tracebacks | medium | mitigate | repo-relative quoting; staged planning-hygiene check before the SUMMARY commit |
</threat_model>

<verification>
- Trigger-based benchmark tests: RED recorded before, GREEN after.
- Parity with live 0.0.81 at 1e-12 for r_squared and NaN-free alpha/beta, including the calendar-mismatch reindex branch.
- AST probe: no quantstats greeks / r_squared reference remains.
- Golden: key-path diff since the phase base still exactly plan 04's PSR path; test_periods_param_rescales_365 and composite parity unedited and green.
- Full suite + mypy strict green.
</verification>

<success_criteria>
The scalar benchmark leg is closed; F-3 fabricated zeros are fixed with a before/after row; every other
scalar benchmark value is bit-identical to live quantstats.
</success_criteria>

<output>
Create `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-05-SUMMARY.md`
with RED-first evidence per new test, the D-10 rows (benchmark trigger r_squared / alpha / beta / treynor; NaN-class
alpha / beta / treynor - treynor is cagr / beta, a derived mover, recorded by measured value), the recorded F-1 note (alpha annualized on the frequency clock, unchanged), and
confirmation that the r_squared / alpha / beta golden key paths did not move.

**SUMMARY hygiene gate (mandatory, before the SUMMARY commit).** `.planning/` is public. Quote every
captured output repo-relative: an interpreter / venv / site-packages prefix becomes `<venv>/`, the
session scratchpad becomes `<scratchpad>/`, any other absolute path becomes its repo-relative form -
never paste a traceback's absolute file paths verbatim. Then, from the worktree root: `git add` the
SUMMARY (the checker enumerates `git ls-files`, so an unstaged new file is invisible to it), link
`node_modules` per D-19 if absent, run `./node_modules/.bin/tsx scripts/check-planning-hygiene.ts`,
then remove the link when it is a symlink. A non-zero exit or any violation line blocks the commit:
fix the text, re-stage, re-run until clean.
</output>
