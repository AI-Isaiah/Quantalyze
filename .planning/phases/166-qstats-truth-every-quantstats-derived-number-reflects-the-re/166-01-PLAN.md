---
phase: 166-qstats-truth-every-quantstats-derived-number-reflects-the-re
plan: 01
type: execute
wave: 1
depends_on: []
coupling_justified: ["166-02: both plans create and remove a worktree-root node_modules symlink (D-19, the planning-hygiene run), but each same-wave plan executes in its OWN isolated worktree, so the two links are different paths; the link is gitignored and removed before every commit, so neither plan can see or stage the other's"]
files_modified:
  - analytics-service/services/metrics.py
autonomous: true
requirements: [TODOS-0f, SC-2]
user_setup: []

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-04: the drawdown-series, max-drawdown, annualized vol/Sharpe, downside-RMS and CVaR-of-tail math each exist ONCE as a module-level primitive in services/metrics.py, and every pre-existing call site that spelled them inline now calls the primitive"
    - "D-04: the extraction is byte-neutral - the full analytics-service suite and tests/test_metrics_parity.py pass with ZERO test-file edits and zero golden-fixture movement, BEFORE any mirror lands (the order D-04 makes binding)"
    - "D-08: no annualization convention changes; each primitive reproduces the operation order of the site it came from (Sharpe = (mean*ppy)/(std*sqrt(ppy)); max_dd applies min then minus 1.0 with no -0.0 replace; the drawdown SERIES keeps its trailing inf/-0.0 replace)"
    - "mypy --strict stays green for services/"
    - "the extraction adds no runtime side effect: `_annualized_vol_sharpe` returns a NaN sharpe WITHOUT dividing when vol is 0 or NaN, so the backbone's zero/NaN-vol path emits no numpy RuntimeWarning it did not emit before, and every caller's persisted value is unchanged (NaN and inf both map to None through `_safe_float`)"
    - "the SUMMARY passed the planning-hygiene check while staged, before it was committed"
  artifacts:
    - path: analytics-service/services/metrics.py
      provides: "_drawdown_series_from_wealth, _max_drawdown_from_wealth, _annualized_vol_sharpe, _downside_rms, _cvar_of_tail"
      contains: "def _annualized_vol_sharpe"
  key_links:
    - from: "compute_all_metrics (RANK-05 drawdown block, headline sharpe/sortino, smart_sortino, cvar block)"
      to: "the five primitives"
      via: "direct call, same operand each site used before"
      pattern: "_max_drawdown_from_wealth\\(|_drawdown_series_from_wealth\\(|_annualized_vol_sharpe\\(|_downside_rms\\(|_cvar_of_tail\\("
    - from: "sharpe_vol_status_from_backbone"
      to: "_annualized_vol_sharpe"
      via: "direct call; the status ladder stays outside the primitive"
      pattern: "_annualized_vol_sharpe\\("
---

<objective>
Extract the money-math primitives that Phase 159 hand-copied inline, as module-level functions, and
re-point every existing call site at them - a pure, byte-neutral refactor that lands BEFORE any
quantstats mirror is written (D-04 order is binding; TODOS 0f `[159-SIMPLIFY-DEFER]`).

Purpose: the mirrors in plans 03-06 (ulcer family, recovery factor, serenity in 03; PSR base in 04) must be
built ON these primitives rather than adding a third hand-copy. Proving zero movement now, with no
test edits, is what lets plans 03-06 attribute every later value change to the mirror and nothing else.
Output: five primitives in `analytics-service/services/metrics.py`; call sites re-pointed; the suite,
the golden parity file and mypy strict all green with no test file touched.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-CONTEXT.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-PATTERNS.md
@analytics-service/services/metrics.py

**Environment (every verify command below is self-contained; read this once).**
- Python runs ONLY from the worktree's `analytics-service/` directory - never from the repo root
  (cassette misses there make LIVE broker calls). The interpreter is the MAIN checkout's venv,
  resolved at run time, never written as a literal path: `QZ_VENV_PY` =
  the first `worktree` path printed by `git worktree list --porcelain` (captured into `QZ_WT` first so a git failure stops the chain) joined with `/analytics-service/.venv/bin/python`
  (the first `worktree` line of `git worktree list --porcelain` is the main checkout). A worktree has
  no `.venv` of its own. A command that exits 127 or prints "No such file" for the interpreter is a
  FAIL, never a skip.
- No database, no network, no `uvicorn main:app`, no credential is used by this plan.
- No SPEC exists for this phase and it has no formal requirement IDs, so the spec-less probe
  fallback was skipped and no probe-derived predicates were generated. Requirement labels used:
  SC-1..SC-5 (ROADMAP success criteria), TODOS-0f, WINDOWS-5, WINDOWS-9.
- Cite code by SYMBOL. Any line number in RESEARCH/PATTERNS is approximate; re-resolve by symbol.
- Baseline at planning time (research): full suite 6109 passed / 90 skipped; `tests/test_metrics.py
  tests/test_metrics_parity.py` 206 passed. Re-measure at your HEAD before editing and record it.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): extract the drawdown primitives and prove one extracted site byte-neutral end-to-end through compute_all_metrics and the golden parity file</name>
  <files>analytics-service/services/metrics.py</files>
  <read_first>
    - analytics-service/services/metrics.py - `compute_all_metrics`, the "RANK-05 (Phase 159) - WHY INLINE, NOT quantstats. `max_drawdown` and `to_drawdown_series`" block (locals `_wealth`, `max_dd`, `dd_series`, `cumulative`, `returns_for_chart`); also the separate `_underwater` drawdown path earlier in the same function
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-PATTERNS.md - "Analog A - drawdown" (constraints to carry)
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - "Pattern 1" and "Pitfall 6: -0.0 vs 0.0"
    - analytics-service/tests/test_metrics_parity.py - module docstring (1e-12 scalar / 1e-9 series comparator); DO NOT edit this file
  </read_first>
  <action>
    Per D-04 (extraction FIRST, byte-neutral) and D-08 (no convention change):
    1. Before editing, run the full suite once and record the pass/skip counts in the SUMMARY as this plan's baseline.
    2. Add two module-level functions in services/metrics.py, placed before `compute_all_metrics` under a section comment naming them "RANK-05 / Phase 166 primitives":
       - `_max_drawdown_from_wealth(wealth: pd.Series) -> float` - returns `float((wealth / wealth.cummax().clip(lower=1.0)).min()) - 1.0`, exactly that operation order, with NO inf/-0.0 replace (the current `max_dd` site's order; RESEARCH Pitfall 6).
       - `_drawdown_series_from_wealth(wealth: pd.Series) -> pd.Series` - returns `(wealth / wealth.cummax().clip(lower=1.0) - 1.0)` followed by the same `.replace([np.inf, -np.inf, -0.0], 0.0)` the `dd_series` site applies.
       Both take a WEALTH CURVE, not returns: each caller keeps building its own wealth (the `max_dd` site from `returns.fillna(0)` unclipped; the `dd_series` site from `cumulative`, which derives from `returns_for_chart` floored at `_LOG_RETURN_FLOOR`). Do not unify the clip.
    3. Move the existing RANK-05 docblock text (the baseline-ladder divergence pinned at 1.0 and the NaN CONVENTION fillna(0) rationale) onto the primitives' docstrings, and leave a one-line pointer comment at the call site naming the primitive. Keep the caller's `_safe_float(...)` wrapper at the call site.
    4. Re-point the `max_dd` and `dd_series` assignments in `compute_all_metrics` to call the primitives with the same operands they used before.
    5. Leave any OTHER drawdown spelling (for example the `_underwater` path) untouched unless its operations are identical in order; list every drawdown spelling you found and whether it was re-pointed in the SUMMARY.
    6. Run the full suite, the parity file and mypy strict. Any movement in `test_metrics_parity.py` means the primitive's operation order differs from the site's - fix the primitive, never the test or the fixture.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py tests/test_metrics_parity.py -q</automated>
    <fails_when>non-zero exit (exit 5 means no tests were collected), or the summary line contains " failed" or " error", or lacks " passed"</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest -q -n auto</automated>
    <fails_when>non-zero exit, or the final summary line reports any " failed" or " error", or its passed count is below the baseline recorded in step 1</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>non-zero exit, or output does not contain "Success: no issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def _max_drawdown_from_wealth(' analytics-service/services/metrics.py` prints 1
    - `grep -c '^def _drawdown_series_from_wealth(' analytics-service/services/metrics.py` prints 1
    - `grep -c '_max_drawdown_from_wealth(_wealth)' analytics-service/services/metrics.py` prints at least 1 (the `max_dd` site calls the primitive on its own wealth curve)
    - `git status --porcelain -- analytics-service/tests` prints nothing (no test or fixture file touched)
    - `git show --name-only --format= HEAD` after the task commit prints exactly `analytics-service/services/metrics.py`
    - all three verify commands exit 0
  </acceptance_criteria>
  <done>The two drawdown primitives exist, both compute_all_metrics sites call them, and the full suite + golden parity + mypy strict pass with no test file edited.</done>
</task>

<task type="auto">
  <name>Task 2: extract _annualized_vol_sharpe, _downside_rms and _cvar_of_tail and re-point every inline spelling, still byte-neutral</name>
  <files>analytics-service/services/metrics.py</files>
  <read_first>
    - analytics-service/services/metrics.py - `compute_all_metrics` headline site (`_stat_std`, `sharpe`, `_sortino_excess`, `_downside_sq_sum`, `_sortino_n`, `_downside`), the `smart_sharpe` / `smart_sortino` block (`_smart_r`, `_smart_n`, `_smart_sortino_downside`), the inline cvar block (`_var_95`, `_cvar_threshold`, local `_cvar_tail`), and `sharpe_vol_status_from_backbone`
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-PATTERNS.md - "Analog B", "Analog C", "Analog D"
    - TODOS.md - entry 0f `[159-SIMPLIFY-DEFER]` (the named primitives)
  </read_first>
  <action>
    Per D-04 and TODOS 0f:
    1. Add module-level `_annualized_vol_sharpe(r: pd.Series, periods_per_year: int) -> tuple[float, float]` returning `(vol, sharpe)` where `vol = r.std() * math.sqrt(periods_per_year)` and `sharpe = (r.mean() * periods_per_year) / vol` - the operation order both existing spellings use (RESEARCH Pattern 1 shows this is bit-identical for both). When `vol` is 0 or NaN the primitive returns `(vol, float("nan"))` WITHOUT performing the division: the backbone today checks vol before it divides and so emits no numpy RuntimeWarning on its zero/NaN-vol path, and the extraction must not add one. This changes no persisted value: the headline site maps the old inf/NaN quotient and the new NaN identically to None through `_safe_float`, and the backbone - which now calls the primitive BEFORE its status ladder - would otherwise divide on exactly the path it used to guard. Returns raw floats (NaN allowed); callers keep their own `_safe_float` and status handling. State the no-divide branch and its reason in the docstring.
    2. Re-point the headline `sharpe` in `compute_all_metrics` (operand `stat_returns`) and `sharpe_vol_status_from_backbone` (operand `returns`) to it. In the backbone keep the `insufficient_history` / `nan_vol` / `zero_volatility` / `ok` status ladder OUTSIDE the primitive and keep its existing `_safe_float(vol)` step. With `periods_per_year=1` the primitive yields `mean/std` exactly - plan 04's PSR base depends on that; state it in the docstring.
    3. Add `_downside_rms(x: pd.Series) -> float`: sum of squares of the strictly-negative entries divided by `int(x.count())`, square-rooted; NaN when the count is 0. Re-point the headline sortino (operand `_sortino_excess`) and `smart_sortino` (operand `_smart_r`, which is already dropna'd so count equals len). Keep each site's surrounding NaN-convention comment and its "downside == 0 means undefined" handling at the site.
    4. Add `_cvar_of_tail(series: pd.Series, threshold: float) -> float`: mean of `series[series < threshold]` when that tail is non-empty, else `threshold`. Named `_cvar_of_tail` (not `_cvar_tail`) because `compute_all_metrics` already binds a local `_cvar_tail`; rename that local or drop it when re-pointing. The `None`-threshold branch stays at the call site. Plan 03's serenity mirror reuses this on a drawdown series.
    5. Each primitive gets a short docstring naming the 0.0.81 function it reproduces (per the P114 house style) and the sites that call it.
    6. Prove the no-new-side-effect property on the backbone's zero-vol path: in a throwaway scratchpad script (never committed), call `sharpe_vol_status_from_backbone` on a length-10 series of 0.0 (std exactly 0) and on a length-10 all-NaN series with divide-warnings promoted to errors (`warnings.filterwarnings("error", message=".*divide.*")`) and record that neither raises (the status comes back `zero_volatility` / `nan_vol` as before). Run the all-NaN case on the pre-extraction tree too: if it already raised there, the warning comes from pandas' own std and is pre-existing - record that and rely on the zero-vol case (the automated verify below), which isolates the primitive's division. Paste the result tuples into the SUMMARY.
    7. Run the full suite, parity file and mypy strict. Zero test edits. If a spelling cannot be re-pointed without moving a golden byte, leave it inline and record why in the SUMMARY rather than editing a test.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest -q -n auto</automated>
    <fails_when>non-zero exit, or the final summary line reports any " failed" or " error", or its passed count is below the baseline recorded in Task 1</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics_parity.py -q</automated>
    <fails_when>non-zero exit, or the summary line contains " failed", or lacks " passed"</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -c "import warnings, numpy as np, pandas as pd; warnings.filterwarnings('error', message='.*divide.*'); from services.metrics import sharpe_vol_status_from_backbone as f; a=f(pd.Series([0.0]*10), 365); print('zero-vol:', a); assert a[2]=='zero_volatility'"</automated>
    <fails_when>non-zero exit (a divide RuntimeWarning promoted to an error - the primitive divided on the zero/NaN-vol path - or an ImportError), or the printed status is not zero_volatility</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>non-zero exit, or output does not contain "Success: no issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def _annualized_vol_sharpe(' analytics-service/services/metrics.py` prints 1
    - `grep -c '^def _downside_rms(' analytics-service/services/metrics.py` prints 1
    - `grep -c '^def _cvar_of_tail(' analytics-service/services/metrics.py` prints 1
    - `grep -c '_annualized_vol_sharpe(' analytics-service/services/metrics.py` prints at least 3 (definition + headline + backbone)
    - `grep -c '_downside_rms(' analytics-service/services/metrics.py` prints at least 3 (definition + headline sortino + smart_sortino)
    - `git status --porcelain -- analytics-service/tests` prints nothing
    - all four verify commands exit 0
  </acceptance_criteria>
  <done>All five primitives exist, every inline spelling that is bit-identical calls them, the suite/parity/mypy are green with no test edits, and the SUMMARY lists every site re-pointed or deliberately left inline with its reason.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Consumers |
|---|---|---|
| `_max_drawdown_from_wealth(wealth)` | function, services/metrics.py | compute_all_metrics `max_dd`; plan 03 `_recovery_factor` |
| `_drawdown_series_from_wealth(wealth)` | function, services/metrics.py | compute_all_metrics `dd_series`; plan 03 ulcer / upi / serenity mirrors |
| `_annualized_vol_sharpe(r, periods_per_year)` | function, services/metrics.py | headline sharpe, `sharpe_vol_status_from_backbone`; plan 04 PSR base (ppy=1) |
| `_downside_rms(x)` | function, services/metrics.py | headline sortino, smart_sortino (pure dedup; no mirror consumes it - RESEARCH D-04 row) |
| `_cvar_of_tail(series, threshold)` | function, services/metrics.py | compute_all_metrics cvar; plan 03 `_serenity_index` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager-supplied returns -> analytics worker | CSV/API-derived daily returns enter `compute_all_metrics`; values persist to `strategy_analytics.metrics_json` (read by public surfaces) |
| planning docs -> public repo | `.planning/` and SUMMARY files are world-readable once pushed |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-01 | Tampering (integrity of persisted metrics) | extracted primitives in services/metrics.py | medium | mitigate | byte-neutrality proven by the unmodified golden parity file (1e-12) and full suite; no test edited, so a drift cannot be hidden by a re-baseline |
| T-166-02 | Denial of service (NaN/inf reaching JSONB) | primitives return raw floats | low | mitigate | every call site keeps its existing `_safe_float` wrapper; primitives never write to metrics_json directly |
| T-166-03 | Information disclosure | SUMMARY written to the public repo | low | mitigate | SUMMARY cites code by symbol; no home path, username, credential or strategy name; interpreter path is resolved at run time, never written |
| T-166-SC | Tampering (supply chain) | dependencies | low | accept | this plan installs nothing and changes no dependency file (D-01) |
</threat_model>

<verification>
- Full analytics-service suite green with a pass count no lower than the recorded baseline.
- `tests/test_metrics_parity.py` green and unedited (the byte oracle).
- `mypy --strict --follow-imports=silent services/ routers/ models/` green.
- `git log -p` for this plan's commits touches only `analytics-service/services/metrics.py`.
</verification>

<success_criteria>
Five primitives exist once each; every bit-identical inline spelling calls them; zero test-file edits;
zero golden movement; mypy strict green - so D-04's "extract first, byte-neutral" precondition is
established before plan 03 writes any mirror (plans 03-06).
</success_criteria>

<output>
Create `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-01-SUMMARY.md`.
It must contain: the baseline and after pass/skip counts; the list of every inline spelling found per
primitive, re-pointed or left inline with the reason; confirmation that no file under
`analytics-service/tests/` changed. Use `verified_at_sha` + `drift_subjects` conventions if any
verification frontmatter is written (the banned fingerprint keys must not appear).

**SUMMARY hygiene gate (mandatory, before the SUMMARY commit).** `.planning/` is public. Quote every
captured output repo-relative: an interpreter / venv / site-packages prefix becomes `<venv>/`, the
session scratchpad becomes `<scratchpad>/`, any other absolute path becomes its repo-relative form -
never paste a traceback's absolute file paths verbatim. Then, from the worktree root: `git add` the
SUMMARY (the checker enumerates `git ls-files`, so an unstaged new file is invisible to it), link
`node_modules` per D-19 if absent, run `./node_modules/.bin/tsx scripts/check-planning-hygiene.ts`,
then remove the link when it is a symlink. A non-zero exit or any violation line blocks the commit:
fix the text, re-stage, re-run until clean.
</output>
