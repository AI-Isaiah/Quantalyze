---
phase: 166-qstats-truth-every-quantstats-derived-number-reflects-the-re
plan: 02
type: execute
wave: 1
depends_on: []
coupling_justified: ["166-01: both plans create and remove a worktree-root node_modules symlink (D-19), but each same-wave plan executes in its OWN isolated worktree, so the two links are different paths; the link is gitignored and removed before every commit, so neither plan can see or stage the other's"]
files_modified:
  - src/lib/queries.percentile-columns.test.ts
  - src/lib/queries.ts
  - src/app/api/strategies/csv-finalize/route.ts
  - src/lib/closed-sets.ts
  - src/__tests__/csv-finalize-cross-submission-merge.test.ts
autonomous: true
requirements: [TODOS-0f]
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-12 / D-18: PERCENTILE_ANALYTICS_COLUMNS (queries.ts) and CLOCK_SAFETY_KPI_COLUMNS plus the clock-safety guard's select string (csv-finalize route) are DERIVED from the one existing exported array PERCENTILE_METRICS (src/lib/percentile-core.ts) - no new array is created"
    - "D-18: a byte pin exists that asserts the exact pre-phase strings reach .select(): 'cagr, sharpe, sortino, calmar, max_drawdown, volatility, cumulative_return' (+ ', computation_status' where composed) - observed GREEN on the literals BEFORE derivation, still GREEN after, and observed RED when the join separator is mutated"
    - "Phase 159 D-03 holds: the strings are byte-identical to today; the derivation is proven byte-neutral, not asserted"
    - "Every prose site that said the lists 'mirror member for member' or are 'duplicated rather than imported' is rewritten in the same commit to say they are derived from PERCENTILE_METRICS, so no comment becomes false"
    - "D-19: TS verification ran through a symlinked node_modules from the main checkout; the link is removed before every commit and never committed; exit 127 counts as FAIL"
    - "the SUMMARY passed the planning-hygiene check while staged, before it was committed"
  artifacts:
    - path: src/lib/queries.percentile-columns.test.ts
      provides: "byte pin on the KPI projection strings reaching .select() in getPercentiles (both branches) and getOwnRowPercentiles"
    - path: src/__tests__/csv-finalize-cross-submission-merge.test.ts
      provides: "byte pin on the clock-safety guard's strategy_analytics .select() string"
    - path: src/lib/queries.ts
      provides: "PERCENTILE_ANALYTICS_COLUMNS = PERCENTILE_METRICS.join(', ')"
      contains: "PERCENTILE_METRICS.join"
    - path: src/app/api/strategies/csv-finalize/route.ts
      provides: "CLOCK_SAFETY_KPI_COLUMNS derived from PERCENTILE_METRICS; guard select composed with PERCENTILE_GATE_COLUMN"
      contains: "PERCENTILE_METRICS"
  key_links:
    - from: src/lib/queries.ts
      to: src/lib/percentile-core.ts
      via: "the existing import line from ./percentile-core, extended with PERCENTILE_METRICS"
      pattern: "PERCENTILE_METRICS"
    - from: src/app/api/strategies/csv-finalize/route.ts
      to: src/lib/percentile-core.ts
      via: "import of PERCENTILE_METRICS from @/lib/percentile-core"
      pattern: "from \"@/lib/percentile-core\""
---

<objective>
Derive the two hand-maintained KPI column lists from the ONE existing exported array
`PERCENTILE_METRICS`, byte-identically, and add the byte pin that today exists only as prose
(D-12, D-18; the third item of TODOS 0f `[159-SIMPLIFY-DEFER]`).

Purpose: the "BYTE-FROZEN" and "mirrors member for member" guarantees are enforced by nothing today
(research Q7: no test pins either literal). A pin first, then the derivation, proves Phase 159 D-03's
byte-unchanged rule instead of asserting it, and deletes the mirror-prose machinery.
Output: one new vitest pin file, one extended route test, three source files re-derived with prose
corrected. This plan is file-disjoint from every Python plan and runs in wave 1.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-CONTEXT.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-PATTERNS.md
@src/lib/percentile-core.ts
@src/lib/queries.ts
@src/app/api/strategies/csv-finalize/route.ts
@src/lib/closed-sets.ts
@AGENTS.md

**Environment (D-19).** A sibling git worktree does not resolve the main checkout's `node_modules`
by walking up, and `npm install` is forbidden (disk). Before any TS command, from the worktree ROOT:
capture `git worktree list --porcelain` into `QZ_WT`, take its first `worktree` path as `QZ_MAIN`
(the main checkout), and if `node_modules` does not already exist create the symlink
`node_modules -> $QZ_MAIN/node_modules`. Before EVERY commit remove it again, only when it is a
symlink (`test -L node_modules && rm node_modules`) - in the main checkout itself `node_modules` is a
real directory and must not be touched. `/node_modules` is also gitignored, so it cannot be staged,
but removal is still required by D-19. A TS command that exits 127 or prints "command not found" is a
FAIL, never a skip. Never write a home path into any file.

This plan touches no Python and no database. No SPEC exists; the spec-less probe fallback was skipped
(no requirement IDs), so no probe-derived predicates were generated.

**Verbatim values to preserve (research Q7, re-read them from the files before editing):**
- queries.ts `PERCENTILE_ANALYTICS_COLUMNS` (module-private): `cagr, sharpe, sortino, calmar, max_drawdown, volatility, cumulative_return`
- route.ts `CLOCK_SAFETY_KPI_COLUMNS`: the same seven as an `as const` array, same order
- route.ts guard select literal: the same seven joined by comma-space, then `, computation_status`
- percentile-core.ts `export const PERCENTILE_METRICS`: the same seven, same order, `as const`, and the file has no imports
- closed-sets.ts `export const PERCENTILE_GATE_COLUMN = "computation_status"`
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): byte-pin the queries.ts KPI projection, observe it GREEN on the literal and RED on a mutated separator, then derive PERCENTILE_ANALYTICS_COLUMNS from PERCENTILE_METRICS and observe it still GREEN</name>
  <files>src/lib/queries.percentile-columns.test.ts, src/lib/queries.ts</files>
  <read_first>
    - src/lib/queries.ts - `PERCENTILE_ANALYTICS_COLUMNS` and its "BYTE-FROZEN" docblock, `getPercentiles` (the `analyticsColumns` composition and both `.select(...)` branches), `getOwnRowPercentiles` (its `strategy_analytics (...)` select), and the existing `./percentile-core` import line
    - src/lib/queries.percentiles.test.ts - the `vi.mock("@/lib/supabase/server", ...)` thenable chain (the analog; its `chain.select` currently ignores its argument)
    - src/lib/queries.test.ts - the RANK-02 `captureSelect` pattern (records and asserts on the `.select()` string)
    - src/lib/percentile-core.ts - `PERCENTILE_METRICS`
  </read_first>
  <behavior>
    - Test A: getPercentiles() without a category records a `.select()` string containing exactly `strategy_analytics (cagr, sharpe, sortino, calmar, max_drawdown, volatility, cumulative_return, computation_status)`
    - Test B: getPercentiles("some-slug") records a `.select()` string containing the same exact parenthesised column list
    - Test C: getOwnRowPercentiles(...) records a `.select()` string containing the same exact parenthesised column list (if its mock surface cannot be satisfied from the same supabase mock, record why in the SUMMARY and pin A+B only - do not weaken A or B)
    - Test D: the pinned literal is the OLD literal written out in the test file, never recomputed from PERCENTILE_METRICS (a pin derived from the source it pins cannot fail)
  </behavior>
  <action>
    Per D-18 (pin FIRST) then D-12 (derive):
    1. Create `src/lib/queries.percentile-columns.test.ts` using the `queries.percentiles.test.ts` mock chain, modified so `chain.select` pushes its argument onto a `recorded` array before returning the chain. Seed enough rows for `getPercentiles` to reach its select (it selects before it counts). Write Tests A-D. The expected strings are hand-written literals in the test file.
    2. Link node_modules per D-19, run the new test against the UNCHANGED queries.ts: it must be GREEN. Record the output line in the SUMMARY.
    3. Anti-vacuity drill (never use git checkout/restore/stash for it): copy queries.ts to a byte backup in your session scratchpad (a file name unique to this plan), edit the `PERCENTILE_ANALYTICS_COLUMNS` literal's first ", " to "," , run the test and observe RED naming the pinned string; restore from the byte backup with cp, confirm `cmp` reports identical, re-run GREEN. Record RED and GREEN lines in the SUMMARY.
    4. Derive: extend the existing `./percentile-core` import in queries.ts with `PERCENTILE_METRICS` (do not add a second import line from that module) and set `PERCENTILE_ANALYTICS_COLUMNS = PERCENTILE_METRICS.join(", ")`. Rewrite the "BYTE-FROZEN" docblock to say the list is derived from `PERCENTILE_METRICS`, that the byte pin in `queries.percentile-columns.test.ts` enforces Phase 159 D-03, and that the gate column still rides alongside (never appended). Remove the sentence claiming the route "mirrors it member-for-member in its prose".
    5. Re-run the pin: GREEN. Run `tsc --noEmit` and eslint on the two files. Remove the node_modules link, then commit.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/queries.percentile-columns.test.ts src/lib/queries.percentiles.test.ts</automated>
    <fails_when>non-zero exit (127 = toolchain not resolved = FAIL), or output contains "No test files found" or " failed", or the "Tests" line lacks " passed"</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsc --noEmit</automated>
    <fails_when>non-zero exit, or any "error TS" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PERCENTILE_METRICS.join(", ")' src/lib/queries.ts` prints 1
    - `grep -c 'cagr, sharpe, sortino, calmar, max_drawdown, volatility, cumulative_return' src/lib/queries.percentile-columns.test.ts` prints at least 1 (the pin carries the OLD literal by hand)
    - `grep -c 'from "./percentile-core"' src/lib/queries.ts` prints 1 (import extended, not duplicated)
    - the SUMMARY records three observed vitest lines: GREEN on the literal, RED on the mutated separator, GREEN after derivation
    - `test -L node_modules` is false at commit time, and `git show --name-only --format= HEAD` lists no `node_modules` path
    - both verify commands exit 0
  </acceptance_criteria>
  <done>The queries.ts projection is pinned byte-for-byte, the pin was seen able to fail, and PERCENTILE_ANALYTICS_COLUMNS is derived from PERCENTILE_METRICS with the pin still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pin the csv-finalize clock-safety guard's select, then derive CLOCK_SAFETY_KPI_COLUMNS and the guard select from PERCENTILE_METRICS and PERCENTILE_GATE_COLUMN, and correct the mirror prose in closed-sets.ts</name>
  <files>src/__tests__/csv-finalize-cross-submission-merge.test.ts, src/app/api/strategies/csv-finalize/route.ts, src/lib/closed-sets.ts</files>
  <read_first>
    - src/app/api/strategies/csv-finalize/route.ts - `CLOCK_SAFETY_KPI_COLUMNS` and its docblock ("MIRRORING ... member for member ... duplicated rather than imported deliberately"), the clock-safety guard's `.from("strategy_analytics").select(...)` literal and its inline "The column set MIRRORS" / "computation_status JOINS THE PROJECTION" comments, the `CLOCK_SAFETY_KPI_COLUMNS.some(...)` consumer, and the two existing `@/lib/closed-sets` import lines
    - src/__tests__/csv-finalize-cross-submission-merge.test.ts - the supabase mock's `strategy_analytics` branch (its `select` ignores the column argument today) and an existing test that drives the guard's read
    - src/lib/closed-sets.ts - the RANK-01 comment above `PERCENTILE_GATE_COLUMN` ("mirrored member-for-member ... would silently falsify three comments")
    - AGENTS.md - Next.js in this repo differs from training data; a route file may only export route handlers/config, so never export the constant from route.ts to test it
  </read_first>
  <behavior>
    - Test E: when the clock-safety guard reads `strategy_analytics`, the recorded select string equals exactly `cagr, sharpe, sortino, calmar, max_drawdown, volatility, cumulative_return, computation_status`
    - Test F: the expected string is a hand-written literal in the test file, not derived from PERCENTILE_METRICS
  </behavior>
  <action>
    Per D-18 then D-12:
    1. In csv-finalize-cross-submission-merge.test.ts, make the `strategy_analytics` mock branch record the argument passed to `select` (push to a test-scoped array), without changing any existing test's behaviour, and add Test E driven through an existing guard-reaching scenario. Run the file on the UNCHANGED route: GREEN; record the line.
    2. Anti-vacuity drill as in Task 1 (byte backup in the session scratchpad under a plan-unique name; mutate the guard literal's first ", " to ","; observe RED; restore with cp; `cmp` identical; GREEN). Never use git checkout/restore/stash.
    3. Derive in route.ts: add `import { PERCENTILE_METRICS } from "@/lib/percentile-core";`; fold `PERCENTILE_GATE_COLUMN` into one of the existing `@/lib/closed-sets` import statements (do not add a third); set `CLOCK_SAFETY_KPI_COLUMNS = PERCENTILE_METRICS`; build the guard select as a template of `PERCENTILE_METRICS.join(", ")`, then `", "`, then `PERCENTILE_GATE_COLUMN`. Do NOT export anything new from route.ts.
    4. Rewrite in the SAME commit every prose site that becomes false: the `CLOCK_SAFETY_KPI_COLUMNS` docblock (now: derived from `PERCENTILE_METRICS`, the same source `queries.ts` derives from; the "not exported / client-reachable" reason no longer applies because percentile-core has no imports), the guard's inline "MIRRORS" comment, and the closed-sets.ts RANK-01 comment (the gate column is still a separate constant composed alongside; the lists are now derived, pinned by the two byte-pin tests, so there are no mirror comments left to falsify). Keep the `computation_status is NOT a member of CLOCK_SAFETY_KPI_COLUMNS` reasoning intact.
    5. Re-run: GREEN. Run tsc, eslint on the three files, every vitest file whose path contains `csv-finalize` (vitest's positional filename filter), and `src/lib/queries.percentile-columns.test.ts`. Record the research note that `get_verified_cohort_rank` carries its own SQL KPI list and is out of D-12 scope (TS cannot derive it) in the SUMMARY. Remove the node_modules link, then commit.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/__tests__/csv-finalize-cross-submission-merge.test.ts src/lib/queries.percentile-columns.test.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), or output contains "No test files found" or " failed", or the "Tests" line lacks " passed"</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run csv-finalize</automated>
    <fails_when>non-zero exit, or output contains " failed" or "No test files found"</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint src/app/api/strategies/csv-finalize/route.ts src/lib/closed-sets.ts src/lib/queries.ts src/lib/queries.percentile-columns.test.ts src/__tests__/csv-finalize-cross-submission-merge.test.ts</automated>
    <fails_when>non-zero exit, or any "error TS" line, or eslint prints any "error" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "@/lib/percentile-core"' src/app/api/strategies/csv-finalize/route.ts` prints 1
    - `grep -c 'CLOCK_SAFETY_KPI_COLUMNS = PERCENTILE_METRICS' src/app/api/strategies/csv-finalize/route.ts` prints 1
    - `grep -c 'PERCENTILE_GATE_COLUMN' src/app/api/strategies/csv-finalize/route.ts` prints at least 2 (import + guard composition)
    - `grep -c 'cagr, sharpe, sortino, calmar, max_drawdown, volatility, cumulative_return, computation_status' src/__tests__/csv-finalize-cross-submission-merge.test.ts` prints at least 1
    - `grep -ciE 'member[- ]for[- ]member' src/app/api/strategies/csv-finalize/route.ts src/lib/queries.ts` prints 0 for each file (no stale single-line mirror prose survives)
    - closed-sets.ts wraps its stale sentence across lines ("mirrored member-for-" / "member by ..."), which a line-based `member-for-member` grep cannot see, so it is checked by two single-line phrases unique to that comment: `grep -c 'mirrored member-for-' src/lib/closed-sets.ts` prints 0 AND `grep -c 'falsify three comments' src/lib/closed-sets.ts` prints 0 (both print 1 before this task - confirm that first, so the check is seen able to fail)
    - the SUMMARY records GREEN-before, RED-on-mutation, GREEN-after for Test E
    - `test -L node_modules` is false at commit time
    - all verify commands exit 0
  </acceptance_criteria>
  <done>Both route lists derive from PERCENTILE_METRICS and PERCENTILE_GATE_COLUMN, the guard's select is pinned byte-for-byte and was seen able to fail, and no comment still claims a hand mirror.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind |
|---|---|
| `src/lib/queries.percentile-columns.test.ts` | NEW vitest file - byte pins on the KPI projection strings (Tests A-D) |
| Test E in `src/__tests__/csv-finalize-cross-submission-merge.test.ts` | new test + select-recording mock branch |
| `PERCENTILE_ANALYTICS_COLUMNS` derived from `PERCENTILE_METRICS` | changed constant, queries.ts |
| `CLOCK_SAFETY_KPI_COLUMNS` = `PERCENTILE_METRICS`; guard select composed with `PERCENTILE_GATE_COLUMN` | changed constants, csv-finalize route.ts |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server route -> Supabase | the clock-safety guard's projection decides which KPI columns are read before a clock relabel |
| client-reachable module graph | queries.ts is client-reachable; percentile-core.ts is import-free |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-04 | Tampering (ranking integrity) | PERCENTILE_ANALYTICS_COLUMNS / guard select | medium | mitigate | hand-written byte pins observed RED on a separator mutation; derivation from one array removes silent divergence between the ranking projection and the guard |
| T-166-05 | Information disclosure (bundle) | importing percentile-core into the route | low | accept | percentile-core has no imports and holds only public column names already present in client code |
| T-166-06 | Elevation / contract break | route.ts exports | low | mitigate | nothing new is exported from route.ts (Next.js route files may only export handlers/config); tsc verifies |
| T-166-SC | Tampering (supply chain) | node_modules | low | mitigate | no install; the main checkout's already-installed tree is linked (D-19), removed before commit, gitignored |
</threat_model>

<verification>
- The two byte pins are green, each was observed RED on a mutated separator and restored by byte backup.
- tsc --noEmit and eslint clean on the touched files; csv-finalize and percentile suites green.
- No stale "mirror" prose remains in the three source files.
</verification>

<success_criteria>
The two KPI lists and the guard select are derived from `PERCENTILE_METRICS` (+ `PERCENTILE_GATE_COLUMN`),
byte-identical to the pre-phase strings as proven by pins that were seen able to fail.
</success_criteria>

<output>
Create `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-02-SUMMARY.md`
recording: the GREEN / RED / GREEN observation lines for both pins, the prose sites rewritten, whether
Test C (getOwnRowPercentiles) was pinned or why not, and the out-of-scope note on the SQL RPC's own KPI list.

**SUMMARY hygiene gate (mandatory, before the SUMMARY commit).** `.planning/` is public. Quote every
captured output repo-relative: a `node_modules` / toolchain prefix becomes `<node_modules>/`, the
session scratchpad becomes `<scratchpad>/`, any other absolute path becomes its repo-relative form -
never paste a stack trace's absolute file paths verbatim. Then, from the worktree root: `git add` the
SUMMARY (the checker enumerates `git ls-files`, so an unstaged new file is invisible to it), link
`node_modules` per D-19 if absent, run `./node_modules/.bin/tsx scripts/check-planning-hygiene.ts`,
then remove the link when it is a symlink. A non-zero exit or any violation line blocks the commit:
fix the text, re-stage, re-run until clean.
</output>
