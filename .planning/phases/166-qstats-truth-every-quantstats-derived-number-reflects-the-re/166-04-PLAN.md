---
phase: 166-qstats-truth-every-quantstats-derived-number-reflects-the-re
plan: 04
type: execute
wave: 3
depends_on: ["166-03"]
files_modified:
  - analytics-service/services/metrics.py
  - analytics-service/tests/test_metrics.py
  - analytics-service/tests/fixtures/golden_252d_expected.json
autonomous: true
requirements: [SC-2, SC-4, SC-5, WINDOWS-5, WINDOWS-9]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC-4 / D-09 (loss/Sharpe family): on _rank05_trigger_series common_sense_ratio is None and probabilistic_sharpe_ratio > 0.5, in compute_qstats_scalars AND in compute_all_metrics' metrics_json - each observed RED against plan 03's end state first"
    - "D-09 sharpened (research Q4): on _q166_trigger_nonmonotone kelly_criterion, cpc_index and common_sense_ratio are None - observed RED first"
    - "order independence is proven RED-first: test_q166_shuffle_order_independent_scalars is authored in Task 1's RED set and OBSERVED RED against plan 03's end state (where these four keys still dispatch to live quantstats), then GREEN on the mirrors"
    - "D-03 applied: none of kelly_criterion, probabilistic_ratio, common_sense_ratio, cpc_index is closed by the prepare_returns keyword - all four are inline mirrors; no quantstats reference to a dispatched scalar, payoff_ratio, win_loss_ratio or cvar remains"
    - "D-04: the PSR base uses plan 01's _annualized_vol_sharpe with ppy=1 - no third hand-copy"
    - "D-08: kelly / common_sense / cpc equal live quantstats 0.0.81 at rel 1e-12 on four benign fixtures"
    - "D-16: PSR uses the NON-excess fourth moment; its anchor is an in-test Bailey-Lopez de Prado computation from sample moments (scipy, not pandas, not quantstats); the golden key path metrics_json.metrics_json.probabilistic_sharpe_ratio is the only golden key path this plan moves, with a D-10 row"
    - "the SUMMARY passed the planning-hygiene check while staged, before it was committed"
  artifacts:
    - path: analytics-service/services/metrics.py
      provides: "_payoff_ratio_no_guess, _kelly_criterion, _probabilistic_sharpe_ratio, _common_sense_ratio, _cpc_index; scipy norm import; the four remaining table entries re-pointed"
      contains: "def _probabilistic_sharpe_ratio"
    - path: analytics-service/tests/test_metrics.py
      provides: "loss-family invariant, shuffle, parity, composed-leaf fault and PSR-anchor tests"
      contains: "test_q166_shuffle_order_independent_scalars"
    - path: analytics-service/tests/fixtures/golden_252d_expected.json
      provides: "metrics_json.metrics_json.probabilistic_sharpe_ratio updated per D-16 (single key path)"
  key_links:
    - from: compute_qstats_scalars
      to: "_QSTATS_SINGLE_ARG_SCALARS (key -> module mirror, all eight)"
      via: "_safe_qstats_scalar(result_key, fn, returns, returns_len)"
      pattern: "_safe_qstats_scalar\\("
    - from: "_kelly_criterion / _common_sense_ratio / _cpc_index"
      to: "quantstats leaves profit_factor, tail_ratio, win_rate, avg_win, avg_loss called with prepare_returns=False"
      via: "composition on P(r); payoff composed in _payoff_ratio_no_guess, never via the quantstats payoff/win-loss functions"
      pattern: "prepare_returns=False"
---

<objective>
Close the quantstats price-detection heuristic on the four LOSS/SHARPE-family scalars dispatched by
`compute_qstats_scalars` - `kelly_criterion`, `probabilistic_ratio` (PSR), `common_sense_ratio`,
`cpc_index` - by inline mirrors (D-03 outcome, D-04), prove the remaining WINDOWS.md entry 9 wrong
values are gone using economic invariants AND an order-independence test that are all observed RED
first (SC-4, D-09), and apply the D-16 PSR kurtosis correction with its disclosed golden movement
(D-10).

Purpose: these values persist to `strategy_analytics.metrics_json`. Research Q2 measured that the
`prepare_returns=` keyword closes NONE of them (it is not forwarded to the transitive preparers), which
refutes the "kwarg-closable" wording in WINDOWS.md entry 9 and the 159-05 Residual table. Plan 03 left
these four table entries pointing at the quantstats functions, which is the state every RED
observation here is taken against.
Output: five new module functions, the last four table entries re-pointed, tests, one golden key path
moved with a recorded reason.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-CONTEXT.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-PATTERNS.md
Also read `166-03-SUMMARY.md` in this phase directory once the plan it belongs to has executed (it records the interim table entries this plan replaces; it does not exist at planning time, so it is not an @-reference).
@analytics-service/services/metrics.py
@analytics-service/tests/test_metrics.py

**Environment.** Python runs ONLY from the worktree's `analytics-service/` directory, with the MAIN
checkout's venv interpreter resolved at run time: capture `git worktree list --porcelain` into `QZ_WT`,
take its first `worktree` path, append `/analytics-service/.venv/bin/python` -> `QZ_VENV_PY`. Never
write that path into a file. Exit 127 = FAIL. No database, no network, no `uvicorn main:app`, no
credential. No SPEC; spec-less probe fallback skipped.

**The 0.0.81 formulas are quoted in 166-RESEARCH.md §Q3** (read the installed source with
`inspect.getsource`). `P(r)` = plan 03's `_prepared_returns_no_guess`. NaN convention: P(r) (fillna(0)),
as recorded by plan 03 (research Pattern 2).

**Measured pre-fix values (research Q4) for docstrings and the RED records:**
canonical trigger: probabilistic_sharpe_ratio 0.1531252134903383, common_sense_ratio 0.0,
kelly_criterion None, cpc_index None. Non-monotone trigger: kelly 0.3890395480225989,
PSR 0.9997018052168664, csr 23.98015435501653, cpc 11.695887516415286. Under a row shuffle, live values
move (research: kelly 0.389 -> 0.485, csr 23.98 -> 166.96, cpc 11.70 -> 557.49). Re-measure; do not trust.

**Golden layout and diff base.** The persisted scalars live at key paths
`metrics_json.metrics_json.<key>` in `golden_252d_expected.json`. Golden checks compare FLATTENED key
paths against the PHASE-BASE golden (the parent of the first commit since
`git merge-base HEAD origin/main` touching `analytics-service/services/metrics.py`), never HEAD, so
the verdict is identical before and after the golden change is committed.

**Never use `git checkout --`, `git restore` or `git stash` in any drill** - byte backups in your
session scratchpad (plan-unique names), re-taken after every edit.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): RED-first loss-family invariants AND the shuffle order-independence test against plan 03's end state, then the four loss-family mirrors wired into the dispatch table and metrics_json</name>
  <files>analytics-service/tests/test_metrics.py, analytics-service/services/metrics.py</files>
  <read_first>
    - analytics-service/services/metrics.py - `_QSTATS_SINGLE_ARG_SCALARS` (the four entries plan 03 left on quantstats function objects), `_prepared_returns_no_guess`, `_annualized_vol_sharpe`, `_safe_qstats_scalar`, the module imports
    - analytics-service/tests/test_metrics.py - the Phase 166 section from plan 03 (`_q166_trigger_nonmonotone`, the drawdown-family tests), `_RANK05_ORDER_INVARIANT(_MJ)` + `test_rank05_order_independent_statistics_survive_a_shuffle`
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - §Q2 (spy matrix: payoff_ratio drops the keyword), §Q3 (formulas), §Q4 (invariants, shuffle paragraph)
  </read_first>
  <behavior>
    - test_q166_all_winning_series_has_no_loss_derived_ratios: on `_rank05_trigger_series()`, `compute_qstats_scalars(s, None)` gives common_sense_ratio None (profit factor undefined with zero losses) and probabilistic_sharpe_ratio > 0.5 (a positive-Sharpe series)
    - test_q166_all_winning_loss_values_reach_metrics_json: the same two assertions hold on `compute_all_metrics(_rank05_trigger_series())`
    - test_q166_nonmonotone_series_has_no_loss_derived_ratios: on `_q166_trigger_nonmonotone()`, kelly_criterion, cpc_index and common_sense_ratio are None (no losing day: payoff and profit factor undefined)
    - test_q166_shuffle_order_independent_scalars: on `_q166_trigger_nonmonotone()`, permuting the rows (fixed seed, re-indexed onto the original index) leaves the values `compute_qstats_scalars` returns for PSR (rel 1e-9), kelly_criterion, common_sense_ratio and cpc_index unchanged (None == None counts as equal). Path-dependent scalars (ulcer, upi, serenity, recovery) are deliberately excluded and the docstring says why. Anti-vacuity inside the test: live `qs.stats.kelly_criterion` on the original and the shuffled series DIFFER (the fixture exercises the guess)
    - Every docstring states the measured pre-fix value and derives the invariant from economics, never from the implementation's output
  </behavior>
  <action>
    Per D-09 (RED first), D-03 (all four inline), D-04 (PSR base on the plan 01 primitive), D-08 (0.0.81 arithmetic minus the guess):
    1. Write the four behaviour tests above in the Phase 166 section. Run them against the CURRENT metrics.py (plan 03's end state - these four keys still dispatch to live quantstats): ALL FOUR must be RED, including the shuffle test (its equality assertion fails because the guess makes live values order-dependent). Paste each RED line, with the observed values, into the SUMMARY. A test GREEN before the mirror is vacuous - fix the test, never proceed.
    2. In services/metrics.py add `from scipy.stats import norm` at module scope (house style: services/portfolio_metrics.py imports scipy directly; mypy already ignores missing scipy imports) and, in the plan 03 mirror section, the module functions:
       - `_payoff_ratio_no_guess(p)` = `qs.stats.avg_win(p, prepare_returns=False) / abs(qs.stats.avg_loss(p, prepare_returns=False))`, reproducing 0.0.81 `payoff_ratio` (which must NOT be called through quantstats: it drops the keyword - research Q2/F-5).
       - `_kelly_criterion(r)`: p = P(r); wl = `_payoff_ratio_no_guess(p)`; wp = `qs.stats.win_rate(p, prepare_returns=False)`; NaN when wl == 0 or wl is NaN else `((wl * wp) - (1 - wp)) / wl`.
       - `_probabilistic_sharpe_ratio(r)`: base = `_annualized_vol_sharpe(P(r), 1)[1]` (bit-identical to 0.0.81's non-annualized sharpe - plan 01); skew = `r.skew()`, kurt = `r.kurtosis()`, n = `len(r)` on RAW r; sigma and `norm.cdf(base / sigma)` exactly as 0.0.81 in THIS task (the `(kurt - 3)` excess-kurtosis quirk is reproduced here so the golden stays byte-identical until Task 3 applies D-16). Use numpy's sqrt as 0.0.81 does (a negative inner term yields NaN -> None, not an exception).
       - `_common_sense_ratio(r)`: p = P(r); `qs.stats.profit_factor(p, prepare_returns=False) * qs.stats.tail_ratio(p, prepare_returns=False)` (the same kwarg-proven leaves compute_all_metrics already calls - no second implementation).
       - `_cpc_index(r)`: p = P(r); profit_factor(p) * win_rate(p) * `_payoff_ratio_no_guess(p)`, leaves called with `prepare_returns=False`.
       Each mirror carries the P114 docblock (WHY INLINE citing research §Q2; MATH PARITY quoting the 0.0.81 body; NaN CONVENTION; any recorded divergence).
    3. Re-point the four remaining `_QSTATS_SINGLE_ARG_SCALARS` entries to these mirrors and rewrite the table comment so it no longer names quantstats entries or plan 04. Every entry is now a module mirror.
    4. Re-run: the four behaviour tests GREEN; plan 03's tests, the re-targeted dispatch/fault tests, full suite, golden parity (unchanged - PSR is still 0.0.81 form) and mypy strict GREEN.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py -q -k "q166_all_winning or q166_nonmonotone or q166_shuffle or qstats_scalars"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or the summary contains " failed" / " error", or fewer than 18 tests passed (plan 03's 4 + this task's 4 + dispatch table's 8 rows + warning test + complete_set)</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -c "import ast,sys; t=ast.parse(open('services/metrics.py').read()); S={'recovery_factor','ulcer_index','ulcer_performance_index','kelly_criterion','probabilistic_ratio','common_sense_ratio','cpc_index','serenity_index','payoff_ratio','win_loss_ratio','cvar','conditional_value_at_risk'}; bad=[(n.attr,n.lineno) for n in ast.walk(t) if isinstance(n,ast.Attribute) and ast.unparse(n.value)=='qs.stats' and n.attr in S]; bad+=[('attr-dispatch',n.lineno) for n in ast.walk(t) if isinstance(n,ast.Call) and getattr(n.func,'id',None)=='getattr' and n.args and ast.unparse(n.args[0])=='qs.stats']; print('open quantstats nodes:',bad); sys.exit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or the printed "open quantstats nodes:" list is not empty</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>non-zero exit, or the pytest summary reports any " failed" / " error", or mypy does not print "Success: no issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY carries the RED run of all four Task 1 tests (including test_q166_shuffle_order_independent_scalars) against plan 03's end state, with observed values (e.g. PSR 0.1531..., kelly 0.389..., the shuffled-vs-original live values)
    - `grep -c '^def test_q166_shuffle_order_independent_scalars' analytics-service/tests/test_metrics.py` prints 1
    - `grep -cE '^def _(kelly_criterion|probabilistic_sharpe_ratio|common_sense_ratio|cpc_index|payoff_ratio_no_guess)\(' analytics-service/services/metrics.py` prints 5
    - all three verify commands exit 0
  </acceptance_criteria>
  <done>All eight dispatched scalars are inline mirrors, the loss-family invariants and the order-independence test went RED on the pre-mirror state then GREEN, and the golden file has not moved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: live-quantstats parity rows for the four loss-family mirrors and a composed-leaf fault test</name>
  <files>analytics-service/tests/test_metrics.py</files>
  <read_first>
    - analytics-service/tests/test_metrics.py - `_Q166_PARITY_SITES` and `test_q166_parity_every_mirror_matches_live_quantstats` (plan 03), `_q166_golden_with_nan_days`, `test_rank05_inlined_scalars_are_failure_soft_without_quantstats`
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - §Q3 "Prototype parity", §Q4 benign parity anchor values
  </read_first>
  <behavior>
    - the existing parametrized parity test now also covers kelly_criterion, probabilistic_sharpe_ratio (0.0.81 form, until Task 3), common_sense_ratio and cpc_index on the same four benign fixtures at rel 1e-12
    - test_q166_composed_leaf_fault_is_failure_soft: monkeypatching `metrics_module.qs.stats.win_rate` to raise makes exactly kelly_criterion and cpc_index None with WARNINGs naming each, while the other six scalars stay non-None on golden_returns
  </behavior>
  <action>
    Per D-08 (parity with live 0.0.81 is the anchor) and the 159 test triad:
    1. Extend `_Q166_PARITY_SITES` with the four loss-family (key, live oracle) rows.
    2. Add the composed-leaf fault test.
    3. Run the tests. Any parity miss is a mirror defect: fix the operation order in services/metrics.py only if a test proves it, and record the change; never loosen 1e-12.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py -q -k "q166_parity or q166_composed_leaf"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or " failed" in the summary, or fewer than 33 passed (8 sites x 4 fixtures + fault). This floor holds only at THIS point, before Task 3. Task 3 removes the PSR parity row (D-16), so any re-run of this command after Task 3 (a retroactive check after plan 04, or in a later plan) uses the floor 29 (7 sites x 4 fixtures + fault)</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py tests/test_metrics_parity.py -q</automated>
    <fails_when>non-zero exit, or " failed" / " error" in the summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def test_q166_composed_leaf_fault_is_failure_soft' analytics-service/tests/test_metrics.py` prints 1
    - both verify commands exit 0
  </acceptance_criteria>
  <done>Every loss-family mirror is proven equal to live quantstats wherever the guess cannot fire, and a fault inside a composed quantstats leaf is failure-soft and named.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-16 PSR correction (non-excess fourth moment) with an independent in-test anchor, and the single disclosed golden key-path move</name>
  <files>analytics-service/services/metrics.py, analytics-service/tests/test_metrics.py, analytics-service/tests/fixtures/golden_252d_expected.json</files>
  <read_first>
    - analytics-service/services/metrics.py - `_probabilistic_sharpe_ratio` (from Task 1)
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - §Q5 F-2 (Bailey & Lopez de Prado, non-excess gamma4), §Q4 (benign PSR values)
    - analytics-service/tests/test_metrics_parity.py - `test_metrics_parity_full` (how `actual` is built from golden_252d_input) and `test_golden_252d_expected_conftest_fixture_matches_committed_file`
    - analytics-service/tests/fixtures/regen_golden.py - the serializer it uses (`json.dumps(expected, indent=2, sort_keys=True, default=_json_default)`) and its guard flags
  </read_first>
  <behavior>
    - test_q166_psr_matches_the_published_formula (`pytest.mark.parametrize` over its THREE fixtures, one collected case each, so each fixture passes or fails on its own; resolve the conftest fixture `golden_returns` inside the test with `request.getfixturevalue`, as plan 05's r_squared parity test does): for `_rank05_benign_mixed()`, `golden_returns` and `_rank05_benign_all_positive()`, the mirror equals an in-test computation `norm.cdf(SR / sqrt((1 - g3*SR + ((g4 - 1)/4)*SR**2) / (n - 1)))` where SR = mean/std(ddof=1) of the fillna(0) series, g3 = scipy.stats.skew(raw, bias=False, nan_policy="omit"), g4 = scipy.stats.kurtosis(raw, fisher=False, bias=False, nan_policy="omit"), n = len(raw) - to rel 1e-10 (algebraically equal, different operation order)
    - test_q166_psr_is_defined_for_a_steadily_winning_series: on `_rank05_benign_all_positive()` PSR is a real number > 0.5 (live 0.0.81 returns None there because its variance term goes negative - research F-2); the docstring records that pre-fix None
    - the anchor never calls quantstats and never calls pandas `.kurtosis()` / `.skew()`
  </behavior>
  <action>
    Per D-16 and D-10:
    1. Write the two behaviour tests; observe them RED against Task 1's 0.0.81-form mirror (record lines in the SUMMARY).
    2. In `_probabilistic_sharpe_ratio`, change exactly one input: feed the NON-excess fourth moment (pandas excess kurtosis + 3) into the unchanged quantstats expression `1 + 0.5*SR**2 - skew*SR + ((gamma4 - 3)/4)*SR**2`, which is algebraically the published `1 - skew*SR + ((gamma4 - 1)/4)*SR**2`. Rewrite the docblock: WHY (F-2 double subtraction, cite the Bailey & Lopez de Prado paper URL from research Sources), the D-16 decision, the D-10 disclosure, and that its anchor is the in-test formula, not live quantstats.
    3. Remove the PSR row from `_Q166_PARITY_SITES` with a comment pointing to the anchor test (a parity row that now must fail is not kept).
    4. Golden (D-10, one key path): compute the new PSR through the same path `test_metrics_parity_full` uses to build `actual`, and write it into golden_252d_expected.json at key path `metrics_json` -> `metrics_json` -> `probabilistic_sharpe_ratio` with a throwaway script in your session scratchpad that loads the JSON, replaces ONLY that nested key, and dumps with regen_golden.py's exact serializer (indent=2, sort_keys=True, same trailing newline). Do NOT run regen_golden.py (it rewrites inputs and every key). Prove scope with the key-path diff verify below (against the PHASE-BASE golden, so it gives the same verdict after the commit), and `git diff --numstat` shows only that file under tests/fixtures. Record old value 0.5815691494050974 (re-read it) and the new value in the SUMMARY as a D-10 row, plus the benign_mixed (research: 0.7693296699257343 -> ~0.769147) and benign_all_positive (None -> value) rows.
    5. Full suite, parity, mypy strict GREEN.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py -q -k "q166_psr"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or " failed" in the summary, or fewer than 4 passed (3 parametrized cases of test_q166_psr_matches_the_published_formula + test_q166_psr_is_defined_for_a_steadily_winning_series)</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && QZ_BASE="$(git merge-base HEAD origin/main)" && QZ_LOG="$(git log --reverse --format=%H "$QZ_BASE"..HEAD -- services/metrics.py)" && QZ_FIRST="$(printf '%s\n' "$QZ_LOG" | head -1)" && test -n "$QZ_FIRST" && git show "$QZ_FIRST^:analytics-service/tests/fixtures/golden_252d_expected.json" > "${TMPDIR:-/tmp}/q166-04-golden-phase-base.json" && "$QZ_VENV_PY" -c "import json,sys; fl=lambda x,p='': [kv for k,v in x.items() for kv in fl(v, p+'.'+k if p else k)] if isinstance(x,dict) else [(p,x)]; a=dict(fl(json.load(open(sys.argv[1])))); b=dict(fl(json.load(open('tests/fixtures/golden_252d_expected.json')))); moved=sorted(k for k in set(a)|set(b) if a.get(k)!=b.get(k)); print('golden key paths moved since phase base:',moved); sys.exit(0 if moved==['metrics_json.metrics_json.probabilistic_sharpe_ratio'] else 1)" "${TMPDIR:-/tmp}/q166-04-golden-phase-base.json"</automated>
    <fails_when>non-zero exit (including an empty QZ_FIRST), or the printed "golden key paths moved since phase base:" list is anything other than exactly ['metrics_json.metrics_json.probabilistic_sharpe_ratio'] - same verdict before and after the commit, because the base is the phase-base golden, not HEAD</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>non-zero exit, or " failed" / " error" in the pytest summary, or mypy lacks "Success: no issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY records: the RED lines for both PSR tests before the change; the D-10 rows (golden PSR old -> new, benign_mixed old -> new, benign_all_positive None -> new)
    - the key-path diff command printed `golden key paths moved since phase base: ['metrics_json.metrics_json.probabilistic_sharpe_ratio']`
    - `grep -c 'probabilistic_sharpe_ratio' analytics-service/tests/test_metrics.py` prints at least 3
    - after the commit, `git show --stat --format= HEAD -- analytics-service/tests/fixtures` lists only `golden_252d_expected.json`
    - all three verify commands exit 0
  </acceptance_criteria>
  <done>PSR uses the published non-excess moment, is anchored independently of quantstats and pandas, and the single golden key path it moves is disclosed with before/after.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind |
|---|---|
| `_payoff_ratio_no_guess` | function, services/metrics.py |
| `_kelly_criterion`, `_probabilistic_sharpe_ratio`, `_common_sense_ratio`, `_cpc_index` | mirror functions, services/metrics.py |
| the four remaining `_QSTATS_SINGLE_ARG_SCALARS` entries re-pointed to mirrors | changed constant |
| `from scipy.stats import norm` | new module import, services/metrics.py |
| `test_q166_*` (loss invariants x2, metrics_json path, shuffle, parity rows, composed-leaf fault, PSR anchor x2) | tests |
| golden `metrics_json.metrics_json.probabilistic_sharpe_ratio` | fixture byte (D-10 row) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager-supplied returns -> analytics worker | a crafted all-winning CSV with a >=100% day is exactly the trigger class; values persist to metrics_json |
| worker -> Postgres JSONB | NaN / inf are rejected by Postgres |
| planning docs -> public repo | the SUMMARY quotes test output and is world-readable once pushed |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-07 | Tampering (integrity of persisted ratios) | four loss-family scalars | high | mitigate | inline mirrors minus the guess; invariants and the shuffle test observed RED against the pre-mirror state; AST probe asserts no dispatched-scalar / payoff / cvar reference remains |
| T-166-08 | Repudiation (silent value change) | golden PSR / benign PSR | medium | mitigate | D-10 rows in the SUMMARY; flattened key-path diff against the phase-base golden proves exactly one key path moved |
| T-166-02 | Denial of service (NaN/inf to JSONB) | every mirror output | medium | mitigate | all outputs pass through `_safe_qstats_scalar` -> `_safe_float` |
| T-166-09 | Tampering (fault masking) | failure-soft wrapper | low | mitigate | composed-leaf fault test injects into a real quantstats leaf; WARNING names each affected scalar |
| T-166-03 | Information disclosure | SUMMARY quoting tracebacks | medium | mitigate | repo-relative quoting; staged planning-hygiene check before the SUMMARY commit |
| T-166-SC | Tampering (supply chain) | scipy import | low | accept | scipy 1.18.0 is already installed and locked (transitive via quantstats); no install; the Phase 165 promotion note is recorded by research |
</threat_model>

<verification>
- Loss-family invariants and the shuffle test: RED recorded against plan 03's end state, GREEN after.
- Parity with live 0.0.81 at rel 1e-12 on four benign fixtures for kelly / csr / cpc; PSR anchored to the published formula.
- AST probe: no dispatched-scalar quantstats reference and no attribute dispatch in services/metrics.py.
- Golden: exactly one key path moved since the phase base, disclosed.
- Full suite + mypy strict green.
</verification>

<success_criteria>
The remaining WINDOWS.md entry 9 values are gone on the same fixture (common-sense None; PSR > 0.5),
kelly and cpc are proven on the non-monotone trigger, order independence is proven RED-first, and every
changed persisted value in this plan has a before/after row.
</success_criteria>

<output>
Create `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-04-SUMMARY.md`
with: the RED-first evidence (test name, observed pre-fix value) for every new invariant and for the
shuffle test; the parity results; the D-10 rows (metric key, fixture, before, after, reason) for the
canonical trigger, the non-monotone trigger, benign PSR and golden PSR; and an explicit statement that
the "kwarg-closable" claim in WINDOWS.md entry 9 / the 159-05 Residual table is REFUTED (research Q2)
for kelly_criterion, common_sense_ratio and cpc_index.

**SUMMARY hygiene gate (mandatory, before the SUMMARY commit).** `.planning/` is public. Quote every
captured output repo-relative: an interpreter / venv / site-packages prefix becomes `<venv>/`, the
session scratchpad becomes `<scratchpad>/`, any other absolute path becomes its repo-relative form -
never paste a traceback's absolute file paths verbatim. Then, from the worktree root: `git add` the
SUMMARY (the checker enumerates `git ls-files`, so an unstaged new file is invisible to it), link
`node_modules` per D-19 if absent, run `./node_modules/.bin/tsx scripts/check-planning-hygiene.ts`,
then remove the link when it is a symlink. A non-zero exit or any violation line blocks the commit:
fix the text, re-stage, re-run until clean.
</output>
