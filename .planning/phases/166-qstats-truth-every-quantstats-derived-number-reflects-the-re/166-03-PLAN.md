---
phase: 166-qstats-truth-every-quantstats-derived-number-reflects-the-re
plan: 03
type: execute
wave: 2
depends_on: ["166-01"]
files_modified:
  - analytics-service/services/metrics.py
  - analytics-service/tests/test_metrics.py
autonomous: true
requirements: [SC-2, SC-4, SC-5, WINDOWS-5, WINDOWS-9]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC-4 / D-09 (drawdown family): on _rank05_trigger_series (60-day all-winning, max_drawdown 0.0) compute_qstats_scalars returns ulcer_index == 0.0 and recovery_factor / upi / serenity_index None - and the same values reach metrics_json through compute_all_metrics - each assertion OBSERVED RED against the pre-mirror code first"
    - "SC-4 / D-09 on _q166_trigger_nonmonotone: ulcer_index == 0.0 and recovery_factor / upi / serenity_index None, observed RED first"
    - "D-03 applied: recovery_factor, ulcer_index, ulcer_performance_index and serenity_index are inline mirrors (the prepare_returns keyword closes none of them - research Q2); the attribute dispatch over the quantstats stats namespace is gone from compute_qstats_scalars"
    - "D-04: the four mirrors are built on plan 01's primitives (drawdown series, max drawdown, CVaR-of-tail) - no third hand-copy"
    - "D-08: each of the four mirrors equals live quantstats 0.0.81 at rel 1e-12 on benign fixtures where the guess cannot fire (benign_mixed, benign_all_positive, golden_returns, golden_returns with 3 NaN days)"
    - "H-0710 failure-soft contract kept: one failing scalar is None with a WARNING naming it; the dispatch table keeps its _QstatsScalarKey Literal typo guard"
    - "no golden key path moves in this plan (key-path diff against the phase-base golden is empty)"
    - "the SUMMARY passed the planning-hygiene check while staged, before it was committed"
  artifacts:
    - path: analytics-service/services/metrics.py
      provides: "_prepared_returns_no_guess, _recovery_factor, _ulcer_index, _ulcer_performance_index, _serenity_index; _QSTATS_SINGLE_ARG_SCALARS retyped to (key, callable)"
      contains: "def _serenity_index"
    - path: analytics-service/tests/test_metrics.py
      provides: "Phase 166 section: _q166_trigger_nonmonotone, _q166_golden_with_nan_days, _Q166_PARITY_SITES, drawdown-family invariant and parity tests, re-targeted fault tests"
      contains: "_q166_trigger_nonmonotone"
  key_links:
    - from: compute_qstats_scalars
      to: "_QSTATS_SINGLE_ARG_SCALARS (key -> callable)"
      via: "_safe_qstats_scalar(result_key, fn, returns, returns_len)"
      pattern: "_safe_qstats_scalar\\("
    - from: "_ulcer_index / _serenity_index / _recovery_factor"
      to: "_drawdown_series_from_wealth / _max_drawdown_from_wealth / _cvar_of_tail (plan 01)"
      via: "direct call on (1.0 + P(r)).cumprod()"
      pattern: "_drawdown_series_from_wealth\\(|_max_drawdown_from_wealth\\("
---

<objective>
Close the quantstats price-detection heuristic on the four DRAWDOWN-family scalars dispatched by
`compute_qstats_scalars` - `recovery_factor`, `ulcer_index`, `ulcer_performance_index` (upi),
`serenity_index` - by inline mirrors built on plan 01's primitives (D-03 outcome, D-04), prove the
WINDOWS.md entry 9 drawdown-family wrong values are gone on the SAME trigger fixture using economic
invariants observed RED first (SC-4, D-09), and retype the dispatch table to module callables so the
attribute dispatch over the quantstats stats namespace disappears.

Purpose: these values persist to `strategy_analytics.metrics_json`. Research Q2 measured that the
`prepare_returns=` keyword closes none of them. This plan is the first half of the eight-scalar
closure; plan 04 closes the loss/Sharpe family (`kelly_criterion`, `probabilistic_ratio`,
`common_sense_ratio`, `cpc_index`) on the same table. The split exists for per-plan context budget
only; both halves land in this phase.
Output: five new module functions, a retyped dispatch table, a Phase 166 test section, re-targeted
fault tests, no golden movement.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-CONTEXT.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md
@.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-PATTERNS.md
Also read `166-01-SUMMARY.md` in this phase directory once the plan it belongs to has executed (it records what that plan actually built; it does not exist at planning time, so it is not an @-reference).
@.planning/phases/159-rank-public-ranking-integrity/159-05-SUMMARY.md
@analytics-service/services/metrics.py
@analytics-service/tests/test_metrics.py

**Environment.** Python runs ONLY from the worktree's `analytics-service/` directory (never the repo
root - cassette misses make live broker calls), with the MAIN checkout's venv interpreter resolved at
run time: capture `git worktree list --porcelain` into `QZ_WT`, take its first `worktree` path, append
`/analytics-service/.venv/bin/python` -> `QZ_VENV_PY`. Never write that path into a file. Exit 127 = FAIL.
No database, no network, no `uvicorn main:app`, no credential. No SPEC; spec-less probe fallback skipped.

**The 0.0.81 formulas to mirror are quoted in 166-RESEARCH.md §Q3** (read the installed source too:
`quantstats/stats.py` inside the main checkout's venv, via
`"$QZ_VENV_PY" -c "import inspect, quantstats.stats as s; print(inspect.getsource(s.<fn>))"`).
`P(r)` = `_prepare_returns` minus the guess = `r.copy().replace([inf,-inf], nan).fillna(0).replace([inf,-inf], nan)`.
`DD(r)` = `to_drawdown_series` minus the guess = `_drawdown_series_from_wealth((1.0 + r.fillna(0)).cumprod())`
(research measured max abs diff 0.0 against `qs.stats.to_drawdown_series` on five benign fixtures).

**NaN convention (research Pattern 2, recorded divergence - Rule 7):** the mirrors use P(r)
(fillna(0), quantstats' own cleanup), NOT Phase 159's skipna, so that ONLY trigger-shaped series change
value and D-11's census predicate describes the whole affected population. Write that divergence at the
mirror section next to a pointer to 159's skipna note.

**Measured pre-fix values (research Q4) for docstrings and the RED records:**
canonical trigger: recovery_factor 2.0737188382869305, ulcer_index 0.9947130555497081, upi 2.9998728744771372,
serenity_index 0.3204442673452879. Non-monotone trigger: recovery 92.05882352941175, ulcer 0.9919239385213344,
upi 4.117455241335786, serenity 0.36207650738764285. Re-measure; do not trust.

**Golden layout (measured at planning time).** `golden_252d_expected.json` nests the persisted scalars
one level down: the PSR, alpha, beta and eight dispatched scalars live at key paths
`metrics_json.metrics_json.<key>`; rolling series live at `sibling.<kind>`. Golden checks in this phase
compare FLATTENED key paths against the PHASE-BASE golden - the golden at the parent of the first
commit since `git merge-base HEAD origin/main` that touches `analytics-service/services/metrics.py`
(plan 01's first commit) - never against HEAD, so the verdict is the same before and after this task
commits.

**Never use `git checkout --`, `git restore` or `git stash` in any drill** - use byte backups in your
session scratchpad (plan-unique file names), re-taken after every edit.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): RED-first drawdown-family invariants on both trigger fixtures, then the four drawdown-family mirrors wired through the retyped dispatch table into compute_qstats_scalars and metrics_json</name>
  <files>analytics-service/tests/test_metrics.py, analytics-service/services/metrics.py</files>
  <read_first>
    - analytics-service/services/metrics.py - `_QstatsScalarKey`, `_QSTATS_SINGLE_ARG_SCALARS` and its "(result_key, qs.stats attribute name)" comment, `_safe_qstats_scalar`, `_safe_float`, `compute_qstats_scalars` (the loop that passes `getattr(qs.stats, qs_attr)`), and plan 01's primitives (`_drawdown_series_from_wealth`, `_max_drawdown_from_wealth`, `_cvar_of_tail`)
    - analytics-service/tests/test_metrics.py - the "Phase 159 / Plan 05 - RANK-05" section (`_rank05_trigger_series`, `_rank05_benign_all_positive`, `_rank05_benign_mixed`, `test_rank05_trigger_fixture_actually_trips_the_heuristic`, `test_rank05_all_winning_series_has_zero_drawdown`); `test_qstats_scalars_logs_warning_on_qs_failure`; `test_qstats_scalars_dispatch_table_per_entry`
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - §Q2 (spy matrix), §Q3 (formulas), §Q4 (invariants and measured values), "Code Examples"
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-PATTERNS.md - the `_QSTATS_SINGLE_ARG_SCALARS` / `compute_qstats_scalars` section and "P114 mirror docblock"
  </read_first>
  <behavior>
    - test_q166_nonmonotone_trigger_actually_trips_the_heuristic: `_q166_trigger_nonmonotone()` is all-non-negative, has max > 1, has no losing day, and is NOT monotone in value (anti-vacuity guard, modelled on the rank05 guard)
    - test_q166_all_winning_series_has_no_drawdown_derived_ratios: on `_rank05_trigger_series()`, `compute_qstats_scalars(s, None)` gives ulcer_index == 0.0 exactly; recovery_factor, upi, serenity_index are None ("the denominator is a drawdown that does not exist")
    - test_q166_all_winning_drawdown_values_reach_metrics_json: the same four assertions hold on `compute_all_metrics(_rank05_trigger_series())` (the end-to-end path into metrics_json)
    - test_q166_nonmonotone_series_has_no_drawdown_derived_ratios: on `_q166_trigger_nonmonotone()`, ulcer_index == 0.0; recovery_factor, upi, serenity_index None
    - Every docstring states the measured pre-fix value ("Pre-fix measurement: ...") and derives the invariant from economics, never from the implementation's output
  </behavior>
  <action>
    Per D-09 (RED first), D-03 (inline), D-04 (built on primitives), D-08 (0.0.81 arithmetic minus the guess):
    1. Add a "Phase 166 / QSTATS-TRUTH" section after the RANK-05 section in test_metrics.py with helper `_q166_trigger_nonmonotone()` (60 bdays from 2024-01-01; 0.004 on even rows, 0.02 on odd rows; row 0 = 1.5; float64, name "returns") and the four behaviour tests above. Run them against the CURRENT metrics.py: the three invariant tests MUST be RED (the anti-vacuity guard is GREEN by design). Paste the RED lines, with the actual values, into the SUMMARY. A behaviour test that is GREEN before the mirror is vacuous - fix the test, never proceed.
    2. In services/metrics.py, under a "Phase 166 - quantstats 0.0.81 mirrors, MINUS the price guess" section, add the module functions:
       - `_prepared_returns_no_guess(r)` = P(r).
       - `_recovery_factor(r)`: p = P(r); `total = p.sum()`; `max_dd = _max_drawdown_from_wealth((1.0 + p).cumprod())`; NaN when max_dd == 0 else `abs(total) / abs(max_dd)`. Record in the docblock that the numerator is an ARITHMETIC sum while upi's is compounded - a quantstats inconsistency RECORDED, not changed (D-08).
       - `_ulcer_index(r)`: dd = DD(r); `sqrt((dd**2).sum() / (r.shape[0] - 1))` with the RAW row count (research Q3).
       - `_ulcer_performance_index(r)`: u = `_ulcer_index(r)`; NaN when u == 0 else `(r.add(1).prod() - 1) / u` on raw r.
       - `_serenity_index(r)`: dd = DD(r); sd = `r.std()` (RAW); NaN when sd == 0; `var = qs.stats.value_at_risk(dd, confidence=0.95, prepare_returns=False)`; `pitfall = -_cvar_of_tail(dd, var) / sd`; den = `_ulcer_index(r) * pitfall`; NaN when den == 0 else `(r.sum()) / den`.
       Each mirror carries the P114 docblock (WHY INLINE citing the research §Q2 spy result for that function; MATH PARITY quoting the 0.0.81 body; NaN CONVENTION; any recorded divergence). Mirrors return raw floats/NaN; `_safe_float` (via `_safe_qstats_scalar`) maps NaN and +/-inf to None - do not special-case D-09's Nones.
    3. Retype `_QSTATS_SINGLE_ARG_SCALARS` to `tuple[tuple[_QstatsScalarKey, Callable[[pd.Series], float]], ...]` (keep the `_QstatsScalarKey` Literal and the key order). The four drawdown-family keys map to their mirrors. The four loss/Sharpe-family keys (`kelly_criterion`, `probabilistic_sharpe_ratio`, `common_sense_ratio`, `cpc_index`) map, in THIS plan, to the quantstats function objects themselves (`qs.stats.kelly_criterion`, `qs.stats.probabilistic_ratio`, `qs.stats.common_sense_ratio`, `qs.stats.cpc_index`) - the exact call today's attribute dispatch makes, so their values are unchanged here; plan 04 replaces those four entries with mirrors and its RED-first tests are observed against this state. Rewrite the table comment to describe (result_key, callable) and name plan 04 as the owner of the four quantstats entries. Change the `compute_qstats_scalars` loop to pass the callable straight to `_safe_qstats_scalar`; the attribute dispatch over the quantstats stats namespace is deleted. Update the `compute_qstats_scalars` docstring lines that name quantstats functions so they stay true.
    4. Re-target the two fault tests broken by step 3 (the 159 Deviation-2 rule - replace the contract, never delete it): `test_qstats_scalars_logs_warning_on_qs_failure` now detonates `metrics_module._max_drawdown_from_wealth` (a real fault inside the recovery_factor mirror) and keeps "recovery_factor is None + WARNING names recovery_factor + the other scalars still computed"; `test_qstats_scalars_dispatch_table_per_entry` parametrizes over the table's keys and monkeypatches `metrics_module._QSTATS_SINGLE_ARG_SCALARS` with that one entry's callable replaced by a raiser, keeping the per-row "None + WARNING naming the key" asserts (8 rows).
    5. Re-run: the three invariant tests GREEN; full suite, golden parity (unchanged) and mypy strict GREEN; the golden key-path diff against the phase base is empty.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py -q -k "q166_all_winning or q166_nonmonotone or qstats_scalars"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or the summary contains " failed" / " error", or fewer than 14 tests passed (4 new + dispatch table's 8 rows + warning test + complete_set)</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -c "import ast,sys; t=ast.parse(open('services/metrics.py').read()); S={'recovery_factor','ulcer_index','ulcer_performance_index','serenity_index'}; bad=[(n.attr,n.lineno) for n in ast.walk(t) if isinstance(n,ast.Attribute) and ast.unparse(n.value)=='qs.stats' and n.attr in S]; bad+=[('attr-dispatch',n.lineno) for n in ast.walk(t) if isinstance(n,ast.Call) and getattr(n.func,'id',None)=='getattr' and n.args and ast.unparse(n.args[0])=='qs.stats']; print('open drawdown-family quantstats nodes:',bad); sys.exit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or the printed "open drawdown-family quantstats nodes:" list is not empty</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && QZ_BASE="$(git merge-base HEAD origin/main)" && QZ_LOG="$(git log --reverse --format=%H "$QZ_BASE"..HEAD -- services/metrics.py)" && QZ_FIRST="$(printf '%s\n' "$QZ_LOG" | head -1)" && test -n "$QZ_FIRST" && git show "$QZ_FIRST^:analytics-service/tests/fixtures/golden_252d_expected.json" > "${TMPDIR:-/tmp}/q166-03-golden-phase-base.json" && "$QZ_VENV_PY" -c "import json,sys; fl=lambda x,p='': [kv for k,v in x.items() for kv in fl(v, p+'.'+k if p else k)] if isinstance(x,dict) else [(p,x)]; a=dict(fl(json.load(open(sys.argv[1])))); b=dict(fl(json.load(open('tests/fixtures/golden_252d_expected.json')))); moved=sorted(k for k in set(a)|set(b) if a.get(k)!=b.get(k)); print('golden key paths moved since phase base:',moved); sys.exit(0 if moved==[] else 1)" "${TMPDIR:-/tmp}/q166-03-golden-phase-base.json"</automated>
    <fails_when>non-zero exit (including an empty QZ_FIRST, i.e. no plan-01 commit found), or the printed "golden key paths moved since phase base:" list is not []</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest -q -n auto && "$QZ_VENV_PY" -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>non-zero exit, or the pytest summary reports any " failed" / " error", or mypy does not print "Success: no issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY carries the RED run of the three invariant tests against the pre-mirror code, with the observed wrong values (e.g. ulcer_index 0.9947..., recovery_factor 2.0737...)
    - `grep -c '^def _q166_trigger_nonmonotone' analytics-service/tests/test_metrics.py` prints 1
    - `grep -cE '^def _(recovery_factor|ulcer_index|ulcer_performance_index|serenity_index|prepared_returns_no_guess)\(' analytics-service/services/metrics.py` prints 5
    - `grep -c 'Callable\[\[pd.Series\], float\]' analytics-service/services/metrics.py` prints at least 1
    - all four verify commands exit 0
  </acceptance_criteria>
  <done>The four drawdown-family scalars are inline mirrors dispatched as module callables, the trigger-fixture invariants went RED then GREEN, the fault contract is re-targeted, the attribute dispatch is gone, and no golden key path has moved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: live-quantstats parity for the four drawdown-family mirrors on benign fixtures, including NaN-bearing input</name>
  <files>analytics-service/tests/test_metrics.py</files>
  <read_first>
    - analytics-service/tests/test_metrics.py - `_RANK05_PARITY_SITES` + `test_rank05_every_closed_site_matches_live_quantstats_on_a_benign_series`; `test_rank05_inlined_scalars_are_failure_soft_without_quantstats`
    - analytics-service/tests/conftest.py - `golden_returns`, `benchmark_returns`
    - .planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-RESEARCH.md - §Q3 "Prototype parity", §Q4 benign parity anchor values
  </read_first>
  <behavior>
    - test_q166_parity_every_mirror_matches_live_quantstats (two stacked `pytest.mark.parametrize` decorators - one over `_Q166_PARITY_SITES`, one over the four benign fixture names - so each (site, fixture) pair is its OWN collected case and the verify floor of 16 counts cases that exist by construction, never a loop inside one case; resolve `golden_returns` and build the others inside the test via `request.getfixturevalue` / the helper, as plan 05's r_squared parity test does; plan 04's floors of 33 and 29 rely on the same one-case-per-pair shape): for each (key, live oracle) and each of `_rank05_benign_mixed()`, `_rank05_benign_all_positive()`, `golden_returns`, `_q166_golden_with_nan_days(golden_returns)` the mirror equals the live 0.0.81 function at rel 1e-12 (None == None where both are undefined)
    - the benign-mixed row reproduces the research anchor values (e.g. recovery_factor 0.9517263110721105, ulcer_index 0.09511182002853455) to rel 1e-12
  </behavior>
  <action>
    Per D-08 (parity with live 0.0.81 is the anchor) and the 159 test triad:
    1. Add helper `_q166_golden_with_nan_days(s)` returning a copy of s with rows 10, 50 and 100 set to NaN, and `_Q166_PARITY_SITES` as a tuple of (key, oracle) where the oracle calls the LIVE quantstats function on the raw series (live 0.0.81 prepares internally, and on these fixtures the guess cannot fire). This plan's rows: recovery_factor, ulcer_index, upi (ulcer_performance_index), serenity_index. Plan 04 extends the SAME tuple with its four keys; leave a one-line comment saying so.
    2. Add the parametrized parity test above; it reads the mirror value through `compute_qstats_scalars` so the dispatch-table wiring is part of what is proven.
    3. Run the new tests. Any parity miss is a mirror defect: fix the operation order in services/metrics.py only if a test proves it, and record the change; never loosen 1e-12.
  </action>
  <verify>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py -q -k "q166_parity"</automated>
    <fails_when>non-zero exit (exit 5 = nothing selected), or " failed" in the summary, or fewer than 16 passed (4 sites x 4 fixtures)</fails_when>
    <automated>cd analytics-service && QZ_WT="$(git worktree list --porcelain)" && QZ_VENV_PY="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')/analytics-service/.venv/bin/python" && test -x "$QZ_VENV_PY" && "$QZ_VENV_PY" -m pytest tests/test_metrics.py tests/test_metrics_parity.py -q</automated>
    <fails_when>non-zero exit, or " failed" / " error" in the summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^_Q166_PARITY_SITES' analytics-service/tests/test_metrics.py` prints 1
    - `grep -c '^def _q166_golden_with_nan_days' analytics-service/tests/test_metrics.py` prints 1
    - both verify commands exit 0
  </acceptance_criteria>
  <done>Every drawdown-family mirror is proven equal to live quantstats wherever the guess cannot fire, including NaN-bearing input.</done>
</task>

</tasks>

## Artifacts this phase produces (this plan's share)

| Artifact | Kind |
|---|---|
| `_prepared_returns_no_guess` | function, services/metrics.py (P(r); reused by plans 04-06) |
| `_recovery_factor`, `_ulcer_index`, `_ulcer_performance_index`, `_serenity_index` | mirror functions, services/metrics.py |
| `_QSTATS_SINGLE_ARG_SCALARS` retyped to `(key, Callable)`; attribute dispatch deleted | changed constant + loop |
| `_q166_trigger_nonmonotone`, `_q166_golden_with_nan_days`, `_Q166_PARITY_SITES` | test helpers, tests/test_metrics.py |
| `test_q166_*` (anti-vacuity guard, drawdown invariants x2, metrics_json path, parity) | tests |
| re-targeted `test_qstats_scalars_logs_warning_on_qs_failure`, `test_qstats_scalars_dispatch_table_per_entry` | tests |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manager-supplied returns -> analytics worker | a crafted all-winning CSV with a >=100% day is exactly the trigger class; values persist to metrics_json |
| worker -> Postgres JSONB | NaN / inf are rejected by Postgres |
| planning docs -> public repo | the SUMMARY quotes test output and is world-readable once pushed |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-07 | Tampering (integrity of persisted ratios) | four drawdown-family scalars | high | mitigate | inline mirrors minus the guess; economic invariants on two trigger fixtures observed RED first; the attribute dispatch removed and asserted absent by an AST probe |
| T-166-02 | Denial of service (NaN/inf to JSONB) | every mirror output | medium | mitigate | all outputs pass through `_safe_qstats_scalar` -> `_safe_float`; D-09 Nones arise from NaN/inf, never special-cased |
| T-166-09 | Tampering (fault masking) | failure-soft wrapper | low | mitigate | re-targeted fault tests inject into real inline math and into each table entry; WARNING names the scalar |
| T-166-03 | Information disclosure | SUMMARY quoting tracebacks | medium | mitigate | repo-relative quoting; staged planning-hygiene check before the SUMMARY commit |
</threat_model>

<verification>
- Trigger-fixture drawdown invariants: RED recorded before the mirrors, GREEN after (both fixtures, and through compute_all_metrics).
- Parity with live 0.0.81 at rel 1e-12 on four benign fixtures for the four mirrors.
- AST probe: no drawdown-family quantstats reference and no attribute dispatch in services/metrics.py.
- Golden: key-path diff against the phase base is empty.
- Full suite + mypy strict green.
</verification>

<success_criteria>
The four drawdown-family WINDOWS.md entry 9 values are gone on the same fixture (ulcer 0.0; recovery,
upi, serenity None), proven RED-first on two trigger fixtures, bit-equal to live quantstats on benign
input, and no golden byte moved.
</success_criteria>

<output>
Create `.planning/phases/166-qstats-truth-every-quantstats-derived-number-reflects-the-re/166-03-SUMMARY.md`
with: the RED-first evidence (test name, observed pre-fix value) for every new invariant; the parity
results; the D-10 rows (metric key, fixture, before, after, reason) for the four drawdown-family keys on
the canonical and non-monotone triggers; the recorded (not changed) recovery_factor
arithmetic-vs-compounded note; the interim state of the four loss-family table entries handed to plan
04; and an explicit statement that the "kwarg-closable" claim in WINDOWS.md entry 9 / the 159-05
Residual table is REFUTED (research Q2) for recovery_factor.

**SUMMARY hygiene gate (mandatory, before the SUMMARY commit).** `.planning/` is public. Quote every
captured output repo-relative: an interpreter / venv / site-packages prefix becomes `<venv>/`, the
session scratchpad becomes `<scratchpad>/`, any other absolute path becomes its repo-relative form -
never paste a traceback's absolute file paths verbatim. Then, from the worktree root: `git add` the
SUMMARY (the checker enumerates `git ls-files`, so an unstaged new file is invisible to it), link
`node_modules` per D-19 if absent, run `./node_modules/.bin/tsx scripts/check-planning-hygiene.ts`,
then remove the link when it is a symlink. A non-zero exit or any violation line blocks the commit:
fix the text, re-stage, re-run until clean.
</output>
