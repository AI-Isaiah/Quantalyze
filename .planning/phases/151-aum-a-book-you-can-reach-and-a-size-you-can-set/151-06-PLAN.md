---
phase: 151-aum-a-book-you-can-reach-and-a-size-you-can-set
plan: 06
type: execute
wave: 3
depends_on: ["151-05"]
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-state.ts
  - src/app/(dashboard)/allocations/lib/scenario-state.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [AUM-01, AUM-03]

must_haves:
  truths:
    - "A blank-slate scenario can set portfolio AUM directly via one editable field; book mode pre-fills from the live sum and stays editable as an override"
    - "Editing AUM keeps weights fixed and rescales dollar figures; scenarioMetrics do NOT change when AUM changes (AUM-01 is not the SCEN-01 fix)"
    - "A pre-existing v4 draft blob without manualAumUsd decodes ok — never reset; a corrupt stored value sanitizes to undefined"
    - "The AUM-zero refusal names only real affordances — never the nonexistent live-holding toggle"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/scenario-state.ts"
      provides: "manualAumUsd optional additive draft field + zod entry (no range refine)"
      contains: "manualAumUsd"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "PORTFOLIO AUM input, liveHoldingsSum rename, scenarioAum = manual ?? liveSum, new refusal copy"
      contains: "PORTFOLIO AUM"
  key_links:
    - from: "scenarioAum derivation"
      to: "draft.manualAumUsd"
      via: "manual-wins-in-both-modes override (sanitized on read via isValidDollar)"
      pattern: "manualAumUsd"
    - from: "refusal copy"
      to: "UI-SPEC never-strings"
      via: "grep-gate: 'toggle on a live holding' absent from non-test src/"
      pattern: "portfolio AUM is not set"
---

<objective>
Add the direct Portfolio AUM input (AUM-01's first half) and fix the refusal copy
(AUM-03): an optional additive `manualAumUsd` draft field (no schema bump, no refine),
`scenarioAum` becomes `manualAumUsd ?? liveHoldingsSum` so all five existing consumers
keep working, the composer summary gains the ONE editable AUM field per UI-SPEC, and
the commit refusal names only real affordances.

Purpose: AUM is derived-only today — a blank-slate scenario (the primary use case)
structurally cannot size or commit, and the refusal instructs the user to use a
control that was deliberately never built (CONSTIT-03). Founder-locked model: "I
should be able to change AUM, and then the weight changes. That is it."

Output: draft field + codec proof, AUM input with seed idiom, new refusal copy with
grep-gates and the :2468 test rewritten. Plan 151-07 adds the per-strategy dollar
input and commit persistence on top.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-RESEARCH.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-PATTERNS.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-UI-SPEC.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-05-SUMMARY.md

<interfaces>
Contract this plan CREATES (consumed by 151-07):
- `ScenarioDraft.manualAumUsd?: number` (scenario-state.ts, SCENARIO_SCHEMA_VERSION stays 4)
- Composer locals: `liveHoldingsSum` (the renamed toggle-scan memo) and `scenarioAum = sanitizedManualAum ?? liveHoldingsSum` — all downstream consumers keep reading `scenarioAum`
- Refusal constants: `AUM_REFUSAL_NO_BOOK` / `AUM_REFUSAL_BOOK_REACHABLE` (exact strings below)

Verified anchors:
- scenario-state.ts: `SCENARIO_SCHEMA_VERSION = 4` (:79); `leverageOverrides` precedent — field docs :126-135, zod entry :865-874 with the LOAD-BEARING note (z.object STRIPS unknown keys → the field MUST be declared or a persisted value is silently dropped) and the DELIBERATE no-range-refine rule (a refine failure routes to the draft-DELETING reset); `boundedRecord` DoS-cap idiom
- Sanitize-on-read precedent: sanitizeLeverageMap applied at composer decode sites (ScenarioComposer.tsx:1711, :1753) — mirror with isValidDollar at the point of use
- ScenarioComposer.tsx: `scenarioAum` memo :3581-3590 (the toggle-scan to RENAME); five consumers that must keep working unchanged: :3531 drawdown scaling, :3675 refusal gate, :3688 per-row size gate, :4426 illustrative note (auto-clears once AUM > 0 — recorded UI-SPEC decision), :5125 drawer prop; refusal string :3677; seed idiom `windowTouchedRef` :1151-1158; blank-≠-zero commit-on-blur/Enter recipe :5456-5480 (`raw.trim() !== ""`); scenarioMetrics deps :2814 (engineSet/engineState/dateMapCache/blendBasis — NOT scenarioAum)
- dollar-validation.ts: `isValidDollar` (bound [0, 1e12)), `formatUsd` (null → "—", never $0) — the ONE money kit; do not mint another
- Old refusal test to REWRITE (not delete): ScenarioComposer.test.tsx:2460-2475 pins /portfolio AUM is zero/i

Refusal copy (UI-SPEC verbatim, binding):
- AUM_REFUSAL_NO_BOOK: "Can't record a scenario commit: portfolio AUM is not set. Set portfolio AUM before submitting."
- AUM_REFUSAL_BOOK_REACHABLE (when canEnterBook): "Can't record a scenario commit: portfolio AUM is not set. Set portfolio AUM, or switch to \"From my book\", before submitting."
- NEVER-strings: "toggle on a live holding", "Connect an exchange API key"

AUM input contract (UI-SPEC verbatim): eyebrow label `PORTFOLIO AUM (USD)` (Geist Mono uppercase micro tier, tracking 0.18em, text-text-muted); input recipe `rounded border border-border bg-surface px-2 py-1 text-right font-mono text-xs`, ~w-32; blank mode starts EMPTY (never pre-filled 0 — "a zero is a claim"), helper "Required to size and commit."; book mode pre-fills from live sum, override note "Overrides live-holdings total {formatUsd(liveSum)}." when diverged; commits on blur/Enter with the raw.trim() !== "" blank-guard.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: manualAumUsd draft field — optional, additive, no refine, codec-proven</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-state.ts, src/app/(dashboard)/allocations/lib/scenario-state.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-state.ts lines 60-170 (schema version + ScenarioDraft fields + the three optional-additive precedents) and lines 855-905 (zod entries for leverageOverrides/window/memberKeyIds with the no-refine rationale)
    - src/app/(dashboard)/allocations/lib/scenario-state.test.ts (existing codec round-trip cases — extend the same describe structure)
  </read_first>
  <behavior>
    - Test 1 (backward decode): a v4 blob WITHOUT manualAumUsd decodes ok (not reset) — the Phase-59 draft-deleting-reset falsifier.
    - Test 2 (round-trip): encode a draft with manualAumUsd 500000 → decode → value preserved exactly.
    - Test 3 (no refine): a blob with manualAumUsd -5 or 1e13 or NaN-as-null still DECODES ok (schema does not reject) — sanitize happens on read, not in the codec (a refine failure would delete the whole saved scenario).
    - Test 4 (strip-guard): a POSTed/persisted draft retains manualAumUsd through the z.object parse (the field is declared — the silent-strip trap from the :865-874 note).
  </behavior>
  <action>
    Add `manualAumUsd?: number` to `ScenarioDraft` with a doc comment following the
    leverageOverrides template: Phase 151 AUM-01 — manual portfolio AUM override;
    optional + additive, NO schema_version bump (userWeightOverrides/window/
    leverageOverrides precedent); DELIBERATELY no zod range refine (refine failure =
    draft-deleting reset; sanitize-on-read at the composer via isValidDollar instead).
    Add the matching zod entry `manualAumUsd: z.number().optional()` beside the
    leverageOverrides entry with the same LOAD-BEARING declared-or-stripped warning
    comment. SCENARIO_SCHEMA_VERSION stays 4 (assert in a test if the file has that
    idiom). Write behaviors 1-4 in scenario-state.test.ts.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" --no-file-parallelism && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "manualAumUsd" "src/app/(dashboard)/allocations/lib/scenario-state.ts"` shows exactly 2 declaration sites (interface + zod) plus comments
    - `grep -n "SCENARIO_SCHEMA_VERSION = 4" "src/app/(dashboard)/allocations/lib/scenario-state.ts"` still returns 1 (no bump)
    - The zod entry has NO .refine/.min/.max chained (grep the line)
    - Behaviors 1-4 pass; existing codec tests green
  </acceptance_criteria>
  <done>Draft field rides the v4 codec additively; old drafts decode untouched; corrupt values survive decode for read-time sanitization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Portfolio AUM input — rename to liveHoldingsSum, derive scenarioAum, seed without re-snap</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx lines 1140-1165 (windowTouchedRef seed idiom), 1700-1760 (draft decode sites where sanitizeLeverageMap runs — the sanitize-on-read call-site pattern), 3520-3600 (scenarioAum memo + drawdown scaling), 4415-4435 (illustrative note), 5440-5490 (blank-≠-zero blur/Enter input recipe), plus the composer summary/header region to place the input (locate the existing summary block near the entry-mode segment)
    - src/lib/dollar-validation.ts (isValidDollar + formatUsd contracts)
    - How the draft is mutated: locate the scenario store/hook's existing setter surface (e.g. setWeightOverride) in ScenarioComposer.tsx / scenario-state.ts and mirror it for manualAumUsd — read before writing (Rule 8)
  </read_first>
  <behavior>
    - Test 5 (blank mode sizing): blank mode, type 1000000 into the AUM input (blur) → scenarioAum consumers see 1_000_000: the commit refusal gate no longer trips on AUM, the illustrative note at :4426 disappears.
    - Test 6 (blank starts empty): blank mode, no manual value → input value "" (never "0"); helper "Required to size and commit." renders.
    - Test 7 (book seed + no re-snap): book mode with live sum 460000 → input pre-fills 460000; user types 500000; a holdings refresh (rerender with changed rawHoldingsSummary) does NOT re-snap the input (touched-ref idiom); override note renders "Overrides live-holdings total $460,000."
    - Test 8 (metrics invariance — the SCEN-01 guard, economic oracle 3): with a fixed engine fixture, scenarioMetrics (Sharpe/CAGR/maxDD render) are IDENTICAL before and after an AUM edit — asserts AUM-01 did not get wired into the metrics path and cannot be mistaken for the SCEN-01 fix.
    - Test 9 (sanitize on read): a draft carrying manualAumUsd -5 or 2e12 renders as unset (em-dash/empty states), never a negative AUM.
  </behavior>
  <action>
    (1) Rename the :3581 memo to `liveHoldingsSum` (body byte-unchanged). Derive
    `const scenarioAum = sanitizedManualAum ?? liveHoldingsSum;` where
    `sanitizedManualAum = isValidDollar(draft.manualAumUsd) && draft.manualAumUsd > 0 ? draft.manualAumUsd : undefined`
    (sanitize-on-read; 0 is treated as unset — a zero is a claim, UI-SPEC). Every one of
    the five existing scenarioAum consumers keeps working unchanged — verify by grep
    that all consumer reads still reference `scenarioAum`. Comment the derivation:
    Phase 151 AUM-01 — manual wins in BOTH modes; blank mode has no live sum by
    construction (holdingsSummary [] at :859) so manual is the only source there.

    (2) Add the draft write path: mirror the existing draft-mutation API (the same
    store/setter family as setWeightOverride) with a setManualAum(value: number |
    undefined) action that stamps draft.manualAumUsd; do NOT invent a parallel state
    channel — the input is uncontrolled-with-seed, the draft is authority.

    (3) Build the input in the composer summary region per the UI-SPEC contract in the
    interfaces block: mono eyebrow label `PORTFOLIO AUM (USD)` with htmlFor + id;
    type="number", min 0, ~w-32, existing input recipe classes; commits on blur/Enter
    with the `raw.trim() !== ""` blank-guard (:5456-5480 recipe — blank is "no value",
    never 0); boundary-validate with isValidDollar and REFUSE invalid input keeping the
    previous value (mirror handleWeightChange's non-finite posture: console.warn + keep);
    blank mode helper line "Required to size and commit." (text-xs text-text-muted);
    book mode: seed the input once from liveHoldingsSum via a touched-ref
    (aumTouchedRef, windowTouchedRef idiom :1151-1158) and render the override note
    "Overrides live-holdings total {formatUsd(liveHoldingsSum)}." (muted, text-xs) when
    a sanitized manual value exists and differs from liveHoldingsSum. Use formatUsd for
    every read-only money echo — add a one-line comment noting formatUsd (not the
    file-local formatCurrency) is deliberate for NEW money surfaces (PATTERNS
    Correction 4; do not migrate existing formatCurrency sites — Rule 3). data-testid
    "scenario-aum-input" on the input. Write behaviors 5-9.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism -t "AUM input" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "liveHoldingsSum" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` >= 3 (memo + derivation + override note)
    - `grep -n "scenario-aum-input" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` returns 1
    - The input JSX contains no defaultValue/value of literal 0 for blank mode; `grep "PORTFOLIO AUM (USD)"` returns 1
    - Test 8 (metrics invariance) passes and FAILS if scenarioAum is added to the scenarioMetrics dep array (mutate once, revert — the guard bites)
    - All five pre-existing scenarioAum consumer behaviors still green (full file test run in Task 3 gate)
  </acceptance_criteria>
  <done>One AUM field, both modes, manual-wins semantics, seed without re-snap, metrics provably AUM-invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AUM-03 refusal copy — real affordances only, old test rewritten, grep-gates</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx lines 3660-3700 (the refusal gate + string at :3677 and the per-row size gate)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx lines 2440-2480 (the test pinning the OLD copy — REWRITE, do not delete; keep its drawer-not-opened and callback-not-fired assertions)
  </read_first>
  <behavior>
    - Test 10 (new copy, no book): canEnterBook false → alert text is EXACTLY AUM_REFUSAL_NO_BOOK (equality, not regex — the 140.1.1 lesson: pin copy by equality against a literal typed into the test).
    - Test 11 (new copy, book reachable): canEnterBook true, blank mode, AUM unset → alert text EXACTLY AUM_REFUSAL_BOOK_REACHABLE (the "From my book" affordance named only when the segment actually renders).
    - Test 12 (never-strings): the alert text does not contain "toggle on a live holding" nor "Connect an exchange API key"; the retained refusal semantics (drawer not opened, commit callback not fired) still hold.
  </behavior>
  <action>
    Define the two refusal constants (interfaces block, verbatim) as module-level consts
    near the commit handler, with a comment naming the never-strings and why (AUM-03 /
    CONSTIT-03: the live-holding toggle was deliberately never built; the founder hit
    this refusal with four venues connected). Replace the :3677 setCommitError string:
    choose AUM_REFUSAL_BOOK_REACHABLE when canEnterBook (the segment genuinely renders)
    else AUM_REFUSAL_NO_BOOK. Rewrite the :2460-2475 test per behaviors 10-12 —
    equality assertions against literals typed into the test, keep the
    drawer/callback negative assertions. Then run the FULL composer test file plus
    typecheck + lint as this plan's gate.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" --no-file-parallelism && npm run typecheck && npm run lint && bash -c '! grep -rn "toggle on a live holding" src/ --include="*.ts" --include="*.tsx" | grep -v ".test."'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "toggle on a live holding" src/ --include="*.ts" --include="*.tsx" | grep -v ".test." | wc -l` returns 0 (the fossil copy is gone from production code; test files may reference it only as a negative assertion)
    - `grep -c "portfolio AUM is zero" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` returns 0 (old string fully replaced)
    - Tests 10-12 pass with EQUALITY assertions (grep the test for toBe/toEqual on the full literal, not a regex-only match)
    - Full composer + scenario-state test files green; typecheck + lint green
  </acceptance_criteria>
  <done>Refusal names only the AUM input and (when real) the book segment; old copy extinct in src/; old test rewritten with its guard semantics intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → draft state → dollar math | attacker-controlled number feeds divisions and rendering |
| localStorage draft blob → composer | persisted values are untrusted on read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-15 | Tampering / DoS (local) | manualAumUsd input | mitigate | isValidDollar at the input boundary AND sanitize-on-read (ASVS V5); 0/negative/1e12+ → unset; no refine so a corrupt blob can never delete the draft |
| T-151-16 | Information Disclosure (honesty) | illustrative-note interplay | mitigate | note keys off scenarioAum <= 0 and clears when manual AUM set (recorded UI-SPEC decision); the persistent PROJECTED pill still carries the hypothetical disclosure |
| T-151-17 | Repudiation (copy honesty) | refusal copy | mitigate | equality-pinned copy + repo grep-gate on never-strings (AUM-03) |
| T-151-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
- Full composer + scenario-state vitest green with --no-file-parallelism; typecheck + lint green.
- Grep-gates: never-strings absent from non-test src/; old refusal string extinct.
- Coverage ratchet: new branches shipped with tests in the same plan (CI thresholds 82/80/74/72 blocking).
</verification>

<success_criteria>
- Blank-slate scenario sets AUM and passes the commit AUM gate; book mode overrides with note; drafts round-trip additively.
- scenarioMetrics provably invariant under AUM edits.
- Refusal copy names only real affordances; never-strings gone.
</success_criteria>

<output>
Create `.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-06-SUMMARY.md` when done.
</output>
