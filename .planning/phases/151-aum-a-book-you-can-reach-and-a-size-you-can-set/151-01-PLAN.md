---
phase: 151-aum-a-book-you-can-reach-and-a-size-you-can-set
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/mt5_concurrency.py
  - analytics-service/services/job_worker.py
  - analytics-service/services/mt5_client.py
  - analytics-service/tests/test_mt5_concurrency.py
autonomous: true
requirements: [AUM-02]

must_haves:
  truths:
    - "The MT5 terminal serialization machinery is importable from a leaf module without importing job_worker"
    - "The derive arm's MT5 behavior is byte-identical after the extraction (same lock registry object, same helpers)"
    - "Any test or module that clears/patches the registry via job_worker still operates on the ONE shared registry"
  artifacts:
    - path: "analytics-service/services/mt5_concurrency.py"
      provides: "_MT5_TERMINAL_LOCKS, _mt5_terminal_lock_for, _mt5_bounded_restart, _MT5_DERIVE_READ_TIMEOUT_S, _MT5_RESTART_TIMEOUT_S, _Mt5PostReadVerificationError"
      min_lines: 60
    - path: "analytics-service/tests/test_mt5_concurrency.py"
      provides: "registry-identity and re-import regression tests"
      min_lines: 40
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "analytics-service/services/mt5_concurrency.py"
      via: "top-level re-import of the six moved symbols"
      pattern: "from services\\.mt5_concurrency import"
    - from: "analytics-service/services/mt5_client.py"
      to: "analytics-service/services/mt5_concurrency.py"
      via: "terminal_key docstring cross-reference"
      pattern: "mt5_concurrency\\._MT5_TERMINAL_LOCKS"
---

<objective>
Extract the MT5 terminal-concurrency machinery (lock registry, bounded restart, read
timeouts, post-read verification error) from `job_worker.py` into a new leaf module
`services/mt5_concurrency.py`, so Phase 151's holdings branch (plan 151-03, in
`allocator_positions.py`) can reuse the SAME registry without an import cycle.

Purpose: the ROADMAP binding trap says a duplicated `_MT5_TERMINAL_LOCKS` dict
"serializes nothing" — the holdings fetch is a SECOND job kind contending for the ONE
shared Windows terminal. `allocator_positions.py` is imported lazily by the handler
precisely to avoid a cycle with job_worker, so the shared machinery must live in a leaf
module (RESEARCH Pattern 2 option (a), the preferred root fix).

Output: `services/mt5_concurrency.py` (new), job_worker re-importing it with call sites
byte-unchanged, updated `mt5_client.py` docstring pointer, identity tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-RESEARCH.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-PATTERNS.md

<interfaces>
Symbols to MOVE (verified anchors, job_worker.py):
- `_MT5_DERIVE_READ_TIMEOUT_S: Final[float]` — :295 (derived from MT5_REQUEST_TIMEOUT_S + 10s; FLIPRETRY-01 baseline)
- `_MT5_RESTART_TIMEOUT_S: Final[float]` — :307
- `async def _mt5_bounded_restart(client: "Mt5Client") -> None` — :332-352 (MT5CONC-01; docstring notes "kept module-level … because plan 137-02 reuses it")
- `_MT5_TERMINAL_LOCKS: dict[str, asyncio.Lock] = {}` — :379 (MT5CONC-02 comment block at :361-378 MUST move with it)
- `def _mt5_terminal_lock_for(terminal_key: str) -> asyncio.Lock` — :382-387 (setdefault-atomicity comment moves too)
- `class _Mt5PostReadVerificationError(Exception)` — :390-410

Existing consumers that must keep working unchanged:
- job_worker.py derive arm: `:3534` (raise _Mt5PostReadVerificationError), `:3547` (async with _mt5_terminal_lock_for), `:3551` (timeout=_MT5_DERIVE_READ_TIMEOUT_S), `:3569`/`:3602` (await _mt5_bounded_restart), `:3611`/`:3620` (except arms)
- tests/test_mt5_derive_branch.py `:332`/`:334`: `jw._MT5_TERMINAL_LOCKS.clear()` — works after the move ONLY because job_worker re-binds the SAME dict object
- mt5_client.py `:441-453` `terminal_key` docstring names `job_worker._MT5_TERMINAL_LOCKS` — binding trap: pointer must be updated in the same edit (PATTERNS Correction 5)

Registry-idiom precedent for the new module's shape: `services/position_reconstruction.py:308-318` (`_RECONSTRUCT_LOCKS` + `_lock_for`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create services/mt5_concurrency.py by MOVING the six symbols; job_worker re-imports them</name>
  <files>analytics-service/services/mt5_concurrency.py, analytics-service/services/job_worker.py, analytics-service/services/mt5_client.py</files>
  <read_first>
    - analytics-service/services/job_worker.py lines 276-430 (the MT5CONC comment blocks + all six symbols — move the comments/docstrings WITH the code; they are load-bearing documentation of MT5CONC-01/02)
    - analytics-service/services/job_worker.py lines 3489-3630 (the derive-arm call sites that must stay byte-unchanged)
    - analytics-service/services/mt5_client.py lines 435-460 (the terminal_key docstring naming the registry)
    - analytics-service/services/closed_sets.py lines 1-41 (the leaf-module docstring convention: "imports nothing from the rest of services")
    - analytics-service/services/position_reconstruction.py lines 300-320 (registry idiom precedent)
  </read_first>
  <action>
    Create `analytics-service/services/mt5_concurrency.py` as a LEAF module (imports only
    stdlib — asyncio, os, typing — plus a `TYPE_CHECKING`-guarded import of `Mt5Client`
    from `services.mt5_client` for the `_mt5_bounded_restart` annotation; it must NEVER
    import `services.job_worker`). MOVE (cut, not copy) these six symbols from
    job_worker.py with their docstrings and the MT5CONC-01/MT5CONC-02 comment blocks
    intact: `_MT5_DERIVE_READ_TIMEOUT_S`, `_MT5_RESTART_TIMEOUT_S`, `_mt5_bounded_restart`,
    `_MT5_TERMINAL_LOCKS`, `_mt5_terminal_lock_for`, `_Mt5PostReadVerificationError`.
    Keep the exact names (no renames — call-site churn is out of scope). The
    `_MT5_DERIVE_READ_TIMEOUT_S` derivation reads `MT5_REQUEST_TIMEOUT_S` — check where
    that constant lives; if it is defined in job_worker.py, move it too (or replicate the
    exact env-var derivation in the leaf); if it lives in mt5_client or closed_sets,
    import from there (still a leaf-safe direction). Add a module docstring following the
    closed_sets.py leaf convention, stating: this module is the SINGLE terminal-lock
    registry shared by the derive job kind (job_worker) and the holdings job kind
    (allocator_positions, Phase 151) — a second registry dict anywhere serializes nothing
    (MT5CONC-02 / RESEARCH Pitfall 2).

    In job_worker.py, replace the moved definitions with a single top-level re-import:
    `from services.mt5_concurrency import _MT5_DERIVE_READ_TIMEOUT_S, _MT5_RESTART_TIMEOUT_S, _MT5_TERMINAL_LOCKS, _mt5_bounded_restart, _mt5_terminal_lock_for, _Mt5PostReadVerificationError`
    with a one-line comment: "Phase 151: moved to services/mt5_concurrency.py (leaf) so
    allocator_positions.py can share the ONE terminal registry without an import cycle;
    re-imported here so existing call sites and test monkeypatch targets are unchanged."
    Every derive-arm call site stays byte-identical. The registry dict must MOVE, not be
    copied — after this edit exactly ONE `dict[str, asyncio.Lock]` terminal-lock literal
    exists in the repo, in mt5_concurrency.py.

    Update `mt5_client.py` `terminal_key` docstring (:444): replace the
    `job_worker._MT5_TERMINAL_LOCKS` cross-reference with
    `mt5_concurrency._MT5_TERMINAL_LOCKS` (PATTERNS Correction 5 — a stale pointer to a
    moved symbol is a defect).

    Before finishing, grep analytics-service/ for all six symbol names and confirm every
    reference resolves (the job_worker re-import covers `jw._MT5_TERMINAL_LOCKS` in
    test_mt5_derive_branch.py because it binds the same object).
  </action>
  <verify>
    <automated>cd analytics-service && python -c "from services import mt5_concurrency, job_worker; assert job_worker._MT5_TERMINAL_LOCKS is mt5_concurrency._MT5_TERMINAL_LOCKS; assert job_worker._mt5_terminal_lock_for is mt5_concurrency._mt5_terminal_lock_for; assert job_worker._Mt5PostReadVerificationError is mt5_concurrency._Mt5PostReadVerificationError" && python -m pytest tests/test_mt5_derive_branch.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dict\[str, asyncio.Lock\]" analytics-service/services/job_worker.py` returns 0 (registry definition no longer in job_worker)
    - `grep -n "from services.mt5_concurrency import" analytics-service/services/job_worker.py` returns exactly 1 line
    - `grep -c "job_worker._MT5_TERMINAL_LOCKS" analytics-service/services/mt5_client.py` returns 0 AND `grep -c "mt5_concurrency._MT5_TERMINAL_LOCKS" analytics-service/services/mt5_client.py` returns 1
    - `grep -c "import job_worker\|from services.job_worker\|from services import job_worker" analytics-service/services/mt5_concurrency.py` returns 0 (leaf invariant)
    - `cd analytics-service && python -m pytest tests/test_mt5_derive_branch.py -q` exits 0 (derive-arm behavior unchanged, including the registry `.clear()` fixtures)
    - The MT5CONC-01 and MT5CONC-02 comment blocks exist verbatim in mt5_concurrency.py: `grep -c "MT5CONC-02" analytics-service/services/mt5_concurrency.py` >= 1
  </acceptance_criteria>
  <done>mt5_concurrency.py exists as a leaf; job_worker re-imports the identical objects; derive-arm test file green; mt5_client docstring pointer updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registry-identity regression tests + full Python gate</name>
  <files>analytics-service/tests/test_mt5_concurrency.py</files>
  <read_first>
    - analytics-service/services/mt5_concurrency.py (as created by Task 1)
    - analytics-service/tests/test_mt5_derive_branch.py lines 320-345 (existing registry-clearing fixture idiom, to mirror isolation hygiene)
    - analytics-service/tests/test_allocator_positions.py lines 1-30 (docstring convention: numbered required-test list naming requirement IDs)
  </read_first>
  <behavior>
    - Test 1 (identity): `job_worker._MT5_TERMINAL_LOCKS is mt5_concurrency._MT5_TERMINAL_LOCKS` — the registry MOVED, was not copied. Fails if anyone re-declares a dict in job_worker.
    - Test 2 (lock identity per key): `job_worker._mt5_terminal_lock_for("h:1") is mt5_concurrency._mt5_terminal_lock_for("h:1")` returns the SAME asyncio.Lock object (`is` comparison, per RESEARCH Pitfall 2 warning: assert object identity, not merely that a lock was acquired). Clear the registry before/after (setdefault side effect).
    - Test 3 (leaf invariant): importing `services.mt5_concurrency` does not import `services.job_worker` — assert via a fresh subprocess (`python -c "import sys; import services.mt5_concurrency; assert 'services.job_worker' not in sys.modules"`), or by inspecting `sys.modules` after a monkeypatched clean import.
    - Test 4 (timeout constants): `_MT5_DERIVE_READ_TIMEOUT_S > _MT5_RESTART_TIMEOUT_S` and both finite/positive — pins the FLIPRETRY-01 derivation survived the move.
  </behavior>
  <action>
    Write `analytics-service/tests/test_mt5_concurrency.py` with the four behaviors above,
    following the repo docstring convention (numbered required-test list, tagged AUM-02 /
    MT5CONC-02). Use a fixture that clears `_MT5_TERMINAL_LOCKS` before and after each
    test (mirror test_mt5_derive_branch.py's hygiene) so parallel pytest shards on the
    shared registry cannot cross-contaminate. Then run the full analytics gate: full
    pytest from analytics-service/ (NEVER repo root — VCR cassette dir) and
    `mypy .` strict; fix any strict-mode fallout from the move via `cast()` per project
    policy, never `# type: ignore`.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_mt5_concurrency.py -q && python -m pytest -q && mypy .</automated>
  </verify>
  <acceptance_criteria>
    - `cd analytics-service && python -m pytest tests/test_mt5_concurrency.py -q` exits 0 with >= 4 tests passed
    - Neutering falsifier observed once during development: re-declaring `_MT5_TERMINAL_LOCKS = {}` locally in job_worker.py makes Test 1 fail (run once, revert — proves the oracle bites)
    - `cd analytics-service && python -m pytest -q` exits 0 (no regression across the suite)
    - `cd analytics-service && mypy .` exits 0 with zero new `# type: ignore` (`git diff | grep -c "type: ignore"` returns 0)
  </acceptance_criteria>
  <done>Identity tests green and proven falsifiable; full pytest + mypy strict green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → shared Windows MT5 terminal | one physical terminal serialized across job kinds; a serialization break is a data-integrity risk (cross-account reads) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-01 | Tampering (data integrity) | terminal lock registry | mitigate | MOVE the single registry dict; `is`-identity test pins one-object invariant (Task 2 Test 1/2) |
| T-151-02 | Denial of Service | worker event loop | mitigate | bounded-restart + wait_for machinery moved verbatim, derive-arm tests re-run green (WEDGE-01 class preserved) |
| T-151-SC | Tampering | package installs | accept | zero installs this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest -q && mypy .` green.
- Grep gates from Task 1 acceptance criteria all hold.
- No behavior change anywhere: `git diff` in job_worker.py is imports + deleted moved blocks only; derive-arm logic untouched.
</verification>

<success_criteria>
- The six symbols live in `services/mt5_concurrency.py` (leaf); job_worker re-imports the identical objects; the registry moved, not copied.
- mt5_client.py docstring points at the new home.
- Full pytest + mypy strict green from analytics-service/.
</success_criteria>

<output>
Create `.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-01-SUMMARY.md` when done.
</output>
