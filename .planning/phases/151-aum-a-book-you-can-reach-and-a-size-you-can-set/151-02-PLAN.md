---
phase: 151-aum-a-book-you-can-reach-and-a-size-you-can-set
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries.ts
  - src/lib/queries.test.ts
  - src/lib/queries.my-allocation.test.ts
autonomous: true
requirements: [AUM-04]

must_haves:
  truths:
    - "A key linked to a live strategy (either link form) is excluded from allocator book-gate eligibility"
    - "bookEntryGateSatisfied is true when >= 1 allocator-eligible key has per-key dailies, even while perKeyDailiesGateSatisfied is false"
    - "perKeyDailiesGateSatisfied and ALL its existing consumers are byte-unchanged"
    - "Both getMyAllocationDashboard return branches carry the three new fields"
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "deriveStrategyLinkedKeyIds (exported pure), allocatorEligibleApiKeyIds + contributingApiKeyIds + bookEntryGateSatisfied payload fields"
      contains: "deriveStrategyLinkedKeyIds"
  key_links:
    - from: "src/lib/queries.ts deriveStrategylessKeys"
      to: "deriveStrategyLinkedKeyIds"
      via: "shared covered-set construction (one join, cannot drift)"
      pattern: "deriveStrategyLinkedKeyIds\\("
    - from: "getMyAllocationDashboard"
      to: "strategy_keys table"
      via: "narrowed one-off builder, owner_id-scoped"
      pattern: "strategy_keys"
---

<objective>
Split the book-entry gate (AUM-04) at the SSR query layer: extract the Phase-149 role
discriminator into a shared pure function, read `strategy_keys` in
`getMyAllocationDashboard`, and emit THREE new additive payload fields —
`allocatorEligibleApiKeyIds`, `contributingApiKeyIds`, `bookEntryGateSatisfied` — on
BOTH return branches, leaving `perKeyDailiesGateSatisfied` and its five consumers
byte-untouched.

Purpose: `perKeyDailiesGateSatisfied` is load-bearing in five places (liveBaselineMetrics
SSR source, usePerKeySources, MEMBER-04 stamp, calm-fallback note, composer gate) —
mutating it in place would present a 2-of-8-key blend as "your live book" (RESEARCH
Pitfall 3). The ROADMAP binding trap mandates a NEW additive field on BOTH return
branches. Manager-side keys (MANAGER role: strategy-linked series) must not pin the
ALLOCATOR's gate false — role-based, never `exchange === 'mt5'`.

Output: new exported pure predicate + three payload fields + tests reproducing the
founder's PROD census. Consumed by plan 151-05 (composer repoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-RESEARCH.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-PATTERNS.md

<interfaces>
Contract this plan CREATES (consumed by 151-05 — names are binding):

```typescript
// src/lib/queries.ts — exported pure, extracted from deriveStrategylessKeys:354-365
export function deriveStrategyLinkedKeyIds(
  ownStrategies: readonly { id: string; api_key_id: string | null; status: string }[],
  strategyKeyLinks: readonly { strategy_id: string; api_key_id: string }[],
): Set<string>;

// New payload fields on MyAllocationDashboardPayload (both return branches):
allocatorEligibleApiKeyIds: string[];  // eligibleKeyIds minus strategy-linked (manager) keys
contributingApiKeyIds: string[];       // allocatorEligible keys WITH a non-empty per-key series
bookEntryGateSatisfied: boolean;       // contributingApiKeyIds.length > 0
```

Existing anchors (verified this session):
- `deriveStrategylessKeys` — queries.ts:342-375 (W-4: archived is NOT coverage; covered-set at :354-365 covers BOTH link forms: strategies.api_key_id direct + strategy_keys composite)
- `getStrategylessActiveKeys` — :392-465; narrowed `strategy_keys` builder at :406-419 (table absent from database.types.ts — workaround precedent). ⚠️ Owner-column asymmetry: `strategies` scopes `.eq("user_id", userId)`, `strategy_keys` scopes `.eq("owner_id", userId)`.
- `allActiveKeysHavePerKeyDailies` — :2854 (all-or-nothing predicate — UNTOUCHED)
- `isPerKeyDailiesEligibleKey` — :2890
- Gate hoist + payload ride — :3717-3725; payload emit branches at ~:3799 (!portfolio branch) and ~:4223 (main branch)
- Payload type decls with SoT-mirror docstrings — :2394-2408 (`perKeyDailiesGateSatisfied`, `eligibleApiKeyIds` precedent)
- Founder PROD census (fixture ground truth, Phase 149): 8 active keys → 4 strategies (one 3-key deribit composite via strategy_keys, three MT5 via strategies.api_key_id) → exactly 2 bare allocator keys (bybit 155 dailies, okx 100); deribit ×3 and mt5 ×3 have ZERO per-key dailies.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract deriveStrategyLinkedKeyIds as an exported pure function + census-pinned tests</name>
  <files>src/lib/queries.ts, src/lib/queries.test.ts</files>
  <read_first>
    - src/lib/queries.ts lines 270-475 (deriveStrategylessKeys + its docstring documenting the two-link-form trap and the W-4 archived ruling + getStrategylessActiveKeys)
    - src/lib/queries.my-strategies.test.ts (existing pure-predicate test sibling for deriveStrategylessKeys — fixture shapes to mirror)
  </read_first>
  <behavior>
    - Test 1 (both link forms): given strategies [{id:"s1", api_key_id:"k1", status:"active"}] and links [{strategy_id:"s2", api_key_id:"k2"}] with s2 live, the returned Set contains k1 AND k2.
    - Test 2 (archived is not coverage, W-4): a key linked ONLY to a strategy with status "archived" (via either form) is NOT in the Set.
    - Test 3 (link to non-live strategy id dropped): a strategy_keys link whose strategy_id is not in the live-strategy set contributes nothing.
    - Test 4 (founder census): 8 keys, 4 live strategies — 3 MT5 keys covered via strategies.api_key_id, 3 deribit keys covered via one composite's strategy_keys rows, bybit + okx uncovered → Set has exactly 6 members, excludes the bybit and okx ids.
    - Test 5 (no drift): deriveStrategylessKeys still returns exactly the 2 bare keys on the census fixture (proves the extraction changed zero behavior).
  </behavior>
  <action>
    In queries.ts, extract the `covered` Set construction from `deriveStrategylessKeys`
    (the block at :354-365: live-strategy filter with `s.status !== "archived"`, liveIds
    Set, direct `api_key_id` links, `strategyKeyLinks` filtered by liveIds membership)
    into a new exported pure function `deriveStrategyLinkedKeyIds(ownStrategies,
    strategyKeyLinks): Set<string>` with the signature in the interfaces block. Have
    `deriveStrategylessKeys` call it — same semantics, zero behavior change. Carry a
    docstring stating: this is THE manager-role discriminator for Phase 151 AUM-04
    (role-based, never venue-based — an `exchange === 'mt5'` predicate is the named
    wrong-fix class), shared by deriveStrategylessKeys and the dashboard book gate so
    the two views of "strategy-linked" cannot drift; W-4 archived-is-not-coverage
    ruling applies. Write the five behavior tests in src/lib/queries.test.ts (create the
    describe block; the file exists) using plain object fixtures.
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.test.ts src/lib/queries.my-strategies.test.ts --no-file-parallelism && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function deriveStrategyLinkedKeyIds" src/lib/queries.ts` returns exactly 1 line
    - `deriveStrategylessKeys` body contains a call to `deriveStrategyLinkedKeyIds(` (grep) and no longer contains its own inline `new Set<string>([` covered-set literal
    - All 5 behavior tests pass; existing queries.my-strategies.test.ts stays green unmodified
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Shared discriminator exported and census-pinned; deriveStrategylessKeys delegates to it with zero behavior change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getMyAllocationDashboard reads strategy_keys and emits the three new fields on BOTH branches</name>
  <files>src/lib/queries.ts, src/lib/queries.my-allocation.test.ts</files>
  <read_first>
    - src/lib/queries.ts lines 2380-2410 (payload type decls + SoT-mirror docstring convention), lines 3690-3810 (gate hoist, eligible-filter idiom, !portfolio return branch), lines 4200-4240 (main return branch)
    - src/lib/queries.ts lines 397-427 (the narrowed strategy_keys builder to replicate — owner_id scope, StrategyKeyLinkRow shape, and the comment logging type-regeneration as deferred)
    - src/lib/queries.my-allocation.test.ts (existing payload tests; the "Phase 37: !portfolio branch exposes the per-key channel with empty/false defaults" test at ~:525 is the precedent to mirror)
  </read_first>
  <behavior>
    - Test 1 (relaxation semantics): fixture with 8 eligible keys where only bybit+okx have per-key series and 6 are strategy-linked → `bookEntryGateSatisfied` true, `contributingApiKeyIds` = [bybit, okx], `allocatorEligibleApiKeyIds` = [bybit, okx], AND `perKeyDailiesGateSatisfied` false in the SAME fixture (the split, not a mutation).
    - Test 2 (partial allocator book): 2 allocator-eligible keys, only 1 with a series → gate true, contributing has exactly 1 id, allocatorEligible has 2.
    - Test 3 (no contributing keys): allocator-eligible keys exist but none has a series → `bookEntryGateSatisfied` false.
    - Test 4 (!portfolio branch defaults): fresh allocator (no portfolio) → fields present with `[]` / `[]` / `false`, never undefined.
    - Test 5 (consumer freeze): `liveBaselineMetrics` source selection still keys on `perKeyDailiesGateSatisfied` — in the Test 1 fixture (old gate false, new gate true) the baseline is still the honest emptyDefault, not a partial blend.
  </behavior>
  <action>
    In `getMyAllocationDashboard`: (1) ensure the existing `strategies` read projects
    `id, api_key_id, status` scoped `.eq("user_id", userId)` (widen the select ONLY if a
    column is missing — surgical); (2) add a `strategy_keys` read via a narrowed one-off
    builder replicating the :406-419 precedent byte-for-byte in shape (type
    `StrategyKeyLinkRow = { strategy_id: string; api_key_id: string }`, literal
    `.eq("owner_id", userId)` — note the owner-column asymmetry vs strategies' user_id),
    fetched in the existing `Promise.all` batch to avoid a waterfall; (3) after the
    existing `perKeyDailiesGateSatisfied` hoist (:3725), compute:
    `strategyLinkedKeyIds = deriveStrategyLinkedKeyIds(ownStrategies, strategyKeyLinks)`,
    `allocatorEligibleApiKeyIds = eligibleKeyIds.filter(id => !strategyLinkedKeyIds.has(id))`,
    `contributingApiKeyIds = allocatorEligibleApiKeyIds.filter(id => (perKeyReturnsByApiKeyId[id]?.length ?? 0) > 0)`,
    `bookEntryGateSatisfied = contributingApiKeyIds.length > 0`; (4) declare the three
    fields on the payload type near :2394-2408 with the SoT-mirror docstring convention
    ("read-only; the client must NEVER re-derive" + Phase 151 / AUM-04 tag); (5) emit
    them on BOTH return branches (~:3799 and ~:4223) with the Phase-37-style additive
    comment — a field on only one branch is `undefined` for fresh allocators and the
    composer's `?? false` fallback would mask it (RESEARCH Pattern 7 checklist item).
    Do NOT touch `allActiveKeysHavePerKeyDailies`, the `perKeyDailiesGateSatisfied`
    computation, or the liveBaselineMetrics ternary. If a strategy_keys read error
    occurs, degrade the same way getStrategylessActiveKeys does (check its error arm and
    mirror it — fail toward an empty link list, which biases keys toward allocator
    eligibility; comment the choice).
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.my-allocation.test.ts src/lib/queries.test.ts --no-file-parallelism && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bookEntryGateSatisfied" src/lib/queries.ts` >= 4 (type decl + compute + two return branches)
    - `grep -c "allocatorEligibleApiKeyIds" src/lib/queries.ts` >= 3 and `grep -c "contributingApiKeyIds" src/lib/queries.ts` >= 3
    - The `strategy_keys` builder in getMyAllocationDashboard scopes `.eq("owner_id",` (grep confirms; NOT user_id)
    - `git diff src/lib/queries.ts` shows ZERO changes to `allActiveKeysHavePerKeyDailies` and zero changes to the line computing `perKeyDailiesGateSatisfied`
    - All 5 behavior tests pass; full file `npx vitest run src/lib/queries.my-allocation.test.ts` green
    - `npm run typecheck` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>Three additive fields ride both branches; old gate and its consumers provably untouched; census fixture proves the split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR query → Postgres (strategy_keys) | new owner-scoped read; RLS backstop but literal scoping is the control |
| SSR payload → browser | gate fields are SoT-mirror: client must never re-derive eligibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-03 | Elevation of Privilege | strategy_keys narrowed builder | mitigate | literal `.eq("owner_id", userId)` per the :406-419 precedent; narrow ONE builder, never widen the client (ASVS V4) |
| T-151-04 | Information Disclosure (honesty) | liveBaselineMetrics selection | mitigate | old gate byte-untouched + Test 5 consumer-freeze assertion — a partial blend can never present as "your live book" |
| T-151-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/queries.test.ts src/lib/queries.my-allocation.test.ts src/lib/queries.my-strategies.test.ts --no-file-parallelism` green.
- `npm run typecheck && npm run lint` green.
- Grep gates from both tasks hold.
</verification>

<success_criteria>
- `deriveStrategyLinkedKeyIds` exported, census-pinned, shared by deriveStrategylessKeys.
- `allocatorEligibleApiKeyIds` / `contributingApiKeyIds` / `bookEntryGateSatisfied` on BOTH return branches with defaults on the !portfolio branch.
- `perKeyDailiesGateSatisfied` and all five consumers provably unchanged.
</success_criteria>

<output>
Create `.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-02-SUMMARY.md` when done.
</output>
