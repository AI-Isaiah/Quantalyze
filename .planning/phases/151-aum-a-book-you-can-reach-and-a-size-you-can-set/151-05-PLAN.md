---
phase: 151-aum-a-book-you-can-reach-and-a-size-you-can-set
plan: 05
type: execute
wave: 2
depends_on: ["151-02"]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [AUM-04]

must_haves:
  truths:
    - "An allocator with >= 1 contributing key sees the 'From my book' segment and the composer initializes to book mode — never forced blank"
    - "Book mode renders toggle rows ONLY for contributing keys; non-contributing eligible keys get the '{N} of {M} keys not yet contributing' note — never silent, never a dead 0.000 row"
    - "liveBaselineMetrics selection, MEMBER-04 stamp, and the calm-fallback's no-contributing case still key on the OLD perKeyDailiesGateSatisfied flag"
    - "Manager keys appear in NEITHER count of the partial-book note"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "canEnterBook/usePerKeySources repointed to bookEntryGateSatisfied; dataSourceKeys narrowed to contributingApiKeyIds; partial-book note"
      contains: "bookEntryGateSatisfied"
  key_links:
    - from: "ScenarioComposer canEnterBook"
      to: "payload.bookEntryGateSatisfied"
      via: "SoT-mirror read (client never re-derives)"
      pattern: "payload\\.bookEntryGateSatisfied"
    - from: "dataSourceKeys"
      to: "payload.contributingApiKeyIds"
      via: "narrowed filter (Pitfall 4)"
      pattern: "contributingApiKeyIds"
---

<objective>
Repoint the composer's book-entry gate to the new split fields from 151-02: the
"From my book" segment renders on a partial book (>= 1 contributing allocator key),
manager keys stop pinning the gate, forced-blank disappears, and the per-key
data-source list narrows to contributing keys with an honest partial-book note.

Purpose: the founder's ~$460k book (8 keys, 3 deribit + 3 mt5 with zero per-key
dailies) is hidden by the all-or-nothing gate — blank slate was FORCED, not chosen
(AUM-04 root cause). The repoint must touch ONLY the in-scope consumers: RESEARCH
Pitfall 3 pins liveBaselineMetrics (queries-side, untouched by construction) and the
MEMBER-04 stamp on the OLD flag; Pitfall 4 mandates the dataSourceKeys narrowing so a
relaxed gate cannot render dead 0.000 rows with a mismatched bookEquity basis.

Output: composer gate surgery + partial-book note + tests. Plans 151-06/07 build the
AUM/dollar inputs on this file afterward (later waves — exclusive file ownership).

Deliberate narrowing (recorded): Task 1 Test 2 pins forced-blank for the
ZERO-contributing-key case. CONTEXT's "never force-initializes to blank" decision is
deliberately narrowed here on RESEARCH grounds (Open Q4): an engineless "From my book"
(zero contributing keys → empty per-key engine) is a worse dead end than blank mode,
and AUM-01's manual AUM input (151-06) removes blank mode's residual harm — it can now
size and commit. The narrowing applies ONLY when contributingApiKeyIds is empty; >= 1
contributing key always reaches book mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-RESEARCH.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-PATTERNS.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-UI-SPEC.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-02-SUMMARY.md

<interfaces>
From 151-02 (payload contract — SoT-mirror, read-only):
- `payload.allocatorEligibleApiKeyIds: string[]` — eligible minus strategy-linked (manager) keys
- `payload.contributingApiKeyIds: string[]` — allocator-eligible keys WITH per-key series
- `payload.bookEntryGateSatisfied: boolean` — contributing.length > 0
- `payload.perKeyDailiesGateSatisfied: boolean` — UNCHANGED all-or-nothing (still on the payload)

Composer consumer map (verified anchors; RESEARCH Pitfall 3 dispositions):
- `:848` `canEnterBook = hasLiveBook && payload.perKeyDailiesGateSatisfied` → REPOINT to bookEntryGateSatisfied (the one in-scope consumer)
- `~:1511` mode-switch handler gated on the same flag → REPOINT (it is canEnterBook-adjacent)
- `:2406` `usePerKeySources` → REPOINT to bookEntryGateSatisfied (RESEARCH Open Q4 decision: "From my book" with an added-only empty engine is a worse dead end than the refusal)
- `:1627/:1657/:1946/:1966` MEMBER-04 membership derive/stamp + reopen fingerprint → STAY on OLD flag (research directive)
- `:2481-2484` `showDataSourcesFallback` → gains `&& !payload.bookEntryGateSatisfied` (renders only when NO key contributes; the partial case is covered by the NEW note)
- `:2489-2492` `dataSourceKeys` filters on `eligibleApiKeyIds` → NARROW to contributingApiKeyIds (Pitfall 4; bookEquity at :2513-2517 sums over dataSourceKeys so the notional basis narrows consistently)
- `:4069-4080` fallback InfoBanner render (template for the new note's placement/recipe)

UI-SPEC partial-book note (binding): copy "{N} of {M} keys not yet contributing — no per-key history yet." where M = allocatorEligibleApiKeyIds.length, N = M − contributingApiKeyIds.length; muted (text-xs text-text-muted), NEVER amber/red (steady state, not transient); plain static text, no role="alert"; rendered in book mode directly above the per-key data-sources list; data-testid on the wrapper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repoint the gate consumers — canEnterBook, mode-switch, usePerKeySources; pin the frozen consumers</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx lines 830-870 (canEnterBook + entryMode init + holdingsSummary), 1500-1520 (mode-switch handler), 1620-1670 and 1940-1970 (MEMBER-04 sites — read to confirm they are NOT edited), 2400-2520 (usePerKeySources, showDataSources, fallback gate, dataSourceKeys, bookEquity)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx lines 1-200 (the payload builder fixtures — extend, never fork; the file is ~10,691 lines and already has builders)
  </read_first>
  <behavior>
    - Test 1 (partial book reaches book mode): payload with bookEntryGateSatisfied true + perKeyDailiesGateSatisfied false + live holdings → "From my book" segment renders AND initial entryMode is "book" (forced-blank removal — CONTEXT locked).
    - Test 2 (no contributing keys): bookEntryGateSatisfied false + live holdings → segment absent, composer initializes blank, showDataSourcesFallback note renders (the no-contributing case keeps its calm note).
    - Test 3 (engine set under partial book): bookEntryGateSatisfied true + perKeyDailiesGateSatisfied false → usePerKeySources true (per-key engine feeds the projection; an added-only empty engine would be a dead end).
    - Test 4 (MEMBER-04 freeze): in the Test 1 fixture, the membership derive/stamp behavior is identical to a fixture where BOTH flags are false — assert whatever observable the stamp produces (fingerprint/memberKeyIds) does not change with the new flag alone (consult the existing MEMBER-04 tests in this file and extend their fixture matrix).
  </behavior>
  <action>
    In ScenarioComposer.tsx: change `:848` to `const canEnterBook = hasLiveBook &&
    (payload.bookEntryGateSatisfied ?? false);` with a comment block replacing the
    ENGINE-03 note: Phase 151 AUM-04 — book entry now keys on the SPLIT gate
    (bookEntryGateSatisfied: >= 1 contributing allocator key; manager keys excluded
    SSR-side); perKeyDailiesGateSatisfied remains all-or-nothing and still governs
    liveBaselineMetrics (queries-side) and the MEMBER-04 stamp — do NOT repoint those
    (RESEARCH Pitfall 3). Repoint the mode-switch handler gate (~:1511) and
    `usePerKeySources` (:2406) to the same `payload.bookEntryGateSatisfied ?? false`
    read, each with a one-line Phase-151 comment naming the Open-Q4 decision on
    usePerKeySources. Leave `:1627/:1657/:1946/:1966` byte-untouched. Add one explicit
    code comment near holdingsSummary/scenarioAum consumption recording the Pitfall-5
    decision: "AUM is custody (all active keys' holdings, manager-side included); the
    gate is modelling capability (allocator keys with per-key history) — deliberate
    asymmetry; no copy may claim the AUM is 'from these N keys'." At the entryMode
    initialization site, add a comment recording the deliberate narrowing: a
    zero-contributing book still initializes blank — a RESEARCH-backed (Open Q4)
    narrowing of CONTEXT's "never force-initializes to blank" decision; engineless book
    mode is a worse dead end, and the 151-06 manual AUM input removes blank mode's
    residual harm. Extend the test
    payload builder with the three new fields (defaulting to the legacy-equivalent
    values so existing tests stay green: allocatorEligibleApiKeyIds = eligibleApiKeyIds,
    contributingApiKeyIds = gate ? eligibleApiKeyIds : [], bookEntryGateSatisfied =
    perKeyDailiesGateSatisfied) and write behaviors 1-4.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism -t "AUM-04" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "payload.bookEntryGateSatisfied" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` >= 3 (canEnterBook, mode-switch, usePerKeySources)
    - `git diff` shows ZERO changes on the MEMBER-04 lines (:1627/:1657/:1946/:1966 regions)
    - Test 1 fails if canEnterBook is reverted to the old flag (mutate once, revert)
    - Behaviors 1-4 pass; the full pre-existing ScenarioComposer.test.tsx suite passes
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Gate repointed at exactly three consumers; frozen consumers proven untouched; partial book reaches book mode with a per-key engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrow dataSourceKeys to contributing keys + partial-book note + fallback reconcile</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx lines 2460-2530 (showDataSources, showDataSourcesFallback, dataSourceKeys, perKeySourceIds, bookEquity), 4060-4085 (the InfoBanner fallback render — placement + recipe template for the new note)
    - .planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-UI-SPEC.md (Copywriting Contract + partial-book invariants — binding copy and color gates)
  </read_first>
  <behavior>
    - Test 5 (narrowing): fixture with 4 allocator-eligible keys, 2 contributing → exactly 2 toggle rows render; the 2 non-contributing keys render NO row (no dead 0.000 inputs — Pitfall 4).
    - Test 6 (note content): same fixture, book mode → the note renders exactly "2 of 4 keys not yet contributing — no per-key history yet." inside a muted (non-amber, non-red) wrapper with a data-testid; no role="alert" attribute.
    - Test 7 (manager keys in neither count): 6 strategy-linked keys present in payload.apiKeys but absent from allocatorEligibleApiKeyIds → counts stay "2 of 4", never "of 10" (UI-SPEC invariant: "not yet contributing" must never describe a key that will never contribute).
    - Test 8 (never silent / fully contributing): all eligible keys contributing → note absent; and blank mode → note absent (book-mode-only).
    - Test 9 (fallback reconcile): bookEntryGateSatisfied true + perKeyDailiesGateSatisfied false → the OLD showDataSourcesFallback banner does NOT render (its "blends your whole book" sentence is now false); bookEntryGateSatisfied false → it still renders.
  </behavior>
  <action>
    Narrow `dataSourceKeys` (:2489-2492) to filter on `payload.contributingApiKeyIds ??
    []` instead of eligibleApiKeyIds, updating the SoT-mirror comment (Phase 151
    AUM-04 / Pitfall 4: a non-contributing key has no engine unit — rendering its
    toggle row would show a dead 0.000 weight and skew the bookEquity basis; bookEquity
    at :2513-2517 sums over dataSourceKeys, so the notional basis narrows with it —
    note that in the comment). Add `&& !(payload.bookEntryGateSatisfied ?? false)` to
    `showDataSourcesFallback` (:2481-2484) with a comment: under a partial book the
    fallback's whole-book-blend sentence is false — the partial-book note (below) owns
    that state. Add the partial-book note: compute
    `allocatorEligibleCount = (payload.allocatorEligibleApiKeyIds ?? []).length` and
    `notYetContributing = allocatorEligibleCount - (payload.contributingApiKeyIds ?? []).length`;
    render when `entryMode === "book" && notYetContributing > 0` a plain muted div
    (className `mt-2 text-xs text-text-muted`, data-testid
    "scenario-partial-book-note", NO role="alert", NO InfoBanner amber styling —
    UI-SPEC color gate: muted, never amber/red) directly above the per-key
    data-sources list, with the exact copy `{N} of {M} keys not yet contributing — no
    per-key history yet.` (U+2014 em dash). Write behaviors 5-9, then run the full
    composer test file and lint.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "scenario-partial-book-note" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` returns 1 render site; the copy string matches the UI-SPEC template with the em dash (grep "not yet contributing — no per-key history yet")
    - The note's wrapper contains `text-text-muted` and does NOT contain `role="alert"`, `border-warning`, or amber tokens (grep the JSX block)
    - `dataSourceKeys` memo references `contributingApiKeyIds` and no longer references `eligibleApiKeyIds` (grep)
    - Behaviors 5-9 pass; full ScenarioComposer.test.tsx green; `npm run typecheck` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>Book mode shows only contributing keys with an honest muted note; the old fallback owns only the zero-contributing state; bookEquity basis consistent with the engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR payload → composer render | gate values are SoT-mirror; the client must not re-derive eligibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-13 | Information Disclosure (honesty) | Overview baseline vs book mode | mitigate | liveBaselineMetrics stays on the old all-or-nothing flag (frozen by 151-02 Test 5 + this plan's diff discipline) — a partial blend never presents as the whole live book |
| T-151-14 | Tampering (client re-derive) | gate reads | mitigate | all three repointed consumers read `payload.bookEntryGateSatisfied` verbatim; no client-side predicate re-derivation (SoT-mirror comments enforced) |
| T-151-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
- Full `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism` green; typecheck + lint green.
- Manual (post-merge PROD UAT, per 151-VALIDATION.md): founder account shows "From my book". Census math: 8 keys, 6 strategy-linked, bybit+okx both contributing ⇒ allocatorEligible=2, contributing=2 ⇒ the partial-book note is ABSENT (0 of 2 not yet contributing) — verify its ABSENCE, not a "6 of …" reading; the note's rendering is exercised by the unit fixtures (Tests 5-7), and manager keys appear in neither count (Test 7).
</verification>

<success_criteria>
- Partial book reaches book mode; forced blank gone; manager keys pin nothing.
- Only contributing keys render toggle rows; the muted partial-book note is never silent in the partial state.
- Frozen consumers (baseline, MEMBER-04) provably untouched.
</success_criteria>

<output>
Create `.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-05-SUMMARY.md` when done.
</output>
