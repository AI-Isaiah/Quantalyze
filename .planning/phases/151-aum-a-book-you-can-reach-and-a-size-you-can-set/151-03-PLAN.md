---
phase: 151-aum-a-book-you-can-reach-and-a-size-you-can-set
plan: 03
type: execute
wave: 2
depends_on: ["151-01"]
files_modified:
  - analytics-service/services/allocator_positions.py
  - analytics-service/services/job_worker.py
  - analytics-service/services/closed_sets.py
  - analytics-service/tests/test_allocator_positions_non_ccxt.py
autonomous: true
requirements: [AUM-02]

must_haves:
  truths:
    - "An MT5 key's holdings sync produces ONE account-equity row (USD account) or an honest human-copy skip — never a raw Python AttributeError in sync_error"
    - "The MT5 holdings read serializes on the SAME terminal lock object as the derive job"
    - "Existing ccxt venues (binance/bybit/okx/deribit) reach fetch_balance/fetch_positions byte-unchanged"
    - "A genuine MT5 transport failure stamps human copy AND retries (transient), never str(exc)"
  artifacts:
    - path: "analytics-service/services/allocator_positions.py"
      provides: "venue dispatch in fetch_allocator_holdings, _fetch_mt5_account_rows, AllocatorHoldingsSyncTransientError, honest-skip copy constants"
      contains: "_NON_CCXT_HOLDINGS_FETCHERS"
    - path: "analytics-service/services/closed_sets.py"
      provides: "NON_CCXT_VENUES frozenset"
      contains: "NON_CCXT_VENUES"
    - path: "analytics-service/tests/test_allocator_positions_non_ccxt.py"
      provides: "MT5 branch tests: disabled, non-USD, equity-not-balance, lock identity, ccxt fall-through"
      min_lines: 120
  key_links:
    - from: "analytics-service/services/allocator_positions.py"
      to: "analytics-service/services/mt5_concurrency.py"
      via: "shared terminal-lock registry import (the ONE registry)"
      pattern: "from services\\.mt5_concurrency import"
    - from: "analytics-service/services/job_worker.py run_poll_allocator_positions_job"
      to: "AllocatorHoldingsSyncTransientError"
      via: "dedicated except arm BEFORE the generic classify_exception arm"
      pattern: "AllocatorHoldingsSyncTransientError"
---

<objective>
Add explicit venue-capability dispatch at the ONE holdings chokepoint
(`fetch_allocator_holdings`) and implement the MT5 branch: kill-switch gate, shared
terminal lock, login bracket, bounded read, ONE account-equity holdings row per MT5
account (account-scoped symbol), honest non-USD skip — and a dedicated handler except
arm so a genuine MT5 failure stamps human copy and retries.

Purpose: PROD key `46293712-…` carries `sync_error = "'Mt5Session' object has no
attribute 'fetch_balance'"` — a raw Python AttributeError in a user-visible column
(AUM-02). `_make_exchange_client` grew mt5/sfox while its holdings consumer stayed
ccxt-only. Fix at the venue CLASS chokepoint (dispatch on the venue STRING, never
hasattr — CONTEXT-locked); plan 151-04 adds the sFOX branch and the parametrized
class-closure proof on this scaffolding.

Output: dispatch + MT5 branch + copy constants + transient-exception plumbing + MT5
tests. sync_error becomes end-user copy (rendered verbatim by AllocatorSyncStatus).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-RESEARCH.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-PATTERNS.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-UI-SPEC.md

<interfaces>
Contract this plan CREATES (consumed by 151-04):

```python
# allocator_positions.py
_NON_CCXT_HOLDINGS_FETCHERS: dict[str, ...]   # "mt5" -> _fetch_mt5_account_rows (151-04 adds "sfox")

class AllocatorHoldingsSyncTransientError(Exception):
    """str(self) IS end-user copy (sync_error column). Handler stamps it verbatim
    with sync_status='error' and error_kind='transient' so the job retries."""

# Copy constants (UI-SPEC "sync_error copy class" — worker-written = end-user copy,
# pattern "{what happened} — {what happens next or what to do}", U+2014 em dash):
UNSUPPORTED_VENUE_NOTE = "Holdings sync isn't supported for {venue} yet — this key was skipped."
MT5_NON_USD_NOTE = "MT5 account currency is {ccy} — USD conversion isn't supported yet, so this account was skipped."
MT5_UNREACHABLE_NOTE = "MT5 terminal unreachable — sync will retry automatically."

async def fetch_allocator_holdings(
    exchange_name: str, exchange: Any, api_key_id: str | None = None
) -> tuple[list[dict[str, Any]], str | None]: ...
```

```python
# closed_sets.py — mirrors _make_exchange_client's non-ccxt branches (single source)
NON_CCXT_VENUES: frozenset[str] = frozenset({"mt5", "sfox"})
```

Verified anchors:
- `fetch_allocator_holdings` — allocator_positions.py:268 (returns `(rows, warning)`; warning → handler maps to complete_with_warnings + sync_error, job_worker.py:7204-7213 — the honest-skip channel is FREE, no new status)
- Handler: job_worker.py:7112 `api_key_id = job["api_key_id"]`; call site :7119 `rows, warning = await fetch_allocator_holdings(venue, ctx.exchange)`; lazy import block :7101-7103; generic leak arm :7155-7165 (where the PROD AttributeError got stamped — non-ccxt exceptions must NEVER reach it)
- MT5 read bracket reference: job_worker.py:3489-3630 (kill-switch :3462, `cast(Mt5Session, ...)` :3478-3481, `_assert_expected_login`, `_mt5_read` closure, lock+wait_for+to_thread :3547-3551, TimeoutError→bounded restart→transient, Mt5AccountMismatchError→transient)
- Equity extraction economics: job_worker.py:3691-3719 — `equity` (NOT balance; balance drops floating uPnL), fail-loud on missing/non-finite
- `Mt5Client.account_info()` — mt5_client.py:327-332 returns equity/balance/currency/login
- `mt5_enabled_server()` / `MT5_DISABLED_DETAIL` — closed_sets.py:107 / :96-104
- Holdings row dict shape — allocator_positions.py:215-231 (venue/symbol/holding_type "spot"/side "flat"/quantity/value_usd/entry_price None/mark_price/unrealized_pnl_usd None/cost_basis_usd None/raw_payload via _cap_raw_payload)
- DDL: allocator_holdings UNIQUE (allocator_id, venue, symbol, asof) — api_key_id NOT in the key (Pitfall 1); symbol MUST match [A-Za-z0-9_-]+ (HOLDING_REF_RE, commit route.ts:81)
- Facade pin (do NOT touch): tests/test_mt5_client_contract.py:719-763 — positions_get forbidden; account_info is the ONLY equity source
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Venue dispatch chokepoint + unknown-venue honest skip + copy constants + transient exception + handler arm</name>
  <files>analytics-service/services/allocator_positions.py, analytics-service/services/closed_sets.py, analytics-service/services/job_worker.py, analytics-service/tests/test_allocator_positions_non_ccxt.py</files>
  <read_first>
    - analytics-service/services/allocator_positions.py (whole file, 350±: module docstring conventions, _map_exception_to_sync_status, _fetch_spot_rows, fetch_allocator_holdings, persist_allocator_holdings; note :150-152 deribit early-return uses getattr duck-typing — CONTEXT bans that form for NEW code; do not copy it, do not "fix" it either, Rule 3)
    - analytics-service/services/job_worker.py lines 7083-7260 (the handler: lazy import, call site, warning mapping, the generic except arm the new exception must preempt)
    - analytics-service/services/closed_sets.py lines 1-130 and 176-215 (leaf docstring rule, detail-string block + the :81-88 STRING-SAFETY collision invariant, frozenset declaration idiom)
    - analytics-service/tests/test_allocator_positions.py lines 1-110 and 401-499 (existing fetch-level AsyncMock tests whose call shape must stay green; handler-level monkeypatch seam)
  </read_first>
  <behavior>
    - Test 1 (ccxt fall-through byte-unchanged): binance AsyncMock exchange → fetch_balance IS awaited, rows returned as before (existing test file also stays green untouched).
    - Test 2 (unknown non-ccxt venue): a venue in NON_CCXT_VENUES with no fetcher entry → `([], UNSUPPORTED_VENUE_NOTE.format(venue=...))`, and NO method on the client object is called (spec-constrained stub fake, NOT AsyncMock — PATTERNS Correction 3: AsyncMock synthesizes any attribute, making hasattr assertions vacuous).
    - Test 3 (handler stamps human copy on transient failure): handler-level test — fetch raises AllocatorHoldingsSyncTransientError(MT5_UNREACHABLE_NOTE) → api_keys update payload has sync_status="error", sync_error exactly the note, and the DispatchResult is FAILED/transient. Neuter falsifier: with the dedicated arm removed, the test must fail (proves the wiring, not the helper).
    - Test 4 (leak invariant, this plan's slice): for the unknown-venue and transient arms, sync_error/warning strings never contain "Traceback", "AttributeError", "object has no attribute", "Mt5Session", "fetch_balance".
  </behavior>
  <action>
    (1) closed_sets.py: declare `NON_CCXT_VENUES: frozenset[str] = frozenset({"mt5", "sfox"})`
    with an MD-01-style single-source comment: mirrors `_make_exchange_client`'s
    (job_worker.py:996-1023) non-ccxt branches; a venue added there without being added
    here re-opens the AUM-02 crash class — keep in lockstep.

    (2) allocator_positions.py: add the copy constants from the interfaces block
    (UNSUPPORTED_VENUE_NOTE, MT5_NON_USD_NOTE, MT5_UNREACHABLE_NOTE — verbatim strings,
    U+2014 em dash; venue names render in product casing e.g. "MT5", hardcoded — PATTERNS
    Correction 2: there is NO Python EXCHANGE_DISPLAY map, do not invent one). Add
    `class AllocatorHoldingsSyncTransientError(Exception)` whose docstring states
    str(self) is end-user copy. Restructure `fetch_allocator_holdings(exchange_name,
    exchange, api_key_id=None)`: a module-level `_NON_CCXT_HOLDINGS_FETCHERS` dispatch
    dict keyed on the venue STRING (mirroring _make_exchange_client — dispatch on
    `exchange_name`, never isinstance/hasattr, CONTEXT-locked); if the venue has a
    fetcher, await it; elif `exchange_name in NON_CCXT_VENUES` (fetcher missing = future
    non-ccxt venue), return `([], UNSUPPORTED_VENUE_NOTE.format(venue=exchange_name.upper() if short else product casing — use "MT5"/"sFOX" style; for unknown venues title-case the code))`;
    else fall through to the existing ccxt dual-path body BYTE-UNCHANGED. Update the
    function docstring to name the chokepoint ("the sfox-vs-mt5-vs-ccxt holdings
    decision lives in exactly ONE place") and note the `api_key_id` param exists for
    account-scoped symbols (Pitfall 1). For this task register "mt5" with a stub that
    raises NotImplementedError only if Task 2 has not yet filled it in — or implement
    Task 2 first in the same session; final state must have no stub.

    (3) job_worker.py: add `AllocatorHoldingsSyncTransientError` to the lazy import at
    :7101-7103, pass `api_key_id=api_key_id` at the :7119 call site, and add a dedicated
    `except AllocatorHoldingsSyncTransientError as exc:` arm BEFORE the generic
    `except Exception` arm at :7155 — stamp `{"sync_status": "error", "sync_error": str(exc)}`
    (human copy, no truncation needed but keep the [:500] cap for symmetry), write the
    same audit event as the generic arm, and return the FAILED DispatchResult with
    `error_kind="transient"` so the job retries. Comment: this arm exists so a non-ccxt
    venue failure can NEVER reach classify_exception's str(exc) fall-through — the exact
    arm that stamped the PROD AttributeError.

    (4) Note in a comment on the honest-skip return that `stamp_first_sync_success`
    (:7268 area) fires for a skip — a conscious, accepted call (PostHog onboarding event
    only; RESEARCH Pattern 3 wrinkle).

    Create tests/test_allocator_positions_non_ccxt.py with the file-top numbered
    required-test docstring convention, and implement behaviors 1-4. Use plain stub
    classes or `MagicMock(spec=...)` fakes for non-ccxt clients.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_allocator_positions_non_ccxt.py tests/test_allocator_positions.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "NON_CCXT_VENUES" analytics-service/services/closed_sets.py` returns >= 1; the set is exactly {"mt5", "sfox"}
    - `grep -c "hasattr\|isinstance(exchange" analytics-service/services/allocator_positions.py` — the NEW dispatch code contains neither (the pre-existing :151 deribit getattr line is the only duck-typing hit and is unchanged in git diff)
    - `grep -n "except AllocatorHoldingsSyncTransientError" analytics-service/services/job_worker.py` returns 1 line, positioned before the generic `except Exception` arm of the fetch try-block
    - Existing tests/test_allocator_positions.py passes UNMODIFIED except (if needed) the fetch call-shape gaining the optional api_key_id kwarg — assert via `git diff --stat`
    - Handler-level Test 3 fails when the dedicated except arm is commented out (run once, revert — wiring falsifier observed)
  </acceptance_criteria>
  <done>Dispatch chokepoint live; unknown non-ccxt venue skips honestly; transient exception path stamps human copy and retries; ccxt path byte-unchanged and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MT5 account-equity branch — kill switch, shared lock, login bracket, bounded read, non-USD skip</name>
  <files>analytics-service/services/allocator_positions.py, analytics-service/tests/test_allocator_positions_non_ccxt.py</files>
  <read_first>
    - analytics-service/services/job_worker.py lines 3440-3720 (the derive-arm MT5 block — the reference implementation: kill-switch posture, cast, _assert_expected_login, sync closure, lock scope, wait_for/to_thread, TimeoutError/Mt5AccountMismatchError/Mt5ClientError arms, equity extraction + finite check, scrub_freeform_string usage)
    - analytics-service/services/mt5_concurrency.py (the shared symbols created by 151-01)
    - analytics-service/services/mt5_client.py lines 300-460 (account_info contract, terminal_key, Mt5ClientError/Mt5AccountMismatchError definitions — find their true home for imports)
    - analytics-service/services/allocator_positions.py lines 200-232 (the row-dict shape to replicate key-for-key)
  </read_first>
  <behavior>
    - Test 5 (kill switch): `mt5_enabled_server()` false → returns `([], MT5_DISABLED_DETAIL)`; the fake session's login/account_info are NEVER called (spec-constrained fake).
    - Test 6 (USD equity row): account_info = {equity: 123456.78, balance: 120000.00, currency: "USD", login: <expected>} → exactly ONE row with value_usd == 123456.78 and quantity == 123456.78 — EQUITY, not balance (economic oracle: assert the literal 123456.78; a fixture where equity != balance must not produce the balance), holding_type "spot", side "flat", mark_price 1.0, entry_price/unrealized_pnl_usd/cost_basis_usd None, venue "mt5".
    - Test 7 (account-scoped symbol): with api_key_id "46293712-59e6-46c0-8204-5dd32afe2503" the row's symbol == "ACCOUNT-46293712" and matches `^[A-Za-z0-9_-]+$`; two different api_key_ids yield two DISTINCT symbols (the Pitfall-1 non-collapse precondition).
    - Test 8 (non-USD honest skip): currency "EUR" → `([], "MT5 account currency is EUR — USD conversion isn't supported yet, so this account was skipped.")`; missing/blank currency → also a skip note, NEVER treated as USD (assumption A3 fail-loud rule — a silent USD default is a fabricated 1.0 FX rate).
    - Test 9 (timeout → transient): the read closure hanging past the timeout (patch the timeout constant small) → bounded restart invoked AND AllocatorHoldingsSyncTransientError raised with str == MT5_UNREACHABLE_NOTE.
    - Test 10 (lock identity across job kinds): the lock object the MT5 holdings branch acquires for terminal_key "h:1" `is` `services.mt5_concurrency._mt5_terminal_lock_for("h:1")` `is` `services.job_worker._mt5_terminal_lock_for("h:1")` — the binding-trap falsifier (a duplicated registry serializes nothing).
    - Test 11 (login bracket): account_info returning a login != the session's expected login → AllocatorHoldingsSyncTransientError (after bounded restart), row list empty — never a wrong-account equity row.
  </behavior>
  <action>
    Implement `_fetch_mt5_account_rows(exchange_name, exchange, api_key_id)` in
    allocator_positions.py and register it in `_NON_CCXT_HOLDINGS_FETCHERS["mt5"]`.
    Structure, mirroring the derive arm (job_worker.py:3489-3630) exactly EXCEPT where
    noted: (a) kill-switch FIRST — `if not mt5_enabled_server(): return ([], MT5_DISABLED_DETAIL)`
    (honest-skip channel, NOT a DispatchResult — the holdings path's parity with the
    derive arm's :3462 posture); (b) `cast(Mt5Session, exchange)` for mypy --strict
    (never type: ignore); (c) a sync closure doing login → account_info →
    _assert_expected_login on that SAME info dict. DECISION (documented in a code
    comment): PRE-bracket only, no post-read re-verify — unlike the derive arm's
    pre+post bracket, the holdings read has exactly ONE economic read and the login
    assertion is made on the very payload that supplies the equity, so a mid-read
    account switch cannot produce a wrong-account figure (RESEARCH Pattern 2 planner
    call, taken and documented); (d) `async with _mt5_terminal_lock_for(session.client.terminal_key):`
    wrapping `await asyncio.wait_for(asyncio.to_thread(closure), timeout=_MT5_DERIVE_READ_TIMEOUT_S)`
    — import both from `services.mt5_concurrency` (the ONE registry; comment that the
    constant is deliberately reused, no new env knob — the holdings read is strictly
    shorter than the derive read it bounds); (e) except arms INSIDE the branch:
    asyncio.TimeoutError → `await _mt5_bounded_restart(client)` → raise
    AllocatorHoldingsSyncTransientError(MT5_UNREACHABLE_NOTE); Mt5AccountMismatchError →
    bounded restart → raise AllocatorHoldingsSyncTransientError(MT5_UNREACHABLE_NOTE);
    Mt5ClientError → raise AllocatorHoldingsSyncTransientError(MT5_UNREACHABLE_NOTE).
    Fixed copy strings only — never embed str(exc) (the whole point of AUM-02); apply
    `scrub_freeform_string` only if any dynamic text is ever included (currently none).
    (f) currency gate: `info.get("currency")` — if missing/blank or != "USD", return
    `([], MT5_NON_USD_NOTE.format(ccy=ccy or "unknown"))`; comment the A3 rule (never
    default to USD — that IS an invented FX rate). (g) equity extraction with the
    :3691-3719 fail-loud discipline: float() both equity and balance, KeyError/TypeError/
    ValueError or non-finite → raise AllocatorHoldingsSyncTransientError(MT5_UNREACHABLE_NOTE)
    (refuse a poisoned anchor). (h) ONE row: symbol = f"ACCOUNT-{api_key_id[:8]}"
    (RESEARCH Open Q3 decision: opaque, stable, non-secret — never the broker login,
    V8 concern; comment that changing this token later orphans rows under the old
    symbol); if api_key_id is None raise AllocatorHoldingsSyncTransientError (config
    fault, but human copy); quantity == value_usd == equity, mark_price 1.0,
    holding_type "spot", side "flat", entry/uPnL/cost None (DDL comment: uPnL is
    derivative-only — None is the conforming choice), raw_payload =
    _cap_raw_payload({"currency": ccy, "equity": equity, "balance": balance}).
    Implement behaviors 5-11 in the test file using a plain stub Mt5Session/Mt5Client
    fake (spec-constrained: exposes ONLY login/account_info/restart/terminal_key/close —
    the facade pin surface; the fake must NOT respond to positions_get or fetch_balance).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_allocator_positions_non_ccxt.py -q && python -m pytest tests/test_allocator_positions.py tests/test_mt5_client_contract.py -q && mypy .</automated>
  </verify>
  <acceptance_criteria>
    - Test 6 asserts the literal 123456.78 (equity) and a run with the row built from balance (mutate once, revert) FAILS — the economic oracle bites
    - Test 10's `is` chain holds; defining a local lock dict in allocator_positions (mutate once, revert) makes it fail
    - `grep -c "positions_get" analytics-service/services/allocator_positions.py` returns 0 (facade pin untouched) and tests/test_mt5_client_contract.py passes unmodified
    - `grep -n "ACCOUNT-" analytics-service/services/allocator_positions.py` shows the symbol format; `grep -c "login}" analytics-service/services/allocator_positions.py` shows the broker login is NOT used in the symbol
    - No new env var introduced: `git diff analytics-service/ | grep -c "MT5_HOLDINGS"` returns 0
    - `cd analytics-service && mypy .` exits 0; zero new `# type: ignore` in the diff
  </acceptance_criteria>
  <done>MT5 branch complete behind the shared lock with honest skips and transient human-copy failures; all MT5 tests green; facade pin and derive arm untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → api_keys.sync_error (user-visible DB column) | worker-written strings render verbatim in the browser (AllocatorSyncStatus pass-through) |
| worker → shared MT5 terminal | second job kind on one serialized terminal; wrong-account reads are data corruption |
| holdings row → commit fingerprint / Holdings UI | the symbol token becomes user-visible and rides scope-refs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-05 | Information Disclosure | sync_error column | mitigate | fixed human-copy constants at the venue branch; dedicated except arm preempts classify_exception's str(exc); Test 4 banned-substring assertions (ASVS V7 — this IS the phase's core fix) |
| T-151-06 | Information Disclosure | MT5 symbol token | mitigate | `ACCOUNT-{api_key_id[:8]}` — opaque, never the broker login (ASVS V8) |
| T-151-07 | Tampering (data integrity) | multi-account upsert | mitigate | account-scoped symbol keeps 3 MT5 accounts as 3 rows under UNIQUE(allocator_id,venue,symbol,asof); Test 7 distinct-symbol assertion; full non-collapse oracle in 151-04 |
| T-151-08 | Denial of Service | worker loop / shared terminal | mitigate | wait_for + to_thread + bounded restart inside the SAME shared lock (WEDGE-01/MT5CONC class); Test 9/10 |
| T-151-09 | Spoofing (wrong account) | login bracket | mitigate | strict login equality on the SAME payload that supplies equity; missing login fails loud (Test 11) |
| T-151-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_allocator_positions_non_ccxt.py tests/test_allocator_positions.py tests/test_mt5_client_contract.py tests/test_mt5_derive_branch.py -q && mypy .` green.
- Post-merge PROD UAT (NOT in-phase; Phase-149 W-3 precedent): after deploy, trigger "Sync now" on an MT5 key — `sync_status` leaves 'error', `sync_error` contains no "AttributeError". Recorded in 151-VALIDATION.md Manual-Only table.
</verification>

<success_criteria>
- Venue dispatch on the venue string at the one chokepoint; ccxt byte-unchanged.
- MT5 USD account → one equity row (equity, not balance) under an account-scoped symbol; non-USD/unknown/disabled → honest human-copy skips via the existing warning channel.
- Genuine MT5 failures stamp human copy with error_kind=transient via the dedicated handler arm.
- Same terminal Lock object as the derive job — proven by `is` identity test.
</success_criteria>

<output>
Create `.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-03-SUMMARY.md` when done.
</output>
