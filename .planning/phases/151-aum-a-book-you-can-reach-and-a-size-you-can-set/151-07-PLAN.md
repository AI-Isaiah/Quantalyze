---
phase: 151-aum-a-book-you-can-reach-and-a-size-you-can-set
plan: 07
type: execute
wave: 4
depends_on: ["151-06"]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioCommitDrawer.tsx
  - src/app/api/allocator/scenario/commit/route.ts
  - src/app/api/allocator/scenario/commit/route.test.ts
autonomous: true
requirements: [AUM-01]

must_haves:
  truths:
    - "'Allocate $500k to this strategy' is expressible: typing 500000 with AUM 2000000 produces weight 0.25 through the ONE existing weight-write path"
    - "A sole-unit dollar edit refuses (never renormalizes); the >1-AUM clamp banner fires from a dollar edit"
    - "A blank-mode manual-AUM commit audits with the client_manual_aum sentinel and a computed size — never silently no_holdings_snapshot/size 0"
    - "manual_aum_usd is an additive labelled sidecar; size_at_decision_usd server recompute (NEW-C18-04) is never overwritten by a client number under a server-sounding sentinel"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "per-strategy dollar input routed through handleWeightChange"
      contains: "allocation (USD)"
    - path: "src/app/api/allocator/scenario/commit/route.ts"
      provides: "manual_aum_usd zod field + client_manual_aum _size_source sentinel"
      contains: "client_manual_aum"
  key_links:
    - from: "dollar input onCommit"
      to: "handleWeightChange"
      via: "amount / scenarioAum — the ONLY weight-write path (v1.11 landmine)"
      pattern: "handleWeightChange\\("
    - from: "ScenarioCommitDrawer body"
      to: "manual_aum_usd"
      via: "conditional spread (request_hash preserved for callers without it)"
      pattern: "manual_aum_usd"
---

<objective>
Complete AUM-01: the per-strategy dollar input ("allocate $500k to this strategy")
back-computing weight through the existing `handleWeightChange` path, and manual-AUM
commit persistence — an additive `manual_aum_usd` body field landing in the audit
trail as an explicitly client-asserted `client_manual_aum` sentinel.

Purpose: the founder's literal sentence must be expressible. The v1.11 landmines are
binding: dollars route through the ONE weight-write path (clamp banner, mixed-book
basis, sole-unit refusal all inherited, never forked); NEW-C18-04 means the audit
trail's server-recomputed size is never overwritten by a client number — manual AUM
rides as a labelled sidecar, fixing the "blank-mode commit audits as $0 /
no_holdings_snapshot" hole (RESEARCH Pitfall 7).

Output: dollar input + economic-oracle tests; drawer threading; route schema + sentinel
(three coordinated edits) + route tests; full TS phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-RESEARCH.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-PATTERNS.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-UI-SPEC.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-06-SUMMARY.md

<interfaces>
From 151-06: `scenarioAum = sanitizedManualAum ?? liveHoldingsSum`; `draft.manualAumUsd`;
`sanitizedManualAum` (isValidDollar-gated, 0 treated as unset).

Verified anchors:
- `handleWeightChange(scopeRef, weight)` — ScenarioComposer.tsx:1160-1275: non-finite → warn + keep previous; >1 → clamp + banner "Weight clamped to 1 — the maximum allocation is 100% of portfolio AUM."; mixed-book basis choice :1226-1240; sole-unit refusal :1250-1253 "A single constituent is always 100%." — ALL inherited by routing dollars through it; never fork a second write path
- Weight-input JSX recipe :5625-5666 (id/sr-only label/type number/recipe classes); blur/Enter commit recipe :5456-5480 with `raw.trim() !== ""`; em-dash derived cell recipe `notionalText` :5400-5411 (+ title/data-testid :5669-5675) — the notional span is a DIFFERENT number and stays untouched
- Drawer: ScenarioCommitDrawer.tsx conditional-spread body builder :534-539 (`init_holdings_fingerprint` precedent — the comment names the idempotency request_hash constraint: adding a key unconditionally changes the hash for every caller); per-row `size_at_decision_usd` from `percent × scenarioAum / 100` already at :461-463; `scenarioAum` prop :114/:179
- Route: `CommitBodySchema` :150-163 (z.object non-strict → undeclared keys silently STRIPPED — must be declared to survive, assumption A5); `_size_source` union :830-834; sentinel branch chain :844-871 (server_aum → lookup_failed → no_holdings_snapshot); enumeration comment :886-901 says "six states" — MUST be updated with the seventh or it becomes a lie
- Route test: route.test.ts EXISTS (1,613 lines — PATTERNS Correction 1); zod describe :321-353; required mock stack :34-198 (vi.mock server-only, supabase/admin, withAllocatorAuth, ratelimit, audit, sentry-capture)
- `MAGNITUDE_CAPS.MAX_DOLLAR_VALUE_USD` — src/lib/closed-sets.ts (the AUM bound; distinct from MAX_TICKET_SIZE_USD)

UI-SPEC dollar-input contract (binding): same recipe as weight input, ~w-24; sr-only label "{name} allocation (USD)"; displays weight × AUM (whole dollars) at rest; AUM <= 0 → read-only "—" with title="Set portfolio AUM to size in dollars" PLUS an sr-only span carrying the same sentence; display rounds, stored weight changes ONLY on user edit (no write-back of the rounded value on render).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-strategy dollar input through handleWeightChange + economic oracles</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx lines 1160-1280 (handleWeightChange — every guard the dollar path must inherit), 5390-5680 (constituent-row JSX: weight input, notional span, blur/Enter recipe — the insertion site)
    - .planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-VALIDATION.md (binding oracle rules — AUM-01 sizing: economic identity, wiring falsifier, sole-unit refusal)
  </read_first>
  <behavior>
    - Test 1 (invertibility, oracle 2 — the founder's sentence): AUM 2_000_000, type 500000 into a strategy's dollar field (blur) → that strategy's weight input reads 0.250 AND the drawer-bound per-row size is exactly 500_000. Hand-computed constants; never recompute weight × AUM inside the test.
    - Test 2 (conservation, oracle 1): AUM 1_000_000, weights {0.25, 0.75} → dollar cells read $250,000 and $750,000 (hand-computed literals; sum equals AUM within one cent).
    - Test 3 (wiring falsifier): with handleWeightChange neutered (spy replaced by no-op), the dollar edit changes NOTHING — then restored, it does; equivalently assert the spy is CALLED with (ref, 0.25). The test must fail if the dollar input writes weight through any other path.
    - Test 4 (clamp inheritance): typing an amount > AUM fires the existing clamp banner "Weight clamped to 1 — the maximum allocation is 100% of portfolio AUM." and the weight lands at 1.
    - Test 5 (sole-unit refusal inheritance): mixed-book fixture with one selected engine unit → dollar edit surfaces "A single constituent is always 100%." and the weight is unchanged (v1.11 landmine — refuse, never renormalize).
    - Test 6 (AUM unset): scenarioAum 0 → the cell renders "—", is not editable, has title "Set portfolio AUM to size in dollars" and an sr-only span with the same sentence; no division occurs (no NaN warnings).
    - Test 7 (leverage invariance re-check, oracle 3): a dollar edit changes weights but a pure AUM change still leaves scenarioMetrics identical (guards the composed state after this task's wiring).
  </behavior>
  <action>
    Add the dollar input to each added-strategy row's input cluster alongside the weight
    input (UI-SPEC contract in the interfaces block): id `alloc-usd-${ref}`, sr-only
    label "{name} allocation (USD)", type number, min 0, ~w-24, existing recipe classes,
    data-testid "scenario-constituent-dollar". At rest display
    `Math.round(weight × scenarioAum)` (display rounds; NEVER write the rounded value
    back — the stored weight changes only on user edit, per the UI-SPEC rounding rule;
    implement as an uncontrolled input re-keyed/defaultValue-refreshed from the derived
    dollar, or controlled-display-with-edit-buffer — follow the blur/Enter commit recipe
    at :5456-5480 with the `raw.trim() !== ""` blank-guard). On commit (blur/Enter):
    validate with isValidDollar (refuse invalid, keep previous — mirror the non-finite
    posture), then call `handleWeightChange(ref, amount / scenarioAum)` — the ONLY
    write path; add a comment naming the v1.11 landmine (clamp banner, mixed-book
    basis, sole-unit refusal are all INHERITED through this call; forking a second
    weight-write path is the named defect class). When `scenarioAum <= 0` render the
    em-dash read-only state per the notionalText recipe (:5400-5411) with title + sr-only
    span — never a disabled input, never $0. The adjacent notional span is a DIFFERENT
    number — untouched. Use formatUsd for any read-only echo. Write behaviors 1-7.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism -t "dollar" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "scenario-constituent-dollar" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` returns 1 input site
    - The dollar commit handler contains `handleWeightChange(` and the file gains NO new `setWeightOverride`/`applyWeightOverrides` call sites outside handleWeightChange (grep count unchanged vs git show HEAD)
    - Test 3's neuter run observed RED once (recorded in SUMMARY), then green restored
    - Tests 1-7 pass; existing weight-input and notional tests green
  </acceptance_criteria>
  <done>Dollars are a first-class input that provably rides the one weight path with all three guards inherited; economics pinned by hand-computed literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: manual_aum_usd persistence — drawer sidecar, route schema, client_manual_aum audit sentinel</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioCommitDrawer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/api/allocator/scenario/commit/route.ts, src/app/api/allocator/scenario/commit/route.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioCommitDrawer.tsx lines 90-200 (props incl. scenarioAum) and 520-545 (the conditional-spread body builder + its request_hash comment)
    - src/app/api/allocator/scenario/commit/route.ts lines 140-170 (CommitBodySchema), 690-760 (audit recompute preamble + serverAum lookup), 820-905 (the sentinel union, branch chain, and the six-states enumeration comment)
    - src/app/api/allocator/scenario/commit/route.test.ts lines 1-200 (mock stack boilerplate) and 321-353 (zod describe to extend)
  </read_first>
  <behavior>
    - Test 8 (schema declared, A5): POST with manual_aum_usd 2_000_000 → 200 path parses and the value reaches the audit builder (not stripped). POST with manual_aum_usd -1, Infinity-as-JSON-null, or 1e12 → 400 (bounds enforced).
    - Test 9 (the sentinel, blank-mode commit): holdings lookup returns zero rows + manual_aum_usd 2_000_000 + a voluntary_add diff at percent_allocated 25 → per-row audit metadata has size_at_decision_usd 500_000, _size_source "client_manual_aum", and size_at_decision_usd_client preserved. Hand-computed literal 500_000.
    - Test 10 (server truth precedence): serverAumUsd > 0 (holdings exist) AND manual_aum_usd present → _size_source stays "server_aum" and the server-recomputed size wins — the client assertion NEVER outranks the server figure (NEW-C18-04).
    - Test 11 (absent field = today's behavior): no manual_aum_usd, empty holdings → _size_source "no_holdings_snapshot", size 0 — byte-identical to the pre-phase audit path; and a body WITHOUT the field produces the same request-shape hash as before (the conditional spread — assert the drawer omits the key when manualAumUsd is undefined).
  </behavior>
  <action>
    Composer→drawer: thread a new optional prop `manualAumUsd?: number` from
    ScenarioComposer (pass `sanitizedManualAum` — only a genuinely manual value, NEVER
    liveHoldingsSum; book-mode-unedited commits must omit the field so the audit stays
    on the server path). Drawer body builder: extend the :534-539 conditional spread —
    include `manual_aum_usd: manualAumUsd` only when `manualAumUsd != null`, with a
    comment mirroring the fingerprint precedent (request_hash unchanged for callers
    without it; the value is a CLIENT-ASSERTED sidecar, audit-labelled server-side).

    Route: (1) declare `manual_aum_usd: z.number().positive().lt(MAGNITUDE_CAPS.MAX_DOLLAR_VALUE_USD).finite().optional()`
    on CommitBodySchema (import MAGNITUDE_CAPS from @/lib/closed-sets if not present)
    with a comment: additive optional (absent → pre-151 behavior); declared or z.object
    silently strips it (A5). (2) Extend the sentinel machinery with the THREE
    coordinated edits (PATTERNS Analog B): add "client_manual_aum" to the union type
    at :830-834; add the branch in the if/else chain — for voluntary_modify /
    voluntary_add / bridge_recommended arms, precedence: `serverAumUsd > 0` →
    server_aum (unchanged, first); else if `manualAumUsd != null` → serverSizeUsd =
    percent_allocated × manualAumUsd / 100 with sizeSource "client_manual_aum"; else
    the existing lookup_failed / no_holdings_snapshot arms; (3) update the enumeration
    comment at :886-901 — "six states" becomes "seven states" with a line:
    client_manual_aum — size computed from the CLIENT-ASSERTED manual AUM (blank-mode
    scenario; no holdings snapshot); never a server-verified figure. Never touch
    size_at_decision_usd_client handling. Extend route.test.ts (existing mock stack)
    with behaviors 8-11.

    Then run the plan-final gate (the full-suite runs live here, not in the per-task
    sampling command): full composer + drawer + route vitest, typecheck, lint, the full
    `npm run test` suite, and `npm run test:coverage` (blocking CI thresholds
    82/80/74/72 — this is the phase's last TS plan, so the coverage ratchet must be
    proven here, not discovered at PR CI).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/allocator/scenario/commit/route.test.ts "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "client_manual_aum" src/app/api/allocator/scenario/commit/route.ts` >= 3 (union + branch + enumeration comment)
    - `grep -n "seven states" src/app/api/allocator/scenario/commit/route.ts` returns 1 and `grep -c "six states"` returns 0 (the count comment is not a lie)
    - `grep -n "manual_aum_usd" "src/app/(dashboard)/allocations/components/ScenarioCommitDrawer.tsx"` shows the conditional-spread form (inside a `!= null &&` spread), not an unconditional key
    - Test 10 proves server precedence: mutating the branch order to prefer the client value (once, reverted) fails it
    - Tests 8-11 pass; full `npm run test` green; `npm run test:coverage` passes the blocking thresholds (82/80/74/72); typecheck + lint green
  </acceptance_criteria>
  <done>Manual AUM persists end-to-end as a labelled client-asserted sidecar; blank-mode commits audit with a real computed size under the honest sentinel; server truth always wins when it exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/allocator/scenario/commit | client-asserted manual_aum_usd crosses into the audit trail |
| dollar input → weight math | attacker-controlled numerator over a possibly-zero denominator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-18 | Tampering / Repudiation | audit size figures | mitigate | NEW-C18-04 preserved: server recompute wins when holdings exist (Test 10); client value only ever lands under the explicit client_manual_aum sentinel + size_at_decision_usd_client untouched |
| T-151-19 | Tampering (input) | manual_aum_usd body field | mitigate | zod positive/.lt(MAX_DOLLAR_VALUE_USD)/.finite bounds server-side; isValidDollar client-side (ASVS V5) |
| T-151-20 | Denial of Service (local) | division by zero AUM | mitigate | scenarioAum <= 0 → read-only em-dash state, no division executes (Test 6) |
| T-151-21 | Tampering (idempotency drift) | commit request_hash | mitigate | conditional spread — callers without the field produce byte-identical bodies (Test 11) |
| T-151-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
- Plan-final gate: full `npm run test` green AND `npm run test:coverage` passes the blocking thresholds (phase-final TS gate); typecheck + lint green.
- Phase-gate follow-ups for the orchestrator: `cd analytics-service && python -m pytest -q && mypy .`, `npx playwright test e2e/composer-axe.spec.ts` (two new inputs + note must stay axe-clean).
</verification>

<success_criteria>
- "Allocate $500k to this strategy" works end-to-end: dollar edit → weight 0.25 via handleWeightChange → commit body carries manual_aum_usd → audit shows client_manual_aum with size 500,000.
- All three weight-path guards inherited by dollar edits; sole-unit refusal intact.
- Server-recomputed audit truth never overwritten; absent-field behavior byte-identical to pre-phase.
</success_criteria>

<output>
Create `.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-07-SUMMARY.md` when done.
</output>
