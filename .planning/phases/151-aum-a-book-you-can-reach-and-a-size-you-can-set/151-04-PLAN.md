---
phase: 151-aum-a-book-you-can-reach-and-a-size-you-can-set
plan: 04
type: execute
wave: 3
depends_on: ["151-03"]
files_modified:
  - analytics-service/services/allocator_positions.py
  - analytics-service/tests/test_allocator_positions_non_ccxt.py
autonomous: true
requirements: [AUM-05, AUM-02]

must_haves:
  truths:
    - "A live sFOX key will not crash the holdings sync: get_balances (never fetch_balance), USD/stablecoin rows at mark 1.0, honest warning for non-priceable assets"
    - "The CLASS is proven closed: the SAME parametrized test body passes for mt5 AND sfox AND an unknown non-ccxt venue, BEFORE the sFOX go-live flip"
    - "No sync_error/warning string across any non-ccxt arm ever contains a Python identifier"
    - "Two MT5 accounts under one allocator survive the upsert as two rows"
  artifacts:
    - path: "analytics-service/services/allocator_positions.py"
      provides: "_fetch_sfox_balance_rows registered in _NON_CCXT_HOLDINGS_FETCHERS"
      contains: "sfox"
    - path: "analytics-service/tests/test_allocator_positions_non_ccxt.py"
      provides: "parametrized class-closure proof + banned-substring invariant + non-collapse oracle"
      contains: "parametrize"
  key_links:
    - from: "_fetch_sfox_balance_rows"
      to: "SfoxClient.get_balances"
      via: "the CONTEXT-locked method (never fetch_balance)"
      pattern: "get_balances"
    - from: "test parametrization"
      to: "mt5 AND sfox branches"
      via: "one test body, two venues (the class proof)"
      pattern: "@pytest.mark.parametrize"
---

<objective>
Close the non-ccxt holdings-sync CLASS: implement the sFOX branch on the 151-03
dispatch scaffolding and land the parametrized proof that the SAME test shape passes
for MT5 and sFOX and an unknown venue — before sFOX's go-live flip, not discovered by
it (AUM-05). Add the remaining economic oracles (multi-account non-collapse).

Purpose: `SfoxClient` exposes `get_balances()`, not ccxt's `fetch_balance()` —
byte-identical latent crash to the MT5 PROD defect, invisible only because the flag is
off with zero keys. The ROADMAP binding trap: fix the venue CLASS, not the MT5
instance; the sFOX branch must be proven by test (no live API exists to observe).

Output: sFOX branch + parametrized class proof + banned-substring invariant + upsert
non-collapse oracle; full Python gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-RESEARCH.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-PATTERNS.md
@.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-03-SUMMARY.md

<interfaces>
From 151-03 (already landed): `_NON_CCXT_HOLDINGS_FETCHERS`, `AllocatorHoldingsSyncTransientError`
(str(self) = end-user copy), `UNSUPPORTED_VENUE_NOTE`, `NON_CCXT_VENUES` (closed_sets.py),
`fetch_allocator_holdings(exchange_name, exchange, api_key_id=None)`, the dedicated
handler except arm.

sFOX contract (verified):
- `SfoxClient.get_balances()` — sfox_client.py:272-277, returns `list[{"currency": str, "balance": str, ...}]` — STRING quantities, NO USD valuation field (RESEARCH Pitfall 9 / assumption A4; shape pinned by tests/test_sfox_client.py:73-79 — re-verify the row keys there before writing the branch)
- GET-only facade, four read methods, no ticker endpoint — a non-stable asset CANNOT be priced without a cross-venue price source (deferred; no-invented-data)
- `sfox_enabled_server()` — closed_sets.py:66; `STABLECOINS` — closed_sets.py:176-178, ALREADY imported by allocator_positions.py:43
- Stablecoin mark-price precedent: allocator_positions.py:209-214 (`asset_upper in STABLECOINS` → mark 1.0)

New copy (this plan; pattern "{what happened} — {what/next}", U+2014):
- SFOX_FETCH_FAILED_NOTE = "Couldn't fetch balances from sFOX — sync will retry automatically."
- SFOX_UNPRICED_ASSETS_NOTE = "sFOX balances in {assets} can't be valued in USD yet — those balances were skipped."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sFOX balance branch — USD/stablecoins at mark 1.0, honest warning for the rest</name>
  <files>analytics-service/services/allocator_positions.py, analytics-service/tests/test_allocator_positions_non_ccxt.py</files>
  <read_first>
    - analytics-service/services/allocator_positions.py (current state after 151-03 — dispatch table, copy constants, row-dict shape at the ccxt spot builder)
    - analytics-service/services/sfox_client.py lines 240-300 (get_balances signature: sync or async? error types raised — find SfoxApiError's real name/home; the branch's except arm must catch the actual class)
    - analytics-service/tests/test_sfox_client.py lines 60-90 (the pinned balance-row shape — source of truth for fixture keys)
    - analytics-service/services/closed_sets.py lines 60-70 and 176-178 (sfox_enabled_server, STABLECOINS)
  </read_first>
  <behavior>
    - Test 12 (USD + stablecoin rows): balances [{currency:"usd", balance:"1000.5"}, {currency:"USDC", balance:"250"}, {currency:"btc", balance:"0.5"}] → exactly TWO rows (USD 1000.50, USDC 250.00) each with mark_price 1.0, value_usd == quantity, venue "sfox", symbol == the uppercased currency code (matches [A-Za-z0-9_-]+), holding_type "spot", side "flat" — AND warning == "sFOX balances in BTC can't be valued in USD yet — those balances were skipped."
    - Test 13 (all priceable → no warning): only USD/stablecoins → (rows, None).
    - Test 14 (kill switch parity): sfox_enabled_server() false → ([], <the existing sFOX disabled detail from closed_sets>) and get_balances never called.
    - Test 15 (fetch failure → transient human copy): get_balances raising the client's real API error → AllocatorHoldingsSyncTransientError with str == SFOX_FETCH_FAILED_NOTE.
    - Test 16 (zero balances): empty list → ([], None) — an empty book is not an error.
  </behavior>
  <action>
    Implement `_fetch_sfox_balance_rows(exchange_name, exchange, api_key_id)` and
    register it under "sfox" in `_NON_CCXT_HOLDINGS_FETCHERS`. Structure: (a) kill-switch
    first via `sfox_enabled_server()` returning `([], <existing sFOX disabled detail
    string from closed_sets.py — reuse, do not mint>)`; (b) `cast(SfoxClient, exchange)`
    for mypy --strict; (c) call `get_balances()` — determine from sfox_client.py whether
    it is sync (wrap in `asyncio.to_thread` + `asyncio.wait_for` with a bounded timeout;
    reuse an existing sfox/read timeout constant if one exists, else a module const with
    a comment) or async (await directly, still bounded with wait_for); catch the client's
    real API/transport error classes and raise
    `AllocatorHoldingsSyncTransientError(SFOX_FETCH_FAILED_NOTE)` — fixed copy, never
    str(exc); (d) row construction: uppercase the currency; USD or member of STABLECOINS
    → mark_price 1.0, quantity = float(balance), value_usd = quantity, same row-dict
    shape as the ccxt spot builder (entry/uPnL/cost None, raw_payload via
    _cap_raw_payload with currency+balance); skip zero-quantity balances; non-stable
    assets → collect their uppercased codes and, if any, set warning =
    SFOX_UNPRICED_ASSETS_NOTE.format(assets=", ".join(sorted(codes))). Add both new copy
    constants near the MT5 ones. Comment the RESEARCH Open-Q2 decision: CONTEXT locks
    get_balances; the get_balance_history NAV-anchor alternative is logged as a deferred
    improvement for the sFOX go-live phase (add a one-line entry to TODOS.md in this
    task: "sFOX holdings: consider get_balance_history usd_value NAV anchor instead of
    per-asset get_balances — deferred from Phase 151, RESEARCH Open Q2"). Implement
    behaviors 12-16 with a spec-constrained SfoxClient stub (exposes only the four read
    methods; must NOT respond to fetch_balance).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_allocator_positions_non_ccxt.py -q -k sfox</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"sfox": _fetch_sfox_balance_rows' analytics-service/services/allocator_positions.py` returns 1 line
    - `grep -c "fetch_balance" analytics-service/services/allocator_positions.py` count is UNCHANGED from before this task (the sFOX branch never references it) — compare against git show HEAD
    - Test 12 asserts the exact warning string including "BTC" and the em dash; tests 12-16 pass
    - TODOS.md gained the deferred get_balance_history line (grep "get_balance_history" TODOS.md)
    - `cd analytics-service && mypy .` exits 0
  </acceptance_criteria>
  <done>sFOX branch complete: priceable rows contribute, non-priceable skip honestly, failures are transient human copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The class-closure proof — parametrized venue table, banned-substring invariant, non-collapse oracle, full gate</name>
  <files>analytics-service/tests/test_allocator_positions_non_ccxt.py</files>
  <read_first>
    - analytics-service/tests/test_allocator_positions_non_ccxt.py (current state — the fakes and tests from 151-03 Task 1/2 and this plan's Task 1)
    - analytics-service/tests/test_mt5_client_contract.py lines 715-765 (the parametrize idiom + the facade pin that must stay green)
    - analytics-service/services/allocator_positions.py `persist_allocator_holdings` (:307 area — upsert conflict target, for the non-collapse oracle's mock shape)
  </read_first>
  <behavior>
    - Test 17 (THE class proof, CONTEXT-locked shape): ONE test body parametrized over [("mt5", mt5_fake_factory), ("sfox", sfox_fake_factory), ("kraken-futures-hypothetical", unknown_stub_factory — a venue injected into NON_CCXT_VENUES for the test)] asserting for each: (a) fetch_allocator_holdings returns without raising OR raises only AllocatorHoldingsSyncTransientError; (b) no ccxt method name (fetch_balance, fetch_positions) is ever invoked on the fake (spec-constrained fakes make unexpected attribute access raise); (c) any returned warning and any raised exception's str() contain NONE of: "Traceback", "AttributeError", "object has no attribute", "Mt5Session", "SfoxClient", "fetch_balance" — the greppable UI-SPEC invariant.
    - Test 18 (multi-account non-collapse, economic oracle 5): two MT5 fakes under one allocator with api_key_ids differing in their first 8 chars, equities 111111.11 and 222222.22 → the two row sets carry DISTINCT symbols; feeding both through persist_allocator_holdings against a mock supabase, the upsert receives TWO rows whose value_usd multiset is exactly {111111.11, 222222.22} (assert the literals — never recompute from the impl).
    - Test 19 (ccxt regression, class boundary): binance still awaits fetch_balance through the dispatch (the additive-dispatch guarantee, re-asserted here as the class test's control arm).
  </behavior>
  <action>
    Add the three tests. For Test 17's unknown-venue arm, monkeypatch NON_CCXT_VENUES
    membership (or parametrize the venue string against a copy of the set) rather than
    editing the production set. For Test 18, mock the supabase table().upsert() chain
    and capture the rows argument; assert on the captured payload (the wiring, not a
    helper). File docstring: update the numbered required-test list tagging AUM-02/AUM-05
    and quoting the acceptance sentence "the same test shape passes for sFOX BEFORE its
    go-live flip". Then run the full phase Python gate: full pytest from
    analytics-service/ and mypy strict.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_allocator_positions_non_ccxt.py -q && python -m pytest -q && mypy .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@pytest.mark.parametrize" analytics-service/tests/test_allocator_positions_non_ccxt.py` >= 1 with a venue table containing both "mt5" and "sfox" in ONE parametrization (the class proof, not two sibling tests)
    - Test 17 fails when the sfox dispatch entry is removed (mutate once, revert — proves the parametrization actually exercises the branch)
    - Test 18 asserts literals 111111.11 and 222222.22 and two distinct symbols; collapsing the symbol to a constant (mutate once, revert) fails it
    - `cd analytics-service && python -m pytest -q` exits 0 (full suite) and `mypy .` exits 0
    - Coverage: the new non-ccxt branches are exercised (pytest --cov=services.allocator_positions on the file shows the mt5/sfox/unknown arms covered; suite-level --cov-fail-under=80 stays green)
  </acceptance_criteria>
  <done>Class closed and proven: one parametrized body over mt5+sfox+unknown, banned-substring invariant, non-collapse oracle; full Python gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → api_keys.sync_error | sFOX arm writes user-visible copy (same class as T-151-05) |
| worker → sFOX API (future live keys) | bearer session; failures must not leak internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-10 | Information Disclosure | sFOX sync_error strings | mitigate | fixed copy constants; Test 17 banned-substring invariant across ALL non-ccxt arms (ASVS V7) |
| T-151-11 | Tampering (fabricated data) | non-stable sFOX assets | mitigate | never priced at an invented rate — honest skip naming the assets (no-invented-data); STABLECOINS closed set is the only 1.0-mark license |
| T-151-12 | Denial of Service | worker loop on sFOX transport | mitigate | wait_for-bounded call; transient classification keeps retries bounded by the existing job budget |
| T-151-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest -q && mypy .` green from analytics-service/.
- The parametrized proof and both mutation falsifiers observed (noted in SUMMARY with the observed RED).
</verification>

<success_criteria>
- sFOX branch: get_balances only; USD/stablecoins contribute at mark 1.0; non-stable assets skip with named honest copy; failures transient with human copy.
- One parametrized test body proves the class (mt5 + sfox + unknown) with the banned-substring invariant.
- Multi-account non-collapse pinned with literal equities.
</success_criteria>

<output>
Create `.planning/phases/151-aum-a-book-you-can-reach-and-a-size-you-can-set/151-04-SUMMARY.md` when done.
</output>
