---
phase: 164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship
verified: 2026-09-18T23:52:00Z
status: passed
score: 6/6 roadmap success criteria verified; prior blocker F-R2-01 CLOSED and independently falsified
verified_at_sha: d6173036242c6b49a9d421539c610e00d9ddcb56
verified_tree: "base d6173036 PLUS the UNCOMMITTED diff on chore/164.8.2-verification; re-derive with `git diff -- .github scripts src | shasum -a 256 | cut -c1-16` -> 6988b9e88dfe252b"
drift_subjects:
  - scripts/test-ledger-drift-check.sh
  - scripts/restore-test-from-baseline.sh
  - .github/workflows/supabase-migrate.yml
  - .github/workflows/test-restore-from-baseline.yml
  - src/__tests__/drift-check-scripts.test.ts
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/supabase-migrate-test-first.test.ts
  - src/__tests__/critical-regressions.test.ts
  - src/__tests__/test-restore-workflow-wiring.test.ts
  - src/__tests__/prod-prober-wiring.test.ts
  - src/__tests__/restore-test-from-baseline.test.ts
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/6
  gaps_closed:
    - "SC-4/WR-05 — the destructive restore's artifact stops carrying unredacted/unscanned text (blocker F-R2-01)"
  gaps_remaining: []
  regressions: []
advisory:
  - finding: "`ALLOWED_SITES` in `src/__tests__/drift-check-scripts.test.ts` carries two stale restated counts introduced by this fix round: the `set +e` section header says EIGHT sites while the shipped list holds TEN, and the `::warning` header says TWO while it holds THREE (measured by parsing the literal)."
    category: other
    reason: "Comment/citation accuracy inside an allowlist whose own discipline is that every entry earns its place — the `[164.7-CITATION-DRIFT-01]` class CLAUDE.md devotes a section to. The allowlist itself is an exact-set check and bites correctly; only the prose count is wrong. Fix-or-drop under the founder's severity bar (not user-facing, not data-integrity). Resolved by updating the two header words in the same edit."
    evidence_status: "none provided — prose only; the shipped gate is correct"
  - finding: "`164.8.2-SECURITY.md` pins the working tree it audited by `git diff -- .github scripts src | shasum -a 256` = `540a8cb4…`; that digest no longer reproduces (now `6988b9e8…`). Three of the nine audited files were modified AFTER the re-audit's own timestamp (`test-restore-from-baseline.yml`, `gate-family-meta.test.ts`, `test-restore-workflow-wiring.test.ts`)."
    category: security
    reason: "By SECURITY.md's own stated standard ('a threats_open: 0 measured at a SHA that no longer describes the tree is UNMEASURED, not clean'), the recorded `threats_open: 0` is pinned to an earlier tree than the one being shipped. NOT a finding about the controls — this verifier re-read every control at the CURRENT tree and they hold. Resolved by re-running the digest line and restating it, or by naming which post-audit edits the verdict covers."
    evidence_status: "digest recomputed and mtimes compared; no control defect found at the current tree"
  - finding: "Two NEW code paths in the backup step's `scan_for_secrets()` carry no executed falsifier: the `grep` rc > 1 (unreadable file) branch, and the `if ! printf … > backup-scan.ok` write-failure branch."
    category: other
    reason: "Both fail CLOSED, and each is caught twice over independently of its own message: an unreadable file never enters `seen`, so the identity floor exits 1; and no verdict file means the staging step default-DENIES both backup files. The untested part is the SENTENCE, not the verdict. Fix-or-drop; a third arm on the existing `runScan` harness (a PATH shim exiting 2, and a read-only dir with a CLEAN pair) would close it."
    evidence_status: "traced by reading the shipped shell; no wrong-verdict path found"
  - finding: "`.planning/ROADMAP.md` Phase 164.8.4 GATERESIDUE still lists `[164.8.2-REFUSAL-STILL-PUBLISHES]` and `[164.8.2-SENTINEL-GREP-NUL-BLIND]` as deferrals it owns; `TODOS.md` on this branch marks both `[x]` CLOSED."
    category: other
    reason: "Record drift between the two ledgers — the ROADMAP over-states 164.8.4's remaining scope by two items. Fix-or-drop; roadmap phase CRUD goes through `/gsd-phase --edit`, never a hand edit."
    evidence_status: "both ledgers read at the current tree"
---

# Phase 164.8.2: GATEHARDENING Verification Report — RE-VERIFICATION

**Phase Goal:** Close the five Warnings `gsd-code-reviewer` raised against Phase 164.8's own gates.
Every one is a control that is weaker than it reads, and two of them are the SAME shape: the phase
hardened one half of a twin pair and left the other.

**Verified:** 2026-09-18, on branch `chore/164.8.2-verification`, base commit `d6173036` PLUS the
uncommitted working-tree diff (11 files). **Status:** passed.
**Re-verification:** Yes — supersedes the `gaps_found` verdict of 2026-09-18T22:55Z.

## Method — what "verified" means in this report

⛔ **Nothing below is carried from a SUMMARY, a review report, or the dispatching prompt.** Every
control was re-read in the shipped file at the current tree, and every gate was re-run in this
process. Where a claim was checkable two ways, it was checked two ways (the byte-identity of the
twin marker steps was confirmed both by the test that asserts it and by `diff` over the two
extracted regions).

⛔ **The changes are UNCOMMITTED.** Auditing by SUMMARY-cited commit hash is misleading here — the
prior verifier established that the ~46 atomic commits named across the SUMMARY files reach HEAD
only through squash-merge `eeb8e731` (PR #772), and the current round-2/round-3 fixes are not
committed at all. Everything here was read out of the working tree.

**One self-correction made mid-verification, recorded because it changed a conclusion.** A first
grep suggested `NEXT_JOB_RE` existed only in `critical-regressions.test.ts` — i.e. that round-2's
`WR2-02` half-class fix was still open. That was a truncation artefact of my own `head -12`. The
bound exists in all three copies (`critical-regressions.test.ts:1099`,
`test-restore-workflow-wiring.test.ts:136`, `supabase-migrate-test-first.test.ts:125`), each with
its deliberately-weaker `ANY_JOB_KEY_RE` sibling and the throw between them. `WR2-02` is genuinely
closed.

## The prior blocker: `F-R2-01` — CLOSED

The prior verdict rested on exactly one finding: the backup step's `scan_for_secrets()` called
`exit 1` on the first hit, ending the whole `run:` step, so a hit in `ledger.csv` left
`schema-before.sql` — a `supabase db dump` of a live hosted database — unscanned *and still staged*
into a 90-day, world-readable artifact on a public repo.

**All four of that gap's `missing:` items are discharged, and I verified each in the shipped file
rather than in a summary of it:**

| Required | Status | Evidence at the current tree |
|---|---|---|
| Accumulate-then-fail, never exit inside the function | ✓ | `scan_for_secrets()` now increments `hits`, records the basename, withholds, and `return 0`s on every branch; the single `exit 1` is after both calls. |
| A positive floor so a scan that ran fewer times is its own MEASURE_FAIL | ✓ | Stronger than asked: the floor pins **identity**, not arity — `SECRET_SCAN_EXPECTED="ledger.csv schema-before.sql"` compared against the sorted SET of basenames actually opened, with a separate `unreadable` list so an unopenable file gets its own diagnosis instead of the "aimed twice at the same file" sentence. |
| A falsifier that seeds a hit and proves the second file is still scanned | ✓ | `test-restore-workflow-wiring.test.ts:2041` **EXECUTES the shipped shell** (sliced out of the workflow, not a copy) against a seeded fixture pair, asserts both files are named and both are gone, and then **re-runs the same block with `return 0` mutated back to `exit 1` and asserts the pre-fix defect reappears** (`schema-before.sql` left on disk and unnamed). The calibration is the original defect verbatim. `:2101` does the same for the identity floor by aiming the second call at the first file. |
| Correct the in-artifact `README.txt` | ✓ | The README no longer claims both files "were scanned". It now states the rule the code actually implements: *"they are now staged only when the backup step left its verdict behind — so … their PRESENCE in this artifact is what tells you they were scanned."* |

**And the fix went further than the gap required**, in the direction that removes the class rather
than the instance: `withhold()` now checks `rm -f` **and** re-checks with `[ -e ]` (an unchecked
`rm -f` under `set -e` would have aborted the step before the annotation, leaving the file on disk
with nothing said), and artifact staging became **default-DENY behind two verdict files** —
`credential-scan.ok` (written only where the script's scan cleared all four `.sql` files) and
`backup-scan.ok` (written only where the backup scan cleared both). Six files, one rule. That closes
the wider `WR2-03` hole — every abort *between* `build_transaction` writing the four `.sql` files and
the scan running used to publish them unscanned — at no cost to the reversal recipe, because every
path the gate denies is a path on which the transaction never ran.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria 1–6)

| # | Truth | Status | Evidence (re-measured in this process) |
|---|-------|--------|-----------------------------------------|
| 1 | WR-02 — the VAC-08 frontier exemption gets a LOUD ceiling | ✓ VERIFIED | `scripts/test-ledger-drift-check.sh:164` bare top-level `FRONTIER_EXEMPT_CEILING=3`; the ceiling check inside `check()`. `bash … --self-test` → `self-test OK (11/11 arms …)`, exit 0, with `frontier-ceiling-exceeded RED (exit 1)` and `frontier-ceiling-boundary GREEN (exit 0)` both printed. |
| 2 | WR-03 — the same script gets an arms ratchet | ✓ VERIFIED | `:959 EXPECTED_ARMS=11`, checked at `:1201` before the pass check, with distinct messages for an arm that DISAPPEARED vs one ADDED, and printed as the success-line denominator. |
| 3 | WR-04 — the reverted-grep stops being NUL-blind | ✓ VERIFIED | `supabase-migrate.yml:927` (TEST twin) and `:1246` (PROD) both `grep -aEiq`. Widened past the original criterion: `test-restore-workflow-wiring.test.ts:5284` now asserts **every** file-reading grep in the destructive workflow carries `-a`, with `BARE_GREP_EXEMPTIONS` **empty** and anti-vacuity floors (`>5` invocations found, `>3` in scope) so the rule cannot silently stop measuring. |
| 4 | WR-05 — the destructive restore's artifact stops carrying unredacted/unscanned text | ✓ VERIFIED | Was the prior FAILED truth. Enumerated staging allowlist pinned as an exact list (`:1532`, `survivors.sql` explicitly IN per the founder amendment); `REDACT_OUTCOME` four-arm guard; **plus** the new two-verdict default-DENY and the accumulate-then-fail scan. Proven by executed arms that run the shipped shell: `:1606` (staged set == allowlist over all real names), `:1724` (forced failure stages nothing), `:1822` (no `credential-scan.ok` ⇒ four `.sql` withheld), `:2150` (no `backup-scan.ok` ⇒ both backup files withheld), `:2041`/`:2101` (the scan itself). |
| 5 | WR-06 — the softening-token scan reaches the destructive workflow, as a TRIPLET | ✓ VERIFIED | Parsed the literal in all three files: `SOFTENING_TOKENS` length **9** in `test-restore-workflow-wiring`, `prod-prober-wiring` and `supabase-migrate-test-first`. The `2>/dev/null` tolerance is an exact SET of named sites, not a count, and reds in both directions and on a swap (`:3606`). |
| 6 | Every fix carries a falsifier that was observed RED | ✓ VERIFIED | Not taken on SUMMARY narration. The falsifiers for this round are **in-test mutations that run in the suite**, so I observed them by running it: the `exit 1` re-insertion (`:2041`), the same-file double-aim (`:2101`), the `grep` PATH shim exiting 2 for F-R2-05, the `extraRc` stub for F-R2-04, the `STALE EXEMPTION` ceiling arm, and the `jobBlock` throw calibrations. 926 tests over the six phase-owned files, 0 failures. |

**Score:** 6/6 truths verified (0 present-but-behaviour-unverified).

### Requirements Coverage — review findings (the phase's requirement IDs are review IDs, not `REQUIREMENTS.md` rows)

⚠️ `WR-*`/`IN-*` are findings in `164.8-REVIEW.md`; they have no `REQUIREMENTS.md` entries, and the
ROADMAP entry says so explicitly (*"Requirements: TBD (no v1.20 requirement IDs) + the five Warnings
and seven Info findings"*). Every ID declared in any of the five PLAN frontmatters is accounted for
below — the union is wider than the dispatching prompt's list, which omitted `WR-05` and
`IN-01/02/05/06/07`.

| ID | Declared in | Status | Evidence |
|----|-------------|--------|----------|
| WR-02 | Plan 01 | ✓ SATISFIED | Truth 1 |
| WR-03 | Plan 01 | ✓ SATISFIED | Truth 2 |
| WR-04 | Plans 02, 03 | ✓ SATISFIED | Truth 3, plus both post-verify greps carrying `-a` (`:4453`) |
| WR-05 | Plan 03 | ✓ SATISFIED | Truth 4 — including the round-2/3 widening |
| WR-06 | Plan 04 | ✓ SATISFIED | Truth 5 |
| IN-01 | Plan 05 | ✓ SATISFIED | `grep -c '^refuse_[a-z_]*() {'` = 10; the script's own refusal word is pinned by derivation |
| IN-02 | Plan 05 | ✓ SATISFIED | Arm list names arm 26; self-test prints 27/27 |
| IN-03 | Plan 02 | ✓ SATISFIED | `suffix()` (`supabase-migrate-test-first.test.ts:1226`) throws `SUFFIX_ANCHOR not found`, with the one-character-degradation calibration at `:1301` |
| IN-04 | Plan 02 | ✓ SATISFIED | `applyJobBlock` bounded by `NEXT_JOB_RE` with the `ANY_JOB_KEY_RE` throw and a fake-trailing-job calibration (`:1224`) |
| IN-05 | Plan 05 | ✓ SATISFIED | `SEAM_FLOOR=16` registered and dated in the threshold meta-gate output |
| IN-06 | Plan 05 | ✓ SATISFIED | `restore-test-from-baseline.sh:1212-1213` — `grep -av` wrapped, `MEASURE_FAIL: could not read … while filtering` |
| IN-07 | Plan 03 | ✓ SATISFIED | Three-way indented-provenance fixture (`:4577`) agreeing `RECORDED_SHA_RE`, the workflow sed and the script sed |
| SC-6 | all five plans | ✓ SATISFIED | Truth 6 |

**Orphaned requirements:** none. No `REQUIREMENTS.md` row maps to Phase 164.8.2.

### Round-2 / round-3 findings — routing traced, not taken on trust

| ID | Sev | Disposition at the current tree |
|----|-----|-------------------------------|
| F-R2-01 | BLOCKER | **CLOSED.** Section above. |
| F-R2-02 | Medium | **CLOSED.** Both marker predicates rc-bounded with `-a` in BOTH copies; I confirmed the two regions are byte-identical by `diff`, and the identity is itself test-enforced (`supabase-migrate-test-first.test.ts:1763`) with an executed undecidable-predicate arm (`test-restore-workflow-wiring.test.ts:2946`). |
| F-R2-04 | Low | **CLOSED.** The advisory extra-ledger query's rc is captured and narrated; stderr stays suppressed on purpose (public log). Runtime RED + CONTROL arms with an `extraRc` stub. |
| F-R2-05 | Low | **CLOSED.** The frontier-tip presence test is rc-bounded; runtime RED arm via a `grep` PATH shim that exits 2 only on `-aqFx`, with a delegating-shim CONTROL proving the shim is not the failure. |
| WR2-01 / WR2-04 | Warning | **CLOSED.** The ancestry sentinel gained `-a` and an rc bound, and the exemption **self-expired**: `BARE_GREP_EXEMPTIONS` is empty, the `STALE EXEMPTION` arm forced the dated entry to be deleted rather than waived, and `TODOS.md` records the closure. |
| WR2-02 | Warning | **CLOSED.** Bound present in all three copies (see the self-correction above). |
| WR2-03 / C1 | Warning | **CLOSED** by the two-verdict default-DENY. |
| R3 WR-02/03/05 | Warning | **CLOSED** — contradictory staging denials (fate now derived from the sibling's flag), unguarded verdict write (`if ! printf … fi`), unreadable-file misdiagnosis (own `unreadable` category). |
| R3 WR-01, R3 Info ×3 | Warning/Info | **BOOKED** in `TODOS.md` as `[164.8.2-R3-META-WINDOW-UNFALSIFIED]` and `[164.8.2-R3-SCAN-COUNTER-RESIDUE]`, each explicitly marked fix-or-drop with its measurement. The scan-residue entry names the same three items I found independently (`hits` double-count on the unwithholdable path, the unreachable `[ -e ]` arm, unquoted `${seen}`) — the record matches the code. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/test-ledger-drift-check.sh` | ceiling, arms ratchet, rc-bounded reads | ✓ VERIFIED | self-test 11/11, exit 0 |
| `scripts/restore-test-from-baseline.sh` | `refuse_credential_in_published_sql` + `credential-scan.ok`, MEASURE_FAIL wrap | ✓ VERIFIED | self-test 27/27, exit 0 |
| `.github/workflows/supabase-migrate.yml` | `grep -aEiq` both C-0331 sites; re-synced marker step | ✓ VERIFIED | byte-identical to the restore workflow's copy by `diff` |
| `.github/workflows/test-restore-from-baseline.yml` | accumulate-then-fail scan, `withhold()`, identity floor, two-verdict default-DENY staging, corrected README | ✓ VERIFIED | was ⚠️ PARTIAL in the prior report; all four gap items discharged |
| `src/__tests__/test-restore-workflow-wiring.test.ts` | executed scan + staging arms with calibrations | ✓ VERIFIED | included in the 926-test run |
| `src/__tests__/drift-check-scripts.test.ts` | F-R2-04/05 runtime arms, exact-set `ALLOWED_SITES` | ✓ VERIFIED | passed; no flake observed this run |
| `src/__tests__/supabase-migrate-test-first.test.ts` | marker-step executed arms, twin byte-identity, `suffix()` throw | ✓ VERIFIED | passed |
| `src/__tests__/restore-test-from-baseline.test.ts` | `SEAM_FLOOR`, credential-scan arms | ✓ VERIFIED | passed |
| `src/__tests__/gate-family-meta.test.ts` | threshold registry incl. the ceiling | ✓ VERIFIED | passed; 12 threshold sites over 9 files all `ok` |
| `src/__tests__/critical-regressions.test.ts` | bounded `applyJob` slices | ✓ VERIFIED | passed |
| `src/__tests__/prod-prober-wiring.test.ts` | 9-token triplet member | ✓ VERIFIED | 105/105 passed |
| `TODOS.md` | round-3 residues booked; two entries closed | ✓ VERIFIED | read directly |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `refuse_credential_in_published_sql` | staging step | `$RESTORE_OUT_DIR/credential-scan.ok` | ✓ WIRED | Script writes to `RESTORE_OUT_DIR`; workflow exports it as `${RUNNER_TEMP}/test-backup`, the same `outdir` the staging step reads. Same directory confirmed by grep on both sides. |
| backup step's scan | staging step | `${outdir}/backup-scan.ok` | ✓ WIRED | Written only after the identity floor and the `hits` check both pass; read with `grep -aq 'carry no credential shape'`. |
| staging allowlist loop | verdict flags | `case … [ "${scan_ok}" -eq 1 ] \|\| continue` | ✓ WIRED | Neither verdict file is in the staged name list — the control's input does not ship beside its subject. |
| redaction step | staging step | `steps.redact.outcome` → `REDACT_OUTCOME` | ✓ WIRED | Four-arm guard; empty/unrecognised is a MEASURE_FAIL that exits 1 rather than a silent permanent deny. |
| `supabase-migrate.yml` marker step | `test-restore-from-baseline.yml` marker step | byte-identity pin | ✓ WIRED | `diff` over the two extracted regions: identical. |

### Behavioral Spot-Checks (run in this process)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| The six phase-owned vitest files | `npx vitest run test-restore-workflow-wiring restore-test-from-baseline gate-family-meta drift-check-scripts supabase-migrate-test-first critical-regressions` | 6 files, **926 passed / 0 failed** | ✓ PASS |
| Triplet third member | `npx vitest run src/__tests__/prod-prober-wiring.test.ts` | 105 passed | ✓ PASS |
| VAC-08 gate self-test | `bash scripts/test-ledger-drift-check.sh --self-test` | `self-test OK (11/11 arms …)`, exit 0 | ✓ PASS |
| Restore-script self-test | `bash scripts/restore-test-from-baseline.sh --self-test` | `self-test OK (27/27 arms …)`, exit 0 | ✓ PASS |
| Typecheck | `npx tsc --noEmit` | exit 0, no output | ✓ PASS |
| Planning hygiene | `npm run check:planning-hygiene` | `OK — 6622 tracked files scanned` | ✓ PASS |
| Leak check over this phase's untracked `.planning` files (hygiene scans tracked files only) | `grep -rlE "<home basename>" .planning/phases/164.8.2-*/*.md` | no matches | ✓ PASS |
| Marker-step twin identity | `diff` over the two extracted `Which database am I on` regions | identical | ✓ PASS |
| Working-tree digest vs the one SECURITY.md pins | `git diff -- .github scripts src \| shasum -a 256 \| cut -c1-16` | `6988b9e8…` ≠ pinned `540a8cb4…` | ⚠️ see Advisory 2 |

### Probe Execution

No `scripts/*/tests/probe-*.sh` is declared by any PLAN or SUMMARY for this phase, and none exists
for these gates — this phase's runnable checks are the two `--self-test` entry points and the vitest
suites, all run above. **Step 7c: not applicable (no probe declared, none conventional for this
phase's subject).**

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/__tests__/drift-check-scripts.test.ts` | `ALLOWED_SITES` section headers | Restated count diverged from the shipped list (EIGHT vs 10; TWO vs 3) | 📋 Advisory | Prose only; the exact-set gate itself is correct and passes. Advisory 1. |
| `.github/workflows/test-restore-from-baseline.yml` | `scan_for_secrets()` rc>1 branch; `if ! printf` branch | New code path with no executed falsifier | 📋 Advisory | Both fail closed and are each caught twice over by independent gates. Advisory 3. |
| `.planning/ROADMAP.md` | Phase 164.8.4 deferral list | Ledger drift vs `TODOS.md` | 📋 Advisory | Advisory 4. |

No `TBD`, `FIXME` or `XXX` debt marker appears in any file this phase modified (checked across all
11 changed files).

⚠️ **Why these are Advisory and not Blockers.** Under this repo's founder-scoped severity bar, only
user-facing or data-integrity findings block — and on this public repo, information disclosure
counts as data-integrity. None of the four is either: two are prose, one is an audit-record pin, and
one is a missing *falsifier* over paths whose *verdict* I traced and found correct. Each is
fix-or-drop, so by the founder's 2026-09-15 scoping none may become a phase or a `<deferred>` entry.
Advisories 1, 2 and 4 are each a one-line edit and belong in the ship commit.

### Human Verification Required

None. Every truth in this phase is a CI control whose behaviour was exercised in this process — the
workflow steps by extracting the shipped shell and running it against seeded fixtures, the scripts
by their own self-tests. No visual, real-time or external-service behaviour is in scope.

ℹ️ **Not a verification item, but the ship step's job:** this tree is UNCOMMITTED, so no CI run has
ever executed these workflows. `sql-mutation`, `sql-gate-lint` and `plan-anchor-verify` have not run
against it. Per this repo's standing rule, a green board must be SHA-bound — count the checks and
match `headSha` after the push rather than reading "no red" as healthy.

## Summary

The phase goal is achieved. All five Warnings `gsd-code-reviewer` raised against Phase 164.8's gates
are closed at the current tree, all seven Info findings with them, and the single blocker that held
the prior verdict — `F-R2-01`, an information-disclosure path on a public repo — is closed with more
mechanism than the gap asked for: accumulate-then-fail, a checked `withhold()`, an identity floor
that pins *which* files were opened rather than how many calls were made, artifact staging turned
default-DENY behind two verdict files, and an executed falsifier whose calibration reproduces the
original defect verbatim before asserting the fix removes it.

The phase's own thesis — that fixing the instance a reviewer names, rather than the class, produces a
half-class the next round finds again — held up under this re-verification in the direction that
matters: the round-2 and round-3 fixes each closed the *class* (both marker copies, all three
softening lists, all six staged files, every file-reading grep in the workflow), and the one
exemption that existed was made to expire by its own ceiling arm rather than being waived. Four
advisory items remain, all prose or record accuracy, none of them a control.

---

_Verified: 2026-09-18T23:52:00Z_
_Verifier: Claude (gsd-verifier)_
