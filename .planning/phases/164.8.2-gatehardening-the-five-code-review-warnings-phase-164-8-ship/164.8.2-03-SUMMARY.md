---
phase: 164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship
plan: 03
subsystem: ci-gates
tags: [ci-gates, github-actions, public-artifact, information-disclosure, wiring-pins]
status: complete

requires:
  - ".github/workflows/test-restore-from-baseline.yml (Phase 164.8 plan 03/05 — backup, redaction and upload steps)"
  - "src/__tests__/test-restore-workflow-wiring.test.ts (extractRunScript / stepIndex / stepBody / calibrate idioms)"
  - "scripts/check-baseline-staleness.mjs (RECORDED_SHA_RE — imported, not restated)"
provides:
  - "An enumerated by-NAME staging allowlist between Redact and Upload: every future file the restore script writes is OUT of the world-readable artifact by default"
  - "Four truthful comments where four lied about the artifact's shape and the secret scan's scope"
  - "A README-vs-allowlist agreement pin, so the README shipped INSIDE the artifact cannot describe a file the artifact does not carry"
  - "grep -a on both remaining post-verify greps, ending the contradiction with the rule stated three lines above them"
  - "A three-way proof that the confirm token, the restore script and the staleness gate read the SAME provenance row"
affects:
  - ".github/workflows/test-restore-from-baseline.yml"
  - "src/__tests__/test-restore-workflow-wiring.test.ts"
  - "scripts/restore-test-from-baseline.sh (header comment only)"

tech-stack:
  added: []
  patterns:
    - "Narrow at a NEW run: step + the upload path:, never by widening the redaction globs (Contradiction C5)"
    - "Allowlist parsed OUT of the workflow rather than restated in the test, so the two cannot drift"
    - "trap … ERR that REMOVES the staging directory, so if-no-files-found: error reddens the upload"
    - "Platform-dependent pre-fix behaviour RECORDED per flavour, never asserted as a permanent test"

key-files:
  created: []
  modified:
    - ".github/workflows/test-restore-from-baseline.yml"
    - "src/__tests__/test-restore-workflow-wiring.test.ts"
    - "scripts/restore-test-from-baseline.sh"

decisions:
  - "The staged set is 13 files, not the plan's restated '7 named + 8 channels'. The 18-name inventory the plan lists is CORRECT and was regenerated; only its channel arithmetic was off. 18 = 7 named + 6 channels + 5 withheld."
  - "The SCOPE comment states that restore.sql EMBEDS survivors.sql, rather than claiming the DDL 'appears exactly once' — the founder's phrasing cannot hold literally because build_transaction cats one into the other, and a comment claiming a property the code lacks is the precise defect WR-05 is about."
  - "The executed NUL arm asserts only the direction that holds on every measured grep flavour. A calibration stripping the `a` would pass on BSD and fail on ugrep — flaky, not evidence."

metrics:
  duration: "~20 min"
  completed: 2026-09-09

actuals:
  tokens: 13475
  tasks: 3
  commits: 3
---

# Phase 164.8.2 Plan 03: WR-05 artifact narrowing, four truthful comments, WR-04 remainder and IN-07 Summary

The world-readable restore artifact stopped being a directory and became an enumerated
by-name allowlist, so every future file the restore script writes is out by default; the four
comments that described the old shape — including one asserting a secret-scan scope the code
never had — now describe the new one; and the two bare `grep -q` lines and the anchored
confirm-token `sed` were closed with executed proofs.

## Commits

| Commit | Task | What |
|---|---|---|
| `8150f9a5` | 1 | Staging step, retargeted upload, six executed wiring arms (WR-05) |
| `ae3450bc` | 2 | Four comment rewrites + README-vs-allowlist and SCOPE pins |
| `d6668529` | 3 | `grep -aq` × 2 (WR-04 remainder) and unanchored sed + three-way fixture (IN-07) |

## The regenerated file inventory — REAL counts

⛔ Regenerated from the WRITERS, not carried from the review (which said seven) or from the
ROADMAP (which said 10 of 18):

```
grep -oE 'RESTORE_OUT_DIR}?/[A-Za-z0-9_.-]+' scripts/restore-test-from-baseline.sh   -> 13 distinct
grep -oE 'outdir}?/[A-Za-z0-9_.-]+'          .github/workflows/test-restore-from-baseline.yml -> 5 distinct
union                                                                                -> 18
```

One false positive was found and discarded: `post-census.txt.` — a prose sentence at
`restore-test-from-baseline.sh:1578` ending in a full stop, not a filename. The real name
`post-census.txt` was already in the set.

| # | file | writer | staged? | why |
|---|---|---|---|---|
| 1 | `census.err` | script | ✅ channel | redacted `*.err` |
| 2 | `census.sql` | script | ✅ named | the catalogue query it ran |
| 3 | `dump.log` | backup step | ✅ channel | redacted `*.log` |
| 4 | `ledger.csv` | backup step | ✅ named | reversal recipe; secret-scanned when written |
| 5 | `ledger.err` | backup step | ✅ channel | redacted `*.err` |
| 6 | `marker.err` | script | ✅ channel | redacted `*.err` |
| 7 | `post-census.rollback-view.txt` | script | ❌ | restates the survivor DDL, no reversal claim |
| 8 | `post-census.txt` | script | ❌ | same DDL + owner-named rows |
| 9 | `pre-census.rollback-view.txt` | script | ❌ | same DDL |
| 10 | `pre-census.txt` | script | ❌ | the leak itself: `CREATE POLICY … USING (<qual>)` + owner rows |
| 11 | `README.txt` | backup step | ✅ named | the artifact's self-description |
| 12 | `refdata.err` | script | ✅ channel | redacted `*.err` |
| 13 | `refdata.sql` | script | ✅ named | forward input |
| 14 | `restore.sql` | script | ✅ named | the transaction psql actually ran |
| 15 | `schema-before.sql` | backup step | ✅ named | reversal recipe; secret-scanned when written |
| 16 | `survivors.keys` | script | ❌ | neither recipe nor channel → out by DEFAULT, not by judgement |
| 17 | `survivors.sql` | script | ✅ named | founder amendment: the DDL lives in the file that needs it |
| 18 | `transaction.out` | script | ✅ channel | redacted `*.out` |

**18 = 7 named + 6 channels + 5 withheld. The staged set is 13 files.**

⚠️ **A correction to the plan, taken under its own instruction** ("if the inventory differs
from these 18 names, the FIXTURE follows the inventory and the SUMMARY says so"). The 18 NAMES
in the plan are exactly right. Its restated expectation of "the 7 named + the 8 channel files"
is arithmetically wrong — there are **6** channel files (`census.err`, `ledger.err`,
`marker.err`, `refdata.err`, `dump.log`, `transaction.out`), not 8. The fixture and every
assertion follow the regenerated inventory. Nothing in the test restates 13 either: the
expectation is DERIVED by filtering the seeded set through the workflow's own two lists.

## Falsifiers — what was neutered, and the RED actually observed

Every neuter asserted that it APPLIED before any result was believed, printed its diff, and was
restored by `cp` from pristine bytes with the sha compared. **No `git checkout --` was used
anywhere.** Backups were re-taken after each task's edits (`wf-t1.yml`, `wf-t2.yml`,
`wf-t3.yml`).

### F1 — WR-05, by hand on the extracted step (`cp -a` neuter)

Neutered: the enumerated `for f in …` copy loop replaced with `cp -a "${outdir}/." "${stage}/"`.
The harness asserted the anchor was present and that the mutant differed, and printed the diff:

```
18,22c18
<   for f in ledger.csv schema-before.sql README.txt census.sql survivors.sql restore.sql refdata.sql; do
...
>   cp -a "${outdir}/." "${stage}/"
```

Observed: **19 files staged**, `pre-census.txt` among them — the old shape reproduced exactly.
Encoded as CALIBRATION 2 inside the executed arm, so it re-runs every suite run.

### F2 — WR-05, on the LIVE workflow (allowlist widened)

Neutered: `pre-census.txt` appended to the live `for f in …` line (sha changed
`6713d9b5…` → `4d77ba0e…`). **2 arms went RED:**

```
FAIL … > the test's allowlist and the step's `for f in …` line are the SAME list
AssertionError: the staging step's `for f in …; do` line no longer copies exactly the
decided allowlist. … expected [ 'ledger.csv', …(7) ] to deeply equal [ 'ledger.csv', …(6) ]
+   "pre-census.txt",

FAIL … > EXECUTED — the staged set is exactly the allowlist over all 18 REAL names
AssertionError: `pre-census.txt` reached the world-readable artifact. … expected true to be false
```

Restored: `restore verified byte-identical.` (`6713d9b5…`).

### F3 — Task 2, README re-insertion on the LIVE workflow

Neutered: the withdrawn `pre-census.txt    the restore script's pre-drop census…` entry put
back into the README's WHAT IS HERE list. **1 arm went RED:**

```
FAIL … > the README inside the artifact describes only files the artifact carries
AssertionError: the README shipped INSIDE the artifact names 1 file(s) the staging step does
not copy. … expected [ 'pre-census.txt' ] to deeply equal []
```

Restored byte-identical (`4b01e01a…`).

### F4 — Task 3, all three fixes reverted on the LIVE workflow

Neutered: `-a` stripped from both post-verify greps and the sed re-anchored (3 substitutions,
each asserted present first). **2 arms went RED:**

```
FAIL … > both remaining post-verify greps carry -a, so the step's own rule has no counter-example
AssertionError: 2 post-verify grep(s) still omit `-a` … expected [ …(2) ] to deeply equal []
+   "if grep -q 'Would push these migrations:' \"${out}\"; then",
+   "if ! grep -q 'Remote database is up to date.' \"${out}\"; then",

FAIL … > all three readers extract the same sha from an INDENTED provenance row
AssertionError: the workflow's confirm-token sed is ANCHORED again. … expected true to be false
```

Restored byte-identical (`1fecf4e2…`).

### F5 — a neuter that did NOT apply, caught before it was believed

The IN-07 calibration's first mutation, `wfProgram.replace(/^s\//, "s/^")`, produced
`s/^.*\|…` — which still matches an indented row because `^.*` consumes the leading spaces. The
arm reported the calibration failing (`expected 'aaaa…' to be ''`) rather than passing a false
RED, because the calibration asserts the mutant goes BLANK. Replaced with a reconstruction of
the actual pre-fix program (`s/^\|…`), and the reason is recorded in a comment beside it.

### F6 — the built-in `calibrate()` twins

Six further mutations run on every suite execution and assert the predicate FLIPS: the
allowlist-parse (name mutated), the upload `path:` (pointed back at the raw directory), the
Redact<Stage ordering, the Stage<Upload ordering, `if: always()` on the stage step, the dead
SCOPE sentence re-inserted, and the SCOPE comment's naming of the staging step.

## The pre-fix grep observation, per flavour (recorded, NOT asserted — RESEARCH A1)

Fixture: `Connecting to remote database...\n<NUL>\nRemote database is up to date.\n` (66 bytes).

| grep | `-q` (pre-fix) | `-aq` (fixed) |
|---|---|---|
| `/usr/bin/grep` — BSD grep 2.6.0-FreeBSD | rc=0 (reads it fine) | rc=0 |
| ugrep 7.8.4 | not re-measured this session; RESEARCH measured **rc=1, reads CLEAN** | rc=0 |
| GNU grep (ubuntu runner) | **UNOBSERVED** — not installed locally, no `ggrep` | — |

⛔ This is why the executed NUL arm asserts only the FIXED direction (exit 0, ledger reported
consistent) and not the pre-fix failure: a calibration stripping the `a` would pass on BSD and
fail on ugrep, which is a flaky test rather than evidence. The deterministic half is the static
arm, which names any post-verify `grep -q` missing `-a` on every platform — and that is the arm
F4 observed RED.

## Verification

| Gate | Result |
|---|---|
| `npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts` | 44 passed (was 33 at plan start; +11 arms) |
| `npx vitest run src/__tests__/restore-test-from-baseline.test.ts` | 19 passed — no pin moved by the header edit |
| `supabase-migrate-test-first.test.ts` + `critical-regressions.test.ts` | 250 passed across all four files |
| Task 1 `<verify><automated>` | exit 0 (`test-backup-artifact` live count ≥ 2) |
| Task 2 `<verify><automated>` | exit 0 (old README line gone; `--help` prints) |
| Task 3 `<verify><automated>` | exit 0 (`grep -aq` live count exactly 2) |
| `bash scripts/restore-test-from-baseline.sh --help` | exit 0; the new T-164.8-21 paragraph prints |
| `npx tsc --noEmit` | exit 0 |
| `npx eslint src/__tests__/test-restore-workflow-wiring.test.ts` | exit 0 |
| `node scripts/verify-plan-anchors.mjs <this plan>` | exit 0, 22 claims checked (before AND after) |
| 9 softening tokens over the new step body | **0 hits on live lines, and 0 including comments** |

## Deviations from Plan

### [Rule 1 - Bug] The IN-07 calibration mutation did not reproduce the pre-fix program
- **Found during:** Task 3
- **Issue:** `replace(/^s\//, "s/^")` yields `s/^.*\|…`, which still matches an indented row.
- **Fix:** reconstruct `s/^\|…` from the program's own prefix, guarded by an assertion that the
  program still begins `s/.*\|`; reason recorded in a comment.
- **Commit:** `d6668529`

### [Rule 1 - Bug] The NUL fixture was written as a RAW NUL byte into the test source
- **Found during:** Task 3
- **Issue:** the first draft put a literal 0x00 into `test-restore-workflow-wiring.test.ts`.
  `grep -n "dry-run.fixture"` then reported the line ABSENT — the repo's own standing
  measurement about `src/lib/wizardErrors.test.ts`, reproduced by accident on a new file.
- **Fix:** replaced with a ` ` escape via a `node:fs` script that asserted the raw-NUL
  count went 1 → 0. The runtime string still carries a NUL; the file's bytes are grep-readable.
- **Commit:** `d6668529`

### [Rule 2 - Missing critical functionality] A comment quoting a banned softening token
- **Found during:** Task 1
- **Issue:** the staging step's comment explaining why `if [ -f ]` is used spelled a banned
  token literally, and Plan 04 will run a 9-token scan over this step. The comment would be a
  hit under any scanner that does not strip comments — the exact class CONTEXT records (a
  widened matcher reading its own example).
- **Fix:** reworded to name the class without quoting a token. Measured after: 0 hits over the
  step body including comments.
- **Commit:** `8150f9a5`

### [Deliberate, per plan] `restore.sql` embeds `survivors.sql`
Not a deviation but the plan's flagged item, closed as instructed: the founder's "the DDL
appears exactly ONCE" cannot hold literally, because `build_transaction` does
`cat "$RESTORE_OUT_DIR/survivors.sql" >> "$out"`. The SCOPE comment and the README both state
this outright — `restore.sql` is kept as the transaction psql actually ran, and the claim made
is that each file carrying the DDL has its own reason to be there, NOT that it appears once.

### [Out of scope, per plan] the redaction arm's `keepers` fixture
`src/__tests__/test-restore-workflow-wiring.test.ts` line ~736 seeds `["schema.sql",
"ledger.csv"]`, and `schema.sql` is a filename this workflow never writes (`schema-before.sql`
is). Harmless — that arm measures the fail-closed destroy path, not the name set — and the plan
explicitly says do not touch it. Left alone; recorded here so it is not lost.

## Known Stubs

None. No placeholder, hardcoded-empty or TODO value was introduced by this plan.

## Threat Flags

None. The three trust boundaries in the plan's register are the ones this plan hardens; no new
network endpoint, auth path, file-access pattern or schema change at a trust boundary was
introduced. `T-164.8.2-11` (survivor DDL in `survivors.sql`/`restore.sql`) remains an **accept**
by founder decision, and is now stated in both the SCOPE comment and the artifact's README.

## What was NOT completed

Nothing in the plan was skipped. Two things are deliberately incomplete and are recorded rather
than fixed:

1. **`T-164.8-21`'s STDOUT half is still open.** The restore script still cats the whole
   pre-census, DDL column included, to the public Actions log. No staging step can reach that.
   The script header now says so in the same paragraph that books the threat.
2. **GNU grep's pre-fix behaviour on the runner is unobserved.** It could not be measured
   locally (no `ggrep`, and RESEARCH records that the `ubuntu:24.04` pull did not complete).
   The permanent arms are written so they do not depend on it.

## Self-Check: PASSED

Run, not asserted (`scratchpad/selfcheck2-164.8.2-03.sh`, `SELFCHECK_RC=0`):

- `.github/workflows/test-restore-from-baseline.yml` — FOUND (modified)
- `src/__tests__/test-restore-workflow-wiring.test.ts` — FOUND (modified), **0 raw NUL bytes**
- `scripts/restore-test-from-baseline.sh` — FOUND (modified)
- `.planning/…/164.8.2-03-SUMMARY.md` — FOUND
- `8150f9a5`, `ae3450bc`, `d6668529` — all resolve as `commit` via `git cat-file -t`
- No file deleted by any commit (`git diff --diff-filter=D --name-only 793b4395..HEAD` empty)
- Working tree clean after every commit

⚠️ **The first self-check reported all three commits MISSING, and that reading was FALSE.**
`git log --oneline --all | grep -q <hash>` under `set -o pipefail`: `grep -q` exits on the
first match, `git` dies of SIGPIPE, and `pipefail` propagates the non-zero to the `if`, so a
present commit reads as absent. Resolved by asking git directly (`git cat-file -t`), which has
no pipeline to misread. Recorded here rather than quietly fixed — a check that reports the
opposite of the truth is the same class this phase exists to close, and it appeared in this
plan's own harness.
