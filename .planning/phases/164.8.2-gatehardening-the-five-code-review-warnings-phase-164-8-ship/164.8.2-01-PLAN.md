---
phase: 164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/test-ledger-drift-check.sh
  - src/__tests__/drift-check-scripts.test.ts
  - src/__tests__/gate-family-meta.test.ts
  - TODOS.md
autonomous: true
requirements: [WR-03, WR-02, SC-6]
tags: [ci-gates, bash-self-test, ratchet, vac-08]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Deleting any one `run_arm` line from `scripts/test-ledger-drift-check.sh --self-test` makes the run print `SELF-TEST FAIL: N arms ran but EXPECTED_ARMS is M` and exit 1 — a vanished arm is RED, never a smaller PASSED (WR-03, SC-2)."
    - "With more migrations above the TEST ledger's frontier tip than `FRONTIER_EXEMPT_CEILING`, the gate emits `::error::` naming the count, the tip, the ceiling and EVERY exempted version, sets `bad=1` and exits 1; with exactly the ceiling it stays green and still prints the exempt count (WR-02, SC-1)."
    - "Both new behaviours were OBSERVED RED on a scratch copy (neuter → red → restore from pristine bytes) before the change was called done, and the SUMMARY carries the observed output (SC-6)."
    - "`gate-family-meta.test.ts` sees the new ceiling as a registered, measured, dated threshold site — the second layer CLAUDE.md requires for any constant a runner gates in one direction only."
  artifacts:
    - "scripts/test-ledger-drift-check.sh — `EXPECTED_ARMS=<n>` with a dated MEASURED line beside it; the `-ne` count check ahead of the pass check; `${EXPECTED_ARMS}` as the success-line denominator; `ARM_FORCE_INVERT` clause in `run_arm`; a harness-calibration arm; `FRONTIER_EXEMPT_CEILING=<n>` at top level with its MEASURED justification; the ceiling check inside `check()`; two ceiling arms (RED above, GREEN at the boundary)."
    - "src/__tests__/drift-check-scripts.test.ts — new arm labels pinned by name; EXPECTED_ARMS pins copied from restore-test-from-baseline.test.ts; ceiling-constant pins; vitest RED/GREEN arms driving the gate through `scaffoldLedgerCase` with a purpose-built migrations corpus."
    - "src/__tests__/gate-family-meta.test.ts — one new `KNOWN_THRESHOLD_SITES` entry for the ceiling."
  key_links:
    - "The ceiling arms are counted by EXPECTED_ARMS — WR-03 lands BEFORE WR-02 in the same file so the new arms cannot vanish silently (RESEARCH ordering, honoured)."
    - "The ceiling constant is a bare `NAME=<digits>` line at top level (NOT `${…:-3}`, NOT `local`), because `findThresholdSites` in gate-family-meta.test.ts only recognises that spelling — Contradiction C2 resolved for the family-meta convention."
    - "The RED ceiling arm asserts on the `::error::` TEXT, not on exit 1 alone, so it cannot be 'ok (exit 1)' for an unrelated reason (Pitfall 2 / A3)."
---

<objective>
Harden `scripts/test-ledger-drift-check.sh` (VAC-08) in the two ways `164.8-REVIEW.md` found it weaker than it reads: give its `--self-test` the arms ratchet its sibling `scripts/restore-test-from-baseline.sh` already carries (WR-03), then give the apply-on-merge frontier exemption a LOUD count ceiling (WR-02, CONTEXT Area 1 — LOCKED: a count ceiling, a ratchet lowered when reality allows and never raised to pass). Both fixes carry falsifiers observed RED on scratch copies and a vitest second layer.

Purpose: today a cleanup that deletes the three frontier arms prints `self-test OK (5/5 …)`, and one failed TEST apply exempts every later migration forever while the gate prints `0 NEW drift`. Each is a control that passes while measuring nothing.

Output: the edited gate script, extended `drift-check-scripts.test.ts`, one registered threshold site in `gate-family-meta.test.ts`, and a dated note on the `[164.8-PUSH-RACE-VAC08]` TODOS entry saying the window is now bounded.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-CONTEXT.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-RESEARCH.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-PATTERNS.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-REVIEW.md
@scripts/test-ledger-drift-check.sh
@scripts/restore-test-from-baseline.sh
@src/__tests__/drift-check-scripts.test.ts
@src/__tests__/restore-test-from-baseline.test.ts
@src/__tests__/gate-family-meta.test.ts
@src/__tests__/mutation-runner-floors.test.ts

<!-- verify-symbol: scripts/test-ledger-drift-check.sh run_arm -->
<!-- verify-symbol: scripts/test-ledger-drift-check.sh exempt_count -->
<!-- verify-symbol: scripts/test-ledger-drift-check.sh frontier_tip -->
<!-- verify-symbol: scripts/test-ledger-drift-check.sh arm_env -->
<!-- verify-symbol: scripts/restore-test-from-baseline.sh EXPECTED_ARMS -->
<!-- verify-symbol: scripts/restore-test-from-baseline.sh arm_harness_calibration -->
<!-- verify-symbol: scripts/restore-test-from-baseline.sh ARM_FORCE_INVERT -->
<!-- verify-symbol: src/__tests__/drift-check-scripts.test.ts scaffoldLedgerCase -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts KNOWN_THRESHOLD_SITES -->
<!-- verify-symbol: src/__tests__/gate-family-meta.test.ts findThresholdSites -->
<!-- verify-symbol: src/__tests__/restore-test-from-baseline.test.ts liveCount -->

Binding rules for every task below:
- ⛔ Never `git checkout --` anywhere in this plan. Every neuter is done on a `cp` of the file into the scratchpad (`/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/*/scratchpad`) or on a `cp`-backed working file whose pristine bytes were copied FIRST; restore is `cp` from that pristine copy, verified by grepping for the newest edit, not by hash alone.
- ⛔ Every number in a comment is a DATED READING regenerated by a named command (`bash scripts/test-ledger-drift-check.sh --self-test`, `grep -v '^\s*#' scripts/test-ledger-drift-check.sh | grep -c '^  run_arm "'`). Never restate a count from prose.
- ⛔ No database, no `supabase` CLI, no workflow dispatch: the whole self-test is stub-driven (`LEDGER_QUERY_CMD`, `BODY_FETCH_CMD`).
- Line numbers quoted in CONTEXT/RESEARCH/PATTERNS are dated readings at `9563705e`; re-resolve every site by the symbols named here before editing.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: WR-03 — the arms ratchet, end-to-end (script constant → count check → vitest pin), observed RED on a deleted arm</name>
  <files>scripts/test-ledger-drift-check.sh, src/__tests__/drift-check-scripts.test.ts</files>
  <action>
Conform the ledger self-test to its sibling's shape (CONTEXT Area 3, LOCKED: separate constants per file, no shared helper; the sibling's own `run_arm` comment says it was COPIED from this script, so the twins stay twins by copy).

**In `scripts/test-ledger-drift-check.sh`, inside `self_test()`:**
1. Add `EXPECTED_ARMS=<n>` as a live line immediately below the `local -a results=()` declaration, with the sibling's dated justification on the line above it: a comment beginning `# MEASURED <today's date> —` stating what `--self-test` prints and exits. `<n>` is the regenerated arm count AFTER this task's calibration arm is added (Task 2 raises it again — leave a comment saying the constant moves only together with the arm that adds one, never lower). Add a second comment line naming the sibling: the constant is a deliberate COPY of `EXPECTED_ARMS` in `scripts/restore-test-from-baseline.sh`, self-contained by the repo's wiring convention (Phase 164.8 Plan 05 Task 2), so nobody "fixes" it into a shared module.
2. In `run_arm`, widen the inversion condition to `if [ "$inverted" = "--expect-inverted" ] || [ "${ARM_FORCE_INVERT:-0}" = "1" ]; then` — the one `||` clause the sibling has and this file lacks, needed by the calibration arm.
3. Add the harness-calibration arm, copied from the sibling's `arm_harness_calibration` and adapted: both halves run `run_arm` in a SUBSHELL (`total=0; pass=0; results=(); ARM_FORCE_INVERT=…; run_arm "calib" 0 <a leg known to exit 1>; printf '%s\n' "${results[@]}"`) against a leg that is KNOWN to exit 1 — reuse the `missing-ledger-row RED` invocation, i.e. `arm_env "$tmp/live" "20260829000000_selftest"`, wrapped in a tiny leg function so it can be named. Flip ON must yield `  ok   calib`; flip OFF must yield `  FAIL calib`; either miss prints a `MEASURE_FAIL:` line saying `run_arm` has stopped discriminating and returns 1. Register it last: `run_arm "harness calibration: run_arm can report FAIL, and the flip inverts" 0 arm_harness_calibration`. Say in its comment that `drift-check-scripts.test.ts` already drives `--expect-inverted` externally (whole-run level) and this arm is the belt-and-braces proof that `run_arm`'s FAIL branch is reachable — not load-bearing for WR-03, but the shape CONTEXT names.
4. Replace the closing tally with the sibling's ordering — count check FIRST, pass check second, success line LAST with the constant as denominator:
   - `if [ "$total" -ne "$EXPECTED_ARMS" ]; then echo "SELF-TEST FAIL: ${total} arms ran but EXPECTED_ARMS is ${EXPECTED_ARMS}. An arm that disappeared is a RED, not a smaller PASSED."; return 1; fi`
   - keep the existing `pass -ne total` check
   - success line becomes `self-test OK (${pass}/${EXPECTED_ARMS} arms — …)` with a comment (copy the sibling's A4 wording) explaining that below both checks `pass`, `total` and `EXPECTED_ARMS` are equal by construction, so the denominator is the ratchet and says so.

**Falsifier — OBSERVED RED, recorded in the SUMMARY verbatim (SC-6):**
- `cp scripts/test-ledger-drift-check.sh "$SCRATCH/ledger.pristine.sh"` FIRST.
- Neuter A on a scratch COPY (`"$SCRATCH/ledger.neuter-a.sh"`): delete the whole `run_arm "tip-equal-missing RED" …` line. Run `bash "$SCRATCH/ledger.neuter-a.sh" --self-test`; it MUST print `SELF-TEST FAIL: <n-1> arms ran but EXPECTED_ARMS is <n>` and exit 1. For contrast run the SAME neuter against a copy of the PRISTINE (pre-task) bytes if you still have them (`git show HEAD:scripts/test-ledger-drift-check.sh > "$SCRATCH/ledger.head.sh"` is a READ, allowed): it prints `self-test OK (7/7 …)` and exits 0 — that is the defect, observed.
- Neuter B: on another scratch copy set `EXPECTED_ARMS` one LOWER than the real count → must exit 1 on the count check (proves `-ne`, not `-lt`).
- Confirm the working file is untouched by the neuters: `cmp scripts/test-ledger-drift-check.sh "$SCRATCH/ledger.pristine.sh"` is NOT the check (the working file has your edits) — instead `grep -c 'EXPECTED_ARMS' scripts/test-ledger-drift-check.sh` shows the expected count and `bash scripts/test-ledger-drift-check.sh --self-test` exits 0.

**In `src/__tests__/drift-check-scripts.test.ts`** (the VAC-08 describe that already pins arm labels by name): add the calibration arm's label to the `toContain` list, and add a new `it` block copied by hand from `restore-test-from-baseline.test.ts`'s `EXPECTED_ARMS=26 is a live line, exactly once, with its MEASURED date beside it` block, re-targeted at `LEDGER_GATE`: (a) exactly one live `EXPECTED_ARMS=<n>` line inside the `self_test()` region (write a small region slicer local to this file — from `self_test() {` to the closing `}` before the `case "${1:-}"` dispatcher — copied, not imported); (b) a `MEASURED` token and a `20\d\d-\d\d-\d\d` date on that line or the line above; (c) the `if [ "$total" -ne "$EXPECTED_ARMS" ]; then` line live exactly once; (d) the success line uses `${pass}/${EXPECTED_ARMS}` exactly once; (e) three calibrations on mutated strings — moved (n→n+1 breaks the exact-line pin), commented-out (0 live), doubled (2 live) — each preceded by `expect(mutant).not.toBe(SRC)`. Put a header comment on the block naming its twin in `restore-test-from-baseline.test.ts` and saying the copy is deliberate. ⚠️ `EXPECTED_ARMS` is OUTSIDE gate-family-meta's name class (no FLOOR/MIN/CEILING/MAX/LIMIT), so this pin is the ONLY thing keeping its date honest — say so in the comment.
  </action>
  <verify>
    <automated>bash scripts/test-ledger-drift-check.sh --self-test && ! bash scripts/test-ledger-drift-check.sh --self-test --expect-inverted && npx vitest run src/__tests__/drift-check-scripts.test.ts</automated>
    <fails_when>the plain run exits non-zero or prints a `SELF-TEST FAIL:` line (count check or pass check tripped), or its success line still reads `(N/N arms` with a bare `${total}` denominator instead of `${EXPECTED_ARMS}`; the `--expect-inverted` run exits 0 / prints `self-test OK` (the `!` then fails the chain — the inversion no longer discriminates); or vitest exits non-zero, prints `failed` in its summary, or reports 0 tests in drift-check-scripts.test.ts (a filter matching nothing is not a pass)</fails_when>
  </verify>
  <done>`--self-test` exits 0 and its success line's denominator is `${EXPECTED_ARMS}`; `--expect-inverted` exits 1; the scratch neuter with one `run_arm` line deleted printed `SELF-TEST FAIL: … arms ran but EXPECTED_ARMS is …` and exited 1 (pasted into the SUMMARY); the drift-check vitest file passes with the new EXPECTED_ARMS pin block and its three calibrations.</done>
</task>

<task type="auto">
  <name>Task 2: WR-02 — FRONTIER_EXEMPT_CEILING: a loud count ceiling with two self-test arms, observed RED</name>
  <files>scripts/test-ledger-drift-check.sh, TODOS.md</files>
  <action>
Implement CONTEXT Area 1 (LOCKED: COUNT ceiling, `exit 1` naming the exempted versions, ratchet direction downward). Conform to `WAIVED_CEILING` in `scripts/mutation-runner/run.mjs` (compare, name the excess, name the constant, forbid raising it as a side effect).

**The constant** — a bare top-level line `FRONTIER_EXEMPT_CEILING=3`, placed with the other top-level seams directly under `GATE=…` in `scripts/test-ledger-drift-check.sh`. NOT `"${FRONTIER_EXEMPT_CEILING:-3}"` (the review's sketch; rejected per Contradiction C2 — `findThresholdSites` needs bare digits after `=`, and an env-overridable ceiling hands CI a knob that raises the ceiling without a reviewed edit, the opposite of `WAIVED_CEILING`) and NOT `local` (the scanner's regex anchors at line start). Above it, within 80 lines, a comment block carrying the literal token `MEASURED` and today's date, saying: what the ceiling bounds (the apply-on-merge window: migrations authored above the TEST ledger's tip that cannot be present yet); why 3 (one above the tip is a timing artefact, a handful is a stalled `apply-test` — CONTEXT); that it is a RATCHET lowered when reality allows and never raised to make a run pass; how to regenerate the live reading (the `ledger frontier: tip=…; N above-tip migration(s) exempted.` line every `sql-tests` run prints); that the stale-HIGH direction cannot be re-derived from the repo because the exempt count is a property of a LIVE ledger, so the second layer is the SC-9 registration + the vitest pins in Task 3, NOT an exact re-derivation (say this plainly — it is the honest statement of what the two-layer rule can and cannot do here); and that it conforms to `WAIVED_CEILING` in `scripts/mutation-runner/run.mjs`.

**[ASSUMED A2] — observe before pinning.** Before writing `3`, try to read the latest real value: `gh run list --workflow ci.yml --branch main --limit 3 --json databaseId,conclusion` then `gh run view <id> --log 2>/dev/null | grep -a 'ledger frontier:'` (⚠️ `gh run view --log` truncates; if the line is absent, try `--job` on the `sql-tests` job id from `gh run view <id> --json jobs`). This is a READ; no dispatch, no approval. If the printed N is > 3, pin the ceiling AT that N (never at a number that reds the next run for a pre-existing condition) and record the reading; if the line cannot be obtained, pin 3, record `A2 UNOBSERVED locally` in the SUMMARY, and note that this PR's own `sql-tests` run is the observation. Either way the MEASURED comment states which happened.

**The check** — in `check()`, as the FIRST `if` after `local bad=0` (so it reports alongside `stale_count`/`new_count` and the file's own `if [ "$bad" = 1 ]; then return 1; fi` verdict decides exit — conform to the neighbours, no bare `exit 1`):
- `if [ "$exempt_count" -gt "$FRONTIER_EXEMPT_CEILING" ]; then` emit `echo "::error::${GATE}: FRONTIER_EXEMPT_CEILING exceeded: ${exempt_count} migration(s) above the ledger frontier (tip ${frontier_tip}) > ceiling ${FRONTIER_EXEMPT_CEILING}. That is a stalled apply, not an apply-on-merge window — supabase-migrate.yml's apply-test has stopped applying to TEST. Fix the apply; do NOT raise the ceiling (a reviewed edit to FRONTIER_EXEMPT_CEILING in scripts/test-ledger-drift-check.sh, never a side effect of a green run)."`, then `sed 's/^/::error::  exempt (above tip): /' "$exempt_file"` so every exempted version is NAMED, then `bad=1`.
- Every emission interpolates a runtime value (`${exempt_count}`, `${frontier_tip}`, `${FRONTIER_EXEMPT_CEILING}`) — gate-family-meta's SC-7 diagnostic-first rule. Reuse `exempt_count` as computed; do NOT touch the `grep -ac … || true` line that computes it (surgical rule — SP-M01 conversion of that line is not this phase's scope; note it in the SUMMARY as observed, not fixed).
- Update the existing comment near `is booked as [164.8-PUSH-RACE-VAC08]` in the same function to say the window's WIDTH is now bounded by `FRONTIER_EXEMPT_CEILING` (this phase) while the ORDERING coupling remains booked there.

**Two self-test arms** (registered BEFORE the calibration arm so it stays last; raise `EXPECTED_ARMS` by 2 and update its MEASURED line):
- Corpora: `mig_ceil_over` = one applied file (`20260201000000_applied.sql`) + FOUR above-tip files (`20260301…`, `20260401…`, `20260501…`, `20260601…`, all `_above*.sql`); `mig_ceil_edge` = one applied + THREE above. ⚠️ Two corpora are REQUIRED, not one: the tip is the greatest PRESENT timestamp, so making one of four above-tip files "present" in a single corpus would move the tip up and turn the other three into BELOW-tip absences — RED for the wrong reason. Write this trap into the corpus comment.
- `frontier-ceiling-exceeded RED`: a leg function that captures the run's output — `out="$(arm_env "$tmp/live" "<the four above names, newline-separated>" "selftest_fn" "$tmp/mig_ceil_over" 2>&1)" || rc=$?` — and returns 1 ONLY when rc is 1 AND `printf '%s' "$out" | grep -aq 'FRONTIER_EXEMPT_CEILING exceeded'` AND every one of the four names appears in the output; otherwise returns 0 (so `run_arm … 1` reports FAIL). This is the Pitfall-2 guard: exit 1 alone could be the absurdity floor or any other red. (Read the floor block: the stub prints nothing for `ledger_rows`, so `ledger_rows=""` and the floor is skipped — A3 is settled by reading, and the text assertion makes it moot anyway.)
- `frontier-ceiling-boundary GREEN`: `run_arm "frontier-ceiling-boundary GREEN" 0 arm_env "$tmp/live" "<the three above names>" "selftest_fn" "$tmp/mig_ceil_edge"` — exactly the ceiling stays green (the `tip-equal-missing RED` spirit: the boundary is where an off-by-one hides).
- `MISSING_NAMES` accepts a multi-line value (the stub uses `printf '%s\n'`), so pass the names joined with real newlines via `$'\n'`.

**Falsifier — OBSERVED RED (SC-6):** on a scratch copy, change the comparison to `-gt 999` (or delete `bad=1`); `--self-test` must print `FAIL frontier-ceiling-exceeded RED (exit 0, expected 1)` and exit 1. Then on another scratch copy raise the ceiling to 4: the RED arm FAILs (exempt 4 is no longer over) — proves the arm reads the constant, not a literal. Restore nothing with git; the working file was never neutered. Paste both observations into the SUMMARY.

**TODOS.md:** append ONE dated line under the `[164.8-PUSH-RACE-VAC08]` entry: the exempt window is now capped at `FRONTIER_EXEMPT_CEILING` (read it by symbol from the script) as of Phase 164.8.2; the ordering coupling stays open and routed to 164.9. Do not close the entry; do not add a new one.
  </action>
  <verify>
    <automated>bash scripts/test-ledger-drift-check.sh --self-test | grep -a 'frontier-ceiling-exceeded RED' | grep -aq '^  ok' && bash scripts/test-ledger-drift-check.sh --self-test | grep -a 'frontier-ceiling-boundary GREEN' | grep -aq '^  ok' && ! bash scripts/test-ledger-drift-check.sh --self-test --expect-inverted && grep -v '^\s*#' scripts/test-ledger-drift-check.sh | grep -Ec '^FRONTIER_EXEMPT_CEILING=[0-9]+$' | grep -qx 1</automated>
    <fails_when>`frontier-ceiling-exceeded RED` or `frontier-ceiling-boundary GREEN` is absent from the self-test output or its line is prefixed `  FAIL` rather than `  ok` (the `grep -aq '^  ok'` exits 1); the `--expect-inverted` run exits 0; or the live `FRONTIER_EXEMPT_CEILING=<digits>` count is anything but 1 — 0 means the constant is missing, commented out, or spelled `${…:-3}`/`local`, 2 means it was doubled</fails_when>
  </verify>
  <done>`--self-test` reports both ceiling arms `ok` and exits 0 with EXPECTED_ARMS raised by 2; `--expect-inverted` exits 1; exactly one live `FRONTIER_EXEMPT_CEILING=<digits>` line exists at top level with `MEASURED` + a date within 80 lines above; the scratch neuters (`-gt 999`, ceiling raised to 4) each produced `FAIL frontier-ceiling-exceeded RED` and are pasted in the SUMMARY; the A2 observation (or its absence) is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: The vitest second layer — SC-9 registration, ceiling pins, and driven RED/GREEN arms in drift-check-scripts.test.ts</name>
  <files>src/__tests__/gate-family-meta.test.ts, src/__tests__/drift-check-scripts.test.ts</files>
  <action>
**gate-family-meta.test.ts:** run `npx vitest run src/__tests__/gate-family-meta.test.ts` FIRST and read the failure it now reports (a threshold site not in `KNOWN_THRESHOLD_SITES`). Add exactly one entry beside the existing ledger-script entry, in the file's key format (`"scripts/test-ledger-drift-check.sh :: FRONTIER_EXEMPT_CEILING=<n>"`), with a trailing comment in the neighbour's style naming the token and date (`'MEASURED' + <date>`). ⚠️ The entry's `<n>` must equal the constant — if Task 2 pinned a value other than 3 per A2, use that. If the run also flags the new `::error::` block under the diagnostic-first scan, the fix is to make the emission carry a runtime quantity (it already interpolates `${exempt_count}`) — never to add an allowlist entry for a bare error. Re-run until green; the set is exact in both directions, so a stale entry also reds.

**drift-check-scripts.test.ts — three additions inside the VAC-08 describe:**
1. Labels: add `frontier-ceiling-exceeded RED` and `frontier-ceiling-boundary GREEN` to the label `toContain` list (the existing comment says labels are pinned BY NAME; extend its sentence to name this phase's two).
2. Constant pins (copied idiom from `mutation-runner-floors.test.ts`'s "is a positive integer — a floor of 0 could never fire" and from Task 1's EXPECTED_ARMS block): read the script with `readFileSync` (never shell grep — this repo has a NUL-blind tracked file), assert exactly ONE live line matching `^FRONTIER_EXEMPT_CEILING=\d+$`, that its value is a positive integer (a ceiling of 0 would disable the exemption and re-open `[164.8-PUSH-RACE-VAC08]`'s consequence), and that the comparison line `if [ "$exempt_count" -gt "$FRONTIER_EXEMPT_CEILING" ]; then` is live exactly once. Calibrate each with a mutated string (`expect(mutant).not.toBe(SRC)` first). In the block's comment state what this layer does NOT cover: stale-HIGH relative to the live ledger (impossible without a database; the human ratchet + the dated MEASURED line is the record).
3. Driven arms through the gate's seams (the file's RED/GREEN/CONTROL triple shape): use `withTempDir` + `scaffoldLedgerCase`, but write your OWN `MIGRATIONS_DIR` corpus (the scaffold writes one migration; copy the `mig_ceil_over` / `mig_ceil_edge` shapes from Task 2) and pass `missing:` = the above-tip names. RED: `status` 1, output contains `FRONTIER_EXEMPT_CEILING exceeded`, and EACH exempted version name. GREEN at the boundary: `status` 0, output contains `3 above-tip migration(s) exempted` (or the pinned N) — the count stays VISIBLE, a ratchet that hides the carried gap is a mute button. Keep `LEDGER_BASELINE_FILE` empty as the scaffold does.

Run the per-wave set: `npx vitest run src/__tests__/drift-check-scripts.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/restore-test-from-baseline.test.ts` (the last one must be UNAFFECTED — its `EXPECTED_ARMS=26` pin is the sibling's and this plan never touches that script).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/drift-check-scripts.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/restore-test-from-baseline.test.ts</automated>
    <fails_when>non-zero exit or `failed` in the vitest summary — in particular gate-family-meta reporting a threshold site not in `KNOWN_THRESHOLD_SITES` (registration missing) or a `KNOWN_THRESHOLD_SITES` entry with no live site (stale/mistyped value), drift-check-scripts failing its label, single-live-line or positive-integer pins or its driven RED arm not finding `FRONTIER_EXEMPT_CEILING exceeded` plus every exempted name, or restore-test-from-baseline.test.ts moving at all (this plan never touches that script; any red there is collateral)</fails_when>
  </verify>
  <done>All three vitest files pass; `KNOWN_THRESHOLD_SITES` carries exactly one new ledger-script entry whose value equals the live constant; drift-check-scripts.test.ts pins the new labels, the ceiling constant (single live line, positive, comparison present) with calibrations, and drives RED (names every exempted version) and GREEN (count visible) through the stub seams without a database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TEST ledger → gate verdict | a LIVE, shared database's row set decides pass/fail of a repo gate; the gate must not be silenced by the pipeline's own timing |
| CI env → gate constants | an env-overridable ceiling would let a runner raise the tolerance without a reviewed diff |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.2-01 | Repudiation | frontier exemption (unbounded window) | high | mitigate | `FRONTIER_EXEMPT_CEILING` + `::error::` naming every exempted version; RED arm asserts the message text |
| T-164.8.2-02 | Repudiation | self-test tally shrinks silently | high | mitigate | `EXPECTED_ARMS` count check before pass check; vitest single-live-line + calibration pins |
| T-164.8.2-03 | Elevation of Privilege (config) | env-overridable ceiling | medium | mitigate | bare top-level constant; no `${…:-N}`; SC-9 registration makes any change a reviewed diff |
| T-164.8.2-04 | Information Disclosure | self-test / arm output | low | accept | stub-driven; no DSN or ref is ever read (`TEST_SUPABASE_DB_URL` is the scaffold's literal `stub-dsn-never-used`) |
| T-164.8.2-SC | Tampering | npm/pip/cargo installs | low | accept | no package is installed by this plan (RESEARCH: Package Legitimacy Audit not applicable) |
</threat_model>

<verification>
- `bash scripts/test-ledger-drift-check.sh --self-test` exits 0; the tally line's denominator is the constant.
- `bash scripts/test-ledger-drift-check.sh --self-test --expect-inverted` exits 1.
- Scratch-copy neuters (arm deleted; ceiling `-gt 999`; ceiling raised by one) each observed RED and pasted into the SUMMARY with the exact line printed.
- `npx vitest run src/__tests__/drift-check-scripts.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/restore-test-from-baseline.test.ts` green.
- `node scripts/verify-plan-anchors.mjs <this plan's path>` exits 0 (run at task start, per that script's execute-time seam).
</verification>

<success_criteria>
- WR-03 closed: a vanished arm is RED, and the vitest pin keeps the constant single, live and dated.
- WR-02 closed: the exemption is bounded by a measured, registered, ratcheting ceiling whose breach names every version.
- SC-6 honoured: every falsifier's RED observation is quoted in the SUMMARY; no `git checkout --` was used.
- The `2>/dev/null` and `|| true` sites this plan did NOT touch are named in the SUMMARY as observed-not-fixed (surgical rule).
</success_criteria>

<output>
Create `.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-01-SUMMARY.md` when done — carrying the verbatim RED observations, the regenerated arm count, and the A2 reading (or its recorded absence).
</output>
