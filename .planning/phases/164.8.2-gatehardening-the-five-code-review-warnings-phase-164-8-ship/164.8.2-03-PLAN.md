---
phase: 164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/test-restore-from-baseline.yml
  - src/__tests__/test-restore-workflow-wiring.test.ts
  - scripts/restore-test-from-baseline.sh
autonomous: true
requirements: [WR-05, WR-04, IN-07, SC-6]
tags: [ci-gates, github-actions, public-artifact, information-disclosure]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low
  # plan-checker 2026-09-09: 110000 is 1.10x the 100000 smart-zone budget (advisory). Task (3) and
  # file (3) counts are inside target and argue against a split; proceed and record the ACTUAL in the SUMMARY.

must_haves:
  truths:
    - "The world-readable `test-restore-backup-*` artifact carries ONLY an enumerated set: `ledger.csv`, `schema-before.sql`, `README.txt`, the four script-written `.sql` files (`census.sql`, `survivors.sql`, `restore.sql`, `refdata.sql`) and the redacted `*.err`/`*.log`/`*.out` channels. `pre-census.txt`, `post-census.txt`, both `*-rollback-view.txt` and `survivors.keys` never enter it — default-OUT for every future file the script writes (WR-05, CONTEXT Area 2 as AMENDED by the founder 2026-09-09)."
    - "An EXECUTED vitest arm seeds a fixture directory with every real filename the backup step and the script write, runs the new staging step's shell, and asserts the staged set equals the allowlist exactly; a 19th unexpected file is excluded; a forced failure inside the step exits 1 and leaves the staging directory absent or empty (fail-closed, so `if-no-files-found: error` reddens the upload)."
    - "Every comment that described the old artifact shape now describes the new one: the redaction step's ⚠️ SCOPE paragraph, the outdir comment above the backup step, `README.txt`'s WHAT IS HERE list, and the T-164.8-21 paragraph in the script header (the four sites RESEARCH names)."
    - "The two bare `grep -q` lines in the post-verify step carry `-a`, ending the contradiction with the rule stated three lines above them (WR-04 remainder, Contradiction C1)."
    - "The confirm-token `sed` reads the SAME provenance row as `RECORDED_SHA_RE` and the script's own `sed` — proven by a three-way fixture on an indented row (IN-07)."
  artifacts:
    - ".github/workflows/test-restore-from-baseline.yml — new `run:` step `Stage the public artifact (enumerated allowlist; default-out)` between Redact and Upload, `if: always()`, `trap … ERR` fail-closed; upload `path:` pointed at the staging dir; four comment/README rewrites; two `grep -aq`; unanchored confirm-token sed."
    - "src/__tests__/test-restore-workflow-wiring.test.ts — executed staging arm with three calibrations; ordering pin Redact < Stage < Upload; README-vs-allowlist agreement pin; IN-07 three-way fixture."
    - "scripts/restore-test-from-baseline.sh — one-paragraph correction in the header's T-164.8-21 text (comment only)."
  key_links:
    - "Narrowing happens at a NEW step + the upload `path:`, never in the redaction loops — the two-glob-loop pin forbids widening them (Contradiction C5) and the upload's `if: always()` / `retention-days: 90` / `if-no-files-found: error` lines are byte-pinned and must survive."
    - "`survivors.sql` STAYS (founder amendment — the DDL lives in the file that needs it); `pre-census.txt` (its source) LEAVES. `restore.sql` also embeds `survivors.sql` by `cat` — it stays under the locked '.sql files' wording as the transaction psql actually ran; the SCOPE comment says so explicitly instead of claiming the DDL appears once."
---

<objective>
Close WR-05 as the founder decided it: NARROW the public artifact to an enumerated allowlist rather than widen redaction or re-scan (CONTEXT Area 2, LOCKED and AMENDED). Ten of the eighteen files that reach `${RUNNER_TEMP}/test-backup` today pass through neither the secret scan (which runs BEFORE the script writes anything) nor the redaction (`*.err/*.log/*.out` only) — regenerate that list from the writers, do not carry the review's "seven". `pre-census.txt` carries reconstructed `CREATE POLICY … USING (<qual>)` and owner-named rows read off live shared TEST. Also close the WR-04 remainder in this file and IN-07.

Purpose: a world-readable artifact on a PUBLIC repo whose step comment claims a scan scope the code does not have. The comment cost Phase 164.8 eight hours; it changes WITH the code here.

Output: a staging step, a retargeted upload, four truthful comments, an executed falsifier, two `-a` flags, one unanchored sed with a three-way proof.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-CONTEXT.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-RESEARCH.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-PATTERNS.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-REVIEW.md
@.github/workflows/test-restore-from-baseline.yml
@src/__tests__/test-restore-workflow-wiring.test.ts
@scripts/restore-test-from-baseline.sh
@scripts/check-baseline-staleness.mjs

<!-- verify-symbol: .github/workflows/test-restore-from-baseline.yml withhold_channels -->
<!-- verify-symbol: .github/workflows/test-restore-from-baseline.yml scan_for_secrets -->
<!-- verify-symbol: .github/workflows/test-restore-from-baseline.yml BACKUP_README -->
<!-- verify-symbol: .github/workflows/test-restore-from-baseline.yml RESTORE_OUT_DIR -->
<!-- verify-symbol: src/__tests__/test-restore-workflow-wiring.test.ts extractRunScript -->
<!-- verify-symbol: src/__tests__/test-restore-workflow-wiring.test.ts stepIndex -->
<!-- verify-symbol: src/__tests__/test-restore-workflow-wiring.test.ts stepBody -->
<!-- verify-symbol: src/__tests__/test-restore-workflow-wiring.test.ts calibrate -->
<!-- verify-symbol: scripts/restore-test-from-baseline.sh split_survivors -->
<!-- verify-symbol: scripts/restore-test-from-baseline.sh census_rollback_view -->
<!-- verify-symbol: scripts/check-baseline-staleness.mjs RECORDED_SHA_RE -->

**Contradiction C4 — resolved: CONTEXT wins.** The review's "secret-scan the whole directory before upload" YAML is NOT lifted into this plan; policy DDL is not a secret in the scanner's sense, so a scan reports green and leaves the leak.

**Contradiction C5 — resolved: narrow at a new step + `path:`.** The redaction step's two channel-glob loops are pinned at exactly `*.err *.log *.out` and must not widen; the upload's three pinned lines must survive; `path:` is unpinned and moves.

**Mechanism (RESEARCH Option B, chosen over A and C):** a staging directory. A single `path:` on a `uses:` step cannot be executed by a test; a positive-globs `path: |` is an allowlist the upload trusts by READING; deleting non-allowlisted files in place is destructive-by-enumeration in the other direction. A `run:` step that COPIES the enumerated names into a fresh directory is executable by `extractRunScript` + the file's fixture-dir idiom, keeps the original directory intact for the job log, and makes fail-closed simple: on any error the staging dir is removed and the upload's `if-no-files-found: error` reddens.

**The staged set (from the founder's amendment + the locked wording, not from the review):** `ledger.csv`, `schema-before.sql`, `README.txt` (backup step); `census.sql`, `survivors.sql`, `restore.sql`, `refdata.sql` (script — "the `.sql` files", each NAMED, never `*.sql`, so a future `.sql` is out by default); `*.err`, `*.log`, `*.out` (channels, after redaction). NOT staged: `pre-census.txt`, `post-census.txt`, `pre-census.rollback-view.txt`, `post-census.rollback-view.txt` (the founder's explicit leave-list), and `survivors.keys` (script-written, not `.sql`, not a channel — "everything else the script writes does not enter"). Regenerate the writer inventory with `grep -oE 'RESTORE_OUT_DIR/[A-Za-z0-9_.-]+' scripts/restore-test-from-baseline.sh | sort -u` and the backup-step `outdir` writes before pinning the fixture list; if the inventory differs from these 18 names, the FIXTURE follows the inventory and the SUMMARY says so.

⚠️ `restore.sql` embeds `survivors.sql` (`cat "$RESTORE_OUT_DIR/survivors.sql" >> "$out"` in `build_transaction`), so the survivor DDL is present TWICE among the staged `.sql` files. That is within the locked wording ("the `.sql` files") and the founder's leave-list, and `restore.sql` has its own claim — it is the transaction psql actually ran, which an aborted-restore post-mortem needs. The SCOPE comment must state this rather than claim "exactly once". Flagged to the orchestrator in the planner's report; not re-opened here.

Binding rules: never `git checkout --`; no database, no dispatch; the repo is PUBLIC — no ref, DSN, or the marker's full text in any fixture or comment (use `db.exampleprojref.supabase.co`-style placeholders as the file already does).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The staging step, the retargeted upload, and the EXECUTED allowlist arm — end-to-end</name>
  <files>.github/workflows/test-restore-from-baseline.yml, src/__tests__/test-restore-workflow-wiring.test.ts</files>
  <action>
**Workflow — new step**, placed AFTER `Redact connection metadata from the backup directory (public artifact)` and BEFORE `Upload the pre-restore backup (schema + ledger; NOT data)`, named exactly `Stage the public artifact (enumerated allowlist; default-out)`, `if: always()`, `run: |` with `set -euo pipefail`:
- `outdir="${RUNNER_TEMP}/test-backup"`, `stage="${RUNNER_TEMP}/test-backup-artifact"`.
- `trap 'rc=$?; rm -rf "${stage}"; echo "::error::staging FAILED (exit ${rc}) — the staging directory was REMOVED so the upload finds nothing (if-no-files-found: error) rather than publishing an un-enumerated directory. Nothing is uploaded from ${outdir} directly."; exit 1' ERR` — the redaction step's fail-closed discipline, applied to the new boundary.
- If `${outdir}` is absent: print a line saying there is nothing to stage and `exit 1` is NOT wanted here — instead create the stage dir with only a one-line `README.txt` saying the run failed before the backup step (so the upload does not red for the wrong reason; the run is already red). Say in a comment why this branch exists.
- `mkdir -p "${stage}"`; then copy by NAME: `for f in ledger.csv schema-before.sql README.txt census.sql survivors.sql restore.sql refdata.sql; do [ -f "${outdir}/${f}" ] && cp -p "${outdir}/${f}" "${stage}/"; done` — with `[ -f ]` guarded so an aborted run (fewer files) is not an error, but a `cp` failure IS (the trap). Then channels: `for c in "${outdir}"/*.err "${outdir}"/*.log "${outdir}"/*.out; do [ -f "${c}" ] && cp -p "${c}" "${stage}/"; done`. ⚠️ Do NOT use `|| true`, `|| :`, `2>/dev/null` or `exit 0` — Plan 04's 9-token scan will run over this step; use `if [ -f … ]; then cp …; fi` blocks rather than `&&` chains if `set -e` semantics make the guarded form awkward.
- Print a manifest: `ls -1 "${stage}"` prefixed, and a count line `staged N file(s) from ${outdir}; everything else the script wrote stays in the job's temp dir and never enters the artifact.` The count is a runtime value (SC-7).
- A block comment above the step: the allowlist is by NAME (not `*.sql`) so a future file the script writes is OUT by default; why `survivors.sql` stays and `pre-census.txt` leaves (founder decision 2026-09-09: the DDL lives in the file that needs it; the census, the post-census and both rollback views carry the same DDL again with no claim on it; T-164.8-21); that `restore.sql` embeds `survivors.sql` and is kept as the transaction actually run; and that `survivors.keys` is out because it is neither recipe nor channel.

**Upload step:** change only `path:` to `${{ runner.temp }}/test-backup-artifact`. `if: always()`, `retention-days: 90`, `if-no-files-found: error` and the step name stay byte-identical (pinned). Update the comment above it: the artifact is now the STAGED set; the pre-drop census is deliberately NOT in it (it was, before 164.8.2).

**Wiring test — the executed falsifier** (copy the redaction arm's fixture-dir idiom; do not share helpers):
- `const STAGE = "Stage the public artifact (enumerated allowlist; default-out)"`.
- Seed `join(runnerTemp, "test-backup")` with ALL real names from the regenerated inventory (the 18: `ledger.csv ledger.err schema-before.sql dump.log README.txt marker.err census.sql census.err pre-census.txt survivors.keys survivors.sql restore.sql refdata.sql refdata.err transaction.out post-census.txt pre-census.rollback-view.txt post-census.rollback-view.txt`), each with a distinctive marker line; `pre-census.txt`, `survivors.sql`, `restore.sql` and both rollback views get a `CREATE POLICY p ON t USING (owner = current_user)` line. Add a 19th file `future-thing.txt`.
- Run `extractRunScript(WF, STAGE)` via `spawnSync("bash", …)` with `RUNNER_TEMP` injected. Assert exit 0; assert `readdirSync(stage).sort()` EQUALS the expected list exactly (the 7 named + the 8 channel files by their real names — derive the expectation from the seeded list filtered by the allowlist you write in the TEST, then assert the two allowlists agree by construction: the test's list must equal the names parsed from the step's `for f in … ; do` line, else a drift between test and workflow is a green test over the wrong list — calibrate that parse by mutating a name).
- Assert `pre-census.txt`, `post-census.txt`, both rollback views, `survivors.keys` and `future-thing.txt` are NOT in the stage; assert `survivors.sql` IS (the founder's call, pinned so nobody "completes" the narrowing by dropping it).
- CALIBRATION 2 (neuter on a scratch string): replace the enumerated `for f in …` copy with `cp -a "${outdir}/." "${stage}/"`, assert the mutant differs, run it, assert `pre-census.txt` IS present — the arm reds on the old shape. This is the OBSERVED RED for SC-6; paste its output into the SUMMARY.
- CALIBRATION 3 (fail-closed): replace the first `cp -p` with `false -p` (the redaction arm's `false` idiom), run, assert exit 1, `::error::` + `staging FAILED` in output, and the stage dir absent or empty.
- Ordering pin: extend the "runs BEFORE the upload" describe with Redact < Stage < Upload using `stepIndex`, with a swap calibration like the existing one. Also pin `if: always()` on the stage step (it must run on an aborted run — that is the run whose channels matter).
- Do NOT touch the redaction arm's `keepers = ["schema.sql", "ledger.csv"]` fixture (RESEARCH notes `schema.sql` is not a real name; harmless, out of scope — say so in the SUMMARY).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts && grep -v '^\s*#' .github/workflows/test-restore-from-baseline.yml | grep -c 'test-backup-artifact' | awk '{ exit ($1 >= 2) ? 0 : 1 }'</automated>
    <fails_when>vitest exits non-zero or prints `failed` — the executed staging arm's `readdirSync(stage)` set differs from the allowlist (a census/keys/19th file present, or `survivors.sql` absent), the test-side list disagrees with the names parsed from the step's `for f in …` line, the `cp -a` neuter does NOT put `pre-census.txt` in the stage (the arm cannot see the old shape), the `false -p` run exits 0 or leaves files in the stage (not fail-closed), or the Redact<Stage<Upload ordering pin / `if: always()` pin fails; or the live `test-backup-artifact` count is below 2 — the staging step's `stage=` line or the upload's `path:` is missing, so the upload still points at the raw directory</fails_when>
  </verify>
  <done>The stage step exists between Redact and Upload with `if: always()` and a fail-closed trap; the upload `path:` targets the staging dir with its three pinned lines untouched; the executed arm proves the staged set equals the by-name allowlist, excludes the five census/keys files and the 19th file, keeps `survivors.sql`, reds on the `cp -a` neuter (pasted in the SUMMARY) and fails closed on a forced error; the ordering pin covers all three steps.</done>
</task>

<task type="auto">
  <name>Task 2: The four comments that lie change with the code — SCOPE paragraph, outdir comment, README, script header</name>
  <files>.github/workflows/test-restore-from-baseline.yml, scripts/restore-test-from-baseline.sh, src/__tests__/test-restore-workflow-wiring.test.ts</files>
  <action>
1. **Redaction step's ⚠️ SCOPE paragraph** (the comment block directly above `- name: Redact connection metadata …`): rewrite it to say what is TRUE — the redaction removes/scrubs only `*.err/*.log/*.out`; `ledger.csv` and `schema-before.sql` are left byte-exact and were secret-scanned in the BACKUP step at the point they were written; the script-written files are NEVER scanned or redacted here — what keeps them out of the public artifact is the staging step's by-name allowlist (name it), which admits the four script `.sql` files (`census.sql`, `survivors.sql`, `restore.sql` — which embeds `survivors.sql` — and `refdata.sql`) and excludes the census text files and `survivors.keys`. Delete the sentences "They are the only files that can carry connection metadata" and "the two .sql files … are secret-SCANNED at the point they are written" — both are false (there are five `.sql` files in the directory and only one is scanned). Keep the FAIL-CLOSED paragraph above it unchanged.
2. **Outdir comment above the backup step** (`RESTORE_OUT_DIR is pointed at this same directory, so the script's pre-drop census … lands in the artifact as well`): rewrite — the script still writes there so an aborted transaction leaves its files in the job's temp dir, but since Phase 164.8.2 only the staged allowlist reaches the artifact; `survivors.sql` is in it, the census text is not.
3. **`README.txt` WHAT IS HERE list** (inside the `BACKUP_README` heredoc): replace the `pre-census.txt` / `survivors.keys` / `survivors.sql` lines with lines for exactly the staged names: `census.sql` (the catalogue query the script ran), `survivors.sql` (the non-public objects depending on `public` and the DDL that re-creates them — the ONLY place the policy/trigger DDL appears as a standalone file), `restore.sql` (the full transaction as assembled, embeds `survivors.sql` and `refdata.sql`), `refdata.sql`, and the channel files. State that the pre-drop census text files are deliberately withheld from the public artifact (they restate the same DDL with owner-named rows). Keep the rest of the README byte-identical.
4. **Script header, T-164.8-21 paragraph** in `scripts/restore-test-from-baseline.sh` (the sentences around `print survivor KEYS here and leave the DDL in \`survivors.sql\` inside the artifact`): comment-only edit — add one sentence dated 2026-09-09: the ARTIFACT half is now closed by the workflow's staging step (Phase 164.8.2 WR-05: `pre-census.txt` no longer ships, `survivors.sql` does); the STDOUT half (this script still cats the whole pre-census, DDL column included, to the public Actions log) remains booked. Do not touch any live line; run `bash scripts/restore-test-from-baseline.sh --help` to confirm the header still prints and `npx vitest run src/__tests__/restore-test-from-baseline.test.ts` to confirm no pin moved (that file pins live regions and counts, not this paragraph — confirm by running).
5. **Agreement pin** in the wiring test: parse the README heredoc's WHAT IS HERE entries (leading filename tokens) and assert every named file is either in the stage step's by-name list or matches one of its channel globs — derive both from `WF`, so the README cannot describe a file the artifact does not carry. Calibrate by inserting `pre-census.txt` back into a scratch README and asserting the predicate flips.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/restore-test-from-baseline.test.ts && ! grep -v '^\s*#' .github/workflows/test-restore-from-baseline.yml | grep -q 'pre-census.txt    the restore script' && bash scripts/restore-test-from-baseline.sh --help >/dev/null</automated>
    <fails_when>vitest exits non-zero or prints `failed` (the README-vs-allowlist agreement pin found a README entry the artifact does not carry, its `pre-census.txt` re-insertion calibration did not flip, or restore-test-from-baseline.test.ts moved — the header edit touched a live line); the negated grep finds the old `pre-census.txt    the restore script` README line still live (grep -q exits 0, `!` turns it red); or `--help` exits non-zero (the header comment edit broke the script's parse)</fails_when>
  </verify>
  <done>All four sites describe the staged artifact truthfully; the README no longer lists `pre-census.txt`/`survivors.keys` as contents; the agreement pin passes and its calibration flips; the restore script's tests are unaffected and `--help` prints.</done>
</task>

<task type="auto">
  <name>Task 3: WR-04 remainder (`grep -aq` in post-verify) and IN-07 (unanchored confirm-token sed with a three-way fixture)</name>
  <files>.github/workflows/test-restore-from-baseline.yml, src/__tests__/test-restore-workflow-wiring.test.ts</files>
  <action>
**WR-04 remainder** — in `Post-verify with the Supabase CLI — ledger SHAPE`: change `grep -q 'Would push these migrations:'` and `! grep -q 'Remote database is up to date.'` to `grep -aq …` (two lines). The rule text three lines above (`-a` is mandatory repo-wide …) now has no counter-example in its own step. Check the wiring test for byte pins on these two lines by RUNNING it, not by reading; if a pin moves, move it in the same edit with a one-line comment. Falsifier: in the wiring test, extract this step's script, replace the `supabase db push … --dry-run` line with a fixture `cat` (as Plan 02 does for the migrate twin), seed `dry-run.raw` with a NUL byte followed by `Remote database is up to date.`, and assert exit 0 with `-a` (the positive check READ the file); the pre-fix observation (`! grep -q` on a NUL file → on ugrep exit 1 reads as "did not print" → the step exits 1 for the WRONG reason) is recorded in the SUMMARY per grep flavour, not asserted (A1, same policy as Plan 02).

**IN-07** — in `Refuse restore without the confirm token`: drop the leading `^` from the `sed -nE 's/^\|[[:space:]]*sha256…'` program so it reads `s/.*\|[[:space:]]*sha256…` like the script's `sed` and `RECORDED_SHA_RE`; the comment above it ("The SAME provenance row … spelled for sed") becomes true. Three-way fixture in the wiring test: an INDENTED row `  | sha256 | \`<64 hex>\` |` plus a decoy earlier row without sha256; run (a) `RECORDED_SHA_RE` imported from `scripts/check-baseline-staleness.mjs` (a script module, not a test file — importing it keeps the convention), (b) the workflow's sed program extracted from the step via `extractRunScript` and run with `spawnSync("sed", …)` — ⚠️ the local sed is BSD, `sed -nE` with this program is portable, but assert the program string first and skip nothing silently, (c) the script's sed program extracted from `scripts/restore-test-from-baseline.sh` by the `recorded=$(sed -nE …` line; assert all three yield the same 64-hex string. Calibration: re-add `^` to the extracted workflow program on a scratch string and assert it yields EMPTY on the indented row while the other two still match — the pre-fix divergence, observed.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts && grep -v '^\s*#' .github/workflows/test-restore-from-baseline.yml | grep -c "grep -aq 'Would push these migrations:'\|grep -aq 'Remote database is up to date.'" | grep -qx 2</automated>
    <fails_when>vitest exits non-zero or prints `failed` — the NUL `dry-run.raw` fixture makes the extracted post-verify step exit non-zero (the positive check still cannot read past the NUL), a moved byte-pin on either grep line was not moved with it, the three-way sha fixture yields three different strings (or an empty one from the workflow's sed — still anchored), or the re-anchored `^` mutant calibration does NOT yield empty on the indented row (calibration proves nothing); or the live count of `grep -aq` post-verify lines is not exactly 2 — 0/1 means a line still reads bare `grep -q`</fails_when>
  </verify>
  <done>Both post-verify greps carry `-a` (live count 2) and the executed NUL fixture proves the positive check reads the file; the confirm-token sed is unanchored and the three readers agree on an indented row while the re-anchored mutant does not; the wiring test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job temp dir → world-readable artifact | everything under the upload `path:` is published on a PUBLIC repo for 90 days |
| live shared TEST → census text | policy USING expressions, trigger defs and owner-named rows read off a shared database |
| CLI output → post-verify verdict | `supabase db push --dry-run` bytes decide the ledger-shape verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.2-09 | Information Disclosure | census text files in the public artifact (T-164.8-21) | high | mitigate | by-name staging allowlist; census/keys never staged; executed arm + `cp -a` neuter |
| T-164.8.2-10 | Information Disclosure | staging step fails and the raw dir uploads | high | mitigate | `trap … ERR` removes the stage dir; upload `path:` no longer points at the raw dir; `if-no-files-found: error` |
| T-164.8.2-11 | Information Disclosure | survivor DDL in `survivors.sql`/`restore.sql` | medium | accept | founder decision 2026-09-09: reversibility of an aborted restore requires it; stated in SCOPE comment and README |
| T-164.8.2-12 | Tampering | NEGATIVE/positive grep NUL-blind in post-verify | medium | mitigate | `grep -aq` on both lines; executed NUL fixture |
| T-164.8.2-13 | Repudiation | README/comments describing a different artifact | medium | mitigate | four rewrites + README-vs-allowlist agreement pin |
| T-164.8.2-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed |
</threat_model>

<verification>
- `npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/restore-test-from-baseline.test.ts` green.
- The stage step contains none of the 9 softening tokens (Plan 04 will scan it; pre-check with `grep -nF -e '|| true' -e '|| :' -e '2>/dev/null' -e 'exit 0' -e 'set +e' -e 'set +o pipefail' -e '::warning' -e 'continue-on-error' -e '|| exit 0'` over the step body — expect zero live hits).
- SUMMARY carries: the regenerated 18-name inventory, the `cp -a` neuter's RED output, the per-flavour pre-fix grep observations, and the note that `restore.sql` embeds `survivors.sql`.
- `node scripts/verify-plan-anchors.mjs <this plan's path>` exits 0 at task start.
</verification>

<success_criteria>
- WR-05 closed by narrowing, exactly as the founder amended it; every future script file is out by default; every comment that described the old shape now describes the new one.
- WR-04's two restore-workflow lines closed; IN-07 closed with a three-way proof.
- No `git checkout --`, no database, no dispatch; nothing in this plan's fixtures names a real project ref or DSN.
</success_criteria>

<output>
Create `.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-03-SUMMARY.md` when done.
</output>
