# Phase 164.8.2 — review-fix batch C

**Subject:** VAC-08's ledger drift-check — `scripts/test-ledger-drift-check.sh` and
`src/__tests__/drift-check-scripts.test.ts`. No other file was touched.

**Base:** `c95f09fbe5dc743c6e2af5397e05e5090008bd61` (verified at start; matched).
**Commits:** four, one per finding.

| | commit | finding |
|---|---|---|
| 1 | `d4e4a8cf` | F5 (+ IN-03, which it subsumes) |
| 2 | `e74d40df` | F6 |
| 3 | `f0e98b67` | IN-01 |
| 4 | `407cfab1` | IN-02 |

`git diff --stat c95f09fb..HEAD` → 2 files, +454 / −8. Working tree clean.

---

## Ratchets: neither moved

- **`EXPECTED_ARMS` is still `11`.** No self-test arm was added or removed. Re-derived from the
  shipped file rather than restated: `grep -av '^\s*#' … | grep -ac '^  run_arm "'` → **11**, and
  the green-arm derivation the success sentence claims → **3** ("all THREE green paths").
- **`FRONTIER_EXEMPT_CEILING` is still a bare top-level `FRONTIER_EXEMPT_CEILING=3`** at
  `scripts/test-ledger-drift-check.sh:164`. Not `${…:-3}`, not `local`. Read by symbol from the
  shipped file, not from prose.

Nothing was weakened to make anything pass.

---

## The four fixes, each with the RED I observed

### F5 (+ IN-03) — five `|| true` reads, and the pre-fix failure was a FALSE GREEN

The finding named three count reads. Reading the block showed **five** on the same shape: the two
`grep -aFxv` **filter** sites at `:408`/`:410` write `missing.new.txt` and `baseline.stale.txt`, so
an rc-2 filter empties the file one layer *before* any count is taken. Same class, same block, same
verdict — I closed all five rather than three, and said so in the commit.

The finding rated this "likelihood low, no false green". **That second half was wrong, and I have the
runs.** Driving the real gate with a real `grep` PATH shim that really exits 2 for exactly one file
and delegates every other call:

| broken read | pre-fix | post-fix |
|---|---|---|
| `exempt.frontier.txt` | `::error::… FRONTIER_EXEMPT_CEILING exceeded: 4 … > ceiling 3.` exit **1** → `ledger frontier: tip=20260201000000; 0 above-tip migration(s) exempted.` + `::notice::… ledger and body checks clean.` exit **0** | `MEASURE_FAIL: could not count the frontier-exempted migrations (grep exited 2 …)` exit 1 |
| `missing.new.txt` | `::error::… 1 repo migration(s) are not present in the TEST ledger and are NOT baselined:` exit **1** → `0 NEW drift.` + `checks clean` exit **0** | `MEASURE_FAIL: could not count the NEW-drift rows …` exit 1 |
| `baseline.stale.txt` | `::error::… 1 baseline entr(y/ies) are no longer MEASURED absent.` exit **1** → `checks clean` exit **0** | `MEASURE_FAIL: could not count the stale-baseline rows …` exit 1 |
| filter site `:408` | same NEW-drift finding exit **1** → `0 NEW drift.` + `checks clean` exit **0** | `MEASURE_FAIL: could not filter the measured-missing rows against … (grep exited 2)` exit 1 |
| filter site `:410` | same stale finding exit **1** → `checks clean` exit **0** | `MEASURE_FAIL: could not filter … against the measured-missing rows (grep exited 2)` exit 1 |

Every one of the five turned **exit 1 into exit 0 with `ledger and body checks clean`.** Not a lost
diagnostic — a gate reporting a pass it did not earn.

The exempt read is the one that belongs to *this* phase: `exempt_count=0` makes
`-gt "$FRONTIER_EXEMPT_CEILING"` unreachable, so WR-02's brand-new ceiling reports nothing while
measuring nothing.

**Shape:** the sibling's IN-06 wrap verbatim, no second idiom —
`set +e; … ; grep_rc=$?; set -e` then `[ "$grep_rc" -le 1 ] || fail "MEASURE_FAIL: …"`. `-le 1` and
not `-eq 0`, because `grep -v` exits 1 when every line was filtered away and that is legitimate.
IN-06's own claim that "the two now fail the same way" is now true of the twins as well.

**Falsifier, observed RED.** Five new driven arms. Each asserts its CONTROL is red *for its own
reason* first — without that, "the broken run exits 1" would be satisfied by any of the gate's
several other red modes. The stale-filter arm keys its shim on grep's **last** argument, because both
filter lines name `baseline.names.txt` and only one takes it as INPUT; keyed on any argument the
other site fires first and the arm would pass on the wrong message.

Neuter: `cp` the pristine bytes over the script. Proved applied two ways —
`grep -c 'MEASURE_FAIL: could not count the NEW-drift rows'` → **0**, and `git diff --stat` **empty**
(byte-identical to HEAD, i.e. the exact pre-fix state). Result:

```
× F5 RED: … counting the NEW-drift rows …
× F5 RED: … counting the STALE-baseline rows …
× F5 RED: … FILTERING the measured-missing rows against the baseline …
× F5 RED: … FILTERING the baseline against the measured-missing rows …
× F5 RED: … counting the EXEMPTED migrations …
AssertionError: an uncountable NEW-drift count was reported as a clean gate: expected +0 to be 1
AssertionError: an uncountable stale count was reported as a clean gate: expected +0 to be 1
AssertionError: an unreadable filter input was reported as a run with no new drift: expected +0 to be 1
AssertionError: an unreadable baseline was reported as a baseline with no stale entries: expected +0 to be 1
AssertionError: an uncountable exemption disabled the ceiling and passed the gate: expected +0 to be 1
      Tests  5 failed | 507 skipped (512)
```

`expected +0 to be 1` is the false green, stated by the harness. Restored; verified by grepping the
newest fix text (3 × 1 hit) and an empty `git status`.

### F6 — a failing arm now says why, and the exit-code contract is untouched

`run_arm` ran every arm as `( "$@" ) >/dev/null 2>&1`. The finding's reading is right: there was no
false green — `arm_frontier_ceiling_exceeded` is `want 1` and `return 0`s on each of its four
MEASURE_FAIL paths, so the arm genuinely FAILs. What was lost is the reason.

`arm_out="$( ( "$@" ) 2>&1 )" || rc=$?`, re-emitted on the FAIL branch only, withheld when the arm
printed nothing. `rc` still comes from the arm and is still compared against `want`.

**Falsifier, observed RED.** The mutation is the ceiling arm's own naming `sed`, deleted — one line,
`diff` shown before trusting anything:

```
567d566
<     sed 's/^/::error::  exempt (above tip): /' "$exempt_file"
```

Pre-fix and post-fix on that same copy are **identical** in verdict and exit —
`FAIL frontier-ceiling-exceeded RED (exit 0, expected 1)` / `SELF-TEST FAIL: 10/11 arms behaved as
declared.` / exit 1 — and differ by one line:

```
       | MEASURE_FAIL: the ceiling ERROR did not NAME the exempted migration 20260301000000_above1.
```

So the arm binds to the **diagnosis**, not the exit code, and it has to: `expect(status).toBe(1)`
would have passed pre-fix and proved nothing. Reverting `run_arm` to the discard (neuter proved:
capture-needle 0 hits, discard-needle 1 hit):

```
× F6: a FAILING self-test arm re-emits its own MEASURE_FAIL sentence …
AssertionError: the arm failed mutely — its MEASURE_FAIL sentence went to the discarded channel,
which is the whole of F6: expected '  ok   missing-ledger-row RED (exit 1…' to contain
'MEASURE_FAIL: the ceiling ERROR did n…'
      Tests  1 failed | 1 passed | 512 skipped
```

The two contract legs (`status === 1`, `SELF-TEST FAIL: 10/11`) **passed** in that red run — which is
the point.

⚠️ **Honest note on the second F6 leg.** "a PASSING arm stays silent" passed both pre- and post-fix,
so it is a **directional guard, not a falsifier**: it exists so the fix cannot be "print `arm_out`
unconditionally", which would bury a green run under `arm_calib_leg`'s ~20 lines of gate output. It
*can* fail — against that wrong fix. It is not evidence that F6 was fixed.

### IN-01 — the ratchet names the direction it measured

Pre-fix, with a `run_arm` line duplicated: `12 arms ran but EXPECTED_ARMS is 11. An arm that
disappeared is a RED, not a smaller PASSED.` Count right, reader sent looking for a deletion that
never happened. The two directions carry **opposite** remedies — restore vs ratchet — which is why
one sentence could not serve both, and conflating them nudges toward the one edit this file forbids.

Branched on `-lt`/`-gt`. The outer `if [ "$total" -ne "$EXPECTED_ARMS" ]` line and its ordering before
the pass check are unchanged, so the WR-03 pins still bind.

**Falsifier, observed RED.** Both legs **drive** the real harness on a scratch copy — a static read of
the two strings would pass on a script whose `-lt` branch is unreachable. Each asserts its mutation
applied (arm line unique → duplicated gives 2 occurrences / deleted gives 0) before trusting the run.

```
× an ADDED arm is reported as ADDED — with the remedy that fits it
× a VANISHED arm is reported as vanished — and the ratchet is not offered as the fix
AssertionError: an ADDED arm is still described as one that disappeared …: expected … to contain
'An arm was ADDED without raising the …'
AssertionError: expected … to contain 'An arm DISAPPEARED'
      Tests  2 failed | 514 skipped
```

### IN-02 — the summary stops claiming a clean verdict beside its own error

Pre-fix, one run:

```
::error::… FRONTIER_EXEMPT_CEILING exceeded: 4 migration(s) … > ceiling 3.
::error::  exempt (above tip): 20260301000000_above1
  ledger presence: 4 absent — 0 baselined (…), 4 exempt as above the ledger frontier; 0 NEW drift.
```

`new_count` genuinely is 0, so the fix is not to hide the number. The **counts** are a measurement and
stay printed either way; the trailing `0 NEW drift.` is a **verdict** and is now conditioned on `bad`.

**Falsifier, observed RED.** The needle is `0 NEW drift.` **with its terminating period** — the bare
phrase still appears post-fix inside the caveat, so binding to it would make the arm unable to fail.

```
× IN-02 RED: a ceiling breach's summary does not claim a clean verdict beside its own ::error::
AssertionError: the presence summary still ends `0 NEW drift.` while an ::error:: is being raised —
two contradictory sentences in one run: expected 'Repo migrations: 5\n  ledger frontier…' not to
contain '0 NEW drift.'
```

Paired with a CONTROL leg: a genuinely clean run at exactly the ceiling still ends `0 NEW drift.` with
no caveat. Without it the fix could be "always print the caveat", which passes the RED arm while
telling every green run it is not clean.

---

## Verification — and WHERE it ran

⚠️ **Everything below ran inside the harness-provided agent worktree**
`.claude/worktrees/agent-aa891920b9a3f18f4`, **not the
main checkout.** The worktree has no `node_modules` of its own; node resolution walked up to the main
checkout's (`node_modules/vitest`, v4.1.10), so the
toolchain is the repo's real one and the numbers are reproducible from the main checkout once these
commits land — but they were *measured* in the worktree.

```
bash scripts/test-ledger-drift-check.sh --self-test        -> 11/11, exit 0
npx vitest run src/__tests__/drift-check-scripts.test.ts   -> 518 passed (518)
npx tsc --noEmit -p tsconfig.json                          -> exit 0
npx eslint src/__tests__/drift-check-scripts.test.ts       -> exit 0
node scripts/verify-plan-anchors.mjs --pending             -> 3 plans, 17 claims, no stale claims
npx vitest run gate-family-meta / vac08-ledger-dispositions
             / sql-body-normalize / restore-test-from-baseline -> 95 passed (4 files)
```

The last row matters because my edits shift line numbers in a script four other suites read; all four
are green, and `plan-anchor-verify` re-resolved every pending anchor without a miss.

**Not run, and why:** `sql-mutation` and `sql-gate-lint` (they scan `supabase/tests`; no SQL changed,
and `sql-mutation` needs its own PostgreSQL lane), the full vitest suite (three sibling fixers were
running concurrently on other files — a full-suite number taken here would measure their in-flight
edits, not mine), and VAC-08 against live TEST (needs `TEST_SUPABASE_DB_URL`; the gate is exercised
here through its injected stub seam).

## Not completed

Nothing in batch C was left undone. Every finding routed to me — F5, F6, and the three INFO items
(the ratchet direction, the clean-verdict-beside-an-error, and "the ceiling sits on a `grep … || true`",
which is F5 and was closed by it) — is fixed, falsified and committed.

One thing I did **beyond** the brief, flagged so it is a decision and not drift: F5 named three count
reads; I fixed **five**, adding the two `grep -aFxv` filter sites in the same block. They are the same
`|| true` shape, the same rc-1-vs-rc-2 confusion, and they produce the same false green one layer
earlier. Leaving them would have been a point-fix on a class.
