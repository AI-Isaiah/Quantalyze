---
phase: 164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship
plan: 04
type: execute
wave: 2
depends_on: ["164.8.2-02", "164.8.2-03"]
files_modified:
  - src/__tests__/test-restore-workflow-wiring.test.ts
  - src/__tests__/prod-prober-wiring.test.ts
  - src/__tests__/supabase-migrate-test-first.test.ts
autonomous: true
requirements: [WR-06, SC-6]
tags: [wiring-tests, softening-tokens, exact-set-allowlist]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All THREE softening-token lists (`test-restore-workflow-wiring`, `prod-prober-wiring`, `supabase-migrate-test-first`) carry the same nine tokens as separate, commented constants — no shared helper (CONTEXT Area 3, LOCKED; scope AMENDED to the triplet 2026-09-09)."
    - "The destructive workflow's scan tolerates its existing `2>/dev/null` sites ONLY as an exact-set count with a per-site justification (the `gate-family-meta` idiom already used in `restore-test-from-baseline.test.ts`), regenerated through the test's own acquire/release slicing — a NEW `2>/dev/null` in a scannable step is RED (WR-06, SC-5)."
    - "The defect was OBSERVED: a `psql … 2>/dev/null` inserted into the `Which database am I on` step of a scratch copy stayed GREEN under the 5-token scan and goes RED under the fixed one (SC-6)."
    - "Each file pins that its own list has length 9, so the next time one member widens the other two red."
  artifacts:
    - "src/__tests__/test-restore-workflow-wiring.test.ts — 9-token `SOFTENING_TOKENS`; `softeningOffenders` split into forbidden-set + `ALLOWED = { '2>/dev/null': <measured> }` exact count; calibrations."
    - "src/__tests__/prod-prober-wiring.test.ts — 9-token list with sibling comment; one calibration per new token."
    - "src/__tests__/supabase-migrate-test-first.test.ts — comment naming both siblings; length-9 pin (no behaviour change)."
  key_links:
    - "Runs AFTER Plan 03 (wave 2): the new staging step is inside the scannable restore block, and the `2>/dev/null` exact count must be measured on the FINAL workflow, not on a file that is about to gain a step — this is the stated reason for deviating from RESEARCH's WR-06 → WR-05 order."
    - "The migrate twin tolerates ITS six `2>/dev/null` sites because they all sit inside the acquire/release mutex copies that `scannableTestJob` slices out — read that before copying; the restore workflow's five are OUTSIDE the sliced regions, which is why it needs the allowlist and the migrate twin does not."
---

<objective>
Close WR-06 across the whole class: Phase 164.8 found the 5-token softening list was "five spellings of a class", widened it to nine for `supabase-migrate.yml`, and left the DESTRUCTIVE workflow's scan — and the origin copy in `prod-prober-wiring.test.ts` — on five. Bring all three level as separate constants, and arm the restore workflow's tolerated `2>/dev/null` sites as an exact set.

Purpose: the restore workflow is the one that cannot be undone by a re-run; `2>/dev/null` on its marker step would swallow the stderr that IS the evidence the database could not be identified.

Output: three 9-token constants with sibling comments, one exact-set allowlist, calibrations that flip.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-CONTEXT.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-RESEARCH.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-PATTERNS.md
@src/__tests__/test-restore-workflow-wiring.test.ts
@src/__tests__/prod-prober-wiring.test.ts
@src/__tests__/supabase-migrate-test-first.test.ts
@src/__tests__/restore-test-from-baseline.test.ts
@.github/workflows/test-restore-from-baseline.yml
@.github/workflows/prod-prober.yml

Also read Plan 03's SUMMARY (`164.8.2-03-SUMMARY.md` in this phase directory) before starting — it records the staging step's final shape and any token it carries.

<!-- verify-symbol: src/__tests__/test-restore-workflow-wiring.test.ts SOFTENING_TOKENS -->
<!-- verify-symbol: src/__tests__/test-restore-workflow-wiring.test.ts softeningOffenders -->
<!-- verify-symbol: src/__tests__/test-restore-workflow-wiring.test.ts scannableRestoreBlock -->
<!-- verify-symbol: src/__tests__/prod-prober-wiring.test.ts SOFTENING_TOKENS -->
<!-- verify-symbol: src/__tests__/supabase-migrate-test-first.test.ts SOFTENING_TOKENS -->
<!-- verify-symbol: src/__tests__/supabase-migrate-test-first.test.ts scannableTestJob -->
<!-- verify-symbol: src/__tests__/restore-test-from-baseline.test.ts ALLOWED -->

**Contradiction C3 — resolved: the triplet is in scope.** CONTEXT amended the scope on measurement: `prod-prober.yml` carries 0 occurrences of the four extra tokens (regenerate with `grep -cF` before relying on it), so widening its list is free — no allowlist, no triage. Fixing two of three members leaves the class "one half hardened" intact.

Binding rules: no shared helper module; no import between test files; every calibration starts with `expect(mutant).not.toBe(SRC)`; never `git checkout --`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The destructive workflow's scan goes to nine tokens with an exact-set allowlist — observed RED on a `2>/dev/null` insertion first</name>
  <files>src/__tests__/test-restore-workflow-wiring.test.ts</files>
  <action>
**Observe the defect BEFORE editing (SC-6):** in the scratchpad, write a small node script that loads `.github/workflows/test-restore-from-baseline.yml`, inserts ` 2>/dev/null` at the end of a live `psql` line inside the `Which database am I on` step, and runs the CURRENT `softeningOffenders` logic (copy its 5-token list and the acquire/release slicing) — result `[]`, i.e. GREEN on a softened marker step. Paste that into the SUMMARY.

**Then edit `src/__tests__/test-restore-workflow-wiring.test.ts`:**
1. Replace the 5-token `SOFTENING_TOKENS` with the nine (`continue-on-error`, `|| true`, `exit 0`, `::warning`, `set +e`, `|| :`, `2>/dev/null`, `set +o pipefail`, `|| exit 0`), preceded by a doc comment copied by hand from the migrate twin's rationale (the four added were the spellings that fit these workflows; `2>/dev/null` is the shape that would swallow the marker step's stderr) and a sentence naming BOTH siblings (`supabase-migrate-test-first.test.ts`, `prod-prober-wiring.test.ts`): restated rather than imported for the self-containment reason in this file's header; keep the three level by hand.
2. Rework `softeningOffenders` into forbidden-set + exact-set: eight tokens must be ABSENT from the scannable restore block; `2>/dev/null` is checked against `ALLOWED: Record<string, number> = { "2>/dev/null": <n> }` where `<n>` is REGENERATED through this file's own slicing (`liveLines(scannableRestoreBlock(WF)).join("\n").split("2>/dev/null").length - 1`) — print it once with `process.stdout.write` while developing, then pin it. Expected at this reading: 5 (the `git merge-base --is-ancestor` probe whose exit is consumed by `if !`; the three `pg_config`/`ls` probe echoes; the `SELECT count(*)` probe whose rc is captured and asserted) — but the NUMBER comes from the run, and Plan 03's new staging step must contribute ZERO (if it does not, the staging step is the defect, not the allowlist). Above `ALLOWED`, a per-site justification block in the `restore-test-from-baseline.test.ts` style: each site named by its step and why its exit status is CHECKED rather than swallowed, dated `RE-MEASURED 2026-09-09`. A count that moves in EITHER direction is red with the message "a new one has to earn its place in this allowlist with a justification".
3. Keep the two existing calibrations (`continue-on-error` on the run step; `|| exit 0` on the script line). Add: (a) a calibration inserting ` 2>/dev/null` on a live line INSIDE a scannable step (the `Which database am I on` psql line — the exact shape the migrate twin's header names) asserting the offenders list is non-empty and names `2>/dev/null` with count `<n>+1`; (b) one calibration each for `|| :` and `set +o pipefail` inserted after `set -euo pipefail` in the `Run the restore script` step; (c) a length pin `expect(SOFTENING_TOKENS).toHaveLength(9)` with a message naming the two siblings.
4. Re-run the observation from the top of this task against the FIXED logic: the same insertion now reds. Paste both outputs (green-before, red-after) into the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts && node -e 'const s=require("fs").readFileSync("src/__tests__/test-restore-workflow-wiring.test.ts","utf8");const m=s.match(/const SOFTENING_TOKENS = \[([\s\S]*?)\];/);if(!m)process.exit(1);const n=(m[1].match(/"/g)||[]).length/2;process.exit(n===9?0:1)'</automated>
    <fails_when>vitest exits non-zero or prints `failed` — the ` 2>/dev/null` insertion on the marker step's psql line is NOT reported (offenders empty, the scan is still blind to it), the exact-set count differs from the pinned `<n>` in either direction, a `|| :` / `set +o pipefail` calibration is not reported, or the length-9 pin fails; or the node one-liner exits 1 — no `const SOFTENING_TOKENS = [ … ];` block matched (renamed/reshaped constant) or it holds a token count other than 9</fails_when>
  </verify>
  <done>The wiring test scans nine tokens; the `2>/dev/null` sites are an exact-set count regenerated through the test's slicing with a dated per-site justification; the `psql … 2>/dev/null` insertion was observed GREEN before and RED after (both pasted in the SUMMARY); all calibrations flip; the file is green.</done>
</task>

<task type="auto">
  <name>Task 2: The other two members — `prod-prober-wiring` to nine tokens, and the migrate twin's sibling comment + length pin</name>
  <files>src/__tests__/prod-prober-wiring.test.ts, src/__tests__/supabase-migrate-test-first.test.ts</files>
  <action>
1. **`prod-prober-wiring.test.ts`:** regenerate the premise first — `grep -cF -e '|| :' .github/workflows/prod-prober.yml; grep -cF -e '2>/dev/null' …; grep -cF -e 'set +o pipefail' …; grep -cF -e '|| exit 0' …` — each must print 0 (CONTEXT measured 0 on 2026-09-09; if any is non-zero, STOP and report — the "free" amendment premise is false and the site needs the exact-set treatment instead). Then widen `SOFTENING_TOKENS` to the nine with a comment naming both siblings and the self-containment reason (this file is the ORIGIN of the idiom — say so, and that it was the last to widen). Its consumer uses `text.includes(t)` over its scanned region; add one calibration per NEW token inserting the token into a scratch copy of `prod-prober.yml`'s scanned region and asserting the offender is reported (prefix each with `expect(mutant).not.toBe(...)`), plus the length-9 pin.
2. **`supabase-migrate-test-first.test.ts`:** no behaviour change. Extend the existing header comment above its `SOFTENING_TOKENS` ("A superset of the list `test-restore-workflow-wiring.test.ts` scans with") — it is no longer a superset, it is one of THREE equal copies as of Phase 164.8.2; name the other two files and the rule "keep the three level by hand; no shared helper (Phase 164.8 Plan 05 Task 2)". Add the length-9 pin `it` with the same message.
3. Run the three files together and paste the three `SOFTENING_TOKENS` blocks' token counts (regenerated with the verify command's node one-liner adapted per file) into the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/test-restore-workflow-wiring.test.ts && for f in src/__tests__/prod-prober-wiring.test.ts src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/test-restore-workflow-wiring.test.ts; do node -e 'const s=require("fs").readFileSync(process.argv[1],"utf8");const m=s.match(/const SOFTENING_TOKENS = \[([\s\S]*?)\];/);if(!m)process.exit(1);process.exit(((m[1].match(/"/g)||[]).length/2)===9?0:1)' "$f" || exit 1; done</automated>
    <fails_when>vitest exits non-zero or prints `failed` (a prod-prober per-token calibration does not report the inserted token, or a length-9 pin fails), or the loop exits 1 on any of the three files — that file's `SOFTENING_TOKENS` array was not found by the regex or does not hold exactly 9 tokens, i.e. the triplet is not level</fails_when>
  </verify>
  <done>All three files declare nine tokens; `prod-prober-wiring` reports each new token when inserted into its scanned region; the migrate twin's comment names both siblings and its length pin passes; the premise (0 hits of the four extra tokens in `prod-prober.yml`) was regenerated and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow YAML → CI verdict | a softening token in a step turns a failed destructive run into a green one |
| test constant ↔ its two copies | three hand-kept copies can drift; a length pin in each is the tripwire |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.2-14 | Repudiation | `2>/dev/null` on the marker step swallows the identity evidence | high | mitigate | 9-token scan + exact-set count; calibration inserts the exact shape |
| T-164.8.2-15 | Repudiation | one copy of the list widens, the others stay | medium | mitigate | length-9 pin in each of the three files with sibling-naming messages |
| T-164.8.2-16 | Tampering | allowlist widened to admit a new site | medium | mitigate | exact count in BOTH directions with a per-site justification; no `||`/env override |
| T-164.8.2-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed |
</threat_model>

<verification>
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/test-restore-workflow-wiring.test.ts` green.
- Each file's `SOFTENING_TOKENS` has nine entries (the node one-liner in the verify commands).
- SUMMARY carries the green-before / red-after observation of the `psql … 2>/dev/null` insertion and the regenerated `2>/dev/null` count with its five (or however many) named sites.
- `node scripts/verify-plan-anchors.mjs <this plan's path>` exits 0 at task start.
</verification>

<success_criteria>
- WR-06 closed for the whole triplet, with separate constants and sibling comments (no shared helper).
- The restore workflow's tolerated sites are an exact set that reds on a new one in either direction.
- The defect was observed green-before, red-after, and both are quoted.
</success_criteria>

<output>
Create `.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-04-SUMMARY.md` when done.
</output>
