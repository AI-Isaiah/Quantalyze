---
phase: 164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/supabase-migrate.yml
  - src/__tests__/supabase-migrate-test-first.test.ts
  - src/__tests__/critical-regressions.test.ts
autonomous: true
requirements: [WR-04, IN-03, IN-04, SC-6]
tags: [ci-gates, github-actions, grep-nul-blind, byte-pins]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The TEST-side reverted-grep in `supabase-migrate.yml` carries `-a`, and an EXECUTED vitest arm proves that a `test-migration-list.txt` with a NUL byte before a `| reverted |` row makes the step exit 1 with `Reverted migrations detected` (WR-04, SC-3)."
    - "The PROD twin carries `-a` too, and `PROD_C0331_TAIL` was moved DELIBERATELY with its own justification — never as a side effect (RESEARCH Pitfall 3)."
    - "The pre-fix behaviour (plain `grep -Eiq` reading the NUL fixture as CLEAN) was OBSERVED and recorded per grep flavour; it is NOT asserted as a permanent platform-dependent test ([ASSUMED A1])."
    - "`suffix()` throws when `SUFFIX_ANCHOR` is absent instead of degrading to a one-character comparison (IN-03); the `apply:` job slices in critical-regressions.test.ts are bounded at the next job (IN-04)."
  artifacts:
    - ".github/workflows/supabase-migrate.yml — `grep -aEiq` in both C-0331 sites (TEST twin and PROD)."
    - "src/__tests__/supabase-migrate-test-first.test.ts — `REVERTED_IF` and `PROD_C0331_TAIL` updated; new executed NUL-fixture arm with a clean-fixture control; `suffix()` fail-loud with a calibration."
    - "src/__tests__/critical-regressions.test.ts — both `applyJob` slices bounded with the `jobBlock` regex, calibrated with a fake trailing job."
  key_links:
    - "`REVERTED_IF` is asserted live-exactly-once against the TEST job; the workflow edit and the pin move in the SAME task or the test reds on the wrong message."
    - "The executed arm slices the group block out of `extractRunScript(WF, TEST_PUSH_STEP)` and substitutes the `supabase migration list` line with a fixture read — no CLI, no database."
---

<objective>
Close WR-04: the C-0331 twin added by Phase 164.8 to `supabase-migrate.yml`'s `apply-test` job is a NEGATIVE check without `-a`, so a byte the locale calls binary makes `grep -Eiq` exit 1, read as clean, and green-light the PROD apply. Add `-a` to the TEST line AND its PROD twin (Contradiction C1 resolved below), move the two byte-pins deliberately, and prove the fix with an executed NUL-fixture arm. Fold in the two Info findings that live in the same test files (IN-03 `suffix()` degradation, IN-04 unbounded `applyJob` slice).

Purpose: `apply` pushes DDL to PROD on the strength of this check. A check that never read the file is not a check.

Output: the two-flag workflow edit, moved pins, one executed arm with a clean-fixture control, IN-03/IN-04 hardened with calibrations.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-CONTEXT.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-RESEARCH.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-PATTERNS.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-REVIEW.md
@.github/workflows/supabase-migrate.yml
@src/__tests__/supabase-migrate-test-first.test.ts
@src/__tests__/critical-regressions.test.ts
@src/__tests__/test-restore-workflow-wiring.test.ts

<!-- verify-symbol: src/__tests__/supabase-migrate-test-first.test.ts REVERTED_IF -->
<!-- verify-symbol: src/__tests__/supabase-migrate-test-first.test.ts PROD_C0331_TAIL -->
<!-- verify-symbol: src/__tests__/supabase-migrate-test-first.test.ts TEST_PUSH_STEP -->
<!-- verify-symbol: src/__tests__/supabase-migrate-test-first.test.ts SUFFIX_ANCHOR -->
<!-- verify-symbol: src/__tests__/supabase-migrate-test-first.test.ts extractRunScript -->
<!-- verify-symbol: src/__tests__/supabase-migrate-test-first.test.ts calibrate -->
<!-- verify-symbol: src/__tests__/critical-regressions.test.ts applyJob -->
<!-- verify-symbol: src/__tests__/test-restore-workflow-wiring.test.ts jobBlock -->

**Contradiction C1 — resolved here, not blended.** CONTEXT's `<code_context>` describes WR-04 as one flag on the TEST line; the review names four lines (TEST, PROD twin, and two `grep -q` lines in the restore workflow's post-verify step). Decision: fix ALL FOUR, because the rule "`-a` is mandatory repo-wide" is stated in 164.8's own restore workflow two lines above a bare `grep -q` and CLAUDE.md's fix-campaign rule says close the class across the surface it owns; but do NOT sweep `ci.yml` or the five byte-identical mutex-copy greps (their pins move in three files — a different campaign). The PROD twin is its own task with its own justification because `PROD_C0331_TAIL`'s message says exactly that. The two restore-workflow lines are owned by Plan 03 (that plan owns the file; same-wave file-disjointness).

Binding rules: never `git checkout --`; every mutation is on a scratch string or a scratch `cp`; no `supabase` CLI is invoked (this checkout is linked to PROD); `[ASSUMED A1]` (GNU grep's rc on a NUL file) is OBSERVED and recorded, never asserted as a permanent cross-platform test.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: `-a` on the TEST reverted-grep, `REVERTED_IF` moved, and an EXECUTED NUL-fixture arm — end-to-end</name>
  <files>.github/workflows/supabase-migrate.yml, src/__tests__/supabase-migrate-test-first.test.ts</files>
  <action>
1. In `.github/workflows/supabase-migrate.yml`, inside the `apply-test` job's `Push migrations to TEST` step, the group `Post-apply migration list verification (C-0331 twin)`: change `grep -Eiq` to `grep -aEiq` on the `reverted` line. Nothing else on that line changes. Add ONE comment line above the `if` stating the repo rule in the imperative, copied from the restore workflow's wording: `-a` is mandatory repo-wide because a tracked file carries a deliberate NUL byte and plain grep reports such input clean — and that a NEGATIVE check reading clean is the failure mode this line guards PROD against.
2. In `src/__tests__/supabase-migrate-test-first.test.ts`, move `REVERTED_IF` to the new bytes (`grep -aEiq …`). Run the file: the live-exactly-once assertion and its two calibrations must be green again. Add a one-line comment above the constant: moved 2026-09-09 for Phase 164.8.2 WR-04 (`-a`).
3. Add an EXECUTED arm in the same describe that already executes the affirmative block ("it is EXECUTED below, which is strictly stronger"). Shape:
   - `const script = extractRunScript(WF, TEST_PUSH_STEP)`; slice from the line containing `::group::Post-apply migration list verification (C-0331 twin)` through the first `echo "::endgroup::"` after it (throw with a named message if either anchor is missing — the arm must not silently scan nothing).
   - Replace the `supabase migration list --db-url "${dsn}" | tee …` line with `cat "${FIXTURE}" | tee "${RUNNER_TEMP}/test-migration-list.txt"`. Assert the replacement changed the string.
   - Prepend `set -euo pipefail` (the step's own preamble) and write the block to a temp file; run with `spawnSync("bash", …)` with `RUNNER_TEMP` and `FIXTURE` injected via env (`...process.env` first, then the injection — the file's idiom).
   - NUL fixture written with `Buffer.from` so the `\0` is a real byte: a header row, a row carrying `\x00`, then `   20260101000000 | reverted | 2026-01-01`. ASSERT: exit 1 AND stdout contains `Reverted migrations detected`. This is the invariant that holds on every grep measured (ugrep, BSD, GNU).
   - CONTROL: the same block on a CLEAN fixture (no `reverted` row, no NUL) → exit 0 and no `Reverted migrations detected`. Proves the exit 1 above comes from the branch, not from the harness.
   - CALIBRATION (text-level, platform-safe): strip the `a` from `-aEiq` in the sliced block and assert the mutant is not identical; then ALSO run the mutant against the NUL fixture and RECORD its exit code in the assertion message rather than asserting a value — i.e. `expect(typeof r.status).toBe("number")` with a message that prints `pre-fix rc=<n> on <grep --version | head -1>`. The observation you get locally (ugrep: expected rc 0 from the step = defect reproduced) goes in the SUMMARY under A1; the CI run's log line is the GNU reading. Do NOT make the test assert the pre-fix rc.
4. Observed RED for the fix itself (SC-6): on a scratch copy of the workflow string inside the test file's calibration (or ad hoc in the scratchpad), run the sliced block with the ORIGINAL `-Eiq` bytes against the NUL fixture on this machine and paste the exit code and the grep flavour (`type -a grep; /usr/bin/grep --version | head -1; ugrep --version | head -1`) into the SUMMARY. On ugrep the step exits 0 and prints `::endgroup::` — the defect, observed.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/supabase-migrate-test-first.test.ts && grep -v '^\s*#' .github/workflows/supabase-migrate.yml | grep -c "grep -aEiq '(^|\[\[:space:\]\]|)reverted" | grep -qx 1</automated>
    <fails_when>vitest exits non-zero or prints `failed` — the `REVERTED_IF` live-exactly-once pin still holds the old `-Eiq` bytes, the NUL-fixture arm sees exit 0 or no `Reverted migrations detected`, or the clean-fixture control exits 1 / prints that message (the harness, not the branch, is red); or the live `grep -aEiq … reverted` count is not exactly 1 — 0 means the TEST line still lacks `-a` (or was commented out), 2 means the PROD twin was edited here instead of in Task 2</fails_when>
  </verify>
  <done>The TEST line reads `grep -aEiq`; `REVERTED_IF` pins the new bytes and its calibrations pass; the executed NUL arm exits 1 with `Reverted migrations detected`, the clean control exits 0, and the pre-fix observation (rc + grep flavour) is recorded in the SUMMARY, not asserted.</done>
</task>

<task type="auto">
  <name>Task 2: The PROD twin — `-a` on the `apply` job's C-0331 grep, with `PROD_C0331_TAIL` moved deliberately</name>
  <files>.github/workflows/supabase-migrate.yml, src/__tests__/supabase-migrate-test-first.test.ts</files>
  <action>
1. In the `apply` job's `Push migrations to production` step, group `Post-apply migration list verification (C-0331)`: change `grep -Eiq` to `grep -aEiq`. One flag; nothing else in the tail moves (the `--linked`, `/tmp/migration-list.txt`, the error text and `exit 1` stay byte-identical).
2. In `supabase-migrate-test-first.test.ts`, update the third line of the `PROD_C0331_TAIL` array to the new bytes. Rewrite the `it` title and message so they state the truth: the tail is byte-identical to its pre-164.8-05 form EXCEPT the `-a` added by Phase 164.8.2 WR-04 on 2026-09-09, and say WHY that edit was made on the one automatic applier of DDL to production (a NEGATIVE check that reads a binary-looking byte as clean is a PROD apply on a check that never read the file). The existing calibration (dropping the `| tee`) stays and must still bite.
3. Run `npx vitest run src/__tests__/critical-regressions.test.ts` as well: it slices the `apply:` job for `needs`/`if` pins and must be unaffected (RESEARCH: no `reverted(` pin there — confirm by running, not by reading).
4. Record in the SUMMARY that this is the deliberate pin move Pitfall 3 describes, and quote the pin's old message.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/critical-regressions.test.ts && grep -v '^\s*#' .github/workflows/supabase-migrate.yml | grep -c "grep -aEiq '(^|\[\[:space:\]\]|)reverted" | grep -qx 2</automated>
    <fails_when>vitest exits non-zero or prints `failed` — `PROD_C0331_TAIL` still pins `-Eiq` (or its `| tee` calibration stopped biting), or critical-regressions moved (a pin there sliced the `apply:` job and caught the edit); or the live `grep -aEiq … reverted` count is not exactly 2 — 1 means the PROD twin still lacks `-a`, 3 means a third site was touched outside this plan's scope</fails_when>
  </verify>
  <done>Both C-0331 greps in `supabase-migrate.yml` carry `-a` (live count 2); `PROD_C0331_TAIL` pins the new bytes with a message naming the deliberate 164.8.2 edit; its calibration still bites; critical-regressions is green.</done>
</task>

<task type="auto">
  <name>Task 3: IN-03 `suffix()` fails loud; IN-04 `applyJob` slices bounded at the next job</name>
  <files>src/__tests__/supabase-migrate-test-first.test.ts, src/__tests__/critical-regressions.test.ts</files>
  <action>
**IN-03** — in the cross-file mutex describe of `supabase-migrate-test-first.test.ts`, replace `const suffix = (s) => s.slice(s.indexOf(SUFFIX_ANCHOR))` with a function that computes `i = s.indexOf(SUFFIX_ANCHOR)`, throws `new Error("SUFFIX_ANCHOR not found — the byte-identity pin has no subject")` when `i < 0`, and returns `s.slice(i)` otherwise. Add a calibration `it`: `expect(() => suffix("no anchor here")).toThrow(/SUFFIX_ANCHOR/)`, plus the positive case on the real `ciStep`. Comment: `indexOf` returning -1 made `slice(-1)` a one-character comparison — a silent degradation by construction (164.8-REVIEW IN-03).

**IN-04** — in `critical-regressions.test.ts`, BOTH occurrences of `const applyJob = src.slice(applyIdx)` (RESEARCH: two, not one — find them by that string): bound the slice with the wiring test's `jobBlock` shape copied inline (not imported — the repo's wiring tests are self-contained): after `applyIdx`, take `after = src.slice(applyIdx + "\n  apply:\n".length)`, find `after.match(/\n {2}[A-Za-z_][\w-]*:\n/)`, and slice to `next.index` when present. Add ONE calibration: append a fake trailing job (`"\n  zz_after_apply:\n    needs: [plan, apply-test]\n    if: needs.apply-test.result == 'success'\n"`) to a scratch copy of `src`, re-run the bounded slice, and assert it does NOT contain `zz_after_apply` while the UNBOUNDED slice does (proves the bound bites and the old form did not). Keep every existing assertion on `applyJob` unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/critical-regressions.test.ts</automated>
    <fails_when>non-zero exit or `failed` in the summary — the `suffix("no anchor here")` calibration does not throw (`indexOf` still degrades to `slice(-1)`), the positive `suffix(ciStep)` case throws (anchor drifted), or the `zz_after_apply` calibration reports the fake job INSIDE the bounded `applyJob` slice (bound not biting) or OUTSIDE the unbounded one (calibration seeded on the wrong string)</fails_when>
  </verify>
  <done>`suffix()` throws on a missing anchor and the calibration proves it; both `applyJob` slices stop at the next top-level job and the fake-job calibration shows the bounded slice excludes it while the unbounded one included it; both files green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `supabase migration list` output → PROD-apply verdict | untrusted CLI bytes (box-drawing, possible `\0`) decide whether `apply` pushes to production |
| test fixture → executed shell | the arm runs a workflow step's shell against fixture bytes; must stay hermetic (no CLI on PATH is invoked) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.2-05 | Tampering | NEGATIVE grep reading binary input as clean → PROD apply | high | mitigate | `grep -aEiq` on both C-0331 sites; executed NUL arm asserts exit 1 |
| T-164.8.2-06 | Repudiation | byte-pin moved silently | medium | mitigate | `PROD_C0331_TAIL` moved in its own task with the justification in the pin's message |
| T-164.8.2-07 | Repudiation | platform-dependent pre-fix assertion flakes CI | medium | mitigate | pre-fix rc is OBSERVED and recorded (A1), only the `-a` invariant is asserted |
| T-164.8.2-08 | Tampering | test helper degrading to a 1-char compare / slice past job end | low | mitigate | IN-03 throw + IN-04 bounded slice, both calibrated |
| T-164.8.2-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed |
</threat_model>

<verification>
- `npx vitest run src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/critical-regressions.test.ts` green.
- Live count of `grep -aEiq … reverted` lines in `supabase-migrate.yml` is exactly 2 (comments excluded).
- SUMMARY carries: the pre-fix rc on this machine's grep flavours, the deliberate `PROD_C0331_TAIL` move with the old message quoted, and the CI `sql-tests`/wiring run id once it exists (the GNU-grep reading for A1).
- `node scripts/verify-plan-anchors.mjs <this plan's path>` exits 0 at task start.
</verification>

<success_criteria>
- WR-04 closed on both C-0331 sites with an executed falsifier; the two restore-workflow `grep -q` lines are handed to Plan 03 by name.
- IN-03 and IN-04 closed with calibrations that would fail on the old code.
- No `supabase` CLI invocation, no database, no `git checkout --`.
</success_criteria>

<output>
Create `.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-02-SUMMARY.md` when done.
</output>
