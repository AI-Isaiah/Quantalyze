---
phase: 164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship
plan: 05
type: execute
wave: 2
depends_on: ["164.8.2-03"]
files_modified:
  - scripts/restore-test-from-baseline.sh
  - src/__tests__/restore-test-from-baseline.test.ts
autonomous: true
requirements: [IN-06, IN-01, IN-02, IN-05, SC-6]
tags: [restore-script, measure-fail, record-corrections]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The one unwrapped `grep -av` in `build_transaction` reports `MEASURE_FAIL: could not read <baseline> while filtering (grep exited N)` on an unreadable dump instead of dying under `set -e` with no diagnosis — observed RED on the pristine bytes (exit with no MEASURE_FAIL) and GREEN-by-name after (IN-06, SC-6)."
    - "The `set +e` exact-set allowlist in `restore-test-from-baseline.test.ts` moved 6 → 7 WITH a justification naming the new site, and its calibration still flips."
    - "The script's own narrative is correct: the refusal count word matches `grep -c '^refuse_[a-z_]*() {'` (IN-01), the arm list names arm 26 (IN-02), and the `ENV SEAMS` block names every `NAME=\"${NAME:-…}\"` seam the script declares — pinned by derivation so it cannot go stale (IN-05)."
  artifacts:
    - "scripts/restore-test-from-baseline.sh — wrapped `grep -av`; three comment corrections; five ENV SEAMS lines."
    - "src/__tests__/restore-test-from-baseline.test.ts — `ALLOWED` 6→7 with justification; sourced-function MEASURE_FAIL arm; refusal-word agreement pin; ENV-SEAMS derivation pin."
  key_links:
    - "Runs AFTER Plan 03 (which edits the same script's header comment) so the two edits never race on one file."
    - "The MEASURE_FAIL arm calls `build_transaction` on a SOURCED scratch copy with the final `main \"$@\"` line removed — the reviewer's WR-01 technique — so no throwaway cluster and no database is needed."
---

<objective>
Close the four Info findings that live in `scripts/restore-test-from-baseline.sh`: IN-06 (a silent `set -e` death instead of a named MEASURE_FAIL), IN-01/IN-02 (the self-test's own comment miscounts its refusals and its arm list), IN-05 (the `--help` contract omits five env seams that reach SQL). Each record correction gets a derivation pin so the words cannot drift from the code again; IN-06 gets an observed-RED falsifier.

Purpose: a narrative that miscounts its own guards is the same defect class as a stale floor (the script says so itself); a reading that dies with no diagnosis sends the operator to guess.

Output: the wrapped grep, the corrected comments, the widened seams block, and pins that derive from the file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-CONTEXT.md
@.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-RESEARCH.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-REVIEW.md
@scripts/restore-test-from-baseline.sh

Also read Plan 03's SUMMARY (`164.8.2-03-SUMMARY.md` in this phase directory) before starting — it changed this script's header comment and this plan must not undo that edit.
@src/__tests__/restore-test-from-baseline.test.ts

<!-- verify-symbol: scripts/restore-test-from-baseline.sh build_transaction -->
<!-- verify-symbol: scripts/restore-test-from-baseline.sh FILTER_PATTERN -->
<!-- verify-symbol: scripts/restore-test-from-baseline.sh FILTERED_N -->
<!-- verify-symbol: scripts/restore-test-from-baseline.sh REFDATA_KIND_CHECK -->
<!-- verify-symbol: src/__tests__/restore-test-from-baseline.test.ts runRegion -->
<!-- verify-symbol: src/__tests__/restore-test-from-baseline.test.ts ALLOWED -->

Binding rules: never `git checkout --` (the sourced-copy harness is exactly the kind where it destroys uncommitted work — copy pristine bytes to the scratchpad FIRST); no throwaway cluster is required by any task here (`--self-test` of this script needs one and is NOT run by this plan — run only the vitest file and `--help`); no database.
</context>

<tasks>

<task type="auto">
  <name>Task 1: IN-06 — wrap the unwrapped `grep -av` with the script's own MEASURE_FAIL idiom, move the `set +e` allowlist to 7, prove it on a sourced copy</name>
  <files>scripts/restore-test-from-baseline.sh, src/__tests__/restore-test-from-baseline.test.ts</files>
  <action>
1. In `build_transaction`, the line `grep -av "$FILTER_PATTERN" "$BASELINE_FILE" >> "$out"`: wrap it exactly like the `FILTERED_N` count a few hundred lines above — `set +e; grep -av "$FILTER_PATTERN" "$BASELINE_FILE" >> "$out"; rc=$?; set -e` then `[ "$rc" -le 1 ] || fail "MEASURE_FAIL: could not read ${BASELINE_FILE} while filtering (grep exited ${rc}). An unreadable dump is not an empty one."`. ⚠️ `grep -v` exits 1 when EVERY line was filtered (empty output) — `-le 1` is the right bound; say so in the comment. Keep the existing comment about `-a` above it.
2. In `src/__tests__/restore-test-from-baseline.test.ts`, the exact-set `ALLOWED` block: `set +e` moves 6 → 7. Add the seventh site to the justification list above it, dated `RE-MEASURED 2026-09-09 (Phase 164.8.2 IN-06)`: the `build_transaction` filter grep, whose rc is now captured and CHECKED instead of killing the script under `set -e` with no `::error::`. `|| true` stays at 4. The existing `|| true` calibration must still flip; add a mirror calibration for `set +e` (insert one more `set +e; :; set -e` pair on a live line of the `--run` region → count 8).
3. **Falsifier, observed RED (SC-6), as a permanent vitest arm in the same file:** write a scratch copy of the script with the final `main "$@"` line removed (assert the removal changed the string and that the copy still ends with the function definitions). In a bash harness: `source "$COPY"` with env `RESTORE_OUT_DIR=<tmp>` (containing an empty `survivors.keys`), `BASELINE_FILE=<tmp>/does-not-exist.sql`, `RESTORE_DB_URL=stub-never-used`, then call `build_transaction restore`. Assert exit 1 and that combined output contains `MEASURE_FAIL: could not read`. Then the pre-fix observation: the same harness against `git show HEAD:scripts/restore-test-from-baseline.sh` (a READ) written to the scratchpad — records exit code and the ABSENCE of `MEASURE_FAIL` (under `set -e`, grep's exit 2 kills the shell with no diagnosis). Paste both into the SUMMARY. ⚠️ If sourcing the script executes something at top level beyond function and default definitions (it should not — the reviewer sourced it for WR-01), STOP and report rather than working around it with `set +e` in the harness.
4. Run `bash scripts/restore-test-from-baseline.sh --help` to confirm the script still parses.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/restore-test-from-baseline.test.ts && bash scripts/restore-test-from-baseline.sh --help >/dev/null && grep -v '^\s*#' scripts/restore-test-from-baseline.sh | grep -c 'could not read ${BASELINE_FILE} while filtering' | grep -qx 1</automated>
    <fails_when>vitest exits non-zero or prints `failed` — the `set +e` exact-set count is not 7 (allowlist not moved, or moved without the new site actually existing), its mirror calibration does not read 8, or the sourced-copy arm sees exit 0 / no `MEASURE_FAIL: could not read` on a missing baseline (the grep is still unwrapped and dies silently under `set -e`); `--help` exits non-zero (the edit broke the parse); or the live count of the MEASURE_FAIL message is not exactly 1 — 0 means the wrap is missing or the message text drifted from the pin, 2 means it was pasted twice</fails_when>
  </verify>
  <done>The filter grep is rc-bounded with a named MEASURE_FAIL; `ALLOWED` pins `set +e` at 7 with a dated justification and a flipping calibration; the sourced-copy arm proves the named failure and the pre-fix silent death is recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: IN-01, IN-02, IN-05 — three record corrections, each pinned by derivation</name>
  <files>scripts/restore-test-from-baseline.sh, src/__tests__/restore-test-from-baseline.test.ts</files>
  <action>
1. **IN-01** — in the `self_test` closing comment (the `W2` block above the success line): regenerate `grep -c '^refuse_[a-z_]*() {' scripts/restore-test-from-baseline.sh` and replace `(2026-09-09: EIGHT, after \`refuse_bad_refdata_allowlist\` joined the block)` with the word matching the regenerated count and the last refusal that joined (`refuse_backticks_in_txn_heredocs`). ⚠️ KEEP the line's shape `# (YYYY-MM-DD: WORD, after \`refuse_…\` joined the block)` — a comment line starting with a parenthesised date, then the upper-case word followed by a comma — because this task's `<verify>` derives the word from the live count and greps THAT dated line for `WORD,` inside the region from `W2 — THIS SENTENCE` to the `self-test OK` echo. Measured 2026-09-09 (plan revision): the count is 9, the emitted success line already says `NINE refusals`, and the W2 comment says `EIGHT` — so the verify conjunct is RED on the unfixed script and turns GREEN only when the comment's word matches the derived count. (The plan-checker caught the previous conjunct comparing the count to a byte-identical copy of itself, which exited 0 in every state of the file — the anti-vacuity class this phase exists to remove.) Pin: in the vitest file, derive the count with `readFileSync` + the same regex, map it to the English word table the script uses (EIGHT/NINE/TEN …), and assert the W2 comment AND the emitted success line (`NINE refusals`) both carry that word — a drift in either is red with a message naming the regenerate command. Calibrate by mutating the comment's word on a scratch string.
2. **IN-02** — same comment block: `arms 1-7, 20, 21 and 24` → `arms 1-7, 20, 21, 24 and 26`. Pin: assert the sentence names every arm number that asserts a refusal's NAMED message — derive the set by scanning `run_arm "<n> -----` labels whose leg greps for a `refuse` message (if that derivation is more than ~20 lines, fall back to asserting the literal list contains `26` with a comment saying why the derivation was not built — do not over-engineer a comment pin).
3. **IN-05** — add five lines to the `ENV SEAMS` header block, in the block's column-aligned style, for `REFDATA_ALLOWLIST`, `REFDATA_EXTRACTOR`, `REFDATA_KIND_REGISTRY`, `REFDATA_KIND_REGISTRY_COL`, `REFDATA_KIND_CHECK` — each with its default (read from the declaration, not from memory) and, for the last three, the note that they are interpolated into the in-transaction gate as identifiers AND string literals, which is why they are charset-refused. Pin (cannot go stale): derive every `^[A-Z_]+="\$\{[A-Z_]+:-` declaration name from the script and assert each appears as a token inside the `ENV SEAMS` block (slice from `ENV SEAMS` to the next `# ──` rule); exclude nothing silently — if a seam is deliberately undocumented, list it in an explicit `UNDOCUMENTED_SEAMS` array with a reason, so the exception is a reviewed line. Calibrate by removing one seam line from a scratch string.
4. `bash scripts/restore-test-from-baseline.sh --help` prints the widened block.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/restore-test-from-baseline.test.ts && bash scripts/restore-test-from-baseline.sh --help | grep -aq 'REFDATA_KIND_CHECK' && n=$(grep -v '^\s*#' scripts/restore-test-from-baseline.sh | grep -c '^refuse_[a-z_]*() {'); w=$(printf 'ZERO ONE TWO THREE FOUR FIVE SIX SEVEN EIGHT NINE TEN ELEVEN TWELVE' | tr ' ' '\n' | sed -n "$((n+1))p"); [ -n "$w" ] && sed -n '/W2 — THIS SENTENCE/,/self-test OK/p' scripts/restore-test-from-baseline.sh | grep -a '^\s*# (20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]: ' | grep -aq "$w," && sed -n '/W2 — THIS SENTENCE/,/self-test OK/p' scripts/restore-test-from-baseline.sh | grep -a 'self-test OK' | grep -aq "$w refusals"</automated>
    <fails_when>vitest exits non-zero or prints `failed` (the derivation pins for the refusal word, the arm list or the ENV SEAMS block did not hold, or a scratch-string calibration did not flip); `--help` output lacks `REFDATA_KIND_CHECK` (the seams block was not widened, or the header no longer prints); or the IN-01 conjunct fails: `$w` is empty (count outside the ZERO…TWELVE table — extend the table, do not drop the check), the dated `# (YYYY-MM-DD: WORD, after …)` line inside the W2 block does not carry the word derived from the live `refuse_*() {` count (this is the pre-fix state — today it says `EIGHT,` while the count is 9 — so the conjunct is RED on an unfixed script by construction), or the emitted `self-test OK` line does not say `<WORD> refusals`</fails_when>
  </verify>
  <done>The W2 comment's refusal word equals the regenerated count and the emitted line, pinned by derivation; the arm list names 26; the ENV SEAMS block names all five REFDATA seams and a derivation pin proves every declared seam is documented (or listed as a reviewed exception); `--help` prints them; the vitest file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline dump file → assembled `DROP SCHEMA public CASCADE` transaction | an unreadable dump must never produce a transaction, and must say why it did not |
| script header → operator | `--help` is the contract an operator reads before running a destructive tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.2-17 | Repudiation | silent `set -e` death with no `::error::` | medium | mitigate | rc-bounded grep + named MEASURE_FAIL; sourced-copy arm |
| T-164.8.2-18 | Repudiation | narrative miscounts its own guards | low | mitigate | derivation pins for the refusal word, arm list, ENV SEAMS |
| T-164.8.2-19 | Information Disclosure | harness echoing a DSN | low | accept | harness uses the literal `stub-never-used`; no real DSN exists in the test env |
| T-164.8.2-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed |
</threat_model>

<verification>
- `npx vitest run src/__tests__/restore-test-from-baseline.test.ts` green; `--help` prints the five seams.
- SUMMARY carries the pre-fix (silent exit, no MEASURE_FAIL) and post-fix (named MEASURE_FAIL) harness outputs, and the regenerated refusal count.
- `node scripts/verify-plan-anchors.mjs <this plan's path>` exits 0 at task start.
</verification>

<success_criteria>
- ⚠️ CHANGELOG entry + `VERSION`/`package.json` bump (CLAUDE.md "CHANGELOG discipline"): **owned by `/gsd-ship`, NOT by any task in this phase's five plans.** The ship step enumerates every commit on the branch, groups by theme and cross-checks that each commit maps to a bullet; a plan task writing a partial entry mid-phase would be replaced by that unified entry anyway. Recorded here (the last plan by number and wave) so the plan-checker's `claude_md_compliance` warning is answered once and not re-raised — do not add a CHANGELOG task to any plan.
- IN-06 closed with an observed-RED falsifier and the exact-set allowlist moved with a justification.
- IN-01, IN-02, IN-05 closed with pins that derive from the file, so the words cannot drift again.
- `--self-test` of this script (needs a throwaway cluster) is NOT claimed as run by this plan; the SUMMARY says so explicitly rather than implying it.
</success_criteria>

<output>
Create `.planning/phases/164.8.2-gatehardening-the-five-code-review-warnings-phase-164-8-ship/164.8.2-05-SUMMARY.md` when done.
</output>
