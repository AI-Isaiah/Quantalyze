# Phase 164.8.2 — review fix batch A: the WORLD-READABLE ARTIFACT findings

**Base:** `c95f09fbe5dc743c6e2af5397e05e5090008bd61`
**Files owned and touched — these two only:**
`.github/workflows/test-restore-from-baseline.yml`, `src/__tests__/test-restore-workflow-wiring.test.ts`

**Six findings, six atomic commits, all six fixed. Nothing skipped.**

| commit | finding | what it closed |
|---|---|---|
| `e83fb38c` | WR-01 | the staging step was default-**IN** for `*.err/*.log/*.out` |
| `5079f806` | WR-06 | a failed redaction published the channels its own error forbids |
| `21b68a90` | WR-02 | the in-artifact README said the scan story was complete |
| `e4539280` | F4 | `--is-ancestor` rc 128 read as "is not an ancestor" |
| `0664a455` | F2 | the `2>/dev/null` allowlist was a COUNT, not a set of sites |
| `cd5befc6` | F3 | the nine-token scan could not see `set -uo pipefail` |

Suite on this file: **46 passed at base → 51 passed now.** `tsc --noEmit` exit 0,
`eslint` exit 0, the workflow parses as YAML, 0 NUL bytes in either file, and the
eight test files that read this workflow or share its token list are **835 passed**.

---

## 1. Falsifiers, and the RED I observed

Every fix below was written test-first: the arm went in, I ran it against the
**unmodified** source, and I am quoting the failure verbatim. Nothing here is an
assertion I only ever ran green.

### WR-01 — the leak, reproduced by execution

The arm was added with `UNEXPECTED_CHANNEL = "future-census.out"` seeded with
`CREATE POLICY p ON t USING (owner = current_user)`, then run against the shipped
step. The staged manifest it printed:

```
staged manifest:
  census.err
  census.sql
  dump.log
  future-census.out          <- nobody named this file
  ledger.csv
  ledger.err
  marker.err
  README.txt
  refdata.err
  refdata.sql
  restore.sql
  schema-before.sql
  survivors.sql
  transaction.out
staged 14 file(s) from …; everything else the script wrote stays in the job's temp
dir and never enters the artifact.
```

Three arms RED, 43 passed. The reviewer's measurement reproduced exactly: a file
the job does not write, carrying policy-shaped DDL, in a 90-day world-readable
artifact — announced by a sentence saying it could not happen.

Kept as a standing twin (CALIBRATION 3): restore the glob on a scratch copy and
the arm must see `future-census.out` reach the artifact **with its policy line
intact** (`leaked.contents[UNEXPECTED_CHANNEL]` is asserted to contain the
marker, not merely the filename).

### WR-06 — the channels published after a failed redaction

```
AssertionError: `census.err` was published after a FAILED redaction.
`withhold_channels()` prints "Do NOT publish this run's artifact: treat the TEST
pooler host, its IP and the DB user as disclosed" — and this step then published
it anyway (review WR-06).: expected true to be false
```

and, separately:

```
AssertionError: the redaction step lost its `id: redact` … expected false to be true
```

Two arms RED, 46 passed. Twin: strip the guard on a scratch copy and all six
channels return on the same failed redaction.

### WR-02 — the README, measured against the code

```
AssertionError: the README that ships INSIDE the world-readable artifact does not
tell its reader that 11 of the file(s) it carries went through NO secret scan:
README.txt, census.sql, survivors.sql, restore.sql, refdata.sql, census.err,
ledger.err, marker.err, refdata.err, dump.log, transaction.out.
```

One arm RED, 48 passed. **11 of the 13 staged files.**

### F4 — the false statement about PROD's apply, printed verbatim

Driving the real asserter (extracted from the YAML, run under the W5 stub
harness) with a 40-hex sha that is not a commit:

```
::error::the PROD-ledger premise does not hold — ancestor(00000000000000000000
00000000000000000001 is not an ancestor of this checkout's HEAD).
```

git never said that. It exited **128** — measured here:
`fatal: Not a valid commit name 0000…0001`, `rc=128` — and that sentence went to
`/dev/null`. One arm RED, 49 passed.

### F2 — the count's blindness, measured rather than argued

The new arm carries `countRuleOffenderCount()`, the **superseded count rule kept
as a reference oracle**. On the count-preserving swap (one benign probe
suppression deleted, one added on the marker step's psql) it asserts:

- the live token count is still 4 — the swap really is count-preserving;
- **the old rule reports ZERO offenders** — the blindness, executed;
- the new site rule reports `UNLISTED SITE in step "Which database am I on"` **and**
  `VANISHED OR DUPLICATED SITE`.

If the old rule had caught it, the arm fails with *"CALIBRATION: the SUPERSEDED
count rule already caught this swap, so the site allowlist is not buying anything
and F2 was not a finding."*

Separately, F4's site removal made the pre-existing exact-set arm fire on its own
before I touched it — the control working:

```
2>/dev/null (4, the allowlist admits exactly 5) — … a vanished one means an exit
status that used to be checked stopped being checked
```

### F3 — RED against the REAL workflow, not only a mutant

I neutered the shipped file (`set -euo pipefail` → `set -uo pipefail` in
`Which database am I on`), asserted the neuter applied (`NEUTER APPLIED, bytes
116512 -> 116511`) and printed the diff before trusting anything:

```diff
       - name: Which database am I on
         run: |
-          set -euo pipefail
+          set -uo pipefail
```

RED:

```
AssertionError: a `run: |` block in the restore job never turns `-e` on and is not
a named exception:
[ "NO ERREXIT in step \"Which database am I on\" — its `run: |` block never turns
  `-e` on, which softens every command in it exactly as `set +e` would while
  carrying none of the nine banned tokens. …" ]
```

The same arm asserts `softeningOffenders(mutant)` is `[]` on that mutant — the
nine-token scan's blindness measured, not claimed.

**Restore:** from a byte backup taken *after* the last edit, verified by sha256
(`de297381…` on both sides) and by `git status --porcelain` showing only the test
file dirty, plus a grep confirming the newest fixes (`REDACT_OUTCOME:-`,
`ancestry_rc`) survived. No `git checkout --` was used anywhere in this session.

---

## 2. Counts, each regenerated from the file as it is now

| figure | value | command |
|---|---|---|
| diagnostic channels written into the backup dir | **6** | `grep -aoE 'RESTORE_OUT_DIR}?/[A-Za-z0-9_.-]+\.(err\|log\|out)' scripts/restore-test-from-baseline.sh` + the same over `outdir}?/…` in the workflow → `census.err ledger.err marker.err refdata.err dump.log transaction.out` |
| files the artifact can carry | **13** | 7 in `for f in …` (`:1663`) + 6 in `for c in …` (`:1682`) |
| files any secret scan reads | **2** | `grep -n 'scan_for_secrets "\${outdir}' …` → `ledger.csv`, `schema-before.sql` |
| staged files scanned by nothing | **11** | derived in-test: staged minus scanned |
| live `2>/dev/null` sites in the scannable block | **4** (was 5) | `liveLines(scannableRestoreBlock(WF)).filter(l => l.includes("2>/dev/null"))` — the whole file carries 10; the rest are in the excluded mutex copies or in comments |
| `run: \|` blocks in the scannable block | **16** | enumerated by the same parser the arm ships; 13 set `-e`, 3 do not |
| `set -uo pipefail` exemptions | **3** | exactly the three F3 named — nothing else surfaced |
| README numeral | **THIRTEEN** | derived in-test from the allowlist length, not typed |

⚠️ The README heading's numeral and the channel list are both **pinned to the
allowlist**, so neither can go stale silently the way "both files were scanned"
did.

---

## 3. Formerly-false sentences, and what makes each true now

⛔ None of these was fixed by deleting it. In every case the **code** was changed
and the sentence now describes the code.

| sentence | why it was false | what makes it true |
|---|---|---|
| `⛔ THE ALLOWLIST IS BY NAME, NEVER BY GLOB, AND THAT IS THE WHOLE POINT.` (`:1573`) | the channel loop three lines below was `for c in "${outdir}"/*.err …` | the channels are enumerated by name; a dated WR-01 paragraph directly under it records what the glob cost and forbids restoring it |
| `a file the script gains tomorrow is OUT until someone adds its name here and says why` (`:1595`) | held for content files only | now holds for `.err/.log/.out` too |
| the runtime echo `everything else the script wrote … never enters the artifact` (`:1693`) | `future-census.out` entered it | the step's two loops are now both by name, so the echo describes the whole step |
| `The diagnostic channels, AFTER the redaction step above has scrubbed them` | stated an assumption as fact under `if: always()` | gated on `steps.redact.outcome == success`, default-DENY, with an `::error::` on the else branch |
| README: `both files were scanned` | true of a two-file artifact, false of a thirteen-file one | replaced by a scoped heading plus `⛔ EVERYTHING ELSE HERE WAS SCANNED BY NOTHING`, naming all 11; the old sentence is pinned as DEAD with a re-insertion twin |
| doc comment: site 1's `2>/dev/null` justification (*"git's stderr here would only restate 'not a valid object' for a sha the case above has already accepted as hex"*) | it conceded the defect while calling it a reason | the site is gone; the text is preserved **as the finding it was**, under a note that a justification arguing for suppressing evidence is a finding, not an entry |
| the `it` title *"the `2>/dev/null` allowlist is an exact set"* | the exactness was over TOKENS | the sites are executable and the rule is a bijection; the title says `OF SITES` |

---

## 4. Things I changed that the findings did not ask for, recorded rather than buried

1. **One pre-existing calibration was re-anchored, not deleted.** *"the redaction
   runs on an aborted run too"* spliced on
   `- name: <REDACT>\n        if: always()\n`; WR-06's `id: redact` now sits
   between those lines, so the replace became a no-op. It was caught by
   `calibrate()`'s own *"the mutation produced an identical string"* assertion —
   which is what that assertion exists for. The reason is in a comment beside it.

2. **A first attempt at F2's step attribution was written and REVERTED.** Matching
   an allowlisted site "anywhere in the step" would let a second suppression ride
   in on site 4's justification inside the backup step. The step name is used for
   the *message* only; the match stays per-line. The reverted attempt is recorded
   in the comment so nobody re-introduces it as a simplification.

3. **F3's vacuity guard was loosened from a second floor to `> 0`.** The tight
   version SHADOWED the finding — the neuter failed on *"expected 12 to be >= 13"*
   instead of on the offender that names the step. A guard that fires before the
   rule it guards is a worse message, not a stronger check. Recorded in a comment.

4. **WR-02's first calibration did not bite and was replaced.** It deleted
   `survivors.sql` from the scan-scope section, but that name appears **twice**
   there, so the mutation was a no-op the predicate could not see. It now mutates
   `census.err`, which appears once. The reason is in the comment — this is the
   *"a neuter that does not APPLY reads as GREEN"* class, caught by `calibrate()`.

5. **`runStage()` now returns the staged files' BYTES** (read before teardown), so
   the WR-01 twin can assert the leaked channel *carried the policy line* rather
   than merely that a filename appeared in a manifest.

6. **The deleted `Object.keys(ALLOWED)` assertion was replaced by something
   stronger**, not dropped: the other eight tokens now have no allowlist at all,
   and that is proven by mutating each of the eight into the job in turn and
   asserting each is reported by name.

---

## 5. Not done, and why

**Nothing in batch A was skipped.** All six findings are fixed and committed.

Two things I deliberately did **not** touch, both out of scope for this batch and
owned by sibling fixers:

- WR-03, WR-04, WR-05, IN-01…IN-06 and F1, F5–F8 — other files, other fixers.
- `scripts/restore-test-from-baseline.sh` — F4's sibling `|| true` shapes (F5) and
  the `MEASURE_FAIL` discard (F6) live there. I did not edit it; the only thing I
  did with it was **read** it, to regenerate the channel-name list from the writer
  rather than carry it from the finding.

**One residual worth naming, not a defect:** the six-name channel allowlist is a
list that can go stale — if the restore script gains a `.err` file, it will not be
published until someone adds it. That is the *safe* direction and it is the whole
point of default-out, but it means a future diagnostic can be silently absent from
an artifact rather than silently present. The staging step's own comment says so,
and the README tells the artifact's reader the same thing.

**Gates run here, and where:** everything above ran in this agent's isolated
worktree (`.claude/worktrees/agent-a81b33466333daf5a`) against its own
`node_modules`, at the six commits listed. No CI job was dispatched, no database
was touched, and nothing was run against shared TEST or PROD.

---

_Batch A fixer, 2026-09-10. Base `c95f09fb`, head `cd5befc6`._
