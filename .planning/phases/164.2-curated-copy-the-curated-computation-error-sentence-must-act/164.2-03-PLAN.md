---
phase: 164.2-curated-copy-the-curated-computation-error-sentence-must-act
plan: 03
type: execute
subsystem: factsheet
tags: [anonymous-placeholder, copy, anti-vacuity]
wave: 1
depends_on: []
files_modified:
  - src/app/factsheet/[id]/v2/page.tsx
  - src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx
autonomous: true
requirements: [164.2-SC9]
provides:
  - "The uncomputed-factsheet placeholder addresses its anonymous reader: no dev-server instruction, no internal gate detail"
affects: [164.2-10]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The anonymous render of a published-but-uncomputed factsheet contains neither `dev-server console` nor `bundled benchmark window` nor `2023-04-26`, and says the factsheet has not been computed yet without promising when"
    - "A test bound to the ANONYMOUS render (the surface measured on PROD at /factsheet/8581f739-1a7b-42a4-a209-3acfa327e259) fails when the developer-facing sentence is restored"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/page.tsx"
      provides: "replacement paragraph at the :414-421 site"
    - path: "src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx"
      provides: "V2b: anonymous-lane negative + positive assertions over the uncomputed branch"
  key_links:
    - from: "src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx"
      to: "src/app/factsheet/[id]/v2/page.tsx"
      via: "renderPage() calls FactsheetV2Page directly with no auth mock (:180-183) — the public lane"
      pattern: "dev-server console"
---

<objective>
Criterion 9: the public "still computing" placeholder (`src/app/factsheet/[id]/v2/page.tsx:414-421`) tells an anonymous PROD visitor to "See the dev-server console for the exact gate the request fell through" and volunteers an internal gate detail ("insufficient observations inside the bundled benchmark window (2023-04-26 onward)"). Measured on PROD 2026-09-05 at `/factsheet/8581f739-1a7b-42a4-a209-3acfa327e259` (`Fibonacci Ghost`). Replace it with a sentence addressed to the reader it actually has. COPY scope only — whether that strategy should be published-but-uncomputed is explicitly not claimed (CONTEXT, Deferred).

Purpose: the sentence a user reads must be true and addressed to them.
Output: the replacement paragraph and a test over the anonymous render that fails when the old sentence returns.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-CONTEXT.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-PATTERNS.md

Read first (PATTERNS §11): `src/app/factsheet/[id]/v2/page.tsx:185` (`lane` defaults to `"public"`), lines 368-423 (the `buildFactsheetPayload returned null` arm; only `OwnerUnpublishedPanel` at lines 399-404 is lane-gated; the `console.warn` hint at lines 369-372 is server-side and STAYS — it is the developer's channel); `src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx:128-145` (request-client mock without `auth`), lines 164-178 (`collectText`), lines 180-183 (`renderPage`), lines 202-212 (V2). `page.owner-lane.test.tsx:432` covers the owner lane of the same branch and is left alone.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: V2b test on the anonymous render, then the replacement sentence</name>
  <files>src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx, src/app/factsheet/[id]/v2/page.tsx</files>
  <behavior>
    - V2b (beside V2 at lines 202-212, same `renderPage("failed")` + `collectText` harness — the ANONYMOUS lane because the request-client mock has no `auth` member): the collected text does NOT contain `dev-server console`, does NOT contain `bundled benchmark window`, does NOT contain `2023-04-26`; DOES contain `still computing` (V2's existing anchor, unchanged) and DOES contain the replacement sentence's distinctive phrase `has not been computed yet`.
    - The `it` title names criterion 9 and the PROD URL where it was measured.
  </behavior>
  <action>
    Write V2b first; run it RED against the current page (the negative assertions fail). Then replace the lines 414-421 paragraph with one that keeps the same `className` and says, in plain words, that the factsheet has not been computed yet and that the page will show the full panel set once it has — without a time promise ("a few minutes" goes too). Keep lines 408-413 (the "still computing" paragraph V2 anchors on) unchanged. Add a two-line comment above the paragraph naming criterion 9 and stating that the internal gate detail lives in the `console.warn` hint at lines 369-372, which is where a developer looks. Do not lane-gate the sentence: the owner has no dev console on PROD either.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx" "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" && npx tsc --noEmit</automated>
    <fails_when>vitest exits non-zero — V2b's negative assertions find `dev-server console`, `bundled benchmark window` or `2023-04-26` in the collected text (the paragraph is unchanged or only partly rewritten); V2b's positive assertion cannot find `has not been computed yet` (the replacement phrase differs from the one the test pins); V2 loses its `still computing` anchor (the paragraph above it was touched); the owner-lane suite breaks because the rewrite was lane-gated or its `className` changed; or `tsc` exits non-zero (JSX left unbalanced by the edit)</fails_when>
  </verify>
  <done>V2, V2b, V3 and the owner-lane suite green; `grep -n "dev-server console" "src/app/factsheet/[id]/v2/page.tsx"` returns only the server-side `console.warn` hint region (lines 369-372) or nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Neuter-and-observe: restore the developer sentence, watch V2b go RED</name>
  <files>src/app/factsheet/[id]/v2/page.tsx</files>
  <action>
    Byte backup (`cp`), `shasum -a 256` before. Neuter: paste the original lines 414-421 paragraph back (the exact text is quoted in PATTERNS §11). Run the test file; expected RED: V2b only, naming the `dev-server console` assertion. Record the FAIL block verbatim. Restore by `cp`; `shasum` after equals before; run GREEN. Never `git checkout --`. Write the observation and hash pair into the SUMMARY under `## Anti-vacuity observations`.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx" && shasum -a 256 "src/app/factsheet/[id]/v2/page.tsx"</automated>
    <fails_when>vitest exits non-zero (the developer sentence pasted back for the neuter is still in the file — V2b is RED on the file as left); or `shasum` exits non-zero (the page file missing at its path). ⚠️ CANNOT FAIL on the task's own claim: `shasum -a 256` PRINTS the post-restore hash and exits 0 for any readable file — it compares nothing, so "the restore hash differs from the pre-neuter hash" is NOT a failure this command can detect; that equality is established only by the executor reading the before/after pair it records in the SUMMARY. Likewise "a neuter left the suite GREEN" (the vacuity this task exists to catch) is observed during the task's manual RED runs and never by this command, which runs on the restored file.</fails_when>
  </verify>
  <done>One RED observation recorded; hash restored; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> anonymous reader | RSC output for an unauthenticated visitor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.2-05 | Information Disclosure | page.tsx placeholder | low | mitigate | the internal gate detail (benchmark window bounds) no longer renders to anonymous readers; it stays in the server-side console.warn |
| T-164.2-SC | Tampering | npm installs | low | accept | no package installed |
</threat_model>

<verification>
- Test files green; anti-vacuity observation recorded.
</verification>

<success_criteria>
Criterion 9: no dev-only instruction and no internal gate detail on the anonymous path, proven by a test bound to the anonymous render that fails when the sentence is restored.
</success_criteria>

<output>
Create `.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-03-SUMMARY.md` when done.
</output>
