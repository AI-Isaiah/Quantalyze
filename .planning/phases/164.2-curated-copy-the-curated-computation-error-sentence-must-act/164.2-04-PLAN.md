---
phase: 164.2-curated-copy-the-curated-computation-error-sentence-must-act
plan: 04
type: execute
subsystem: wizard-error-roster
tags: [wizard-copy, roster-laws, create-with-key, DRAFT_ALREADY_EXISTS, RATE_LIMITED, anti-vacuity]
wave: 1
depends_on: []
files_modified:
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.test.ts
  - src/lib/wizardErrors.invariant.test.ts
  - src/app/api/strategies/create-with-key/route.ts
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
autonomous: true
requirements: [164.2-SC4, 164.2-SC5]
provides:
  - "Two minted WizardErrorCode members: PRESELECT_REQUEST_INVALID (400, preselect request-shape guard) and DRAFT_SESSION_COLLISION (409, stale-session 23505)"
  - "create-with-key's two userActionLimiter arms emit RATE_LIMITED (our cap) instead of KEY_RATE_LIMIT (exchange)"
  - "EXPECTED_TABLE_SIZE 90 -> 92 at both pins; 409 hand-typed set 4 -> 5; a 429 hand-typed twin"
affects: [164.2-05, 164.2-09, 164.2-10]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The preselect request-shape guard (src/app/api/strategies/create-with-key/route.ts:476-481) no longer wears a KEY_* verdict: it answers 400 PRESELECT_REQUEST_INVALID whose title/cause speak of our request, not of a credential or an empty field; KEY_MISSING_REQUIRED_FIELD stays on the five credential-arm sites where it is true"
    - "create-with-key's two userActionLimiter 429s (src/app/api/strategies/create-with-key/route.ts:494, :900) answer RATE_LIMITED, whose copy says the cap is ours; KEY_RATE_LIMIT's exchange copy is untouched and still reached only through classifyKeyValidationError (a genuine venue throttle)"
    - "DRAFT_ALREADY_EXISTS's cause is SPLIT: on the preselect 23505 arm the route reads the colliding draft's api_key_id — same key -> DRAFT_ALREADY_EXISTS (true: same API key); different key or read fault -> DRAFT_SESSION_COLLISION whose cause names the (user_id, wizard_session_id, source) collision. The credential-arm 23505 (:1440) keeps DRAFT_ALREADY_EXISTS"
    - "All five roster laws move together in one commit: union, EXPECTED_TABLE_SIZE x2 (NUL-byte file), divergence test, ConnectKeyStep roster, invariant scanner (expectedSites stays 13; 409 set gains DRAFT_SESSION_COLLISION; a new 429 hand-typed twin proves RATE_LIMITED is rostered)"
  artifacts:
    - path: "src/lib/wizardErrors.ts"
      provides: "two new union members and WIZARD_ERROR_COPY entries; DRAFT_ALREADY_EXISTS's cause unchanged"
    - path: "src/app/api/strategies/create-with-key/route.ts"
      provides: "three emitter edits (:476-481, :494 and :900, :666-672 split)"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      provides: "KNOWN_CREATE_WITH_KEY_CODES gains PRESELECT_REQUEST_INVALID, DRAFT_SESSION_COLLISION, RATE_LIMITED, each with a dated comment"
  key_links:
    - from: "src/app/api/strategies/create-with-key/route.ts"
      to: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      via: "a code the route emits must be in KNOWN_CREATE_WITH_KEY_CODES or it renders UNKNOWN (ConnectKeyStep.tsx:281-283)"
      pattern: "KNOWN_CREATE_WITH_KEY_CODES"
    - from: "src/lib/wizardErrors.invariant.test.ts"
      to: "src/app/api/strategies/create-with-key/route.ts"
      via: "source scan of `NextResponse.json({ code: \"X\", … }, { status: 400 })` literals — code FIRST, as a literal"
      pattern: "expectedSites: 13"
---

<objective>
Criteria 4 and 5 on the emitter that owns them, `src/app/api/strategies/create-with-key/route.ts`, plus the roster laws that a minted code moves.

(4a) The preselect request-shape guard at lines 476-481 emits `KEY_MISSING_REQUIRED_FIELD` two lines under a comment that says the arm "may not wear a `KEY_*` verdict that blames a credential". The copy's title ("One of the required fields is empty.") is false on a screen with no fields. `fixRequires` gates `fix[]` only, so the fix is at the EMITTER: mint `PRESELECT_REQUEST_INVALID`.
(4b) The two `userActionLimiter` 429s (line 494, line 900) emit `KEY_RATE_LIMIT`, whose copy blames the exchange and whose `fix[1]` ("try a different exchange account") cannot clear a per-USER bucket. `RATE_LIMITED` already carries the honest our-limiter copy (`wizardErrors.ts:3326-3335`, pinned by `SubmitStep.test.tsx:867`, `a RATE_LIMITED wire code renders the HONEST rate-limit copy`) and is simply not rostered for this route. This is WIRING, not authoring (CONTEXT correction 5). The other three limiter routes are plan 05.
(5) `DRAFT_ALREADY_EXISTS`'s cause ("with the same API key") is true on the TOCTOU and credential arms and false on the stale-session path, where the collision is the `strategies_user_wizard_session_source_uniq` index (`20260728120000:167`). SPLIT by minting `DRAFT_SESSION_COLLISION` and choosing between the two at the preselect 23505 arm by reading which key the colliding draft holds. ⛔ Scope fence: the FUNCTIONAL fix (minting a fresh session id / re-resolving to the found draft) is Phase 164.2.1 SESSIONID-FENCE — this plan only chooses which TRUE sentence to show and still answers 409.

Purpose: no refusal blames a party that did not cause the failure.
Output: two minted members, three emitter edits, five roster laws moved, pins flipped not deleted, neuters observed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-CONTEXT.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-PATTERNS.md

Read first (PATTERNS §5 carries the excerpts and the five-law table): `wizardErrors.ts:136-142` (`"KEY_MISSING_REQUIRED_FIELD"` — union, dated-comment idiom), lines 1919-1936 (DRAFT_ALREADY_EXISTS — note its two preselect-only bullets already describe the "belongs to a different key of yours" case), lines 3326-3335 (RATE_LIMITED), lines 3353-3364 (SEAM_MISCONFIGURED, for the copy register); `wizardErrors.test.ts` — ⛔ NUL byte at line 1572: `grep` and `ugrep` are SILENTLY BLIND to this file and exit 1; use `grep -an`, `LC_ALL=C sed -n`, or the Edit tool. Sites: lines 2016-2030 and lines 2496-2502 (`EXPECTED_TABLE_SIZE = 90` and their docblocks), lines 3844-3880 (divergence law); `src/lib/wizardErrors.invariant.test.ts:240-256` (`expectedSites: 13`), lines 1994-2060 (the 409 hand-typed set, the shape the 429 twin copies); `src/app/api/strategies/create-with-key/route.ts:465-500` (`ONE GUARD FOR BOTH FIELDS`), lines 600-700 (reuse arm: `admin` client in scope, RPC `create_wizard_strategy_for_key`, the SQLSTATE discrimination comment), lines 884-900, lines 1375-1390 (code-first literal + "roster row owed by hand" precedent), lines 1436-1442; `src/app/api/strategies/create-with-key/route.test.ts:1236-1243`, lines 1263-1270 (credential arm — STAY), lines 3951-3958, lines 3965-3972 (reuse arm — FLIP), the fixtures lines 3173-3323, and the inverted-pin idiom at lines 1695-1709; `ConnectKeyStep.tsx:272-300`; `ConnectKeyStep.preselect-refusal-class.test.tsx:181-187`, lines 445-470 (every code the route can emit on the preselect path is rendered and swept for unpainted controls — the two new codes enter that population automatically and their copy must survive it).

Anti-vacuity harness: `cp` byte backup, `shasum -a 256` before/after, restore by `cp`, NEVER `git checkout --`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mint PRESELECT_REQUEST_INVALID and DRAFT_SESSION_COLLISION; move the copy-table laws</name>
  <files>src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts, src/lib/wizardErrors.invariant.test.ts</files>
  <behavior>
    - `WIZARD_ERROR_COPY.PRESELECT_REQUEST_INVALID` exists: title and cause speak of OUR request being malformed on the reuse path (no field, no credential, no exchange named); fix bullets: reload the page and press Continue again; if it repeats, email security@ with the correlation id. `actions` mirror `UNKNOWN`'s recoverable set only if a Retry control is painted on the preselect surface (read `ConnectKeyStep.preselect-refusal-class.test.tsx:451-470`, `it.each(PRESELECT_REACHABLE)`, for the rule before choosing).
    - `WIZARD_ERROR_COPY.DRAFT_SESSION_COLLISION` exists: title "A draft from an earlier session is still open."; cause names the collision honestly — a draft opened under this wizard session (the same session token, not the same key) is still in progress and this screen cannot reach it; fix bullets carry over the two preselect-only bullets from DRAFT_ALREADY_EXISTS (lines 1926-1929) as ALWAYS-on (this code only ever renders on the preselect path), so no `fixRequires` is needed. Cross-reference Phase 164.2.1 in the entry's comment as the owner of the functional fix.
    - `DRAFT_ALREADY_EXISTS`'s `cause` is byte-unchanged (it is true on the arms that keep it).
    - Both `EXPECTED_TABLE_SIZE` pins read 92; the divergence test (lines 3844-3880) still finds exactly two declarations with one value; each pin's docblock sentence ("90 is what … left") is re-written per L3's rule ("re-run EACH docblock's reasoning") to say 92 and name this plan.
    - `EXPECTED_409_CODES` (`src/lib/wizardErrors.invariant.test.ts:2017-2022`, `"KEY_REUSE_UNAVAILABLE"`) gains `DRAFT_SESSION_COLLISION` (hand-typed 4 -> 5) with a dated comment in the file's own shape.
    - A NEW hand-typed twin beside it, `[164.2-04] create-with-key's 429 refusals clear ConnectKeyStep's roster too`: `deriveEmittedCodes(stripped(route.route), "429")` — but read `rateLimitDenyJson`'s call shape (`throttledBody: { code: "RATE_LIMITED", … }` inside a call, NOT a `NextResponse.json` literal) and `emitterRe` before assuming the scanner can see it; if it cannot, the twin instead asserts by a hand-typed `EXPECTED_429_CODES = ["RATE_LIMITED"]` that (a) the route source contains `code: "RATE_LIMITED"` exactly twice (count on the comment-stripped source) and zero `code: "KEY_RATE_LIMIT"`, and (b) the roster contains every member. Include the file's "NOT VACUOUS — population has a floor" positive control.
  </behavior>
  <action>
    Union (`wizardErrors.ts:141`, `"KEY_MISSING_REQUIRED_FIELD"`, and its neighbourhood): add both members with a dated comment in the lines 136-140 shape naming Phase 164.2 criteria 4/5. Copy entries: place `PRESELECT_REQUEST_INVALID` beside `KEY_MISSING_REQUIRED_FIELD`'s entry and `DRAFT_SESSION_COLLISION` beside `DRAFT_ALREADY_EXISTS` (line 1919). Every sentence must be true on the ONE arm that renders it; do not name a control that the preselect surface does not paint. `wizardErrors.test.ts`: edit the two `EXPECTED_TABLE_SIZE` lines with the Edit tool (byte-exact; the file's NUL byte must survive — verify with `LC_ALL=C sed -n '1572p' src/lib/wizardErrors.test.ts | od -c | head -2` before and after that the NUL is still there) and re-write both docblocks. Invariant test per `<behavior>`; keep `expectedSites: 13` and its comment accurate (the reuse arm still contributes exactly ONE 400 emitter — now `PRESELECT_REQUEST_INVALID` — update the lines 247-255 prose to say so).
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.test.ts src/lib/wizardErrors.invariant.test.ts && npx tsc --noEmit</automated>
    <fails_when>vitest exits non-zero — `wizardErrors.test.ts` fails because an `EXPECTED_TABLE_SIZE` pin still reads 90 against a 92-entry table, or the two declarations diverge (the divergence test names both), or the NUL byte at line 1572 was destroyed by a non-byte-exact edit and the file no longer parses; `wizardErrors.invariant.test.ts` fails on `EXPECTED_409_CODES` still at four, on the new 429 twin (roster lacks `RATE_LIMITED`, or the route still emits `KEY_RATE_LIMIT` — EXPECTED between Tasks 1 and 2, as `<done>` states), or on `expectedSites: 13` if the reuse arm no longer contributes exactly one 400 emitter; or `tsc` exits non-zero (a union member added without its `WIZARD_ERROR_COPY` entry, or the reverse — the `Record<WizardErrorCode, …>` type names the missing key). Because the invariant suite is expected red until Task 2 lands, a red here is a defect only once Task 2 is in</fails_when>
  </verify>
  <done>Both test files green with the table at 92; the invariant suite is RED on the roster/scanner laws until Task 2 lands (expected — run the pair again after Task 2), or green if the executor lands Tasks 1 and 2 in one commit, which is the recommended shape ("all laws move in the same commit").</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The three emitter edits, the roster rows, and the flipped route pins</name>
  <files>src/app/api/strategies/create-with-key/route.ts, src/app/api/strategies/create-with-key/route.test.ts, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx</files>
  <behavior>
    - line 3957 and line 3971 (reuse-arm 400s) FLIP to `PRESELECT_REQUEST_INVALID` with the lines 1695-1709 inverted-pin comment (what the pin used to assert, why that was the defect — the guard comment at lines 470-475 said so — and that the reversal is deliberate). line 1242 and line 1269 (credential arm) are untouched and stay green.
    - New: the preselect limiter (line 494) and the credential limiter (line 900) answer 429 with `{ code: "RATE_LIMITED", error: "Too many requests" }` — `code` FIRST, as a literal; `Retry-After` and `NO_STORE_HEADERS` unchanged; the 503 `SEAM_MISCONFIGURED` arm unchanged. Find the existing 429 tests in `route.test.ts` (grep `KEY_RATE_LIMIT`) and flip them the same way.
    - New: preselect 23505 with a colliding draft whose `api_key_id` EQUALS `reuse_api_key_id` -> 409 `DRAFT_ALREADY_EXISTS`; with a DIFFERENT `api_key_id` -> 409 `DRAFT_SESSION_COLLISION`; with the read faulting -> 409 `DRAFT_SESSION_COLLISION` (the session collision is the constraint that actually fired, so that sentence is true without the read). Mock the `admin.from("strategies")` chain the way the file's existing fixtures at lines 3173-3323 mock the reuse arm.
    - line 1440 credential-arm 23505 still answers `DRAFT_ALREADY_EXISTS` (existing test stays green).
  </behavior>
  <action>
    `route.ts`: (i) lines 476-481 -> `{ code: "PRESELECT_REQUEST_INVALID", error: "wizard_session_id and reuse_api_key_id must be uuids" }`, status 400, and rewrite the lines 470-475 comment to record that the contradiction it described is closed. (ii) line 494 and line 900 -> `throttledBody: { code: "RATE_LIMITED", error: "Too many requests" }`; rewrite lines 891-897 ("our outage, blamed on their exchange") to record that the limiter arm now says so, and add the "roster row owed by hand" note in the lines 1383-1387 shape (429 is outside the `statusRe: "400"` derived population). (iii) lines 666-672: after the 23505, read `admin.from("strategies").select("api_key_id").eq("user_id", user.id).eq("wizard_session_id", wizardSessionId).eq("source", <the value the RPC inserts — read create_wizard_strategy_for_key's latest migration body and the index at 20260728120000:167 to pick it>).maybeSingle()`; equal key -> the existing `DRAFT_ALREADY_EXISTS` response byte-unchanged; otherwise `{ code: "DRAFT_SESSION_COLLISION", error: "A draft from an earlier wizard session is still open." }` 409 `NO_STORE_HEADERS`. Log the branch taken with `console.warn` naming the correlation id. Comment: this is the COPY half; Phase 164.2.1 owns the fence.

    `ConnectKeyStep.tsx:272-300` (`"VENUE_ALREADY_CONNECTED"`): add `PRESELECT_REQUEST_INVALID`, `DRAFT_SESSION_COLLISION`, `RATE_LIMITED`, each with a comment in the lines 289-299 shape (the UNKNOWN-fallthrough trap written out, dated, naming this plan). `KEY_MISSING_REQUIRED_FIELD` STAYS (five credential-arm emitters).

    Then run `ConnectKeyStep.preselect-refusal-class.test.tsx` — the two new codes enter `PRESELECT_REACHABLE` automatically; if either fails the "names only painted controls" sweep, fix the COPY (never the sweep).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/create-with-key/route.test.ts "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.preselect-refusal-class.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx" src/lib/wizardErrors.invariant.test.ts src/lib/wizardErrors.test.ts src/lib/envelope.test.ts "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx" && npx tsc --noEmit</automated>
    <fails_when>vitest exits non-zero — `route.test.ts` still expects `KEY_MISSING_REQUIRED_FIELD` on the reuse-arm 400s (pins not inverted) or `KEY_RATE_LIMIT` on the 429s; a new 23505 case gets `DRAFT_ALREADY_EXISTS` for a DIFFERENT `api_key_id` or for a faulting read (the `admin.from("strategies")` read is skipped or its result ignored), or the credential-arm 400/23505 tests move (that arm was touched); the preselect-refusal-class sweep rejects `PRESELECT_REQUEST_INVALID` or `DRAFT_SESSION_COLLISION` for naming a control the preselect surface does not paint; `ConnectKeyStep.test.tsx` / `SubmitStep.test.tsx` break on the roster edit; the invariant roster laws fail (`expectedSites`, the 429 twin, or the 400-population scan finding an un-rostered code); `envelope.test.ts` fails because the `KEY_RATE_LIMIT` envelope entry was edited; or `tsc` exits non-zero (the `.maybeSingle()` chain's typing, or a roster `as const` set no longer assignable to its type)</fails_when>
  </verify>
  <done>Every listed suite green; `grep -c '"KEY_RATE_LIMIT"' src/app/api/strategies/create-with-key/route.ts` is 0; `envelope.test.ts:78-90` (KEY_RATE_LIMIT's envelope) unchanged and green because the entry is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Neuter-and-observe the roster laws and the split, byte-verified</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/api/strategies/create-with-key/route.ts</files>
  <action>
    Neuters (fresh `cp` backup each; `shasum -a 256` before/after; restore by `cp`; never `git checkout --`):
    1. Remove `"RATE_LIMITED"` from `KNOWN_CREATE_WITH_KEY_CODES`. Expected RED: the new 429 twin in `wizardErrors.invariant.test.ts`, by name. (This is the pin that makes the "owed by hand" roster row enforceable.)
    2. Remove `"PRESELECT_REQUEST_INVALID"` from the roster. Expected RED: the `[142.2-07 / MT5-04]` roster law (`expectedSites` derivation finds an un-rostered 400 code).
    3. In `route.ts`, make the 23505 arm always answer `DRAFT_ALREADY_EXISTS` (skip the read). Expected RED: the DIFFERENT-key and read-fault cases from Task 2, by name.
    Record each FAIL block and hash pair in the SUMMARY under `## Anti-vacuity observations`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.invariant.test.ts src/app/api/strategies/create-with-key/route.test.ts && shasum -a 256 "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx" src/app/api/strategies/create-with-key/route.ts</automated>
    <fails_when>vitest exits non-zero (a roster member is still missing after the restore, or the 23505 arm still skips the read — the invariant law or the DIFFERENT-key / read-fault cases are RED on the files as left); or `shasum` exits non-zero (either file missing at its path). ⚠️ CANNOT FAIL on the task's own claim: `shasum -a 256` PRINTS the post-restore hash and exits 0 for any readable file — it compares nothing, so "the restore hash differs from the pre-neuter hash" is NOT a failure this command can detect; that equality is established only by the executor reading the before/after pair it records in the SUMMARY. Likewise "a neuter left the suite GREEN" (the vacuity this task exists to catch) is observed during the task's manual RED runs and never by this command, which runs on the restored file.</fails_when>
  </verify>
  <done>Three RED observations recorded; hashes restored; suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> create-with-key route | user-supplied `wizard_session_id`, `reuse_api_key_id` |
| route -> strategies read (service role) | the post-23505 read of the colliding draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.2-06 | Information Disclosure | DRAFT_SESSION_COLLISION read | medium | mitigate | the read is scoped by `user_id = user.id` and returns only `api_key_id`, which is compared and never rendered; the response body carries no key id |
| T-164.2-07 | Elevation of Privilege | preselect 400 guard | low | accept | the guard is unchanged in behaviour (still refuses non-uuids before any read); only the code/copy changes |
| T-164.2-08 | Denial of Service | RATE_LIMITED arms | low | accept | the 429 body shape, Retry-After and headers are byte-compatible; only the code token changes |
| T-164.2-SC | Tampering | npm installs | low | accept | no package installed |
</threat_model>

<verification>
- All suites in Task 2's verify green; `npx tsc --noEmit` green.
- `LC_ALL=C sed -n '1572p' src/lib/wizardErrors.test.ts | od -c | head -1` still shows the NUL byte.
- SUMMARY: three anti-vacuity observations.
</verification>

<success_criteria>
Criterion 4 (preselect guard wears no KEY_* verdict; create-with-key's limiter says the cap is ours) and criterion 5 (DRAFT_ALREADY_EXISTS split, true on every arm that renders it).
</success_criteria>

<output>
Create `.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-04-SUMMARY.md` when done.
</output>
