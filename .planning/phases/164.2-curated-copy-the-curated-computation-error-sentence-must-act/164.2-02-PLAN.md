---
phase: 164.2-curated-copy-the-curated-computation-error-sentence-must-act
plan: 02
type: execute
subsystem: freshness
tags: [WR-06-UTC, HONEST-08-RESIDUAL, freshness, factsheet-chip, sync-badge, anti-vacuity]
wave: 1
depends_on: []
files_modified:
  - src/lib/freshness.ts
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/components/strategy/SyncBadge.staler-of-two.test.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.chip-honesty.test.tsx
  - src/lib/freshness.two-surfaces.test.tsx
autonomous: true
requirements: [WR-06-UTC, HONEST-08-RESIDUAL]
provides:
  - "SERIES_END_FUTURE_ALLOWANCE_DAYS exported from src/lib/freshness.ts and imported by BOTH bucketers"
  - "A joint two-surface test rendering SyncBadge and FreshnessChip from ONE row"
  - "The FreshnessChip twin of staler-of-two Test 2 (sync arm binds over a fresh series)"
affects: [164.2-10]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A series_end up to SERIES_END_FUTURE_ALLOWANCE_DAYS ahead of the browser clock buckets `fresh` on BOTH surfaces (badge dot+sentence, factsheet chip); beyond it, `future` on both"
    - "The allowance applies to the SERIES arm only: a future computedAt still reads `future — check data` (chip-honesty C-10 stays green)"
    - "Each bucketer is proven by a test that goes RED with the allowance removed, and one test renders both surfaces from one row and asserts they agree"
    - "FreshnessChip binds to the SYNC copy (eyebrow `Computed`) on a row whose series is fresh and whose sync is staler; the test goes RED when the strict `>` at FactsheetView.tsx:1207 becomes `>=`"
    - "A live observation of such a published PROD row is recorded in the SUMMARY beside the test, not instead of it"
  artifacts:
    - path: "src/lib/freshness.ts"
      provides: "export const SERIES_END_FUTURE_ALLOWANCE_DAYS = 1 with a docblock in the CLOCK_SKEW_TOLERANCE_MINUTES shape; bucketSeriesAge consumes it"
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "the series call of bucketByAge honours the imported allowance; the computedAt call does not"
    - path: "src/lib/freshness.two-surfaces.test.tsx"
      provides: "one row -> SyncBadge + FactsheetBody chip -> same verdict, hand-typed matrix"
  key_links:
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "src/lib/freshness.ts"
      via: "import { SERIES_FRESH_DAYS, SERIES_STALE_DAYS, SERIES_END_FUTURE_ALLOWANCE_DAYS } from \"@/lib/freshness\" (extend line 9)"
      pattern: "SERIES_END_FUTURE_ALLOWANCE_DAYS"
---

<objective>
WR-06-UTC (criterion 7): BOTH bucketers — `bucketSeriesAge` in `src/lib/freshness.ts:207-218` (`function bucketSeriesAge`; today a 5-MINUTE clock-skew allowance) and `bucketByAge` in `src/app/factsheet/[id]/v2/FactsheetView.tsx:1108-1115` (`function bucketByAge`; today none) — take a DAY-granularity allowance for a future-dated `series_end` from ONE exported constant, imported rather than re-typed, the convention `freshness.ts:23-30` (`EXPORTED since 163-REVIEW`) already states. A `series_end` is a UTC DATE: a venue that stamps today's bar can legitimately sit up to a day "ahead" of a browser west of UTC, which is not a corrupt write. Fixing one bucketer manufactures a new two-surface contradiction, which is the class this work exists to close; TODOS asks for "one commit, with a test that renders both surfaces from one row and asserts they agree" — honour the shape.

HONEST-08-RESIDUAL (criterion 8): the badge side is already proven (`SyncBadge.staler-of-two.test.tsx:62-71`, `Synced 3d ago`: sync arm binds over a 1-day-old series). The missing piece is the `FreshnessChip` twin, plus one live PROD observation recorded alongside it.

Purpose: two of the nine criteria, both about the same two surfaces.
Output: the constant, both bucketers, five tests (badge allowance arm + moved control; chip allowance arm; chip HONEST-08 twin; joint two-surface), every one observed RED under neuter.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-CONTEXT.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-PATTERNS.md

Read first (PATTERNS §8, §9 carry the excerpts): `src/lib/freshness.ts:15-30` (exports, `CLOCK_SKEW_TOLERANCE_MINUTES`), lines 207-218 (`bucketSeriesAge`), lines 233-241 and lines 268-300 (`resolveEffectiveRecency`, the "older date wins" reasoning and the SERIES-arm copy); `src/components/strategy/SyncBadge.tsx` in full (find every consumer of `CLOCK_SKEW_TOLERANCE_MINUTES` — `timeAgo` at line 44 is the SYNC copy and keeps the 5-minute skew; any series-line copy path must move to the day allowance or `staler-of-two:206` "dot and sentence agree" will catch the contradiction); `FactsheetView.tsx:9`, lines 1108-1115, lines 1186-1209; `SyncBadge.staler-of-two.test.tsx:189-338` (whole WR-06 block; line 248 +30 s grace, line 269 control, line 287 stale control); `FactsheetView.chip-honesty.test.tsx:93-176` (helpers `isoHoursAgo`, `payloadWith`, `renderFactsheet`, `readChip`, `NEGATIVE`, `MUTED`), lines 261-269 (C-4), lines 340-349 (C-10).

Anti-vacuity harness for every test: `cp` byte backup, `shasum -a 256` before/after, restore by `cp`, NEVER `git checkout --`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One constant, both bucketers, badge-side tests (allowance arm + moved control)</name>
  <files>src/lib/freshness.ts, src/app/factsheet/[id]/v2/FactsheetView.tsx, src/components/strategy/SyncBadge.staler-of-two.test.tsx</files>
  <behavior>
    - NEW positive arm beside line 248: `seriesEnd = Date.now() + HOUR` (one hour ahead) with `computedAt = agoIso(2 * HOUR)` renders the FRESH dot class and no `/Track record ends in the future/` sentence. This is the test that goes RED with the allowance removed.
    - The line 269 GRACE CONTROL is MOVED, not deleted: fixture becomes `Date.now() + 2 * DAY` (beyond the allowance), the two assertions (`bg-text-muted`, `/Track record ends in the future/i`) stay, and the comment records — in the `portfolio-optimizer/route.test.ts:440-462` (`INVERTED rather than deleted`) shape — that the old `+HOUR` fixture pinned a behaviour WR-06-UTC reversed and now lives in the positive arm above.
    - line 248 (+30 s), line 287 (112-day stale), line 206 (dot and sentence agree), line 220 (badge agrees with chip) all stay green unchanged.
  </behavior>
  <action>
    `freshness.ts`: add `export const SERIES_END_FUTURE_ALLOWANCE_DAYS = 1;` immediately after `CLOCK_SKEW_TOLERANCE_MINUTES` (line 30) with a docblock in the same shape explaining (i) `series_end` is a UTC date so a same-day bar can be up to a day ahead of a western browser, (ii) it is EXPORTED so both bucketers import it rather than re-type it (163-REVIEW / WR-06 convention), (iii) it applies to the SERIES arm only — the sync arm (`computedAt`) keeps `CLOCK_SKEW_TOLERANCE_MINUTES`. In `bucketSeriesAge` (lines 210-213) replace the minutes test with `-days <= SERIES_END_FUTURE_ALLOWANCE_DAYS ? "fresh" : "future"` (a day subsumes the five minutes on this arm). Then read `resolveEffectiveRecency` and `SyncBadge.tsx`: wherever the SERIES sentence ("Track record ends in the future") is chosen by a `CLOCK_SKEW_TOLERANCE_MINUTES` comparison, switch that comparison to the day allowance so dot and sentence cannot disagree; leave the sync-arm `timeAgo` untouched.

    `FactsheetView.tsx`: extend the line 9 import with `SERIES_END_FUTURE_ALLOWANCE_DAYS`; at the SERIES call (lines 1192-1194) apply the allowance — give `bucketByAge` an optional second parameter `futureAllowanceDays = 0` and pass the constant ONLY from the series call so the line 1188 computedAt call is byte-unchanged (C-10 is the wrong-scope tripwire and must stay green). Update the lines 1109-1110 comment (NEW-C20-07) to say the series arm tolerates a day and why.

    Tests in `SyncBadge.staler-of-two.test.tsx` per `<behavior>`; write them first, observe the new arm RED against the old bucketer, then implement.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/SyncBadge.staler-of-two.test.tsx "src/app/factsheet/[id]/v2/FactsheetView.chip-honesty.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>Both files import the one constant; badge allowance arm green; moved control green with the inverted-pin comment; chip-honesty C-1..C-12 unchanged and green (C-10 in particular).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chip-side allowance arm, the HONEST-08 chip twin, and the joint two-surface test</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.chip-honesty.test.tsx, src/lib/freshness.two-surfaces.test.tsx</files>
  <behavior>
    - chip-honesty C-13 (WR-06-UTC): a payload whose series ends ONE HOUR AHEAD with a fresh computedAt reads `Computed · fresh` (or whatever the fresh label is — read `readChip` and the label table before typing it), NOT a `Track record · future` chip. RED with the allowance removed from `FactsheetView.tsx`.
    - chip-honesty C-14 (WR-06-UTC control): series TWO DAYS ahead -> the future chip binds (`Track record · future …`). Stays green in both states of the allowance; it is the chip's control.
    - chip-honesty C-15 (HONEST-08-RESIDUAL, "the chip twin of staler-of-two:62"): computedAt 3 days ago, series 1 day ago -> eyebrow SUBJECT is `Computed`, label does NOT start with `Track record`, tone is the job's (NEGATIVE per C-4's ladder — confirm against `TONE_RANK`). Neuter: `>` -> `>=` at `FactsheetView.tsx:1207` (`TONE_RANK[seriesTone] > TONE_RANK[jobTone]`) -> subject flips to `Track record` -> RED.
    - `src/lib/freshness.two-surfaces.test.tsx`: a hand-typed matrix of rows `{ computedAt, seriesEnd }` — series +1 h ahead, +2 d ahead, 1 d ago, 30 d ago, each with computedAt 2 h ago — and for EACH row renders `<SyncBadge computedAt seriesEnd />` (the `staler-of-two:63-65` shape) AND the chip via the chip-honesty harness (`FactsheetProvider` + `FactsheetBody` + `buildScenarioFactsheetPayload`, `payloadWith`-style with the SAME computedAt and a `seriesDates` array ending at the SAME seriesEnd), then asserts agreement on two booleans derived from each surface's text: `namesTrackRecord` (badge text matches `/Track record/i`; chip label starts with `Track record`) and `saysFuture` (badge `/in the future/i`; chip label contains `future`). Include a positive-control assertion that the matrix has exactly 4 rows (hand-typed) and that at least one row is expected to name Track record and at least one is not, so the agreement check cannot pass on an all-false matrix.
  </behavior>
  <action>
    Extend chip-honesty with C-13, C-14, C-15 using the file's own helpers (`payloadWith(seriesEndDaysAgo, computedAt)` takes days-ago — a negative value or a new helper gives a future series end; open lines 129-146 before deciding). Give C-15's `it` title the words "chip twin of staler-of-two:62" so the pairing is legible. For the joint file, copy the harness setup lines from chip-honesty (lines 1-7 imports, the `vi.mock`s it needs, `renderFactsheet`, `readChip`) rather than importing test-private helpers; the `it.each` title carries the row description. Place it in `src/lib/` beside `freshness.ts` (the jsdom project's INCLUDE covers `src/**`; confirm with one `npx vitest run` on the path).
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetView.chip-honesty.test.tsx" src/lib/freshness.two-surfaces.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>C-13/C-14/C-15 and the joint matrix green; the joint test's positive control proves the matrix is non-degenerate.</done>
</task>

<task type="auto">
  <name>Task 3: Neuter-and-observe, byte-verified; then the HONEST-08 live observation</name>
  <files>src/lib/freshness.ts, src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <action>
    Neuters (fresh byte backup each; `shasum -a 256` before/after; restore by `cp`; never `git checkout --`):
    1. `freshness.ts`: revert `bucketSeriesAge` to the 5-minute comparison. Expected RED: the badge +HOUR arm and the joint matrix's +1 h row. Record.
    2. `FactsheetView.tsx`: pass `0` instead of the constant at the series call. Expected RED: C-13 and the joint +1 h row; C-10 and C-14 stay green. Record.
    3. `FactsheetView.tsx:1207` (`TONE_RANK[seriesTone] > TONE_RANK[jobTone]`): the strict greater-than becomes greater-or-equal. Expected RED: C-15 (and possibly C-4 — record whichever fire). Record.
    Each restore GREEN. Write the RED excerpts and shasum pairs into the SUMMARY under `## Anti-vacuity observations`.

    Live observation (criterion 8 names a measurement, not instead of the test): in a real browser open `https://quantalyze.xyz/discovery/crypto-sma` (the default public cohort, `src/proxy.ts:25`, `/discovery/crypto-sma`), find a published strategy whose track record ended within the last ~3 days, open its `/factsheet/<id>`, and read the FreshnessChip eyebrow + label. Record the URL, the date, the chip text, and whether the eyebrow says `Computed` (sync arm binding) in the SUMMARY under `## HONEST-08 live observation`. If no such row is public on the day, enumerate the other cohort slugs under `src/app/discovery/` and try each; if still none, record `NOT OBSERVED — no public row with a fresh series on <date>` verbatim and do NOT substitute a PROD query (the criterion binds to the rendered chip). Never write to PROD.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/SyncBadge.staler-of-two.test.tsx "src/app/factsheet/[id]/v2/FactsheetView.chip-honesty.test.tsx" src/lib/freshness.two-surfaces.test.tsx && shasum -a 256 src/lib/freshness.ts "src/app/factsheet/[id]/v2/FactsheetView.tsx"</automated>
  </verify>
  <done>Three RED observations with test names and hash pairs; final hashes equal pre-neuter hashes; suite green; live observation (or its honest absence) recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB row -> rendered freshness claim | a future-dated series_end is trusted up to one day; beyond that it is still flagged `future` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.2-03 | Tampering | bucketSeriesAge / bucketByAge | low | mitigate | the allowance is bounded (1 day) and the control tests (+2 d) prove a corrupt far-future write still renders `future`, never `fresh` |
| T-164.2-04 | Repudiation | live observation | low | accept | the observation is recorded with URL and date; the durable proof is the test |
| T-164.2-SC | Tampering | npm installs | low | accept | no package installed |
</threat_model>

<verification>
- The three test files green; `npx tsc --noEmit` green.
- `grep -n "SERIES_END_FUTURE_ALLOWANCE_DAYS" src/lib/freshness.ts "src/app/factsheet/[id]/v2/FactsheetView.tsx"` shows the export and the import — no second literal `1` re-typed as an allowance.
- SUMMARY has `## Anti-vacuity observations` (three) and `## HONEST-08 live observation`.
</verification>

<success_criteria>
Criterion 7 (both bucketers, one constant, each proven, joint agreement test) and criterion 8 (chip twin proven RED under neuter, live observation recorded).
</success_criteria>

<output>
Create `.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-02-SUMMARY.md` when done.
</output>
