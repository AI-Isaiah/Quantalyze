---
phase: 164.2-curated-copy-the-curated-computation-error-sentence-must-act
plan: 08
type: execute
subsystem: analytics-service-writers
tags: [python, provenance, job_worker, analytics_runner, ast-census, anti-vacuity]
wave: 3
depends_on: ["164.2-06"]
files_modified:
  - analytics-service/services/analytics_runner.py
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_computation_error_provenance_census.py
autonomous: true
requirements: [164.2-SC2]
provides:
  - "Every Python strategy_analytics failure literal (10 of 11; P11 deliberately exempt) carries computation_error_source='writer' and computation_error_job_id in the SAME dict; both success resets carry both as None"
  - "run_csv_strategy_analytics takes keyword-only job_id: str | None = None, passed only by job_worker.py:2188"
  - "An AST census (sibling of test_computing_started_at_stamp.py) with hand-typed floors that reds when any literal drops a marker"
affects: [164.2-10]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A writer that sets computation_error without the marker is the failure mode the census catches: delete the two keys from ONE literal -> the census names that file:line -> RED"
    - "P11 (analytics_runner.py:2110-2120, the PostgREST schema-cache-miss RE-ISSUE arm) stays marker-free ON PURPOSE — it exists for the deploy window in which PostgREST does not yet know a NEW column, and this phase adds two new columns; its exact key set is what test_computing_started_at_stamp.py:161-170 exempts, so it is untouched and both censuses agree on it"
    - "run_csv_strategy_analytics's three non-job callers are byte-unchanged (job_id defaults to None -> marker NULL -> today's behaviour, correct for a pre-job failure)"
    - "mypy --strict and the analytics-service suite are green, run FROM analytics-service/ with python3"
  artifacts:
    - path: "analytics-service/tests/test_computation_error_provenance_census.py"
      provides: "reuses tests/_scan_helpers.py; EXPECTED_STAMPED_SITES = 10, EXPECTED_EXEMPT_SITES = 1 (P11 by exact key set), EXPECTED_RESET_SITES = 2, all hand-typed"
  key_links:
    - from: "analytics-service/services/job_worker.py:2188"
      to: "analytics-service/services/analytics_runner.py:1178"
      via: "job_id=job.get(\"id\") on the one job caller of run_csv_strategy_analytics"
      pattern: "job_id="
---

<objective>
Criterion 2's Python half — "Every Python writer stamps it" (CONTEXT Area 1), as corrected by RESEARCH (CONTEXT correction 4): the census is ELEVEN `strategy_analytics` failure writers, all dict literals (P1-P5 in `job_worker.py`, P6-P11 in `analytics_runner.py`), NOT `routers/portfolio.py:722-734` (it writes `portfolio_analytics`, which has no bridge — verified out of the class, say so in the census docstring) and NOT the four TypeScript pre-enqueue writers (branch (a) correctly supersedes them; they leave the marker NULL).

The dict-literal shape is load-bearing: `analytics_runner.py:1340-1349` records that the JOB-01 census can only read a literal, and a helper returning a dict made five writers invisible to it. So the marker is two keys typed into each literal, never a wrapper.

Purpose: the SQL bridge (plan 06) can only respect provenance the writers actually set.
Output: ten stamped literals, two reset literals, one threaded parameter, one census observed RED under neuter.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-CONTEXT.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-RESEARCH.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-PATTERNS.md

Read first (PATTERNS §3-§4, RESEARCH Finding 2's table): `job_worker.py` P1 lines 2094-2110, P2 lines 2979-2993, P3 lines 3013-3027, P4 lines 6159-6171, P5 lines 6187-6198, the success reset lines 7770-7778, the caller lines 2186-2194, and lines 9436-9451 (why the bridge is not called pre-mark); `analytics_runner.py` signature lines 1178-1192, lines 1333-1362 (`_guard_failure_payload_inplace` and the in-place rule), P6 lines 1468-1483, P7 lines 1604-1618, the success reset lines 1868-1872, P8 lines 1972-1990, P9 lines 2018-2032, P10 lines 2072-2085, P11 lines 2104-2122; `tests/test_computing_started_at_stamp.py:161-170` (`_STAMP_OMISSION_EXEMPT_KEYS`, the exempt key set), lines 1-60 (docstring), lines 100-120 (D-10: helpers live in `tests/_scan_helpers.py`), lines 448-470 (`_dict_value`, `_has_key`, `_is_none`, `_payload_keys`), lines 504-530 (`_scan_python`, `test_python_status_writers_stamp_and_clear`), lines 709-715 (hand-typed floors).

⚠️ Python runs FROM `analytics-service/` with `python3` (a repo-root pytest misses VCR cassettes and makes LIVE broker calls). Deploy order (RESEARCH Pitfall 8 / Runtime State Inventory): the migration must be on PROD before a worker sends the new keys or PostgREST answers a schema-cache miss and the failure upsert is LOST — Railway deploys after main CI, the migration auto-applies on the same merge, and the P11 fallback covers only P10's arm. Record this in the SUMMARY's deploy notes; plan 10 carries the post-merge check.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread job_id into run_csv_strategy_analytics; stamp P6-P10 and the runner reset</name>
  <files>analytics-service/services/analytics_runner.py, analytics-service/services/job_worker.py</files>
  <behavior>
    - `run_csv_strategy_analytics(strategy_id, *, refresh_source=None, refresh_publish_status=None, refresh_publish_warned=False, job_id: str | None = None)` — keyword-only, default None, docblock in the lines 1186-1192 `refresh_source` shape naming Phase 164.2 and stating the NULL semantic.
    - P6, P7, P8, P9, P10 each gain, INSIDE the literal beside `"computation_error"`, `"computation_error_source": "writer"` and `"computation_error_job_id": job_id`.
    - The line 1870 success reset gains `"computation_error_source": None, "computation_error_job_id": None` beside `"computation_error": None`.
    - P11 (lines 2110-2116) is byte-unchanged; add a comment INSIDE its enclosing block (not inside the literal — the key set must stay exact) saying why it is marker-free: it re-issues with the minimal key set PostgREST knew before the newest column, and this phase adds two new columns, so adding them here would defeat the arm on the very window it exists for; both censuses exempt it by exact key set.
    - `job_worker.py:2188` (`await run_csv_strategy_analytics(`) passes `job_id=job.get("id")`; no other caller changes (grep `run_csv_strategy_analytics(` across `analytics-service/` — the CSV-first wizard route and the composite finalizer stay byte-unchanged).
  </behavior>
  <action>
    Implement per `<behavior>`. `_guard_failure_payload_inplace` (line 1333) mutates the literal in place and must keep doing so; if it has a key allow-list, extend it. Run `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` from `analytics-service/` and the two existing writer suites: `tests/test_job_worker.py` (pins the scrub and curated sentences) and `tests/test_derive_silent_failure_f1_f4_f7.py`, `tests/test_stitch_composite_job.py` — their fake-Supabase upsert captures will now carry two more keys; where a test asserts an upsert payload with `==` on the whole dict, extend the expected dict (the marker IS the behaviour under test, so it belongs in the expectation) rather than loosening the assertion.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/ && python3 -m pytest tests/test_job_worker.py tests/test_derive_silent_failure_f1_f4_f7.py tests/test_stitch_composite_job.py tests/test_computing_started_at_stamp.py -x -q</automated>
    <fails_when>`cd` fails when the command is not run from the repo root (the path is relative, and the `cd` is load-bearing: from anywhere else pytest misses its VCR cassettes and makes live broker calls); `mypy` exits 1 printing `error:` lines (the new keyword-only `job_id: str | None` parameter, or `job.get("id")` typed `Any`/`object` where `str | None` is expected); `pytest -x` exits non-zero at the FIRST failure — a `==` whole-dict assertion on a captured upsert now sees two extra keys it was not extended with, `_guard_failure_payload_inplace` strips the new keys (allow-list not extended) so a payload lacks them, or the JOB-01 census reports `EXPECTED_STAMP_OMISSION_EXEMPT` ≠ 1 because P11's key set moved; or a listed test file is absent (pytest exit 4, `file or directory not found`)</fails_when>
  </verify>
  <done>mypy clean; the four suites green (the JOB-01 census still reports `EXPECTED_STAMP_OMISSION_EXEMPT = 1` because P11's key set did not move); the signature carries `job_id`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stamp P1-P5 and the worker reset; write the census</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_computation_error_provenance_census.py</files>
  <behavior>
    - P1-P5 each carry both keys beside `"computation_error"` with `"computation_error_job_id": job.get("id")` (the line 2979 idiom; `job` is in scope in every handler — `run_sync_trades_job(job)` line 1399, `run_derive_broker_dailies_job(job)` line 2554, `run_stitch_composite_job(job)` line 5954; the inner `_upsert*` closures see it). The line 7775 reset carries both as `None`.
    - Census `test_computation_error_provenance_census.py`: imports `_py_scan_files` from `tests._scan_helpers` and the dict helpers (import them from `test_computing_started_at_stamp` if they are module-level there, else copy the four 5-line helpers with a comment naming the source). Rule A: every `strategy_analytics` write literal whose `computation_error` value is a non-`None` expression has BOTH marker keys, `computation_error_source` a `Constant('writer')` and `computation_error_job_id` a non-`None` expression — EXCEPT a literal whose exact key set equals the hand-typed `PROVENANCE_EXEMPT_KEYS` frozenset (P11's five keys, copied from `test_computing_started_at_stamp.py:161-170`), counted separately. Rule B: every literal whose `computation_error` is `None` has both markers `None`. Hand-typed floors: `EXPECTED_STAMPED_SITES = 10`, `EXPECTED_EXEMPT_SITES = 1`, `EXPECTED_RESET_SITES = 2`, each asserted with `==` and a message in the lines 709-715 shape; a positive control that the scan found `> 0` files; the docstring repeats lines 29-38's caveat in its own terms — the Python census is half the invariant, the SQL gate (plan 07) is the other half — and records that `routers/portfolio.py` writes `portfolio_analytics` and is out of the class.
  </behavior>
  <action>
    Implement; run the census and the whole analytics-service suite from `analytics-service/`. ⚠️ Do not run vitest concurrently (memory: contention fakes regressions).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_computation_error_provenance_census.py tests/test_computing_started_at_stamp.py -x -q && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>`cd` fails off the repo root; `pytest -x` exits non-zero — Rule A names a `strategy_analytics` failure literal (file:line) lacking a marker key or whose `computation_error_source` is not `Constant('writer')`, Rule B names a `computation_error: None` reset whose markers are not `None`, a floor mismatches (`EXPECTED_STAMPED_SITES == 10` / `EXPECTED_EXEMPT_SITES == 1` / `EXPECTED_RESET_SITES == 2` read 9/2/1 if P11 was stamped or a site missed), the positive control finds 0 scanned files (the `_py_scan_files` import or path is wrong — the derivation went blind), the JOB-01 census moved, or the new test file is not collected (exit 4 / 5); or `mypy` exits 1 on the P1–P5 / reset edits in `job_worker.py`</fails_when>
  </verify>
  <done>Census green at 10/1/2; JOB-01 census unchanged and green; mypy clean.</done>
</task>

<task type="auto">
  <name>Task 3: Neuter-and-observe the census, byte-verified; full Python suite</name>
  <files>analytics-service/services/analytics_runner.py, analytics-service/services/job_worker.py</files>
  <action>
    Neuters (fresh `cp` backup each; `shasum -a 256` before/after; restore by `cp`; never `git checkout --`):
    1. Delete the two marker keys from P7 (`analytics_runner.py`, the "Insufficient CSV history" literal — the ROADMAP's own example). Expected RED: the census names `analytics_runner.py:<line>` and reports 9 stamped / floor 10. Record verbatim.
    2. Change P2's `"computation_error_source": "writer"` to `"bridge"` (`job_worker.py`). Expected RED: Rule A's value check names the site. Record.
    3. Delete the two `None` markers from the line 7775 reset. Expected RED: Rule B, 1 reset / floor 2. Record.
    Restore, hashes equal, GREEN. Then the full suite once, alone: `cd analytics-service && python3 -m pytest -q`. Write the three observations under `## Anti-vacuity observations` and the deploy-order note under `## Deploy notes` in the SUMMARY.
  </action>
  <verify>
    <automated>cd analytics-service && L=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p08-pytest.log && python3 -m pytest -q > "$L" 2>&1; rc=$?; tail -5 "$L"; test "$rc" = "0" && shasum -a 256 services/analytics_runner.py services/job_worker.py</automated>
    <fails_when>⛔ CANNOT FAIL on the suite: `pytest`'s exit status is discarded by the pipe into `tail -5` (no `pipefail` in the executor's shell) — a red full suite prints its `N failed` summary in the tail and the chain continues; `shasum` then exits 0 for any two readable files and compares nothing. Only `cd` off the repo root or a missing service file breaks the chain. Both claims in `<done>` — suite green, hashes equal — are read by the executor from the printed output (`passed` with no `failed` in the last five lines; the printed hashes equal to the recorded pre-neuter pair), not enforced by the exit status. ⚠️ CANNOT FAIL on the task's own claim: `shasum -a 256` PRINTS the post-restore hash and exits 0 for any readable file — it compares nothing, so "the restore hash differs from the pre-neuter hash" is NOT a failure this command can detect; that equality is established only by the executor reading the before/after pair it records in the SUMMARY. Likewise "a neuter left the suite GREEN" (the vacuity this task exists to catch) is observed during the task's manual RED runs and never by this command, which runs on the restored file.</fails_when>
  </verify>
  <done>Three RED observations recorded with the census's own site-naming output; hashes restored; full analytics-service suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker (service role) -> strategy_analytics via PostgREST | the writers stamp provenance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.2-17 | Denial of Service | failure upsert during the schema-cache window | medium | mitigate | migration merges (auto-applies) before the worker deploys on the same main push; P11's minimal-key re-issue covers P10's arm; the window is recorded in the SUMMARY and plan 10 checks PROD after merge |
| T-164.2-18 | Tampering | job_id from job dict | low | accept | `job["id"]` is the claimed job's own id from the queue RPC, the same value the worker passes to mark_compute_job_failed |
| T-164.2-SC | Tampering | pip installs | low | accept | none |
</threat_model>

<verification>
- Task 3's verify green; mypy clean; census 10/1/2.
- SUMMARY: three anti-vacuity observations, deploy notes.
</verification>

<success_criteria>
Criterion 2's Python half: every in-class writer stamps provenance in the same statement, proven by a census that reds when one does not.
</success_criteria>

<output>
Create `.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-08-SUMMARY.md` when done.
</output>
