---
phase: 164.2-curated-copy-the-curated-computation-error-sentence-must-act
verified: 2026-09-06T17:32:45Z
verified_at_head: 513e7f50
status: passed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 1
overrides:
  - must_have: "WIZFORM-02 is closed by MEASUREMENT, not inspection: for every server-classified error code the wizard can surface, a test asserts the rendered surface shows THAT code and never code: UNKNOWN"
    reason: "Founder decision 2026-09-06 (TODOS.md:100-114, 164.2-10-SUMMARY.md '## WIZFORM-02 left open'): accept criterion 3 on the FOUR surfaces that have a code->copy render path (82 rows, observed RED under two neuters). The fifth locked surface, keys/validate-and-encrypt, has NO code-channel consumer at HEAD (ApiKeyManager.tsx:245, StrategyForm.tsx:187, AllocatorExchangeManager.tsx:583 all read the prose), so a render row there would be a tautology. Its wiring is booked as [WIZFORM-02-VE-CODECHANNEL] (TODOS.md:1121) and the exclusion is pinned by a live assertion (roster-render.test.tsx:1028-1052) that reds the day a consumer reads code. The GATED ON WIZFORM-02 CLOSING marker (TODOS.md:5280) stays down deliberately."
    accepted_by: "founder (recorded by plan 10)"
    accepted_at: "2026-09-06T00:00:00Z"
human_verification:
  - test: "Confirm plan 06 Task 4 (the founder gate on migration 20260906120000 before it reaches a PR) was actually discharged. 164.2-06-SUMMARY.md still reads `status: paused-at-checkpoint # Task 4 (founder gate) still open` and its body says 'Task 4 ... has NOT been reached'; 164.2-10-SUMMARY.md records a founder decision 'taken at the plan-06 gate' but only about criterion 3 / WIZFORM-02, not about approving the migration for PROD."
    expected: "Either an explicit founder approval of the migration exists (then plan 06's SUMMARY frontmatter should be updated to complete), or the gate is still owed and must be passed before /gsd-ship opens the PR — merging supabase/migrations/** to main auto-applies to PROD."
    why_human: "A founder decision is not observable in the codebase; the two SUMMARYs disagree on whether it happened."
  - test: "Read-only confirmation that nothing reached PROD or shared TEST: run `SELECT column_name FROM information_schema.columns WHERE table_name='strategy_analytics' AND column_name LIKE 'computation_error_%';` against PROD and against TEST (after the CLAUDE.md `shobj_description` marker query), or GET `/rest/v1/strategy_analytics?select=computation_error_source&limit=1` on each PostgREST."
    expected: "Only `computation_error` comes back on both (PostgREST answers 400 'column ... does not exist' for computation_error_source). The verifier established this only by inference: the migration is absent from origin/main (0 commits, not in the tree) and the migrate workflow is main-only; a direct PROD probe was blocked by the permission classifier (it would have read the service key from .env) and no TEST credential exists in this checkout."
    why_human: "Direct database observation needs credentials the verifier was not permitted to read."
  - test: "Criterion 8's LIVE clause: on a day when a /browse row has a track record ending within ~3 days (or after deliberately publishing one), open its factsheet and read the FreshnessChip eyebrow."
    expected: "The chip reads `Computed · <sync verdict>` (sync arm binds) rather than `Track record · fresh`. Plan 02 recorded this as `NOT OBSERVED — no public row with a fresh series on 2026-09-06` (164.2-02-SUMMARY.md '## HONEST-08 live observation'); the test half (chip-honesty C-4, RED under the TONE_RANK neuter) is what the TODOS close rests on."
    why_human: "Depends on production data that did not exist on the verification date."
  - test: "Copy quality on three user-visible sentences the phase minted or rewrote: PRESELECT_REQUEST_INVALID and DRAFT_SESSION_COLLISION (wizardErrors.ts:1721, :2048), the RATE_LIMITED / SEAM_MISCONFIGURED arms now reached on four routes, and the anonymous factsheet placeholder (page.tsx:422-426: 'This factsheet has not been computed yet. Some strategies stay in this state, and this page is all there is until one has been computed.')."
    expected: "Each reads as a careful analyst wrote it and is addressed to the reader who actually sees it. The tests prove TRUTH and non-leakage; they cannot prove the sentences read well (plans 03 D3, 04 D5, 05 D6 all flagged this as human judgment)."
    why_human: "Copy quality on a public/marketing-adjacent surface is a founder/design judgment."
  - test: "After merge and deploy: `curl -s https://quantalyze.xyz/factsheet/8581f739-1a7b-42a4-a209-3acfa327e259 | grep -c 'dev-server console'` and `grep -c 'has not been computed yet'`."
    expected: "0 and 1 respectively (pre-fix PROD read on 2026-09-06 was 1 and 0). Also the [164.2-TYPES-REGEN-CHECK] post-merge type regen and the [164.2-TEST-APPLY-PROVENANCE] hand-apply to TEST, both booked."
    why_human: "Post-deploy observation; not available on the branch."
---

# Phase 164.2: CURATED-COPY Verification Report

**Phase Goal:** Every failure sentence a user reads is the true, specific one — (a) the writer's curated `computation_error` sentence survives the `compute_jobs` transition, and (b) the wizard's refusals stop misattributing (plus the absorbed 161-ERRPREFIX, WR-06-UTC, HONEST-08-RESIDUAL and the anonymous factsheet placeholder).
**Verified:** 2026-09-06T17:32:45Z at HEAD `513e7f50` on `phase-164.2-curated-copy` (diff range `30d4f48a..HEAD`, 40 commits, 70 files)
**Status:** human_needed
**Re-verification:** No — initial verification

Every command below was re-run by the verifier in its own process. SUMMARY figures are cited only as the claim being checked.

## Independent gate runs (this verifier, at HEAD)

| Gate | Command | Result |
|---|---|---|
| SQL mutation corpus | `node scripts/mutation-runner/run.mjs` (log-and-capture) | **exit 0** — `coverage: files 45/72`, `lane-blocked: 0`, `lane-probe: pg_cron AVAILABLE`, `pending: 0`, `arms: 369/369/0`, `biting: 369`, `lane-invocations: 369` (tallies agree), `per-arm lane time: mean 1.1s`, `✅ No defects` |
| New gate inside that run | `test_sync_status_curated_sentence_survives.sql` | baseline exit 0; arms `0, S1, C1, O1, D1, P1, PC1, R1` all `exit 3 RED (identity ok)`; restore exit 0; `sections 8 / judged 8 / annotated 8 / waived 0 / biting 8` |
| SQL gate lint | `node scripts/lint-sql-gates.mjs` | `scanned 72 file(s); 0 finding(s), 0 measure-fail(s), 0 allowlist error(s)` |
| Plan anchors | `node scripts/verify-plan-anchors.mjs --pending` | `claims: 7 checked` / `OK: 2 plan file(s), no stale claims` (no 164.2 plan is pending) |
| Phase vitest files (17) | badge, staler-of-two, chip-honesty, two-surfaces, page.stale-analytics, create-with-key, validate-and-encrypt, add-key, set-members, MultiKeyConnectStep, preselect-refusal-class, roster-render, wizardErrors ×2, seam-wire-vocabulary, seam-ratelimit-posture, ratelimit | **17 passed, 1018 tests passed** |
| Scanners + hygiene (14) | `check-planning-hygiene`, `src/__tests__/contracts`, mutation-runner-floors, mutation-annotation-parser, drift-check-scripts, lint-sql-gates, gate-family-meta, verify-plan-anchors, probe-vocabulary, seam-citations | **14 passed, 935 tests passed** |
| pytest (from `analytics-service/`, `python3`) | provenance census, provenance fallback, computing_started_at stamp, job_worker_csv_kind, sql_gate_scoped_updates | **32 passed** |
| Snapshot | `npm run schema:functions:check` | `SQL function snapshot is current (119 functions).` |
| Live floors | `scripts/mutation-runner/run.mjs` | `FILES_FLOOR = 45` (:815), `ARMS_FLOOR = 369` (:1625), `WAIVED_CEILING = 0` (:1739); `ci.yml:2896-2897` `SENTINEL_FLOOR=9`, `ARMS_FLOOR=180` |

Not run, deliberately: the full vitest suite (contention), any `analytics-service` server, anything against PROD or shared TEST. No file was edited; no TS neuter was re-applied (read-only verification) — TS anti-vacuity rests on the SUMMARY-recorded neuters plus the assertion bodies read here; the SQL twins were re-executed by the runner above.

## Goal Achievement

### Observable Truths (the nine ROADMAP success criteria)

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | Curated sentence survives every `compute_jobs` transition — proven by a test that PERFORMS the transition | ✓ VERIFIED (behavioral) | `supabase/tests/test_sync_status_curated_sentence_survives.sql` drives the real RPCs — `mark_compute_job_failed` at :322/:366/:418/:512/:553/:597 (`'permanent'` and one `'transient'`), `mark_compute_job_done` at :466 — and reads `strategy_analytics.computation_error` back. Branch coverage stated in the header (:1-80) and in the sentinel `ALL 7 ARMS EXECUTED` (:618): (b) S1/C1/O1, (b-prime) P1/PC1, (c) D1, (a) R1. Apply list ends at `20260906120000` (:222). Baseline exit 0 and 8/8 twins biting in this verifier's runner run. Sibling gate `test_sync_status_marked_refresh_protected.sql` carries `TEST FAILED (0c)` (:297) and `ALL 23 ARMS EXECUTED`. Measurement: 164.2-07-SUMMARY `## Twins observed biting` — re-confirmed here. |
| 2 | `computation_error` carries writer/generation provenance and the bridge respects it; SQL-only fix not accepted | ✓ VERIFIED | Migration `20260906120000_computation_error_provenance.sql`: `ADD COLUMN IF NOT EXISTS computation_error_source` (:294) / `computation_error_job_id` (:297), domain CHECK (:317) and pairing CHECK (:349); branch (b) CASE keyed on `= v_latest_job_id` (:936-942), (b-prime) on `= v_protected_job_id` (:1014-1018); `BEFORE UPDATE` trigger `strategy_analytics_drop_stale_error_provenance_trigger` (:1108, :1149-1150); no `COMMENT ON FUNCTION sync_…` reissued (count 0). Snapshot regenerated (`sync_strategy_analytics_status.sql` header, `strategy_analytics_drop_stale_error_provenance.sql` present), `schema:functions:check` current. `database.types.ts:2575-2576/2611-2612/2647-2648`. Python half: `strategy_analytics_provenance.py` couples the pair via `provenance_source(job_id)`; 10 stamped literals (`job_worker.py:2117,3029,3074,6236,6276`; `analytics_runner.py:1506,1652,2049,2104,2174`) each routed through `upsert_or_drop_provenance`; keyword-only `job_id` (`analytics_runner.py:1182-1189`) forwarded at `job_worker.py:2222`. Census floors 10/1/2 (`test_computation_error_provenance_census.py:140-142`); 32 pytest passed. The 20260826120000 header still records the debt as OWED (:183) — now paid by a later migration, as the criterion demands. Note: the writer→DB path is proven in two halves (Python against a mocked PostgREST; the bridge on the pg-lane with directly stamped rows), not as one end-to-end test. |
| 3 | WIZFORM-02 closed by MEASUREMENT for every server-classified code the wizard can surface | ✓ PASSED (override) | Four of five locked surfaces rendered: `src/lib/wizardErrors.roster-render.test.tsx` SURFACES `connect-key`, `multi-key-add`, `multi-key-set-members`, `csv-submit` (:323-360), 82-row `it.each` (:1083), `EXPECTED_POPULATION_MIN = 78` (:493), skip count 0, 96 tests green here. Fifth surface `keys/validate-and-encrypt` measured to have no code reader — verified independently: `ApiKeyManager.tsx:245` and `StrategyForm.tsx:187` throw `err.error`, `AllocatorExchangeManager.tsx:583` reads `result.error`; the exclusion is a live assertion (:1028-1052) that reds if `/\b(err|result|data|body)\.code\b/` appears, plus a consumer census pinned at 3 (:1054). `## Surfaces not rendered` present in 164.2-09-SUMMARY; marker `GATED ON WIZFORM-02 CLOSING. Do not pick this up before that gate opens.` intact at `TODOS.md:5280`; `[WIZFORM-02-VE-CODECHANNEL]` booked at `TODOS.md:1121`; dated `STILL OPEN after Phase 164.2 — FOUNDER DECISION` bullet at `TODOS.md:100`. Neuter REDs recorded in 164.2-09-SUMMARY `## Anti-vacuity observations` (DOM assertion `:1105`/`:1164`). Disclosure is honest and complete; the open marker is NOT a failure. |
| 4 | No refusal blames a party that did not cause it: `KEY_RATE_LIMIT` off the limiter arms; `KEY_MISSING_REQUIRED_FIELD` off the preselect screen | ✓ VERIFIED | `grep -rl '"KEY_RATE_LIMIT"' src/app/api --include=route.ts` → **0** files. `RATE_LIMITED` on all four limiter arms: `create-with-key/route.ts:514,:1013`, `add-key/route.ts:326`, `set-members/route.ts:130`, `validate-and-encrypt/route.ts:384`; its copy says "the cap is ours, not your exchange's" (`wizardErrors.ts:3442-3448`). Preselect shape guard answers `PRESELECT_REQUEST_INVALID` (`create-with-key/route.ts:493`; copy `wizardErrors.ts:1721-1725` names our request, no field, no credential); `KEY_MISSING_REQUIRED_FIELD` retained only on the five credential-arm sites (:807,:868,:916,:937,:944). Rosters carry the codes (`ConnectKeyStep.tsx:355,364,392`; `MultiKeyConnectStep.tsx:337`; `wizardErrors.ts:5093,5107,5121,5258`). Bare-status map `UPSTREAM_STATUS_TO_SEAM_CODE` (`validate-and-encrypt/route.ts:118-125`). Measurements: 164.2-04 and 164.2-05 SUMMARY `## Anti-vacuity observations`; suites re-run green. |
| 5 | `DRAFT_ALREADY_EXISTS`'s cause SPLIT, not replaced | ✓ VERIFIED | `git diff 30d4f48a..HEAD -- src/lib/wizardErrors.ts` touches no `DRAFT_ALREADY_EXISTS` copy line (only comments) — cause "with the same API key" byte-unchanged (:1992-1995). Reuse-arm 23505 (`create-with-key/route.ts:686-760`) reads the colliding draft's `api_key_id` (select at :721, tenant-scoped) and answers `DRAFT_ALREADY_EXISTS` only when it equals the picked key (:740-748), else `DRAFT_SESSION_COLLISION` (:750-758; copy at `wizardErrors.ts:2048-2053` names the earlier session, not the key). Credential-arm 23505 at :1413 keeps `DRAFT_ALREADY_EXISTS`. Union members at `wizardErrors.ts:166,234`; `EXPECTED_TABLE_SIZE = 92` at both pins (`wizardErrors.test.ts:2036,2534`, read with `grep -a`; NUL byte count 1). Four-case describe + neuters 3/3b/3c in 164.2-04-SUMMARY. |
| 6 | 161-ERRPREFIX SPLIT: prose to the user, `err.code` to log + Sentry breadcrumb, class-wide, both halves test-proven | ✓ VERIFIED | `KeyPermissionBadge.tsx:167-183`: `console.error("[KeyPermissionBadge] probe refused with code:", err.code, message)`, `void addSentryBreadcrumb({ category: "key-permission-badge", message: err.code, … })`, `throw new RouteResponseError(message)`; `grep '\${err.code}:'` → no match. `sentry-capture.ts:268 export function addSentryBreadcrumb`, still exactly one `import` line (:1). Test: `ROUTE_REFUSALS` table of the route's six real codes (`KeyPermissionBadge.test.tsx:161-195`: CIRCUIT_OPEN@503, PROBE_RATE_LIMITED@429, KEY_UNDECRYPTABLE@500, PROBE_BACKEND_UNAVAILABLE@502, PROBE_TIMEOUT@502, PROBE_FAILED@502), three `it.each` halves (:207,:231,:261); 41 tests green. Three neuters recorded in 164.2-01-SUMMARY (8/6/7 RED). |
| 7 | WR-06-UTC: BOTH bucketers carry a day-granularity allowance, each proven RED with it removed | ✓ VERIFIED | `freshness.ts:61 export const SERIES_END_FUTURE_ALLOWANCE_DAYS = 1`, consumed at `:259` (`daysAhead <= SERIES_END_FUTURE_ALLOWANCE_DAYS`); `FactsheetView.tsx:14` imports it and passes it at `:1216` on the series call only. Joint test `src/lib/freshness.two-surfaces.test.tsx` (`it.each(ROWS)` :240, hand-typed matrix control :225); chip C-13/C-14 (`chip-honesty.test.tsx:416,427`). Neuters 1 and 2 in 164.2-02-SUMMARY red the badge leg and the chip leg of the same joint row respectively. 44 tests green here. |
| 8 | HONEST-08-RESIDUAL: sync arm PROVEN to still render on a fresh-series row, by measurement | ✓ VERIFIED (test); live clause unmet and disclosed | `FactsheetView.tsx:1229-1230 seriesIsBinding = jobTone !== "neutral" && TONE_RANK[seriesTone] > TONE_RANK[jobTone]`; chip-honesty C-4 retitled as the chip twin of staler-of-two:62 (`:261-276`) and observed RED BY NAME under the TONE_RANK-conjunct neuter (164.2-02-SUMMARY Neuter 3: `expected 'Track record · fresh' to be 'Computed · old'`). The criterion's parenthetical "one exists off the crypto-sma cohort" was measured FALSE on 2026-09-06 (`/browse` has one cohort, both computed rows have dead track records) and recorded verbatim as `NOT OBSERVED`; TODOS.md:4711-4716 closes on the neuter and states the absence. Routed to human verification. |
| 9 | Anonymous uncomputed placeholder addresses its reader; test bound to the ANONYMOUS render fails when the dev sentence returns | ✓ VERIFIED | `page.tsx:422-426` renders "This factsheet has not been computed yet…"; `dev-server console`, `bundled benchmark window`, `2023-04-26` absent from the render (the gate detail survives only in the server-side `console.warn` hint at `:372`). V2b (`page.stale-analytics.test.tsx:225-242`) asserts the three absences and the presence, via `renderPage()` with no auth mock (public lane). Five per-arm neuters N1-N5 in 164.2-03-SUMMARY. 6 tests green here. Pre-fix PROD read (1/1/1/0) recorded; post-deploy read is a human item. |

**Score:** 9/9 truths verified (8 VERIFIED + 1 PASSED (override); 0 present-but-behavior-unverified)

### The four deliberate states, checked as instructed

1. **WIZFORM-02 deliberately open** — disclosure verified honest and complete (row 3). The marker at `TODOS.md:5280` is intact; plan 10's adjusted verify pins the marker sentence at exactly 1 and `GATE OPEN 2026-09-06` at 0.
2. **New gate RED on shared TEST until `20260906120000` is hand-applied** — expected. `[164.2-TEST-APPLY-PROVENANCE]` at `TODOS.md:1185` names both expected reds (`TEST FAILED (0)` and the sibling's `(0c)`) and the ordered remedy (marker query → `psql "$TEST_SUPABASE_DB_URL" -f` → record the first green SHA; never `supabase db push`). Not a regression.
3. **Nothing applied to PROD or shared TEST** — verified by inference: `git log origin/main -- supabase/migrations/20260906120000_*` → 0 commits and `git ls-tree origin/main supabase/migrations/ | grep -c 20260906120000` → 0, so the main-only migrate workflow could not have fired; every gate apply list names the pg-lane fixtures; no TEST credential exists in this checkout (`.env` carries only the PROD URL/key; keychain lookup found no `quantalyze-test` DB URL). A direct read-only PROD probe was blocked by the permission classifier. Routed to human verification for direct confirmation.
4. **`[PROV-WRITER-23514]` implemented but OPEN** — reasoning holds. `provenance_source(job_id)` returns `'writer'` iff `job_id is not None`, so from today's writers the pair always satisfies both `IN ('writer')` and `(source IS NULL) = (job_id IS NULL)`; the retry arm in `upsert_or_drop_provenance` fires only on `code == "23514"` with a marker key present. It is behaviorally proven only against a mocked `APIError` (`test_computation_error_provenance_fallback.py`), never against Postgres, and no writer can reach it — "implemented ≠ proven" is the correct status. Entry text at `TODOS.md:1139-1163` still describes the fix as owed to plan 08 rather than as implemented-but-unreachable; plan 10's SUMMARY carries that nuance, the entry itself does not (Info, see anti-patterns).

### Required Artifacts

`gsd_run query verify.artifacts` on all ten PLANs: **all_passed true** (3/3, 3/3, 2/2, 3/3, 3/3, 3/3, 2/2, 1/1, 1/1, 1/1). Level 3/4 wiring checked manually above (imports and call sites cited per row).

### Key Link Verification

`gsd_run query verify.key-links`: 01-07, 09, 10 all verified. **Plan 08 reported 0/1** — tool limitation only (`from:` is `job_worker.py:2188`, a `file:line`, which the query cannot resolve). Verified by hand: `job_worker.py:2222 job_id=job.get("id")` → `analytics_runner.py:1182-1189 async def run_csv_strategy_analytics(…, *, …, job_id: str | None = None)`. WIRED.

### Data-Flow Trace (Level 4)

| Artifact | Value | Source | Status |
|---|---|---|---|
| `KeyPermissionBadge.tsx` render | route prose `message` | `/api/keys/[id]/permissions` refusal body | ✓ FLOWING |
| `FreshnessChip` verdict | `seriesTone`/`jobTone` | `series_end` / `computed_at` on the row via `bucketByAge` with the imported allowance | ✓ FLOWING |
| `page.tsx` placeholder | static sentence by design (the arm knows nothing else) | — | ✓ intentional static |
| `strategy_analytics.computation_error` after RPC | writer sentence or `computation_error_copy(kind)` | `sync_strategy_analytics_status` CASE on the two marker columns | ✓ FLOWING (proven on lane) |
| Python failure upserts | `computation_error_source`/`_job_id` | `provenance_source(job_id)` with the claimed job's id | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Gate performs the transition and the sentence survives; all eight twins bite | mutation runner (above) | baseline 0 / 8 RED identity ok / restore 0 | ✓ PASS |
| Every phase test file green at HEAD | vitest A + B, pytest | 1018 + 935 + 32 passed | ✓ PASS |
| Snapshot current | `schema:functions:check` | 119 functions, current | ✓ PASS |
| Planning hygiene (username leak plan 07 found) | `check-planning-hygiene.test.ts` | green | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` are declared by this phase. The pg-lane gates above are the phase's probes and were executed by the runner.

### Requirements Coverage

ROADMAP lists no v1.20 requirement IDs (`REQUIREMENTS.md` has no `164.2` mapping — confirmed). The four TODOS entries the phase owns:

| Entry | Status | Evidence |
|---|---|---|
| `[WR-06-UTC]` | ✓ CLOSED by citation | `TODOS.md:2595-2600` cites 164.2-02 `## Anti-vacuity observations` Neuter 1/2 |
| `[161-ERRPREFIX]` | ✓ CLOSED by citation | `TODOS.md:2613, :2624` cites 164.2-01 |
| `[HONEST-08-RESIDUAL]` | ✓ CLOSED by citation (neuter), live half stated as absence | `TODOS.md:4690-4716` |
| `WIZFORM-02` | ⚖️ OPEN by founder decision | `TODOS.md:100-114`, `:1121`, marker `:5280` |
| Bookings | ✓ present | `[164.2-TEST-APPLY-PROVENANCE]` :1185, `[164.2-TYPES-REGEN-CHECK]` :1219, `[164.5-BASELINE-PROVENANCE-COLS]` :1243, `[PROV-COMMENT-STALE]` :1164 |

### Decision Coverage

`check.decision-coverage-verify` returned `could-not-parse` (warning, non-blocking): the CONTEXT `<decisions>` block uses `### Area` headings with prose bullets rather than the parser's expected shape. Manual read: Area 1 (dedicated columns, conditional bridge, every writer stamps, no backfill, transition-performing proof) — all honored; Area 2 (emitter fix + mint, RATE_LIMIT split by source, DRAFT split, roster-driven test) — honored, with the roster-driven test closing four of five surfaces; Area 3 (prose/log split, one constant both bucketers, anonymous placeholder) — honored; Area 4 (test AND live observation, neuter-and-observe everywhere, no `git checkout --`) — honored except the live observation, which was attempted and recorded as unavailable. Research corrections 1-8 honored (the `f.id` projection, marker clears on (a)/(c), RPC-driven test, `portfolio.py` untouched, wiring not authoring for RATE_LIMITED, five laws, inverted pins, `addSentryBreadcrumb`).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `scripts/mutation-runner/run.mjs` | 1595-1599 | Separation-probe comment still says `ARMS_FLOOR=361 (this value)` while `:1625` is `369` (plan 07's own note at :810-812 was updated for FILES_FLOOR; this block was not) | ℹ️ Info | Stale prose beside a live constant; the constant and the floors test are correct |
| `164.2-06-SUMMARY.md` | 6, 61-73 | `status: paused-at-checkpoint`, "Task 4 (founder gate) has NOT been reached" — contradicts the phase being presented as complete | ⚠️ Warning | Either the migration's founder gate is undischarged (a real pre-PR gate) or the SUMMARY is stale; routed to human |
| `TODOS.md` | 1139-1163 | `[PROV-WRITER-23514]` body still reads as owed to plan 08 ("The writer must catch…"); the implemented-but-unreachable status lives only in 164.2-10-SUMMARY | ℹ️ Info | Ledger prose currency |
| `.planning/STATE.md` | 5-11, 189-203 | Still says "164.2 … planning not started / no PLAN files yet" | ℹ️ Info | Ledger currency; orchestrator/ship updates it |
| `.planning/ROADMAP.md` | 1164-1173 | All ten 164.2 plan checkboxes unchecked | ℹ️ Info | Orchestrator bookkeeping |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` in any line the phase ADDED (diff-scoped scan). No skipped tests in any phase test file. `git status` clean; all 24 SUMMARY-cited commit hashes resolve.

### Overclaims checked

- 164.2-07-SUMMARY's full-suite reading (`14 175 passed / 4 failed`, two defects) omitted the `OPS-08-F9` red its own task-3 commit caused — plan 10 found and fixed it (`drift-check-scripts.test.ts` now pins 9/180) and recorded the omission. Not an overclaim at HEAD.
- No SUMMARY claims a live HONEST-08 observation, a TEST/PROD apply, an ubuntu SHA-bound run for the 369-arm corpus (CLAUDE.md:251-253 says explicitly it has not been taken), or a closed WIZFORM-02. Each of those is stated as absent.
- ROADMAP criterion 8's parenthetical "one exists off the `crypto-sma` cohort" is the one sentence in the contract the phase measured false; it is the roadmap's claim, not the phase's.

### Deferred Items (out of scope, logged in `deferred-items.md`)

| # | Item | Where |
|---|---|---|
| 1 | `factsheet-share/[token]/page.tsx:252` carries the same "Try again in a few minutes" time promise on the token-bearer surface | deferred-items.md (plan 03) |
| 2 | `test_stitch_composite_job` source scan does not strip comments | 164.2-08-SUMMARY `## Deferred` |
| 3 | `KEY_RATE_LIMIT` on `KNOWN_SET_MEMBERS_CODES` has no emitter; `set-members` does not route its deny through `rateLimitDenyJson` | 164.2-05-SUMMARY `## Owed Debt` |
| 4 | Stale-`wizardSessionId` functional root cause | Phase 164.2.1 SESSIONID-FENCE (ROADMAP) |

### Human Verification Required

See frontmatter `human_verification` (5 items): the plan-06 Task 4 founder gate on the migration; a direct read-only PROD/TEST column probe; criterion 8's live observation; copy quality of the minted/rewritten sentences; post-deploy PROD read of the placeholder plus the two booked post-merge checks.

### Gaps Summary

No blocking gap. All nine criteria are delivered in the codebase and re-verified here by running the gates rather than reading the SUMMARYs; criterion 3 is satisfied under a recorded founder override (four of five surfaces, fifth blocked by a measured product fact and pinned by a live assertion). The items that keep this at `human_needed` are (1) an internal inconsistency about whether the migration's founder gate was passed — material because merging the PR auto-applies `20260906120000` to PROD — (2) direct confirmation of the "nothing applied" property, which the verifier could establish only by inference, and (3) judgment/observation items the plans themselves flagged as human.

---

_Verified: 2026-09-06T17:32:45Z_
_Verifier: Claude (gsd-verifier)_

---

## Ship-time re-verification — 2026-09-06 (at `4e360157`)

The five `human_verification` items in the frontmatter were written at `513e7f50`.
Three have since been discharged; the frontmatter list is kept unedited as the dated
record of what was owed, and this section is the current reading.

### 1. Founder gate on migration `20260906120000` — ✅ DISCHARGED

Discharged in commit `77f6e202`, which landed **after** the verifier's head `513e7f50`
and so could not have been seen by it. `164.2-06-SUMMARY.md` frontmatter now reads
`status: complete  # Task 4 FOUNDER GATE CLEARED 2026-09-06` and its body carries the
`## Task 4 — founder gate CLEARED` section with all three reviewer verdicts
(migration-reviewer, rls-policy-auditor, silent-failure-hunter), every disposition and
its commit. The two SUMMARYs no longer disagree.

### 2. Nothing reached PROD or shared TEST — ✅ DISCHARGED (by evidence, not inference)

The verifier could only infer this. It is now established by closing every path:

| Path | State |
|---|---|
| Migration present on `origin/main`? | **No** — `git cat-file -e origin/main:supabase/migrations/20260906120000_…sql` fails; 0 commits touch it on `origin/main` |
| `supabase-migrate.yml` trigger | `push: branches: [main]`, `paths: supabase/migrations/**` (+ `workflow_dispatch`) — never fires on a feature branch |
| Anything applying migrations to TEST | **Nothing in CI does** — which is precisely why `[164.2-TEST-APPLY-PROVENANCE]` is booked |
| Where this phase's SQL actually executed | Throwaway `scripts/pg-lane/run.sh` clusters only; the security audit re-confirmed the migration's `shasum -a 256` unchanged after four lane runs |

### 3. HONEST-08 live chip observation — ⏸ POST-DEPLOY, cannot gate a PR

Depends on a public row whose track record ends within ~3 days; none existed on the
verification date. The test half (chip-honesty C-4, RED under the TONE_RANK neuter) is
what the TODOS close rests on and is green.

### 4. Copy quality on the minted sentences — ⏳ **STILL OWED — founder judgment**

The only item of the five that is both pre-merge and undischargeable by measurement.
The sentences were surfaced to the founder in the ship session. **Not cleared at the
time of writing.**

⚠️ Two of the sentences this item covers CHANGED after the verifier wrote it, in the
ship-review fix pass (`4e360157`): `_mark_truncated`'s and
`_restore_publish_state_on_abort`'s `computation_error` were leaking an internal table
name plus a raw uuid, and a Python exception class name, into durable user-visible copy —
a leak this phase's own provenance mechanism created by making writer sentences survive
the bridge. Any copy sign-off must cover the NEW sentences, not the ones the verifier saw.

### 5. Post-deploy PROD curl checks — ⏸ POST-DEPLOY by definition

Plus `[164.2-TYPES-REGEN-CHECK]` and `[164.2-TEST-APPLY-PROVENANCE]`, both booked.

---

## ⛔ Correction to the recorded override (criterion 3 / WIZFORM-02)

The `overrides:` block in the frontmatter states the exclusion of the fifth surface is
*"pinned by a live assertion (roster-render.test.tsx:1028-1052) that reds the day a
consumer reads code."*

**That assertion could not fail when the override was accepted.** The ship review found
it compared a hand-typed array's length to a hand-typed `3` while its comment claimed to
scan every file under `src/`; it read no files. A companion detector matched only four
hard-coded receiver names, three of which no consumer uses for this route's body.

Both were repaired in `4e360157`: the census is now DERIVED from disk and deep-equals
`CONSUMERS`, and the detector derives its receivers from the fetch/json binding with a
destructure leg and a block-scoped backstop. Three observed-RED proofs are recorded in
that commit.

**The override's stated basis is therefore now true, having not been true when it was
accepted.** The decision stands on a guard that works; it did not before. The founder is
entitled to know the premise was repaired rather than merely restated.

---

## Item 4 DISCHARGED — founder copy sign-off, 2026-09-06

**Decision: "Approve as it stands."** Taken in the ship session, on the sentences as they
exist AFTER the review fix pass (`4e360157`) — not the ones the verifier saw. The founder
was shown the before/after for both rewritten sentences:

| | Was (leaked to the user) | Now |
|---|---|---|
| CSV truncation | `CSV analytics aborted (csv_daily_returns strategy_id=<uuid>); operator intervention required.` | "CSV analytics aborted: the dataset exceeds {n:,} rows and needs operator intervention. Nothing was published and your uploaded data was not changed." |
| Interrupted refresh | `Ledger refresh (source=ledger-refresh) aborted by GeneratorExit before it could finish…` | "The data refresh was interrupted by an unexpected worker shutdown before it could finish; your previously published factsheet was left in place." |

The four wizard sentences and the anonymous factsheet placeholder were surfaced unchanged
and are covered by the same approval.

**`status` therefore moves `human_needed` → `passed`.** All five items are now discharged
or classified post-deploy:

| # | Item | State |
|---|---|---|
| 1 | Founder migration gate | ✅ discharged (`77f6e202`) |
| 2 | Nothing reached PROD/TEST | ✅ discharged by evidence |
| 3 | HONEST-08 live chip | ⏸ post-deploy |
| 4 | Copy quality | ✅ **founder-approved 2026-09-06** |
| 5 | Post-deploy curl checks | ⏸ post-deploy |

The status was NOT moved to clear a gate — it was moved because the human the gate was
waiting for made the call. Items 3 and 5 remain owed AFTER deploy and are booked.

**Founder also authorised the full land-and-deploy** (merge once CI is SHA-bound green),
which applies migration `20260906120000` to PRODUCTION.
