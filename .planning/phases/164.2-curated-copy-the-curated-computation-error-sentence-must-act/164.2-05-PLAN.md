---
phase: 164.2-curated-copy-the-curated-computation-error-sentence-must-act
plan: 05
type: execute
subsystem: wizard-error-roster
tags: [RATE_LIMITED, userActionLimiter, class-fix, validate-and-encrypt, WIZFORM-02-tracer]
wave: 2
depends_on: ["164.2-04"]
files_modified:
  - src/app/api/keys/validate-and-encrypt/route.ts
  - src/app/api/keys/validate-and-encrypt/route.test.ts
  - src/app/api/strategies/composite/add-key/route.ts
  - src/app/api/strategies/composite/add-key/route.test.ts
  - src/app/api/strategies/composite/set-members/route.ts
  - src/app/api/strategies/composite/set-members/route.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.invariant.test.ts
autonomous: true
requirements: [164.2-SC4, WIZFORM-02]
provides:
  - "Every userActionLimiter 429 in the wizard family (four routes) answers RATE_LIMITED; KEY_RATE_LIMIT survives only on the classifier's genuine-venue-throttle path"
  - "validate-and-encrypt maps a bare upstream status (401/403/422/429 with no seamCode) to a named code — the PROD-measured `{\"error\":\"Unauthorized\",\"code\":\"UNKNOWN\"}` reproduction is closed at the seam"
affects: [164.2-09, 164.2-10]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "composite/add-key, composite/set-members and keys/validate-and-encrypt answer their userActionLimiter 429 with RATE_LIMITED; each route's roster (KNOWN_ADD_KEY_CODES, KNOWN_SET_MEMBERS_CODES, KNOWN_VALIDATE_AND_ENCRYPT_CODES) admits RATE_LIMITED; KEY_RATE_LIMIT stays rostered because the classifier still returns it at 503 for a real venue throttle"
    - "validate-and-encrypt's `err.seamCode ?? \"UNKNOWN\"` arm (src/app/api/keys/validate-and-encrypt/route.ts:753-765) consults a hand-typed UPSTREAM_STATUS_TO_SEAM_CODE map before falling to UNKNOWN: 401/403 -> SEAM_MISCONFIGURED (our service key was rejected by our own service), 422 -> VALIDATION_FAILED, 429 -> RATE_LIMITED; a status outside the map still answers UNKNOWN and the test says why"
    - "Each flipped pin is INVERTED with its history legible, not deleted"
  artifacts:
    - path: "src/app/api/keys/validate-and-encrypt/route.ts"
      provides: "RATE_LIMITED limiter arm + the bare-status map on the AnalyticsUpstreamError 4xx arm"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx"
      provides: "RATE_LIMITED in KNOWN_ADD_KEY_CODES (:280) and KNOWN_SET_MEMBERS_CODES (:352)"
    - path: "src/lib/wizardErrors.ts"
      provides: "RATE_LIMITED in KNOWN_VALIDATE_AND_ENCRYPT_CODES (:5091)"
  key_links:
    - from: "src/app/api/keys/validate-and-encrypt/route.ts"
      to: "src/lib/wizardErrors.ts"
      via: "SEAM_CODE_TO_WIZARD_CODE (:4864) maps SEAM_MISCONFIGURED / VALIDATION_FAILED / RATE_LIMITED — the mapped codes must already be rows there or they render UNKNOWN anyway"
      pattern: "SEAM_CODE_TO_WIZARD_CODE"
---

<objective>
Criterion 4's limiter half as a CLASS (CLAUDE.md fix-campaign rule: close the whole class across the surface). Plan 04 fixed `create-with-key`'s two `userActionLimiter` arms; the same misattribution lives at `composite/set-members/route.ts:99-101` (bare `NextResponse.json`, older shape), `composite/add-key/route.ts:304-306` and `keys/validate-and-encrypt/route.ts:298-300` (both `rateLimitDenyJson`, the latter with keys in the other order). Each is wiring: `RATE_LIMITED` already exists with honest copy; it is not in these routes' rosters.

Also the WIZFORM-02 TRACER instance (RESEARCH Finding 6): PROD returned `{"error":"Unauthorized","code":"UNKNOWN"}` from `validate-and-encrypt` on 2026-08-25 — a Railway 401 (service-key rejection) carries no `seamCode`, so line 762's `?? "UNKNOWN"` discarded a server classification. A roster row cannot close this (there is no code to roster); the seam must mint one from the bare status. Plan 09's roster-render test then includes this case as its PROD-measured row.

Purpose: no refusal blames the exchange for our cap; a server-classified upstream status never reaches the wizard as UNKNOWN.
Output: three routes rewired, three rosters widened, the status map, pins inverted, neuters observed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-CONTEXT.md
Read when it exists (written by the plan this one depends on): .planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-04-SUMMARY.md

Read first: the three limiter arms named above and their comments (`add-key:300-304` records that "honestly rewording it is plan 140.4-12's change, not this one" — that debt is paid here); `validate-and-encrypt/route.ts:753-765` and lines 810-830 (the second `?? "UNKNOWN"` arm at 500 — transport-shaped failures, LEFT ALONE: it is reached by untyped throws and the comment explains why); `src/lib/analytics-client.ts:120-140` (`readonly status: number`, and `seamCode`); `wizardErrors.ts:4864-4898` (SEAM_CODE_TO_WIZARD_CODE rows) and lines 5091-5125 (KNOWN_VALIDATE_AND_ENCRYPT_CODES); `MultiKeyConnectStep.tsx:280-300` and lines 343-358; `src/lib/wizardErrors.invariant.test.ts:205-220`, lines 370-385 (the hand-typed "SIX members" count for KNOWN_VALIDATE_AND_ENCRYPT_CODES), lines 255-262 and lines 425-432 (ROUTES rows for add-key and validate-and-encrypt); route tests: `add-key/route.test.ts:775-820`, `validate-and-encrypt/route.test.ts:995-1012` and lines 1620-1630, `set-members/route.test.ts` (grep `429`). Inverted-pin idiom: `portfolio-optimizer/route.test.ts:440-462`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validate-and-encrypt — RATE_LIMITED limiter arm, the bare-status map, roster row</name>
  <files>src/app/api/keys/validate-and-encrypt/route.ts, src/app/api/keys/validate-and-encrypt/route.test.ts, src/lib/wizardErrors.ts, src/lib/wizardErrors.invariant.test.ts</files>
  <behavior>
    - line 999 ("429 throttle carries the EXACT { error, code: KEY_RATE_LIMIT } pair") INVERTS to `RATE_LIMITED`, key order preserved (`{ error, code }`), with the inverted-pin comment quoting its own lines 1005-1006 rationale ("the key-connect family… chose KEY_RATE_LIMIT") and stating why that rationale was the defect (family consistency in a FALSE sentence). line 1627 likewise.
    - NEW: an `AnalyticsUpstreamError` with `status: 401` and `seamCode: null` answers 401 `{ error, code: "SEAM_MISCONFIGURED" }` — the PROD reproduction, cited by date in the `it` title. 403 -> `SEAM_MISCONFIGURED`; 422 -> `VALIDATION_FAILED`; 429 -> `RATE_LIMITED`; 418 (unmapped) -> `UNKNOWN`, with the test explaining that an unmapped bare status IS unclassified and UNKNOWN is honest there.
    - NEW: an `AnalyticsUpstreamError` that DOES carry a `seamCode` keeps it verbatim even when its status is in the map (never overwrite an upstream-carried code — lines 758-760's rule).
    - The 500 arm at lines 810-830 is untouched (its existing tests stay green).
  </behavior>
  <action>
    Route: line 300 -> `throttledBody: { error: "Too many requests", code: "RATE_LIMITED" }` (order preserved) and rewrite the lines 294-297 comment. Add a module-level `const UPSTREAM_STATUS_TO_SEAM_CODE: ReadonlyMap<number, string> = new Map([[401, "SEAM_MISCONFIGURED"], [403, "SEAM_MISCONFIGURED"], [422, "VALIDATION_FAILED"], [429, "RATE_LIMITED"]])` with a comment naming the 2026-08-25 PROD measurement and why 401/403 is OURS (the analytics service rejected OUR service key — PYAPI-06 — which is a configuration fault on our side, exactly SEAM_MISCONFIGURED's copy). At line 762: `code: err.seamCode ?? UPSTREAM_STATUS_TO_SEAM_CODE.get(err.status) ?? "UNKNOWN"`. Every mapped value MUST be a key of `SEAM_CODE_TO_WIZARD_CODE` (`wizardErrors.ts:4864-4870`) — assert this in the invariant test with a hand-typed list of the map's values (source-scan the route the way lines 1444-1488 scans, or import the map if you export it — exporting is fine, it is a constant).

    `wizardErrors.ts:5091` (`KNOWN_VALIDATE_AND_ENCRYPT_CODES`): add `"RATE_LIMITED"` to `KNOWN_VALIDATE_AND_ENCRYPT_CODES` with a dated comment. `wizardErrors.invariant.test.ts`: move the hand-typed SIX -> SEVEN at every site that states it (line 215, line 377 and any other — `grep -n "SIX" src/lib/wizardErrors.invariant.test.ts`), re-writing each sentence, not just the digit.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/keys/validate-and-encrypt/route.test.ts src/lib/wizardErrors.invariant.test.ts src/lib/wizardErrors.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Suites green; `grep -c '"KEY_RATE_LIMIT"' src/app/api/keys/validate-and-encrypt/route.ts` is 0; the 401-bare-status test exists and is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: composite add-key and set-members — RATE_LIMITED, rosters, pins; then neuter-and-observe</name>
  <files>src/app/api/strategies/composite/add-key/route.ts, src/app/api/strategies/composite/add-key/route.test.ts, src/app/api/strategies/composite/set-members/route.ts, src/app/api/strategies/composite/set-members/route.test.ts, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx</files>
  <behavior>
    - `add-key/route.test.ts:780-783` (`KEY_RATE_LIMIT`) INVERTS to `RATE_LIMITED`; lines 799-816 (the 503 arm must NOT be KEY_RATE_LIMIT) stays and its assertion also gains `.not.toBe("RATE_LIMITED")` — a misconfiguration is neither throttle.
    - `set-members`: the 429 test (find it; if none exists, write one) asserts `{ code: "RATE_LIMITED", error: "Too many requests" }`, `code` FIRST as a literal, `NO_STORE_HEADERS` — and, since this route uses the bare `NextResponse.json` shape rather than `rateLimitDenyJson`, LEAVE the shape alone (Rule 3: surgical) but say in the comment that it is the older shape.
    - Rosters: `KNOWN_ADD_KEY_CODES` (`MultiKeyConnectStep.tsx:280`, `const KNOWN_ADD_KEY_CODES`) and `KNOWN_SET_MEMBERS_CODES` (line 352) each gain `"RATE_LIMITED"` beside `"KEY_RATE_LIMIT"` (which STAYS: the classifier path), with a dated comment in the file's own idiom. The invariant ROUTES row for add-key (lines 255-262) derives 400s only, so the roster row is owed by hand — add a hand-typed 429 twin for add-key and set-members in the shape plan 04 established for create-with-key.
  </behavior>
  <action>
    Implement per `<behavior>`; run `MultiKeyConnectStep.test.tsx` and `MultiKeyConnectStep.payload.test.ts` too (they exercise both rosters).

    Then neuter-and-observe (fresh `cp` backup each; `shasum -a 256` before/after; restore by `cp`; never `git checkout --`):
    1. Remove `"RATE_LIMITED"` from `KNOWN_ADD_KEY_CODES`. Expected RED: the add-key 429 twin.
    2. Remove `"RATE_LIMITED"` from `KNOWN_VALIDATE_AND_ENCRYPT_CODES`. Expected RED: the SEVEN-member count and/or the validate-and-encrypt 429 twin.
    3. In `validate-and-encrypt/route.ts` drop the map lookup (back to `err.seamCode ?? "UNKNOWN"`). Expected RED: the 401 PROD-reproduction test, by name.
    Record each FAIL block and hash pair in the SUMMARY under `## Anti-vacuity observations`.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/composite/add-key/route.test.ts src/app/api/strategies/composite/set-members/route.test.ts "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.payload.test.ts" src/lib/wizardErrors.invariant.test.ts && npx tsc --noEmit && test "$(grep -rl '"KEY_RATE_LIMIT"' src/app/api --include=route.ts | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>No `route.ts` under `src/app/api` emits `KEY_RATE_LIMIT`; the classifier sites `wizardErrors.ts:4155` and line 4712 still do (untouched); three RED observations recorded; hashes restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> four wizard routes | rate-limited user actions |
| Next route -> analytics service | AnalyticsUpstreamError status/seamCode crossing back to the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.2-09 | Information Disclosure | bare-status map | low | mitigate | the map emits only closed-vocabulary codes; `err.message` handling is unchanged; a 401 from our own service is described as OUR misconfiguration, which discloses nothing about the upstream |
| T-164.2-10 | Denial of Service | limiter arms | low | accept | limiter behaviour (buckets, Retry-After) is untouched; only the code token changes |
| T-164.2-SC | Tampering | npm installs | low | accept | no package installed |
</threat_model>

<verification>
- Task 2's verify command green (includes the zero-emitter grep gate on `route.ts` files only, so prose in tests cannot satisfy it).
- SUMMARY: three anti-vacuity observations.
</verification>

<success_criteria>
Criterion 4's limiter half closed as a class across four routes; the WIZFORM-02 PROD reproduction (bare 401) closed at the seam, ready for plan 09's render test.
</success_criteria>

<output>
Create `.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-05-SUMMARY.md` when done.
</output>
