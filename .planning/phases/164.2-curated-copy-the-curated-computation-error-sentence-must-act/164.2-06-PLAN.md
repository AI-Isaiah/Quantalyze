---
phase: 164.2-curated-copy-the-curated-computation-error-sentence-must-act
plan: 06
type: execute
subsystem: sql-status-bridge
tags: [migration, provenance, sync_strategy_analytics_status, three-reviewer-gate, schema-snapshot]
wave: 2
depends_on: []
files_modified:
  - supabase/migrations/20260906120000_computation_error_provenance.sql
  - supabase/schema/functions/sync_strategy_analytics_status.sql
  - src/lib/database.types.ts
autonomous: false
requirements: [164.2-SC2]
provides:
  - "strategy_analytics.computation_error_source (TEXT, CHECK IN ('writer'), nullable) and computation_error_job_id (UUID, nullable): NULL = bridge/legacy provenance"
  - "sync_strategy_analytics_status re-based on 20260826120000:336-905 with a conditional (b)/(b-prime) and marker clears on (a)/(c)"
  - "regenerated function snapshot and hand-extended database.types.ts"
affects: [164.2-07, 164.2-08, 164.2-10]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The bridge keeps a row's computation_error on branches (b) and (b-prime) ONLY when computation_error_source = 'writer' AND computation_error_job_id equals the job the branch itself resolved (v_latest_job_id / v_protected_job_id); otherwise it writes the per-kind generic AND nulls both markers"
    - "Branches (a) and (c) null both markers alongside the NULL sentence, so a marker can never stand over a NULLed sentence"
    - "The migration does NOT reissue COMMENT ON FUNCTION and does NOT redefine computation_error_copy; its self-verify asserts the 20260826120000 comment survived CREATE OR REPLACE (assumption A1 measured at apply time, on the lane before merge)"
    - "The function body contains no occurrence of the banned operator-column identifier (20260826120000:1290's rule) — comments included"
    - "Three reviewers (migration-reviewer, rls-policy-auditor, silent-failure-hunter) have read the migration and every finding is fixed or recorded, BEFORE the human checkpoint that precedes the PR"
  artifacts:
    - path: "supabase/migrations/20260906120000_computation_error_provenance.sql"
      provides: "two ADD COLUMN IF NOT EXISTS + COMMENT ON COLUMN, the re-based function, the self-verify DO block"
    - path: "supabase/schema/functions/sync_strategy_analytics_status.sql"
      provides: "npm run schema:functions output; CI --check must pass"
    - path: "src/lib/database.types.ts"
      provides: "strategy_analytics Row/Insert/Update carry computation_error_job_id and computation_error_source in generated (alphabetical) order"
  key_links:
    - from: "supabase/migrations/20260906120000_computation_error_provenance.sql"
      to: "supabase/migrations/20260826120000_computation_error_curated_copy.sql"
      via: "CREATE OR REPLACE re-based on :336-905 — body copied, six deltas, everything else byte-identical"
      pattern: "sync_strategy_analytics_status"
---

<objective>
Criterion 2's SQL half. `strategy_analytics.computation_error` gets writer/generation provenance in its OWN columns (CONTEXT Area 1 — never inside the sentence, which renders verbatim), and `sync_strategy_analytics_status` respects it: a writer-provenanced sentence for THE job the bridge just resolved survives branches (b) `20260826120000:804-813` and (b-prime) lines 874-880; everything else behaves exactly as today. NULL markers reproduce today's behaviour for the ~103 legacy PROD rows — no backfill, by decision.

Measured facts the migration must honour (PATTERNS §1, RESEARCH Finding 1, verified at HEAD c1c8cf3c):
- The LATEST bridge is `20260826120000:336-905`; `20260826140000` never defines it. Seven files define the function; re-base on the newest only.
- The `live_failures` CTE (lines 510-645) projects `f.error_kind, f.created_at, … has_live_successor` and NOT `f.id` — the job id must be added to the projection before it can be aggregated.
- The DECLARE block (lines 342-353) has no job-id variable; add `v_latest_job_id UUID` and `v_protected_job_id UUID`, filled by two more `(array_agg(id ORDER BY created_at DESC) FILTER (…))[1]` terms in the lines 654-661 aggregate.
- ACL is a bare `REVOKE ALL … FROM PUBLIC, anon, authenticated` (line 909); there is no GRANT to keep.
- `COMMENT ON FUNCTION` (line 907) is the applied-ness KEY for gate arms 0a/0b; arm 0a's twin edits the OLD file's comment. Option (i) from PATTERNS is adopted: do NOT reissue the comment; assert it survived.
- Twins 2/A-3 and 3/F-3 of the parity gate edit `computation_error_copy` in the old file; do NOT redefine it here.
- `20260826120000:1290` bans the operator column's identifier from the body, comments included. line 1190's anchor (`SET computation_error = computation_error_copy(v_protected_kind)`) will no longer match a CASE; re-anchor it in the new self-verify on the CASE's ELSE arm rather than drop it. line 1296 (the VALUES arm of (b)) stays true because the VALUES arm keeps the generic.

Purpose: the column can finally tell a curated sentence from a stale one.
Output: the migration, reviewed three ways, snapshot regenerated, types extended, human checkpoint passed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-CONTEXT.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-RESEARCH.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-PATTERNS.md

Read first: `supabase/migrations/20260826120000_computation_error_curated_copy.sql` in full (1393 lines — the header's delta-list convention at lines 198-199, the owed-work paragraph at lines 183-196, the function lines 336-905, the ACL line 909, the self-verify block lines 1191-1386 and the RE-ANCHOR rule at lines 237-240); `20260802120000_strategy_analytics_stuck_computing_reaper.sql:215-235` (`ADD COLUMN IF NOT EXISTS computing_started_at`) and lines 540-560 (ADD COLUMN + pg_attribute self-verify — the exact shape to copy, including the nullable / no-default reasoning); `20260803150000:92-94,140-144` (why the type check exists); `test_sync_status_marked_refresh_protected.sql:139-149`, lines 206-252 (applied-ness idiom); `scripts/dump-sql-functions.ts:16-32`; `src/lib/database.types.ts:2569-2600` (Row), and the Insert/Update blocks that follow it.

CLAUDE.md "Which database am I on?": this checkout's Supabase CLI is linked to PROD. Nothing in this plan pushes; the lane is `scripts/pg-lane/run.sh` (throwaway cluster). Merging `supabase/migrations/**` to main AUTO-APPLIES to PROD — that is the apply, and it happens after the checkpoint below and the PR.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Write the migration and prove it on the lane end-to-end</name>
  <files>supabase/migrations/20260906120000_computation_error_provenance.sql</files>
  <action>
    Header (copy the 20260826120000 convention): title, phase/criterion, the OPS warning, THE DEFECT (quote the owed-work paragraph lines 183-196 and say this migration pays it), the design (two columns; NULL = bridge/legacy; job-id keying is what makes "this failure's sentence" decidable against the header's cases (ii) and (iii)), why the reaper's cron sentence needs no marker (it fires only on `computing` rows, which branch (a) already nulled), why the four TypeScript pre-enqueue writers need no marker (branch (a) correctly supersedes them when a job starts), and THE DELTA list against 20260826120000: (1) two ADD COLUMN; (2) `f.id` projected in `live_failures` + two DECLAREs + two array_agg terms; (3) branch (b) CASE on `computation_error` and on both markers; (4) branch (b-prime) same; (5) marker NULLs in (a) and (c); (6) nothing else — COMMENT ON FUNCTION NOT reissued (explain the arm-0a coupling), `computation_error_copy` NOT redefined (explain twins 2/A-3, 3/F-3). ⛔ The operator column's name must not appear anywhere in the FUNCTION BODY, comments included; keep every mention of it in the file header (which pg_get_functiondef does not return).

    Columns: `ALTER TABLE public.strategy_analytics ADD COLUMN IF NOT EXISTS computation_error_source TEXT;` then `ADD CONSTRAINT strategy_analytics_computation_error_source_check CHECK (computation_error_source IN ('writer'))` guarded by `IF NOT EXISTS` on `pg_constraint` (a DO block), and `ADD COLUMN IF NOT EXISTS computation_error_job_id UUID;` — nullable, NO DEFAULT, NO FK (compute_jobs rows are retained for audit; a FK would couple retention to this column). `COMMENT ON COLUMN` for each stating the NULL semantic, who writes it (the Python `strategy_analytics` failure writers, in the same statement as the sentence), who reads it (the bridge), and that the bridge nulls both on every overwrite.

    Function: `CREATE OR REPLACE FUNCTION sync_strategy_analytics_status(p_strategy_id UUID) RETURNS VOID LANGUAGE plpgsql SECURITY DEFINER SET search_path = public, pg_catalog AS $$` — body copied from lines 342-904 and edited ONLY at: DECLARE (+2 vars); CTE projection (`f.id`); aggregate (+2 terms, INTO extended); branch (a) INSERT column list gains both markers with NULL and the DO UPDATE sets both to NULL; branch (b) INSERT column list gains both markers (VALUES NULL), DO UPDATE `computation_error = CASE WHEN strategy_analytics.computation_error_source = 'writer' AND strategy_analytics.computation_error_job_id = v_latest_job_id THEN strategy_analytics.computation_error ELSE EXCLUDED.computation_error END` and the two markers `= CASE WHEN <same predicate> THEN strategy_analytics.<marker> ELSE NULL END`; branch (b-prime) the same CASE keyed on `v_protected_job_id`; branch (c) both markers `= NULL`. The lines 860-873 in-body owed-work comment is REWRITTEN to say the debt is paid here. Then `REVOKE ALL ON FUNCTION sync_strategy_analytics_status FROM PUBLIC, anon, authenticated;` verbatim.

    Self-verify DO block (shape-only, like lines 1191-1386; behaviour is plan 07's gate): pg_attribute checks for both columns (type, nullable, no default) and the CHECK constraint's presence; `pg_get_functiondef` regexes: body references both marker columns; body matches the (b) CASE predicate on `v_latest_job_id` and the (b-prime) CASE on `v_protected_job_id`; line 1296's VALUES anchor carried forward; line 1190 RE-ANCHORED on `ELSE\s+computation_error_copy\s*\(\s*v_protected_kind\s*\)`; the banned-identifier ban carried forward verbatim; `obj_description(...) ~ '20260826120000'` AND `~ '20260825150000'` (A1 measured — if this RAISEs on the lane, CREATE OR REPLACE did not preserve the comment and option (ii) must be taken: reissue with all ids and re-point arm 0a's twin in plan 07); SECURITY DEFINER, `search_path=public`, anon/authenticated cannot EXECUTE (lines 927-960 shape). Every RAISE names the criterion and the cause.

    Prove on the lane BEFORE anything else: run `scripts/pg-lane/run.sh --apply <every file in test_sync_status_marked_refresh_protected.sql:156's apply list, in order> supabase/migrations/20260906120000_computation_error_provenance.sql --gate supabase/tests/test_sync_status_marked_refresh_protected.sql` (read `scripts/pg-lane/run.sh:20-45`, `--gate <gate.sql>`, for the flag grammar). Exit 0 = the migration applies on the lane's 5-column stand-in AND the existing 16-arm template gate still passes against the NEW body. Then `grep -n <banned identifier> ` over the function region only (from the CREATE line to the `$$;`) must return nothing; over the whole file it may hit the header.
  </action>
  <verify>
    <automated>bash scripts/pg-lane/run.sh --apply scripts/pg-lane/fixtures/01-fixture-core.sql scripts/pg-lane/fixtures/02-fixture-sanitize-tables.sql scripts/pg-lane/fixtures/03-fixture-compute-jobs.sql scripts/pg-lane/fixtures/27-fixture-strategy-analytics-computation-error.sql supabase/migrations/20260411144407_compute_jobs_queue.sql scripts/pg-lane/fixtures/04-fixture-compute-jobs-targets.sql supabase/migrations/20260510175507_process_key_long_compute_job_kinds_repair.sql supabase/migrations/20260515114555_compute_jobs_claim_token_fencing.sql supabase/migrations/20260522111858_compute_analytics_from_csv_kind.sql supabase/migrations/20260614120000_derive_broker_dailies_kind.sql supabase/migrations/20260708120000_sync_status_failed_final_bounce.sql supabase/migrations/20260710120000_strategy_keys.sql supabase/migrations/20260710130000_stitch_composite_kind.sql supabase/migrations/20260825150000_sync_status_protect_marked_refresh.sql supabase/migrations/20260826120000_computation_error_curated_copy.sql supabase/migrations/20260906120000_computation_error_provenance.sql --gate supabase/tests/test_sync_status_marked_refresh_protected.sql</automated>
    <fails_when>⛔ AS WRITTEN THIS COMMAND CANNOT PASS: `run_lane` validates `--workdir is required` before booting anything (measured in `scripts/pg-lane/run.sh`, the `fail` on an empty `$workdir`), and no `--workdir` is passed here — it exits 1 with that message whatever the migration's state, so a green cannot be read from it and a red proves nothing. The executor must supply a fresh `--workdir <mktemp slot>` (every caller in the repo does), and only then do the real failure modes apply: exit 2 (port collision, refused); `apply file not found` for a mistyped path (the new migration file does not exist yet); a migration's OWN self-verify RAISE aborting the apply — for the new file, a missing column or CHECK constraint, a `pg_get_functiondef` regex that does not match the body, the `obj_description` comment lacking `20260826120000` or `20260825150000` (A1 — CREATE OR REPLACE did not preserve the comment; take option (ii)), or anon/authenticated still able to EXECUTE; or the 16-arm template gate raising `TEST FAILED (…)` against the new body (arm 0a's comment twin, or a bridge branch whose output moved). The banned-identifier grep over the function region named in the action is NOT part of this command</fails_when>
  </verify>
  <done>Lane exit 0 with the new migration applied and the 16-arm template gate green against the new body; the self-verify's comment-survival assertion passed (A1 measured); the banned identifier is absent from the function region.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot, types, and the three-reviewer gate</name>
  <files>supabase/schema/functions/sync_strategy_analytics_status.sql, src/lib/database.types.ts</files>
  <action>
    `npm run schema:functions` then `npm run schema:functions:check` (exit 0); commit the regenerated snapshot. `src/lib/database.types.ts`: there is no wrapper script and the CLI is linked to PROD, where the column does not exist yet (RESEARCH Open Q5) — so extend by hand, in the generated form: add `computation_error_job_id: string | null` and `computation_error_source: string | null` to `strategy_analytics`'s `Row`, and the optional forms `?: string | null` to `Insert` and `Update`, each in alphabetical position (after `computation_error`, before `computation_status`). Record in the SUMMARY that a `supabase gen types typescript --linked` run AFTER the PROD apply is expected to produce no diff on these lines — that is plan 10's post-merge check. `npx tsc --noEmit` and `npx vitest run src/__tests__/contracts` must stay green.

    Three-reviewer gate (CLAUDE.md memory rule: three reviewers BEFORE asking to apply any migration): spawn `migration-reviewer` and `rls-policy-auditor` (`~/.claude/agents/`) and `pr-review-toolkit:silent-failure-hunter` against the new migration file plus the regenerated snapshot, each with the header's delta list and the coupling facts above as their brief. FIX every finding (memory: fix all findings; only small+difficult+non-user-facing may wait, and then only with a TODOS entry). Re-run Task 1's lane command after any edit to the migration. Record each reviewer's findings and their disposition in the SUMMARY under `## Three-reviewer gate`. Do not trust a reviewer's "clean" — re-run the lane yourself.
  </action>
  <verify>
    <automated>npm run schema:functions:check && npx tsc --noEmit && npx vitest run src/__tests__/contracts</automated>
    <fails_when>`schema:functions:check` exits 1 printing `SQL function snapshot is stale (N file(s))` with `stale:` / `missing:` / `orphaned:` lines (the `sync_strategy_analytics_status.sql` snapshot was not regenerated after the migration, or the migration was edited again after a reviewer finding without re-running `npm run schema:functions`) — it parses `supabase/migrations/**` on disk and needs no database, so an absent credential is not a failure mode here; `tsc` exits non-zero (the hand-added `strategy_analytics` Row/Insert/Update lines are malformed, or a consumer of the generated type breaks); or a contract test fails (the contracts scan ALL of `src/`, so a literal or comment introduced anywhere in this phase can trip one). The three-reviewer gate has no automated signal in this command — its findings and dispositions are checked in the SUMMARY</fails_when>
  </verify>
  <done>Snapshot current; types typecheck; three reviewer reports recorded with every finding fixed or booked; lane command still exit 0 after fixes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Founder gate before the migration reaches a PR (auto-applies to PROD on merge)</name>
  <what-built>A migration that adds two provenance columns to `strategy_analytics` and re-bases `sync_strategy_analytics_status` so a writer-stamped sentence for the resolved job survives the status bridge. Proven on the pg-lane against the existing 16-arm gate; reviewed by migration-reviewer, rls-policy-auditor and silent-failure-hunter.</what-built>
  <how-to-verify>Read the SUMMARY's `## Three-reviewer gate` section and the lane output. Confirm: (1) no backfill is intended (NULL = today's behaviour for the ~103 legacy rows); (2) the deploy-order caveat — the migration must be on PROD before a worker sends the new keys, or PostgREST answers a schema-cache miss on the failure upsert (plan 08 carries the P11 minimal-key fallback and its own precondition); (3) you accept that merging this file to main applies it to PROD.</how-to-verify>
  <resume-signal>Reply "approved" to let plans 07/08 proceed on this base, or name the change you want first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python service role -> strategy_analytics | writers set the marker columns |
| SECURITY DEFINER bridge -> strategy_analytics | the bridge reads and nulls the markers |
| anon/authenticated -> strategy_analytics reads | the new columns are readable wherever the row is |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.2-11 | Tampering | computation_error_source | medium | mitigate | CHECK constraint restricts the value to 'writer'; only the service role writes strategy_analytics (existing RLS, confirmed by rls-policy-auditor in Task 2); a forged marker with a stale job id never matches the bridge's resolved job |
| T-164.2-12 | Information Disclosure | computation_error_job_id | low | accept | a compute_jobs UUID on a row the reader can already see; no secret |
| T-164.2-13 | Elevation of Privilege | SECURITY DEFINER bridge | high | mitigate | REVOKE line preserved verbatim; self-verify asserts anon/authenticated cannot EXECUTE and search_path is pinned |
| T-164.2-14 | Denial of Service | ADD COLUMN on PROD | low | accept | nullable, no default, no rewrite; ADD COLUMN IF NOT EXISTS is metadata-only |
| T-164.2-SC | Tampering | package installs | low | accept | none |
</threat_model>

<verification>
- Task 1's lane command exit 0; Task 2's verify green.
- SUMMARY: three-reviewer findings + dispositions; A1 recorded as MEASURED (comment survived) or the option-(ii) fallback taken and named.
</verification>

<success_criteria>
Criterion 2's SQL half: provenance columns exist, the bridge respects them, the debt recorded in 20260826120000's header is paid in a reviewed migration that applies on the lane.
</success_criteria>

<output>
Create `.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-06-SUMMARY.md` when done.
</output>
