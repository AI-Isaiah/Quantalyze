---
phase: 164.2-curated-copy-the-curated-computation-error-sentence-must-act
plan: 07
type: execute
subsystem: sql-gates
tags: [sql-gate, pg-lane, mutation-runner, RED-UNDER, floors, anti-vacuity]
wave: 3
depends_on: ["164.2-06"]
files_modified:
  - supabase/tests/test_sync_status_curated_sentence_survives.sql
  - supabase/tests/test_sync_status_marked_refresh_protected.sql
  - supabase/tests/test_compute_jobs_error_kind_copy_parity.sql
  - supabase/tests/test_retention_orphaned_running.sql
  - scripts/mutation-runner/run.mjs
autonomous: true
requirements: [164.2-SC1, 164.2-SC2]
provides:
  - "A pg-lane gate that PERFORMS the compute_jobs transition through mark_compute_job_failed / mark_compute_job_done and reads computation_error back, with RED-UNDER-M twins the mutation runner can attribute"
  - "The three coupled gates test the DEPLOYED bridge (apply lists extended); FILES_FLOOR and ARMS_FLOOR raised to the measured values"
affects: [164.2-10]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A writer-stamped curated sentence for job J survives mark_compute_job_failed(J, …, 'permanent', tok) — read back from strategy_analytics after the RPC, not inspected in the body"
    - "The control: the same sequence WITHOUT the markers yields computation_error_copy('permanent')'s literal sentence (spelled literally, never compared to the function)"
    - "A marker for a DIFFERENT (older) job is overwritten AND both markers are nulled; a done job (branch (c)) nulls the sentence AND both markers; the protected b-prime arm survives the same way; a RETRYABLE mark_compute_job_failed (failed_retry, non-terminal per 20260826120000:437,624) reaches branch (a), which nulls the sentence AND both markers and reads computing — so every path where a compute_jobs row transitions is covered by branch, and the gate header says which arm covers which branch"
    - "Every arm uses the `TEST FAILED (X):` spelling so the runner attributes it (the template's 16 `ARM x FAILED` arms are INVISIBLE to the runner — measured: its RED-UNDER-M count is 1); each arm has a RED-UNDER-M twin that edits the NEW migration and is observed biting"
    - "node scripts/mutation-runner/run.mjs exits 0 with `No defects`, the three coupled gates still pass with the new migration appended, and FILES_FLOOR/ARMS_FLOOR are pinned at the run's own coverage:/arms: readings"
  artifacts:
    - path: "supabase/tests/test_sync_status_curated_sentence_survives.sql"
      provides: "applied-ness keyed on pg_attribute (independent of the bridge body), N arms with TEST FAILED spelling, RED-UNDER + RED-UNDER-M per arm, ALL N ARMS EXECUTED sentinel"
    - path: "scripts/mutation-runner/run.mjs"
      provides: "FILES_FLOOR = 45 (:789) and ARMS_FLOOR = <measured> (:1578); WAIVED_CEILING stays 0 (:1692)"
  key_links:
    - from: "supabase/tests/test_sync_status_curated_sentence_survives.sql"
      to: "supabase/migrations/20260906120000_computation_error_provenance.sql"
      via: "RED-UNDER-SETUP apply list ending at the new migration; RED-UNDER-M twins edit its CASE arms (layered with the self-verify anchors that would otherwise abort the apply)"
      pattern: "RED-UNDER-M"
---

<objective>
Criterion 1 as MEASUREMENT: "proven by a test that performs the transition and asserts the curated sentence survives it. Inspection of the bridge is not evidence." There is no trigger — the bridge is PERFORMed inside `mark_compute_job_failed` (`20260529180000:137`) and `mark_compute_job_done` (`20260603120000:439`) — so the gate must call the RPC on a real cluster, which is exactly what `test_sync_status_marked_refresh_protected.sql` does on the pg-lane ("it drives the REAL RPC", lines 18-21).

⭐ THE ANTI-VACUITY TRAP (PATTERNS correction 2): the template's 16 arms say `ARM x FAILED` and are invisible to the runner's identity regex (its own header, lines 139-143). A new gate that copies that spelling adds ZERO to `ARMS_FLOOR` — proof-shaped, proof-free. Copy the template's SEED -> claim -> RPC -> read-back SHAPE (lines 284-345) but the parity gate's `RAISE EXCEPTION 'TEST FAILED (X): …'` SPELLING (lines 269-271).

Also the coupling (CONTEXT's "biggest hazard"): three gates must test the DEPLOYED bridge, so the new migration is appended to their apply lists — in `test_sync_status_marked_refresh_protected.sql:156` (`RED-UNDER-SETUP`) after its last entry, 20260826120000, and after `20260826140000` in `test_compute_jobs_error_kind_copy_parity.sql:80` and `test_retention_orphaned_running.sql:269` (`RED-UNDER-SETUP`; PATTERNS correction 1: apply order is file order and 20260826140000 widens the error_kind CHECK the bridge reads). Then the mutation runner locally to `No defects` BEFORE push (CI's `sql-mutation` job exits 1 on any arm that does not bite, on a floor above measured, or on a waiver).

Purpose: criterion 1 closed by a test that would have caught the defect the day it shipped.
Output: the gate (seven arms + applied-ness), its twins observed biting, three apply lists with twin 0a re-proven, two floors, a clean runner exit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-CONTEXT.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-PATTERNS.md
Read when it exists (written by the plan this one depends on): .planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-06-SUMMARY.md

Read first: `scripts/mutation-runner/GRAMMAR.md` in full (marker at comment line start; `RED-UNDER-M` on the line after its `RED-UNDER:` prose; `find`/`anchor` may not name a `TEST FAILED (` literal — `parse.mjs:159` (`may name a`); the FIRST TEST FAILED names the arm — `scripts/mutation-runner/run.mjs:40` (`Red-anywhere is not success`)); `test_sync_status_marked_refresh_protected.sql` lines 117-149 (sentinel + counting rule + the arm-visibility caveat), line 156 (apply list), lines 193-197 (claim-fence rule), lines 206-252 (applied-ness idiom), lines 255-345 (seed + arm A), line 353 (why the expected sentence is spelled literally), line 796 (sentinel); `test_compute_jobs_error_kind_copy_parity.sql:80`, line 175, lines 252-271 (the LAYERING lesson: a twin that trips the migration's own self-verify aborts the apply and "no arm can be the first failure" — so a twin that reverts a CASE must ALSO re-point whatever self-verify anchor would RAISE on the reverted body); `scripts/lint-sql-gates.mjs:14-21` (R1-R7; R2 is the one a body-shape assertion trips); `scripts/mutation-runner/run.mjs:789`, line 1578, line 1692; the new migration's self-verify anchors (plan 06) — the twins are planned against them.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The survive arm and its control, end-to-end on the lane</name>
  <files>supabase/tests/test_sync_status_curated_sentence_survives.sql</files>
  <action>
    Create the gate with the template's header structure (purpose; why the RPC and not a table poke; the arm roster; the counting rule) and its `RED-UNDER-SETUP` apply list = `test_sync_status_marked_refresh_protected.sql:156` (`RED-UNDER-SETUP`) list + `supabase/migrations/20260906120000_computation_error_provenance.sql` appended. Applied-ness gate FIRST, keyed on something INDEPENDENT of the bridge body (lines 206-215's rule): `pg_attribute` for `computation_error_source` (the `20260802120000:544-556` shape) — RAISE `TEST FAILED (0): …` on absence, never SKIP. Seed users/profiles/api_keys/strategies as lines 255-279. Then:

    ARM S (survive): seed `strategy_analytics` for `s_a`; INSERT a `compute_jobs` row `status='running'` with a `claim_token` (the claim-fence rule) RETURNING `j`; emulate the WRITER: `UPDATE strategy_analytics SET computation_error = 'Insufficient CSV history. At least 2 data points required.', computation_error_source = 'writer', computation_error_job_id = j WHERE strategy_id = s_a`; `PERFORM mark_compute_job_failed(j, 'diag text', 'permanent', tok)`; assert the job is `failed_final` (else `TEST FAILED (S-SETUP): …` — the vacuity guard, lines 317-320's shape); SELECT `computation_error, computation_error_source, computation_error_job_id, computation_status` back; assert the sentence is byte-equal to the curated literal, status is `failed`, and the markers are still `'writer'` / `j` — each a separate `TEST FAILED (S): …` with the observed value in the message.
    ARM C (control): same for `s_b` WITHOUT the marker columns; assert `computation_error` equals the literal sentence `computation_error_copy('permanent')` returns — spelled LITERALLY (copy it from `20260826120000`'s function body, cite the line) — and both markers are NULL.

    Sentinel: `RAISE NOTICE 'ALL 2 ARMS EXECUTED (S, C): …'` for now (Task 2 widens it). Run the `<verify>` command below to exit 0 — MEASURED: `run.sh` does NOT read a gate's `RED-UNDER-SETUP` line; it needs `--workdir`, the list as `--apply`, and `--gate` (`scripts/pg-lane/run.sh:20-45`), which is why the command extracts the list off the gate (grep -a -o, then node -e over the JSON) and hands it to an inner bash -c (zsh does not word-split an unquoted variable; positional $2 inside bash -c does — commit 872135b6).

    W6 — the header states, BY BRANCH, why the arm roster covers every path where a `compute_jobs` row transitions: `mark_compute_job_failed` with `permanent`, or with attempts exhausted → `failed_final` → branch (b) [S, C, O] or, when the job is a marked refresh over a terminal-success row, (b-prime) [P, PC]; `mark_compute_job_failed` RETRYABLE (`transient`/`unknown` with attempts left, `20260529180000:109-122`) → `failed_retry`, which the bridge counts as NON-terminal (`20260826120000:437,624`) → branch (a) [R, Task 2]; `mark_compute_job_done` → `done` → branch (c) [D]; the Python DEFERRED path (`services.job_worker.dispatch` PERFORMs the SAME function directly while the job is still non-terminal, per the function's own COMMENT) → branch (a) — the CALLER differs, the branch and its write do not, so arm R is its proof too and the header says so. There is no fifth branch and no other writer of `compute_jobs.status` that PERFORMs the bridge (the 20260826120000 header's writer census, lines 73-160).
  </action>
  <verify>
    <automated>W=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p07-t1 && rm -rf "$W" && mkdir -p "$W" && g=supabase/tests/test_sync_status_curated_sentence_survives.sql && list=$(grep -a -o 'RED-UNDER-SETUP: .*' "$g" | sed 's/^RED-UNDER-SETUP: //' | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8"));console.log(j.apply.join(" "))') && test -n "$list" && bash -c 'bash scripts/pg-lane/run.sh --workdir "$1" --apply $2 --gate "$3"' _ "$W" "$list" "$g"</automated>
    <fails_when>The command passes a fresh `--workdir` (`$W`), the apply list read off the gate's own `RED-UNDER-SETUP` line (`grep -a -o … | node -e`, guarded by `test -n "$list"`) and `--gate`, through an inner `bash -c` so the list word-splits under zsh (commit 872135b6 — measured working on the same 15-file list, exit 0), so the exit status is the lane's own. It fails when: the gate file does not exist yet (`grep` finds no `RED-UNDER-SETUP` line → `$list` empty → `test -n` exits 1 before the lane boots); the line is not valid JSON with an `apply` array (`node -e` throws, `$list` empty, same `test -n` stop); `gate file not found` (the file was not created at that path); exit 2 (port collision); a listed migration's self-verify RAISE aborting the apply; `TEST FAILED (0)` (the provenance column is absent — the new migration is missing from the list); `TEST FAILED (S-SETUP)` (the job did not reach `failed_final` — claim-token mismatch or wrong signature); `TEST FAILED (S)` (the writer's sentence was overwritten with the generic literal, the status is not `failed`, or a marker was nulled — branch (b)'s CASE not keyed on this job); or `TEST FAILED (C)` (the control row without markers kept a sentence, or got a non-NULL marker). ⛔ Do NOT rewrite the inner `bash -c` as a plain `--apply $list` — under zsh that passes the whole list as ONE argument and the lane fails with a file-not-found on a path containing spaces (the B1 trap, measured)</fails_when>
  </verify>
  <done>Lane exit 0; both arms executed (sentinel printed); the survive arm reads the curated sentence back AFTER the real RPC.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining arms and every RED-UNDER-M twin, each observed biting</name>
  <files>supabase/tests/test_sync_status_curated_sentence_survives.sql</files>
  <action>
    Add: ARM O (older job): marker present but `computation_error_job_id` = a DIFFERENT, older `compute_jobs` row (insert one, mark it failed_final first or simply use a random uuid — prefer a real older job so the case is the header's (ii)); after the RPC the sentence is the generic literal AND both markers are NULL. ARM D (done clears): writer sentence + markers, then `PERFORM mark_compute_job_done(j, tok)` (read `20260603120000`'s signature); assert `computation_error IS NULL` and both markers NULL, status `complete`. ARM P (protected b-prime survives): seed the row as `complete_with_warnings` with `computed_at` in the past and the job with `metadata.source = 'ledger-refresh'` exactly as template arm A does (lines 284-300); writer-stamp; RPC; assert the sentence survived, status unchanged, `computed_at` unchanged. ARM PC (protected control): same without markers; assert the generic literal. ARM R (retryable → branch (a), W6): seed a row for `s_r` with a writer sentence + markers, INSERT a `running` job with a `claim_token`, `attempts = 0` and `max_attempts` at the table default (read the column's default in `20260411144407`), `PERFORM mark_compute_job_failed(j, 'diag text', 'transient', tok)`; assert the job is `failed_retry` (else `TEST FAILED (R-SETUP)` — the vacuity guard: a `permanent` kind or exhausted attempts would silently make this arm a duplicate of S); read back and assert `computation_status = 'computing'`, `computation_error IS NULL` AND both markers NULL — each its own `TEST FAILED (R): …` with the observed value. This is the branch-(a) proof and the proof for the Python DEFERRED direct call (same branch, different caller — see the Task 1 header statement). Widen the sentinel to `ALL 7 ARMS EXECUTED (S, C, O, D, P, PC, R)` and the header roster to match (CI's `sql-tests` loop counts them).

    Twins — one `RED-UNDER:` prose line + `RED-UNDER-M` JSON per arm, at comment line start, on consecutive lines, `find` strings never containing `TEST FAILED (`:
    - S: edit the NEW migration's branch-(b) CASE so the THEN arm becomes `EXCLUDED.computation_error` (the pre-fix behaviour) — LAYERED with an edit that re-points the migration's own (b)-CASE self-verify regex so the apply does not abort (lines 252-262's lesson). Expected first failure: `TEST FAILED (S)`.
    - C: edit the (b) predicate to `TRUE` (always keep) — layered the same way. Expected: `TEST FAILED (C)`.
    - O: drop the `computation_error_job_id = v_latest_job_id` conjunct — layered. Expected: `TEST FAILED (O)`.
    - D: remove the two marker NULLs from branch (c). Expected: `TEST FAILED (D)`.
    - P / PC: the b-prime CASE, mirrored. Expected: `TEST FAILED (P)` / `(PC)`.
    - R: remove the two marker NULLs from branch (a)'s `DO UPDATE` (the sentence NULL stays — that is today's behaviour). Expected: `TEST FAILED (R)` naming a marker that survived. ⚠️ If plan 06's self-verify anchors branch (a)'s marker NULLs, LAYER a re-point of that anchor as for S.
    - 0 (applied-ness, W3): GRAMMAR supports `edit`, `insert-after` and `sql` ONLY, and a `sql` step runs AFTER the apply list (the `--post-apply` hook, `run.sh` header). So the twin is `{"kind":"sql","stmt":"ALTER TABLE public.strategy_analytics DROP COLUMN computation_error_source"}` — the column (and its CHECK constraint with it) is gone when the gate's `pg_attribute` applied-ness probe runs. Expected: `TEST FAILED (0)`. ⛔ NOT an `edit` that typos the `ADD COLUMN` name: the migration's own `pg_attribute` self-verify would RAISE, abort the apply, and no arm could be the first failure (the LAYERING lesson, parity gate lines 252-271) — the runner scores a defect, not a bite.
    Run each twin through the runner's single-gate mode (read `run.mjs`'s CLI for the flag; plan 164.3-10's SUMMARY documents the invocation) and record the first-failure line for every arm in the SUMMARY under `## Twins observed biting`. A twin that scores `no-red` or `NO-IDENTITY` is a defect in the twin or the spelling — fix it; never waive (`WAIVED_CEILING` stays 0, two founder decisions deep).
    Then `node scripts/lint-sql-gates.mjs` (R1-R7) clean.
  </action>
  <verify>
    <automated>W=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p07-t2 && rm -rf "$W" && mkdir -p "$W" && g=supabase/tests/test_sync_status_curated_sentence_survives.sql && list=$(grep -a -o 'RED-UNDER-SETUP: .*' "$g" | sed 's/^RED-UNDER-SETUP: //' | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8"));console.log(j.apply.join(" "))') && test -n "$list" && bash -c 'bash scripts/pg-lane/run.sh --workdir "$1" --apply $2 --gate "$3"' _ "$W" "$list" "$g" && node scripts/lint-sql-gates.mjs</automated>
    <fails_when>The first link is Task 1's lane invocation (fresh `--workdir`, the list off `RED-UNDER-SETUP` through the inner `bash -c`, `test -n` guard), so its status is the lane's own and a red there stops the `&&` chain before the lint. It fails on: any `TEST FAILED (S|C|O|D|P|PC|R|0)` as the gate's first failure (the arm's predicate is wrong for the branch it targets — (b) for S/C/O, (c) for D, (b-prime) for P/PC, (a) for R — or a fixture does not reach the branch: R-SETUP fires when the transient job did not land on `failed_retry`), a sentinel that counts fewer arms than the roster (CI's `sql-tests` loop reads it), exit 2 on port collision; then `node scripts/lint-sql-gates.mjs` exits 1 printing the rule id and file for any R1–R7 firing on the corpus (a `RED-UNDER-M` `find` string containing `TEST FAILED (`, a twin not on consecutive comment lines, a prose line without its JSON twin). Each twin "observed biting" is a `run.mjs --file/--arm` observation recorded in the SUMMARY, not a signal of this command</fails_when>
  </verify>
  <done>Seven arms + applied-ness, eight twins (S, C, O, D, P, PC, R, 0), each observed biting with its own `TEST FAILED (X)` as the FIRST failure; the arm-0 twin is a `sql` DROP COLUMN step; lint clean; sentinel (`ALL 7 ARMS EXECUTED`) and roster agree.</done>
</task>

<task type="auto">
  <name>Task 3: Couple the three existing gates, raise the floors, run the corpus to No defects</name>
  <files>supabase/tests/test_sync_status_marked_refresh_protected.sql, supabase/tests/test_compute_jobs_error_kind_copy_parity.sql, supabase/tests/test_retention_orphaned_running.sql, scripts/mutation-runner/run.mjs</files>
  <action>
    Append `supabase/migrations/20260906120000_computation_error_provenance.sql` to the `RED-UNDER-SETUP` apply list: `marked_refresh_protected:156` (after `20260826120000`), `error_kind_copy_parity:80` and `retention_orphaned_running:269` (after `20260826140000` — it is the LAST entry there; confirm by reading each line). Run each of the three with the lane (fresh `--workdir`, its list, `--gate`, as in Task 1) to exit 0 (arm 0a/0b of the template must still pass — the comment survived; twins 2/A-3 and 3/F-3 still edit `computation_error_copy` in the OLD file, which the new migration does not redefine, so they keep biting).

    B2 — twin 0a MUST be re-run and observed biting AFTER the coupling: `test_sync_status_marked_refresh_protected.sql:232`'s twin strips `20260825150000` from the OLD migration's COMMENT, and the new migration now sits AFTER it in that apply list. Plan 06 keyed the new self-verify on `20260826120000` ONLY (option 1 of the checker's two — chosen because it keeps A1 measured without adding a second `edit` step to a twin that already carries two, and because a twin that must re-point a NEWER migration's assertion every time a migration is appended is the re-base hazard the gate's own comment at lines 225-231 warns about), so the new DO block must NOT RAISE under the twin and `TEST FAILED (0a)` must still be the FIRST failure. Run it through the runner's single-gate mode (`run.mjs --file supabase/tests/test_sync_status_marked_refresh_protected.sql`, arm `0a`) and quote its first-failure line in the SUMMARY under `## Twins observed biting` beside the eight new ones. If instead the new migration's DO block is the first failure, plan 06's self-verify still asserts the stripped id — fix plan 06's assertion (never the twin, never a waiver).

    Then the FULL corpus: `node scripts/mutation-runner/run.mjs` (about 10 minutes on this box). Read its OWN `coverage: files N/72` and `arms: A/A/0` lines. Set `FILES_FLOOR = 45` at `scripts/mutation-runner/run.mjs:789` (`FILES_FLOOR = 44` today) and `ARMS_FLOOR = A` at line 1578 (the measured value — expected 361 + the number of twins in Task 2, but READ it, never compute it in prose); leave `WAIVED_CEILING = 0`. Re-run the corpus: expected `✅ No defects`, exit 0, tallies agree. Any defect naming an arm in the three coupled gates is the coupling hazard — fix the cause (never the floor, never a waiver). Record the two lines and the exit code verbatim in the SUMMARY; per the CLAUDE.md rule, no number in prose is authoritative — the SUMMARY quotes the run.
  </action>
  <verify>
    <automated>L=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p07-runner.log && node scripts/mutation-runner/run.mjs > "$L" 2>&1; rc=$?; tail -25 "$L"; test "$rc" = "0"</automated>
    <fails_when>`run.mjs` is NOT piped — its stdout/stderr go to `$L` by redirection and its exit status is captured into `rc` on the very next statement, BEFORE `tail` runs; the final `test "$rc" = "0"` is the command's status. So it fails exactly when `run.mjs` exits non-zero: 1 on a `defects` table with rows (`no-red`, `wrong-first-failure`, `occurrence-mismatch`, a floor breach `FILES_FLOOR`/`ARMS_FLOOR` above measured, a tally disagreement between `arms:` and `lane-invocations:`, `lane-blocked-stale`); 2 on a narrowed run; 3 on a usage error or `the pg-lane is missing`. `tail -25` is display only — its own status is discarded and cannot mask `rc`. ⛔ Do NOT introduce `${PIPESTATUS[0]}` (EMPTY in this machine's zsh — measured) or `set -o pipefail`; the `rc=$?` capture is the mechanism, keep it. What the command cannot see: whether the floors were raised to the run's OWN readings rather than a number typed from prose — that is the SUMMARY's quoted `coverage:` / `arms:` lines against `scripts/mutation-runner/run.mjs:789` and line 1578</fails_when>
  </verify>
  <done>Corpus exit 0, `No defects`, `coverage: files 45/72`, `arms: A/A/0` with `ARMS_FLOOR = A` pinned in run.mjs, `WAIVED_CEILING = 0`; the three coupled gates pass with the new migration in their apply lists; twin 0a observed biting AFTER the coupling, its first-failure line quoted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate -> throwaway pg-lane cluster | never shared TEST, never PROD (`run.sh` EXIT trap) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.2-15 | Repudiation | mutation floors | medium | mitigate | floors pinned at the run's measured values in the same commit; CI `sql-mutation` exits 1 on a floor above measured or an arm that does not bite |
| T-164.2-16 | Tampering | twins editing the new migration | low | accept | twins are applied on the throwaway lane only and restored by the runner; the migration file in the tree is never mutated in place |
| T-164.2-SC | Tampering | package installs | low | accept | none |
</threat_model>

<verification>
- `node scripts/mutation-runner/run.mjs` exit 0 / `No defects`; `node scripts/lint-sql-gates.mjs` clean.
- SUMMARY: `## Twins observed biting` (eight new first-failure lines plus the template's twin 0a re-run after coupling) and the corpus `coverage:`/`arms:` lines quoted.
</verification>

<success_criteria>
Criterion 1 proven by a transition-performing gate with runner-attributable arms; criterion 2's bridge behaviour proven on both branches; the mutation corpus clean with floors raised.
</success_criteria>

<output>
Create `.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-07-SUMMARY.md` when done.
</output>
