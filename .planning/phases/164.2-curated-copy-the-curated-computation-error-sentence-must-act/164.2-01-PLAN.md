---
phase: 164.2-curated-copy-the-curated-computation-error-sentence-must-act
plan: 01
type: execute
subsystem: wizard-error-surfaces
tags: [161-ERRPREFIX, sentry, key-permission-badge, anti-vacuity]
wave: 1
depends_on: []
files_modified:
  - src/lib/sentry-capture.ts
  - src/components/connect/KeyPermissionBadge.tsx
  - src/components/connect/KeyPermissionBadge.test.tsx
autonomous: true
requirements: [161-ERRPREFIX]
provides:
  - "addSentryBreadcrumb sibling of captureToSentry in src/lib/sentry-capture.ts"
  - "KeyPermissionBadge renders prose only; the structured code reaches console.error and a Sentry breadcrumb"
affects: [164.2-10]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A KeyPermissionBadge probe refusal renders the route's prose sentence and NOT a `CODE: ` prefix, for EVERY code the route can answer (class, not one string)"
    - "The structured `err.code` is an argument of its own to console.error AND the message of a Sentry breadcrumb, so support-ticket greppability survives the split"
    - "Both halves are pinned by tests observed RED under their neuter: prefix restored -> render tests RED; code dropped from the log -> log test RED; breadcrumb removed -> breadcrumb test RED"
  artifacts:
    - path: "src/lib/sentry-capture.ts"
      provides: "export function addSentryBreadcrumb(...) with the captureToSentry try / dynamic-import / inner-try / .catch / outer-catch skeleton"
    - path: "src/components/connect/KeyPermissionBadge.tsx"
      provides: "throw new RouteResponseError(message) with err.code routed to console.error and addSentryBreadcrumb"
    - path: "src/components/connect/KeyPermissionBadge.test.tsx"
      provides: "three INVERTED pins (:121-140, :336, :344-368) plus the log-half and breadcrumb-half tests, it.each over the code class"
  key_links:
    - from: "src/components/connect/KeyPermissionBadge.tsx"
      to: "src/lib/sentry-capture.ts"
      via: "import { addSentryBreadcrumb } from \"@/lib/sentry-capture\" (client-safe module, already imported by other \"use client\" components)"
      pattern: "addSentryBreadcrumb"
---

<objective>
Close 161-ERRPREFIX per the 2026-08-26 founder ruling: a SPLIT, not a deletion. `KeyPermissionBadge.tsx:135-141` (`err.code ?` is the prefix branch) today throws a RouteResponseError whose message is the code, a colon, then the sentence, so a founder with a broken key reads `KEY_UNDECRYPTABLE: This stored key can no longer be decrypted…`. After this plan the user reads the prose only, and the structured code goes to `console.error` (as its own argument) and to a Sentry breadcrumb — preserving the greppability the prefix existed for (lines 137-138).

This is a CLASS change: the same site emits `PROBE_BACKEND_UNAVAILABLE`, `PROBE_FAILED`, `KEY_UNDECRYPTABLE` and others. The tests parametrise over the class.

Purpose: criterion 6 of Phase 164.2. Losing the code from the logs fails the ruling as surely as leaving the prefix in the render (CONTEXT, Specifics).
Output: the split component, the breadcrumb helper, and tests whose RED under neuter has been observed and recorded.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-CONTEXT.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-PATTERNS.md

Read first (PATTERNS §7 and §10 carry the excerpts): `src/lib/sentry-capture.ts:180-230` (client-safety constraint at lines 180-186 — the module imports exactly one module, `./seam-redaction`, and must keep that property; `captureToSentry` skeleton at lines 191-230); `src/components/connect/KeyPermissionBadge.tsx:125-160`; `src/components/connect/KeyPermissionBadge.test.tsx:121-140`, lines 318-368, lines 372-397; the repo's inverted-pin idiom at `src/app/api/portfolio-optimizer/route.test.ts:440-462`.

Anti-vacuity harness (every test in this plan): byte backup with `cp`, `shasum -a 256` before and after, restore with `cp` from the backup, NEVER `git checkout --`. Record each RED output verbatim in the SUMMARY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add addSentryBreadcrumb beside captureToSentry</name>
  <files>src/lib/sentry-capture.ts</files>
  <action>
    Add a second export, `addSentryBreadcrumb(options: { category: string; message: string; data?: Record<string, unknown>; level?: "fatal" | "error" | "warning" | "info" | "debug" })`, returning `Promise<void>`, with the IDENTICAL shape to `captureToSentry` at lines 191-230: outer try, `scrubRecord(options.data, [])` for the data, dynamic `import("@sentry/nextjs")`, inner try calling `Sentry.addBreadcrumb({ category, message, data, level: options.level ?? "info" })`, inner catch swallowing, `.catch(() => {})` on the import that RESOLVES rather than rejects (the lines 211-214 reasoning applies verbatim), and an outer catch returning `Promise.resolve()`. Do NOT add an import — the module must keep its one-import property (lines 180-186). Docblock: name the 2026-08-26 161-ERRPREFIX ruling as the reason this helper exists and state that the message is a machine code, never user prose. This is the first `addBreadcrumb` call in `src/` (measured: `grep -rn addBreadcrumb src` returns 0 hits), so say so in the docblock.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "^import" src/lib/sentry-capture.ts | grep -qx 1</automated>
  </verify>
  <done>`addSentryBreadcrumb` is exported, typechecks, and `src/lib/sentry-capture.ts` still has exactly one `import` line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split the badge — prose to the render, code to console.error and the breadcrumb</name>
  <files>src/components/connect/KeyPermissionBadge.tsx, src/components/connect/KeyPermissionBadge.test.tsx</files>
  <behavior>
    - it.each over the class [PROBE_BACKEND_UNAVAILABLE @502, PROBE_FAILED @503, KEY_UNDECRYPTABLE @409]: the route's `error` sentence is in the DOM and `document.body.textContent` does NOT contain the code string (the render half).
    - For the same fixtures: `console.error` was called with the code string as an argument BY IDENTITY (`consoleSpy.mock.calls.find((call) => call.some((arg) => arg === code))`, the lines 372-397 B-27 idiom) (the log half).
    - `addSentryBreadcrumb` (mocked via `vi.mock("@/lib/sentry-capture", () => ({ captureToSentry: vi.fn(), addSentryBreadcrumb: vi.fn() }))`) was called with `expect.objectContaining({ message: code })` (the breadcrumb half).
    - The existing pins at lines 121-140, line 336 and lines 344-368 are INVERTED, not deleted: keep every fixture, flip the assertion to prose-present + code-absent, and write the comment in the `portfolio-optimizer/route.test.ts:440-462` (`INVERTED rather than deleted`) shape (what the old pin asserted, why it was the defect, that the reversal is deliberate, the 2026-08-26 ruling). line 336 is a `waitFor(getByText(/PROBE_FAILED/))` used as the wait condition for the "no stale security claim survives" test — it flips to waiting on the prose "Could not check key scopes. Try again."; its two real assertions (`scopeChips()` empty, summary absent) stay.
  </behavior>
  <action>
    Component (`KeyPermissionBadge.tsx:135-141`, `const message = err.error`): compute `message` as today; when `err.code` is a non-empty string, call `console.error("[KeyPermissionBadge] probe refused with code:", err.code, message)` — the code MUST be its own argument, not interpolated, or the identity assertion cannot see it — and `void addSentryBreadcrumb({ category: "key-permission-badge", message: err.code, data: { status: res.status }, level: "warning" })`; then `throw new RouteResponseError(message)` with no prefix. Add the import `import { addSentryBreadcrumb } from "@/lib/sentry-capture"` (the file is `"use client"`; `EquityChart.tsx:17` (`captureToSentry`) already imports this module from a client component, so the boundary is proven). Rewrite the lines 136-137 comment to record the split and the ruling; keep line 149's `console.error` of the Error object as is. Everything else in the catch (lines 145-160, B-27) is untouched.

    Tests: write the `<behavior>` tests first and run them RED against the unsplit component, then implement. Add the `vi.mock("@/lib/sentry-capture", …)` factory at the top of the test file (measured: this test file has no `vi.mock` today); `vi.mocked(addSentryBreadcrumb).mockClear()` in a `beforeEach`.
  </action>
  <verify>
    <automated>npx vitest run src/components/connect/KeyPermissionBadge.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>All KeyPermissionBadge tests green; no test in the file asserts a `CODE: ` shape in the DOM; the three flipped pins carry the inverted-pin comment.</done>
</task>

<task type="auto">
  <name>Task 3: Neuter-and-observe all three halves, byte-verified restore</name>
  <files>src/components/connect/KeyPermissionBadge.tsx</files>
  <action>
    Three neuters, each on a fresh byte backup (`cp` to the scratchpad; `shasum -a 256` before; restore with `cp`; `shasum` after must equal before; NEVER `git checkout --`):
    1. Restore the prefix: change the throw back to `err.code ? `${err.code}: ${message}` : message`. Run the test file. Expected RED: every it.each render row AND the three inverted pins, by name. Record the `FAIL` lines verbatim.
    2. Drop the code from the log: remove the `err.code` argument from the new `console.error` call (leave the breadcrumb). Expected RED: only the log-half tests. Record.
    3. Remove the `addSentryBreadcrumb` call. Expected RED: only the breadcrumb-half tests. Record.
    After each restore, re-run the file GREEN. If any neuter leaves the suite green, the test is vacuous — fix the test, do not weaken the neuter. Write the three RED excerpts and the three shasum pairs into the SUMMARY under a heading `## Anti-vacuity observations`.
  </action>
  <verify>
    <automated>npx vitest run src/components/connect/KeyPermissionBadge.test.tsx && shasum -a 256 src/components/connect/KeyPermissionBadge.tsx</automated>
  </verify>
  <done>Three RED observations recorded with the failing test names; file hash after the final restore equals the pre-neuter hash; suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route response -> browser DOM | the `{ error, code }` body of `/api/keys/[id]/permissions` is rendered to the user |
| browser -> Sentry | breadcrumb data leaves the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.2-01 | Information Disclosure | addSentryBreadcrumb data | low | mitigate | data passes through `scrubRecord` exactly as captureToSentry's does; message is the machine code, never the sentence |
| T-164.2-02 | Information Disclosure | KeyPermissionBadge render | low | accept | B-27 (lines 145-160) still restricts the render to the route's own reviewed body; this plan only removes the code prefix |
| T-164.2-SC | Tampering | npm installs | low | accept | no package is installed by this plan |
</threat_model>

<verification>
- `npx vitest run src/components/connect/KeyPermissionBadge.test.tsx && npx tsc --noEmit` green.
- SUMMARY carries three RED observations with test names and three shasum pairs.
- `grep -n '\${err.code}:' src/components/connect/KeyPermissionBadge.tsx` returns nothing.
</verification>

<success_criteria>
Criterion 6: prose only to the user, code to log AND breadcrumb, class-wide, both halves proven by tests observed RED under neuter.
</success_criteria>

<output>
Create `.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-01-SUMMARY.md` when done, with the `## Anti-vacuity observations` section.
</output>
