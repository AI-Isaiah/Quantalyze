# Phase 164.2: CURATED-COPY — Pattern Map

**Mapped:** 2026-09-06 at HEAD `03ae5fa0` (branch `phase-164.1-prod-observability`)
**Files analyzed:** 31 (9 CREATE, 22 MODIFY)
**Analogs found:** 29 / 31 (2 partial — see "No Analog Found")

Every `path:line` below was opened with `sed -n` / `grep -a` in this session unless it is
explicitly marked "(per RESEARCH, not re-opened here)". Every analog path was checked with
`git ls-files` and is tracked. Two path corrections against RESEARCH: `main_worker.py` is at
`analytics-service/main_worker.py` (not `services/`), and `EquityChart.tsx` is at
`src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx`.

## ⚠️ Corrections to RESEARCH, measured this session

The planner must not inherit these four sentences from RESEARCH:

1. **Only ONE of the three coupled gates' apply lists ends at `20260826120000`.**
   `test_sync_status_marked_refresh_protected.sql:156` ends there; the other two —
   `test_compute_jobs_error_kind_copy_parity.sql:80` and `test_retention_orphaned_running.sql`
   — END AT `20260826140000_compute_jobs_error_kind_orphaned.sql`. The new migration is appended
   AFTER `20260826140000` in those two (apply order is file order, and `20260826140000` widens
   the `error_kind` CHECK the bridge reads).
2. **The template gate's 16 arms are INVISIBLE to the mutation runner.** Its own header says so
   (`:139-143`): "ONLY THE TWO APPLIED-NESS GATES USE THE `TEST FAILED (…)` IDIOM. All 16 arms
   below say `ARM x FAILED` / `ARM x SETUP BROKEN`, so they are invisible to the runner's
   identity regex". Measured: `grep -c '^[ \t]*-- RED-UNDER-M:'` on that file = **1** (arm `0a`).
   A new gate that copies the template's `ARM x FAILED` spelling contributes ZERO arms to
   `ARMS_FLOOR`. The new gate must copy the template's SEED → claim → RPC → read-back SHAPE but
   the parity gate's `RAISE EXCEPTION 'TEST FAILED (X): …'` SPELLING (`:269-271`).
3. **`KeyPermissionBadge.test.tsx` has TWO prefix pins, not one.** `:121-140` (RESEARCH's) AND
   `:344-368`, which asserts the literal `"PROBE_FAILED: Could not check key scopes. Try again."`
   at `:366`. A third site, `:336` `getByText(/PROBE_FAILED/)`, asserts the code is in the DOM
   for a test whose fixture (`:318-335`) I did not open — the executor must open it before
   deciding whether it also flips. Both pins go RED the moment the prefix leaves the render.
4. **No `GRANT … TO service_role` exists for the bridge in the latest definition.** `:909` is a
   bare `REVOKE ALL … FROM PUBLIC, anon, authenticated`; the symmetric REVOKE+GRANT pair at
   `:333-334` is for `computation_error_copy`, not the bridge. The re-base preserves `:909`
   exactly; "keep the GRANT block" in RESEARCH Pattern 2 (2) has nothing to keep.

## File Classification

| New/Modified File | C/M | Role | Data Flow | Closest Analog | Match |
|---|---|---|---|---|---|
| `supabase/migrations/<ts>_computation_error_provenance.sql` | C | migration (schema + plpgsql re-base) | transform | `supabase/migrations/20260826120000_computation_error_curated_copy.sql` (re-base body) + `20260802120000_strategy_analytics_stuck_computing_reaper.sql:215-235,544-556` (ADD COLUMN + self-verify) | exact |
| `supabase/schema/functions/sync_strategy_analytics_status.sql` | M | generated snapshot | batch | itself — regenerated by `npm run schema:functions` (`scripts/dump-sql-functions.ts:16-32,289-311`) | exact |
| `supabase/tests/test_sync_status_curated_sentence_survives.sql` | C | test (SQL gate, pg-lane) | request-response (RPC) | `test_sync_status_marked_refresh_protected.sql` (shape) + `test_compute_jobs_error_kind_copy_parity.sql` (arm idiom + body-editing twins) | exact (split across two) |
| `supabase/tests/test_sync_status_marked_refresh_protected.sql` | M (`:156` apply list) | test | — | itself | exact |
| `supabase/tests/test_compute_jobs_error_kind_copy_parity.sql` | M (`:80` apply list) | test | — | itself | exact |
| `supabase/tests/test_retention_orphaned_running.sql` | M (apply list) | test | — | itself | exact |
| `scripts/mutation-runner/run.mjs` | M (`:789`, `:1578`) | config (floors) | — | itself | exact |
| `scripts/pg-lane/fixtures/27-fixture-strategy-analytics-computation-error.sql` | M only if needed | fixture | — | itself (`:19-20`) | exact |
| `analytics-service/services/analytics_runner.py` | M (P6–P11, signature `:1178-1184`) | service (writer) | CRUD upsert | itself — P7 literal `:1604-1619` | exact |
| `analytics-service/services/job_worker.py` | M (P1–P5, caller `:2188-2193`) | service (writer) | CRUD upsert | itself — `:2983-2993`, `:3013-3027` | exact |
| `analytics-service/tests/test_computation_error_provenance_census.py` | C | test (AST census) | batch | `analytics-service/tests/test_computing_started_at_stamp.py` | exact |
| `analytics-service/tests/test_computing_started_at_stamp.py` | M only if the exempt key set moves (`:161-170`) | test | — | itself | exact |
| `src/lib/database.types.ts` | M (regenerated) | generated types | — | none (Open Q5) | — |
| `src/app/api/strategies/create-with-key/route.ts` | M (`:478`, `:494`, `:666-672`, `:900`) | route (emitter) | request-response | itself — `KEY_ORPHANED` hand-roster precedent `:1375-1390` | exact |
| `src/app/api/strategies/composite/set-members/route.ts` `:99-101`, `composite/add-key/route.ts` `:304-306`, `keys/validate-and-encrypt/route.ts` `:298-300` | M | route (limiter arms) | request-response | `create-with-key/route.ts:487-500` (same `rateLimitDenyJson` shape) | exact |
| `src/lib/wizardErrors.ts` | M (union `:141`; new entry; `:1919-1936` DRAFT cause; roster `:5091`) | config (copy roster) | — | itself — `RATE_LIMITED` entry `:3326-3335` | exact |
| `src/lib/wizardErrors.test.ts` (⚠️ NUL byte at 1572) | M (`:2021`, `:2496`) | test | — | itself | exact |
| `src/lib/wizardErrors.invariant.test.ts` | M (`:1994-2012` add 429 twin; `:240-256` stays 13) | test | — | itself | exact |
| `src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx` `:272-300`, `MultiKeyConnectStep.tsx:280` | M (rosters) | component config | — | `ConnectKeyStep.tsx:289-299` (VENUE_ALREADY_CONNECTED row + its comment) | exact |
| `src/app/api/strategies/create-with-key/route.test.ts` | M (pins at `:1242`, `:1269`, `:3957`, `:3971` — grep hits) | test | — | itself — inverted-pin idiom `:1695-1709` | exact |
| `src/lib/wizardErrors.roster-render.test.tsx` | C | test (render, table-driven) | request-response (mocked fetch) | `ConnectKeyStep.preselect-refusal-class.test.tsx:58-68,181-187,451-470` + `SubmitStep.test.tsx:867-905` + `wizardErrors.test.ts:3844-3880` | role-match (three parts) |
| `src/components/connect/KeyPermissionBadge.tsx` | M (`:135-141`) | component | request-response | itself | exact |
| `src/components/connect/KeyPermissionBadge.test.tsx` | M (invert `:121-140`, `:344-368`; check `:336`; log-half from `:372-397`) | test | — | itself + `portfolio-optimizer/route.test.ts:440-462` (flip idiom) | exact |
| `src/lib/sentry-capture.ts` | M (add `addSentryBreadcrumb`) | utility | event (fire-and-forget) | itself — `captureToSentry` `:191-230` | exact |
| `src/lib/freshness.ts` | M (`:30` sibling const; `:210-213`) | utility | transform | itself — `CLOCK_SKEW_TOLERANCE_MINUTES` `:23-30` | exact |
| `src/app/factsheet/[id]/v2/FactsheetView.tsx` | M (`:9` import; `:1191-1194` only) | component | transform | itself | exact |
| `src/components/strategy/SyncBadge.staler-of-two.test.tsx` | M (`:265-280` move control) | test | — | itself | exact |
| `src/app/factsheet/[id]/v2/FactsheetView.chip-honesty.test.tsx` | M (chip twin of `staler-of-two:62-71`) | test | — | C-4 `:261-269` (fixture) + C-10 `:340-349` (wrong-scope tripwire) | exact |
| `src/lib/freshness.two-surfaces.test.tsx` (or hosted in chip-honesty) | C | test (joint render) | — | `SyncBadge.staler-of-two.test.tsx:220-235` (prose-only agreement) | role-match |
| `src/app/factsheet/[id]/v2/page.tsx` | M (`:414-421`) | RSC page | request-response | itself | exact |
| `src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx` | M (extend V2 `:202-212`) | test | — | itself | exact |

## Pattern Assignments

### 1. `supabase/migrations/<ts>_computation_error_provenance.sql` (CREATE — migration)

**Timestamp:** must sort after the newest file, `20260827130000_sanitize_user_revoke_strategy_shares.sql` (measured `ls supabase/migrations | tail`). `supabase/migrations/down/` holds `<ts>-rollback.sql` companions for some migrations (last: `20260714090000-rollback.sql`) — optional convention, not gated.

**Analog for the re-base:** `supabase/migrations/20260826120000_computation_error_curated_copy.sql` (1393 lines) — the LATEST definition of `sync_strategy_analytics_status`. Measured: `grep -ln "FUNCTION .*sync_strategy_analytics_status" supabase/migrations/*.sql` → seven files, newest `20260826120000`. `20260826140000` is not among them.

**How that file documents its own re-base** (`:198-199`, copy this convention verbatim in the new header):
```sql
-- THE DELTA against 20260825150000 (five prior definitions exist; this re-bases
-- on the LATEST, 20260825150000_sync_status_protect_marked_refresh.sql):
--   1. NEW `computation_error_copy(TEXT)` — …
--   5. Nothing else. Every CR-01 / JOB-01 / F-3 / PUB-02 / SI-02 anchor in the
--      self-verify block is carried forward; the four that were keyed on the
--      old `v_protected_error` spelling are RE-ANCHORED rather than deleted
```
The new header's delta list is: (1) two `ADD COLUMN`s; (2) two new `DECLARE`s + two `array_agg` aggregates; (3) branch (b) `CASE`; (4) branch (b-prime) `CASE`; (5) marker clears in (a)/(c); (6) nothing else.

**What the re-base has to preserve — the function head** (`:336-341`):
```sql
CREATE OR REPLACE FUNCTION sync_strategy_analytics_status(p_strategy_id UUID)
RETURNS VOID
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_catalog
AS $$
```
and the ACL line after it (`:909`) — a bare REVOKE, no GRANT:
```sql
REVOKE ALL ON FUNCTION sync_strategy_analytics_status FROM PUBLIC, anon, authenticated;
```
Its self-verify asserts these took (`:927` SECURITY DEFINER, `:936-937` `search_path=public`, `:957`/`:960` anon/authenticated cannot EXECUTE) — carry those four assertions forward.

**Open Question 1 answered (which variable identifies the failing job):** NONE today. The `DECLARE` block (`:342-353`) declares `v_latest_kind TEXT` (`:352`) and `v_protected_kind TEXT` (`:353`) and nothing job-id-shaped (`grep -n job_id` over the file → no DECLARE hit). Both kinds come off the aggregate at `:654-661`:
```sql
    (array_agg(error_kind ORDER BY created_at DESC)
       FILTER (WHERE NOT is_protected))[1],
    (array_agg(error_kind ORDER BY created_at DESC)
       FILTER (WHERE is_protected))[1]
    INTO v_failed_count, v_protected_count, v_unresolved_count,
         v_latest_kind, v_protected_kind
    FROM live_failures;
```
The same shape yields the ids: add `v_latest_job_id UUID; v_protected_job_id UUID;` to DECLARE and two more `(array_agg(<job id column> ORDER BY created_at DESC) FILTER (…))[1]` terms. ⚠️ The `live_failures` CTE's SELECT list (`:~380-640`, `FROM compute_jobs f` at `:631`) was NOT opened this session — the planner's migration task must confirm `f.id` is projected (or add it) before writing the aggregate.

**The four writes the new body changes** — quote them from the old file, never retype:
- (a) `:757-766` — `VALUES (p_strategy_id, 'computing', NULL, now())` … `computation_error = EXCLUDED.computation_error` → add both marker columns to the INSERT column list with NULL.
- (b) `:804-813`:
  ```sql
  IF v_failed_count > 0 THEN
    INSERT INTO strategy_analytics (strategy_id, computation_status, computation_error, computing_started_at)
    VALUES (p_strategy_id, 'failed', computation_error_copy(v_latest_kind), NULL)
    ON CONFLICT (strategy_id) DO UPDATE
       SET computation_status = EXCLUDED.computation_status,
           computation_error  = EXCLUDED.computation_error,
  ```
  → the `DO UPDATE` arm becomes `computation_error = CASE WHEN strategy_analytics.computation_error_source = 'writer' AND strategy_analytics.computation_error_job_id = v_latest_job_id THEN strategy_analytics.computation_error ELSE EXCLUDED.computation_error END` (the `VALUES` arm keeps the generic — a fresh row has no writer sentence to preserve).
- (b-prime) `:874-880` — `SET computation_error = computation_error_copy(v_protected_kind), computing_started_at = NULL WHERE strategy_id = p_strategy_id;` → same CASE keyed on `v_protected_job_id`.
- (c) `:893-905` — `computation_error = NULL` → add both markers `= NULL`.

**Anchors in the OLD file's self-verify that the NEW body will no longer match** — these run only at `20260826120000`'s own apply time (against its own body), so they do not break, but the new migration must RE-ANCHOR them (the `:237-240` rule), not drop them:
- `:1296` `IF v_fn !~* 'VALUES\s*\(\s*p_strategy_id\s*,\s*''failed''\s*,\s*computation_error_copy\s*\(\s*v_latest_kind\s*\)'` — still true if the VALUES arm keeps the generic.
- `:1190` `IF v_fn !~* 'SET\s+computation_error\s*=\s*computation_error_copy\s*\(\s*v_protected_kind\s*\)'` — becomes FALSE under a CASE; re-anchor on the CASE's ELSE arm.
- `:1290-1292` the `last_error` ban — verbatim, keep it:
  ```sql
  IF v_fn ~* 'last_error' THEN
    RAISE EXCEPTION 'HONEST-01 verification failed: sync_strategy_analytics_status references compute_jobs.last_error. … If this fired on a COMMENT rather than on code, the comment is still the defect: move the prose to the migration file header, which pg_get_functiondef does not return';
  ```
  ⛔ Do not write the words `last_error` anywhere inside the new function body, comments included.

**ADD COLUMN analog:** `20260802120000_strategy_analytics_stuck_computing_reaper.sql:215-235` — nullable, no default, `COMMENT ON COLUMN` stating the NULL semantic:
```sql
-- Nullable, NO DEFAULT, NO SET NOT NULL. A `NOT NULL DEFAULT now()` here would
-- be a 23502 timebomb against every existing writer that upserts
-- strategy_analytics without the column (migration-reviewer #6), …
ALTER TABLE public.strategy_analytics
  ADD COLUMN IF NOT EXISTS computing_started_at timestamptz;

COMMENT ON COLUMN public.strategy_analytics.computing_started_at IS
  'JOB-01 (Phase 142): when the CURRENT computation entered '
  'computation_status = ''computing''. NULL means "not currently computing" -- '
```
and its self-verify (`:544-556`) via `pg_attribute` — type + nullability asserted, not assumed:
```sql
    FROM pg_attribute a
   WHERE a.attrelid = 'public.strategy_analytics'::regclass
     AND a.attname = 'computing_started_at'
     AND NOT a.attisdropped;
  IF v_atttypid IS NULL THEN
    RAISE EXCEPTION 'JOB-01 verification failed: strategy_analytics.computing_started_at column missing after ADD COLUMN';
  END IF;
  IF v_atttypid <> 'timestamptz'::regtype THEN …
  IF v_attnotnull THEN
    RAISE EXCEPTION '… is NOT NULL (a 23502 timebomb against existing writers); it must be nullable';
```
`20260803150000_strategy_analytics_series_completeness.sql:92-94,140-144` is the second precedent and records WHY the type check exists: "ADD COLUMN IF NOT EXISTS silently no-ops on a pre-existing column of ANY type".

**⛔ The COMMENT / apply-list coupling (the phase's biggest hazard) — measured, not restated:**
- `COMMENT ON FUNCTION` is at `:907` of the old file and is the applied-ness KEY for arms `0a`/`0b` of the template gate (`:233-236` regex `!~ '20260825150000'`, `:247-250` regex `!~ '20260826120000'`).
- Arm `0a`'s twin (`test_sync_status_marked_refresh_protected.sql:232`) EDITS the OLD file's comment text:
  ```
  -- RED-UNDER-M: {"arm":"0a","apply":[{"kind":"edit","file":"supabase/migrations/20260826120000_computation_error_curated_copy.sql","find":"PROTECTED MARKED REFRESH (mig 20260825150000, Phase 161.1 CR-01)","replace":"PROTECTED MARKED REFRESH (Phase 161.1 CR-01)","occurrences":1},{"kind":"edit","file":"…20260826120000…","find":"+ 20260802120000 + 20260825150000 + 20260826120000.';","replace":"+ 20260802120000 + 20260826120000.';","occurrences":1}]}
  ```
  and the gate's own header explains the re-base rule (`:146-149`): "The twin targets 20260826120000, NOT the 20260825150000 the arm names: that COMMENT is re-stamped by five prior migrations and last by 20260826120000:907, so a mutation of the earlier stamp is overwritten later in the same apply list and proves nothing."
- Two options, surfaced rather than blended (Rule 7):
  - **(i) RECOMMENDED — do NOT reissue `COMMENT ON FUNCTION`.** `CREATE OR REPLACE` keeps the function's oid, so the `pg_description` row survives; arm `0a` keeps biting unchanged. Assert this in the new self-verify with the gate's own idiom — `IF COALESCE(obj_description('sync_strategy_analytics_status(uuid)'::regprocedure,'pg_proc'),'') !~ '20260826120000' THEN RAISE …` — so A1 is measured at apply time. The NEW gate's applied-ness key is then the COLUMN (the `pg_attribute` shape above), which is independent of the bridge body — the property the template's header demands at `:206-215` ("A presence gate must be independent of the thing under test").
  - (ii) Reissue with all ids + the new one, AND re-point `0a`'s twin at the new file in the same commit. More edits, same coverage. Flag for cleanup if chosen: the `See migrations …` roll-call convention is what pulls toward (ii).
- Twins `2/A-3` and `3/F-3` in the parity gate (`:175`, `:263`) edit `computation_error_copy` inside the OLD file. ⛔ The new migration must NOT redefine `computation_error_copy`, or those mutations are overwritten in-list and score `no-red` → `sql-mutation` exit 1.
- Floors: `scripts/mutation-runner/run.mjs:789` `FILES_FLOOR = 44`, `:1578` `ARMS_FLOOR = 361`, `:1692` `WAIVED_CEILING = 0`. A new annotated gate raises the first two in the same commit; read the run's `coverage:`/`arms:` lines, never a number in prose.

**After the migration:** `npm run schema:functions` regenerates `supabase/schema/functions/sync_strategy_analytics_status.sql` (the four snapshots present: `computation_error_copy`, `mark_compute_job_done`, `mark_compute_job_failed`, `sync_strategy_analytics_status`); CI's `--check` (`scripts/dump-sql-functions.ts:32`, drift report `:300-311`) fails on a stale snapshot.

---

### 2. `supabase/tests/test_sync_status_curated_sentence_survives.sql` (CREATE — SQL gate on the pg-lane)

**Analog A (shape):** `supabase/tests/test_sync_status_marked_refresh_protected.sql` (799 lines).

**Why this file is the template — its own words** (`:18-21`): "This gate therefore does the ONE thing those could not: it drives the REAL RPC. Every arm calls `mark_compute_job_failed(...)` and then reads `strategy_analytics.computation_status` back. Nothing here asserts on an intermediate payload."

**Apply list** (`:156`, copy and append the new migration at the END):
```
-- RED-UNDER-SETUP: {"apply":["scripts/pg-lane/fixtures/01-fixture-core.sql","scripts/pg-lane/fixtures/02-fixture-sanitize-tables.sql","scripts/pg-lane/fixtures/03-fixture-compute-jobs.sql","scripts/pg-lane/fixtures/27-fixture-strategy-analytics-computation-error.sql","supabase/migrations/20260411144407_compute_jobs_queue.sql","scripts/pg-lane/fixtures/04-fixture-compute-jobs-targets.sql","supabase/migrations/20260510175507_process_key_long_compute_job_kinds_repair.sql","supabase/migrations/20260515114555_compute_jobs_claim_token_fencing.sql","supabase/migrations/20260522111858_compute_analytics_from_csv_kind.sql","supabase/migrations/20260614120000_derive_broker_dailies_kind.sql","supabase/migrations/20260708120000_sync_status_failed_final_bounce.sql","supabase/migrations/20260710120000_strategy_keys.sql","supabase/migrations/20260710130000_stitch_composite_kind.sql","supabase/migrations/20260825150000_sync_status_protect_marked_refresh.sql","supabase/migrations/20260826120000_computation_error_curated_copy.sql"]}
```
The lane's `strategy_analytics` is a 5-column stand-in (`scripts/pg-lane/fixtures/03-fixture-compute-jobs.sql:42-48`) plus fixture 27's `ADD COLUMN IF NOT EXISTS computation_error TEXT` (`:19-20`). The new migration's own `ADD COLUMN IF NOT EXISTS` adds the two marker columns to that stand-in, so fixture 27 needs no edit unless the migration references production-only columns.

**Seed → claim → RPC → read-back** (`:284-345`):
```sql
  v_before := now() - INTERVAL '3 days';
  INSERT INTO strategy_analytics (strategy_id, computation_status, computation_warned, computed_at)
  VALUES (s_a, 'complete_with_warnings', TRUE, v_before), …
  -- ===== ARM A — protected single-key refresh ============================
  tok := gen_random_uuid();
  INSERT INTO compute_jobs (strategy_id, kind, status, claim_token, attempts, max_attempts, metadata)
  VALUES (s_a, 'derive_broker_dailies', 'running', tok, 1, 3,
          jsonb_build_object('source', 'ledger-refresh', 'enqueued_at', now()))
  RETURNING id INTO j;

  PERFORM mark_compute_job_failed(j, 'mt5 gateway IPC timeout (-10005)', 'permanent', tok);

  SELECT status INTO v_jobstat FROM compute_jobs WHERE id = j;
  IF v_jobstat IS DISTINCT FROM 'failed_final' THEN
    RAISE EXCEPTION 'ARM A SETUP BROKEN: the job is % , not failed_final. Nothing was ever asked of the bridge, so the status assertion below would pass vacuously.', v_jobstat;
  END IF;

  SELECT computation_status, computation_error, computed_at, computing_started_at
    INTO v_status, v_error, v_computed, v_anchor
    FROM strategy_analytics WHERE strategy_id = s_a;
```
The claim-fence rule is at `:193-197`: "Every arm seeds its job as status='running' with a claim_token, because mark_compute_job_failed refuses anything else … Finality is forced with p_error_kind = 'permanent'". The users/profiles/api_keys/strategies seed is `:255-279`.

For the new gate the WRITER-emulation step goes between the job INSERT and the `PERFORM`: `UPDATE strategy_analytics SET computation_error = '<curated>', computation_error_source = 'writer', computation_error_job_id = j WHERE strategy_id = s_x;` — for the survive arm; omit the two marker columns for the control arm (expect `computation_error_copy('permanent')`'s literal, spelled out as at `:353` — "Spelled LITERALLY on purpose — asserting `= computation_error_copy('permanent')` would pass against a copy function that leaks its argument"). A third arm: marker present but `job_id` = a DIFFERENT (older) job → expect overwrite (header cases (ii)/(iii)). A fourth: branch (c) clears the markers (a done job after a writer sentence).

**Analog B (arm SPELLING the runner can attribute):** `supabase/tests/test_compute_jobs_error_kind_copy_parity.sql:269-271`:
```sql
  IF v_orphaned = v_permanent THEN
    RAISE EXCEPTION 'TEST FAILED (3/F-3): computation_error_copy(''orphaned'') returns the SAME sentence as ''permanent''. …';
  END IF;
```
`run.mjs:40`: "FIRST `TEST FAILED (…)` names the annotated arm. Red-anywhere is not success". Every arm in the NEW gate uses `TEST FAILED (<ARM>):`, not `ARM x FAILED`.

**The twin that edits a migration body** (`:263`, layered — GRAMMAR "Shape 3"):
```
-- RED-UNDER-M: {"arm":"3/F-3","apply":[{"kind":"edit","file":"supabase/migrations/20260826120000_computation_error_curated_copy.sql","find":"      'Analytics stopped before it finished because the process running it went away. Nothing is wrong with this strategy — retry the sync.'","replace":"      'Analytics stopped before it finished because the process running it went away. Nothing is wrong with this strategy.'","occurrences":1},{"kind":"edit","file":"…","find":"IF computation_error_copy('orphaned') NOT ILIKE '%retry the sync%' THEN","replace":"IF computation_error_copy('orphaned') NOT ILIKE '%went away%' THEN","occurrences":1}]}
```
The layering note at `:252-262` is the lesson: a mutation that trips the migration's OWN self-verify aborts the apply and "no arm can be the first failure" — so a twin that reverts the CASE in the new migration must ALSO re-point whatever new self-verify anchor would RAISE on the reverted body. Plan the anchor and the twin together.

Grammar rules that bite: marker must be at comment line start (`GRAMMAR.md:44-47`); the `RED-UNDER-M` line sits on the line after its prose `RED-UNDER:` (`:34-39`); `find`/`anchor` may not name a `TEST FAILED (` literal (`parse.mjs:159`).

**Sentinel:** the template ends with `RAISE NOTICE 'ALL 16 ARMS EXECUTED (A, B, C, D, E, F, G, H, H2, I, I2, I3, I4, J, J2, K): …'` (`:796`); its header (`:117-129`) says CI's `sql-tests` loop counts the roster and fails when it disagrees with N. Copy the shape with the new gate's own N and roster.

**Local run:** `scripts/pg-lane/run.sh --gate supabase/tests/<file>.sql` (`run.sh:22`, `:264` "--gate is required"; pg_cron preloaded `:53`), then `node scripts/mutation-runner/run.mjs` to `✅ No defects`, then `node scripts/lint-sql-gates.mjs` (rules R1–R7, `lint-sql-gates.mjs:14-21`; R2 "pg_get_functiondef regex satisfied by a comment" is the one a body-shape assertion is most likely to trip).

**Also MODIFY:** append the new migration to `test_sync_status_marked_refresh_protected.sql:156` (after `20260826120000`), `test_compute_jobs_error_kind_copy_parity.sql:80` and `test_retention_orphaned_running.sql` (both after `20260826140000`).

---

### 3. Python writers — `analytics_runner.py` (P6–P11) and `job_worker.py` (P1–P5) (MODIFY)

**Representative literal** — `analytics-service/services/analytics_runner.py:1603-1619` (P7, the ROADMAP sentence):
```python
                def _mark_failed() -> None:
                    _insufficient_payload: dict[str, Any] = {
                            "strategy_id": strategy_id,
                            "computation_status": "failed",
                            # SI-02 (MEDIUM-2): clear the runner-owned warned marker.
                            "computation_warned": False,
                            # JOB-01: clear on exit so a stale stamp can never re-trigger the reaper.
                            "computing_started_at": None,
                            "computation_error": "Insufficient CSV history. At least 2 data points required.",
                            "data_quality_flags": {"csv_source": True},
                    }
                    _guard_failure_payload_inplace(_insufficient_payload)
                    supabase.table("strategy_analytics").upsert(
                        _insufficient_payload,
                        on_conflict="strategy_id",
                    ).execute()
```
The two new keys go INSIDE that literal, beside `"computation_error"`: `"computation_error_source": "writer", "computation_error_job_id": job_id,`.

**The JOB-01 constraint that forces the dict-literal shape** — `analytics_runner.py:1340-1349`, verbatim:
```python
        ⛔ IN-PLACE, AND THAT IS NOT A STYLE CHOICE. `tests/
        test_computing_started_at_stamp.py` is a JOB-01 census that walks the AST
        of every `strategy_analytics` write and can only read a payload that is a
        dict LITERAL (or a name bound to one); a payload produced by a helper
        CALL is recorded as unreadable and stepped over (its D-07 resolution
        policy). … Do not "clean this up" into a return value.
```
So the marker is NOT a helper (`_with_provenance(payload)` returning a dict) — it is two keys typed into each of the eleven literals.

**The job_worker twin** — `job_worker.py:2983-2993` (P2, `_upsert_error_only`, the exact site the migration header names as lost):
```python
                    def _upsert_error_only() -> None:
                        ctx.supabase.table("strategy_analytics").upsert(
                            {
                                "strategy_id": strategy_id,
                                # JOB-01 still applies: clear the reaper anchor on
                                # exit. It carries no publish meaning.
                                "computing_started_at": None,
                                "computation_error": scrubbed,
                            },
                            on_conflict="strategy_id",
                        ).execute()
```
`job` (the dict) is in scope in every job_worker handler (`run_derive_broker_dailies_job(job)` etc.); `job.get("id")` is already the idiom (`:2979`).

**Threading `job_id` into `run_csv_strategy_analytics`** — the precedent is `refresh_source` (`:1178-1192`):
```python
async def run_csv_strategy_analytics(
    strategy_id: str,
    *,
    refresh_source: str | None = None,
    refresh_publish_status: str | None = None,
    refresh_publish_warned: bool = False,
) -> dict[str, Any]:
    """…
    ``refresh_source`` (Phase 161.1 / CR-03) … It is KEYWORD-ONLY and
    defaults to None, so every other caller — the CSV-first wizard route, the
    composite finalizer, the tests — is byte-unchanged
```
Add `job_id: str | None = None` the same way; the one job caller is `job_worker.py:2188-2193` (`await run_csv_strategy_analytics(strategy_id, refresh_source=…, …)`) — pass `job_id=job.get("id")` there and nowhere else.

**The RPC that overwrites** (why stamping matters — `analytics-service/main_worker.py:951-960`):
```python
                    supabase.rpc(
                        "mark_compute_job_failed",
                        {
                            "p_job_id": jid,
                            "p_error": err or "Unknown error",
                            "p_error_kind": kind or "unknown",
                            "p_claim_token": tok,
                        },
                    ).execute()
```
→ `20260529180000_fix_mark_compute_job_failed_error_kind_column.sql:128-139`:
```sql
  UPDATE compute_jobs
     SET status = v_new_status,
         last_error = p_error,
         error_kind = p_error_kind,
         next_attempt_at = v_next_attempt
   WHERE id = p_job_id;
  -- Phase 18: atomic UI bridge (preserved from mig 099).
  IF v_strategy_id IS NOT NULL THEN
    PERFORM sync_strategy_analytics_status(v_strategy_id);
  END IF;
```

**⚠️ Census exemption hazard.** `test_computing_started_at_stamp.py:161-170` exempts ONE literal by EXACT key set:
```python
_STAMP_OMISSION_EXEMPT_KEYS: Final[frozenset[str]] = frozenset(
    {"strategy_id", _STATUS_KEY, _WARNED_KEY, "computation_error", "data_quality_flags"}
)
EXPECTED_STAMP_OMISSION_EXEMPT: Final[int] = 1
```
That literal is the schema-cache-miss RE-ISSUE writer (RESEARCH row P11, `analytics_runner.py:2110-2120` — not re-opened here). Adding the two marker keys to it changes its key set → the exemption stops matching → `test_python_status_writers_stamp_and_clear` RED. Decide deliberately: the re-issue arm exists precisely because a NEW column was unknown to PostgREST (`:150-154`), and this phase adds two new columns (RESEARCH Pitfall 8) — the honest shape is probably to leave P11 marker-free as the minimal-key fallback and say so in its comment; if the markers are added, extend `_STAMP_OMISSION_EXEMPT_KEYS` in the same commit. Either way the new census's hand-typed site count is 10 or 11, decided by this.

---

### 4. `analytics-service/tests/test_computation_error_provenance_census.py` (CREATE — AST census)

**Analog:** `analytics-service/tests/test_computing_started_at_stamp.py`. Reuse its scan, do not re-implement: `_scan_python(files)` / `_py_scan_files()` (`:527-529`), and the key helpers (`:448-470`):
```python
def _dict_value(node: ast.Dict, key: str) -> ast.expr | None: …
def _has_key(node: ast.Dict, key: str) -> bool:
    return any(isinstance(k, ast.Constant) and k.value == key for k in node.keys)
def _is_none(node: ast.expr | None) -> bool:
    return isinstance(node, ast.Constant) and node.value is None
def _payload_keys(node: ast.Dict) -> frozenset[str]: …
```
**Hand-typed counts as the vacuity floor** (`:709-715`) — copy this idiom with the new file's own numbers:
```python
    EXPECTED_STATUS_DICTS = 12
    EXPECTED_RUNNER_DICTS = 8
    EXPECTED_WORKER_DICTS = 4
    …
    assert len(scan.kept) == EXPECTED_STATUS_DICTS, (
        f"expected exactly {EXPECTED_STATUS_DICTS} python strategy_analytics "
```
New rule: every literal whose `computation_error` value is not `None` also has BOTH marker keys; every literal whose `computation_error` IS `None` (the success resets, `job_worker.py:7775`, `analytics_runner.py:1870` per RESEARCH) has both markers `None`. Its docstring should repeat `:29-38`'s caveat in its own terms: the Python census is not the whole invariant — the SQL half is the new gate (§2).

Run from `analytics-service/` only (`python3 -m pytest tests/<file> -x -q`).

---

### 5. Wizard roster — minting the preselect code and wiring `RATE_LIMITED` (MODIFY ×7)

**All five laws, each opened:**

| Law | `path:line` | Excerpt |
|---|---|---|
| L1 union | `src/lib/wizardErrors.ts:141` | `  \| "KEY_MISSING_REQUIRED_FIELD"` (also `:166` `KEY_RATE_LIMIT`, `:194` `DRAFT_ALREADY_EXISTS`) — add the new member beside `:141` with a dated comment like `:138-140`'s |
| L2 size ×2 | `src/lib/wizardErrors.test.ts:2021` and `:2496` | `  const EXPECTED_TABLE_SIZE = 90;` — both to 91 |
| L3 divergence | `src/lib/wizardErrors.test.ts:3844-3880` | regex `/const EXPECTED_TABLE_SIZE = (\d+);/g` over its own source; asserts `declarations.length === 2` and `new Set(declarations).size === 1` |
| L4 roster | `src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx:272-300` | see below |
| L5 derived scanner | `src/lib/wizardErrors.invariant.test.ts:240-256` | `statusRe: "400"`, `expectedSites: 13` |

**⚠️ NUL byte, measured:** `grep -n EXPECTED_TABLE_SIZE src/lib/wizardErrors.test.ts` → NO output, exit 1. `grep -an` → `2021`, `2029`, `2496`, `2502`, `3827`, `3844`, `3847`, `3853`, `3864`, `3872`. Edit with the `Edit` tool (byte-exact); verify with `sed -n '2021p;2496p'`.

**L2 excerpt** (`:2018-2030`):
```ts
   * four members. The baseline was re-measured at HEAD before it moved — 89 is
   * what 161-REVIEW left and nothing between it and this plan minted a member.
   */
  const EXPECTED_TABLE_SIZE = 90;

  it("the scan actually covers the table — hand-typed size guard", () => {
    expect(
      Object.keys(WIZARD_ERROR_COPY).length,
      "If the table grew, re-run this guard's reasoning over the new entries " + …
    ).toBe(EXPECTED_TABLE_SIZE);
```
Update the docblock's "90 is what … left" sentence at both sites — L3's message (`:3872-3878`) requires "re-run EACH docblock's reasoning".

**L4 — the roster row idiom** (`ConnectKeyStep.tsx:289-299`, the `VENUE_ALREADY_CONNECTED` row and its comment; copy the shape for the new preselect code AND for `RATE_LIMITED`):
```ts
    "DRAFT_ALREADY_EXISTS",
    // 154.1 / WIZCONT-02 review CR — the venue fence's REFUSAL half, admitted
    // HERE IN THE SAME COMMIT the route starts emitting it. Omit this line and
    // the code fails the membership check below, falls through to `UNKNOWN` —
    // whose copy IS recoverable — so the user gets "Try the last action again."
    // with a Retry control for a submit that is refused identically every time,
    …
    "VENUE_ALREADY_CONNECTED",
```
`KEY_MISSING_REQUIRED_FIELD` STAYS in this roster (`:282`) — the credential arm still emits it at `:719, :780, :828, :849, :856` (per RESEARCH; not re-opened).

**L5 — `expectedSites: 13` stays IF the new code replaces `KEY_MISSING_REQUIRED_FIELD` on the ONE reuse-arm 400** (`:247-255` documents that the reuse arm contributes "exactly ONE 400 emitter"). The emitter to change, `create-with-key/route.ts:476-481`:
```ts
  if (!isUuid(wizardSessionId) || !isUuid(reuseKeyId)) {
    return NextResponse.json(
      { code: "KEY_MISSING_REQUIRED_FIELD", error: "wizard_session_id and reuse_api_key_id must be uuids" },
      { status: 400, headers: NO_STORE_HEADERS },
```
`code` FIRST as a literal — the scanner's required shape (`:1375-1376`: "`code` FIRST, as a LITERAL — the shape `wizardErrors.invariant.test.ts`'s scanner requires").

**`RATE_LIMITED` is wiring, not authoring** — the honest member already exists (`wizardErrors.ts:3326-3335`):
```ts
  RATE_LIMITED: {
    title: "You have reached our request limit.",
    cause:
      "We cap how often this action can run and this attempt went over the cap. Nothing was submitted and nothing was changed — the cap is ours, not your exchange's.",
    fix: [
      "Wait, then run the same action again. We do not queue it or retry it for you.",
    ],
```
Four emitter sites, one shape: `create-with-key/route.ts:494` and `:900` (`throttledBody: { code: "KEY_RATE_LIMIT", error: "Too many requests" }` inside `rateLimitDenyJson(rl, { … })`, `ratelimit.ts:445`), `composite/add-key/route.ts:306` (same `rateLimitDenyJson` shape), `keys/validate-and-encrypt/route.ts:300` (same, keys in the other order), `composite/set-members/route.ts:99-101` (a bare `NextResponse.json({ code: "KEY_RATE_LIMIT", … })` — older shape). The `:891-897` comment ("our outage, blamed on their exchange") is the record to update.

429 is OUTSIDE the derived population — the hand-typed twin to extend is `wizardErrors.invariant.test.ts:1994-2012` (`deriveEmittedCodes(stripped(route.route), "409")` with a hand-typed count "never `codes.length`"), and the roster row is "OWED BY HAND" (`route.ts:1383-1387`). Rosters to widen: `ConnectKeyStep.tsx:272`, `MultiKeyConnectStep.tsx:280` (`KNOWN_ADD_KEY_CODES`), `wizardErrors.ts:5091` (`KNOWN_VALIDATE_AND_ENCRYPT_CODES`); which roster admits `set-members` was not determined this session.

**Pins that move:** `envelope.test.ts:80-90` pins `KEY_RATE_LIMIT`'s envelope verbatim (unchanged if the entry is untouched); `SubmitStep.test.tsx:867-905` pins `RATE_LIMITED`'s render (unchanged, and it is the assertion idiom for the new roster test — `:887-890`):
```ts
    await vi.waitFor(() => expect(findWizardError()).toBeDefined());
    expect(findWizardError()!.code).toBe("RATE_LIMITED");
    expect(findWizardError()!.code).not.toBe("UNKNOWN");
```
`create-with-key/route.test.ts` asserts `KEY_MISSING_REQUIRED_FIELD` at `:1242`, `:1269`, `:3957`, `:3971` and as fixtures at `:3173-3323` (grep hits, bodies not opened) — the executor opens each to find which belong to the reuse arm and flips those with the idiom in §7.

**`DRAFT_ALREADY_EXISTS` split** — the entry `wizardErrors.ts:1919-1922`:
```ts
  DRAFT_ALREADY_EXISTS: {
    title: "You already have a wizard session open for this key.",
    cause:
      "A draft strategy with the same API key is already in progress. Each key can back one listing at a time.",
```
`fixRequires` (`:1930-1935`) gates `fix[]` only, so the cause cannot be surface-split by that mechanism. The preselect 23505 arm is `route.ts:666-672`:
```ts
    if (error.code === "23505") {
      // The wizard-session fence. Byte-identical to the credential arm's
      // fallthrough: a draft for THIS session already exists.
      return NextResponse.json(
        { code: "DRAFT_ALREADY_EXISTS", error: "A wizard session with this key is already in progress." },
        { status: 409, headers: NO_STORE_HEADERS },
```
A second code (or a second entry) for the stale-session cause is another `EXPECTED_TABLE_SIZE` move (→ 92) and another 409 hand-roster row; the planner decides between a new member and a cause that is true on both arms.

---

### 6. `src/lib/wizardErrors.roster-render.test.tsx` (CREATE — criterion 3, `code: UNKNOWN` class)

Three analogs, one per part:

**Population from source** — `ConnectKeyStep.preselect-refusal-class.test.tsx:58-68`:
```ts
const ROUTE_PATH = join(REPO, "src/app/api/strategies/create-with-key/route.ts");
const STEP_PATH = join(REPO, "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx");
const ROUTE_SRC = readFileSync(ROUTE_PATH, "utf-8");
const STEP_SRC = readFileSync(STEP_PATH, "utf-8");
```
and `:181-187` builds `PRESELECT_REACHABLE: [WizardErrorCode, number][]` from `SERVER_REFUSALS` + `CLIENT_MINTED` + `["UNKNOWN", 500]`. The self-scan-by-regex idiom with a positive-control count is `wizardErrors.test.ts:3844-3866` (§5 L3).

**Render harness** — `:445-470`:
```ts
    const { container } = render(<PreselectHost />);
    fireEvent.click(screen.getByTestId("wizard-preselect-continue"));
    const envelope = await screen.findByTestId("error-envelope");
    return { envelope, container };
  }

  it.each(PRESELECT_REACHABLE)(
    "%s: names only painted controls, and any Retry it offers does something",
    async (code, status) => {
      const { envelope, container } = await refuseWith(code, status);
```
**Assertion** — `SubmitStep.test.tsx:887-890` (§5), `findWizardError()` defined at `:78`.

The neuter with a recorded precedent: delete one `SEAM_CODE_TO_WIZARD_CODE` row or one roster member → that code RED by name (RESEARCH Finding 6, `TODOS.md:895-900`; not re-opened).

---

### 7. Inverting a pin instead of deleting it — the repo idiom

**Best analog:** `src/app/api/portfolio-optimizer/route.test.ts:440-462`. The comment states the old pin, why it was the defect, and that the reversal is deliberate; the `it` title carries the phase tag; the assertion is the OLD one negated:
```ts
  // and 503 arms called `bridgeComputeLimiter.resetUsedTokens(key)` — red-team
  // R-0002's "symmetric refund", correct against the 5/60s bucket this route
  // spent when it was written. … They are
  // INVERTED rather than deleted because the old behaviour is the defect and a
  // reader needs to see that the reversal was deliberate.
  it("[163 WR-03] a 504 timeout does NOT reset the window — the token stays spent", async () => {
    …
    expect(
      STATE.refundCalls,
      "`resetUsedTokens` clears the caller's entire 10/3600s window. …",
    ).toHaveLength(0);
```
The one-line form for the file header is `:102-103`: "The assertions INVERTED in this phase — they used to pin that the 5xx arms refund; they now pin that the token stays spent."

Two more, same shape: `finalize-wizard/route.test.ts:3655-3663` ("THIS ASSERTION USED TO PIN THE DEFECT. It read `expect(body.code).toBeUndefined()` … A test written the old way is worse than no test — it makes the dead end a contract, so removing it would have read as a regression") and `create-with-key/route.test.ts:1695-1709` ("THIS CASE'S EXPECTATION WAS INVERTED BY PHASE 156 … It previously read … and asserted 200 + one RPC call").

**Apply to `KeyPermissionBadge.test.tsx:121-140`** — today:
```ts
  it("prepends the structured `code` field to the error message", async () => {
    global.fetch = vi.fn().mockResolvedValueOnce({
      ok: false, status: 502, statusText: "Bad Gateway",
      json: async () => ({
        error: "Could not reach the permissions service. Try again shortly.",
        code: "PROBE_BACKEND_UNAVAILABLE",
      }),
    } as Response) as unknown as typeof fetch;
    render(<KeyPermissionBadge apiKeyId="key-1" />);
    await waitFor(() =>
      expect(screen.getByText(/PROBE_BACKEND_UNAVAILABLE: Could not reach the permissions service/)).toBeInTheDocument(),
    );
```
→ keep the fixture, flip to: prose present (`getByText("Could not reach the permissions service. Try again shortly.")`), `document.body.textContent` does NOT contain `PROBE_BACKEND_UNAVAILABLE`, comment names the 2026-08-26 ruling. **And the second pin at `:344-368`** (`"PROBE_FAILED: Could not check key scopes. Try again."` at `:366`) flips the same way; open `:318-335` to decide `:336`.

**The log half** — copy `:372-397` (B-27), which already spies `console.error` and asserts an argument by identity:
```ts
      const consoleSpy = vi.spyOn(console, "error").mockImplementation(() => {});
      …
      // The raw value never reaches the DOM …
      expect(document.body.textContent).not.toContain("ECONNREFUSED");
      // … but it DOES reach the console, so debuggability was not traded away.
      expect(
        consoleSpy.mock.calls.find((call) => call.some((arg) => arg === raw)),
      ).toBeDefined();
```
For the code: `call.some((arg) => arg === "PROBE_BACKEND_UNAVAILABLE")`, plus `vi.mock("@/lib/sentry-capture", () => ({ captureToSentry: vi.fn(), addSentryBreadcrumb: vi.fn() }))` and `expect(vi.mocked(addSentryBreadcrumb)).toHaveBeenCalledWith(expect.objectContaining({ message: "PROBE_BACKEND_UNAVAILABLE" }))`.

**Apply to `SyncBadge.staler-of-two.test.tsx:265-280`** — today the control is:
```ts
  it("WR-06 GRACE CONTROL: beyond the tolerance it is still a future date", () => {
    const { container } = render(
      <SyncBadge
        computedAt={agoIso(2 * HOUR)}
        // An hour ahead is an order of magnitude past the tolerated drift.
        seriesEnd={new Date(Date.now() + HOUR).toISOString()}
      />,
    );
    expect(dotClass(container)).toContain("bg-text-muted");
    expect(container.textContent).toMatch(/Track record ends in the future/i);
```
Under a day allowance `+ HOUR` becomes `fresh` → RED. It is not deleted: the fixture moves past the new allowance (`Date.now() + 2 * DAY`), the assertions stay, the comment records the move with the `:440-447` idiom, and the `+HOUR` case is ADDED as the positive arm beside `:248` (+30 s) — the test that fails with the allowance removed. `:287` (112-day stale control) is untouched.

---

### 8. `src/lib/freshness.ts` + `FactsheetView.tsx` (MODIFY — WR-06-UTC)

**The stated convention** (`freshness.ts:23-30`) — exported, imported, never re-typed:
```ts
 * EXPORTED since 163-REVIEW / WR-06 because the COPY has to honour the same
 * tolerance as the COLOUR. `SyncBadge`'s `timeAgo` renders the sentence beside
 * the dot this module buckets; if it called anything ahead of "now" a future
 * date while `computeFreshness` was still calling the same instant `fresh`,
 * the badge would contradict itself on ordinary browser-clock drift …
 */
export const CLOCK_SKEW_TOLERANCE_MINUTES = 5;
```
Add `export const SERIES_END_FUTURE_ALLOWANCE_DAYS = 1;` beside it with the same docblock shape. `FactsheetView.tsx:9` already imports from this module: `import { SERIES_FRESH_DAYS, SERIES_STALE_DAYS } from "@/lib/freshness";` — extend that line.

**Bucketer 1** (`freshness.ts:207-218`, module-private, consumed by `resolveEffectiveRecency` at `:287`):
```ts
function bucketSeriesAge(seriesEndMs: number, now: Date): SeriesVerdict {
  const days = (now.getTime() - seriesEndMs) / (1000 * 60 * 60 * 24);
  if (!Number.isFinite(days)) return "stale";
  if (days < 0) {
    const minutesAhead = -days * 24 * 60;
    return minutesAhead <= CLOCK_SKEW_TOLERANCE_MINUTES ? "fresh" : "future";
  }
```
**Bucketer 2** (`FactsheetView.tsx:1108-1115`):
```ts
function bucketByAge(days: number): FreshnessTone {
  if (!Number.isFinite(days)) return "neutral";
  if (days < 0) return "future";
```
⛔ `bucketByAge` is called TWICE: `:1188` `const jobTone = bucketByAge(days);` (computedAt) and `:1192-1194` the series call. The allowance is applied at the SERIES call site only:
```ts
  const seriesAgeTone: FreshnessTone = seriesEnd
    ? bucketByAge((nowMs - new Date(seriesEnd.iso).getTime()) / 86_400_000)
    : "unknown";
```
The wrong-scope tripwire is chip-honesty C-10 (`:340-349`): `computedAt = Date.now() + 400 * DAY_MS` → `"Computed · future — check data"`. It must stay green.

**Joint two-surface test** — the closest existing is `SyncBadge.staler-of-two.test.tsx:220-235` ("the badge agrees with the factsheet chip"), which renders ONLY the badge and asserts agreement by prose. The new test renders both from one row: badge via `render(<SyncBadge computedAt seriesEnd />)` (`:63-65`) and chip via the chip-honesty harness (`FactsheetView.chip-honesty.test.tsx:1-7` imports `FactsheetBody`, `FactsheetProvider`, `buildScenarioFactsheetPayload`; `renderFactsheet(payloadWith(…))` at `:264`) — and asserts the same verdict word on both.

---

### 9. HONEST-08-RESIDUAL — the `FreshnessChip` twin (MODIFY chip-honesty test)

**The existing badge-side proof** (`SyncBadge.staler-of-two.test.tsx:62-71`):
```ts
  it("Test 2: when the SYNC is the staler fact, the sync-keyed form is unchanged", () => {
    // computedAt 3d ago (stale on the 12h/48h ladder), series 1d ago.
    const { container } = render(
      <SyncBadge computedAt={agoIso(3 * DAY)} seriesEnd={agoIso(1 * DAY)} />,
    );
    expect(container.textContent).toMatch(/Synced 3d ago/);
    expect(container.textContent).not.toMatch(/Track record/i);
    expect(dotClass(container)).toContain("bg-negative");
```
**The chip-side fixture shape** (C-4, `:261-269`):
```ts
  it("C-4: a live series does NOT rescue a stale job — the blend takes the WORSE of the two", () => {
    const { container } = renderFactsheet(payloadWith(1, isoHoursAgo(24 * 30)));
    const chip = readChip(container);
    expect(chip.label).toBe("Computed · old");
    expect(chip.tone).toBe(NEGATIVE);
```
The twin asserts the SUBJECT, which C-4 already does implicitly via the label prefix — `FactsheetView.tsx:1206-1209`:
```ts
  const seriesIsBinding =
    jobTone !== "neutral" && TONE_RANK[seriesTone] > TONE_RANK[jobTone];
  const tone: FreshnessTone = seriesIsBinding ? seriesTone : jobTone;
  const subject = seriesIsBinding ? "Track record" : "Computed";
```
Neuter: `>` → `>=` at `:1207` → subject flips to "Track record" on a tie/fresh series → RED. The new `it` should say in its title that it is the chip twin of `staler-of-two:62`.

---

### 10. `src/lib/sentry-capture.ts` — `addSentryBreadcrumb` sibling (MODIFY)

Measured: `grep -rn addBreadcrumb src | wc -l` → **0**. The skeleton to copy is `captureToSentry` (`:191-230`):
```ts
export function captureToSentry(
  err: unknown,
  options: { tags: Record<string, string>; extra?: Record<string, unknown>; level?: "fatal" | "error" | "warning" | "info"; secrets?: readonly unknown[]; },
): Promise<void> {
  try {
    const secrets = options.secrets ?? [];
    const payload = scrubCaptureInput(err, secrets);
    const tags = scrubRecord(options.tags, secrets) ?? {};
    const extra = scrubRecord(options.extra, secrets);
    return import("@sentry/nextjs")
      .then((Sentry) => {
        try {
          Sentry.captureException(payload, { tags, extra, level: options.level ?? "error" });
        } catch {
          // Swallow — caller already logged via console.error / warn.
        }
      })
      .catch(() => {
        // Sentry import failed — swallow. RESOLVING rather than rejecting is
        // load-bearing now that the chain is returned …
      });
  } catch {
    return Promise.resolve();
  }
}
```
Extending it = a second export with the identical try / dynamic-import / inner-try / `.catch` / outer-catch shape, calling `Sentry.addBreadcrumb({ category, message, data: scrubRecord(data, []), level })` and returning the settled promise. The client-safety constraint (`:180-186`): this module "MUST NOT import `next/server` … It imports exactly one module (`./seam-redaction`) and must keep that property." `KeyPermissionBadge.tsx` is `"use client"` (`:1`) and imports only `react`, `@/lib/utils`, `@/lib/route-response-error` (`:3-5`); adding `@/lib/sentry-capture` is already done by other client components (`src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx` — path verified, line not opened).

Test mock idiom (`page.stale-analytics.test.tsx:38` and three siblings): `vi.mock("@/lib/sentry-capture", () => ({ captureToSentry: vi.fn() }));` — add the new export to the factory in the badge test.

**The component site** (`KeyPermissionBadge.tsx:135-141`):
```ts
        const message = err.error ?? `HTTP ${res.status}`;
        // Prepend the route's structured `code` (e.g. PROBE_BACKEND_UNAVAILABLE)
        // so the displayed text is greppable in support tickets.
        throw new RouteResponseError(
          err.code ? `${err.code}: ${message}` : message,
        );
```
`:149` `console.error("[KeyPermissionBadge] probe failed:", e);` already logs the Error object; `:158-160` renders `e.message` when `e instanceof RouteResponseError`. The split: log `err.code` as its own `console.error` argument (so the B-27-style identity assertion works) + breadcrumb, then `throw new RouteResponseError(message)`.

---

### 11. `src/app/factsheet/[id]/v2/page.tsx:414-421` + `page.stale-analytics.test.tsx` (MODIFY — criterion 9)

**The sentence** (`page.tsx:414-421`, RSC, inside the `buildFactsheetPayload returned null` arm; `lane` declared at `:185`, only `OwnerUnpublishedPanel` is lane-gated at `:399-404`):
```tsx
        <p className="mt-3 text-fixed-12 text-text-muted italic">
          If this persists for more than a few minutes, the strategy may
          have insufficient observations inside the bundled benchmark
          window (2023-04-26 onward). See the dev-server console for the
          exact gate the request fell through.
        </p>
```
**The anonymous render test, already there** (`page.stale-analytics.test.tsx:202-212`):
```ts
  it("V2: it falls to the EXISTING still-computing placeholder, not a new error state", async () => {
    const ui = await renderPage("failed");
    const text = collectText(ui).join(" ");

    expect(text).toContain("still computing");
    // The strategy is not deleted and is not shouted at: it keeps its name and
    // the page carries no error/failure vocabulary.
    expect(text).toContain("Orpheus");
    expect(text.toLowerCase()).not.toContain("failed");
    expect(text.toLowerCase()).not.toContain("error");
```
Why it is the ANONYMOUS lane: `renderPage` (`:180-183`) calls `FactsheetV2Page({ params })` directly; the request-client mock (`:128-145`) has no `auth` member, so any `supabase.auth.getUser()` would throw — the page never reaches it on this path. `collectText` (`:164-178`) flattens the RSC tree. Add a V2b: `expect(text).not.toContain("dev-server console")`, `not.toContain("bundled benchmark window")`, and a positive `toContain(<replacement>)`; the neuter is restoring the sentence.

## Shared Patterns

### Neuter → RED → restore, byte-verified
**Source:** RESEARCH Pattern 5 (the repo's shape); the migration analog records the rule at `20260826120000:237-240` for SQL anchors. Applies to every test in §2, §4, §6, §7, §8, §9, §11. `shasum -a 256` before/after; never `git checkout --`.

### Hand-typed counts as vacuity floors
**Sources:** `wizardErrors.test.ts:3856-3866` (positive control on a regex match count), `test_computing_started_at_stamp.py:709-715`, `wizardErrors.invariant.test.ts:2002-2012` ("HAND-TYPED, measured … never `codes.length`"). Apply to: the new census (§4), the roster-render population (§6), the new gate's `ALL N ARMS EXECUTED` roster (§2).

### `code` first, as a literal, in every `NextResponse.json`
**Source:** `create-with-key/route.ts:1375-1381`. Apply to: every emitter edit in §5 (the invariant scanner derives its population from that shape).

### Applied-ness gates RAISE, never SKIP, and key on something INDEPENDENT of the thing under test
**Source:** `test_sync_status_marked_refresh_protected.sql:206-231`. Apply to: the new gate (§2) — key on `pg_attribute` for the new column (`20260802120000:544-556` shape), not on the bridge body.

### Mutation-runner twin hygiene
**Sources:** `GRAMMAR.md:34-47`, `parse.mjs:159`, `run.mjs:40`, parity gate `:252-263`. Apply to: every `RED-UNDER-M` in §2; the floors at `run.mjs:789/1578` move in the same commit.

## No Analog Found

| File | Role | Data Flow | Reason |
|---|---|---|---|
| `src/lib/database.types.ts` regeneration | generated types | — | No wrapper script in `package.json`/`scripts/` (RESEARCH Open Q5, not re-verified). CLI is linked to PROD — `supabase gen types typescript --linked` is a READ, run only after the migration has applied on merge, or generate from the pg-lane `--db-url`. |
| Which roster admits `composite/set-members`'s 429 | component config | — | Not determined this session; the executor greps `set-members` in `src/app/(dashboard)/strategies/new/wizard/steps/*.tsx` before adding `RATE_LIMITED` there. |

## Metadata

**Analog search scope:** `supabase/migrations/`, `supabase/tests/`, `supabase/schema/functions/`, `scripts/mutation-runner/`, `scripts/pg-lane/`, `scripts/lint-sql-gates.mjs`, `scripts/dump-sql-functions.ts`, `analytics-service/services/`, `analytics-service/tests/`, `analytics-service/main_worker.py`, `src/lib/`, `src/components/connect/`, `src/components/strategy/`, `src/app/api/strategies/**`, `src/app/api/keys/**`, `src/app/(dashboard)/strategies/new/wizard/steps/`, `src/app/factsheet/[id]/v2/`, `src/app/api/portfolio-optimizer/`.
**Files opened:** 38 (targeted `sed -n` ranges; no file re-read on an overlapping range).
**Tracked-source gate:** every analog path above returned non-empty from `git ls-files`; none is a `.gsd/capabilities/` mirror.
**Pattern extraction date:** 2026-09-06
