---
phase: 164.2-curated-copy-the-curated-computation-error-sentence-must-act
plan: 09
type: execute
subsystem: wizard-error-surfaces
tags: [WIZFORM-02, roster-render, code-UNKNOWN, anti-vacuity]
wave: 3
depends_on: ["164.2-04", "164.2-05"]
files_modified:
  - src/lib/wizardErrors.roster-render.test.tsx
autonomous: true
requirements: [WIZFORM-02]
provides:
  - "A table-driven RENDER test over the UNION of every server-classified code the wizard can surface, asserting the rendered surface shows THAT code and never UNKNOWN, with a hand-typed population floor and a recorded RED under neuter"
affects: [164.2-10]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "For EVERY code in the population — (a) every key of SEAM_CODE_TO_WIZARD_CODE, (b) every member of the four KNOWN_* rosters, (c) every `code:` literal the invariant ROUTES scanner derives, (d) every WizardErrorCode union member except UNKNOWN itself — a server body `{ code, error }` fed through the step's real fetch path renders an envelope whose code equals the expected wizard code (the seam-mapped code for (a)) and is never UNKNOWN"
    - "The population is derived from live source at test time (so a code minted tomorrow enters automatically) AND floored by a hand-typed minimum that reds when the derivation goes blind"
    - "The PROD-measured instance — a bare 401 from validate-and-encrypt — is a named row: the route (plan 05) now answers SEAM_MISCONFIGURED and the surface renders it"
    - "Neutered by deleting one SEAM_CODE_TO_WIZARD_CODE row (recorded precedent: TODOS.md:895-900) and by deleting one roster member, the test is RED naming that exact code"
  artifacts:
    - path: "src/lib/wizardErrors.roster-render.test.tsx"
      provides: "population builder (source scan, the wizardErrors.test.ts:3844-3866 / invariant :1444-1488 idiom), refuseWith-style harness per surface, it.each over (surface, code, status)"
  key_links:
    - from: "src/lib/wizardErrors.roster-render.test.tsx"
      to: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.preselect-refusal-class.test.tsx"
      via: "the refuseWith(code, status) harness shape (:445-470) and the source-derived population shape (:58-68, :181-187)"
      pattern: "refuseWith"
---

<objective>
Criterion 3 — WIZFORM-02 closed by MEASUREMENT: "for every server-classified error code the wizard can surface, a test asserts the rendered surface shows THAT code and never `code: UNKNOWN`. The test must be shown to fail when the classification is neutered." Phase 153's span verification FAILED on 2026-08-13 because inspection was accepted (`153-VERIFICATION.md:19-47`, `left the CLASS open`); the `GATED ON WIZFORM-02 CLOSING` marker in TODOS comes down (plan 10) only when this test exists and has been observed red.

RESEARCH Finding 6: three derived populations exist and none is a render test; the union of four sources is the honest population. The client terminal is `formatKeyError` (`wizardErrors.ts:3900-3910`, `return WIZARD_ERROR_COPY.UNKNOWN`) / `recogniseSeamErrorCode` (lines 4899-4904); the server minters are 40 `code: "UNKNOWN"` sites, of which the seam-level `?? "UNKNOWN"` for a bare upstream status was the live PROD instance — closed at the route by plan 05, rendered here.

Purpose: a roster mechanism that "demonstrably works" on two surfaces is not proof for the class; this test is.
Output: one test file, observed RED two ways.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-CONTEXT.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-RESEARCH.md
Read when it exists (written by the plan this one depends on): .planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-04-SUMMARY.md
Read when it exists (written by the plan this one depends on): .planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-05-SUMMARY.md

Read first (PATTERNS §6): `ConnectKeyStep.preselect-refusal-class.test.tsx:1-70` (`readFileSync(ROUTE_PATH`; setup, mocks, source reads), lines 181-187 (population), lines 420-470 (`PreselectHost`, `refuseWith`); `SubmitStep.test.tsx:60-90` (`findWizardError`) and lines 867-905 (the assertion idiom: `.code` equals, `.not.toBe("UNKNOWN")`); `src/lib/wizardErrors.invariant.test.ts:240-262` (`const ROUTES: readonly RouteUnderTest[]`; ROUTES rows, `deriveEmittedCodes`, `deriveRoster`, `deriveUnionMembers` — import these helpers if exported, else copy with attribution), lines 1444-1488 (source scan of SEAM_CODE_TO_WIZARD_CODE), lines 1515-1564 (parser self-tests); `wizardErrors.ts:4864-4898` (the map), lines 3900-3910; `MultiKeyConnectStep.tsx:280-300`, lines 352-357; `CsvSubmitStep.tsx:190` (`KNOWN_CSV_FINALIZE_CODES`); the four route files' fetch paths (which step POSTs to which route — the population row must be rendered on the step that actually consumes that route's response, or the test proves nothing).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Population from source, harness per surface, it.each render, floors</name>
  <files>src/lib/wizardErrors.roster-render.test.tsx</files>
  <behavior>
    - `POPULATION: ReadonlyArray<{ surface: "connect-key" | "multi-key-add" | "multi-key-set-members" | "validate-and-encrypt" | "csv-submit"; wireCode: string; expectedCode: WizardErrorCode; status: number }>` built at module load from: (a) `SEAM_CODE_TO_WIZARD_CODE` rows (wire = seam key, expected = mapped wizard code, status 502 unless the seam code implies another); (b) each `KNOWN_*` roster's members on its own surface (wire = expected = member, status from the route's emitter for that code where the scanner knows it, else 400/409/429 by family); (c) each ROUTES-derived `code:` literal on its route's surface; (d) every union member not already covered, on the connect-key surface. De-duplicate by (surface, wireCode). `UNKNOWN` itself is EXCLUDED from the population and its exclusion is asserted by a dedicated test ("UNKNOWN is the terminal, not a member").
    - Hand-typed floors: `EXPECTED_POPULATION_MIN = <count at authoring, typed after measuring>` and per-source non-zero controls; the derivation going blind (regex mismatch) must RED here, not pass on an empty scan.
    - A named row for the PROD reproduction: surface validate-and-encrypt, wire `SEAM_MISCONFIGURED` at 401 (what plan 05 makes the route answer for a bare upstream 401), expected `SEAM_MISCONFIGURED` — with the 2026-08-25 measurement in the row's comment.
    - `it.each(POPULATION)("%s: %s renders as %s, never UNKNOWN")`: mock `fetch` to answer `{ code: wireCode, error: "x" }` with `status`, render the surface's host and trigger its submit the way each surface's existing test does (the preselect harness for connect-key; the MultiKey and CsvSubmit suites for theirs), `await vi.waitFor(() => expect(findWizardError()).toBeDefined())`, then `expect(code).toBe(expectedCode)` and `.not.toBe("UNKNOWN")`.
  </behavior>
  <action>
    Build per `<behavior>`. Keep one host component per surface, copied from the surface's own test rather than invented; if a surface has no existing render harness, say so in a `describe.skip` with the reason and DO NOT count that surface's rows toward the floor — an honest gap beats a fake pass (the 153 lesson). Each surface's roster gate is different code, so a `findWizardError`-style reader per surface is expected. Run under the jsdom project (`npx vitest run src/lib/wizardErrors.roster-render.test.tsx`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.roster-render.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Every population row green; floors asserted; the PROD 401 row green; any skipped surface named with its reason in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Neuter the classification two ways; observe RED by name; byte-verified restore</name>
  <files>src/lib/wizardErrors.roster-render.test.tsx</files>
  <action>
    Neuters on OTHER files (fresh `cp` backup each; `shasum -a 256` before/after; restore by `cp`; never `git checkout --`) — this task modifies no file permanently, the `<files>` entry is the test whose output is being observed:
    1. Delete the `["SEAM_MISCONFIGURED", "SEAM_MISCONFIGURED"]` row from `SEAM_CODE_TO_WIZARD_CODE` in `src/lib/wizardErrors.ts`. Expected RED: the SEAM_MISCONFIGURED rows on every surface (including the PROD 401 row), named. Record.
    2. Delete `"VENUE_ALREADY_CONNECTED"` from `KNOWN_CREATE_WITH_KEY_CODES` (`ConnectKeyStep.tsx`). Expected RED: that one row on connect-key, named — the recorded precedent (TODOS.md:900-915: deleting `SEAM_INTERNAL_FAULT` from a roster once left the suite green until W-153.7-1 landed). Record.
    Restore both; hashes equal; GREEN. Record under `## Anti-vacuity observations` in the SUMMARY, quoting the FAIL lines with the code names. This SUMMARY section is what plan 10 cites when it brings the `GATED ON WIZFORM-02 CLOSING` marker down.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.roster-render.test.tsx && shasum -a 256 src/lib/wizardErrors.ts "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"</automated>
  </verify>
  <done>Two RED observations naming the neutered code; hashes restored; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server body -> wizard envelope | classification of a server-supplied code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.2-19 | Repudiation | population derivation | medium | mitigate | hand-typed floors and per-source non-zero controls; an empty derivation reds |
| T-164.2-SC | Tampering | npm installs | low | accept | none |
</threat_model>

<verification>
- Test file green with floors; two RED observations recorded.
</verification>

<success_criteria>
Criterion 3: WIZFORM-02 closed by a render test over the whole population, shown to fail when the classification is neutered.
</success_criteria>

<output>
Create `.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-09-SUMMARY.md` when done.
</output>
