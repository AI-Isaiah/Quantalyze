---
phase: 164.2-curated-copy-the-curated-computation-error-sentence-must-act
plan: 10
type: execute
subsystem: ledgers
tags: [TODOS, ledger, closeout, full-suite]
wave: 4
depends_on: ["164.2-01", "164.2-02", "164.2-03", "164.2-04", "164.2-05", "164.2-06", "164.2-07", "164.2-08", "164.2-09"]
files_modified:
  - TODOS.md
  - CLAUDE.md
autonomous: true
requirements: [WIZFORM-02, WR-06-UTC, HONEST-08-RESIDUAL, 161-ERRPREFIX]
provides:
  - "The four TODOS entries closed with their proof cited; the GATED ON WIZFORM-02 CLOSING marker brought down; two new TODOS entries booked (baseline.sql column for 164.5; post-merge type-regen check)"
  - "Full-suite gate run serially: vitest, then pytest, then mypy, then the mutation runner"
affects: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "TODOS.md closes WIZFORM-02, WR-06-UTC, HONEST-08-RESIDUAL and 161-ERRPREFIX ONLY by citing the plan SUMMARY section that holds the RED observation for each — never by restating the code"
    - "The `GATED ON WIZFORM-02 CLOSING` marker (TODOS.md:5040-5055, `Do not pick this up before that gate opens`) is replaced by a dated `GATE OPEN` note citing plan 09's `## Anti-vacuity observations` — ONLY IF plan 09's SUMMARY has no `## Surfaces not rendered` section and its test file has zero `describe.skip`/`it.skip` (W4); otherwise WIZFORM-02 stays OPEN, the marker stays, and the SUMMARY says which surface is unproven"
    - "The full suites are green serially on this box (never concurrently)"
  artifacts:
    - path: "TODOS.md"
      provides: "four closes, one marker flip, two new entries"
  key_links:
    - from: "TODOS.md"
      to: ".planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-09-SUMMARY.md"
      via: "the WIZFORM-02 close cites the section by name"
      pattern: "Anti-vacuity observations"
---

<objective>
Close the ledgers once, in one place (memory: TODOS.md is the ONE backlog ground truth; add/close items only there), after every proof exists — which is why this plan waits for all nine. Every other plan deliberately left TODOS.md alone so that no two same-wave plans touch it.

Purpose: the phase's claims are recorded where the next reader looks, each pointing at its measurement.
Output: TODOS.md edits, a dated CLAUDE.md currency line for the mutation floors, and a serial full-suite run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-CONTEXT.md

Read first: every `164.2-0N-SUMMARY.md` in the phase dir (their `## Anti-vacuity observations`, `## Twins observed biting`, `## HONEST-08 live observation`, `## Three-reviewer gate`, `## Deploy notes` sections); `TODOS.md:92-100` (WIZFORM-02 §D), lines 2365-2372 (WR-06-UTC, 161-ERRPREFIX), lines 4426-4432 (HONEST-08-RESIDUAL), lines 4996-5011 (the GATED marker); CLAUDE.md's "SQL gate integrity jobs" section (its closing paragraph is the dated currency log the floors line joins).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close four entries, flip the marker, book two</name>
  <files>TODOS.md, CLAUDE.md</files>
  <action>
    In TODOS.md, for each of WIZFORM-02, WR-06-UTC, HONEST-08-RESIDUAL, 161-ERRPREFIX: mark closed (the file's own close idiom — read a neighbouring closed entry and match it), dated 2026-09-06, Phase 164.2 plan NN, and cite the SUMMARY section holding the RED observation (WIZFORM-02 -> plan 09; WR-06-UTC -> plan 02, both bucketer neuters and the joint test; HONEST-08-RESIDUAL -> plan 02, the chip twin RED plus the live observation or its honest absence; 161-ERRPREFIX -> plan 01, three halves). ⛔ W4 PRECONDITION for the WIZFORM-02 close and the marker flip — check BOTH before editing: (1) `164.2-09-SUMMARY.md` contains NO `## Surfaces not rendered` section; (2) `grep -c 'describe.skip\|it.skip' src/lib/wizardErrors.roster-render.test.tsx` prints `0`. If either fails, do NOT close WIZFORM-02 and do NOT touch the GATED marker: close the other three entries, record under `## WIZFORM-02 left open` which surface is unproven and why, and let the `<verify>` below fail on the marker count — that is the honest outcome, not a defect in this plan. Otherwise: replace the `GATED ON WIZFORM-02 CLOSING` text at lines 4996-5011 with `GATE OPEN 2026-09-06 (Phase 164.2 plan 09 — roster-render test asserts the rendered DOM over all five surfaces and was observed RED under neuter; see its SUMMARY)` and leave the `CSV_FINALIZE_MOVED` option itself open for pickup. Book two new entries: (1) `[164.5-BASELINE-PROVENANCE-COLS]` — `supabase/schema/baseline.sql` does not carry `computation_error_source` / `computation_error_job_id`; Phase 164.5 gates the baseline hash and must regenerate it (cite the migration file). (2) `[164.2-TYPES-REGEN-CHECK]` — after the migration PR merges and auto-applies, run `supabase gen types typescript --linked` (READ only; CLI is linked to PROD; run the which-database check first) and confirm the `strategy_analytics` lines hand-extended in plan 06 produce no diff; also confirm on PROD that no failure upsert was lost in the schema-cache window (read `net._http_response` / worker logs for `PGRST204` around the deploy). Also record the `RATE_LIMITED` class fix and the `DRAFT_SESSION_COLLISION` mint under the WIZFORM/§D area as context for Phase 164.2.1.

    CLAUDE.md: append ONE dated paragraph to the end of the "SQL gate integrity jobs" section: Phase 164.2 plan 07 added `test_sync_status_curated_sentence_survives.sql`; floors moved to the values read off the run (quote the SUMMARY's `coverage:` / `arms:` lines, and repeat the standing rule that `run.mjs` is authoritative). Nothing else in CLAUDE.md changes.
  </action>
  <verify>
    <automated>test "$(grep -c 'GATED ON WIZFORM-02 CLOSING' TODOS.md)" = "0" && grep -c 'GATE OPEN 2026-09-06' TODOS.md | grep -qx 1 && grep -q '164.5-BASELINE-PROVENANCE-COLS' TODOS.md && grep -q '164.2-TYPES-REGEN-CHECK' TODOS.md</automated>
    <fails_when>the first `test` fails because `grep -c` prints a non-zero count — `GATED ON WIZFORM-02 CLOSING` still appears somewhere in `TODOS.md` (the marker text was not replaced, or the phrase is quoted in another entry: this is a whole-file count, so a historical mention elsewhere trips it too); the `GATE OPEN 2026-09-06` line count is not exactly `1` (`grep -qx 1` exits 1 — marker absent, or pasted twice); or `164.5-BASELINE-PROVENANCE-COLS` / `164.2-TYPES-REGEN-CHECK` is absent (`grep -q` exits 1 — an entry not booked, or its id mistyped). Not checked by this command: that the four closes cite their SUMMARY sections, and that CLAUDE.md gained exactly one dated paragraph</fails_when>
  </verify>
  <done>Marker flipped exactly once; two new entries present; four closes cite SUMMARY sections; CLAUDE.md carries one new dated paragraph.</done>
</task>

<task type="auto">
  <name>Task 2: Serial full-suite gate</name>
  <files>TODOS.md</files>
  <action>
    Run, one after another, never concurrently (memory: contention fakes regressions): `npm test` (the full vitest suite — contract tests scan all of `src/`, so file-scoped runs in earlier plans cannot have cleared them); then `cd analytics-service && python3 -m pytest -q`; then `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` from `analytics-service/`; then `npm run schema:functions:check`; then `node scripts/mutation-runner/run.mjs` (exit 0, `No defects`). Any red is a regression to fix at root, not to note. Record each command's tail and exit code in the SUMMARY. If a fix is needed in a file another plan owned, make it, re-run that plan's verify command, and say so.
  </action>
  <verify>
    <automated>npm run schema:functions:check && npx tsc --noEmit</automated>
    <fails_when>`schema:functions:check` exits 1 with `stale:` / `missing:` / `orphaned:` lines (a migration was touched after plan 06's regeneration without re-running `npm run schema:functions`); or `tsc` exits non-zero (any type error left by the phase's TypeScript plans). This command covers TWO of the five serial gates the action names: `npm test`, the full pytest, `mypy` and the mutation corpus have NO exit-status signal here — their tails and exit codes are recorded in the SUMMARY by hand, and a red in any of them is invisible to this verify command</fails_when>
  </verify>
  <done>All five gates green serially, recorded with exit codes; the phase is ready for `/gsd-verify-work 164.2`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | ledger edits only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.2-20 | Repudiation | TODOS closes | low | mitigate | each close cites the SUMMARY section with the RED observation |
| T-164.2-SC | Tampering | package installs | low | accept | none |
</threat_model>

<verification>
- Task 1 and Task 2 verify commands green; SUMMARY records the five serial gate results.
</verification>

<success_criteria>
The four TODOS entries are closed by measurement, the WIZFORM-02 gate is open, follow-ups are booked, and the whole tree is green serially.
</success_criteria>

<output>
Create `.planning/phases/164.2-curated-copy-the-curated-computation-error-sentence-must-act/164.2-10-SUMMARY.md` when done.
</output>
