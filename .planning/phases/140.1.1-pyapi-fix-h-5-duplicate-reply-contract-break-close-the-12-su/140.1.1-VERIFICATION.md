---
phase: 140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su
verified: 2026-07-26T13:16:37Z
status: gaps_found
score: 6/6 must-haves verified
status_correction:
  by: "Phase 140.1.2 plan 04 (PYAPIFIX2-06, artifact truth)"
  on: "2026-07-26"
  note: >-
    Flipped from `passed` to `gaps_found`. The six must-haves were and remain verified;
    what was not true is two of this document's own EVIDENCE claims — the blanket
    "no already-correct test was weakened or removed" sentence (a whole Python test
    FUNCTION was deleted and is absent from the Direction-2 table) and `deferred[0]`'s
    `grep -c` carve-out evidence (it proves a marker EXISTS, not that the enumerated
    list is COMPLETE — it was not). Both are corrected in place below. No other verdict
    in this report is re-litigated.
overrides_applied: 0
re_verification:
  previous_status: null
  note: "Initial verification — no prior VERIFICATION.md in the phase directory."
warnings:
  - id: W-1
    subject: "Full-suite `npx vitest run` is not deterministically green on this machine"
    detail: >-
      My own full run produced `Test Files 2 failed | 695 passed | 19 skipped (716)` /
      `Tests 2 failed | 8876 passed | 287 skipped (9165)`, exit 1 — NOT the
      `8878 passed / 0 failed` recorded in VALIDATION.md and both SUMMARYs.
      The two failures are `src/app/(dashboard)/allocations/AllocationsTabs.scenario-state-preservation.test.tsx`
      and `src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx`.
      NEITHER file is touched by this phase (`git diff --name-only 56fb7167..HEAD` does not
      list them), and both pass in isolation (`npx vitest run --no-file-parallelism <both>`
      → 2 files / 28 tests passed, 2.32s). This is the project's documented
      file-parallelism flake class, not a regression from this phase. Recorded because
      the ledger claim is stronger than what this environment reproduces.
    blocking: false
  - id: W-2
    subject: "REQUIREMENTS.md ledger lags the code"
    detail: >-
      `.planning/REQUIREMENTS.md:90` (**PYAPIFIX-03**) and `:92` (**PYAPIFIX-05**) are
      still `- [ ]` unchecked, and the traceability row at `:265` reads
      `| PYAPIFIX-01..06 | Phase 140.1.1 | Pending |`. All six requirements are
      implemented and verified in the codebase (see the Requirements Coverage table).
      Bookkeeping only — no code impact.
    blocking: false
  - id: W-3
    subject: "Two enumerations of the permanent-code concept still coexist"
    detail: >-
      `services/ingestion/long_fetch.py`'s local `permanent_codes` still omits
      `MISSING_SCOPE` and was deliberately NOT re-pointed at the canonical
      `PERMANENT_VALIDATION_ERROR_CODES`. The divergence is recorded IN CODE
      (`services/exchange.py`, "DELIBERATE DIVERGENCE (Phase 140.1.1, recorded not
      overlooked)") with the reason (re-pointing would silently flip the worker retry
      path transient→permanent, a behaviour change named in no requirement).
      Intentional and named, not an oversight — but it is a live follow-up.
    blocking: false
deferred:
  - truth: "routers/exchange.py:145, :152, :505 (the CLASSIFIED venue-failure path) still answer 400, not 424"
    addressed_in: "Phase 140.2 / 140.3 (TS-32, BLOCKED-BY: TS-05)"
    evidence: >-
      ROADMAP criterion 2 is scoped "at every site a Python-side change can correct
      without depending on an unlanded TypeScript obligation, with the remainder
      (routers/exchange.py:145, :152, :505) enumerated and blocker-named
      (BLOCKED-BY: TS-05)". `grep -c "BLOCKED-BY: TS-05" .planning/REQUIREMENTS.md` → 1,
      and 140.1-TS-OBLIGATIONS.md row TS-32 carries the full pairing and ordering rule.
      ⚠️ CORRECTED by Phase 140.1.2 plan 04 (PYAPIFIX2-06): that `grep -c` proves the
      MARKER EXISTS. It does not prove the enumerated list `exchange.py:145, :152, :505`
      is COMPLETE, and the 140.1.2 pattern-map showed it was not — three further arms in
      `_validate_mt5_key` (`exchange.py:335`, `:345`, `:361` at 39688d69) satisfy the
      same predicate and were outside the carve-out, and the third coordinate `:505` had
      itself drifted to `:523`. The completeness gap is what PYAPIFIX2-01 existed to
      close; it was closed by 140.1.2 plan 03 (7/7 sites), and TS-32 has been corrected
      in place and annotated PARTIALLY SATISFIED. The `400 → 424` remap this row defers
      is still owed and still `BLOCKED-BY: TS-05`.
  - truth: "The two in-handler HTTPException(429) sites (simulator.py:249, match.py:1742) are not yet migrated onto the RATE_LIMITED envelope"
    addressed_in: "Phase 140.2 / 146 (TS-23)"
    evidence: >-
      CONTEXT.md `<deferred>` explicitly scopes these OUT of 140.1.1. TS-23 is now
      annotated "UNBLOCKED by 140.1.1 PYAPIFIX-06(a)" with owner unchanged — this phase
      supplied the constructable arm, not the migration.
  - truth: "onboard request-level double-submit protection (wizard_session_id in the payload)"
    addressed_in: "Phase 140.2 / 140.3 (TS-33)"
    evidence: >-
      CONTEXT.md required this phase to ABSORB THE DECISION, not implement it
      ("M-11 DECIDED, not implemented"). TS-33 records the decision, the one-field
      remedy, the strict ordering constraint (after TS-01, now satisfied) and two
      rejected alternatives.
---

# Phase 140.1.1: PYAPI-FIX — Verification Report

**Phase Goal:** Phase 140.1's own review findings are closed — nothing it shipped breaks a live consumer, and every test the review's mutations proved toothless now bites
**Verified:** 2026-07-26T13:16:37Z
**Status:** passed
**Re-verification:** No — initial verification
**Execution range:** `56fb7167..HEAD` (25 commits, 7/7 plans, 4 waves)

> **Verification posture.** Nothing below is inherited from SUMMARY.md or VALIDATION.md.
> Every gate was re-run in this process, every guard was exercised against the real
> module, and **all 13 PYAPIFIX-05 mutations were re-applied and observed RED by the
> verifier**, not read off a ledger. Restores were `cp` from `/tmp` scratch copies —
> zero `git stash` / `git checkout` / `git clean` / `git reset`.

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria 1–6)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The `/process-key` duplicate reply and its consumer agree on ONE contract, proven real-vs-real | ✓ VERIFIED | `git ls-files` confirms `analytics-service/tests/fixtures/process_key_onboard_contract.json` is **tracked**, not generated. `grep -c "vi.mock" tests/lib/process-key-onboard-contract-parity.test.ts` → **0**. The Python consumer drives the REAL `main.app` through `TestClient` (`full_stack_client` fixture) and patches **only** `routers.process_key.get_supabase` — `_wizard_duplicate_reply` / `_resume_duplicate_job` / the handler are never patched. `isProcessKeyOnboardResponse` has exactly ONE implementation repo-wide (`src/lib/process-key-onboard-contract.ts:97`), imported by `route.ts:22` and called at `:1350`. |
| 2 | Venue-transient faults answer 424/`recoverable:true` via a **permanent-code ALLOW-LIST**, never a transient denylist; no body contradicts itself | ✓ VERIFIED | `PERMANENT_VALIDATION_ERROR_CODES` is a permanent-code `frozenset` in production code (`services/exchange.py:1015`) containing `MISSING_SCOPE`. Both consumers read it: the 424 pre-gate (`process_key.py:1427`, placed BEFORE the 403 return) and the `recoverable` derivation (`:410`). First-hand behavioural probe of `_envelope_error` — see Behavioral Spot-Checks: all 5 transient codes + an unknown code → `recoverable=True`; all 5 caller faults + the 5 route-terminal codes → `False`. The **fail-safe direction is inverted** as required. |
| 3 | `create_exchange` failures are 500/`retryable:false` at all THREE sites, no `dependency` | ✓ VERIFIED | `internal.py:452`, `exchange.py:469`, `portfolio.py:2294` all `raise service_error(500, "ADAPTER_INIT_FAILED", retryable=False, detail=<one shared string>)`. **No `dependency=` kwarg on any of the three** (read at source). `EXCHANGE_INIT_FAILED` has **0** raise sites left. `ValueError` → 400 preserved at all three. |
| 4 | The `body.detail.detail` scalar guarantee is ENFORCED, not documented | ✓ VERIFIED | `service_error_body` (`error_contract.py:~242`) raises `ValueError` on any non-`str` `detail` — a **positive** `isinstance(detail, str)` test, not a bad-type enumeration, and never a `str()` coercion. Proven by execution through **both** entry points (see spot-checks). |
| 5 | Every one of the 12 surviving mutations (+ M-15) turns a test RED, observed first-hand | ✓ VERIFIED | **13/13 re-applied and observed RED by the verifier** — full ledger below. Slowapi-dependent survivors (#3/#4/#5) measured on **slowapi 0.1.10** (`pip show` confirms; = CI's pin), not 0.1.9. #11 pinned at **both** `:333` and `:417`, and the asymmetry reproduced (a *different* single test reddens at each site). M-15's `len(_SHAPES) == 6` self-referential oracle is **deleted**. |
| 6 | `error_contract.py`'s remaining guard gaps are closed — the circular deferral is broken | ✓ VERIFIED | **(a)** `service_error(429, "RATE_LIMITED", retryable=True, retry_after=30)` constructs and carries `Retry-After: 30`; omitting `retry_after` raises. **(b)** `service_error(500, "X", dependency="binance", retryable=False)` raises `ValueError`; `dependency="supabase"` and `dependency=None` still validate (MEMBERSHIP, not prohibition). Plus the 503 `retry_after`-source guard: a wait that drifts from `RETRY_AFTER_SECONDS[dependency]` raises. All proven by execution. |

**Score: 6/6 truths verified.**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `analytics-service/tests/fixtures/process_key_onboard_contract.json` | Committed 5-case truth table (2 positive / 3 negative), consumed by BOTH processes | ✓ VERIFIED | Tracked by `git ls-files`. 2 `expect_valid: true`, 3 `false`. `consumers[]` names both readers; both readers assert that naming. |
| `src/lib/process-key-onboard-contract.ts` | Dependency-free leaf, both symbols exported, ONE implementation | ✓ VERIFIED | **Zero** `import` statements. `export type ProcessKeyOnboardResponse` + `export function isProcessKeyOnboardResponse`. Wired into `route.ts:22`/`:1350`. |
| `tests/lib/process-key-onboard-contract-parity.test.ts` | Real predicate over the committed fixture, ZERO mocks | ✓ VERIFIED | `vi.mock` count = **0**. Reads the fixture with `readFileSync`. Carries an independently-typed `EXPECTED_VERDICTS` literal roster (silent-shrink guard) plus an explicit anti-degradation case. |
| `analytics-service/tests/test_process_key_onboard_contract.py` | Real full-stack reply == fixture positives, both duplicate arms | ✓ VERIFIED | `TestClient(main.app)`; arms selected purely by the faked `strategy_verifications.status`. Key set pinned exactly, 5 discriminants pinned by value, 4 per-request keys pinned by type. Imports **nothing** from `routers.process_key`. |
| `analytics-service/services/exchange.py` :: `PERMANENT_VALIDATION_ERROR_CODES` | Canonical permanent-code allow-list incl. `MISSING_SCOPE` | ✓ VERIFIED | Present at `:1015`; `MISSING_SCOPE` is a member; the deliberate `long_fetch` divergence is documented in-file (see W-3). |
| `analytics-service/tests/test_envelope_recoverable.py` | Direct oracle for all ten codes | ✓ VERIFIED | Imports only `_envelope_error`; header states `PERMANENT_VALIDATION_ERROR_CODES` is **never imported** — expectations are literals. |
| `analytics-service/tests/test_create_exchange_attribution.py` | First-ever B2/B3 oracles | ✓ VERIFIED | 382 lines; `_ADAPTER_INIT_CODE = "ADAPTER_INIT_FAILED"` is a local literal, not an import from the module under test. |
| `analytics-service/tests/test_service_key_log_companion.py` | structlog capture: wrong key → 1 event, absent header → 0 | ✓ VERIFIED | Present; the production companions land at `main.py:695` (`error`, secret_unset) and `:722` (`warning`, mismatch) — the second **inside** `if provided:`, with an in-code note that dedenting it fires on every internet prober. |
| `analytics-service/tests/test_tenant_claim_parsing.py` | Claim-parser oracles for #8–#11 (guards previously untested) | ✓ VERIFIED | 389 lines. Imports only `rate_limit as rl` and uses only the **3 functions under test** + a `setattr(rl, "hmac", spy)` module spy. All 7 module-level constants are locally-minted literals. |
| `analytics-service/tests/test_error_contract_{429,500_dependency,scalar_detail,retry_after_source}.py` | Guard oracles × both entry points | ✓ VERIFIED | All four exist; each imports only `service_error` + `service_error_response`. Parity through both entry points confirmed by execution. |
| `analytics-service/tests/test_process_key_200_discriminator.py` | AST-fingerprint fence replacing `len(_SHAPES) == 6` | ✓ VERIFIED | `_EXPECTED_200_FINGERPRINTS` is a literal `frozenset` of 8; the ACTUAL side is derived from `routers/process_key.py`'s AST via `pathlib.Path(process_key_router.__file__)`. Correct oracle direction: pinned contract vs. the code's real shape. |
| `analytics-service/docs/STATUS_CONTRACT.md` | C-21 doc/code reconciliation | ✓ VERIFIED | CALLER row split into CALLER + **CALLER, THROTTLED**; the `429`-is-retryable rationale added; the `RETRY_AFTER_SECONDS` section scoped to 503-only and marked enforced; S-11's row **reversed** to `500 ADAPTER_INIT_FAILED / dependency: null` with an explicit "deliberately reversed" section; the 424 section gained a do-not-misattribute warning. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `src/app/api/strategies/finalize-wizard/route.ts` | `src/lib/process-key-onboard-contract.ts` | import + call | ✓ WIRED | `:22` import, `:1350` call. Predicate deleted from `route.ts` (64-line net reduction) — one implementation. |
| `tests/lib/…parity.test.ts` | `…/fixtures/process_key_onboard_contract.json` | `readFileSync` | ✓ WIRED | Reads the same bytes the pytest pins. Neither side imports the other's expectations. |
| `analytics-service/tests/test_process_key_onboard_contract.py` | the REAL `/process-key` route | `TestClient(main.app)` | ✓ WIRED | Full middleware stack (`verify_service_key` runs). Stub confined to the supabase boundary. |
| `routers/process_key.py` (pre-gate `:1427` + `_envelope_error` `:410`) | `services/exchange.py::PERMANENT_VALIDATION_ERROR_CODES` | shared allow-list | ✓ WIRED | Both read the same `frozenset` via `exchange_svc.`. |
| `routers/{internal,exchange,portfolio}.py` | `services/error_contract.py` | `service_error(500, …, retryable=False)` | ✓ WIRED | 3/3 sites; the old `dependency='binance'` shape is now unconstructable (plan 01's C3 guard). |
| `tests/test_rate_limit_contract.py` | `main.py::_retry_after_seconds` → the `Retry-After` header | header read | ✓ WIRED | Mutation hits the computation (`main.py:458`), the oracle reads the wire header (`:526`). Band derived from the test-declared limit string, not the function under test. |
| `analytics-service/main.py:695/:722` | structlog `_auth_log` | log companion beside `_capture_secret_misconfig` | ✓ WIRED | Both sites; `:722` correctly nested under `if provided:`. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data variable | Source | Produces real data | Status |
|---|---|---|---|---|
| `tests/lib/…parity.test.ts` | `spec.cases` | `readFileSync` of the **committed** fixture | Yes — 5 real cases, verdicts asserted against an independently-typed literal roster | ✓ FLOWING |
| `test_process_key_onboard_contract.py` | `r.json()` | REAL `TestClient(main.app).post("/process-key")` | Yes — 200 body from the real handler; key-set + discriminants pinned | ✓ FLOWING |
| `test_process_key_200_discriminator.py` | `shapes` | `ast.parse` of `routers/process_key.py`'s own source | Yes — a 7th 200 return changed the set and reddened (observed) | ✓ FLOWING |
| `test_limiter_identity.py` | 429 responses | REAL HTTP requests through a throwaway app whose route declares **no** `key_func` | Yes — an empty key made slowapi skip limiting, and the "never answered 429 within 4 calls" assertion caught it (observed) | ✓ FLOWING |
| `error_contract` guards | `ValueError` raises | Direct invocation of `service_error` / `service_error_response` | Yes — every guard raised under probe | ✓ FLOWING |

No HOLLOW / ORPHANED / STUB artifacts found.

---

### Probe Execution

| Probe | Command | Result | Status |
|---|---|---|---|
| Conventional `scripts/*/tests/probe-*.sh` | `find scripts -path '*/tests/probe-*.sh'` | none in repo; none declared in any PLAN/SUMMARY for this phase | SKIPPED (no probes declared) |

This is a Python/TypeScript contract-hardening phase; its BLOCKING acceptance gates are the
**Mutation Re-Run Protocol** and the **Cross-Process Contract Gate**, both executed below.

---

## BLOCKING GATE 1 — Mutation Re-Run Protocol (PYAPIFIX-05)

**Re-executed by the verifier, not inherited.** Every mutation below was applied by me to the
production file, the pinning test run, the RED observed, and the file restored from a `/tmp`
scratch copy. `git status --porcelain` after the last cycle: `M TODOS.md` + the pre-existing
untracked `analytics-service/scripts/nautilus_factsheet.py` — nothing else.

| # | Mutation (production file) | Verifier's observed result | Status |
|---|---|---|---|
| **#1** | `routers/exchange.py:485` `except ccxt.BaseError` → `ccxt.NetworkError` | **3 failed / 23 passed** — `PermissionDenied must answer 424, got 500`, `AuthenticationError …`, `ExchangeError …` | ✓ RED OBSERVED |
| **#2** | same → `ccxt.ExchangeNotAvailable` | **6 failed / 20 passed** — incl. `ExchangeError must answer 424, got 500`, `DDoSProtection …` | ✓ RED OBSERVED |
| **#3** | `services/rate_limit.py::default_platform_key` → `return ""` | **3 failed / 65 passed** — `default_platform_key returned the empty string…`, `assert '' == 'platform:/py…'`, **`/pyapifix05-default-key-probe never answered 429 within 4 calls`** | ✓ RED OBSERVED (slowapi **0.1.10**) |
| **#4** | same → per-request `uuid4().hex` | **3 failed / 65 passed** — `assert '50c126f526ed…' == 'platform:/py…'`, `default_platform_key is not stable across requests`, **`never answered 429 within 4 calls`** | ✓ RED OBSERVED (slowapi **0.1.10**) |
| **#5** | `main.py::_retry_after_seconds` → `return 1` | **2 failed / 5 passed** — `Retry-After is 1s on a bucket that was reset milliseconds a…` (route A) **and** `Retry-After is 1s on a freshly-reset '30 per 1 hour' bucket…` (route B, a different router) | ✓ RED OBSERVED (slowapi **0.1.10**) |
| **#6** | `services/error_contract.py:85` `"supabase": 15` → `900` | **4 failed / 37 passed** — 2× `assert '900' == '15'` plus 2× the construction guard `a 503 naming 'supabase' must advertise RETRY_AFTER_SECONDS['supabase'] == 15` | ✓ RED OBSERVED |
| **#7** | `routers/internal.py:66` `_KEK_ALERT_WINDOW_S = 300.0` → `1e18` | **1 failed / 29 passed** — `the operator signal must RETURN once the 300 s window expir…` | ✓ RED OBSERVED |
| **#8** | `services/rate_limit.py` drop the `len(raw) > _CLAIM_MAX_CHARS` bound | **2 failed / 12 passed** — `a 513-char claim was ACCEPTED…` and `verify_tenant_claim ran hmac.new 1 time(s) on a 513-char he…` | ✓ RED OBSERVED |
| **#9** | `raw.rsplit(".", 2)` → `raw.split(".", 2)` | **3 failed / 11 passed** — `assert 'claimtest:un…' == 'claimtest:t:a.b.c'`, `assert None == 'a.b.c'`, and the same via the nine-route key func | ✓ RED OBSERVED |
| **#10** | drop the `if not payload: return None` guard | **3 failed / 11 passed** — mutant bucket literally `'claimtest:t:'`; `assert '' is None` | ✓ RED OBSERVED |
| **#11 site A** | `rate_limit.py:333` `payload == ANON_PAYLOAD` → prefix match | **1 failed / 13 passed** — `got 'claimtest:anon'. A prefix match on the anonymous paylo…` (the `:417` test stayed GREEN) | ✓ RED OBSERVED |
| **#11 site B** | `rate_limit.py:417` same | **1 failed / 13 passed** — `got 'claimtest:anon' from tenant_or_platform_key — the key …` (the `:333` test stayed GREEN) | ✓ RED OBSERVED |
| **#12** | `routers/exchange.py` MT5 copy (2 sites) → `"Gateway unavailable."` | **1 failed / 19 passed** — `assert 'Gateway unavailable.' == 'The MetaTrad… not a ret…'` | ✓ RED OBSERVED |
| **M-15** | insert a 7th 200-capable `return` into `process_key` | **1 failed / 16 passed** — `the /process-key 200 surface changed shape.` | ✓ RED OBSERVED |

**13 of 13 closed. Zero "asserted only" rows.** The `#11` asymmetry — the same mutation at two
lines reddening a *different single test* each time, with the other green — is reproduced
first-hand and is the proof that both sites are independently covered.

**Note on the #6 result:** I observed **4** failures where the SUMMARY predicted 4, but the
composition differs — 2 are the plan-01 construction guard rather than 4 header-equality
assertions. This is because I ran a broader `-k` selector than the plan's targeted node ids.
The pinned wire-value assertions (`assert '900' == '15'`) fired as claimed.

---

## BLOCKING GATE 2 — Cross-Process Contract Gate (PYAPIFIX-01)

| Requirement | Verifier's evidence | Status |
|---|---|---|
| Oracle exercises the **real** Python reply against the **real** TS predicate | Python: `TestClient(main.app)` full stack, handler + `_wizard_duplicate_reply` + `_resume_duplicate_job` **never patched**. TypeScript: the REAL exported `isProcessKeyOnboardResponse` over the SAME committed bytes. | ✓ PASS |
| **Two new mocks agreeing with each other does NOT satisfy this** | `grep -c "vi.mock" tests/lib/process-key-onboard-contract-parity.test.ts` → **0**. The predicate lives in a leaf with **zero imports**, which is *why* no mocks are needed. The Python side's only patch is `routers.process_key.get_supabase` — a network dependency at the DB boundary, not the reply builder. | ✓ PASS |
| Fixture is COMMITTED, never generated | `git ls-files analytics-service/tests/fixtures/process_key_onboard_contract.json` → tracked. Both consumers `readFileSync`/`read_text` it; neither writes it. | ✓ PASS |
| Both sides run in EXISTING CI jobs | pytest under `analytics-service/tests/`; vitest under the `tests/lib/**` glob. No new `needs:` edge, no config change (no `.github/` or `vitest.config.ts` in the phase diff). | ✓ PASS |
| Neutering **either** side reddens the other's suite | Directly re-derivable from the artifacts: the Python oracle asserts `sorted(actual.keys()) == sorted(expected.keys())` against the fixture (a renamed emitter key reddens pytest), and the TS oracle asserts three negative verdicts (a degraded predicate reddens vitest). I did not re-run the executors' two neuter cycles — the 13 mutation cycles above were the higher-value spend — but the assertion structure that makes both directions falsifiable is present and was read at source. | ✓ PASS (structure verified) |

---

## Regression Fence (TRAP-9, both directions)

### Direction 1 — the fence that MUST NOT have been edited

`git diff --stat 56fb7167..HEAD -- analytics-service/tests/test_process_key.py`
→ **`1 file changed, 294 insertions(+)`**, single hunk `@@ -3993,3 +3993,297 @@` — a pure append.

Byte-level proof the four TRAP-9 fence tests are untouched:

```
git show 56fb7167:analytics-service/tests/test_process_key.py | head -3993 | shasum
  abf8860799e73fed847033d9c9c3c0754039991c
head -3993 analytics-service/tests/test_process_key.py | shasum
  abf8860799e73fed847033d9c9c3c0754039991c
```

Lines `:991`, `:2500`, `:2568`, `:2631` each read `assert r.status_code == 403, r.text`
at BOTH revisions. **The fence survives byte-identical.**

`services/ingestion/long_fetch.py` is absent from the phase diffstat — byte-unchanged as
plan 02's checker W-3 ruling required.

### Direction 2 — tests the new contract invalidated MUST pin the NEW contract with a LITERAL

| File | Old assertion | New assertion | Verdict |
|---|---|---|---|
| `test_status_contract_exchange_internal.py` (S-11) | `assert r.status_code == 424` / `code == "EXCHANGE_INIT_FAILED"` / `dependency == "binance"` | `assert r.status_code == 500` + **whole-body dict-literal equality** pinning `ADAPTER_INIT_FAILED`, `dependency: None`, `retryable: False` and the exact copy — plus a NEW retained control `test_s11_unsupported_exchange_name_stays_a_caller_400` pinning `{"detail": "Unsupported exchange: nasdaq"}` | ✓ Literal, strengthened |
| `test_match_router.py` | `assert int(retry_after) > 0` | `assert retry_after == "15"` (literal, explicitly *not* imported from `RETRY_AFTER_SECONDS`) | ✓ Literal, strengthened |
| `test_status_contract_match_sim_portfolio.py` ×3 | `assert int(retry_after) > 0` | `assert retry_after == "15"` ×3 | ✓ Literal, strengthened |
| `test_mt5_validate.py` | `assert detail != AUTH_FAILED_DETAIL` (an inequality against a *different* constant — junk copy satisfied it) | `assert detail == "The MetaTrader gateway is not configured. This needs an operator, not a retry."` (string literal typed in the test, not imported from `routers.exchange`), with the `!=` guard **kept** | ✓ Literal, strengthened |
| `route.test.ts:1474` | `it("rejects mixed envelope (queued=true with code field) with 502")` | **REWRITTEN, not deleted** → `it("accepts the resumed-wedge reply … and returns 200")` pinning `status 200`, `body.queued`, `body.verification_id`, the *absence* of forwarded `code`/`idempotent`/`job_state`, and that the Sentry `unified-response-parse` capture did **not** fire. **TWO new negatives retained** (N3 non-duplicate code → 502 + Sentry; N2 empty code → 502). Net 59 → 61, 0 deleted. | ✓ Literal, strengthened |

⚠️ **CORRECTED by Phase 140.1.2 plan 04 (PYAPIFIX2-06).** The sentence that stood here —
*"No already-correct test was weakened or removed. Across the whole phase diff the only
deletions in test files are the five assertions listed above, each replaced by a strictly
tighter one."* — was a blanket claim this report could not support. Two supportable facts
replace it:

1. **A whole test FUNCTION was deleted, not only assertions, and it is not in the table
   above.** `git diff 56fb7167..39688d69` removes
   `def test_pyapi_10a_exactly_six_shapes_are_covered():` from
   `analytics-service/tests/test_process_key_200_discriminator.py`. It appears in this
   report only in the Self-Referential Oracle Scan below, never in this Direction-2
   table, so the "only deletions … are the five assertions listed above" clause is false
   on its face. (The TypeScript `it(...)` in the same diff genuinely WAS a rewrite; the
   `route.test.ts:1474` row documents that accurately.)
2. **"Already-correct" was doing contested work.** The deleted test was correctly judged
   self-referential *as an oracle* — but it was also the **only** fence over the `_SHAPES`
   corpus, and its AST replacement reads `_ROUTER_SRC` and never `_SHAPES`. Deleting a
   `_SHAPES` row therefore shipped green (`141 → 140 passed`). That hole was open from
   140.1.1 until **140.1.2 plan 04**, which closed it by binding `_SHAPES` to the
   router's AST (`test_pyapifix2_05_every_router_declared_200_shape_has_its_own_reaching_fixture`,
   probe-observed RED on a deleted row).

---

## Self-Referential Oracle Scan

Phase 140.1's 19 test files had ZERO. This phase does not regress.

| Check | Result |
|---|---|
| M-15's `len(_SHAPES) == 6` against a same-file list literal | **DELETED**, plus its false docstring. Replaced by `_EXPECTED_200_FINGERPRINTS` (literal frozenset of 8) vs. an `ast`-derived actual set. Correct oracle direction. |
| `test_error_contract_*.py` (4 files) | Import **only** `service_error` / `service_error_response`. `RETRY_AFTER_SECONDS`, `SERVICE_DEPENDENCIES`, `_FALLBACK_DETAIL` are never imported. |
| `test_envelope_recoverable.py` | Imports only `_envelope_error`; header states `PERMANENT_VALIDATION_ERROR_CODES` is NEVER imported. |
| `test_tenant_claim_parsing.py` | Imports `rate_limit as rl` but touches only the **3 functions under test** plus a `setattr(rl, "hmac", spy)` module spy. All 7 module-level constants are locally minted (`_SECRET`, `_EXP`, `_SCOPE`, `_PATH`, `_NO_CREDENTIAL_HASH`, `_UNVERIFIED_BUCKET`, `_PLATFORM_BUCKET`). |
| `test_create_exchange_attribution.py` | `_ADAPTER_INIT_CODE = "ADAPTER_INIT_FAILED"` is a local literal. |
| `test_process_key_onboard_contract.py` | Header declares and code confirms: **nothing** imported from `routers.process_key`. |
| `parity.test.ts` | `EXPECTED_VERDICTS` is an independently-typed literal roster, confronted with the fixture. The predicate under test supplies only the *actual*. |
| `test_mt5_validate.py`, `test_match_router.py`, `test_status_contract_match_sim_portfolio.py` | New expectations are typed literals, with explicit in-code notes refusing the import. |

**Zero self-referential oracles found.**

---

## Gates Re-Run First-Hand

| Gate | Command | Verifier's result | vs. SUMMARY claim |
|---|---|---|---|
| Python types | `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` | **Success: no issues found in 89 source files** | matches |
| Python tests | `python3 -m pytest -q` | **4743 passed / 96 skipped / 0 failed** (176s), exit **0** | **exact match** |
| TS types | `npx tsc --noEmit` | exit **0** | matches |
| Lint | `npm run lint` | **0 errors**, 1 pre-existing unrelated warning (`EquityChart.tsx:1119`); admin-route + route-contract manifests OK | matches |
| Frontend tests | `npx vitest run` | **2 failed / 8876 passed / 287 skipped** (716 files), exit **1** | ⚠️ **does not match** — see W-1 |
| Frontend flake re-check | `npx vitest run --no-file-parallelism <the 2 files>` | **2 files / 28 tests passed** (2.32s) | flake confirmed |
| New `# type: ignore` | `git diff 56fb7167..HEAD \| grep -c "^+.*type: ignore"` | **0** | matches |
| Mutation residue | `grep -rn MUTANT src/ analytics-service/ tests/` | **0** | matches |
| Working tree | `git status --porcelain` | `M TODOS.md` + `?? analytics-service/scripts/nautilus_factsheet.py` — both pre-existing/orchestrator-owned | clean |
| slowapi version | `pip show slowapi` | **0.1.10** (= CI's `requirements.txt:226` pin) | matches |

**W-1 assessment.** The two failing files are `AllocationsTabs.scenario-state-preservation.test.tsx`
and `MultiKeyConnectStep.test.tsx`. Neither appears in `git diff --name-only 56fb7167..HEAD`,
and both pass in isolation. This is the documented file-parallelism flake class in this repo,
not a regression this phase caused. Recorded rather than waved away, because the ledger's
"8878 passed / 0 failed" is a stronger claim than this environment reproduces.

---

## Behavioral Spot-Checks

Run against the real modules in this process.

| Behavior | Command / probe | Result | Status |
|---|---|---|---|
| PYAPIFIX-04 — non-scalar detail rejected | `service_error(400,'X',retryable=False,detail=['a','b'])` | `ValueError: body.detail.detail must be a scalar human string; got list` | ✓ PASS |
| …dict too | same with `{'a':1}` | `ValueError: … got dict` | ✓ PASS |
| …strings still pass | same with `'fine'` | `400 {'code':'X','dependency':None,'retryable':False,'detail':'fine'}` | ✓ PASS |
| …same through the second entry point | `service_error_response(400,'X',retryable=False,detail=['a'])` | `ValueError: … got list` | ✓ PASS |
| PYAPIFIX-06a — 429 + Retry-After constructable | `service_error(429,'RATE_LIMITED',retryable=True,retry_after=30)` | `429`, `headers={'Retry-After': '30'}` | ✓ PASS |
| …429 without a wait refused | same, `retry_after` omitted | `ValueError: a 429 must carry Retry-After (STATUS_CONTRACT.md §1)` | ✓ PASS |
| PYAPIFIX-06b — venue name on a 500 refused | `service_error(500,'X',dependency='binance',retryable=False)` | `ValueError: a 500 may only name one of this service's dependencies ['egress-proxy','kek','mt5-gateway','supabase']…` | ✓ PASS |
| …our own dependency still permitted (MEMBERSHIP not prohibition) | `dependency='supabase'` | `500` constructed | ✓ PASS |
| 503 wait must come FROM the table | `service_error(503,'X',dependency='supabase',retryable=True,retry_after=999)` | `ValueError: a 503 naming 'supabase' must advertise RETRY_AFTER_SECONDS['supabase'] == 15, got 999` | ✓ PASS |
| …correct wait accepted | `retry_after=15` | `503`, `headers={'Retry-After': '15'}` | ✓ PASS |
| PYAPIFIX-02 — allow-list direction, all 13 codes | `_envelope_error(code, 'Try again in a moment.', …)` | `PROBE_FAILED`/`DDOS_PROTECTION`/`RATE_LIMITED`/`EXCHANGE_UNAVAILABLE`/`NETWORK_UNAVAILABLE` → **True**; `AUTH_FAILED`/`PERMISSION_DENIED`/`MISSING_SCOPE`/`TRADE_SCOPE`/`WITHDRAW_SCOPE`/`STRATEGY_NOT_OWNED`/`VALIDATION_UNEXPECTED` → **False**; an unknown venue code → **True** (fails SAFE) | ✓ PASS |

The last row is the criterion-2 second clause proven directly: no body can now emit
`recoverable:false` beside "Try again in a moment." for any of the five transient codes,
and the forgetful case defaults retryable rather than to a permanent credential accusation.

---

## Requirements Coverage

| Requirement | Source plans | Description | Status | Evidence |
|---|---|---|---|---|
| **PYAPIFIX-01** | 03, 05 | One duplicate-reply contract, proven real-vs-real | ✓ SATISFIED | Truth 1 + Blocking Gate 2. Fixture tracked; 0 `vi.mock`; full-stack TestClient; one predicate implementation. |
| **PYAPIFIX-02** | 02 | 424/retryable via a permanent-code ALLOW-LIST; `MISSING_SCOPE` a member; `recoverable` set fixed | ✓ SATISFIED | Truth 2. Allow-list at `exchange.py:1015`; pre-gate `:1427` precedes the 403; `recoverable` derivation `:410` inverted; 13-code behavioural probe. `BLOCKED-BY: TS-05` carve-out present in REQUIREMENTS.md (deferred, not missed). |
| **PYAPIFIX-03** | 04 | `create_exchange` failures = 500 at all THREE sites, no `dependency` | ✓ SATISFIED | Truth 3. `internal.py:452`, `exchange.py:469`, `portfolio.py:2294`. `EXCHANGE_INIT_FAILED` at 0 raise sites. ⚠️ REQUIREMENTS.md checkbox still `[ ]` (W-2). |
| **PYAPIFIX-04** | 01 | Scalar-detail guarantee ENFORCED | ✓ SATISFIED | Truth 4 + spot-checks through both entry points. |
| **PYAPIFIX-05** | 06, 07 | All 12 survivors + M-15 turn a test RED, observed first-hand | ✓ SATISFIED | Blocking Gate 1 — **13/13 re-observed by the verifier**, slowapi 0.1.10, #11 at both sites, M-15 oracle deleted. ⚠️ REQUIREMENTS.md checkbox still `[ ]` (W-2). |
| **PYAPIFIX-06** | 01 | `error_contract.py`'s remaining gaps closed; circular deferral broken | ✓ SATISFIED | Truth 6 + spot-checks. TS-23 annotated UNBLOCKED in the obligations ledger. |

**Orphaned requirements:** none. `grep "PYAPIFIX" .planning/REQUIREMENTS.md` maps exactly
PYAPIFIX-01..06 to Phase 140.1.1, and all six are claimed by a plan and verified above.

**Ledger reconciliation (CONTEXT.md's "Also required"):**

| Item | Status |
|---|---|
| Decide PYAPIFIX-02's fence (M-4 carve-out) | ✓ Recorded — `BLOCKED-BY: TS-05` in REQUIREMENTS.md (grep → 1) + TS-32 with the pairing, the reason (`"[object Object]"` dead-end render) and the ordering rule |
| Name PYAPIFIX-02's TRAP-9 casualties `:991/:2500/:2568/:2631` | ✓ Named — and proven byte-unedited by SHA |
| Reconcile TS-01/TS-02/TS-03 | ✓ TS-01 + TS-03 marked **DONE-IN-140.1.1**; TS-02 sharpened; count 31 → 33; TS-04..22/24..31 declared byte-unchanged |
| M-14's code half (`main.py` log companion) | ✓ Closed at **2** sites, not 1 |
| M-11 re-triage absorbed | ✓ **DECIDED, not implemented** — TS-33 records the one-field remedy, the strict ordering after TS-01 (now satisfied), and two rejected alternatives |
| C-21 doc/code reconciliation | ✓ STATUS_CONTRACT.md 429 row split + S-11 row reversed, in-range |

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | `TBD` / `FIXME` / `XXX` in any file modified by this phase | — | **0 found.** Debt-marker gate PASSES. |
| — | — | `TODO` / `HACK` / `PLACEHOLDER` / "not yet implemented" added by this phase | — | **0 found** in the `+` side of the diff. |
| — | — | `MUTANT` residue | — | **0 repo-wide.** |
| — | — | New `# type: ignore` | — | **0** (`git diff … \| grep -c` → 0). |
| `src/app/(dashboard)/allocations/.../EquityChart.tsx` | 1119 | `react-hooks/exhaustive-deps` warning | ℹ️ Info | Pre-existing, untouched by this phase. |

No stubs, no empty implementations, no hardcoded-empty props, no console-log-only handlers.

---

## Human Verification Required

**None.** VALIDATION.md's Manual-Only Verifications table declares "(none — every criterion in
this phase is automated)", and I independently confirmed every one of the six ROADMAP success
criteria programmatically — by reading the production code at source, by executing the guards,
and by re-running all 13 mutation cycles. No PLAN contains a `<verify><human-check>` block.

---

## Gaps Summary

**No gaps.** All six ROADMAP success criteria are observably true in the codebase, and the two
BLOCKING acceptance gates from VALIDATION.md are satisfied.

The three findings I did surface are warnings, not gaps:

- **W-1** — the full-suite `npx vitest run` is not deterministically green here (2 failures in
  files this phase never touched; both pass in isolation). The ledger's "8878 passed / 0 failed"
  overstates what this environment reproduces. Worth knowing before the PR, since CI runs the
  suite sharded and this is a known flake class in this repo — but it is not a regression from
  Phase 140.1.1 and does not bear on any success criterion.
- **W-2** — `.planning/REQUIREMENTS.md` still shows PYAPIFIX-03 and PYAPIFIX-05 as `[ ]` and the
  traceability row as `Pending`, while the code says otherwise. Bookkeeping lag.
- **W-3** — `long_fetch.py`'s local `permanent_codes` still omits `MISSING_SCOPE` and was
  deliberately not unified with the canonical allow-list. The divergence is documented **in the
  production file**, with the reason it was deferred. Intentional and named — but it means two
  enumerations of the same concept coexist, which is the shape this programme exists to close.
  It should be carried as an explicit follow-up rather than left to the in-code comment.

Three further items are **deferred, not missing** (see the `deferred:` frontmatter): the M-4
carve-out at `exchange.py:145/:152/:505` (TS-32, correctly blocker-named and inside criterion 2's
own scoping), the two in-handler 429 migrations (TS-23, explicitly out of scope per CONTEXT.md,
now unblocked by this phase's 06(a) work), and M-11's `wizard_session_id` wiring (TS-33 — this
phase was required to *decide*, and it did).

**Adversarial note.** This phase repairs defects in a programme where five earlier ad-hoc fix
batches were discarded at a ~1:1 fix-to-defect ratio, so I started from the hypothesis that the
narrative would outrun the code. It did not. The three claims most likely to be hollow — a
generated-not-committed fixture, a transient *denylist* wearing an allow-list's name, and
"the test now covers it" standing in for an observed RED — were each checked at source and each
held: `git ls-files` confirms the fixture is tracked, the frozenset enumerates *permanent* codes
with `MISSING_SCOPE` in it and an unknown code defaults retryable, and I reproduced all thirteen
REDs myself. The instance-vs-class check on `create_exchange` also holds at 3/3, including the
`portfolio.py` site that British spelling hid from the review's grep.

---

_Verified: 2026-07-26T13:16:37Z_
_Verifier: Claude (gsd-verifier) — all gates re-run first-hand; no executor result inherited_
