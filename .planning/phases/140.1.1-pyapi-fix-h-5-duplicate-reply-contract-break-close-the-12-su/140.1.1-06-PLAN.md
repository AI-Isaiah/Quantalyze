---
phase: 140.1.1-pyapi-fix
plan: 06
type: execute
wave: 3
depends_on: [140.1.1-01, 140.1.1-02, 140.1.1-04]
files_modified:
  - analytics-service/tests/test_status_contract_exchange_internal.py
  - analytics-service/tests/test_match_router.py
  - analytics-service/tests/test_status_contract_match_sim_portfolio.py
  - analytics-service/tests/test_mt5_validate.py
  - analytics-service/tests/test_process_key_200_discriminator.py
autonomous: true
requirements: [PYAPIFIX-05]
must_haves:
  truths:
    - "Survivors #1/#2 (ccxt narrowing), #6 (RETRY_AFTER supabase 15→900), #7 (KEK window 300→1e18), #12 (mt5 human copy) each turn a named test RED — re-run and observed first-hand, verbatim failure text captured (ROADMAP criterion 5)"
    - "M-15's self-referential fence (len(_SHAPES)==6 against a same-file literal) is DELETED and replaced by an AST-fingerprint fence derived from the router's own source; a seventh return site turns it RED (observed); a pure line shift does NOT (observed); a non-200 JSONResponse return does NOT (observed — the fence filters on 200-capable)"
    - "Working tree clean after every mutation cycle; zero MUTANT strings left anywhere"
  artifacts:
    - path: analytics-service/tests/test_status_contract_exchange_internal.py
      provides: "ccxt-family parametrisation (7 subclasses → 424, RuntimeError → 500, literal statuses) + two-phase clock-controlled KEK oracle"
    - path: analytics-service/tests/test_process_key_200_discriminator.py
      provides: "AST-fingerprint shape fence replacing the len(_SHAPES)==6 literal; false docstring deleted"
  key_links:
    - from: analytics-service/tests/test_process_key_200_discriminator.py
      to: analytics-service/routers/process_key.py
      via: "ast walk of the module under test — the expectation comes from the router's source, never a literal beside it"
      pattern: "import ast"
---

<objective>
PYAPIFIX-05, batch 1: the five NON-slowapi survivors (#1, #2, #6, #7, #12 from RESEARCH
§PYAPIFIX-05's authoritative 12-row table) plus M-15 (the 13th toothless test, folded in by the
requirement). Batch 2 (slowapi-dependent #3/#4/#5 + claim-parser #8–#11) is plan 07.

The bar (VALIDATION.md Mutation Re-Run Protocol — BLOCKING): "the test now covers it" is NOT
evidence. Each survivor: apply the exact named mutation → observe RED first-hand and capture the
assertion text → revert → observe GREEN → record all of it in the SUMMARY. 140.1 shipped 8/8
with zero "asserted only" rows — match that bar.

Wave-3 placement is deliberate: the mutation targets (error_contract.py, routers/exchange.py,
routers/internal.py, routers/process_key.py) were all edited by waves 1-2; mutations must be
applied to the SETTLED code.

Output: 5 re-toothed oracles + 1 replaced fence, 6 recorded mutation cycles (8 runs — M-15 has
three acceptance probes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md  (§PYAPIFIX-05 survivor table rows 1,2,6,7,12; §Newly Folded Findings M-15; C-20; §Validation Architecture — protocol + M-15/M-15b rows)
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md  (§5 Class D rows 1,2,6,7,12,13 — verified subject/oracle lines; §8 analog rows)
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-VALIDATION.md  (Mutation Re-Run Protocol)
@analytics-service/tests/test_status_contract_exchange_internal.py
@analytics-service/tests/test_process_key_200_discriminator.py
</context>

<interfaces>
Verified subjects and oracles (PATTERNS §5 — trust these lines over the review's):
- #1/#2: subject routers/exchange.py:467 `except ccxt.BaseError as e:`; weak oracle
  test_status_contract_exchange_internal.py:276-300 raises only ccxt.ExchangeNotAvailable (a
  NetworkError subclass). Analog to copy: tests/test_process_key.py:2434-2446 parametrize+ids.
- #6: subject services/error_contract.py:75 `"supabase": 15`; weak oracles (all
  `int(retry_after) > 0`): test_match_router.py:2636;
  test_status_contract_match_sim_portfolio.py:127, :154, :394. Correct idiom to copy VERBATIM:
  test_status_contract_exchange_internal.py:248-249 — "# Literal, NOT imported from
  RETRY_AFTER_SECONDS — this pins the wire value." + equality against "30" (substitute "15").
- #7: subject routers/internal.py:66 `_KEK_ALERT_WINDOW_S = 300.0`; weak oracle
  test_status_contract_exchange_internal.py:489-523, assertion :515 call_count == 1.
- #12: subject tests/test_mt5_validate.py:484 `assert ei.value.detail["detail"] !=
  AUTH_FAILED_DETAIL` — an inequality against a DIFFERENT constant; junk copy also passes.
- M-15 (#13): tests/test_process_key_200_discriminator.py — assertion :424 `len(_SHAPES) == 6`,
  `_SHAPES` literal :372-383 same file. C-20: the "six" is a COINCIDENCE (:1173/:1250 collapse
  via _wizard_duplicate_reply; :995 expands via _run_validate_only's two returns) — a
  count-based replacement would look right and mean something else. AST-read analog:
  tests/test_limiter_identity.py:477-478 (_source_name_tokens) and :486-495 (import ast +
  ROUTERS_DIR.glob).
- NOTE plans 02/04 may have shifted line numbers in the subject files — re-locate each subject by
  its quoted source text, not by the line number.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Survivors #1/#2 — parametrise the ccxt family; kill both narrowing mutants</name>
  <files>analytics-service/tests/test_status_contract_exchange_internal.py</files>
  <read_first>
    - analytics-service/tests/test_status_contract_exchange_internal.py:270-330 (the S-06 split tests — the one-type weak oracle and the :302-328 non-ccxt 500 sibling)
    - analytics-service/routers/exchange.py (locate the `except ccxt.BaseError` arm — post-plan-04 line numbers)
    - analytics-service/tests/test_process_key.py:2434-2446 (the parametrize + ids idiom)
  </read_first>
  <action>
    Extend/replace the S-06 424 oracle with a parametrisation over the ccxt family, each expected
    status a LITERAL in the test: ccxt.RateLimitExceeded, ccxt.PermissionDenied,
    ccxt.AuthenticationError, ccxt.RequestTimeout, ccxt.ExchangeError, ccxt.ExchangeNotAvailable,
    ccxt.DDoSProtection → each 424; plus a non-ccxt RuntimeError → 500. The oracle is the ccxt
    class hierarchy, never the arm's own text. Then two mutation cycles per the protocol
    (scratch copy outside repo, single-line, occurrence-count verified, targeted node id only,
    verbatim failure pasted, restore, tree clean): mutation A `except ccxt.BaseError` →
    `except ccxt.NetworkError`; mutation B → `except ccxt.ExchangeNotAvailable`. Both must go
    RED now (they SURVIVED twice in the review — under the narrowed arm RateLimitExceeded
    answers 500, the exact A-01/C-12 defect this programme exists to fix).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_status_contract_exchange_internal.py -q 2>&1 | tail -3 && git status --porcelain | grep -v "^??" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Parametrisation covers all 7 ccxt subclasses + 1 non-ccxt control, statuses as literals (grep the test: "424" and "500" literal hits; zero imports of the router's status constants).
    - Mutation A observed RED (verbatim failure line + counts in SUMMARY); mutation B observed RED; both reverted, GREEN re-observed.
    - `grep -rn MUTANT analytics-service/ src/` → empty; working tree clean (tracked files).
  </acceptance_criteria>
  <done>One parametrisation kills both H-4 survivors; the class is pinned by the hierarchy, not one member.</done>
</task>

<task type="auto">
  <name>Task 2: Survivors #6, #7, #12 — wire-value literal, two-phase clock, copy equality</name>
  <files>analytics-service/tests/test_match_router.py, analytics-service/tests/test_status_contract_match_sim_portfolio.py, analytics-service/tests/test_status_contract_exchange_internal.py, analytics-service/tests/test_mt5_validate.py</files>
  <read_first>
    - analytics-service/tests/test_status_contract_exchange_internal.py:240-260 (the mt5-gateway "30" control that CAUGHT its mutation — the pattern to replicate) and :489-523 (the KEK test)
    - analytics-service/tests/test_match_router.py:2630-2640 and tests/test_status_contract_match_sim_portfolio.py:120-160, :390-400 (the four `int(retry_after) > 0` weak oracles)
    - analytics-service/tests/test_mt5_validate.py:475-495 (the inequality oracle)
    - analytics-service/routers/internal.py (locate _KEK_ALERT_WINDOW_S and the capture path — post-plan-04 lines)
  </read_first>
  <action>
    #6: at ALL FOUR weak-oracle sites (test_match_router.py:2636;
    test_status_contract_match_sim_portfolio.py:127, :154, :394) replace `int(retry_after) > 0`
    with equality against the quoted literal "15" plus the verbatim two-line idiom
    "# Literal, NOT imported from RETRY_AFTER_SECONDS — this pins the wire value." Mutation
    cycle: services/error_contract.py `"supabase": 15` → `900`; run ONLY the four named node ids
    (full-suite runs are what let M-16 hide — its full-suite failure set was byte-identical to
    baseline); observe ≥1 RED per file; revert; GREEN.
    #7: rewrite the KEK alert test into the two-phase clock-controlled form: fire → assert
    exactly 1 capture; a second fire INSIDE the window → still 1; advance the monotonic clock
    (monkeypatch time.monotonic) past the LITERAL 300 seconds (literal in the test) → fire →
    assert 2 captures. Mutation cycle: `_KEK_ALERT_WINDOW_S = 300.0` → `1e18`; the phase-2
    assertion (2 captures after expiry) must go RED; revert; GREEN.
    #12: in test_mt5_validate.py:484, add an equality against the expected human copy as a STRING
    LITERAL typed into the test file (read the current copy from the router source and quote it);
    KEEP the existing `!= AUTH_FAILED_DETAIL` line as a second guard. Mutation cycle: junk the
    human copy at its production site → the new equality goes RED; revert; GREEN.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_match_router.py tests/test_status_contract_match_sim_portfolio.py tests/test_status_contract_exchange_internal.py tests/test_mt5_validate.py -q 2>&1 | tail -3 && git status --porcelain | grep -v "^??" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "int(retry_after) > 0" analytics-service/tests/test_match_router.py analytics-service/tests/test_status_contract_match_sim_portfolio.py` → 0 hits at the four subject sites; `grep -c "Literal, NOT imported" ` across the two files ≥ 4.
    - Three mutation cycles recorded FIRST-HAND with verbatim failure lines and counts (#6: 15→900; #7: 300→1e18; #12: junk copy). No full-suite mutation runs — targeted node ids only.
    - The KEK test asserts all three phases (1 inside, still 1 inside, 2 after expiry) with 300 as a literal.
    - Tree clean; zero MUTANT strings.
  </acceptance_criteria>
  <done>The three value-pinning survivors are dead: wire values are literals, the window expiry is observed, the human copy is pinned.</done>
</task>

<task type="auto">
  <name>Task 3: M-15 — replace the enumeration fence with an AST-fingerprint set</name>
  <files>analytics-service/tests/test_process_key_200_discriminator.py</files>
  <read_first>
    - analytics-service/tests/test_process_key_200_discriminator.py:360-430 (_SHAPES literal :372-383, the fence :416-424, the FALSE docstring claim)
    - analytics-service/routers/process_key.py (the handler's return sites — post-plan-02 positions; RESEARCH C-20's collapse/expansion map: :1173/:1250 collapse via _wizard_duplicate_reply, :995 expands via _run_validate_only)
    - analytics-service/tests/test_limiter_identity.py:470-500 (the in-repo "read the module's source with ast" idiom)
  </read_first>
  <action>
    Delete the `assert len(_SHAPES) == 6` fence AND its false docstring ("adding a seventh return
    site ... red test" — false; deleting the claim is mandatory even though the assertion is also
    replaced). Replace with an AST-fingerprint SET fence (~40 lines, stdlib ast): walk
    routers/process_key.py's `process_key` handler; collect the 200-capable top-level returns AND
    the returns of any helper a return delegates to (_wizard_duplicate_reply, _run_validate_only).
    200-CAPABLE (checker W-4 — the fence MUST filter on this predicate): a return is
    200-capable iff it has NO `status_code=` keyword, OR a `status_code=` whose literal value
    is 200. Filter every `return JSONResponse(...)` node on that predicate BEFORE
    fingerprinting — plan 02 (a declared dependency of this plan) adds a
    `return JSONResponse(status_code=424, ...)` to this same handler, and the existing 403
    return sits at ~:1360-1365; a naive walk would sweep both in, either reddening the fence
    spuriously or silently widening the pinned set — reintroducing the very defect class the
    fence exists to catch. Assert IN THE TEST that the 403 arm and plan 02's 424 arm are
    excluded by name. Then
    render each as a stable fingerprint — sorted top-level key names for a dict literal, callee
    name for a delegation; assert the SET equals a literal set written in the test. The
    expectation derives from the router's source; the literal set is the pinned CONTRACT (this is
    the sanctioned direction: source-derived actual vs pinned expected — never literal vs literal
    in the same file). NEVER key on line numbers or return COUNT (C-20: six is a coincidence of
    one collapse cancelling one expansion).
    Three acceptance probes, all run first-hand (RESEARCH Validation rows M-15/M-15b + checker W-4):
    (a) add a throwaway `return {"ok": True, "code": None, "mutant": 1}` inside the handler
    outside any covered branch → run the fence → EXACTLY 1 failure naming the new fingerprint →
    revert; (b) insert a blank line above the handler → re-run → GREEN (a red here means the
    fingerprint keyed on positions — re-derive it) → revert; (c) add a throwaway
    `return JSONResponse(status_code=418, content={})` inside the handler → re-run → GREEN
    (checker W-4 probe: a non-200 JSONResponse return must NOT enter the fingerprint set —
    a red here means the 200-capable filter is missing) → revert.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_process_key_200_discriminator.py -q 2>&1 | tail -3 && grep -c "len(_SHAPES) == 6" tests/test_process_key_200_discriminator.py && python3 -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "len(_SHAPES) == 6" tests/test_process_key_200_discriminator.py` → 0; the false docstring sentence gone.
    - `grep -c "import ast" tests/test_process_key_200_discriminator.py` ≥ 1; no assertion compares two literals declared in the same file.
    - Probe (a) observed: 1 failed naming the mutant fingerprint (verbatim in SUMMARY); probe (b) observed: green on a pure line shift; probe (c) observed: green on an added non-200 JSONResponse return (the 200-capable filter excludes it — and the test names the 403 arm and plan 02's 424 arm as excluded). All three reverted; tree clean.
    - The rest of the discriminator suite untouched and green.
  </acceptance_criteria>
  <done>The 13th toothless test is dead: the fence reads the router's AST, reddens on a seventh shape, and ignores line churn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → CI gate | these oracles ARE the gate; a toothless oracle is a silently-open gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1.1-16 | Tampering | ccxt-arm narrowing regression | mitigate | family parametrisation — a future narrowing turns 7 literal-status assertions RED instead of shipping 500s for venue throttles |
| T-140.1.1-17 | Repudiation | mutation evidence | mitigate | protocol: verbatim failure text + counts per cycle, FIRST-HAND label, tree-clean check after each — "asserted" rows are non-compliant |
| T-140.1.1-18 | Tampering | scratch-copy hygiene | mitigate | copies outside the repo; never git stash/checkout (gitignored .planning ledgers have no backup) |
| T-140.1.1-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- 5 survivors + M-15 closed with first-hand RED/GREEN cycles; 8 recorded probes total (2+3+3).
- No full-suite mutation runs; targeted node ids only.
- `git status --porcelain` clean (tracked) and `grep -rn MUTANT analytics-service/ src/` empty after every cycle.
- Full pytest green at plan end: `python3 -m pytest -q`.
</verification>

<success_criteria>
ROADMAP criterion 5 for survivors #1/#2/#6/#7/#12 + the M-15 fold-in: each mutation now turns a
named test RED, re-run and observed — including the ccxt narrowing that survived twice.
</success_criteria>

<output>
Create `.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-06-SUMMARY.md` when done.
Record: a mutation ledger table (survivor · mutation · node id · verbatim RED line · counts · FIRST-HAND), the M-15 probe evidence, and the fingerprint set chosen.
</output>
