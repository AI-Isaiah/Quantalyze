# Phase 140.1.1 — Stage 2 red-team findings (5 lenses)

Range `56fb7167..39688d69`. All read-only, by SHA, no mutations injected (Stage 1 covered that).
Lenses: gate/claims audit · reachability & blast radius · enumeration by behaviour ·
adversarial refutation · CSV settlement (reported separately in STAGE1-FINDINGS.md).

---

## RT-1 — GATE / CLAIMS AUDIT

**Headline: this is NOT the precedent repeating.** 140.1's gate claimed "8/8, none asserted-only"
while an independent pass found 12 survivors — a direct contradiction. Here, Stage 1 injected 36
FRESH mutations and found 6 survivors, and **not one of the 6 is among the 13 the gate certified.**
The ledger is not contradicted.

### UPHELD
- **"13/13 observed RED" — UPHELD.** All 12 mapped back to `140-REVIEW.md` (H-4×2, M-7×2, M-8, M-16,
  M-18, L-4×4, L-12) + M-15 = 13. No renumbering, no substitution. **#7/#11 were skipped for
  anchor ambiguity, not measured-and-found-alive** — confirmed mechanically (`_KEK_ALERT_WINDOW_S`
  occurs 3× in `internal.py`; `if payload == ANON_PAYLOAD:` occurs exactly 2× in `rate_limit.py`); a
  unique-anchor harness MUST refuse both. Both are structurally airtight on read.
  *Named weakness: no raw output retained — every row is prose. Re-derivable, not re-readable.*
- **PYAPIFIX-03 = 3/3 — UPHELD at source.** `internal.py:452`, `exchange.py:469`,
  `portfolio.py:2294`, no `dependency=` on any; `EXCHANGE_INIT_FAILED` 0 raise sites repo-wide.
- **Must-haves 1, 3, 4, 5, 6b — SOUND**, each with a real falsifier and evidence that establishes it.
- **Vitest: BOTH numbers are right.** RT-1 independently re-ran → **8878 passed / 0 failed, exit 0**,
  reproducing the EXECUTORS exactly. Arithmetic reconciles the verifier: 8876+2 = 8878, same corpus,
  exactly 2 flipped. `vitest.config.ts:6-16` **documents this flake in-repo** with its exact signature.
  pytest 4743/96/0 matches everyone. **The verifier was RIGHT to log the discrepancy.**

### ⭐ RT1-A (HIGH) — the gate's own strongest claim is FALSE
`140.1.1-VERIFICATION.md:239-241` (Regression Fence, Direction 2): *"**No already-correct test was
weakened or removed.**"* Measured: `git diff | grep -E "^-\s*(def |it\()"` returns **two** deleted
tests. The TS one is in the deletions table (correctly, as REWRITTEN). **The Python one
(`test_pyapi_10a_exactly_six_shapes_are_covered`) is not** — it is filed under "Self-Referential
Oracle Scan" as an unambiguous good. **It was BOTH**: self-referential against the router, AND the
only guard against the `_SHAPES` corpus shrinking — the axis Stage 1 has now OBSERVED a mutation
surviving on. Direction 2, the structure built to catch exactly this, **under-enumerated by one.**

### ⭐ RT1-B (HIGH) — PYAPIFIX-02's carve-out completeness was ASSERTED, NOT CHECKED, and is false
- **SF-3 partly over-called:** `exchange.py:522` **is already the deferred site**, reported in base
  coordinates — today's `:523` IS base `:505`, named in TS-32. *(SF-3 is itself an instance of the
  CM-5 stale-coordinate class.)*
- **But the enumerated "remainder" is not the remainder.** In the SAME route, `_validate_mt5_key`
  answers **400** at `exchange.py:335` (probe timeout), `:345` (`Mt5AccountMismatchError`), `:361`
  (transient `Mt5ClientError`) — structurally identical to the enumerated sFOX site `:152`. **MT5 is
  LIVE.** None is in `{145, 152, 505}`. Plus `portfolio.py:2322`, whose TS consumer is not
  `classifyKeyValidationError`, so TS-05 is not even established as its blocker.
- **ROADMAP criterion 2 says only "not 403". Every missed site answers 400 — the criterion
  structurally cannot see them. TOO NARROW.** `REQUIREMENTS.md:89` carries the stronger clause, and
  **that clause IS falsified.**
- **The gate mis-cited its own scope**: its `deferred:` frontmatter attributes the scoping clause to
  the ROADMAP criterion. `grep -c "a Python-side change can correct" .planning/ROADMAP.md` → **0**;
  REQUIREMENTS.md → **1**.
- **And its evidence was a string-existence check** — `grep -c "BLOCKED-BY: TS-05" → 1` proves the
  marker exists, not that the list is complete. **This is the one place the gate accepted an executor
  claim it did not check.**

### RT1-C (MED) — two must-haves are unfalsifiable as phrased
- **MH-2 is one-directional**: it forbids only false-negatives. Nothing forbids a NON-venue fault
  answering 424/`recoverable:true`. **"Make everything 424" satisfies it** — which is materially what
  the CSV path now does. **The criterion literally cannot fail on the phase's most-cited HIGH.**
- **MH-6a** requires only that `service_error(429, …)` construct. **Says nothing about the wire —
  satisfied by dead code.** TD-1 sits entirely outside it.

### RT-1 verdict
**`140.1.1-VERIFICATION.md` should read `gaps_found`, not `passed`** — two narrow, specific gaps
(RT1-A, RT1-B). Everything else holds under independent re-execution.
**Credit where due:** the gate logged W-1 against its own executors rather than smoothing it; labelled
one row "PASS (structure verified)" instead of claiming execution; recorded the `long_fetch`
divergence as a live follow-up; and flagged that its own #6 mutation composition differed from the
summary. **It did not over-certify the mutation ledger — which is what the precedent said to look for.**

---

## RT-3 — ENUMERATION BY BEHAVIOUR

**Method:** AST walk over all non-test `.py` (callee ∈ HTTPException/JSONResponse/service_error/
service_error_response/Response/PlainTextResponse), plus a second pass asserting **no non-literal
`status_code`** outside `error_contract.py` and **no `status_code=` in any route decorator** — so the
literal enumeration is **provably total**. Not a grep.

### ⭐⭐ RT3-1 (HIGH) — `MT5_WRONG_SERVER` / `MT5_MASTER_PASSWORD` are permanent caller faults classified as recoverable VENUE faults
`services/ingestion/mt5.py:104` and `:224`. Neither is in `PERMANENT_VALIDATION_ERROR_CODES`, so the
424 pre-gate answers **424 FAILED_DEPENDENCY, `recoverable:true`** — *"your exchange did not complete
the check, try again shortly"* — to a user who typed the wrong broker server **or pasted their
master (trade-capable) password**. `mt5.py:218-227` rejects the latter *precisely because it is
trade-capable*, and the reply blames the venue.
**The TS side already disagrees**: `wizardErrors.ts:1002/1005` classifies both as **400, "client
faults (fixable by the user)"**. **Python and TypeScript now hold opposite verdicts on the same codes.**
**Worse on the live worker path:** MT5 keys are `flow_type ∈ {onboard, resync}` → `long_fetch.py`,
whose local `permanent_codes` also omits them → `error_kind="transient"` → **3 attempts →
`failed_final`**, each serializing through the single MT5 gateway lock and spinning a Wine terminal,
for a credential that **can never succeed**.

### ⭐ RT3-2 (HIGH) — `/api/validate-key`'s collapse renders as `UNKNOWN`/500 "team has been notified"
`exchange.py:522-523` → `analytics-client.ts:174-181` wraps as `AnalyticsUpstreamError(detail, status)`
→ `create-with-key/route.ts:472` / `composite/add-key/route.ts:427` call
`classifyKeyValidationError`, **a substring cascade over the human string** (`wizardErrors.ts:967-1035`).
Running the five detail strings through it: `RATE_LIMITED`→`KEY_RATE_LIMIT`✓ · `PROBE_FAILED`/
`MISSING_SCOPE`→`KEY_PROBE_FAILED`✓ · `DDOS_PROTECTION`→`KEY_IP_ALLOWLIST` (wrong code, right flavour)
· **`NETWORK_UNAVAILABLE` → falls through → `UNKNOWN`/500** · **`EXCHANGE_UNAVAILABLE` → falls through
→ `UNKNOWN`/500** · **`UNSUPPORTED_EXCHANGE` → `UNKNOWN`/500** (a permanent caller fault rendered as
*our* 500).
**A Binance maintenance window on the live key-connect wizard renders as "something went wrong, our
team has been notified", with no retry affordance** — verbatim the DOGFOOD-3 dead end
`wizardErrors.ts:940-960` was written to kill. *(`NETWORK_ERROR_DETAIL` contains "Network" but the
branch tests "timeout"/"etimedout" — a genuine two-string near-miss.)*

### ⭐ RT3-3 (HIGH) — the allow-list is STRUCTURALLY UNCOMPLETABLE
`csv_adapter.py:172` mints `first_rule.upper()` — an **open code space** derived from pandera rule
names. **No enumeration of `PERMANENT_VALIDATION_ERROR_CODES` can ever cover it**, so
"unknown ⇒ recoverable" keeps misclassifying by construction, not by omission. Confirms the CSV
settlement's fix direction: gate on `source`, never on a string list.

### RT3-4 (MED) — the KEK-503 class is closed at 3 of 4
`exchange.py:578-580` is the **third `get_kek()` arm in the same file, 37 lines below the one the
phase fixed**, still a bare 503 with no `Retry-After`, no `dependency`, no `code` — contradicting
S-07's own rationale ("*the previous 503 was the self-sustaining-outage shape at its worst*").
Behaviour predicate "*an `except RuntimeError` around `get_kek()`*" finds 4 HTTP-route arms
(`exchange.py:535`, `:578`, `internal.py:272`, `cron.py:607`). **The seam-reachability exemption
answers the breaker question, not the class question.**

### RT3-5 (MED) — `exchange.py:680`: every venue I/O failure on `/api/fetch-trades` → 500
CALLER'S EXCHANGE answered as SERVICE-PERMANENT. C-12 shape.

### RT3-6 (MED) — 6 sites → **3 live 429 shapes**, not 4; four have NO `Retry-After`
`main.py:513` (rich envelope, always has the header, inherited by every `@limiter.limit` route) ·
`internal.py:227` (scalar + header, internal only) · **`match.py:1741`, `portfolio.py:1959`,
`portfolio.py:2244`, `simulator.py:248` — scalar detail, NO header, USER-FACING** ·
`service_error(429)` = zero call sites. **Shape C is the majority by site count and tells a throttled
caller nothing about when to return.** O-6 has a 429-shaped hole four sites wide.

### RT3-7 (MED) — `UNSUPPORTED_EXCHANGE` (`exchange.py:1219`): permanent caller fault → `recoverable:true`+424 in Python, `UNKNOWN`/500 in TS.

### RT3-8 — there are **FOUR** sibling vocabularies, not two
`PERMANENT_VALIDATION_ERROR_CODES` (5, unsafe) · `long_fetch.py:391 permanent_codes` (4, unsafe) ·
**`cron.py:138 CREDENTIAL_REJECTION_CODES` (4, SAFE — omission means "retry next tick", never
"deactivate")** · `_ROUTE_TERMINAL_ERROR_CODES` (5, complete ✓).
**`cron.py:138` is a fourth copy nobody named — and it is the only fail-safe one, i.e. the design the
other three should adopt.**

### Genuinely CLOSED — attacked by behaviour and could not be moved
- **`ADAPTER_INIT_FAILED` 3/3 CONFIRMED** by re-running the phase's own predicate ("*an `except` arm
  around a callee performing no network I/O*") over all **21** `create_exchange` call sites. Nothing missed.
- **The S-01…S-24 5xx site map is COMPLETE — delta 0.** Independent AST count: 11 raw 5xx + 21 builder
  5xx + 1 `/health` literal, matching the doc site-for-site. *"The one enumeration in the phase I could
  not break."* **⇒ TD-2's "~40 raw `HTTPException` sites, several 5xx" overstated the 5xx count: it is 11.**
- **S-22 ("`process_key.py` has ZERO explicit 5xx") is TRUE** (statuses: 403,403,422,422,403,403,424,401).
- **`_ROUTE_TERMINAL_ERROR_CODES` is complete today** — 3 literals + `UNKNOWN` default +
  `VALIDATION_UNEXPECTED` fallback = exactly 5. (The structural-link concern stands; the count does not.)

### RT-3 bottom line
*"The phase enumerated codes by reading THE ROUTE's call sites; the producers live in
`services/ingestion/`, and one generates code strings at runtime. Searching for the behaviour 'what can
appear in `val.error_code`' rather than 'what does this file pass to `_envelope_error`' is what
surfaces the MT5, CSV and `UNSUPPORTED_EXCHANGE` findings — the same lesson the phase recorded for
`create_exchange`, applied one layer down."*

---

## RT-2 — REACHABILITY & BLAST RADIUS

### Q1 CSV → 424 gate: **UNREACHABLE, doubly blocked**
The gate sits in the sync pipeline at step 4, entered only when `strategy_id` is non-None
(`process_key.py:1122`). Both production CSV callers build `context` from an **explicit key list** (no
raw-body spread), so a browser cannot inject `strategy_id`; the third finalize arm bypasses
`/process-key` entirely; and `flow_type="csv"` is the only flow the H-11 whitelist admits
`source="csv"` for. Second independent block: `INTERNAL_API_TOKEN` (`:836`) then the ownership gate.
⚠️ **RT-2 CONTRADICTS the CSV-settlement RT on one detail:** it says the `STRATEGY_NOT_OWNED` /
`validate_called=0` wall was **NOT a fixture artifact — it is the real precondition, and the second of
two.** Both agree on UNREACHABLE. *(Unresolved sub-detail; does not change the verdict.)*

### ⭐ RT2-A (NEW, MED) — the gate's SOLE live entry is the PUBLIC teaser, and its improvement is invisible
`flow_type="teaser"` force-sets `strategy_id = TEASER_ANCHOR_STRATEGY_ID` at `process_key.py:933`
**before** the None check, so it always falls through to the sync pipeline. (`internal_report` has zero
callers; `onboard`/`resync` are long-fetch.) So the 424 arm fires on **public landing-page
submissions** where the venue probe failed — a **correct** reclassification (they were 403 at baseline).
**But `VerificationForm.tsx:53-55` does `throw new Error(data.error ?? "Verification failed")` — the
envelope carries `code`/`human_message`/`recoverable` and NO `error` key, so every 424 renders on the
public form as the bare string "Verification failed".** The venue's "Try again in a moment." and
`recoverable:true` are both dropped. **This is finding B-16 from the original programme, still live.**
Not a regression (the pre-140.1 shape rendered "Verification service returned an invalid response").

### Q2 breaker: **2 of 3 links hold; the third route is DEAD; and it is PRE-PRODUCTION**
| Site | Behind `resilientFetch`? |
|---|---|
| `internal.py:450` → `/internal/keys/{id}/permissions` | **YES** |
| `exchange.py:467` → `/api/validate-key` | **YES** |
| `portfolio.py:2292` → Python `/api/verify-strategy` | **DEAD — no caller repo-wide** (legacy arm deleted in Phase 106 Stage B). **`STATUS_CONTRACT.md`'s new row lists it as live: a documentation over-claim.** |

- **No filter** between the Python 500 and `recordSeamFailure()` — `resilient-fetch.ts:562` is
  unconditional, before the caller sees the body.
- **Quantified: 5 real user actions in 30s**, from any users on any venue, opens the circuit for every
  seam call. The permissions route's 60s `unstable_cache` does **not** damp it — a non-ok response
  throws out of the cached callback and is deliberately never cached.
- ⚠️ **Two calibrations that LOWER the severity materially:**
  1. **This is not live.** `src/lib/resilient-fetch.ts` **does not exist on `origin/main`** — the whole
     breaker is branch-only. **Ship-blocker risk, not a production defect.**
  2. **Relative to `main`, `internal.py` moves 502 → 500 — no breaker-accounting delta.** Only
     `exchange.py`'s **400 → 500** is genuinely new fuel. And `exchange.py:511` already answers
     `500 INTERNAL` on the same handler, with the code explicitly reasoning about the
     "five keys on one dashboard render" hazard at `:479-484`.
- **User-visible delta at the permissions consumer: NONE.** `route.ts:143` throws
  `err.detail ?? …` where `err.detail` is the `service_error` **object** → `"[object Object]"`,
  matching no classifier branch. Old 424 and new 500 render identically. **Only the accounting changed.**

### Q3 `UNSUPPORTED_EXCHANGE`: **UNREACHABLE**, latent hazard confirmed
All ten `validate_key_permissions` callers pass a `create_exchange` product, and `ccxt.{binance,okx,
bybit,deribit}.id` equals its dict key by construction. **sFOX and MT5 do NOT open it up** — both branch
*before* `create_exchange` (`internal.py:220-222`, `exchange.py:351-380`), have their own `validate()`,
and never call `validate_key_permissions`.

### Q4: **SIX 429 sites, not four — and the new arm governs ZERO of them**
Sweeping every `service_error(...)` call yields statuses `{400,422,424,500,503}` — **no
`service_error(429, …)` anywhere.** Live shapes: `main.py:513` (full envelope + header, inherited by
every `@limiter.limit` route) · `internal.py:228` (bare `{"detail"}` + header — **and its 429 status
AND `Retry-After` are both discarded by `keys/[id]/permissions/route.ts:143`, laundered into
`502 / PROBE_FAILED`**) · `match.py:1742`, `portfolio.py:1960`, `simulator.py:249` (bare, **no
header**, user-facing) · `portfolio.py:2245` (bare, no header, **dead route**).

### Q5 two bounded items
- **PYAPIFIX-01 fixed a real defect that would have shipped — blast radius zero in production.** The
  pre-diff predicate rejected `{queued:true, code:"WIZARD_DUPLICATE", idempotent:true}` as a "mixed
  envelope", firing 502 + Sentry on a *successful* resume. On `origin/main` the duplicate reply is
  hardcoded `"queued": False` and `_resume_duplicate_job` does not exist — **the defect was introduced
  by 140.1 within this same unreleased branch and closed here.**
- **The scalar-detail guard cannot fire.** All 23 `service_error` `detail=` args are string literals or
  f-strings. Forward guard, no blast radius.

### Blast-radius ranking (reachable only)
1. **Global breaker amplification via `exchange.py:467`** — *gated: pre-production, needs a dependency
   bump to fire, and `internal.py`'s contribution is not a delta vs main.*
2. **Teaser 424 copy loss** — every public submitter hitting a venue blip sees bare "Verification
   failed". Continuous, low-severity, top-of-funnel.
3. **`internal.py:228` 429 → 502 laundering** — `Retry-After` stripped. Marginal frequency.

### Stage-1 claims RT-2 marks OVER-STATED
CSV→424 (path does not exist) · `recoverable` inversion user impact (no TS consumer reads the field) ·
`portfolio.py:2292` (**dead route**) · "four 429 shapes" (**six**, and the new arm governs none).
