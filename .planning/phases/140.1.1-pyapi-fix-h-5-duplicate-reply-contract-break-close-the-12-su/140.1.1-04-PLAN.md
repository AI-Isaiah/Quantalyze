---
phase: 140.1.1-pyapi-fix
plan: 04
type: execute
wave: 2
depends_on: [140.1.1-01]
files_modified:
  - analytics-service/routers/internal.py
  - analytics-service/routers/exchange.py
  - analytics-service/routers/portfolio.py
  - analytics-service/main.py
  - analytics-service/docs/STATUS_CONTRACT.md
  - analytics-service/tests/test_status_contract_exchange_internal.py
  - analytics-service/tests/test_create_exchange_attribution.py
  - analytics-service/tests/test_service_key_log_companion.py
autonomous: true
requirements: [PYAPIFIX-03]
must_haves:
  truths:
    - "A non-ValueError escape from create_exchange answers 500 retryable:false at ALL THREE classification sites — never 424 (blames the venue) and never 400 (blames the caller) (ROADMAP criterion 3)"
    - "The 500 names no dependency and carries no Retry-After — and after plan 01's 06(b) guard, constructing the old internal.py shape (500 + dependency='binance') raises at construction"
    - "ValueError (unsupported exchange name) stays 400 at every site — that IS caller input"
    - "Both _capture_secret_misconfig sites with no log companion (main.py:690 unset, :710 mismatched) now log; the absent-header case logs NOTHING (M-14, count 2 not 1 — PATTERNS §7/§9-M5)"
  artifacts:
    - path: analytics-service/tests/test_create_exchange_attribution.py
      provides: "B2/B3 oracles — none existed (grep-proven: 'Failed to initiali[sz]e exchange connection' has zero test hits)"
    - path: analytics-service/tests/test_service_key_log_companion.py
      provides: "structlog capture: wrong key → 1 event; absent header → 0 events"
  key_links:
    - from: analytics-service/routers/internal.py
      to: analytics-service/services/error_contract.py
      via: "service_error(500, ..., retryable=False) — no dependency kwarg survives (06(b) enforces)"
      pattern: "service_error"
    - from: analytics-service/routers/portfolio.py
      to: analytics-service/services/exchange.py
      via: "B3's except arm around create_exchange"
      pattern: "create_exchange"
---

<objective>
PYAPIFIX-03: a failure in code that performs NO network I/O is attributed to us (500), never to
the caller's venue (424) or the caller (400) — so it counts, and someone is paged.

Class B per PATTERNS §1/§3 — **3 members, count pinned** (the review named 1, RESEARCH 2; the
pattern-mapper found the third):
- B1 `routers/internal.py:414-433` (S-11): except Exception → service_error(424,
  "EXCHANGE_INIT_FAILED", dependency=str(exchange_name), retryable=True). The review's named
  subject. Exact lines: except :418, logger.warning :423, service_error( :427, dependency= :430
  (the review's :423/:431 cites are off — §9-M11).
- B2 `routers/exchange.py:444-454`: except Exception :448 → HTTPException(400, "Failed to
  initialize exchange connection").
- B3 `routers/portfolio.py:2265-2277`: except Exception :2269 → HTTPException(400, "Failed to
  initialise exchange connection") :2277 — named by NOBODY; found only because British spelling
  defeats the grep. The in-repo proof the class is real: portfolio.py:2316 is the SAME function's
  SECOND create_exchange and already answers 500 (B4, already correct — do not touch).
Non-members (do not touch): B5-B8 unwrapped sites (exchange.py:602, cron.py:224,
debug_key_flow.py:129/:212, funding_fetch.py:852, job_worker.py:968, ingestion adapters) —
verified none wraps create_exchange in a venue-attributing except further up.

Also folds in M-14 (Class F, operator-signal theme): TWO missing log companions, not one —
main.py:690 (SERVICE_KEY unset) and :710 (mismatched, "C-11's HEADLINE"). 3 of 5 sites pair with
a log line today, not the review's "four of five" (§9-M5).

Output: 3 rewritten except arms, S-11 test rewrite, 2 new test files, 2 log lines, doc row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md  (§3 Class B inventory; §7 Class F; §8 analog rows)
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md  (§PYAPIFIX-03; §Newly Folded Findings M-14; OQ-2)
@analytics-service/routers/internal.py
@analytics-service/routers/exchange.py
@analytics-service/routers/portfolio.py
@analytics-service/main.py
</context>

<interfaces>
Verified at 56fb7167:
- `create_exchange` services/exchange.py:817-861: EXCHANGE_CLASSES.get(), a dict build,
  cls(config), two attribute sets. ZERO network I/O. Every non-ValueError escape is ours.
- Emission analog: routers/exchange.py:487-495 (S-06b) — 500 / retryable=False / no dependency /
  no Retry-After / copy naming no credential and no venue. Do NOT copy its code string "INTERNAL".
- Logger analog: routers/internal.py:441-444 — logger.error with key/exchange identifiers and no
  raw exception text in a client body.
- Test template: tests/test_status_contract_exchange_internal.py:302-328
  (test_s06_non_ccxt_escape_is_permanent_500_and_never_blames_credentials) — whole-body pin.
  Model rewrite idiom: tests/test_verify_service_key_middleware.py (entire body as a dict literal
  + assert "Retry-After" not in resp.headers).
- M-14 log analog: main.py:620 `_auth_log.warning("process_key.auth.token_mismatch",
  path=request.url.path)`; `_auth_log` defined main.py:561. main.py is OUTSIDE the mypy gate's
  path list — no cast risk there.
- Plan 01 (landed, wave 1) added the 06(b) membership guard: service_error(500, ...,
  dependency="binance") now raises ValueError at construction — B1's old shape is unmakeable.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: B1 — internal.py S-11: 424 → 500 retryable:false, dependency dropped, logger upgraded; S-11 test rewritten</name>
  <files>analytics-service/routers/internal.py, analytics-service/docs/STATUS_CONTRACT.md, analytics-service/tests/test_status_contract_exchange_internal.py</files>
  <read_first>
    - analytics-service/routers/internal.py:405-460 (the S-11 arm :414-433 and the sibling logger.error at :441-444)
    - analytics-service/tests/test_status_contract_exchange_internal.py (locate the S-11 424 oracle — search "s11"; and the :302-328 template)
    - analytics-service/docs/STATUS_CONTRACT.md (the S-table row for S-11; §2 class table)
  </read_first>
  <behavior>
    RED first: rewrite the S-11 test to the NEW contract before touching the router — 500 (literal),
    body retryable is False, dependency key ABSENT or None, "Retry-After" not in headers, detail a
    scalar string naming no venue and no credential, code == "ADAPTER_INIT_FAILED" (literal in the
    test). Pin the ENTIRE body as a dict literal minus dynamic keys (the :302-328 template).
    Observe RED against the shipped 424. Then fix the router; observe GREEN.
  </behavior>
  <action>
    In internal.py, replace the except Exception arm (:418-433): raise service_error(500,
    "ADAPTER_INIT_FAILED", retryable=False, detail a scalar string of the S-06b flavor
    ("Something went wrong on our side while opening this connection. Nothing is wrong with your
    key.") — NO dependency kwarg (a 500 naming a venue now raises, plan 01's guard; that is the
    point), NO Retry-After. Code decision (RESEARCH OQ-2, taken here): "ADAPTER_INIT_FAILED" —
    it names OUR adapter construction, not the venue; "EXCHANGE_INIT_FAILED" semantically names
    the venue and contradicts a SERVICE-PERMANENT attribution. Keep the except ValueError → 400
    arm untouched (unsupported exchange name IS caller input). Upgrade logger.warning (:423) to
    logger.error with the :441-444 sibling's shape (key/exchange identifiers, no exception text
    into any client body) — a 500 is page-worthy; WARNING undersells it.
    STATUS_CONTRACT.md: update the S-11 row (424 → 500, code ADAPTER_INIT_FAILED) and register
    the code so 140.2's discriminator map has it. Note in the row that the change is deliberate
    (H-2): create_exchange performs no network I/O.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_status_contract_exchange_internal.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - S-11 test asserts 500 + retryable False + no dependency + no Retry-After header + code "ADAPTER_INIT_FAILED", all literals; observed RED against the 424 first (paste counts).
    - `grep -n "dependency" analytics-service/routers/internal.py` shows no dependency kwarg in the rewritten arm (the six legit kek sites at :276/:292 untouched).
    - `grep -c "logger.error" analytics-service/routers/internal.py` increased by exactly 1; logger.warning gone from the S-11 arm.
    - except ValueError → 400 arm byte-unchanged.
  </acceptance_criteria>
  <done>S-11 answers 500 retryable:false with no venue named; the old shape is both fixed and unconstructable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: B2 + B3 — the two 400-blaming sites, with NEW tests (none exist)</name>
  <files>analytics-service/routers/exchange.py, analytics-service/routers/portfolio.py, analytics-service/tests/test_create_exchange_attribution.py</files>
  <read_first>
    - analytics-service/routers/exchange.py:440-460 (B2: the except arm :448-454) and :487-495 (the S-06b emission analog in the SAME file)
    - analytics-service/routers/portfolio.py:2260-2320 (B3 :2265-2277 AND the already-correct B4 at :2315-2316 — do not touch B4; cite it in the test file's docstring as the in-function proof)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md §3 (the 11-site candidate table — B5-B8 rejected with reasons; do not "find" them again)
  </read_first>
  <behavior>
    RED first in new tests/test_create_exchange_attribution.py: for each of B2 and B3, drive the
    route with create_exchange patched to raise TypeError (a non-ValueError, our-bug class) and
    assert 500 (literal) + retryable False + scalar detail + no dependency + no Retry-After;
    and with ValueError and assert 400 stays. Grep-proof in the file docstring: no prior test
    covered either site ('Failed to initiali[sz]e exchange connection' had zero test hits).
    Observe RED (both currently answer 400), then fix, observe GREEN.
  </behavior>
  <action>
    B2 (exchange.py:448-454) and B3 (portfolio.py:2269-2277): replace the
    HTTPException(400, "Failed to initialize/initialise exchange connection") in each except
    Exception arm with raise service_error(500, "ADAPTER_INIT_FAILED", retryable=False, detail
    the same scalar copy as B1) — one code, one copy, three sites, zero drift. Preserve each
    site's existing logger.exception call (keep the diagnostics; they already log). If either
    site lacks a ValueError → 400 arm for the unsupported-exchange case, add it ONLY if
    create_exchange's ValueError can actually reach that except (read the call: if the exchange
    name was validated upstream, do not add dead arms — record the finding instead).
    TS-consequence note for the SUMMARY (not a code change): these 500s carry an object
    body.detail, which analytics-client.ts renders as the generic dead-end — CORRECT for an
    our-bug 500 (unlike M-4's venue-transient case); TS-05 will sharpen rendering later.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_create_exchange_attribution.py tests/test_status_contract_exchange_internal.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - B2 and B3 each: TypeError-escape → 500 retryable:false (observed RED first); ValueError → 400 unchanged.
    - Site count check: `grep -rn "ADAPTER_INIT_FAILED" analytics-service/routers/ | wc -l` ≥ 3 (internal.py, exchange.py, portfolio.py — the whole class, one code).
    - portfolio.py:2316's B4 arm (500 "Strategy verification failed") byte-unchanged.
    - "Failed to initialize exchange connection" AND "Failed to initialise exchange connection" both have 0 remaining production hits: `grep -rn "Failed to initiali" analytics-service/routers/` → 0.
  </acceptance_criteria>
  <done>The class is closed at 3/3 — not the review's 1 or RESEARCH's 2; the British-spelling member included.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: M-14 — the two missing log companions in main.py + wave gate</name>
  <files>analytics-service/main.py, analytics-service/tests/test_service_key_log_companion.py</files>
  <read_first>
    - analytics-service/main.py:684-712 (verify_service_key: the :690 unset arm, the :698-707 comment on why an absent header must stay silent, the :710 mismatched arm inside `if provided:`)
    - analytics-service/main.py:561 (_auth_log) and :594, :620 (the sibling log lines whose idiom to copy)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md §7 (the 5-site table — 3 of 5 have companions, not 4 of 5)
  </read_first>
  <behavior>
    RED first in new tests/test_service_key_log_companion.py, with a structlog capture fixture:
    (1) request with a WRONG service key → exactly 1 "service_key.mismatch" event;
    (2) request with NO service-key header → 0 service_key.* events (the absent-header case is
    internet background noise — logging it makes the signal meaningless, per the :698-707 comment);
    (3) SERVICE_KEY unset (monkeypatched env) → 1 "service_key.secret_unset" event.
  </behavior>
  <action>
    Two one-line additions in main.py, each immediately before its _capture_secret_misconfig call:
    at :690 (unset): `_auth_log.error("service_key.secret_unset", path=request.url.path)` —
    error matches :594's severity for the unset fault; at :710 (mismatched): `_auth_log.warning(
    "service_key.mismatch", path=request.url.path)` — warning matches :620's mismatched sibling,
    and it MUST stay inside the `if provided:` block. No other main.py change (main.py is outside
    the mypy gate paths — no cast needed). SENTRY_DSN is set on Railway, so this is hygiene, not
    a blind spot — say so in the SUMMARY.
    Then run the wave-2 gate: full pytest + mypy.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_service_key_log_companion.py -q && python3 -m pytest -q 2>&1 | tail -3 && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <acceptance_criteria>
    - Wrong key → exactly 1 mismatch event; absent header → exactly 0 events (both asserted; the 0-case is the one that catches a line escaping the `if provided:` block — RESEARCH Validation row M-14).
    - `grep -c "_capture_secret_misconfig" analytics-service/main.py` unchanged (5); `grep -c "service_key.mismatch\|service_key.secret_unset" analytics-service/main.py` == 2.
    - Full analytics-service pytest green; mypy "Success: no issues found in 89 source files"; 0 new `# type: ignore` anywhere in the plan's diff.
  </acceptance_criteria>
  <done>All 5 _capture_secret_misconfig sites now pair with a log companion (was 3 of 5); wave-2 gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → internal/exchange/portfolio routes | attribution verdicts cross to callers and to the 140.2 breaker |
| Vercel → Railway service auth | SERVICE_KEY verification, the M-14 log surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1.1-10 | Repudiation | create_exchange failures as 424/400 | mitigate | 500 retryable:false at all 3 sites — our bugs count and page instead of hiding behind a tidy 4xx (H-2's "nobody is ever paged") |
| T-140.1.1-11 | Information disclosure | exception text in client bodies | mitigate | scalar fixed copy at all 3 sites; diagnostics stay in logger.error/exception, never the body |
| T-140.1.1-12 | Repudiation | secret-misconfig invisible without Sentry | mitigate | M-14 log companions at both silent sites; absent-header stays silent so the signal keeps meaning |
| T-140.1.1-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- 3/3 class members emit 500 retryable:false with one shared code; B4 and the B5-B8 non-members untouched.
- S-11 test rewritten to the whole-body-literal template; B2/B3 covered for the first time.
- M-14: 2 log lines, 3 structlog assertions including the 0-event case.
- Full pytest + mypy green (wave-2 gate encoded).
</verification>

<success_criteria>
ROADMAP criterion 3 observably true (RESEARCH Validation row 3: no dependency venue name in any
create_exchange-class body, no Retry-After, no 424/400 anywhere in the class). PYAPIFIX-03 closed
at 3/3 sites; M-14 closed at 2/2.
</success_criteria>

<output>
Create `.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-04-SUMMARY.md` when done.
Record: RED-first evidence per site, the ADAPTER_INIT_FAILED code decision (OQ-2), the B3 British-spelling find, the TS-consequence note.
</output>
