---
phase: 153.3-wizform-gw-mt5-gateway-honesty
plan: 05
type: execute
wave: 5
depends_on: ["153.3-04"]
files_modified:
  - analytics-service/services/mt5_client.py
  - analytics-service/routers/exchange.py
  - docs/runbooks/mt5-go-live.md
  - analytics-service/tests/test_mt5_client_contract.py
  - analytics-service/tests/test_mt5_validate.py
autonomous: true
requirements: [WIZFORM-05]
user_setup: []

must_haves:
  truths:
    - "Every MT5 stage emits stage + duration_ms on BOTH success and failure — the first real timing this system has ever had"
    - "The lease wait is timed SEPARATELY from the operation, so a queue wait is never mistaken for broker latency"
    - "No credential value reaches any instrumentation event"
    - "The runbook states the single-replica invariant as a CORRECTNESS constraint, not a capacity knob"
    - "The runbook carries the operator remedy for the terminal-level trade disable that D-31 now refuses on"
    - "The budget is recorded as PROVISIONAL with Phase 155 named as the owner that tightens it from this data"
  artifacts:
    - path: "analytics-service/services/mt5_client.py"
      provides: "Per-stage perf_counter brackets emitting stage + duration_ms"
      contains: "duration_ms"
    - path: "analytics-service/routers/exchange.py"
      provides: "Lease-wait timing + a per-validate outcome event"
      contains: "duration_ms"
    - path: "docs/runbooks/mt5-go-live.md"
      provides: "The single-replica invariant (D-33), the terminal trade-permission step (D-31), the provisional-budget note (D-27)"
      contains: "single replica"
  key_links:
    - from: "Mt5Client stage brackets"
      to: "structlog event with stage + duration_ms"
      via: "time.perf_counter around each MT5 call, emitted on success AND failure"
      pattern: "perf_counter"
    - from: "docs/runbooks/mt5-go-live.md"
      to: "the one-session invariant"
      via: "a stated correctness constraint at stand-up and at gate-check"
      pattern: "single replica"
---

<objective>
**D-32 + D-27 + D-33 — instrument before tuning, then write down what ops must never break.**

`mt5_client.py` (483 lines) and `routers/exchange.py:222-470` contain **zero** timing —
verified: no `time.monotonic`, no `perf_counter`, no elapsed capture around `initialize()`,
`login()`, `account_info()`, `history_deals_get()`, `order_check()`, `close()` or `restart()`
(`153-EVIDENCE-mt5-latency.md` §2). That is precisely why **no uncensored measurement of a
successful MT5 validation exists anywhere** — not in Railway logs, not in Sentry, not in
`docs/evidence/`. The only clock in the system is the timeout itself.

⛔ **D-27: the current numbers are PROVISIONAL and must be labelled so.** Do not cite "35–70 s"
from `153-UI-SPEC.md:226` as evidence — that line is derived from the timeout constants and says
so itself. This plan produces the p50/p95; **Phase 155 (MT5-VERIFY)**, already live-gated, is what
acts on it. Tightening the budget here is out of scope.

**D-33 — pin the gateway to a SINGLE replica.** Ops, not code: the one-session invariant is false
the moment a second replica exists, because every rpyc connection binds the same process-global
`MetaTrader5` module and the serializing lock is a per-event-loop `asyncio.Lock`
(`153-EVIDENCE-mt5-platform.md` §A2, `mt5_concurrency.py:116-125`). A scale-up is a
**correctness** change, not a capacity change. Record it in the runbook.

Purpose: make the next budget conversation start from data instead of from zero.
Output: per-stage timing on both outcomes, a separately-timed queue wait, and three runbook
entries that stop an operator silently invalidating this sub-phase's work.

⚠️ **Instrumentation lands LAST deliberately** — it wraps the FINAL stage decomposition (post
end-to-end deadline, post lease), so what it measures is what production will run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153.3-wizform-gw-mt5-gateway-honesty/153.3-CONTEXT.md
@.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-EVIDENCE-mt5-latency.md
@.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-EVIDENCE-mt5-platform.md
@.planning/phases/153.3-wizform-gw-mt5-gateway-honesty/153.3-04-SUMMARY.md
@AGENTS.md
@CLAUDE.md

<interfaces>
<!-- Contracts extracted this session. Do not go exploring. -->

FROM `analytics-service/routers/process_key.py` — ⭐ **the in-repo `duration_ms` analog to mirror**:
- `import structlog` (`:38`), `import time` (`:35`)
- `duration_ms = int((time.monotonic() - started_at) * 1000)` (`:775`)
- `log.info("process_key.validate_only_ok", duration_ms=duration_ms)` (`:785`) and
  `log.info("process_key.validate_only_failed", error_code=val.error_code, duration_ms=duration_ms)` (`:777-781`)
- ⚠️ EVIDENCE §2 records that this timer **is never reached in production for MT5** — the wizard's MT5 arm goes to `/validate-key`, which has no timer. That is the gap this plan closes.

FROM `analytics-service/services/logging_config.py` — structlog is already wired process-wide; `services/exchange.py`, `services/redact.py`, `services/audit.py` and `services/rate_limit.py` all use it. `services/mt5_client.py` currently uses `logging.getLogger("quantalyze.analytics")` (`:73`).

FROM `analytics-service/services/mt5_client.py` — the call sites to bracket (line numbers as of HEAD; re-derive before editing, they move with plans 01–03):
- `initialize()` (`:305`), `login()` (`:313`), `account_info()` (`:329`), `history_deals_get()` (`:350`), `order_check()` (`:371`), `close()` (`:384`), `restart()` (`:428`), plus `terminal_info()` (added by plan 153.3-01) and `release()` (added by plan 153.3-03)
- `_guarded_read` (`:262-279`) — the ONE wrapper every non-login read already passes through
- secret hygiene (`:50-56`): the login / investor password / broker server NEVER appear in any exception or log surface; `Mt5Session`'s three credential fields are `repr=False` (`:474-483`)

FROM `analytics-service/routers/exchange.py` (post plans 03 + 04): `_MT5_LEASE_WAIT_S`, `_MT5_VALIDATE_DEADLINE_S`, `_MT5_VALIDATE_STAGE_TIMEOUT_S`, `_MT5_RELEASE_TIMEOUT_S`, and the lease `async with`.

FROM `docs/runbooks/mt5-go-live.md` (304 lines) — the section structure to extend:
- `## Step 0 — HOST DECISION + the A2 dual-stack check` (`:41`)
- `## Step 1 — STAND-UP (MT5GW-01)` (`:74`)
- `## Step 2 — ONE-TIME VNC INSTALL + INVESTOR LOGIN (MT5GW-01)` (`:115`) ← the only step with a human in the terminal GUI
- `## Step 4 — SOAK (MT5GOLIVE-02)` (`:159`)
- `## Step 5 — GATE-CHECK (explicit, never assumed)` (`:186`)
- `## Step 6 — FLIP` (`:209`), `## Step 7 — PROD VERIFY` (`:249`), `## Step 8 — ROLLBACK` (`:269`)
- the Live-Update hazard is already flagged at `:93-98`

FROM `153-EVIDENCE-mt5-latency.md` §6 Step 4 — the explicit instruction this plan implements:
*"Add `time.perf_counter()` brackets inside `Mt5Client` around `initialize`, `login`, `account_info`, `order_check`, `shutdown`, and emit them as a structlog event with `stage` + `duration_ms` on **both** success and failure. Also time the lock wait … separately from the read."*
</interfaces>

⛔ **ZERO TypeScript files.** ⛔ **`MT5_REQUEST_TIMEOUT_S` byte-unchanged.** ⛔ **No account cap.**
⛔ **Do not tune any timeout in this plan** — measuring and tuning in the same change destroys the
before/after (D-27; Phase 155 owns the tuning).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-stage timing inside Mt5Client — on success AND on failure</name>
  <read_first>
    - `analytics-service/services/mt5_client.py` — the whole file at its current state (plans 01–03 have modified it); especially `_guarded_read` (`:262-279`), `_raise_last` (`:235-260`), `login` (`:281-325`), and the module docstring's Secret-hygiene block (`:50-56`)
    - `analytics-service/routers/process_key.py:749-786` — the `duration_ms` emission analog
    - `.planning/phases/153-.../153-EVIDENCE-mt5-latency.md` §2 (what is NOT instrumented) and §6 Step 4 (what to add)
  </read_first>
  <files>analytics-service/services/mt5_client.py, analytics-service/tests/test_mt5_client_contract.py</files>
  <action>
Add a single private timing helper on `Mt5Client` — one place, not a bracket copy-pasted at nine
call sites — that runs a callable, measures `time.perf_counter()` around it, and emits a structlog
event carrying at minimum `stage` (the MT5 call name) and `duration_ms` (an `int`), **on both the
success and the exception path**, re-raising the exception unchanged afterwards. Failure events
must also carry an outcome discriminator (e.g. `ok=False` plus the exception CLASS NAME) so a
30 s censor is separable from a fast error in a Railway query.

Route it through structlog (`structlog.get_logger(...)`), mirroring
`process_key.py:785`'s `log.info("<event>", duration_ms=…)` shape so the fields are queryable
rather than embedded in a message string. Keep the module's existing stdlib `logger` for the
warnings that already use it — do not rewrite those lines.

Apply the helper to every MT5 round-trip in the client: `initialize`, `login`, `account_info`,
`terminal_info`, `history_deals_get`, `order_check`, `close`, `release`, `restart`. Compose it
with `_guarded_read` rather than around it, so a transport raise is still converted to a
scrubbed typed `Mt5ClientError` exactly as today — ⛔ the timing must not change what any caller
observes, only add an emission. Also time the `_raise_last()` round-trip: it is itself a remote
call and EVIDENCE §1b shows a failed attempt paying a second full 30 s after the verdict.

🔒 **Secret hygiene is a hard constraint, not a nicety.** The event may carry only: the stage
name, `duration_ms`, an ok/failed flag, the exception class name, and `terminal_key`
(host:port — infrastructure identity, already a public-to-us value used as the lock key). ⛔
NEVER the login number, the investor password, the broker server, `last_error()` text, or the
interpolated remote `code` string. The module docstring (`:50-56`) records why: `mt5linux`
f-string-interpolates the password into the remotely-eval'd code, so leaked error text is a real
credential disclosure.

Add a `stage` naming convention comment so the names are stable enough to aggregate on across
deploys, and state that Phase 155 reads them.

Tests:
  - a successful read emits an event carrying `stage` and a non-negative integer `duration_ms`.
  - ⭐ a FAILING read (transport raise) emits an event too, and the `Mt5ClientError` still
    propagates unchanged with the same code and the same scrubbed message. This is the assertion
    that catches instrumentation swallowing an error.
  - every one of the nine stages emits — assert by driving each method against the double and
    collecting the emitted `stage` values, then comparing to a hand-typed expected set. ⛔ Do not
    derive the expected set from the production module: an oracle that reads its expectation out
    of the thing under test cannot fail.
  - ⭐ the secret sweep: drive a login with `login=123456`, `password="s3cr3t-pw"`,
    `server="MyBroker-Live"`, then assert none of those three literals appears in the repr of ANY
    captured structlog event. Clone the sweep shape at `test_mt5_validate.py:507-513`.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_mt5_client_contract.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "perf_counter" analytics-service/services/mt5_client.py` returns ≥ 1.
    - A test asserts a failing stage emits its event AND re-raises the identical `Mt5ClientError` code.
    - A test compares the emitted `stage` set against a hand-typed set of nine names.
    - A test asserts no credential literal appears in any captured event.
    - `cd analytics-service && python3 -m pytest tests/ -k mt5 -x -q` exits 0.
    - `cd analytics-service && python3 -m mypy --strict services/mt5_client.py` reports no error.
  </acceptance_criteria>
  <done>Every MT5 round-trip is timed on both outcomes; no caller-visible behaviour changed; no credential can reach an event.</done>
</task>

<task type="auto">
  <name>Task 2: Time the lease wait separately from the operation, and emit one honest per-validate outcome</name>
  <read_first>
    - `analytics-service/routers/exchange.py:222-470` — `_validate_mt5_key` as restructured by plans 153.3-03 and 153.3-04: the lease `async with`, the end-to-end deadline, the stages, the `finally` release
    - `.planning/phases/153-.../153-EVIDENCE-mt5-latency.md` §4 *"Consequence for budgeting"* — a budget derived from a single-user T is wrong for user #2 by construction, which is why the wait must be measured apart from the read
  </read_first>
  <files>analytics-service/routers/exchange.py, analytics-service/tests/test_mt5_validate.py</files>
  <action>
Bracket the lease acquisition with its own `perf_counter` and emit a `stage="lease_wait"` event
with `duration_ms`, on BOTH outcomes — acquired, and refused via `Mt5TerminalBusyError`. ⭐ This
is the measurement nothing in the system has: the queue wait, separate from the terminal read. A
budget sized off a combined number is wrong for the second concurrent user by construction.

Bracket the whole `_validate_mt5_key` call and emit one terminal outcome event with
`duration_ms` and a categorical outcome — one of the verdicts the function can actually produce
(`read_only`, `master_rejected`, `undetermined`, `auth`, `wrong_server`, `transient`,
`deadline_exceeded`, `lease_busy`, `gateway_unreachable`, `gateway_unconfigured`). Use the
categories the code already branches on; do not invent a taxonomy the branches cannot produce.
Emit it on EVERY path — put the emission where the release already lives so the "runs on every
path" property is inherited rather than re-derived.

🔒 Same hygiene bar as Task 1: stage, duration_ms, outcome, terminal_key. ⛔ No login, password,
broker server, `last_error()` text or exception message. The existing sweep at
`test_mt5_validate.py:507-513` must be extended to cover the structlog events too, not only the
stdlib `logger` calls it checks today.

Tests:
  - the happy path emits a `lease_wait` event and a terminal outcome event with
    `outcome="read_only"`.
  - a validate refused by the lease bound emits `lease_wait` with `ok=False` and a terminal
    outcome event with `outcome="lease_busy"` — and the `lease_wait` duration is measured, not
    absent.
  - ⭐ a validate that hits the end-to-end deadline still emits its terminal outcome event
    (`outcome="deadline_exceeded"`). An instrumentation that only fires on success measures a
    population that excludes exactly the failures we are trying to explain.
  - the credential sweep, extended over structlog events.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_mt5_validate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "perf_counter" analytics-service/routers/exchange.py` returns ≥ 2 (lease wait + whole-validate).
    - A test asserts a `lease_wait` event with a `duration_ms` on both the acquired and the refused path.
    - A test asserts a terminal outcome event fires when the end-to-end deadline fires.
    - The extended credential sweep covers structlog events and passes.
    - `cd analytics-service && python3 -m pytest tests/ -k mt5 -x -q` exits 0.
    - `cd analytics-service && python3 -m mypy --strict routers/exchange.py` reports no error.
  </acceptance_criteria>
  <done>Queue wait and terminal work are separately measurable; every validate terminates in exactly one categorical, credential-free outcome event.</done>
</task>

<task type="auto">
  <name>Task 3: Runbook — the single-replica invariant, the terminal trade-permission step, and the provisional budget</name>
  <read_first>
    - `docs/runbooks/mt5-go-live.md` — the whole file (304 lines), especially `## Step 1 — STAND-UP` (`:74`), `## Step 2 — ONE-TIME VNC INSTALL + INVESTOR LOGIN` (`:115`), `## Step 4 — SOAK` (`:159`), `## Step 5 — GATE-CHECK` (`:186`), and the Live-Update hazard at `:93-98` (the existing register of "an operator action can invalidate this")
    - `.planning/phases/153-.../153-EVIDENCE-mt5-platform.md` §A2 (one `ThreadedServer` process, one MT5 module, one IPC pipe), §C12 (the terminal option), Correction **C-5**
    - `analytics-service/services/mt5_concurrency.py:116-125` — the single-event-loop scope statement, as corrected by plan 153.3-04
  </read_first>
  <files>docs/runbooks/mt5-go-live.md</files>
  <action>
Three additions. Match the file's existing register — imperative, numbered, with an explicit
gate-check line for each — and do not restructure the document.

**(a) D-33 — the single-replica invariant.** Add it to `## Step 1 — STAND-UP` as a **stated
invariant**, not a setting: the mt5-gateway service and the analytics service that talks to it
run at **exactly one replica each**, and scaling either is a **CORRECTNESS change, not a capacity
change**. Give the mechanism so a future operator can evaluate it rather than obey it:
`mt5linux` serves every rpyc connection from ONE `ThreadedServer` process sharing one
`MetaTrader5` module instance, so there is one IPC pipe and one logged-in account per gateway
(EVIDENCE §A2); and the serializing terminal lease is a per-event-loop `asyncio.Lock`
(`mt5_concurrency.py`), so it serializes nothing across replicas. A second replica therefore
reintroduces exactly the interleaved-`login()` race the lease was added to remove.
Add the matching line to `## Step 5 — GATE-CHECK`: replica count verified = 1 for both services,
recorded with the value, never assumed. State that raising it requires a durable cross-process
serializer first, and that wanting it is a `TODOS.md` item, not a knob.

**(b) D-31 — the terminal trade-permission step.** Add to `## Step 2 — ONE-TIME VNC INSTALL +
INVESTOR LOGIN`, which is the only step with a human in the terminal GUI. State plainly:
MetaTrader's option *"Disable automatic trading through the external Python API"* is **ON by
default, for security reasons** (MetaQuotes), and while it is on, `order_check` from Python is
rejected regardless of investor vs master — so a MASTER password is indistinguishable from an
investor password. As of this phase the service **REFUSES** a key it cannot classify rather than
stamping it read-only (D-31), which means **every MT5 validation fails while the option is on**.
Give the operator the remedy (turn the option off in the terminal's options, leaving the account
itself in investor mode), the verification (`terminal_info()` reports `connected: true` and
`trade_allowed: true`), and the failure signature to expect if it is missed: a permanent
`MT5_GATEWAY_UNCONFIGURED` refusal with `retryable: false`, never an accusation of the user's
credentials. Add the matching gate-check line in `## Step 5`.

**(c) D-27 — the budget is provisional.** Add a short subsection near `## Step 4 — SOAK` (which
is where numbers are collected) recording: the validate chain this phase installed —
initialize/login IPC 45 000 ms, rpyc 55 s, stage 60 s, end-to-end deadline 75 s, lease wait 20 s,
release 10 s — is **provisional**, derived from vendor defaults and the D-26 client ceiling, NOT
from measurement. ⛔ State explicitly that **no uncensored measurement of a successful MT5
validation existed when these numbers were chosen**, and that the "35–70 s" figure circulating in
the planning documents is a derivation from the old constants, not an observation. Name the
telemetry this phase added (`stage` + `duration_ms` on every MT5 call, plus `lease_wait` and the
per-validate outcome event) as the source the real p50/p95 will come from, and name **Phase 155
(MT5-VERIFY)** as the owner that tightens the numbers from it. Record the invariant any future
change must preserve: the server end-to-end worst case (lease wait + deadline + release) stays
strictly under the client budget, which is Phase 153.4's `120_000` ms.
  </action>
  <verify>
    <automated>grep -c "single replica" docs/runbooks/mt5-go-live.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -in "single replica" docs/runbooks/mt5-go-live.md` returns lines in BOTH the stand-up section and the gate-check section.
    - `grep -in "external Python API" docs/runbooks/mt5-go-live.md` returns at least one line inside Step 2.
    - `grep -in "provisional" docs/runbooks/mt5-go-live.md` returns a line, and `grep -in "155" docs/runbooks/mt5-go-live.md` names Phase 155 as the owner of the tightening.
    - The document contains the six chain values (45 000 / 55 / 60 / 75 / 20 / 10) and the `120_000` ms ceiling, so an operator can check the arithmetic without reading the code.
    - `npm run lint` is unaffected (markdown-only change); no code file is touched by this task.
  </acceptance_criteria>
  <done>An operator can no longer silently invalidate the one-session invariant, cannot miss the terminal option that now blocks every validation, and cannot mistake the provisional numbers for measurements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 call values (login / password / broker server) → log and telemetry sinks | Railway logs and Sentry are a disclosure surface |
| operator action (scale-up, terminal option) → the one-session invariant | an ops change can silently break a correctness property |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.3-23 | Information Disclosure | new per-stage telemetry events | **mitigate** | Events carry ONLY `stage`, `duration_ms`, an ok flag, the exception CLASS name and `terminal_key`. ⛔ No credential, no `last_error()` text, no interpolated remote code string. A credential sweep test asserts the absence across all captured events, extending the existing sweep at `test_mt5_validate.py:507-513`. |
| T-153.3-24 | Tampering | instrumentation changing caller-visible behaviour | **mitigate** | The timing helper re-raises the original exception unchanged and composes with `_guarded_read` rather than replacing it; a test asserts the same error code and the same scrubbed message survive. |
| T-153.3-25 | Denial of Service | a second gateway/analytics replica silently reopening the interleaved-`login()` race | **mitigate (documented control)** | D-33: the runbook states the single-replica invariant as a correctness constraint with its mechanism, plus an explicit gate-check line recording the verified replica count. |
| T-153.3-26 | Elevation of Privilege | an operator leaving the terminal's external-Python trade disable ON | **mitigate (documented control)** | Runbook Step 2 names the option, its default-ON status, its effect (a master password indistinguishable from an investor password) and the refusal signature D-31 now produces, plus a gate-check line. The code already fails CLOSED (plan 153.3-01); the runbook stops the failure being misdiagnosed. |
| T-153.3-27 | Repudiation | tuning the budget off the same run that first measured it | **mitigate** | D-27: this plan measures and does not tune; the runbook records the numbers as provisional and names Phase 155 as the owner of the tightening. |
| T-153.3-SC | Tampering | npm/pip/cargo installs | **mitigate** | **No package is installed by this plan** — `structlog` and `time` are already dependencies in use across `analytics-service/`. Zero dependency changes; no legitimacy gate applies. |
</threat_model>

<verification>
```
cd analytics-service && python3 -m pytest tests/ -k mt5 -x -q
cd analytics-service && python3 -m pytest -x -q
cd analytics-service && python3 -m mypy --strict services/ routers/
grep -in "single replica\|external Python API\|provisional" docs/runbooks/mt5-go-live.md
```
⚠️ pytest MUST run from `analytics-service/`.
⚠️ Run `mypy --strict` before considering the phase done — the milestone gate runs pytest only,
so mypy errors stay latent until PR CI. Fix via `cast()`, never `# type: ignore`.
⚠️ Never run the python and e2e-seeded suites concurrently against the shared TEST DB.
**What remains unverifiable offline:** the actual p50/p95 the new telemetry will report. CI never
installs `mt5linux`, so every stage above is timed against the injected double — the tests prove
the events FIRE and carry no secret, not what the numbers ARE. Producing real numbers is
**Phase 155 (MT5-VERIFY)**, live-gated on a trading day.
</verification>

<success_criteria>
- Every MT5 round-trip and the lease wait emit `stage` + `duration_ms` on both success and failure.
- Every validate terminates in exactly one categorical outcome event, including the deadline path.
- No credential literal can reach any event; a test proves it.
- No caller-visible behaviour changed and no timeout was tuned.
- The runbook states the single-replica invariant with its mechanism and a gate-check line; carries the terminal trade-permission step; and labels the chain provisional with Phase 155 named.
- `MT5_REQUEST_TIMEOUT_S` byte-unchanged; zero TypeScript files; no account cap.
</success_criteria>

<output>
Create `.planning/phases/153.3-wizform-gw-mt5-gateway-honesty/153.3-05-SUMMARY.md` when done.
</output>
