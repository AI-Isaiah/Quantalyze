# Phase 153.3: WIZFORM-GW — MT5 gateway honesty (Python; file-disjoint) - Context

**Gathered:** 2026-08-08
**Status:** Ready for planning
**Source:** Inherited from the Phase 153 split (founder-approved 2026-08-08)

<domain>
## Phase Boundary

**Delivers.** An MT5 validation's honest verdict can *physically arrive* — the nested-timeout
inversion is removed, the terminal is shared through the lease that already exists instead of
raced, and a key we cannot classify is refused rather than stamped read-only.

Concretely:

- `initialize()`'s missing `timeout=` (vendor default **60 000 ms** inside an rpyc
  `sync_request_timeout` of **30 s**) is fixed, and the ordering guard at
  `mt5_client.py:213-219` is extended to cover **every** MT5 call carrying its own timeout,
  not just `login()` (D-24).
- ONE bounded end-to-end probe deadline replaces three independent `_MT5_PROBE_TIMEOUT_S`
  stages, while preserving what the `+ 5.0` actually is — a diagnostic-ordering margin, not
  a budget (D-02, D-03).
- The worker's module constant `MT5_REQUEST_TIMEOUT_S = 30` stays **byte-unchanged**; the
  validate path takes its own longer chain via the existing `request_timeout_s` ctor arg
  (D-25).
- `routers/exchange.py` takes `_mt5_terminal_lock_for` — the lease it is the only caller to
  skip — with a **bounded acquisition timeout distinct from the operation timeout** (D-29).
- `shutdown()` comes **out** of the request path (D-30), which supersedes D-28's
  "bound `close()` separately".
- `is_trade_capable` reads `terminal_info()` and **fails CLOSED**; `10017
  TRADE_RETCODE_TRADE_DISABLED` stops being untested (D-31) — a **security** fix.
- Per-stage `stage` + `duration_ms` instrumentation is added so the budget can later be
  tightened from data rather than judgement (D-27, D-32).
- The single-replica gateway invariant is recorded in the runbook (D-33).

**Requirements:** WIZFORM-05 (**server leg**).

**Depends on:** nothing — file-disjoint from the TypeScript chain and may run in parallel
with 153.1/153.2. Sequenced **BEFORE** 153.4 per D-24: a budget cannot fix a structurally
censored verdict.

**Owns:** `analytics-service/services/mt5_client.py`, `services/mt5_validation.py`,
`services/mt5_concurrency.py`, `routers/exchange.py`, `tests/test_mt5_*.py`,
`docs/runbooks/mt5-go-live.md`.

**Does NOT deliver — and who owns each excluded item:**

- Any TypeScript change at all. The client `validate-key` budget, `budgetKeyFor`,
  `BREAKER_LOCK_TOMBSTONE_S`, the 16 pin sites, the long-wait card and `Stop waiting` —
  **Phase 153.4** (WIZFORM-BUDGET). D-29's "queueing is surfaced honestly" states the
  *requirement* the server places on the UI; **153.4 builds that UI**.
- Honest codes, the derived roster, `VENUE_CAPABILITIES`, `MIN_DESCRIPTION_CHARS` —
  **Phase 153.1** (WIZFORM-CODES).
- Inline field validation, the submit-path probe gate, the MT5 chip set and preselect —
  **Phase 153.2** (WIZFORM-FIELD). D-14(a) leans on `_validate_mt5_key` already proving
  read-only; this sub-phase *hardens* that proof (D-31) but does not touch the TS gate.
- Live MT5 numeric verification, and **tightening the budget from the new instrumentation**
  — **Phase 155** (MT5-VERIFY), which is already live-gated. This sub-phase makes the
  number measurable; it does not set the final one.

</domain>

<decisions>
## Implementation Decisions

### Timeouts and budgets — the server leg (founder-locked, do not re-derive)

- **D-02: The `+ 5.0` in `_MT5_PROBE_TIMEOUT_S = MT5_REQUEST_TIMEOUT_S + 5.0`
  (`routers/exchange.py:62`) is NOT a deadline budget and must not be treated as one.**
  It is a margin over the inner rpyc `sync_request_timeout` (`mt5_client.py:82`, default
  30s) so the inner client timeout fires first and yields a real MT5 error rather than a
  bare `asyncio.TimeoutError` with no venue detail. It is a diagnostic-ordering device
  between two nested timeouts.
- **D-03: The server gets ONE bounded end-to-end probe deadline, replacing three
  independent 35s stages.** Today `_MT5_PROBE_TIMEOUT_S` is applied separately at
  `routers/exchange.py:328` (connect/login), `:380` (probe) and `:456` (close), so the
  honest worst case is ~105s **before** the single-terminal MT5 lock queue wait.

- **D-24: ⭐ THE ROOT CAUSE IS NOT THE CLIENT BUDGET — it is an unguarded nested-timeout
  inversion inside Python.** Measured evidence (`scratchpad/mt5-latency-evidence.md`,
  9/9 correlated gateway↔worker failures on 2026-08-06/08) clusters at **30.1–31.0 s**,
  i.e. exactly the rpyc bound — not a broker-latency spread. Cause, verified at source:
  `mt5_client.py:306` calls `self._mt5.initialize()` **with no `timeout=`**, so MetaTrader5's
  vendor default of **60 000 ms** applies, *inside* an rpyc `sync_request_timeout` of
  **30 s** (`MT5_REQUEST_TIMEOUT_S`, `mt5_client.py:82`). The first MT5 call is therefore
  **structurally incapable** of returning a verdict before rpyc gives up. No client-side
  budget — 90 s, 120 s or 10 min — can fix this; it only moves where the pile-up happens.
  **Fix the inversion first.**
  ⚠️ This is the *same* defect class the codebase already knows about: the ordering guard
  at `mt5_client.py:213-219` fails loud on `MT5_LOGIN_TIMEOUT_MS >= request_timeout_s*1000`
  — it covers `login()`, the instance the author had in mind, and **not `initialize()`**,
  the second member. Extend the guard to cover every MT5 call carrying its own timeout.

- **D-25: the worker's `MT5_REQUEST_TIMEOUT_S = 30` must NOT be raised globally.** Its own
  comment (`mt5_client.py:79-81`) records why: a hung terminal must not wedge the
  SEQUENTIAL worker past the ~90 s healthz budget — that is the v1.11 WEDGE-01 wedge class.
  `Mt5Client` already accepts `request_timeout_s` as a constructor argument
  (`mt5_client.py:196`), so **the validate path gets its own longer chain** while the worker
  read path stays byte-unchanged at 30 s. Any plan that raises the module constant has
  reopened WEDGE-01.

- **D-27: this number is PROVISIONAL, and the phase must make it measurable.** No
  uncensored successful MT5 validation exists anywhere — not in Railway logs, not in
  Sentry, not in `docs/evidence/`. Elapsed time is **not instrumented** on the wizard's
  path (`mt5_client.py` and `routers/exchange.py:222-470` contain zero timing).
  ⛔ Do not cite "35–70 s" from `153-UI-SPEC.md:226` as evidence — that line is derived
  from the timeout constants and says so itself. This phase must add per-stage
  `stage` + `duration_ms` instrumentation so the real p50/p95 exists; the budget is then
  tightened from data during **Phase 155** (MT5-VERIFY), which is already live-gated.

### Founder architecture call — 2026-08-08 (BINDING, sets WIZFORM-05's posture)

Presented with the one-concurrent-user ceiling and the read-only fail-open
(`153-EVIDENCE-mt5-platform.md`), the founder chose: **ship 153 as beta with a
one-account cap.** Keep the single terminal; do not buy a managed provider yet; do not
block 153 on running the 134 spike. WIZFORM-05 proceeds — but as an honest beta, not as
a claim of multi-tenancy.

- **D-29 (REVISED 2026-08-08 — founder: *"We need to update MT5 accounts per client only
  once [per day]. So potentially, one account can do a lot of them throughout the day.
  Without concurrency"*). The cap is on CONCURRENCY, not on ACCOUNTS.**
  The earlier reading ("one-account cap") was wrong. MT5 binds one account per terminal
  *at a time* — that is a **serialization** constraint, not a capacity one. A daily sync is
  sequential and latency-tolerant, so ONE terminal can cycle through many accounts across a
  day: capacity ≈ (usable daily window ÷ per-account cycle time), which is hundreds, not one.
  **There is therefore NO account cap. The number of MT5 clients is not architecturally
  limited.** What must never overlap is two *simultaneous* uses of the terminal.
  - ⭐ **The mechanism already exists and the validate path simply does not use it.**
    `_mt5_terminal_lock_for` / `_MT5_TERMINAL_LOCKS` (`services/mt5_concurrency.py:126-134`)
    is acquired by `job_worker.py:364`, `job_worker.py:3572` and
    `allocator_positions.py:656` — and **`routers/exchange.py` acquires it ZERO times**
    (verified). The wizard's validate path is the ONE caller that skips the lease. That is
    the whole bug. Fix = take the same lease.
  - **The lease needs a BOUNDED acquisition timeout**, separate from the operation timeout.
    Today `wait_for` sits *inside* the lock, so a queued caller's wait is unbounded and its
    own timer only starts once it holds the lock. An interactive validation must be able to
    give up waiting for the terminal without waiting for the terminal.
  - **Queueing is surfaced honestly, not hidden**: "waiting for the connection" is a
    different state from "validating", and the UI-SPEC's long-wait card + `Stop waiting`
    already provide the affordance. Copy names an action (WIZFORM-04) and leaks no
    infrastructure (WIZFORM-03).
  - ⚠️ Only the **interactive** path needs the bounded wait. The daily batch should queue
    patiently — it has all day.
- **D-30: `shutdown()` comes OUT of the request path.** `routers/exchange.py:449-466`
  calls `close()` in `finally:` on every validate; on a shared session that tears down the
  IPC pipe for any concurrent caller (`-10004`). Attach once, do not tear down per request.
  ⚠️ This REPLACES D-28's "bound `close()` separately" — with `shutdown()` off the request
  path there is no per-request close to bound. D-28 is superseded.
- **D-31: 🔒 close the read-only fail-open — `terminal_info()` guard.** `is_trade_capable`
  (`services/mt5_validation.py:133-149`) infers investor mode from two negative signals,
  but the terminal's **default-ON** *"Disable automatic trading through the external Python
  API"* makes both negative for a MASTER account too. Read `terminal_info()`
  (called NOWHERE today — verified) and **fail CLOSED**: if the terminal-level trade
  disable is on, we cannot distinguish investor from master, so REFUSE the key rather than
  stamp it read-only. Also stop asserting `_TRADE_RETCODE_DONE = 10009` alone — `10017
  TRADE_RETCODE_TRADE_DISABLED` is the documented investor signal and is never tested.
- **D-32: instrument before tuning.** Emit `stage` + `duration_ms` around each MT5 stage.
  D-26's 120 000 ms stands as the provisional client budget; the instrumentation is what
  makes the next number evidence rather than judgement (D-27).
- **D-33: pin the gateway to a SINGLE replica.** Ops, not code — but the one-session
  invariant is false the moment a second replica exists. Record it in the runbook; a
  scale-up is a correctness change, not a capacity change.

### Superseded decision — carried for the record, NOT to be implemented

- **D-28: `close()` must be bounded separately.** Every one of the 9 failures was followed
  by a *second* full 30.02–30.03 s in `Mt5Client.close: shutdown() raised` — so a failed
  attempt costs ~60 s of server wall clock, already 2× the client budget before any broker
  slowness. The `finally`-close (`exchange.py:448-465`) runs on every path and must keep
  doing so; it needs its own bound, NOT inclusion inside the end-to-end deadline (that
  would leak the RPyC session).

  ⚠️ **SUPERSEDED by D-30**, which takes `shutdown()` off the request path entirely.
  ⛔ The surviving obligation is the one D-28 and RESEARCH Pitfall 6 share: **the
  `finally`-close must still run when the end-to-end deadline fires**, and must never be
  wrapped *inside* that deadline (that leaks the RPyC session). That is a Wave-0 test row.

### Inherited but NOT owned here

- **D-26 (owner: Phase 153.4)** — the client budget is `120_000` ms with
  `BREAKER_LOCK_TOMBSTONE_S` 60 → 90 in the same commit. The server worst case this
  sub-phase produces must fit **inside** that ceiling; D-04's invariant is
  `client ceiling > server worst case`, sized END-TO-END so lock-queue wait sits *inside*
  the client budget.
- **D-04 (owner: Phase 153.4)** — `src/lib/seam-budgets.invariant.test.ts` is the gate that
  stops a naive bump. It is a TypeScript test; this sub-phase cannot satisfy it, only
  break it by producing a worst case that exceeds the ceiling.
- **D-05 / D-01 (owner: Phase 153.4)** — the honest in-progress copy and the venue-aware
  long budget that make D-29's "waiting for the connection" state visible to a user.
- **D-14 / D-22 (owner: Phase 153.2)** — the TS-side probe gate. D-31 hardens the read-only
  proof that gate relies on; do not weaken `_validate_mt5_key`'s contract without telling
  153.2.
- **D-09b / D-34 (owner: Phase 153.1)** — any new server-side refusal reason that must
  reach the wizard as an honest code travels through the TS route's code table, which this
  sub-phase does not own.

### Claude's Discretion

- *(None of the parent's discretion items apply to this sub-phase — all three concern
  TypeScript surfaces owned by 153.1, 153.2 and 153.4.)* The shape of the end-to-end
  deadline, the lease-acquisition timeout value and the instrumentation's emission
  mechanism are constrained by D-02/D-03/D-25/D-29/D-32 and by RESEARCH Finding 7 — treat
  those as the contract and choose the implementation that satisfies them.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-CONTEXT.md`
  — the parent's full D-01..D-34 (this file carries only this sub-phase's slice)
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-RESEARCH.md`
  — **Finding 7** (The Python end-to-end deadline that preserves D-02) — the load-bearing
  one; **Pitfall 6** (wrapping the MT5 `finally`-close inside the new end-to-end deadline);
  §Architectural Responsibility Map and §Runtime State Inventory for where the MT5 session
  state actually lives; §Environment Availability for what can be exercised locally
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-PATTERNS.md`
  — the **`analytics-service/routers/exchange.py` (Python probe deadline)** section, which
  carries the verbatim comment block for the event-loop bound, the derive-from-
  `MT5_REQUEST_TIMEOUT_S` margin that mirrors `ingestion/mt5.py:_MT5_PROBE_TIMEOUT_S`, and
  the **dual-timeout ORDERING guard** (Pitfall 3 / T-134-04) that D-24 extends
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-VALIDATION.md`
  — Wave 0 item owned here: *"Python: `finally`-close survives the end-to-end deadline;
  ordering-chain assertion — WIZFORM-05"*. Test-map rows: *"Python nested ordering
  `LOGIN_MS < REQUEST_S < STAGE_S < DEADLINE_S` holds (D-02)"* and *"The `finally`-close
  still runs when the end-to-end deadline fires"* — both via
  `cd analytics-service && python3 -m pytest tests/ -k mt5 -x`. Falsifiability row
  **SC-4**: *"In `exchange.py`, set the stage timeout **above** the end-to-end deadline"*
  must red the ordering-chain assertion. Also §Test Infrastructure: ⚠️ **pytest MUST run
  from `analytics-service/`** — a repo-root run misses the VCR `cassette_library_dir` and
  fires LIVE broker calls; ⚠️ never run the python and e2e-seeded suites concurrently
  against the shared TEST DB.
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-UI-SPEC.md`
  — **no UI surface** in this sub-phase. D-29's honestly-surfaced queue state is specified
  in §Surface 1 (the long-wait card, the escalation ladder, `Stop waiting`) and is built by
  **Phase 153.4**; read it only to know what states the server must be able to report.
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-EVIDENCE-mt5-platform.md`
  — **required.** §A3 (one terminal cannot hold more than one account — the serialization
  premise of D-29), §A4 `initialize()` exact semantics and §A5 `login()` vs
  `initialize(login=…)` (D-24), §A6 `shutdown()` semantics (D-30), §A7 error codes
  (incl. `-10004`), §C11–C12 the investor-password model and verifying read-only-ness
  programmatically (D-31), §D13–D14 login latency and repeated-login lockouts,
  §"Corrections to our current implementation", §"Structural risks, ranked by severity"
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-EVIDENCE-mt5-latency.md`
  — **required.** §1a/1b (the 9 correlated failures at 30.1–31.0 s, and the second full 30 s
  in `close()`), §1c (the only uncensored datapoints), §1d (what is NOT a measurement),
  §2 (elapsed time is not instrumented — the gap D-32 closes), §3 and
  **⚠️ "The ordering invariant is violated for `initialize()`"** (D-24's proof),
  §4 (the lock, who takes it and who does NOT, **lock-acquisition timeout: NONE**,
  consequence for budgeting — D-29), §6 (cheapest experiment that yields an UNCENSORED
  number, incl. permanent instrumentation), and the **Appendix — file:line index of every
  code claim**

</canonical_refs>

<specifics>
## Specific Ideas

**Binding traps (verbatim from `.planning/ROADMAP.md` §Phase 153.3):**

- 🔒 **D-31 is a SECURITY fix, not a refactor.** `is_trade_capable` infers investor mode from two signals that are BOTH false for a MASTER account under the terminal's default-ON "Disable automatic trading through the external Python API". `terminal_info()` is called nowhere and does not exist on `Mt5Client` — it must be ADDED. Fail **CLOSED**: refuse what we cannot classify.
- ⛔ **D-25: `MT5_REQUEST_TIMEOUT_S` stays byte-unchanged.** The validate path takes its own longer chain via the existing `request_timeout_s` ctor arg. Moving the module constant reopens the v1.11 WEDGE-01 wedge class.
- ⭐ **D-29: the lease already exists and this path is the one caller that skips it.** `_mt5_terminal_lock_for` (`mt5_concurrency.py:126-134`) is taken by `job_worker.py:364`/`:3572` and `allocator_positions.py:656`, and by `routers/exchange.py` **zero** times. Add a BOUNDED acquisition timeout distinct from the operation timeout — today `wait_for` sits inside the lock, so a queued caller waits unbounded. No account cap: accounts are unlimited, concurrency is one.

**File:line anchors this sub-phase must not lose** (all copied from the parent CONTEXT /
ROADMAP; re-derive each from source before editing — D-10's instruction applies repo-wide):

- `analytics-service/routers/exchange.py` `:62` — `_MT5_PROBE_TIMEOUT_S`; `:328`
  (connect/login), `:380` (probe), `:456` (close) — its three independent applications;
  `:222-470` — `_validate_mt5_key`, which contains **zero** timing today;
  `:448-465` / `:449-466` — the `finally`-close that must keep running.
- `analytics-service/services/mt5_client.py` `:79-81` — why `MT5_REQUEST_TIMEOUT_S` must
  not be raised (the v1.11 WEDGE-01 comment); `:80-85` — the 30 s bound and why the inner
  IPC timeout must stay strictly below it; `:82` — the rpyc `sync_request_timeout`;
  `:196` — the `request_timeout_s` constructor argument the validate path uses;
  `:213-219` — the ordering guard that covers `login()` and must be extended;
  `:306` — `self._mt5.initialize()` **with no `timeout=`** (the inversion).
- `analytics-service/services/mt5_concurrency.py` `:126-134` — `_mt5_terminal_lock_for` /
  `_MT5_TERMINAL_LOCKS`, the lease to take.
- Existing lease callers, for the shape: `job_worker.py:364`, `job_worker.py:3572`,
  `allocator_positions.py:656`.
- `analytics-service/services/mt5_validation.py` `:133-149` — `is_trade_capable`, the
  read-only fail-open; `_TRADE_RETCODE_DONE = 10009` and the untested
  `10017 TRADE_RETCODE_TRADE_DISABLED`.
- The measured cluster: **30.1–31.0 s** across 9/9 correlated gateway↔worker failures on
  2026-08-06/08; the `close()` leg a second **30.02–30.03 s**; vendor `initialize()`
  default **60 000 ms**.
- ⛔ Do NOT cite "35–70 s" from `153-UI-SPEC.md:226` as evidence — that line is derived
  from the timeout constants and says so itself (D-27).
- ⚠️ Run `mypy --strict` on `analytics-service` before shipping; fix via `cast()`, not
  `# type: ignore`.

</specifics>

<deferred>
## Deferred Ideas

⚠️ **Explicitly NOT in this phase** (verbatim from the parent CONTEXT, recorded so the
planner does not drift into them): buying a managed provider; a per-account container
fleet; the two-level broker→server picker (backlog — a curated `broker → servers[]` table,
since MetaQuotes' directory is binary/undocumented and parsing it risks ToS); moving the
server name out of `passphrase_encrypted` into a plain `mt5_server` column; running the
Phase-134 spike.

- Any TypeScript edit — the client budget, `budgetKeyFor`, the pin surface,
  `BREAKER_LOCK_TOMBSTONE_S`, the long-wait card, `Stop waiting` — **Phase 153.4**
  (D-01, D-04, D-05, D-19, D-26).
- Honest codes, the derived roster, `VENUE_CAPABILITIES`, `MIN_DESCRIPTION_CHARS` —
  **Phase 153.1**.
- The submit-path probe gate, the `KEY_NETWORK_TIMEOUT` catch-all, the MT5 chip set and
  preselect, inline field validation — **Phase 153.2**.
- **Tightening the budget from the new instrumentation** — **Phase 155** (MT5-VERIFY),
  live-gated (D-27, D-32). This sub-phase produces the p50/p95; it does not act on it.
- Live MT5 numeric verification against an external oracle on a trading day — **Phase 155**.
- Scaling the gateway past one replica — forbidden by D-33 without treating it as a
  correctness change; if it is ever wanted, that is a `TODOS.md` item, not a capacity knob.

</deferred>

---

*Phase: 153.3-wizform-gw-mt5-gateway-honesty*
*Context inherited: 2026-08-08 from the Phase 153 split*
