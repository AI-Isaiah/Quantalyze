---
phase: 140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - src/lib/sentry-capture.ts
  - src/lib/sentry-capture.test.ts
  - src/lib/resilient-fetch.ts
  - src/lib/resilient-fetch.test.ts
  - src/lib/ratelimit.ts
  - src/lib/ratelimit.test.ts
autonomous: true
requirements: [SEAMRIM-04]

must_haves:
  truths:
    - "A Sentry capture is no longer detached before it resolves — the promise is returned and the waitUntil window stays open"
    - "The breaker OPENING and both of its store-failure catches reach a sink, not just one console.warn"
    - "The limiter's third posture — the timeout sentinel that silently fails OPEN — is recorded rather than discarded"
  artifacts:
    - path: "src/lib/sentry-capture.ts"
      provides: "captureToSentry returns its import chain as a Promise, exactly as audit.ts's reportToSentry does"
      contains: "Promise<void>"
    - path: "src/lib/resilient-fetch.ts"
      provides: "breaker transition + both store catches routed through the capture chokepoint, scheduled with after()"
  key_links:
    - from: "src/lib/resilient-fetch.ts"
      to: "src/lib/sentry-capture.ts"
      via: "the breaker's only health sink stops being a lone console.warn"
      pattern: "captureToSentry"
---

<objective>
Make the breaker observable — **and do the two halves in the order the subject's own docblock
demands**, because doing them the other way round builds an alert on sand.

## The measured state

| fact | evidence |
|---|---|
| `captureToSentry` count in `resilient-fetch.ts` and `ratelimit.ts` | **0** in both |
| the breaker's only health sink | one `console.warn` in `emitBreakerTransition` |
| both store catches | swallow — `isBreakerOpen` returns `{open:false}`, `recordSeamFailure` records nothing |
| `Sentry.init` integrations | **none** (`src/instrumentation.ts`) ⇒ `captureConsoleIntegration` is not enabled ⇒ `console.warn` never reaches Sentry |
| `next.config.ts` | **no `withSentryConfig`** |
| `captureToSentry` shape | `void import("@sentry/nextjs").then(…).catch(() => {})` — the detached-promise shape |
| production `captureToSentry` call sites | **109** across **46** files (re-measured at plan time) |

## The sequencing constraint is written in the subject's own docblock

`resilient-fetch.ts`, above `emitBreakerTransition`:

> *"A PLAIN STRUCTURED LOG, deliberately — it performs no I/O, so there is nothing to await and TRAP-7
> … cannot apply. **If this ever gains a network sink it must become an awaited call at both sites**,
> for exactly the reason the recording arms are awaited inline."*

And the repo **already found, diagnosed and fixed this exact defect** — in a different file, in May
2026 — and `sentry-capture.ts` re-created it. `src/lib/audit.ts`, above `reportToSentry`:

> *"**NEW-C10-03** (audit-2026-05-26 red-team): the prior implementation used `void import(...)...`
> which returns synchronously — the in-flight dynamic-import promise was detached before
> `captureException` resolved. **Under `after()` on a cold-finish the lambda can be reaped before
> Sentry flushes, producing a double silent failure: no audit row AND no Sentry event.** Fixed by
> returning the import chain as a Promise (awaited by the caller) so the `waitUntil` window stays
> open until capture settles."*

Two adjacent modules, one rule, applied in one and not the other. **Copy `audit.ts`. Do not invent.**

## ⚠️ PLAN-TIME MEASUREMENT THAT CHANGES THE DESIGN — read before you write a line

`captureToSentry` is imported by **six `"use client"` components**, measured at plan time:
```
src/app/(marketing)/for-quants/ForQuantsCtas.tsx
src/app/(dashboard)/allocations/components/ScenarioCommitDrawer.tsx
src/app/(dashboard)/allocations/widgets/lib/widget-boundary.tsx
src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx
src/components/mandate/useMandateAutoSave.ts
src/lib/storage/cross-tab.ts
```
⇒ **`src/lib/sentry-capture.ts` MUST NOT import `next/server`.** Putting `after()` in the chokepoint
would pull a server module into the browser bundle — the same class of defect the plan-checker caught
pre-execution in the original Phase 140 (it would have shipped `@upstash/redis` into the wizard
bundle). `sentry-capture.ts` imports exactly one module today (`./seam-redaction`) and must keep that
property.

**Where `after()` goes instead:** `src/lib/resilient-fetch.ts`, which is **server-only** — re-measured at plan-check:
`grep -rlE 'from "(@/lib/|\./|\.\./)resilient-fetch"' src/ tests/` returns **9 files, 5 of them
production** (`keys/[id]/permissions`, `keys/validate-and-encrypt`, `strategies/finalize-wizard`,
`analytics-client.ts`, `process-key-client.ts`), and **zero carry `"use client"`** — and
`src/lib/ratelimit.ts`, which **already imports `next/server`**.
(The earlier figure of "66 importers" was wrong: it conflated this module's importers with
`@/lib/ratelimit`'s, a far larger set. No integer is quoted for that set deliberately — it is not
load-bearing here, and an unowned count inside a correction about unowned counts is the exact
class 140.5 exists to remove. The load-bearing half — **zero client importers** — is unchanged,
and it is the half the design depends on.)

## Coverage-law classification (CONTEXT §2 — binding)

- **Task 1 — ROW 1 (a chokepoint leaf).** One function change reaches all **109** capture sites: every
  one of them stops detaching its promise. Source-compatible, because an unused returned promise is
  legal.
- **Task 2, limiter sentinel — ROW 1** (one leaf, `checkLimit`, and every caller in the repo inherits it).
- **Task 2, the breaker's three sinks — ROW 3 (per-site edit), and therefore PARTIAL BY CONSTRUCTION.**
  Measured coverage for that mechanism in this programme: 1/8 · 2/56 · **0/32**. Three hand-enumerated
  call sites in one file is a bounded per-site edit, not a chokepoint — living in the same file does
  not make them one artefact. It is acceptable here **only** because the set is fully enumerated
  (`emitBreakerTransition`, `isBreakerOpen`'s catch, `recordSeamFailure`'s catch — the file contains no
  fourth) and because Task 1's genuine row-1 change is what makes each of them deliver. **Say "partial
  by construction" in the SUMMARY, in those words.** M97 mutates the **second** of the three.

## ⚠️ THIS PLAN MUST NOT MARK C-5 CLOSED

CONTEXT §5c, carried verbatim as an **OPEN QUESTION the plan carries rather than resolves**:

> *"Sentry delivery without `withSentryConfig` is **unverified** (researcher confidence LOW). It maps
> onto founder-owed item §7.3. **C-5 MUST NOT be marked closed without it.**"*

The founder-owed verification (§7.3) is: **force a terminal-arm failure by pointing
`ANALYTICS_SERVICE_URL` at an unroutable host in a preview deploy** — the stock "trip the breaker"
instruction is wrong, because tripping the breaker captures **nothing by design** at all 15 routes
today. Adding `withSentryConfig` is **out of scope** here: it buys source maps, release tagging and
tunnelling, is a build-config change with its own blast radius, and is not on the critical path for
*"can we see the breaker trip"*. Record the open question in the SUMMARY; do not close C-5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-CONTEXT.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-RESEARCH.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-PATTERNS.md

<standing_constraints>
Binding on every task. You are a fresh agent with no memory of this phase's discussion.

- **This is NOT the Next.js you know** (`AGENTS.md`). Read
  `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/after.md` before writing any
  `after()` code. Verified at research time: `after` is **stable**, exported from `next/server` in
  16.2.11 (`node -e "const s=require('next/server'); console.log('after' in s, typeof s.after)"` →
  `true function`), *"will run for the platform's default or configured max duration of your route"*,
  and *"will be executed even if the response didn't complete successfully."*
- **Verify with the FULL suite, never a scoped path.** Baselines (CONTEXT §8): `npx vitest run` →
  **9792 passed | 287 skipped** (715 files) · `tsc --noEmit` exit 0 · `npm run lint` 0 errors.
  ⚠️ Mutation M8 in run 2 was **GREEN on both files named "rate limit"** and RED only on the full run.
  A scoped run on `ratelimit.test.ts` proves nothing here.
- **`vi.spyOn` + `vi.restoreAllMocks()`. NEVER `vi.stubGlobal`** (DEF-16-1).
- **Zero new packages.** `@sentry/nextjs` 10.64.0 and `next` 16.2.11 are already installed.
- `.planning/` is gitignored with no git backup. Never `git add .planning`. Never `gsd-sdk query state.*`.
- **Branch protection is OFF** ⇒ *"would have caught"*, never *"did stop"*.
- **grep is LINE-BASED** (DEF-16-2). **`grep -h` with a downstream filename filter is PROHIBITED.**
  Comment-strip before counting — `resilient-fetch.ts`'s docblocks discuss the shapes being changed.
- **Every pre-measured value here was taken on the untouched tree at HEAD `a77d607e`.** Re-run first.
</standing_constraints>

<read_first>
- `src/lib/audit.ts`, located by text — **the template, twice over:**
  - `reportToSentry` and the `NEW-C10-03` docblock quoted above. It **returns** the import chain as a
    `Promise<void>` with two swallowing catches.
  - `logAuditEvent`'s scheduling shape: `try { after(() => p.catch(() => {})) } catch { console.warn("… non-request scope"); queueMicrotask(…) }`.
    **`after()` throws outside a request scope** (cron, prerender) — the fallback is not optional and
    must not be "simplified away" (research assumption A6).
- `src/lib/sentry-capture.ts` — `captureToSentry`, its `scrubCaptureInput` / `scrubRecord` calls, the
  unconditional chokepoint scrub, and the `void import("@sentry/nextjs")` shape. Note its **single**
  import: `./seam-redaction`.
- `src/lib/resilient-fetch.ts`, located by text — the three sinks:
  - `function emitBreakerTransition(...)` ending in `console.warn(\`[resilient-fetch] seam.breaker.${kind}\`, payload)`,
    and the docblock quoted in the objective.
  - `isBreakerOpen`'s catch: *"breaker check failed — failing OPEN"* → returns `{ open: false }`.
  - `recordSeamFailure`'s catch: *"failed to record seam failure — breaker state may be stale"*.
  Both already call `scrubSeamError(err)`; **keep that**.
- `src/lib/ratelimit.ts` — `checkLimit`. The line `const { success, reset } = await limiter.limit(identifier);`
  is where the timeout sentinel's `reason` is discarded. Also read `isRateLimitMisconfigured` and the
  `MISCONFIGURED_RETRY_AFTER_S` constant.
- `src/lib/resilient-fetch.ts`, the posture docblock: *"A limiter whose store is missing silently
  removes a regulatory cap, so it should be loud and refuse traffic. A breaker whose store is missing
  is simply absent — and a breaker that refuses traffic because of its own misconfiguration is
  strictly worse than having no breaker at all (SEAM-03)."* **That asymmetry is CORRECT and this plan
  does not change either posture.** The two components protect different parties; making them
  symmetric in either direction is worse. What is wrong is that the limiter has **three** postures and
  nothing records which one fired.
- `src/lib/sentry-capture.test.ts` — ⚠️ it exists but is **UNREGISTERED** in `CONTRACT_GUARDS`
  (finding F-15). Plan `140.4-10` registers it; do not add a registry row here (that file is owned by
  another plan).
</read_first>

<interfaces>
**`captureToSentry` becomes `Promise<void>`-returning.** Copy `audit.ts`'s `reportToSentry`: return
the `import(...).then(...).catch(...)` chain instead of `void`-ing it. Keep both swallowing catches,
keep the unconditional chokepoint scrub (`scrubCaptureInput` / `scrubRecord` before the import — a
third party must never receive an unscrubbed payload), and keep the outer `try/catch`.

**Source compatibility is the point.** All 109 existing call sites discard the return value; an unused
promise is legal, so **no call site outside this plan's `files_modified` needs to change.** Do not
convert other call sites in this plan.

**The three seam sinks** (`emitBreakerTransition`, and both store catches) call `captureToSentry` and
**schedule the returned promise with `after()`**, using `audit.ts`'s `try { after(...) } catch { queueMicrotask(...) }`
shape. Severity: `warning` for the transition (a breaker opening is an operational fact and the OPEN
half is a mitigation working — routing it to `error` would make every recovery look like a fault, which
is the reason the existing log is `warn`); `error` for the two store failures.

**`checkLimit` reads the timeout sentinel.** `@upstash/ratelimit` 2.0.8 races a 5000 ms timeout that
resolves `{ success: true, limit: 0, remaining: 0, reason: "timeout" }` — meaning *"store unavailable,
let traffic through"*. Today only `{ success, reset }` are destructured, so that fail-OPEN is silent.
Read `reason`, and when it is the timeout sentinel, record it (structured log + a `captureToSentry`
at `warning`) **while still allowing the traffic through** — the posture does not change, only its
observability.

**Out of scope, stated so it is not smuggled in:**
- `withSentryConfig` in `next.config.ts` — deferred, with the reason recorded.
- Changing either fail posture (limiter CLOSED / breaker OPEN). **Explicitly preserved.**
- The 11-route 429-vs-503 split — plan `140.4-13` (which also touches `ratelimit.ts`, in a later wave).
- Converting any of the other ~106 `captureToSentry` call sites to `after()`.
- Registering guards in `CONTRACT_GUARDS` — plan `140.4-10`.
- Phase 141's retry work.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: captureToSentry returns its promise — copy audit.ts, and keep next/server OUT of the client bundle</name>
  <files>src/lib/sentry-capture.ts, src/lib/sentry-capture.test.ts</files>
  <read_first>
- `src/lib/audit.ts`'s `reportToSentry` + its `NEW-C10-03` docblock.
- `src/lib/sentry-capture.ts` in full.
- `src/lib/sentry-capture.test.ts` — every existing case.
  </read_first>
  <behavior>
- `captureToSentry(...)` returns a promise that **resolves after** `Sentry.captureException` has been
  called. A test that awaits the return value observes the call; on the untouched tree the same test
  observes nothing, because the chain is detached.
- A rejecting `import("@sentry/nextjs")` → the returned promise **resolves** (does not reject). A
  floating rejection here would become an unhandled rejection at 109 call sites.
- `Sentry.captureException` throwing → the returned promise still resolves.
- A hostile getter in `extra` → still dropped, payload still dispatched or suppressed exactly as today.
- **The scrub still happens before dispatch** (V8 / third-party data protection): the payload,
  tags and extra handed to Sentry are scrubbed. Assert this survives the refactor — it is the property
  most likely to be lost in a "returns a promise now" edit.
  </behavior>
  <action>
Change `captureToSentry`'s return type to `Promise<void>` and return the import chain, following
`reportToSentry`'s shape exactly. Preserve every existing behaviour: the chokepoint scrub before the
import, both swallowing catches, the outer `try/catch`, the default level.

Docblock: cite `audit.ts`'s `NEW-C10-03` by **symbol name**, state that this module is imported by
client components and therefore **must not import `next/server`**, and state that scheduling is the
caller's job.

⚠️ **Do not add any import to this file.** It has exactly one (`./seam-redaction`) and six
`"use client"` consumers.

Extend `sentry-capture.test.ts` with the behaviour cases. Use `vi.spyOn` on the module boundary and
`vi.restoreAllMocks()` in `afterEach`; **never `vi.stubGlobal`**.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/sentry-capture.test.ts</automated>
  </verify>
  <acceptance_criteria>
- **Pre-value, re-measure first:** `grep -c 'void import("@sentry/nextjs")' src/lib/sentry-capture.ts`
  → **1**. After the change → **0**.
- `grep -c "Promise<void>" src/lib/sentry-capture.ts` → **≥ 1**.
- **The client-bundle fence, and it is the load-bearing criterion:**
  `grep -c 'from "next/server"' src/lib/sentry-capture.ts` → **0**, and the file's total import count
  is unchanged: `grep -cE '^import ' src/lib/sentry-capture.ts` → **1** (its pre-value).
- The awaited-capture case **fails on the untouched tree**. Stash the production change, run the test,
  paste the RED output, restore.
- The scrub-survives case exists and asserts on the **payload handed to Sentry**, not merely that
  `captureToSentry` was called.
- **Zero call sites outside this file changed:** `git diff --name-only` for this task contains only
  `src/lib/sentry-capture.ts` and its test.
- `npx tsc --noEmit` exit 0 — the return-type change must be source-compatible with all 109 sites.
  </acceptance_criteria>
  <done>
`captureToSentry` returns its import chain as a promise, resolves on every failure path, still scrubs
before dispatch, and `sentry-capture.ts` still imports exactly one module.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the breaker's three sinks reach Sentry via after(), and the limiter's third posture becomes observable</name>
  <files>src/lib/resilient-fetch.ts, src/lib/resilient-fetch.test.ts, src/lib/ratelimit.ts, src/lib/ratelimit.test.ts</files>
  <read_first>
- `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/after.md` — **mandatory**
  (`AGENTS.md`), before writing `after()`.
- `src/lib/audit.ts`'s `logAuditEvent` scheduling shape (`try { after(...) } catch { console.warn(…); queueMicrotask(…) }`).
- `src/lib/resilient-fetch.ts`'s three sinks and the posture docblock.
- `src/lib/ratelimit.ts`'s `checkLimit` and `isRateLimitMisconfigured`.
  </read_first>
  <behavior>
- A breaker **open** transition → `captureToSentry` is called at level `warning` with the transition
  payload (breaker key, failure count, cooldown, correlation id), and the existing
  `console.warn("[resilient-fetch] seam.breaker.open", …)` still fires. **Both**, not one instead of
  the other.
- A breaker **close** transition → same, at `warning`.
- `isBreakerOpen`'s store rejection → `captureToSentry` at `error`, the existing scrubbed
  `console.error` still fires, **and the function still returns `{ open: false }`** (the fail-OPEN
  posture is preserved — assert it explicitly).
- `recordSeamFailure`'s store rejection → `captureToSentry` at `error`, existing scrubbed
  `console.error` still fires, and the function still swallows.
- **Outside a request scope** (`after()` throws) → the capture is still attempted via the
  `queueMicrotask` fallback and nothing propagates. This is research assumption A6 and the case that
  breaks if the fallback is "simplified away".
- `checkLimit` with a limiter resolving the timeout sentinel (`{ success: true, reason: "timeout" }`)
  → **still returns `{ success: true }`** (traffic passes, posture unchanged) **and** records the
  event distinguishably.
- `checkLimit` with a genuine allow (`{ success: true }`, no `reason`) → records **nothing**. The
  negative control; without it the previous case is satisfied by recording on every request.
- The two existing postures are unchanged: a null limiter in production still returns
  `reason: "ratelimit_misconfigured"`; a throwing limiter in production still fails CLOSED.
  </behavior>
  <action>
Route the three `resilient-fetch.ts` sinks through `captureToSentry` and schedule the returned promise
with `after()`, copying `audit.ts`'s `try`/`catch`/`queueMicrotask` shape verbatim. **Keep every
existing `console.warn` / `console.error` and every `scrubSeamError(err)` call** — the console line is
the operator's local trace and the scrub is TRAP-1.

Update `emitBreakerTransition`'s docblock: it currently says *"A PLAIN STRUCTURED LOG, deliberately —
it performs no I/O"*, and that is about to stop being true. Rewrite it to state that it now has a
network sink, that the sink is therefore scheduled rather than fire-and-forget, and **why** (TRAP-7: a
fire-and-forget promise is orphaned by Fluid Compute freeze during exactly the correlated incident it
exists for). Leave the `warn`-not-`error` reasoning intact.

In `checkLimit`, stop discarding the sentinel: destructure `reason` alongside `success` and `reset`,
and when a success carries the timeout reason, emit a structured `console.warn` **and** a
`captureToSentry` at `warning`, then return `{ success: true }` unchanged. Comment it as *the third
posture* — the limiter fails CLOSED on an HTTP-5xx store, fails **OPEN** (+ up to 5 s of the user's
latency) on a hanging store, and is **non-deterministic** on a refused socket where the SDK's ~4.29 s
retry ladder races the 5 s sentinel. **Do not change any posture.**

⚠️ `ratelimit.ts` is also edited by plan `140.4-13` in a **later wave**. Keep your diff to `checkLimit`
and its imports so the two changes do not collide.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/resilient-fetch.test.ts src/lib/ratelimit.test.ts src/lib/sentry-capture.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
- **Pre-values, re-measure first** (comment-stripped — these files' docblocks discuss the shapes):
  `grep -vE '^\s*(//|\*|/\*)' src/lib/resilient-fetch.ts | grep -c "captureToSentry"` → **0** today,
  **≥ 3** after (the transition plus both store catches).
  `grep -vE '^\s*(//|\*|/\*)' src/lib/ratelimit.ts | grep -c "captureToSentry"` → **0** today,
  **≥ 1** after.
- The consoles survived: comment-stripped `grep -c "console.warn\|console.error"` on
  `resilient-fetch.ts` is **≥** its pre-value. State both numbers. (A criterion that only counted
  `captureToSentry` would be satisfied by *replacing* the operator's local trace, which is a
  regression.)
- `grep -vE '^\s*(//|\*|/\*)' src/lib/resilient-fetch.ts | grep -c "scrubSeamError"` is **≥** its
  pre-value — TRAP-1 was not weakened.
- The fail-OPEN posture is asserted by a named test: `isBreakerOpen` still returns `{ open: false }`
  on a store rejection. The fail-CLOSED posture is asserted by a named test: a throwing limiter in
  production still returns `reason: "ratelimit_misconfigured"`.
- The `after()`-throws (non-request-scope) case exists and passes.
- The timeout-sentinel **negative control** exists: a genuine allow records nothing.
- `grep -c "vi.stubGlobal" src/lib/resilient-fetch.test.ts src/lib/ratelimit.test.ts` → **0** in both.
- `git diff --name-only` contains **zero** occurrences of `next.config.ts` and zero of
  `src/instrumentation.ts` (both out of scope).
- `npm run lint` 0 errors.
  </acceptance_criteria>
  <done>
The breaker's opening and both of its store failures reach the capture chokepoint on an `after()`
-scheduled promise with the console lines and scrubs intact, both fail postures are unchanged and
pinned, and the limiter's timeout sentinel is recorded without changing what it returns.
  </done>
</task>

<task type="auto">
  <name>Task 3: falsify both halves (M97, M98) and carry the OPEN founder-owed question — C-5 is NOT closed</name>
  <files>src/lib/resilient-fetch.ts, src/lib/ratelimit.ts</files>
  <read_first>
- `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md` — the
  `## Falsifiability Ledger` rules block and rows M86–M89. **Read; do not edit.** Highest row in the
  programme: **M89**; plans 01–05 take M90–M96; this plan's rows are **M97** and **M98**.
  </read_first>
  <action>
Two mutations, one at a time, each reverted. **Semantic**, to **PRODUCTION** source; never a syntax
error, never a type error, never a test edit. Record both in this plan's SUMMARY under
`## Falsifiability Ledger — rows M97, M98`.

**M97 — the capture is LIVE at the SECOND sink, and it is scheduled, not detached.**
The class is "a breaker sink that reaches Sentry". The transition is member one. **Mutate the second
member**: in `isBreakerOpen`'s catch, replace the `after(...)` scheduling with a bare `void` of the
returned promise — i.e. re-create precisely the `NEW-C10-03` detachment `audit.ts` diagnosed, while
leaving the `captureToSentry` call itself in place.
⚠️ This is the mutation that distinguishes LIVE from INERT: **a grep for `captureToSentry` in that
file still returns a hit afterwards.** Only a test that observes the scheduling can see it. If your
Task 2 tests cannot distinguish `after(p)` from `void p`, say so and add the case before recording the
row — an undetectable mutation is a finding about the receipt, not a pass.
Must stay GREEN and record it: the transition sink's own case.

**M98 — the sentinel read is LIVE, and the negative control is not vacuous.**
Mutation: in `checkLimit`, change the sentinel predicate to match a reason string that cannot occur
(e.g. `"timed_out"` instead of `"timeout"`), so the branch is **present but unreachable**.
Must redden: the timeout-sentinel case.
Must stay GREEN: the genuine-allow negative control, the null-limiter fail-CLOSED case and the
throwing-limiter fail-CLOSED case — recording those is what proves the sentinel case is testing the
sentinel and not the limiter in general.
⚠️ **Run the FULL suites for this row.** Mutation M8 in run 2 was GREEN on both files named
"rate limit" and RED only on the full run.

**Then carry the open question.** In the SUMMARY, under a heading `## OPEN — C-5 is NOT closed`,
record verbatim: Sentry **delivery** without `withSentryConfig` is unverified (researcher confidence
LOW); it maps onto founder-owed item CONTEXT §7.3; the correct probe is to point
`ANALYTICS_SERVICE_URL` at an unroutable host in a preview deploy and force a **terminal-arm** failure,
because tripping the breaker captures nothing by design at all 15 routes today. State that this plan
delivers the *mechanism* and not the *proof of delivery*, and that C-5 must not be marked closed
without §7.3. Branch protection is OFF ⇒ *"would have caught"*, never *"did stop"*.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
- Rows M97 and M98, each with pasted RED output including the failing test name and assertion text.
- M97 explicitly states that the mutation **left the `captureToSentry` call in place**, so a grep
  receipt would have reported the guard satisfied — that is the row's whole point.
- M97 states it mutated the **second** sink and names the first. M98 names its three GREEN-side
  observations.
- The SUMMARY contains the `## OPEN — C-5 is NOT closed` section with the §7.3 probe described.
- The SUMMARY classifies **Task 2's three breaker sinks as ROW 3** and contains the words
  **"partial by construction"** for them, while classifying Task 1 (one leaf → 109 call sites) and
  the limiter sentinel (one leaf → every caller) as ROW 1. Three hand-enumerated call sites in one
  file is a bounded per-site edit; co-location is not a chokepoint.
- `git status --porcelain` after both reverts shows no residual mutation.
- **FULL suite green:** `npx vitest run` at or above the branch baseline (state exact numbers);
  `npx tsc --noEmit` exit 0; `npm run lint` 0 errors.
- CI-parity run: `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` green.
  </acceptance_criteria>
  <done>
Both halves are proven falsifiable — including a mutation a grep receipt would have missed — and the
unverified-delivery question is carried forward rather than silently closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server module → browser bundle | `sentry-capture.ts` has **six `"use client"` consumers**; a `next/server` import here ships a server module to the browser |
| our runtime → Sentry (a third party) | Captured payloads must be scrubbed at the chokepoint before dispatch |
| the breaker's own state → operators | Today its only sink is a `console.warn` that `Sentry.init` is not configured to capture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.4-06-01 | **Repudiation** | the breaker trips and nothing is recorded | mitigate | Transition + both store catches route through `captureToSentry`, scheduled with `after()`. Row 1 — one chokepoint, three sinks. Falsified by M97 |
| T-140.4-06-02 | **Repudiation** | a capture detached before it resolves and reaped on cold-finish | mitigate | `captureToSentry` returns its import chain (copying `audit.ts`'s `NEW-C10-03` fix); callers `after()` it. M97 re-creates the detachment specifically |
| T-140.4-06-03 | **Information disclosure** | an unscrubbed payload reaching a third party | avoid | The unconditional chokepoint scrub is preserved and asserted by a named case; `scrubSeamError` counts at the seam sinks must not decrease |
| T-140.4-06-04 | **Tampering (bundle)** | `next/server` entering the browser via `sentry-capture.ts` | avoid | Asserted: zero `next/server` imports and an unchanged total import count of 1 in that file. `after()` lives only in `resilient-fetch.ts` / `ratelimit.ts`, both verified server-only |
| T-140.4-06-05 | **Denial of service (self-inflicted)** | changing the breaker to fail CLOSED or the limiter to fail OPEN | avoid | Both postures preserved and each pinned by a named test; the posture docblock's reasoning is quoted, not rewritten |
| T-140.4-06-06 | Repudiation | the limiter's silent third posture (timeout sentinel → fail OPEN + 5 s latency) | mitigate | `reason` is read and recorded; the return value is unchanged; a negative control proves a genuine allow records nothing. Falsified by M98 |
| T-140.4-06-07 | Repudiation | marking C-5 closed on an unverified sink | avoid | The `## OPEN — C-5 is NOT closed` section carries CONTEXT §5c's open question and names the §7.3 probe |
| T-140.4-06-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed** — `@sentry/nextjs` 10.64.0 and `next` 16.2.11 already present (`140.4-RESEARCH.md` §Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run` (FULL, never scoped — M8 was green on both files named "rate limit") at or above
  the branch baseline; state exact numbers.
- `npx tsc --noEmit` exit 0 · `npm run lint` 0 errors.
- `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` green.
- `next.config.ts` and `src/instrumentation.ts` are **not** in the diff.
- `src/lib/sentry-capture.ts` imports exactly one module.
- Zero Python files, zero `supabase/migrations/`, zero `.planning/` paths staged.
</verification>

<success_criteria>
- `captureToSentry` returns its promise, resolves on every failure path, still scrubs at the
  chokepoint, and did not gain an import.
- The breaker's transition and both store catches reach the chokepoint on an `after()`-scheduled
  promise, with every console line and scrub intact and both fail postures unchanged.
- The limiter's timeout sentinel is recorded without changing what `checkLimit` returns.
- M97 and M98 record observed RED, M97 on a mutation a grep receipt would have missed.
- The SUMMARY states C-5 is **not** closed and names the founder-owed §7.3 probe.
</success_criteria>

<output>
Create `.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-06-SUMMARY.md`.

Record: the pre/post `captureToSentry`, `console.*` and `scrubSeamError` counts for both files; the
client-bundle reasoning and the proof `sentry-capture.ts` still imports one module; the rewritten
`emitBreakerTransition` docblock verbatim; the three limiter postures as commented; the RED output for
the awaited-capture case against the untouched tree; the `## Falsifiability Ledger — rows M97, M98`
section; and the `## OPEN — C-5 is NOT closed` section.
</output>
