---
phase: 140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open
verified: 2026-07-29T01:50:00Z
status: gaps_found
score: 39/43 must-have truths verified (2 partial-failed, 2 uncertain)
overrides_applied: 0
verifier_mutations_run: 17
verifier_mutations_red: 17
tree_clean_after_revert: true
gaps:
  - truth: "A wire code the ONE translation table recognises renders its copy without a second roster edit (SEAMRIM-08, plan 12 truth 1) — read as a CLASS claim"
    status: partial
    reason: >-
      Plan 140.4-13 minted `code: "SEAM_MISCONFIGURED"` on the 503 arms of three routes.
      Plan 140.4-12 gave the kickoff arm a translation hop, closing ONE of the three.
      The other two consuming clients have no translation hop and no roster member, so the
      code collapses to `UNKNOWN`. Plan 13's SUMMARY assigned the roster edit to plan 12 /
      M104; plan 12's GREEN commit (2965877d) landed BEFORE plan 13's (2dc6d1f9), plan 12's
      files_modified never contained either file, and plan 12's `## OPEN` does not name it.
      Each plan believed the other owned it. Neither closed it.
    artifacts:
      - path: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
        issue: >-
          :392 membership-checks `data.code` against KNOWN_CREATE_WITH_KEY_CODES (:215-238),
          which has no SEAM_MISCONFIGURED member and no recogniseSeamErrorCode hop.
          `create-with-key/route.ts:242` emits it. Renders UNKNOWN — "Try the last action
          again." — for a fault whose own authored copy says "Retrying will not clear it."
      - path: "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx"
        issue: >-
          :727 membership-checks against KNOWN_ADD_KEY_CODES (:183-205), same shape.
          `composite/add-key/route.ts:254` emits it.
    missing:
      - "A translate-first hop at ConnectKeyStep.tsx:391 and MultiKeyConnectStep.tsx:726, matching SyncPreviewStep.tsx:670-675 — NOT a roster member, which would be coverage-law row 2"
      - "A named test per surface, in the shape of SyncPreviewStep.render.test.tsx:1751"
  - truth: "`analytics-client`'s thrown twin is scrubbed — declared UNFALSIFIABLE (`140.4-VALIDATION.md`:162, \"There is no guard to falsify\")"
    status: failed
    reason: >-
      The no-row claim is false and I falsified it. A semantic mutation to production source
      (dropping `scrubSeamString` from the interpolation at `analytics-client.ts:548`) reddens
      a named test. Plan 09's own SUMMARY contradicts VALIDATION.md — it says nothing can fail
      "except the three cases added in analytics-client.test.ts". A ledger row was owed and
      was not written; the coverage claim (row 3, one site) is separately correct.
    artifacts:
      - path: ".planning/phases/140.4-.../140.4-VALIDATION.md"
        issue: "line 162 asserts 'There is no guard to falsify' for a property that a named test does guard"
    missing:
      - "A ledger row (Mxx) recording the observed RED at src/lib/analytics-client.test.ts:1959, or an amended no-row entry saying the guard is instance-scoped rather than absent"
human_verification:
  - test: "TRAP-4 — five clicks in a REAL browser against a live composite draft"
    expected: "The destructive control is never the sole way forward; the confirm dialog behaves"
    why_human: "jsdom asserts only the dialog's `open` attribute; the seed-gated e2e specs skip. VALIDATION.md Manual-Only §7.2 — a PRECONDITION for closing C-4 / SEAMRIM-07, not a follow-up. The 140.3-16 checkpoint auto-approved under workflow.auto_advance; that is auto-approval, not confirmation."
  - test: "Sentry ingestion — point ANALYTICS_SERVICE_URL at an UNROUTABLE host in a preview deploy and force a TERMINAL-ARM failure"
    expected: "The capture arrives in Sentry"
    why_human: "Requires live ingestion in a deployed preview. instrumentation.ts calls Sentry.init with no `integrations` and next.config.ts has no withSentryConfig, so delivery is structurally unverified. Plans 06 and 13 BOTH state in their `## OPEN`: C-5 / SEAMRIM-04 must not be marked closed without this. The stock 'trip the breaker' instruction is WRONG — that captures nothing by design at all 15 routes."
  - test: "Copy vs DESIGN.md §Voice — the sentences this phase authored"
    expected: "Each reads as §Voice, and the CSV_SUBMIT_FAILED / CSV_SUBMIT_NO_STRATEGY_ID pair no longer contradict"
    why_human: "Prose quality is not a program property. Sentences quoted verbatim in 140.4-03-SUMMARY.md:401-437 (3), 140.4-09-SUMMARY (1), 140.4-13-SUMMARY.md:439-452 (2). VALIDATION.md Manual-Only §7.1."
  - test: "D-01 — `npm run test:redis` against a real Upstash instance"
    expected: "The live-Redis lane passes"
    why_human: "Unrunnable in a worktree; unverified by EITHER mutation sample. CONTEXT §6 KNOWN BLIND SPOT. No plan touches tests/redis/** and no plan claims it closed. Carried, correctly, in plan 13's `## OPEN`."
  - test: "Run the SQL lane against the TEST project (`qmnijlgmdhviwzwfyzlc`) on the PR"
    expected: "supabase/tests/test_csv_finalize_double_submit.sql exits 0, all four parts including the cross-source control"
    why_human: "`psql` is not installed on this machine and TEST_SUPABASE_DB_URL exists only as a write-only GitHub Actions secret. I could not re-run M93/M94 or the receipt. Plan 03's TEST-project apply is attributed to 'the coordinator', not observed by the executor."
---

# Phase 140.4: SEAMRIM Verification Report

**Phase Goal (ROADMAP.md:246):** *The surfaces stop asserting things we did not measure, stop offering a destructive control as the way forward, and stop attributing our own faults to the user or their venue — and the guards that claim these classes are closed can actually fail.*

**Verified:** 2026-07-29T01:50:00Z · HEAD `2da9295e` · branch `feat/v1.16-production-resilience`
**Phase base:** `a77d607e` (parent of `d1e4521a`, the first 140.4 commit)
**Status:** gaps_found
**Re-verification:** No — initial verification

---

## What I measured myself

Nothing below is taken from a SUMMARY. **17 mutations applied by me to production source, one at a time, each reverted.** Zero syntax errors, zero type errors, zero test edits.

### Suites, re-run at HEAD by me

| Gate | Result | Note |
|---|---|---|
| `npx vitest run` (FULL) | **719 files passed / 19 skipped · 10024 passed / 287 skipped / 0 failed** | **Local Node 25.** I did NOT run the mandated `PATH=/opt/homebrew/opt/node@22/bin` CI-parity pass — see Residuals. |
| `npx tsc --noEmit` | **exit 0** | |
| `npm run lint` | **0 errors, 1 warning** | `EquityChart.tsx:1119` — pre-existing, unrelated |
| `cd analytics-service && pytest -q` | **4801 passed / 96 skipped** | Baseline was 4778; +23 is the new census file (19) + plan 03's process_key cases |
| `mypy --strict` | **Success: no issues found in 89 source files** | |
| `git diff a77d607e..HEAD -- package.json package-lock.json` | **EMPTY** | Wave-0 "ZERO packages" contract HELD |
| `git status --porcelain` after all 17 reverts | `M TODOS.md` + `?? analytics-service/scripts/nautilus_factsheet.py` | **Both pre-existing at session start.** No residual mutation. |

Post-revert regression run over every file I mutated: **27 files / 916 tests passed**, plus `test_raw_5xx_census.py` **19 passed**.

---

## Falsifiability — re-run first-hand

**Rule applied:** a grep proves a state; only a guard proves the state is held. I did not accept a single SUMMARY-recorded RED.

| Row | My mutation (production source) | Result | Guard that caught it |
|---|---|---|---|
| **M90** | `strategyGate.ts:210` `if (spanDays === null)` → `&& input.tradeCount < 0` (present-but-unreachable) | 🔴 **RED** | `strategyGate.test.ts` — 3 tests |
| **M91** | `admin/strategy-review/route.ts:195` `isReadFailure(latestTradeError)` → `earliestTradeError` (**second member**) | 🔴 **RED** | `route.test.ts:868` — 503 → 200 |
| **M92** | `SyncPreviewStep.tsx` — delete the `earliestRes.error` throw **and** restore `?? []` (plan 02's exact recorded mutation) | 🔴 **RED** | `SyncPreviewStep.readfailure.runtime.test.tsx:410` |
| **M92′** | my weaker variant (drop the check, keep `earliestRes.data`) | 🟢 green — **correctly.** `earliest = null` then flows into `checkStrategyGate`, which now THROWS (M90's fix). Layered, not a hole |
| **M95b** | `portfolio.py:2326` anonymous teaser `status_code=500` → `502` — **the mutation that was GREEN in phase 140's sample** | 🔴 **RED** | `test_raw_5xx_census.py` — **6 tests** |
| **M96** | `AllocatorMatchQueue.tsx:412` `<LiveRegion>` element deleted, **import kept** (**second surface**) | 🔴 **RED** | `AllocatorMatchQueue.test.tsx` — 5 tests. `grep -c 'import { LiveRegion }'` still **1** |
| **M97** | `resilient-fetch.ts:915` `after(() => capture.catch(…))` → `void capture.catch(…)` | 🔴 **RED** | `resilient-fetch.test.ts` — 5 tests. `grep -c captureToSentry` still **7** |
| **M98** | `ratelimit.ts:397` `LIMITER_TIMEOUT_REASON = "timeout"` → `"timed_out"` (present-but-unreachable) | 🔴 **RED** | `ratelimit.test.ts:367` |
| **M99** ⭐ | `verify-strategy/route.ts:445` `scrubSeamError(persistError, …)` → `persistError` (**second member**) | 🔴 **RED** | `seam-log-coverage.test.ts:856` — *"…:443 passes bare `persistError`"* |
| **M100** ⭐ | `scenario/optimize/route.ts:274` `scrubSeamError(err)` → `err` (**second site**) | 🔴 **RED** | `seam-log-coverage.test.ts:819` — *"…:274 passes bare `err`"* |
| **M101** | `simulator/route.ts:222` re-expressed as a one-hop alias (`const message = err instanceof Error ? err.message : String(err)`) in a **rostered** file | 🔴 **RED** | `seam-log-coverage.test.ts:874` alias half. The old bare-identifier grep scores **0** under it |
| **M102** | `admin/match/recompute/route.ts:9` `@/lib/analytics-client` → `../../../../../lib/analytics-client` (**narrowing**) | 🔴 **RED** | `seam-log-coverage` + `seam-budgets.invariant` — **2 files / 2 tests / 154 passed**, byte-matching plan 10's recorded counts |
| **M103** | `SyncPreviewStep.tsx:1544` property → `errorCode !== "GATE_NO_DATA_SOURCE"` (instance-only regression) | 🔴 **RED** | `SyncPreviewStep.render.test.tsx:2361` — 4 tests; the property test names `RATE_LIMITED` |
| **M104** | `SyncPreviewStep.tsx:672-675` translation made present-but-unreachable (plan 12's substitute) | 🔴 **RED** | 6 tests. `grep -c recogniseSeamErrorCode` still **2** |
| **M105** | `keys/sync/route.ts:149` **second ARM** reverted to an inlined 429 | 🔴 **RED** | `keys/sync/route.test.ts:1309` + `seam-ratelimit-posture.invariant.test.ts` — 2 files |
| **M106** | `admin/strategy-review/route.ts:131` **array-form** member 2 unbinds `error` | 🔴 **RED** | `npx eslint` — `quantalyze/no-unchecked-supabase-read`, message reproduced verbatim |
| **M107** | `LiveRegion.tsx:64` `className="sr-only"` → `"sr-only text-negative"` | 🔴 **RED** | `LiveRegion.test.tsx:51`. **The grep receipt went 2 → 1 under the mutation** — see Finding 5 |
| *(split row)* | `wizardErrors.ts:886` inject the banned fragment *"been notified"* | 🔴 **RED** | `wizardErrors.test.ts:1500` — hand-typed `FORBIDDEN` |

**17 of 17 mutations reddened.** Every "second member" I mutated was genuinely guarded. **The instance-vs-class defect that created this phase does not reproduce in this phase's own remedies.**

**Not reproducible by me:** M93, M94 (SQL lane — `psql` not installed, `TEST_SUPABASE_DB_URL` is a write-only GH secret). M95 and M108 verified at source instead (see below).

---

## The six "no row possible" claims — attacked

| Claim | Plan | Survives attack? | Evidence |
|---|---|---|---|
| Prose quality of the three reconciled `wizardErrors.ts` strings | 03 | ✅ **YES** — genuinely unfalsifiable | The truth of a sentence is not a program property. |
| …its **behavioural half** (the split) | 03 | ✅ **CORRECTLY SPLIT** | I injected *"been notified"* into `wizardErrors.ts:886`; `wizardErrors.test.ts:1500` reddened. `FORBIDDEN` is hand-typed (`:1440-1473`), 4 fragments. |
| `analytics-client`'s **thrown twin** is scrubbed | 09 | ❌ **NO — FALSIFIED** | See Finding 2. |
| Registry rows exist and the floor matches the count | 10 | ✅ **YES** | `CONTRACT_GUARDS` and its floor both live in `contracts-registry.test.ts` — every available mutation is a test/file deletion, forbidden by the rules. Independently: **52 rows** (`grep -c '^  { path:'`) against a hand-typed `toBeGreaterThanOrEqual(52)` (`:247`) — **zero slack**, and `grep -cE 'toBeGreaterThanOrEqual\(CONTRACT_GUARDS'` → **0**, so hazard #2 is not present. |
| **D-01** — the live-Redis lane | — | ✅ **YES** | `test:redis` has `include: tests/redis/**` and this phase adds nothing to it. No code exists to mutate. |
| *(M107, M108 — the two that became rows)* | 05, 04 | ✅ correctly promoted | M107 observed RED by me. M108's constants are published **with** their predicates: `EXPECTED_NON_LITERAL_STATUS_SITES = 3` (strict `ast.Call` + `ast.Name`) and `…_UNDER_LOOSE_READING = 4`, asserted `== +1` of each other (`test_raw_5xx_census.py:203-204, 656-672`). |

**One of six is wrong, and it is the one the phase's own SUMMARY already contradicts.**

---

## Observable truths

| # | Req | Truth | Status | Evidence |
|---|---|---|---|---|
| 1 | 01 | An admin cannot publish a strategy whose span was never read | ✓ VERIFIED | M90 RED; `strategyGate.ts:210` throws `StrategyGateUnevaluableError` |
| 2 | 01 | A read failure on the admin approve path answers 503 about US | ✓ VERIFIED | All 6 `Promise.all` members error-bound, `route.ts:135-209`; M91 RED |
| 3 | 01 | The refusal holds for a third consumer, because it lives in the gate | ✓ VERIFIED | Refusal is in `strategyGate.ts`, not the route; M92′ demonstrates the second layer catching a neutered first layer |
| 4 | 02 | A failed read and an empty account no longer render the same sentence | ✓ VERIFIED | M92 RED at `readfailure.runtime.test.tsx:410`; positive counterpart present |
| 5 | 02 | The wizard never states a number it did not measure | ✓ VERIFIED | 4 error-bound reads `SyncPreviewStep.tsx:1203-1222` before `:1244-1247` |
| 6 | 02 | A read failure routes into the composite arm's escalation | ✓ VERIFIED | throws → `heavyFetchErrorsRef` `:1354-1355`, same sink as `:924` |
| 7 | 03 | A CSV double-submit cannot create a second strategy | ? UNCERTAIN | Structurally verified (index `20260728120000:167`, `finalize_csv_strategy.sql:102-109` writes `wizard_session_id`). **Not executable here** — no psql |
| 8 | 03 | A legitimate FIRST CSV submit still succeeds after an abandoned API draft | ? UNCERTAIN | `localStorage.ts:404` gates the restore on `source`; index is 3-column. Cross-source control is Part 4 of the SQL receipt — unrunnable here |
| 9 | 03 | The instructed retry answers 200 with the existing strategy id | ✓ VERIFIED | `process_key.py:1121` 23505 arm; pytest green |
| 10 | 03 | No raw Postgres constraint text reaches a user-facing message | ✓ VERIFIED | `process_key.py:1117-1147`; banned-claims guard reddens on injection |
| 11 | 04 | A new raw 5xx outside the contract fails a named test | ✓ VERIFIED | M95b RED — 6 tests |
| 12 | 04 | The mutation that was GREEN now reddens | ✓ VERIFIED | **M95b run by me at the exact site** (`portfolio.py:2326`, anonymous public teaser) |
| 13 | 04 | The census names its own blind spot | ✓ VERIFIED | `:197-204` two constants, each with its predicate; `:656-672` asserts both |
| 14 | 05 | A screen-reader user is told when these three surfaces fail | ✓ VERIFIED | `<LiveRegion>` at 3 sites; M96 RED |
| 15 | 05 | Visual rendering byte-identical | ✓ VERIFIED | `className` is a hard-coded literal, no override prop (`LiveRegion.tsx:32-35, 64`); M107 pins equality not containment |
| 16 | 05 | A deleted announcement reddens a named test | ✓ VERIFIED | M96 RED with the **import kept** — a grep receipt reports it satisfied |
| 17 | 06 | The capture is no longer detached before it resolves | ✓ VERIFIED | `sentry-capture.ts` returns `Promise<void>`; M97 RED |
| 18 | 06 | Breaker OPENING + both store catches reach a sink | ✓ VERIFIED | `scheduleSeamCapture` at 3 sites; M97 RED at the chokepoint |
| 19 | 06 | The limiter's third posture is recorded | ✓ VERIFIED | `ratelimit.ts:466-481`; M98 RED |
| 20 | 07 | No console site on the three routes passes a caught value unscrubbed | ✓ VERIFIED | `seam-log-coverage.test.ts` green over all 19 roster files; M99 RED |
| 21 | 07 | The syscall token survives the scrub | ✓ VERIFIED | `FAILURE_REASON` (`:737-746`) names the A-10 anti-fix; `SAFE_PROPERTIES` is 3 |
| 22 | 07 | The tree is clean BEFORE the roster widens | ✓ VERIFIED | 79/79 tests green at the widened roster |
| 23 | 08 | The six remaining import-edge routes pass no caught value unscrubbed | ✓ VERIFIED | M100 RED at `scenario/optimize` |
| 24 | 08 | `ratelimit.ts`'s store-failure log stops shipping the Upstash header | ✓ VERIFIED | `grep -c scrubSeamError src/lib/ratelimit.ts` ≥ 1; roster covers the file |
| 25 | 08 | Every offence closed was enumerated by the guard's own predicate | ✓ VERIFIED | M101 shows the predicate enumerating a shape a grep misses |
| 26 | 09 | csv-validate's 502 carries a static message | ✓ VERIFIED | `csv-validate/route.ts` + `csv-validate-route.test.ts` |
| 27 | 09 | The scrub predicate follows a one-hop alias, and only the text-carrying channel | ✓ VERIFIED | M101 RED; `TEXT_CARRYING_PROPERTIES` (`:497`) is 4, fixpoint alias walk (`:615-680`) |
| 28 | 09 | The predicate scores ZERO on the roster after the change | ✓ VERIFIED | Full suite green |
| 29 | 10 | A new route consuming a seam wrapper reddens on the day it is written | ✓ VERIFIED | `deriveSeamRouteFiles` (`:104`) + set equality both ways (`:778`) |
| 30 | 10 | A route silently LEAVING also reddens | ✓ VERIFIED | **M102 reproduced exactly** — 2 files / 2 tests. Three `it.each` cases silently vanished under it, which is the point |
| 31 | 10 | Guards created in this phase cannot be deleted with green CI | ✓ VERIFIED | 4 new guards in both `contracts-registry.test.ts:216-219` and `REGISTRY.md:89,95,96,97`; floor 52 = row count, zero slack |
| 32 | 11 | No terminal state offers an unconfirmed destructive control as its SOLE affordance | ✓ VERIFIED | `<Link>` is UNCONDITIONAL (`:1609`); M103 RED. **Founder §7.2 still owed for C-4 closure** |
| 33 | 11 | The control renders only for codes that EARN it, derived from the table | ✓ VERIFIED | `keyReplacementIsEarned = errorActions.includes("try_another_key")` (`:1544`) — row 1. `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` grep → **0**, the roster is gone |
| 34 | 11 | A code whose only retry re-evaluates an identical gate does not advertise a retry | ✓ VERIFIED | `kickoffRetryCanChangeTheOutcome` (`:1564-1565`) split from `recoverable` |
| 35 | 12 | A translated wire code renders without a second roster edit | ✗ **PARTIAL** | Closed at `SyncPreviewStep` (M104 RED). **Open at `ConnectKeyStep` and `MultiKeyConnectStep`** — Finding 1 |
| 36 | 12 | The honest RATE_LIMITED copy becomes reachable | ✓ VERIFIED | `SEAM_CODE_TO_WIZARD_CODE` authoritative at both fixed surfaces; rosters unchanged in size AND membership |
| 37 | 12 | A code in the nested Python envelope is read | ✓ VERIFIED | `seamErrorCode(failure)` at `SyncPreviewStep.tsx:669` and `SubmitStep.tsx` |
| 38 | 13 | A limiter misconfiguration answers 503 on every seam route | ✓ VERIFIED | M105 RED **per ARM**; posture invariant derives its population every run |
| 39 | 13 | Genuine 429 bodies and headers byte-unchanged | ✓ VERIFIED | Per-route "BYTE-IDENTICAL v0 envelope" cases; `keys/sync` keeps `{error, code:"RATE_LIMITED"}` key order |
| 40 | 13 | A new seam route without the split reddens | ✓ VERIFIED | `seam-ratelimit-posture.invariant.test.ts` — derived population + hand-typed roster + vacuity fence |
| 41 | 14 | A new unchecked Supabase read fails at EDIT time | ✗ **PARTIAL** | M106 RED — but the `error` glob covers **exactly one file** (`eslint.config.mjs:183`). Finding 6 |
| 42 | 14 | The dirty repo-wide baseline is not red-CI'd | ✓ VERIFIED | Scoped glob; `npm run lint` 0 errors |
| 43 | 14 | The rule cannot be satisfied by a destructure that merely mentions error | ✓ VERIFIED | `RuleTester` fixtures cover both forms; object-only variant scores 0 on the array form |

**Score: 39 VERIFIED · 2 PARTIAL/FAILED · 2 UNCERTAIN — 43 truths.**

---

## Findings

### 🛑 Finding 1 — `SEAM_MISCONFIGURED` is unreachable on 2 of the 3 named routes. Each plan believed the other owned it.

**This is a real hand-off hole, and the orchestrator's suspicion was right.**

Plan 13 minted the code on three routes' 503 arms. Its SUMMARY (`:429`) states all three rosters lack it and assigns the fix to *"plan `140.4-12`, ledger row M104"*. **But plan 12 had already landed:**

```
2965877d  feat(140.4-12): GREEN — the kickoff arm translates before it membership-checks
2dc6d1f9  feat(140.4-13): adopt the deny chokepoint at all 12 seam call sites   ← later
```

Plan 12's `files_modified` is `SubmitStep.tsx`, `SubmitStep.test.tsx`, `SyncPreviewStep.tsx`, `SyncPreviewStep.render.test.tsx`. Neither `ConnectKeyStep.tsx` nor `MultiKeyConnectStep.tsx` is in it, and plan 12's `## OPEN` (7 items) does not mention the gap.

**Verified at source, route by route:**

| Producer | Consumer | Translation hop? | Roster member? | Renders |
|---|---|---|---|---|
| `keys/sync/route.ts:141,155` | `SyncPreviewStep.tsx:670-675` | ✅ `recogniseSeamErrorCode` | n/a | ✅ `SEAM_MISCONFIGURED` |
| `create-with-key/route.ts:242` | `ConnectKeyStep.tsx:392` | ❌ none | ❌ `KNOWN_CREATE_WITH_KEY_CODES:215-238` | ✗ `UNKNOWN` |
| `composite/add-key/route.ts:254` | `MultiKeyConnectStep.tsx:727` | ❌ none | ❌ `KNOWN_ADD_KEY_CODES:183-205` | ✗ `UNKNOWN` |

`SEAM_CODE_TO_WIZARD_CODE` **does** carry `["SEAM_MISCONFIGURED","SEAM_MISCONFIGURED"]` (`wizardErrors.ts:1629`), so the ONE translation table recognises it — it is simply never consulted at those two surfaces.

**User-visible consequence.** `SEAM_MISCONFIGURED` copy (`wizardErrors.ts:1163-1179`) reads *"Retrying will not clear it: the setting stays wrong until we fix it and redeploy"* and deliberately carries **no** `clear_and_retry`. `UNKNOWN` (`:1194-1204`) instead says *"Try the last action again"* and carries `actions: ["clear_and_retry", "request_call"]`.

**Severity, stated honestly:** this is a **net improvement, not a regression.** Before plan 13 the misconfiguration answered 429 `KEY_RATE_LIMIT` — *"a transient, exchange-side throttle and not a problem with your key"* — our outage blamed on their exchange. That copy no longer renders. The goal clause *"stop attributing our own faults to the user or their venue"* is met at these two surfaces; the authored honest copy is merely unreachable. **Gap, not blocker.**

**Not deferred to 140.5.** 140.5's remit is explicitly the *descriptive* rim (comments, citations, prose) plus `Retry-After`, `SERVICE_UNREACHABLE`, `draft_state_invalid`. It does not name `SEAM_MISCONFIGURED` or these two rosters. Per Step 9b's conservatism rule I kept it as a gap.

### 🛑 Finding 2 — the thrown-twin "no row is possible" claim is false, and I falsified it

`140.4-VALIDATION.md`:162 states: *"A **thrown** sink is structurally invisible to a `console.*`-scoped predicate. **There is no guard to falsify**."*

**Mutation** (semantic, production source, `tsc` clean): drop `scrubSeamString` from the interpolation at `analytics-client.ts:548`.

```
FAIL src/lib/analytics-client.test.ts > [140.4-09 / SEAMRIM-06] parseResponse's THROWN
     message is scrubbed, like its log > does NOT put a known secret into the THROWN message
AssertionError: a credential reached a THROWN Error.message. …
Expected: "int_5c1d90b7ae42f68d3b0e7a91c4f25d8e6b30a7f1"
Received: "Analytics response contract violation on /api/optimize-weights: weights.int_5c1…"
  ❯ src/lib/analytics-client.test.ts:1959
```

Plan 09's own SUMMARY says it plainly: *"nothing can fail when a future edit re-opens it **except the three cases added in `analytics-client.test.ts`**."* VALIDATION.md's absolute *"there is no guard"* is the wrong sentence, and it is the sentence a phase-gate reader would rely on. The coverage classification (row 3, one site, partial by construction) is separately correct and I do not dispute it. **What is wrong is the falsifiability claim, in the one table this phase created specifically to stop unfalsified closures.**

### ⚠️ Finding 3 — stale line citations inside plan 10's M99/M100 receipts

Plan 10 records the offence strings as `verify-strategy/route.ts:431` and `scenario/optimize/route.ts:263`. Re-run at HEAD, the guard emits **`:443`** and **`:274`**. Both files grew after plan 10 measured (plan 13 edits). The substantive claim — that the class guard sees both — is **correct and I confirmed it**; only the transcription drifted. This is the citation-rot class 140.5 owns, occurring inside this phase's own receipts.

### ⚠️ Finding 4 — SEAMRIM-11's edit-time backstop covers ONE file

`eslint.config.mjs:183` scopes `quantalyze/no-unchecked-supabase-read: "error"` to `src/app/api/admin/strategy-review/route.ts` and nothing else. `SyncPreviewStep.tsx` — the phase's other converted file — is outside it, so a new unchecked read there does **not** fail at edit time. Disclosed: plan 14's truth 2 says so, and plan 12's `## OPEN` item 6 names it explicitly. Honest, and correct as a ratchet — but truth 41 is 1-of-2, not a class.

### ℹ️ Finding 5 — the acceptance-criteria defect sweep found NO new instance

I re-measured 20 numeric criteria against the untouched tree. The four that look wrong at first pass all resolve:

| Criterion | Raw | Comment-stripped | Verdict |
|---|---|---|---|
| `grep -c 'className="sr-only"' LiveRegion.tsx` | **2** | **1** | Line 15 is the docblock quoting `CardShell.tsx:77`. **Plan 05's SUMMARY:145 already records the raw 2 and the reason**; the plan's criterion (`:224`) says *"and comment-stripped"*. Handled. ⚠️ Note the sharpest form: under M107 the raw grep goes **2 → 1**, so a naive `== 1` receipt would *pass on the broken tree and fail on the correct one*. `LiveRegion.test.tsx:51` asserts **equality**, which is the form that reddens. |
| `grep -c ErrorEnvelope LiveRegion.tsx` | **1** | **0** | Docblock line 29. Handled. |
| `grep -c scrubSeamError keys/[id]/permissions/route.ts` | **4** | **3** | Comment at `:27`. Matches the SUMMARY's stated 3. |
| `grep -cE rateLimitDenyJson keys/sync/route.ts` | **4** | **3** | Prose mention. Matches. |
| `grep -c "toBe(15)" seam-budgets.invariant.test.ts` | **4** | — | Confirmed. **DEF-16-2, already found and disclosed by plan 10** (`:136,:544,:876,:894` — one assertion, three comments citing it). |
| `grep -rlE 'from ".*resilient-fetch"' src/ tests/` | **10** | — | Plan 06 measured 9. The tenth is `seam-ratelimit-posture.invariant.test.ts`, created later **by this phase**. Legitimate in-phase drift, not a mis-measurement. |

`grep -cE 'toBeGreaterThanOrEqual\(CONTRACT_GUARDS'` → **0** and `grep -cE 'EXPECTED_RULES\s*=.*Object\.keys'` → **0** — Oracle-Independence hazards #2 and #3 are absent. `EXPECTED_TABLE_SIZE = <literal>` appears **2**× (hazard #1 intact).

**Conclusion:** the ninth instance (DEF-16-2 on `toBe(15)`) is the last one. I found no tenth.

### ℹ️ Finding 6 — a standing-constraint deviation nobody declared

`.gitignore` was changed in this phase (commit `5776e451`) to **un-ignore `.planning/`**. CONTEXT §8 states: *"`.planning/` is **gitignored**, `commit_docs: false` — never `git add .planning`."* The reversal carries a long, defensible rationale in the file itself (worktrees could not read their own PLAN.md; ledgers had no git backup). It is not a phase-goal issue and I am not calling it a gap — but `.gitignore` is in no plan's `files_modified`, and the constraint it overturns is one the CONTEXT marked binding. Flagging for the orchestrator's awareness only.

---

## Anti-patterns

| Class | Result |
|---|---|
| `TBD` / `FIXME` / `XXX` in the 72 phase-changed source files | **0** — debt-marker gate PASSES |
| `TODO` / `HACK` / `PLACEHOLDER` | **0** |
| Stubs / hardcoded empty data reaching a render | **none found.** The one *unwired* thing is Finding 1, and plan 13 named it in its own `## Known Stubs` section rather than hiding it |
| `vi.stubGlobal` (DEF-16-1) in new/modified test files | **0** in `resilient-fetch.test.ts`, `ratelimit.test.ts`, `seam-ratelimit-posture.invariant.test.ts`, `SyncPreviewStep.readfailure.runtime.test.tsx` |

---

## Residuals I could not close

1. **Node 22 CI-parity run not performed.** VALIDATION.md mandates `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` before declaring a wave green. I ran **local Node 25 only**. `DEF-140.4-A` records four files that 5 s-timeout-flake under Node 22 on a loaded machine, proven load-dependent and out of plan 02's fence. **The orchestrator should run the Node 22 pass before merge.**
2. **The SQL lane.** `psql` is not installed; `TEST_SUPABASE_DB_URL` is a write-only GH secret. M93, M94 and `supabase/tests/test_csv_finalize_double_submit.sql` are unrunnable here. The file is well-formed for the CI discoverer (`supabase/tests/test_*.sql`, **0** `psql` meta-commands, **13** `RAISE EXCEPTION`).
3. **`analytics-service/tests/test_raw_5xx_census.py` is unregisterable** in `CONTRACT_GUARDS` (every registered path is TS-side, runner is vitest). VALIDATION.md records this as a residual; it remains one.

---

## Bottom line

**The phase goal is substantially achieved and the guards genuinely fail.** 17 of 17 mutations I applied myself reddened, including every "second member" — which is the exact property phase 140 shipped without. The two flagship suspicions in the brief resolve in the phase's favour: **M99 and M100 both redden at HEAD against the widened class guard**, with the guard naming the file and line.

Two things are genuinely open: a **hand-off hole between plans 12 and 13** that leaves the honest configuration-fault copy unreachable on two wizard surfaces, and a **false "no row is possible" claim** that a named test refutes. Neither prevents the next phase from starting; both should be closed before the milestone audit reads this ledger as complete.

**C-4 and C-5 are NOT closed** and no plan claims they are — §7.2 and §7.3 are founder-owed preconditions, and D-01 remains the recorded blind spot.

---

_Verified: 2026-07-29T01:50:00Z_
_Verifier: Claude (gsd-verifier) — goal-backward, FORCE stance_
_Tree state after verification: `M TODOS.md` + `?? analytics-service/scripts/nautilus_factsheet.py`, both pre-existing. No residual mutation._
