# Phase 140.4 — SEAMRIM · CONTEXT (locked decisions)

**Written 2026-07-28 by the orchestrator, from the end-of-milestone review.**
There was no `discuss-phase` for this phase. **`.planning/reviews/140-SYNTHESIS.md` is the requirements
source**, backed by 16 registers in `.planning/reviews/`. Where a register and the synthesis conflict,
**the synthesis governs**; where the synthesis and this file conflict, **this file governs**.

---

## 1. Why this phase exists

The end-of-milestone review adjudicated **all 94** original Phase-140 findings at HEAD `a77d607e`:

| | CLOSED | PARTIAL | OPEN | SUPERSEDED |
|---|---|---|---|---|
| A — seam core | 22 | 5 | **0** | 2 |
| B — wizard/client | 10 | **14** | **4** | 0 |
| C — Python contract | 16 | 5 | 2 | 0 |
| D — harness integrity | 10 | 2 | 2 | 0 |

**The core is genuinely closed; the rim is not.** Two independent mutation samples (28 mutations,
isolated worktrees) measured **26 RED / 2 GREEN — 93% of sampled CLOSED verdicts genuinely guarded**.
Cluster B's CLOSED subset held **perfectly**; B's problem was never its CLOSED items, it is the 14
PARTIAL + 4 OPEN. **Scope this phase at the rim, not the core.**

---

## 2. ⚠️ THE COVERAGE LAW — this governs plan design, not just review

Measured across everything since Phase 140:

| fix mechanism | measured coverage |
|---|---|
| forced through a **shared artefact** (chokepoint / leaf / table / component) | **100%** |
| **hand-typed roster** or allow-list | 9/37 codes · 8/15 files · 2/3 codes |
| **per-site edit**, no artefact | 1/8 · 2/56 · **0/32** |

Coverage follows from the **mechanism**, not the effort. Every genuinely-closed class in this
programme used a chokepoint; every partial one used a hand-typed list.

**BINDING ON EVERY PLAN:** state which row your remedy lands in. A remedy in row 2 or 3 is
**partial by construction** and the plan must say so in those words. Prefer row 1 or justify why not.

---

## 3. ⚠️ A GREP PROVES A STATE; ONLY A GUARD PROVES THE STATE IS HELD

The single GREEN in mutation run 1 landed on the closure audit's **own receipt** — a one-time
`grep … → empty`. Changing one character class (`HTTPException(status_code=500 → 502)` at
`analytics-service/routers/portfolio.py:2326`, the **anonymous public teaser**) left **both full
suites byte-identical**. The 502/504 ban is real but lives in the `service_error()` constructor;
**9 sites raise `HTTPException` directly and bypass it**.

> **Every CLOSED verdict whose receipt was a named test or structural guard reddened.
> The one whose receipt was a measured command output did not.**

⇒ **No plan may close a finding with a grep as its receipt.** The receipt must be a test or a
structural guard that fails when the property is broken.

---

## 4. Verified findings (severity AFTER adversarial verification)

Three verifiers, in isolated worktrees, were told to **refute** and to default to refuted.
**Severities moved in both directions — use these, not the raw register severities.**

| id | verdict | note |
|---|---|---|
| **C-2** | **CRITICAL — CONFIRMED, could not be refuted on any sub-claim** | Migration `20260726000225` drops the platform-global `UNIQUE(wizard_session_id)`; `finalize_csv_strategy` mints a **fresh** `strategy_id` then inserts the SV row, so the new composite **can never collide**. CSV double-submit → **200 OK + duplicate `strategies` + duplicate `strategy_verifications`**, silent, logged `csv_finalize_ok`. **Both guards were removed in adjacent commits** — `5ad45c22` dropped the index, `ca9a9235` removed the Python `WIZARD_DUPLICATE` short-circuit as "a dead end". Pinned green by a passing test (TRAP-9). `CSV_SUBMIT_NO_STRATEGY_ID`'s remedy text tells the user *"Retry to confirm"* — instructing the duplicating action. **Auto-application of `supabase/migrations/**` on merge is a SETTLED founder decision and is working as intended — do NOT re-raise it as a risk.** It affects only WHEN this reaches prod, not whether it is a defect. The fix should still ship in the same PR as the migration, because shipping the migration without it is what introduces the duplicate-write path. **DO NOT REVERT the migration — it closes a real cross-tenant leak.** Fix = add `wizard_session_id` to the `strategies` INSERT so the already-live `strategies_user_wizard_session_uniq` partial index bites. Caveat: no live DB was available; verdict is structural (a fresh surrogate key cannot collide on a composite containing it), not executed. |
| **C-3** | **CRITICAL+ — CONFIRMED, re-graded UP, demonstrated by rendering** | `SyncPreviewStep.tsx:1053-1114`: **zero `.error` reads across all seven `Promise.all` members** (supabase-js 2.110.1 resolves rather than throws). Read-failed and genuinely-empty produce **byte-identical DOM**: *"We found only 0 filled trade(s) on this key."* **The under-pled half is worse:** a 2-day strategy correctly blocked as `INSUFFICIENT_DAYS` becomes **`passed: true`** when the `earliest`/`latest` reads fail — `spanDays !== null &&` skips the entire 7-day gate, **publishing an under-history track record as verified.** On a product whose core value is a trustworthy verified track record, this is the top item. Remedy must cover **all seven** reads. |
| **C-5** | **CONFIRMED on every link; mechanism worse than described** | `captureToSentry` count **0** in both `resilient-fetch.ts` and `ratelimit.ts`; `instrumentation.ts` calls `Sentry.init` with **no `integrations`**, so console is not captured — the breaker's only health sink is one `console.warn`. Counts exact: 15 seam rows, **3** call `isRateLimitMisconfigured`, **11** answer 429, 1 has no limiter. **The posture is outage-mode dependent, not simply "limiter closed / breaker open"**: `@upstash/ratelimit` races a 5 s timeout resolving `{success:true}` and `@upstash/redis` does not retry non-ok HTTP ⇒ HTTP-5xx → fail CLOSED; hanging store → fail **OPEN** +5 s latency; socket-refused → **non-deterministic**. `KEY_RATE_LIMIT` copy (*"exchange-side throttle and not a problem with your key"*) is emitted at **three** sites for **our own** limiter, contradicting `wizardErrors.ts:1320-1327`'s stated contract. First-click confirmed. |
| **C-4** | **HIGH — CONFIRMED but re-graded DOWN: the count is 1, not 9** | 11 codes reach the render and the allow-list is exactly 2 — both exact. But `ErrorEnvelope` renders only `Retry`/`Cancel` and **`request_call` renders no control**, so 8 of 11 are `[Retry, destructive]`. **Exactly one — `GATE_NO_DATA_SOURCE` — has the destructive control as its SOLE route.** The five-clicks mechanism **does** reproduce, and `isComposite` is false on every non-2xx kickoff. Sharpest unpled instance: **`COMPOSITE_MEMBERSHIP_UNKNOWN`** — a code about composites renders the single-key destructive control. `GATE_INSUFFICIENT_TRADES` renders *"Your draft is saved for 30 days"* beside the button that deletes it now; its `recoverable` is **mislabelled** (earned by `try_another_key`), not spurious. |
| **C-1** | **LOW — RE-GRADED DOWN from CRITICAL. Three sub-claims REFUTED by measurement.** | No real transport failure inlines the token (ECONNREFUSED/ECONNRESET/DNS/timeout/parse all give a constant `fetch failed`). `postProcessKey` classifies everything and **never rethrows** — run against real sockets, the catch arm never executed on any credential path. `captureToSentry` **scrubs unconditionally** at the chokepoint and `INTERNAL_API_TOKEN` is in `SEAM_SECRET_ENV_NAMES`; the `secrets:` arg is only for per-request secrets. **`grep -c scrubSeamError → 0` reproduces and is a FALSE SIGNAL.** The probe that "measured the token in the body" mocked the client wholesale. **What survives: raw internal prose in a 502 body + an unscrubbed `console.error`** — real hygiene (`bridge/route.ts:190-193` states the rule verbatim), same cheap fix, **not a credential incident.** |

⚠️ **Replication is NOT evidence.** C-1 was found by **five** independent registers and was still
substantially wrong — they read the same code and inherited the same premise, so replication counted
the *premise* five times. Weight demonstrations, not agreement.

---

## 5. Other confirmed items for the planner (from the synthesis, not re-verified individually)

- **`SEAM_FILES` should be DERIVED** from `SEAM_ROUTE_BUDGETS ∪ SEAM_EXCLUSIONS` (measured complete,
  20/20 over `src/app/api/**`) so a new seam route reddens the log guard the day it is written.
  Named **highest-leverage single change** in the review. The guard's own docblock argues against
  deriving it — the alternative already produced the 7-file hole.
- ~~**The "middle tier" gap**~~ — **REFUTED by the pattern-mapper (2026-07-28).** `SEAM_IMPORT_EDGE`
  already matches all three wrapper modules, so a new route consuming a seam wrapper **is** caught.
  Do not plan a remedy for this. (The residual worth keeping: the 15-row table is still
  asserted-nowhere as *complete* — that is WP-1's derived roster, not a separate gap.)
- **11 raw `HTTPException(status_code=5xx)` sites** bypass `service_error()`'s status ban (§3) —
  **NOT 9.** ⚠️ The review's count was produced by a **line-based grep**; two sites carry
  `status_code=` on a continuation line (`cron.py:630`, `debug_key_flow.py:99`). `job_worker.py:602`
  is excluded — it is inside a comment. **This is DEF-16-2 occurring inside the very population §3
  holds up as its cautionary example.** Treat it as the phase's standing warning about its own greps.
- **`role="alert"`**: three deletions, zero additions in-range; announcement was the **only** channel
  (focus is already on `<body>`); a11y splits perfectly — 100% of `ErrorEnvelope` consumers announce,
  0% of the hand-rolled error `<p>` surfaces do. **DESIGN.md §Error Envelope mandates it.**
  ⚠️ **The population is 27, not 6** (mapper, measured) — the review scoped to seam-adjacent surfaces;
  the class is app-wide. **Scope this deliberately in the plan and say where the fence is.**
- **Wire vs render vocabulary**: ~50–90 wire codes vs **6** translations. `draft_state_invalid` is
  **lowercase** — structurally incapable of matching either SCREAMING_SNAKE table. `CsvSubmitStep.tsx:39`
  types `code: string` with **no membership check**. Two phantom codes are branched on with no producer.
- **`140.3-01` ships with ZERO user-visible effect for the two codes it added** — `recogniseSeamErrorCode`
  translates them and then `KNOWN_FINALIZE_CODES` (a **second** 9-member allow-list in the same function)
  collapses them to `UNKNOWN`. The honest `RATE_LIMITED` copy (*"the cap is ours, not your exchange's"*)
  **exists and is unreachable**, while `KEY_RATE_LIMIT` — the one that blames the exchange — renders.
- **Comment/citation rot as a class**: 7 of 17 comment findings are **140.2 comments falsified by
  140.3 commits in the same range**; line-number citations are an unowned-count class (**19 checked,
  5 wrong**; three in-range commits each broke some, including our own `2d58fd45` whose stated purpose
  was *"a pointer cannot drift"*). `keys/[id]/permissions` documents a **"5 minutes"** cache; the
  literal is `revalidate: 60`. `sentry-capture.ts` claims *"the seam captures nothing to Sentry"*;
  it is **41 call sites across all 15 routes**, and was never true.
- **Contract registry understates its own guards**: *"exactly three predicates"* (it is five);
  *"the six seam files"* (the roster is eight, and the guard's own docblock says EIGHT).
- **`mintTenantClaim(payload: string, secret: string)`** — two adjacent same-typed strings;
  `TenantIdentity` exists 30 lines above and is **not applied**. **LATENT type hazard, NOT a live
  attacker path** (orchestrator-resolved; the attacker register's refutation was factually wrong).

---

### 5b. Pattern-mapper corrections (2026-07-28) — these OVERRIDE the synthesis

- **Unchecked Supabase reads: `32` is correct ONLY for `src/app/api/**`; repo-wide the population is
  **73**. The synthesis never stated the scope fence.** State yours explicitly.
- **The unchecked-read class has TWO carriers, not one.** Three of the "client members"
  (`MatchQueueIndex.tsx:147,:217`, `StrategyActions.tsx:41`) are unchecked **`fetch()`**, not Supabase.
  A `readOne` helper is **coverage-law row 1 for the Supabase carrier and row 3 for the fetch carrier** —
  the plan must say **"partial by construction"** for the second.
- **WP-1 (derive `SEAM_FILES`) is NOT a ~10-line wiring job.** The derived set is set-identical to
  `SEAM_ROUTE_BUDGETS` (15/15, both difference directions empty) — but porting the existing scanner
  over all 15 routes reddens **9 files / 17 sites** immediately, all 9 outside the current roster while
  all 5 rostered routes are clean. **Budget the tail or the plan lands a red tree.**
- **`SEAM_FILES` has zero size or set pin** — and the mapper measured the positive counterpart
  (8 mentions) so the empty grep is evidence, not a false signal (§3 discipline applied correctly).
- **Line-citation rot: 881 citations, 18 provably wrong** (they point past EOF) — a hard **lower bound**,
  since wrong-line-within-file is undetectable. **15 of the 18 are in two files outside 140.4's scope.**
- **1109 hand-typed count assertions**, 62 on seam/contract guards. `CONTRACT_GUARDS` = 44 rows against
  a `>= 20` floor ⇒ **24 deletable**. **16 unregistered guard-shaped files is a FLOOR, not the
  population** — two known guards match no naming convention, so this class **is not enumerable by
  filename**. Do not write an acceptance criterion that assumes it is.

**Analogs to copy from (all verified by the mapper at HEAD):** derived-roster shape =
`resilient-fetch.wiring.test.ts:735-798` (disk discovery → vacuity fence → three set equalities) ·
needle self-test = `seam-ssr-exposure.pin.test.ts:237-246` · positive counterpart =
`critical-regressions.test.ts:134` · census with quarantine allow-list = `test_limiter_identity.py:462-479`
(uses `==` against a frozenset, not containment) · **checked-read analog sits 200 lines ABOVE the C-3
defect in the same function — `SyncPreviewStep.tsx:848-867` — and its escalation sink already exists.**

---

### 5c. Researcher corrections + orchestrator reconciliation (2026-07-28) — HIGHEST PRECEDENCE

**⭐ C-3 ESCALATES: the same fail-open is at the ADMIN PUBLISH GATE, not just the wizard.**
`src/app/api/admin/strategy-review/route.ts:99-115` destructures **six** members and reads `.error` on
exactly **one** (`csvRowCount`). `earliestTrade`/`latestTrade` — the two driving the 7-day span gate —
have none, and the route writes `status: "published"` at `:175`. **ORCHESTRATOR-VERIFIED at source.**
The file's existing fail-loud guards defend the **rejection** direction; **nothing defends the
publishing one.** Researcher demonstrated by execution:
```
timestamps READ OK (2-day span): {"passed":false,"code":"INSUFFICIENT_DAYS",...}
timestamp reads FAILED         : {"passed":true,"code":null,...}
```
⇒ **an admin can publish an under-history track record as verified.** `checkStrategyGate` has exactly
**two** consumers; the remedy must cover both, and this one outranks the wizard surface.

**⭐ C-2's RECORDED FIX IS WRONG AND WOULD BREAK LEGITIMATE FIRST SUBMITS.** §4 says "add
`wizard_session_id` to the `strategies` INSERT so `strategies_user_wizard_session_uniq` bites."
`src/lib/wizard/localStorage.ts:377-379` restores `wizardSessionId` **unconditionally on source** from
one shared storage key — so an abandoned API draft collides with a later CSV finalize on
`(user_id, wizard_session_id)`, **permanently**, because retry reuses the same id.
**The index must be `(user_id, wizard_session_id, source)`.** API writers use `source='wizard'`, CSV
uses `'csv'`, so scoping costs nothing and removes the collision. **This supersedes §4's fix text.**
Classic TRAP shape: the naive fix breaks something else.

**Raw 5xx census — SETTLED BY ORCHESTRATOR MEASUREMENT: 12.** 11 `HTTPException` (multi-line-aware)
+ 1 `JSONResponse`. Review said 9, mapper 11, researcher 11+1. **It has now drifted three times inside
one programme — derive it, never transcribe it.**

**⚠️ UNCHECKED-SUPABASE-READ POPULATION IS DEFINITION-DEPENDENT — DO NOT PICK A NUMBER, STATE A
PREDICATE.** Three honest measurements disagree because they measured different things:
`32` (synthesis, `src/app/api/**` only) · `73` (mapper, repo-wide destructure-never-reads-`.error`) ·
`139` (researcher, 372 total sites − 233 checked; 125 reads + 14 silent writes).
**The plan MUST publish its predicate and its scope fence, and derive the number from them.** A plan
that quotes one of these three without its definition is repeating the defect this phase exists to fix.
The same applies to the WP-1 widening tail: mapper measured **9 files / 17 sites**, researcher
**8 routes / 12 sites**. Re-derive at plan time from the actual widened guard.

**`rateLimitDenyJson` ALREADY EXISTS** — 9 production call sites, 2 wrappers, and **zero on the seam**.
⇒ the C-5 remedy is **adoption of an existing chokepoint (coverage-law ROW 1)**, not 11 new predicate
calls (row 3). This materially reduces the work and improves the coverage.

**`ErrorEnvelope.code` is `string`, not `WizardErrorCode`** — only `buildEnvelope` is type-fenced. So
routing the hand-rolled surfaces through the component is type-possible, **but it is a VISUAL redesign
requiring founder approval of DESIGN.md.** ⇒ recommended remedy is a **visually-inert `<LiveRegion>`
primitive**, which closes the a11y class without touching the founder-owned decision. **Do not edit
DESIGN.md.**

**27 TS codes are lowercase, not one** — it is a two-vocabulary convention, not a defect. The sole
offender crossing into the wizard surface is TS-minted at `finalize-wizard/route.ts:1190`. Scope there.

**⚠️ The scrub predicate has a ONE-HOP ALIAS HOLE.** `csv-validate:262` binds `const message = err.message`
and both the researcher's scan and **the shipped guard** score that file **clean**. Following aliases
naively yields ~47 false positives from scope-blindness ⇒ **the predicate must be AST-scope-aware**, or
the plan must say it is partial by construction.

**H-13 "middle tier" — REFUTED INDEPENDENTLY BY BOTH mapper and researcher.** `SEAM_IMPORT_EDGE`
matches all three seam modules; derived set == `SEAM_ROUTE_BUDGETS`, **15 == 15, zero diff**, so the
closing assertion can be an equality with **zero slack today**. Do not plan a remedy for the gap.

**OPEN QUESTION the plan must carry, not resolve silently:** Sentry delivery without `withSentryConfig`
is **unverified** (researcher confidence LOW). It maps onto founder-owed item §7.3. **C-5 MUST NOT be
marked closed without it.**

---

## 6. Scope fences

**IN SCOPE:** the rim — cluster B's 14 PARTIAL + 4 OPEN, the verified C-2/C-3/C-4/C-5, the derived
`SEAM_FILES` roster, the middle-tier assertion, the raw-5xx census, a11y announcement, the wire↔render
vocabulary boundary, and the comment/citation-rot class.

**OUT OF SCOPE — do not let these leak in:**
- The seam **core** (cluster A, zero OPEN, 10/10 mutations caught). Do not re-open it.
- **Phase 141's retry work.** 141 owns retry; this phase must not add one.
- The three **founder-owed manual verifications** (§7) — they are not code.
- **`140.3-G3-DECISION-BRIEF.md`** — the DESIGN.md §Error Envelope rule is **type-impossible**
  (`buildEnvelope(code: WizardErrorCode)` vs 4 of 5 surface families having no such code).
  **Founder-owned normative decision. Do not edit DESIGN.md.**

**KNOWN BLIND SPOT:** the live-Redis lane (**D-01**, the register's "single most important closure")
was unrunnable in a worktree and is **unverified by mutation**. Neither sample speaks to it.

---

## 7. Owed by the founder (not code, do not plan around them)

The `140.3-16` checkpoint **auto-approved** under `workflow.auto_advance: true`. That is auto-approval,
**not confirmation**. Still owed:
1. Copy vs `DESIGN.md` §Voice (~12 sentences) — tests prove the false claims are gone, not that the replacements are good.
2. **TRAP-4 five clicks in a real browser** against a live composite draft. jsdom asserts the dialog's `open` attribute; seed-gated e2e specs skip.
3. **Sentry ingestion.** ⚠️ The stock instruction is WRONG — tripping the breaker captures **nothing by design** at all 15 routes. Force a terminal-arm failure (point `ANALYTICS_SERVICE_URL` at an unroutable host in preview).

---

## 8. Standing constraints

- **Branch protection on `main` is OFF until paying clients (SETTLED — do not re-raise).** Every CI
  gate is **advisory at merge** ⇒ write *"would have caught"*, **never** *"did stop"*.
- `.planning/` is **gitignored**, `commit_docs: false` — never `git add .planning`.
- **NEVER `gsd-sdk query state.*`** — recorded history of corrupting `.planning/STATE.md`, which is
  gitignored with **no git backup**. Orchestrator owns STATE.md and ROADMAP.md.
- **`vi.spyOn` + `restoreAllMocks`, NEVER `vi.stubGlobal`** (DEF-16-1 — this repo's known CI-only
  failure cause; CI Node 22 vs local Node 25).
- **Verify with the FULL suite, never a scoped path.** Five separate times in this programme a
  scoped-green run would have shipped a red tree (25 red, 8 red, and a rate-limit mutation GREEN on
  both files named "rate limit").
- **TRAP-1..TRAP-10 remain binding** (`.planning/phases/140-.../140-FINDINGS-CONSOLIDATED.md` PART 2).
  ⚠️ Two TRAP premises are now **stale**: TRAP-4 pins its hazard to *"denies codeless"* (closed by
  140.3-10, so a literal reading concludes it is discharged — that is exactly the reasoning that left
  the members unexamined), and TRAP-1 names two sibling files that are clean while the real leaks are
  in three it never names. **Restate both as properties, not instances.**
- Baselines: `npx vitest run` → **9792 passed | 287 skipped** (715 files) · `pytest` → **4778 passed /
  96 skipped** · `mypy --strict` clean · `tsc --noEmit` exit 0 · lint 0 errors (1 pre-existing warning).
