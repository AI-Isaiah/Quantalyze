---
phase: 140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open
plan: 08
type: execute
wave: 3
depends_on: ["140.4-06"]
files_modified:
  - src/app/api/admin/match/eval/route.ts
  - src/app/api/admin/match/recompute/route.ts
  - src/app/api/bridge/route.ts
  - src/app/api/portfolio-optimizer/route.ts
  - src/app/api/scenario/optimize/route.ts
  - src/app/api/simulator/route.ts
  - src/lib/ratelimit.ts
autonomous: true
requirements: [SEAMRIM-06]

must_haves:
  truths:
    - "The remaining six import-edge seam routes pass no caught value to a log unscrubbed"
    - "The limiter's own store-failure log stops shipping the Upstash Authorization header to the Vercel log"
    - "Every offence site closed here was enumerated by the guard's own predicate, not by a hand count"
  artifacts:
    - path: "src/lib/ratelimit.ts"
      provides: "a scrubbed store-failure log on the module every seam route calls first"
      contains: "scrubSeamError"
  key_links:
    - from: "the six routes + ratelimit.ts"
      to: "src/lib/seam-redaction.ts"
      via: "scrubSeamError at every console site standing over a caught or PostgREST error value"
      pattern: "scrubSeamError"
---

<objective>
Land the **second half of the scrub tail** — the remaining 12 of the 28 sites the derived roster
exposes — plus one extra offender found at plan time that **no derivation will ever reach**.

⚠️ **Pitfall 1 again:** land the scrubs **before** plan `140.4-10` widens the roster. *"Warning sign:
a commit that adds the derivation and an exclusion list in the same diff."*

## The 12 sites, RE-DERIVED at plan time from the guard's own predicate

Both halves of `src/lib/seam-log-coverage.test.ts` (catch-bound **and** the `error`/`*Err`/`*Error`
naming half) ported over the import-edge-derived roster. Plan `140.4-07` owns 15 sites at three files;
plan `140.4-09` owns 1 (an alias the current predicate cannot see). **This plan owns these 12:**

| file | sites (starting points — locate by TEXT) | identifier | half |
|---|---|---|---|
| `src/app/api/admin/match/eval/route.ts` | 185, 217, 245 | `err` | catch |
| `src/app/api/admin/match/recompute/route.ts` | 136, 172, 201 | `err` | catch |
| `src/app/api/bridge/route.ts` | 194 | `err` | catch |
| `src/app/api/portfolio-optimizer/route.ts` | 126, 236 | `err` | catch |
| `src/app/api/scenario/optimize/route.ts` | 198, 245 | `err` | catch |
| `src/app/api/simulator/route.ts` | 219 | `err` | catch |

Two of these are `err instanceof Error ? err.message : err` ternaries with **three** flagged references
in one call (`portfolio-optimizer`'s rate-limit-refund catch, and one on `recompute`). **`err.message`
is banned deliberately** — `message` is exactly where undici puts the outgoing headers.

## ⚠️ The 13th site: `src/lib/ratelimit.ts` — found at plan time, and NO derivation reaches it

Measured while sizing this plan:

```
src/lib/ratelimit.ts   console.error("[ratelimit] check failed:", err);   ← bare caught value
```

That `err` is an **`@upstash/redis` rejection**, and the store command carries
`UPSTASH_REDIS_REST_TOKEN` in an `Authorization` header — which undici inlines into the error it
produces. `resilient-fetch.ts`'s two store catches already scrub for exactly this reason and say so:
*"the store command carries `UPSTASH_REDIS_REST_TOKEN` in an Authorization header — which undici
inlines into the error it produces."* The sibling module does not.

**`ratelimit.ts` is not on the derived roster and never will be** — it does not import any of the three
seam modules, so the import edge cannot see it. It is a member of the class by behaviour, not by
topology. Surfacing it rather than silently omitting it is the point; plan `140.4-10` decides whether
it joins the hand-typed lib half of the roster, and this plan closes the leak either way.

## Coverage-law classification (CONTEXT §2 — binding)

**ROW 3 — a per-site edit with no artefact. PARTIAL BY CONSTRUCTION.** Measured coverage for that
mechanism in this programme: 1/8 · 2/56 · **0/32**. Acceptable only because the set is fully
enumerated from the guard's predicate and the row-1 artefact (the derived roster, plan `140.4-10`)
lands immediately after and fails on anything missed. **Say "partial by construction" in the SUMMARY,
in those words.** State separately that `ratelimit.ts` is **outside** even that artefact's reach, so
its fix is row 3 with no backstop at all until a roster decision is taken.

## TRAP-1 as a PROPERTY (CONTEXT §8 — its instance list is stale)

> **No caught value, nor any alias of one, may reach a log or a response body unscrubbed** — and the
> scrub must not over-redact: *"a short secret substring-matched against prose destroys the
> `ECONNREFUSED`/`ENOTFOUND` token — the most valuable thing in the line."*

**Do not answer any site by DROPPING the value.** That is the A-10 defect and the guard's own failure
message forbids it verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-CONTEXT.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-RESEARCH.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-PATTERNS.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-06-SUMMARY.md

<standing_constraints>
Binding on every task. You are a fresh agent with no memory of this phase's discussion.

- **Verify with the FULL suite, never a scoped path.** Baselines (CONTEXT §8): `npx vitest run` →
  **9792 passed | 287 skipped** (715 files) · `tsc --noEmit` exit 0 · `npm run lint` 0 errors.
  Plans in earlier waves may have moved the counts **up**; re-establish on your branch before editing.
- **`vi.spyOn` + `vi.restoreAllMocks()`. NEVER `vi.stubGlobal`** (DEF-16-1).
- **Zero new packages.**
- `.planning/` is gitignored with no git backup. Never `git add .planning`. Never `gsd-sdk query state.*`.
- **Branch protection is OFF** ⇒ *"would have caught"*, never *"did stop"*.
- **grep is LINE-BASED** (DEF-16-2), and these files' docblocks discuss the banned shapes — the guard
  comment-strips *before* matching for that exact reason. Comment-strip every count:
  `grep -vE '^\s*(//|\*|/\*)'`. **`grep -h` with a downstream filename filter is PROHIBITED.**
- ⚠️ **`src/lib/ratelimit.ts` was edited by plan `140.4-06`** (the timeout-sentinel read in
  `checkLimit`) and will be edited again by plan `140.4-13` (the deny builder) in a later wave. Read
  `140.4-06-SUMMARY.md` first and keep your diff to the store-failure `console.error` line.
- **Every pre-measured value here was taken on the untouched tree at HEAD `a77d607e`.** Re-run first.
</standing_constraints>

<read_first>
- `src/lib/seam-log-coverage.test.ts` — the predicate you are satisfying: `SCRUBBERS`,
  `SAFE_PROPERTIES` (`retryAfterS`, `deadlineExceeded`, `code` — all non-string facts from types we
  define), the `errorish` naming regex, and the `FAILURE_REASON` string that names the fix and forbids
  dropping the value.
- `src/lib/seam-redaction.ts` — `scrubSeamError(err, extraSecrets = [])`, `SEAM_SECRET_ENV_NAMES`,
  `SEAM_PRESERVE_TOKENS`, `MIN_REDACTABLE_SECRET_LENGTH`.
- `src/lib/resilient-fetch.ts`'s two store catches — **the exemplar for the `ratelimit.ts` fix**, with
  the comment explaining why the Upstash token is in the error. Copy the reasoning into your comment.
- `src/app/api/admin/match/eval/route.ts` and `src/app/api/scenario/optimize/route.ts` — measured at
  plan time: **both already import `@/lib/seam-redaction`** and still have unscrubbed sites. That is
  the instance-not-class shape in one file; it is why the acceptance check is the predicate and not
  an import grep.
- `src/app/api/bridge/route.ts` — read the docblock explaining why this route stays on plain
  `withAuth` with an INLINE limiter (*"converging onto withAuthLimited's default `rateLimitDenyJson`
  would drop this route's bespoke 429/503 copy AND regress the NO_STORE_HEADERS"*). **Do not act on
  it here** — that is plan `140.4-13`'s decision surface. It is listed so you do not "tidy" it.
- The six routes' colocated tests. Do not weaken any case.
</read_first>

<interfaces>
**The edit at each site:** wrap the offending identifier in `scrubSeamError(...)`, adding the
`@/lib/seam-redaction` import where absent. Measured at plan time: `admin/match/eval` and
`scenario/optimize` **already import it**; `admin/match/recompute`, `bridge`, `portfolio-optimizer`
and `simulator` **do not**.

**Ternaries:** replace the whole `err instanceof Error ? err.message : err` expression with a single
`scrubSeamError(err)` call. Scrubbing one arm and leaving the others still reports.

**Keep the operator's information** — ids, labels, budget keys, `reason` strings and an allowlisted
`.code` are not secrets and stay.

**No control-flow change.** No status, no body, no branch. Logging only.

**`ratelimit.ts`:** scrub the store-failure `console.error`'s caught value, with a comment naming the
Upstash `Authorization` header, mirroring the two `resilient-fetch.ts` store catches. Nothing else in
that file.

**Out of scope, stated so it is not smuggled in:**
- `src/lib/seam-log-coverage.test.ts` — plan `140.4-10` owns the roster. A commit that widens the
  roster alongside scrubs is the named warning sign.
- Any 429/503 deny-arm change, any `rateLimitDenyJson` adoption — plan `140.4-13`.
- `checkLimit`'s sentinel read — plan `140.4-06`, already landed.
- The `admin/match/*` 4xx-flattened-to-500 finding (B-12/B-24) — not this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the two admin/match routes — 6 sites, and two files that already import the scrubber</name>
  <files>src/app/api/admin/match/eval/route.ts, src/app/api/admin/match/recompute/route.ts</files>
  <read_first>
- Both files at their three sites each. Locate by text, not line — read each `catch (err) {` block and
  its `console.error` / `console.warn`.
- Note `eval/route.ts` **already imports** `@/lib/seam-redaction` and still has three unscrubbed
  sites: a scrubber in the file proves nothing about the sites in it.
  </read_first>
  <action>
Wrap all six offending `err` references. Add the import to `recompute` (it lacks one); verify
`eval`'s existing import rather than duplicating it.

For the `recompute` site whose call has multiple flagged references (an `instanceof Error` ternary),
replace the whole expression with one `scrubSeamError(err)`.

Keep every non-secret identifier already in the line. No control-flow change.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/app/api/admin/match/</automated>
  </verify>
  <acceptance_criteria>
- **Pre-values, comment-stripped, re-measure first:** `scrubSeamError` count in
  `admin/match/eval/route.ts` and `admin/match/recompute/route.ts` — record both, assert each rises by
  **≥ 3**.
- Re-run the guard's two-half predicate against both files: **0 offences**. Paste the output. This,
  not the grep, is the acceptance check.
- No value dropped: each of the six identifiers still reaches its line, wrapped. State them.
- `git diff` shows no status, body or branch change on either file.
  </acceptance_criteria>
  <done>
Both admin/match routes score zero offences under the guard's predicate, with every caught value still
present and wrapped.
  </done>
</task>

<task type="auto">
  <name>Task 2: bridge, portfolio-optimizer, scenario/optimize, simulator — 6 sites across four files</name>
  <files>src/app/api/bridge/route.ts, src/app/api/portfolio-optimizer/route.ts, src/app/api/scenario/optimize/route.ts, src/app/api/simulator/route.ts</files>
  <read_first>
- The four files at their six sites, located by text.
- `portfolio-optimizer`'s rate-limit **refund** catch — the one whose comment says the refund
  *"never shadows the original 5xx the caller is being told about."* Its call is
  `err instanceof Error ? err.message : err` — **three** flagged references in one call.
- `bridge`'s docblock about staying on an inline limiter. **Read it; do not act on it here.**
  </read_first>
  <action>
Wrap all six offending references, adding the `@/lib/seam-redaction` import to `bridge`,
`portfolio-optimizer` and `simulator` (measured: none of the three imports it); verify
`scenario/optimize`'s existing import.

Replace each `instanceof Error` ternary with a single `scrubSeamError(err)` call rather than scrubbing
one arm.

Keep the budget keys, ids and reason labels. No control-flow change, and **no rate-limit deny-arm
change** — that is plan `140.4-13`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/app/api/bridge/ src/app/api/portfolio-optimizer/ src/app/api/scenario/ src/app/api/simulator/</automated>
  </verify>
  <acceptance_criteria>
- **Pre-values, comment-stripped:** `scrubSeamError` count in each of the four files — record all
  four; the three without an import are **0** today.
- Re-run the guard's two-half predicate against the four files: **0 offences**. Paste the output.
- The `portfolio-optimizer` refund site: the SUMMARY quotes the line before and after and states that
  **all three** references were replaced by one call.
- No value dropped; no status, body or branch changed on any of the four.
- `grep -c "rateLimitDeny" src/app/api/bridge/route.ts` is **unchanged** from its pre-value — the
  bridge deny arm was not "tidied" into the shared builder (that is plan `140.4-13`'s decision).
  </acceptance_criteria>
  <done>
All four files score zero offences under the predicate, the multi-reference ternary is fully replaced,
and no deny arm was touched.
  </done>
</task>

<task type="auto">
  <name>Task 3: ratelimit.ts's store-failure log, and Falsifiability Ledger row M100</name>
  <files>src/lib/ratelimit.ts</files>
  <read_first>
- `.planning/phases/140.4-.../140.4-06-SUMMARY.md` — what plan `140.4-06` changed in this file.
- `src/lib/ratelimit.ts` — `checkLimit`'s catch and its `console.error("[ratelimit] check failed:", err)`.
- `src/lib/resilient-fetch.ts`'s two store catches — the exemplar and its stated reason.
- `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md` — the
  `## Falsifiability Ledger` rules block and rows M86–M89. **Read; do not edit.** Highest row in the
  programme: **M89**; plans 01–07 take M90–M99; this plan's row is **M100**.
  </read_first>
  <action>
**(a)** Wrap the caught value in `scrubSeamError(...)` at `checkLimit`'s store-failure log. Add the
`@/lib/seam-redaction` import. Write the comment in the shape `resilient-fetch.ts`'s store catches
use: name the `UPSTASH_REDIS_REST_TOKEN` `Authorization` header as the reason, and state that the
syscall token must survive (a refused store and an expired certificate are different incidents).

Change **nothing else** in this file: not the postures, not `checkLimit`'s return values, not the
deny builders. Two other plans own those.

**(b) Falsifiability Ledger row M100 — the scrub is LIVE at the SECOND member of the class.**
One mutation, **semantic**, to **PRODUCTION** source; never a syntax error, never a type error, never
a test edit. Record it under `## Falsifiability Ledger — row M100`.

The class is "a seam-route console site scrubbing a caught value". Member one is
`admin/match/eval`'s first site. **Mutate the second member**: unwrap
`src/app/api/scenario/optimize/route.ts`'s **second** site back to a bare `err`.

⚠️ Like M99, **this reddens nothing today** — `scenario/optimize` is not on `SEAM_FILES` until plan
`140.4-10` widens the roster. Record the row **honestly, in two parts**:
  1. **Observed now:** the FULL-suite result with the mutation applied. If green, say so plainly.
  2. **Observed against the widened predicate:** the plan-time predicate port (both halves, derived
     roster) with the mutation applied, pasted, showing the site reported. That is the *"would have
     caught"* half.

Do not claim a RED you did not observe. Do not pre-empt plan `140.4-10` by widening `SEAM_FILES`.

**Also record, explicitly:** `src/lib/ratelimit.ts` is a member of this class **by behaviour and not
by topology** — the import-edge derivation cannot reach it, so its fix has **no** structural backstop
until a roster decision is taken. Flag it to plan `140.4-10` in the SUMMARY as a decision it must make
(add to the hand-typed lib half, or record as a deliberate exclusion with a reason). **Do not decide
it here.**
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
- **Pre-value, comment-stripped:** `grep -vE '^\s*(//|\*|/\*)' src/lib/ratelimit.ts | grep -c "console.error"`
  → record it; and the count of bare-`err` console arguments in that file falls to **0** under the
  predicate.
- Re-run the guard's two-half predicate across **all seven files this plan owns**: **0 offences**.
  Paste the output. **This is the plan's real acceptance check** — 13 sites closed.
- `git diff` on `src/lib/ratelimit.ts` touches only the store-failure log line and its import — no
  posture, no return value, no deny builder. State this.
- Row M100 recorded in **two parts** with the words *"would have caught"*, stating it mutated the
  **second** member and naming the first.
- The SUMMARY carries the explicit hand-off note about `ratelimit.ts`'s topology for plan `140.4-10`.
- `git status --porcelain` after revert shows no residual mutation.
- **FULL suite green:** `npx vitest run` at or above the branch baseline (state exact numbers);
  `npx tsc --noEmit` exit 0; `npm run lint` 0 errors.
- CI-parity run: `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` green.
- `git diff --name-only` contains **zero** occurrences of `src/lib/seam-log-coverage.test.ts`.
  </acceptance_criteria>
  <done>
The limiter's store-failure log stops shipping the Upstash Authorization header, all 13 sites in this
plan score zero under the predicate, M100 records honestly what is observable today, and the
`ratelimit.ts` roster question is handed to plan `140.4-10` rather than decided silently.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| undici error → Vercel log | Outgoing headers are embedded in `err.message` and, in one shape, in `err.name` |
| `@upstash/redis` rejection → Vercel log | The store command carries `UPSTASH_REDIS_REST_TOKEN` in an `Authorization` header |
| import-edge derivation → the class | `ratelimit.ts` is in the class by behaviour and outside it by topology |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.4-08-01 | **Information disclosure** | 12 unscrubbed console sites across six import-edge seam routes | mitigate | `scrubSeamError` at each. **Row 3 — per-site, partial by construction**; the row-1 backstop is plan `140.4-10`'s derived roster |
| T-140.4-08-02 | **Information disclosure** | `ratelimit.ts`'s store-failure log leaking the Upstash token | mitigate | Scrubbed, mirroring `resilient-fetch.ts`'s two store catches. **Row 3 with NO structural backstop** — surfaced to plan `140.4-10` as a roster decision rather than silently omitted |
| T-140.4-08-03 | Repudiation | answering a scrub finding by DROPPING the value | avoid | The A-10 defect; each site must show the value still reaching the line, wrapped |
| T-140.4-08-04 | Tampering (class-not-instance) | scrubbing one arm of an `instanceof Error` ternary | avoid | Each ternary is replaced by one call; the acceptance check is the predicate, which reports residual references |
| T-140.4-08-05 | Tampering | widening the roster in the same commit as the scrubs | avoid | `seam-log-coverage.test.ts` excluded from `files_modified` and asserted absent from the diff |
| T-140.4-08-06 | Tampering | colliding with the other two plans that edit `ratelimit.ts` | avoid | The diff is fenced to the store-failure log line; `140.4-06` landed in an earlier wave and `140.4-13` runs in a later one |
| T-140.4-08-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed** (`140.4-RESEARCH.md` §Package Legitimacy Audit) |
</threat_model>

<verification>
- The guard's two-half predicate reports **0 offences** across all seven files. Paste the run.
- `npx vitest run` (FULL) at or above the branch baseline; `npx tsc --noEmit` exit 0; `npm run lint` 0 errors.
- `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` green.
- `src/lib/seam-log-coverage.test.ts` is **not** in the diff.
- `src/lib/ratelimit.ts`'s postures, return values and deny builders are unchanged.
- Zero Python files, zero `supabase/migrations/`, zero `.planning/` paths staged.
</verification>

<success_criteria>
- All 12 enumerated route sites plus `ratelimit.ts`'s store-failure log scrub, with no value dropped
  and no control flow changed.
- The guard's predicate scores all seven files at zero offences.
- M100 records the second-member mutation honestly in two parts, using *"would have caught"*.
- The `ratelimit.ts` topology question is handed to plan `140.4-10` in writing.
</success_criteria>

<output>
Create `.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-08-SUMMARY.md`.

Record: the 13 sites with identifier, file and before/after; which files needed the import added; the
pasted predicate run showing zero offences across all seven; the explicit **"partial by construction"**
statement; the `ratelimit.ts` hand-off note for plan `140.4-10`; and the
`## Falsifiability Ledger — row M100` two-part section.
</output>
