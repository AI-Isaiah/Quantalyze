---
phase: 140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open
plan: 11
type: execute
wave: 3
depends_on: ["140.4-02"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx
autonomous: true
requirements: [SEAMRIM-07]

must_haves:
  truths:
    - "No terminal error state offers an unconfirmed destructive control as its SOLE affordance"
    - "The destructive control renders only for codes that EARN it, derived from the code table, not from a hand-typed roster"
    - "A code whose only 'retry' re-evaluates an identical gate does not advertise a retry"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      provides: "the destructive-control decision inverted into a property of the code's actions"
  key_links:
    - from: "a WizardErrorCode's `actions`"
      to: "which controls render"
      via: "the destructive control is EARNED by an action, not exempted by a roster"
      pattern: "actions"
---

<objective>
Close **C-4** at the severity CONTEXT §4 actually assigns it, and close it as a **property** rather
than by widening a roster.

## ⚠️ The count is 1, not 9. Scope honestly.

CONTEXT §4: **HIGH — CONFIRMED but re-graded DOWN: the count is 1, not 9.** Independently re-derived
by the pattern-mapper and confirmed by the orchestrator.

11 distinct codes reach `setPhase("gate_failed")`. The allow-list `DESTRUCTIVE_CONTROL_IS_WRONG_FOR`
is exactly 2 (`GATE_DRAFT_GONE`, `VALIDATION_FAILED`). But `ErrorEnvelope` renders only **Retry**
(iff `envelope.recoverable && Boolean(onRetry)`) and **Cancel** (and `WizardErrorEnvelope`, a 7-line
re-export, passes no `onCancel`) — **`request_call` and `start_fresh` render NO control at all.** So 8
of the 11 render `[Retry, destructive]`, which is a bad affordance but not a sole one.

| # | code | `actions` | `recoverable` | in the allow-list | controls rendered |
|---|---|---|---|---|---|
| 1 | `SYNC_FAILED` | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive |
| 2 | `RATE_LIMITED` | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive |
| 3 | `GATE_DRAFT_GONE` | start_fresh, request_call | ✗ | **✔** | Link only — safe |
| 4 | **`COMPOSITE_MEMBERSHIP_UNKNOWN`** | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive |
| 5 | `VALIDATION_FAILED` | request_call | ✗ | **✔** | Link only — safe |
| 6 | `UNKNOWN` | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive |
| 7 | `KEY_NETWORK_TIMEOUT` | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive |
| 8 | `GATE_ANALYTICS_FAILED` | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive |
| 9 | `GATE_INSUFFICIENT_TRADES` | try_another_key, request_call | ✔ | ✗ | Retry + destructive |
| 10 | `GATE_INSUFFICIENT_DAYS` | try_another_key, request_call | ✔ | ✗ | Retry + destructive |
| 11 | **`GATE_NO_DATA_SOURCE`** | **start_fresh, request_call** | **✗** | **✗** | **destructive ONLY** |

**Exactly one member — `GATE_NO_DATA_SOURCE` — has the destructive control as its SOLE route.** Its
`actions` are **byte-identical to `GATE_DRAFT_GONE`'s** — the code the allow-list was created for —
which is precisely why it should have been in it.

**The sharpest unpled instance:** `COMPOSITE_MEMBERSHIP_UNKNOWN` — a code **about composites** —
renders the **single-key** destructive control, because `isComposite` is `false` on every non-2xx
kickoff, so the non-destructive `onReviewKeys` escape hatch is structurally unreachable on exactly the
arms where the destructive button appears.

**`GATE_INSUFFICIENT_TRADES` / `GATE_INSUFFICIENT_DAYS`:** `recoverable` is **MISLABELLED, not
spurious** — it is earned by `try_another_key`, while the handler `onRetry` actually receives is
`handleKickoffRetry`, which re-runs the kickoff and re-evaluates an **identical** gate. And
`GATE_INSUFFICIENT_TRADES`'s copy says *"Your draft is saved for 30 days"* **beside the button that
deletes it now.**

## The remedy: INVERT, do not widen

**Widening `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` to three members is coverage-law ROW 2 — a hand-typed
roster, partial by construction (measured 9/37 codes · 8/15 files · 2/3 codes).** And this range
already proves it: commit `39ea3b20`'s failure message instructing a future author to widen the list
*"IN THE SAME COMMIT"* was written **after** `RATE_LIMITED` had already been admitted without it.

**Inverting it — render the destructive control only for codes that EARN it — makes it a property of
the code table: ROW 1.** There is no roster left to forget to edit.

## TRAP-4, restated as a PROPERTY (CONTEXT §8 — its premise is STALE)

The register pins TRAP-4's hazard to *"`/api/keys/sync` … denies **codeless**"*, which `140.3-10`
closed — **so a literal reading concludes it is discharged, and that is exactly the reasoning that
left the members unexamined.** Restate and obey it as:

> **A terminal error render must not offer a destructive control as the sole affordance, nor place a
> retry beside one. This is a property of the RENDER, not of a code list — and treat `recoverable` as
> unreliable, because `try_another_key` earns it for codes whose only retry is destructive.**

## ⚠️ The founder-owed precondition, which this plan CANNOT discharge

CONTEXT §7.2 and the synthesis §7.1: **"TRAP-4: five clicks in a REAL browser against a live composite
draft"** is *"a precondition for closing WP-7, not a follow-up."* The `140.3-16` checkpoint
**auto-approved** under `workflow.auto_advance: true` — that is auto-approval, **not confirmation** —
and the allow-list was accepted as the discharge, which §2 C-4 shows it is not. jsdom asserts the
dialog's `open` attribute; the seed-gated e2e specs skip.

**This plan is code. It does not discharge that.** Record it in the SUMMARY under
`## OPEN — founder-owed precondition` and do **not** mark C-4 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@DESIGN.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-CONTEXT.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-RESEARCH.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-PATTERNS.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-02-SUMMARY.md

<standing_constraints>
Binding on every task. You are a fresh agent with no memory of this phase's discussion.

- **Read `DESIGN.md` before any visual decision** (CLAUDE.md). ⚠️ The `§Error Envelope` **visual
  contract** is founder-owned and **fenced off** (CONTEXT §6): `buildEnvelope(code: WizardErrorCode)`
  makes the "route everything through the component" rule type-impossible for 4 of 5 surface families.
  **Do not edit `DESIGN.md`. Do not change `ErrorEnvelope`'s visual contract.** This plan changes
  *which control renders*, not how any control looks.
- **Verify with the FULL suite, never a scoped path.** Baselines (CONTEXT §8): `npx vitest run` →
  **9792 passed | 287 skipped** (715 files) · `tsc --noEmit` exit 0 · `npm run lint` 0 errors.
  Plan `140.4-02` landed in this file first; re-establish the baseline on your branch.
- **`vi.spyOn` + `vi.restoreAllMocks()`. NEVER `vi.stubGlobal`** (DEF-16-1).
- **Zero new packages.**
- `.planning/` is gitignored with no git backup. Never `git add .planning`. Never `gsd-sdk query state.*`.
- **Branch protection is OFF** ⇒ *"would have caught"*, never *"did stop"*.
- **grep is LINE-BASED** (DEF-16-2). Comment-strip every count — this file's docblocks name the codes
  being discussed, and a raw `grep -c` on a code name counts prose.
  **`grep -h` with a downstream filename filter is PROHIBITED.**
- ⚠️ `src/lib/wizardErrors.ts` is **NOT** in `files_modified` and must not be edited here: plan
  `140.4-03` (wave 1) reconciled its copy and plan `140.4-12` (wave 4) owns its vocabulary. TRAP-8.
- **Every pre-measured value here was taken on the untouched tree at HEAD `a77d607e`.** Re-run first.
</standing_constraints>

<read_first>
- `src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx`, located by TEXT:
  - `const DESTRUCTIVE_CONTROL_IS_WRONG_FOR: readonly WizardErrorCode[] = [` and its docblock, which
    already says the flag *"is now about the PROPERTY … not about one code — an `===` against a single
    member is the instance-check this programme keeps finding."* **This plan finishes that sentence's
    argument: a roster of exemptions is still an instance list; the property is the code's `actions`.**
  - `const errorStateHidesDestructiveControl =` and the render block containing
    `errorStateHidesDestructiveControl ? (<Link href="/strategies" data-testid="wizard-back-to-strategies">…) : (<Button … data-testid="wizard-try-another-key">…)`.
  - `<WizardErrorEnvelope envelope={errorEnvelope} onRetry={errorEnvelope.recoverable ? handleKickoffRetry : undefined} />`.
  - `isComposite` and the `isComposite && onReviewKeys ? onReviewKeys : onTryAnotherKey` selection.
- `src/lib/envelope.ts` — `RECOVERABLE_ACTIONS = new Set(["clear_and_retry", "try_another_key"])` and
  `buildEnvelope`'s `recoverable: copy.actions.some((a) => RECOVERABLE_ACTIONS.has(a))`. **READ ONLY.**
- `src/components/error/ErrorEnvelope.tsx` — `const showRetry = envelope.recoverable && Boolean(onRetry);`
  and the `showWait` docblock, which is the model for *stating two load-bearing conditions and why each
  rejects an easier version*. **READ ONLY — do not change this component.**
- `src/lib/wizardErrors.ts` — the `actions` arrays for all 11 codes in the table above, and
  `gateFailureToWizardError`. **READ ONLY (another plan owns this file).**
- `src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx` — `onTryAnotherKey` →
  `void handleDeleteDraft()`, and `handleStartFresh`, which `140.3-10` deliberately routed **through a
  confirm dialog**. That asymmetry — one destructive path confirmed, the other not — is the third
  sub-finding.
- `SyncPreviewStep.render.test.tsx` — the existing gate-failure render cases. Do not weaken them.
</read_first>

<interfaces>
**Invert the decision.** Replace *"hide the destructive control for these two codes"* with *"render the
destructive control only when the code's `actions` earn it"*. The earning action is the one that
actually means *replace the key*: `try_another_key`. A code whose actions are `start_fresh` /
`request_call` / `clear_and_retry` has **not** earned a control that deletes the draft.

Derive it from the code's `actions` — the same source `recoverable` is derived from — so there is no
second list to maintain. Where a code earns nothing, render the existing non-destructive
`<Link href="/strategies">` affordance, which is already the state's own fix line
(*"Start a new strategy from the strategies page."*).

**Split `recoverable` from "a kickoff retry will help".** `onRetry` is `handleKickoffRetry`, which
re-runs the kickoff and re-evaluates an identical gate. For `GATE_INSUFFICIENT_TRADES` and
`GATE_INSUFFICIENT_DAYS` that is a false affordance. Pass `onRetry` only when a kickoff retry can
plausibly change the outcome — i.e. when the failure is transient — and let `ErrorEnvelope`'s existing
`showRetry = recoverable && Boolean(onRetry)` do the rest. **Do not change `ErrorEnvelope` and do not
change `RECOVERABLE_ACTIONS`** — both are shared artefacts owned elsewhere; this is a decision about
what this step passes.

**Confirm the destructive path.** Route the destructive control through the same confirm dialog
`handleStartFresh` already uses, or state in the SUMMARY precisely why that is not possible from this
component and what the residual is. **An unconfirmed action that cascades every `strategy_keys` member
is the most expensive control in the product.**

**Out of scope, stated so it is not smuggled in:**
- `ErrorEnvelope`'s visual contract, `DESIGN.md`, `buildEnvelope`, `RECOVERABLE_ACTIONS`.
- `src/lib/wizardErrors.ts` — copy and vocabulary (plans `140.4-03` / `140.4-12`).
- The wire→render translation at the kickoff arm (`KNOWN_KICKOFF_CODES`) — plan `140.4-12`.
- `isComposite`'s derivation. **Do not "fix" it to make `onReviewKeys` reachable** — that changes what
  the wizard believes about a draft mid-computation, which is a different finding with a different
  blast radius. Record it as a residual instead.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the destructive control must be EARNED — invert the roster into a property</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx</files>
  <read_first>
- The five anchors in `SyncPreviewStep.tsx` listed above, located by text.
- `src/lib/envelope.ts`'s `RECOVERABLE_ACTIONS` and `buildEnvelope` (READ ONLY).
- `src/lib/wizardErrors.ts`'s `actions` arrays for the 11 codes (READ ONLY).
- `WizardClient.tsx`'s `onTryAnotherKey` → `handleDeleteDraft` and `handleStartFresh`'s confirm dialog.
  </read_first>
  <behavior>
- **`GATE_NO_DATA_SOURCE` no longer renders the destructive control as its sole affordance.** It
  renders the non-destructive `<Link>`. *This is the one verified violation; it must be the first case
  you write and it must redden on the untouched tree.*
- `GATE_DRAFT_GONE` and `VALIDATION_FAILED` keep today's behaviour (Link only) — **through the new
  property, not through the deleted roster**. If they regress, the inversion mis-derives.
- `GATE_INSUFFICIENT_TRADES` and `GATE_INSUFFICIENT_DAYS` still offer the key-replacement route (they
  earn it — replacing the key is the genuine remedy for "this key has too little history"), but **no
  Retry**, because `handleKickoffRetry` re-evaluates an identical gate.
- `SYNC_FAILED`, `RATE_LIMITED`, `KEY_NETWORK_TIMEOUT`, `GATE_ANALYTICS_FAILED`, `UNKNOWN` keep their
  Retry and **no longer** render the destructive control (their actions do not include
  `try_another_key`).
- **`COMPOSITE_MEMBERSHIP_UNKNOWN`** — a code about composites — no longer renders the single-key
  destructive control.
- **The table-driven negative that names the property:** for **every** one of the 11 codes, the
  rendered control set is **never** exactly `{destructive}`. Drive all 11.
- The destructive control, wherever it renders, is confirmed before it fires.
  </behavior>
  <action>
Replace `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` and `errorStateHidesDestructiveControl` with a derivation
over the code's `actions`. Delete the roster — leaving it beside the property is how the next author
edits the wrong one.

Pass `onRetry` only when a kickoff retry can change the outcome. Write the reasoning in the shape of
`ErrorEnvelope`'s `showWait` docblock: state **both** load-bearing conditions and why each rejects an
easier version.

Route the destructive control through the confirm dialog `handleStartFresh` uses, or record the exact
residual.

Update the docblock: it currently defends a hand-typed exemption list. Rewrite it to state that the
decision is now a property of the code's `actions`; that the count of genuine sole-control violations
was **1** (`GATE_NO_DATA_SOURCE`), not 9; that `recoverable` is **mislabelled** rather than spurious
for the two history codes; and **restate TRAP-4 as the property**, striking the stale "denies codeless"
premise. Reference codes by name, never by line number.

⚠️ **Do not touch `wizardErrors.ts`, `envelope.ts` or `ErrorEnvelope.tsx`.** If the inversion appears
to need a change in one of them, stop and report — that is a different plan's surface.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/strategies/new/wizard/"</automated>
  </verify>
  <acceptance_criteria>
- **Pre-value, re-measure first (comment-stripped):**
  `grep -vE '^\s*(//|\*|/\*)' "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx" | grep -c "DESTRUCTIVE_CONTROL_IS_WRONG_FOR"`
  → **2** today (the declaration and its use). After the inversion → **0**. The roster is gone, not
  widened. ⚠️ Comment-stripping is load-bearing: the identifier appears in prose in this file.
- The `GATE_NO_DATA_SOURCE` case **fails on the untouched tree.** Paste the RED output.
- The 11-code table-driven negative exists and drives **all 11** codes. State the test name.
- `GATE_DRAFT_GONE` and `VALIDATION_FAILED` still render Link-only, **and the SUMMARY states that this
  is now derived rather than exempted** — if either regressed, the inversion mis-derives.
- `git diff --name-only` contains **zero** occurrences of `src/lib/wizardErrors.ts`,
  `src/lib/envelope.ts`, `src/components/error/ErrorEnvelope.tsx` and `DESIGN.md`.
- The destructive control is confirmed, or the SUMMARY names the exact residual and why.
- `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>
The destructive control renders only for codes whose `actions` earn it, the exemption roster is
deleted rather than widened, no code renders it as a sole affordance, the two history codes no longer
advertise a retry that cannot help, and no shared artefact was touched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pin the property across all eleven codes, and prove the escape hatch's unreachability is recorded</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx</files>
  <read_first>
- `SyncPreviewStep.render.test.tsx` — the existing gate-failure render cases and the harness that
  drives a code to `phase === "gate_failed"`.
- `src/components/error/ErrorEnvelope.tsx` — `showRetry`, and the fact that `request_call` and
  `start_fresh` render **no** control (so "no control" is a real render outcome, not a bug).
  </read_first>
  <action>
Write the table-driven pin. Its population must be **derived from the code table**, not hand-typed as
11 separate `it()` blocks: iterate the reachable codes and assert, for each, that the rendered control
set is never exactly `{destructive}`, and that the destructive control renders **only** when the code's
actions earn it. A hand-typed list of 11 cases is the roster problem in test clothing, and it is how a
12th code ships unexamined.

Add a **vacuity fence**: assert the driven population has at least 11 members, with a message saying a
scanner that drove nothing would report agreement forever. Copy the shape from
`resilient-fetch.wiring.test.ts`'s `toBeGreaterThanOrEqual(14)` fence.

Add the **positive counterpart**: at least one code (a history code) **does** render the
key-replacement control. A negative-only oracle is what M-9 measured going vacuous.

Add a case pinning that `GATE_INSUFFICIENT_TRADES` renders **no Retry**, referencing in its name the
reason (`handleKickoffRetry` re-evaluates an identical gate).

Record — in a docblock, not as an assertion — that `isComposite` is provably `false` on every non-2xx
kickoff, so `onReviewKeys` is structurally unreachable on exactly the arms where the destructive
button appeared; that this plan did **not** change `isComposite`'s derivation; and that the residual
is therefore *"a code about composites renders the single-key affordance"*, now non-destructive.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
- The population is **derived** from the code table: the test file contains no hand-typed array of 11
  code strings used as the population. State how it is derived.
- The vacuity fence exists (`>= 11`) with its "would report agreement forever" message.
- The positive counterpart exists.
- The `GATE_INSUFFICIENT_TRADES`-renders-no-Retry case exists and names its reason.
- `git diff -U0` shows **zero** removed `it(` lines from pre-existing cases, or each removal is listed
  with its strictly stronger replacement (TRAP-9).
- `grep -c "vi.stubGlobal" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx"` → **0**.
  </acceptance_criteria>
  <done>
A derived-population pin asserts across every reachable code that no terminal state offers a
destructive control as its sole affordance, with a vacuity fence and a positive counterpart, and the
`isComposite` residual is recorded rather than silently fixed.
  </done>
</task>

<task type="auto">
  <name>Task 3: falsify the property — row M103 — and record the founder-owed precondition</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx</files>
  <read_first>
- `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md` — the
  `## Falsifiability Ledger` rules block and rows M86–M89, **including M66c**, which is the prior
  mutation against this same flag under its old name. **Read; do not edit.** Highest row in the
  programme: **M89**; plans 01–10 take M90–M102; this plan's row is **M103**.
  </read_first>
  <action>
One mutation, **semantic**, to **PRODUCTION** source; never a syntax error, never a type error, never
a test edit. Record it under `## Falsifiability Ledger — row M103`.

**M103 — the property is LIVE for a code OTHER than the one that motivated it.**
The class is "a terminal code whose control set is decided by its actions". `GATE_NO_DATA_SOURCE` is
member one — the verified violation and the reason for the change. CONTEXT: **mutate the SECOND
member.** Choose `COMPOSITE_MEMBERSHIP_UNKNOWN` — the sharpest *unpled* instance — and mutate the
derivation so that code alone falls back to rendering the destructive control (e.g. add it to an
inline earned-set the property consults).

⚠️ This is the mutation that distinguishes a **property** from an **instance fix**: a change that only
special-cased `GATE_NO_DATA_SOURCE` would stay green here. Say so in the row.

Must redden: the table-driven pin, at the `COMPOSITE_MEMBERSHIP_UNKNOWN` row specifically. Record
whether it also reddens elsewhere; if it reddens everywhere, the pin is coupled and you must say so.
Must stay GREEN and record it: `GATE_NO_DATA_SOURCE`'s own case (proving the two are independent) and
the positive counterpart.

Record the exact diff, the command, pasted RED output with the failing test name and assertion text,
the GREEN-side observations, and `git status` clean after revert. Branch protection is OFF ⇒
*"would have caught"*, never *"did stop"*.

**Then record the precondition.** In the SUMMARY, under `## OPEN — founder-owed precondition`, state:
TRAP-4's five clicks in a **real browser** against a **live composite draft** remain owed
(CONTEXT §7.2); the `140.3-16` checkpoint **auto-approved** under `workflow.auto_advance: true`, which
is auto-approval and **not confirmation**; the allow-list was accepted as the discharge and §2 C-4
shows it is not one; jsdom asserts only the dialog's `open` attribute and the seed-gated e2e specs
skip. **C-4 must not be marked closed without it.** Also record the two residuals: `isComposite`'s
falsity on non-2xx kickoffs, and any un-confirmed destructive path you could not route through the
dialog.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
- Row M103 recorded with pasted RED output including the failing test name and assertion text.
- The row states that a change which only special-cased `GATE_NO_DATA_SOURCE` **would have stayed
  green** under this mutation — the distinction between a property and an instance fix.
- At least two named GREEN-side observations, one of them `GATE_NO_DATA_SOURCE`'s own case.
- `git status --porcelain` after revert shows no residual mutation.
- The SUMMARY contains the `## OPEN — founder-owed precondition` section with all four facts, and the
  two residuals.
- **FULL suite green:** `npx vitest run` at or above the branch baseline (state exact numbers);
  `npx tsc --noEmit` exit 0; `npm run lint` 0 errors.
- CI-parity run: `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` green.
  </acceptance_criteria>
  <done>
M103 proves the change is a property rather than a special case, and the founder-owed browser
verification is carried forward as an unmet precondition rather than silently treated as discharged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a terminal error render → the user's only way forward | The offered control is the user's entire option set; a destructive sole affordance is a forced choice |
| `onTryAnotherKey` → `handleDeleteDraft` | An unconfirmed action that cascades every `strategy_keys` member — the most expensive control in the product |
| a code's `actions` → what renders | The only non-roster source of truth about what a code means |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.4-11-01 | **Denial of service (user data)** | `GATE_NO_DATA_SOURCE` rendering the draft-deleting control as its sole affordance | mitigate | The control is derived from the code's `actions` — **row 1**, a property, not a widened roster (which would be row 2, partial by construction). Falsified by M103 |
| T-140.4-11-02 | **Denial of service (user data)** | `COMPOSITE_MEMBERSHIP_UNKNOWN` rendering the single-key destructive control | mitigate | Covered by the same property; M103 mutates exactly this member to prove it is not a special case |
| T-140.4-11-03 | Repudiation | a Retry that re-evaluates an identical gate | mitigate | `onRetry` is passed only when a kickoff retry can change the outcome; a named case pins `GATE_INSUFFICIENT_TRADES` rendering none |
| T-140.4-11-04 | **Denial of service (user data)** | an unconfirmed destructive action | mitigate | Routed through the confirm dialog `handleStartFresh` already uses, or the exact residual is recorded |
| T-140.4-11-05 | Tampering (design authority) | drifting into the founder-owned §Error Envelope decision | avoid | `ErrorEnvelope`, `buildEnvelope`, `RECOVERABLE_ACTIONS` and `DESIGN.md` are excluded from `files_modified` and asserted absent from the diff |
| T-140.4-11-06 | Repudiation | marking C-4 closed on an auto-approved checkpoint | avoid | The `## OPEN — founder-owed precondition` section records that auto-approval is not confirmation |
| T-140.4-11-07 | Tampering (guard decay) | a hand-typed 11-case test that a 12th code slips past | avoid | The pin's population is derived from the code table, with a vacuity fence and a positive counterpart |
| T-140.4-11-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed** (`140.4-RESEARCH.md` §Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run` (FULL) at or above the branch baseline; `npx tsc --noEmit` exit 0; `npm run lint` 0 errors.
- `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` green.
- `src/lib/wizardErrors.ts`, `src/lib/envelope.ts`, `src/components/error/ErrorEnvelope.tsx` and
  `DESIGN.md` are **not** in the diff.
- Zero Python files, zero `supabase/migrations/`, zero `.planning/` paths staged.
</verification>

<success_criteria>
- The exemption roster is deleted and the destructive control is derived from the code's `actions`.
- No reachable terminal code renders a destructive control as its sole affordance; the two history
  codes no longer advertise a retry that cannot help; the destructive path is confirmed or the residual
  is recorded.
- The pin's population is derived, fenced against vacuity, and carries a positive counterpart.
- M103 reddens on the second member, proving a property rather than a special case.
- The founder-owed browser precondition is recorded as unmet; C-4 is not marked closed.
</success_criteria>

<output>
Create `.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-11-SUMMARY.md`.

Record: the 11-code table as it stands after the change (code → actions → controls rendered), with the
before/after for each; the pre/post `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` counts; the RED output for the
`GATE_NO_DATA_SOURCE` case against the untouched tree; the rewritten docblock verbatim including
TRAP-4 restated as a property; how the pin's population is derived; the confirm-dialog outcome or its
residual; the `isComposite` residual; the `## Falsifiability Ledger — row M103` section; and the
`## OPEN — founder-owed precondition` section.
</output>
