---
phase: 140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/tests/test_raw_5xx_census.py
autonomous: true
requirements: [SEAMRIM-09]

must_haves:
  truths:
    - "A new raw 5xx raised outside the error contract fails a named test on the day it is written"
    - "Flipping a quarantined site's status from 500 to 502 reddens — the mutation that was GREEN in this programme"
    - "The census names its own blind spot rather than implying completeness"
  artifacts:
    - path: "analytics-service/tests/test_raw_5xx_census.py"
      provides: "an ast-derived census equal to a shrinking quarantine, with a two-polarity needle self-test"
      contains: "QUARANTINE"
  key_links:
    - from: "analytics-service/routers/**, services/**, main.py"
      to: "the quarantine mapping"
      via: "an ast walk over HTTPException/JSONResponse literal statuses >= 500, compared with =="
      pattern: "ast.walk"
---

<objective>
Install the receipt for CONTEXT §3's own cautionary example.

> *"The single GREEN in mutation run 1 landed on the closure audit's **own receipt** — a one-time
> `grep … → empty`. Changing one character class (`HTTPException(status_code=500 → 502)` at
> `analytics-service/routers/portfolio.py:2326`, the **anonymous public teaser**) left **both full
> suites byte-identical**."*

The 502/504 ban is real and it is strong — but it lives inside `service_error()`'s `_validate`
(`analytics-service/services/error_contract.py:160-163`), reachable only through `service_error` /
`service_error_response`. **A direct `raise HTTPException(...)` never reaches it.** The gap is purely
*reach*, and nothing measures the reach.

## The census, DERIVED at plan time — and it has drifted three times, so derive it, never transcribe it

CONTEXT §5c settled the count at **12**. Re-derived independently at plan time with a Python `ast`
walk over `analytics-service/routers/**`, `services/**` and `main.py`, excluding `tests/`, collecting
every `HTTPException(...)` and `JSONResponse(...)` whose status is an **integer literal ≥ 500**
(keyword `status_code=` or first positional):

```
TOTAL raw 5xx construction sites: 12          (11 HTTPException + 1 JSONResponse)
  main.py:867                     JSONResponse  503   health
  routers/cron.py:613             HTTPException 500   cron_sync
  routers/cron.py:630             HTTPException 500   cron_sync          ← line-based grep MISSES this
  routers/csv.py:101              HTTPException 500   csv_validate
  routers/debug_key_flow.py:56    HTTPException 503   _verify_internal_token
  routers/debug_key_flow.py:99    HTTPException 503   _read_test_creds   ← line-based grep MISSES this
  routers/exchange.py:681         HTTPException 503   fetch_trades
  routers/exchange.py:719         HTTPException 500   fetch_trades
  routers/exchange.py:781         HTTPException 500   fetch_trades
  routers/portfolio.py:2326       HTTPException 500   verify_strategy    ← the M11 canary, ANONYMOUS public teaser
  routers/portfolio.py:2550       HTTPException 500   verify_strategy
  services/analytics_runner.py:1725 HTTPException 500 run_csv_strategy_analytics
```

The review said **9**, the mapper **11**, the researcher **11 + 1**. The review's 9 came from a
**line-based** grep and missed two `status_code=` continuation lines — **DEF-16-2 occurring inside the
very population CONTEXT §3 holds up as its cautionary example.** `services/job_worker.py:602` is
excluded: `HTTPException(500)` there is inside a `#` comment (verified by reading `:596-606`).

## ⚠️ A `frozenset` of `(module, function, status)` triples is NOT sufficient — measured

The 12 sites collapse to **9 distinct triples**, because three functions each raise the same status
twice:

| triple | count |
|---|---|
| `("analytics_runner.py", "run_csv_strategy_analytics", 500)` | 1 |
| `("cron.py", "cron_sync", 500)` | **2** |
| `("csv.py", "csv_validate", 500)` | 1 |
| `("debug_key_flow.py", "_read_test_creds", 503)` | 1 |
| `("debug_key_flow.py", "_verify_internal_token", 503)` | 1 |
| `("exchange.py", "fetch_trades", 500)` | **2** |
| `("exchange.py", "fetch_trades", 503)` | 1 |
| `("main.py", "health", 503)` | 1 |
| `("portfolio.py", "verify_strategy", 500)` | **2** |

A set equality over triples is **satisfied by deleting one of a duplicated pair** — a broken tree
returns the same value as a correct one, which is the falsification question CONTEXT demands you ask
of every criterion. **The quarantine must be a multiplicity-preserving mapping (a `Counter` /
`dict[triple, int]`) compared with `==`.**

## Coverage-law classification (CONTEXT §2 — binding)

- **ROW 1 for the population** — it is derived from source by an `ast` walk, so a site added tomorrow
  in a file nobody thought of is inside the census by construction.
- **ROW 2 for the quarantine mapping** — a hand-typed roster, and therefore **partial by
  construction**. It is acceptable here, and only here, because it is a roster of **known offenders**
  (which can only shrink) rather than of **protected sites** (which could silently omit one), and
  because the comparison is `==` rather than containment: *"If it is ever repaired, SHRINK this
  allow-list; the equality below is deliberate so a repair cannot leave a stale exemption behind"* —
  `analytics-service/tests/test_limiter_identity.py:468-469`.

## Scope fence

**This plan writes a test. It changes ZERO production Python.** Re-homing sites onto `service_error()`
is deliberately **not** bundled — TRAP-8 (*"two fixes touching the same file in one pass reintroduced
each other's bugs"*). The census is what makes a later re-homing verifiable, one site at a time.

Out of scope and explicitly not touched: the 71 raw Python **4xx** sites (synthesis §8 —
`CVI RIM-7` falsified the fence and does not call them a defect), and any change to
`error_contract.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-CONTEXT.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-RESEARCH.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-PATTERNS.md

<standing_constraints>
Binding on every task. You are a fresh agent with no memory of this phase's discussion.

- **Verify with the FULL suite, never a scoped path.** Baselines (CONTEXT §8):
  `cd analytics-service && python3 -m pytest -q` → **4778 passed / 96 skipped** · `mypy --strict`
  clean · `npx vitest run` → **9792 passed | 287 skipped** · `tsc --noEmit` exit 0 · lint 0 errors.
  ⚠️ **Mutation M8 in run 2 was GREEN on both files named "rate limit" and RED only on the full pytest
  run.** Re-establish the baseline before editing.
- **Run mypy before declaring green:** `cd analytics-service && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/`.
- ⚠️ **A local red in `csv_validator` / `middleware` / `mt5` is a missing `pandera`, not a bug.**
  `pip install 'pandera==0.32.1' --break-system-packages`.
- **Zero new packages.** `ast` is stdlib; **14 test files in this suite already `import ast`**.
- `.planning/` is gitignored with no git backup. Never `git add .planning`. Never `gsd-sdk query state.*`.
  Never edit `.planning/STATE.md` or `.planning/ROADMAP.md`.
- **Branch protection is OFF** ⇒ *"would have caught"*, never *"did stop"*.
- **grep is LINE-BASED** — that is the defect this plan exists to fence. Do not size, verify or assert
  anything here with a line regex. **`grep -h` with a downstream filename filter is PROHIBITED.**
- **Every pre-measured value here was taken on the untouched tree at HEAD `a77d607e`.** Re-run first.
</standing_constraints>

<read_first>
- `analytics-service/tests/test_limiter_identity.py:140-155` and `:457-491` — **the analog, verified
  as cited.** Two halves, and only one transfers:
  - **TRANSFERS:** `assert offenders == IP_KEYED_QUARANTINE` — an **equality**, with `:468-469`
    spelling out why containment would be wrong. Copy this exactly.
  - **DOES NOT TRANSFER:** `_source_name_tokens` (Python `tokenize`, NAME tokens only). That is the
    right tool for *"does this file reference this symbol"* and the **wrong** tool for ours, which is
    about a **value**: `HTTPException` appears at many legitimate 4xx sites, and `500`/`502` are
    NUMBER tokens with no association to the call they belong to.
  - Also read `:558-568` — a set equality whose failure message prints **both** `unexpected=` and
    `missing=`.
- `analytics-service/services/error_contract.py:150-216` — `_validate`, and the message
  *"the only permanent 5xx in this contract is 500, got 502; a transient fault is 503, an exchange
  fault is 424"*. This is the rule the census measures the reach of. **READ ONLY.**
- `src/lib/seam-ssr-exposure.pin.test.ts:237-246` — **the needle self-test template, verified as
  cited: 4 assertions, 2 polarities.** `:242-243` asserts `true` on two real forms; `:244-245` asserts
  `false` on a prose mention and a non-matching form. `:238-241` states why the negative half is
  load-bearing: *"a scan that flagged those would be 'fixed' by weakening it."*
- `src/__tests__/critical-regressions.test.ts:131-144` — the positive-counterpart discipline: assert
  the good thing is PRESENT beside asserting the bad thing is ABSENT. A negative-only oracle is what
  M-9 measured going vacuous.
- Two or three of the 12 sites in situ, at least `routers/portfolio.py:2326` (the anonymous public
  teaser, the M11 canary) and `routers/cron.py:630` (the continuation-line site a line grep misses).
  </read_first>

<interfaces>
**The scanner.** Python `ast`. Walk `routers/`, `services/` and `main.py`, skipping anything under a
`tests` path segment. For every `ast.Call` whose `func` is a `Name` in `{"HTTPException",
"JSONResponse"}`, resolve the status from a `status_code=` keyword **or** the first positional
argument, accepting only `ast.Constant` integers. Keep those `>= 500`. Key each hit on
`(module_basename, enclosing_function_name, status)` — resolved by walking parent links, **never on a
line number**: P2 measured 19 citations checked / 5 wrong, and three in-range commits each broke some,
including `2d58fd45` whose stated purpose was *"a pointer cannot drift"*.

**The comparison.** `collections.Counter` of those keys, `==` a hand-typed `QUARANTINE` Counter. Not a
set. Not containment. The failure message prints `unexpected=` and `missing=` separately.

**The status assertion.** Every quarantined member must be **500** — the only permanent 5xx the
contract allows — **or** carry an explicit per-member reason string for a non-500. Today four members
are 503 (`debug_key_flow` ×2, `exchange.py:681`, `main.py`'s `/health`). List each with its reason;
do not silently accept them.

**The blind spots, asserted rather than implied. THERE ARE TWO, and each constant is meaningless
without the predicate that produced it.** CONTEXT §5c: *a population is definition-dependent — state a
predicate, do not pick a number.* A bare integer is unowned by construction.

**THE SCANNER'S PREDICATE, stated exactly (this is the definition both constants are measured under):**
> An `ast.Call` whose `func` is an **`ast.Name`** with `id ∈ {"HTTPException", "JSONResponse"}` — i.e. a
> **direct constructor call by that bare name** — found by walking `routers/**/*.py`,
> `services/**/*.py` and `main.py`, skipping any path containing a `tests` segment.

Two consequences follow, and both must be written into the docstring:

**(a) `super().__init__(status_code=…)` is NOT a matched construction.** Its `func` is an
`ast.Attribute`, not an `ast.Name`. The site that makes this load-bearing is
`services/error_contract.py`'s `VenueTransientHTTPException.__init__`, which calls
`super().__init__(status_code=status_code, detail=detail)`.
⇒ **Non-literal statuses on MATCHED constructors: 3** — keyed line-free as
`("error_contract.py", "service_error", "HTTPException")`,
`("error_contract.py", "service_error_response", "JSONResponse")` and
`("main.py", "venue_transient_exception_handler", "JSONResponse")`. All three are legitimate
pass-throughs of an already-validated status.
⚠️ **Under a LOOSE reading — "any `status_code=` keyword anywhere, any callee" — the answer is 4**, the
fourth being that `super().__init__`. Both measurements are honest; they measure different things.
**Pin 3 with the strict predicate beside it**, or the constant is a number an executor cannot reproduce.

**(b) Subclass constructions are not matched at all.** `VenueTransientHTTPException` is the one
`HTTPException` subclass defined in-tree, and it is constructed at **7** sites, **all `status_code=424`**
— a 4xx, so outside this census's `>= 500` scope today. **The gap is latent, not live**, and it must be
pinned as such: a subclass site acquiring a 5xx would be invisible to the census. Assert both the
subclass count (**1**) and the construction-site count (**7**) so that change forces a decision.

Record each number as a **hand-typed constant with its predicate in the docstring**, and assert it — so
a fourth non-literal, or a subclass site crossing 500, reddens. A guard that implies completeness it
does not have is worse than none.

**Out of scope, stated so it is not smuggled in:** re-homing any site onto `service_error()`; the
Python 4xx sites; `error_contract.py`; any TypeScript file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the ast census, equal to a multiplicity-preserving quarantine, with a two-polarity needle self-test</name>
  <files>analytics-service/tests/test_raw_5xx_census.py</files>
  <read_first>
- `analytics-service/tests/test_limiter_identity.py:140-155`, `:457-491`, `:558-568`.
- `analytics-service/services/error_contract.py:150-216`.
- `src/lib/seam-ssr-exposure.pin.test.ts:237-246` (needle self-test shape, both polarities).
  </read_first>
  <behavior>
- Run against the untouched tree: the derived Counter **equals** the hand-typed QUARANTINE (12 sites,
  9 distinct triples, three of them with count 2).
- A **new** raw `HTTPException(status_code=500)` added in any scanned file → `unexpected=` non-empty → RED.
- A quarantined site **removed** (re-homed onto `service_error()`) → `missing=` non-empty → RED, with a
  message telling the author to SHRINK the quarantine.
- **One of a duplicated pair deleted** (e.g. one of `cron.py`'s two) → RED. *This is the case a
  `frozenset` cannot see, and the reason the quarantine is a Counter.*
- A quarantined site's status flipped 500 → 502 → RED on **both** the Counter equality and the
  status assertion.
- Needle self-test, four assertions, two polarities, over string samples parsed in-test:
  `raise HTTPException(status_code=500, …)` → detected · a status on a **continuation line** →
  detected (the shape a line grep misses) · `HTTPException(500)` positional → detected · a
  `# HTTPException(500)` **comment** → NOT detected · `HTTPException(status_code=404)` → NOT detected.
- Vacuity fence: the scanner visited `>= 30` files and found `>= 1` `HTTPException` call of **any**
  status. A scanner that matched nothing would report agreement forever.
- The non-literal blind-spot count equals its hand-typed constant (**3**) **under the stated strict
  predicate**, and the docstring states that a loose "any callee" reading yields 4 and why they differ.
- The subclass blind spot is pinned: **1** in-tree `HTTPException` subclass (`VenueTransientHTTPException`)
  constructed at **7** sites, **all 424**. A subclass site acquiring a `>= 500` literal reddens.
  </behavior>
  <action>
Write `analytics-service/tests/test_raw_5xx_census.py`.

Module docstring must state, in this order: what property this pins; that the ban lives in
`_validate` and is reachable only through `service_error` / `service_error_response`, so a direct
`raise` bypasses it; **that this test exists because a 500→502 flip at the anonymous public teaser
left both full suites byte-identical (ledger row M11)**; that the quarantine is a roster of KNOWN
OFFENDERS which may only shrink; and what the census cannot see (non-literal statuses, dynamically
constructed exceptions, anything outside the three scanned roots). Reference every code location by
**module and function name**, never by line number.

Implement the scanner and the assertions per `<interfaces>`. Hand-type the QUARANTINE from the table
in this plan's objective — then **re-derive it from the tree and diff the two before you commit**. If
they disagree, the tree moved: report the difference, do not silently adopt the tree's answer (that
would make the roster a copy of the thing it measures, which is a self-referential oracle — the exact
defect `feedback_economic_invariant_oracles_not_self_referential` records).

Give each 503 member an explicit reason string in a companion mapping, so the status assertion reads
as a decision rather than an exemption.

Add the needle self-test and the vacuity fence as separate named tests.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest -q tests/test_raw_5xx_census.py && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <acceptance_criteria>
- **Pre-value, re-measure before you start.** Run the plan's own AST derivation (or your scanner in
  isolation) against the untouched tree: **12** sites, **9** distinct triples, and the three
  count-2 triples exactly as listed. If your number differs, STOP and report — do not adjust the
  plan's number silently. This is the phase's standing warning about its own greps.
- The quarantine is multiplicity-preserving: `grep -c "Counter" analytics-service/tests/test_raw_5xx_census.py`
  → **≥ 1**, and the census asserts its own type **in-test**: `assert isinstance(QUARANTINE, Counter)`.
  ⚠️ **Do NOT assert `grep -c "frozenset" … → 0`.** This plan's `<interfaces>` *requires* a docstring
  explaining why a set is insufficient, so any honest docstring contains the word and fails the grep —
  a criterion that punishes the documentation it mandates. The in-test `isinstance` check is
  falsifiable, comment-proof, and runs on every invocation.
  (Falsification check: a `frozenset` implementation passes with one of `cron.py`'s two sites deleted.
  A Counter does not — and M95 exercises exactly that.)
- The comparison is an equality, not containment: the file contains `== QUARANTINE` (or the same
  shape) and contains **no** `<=` / `issubset` over the census.
- The needle self-test has **both** polarities: at least 3 positive-detection assertions and at least
  2 negative assertions, one of which is a commented-out raise. Name them in the SUMMARY.
- The continuation-line sample is present and detected — the shape that produced the review's wrong
  count of 9.
- The vacuity fence exists and its two lower bounds are stated in the SUMMARY.
- **The docstring states the scanner's predicate in full** — node type (`ast.Call` with an `ast.Name`
  func), the two matched names, the three walked roots, the `tests` exclusion — and names
  `VenueTransientHTTPException.__init__`'s `super().__init__` as the site that makes the
  `ast.Name`-vs-`ast.Attribute` distinction load-bearing.
- Both blind-spot constants are hand-typed with their predicates beside them: non-literal **3**,
  subclasses **1**, subclass construction sites **7**.
- `cd analytics-service && python3 -m pytest -q` at or above **4778 passed / 96 skipped**; state exact
  numbers. `mypy --strict` clean.
- `git diff --name-only` contains **zero** production Python files — this task adds a test only.
  </acceptance_criteria>
  <done>
An `ast`-derived census equals a multiplicity-preserving quarantine of known offenders, every member's
status is 500 or a listed 503 with a reason, the needle proves itself in both polarities, the scanner
proves it is not vacuous, and the blind spot is asserted rather than implied.
  </done>
</task>

<task type="auto">
  <name>Task 2: falsify the census — Falsifiability Ledger rows M95, M95b and M108, including a re-run of the mutation that was GREEN</name>
  <files>analytics-service/routers/portfolio.py, analytics-service/routers/exchange.py</files>
  <read_first>
- `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md` — the
  `## Falsifiability Ledger` rules block and rows M86–M89. **Read; do not edit.** Highest row in the
  programme: **M89**; plans 01–03 take M90–M94; this plan's rows are **M95** and **M95b**.
  </read_first>
  <action>
Three mutations, one at a time, each reverted before the next. Each is a **semantic** edit to
**PRODUCTION** source; never a syntax error, never a type error, never a test edit. Record all three in
this plan's SUMMARY under `## Falsifiability Ledger — rows M95, M95b, M108`.

**M95 — the status assertion is LIVE, at the SECOND member of the class.**
The class is "a raw 5xx site whose status is pinned". CONTEXT: **mutate the SECOND member.** The M11
canary — `routers/portfolio.py`'s **first** `verify_strategy` 500 — was the original mutation and is
member one. Mutate the **second** `verify_strategy` 500 in that same function: flip `status_code=500`
to `status_code=502`.
Must redden: **both** the Counter equality (the triple's status changed) and the status assertion.
Record which failure message printed `unexpected=` / `missing=`.

**M95b — the mutation that was GREEN, re-run.**
Repeat M11 exactly: flip the **first** `portfolio.py` `verify_strategy` `status_code=500` to `502` —
the anonymous public teaser. On the untouched tree this left both full suites byte-identical.
Must redden now. Run the **FULL** pytest suite for this row, not a scoped path: the whole point of M11
is that a scoped run said nothing. Record the before (byte-identical, from CONTEXT §3) and the after
(the named failing test).
State the result as *"this guard **would have caught** M11"* — branch protection is OFF, so it did not
stop anything.

**M108 — the BLIND-SPOT constant is LIVE, and the evasion it names is a one-token edit.**
The census matches only a **direct `HTTPException(...)` / `JSONResponse(...)` constructor call by bare
name** (see `<interfaces>` for the predicate verbatim), and only an **integer-literal** `status_code=`.
Under **that** predicate a hand-typed constant pins the non-literal count at **3**.

⚠️ **RE-MEASURE WITH THE STATED PREDICATE BEFORE YOU START — not with a looser one.** Measured at
plan time on the untouched tree: the strict predicate yields **3**; *"any `status_code=` keyword
anywhere, any callee"* yields **4**, the fourth being `VenueTransientHTTPException.__init__`'s
`super().__init__(status_code=…)`, whose `func` is an `ast.Attribute` and therefore not a matched
construction. **If your measurement says 4, you used the loose reading — do not "fix" the constant to
match it, and do not report a disagreement.** If the predicate you implement genuinely yields 4,
change the **constant**, never the predicate: the predicate is the thing that has to be honest.

The constant exists, in this plan's own words, *"so a fourth appearing reddens and forces a decision."*

Mutation: at a **quarantined** site, replace the integer literal with a non-literal expression that
evaluates to the same value — e.g. `status_code=500` → `status_code=int("500")`, or hoist it to a
module-level constant and reference it. **Same behaviour, same status on the wire.**

This is a genuine defect re-creation, not a feature change: it makes a raw permanent 5xx **invisible to
the census** while the site still raises it. That is the evasion path a future author takes without
malice — hoisting a status to a constant looks like a tidy-up — and it is exactly what the blind-spot
constant exists to surface.

Must redden: the blind-spot assertion (the strict-predicate count becomes **4**) **and** the Counter
equality (the site leaves the derived census). Record both, and record which message named the change —
if only one fires, say which, because that tells you which assertion is load-bearing.
Must stay GREEN and record it: the needle self-test, the vacuity fence, and **both subclass constants**
(1 subclass / 7 sites) — proving the blind-spot assertion that fired is the non-literal one and not a
coupled failure.

⚠️ **This row was nearly not written.** The first draft of `140.4-VALIDATION.md` called it *"impossible
— a feature change, not a defect re-creation."* That was wrong: hiding a raw 5xx from its own census is
the defect. Recorded here because *"no row is possible"* is a claim that must itself be falsifiable.

For each row record: the exact diff, the command, pasted RED output with the failing test name and
assertion text, at least one named GREEN-side observation, and `git status` clean after revert.
**"Asserted — not observed" is not acceptable.**
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest -q && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <acceptance_criteria>
- Rows M95, M95b **and M108**, each with pasted RED output including the failing test name.
- M108 states which of the two assertions fired (the blind-spot count, the Counter equality, or both),
  and confirms the mutated site still raises the same status on the wire.
- **M108 quotes the scanner's predicate and BOTH measurements** — strict **3**, loose **4** — and states
  that the row was run against the strict one. A row that reports a bare integer without the predicate
  that produced it is unreproducible, which is the defect this phase exists to close.
- M95 states explicitly that it mutated the **second** member and names the first.
- M95b states the before/after and uses the words *"would have caught"*.
- Both rows name at least one thing that stayed GREEN.
- `git status --porcelain` after both reverts shows no residual mutation to any `analytics-service/routers/` file.
- **FULL suites green:** `cd analytics-service && python3 -m pytest -q` at or above **4778 / 96**;
  `mypy --strict` clean; `npx vitest run` unchanged from baseline (this plan touches no TypeScript);
  `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>
The census is proven falsifiable at the second member of the class, and the one mutation this
programme measured GREEN now reddens under the full suite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python service → every TS seam caller | The status line alone decides whether a failure counts toward the **global** breaker; a 502/504 where a 500 belongs re-arms the global-outage harm |
| anonymous internet → `verify_strategy` | `routers/portfolio.py`'s teaser path is unauthenticated; its status choices are reachable without a session |
| the error contract → sites that never call it | 12 sites construct a 5xx without passing through `_validate` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.4-04-01 | **Denial of service (global)** | a raw 5xx outside the contract re-arming the global breaker | mitigate | An `ast`-derived census equal to a multiplicity-preserving quarantine; every member's status pinned to 500 or a listed 503 with its reason. Row 1 population / row 2 quarantine (**partial by construction**, but a roster of known offenders that can only shrink). Falsified by M95 |
| T-140.4-04-02 | Tampering (guard decay) | a `frozenset` quarantine satisfied by deleting one of a duplicated pair | avoid | `Counter`, not `frozenset`; asserted by a grep criterion **and** by the M95 mutation |
| T-140.4-04-03 | Tampering (guard decay) | a scanner that matches nothing and reports agreement forever | mitigate | A vacuity fence (files visited, `HTTPException` calls of any status found) plus a two-polarity needle self-test |
| T-140.4-04-04 | Repudiation | a census implying completeness it does not have | mitigate | The non-literal-status blind spot is a hand-typed constant, asserted, and named in the docstring |
| T-140.4-04-05 | Tampering | bundling the re-homing with the census | avoid | This plan changes **zero** production Python; asserted by `git diff --name-only`. TRAP-8 |
| T-140.4-04-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** `ast` is stdlib and 14 test files in this suite already import it |
</threat_model>

<verification>
- `cd analytics-service && python3 -m pytest -q` (FULL, never scoped) at or above **4778 / 96**.
- `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` clean.
- `npx vitest run` and `npx tsc --noEmit` unchanged — this plan touches no TypeScript.
- Zero production Python modified (Task 2's mutations are reverted).
- Zero `.planning/` paths staged.
</verification>

<success_criteria>
- The census derives its population from source and compares it to a multiplicity-preserving
  quarantine with `==`, printing `unexpected=` and `missing=` on failure.
- Every quarantined member is 500, or a 503 carrying an explicit reason.
- The needle proves itself in both polarities, including the continuation-line shape a line grep misses.
- The blind spot is asserted, not implied.
- M95 reddens at the second member; M95b reddens on the mutation that was GREEN, under the full suite.
</success_criteria>

<output>
Create `.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-04-SUMMARY.md`.

Record: the re-derived census (12 sites / 9 triples / three count-2 triples) and whether it matched
this plan's pre-measured table; the four 503 members with their reasons; the non-literal blind-spot
count; the needle self-test names in both polarities; the vacuity-fence bounds; and the
`## Falsifiability Ledger — rows M95, M95b, M108` section with pasted RED output, the second-member
statement, and the M11 before/after phrased as *"would have caught"*.
</output>
