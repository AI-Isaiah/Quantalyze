---
phase: 142.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/schema/functions/sync_strategy_analytics_status.sql
  - .planning/REQUIREMENTS.md
  - TODOS.md
  - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md
  - .planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-RESEARCH.md
autonomous: true
requirements: [JOB-01, JOB-02, JOB-03]

must_haves:
  truths:
    - "npx tsx scripts/dump-sql-functions.ts --check exits 0 — the only hard-red CI gate on the branch is green, as the FIRST commit of the phase"
    - "The canonical snapshot of sync_strategy_analytics_status carries the 142 stamp semantics, so a re-base from the snapshot can no longer silently revert JOB-01"
    - "REQUIREMENTS.md JOB-03 states the chain-inclusive derivation the shipped code actually uses, with batch_size × max_per_kind_timeout named ONLY as the explicitly-rejected compute_jobs formula that yields 9,000 s and would reap healthy chains"
    - "Phase 142's Falsifiability Ledger shows 11/11 Observed with Pass C's evidence, corrected coordinates, and honest provenance — never '⬜ pending' reported as closed"
    - "D-08 is recorded as DEF-142.1-08 in TODOS.md with the OQ-1 evidence and the Oracle-Independence conflict — not silently dropped, not 'helpfully' implemented"
  artifacts:
    - path: "supabase/schema/functions/sync_strategy_analytics_status.sql"
      provides: "Regenerated canonical body with computing_started_at maintenance (three-arm CASE + branch b/c NULL clears)"
      contains: "computing_started_at"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected JOB-03 derivation text"
    - path: "TODOS.md"
      provides: "DEF-142.1-08 deferral entry"
      contains: "DEF-142.1-08"
    - path: ".planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md"
      provides: "Backfilled ledger (7 rows), corrected mutation coordinates, updated frontmatter + sign-off, explicit per-task-map non-backfill note"
    - path: ".planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-RESEARCH.md"
      provides: "Open Questions section retitled RESOLVED with inline markers citing C-5 / C-11 / plan 142.1-07"
      contains: "Open Questions (RESOLVED)"
  key_links:
    - from: "supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql"
      to: "supabase/schema/functions/sync_strategy_analytics_status.sql"
      via: "npm run schema:functions (scripts/dump-sql-functions.ts replay)"
      pattern: "computing_started_at"
    - from: ".planning/phases/142-.../142-VERIFICATION.md Behavioral Spot-Checks"
      to: "142-VALIDATION.md Falsifiability Ledger"
      via: "verbatim transcription of Observed/Evidence, corrected Mutation coordinates"
      pattern: "ledger row closed"
---

<objective>
Clear the phase's only hard-red CI gate (D-12, FIRST commit), then close the three
documentation-integrity items: the JOB-03 requirement text that contradicts the shipped
derivation (D-13), the Phase-142 falsifiability ledger left 7/11 unobserved (D-14), and
the formal deferral of the CUT item D-08 as DEF-142.1-08.

Purpose: Phase 142.1 exists partly because evidence artifacts over-claimed. This plan makes
the branch CI-green and the paper trail honest before any code work begins.
Output: regenerated SQL function snapshot (commit 1 of the phase), corrected REQUIREMENTS.md,
backfilled 142-VALIDATION.md, TODOS.md deferral entry, resolved RESEARCH.md open questions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-12 run 1 — regenerate the stale SQL function snapshot (FIRST commit of the phase)</name>
  <files>supabase/schema/functions/sync_strategy_analytics_status.sql</files>
  <read_first>
    - scripts/dump-sql-functions.ts:1-33 (what the generator does; the --check contract)
    - supabase/schema/functions/sync_strategy_analytics_status.sql (current stale body — header says "source migration: 20260710150000")
    - .github/workflows/sql-function-snapshot.yml:81-84 (the CI gate that is currently red)
  </read_first>
  <action>
    Per D-12 (hard-red CI, LOCKED as the phase's first commit): run `npm run schema:functions`
    from the repo root. Confirm the regenerated `supabase/schema/functions/sync_strategy_analytics_status.sql`
    now carries the 142 re-based body (header cites migration 20260802120000; body contains the
    three-arm `computing_started_at` CASE and the branch (b)/(c) NULL clears). Then run
    `npx tsx scripts/dump-sql-functions.ts --check` and confirm exit 0.
    Record for ledger row SC-12: the pre-fix state was RED (exit 1 naming
    sync_strategy_analytics_status.sql — orchestrator-verified 2026-08-02); paste the post-fix
    exit-0 output into the SUMMARY under "Falsifiability evidence / SC-12".
    Commit ONLY the regenerated snapshot file(s) under supabase/schema/functions/ as the phase's
    FIRST commit, message: `fix(142.1): regenerate stale sync_strategy_analytics_status snapshot (D-12 run 1)`.
    Do NOT bundle any other edit into this commit (S-9). If `npm run schema:functions` touches
    additional snapshot files, include them in the same commit — they are one generated artifact set.
  </action>
  <verify>
    <automated>npx tsx scripts/dump-sql-functions.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/dump-sql-functions.ts --check` exits 0
    - `grep -v '^--' supabase/schema/functions/sync_strategy_analytics_status.sql | grep -c computing_started_at` returns a value >= 3
    - `git log --oneline -1 --name-only` shows a commit touching only files under supabase/schema/functions/
  </acceptance_criteria>
  <done>Snapshot regenerated, --check exits 0, committed alone as the phase's first commit; SC-12 evidence recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: D-13 — correct JOB-03's derivation text; append DEF-142.1-08 to TODOS.md; mark RESEARCH.md's Open Questions RESOLVED</name>
  <files>.planning/REQUIREMENTS.md, TODOS.md, .planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-RESEARCH.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:53 (the JOB-03 text under repair — currently names `batch_size × max_per_kind_timeout`)
    - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VERIFICATION.md:69-76 (gap 7 — why the formula is wrong: it IS the compute_jobs formula, yields 9,000 s ≈4.9× too small)
    - 142.1-CONTEXT.md correction C-5 (the full D-08 cut rationale: OQ-1 zero-hit greps + Oracle-Independence conflict)
    - TODOS.md:1-40 (house structure — find the right section to append a deferred item)
    - 142.1-RESEARCH.md:1209-1226 (the three Open Questions — all three ARE resolved downstream: OQ-1 by CONTEXT correction C-5, OQ-3 by C-11, OQ-2 procedurally by plan 142.1-07 Task 3)
  </read_first>
  <action>
    Per D-13: rewrite the JOB-03 parenthetical at .planning/REQUIREMENTS.md:53. The corrected text
    must state: the threshold is derived from the CHAIN-INCLUSIVE worst case over
    `JOB_CHAIN_FOLLOW_ON` × `TIMEOUT_PER_KIND` — per-hop `(batch_size-1)×max_handler + handler×max_attempts + backoff`,
    yielding a 43,920 s (12.2 h) ceiling under the shipped 16 h threshold — and must explicitly name
    `batch_size × max_per_kind_timeout` as the REJECTED compute_jobs formula (research collision C-6
    proved it yields 9,000 s, ≈4.9× too small, and would reap healthy in-flight chains). The word
    "rejected" (any case) MUST appear on the SAME LINE as the formula, so the co-occurrence verify
    below can gate it — the formula stays in the text precisely so a future re-deriver sees it
    labelled as the wrong answer, never as the operative derivation. Keep the existing clause
    "never copied from the compute_jobs 4h number" and the
    `test_every_kind_has_watchdog_headroom` mirror reference. Do not renumber or touch any other requirement.

    Per correction C-5 (D-08 is CUT): append a deferred item `DEF-142.1-08` to TODOS.md in the
    appropriate backlog section (mid-term / convention questions — match house style). The entry must contain:
    (a) the item — `test_main_worker.py:1295` `assert len(TIMEOUT_PER_KIND) == 15` trains literal-bumping;
    (b) OQ-1 evidence — `grep -rn "STRATEGY_SCOPED\|_SCOPED_KINDS\|ALLOCATOR_KINDS"` over
    analytics-service returns ZERO hits, so no machine-readable strategy-scoped/allocator-scoped
    partition exists to derive the replacement assertion from;
    (c) the Oracle-Independence conflict — the VALIDATION template REQUIRES literal counts
    ("Table/registry sizes are pinned to a literal count, not len(THE_TABLE)"), which is exactly the
    form D-08 argues against; this is a convention decision, not a remediation task;
    (d) the two bad remedies (hand-maintained frozenset re-imports the complaint; deriving from the
    compute_jobs enqueue surface is a materially larger change) and
    (e) the hard rule: ⛔ never close this by bumping the literal to 16.
    Do NOT implement any part of D-08 anywhere in this phase.

    Then mark the research document's Open Questions resolved (they all are, downstream —
    leaving the section open-looking invites a future reader to re-litigate settled questions):
    in 142.1-RESEARCH.md, retitle the `## Open Questions` heading at :1209 to
    `## Open Questions (RESOLVED)` and append an inline `**RESOLVED:**` marker to each of the
    three questions WITHOUT altering the original question text:
    OQ-1 → RESOLVED by CONTEXT correction C-5 — D-08 is CUT, tracked as DEF-142.1-08 in TODOS.md;
    OQ-2 → RESOLVED procedurally by plan 142.1-07 Task 3 — psql preferred, MCP execute_sql
    block-by-block fallback, route stated in the D-16 evidence;
    OQ-3 → RESOLVED by correction C-11 — both negative anchors are scoped to the bridge's
    pg_get_functiondef, not cron.job.command; the binding constraint is instead
    `intervals == {expected}` over the whole $cron$ body (no second interval literal).
    Commit all three edits together:
    `docs(142.1): correct JOB-03 derivation text (D-13); defer D-08 as DEF-142.1-08; mark research OQs resolved`.
  </action>
  <verify>
    <automated>bash -c "grep -F 'batch_size × max_per_kind_timeout' .planning/REQUIREMENTS.md | grep -qi 'reject' && grep -qF '43,920' .planning/REQUIREMENTS.md && grep -q 'DEF-142.1-08' TODOS.md && grep -qF 'Open Questions (RESOLVED)' .planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-RESEARCH.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'batch_size × max_per_kind_timeout' .planning/REQUIREMENTS.md | grep -ci 'reject'` returns exactly 1 — the formula appears ONLY as the labelled-rejected mention on a line that also says rejected, never as the operative derivation
    - `grep -c 'DEF-142.1-08' TODOS.md` >= 1, and the entry contains the strings "STRATEGY_SCOPED", "Oracle", and "never close this by bumping"
    - JOB-03's text at .planning/REQUIREMENTS.md contains "43,920" and "chain-inclusive"
    - `grep -rn 'len(TIMEOUT_PER_KIND)' analytics-service/tests/test_main_worker.py` still shows the UNCHANGED assertion (D-08 not implemented)
    - 142.1-RESEARCH.md's section heading reads `## Open Questions (RESOLVED)` and each of OQ-1/OQ-2/OQ-3 carries an inline RESOLVED marker citing, respectively, C-5, plan 142.1-07 Task 3, and C-11 — original question text unaltered
  </acceptance_criteria>
  <done>JOB-03 text matches the shipped derivation with the rejected formula labelled in place; DEF-142.1-08 appended with full evidence; D-08 code untouched; research OQs closed with their downstream resolutions. SC-13 is human/reviewer-only — record that explicitly in the SUMMARY, never as an automated pass.</done>
</task>

<task type="auto">
  <name>Task 3: D-14 — backfill the seven Phase-142 ledger rows from Pass C, correct two stale coordinates, fix frontmatter + sign-off</name>
  <files>.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md</files>
  <read_first>
    - .planning/phases/142-.../142-VERIFICATION.md:158-192 (Pass C's Behavioral Spot-Checks table — the seven rows tagged "[ledger row closed]" are the SOURCE data)
    - .planning/phases/142-.../142-VALIDATION.md:108-126 (the ledger — SC-3, SC-3b, SC-4, SC-4b, SC-5, SC-5b, SC-5c at ⬜ pending) and :1-8 (frontmatter) and the Validation Sign-Off block
    - 142.1-CONTEXT.md corrections C-3 (the two stale coordinates) — SC-5b actual is job_worker.py:6704 (ledger says ~:6635/~:6744); SC-5c actual is keys/sync/route.ts:569 (ledger says :532)
    - 142.1-RESEARCH.md § "D-14 — falsifiability ledger backfill" (the row→row mapping table and the three bonus mutations that must NOT be force-fitted)
  </read_first>
  <action>
    Per D-14 with correction C-3: in 142-VALIDATION.md, fill the Observed?/Evidence columns of the
    seven pending rows (SC-3, SC-3b, SC-4, SC-4b, SC-5, SC-5b, SC-5c) by transcribing Pass C's
    results VERBATIM from 142-VERIFICATION.md's Behavioral Spot-Checks rows tagged
    "[ledger row closed]" (:181, :183, :185, :187, :188, :190, :191). Mark each `✅ Observed`.
    Do NOT re-derive, do NOT re-run, and do NOT fold in the three bonus mutations Pass C also ran
    (:182 SC-3 upper bound, :184 conjunct deletion, :186 script resurrection, :189 SC-5 None variant) —
    they are not ledger rows.
    The "transcribe verbatim" rule applies to Observed/Evidence ONLY: CORRECT the Mutation column's
    stale coordinates in the same edit — SC-5b's mutation coordinate becomes `job_worker.py:6704`
    (replacing the ~:6635/~:6744 claim), SC-5c's becomes `src/app/api/keys/sync/route.ts:569`
    (replacing :532).
    Add a one-line provenance note directly above the ledger table: rows SC-3..SC-5c were backfilled
    2026-08-02 from 142-VERIFICATION.md Pass C (verifier-run post-execution, not run at execution
    time), and the earlier in-session "11 Observed / 0 SKIPPED" report was WRONG (7 rows were pending).
    Update the frontmatter: `status: executed`, `wave_0_complete: true`. Update the Validation
    Sign-Off block: check the boxes that are now genuinely true, and set the Approval line to
    closed 2026-08-02 citing Pass C. Leave the Per-Task Verification Map ROWS untouched — they are
    outside D-14's scope — BUT add an explicit inline note directly above the Per-Task
    Verification Map table stating: the map was deliberately NOT backfilled (D-14's scope is the
    Falsifiability Ledger only), its ⬜ pending / ❌ rows are NOT evidence of execution, and the
    frontmatter's `status: executed` refers to the ledger's Pass-C closure — not to the per-task
    map. Without this note, declaring the file executed over an all-pending status table is a
    diluted instance of the exact "no SKIPPED rows remain ≠ all rows observed" conflation that
    created this phase.
    Commit: `docs(142.1): backfill 142 falsifiability ledger from Pass C, correct SC-5b/SC-5c coordinates (D-14)`.
  </action>
  <verify>
    <automated>bash -c "awk '/## Falsifiability Ledger/,/DEPLOYED-RUN/' '.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md' | grep -c '⬜ pending' | grep -qx 0 && grep -q 'job_worker.py:6704' '.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md' && grep -q 'route.ts:569' '.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md'"</automated>
  </verify>
  <acceptance_criteria>
    - Zero '⬜ pending' remain between "## Falsifiability Ledger" and the "DEPLOYED-RUN" heading in 142-VALIDATION.md
    - SC-5b's Mutation cell contains `job_worker.py:6704` and no longer contains `6635` or `6744`
    - SC-5c's Mutation cell contains `keys/sync/route.ts:569` and no longer contains `:532`
    - Frontmatter reads `status: executed` and `wave_0_complete: true`
    - The provenance note names the prior over-claim explicitly
    - An inline note above the Per-Task Verification Map states it was deliberately not backfilled and its pending rows are not evidence of execution
    - All seven Evidence cells contain text traceable verbatim to 142-VERIFICATION.md (spot-check SC-3 contains "5,760" or "headroom" and SC-5c contains "TS half RED")
  </acceptance_criteria>
  <done>142-VALIDATION.md shows 11/11 Observed with honest provenance, corrected coordinates, updated frontmatter and sign-off, and an explicit non-backfill disclaimer over the per-task map. SC-14 is human/orchestrator-only — record that explicitly in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot → future migration authors | the canonical function snapshot is the re-base source; a stale body is an integrity attack surface on JOB-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.1-07 | Tampering (integrity) | supabase/schema/functions snapshot showing pre-142 body → next author silently reverts JOB-01 stamp semantics | mitigate | Task 1 regenerates; blocking gate sql-function-snapshot.yml:84 stays armed; second run in plan 142.1-05 after the trigger lands |
| T-142.1-SC | Tampering | package installs | accept (N/A) | This phase installs nothing — no npm/pip/cargo install tasks exist in any 142.1 plan |
</threat_model>

<verification>
- `npx tsx scripts/dump-sql-functions.ts --check` exits 0
- `cd analytics-service && pytest tests/test_main_worker.py -q -x` still green (no code touched — regression guard)
- Ledger section of 142-VALIDATION.md has zero pending rows; frontmatter updated
- TODOS.md carries DEF-142.1-08; REQUIREMENTS.md carries the chain-inclusive derivation with the formula labelled rejected on its own line
- 142.1-RESEARCH.md's Open Questions section is retitled RESOLVED with the three inline markers
</verification>

<success_criteria>
- The phase's first commit is the snapshot regeneration and the branch's only hard-red gate is green
- D-13, D-14 closed per their corrections (C-3); D-08 deferred per C-5 with zero code impact
- SC-12 evidence recorded; SC-13/SC-14 recorded as human-only, never as automated passes
</success_criteria>

<output>
Create `.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-01-SUMMARY.md` when done.
Include a "Falsifiability evidence" section with the SC-12 output and explicit human-only notes for SC-13/SC-14.
</output>
