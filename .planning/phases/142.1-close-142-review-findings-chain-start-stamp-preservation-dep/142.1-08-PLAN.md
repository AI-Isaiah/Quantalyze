---
phase: 142.1
plan: 08
type: execute
wave: 4
depends_on: [142.1-03, 142.1-05]
files_modified:
  - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
  - analytics-service/tests/test_sql_gate_scoped_updates.py
autonomous: true
requirements: [JOB-01]

must_haves:
  truths:
    - "SQL gate Part 6 makes the trigger's silent coercion falsifiable: 6a proves never-advance (a Python-writer-shaped UPDATE cannot advance a seeded sentinel), 6b proves the NULL-stamp closure (trigger arm (b) stamps a computing row whose stamp is NULL on the next UPDATE), 6c proves exit-clear (arm (d) NULLs the stamp even when the writer supplies one) — deleting any trigger arm turns its Part-6 arm RED"
    - "Every Part-6 state is established at INSERT, never by UPDATE-ing a computing row (D-18 Part 2): 6a's row is seeded (computing, sentinel) and 6b's row is seeded (computing, NULL) directly — the prior 'intermediate transition-out' dance is gone (D-18 resolved old W7), because the UPDATE-only trigger does not fire on INSERT"
    - "Part 6 is UNGATED — it needs only the trigger, so it must REDDEN (not skip) when migration 20260803120000 is unapplied; Parts 2/3 remain the file's only legitimate skips"
    - "Every Part-6 driver UPDATE is scoped `WHERE strategy_id = <seeded id>` and the plan-142.1-03 grep gate's per-file pin is incremented in the same commit (reaper: 0 → 3)"
  artifacts:
    - path: "supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql"
      provides: "Part 6 (G1 trigger sentinel, ungated; 6a never-advance / 6b NULL-closure / 6c exit-clear; seeds at INSERT)"
      contains: "Part 6"
  key_links:
    - from: "Part 6 driver UPDATEs"
      to: "trigger strategy_analytics_stamp_computing_started_trigger (migration 20260803120000, plan 142.1-05)"
      via: "direct UPDATEs exercise the BEFORE UPDATE trigger arms — never the bridge RPC (Part 4 owns that oracle)"
      pattern: "strategy_analytics_stamp_computing_started"
    - from: "Part 6 seed INSERTs (computing, sentinel) and (computing, NULL)"
      to: "the trigger's UPDATE-only event scope"
      via: "gate-seeding discipline (D-18 Part 2) — the seeds land verbatim because the trigger does not fire on INSERT"
      pattern: "interval '3 hours'"
    - from: "analytics-service/tests/test_sql_gate_scoped_updates.py per-file pin"
      to: "the three new Part-6 driver UPDATEs"
      via: "pin increment reaper: 0 → 3 in the SAME commit (anti-vacuity contract from plan 142.1-03)"
      pattern: "test_strategy_analytics_stuck_computing_reaper"
---

<objective>
Append SQL gate Part 6 — the G1 trigger sentinel — in its settled D-18 shape: every state is
seeded at INSERT (the UPDATE-only trigger does not fire there), three assertion arms cover the
trigger's three behavioral guarantees, and the part is ungated so an unapplied migration is a
RED, never a skip.

Purpose: the trigger coerces SILENTLY by design (a RAISE would strand live writers), so
without Part 6 its predicate would be unfalsifiable — the exact "gate that cannot fail" class
this phase exists to kill. Part 6 is what makes ledger rows SC-17/01 achievable at plan
142.1-07's D-16 run.
Output: Part 6 appended to the reaper gate; grep-gate pin incremented.

Split provenance (W-b): this work was plan 142.1-05 Task 2. It is now its own plan because
plan 05 was the heaviest in the phase and D-18 added the Part-4 retrofit to it; the
same-commit-coupled gate edits (arm D inversion, Part 4 retrofit) STAYED in plan 05 with the
migration — Part 6 has no same-commit coupling to the migration and lands here.

CROSS-PLAN CONTRACT (fixed): migration
`20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql`, function
`strategy_analytics_stamp_computing_started`, trigger
`strategy_analytics_stamp_computing_started_trigger` — pinned by plan 142.1-05 (this plan's
depends_on). Cite verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-CONTEXT.md
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL gate Part 6 — the G1 trigger sentinel (ungated, seeds at INSERT, must redden when the trigger is absent)</name>
  <files>supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql, analytics-service/tests/test_sql_gate_scoped_updates.py</files>
  <read_first>
    - 142.1-CONTEXT.md § "⭐ D-18" Parts 1-2 (the settled trigger shape and gate-seeding discipline — every Part-6 state is seeded at INSERT; the old "intermediate transition-out" dance is superseded)
    - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql — Part 4 AS RETROFITTED BY PLAN 142.1-05 (the donor for framing: own BEGIN; SET LOCAL lock_timeout = '5s'; single DO block; ROLLBACK; the FK seed chain; note its seed INSERT now carries the sentinel — Part 6 copies that seeding discipline, and the file no longer contains any mid-test stamp UPDATE to copy); :52-66 (the per-part transaction rule S-3: psql nested BEGIN creates no savepoint; NEVER an outer transaction); the header's isolation-by-construction section (plan 142.1-03's rewrite)
    - 142.1-PATTERNS.md § "4. NEW SQL gate Part 6" — for the framing checklist and the no-skip-arm rule ONLY. ⚠️ Its "copy the sentinel technique at :546-566 verbatim" instruction is SUPERSEDED by D-18: those lines' mid-test UPDATE was deleted by plan 142.1-05; Part 6 seeds the sentinel in its INSERT instead (the shape 142.1-VALIDATION.md's SC-17/01 row specifies). Its 6b "intermediate transition-out" step is also superseded — seed (computing, NULL) directly
    - 142.1-RESEARCH.md § "4. A gate that CAN fail on a Python re-advance" (why G1 beat G2/G3: the frozen-clock hazard, the self-referential-oracle rejection)
  </read_first>
  <action>
    Append Part 6 to the reaper SQL gate, copying the retrofitted Part 4's framing (own
    BEGIN; SET LOCAL lock_timeout = '5s'; single DO block; ROLLBACK; FK seed chain
    auth.users → profiles → strategies with a fresh uuid; DELETE FROM auth.users at the end of
    the block). NO pg_cron gate and NO skip arm — Part 6 needs only the trigger, so it must
    REDDEN (not skip) when migration 20260803120000 is unapplied (Parts 2/3 are the only
    legitimate skips in the file). Scope every read/write to the block's own seeded ids (S-7).

    Seed TWO strategies under the one seeded user (strategy_analytics is one row per
    strategy, and 6a's row must keep its sentinel while 6b needs a NULL stamp):
    - v_strat_a with strategy_analytics ('computing', FALSE, v_sentinel) where
      v_sentinel := now() - interval '3 hours' — seeded at INSERT; the UPDATE-only trigger
      does not fire on INSERT, so the sentinel lands verbatim (D-18 Part 2).
    - v_strat_b with strategy_analytics ('computing', FALSE, NULL) — the (computing, NULL)
      state seeded DIRECTLY at INSERT; no intermediate transition-out dance (D-18 resolved
      the old W7 — the dance existed only because the superseded INSERT-firing trigger made
      this state unreachable).
    S-2 anti-vacuity seed-integrity controls BEFORE any driver: assert row A is at
    'computing' with stamp IS NOT DISTINCT FROM v_sentinel, and row B at 'computing' with
    stamp IS NULL — a broken seed cannot green the part vacuously.

    Three assertion arms, each with a distinct RAISE EXCEPTION message naming Part 6 / G1 /
    the D-number:
    (6a) NEVER-ADVANCE (the D-01 proof, ledger row SC-17/01): issue the Python-writer-shaped
    driver — a DIRECT `UPDATE public.strategy_analytics SET computation_status = 'computing',
    computing_started_at = now() WHERE strategy_id = v_strat_a;` — and assert the stamp IS
    NOT DISTINCT FROM v_sentinel. now() is transaction-constant, so without the seeded
    sentinel this assertion could NEVER fail — the 3-hour offset is what makes deleting the
    trigger's coercion arm (a) turn this RED.
    (6b) NULL-STAMP CLOSURE (the UPDATE-path half of D-11, trigger arm (b)): issue
    `UPDATE public.strategy_analytics SET computation_status = 'computing'
    WHERE strategy_id = v_strat_b;` — no stamp key supplied, row stays computing with a NULL
    stamp entering the trigger — and assert the stamp comes back NON-NULL (arm (b) stamped
    the frozen now()). Deleting arm (b) leaves it NULL ⇒ RED.
    (6c) EXIT-CLEAR (trigger arm (d)): issue `UPDATE public.strategy_analytics SET
    computation_status = 'failed', computing_started_at = now() - interval '1 hour'
    WHERE strategy_id = v_strat_a;` — deliberately supplying a non-NULL stamp on a terminal
    transition — and assert the stamp comes back NULL (arm (d) coerced it — no writer can
    leave a stale stamp on a terminal row). Deleting arm (d) leaves the supplied stamp ⇒ RED.
    Close with a Part 6 RAISE NOTICE summarizing the three proofs.

    Same commit: increment plan 142.1-03's per-file anti-vacuity pin in
    analytics-service/tests/test_sql_gate_scoped_updates.py —
    "test_strategy_analytics_stuck_computing_reaper.sql": 0 → 3 (the three Part-6 drivers;
    total 6 → 9) — the pin firing on this edit is its design working; every driver is
    `= v_strat_a` / `= v_strat_b`, which the gate's one rule accepts as block-local seeded
    identifiers.

    Local verification is structural only (no DB in this runtime — the behavioral run is plan
    142.1-07's D-16): assert the file's shape with the acceptance greps below, and confirm the
    Python suites still pass. The Part-6 assertions themselves are exercised end-to-end by the
    orchestrator in plan 142.1-07.
    Commit: `test(142.1): SQL gate Part 6 — trigger sentinel G1, seeds at INSERT (never-advance, NULL-closure, exit-clear)`.
  </action>
  <verify>
    <automated>bash -c "grep -q 'Part 6' supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql && cd analytics-service && pytest tests/test_sql_gate_scoped_updates.py tests/test_main_worker.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - Part 6 exists with its own BEGIN/ROLLBACK framing; `awk '/Part 6/,0' supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql | grep -c 'RETURN;'` returns 0 (no skip arm)
    - Part 6 contains `interval '3 hours'` (the sentinel) INSIDE its seed INSERT's value list — and `grep -c 'SET computing_started_at = v_sentinel' supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql` still returns 0 file-wide (no mid-test sentinel UPDATE was reintroduced)
    - Part 6 contains NO transition-out-then-back-in dance: 6b's driver UPDATE sets computation_status = 'computing' on the (computing, NULL)-seeded row directly, with no prior UPDATE to 'failed' on v_strat_b
    - Part 6 contains three distinct RAISE EXCEPTION arms (6a/6b/6c) plus BOTH seed-integrity controls
    - All three driver UPDATEs are scoped `WHERE strategy_id = v_strat_a` or `= v_strat_b`; `cd analytics-service && pytest tests/test_sql_gate_scoped_updates.py -q` green with the pin at reaper: 3
  </acceptance_criteria>
  <done>Part 6 committed in the D-18 shape; the trigger predicate has a gate that CAN fail per arm (proven live in plan 142.1-07); ledger row SC-17/01 is now achievable; the pin contract with plans 03/05 is discharged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL gate (service_role via psql/MCP) → shared TEST project | Part 6 writes only its own seeded rows inside a rolled-back transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.1-05 | Denial of Service (CI/TEST) | cross-tenant locks from gate writes | mitigate | every Part-6 write is scoped to the block's own seeded strategy ids and rolled back; the plan-142.1-03 grep gate enforces the scoping mechanically |
| T-142.1-01 | Tampering (integrity) | a silently-coercing trigger with no gate that can fail | mitigate | Part 6's three arms each redden on deletion of the corresponding trigger arm; the seed-integrity controls keep it non-vacuous |
| T-142.1-SC | Tampering | package installs | accept (N/A) | no installs this phase |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_sql_gate_scoped_updates.py -q` green (pin at reaper: 3)
- `cd analytics-service && pytest tests/test_main_worker.py -q` still green
- Structural acceptance greps above all hold
</verification>

<success_criteria>
- Part 6 exists, ungated, seeded-at-INSERT, with two seeded strategies and three falsifiable arms — SC-17/01 achievable at the D-16 run
- No mid-test stamp UPDATE and no transition-out dance anywhere in Part 6
- The grep-gate pin contract is honored in the same commit
</success_criteria>

<output>
Create `.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-08-SUMMARY.md` when done.
Include a "Falsifiability evidence" section noting SC-17/01's behavioral halves are DEFERRED to plan 142.1-07's orchestrator run (state this explicitly — do NOT mark them observed), plus the structural greps and the green pin run.
</output>
