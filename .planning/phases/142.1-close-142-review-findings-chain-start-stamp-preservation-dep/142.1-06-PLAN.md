---
phase: 142.1
plan: 06
type: execute
wave: 4
depends_on: [142.1-04, 142.1-05]
files_modified:
  - analytics-service/services/analytics_runner.py
  - analytics-service/tests/test_pgrst204_recovery_fallback.py
  - analytics-service/tests/test_computing_started_at_stamp.py
autonomous: true
requirements: [JOB-01, JOB-02]

must_haves:
  truths:
    - "_mark_computing's comment no longer asserts the false premise 'every invocation of this writer genuinely transitions the row INTO computing' — it names the trigger that coerces the value on an already-computing row, and the CSV-first path where the stamp IS the genuine transition"
    - "Under PGRST204 (schema-cache miss on computing_started_at), _mark_unrecoverable falls back to a payload without the unknown key and the terminal failed STILL lands — a deploy-window PGRST204 can no longer convert into a permanent spinner because the job could not even stamp failed"
    - "The fallback payload is an explicit second dict literal (never dict(...)/{**base}) with a pinned, counted exemption in the stamp gate — C-12's ordering (D-07 first) is satisfied and the census stays anti-vacuous"
  artifacts:
    - path: "analytics-service/services/analytics_runner.py"
      provides: "corrected _mark_computing comment + PGRST204 fallback in _mark_unrecoverable"
      contains: "PGRST204"
    - path: "analytics-service/tests/test_pgrst204_recovery_fallback.py"
      provides: "D-02/R2 behavioral test — simulated PGRST204 ⇒ terminal failed lands via the fallback"
      min_lines: 60
  key_links:
    - from: "_mark_unrecoverable"
      to: "strategy_analytics terminal failed row"
      via: "except APIError code==PGRST204 → re-issue upsert without computing_started_at"
      pattern: "PGRST204"
    - from: "analytics_runner.py:1236 comment"
      to: "trigger strategy_analytics_stamp_computing_started_trigger (migration 20260803120000)"
      via: "prose names the DB object so source and \\d strategy_analytics agree"
      pattern: "strategy_analytics_stamp_computing_started"
---

<objective>
Close the Python-writer half of D-01 (the false comment — mandatory under Option A: the
trigger makes the comment MORE misleading, not less) and D-02/R2 (the PGRST204 recovery-path
fallback — the amplifier that turns any schema-cache miss into a job that cannot even stamp
failed).

Purpose: research confirmed ALL 11 Python exit writers carry the same unknown key and ZERO
PGRST204 handling exists repo-wide; _mark_unrecoverable is the catch-all, so the fallback
there converts a permanent spinner into a normal failed. Sequenced after D-07 (C-12) so the
fallback can be written without tripping the AST gate.
Output: corrected comment, bounded fallback, new behavioral pytest, updated census counts.

CROSS-PLAN CONTRACT: the trigger names cited here — migration
`20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql`, function
`strategy_analytics_stamp_computing_started`, trigger
`strategy_analytics_stamp_computing_started_trigger` — are the pinned contract values created
by plan 142.1-05 (this plan's depends_on). Cite them verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-CONTEXT.md
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-01 — correct the false _mark_computing comment</name>
  <files>analytics-service/services/analytics_runner.py</files>
  <read_first>
    - analytics-service/services/analytics_runner.py:1227-1242 (_mark_computing and the false comment at :1233-1239)
    - 142.1-PATTERNS.md § "12. D-01's comment correction" (what the replacement must say)
    - 142.1-RESEARCH.md § "1. Read the actual code" (the verified call chain + the C-9 refinement: compute_analytics_from_csv is ALSO enqueued directly by csv-finalize/route.ts:823 as the FIRST job — on that path this stamp IS the genuine transition-in)
  </read_first>
  <action>
    Rewrite the comment block above the `"computing_started_at"` payload key in _mark_computing.
    Delete the false claim ("Unconditional here is correct — every invocation of this writer
    genuinely transitions the row INTO computing"). The replacement must state, concretely:
    (a) on the 4-hop chain (process_key_long → sync_trades → derive_broker_dailies →
    compute_analytics_from_csv) the row has been 'computing' since hop 1 and the DB trigger
    `strategy_analytics_stamp_computing_started_trigger` (migration 20260803120000) SILENTLY
    COERCES this value to preserve the hop-1 chain-start stamp — the payload value is written
    but discarded by design;
    (b) on the CSV-first path (csv-finalize/route.ts:823 enqueues compute_analytics_from_csv as
    the strategy's FIRST job, no prior bridge call) this stamp is the genuine transition-in and
    the trigger respects it;
    (c) client-side UTC ISO remains correct here because a PostgREST payload is a literal
    (keep that half of the old comment).
    Name the trigger by object name so `\d strategy_analytics` and the source agree.
    Keep the payload LINE itself byte-unchanged — only prose changes; the stamp gate's census
    must not move.
    This is a prose fix: record in the SUMMARY that its verification is reviewer-only (the
    142.1-VALIDATION Manual-Only row), never an automated pass.
    Commit: `docs(142.1): correct _mark_computing's false transition-in premise (D-01)`.
  </action>
  <verify>
    <automated>bash -c "! grep -q 'genuinely transitions the row INTO computing' analytics-service/services/analytics_runner.py && grep -q 'strategy_analytics_stamp_computing_started' analytics-service/services/analytics_runner.py && cd analytics-service && pytest tests/test_computing_started_at_stamp.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'genuinely transitions the row INTO computing' analytics-service/services/analytics_runner.py` returns 0
    - The comment names strategy_analytics_stamp_computing_started_trigger and migration 20260803120000, and describes BOTH paths (4-hop coerced; CSV-first respected)
    - `git diff` on the file shows comment-only changes in _mark_computing (the payload dict is byte-identical)
    - Stamp gate still green
  </acceptance_criteria>
  <done>The comprehension hazard research named as Option A's real cost is mitigated at the source; recorded as reviewer-only evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-02/R2 — PGRST204 fallback in _mark_unrecoverable + behavioral pytest + census exemption</name>
  <files>analytics-service/services/analytics_runner.py, analytics-service/tests/test_pgrst204_recovery_fallback.py, analytics-service/tests/test_computing_started_at_stamp.py</files>
  <read_first>
    - analytics-service/services/analytics_runner.py:1686-1711 (_mark_unrecoverable — the catch-all under repair; its upsert payload is the dict literal the fallback mirrors)
    - 142.1-PATTERNS.md § "5. NEW pytest — PGRST204 fallback" (the FULL test recipe: APIError construction from test_compute_jobs_fencing.py:161-168; the __name__-routed db_execute harness from test_csv_analytics_runner.py:290-317 — S-8, never call-counter routing; the `return fn()` arm at :434-440 that runs the REAL callable — do NOT patch db_execute away; the payload assertion idiom at :450-460; the D-07 interaction warning)
    - analytics-service/tests/test_computing_started_at_stamp.py — the Python census counts (test_python_writer_census_counts) and the terminal-status rule; the fallback adds a NEW terminal dict literal WITHOUT the stamp key, which both must account for
  </read_first>
  <behavior>
    - Test 1 (the fallback works): drive _mark_unrecoverable through the real callable against a supabase mock whose upsert .execute() raises APIError({"code":"PGRST204","message":"Could not find the 'computing_started_at' column of 'strategy_analytics' in the schema cache"}) on the FIRST call only; assert a SECOND upsert lands with computation_status == "failed" and WITHOUT the computing_started_at key (payload-assertion idiom), and that computation_warned is False in the fallback payload
    - Test 2 (non-PGRST204 errors still propagate): same harness, APIError code "40001"; assert it is re-raised (the fallback must be narrow — swallowing every APIError would hide real failures)
    - Test 3 (negative control): no error injected; assert exactly ONE upsert lands carrying computing_started_at: None (the normal path is unchanged)
  </behavior>
  <action>
    Write the three tests FIRST in `analytics-service/tests/test_pgrst204_recovery_fallback.py`
    (RED), then implement:
    In _mark_unrecoverable, wrap the existing `.upsert(...).execute()` in try/except
    `postgrest.exceptions.APIError`. If `getattr(exc, "code", None) == "PGRST204"`: log a loud
    warning naming the deploy-window cause (schema cache missing computing_started_at — D-02),
    then re-issue the upsert with a SECOND EXPLICIT DICT LITERAL — the same keys minus
    `computing_started_at` (strategy_id, computation_status "failed", computation_warned False,
    computation_error, data_quality_flags prior_flags), same on_conflict="strategy_id". The
    stamp key is omitted BECAUSE the column does not exist server-side in this window — the
    trigger (which owns exit-clears once the column exists) makes the omission safe. Any other
    APIError re-raises unchanged. Do NOT build the fallback via dict(...) or {**base} — C-12:
    even with D-07 softened, a literal keeps the census resolvable and the counts stable. Scope
    is _mark_unrecoverable ONLY (R2 is deliberately bounded — the catch-all is the amplifier;
    the other 10 exit writers reach it on failure).
    Update test_computing_started_at_stamp.py's Python census for the new terminal literal: the
    fallback dict has a terminal status WITHOUT the stamp key, which the exit-clear rule would
    flag — add a pinned, counted exemption (e.g. a PGRST204_FALLBACK_EXEMPT literal count == 1
    beside the census counts, with a comment naming D-02/R2 and WHY the key is deliberately
    absent), so (a) the gate stays green on the fallback, (b) a second unstamped terminal writer
    anywhere else still reddens, and (c) if the fallback is ever deleted the exemption count
    itself reddens (anti-vacuity both directions). Re-derive the census totals (now +1 status
    dict).
    SC-02 probe, pre-commit: transiently delete the except-arm (restore the bare .execute()),
    run Test 1, observe RED (the terminal failed never lands — the row stays computing), paste
    into SUMMARY, revert, re-run green.
    Run `cd analytics-service && mypy --strict --follow-imports=silent services/analytics_runner.py`
    (P6; fix via cast(), never # type: ignore). The per-task mypy run is SCOPED to the touched
    module to hold the 45 s feedback ceiling; the FULL `services/ routers/ models/` run stays in
    this plan's <verification> block (the wave gate), where 142.1-VALIDATION.md places it.
    Commit: `fix(142.1): PGRST204 fallback in _mark_unrecoverable — terminal failed always lands (D-02/R2)`.
  </action>
  <verify>
    <automated>bash -c "cd analytics-service && pytest tests/test_pgrst204_recovery_fallback.py tests/test_computing_started_at_stamp.py -q && mypy --strict --follow-imports=silent services/analytics_runner.py"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PGRST204' analytics-service/services/analytics_runner.py` >= 1, and the except arm re-raises non-PGRST204 APIErrors
    - The fallback payload is an inline dict literal without computing_started_at; `grep -c 'dict(' analytics-service/services/analytics_runner.py` unchanged from before the task within _mark_unrecoverable (no dict(...) construction added)
    - All three behavioral tests green; SC-02 transient-deletion RED pasted in SUMMARY
    - Stamp-gate census green with the pinned exemption; deleting the fallback would redden the exemption count (assert by reading the count coupling)
    - mypy --strict clean on services/analytics_runner.py (scoped per-task run — ⚠️ never pipe mypy into `tail` in a gate: the pipeline's exit status becomes tail's and the gate cannot fail) and on the full services/ routers/ models/ surface at plan end (<verification> block); `cd analytics-service && pytest -q` full suite green
  </acceptance_criteria>
  <done>A deploy-window PGRST204 now produces a normal terminal failed instead of a permanent spinner; the fallback is narrow, literal-shaped, tested, and census-accounted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker (service_role) → strategy_analytics via PostgREST | during a deploy window the schema cache may not know the stamp column; the recovery write is the last line before a permanent spinner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.1-06 | Tampering (integrity) | schema/code divergence window → PGRST204 on every recovery write → job cannot stamp failed | mitigate | Task 2 fallback (R2); R1 already landed in plan 142.1-03; narrow except-arm so real errors still surface |
| T-142.1-12 | Repudiation | a silently-discarded stamp write misleads future readers of _mark_computing | mitigate | Task 1 corrects the prose and names the DB object; COMMENT ON TRIGGER (plan 142.1-05) discloses it DB-side |
| T-142.1-SC | Tampering | package installs | accept (N/A) | no installs this phase |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_pgrst204_recovery_fallback.py tests/test_computing_started_at_stamp.py tests/test_main_worker.py -q` green
- `cd analytics-service && mypy --strict --follow-imports=silent services/ routers/ models/` clean (full-surface run — wave gate; exit status unmasked, no tail/pipe)
- `cd analytics-service && pytest -q` full suite green at plan end
</verification>

<success_criteria>
- D-01's prose half closed (reviewer-verifiable); the code half was closed by the trigger in plan 142.1-05
- D-02/R2 closed: SC-02 observed RED under fallback deletion; census exemption pinned and bidirectionally anti-vacuous
- C-12 honored: the fallback was written AFTER D-07's softening, as a literal
</success_criteria>

<output>
Create `.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-06-SUMMARY.md` when done.
Include a "Falsifiability evidence" section with the SC-02 pasted RED and the explicit reviewer-only note for D-01's comment.
</output>
