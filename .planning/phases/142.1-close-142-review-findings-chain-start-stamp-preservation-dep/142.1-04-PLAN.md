---
phase: 142.1
plan: 04
type: execute
wave: 3
depends_on: [142.1-02]
files_modified:
  - analytics-service/tests/test_computing_started_at_stamp.py
  - src/app/api/strategies/csv-finalize/route.ts
autonomous: true
requirements: [JOB-01]

must_haves:
  truths:
    - "The AST gate no longer raises on a statically-unresolvable strategy_analytics payload — an unrelated refactor into a payload-builder helper cannot force a developer to abandon the refactor or extend a 785-line test file (the DB trigger now owns the stamp invariant)"
    - "The TS census sees the whole TS write surface — a strategy_analytics writer planted in src/lib/ or a .tsx server action turns the census RED (today it passes silently)"
    - "Every terminal-status strategy_analytics object literal carries computation_warned — deleting the key from csv-finalize turns the extended gate RED (today that mutation has NO observable, which is why D-03's fix and the gate extension ship together)"
    - "csv-finalize's terminal-failed writer carries computation_warned: false, so bridge branch (a) can no longer resolve a failed row back to complete_with_warnings and render a green factsheet from the prior run's stale metrics"
  artifacts:
    - path: "analytics-service/tests/test_computing_started_at_stamp.py"
      provides: "softened resolution policy + widened TS glob + terminal-warned parity rule + rewritten docstrings + re-derived census counts"
    - path: "src/app/api/strategies/csv-finalize/route.ts"
      provides: "computation_warned: false in the terminal-failed placeholder upsert"
      contains: "computation_warned"
  key_links:
    - from: "src/app/api/strategies/csv-finalize/route.ts terminal-failed upsert"
      to: "bridge branch (a) computation_warned marker read (migration 20260802120000:315-317)"
      via: "computation_warned: false clears the stale marker"
      pattern: "computation_warned:\\s*false"
    - from: "test_computing_started_at_stamp.py TS half"
      to: "the union TS write surface (src/**/*.ts, src/**/*.tsx excl. tests)"
      via: "widened _ts_scan_files + re-derived EXPECTED_PER_FILE/EXPECTED_TOTAL"
      pattern: "_ts_scan_files"
---

<objective>
Rework the two-runtime stamp gate: soften the raise-on-unresolvable-payload arms (D-07 — the
DB trigger landing in plan 142.1-05 this same wave now owns the stamp invariant), widen the TS
census past src/app/api/**/*.ts (D-04), extend the TS rule with terminal-writer
computation_warned parity, and land the one-line csv-finalize fix (D-03) inside the same
commit as the gate extension so the fix has an observable from birth.

Purpose: D-03 is the money-surface false-success bug (a failed CSV import renders a green
factsheet from stale metrics), and correction C-7 is explicit — adding the key alone has NO
observable today; the gate extension is mandatory, not optional.
Output: reworked test_computing_started_at_stamp.py, fixed csv-finalize route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-CONTEXT.md
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-07 — soften the raise-on-unresolvable arms; re-derive the liveness counters; add a tolerance self-test</name>
  <files>analytics-service/tests/test_computing_started_at_stamp.py</files>
  <read_first>
    - analytics-service/tests/test_computing_started_at_stamp.py:270-380 (_resolve_name's five arms at :295-303, :311-321, :327-336; the policy paragraph at :282-283 "Every unresolvable shape FAILS LOUD"; the terminal raise at :372-377) — line numbers may have shifted after plan 142.1-02's helper extraction; anchor by content
    - same file :527-528 and :551-567 (test_python_writer_census_counts — the n1/n2 liveness counters COUPLED to the raise arms; PATTERNS 7(c): re-derive, do not delete)
    - 142.1-RESEARCH.md § "What the trigger does NOT fix" (D-07 row: once the DB owns the stamp, the raise has no invariant left to protect) and § "Architecture Patterns" Pattern 1 (the trigger this softening leans on — landing in plan 142.1-05, same wave)
  </read_first>
  <action>
    Per D-07 (gated on D-17, which is SETTLED to Option A — the BEFORE INSERT OR UPDATE trigger
    `strategy_analytics_stamp_computing_started` in migration
    20260803120000, plan 142.1-05, same wave): convert the gate's unresolvable-payload behavior
    from RAISE to record-and-skip. Concretely: keep every existing resolution arm (n1 Name, n2
    parameter-default — they still resolve real payloads and their REDs are live, proven by
    142's SC-5b); at the three fail-loud sites (the terminal AssertionError at :372-377 and the
    unresolvable fallthroughs in _resolve_name), append the site to a new `skipped: list[str]`
    instead of raising. Surface the skip count as a third pinned counter (e.g.
    EXPECTED_SKIPPED_UNRESOLVED = 0 today) inside test_python_writer_census_counts so the census
    stays anti-vacuous: a refactor that makes a payload unresolvable moves a site from
    kept→skipped and reddens the COUNT assertion (visible, decidable) rather than hard-failing
    the build with a demand to extend AST arms. Re-derive EXPECTED_KEPT_VIA_N1 /
    EXPECTED_SITES_VIA_N2 (they should be unchanged — no production payload changes in this
    task — but re-derive from a run, never assume).
    Rewrite the policy paragraph at :282-283: the stamp invariant is now owned by the DB trigger
    (name it and cite migration 20260803120000 and SQL gate Part 6); this static census is
    best-effort payload hygiene, and an unresolvable payload is counted, not fatal.
    Add a scanner self-test (the :114-127 _count_in_text precedent): feed synthetic in-memory
    source where the upsert payload is returned from a helper function; assert the scan completes
    WITHOUT raising and records exactly one skipped site. This is SC-07's standing observable.
    SC-07 probe, pre-commit: transiently restore the raise at the terminal site, run the new
    self-test, observe RED (the raise fires instead of the skip), paste into SUMMARY, revert,
    re-run green.
    Commit: `test(142.1): AST gate records unresolvable payloads instead of raising (D-07)`.
  </action>
  <verify>
    <automated>bash -c "cd analytics-service && pytest tests/test_computing_started_at_stamp.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'which the gate cannot resolve to a' analytics-service/tests/test_computing_started_at_stamp.py` returns 0 (the terminal raise text is gone)
    - The policy paragraph no longer contains "FAILS LOUD"; it names the trigger function and migration 20260803120000
    - test_python_writer_census_counts pins three counters (n1, n2, skipped=0) with literal expected values
    - New self-test green; SC-07 transient-restore RED pasted in SUMMARY
    - Full file green: `cd analytics-service && pytest tests/test_computing_started_at_stamp.py -q`
  </acceptance_criteria>
  <done>Unresolvable payloads are counted, not fatal; counters re-derived; SC-07 falsifiable via the self-test.</done>
</task>

<task type="auto">
  <name>Task 2: D-04 — widen the TS census past src/app/api/**/*.ts; rewrite the boundary docstring</name>
  <files>analytics-service/tests/test_computing_started_at_stamp.py</files>
  <read_first>
    - analytics-service/tests/test_computing_started_at_stamp.py:29-54 (the docstring LEDGER of what widening must survive — three benign shapes named verbatim: src/lib/utils.ts EMPTY_ANALYTICS; the portfolios page.tsx portfolio_analytics payload i.e. the WRONG-table trap; type-annotation members at src/lib/queries.ts:414,452 and src/components/admin/AdminTabs.tsx:82) and :623 (_TS_KEY_RE), :631-639 (_ts_scan_files), :650-688 (_enclosing_object_literal), :754-776 (EXPECTED_PER_FILE / EXPECTED_TOTAL / the unexpected fail-loud arm)
    - src/app/(dashboard)/portfolios/[id]/page.tsx:495-510 (the portfolio_analytics payload the widened rule must NOT flag)
    - src/lib/queries.ts:410-455 and src/components/admin/AdminTabs.tsx:78-86 (the type-annotation members the widened rule must NOT flag)
  </read_first>
  <action>
    Per D-04 (reduced-not-deleted under D-17 per C-1: the trigger moots the STAMP half but
    computation_warned is not a stamp property — the census survives): widen `_ts_scan_files`
    from `src/app/api/**/*.ts` to the union of `src/**/*.ts` and `src/**/*.tsx`, excluding test
    files (`*.test.ts`, `*.test.tsx`, anything under `src/__tests__/` or `__tests__/` segments)
    and `.d.ts`. The widened match rule must survive the three benign shapes the docstring
    ledger names, without a TS parser:
    (a) distinguish PAYLOAD keys from TYPE-ANNOTATION members — a payload's
    `computation_status` is followed by a quoted string literal or a known-status expression,
    while an interface/generic member is followed by a type name; implement by checking the
    matched value token (quoted literal or null) before counting a site, and
    (b) survive the WRONG-table trap — the portfolios page's `portfolio_analytics` payload must
    not be counted; scope by requiring the enclosing statement/chain to reference
    `strategy_analytics` (mirror the Python half's chain-scoping rationale) or by an explicit,
    commented per-file benign-shape allowlist pinned by literal count (choose the mechanism that
    keeps the gate's green falsifiable; document the choice in the docstring).
    Re-derive EXPECTED_PER_FILE and EXPECTED_TOTAL from the widened scan (the three compliant
    route files + csv-finalize remain the genuine payload sites; new benign entries must be
    explicit, never silently absorbed). Keep the `unexpected` fail-loud arm.
    REWRITE the :29-54 docstring in the SAME commit — it currently states the boundary is
    "deliberately NOT widened (W-8)", which becomes a live contradiction; the new text explains
    the union boundary, the benign-shape discriminators, and cites D-04.
    SC-04 probe, pre-commit: create a temporary file under src/lib/ containing a
    strategy_analytics upsert object literal with computation_status but no stamp key, run the
    gate, observe the census RED naming the planted file, paste into SUMMARY, delete the file,
    re-run green.
    Commit: `test(142.1): widen TS census to the full src write surface (D-04)`.
  </action>
  <verify>
    <automated>bash -c "cd analytics-service && pytest tests/test_computing_started_at_stamp.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - _ts_scan_files rglobs both .ts and .tsx under src/ and excludes test files (verify by reading the function + the passing gate)
    - `grep -c 'deliberately NOT widened' analytics-service/tests/test_computing_started_at_stamp.py` returns 0
    - Gate passes against the REAL tree (the three benign shapes do not trip it)
    - SC-04 planted-writer RED pasted in SUMMARY; tree clean afterwards (`git status --porcelain src/lib/` empty)
    - EXPECTED_TOTAL / EXPECTED_PER_FILE are literal values re-derived from the widened scan, with csv-finalize still listed
  </acceptance_criteria>
  <done>TS census covers .tsx and src/lib; benign shapes survive; SC-04 observed RED.</done>
</task>

<task type="auto">
  <name>Task 3: D-03 — terminal-warned parity rule (observe the RED) + the one-line csv-finalize fix (observe the GREEN)</name>
  <files>analytics-service/tests/test_computing_started_at_stamp.py, src/app/api/strategies/csv-finalize/route.ts</files>
  <read_first>
    - 142.1-PATTERNS.md § "7(b) D-03 — the terminal-writer computation_warned parity rule" (the Python-half analog to port: _TERMINAL_STATUSES at :94-97 applied at :455-461; the defective site excerpt; the compliant finalize-wizard shape to copy)
    - src/app/api/strategies/csv-finalize/route.ts:760-775 (the defective terminal-failed upsert — the ONLY writer of 17 across three languages missing the key, per the D-03 census)
    - src/app/api/strategies/finalize-wizard/route.ts:1476-1482 (the compliant analog — key + comment placement to copy exactly)
    - 142.1-RESEARCH.md § "D-03 — terminal-writer parity" (why this is a money-surface false success: bridge branch (a) resolves computation_warned=true back to complete_with_warnings and the factsheet renders green from the prior run's stale metrics_json)
  </read_first>
  <action>
    Per D-03 with correction C-7 (fix INLINE — a shared payload constant is prohibited; and the
    fix alone has NO observable, so gate + fix ship in ONE commit with the intermediate RED
    observed):
    STEP 1 — extend the TS half: port the terminal-status concept across the language boundary.
    Using the existing `_enclosing_object_literal` anchor, add a rule: when a literal's
    `computation_status` value is one of {"failed", "complete", "complete_with_warnings"}
    (declare a TS-side frozenset mirroring the Python `_TERMINAL_STATUSES`), `computation_warned`
    must appear in the SAME literal. Add the anti-vacuity positive control (S-2): pin the literal
    count of terminal-status TS literals so a gutted rule cannot pass vacuously.
    STEP 2 — run the gate and OBSERVE THE RED: it must fail naming exactly
    src/app/api/strategies/csv-finalize/route.ts (the lone defective site). Paste this failing
    output into the SUMMARY as ledger row SC-03's mutation evidence — this observed RED is the
    proof the extension can fail, and it doubles as the D-03 mutation observation.
    STEP 3 — apply the one-line fix: insert `computation_warned: false,` in the csv-finalize
    terminal-failed upsert, between `computation_status: "failed",` and the JOB-01
    comment line, copying the finalize-wizard:1476-1482 shape exactly. Do NOT hoist the payload
    into a constant; do NOT touch metrics_json or any other key.
    STEP 4 — re-run the gate (GREEN), run `npm run typecheck` (exit 0), and run the touched-file
    lint. Commit gate + fix together:
    `fix(142.1): csv-finalize terminal-failed carries computation_warned:false + parity gate (D-03)`.
  </action>
  <verify>
    <automated>bash -c "cd analytics-service && pytest tests/test_computing_started_at_stamp.py -q && cd .. && npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - Intermediate RED observed and pasted in SUMMARY, naming csv-finalize/route.ts (SC-03 evidence)
    - `grep -A3 'computation_status: "failed"' src/app/api/strategies/csv-finalize/route.ts | grep -c 'computation_warned: false'` returns 1
    - The gate passes after the fix; `npm run typecheck` exits 0
    - No shared payload constant introduced: the csv-finalize upsert argument is still an inline object literal
    - `npx eslint src/app/api/strategies/csv-finalize/route.ts` clean
  </acceptance_criteria>
  <done>The false-success laundering path is closed AND falsifiable: deleting the key now reddens the extended gate (observed live during this task).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal writers → bridge status resolution | a terminal write that leaves a stale computation_warned marker lets bridge branch (a) launder failed → complete_with_warnings on a money surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.1-10 | Tampering (data integrity) | csv-finalize failed row laundered to a green factsheet built from prior-run stale metrics | mitigate | Task 3: computation_warned:false inline + the parity rule that reddens if the key is ever removed |
| T-142.1-11 | Tampering (gate integrity) | TS census blind to .tsx/src-lib writers — permanent-spinner class re-creatable outside the glob | mitigate | Task 2: widened union glob + benign-shape discriminators + fail-loud unexpected arm |
| T-142.1-SC | Tampering | package installs | accept (N/A) | no installs this phase |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_computing_started_at_stamp.py tests/test_job07_reaper_off_worker_loop.py tests/test_main_worker.py -x -q` green
- `npm run typecheck` exits 0
- `cd analytics-service && pytest -q` full suite green at plan end
</verification>

<success_criteria>
- D-07 softened with re-derived counters and a standing tolerance self-test (SC-07 observed)
- D-04 widened surviving all three named benign shapes (SC-04 observed)
- D-03 fixed inline with its observable born in the same commit (SC-03's RED observed live)
- Constraint 4 unblocked: plan 142.1-06's R2 fallback can now be written in idiomatic form
</success_criteria>

<output>
Create `.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-04-SUMMARY.md` when done.
Include a "Falsifiability evidence" section with pasted outputs for SC-03, SC-04, SC-07.
</output>
