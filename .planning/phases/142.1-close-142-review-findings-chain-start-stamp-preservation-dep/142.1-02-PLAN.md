---
phase: 142.1
plan: 02
type: execute
wave: 2
depends_on: [142.1-01]
files_modified:
  - analytics-service/tests/_scan_helpers.py
  - analytics-service/tests/test_computing_started_at_stamp.py
  - analytics-service/tests/test_job07_reaper_off_worker_loop.py
  - analytics-service/tests/test_dark_path_deleted.py
  - analytics-service/tests/test_cash_basis_series_sc4.py
  - analytics-service/services/ingestion/long_fetch.py
  - analytics-service/tests/test_long_fetch_follow_on_guard.py
autonomous: true
requirements: [JOB-01, JOB-02, JOB-03]

must_haves:
  truths:
    - "One shared _py_scan_files covers the union {main_worker.py, main.py} ∪ services/** ∪ routers/** ∪ scripts/** — a reaper identifier planted in services/basis_series.py turns test_job07's structural gate RED (today it is green, and that is the bug)"
    - "No test file carries its own copy of _repo_root / the comment-detection helper / _py_scan_files — the silent-narrowing divergence class (D-04/D-10) has one source of truth"
    - "The yielding control arm no longer pins wall-clock latency; the tick_before_work/tick_after_work asymmetry pair remains the deterministic proof"
    - "A JOB_CHAIN_FOLLOW_ON tuple of unexpected arity can no longer raise out of run_process_key_long_job's follow-on block — the unpack is inside the try: whose comment forbids crashing the handler"
  artifacts:
    - path: "analytics-service/tests/_scan_helpers.py"
      provides: "_repo_root, _is_pure_comment, _py_scan_files (wide union), _count_in_text, _count"
      min_lines: 60
    - path: "analytics-service/tests/test_long_fetch_follow_on_guard.py"
      provides: "D-15 arity-tolerance pytest (monkeypatch 3-tuple ⇒ handler does not raise)"
      min_lines: 40
  key_links:
    - from: "analytics-service/tests/test_job07_reaper_off_worker_loop.py"
      to: "analytics-service/tests/_scan_helpers.py"
      via: "from tests._scan_helpers import"
      pattern: "from tests\\._scan_helpers import"
    - from: "analytics-service/services/ingestion/long_fetch.py"
      to: "services.job_worker.JOB_CHAIN_FOLLOW_ON"
      via: "late import + unpack INSIDE the best-effort try:"
      pattern: "JOB_CHAIN_FOLLOW_ON"
---

<objective>
Close the test-hygiene cluster: D-10 (extract the shared scan helpers once, widening the two
silently-narrow file lists to the union), D-06 (delete the flaky wall-clock budget), D-09
(drop the misleading 5,000-row backlog parameter), and D-15 (hoist the JOB_CHAIN_FOLLOW_ON
unpack inside its guard, with a new arity-tolerance pytest).

Purpose: the _py_scan_files divergence means reaper work in services/basis_series.py is
INVISIBLE to the JOB-07 structural gate today while its docstring claims parity — the same
silent-narrowing class as D-04. This plan is Wave-0 surface for later plans' gates.
Output: analytics-service/tests/_scan_helpers.py, four re-pointed test files, hardened
long_fetch.py, new arity-tolerance test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-CONTEXT.md
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-10 — create tests/_scan_helpers.py, re-point four files, widen the union</name>
  <files>analytics-service/tests/_scan_helpers.py, analytics-service/tests/test_computing_started_at_stamp.py, analytics-service/tests/test_job07_reaper_off_worker_loop.py, analytics-service/tests/test_dark_path_deleted.py, analytics-service/tests/test_cash_basis_series_sc4.py</files>
  <read_first>
    - 142.1-PATTERNS.md § "3. NEW analytics-service/tests/_scan_helpers.py" (the FULL spec: canonical bodies, the naming decision, the union list, the false-twin warning, the self-test precedent)
    - analytics-service/tests/test_computing_started_at_stamp.py:107-170 (donor: _repo_root, _is_pure_comment, _rel, _py_scan_files wide form + dedupe tail) and :68-70 (module-docstring pointer to update)
    - analytics-service/tests/test_job07_reaper_off_worker_loop.py:91-162 (narrow list + the FALSE parity docstring at :140) and :114-127 (_count_in_text/_count self-test split — carry it over)
    - analytics-service/tests/test_dark_path_deleted.py:37-95 (second narrow copy)
    - analytics-service/tests/test_cash_basis_series_sc4.py:669 area (_strip_comment + _repo_root only — check its directory depth before re-pointing)
  </read_first>
  <action>
    Per D-10 and correction C-8: create `analytics-service/tests/_scan_helpers.py` (plain module,
    NOT conftest.py — pytest.ini sets pythonpath=. and tests/__init__.py exists; import style is
    `from tests._scan_helpers import ...`, precedents at test_c19_portfolio_fixes.py:29 and
    test_e2_seam_ledger.py:40). Move in, verbatim from the donors named in read_first:
    _repo_root (walk-up form — note in the docstring that Path(__file__) resolves relative to
    THIS module and the directory depth is unchanged), the comment-detection helper under the
    HONEST name `_is_pure_comment` (the `_strip_comment` name lies — it returns bool and strips
    nothing; test_computing_started_at_stamp.py:120 already renamed it; this is the surface-the-
    conflict rule, not averaging), `_py_scan_files` in the WIDE form (the union
    {main_worker.py, main.py} ∪ services/** ∪ routers/** ∪ scripts/**, i.e. the stamp gate's
    existing list — a strict superset of the other two) with the shared dedupe tail, and
    `_count_in_text` / `_count` so the scanner self-test's dependency moves too.
    Re-point all four files to import from tests._scan_helpers and DELETE their local copies.
    test_cash_basis_series_sc4.py takes only _repo_root and _is_pure_comment.
    FIX the false docstring at test_job07_reaper_off_worker_loop.py:140 ("same file list as
    test_dark_path_deleted._py_scan_files" plus the false "any wiring of reaper work" coverage
    claim) — after unification the parity claim is true again; the docstring must now say the
    surface is the shared union in tests._scan_helpers and why (landmine 7 in PATTERNS).
    Update the module-docstring pointer at test_computing_started_at_stamp.py:68-70 to name
    tests/_scan_helpers.py.
    ⛔ Do NOT fold: `_strip_comments` (plural) at test_job_worker_flipretry.py:278 (different
    function — whole-text → str), and test_main_worker.py's parents[2] repo-root mechanism
    (different mechanism, same result — leave both alone).
    Then run the SC-10 falsifiability probe IN-REPO, pre-commit: append a non-comment line
    containing the token `reap_strategy_analytics_stuck_computing` to
    analytics-service/services/basis_series.py, run
    `cd analytics-service && pytest tests/test_job07_reaper_off_worker_loop.py -q`, observe the
    structural gate go RED naming basis_series.py (this RED is IMPOSSIBLE before the widening —
    that is the point), paste the failing assertion into the SUMMARY, then revert with
    `git checkout -- analytics-service/services/basis_series.py` and re-run to green.
    Commit: `refactor(142.1): extract tests/_scan_helpers.py, widen scan union (D-10)`.
  </action>
  <verify>
    <automated>bash -c "cd analytics-service && pytest tests/test_computing_started_at_stamp.py tests/test_job07_reaper_off_worker_loop.py tests/test_dark_path_deleted.py tests/test_cash_basis_series_sc4.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - analytics-service/tests/_scan_helpers.py exists and exports _repo_root, _is_pure_comment, _py_scan_files, _count_in_text, _count
    - `grep -c 'def _repo_root' analytics-service/tests/test_computing_started_at_stamp.py analytics-service/tests/test_job07_reaper_off_worker_loop.py analytics-service/tests/test_dark_path_deleted.py analytics-service/tests/test_cash_basis_series_sc4.py` shows 0 per file (all four re-pointed)
    - `grep -c 'basis_series' analytics-service/tests/_scan_helpers.py` returns 0 (union comes from rglob, not a hand list)
    - _py_scan_files in _scan_helpers.py rglobs "services", "routers", "scripts" (all three)
    - test_job07's _py_scan_files docstring no longer claims a narrower surface; `grep -c '"services" / "analytics_runner.py"' analytics-service/tests/test_job07_reaper_off_worker_loop.py` returns 0
    - SC-10 probe: pasted RED output in SUMMARY naming services/basis_series.py, then green after revert
    - `git status --porcelain analytics-service/services/basis_series.py` is empty at commit time
  </acceptance_criteria>
  <done>One shared helper module; four consumers; union surface; SC-10 observed as a genuinely-new RED; false parity docstring corrected.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 + D-09 — delete the wall-clock budget; zero the decorative backlog parameter</name>
  <files>analytics-service/tests/test_job07_reaper_off_worker_loop.py</files>
  <read_first>
    - 142.1-PATTERNS.md § "11. D-06 / D-09 — deletions" (exact excerpts: the assertion to delete, the two to KEEP, the docstring that already frames the pair correctly)
    - analytics-service/tests/test_job07_reaper_off_worker_loop.py — the yielding arm (search for `obs["latency"] < 0.1`, originally :553-556) and the `backlog_rows=5_000` call (originally :346; line numbers have shifted after Task 1 — anchor by content)
  </read_first>
  <action>
    Per D-06: in the yielding control arm, delete ONLY the wall-clock assertion
    `assert obs["latency"] < 0.1, ("a yielding reap must not delay healthz — ...")`. KEEP the
    `tick_after_work > tick_before_work` assertion and the `probe_landed_mid_dispatch` assertion —
    that surviving pair, contrasted with the blocking arm, IS the deterministic proof (delete,
    do not loosen: a 200 ms GC pause on a shared runner reddens `analytics` on unrelated PRs and
    the message misdirects to the reaper). The docstring framing the pair as the property is
    already correct post-deletion — leave it.
    Per D-09: change `backlog_rows=5_000` to `backlog_rows=0` at the `_stranded_backlog_supabase`
    call in the janitor-sim test (or drop the parameter if it has a default of 0) — the 15,000
    dicts are read by nothing (dispatch is patched; the reaper is in pg_cron) and the number
    falsely implies backlog size is load-bearing for the healthz outcome.
    Run the file's full test set; all tests must stay green — SC-09's observable is the UNCHANGED
    green (regression-only; record honestly as such, never as "caught").
    For SC-06 falsifiability: pre-commit, transiently swap the yielding twin's asyncio.sleep for
    a blocking time.sleep (the same one-token swap the control pair is built around), run the
    file, observe the surviving pair flip RED, paste the output into the SUMMARY, revert, re-run
    green.
    Commit: `test(142.1): delete wall-clock budget from yielding arm, zero decorative backlog (D-06, D-09)`.
  </action>
  <verify>
    <automated>bash -c "cd analytics-service && pytest tests/test_job07_reaper_off_worker_loop.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF 'obs["latency"] < 0.1' analytics-service/tests/test_job07_reaper_off_worker_loop.py` returns 0
    - `grep -c 'tick_after_work' analytics-service/tests/test_job07_reaper_off_worker_loop.py` >= 1 (surviving pair intact)
    - `grep -cF 'backlog_rows=5_000' analytics-service/tests/test_job07_reaper_off_worker_loop.py` returns 0
    - All tests in the file pass; SC-06 flip RED pasted in SUMMARY; SC-09 recorded as regression-only green
  </acceptance_criteria>
  <done>Wall-clock budget gone, backlog parameter zeroed, 8 tests green, SC-06/SC-09 evidence recorded honestly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-15 — hoist the JOB_CHAIN_FOLLOW_ON unpack inside the try:; arity-tolerance pytest</name>
  <files>analytics-service/services/ingestion/long_fetch.py, analytics-service/tests/test_long_fetch_follow_on_guard.py</files>
  <read_first>
    - analytics-service/services/ingestion/long_fetch.py:580-612 (the block under repair — the late import at :587, the 2-tuple unpack at :591, the try: at :599 whose comment says the block must never crash the handler)
    - 142.1-PATTERNS.md § "6. NEW pytest — JOB_CHAIN_FOLLOW_ON arity tolerance" (monkeypatch.setitem idiom from test_stitch_composite_job.py:1196; why setitem works against the late import)
    - analytics-service/services/job_worker.py:509-517 (the map's shape and the LOAD-BEARING tuple-order comment)
    - .planning/phases/142-.../142-VERIFICATION.md:53-60 (the failure scenario: ValueError escapes, retry short-circuits on the idempotency check, strategy left published with NO analytics chain)
  </read_first>
  <behavior>
    - Test 1 (the new protection): monkeypatch.setitem(_jw.JOB_CHAIN_FOLLOW_ON, "process_key_long", ("a","b","c")) — a 3-tuple; drive the non-csv tail-selection path of the long-fetch handler; assert the handler does NOT raise (the arity error is contained by the try: and logged, mirroring the enqueue-blip semantics the comment promises)
    - Test 2 (negative control): with the real 2-tuple in place, drive the same path; assert the follow-on enqueue is attempted with a tail kind from the tuple (the guard did not silently disable the feature)
  </behavior>
  <action>
    Per D-15 (LOCKED as a two-line hoist — Pass A raised this THRICE and verifiers correctly
    refuted it thrice as latent-not-live; do NOT record it as "Pass A missed it"): move the late
    import `from services.job_worker import JOB_CHAIN_FOLLOW_ON` and the fixed-arity unpack
    `ledger_tail, trade_tail = JOB_CHAIN_FOLLOW_ON["process_key_long"]` (plus the dependent
    `tail_kind =` selection) INSIDE the `try:` at long_fetch.py:599, so a future topology edit
    (3-tuple) is contained by the same best-effort guard as an enqueue blip. Preserve the
    load-bearing tuple-order comment. Change no other behavior.
    Write tests FIRST in the new file `analytics-service/tests/test_long_fetch_follow_on_guard.py`
    per the behavior block, using the monkeypatch.setitem idiom (setitem on the live dict mutates
    the object the late import re-resolves; it restores automatically). Route any db/supabase
    doubles by callable __name__ (S-8), never call-counter position.
    Observe Test 1 RED against the unhoisted code first (RED→GREEN), then apply the hoist, then
    green. For ledger row SC-15: the pre-hoist RED IS the mutation observation (restoring the
    unpack outside the try: ⇒ ValueError escapes ⇒ RED) — paste it into the SUMMARY.
    Run `cd analytics-service && mypy --strict --follow-imports=silent services/ingestion/long_fetch.py`
    before committing (P6 — the GSD path runs pytest only; fix via cast(), never # type: ignore).
    The per-task mypy run is SCOPED to the touched module to hold the 45 s feedback ceiling; the
    FULL `services/ routers/ models/` run stays in this plan's <verification> block (the wave
    gate), where 142.1-VALIDATION.md places it.
    Commit: `fix(142.1): contain JOB_CHAIN_FOLLOW_ON arity inside the follow-on guard (D-15)`.
  </action>
  <verify>
    <automated>bash -c "cd analytics-service && pytest tests/test_long_fetch_follow_on_guard.py -q && mypy --strict --follow-imports=silent services/ingestion/long_fetch.py"</automated>
  </verify>
  <acceptance_criteria>
    - In long_fetch.py, the `from services.job_worker import JOB_CHAIN_FOLLOW_ON` line and the tuple unpack both sit AFTER the `try:` keyword of the best-effort block (verify by reading the diff; `awk` over the function body shows the import at deeper indentation than the try:)
    - New pytest passes: 3-tuple ⇒ no raise; 2-tuple negative control ⇒ enqueue attempted
    - Pre-hoist RED (ValueError escaping) pasted in SUMMARY as SC-15 evidence
    - mypy --strict clean on services/ingestion/long_fetch.py (scoped per-task run — ⚠️ never pipe mypy into `tail` in a gate: the pipeline's exit status becomes tail's and the gate cannot fail) and on the full services/ routers/ models/ surface at plan end (<verification> block); `cd analytics-service && pytest -q` full suite green (guard against collateral)
  </acceptance_criteria>
  <done>Unpack contained; thrice-refuted latent finding is falsifiable for the first time; SC-15 observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test scan surface → production write surface | a silently-narrowed scan list lets a reaper/writer land unseen (integrity of the JOB-01/JOB-07 gates) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.1-08 | Tampering (gate integrity) | _py_scan_files divergence — gates report green while covering a shrunken surface | mitigate | Task 1: single shared union list; SC-10 probe proves the new coverage reddens |
| T-142.1-09 | Denial of Service (worker) | ValueError escaping run_process_key_long_job leaves a published strategy with no analytics chain | mitigate | Task 3: unpack inside the try:; arity-tolerance pytest pins containment |
| T-142.1-SC | Tampering | package installs | accept (N/A) | no installs this phase |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_computing_started_at_stamp.py tests/test_job07_reaper_off_worker_loop.py tests/test_main_worker.py -x -q` (per-task sampling command)
- `cd analytics-service && pytest -q` full suite green at plan end
- `cd analytics-service && mypy --strict --follow-imports=silent services/ routers/ models/` clean (full-surface run — wave gate; exit status unmasked, no tail/pipe)
</verification>

<success_criteria>
- D-10, D-06, D-09, D-15 closed per PATTERNS §3/§11/§6; no helper copies remain; SC-10 and SC-15 observed as new REDs; SC-06 flip observed; SC-09 recorded regression-only
</success_criteria>

<output>
Create `.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-02-SUMMARY.md` when done.
Include a "Falsifiability evidence" section with pasted outputs for SC-06, SC-09 (regression-only), SC-10, SC-15.
</output>
