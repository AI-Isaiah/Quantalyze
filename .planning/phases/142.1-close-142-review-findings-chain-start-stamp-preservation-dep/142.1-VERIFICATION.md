---
phase: 142.1-close-142-review-findings-chain-start-stamp-preservation-dep
verified: 2026-08-03T05:48:48Z
status: human_needed
score: 41/42 must-haves verified (+1 QUALIFIED — G5, see below)
overrides_applied: 0
re_verification: null
deferred: []
human_verification:
  - test: "Read the rewritten `.planning/REQUIREMENTS.md:53` JOB-03 text against the shipped `TestReaperThresholdInvariant` in `analytics-service/tests/test_main_worker.py`, and judge whether the chain-inclusive derivation as WORDED is a correct and sufficient statement of what the code derives."
    expected: "The 43,920 s chain-inclusive derivation in the requirement is the same derivation the invariant enforces; the `batch_size x max_per_kind_timeout` formula is named only as rejected."
    why_human: "Ledger row SC-13. The automated verify proves string presence and co-occurrence ONLY (rejected-formula string co-occurs with a rejection marker = 1; `43,920` = 1; `chain-inclusive` = 1). A grep can never establish that replacement prose is TRUE. Recorded OPEN by the phase itself."
  - test: "Confirm the seven backfilled rows in `142-VALIDATION.md`'s Falsifiability Ledger (SC-3, SC-3b, SC-4, SC-4b, SC-5, SC-5b, SC-5c) match Pass C's Observed/Evidence in `142-VERIFICATION.md`, row by row."
    expected: "Each backfilled Observed/Evidence cell is a faithful transcription of Pass C's result; the corrected coordinates `job_worker.py:6704` and `keys/sync/route.ts:569` are right."
    why_human: "Ledger row SC-14. This phase produced NO new mutation observation for these rows — they were transcribed, not re-run. The verifier confirmed the file SHAPE only (11 Observed / 0 pending in lines 122-241; stale coordinates absent). The 142 ledger was ONCE reported closed on exactly this kind of shape-check; that is why the row exists."
  - test: "Read the replacement `_mark_computing` comment at `analytics-service/services/analytics_runner.py:1243-1271` and judge whether the two-path story it now tells is true of the code."
    expected: "The 4-hop chain bullet and the CSV-first bullet each describe a real reachable path with the stated trigger behaviour."
    why_human: "D-01 prose half. Greps prove the false 'every invocation genuinely transitions in' clause is gone and the trigger is named; nothing proves the replacement prose true. The BEHAVIOURAL claim underneath it WAS confirmed live on TEST (VALIDATION.md Task 2)."
  - test: "At merge, after `supabase-migrate` auto-applies to PROD, query `cron.job.command` for `reap_strategy_analytics_stuck_computing` and confirm it carries TWO single-evaluation `WITH batch AS MATERIALIZED (...)` batches — one per arm — not merely that a `LIMIT` token is present."
    expected: "`(length(command) - length(replace(upper(command),'AS MATERIALIZED','')))/length('AS MATERIALIZED') = 2` and no `IN ( SELECT ... LIMIT` shape."
    why_human: "D-19 residual. `20260803130000` is applied to TEST ONLY (stamped `20260802212852`); PROD's registered body still carries the unbounded shape until `feat/v1.16-142-146-job-rate` merges. Token-presence checking is exactly what every gate in phases 142 and 142.1 passed over. Tracked as TODOS item 6."
  - test: "On a repo clone with the Supabase secrets removed, push a commit touching `supabase/migrations/**` to `main` and confirm the `supabase-migrate` workflow FAILS (exit 1) rather than reporting green."
    expected: "Four `::error::` lines and a failed run — not a green no-op."
    why_human: "The workflow SHAPE is pinned by vitest (`critical-regressions.test.ts:1078-1128`, 128 tests green) and all three arms of the extracted shell body were driven locally, but that is evidence about the SCRIPT, not about GitHub's dispatch of it. Recorded as reviewer-observable-only by the phase."
  - test: "Decide whether the SC-17/01 and SC-11 RED halves (delete the trigger's coercion arm; delete the companion cron arm) should be mutant-run against the TEST project, or whether design-proof + the local throwaway-PostgreSQL-16 fixture run is sufficient."
    expected: "A recorded decision. If TEST-side runs are wanted, each mutation must redden its Part-6 / arm-D assertion on TEST."
    why_human: "Both rows are recorded as POSITIVE-half-observed-on-TEST with the RED half explicitly design-proven + local-fixture only, NOT mutant-run on TEST. This is honestly recorded, not hidden — the open question is whether the founder accepts it."
  - test: "Carried from Phase 142, unchanged: PROD cron registration, the PROD backfill census, and the live wizard render of a reaped strategy."
    expected: "Per `142-VERIFICATION.md`'s human items."
    why_human: "Requires PROD access and a live browser session. Not touched by 142.1."
---

# Phase 142.1: Close 142 review findings — Verification Report

**Phase Goal:** Close the 16 items raised by three independent passes over Phase 142 — so that the reap clock genuinely measures the whole job chain, no terminal writer can launder a failure into a green factsheet, the `sql-function-snapshot` CI gate is green again, and Phase 142's own falsifiability evidence is complete rather than 4/11.

**Verified:** 2026-08-03T05:48:48Z
**Status:** human_needed
**Re-verification:** No — initial verification
**Verified at commit:** `41eeca8f` (working tree clean before and after this verification)

**Must-have source:** ROADMAP phase 142.1 carries no `success_criteria` array, so must-haves were merged from (a) the five goal clauses and (b) the `must_haves.truths` of all eight PLAN frontmatters (37 truths). Total 42.

---

## Goal Achievement

### Goal-level truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| G1 | The reap clock genuinely measures the WHOLE job chain — no writer can advance `computing_started_at` on an already-computing row | ✓ VERIFIED | Enforced at the TABLE, not per-writer. `20260803120000:116-188` — `CREATE OR REPLACE FUNCTION public.strategy_analytics_stamp_computing_started()` with four arms; arm (a) at `:127-139` `IF OLD.computation_status='computing' AND OLD.computing_started_at IS NOT NULL THEN NEW.computing_started_at := OLD.computing_started_at`. Bound at `:184-188` `BEFORE UPDATE ON public.strategy_analytics FOR EACH ROW`. `SET search_path = public, pg_catalog`, SECURITY INVOKER (no `SECURITY DEFINER` token), `REVOKE ALL ON FUNCTION ... FROM PUBLIC, anon, authenticated` at `:179`. Self-verify `DO` blocks at `:214-234` reject any tgtype that includes insert or delete. Falsifiable at `supabase/tests/...reaper.sql` Part 6/6a (`:861-874`) via a 3-hour sentinel offset. |
| G2 | No terminal writer can launder a failure into a green factsheet | ✓ VERIFIED — **and independently neuter-tested by this verifier** | `src/app/api/strategies/csv-finalize/route.ts:768` now carries `computation_warned: false` in the terminal-`failed` upsert. **Neuter test run by the verifier:** deleting line 768 → `test_typescript_terminal_writers_carry_computation_warned` FAILED naming `src/app/api/strategies/csv-finalize/route.ts:767: terminal "failed"` with the money-surface message; file restored via `git checkout --`, `git status --short` empty. Rule is anti-vacuous (`EXPECTED_TERMINAL_SITES = 4` pins the population). `analytics_runner._mark_unrecoverable` also carries `computation_warned: False` on BOTH the primary and the PGRST204 fallback payload. |
| G3 | The `sql-function-snapshot` CI gate is green again | ✓ VERIFIED — **re-run by this verifier** | `npx tsx scripts/dump-sql-functions.ts --check` → `SQL function snapshot is current (105 functions).`, **exit 0**. The gate is live in CI at `.github/workflows/sql-function-snapshot.yml:84` running that exact command. `supabase/schema/functions/sync_strategy_analytics_status.sql` carries `computing_started_at` (7 occurrences); the new `supabase/schema/functions/strategy_analytics_stamp_computing_started.sql` (61 lines) is tracked (D-12 run 2). |
| G4 | Phase 142's falsifiability evidence is complete (11/11) rather than 4/11 | ✓ VERIFIED (shape) — judgment routed to SC-14 | `142-VALIDATION.md` Falsifiability Ledger (lines 122-241): **11 rows, 11 `✅ Observed`, 0 `⬜ pending`** — SC-1, SC-1b, SC-2, SC-2b, SC-3, SC-3b, SC-4, SC-4b, SC-5, SC-5b, SC-5c. Corrected coordinates `job_worker.py:6704` and `route.ts:569` present; stale coordinates absent (zero hits). The 22 surviving `⬜` markers are ALL in the Per-Task Verification Map (lines 65-83), which carries an explicit non-backfill banner at `:55-60` stating the map "must not be read as 'these checks were run and are outstanding' nor as 'these checks failed'". That banner was a plan-01 must-have and it landed. |
| G5 | The 16 items raised by the three passes are closed | ✓ **VERIFIED (QUALIFIED) — 15 closed, 1 (D-08) explicitly CUT before execution and tracked** | See "The 16-item count, stated plainly" below. D-08 was cut by orchestrator decision CONTEXT.md correction **C-5**, recorded before any plan ran, tracked as `DEF-142.1-08` in `TODOS.md:710-728`, and explicitly excluded from the ledger (`142.1-VALIDATION.md:318` "⛔ SC-08 is absent by design"). ⛔ **Verified NOT gamed:** `test_main_worker.py:1295` still reads `assert len(TIMEOUT_PER_KIND) == 15` — the literal was NOT bumped to 16. |

### The 16-item count, stated plainly

The goal sentence says "close the 16 items". The honest number is **15 closed, 1 cut**:

| Item | Disposition | Evidence in the tree |
|------|-------------|----------------------|
| D-01 chain-start stamp | CLOSED | BEFORE UPDATE trigger arm (a), `20260803120000:127-139` + comment rewrite `analytics_runner.py:1249-1270` |
| D-02 PGRST204 | CLOSED (R1+R2) | `supabase-migrate.yml:159-162` fail-loud; `analytics_runner.py:1745-1777` fallback |
| D-03 terminal-warned | CLOSED | `csv-finalize/route.ts:768` + parity gate |
| D-04 TS census width | CLOSED | `_ts_scan_files` over the union `src/**/*.ts`, `src/**/*.tsx` |
| D-05 neutralizing UPDATEs | CLOSED | all three DELETED — only 3 `UPDATE public.strategy_analytics` statements remain in the gate (`:861`, `:884`, `:906`), all Part-6 drivers scoped to seeded ids; `<> ALL` survives only inside a prose comment at `:91` |
| D-06 wall-clock arm | CLOSED | latency assertion deleted; `test_job07...py:507-517` records why |
| D-07 AST raise | CLOSED | record-and-continue + positive control |
| **D-08 `len(TIMEOUT_PER_KIND)`** | ⛔ **CUT** (C-5) | `DEF-142.1-08` in TODOS.md; literal confirmed still `15` |
| D-09 `backlog_rows` | CLOSED at the named site | `:303` now `backlog_rows=0`; `:399` survives DELIBERATELY inside `test_healthz_stays_200_with_large_stranded_backlog`, where `assert len(stranded) == 5_000` is the fixture-honesty guard and the test's own NAME states the number — executor refused to game the grep and flagged it (02-SUMMARY deviation 3). Correct behaviour. |
| D-10 helper divergence | CLOSED | `tests/_scan_helpers.py`; zero remaining local `_repo_root` / `_py_scan_files` / `_is_pure_comment` definitions in any other test file; 5 consumers import it |
| D-11 NULL-stamp population | CLOSED | companion clock-start arm, `20260803120000:316-337`, ordered FIRST in the `$cron$` body |
| D-12 snapshot gate | CLOSED | verifier re-ran: exit 0, 105 functions |
| D-13 JOB-03 text | CLOSED in text; judgment → SC-13 | `REQUIREMENTS.md:53` |
| D-14 142 ledger | CLOSED in shape; judgment → SC-14 | 11/11, banner on the map |
| D-15 arity hoist | CLOSED | `long_fetch.py` — late import AND unpack now inside the best-effort `try:`, `tail_kind: str \| None = None` pre-declared |
| D-16 gate never run | CLOSED — and it FOUND D-19 | VALIDATION.md § ORCHESTRATOR RUN Task 3 |

**Scope growth, correctly recorded:** the phase shipped a SECOND, unplanned migration `20260803130000_reaper_limit_bound_materialized_cte.sql` for D-19, a defect found by the D-16 run. It is in no plan, no wave and no ledger row, and the VALIDATION file, the plan-07 SUMMARY and TODOS item 6 all say so explicitly.

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/20260803120000_...sql` | trigger fn + BEFORE UPDATE trigger + cron re-register + self-verify | ✓ VERIFIED | 22,583 bytes; 4 trigger arms; UPDATE-only; 5 self-verify `RAISE EXCEPTION` guards on tgtype/security/body-shape; two-statement `$cron$` body |
| `supabase/migrations/20260803130000_...sql` (UNPLANNED, D-19) | MATERIALIZED batch CTE on both arms | ✓ VERIFIED | `WITH batch AS MATERIALIZED` at `:111` and `:132`; self-verify counts exactly 2 at `:182-184`; one `interval '16 hours'` literal in body |
| `supabase/schema/functions/strategy_analytics_stamp_computing_started.sql` | tracked canonical trigger-fn body (D-12 run 2) | ✓ VERIFIED | 61 lines; snapshot total 105; `--check` exit 0 |
| `supabase/schema/functions/sync_strategy_analytics_status.sql` | 142 stamp semantics in the canonical body | ✓ VERIFIED | 7 `computing_started_at` occurrences |
| `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql` | Part 6 + arm D inverted + Part 4 retrofit + neutralizers deleted | ✓ VERIFIED | 925 lines; Part 6 at `:754-923` (UNGATED — header `:774-778` states it must REDDEN not skip when the migration is unapplied); arm D at `:442` now asserts stamp NOT NULL; Part 4a sentinel seeded at INSERT `:638`; residual isolation assumption in header `:104-105`; 3 × `interval '100 years'` |
| `analytics-service/tests/_scan_helpers.py` | shared `_repo_root` / `_is_pure_comment` / `_py_scan_files` / `_count_in_text` / `_count` (+ `_rel`) | ✓ VERIFIED | 6,488 bytes; `_py_scan_files` = `{main_worker.py, main.py} ∪ rglob(services) ∪ rglob(routers) ∪ rglob(scripts)`; 5 importers |
| `analytics-service/tests/test_long_fetch_follow_on_guard.py` | D-15 arity-tolerance pytest | ✓ VERIFIED | 9,654 bytes; passes (included in the 30-test run below) |
| `analytics-service/tests/test_sql_gate_scoped_updates.py` | D-05 one-rule grep gate + per-file pin + 5-polarity self-test | ✓ VERIFIED | 10,552 bytes; `_EXPECTED_MATCH_COUNTS = {metrics_by_basis: 4, reaper: 3, wizard_composite: 2}` — the reaper pin moved 0→3 with Part 6, as contracted |
| `analytics-service/tests/test_pgrst204_recovery_fallback.py` | D-02/R2 behavioural test | ✓ VERIFIED | 12,141 bytes; passes |
| `analytics-service/tests/test_computing_started_at_stamp.py` | softened AST policy + widened TS glob + terminal-warned parity | ✓ VERIFIED | `EXPECTED_TERMINAL_SITES = 4`; `EXPECTED_TOTAL = 4`; `EXPECTED_SKIPPED_UNRESOLVED = 0` (honestly re-measured, see plan-06 deviation 1); helper-built-payload self-test with positive control |
| `src/app/api/strategies/csv-finalize/route.ts` | `computation_warned: false` on terminal-failed | ✓ VERIFIED (neuter-tested) | `:768` |
| `analytics-service/services/analytics_runner.py` | corrected `_mark_computing` comment + PGRST204 fallback | ✓ VERIFIED | `:1249-1270`; `:1745-1777` — narrow `code != PGRST204 → raise`, explicit SECOND dict literal (not `dict(...)` / `{**base}`) |
| `.github/workflows/ci.yml` | `sql-tests` in `shared-test-db` | ✓ VERIFIED | `:903-905` `concurrency: group: shared-test-db, cancel-in-progress: false` |
| `.github/workflows/supabase-migrate.yml` | fail-loud unset secrets on push | ✓ VERIFIED | `:144` `IS_PUSH`; `:159-162` `::error::` × N then `exit 1` |
| `src/__tests__/critical-regressions.test.ts` | YAML-shape pins for both workflows | ✓ VERIFIED | `:1078-1128` (migrate), `:1140-1160` (sql-tests); 128 tests green |
| `.planning/REQUIREMENTS.md` | corrected JOB-03 derivation | ✓ VERIFIED | `:53` |
| `TODOS.md` | `DEF-142.1-08` + WR-01/WR-02 routing | ✓ VERIFIED | `:710-728` (item 1), `:800-...` (items 8, 9) |
| `142-VALIDATION.md` | backfilled 11/11 + map non-backfill banner | ✓ VERIFIED | see G4 |
| `142.1-VALIDATION.md` | consolidated ledger + sign-off | ✓ VERIFIED (see next section) | 370 lines |

---

## The 142.1 Ledger — adversarial audit (the thing this phase most had to get right)

Phase 142.1 exists because Phase 142's ledger was reported 11/11 Observed when 7 rows had never been run. I attacked the 142.1 ledger for a repeat of that failure and did not find one.

| Check | Result |
|-------|--------|
| Row count | **15** rows: SC-12, SC-17/01, SC-02, SC-03, SC-05, SC-06, SC-07, SC-09, SC-10, SC-11, SC-15, SC-04, SC-13, SC-14, SC-16 |
| ⛔ **SC-08 ABSENT?** | ✅ **YES — no SC-08 row exists.** `:318` carries the standing rule "SC-08 is absent by design — D-08 is CUT (CONTEXT.md correction C-5). Do not add a row for it." Nobody added one. |
| Census arithmetic | 9 + 2 + 1 + 1 + 2 = **15**. Consistent with the row count. |
| Does any row claim ✅ Observed without pasted evidence? | **No.** Every ✅ row pastes an actual failing assertion string (e.g. SC-06 `assert 1785695163.895055 > 1785695163.895055`; SC-02 `assert 1 == 2` / `assert 0 == 1` / `assert 12 == 13`; SC-05 `expected {…: 4} vs actual {…: 5}`; SC-10 the planted `basis_series.py:371` hit; SC-15 `ValueError: too many values to unpack (expected 2, got 3)`). |
| Is 15/15 claimed anywhere? | **No — the opposite.** `:290-294` carries an explicit ⛔ block: "It does not say 15/15 Observed — it is 12 observed, 1 regression-only, 2 human-only." Sign-off box `:360` repeats "⛔ This is NOT '15/15 Observed' and must never be summarised as such." |
| SC-13 / SC-14 quietly closed? | **No.** Both carry 👤 and the words "NOT closed by this phase". SC-13: "closes only on a human reading the JOB-03 text". SC-14: "this phase produced NO new mutation observation for it". Both are carried into this report's `human_verification`. |
| SC-17/01 and SC-11 RED halves recorded as observed REDs? | **No.** Both read "⚠️ RED half — NOT observed on TEST. No TEST-side mutant run was performed", then state design-proof + a LOCAL throwaway PostgreSQL 16 fixture, and explicitly disclaim the fixture: "it does not stand in for a TEST-side mutant run and is not recorded as one." |
| SC-09 credited as a catch? | **No.** "REGRESSION-ONLY — no mutation exists, and NO red was observed. Explicitly NOT recorded as caught." It even self-corrects the plan's own wrong test count (`<done>` said 8, the file has 7). |
| SC-16 (the D-16 run) over-claimed? | **No.** It states the route (MCP `execute_sql`, not `psql`), carries an EVIDENCE-CHANNEL CAVEAT that MCP does not return `RAISE NOTICE` so "no exception raised" is the pass signal, and installs two controls (pg_cron presence verified FIRST making the SKIP arms unreachable; deliberate terminal `RAISE EXCEPTION` used as a value-reporting channel). |
| D-19 disclosed as scope growth? | **Yes**, in three places — VALIDATION `:265-268`, plan-07 SUMMARY, TODOS item 6 — including the honest impact framing "LOCK-DURATION and BLAST-RADIUS defect, NOT data corruption" and the live PROD residual. |

### `nyquist_compliant: false` — deliberate, and correct

**Confirmed still `false`** (`142.1-VALIDATION.md:12`), with a seven-line frontmatter comment explaining it, and three sign-off boxes left **unchecked with `⚠️ NOT TRUE` prose** rather than ticked:

- Sampling continuity — plan 07's `T1 → T2 → T3` are exactly three consecutive tasks with no `<automated>` verify (MCP is stripped from subagents).
- Feedback latency < 45 s — false for plan 07, whose feedback is a multi-block DB run measured in hours.
- `nyquist_compliant: true` — deliberately false; the file states the consequence up front ("`audit-milestone` maps `false` to PARTIAL").

The narrower "code-producing plans only" reading is recorded as defensible and explicitly **not adopted**, with the reason: "the conservative reading cannot become an over-claim." **This is the correct outcome and is NOT treated as a gap.**

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `test_main_worker.py::_REAPER_MIGRATION_NAME` + `job_worker.py:523` | `20260803130000_reaper_limit_bound_materialized_cte.sql` | re-pointed literal + comments, same commit (`c1a57c30`, D-19 F-1) | ✓ WIRED — **neuter-proven by this verifier** | Both point at `20260803130000`. **Verifier mutation:** changing `interval '16 hours'` → `'17 hours'` INSIDE the `$cron$` body (`:137`) → `TestReaperThresholdDriftGate::test_sql_interval_literal_equals_python_constant` FAILED with `assert {'17 hours'} == {'16 hours'}` at `test_main_worker.py:2295`. **Control:** the SAME mutation applied to the prose comment at `:84` (outside `$cron$`) left the gate GREEN — grep-gate hygiene holds, prose neither satisfies nor trips the gate. Migration restored; `git status --short` empty. |
| `_mark_unrecoverable` | terminal `failed` row under PGRST204 | `except APIError, code == PGRST204` → re-issue without the key | ✓ WIRED | `analytics_runner.py:1745-1777`; non-PGRST204 codes still `raise` (narrow on purpose) |
| `csv-finalize` terminal-failed upsert | bridge branch (a) `computation_warned` read | `computation_warned: false` clears the stale marker | ✓ WIRED | neuter-proven above |
| `long_fetch.run_process_key_long_job` | `services.job_worker.JOB_CHAIN_FOLLOW_ON` | late import + unpack INSIDE the best-effort `try:` | ✓ WIRED | both the import and the 2-tuple unpack now sit after `try:`; `tail_kind` pre-declared so the `except` arm cannot NameError |
| `test_job07` / `test_dark_path_deleted` / `test_cash_basis_series_sc4` / `test_computing_started_at_stamp` | `tests/_scan_helpers.py` | `from tests._scan_helpers import` | ✓ WIRED | 5 importers; **zero** surviving duplicate definitions |
| Part 6 driver UPDATEs | `strategy_analytics_stamp_computing_started_trigger` | 3 direct `UPDATE`s, each `WHERE strategy_id = v_strat_a\|b` | ✓ WIRED | `:861`, `:884`, `:906`; grep-gate per-file pin incremented 0→3 in the same commit |
| `ci.yml` `sql-tests` | shared TEST project | `concurrency: group: shared-test-db` | ✓ WIRED | identical group name to `python` / `e2e-seeded`; the ⛔-forbidden private group name is absent |
| Part 2 arm D / Part 3 arm E | the DEPLOYED `$cron$` body | `EXECUTE v_command` from `cron.job` | ✓ WIRED | this is the oracle that caught D-19 |

---

## Data-Flow Trace (Level 4)

| Artifact | Data variable | Source | Produces real data | Status |
|----------|---------------|--------|--------------------|--------|
| trigger fn | `NEW.computing_started_at` | `OLD` tuple / `now()` | Yes — TEST probe observed sentinel `2026-08-02 18:09:15.214004+00` surviving an advance to `now()` (3-hour gap = the seeded interval, so this is coercion, not two coincidentally equal stamps) | ✓ FLOWING |
| companion cron arm | `strategy_analytics.computing_started_at` | `UPDATE ... SET computing_started_at = now() WHERE ... IS NULL AND no active job` | Yes — arm D passed against the real deployed body on TEST | ✓ FLOWING |
| reaper arm (post-D-19) | batch of ≤25 `strategy_id` | `WITH batch AS MATERIALIZED (... LIMIT 25 FOR UPDATE SKIP LOCKED)` | Yes — measured on TEST `failed=25, still_computing=1` from 26 seeds | ✓ FLOWING |
| `_scan_helpers._py_scan_files` | file list | `rglob` walks, no hand-written list | Yes — SC-10's planted identifier in `services/basis_series.py:371` reddened the gate, a file no prior narrow list contained | ✓ FLOWING |
| TS census | `_ts_scan_files` hits | `src/**/*.ts` ∪ `src/**/*.tsx` | Yes — SC-04's probe in `src/lib/` reddened the census; `EXPECTED_TERMINAL_SITES = 4` pins the population against vacuity | ✓ FLOWING |

---

## Behavioral Spot-Checks (run by this verifier, not transcribed)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| SQL function snapshot gate is green | `npx tsx scripts/dump-sql-functions.ts --check` | `SQL function snapshot is current (105 functions).` exit 0 | ✓ PASS |
| Phase 142.1 pytest surface | `python3 -m pytest tests/{test_sql_gate_scoped_updates,test_pgrst204_recovery_fallback,test_long_fetch_follow_on_guard,test_computing_started_at_stamp,test_job07_reaper_off_worker_loop,test_dark_path_deleted}.py -q` (from `analytics-service/`) | `30 passed in 5.17s` | ✓ PASS |
| Reaper threshold + drift gates | `python3 -m pytest tests/test_main_worker.py -q -k "Reaper or reaper or Drift or Threshold"` | `8 passed, 47 deselected` | ✓ PASS |
| Workflow YAML shape pins | `npx vitest run src/__tests__/critical-regressions.test.ts --no-file-parallelism` | `128 passed` | ✓ PASS |
| **Neuter #1 — drift gate reads the DEPLOYED body** | mutate `interval '16 hours'` → `'17 hours'` at `20260803130000:137`, run `-k Drift` | `1 failed` — `assert {'17 hours'} == {'16 hours'}` at `test_main_worker.py:2295`; restored, tree clean | ✓ PASS (falsifiable) |
| **Neuter #1b — control: prose must NOT trip the gate** | same mutation at `:84` (comment, outside `$cron$`) | `3 passed` — gate correctly ignores prose | ✓ PASS (hygiene holds) |
| **Neuter #2 — money-surface parity rule** | delete `computation_warned: false` from `csv-finalize/route.ts:768`, run `-k terminal` | `1 failed` naming `route.ts:767: terminal "failed"` with the green-factsheet message; restored, tree clean | ✓ PASS (falsifiable) |
| Debt markers in the 22 phase-modified files | `grep -nE "\bTBD\b\|\bFIXME\b\|\bXXX\b"` and `TODO/HACK/PLACEHOLDER` over the `a3d62a5e..41eeca8f` diff | **zero hits in both tiers** | ✓ PASS |
| Working tree integrity after verification | `git status --short` | empty | ✓ PASS |

**Not re-run by this verifier (orchestrator-established, and deliberately not re-derived):** full pytest (4853 passed / 96 skipped), `mypy --strict` (89 files clean), `npx tsc --noEmit` (exit 0), `npm run lint` (0 errors). **Not run at all, by project rule:** anything against a Supabase project.

---

## Probe Execution

No `scripts/*/tests/probe-*.sh` files exist in this repository and no PLAN or SUMMARY declares a probe path. Step 7c: **SKIPPED (no probes in this project).** The analogous artifact — the 637-line SQL gate — is orchestrator/CI-only (needs `TEST_SUPABASE_DB_URL`) and is covered under human verification and the orchestrator evidence.

---

## Requirements Coverage

| Requirement | Source plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| JOB-01 | 01, 02, 03, 04, 05, 06, 07, 08 (all 8) | `strategy_analytics` carries a writer-stamped `computing_started_at` set in the same statement as `computation_status='computing'` | ✓ SATISFIED (strengthened) | 142.1 moved the invariant from a per-writer convention to a TABLE-level `BEFORE UPDATE` trigger, closing the class of writer that stamps but stamps at the WRONG time. Falsifiable at Part 6/6a-6b-6c. |
| JOB-02 | 01, 02, 03, 05, 06, 07 | pg_cron reaper transitions stranded rows to terminal `failed` with a recoverable message | ✓ SATISFIED (strengthened) | 142.1 added the non-destructive companion clock-start arm (D-11) and restored the LIMIT bound (D-19). Both verified against the real deployed body on TEST. |
| JOB-03 | 01, 02, 07 | Threshold derived from the chain-inclusive worst case (43,920 s), never the `compute_jobs` formula | ✓ SATISFIED in code and text; **judgment routed to SC-13** | `REQUIREMENTS.md:53` rewritten; drift gate re-pointed at the deployed body and neuter-proven. Whether the WORDING is a correct and sufficient statement is a human read (SC-13). |

**Orphaned requirements:** none. `REQUIREMENTS.md`'s coverage table maps JOB-01/02/03 (and JOB-07) to **Phase 142**, not 142.1; 142.1 declares "no new REQ IDs" and adds none. All three declared IDs are accounted for.

ℹ️ **Traceability note (informational, not a gap):** D-10 widened the **JOB-07** structural gate (`test_job07_reaper_off_worker_loop.py`), but no 142.1 plan lists `JOB-07` in its `requirements:` frontmatter. The work belongs to Phase 142's JOB-07 and the ID is still owned there, so nothing is lost — but a reader tracing JOB-07 forward will not find 142.1 from the frontmatter.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD` / `FIXME` / `XXX` debt markers across all 22 phase-modified files | — | **ZERO hits.** The debt-marker gate is clean. |
| — | — | `TODO` / `HACK` / `PLACEHOLDER` | — | **ZERO hits.** |
| `supabase/migrations/20260803130000_...sql` | 188 | Dead self-verify guard: `v_command ~* 'IN\s*\(\s*SELECT[^)]*LIMIT'` cannot match, because `[^)]*` cannot cross the `)` of the intervening `AND NOT EXISTS (...)` | ℹ️ Info | **Not exposure.** The bound is genuinely guarded by the `v_mat <> 2` MATERIALIZED-count check immediately above, which does fire. Deliberately not fixed in place because `20260803130000` is already applied to TEST and editing an applied migration is a tracked invariant violation. Recorded as `TODOS.md` item 8 (WR-01) with a named forward-only remedy. |
| `142.1-08-SUMMARY.md` | 304 | SUMMARY records the `awk`-range hazard as closed when it is not — plan 05 later added "Part 6" mentions at gate lines `:595` / `:602`, so `awk '/Part 6/,0'` now starts at `:595` and sweeps Parts 4-6 | ⚠️ Warning (documentation) | **Verifier-confirmed:** `awk '/Part 6/{print NR; exit}'` returns **595**, not 754. The measured value is **0 either way**, so there is no false green. Already recorded honestly as `TODOS.md` item 9 (WR-02), including the observation that the SUMMARY over-claims. Does not clear the founder blast-radius bar. |
| `.planning/STATE.md` | 51, 250-252, 255 | Stale orchestrator ledger: `awaiting /gsd:plan-phase 142.1` and `Plan: 0 of 0 (142.1 not planned)` while 8/8 plans exist and are executed; and `no 142-VERIFICATION.md exists` while that file exists with `status: gaps_found, score: 9/10` | ⚠️ Warning (documentation) | Not user-facing, not data-integrity. Flagged because this phase's own subject is evidence integrity, and STATE.md is the orchestrator's ledger — a reader consulting it would get three wrong facts. Worth one cleanup commit. |
| `142.1-VALIDATION.md` | 343 | Stale tense: "`TestReaperThresholdInvariant`'s premise is **currently** false ... SC-17/01 **fixes** the code so the premise becomes true" — SC-17/01 has shipped, so the premise now holds | ℹ️ Info | Prose only. |

**Stub classification applied:** none of the `return null` / `= []` / `= {}` shapes in the phase's diff are stubs — every one is either a test fixture, a pre-declared `tail_kind: str | None = None` (whose whole purpose is the `except` arm), or a deliberate `NEW.computing_started_at := NULL` exit-clear, which is the specified behaviour.

---

## Self-Corrections by Executors — verified as recorded, and correct

The prompt asked me to check that honest self-corrections are recorded accurately, not penalised. Both hold up:

| Executor | What it refused to do | Verified in the tree | Verdict |
|----------|----------------------|----------------------|---------|
| Plan 06 | Refused to force `EXPECTED_SKIPPED_UNRESOLVED` from 0 → 1 as plan 04 forecast, because that forecast was conditional on building the fallback with `dict(...)` / `{**base}` — which C-12 forbids | `test_computing_started_at_stamp.py:661` reads `EXPECTED_SKIPPED_UNRESOLVED = 0`. The fallback at `analytics_runner.py:1768-1777` IS an explicit second dict literal, so it is statically resolvable and produces no skip. The census was **re-measured from a live scan**, not assumed | ✓ CORRECT — re-measuring beats satisfying an inherited literal |
| Plan 02 | Left acceptance criterion 3 (`grep -cF 'backlog_rows=5_000' == 0`) knowingly UNMET rather than gut a different test or hide the literal behind a constant | The surviving site is `:399`, inside `test_healthz_stays_200_with_large_stranded_backlog`, where `assert len(stranded) == 5_000` at `:405` is the fixture-honesty guard the docstring cites and the test's own NAME states the number. The site D-09 actually names (`:303`) IS zeroed | ✓ CORRECT — the acceptance grep was written assuming one call site; gaming it would have damaged the codebase |
| Plan 03 | Removed the ⛔-forbidden group name from its own comment because naming it made a `grep -c … == 0` criterion return 1 | `ci.yml:893-897` warns by pointing at `142-REVIEW.md:286-291` instead of quoting the forbidden name | ✓ CORRECT |
| Plan 05 | Reported one acceptance criterion as unmeetable-as-written rather than working around it | Recorded in 05-SUMMARY deviation 5 | ✓ CORRECT |

---

## Human Verification Required

Seven items, all of them surfaced by the phase itself rather than discovered here — which is itself evidence the ledger is honest. Full detail in the frontmatter `human_verification` block.

1. **SC-13** — read the rewritten JOB-03 requirement text against the shipped `TestReaperThresholdInvariant`. Greps prove string presence only.
2. **SC-14** — confirm the seven backfilled Phase-142 ledger rows against `142-VERIFICATION.md`. This phase transcribed, it did not re-run.
3. **D-01 prose** — judge whether the replacement `_mark_computing` comment is TRUE. (Its behavioural claim WAS confirmed live on TEST.)
4. **D-19 merge-time PROD check** — confirm the deployed cron body carries TWO single-evaluation `MATERIALIZED` batches. Token presence is not enough; token presence is exactly what two phases of gates passed over. ⛔ **PROD still runs the unbounded body until this branch merges.**
5. **`supabase-migrate` fail-loud** — needs a real push to `main` on a repo with the secrets removed. The shape is pinned; GitHub's dispatch of it is not.
6. **SC-17/01 and SC-11 RED halves** — decide whether the design-proof + local-fixture evidence is accepted, or whether TEST-side mutant runs are wanted.
7. **Carried from 142** — PROD cron registration, PROD backfill census, live wizard render.

---

## Gaps Summary

**No gaps.** No must-have truth FAILED, no artifact is missing or a stub, no key link is unwired, and the debt-marker gate is clean across all 22 phase-modified files. Two independent neuter tests run by this verifier (drift gate, terminal-warned parity) confirmed the two most load-bearing gates are genuinely falsifiable and not green-over-nothing, and a control mutation confirmed the drift gate's prose-hygiene scoping.

Three things a reader should carry forward, none of which clears the founder blast-radius bar:

1. **The goal says "16 items"; the honest number is 15 closed + 1 CUT.** D-08 was cut by CONTEXT correction C-5 **before any plan ran**, is tracked as `DEF-142.1-08` in `TODOS.md`, is explicitly excluded from the ledger, and — the thing that mattered most — was **not** closed by bumping `assert len(TIMEOUT_PER_KIND) == 15` to 16. Verified: the literal is still 15. This is a documented scope decision, not an incomplete implementation. If you want the accounting to read cleanly rather than qualified, add:

```yaml
overrides:
  - must_have: "Close the 16 items raised by three independent passes over Phase 142"
    reason: "D-08 was CUT before execution (CONTEXT.md correction C-5) — no derivation source exists for its proposed replacement, and it collides with the GSD Oracle-Independence convention requiring literal registry counts. Tracked as DEF-142.1-08 in TODOS.md with a hard rule forbidding closure-by-literal-bump. 15 of 16 items closed."
    accepted_by: "{your name}"
    accepted_at: "{ISO timestamp}"
```

2. **The phase shipped one migration MORE than it planned.** `20260803130000` (D-19) was in no plan, no wave and no ledger row. It exists because the D-16 run finally happened, and it fixed a real bound defect that every static gate in two phases passed over. This is scope growth, correctly disclosed in three places — and it is the single strongest piece of evidence that D-16 was worth doing.

3. **`nyquist_compliant: false` and the three unchecked sign-off boxes are the CORRECT outcome**, not a residual to be cleaned up. `audit-milestone` will map this phase to PARTIAL. That is the accurate signal.

**Status is `human_needed`, not `passed`,** solely because seven items genuinely require a human or a PROD/TEST session. Every automated check this verifier could run is green.

---

_Verified: 2026-08-03T05:48:48Z_
_Verifier: Claude (gsd-verifier) — goal-backward, FORCE stance_
