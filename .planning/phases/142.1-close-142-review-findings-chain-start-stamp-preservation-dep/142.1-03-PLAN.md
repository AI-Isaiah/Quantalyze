---
phase: 142.1
plan: 03
type: execute
wave: 2
depends_on: [142.1-01]
files_modified:
  - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
  - .github/workflows/ci.yml
  - .github/workflows/supabase-migrate.yml
  - src/__tests__/critical-regressions.test.ts
  - analytics-service/tests/test_sql_gate_scoped_updates.py
autonomous: true
requirements: [JOB-01, JOB-02]

must_haves:
  truths:
    - "The three cross-tenant neutralizing UPDATEs (:340, :443, :466) are DELETED — not narrowed, not re-targeted (CONTEXT D-18 Part 3): under plan 142.1-05's UPDATE-only trigger the stale-stamp half of any neutralizer would be a silent no-op (trigger arm (a) reverts NEW := OLD) while the grep gate reported green — the exact silent-narrowing class this phase exists to kill"
    - "Isolation is BY CONSTRUCTION, not by mutation: rows the gate expects the reaper to reap are seeded at an extreme-old computing_started_at (now() - interval '100 years'), so the deployed reap arm's ORDER BY computing_started_at ASC … LIMIT 25 deterministically selects them ahead of any plausible foreign row; every assertion is scoped to the block's own seeded ids — the project's recorded e2e-seeded lesson: assert your OWN seed invariant, never a global count"
    - "The gate header states the residual isolation assumption honestly: correctness of the reap-arm assertions rests on no foreign row on the shared TEST project carrying a computing_started_at older than the seed epoch"
    - "A new grep gate reddens if anyone adds an UPDATE on strategy_analytics — schema-qualified OR unqualified (`UPDATE strategy_analytics` at test_metrics_by_basis_write.sql:68,71,74,83 is the proof the unqualified form is live today) — in supabase/tests/ whose strategy_id predicate is not bound to a block-local seeded identifier; NO neutralization form remains to special-case, and a per-file literal pin keeps the scanner anti-vacuous"
    - "The sql-tests CI job is serialized into the existing shared-test-db concurrency group — an innocent PR can no longer hit 55P03 under the 5 s lock_timeout"
    - "supabase-migrate.yml can no longer go green while applying nothing on a push to main — unset secrets now fail loud (::error:: + exit 1), closing PGRST204 producer 1; both workflow shapes are pinned by vitest YAML-shape assertions"
  artifacts:
    - path: "analytics-service/tests/test_sql_gate_scoped_updates.py"
      provides: "D-05 grep gate over supabase/tests/*.sql (comment-strip-first; ONE rule — block-local seeded strategy_id predicate; matches qualified AND unqualified UPDATE forms; per-file anti-vacuity pin)"
      min_lines: 50
    - path: ".github/workflows/ci.yml"
      provides: "sql-tests job carries group: shared-test-db, cancel-in-progress: false"
      contains: "shared-test-db"
    - path: ".github/workflows/supabase-migrate.yml"
      provides: "fail-loud unset-secrets branch on push"
      contains: "::error::"
  key_links:
    - from: ".github/workflows/ci.yml sql-tests job"
      to: "shared TEST project qmnijlgmdhviwzwfyzlc"
      via: "concurrency group shared-test-db (same group as python and e2e-seeded)"
      pattern: "group:\\s*shared-test-db"
    - from: "src/__tests__/critical-regressions.test.ts"
      to: "both workflow files"
      via: "job-slicing regex + expectMatch (analog :1033-1065)"
      pattern: "shared-test-db"
    - from: "Part 2/3 extreme-old seeds (now() - interval '100 years')"
      to: "the deployed reap arm's ORDER BY computing_started_at ASC LIMIT 25"
      via: "isolation-by-construction — seeded rows outrank any plausible foreign row for the budget; assertions scoped to the seeded set"
      pattern: "interval '100 years'"
---

<objective>
Close D-05 per the settled CONTEXT D-18 Part 3 (DELETE the three cross-tenant neutralizing
UPDATEs entirely; replace isolation-by-mutation with isolation-by-construction via extreme-old
seeds and seeded-set assertions; simplify the grep gate to ONE rule; serialize sql-tests into
the existing shared-test-db group) and D-02/R1 (the supabase-migrate silent-green skip — the
only PGRST204 producer that is both silent and reachable today), pinning both with vitest
YAML-shape gates.

Purpose: constraint 3 (LOCKED) — D-05's gate rework must land before D-16's run against the
shared TEST project. ⛔ Iteration 1's fresh-stamp re-target is WITHDRAWN (D-18): under plan
142.1-05's trigger, a neutralizing UPDATE against a stale-stamped foreign computing row is
silently reverted by trigger arm (a) — the mechanism would be dead while its grep gate stayed
green. Deleting the writes closes D-05 more thoroughly than narrowing did: the cross-tenant
writes are REMOVED, not scoped.
Output: retrofitted SQL gate seeds/assertions/header, hardened ci.yml + supabase-migrate.yml,
one new pytest grep gate, two new vitest assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-CONTEXT.md
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-05 — DELETE the three neutralizing UPDATEs; isolation-by-construction (extreme-old seeds + seeded-set assertions); ONE-rule grep gate</name>
  <files>supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql, analytics-service/tests/test_sql_gate_scoped_updates.py</files>
  <read_first>
    - .planning/phases/142.1-.../142.1-CONTEXT.md § "⭐ D-18" Part 3 (the SETTLED design this task implements — do not re-derive it, do not restore the withdrawn fresh-stamp re-target)
    - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:338-343, :443-446, :466-469 (the three neutralizing UPDATEs to DELETE, with their adjacent comments); :312-336 (Part 2's four arm seeds — arm A at :314-316 moves to the extreme-old epoch); :432-441 (Part 3's 26-row staggered seed loop — moves to the extreme-old epoch); :448-478 (Part 3 arm E's tick-1/tick-2 assertions to reseat identity-scoped); :52-81 (the header's TRANSACTION FRAMING and "SHARED-TEST-DB ISOLATION (why the neutralization UPDATE exists)" sections — BOTH need rewriting: the lock_timeout rationale at :63-66 references the neutralization UPDATE, and :68-81 justifies a mechanism this task deletes)
    - ⚠️ the UPDATE at :553-555 (`WHERE strategy_id = v_strat`) is OUT OF SCOPE here — plan 142.1-05's Part-4 retrofit deletes it in the same commit as the trigger; this task leaves it byte-unchanged and the gate must ACCEPT it as a block-local seeded form
    - 142.1-PATTERNS.md § "10. D-05's grep gate" (analog test_f4_sweep_fixture_parity.py:26-56 — comment-strip-FIRST pure-stdlib pytest over supabase/tests/*.sql)
    - supabase/tests/test_wizard_composite_members.sql:314,338 (`UPDATE public.strategy_analytics ... WHERE strategy_id = strat_comp` — legitimately scoped under a different variable name; the gate must accept)
    - supabase/tests/test_metrics_by_basis_write.sql:68,71,74,83 (`UPDATE strategy_analytics ... WHERE strategy_id = strat` — the UNQUALIFIED form, legitimately scoped; the gate's pattern must match `UPDATE\s+(public\.)?strategy_analytics` or an unscoped unqualified UPDATE added later evades it)
    - analytics-service/tests/_scan_helpers.py IF plan 142.1-02 has merged (same wave — it may not have; if absent, use the parents[2] repo-root idiom from test_f4_sweep_fixture_parity.py:26-31 instead; do NOT block on it)
  </read_first>
  <action>
    Implement CONTEXT D-18 Part 3 exactly. Three coupled edits to the reaper SQL gate, all in
    this ONE commit (deleting the neutralizers without the seed/assertion retrofit would leave
    the gate flaky in between):

    1. DELETE the three cross-tenant neutralizing UPDATEs entirely — the statements at
       :340-343 (Part 2), :443-446 (Part 3 pre-tick-1), :466-469 (Part 3 pre-tick-2) — along
       with their adjacent "Shared-TEST-DB isolation" comments. Do NOT narrow them. Do NOT
       re-target them to now() (iteration 1's re-target is WITHDRAWN by D-18: under plan
       142.1-05's UPDATE-only trigger, an UPDATE that rewrites computing_started_at on a
       foreign row whose stale stamp is non-NULL is silently reverted by trigger arm (a) —
       NEW := OLD — so the mechanism would be dead code that a grep gate still reports green;
       that is the D-04/D-10 silent-narrowing class this phase exists to kill).

    2. Replace isolation-by-mutation with isolation-by-construction:
       - Part 2 arm A's seed (:314-316) changes its computing_started_at from
         `v_fresh - interval '100 days'` to `v_fresh - interval '100 years'` (update the arm-A
         seed comment and the "stranded 100 days" phrasing in the arm-A assertion message).
         Arms B (fresh stamp), C (active-job conjunct excludes it from the candidate set
         regardless of stamp age) and D (NULL stamp) keep their current seeds.
       - Part 3's loop seed (:439-440) changes to
         `v_fresh - interval '100 years' - (i * interval '1 minute')` — 26 staggered
         extreme-old rows, i=26 the oldest, i=1 the youngest seed.
       - WHY this works: the deployed reap arm is `ORDER BY computing_started_at ASC …
         LIMIT 25`, so seeds a century old sort ahead of any plausible foreign stranded row —
         the seeded rows win the budget by construction instead of by mutating foreign rows
         out of the candidate set.
       - Reseat Part 3 arm E IDENTITY-scoped (D-18: "my 25 expected-reaped seeds were reaped
         and my youngest seed was not" — never a global count): capture
         `v_youngest := v_seeded[1]` (the i=1, least-old seed). After tick 1 assert
         (i) `SELECT count(*) … WHERE strategy_id = ANY (v_seeded) AND strategy_id <> v_youngest
         AND computation_status = 'failed'` equals 25 — the 25 oldest seeds were all reaped
         (fewer ⇒ not draining / foreign crowd-out ⇒ RED), and (ii) the row for v_youngest is
         still 'computing' — if the LIMIT were gone the reap would have taken it too ⇒ RED
         (this replaces the old "exactly 25 of 26" global-shape as the bounded proof). After
         tick 2 (which now has NO pre-EXECUTE neutralizer) assert v_youngest is 'failed' and
         the seeded failed count equals 26 (progressing). Update arm E's RAISE messages and
         the Part 3 header comment to the identity-scoped semantics.

    3. Rewrite the file header:
       - :68-81 "SHARED-TEST-DB ISOLATION (why the neutralization UPDATE exists)" becomes
         "SHARED-TEST-DB ISOLATION (isolation by construction)": the gate seeds its
         must-be-reaped rows at now() - interval '100 years' so they outrank any plausible
         foreign row for the reap arm's ORDER BY … LIMIT 25 budget, and every assertion is
         scoped to the block's own seeded ids. STATE THE RESIDUAL ASSUMPTION HONESTLY
         (D-18): isolation now rests on no foreign row on the shared TEST project carrying a
         computing_started_at older than the seed epoch. Cite the project's e2e-seeded
         lesson: assert your OWN seed invariant, never a global count or a global empty
         state.
       - :63-66's lock_timeout rationale (currently "bounds the row locks the neutralization
         UPDATE (below) takes") is rewritten: the 5 s lock_timeout now bounds the locks the
         EXECUTEd deployed body itself takes — its reap statement is FOR UPDATE SKIP LOCKED
         and can briefly lock up to 25 foreign candidate rows inside the part's transaction
         until ROLLBACK; SKIP LOCKED means the gate never BLOCKS on other tenants' locks, and
         Task 2's serialization removes the concurrent-gate-run case.

    Create `analytics-service/tests/test_sql_gate_scoped_updates.py` (pure-stdlib pytest,
    modelled on test_f4_sweep_fixture_parity.py), implementing D-18's SIMPLIFIED gate — ONE
    rule, no special cases: for every `supabase/tests/test_*.sql` file, strip SQL line
    comments FIRST (`--[^\n]*` — the reaper gate's header discusses UPDATE patterns in
    prose), then every statement matching `UPDATE\s+(public\.)?strategy_analytics` — the
    optional-schema alternation is MANDATORY: test_metrics_by_basis_write.sql:68,71,74,83 use
    the unqualified form and are correctly scoped today, but an unscoped unqualified UPDATE
    added later must not evade a `public.`-only pattern — MUST carry a strategy_id predicate
    bound to a block-local seeded identifier: `strategy_id = <plpgsql identifier>`
    (`= v_strat` at reaper :553, `= strat` in test_metrics_by_basis_write.sql, `= strat_comp`
    in test_wizard_composite_members.sql) or `strategy_id = ANY (<identifier>)`. There is NO
    accepted `<> ALL (…)` form and NO staleness-conjunct branch — the neutralization form no
    longer exists in the tree after edit 1 above.
    Anti-vacuity pin (S-2), re-derived against the REAL tree at this commit (the old literal
    3 counted the now-deleted neutralization sites and is WRONG): a module-level literal
    per-file map — {"test_metrics_by_basis_write.sql": 4,
    "test_wizard_composite_members.sql": 2,
    "test_strategy_analytics_stuck_computing_reaper.sql": 1} (total 7; the reaper's 1 is the
    Part-4 sentinel site at :553) — asserted exactly: a file with matches absent from the map,
    or a count drift, reddens with a message naming D-05. Comment beside the map: plan
    142.1-05's Part-4 retrofit deletes the :553 site (reaper → 0) and plan 142.1-08's Part 6
    adds three scoped driver UPDATEs (reaper → 3); EACH of those plans updates this map in
    the SAME commit as its SQL edit — that is the pin doing its job, not churn.
    Include a scanner self-test against synthetic in-memory SQL covering five polarities: an
    unscoped SCHEMA-QUALIFIED UPDATE is flagged; an unscoped UNQUALIFIED UPDATE is flagged;
    `= v_strat` passes; `= ANY (v_seeded)` passes; the OLD neutralization shape
    `<> ALL (v_seeded)` is now FLAGGED (it is not an accepted form) — so the gate's green is
    falsifiable without touching repo files.
    SC-05 probe, pre-commit: transiently ADD an unscoped, schema-UNQUALIFIED
    `UPDATE strategy_analytics SET computation_error = NULL;` inside one supabase/tests file,
    run the new pytest, observe RED (both the rule and the pin fire), paste output into
    SUMMARY, revert, re-run green.
    Commit: `test(142.1): delete cross-tenant neutralizers — isolation by construction + one-rule grep gate (D-05, D-18)`.
  </action>
  <verify>
    <automated>bash -c "cd analytics-service && pytest tests/test_sql_gate_scoped_updates.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^[[:space:]]*--' supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql | grep -c '<> ALL (v_seeded)'` returns 0 (all three neutralizers deleted; the `= ANY (v_seeded)` assertion scopes survive and are unaffected by this pattern; never use `\s` in BRE — BSD grep on macOS does not support it and the comment strip silently fails)
    - `grep -v '^[[:space:]]*--' supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql | grep -c 'SET computing_started_at = NULL'` returns 0 (the NULL-manufacturing technique is gone; note Part 1's `computing_started_at\s*=\s*NULL` FUNCTIONDEF anchor at :185 is a regex inside a quoted literal and unaffected)
    - `grep -c "interval '100 years'" supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql` >= 2 (Part 2 arm A + Part 3 loop)
    - Part 3 declares v_youngest and asserts it is still 'computing' after tick 1 and 'failed' after tick 2; no pre-tick-2 neutralizer remains
    - The header contains the residual-assumption statement ("no foreign row … older than the seed epoch") and no longer titles a section "why the neutralization UPDATE exists"
    - The UPDATE at the old :553 still reads `WHERE strategy_id = v_strat` (byte-unchanged — plan 142.1-05 owns its deletion) and the new gate ACCEPTS it
    - New pytest green on the otherwise-unmodified repo, including test_metrics_by_basis_write.sql's four unqualified `= strat` sites and test_wizard_composite_members.sql's two `= strat_comp` sites; its self-test drives all five polarities on synthetic SQL; the per-file pin totals 7
    - SC-05 transient-mutation RED pasted in SUMMARY
  </acceptance_criteria>
  <done>The cross-tenant writes are REMOVED, not scoped; the gate isolates by construction with the residual assumption stated; the grep gate is one-rule, matches the unqualified form, and is anti-vacuously pinned; SC-05 observed.</done>
</task>

<task type="auto">
  <name>Task 2: D-05 — serialize sql-tests into shared-test-db; pin with a vitest YAML gate</name>
  <files>.github/workflows/ci.yml, src/__tests__/critical-regressions.test.ts</files>
  <read_first>
    - .github/workflows/ci.yml:830-880 (the sql-tests job — currently NO concurrency block), :1071-1084 (the python job — origin of group shared-test-db), :1459-1483 (e2e-seeded — the one to COPY: proves needs: + the same group is safe in this repo, and its comment is the house style to mirror)
    - src/__tests__/critical-regressions.test.ts:1033-1065 (the job-slicing regex + expectMatch analog) and :563-576 (findOrFail/expectMatch/expectNoMatch helpers), :21 (readText)
  </read_first>
  <action>
    Per D-05: add to the `sql-tests` job in ci.yml, after its `if:` line, the EXISTING group
    verbatim — `concurrency:` with `group: shared-test-db` and `cancel-in-progress: false` —
    plus a comment in the :1459-1478 house style naming WHY: the reaper gate EXECUTEs the
    real deployed cron body against the shared TEST project, which can briefly FOR UPDATE
    SKIP LOCKED up to 25 foreign candidate rows per tick inside a rolled-back transaction
    under a 5 s lock_timeout — two concurrent gate runs (or a gate run against python/
    e2e-seeded writes) must serialize, not race (queues behind python ~7 min and that cost is
    accepted, as ci.yml:1475-1478 already documents for e2e-seeded). ⛔ Do NOT invent
    `shared-test-supabase-sql` — 142-REVIEW.md:286-291 proposes it and a DIFFERENT group name
    does not serialize against the python/e2e-seeded jobs writing the same project.
    In critical-regressions.test.ts, add a describe beside the :1033-1065 analog: slice the
    `^ {2}sql-tests:` job out of ci.yml with the same stop-at-next-job regex, then
    expectMatch(/group:\s*shared-test-db/) and expectMatch(/cancel-in-progress:\s*false/), with
    failure messages naming D-05 and the 55P03 flake class.
    Run the new vitest describe; then transiently change the group name in ci.yml to a junk
    value, observe the assertion RED, revert, re-run green (falsifiability for the YAML gate).
    Commit: `ci(142.1): serialize sql-tests into shared-test-db + YAML-shape pin (D-05)`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The sql-tests job in ci.yml contains `group: shared-test-db` and `cancel-in-progress: false`
    - `grep -c 'shared-test-supabase-sql' .github/workflows/ci.yml` returns 0
    - The new vitest assertion passes, and its transient-mutation RED is pasted in SUMMARY
  </acceptance_criteria>
  <done>sql-tests serialized with python/e2e-seeded; shape pinned by vitest; A5 (no deadlock — sql-tests has no needs: on python) noted in the SUMMARY for confirmation on the first CI run.</done>
</task>

<task type="auto">
  <name>Task 3: D-02/R1 — fail loud when supabase-migrate secrets are unset; pin with a vitest YAML gate</name>
  <files>.github/workflows/supabase-migrate.yml, src/__tests__/critical-regressions.test.ts</files>
  <read_first>
    - .github/workflows/supabase-migrate.yml:84-88 (push: branches:[main] trigger — the workflow only runs when migrations genuinely merged), :129-141 (the silent-green branch under repair: ::notice:: + configured=false), :143-148 and :162-164 (the downstream gates that skip)
    - 142.1-PATTERNS.md § "9. supabase-migrate.yml — silent green skip" (the fail-loud analog ci.yml:940-956, verbatim, and the R1 design: gate the hard fail on github.event_name == 'push')
    - .planning/phases/142.1-.../142.1-RESEARCH.md § "D-02 — deploy sequencing" (why this is producer 1 of PGRST204 — C-2: the causal story in the item text is INVERTED; on the normal merge path the DDL lands FIRST; the silent skip is the reachable producer)
  </read_first>
  <action>
    Per D-02/R1 with correction C-2 (the remedy is R1+R2, NOT a column-capability probe — that
    was explicitly REJECTED as re-importing D-07's static-resolution problem): in
    supabase-migrate.yml's "Check secrets are configured" step, replace the silent branch — when
    `SUPABASE_PROJECT_REF` / `SUPABASE_ACCESS_TOKEN` / `SUPABASE_DB_PASSWORD` are unset AND
    `github.event_name == 'push'`, emit `::error::` lines naming each missing secret and
    `exit 1` (mirroring ci.yml:940-956's "a skip is not tolerated" idiom). Keep the tolerant
    `configured=false` + `::notice::` path ONLY for `workflow_dispatch` (a manual dispatch on an
    unconfigured clone stays tolerable). There is no legitimate unconfigured path on a push to
    main: migrations merged means PROD must be migrated, and a green no-op here is exactly how a
    new worker meets an old schema (PGRST204) while every dashboard shows green.
    In critical-regressions.test.ts, add a describe beside the plan/apply env-gate symmetry
    analog (:1033-1049): slice the `plan` job (or the check step's run block) and
    expectMatch a `::error::` + `exit 1` shape inside the unset-secrets branch, and
    expectMatch(/github\.event_name\s*==\s*'push'/) so the push-gating survives; failure message
    names D-02/R1 and the silent-green producer.
    Transiently revert the yml branch to ::notice::-only, observe the vitest RED, revert,
    re-run green.
    R1's live behavior (workflow actually reddening with secrets unset) is NOT per-commit
    observable — record in the SUMMARY that the YAML-shape assertion + reviewer inspection is the
    standing observable, per 142.1-VALIDATION.md Manual-Only table.
    Commit: `ci(142.1): supabase-migrate fails loud on unset secrets for push (D-02/R1)`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - supabase-migrate.yml's unset-secrets branch on push emits `::error::` and `exit 1`; workflow_dispatch keeps the tolerant path
    - The tolerant `::notice::` path survives ONLY inside the workflow_dispatch-gated branch — asserted by the vitest describe's sliced-job shape (the push-gate condition co-located with the ::error::/exit 1 pair), NOT by a whole-file grep count (a bare `grep -c … == 0` cannot express "zero on the push path"); reviewer additionally confirms by reading the yml diff
    - New vitest assertion passes; transient-mutation RED pasted in SUMMARY
    - `npm run typecheck` exits 0 and full `npx vitest run src/__tests__/critical-regressions.test.ts` green
  </acceptance_criteria>
  <done>PGRST204 producer 1 closed fail-loud; shape pinned; the live-workflow half recorded as reviewer-observable only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runners → shared TEST project | concurrent jobs from different PRs write one physical database |
| merge-to-main → PROD schema | supabase-migrate is the only automatic DDL applier; its silence is a schema/code divergence vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.1-05 | Denial of Service (CI) | cross-tenant row locks on the shared TEST project during gate runs under 5 s lock_timeout | mitigate | Task 1 DELETES the cross-tenant neutralizing writes (D-18) — the gate itself no longer mutates foreign rows; residual locking is only the deployed body's own SKIP LOCKED reap inside a rolled-back txn; Task 2 serializes sql-tests into shared-test-db; the one-rule grep gate keeps future test authors honest |
| T-142.1-06 | Tampering (integrity) | silent-green migrate skip → new worker against old schema → PGRST204, job cannot even stamp failed | mitigate | Task 3 fail-loud on push; vitest YAML pin; R2 (plan 142.1-06) removes the amplifier |
| T-142.1-SC | Tampering | package installs | accept (N/A) | no installs this phase |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_sql_gate_scoped_updates.py -q` green
- `npx vitest run src/__tests__/critical-regressions.test.ts` green
- `npm run typecheck` exits 0
- `cd analytics-service && pytest tests/test_computing_started_at_stamp.py tests/test_job07_reaper_off_worker_loop.py tests/test_main_worker.py -x -q` still green
</verification>

<success_criteria>
- D-05 fully closed per D-18 Part 3 (neutralizers DELETED + isolation-by-construction + residual assumption stated + serialization + one-rule grep gate + YAML pin) BEFORE any TEST gate run — constraint 3 satisfied for plan 142.1-07
- No `<> ALL (v_seeded)` neutralization form remains anywhere; the grep gate has no special-case branch for one
- The grep gate matches the schema-UNQUALIFIED `UPDATE strategy_analytics` form (test_metrics_by_basis_write.sql proves it exists in the tree)
- D-02/R1 closed with the push-gated fail-loud shape; C-2's corrected causal story honored (no capability probe built)
- SC-05 observed RED; both YAML pins observed RED under transient mutation
</success_criteria>

<output>
Create `.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-03-SUMMARY.md` when done.
Include a "Falsifiability evidence" section with pasted outputs for SC-05 and both YAML-pin mutations, and the explicit reviewer-only note for D-02/R1's live half.
</output>
