---
phase: 142.1
plan: 05
type: execute
wave: 3
depends_on: [142.1-03]
files_modified:
  - supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql
  - analytics-service/tests/test_main_worker.py
  - analytics-service/services/job_worker.py
  - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
  - analytics-service/tests/test_sql_gate_scoped_updates.py
  - supabase/schema/functions/strategy_analytics_stamp_computing_started.sql
  - supabase/schema/functions/sync_strategy_analytics_status.sql
autonomous: true
requirements: [JOB-01, JOB-02]

must_haves:
  truths:
    - "The 'never advance, always clear on exit' invariant is enforced at the TABLE by a self-contained BEFORE UPDATE trigger — UPDATE ONLY, per CONTEXT D-18 Part 1: _mark_computing is an upsert whose CONFLICT path (the row already 'computing' from hop 1) is D-01's real bug and fires BEFORE UPDATE; the insert path is a genuine first transition with no prior stamp to advance, and a production INSERT of (computing, NULL) is exactly the D-11 population whose designated remedy is the companion cron arm — never duplicated inside the trigger"
    - "The trigger coerces silently (never RAISEs) — a raising trigger would kill the live _mark_computing call, burn 3 retries, and strand the strategy; the coercion is observable via SQL gate Part 6 (plan 142.1-08), not invisible"
    - "A (computing, NULL) row with no active job gets its clock STARTED (non-destructively) by a companion statement in the SAME $cron$ body, so the existing reaper threshold then applies — the D-11 blind spot is closed by a runtime mechanism, never by the static-scan category error"
    - "The SQL gate establishes state at INSERT, never by UPDATE-ing a computing row (D-18 Part 2): Part 4a seeds its sentinel in the INSERT (reachable — the trigger does not fire on INSERT), the :553-555 mid-test UPDATE is deleted, and 4b's assertion is byte-unchanged. ⚠️ 4b is DOUBLE-mutation defence-in-depth, NOT a single-mutation observable (trigger arm (a) and the bridge's own keep-arm each independently preserve the sentinel); SC-2b's single-mutation proof is Part 6/6a in plan 142.1-08, and 4a's stamp check is a seed-integrity control rather than a proof. Neither may be credited as the SC-2b observable in the D-16 evidence."
    - "The inverted arm D is deterministic by construction, not by neutralization: its (computing, NULL) seed at :334-336 is an INSERT and lands verbatim under the UPDATE-only trigger; determinism on the shared TEST project rests on the residual assumption stated in the gate header (plan 142.1-03), plus fewer than 25 foreign (computing, NULL, no-active-job) rows competing for the companion arm's budget at run time — stated beside the inverted arm"
    - "The drift gate reads the DEPLOYED body: _REAPER_MIGRATION_NAME AND the prose comments in test_main_worker.py:2163 AND job_worker.py:523 all point at the new migration in the same commit (C-10 + W-a — the drift gate's own failure message demands the constant and the comment move together), and the companion arm introduces no second interval literal"
    - "The SQL function snapshot is regenerated AFTER the trigger lands (D-12 run 2) — the snapshot gate is green with the trigger function tracked"
  artifacts:
    - path: "supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql"
      provides: "trigger fn + BEFORE UPDATE trigger + cron re-register (companion arm + byte-identical reap) + self-verify DO blocks"
      min_lines: 120
    - path: "supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql"
      provides: "inverted arm D + Part 4a/4b retrofit (sentinel seeded at INSERT, mid-test UPDATE deleted)"
      contains: "v_sentinel"
    - path: "supabase/schema/functions/strategy_analytics_stamp_computing_started.sql"
      provides: "tracked canonical trigger-function body (D-12 run 2)"
  key_links:
    - from: "analytics-service/tests/test_main_worker.py _REAPER_MIGRATION_NAME (+ :2163 comment) and analytics-service/services/job_worker.py:523 comment"
      to: "supabase/migrations/20260803120000_..."
      via: "re-pointed literal filename + comments (same commit as the migration — C-10, W-a)"
      pattern: "20260803120000"
    - from: "supabase/tests/... Part 2 EXECUTE v_command"
      to: "the new two-statement $cron$ body"
      via: "arm D INVERTED in the same commit (C-6): the NULL-stamp row must now be STAMPED, status still computing"
      pattern: "arm D"
    - from: "new trigger function"
      to: "PostgREST RPC surface"
      via: "REVOKE ALL FROM PUBLIC, anon, authenticated (never invocable as RPC)"
      pattern: "REVOKE ALL ON FUNCTION"
    - from: "Part 4a seed INSERT (computing, sentinel) + inverted arm D seed (computing, NULL)"
      to: "the BEFORE UPDATE-only trigger event"
      via: "gate-seeding discipline (D-18 Part 2) — state is established at INSERT, where the trigger by design does not fire"
      pattern: "BEFORE UPDATE"
---

<objective>
Land the D-17 trigger in its SETTLED D-18 shape (BEFORE UPDATE ONLY — see CONTEXT § "⭐ D-18";
⛔ RESEARCH § Architecture Patterns' `BEFORE INSERT OR UPDATE` sketch and its arm-(b)/(c)
INSERT behaviour are SUPERSEDED — do not restore them) and the D-11 companion clock-start arm
in ONE new forward-only migration, with every same-commit coupling the corrections mandate:
arm D inversion (C-6), the Part 4a/4b retrofit (D-18 Part 2), _REAPER_MIGRATION_NAME +
test_main_worker.py:2163 + job_worker.py:523 re-points (C-10 + W-a), no second interval
literal (C-11), and the grep-gate pin decrement for the deleted :553 site. Then the second
D-12 snapshot run.

Purpose: this closes D-01 (never advance) at the table for every writer including future
ones — the upsert's conflict path IS the bug and fires BEFORE UPDATE — and closes D-11's
blind spot with a runtime mechanism, the ONLY tier that can see rows (Architectural
Responsibility Map). SQL gate Part 6 (plan 142.1-08, depends on this plan) is what proves the
trigger predicate.
Output: migration 20260803120000, re-pointed drift gate + prose comments, inverted arm D,
retrofitted Part 4, regenerated snapshots.

CROSS-PLAN CONTRACT (fixed — do not rename): the migration filename
`20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql`, the trigger
function `strategy_analytics_stamp_computing_started`, and the trigger
`strategy_analytics_stamp_computing_started_trigger` are pinned contract values. Plan 142.1-04
(SAME wave, parallel worktree), plan 142.1-06 and plan 142.1-08 cite them in prose and
comments; a name drift here is a defect against that contract, not a local choice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-CONTEXT.md
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-PATTERNS.md
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 20260803120000 (BEFORE UPDATE trigger + companion cron arm) + drift-gate/comment re-points + arm D inversion + Part 4 retrofit — ONE commit</name>
  <files>supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql, analytics-service/tests/test_main_worker.py, analytics-service/services/job_worker.py, supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql, analytics-service/tests/test_sql_gate_scoped_updates.py</files>
  <read_first>
    - 142.1-CONTEXT.md § "⭐ D-18" IN FULL — the settled trigger shape and gate-seeding discipline this task implements; it OVERRIDES the RESEARCH sketch
    - 142.1-RESEARCH.md § "Architecture Patterns" Pattern 1 — for the predicate-arm VOCABULARY and the 10-writer compatibility table ONLY. ⚠️ Its `BEFORE INSERT OR UPDATE` event and arm-(b)/(c) INSERT behaviour are SUPERSEDED by D-18 (the trigger never fires on INSERT); do not "restore" them. § "D-11 — companion cron arm" (placement: SECOND statement in the SAME $cron$ body, clock-start BEFORE the reap; stamp value: now(); the CADENCE HONESTY addendum content) still stands
    - supabase/migrations/20260716131000_guard_strategies_publish_transition.sql (the SHAPE to copy VERBATIM: SET LOCAL lock_timeout '3s', SECURITY INVOKER, SET search_path = public, pg_catalog, COMMENT ON FUNCTION, REVOKE ALL ... FROM PUBLIC, anon, authenticated, DROP TRIGGER IF EXISTS + CREATE TRIGGER, COMMENT ON TRIGGER, self-verifying pg_trigger DO block at :86-98)
    - supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql:309-344 (the bridge's ON CONFLICT stamp CASE — the conflict path the trigger now guards), :495-535 (the cron.schedule registration incl. pg_cron gating — mirror it; the reap statement at :505-526 to copy BYTE-IDENTICALLY) and :573-631 (the pg_get_functiondef → v_fn → IF v_fn ~* ... RAISE idiom for the self-containment check; STEP 8 cron.job.command check idiom at :634+)
    - supabase/migrations/20260516170000_match_decisions_visibility_check_secdef_fix.sql:3-11 (the PRODUCTION-BREAKING anti-pattern the body must never contain) and :18-19 (the forward-only delta header idiom to copy)
    - analytics-service/tests/test_main_worker.py:2158-2201 (the JOB-03/SC-3b section: the prose comment at :2163 naming migration 20260802120000, _REAPER_MIGRATION_NAME at :2169-2171 + the $cron$ extraction regex), :2260-2266 (intervals == {expected} over EVERY interval literal in the body — C-11), :2299-2314 ("computed_at" not in body + the computing_started_at positive control)
    - analytics-service/services/job_worker.py:519-533 (the REAPER_STALENESS_THRESHOLD comment block — :522-523 names 20260802120000 as the migration embedding the interval literal; the drift gate's test_migration_file_exists failure message says the constant and THIS comment move together — W-a)
    - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:332-336 (arm D seed — an INSERT, reachable under the UPDATE-only trigger) and :380-390 (arm D assertions to INVERT + the RAISE NOTICE to update); :487-585 (Part 4 in full — the retrofit target: the 'pending' seed at :523-525, v_sentinel at :552, the mid-test UPDATE at :553-555 to DELETE, the 4b assertion at :559-566 to leave byte-unchanged)
    - analytics-service/tests/test_sql_gate_scoped_updates.py (plan 142.1-03's per-file anti-vacuity pin — this task's Part-4 retrofit deletes the reaper file's last UPDATE site, so the pin must move reaper: 1 → 0 in the SAME commit)
  </read_first>
  <action>
    Create `supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql`
    (timestamp strictly later than 20260802120000, which is applied to TEST as version
    20260802111053 and must NEVER be amended — forward-only delta, header idiom from
    20260516170000:18-19 naming 20260802120000 as the source migration). The filename, function
    name, and trigger name are the CROSS-PLAN CONTRACT values from the objective — plans
    142.1-04, 142.1-06 and 142.1-08 reference them verbatim; do not deviate.

    MIGRATION CONTENT (copy shapes from the read_first anchors; concrete names below):
    1. Header comments: forward-only-delta notice; the self-containment invariant (body reads
       only NEW/OLD/TG_OP and calls only now() — cite the 20260516170000 production-breaker as
       WHY); the D-18 EVENT-SCOPE notice — the trigger is **BEFORE UPDATE ONLY**, because
       (i) _mark_computing (analytics_runner.py:1227-1240) is an
       `.upsert(…, on_conflict="strategy_id")` whose CONFLICT path — the row already
       'computing' from hop 1 — is D-01's actual bug and fires BEFORE UPDATE, (ii) the insert
       path is a genuine first transition with no prior stamp to advance, and (iii) a
       production INSERT of (computing, NULL) IS the D-11 population, whose designated remedy
       is the companion arm below — duplicating it in the trigger would fight the SQL gate's
       seed INSERTs (D-18 Part 2: the gate establishes state at INSERT); UPDATE-only notice
       re DELETE (extending to DELETE would import the quantalyze.sanitize_in_progress
       exemption obligation, 20260710160000:67); a CADENCE HONESTY addendum — the trigger
       restores the main arm's ~16h15m worst case to TRUE (D-01 fixed at the writer boundary),
       and the companion arm's OWN worst case for a NULL-stamp row is stranding → detection
       ≤15 min → +16 h → ≤15 min ≈ up to ~32.5 h; state both bounds separately, never one
       number for two mechanisms.
    2. `SET LOCAL lock_timeout = '3s';` (fail loud on the ACCESS EXCLUSIVE CREATE TRIGGER lock,
       per 20260716131000:35).
    3. Function `public.strategy_analytics_stamp_computing_started()` RETURNS TRIGGER, LANGUAGE
       plpgsql, SECURITY INVOKER (default — do NOT write SECURITY DEFINER), SET search_path =
       public, pg_catalog. Body — UPDATE-path logic only (TG_OP is always 'UPDATE'; keep the
       arms self-documenting), exactly four arms:
       (a) NEW.computation_status = 'computing' AND OLD.computation_status = 'computing'
       AND OLD.computing_started_at IS NOT NULL → coerce NEW.computing_started_at :=
       OLD.computing_started_at (NEVER advance; silent — a RAISE would kill the live
       _mark_computing writer, burn 3 retries, strand the strategy). ⚠️ The
       `OLD.computing_started_at IS NOT NULL` conjunct is LOAD-BEARING: it is what lets the
       companion arm's clock-start UPDATE on a (computing, NULL) row pass through instead of
       being coerced back to NULL;
       (b) else if NEW.computation_status = 'computing' AND NEW.computing_started_at IS NULL →
       NEW.computing_started_at := now() (a computing row must never sit unstamped after an
       UPDATE — closes the NULL-stamp hole at the writer boundary for the UPDATE path; the
       INSERT-path population is the companion arm's job);
       (c) else if NEW.computation_status = 'computing' with an explicit non-NULL stamp on a
       genuine transition-in (OLD.computation_status IS DISTINCT FROM 'computing') → respect
       it (the STEP-2 backfill anchor and the bridge's transition-in now() both rely on this);
       (d) NEW.computation_status <> 'computing' → NEW.computing_started_at := NULL
       (every exit clears the reaper key, unconditionally). RETURN NEW. ZERO PERFORM, ZERO
       helper calls, ZERO side effects.
    4. COMMENT ON FUNCTION naming the coercion; REVOKE ALL ON FUNCTION ... FROM PUBLIC, anon,
       authenticated (S-5 — never invocable as a PostgREST RPC).
    5. DROP TRIGGER IF EXISTS strategy_analytics_stamp_computing_started_trigger ON
       public.strategy_analytics; CREATE TRIGGER ... **BEFORE UPDATE** ON
       public.strategy_analytics FOR EACH ROW EXECUTE FUNCTION ... (⛔ NOT `BEFORE INSERT OR
       UPDATE` — D-18 Part 1); COMMENT ON TRIGGER stating the coercion AND the
       UPDATE-only scope explicitly so `\d strategy_analytics` discloses both.
    6. Cron re-register, mirroring 20260802120000:495-535's pg_cron gating: PERFORM
       cron.schedule('reap_strategy_analytics_stuck_computing', '*/15 * * * *', $cron$ ... $cron$)
       — SAME jobname (pg_cron upserts by name), body now TWO statements:
       FIRST the companion clock-start — UPDATE public.strategy_analytics sa SET
       computing_started_at = now() WHERE sa.strategy_id IN (SELECT s.strategy_id FROM
       public.strategy_analytics s WHERE s.computation_status = 'computing' AND
       s.computing_started_at IS NULL AND NOT EXISTS (the BYTE-IDENTICAL
       ('pending','running','done_pending_children','failed_retry') compute_jobs subquery from
       the reap arm) LIMIT 25 FOR UPDATE SKIP LOCKED) AND sa.computation_status = 'computing'
       AND sa.computing_started_at IS NULL — with NO interval literal of its own (C-11: the
       drift gate asserts intervals == {'16 hours'} over the WHOLE body) and no mention of
       computed_at (the :2299 assertion);
       THEN the reap statement copied BYTE-IDENTICALLY from 20260802120000:505-526 (so the
       drift gate's threshold-literal, NOT-EXISTS-list, LIMIT and compare-and-set assertions
       keep holding). Clock-start-first ordering means a row stamped this tick is not reaped in
       the same tick — it gets a full threshold.
       GATE-DETERMINISM NOTE (state it in a comment beside the companion arm, replacing the
       withdrawn iteration-1 neutralization contract): plan 142.1-03 DELETED the SQL gate's
       cross-tenant neutralizers (D-18 Part 3) — isolation is by construction. The inverted
       arm D asserts the gate's OWN seeded row v_d; its determinism on the shared TEST project
       rests on the residual assumption stated in the gate's header plus fewer than 25 foreign
       (computing, NULL, no-active-job) rows competing for this arm's LIMIT-25 budget at run
       time. Do NOT change this arm's target predicate (computing, computing_started_at IS
       NULL, no active job) without re-deriving that determinism argument in the gate in the
       SAME commit, and vice versa.
    7. Self-verify DO blocks (S-4), each RAISE EXCEPTION on failure:
       (i) pg_trigger existence for the new tgname on 'public.strategy_analytics'::regclass
       with NOT tgisinternal (copy 20260716131000:86-98), AND event scope: tgtype must encode
       UPDATE-only — assert the trigger does NOT fire on INSERT (check pg_trigger.tgtype's
       INSERT bit is unset, or equivalently that
       `pg_get_triggerdef` contains 'BEFORE UPDATE' and not 'INSERT');
       (ii) self-containment: v_fn := pg_get_functiondef of the trigger function; RAISE if
       v_fn ~* '\mPERFORM\M' or v_fn ~* 'SECURITY DEFINER'; positive anchor: v_fn must contain
       'RETURN NEW' (⚠️ do NOT grep for 'public\.' — pg_get_functiondef's SET search_path
       clause contains it legitimately);
       (iii) cron.job.command check (STEP-8 idiom): the command must contain BOTH
       'computing_started_at IS NULL' (companion) and "interval '16 hours'" (reap), and must
       NOT contain 'computed_at'.
    SAME COMMIT — the coupled test and comment edits:
    8. Re-point `_REAPER_MIGRATION_NAME` in analytics-service/tests/test_main_worker.py:2169-2171
       to "20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql" (C-10 —
       otherwise the drift gate silently guards the SUPERSEDED body; this is the third instance
       of the D-04/D-10 silent-narrowing class). ALSO update the prose comment at :2163 ("the
       interval literal baked into the pg_cron body of migration 20260802120000") to name the
       NEW migration, AND (W-a) the comment block at
       analytics-service/services/job_worker.py:522-523 ("Migration
       20260802120000_strategy_analytics_stuck_computing_reaper.sql embeds this literal in its
       pg_cron body") to name the NEW migration — the drift gate's own failure message
       (test_migration_file_exists) instructs that `_REAPER_MIGRATION_NAME` and that comment
       move TOGETHER. The acceptance greps below demand ZERO occurrences of the old literal in
       BOTH files, comments included. The `\$cron\$(.*?)\$cron\$` extraction now reads the new
       file's single $cron$ pair.
    9. INVERT SQL-gate arm D (C-6): at supabase/tests/...reaper.sql:380-390-area, the assertion
       "IF v_stamp IS NOT NULL THEN RAISE ... the NULL-stamp row was mutated" inverts to
       "IF v_stamp IS NULL THEN RAISE ... the companion arm did not start the clock on a
       (computing, NULL, no-active-job) row"; KEEP the status-still-computing assertion
       (:383-385) unchanged — the companion arm is non-destructive; update arm D's seed comment
       (:332-336) and the Part 2 RAISE NOTICE (:390) to name the new behavior. The seed itself
       is an INSERT and lands verbatim under the UPDATE-only trigger (D-18 resolved the prior
       blocker — the seed is reachable). This inverted arm IS SC-11's gate: deleting the
       companion arm from the migration ⇒ the stamp stays NULL ⇒ RED. State its determinism
       argument beside it (the step-6 GATE-DETERMINISM NOTE, gate side).
    10. RETROFIT Part 4a/4b (D-18 Part 2 — same commit as the trigger; ⚠️ prior iterations had
       NO plan owning Part 4, which was blocker 2):
       - Move the `v_sentinel := now() - interval '3 hours';` assignment (currently :552)
         ABOVE the Part-4 seed INSERT.
       - Change the seed INSERT at :523-525 from ('pending', FALSE, NULL) to
         ('computing', FALSE, v_sentinel) — state is established at INSERT, where the
         UPDATE-only trigger by design does not fire, so the sentinel lands verbatim.
       - DELETE the mid-test `UPDATE public.strategy_analytics SET computing_started_at =
         v_sentinel WHERE strategy_id = v_strat;` at :553-555 — under the trigger it would be
         coerced (arm (a)) and the sentinel would never take; the gate must never establish
         state by UPDATE-ing a computing row.
       - LEAVE the 4b assertion at :559-566 BYTE-UNCHANGED, but ⚠️ **DO NOT record 4b as a
         single-mutation observable.** After the retrofit 4b is a DOUBLE-MUTATION
         defence-in-depth assertion: a bridge that stamps unconditionally is coerced back to
         the sentinel by trigger arm (a) (green), and deleting trigger arm (a) alone leaves
         the bridge's OWN keep-arm (Arm 3) preserving the sentinel (also green). Only breaking
         BOTH reddens it. State this in the Part 4 header. **SC-2b's single-mutation observable
         is Part 6/6a (plan 142.1-08), and the bridge-unconditional mutation is caught by the
         gate's Part 1 negative anchor at :163 and the migration's STEP-7 anchor** — 4b adds
         depth, not coverage. ⛔ The D-16 evidence in plan 142.1-07 must NOT credit 4b as the
         SC-2b proof; over-claiming an assertion's strength is the exact failure class this
         phase exists to close.
       - Update 4a's comments and assertion messages, and ⚠️ **label 4a's
         `IF v_stamp IS NULL THEN RAISE` explicitly as an S-2 SEED-INTEGRITY CONTROL, not a
         proof** — after the retrofit it is satisfied by the seed itself and can no longer
         fail. The row is now SEEDED at computing with the sentinel, so 4a demonstrates "a
         mid-chain bridge hop keeps the row computing and PRESERVES the pre-existing stamp".
         The behavioral proof that a transition INTO computing stamps has LEFT Part 4 — it now
         lives in Part 1's structural anchors (:152 `= CASE`, :155 `IS DISTINCT FROM
         'computing'`), the migration's own STEP-7 anchors, and behaviorally in trigger arm (b)
         at Part 6/6b. **Say exactly that in the Part 4 header**, so nobody re-adds a 'pending'
         seed to "restore" the transition-in proof and re-breaks the sentinel. Update the Part 4
         header comment (4a's description and the "Pre-transition state" comment). 4c is
         unchanged.
    11. Grep-gate pin decrement (same commit): step 10 deleted the reaper file's last UPDATE
       site (:553), so update plan 142.1-03's per-file pin in
       analytics-service/tests/test_sql_gate_scoped_updates.py —
       "test_strategy_analytics_stuck_computing_reaper.sql": 1 → 0 (total 7 → 6) — with a
       comment naming this migration. The pin firing on this exact edit is the anti-vacuity
       design working; updating it here is the contract, not churn.
    Verify: `cd analytics-service && pytest tests/test_main_worker.py tests/test_sql_gate_scoped_updates.py -q`
    — the drift-gate tests must pass reading the NEW file (intervals set == {'16 hours'},
    computed_at absent, computing_started_at positive control present) and the scoped-updates
    pin must pass with reaper: 0.
    Commit all of the above as ONE commit:
    `feat(142.1): BEFORE UPDATE stamp trigger + NULL-stamp clock-start companion arm (D-17/D-01/D-11, D-18)`.
  </action>
  <verify>
    <automated>bash -c "cd analytics-service && pytest tests/test_main_worker.py tests/test_sql_gate_scoped_updates.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists with prefix 20260803120000 (strictly > 20260802120000); `git log` shows it in the SAME commit as the test_main_worker.py/job_worker.py re-points, the arm D inversion, the Part 4 retrofit, and the pin decrement
    - `grep -v '^[[:space:]]*--' supabase/migrations/20260803120000_*.sql | grep -c 'BEFORE UPDATE ON'` returns 1 and `grep -v '^[[:space:]]*--' supabase/migrations/20260803120000_*.sql | grep -c 'BEFORE INSERT'` returns 0 (D-18 Part 1 — UPDATE-only; never `\s` in BRE, BSD grep on macOS does not support it)
    - `grep -c 'SECURITY DEFINER' supabase/migrations/20260803120000_*.sql` returns 0
    - Every PERFORM in the migration is `PERFORM cron.schedule`: `grep -v '^[[:space:]]*--' supabase/migrations/20260803120000_*.sql | grep -c 'PERFORM'` equals `grep -v '^[[:space:]]*--' supabase/migrations/20260803120000_*.sql | grep -c 'PERFORM cron.schedule'`
    - `grep -c 'REVOKE ALL ON FUNCTION' supabase/migrations/20260803120000_*.sql` >= 1; the REVOKE names PUBLIC, anon, authenticated
    - The $cron$ body contains exactly one interval literal: `awk '/\$cron\$/,/\$cron\$/' supabase/migrations/20260803120000_*.sql | grep -o "interval '[^']*'" | sort -u` prints only "interval '16 hours'"
    - `grep -c '20260802120000' analytics-service/tests/test_main_worker.py` returns 0 (re-pointed, INCLUDING the :2163 prose comment) and `grep -c '20260803120000' analytics-service/tests/test_main_worker.py` >= 1
    - `grep -c '20260802120000' analytics-service/services/job_worker.py` returns 0 and `grep -c '20260803120000' analytics-service/services/job_worker.py` >= 1 (W-a — comment-only change; `git diff` on job_worker.py shows no non-comment lines touched)
    - `git diff HEAD~1 -- supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql` is empty (the applied migration was NOT amended)
    - Arm D in the SQL gate now RAISEs on `v_stamp IS NULL` and still asserts status = 'computing'
    - Part 4: `grep -c 'SET computing_started_at = v_sentinel' supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql` returns 0 (mid-test UPDATE deleted); the Part-4 seed INSERT contains `v_sentinel`; the 4b assertion block (`IS DISTINCT FROM v_sentinel` + its RAISE) is byte-unchanged vs HEAD~1
    - test_sql_gate_scoped_updates.py's per-file pin reads reaper: 0 and the gate is green
    - `cd analytics-service && pytest tests/test_main_worker.py -q` green
  </acceptance_criteria>
  <done>One commit carries the migration + every coupled edit (drift-gate constant, both prose comments, arm D inversion, Part 4 retrofit, pin decrement); the trigger is UPDATE-only per D-18; the gate seeds state at INSERT everywhere; the 142 migration untouched.</done>
</task>

<task type="auto">
  <name>Task 2: D-12 run 2 — regenerate the snapshot for the new trigger function (own commit)</name>
  <files>supabase/schema/functions/strategy_analytics_stamp_computing_started.sql, supabase/schema/functions/sync_strategy_analytics_status.sql</files>
  <read_first>
    - scripts/dump-sql-functions.ts:15-20 (trigger functions ARE tracked — guard_strategies_publish_transition.sql is among the 104 snapshots; adding a trigger function re-arms the drift gate)
  </read_first>
  <action>
    Per the LOCKED sequencing constraint 2 (D-12 runs TWICE): run `npm run schema:functions`
    again now that migration 20260803120000 adds a tracked function. Confirm
    `supabase/schema/functions/strategy_analytics_stamp_computing_started.sql` is created and
    `npx tsx scripts/dump-sql-functions.ts --check` exits 0. Commit ONLY the snapshot changes,
    its own commit (S-9): `fix(142.1): regenerate SQL function snapshot after trigger migration (D-12 run 2)`.
  </action>
  <verify>
    <automated>npx tsx scripts/dump-sql-functions.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `--check` exits 0
    - supabase/schema/functions/strategy_analytics_stamp_computing_started.sql exists and contains `RETURN NEW` and no `PERFORM`
    - The commit touches only supabase/schema/functions/ files
  </acceptance_criteria>
  <done>Snapshot gate green with the trigger tracked; the D-12 trap is not re-armed at merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| every strategy_analytics writer (service_role PostgREST, SECDEF bridge, pg_cron) → the table | the trigger fires on all three privileged paths' UPDATEs; RLS already denies anon/authenticated writes (20260405061912:43-44) |
| PostgREST RPC surface → trigger function | a trigger function with default ACLs is callable as an RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.1-01 | Denial of Service | trigger body PERFORMing a REVOKEd SECDEF helper → 42501 on every service_role write (the 20260516170000 PRODUCTION-BREAKING class) | mitigate | body is 100% self-contained (NEW/OLD/TG_OP/now() only); self-verify DO block (ii) greps the deployed functiondef for PERFORM/SECURITY DEFINER at apply time |
| T-142.1-02 | Tampering | caller-suppliable parameter on a cross-tenant mechanism (the 20260516170100 incident class: "the parameter IS the attack surface", mig:114-118) | mitigate | trigger is parameterless (ASVS V5); the companion arm's bounds are literals in the cron body |
| T-142.1-03 | Elevation of Privilege | trigger function invocable via PostgREST RPC by anon/authenticated | mitigate | REVOKE ALL FROM PUBLIC, anon, authenticated (ASVS V4, 20260716131000:74 convention); acceptance grep pins it |
| T-142.1-04 | Denial of Service / compliance | a DELETE guard would abort the sanitize_user GDPR cascade | mitigate | trigger is UPDATE-only; header comment names the sanitize_in_progress exemption obligation so nobody widens it blind |
| T-142.1-07 | Tampering (integrity) | trigger function untracked in the canonical snapshot → next author re-bases from a stale body | mitigate | Task 2 (D-12 run 2) in its own commit; sql-function-snapshot.yml:84 stays blocking |
| T-142.1-SC | Tampering | package installs | accept (N/A) | no installs this phase |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_main_worker.py -q` green (drift gates on the new file)
- `cd analytics-service && pytest tests/test_sql_gate_scoped_updates.py -q` green (pin decremented to reaper: 0)
- `npx tsx scripts/dump-sql-functions.ts --check` exits 0 (after Task 2)
- `cd analytics-service && pytest -q` full suite green at plan end
</verification>

<success_criteria>
- ONE forward-only migration carries the BEFORE UPDATE trigger and the companion arm; 20260802120000 untouched
- All same-commit couplings honored: C-6 arm D inversion, C-10 re-point incl. the :2163 comment, W-a's job_worker.py:523 comment, C-11 single interval, D-18 Part 2's Part 4a/4b retrofit, and the grep-gate pin decrement
- Part 4 seeds its sentinel at INSERT; the mid-test UPDATE is gone; 4b's assertion byte-unchanged, and its Part-4 header states plainly that 4b is double-mutation depth (not the SC-2b observable) and that 4a's stamp check is a seed-integrity control
- The migration's self-verify block (i) proves the deployed trigger is UPDATE-only
- Snapshot regenerated a second time in its own commit
- NOTE for the record: the trigger closes D-11 only AT THE SOURCE for UPDATE-path writes; the companion arm is what rescues rows that ALREADY exist or arrive via INSERT — never claim the trigger subsumes D-11 (C-1, D-18 Part 1)
</success_criteria>

<output>
Create `.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-05-SUMMARY.md` when done.
Include a "Falsifiability evidence" section noting SC-11 and SC-16 are DEFERRED to plan 142.1-07's orchestrator run and SC-17/01 to plan 142.1-08's Part 6 + that same run (state this explicitly — do NOT mark them observed), plus the drift-gate green on the new file.
</output>
