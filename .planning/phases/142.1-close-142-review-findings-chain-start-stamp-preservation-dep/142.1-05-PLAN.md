---
phase: 142.1
plan: 05
type: execute
wave: 3
depends_on: [142.1-03]
files_modified:
  - supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql
  - analytics-service/tests/test_main_worker.py
  - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
  - supabase/schema/functions/strategy_analytics_stamp_computing_started.sql
  - supabase/schema/functions/sync_strategy_analytics_status.sql
autonomous: true
requirements: [JOB-01, JOB-02]

must_haves:
  truths:
    - "The 'stamp only on transition into computing, never advance, always clear on exit' invariant is enforced at the TABLE by a self-contained BEFORE INSERT OR UPDATE trigger — payload shape and call-site language are irrelevant, closing D-01 for every current and future writer"
    - "The trigger coerces silently (never RAISEs) — a raising trigger would kill the live _mark_computing call, burn 3 retries, and strand the strategy; the coercion is observable via SQL gate Part 6, not invisible"
    - "A (computing, NULL) row with no active job gets its clock STARTED (non-destructively) by a companion statement in the SAME $cron$ body, so the existing reaper threshold then applies — the D-11 blind spot is closed by a runtime mechanism, never by the static-scan category error"
    - "The neutralization contract with plan 142.1-03 holds: during the SQL gate's transaction, foreign computing rows carry fresh non-NULL stamps and are invisible to BOTH cron arms — the companion arm's LIMIT-25 budget can only reach the seeded arm-D row, so the inverted arm D is deterministic, not lucky"
    - "The drift gate reads the DEPLOYED body: _REAPER_MIGRATION_NAME points at the new migration in the same commit, and the companion arm introduces no second interval literal"
    - "The SQL function snapshot is regenerated AFTER the trigger lands (D-12 run 2) — the snapshot gate is green with the trigger function tracked"
  artifacts:
    - path: "supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql"
      provides: "trigger fn + trigger + cron re-register (companion arm + byte-identical reap) + self-verify DO blocks"
      min_lines: 120
    - path: "supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql"
      provides: "Part 6 (G1 trigger sentinel, ungated) + inverted arm D"
      contains: "Part 6"
    - path: "supabase/schema/functions/strategy_analytics_stamp_computing_started.sql"
      provides: "tracked canonical trigger-function body (D-12 run 2)"
  key_links:
    - from: "analytics-service/tests/test_main_worker.py _REAPER_MIGRATION_NAME"
      to: "supabase/migrations/20260803120000_..."
      via: "re-pointed literal filename (same commit as the migration — C-10)"
      pattern: "20260803120000"
    - from: "supabase/tests/... Part 2 EXECUTE v_command"
      to: "the new two-statement $cron$ body"
      via: "arm D INVERTED in the same commit (C-6): the NULL-stamp row must now be STAMPED, status still computing"
      pattern: "arm D"
    - from: "new trigger function"
      to: "PostgREST RPC surface"
      via: "REVOKE ALL FROM PUBLIC, anon, authenticated (never invocable as RPC)"
      pattern: "REVOKE ALL ON FUNCTION"
    - from: "companion arm target predicate (computing, NULL, no active job)"
      to: "plan 142.1-03's fresh-stamp neutralization form"
      via: "bi-arm invisibility contract — changing either side alone re-opens the D-16 flake"
      pattern: "computing_started_at IS NULL"
---

<objective>
Land the D-17 Option A trigger (the settled architecture decision) and the D-11 companion
clock-start arm in ONE new forward-only migration, with the three same-commit couplings the
corrections mandate (arm D inversion — C-6; _REAPER_MIGRATION_NAME re-point — C-10; no second
interval literal — C-11), the new SQL gate Part 6 that makes the coercion falsifiable, and the
second D-12 snapshot run.

Purpose: this closes D-01 (never advance) at the table for every writer including future ones,
and closes D-11's blind spot with a runtime mechanism — the ONLY tier that can see rows
(Architectural Responsibility Map). The trigger predicate sketched in RESEARCH A4 is a SKETCH;
Part 6 is what proves it.
Output: migration 20260803120000, re-pointed drift gate, Part 6 + inverted arm D, regenerated
snapshots.

CROSS-PLAN CONTRACT (fixed — do not rename): the migration filename
`20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql`, the trigger
function `strategy_analytics_stamp_computing_started`, and the trigger
`strategy_analytics_stamp_computing_started_trigger` are pinned contract values. Plan 142.1-04
(SAME wave, parallel worktree) and plan 142.1-06 cite them in prose and comments; a name drift
here is a defect against that contract, not a local choice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-CONTEXT.md
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-PATTERNS.md
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 20260803120000 (trigger + companion cron arm) + re-point the drift gate + invert arm D — ONE commit</name>
  <files>supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql, analytics-service/tests/test_main_worker.py, supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql</files>
  <read_first>
    - 142.1-RESEARCH.md § "Architecture Patterns" Pattern 1 (the FULL trigger body sketch — predicate arms, compatibility table for all 10 existing writers; ⚠️ it is a SKETCH, proven only by Part 6) and § "D-11 — companion cron arm" (placement decision: SECOND statement in the SAME $cron$ body, clock-start BEFORE the reap; stamp value decision: now(); the CADENCE HONESTY addendum content)
    - supabase/migrations/20260716131000_guard_strategies_publish_transition.sql (the SHAPE to copy VERBATIM: SET LOCAL lock_timeout '3s', SECURITY INVOKER, SET search_path = public, pg_catalog, COMMENT ON FUNCTION, REVOKE ALL ... FROM PUBLIC, anon, authenticated, DROP TRIGGER IF EXISTS + CREATE TRIGGER, COMMENT ON TRIGGER, self-verifying pg_trigger DO block at :86-98)
    - supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql:495-535 (the cron.schedule registration incl. pg_cron gating — mirror it; the reap statement at :505-526 to copy BYTE-IDENTICALLY) and :573-631 (the pg_get_functiondef → v_fn → IF v_fn ~* ... RAISE idiom for the self-containment check; STEP 8 cron.job.command check idiom at :634+)
    - supabase/migrations/20260516170000_match_decisions_visibility_check_secdef_fix.sql:3-11 (the PRODUCTION-BREAKING anti-pattern the body must never contain) and :18-19 (the forward-only delta header idiom to copy)
    - analytics-service/tests/test_main_worker.py:2158-2201 (the JOB-03/SC-3b section: the prose comment at :2163 naming migration 20260802120000, _REAPER_MIGRATION_NAME at :2169-2171 + the $cron$ extraction regex), :2260-2266 (intervals == {expected} over EVERY interval literal in the body — C-11), :2299-2314 ("computed_at" not in body + the computing_started_at positive control)
    - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:332-336 (arm D seed) and :380-390 (arm D assertions to INVERT + the RAISE NOTICE to update); :338-343 (the plan-142.1-03 re-targeted neutralization + its contract comment — the companion arm's determinism inside the gate depends on it)
  </read_first>
  <action>
    Create `supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql`
    (timestamp strictly later than 20260802120000, which is applied to TEST as version
    20260802111053 and must NEVER be amended — forward-only delta, header idiom from
    20260516170000:18-19 naming 20260802120000 as the source migration). The filename, function
    name, and trigger name are the CROSS-PLAN CONTRACT values from the objective — plans
    142.1-04 and 142.1-06 reference them verbatim; do not deviate.

    MIGRATION CONTENT (copy shapes from the read_first anchors; concrete names below):
    1. Header comments: forward-only-delta notice; the self-containment invariant (body reads
       only NEW/OLD/TG_OP and calls only now() — cite the 20260516170000 production-breaker as
       WHY, and P3: ON CONFLICT DO UPDATE fires BOTH BEFORE INSERT and BEFORE UPDATE so the body
       must be side-effect-free); INSERT/UPDATE-only notice (extending to DELETE would import
       the quantalyze.sanitize_in_progress exemption obligation, 20260710160000:67); a CADENCE
       HONESTY addendum — the trigger restores the main arm's ~16h15m worst case to TRUE (D-01
       fixed at the writer boundary), and the companion arm's OWN worst case for a NULL-stamp
       row is stranding → detection ≤15 min → +16 h → ≤15 min ≈ up to ~32.5 h; state both bounds
       separately, never one number for two mechanisms.
    2. `SET LOCAL lock_timeout = '3s';` (fail loud on the ACCESS EXCLUSIVE CREATE TRIGGER lock,
       per 20260716131000:35).
    3. Function `public.strategy_analytics_stamp_computing_started()` RETURNS TRIGGER, LANGUAGE
       plpgsql, SECURITY INVOKER (default — do NOT write SECURITY DEFINER), SET search_path =
       public, pg_catalog. Body per the RESEARCH Pattern-1 sketch, exactly four arms:
       (a) NEW.computation_status = 'computing' AND TG_OP='UPDATE' AND OLD.computation_status =
       'computing' AND OLD.computing_started_at IS NOT NULL → coerce NEW.computing_started_at :=
       OLD.computing_started_at (NEVER advance; silent — a RAISE would kill the live
       _mark_computing writer, burn 3 retries, strand the strategy);
       (b) else if entering/staying 'computing' with NEW.computing_started_at IS NULL →
       NEW.computing_started_at := now() (closes the NULL-stamp hole at the source);
       (c) else if 'computing' with an explicit non-NULL stamp on a genuine transition-in →
       respect it (the STEP-2 backfill anchor and the companion arm both rely on this);
       (d) NEW.computation_status <> 'computing' → NEW.computing_started_at := NULL
       (every exit clears the reaper key, unconditionally). RETURN NEW. ZERO PERFORM, ZERO
       helper calls, ZERO side effects.
    4. COMMENT ON FUNCTION naming the coercion; REVOKE ALL ON FUNCTION ... FROM PUBLIC, anon,
       authenticated (S-5 — never invocable as a PostgREST RPC).
    5. DROP TRIGGER IF EXISTS strategy_analytics_stamp_computing_started_trigger ON
       public.strategy_analytics; CREATE TRIGGER ... BEFORE INSERT OR UPDATE ON
       public.strategy_analytics FOR EACH ROW EXECUTE FUNCTION ...; COMMENT ON TRIGGER stating
       the coercion explicitly so `\d strategy_analytics` discloses it.
    6. Cron re-register, mirroring 20260802120000:495-535's pg_cron gating: PERFORM
       cron.schedule('reap_strategy_analytics_stuck_computing', '*/15 * * * *', $cron$ ... $cron$)
       — SAME jobname (pg_cron upserts by name), body now TWO statements:
       FIRST the companion clock-start — UPDATE public.strategy_analytics sa SET
       computing_started_at = now() WHERE sa.strategy_id IN (SELECT s.strategy_id FROM
       public.strategy_analytics s WHERE s.computation_status = 'computing' AND
       s.computing_started_at IS NULL AND NOT EXISTS (the BYTE-IDENTICAL
       ('pending','running','done_pending_children','failed_retry') compute_jobs subquery from
       the reap arm) LIMIT 25 FOR UPDATE SKIP LOCKED) AND sa.computation_status = 'computing'
       AND sa.computing_started_at IS NULL — with NO interval literal of its own (C-11: the
       drift gate asserts intervals == {'16 hours'} over the WHOLE body) and no mention of
       computed_at (the :2299 assertion);
       THEN the reap statement copied BYTE-IDENTICALLY from 20260802120000:505-526 (so the
       drift gate's threshold-literal, NOT-EXISTS-list, LIMIT and compare-and-set assertions
       keep holding). Clock-start-first ordering means a row stamped this tick is not reaped in
       the same tick — it gets a full threshold.
       D-05 CONTRACT (plan 142.1-03, this plan's depends_on — state it in a comment beside the
       companion arm): the SQL gate's three cross-tenant neutralizing UPDATEs were re-targeted
       in plan 142.1-03 to SET a FRESH now() stamp on foreign NULL-or-stale computing rows
       precisely so foreign rows are invisible to BOTH this companion arm (stamp not NULL) and
       the reap arm (stamp not stale) during the gate's transaction — the companion arm's
       LIMIT-25 budget inside the gate can therefore only reach the seeded arm-D row v_d, which
       is what makes the inverted arm D deterministic on the D-16 run instead of a shared-DB
       flake. Do NOT change this arm's target predicate (computing, computing_started_at IS
       NULL, no active job) without updating the gate's neutralization form in the SAME commit,
       and vice versa.
    7. Self-verify DO blocks (S-4), each RAISE EXCEPTION on failure:
       (i) pg_trigger existence for the new tgname on 'public.strategy_analytics'::regclass
       with NOT tgisinternal (copy 20260716131000:86-98);
       (ii) self-containment: v_fn := pg_get_functiondef of the trigger function; RAISE if
       v_fn ~* '\mPERFORM\M' or v_fn ~* 'SECURITY DEFINER'; positive anchor: v_fn must contain
       'RETURN NEW' (⚠️ do NOT grep for 'public\.' — pg_get_functiondef's SET search_path
       clause contains it legitimately);
       (iii) cron.job.command check (STEP-8 idiom): the command must contain BOTH
       'computing_started_at IS NULL' (companion) and "interval '16 hours'" (reap), and must
       NOT contain 'computed_at'.
    SAME COMMIT — two coupled test edits:
    8. Re-point `_REAPER_MIGRATION_NAME` in analytics-service/tests/test_main_worker.py:2169-2171
       to "20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql" (C-10 —
       otherwise the drift gate silently guards the SUPERSEDED body; this is the third instance
       of the D-04/D-10 silent-narrowing class). ALSO update the prose comment at :2163 ("the
       interval literal baked into the pg_cron body of migration 20260802120000") to name the
       NEW migration — the deployed body is now 20260803120000's, and the acceptance grep below
       demands ZERO occurrences of the old literal anywhere in the file, comments included.
       The `\$cron\$(.*?)\$cron\$` extraction now reads the new file's single $cron$ pair.
    9. INVERT SQL-gate arm D (C-6): at supabase/tests/...reaper.sql:380-390-area, the assertion
       "IF v_stamp IS NOT NULL THEN RAISE ... the NULL-stamp row was mutated" inverts to
       "IF v_stamp IS NULL THEN RAISE ... the companion arm did not start the clock on a
       (computing, NULL, no-active-job) row"; KEEP the status-still-computing assertion
       (:383-385) unchanged — the companion arm is non-destructive; update arm D's seed comment
       (:332-336) and the Part 2 RAISE NOTICE (:390) to name the new behavior. This inverted
       arm IS SC-11's gate: deleting the companion arm from the migration ⇒ the stamp stays
       NULL ⇒ RED. Its determinism on the shared TEST project rests on the D-05 contract above
       — foreign rows cannot compete for the budget because the plan-142.1-03 neutralization
       leaves them fresh-stamped, and pre-existing foreign (computing, NULL) rows are swept
       into the fresh-stamp state by the same neutralizer.
    Verify: `cd analytics-service && pytest tests/test_main_worker.py -q` — the drift-gate tests
    must pass reading the NEW file (intervals set == {'16 hours'}, computed_at absent,
    computing_started_at positive control present).
    Commit all of the above as ONE commit:
    `feat(142.1): stamp trigger + NULL-stamp clock-start companion arm (D-17/D-01/D-11)`.
  </action>
  <verify>
    <automated>bash -c "cd analytics-service && pytest tests/test_main_worker.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists with prefix 20260803120000 (strictly > 20260802120000); `git log` shows it in the SAME commit as the test_main_worker.py re-point and the arm D inversion
    - `grep -c 'SECURITY DEFINER' supabase/migrations/20260803120000_*.sql` returns 0
    - Every PERFORM in the migration is `PERFORM cron.schedule`: `grep -v '^[[:space:]]*--' supabase/migrations/20260803120000_*.sql | grep -c 'PERFORM'` equals `grep -v '^[[:space:]]*--' supabase/migrations/20260803120000_*.sql | grep -c 'PERFORM cron.schedule'` (never `\s` in BRE — BSD grep on macOS does not support it and the comment strip silently fails)
    - `grep -c 'REVOKE ALL ON FUNCTION' supabase/migrations/20260803120000_*.sql` >= 1; the REVOKE names PUBLIC, anon, authenticated
    - The $cron$ body contains exactly one interval literal: `awk '/\$cron\$/,/\$cron\$/' supabase/migrations/20260803120000_*.sql | grep -o "interval '[^']*'" | sort -u` prints only "interval '16 hours'"
    - `grep -c '20260802120000' analytics-service/tests/test_main_worker.py` returns 0 (re-pointed, INCLUDING the :2163 prose comment) and `grep -c '20260803120000' analytics-service/tests/test_main_worker.py` >= 1
    - `git diff HEAD~1 -- supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql` is empty (the applied migration was NOT amended)
    - Arm D in the SQL gate now RAISEs on `v_stamp IS NULL` and still asserts status = 'computing'
    - A comment beside the companion arm names the D-05 neutralization contract with plan 142.1-03
    - `cd analytics-service && pytest tests/test_main_worker.py -q` green
  </acceptance_criteria>
  <done>One commit carries the migration + both coupled test edits; drift gate guards the deployed body (code and comments); no interval collision; the 142 migration untouched; the bi-arm neutralization contract stated on this side too.</done>
</task>

<task type="auto">
  <name>Task 2: SQL gate Part 6 — the G1 trigger sentinel (ungated, must redden when the trigger is absent)</name>
  <files>supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql</files>
  <read_first>
    - 142.1-PATTERNS.md § "4. NEW SQL gate Part 6" (the COMPLETE spec: Part-4 framing to copy — BEGIN; SET LOCAL lock_timeout='5s'; DO $$...$$; ROLLBACK; the FK seed chain verbatim; the ⭐ sentinel technique at :546-566 verbatim; Part 6's one difference — the driver is a DIRECT UPDATE, not the bridge RPC; the no-skip-arm rule from Part 4's header :496-497)
    - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:487-585 (Part 4 in full — the donor) and :52-66 (the per-part transaction rule S-3: psql nested BEGIN creates no savepoint; NEVER an outer transaction)
    - 142.1-RESEARCH.md § "4. A gate that CAN fail on a Python re-advance" (why G1 beat G2/G3: the frozen-clock hazard, the self-referential-oracle rejection)
  </read_first>
  <action>
    Append Part 6 to the reaper SQL gate, copying Part 4's framing exactly (own
    BEGIN; SET LOCAL lock_timeout = '5s'; single DO block; ROLLBACK; FK seed chain
    auth.users → profiles → strategies → strategy_analytics with a fresh uuid; DELETE FROM
    auth.users at the end of the block). NO pg_cron gate and NO skip arm — Part 6 needs only the
    trigger, so it must REDDEN (not skip) when migration 20260803120000 is unapplied (Part 4's
    header rule; Parts 2/3 are the only legitimate skips in the file). Scope every read/write to
    the block's own seeded ids (S-7) — every driver UPDATE is `WHERE strategy_id = v_strat`,
    which the D-05 grep gate (plan 142.1-03) accepts as its block-local-seeded form (a).
    Three assertion arms, each with a distinct RAISE EXCEPTION message naming Part 6 / G1 /
    the D-number:
    (6a) NEVER-ADVANCE (the D-01 proof, sentinel technique verbatim from :546-566): seed the row
    at computation_status='computing', then UPDATE computing_started_at to a sentinel
    (now() - interval '3 hours'); issue the Python-writer-shaped driver — a DIRECT
    `UPDATE public.strategy_analytics SET computation_status='computing',
    computing_started_at = now() WHERE strategy_id = v_strat;` — and assert the stamp IS NOT
    DISTINCT FROM the sentinel. now() is transaction-constant, so without the sentinel this
    assertion could NEVER fail — the sentinel is what makes deleting the trigger's coercion arm
    turn this RED (ledger row SC-17/01).
    (6b) NULL-STAMP CLOSURE (the at-the-source half of D-11): after 6a the seeded row sits at
    'computing' WITH the preserved sentinel stamp, so a direct transition-to-computing driver
    would hit trigger arm (a) again and silently re-test 6a instead of arm (b). 6b therefore
    needs an EXPLICIT INTERMEDIATE STEP: first UPDATE the row to computation_status='failed'
    (no stamp key supplied — trigger arm (d) NULLs the stamp on exit; optionally assert the
    NULL as a seed-integrity check, though the dedicated exit-clear proof is 6c), THEN UPDATE
    back to computation_status='computing' WITHOUT supplying computing_started_at; assert the
    stamp comes back NON-NULL (arm (b) stamped now()).
    (6c) EXIT-CLEAR: UPDATE the computing row to computation_status='failed' while deliberately
    supplying a non-NULL computing_started_at in the same statement; assert the stamp comes back
    NULL (arm (d) coerced it — no writer can leave a stale stamp on a terminal row).
    Add the S-2 anti-vacuity control: before 6a's driver, assert the seeded row IS at
    'computing' with the sentinel value, so a broken seed cannot green the part vacuously.
    Local verification is structural only (no DB in this runtime — the behavioral run is plan
    142.1-07's D-16): assert the file's shape with the acceptance greps below, and confirm the
    Python suites still pass. The Part-6 assertions themselves are exercised end-to-end by the
    orchestrator in plan 142.1-07.
    Commit: `test(142.1): SQL gate Part 6 — trigger sentinel G1 (never-advance, NULL-closure, exit-clear)`.
  </action>
  <verify>
    <automated>bash -c "grep -q 'Part 6' supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql && cd analytics-service && pytest tests/test_main_worker.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - Part 6 exists with its own BEGIN/ROLLBACK framing; `awk '/Part 6/,0' supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql | grep -c 'RETURN;'` returns 0 (no skip arm)
    - Part 6 contains `interval '3 hours'` (the sentinel) and a direct `UPDATE public.strategy_analytics SET computation_status` driver scoped `WHERE strategy_id = v_strat`
    - Part 6's 6b arm contains the intermediate transition-OUT (to 'failed', no stamp supplied) BEFORE the transition-in driver — it tests trigger arm (b), not a second pass over arm (a)
    - Part 6 contains three distinct RAISE EXCEPTION arms (6a/6b/6c) plus the seed-integrity control
    - No unqualified UPDATE was introduced: `cd analytics-service && pytest tests/test_sql_gate_scoped_updates.py -q` green (the D-05 grep gate from plan 142.1-03 accepts Part 6's `= v_strat` sites as form (a))
  </acceptance_criteria>
  <done>Part 6 committed; the trigger predicate has a gate that CAN fail (proven live in plan 142.1-07); ledger rows SC-17/01 and SC-11 are now achievable; 6b provably exercises arm (b).</done>
</task>

<task type="auto">
  <name>Task 3: D-12 run 2 — regenerate the snapshot for the new trigger function (own commit)</name>
  <files>supabase/schema/functions/strategy_analytics_stamp_computing_started.sql, supabase/schema/functions/sync_strategy_analytics_status.sql</files>
  <read_first>
    - scripts/dump-sql-functions.ts:15-20 (trigger functions ARE tracked — guard_strategies_publish_transition.sql is among the 104 snapshots; adding a trigger function re-arms the drift gate)
  </read_first>
  <action>
    Per the LOCKED sequencing constraint 2 (D-12 runs TWICE): run `npm run schema:functions`
    again now that migration 20260803120000 adds a tracked function. Confirm
    `supabase/schema/functions/strategy_analytics_stamp_computing_started.sql` is created and
    `npx tsx scripts/dump-sql-functions.ts --check` exits 0. Commit ONLY the snapshot changes,
    its own commit (S-9): `fix(142.1): regenerate SQL function snapshot after trigger migration (D-12 run 2)`.
  </action>
  <verify>
    <automated>npx tsx scripts/dump-sql-functions.ts --check</automated>
  </verify>
  <acceptance_criteria>
    - `--check` exits 0
    - supabase/schema/functions/strategy_analytics_stamp_computing_started.sql exists and contains `RETURN NEW` and no `PERFORM`
    - The commit touches only supabase/schema/functions/ files
  </acceptance_criteria>
  <done>Snapshot gate green with the trigger tracked; the D-12 trap is not re-armed at merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| every strategy_analytics writer (service_role PostgREST, SECDEF bridge, pg_cron) → the table | the trigger fires on all three privileged paths; RLS already denies anon/authenticated writes (20260405061912:43-44) |
| PostgREST RPC surface → trigger function | a trigger function with default ACLs is callable as an RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.1-01 | Denial of Service | trigger body PERFORMing a REVOKEd SECDEF helper → 42501 on every service_role write (the 20260516170000 PRODUCTION-BREAKING class) | mitigate | body is 100% self-contained (NEW/OLD/TG_OP/now() only); self-verify DO block (ii) greps the deployed functiondef for PERFORM/SECURITY DEFINER at apply time |
| T-142.1-02 | Tampering | caller-suppliable parameter on a cross-tenant mechanism (the 20260516170100 incident class: "the parameter IS the attack surface", mig:114-118) | mitigate | trigger is parameterless (ASVS V5); the companion arm's bounds are literals in the cron body |
| T-142.1-03 | Elevation of Privilege | trigger function invocable via PostgREST RPC by anon/authenticated | mitigate | REVOKE ALL FROM PUBLIC, anon, authenticated (ASVS V4, 20260716131000:74 convention); acceptance grep pins it |
| T-142.1-04 | Denial of Service / compliance | a DELETE guard would abort the sanitize_user GDPR cascade | mitigate | trigger is INSERT/UPDATE-only; header comment names the sanitize_in_progress exemption obligation so nobody widens it blind |
| T-142.1-07 | Tampering (integrity) | trigger function untracked in the canonical snapshot → next author re-bases from a stale body | mitigate | Task 3 (D-12 run 2) in its own commit; sql-function-snapshot.yml:84 stays blocking |
| T-142.1-SC | Tampering | package installs | accept (N/A) | no installs this phase |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_main_worker.py -q` green (drift gates on the new file)
- `npx tsx scripts/dump-sql-functions.ts --check` exits 0 (after Task 3)
- `cd analytics-service && pytest -q` full suite green at plan end
- `cd analytics-service && pytest tests/test_sql_gate_scoped_updates.py -q` green (Part 6 respects D-05 scoping)
</verification>

<success_criteria>
- ONE forward-only migration carries the trigger and the companion arm; 20260802120000 untouched
- All three same-commit couplings honored (C-6 arm D, C-10 re-point incl. the :2163 comment, C-11 single interval)
- The D-05 bi-arm neutralization contract is stated beside the companion arm and honored — the inverted arm D is deterministic on the shared TEST project
- Part 6 exists, ungated, sentinel-based, with 6b's intermediate transition-out — SC-17/01 and SC-11 achievable at the D-16 run
- Snapshot regenerated a second time in its own commit
- NOTE for the record: the trigger closes D-11 only AT THE SOURCE (future writes); the companion arm is what rescues rows that ALREADY exist — never claim the trigger subsumes D-11 (C-1)
</success_criteria>

<output>
Create `.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-05-SUMMARY.md` when done.
Include a "Falsifiability evidence" section noting SC-17/01, SC-11 and SC-16 are DEFERRED to plan 142.1-07's orchestrator run (state this explicitly — do NOT mark them observed), plus the drift-gate green on the new file.
</output>
