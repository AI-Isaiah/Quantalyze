---
phase: 142.1
slug: close-142-review-findings-chain-start-stamp-preservation-dep
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-08-02
---

# Phase 142.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `142.1-RESEARCH.md` § "Validation Architecture".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (SQL)** | plain PL/pgSQL under `psql -v ON_ERROR_STOP=1` — **pgTAP is NOT installed** |
| **Framework (Python)** | pytest, `asyncio_mode = auto` — ⚠️ run from `analytics-service/` ONLY (repo-root runs miss the VCR `cassette_library_dir` and make LIVE broker calls) |
| **Framework (TS)** | Vitest 4.1.2 |
| **Config files** | `analytics-service/pytest.ini`; `vitest.config.ts`; SQL gates wired by glob at `.github/workflows/ci.yml:957-975` |
| **Quick run command** | `cd analytics-service && pytest tests/test_computing_started_at_stamp.py tests/test_job07_reaper_off_worker_loop.py tests/test_main_worker.py -x -q` |
| **Full suite command** | `cd analytics-service && pytest -q` + `npm test` + every `supabase/tests/test_*.sql` + `npm run typecheck` + `mypy --strict` |
| **Estimated runtime** | quick ~45 s · full ~14 min |

---

## Sampling Rate

- **After every task commit:** `cd analytics-service && pytest tests/test_computing_started_at_stamp.py tests/test_job07_reaper_off_worker_loop.py tests/test_main_worker.py -x -q`
- **After every plan wave:** `cd analytics-service && pytest -q` + `npm run typecheck` + `npx tsx scripts/dump-sql-functions.ts --check` + `cd analytics-service && mypy --strict --follow-imports=silent services/ routers/ models/`
- **Before `/gsd:verify-work`:** full pytest + full vitest + every `supabase/tests/test_*.sql` green, **including D-16's orchestrator run**
- **Max feedback latency:** 45 seconds

⚠️ `npx tsx scripts/dump-sql-functions.ts --check` appears in the per-wave command **because the trigger migration re-arms it**. It is not enough to run it once at D-12.

---

## Per-Task Verification Map

*Populated by the planner — one row per task, keyed to the plan IDs it produces.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 142.1-01.T1 | 01 | 1 | JOB-01 | T-142.1-07 | canonical snapshot carries the 142 stamp semantics (D-12 run 1, FIRST commit) | CLI gate | `npx tsx scripts/dump-sql-functions.ts --check` | ✅ exists | ⬜ pending |
| 142.1-01.T2 | 01 | 1 | JOB-03 | — | JOB-03 text states the chain-inclusive derivation (D-13); DEF-142.1-08 logged (C-5) | grep + reviewer | `grep -qF '43,920' .planning/REQUIREMENTS.md && grep -q DEF-142.1-08 TODOS.md` | ✅ exists | ⬜ pending |
| 142.1-01.T3 | 01 | 1 | JOB-01/02/03 | — | 142 ledger 11/11 Observed, SC-5b/SC-5c coordinates corrected (D-14, C-3) | artifact / human | ledger-section grep (zero ⬜ pending) | ✅ exists | ⬜ pending |
| 142.1-02.T1 | 02 | 2 | JOB-01/02 | T-142.1-08 | ONE shared union scan surface; planted reaper id in services/basis_series.py reddens (D-10, SC-10) | pytest | `cd analytics-service && pytest tests/test_computing_started_at_stamp.py tests/test_job07_reaper_off_worker_loop.py tests/test_dark_path_deleted.py tests/test_cash_basis_series_sc4.py -q` | ❌ W0 (`_scan_helpers.py`) | ⬜ pending |
| 142.1-02.T2 | 02 | 2 | JOB-02 | — | wall-clock budget deleted; tick pair asymmetry survives (D-06, SC-06); backlog_rows=0 (D-09, SC-09 regression-only) | pytest | `cd analytics-service && pytest tests/test_job07_reaper_off_worker_loop.py -q` | ✅ exists | ⬜ pending |
| 142.1-02.T3 | 02 | 2 | JOB-03 | T-142.1-09 | 3-tuple JOB_CHAIN_FOLLOW_ON cannot raise out of the handler (D-15, SC-15) | pytest (tdd) | `cd analytics-service && pytest tests/test_long_fetch_follow_on_guard.py -q` | ❌ W0 (new test) | ⬜ pending |
| 142.1-03.T1 | 03 | 2 | JOB-02 | T-142.1-05 | three neutralizing UPDATEs narrowed; unscoped strategy_analytics UPDATE reddens (D-05, SC-05) | pytest grep gate | `cd analytics-service && pytest tests/test_sql_gate_scoped_updates.py -q` | ❌ W0 (new gate) | ⬜ pending |
| 142.1-03.T2 | 03 | 2 | JOB-02 | T-142.1-05 | sql-tests serialized into shared-test-db; shape pinned (D-05, C-4) | vitest YAML gate | `npx vitest run src/__tests__/critical-regressions.test.ts` | ✅ exists | ⬜ pending |
| 142.1-03.T3 | 03 | 2 | JOB-01 | T-142.1-06 | supabase-migrate fails loud on unset secrets for push (D-02/R1, C-2); shape pinned | vitest YAML gate + reviewer | `npx vitest run src/__tests__/critical-regressions.test.ts` | ✅ exists | ⬜ pending |
| 142.1-04.T1 | 04 | 3 | JOB-01 | — | AST gate records unresolvable payloads instead of raising; counters re-derived (D-07, SC-07) | pytest self-test | `cd analytics-service && pytest tests/test_computing_started_at_stamp.py -q` | ✅ exists | ⬜ pending |
| 142.1-04.T2 | 04 | 3 | JOB-01 | T-142.1-11 | TS census covers src/**/*.ts(x); planted src/lib writer reddens; 3 benign shapes survive (D-04, SC-04) | pytest | `cd analytics-service && pytest tests/test_computing_started_at_stamp.py -q` | ✅ exists | ⬜ pending |
| 142.1-04.T3 | 04 | 3 | JOB-01 | T-142.1-10 | terminal-status TS literal must carry computation_warned; csv-finalize fixed inline (D-03, C-7, SC-03 RED observed live) | pytest + typecheck | `cd analytics-service && pytest tests/test_computing_started_at_stamp.py -q && npm run typecheck` | ✅ exists | ⬜ pending |
| 142.1-05.T1 | 05 | 3 | JOB-01/02 | T-142.1-01..04 | trigger + companion arm in ONE migration; arm D inverted + _REAPER_MIGRATION_NAME re-pointed SAME commit (D-17/D-01/D-11, C-6/C-10/C-11) | pytest drift gates | `cd analytics-service && pytest tests/test_main_worker.py -q` | ✅ exists | ⬜ pending |
| 142.1-05.T2 | 05 | 3 | JOB-01 | T-142.1-01 | SQL gate Part 6 (G1 sentinel, ungated, no skip arm) — SC-17/01 achievable | SQL gate (runs at D-16) | structural greps + `pytest tests/test_sql_gate_scoped_updates.py -q` | ❌ W0 (Part 6) | ⬜ pending |
| 142.1-05.T3 | 05 | 3 | JOB-01 | T-142.1-07 | snapshot regenerated AFTER the trigger (D-12 run 2, own commit) | CLI gate | `npx tsx scripts/dump-sql-functions.ts --check` | ✅ exists | ⬜ pending |
| 142.1-06.T1 | 06 | 4 | JOB-01 | T-142.1-12 | _mark_computing comment names the trigger; false premise deleted (D-01 prose — reviewer-only) | grep + reviewer | `! grep -q 'genuinely transitions the row INTO computing' analytics-service/services/analytics_runner.py` | ✅ exists | ⬜ pending |
| 142.1-06.T2 | 06 | 4 | JOB-01/02 | T-142.1-06 | PGRST204 ⇒ fallback literal without the stamp key ⇒ terminal failed still lands (D-02/R2, C-12, SC-02) | pytest (tdd) + mypy | `cd analytics-service && pytest tests/test_pgrst204_recovery_fallback.py tests/test_computing_started_at_stamp.py -q` | ❌ W0 (new test) | ⬜ pending |
| 142.1-07.T1 | 07 | 5 | JOB-01/02 | T-142.1-13 | [BLOCKING] migration applied to TEST + verified by direct query (trigger, cron command, ACL) | orchestrator MCP | manual — pasted query outputs | — | ⬜ pending |
| 142.1-07.T2 | 07 | 5 | JOB-01/02 | T-142.1-13 | D-01 sentinel probe live on TEST (BEGIN/ROLLBACK); D-02 schema-state confirmation | orchestrator MCP | manual — pasted outputs | — | ⬜ pending |
| 142.1-07.T3 | 07 | 5 | JOB-01/02/03 | T-142.1-05/13 | D-16: 6-part gate end-to-end on TEST, exit 0, no pg_cron SKIP in Parts 2/3; ledger consolidated 15/15 honest | orchestrator psql/MCP | manual — pasted gate output (SC-16, SC-17/01, SC-11) | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

New test surface that must exist before the fixes can be proven:

- [ ] `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql` — **new Part 6** (G1 trigger sentinel gate) — D-17 / D-01
- [ ] same file — **new companion-arm assertions** *and* **INVERT existing arm D at `:333-336`** ("NULL-stamp row left unmutated") — D-11
- [ ] `analytics-service/tests/_scan_helpers.py` — shared `_repo_root` / comment-detection / `_py_scan_files` over the **union** list `{main_worker.py, main.py} ∪ services/** ∪ routers/** ∪ scripts/**` — D-10
- [ ] `analytics-service/tests/test_computing_started_at_stamp.py` — TS rule extended so every **terminal-status object literal** must carry `computation_warned`, and the TS glob widened past `src/app/api/**/*.ts` — D-03 / D-04
- [ ] new pytest — PGRST204 fallback in `_mark_unrecoverable` — D-02 / R2
- [ ] new pytest — `JOB_CHAIN_FOLLOW_ON` arity tolerance — D-15

⛔ **D-08's Wave-0 item ("a derivation source for kinds that can hold a `strategy_analytics` row at `computing`") is deliberately absent — D-08 is CUT.** See CONTEXT.md correction C-5.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The 637-line SQL gate passes end-to-end against TEST (D-16) | JOB-01/02/03 | Needs real `auth.users` / `profiles` / `compute_jobs` constraints; **MCP tools are stripped from subagents** (`anthropics/claude-code#13898`) | Prefer `psql -v ON_ERROR_STOP=1 -f supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql` (runs as one unit, preserves inter-block state). Fall back to MCP `execute_sql` block-by-block only if `psql`/`TEST_SUPABASE_DB_URL` is unavailable — see OQ-2. **TEST = `qmnijlgmdhviwzwfyzlc`. NEVER `khslejtfbuezsmvmtsdn` (PROD).** ⚠️ D-05 must land first. **State which route was used in the evidence.** |
| Falsifiability ledger backfill is faithful to Pass C (D-14) | JOB-01/02/03 | Planning artifact — no runtime observable | Transcribe Observed/Evidence **verbatim** from `142-VERIFICATION.md`; **correct** two stale Mutation coordinates (SC-5b → `job_worker.py:6704`, not `~:6635/~:6744`; SC-5c → `keys/sync/route.ts:569`, not `:532`). Do NOT re-derive the mutations. |
| JOB-03's threshold text matches the shipped derivation (D-13) | JOB-03 | Prose in `.planning/REQUIREMENTS.md:53` | Reviewer confirms the text states the **43,920 s chain-inclusive** derivation, not `batch_size × max_per_kind_timeout` (which yields 9,000 s, ≈4.9× too small). |
| `_mark_computing`'s comment no longer asserts a false premise (D-01) | JOB-01 | Prose at `analytics_runner.py:1236-1237` | Reviewer confirms the comment no longer claims *"every invocation of this writer genuinely transitions the row INTO computing"* — it does not; the row is already `computing` from hop 1. |
| `supabase-migrate.yml` fails loud when its secrets are unset (D-02 / R1) | JOB-02 | Only fully observable by deliberately unsetting a repo variable — not a per-commit gate | Assert the YAML shape in a test (the `apply` step is not gated behind a condition that can silently skip while the workflow reports success — today `:129-141` does exactly that). Reviewer confirms the fail-loud branch. |

---

## Falsifiability Ledger

> **Coverage answers "is it verified?". This section answers "CAN the verification FAIL?"**
> One row per in-scope item. Complete the Observed column at execution time.

⚠️ **This phase exists partly because Phase 142's ledger was reported closed when 7 of 11 rows had never been run.** Do not repeat it. `⬜ pending` is not `✅ Observed`, and "no SKIPPED rows remain" is not "all rows observed".

| SC | Mutation (exact edit to production source) | Must turn RED | Observed? | Evidence |
|----|-------------------------------------------|---------------|-----------|----------|
| SC-12 | revert `supabase/schema/functions/sync_strategy_analytics_status.sql` to the `20260710150000` body | `npx tsx scripts/dump-sql-functions.ts --check` exits 1 | ⬜ pending | *(currently RED — confirmed by orchestrator 2026-08-02 and re-confirmed by research)* |
| SC-17/01 | delete the trigger's coercion arm in the new migration | new SQL gate **Part 6 / G1**: seed `(computing, sentinel=now()-3h)`, `UPDATE … SET computation_status='computing', computing_started_at=now()`, assert the sentinel survives ⇒ observed becomes frozen `now()` ≠ sentinel | ⬜ pending | |
| SC-02 | delete the PGRST204 fallback from `_mark_unrecoverable` | new pytest: under a simulated PGRST204 the terminal `failed` never lands, the row stays `computing` | ⬜ pending | |
| SC-03 | delete `computation_warned: false` from `csv-finalize/route.ts:768` | the **extended** TS parity rule in `test_computing_started_at_stamp.py` | ⬜ pending | ⚠️ **Today this mutation has NO observable — the existing gate only checks `computing_started_at`. The gate extension is what makes this row falsifiable at all.** |
| SC-04 | plant a `strategy_analytics` writer in `src/lib/` or a `.tsx` server action | the widened TS census (`EXPECTED_TOTAL` / `EXPECTED_PER_FILE`) | ⬜ pending | |
| SC-05 | restore an unqualified `UPDATE public.strategy_analytics` (no `strategy_id = ANY(v_seeded)` scope) in `supabase/tests/` | the new grep gate | ⬜ pending | Three real sites: `:340`, `:443`, `:466`. `:553` is correctly scoped — leave it. |
| SC-06 | make the yielding twin blocking in `test_job07_reaper_off_worker_loop.py` | the surviving `tick_before_work` / `tick_after_work` control pair flips | ⬜ pending | Removal of an assertion has no positive observable; **the asymmetry of the surviving pair IS the proof.** |
| SC-07 | restore the AST gate's raise-on-unresolvable-payload | a payload refactored into a helper (temp copy) makes the gate raise instead of pass | ⬜ pending | |
| SC-09 | — | *no mutation available: this is a negative change (`backlog_rows=5_000` → `0`).* **The observable is the unchanged green of the same 8 tests, not a red.** | ⬜ pending | Regression-only. Record honestly as such — do **not** record it as caught. |
| SC-10 | plant a reaper identifier in `analytics-service/services/basis_series.py` | `test_job07`'s structural gate, via the extracted `_scan_helpers._py_scan_files()` union list | ⬜ pending | ⭐ **Genuinely new RED — today this is green, and that is the bug.** `test_job07:140` currently *claims* parity it does not have. |
| SC-11 | delete the companion cron arm from the new migration | SQL gate: seed `(computing, NULL, no active job)`, `EXECUTE v_command`, assert the stamp is non-NULL and status is still `computing` ⇒ stamp stays NULL | ⬜ pending | ⚠️ Must **invert** existing arm D (`:333-336`) in the SAME commit. |
| SC-15 | restore the fixed-arity 2-tuple unpack outside the `try:` in `long_fetch.py:588-592` | new pytest: monkeypatch `JOB_CHAIN_FOLLOW_ON["process_key_long"]` to a 3-tuple, call the tail path, assert no raise ⇒ `ValueError` escapes | ⬜ pending | ⭐ Makes a **thrice-refuted latent** finding falsifiable for the first time. |
| SC-13 | — | *prose in `.planning/REQUIREMENTS.md:53`* | ⬜ pending | **Human/reviewer only.** `TestReaperThresholdInvariant` already fails on the bad formula, but the requirement text itself has no automated observable. |
| SC-14 | — | *planning artifact* | ⬜ pending | **Human/orchestrator only.** |
| SC-16 | unapply migration `20260802120000` | `psql … -f test_strategy_analytics_stuck_computing_reaper.sql` exits non-zero | ⬜ pending | **Orchestrator-only (MCP/DB).** Pass C already proved the gate reddens when the migration is unapplied; what has never been proven is that it passes end-to-end when applied. |

*Rules:*
- **Observed means run.** "The test covers it" is not evidence. Paste the failing assertion.
- **A mutation that is skipped** (ambiguous anchor, unreachable) is recorded as skipped, **never as caught**.
- **Prefer the second member of a class.** SC-10's planted identifier in `basis_series.py` is exactly this — a site the author did not have in mind.
- ⛔ **SC-08 is absent by design** — D-08 is CUT (CONTEXT.md correction C-5). Do not add a row for it.

---

## Oracle Independence

> The failure this catches: assertions that read their expected value out of the module under test, so the test passes for any implementation.

- [ ] No test imports a **constant** from the module it tests — expected values are **literals** in the test
- [ ] No assertion compares a value to itself via a re-export, fixture, or table under test
- [ ] Table/registry sizes are pinned to a **literal count**, not to `len(THE_TABLE)`
- [ ] Any fake/double is pinned against the real contract it stands in for (version, key shape, semantics)

**Deliberate note on the third box:** `test_main_worker.py:1295`'s `assert len(TIMEOUT_PER_KIND) == 15` **satisfies** this rule. D-08 argued against it and has been **cut** for exactly that reason — the conflict between "literal counts are the convention" and "literal counts are a trip-wire" is a convention decision, not a remediation task. Tracked as `DEF-142.1-08` in `TODOS.md`.

**Money-math oracle rule (project doctrine):** the reap-threshold assertions must pin the **economics** (the 43,920 s chain-inclusive derivation and what it protects against), not the implementation's own constant. `TestReaperThresholdInvariant`'s premise is currently false because the stamp does not measure the whole chain — SC-17/01 fixes the code so the premise becomes true, rather than relaxing the assertion.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 45 s
- [ ] **Every in-scope item has a Falsifiability Ledger row** (15 rows; SC-08 absent by design)
- [ ] **Every ledger row is `Observed ✅` with pasted evidence, or explicitly marked skipped-with-reason**
- [ ] **The five human/orchestrator-only rows (SC-13, SC-14, SC-16, and the prose halves of SC-01 and SC-02) are recorded as such — never as automated passes**
- [ ] **Oracle Independence checklist complete**
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
