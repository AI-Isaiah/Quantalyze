---
phase: 142.1
plan: 07
type: execute
wave: 5
depends_on: [142.1-03, 142.1-05, 142.1-06, 142.1-08]
files_modified:
  - .planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-VALIDATION.md
autonomous: false
requirements: [JOB-01, JOB-02, JOB-03]

must_haves:
  truths:
    - "Migration 20260803120000 is APPLIED to TEST (qmnijlgmdhviwzwfyzlc) and verified by direct query — trigger present, cron command carries both statements, function ACL revoked — never by trusting an apply tool's success:true"
    - "The 637-line SQL gate (now 6 parts) has been run END-TO-END against TEST for the first time by anyone: exit 0, Parts 2/3 did NOT print the pg_cron SKIP, and the route used (psql vs MCP block-by-block) is stated in the evidence"
    - "D-01's and D-02's TEST-DB confirmations are recorded with pasted query output"
    - "The 142.1 falsifiability ledger is consolidated: 15 rows each ✅ Observed with pasted evidence, or explicitly human-only/regression-only/skipped-with-reason — never a pending row reported as closed (the exact over-claim that created this phase)"
  artifacts:
    - path: ".planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-VALIDATION.md"
      provides: "completed 15-row ledger + sign-off, transcribed from executor SUMMARYs + this plan's orchestrator runs"
  key_links:
    - from: "supabase/migrations/20260803120000_..."
      to: "TEST project qmnijlgmdhviwzwfyzlc"
      via: "MCP apply_migration (stamps now() as version — known drift, record the stamped version)"
      pattern: "20260803120000"
    - from: "supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql"
      to: "TEST project"
      via: "psql -v ON_ERROR_STOP=1 -f (preferred; preserves inter-part ordering) or MCP execute_sql block-by-block"
      pattern: "ON_ERROR_STOP"
---

<objective>
The orchestrator-only close-out: apply the new migration to TEST [BLOCKING], confirm the D-01
stamp behavior and the D-02 schema state on TEST, run the SQL gate end-to-end against TEST for
the first time ever (D-16), and consolidate the phase's falsifiability ledger.

Purpose: MCP tools are stripped from subagents (anthropics/claude-code#13898) — every task in
this plan states the EXACT command or MCP call so an executor STOPS rather than silently
skipping. Build/typecheck pass WITHOUT the apply (types come from config, not the live DB) —
a false-positive verification state. The phase CANNOT pass verification without this plan.
⛔ TEST = qmnijlgmdhviwzwfyzlc. PROD = khslejtfbuezsmvmtsdn — NEVER touch PROD.
Output: applied+verified TEST schema, D-16 evidence, completed 142.1-VALIDATION.md ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-CONTEXT.md
@.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-VALIDATION.md
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migration 20260803120000 to TEST via MCP and verify by direct query</name>
  <files>none (TEST database state)</files>
  <read_first>
    - supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql (the file being applied)
    - .planning/phases/142-.../142-VALIDATION.md § "‡ DEPLOYED-RUN" (the 142 precedent: verify by direct query, never trust success:true; MCP stamps now() as the version — known drift, 142's file 20260802120000 is recorded on TEST as 20260802111053)
  </read_first>
  <action>
    ORCHESTRATOR ONLY (MCP is stripped from subagents — an executor reaching this task must STOP
    and return control; do not skip, do not fake).
    1. Apply: `mcp__plugin_supabase_supabase__apply_migration` with
       project_id "qmnijlgmdhviwzwfyzlc", name
       "strategy_analytics_stamp_trigger_null_stamp_clock_start", query = the FULL contents of
       supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql.
       Record the version MCP stamps (it will be its own now(), NOT 20260803120000 — known
       drift; note it so the merge-time PROD apply of the FILE timestamp is not mistaken for a
       re-apply).
    2. Verify by direct `mcp__plugin_supabase_supabase__execute_sql` queries (paste each result):
       (a) SELECT tgname FROM pg_trigger WHERE tgname = 'strategy_analytics_stamp_computing_started_trigger' AND tgrelid = 'public.strategy_analytics'::regclass AND NOT tgisinternal; → exactly 1 row
       (b) SELECT command FROM cron.job WHERE jobname = 'reap_strategy_analytics_stuck_computing'; → ONE row whose command contains BOTH `computing_started_at IS NULL` (companion arm) and `interval '16 hours'` (reap arm), and does NOT contain `computed_at`
       (c) SELECT proacl FROM pg_proc WHERE proname = 'strategy_analytics_stamp_computing_started'; → ACL shows no PUBLIC/anon/authenticated EXECUTE
       (d) SELECT count(*) FROM strategy_analytics WHERE computation_status = 'computing' AND computing_started_at IS NULL; → record the count (the companion arm's rescue population; expected 0 per the authoring-time census, but record the ACTUAL number)
    ⛔ Never run any of this against khslejtfbuezsmvmtsdn.
  </action>
  <verify>
    <automated>MISSING — orchestrator MCP calls; the pasted query outputs in 142.1-VALIDATION.md / the SUMMARY are the evidence</automated>
  </verify>
  <acceptance_criteria>
    - apply_migration succeeded AND all four verification queries pasted with the expected shapes
    - The MCP-stamped version is recorded next to the filename timestamp
    - Zero statements executed against PROD
  </acceptance_criteria>
  <resume-signal>Paste the four query outputs; type "applied" to continue</resume-signal>
  <done>TEST carries the trigger + two-statement cron body, verified by query, drift recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: D-01 + D-02 TEST-DB confirmations</name>
  <files>none (TEST database state)</files>
  <read_first>
    - 142.1-RESEARCH.md § "Architecture Patterns" Pattern 1 (the trigger arms this probe exercises) and § "D-02 — deploy sequencing" (what is and is not confirmable on TEST: PGRST204 is a PostgREST schema-cache error; on TEST the column EXISTS, so the honest confirmation is schema state + ledger versions, with R2's behavior proven by the plan 142.1-06 pytest)
  </read_first>
  <action>
    ORCHESTRATOR ONLY.
    D-01 confirmation (the never-advance stamp behavior, live on TEST): run ONE
    `mcp__plugin_supabase_supabase__execute_sql` call against qmnijlgmdhviwzwfyzlc containing a
    single multi-statement script wrapped in BEGIN; ... ROLLBACK; that: seeds an
    auth.users/profiles/strategies/strategy_analytics FK chain with a fresh uuid (copy the seed
    shape from the SQL gate's Part 4), INSERTs the strategy_analytics row DIRECTLY at computation_status='computing' with
    computing_started_at = now() - interval '3 hours' (the sentinel — established at INSERT, where the UPDATE-only trigger under test does not fire, D-18; never by a post-seed UPDATE, which the trigger would coerce), then issues
    UPDATE public.strategy_analytics SET computation_status='computing', computing_started_at = now()
    WHERE strategy_id = <the seeded id>, then SELECTs computing_started_at. PASS = the returned
    stamp equals the sentinel (the trigger coerced the advance). Paste the output. The ROLLBACK
    guarantees nothing persists.
    D-02 confirmation (schema/deploy state): execute_sql —
    (a) SELECT column_name FROM information_schema.columns WHERE table_name='strategy_analytics' AND column_name='computing_started_at'; → 1 row (the column exists on TEST, so the PGRST204 window is closed THERE);
    (b) SELECT version FROM supabase_migrations.schema_migrations WHERE version IN ('20260802111053','20260803120000') OR version LIKE '202608%' ORDER BY version; → paste; confirm both the 142 twin (20260802111053) and Task 1's stamped version appear.
    Record explicitly: the PGRST204 BEHAVIOR (fallback) is proven by the plan 142.1-06 pytest
    (SC-02), not by a live TEST probe — a live probe would require deliberately breaking the
    TEST schema cache, which is not acceptable on the shared project. This split must be stated
    in the evidence, not blurred.
  </action>
  <verify>
    <automated>MISSING — orchestrator MCP calls; pasted outputs are the evidence</automated>
  </verify>
  <acceptance_criteria>
    - D-01 probe output pasted: sentinel survived the now() advance (trigger live on TEST)
    - Column-presence + migration-ledger outputs pasted
    - The SC-02-is-pytest-proven / TEST-probe-is-schema-only split stated verbatim in the evidence
  </acceptance_criteria>
  <resume-signal>Paste both confirmations; type "confirmed" to continue</resume-signal>
  <done>D-01's coercion observed live against the real deployed trigger; D-02's schema state recorded honestly.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: D-16 — run the SQL gate end-to-end against TEST; consolidate the 142.1 ledger</name>
  <files>.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-VALIDATION.md</files>
  <read_first>
    - 142-VERIFICATION.md:78-80 (the settling command and the no-pg_cron-SKIP requirement)
    - 142.1-VALIDATION.md § "Manual-Only Verifications" row 1 (psql preferred — runs as one unit; MCP execute_sql block-by-block ONLY as fallback, in order — each DO block re-seeds its own data) and § "Falsifiability Ledger" (the 15 rows to consolidate)
    - All seven executor SUMMARYs 142.1-01 through 142.1-06 plus 142.1-08 ("Falsifiability evidence" sections — the pasted REDs to transcribe)
  </read_first>
  <action>
    ORCHESTRATOR ONLY. Prerequisites already sequenced: D-05 landed (plan 142.1-03, wave 2) so
    the gate's cross-tenant neutralizers are DELETED and it isolates by construction (extreme-old seeds + seeded-set assertions, residual assumption in the gate header — D-18), Part 6 landed (plan 142.1-08, wave 4), and Task 1 applied the migration.
    1. Run the gate. PREFERRED: `psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql`
       — expected exit 0 with NOTICEs from Parts 1-6, and Parts 2/3 must NOT print
       "SKIP ... pg_cron not installed" (a skip there means the gate is a no-op on the only
       project it is trusted on). FALLBACK if TEST_SUPABASE_DB_URL is unset (it was for Pass C):
       `mcp__plugin_supabase_supabase__execute_sql` against qmnijlgmdhviwzwfyzlc, block by block
       IN FILE ORDER (each Part is one BEGIN/DO/ROLLBACK unit and re-seeds its own data).
       STATE WHICH ROUTE WAS USED in the evidence (OQ-2). Paste the NOTICE tail + exit code.
       This run observes SC-16 (gate passes end-to-end when applied) and — via Part 6 and the
       inverted arm D executing against the real deployed objects — SC-17/01 and SC-11's
       positive halves. The RED halves of SC-17/01 and SC-11 (delete the coercion arm / the
       companion arm ⇒ gate reddens) were proven falsifiable by Part 6's sentinel design and
       arm D's inversion; if a TEST-side mutant run is not performed, record those halves as
       design-proven-not-mutant-run — honestly, not as observed REDs.
    2. Consolidate 142.1-VALIDATION.md: transcribe every "Falsifiability evidence" section from
       the seven executor SUMMARYs into the 15-row ledger (SC-12, SC-02, SC-03, SC-04, SC-05,
       SC-06, SC-07, SC-09, SC-10, SC-15 from SUMMARYs; SC-17/01, SC-11, SC-16 from step 1;
       SC-13, SC-14 recorded as human/reviewer-only). Every row becomes ✅ Observed with pasted
       evidence, or explicitly regression-only (SC-09), human-only (SC-13, SC-14), or
       design-proven-with-reason. ⛔ A row that was not run is ⬜ pending or
       skipped-with-reason — NEVER ✅ Observed; "no SKIPPED rows remain" is NOT "all rows
       observed" (P7 — the exact conflation that created this phase).
    3. Complete the Validation Sign-Off block and set `nyquist_compliant: true` +
       `wave_0_complete: true` + `status: executed` in the frontmatter only if the checklist is
       genuinely true. Commit:
       `docs(142.1): D-16 gate run evidence + consolidated falsifiability ledger`.
    NOT in scope (Deferred, per CONTEXT): PROD cron registration, the PROD backfill census, the
    live wizard render — those remain 142-VERIFICATION.md human items for the merge gate.
  </action>
  <verify>
    <automated>MISSING — orchestrator DB run; the pasted psql/MCP output + exit code in 142.1-VALIDATION.md is the evidence</automated>
  </verify>
  <acceptance_criteria>
    - Gate output pasted: exit 0, Parts 1-6 NOTICEs, no pg_cron SKIP in Parts 2/3, route stated
    - All 15 ledger rows carry an honest final state; zero rows claim Observed without pasted evidence
    - Sign-off block complete; frontmatter updated only if true
    - SC-08 remains absent by design (D-08 CUT — verify no row was "helpfully" added)
  </acceptance_criteria>
  <resume-signal>Paste the gate output and confirm the ledger is consolidated; type "verified" to close the phase</resume-signal>
  <done>The 637-line gate has finally been run end-to-end against the schema it is trusted on; the phase's own evidence is complete and honest — the failure mode that created 142.1 is not repeated by 142.1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator session → TEST project | privileged DB access; one wrong project_id is a PROD incident |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.1-13 | Tampering | running the gate or apply against PROD (khslejtfbuezsmvmtsdn) | mitigate | every command in this plan hard-codes qmnijlgmdhviwzwfyzlc; PROD ref appears only in ⛔ prohibitions; D-01 probe wraps in BEGIN/ROLLBACK |
| T-142.1-05 | Denial of Service (CI/TEST) | gate run locking other tenants' rows | mitigate | D-05 landed first (plan 142.1-03, LOCKED constraint 3); cross-tenant neutralizers DELETED — isolation by construction, residual assumption stated in the gate header (D-18); Part-6/Part-4 writes are seeded-id-scoped and rolled back |
| T-142.1-SC | Tampering | package installs | accept (N/A) | no installs this phase |
</threat_model>

<verification>
- All three checkpoint tasks completed with pasted evidence
- 142.1-VALIDATION.md ledger 15/15 in an honest final state; sign-off complete
- Full local suites remain green: `cd analytics-service && pytest -q`, `npm test`, `npm run typecheck`, `npx tsx scripts/dump-sql-functions.ts --check`
</verification>

<success_criteria>
- D-16 closed: the gate ran end-to-end on TEST with Parts 2/3 live (not skipped), route documented
- D-01/D-02 TEST confirmations recorded with the honest pytest-vs-live split
- The phase's falsifiability contract is discharged the way 142's was not
</success_criteria>

<output>
Create `.planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-07-SUMMARY.md` when done.
</output>
