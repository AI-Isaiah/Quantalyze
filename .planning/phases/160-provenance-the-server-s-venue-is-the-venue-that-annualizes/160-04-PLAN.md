---
phase: 160-provenance-the-server-s-venue-is-the-venue-that-annualizes
plan: 04
type: execute
wave: 2
depends_on: ["160-01"]
files_modified:
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
autonomous: true
requirements: [RANK-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The finalize-wizard asset_class stamp derives from the ATTESTED venue binding, never the forgeable api_keys.exchange binding (RANK-04, D-07)"
    - "The guard extension and the stamp swap land in the SAME change: skipAssetClassWrite keys on the attested binding being null, so a NULL attestation SKIPS — it never stamps traditional/√252 onto a crypto strategy through the isCryptoExchange(null)===false trap (D-07; ROADMAP 160 SC-3)"
    - "The new guard is a strict superset of the old lookup-fault guard: a lookup FAULT (both bindings null) still skips, and an unattested row with a RESOLVED exchange now also skips (Pitfall 7 — one binding, the attested one)"
    - "The B-D2 oracles pin the ECONOMICS as literal fixtures — attested deribit ⇒ crypto, attested mt5 ⇒ traditional, NULL attestation with resolved exchange ⇒ NO update captured — never re-computing the implementation's own expression (D-08; Pitfall 8; founder economic-invariant rule)"
    - "Every new oracle was observed RED under a neuter (guard reverted to the lookup-fault-only form, or swap reverted) before being trusted (founder anti-vacuity rule; ROADMAP 160 SC-4)"
    - "create-with-key's draft stamp is confirmed unchanged — it already reads the route-local exchange the same request validated (:1089); the null-attestation SKIP is safe precisely because that server-derived draft stamp survives (PATTERNS §Explicit Non-Changes; ARCHITECTURE B.3 confirmation)"
  artifacts:
    - path: "src/app/api/strategies/finalize-wizard/route.ts"
      provides: "attestation-derived stamp + extended null-attestation guard, pragma window intact, stale one-identifier comment rewritten"
      contains: "isCryptoExchange(attestedVenue)"
    - path: "src/app/api/strategies/finalize-wizard/route.test.ts"
      provides: "B-D2 economics oracles + adminApiKeysAttestedVenue harness knob; existing fault-arm oracle still green"
      contains: "adminApiKeysAttestedVenue"
  key_links:
    - from: "src/app/api/strategies/finalize-wizard/route.ts"
      to: "src/lib/closed-sets.ts"
      via: "isCryptoExchange(attestedVenue) — the branch input is the attestation; closed-sets is NOT modified (mt5 deliberately excluded from CRYPTO_EXCHANGES = √252)"
      pattern: "isCryptoExchange\\(attestedVenue\\)"
    - from: "strategies.asset_class"
      to: "analytics worker periods_per_year_for_asset_class"
      via: "the worker reads the stamp DIRECTLY as the annualization clock (√365 crypto / √252 traditional) — why a wrong stamp is a silent ~×1.203 Sharpe error; no Python change in this phase"
      pattern: "asset_class"
  prohibitions:
    - statement: "The attestation read never falls back to the forgeable column — no nullish-coalescing of the attested binding onto the exchange binding anywhere in the stamp or guard path (the in-file ⛔ rule at :1275-1278 extended to the stamp)"
      status: unverified
      flagged: true
      verification: "grep of the stamp/guard region finds no '?? apiKeyExchange' feeding skipAssetClassWrite or the asset_class branch; the NULL-attestation-with-resolved-exchange oracle would go RED under such a fallback"
    - statement: "The composite and CSV arms of the asset_class expression are byte-unchanged — the swap touches only the single-key branch input"
      status: unverified
      flagged: true
      verification: "diff of the .update block shows changes confined to the apiKeyId-truthy branch input and the guard line"
---

<objective>
Swap the money-math stamp input from the forgeable venue to the server's attestation, WITH
the null-attestation guard extension in the same change (D-07), proven by economics-pinning
B-D2 oracles observed RED under neuters (D-08). This is the RANK-04 core: the √365/√252
annualization clock derives from the venue the server attested — and a NULL attestation
annualizes on nothing (it skips), because `isCryptoExchange(null) === false` would otherwise
stamp √252 onto crypto strategies and silently inflate Sharpe ~×1.20 (√365/√252 ≈ 1.203).

Gated on the B-M1 census (D-01: the census gates the stamp swap). Runs parallel to plan 02
(zero file overlap). PR-1 material — no migration.

TODOS.md's "one-identifier change" framing is measured WRONG (160-CONTEXT §Specifics); the
in-file comment still carrying that framing is rewritten by this plan's swap commit.

Flagged planner assumption (spec-less edge probe): the probe returned `unclassified` for
RANK-04 — it stays unresolved per protocol. The oracle set below (attested-crypto /
attested-traditional / null-skip / fault-arm-regression) is the planner's MANUAL edge
enumeration for this requirement; the verifier should treat its completeness as a flagged
assumption, not a probe-derived guarantee.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-RESEARCH.md
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PATTERNS.md
@src/app/api/strategies/finalize-wizard/route.ts
@src/lib/closed-sets.ts
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–06)

See 160-01-PLAN.md for the canonical phase-wide list. This plan produces:
- `src/app/api/strategies/finalize-wizard/route.ts` — guard extension + stamp swap (single-key branch input only), rewritten stamp-rationale comment, pragma window preserved
- `src/app/api/strategies/finalize-wizard/route.test.ts` — NEW harness fixture knob `adminApiKeysAttestedVenue`; NEW B-D2 oracles (attested crypto / attested mt5 / null-skip); existing fault-arm oracle retained
- Explicit NON-changes: `src/lib/closed-sets.ts` (isCryptoExchange semantics untouched), `src/app/api/strategies/create-with-key/route.ts` (draft stamp already server-derived at :1089 — the ARCHITECTURE B.3 confirmation this plan records)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: B-D2 oracles + guard extension + stamp swap, one change</name>
  <precondition>.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md is committed with real PROD results (zero "RESULTS: PENDING" markers) — the census gates the stamp swap (D-01)</precondition>
  <files>src/app/api/strategies/finalize-wizard/route.ts, src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/finalize-wizard/route.ts:1246-1340 (the ONE query selecting both columns :1254; the attestedVenue binding with its ⛔ never-coalesce rule :1274-1282; the guard :1292; the stale one-identifier comment block :1299-1309; the @audit-skip pragma window :1311-1318; the .update stamp :1319-1329; the non-blocking error handling :1332-1339)
    - src/app/api/strategies/finalize-wizard/route.test.ts:1394-1497 (the #597 asset_class persistence describe — the harness STATE knobs, okProbe, importPost, and the fault-arm test at :1463-1478 that is the closest template AND must stay green)
    - src/lib/closed-sets.ts:553-574 (CRYPTO_EXCHANGES and the isCryptoExchange null-short-circuit — the trap; do NOT modify this file)
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PATTERNS.md §finalize-wizard (the exact before/after shapes)
  </read_first>
  <behavior>
    - Test 1 (economics: attested crypto venue annualizes √365): fixture attestation "deribit" (exchange also resolved) ⇒ STATE.assetClassUpdates contains exactly { asset_class: "crypto" } — the literal, not a recomputation
    - Test 2 (economics: attested mt5 annualizes √252): fixture attestation "mt5" ⇒ STATE.assetClassUpdates contains { asset_class: "traditional" }
    - Test 3 (THE B-D2 economics: a null attestation annualizes on nothing): fixture attestation NULL while adminApiKeysExchange resolves "bybit" ⇒ STATE.assetClassUpdates has length 0 AND the response is still 200 (skip is non-fatal)
    - Test 4 (fault-arm regression): the existing :1463 lookup-fault test stays green unmodified — fault ⇒ both bindings null ⇒ still SKIP (strict-superset guard)
  </behavior>
  <action>
    ONE commit-scoped change (D-07 — the guard extension and the swap move together;
    landing either alone is the defect this phase exists to prevent).

    Tests first: extend the #597 harness with an `adminApiKeysAttestedVenue` STATE knob so
    the mocked api_keys read returns { exchange, attested_venue } independently, then add
    Tests 1-3 above (Test 4 already exists). Each `it` carries a prose block naming the
    economic WHY and which revert reddens it (house style at :1404-1406, :1455-1462).
    Oracles pin literal fixtures per D-08 — no oracle may call isCryptoExchange or mirror
    the route's conditional (Pitfall 8, the self-referential-oracle failure class).

    Then the route edit, all within :1246-1340:
    1. Guard: `skipAssetClassWrite` becomes `Boolean(apiKeyId) && attestedVenue === null`
       — ONE binding, the attested one (Pitfall 7). Never AND the old and new conditions.
       The forgeable binding may survive ONLY in the skip warn-log copy ("venue resolved as
       X but unattested — skipping"), or be dropped from the region; it must not feed the
       guard or the stamp.
    2. Stamp: the single-key branch input becomes `isCryptoExchange(attestedVenue)`.
       Composite/CSV arms byte-unchanged. The .eq scoping and non-blocking error handling
       (:1332-1339) byte-unchanged.
    3. Rewrite the stale comment block (:1299-1309) that still carries the measured-wrong
       "ONE-IDENTIFIER change" framing: the new prose states the attestation is the stamp
       input, that a null attestation SKIPS (naming the isCryptoExchange(null)===false trap
       and the ~×1.203 Sharpe consequence), and that the skip is safe because
       create-with-key's draft stamp is server-derived (:1089). New commentary goes ABOVE
       the @audit-skip pragma; the pragma must remain within 8 lines of the .update( chain
       start (Pitfall 4 — the audit-coverage window silently un-instruments otherwise).
    4. Zero new queries: the swap consumes the EXISTING attestedVenue binding from the one
       :1254 read (SEAM_ROUTE_BUDGETS pin).
    <!-- planner-discipline-allow: isCryptoExchange(apiKeyExchange) -->
    <!-- planner-discipline-allow: apiKeyExchange === null -->
    The old forms `isCryptoExchange(apiKeyExchange)` and `apiKeyExchange === null` must not
    survive anywhere in the file after this task — including inside comments (do not quote
    the old expression when rewriting the comment block; describe it as "the forgeable
    binding" instead).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts src/__tests__/audit-coverage.test.ts --no-file-parallelism && grep -q 'isCryptoExchange(attestedVenue)' src/app/api/strategies/finalize-wizard/route.ts && grep -q 'attestedVenue === null' src/app/api/strategies/finalize-wizard/route.ts && ! grep -q 'isCryptoExchange(apiKeyExchange)' src/app/api/strategies/finalize-wizard/route.ts && ! grep -q 'apiKeyExchange === null' src/app/api/strategies/finalize-wizard/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests 1-3 added with prose rationale blocks; Test 4 (existing fault-arm) passes unmodified
    - The guard and stamp both read the attested binding; the old forms appear nowhere in the file (code OR comments)
    - The @audit-skip pragma sits within 8 lines above the .update chain start after the edit (audit-coverage suite green)
    - git diff shows zero changes to src/lib/closed-sets.ts and zero changes to src/app/api/strategies/create-with-key/route.ts — the B.3 confirmation is recorded in the SUMMARY with the :1089 citation
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>The stamp reads the attestation, a null attestation skips, the oracles pin the economics, and the stale framing is gone from the file.</done>
</task>

<task type="auto">
  <name>Task 2: Neuter audit — observe every new oracle RED, then restore</name>
  <files>src/app/api/strategies/finalize-wizard/route.ts, src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/finalize-wizard/route.ts:1246-1340 (post-Task-1 state — the two lines to neuter and restore)
  </read_first>
  <action>
    Founder anti-vacuity discipline (a test that cannot fail is worse than none) — run two
    neuter cycles, observing which named tests go RED, then restore from the committed
    Task-1 state (git checkout of the file region, never a from-memory retype):

    Cycle A (guard neuter): revert ONLY the guard line to key on the forgeable binding
    being null (the pre-Task-1 form). Expected RED: Test 3 (null-attestation skip) — the
    unattested-but-resolved fixture now takes the stamp arm and mints a traditional update.
    Expected GREEN: Tests 1, 2, 4.

    Cycle B (swap neuter): restore the guard, then revert ONLY the stamp branch input to
    the forgeable binding. Expected RED: at least one of Tests 1/2 under a divergent
    fixture — if Tests 1/2 stay green because both fixtures set exchange === attestation,
    FIRST make the fixtures divergent (attestation "deribit" with exchange "mt5" in Test 1)
    so the oracle discriminates the two bindings, observe RED, then restore. A fixture pair
    that cannot distinguish the bindings is itself a vacuous oracle — fix it now.

    After both cycles: restore, re-run the full scoped suite green, and record each
    cycle's exact RED test names + assertion messages in the SUMMARY. Working tree clean of
    neuter artifacts (git status + git diff empty against the Task-1 commit).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts --no-file-parallelism && git diff --quiet -- src/app/api/strategies/finalize-wizard/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both neuter cycles executed with first-hand RED observations (test names + failure messages transcribed into the SUMMARY — never "asserted only")
    - The Test-1 fixture is binding-divergent (attestation differs from exchange) so the swap neuter reddens it
    - Final state: suite green, file byte-identical to the Task-1 commit
  </acceptance_criteria>
  <done>ROADMAP 160 SC-4 discharged: the B-D2 oracle set is proven falsifiable against both the guard and the swap, first-hand.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api_keys.exchange (client-writable until PR-2) → money-math stamp | the forgeable input being removed from the stamp path |
| strategies.asset_class → analytics worker | the annualization clock consumers trust blindly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-160-14 | Tampering | client-forged venue steering the √365/√252 clock (the RANK-04 threat) | critical | mitigate | stamp branch input swapped to the attested binding; attestation is trigger/CHECK-protected server-side |
| T-160-15 | Tampering | NULL attestation laundering a wrong √252 stamp through isCryptoExchange(null)===false | critical | mitigate | guard extension in the SAME change (D-07); Test 3 pins the skip; neuter Cycle A proves the oracle bites |
| T-160-16 | Repudiation | vacuous oracles certifying the swap (self-referential expression re-computation) | high | mitigate | literal-fixture oracles (D-08) + mandatory divergent-fixture check in Cycle B |
| T-160-17 | Information Disclosure | none new — the plan adds no response surface | low | accept | no new output fields; skip warn-log carries venue strings already public in the row |
</threat_model>

<verification>
- Scoped route suite + audit-coverage suite green; tsc clean.
- Positive greps: attested forms present; negative greps: forgeable forms absent file-wide.
- SUMMARY carries: both neuter cycles' RED evidence, the B.3 create-with-key confirmation,
  and the pragma-window check.
</verification>

<success_criteria>
- ROADMAP 160 SC-3 and SC-4 hold: the stamp derives from the attested venue, the swap moved
  WITH the null-attestation guard extension, a NULL attestation skips (never stamps
  traditional/√252), and the oracles pin the economics with first-hand RED observations.
</success_criteria>

<output>
Create `.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-04-SUMMARY.md`
when done, recording: the neuter-cycle evidence table, the divergent-fixture note, the
create-with-key non-change confirmation, and the flagged RANK-04 edge-probe assumption for
the verifier.
</output>
