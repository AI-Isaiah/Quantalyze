---
phase: 160-provenance-the-server-s-venue-is-the-venue-that-annualizes
plan: 02
type: execute
wave: 2
depends_on: ["160-01"]
files_modified:
  - src/app/api/keys/validate-and-encrypt/route.ts
  - src/app/api/keys/validate-and-encrypt/route.test.ts
  - src/components/strategy/ApiKeyManager.tsx
  - src/components/strategy/ApiKeyManager.test.tsx
autonomous: true
requirements: [RANK-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A persist-mode request writes the api_keys row SERVER-SIDE with exchange AND attested_venue both derived from the single exchangeNormalized binding the route validated — the client-supplied venue string can no longer reach the row unvalidated (RANK-03, D-04)"
    - "The persist-mode response carries api_key_id and NO ciphertext field — key material stops round-tripping through the browser on the new path (RANK-03)"
    - "ApiKeyManager no longer INSERTs into api_keys from the browser; it consumes the route's api_key_id and its strategies.api_key_id link update and DELETE path are byte-preserved (D-05)"
    - "A request without the strict-boolean persist discriminator is never served by the persist arm — zero server-side inserts occur for legacy bodies, so a stale tab in the skew window cannot cause a double insert (edge: idempotency, resolved explicit; RESEARCH Pattern 2)"
    - "The persist arm derives both venue columns from ONE binding, so a divergent exchange/attested_venue pair is impossible at the writer and independently rejected by the DB CHECK api_keys_attested_venue_matches_exchange (edge: adjacency, resolved explicit)"
    - "A persist request with a missing or unrecognized exchange, or missing credentials, yields a coded 4xx envelope and mints no api_keys row; an absent label falls back to the server default (edge: empty, resolved explicit)"
    - statement: "If the persist-arm INSERT fails or the response is lost mid-flight, no ciphertext has been exposed to the browser and no partial row state exists — the write is a single INSERT statement (edge: concurrency)"
      verification: backstop
  artifacts:
    - path: "src/app/api/keys/validate-and-encrypt/route.ts"
      provides: "persist arm — strict persist === true branch after exchangeNormalized, admin-client INSERT, { api_key_id, valid, read_only } response"
      contains: "attested_venue"
    - path: "src/app/api/keys/validate-and-encrypt/route.test.ts"
      provides: "persist-arm oracles: captured insert payload, no-ciphertext assertion, legacy-body passthrough, non-boolean discriminator rejection"
      contains: "api_key_id"
    - path: "src/components/strategy/ApiKeyManager.tsx"
      provides: "converted connect flow — persist request, api_key_id consumption, retained link update and DELETE"
      contains: "persist: true"
  key_links:
    - from: "src/components/strategy/ApiKeyManager.tsx"
      to: "src/app/api/keys/validate-and-encrypt/route.ts"
      via: "POST /api/keys/validate-and-encrypt with persist: true; response api_key_id feeds the strategies.api_key_id link update"
      pattern: "persist: true"
    - from: "src/app/api/keys/validate-and-encrypt/route.ts"
      to: "public.api_keys (scrub trigger + coupling CHECK)"
      via: "createAdminClient() INSERT — service_role is on the scrub trigger's allowlist so attested_venue survives; the CHECK forces attested_venue = exchange"
      pattern: "createAdminClient"
  prohibitions:
    - statement: "No persist-mode response, on any arm including error arms, carries a ciphertext/encrypted-key field back to the browser"
      status: unverified
      flagged: true
      verification: "route.test.ts asserts the persist success and persist error envelopes contain no encrypted/ciphertext-named key; grep of the persist arm's NextResponse.json payloads shows none"
    - statement: "No new arm in the route logs or reports raw key material — every new catch/log path applies the perRequestSecrets scrub before console/Sentry emission"
      status: unverified
      flagged: true
      verification: "every new console.error/captureToSentry in the persist arm passes through scrubSeamError/secrets per route.ts:395-409 pattern"
---

<objective>
Land the phase's TRACER: one production-quality end-to-end path — ApiKeyManager → persist
arm in `validate-and-encrypt` → admin-client INSERT stamping both venue columns from the
server-validated value → `{ api_key_id }` back → strategies link update. This is the
Phase-156 service-role-writer pattern extended per D-04, with the request-versioned skew
discriminator (RESEARCH Pattern 2) so stale tabs keep working during the soak window.

This plan is PR-1 material ONLY: no migration file may appear here (D-06 — deploy-first,
revoke-second, never migration-first).

Purpose: ROADMAP 160 SC-2 — every INSERT path into api_keys writes the server-validated
exchange; this plan proves the architecture on one path before plan 03 expands to the
remaining two components.
Output: persist arm + converted ApiKeyManager, both tested; legacy behavior byte-preserved
for absent-discriminator bodies (until plan 05 retires that arm in the PR-2 landing).

Edge-coverage ledger (spec-less probe, RANK-03): adjacency → truths (explicit); empty →
truths (explicit); idempotency → truths (explicit); concurrency → truths (backstop marker);
ordering → carried by plan 160-05's truths (deploy-first/revoke-second sequencing).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-RESEARCH.md
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PATTERNS.md
@src/app/api/keys/validate-and-encrypt/route.ts
@src/components/strategy/ApiKeyManager.tsx
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–06)

See 160-01-PLAN.md for the canonical phase-wide list. This plan produces:
- `src/app/api/keys/validate-and-encrypt/route.ts` — persist arm (request discriminator `persist: true`, strict boolean; response `{ api_key_id, valid, read_only }`, no ciphertext; admin-client INSERT `{ user_id, exchange: exchangeNormalized, attested_venue: exchangeNormalized, label, ...ciphertext fields }`)
- `src/app/api/keys/validate-and-encrypt/route.test.ts` — persist-arm + skew-window oracles
- `src/components/strategy/ApiKeyManager.tsx` — converted connect flow (insert removed, id consumed, link + DELETE retained)
- `src/components/strategy/ApiKeyManager.test.tsx` — conversion oracles
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end persist key-connect — one path (ApiKeyManager), server writes the attested row</name>
  <precondition>.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md is committed with real PROD results (zero "RESULTS: PENDING" markers) — the D-01 gate</precondition>
  <files>src/app/api/keys/validate-and-encrypt/route.ts, src/app/api/keys/validate-and-encrypt/route.test.ts, src/components/strategy/ApiKeyManager.tsx, src/components/strategy/ApiKeyManager.test.tsx</files>
  <read_first>
    - src/app/api/keys/validate-and-encrypt/route.ts (FULL file — the normalization chokepoint at :78, the withAuth userId threading at :182-185, the coded-envelope conventions, NO_STORE_HEADERS, the perRequestSecrets scrub at :395-409, and the legacy handler internals you will reuse)
    - src/app/api/keys/validate-and-encrypt/route.test.ts:1-90 (the H-0281 harness: vi.hoisted mocks, server-only stub, ratelimit importActual-extension, analytics-client mock)
    - src/components/strategy/ApiKeyManager.tsx:190-385 (the fetch→insert→link sequence at :230-277 being converted; the link update + NEW-C37-03 error surfacing at :269-277; the SEAMUX-05 background-sync observation at :279+; the DELETE at :352 that stays)
    - src/app/api/strategies/finalize-wizard/route.ts:1192-1199 (the createAdminClient() privileged-write idiom to copy)
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PATTERNS.md §validate-and-encrypt and §component conversions (verified line coordinates and the response contract)
    - src/__tests__/audit-coverage.test.ts:1-60 (the audit contract the new INSERT must satisfy: logAuditEvent/logAuditEventAsUser in the same function body, or an @audit-skip pragma within 8 lines above the mutation chain start)
  </read_first>
  <action>
    Wire ONE path end-to-end (D-04, D-05). No other call sites, no batching; real error
    handling on the single path.

    Route (`validate-and-encrypt/route.ts`):
    - After `exchangeNormalized` is computed (:78) and BEFORE the legacy handler dispatch
      (:182-185), branch on `body.persist === true` — STRICT boolean equality; any other
      value (absent, string "true", 1) falls through to the legacy arm byte-unchanged
      (RESEARCH Pattern 2, the skew-window discriminator).
    - Persist arm: reuse the module's existing validate + encrypt path to obtain the
      validation verdict (`valid`, `read_only`) and the ciphertext fields — extract the
      shared core into a module-local helper if needed rather than duplicating any seam
      call (the rate limiter, circuit breaker, and typed catch arms must police the persist
      arm identically to the legacy arm).
    - On successful validation+encryption, INSERT via `createAdminClient()` (the
      finalize-wizard :1192 idiom): `.from("api_keys").insert({ user_id: userId,
      exchange: exchangeNormalized, attested_venue: exchangeNormalized, label,
      ...ciphertext fields }).select("id").single()`. `userId` is threaded from the
      `withAuth` session exactly as the legacy handler does — NEVER read from the body
      (TS-04/SC7 precedent at :182-185). Both venue columns come from the ONE
      `exchangeNormalized` binding: never two reads, never the raw body value. The scrub
      trigger admits service_role by name (migration 20260811210000:534) so the supplied
      attestation survives; the CHECK (:294) enforces the coupling as the DB backstop.
    - `label`: accept optional `body.label` string — trim, cap at 120 chars; default
      `` `${exchangeNormalized} key` `` when absent/empty. It becomes server-written text,
      hence the cap (ASVS V5).
    - Response (persist success): `NextResponse.json({ api_key_id, valid: true,
      read_only }, { headers: NO_STORE_HEADERS })` — NO ciphertext field of any name.
    - Persist-arm failures (including the INSERT itself failing): the file's coded envelope
      convention `{ error, code }` with curated copy — never raw Postgres/SQLSTATE text
      (Pitfall 3). Reuse the existing code union (`KEY_INVALID_FORMAT` for request-shape
      rejections, `UNKNOWN`-family for the insert fault) rather than minting new codes here.
    - Audit-coverage compliance: the new `.from("api_keys").insert` is a route mutation the
      audit-coverage gate scans — emit `logAuditEvent`/`logAuditEventAsUser` in the same
      function body per the repo convention, or place an `@audit-skip:` pragma with a
      reason within 8 lines above the mutation chain start; keep any new commentary ABOVE
      the pragma (Pitfall 4 window discipline).
    - Secrets: every new catch/log arm applies the `perRequestSecrets` +
      `scrubSeamError` + `captureToSentry({ secrets })` pattern (:395-409) — the body
      carries raw key material.

    Component (`ApiKeyManager.tsx`):
    - The connect fetch body gains `persist: true` and `label` (preserve the component's
      current label semantics).
    - Delete the client INSERT block (:250-261). The variable feeding the link update
      becomes the route response's `api_key_id`.
    - PRESERVE byte-for-byte: the strategies link update + its surfaced error
      (:269-277, NEW-C37-03), the background-sync HTTP-outcome observation below it
      (SEAMUX-05), and the DELETE handler at :352 (locked D-05: REVOKE is INSERT-only).
    <!-- planner-discipline-allow: from("api_keys").insert( -->

    Trust ceiling honesty (state in code comment at the persist arm, wording template at
    finalize-wizard/route.ts:1231-1238): any server route holding createAdminClient() can
    still pass any venue — "only our own server code can forge" is the ceiling; never
    claim "cannot be forged" (ADR-0001/0003).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/keys/validate-and-encrypt/route.test.ts src/components/strategy/ApiKeyManager.test.tsx src/__tests__/audit-coverage.test.ts --no-file-parallelism && grep -q 'attested_venue: exchangeNormalized' src/app/api/keys/validate-and-encrypt/route.ts && grep -q 'persist: true' src/components/strategy/ApiKeyManager.tsx && ! tr -d ' \n\t' < src/components/strategy/ApiKeyManager.tsx | grep -q 'from("api_keys").insert('</automated>
  </verify>
  <acceptance_criteria>
    - route.test.ts (new tests, mirroring the finalize-wizard STATE-capture idiom for the createAdminClient mock): (1) persist request with exchange "deribit" ⇒ captured insert payload has exchange === "deribit" AND attested_venue === "deribit" AND user_id === the withAuth TEST_USER id, never a body-supplied uid; (2) a normalization case (the sfox/mt5 detection path) ⇒ both columns carry the NORMALIZED venue, not the raw body string; (3) persist success response contains api_key_id and NO ciphertext-named field; (4) absent-persist body ⇒ the pre-existing envelope, unchanged assertions still green; (5) persist: "true" (string) ⇒ legacy arm, zero captured inserts
    - ApiKeyManager.test.tsx: the connect flow issues no api_keys insert through the supabase client mock; the strategies link update is called with the route-returned api_key_id; the DELETE path test remains green
    - The whitespace-collapsed negative grep confirms zero client-side api_keys insert chains remain in ApiKeyManager.tsx
    - src/__tests__/audit-coverage.test.ts passes with the new route mutation present (audit emission or pragma within the 8-line window)
    - npx tsc --noEmit is clean
  </acceptance_criteria>
  <done>One real user path (strategy-page key add) runs browser → route → validated venue → attested server-side INSERT → api_key_id → link update, committed with green scoped tests. The architecture is proven; plans 03/05 expand and retire.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden the persist arm — skew-window parity, error arms, secrets scrub, no-ciphertext invariant</name>
  <files>src/app/api/keys/validate-and-encrypt/route.ts, src/app/api/keys/validate-and-encrypt/route.test.ts</files>
  <read_first>
    - src/app/api/keys/validate-and-encrypt/route.ts (state after Task 1)
    - src/app/api/keys/validate-and-encrypt/route.test.ts (harness + Task 1 tests)
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-RESEARCH.md §Security Domain and §Common Pitfalls 2-3
  </read_first>
  <behavior>
    - Test 1: persist request hitting the rate limiter ⇒ same KEY_RATE_LIMIT coded envelope and headers as the legacy arm (limiter polices both arms)
    - Test 2: persist request whose upstream validation fails (invalid credentials) ⇒ coded envelope, zero captured inserts, no ciphertext field
    - Test 3: persist request whose admin INSERT rejects (simulated PG error with SQLSTATE text in message) ⇒ 5xx coded envelope whose error string does NOT contain the raw PG message; Sentry/console mocks show the scrubbed form only
    - Test 4: every persist-arm response shape (success + each error arm) asserted to lack any key matching /encrypt|cipher|secret/i
    - Test 5: legacy-arm regression sweep — the full pre-existing legacy test set passes unmodified (byte-parity of the absent-discriminator contract during the soak window)
  </behavior>
  <action>
    Close every gap the tracer left on the SINGLE path — no new call sites. Write the
    behavior tests first, observe the relevant ones RED where the arm is not yet complete,
    then finish the arm: rate-limiter parity, upstream-failure passthrough with zero
    inserts, INSERT-failure coded envelope with scrubbed logging (perRequestSecrets applied
    on the new catch arm), and the response-shape invariant (no ciphertext-named key on any
    persist-mode arm). Do not modify any legacy-arm code path in this task — Test 5 pins it.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/keys/validate-and-encrypt/route.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All five behavior tests exist and pass; Tests 2-4 were observed RED before the corresponding arm code landed (record the observation in the SUMMARY)
    - Zero modifications to legacy-arm code paths in this task's diff (git diff of the task commit shows persist-arm and test changes only)
    - The persist arm's catch arms all route through the scrub pattern — no bare console.error(err) with the raw error object
  </acceptance_criteria>
  <done>The persist arm is production-hardened: limiter parity, honest coded failures, scrubbed secrets, and a tested no-ciphertext invariant — ready for plan 03 to point the two remaining components at it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/keys/validate-and-encrypt | untrusted body: venue string, raw credentials, label, persist flag |
| route → PROD api_keys (service_role) | privileged write path — the trust ceiling |
| route → Python /validate-key, /encrypt-key | seam carrying raw key material |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-160-04 | Tampering | forged/mismatched venue at row creation (the RANK-03 threat) | critical | mitigate | both columns written from the single exchangeNormalized binding the SERVER validated; DB CHECK + scrub trigger as independent backstops; client value never reaches the INSERT |
| T-160-05 | Spoofing | body-supplied user_id minting a row for another tenant | critical | mitigate | user_id threaded from the withAuth session only (:182-185 precedent); test asserts a body uid is ignored |
| T-160-06 | Tampering | deploy-vs-revoke skew: stale tab + new route ⇒ double insert | high | mitigate | strict persist === true discriminator; legacy bodies produce ZERO server-side inserts (tested, incl. string-"true" probe); REVOKE later turns stale client inserts into honest 42501 |
| T-160-07 | Elevation | service-role write path abuse (any server route can forge) | high | accept | standing ADR-0001/0003 trust ceiling — stated honestly in the persist-arm comment; "only our own server code can forge", never claimed impossible; unchanged by this phase |
| T-160-08 | Information Disclosure | credentials/ciphertext in persist responses or logs | high | mitigate | persist mode returns NO ciphertext (tested invariant); perRequestSecrets scrub on every new catch/log arm; error copy curated, no raw PG text |
| T-160-09 | Denial of Service | persist arm bypassing the key-validation rate limiter | medium | mitigate | limiter polices both arms (Test 1); no new unthrottled entry point |
</threat_model>

<verification>
- Scoped suites green: validate-and-encrypt route tests, ApiKeyManager tests,
  audit-coverage gate; tsc clean.
- No migration file exists in this plan's diff (D-06 — PR-1 is TS-only).
- Legacy-arm contract byte-preserved (Test 5) — the soak window depends on it.
</verification>

<success_criteria>
- ROADMAP 160 SC-2 (first half): the writer exists — validate-and-encrypt persists the row
  with the server-validated venue in both columns and returns { api_key_id }; ApiKeyManager
  no longer inserts; stale tabs keep working. Plan 03 completes the client surface; plan 05
  performs the withdrawal.
</success_criteria>

<output>
Create `.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-02-SUMMARY.md`
when done, recording: the persist-arm contract as shipped (request/response field names),
the RED observations for the behavior tests, and confirmation that no migration file was
touched.
</output>
