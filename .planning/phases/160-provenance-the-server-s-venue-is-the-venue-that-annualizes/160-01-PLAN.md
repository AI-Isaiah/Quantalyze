---
phase: 160-provenance-the-server-s-venue-is-the-venue-that-annualizes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md
autonomous: false
requirements: [RANK-03, RANK-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "160-CENSUS.md exists as a committed artifact with real PROD numbers BEFORE the stamp swap (plan 160-04) or any writer/REVOKE code lands (D-01: the census is the phase's first task and gates everything downstream)"
    - "The census measures un-attested api_keys rows since 2026-08-11 split by exchange, strategy linkage (single AND composite via EXISTS, never JOIN fan-out), and wizard_session_id carriage (ROADMAP 160 SC-1)"
    - "The census documents the B-D1 threshold and applies it MECHANICALLY: the golden-parity candidate list is exactly the Q2 rows where stamped <> venue_derived; an empty list means plan 160-06 is a recorded no-op — no separate user gate (D-02)"
    - "The artifact carries hand-typed pinned counts in the 20260811210000 discipline, ready for the PR-2 migration guard to re-run and abort on drift (D-01/D-03)"
    - statement: "The census numbers were produced by read-only SQL against the PROD database (ref khslejtfbuezsmvmtsdn), never TEST (edge: measuring TEST would gate PROD money-math on the wrong population)"
      verification: backstop
  artifacts:
    - path: ".planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md"
      provides: "B-M1 census — un-attested population split (Q1/Q1b), golden-parity candidate list (Q2), B-D1 threshold + mechanical decision, pinned counts for the PR-2 guard"
      contains: "attested_venue IS NULL"
  key_links:
    - from: ".planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md"
      to: "160-04-PLAN.md and 160-05-PLAN.md"
      via: "wave barrier + preconditions — the stamp swap and the REVOKE guard constants may not proceed until this artifact carries real PROD results"
      pattern: "unattested"
  prohibitions:
    - statement: "The census artifact contains no emails, no auth uids, no user names, no key material, no credentials — counts, dates, and strategy/key ids ONLY (repo is PUBLIC and .planning/ is tracked; 159-CENSUS.md rule copied verbatim)"
      status: unverified
      flagged: true
      verification: "grep of 160-CENSUS.md finds no email-shaped token; no column named email/uid/user_name appears in any pasted result table"
    - statement: "The census SQL performs zero mutations — no INSERT/UPDATE/DELETE/ALTER/CREATE/DROP/TRUNCATE statement appears in any census query block"
      status: unverified
      flagged: true
      verification: "grep -inE '^\\s*(insert|update|delete|alter|create|drop|truncate) ' of the SQL blocks in 160-CENSUS.md returns nothing"
---

<objective>
Produce and commit the B-M1 PROD census artifact (`160-CENSUS.md`) — the phase's FIRST task,
which gates the RANK-04 stamp swap and mechanically decides B-D1 scope (specifically: whether
a golden-parity re-annualization population exists, and for which strategy ids). Copies the
count-pinned, abort-on-drift census discipline from migration `20260811210000` and the
artifact/orchestrator-execution pattern from `159-CENSUS.md`.

Decision ledger cited in this phase (planner-assigned IDs over 160-CONTEXT.md `<decisions>`,
which carries prose bullets without IDs — the section name is given for traceability):

- D-01 (§B-M1 census & B-D1 scope gating): census is a committed early artifact with pinned
  counts, discipline copied from 20260811210000 (count-pinned, abort-on-drift).
- D-02 (§B-M1 census & B-D1 scope gating): the census decides B-D1 scope mechanically;
  threshold documented in the artifact, no separate user gate.
- D-03 (§B-M1 census & B-D1 scope gating): at REVOKE time the migration guard re-runs the
  count-pinned census and aborts on drift.
- D-04 (§Server-authoritative INSERT rollout): writer = extend validate-and-encrypt to insert
  the api_keys row and return { api_key_id } — Phase-156 service-role-writer pattern.
- D-05 (§Server-authoritative INSERT rollout): client INSERT sites stop inserting; DELETE
  stays client-side — REVOKE INSERT only; grep every .from("api_keys") mutation before the
  REVOKE.
- D-06 (§Server-authoritative INSERT rollout): two PRs — writer deploy FIRST, soak (one
  deploy cycle + prod smoke of the wizard key-add flow), then the REVOKE migration; NEVER
  migration-first.
- D-07 (§asset_class stamp + B-D2 oracle): the finalize-wizard stamp derives from the
  attested venue and the swap lands in the SAME change as the null-attestation extension of
  skipAssetClassWrite — a NULL attestation SKIPS.
- D-08 (§asset_class stamp + B-D2 oracle): the oracle pins the ECONOMICS, never the
  implementation's own expression.
- D-09 (§asset_class stamp + B-D2 oracle): census-identified affected strategies get
  golden-parity re-annualization; no blanket backfill.

Purpose: ROADMAP 160 SC-1 — the census is measured and committed as an EARLY phase artifact;
it gates the stamp swap and decides B-D1 scope.
Output: `.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md`
with real PROD numbers, committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-RESEARCH.md
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CONTEXT.md
@.planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–06)

- `.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md` — NEW B-M1 census artifact (this plan); pre-REVOKE re-measure addendum appended by plan 05's checkpoint
- `src/app/api/keys/validate-and-encrypt/route.ts` — MODIFIED: NEW persist arm — request discriminator `persist: true` (strict boolean), response `{ api_key_id, valid, read_only }` with NO ciphertext, admin-client INSERT stamping `exchange` AND `attested_venue` from the single `exchangeNormalized` binding (plan 02); legacy ciphertext arm REMOVED and replaced by a coded `STALE_CLIENT` refusal (plan 05)
- `src/app/api/keys/validate-and-encrypt/route.test.ts` — persist-arm + skew-window tests (plan 02); legacy-arm-removal tests (plan 05)
- `src/components/strategy/ApiKeyManager.tsx` — client INSERT removed; consumes `api_key_id`; strategies link update and DELETE retained (plan 02)
- `src/components/strategy/ApiKeyManager.test.tsx` — extended (plan 02)
- `src/components/strategy/StrategyForm.tsx` + `StrategyForm.test.tsx` — client INSERT removed (plan 03)
- `src/components/exchanges/AllocatorExchangeManager.tsx` + `AllocatorExchangeManager.test.tsx` — client INSERT removed; row re-fetched by id via `API_KEY_USER_COLUMNS` (plan 03)
- `supabase/tests/test_api_keys_insert_not_client_writable.sql` — NEW SQL gate: service_role attestation-retention positive (A1, armable immediately) + state-adaptive authenticated-INSERT-denied negative keyed on the column-comment marker substring `revoke_api_keys_insert` (plan 03)
- `src/app/api/strategies/finalize-wizard/route.ts` — `skipAssetClassWrite` null-attestation extension + stamp branch input swap to the attested binding, in ONE change (plan 04)
- `src/app/api/strategies/finalize-wizard/route.test.ts` — B-D2 economics oracles + NEW harness fixture knob `adminApiKeysAttestedVenue` (plan 04)
- `supabase/migrations/<UTC-timestamp>_revoke_api_keys_insert.sql` — NEW PR-2-ONLY migration: census re-run guard + `REVOKE INSERT ON public.api_keys FROM anon, authenticated` + aborting post-verify + column-comment marker append (plan 05; slug `revoke_api_keys_insert` is pinned — it is the SQL gate's arming marker)
- `.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md` — NEW golden-parity re-annualization record, or the recorded no-op (plan 06)
- Explicit NON-changes (PATTERNS §Explicit Non-Changes): `src/app/api/strategies/create-with-key/route.ts` (draft stamp already server-derived at :1089), `ApiKeyManager.tsx:352` DELETE, RLS policies / scrub trigger / coupling CHECK, the Python analytics worker
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the 160-CENSUS.md scaffold with the exact read-only SQL and the B-D1 threshold</name>
  <files>.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md</files>
  <read_first>
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-RESEARCH.md §Code Examples (Q1/Q1b/Q2 SQL to copy VERBATIM) and §Common Pitfalls 9–10 (EXISTS-not-JOIN, report-vs-enforce pin split, no-PII rule)
    - .planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md (the artifact template: header shape, PROD-ref confirmation line, PII rule section, query+RESULTS structure)
    - supabase/migrations/20260811210000_api_keys_attested_venue.sql lines ~590-674 (the pin discipline being copied: hand-typed CONSTANTs, PROD-signature discriminator, the ":660-666 THE TOTAL IS REPORTED, NEVER ENFORCED" split)
  </read_first>
  <action>
    Create `160-CENSUS.md` (D-01 artifact) in the phase directory. Structure, copying
    159-CENSUS.md's shape:

    1. Header: artifact name (B-M1), authored date (scaffold) vs executed-against-PROD date
       (blank until Task 2), and the PROD project-ref confirmation line copied from
       159-CENSUS.md:6 — PROD ref `khslejtfbuezsmvmtsdn`, TEST ref `qmnijlgmdhviwzwfyzlc`
       named as NOT used.
    2. "Rule of this file: no PII, ever" section copied verbatim from 159-CENSUS.md:25-35 —
       counts, dates, and strategy/key ids ONLY; all queries read-only SELECTs; the
       orchestrator executes exactly the blocks on this page, nothing improvised.
    3. Section "Query 1 / Q1b — the un-attested population since the 20260811210000 cutoff":
       copy the Q1 and Q1b SQL from 160-RESEARCH.md §Code Examples byte-for-byte (split by
       exchange, linked_single, linked_composite, wizard_carriage — EXISTS filters, never
       JOIN), each followed by a RESULTS subsection containing the single marker line
       `RESULTS: PENDING`.
    4. Section "Query 2 — affected strategies (the golden-parity candidate list)": copy the
       Q2 SQL verbatim (stamped vs venue_derived per strategy, the CASE list mirroring
       CRYPTO_EXCHANGES = binance/okx/bybit/deribit/sfox from closed-sets.ts:553-559), plus
       `RESULTS: PENDING`.
    5. Section "The B-D1 threshold (mechanical — D-02)": state the documented threshold in
       one sentence pair: (a) the writer + REVOKE arc (B-1..B-3) proceeds REGARDLESS — it is
       locked in 160-CONTEXT.md, and the ongoing client-INSERT flow (which exists until the
       REVOKE) makes the full arc the only correct cut; (b) the census's mechanical decision
       is the golden-parity population: the re-annualization candidate list is EXACTLY the
       Q2 rows where `stamped <> venue_derived`; zero such rows means plan 160-06 records a
       no-op (D-09: per-strategy golden parity, never a blanket backfill). No user gate.
    6. Section "Pins for the PR-2 guard (D-03)": a table template to be filled at Task 2
       with the hand-typed values the REVOKE migration will re-pin: the un-attested count
       per Q1 total (the ENFORCED pin — the population whose drift changes the decision) and
       the Q1b pre-cutoff residual; a note copied from 20260811210000:660-666 discipline
       that TOTAL api_keys row count is REPORTED as a delta only, NEVER enforced (api_keys
       is live and user-mutable — pinning it is the latent-outage shape that migration
       explicitly walked back). On drift at REVOKE time: re-measure and re-cut the constants
       together, never soften the comparison.

    All SQL is read-only SELECTs. No project URL, credential, email, or uid anywhere in the
    file. Note assumption A4 from RESEARCH inline above Q1: if `strategy_keys.api_key_id`
    is not the linkage column, the query errors loudly at execution — fix the column name
    at the checkpoint from the live schema, never by guessing.
  </action>
  <verify>
    <automated>test -f .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md && grep -c 'RESULTS: PENDING' .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md | grep -qx 3 && grep -q 'attested_venue IS NULL' .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md && grep -q 'wizard_carriage' .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md && ! grep -inE '^\s*(insert|update|delete|alter|create|drop|truncate) ' .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md</automated>
  </verify>
  <acceptance_criteria>
    - The scaffold contains three SQL blocks (Q1, Q1b, Q2) and exactly three `RESULTS: PENDING` markers
    - Q1/Q1b/Q2 are byte-identical to 160-RESEARCH.md §Code Examples (diff of the extracted blocks against the research file's blocks is empty)
    - Linkage filters use EXISTS subqueries; the string "LEFT JOIN strategy" appears nowhere in the census SQL (Pitfall 9 fan-out)
    - The B-D1 threshold section states the mechanical rule (golden-parity list = Q2 rows where stamped <> venue_derived; empty list = recorded no-op) and that B-1..B-3 proceed regardless
    - The pins section carries the REPORTED-never-ENFORCED split for the total row count and names the un-attested count as the enforced pin
    - The PII rule paragraph and the PROD-ref confirmation line are present
  </acceptance_criteria>
  <done>A committed scaffold exists whose SQL is deterministic and research-derived — the checkpoint needs no SQL authorship, only execution and transcription.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: ORCHESTRATOR executes the census read-only against PROD, fills the B-D1 decision, and commits</name>
  <files>.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md</files>
  <read_first>
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md (the scaffold from Task 1 — every SQL block to run is on disk)
  </read_first>
  <action>
    STOP and return `## CHECKPOINT REACHED`. This task is executed by the ORCHESTRATOR
    session, not this executor: Supabase MCP tools are stripped from subagents; only the
    orchestrator holds read-only SQL access to PROD. Follow the instructions block; resume
    only on the resume signal.
  </action>
  <instructions>
    Orchestrator protocol:
    1. Confirm the Supabase MCP target is the PROD project (ref khslejtfbuezsmvmtsdn — NEVER
       the TEST ref qmnijlgmdhviwzwfyzlc) against the project list before running anything;
       record the confirmed ref and date in the census header. If only TEST is reachable,
       HALT and surface — never substitute TEST numbers.
    2. Execute Q1, Q1b, and Q2 read-only, in order. If Q1's linked_composite arm errors on a
       column name (assumption A4), read the strategy_keys schema, fix the column reference
       in the artifact (recording the correction), and re-run.
    3. Replace each `RESULTS: PENDING` marker with the actual markdown result table.
       Counts, dates, and strategy/key ids only — strip anything email- or uid-shaped
       before pasting (repo is PUBLIC).
       <!-- planner-region-allow: RESULTS: PENDING -->
       (Task 1 deliberately authors the three PENDING markers this task replaces — the
       negative gate below is the intended sequential transform, not a conflict.)
    4. Fill the "Pins for the PR-2 guard" table with hand-typed values from Q1/Q1b.
    5. Apply the B-D1 threshold mechanically and write the decision sentence: either the
       explicit golden-parity candidate list (strategy ids from Q2 where stamped <>
       venue_derived) or "zero candidates — plan 160-06 is a recorded no-op".
    6. Commit 160-CENSUS.md.
  </instructions>
  <resume-signal>160-CENSUS.md contains zero `RESULTS: PENDING` markers, the pins table and B-D1 decision are filled, and the file is committed. Only then may plans 160-02 and 160-04 begin — this artifact gates everything downstream (D-01/D-02).</resume-signal>
  <verification>
    - All three RESULTS sections carry real PROD tables (visual check of the committed file)
    - The confirmed PROD ref appears in the header; no TEST-derived number was substituted
    - The B-D1 decision names concrete strategy ids or states "zero candidates" — never left blank
  </verification>
  <verify>
    <automated>! grep -q 'RESULTS: PENDING' .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md && git log --oneline -1 -- .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - All three RESULTS sections carry real PROD result tables; zero PENDING markers remain
    - The pins table carries hand-typed counts (un-attested = enforced pin; total = reported delta only)
    - The B-D1 decision is written mechanically per the documented threshold — candidate ids listed, or "zero candidates" recorded
    - No email/uid-shaped token appears in any pasted table
    - The file is committed (git log shows a commit touching it)
  </acceptance_criteria>
  <done>The B-M1 census exists with real PROD numbers, a filled pins table, and a mechanical B-D1 decision, committed BEFORE any downstream plan runs — the D-01 ordering gate is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator MCP → PROD DB | read-only SQL against production data |
| .planning census artifact → public repo | results are world-readable on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-160-01 | Information Disclosure | 160-CENSUS.md in a PUBLIC repo | high | mitigate | PII rule encoded in the scaffold header + checkpoint step 3 strips email/uid-shaped values + acceptance criterion; ids/counts/dates only |
| T-160-02 | Tampering | census SQL mutating PROD | critical | mitigate | scaffold contains only SELECTs (negative mutation-grep in Task 1's automated gate); orchestrator executes only the on-disk blocks per the checkpoint protocol |
| T-160-03 | Repudiation | downstream code lands without the census evidence (stamp swap or REVOKE ungated) | high | mitigate | wave barrier: plans 160-02/160-04 carry preconditions grepping this artifact for completed results; plan 160-05's guard re-pins from this file (D-03) |
</threat_model>

<verification>
- Task 1 gate: scaffold structure + verbatim-SQL + no-mutation greps.
- Task 2 gate: zero PENDING markers, pins filled, committed, PII clean.
- Phase-level: 160-02 and 160-04 must observe this artifact complete before their first
  edit (D-01 ordering: census committed BEFORE the writer or the swap exists on the branch).
</verification>

<success_criteria>
- ROADMAP 160 SC-1 holds: the B-M1 census (un-attested rows since 2026-08-11, split by
  exchange, strategy linkage, wizard_session_id carriage) is measured and committed as an
  EARLY phase artifact with the 20260811210000 pin discipline; the B-D1 decision is made
  mechanically inside the artifact; no PII.
</success_criteria>

<output>
Create `.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-01-SUMMARY.md`
when done, recording: the confirmed PROD ref, the headline counts (un-attested total,
per-exchange split, wizard carriage), and the B-D1 outcome (candidate ids or recorded no-op).
</output>
