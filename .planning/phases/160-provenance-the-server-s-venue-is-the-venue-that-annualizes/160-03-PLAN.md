---
phase: 160-provenance-the-server-s-venue-is-the-venue-that-annualizes
plan: 03
type: execute
wave: 3
depends_on: ["160-02"]
files_modified:
  - src/components/strategy/StrategyForm.tsx
  - src/components/strategy/StrategyForm.test.tsx
  - src/components/exchanges/AllocatorExchangeManager.tsx
  - src/components/exchanges/AllocatorExchangeManager.test.tsx
  - supabase/tests/test_api_keys_insert_not_client_writable.sql
autonomous: true
requirements: [RANK-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ALL THREE client INSERT sites are converted before any REVOKE can land: StrategyForm and AllocatorExchangeManager (this plan) join ApiKeyManager (plan 02) in consuming the persist arm — the third site RESEARCH found (AllocatorExchangeManager.tsx:591) is not left behind (Pitfall 1; D-05)"
    - "AllocatorExchangeManager still renders the connected key optimistically: it re-fetches the row by the returned api_key_id via the API_KEY_USER_COLUMNS projection the migration-027 SELECT allowlist covers"
    - "A CI SQL gate proves a service_role INSERT supplying attested_venue RETAINS it (assumption A1 discharged as an enforced fact, not an assumption)"
    - "The SQL gate's authenticated-INSERT-denied negative is state-adaptive: it SKIPs loudly on a pre-REVOKE database and ARMs when the api_keys.exchange column comment carries the marker substring the PR-2 migration stamps (Pitfall 11 — no sql-tests red on open PRs)"
    - "The gate's positive controls run UNCONDITIONALLY: owner can still SELECT own rows and DELETE own rows — the DELETE-retained canary that must stay green after the REVOKE (D-05)"
  artifacts:
    - path: "src/components/strategy/StrategyForm.tsx"
      provides: "converted connect flow — persist request with label, no client insert"
      contains: "persist: true"
    - path: "src/components/exchanges/AllocatorExchangeManager.tsx"
      provides: "converted allocator connect flow — persist request, row re-fetch by api_key_id"
      contains: "persist: true"
    - path: "supabase/tests/test_api_keys_insert_not_client_writable.sql"
      provides: "A1 retention positive (unconditional) + state-adaptive authenticated-INSERT-denied negative + owner SELECT/DELETE canaries"
      contains: "revoke_api_keys_insert"
  key_links:
    - from: "src/components/exchanges/AllocatorExchangeManager.tsx"
      to: "src/app/api/keys/validate-and-encrypt/route.ts"
      via: "POST with persist: true, then .from('api_keys').select(API_KEY_USER_COLUMNS).eq('id', api_key_id).single() re-fetch for optimistic render"
      pattern: "API_KEY_USER_COLUMNS"
    - from: "supabase/tests/test_api_keys_insert_not_client_writable.sql"
      to: "supabase/migrations/<UTC-timestamp>_revoke_api_keys_insert.sql (plan 05)"
      via: "arming marker — col_description of api_keys.exchange LIKE '%revoke_api_keys_insert%' flips the negative from SKIP to ARMED"
      pattern: "col_description"
  prohibitions:
    - statement: "Neither converted component retains any code path that INSERTs into api_keys from the browser — no fallback arm, no feature-flagged old path"
      status: unverified
      flagged: true
      verification: "whitespace-collapsed grep of both components finds no api_keys insert chain; component tests assert the supabase mock receives no api_keys insert"
    - statement: "The SQL gate never weakens the sibling gate's coverage — test_api_keys_exchange_not_user_writable.sql is not modified by this plan"
      status: unverified
      flagged: true
      verification: "git diff of this plan's commits shows no change to the sibling SQL gate file"
---

<objective>
Expand the proven tracer to the remaining two client INSERT sites (D-05, Pitfall 1 — the
third site `AllocatorExchangeManager.tsx:591` that 160-CONTEXT.md missed and RESEARCH
verified at HEAD), and land the CI SQL gate that (a) discharges assumption A1 (service_role
INSERT retains its attestation) immediately, and (b) pre-authors the post-REVOKE assertions
state-adaptively so `sql-tests` stays green until PR-2 applies.

Still PR-1 material ONLY: no migration file (D-06). `supabase/tests/*.sql` files do not
auto-apply — they run in CI's `sql-tests` job.

Purpose: ROADMAP 160 SC-2 — after this plan, zero browser components INSERT into api_keys,
which is the precondition the plan-05 REVOKE greps for.
Output: two converted components with tests, one new state-adaptive SQL gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PATTERNS.md
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-02-SUMMARY.md
@src/components/strategy/StrategyForm.tsx
@src/components/exchanges/AllocatorExchangeManager.tsx
@supabase/tests/test_api_keys_exchange_not_user_writable.sql
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–06)

See 160-01-PLAN.md for the canonical phase-wide list. This plan produces:
- `src/components/strategy/StrategyForm.tsx` + test — insert removed, persist request with default label
- `src/components/exchanges/AllocatorExchangeManager.tsx` + test — insert removed, persist request + row re-fetch by `api_key_id` using `API_KEY_USER_COLUMNS`
- `supabase/tests/test_api_keys_insert_not_client_writable.sql` — NEW SQL gate; arming marker substring pinned as `revoke_api_keys_insert`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert StrategyForm — stop inserting, send the persist body</name>
  <files>src/components/strategy/StrategyForm.tsx, src/components/strategy/StrategyForm.test.tsx</files>
  <read_first>
    - src/components/strategy/StrategyForm.tsx:80-190 (the fetch→insert flow at :140 being converted; the H-0405 error-redaction arm that must survive; the current default label expression)
    - src/components/strategy/StrategyForm.test.tsx (existing spec to extend — do not create a parallel file)
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-02-SUMMARY.md (the persist contract as actually shipped)
  </read_first>
  <behavior>
    - Test 1: the connect flow POSTs the persist body (persist: true + the component's default label) and issues NO api_keys insert through the supabase mock
    - Test 2: a route error response surfaces through the component's EXISTING redacted error arm (H-0405) — no raw message leak regression
  </behavior>
  <action>
    Convert per the plan-02 pattern (D-05): the validate-and-encrypt fetch body gains
    `persist: true` and `label` using the component's existing default (the
    exchange-canonical "key" template currently built at the insert site); delete the
    client INSERT block at :140 (no id is captured there today, so nothing downstream
    consumes one — verify by reading before deleting). Preserve the H-0405 error
    redaction arm and every non-insert behavior byte-for-byte. Match the component's
    existing error-handling style; do not restructure unrelated state.
    <!-- planner-discipline-allow: from("api_keys").insert( -->
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/StrategyForm.test.tsx --no-file-parallelism && grep -q 'persist: true' src/components/strategy/StrategyForm.tsx && ! tr -d ' \n\t' < src/components/strategy/StrategyForm.tsx | grep -q 'from("api_keys").insert('</automated>
  </verify>
  <acceptance_criteria>
    - Both behavior tests exist and pass; Test 1 observed RED against the unconverted component before the edit (record in SUMMARY)
    - The whitespace-collapsed negative grep confirms zero api_keys insert chains remain
    - The H-0405 redaction test (existing) still passes unmodified
  </acceptance_criteria>
  <done>StrategyForm's key connect runs entirely through the server writer; the second of three client INSERT sites is retired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert AllocatorExchangeManager — stop inserting, re-fetch the row by id</name>
  <files>src/components/exchanges/AllocatorExchangeManager.tsx, src/components/exchanges/AllocatorExchangeManager.test.tsx</files>
  <read_first>
    - src/components/exchanges/AllocatorExchangeManager.tsx:560-620 (the insert at :590-606 selecting API_KEY_USER_COLUMNS back; the optimistic render / pending_insert stamping at :613-619; the setFormError-style error handling to match)
    - src/components/exchanges/AllocatorExchangeManager.test.tsx (existing spec to extend)
    - src/lib/constants.ts:170-171 (API_KEY_USER_COLUMNS — every column the re-fetch needs is on the migration-027 SELECT allowlist)
  </read_first>
  <behavior>
    - Test 1: the connect flow POSTs the persist body, issues NO api_keys insert, and re-fetches the row via select(API_KEY_USER_COLUMNS) filtered to the returned api_key_id
    - Test 2: the optimistic render receives the re-fetched row (same fields the old insert-returning path provided) — the pending_insert UI stamping behavior is unchanged
    - Test 3: a failed re-fetch surfaces through setFormError with curated copy, not a thrown raw error
  </behavior>
  <action>
    Convert the THIRD insert site (Pitfall 1 — missing this one kills the allocator
    connect flow at the PR-2 merge). The fetch body gains `persist: true` and the
    component's label semantics. Delete the insert block at :590-606. Because the UI needs
    the full row for optimistic render, re-fetch by id with the user-scoped client:
    `.from("api_keys").select(API_KEY_USER_COLUMNS).eq("id", api_key_id).single()` —
    the planner's resolution of RESEARCH Open Question 2 (the route's locked response
    stays minimal `{ api_key_id, valid, read_only }`; the SELECT allowlist covers the
    re-fetch). Keep the component's setFormError error style; keep the pending_insert
    stamping logic fed by the re-fetched row.
    <!-- planner-discipline-allow: from("api_keys").insert( -->
  </action>
  <verify>
    <automated>npx vitest run src/components/exchanges/AllocatorExchangeManager.test.tsx --no-file-parallelism && grep -q 'persist: true' src/components/exchanges/AllocatorExchangeManager.tsx && ! tr -d ' \n\t' < src/components/exchanges/AllocatorExchangeManager.tsx | grep -q 'from("api_keys").insert('</automated>
  </verify>
  <acceptance_criteria>
    - All three behavior tests exist and pass; Test 1 observed RED pre-conversion (record in SUMMARY)
    - The whitespace-collapsed negative grep confirms zero api_keys insert chains remain
    - The re-fetch uses exactly the API_KEY_USER_COLUMNS constant (import, not a re-typed column list)
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>All three client INSERT sites are converted; the browser's only remaining api_keys verbs are SELECT and DELETE — exactly what survives the REVOKE.</done>
</task>

<task type="auto">
  <name>Task 3: Author the state-adaptive SQL gate — A1 retention positive + post-REVOKE negatives</name>
  <files>supabase/tests/test_api_keys_insert_not_client_writable.sql</files>
  <read_first>
    - supabase/tests/test_api_keys_exchange_not_user_writable.sql (FULL file — the template: header essay :1-129, fixture seeding + JWT forgery + SET LOCAL ROLE idiom :172-183, unconditional positives :192-201 and :270-284 including the owner-DELETE assertion 4 whose wording this gate reuses, the column-comment marker gate :203-217, the 5d three-part refusal idiom :510-559, the 5b privileged-write read-back :409-423)
    - supabase/migrations/20260811210000_api_keys_attested_venue.sql:520-545 (the scrub trigger allowlist the A1 positive exercises) and :290-300 (the coupling CHECK — already asserted by the sibling gate's 5e; cite, don't duplicate)
  </read_first>
  <action>
    Create `supabase/tests/test_api_keys_insert_not_client_writable.sql` modeled wholesale
    on the sibling gate (same table, same discipline, one verb over). Whole-file
    `BEGIN; ... ROLLBACK;`, `psql -v ON_ERROR_STOP=1` header note, CI auto-discovery note.

    Contents, in order:
    1. Header essay: what the gate asserts, why INSERT (RANK-03), the two-landing
       deploy-first/revoke-second design (D-06), and the SKIP-vs-ARMED state model.
    2. Fixtures: auth.users + profiles + api_keys rows via gen_random_uuid(), following the
       sibling's seeding block.
    3. UNCONDITIONAL positive controls (anti-vacuity, run in every state): owner can SELECT
       own row through the column allowlist; owner can DELETE own row — reuse the sibling's
       assertion-4 wording; this is the canary that the REVOKE stayed INSERT-only (D-05).
    4. A1 retention positive (UNCONDITIONAL — armable immediately, discharges RESEARCH
       assumption A1): as a privileged role, INSERT an api_keys row supplying
       attested_venue = exchange, read it back, RAISE EXCEPTION if attested_venue came back
       NULL (i.e. the trigger scrubbed a writer it should admit). Model on the sibling's 5b
       privileged-write read-back. Do NOT re-assert the coupling CHECK (sibling 5e owns it —
       cite the sibling file in a comment instead).
    5. STATE-ADAPTIVE negative (Pitfall 11): detect the REVOKE via the sibling's
       col_description idiom on public.api_keys.exchange, testing
       `LIKE '%revoke_api_keys_insert%'` — the marker substring the plan-05 migration is
       contractually bound to append (slug-based so the gate need not know the timestamp).
       Pre-REVOKE: loud `RAISE NOTICE 'SKIP ...'` naming what is not yet armed. Armed: as
       forged-JWT `authenticated`, attempt an INSERT and assert the refusal TWO ways per the
       sibling's 5d idiom — SQLSTATE 42501 exactly, AND row count unchanged before/after.
    6. Post-verify block (armed state only): `has_table_privilege('authenticated',
       'public.api_keys','INSERT')` is false while `'DELETE'` remains true.
  </action>
  <verify>
    <automated>test -f supabase/tests/test_api_keys_insert_not_client_writable.sql && grep -q 'revoke_api_keys_insert' supabase/tests/test_api_keys_insert_not_client_writable.sql && grep -q '42501' supabase/tests/test_api_keys_insert_not_client_writable.sql && grep -q 'has_table_privilege' supabase/tests/test_api_keys_insert_not_client_writable.sql && grep -qi 'ROLLBACK' supabase/tests/test_api_keys_insert_not_client_writable.sql && grep -ci 'RAISE NOTICE' supabase/tests/test_api_keys_insert_not_client_writable.sql | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - The file follows the sibling's structure: header essay, BEGIN/ROLLBACK envelope, fixtures, unconditional positives, A1 positive, state-adaptive negative, armed-only post-verify
    - The A1 positive runs unconditionally (not inside the marker gate) — it is true today and must be enforced today
    - The negative arms ONLY on the col_description marker; the SKIP branch is a loud NOTICE, never silent
    - The owner-DELETE positive is unconditional (green pre- AND post-REVOKE)
    - VERIFICATION note recorded in the SUMMARY: nothing in the armed set is observed armed-and-green until the REVOKE migration reaches TEST — the Phase-156 5d/5f/5g/5h precedent, stated honestly
  </acceptance_criteria>
  <done>CI enforces A1 from this commit forward, and the post-REVOKE door assertions exist on disk, armed by the exact marker the PR-2 migration must stamp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/keys/validate-and-encrypt | untrusted body on two more surfaces (StrategyForm, Allocator) |
| browser (authenticated role) → api_keys | the verb surface being withdrawn; SELECT/DELETE remain |
| CI sql-tests → TEST DB | the gate's execution environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-160-10 | Tampering | forged venue via the two remaining client INSERT paths | critical | mitigate | both components stop inserting; the server writer stamps both columns; the state-adaptive gate proves the door closes at REVOKE |
| T-160-11 | Tampering | silent attestation scrub of the new writer (A1 wrong ⇒ unattested rows minted silently) | high | mitigate | A1 retention positive runs unconditionally in CI from this plan onward |
| T-160-12 | Denial of Service | the REVOKE killing the allocator connect flow (third site unconverted) | high | mitigate | Task 2 converts it; plan 05's pre-REVOKE re-grep is the second net (Pitfall 1) |
| T-160-13 | Denial of Service | sql-tests red on every open PR from a prematurely-armed negative | medium | mitigate | state-adaptive marker gate with loud SKIP (Pitfall 11, Phase-156 shape) |
</threat_model>

<verification>
- Both component suites green; whitespace-collapsed negative greps clean on both files.
- SQL gate present with marker, refusal SQLSTATE, privilege post-verify, ROLLBACK envelope.
- No migration file in this plan's diff (D-06 — still PR-1).
</verification>

<success_criteria>
- ROADMAP 160 SC-2 (client half complete): all three INSERT sites converted, A1 CI-enforced,
  post-REVOKE assertions pre-authored and correctly dormant.
</success_criteria>

<output>
Create `.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-03-SUMMARY.md`
when done, recording: RED observations for the conversion tests, the SKIP-state honesty note
for the SQL gate, and the pinned arming marker substring.
</output>
