---
phase: 160-provenance-the-server-s-venue-is-the-venue-that-annualizes
plan: 07
type: execute
wave: 6
depends_on: ["160-05"]
files_modified:
  - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md
  - TODOS.md
autonomous: false
gap_closure: true
requirements: [RANK-03]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The adjudication of the failed STALE_CLIENT must_have in 160-VERIFICATION.md is authored by a FRESH, independent gsd-verifier pass measuring at branch HEAD 2fe28b89 or later — never by the session that authored the fix — and the frontmatter score/status/gaps match that verifier's own measurements, including an explicit ruling on the now-retroactively-unmeetable 'deployed in the same merge as the migration' ordering clause (the migration merged in PR #704 WITHOUT the retirement; the retirement is a later landing)"
    - "The persist arm has been exercised at least once on PROD by a real key connect through a surface whose DevTools Network trace shows the POST to /api/keys/validate-and-encrypt: the minted api_keys row carries non-NULL attested_venue equal to exchange, and the observed response body carried api_key_id and none of the five ciphertext key names"
    - "The STALE_CLIENT refusal is measured on PROD after the closure branch deploys (authed legacy-shaped POST answers 409 with code STALE_CLIENT and no ciphertext key) — or it is held open in 160-VERIFICATION.md as an explicit, dated pending-deploy gate with the exact command to run; never silently passed"
    - "Every smoke flow NOT performed remains explicitly listed as behavior-unverified in 160-VERIFICATION.md — closure never widens beyond what was observed"
  artifacts:
    - path: ".planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md"
      provides: "independent re-verification stamp (re_verified frontmatter key naming the measured HEAD) + dated PROD smoke record transcribed from the checkpoint evidence"
      contains: "PROD smoke record"
  key_links:
    - from: ".planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md"
      to: "src/app/api/keys/validate-and-encrypt/route.ts"
      via: "Task-1 independent verifier re-measures the route/tests at HEAD and re-cuts the frontmatter from its OWN observations"
      pattern: "re_verified"
    - from: ".planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md"
      to: "PROD api_keys table"
      via: "Task-3 transcription of the Task-2 checkpoint evidence (counts/dates/ids only — repo is PUBLIC)"
      pattern: "PROD smoke record"
  prohibitions:
    - statement: "No one re-scores the failed must_have in 160-VERIFICATION.md except the independent Task-1 verifier — the fix was authored by the orchestrator session and self-certification is void"
      status: unverified
      flagged: true
      verification: "the commit that changes the gaps/score frontmatter is the verifier-output commit from Task 1, not an orchestrator hand-edit"
    - statement: "No PII, credentials, key labels carrying account hints, or emails enter any .planning artifact or TODOS.md — repo is PUBLIC and .planning/ is tracked; counts, dates, and ids only"
      status: unverified
      flagged: true
      verification: "email-shaped-token grep of the touched artifacts returns 0 hits; the smoke record names venues and row ids, never key material"
---

<objective>
Close out Phase 160's verification honestly. 160-VERIFICATION.md (written at `36e783de`)
records one FAILED must_have — the legacy ciphertext arm / STALE_CLIENT retirement — that
has since been implemented at `2fe28b89`, and one behavior_unverified item — the persist
arm has never handled a real PROD connect. This plan (1) gets the failed must_have
re-adjudicated by an INDEPENDENT verifier at HEAD (the fix author must not self-certify),
(2) scripts the human PROD smoke that is the writer's first true exercise, and (3) records
both outcomes without over-claiming.

⚠️ Deploy honesty, load-bearing for both tasks: HEAD `2fe28b89` is branch-only
(`chore/close-phase-160`). PROD today runs the post-PR-2 state — the persist arm IS live
(PR-1, deployment READY 2026-08-23 17:12 UTC), but the legacy arm STILL RETURNS CIPHERTEXT
on PROD until this branch lands. "Code-complete at HEAD" and "closed on PROD" are
different claims; this plan keeps them separate.

Purpose: the phase record must match reality at HEAD, and the ONLY remaining writer of
api_keys on the non-wizard surfaces must be observed working in production before the
phase closes over it.
Output: re-verified 160-VERIFICATION.md + dated PROD smoke record + TODOS.md hygiene.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-05-SUMMARY.md
@src/app/api/keys/validate-and-encrypt/route.ts
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: INDEPENDENT RE-VERIFICATION — a fresh gsd-verifier re-adjudicates the STALE_CLIENT must_have at HEAD</name>
  <files>.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md</files>
  <precondition>Branch `chore/close-phase-160` is at `2fe28b89` or a descendant — the retirement commit is in the tree being verified.</precondition>
  <action>
    STOP and return `## CHECKPOINT REACHED`. This gate is orchestrator work: spawning a
    verifier is a GSD-skill invocation (subagents cannot do it, and hand-spawning
    `Agent(gsd-*)` skips the orchestrator-only gates). Resume only on the resume signal.
  </action>
  <instructions>
    Orchestrator protocol:
    1. Spawn a FRESH, independent verifier via the GSD skill (`Skill(gsd-verify-work)` /
       the standard verify flow for Phase 160) — ⛔ never `Agent(gsd-*)` by hand, never a
       resumed prior verifier. The session that authored `2fe28b89` must not adjudicate it.
    2. Scope the pass as a RE-verification at current branch HEAD, covering:
       a. The failed must_have (route serves no ciphertext to any caller; coded
          STALE_CLIENT refusal for absent-discriminator bodies) and its artifact pattern
          (`contains: STALE_CLIENT` in route.ts).
       b. Re-trace every plan 160-02 and 160-05 truth whose recorded evidence cited
          `src/app/api/keys/validate-and-encrypt/route.ts` or `route.test.ts` coordinates —
          `2fe28b89` rewrote both files (route.ts −95/+79; a 139-line test diff), so the
          old line-anchored evidence is stale by construction. In particular: the WR-01
          spread-first/provenance-last insert ordering; the persist response
          `{ api_key_id, valid, read_only }` with no ciphertext key; the no-ciphertext
          prohibitions; the skew-window truths in their final refusal form (absent
          discriminator ⇒ zero inserts, zero live venue calls); the scrubbed catch arms;
          and `tests/integration/process-key-thin-adapters.test.ts`'s API-2 lock case.
       c. The founder anti-vacuity rule: the orchestrator's three recorded neuter cycles
          (ciphertext key injected into the refusal envelope; strict boolean relaxed to
          falsy; gate disabled) are dated CLAIMS to re-check — re-run any the verifier does
          not accept on the record.
       d. Deploy honesty (see objective): the truth's "deployed in the same merge as the
          migration" clause is retroactively unmeetable — PR #704 merged without the
          retirement. The verifier must RULE on that clause explicitly (e.g. substance
          verified at HEAD, ordering clause noted as superseded by a later landing plus the
          Task-2 Part-B PROD gate) — never silently drop it, never mark the full truth
          verified without addressing it.
    3. The VERIFIER — not the orchestrator — re-cuts 160-VERIFICATION.md: the gaps entry,
       score, status, and a new frontmatter key `re_verified:` carrying the ISO date and
       the git HEAD it measured. Whatever the verifier measures is what gets recorded,
       including any NEW gap it finds. The orchestrator commits the verifier's output
       unedited.
  </instructions>
  <resume-signal>160-VERIFICATION.md re-cut by the independent verifier with `re_verified:` naming the measured HEAD, frontmatter matching the verifier's own verdicts, committed unedited.</resume-signal>
  <verification>
    - A fresh verifier session was spawned via the GSD skill and measured at branch HEAD (2fe28b89 or a descendant)
    - 160-VERIFICATION.md's gaps entry, score, and status changed only in the verifier-output commit
    - The `re_verified:` frontmatter key names the ISO date and the git HEAD the verifier measured
    - The ordering clause ("same merge as the migration") carries an explicit ruling in the body
  </verification>
  <verify>
    <automated>grep -q '^re_verified:' .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md && git log --oneline -1 -- .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - The verifier pass is fresh and independent (spawned via the GSD skill, not the fix-authoring session, not a resumed agent)
    - The frontmatter score/status/gaps are the verifier's measurements, with the ordering clause explicitly ruled on
    - The behavior_unverified item (PROD smoke) survives in the record until Task 2/3 close it with evidence — the re-verification must not absorb it
  </acceptance_criteria>
  <done>The phase record's verdict on the retirement is owned by an independent measurement at HEAD, not by the author of the fix.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: PROD SMOKE — a real key connect exercises the persist arm; post-deploy, the refusal is measured too</name>
  <files>.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md</files>
  <precondition>PR-1's Vercel deployment is live on PROD (established in the 160-05 soak record) and a real, revocable READ-ONLY API credential for one supported venue is available to the human.</precondition>
  <action>
    STOP and return `## CHECKPOINT REACHED`. This is the human item 160-VERIFICATION.md
    routed to a person: it needs real exchange credentials on the production site, and the
    row check needs orchestrator read-only DB access. It must not be faked, skipped, or
    closed on the absence of evidence — the 47-minute soak already measured absence and
    that is exactly what was insufficient.
  </action>
  <what-built>
    Plans 160-02/03/05 made the `validate-and-encrypt` persist arm the only writer of
    `api_keys` for the three non-wizard connect surfaces (ApiKeyManager on the strategy
    edit page, StrategyForm, AllocatorExchangeManager) and withdrew the browser's INSERT
    verb on PROD. The closure commit `2fe28b89` (branch-only) retires the legacy
    ciphertext arm behind a 409 STALE_CLIENT refusal. What has NEVER happened: a real key
    connected on PROD through the persist arm — zero keys were connected during the
    47-minute soak, so the arm's first true production exercise is still owed.
  </what-built>
  <how-to-verify>
    Follow the instructions below: Part A connects one real read-only key on the
    production site with DevTools open, asserts the observed response (api_key_id, no
    ciphertext key names) and the minted row (attested_venue = exchange, non-NULL,
    count +1); Part B, after the closure branch deploys, sends one authed legacy-shaped
    POST and asserts the 409 STALE_CLIENT envelope.
  </how-to-verify>
  <instructions>
    ### Part A — the persist arm's first real exercise (runnable NOW; PR-1 is live)

    1. Have a real READ-ONLY API key + secret for one supported venue (binance / okx /
       bybit / deribit). ⚠️ Prefer a ccxt venue over mt5 for this first smoke — the known
       MT5 gateway wedge (-10005 IPC timeout) would conflate a terminal fault with an arm
       fault. Log in on the production site with your own account.
    2. Open DevTools → Network, filter: `validate-and-encrypt`. Keep it open for the whole
       flow.
    3. Minimum qualifying flow — a surface that POSTs this route by construction: open a
       strategy you own → its edit page → the API-key section (ApiKeyManager) → choose the
       venue, paste key + secret (+ passphrase if the venue needs one) → submit.
       ⚠️ Ground truth for "this smoke counts" is the POST to
       `/api/keys/validate-and-encrypt` appearing in the Network trace — NOT which page you
       stood on. The wizard-proper connect rides the Phase-156 server-side RPC path and
       does not answer for this arm (160-05 Pitfall-5); 160-VERIFICATION.md's "at minimum
       the wizard flow" wording is imprecise on this point and this script supersedes it.
    4. Assert on the observed response: HTTP 200; body carries `api_key_id` (a uuid),
       `valid: true`, `read_only: true`; and the body's key NAMES include none of:
       `api_key_encrypted`, `api_secret_encrypted`, `passphrase_encrypted`,
       `dek_encrypted`, `nonce`.
    5. Assert in the UI: the key shows as connected and the strategy's key link updated
       (no link-error toast — the client-side `strategies.api_key_id` update ran).
    6. Orchestrator row check, read-only against PROD (confirm the project ref against the
       project list first — never TEST): the newest `api_keys` row has non-NULL
       `attested_venue` equal to `exchange` equal to the venue just connected, and the
       total row count moved +1 against the post-REVOKE baseline (31 at the 160-05
       post-apply measurement).
    7. Optional but RECORDED either way: repeat through StrategyForm (`/strategies/new`)
       and AllocatorExchangeManager (profile → exchanges tab). Any surface NOT smoked
       stays listed by name as behavior-unverified in the record — it does not inherit
       Part A's pass.
    8. Cleanup: transcribe the row id and counts into the checkpoint record FIRST, then
       delete the smoke key via the UI if it should not persist (client DELETE is a live
       path — exercising it is itself smoke), and re-check the count.

    ### Part B — the refusal, measured on PROD (gated on this branch landing)

    9. After the closure branch merges and its Vercel production deployment reads READY
       (a merge means a deploy): with an authed session (the established passwordless
       magic-link → setSession → curl pattern), POST `/api/keys/validate-and-encrypt`
       with a legacy-shaped body (venue + key fields, no discriminator). Expect: HTTP 409,
       body `code` = `STALE_CLIENT`, copy naming a reload, and none of the five ciphertext
       key names from step 4.
       If the phase record must close before the branch lands, Part B is written into
       160-VERIFICATION.md as an explicit, dated pending-deploy gate carrying this exact
       step — a founder-recorded deferral, never a silent pass.

    ### Failure arm — do not soften

    10. If Part A fails (the connect errors, the row is missing, attested_venue is NULL,
        or ciphertext appears), STOP: after the REVOKE this arm is the only door into
        `api_keys` for the non-wizard surfaces, so a production-only fault here breaks
        connect-a-key for every tenant — squarely inside the founder stopping rule.
        Route to a root-cause investigation; the phase stays open. Absence of a successful
        connect is never recorded as a pass.

    PII discipline: counts, dates, venue names, row ids, deployment ids ONLY into any
    artifact. Never key material, labels with account hints, or emails — the repo is
    PUBLIC and `.planning/` is tracked.
  </instructions>
  <resume-signal>Part A green with the response keys, row id, and count delta transcribed; Part B measured green post-deploy OR present as an explicit dated pending-deploy gate recorded by founder decision. Anything not smoked is named as still-unverified.</resume-signal>
  <verify>
    <human-check>The human confirms Part A steps 3-6 were performed and observed (not inferred), and states which of the three surfaces were exercised and which were not.</human-check>
    <automated>MISSING — this is the irreducible human item; the Task-3 record gate (grep 'PROD smoke record') is its persistent trace</automated>
  </verify>
  <acceptance_criteria>
    - The persist arm handled at least one real PROD connect, evidenced by the Network-observed response AND the measured row — both, not either
    - The refusal behavior has a PROD measurement or an explicit dated pending-deploy gate — never an implicit pass
    - Un-smoked surfaces are enumerated, not absorbed
    - A Part-A failure halts closure and triggers investigation — no soft-pass path exists
  </acceptance_criteria>
  <done>The only non-wizard writer of api_keys has been seen working in production by a human, or the phase is honestly still open.</done>
</task>

<task type="auto">
  <name>Task 3: RECORD — transcribe the smoke evidence into the phase record; TODOS.md hygiene</name>
  <files>.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md, TODOS.md</files>
  <read_first>
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md (the re-verified state after Task 1; the behavior_unverified_items entry to close or annotate)
    - The Task-2 checkpoint record (the evidence being transcribed — transcribe, never embellish)
  </read_first>
  <action>
    Part A — the smoke record. Append a section titled `## PROD smoke record (gap closure
    160-07)` to 160-VERIFICATION.md: date; which surfaces were exercised and which were
    not; the observed response key names; the row id, venue, and count delta; the cleanup
    outcome; and the Part-B result (409 + code observed post-deploy, with the deployment
    id) or the explicit dated pending-deploy gate from Task 2 step 9. Update the
    frontmatter `behavior_unverified` count and the `behavior_unverified_items` entry to
    match the evidence exactly: close it only if Part A's minimum bar was met; if only a
    subset of surfaces was smoked, the entry narrows to name the remaining surfaces rather
    than disappearing. Counts/dates/ids only.

    Part B — backlog hygiene (TODOS.md is the single ground truth; annotate by
    measurement, never silently delete):
    1. Grep TODOS.md for any open item deferring the legacy-arm/STALE_CLIENT retirement
       (the 160-VERIFICATION.md override path anticipated one). If present, close it
       citing commit `2fe28b89` — the arm was retired, not accepted-as-deferred. (A
       pre-planning grep found none; re-check at execution HEAD.)
    2. TODOS.md carries a security item (§D, near line 110) whose premise is that
       `authenticated` keeps INSERT on `api_keys`, making the REVOKE UPDATE bypassable via
       DELETE + re-INSERT. Migration `20260823120000` withdrew INSERT and the 160-05
       post-apply measured it false on PROD. ANNOTATE that item with the measurement
       (migration id + date + "INSERT: false, measured") and flag it for founder closure —
       the item says it needs a design decision, so the closing call is the founder's, not
       this task's.
  </action>
  <verify>
    <automated>grep -q 'PROD smoke record' .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-VERIFICATION.md && grep -q '20260823120000' TODOS.md</automated>
  </verify>
  <acceptance_criteria>
    - The smoke record matches the checkpoint evidence one-to-one — nothing appears in the record that was not observed, and everything observed appears
    - The behavior_unverified frontmatter agrees with the record body (closed, narrowed, or open — whichever the evidence supports)
    - The TODOS.md security item is annotated with the measurement, not deleted; any stale retirement-deferral item is closed with the commit cited
    - Email-shaped-token grep of both touched files returns 0 hits
  </acceptance_criteria>
  <done>The phase record and the backlog both say exactly what was measured — no more, no less.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fix author → phase record | the session that wrote the fix could also score it — the gap this plan exists to prevent |
| human smoke → .planning artifacts | real-credential evidence crossing into a PUBLIC tracked repo |
| PROD browser → persist arm | the first real exercise of the only non-wizard api_keys writer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-160-24 | Repudiation | 160-VERIFICATION.md frontmatter | high | mitigate | Task-1: only an independent, freshly-spawned verifier re-scores the failed must_have; the orchestrator commits its output unedited |
| T-160-25 | Denial of Service | persist arm production-only fault (env, service credential, origin, rate limit) breaking connect-a-key for every tenant | critical | mitigate | Task-2 Part A is a blocking human smoke with a hard failure arm routed to investigation — never closed on absence of evidence |
| T-160-26 | Information Disclosure | ciphertext still served by PROD's legacy arm until the closure branch deploys | high | mitigate | deploy-honesty framing throughout; Task-2 Part B measures the refusal on PROD post-deploy or records an explicit dated pending gate |
| T-160-27 | Information Disclosure | smoke evidence leaking credentials/PII into tracked artifacts | high | mitigate | counts/dates/ids-only transcription rule in Task 2 and Task 3; email-token grep in Task-3 acceptance |

No packages are installed by this plan — the supply-chain row is not applicable.
</threat_model>

<verification>
- Task 1's gate is the independent verifier's own committed output (`re_verified:` naming
  the measured HEAD), not any claim by the fix author.
- Task 2's gate is human observation transcribed under Task 3's `PROD smoke record`
  heading; the automated trace is the heading grep plus the frontmatter agreeing with the
  body.
- The plan never marks the phase passed itself — it produces the evidence and the record;
  the pass/fail verdict belongs to the verifier and the phase-close gate.
</verification>

<success_criteria>
- 160-VERIFICATION.md reflects HEAD by independent measurement: the STALE_CLIENT gap
  adjudicated (including the ordering clause), the score/status re-cut by the verifier.
- The persist arm has a witnessed production exercise (row + response), or the phase is
  explicitly still open with the failure under investigation.
- The refusal has a PROD measurement or an explicit dated pending-deploy gate.
- TODOS.md agrees with what shipped: no stale deferral survives, the half-dead security
  premise is annotated with the measurement.
</success_criteria>

<output>
Create `.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-07-SUMMARY.md`
when done, recording: the verifier's verdict (as committed, not paraphrased), the smoke
evidence transcription, which surfaces remain unverified, the Part-B state
(measured / pending-deploy), and the TODOS.md annotations made.
</output>
