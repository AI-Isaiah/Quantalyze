---
phase: 160-provenance-the-server-s-venue-is-the-venue-that-annualizes
plan: 06
type: execute
wave: 5
depends_on: ["160-04", "160-05"]
files_modified:
  - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md
autonomous: false
requirements: [RANK-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every census-identified affected strategy (160-CENSUS.md Q2 rows where stamped <> venue_derived) received golden-parity re-annualization — old-vs-new metric snapshot with EVERY delta explained; if the census found zero candidates, the no-op is RECORDED with the census line cited, never silently skipped (D-09; ROADMAP 160 SC-4)"
    - "The expected-delta math is pinned BEFORE execution: RISK metrics (Sharpe, Sortino, volatility) scale by √(365/252) ≈ 1.203 in the direction the stamp moved; CAGR and cumulative return MUST NOT move (#597: RISK = frequency, RETURN/CAGR = CALENDAR)"
    - "Re-annualization is per-strategy and bounded to the explicit census id list — no blanket backfill, no WHERE clause broader than the enumerated ids (D-09)"
  artifacts:
    - path: ".planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md"
      provides: "golden-parity record — candidate list from the census, per-strategy before/after metric snapshots, delta adjudication; or the recorded no-op"
      contains: "1.203"
  key_links:
    - from: ".planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md"
      to: ".planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md"
      via: "the candidate list is transcribed from the census Q2 mismatch rows — the artifact's ids must be exactly that set"
      pattern: "venue_derived"
  prohibitions:
    - statement: "No blanket backfill: no UPDATE against strategies or strategy_analytics without an explicit id list transcribed from the census; zero candidates means zero writes"
      status: unverified
      flagged: true
      verification: "every mutation block in 160-PARITY.md carries WHERE id IN (<explicit uuid list>); no WHERE clause predicated on attested_venue/asset_class alone"
    - statement: "The parity artifact contains no PII — counts, dates, metric values, and strategy/key ids only (repo is PUBLIC)"
      status: unverified
      flagged: true
      verification: "no email- or uid-shaped token and no user-name column appears in the artifact"
---

<objective>
Close the RANK-04 remediation arm: census-identified strategies whose asset_class stamp
contradicts their venue get bounded, per-strategy golden-parity re-annualization — the
Phase-78 discipline ("golden parity gated the production switch... every delta explained"),
never a blanket backfill (D-09). If the B-M1 census found zero candidates, this plan's
output is the RECORDED no-op — decided by the census's mechanical B-D1 threshold (D-02),
surfaced, never silent.

Runs after the soak (PR-1 live on PROD — the corrected stamps must be consumed by the
deployed attestation-derived path, and finalize can no longer re-stamp them wrongly).

Purpose: ROADMAP 160 SC-4 (second half): "if the census finds affected strategies, their
re-annualization gets golden-parity treatment."
Output: `160-PARITY.md`, committed, with real before/after PROD numbers or the recorded
no-op.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-RESEARCH.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–06)

See 160-01-PLAN.md for the canonical phase-wide list. This plan produces:
- `.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md` — the golden-parity record or the recorded no-op
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the 160-PARITY.md scaffold from the census candidate list</name>
  <precondition>.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md Q2 RESULTS table is filled (the candidate source), and the plan-05 soak addendum exists (PR-1 confirmed live)</precondition>
  <files>.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md</files>
  <read_first>
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md (the Q2 results table and the B-D1 decision section — the candidate source)
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-RESEARCH.md §Pattern 4 (golden-parity discipline + the expected delta shape) and §Common Pitfalls 10 (PII rule)
  </read_first>
  <action>
    Read the census B-D1 decision. Branch mechanically (D-02 — no user gate):

    ZERO candidates: author `160-PARITY.md` as the recorded no-op — cite the census Q2
    result verbatim (zero rows where stamped differs from venue_derived), state that D-09's
    golden-parity obligation is discharged empty, and include the expected-delta math
    section anyway (√(365/252) ≈ 1.203 on RISK; CAGR/cumulative calendar-invariant) so a
    future candidate has the adjudication frame on record. Task 2 then only confirms and
    commits.

    NON-ZERO candidates: author the working scaffold: (1) the candidate table transcribed
    exactly from census Q2 (strategy_id, api_key_id, exchange, stamped, venue_derived);
    (2) per-candidate procedure blocks for the orchestrator — read-only BEFORE snapshot
    SELECT of the strategy's analytics row (sharpe, sortino, volatility, cagr,
    cumulative_return, max_drawdown, computation_status), a single-row stamp correction
    `UPDATE strategies SET asset_class = '<venue_derived>' WHERE id = '<uuid>'` (one
    statement per candidate, explicit uuid, never a set-based predicate), a recompute
    trigger note (enqueue via the existing per-strategy recompute path the sync flow uses),
    and an AFTER snapshot placeholder `RESULTS: PENDING`; (3) the adjudication table
    template: per metric, expected factor (RISK ×≈1.203 in the stamp's direction, RETURN ×1.000),
    observed factor, verdict — every delta explained or the run is not done (Phase-78
    discipline); (4) the PII rule paragraph copied from the census.
  </action>
  <verify>
    <automated>test -f .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md && grep -q '1.203' .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md && grep -qi 'no blanket backfill' .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md</automated>
  </verify>
  <acceptance_criteria>
    - The artifact branch matches the census B-D1 decision (no-op record XOR working scaffold) — the branch taken is stated in the file with the census line quoted
    - Non-zero branch: every candidate id from census Q2 appears exactly once; every mutation is a single-row UPDATE with an explicit uuid
    - The expected-delta section pins RISK ×≈1.203 and RETURN/CAGR ×1.000 with the #597 RISK-frequency/RETURN-calendar rationale
    - The PII rule paragraph is present
  </acceptance_criteria>
  <done>The parity artifact exists with a mechanically-derived branch and, if candidates exist, a deterministic per-strategy procedure the orchestrator can execute without improvisation.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: ORCHESTRATOR executes the parity procedure (or confirms the no-op) and commits</name>
  <files>.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md</files>
  <read_first>
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md (the scaffold from Task 1)
  </read_first>
  <action>
    STOP and return `## CHECKPOINT REACHED`. PROD reads/writes and recompute triggering are
    orchestrator-only (MCP stripped from subagents). Resume only on the resume signal.
  </action>
  <instructions>
    Orchestrator protocol:
    1. Confirm the PROD ref (khslejtfbuezsmvmtsdn) against the project list.
    2. NO-OP branch: verify the census Q2 table indeed shows zero mismatch rows, add the
       dated confirmation line, commit. Done.
    3. CANDIDATE branch, per candidate, one at a time: run the BEFORE snapshot SELECT and
       paste the row; execute the single-row stamp UPDATE for that uuid only; trigger the
       per-strategy recompute via the existing recompute path; wait for the computation to
       reach a terminal status; run the AFTER snapshot and paste it; fill the adjudication
       row — observed RISK factor vs ≈1.203, observed RETURN/CAGR factor vs 1.000. Any
       unexplained delta: STOP after that candidate, record the discrepancy, surface it —
       do not proceed to the next candidate on an unexplained number.
       <!-- planner-region-allow: RESULTS: PENDING -->
    4. Replace every pending marker; commit 160-PARITY.md.
  </instructions>
  <resume-signal>160-PARITY.md is committed with zero pending markers: either the confirmed no-op, or every candidate's before/after snapshot with every delta adjudicated.</resume-signal>
  <verification>
    - The committed artifact matches the census candidate set exactly (no extra ids, none missing)
    - Candidate branch: every adjudication row filled; RISK factors ≈1.203-consistent, RETURN/CAGR unmoved — or the discrepancy recorded and surfaced
  </verification>
  <verify>
    <automated>! grep -q 'RESULTS: PENDING' .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md && git log --oneline -1 -- .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PARITY.md | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - Zero pending markers; the file is committed
    - Every executed mutation was a single-row UPDATE with an explicit uuid from the census list (transcribed in the artifact)
    - Every delta is explained against the pinned expectation, or the run stopped and surfaced at the first unexplained one
  </acceptance_criteria>
  <done>ROADMAP 160 SC-4 fully discharged: affected strategies re-annualized under golden-parity discipline, or the no-op recorded — decided by the census, never by silence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator MCP → PROD strategies/strategy_analytics | bounded single-row writes + recompute triggering |
| .planning parity artifact → public repo | world-readable on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-160-24 | Tampering | an over-broad backfill mutating strategies beyond the census set | critical | mitigate | explicit-uuid single-row statements only (prohibition + acceptance grep); zero candidates ⇒ zero writes |
| T-160-25 | Repudiation | a silent skip of the remediation (no record either way) | high | mitigate | the artifact is mandatory in BOTH branches — the no-op is a committed record citing the census line |
| T-160-26 | Information Disclosure | PII in the public parity artifact | high | mitigate | census PII rule copied; ids/counts/metrics only |
| T-160-27 | Tampering | a wrong correction direction (stamping crypto onto a genuinely traditional venue) | high | mitigate | venue_derived comes from the census Q2 CASE mirroring CRYPTO_EXCHANGES; adjudication pins the delta DIRECTION per stamp move and stops on any unexplained number |
</threat_model>

<verification>
- Artifact committed, branch consistent with the census, zero pending markers.
- Candidate branch: adjudication complete, RISK ≈×1.203 / RETURN ×1.000 confirmed per
  candidate or discrepancy surfaced.
</verification>

<success_criteria>
- D-09 discharged: golden-parity re-annualization for exactly the census-identified
  strategies (or a recorded, cited no-op); no blanket backfill anywhere in the phase.
</success_criteria>

<output>
Create `.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-06-SUMMARY.md`
when done, recording: the branch taken, candidate count, and the adjudication headline
(all deltas explained / no-op confirmed).
</output>
