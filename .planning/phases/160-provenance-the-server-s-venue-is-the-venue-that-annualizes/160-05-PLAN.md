---
phase: 160-provenance-the-server-s-venue-is-the-venue-that-annualizes
plan: 05
type: execute
wave: 4
depends_on: ["160-02", "160-03"]
files_modified:
  - supabase/migrations/TIMESTAMP_revoke_api_keys_insert.sql
  - src/app/api/keys/validate-and-encrypt/route.ts
  - src/app/api/keys/validate-and-encrypt/route.test.ts
  - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md
autonomous: false
requirements: [RANK-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The two landings are ordered deploy-first / revoke-second: the REVOKE migration exists ONLY in this wave-4 plan, is authored only after the soak checkpoint confirms PR-1 merged + deployed + prod-smoked, and never shares a landing with the writer deploy (D-06; edge: ordering, resolved explicit)"
    - "The migration withdraws exactly one verb: REVOKE INSERT ON public.api_keys FROM anon, authenticated — DELETE is untouched (live client path) and RLS is untouched (grants are the lever) (D-05)"
    - "The migration guard re-runs the count-pinned census and ABORTS on drift of the un-attested population — constants hand-typed from the pre-REVOKE re-measure addendum, with a PROD-signature discriminator so a TEST/CI apply takes the lenient branch and PROD never can (D-03)"
    - "Every .from('api_keys') mutation in the WHOLE repo (src/, e2e/, tests/, scripts/, analytics-service/) was re-grepped at this plan's HEAD and classified before the REVOKE was committed — zero browser-context INSERT chains remain (D-05; v1.10 lesson: grep the whole repo, not src/ only)"
    - "After this landing the route serves NO ciphertext to any caller: absent-discriminator bodies receive a coded STALE_CLIENT refusal instead of the legacy ciphertext envelope, deployed in the same merge as the migration"
    - "The migration appends the marker substring revoke_api_keys_insert to the api_keys.exchange column comment while PRESERVING every existing substring — the plan-03 SQL gate arms on it and the sibling gate's marker-durability assertions depend on the preserved chain"
  artifacts:
    - path: "supabase/migrations/TIMESTAMP_revoke_api_keys_insert.sql"
      provides: "census-guarded REVOKE INSERT + aborting post-verify + column-comment marker append (executor stamps the real UTC timestamp; slug is pinned)"
      contains: "REVOKE INSERT ON public.api_keys FROM anon, authenticated"
    - path: "src/app/api/keys/validate-and-encrypt/route.ts"
      provides: "legacy ciphertext arm retired — coded STALE_CLIENT refusal for absent-discriminator bodies"
      contains: "STALE_CLIENT"
    - path: ".planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md"
      provides: "dated pre-REVOKE re-measure addendum with the re-cut constants the migration hand-types"
      contains: "re-measure"
  key_links:
    - from: "supabase/migrations/TIMESTAMP_revoke_api_keys_insert.sql"
      to: ".planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md"
      via: "hand-typed guard constants copied from the re-measure addendum; drift at apply time aborts the transaction (D-03)"
      pattern: "CONSTANT"
    - from: "supabase/migrations/TIMESTAMP_revoke_api_keys_insert.sql"
      to: "supabase/tests/test_api_keys_insert_not_client_writable.sql"
      via: "col_description marker append — the migration's landing flips the plan-03 gate from SKIP to ARMED on TEST"
      pattern: "revoke_api_keys_insert"
  prohibitions:
    - statement: "The REVOKE never widens beyond INSERT — no DELETE/UPDATE/SELECT withdrawal, no RLS policy change, no scrub-trigger or CHECK modification appears in the migration"
      status: unverified
      flagged: true
      verification: "the migration file contains exactly one REVOKE statement naming INSERT; grep finds no ALTER POLICY / DROP POLICY / CREATE OR REPLACE FUNCTION / ALTER TABLE ... CHECK"
    - statement: "On census drift the constants are re-measured and re-cut together — the comparison is never softened, the abort never downgraded to a NOTICE for PROD (Pitfall 9)"
      status: unverified
      flagged: true
      verification: "the guard's PROD branch RAISEs EXCEPTION on the enforced-pin mismatch; only the non-PROD discriminator branch may NOTICE"
    - statement: "No applied migration is edited — 20260810120000 and 20260811210000 are untouched; corrections live only in the NEW file"
      status: unverified
      flagged: true
      verification: "git diff of this plan's commits touches no pre-existing file under supabase/migrations/"
---

<objective>
The second landing (PR-2): withdraw the browser's INSERT verb on api_keys behind a
census-drift guard, and retire the route's legacy ciphertext arm in the same deploy — only
after the soak checkpoint proves PR-1 is live and smoked (D-06: deploy-first /
revoke-second, ⚠️ NEVER migration-first; merging supabase/migrations/** to main
AUTO-applies to PROD and fires the Vercel build on the same merge — acceptable ONLY in this
direction, the 20260813150106 asymmetry).

⚠️ SCHEMA-PUSH NOTE for the executor and orchestrator: this plan's migration is a separate,
second landing. It must never be committed to the same PR/branch state that first ships the
plan-02/03 writer code. There is no local `db push` task; the enforcement gate is CI's
`sql-tests` plus the migration's own aborting post-verify at auto-apply time.

Reversibility: the REVOKE is a one-way door (undo requires a counter-migration re-granting
INSERT on PROD, against clients that no longer insert). The DOOR DECISION itself is already
locked in 160-CONTEXT.md (§Server-authoritative INSERT rollout) — so the Task-2
checkpoint:decision does not re-ask WHAT was decided; it gates the TIMING of the one-way
door (soak sufficiency + census-drift explanation), blocking-human so auto-advance can
never walk through it unattended. The rating is recorded on the migration task itself.

Purpose: ROADMAP 160 SC-2 (completion): every INSERT path into api_keys is now structurally
server-side.
Output: the REVOKE migration + census addendum + legacy-arm retirement, landed as one PR
after soak.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-RESEARCH.md
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-PATTERNS.md
@.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md
@supabase/migrations/20260810120000_lock_api_keys_exchange_column.sql
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–06)

See 160-01-PLAN.md for the canonical phase-wide list. This plan produces:
- `supabase/migrations/<UTC-timestamp>_revoke_api_keys_insert.sql` — the PR-2-only migration (slug pinned: `revoke_api_keys_insert`)
- `src/app/api/keys/validate-and-encrypt/route.ts` + test — legacy ciphertext arm retired; NEW response code `STALE_CLIENT`
- `160-CENSUS.md` — dated pre-REVOKE re-measure addendum (checkpoint output)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: SOAK GATE — orchestrator confirms PR-1 live + smoked, re-measures the census, commits the addendum</name>
  <files>.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md</files>
  <read_first>
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md (Q1/Q1b SQL to re-run verbatim; the pins table to extend)
  </read_first>
  <action>
    STOP and return `## CHECKPOINT REACHED`. This gate is orchestrator/human work: PR
    landing, PROD deploy verification, authed prod smoke, and Supabase MCP access are all
    outside this executor. Resume only on the resume signal.
  </action>
  <instructions>
    Orchestrator/human protocol (D-06 — soak = one deploy cycle + prod smoke):
    1. Confirm the PR carrying plans 160-02/03/04 is MERGED to main and the Vercel PROD
       deployment for that merge is live (deployment commit hash matches the merge).
    2. Prod smoke, per the locked decision AND the Pitfall-5 augmentation (the wizard's
       writes have been server-side since Phase 156, so the wizard smoke alone would not
       exercise the new arm): (a) the wizard key-add flow (locked wording), AND (b) the
       three converted surfaces — ApiKeyManager strategy-page key add, StrategyForm modal
       connect, AllocatorExchangeManager connect. Each mints a row whose attested_venue
       equals its exchange (verify via read-only MCP SELECT on the new row ids). Use the
       qa-demo account; delete smoke keys afterward via the UI (DELETE is a live client
       path — exercising it is itself part of the smoke).
    3. Re-measure: re-run census Q1 and Q1b read-only against PROD (ref khslejtfbuezsmvmtsdn,
       confirmed against the project list first).
    4. Append a dated "Pre-REVOKE re-measure addendum" section to 160-CENSUS.md: the fresh
       un-attested count (the enforced pin the migration will hand-type), the Q1b residual,
       and the delta vs the original census with a one-line explanation of the drift
       (expected: rows minted by clients during the soak window). Counts/dates/ids only —
       no PII (repo is PUBLIC).
    5. Commit the addendum.
  </instructions>
  <resume-signal>PR-1 confirmed live on PROD, all four smoke flows green with attested rows observed, and 160-CENSUS.md carries the committed re-measure addendum with the re-cut enforced pin. Only then does Task 2 present the go/no-go.</resume-signal>
  <verification>
    - Vercel PROD deployment hash matches the PR-1 merge commit
    - Four smoke flows recorded (wizard + three converted surfaces) with observed attested_venue = exchange on the minted rows
    - The addendum carries a fresh hand-typed un-attested count and names the drift
  </verification>
  <verify>
    <automated>grep -qi 're-measure' .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md && git log --oneline -1 -- .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - The addendum section exists, is dated, carries the enforced-pin value and the Q1b residual, and is committed
    - The smoke evidence (four flows, attested rows) is transcribed into the addendum or the checkpoint record — never asserted without observation
    - No TEST-derived number was substituted for a PROD number
  </acceptance_criteria>
  <done>The soak is proven, the pin is fresh, and the one-way door has a same-day measurement to guard against.</done>
</task>

<task type="checkpoint:decision" gate="specialist-review">
  <name>Task 2: GO/NO-GO — specialist-reviewer gate on the one-way door</name>
  <gate_authorization>
    ⭐ FOUNDER AMENDMENT (2026-08-23, in-session) — supersedes `gate="blocking-human"` for
    THIS TASK ONLY. The human go/no-go is DELEGATED to two specialist reviewers: run BOTH
    `migration-reviewer` and `rls-policy-auditor` against the authored migration, fix every
    issue raised, then merge. No user prompt is raised at this gate.

    This changes WHO decides, not WHAT is judged. The Task-1 soak evidence and the
    census-drift criteria below still gate the door, and a reviewer BLOCKER is a NO-GO
    exactly as unexplained drift would have been. Ordering is unchanged: Task 3 authors the
    migration, THEN both reviewers run on it, THEN fixes, THEN merge. A reviewer finding is
    never waived by re-running the reviewer until it agrees.
  </gate_authorization>
  <decision>Proceed to author and land the REVOKE migration on the current soak evidence, or extend the soak</decision>
  <context>
    The REVOKE decision itself is LOCKED (160-CONTEXT.md §Server-authoritative INSERT
    rollout) — this gate does not re-ask it. The open human judgment is TIMING: merging the
    migration auto-applies to PROD in the same merge (one-way; undo = a counter-migration).
    Judge the Task-1 evidence: all four smoke flows green with attested rows, and the
    census drift between the original B-M1 numbers and the re-measure addendum fully
    explained by soak-window client mints. Unexplained drift or a failed smoke flow means
    the door is not ready.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed — author the REVOKE now</name>
      <pros>Soak evidence green and drift explained; the phase's control lands; Phase 164 (SHARE) is sequenced behind this soak</pros>
      <cons>Any missed browser INSERT path fails at merge (mitigated by Task 4's whole-repo re-grep hard stop)</cons>
    </option>
    <option id="extend-soak">
      <name>Extend the soak</name>
      <pros>More PROD cycles for stale tabs to drain and for the persist arm to accumulate evidence</pros>
      <cons>Phase stays open; census pin must be re-measured again at the eventual landing</cons>
    </option>
  </options>
  <resume-signal>Both `migration-reviewer` and `rls-policy-auditor` have run against the authored REVOKE migration, every finding is either fixed or explicitly adjudicated in writing, and neither holds an open BLOCKER. Only then does the merge proceed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author the census-guarded REVOKE migration</name>
  <reversibility rating="one-way">Merging this file auto-applies REVOKE INSERT to PROD; undo requires a counter-migration re-granting a verb no deployed client uses any more. The door decision is locked in 160-CONTEXT.md; Task 1's blocking-human soak gate is the immediate human confirmation before it.</reversibility>
  <files>supabase/migrations/TIMESTAMP_revoke_api_keys_insert.sql</files>
  <read_first>
    - supabase/migrations/20260810120000_lock_api_keys_exchange_column.sql (the REVOKE precedent at :104 and the file's overall shape)
    - supabase/migrations/20260811210000_api_keys_attested_venue.sql:590-674 (the census-pin guard being copied: hand-typed CONSTANTs :627-629, the PROD-signature discriminator, the :660-666 REPORTED-never-ENFORCED split)
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-CENSUS.md (the re-measure addendum — the source of the hand-typed constants)
    - supabase/tests/test_api_keys_insert_not_client_writable.sql (the marker-substring contract this migration must satisfy) and supabase/tests/test_api_keys_exchange_not_user_writable.sql:203-217 (how gates read the column comment; the marker-durability assertions the append must not break)
  </read_first>
  <action>
    Create `supabase/migrations/<UTC-timestamp>_revoke_api_keys_insert.sql` — stamp the
    real current UTC timestamp (YYYYMMDDHHMMSS) at authoring; the slug
    `revoke_api_keys_insert` is CONTRACTUAL (the plan-03 gate arms on it). Structure,
    inside `BEGIN; SET lock_timeout = '3s'; ... COMMIT;`:

    1. Header comment: the two-landing rationale (deploy-first/revoke-second, D-06), the
       20260813150106 direction-asymmetry citation, and why DELETE survives.
    2. Census re-run guard (D-03, Pattern 3): a DO block with hand-typed CONSTANTs copied
       from the re-measure addendum — the ENFORCED pin is the un-attested count (the
       population whose drift changes the decision); TOTAL api_keys rows are computed and
       RAISEd as a NOTICE delta only, NEVER compared-and-aborted (Pitfall 9 — the
       latent-outage shape 20260811210000:660-666 walked back). Copy that migration's
       PROD-signature discriminator idiom so a TEST/local/CI apply takes a lenient NOTICE
       branch and a PROD apply can never silently do so. On enforced-pin mismatch under the
       PROD signature: RAISE EXCEPTION printing the pin used and the measured value, with
       the instruction "re-measure and re-cut the constants together; never soften".
    3. The withdrawal, mirroring 20260810120000:104 with the verb changed:
       `REVOKE INSERT ON public.api_keys FROM anon, authenticated;` — nothing else revoked.
    4. Column-comment marker: read the CURRENT col_description of public.api_keys.exchange
       and re-stamp it as the existing text plus an appended sentence carrying this
       migration's full filename (which contains revoke_api_keys_insert) — an APPEND via
       concatenation of the read value, never a rewrite, so every older marker substring
       survives (the sibling gate's 5a/5a' durability assertions exist because a re-stamp
       once dropped one).
    5. ABORTING post-verify DO block: `has_table_privilege('authenticated',
       'public.api_keys','INSERT')` must be false AND the same for 'anon'; 'DELETE' for
       'authenticated' must remain true; spot-check `has_column_privilege` for
       'authenticated' SELECT on id and exchange (the migration-027 allowlist intact); the
       col_description must contain the new marker AND the older-migration substrings.
       Any failure: RAISE EXCEPTION (the transaction rolls back the REVOKE).
  </action>
  <verify>
    <automated>ls supabase/migrations/ | grep -q '_revoke_api_keys_insert\.sql$' && grep -q 'REVOKE INSERT ON public.api_keys FROM anon, authenticated' supabase/migrations/*_revoke_api_keys_insert.sql && grep -q 'has_table_privilege' supabase/migrations/*_revoke_api_keys_insert.sql && grep -qi 'lock_timeout' supabase/migrations/*_revoke_api_keys_insert.sql && grep -c 'REVOKE' supabase/migrations/*_revoke_api_keys_insert.sql | grep -qx 1</automated>
  </verify>
  <blocking_landmine_found_at_execution>
    ⛔ **MEASURED AT HEAD by plan 160-03's executor and independently CONFIRMED by the
    orchestrator (2026-08-23) — this task MUST address it in the SAME commit as the
    migration, or merging PR-2 hard-fails `sql-tests` on main.**

    `supabase/tests/test_api_keys_exchange_not_user_writable.sql` assertion **5c**
    (`:451-455`) drives a client re-INSERT as `authenticated` and RAISES if it is
    REFUSED — "153.6 D-02/D-03 keep this path open on purpose … A refusal here means
    connect-a-key is broken." The REVOKE reverses exactly that premise.

    It is ARMED, not dormant: the orchestrator read `col_description` on
    `public.api_keys.exchange` on the TEST DB (`qmnijlgmdhviwzwfyzlc`) and the gating
    marker `20260810120000` IS present. So 5c runs today and will fail the moment INSERT
    is withdrawn.

    Required: make 5c state-adaptive on the SAME `revoke_api_keys_insert` marker substring
    this migration appends — pre-REVOKE it keeps its current "refusal = broken" teeth;
    post-REVOKE it must instead ASSERT the refusal (42501) and keep an unconditional
    anti-vacuity positive so the armed branch cannot pass by testing nothing. Plan 160-03
    deliberately did NOT touch this file (its plan forbade it) — the fix belongs here.

    ⭐ Also correct the now-STALE sentence in that same column comment while appending the
    marker: it currently reads "Input to the strategies.asset_class annualization stamp
    (√365 crypto vs √252 traditional) — that reader deliberately still uses this column
    (153.6 OQ-2, out of scope)." Plan 160-04 closed OQ-2: the stamp now reads
    `attested_venue`. Leaving that sentence would make the database's own documentation
    assert the exact forgeable-input design this phase removed.
  </blocking_landmine_found_at_execution>
  <acceptance_criteria>
    - Exactly ONE REVOKE statement in the file, naming INSERT and both roles; zero statements touching DELETE/UPDATE/SELECT grants, RLS policies, the scrub trigger, or the CHECK
    - The guard constants match the re-measure addendum values character-for-character (transcribe the addendum table into the SUMMARY beside the constants for the reviewer)
    - The total-rows figure appears only in a NOTICE, never in an abort comparison; the enforced pin aborts only under the PROD-signature branch
    - The comment re-stamp is a concatenating append of the read current value (the DO block reads col_description before writing)
    - No pre-existing MIGRATION file is modified (the sibling SQL TEST at `supabase/tests/test_api_keys_exchange_not_user_writable.sql` IS modified — see the blocking landmine above — and that edit lands in the same commit as the migration)
    - Assertion 5c is state-adaptive on the `revoke_api_keys_insert` marker and its post-REVOKE branch asserts a 42501 refusal with an unconditional anti-vacuity positive
    - The stale OQ-2 sentence in the `api_keys.exchange` comment is corrected to name `attested_venue` as the stamp input
  </acceptance_criteria>
  <done>The one-way door is authored, guarded by a same-day census pin, self-verifying at apply time, and arms the plan-03 SQL gate on TEST when merged.</done>
</task>

<task type="auto">
  <name>Task 4: Pre-REVOKE whole-repo write-surface re-grep + retire the legacy ciphertext arm</name>
  <files>src/app/api/keys/validate-and-encrypt/route.ts, src/app/api/keys/validate-and-encrypt/route.test.ts</files>
  <read_first>
    - .planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-RESEARCH.md §The write surface (the classified inventory to re-verify at THIS HEAD — it is a 2026-08-23 snapshot, not a fact)
    - src/app/api/keys/validate-and-encrypt/route.ts (post-plan-02 state: the persist arm and the legacy handler dispatch)
    - src/app/api/keys/validate-and-encrypt/route.test.ts (the legacy-parity tests this task deliberately supersedes)
  </read_first>
  <action>
    Part A — the locked pre-REVOKE grep (D-05, Pitfall 1, assumption A2): run
    `grep -rn 'from("api_keys")' src/ e2e/ tests/ scripts/ analytics-service/` at this
    plan's HEAD. Classify EVERY hit against the RESEARCH §write-surface table: verb,
    execution context (browser authenticated vs service_role/admin vs test-fixture).
    Acceptance is zero browser-context INSERT chains; any unclassified or new
    browser-context INSERT is a HARD STOP — surface it, do not proceed to commit the
    migration alongside an unconverted writer. Paste the classified inventory into the
    SUMMARY.

    Part B — retire the legacy arm in the SAME landing as the migration (RESEARCH
    §Recommended rollout, PR-2 row): bodies without `persist === true` no longer receive
    the ciphertext envelope. Replace the legacy dispatch with a coded refusal:
    `NextResponse.json({ error: <curated copy telling the user this page is out of date
    and to reload>, code: "STALE_CLIENT" }, { status: 409, headers: NO_STORE_HEADERS })`.
    Add `STALE_CLIENT` to the route's response-code surface following the file's existing
    code-union convention (extend at the pin if the union is centrally typed). Delete the
    now-dead legacy handler code path; keep the shared validate+encrypt core the persist
    arm uses. Update route.test.ts: the plan-02 legacy-parity tests flip to assert the
    STALE_CLIENT contract (409, coded envelope, NO ciphertext field, zero inserts); add
    one test asserting NO response arm in the whole route ever carries a
    ciphertext-named field now. The skew-window truth survives in its final form: an
    absent-discriminator body still causes zero server-side inserts.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/keys/validate-and-encrypt/route.test.ts --no-file-parallelism && grep -q 'STALE_CLIENT' src/app/api/keys/validate-and-encrypt/route.ts && ! (for f in $(grep -rl --include='*.ts' --include='*.tsx' 'from("api_keys")' src/components/ src/app/ | grep -v '\.test\.'); do tr -d ' \n\t' < "$f" | grep -q 'from("api_keys").insert(' && echo "$f"; done | grep -q .) && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The classified whole-repo inventory is in the SUMMARY with every hit accounted for; zero browser-context INSERTs
    - Absent-discriminator bodies receive the 409 STALE_CLIENT coded envelope; no route response arm carries a ciphertext-named field (tested)
    - The persist arm's behavior is unchanged by the retirement (plan-02 persist tests still green unmodified)
    - VERSION + package.json bump note honored at ship time per repo workflow (recorded for the shipper, not performed here)
  </acceptance_criteria>
  <done>The route serves exactly one contract (persist), stale tabs fail honestly with curated copy, and the write surface is proven clean at the commit that carries the REVOKE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main merge → PROD Postgres | supabase/migrations auto-apply — the merge IS the apply |
| stale browser JS → route + api_keys | the skew window this landing closes |
| orchestrator MCP → PROD | soak verification + re-measure (read-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-160-18 | Denial of Service | migration-first ordering 42501-ing every connect for the merge window | critical | mitigate | wave-4 placement + Task-1 blocking-human soak gate before authoring; migration exists in no earlier plan |
| T-160-19 | Tampering | REVOKE applied against a drifted population (keys minted/deleted between measure and merge) | high | mitigate | census re-run guard with hand-typed enforced pin from the same-day addendum; abort-on-drift under the PROD signature (D-03) |
| T-160-20 | Denial of Service | an unconverted INSERT path dying at merge (a fourth site, A2) | high | mitigate | Task-4 whole-repo re-grep with HARD STOP on any browser-context INSERT; the plan-03 conversions are the first net |
| T-160-21 | Information Disclosure | ciphertext still round-tripping through browsers post-REVOKE | high | mitigate | legacy arm retired in the same deploy; no-ciphertext-anywhere test |
| T-160-22 | Tampering | deploy-vs-revoke skew: stale tab double insert during the soak | high | mitigate | discriminator already live (plan 02); post-landing, stale inserts fail 42501 at the DB and stale POSTs fail STALE_CLIENT at the route — both honest |
| T-160-23 | Elevation | the migration silently widening privileges (comment rewrite dropping markers, or a CREATE OR REPLACE re-granting PUBLIC) | high | mitigate | append-only comment re-stamp verified by the aborting post-verify; prohibition: no function DDL in the file at all |
</threat_model>

<verification>
- The migration exists only in this plan; its aborting post-verify covers INSERT-false /
  DELETE-true / allowlist / marker-chain.
- CI `sql-tests` is the enforcement gate; note honestly in the SUMMARY that the plan-03
  gate's armed assertions are first OBSERVED armed-and-green only after this migration
  reaches TEST (Phase-156 precedent).
- Ship discipline: this plan's outputs form their own PR, landed alone, after PR-1 —
  never bundled with the writer deploy.
</verification>

<success_criteria>
- ROADMAP 160 SC-2 complete: every INSERT path into api_keys is server-side and the client
  verb is withdrawn — deploy-first/revoke-second honored, whole-repo write surface grepped
  at the landing HEAD, census-guarded, DELETE untouched.
</success_criteria>

<output>
Create `.planning/phases/160-provenance-the-server-s-venue-is-the-venue-that-annualizes/160-05-SUMMARY.md`
when done, recording: the soak evidence, the re-cut pin values beside the migration
constants, the classified write-surface inventory, and the SKIP→ARMED expectation for the
SQL gate on TEST.
</output>
