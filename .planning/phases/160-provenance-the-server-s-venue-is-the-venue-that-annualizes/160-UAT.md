---
status: complete
phase: 160-provenance-the-server-s-venue-is-the-venue-that-annualizes
source: 160-01..07-SUMMARY.md
started: 2026-08-25T20:10:00Z
updated: 2026-08-25T21:40:00Z
---

## Current Test

[testing complete]

## Tests

### 1. PROD persist-arm smoke (Part A)
expected: |
  Connect one real exchange API key at `/strategies/<id>/edit` on production
  (that page mounts BOTH `ApiKeyManager` and `StrategyForm` — the two live
  surfaces that call the persist arm).

  TWO independent confirmations are required; either alone is insufficient:
    (a) prod `api_keys` census goes 32 -> 33, and the new row's
        `attested_venue` equals the venue connected;
    (b) prod request logs show `requestPath = /api/keys/validate-and-encrypt`.

  (b) is the load-bearing one. A correct new row does NOT identify the writer.
result: pass
evidence: |
  Founder connected a real OKX read-only key at /strategies/<id>/edit
  (ApiKeyManager) on production, 2026-08-25T21:38:12Z.

  BOTH required confirmations hold:
    (a) prod `api_keys` census 32 -> 33. New row: label "160 gate smoke",
        exchange = 'okx', attested_venue = 'okx', owner sm@quantalyze.test.
        The server-stamped venue EQUALS the requested exchange, which is the
        160-02 / RANK-03 invariant this gate exists to prove.
    (b) prod request logs show `/api/keys/validate-and-encrypt` (5 hits in the
        window). The persist arm ran; the write did not ride the Phase-156 RPC
        path the 2026-08-23 OKX connect used.

  ⚠️ THE GATE WAS NOT MERELY UNTESTED - THE ARM WAS DEAD.
  The first three attempts returned 401. Root cause was NOT the persist arm:
  Vercel's `ANALYTICS_SERVICE_KEY` did not match Railway's `SERVICE_KEY`, so
  every guarded analytics route refused. Key-connect had been impossible in
  production for an unknown duration, and a scheduled job
  (`/api/match/cron-recompute`) was failing on the same cause. Filed as
  TODOS 0.05 (resolved) and 0.04 (PYAPI-06 could not detect it -> Phase 164.1).

  After that fix, two further real defects surfaced before a key would connect:
  TODOS 0.03 (credentials are never whitespace-trimmed) and 0.02 (OKX is
  hardcoded to ccxt's default entity, so a post-re-KYC key on another entity
  cannot authenticate). The key that finally connected was a different one.

  ⚠️ CORRECTION TO THIS FILE'S OWN EARLIER NOTE: an earlier revision claimed
  STATE.md's `attested_venue = exchange` criterion "matches ZERO rows" because
  the column stores a venue name. That was a MISREADING - `exchange` there is a
  COLUMN REFERENCE, not the literal string. The criterion is correct as written
  and is satisfied. The erroneous note is retracted; it would have invited
  someone to loosen a criterion that was never broken.

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

(none — the test PASSED. The four defects found on the way to it are filed in
TODOS as 0.02-0.06 and routed to Phases 162 / 164.1; they are not gaps in
Phase 160's own goal, which this test confirms.)
