---
status: partial
phase: 160-provenance-the-server-s-venue-is-the-venue-that-annualizes
source: 160-01..07-SUMMARY.md
started: 2026-08-25T20:10:00Z
updated: 2026-08-25T20:10:00Z
---

## Current Test

number: 1
name: PROD persist-arm smoke (Part A)
expected: |
  A real exchange API key connected through a NON-WIZARD surface reaches
  `api_keys` VIA the `persist: true` arm of `/api/keys/validate-and-encrypt`.
awaiting: founder action (live prod connect with a real credential)

## Tests

### 1. PROD persist-arm smoke (Part A)
expected: |
  Connect one real exchange API key at `/strategies/<id>/edit` on production
  (that page mounts BOTH `ApiKeyManager` and `StrategyForm` — the two live
  surfaces that call the persist arm).

  TWO independent confirmations are required; either alone is insufficient:
    (a) prod `api_keys` census goes 32 -> 33, and the new row's
        `attested_venue` equals the venue connected;
    (b) prod request logs show `requestPath = /api/keys/validate-and-encrypt`.

  (b) is the load-bearing one. A correct new row does NOT identify the writer.
result: blocked
blocked_by: third-party
reason: |
  Requires a real exchange API credential and a live browser session. Cannot be
  performed by an agent, and MUST NOT be simulated — this arm is the ONLY door
  into `api_keys` for the three converted non-wizard surfaces, so a fabricated
  pass would leave the single untested write path in production believed-tested.

  ⛔ The wizard does NOT exercise this arm. The 2026-08-23 OKX connect is
  already on record as NOT closing this gate: prod logs for that write show
  `/strategies/new/wizard` + `/api/strategies/create-with-key` with ZERO hits on
  the persist arm. It rode the Phase-156 RPC path.

  Measured 2026-08-25T20:10Z: census 32, newest row 2026-08-23 21:39 (that same
  OKX connect). Unchanged since the gate was recorded.

  ⚠️ CRITERION CORRECTION: STATE.md says to expect `attested_venue = exchange`.
  Read literally that matches ZERO rows — the column stores the venue NAME
  (deribit 13 / okx 10 / bybit 5 / mt5 4, null 0). Assert the new row's
  `attested_venue` equals the venue actually connected. Taken literally the
  check fails on a genuine pass, which invites "fixing" it by loosening the
  assertion until it cannot fail.

## Summary

total: 1
passed: 0
issues: 0
pending: 0
skipped: 0
blocked: 1

## Gaps

(none — the test is blocked on a human action, not failed. Do not convert a
blocked test into a gap: nothing is known to be broken.)
