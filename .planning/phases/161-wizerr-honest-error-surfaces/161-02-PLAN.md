---
phase: 161-wizerr-honest-error-surfaces
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/mt5_probe.py
  - analytics-service/services/mt5_validation.py
  - analytics-service/services/mt5_client.py
  - analytics-service/services/ingestion/mt5.py
  - analytics-service/services/job_worker.py
  - analytics-service/routers/exchange.py
  - analytics-service/tests/test_mt5_validate_parity.py
  - analytics-service/tests/test_job_worker.py
  - analytics-service/tests/test_ingestion_mt5.py
autonomous: true
requirements: [WIZERR-01]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "When the gateway reports trade_allowed false (and the named API option is NOT in force), the user-facing detail names the Allow-algorithmic-trading cause — the founder-measured live case renders the true blocker"
    - "When the named external-API option IS in force (tradeapi_disabled true), the detail names that option; when BOTH flags indicate blockage, the option arm wins deterministically (documented precedence, pinned by a test)"
    - "When the flags are absent or ambiguous, today's generic curated constant renders unchanged — an absent key can never KeyError and never mis-selects an arm"
    - "Every constant the builder can emit passes the curated-message fence (case-insensitive substring over all 14 denylist tokens), pinned by the parity test parametrized over the builder's full output set"
    - "Both raise sites (ingestion and router validate path) consume the ONE shared builder, so the next carrier inherits the class fix"
    - "Mt5GatewayMisconfigured() with no arguments still yields a curated constant (default-argument property preserved — raw remote text can never ride in by omission)"
    - "No loading branch and no success branch on the MT5 gateway-misconfigured surface (E1) is edited — the diff touches the curated-constant selection and its carrier prose only"
    - statement: "The two new curated causes are long single paragraphs; they wrap within the existing wizard envelope body without truncation — covered by the existing envelope render tests over the tallest curated cause"
      verification: backstop
    - statement: "The MT5 envelope body grows downward inside its existing mount; no fixed-height clipping on this surface"
      verification: backstop
  artifacts:
    - path: "analytics-service/services/mt5_validation.py"
      provides: "flag→cause builder beside terminal_trade_permission_off (the ONE cause seam)"
    - path: "analytics-service/tests/test_mt5_validate_parity.py"
      provides: "fence assertions parametrized over every builder-emittable constant + arm-selection cases"
  key_links:
    - from: "analytics-service/services/ingestion/mt5.py"
      to: "the flag→cause builder"
      via: "raise site 1 passes the terminal dict it already holds"
      pattern: "Mt5GatewayMisconfigured"
    - from: "analytics-service/routers/exchange.py"
      to: "the flag→cause builder"
      via: "raise site 2 (interactive validate) derives its MT5_GATEWAY_UNCONFIGURED detail from the same builder"
      pattern: "MT5_GATEWAY_UNCONFIGURED"
  prohibitions:
    - statement: "No curated MT5 constant may contain, as a lowercase substring, any fence token from _WRONG_SERVER_TOKENS, _AUTH_TOKENS, or the credential words — and the fence itself MUST NOT be relaxed (ledger: rewrite within the fence, never relax it)"
      status: resolved
      verification: test
    - statement: "Flag names (tradeapi_disabled, trade_allowed) MUST NOT appear in any user-facing sentence — the surface gets the cause, never the sensor reading"
      status: resolved
      verification: test
    - statement: "The builder MUST NOT be a second copy of the four-condition shape test — extend beside terminal_trade_permission_off, never duplicate it (a shape test copied twice drifts)"
      status: resolved
      verification: judgment
---

<objective>
WIZERR-01: the MT5 "gateway misconfigured" copy names the ACTUAL blocker, derived from the
`terminal_info` flags both raise sites already hold — fixed as a CLASS via one shared
flag→cause builder, inside the curated-message fence. All comment-prose carriers repeating the
false claim are corrected in the same pass.

Purpose: today's constant asserts the 'Disable automatic trading through the external Python
API' option is in force — founder-measured FALSE on the live gateway (`tradeapi_disabled` was
False while `trade_allowed` was False; the real blocker is Allow-algorithmic-trading, re-set
off on every account change). A false sentence on a founder-hit surface is the exact defect
class this phase exists to kill.
Output: three-arm curated builder + fence extension + corrected prose across all six carrier files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-wizerr-honest-error-surfaces/161-RESEARCH.md   <!-- § SC-1 inventory + § Curated-Message Fence — read BOTH in full -->
@.planning/phases/161-wizerr-honest-error-surfaces/161-UI-SPEC.md    <!-- § Copy Spec WIZERR-01: the two fence-checked constants, verbatim -->
@analytics-service/services/mt5_probe.py
@analytics-service/services/mt5_validation.py
@analytics-service/tests/test_mt5_validate_parity.py
</context>

<assumption_delta_decision>
Detector fired (pluralization). Real transition in this plan: ONE curated constant becomes a
FAMILY of cause-variant constants. Decision: **promote** — the flag→cause builder becomes the
primary identity both raise sites consume; the old `MT5_GATEWAY_MISCONFIGURED_DETAIL` demotes
to the builder's generic-fallback arm. Rationale: add-alongside (sites picking constants ad
hoc) would let the next carrier bypass the fence; the fence is extended over every constant
the builder can emit, so the generalized intent is enforced at the single seam.
</assumption_delta_decision>

## Planner assumptions (named, per RESEARCH Assumptions Log)

- **A1 (UNVERIFIED, founder-measured once 2026-08-13, zero production readers):**
  `terminal_info()` carries `tradeapi_disabled` as a dict key on the live gateway. The builder
  guards defensively (`.get()` reads; absent ⇒ generic arm). Live re-measurement is
  founder-gated and stays a Manual-Only verification item (161-VALIDATION.md) — the code path
  must be safe whether or not A1 holds.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: flag→cause builder + both raise sites + fence extension, end-to-end</name>
  <read_first>
    - analytics-service/services/mt5_validation.py:79-96 (the token tables — the binding denylist), :243-266 (terminal_trade_permission_off — the seam to extend beside, never duplicate)
    - analytics-service/services/mt5_probe.py:75-104 (today's constant + Mt5GatewayMisconfigured default-argument property)
    - analytics-service/services/ingestion/mt5.py:315-345 (raise site 1) and analytics-service/routers/exchange.py:615-624, :851-867 (raise site 2 + the env-gap sibling that does NOT change)
    - analytics-service/tests/test_mt5_validate_parity.py:380-403 (the fence test to extend), tests/test_job_worker.py:137+ (len>40 pin), tests/test_ingestion_mt5.py:310+
    - 161-UI-SPEC.md § Copy Spec WIZERR-01 — the two new constants, checker-verified clean against all 14 tokens; use them VERBATIM
  </read_first>
  <files>analytics-service/services/mt5_validation.py, analytics-service/services/mt5_probe.py, analytics-service/services/ingestion/mt5.py, analytics-service/routers/exchange.py, analytics-service/tests/test_mt5_validate_parity.py, analytics-service/tests/test_job_worker.py, analytics-service/tests/test_ingestion_mt5.py</files>
  <behavior>
    - builder(terminal) with tradeapi_disabled True ⇒ the option-in-force constant (UI-SPEC arm 2)
    - builder(terminal) with tradeapi_disabled falsy AND trade_allowed False ⇒ the Allow-algorithmic-trading constant (UI-SPEC arm 1, the founder-measured live case)
    - builder(terminal) with BOTH indicating blockage ⇒ the option-in-force constant (deterministic precedence: the named option subsumes; documented in the builder docstring)
    - builder({}) / absent keys / non-dict-ish values ⇒ today's generic constant, no exception
    - fence test parametrized over ALL THREE emittable constants: no denylist substring (lowercased), non-empty, len > 40
  </behavior>
  <action>
    Add a flag→cause builder function beside `terminal_trade_permission_off` in
    `mt5_validation.py` (or `mt5_probe.py` if import cycles force it — decide and state why in
    the SUMMARY) returning one of three module-level `Final[str]` curated constants: the two
    NEW sentences from 161-UI-SPEC § Copy Spec WIZERR-01 (verbatim — they are fence-checked)
    and today's generic `MT5_GATEWAY_MISCONFIGURED_DETAIL` as the fallback arm. All reads via
    `.get()`; precedence per the behavior block. Wire BOTH raise sites: ingestion/mt5.py
    passes the `terminal` dict it holds at :315 into `Mt5GatewayMisconfigured(builder(terminal))`;
    routers/exchange.py:851-867 derives its `MT5_GATEWAY_UNCONFIGURED` detail from the same
    builder (the `terminal` dict is in scope). The env-gap raise at exchange.py:615-624 keeps
    its own detail unchanged (no terminal dict in hand — no cause to name; honest fallback).
    Preserve the default-argument property: `Mt5GatewayMisconfigured()` with no args still
    yields the generic curated constant.
    Extend the parity fence: parametrize
    `test_mt5_gateway_misconfigured_message_is_curated_and_credential_free` (or add sibling
    cases) over EVERY builder-emittable constant, and add arm-selection cases per the behavior
    block. Extend the test_job_worker and test_ingestion_mt5 pins so `classify_exception` and
    the ingestion raise still carry curated constants (never str of arbitrary exc) for every arm.
    ANTI-VACUITY: (a) working-tree-only, insert a fence-illegal token into one NEW constant →
    observe the parity test RED first-hand → restore byte-identical; (b) neuter the builder to
    always return the generic constant → arm-selection cases RED → restore. Record both
    observed messages for the SUMMARY.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_mt5_validate_parity.py tests/test_job_worker.py tests/test_ingestion_mt5.py -x</automated>
  </verify>
  <done>Both raise sites consume the builder; three arms select deterministically; every emittable constant is fence-pinned; both neuter cycles produced observed RED and were restored byte-identical.</done>
  <acceptance_criteria>
    - The two new constants are byte-identical to 161-UI-SPEC § Copy Spec WIZERR-01 (they were checker-verified against all 14 tokens — any wording change requires re-checking every token by hand BEFORE running the test, and stating so).
    - No user-facing sentence contains a flag name.
    - Absent-key case pinned by a test (generic arm, no exception) — the A1 quarantine.
    - Precedence documented in the builder docstring AND pinned by the both-flags test case.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: correct the false-claim prose across all six carrier files + full Python gate</name>
  <read_first>
    - 161-RESEARCH.md § SC-1 "The six carrier files" table — the 9 prose/copy locations with line refs (re-locate by content at HEAD; line numbers rot)
  </read_first>
  <files>analytics-service/services/mt5_probe.py, analytics-service/services/mt5_validation.py, analytics-service/services/mt5_client.py, analytics-service/services/ingestion/mt5.py, analytics-service/routers/exchange.py, analytics-service/services/job_worker.py</files>
  <action>
    Correct every comment/docstring that repeats the false claim that the external-Python-API
    option is what makes `trade_allowed` false. The measured truth to state instead (from the
    founder's 2026-08-13 live measurement, RESEARCH § SC-1): `trade_allowed` is governed by the
    Expert-Advisors "Allow algorithmic trading" setting (`Enabled` in `[Experts]`), which the
    gateway re-sets off on every account change (`Account=1`/`Profile=1`), and the worker logs
    in on every job — that is why it RECURS; the external-API option (`Api`) is an independent
    checkbox read through `tradeapi_disabled`. Sites (re-locate each by its quoted content):
    mt5_probe.py class docstring + the comment carrying the claim verbatim near the probe;
    mt5_validation.py module + classify_trade_capability docstrings + the branch-5 comment;
    mt5_client.py terminal_info docstring; ingestion/mt5.py raise-site comment;
    routers/exchange.py raise-site comment; job_worker.py classify_exception comment.
    Keep every correction surgical — prose only, zero behavior diffs in this task.
    Then run the FULL Python gate for the plan.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest -x -q && python3 -m mypy --strict . | tail -5</automated>
  </verify>
  <done>All carrier prose states the measured mechanism; full pytest green from analytics-service/ (repo-root pytest is forbidden — VCR cassette misses issue LIVE broker calls); mypy --strict clean.</done>
  <acceptance_criteria>
    - Zero non-comment/non-docstring diffs in this task (git diff shows prose-only hunks).
    - The corrected prose names the recurrence mechanism (re-set on account change), not just the setting.
    - mypy --strict run BEFORE ship (standing rule — pytest-only leaves mypy errors latent until PR CI).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 gateway → analytics-service | terminal_info dict from a remote process; treated as untrusted shape (A1) |
| analytics-service → user copy | curated constants are the only text that crosses; fence-gated |

## STRIDE Threat Register (ASVS L1, block_on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-04 | Information Disclosure | curated constants | high | mitigate | Fence test parametrized over every builder-emittable constant (credential words + venue-internal tokens denied); default-argument property keeps raw remote text out by construction (mt5linux interpolates the password into remotely-eval'd source — T-134-01 lineage) |
| T-161-05 | Denial of Service | builder on malformed terminal dict | medium | mitigate | .get() reads + generic-arm fallback pinned by test; a KeyError here would fail the whole job permanently |
| T-161-06 | Spoofing | gateway-supplied flags select copy | low | accept | Flags only select between three curated constants; worst case is a wrong-but-safe curated sentence, no injection surface |

No package installs — supply-chain row not applicable.
</threat_model>

<verification>
- pytest ONLY from `analytics-service/` with `python3` (repo-root run misses VCR cassettes → LIVE broker calls).
- `mypy --strict` clean before the plan closes.
- CI-gate prose: "would have caught", never "did stop" (gates advisory at merge).
</verification>

<success_criteria>
- WIZERR-01 closed as a CLASS: one builder, both raise sites, fence over every emittable constant, false prose corrected at all carriers.
- The founder-measured live case renders the true cause; the ambiguous case renders the honest generic.
</success_criteria>

<output>
Create `.planning/phases/161-wizerr-honest-error-surfaces/161-02-SUMMARY.md` when done, including both observed-RED records and the builder-location decision.
</output>

## Artifacts this plan produces

- flag→cause builder function (new, `mt5_validation.py` — name at executor discretion, documented) + TWO new curated `Final[str]` constants (UI-SPEC arm 1 and arm 2 sentences).
- Parametrized fence cases + arm-selection cases in `test_mt5_validate_parity.py`; extended pins in `test_job_worker.py` / `test_ingestion_mt5.py`.
- Corrected prose at 9 locations across the six carrier files.
