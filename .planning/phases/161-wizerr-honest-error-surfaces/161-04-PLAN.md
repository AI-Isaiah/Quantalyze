---
phase: 161-wizerr-honest-error-surfaces
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/app/(dashboard)/strategies/new/wizard/ (the existing suite that exercises onTryAnotherKey)
autonomous: true
requirements: [WIZERR-02]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Clicking 'Try another key' issues NO draft DELETE and cascades away NO strategy_keys members — it is a pure step transition back to connect_key"
    - "The deliberate delete paths (confirm-dialog danger button, start_fresh) still delete, with their existing confirmation, byte-identical"
    - "The LOW-2 session-replay calculus is re-answered explicitly in a code comment at the callback and in the SUMMARY — not silently dropped with the delete"
    - "Resubmitting the SAME key after 'Try another key' resumes the surviving draft via the venue-identity resolver (honest resume, not a silent replay of stale state)"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      provides: "non-destructive onTryAnotherKey (onReviewKeys-shaped pure transition)"
  key_links:
    - from: "WizardClient.tsx onTryAnotherKey"
      to: "the connect_key step"
      via: "setStep only — no handleDeleteDraft call, mirroring the onReviewKeys precedent one prop away"
      pattern: "onTryAnotherKey"
  prohibitions:
    - statement: "A destructive action MUST NOT ride behind a remedy-labeled button — deletion happens only behind the existing explicit confirmations"
      status: resolved
      verification: test
    - statement: "The confirm-dialog and start_fresh delete paths MUST NOT be weakened, relabeled, or de-confirmed — they stay byte-identical"
      status: resolved
      verification: test
---

<objective>
WIZERR-02: "Try another key" never destroys the draft or cascades away composite members. The
remedy becomes the pure step transition the neighbouring `onReviewKeys` prop already models,
and the LOW-2 session-replay question the old destructive path answered is re-answered
explicitly for the new shape.

Purpose: the recorded class is user-inflicted data loss (DoS) — a remedy button that
fire-and-forget deletes the draft and every strategy_keys member under it, offered on every
refusal.
Output: non-destructive callback + wiring tests proving no DELETE is issued.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-wizerr-honest-error-surfaces/161-RESEARCH.md   <!-- § SC-2 WIZERR-02 mutation trace + design constraints (a)(b)(c) + Open Question 3 -->
@.planning/phases/161-wizerr-honest-error-surfaces/161-UI-SPEC.md    <!-- § Copy Spec WIZERR-02 -->
@src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
</context>

<assumption_delta_decision>
Detector fired on "another" (pluralization) in the phase goal. Decision: **no-change** — the
remedy's identity model does not change; the wizard already supports switching keys, and only
the destructiveness of the transition changes. No second identity is introduced and no primary
key/anchor moves.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: onTryAnotherKey becomes a pure step transition + LOW-2 re-answer</name>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx:1229-1260 — onReviewKeys (the non-destructive precedent, with its own comment explaining why it does NOT delete and does NOT regenerate the session id) and today's onTryAnotherKey (optimistic setWizardSessionId + fire-and-forget handleDeleteDraft)
    - WizardClient.tsx:959-1073 — handleDeleteDraft + the deliberate delete paths (confirm dialog danger button, start_fresh) which STAY
    - 161-RESEARCH.md § SC-2 WIZERR-02 — the full mutation trace, the LOW-2 red-team history (a failed DELETE + old session id silently replayed the OLD draft + OLD key on resubmit), and Open Question 3
  </read_first>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx</files>
  <action>
    Rewrite onTryAnotherKey to the onReviewKeys shape: `setStep("connect_key")` only — no
    handleDeleteDraft, no wizardSessionId regeneration (mirror onReviewKeys, which its own
    comment justifies). Decision (per RESEARCH Open Question 3, recorded here): keep-and-resume
    — the draft deliberately survives; a user who wants a clean slate has the retained
    start_fresh / confirm-dialog delete paths.
    LOW-2 RE-ANSWER (write this into the replacing code comment): LOW-2's threat was a FAILED
    background DELETE leaving the old draft alive while a REGENERATED session id made the
    client believe it had a clean slate — the silent divergence between client belief and
    server state was the bug. With no delete attempted and no id regenerated, client belief and
    server state agree by construction: same key resubmitted ⇒ the venue-identity resolver
    resumes the surviving draft (correct — the draft IS current state); different key ⇒ the
    normal create path runs, and the old draft ages into the existing ≥7-day nightly sweep or
    the user's explicit delete. No silent divergence exists to exploit. If, while implementing,
    you find the resolver does something OTHER than resume for the same-key case, STOP and
    surface it in the SUMMARY rather than papering over it.
    Also update the adjacent stale comments (the "Regenerate the idempotency token
    OPTIMISTICALLY" block and the sibling comment describing the cascade) — a comment
    describing behavior that no longer exists is a false sentence in this phase's own class.
  </action>
  <verify>
    <automated>npx vitest run src/app/\(dashboard\)/strategies/new/wizard/WizardClient.test.tsx 2>/dev/null || npx vitest run --dir 'src/app/(dashboard)/strategies/new/wizard'</automated>
  </verify>
  <done>onTryAnotherKey is a pure transition; LOW-2 re-answer lives in the code comment; stale comments corrected; existing wizard suites green.</done>
  <acceptance_criteria>
    - No call to handleDeleteDraft and no setWizardSessionId inside the new onTryAnotherKey body.
    - The confirm-dialog danger path and start_fresh are byte-identical (git diff shows no hunks in those regions).
    - The LOW-2 reasoning is present at the callback, naming the original threat and why it no longer applies.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wiring tests — prove no DELETE is issued, and the deliberate deletes still are</name>
  <read_first>
    - The existing suite that exercises onTryAnotherKey / the wizard delete flows (locate: grep -rn "onTryAnotherKey\|handleDeleteDraft" src --include='*.test.tsx' — extend the suite that mounts the real WizardClient wiring, not a stub)
  </read_first>
  <files>the located wizard suite file(s)</files>
  <behavior>
    - Clicking Try another key: NO fetch with method DELETE to the draft route is issued (spy on fetch); the step transitions to connect_key; the draft/strategy state in the client is untouched
    - Negative control: the confirm-dialog danger path DOES issue the DELETE (the deliberate path still works — this is the test that would catch an over-eager class fix)
  </behavior>
  <action>
    Add both cases to the located suite, testing the WIRING (the real WizardClient callback,
    not a reimplementation — a test of a hand-built callback proves nothing about the call
    site). Use vi.spyOn on global fetch with mockRestore (never vi.stubGlobal — DEF-16-1 and
    the OPS-11 order-sensitive-flake class). ANTI-VACUITY: neuter by re-adding a
    handleDeleteDraft call into onTryAnotherKey (working-tree only) → observe the no-DELETE
    case RED first-hand → restore byte-identical; record the observed message.
  </action>
  <verify>
    <automated>npx vitest run --dir 'src/app/(dashboard)/strategies/new/wizard'</automated>
  </verify>
  <done>Both wiring cases green; neuter cycle observed RED; wizard directory suite green.</done>
  <acceptance_criteria>
    - The positive case fails when the delete is re-added (observed, quoted in SUMMARY).
    - The negative control exercises the real confirm-dialog path, not a synthetic caller.
    - No vi.stubGlobal introduced.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → draft mutation | a remedy-labeled control must not mutate destructively |

## STRIDE Threat Register (ASVS L1, block_on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-10 | Denial of Service (user-inflicted data loss — the recorded class) | onTryAnotherKey | high | mitigate | Pure step transition; deletion only behind explicit confirmation; wiring test proves no DELETE issued |
| T-161-11 | Spoofing / session replay (LOW-2 lineage) | wizard session id | medium | mitigate | No id regeneration + no background delete ⇒ client belief and server state agree by construction; reasoning recorded at the callback |

No package installs — supply-chain row not applicable.
</threat_model>

<verification>
- vitest from the repo root, never a worktree, never wrapped in gstack-evidence.
- The full-suite wave gate (npm run test) is what clears contract tests — file-scoped greens do not.
- CI-gate prose: "would have caught", never "did stop".
</verification>

<success_criteria>
- WIZERR-02 closed: the remedy cannot destroy anything; the deliberate deletes still can, behind their confirmations; LOW-2 re-answered on the record.
</success_criteria>

<output>
Create `.planning/phases/161-wizerr-honest-error-surfaces/161-04-SUMMARY.md` when done, including the LOW-2 re-answer and the observed-RED record.
</output>

## Artifacts this plan produces

- Rewritten `onTryAnotherKey` callback (pure transition) + corrected adjacent comments in `WizardClient.tsx`.
- Two wiring test cases (no-DELETE positive, deliberate-delete negative control).
