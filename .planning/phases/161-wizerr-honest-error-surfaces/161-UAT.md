---
status: partial
phase: 161-wizerr-honest-error-surfaces
source: 161-VERIFICATION.md (3 human_verification items; 4/4 criteria and 13/13 requirements already verified)
started: 2026-09-05T22:00:00Z
updated: 2026-09-05T22:00:00Z
---

## Current Test

[testing paused — 2 items outstanding, both founder-gated; neither is blocked on effort]

## Tests

### 1. Read the sentence on a live MT5 `undetermined` capability verdict
expected: The sentence names "Allow algorithmic trading" (arm 1) if the gateway's Experts setting is off, or the external-Python-API option (arm 2) ONLY if `terminal_info` actually reports `tradeapi_disabled`. Naming the external-Python-API option while that flag is off is the exact defect WIZERR-01 exists to remove.
result: blocked
blocked_by: third-party
reason: |
  PREMISE RE-MEASURED 2026-09-05, unchanged. `railway logs --service quantalyze-analytics
  --environment production` returns **zero** lines matching `mt5` (case-insensitive), and zero
  matching `undetermined` or `tradeapi_disabled`. So the fallback the item itself offered — "read
  the operator surface for the next one that occurs" — still has nothing to read.

  The 2026-08-28 analysis holds and I did not re-derive it: the verdict has NO durable sink
  (`_Mt5ValidateTrace.outcome` reaches `emit_mt5_stage_event` at
  `analytics-service/services/mt5_client.py:205`, which writes a STRUCTURED LOG EVENT, not a row),
  so there is no table to query for a historical `undetermined`, and log retention is short enough
  that a past occurrence would be gone.

  Genuinely founder-only: the arm is selected by the LIVE terminal's `tradeapi_disabled` flag, so
  closing it needs one real MT5 validate against a terminal with "Allow algorithmic trading" OFF —
  broker credentials and a gateway I do not have. The code half is already pinned (flag→cause
  builder verified in source plus a parametrized fence), so a live run adds exactly one fact:
  which input actually arrives.

### 2. KeyPermissionBadge on a key that can no longer be decrypted
expected: The founder can read and act on "This stored key can no longer be decrypted. Reconnect the key — retrying will not help."
result: pass
reported: "RULED 2026-08-26 (SPLIT: prose to the user, structured code to the log and the Sentry breadcrumb). Fact re-verified at HEAD 2026-09-05."
notes: |
  The item stopped needing a broken key once the fact was established in source, leaving only a
  judgment — and the judgment was made. Re-confirmed at HEAD today: the prefix still ships.
  `SyncPreviewStep`'s sibling site is unaffected; the relevant line is

      err.code ? `${err.code}: ${message}` : message

  with the comment two lines above stating the prefix exists "so the displayed text is greppable
  in support tickets". So the string a founder reads today is still
  `KEY_UNDECRYPTABLE: This stored key can no longer be decrypted...` — expected, since the ruling
  is a decision to change it, not the change itself.

  ⛔ STALE ROUTING CORRECTED. `161-VERIFICATION.md` says "Implementation routed to Phase 164.1,
  which already owns phase 161's deferred error-surface items." That is no longer true. The
  2026-09-05 re-partition moved **161-ERRPREFIX to Phase 164.2 CURATED-COPY** — `ROADMAP.md:1100`
  states it ("161-ERRPREFIX moves here from 164.1 — `KeyPermissionBadge.tsx:140` renders a
  sentence a user reads, which is this phase's subject, not gate integrity"), it is 164.2's
  success criterion 6 at `:1109`, and it is in 164.2's `**Requirements**` line at `:1114`. The
  statement still visible in 164.1's section at `:962` is retained lineage, which that phase's
  re-partition block says explicitly is superseded by the owner column.

  So this item is CLOSED as a ruling with a live owner; nothing about it is waiting on 161.

### 3. Draft survives "Try another key" plus a real page reload
expected: The draft and (on a composite) every stored member are still there; the user resumes rather than starting over
result: blocked
blocked_by: third-party
reason: |
  ⭐ THE RECORDED BLOCKER IS GONE — and that is worth stating, because it was the reason given.
  `161-VERIFICATION.md` says "BLOCKED ON A RENDERED VIEWPORT ... this environment's Chrome reports
  `innerWidth/innerHeight = 0` ... nothing is painted and no click can be aimed." That is FIXED:
  this session drove real logged-in Chrome successfully twice — Phase 162 item (c) was discharged
  by clicking through the scenario composer, and Phase 159 test 1 was re-measured on the live
  public pages. Clicks can be aimed now.

  TWO blockers remain, and neither is the display:

  (a) CREDENTIALS, founder-gated. Reaching the state under test needs an API key that FAILS the
      capability gate. I do not supply exchange credentials, and the account this browser holds is
      an ALLOCATOR (`/strategies` 307s to `/allocations`), not a manager walking the wizard.
      ⚠️ Measured while checking reachability: `Try another key` has TWO render sites in
      `SyncPreviewStep.tsx` — `:2607` in the SUCCESS/preview branch beside "Use this key and
      continue", and `:2157` in the branch whose label flips to "Review your keys" when
      `compositeReviewIsAvailable`. So the button is reachable on a HAPPY path. That does NOT make
      the item reachable: the property under test is the transition out of a GATE REFUSAL being
      non-destructive, and clicking the happy-path twin would prove a different thing while looking
      like the same click. Recorded so a later reader does not mistake one for the other.

  (b) IT SHOULD NOT BE A HAND-CLICK. The item's own closing recipe says so: seed a draft, stub the
      validate response to the refusal shape, assert the draft and every stored member survive
      `page.reload()`. A one-off manual look would not be repeatable and would not defend the
      behaviour against the next regression.

  ⛔ STALE ROUTING. The recipe ends "it belongs with the e2e work 164.3 is standing up." 164.3
  DEFERRED VAC-07 by founder decision (`[VAC-07-DEFER]`) and its app-level e2e lane moved to
  **Phase 164.5**, whose item (6) is that lane and whose item (1) repoints
  `scripts/local-stack/run.sh` at the committed baseline so the lane can boot at all. This spec
  should be written there, alongside Phase 159's two items, which are blocked on the same lane.

## Summary

total: 3
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 2

## Gaps

- truth: "An MT5 capability verdict that exists only in a rotating log cannot be verified after the fact by anyone"
  status: resolved
  reason: >
    `161-VERIFICATION.md` recorded the recommendation — "⚠️ Worth pairing with a durable sink when
    it is done — an outcome that exists only in a rotating log cannot be verified after the fact by
    anyone, which is what made this item unclosable for a week" — and it was never booked. MEASURED
    2026-09-05: `grep -rn "durable sink\|emit_mt5_stage_event" TODOS.md .planning/ROADMAP.md`
    returns NOTHING. So the recommendation exists only inside the verification file of the phase it
    blocks, which is the one place nobody planning the fix will read.
    This is not academic. Item 1 above has now been unclosable for over a week for exactly this
    reason, and re-measuring it today cost a Railway query that could only ever return "still
    nothing" — the absence of a sink makes the item permanently unverifiable rather than merely
    not-yet-verified. `_Mt5ValidateTrace.outcome` reaches `emit_mt5_stage_event`
    (`analytics-service/services/mt5_client.py:205`), which writes a structured log event, not a row.
  severity: minor
  test: 1
  root_cause: "A recommendation was written into a phase verification file instead of the backlog."
  artifacts:
    - path: "analytics-service/services/mt5_client.py"
      issue: ":205 — the capability verdict's only sink is a structured log event; nothing durable records which arm fired"
  missing:
    - "Book a TODOS item: give the MT5 capability verdict a durable sink (a row, or a counter that survives log rotation) so an `undetermined` outcome can be read after the fact instead of only while it is on screen."
    - "Natural owner is the MT5 observability work — Phase 164.1's MT5-WEDGE-OBS-01 arm is the closest live mechanism, but that arm probes liveness (-10004/-10005), not capability verdicts, so this is adjacent rather than the same item. Founder call on whether it rides along or waits."
  debug_session: ""
  resolved: |
    ✅ CLOSED 2026-09-12 — the `missing:` clause asked for exactly two things and both exist.
    (1) "Book a TODOS item" — `[MT5-VERDICT-SINK-01]` is booked at `TODOS.md:3362`.
    (2) "Natural owner is the MT5 observability work" — it HAS an owning phase: **164.6
    GATE-HYGIENE**, whose item (11) `[MT5-GATEWAY-LOGIN-01]` names it explicitly
    ("⚠️ ADJACENT to MT5-VERDICT-SINK-01, already in this phase") and draws the distinction this
    gap drew: that entry is SESSION ESTABLISHMENT, this one is a CAPABILITY verdict with no
    durable sink.
    ⚠️ THE UNDERLYING DEFECT IS NOT FIXED and is not claimed to be — the verdict still lives only
    in a rotating log, which is why the sibling human item ("read the sentence on a live MT5
    `undetermined` verdict") remains genuinely BLOCKED: re-measured 2026-09-05, `railway logs`
    returns ZERO matching lines, so there is nothing to read. This gap is closed because it asked
    for a booking with an owner, not for the fix.

- truth: "Both of Phase 161's closing recipes name owners the 2026-09-05 re-partition changed"
  status: resolved
  reason: >
    Item 2's ruling says "routed to Phase 164.1"; 161-ERRPREFIX now belongs to 164.2
    (`ROADMAP.md:1100`, criterion 6 at `:1109`, requirements at `:1114`). Item 3's recipe says "the
    e2e work 164.3 is standing up"; that lane moved to 164.5 after 164.3 deferred VAC-07. Neither
    is wrong about WHAT to do — both are wrong about WHO. A closing recipe that names a phase which
    no longer owns the work sends the next reader to a completed phase, which is the same failure
    Phase 164.3's own gap G2 was raised for.
    Corrected in this file rather than by editing the verification record, which is a dated
    artifact of what was true on 2026-08-24.
  severity: cosmetic
  test: 2
  root_cause: "Re-partitions update the roadmap and the moved items, not the prose in prior phases' verification files that points at them."
  artifacts:
    - path: ".planning/phases/161-wizerr-honest-error-surfaces/161-VERIFICATION.md"
      issue: "two human_verification items name pre-re-partition owners (164.1, 164.3)"
  missing:
    - "No edit to the verification file — it is dated evidence. This UAT is the correction of record."
    - "Consider, at the next re-partition, grepping prior phases' VERIFICATION.md files for the moved item ids; a scope amendment that touches one file is incomplete."
  debug_session: ""
  resolved: |
    ✅ CLOSED 2026-09-12 — both recipes now name owners that exist, and the re-partition drift this
    gap caught is resolved rather than re-stated.
    Recipe 1 (the MT5 `undetermined` sentence) → `[MT5-VERDICT-SINK-01]`, owned by **Phase 164.6
    GATE-HYGIENE** item (11). Recipe 2 (draft survives "Try another key" plus a reload) → it is a
    FOUNDER-UAT item, now listed with its real prerequisites in `.planning/FOUNDER-UAT-v1.20.md`.
    ⭐ This gap was graded `cosmetic` and it was not: an owner that a re-partition has dissolved is
    the same failure as no owner at all, and it is the reason the founder rule says every deferral
    must name a PHASE rather than a note. Two other items in this same sweep had exactly this shape
    — Phase 164.7 criteria 3/4 and Phase 161.1's ACTIVATION, each deferred to something that could
    not carry it, both re-routed to Phase 164.5.1 on 2026-09-12.
