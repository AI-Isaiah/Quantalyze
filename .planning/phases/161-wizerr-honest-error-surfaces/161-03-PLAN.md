---
phase: 161-wizerr-honest-error-surfaces
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/csv_validator.py
  - analytics-service/routers/process_key.py
  - analytics-service/tests/ (csv_validator + process_key envelope suites)
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.test.tsx
  - src/app/api/strategies/csv-finalize/route.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.upstream-arm.test.tsx
  - src/__tests__/csv-finalize-c14-regression.test.ts
autonomous: true
requirements: [WIZERR-12, WIZERR-13]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A dataframe-level pandera failure (column is NaN/absent) renders a message that omits the column clause entirely — the literal string n-a-n never reaches user copy"
    - "The submit path's error envelope forwards the per-row data half in the typed shape {rule, row, message} the upload path already renders — the row detail is no longer discarded at _envelope_error"
    - "The raw failing cell value (failure_case) never appears in copy, logs, or persisted metadata — the existing no-echo discipline survives every edit"
    - "The csv-finalize A2 terminal-status-mismatch 409 renders a sentence describing the ACTUAL case (same track record, different flow) — validated against the arm's own docblock before shipping"
    - "The default CSV_SESSION_REUSED sentence stays byte-identical for its own true case (different track record)"
    - "Zero rows ⇒ the breakdown section is absent (not an empty shell); 1..N rows each render rule + row index + message"
    - statement: "Long rule names and messages wrap without truncating the row index; untrusted cell contents are never echoed (component test asserts both)"
      verification: backstop
  artifacts:
    - path: "analytics-service/services/csv_validator.py"
      provides: "nan-guarded column clause in the SchemaErrors message builder"
    - path: "analytics-service/routers/process_key.py"
      provides: "_envelope_error forwards typed per-row errors into debug_context"
    - path: "src/app/api/strategies/csv-finalize/route.ts"
      provides: "case-specific humanMessage on the A2 terminal-status-mismatch refuse() call"
  key_links:
    - from: "analytics-service/routers/process_key.py"
      to: "src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx"
      via: "debug_context.pandera_errors — the typed row shape the client already reads at ~:123"
      pattern: "pandera_errors"
    - from: "src/app/api/strategies/csv-finalize/route.ts refuse()"
      to: "CsvSubmitStep.upstream-arm.test.tsx fixtures"
      via: "verbatim fixture strings — byte-equality is enforced by hand-verification, not code (IN-05)"
      pattern: "refuse\\("
  prohibitions:
    - statement: "The raw failing cell value (pandera failure_case) MUST NOT be echoed into any message, log line, Sentry event, or persisted envelope — it is untrusted CSV content that can carry PII and is persisted into strategy_verifications metadata (injection surface, not a wording problem)"
      status: resolved
      verification: test
    - statement: "The A2 fix MUST NOT ship an unvalidated technical description — the sentence must be checked against the arm's docblock semantics first; a wrong description is a NEW false sentence"
      status: resolved
      verification: judgment
    - statement: "No refusal is weakened: every arm that refused before still refuses; only classification, copy, and the forwarded data shape change"
      status: resolved
      verification: test
---

<objective>
CSV surfaces tell the truth about their data: the per-row breakdown renders its data half
without leaking the literal not-a-number string or echoing untrusted cell contents
(WIZERR-13), and the csv-finalize A2 409 describes the actual case — same track record,
different flow (WIZERR-12).

Purpose: WIZERR-13 is data-integrity (CONTEXT classes the cell echo as an injection surface);
WIZERR-12 is a false sentence on a founder-hit refusal.
Output: nan-guarded producer, forwarding _envelope_error, case-specific A2 humanMessage with
hand-re-pointed fixture pins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/161-wizerr-honest-error-surfaces/161-RESEARCH.md   <!-- § SC-4: WIZERR-12 (refuse two-liner + IN-05 pins) and WIZERR-13 (three verified facts) -->
@.planning/phases/161-wizerr-honest-error-surfaces/161-UI-SPEC.md    <!-- § Copy Spec WIZERR-12 / WIZERR-13 -->
@analytics-service/services/csv_validator.py
@analytics-service/routers/process_key.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: nan-guard at the producer + forward the data half through _envelope_error</name>
  <read_first>
    - analytics-service/services/csv_validator.py:750-800 — the SchemaErrors loop, the existing no-echo discipline comments (PRESERVE them verbatim)
    - analytics-service/routers/process_key.py:360-419 — _envelope_error rebuilding debug_context with only verification_id
    - src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx:110-140 — the client reader shape (debug_context.pandera_errors)
    - analytics-service/routers/csv.py:100-113 — the upload path that already works (the parity target)
  </read_first>
  <files>analytics-service/services/csv_validator.py, analytics-service/routers/process_key.py, analytics-service/tests/ (the csv_validator and process_key suites)</files>
  <behavior>
    - Column present ⇒ today's sentence shape stays: Column '{col}' failed rule '{rule_name}' at row {row_idx}.
    - Column NaN/None/absent (dataframe-level check) ⇒ the message omits the column clause: Failed rule '{rule_name}' at row {row_idx}.
    - The literal string rendering of float nan never appears in any produced message (pytest asserts over a fixture whose failure_cases carry a NaN column)
    - _envelope_error forwards rows as {rule, row, message} into debug_context.pandera_errors when per-row errors exist upstream; verification_id behavior unchanged; no rows ⇒ key absent (not an empty list vs absent decision left to match what CsvValidationEnvelope treats as absent — mirror the upload path)
    - failure_case is never read into any forwarded row
  </behavior>
  <action>
    In csv_validator.py's SchemaErrors loop, guard the column interpolation: detect
    NaN/None/absent via a small local predicate (pandas NaN is a float — check
    isinstance/float-nan or pd.isna; match the module's existing import surface) and build the
    column-less message for that case. Preserve both existing discipline comments byte-identical.
    In process_key.py's _envelope_error, thread the per-row error list from the validator
    result through to debug_context.pandera_errors in the typed shape — read how the upload
    path (routers/csv.py) surfaces validate_csv's result and mirror that shape exactly, so the
    client renders both paths identically with zero client edits. Add pytest cases for every
    behavior bullet. ANTI-VACUITY: neuter the nan-guard (restore the unguarded interpolation,
    working-tree only) → observe the new pytest case RED first-hand (the literal in the
    message) → restore byte-identical; record the message.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/ -k "csv_validator or process_key" -x -q && python3 -m mypy --strict . | tail -3</automated>
  </verify>
  <done>Producer nan-guarded; submit path forwards the typed data half; upload path untouched; neuter cycle observed RED and restored.</done>
  <acceptance_criteria>
    - The two no-echo discipline comments in csv_validator.py are byte-identical after the edit.
    - No forwarded row object carries any key beyond rule/row/message.
    - The pytest NaN fixture drives a dataframe-level failure (not a hand-built dict that bypasses the real loop) — the wiring is tested, not just a helper.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: client render pins for the data half</name>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.tsx — the render component (rows, absent-section behavior)
    - src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.test.tsx — existing case shape
  </read_first>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.test.tsx</files>
  <behavior>
    - 0 rows ⇒ breakdown section absent from the DOM (not an empty shell)
    - 1..N rows ⇒ each renders rule, row index, message
    - A message built from the column-less producer shape renders cleanly (no dangling separator where the column clause was)
    - A fixture envelope smuggling an extra failure_case-like key with a PII-looking value ⇒ that value never appears in the DOM
    - Long rule/message strings ⇒ the row index is still present in the rendered output (the long-text backstop)
  </behavior>
  <action>
    Extend the existing component test with the behavior cases above. Component production code
    changes ONLY if a case fails for a rendering reason (e.g. the component assumes a column
    clause) — if so, make the minimal render fix and say so in the SUMMARY. Fixtures are
    hand-built wire envelopes (transcribed shape, not imported constants — Oracle Independence).
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/strategies/new/wizard/steps/CsvValidationEnvelope.test.tsx'</automated>
  </verify>
  <done>All behavior cases green; zero-row absence and no-echo pinned.</done>
  <acceptance_criteria>
    - The PII-fixture case can fail: temporarily render the extra key (working-tree only) → observe RED → restore byte-identical (anti-vacuity for the no-echo pin).
    - No loading-branch or success-branch diff on this surface.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: WIZERR-12 — the A2 case-specific humanMessage + hand-re-pointed pins</name>
  <read_first>
    - src/app/api/strategies/csv-finalize/route.ts:1249-1329 — refuse() (the optional humanMessage param exists), the default sentence, and the A2 arm's OWN docblock (~:1293-1310) — the docblock is the truth source for the sentence
    - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.upstream-arm.test.tsx:90-110 — the verbatim fixture strings (BOTH the mocked wire payload and the expected DOM text)
    - src/__tests__/csv-finalize-c14-regression.test.ts — the c14 fixture holders
  </read_first>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.upstream-arm.test.tsx, src/__tests__/csv-finalize-c14-regression.test.ts</files>
  <action>
    TRUTH OBLIGATION FIRST: read the A2 arm's docblock and confirm the case it guards is
    "same track record already committed through a different flow" (manager resubmit onto a
    committed private contribution row, or the mirror). Only then pass the case-specific
    humanMessage on the A2 refuse() call — 161-UI-SPEC § Copy Spec WIZERR-12's proposed
    sentence, adjusted ONLY if the docblock contradicts it (state any adjustment + why in the
    SUMMARY; shipping a wrong description would be a new false sentence). The internal
    reason string (terminal status mismatch detail) stays as-is; the default sentence stays
    byte-identical for its own true case. Re-point the verbatim fixture pins by hand in BOTH
    test files: the fixtures are simultaneously the mocked wire payload and the expected DOM
    text, and their correspondence to the route is enforced by a comment, not code (IN-05) —
    after editing, hand-verify fixture↔route byte-equality again and STATE in the SUMMARY that
    you did, quoting the shared string once. ANTI-VACUITY: with the route sentence changed but
    fixtures not yet re-pointed, run the two suites and observe the RED (that IS the
    falsifiability evidence for these pins); then re-point and observe green.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/csv-finalize-c14-regression.test.ts 'src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.upstream-arm.test.tsx'</automated>
  </verify>
  <done>A2 renders the true-case sentence; default sentence untouched; fixture↔route byte-equality hand-verified and stated.</done>
  <acceptance_criteria>
    - The docblock-vs-sentence validation is recorded in the SUMMARY (what the docblock says, why the sentence matches).
    - The intermediate RED (route changed, fixtures stale) was observed and is quoted in the SUMMARY.
    - c14 regression suite green with the re-pointed fixtures.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CSV → validator → persisted metadata | untrusted cell contents cross into strategy_verifications metadata unless the discipline holds |
| analytics-service → wizard client | debug_context rows render into the DOM |

## STRIDE Threat Register (ASVS L1, block_on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-07 | Tampering / Information Disclosure | per-row CSV breakdown | high | mitigate | Forward {rule,row,message} ONLY; never failure_case; component test proves an injected extra key never reaches the DOM; producer discipline comments preserved |
| T-161-08 | Information Disclosure | _envelope_error forwarding | medium | mitigate | Rows are templated messages built server-side from rule names + indices — no raw cell values exist in the forwarded shape by construction |
| T-161-09 | Repudiation | A2 sentence describes the wrong flow | low | mitigate | Docblock-validated sentence; default sentence retained for its true case |

No package installs — supply-chain row not applicable.
</threat_model>

<verification>
- pytest from `analytics-service/` with `python3`; mypy --strict before close.
- vitest from the repo root, never a worktree, never wrapped in gstack-evidence.
- CI-gate prose: "would have caught", never "did stop".
</verification>

<success_criteria>
- WIZERR-13: data half renders on the submit path; nan and cell-echo both impossible, each pinned by a test that was observed RED under neuter.
- WIZERR-12: A2 sentence true, default sentence untouched, IN-05 byte-equality re-verified by hand and stated.
</success_criteria>

<output>
Create `.planning/phases/161-wizerr-honest-error-surfaces/161-03-SUMMARY.md` when done.
</output>

## Artifacts this plan produces

- nan-guard predicate + column-less message variant in `csv_validator.py`.
- `_envelope_error` per-row forwarding (typed `{rule, row, message}` shape) in `process_key.py`.
- A2 case-specific `humanMessage` string in `csv-finalize/route.ts` + re-pointed verbatim fixtures in two test files.
- New pytest + vitest cases (nan fixture, forwarding shape, zero-row absence, no-echo DOM pin).
