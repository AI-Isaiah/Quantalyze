---
phase: 161-wizerr-honest-error-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/keys/[id]/permissions/route.ts
  - src/app/api/keys/[id]/permissions/route.test.ts
  - src/app/api/keys/[id]/permissions/route.seam.test.ts
  - src/lib/probe-vocabulary.invariant.test.ts
autonomous: true
requirements: [WIZERR-04]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A permissions probe failing with server-classified KEY_UNDECRYPTABLE renders the reconnect remedy with code KEY_UNDECRYPTABLE — never the generic PROBE_FAILED retry sentence"
    - "A genuinely unclassified probe failure still renders PROBE_FAILED with its existing sentence, byte-identical"
    - "The PROBE_* vocabulary law derives its population from route source, asserts the population is non-empty, and pins a hand-typed site count (never derived.length)"
    - "The law was proven falsifiable: a neuter mutation produced an observed RED, then the code was restored byte-identical"
    - statement: "Long remedy sentences wrap within the existing KeyPermissionBadge/envelope mounts without truncation — covered by existing render tests"
      verification: backstop
  artifacts:
    - path: "src/lib/probe-vocabulary.invariant.test.ts"
      provides: "Derived-population coverage law over the keys/[id]/permissions private code vocabulary"
      min_lines: 80
    - path: "src/app/api/keys/[id]/permissions/route.ts"
      provides: "KEY_UNDECRYPTABLE arm keyed on the seam-failure cause, placed before the substring cascade"
  key_links:
    - from: "src/app/api/keys/[id]/permissions/route.ts"
      to: "src/lib/probe-vocabulary.invariant.test.ts"
      via: "law reads the route source via readFileSync and scans the emitted code vocabulary"
      pattern: "permissions/route\\.ts"
    - from: "src/app/api/keys/[id]/permissions/route.ts terminal catch"
      to: "readSeamFailureCause"
      via: "the new arm consults the seam-failure cause the route already attaches (buildSeamFailureCause at ~:286-294) and already reads for the 429 arm (~:487-488)"
      pattern: "readSeamFailureCause"
  prohibitions:
    - statement: "The PROBE_* substring cascade MUST NOT be replaced by the shared classifyKeyValidationError classifier — it gets a law, not a deletion (routed through the shared classifier, five of the route's six real thrown messages fall to UNKNOWN 500; the route's own docblock measured this)"
      status: resolved
      verification: test
    - statement: "No probe arm may render or stamp a Retry-After the upstream did not advertise — absence means no wait was advertised, never zero (TRAP-3)"
      status: resolved
      verification: test
    - statement: "KeyPermissionBadge's render treatment MUST NOT change — MT5-13 (the PROBE_FAILED red text on the success screen) is parked v1.18; do not absorb it, do not foreclose its per-venue capability-flag fix"
      status: resolved
      verification: judgment
---

<objective>
PHASE TRACER. Fix one surface end-to-end in the exact shape every later plan repeats: a
server-classified code the client currently discards (`KEY_UNDECRYPTABLE` falling to
`PROBE_FAILED` "Try again.") starts being consulted and renders an honest remedy — then a
derived-population coverage law is armed over the whole private vocabulary so the class cannot
regrow, and the law is neuter-verified RED-first.

Purpose: WIZERR-04. This is the thinnest slice touching every layer of the phase's motif
(route classification → honest copy → coverage law → anti-vacuity proof). Proving it first
validates the pattern all nine later plans expand.
Output: honest `KEY_UNDECRYPTABLE` arm + `src/lib/probe-vocabulary.invariant.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/161-wizerr-honest-error-surfaces/161-RESEARCH.md   <!-- § SC-3 / WIZERR-04 -->
@.planning/phases/161-wizerr-honest-error-surfaces/161-PATTERNS.md   <!-- §2 law template, §5 anti-vacuity idioms -->
@.planning/phases/161-wizerr-honest-error-surfaces/161-UI-SPEC.md    <!-- § Copy Spec WIZERR-04 -->
@src/app/api/keys/[id]/permissions/route.ts
@src/lib/wizardErrors.invariant.test.ts
@src/lib/seam-wire-vocabulary.invariant.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: KEY_UNDECRYPTABLE honest remedy — one arm, end-to-end</name>
  <read_first>
    - src/app/api/keys/[id]/permissions/route.ts — the seam callback that attaches the cause (~:286-294), the 429 arm that already reads it (~:487-488), the substring cascade (~:541-570), the three curated terminal sentences
    - src/app/api/keys/[id]/permissions/route.seam.test.ts and route.test.ts — existing mocking shape for seam failures
    - src/components/connect/KeyPermissionBadge.tsx:114-141 — the consumer; renders `err.code: message` with zero edits needed
  </read_first>
  <files>src/app/api/keys/[id]/permissions/route.ts, src/app/api/keys/[id]/permissions/route.test.ts, src/app/api/keys/[id]/permissions/route.seam.test.ts</files>
  <action>
    In the terminal catch of the permissions route, add ONE arm BEFORE the substring cascade,
    keyed on the seam-failure cause the route already holds: when
    `readSeamFailureCause(err)?.code === "KEY_UNDECRYPTABLE"`, respond 500 with body
    `{ error: "This stored key can no longer be decrypted. Reconnect the key — retrying will not help.", code: "KEY_UNDECRYPTABLE" }`
    and `NO_STORE_HEADERS`. ("Reconnect" is legal here — the Python curated-message fence scans
    ONLY the Python MT5 constants, verified first-hand in 161-UI-SPEC § Checker Sign-Off; do not
    widen that fence's reach.) Key the arm on this ONE code explicitly — do NOT
    blanket-forward every seam code through the terminal arm (the identity-admission trap,
    RESEARCH Pitfall 4; the cascade stays authoritative for everything else). The existing
    `PROBE_FAILED` sentence ("Could not check key scopes. Try again.") stays byte-identical for
    arms where retry CAN work. Keep `scrubSeamError` logging and the Sentry
    capture-on-terminal-arm-only policy untouched. Sentence casing/wording is the UI-SPEC
    Copy Spec contract — use it verbatim.
    Add route-test cases: (a) a seam failure carrying code KEY_UNDECRYPTABLE renders the new
    body (assert code AND the exact sentence, hand-typed in the test — Oracle Independence:
    transcribe the sentence, never import it from the route); (b) negative control: an
    unclassified thrown error still answers PROBE_FAILED with its existing sentence; (c) the
    429 and circuit-open arms are untouched (existing cases stay green).
  </action>
  <verify>
    <automated>npx vitest run 'src/app/api/keys/[id]/permissions/route.test.ts' 'src/app/api/keys/[id]/permissions/route.seam.test.ts'</automated>
  </verify>
  <done>KEY_UNDECRYPTABLE renders the reconnect remedy end-to-end (route body → KeyPermissionBadge's existing `code: message` render needs zero edits); unclassified failures still answer PROBE_FAILED byte-identical.</done>
  <acceptance_criteria>
    - The new arm sits BEFORE the substring cascade and consults only `seamFailure.code === "KEY_UNDECRYPTABLE"`.
    - The response sentence names the real action (reconnect) and contains no retry framing.
    - All pre-existing arms (429 w/ forwarded Retry-After, CIRCUIT_OPEN, the three cascade sentences) byte-identical — assert via existing tests staying green with no fixture edits.
    - Test sentences are hand-typed transcriptions, not imports of the route constant.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: derived-population law over the private PROBE_* vocabulary (Wave 0)</name>
  <read_first>
    - src/lib/seam-wire-vocabulary.invariant.test.ts — the lighter derived-population form to follow (hand-typed roster + both-halves disposition + vacuity floor), per the locked CONTEXT decision and the recovered-ledger shape note
    - src/lib/wizardErrors.invariant.test.ts:1-120, :155-300, :1022-1120 — stripCommentsPreserveLines usage, Oracle-Independence docblock, SELF-TEST idiom
    - src/lib/source-scan.ts (or wherever stripCommentsPreserveLines is exported) — import path
  </read_first>
  <files>src/lib/probe-vocabulary.invariant.test.ts</files>
  <behavior>
    - Population: scanner over the comment-stripped permissions route source derives every emitted private code (`CIRCUIT_OPEN`, `PROBE_RATE_LIMITED`, `PROBE_BACKEND_UNAVAILABLE`, `PROBE_TIMEOUT`, `PROBE_FAILED`, `KEY_UNDECRYPTABLE` after Task 1 — MEASURE at HEAD, do not trust this list blindly)
    - Both halves: every scanner-derived code is in the hand-typed roster, AND every roster member has a user-facing sentence literal in the route
    - Vacuity floor: the derived population is non-empty AND its size equals a hand-typed literal (NEVER derived.length)
    - SELF-TEST positive: the scanner reads a code out of BOTH real emitter shapes in this route — the `{ error: "...", code: "X" }` json-literal shape AND the ternary string-literal shape (~:562-565)
    - SELF-TEST negative: a code that appears only inside a comment is NOT counted
  </behavior>
  <action>
    Create `src/lib/probe-vocabulary.invariant.test.ts` with `// @vitest-environment node` first
    line, following the venue-vocabulary law form. IMPORTANT measured fact: this route's
    emitters are error-first json literals plus a ternary of bare string literals — the
    incumbent wizardErrors emitterRe would NOT see them. Build this law's OWN scanner against
    the route's real shapes (both proven by the SELF-TESTs); do NOT restructure working route
    arms just to satisfy a scanner shape (this deviates from 161-UI-SPEC's parenthetical
    "code-first-literal" note deliberately: that note assumed reuse of the incumbent emitterRe;
    per the UI-SPEC's own preamble the copy and invariants are the contract, mechanics are
    planner-adjustable). Docblock states the emitter predicate in full prose so the count is
    reproducible without reading the regex, and states WHY the cascade is deliberately
    un-shared with classifyKeyValidationError (cite the route's own docblock ~:530-540).
    ANTI-VACUITY (non-negotiable): after the law is green, neuter production behavior — delete
    the Task-1 KEY_UNDECRYPTABLE arm from the route (working-tree only) — observe the law AND
    the Task-1 route test go RED first-hand, record the observed failure message for the
    SUMMARY, then restore byte-identical (verify with git diff --stat == clean for the route).
    Then run the second mutation: blank one roster entry → law RED → restore.
  </action>
  <verify>
    <automated>npx vitest run src/lib/probe-vocabulary.invariant.test.ts && npx vitest run 'src/app/api/keys/[id]/permissions/route.test.ts'</automated>
  </verify>
  <done>Law green at HEAD; two neuter mutations each produced observed RED and were restored byte-identical; SELF-TESTs prove the scanner reads both real emitter shapes and rejects comments.</done>
  <acceptance_criteria>
    - Hand-typed roster + hand-typed count literal; no assertion compares the derivation to itself.
    - Population non-empty assertion present (an empty-set law passes trivially — forbidden).
    - Both observed RED messages recorded in the SUMMARY (the three-part house shape: what was mutated → observed RED → restored + counts).
    - The route file is byte-identical after the neuter cycles (git diff clean).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed client → permissions route | probe failures carry upstream detail; raw messages must stay server-side |
| permissions route → analytics seam | seam failures carry machine codes + Retry-After |

## STRIDE Threat Register (ASVS L1, block_on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-01 | Information Disclosure | permissions terminal catch | high | mitigate | New arm answers a STATIC curated sentence; raw thrown message never reaches the body; `scrubSeamError` logging preserved on every edited arm |
| T-161-02 | Denial of Service | Retry-After stamping | medium | mitigate | Absent upstream value ⇒ header omitted entirely (existing :503-521 pattern untouched); no invented wait |
| T-161-03 | Tampering | law file reads route source | low | accept | Test-time filesystem read of in-repo source only; no external input |

No package installs this phase — supply-chain row not applicable (RESEARCH § Standard Stack: no new packages).
</threat_model>

<verification>
- `npx vitest run` targeted suites above from the REPO ROOT (never a worktree — no node_modules there; never wrapped in gstack-evidence).
- Contract tests in `src/__tests__/contracts/` scan all of src/ — only the wave-level full `npm run test` is authoritative for them; a file-scoped green does not clear them.
- Verification prose: CI gates are advisory at merge (no branch protection) — write "would have caught", never "did stop".
</verification>

<success_criteria>
- WIZERR-04 both halves: the reconnect remedy ships AND the derived-population law is armed and proven falsifiable.
- Zero behavior change on any arm other than the one new KEY_UNDECRYPTABLE arm.
</success_criteria>

<output>
Create `.planning/phases/161-wizerr-honest-error-surfaces/161-01-SUMMARY.md` when done, including both observed-RED records.
</output>

## Artifacts this plan produces

- `src/lib/probe-vocabulary.invariant.test.ts` — NEW coverage law (population: the permissions route's private code vocabulary).
- `KEY_UNDECRYPTABLE` route arm + curated remedy sentence (route-local; NOT a new WizardErrorCode member — the code already exists on the wire).
- New route-test cases (positive, negative control) in the two existing permissions test files.
