---
phase: "164.5.1"
slug: "cronrepoint-the-live-match-engine-cron-row-is-repointed-at-the-mechanism-the-repo-actually-describes-and-the-migration-vs-runbook-rule-is-settled-first-inserted"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-16"
---

# Phase 164.5.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `164.5.1-RESEARCH.md` § Validation Architecture. The planner fills the
> per-task rows; the commands and gaps below are MEASURED and are not placeholders.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (Python)** | pytest — `analytics-service/pytest.ini` (`testpaths = tests`, `pythonpath = .`, `asyncio_mode = auto`) |
| **Framework (Node)** | vitest — `vitest.config.ts`, sharded 2-way in CI and merged by the `frontend-coverage` job |
| **Quick run command (Python)** | `cd analytics-service && pytest tests/test_match_router.py -x` |
| **Quick run command (Node)** | `npx vitest run src/__tests__/prod-prober-wiring.test.ts` |
| **Full suite (Python)** | `cd analytics-service && pytest --cov=services --cov=routers --cov=main_worker --cov-fail-under=80` |
| **Full suite (Node)** | `npx vitest run --coverage` |
| **Estimated runtime** | Python full suite ~several minutes; targeted vitest file ~seconds |

⚠️ **pytest runs ONLY from `analytics-service/`.** From the repo root the cassettes miss and the
suite makes LIVE broker calls. Use `analytics-service/.venv/bin/python` where an interpreter is
named explicitly.
⛔ Never start a local `uvicorn main:app` — it claims REAL production compute jobs.
⚠️ The Python coverage gate is `--cov-fail-under=80`; read the live value from the `python` job
in `.github/workflows/ci.yml` BY SYMBOL, not from a line number restated here
(`[164.7-CITATION-DRIFT-01]`).

---

## Sampling Rate

- **After every task commit:** the matching quick-run command above.
- **After every plan wave:** full Python suite AND full vitest suite.
- **Before `/gsd-verify-work`:** both full suites green.
- **Wave B is NOT CI-gated.** Its PROD statements are gated by D4 — three reviewers
  (`migration-reviewer`, `rls-policy-auditor`, `silent-failure-hunter`) plus founder execution.
  That gate cannot be automated and must not be reported as if CI covered it.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | A | Criterion 8 — kill switch fails CLOSED | Sentry `QUANTALYZE-1D` | After `DB_READ_MAX_RETRIES` consecutive gateway-timeout-shaped exceptions, `_engine_is_enabled` returns the closed state with a DISTINCT status, never `True` | unit | `cd analytics-service && pytest tests/test_match_router.py -k kill_switch -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | A | Criterion 9 — batching | `QUANTALYZE-11`, `QUANTALYZE-X` | `cron_recompute()` with N+1 allocators returns `status="partial"` + `next_cursor`; a second call resumes from it and no call approaches the 60s ceiling | unit | `cd analytics-service && pytest tests/test_match_router.py -k TestCronResponseShape -x` | ⚠️ extend `TestCronResponseShape`, do not create fresh | ⬜ pending |
| TBD | TBD | A | Criterion 3 — pre-flight aborts | — | The pre-flight verb exits non-zero and writes NOTHING against a deliberately mismatched manifest; exits 0 against a matching one | unit (self-test scenario) | `node scripts/prod-prober/run.mjs --self-test` | ⚠️ harness exists, scenario missing | ⬜ pending |
| TBD | TBD | A | `T-OPEN-04` residual — the GATE | — | CI goes RED when the arm's exported `NORMALIZATION` and the manifest's `normalization` key disagree | unit | `npx vitest run src/__tests__/prod-prober-wiring.test.ts` | ⚠️ file exists, case missing | ⬜ pending |
| TBD | TBD | B | Criterion 7 — `service_role` timeout | `[164.5.1-GATEWAY-CEILING-INVERSION]` | `pg_roles.rolconfig` reads `statement_timeout=45s` for `service_role` AFTER the apply | manual-only | — | N/A | ⬜ pending |
| TBD | TBD | B | Criterion 2 — zero cron drift | `CRON-DRIFT-01` | After the repoint + re-capture, the cron-drift arm reports ZERO drift for `match_engine_cron` | manual (prober run) | — | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] A test asserting `_engine_is_enabled` returns the fail-CLOSED status after
      `DB_READ_MAX_RETRIES` consecutive gateway-timeout-shaped exceptions — criterion 8.
      ⛔ Anti-vacuity: this one is in scope for neuter → observe RED → restore, because it pins
      a SAFETY control. Prove the neuter APPLIED before believing the result.
- [ ] An extension of `TestCronResponseShape` (`analytics-service/tests/test_match_router.py`)
      covering the `status="partial"` + `next_cursor` branch — criterion 9.
- [ ] A new `--self-test` scenario for the pre-flight verb (mismatched manifest → abort, nothing
      written) — criterion 3. ⚠️ Bump `SELF_TEST_SCENARIOS` in `scripts/prod-prober/run.mjs` AND
      its pinned mirror in `src/__tests__/prod-prober-wiring.test.ts` in the SAME edit — that
      file carries its own ONE-EDIT RENUMBER rule.
- [ ] A vitest case asserting `manifest.normalization === NORMALIZATION` — the
      `[164.8.5-MANIFEST-SIDE-LOOP-DEAD]` / `T-OPEN-04` residual gate.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `service_role` carries `statement_timeout=45s` | Criterion 7 | It is a production role configuration; criterion 7 explicitly requires reading `pg_roles.rolconfig` on PROD AFTER the apply and RECORDING the output | Run the P0 marker query FIRST and record its OUTPUT; then `SELECT rolname, rolconfig FROM pg_roles WHERE rolname = 'service_role';` and paste the result |
| Cron-drift arm reports ZERO drift | Criterion 2 | Requires the live PROD `cron.job` table and a prober run | After the repoint and re-capture, dispatch `prod-prober.yml` and record the cron-drift arm's line |
| `match_engine_cron` repoint + `ledger_refresh_fanout` registration | D1, `[164.7-ACTIVATION-DEFERRED]` | D4: founder runs or approves the live `cron.schedule` | Follow the runbook section written by this phase, in one session, re-capturing the manifest once at the end |
| P3-C re-entry gate | `[164.7-ACTIVATION-DEFERRED]` | Reads live PROD freshness | ⛔ Repoint `cj.completed_at` → `cj.updated_at` in BOTH copies FIRST — `compute_jobs` has no `completed_at` and the query aborts 42703 (measured 2026-09-16). Budget for a DEFER outcome; its last real reading FAILED the bar |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Wave B's manual-only rows are labelled manual, never counted as automated coverage
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
