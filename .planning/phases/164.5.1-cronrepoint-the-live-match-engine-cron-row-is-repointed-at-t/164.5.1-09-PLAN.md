---
phase: 164.5.1
plan: 09
type: execute
wave: 4
depends_on:
  - 164.5.1-03
  - 164.5.1-05
  - 164.5.1-06
  - 164.5.1-07
  - 164.5.1-08
files_modified:
  - scripts/prod-prober/cron-manifest.json
  - .planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-PROD-SESSION.md
  - TODOS.md
autonomous: false
requirements:
  - CRON-DRIFT-01
  - VAULTTICK-EMPTYKEY-01
  - 164.7-ACTIVATION-DEFERRED
  - 164.7-VAULT-ABSENT-RULE
  - 164.8.5-MANIFEST-SIDE-LOOP-DEAD

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Three reviewers — `migration-reviewer`, `rls-policy-auditor`, `silent-failure-hunter` — ran and their findings were dispositioned BEFORE any production statement (criterion 5 / D4). That gate cannot be automated and is never reported as if CI covered it"
    - "The database-marker query ran FIRST in the session and its OUTPUT is recorded verbatim in the execution record — the query text is not evidence"
    - "PROD's `cron.job` jobid 1 command is `SELECT public.match_engine_cron_tick();` after the session, with the jobid, the `0 * * * *` schedule and `active` UNCHANGED, read back and recorded"
    - "`pg_roles.rolconfig` for `service_role` reads an explicit `statement_timeout` after the migration applied, read back on PROD and recorded as output (criterion 7's own oracle)"
    - "`scripts/prod-prober/cron-manifest.json` was re-captured ONCE, at the end of the session, by `captureManifest` — never hand-edited — and committed as its own reviewed diff"
    - "After the re-capture, a prod-prober run reports ZERO `cron-drift` defects for `match_engine_cron` (criterion 2), and the recorded line is the arm's own output, not a summary of it"
    - "`[VAULTTICK-EMPTYKEY-01]` is closed by a READING of the live command after the repoint, not by a repo-side measurement — a repo measurement never settles a production claim in this phase"
    - "If P3-C fails its bar the activation DEFERS and that is recorded as a legitimate outcome; the repoint half still completes and is still re-captured"
    - statement: "The first hourly tick after the repoint returns HTTP 2xx and records a `duration_s` materially below the 44.67 s baseline, read from `net._http_response`"
      verification: backstop
  artifacts:
    - path: ".planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-PROD-SESSION.md"
      provides: "The execution record: every marker, verify-back and gate OUTPUT pasted, with the DEFER disposition"
    - path: "scripts/prod-prober/cron-manifest.json"
      provides: "The re-captured oracle, generated by captureManifest and reviewed command-by-command before commit"
    - path: "TODOS.md"
      provides: "`[CRON-DRIFT-01]` LIVE-ROW half, `[VAULTTICK-EMPTYKEY-01]`, `[164.7-VAULT-ABSENT-RULE]` and `[164.7-ACTIVATION-DEFERRED]` dispositioned from the recorded outputs"
  key_links:
    - "the three reviewers -> the D4 gate -> the founder running or approving the live `cron.schedule`"
    - "`preflightCronRepoint` (plan 04) -> abort-before-write -> the live repoint"
    - "the repoint -> `match_engine_cron_tick()`'s `btrim(v_key) = ''` guard becoming REACHED -> `[VAULTTICK-EMPTYKEY-01]`"
    - "`captureManifest` -> the committed oracle -> the hourly `cron-drift` arm reporting zero drift"
  prohibitions:
    - "No PROD write may be performed without the database-marker query's recorded OUTPUT. `current_database()` is `postgres` on BOTH projects and proves nothing; a NULL marker means re-set it, never proceed on a guess."
    - "Never hand-edit `scripts/prod-prober/cron-manifest.json` to make the drift arm green. The sha is REGENERATED by capture, never typed — a hand-edited oracle is the adversary this whole arm models."
    - "Never report the D4 reviewer gate as if CI covered it. Wave B is NOT CI-gated and saying otherwise converts a human control into a claim."
    - "Never keep a decorative `DO` wrapper around the repointed call to satisfy a grep. If `vault-absent` fires against `SELECT public.match_engine_cron_tick();`, that is a regression to investigate — `[164.7-VAULT-ABSENT-RULE]` forbids the wrapper by name."
    - "Never run `supabase db push`, `supabase db reset --linked`, or any `--project-ref` / `--db-url` command from this checkout. They all target PRODUCTION here; the apply path is merge -> apply-test -> the Production environment's human reviewer gate."
    - "Never paste a secret, an MT5 account number, a broker server name or an absolute home path into the execution record or the manifest — the repository is PUBLIC and both files are tracked."
    - "Never convert a DEFER into a pass by argument. P3-C's last real reading FAILED the bar; a deferred activation is a recorded outcome, not a failed session to be talked past."
---

<objective>
Wave B: the live production session. This is the only plan in the phase that writes to PROD, and it
writes to the most sensitive surface in the 164.x series — `cron.job` jobid 1, a credential-bearing
row whose command reaches `vault.decrypted_secrets`.

⚠️ PROD is currently CORRECT (verified 2026-09-01, `net._http_response` id 3485 returned 200). This
closes a REPRODUCIBILITY gap, not an outage — there is no time pressure, which is exactly why the
gates below are affordable.

Locked decisions recorded and executed here:
- **D4 (D-04)** — the PROD apply stays a HUMAN gate. Three reviewers (`migration-reviewer`,
  `rls-policy-auditor`, `silent-failure-hunter`) before ANY production statement, and the founder
  runs or approves the live `cron.schedule`. Everything the phase decided removes GUESSWORK, not
  OVERSIGHT.
- **D1 (D-01)** — the registration statement is run from the runbook, never from a migration.
- **D3 (D-03)** — the re-capture is `captureManifest`, run once, at the end.

⛔ **SCHEMA-PUSH OVERRIDE, restated because this is the plan most likely to reach for it.** This
checkout's Supabase CLI is linked to PRODUCTION: `supabase/.temp/project-ref` holds the same ref
`src/lib/test-safety.ts` pins as prod, so `db push`, `db reset --linked`, `--project-ref` and
`--db-url` all write to PROD from this directory. The link is deliberate and must not be "fixed" by
unlinking. Plan 03's migration reaches PROD through merge -> `supabase-migrate.yml`'s `apply-test`
(shared TEST, advisory key `61616158`, marker query first) -> PROD's `apply` behind the
`Production` environment's human reviewer gate. No local push, in this plan or any other.

⚠️ **Wave B is NOT CI-gated**, and its manual verifications must never be counted as automated
coverage. `164.5.1-VALIDATION.md` marks these rows `manual-only` for that reason.

Purpose: point the live row at the mechanism the repo actually describes, and take the readings
that prove it.
Output: an execution record of OUTPUTS, a re-captured oracle, and four dispositioned backlog items.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-CONTEXT.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-08-SUMMARY.md
@docs/runbooks/match-engine.md
@docs/runbooks/ledger-refresh-go-live.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: D4 — three reviewers before any production statement</name>
  <read_first>
    - `.planning/phases/164.5.1-.../164.5.1-CONTEXT.md` § "Locked by the ROADMAP", first bullet —
      D4 verbatim.
    - `docs/runbooks/match-engine.md` § the go-live section written by plan 08 — the exact
      statements that will be run, so the reviewers review the real text.
    - `supabase/migrations/20260916120000_service_role_statement_timeout.sql` — plan 03's migration,
      which is the other production statement in this phase.
    - `.planning/phases/164.5.1-.../164.5.1-03-SUMMARY.md` through `164.5.1-08-SUMMARY.md` — what
      actually shipped, since a reviewer reviewing the plan rather than the result reviews the wrong
      thing.
  </read_first>
  <decision>
Production DDL on a credential-bearing cron row, plus a role-level GUC change, are about to be
applied to PROD. Criterion 5 requires three reviewers first, and the founder runs or approves the
live `cron.schedule`.

Run all three specialist reviewers against the shipped diff and the runbook text:
`migration-reviewer`, `rls-policy-auditor`, `silent-failure-hunter`. Disposition every finding —
fixed, or recorded with a reason — BEFORE proceeding. Then the founder confirms the session may
begin.

⛔ This gate cannot be automated and `workflow.auto_advance` does not apply to it. Do not report it
as covered by CI; Wave B has no CI coverage at all.
  </decision>
  <context>
Criterion 5 and D4 (D-04) both require three named reviewers before ANY production statement, and
the founder running or approving the live `cron.schedule`. PROD is currently CORRECT (verified
2026-09-01, `net._http_response` id 3485 returned 200), so this closes a reproducibility gap and
not an outage — there is no time pressure, which is exactly why the gate is affordable. Plans 01-08
have landed and are CI-green; nothing in Wave A depends on Wave B, so holding here costs nothing
already built.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed — reviewers clean, founder approves the session</name>
      <pros>All three reviewers ran against the SHIPPED diff and the runbook text, every finding is
      dispositioned, and the session can run in one sitting as CONTEXT.md Area 3 requires (one
      session, one re-capture).</pros>
      <cons>Commits to a live write on a credential-bearing production row; the pre-repoint command
      survives only in the manifest's pre-capture state and this session's record.</cons>
    </option>
    <option id="fix-first">
      <name>Fix first — a reviewer finding changes a statement or the runbook</name>
      <pros>Fixes land before PROD sees them rather than after.</pros>
      <cons>A fix round is where regressions enter: a new code path is outside every existing gate
      by default and the suite stays green. Re-run the affected reviewer and re-present this
      checkpoint rather than assuming the fix is free.</cons>
    </option>
    <option id="hold">
      <name>Hold — do not run a live session yet</name>
      <pros>Plans 01-08 stay landed and shippable on their own; the kill switch, the timeout and
      the batching are the degrading half and they are already closed.</pros>
      <cons>`CRON-DRIFT-01`'s LIVE-ROW half, `[VAULTTICK-EMPTYKEY-01]` and
      `[164.7-ACTIVATION-DEFERRED]` stay open, and the phase cannot be sealed.</cons>
    </option>
  </options>
  <resume-signal>Reply `proceed`, `fix-first` (naming the finding), or `hold`. For `proceed`, paste the three reviewers' verdicts and the founder's approval date into the SUMMARY first.</resume-signal>
  <acceptance_criteria>
    - The SUMMARY names all three reviewers and, per reviewer, either "no findings" or the
      findings with their dispositions.
    - The founder's approval to begin the session is recorded with its date.
    - No production statement was run before this checkpoint cleared.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this is a human gate; criterion 5 is satisfied by the recorded reviewer dispositions and the founder's approval, and there is no automated substitute</automated>
  </verify>
  <done>Three reviewers have run, every finding is dispositioned, and the founder has approved the
  session.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: The live session — repoint, activate or defer, and record every OUTPUT</name>
  <precondition>Plan 03's migration has APPLIED to PROD through `supabase-migrate.yml` (merge ->
  `apply-test` green -> PROD `apply` approved in the GitHub UI). Confirm from the workflow run,
  not from the repo: a migration file in the tree proves nothing about what applied. If it has not
  applied, halt — criterion 7's read-back has nothing to read.</precondition>
  <reversibility rating="one-way">`cron.schedule` on jobid 1 replaces the hand-repaired 2026-09-01
  command that exists in NO migration. Once overwritten, the only surviving copy of that text is
  `scripts/prod-prober/cron-manifest.json`'s pre-re-capture state and this session's own record.
  The founder is walking through a door on a credential-bearing production row; task 1 is the
  confirmation before it.</reversibility>
  <files>.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-PROD-SESSION.md</files>
  <read_first>
    - `docs/runbooks/match-engine.md` § the go-live section — follow it; do not improvise from this
      plan. The runbook is the procedure and this task is its execution record.
    - `docs/runbooks/ledger-refresh-go-live.md` § P0, § P3 Part C (the repaired P3-C), § Step 1
      (1a/1b/1d) and § Step 2 — the activation half's own steps, referenced rather than duplicated.
    - `.planning/phases/164.5.1-.../164.5.1-04-SUMMARY.md` — the pre-flight verb's CLI form and
      exit codes.
    - `CLAUDE.md` § "Which database am I on? (ask FIRST, every time)" — the marker query and why
      `current_database()` proves nothing.
  </read_first>
  <action>
⛔ FOUNDER ACTION. Follow `docs/runbooks/match-engine.md`'s go-live section end to end in ONE
session, recording every OUTPUT — not every statement — into
`.planning/phases/164.5.1-.../164.5.1-PROD-SESSION.md`. Pasting a query is not evidence; pasting
its result is. This is the `[164.7-P3C-PRESENCE-ORACLE]` vacuity the gate exists against.

What the record must contain, in order:

1. **The marker query's OUTPUT**, verbatim, naming PRODUCTION. A NULL marker means the
   `COMMENT ON DATABASE` was lost — re-set it before writing, do not proceed on a guess.
2. **`pg_roles.rolconfig` for `service_role`**, read AFTER plan 03's migration applied, pasted as
   output. This is criterion 7's own oracle and the statement is not a substitute for it. Record
   the sibling roles' values in the same read so the inversion's closure is visible.
   ⚠️ If it comes back unchanged or the apply returned `42501`, record that verbatim and STOP the
   criterion-7 leg — the mechanism needs a different answer and that is a finding, not a failure to
   work around with a console statement.
3. **The pre-flight verb's OUTPUT and exit code.** Any non-zero ABORTS the session.
4. **The repoint's verify-back OUTPUT** — `jobid`, `jobname`, `schedule`, `command`, `active` — with
   the jobid shown UNCHANGED from before.
5. **The `[VAULTTICK-EMPTYKEY-01]` reading**: the live command now reaches the hardened callable.
   Record it as a reading of `cron.job.command`, and record the narrower residual honestly —
   `btrim()` trims spaces only, not tabs or newlines, and a tab-only secret is explicitly NOT closed
   by this.
6. **P3-C's OUTPUT**, from the repaired query, with the disposition: PASS (both kinds present, both
   fresh within the one-day bar) or DEFER. ⛔ Its last real reading FAILED the bar; budget for
   DEFER rather than assuming the gate opens.
7. **If P3-C passes**: the flag flip's and the `ledger_refresh_fanout` registration's verify-back
   OUTPUTs, plus the deliberate exercise of the all-candidates-failed branch of the ledger fan-out —
   a branch that has never run and becomes reachable only at activation. Record what was done to
   reach it and what it did.
   **If P3-C defers**: record the DEFER with its reading, and note that the repoint half is complete
   and Step 4 still runs for it alone.
8. **Freshness reading**: `strategy_analytics.computed_at`, NEVER `api_keys.last_sync_at` — the
   latter reports recent while the analytics behind it are days stale, and that lie is what Phase
   161.1 was written against.
9. **The ONE `captureManifest` invocation's OUTPUT**, at the end of the session.

⛔ The repository is PUBLIC and both this record and the manifest are tracked. Never paste a secret,
an MT5 account number, a broker server name or an absolute home path.
  </action>
  <instructions>
**Already automated by this phase, so the founder does not repeat it:** the retry seam and the
fail-closed kill switch (plan 02), the worker seam (plan 05), the batching and its cursor (plan 06),
the `statement_timeout` migration and its static gate (plan 03 — applied through merge ->
`apply-test` -> the `Production` environment's human reviewer gate, never a local push), the
pre-flight verb and its abort proof (plan 04), the normalization CI gate (plan 07), and the whole
procedure with its verify-backs, `Expected:` lines and DEFER branch (plan 08).

**The ONE thing requiring a human:** running the live statements against PRODUCTION and recording
their OUTPUT. `cron.schedule` on jobid 1, the `system_flags` flip and the `ledger_refresh_fanout`
registration are founder-run or founder-approved per D4 (D-04); there is no API path that would
make them safe to automate, and the browser SQL editor and the CLI have no automated production
guard at all — the `COMMENT ON DATABASE` marker is the only control.

Open `docs/runbooks/match-engine.md`'s go-live section and follow it top to bottom in one sitting.
Paste each step's RESULT into
`.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-PROD-SESSION.md`
as you go, rather than reconstructing the session afterwards. If the pre-flight exits non-zero,
stop and report — that is the gate doing its job. If P3-C fails its bar, take the DEFER branch: it
is a recorded outcome, not a failed session.
  </instructions>
  <verification>
Claude can check, after the session and without any further production write: that
`164.5.1-PROD-SESSION.md` contains all nine numbered OUTPUT sections; that the recorded
`cron.job.command` for `match_engine_cron` is the bare one-line call with no wrapper; that the
recorded jobid matches the pre-session jobid; that the recorded `pg_roles.rolconfig` names an
explicit `statement_timeout` for `service_role`; that the record contains no credential material,
account number or absolute home path; and — in task 3 — that the re-captured manifest's
`normalization` matches the arm's exported constant and that jobid 1's `command_sha256` recomputes
from its own `command`.
  </verification>
  <resume-signal>Reply `session complete` when `164.5.1-PROD-SESSION.md` holds every OUTPUT, or `deferred` naming the gate that stopped it, or `aborted` with the pre-flight's exit code and log line.</resume-signal>
  <acceptance_criteria>
    - `164.5.1-PROD-SESSION.md` exists and contains, as pasted OUTPUT: the marker result, the
      `pg_roles.rolconfig` read, the pre-flight's exit code, the repoint verify-back showing an
      unchanged jobid, the live `cron.job.command` after the repoint, P3-C's result with its
      disposition, the `strategy_analytics.computed_at` reading, and the capture's summary line.
    - The recorded `cron.job.command` for `match_engine_cron` is the one-line call to
      `public.match_engine_cron_tick()` with no wrapper.
    - Every section of the record is an OUTPUT; no section consists solely of a query.
    - If the activation deferred, the record says so explicitly and names the failing reading.
    - The record contains no secret, no account number, no broker server name and no absolute home
      path.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this is a live PROD session behind the D4 human gate; `164.5.1-VALIDATION.md` marks these rows manual-only and there is no automated substitute</automated>
    <human-check>Read `164.5.1-PROD-SESSION.md` and confirm each of the nine numbered items is present as pasted OUTPUT rather than as a restated query, and that the jobid in the repoint verify-back matches the jobid recorded before the session.</human-check>
  </verify>
  <done>The live session ran in one sitting, every gate's OUTPUT is recorded, and the activation is
  either complete or explicitly deferred with its reading.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the re-captured oracle, confirm zero drift, and disposition the backlog</name>
  <precondition>Task 2's session completed and `captureManifest` produced a candidate manifest.
  The candidate must have been generated BY the tool — if the file was edited by hand at any point,
  discard it and re-capture; a hand-edited sha is the adversary the arm models.</precondition>
  <files>scripts/prod-prober/cron-manifest.json, TODOS.md, .planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-PROD-SESSION.md</files>
  <read_first>
    - `.github/workflows/prod-prober.yml` — its `workflow_dispatch` inputs, in particular the
      `capture-manifest` mode that uploads the oracle as an ARTIFACT for human review and never
      commits it. That is how the capture reaches the repo: an artifact, reviewed, then moved into
      place as a commit.
    - `docs/runbooks/ledger-refresh-go-live.md` § "2e — re-capture the cron manifest" — the
      instruction to read the `command` text of every job in the resulting diff before committing,
      and the `Expected:` / `Abort if:` pair.
    - `scripts/prod-prober/cron-manifest.json` as committed today — the baseline the diff is read
      against.
    - `TODOS.md` entries `[CRON-DRIFT-01]`, `[VAULTTICK-EMPTYKEY-01]`, `[164.7-VAULT-ABSENT-RULE]`
      and `[164.7-ACTIVATION-DEFERRED]`.
    - `.planning/phases/164.5.1-.../164.5.1-07-SUMMARY.md` — the normalization gate, which now
      guards this file and must stay green after the new capture lands.
  </read_first>
  <action>
Move the captured manifest into `scripts/prod-prober/cron-manifest.json` and commit it as its own
reviewed change. Before committing, read the `command` text of EVERY job in the diff — the manifest
is a committed, world-readable file in a PUBLIC repository and each command string was individually
approved for publication when it was first captured.

`Expected:` the diff changes jobid 1's `command` and `command_sha256`, changes `captured_at`, and —
if the activation ran — ADDS exactly one job, `ledger_refresh_fanout`, `25 * * * *`, active.
`Abort if:` any other job moved. A second unexplained difference means PROD drifted somewhere this
procedure is not looking, and committing the manifest would bless it.

⚠️ Expect manifest-side `cron-secret-in-command` findings nobody has seen before. Clearing
`manifest-invalid` re-animates `compareManifest`'s manifest-side hygiene loop, which has been dead
on every production run for weeks — measured, a valid manifest yields 1 manifest-side credential
defect while v1 / a wrong schema version / a marker mismatch yield 0. These are NOT new leaks; they
are a check that has not run for weeks starting to run again. Record each one and disposition it
explicitly rather than letting it read as an alarm. ⛔ A credential LIVE IN PROD is still reported
on every path, so none of this is a reason to soften the arm.

Then dispatch `prod-prober.yml` in probe mode and record the `cron-drift` arm's own output line for
`match_engine_cron`. `Expected:` zero drift (criterion 2). Paste the line; do not summarise it.

Finally, disposition the backlog from the recorded outputs, never from the repo:
- `[CRON-DRIFT-01]` — close the LIVE-ROW half, citing the recorded verify-back. The DETECT half is
  Phase 164.1's and stays closed.
- `[VAULTTICK-EMPTYKEY-01]` — close, citing the post-repoint reading of the live command, and carry
  the narrower `btrim`-trims-spaces-only residual forward as a sentence rather than silently
  dropping it.
- `[164.7-VAULT-ABSENT-RULE]` — close, citing that the repointed command passed `vault-absent` via
  `reachesVaultRead`'s committed-snapshot resolution with NO decorative wrapper. If it did not pass,
  do NOT close it: record the regression and stop.
- `[164.7-ACTIVATION-DEFERRED]` — close if the activation ran; if it deferred, update it with the
  fresh P3-C reading and its date, and leave it open with this phase still named as owner. ⛔ A
  DEFER is not converted into a close by argument.
- `[164.8.5-MANIFEST-SIDE-LOOP-DEAD]` — record that the re-capture re-animated the loop and list the
  manifest-side findings it produced, so the next reader meets them as expected rather than as news.

⛔ Do not run any `supabase` CLI command from this checkout.
  </action>
  <acceptance_criteria>
    - `node -e` reading the committed manifest reports `normalization` equal to the arm's
      `NORMALIZATION` and `schema_version` equal to `MANIFEST_SCHEMA_VERSION` — plan 07's gate is
      green against the NEW file.
    - The committed manifest's `match_engine_cron` entry has a `command` that is the one-line call
      and a `command_sha256` that recomputes from that command under `normalizeCommand`.
    - `git show <manifest-commit> -- scripts/prod-prober/cron-manifest.json` (a commit-pinned
      reading, because an unpinned working-tree comparison is empty once the change is committed
      and would pass vacuously) shows changes confined to `captured_at`, jobid 1's `command` and
      `command_sha256`, and (if the activation ran) one added job.
    - `164.5.1-PROD-SESSION.md` carries the `cron-drift` arm's own output line reporting zero drift
      for `match_engine_cron`, pasted.
    - Each of the five backlog entries carries a dated disposition citing a recorded OUTPUT.
    - `git log --oneline` shows the manifest committed in its own change, separate from the TODOS
      dispositions.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports `0 passed` / `No test files found` — the normalization gate plan 07 shipped must be green against the NEW manifest</fails_when>
    <automated>node -e "const m=require('./scripts/prod-prober/cron-manifest.json');import('./scripts/prod-prober/arms/cron-drift.mjs').then(a=>{const j=m.jobs.find(x=>x.jobname==='match_engine_cron');const sha=a.sha256Hex(a.normalizeCommand(j.command));console.log('normalization',m.normalization,a.NORMALIZATION);console.log('command',JSON.stringify(j.command));console.log('sha-recomputes',sha===j.command_sha256);})"</automated>
    <fails_when>the two `normalization` values printed differ, or `sha-recomputes` prints `false`, or the printed `command` is anything other than the one-line call to `public.match_engine_cron_tick()`</fails_when>
    <automated>node scripts/prod-prober/run.mjs --self-test</automated>
    <fails_when>non-zero exit, or the final line does not report the full scenario count, or any `❌` appears</fails_when>
  </verify>
  <done>The oracle matches PROD, the drift arm reports zero drift for `match_engine_cron` in its own
  words, and every backlog item is dispositioned from a recorded output rather than from the repo.</done>
</task>

</tasks>

<edge_assumptions>
## Edge-coverage assumptions owned by this plan

The consolidated 21-item table lives in `164.5.1-01-PLAN.md` § Edge-coverage assumptions. All 21
rows are `unresolved`; none is auto-resolved and none is dropped. The four rows this plan owns:

| # | Requirement | Category | Explicit assumption this plan makes |
|---|---|---|---|
| 4 | VAULTTICK-EMPTYKEY-01 | adjacency | "When two things are exactly equal or just touch, do they merge, collide, or separate?" **Assumed and recorded as a LIMIT:** `btrim(v_key) = ''` treats a spaces-only secret as empty and a TAB-only or newline-only secret as non-empty. Those two cases sit adjacent and do NOT merge. The callable's own comment already records this as a decision with its own evidence; this plan records it in the session log rather than silently closing the item as if it covered both. |
| 5 | VAULTTICK-EMPTYKEY-01 | empty | "What is the result for empty, single-element, or null input?" **Assumed and implemented by the callable:** `v_key IS NULL OR btrim(v_key) = ''` RAISES rather than sending a null header — that is the whole fix, and the session records the live command reaching it. |
| 6 | VAULTTICK-EMPTYKEY-01 | ordering | "When elements compare equal, is output order specified and stable?" **Assumed:** the sibling cardinality concern — a duplicate secret NAME selecting an arbitrary row — was Phase 164.8.6's and is already addressed in the callable (one scan, counting and taking together, refusing by name). Recorded rather than dropped so a reader knows the ordering question was considered and has an owner. |
| 21 | 164.5.1-GATEWAY-CEILING-INVERSION | concurrency | "If interrupted or run in parallel, what is guaranteed?" **Assumed and stated as a limit:** nothing guards a second operator or a concurrent CI apply during this session. Shared TEST is SHARED and PROD's apply is behind a human gate, but the browser SQL editor has no automated guard at all — the marker query is the only control, and it is a point-in-time reading, not a lock. Recorded here rather than left implicit. |
</edge_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder (browser SQL editor / CLI) -> PROD | UNGUARDED by automation; the `COMMENT ON DATABASE` marker is the only control |
| `cron.job` jobid 1 -> `vault.decrypted_secrets` | The repointed command reaches Vault through a `SECURITY DEFINER` callable executing as its owner |
| `scripts/prod-prober/cron-manifest.json` -> the public internet | A committed, world-readable file whose every command string is published |
| the execution record -> the public internet | `.planning/` is TRACKED and the repo is PUBLIC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5.1-09-01 | Spoofing | which database the session writes to | critical | mitigate | The marker query runs FIRST and its OUTPUT is recorded; a NULL marker halts the session. `current_database()` is `postgres` on both projects and is explicitly named as proving nothing |
| T-164.5.1-09-02 | Tampering | `cron.job` jobid 1 | critical | mitigate | Three reviewers (task 1) plus the founder's own execution, plus the pre-flight verb aborting before any write on a live-vs-manifest mismatch, plus a verify-back asserting the jobid is unchanged |
| T-164.5.1-09-03 | Information Disclosure | the re-captured manifest in a PUBLIC repo | high | mitigate | `captureManifest` REFUSES to capture a row failing any hygiene rule, and the operator reads every command in the diff before committing. The prohibitions forbid pasting credential material into the record |
| T-164.5.1-09-04 | Repudiation | a gate satisfied by pasting its query | high | mitigate | Every acceptance criterion demands an OUTPUT; the `[164.7-P3C-PRESENCE-ORACLE]` vacuity is named by id in the runbook and in this plan |
| T-164.5.1-09-05 | Tampering | the committed oracle | high | mitigate | The manifest is generated by the tool and never hand-edited; plan 07's gate re-runs against the NEW file, and task 3 recomputes jobid 1's sha from its own command text |
| T-164.5.1-09-06 | Denial of Service | the hourly recompute after the repoint | high | mitigate | Areas 1 and 2 deploy FIRST (runbook Step 0), so the repointed job calls into a service that fails closed, has a catchable timeout and batches its work. The first tick's `net._http_response` reading is the backstop truth |
| T-164.5.1-09-07 | Denial of Service | the ledger fan-out's never-run branch | medium | mitigate | The all-candidates-failed branch becomes reachable AT activation and is exercised DELIBERATELY with the result recorded, rather than assumed to work |
| T-164.5.1-09-08 | Elevation of Privilege | a local `supabase db push` reaching PROD | critical | mitigate | Prohibited outright; the apply path is merge -> `apply-test` -> the `Production` environment's human reviewer gate, and the override is recorded in the objective |
| T-164.5.1-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists; RESEARCH.md § Package Legitimacy Audit records `N/A` for the phase |
</threat_model>

## Artifacts this phase produces

Produced by THIS plan:
- `.planning/phases/164.5.1-.../164.5.1-PROD-SESSION.md` — the execution record
- The re-captured `scripts/prod-prober/cron-manifest.json`
- PROD state: `cron.job` jobid 1 repointed; `service_role` carrying an explicit `statement_timeout`;
  and — if P3-C passed — a `ledger_refresh_fanout` row plus
  `system_flags.ledger_refresh_enabled = true`
- `TODOS.md` dispositions for `[CRON-DRIFT-01]` (LIVE-ROW half), `[VAULTTICK-EMPTYKEY-01]`,
  `[164.7-VAULT-ABSENT-RULE]`, `[164.7-ACTIVATION-DEFERRED]` and
  `[164.8.5-MANIFEST-SIDE-LOOP-DEAD]`

The full phase-wide artifact list is in `164.5.1-01-PLAN.md` § Artifacts this phase produces.

<verification>
- Task 3's `<automated>` commands green.
- `npx vitest run` full suite green after the manifest lands.
- The execution record carries the `cron-drift` arm's own zero-drift line, pasted.
- ⛔ Tasks 1 and 2 have no automated verification and must never be reported as if CI covered them.
</verification>

<success_criteria>
- Criterion 2: the cron-drift arm reports ZERO drift for `match_engine_cron` against the
  re-captured manifest.
- Criterion 5 / D4: three reviewers ran and the founder executed or approved the live statements.
- Criterion 7: `pg_roles.rolconfig` was read on PROD AFTER the apply and its OUTPUT recorded.
- `[VAULTTICK-EMPTYKEY-01]` is closed BY the repoint and by a live reading, with its narrower
  residual carried forward rather than dropped.
</success_criteria>

<output>
Create `.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-09-SUMMARY.md` when done
</output>
