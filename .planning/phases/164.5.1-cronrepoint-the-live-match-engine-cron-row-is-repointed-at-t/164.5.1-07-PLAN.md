---
phase: 164.5.1
plan: 07
type: execute
wave: 2
depends_on:
  - 164.5.1-04
files_modified:
  - src/__tests__/prod-prober-wiring.test.ts
  - scripts/prod-prober/run.mjs
  - scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json
autonomous: true
requirements:
  - T-OPEN-04
  - T-OPEN-03
  - 164.8.5-MANIFEST-SIDE-LOOP-DEAD

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CI goes RED when the committed manifest's `normalization` key and the arm's exported `NORMALIZATION` constant disagree — the gap that let a control silently stop comparing anything for weeks"
    - "The same gate covers `schema_version` against `MANIFEST_SCHEMA_VERSION`, because that is the other key `compareManifest` invalidates the whole comparison on"
    - "The gate fails in BOTH directions: a constant bumped without a re-capture, and a manifest hand-edited to a value the arm does not compute"
    - "The gate is CALIBRATED — its predicate has been run against a divergent manifest object and observed to throw. A predicate only ever applied to passing input is not evidence"
    - "A `--self-test` scenario drives a manifest declaring the SUPERSEDED normalization through the arm and observes `manifest-invalid` naming the normalization route — distinct from the already-covered absent-oracle, schema-bumped and sha-mismatch routes"
    - "The manifest-side hygiene loop that the 2026-09-11 re-capture re-animated stays reachable: the self-test scenario asserts the PROD-side credential finding is STILL reported on the invalid-manifest path, so an invalid oracle is never an off switch for the live scan"
    - "`SELF_TEST_SCENARIOS` and its hand-typed mirror move in the SAME edit"
    - statement: "After this lands, the next normalization bump is caught by a red CI check rather than by an hourly production probe an hour after it ships"
      verification: backstop
  artifacts:
    - path: "src/__tests__/prod-prober-wiring.test.ts"
      provides: "The committed-manifest-vs-arm-constants contract gate and its two-direction calibration; the mirrored scenario-count pin 81 -> 82"
    - path: "scripts/prod-prober/run.mjs"
      provides: "One self-test scenario driving the superseded-normalization oracle; `SELF_TEST_SCENARIOS` 81 -> 82"
    - path: "scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json"
      provides: "A manifest fixture declaring the superseded normalization with everything else valid"
  key_links:
    - "`CRON_DRIFT_MOD.NORMALIZATION` + `MANIFEST_SCHEMA_VERSION` <-> `scripts/prod-prober/cron-manifest.json`'s `normalization` + `schema_version` keys — the pin this plan creates"
    - "the contract gate -> the sharded `frontend` vitest job -> the `frontend` aggregator, which gates on it"
    - "`compareManifest`'s normalization branch -> `manifest-invalid` -> the self-test scenario that proves it still reports the PROD-side credential anyway"
  prohibitions:
    - "A red drift arm must NEVER be cleared by relaxing the rule or by leaving the oracle stale. The instrument is not shaped to its own false positive — that is the class `[164.7-VAULT-ABSENT-RULE]` names by hand and this gate exists to make loud."
    - "Never hand-edit `scripts/prod-prober/cron-manifest.json` to make this gate green. The sha is regenerated by `captureManifest`, never typed; a hand-edited oracle is the adversary this whole arm models."
    - "Never derive the mirrored `expect(SELF_TEST_SCENARIOS).toBe(N)` literal from the imported constant — hand-typed on purpose, per that file's own header rule."
    - "Never widen the gate into a general 'the manifest is fresh' assertion. It pins two keys against two constants. A freshness gate over `captured_at` would fail on every day that passes and would be silenced within a week, which is the opposite of what this plan is for."
    - "Never add a CI gate against future dead PROSE pointers while here. That was considered and recorded as a deliberate NOT-done in criterion 6's amendment; anti-vacuity is scoped to data-integrity and user-facing behaviour."
---

<objective>
The RESIDUAL Phase 164.8.5 left is the GATE, not the data.

Measured and recorded: `scripts/prod-prober/cron-manifest.json` was re-captured on 2026-09-11 at
`ws-collapse-v2` (capture run `34611594511`, landed by PR #776), because the first prober run after
Phase 164.8.5 reported `manifest-invalid` — the arm computed `ws-collapse-v2` while the committed
oracle still declared `ws-collapse-v1`, and two normalizations produce two different shas for
identical text, so **the cron-drift arm was performing NO comparison at all**. Ten of fourteen
commands differed textually and all ten differed only in whitespace, so zero real production drift
was absorbed. The DATA is discharged. `T-OPEN-03`'s newline fold is likewise already restored — v2
preserves line breaks — and it was DOWNGRADED to `low` on measurement rather than accepted as
booked, because only the header-anchored rules and `vault-absent` were lost, making it an
ATTRIBUTION gap and not a credential blind spot.

⛔ What is NOT discharged: **nothing in CI fails when the arm's exported `NORMALIZATION` and the
manifest's `normalization` key disagree.** The mismatch was caught by an hourly PRODUCTION probe an
hour after it shipped. A control that silently stopped being able to find anything is the
anti-vacuity class this repo ranks above ordinary correctness, and any future normalization bump
re-opens it identically.

⚠️ And the re-capture this phase performs in plan 09 CLEARS `manifest-invalid` and thereby
re-animates `compareManifest`'s manifest-side hygiene loop, which has been dead on every production
run for weeks. Expect manifest-side `cron-secret-in-command` findings nobody has seen before; they
are not new leaks. Measured: a valid manifest yields 1 manifest-side credential defect, while v1 /
a wrong `schema_version` / a marker mismatch yield 0.

Locked decision recorded and implemented here: CONTEXT.md **Area 3**, second bullet — *"Ship a
contract test that goes RED on divergence, plus a `--self-test` arm proving it bites. ⛔ A note in
the runbook alone does not satisfy this."*

Purpose: make the next normalization bump a red check instead of a silent hole.
Output: one CI gate with a two-direction calibration, and one arm-side scenario.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-CONTEXT.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-RESEARCH.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-PATTERNS.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The CI gate — the committed oracle declares what the arm computes</name>
  <reversibility rating="reversible">A test file case plus a calibration block; removing it changes
  no runtime behaviour.</reversibility>
  <files>src/__tests__/prod-prober-wiring.test.ts</files>
  <read_first>
    - `src/__tests__/prod-prober-wiring.test.ts` lines 1-50 — the header, its standing rules
      (ONE-EDIT RENUMBER; hand-typed literals; every negative assertion needs a calibration), and
      the import list, so you import `NORMALIZATION` / `MANIFEST_SCHEMA_VERSION` from wherever the
      file already reaches for `CRON_DRIFT_MOD` rather than adding a second import path.
    - `src/__tests__/prod-prober-wiring.test.ts` lines 2080-2180 — the three-lever block, and
      `lever 1 — \`normalization\` drift reaches \`invalid()\`` in particular. That lever proves the
      RUNTIME behaviour on a synthetic copy; it does NOT read the committed file. Understanding the
      difference is the whole point of this task, and your new case must not duplicate it.
    - `src/__tests__/mutation-runner-floors.test.ts` — the fail-in-EITHER-direction shape
      (regression AND staleness) this gate copies its discipline from. Copy the FAILURE-DIRECTION
      discipline, not its re-derivation machinery: here the manifest file IS the artifact to
      compare against, so no independent re-derivation is needed.
    - `scripts/prod-prober/arms/cron-drift.mjs`, symbols `NORMALIZATION`, `MANIFEST_SCHEMA_VERSION`,
      `MANIFEST_PATH`, and the `compareManifest` branch that raises `manifest-invalid` when
      `manifest.normalization !== NORMALIZATION` — read its message so your failure text points at
      the same remedy (re-capture, never hand-edit).
    - `scripts/prod-prober/cron-manifest.json` — its top-level keys only
      (`schema_version`, `normalization`, `captured_at`, `database_marker`, `jobs`). Do NOT read the
      `jobs` array's command text into the test; the gate pins two scalars.
  </read_first>
  <behavior>
    - Given the committed manifest as it stands: both assertions pass.
    - Given a copy whose `normalization` is the superseded value: the predicate throws, and the
      message names both the manifest's declared value and the arm's computed value.
    - Given a copy whose `normalization` is some third, never-used value: the predicate throws —
      the gate is not a two-value allowlist, it is an equality against the constant.
    - Given a copy whose `schema_version` differs from `MANIFEST_SCHEMA_VERSION`: the predicate
      throws.
    - Given a copy missing the `normalization` key entirely: the predicate throws rather than
      comparing `undefined === undefined` to true.
    - Given an empty object: an anti-vacuity guard throws first, so nothing downstream is evaluated
      against absent input and reported green.
  </behavior>
  <action>
Add a case to `src/__tests__/prod-prober-wiring.test.ts` asserting that the COMMITTED manifest at
`CRON_DRIFT_MOD.MANIFEST_PATH` declares exactly the normalization and schema version the arm
computes. Read the file with `readFileSync` + `JSON.parse` — independently of anything the arm does
at runtime, because the whole failure being closed is "the two artefacts disagreed and nothing
noticed".

Extract the comparison into a small exported predicate taking a parsed manifest object and throwing
with a message naming the declared value, the computed value, and the remedy: re-capture with
`captureManifest`, never hand-edit — a hand-edited sha is the adversary this arm models. Assert the
committed file against it, then CALIBRATE it in the same block with `expect(() => ...).toThrow()`
over the five mutant objects in the behaviour list. The calibration is not optional decoration: a
gate that has only ever seen passing input certifies nothing, which is the exact shape
`[PROBER-CALIBRATION-01]` records elsewhere in this repo.

Write the case's title so it states the property, not the mechanism: the committed oracle declares
the normalization the arm computes. Add a comment above it recording, in two sentences, the
2026-09-11 incident this gate closes — the hourly production probe caught it, CI did not — and that
the DATA half is already discharged so this case is about the GATE.

⛔ Do not touch the existing three-lever block. It measures a different thing (the runtime route to
`invalid()`), and duplicating its assertion here would mean one regression reddens two controls and
is credited twice.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/prod-prober-wiring.test.ts` passes.
    - The new case reads `scripts/prod-prober/cron-manifest.json` from disk (not a fixture) and
      compares against `CRON_DRIFT_MOD.NORMALIZATION` and
      `CRON_DRIFT_MOD.MANIFEST_SCHEMA_VERSION` imported from the module, not against hand-typed
      strings.
    - Five calibration mutants each throw: superseded normalization, third-value normalization,
      wrong schema version, missing `normalization` key, empty object.
    - The empty-object mutant throws on the anti-vacuity guard, identifiable by its message, not on
      a downstream equality.
    - The existing `lever 1 — normalization drift reaches invalid()` case is unchanged and still
      passes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports `0 passed` / `No test files found`, or any test is skipped</fails_when>
    <automated>bash -c 'set -e; m=$(node -e "const m=require(\"./scripts/prod-prober/cron-manifest.json\");console.log(m.normalization+\" \"+m.schema_version)"); a=$(node -e "import(\"./scripts/prod-prober/arms/cron-drift.mjs\").then(x=>console.log(x.NORMALIZATION+\" \"+x.MANIFEST_SCHEMA_VERSION))"); echo "manifest=[$m] arm=[$a]"; [ "$m" = "$a" ]'</automated>
    <fails_when>non-zero exit, or the printed `manifest=[...]` and `arm=[...]` values differ</fails_when>
  </verify>
  <done>A red CI check now stands where an hourly production probe used to be the only detector,
  and the check has been observed to fail in both directions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The `--self-test` arm proving it bites, and the ONE-EDIT RENUMBER</name>
  <files>scripts/prod-prober/run.mjs, scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json, src/__tests__/prod-prober-wiring.test.ts</files>
  <read_first>
    - `scripts/prod-prober/run.mjs`, symbol `selfTest` — the three HOIST scenarios ("the PROD
      credential scan fires with the oracle ABSENT", "... STALE (schema_version bumped)", "...
      HAND-EDITED (sha mismatch)"). Read all three in full: they are the template, and reading them
      is how you confirm none of them already drives a NORMALIZATION mismatch. The gap is real —
      absent, schema and sha are covered; normalization is not.
    - `scripts/prod-prober/run.mjs`, the `driftRun` helper and `driftFixturePath`, plus
      `loadFixture`, `fixtureSql`, `FIXTURE_DB_MARKER`, `expect` and `noDefectOfKind`.
    - `scripts/prod-prober/fixtures/cron-drift/manifest-schema-bumped.json` and
      `scripts/prod-prober/fixtures/cron-drift/manifest.json` — the new fixture is `manifest.json`
      with ONE key changed, exactly as the schema-bumped one is.
    - `scripts/prod-prober/fixtures/cron-drift/prod-inline-key.json` — the dirty PROD reading the
      three HOIST scenarios drive, so the new one drives the same input and asserts the same
      "an invalid oracle is not an off switch" property.
    - `scripts/prod-prober/arms/cron-drift.mjs`, the `manifest.normalization !== NORMALIZATION`
      branch and its detail message — the new scenario asserts the detail names the NORMALIZATION
      route specifically, not merely that some `manifest-invalid` fired.
    - `src/__tests__/prod-prober-wiring.test.ts`, the hand-typed `expect(SELF_TEST_SCENARIOS).toBe(81)`
      literal as plan 04 left it, and the header's ONE-EDIT RENUMBER rule.
  </read_first>
  <behavior>
    - Given `manifest-normalization-stale.json` as the oracle and `prod-inline-key.json` as the live
      reading: exit code 1, exactly one `manifest-invalid` defect, whose detail names the superseded
      normalization value.
    - In the same run: the PROD-side `cron-secret-in-command` defect for `prod:match_engine_cron`
      is STILL reported — an invalid oracle is not an off switch for the live credential scan.
    - In the same run: ZERO `cron-drift` defects, because no comparison happened — a drift verdict
      from an uncomparable oracle would be a fabricated measurement.
    - The new fixture differs from `manifest.json` in exactly one key.
  </behavior>
  <action>
Create `scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json` as a copy of
`scripts/prod-prober/fixtures/cron-drift/manifest.json` with ONLY the `normalization` value changed
to the superseded `ws-collapse-v1`. Leave `schema_version`, `captured_at`, `database_marker` and
every `jobs` entry byte-identical, so the scenario drives exactly one broken thing.

Add ONE `--self-test` scenario to `selfTest()`, beside the three HOIST scenarios, titled as a
FOURTH broken-oracle state: the PROD credential scan fires with the oracle STALE (normalization
superseded). Drive `driftRun(prod.data, driftFixturePath("manifest-normalization-stale.json"), quiet)`
and assert the four behaviours above. Include, in the scenario's own comment, the one-line
derivation of why a fourth state is needed: `compareManifest` reaches `invalid()` by several
independent routes and each is a separate candidate off switch for the live scan; three were
covered and the normalization route — the one that actually fired in production on 2026-09-11 — was
not.

Add a fixture-integrity precondition inside the scenario, following the sha-mismatch scenario's own
shape: assert from the fixture itself that its `normalization` differs from
`CRON_DRIFT_MOD.NORMALIZATION`, so the scenario stays honest about which state it is driving
without reading the control's verdict. That check survives any neuter of the branch under test and
fails only if the fixture stops being stale.

⛔ In the SAME edit increment `export const SELF_TEST_SCENARIOS` in `scripts/prod-prober/run.mjs`
by one (plan 04 left it at 81) AND update the hand-typed mirror pin in
`src/__tests__/prod-prober-wiring.test.ts` — the `expect(SELF_TEST_SCENARIOS)` assertion whose
expected value is spelled as a literal rather than derived — to the same new number, leaving no
occurrence of the previous number behind in that file.
  </action>
  <acceptance_criteria>
    - `node scripts/prod-prober/run.mjs --self-test` exits 0 and its final line reports the new
      scenario count as both numerator and denominator, with headers covering 1..N exactly once.
    - The exported `SELF_TEST_SCENARIOS` equals the number of headers the runner printed, read
      from the module rather than from a grep, and is one greater than the 81 plan 04 left.
    - `npx vitest run src/__tests__/prod-prober-wiring.test.ts` passes — that file's mirror
      assertion is what fails when the hand-typed pin and the constant disagree, so a green run IS
      the proof the renumber moved on both sides.
    - `node -e` comparing `manifest.json` and `manifest-normalization-stale.json` reports exactly
      one differing top-level key, and it is `normalization`.
    - The new scenario asserts a NON-ZERO exit, exactly one `manifest-invalid`, the surviving
      PROD-side `cron-secret-in-command`, and zero `cron-drift` — all four, in one leg.
    - `npx vitest run src/__tests__/prod-prober-wiring.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test</automated>
    <fails_when>non-zero exit, or the final `SELF-TEST PASSED` line's numerator and denominator differ, or any `❌` appears in the output</fails_when>
    <automated>node -e "const a=require('./scripts/prod-prober/fixtures/cron-drift/manifest.json'),b=require('./scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json');const d=Object.keys(a).filter(k=>JSON.stringify(a[k])!==JSON.stringify(b[k]));console.log('differing keys:',d.join(','))"</automated>
    <fails_when>the printed `differing keys:` line is anything other than exactly `normalization`, or the command exits non-zero (a missing fixture)</fails_when>
    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports `0 passed` / `No test files found`, or any test is skipped</fails_when>
  </verify>
  <done>A fourth broken-oracle state — the one that actually fired in production — is covered by a
  self-test scenario that also proves the live credential scan survives it; both sides of the
  scenario-count pin moved in one edit.</done>
</task>

</tasks>

<edge_assumptions>
## Edge-coverage assumptions owned by this plan

The consolidated 21-item table lives in `164.5.1-01-PLAN.md` § Edge-coverage assumptions. All 21
rows are `unresolved`; none is auto-resolved and none is dropped. The five rows this plan owns:

| # | Requirement | Category | Explicit assumption this plan makes |
|---|---|---|---|
| 12 | 164.8.5-MANIFEST-SIDE-LOOP-DEAD | unclassified | The probe could not classify it. **Assumed:** the ORDERING half (hoisting the manifest-side loop above `compareManifest`'s early returns) is NOT this phase's — Phase 164.7 was named as its owner and the TODOS entry records the hoist as code-complete-but-unmerged on another branch with a known `null`-element regression. This phase owns the RE-CAPTURE half only, and this plan owns the gate that makes the invalidity visible. If the hoist never lands, the loop re-animates anyway once the re-capture clears `manifest-invalid` — which is why plan 09 warns about unfamiliar manifest-side findings. |
| 13-adjacent | T-OPEN-03 | (row owned by plan 01) | Restated: **assumed** that with `ws-collapse-v2` preserving line breaks, no further code change is needed for the newline fold, and the residual is the gate. Verify against the committed file rather than re-deriving from the ROADMAP's older sentence. |
| 14 | T-OPEN-04 | adjacency | "When two things are exactly equal or just touch, do they merge, collide, or separate?" **Assumed:** the gate is a strict equality on two scalars; there is no near-match, no version-range tolerance and no "compatible" notion. A future `ws-collapse-v2.1` would fail the gate, and that is intended — it forces a re-capture rather than a judgement call. |
| 15 | T-OPEN-04 | empty | "What is the result for empty, single-element, or null input?" **Assumed and implemented:** a manifest missing the `normalization` key, and an empty object, both THROW rather than comparing `undefined` to `undefined`. Two of the five calibration mutants exist for exactly this row. |
| 16 | T-OPEN-04 | encoding | "Whose definition of length/equality applies?" **Assumed:** the gate compares two ASCII identifier strings with `===`; no normalization, casefolding or Unicode equivalence is applied to the KEY itself. The command-text equality question this probe row also implies is owned by plan 04's row 3. |
| 17 | T-OPEN-04 | ordering | "When elements compare equal, is output order specified and stable?" **Assumed:** not applicable — the gate pins two scalars and orders nothing. Recorded rather than dropped, because the probe could not rule it out. |
</edge_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `scripts/prod-prober/cron-manifest.json` -> `compareManifest` | The committed oracle is the only thing PROD is judged against; anything that invalidates it turns the comparison off |
| a human editor -> the committed oracle | Hand-editing the manifest is the adversary this arm explicitly models |
| CI (`frontend` aggregator) -> the gate | The only automated detector this plan creates; if it is skipped rather than run, nothing is measured |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5.1-07-01 | Tampering | the committed cron manifest | high | mitigate | The gate pins `normalization` and `schema_version` against the arm's own exported constants, so a hand-edit to either is a red CI check rather than a silent comparison shutdown |
| T-164.5.1-07-02 | Denial of Service (of the standing control) | `compareManifest`'s comparison | critical | mitigate | This is the exact realised incident of 2026-09-11: an invalid oracle meant NO comparison for weeks. The gate is the detector; the self-test scenario proves the runtime still reports the PROD-side credential on that path, so an invalid oracle is not an off switch |
| T-164.5.1-07-03 | Repudiation | a gate that cannot fail | high | mitigate | Five calibration mutants, each observed to throw, including the empty-input case which must throw on the anti-vacuity guard specifically |
| T-164.5.1-07-04 | Tampering | the self-test scenario count | medium | mitigate | `SELF_TEST_SCENARIOS` and its hand-typed mirror move together; the runner's own tail assertion fails on a stale count and the vitest pin fails on a stale mirror |
| T-164.5.1-07-05 | Information Disclosure | the new fixture in a PUBLIC repo | low | accept | It is a copy of an existing committed fixture with one identifier string changed; it introduces no new command text and no credential material. Accepted |
| T-164.5.1-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists; RESEARCH.md § Package Legitimacy Audit records `N/A` for the phase |
</threat_model>

## Artifacts this phase produces

Produced by THIS plan:
- The contract case "the committed oracle declares the normalization the arm computes" and its
  exported predicate + five-mutant calibration — `src/__tests__/prod-prober-wiring.test.ts`
- Self-test scenario "the PROD credential scan fires with the oracle STALE (normalization
  superseded)" — `scripts/prod-prober/run.mjs`
- `scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json`
- `SELF_TEST_SCENARIOS` 81 -> 82 and its mirrored hand-typed literal

The full phase-wide artifact list is in `164.5.1-01-PLAN.md` § Artifacts this phase produces.

<verification>
- `node scripts/prod-prober/run.mjs --self-test` green at 82/82.
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts` green.
- `npx vitest run` (full suite) green — the new case reads a committed file, so it must not be
  order-dependent.
- `git diff --name-only` lists exactly the three files in `files_modified`.
</verification>

<success_criteria>
- `T-OPEN-04`'s and `[164.8.5-MANIFEST-SIDE-LOOP-DEAD]`'s RESIDUAL — the GATE, not the data — is
  closed by a red CI check that has been observed to fail in both directions.
- `T-OPEN-03`'s residual is covered by the same gate, and its data half is recorded as already
  discharged rather than re-opened as work.
</success_criteria>

<output>
Create `.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-07-SUMMARY.md` when done
</output>