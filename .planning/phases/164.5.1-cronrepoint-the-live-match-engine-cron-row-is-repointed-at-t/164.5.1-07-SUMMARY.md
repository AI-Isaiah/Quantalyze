---
phase: 164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t
plan: 07
subsystem: infra
tags: [prod-prober, cron-drift, ci-gate, anti-vacuity, vitest, self-test]

# Dependency graph
requires:
  - phase: 164.5.1 (plan 04 / preflightCronRepoint)
    provides: "The pattern this plan follows for self-test scenario shape (driftRun/driftFixturePath/loadFixture) and the SELF_TEST_SCENARIOS ONE-EDIT RENUMBER discipline"
provides:
  - "A CI contract gate ([164.5.1-07] describe block in prod-prober-wiring.test.ts) reading the COMMITTED cron-manifest.json from disk and asserting its normalization + schema_version agree with cron-drift.mjs's exported NORMALIZATION / MANIFEST_SCHEMA_VERSION constants, calibrated with 5 mutants"
  - "A fourth --self-test broken-oracle scenario (normalization superseded) proving the PROD-side credential scan survives it, and the manifest-side hygiene loop's reachability is not silently switched off by this route"
  - "scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json — a one-key mutant of manifest.json"
  - "SELF_TEST_SCENARIOS 81 -> 82, both sides moved in the ONE-EDIT RENUMBER"
affects: [164.5.1-08, 164.5.1-09]

# Actuals (#2632) — pairs with the plan's estimate to calibrate future estimates.
actuals:
  tokens: 3287
  tasks: 2
  commits: 2
  plan_head_before: 191b5e1e~1

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A CI contract gate that reads the COMMITTED artifact from disk (not a fixture, not the arm's own runtime output) and compares it against the module's exported constants — closes a class of defect where a synthetic-fixture test can pass while the real committed file has silently drifted."
    - "A calibration block asserting toThrow() on 5 distinct mutant shapes (superseded value, third never-used value, wrong version, missing key, empty object) rather than a single positive assertion, so the gate's own failure mode is proven before it is trusted — including a dedicated anti-vacuity guard for the empty-input case, distinguishable by message from an ordinary equality mismatch."

key-files:
  created:
    - scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json
  modified:
    - src/__tests__/prod-prober-wiring.test.ts
    - scripts/prod-prober/run.mjs

key-decisions:
  - "The predicate (assertManifestDeclaresArmConstants) lives inside the test file's new describe block rather than being exported from cron-drift.mjs — it is test-only machinery over production constants, matching the existing convention in this file (anchorIndex, invalidsFor, withoutField are all locally-scoped test helpers, none of them exported from the modules under test)."
  - "The empty-object anti-vacuity guard is a DEDICATED, FIRST check (`!manifest || typeof !== 'object' || Array.isArray || Object.keys().length === 0`) distinguishable by message from the two equality checks below it — an empty object would otherwise throw on the normalization equality check for the WRONG reason (undefined !== 'ws-collapse-v2'), which is a correct outcome but an indistinguishable one from an ordinary mismatch, and the plan's acceptance criteria required the empty-input case be 'identifiable by its message, not on a downstream equality.'"
  - "The new self-test scenario is placed immediately after the existing 'HOIST: ... HAND-EDITED (sha mismatch)' scenario and before 'MARKER MANDATORY', grouping it with the other three broken-oracle HOIST scenarios it completes the set with, rather than appending it at the end of selfTest()."
  - "SELF_TEST_SCENARIOS and its test-file mirror pin were bumped in Task 2 only, not Task 1, even though Task 1's own vitest run touches the same pin-bearing test. Verified Task 1 in isolation with the pin still at 81 (matching run.mjs's then-current 81), then bumped both sides together in Task 2 — preserving per-task atomicity, the same discipline plan 04's SUMMARY records for the identical situation."

requirements-completed: []  # T-OPEN-04, T-OPEN-03 and 164.8.5-MANIFEST-SIDE-LOOP-DEAD are NOT present in REQUIREMENTS.md (measured: zero grep hits) — they are ROADMAP/TODOS-tracked IDs only (the FANOUT-GLOBAL-01 pattern CONTEXT.md names), so the standard requirements.mark-complete flow has nothing to act on. TODOS.md already carries `[T-OPEN-03]`, `[T-OPEN-04]` and `[164.8.5-MANIFEST-SIDE-LOOP-DEAD]` entries (written by plan 01's first act); their disposition is TODOS.md's, not REQUIREMENTS.md's.

coverage:
  - id: D1
    description: "A CI contract gate reads the COMMITTED scripts/prod-prober/cron-manifest.json from disk and asserts its normalization + schema_version agree with cron-drift.mjs's exported NORMALIZATION / MANIFEST_SCHEMA_VERSION — the exact comparison that was silently absent for weeks in the 2026-09-11 incident. Calibrated with 5 mutants (superseded normalization, third never-used value, schema_version bump, missing key, empty object) each proven to throw, plus a dedicated anti-vacuity guard for the empty-object case."
    requirement: "T-OPEN-04"
    verification:
      - kind: unit
        ref: "src/__tests__/prod-prober-wiring.test.ts#[164.5.1-07] the committed oracle declares the normalization the arm computes"
        status: pass
      - kind: other
        ref: "Neutered the REAL scripts/prod-prober/cron-manifest.json (normalization -> ws-collapse-v1), confirmed the byte change applied (cmp against a cp backup), ran vitest and observed 3 tests go RED, restored from backup, verified byte-identical via cmp + sha256 match, re-ran green (93/93)"
        status: pass
    human_judgment: false
  - id: D2
    description: "A fourth --self-test broken-oracle scenario ('HOIST: the PROD credential scan fires with the oracle STALE (normalization superseded)') drives the new manifest-normalization-stale.json fixture against dirty PROD rows and asserts: exit 1, exactly one manifest-invalid naming the superseded value, the PROD-side cron-secret-in-command finding still reported, and zero cron-drift. This is the runtime-route half of the same defect D1's static gate covers, and it proves the manifest-side hygiene loop's reachability is not switched off by the normalization route specifically."
    requirement: "164.8.5-MANIFEST-SIDE-LOOP-DEAD"
    verification:
      - kind: unit
        ref: "node scripts/prod-prober/run.mjs --self-test, scenario 48/82 'HOIST: the PROD credential scan fires with the oracle STALE (normalization superseded)' — all 6 sub-assertions pass"
        status: pass
      - kind: unit
        ref: "npx vitest run src/__tests__/prod-prober-wiring.test.ts (93/93 passed, including the SELF_TEST_SCENARIOS mirror pin at 82)"
        status: pass
    human_judgment: false
  - id: D3
    description: "T-OPEN-03's DATA half (the newline fold in retention_compute_jobs_orphaned_running) is already discharged by the 2026-09-11 re-capture (PR #776, run 34611594511) — not re-derived or re-verified by this plan's code. This plan's D1 gate is the mechanism that would catch any FUTURE regression of the same normalization-drift class T-OPEN-03 and T-OPEN-04 share."
    requirement: "T-OPEN-03"
    verification:
      - kind: other
        ref: "node -e comparing scripts/prod-prober/cron-manifest.json's committed normalization/schema_version against cron-drift.mjs's exported NORMALIZATION/MANIFEST_SCHEMA_VERSION — printed manifest=[ws-collapse-v2 1] arm=[ws-collapse-v2 1], agreeing"
        status: pass
    human_judgment: false
  - id: D4
    description: "After this lands, the next normalization bump is caught by a red CI check rather than by an hourly production probe an hour after it ships (the plan's own backstop truth)."
    verification: []
    human_judgment: true
    rationale: "A claim about FUTURE behavior — the plan's own must_haves lists this truth's verification method as 'backstop', not an automated check. The indirect evidence is D1's 5 calibration mutants (including the exact superseded value that actually fired in production) and D2's end-to-end self-test scenario, both of which demonstrate the mechanism that would fire on a repeat, but no test can execute 'the next bump' today."

# Metrics
duration: ~50min
completed: 2026-09-16
status: complete
---

# Phase 164.5.1 Plan 07: Cron-Drift Normalization Contract Gate Summary

**A vitest contract gate reads the committed `cron-manifest.json` from disk and asserts its `normalization`/`schema_version` agree with `cron-drift.mjs`'s exported constants (5-mutant calibrated, including a dedicated anti-vacuity guard), paired with a fourth `--self-test` broken-oracle scenario proving the PROD-side credential scan survives a normalization-stale oracle — closing the residual GATE half of the 2026-09-11 `manifest-invalid` incident that an hourly production probe caught and CI did not.**

## Performance

- **Duration:** ~50 min
- **Started:** ~2026-09-16T18:25Z (following plan 06's completion)
- **Completed:** 2026-09-16T19:15Z
- **Tasks:** 2 completed
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments

- New describe block `[164.5.1-07] the committed oracle declares the normalization the arm computes` in `src/__tests__/prod-prober-wiring.test.ts`: reads `scripts/prod-prober/cron-manifest.json` from disk via `MANIFEST_PATH` (newly imported from `cron-drift.mjs` alongside `MANIFEST_SCHEMA_VERSION` and `NORMALIZATION`), and asserts it against a local predicate `assertManifestDeclaresArmConstants` — a dedicated anti-vacuity guard for missing/non-object/empty input, then a normalization equality check, then a schema_version equality check, each throwing a distinct named message. Six `it()`s: the real committed file agreeing, plus five calibration mutants (superseded value, third never-used value, schema_version bump, missing normalization key, empty object) each asserted to throw. Does not duplicate the existing `lever 1 — normalization drift reaches invalid()` case, which proves the runtime route on a synthetic copy and never reads the committed file.
- New fixture `scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json`: `manifest.json` with only `normalization` changed to the superseded `ws-collapse-v1` — verified to differ in exactly that one top-level key.
- New `--self-test` scenario in `scripts/prod-prober/run.mjs`, `HOIST: the PROD credential scan fires with the oracle STALE (normalization superseded)`, placed beside the three existing HOIST scenarios (absent, schema-bumped, hand-edited sha mismatch): drives the new fixture against dirty PROD rows and asserts exit 1, exactly one `manifest-invalid` naming the superseded normalization, the PROD-side `cron-secret-in-command` finding still reported, and zero `cron-drift` — with a fixture-integrity precondition hashed from the fixture itself, mirroring the sha-mismatch scenario's own shape.
- `SELF_TEST_SCENARIOS` moved 81 → 82 in `run.mjs`, and the hand-typed `expect(SELF_TEST_SCENARIOS).toBe(81)` mirror pin in the test file moved to 82 in the SAME task (Task 2), after Task 1 was verified in isolation with the pin unchanged at 81.
- `node scripts/prod-prober/run.mjs --self-test` → **82/82 scenarios pass** (new scenario at 48/82). `npx vitest run src/__tests__/prod-prober-wiring.test.ts` → **93/93 tests pass**.
- Anti-vacuity proof against the REAL committed file (not just the calibration mutants): neutered `scripts/prod-prober/cron-manifest.json`'s `normalization` to the superseded value, confirmed the byte change actually applied (`cmp` against a `cp` backup), ran vitest and observed 3 tests go RED (the "committed manifest agrees" test plus two calibration tests whose own preconditions correctly caught the now-dirty module state), restored from the backup, verified byte-identical restoration via `cmp` and matching sha256, re-ran green.

## Task Commits

Each task was committed atomically:

1. **Task 1: The CI gate — the committed oracle declares what the arm computes** - `191b5e1e` (test)
2. **Task 2: The `--self-test` arm proving it bites, and the ONE-EDIT RENUMBER** - `8ae43f8b` (test)

**Plan metadata:** (pending — this commit)

## Files Created/Modified

- `src/__tests__/prod-prober-wiring.test.ts` — added `MANIFEST_PATH`, `MANIFEST_SCHEMA_VERSION`, `NORMALIZATION` to the existing `cron-drift.mjs` import block; added the `[164.5.1-07]` describe block (predicate + 6 `it()`s); moved the `SELF_TEST_SCENARIOS` mirror pin 81 → 82.
- `scripts/prod-prober/run.mjs` — added the fourth HOIST self-test scenario; moved `export const SELF_TEST_SCENARIOS` 81 → 82.
- `scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json` — new fixture, `manifest.json` with only `normalization` changed to `ws-collapse-v1`.

## Decisions Made

See `key-decisions` in frontmatter above — summarized: (1) the gate's predicate is test-only, local to the new describe block, matching this file's existing convention of not exporting test helpers from the modules under test; (2) the empty-object case gets its own FIRST guard with a distinct message, rather than falling through to the normalization equality check for the technically-correct-but-indistinguishable reason; (3) the new self-test scenario is grouped with the three existing HOIST scenarios rather than appended at the end; (4) the `SELF_TEST_SCENARIOS` renumber was deliberately deferred to Task 2 alone, verifying Task 1 in isolation at the pre-bump count first, preserving per-task atomicity — the same discipline plan 04's SUMMARY records for the identical two-tasks-touch-the-same-pin situation.

## Deviations from Plan

None - plan executed exactly as written. The two tasks' `<action>` and `<acceptance_criteria>` were followed directly; no Rule 1-4 auto-fixes were needed in the plan's own three files.

### Out-of-scope discovery (not a deviation — logged, not fixed)

While running the plan's own full-suite verification step (`npx vitest run`, no path filter), 4 pre-existing test files fail, none of which reference `prod-prober`, `cron-drift`, or `cron-manifest` (confirmed by `grep`) and all of which reproduce in isolation, i.e. independent of this plan's diff:

1. `src/__tests__/contracts/analytics-deploy-tree-compare.contract.test.ts` — network-dependent (`/health` stub unreachable in this sandbox).
2. `src/__tests__/lint-sql-gates.test.ts` — 4 sub-tests exceed the default 5000ms timeout, reproduced in isolation (environment-speed sensitive, not a code regression).
3. `src/__tests__/verify-plan-anchors.test.ts` — a stale `file:line` anchor exists somewhere in `.planning/`'s PLAN.md corpus; this plan modified no PLAN.md.
4. `src/lib/seam-venue-vocabulary.invariant.test.ts` — `KILL_SWITCH_UNAVAILABLE` (introduced by this phase's plan `164.5.1-02`) has no TypeScript disposition in `VENUE_WIRE_CODE_TO_VERDICT`.

Per the scope boundary rule, none of these were auto-fixed. Logged to this phase's `deferred-items.md` and to `.planning/WINDOWS.md` entry #56 (`kind: unrun-verify`), so the plan's own "full suite green" verification claim is not silently overstated. `npx vitest run src/__tests__/prod-prober-wiring.test.ts` — this plan's own scoped file — is green (93/93).

---

**Total deviations:** 0 auto-fixed. **Impact on plan:** None — the plan's three files pass every one of their own acceptance criteria and verify blocks. The 4 unrelated full-suite failures are recorded, not remediated, per scope.

## Issues Encountered

None beyond the out-of-scope discovery documented above.

## User Setup Required

None - no external service configuration required.

## Plan-Level Verification

- `node scripts/prod-prober/run.mjs --self-test` → **82/82 scenarios pass**, exit 0.
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts` → **93/93 tests pass**.
- `npx vitest run` (full suite) → 4 pre-existing, unrelated failures (see above); this plan's own file passes within it.
- `git diff --name-only 191b5e1e~1..HEAD` → exactly the three `files_modified`: `scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json`, `scripts/prod-prober/run.mjs`, `src/__tests__/prod-prober-wiring.test.ts`.
- `git status --porcelain -- supabase/ analytics-service/` re-checked at every task boundary → empty every time.
- `npx eslint scripts/prod-prober/run.mjs src/__tests__/prod-prober-wiring.test.ts` → clean.
- `npx tsc --noEmit` → no errors attributable to this plan's files (one implicit-`any` parameter type error was caught and fixed during Task 1, before commit).
- `cron-manifest.json` left byte-identical: confirmed via `cmp` + matching sha256 after the anti-vacuity neuter/restore cycle.

## Next Phase Readiness

- `T-OPEN-04`'s and `[164.8.5-MANIFEST-SIDE-LOOP-DEAD]`'s RESIDUAL — the GATE, not the data — is now closed by a red CI check observed to fail in both directions (real neuter + 5 calibration mutants) and by a self-test scenario proving the runtime route end-to-end.
- `T-OPEN-03`'s data half remains recorded as already discharged (2026-09-11 re-capture); this plan does not re-open it as work.
- Plan 08 (wave 3) and plan 09 (wave 4, founder gate D4, live PROD session) are unaffected by this plan's scope — no blockers introduced.
- The 4 pre-existing full-suite failures documented above are NOT this plan's to fix; they are visible via `.planning/WINDOWS.md` entry #56 for whichever phase owns them.

## Self-Check: PASSED

`src/__tests__/prod-prober-wiring.test.ts`, `scripts/prod-prober/run.mjs`, and `scripts/prod-prober/fixtures/cron-drift/manifest-normalization-stale.json` confirmed present and modified/created on disk via `git status`.
Both commits (`191b5e1e`, `8ae43f8b`) confirmed present via `git log --oneline --all`.

---
*Phase: 164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t*
*Plan: 07*
*Completed: 2026-09-16*
