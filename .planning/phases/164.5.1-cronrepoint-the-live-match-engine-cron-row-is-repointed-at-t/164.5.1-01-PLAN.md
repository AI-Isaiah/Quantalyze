---
phase: 164.5.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TODOS.md
  - scripts/lint-app-guc.mjs
  - docs/runbooks/match-engine.md
autonomous: true
requirements:
  - T-OPEN-03
  - T-OPEN-04
  - 164.5.1-GATEWAY-CEILING-INVERSION
  - CRON-DRIFT-01

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`grep -c` over TODOS.md returns >= 1 for each of the three ids `164.5.1-GATEWAY-CEILING-INVERSION`, `T-OPEN-03` and `T-OPEN-04` — the FANOUT-GLOBAL-01 failure mode (a requirement that exists only as ROADMAP prose) is closed for all three"
    - "Each of the three new TODOS entries names `Phase 164.5.1 CRONREPOINT` as owner and carries the dated measurement it rests on, so a reader can re-derive rather than trust it"
    - "Every live-file mention of the superseded pointer also carries the redirect to Phase 164.5.1, so one grep surfaces the historical mention AND its correction in the same result set (criterion 6, as amended 2026-09-16)"
    - "`node scripts/lint-app-guc.mjs --self-test` passes and `node scripts/lint-app-guc.mjs` reports 0 findings after the allowlist `reason:` edit — the `occurrences` and `successor` count pins did not move"
    - "`git status --porcelain -- supabase/migrations/` is empty at every task boundary, so no path under `supabase/migrations/` is touched — the three applied migrations stay BYTE-IDENTICAL because PROD's ledger stores their applied SQL, comments included"
    - statement: "A reader arriving at any of the three surviving applied-migration mentions of the superseded pointer reaches Phase 164.5.1 within one grep, because the live-file redirects spell the same literal"
      verification: backstop
  artifacts:
    - path: "TODOS.md"
      provides: "Three new backlog entries (gateway-ceiling inversion, T-OPEN-03, T-OPEN-04) + the criterion-6 redirect at the three existing mentions"
    - path: "scripts/lint-app-guc.mjs"
      provides: "LINEAGE_ALLOWLIST `reason:` string for 20260408215026 repointed at Phase 164.5.1, counts unmoved"
    - path: "docs/runbooks/match-engine.md"
      provides: "Criterion-6 redirect in the 1a-bis section"
  key_links:
    - "TODOS.md id `[164.5.1-GATEWAY-CEILING-INVERSION]` <-> ROADMAP Phase 164.5.1 requirements list <-> Plan 03's migration and Plan 02's retry seam"
    - "`scripts/lint-app-guc.mjs` LINEAGE_ALLOWLIST entry for `20260408215026` <-> its `-- APP-GUC-LINEAGE:` header in that migration (count-pinned in both directions)"
  prohibitions:
    - "Never edit any file under `supabase/migrations/` in this plan, not even a comment. PROD's migration ledger stores the APPLIED SQL including comments and `scripts/prod-body-drift-check.sh` compares against it, so a comment-only edit manufactures a measurable repo-vs-ledger divergence — the class Phase 164.8.6 graded a reviewer Critical."
    - "Never move the `occurrences:` or `successor:` values in `LINEAGE_ALLOWLIST` while editing a `reason:` string — those are the count pins the gate actually validates, and a prose edit that moves them turns a documentation change into a gate change."
    - "Never delete or rewrite the superseded pointer text. The correction is ADDITIVE: the historical sentence stays so the record of what was true on its date survives, and the redirect sits beside it."
    - "Never write a TODOS entry that asserts a measurement without naming its date and its instrument. An undated claim in this ledger is the `[164.5.1-CITATION]` shape the milestone already records — a sentence nobody can re-run."
---

<objective>
Close the `FANOUT-GLOBAL-01` failure mode for this phase and discharge criterion 6 (as amended
2026-09-16). Three of this phase's eight requirement IDs exist ONLY as ROADMAP prose — measured
2026-09-16, zero `TODOS.md` hits for `T-OPEN-03`, `T-OPEN-04` and
`[164.5.1-GATEWAY-CEILING-INVERSION]` — and the ROADMAP itself says the third is OWED an entry.
CONTEXT.md makes writing them this phase's FIRST act. This plan is numbered 01 for that reason;
it is dispatched in wave 1 alongside the tracer (plan 02), which touches a disjoint file set.

⚠️ **Why this plan carries no `type="tracer"` task, recorded so it is a decision and not an
omission.** The phase's tracer — the thinnest production-quality path wired through every layer the
phase modifies — is `164.5.1-02` Task 1 (the fail-closed kill switch, end to end). This plan
touches no code layer at all: it is a ledger and prose act on `TODOS.md`, one allowlist string and
one runbook bullet, file-disjoint from the tracer and from every expansion task. Putting a tracer
here would mean inventing a code slice for a documentation plan. Every EXPANSION task in this phase
sits in wave 2 or later, so the tracer is still verified before any of them — the tracer-first
invariant holds across the phase, and the locked "first act" instruction holds at plan 01.

Then repoint every LIVE-file mention of the superseded "Phase 164.5 item 7" pointer at this phase,
leaving the three APPLIED migrations byte-identical.

Locked decisions recorded and implemented here: the criterion-6 SCOPE AMENDMENT of 2026-09-16
(mirrored in ROADMAP.md and CONTEXT.md "Locked by the ROADMAP"), which is downstream of D2 (D-02)
— the same no-edit-an-applied-migration mechanism that forbids a schedule migration forbids a
comment-only edit to one.

Purpose: a requirement that lives only in a ROADMAP paragraph has no owner, no date and no gate.
This plan gives all three an entry in the single ground-truth backlog, and makes the tree's stale
pointers self-correcting under a grep.

Output: three new `TODOS.md` entries; redirects at four live-file sites; zero movement in
`lint-app-guc.mjs`'s count pins; zero diff under `supabase/migrations/`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-CONTEXT.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-RESEARCH.md
@TODOS.md
@scripts/lint-app-guc.mjs
@docs/runbooks/match-engine.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the three missing TODOS.md entries — the phase's first act</name>
  <reversibility rating="reversible">A backlog entry is prose in one file; correcting or
  withdrawing it costs one edit and breaks nothing that reads it.</reversibility>
  <files>TODOS.md</files>
  <read_first>
    - `TODOS.md` — read the `## 🔴 FIX NOW` section header and at least two existing entries in
      full (e.g. `[SHARE-HOST-01]` near the top, and `[PREFLIGHT-P3C-UNRUNNABLE]`) to copy the
      house entry shape: `- [ ] **\`[ID]\` <one-sentence claim>.**` then MEASURED-on-a-date prose,
      a **Cause, exactly:** or **Fix:** paragraph, and an `Owner:` sentence.
    - `.planning/ROADMAP.md`, the Phase 164.5.1 section — the `[164.5.1-GATEWAY-CEILING-INVERSION]`
      measurement block (`pg_roles.rolconfig` readings, the `44.67s`/60s figures, the 5-of-6 500s,
      Sentry `QUANTALYZE-1D`) and the `T-OPEN-03` / `T-OPEN-04` prose in the **Requirements** line.
      These are the source of every figure the new entries carry.
    - `.planning/phases/164.5.1-.../164.5.1-CONTEXT.md` — the "Locked by the ROADMAP" bullet that
      makes this the first act, and the `<specifics>` block carrying the dated readings.
  </read_first>
  <action>
Add three entries to `TODOS.md`. Place `[164.5.1-GATEWAY-CEILING-INVERSION]` in the
`## 🔴 FIX NOW` section (it is a live, dated production degradation); place `[T-OPEN-03]` and
`[T-OPEN-04]` adjacent to the existing `[164.8.5-MANIFEST-SIDE-LOOP-DEAD]` entry so the three
Phase-164.8.5 residuals read together. Follow the house entry shape exactly (checkbox, bolded
bracketed id, one-sentence claim, dated measurement, owner).

`[164.5.1-GATEWAY-CEILING-INVERSION]` must state, with its date 2026-09-13 and its instrument:
`service_role` is the only PROD role with no `statement_timeout` of its own and therefore inherits
the 120000 ms database default, while Supabase's API gateway gives up at 60 s — so the ceiling that
bites a service-role request sits BELOW the one Postgres enforces and a slow read returns an opaque
504 instead of a catchable Postgres error. Carry the sibling readings verbatim (`anon` 3s,
`authenticated` 8s, `authenticator` 8s), the one successful cron run at `duration_s: 44.67`
(`net._http_response` id 3758), the 5 of 6 retained runs returning 500 with `timed_out: false` and
`error_msg: null`, the negative contention reading (`pg_stat_statements` returns ZERO rows with
`max_exec_time > 20s`), and the blast radius beyond the cron (`_claim_priority` Sentry
`QUANTALYZE-11` 14 events, `_reset` `QUANTALYZE-X` 3 events). State that the fix is
`ALTER ROLE service_role SET statement_timeout = '45s'` by FORWARD MIGRATION, never a console
statement, and that it is a LEGIBILITY fix which makes nothing faster. Owner:
`Phase 164.5.1 CRONREPOINT`. Add the standing note that these are dated readings bound to a
session and must be regenerated rather than trusted.

`[T-OPEN-03]` must state: `retention_compute_jobs_orphaned_running` was committed to
`scripts/prod-prober/cron-manifest.json` with 0 newlines and lexed to a pure comment, so
`visible = 0 of 1791` characters; graded `low` on measurement rather than accepted as booked,
because a spliced key in that row still fires `long-token-anywhere` via the comment producer and
`jwt-shape` / `service-role` / `pg-password` all still fire — only the header-anchored rules and
`vault-absent` were lost, making it an ATTRIBUTION gap and not a credential blind spot. Record that
the DATA half was discharged by the 2026-09-11 re-capture at `ws-collapse-v2` (capture run
`34611594511`, landed by PR #776) and that the RESIDUAL owned here is the CI GATE, not the data.
Owner: `Phase 164.5.1 CRONREPOINT`, plan 07.

`[T-OPEN-04]` must state: the committed manifest declared `normalization: ws-collapse-v1` while the
arm computed `ws-collapse-v2`, and two normalizations produce two different shas for identical text
— so the cron-drift arm was performing NO comparison at all and `manifest-invalid` fired on every
production run. Record that the mismatch was caught by an hourly PRODUCTION probe an hour after it
shipped and by nothing in CI; that the DATA was discharged by the same 2026-09-11 re-capture; and
that the RESIDUAL is the missing CI gate — any future normalization bump re-opens it identically.
Owner: `Phase 164.5.1 CRONREPOINT`, plan 07.

⛔ Do not quote the MT5 account number, any broker server name, any secret, or any absolute home
path — `TODOS.md` is world-readable in a PUBLIC repository.
  </action>
  <acceptance_criteria>
    - `grep -c '\[164.5.1-GATEWAY-CEILING-INVERSION\]' TODOS.md` returns a value >= 1 (it was 0 on
      2026-09-16).
    - `grep -c '\[T-OPEN-03\]' TODOS.md` and `grep -c '\[T-OPEN-04\]' TODOS.md` each return >= 1
      (both were 0 on 2026-09-16).
    - Each of the three new entries contains the literal string `Phase 164.5.1 CRONREPOINT` on an
      `Owner:` line.
    - The gateway-ceiling entry contains the literals `44.67`, `60`, `120000`, `QUANTALYZE-1D` and
      `statement_timeout`.
    - `git status --porcelain` for this task lists `TODOS.md` and no other path (the working
      tree, read before the task's commit).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; for id in "164.5.1-GATEWAY-CEILING-INVERSION" "T-OPEN-03" "T-OPEN-04"; do n=$(grep -c "\[$id\]" TODOS.md); echo "$id -> $n"; [ "$n" -ge 1 ]; done; o=$(grep -c "Phase 164.5.1 CRONREPOINT" TODOS.md); echo "owner-lines -> $o"; [ "$o" -ge 3 ]'</automated>
    <fails_when>non-zero exit, or any `-&gt; 0` line in the output, or `owner-lines -&gt; 0/1/2`</fails_when>
    <automated>bash -c 'out=$(git status --porcelain -- supabase/migrations/); st=$?; if [ "$st" -ne 0 ]; then echo "git status FAILED"; exit 1; fi; printf "migrations-touched:[%s]\n" "$out"; test -z "$out"'</automated>
    <fails_when>non-zero exit, the line `git status FAILED`, or `migrations-touched:[]` printing anything between the brackets (this deliberately reads the WORKING TREE, because the executor runs verify before committing the task)</fails_when>
  </verify>
  <done>All three requirement IDs have a dated, owned `TODOS.md` entry carrying the measurement it
  rests on; no migration file was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint every LIVE-file mention of the superseded pointer at this phase (criterion 6, amended)</name>
  <files>TODOS.md, scripts/lint-app-guc.mjs, docs/runbooks/match-engine.md</files>
  <read_first>
    - `scripts/lint-app-guc.mjs` lines 279-320 — the `LINEAGE_ALLOWLIST` array. Read the entry for
      `20260408215026_schedule_match_cron_hourly.sql` in full: its `occurrences: 4` and
      `successor:` fields are the count pins the gate validates; the `reason:` field is a plain
      concatenated JS string literal and is the only field this task edits.
    - `scripts/lint-app-guc.mjs` lines 590-630 and 795-810 — the assertions that compare an
      allowlist entry against the file's own `-- APP-GUC-LINEAGE:` header, so you can see exactly
      which fields a mismatch would fire on.
    - `docs/runbooks/match-engine.md` lines 20-35 — the `1a-bis` section; the superseded pointer is
      the second bullet.
    - `TODOS.md` around lines 1960-1985 (the `[164.7-VAULT-ABSENT-RULE]` entry, two mentions) and
      around line 3345 (the `[CRON-DRIFT-01]` narrative, one mention).
    - `.planning/ROADMAP.md` Phase 164.5.1 success criterion 6 and its 2026-09-16 SCOPE AMENDMENT —
      the authority for editing three files and not five.
  </read_first>
  <action>
At each of the four LIVE-file sites below, keep the existing sentence intact and append a redirect
that spells the SAME literal a reader would grep, so the historical mention and its correction land
in one result set. The redirect sentence must contain the literal `Phase 164.5 item 7` and the
literal `Phase 164.5.1 CRONREPOINT`, and must say the item was SPLIT OUT on 2026-09-07 by founder
decision — it was not renamed and it was not dropped.

The four sites:
1. `TODOS.md`, inside the `[164.7-VAULT-ABSENT-RULE]` entry — both the mid-entry mention and the
   trailing `Owner:` sentence. Change the owner to `Phase 164.5.1 CRONREPOINT` and keep the
   historical phrasing beside it.
2. `TODOS.md`, inside the `[CRON-DRIFT-01]` narrative where it says the LIVE-ROW half stays open
   for the superseded item.
3. `docs/runbooks/match-engine.md`, the `1a-bis` bullet beginning "⚠️ **The LIVE `cron.job` row is
   not yet pointed at that function.**".
4. `scripts/lint-app-guc.mjs`, the `reason:` string of the `LINEAGE_ALLOWLIST` entry for
   `20260408215026_schedule_match_cron_hourly.sql` — edit ONLY the `reason:` concatenated string.
   Leave `file:`, `occurrences: 4` and `successor:` byte-identical.

⛔ Do NOT touch `supabase/migrations/20260408215026_schedule_match_cron_hourly.sql`,
`supabase/migrations/20260408113029_cron_heartbeat.sql`,
`supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql`, or
`supabase/migrations/20260911120000_vault_tick_hardening.sql`. Their text is correct for their
date, and PROD's migration ledger stores the applied SQL comments included. This is the criterion-6
amendment, not an omission. `supabase/schema/baseline.sql` is a generated dump and already names
Phase 164.5.1 — leave it alone too.

⛔ Do NOT add a CI gate against future dead pointers. That was considered and recorded as a
deliberate NOT-done in the amendment: anti-vacuity is scoped to data-integrity and user-facing
behaviour, and a prose-pointer gate is the unbounded tail that scoping exists to cut.
  </action>
  <acceptance_criteria>
    - `grep -c 'Phase 164.5.1 CRONREPOINT' docs/runbooks/match-engine.md` returns >= 1 and
      `grep -c 'Phase 164.5 item' docs/runbooks/match-engine.md` still returns >= 1 — both the
      historical mention and the redirect are present at the same site.
    - `grep -c 'Phase 164.5.1' scripts/lint-app-guc.mjs` returns >= 1.
    - The `LINEAGE_ALLOWLIST` entry for `20260408215026_schedule_match_cron_hourly.sql` still reads
      `occurrences: 4` and its `successor:` is still
      `20260907120000_analytics_service_settings_and_vault_tick.sql`.
    - `node scripts/lint-app-guc.mjs --self-test` prints its OK line and exits 0.
    - `node scripts/lint-app-guc.mjs` prints 0 findings and exits 0.
    - `git status --porcelain -- supabase/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/lint-app-guc.mjs --self-test</automated>
    <fails_when>non-zero exit, or the `self-test OK` line is absent from stdout</fails_when>
    <automated>node scripts/lint-app-guc.mjs</automated>
    <fails_when>non-zero exit, or the printed findings count is anything other than 0</fails_when>
    <automated>bash -c 'set -e; grep -q "occurrences: 4" scripts/lint-app-guc.mjs; grep -c "Phase 164.5.1" scripts/lint-app-guc.mjs; grep -c "Phase 164.5.1 CRONREPOINT" docs/runbooks/match-engine.md; out=$(git status --porcelain -- supabase/); st=$?; if [ "$st" -ne 0 ]; then echo "git status FAILED"; exit 1; fi; printf "supabase-touched:[%s]\n" "$out"; test -z "$out"'</automated>
    <fails_when>non-zero exit, either grep count printing `0`, the line `git status FAILED`, or `supabase-touched:[]` printing anything between the brackets</fails_when>
  </verify>
  <done>All four live-file sites carry the redirect beside the historical mention; the app-GUC
  lint self-test and corpus scan are both green with unmoved count pins; nothing under `supabase/`
  changed.</done>
</task>

</tasks>

<edge_assumptions>
## Edge-coverage assumptions — the CONSOLIDATED 21-item table for Phase 164.5.1

`164.5.1-EDGE-COVERAGE.json` reports `applicable: 21, resolved: 0, unresolved: 21`. Every row is
therefore an EXPLICIT FLAGGED ASSUMPTION, never a silent drop and never auto-resolved. None is
lifted into `must_haves.truths` as a resolved edge, because the probe resolved none. The owning
plan surfaces each row again beside the work it constrains.

| # | Requirement | Category | Probe | Owning plan |
|---|---|---|---|---|
| 1 | CRON-DRIFT-01 | unclassified | unclassified — review manually | 164.5.1-04 |
| 2 | 164.7-VAULT-ABSENT-RULE | empty | result for empty / single-element / null input? | 164.5.1-04 |
| 3 | 164.7-VAULT-ABSENT-RULE | encoding | whose definition of length/equality — bytes, code points, grapheme clusters, normalized form? | 164.5.1-04 |
| 4 | VAULTTICK-EMPTYKEY-01 | adjacency | exactly equal or just touching — merge, collide or separate? | 164.5.1-09 |
| 5 | VAULTTICK-EMPTYKEY-01 | empty | result for empty / single-element / null input? | 164.5.1-09 |
| 6 | VAULTTICK-EMPTYKEY-01 | ordering | when elements compare equal, is output order specified and stable? | 164.5.1-09 |
| 7 | 164.7-ACTIVATION-DEFERRED | adjacency | exactly equal or just touching — merge, collide or separate? | 164.5.1-08 |
| 8 | 164.7-ACTIVATION-DEFERRED | empty | result for empty / single-element / null input? | 164.5.1-08 |
| 9 | 164.7-ACTIVATION-DEFERRED | ordering | when elements compare equal, is output order specified and stable? | 164.5.1-08 |
| 10 | 164.7-ACTIVATION-DEFERRED | idempotency | what happens if this runs twice on the same input? | 164.5.1-08 |
| 11 | 164.7-ACTIVATION-DEFERRED | concurrency | if interrupted or run in parallel, what is guaranteed? | 164.5.1-08 |
| 12 | 164.8.5-MANIFEST-SIDE-LOOP-DEAD | unclassified | unclassified — review manually | 164.5.1-07 |
| 13 | T-OPEN-03 | unclassified | unclassified — review manually | 164.5.1-01 (below) |
| 14 | T-OPEN-04 | adjacency | exactly equal or just touching — merge, collide or separate? | 164.5.1-07 |
| 15 | T-OPEN-04 | empty | result for empty / single-element / null input? | 164.5.1-07 |
| 16 | T-OPEN-04 | encoding | whose definition of length/equality applies? | 164.5.1-07 |
| 17 | T-OPEN-04 | ordering | when elements compare equal, is output order specified and stable? | 164.5.1-07 |
| 18 | 164.5.1-GATEWAY-CEILING-INVERSION | adjacency | exactly equal or just touching — merge, collide or separate? | 164.5.1-02 |
| 19 | 164.5.1-GATEWAY-CEILING-INVERSION | empty | result for empty / single-element / null input? | 164.5.1-02 |
| 20 | 164.5.1-GATEWAY-CEILING-INVERSION | ordering | when elements compare equal, is output order specified and stable? | 164.5.1-02 |
| 21 | 164.5.1-GATEWAY-CEILING-INVERSION | concurrency | if interrupted or run in parallel, what is guaranteed? | 164.5.1-09 |

No-silent-drop equality: 21 probe-surfaced items == 0 authored as resolved truths + 21 surfaced as
flagged assumptions.

### Row 13 — the assumption this plan owns

`T-OPEN-03` (unclassified): the probe could not classify it, so nothing about it is resolved here.
The standing assumption this plan makes explicit is that a `TODOS.md` entry is a sufficient home
for `T-OPEN-03`'s residual — i.e. that recording the ATTRIBUTION gap (header-anchored rules and
`vault-absent` lost on a newline-folded row, other credential rules unaffected) in prose, with the
CI gate owned by plan 07, closes it. If a reviewer judges that the attribution gap needs its own
runtime control rather than a gate on the normalization key, that is a NEW finding and must be
raised, not absorbed silently.
</edge_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> PROD migration ledger | `scripts/prod-body-drift-check.sh` compares repo migration text against the SQL PROD's ledger recorded at apply time; a repo-side edit crosses this boundary without any apply |
| repo -> public internet | This repository is PUBLIC and `TODOS.md` is tracked; every sentence written here is world-readable on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5.1-01-01 | Tampering | `supabase/migrations/2026040821*`, `2026040811*`, `2026090712*`, `2026091112*` | high | mitigate | The plan edits none of them; Task 2's verify asserts `git status --porcelain -- supabase/` is empty, so an accidental edit is a red check rather than a review catch |
| T-164.5.1-01-02 | Tampering | `scripts/lint-app-guc.mjs` `LINEAGE_ALLOWLIST` | medium | mitigate | Only the `reason:` string is edited; the acceptance criterion re-greps `occurrences: 4` and both gate legs (`--self-test`, corpus scan) run in `<verify>` |
| T-164.5.1-01-03 | Information Disclosure | `TODOS.md` in a PUBLIC repo | high | mitigate | The action forbids quoting the MT5 account number, broker server names, secrets and absolute home paths; the new entries carry Sentry issue SLUGS and run ids only, never credential material |
| T-164.5.1-01-04 | Repudiation | the three new backlog entries | medium | mitigate | Every entry carries its measurement date and instrument (run id, Sentry slug, PR number) so a later reader re-derives instead of trusting; an undated claim is named a defect in the prohibitions |
| T-164.5.1-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan or this phase — RESEARCH.md § Package Legitimacy Audit records `N/A — no external packages are installed by this phase`, and every seam consumed is already a dependency |
</threat_model>

<assumption_delta_decision>
`gsd-tools query assumption-delta scan 164.5.1` reported `detected: true` with four signals
(`pluralization`: "alongside", "also", "second"; `chosen`: "choose"). Three are prose artifacts of
the ROADMAP's own lineage sentences and carry no modeling change.

The one real transition in this phase is in plan 02, not here: `db_execute` is today the ONE seam
every Supabase read passes through, and this phase adds `db_read_with_retry` **alongside** it.

- Noun now primary: still `db_execute` — it remains the single executor seam; `db_read_with_retry`
  wraps it rather than replacing it.
- Decision: **add-alongside**, with a one-line rationale — CONTEXT.md Area 1 explicitly forbids
  promotion ("⛔ Do NOT retry inside `db_execute` itself — that silently changes timing semantics
  for every caller, including ones that must fail fast").
- Accepted debt: two read seams now exist and a future reader must know which one a call site
  wants. What would force a later promote: if a majority of `db_execute` call sites end up wrapped
  in `db_read_with_retry`, the retry stops being the exception and the default should invert —
  at that point promote and give the fail-fast callers an explicit opt-out.

Advisory only; nothing in this phase is blocked on it.
</assumption_delta_decision>

## Artifacts this phase produces

Produced by THIS plan:
- `TODOS.md` entry id `[164.5.1-GATEWAY-CEILING-INVERSION]` (new)
- `TODOS.md` entry id `[T-OPEN-03]` (new)
- `TODOS.md` entry id `[T-OPEN-04]` (new)
- Redirect text at four live-file sites containing the literals `Phase 164.5 item 7` and
  `Phase 164.5.1 CRONREPOINT`

Produced elsewhere in the phase (listed so the set is visible from any plan):
- `_is_gateway_timeout`, `DB_READ_MAX_RETRIES`, `DB_READ_BACKOFF_BASE_S`, `db_read_with_retry` —
  `analytics-service/services/db.py` (plan 02)
- `KILL_SWITCH_ENABLED`, `KILL_SWITCH_DISABLED`, `KILL_SWITCH_UNAVAILABLE` and the async
  `_engine_is_enabled` / `_engine_is_enabled_cached` — `analytics-service/routers/match.py` (plan 02)
- `supabase/migrations/20260916120000_service_role_statement_timeout.sql` (plan 03)
- `src/__tests__/service-role-statement-timeout-migration.test.ts` (plan 03)
- `preflightCronRepoint` + the `--preflight-repoint` CLI flag — `scripts/prod-prober/run.mjs` (plan 04)
- Self-test scenario "the repoint pre-flight ABORTS on a mismatched manifest and writes nothing"
  and `SELF_TEST_SCENARIOS` 80 -> 81 (plan 04)
- `MATCH_ENGINE_CURSOR_KEY`, `_read_cron_cursor`, `_write_cron_cursor`, `CRON_BATCH_SIZE` and the
  `partial` status value + `next_cursor` key — `analytics-service/routers/match.py` (plan 06)
- Vitest case "the committed cron manifest declares the normalization the arm computes" +
  its calibration, and `SELF_TEST_SCENARIOS` 81 -> 82 (plan 07)
- `docs/runbooks/match-engine.md` § "Go-live — repointing `match_engine_cron`" (plan 08)
- `.planning/phases/164.5.1-.../164.5.1-PROD-SESSION.md` execution record (plan 09)

<verification>
- Both tasks' `<automated>` commands green.
- `git show --stat` on each of the plan's commits lists only `TODOS.md`, `scripts/lint-app-guc.mjs`
  and `docs/runbooks/match-engine.md`.
- `npx vitest run src/__tests__/check-planning-hygiene.test.ts` still green (this plan touches
  `.planning/` only through its own PLAN/SUMMARY).
</verification>

<success_criteria>
- All three previously-ROADMAP-only requirement IDs have a dated, owned `TODOS.md` entry.
- Criterion 6 (as amended) is discharged across the three LIVE files, with the three applied
  migrations byte-identical.
- `lint-app-guc.mjs` self-test and corpus scan are both green and its count pins are unmoved.
</success_criteria>

<output>
Create `.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-01-SUMMARY.md` when done
</output>
