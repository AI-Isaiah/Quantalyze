---
phase: 164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t
plan: 03
subsystem: database
tags: [postgres, supabase, migration, statement_timeout, vitest, static-gate, anti-vacuity]

# Dependency graph
requires:
  - phase: 164.5.1-01
    provides: "the OWED TODOS.md entry for [164.5.1-GATEWAY-CEILING-INVERSION] this plan's requirement ID depends on being tracked"
provides:
  - "supabase/migrations/20260916120000_service_role_statement_timeout.sql — the forward migration setting service_role's statement_timeout to 45s, reviewable but NOT applied from this checkout"
  - "src/__tests__/service-role-statement-timeout-migration.test.ts — the static gate over that migration, calibrated to fail RED on the value band in both directions, on any schedule-registration verb however assembled, and on a misapplied prod-body-ack/APP-GUC-LINEAGE pragma"
affects: [164.5.1-09]

# Actuals (#2632) — pairs with the plan's estimate to calibrate future estimates.
actuals:
  tokens: 5914
  tasks: 2
  commits: 2
  plan_head_before: 8ee99ede924ee7de38435d319feafdbeabecc6af

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Raw-text gate hygiene: the forbidden literals (schedule-registration verbs, the PROD-body acknowledgment pragma token, the custom-GUC lineage-header token) are spelled ONLY in the gate file. The artifact under test is written deliberately not to name any of them — in prose or otherwise — so the gate's own raw-text scan is never self-tripping."
    - "Value-band assertion with STRICT inequalities on both sides, calibrated by synthetic mutants in BOTH directions (too high AND too low), following this repo's fail-in-either-direction ratchet convention (mutation-runner-floors.test.ts precedent)."
    - "A built-in Postgres GUC (statement_timeout) set by ALTER ROLE is a materially different, unrestricted code path from the app.* custom placeholder GUCs this repo's history shows failing with 42501 — the two must not be conflated as the same risk class."

key-files:
  created:
    - supabase/migrations/20260916120000_service_role_statement_timeout.sql
    - src/__tests__/service-role-statement-timeout-migration.test.ts
  modified: []

key-decisions:
  - "Corrected a self-contradiction discovered mid-execution between Task 1's acceptance criteria (raw migration text must have ZERO occurrences of the prod-body-ack / APP-GUC-LINEAGE tokens) and its own action prose (which spelled both repeatedly, unlike its explicit treatment of the schedule-registration verbs). Resolved under Rule 1 (bug fix, not scope change): extended the SAME 'describe, don't spell' hygiene the plan already mandated for the schedule verbs to these two tokens as well, since the acceptance criteria and Task 2's static gate are the actual verifiable contract, not the action's illustrative prose."
  - "Measured (never transcribed) the superseded prod-body-ack pragma count at HEAD, 2026-09-16: 20260906120000_computation_error_provenance.sql=1, 20260907130000_ledger_refresh_switch_to_system_flags.sql=2, 20260911120000_vault_tick_hardening.sql=1, 20260911130000_ledger_fanout_grantees_and_dormancy.sql=2, TOTAL=6. The ROADMAP's prose says 'three' beside four named files; the migration header carries the measured number and the date."
  - "Confirmed by grep that the only ALTER ROLE/ALTER DATABASE hits anywhere in supabase/migrations/** are the three files CONTEXT.md/RESEARCH.md name (20260407164606, 20260408113029, 20260408215026), every one comment-only and targeting an app.* custom GUC — no executed ALTER ROLE precedent exists in this repo's history. The statement was written directly from Supabase's documented form, not copied from any in-tree precedent."
  - "The vitest gate's string-concatenation-folding regex omits the 's' (dotAll) flag present in the shape of the Python original (test_ledger_refresh_gates.py's _fold_sql_string_concatenations) because this repo's tsconfig targets ES2017 (pre-ES2018, where the 's' flag is unavailable — confirmed by a real tsc error during verification). Semantically identical here: the character classes used never rely on '.' matching a newline."

patterns-established:
  - "Pattern 1: a migration/gate pair where the gate OWNS every forbidden literal and the migration is written to never spell any of them — makes the raw-text scan legitimate rather than self-tripping, and keeps the migration's own header prose from ever accidentally satisfying or defeating the gate it must pass."
  - "Pattern 2: a value-band gate states its failure message with BOTH bounds and their provenance (the measured baseline; the platform ceiling) every time it fires, regardless of which direction was violated — the message is useful on its own without cross-referencing the source."

requirements-completed: []  # 164.5.1-GATEWAY-CEILING-INVERSION is shared across 6 plans in this phase (01, 02, 03, 05, 06, 09) and stays BLOCKED (shared-ID gate #2388) until every declaring plan finishes. Not marked complete by this plan alone.

coverage:
  - id: D1
    description: "One new forward migration (20260916120000_service_role_statement_timeout.sql) exists, carrying exactly one ALTER ROLE statement setting service_role's statement_timeout to '45s' (strictly inside the (44.67s, 60s) band), registering no schedule and removing none, carrying no prod-body-ack or APP-GUC-LINEAGE pragma, no applied migration touched"
    requirement: "164.5.1-GATEWAY-CEILING-INVERSION"
    verification:
      - kind: other
        ref: "Task 1's own automated <verify>: filename regex match; comment-stripped grep confirms exactly 1 ALTER ROLE line and 1 service_role occurrence; git status --porcelain -- supabase/migrations/ shows exactly one untracked new path"
        status: pass
      - kind: unit
        ref: "src/__tests__/service-role-statement-timeout-migration.test.ts > service_role statement_timeout migration — static gate > the shipped migration passes every assertion"
        status: pass
    human_judgment: false
  - id: D2
    description: "A repo-owned static gate fails RED if the timeout value leaves the (44670ms, 60000ms) band in EITHER direction, if a schedule-registration verb appears anywhere in the migration's raw text however assembled (including via string concatenation), or if a prod-body-ack / APP-GUC-LINEAGE pragma is added — every failure mode observed against a synthetic mutant, not merely argued"
    requirement: "164.5.1-GATEWAY-CEILING-INVERSION"
    verification:
      - kind: unit
        ref: "npx vitest run src/__tests__/service-role-statement-timeout-migration.test.ts (12/12 passed, including 9 calibration cases: 60s-too-high, 40s-too-low, comment-embedded verb, concatenation-assembled verb, misapplied prod-body-ack pragma, misapplied APP-GUC-LINEAGE pragma, empty file, truncated file, missing/duplicate ALTER ROLE)"
        status: pass
      - kind: other
        ref: "grep -cE 'it\\.skip|describe\\.skip|it\\.only|--watch' src/__tests__/service-role-statement-timeout-migration.test.ts -> 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "Whether ALTER ROLE service_role SET statement_timeout actually applies successfully against THIS project's hosted Supabase instance (RESEARCH assumption A3, [CITED] not [VERIFIED]), and what pg_roles.rolconfig reads on PROD afterward"
    verification: []
    human_judgment: true
    rationale: "Deliberately NOT verified by this plan. Criterion 7's own oracle is a post-apply pg_roles.rolconfig read on PROD, gated behind D4's three-reviewer + founder-execution human review — that is plan 09's recorded execution output, not something this plan (which performs no local push and applies nothing) can establish. The PROD role configuration remains genuinely unverified as of this SUMMARY."

# Metrics
duration: ~35min
completed: 2026-09-16
status: complete
---

# Phase 164.5.1 Plan 03: Gateway-Ceiling Migration + Static Gate Summary

**A reviewable, unapplied forward migration sets service_role's statement_timeout to 45s (strictly inside the 44.67s-60s band), paired with a calibrated vitest gate that fails RED on the value band in either direction, on any schedule-registration verb however assembled, and on a misapplied PROD-body-ack or custom-GUC-lineage pragma.**

## Performance

- **Duration:** ~35 min
- **Started:** ~2026-09-16T18:35Z (following plan 02's completion)
- **Completed:** 2026-09-16T19:10Z
- **Tasks:** 2 completed
- **Files modified:** 2 (both new)

## Accomplishments

- `supabase/migrations/20260916120000_service_role_statement_timeout.sql`: the single `ALTER ROLE service_role SET statement_timeout = '45s';` statement, with a header carrying full OPS/rollback/carry-forward provenance and deliberately naming no schedule-registration verb, no PROD-body-ack pragma, and no custom-GUC-lineage token anywhere in its raw text.
- `src/__tests__/service-role-statement-timeout-migration.test.ts`: the static gate over that migration — anti-vacuity floor first, exact ALTER ROLE count, the value band (strict both directions), the folded raw-text no-schedule-verb scan, the no-misapplied-pragma scan, and header provenance — with a 9-case calibration block proving every failure mode against a synthetic mutant.
- The schema-push gate's recorded project override was honored throughout: no `supabase db push`, `db reset --linked`, `--project-ref`, or `--db-url` command was run at any point; the apply path (merge -> `apply-test` on shared TEST -> PROD's human-reviewed `apply`) is stated in the migration's own header.
- D1/D2's scope (forbidding a migration that schedules or unschedules, not one that sets a role GUC) is recorded in the migration's own header prose, so no future reviewer reads the two as a conflict.

## Task Commits

Each task was committed atomically:

1. **Task 1: [BLOCKING] Write the forward migration setting service_role's statement_timeout** - `5acfb710` (feat)
2. **Task 2: The static gate over that migration — value band, no schedule, no misapplied pragma** - `6dfcdf44` (test)

**Plan metadata:** (pending — this commit)

## Files Created/Modified

- `supabase/migrations/20260916120000_service_role_statement_timeout.sql` — new forward migration; sets `service_role`'s `statement_timeout` to `'45s'`; no schedule registered or removed; no applied migration touched.
- `src/__tests__/service-role-statement-timeout-migration.test.ts` — new vitest file; exports `assertServiceRoleStatementTimeoutMigration` (the assertion function) and `SCHEDULE_VERBS` (the hand-typed literal constant), per the plan's "Artifacts this phase produces" contract.

## Decisions Made

See `key-decisions` in frontmatter above — summarized: (1) the prod-body-ack/APP-GUC-LINEAGE literal-spelling conflict between Task 1's acceptance criteria and its own action prose was resolved in favor of the verifiable acceptance criteria, extending the schedule-verb hygiene pattern to both tokens; (2) the superseded-pragma count was measured fresh at HEAD (6 across 4 files) rather than transcribed from the ROADMAP's "three"; (3) the ALTER ROLE/ALTER DATABASE grep was re-run and confirmed to show no executed precedent, only comment-only app.* custom-GUC hits; (4) the gate's string-concatenation-fold regex was written without the ES2018-only 's' flag to match this repo's ES2017 tsconfig target, with no semantic change.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Task 1's action prose contradicted its own acceptance criteria on the prod-body-ack / APP-GUC-LINEAGE tokens**
- **Found during:** Task 1 (writing the migration header)
- **Issue:** The plan's Task 1 `<action>` instructed writing prose that repeatedly spells `prod-body-ack` and `APP-GUC-LINEAGE` literally (e.g. "the header must carry the number this task MEASURES," referencing `` `prod-body-ack` `` pragmas by name), while Task 1's own `<acceptance_criteria>` and Task 2's static gate both require the migration's raw text to contain ZERO occurrences of either token — the same hygiene rule the plan explicitly applied to the schedule-registration verbs ("Write this WITHOUT spelling the pg_cron registration verbs anywhere in the file"). An initial draft of the migration file, written literally per the action prose, contained the forbidden tokens 6+ times.
- **Fix:** Rewrote the two carry-forward-correction sections and the "do not add" paragraph to describe the PROD-body acknowledgment pragma and the custom-GUC lineage header without ever spelling either token literally (e.g. "the machine-read pragma `scripts/prod-body-drift-check.sh` greps for" instead of the literal token) — mirroring the existing treatment of the schedule verbs.
- **Files modified:** `supabase/migrations/20260916120000_service_role_statement_timeout.sql`
- **Verification:** `grep -ci "prod-body-ack"` and `grep -ci "app-guc-lineage"` both return `0` against the final file; all of Task 1's automated `<verify>` commands and acceptance criteria pass.
- **Committed in:** `5acfb710` (Task 1 commit; the fix was made before the commit, not as a follow-up)

**2. [Rule 3 - Blocking] The gate's `s` (dotAll) regex flag failed TypeScript compilation under this repo's ES2017 target**
- **Found during:** Task 2 (post-write TypeScript verification, run proactively beyond the plan's stated `<verify>` commands)
- **Issue:** `SQL_CONCAT_RE` was initially written with the `s` flag (mirroring the DOTALL flag in the Python original's regex), which `tsc --noEmit` rejected: "This regular expression flag is only available when targeting 'es2018' or later" — this repo's `tsconfig.json` targets `ES2017`.
- **Fix:** Removed the `s` flag. No semantic change: the pattern's character classes (`[^']`, alternated single-quote-escaped sequences) never rely on `.` matching a newline, so folding behavior is identical with or without the flag.
- **Files modified:** `src/__tests__/service-role-statement-timeout-migration.test.ts`
- **Verification:** `npx tsc --noEmit -p tsconfig.json` reports no errors for this file; `npx vitest run` still passes all 12 tests including the concatenation-fold calibration case.
- **Committed in:** `6dfcdf44` (Task 2 commit; the fix was made before the commit)

---

**Total deviations:** 2 auto-fixed (1 bug — acceptance-criteria/action-prose contradiction; 1 blocking — TS target incompatibility). **Impact on plan:** both fixes are necessary for correctness (the first for the gate to actually be non-self-tripping and pass its own acceptance criteria; the second for the file to compile under this repo's TypeScript configuration). No scope creep — nothing outside Task 1/Task 2's own files was touched.

## Issues Encountered

None beyond the two deviations above.

## Plan-Level Verification

- `npx vitest run src/__tests__/service-role-statement-timeout-migration.test.ts` → **12/12 passed** (1 baseline pass, 1 value-band sanity check, 9 calibration cases, 1 second value-band-and-count check).
- `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` → parses cleanly, no workflow change made.
- `git show --stat` on this plan's two commits → exactly two paths total, one per commit (`supabase/migrations/20260916120000_service_role_statement_timeout.sql`, `src/__tests__/service-role-statement-timeout-migration.test.ts`).
- `grep -cE "it\.skip|describe\.skip|it\.only|--watch" src/__tests__/service-role-statement-timeout-migration.test.ts` → `0`.
- `git status --porcelain -- supabase/migrations/` re-checked at every task boundary → exactly one path, always the new untracked/added file, never a modification.
- ⛔ NOT verified here, deliberately: that the `ALTER ROLE` statement APPLIES on PROD. That is plan 09's recorded `pg_roles.rolconfig` read after D4's human review gate, and it is criterion 7's own oracle — see coverage `D3` above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 09 can now proceed to apply `20260916120000_service_role_statement_timeout.sql` through the standard D4 gate (three reviewers, then merge -> `apply-test` on shared TEST -> PROD's human-reviewed `apply`), and must record the post-apply `pg_roles.rolconfig` read for `service_role` as criterion 7's own oracle. If the apply returns `42501`, the migration's own rollback note directs: record the error verbatim, stop, and do not reach for a console `ALTER ROLE` — criterion 7 would need a different mechanism.
- Plan 02's measured retry budget (~3s worst case, 3 attempts + backoff) is confirmed strictly below the 45s `statement_timeout` this plan sets, as plan 02's SUMMARY already anticipated.
- `164.5.1-GATEWAY-CEILING-INVERSION` remains **blocked** in `REQUIREMENTS.md`'s shared-ID sense (plans 01, 02, 03, 05, 06, 09 all declare it) until the last declaring plan produces its own SUMMARY — this plan's contribution is complete but the requirement itself is not yet markable.
- No blockers for the next plan in this phase.

## Self-Check: PASSED

Both created files (`supabase/migrations/20260916120000_service_role_statement_timeout.sql`, `src/__tests__/service-role-statement-timeout-migration.test.ts`) confirmed present on disk via `[ -f ]`.
Both commits (`5acfb710`, `6dfcdf44`) confirmed present via `git log --oneline --all`.

---
*Phase: 164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t*
*Plan: 03*
*Completed: 2026-09-16*
