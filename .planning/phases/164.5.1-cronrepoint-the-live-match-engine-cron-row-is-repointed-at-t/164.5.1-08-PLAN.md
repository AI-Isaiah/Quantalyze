---
phase: 164.5.1
plan: 08
type: execute
wave: 3
depends_on:
  - 164.5.1-01
  - 164.5.1-04
  - 164.5.1-06
files_modified:
  - docs/runbooks/ledger-refresh-go-live.md
  - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-PREFLIGHT.md
  - docs/runbooks/match-engine.md
  - TODOS.md
autonomous: true
requirements:
  - 164.7-ACTIVATION-DEFERRED
  - CRON-DRIFT-01
  - VAULTTICK-EMPTYKEY-01

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "P3-C can EXECUTE: every `compute_jobs` column reference in both committed copies of the gate resolves against the real table, so the query returns a measurement instead of aborting 42703"
    - "`docs/runbooks/match-engine.md` carries a go-live section that an operator can follow end to end in ONE session, in the order CONTEXT.md fixes: deploy, repoint, register the fan-out and flip the flag, then ONE manifest re-capture at the end"
    - "The P0 database-marker query is the FIRST statement of that section and the runbook demands its OUTPUT be recorded — pasting the query text is explicitly named as not evidence"
    - "The pre-flight verb runs BEFORE the live repoint and a non-zero exit ABORTS the session — the runbook says so in the imperative, not as advice"
    - "D1 (D-01), D2 (D-02) and D3 (D-03) are TRANSCRIBED into the runbook with the measurement or precedent each rests on, and D2's window is named in the exact words CONTEXT.md gives: from a rebuild until the registration step is run, the hourly recompute does not fire and `cron_runs` says why"
    - "The runbook states plainly what a rebuild-from-migrations produces — NO `match_engine_cron` job at all, plus one `cron_runs` row reading `error = 'GUC unset at migration 015'` — which is criterion 4"
    - "The losing convention (Phase 164.5's old criterion 7, 'write ONE forward migration re-scheduling `match_engine_cron`') is FLAGGED for cleanup in the tree, not left to disagree silently"
    - "The section carries a DEFER branch: P3-C failing is a legitimate outcome that stops the activation half while leaving the repoint half complete"
    - statement: "Re-run at D4 time, P3-C returns both mt5-keyed job kinds with a `newest` inside the one-day bar — its last real reading (2026-09-12) FAILED that bar, so a DEFER is the expected-value outcome and not a surprise"
      verification: backstop
  artifacts:
    - path: "docs/runbooks/match-engine.md"
      provides: "The go-live section for the jobid-1 repoint + the ledger-refresh activation, in one ordered session with the D1/D2/D3 transcription and the DEFER branch"
    - path: "docs/runbooks/ledger-refresh-go-live.md"
      provides: "P3-C and the other compute_jobs diagnostics repointed at a column that exists"
    - path: ".planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-PREFLIGHT.md"
      provides: "The second copy of P3-C, repointed identically"
    - path: "TODOS.md"
      provides: "`[PREFLIGHT-P3C-UNRUNNABLE]` marked closed with the dated repair, and `[164.7-REPAIR-REVERTED]`'s jobid claim left as-is"
  key_links:
    - "the runbook's step order -> plan 09's checkpoints, which execute it — one runbook session, one re-capture"
    - "`preflightCronRepoint` (plan 04) -> the runbook's abort gate before the live `cron.schedule`"
    - "P3-C's repaired query -> the DEFER branch -> `[164.7-ACTIVATION-DEFERRED]`'s re-entry gate"
  prohibitions:
    - "No PROD or TEST write may be performed without the database-marker query's recorded OUTPUT. Pasting the query text satisfied a verify leg once — booked as `[164.7-P3C-PRESENCE-ORACLE]` — and that vacuity is exactly what this runbook section exists against."
    - "Never write the `cron.schedule` registration into a migration. It lives in the runbook and nowhere else (D1), and the two in-tree precedents say so in a boxed rule of their own."
    - "Never treat the 2026-09-10 / 2026-09-12 P3-C readings as current. They are dated prose, they were derived by arithmetic rather than by running the query, and MT5 remediation has landed since. Re-measure fresh or DEFER."
    - "Never write a runbook step whose success criterion is that a statement was RUN. Every live step in this section pairs its statement with a verify-back SELECT and an explicit `Expected:` line naming the row shape."
    - "Never claim `[VAULTTICK-EMPTYKEY-01]` is closed before the repoint. The `btrim(v_key) = ''` guard is live in the CALLABLE and the callable is UNREACHED; only the repoint closes it, and only a post-repoint reading of the live command proves it."
---

<objective>
Wave B's paperwork, written before Wave B runs. Two things stand between this phase and a live
session: the activation's own re-entry gate cannot execute, and the procedure does not exist.

**P3-C is UNRUNNABLE, not stale.** Measured 2026-09-16: `164.7-ACTIVATION-PREFLIGHT.md` and
`docs/runbooks/ledger-refresh-go-live.md` both query `max(cj.completed_at)` over `compute_jobs`,
and that table carries `claimed_at`, `created_at`, `updated_at` and `next_attempt_at` — there is NO
`completed_at` column. The query aborts with 42703; it does not return a stale answer. So the ONE
gate standing between a deferred activation and a live production `cron.schedule` errors instead of
measuring, and by its own TODOS entry's account it has never executed successfully.

**The registration statement does not exist yet.** `grep -n "cron.schedule" docs/runbooks/*.md`
finds `ledger-refresh-go-live.md:412` and `flipretry-derived-equity-go-live.md:162` — two in-tree
precedents for the runbook convention — and nothing for `match_engine_cron`. Writing that section
is this phase's work.

Locked decisions recorded and implemented here:
- **D1 (D-01)** — registration lives in the RUNBOOK, never in a migration. Basis: the 164.7 rule
  shipped in `20260907120000` (*"A migration that schedules is a scope violation"*) plus the two
  in-tree precedents.
- **D2 (D-02)** — a rebuild-from-migrations produces NO `match_engine_cron` job at all, plus one
  loud `cron_runs` row. `20260408215026` skips scheduling whenever the two `app.*` GUCs are empty,
  and they are ALWAYS empty here because setting them returns 42501 on this platform. The migration
  fails CLOSED and leaves a trace; that is the convention working, not a gap to patch.
- **D3 (D-03)** — the re-capture is a SCRIPT and it already exists (`captureManifest`). The runbook
  invokes it; it does not describe hand work.
- **D4 (D-04)** — the apply stays a human gate. This plan writes the procedure; plan 09 executes it
  behind the three reviewers.

Criterion 1 is a RECORDING step: transcribe D1/D2/D3 with their measurements, name D2's window, and
flag the losing convention for cleanup rather than leaving it to disagree silently.

Purpose: a live production session on a credential-bearing cron row should be a procedure someone
follows, not a sequence someone remembers.
Output: an executable gate, an ordered runbook section, and a stated rebuild outcome.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-CONTEXT.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-RESEARCH.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-PATTERNS.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-04-SUMMARY.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make P3-C executable — repoint every dead `compute_jobs` column reference</name>
  <precondition>`compute_jobs` on PROD has no `completed_at` column (measured 2026-09-12 and
  re-read from `supabase/schema/baseline.sql` on 2026-09-16: the table carries `claimed_at`,
  `created_at`, `updated_at`, `next_attempt_at`). Re-read the `CREATE TABLE` for `compute_jobs` in
  `supabase/schema/baseline.sql` before editing and halt if a `completed_at` column is in fact
  present — the whole repair would then be wrong.</precondition>
  <reversibility rating="reversible">Prose and SQL inside two documents; the change is a column
  name and its dated rationale.</reversibility>
  <files>docs/runbooks/ledger-refresh-go-live.md, .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-PREFLIGHT.md, TODOS.md</files>
  <read_first>
    - `supabase/schema/baseline.sql`, the `CREATE TABLE ... compute_jobs` block — the real column
      list. This is the source of truth for the repair, not any prose.
    - `docs/runbooks/ledger-refresh-go-live.md` § "P3 — ⛔ venue enable flags — THE HARD GATE",
      specifically Part C (the behavioural probe) and its `Expected:` / `Abort if:` lines, plus the
      dated 2026-09-07 note that follows.
    - `docs/runbooks/ledger-refresh-go-live.md` § "Watching it" and § "Rollback, part 2" — the two
      later diagnostic queries that ALSO reference `cj.completed_at` / `tail.completed_at`
      (including a `cj.completed_at - cj.claimed_at AS duration` expression and an
      `ORDER BY tail.completed_at DESC NULLS LAST`). These abort 42703 for the same reason and are
      part of the same defect class.
    - `.planning/phases/164.7-.../164.7-ACTIVATION-PREFLIGHT.md` § "P3-C — the hard gate, and the
      one expected to fail" — the second committed copy, and the `ABORT if` bar ("stale by more
      than a day") the repaired query is measured against.
    - `TODOS.md` entry `[PREFLIGHT-P3C-UNRUNNABLE]` — read it in full; it names this repair, names
      this phase as owner, and records that the 2026-09-10 DEFER value was derived from the terminal
      Journal and arithmetic rather than from running the query.
  </read_first>
  <action>
In BOTH copies of P3-C — `docs/runbooks/ledger-refresh-go-live.md` Part C and
`.planning/phases/164.7-.../164.7-ACTIVATION-PREFLIGHT.md` — replace `max(cj.completed_at) AS newest`
with `max(cj.updated_at) AS newest` and `cj.completed_at > now() - INTERVAL '3 days'` with
`cj.updated_at > now() - INTERVAL '3 days'`. Keep everything else in the query byte-identical: the
join, the `ak.exchange = 'mt5'` filter, the two `cj.kind` values, `cj.status = 'done'` and the
`GROUP BY`.

Do the same for the other dead references in `docs/runbooks/ledger-refresh-go-live.md`: the
`cj.completed_at` selections and the `cj.completed_at - cj.claimed_at AS duration` expression in
the "Watching it" section, and the `tail.completed_at` selection and `ORDER BY` in the
"Rollback, part 2 — Detect" section. Fixing only the two P3-C copies would leave the same 42703
waiting in the two places an operator reaches for when something has gone wrong — close the whole
class, not the instance. ⛔ For the `duration` expression, note in one clause that
`updated_at - claimed_at` is a proxy for completion latency and is only meaningful for rows whose
terminal status is `done`; do not present it as an exact completion duration.

Beside each repaired query add ONE dated sentence: repointed 2026-09-16 from `completed_at`, which
does not exist on `compute_jobs` (measured on PROD 2026-09-12, `ERROR: 42703`, and re-read from
`supabase/schema/baseline.sql`); `updated_at` is the terminal-status timestamp for a `done` row.

In `docs/runbooks/ledger-refresh-go-live.md`'s P3-C block, add an explicit warning that the
readings recorded in prose (2026-09-10 and 2026-09-12) are DATED and were derived by arithmetic
rather than by running this query, so the gate must be RE-RUN fresh at D4 time and its OUTPUT
recorded. Name the 34.7-hour `poll_allocator_positions` reading as the one that FAILED the one-day
bar, so a reader knows a DEFER is the expected-value outcome.

In `TODOS.md`, mark `[PREFLIGHT-P3C-UNRUNNABLE]` closed with the date and the list of repaired
sites, keeping the entry's history intact. ⛔ Do NOT touch `[164.7-REPAIR-REVERTED]`'s jobid claim
while you are in the file — `cron.schedule` UPSERTs on `(jobname, username)` and PRESERVES the
jobid, that correction is already recorded in the ROADMAP, and re-litigating it here is out of
scope.
  </action>
  <acceptance_criteria>
    - `grep -c 'completed_at' docs/runbooks/ledger-refresh-go-live.md` returns 0 for SQL
      occurrences; any surviving occurrence is inside the dated explanatory sentence naming the
      42703, and is identifiable as prose.
    - `grep -c 'cj.updated_at' docs/runbooks/ledger-refresh-go-live.md` returns at least 4.
    - `grep -c 'completed_at' .planning/phases/164.7-*/164.7-ACTIVATION-PREFLIGHT.md` returns 0 for
      SQL occurrences.
    - Both repaired P3-C copies are otherwise textually identical to each other in the SQL block —
      diffing the two extracted queries yields no difference.
    - `TODOS.md`'s `[PREFLIGHT-P3C-UNRUNNABLE]` entry carries a closure date and names all repaired
      sites.
    - `[164.7-REPAIR-REVERTED]` is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; a=$(grep -c "cj.updated_at" docs/runbooks/ledger-refresh-go-live.md); b=$(grep -c "cj.updated_at" .planning/phases/164.7-*/164.7-ACTIVATION-PREFLIGHT.md); echo "runbook=$a preflight=$b"; [ "$a" -ge 4 ]; [ "$b" -ge 2 ]'</automated>
    <fails_when>non-zero exit, or the printed line shows `runbook=` below 4 or `preflight=` below 2</fails_when>
    <automated>npx vitest run src/__tests__/check-planning-hygiene.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports `0 passed` / `No test files found`</fails_when>
  </verify>
  <done>Both copies of P3-C, and the two later diagnostics in the same runbook, reference columns
  that exist; the gate returns a measurement instead of aborting.</done>
</task>

<task type="auto">
  <name>Task 2: Write the go-live section — ONE session, ONE re-capture, in the fixed order</name>
  <files>docs/runbooks/match-engine.md</files>
  <read_first>
    - `docs/runbooks/ledger-refresh-go-live.md` § "P0 — which database is this? (BLOCKING, and
      FIRST in every session)", § "Step 1 — the activation flag (LIVE op 1 of 2)" with its 1a/1b/1d
      sub-steps, § "Step 2 — the schedule (LIVE op 2 of 2)" with the boxed
      "⛔ **This statement lives HERE and NEVER in a migration.**" rule and its rationale, and
      § "2e — re-capture the cron manifest, IMMEDIATELY after registering". This is the exact
      structure to copy: statement, then an inline verify-back SELECT, then an explicit `Expected:`
      line, then an `Abort if:` line.
    - `docs/runbooks/flipretry-derived-equity-go-live.md` around its `cron.schedule` registration
      and the cross-referenced "never in a migration" rule — the second precedent, confirming the
      shape is a house convention and not a one-off.
    - `docs/runbooks/match-engine.md` in full (it is short) — its existing headings, the `1a-bis`
      section plan 01 repointed, and the Metrics/Common-issues sections plan 06 extended. The new
      section slots after "Manual operations" and before "Metrics to watch".
    - `supabase/schema/functions/match_engine_cron_tick.sql` — the callable the row is repointed at.
      Read its `btrim(v_key) = ''` guard and its own comment admitting the narrower residual
      (`btrim` trims spaces only, not tabs or newlines), so the runbook's post-repoint check names
      the right thing and does not overclaim.
    - `supabase/migrations/20260408215026_schedule_match_cron_hourly.sql` — the skip branch
      (`RAISE NOTICE`, one `cron_runs` row `status='error', error='GUC unset at migration 015'`,
      `RETURN`) that D2's rebuild statement describes. Quote its error string exactly.
    - `.planning/phases/164.5.1-.../164.5.1-04-SUMMARY.md` — the pre-flight verb's final CLI form
      and exit-code meanings, so the runbook invokes it correctly.
  </read_first>
  <action>
Add a section to `docs/runbooks/match-engine.md` titled for what it does — the go-live repoint of
`match_engine_cron` — carrying the whole Wave B session in ONE numbered sequence. Copy
`ledger-refresh-go-live.md`'s discipline exactly: every live step is a statement, an inline
verify-back SELECT, an `Expected:` line naming the row shape, and an `Abort if:` line.

The sequence, in this order and no other (CONTEXT.md Area 3 fixes it; two re-captures means two red
prober runs):

**P0 — which database is this.** The `shobj_description(oid, 'pg_database')` marker query, FIRST in
the session, with the instruction to RECORD ITS OUTPUT into the execution record. State plainly
that `current_database()` is `postgres` on BOTH projects and proves nothing, that the CLI and the
browser SQL editor have no automated production guard, and that pasting the QUERY is not evidence —
only its output is. Cite `[164.7-P3C-PRESENCE-ORACLE]` as the booked vacuity this rule exists
against.

**Step 0 — deploy Areas 1 and 2 first.** The kill-switch fail-closed change, the
`statement_timeout` migration and the batching must be live before the repoint, because the
repointed job calls into the service that those changes harden. State the dependency; do not
restate what they do.

**Step 1 — the pre-flight, and it can abort the session.** Invoke the plan-04 verb against the
committed manifest. State the exit-code meanings (0 = compared and clean, 1 = compared and found
something, 3 = nothing was measured) and that ANY non-zero stops the session. Include the line about
recording the marker the verb prints.

**Step 2 — the repoint (LIVE op 1).** The `cron.schedule` UPSERT naming `match_engine_cron`, the
EXISTING hourly cadence `0 * * * *` — only the command changes — and the body
`SELECT public.match_engine_cron_tick();`. Immediately below it the boxed rule, in this runbook's
own words: this statement lives HERE and NEVER in a migration (D1), with the rationale (migrations
auto-apply to PROD on merge and a silently-skipped worker deploy starts a schedule against a worker
that cannot serve it) and a cross-reference to both existing precedents. Verify-back:
`SELECT jobid, jobname, schedule, command, active FROM cron.job WHERE jobname = 'match_engine_cron';`
with an `Expected:` line stating the jobid is UNCHANGED — `cron.schedule` UPSERTs on
`(jobname, username)` and PRESERVES the jobid — the schedule is unchanged, `active` is true, and
the command is the one-line call.

**Step 2b — the `[VAULTTICK-EMPTYKEY-01]` side-effect check.** A post-repoint assertion that the
live `cron.job.command` now reaches the hardened callable, recorded as a reading and not an
argument. State the narrower residual honestly, in the callable's own words: `btrim()` trims spaces
only, not tabs or newlines, so a tab-only secret is NOT closed by this and is explicitly out of
scope. ⛔ Do NOT claim the item was already closed by the repo — the guard is live in the callable
and the callable was UNREACHED; only this step closes it.

**Step 3 — the activation (LIVE op 2).** Re-run the repaired P3-C from
`docs/runbooks/ledger-refresh-go-live.md` (reference it; do not create a third copy that can rot)
and record its OUTPUT. Then the flag flip on `system_flags.ledger_refresh_enabled` and the
`ledger_refresh_fanout` registration at `25 * * * *`, each with its verify-back and `Expected:`
line, cross-referencing the sibling runbook's Step 1 and Step 2 rather than duplicating their
prose. Add the deliberate exercise the ROADMAP demands: the all-candidates-failed branch of the
ledger fan-out is UNREACHABLE while dormant and becomes reachable AT ACTIVATION — it is a branch
that has never run, so exercise it deliberately rather than assuming it works, and say how.

**Step 3-DEFER — the branch that is expected.** If P3-C fails its bar, STOP after Step 2b: the
repoint half is complete and the activation half defers. Say what that leaves (jobid 1 repointed,
no `ledger_refresh_fanout` row, the flag still false) and that the manifest re-capture in Step 4
still happens for the repoint alone. A DEFER is a legitimate outcome, not a failed session.

**Step 4 — ONE re-capture, at the end.** The `captureManifest` invocation, then read the `command`
text of every job in the resulting diff before committing — the manifest is a committed,
world-readable file in a PUBLIC repository and each command string was individually approved for
publication when first captured. `Expected:` the diff changes jobid 1's command and sha, and adds
exactly one job if Step 3 ran. `Abort if:` any other job moved.

Add the standing warning CONTEXT.md requires: clearing `manifest-invalid` re-animates
`compareManifest`'s manifest-side hygiene loop, which has been dead on every production run for
weeks. Expect manifest-side `cron-secret-in-command` findings nobody has seen before — measured, a
valid manifest yields 1 — and do NOT read them as new leaks.

⛔ Also note that this repository is PUBLIC: never paste a secret, an MT5 account number or a broker
server name into the execution record.
  </action>
  <acceptance_criteria>
    - `docs/runbooks/match-engine.md` contains a section whose first live instruction is the
      `shobj_description` marker query, and the section text contains the phrase requiring its
      OUTPUT be recorded.
    - The section contains the literals `match_engine_cron`, `match_engine_cron_tick`,
      `ledger_refresh_fanout`, `system_flags`, `0 * * * *` and `25 * * * *`.
    - The section contains a boxed "never in a migration" rule and cross-references both
      `ledger-refresh-go-live.md` and `flipretry-derived-equity-go-live.md`.
    - Every live SQL statement in the section is followed by a verify-back SELECT and an
      `Expected:` line — count the statements and the `Expected:` lines and assert they match.
    - The section contains exactly ONE `captureManifest` invocation, and it is the last step.
    - A DEFER branch exists and names what a deferred session leaves behind.
    - The section contains no secret, no account number and no absolute home path.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=docs/runbooks/match-engine.md; for t in shobj_description match_engine_cron_tick ledger_refresh_fanout system_flags; do n=$(grep -c -- "$t" "$f"); echo "$t -> $n"; [ "$n" -ge 1 ]; done; c=$(grep -c -- "--capture-manifest" "$f"); echo "capture-invocations -> $c"; [ "$c" -eq 1 ]'</automated>
    <fails_when>non-zero exit, or any `-&gt; 0` line, or `capture-invocations -&gt;` printing anything other than `1` (two re-captures means two red prober runs)</fails_when>
    <automated>bash -c 'set -e; f=docs/runbooks/match-engine.md; e=$(grep -c "^- \*\*Expected:\*\*\|Expected:" "$f"); a=$(grep -c "Abort if" "$f"); echo "expected-lines=$e abort-lines=$a"; [ "$e" -ge 5 ]; [ "$a" -ge 3 ]'</automated>
    <fails_when>non-zero exit, or `expected-lines=` below 5, or `abort-lines=` below 3 — a live step without an Expected/Abort pair is a step whose success criterion is that a statement was run</fails_when>
  </verify>
  <done>An operator can execute the whole Wave B session from one document, in the fixed order,
  with every live statement paired to a verify-back and an expected row shape, and with a DEFER
  branch that is a legitimate ending rather than a failure.</done>
</task>

<task type="auto">
  <name>Task 3: Transcribe D1/D2/D3 with their measurements, state the rebuild outcome, flag the losing convention</name>
  <files>docs/runbooks/match-engine.md, TODOS.md</files>
  <read_first>
    - `.planning/phases/164.5.1-.../164.5.1-CONTEXT.md` § "DECIDED 2026-09-09 — transcribed VERBATIM
      per success criterion 1" — D1, D2, D3, D4 in full, with the basis each rests on. Criterion 1
      is a RECORDING step and these are the words to record.
    - `.planning/ROADMAP.md` Phase 164.5.1, success criteria 1 and 4, and the struck-through
      lineage paragraphs above the DECIDED block — the losing convention (Phase 164.5's old
      criterion 7) is quoted there and is what gets flagged.
    - `supabase/migrations/20260408215026_schedule_match_cron_hourly.sql` — the skip branch, so
      D2's rebuild statement quotes the real `RAISE NOTICE` and the exact `cron_runs` error string.
    - `scripts/lint-app-guc.mjs` line ~429 — the measured conclusion that an `app.*` read "resolves
      to NULL wherever it runs", which is why the skip branch is the ONLY branch.
    - `TODOS.md` entries `[CRON-DRIFT-01]` and `[164.7-VAULT-ABSENT-RULE]` as plan 01 left them.
  </read_first>
  <action>
Add a short decisions block at the head of the go-live section transcribing D1, D2 and D3 with the
measurement or precedent each rests on. These are ANSWERS, not open questions: write them as
settled, and say in one sentence that they were settled on 2026-09-09 and are not to be re-opened.

D2's block must state, plainly, what a rebuild-from-migrations produces — that is criterion 4:
`20260408215026` skips scheduling whenever the two `app.*` GUCs are empty; they are ALWAYS empty
here because setting them returns 42501 on this platform (measured on PROD 2026-09-05); so a
rebuilt database has NO hourly `match_engine_cron` job at all and ONE `cron_runs` row reading
`error = 'GUC unset at migration 015'`, and the runbook registration is what creates the job. Frame
it as the convention working as designed — the migration fails CLOSED and leaves a trace — not as a
gap to be patched.

Then name D2's WINDOW in the exact words CONTEXT.md gives: *from a rebuild until the registration
step is run, the hourly recompute does not fire, and `cron_runs` says why.* That sentence is
criterion 1's own requirement and must appear verbatim.

Flag the losing convention for cleanup rather than leaving it to disagree silently: Phase 164.5's
old criterion 7 said "Write ONE forward migration re-scheduling `match_engine_cron` to the
achievable Vault-backed command", and D1 supersedes it. Record that in the runbook in one sentence
and add a matching line to the `[CRON-DRIFT-01]` entry in `TODOS.md` so both ledgers agree. ⛔ Do
not delete the old wording anywhere it survives as lineage; flagging is additive.

Finally, add a one-line cross-reference from the runbook's `1a-bis` bullet (which plan 01
repointed) down to the new go-live section, so a reader who lands on the "the LIVE `cron.job` row
is not yet pointed at that function" sentence finds the procedure that changes it.
  </action>
  <acceptance_criteria>
    - `docs/runbooks/match-engine.md` contains the literal sentence fragment
      `the hourly recompute does not fire` together with `cron_runs says why` in the same paragraph.
    - It contains the literal `GUC unset at migration 015`.
    - It contains the literal `42501` with its 2026-09-05 measurement date.
    - It names D1, D2 and D3 explicitly and states they were settled 2026-09-09.
    - It contains a sentence flagging Phase 164.5's old criterion 7 as superseded, and `TODOS.md`'s
      `[CRON-DRIFT-01]` entry carries a matching line.
    - The `1a-bis` bullet cross-references the new section.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -e; f=docs/runbooks/match-engine.md; for t in "GUC unset at migration 015" "42501" "2026-09-09" "cron_runs"; do n=$(grep -c -- "$t" "$f"); echo "$t -> $n"; [ "$n" -ge 1 ]; done'</automated>
    <fails_when>non-zero exit, or any `-&gt; 0` line in the output</fails_when>
    <automated>bash -c 'set -e; n=$(grep -c "the hourly recompute does not fire" docs/runbooks/match-engine.md); echo "window-sentence -> $n"; [ "$n" -ge 1 ]'</automated>
    <fails_when>non-zero exit, or `window-sentence -&gt; 0` (criterion 1 requires the window named in those words)</fails_when>
  </verify>
  <done>Criterion 1 is discharged as a recording step and criterion 4 is stated plainly in the
  runbook; the losing convention is flagged in both ledgers rather than left to disagree.</done>
</task>

</tasks>

<edge_assumptions>
## Edge-coverage assumptions owned by this plan

The consolidated 21-item table lives in `164.5.1-01-PLAN.md` § Edge-coverage assumptions. All 21
rows are `unresolved`; none is auto-resolved and none is dropped. The five rows this plan owns, all
against `164.7-ACTIVATION-DEFERRED`:

| # | Category | Probe | Explicit assumption this plan makes |
|---|---|---|---|
| 7 | adjacency | "When two things are exactly equal or just touch, do they merge, collide, or separate?" | **Assumed and written into the runbook:** `cron.schedule` UPSERTs on `(jobname, username)`, so re-registering an EXISTING jobname MERGES (same jobid, new command) rather than creating a second row. `[164.7-REPAIR-REVERTED]`'s overstated claim that jobid changes on reschedule is already corrected in the ROADMAP; the verify-back's `Expected:` line asserts the jobid is unchanged, which is how the assumption gets measured rather than trusted. |
| 8 | empty | "What is the result for empty, single-element, or null input?" | **Assumed:** P3-C returning ZERO rows for one or both `cj.kind` values is an ABORT, not a pass — the existing `Abort if: either kind is absent` line already says so and the repair does not weaken it. An empty result is a failed gate, never an unmeasured one. |
| 9 | ordering | "When elements compare equal, is output order specified and stable?" | **Assumed:** not applicable — P3-C is a `GROUP BY cj.kind` aggregate over two named kinds, and the runbook reads it as two labelled values rather than as an ordered list. Recorded rather than dropped. |
| 10 | idempotency | "What happens if this runs twice on the same input?" | **Assumed and written into the runbook:** the repoint is idempotent (a second `cron.schedule` with identical arguments leaves the same row) and the flag flip is idempotent (`UPDATE … SET enabled = true` on an already-true row). The RE-CAPTURE is NOT idempotent in effect: running it twice in one session produces two committed diffs and two red prober windows, which is why the runbook fixes it as ONE step at the end. |
| 11 | concurrency | "If interrupted or run in parallel, what is guaranteed?" | **Assumed and stated as a limit:** nothing in this procedure holds a lock. If the session is interrupted between Step 2 and Step 4, PROD carries a repointed row the committed manifest does not describe and the hourly prober reports drift — CORRECTLY. The runbook's DEFER branch tells the operator to still run Step 4 for whatever completed, which is the mitigation. A genuinely concurrent second operator is out of scope and unguarded; recorded here rather than left implicit. |
</edge_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> PROD via the browser SQL editor or the CLI | UNGUARDED by automation; the `COMMENT ON DATABASE` marker is the only control, and this section's P0 step is where it is exercised |
| the runbook -> the operator's actions | A step whose success criterion is "the statement was run" produces no evidence; every live step must pair with a verify-back |
| `.planning/` and `docs/` -> the public internet | The repo is PUBLIC and both trees are tracked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5.1-08-01 | Spoofing | which database the session is connected to | critical | mitigate | P0 marker query FIRST, its OUTPUT recorded, with the explicit statement that pasting the query is not evidence and that `current_database()` proves nothing |
| T-164.5.1-08-02 | Repudiation | a gate that errors instead of measuring | high | mitigate | P3-C is repaired to reference real columns, in both copies and in the two later diagnostics, so the gate returns a measurement; the dated prose readings are labelled as not-current |
| T-164.5.1-08-03 | Tampering | the committed manifest | high | mitigate | ONE re-capture, at the end, with an instruction to read every command in the diff before committing and to abort if any unrelated job moved |
| T-164.5.1-08-04 | Elevation of Privilege | a schedule reaching PROD via a migration | high | mitigate | D1 transcribed with its basis and the boxed never-in-a-migration rule copied from both in-tree precedents |
| T-164.5.1-08-05 | Information Disclosure | the execution record and the committed manifest in a PUBLIC repo | high | mitigate | Explicit instruction never to paste a secret, an MT5 account number or a broker server name, and to read every command string in the manifest diff before committing |
| T-164.5.1-08-06 | Denial of Service | an interrupted session leaving PROD ahead of the oracle | medium | accept | Nothing holds a lock across the session. Accepted, with the DEFER branch instructing the operator to still re-capture for whatever completed — the drift the prober would report is CORRECT, and leaving it red is what teaches people to ignore the arm |
| T-164.5.1-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install task exists; RESEARCH.md § Package Legitimacy Audit records `N/A` for the phase |
</threat_model>

## Artifacts this phase produces

Produced by THIS plan:
- `docs/runbooks/match-engine.md` § the `match_engine_cron` go-live section — P0, Step 0, Step 1
  (pre-flight), Step 2 (repoint), Step 2b (`[VAULTTICK-EMPTYKEY-01]` side-effect check), Step 3
  (activation), Step 3-DEFER, Step 4 (ONE re-capture), plus the D1/D2/D3 transcription and the
  rebuild statement
- Repaired P3-C in `docs/runbooks/ledger-refresh-go-live.md` and
  `.planning/phases/164.7-.../164.7-ACTIVATION-PREFLIGHT.md`
- `TODOS.md`: `[PREFLIGHT-P3C-UNRUNNABLE]` closed; `[CRON-DRIFT-01]` carrying the
  superseded-convention flag

The full phase-wide artifact list is in `164.5.1-01-PLAN.md` § Artifacts this phase produces.

<verification>
- All three tasks' `<automated>` commands green.
- `npx vitest run src/__tests__/check-planning-hygiene.test.ts` green (a `.planning/` file of a
  prior phase is edited).
- `npx vitest run` full suite green.
- `git diff --name-only` lists exactly the four files in `files_modified`.
- ⛔ NOT verified here: that the procedure works. That is plan 09, on live PROD, behind D4.
</verification>

<success_criteria>
- Criterion 1 is discharged as a recording step, with D2's window named in its own words.
- Criterion 4 is stated plainly: what a rebuild-from-migrations produces.
- `[PREFLIGHT-P3C-UNRUNNABLE]` is closed and the activation's re-entry gate can execute.
- The Wave B session exists as a procedure with an expected DEFER branch.
</success_criteria>

<output>
Create `.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-08-SUMMARY.md` when done
</output>