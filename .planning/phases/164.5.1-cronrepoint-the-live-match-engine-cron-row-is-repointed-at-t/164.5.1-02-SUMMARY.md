---
phase: 164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t
plan: 02
subsystem: api
tags: [fastapi, postgrest, supabase, retry, kill-switch, anti-vacuity, pytest]

# Dependency graph
requires:
  - phase: 164.5.1-01
    provides: the OWED TODOS.md entry for [164.5.1-GATEWAY-CEILING-INVERSION] and the criterion-6 redirects this plan's requirement ID depends on being tracked
provides:
  - "async tri-state kill switch (_engine_is_enabled / _engine_is_enabled_cached returning KILL_SWITCH_ENABLED/DISABLED/UNAVAILABLE) replacing the fail-open bool contract"
  - "db_read_with_retry + _is_gateway_timeout — the named retry seam beside db_execute (services/db.py), consumable by plan 05's _claim_priority/_reset"
  - "a TTL cache (_kill_switch_cache) that never serves a value produced by a failed poll"
  - "a proven-by-injected-failure anti-vacuity record for criterion 8 (neuter -> observe RED -> restore, both levers)"
affects: [164.5.1-03, 164.5.1-05, 164.5.1-06, 164.5.1-09]

# Actuals (#2632) — pairs with the plan's estimate to calibrate future estimates.
actuals:
  tokens: 13068
  tasks: 3
  commits: 3
  plan_head_before: 395385d8c4809bda8c3a0d7fcccec5810d69b108

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Retry-with-backoff seam lives BESIDE the seam it retries (db_read_with_retry beside db_execute), never inside it — preserves fail-fast timing for every other caller"
    - "Tri-state status vocabulary (ENABLED/DISABLED/UNAVAILABLE) for a safety control, so the failure cause is never collapsed onto the operator-controlled cause"
    - "A TTL cache's failure contract is INVALIDATE-on-failure, not populate-on-failure — an exhausted poll clears the cache rather than writing a value to it"
    - "Anti-vacuity procedure: cp byte backup -> neuter one lever -> diff proves it applied -> run named test selection, observe RED -> cp restore -> cmp proves byte-identical -> re-run GREEN. Never git checkout -- to restore."

key-files:
  created: []
  modified:
    - analytics-service/services/db.py
    - analytics-service/routers/match.py
    - analytics-service/tests/test_match_router.py
    - analytics-service/tests/test_maybe_single_none_guards.py
    - analytics-service/tests/test_status_contract_match_sim_portfolio.py

key-decisions:
  - "The structural fork CONTEXT.md left open (async vs sync retry helper) is answered as the plan's own objective directs: _engine_is_enabled and _engine_is_enabled_cached become async def; db_read_with_retry is async and wraps db_execute. Not re-derived — the plan's own reasoning (one seam not two, house idiom is async, the read joins the bounded executor) was transcribed, not re-litigated."
  - "_is_gateway_timeout checks BOTH the string \"504\" and the int 504 on .code (RESEARCH assumption A2) — a single-type comparison would silently never match on the ambiguous wire type the ROADMAP's cited CI sighting shows."
  - "The kill switch's except block has NO surviving fail-open arm: both the gateway-timeout-exhausted branch and any other exception branch return KILL_SWITCH_UNAVAILABLE. The old 'fail-open on any Supabase exception' contract is replaced, not kept beside the new one."
  - "The TTL cache's failure contract (task 2): an exhausted poll calls _kill_switch_cache.clear() and returns KILL_SWITCH_UNAVAILABLE directly, WITHOUT writing at/value. Only ENABLED/DISABLED are ever cached — closes the open half of the safety property the tracer gate flagged (the cache-hit default fallback could otherwise still serve a stale ENABLED value)."
  - "Real call-site count for _engine_is_enabled, measured at HEAD: 33 monkeypatch.setattr sites + 3 direct calls = 36 total in the three test files, not the plan's ~23 estimate. Reported plainly at the tracer checkpoint; all 36 converted, verified via a repo-wide grep returning only the 4 files this plan's frontmatter lists."
  - "pytest-timeout plugin is NOT a dependency of this project (absent from requirements, no CI precedent) — per Rule 3's package-install exclusion, task 3's <verify> command's --timeout=600 flag was NOT satisfied by installing a new package. Substituted an OS-level `timeout` shell wrapper for the same safety property; the full suite completed in 153-227s, well inside the 600s budget, 0 failures."
  - "Tasks 1 and 2 carried tdd=\"true\" but were executed as ONE atomic commit each (test additions + implementation together, verified via the acceptance-criteria HARD GATE loop) rather than separate test()/feat()/refactor() commits. This plan's frontmatter type is `execute` (not `tdd`), and this project's workflow.tdd_mode is `false` (.planning/config.json) — tdd.md's Gate Enforcement Rules are explicitly scoped to `type: tdd` plans with tdd_mode enabled, neither of which applies here, so the formal RED/GREEN/REFACTOR commit-separation machinery was never armed for this plan."

patterns-established:
  - "Pattern 1: a retry seam that decides nothing for its callers — db_read_with_retry re-raises the original exception unchanged on exhaustion; each of the 3+ call sites (kill switch, and later cron_recompute/_claim_priority/_reset) decides its OWN disposition."
  - "Pattern 2: a cache's failure contract is verified with a poll double that WOULD raise/fail if invoked, proving the cache-hit path genuinely short-circuits before the poll runs (test_kill_switch_cache_hit_never_calls_a_failing_poll)."

requirements-completed: []  # 164.5.1-GATEWAY-CEILING-INVERSION is shared across 6 plans in this phase (01,02,03,05,06,09) and stays BLOCKED (shared-ID gate #2388) until every declaring plan finishes. Not marked complete by this plan alone.

coverage:
  - id: D1
    description: "Named retry seam db_read_with_retry (services/db.py), beside db_execute never inside it, retrying only a gateway-timeout-shaped exception up to DB_READ_MAX_RETRIES=3 times with exponential backoff+jitter, bounded well below the 45s statement_timeout plan 03 sets"
    requirement: "164.5.1-GATEWAY-CEILING-INVERSION"
    verification:
      - kind: unit
        ref: "tests/test_match_router.py::TestKillSwitchFailClosed::test_kill_switch_non_gateway_exception_is_not_retried"
        status: pass
      - kind: unit
        ref: "tests/test_match_router.py::TestKillSwitchFailClosed::test_kill_switch_stops_cron_on_exhausted_gateway_timeout"
        status: pass
      - kind: other
        ref: "python -c AST check: db_read_with_retry/_is_gateway_timeout/db_execute all present at module level in services/db.py"
        status: pass
    human_judgment: false
  - id: D2
    description: "_is_gateway_timeout fires on BOTH string \"504\" and int 504 .code, and does not fire on \"42883\" or a missing .code — including against a REAL postgrest.exceptions.APIError, not only a hand-built mock"
    requirement: "164.5.1-GATEWAY-CEILING-INVERSION"
    verification:
      - kind: unit
        ref: "tests/test_match_router.py::TestGatewayTimeoutCalibration::test_gateway_timeout_fires_on_real_postgrest_api_error"
        status: pass
      - kind: unit
        ref: "tests/test_match_router.py::TestGatewayTimeoutCalibration::test_gateway_timeout_fires_on_int_code"
        status: pass
    human_judgment: false
  - id: D3
    description: "The kill switch fails CLOSED end to end: a gateway 504 exhausting retries makes POST /api/match/cron-recompute respond kill_switch_unavailable (all counters 0, numeric duration_s, scoring never called); the founder's disabled flag and the outage status are never equal"
    requirement: "164.5.1-GATEWAY-CEILING-INVERSION"
    verification:
      - kind: unit
        ref: "tests/test_match_router.py::TestKillSwitchFailClosed::test_kill_switch_stops_cron_on_exhausted_gateway_timeout"
        status: pass
      - kind: unit
        ref: "tests/test_match_router.py::TestKillSwitchFailClosed::test_kill_switch_unavailable_is_distinct_from_founder_disabled"
        status: pass
    human_judgment: false
  - id: D4
    description: "The TTL cache never serves a value produced by a failed poll: a fresh cache hit short-circuits before a failing poll is even called; an exhausted poll invalidates (clears) the cache rather than populating it; a stale-but-ENABLED entry is never resurrected by an exhausted re-poll; a successful poll immediately after repopulates normally"
    requirement: "164.5.1-GATEWAY-CEILING-INVERSION"
    verification:
      - kind: unit
        ref: "tests/test_match_router.py::TestKillSwitchCacheFailClosed::test_kill_switch_cache_hit_never_calls_a_failing_poll"
        status: pass
      - kind: unit
        ref: "tests/test_match_router.py::TestKillSwitchCacheFailClosed::test_kill_switch_cache_exhausted_poll_invalidates_empty_cache"
        status: pass
      - kind: unit
        ref: "tests/test_match_router.py::TestKillSwitchCacheFailClosed::test_kill_switch_cache_stale_enabled_not_resurrected_by_exhausted_repoll"
        status: pass
      - kind: unit
        ref: "tests/test_match_router.py::TestKillSwitchCacheFailClosed::test_kill_switch_cache_repopulates_normally_after_invalidation"
        status: pass
    human_judgment: false
  - id: D5
    description: "The mid-run per-allocator kill-switch re-check (F1/MED8) still calls the UNCACHED accessor and still polls fresh every iteration after the async conversion — unchanged safety property"
    requirement: "164.5.1-GATEWAY-CEILING-INVERSION"
    verification:
      - kind: unit
        ref: "tests/test_match_router.py::TestCronPartialFailure::test_mid_run_kill_switch_recheck_is_uncached"
        status: pass
      - kind: unit
        ref: "tests/test_match_router.py::TestCronPartialFailure::test_kill_switch_flip_mid_run_breaks_loop"
        status: pass
    human_judgment: false
  - id: D6
    description: "Criterion 8's fail-closed disposition and the A2 dual-wire-type predicate were each OBSERVED to fail when neutered — not merely argued — with the neuter PROVEN applied (diff against a cp byte backup) before the RED was believed, and the restore PROVEN byte-identical via cmp"
    verification:
      - kind: manual_procedural
        ref: "See ## Anti-Vacuity Procedure (Task 3) below — full transcript of diff/RED/cmp/GREEN for both levers"
        status: pass
    human_judgment: true
    rationale: "The anti-vacuity procedure's evidentiary value depends on a human reading the actual diff lines, RED test names, and cmp exit codes recorded below, not just a boolean pass/fail — this is exactly the kind of proof a test suite alone cannot certify."

# Metrics
duration: 55min
completed: 2026-09-16
status: complete
---

# Phase 164.5.1 Plan 02: Fail-Closed Kill Switch Summary

**A Supabase gateway 504 on the kill-switch read now stops the match engine with a distinct `kill_switch_unavailable` status instead of the documented fail-open `True` — proven end to end by an injected, neuter-and-observe-RED failure, not by argument.**

## Performance

- **Duration:** 55 min
- **Started:** 2026-09-16T~17:35Z (tracer gate approved at ~17:55Z after human verification)
- **Completed:** 2026-09-16T~18:30Z
- **Tasks:** 3 completed (1 tracer + 2 auto)
- **Files modified:** 5

## Accomplishments

- `analytics-service/services/db.py`: new `db_read_with_retry` + `_is_gateway_timeout` retry seam beside `db_execute` — 3 attempts, exponential backoff + jitter, dual string/int `.code` check for a Supabase gateway 504.
- `analytics-service/routers/match.py`: `_engine_is_enabled`/`_engine_is_enabled_cached` converted from sync fail-open bool to async tri-state (`KILL_SWITCH_ENABLED`/`KILL_SWITCH_DISABLED`/`KILL_SWITCH_UNAVAILABLE`); all 3 production call sites updated; the mid-run re-check keeps the UNCACHED accessor and keeps polling fresh (F1/MED8, unchanged safety property).
- The TTL cache (`_kill_switch_cache`) now invalidates rather than populates on an exhausted poll — closes the open half of the safety property the tracer feedback gate flagged before Task 2 began.
- Criterion 8's fail-closed disposition and the A2 dual-wire-type predicate were each proven by an INJECTED failure, observed RED under a neuter that was itself proven applied, then restored and re-verified byte-identical.
- Real test-surface count measured and reported: 36 `_engine_is_enabled` references (33 `monkeypatch.setattr` + 3 direct calls) against the plan's ~23 estimate — all converted, none missed.

## Task Commits

Each task was committed atomically:

1. **Task 1 (tracer): TRACER — a gateway 504 makes the engine STOP, end to end** — `43ce2233` (feat)
2. **Task 2: The TTL cache never serves a value produced by a failed poll** — `a909136c` (feat)
3. **Task 3: Prove the fail-closed disposition by an INJECTED failure — neuter, observe RED, restore** — `1817bfff` (test)

**Plan metadata:** (pending — this commit)

## Files Created/Modified

- `analytics-service/services/db.py` — new retry seam (`db_read_with_retry`, `_is_gateway_timeout`, `DB_READ_MAX_RETRIES`, `DB_READ_BACKOFF_BASE_S`, `DB_READ_JITTER_MAX_S`)
- `analytics-service/routers/match.py` — async tri-state kill switch guard, 3 call sites, fail-closed TTL cache contract
- `analytics-service/tests/test_match_router.py` — 36 `_engine_is_enabled` references converted to async tri-state doubles; 4 new test classes (`TestKillSwitchFailClosed`, `TestKillSwitchCacheFailClosed`, `TestGatewayTimeoutCalibration`, plus the rewritten `TestKillSwitchEnabled`)
- `analytics-service/tests/test_maybe_single_none_guards.py` — converted to async, `KILL_SWITCH_ENABLED` import
- `analytics-service/tests/test_status_contract_match_sim_portfolio.py` — one `_arrange_recompute` helper converted to an async double

## Decisions Made

See `key-decisions` in frontmatter above — summarized: the async structural fork was answered as directed (not re-derived); the dual string/int `.code` check is load-bearing, not decorative; the except block has no surviving fail-open arm; the TTL cache's failure contract closes the open half of the safety property the tracer gate identified; the measured 36-site test surface (vs ~23 estimated) was reported plainly; the `pytest-timeout` plugin was not installed (Rule 3 package-install exclusion) in favor of an equivalent OS-level `timeout` wrapper; tasks 1/2's `tdd="true"` attribute was executed as single atomic commits since this plan is `type: execute` with `workflow.tdd_mode: false` project-wide.

## Anti-Vacuity Procedure (Task 3)

Full transcript, both levers, run by hand against `analytics-service/routers/match.py` and `analytics-service/services/db.py` respectively, using `cp` byte backups (never `git checkout --`).

### Lever 1 — the disposition (`routers/match.py`, symbol: `_engine_is_enabled`'s `except` block final return)

- **Backup taken:** `cp routers/match.py <scratchpad>/match.py.bak.lever1`; SHA-256 of both sides matched before any edit.
- **Neuter applied:** changed the except block's final `return KILL_SWITCH_UNAVAILABLE` to `return KILL_SWITCH_ENABLED  # NEUTER-LEVER-1`.
- **Diff proving the neuter applied, BEFORE running anything:**
  ```
  368c368
  <         return KILL_SWITCH_UNAVAILABLE
  ---
  >         return KILL_SWITCH_ENABLED  # NEUTER-LEVER-1 (164.5.1-02 task 3)
  ```
- **RED observed:** `pytest tests/test_match_router.py -k "kill_switch or gateway_timeout" -q` → **2 failed, 11 passed**.
  - `FAILED tests/test_match_router.py::TestKillSwitchFailClosed::test_kill_switch_stops_cron_on_exhausted_gateway_timeout` — `AssertionError: assert 'no_allocators' == 'kill_switch_unavailable'` (the fail-closed end-to-end assertion).
  - `FAILED tests/test_match_router.py::TestKillSwitchFailClosed::test_kill_switch_unavailable_is_distinct_from_founder_disabled` — same assertion, on the distinct-status pairing.
  - The founder-flip `disabled` control (`TestRecomputeEndpoint::test_kill_switch_off_returns_disabled_status`) stayed GREEN — one RED credited to one control, exactly as the acceptance criterion requires.
- **Restore:** `cp <backup> routers/match.py`.
- **Restore proven:** `cmp <backup> routers/match.py` → **exit 0** (no output); SHA-256 of both sides re-verified identical (`fa77c9ca991f2c86482bfaf226d53870c644a23cdfbc796342bd02cd8587c936`).
- **Re-run GREEN:** `pytest tests/test_match_router.py -k "kill_switch or gateway_timeout" -q` → **13 passed**.

### Lever 2 — the predicate's wire type (`services/db.py`, symbol: `_is_gateway_timeout`)

- **Fresh backup taken:** `cp services/db.py <scratchpad>/db.py.bak.lever2`; SHA-256 matched before any edit.
- **Neuter applied:** narrowed `return code in ("504", 504)` to `return code == "504"  # NEUTER-LEVER-2`.
- **Diff proving the neuter applied, BEFORE running anything:**
  ```
  189c189
  <     return code in ("504", 504)
  ---
  >     return code == "504"  # NEUTER-LEVER-2 (164.5.1-02 task 3)
  ```
- **RED observed:** `pytest tests/test_match_router.py -k "kill_switch or gateway_timeout" -q` → **1 failed, 12 passed**.
  - `FAILED tests/test_match_router.py::TestGatewayTimeoutCalibration::test_gateway_timeout_fires_on_int_code` — `AssertionError: assert False is True` — reddened the A2 dual-wire-type calibration and **nothing else** (the str-`"504"`-carrying real-`APIError` test in the same class stayed GREEN, as expected of a narrowing that keeps the string branch).
- **Restore:** `cp <backup> services/db.py`.
- **Restore proven:** `cmp <backup> services/db.py` → **exit 0**; SHA-256 re-verified identical (`a211be5e35fc4255c4bb9a9232327ed39ee23c4a04473c7d42913d83baba124b`).
- **Re-run GREEN:** `pytest tests/test_match_router.py -k "kill_switch or gateway_timeout" -q` → **13 passed**.

### Net working-tree state after both levers

`git diff --stat` for `routers/match.py` and `services/db.py` against the pre-task commit is **empty** (byte-identical restoration, proven above) — the only net change committed in Task 3 is the new `TestGatewayTimeoutCalibration` class (46 lines added to `tests/test_match_router.py`).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added missing return type annotation on new closure**
- **Found during:** Task 1, mypy verification pass
- **Issue:** the new `_read()` closure inside `_engine_is_enabled` (routing the Supabase call through `db_read_with_retry`) had no return type annotation, tripping `mypy`'s `no-untyped-def`.
- **Fix:** added `-> Any` (already imported in `routers/match.py`).
- **Files modified:** `analytics-service/routers/match.py`
- **Verification:** `mypy services/db.py routers/match.py` clean after the fix; full pytest re-run still 100% green.
- **Committed in:** `43ce2233` (Task 1 commit)

**2. [Rule 3 - Blocking, package-install excluded] Substituted OS-level `timeout` for the missing `pytest-timeout` plugin**
- **Found during:** Task 3, running the plan's own `<verify>` command `pytest tests/ -q -x --timeout=600`
- **Issue:** `pytest --timeout=600` requires the `pytest-timeout` plugin, which is absent from this project's dependencies (no `requirements.txt` entry, `pip show` reports no module, no `.github/workflows/*.yml` precedent for `--timeout=`).
- **Fix:** per Rule 3's explicit package-install exclusion, did **not** install `pytest-timeout`. Wrapped the same command in a shell-level `timeout 590 ...` instead, achieving the identical safety property (kill a hung run) without adding a new dependency.
- **Files modified:** none (verification-only substitution, not a code change)
- **Verification:** full suite completed in 153.78s (first run) / 227.31s (coverage run), both well inside the 600s budget; `5828 passed, 89 skipped, 0 failed` both times.
- **Committed in:** N/A (no code change; recorded here and in `.planning/WINDOWS.md` entry 54 for visibility)

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking/package-install-excluded). **Impact on plan:** the mypy fix is typing-only with no behavior change; the pytest-timeout substitution changes only the verification mechanism, not what was verified — both full-suite runs (task 3 and the plan-level coverage verification) reported identical zero-failure results.

## Issues Encountered

None beyond the two deviations above.

## Plan-Level Verification

- `cd analytics-service && pytest tests/ -q` → **5828 passed, 89 skipped, 0 failed** (153.78s, well within any reasonable timeout budget).
- `cd analytics-service && pytest --cov=services --cov=routers --cov=main_worker --cov-report=term-missing --cov-fail-under=80` → **PASSED — total coverage 91.18%** against the live CI gate value of 80% (read by symbol from `.github/workflows/ci.yml:4246`, not restated from memory).
- Anti-vacuity procedure for both levers: see `## Anti-Vacuity Procedure (Task 3)` above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 03 (statement_timeout migration) can now cite this plan's retry budget (~3s worst case across 3 attempts) as strictly below the 45s `statement_timeout` it sets — the arithmetic this plan's objective promised is delivered, not merely asserted.
- Plan 05 (`_claim_priority`/`_reset` in `main_worker.py`) can consume `db_read_with_retry` directly — the seam is a stable, tested, `async def` function taking `Callable[[], _T]` and re-raising unchanged on exhaustion; no further shape decisions remain for plan 05 to make.
- Plan 06's `TestCronResponseShape` extension for `status="partial"` is unaffected — this plan's `_early_return(state, disabled=True)` change is additive (new `kill_switch_unavailable` status value alongside the existing `disabled`), and `_REQUIRED_KEYS` remains a subset assertion.
- `164.5.1-GATEWAY-CEILING-INVERSION` remains **blocked** in `REQUIREMENTS.md`'s shared-ID sense (plans 01, 02, 03, 05, 06, 09 all declare it) until the last of those plans produces its own SUMMARY — this plan's contribution is complete but the requirement itself is not yet markable.
- No blockers for the next plan in this phase.

---
*Phase: 164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t*
*Plan: 02*
*Completed: 2026-09-16*
