---
phase: 164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t
plan: 08
subsystem: docs
tags: [todos, runbook, cron, prod-prober, ledger-refresh, match-engine, compute-jobs, documentation]

# Dependency graph
requires:
  - phase: 164.5.1-01
    provides: "TODOS.md entries for the three ROADMAP-only requirement IDs; the 1a-bis criterion-6 redirect this plan cross-references from"
  - phase: 164.5.1-04
    provides: "preflightCronRepoint (scripts/prod-prober/run.mjs --preflight-repoint), whose exit-code contract Step 1 of the new go-live section invokes"
  - phase: 164.5.1-06
    provides: "the partial/kill_switch_unavailable status vocabulary block in docs/runbooks/match-engine.md, left byte-identical by this plan"
provides:
  - "P3-C executable on both committed copies plus the two later compute_jobs diagnostics sharing the same defect — the activation re-entry gate returns a measurement instead of aborting 42703"
  - "docs/runbooks/match-engine.md's Wave B go-live section: P0 marker, Step 0 (Wave A deploy check), Step 1 (pre-flight abort gate), Step 2 (the repoint), Step 2b (VAULTTICK-EMPTYKEY-01 post-repoint check), Step 3 (activation by reference + the deliberate all-candidates-failed exercise), Step 3-DEFER, Step 4 (one re-capture)"
  - "D1/D2/D3 transcribed with their measurements at the head of the go-live section; D2's window named verbatim; the old Phase 164.5 criterion 7 convention flagged superseded in both the runbook and TODOS.md"
affects: [164.5.1-09]

# Actuals (#2632)
actuals:
  tokens: 8650
  tasks: 3
  commits: 3
  plan_head_before: 568cdaa9

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A runbook step that names a live-write op always pairs a verify-back SELECT with an explicit Expected:/Abort if: pair, copied from the two existing in-tree precedents rather than inventing a fourth convention"
    - "A dead column reference sharing one defect class (P3-C plus two later diagnostics reading the same nonexistent compute_jobs.completed_at) is repaired as one class, not as isolated instances, because an operator reaching for a diagnostic during an incident hits the same 42703 the primary gate hit"

key-files:
  created: []
  modified:
    - docs/runbooks/ledger-refresh-go-live.md
    - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-PREFLIGHT.md
    - docs/runbooks/match-engine.md
    - TODOS.md

key-decisions:
  - "compute_jobs.completed_at does not exist (compute_jobs has claimed_at/created_at/updated_at/next_attempt_at, re-read from supabase/schema/baseline.sql) — repointed to updated_at in P3-C's two committed copies AND the two later diagnostics that shared the defect (ledger-refresh-go-live.md's 'Watching it' duration expression and its 'Rollback, part 2 — Detect' LATERAL query), not just the primary gate. The duration expression's meaning changed from an exact completion duration to a completion-latency proxy, stated explicitly rather than silently relabeled."
  - "P3-C is referenced from the new go-live section, never pasted a third time — plan 08's own must_haves forbid a third copy that can rot independently of the other two."
  - "The ledger fan-out's all-candidates-failed branch (per-candidate EXCEPTION WHEN OTHERS inside enqueue_ledger_refresh_for_strategies) is unreachable while dormant and becomes reachable only at activation. Deliberately fabricating a PROD failure to exercise it was considered and rejected — it would mean forcing a real enqueue_compute_job call to fail against funded strategies, which is an invented-failure-state this repo's rules forbid. Exercised instead via a mandated manual first invocation (reading both the returned integer and the interactive NOTICE/WARNING panel, which cron.job_run_details.return_message never surfaces) plus an explicit code-read confirmation that the per-candidate handler cannot leak the advisory lock or abort the tick — recorded as a code-read, not a live exercise, if no WARNING fires."
  - "Phase 164.5's old criterion 7 ('write ONE forward migration re-scheduling match_engine_cron...') is flagged superseded by D1 with one sentence in the runbook and a matching TODOS.md [CRON-DRIFT-01] entry, additively — the old wording stays in place as lineage per the plan's own prohibition against deleting it."

patterns-established: []

requirements-completed: []  # 164.7-ACTIVATION-DEFERRED, CRON-DRIFT-01 and VAULTTICK-EMPTYKEY-01 are all shared with plan 09 (shared-ID gate #2388); `requirements.ready-ids` returned 0/3 ready — none is markable until plan 09 (the live PROD session) produces its own SUMMARY. This plan writes the procedure and the executable gate; it performs no live write.

coverage:
  - id: D1
    description: "P3-C executable on both committed copies (docs/runbooks/ledger-refresh-go-live.md, 164.7-ACTIVATION-PREFLIGHT.md) plus the two later compute_jobs diagnostics sharing the defect, repointed completed_at -> updated_at with a dated explanatory sentence beside each; both SQL copies verified byte-identical; TODOS.md [PREFLIGHT-P3C-UNRUNNABLE] closed with the repaired-site list"
    requirement: "CRON-DRIFT-01"
    verification:
      - kind: other
        ref: "bash -c 'a=$(grep -c cj.updated_at docs/runbooks/ledger-refresh-go-live.md); b=$(grep -c cj.updated_at .planning/phases/164.7-*/164.7-ACTIVATION-PREFLIGHT.md); [ \"$a\" -ge 4 ]; [ \"$b\" -ge 2 ]' (Task 1 <verify>, runbook=5 preflight=2)"
        status: pass
      - kind: other
        ref: "npx vitest run src/__tests__/check-planning-hygiene.test.ts (Task 1 <verify>, 42/42 passed)"
        status: pass
      - kind: other
        ref: "diff of the two extracted P3-C SQL bodies (docs/runbooks/ledger-refresh-go-live.md vs 164.7-ACTIVATION-PREFLIGHT.md) — byte-identical"
        status: pass
    human_judgment: false
  - id: D2
    description: "docs/runbooks/match-engine.md carries a go-live section covering the whole Wave B session in the fixed order (P0, Step 0, Step 1 abort gate, Step 2 repoint, Step 2b VAULTTICK-EMPTYKEY-01 check, Step 3 activation by reference + the deliberate branch exercise, Step 3-DEFER, Step 4 one re-capture), every live statement paired with a verify-back + Expected:/Abort if:, exactly one --capture-manifest invocation, no secret/account number/absolute path"
    requirement: "164.7-ACTIVATION-DEFERRED"
    verification:
      - kind: other
        ref: "bash -c per-literal grep for shobj_description/match_engine_cron_tick/ledger_refresh_fanout/system_flags (Task 2 <verify>, all >=1: 1/9/4/5) plus --capture-manifest count == 1"
        status: pass
      - kind: other
        ref: "bash -c Expected:/Abort if: line counts (Task 2 <verify>, expected-lines=8 >=5, abort-lines=9 >=3)"
        status: pass
      - kind: other
        ref: "grep for helios-mammut / /Users/ / account number / broker server name across docs/runbooks/match-engine.md — zero hits outside the section's own no-secrets reminder sentence"
        status: pass
    human_judgment: false
  - id: D3
    description: "D1/D2/D3 transcribed at the head of the go-live section with their measurements; D2's window named verbatim ('from a rebuild until the registration step is run, the hourly recompute does not fire, and cron_runs says why'); D2's rebuild outcome (no job, one cron_runs row reading 'GUC unset at migration 015') stated plainly; the old Phase 164.5 criterion 7 convention flagged superseded in both docs/runbooks/match-engine.md and TODOS.md's [CRON-DRIFT-01]"
    requirement: "CRON-DRIFT-01"
    verification:
      - kind: other
        ref: "bash -c per-literal grep for 'GUC unset at migration 015'/42501/2026-09-09/cron_runs (Task 3 <verify>, all >=1)"
        status: pass
      - kind: other
        ref: "bash -c grep -c 'the hourly recompute does not fire' docs/runbooks/match-engine.md (Task 3 <verify>, ->1)"
        status: pass
      - kind: other
        ref: "grep -n 'Phase 164.5.1 plan 08, D1' TODOS.md — the matching superseded-convention line is present"
        status: pass
    human_judgment: false
  - id: D4
    description: "That the procedure actually works against real PROD — P3-C returning a fresh, non-stale reading; the repoint UPSERT preserving jobid 1; the ledger fan-out's all-candidates-failed branch genuinely firing or not; the manifest re-capture diff matching Expected: exactly"
    verification: []
    human_judgment: true
    rationale: "Explicitly NOT verified by this plan, per its own <verification> section: 'NOT verified here: that the procedure works. That is plan 09, on live PROD, behind D4.' This SUMMARY states it plainly so a future reader does not mistake a green repo-side check for a PROD-side confirmation."

# Metrics
duration: ~35min
completed: 2026-09-16
status: complete
---

# Phase 164.5.1 Plan 08: Wave B Go-Live Runbook + P3-C Repair Summary

**Repointed every dead `compute_jobs.completed_at` reference to `updated_at` across both P3-C copies and two sibling diagnostics that shared the defect, then wrote `docs/runbooks/match-engine.md`'s Wave B go-live section — one ordered session covering the `match_engine_cron` repoint and the ledger-refresh activation, with D1/D2/D3 transcribed and the old Phase 164.5 criterion 7 convention flagged superseded.**

## Performance

- **Duration:** ~35 min (approximate — PLAN_START_EPOCH not captured as a discrete step; estimated from first context read to final commit)
- **Started:** ~2026-09-16T19:45:00Z (approximate)
- **Completed:** 2026-09-16T19:51:21Z
- **Tasks:** 3 completed
- **Files modified:** 4

## Accomplishments

- `compute_jobs.completed_at` — a column that does not exist (measured on PROD 2026-09-12, `ERROR: 42703`; re-read from `supabase/schema/baseline.sql`: the table carries `claimed_at`, `created_at`, `updated_at`, `next_attempt_at`, no `completed_at`) — repointed to `updated_at` in **both** committed copies of P3-C (`docs/runbooks/ledger-refresh-go-live.md`, `164.7-ACTIVATION-PREFLIGHT.md`) **and** the two later diagnostics sharing the defect (the "Watching it" `duration` expression, now labelled a completion-latency proxy rather than an exact duration; the "Rollback, part 2 — Detect" LATERAL query). Both P3-C SQL bodies verified byte-identical. `TODOS.md`'s `[PREFLIGHT-P3C-UNRUNNABLE]` closed with the repaired-site list.
- `docs/runbooks/match-engine.md` gained a "Go-live: repoint `match_engine_cron` + ledger-refresh activation (Phase 164.5.1 Wave B)" section: the P0 database-marker gate (output must be recorded, pasting the query is not evidence — `[164.7-P3C-PRESENCE-ORACLE]`), Step 0 (Wave A must be deployed first), Step 1 (`preflightCronRepoint`, any non-zero exit aborts the whole session), Step 2 (the `cron.schedule` UPSERT repointing jobid 1, with the boxed never-in-a-migration rule cross-referencing both in-tree precedents), Step 2b (the post-repoint `[VAULTTICK-EMPTYKEY-01]` check, `btrim` residual stated honestly), Step 3 (re-running the repaired P3-C by reference, the ledger-refresh flag flip and fan-out registration by reference, and the deliberate exercise of the fan-out's unreachable-while-dormant all-candidates-failed branch), Step 3-DEFER (a legitimate stopping point, not a failure), and Step 4 (exactly one manifest re-capture, with the manifest-side hygiene-loop re-animation warning and the public-repo no-secrets reminder).
- A decisions block transcribes D1 (registration lives in the runbook, never a migration), D2 (a rebuild-from-migrations produces no `match_engine_cron` job plus one `cron_runs` row reading `error = 'GUC unset at migration 015'`, because the two `app.*` GUCs return `42501` on this platform — measured on PROD 2026-09-05 — with D2's window named verbatim: *"from a rebuild until the registration step is run, the hourly recompute does not fire, and `cron_runs` says why"*), and D3 (the re-capture is a script, not hand work), all settled 2026-09-09.
- Phase 164.5's old criterion 7 (*"Write ONE forward migration re-scheduling `match_engine_cron`..."*) is flagged superseded by D1 in one sentence in the runbook and a matching line in `TODOS.md`'s `[CRON-DRIFT-01]` entry — both ledgers agree, and the old wording is preserved as lineage rather than deleted.
- A one-line cross-reference added from the `1a-bis` bullet (plan 01's redirect, left byte-identical) down to the new go-live section.

## Task Commits

Each task was committed atomically:

1. **Task 1: Make P3-C executable — repoint every dead `compute_jobs` column reference** - `ba75e39f` (fix)
2. **Task 2: Write the go-live section — ONE session, ONE re-capture, in the fixed order** - `385bd756` (docs)
3. **Task 3: Transcribe D1/D2/D3, state the rebuild outcome, flag the losing convention** - `4744a652` (docs)

**Plan metadata:** (pending — this commit)

## Files Created/Modified

- `docs/runbooks/ledger-refresh-go-live.md` — P3-C, "Watching it", and "Rollback, part 2 — Detect" repointed to `updated_at`; dated explanatory sentences added
- `.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-PREFLIGHT.md` — P3-C's second copy repointed identically
- `docs/runbooks/match-engine.md` — new Wave B go-live section (Tasks 2-3), plus a one-line 1a-bis cross-reference
- `TODOS.md` — `[PREFLIGHT-P3C-UNRUNNABLE]` closed; `[CRON-DRIFT-01]` carries the superseded-convention flag

## Decisions Made

See `key-decisions` in frontmatter. In short: the dead-column repair closed the WHOLE class (P3-C plus two sibling diagnostics), not just the named instance; P3-C is referenced from the new section rather than pasted a third time; the all-candidates-failed branch is exercised via a mandated manual first invocation plus a code-read safety confirmation rather than by fabricating a PROD failure, which was considered and rejected as an invented-failure-state; the old criterion 7 convention is flagged superseded additively in both ledgers.

## Deviations from Plan

None - plan executed exactly as written. Where the plan explicitly left a mechanism to Claude's Discretion (how to "exercise" the all-candidates-failed branch — the plan said "say how" without specifying the mechanism), the choice made is documented above under Decisions, not as a deviation.

## Issues Encountered

None during execution of this plan's own three tasks. The full-suite run (`npx vitest run`, unscoped) surfaces 2 PRE-EXISTING, unrelated failures in `src/__tests__/verify-plan-anchors.test.ts` (both reference `164.3-07-PLAN.md`'s deferral text, nothing this plan touched) — explicitly flagged as a known, pre-existing red in this plan's own `<plan_specific_guardrails>` ("the latter is currently RED on a stale anchor elsewhere, pre-existing and not yours"). Not fixed here, per scope boundary.

## User Setup Required

None - no external service configuration required.

## Plan-Level Verification

- Task 1's two `<automated>` commands: green (`runbook=5 preflight=2`, both `>=` their floors; `check-planning-hygiene.test.ts` 42/42 passed).
- Task 2's two `<automated>` commands: green (all four literal greps `>=1`; `capture-invocations -> 1`; `expected-lines=8 abort-lines=9`, both above floor).
- Task 3's two `<automated>` commands: green (all four literal greps `>=1`; `window-sentence -> 1`).
- `npx vitest run src/__tests__/check-planning-hygiene.test.ts` green after every task (42/42 each time).
- `npx vitest run` (full suite): **855 passed | 1 failed** test files, **14829 passed | 2 failed | 280 skipped** tests. The 2 failures are `src/__tests__/verify-plan-anchors.test.ts`'s `164.3-07-PLAN.md` deferral assertions — pre-existing, unrelated to this plan's four files, flagged in advance by this plan's own guardrails.
- `git diff --name-only 568cdaa9..HEAD` lists exactly the four files in `files_modified`: `docs/runbooks/ledger-refresh-go-live.md`, `.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-PREFLIGHT.md`, `docs/runbooks/match-engine.md`, `TODOS.md`.
- `git status --porcelain -- supabase/ analytics-service/ src/ scripts/` empty at every task boundary — confirmed after each of the three task commits.
- ⛔ **NOT verified here, deliberately:** that the procedure works. That is plan 09, on live PROD, behind D4. Nothing in this plan is evidence that the live jobid 1 is repointed, that P3-C passes on a fresh PROD read, or that the drift arm is clean.

## Next Phase Readiness

- Plan 09 (not autonomous, wave 4) can now execute the go-live session in `docs/runbooks/match-engine.md` directly — the pre-flight is executable, the procedure exists end-to-end in one document, and the DEFER branch is pre-written for the expected-value outcome (P3-C's last real reading, 2026-09-12, failed the freshness bar).
- `CRON-DRIFT-01`, `164.7-ACTIVATION-DEFERRED` and `VAULTTICK-EMPTYKEY-01` remain **blocked** in the shared-ID sense (declared by plan 09 too) until plan 09 produces its own SUMMARY — confirmed via `requirements.ready-ids` (0/3 ready), consistent with plans 01/04/06.
- No blockers for plan 09. `git status --porcelain -- supabase/` was empty at every task boundary throughout this plan.

## Self-Check: PASSED

- FOUND: `docs/runbooks/ledger-refresh-go-live.md`, `.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-PREFLIGHT.md`, `docs/runbooks/match-engine.md`, `TODOS.md`, this SUMMARY.md
- FOUND commits: `ba75e39f` (Task 1), `385bd756` (Task 2), `4744a652` (Task 3) — confirmed via `git log --oneline --all`

---
*Phase: 164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t*
*Plan: 08*
*Completed: 2026-09-16*
