---
phase: 153.5
slug: wizform-abandon-work-that-outlives-its-timeout
status: reconciled
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-11
reconciled: 2026-08-11
reconciled_against: [153.5-01..05-SUMMARY.md, 153.5-VERIFICATION.md, the shipped tests]
reconciliation_note: >-
  Written at planning and never touched again until this pass (warning W-153.5-2). Two rows were
  MEASURED WRONG during execution and are corrected here, not just ticked: SC-05-2 (WEDGE-01), whose
  stated `ticks > 0` oracle stays GREEN under the design D-36 REJECTED, and the 05-1 roster row,
  whose four-name exempt set can never equal the derivation. A row is ticked only where a test can be
  NAMED; where it cannot, it says so.
---

# Phase 153.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `153.5-RESEARCH.md` § Validation Architecture (test framework, requirements→test
> map, sampling rate, Wave 0 gaps) plus the success criteria of `153.5-01..05-PLAN.md`.
> Format follows `153-VALIDATION.md`, the most recent phase artifact of this kind.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest + pytest-asyncio (`asyncio_mode = auto`), pytest-xdist (`-n auto --dist loadgroup` in CI) |
| **Config file** | `analytics-service/pytest.ini` (+ `pyproject.toml` `[tool.mypy]`) |
| **Quick run command** | `cd analytics-service && .venv/bin/pytest tests/test_mt5_client_contract.py tests/test_mt5_concurrency.py -x -q` |
| **Full suite command** | `cd analytics-service && make test` (pytest + `--cov-fail-under=80`) |
| **Type gate** | `cd analytics-service && make typecheck` (== CI's `mypy --strict --follow-imports=silent services/ routers/ models/`) |
| **Estimated runtime** | quick ~20s · MT5 wave set ~90s · full `make test` ~6min |

⛔ **Run pytest from `analytics-service/`, never the repo root.** `tests/conftest_vcr.py:149`
sets `cassette_library_dir="tests/cassettes"` — a RELATIVE path. A root run misses the
cassettes and makes LIVE broker calls.
⚠️ **`mypy --strict` before shipping.** The GSD path runs pytest only, so type errors stay
latent until PR CI. Fix with `cast()`, never `# type: ignore`.
⚠️ **`pandera` may be missing locally** — `pip install 'pandera==0.32.1' --break-system-packages`.
It is a declared dep and CI has it.
⚠️ **Every test in this phase is offline by construction** — no live gateway, no `mt5linux`,
no rpyc import; all clients are built over the `_connect=` seam.

---

## Sampling Rate

- **After every task commit:** `cd analytics-service && .venv/bin/pytest tests/test_mt5_client_contract.py tests/test_mt5_concurrency.py tests/test_mt5_abandon_fence.py -x -q`
- **After every plan wave:** `.venv/bin/pytest tests/test_mt5_*.py tests/test_ingestion_mt5.py tests/test_allocator_positions_non_ccxt.py tests/test_long_fetch.py tests/test_job_worker.py -q` **plus** `make typecheck`
- **Before `/gsd:verify-work`:** `make test` green (coverage ≥ 80) + `make typecheck` clean
- **Max feedback latency:** 30 seconds (per-task quick run)
- ⚠️ **Timing-sensitivity check:** `tests/test_mt5_abandon_fence.py` runs **three consecutive
  times** in plan 05's verify block. A guard that is green once and red on the third run is a
  fifth flake mechanism, not a pass.

---

## Per-Task Verification Map

> Keyed by plan/task. `❌ W0` = the test file or assertion does not exist yet.
> Requirement ids are the finding ids CONTEXT.md tracks (`#5`, `#6a/#6b`, `#7`) plus the two
> guard requirements (`D-36-static`, `D-37`).
>
> **Reconciled 2026-08-11 against the five SUMMARYs, `153.5-VERIFICATION.md` and the shipped test
> files.** A row is `✅` only where a test can be NAMED and was found in the tree; a row whose only
> evidence is a transient inline check or a SUMMARY's prose says `⚠️ unconfirmed` rather than being
> ticked. Two *Secure Behavior* cells were **measured wrong at planning** and are corrected in place
> (marked ✏️) rather than silently ticked — see the notes below the table.

| Task | Plan | Wave | Req | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|------|------|------|-----|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-1 | 01 | 1 | #7 | T-153.5-01 | The six session-touching methods carry `_assert_live` as their FIRST statement; `restart`/`__init__`/`close`/`release`/`_teardown_transport` carry none | ast shape check | inline `python -c` ast assert in the task verify | ✅ inline | ✅ green — and the transient inline check has a PERMANENT successor: `test_mt5_abandon_roster.py::test_every_session_touching_method_carries_the_fence` + `::test_the_fence_is_the_first_executable_statement`. ⚠️ Shipped count is **seven** fenced methods, not six: `restart` carries `_assert_live("restart")` as of plan 02, so it is no longer in the "carry none" list |
| 01-1 | 01 | 1 | #7 | T-153.5-04 | Lease bumps before release, resets the occupancy token, nothing on a timed-out acquisition | unit | `pytest tests/test_mt5_concurrency.py -x -q` | ✅ extend | ✅ green — `test_the_lease_bumps_the_terminal_epoch_on_a_normal_exit`, `test_a_timed_out_acquisition_bumps_nothing`, `test_the_occupancy_stamp_covers_the_hold_and_is_reset_after_it` |
| 01-2 | 01 | 1 | #7 | T-153.5-01 | A post-release touch raises `Mt5SessionAbandoned` **and the fake records no second session call** | unit | `pytest tests/test_mt5_client_contract.py -x -q` | ❌ **W0** → ✅ landed | ✅ green — `test_a_touch_after_the_lease_released_is_refused_and_never_reaches_the_terminal`. ⚠️ Honest note from 01-SUMMARY: the falsification's red surfaced at `pytest.raises` BEFORE the call-log assertion, so the two oracles were never observed red in one run; the second `mt5.stage account_info` in captured stdout is the direct evidence the touch landed. The verifier's M1 later red on the call-log oracle in `test_mt5_abandon_fence.py` |
| 01-2 | 01 | 1 | #7 | T-153.5-03 | The refusal LOGS a WARNING naming WIZFORM-ABANDON (D-39 — the raise alone is discarded) | unit (caplog) | same | ❌ **W0** → ✅ landed | ✅ green — the caplog assertion inside the same test (`"WIZFORM-ABANDON" in r.getMessage()`); falsified by deleting the `logger.warning` and keeping the `raise` (01-SUMMARY F2) |
| 01-2 | 01 | 1 | #7 | — | ⭐ Preflight/lazy-bind: a client built outside any lease is NOT refused after an unrelated release | unit | same | ❌ **W0** → ✅ landed | ✅ green — `test_a_preflight_built_client_binds_lazily_and_is_not_falsely_refused` |
| 01-2 | 01 | 1 | #7 | T-153.5-02 | `Mt5SessionAbandoned` is a plain `Exception`, and its message contains no `_WRONG_SERVER_TOKENS`/`_AUTH_TOKENS` member for ANY stage string | unit (sweep) | same | ❌ **W0** → ✅ landed | ✅ green — `test_session_abandoned_cannot_be_absorbed_into_a_credential_verdict` + the 8-way parametrized `test_session_abandoned_message_carries_no_classifier_token` |
| 01-2 | 01 | 1 | #7 / D-41 | — | `close()`/`release()` still tear down the transport on a stale client | unit | same | ❌ **W0** → ✅ landed | ✅ green — `test_close_still_tears_down_the_transport_on_a_stale_client` + `test_release_still_tears_down_the_transport_on_a_stale_client` |
| 01-3 | 01 | 1 | #7 | T-153.5-04 | Bump on return / raise / cancellation; **no** bump on `Mt5TerminalBusyError` | unit | `pytest tests/test_mt5_concurrency.py -x -q` | ❌ **W0** → ✅ landed | ✅ green — the three `test_the_lease_bumps_the_terminal_epoch_*` cases plus `test_a_timed_out_acquisition_bumps_nothing` |
| 01-3 | 01 | 1 | #6a/#6b | — | ⭐ The frozen-`contextvars.Context` property `to_thread` rests on, re-verified in-repo | unit (bounded Event) | same | ❌ **W0** → ✅ landed | ✅ green — `test_to_thread_work_carries_a_frozen_copy_of_the_occupancy_stamp` (real lease + real `to_thread`) |
| 01-3 | 01 | 1 | — | — | The epoch registry is NOT re-exported into `job_worker`/`allocator_positions` (the E2 trap cannot recur) | unit | same | ❌ **W0** → ✅ landed | ✅ green — `test_the_epoch_registry_is_not_re_exported_anywhere` |
| 01-3 | 01 | 1 | — | — | ONE shared `reset_terminal_state_for_tests()` serves all five fixtures | isolation | `pytest tests/test_mt5_validate.py tests/test_mt5_derive_branch.py tests/test_allocator_positions_non_ccxt.py tests/test_ingestion_mt5.py -q` | ✅ re-point | ⚠️ **unconfirmed — no standing test.** The five fixtures WERE re-pointed (01-SUMMARY, and the four suites are green), but the only check that they all reach the ONE helper was an inline assert in plan 01's verify block, which does not survive the run. A sixth hand-rolled `.clear()` could be added tomorrow and nothing would red |
| 02-1 | 02 | 2 | **#5** | T-153.5-05 | A restart fenced mid-reconnect NEVER reaches `stale.shutdown()`, yet closes BOTH sockets and restores `_closed = True` | unit | `pytest tests/test_mt5_client_contract.py -x -q` | ❌ **W0** → ✅ landed | ✅ green — `test_a_restart_abandoned_mid_reconnect_never_reaches_the_shared_shutdown`; all three invariants falsified separately (02-SUMMARY A/B/C) and the shutdown half re-falsified by the verifier's M2 |
| 02-1 | 02 | 2 | #5 | — | Entry-fenced restart refuses before ANY I/O — the injected `_connect` is never invoked | unit | same | ❌ **W0** → ✅ landed | ✅ green — `test_a_restart_already_abandoned_at_entry_refuses_before_any_io` |
| 02-1 | 02 | 2 | #5 | — | The live heal is unchanged: one `shutdown()`, stale transport only, `_closed` ends False, client still usable | unit (regression) | same | ❌ **W0** → ✅ landed | ✅ green — `test_a_live_restart_under_a_current_generation_is_unchanged_by_the_fence`. ⚠️ This case had to be REPAIRED mid-execution: as first written it restarted a never-bound client and was blind to its own mutation (02-SUMMARY, auto-fix 1) |
| 02-1 | 02 | 2 | #5 | — | The shutdown roster stays green with **zero** pin edits | static | `pytest tests/test_mt5_shutdown_roster.py -x -q` | ✅ must stay green | ✅ green — `tests/test_mt5_shutdown_roster.py` unchanged except one docstring; `TEARDOWN_BARE_REFERENCE_SITES` (asserted `==`) and `PERMITTED_SHUTDOWN_SITES` both untouched |
| 02-2 | 02 | 2 | **#6a/#6b** | T-153.5-06 | A zombie construction disposes its own just-opened transport and raises — no leaked socket | unit | `pytest tests/test_mt5_client_contract.py -x -q` | ❌ **W0** → ✅ landed | ✅ green — `test_a_zombie_construction_disposes_its_own_socket_and_refuses`; the half-fix (post-connect check deleted) observed red on the BALANCE oracle, and re-observed by the verifier's M5 |
| 02-2 | 02 | 2 | #6a/#6b | — | Already-stale at entry ⇒ refuses BEFORE connect; no socket opened at all | unit | same | ❌ **W0** → ✅ landed | ✅ green — `test_a_construction_already_abandoned_at_entry_opens_no_socket_at_all` |
| 02-2 | 02 | 2 | #6a/#6b | — | ⭐ Preflight exemption is structural (no token ⇒ no fence), with no opt-in parameter anywhere | unit | same | ❌ **W0** → ✅ landed | ✅ green — `test_a_preflight_construction_carries_no_token_and_is_never_fenced` + `test_no_constructor_parameter_opts_into_the_construction_fence` (signature derived from source) |
| 02-2 | 02 | 2 | #6a/#6b | T-153.5-07 | A construction under a genuinely held real lease CANNOT raise — so `exchange.py:536` can never mint a breaker-keying 503 from the fence | unit (real lease) | same | ❌ **W0** → ✅ landed | ✅ green — `test_a_construction_under_a_genuinely_held_lease_cannot_be_refused`, driving the SHIPPED `mt5_terminal_lease`; the structural half is pinned by `test_the_occupancy_stamp_covers_the_hold_and_is_reset_after_it` |
| 02-3 | 02 | 2 | #6a | T-153.5-06 | Router connect-stage timeout: no rpyc conn is left open (open/close **balance**) | path unit (bounded Event) | `pytest tests/test_mt5_validate.py -x -q` | ❌ **W0** → ✅ landed | ✅ green — `test_mt5_validate.py::test_a_connect_stage_timeout_leaves_no_rpyc_socket_open` |
| 02-3 | 02 | 2 | #6b | T-153.5-06 | Worker adapter connect-stage timeout: same balance invariant | path unit | `pytest tests/test_ingestion_mt5.py -x -q` | ❌ **W0** → ✅ landed | ✅ green — `test_ingestion_mt5.py::test_a_connect_stage_timeout_leaves_no_rpyc_socket_open`. ⚠️ 02-SUMMARY's own gap: the balance oracle is arm-agnostic and would be satisfied vacuously by a run where the zombie never started; guarded by a companion non-vacuity assertion, which is a second assertion rather than a property of the oracle |
| 02-3 | 02 | 2 | — | — | Four invalidated rationales re-cut (2 in `mt5_client.py`, 2 in `mt5_concurrency.py`) | prose / diff review | — (see Manual-Only) | ❌ **W0** → ✅ landed | ➖ n/a (prose) — **reviewed, not tested.** Discharged by `153.5-VERIFICATION.md` §"Honest limits — stated in CODE", which located D-43's limit at three source sites and the narrowed-window caveat at `routers/exchange.py`. No production mutation can red a comment |
| 03-1 | 03 | 2 | #5 | T-153.5-09 | ZERO raw `async with _mt5_terminal_lock_for(...)` remain in `job_worker.py` / `allocator_positions.py` (was 3 at HEAD) | ast | inline `python -c` ast assert in the task verify | ✅ inline | ✅ green — the inline assert ran (3 → 0) AND has a permanent successor: `test_mt5_concurrency.py::test_no_production_module_acquires_the_raw_terminal_lock`, re-confirmed by ast at verification (lease blocks at `job_worker:388`, `job_worker:3610`, `allocator_positions:677`, `ingestion/mt5:200`, `exchange:846`) |
| 03-2 | 03 | 2 | #5 | T-153.5-09 | The derive release advances the epoch by an **exact** delta; same on the holdings path | unit | `pytest tests/test_mt5_derive_branch.py tests/test_allocator_positions_non_ccxt.py -q` | ❌ **W0** → ✅ landed | ✅ green — `test_derive_release_bumps_the_terminal_epoch_exactly_once` + `test_mt5_holdings_release_bumps_the_terminal_epoch_exactly_once`; both observed red (`assert (0 - 0) == 1`) when their site was reverted to the raw Lock |
| 03-2 | 03 | 2 | — | — | The `neuter_lock` control still binds after re-pointing at the module the lease READS (E2 trap) | unit (self-detecting) | `pytest tests/test_mt5_derive_branch.py -q` | ✅ re-point | ✅ green — `test_mt5_concurrent_syncs_serialized`, and BOTH directions observed: the control went red on its own negative arm before the re-point ("with the lock neutered the concurrent syncs MUST interleave … got [fully contiguous]"), i.e. the E2 trap made loud instead of silent |
| 03-2 | 03 | 2 | — | T-153.5-10 | ast class pin: no production module under `services/`+`routers/` may reintroduce a raw acquisition; walk floor ≥ 40 files; three self-tests | static | `pytest tests/test_mt5_concurrency.py -q` | ❌ **W0** → ✅ landed | ✅ green — `test_no_production_module_acquires_the_raw_terminal_lock` (88 files walked, floor 40 hand-typed) + `test_the_raw_acquisition_scanner_reports_and_does_not_over_report`. ✏️ **FOUR** self-test assertions shipped, not three (bare form reports, attribute form reports, docstring does not, plain call does not) |
| 04-1 | 04 | 3 | #7 | T-153.5-12 | Router: the refusal answers **424 / NETWORK_UNAVAILABLE / recoverable:true**, `trace.outcome="transient"` — never a bodyless 500 | route unit | `pytest tests/test_mt5_validate.py -x -q` | ❌ **W0** → ✅ landed | ✅ green — `test_an_abandoned_session_refusal_is_transient_never_a_bodyless_500`; observed red at the RED commit as a genuine escape past `pytest.raises(HTTPException)` |
| 04-1 | 04 | 3 | #7 | — | Adapter: propagates unchanged (type identity) — its documented transient disposition | unit | `pytest tests/test_ingestion_mt5.py -x -q` | ❌ **W0** → ✅ landed | ⚠️ **green but NOT falsifiable against today's source** — `test_an_abandoned_session_refusal_propagates_out_of_validate_unchanged` passes, but deleting the `except Mt5SessionAbandoned: raise` arm changes nothing, because propagation is already the default. 04-SUMMARY row G is the honest substitute: it falsifies the arm against the FUTURE it was written for (add a broad `except Exception: return _auth_failed()`, then remove the arm ⇒ `DID NOT RAISE`) |
| 04-1 | 04 | 3 | #7 | T-153.5-14 | Derive: transient FAILED `DispatchResult`, the strategy-analytics failure stamp NOT invoked (spy), and `_mt5_bounded_restart` NOT called | unit | `pytest tests/test_mt5_derive_branch.py -q` | ❌ **W0** → ✅ landed | ✅ green — `test_mt5_abandoned_session_is_transient_with_no_stamp_and_no_restart`; both negatives falsified independently (adding the stamp, and calling the restart) |
| 04-1 | 04 | 3 | #7 | T-153.5-13 | Derive: the `error_message` text carries no `_WRONG_SERVER_TOKENS`/`_AUTH_TOKENS` member | unit | same | ❌ **W0** — see Wave 0 note | ✅ green — **the Wave-0 gap this file itself opened is CLOSED**: `test_the_abandoned_derive_error_message_carries_no_classifier_token` sweeps against the LIVE tables imported from `services/mt5_validation.py`, and reds when `terminal` is put into the message |
| 04-1 | 04 | 3 | #7 | — | Holdings: `AllocatorHoldingsSyncTransientError(MT5_UNREACHABLE_NOTE)`, chained | unit | `pytest tests/test_allocator_positions_non_ccxt.py -q` | ❌ **W0** → ✅ landed | ✅ green — `test_mt5_abandoned_session_raises_the_transient_with_the_fixed_copy`; falsified by raising a `RuntimeError` carrying the internal text on its way to `sync_error` |
| 04-2 | 04 | 3 | #7 | T-153.5-12 | `process_key` :774 and :1586 answer a **coded** transient envelope (body asserted, not just the status int) | route unit | `pytest tests/test_process_key.py -q` | ❌ **W0** → ✅ landed | ✅ green — `test_validate_only_abandoned_session_is_a_coded_transient_never_a_500` (`:774`) + `test_sync_pipeline_abandoned_session_is_a_coded_transient_never_a_500` (`:1586`). ⭐ **Both members falsified independently** — removing either arm reds ONLY that site's case, so an instance fix at either site is detectable. ℹ️ `:1586` is not reachable by an mt5 body today (MT5RECON-01 admits mt5 to long-fetch routes only); covered anyway because the class is "an unguarded `adapter.validate` call site", and said plainly in the test docstring |
| 05-1 | 05 | 3 | **D-36-static** | T-153.5-15 | ✏️ **CORRECTED.** Every `self._mt5`-reaching method carries the guard; `(touching − guarded)` is EXACTLY `{__init__, close, release}` — **three names, not four.** Measured while writing the test: `_teardown_transport` contains ZERO `self._mt5` nodes (it takes the connection as `mt5_obj`), so it can never appear in the difference and the planned four-name set could never be equal to it. It is pinned separately as `EXEMPT_BY_PARAMETER`, asserted disjoint, with `D41_EXEMPT` DERIVED as the union so D-41's four names still appear once | static roster | `pytest tests/test_mt5_abandon_roster.py -x -q` | ❌ **W0** → ✅ landed | ✅ green — `test_every_session_touching_method_carries_the_fence` (exact `==`) + `test_the_teardown_verb_reaches_the_session_only_through_its_parameter`. ⛔ Widening the equality set by a name the derivation can never produce would let it absorb a REAL unfenced method |
| 05-1 | 05 | 3 | D-36-static | — | Self-tests: guarded synthetic reported guarded; ⭐ UNguarded synthetic REPORTED; prose ignored; aliased `stale = self._mt5` caught | static self-test | same | ❌ **W0** → ✅ landed | ✅ green — `test_selftest_predicates_report_guarded_and_unguarded` (the ⭐ one — forcing the guard predicate to `True` reds it), `test_selftest_prose_is_ignored`, `test_selftest_the_alias_shape_is_a_touch`, plus four more (nested closure, late fence, bare-name call, positional predicates) |
| 05-2 | 05 | 3 | **D-37** | T-153.5-16 | A zombie's post-release touch is refused, with the abandonment scheduled by a test-owned Event; ordering by **log index**, never wall clock | runtime barrier | `pytest tests/test_mt5_abandon_fence.py -x -q` | ❌ **W0** → ✅ landed | ✅ green — `test_a_zombies_post_release_touch_is_refused_and_the_control_proves_it`; observed red on the HARM with the full ordered log printed: `[('initialize', 0), ('login', 0), ('<lease released>', 1), ('account_info', 1)]` |
| 05-2 | 05 | 3 | D-37 | T-153.5-16 | Embedded negative control (`neuter=True`): the post-release touch DOES land | runtime | same | ❌ **W0** → ✅ landed | ✅ green — the control lives inside the same case (permanent, not a one-off) and **the control itself was falsified**: disarming `neuter_fence=True` reds with "with the fence neutered the post-release touch MUST land … got []" |
| 05-2 | 05 | 3 | D-37 | T-153.5-17 | ✏️ **CORRECTED — this is the one row that was factually WRONG, not merely unticked.** The oracle is the **RELEASE WINDOW**: `release_ticks > 0 or release_elapsed_s < _ABANDON_BOUND_S`. The planned form — a bare `ticks > 0` — was **MEASURED unable to see its own mutation** and was REPLACED for that reason: the 10 ms ticker has already accumulated ticks during the abandonment bound's own 50 ms, so `ticks > 0` stays GREEN under the very "join the zombie / hold the lease until the worker confirms it stopped" design D-36 ⛔ REJECTED. The bare `ticks > 0` is retained ONLY as a separate, explicitly weaker secondary assertion (it proves the session work runs off the loop at all) with its own message | runtime | same | ❌ **W0** → ✅ landed | ✅ green — `test_the_release_does_not_block_the_event_loop_on_the_zombie`. Falsified twice, by two independent agents: `releasing the lease blocked the event loop for 0.303s and the 10ms ticker advanced 0 times across it` (05-SUMMARY row H, and again as the verifier's M3) |
| 05-2 | 05 | 3 | D-37 | T-153.5-17 | Three consecutive green runs; every gate set in a `finally`; a mis-ordered test reds in <1s, never stalls the 300s `THREAD_JOIN_TIMEOUT` | runtime | `for i in 1 2 3; do pytest tests/test_mt5_abandon_fence.py -q; done` | ❌ **W0** → ✅ landed | ✅ green — 3 consecutive runs, 2 passed each (2.59s / 2.16s / 2.40s); worst case per harness run is `_ARRIVAL_BOUND_S + _BARRIER_BOUND_S ≈ 2.3s`, bounded by construction |
| all | all | 1–3 | — | — | No regression in the existing MT5 surface | regression | `pytest tests/test_mt5_*.py tests/test_ingestion_mt5.py tests/test_allocator_positions_non_ccxt.py tests/test_long_fetch.py -q` | ✅ | ✅ green — full analytics-service suite **5141 passed / 96 skipped / 0 failed** at the phase gate; the verifier's own 10-module MT5 run was 332 passed |
| all | all | 1–3 | — | — | `mypy --strict` clean | type | `make typecheck` | ✅ | ✅ green — `mypy services/ routers/ models/` clean (90 source files) and `mypy --strict --follow-imports=silent services/ingestion/` clean (13 files), re-run at every wave |

*Status: ⬜ pending · ✅ green (test NAMED) · ⚠️ unconfirmed / caveated · ➖ n/a (prose) · ❌ red · ⚠️ flaky*

**Corrections made by this reconciliation (✏️), not just ticks:**

1. **05-2 / T-153.5-17 (WEDGE-01)** — the planned `ticks > 0` oracle was measured GREEN under the
   rejected design. Replaced by the release-window discriminator. This is the same defect class the
   phase itself kept hitting ("a guard that cannot see its own mutation is not a guard"), and it was
   caught in the test's design rather than after it shipped.
2. **05-1 / T-153.5-15 (roster exempt set)** — four names → **three**; `_teardown_transport` moved to
   a separate `EXEMPT_BY_PARAMETER` pin because the derivation can never produce it.
3. **03-2 (ast class pin)** — three self-tests → **four**.
4. **01-1 (fence roster)** — six fenced methods → **seven** (`restart` joined in plan 02).

---

## Wave 0 Requirements

> **All twelve landed** (reconciled 2026-08-11). Each box below is ticked against a file or a named
> test found in the tree, not against a SUMMARY's prose.

- [x] `analytics-service/tests/test_mt5_abandon_fence.py` — the D-37 barrier guard + its embedded negative control + the liveness ticker *(plan 05, Task 2)* — **487 lines, 2 cases.** ✏️ The "liveness ticker" shipped as the RELEASE-WINDOW discriminator; the bare ticker is the weaker secondary assertion (see the corrected `SC-05-2` row)
- [x] `analytics-service/tests/test_mt5_abandon_roster.py` — the D-36 static complement, floors + three-flavour self-tests *(plan 05, Task 1)* — **703 lines, 14 cases**; floors `GUARDED_METHOD_FLOOR = 7` / `SESSION_TOUCH_FLOOR = 9` (measured 10) / `CLIENT_METHOD_FLOOR = 12` (measured 15), all hand-typed literals; **seven** self-tests, not three
- [x] `services/mt5_concurrency.reset_terminal_state_for_tests()` + re-point the **five** existing autouse fixtures at it *(plan 01, Tasks 1 & 3 — RESEARCH Pitfall 8: a leaked epoch under `-n auto --dist loadgroup` buys a sixth flake mechanism)* — helper exists and all five fixtures were re-pointed. ⚠️ Nothing standing asserts that they all still reach it (see the `⚠️ unconfirmed` row in the map above)
- [x] Sink contract cases in `tests/test_mt5_client_contract.py`: stale refusal, WARNING, lazy bind, exempt verbs, exception hygiene *(plan 01, Task 2)* — 13 cases incl. the 8-way parametrized token sweep
- [x] Lease lifecycle + frozen-Context cases in `tests/test_mt5_concurrency.py` *(plan 01, Task 3)* — 7 cases (3 bump paths, the no-bump negative, occupancy lifecycle, frozen-Context copy, re-export hygiene)
- [x] Restart-fence cases (fenced / entry-fenced / live heal) in `tests/test_mt5_client_contract.py` *(plan 02, Task 1)*
- [x] Construction-fence cases + the live-path-cannot-raise proof *(plan 02, Task 2)* — 5 cases, incl. the constructor-signature pin against a future `fence_construction=` opt-in
- [x] Connect-stage leak-balance cases in `tests/test_mt5_validate.py` and `tests/test_ingestion_mt5.py` *(plan 02, Task 3)* — both named `test_a_connect_stage_timeout_leaves_no_rpyc_socket_open`
- [x] Exact-delta epoch-bump cases on the derive and holdings paths + the raw-acquisition ast pin *(plan 03, Task 2)*
- [x] Five classification cases (router, adapter, derive, holdings, `process_key` ×2) *(plan 04, Tasks 1–2)* — six tests across five surfaces
- [x] **Token-freedom assertion on the derive arm's `error_message`** — *(added 2026-08-11 by this VALIDATION: plan 04 Task 1 step 3 REQUIRES the message be free of `_WRONG_SERVER_TOKENS`/`_AUTH_TOKENS`, but its `<behavior>` list carries no assertion pinning it. Owner: plan 04, Task 1 — add the sweep beside the existing derive case; no plan restructure needed.)* — ⭐ **landed and falsified**: `test_the_abandoned_derive_error_message_carries_no_classifier_token`. The gap this file opened is the gap plan 04 closed
- [x] Framework install: **none needed** — confirmed, nothing added to `tech-stack`

⚠️ **One location correction against RESEARCH's map:** RESEARCH files the `no-false-positive`
(lazy-bind) case under `tests/test_mt5_derive_branch.py -k preflight_epoch`; the plans place
it in `tests/test_mt5_client_contract.py` (plan 01, Task 2) because it is a **sink** property
provable without the worker harness. The plans' placement wins; the behaviour is identical.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The four re-cut rationales in `mt5_client.py` / `mt5_concurrency.py` state the POST-153.5 safety argument | plan 02, Task 3 | Prose. No production mutation can red a comment — and this is precisely the "behaviour right, record wrong" state the phase exists to end | Diff review at `/gsd:code-review`: each re-cut names what changed and why; ⛔ re-cut, never delete |
| `test_every_restart_call_site_holds_the_terminal_lease`'s docstring no longer claims to be "the ONLY thing standing between" the permitted shutdown and −10004 | plan 05, Task 2 | Same — docstring-only edit, **zero pins moved** | Diff review: assertions byte-identical; the docstring names its lexical ceiling and cross-references `tests/test_mt5_abandon_fence.py` |
| D-43's honest limit is stated (a call already dispatched on the wire cannot be un-sent), not hidden | plan 02 | An un-closable gap cannot be tested; it can only be recorded | Diff review: the limit appears in the restart comment AND the SUMMARY |
| A real MT5 terminal survives a genuine abandoned-restart in production | #5 | Needs the live gateway and a real wedged pipe | Deferred to Phase 155 (MT5-VERIFY) alongside the live-latency work findings #8/#10 are gated on |

---

## Falsifiability Ledger

> **Coverage answers "is it verified?". This section answers "CAN the verification FAIL?"**
> One row per success criterion. Each row names (a) the **semantic PRODUCTION** mutation and
> (b) the **specific test** that must red. Complete the Observed column at execution time.
> **Observed means run** — paste the failing assertion.

| SC | Mutation (exact edit to production source) | Must turn RED | Observed? | Evidence |
|----|-------------------------------------------|---------------|-----------|----------|
| **SC-01-1** six methods refuse loudly | `mt5_client.py` `_assert_live`: delete the `self._epoch != current` branch, leaving an unconditional `return` | `test_mt5_client_contract.py` stale-refusal case — the fake's log records the post-bump `account_info` | ✅ observed ×2 | 01-SUMMARY F1: `DID NOT RAISE Mt5SessionAbandoned`, **and the captured stdout showed a SECOND `mt5.stage account_info` round-trip** — the touch genuinely landed. Re-run independently by the verifier (M1) against `test_mt5_abandon_fence.py`, which red on the HARM oracle instead: `[('account_info', 1)]` landed after `<lease released>` |
| SC-01-1 *(class, 2nd member)* | Delete `self._assert_live("last_error")` from **`_raise_last` only** — the off-happy-path method, not the one the author had in mind | `test_mt5_abandon_roster.py` exempt-set EXACT equality — `(touching − guarded)` gains `_raise_last` | ⚠️ **NOT observed — this exact member was never neutered** | The CLASS was falsified twice with *other* members: the verifier's M4 dropped `self._assert_live("account_info")` (`Extra items in the left set: 'account_info'`) and 05-SUMMARY row A added a brand-new unfenced `positions_get`. Neither is `_raise_last`, which is what this row deliberately asked for (the off-happy-path member). ⛔ Recorded as skipped, never as caught — the roster's `==` makes it near-certain, but "near-certain" is not "run" |
| SC-01-1 *(D-39 loudness)* | Delete the `logger.warning(...)` from `_assert_live`, keep the `raise` | `test_mt5_client_contract.py` caplog assertion — proves loudness is pinned separately from the raise | ✅ observed | 01-SUMMARY F2: the caplog assertion failed with *"the fence refused silently … (D-39)"* while the `raise` still fired — loudness is pinned separately, as intended |
| **SC-01-2** bump on every exit path | `mt5_concurrency.py`: move `bump_mt5_terminal_epoch(...)` out of the `finally` onto the normal-return path after the `yield` | `test_mt5_concurrency.py` raise-path **and** cancellation-path cases | ✅ observed | 01-SUMMARY F7: the raise-path AND cancellation-path cases red (`assert 0 == 1`); the normal-exit case stayed green, which is what makes the mutation attributable |
| SC-01-2 *(no bump on busy)* | Move the bump above the bounded-acquire `wait_for`, so a `Mt5TerminalBusyError` path also bumps | `test_mt5_concurrency.py` "epoch UNCHANGED after a timed-out acquisition" | ✅ observed | 01-SUMMARY F8: *"a timed-out waiter bumped the generation — it just fenced the sessions of the holder that is legitimately mid-IPC"* |
| SC-01-2 ⚠️ *honest gap* | Swap the order to `lock.release()` **then** `bump_mt5_terminal_epoch(...)` | **NOTHING REDS.** No test schedules a successor acquiring inside that window. Recorded as a gap, never as caught — the ordering is by-construction and by-comment only | ⬜ n/a | **STILL A GAP — confirmed at reconciliation, NOT promoted.** 01-SUMMARY re-ran the reasoning and reached the same answer: no test schedules a successor inside the window, so the swap reds nothing. The shipped ordering — bump, then un-stamp, then `lock.release()` — is guaranteed by construction and by the comment at the `finally` only. ⛔ Do not tick this row without a test that acquires inside the window |
| **SC-01-3** lazy bind / no false positive | `mt5_client.py` `__init__`: bind eagerly — `self._epoch = _mt5_epoch_for(self.terminal_key)` instead of `None` | `test_mt5_client_contract.py` preflight/lazy-bind case ⭐ (this is the test that proves the lazy-bind decision) | ✅ observed | 01-SUMMARY F3: the preflight case red with `Mt5SessionAbandoned` plus the generation-0-vs-1 WARNING — an unrelated release falsely refusing a legitimate client, i.e. exactly the false positive the lazy bind exists to prevent |
| **SC-01-4** exception hygiene | `Mt5SessionAbandoned.__init__`: interpolate the stage — `f"MT5 session abandoned during {stage}"` | `test_mt5_client_contract.py` token sweep — `connect` and `terminal_info` hit `_WRONG_SERVER_TOKENS`, `login` hits `_AUTH_TOKENS` | ✅ observed | 01-SUMMARY F4: **exactly 4 of 8** stages red — `login`, `account_info` (`_AUTH_TOKENS`), `terminal_info`, `connect` (`_WRONG_SERVER_TOKENS`). The row predicted three of the four |
| SC-01-4 *(structural)* | Make `Mt5SessionAbandoned` subclass `Mt5ClientError` | `test_mt5_client_contract.py` `not issubclass(..., Mt5ClientError)` | ✅ observed | 01-SUMMARY F5: `assert not issubclass(...)` red |
| SC-01-5 D-41 exemption | Add `self._assert_live("close")` as the first statement of `close()` | `test_mt5_client_contract.py` exempt-verbs case (a stale client can no longer close) **and** `test_mt5_abandon_roster.py` exempt-set equality | ✅ observed (contract half) / ⚠️ roster half by construction | 01-SUMMARY F6: `test_close_still_tears_down_the_transport_on_a_stale_client` red — a stale client could not close its own socket — while `release` stayed green, confirming the neuter was scoped. ⚠️ The roster half was NOT run: the roster did not exist in wave 1, and nobody re-ran this mutation after plan 05 landed. The `==` equality makes it structural, but it is unobserved |
| **SC-02-1** finding #5 closed | `mt5_client.py`: delete the SECOND check (immediately before `stale.shutdown()`), keeping ONLY the entry check — the plausible half-fix | `test_mt5_client_contract.py` fenced-restart case: `shutdown_calls == 0` fails | ✅ observed ×2 ⭐ | 02-SUMMARY row A: *"an abandoned restart still issued the ONE permitted mt5.shutdown() … assert 1 == 0"* — on the CALL-LOG oracle, not on the exception type. Re-run by the verifier (M2) with `restart`'s second check set to `if False:`, redding BOTH the roster positional case and the contract case. ⭐ **The half-fix a reviewer would have signed off does not survive** |
| SC-02-1 *(invariant 2 of 3)* | On the fenced path delete `self._closed = True` | fenced-restart `client._closed is True` assertion | ✅ observed | 02-SUMMARY row B: ``` `_closed` was left False on the abandoned path … assert False is True ``` |
| SC-02-1 *(invariant 3 of 3)* | On the fenced path delete the `_teardown_transport(self._mt5, ...)` for the FRESH WR-01 connection | fenced-restart "fresh conn was closed" assertion | ✅ observed | 02-SUMMARY row C: *"the WR-01 reconnect's FRESH socket was left open on the abandoned path … assert 0 == 1"* |
| SC-02-1 *(live heal unbroken)* | Make the entry check refuse whenever `self._epoch is not None` | live-heal case: `stale.shutdown()` called exactly once, client still usable | ✅ observed — **after the test itself had to be repaired** | 02-SUMMARY row D: the FIRST run of this neuter redded an unrelated pre-existing case (`test_per_instance_ipc_timeouts_survive_restart`) and sailed past the live-heal case, which restarted a never-bound client. The case was fixed to bind the generation with a read first (the production shape — a restart is triggered BY a failed read), the neuter re-run, and the intended red observed |
| **SC-02-2** finding #6 closed | `mt5_client.py` `__init__`: delete the POST-connect re-check, keeping only the PRE-connect one — again the plausible half-fix | `test_mt5_client_contract.py` zombie-construction case: the just-opened conn's close never happens | ✅ observed ×2 ⭐ | 02-SUMMARY row E: *"the abandoned construction left an rpyc socket OPEN with no owner … assert 1 == 0"*. Re-run by the verifier (M5), redding both the unit case and the router end-to-end case. ⭐ **The second half-fix does not survive either** |
| SC-02-2 *(mandatory teardown)* | Replace the post-connect `_teardown_transport(...)` with a bare `raise` | same case, on the open/close **balance** assertion | ✅ observed | 02-SUMMARY row F: same case, same balance assertion, `assert 1 == 0` — a bare raise after `connect()` is Pitfall 6 in a new costume |
| SC-02-2 *(structural exemption)* | Treat a `None` occupancy token as `(key, 0)` instead of exempt | preflight-exemption case (a legitimate preflight build starts being refused) | ✅ observed | 02-SUMMARY row G: `Mt5SessionAbandoned` out of `test_a_preflight_construction_carries_no_token_and_is_never_fenced` — a legitimate preflight build refused |
| SC-02-2 *(paths)* | Same post-connect deletion, observed end-to-end | `test_mt5_validate.py` #6a and `test_ingestion_mt5.py` #6b leak-balance cases | ✅ observed — **BOTH members** | 02-SUMMARY row E′: `test_mt5_validate.py` #6a *"the abandoned connect-stage thread left an rpyc session open …"* and `test_ingestion_mt5.py` #6b's sibling assertion. Both members of the class, not just the one the author had in mind |
| **SC-02-3** live path cannot raise | `begin_mt5_lease_occupancy`: stamp `_mt5_epoch_for(key) + 1` (off-by-one) | `test_mt5_client_contract.py` construct-inside-a-real-lease case — proves `exchange.py:536` cannot mint a breaker-keying 503 | ✅ observed | 02-SUMMARY row H: `Mt5SessionAbandoned` out of `test_a_construction_under_a_genuinely_held_lease_cannot_be_refused` |
| SC-02-4 re-cuts | *(prose — no production mutation can red a comment)* | **not falsifiable**; covered by the Manual-Only diff review above | ⬜ n/a | prose — **discharged.** `153.5-VERIFICATION.md` reviewed all four re-cuts plus the two accepted limits and found neither "quietly dropped" nor "overclaimed as fixed" |
| **SC-03-1** every acquisition leases | Revert **`allocator_positions.py:656`** to `async with _mt5_terminal_lock_for(key):` — the site the author did NOT have in mind | the plan-03 ast class pin **and** the holdings exact-delta bump assertion | ✅ observed — **both oracles** | 03-SUMMARY F1: the ast pin named `('services/allocator_positions.py', 676)` AND the holdings delta read `assert (0 - 0) == 1`. The deliberately-second-member row fired |
| SC-03-1 *(the motivating path)* | Revert `job_worker.py:3572` (the derive read — finding #5's own path) | derive exact-delta bump assertion | ✅ observed | 03-SUMMARY F2: derive delta `assert (0 - 0) == 1` with *"released through the raw Lock, which has no release hook"*; ast pin named `('services/job_worker.py', 3606)` |
| **SC-03-2** the neuter control has teeth | `mt5_concurrency.py`: make `_mt5_terminal_lock_for` return a FRESH `asyncio.Lock()` per call (serializes nothing) | `test_mt5_derive_branch.py` `_run_two_concurrent_mt5` positive (serialization) arm — proves the re-pointed patch binds to the real registry | ✅ observed | 03-SUMMARY F5: the POSITIVE serialization arm red — *"two concurrent mt5 syncs interleaved … [('A','enter'), ('B','enter'), ('A','exit'), ('B','exit')]"*. ⭐ Plus the E2 trap caught live in the other direction: before the re-point the "neutered" run SERIALIZED (fully contiguous), i.e. the patch had silently become a no-op |
| **SC-03-3** class pinned shut | Introduce a raw `async with _mt5_terminal_lock_for(k):` in a **third** module (`services/ingestion/mt5.py`) | the ast class pin — proves the walk covers all of `services/`+`routers/`, not just the two edited files | ✅ observed | 03-SUMMARY F3: reported as `('services/ingestion/mt5.py', 444)` — the walk covers nested packages, not just the two edited files |
| SC-03-3 *(anti-vacuity)* | Narrow the pin's walk to a single directory | the ≥ 40-file floor, with the measured number in the failure message | ✅ observed | 03-SUMMARY F4: *"the walk scanned only 11 production files, below the hand-typed floor of 40 (88 were measured…)"* — `assert 11 >= 40` |
| **SC-04-1** transient dispositions | `routers/exchange.py`: delete the `except Mt5SessionAbandoned` arm | `test_mt5_validate.py` — 424/NETWORK_UNAVAILABLE becomes an unhandled 500 | ✅ observed | 04-SUMMARY row A (the RED commit): `Mt5SessionAbandoned` propagated out of `validate_key` past `pytest.raises(HTTPException)` — the unhandled bodyless 500 itself, with the sink's WARNING in the captured log confirming the fence produced it. Row A′: the same escape out of `run_derive_broker_dailies_job` and `fetch_allocator_holdings` |
| SC-04-1 *(no user-blame)* | `job_worker.py` derive arm: add the `_stamp_strategy_analytics_failed` call | derive spy assertion (the stamp must NOT be invoked) | ✅ observed | 04-SUMMARY row B: *"a login-bracket mismatch must NOT stamp a user-blame analytics row; upserts=['strategy_analytics']"* |
| SC-04-1 *(no self-harm)* | `job_worker.py` derive arm: call `_mt5_bounded_restart` from the refusal arm | derive case's restart spy — restarting someone else's terminal is exactly what this phase stops | ✅ observed | 04-SUMMARY row C: *"the refusal arm restarted the terminal — but a fence refusal MEANS the terminal was handed on … assert [<Mt5Client…>] == []"* |
| SC-04-1 *(copy contract)* | `allocator_positions.py`: raise a permanent error instead of `AllocatorHoldingsSyncTransientError(MT5_UNREACHABLE_NOTE)` | holdings type + `str()` assertion | ✅ observed | 04-SUMMARY row E: `RuntimeError: mt5 holdings read refused: MT5 session abandoned…` out of `fetch_allocator_holdings` — the fixed-copy contract broken and the internal text on its way to the user-visible `sync_error` column |
| SC-04-1 *(token freedom)* | Put the word `terminal` into the derive arm's `error_message` | the token-freedom sweep — ⚠️ **Wave-0 gap: this assertion does not exist yet** (see Wave 0 note). Until it lands this row is **not caught** | ✅ observed — **the gap is CLOSED** | 04-SUMMARY row D: *"… contains the classifier token 'terminal' …"*. `test_the_abandoned_derive_error_message_carries_no_classifier_token` landed in plan 04 (this file opened the gap; plan 04 owned and closed it) and sweeps against the LIVE tables imported from `services/mt5_validation.py` |
| **SC-04-2** process_key cannot 500 | `routers/process_key.py`: remove the try/except at **`:1586`** (the second site, not the first) | that site's route test — body-shape assertion, not just the status int | ✅ observed — **and mirrored** | 04-SUMMARY rows F / F′: removing ONLY the `:1586` arm reds ONLY `test_sync_pipeline_abandoned_session_is_a_coded_transient_never_a_500`; removing ONLY the `:774` arm reds ONLY `test_validate_only_…`. ⭐ Each site's test reds for its own arm and only its own, so an instance fix at either is detectable |
| **SC-05-1** roster catches the forgotten guard | Add a new `Mt5Client` method touching `self._mt5` (e.g. `positions_get`) **without** `_assert_live` | `test_mt5_abandon_roster.py` exempt-set exact equality — the forgotten-guard class, caught with zero pin edits | ✅ observed | 05-SUMMARY row A: *"the session-touching methods WITHOUT the _assert_live() fence are ['__init__', 'close', 'positions_get', 'release'], expected exactly ['__init__', 'close', 'release']"* — caught with **zero pin edits**, which is the whole reason the file is an ast derivation rather than a list |
| SC-05-1 *(alias trap)* | Change `restart` to reach the session only via `stale = self._mt5` with no direct `self._mt5.<x>` call | the aliased-synthetic self-test must keep the predicate honest (it must still report `restart` as touching) | ✅ observed | 05-SUMMARY row B: narrowing the touch predicate to `self._mt5.<x>` call shapes reds **4 cases** — the aliased synthetic, the nested-closure synthetic, the touch floor, and the roster equality. The shipped predicate matches the ATTRIBUTE in any context, because `restart` (finding #5 itself) reaches the session only through the alias |
| **SC-05-2** runtime guard non-vacuous | Comment out the epoch comparison in `_assert_live` (the D-37 falsification, run once during development) | `test_mt5_abandon_fence.py` primary — the post-release touch lands in the ordered log; the `neuter=True` control stays green, which is the point | ✅ observed ×2 | 05-SUMMARY row G: *"a session touch landed AFTER the lease released: [('account_info', 1)] (full ordered log: [('initialize', 0), ('login', 0), ('<lease released>', 1), ('account_info', 1)])"* — **and the neutered control arm stayed green, which is the point.** Re-run by the verifier as M1. ⭐ Row I additionally falsified the CONTROL itself (disarming `neuter_fence=True` reds it), so the control is not decorative |
| SC-05-2 *(WEDGE-01)* | `mt5_concurrency.py`: make the lease's `finally` synchronously join the worker thread (the ⛔ REJECTED design) | ✏️ **CORRECTED — the oracle stated here was WRONG.** Shipped: the **release-window** assertion `release_ticks > 0 or release_elapsed_s < _ABANDON_BOUND_S` in `test_the_release_does_not_block_the_event_loop_on_the_zombie`. The planned form — the bare 10 ms loop-liveness ticker, "`ticks > 0` fails" — was **MEASURED green under this very mutation** and was replaced BECAUSE of that: the ticker has already accumulated ticks during the abandonment bound's own 50 ms, so `ticks > 0` never reaches the release and stays green under the design D-36 ⛔ REJECTED. The bare `ticks > 0` survives ONLY as a separate, explicitly weaker secondary assertion (it proves the session work runs off the loop at all), with its own message and a docstring stating in full why it is not the discriminator | ✅ observed ×2 — **against the corrected oracle** | 05-SUMMARY row H, and independently the verifier's M3 (`time.sleep(0.25)` in the lease's `finally`): *"releasing the lease blocked the event loop for 0.303s and the 10ms ticker advanced 0 times across it"*. ⛔ **Had the ledger's original oracle shipped, this mutation would have stayed GREEN** — a guard that cannot see its own mutation is not a guard, the same defect class SC-02-1's live-heal case hit. ℹ️ Row H also redded the guard-#16 case on its scheduling gate (*"the zombie's barrier TIMED OUT instead of being released by the test"*) — correct behaviour (the harness reports "this run proves nothing" rather than misreporting a scheduling failure as a verdict), but it means H is cleanly attributable only because the liveness case names its actual measurement |
| SC-05-3 shutdown roster docstring | *(prose — docstring-only, zero pins move)* | **not falsifiable**; covered by the Manual-Only diff review (assertions must be byte-identical) | ⬜ n/a | prose — **discharged by proof, not by inspection.** Both revisions were parsed, every docstring stripped, re-parsed and `ast.dump`ed: **identical**. Zero pins moved, zero assertions changed |

**Falsifications observed during execution that this ledger has NO row for** (recorded so the count is
honest — the ledger was written at planning and could not anticipate them): 01-SUMMARY F9
(`begin_mt5_lease_occupancy → set(None)` reds both the occupancy-lifecycle and the frozen-copy case,
so the frozen-copy oracle is not vacuous); 04-SUMMARY F′ (the `:774` mirror) and G (the adapter arm
falsified against the FUTURE broad-`except` it was written for); 05-SUMMARY C (touch predicate
collapsed to `False` — reds the hand-typed touch floor), D (guard predicate forced `True` — reds the
self-test the roster's whole credibility rests on), E (`restart`'s inline comparison deleted), F
(`__init__` stops reading `_MT5_LEASE_OCCUPANCY`), I (the negative control itself disarmed); plus
03-SUMMARY's measured E2 patch-target correction (epoch delta 1 / 1 / 0 across
unpatched / owner-patched / reader-patched).

**Ledger tally at reconciliation — 36 rows:** 32 observed ✅ · 3 recorded n/a (2 prose + **1 honest
gap**, `SC-01-2`'s release-ordering swap, which stays a gap) · **1 unobserved**, `SC-01-1`'s second
member (`_raise_last`), marked as such. One of the 32 is observed only in PART — `SC-01-5`'s contract
half red, its roster half never re-run after plan 05 landed. ⛔ Neither the unobserved row nor the
unrun half is counted as caught.

*Rules:*
- **Observed means run.** "The test covers it" is not evidence. Paste the failing assertion.
- **A mutation that is skipped** (ambiguous anchor, unreachable) is recorded as skipped, **never as caught**.
- **Prefer the second member of a class.** Four rows above (SC-01-1 `_raise_last`, SC-03-1
  `allocator_positions`, SC-04-2 site `:1586`, SC-03-3 a third module) exist specifically to
  detect instance-fixes masquerading as class-fixes — the mistake this milestone has paid for
  sixteen times.
- ⭐ **The two half-fix rows are the most important in this file.** SC-02-1 (entry check only)
  and SC-02-2 (pre-connect check only) are exactly the shapes a reviewer would sign off on.
  If either mutation stays green, the phase has shipped a fence that does not fence.

---

## Oracle Independence

> The failure this catches: assertions that read their expected value out of the module under
> test, so the test passes for any implementation. This project has already paid for it —
> three money bugs survived six review passes behind self-referential oracles.

| Oracle | What it pins | INVARIANT, not the impl's own formula — how we know |
|--------|--------------|------------------------------------------------------|
| Fake-session **call log** (plan 01 T2, plan 05 T2) | "No session touch landed after the lease released" | The oracle is the **absence of an effect on a test-owned double**, not the exception type the production code chose to raise. It stays true if `Mt5SessionAbandoned` is renamed, re-typed, or replaced — and it goes false the moment a touch lands, which is the economic harm itself |
| Ordered log **index** (plan 05 T2) | Post-release ordering | Index into a recorded list, ⛔ never `at_time` / wall clock. A wall-clock comparison is a sleep-ordered race wearing a different hat and would pass or fail on scheduler luck rather than on the property |
| Epoch **exact delta** (plan 03 T2) | "One lease release ⇒ exactly one bump" | The expected delta is a **hand-typed integer** matching the scenario's known hold count — not `len(holds)` and not a counter the production path increments. A "changed / non-zero" assertion would pass for a double-bump, which would fence a legitimate successor |
| `_WRONG_SERVER_TOKENS` / `_AUTH_TOKENS` sweep (plan 01 T2, plan 04) | "The refusal message and the classifier's tables are disjoint" | ⚠️ **Deliberate cross-module import** — the test imports the LIVE tables from `services/mt5_validation.py`. Justified: the invariant is disjointness from *whatever the classifier actually matches on today*; a hand-copied table would go stale and pass while the real classifier gained a token. It is not self-referential, because the table lives in a **different** module from the one under test (`mt5_client.py`) |
| `GUARDED_METHOD_FLOOR = 7` (plan 05 T1) | Roster non-vacuity | A **hand-typed literal**, asserted `>=` so a newly fenced method passes. ⛔ Never `len(EXPECTED_METHODS)` — a list compared to its own length cannot fail |
| `SESSION_TOUCH_FLOOR` (plan 05 T1) | Walk non-collapse | ⭐ Must be **measured once while writing the test and hand-typed as `(measured − 1)`** — see the plan's own wording. A floor derived at execution time from the same walk it guards is the unfailable self-referential floor `test_mt5_shutdown_roster.py:33-37` forbids |
| ast walk **file-count floor ≥ 40** (plan 03 T2) | Class-pin non-vacuity | Hand-typed, with the measured HEAD number in the failure message. A pin over zero files passes silently |
| Roster **self-tests** (plan 05 T1) | The predicate itself | ⭐ The unguarded synthetic is load-bearing: without it, a predicate returning "guarded" unconditionally is indistinguishable from a correct one. Plus the prose-ignored flavour (the thing grep cannot do) and the aliased `stale = self._mt5` flavour |
| Embedded `neuter=True` control (plan 05 T2) | The positive assertion can fail | Both directions live permanently in one file — the positive is never trusted without its negative |
| **Real lease** in the live-path proof (plan 02 T2) | Mutual exclusion, not the fence's logic | The test drives the shipped `mt5_terminal_lease`, so the oracle is "an exclusive lock means the epoch cannot move while held" — a property of the lock, independent of anything `_assert_live` computes |
| Open/close **balance** (plan 02 T3) | "No conn remains open" | Balance, not "the post-connect arm fired". Which arm the zombie takes depends on where its thread was when the bump landed; a per-arm oracle would flake on a run where the leak property genuinely held |
| Response **body** (plan 04) | The user-facing contract | Status + `code` + retry semantics asserted from the response, never from the arm's source. A source-level assertion would pass for an arm that raises but never reaches the serializer |

Checklist:

- [x] No test imports a **constant** from the module it tests — expected values are **literals** in the test
      *(the two deliberate exceptions are listed below and both are cross-module: the live token
      tables, and the epoch registry read, which MUST go through its owner `services.mt5_client` —
      that is the E2 trap, not a self-reference)*
- [x] No assertion compares a value to itself via a re-export, fixture, or table under test
      — ⭐ enforced the hard way in plan 03: the bump no-op's patch target was **MEASURED** across
      unpatched / owner-patched / reader-patched (deltas 1 / 1 / 0) rather than trusted from the
      plan's text, which named the silent-no-op target
- [x] Roster/registry sizes are pinned to a **hand-typed literal**, never `len(THE_TABLE)` — all
      three floors carry an explicit ⛔ comment forbidding the `len(derived) - 1` form
- [x] `SESSION_TOUCH_FLOOR` is hand-typed `(measured − 1)`, with the measured number in the failure
      message — `= 9` against a measured 10; the red reads *"…(hand-typed floor 9, MEASURED 10 …)"*
- [x] `_FakeMt5` and the new spy double are pinned against the real contract they stand in for
      (`test_public_surface_is_exactly_the_contract` must stay green **unchanged** — if it reds,
      the production shape is wrong; ⛔ do not re-cut the pin) — **verified by diff**: the pin is
      untouched across the whole phase range (`2c6d8c9d..HEAD`), and `_SpyMt5` subclasses the
      shipped `_FakeMt5` rather than re-implementing it
- [x] Every fence assertion has an **effect-based** oracle (the fake's log), not merely "the
      exception type was raised" — and the effect assertions are ordered BEFORE the type
      assertions, so a half-fix reds on the HARM. ⚠️ One measured caveat: in the plan-01 contract
      case the falsification's red still surfaced at `pytest.raises` first, so its two oracles were
      never observed red in the same run (the runtime guard's red, `[('account_info', 1)]`, is the
      clean one)
- [x] The epoch-registry read in tests goes through `services.mt5_client` (the OWNER), never a
      re-export — the E2 silent-no-op trap. Pinned in both directions:
      `test_the_epoch_registry_is_not_re_exported_anywhere` keeps the re-export from existing at
      all, and plan 03 measured the reader-vs-owner distinction rather than assuming it

*Deliberate self-referential oracles, and what independently covers them:*
1. The **token-table import** (above) — deliberate, because the live table IS the mechanism.
   Independently covered by `mt5_validation`'s own classifier tests.
2. The **ast rosters** derive their expected set from the same source file they guard — that is
   the mechanism, not a bug. Protected from vacuity by the hand-typed floors, the exact-equality
   exempt set, and the unguarded-synthetic self-test. All three must move together or the
   derivation can silently match nothing.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or a Wave 0 dependency
- [x] Sampling continuity: no 3 consecutive tasks without an automated verify
- [x] Wave 0 covers every `❌ W0` row — **12 bullets**; the token-freedom bullet was added by
      this file (plan 04 Task 1 owns it) — **all twelve landed**
- [x] No watch-mode flags anywhere
- [x] Feedback latency < 30s
- [x] **Every success criterion has a Falsifiability Ledger row** (prose-only criteria are
      recorded as not-falsifiable with a named manual reviewer, never as caught)
- [⚠️] **Every ledger row is `Observed ✅` with pasted evidence, or explicitly marked
      skipped/gap-with-reason** — in particular the two ⭐ half-fix rows (SC-02-1, SC-02-2).
      ⭐ **Both half-fix rows red on the load-bearing oracle** (02-SUMMARY A and E, each re-run
      independently by the verifier as M2 and M5) — the phase did not ship a fence that does not
      fence. ⚠️ **Not fully met:** `SC-01-1`'s second member (`_raise_last`) was never neutered, and
      `SC-01-5`'s roster half was never re-run after plan 05 landed. Both are marked unobserved in
      the ledger rather than ticked
- [x] **Oracle Independence checklist complete** — see the box below; the one item that could not be
      confirmed from artifacts is marked there
- [x] `tests/test_mt5_abandon_fence.py` green on **three consecutive runs** — 2 passed each
      (2.59s / 2.16s / 2.40s), no timing sensitivity
- [x] `make typecheck` clean (⚠️ pytest-only is how mypy errors reach PR CI latent) — 90 source
      files, plus `mypy --strict --follow-imports=silent services/ingestion/` over 13
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** reconciled 2026-08-11 (post-execution, post-verification — closing warning
`W-153.5-2`). ⛔ This is a RECORD reconciliation, not a re-verification: every ✅ cites a test found
in the tree or a red observed and pasted by the plan that ran it. Two ledger rows and one honest gap
remain explicitly NOT caught, and one ✏️ correction (SC-05-2 / WEDGE-01) means the ledger no longer
instructs a reader to trust an oracle that was measured unable to see its own mutation.
