---
phase: 153.5-wizform-abandon
plan: 03
type: execute
wave: 2
depends_on: [153.5-01]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/services/allocator_positions.py
  - analytics-service/tests/test_mt5_derive_branch.py
  - analytics-service/tests/test_allocator_positions_non_ccxt.py
  - analytics-service/tests/test_mt5_concurrency.py
autonomous: true
requirements: [ABANDON-05]
must_haves:
  truths:
    - "Every production terminal acquisition goes through mt5_terminal_lease — so EVERY release bumps the epoch, including the worker paths finding #5 actually runs on"
    - "The derive job's release advances the terminal epoch (observed via _mt5_epoch_for after a job completes) — without this, plan 02's restart fence never fires on the path that motivated it"
    - "The neuter negative control in the derive concurrency test still reds when the lock is neutered — its monkeypatch was re-pointed at the module the lease actually READS (the E2 silent-no-op trap)"
    - "No production module can quietly reintroduce a raw async-with over _mt5_terminal_lock_for — an ast pin (comments invisible) reports any such site"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "sync-balance (:364) and derive-read (:3572) blocks acquire via mt5_terminal_lease"
      contains: "mt5_terminal_lease"
    - path: "analytics-service/services/allocator_positions.py"
      provides: "holdings read (:656) acquires via mt5_terminal_lease"
      contains: "mt5_terminal_lease"
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "services.mt5_concurrency.mt5_terminal_lease"
      via: "import added to the existing mt5_concurrency import block (:125-133)"
      pattern: "mt5_terminal_lease"
---

<objective>
Make "the lease bumps on release" TRUE for the paths that matter. At HEAD, three of the
five production terminal acquisitions bypass `mt5_terminal_lease` and use the raw Lock
directly — `services/job_worker.py:364` (sync balance), `services/job_worker.py:3572`
(the derive read — finding #5's OWN path), `services/allocator_positions.py:656`
(holdings). RESEARCH §Q3 / PATTERNS §Artifact 1 claimed the lease `finally` "is the ONE
release point for all five call sites"; source shows it is 2 of 5. If the bump lives
only in the lease, the derive path never bumps, the zombie restart's second check reads
an unchanged epoch, and finding #5 stays open with every test green.

⭐ Deviation from research, stated per the fidelity rule: this plan makes the research's
claim true by converting the three raw sites to the lease, rather than hand-rolling a
bump beside three raw releases (the drift shape D-35 exists to end). This is NOT a
violation of D-36's "zero call-site edits" — that phrase describes the fence; these
edits change HOW the same lock is acquired, not what any call does. The shutdown
roster's `_LEASE_NAMES` already accepts both forms (`test_mt5_shutdown_roster.py:107`),
so every lexical lease proof stays green.

Purpose: ABANDON-05's precondition — no release path that skips the bump.
Output: three converted call sites, re-pointed test patches, an ast pin closing the
class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153.5-wizform-abandon-work-that-outlives-its-timeout/153.5-CONTEXT.md

Targeted reads (one pass each):
- `analytics-service/services/job_worker.py:105-135` (the mt5_concurrency import block and its ⚠️ 151-review-E2 comment — a re-cut target), `:352-380` (sync-balance block), `:3560-3600` (derive read block).
- `analytics-service/services/allocator_positions.py:80-95` (import block), `:640-700` (holdings read block).
- `analytics-service/tests/test_mt5_derive_branch.py:917-1045` (`_OrderedMt5Transport`, `_run_two_concurrent_mt5` incl. the `neuter_lock` patch at ~:988-996 targeting `services.job_worker._mt5_terminal_lock_for`).
- `analytics-service/tests/test_mt5_concurrency.py:58-100` (`test_registry_object_is_shared_across_modules` — identity pins to extend).
- `analytics-service/services/mt5_concurrency.py:208-273` — the lease signature only (`wait_s: float | None = None`; None = unbounded acquire, identical semantics to the raw `await lock.acquire()`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the three raw-lock acquisitions to mt5_terminal_lease</name>
  <files>analytics-service/services/job_worker.py, analytics-service/services/allocator_positions.py</files>
  <action>
1. `services/job_worker.py:364` and `:3572`: replace
   `async with _mt5_terminal_lock_for(<key>):` with `async with mt5_terminal_lease(<key>):`
   — no `wait_s` argument (the worker must wait unboundedly, exactly as the raw acquire
   does today; ⛔ never give the worker the bounded arm — `Mt5TerminalBusyError` is the
   INTERACTIVE path's contract, D-29). Add `mt5_terminal_lease` to the existing
   `from services.mt5_concurrency import (...)` block at :125-133. Keep every existing
   name in that import (tests pin `jw._mt5_terminal_lock_for is ...` identity —
   removing a re-export reds `test_registry_object_is_shared_across_modules`).
2. Re-cut the ⚠️ 151-review-E2 comment above that import block (:113-124): it currently
   states job_worker READS `_mt5_terminal_lock_for` at its own call sites — after this
   change job_worker reads `mt5_terminal_lease` instead, and `_mt5_terminal_lock_for`
   joins the re-export-only set, which flips which module a monkeypatch must target.
   State the new read/re-export split explicitly (this comment is the E2 trap's only
   in-file map; leaving it stale converts it from a guard into a trap).
3. `services/allocator_positions.py:656`: same conversion; add `mt5_terminal_lease` to
   the import block at :83-87, keeping `_mt5_terminal_lock_for` if other references
   remain (grep the file; the identity test also pins names here).
4. Each converted site gets a one-line comment: WIZFORM-ABANDON / D-36 — acquisition
   moved to the lease so RELEASE BUMPS THE EPOCH; the raw Lock has no release hook.
5. Behavior invariants to preserve, verbatim: what is inside vs outside each block
   (post-read work stays OUTSIDE the lease — the existing comments say why); the
   `except (asyncio.TimeoutError, Exception)` catch-all around the sync-balance block;
   the derive block's TimeoutError arm calling `_mt5_bounded_restart` INSIDE the held
   lease (that placement is what makes the legitimate heal pass the entry check).
Run `cd analytics-service && make typecheck`.
  </action>
  <verify>
    <automated>cd analytics-service && python -c "
import ast, pathlib
bad = []
for f in ('services/job_worker.py','services/allocator_positions.py'):
    tree = ast.parse(pathlib.Path(f).read_text())
    for node in ast.walk(tree):
        if isinstance(node, ast.AsyncWith):
            for item in node.items:
                c = item.context_expr
                if isinstance(c, ast.Call) and getattr(c.func, 'id', getattr(c.func, 'attr', '')) == '_mt5_terminal_lock_for':
                    bad.append((f, node.lineno))
assert not bad, bad
print('zero raw async-with lock acquisitions (was 3 at HEAD)')" && .venv/bin/pytest tests/test_mt5_derive_branch.py tests/test_allocator_positions_non_ccxt.py -q && make typecheck</automated>
  </verify>
  <done>Zero `async with _mt5_terminal_lock_for(...)` in the two modules (ast, not grep — both files contain the phrase in comments); both worker suites green; imports/identity pins intact; mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-point the neuter patches, prove the worker release bumps, pin the class closed</name>
  <files>analytics-service/tests/test_mt5_derive_branch.py, analytics-service/tests/test_allocator_positions_non_ccxt.py, analytics-service/tests/test_mt5_concurrency.py</files>
  <behavior>
    - `_run_two_concurrent_mt5(neuter_lock=True)` still produces the interleave its assertion demands — after re-pointing its patch from `services.job_worker._mt5_terminal_lock_for` to `services.mt5_concurrency._mt5_terminal_lock_for` (the module `mt5_terminal_lease` actually reads). ⭐ Self-detecting: if the re-point is wrong, the patch is a no-op, real locking serializes, the interleave assertion itself reds — run it and OBSERVE green, then note in the SUMMARY why green here means the patch bound.
    - Worker release bumps (the ABANDON-05 precondition, pinned): after a derive-branch scenario completes, `_mt5_epoch_for(<terminal key>)` (imported from `services.mt5_client`, the owner) has advanced by exactly the number of lease holds the scenario performed. Assert a concrete delta, not merely "changed".
    - Same one-hold bump assertion on the allocator holdings path in test_allocator_positions_non_ccxt.py.
    - Identity extension in test_mt5_concurrency.py: `jw.mt5_terminal_lease is mt5_concurrency.mt5_terminal_lease` (and the allocator's binding), beside the existing pins.
    - Class pin (ast, with teeth): a test in test_mt5_concurrency.py that walks every production `*.py` under `services/` and `routers/` (path + ast.parse, never inspect.getsource; exclude `mt5_concurrency.py` itself, whose lease legitimately calls `_mt5_terminal_lock_for` as a plain call, not an async-with) and asserts NO `AsyncWith` item's context expression is a call to `_mt5_terminal_lock_for`. Anti-vacuity: assert the walk scanned ≥ 40 files (measured at HEAD: services/ + routers/ hold well over that; state the measured number in the failure message). Self-test: `scan_source` driven against a synthetic snippet containing a raw `async with _mt5_terminal_lock_for(k):` MUST report it, and against the same text inside a docstring MUST NOT.
  </behavior>
  <action>
Grep both worker test files for every `patch(` / `monkeypatch.setattr` naming
`_mt5_terminal_lock_for` and re-point each whose reader moved (the E2 rule: patch the
module that READS the name). Expected at HEAD: the `neuter_lock` patcher in
`_run_two_concurrent_mt5` (~:993). Update its comment to name the new reader. Then add
the four behaviors above. For the epoch-delta assertions, compute the terminal key by
calling the shipped `Mt5Client.terminal_key` property shape via the existing
`_expected_terminal_key`-style helper — never retype the host:port format.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/pytest tests/test_mt5_derive_branch.py tests/test_allocator_positions_non_ccxt.py tests/test_mt5_concurrency.py -q && make typecheck</automated>
  </verify>
  <done>Neuter control observed red-capable (its interleave assertion passes only because the re-pointed patch binds); worker releases provably bump with exact deltas; the raw-acquisition class is pinned shut by an ast test with a self-test and a stated floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker release path → epoch registry | a release that skips the bump silently disarms every fence downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.5-09 | Tampering | derive/holdings release paths | mitigate | all acquisitions through the lease; exact-delta bump tests |
| T-153.5-10 | Repudiation | future raw-lock reintroduction | mitigate | ast class pin with self-test + floor |
| T-153.5-11 | DoS | worker wedged by bounded lease arm | mitigate | conversions pass no `wait_s`; D-29 boundary stated in comments |
</threat_model>

<verification>
cd analytics-service && .venv/bin/pytest tests/test_mt5_derive_branch.py tests/test_allocator_positions_non_ccxt.py tests/test_mt5_concurrency.py tests/test_job_worker.py -q && make typecheck
</verification>

<success_criteria>
- All five production terminal acquisitions release through `mt5_terminal_lease`; epoch bumps proven with exact deltas on both worker paths.
- The E2 patch-target map (comment + patches) matches the new reader reality; neuter control demonstrably still has teeth.
- The raw-acquisition class cannot silently return (ast pin).
</success_criteria>

<output>
Create `.planning/phases/153.5-wizform-abandon-work-that-outlives-its-timeout/153.5-03-SUMMARY.md` when done.
</output>
