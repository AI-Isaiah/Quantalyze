---
phase: 153.5-wizform-abandon
plan: 05
type: execute
wave: 3
depends_on: [153.5-01, 153.5-02]
files_modified:
  - analytics-service/tests/test_mt5_abandon_roster.py
  - analytics-service/tests/test_mt5_abandon_fence.py
  - analytics-service/tests/test_mt5_shutdown_roster.py
autonomous: true
requirements: [ABANDON-05, ABANDON-06, ABANDON-07]
must_haves:
  truths:
    - "The ast roster derives the session-touching method set from source and asserts every non-exempt member carries the guard — a method reaching self._mt5 by a NEW verb is caught with zero pin edits (D-36 static complement)"
    - "The runtime barrier test (guard #16 / D-37) proves the temporal property the roster structurally cannot: a zombie's post-release touch is refused, with the abandonment SCHEDULED by a test-owned Event, never by the scheduler"
    - "The runtime test carries an embedded negative control: with the fence neutered the post-release touch DOES land — the positive assertion is provably non-vacuous"
    - "test_mt5_shutdown_roster's lease-holding case names its own ceiling (lexical) and cross-references the runtime guard — it no longer claims to be 'the ONLY thing standing between' the permitted shutdown and -10004"
  artifacts:
    - path: "analytics-service/tests/test_mt5_abandon_roster.py"
      provides: "ast-derived guard roster with hand-typed floors, exempt-set equality, and three-flavour self-tests"
      min_lines: 150
    - path: "analytics-service/tests/test_mt5_abandon_fence.py"
      provides: "D-37 barrier + fake-session spy runtime guard, negative control, loop-liveness ticker"
      min_lines: 120
  key_links:
    - from: "analytics-service/tests/test_mt5_abandon_fence.py"
      to: "services.mt5_concurrency.mt5_terminal_lease + services.mt5_client.Mt5Client"
      via: "a real client over the _connect= seam, driven through a real lease with a tiny local wait_for"
      pattern: "mt5_terminal_lease"
---

<objective>
The two guards, each answering exactly one question (CONTEXT's boundary — do not blur
them): the ast roster asks "does every session-touching method CARRY the check?"
(static, catches the forgotten guard on next month's new method); the runtime barrier
asks "is the check ENFORCED when the lease has moved on?" (temporal, the thing lexical
analysis reads as green — `test_every_restart_call_site_holds_the_terminal_lease` is
green on finding #5 today by construction). Plus the shutdown-roster docstring re-cut
that stops overclaiming.

Purpose: without the roster the guard is correct today and forgotten by the next method
written; without the runtime assertion the whole phase's property is untested at the
only level where it exists.
Output: two NEW test files + one docstring re-cut.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153.5-wizform-abandon-work-that-outlives-its-timeout/153.5-CONTEXT.md

Targeted reads (one pass each):
- `analytics-service/tests/test_mt5_shutdown_roster.py` — read in full ONCE (668 lines); it is the skeleton to copy section-by-section: module docstring "why ast never regex" + never-inspect.getsource (:1-38), hand-typed pins with WHY (:46-121, esp. the derived-never-typed-twice `_TEARDOWN_NAMES` comment :113-119), the NodeVisitor + outermost-def rule (:151-263), `scan_source` exposed for self-tests (:266-271), anti-vacuity messages spelling the predicate (:306-339), three-flavour self-tests (:475-667), and the re-cut target `test_every_restart_call_site_holds_the_terminal_lease` (:393-417).
- `analytics-service/services/mt5_client.py` — post-plan-02 state: the fenced methods, `_assert_live`, the restart second check, the `__init__` occupancy participation (read to derive the roster's expected sets — do not guess them from this plan).
- Runtime-test analogs: `tests/test_mt5_derive_branch.py:917-1045` (`_OrderedMt5Transport` bounded-Event idiom + `_run_two_concurrent_mt5(neuter_lock=...)` embedded negative control + the loop-liveness ticker at :825-851), `tests/test_mt5_client_contract.py` `_FakeMt5:99`/`_make:190` (the `_connect=` seam), `tests/test_mt5_concurrency.py:48-56` (autouse reset — reuse `reset_terminal_state_for_tests`).
- Rationale on demand: `153.5-PATTERNS.md` §Artifact 4 (roster: predicate, alias trap, floors) and §Artifact 5 (barrier test: what to copy/change/trap). Consult by section.
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_mt5_abandon_roster.py — the static complement</name>
  <files>analytics-service/tests/test_mt5_abandon_roster.py</files>
  <action>
NEW file, copying the shutdown roster's skeleton with these deltas:

1. Scope: ONE file — `services/mt5_client.py`, `ClassDef Mt5Client` — read by path +
   `ast.parse` (⛔ never `inspect.getsource` on an imported module; the :28-31 reason
   carries over verbatim).
2. Predicate "touches the session": the method's body walk (nested closures included)
   contains an `ast.Attribute(value=ast.Name('self'), attr='_mt5')` node — Load OR
   Store. ⚠️ The alias trap: `restart` reaches the session via `stale = self._mt5` then
   `stale.shutdown()` — the RHS of that assignment IS such a node, so the predicate
   catches it; add a self-test with exactly that aliased synthetic so a regression in
   the predicate (e.g. someone narrowing it to `self._mt5.<x>` call shapes) reds.
3. Guard predicate: the method carries a call to `_assert_live` among its first
   statements, OR is `restart` (whose entry check sits in its own body pre-`_timed` and
   whose second check is an inline epoch comparison — assert BOTH facts for restart:
   the `_assert_live` call AND an inline `_mt5_epoch_for` comparison inside the nested
   def, positionally before the `shutdown` call node).
4. Hand-typed pins, each with its WHY comment:
   - `EXEMPT: frozenset = {"__init__", "close", "release", "_teardown_transport"}` —
     asserted with EXACT equality against (touching − guarded), D-41's list: `__init__`
     is covered by the construction fence (assert separately that its body references
     `_MT5_LEASE_OCCUPANCY` — the static complement for mechanism (ii)); the three
     teardown verbs touch only OUR socket since D-35 and MUST stay reachable
     post-release (fencing them re-opens Pitfall 6 — write this sentence in the
     comment so nobody "fixes" the exemption).
   - `GUARDED_METHOD_FLOOR = 7` (login, account_info, terminal_info,
     history_deals_get, order_check, _raise_last, restart — the measured post-plan-02
     count; assert `>=`, so a new fenced method passes, and a hand-typed list is never
     compared to its own len).
   - `SESSION_TOUCH_FLOOR`: derive the measured count of touching methods at execution
     time, floor it BELOW by one, and spell the predicate in the failure message
     (anti-vacuity — a collapsed walk must red, not pass empty).
5. Three-flavour self-tests via an exposed `scan_source(source)`: (a) a synthetic
   guarded method is reported guarded; (b) ⭐ a synthetic method touching `self._mt5`
   WITHOUT the guard is REPORTED — without this the roster is indistinguishable from a
   predicate returning "guarded" unconditionally; (c) the verbs in prose (docstring +
   comment + string literal) are ignored — the thing a grep could not do. Plus the
   aliased synthetic from (2).
6. Module docstring: ONE question ("does every session-touching method CARRY the
   check?"), the two-guards table (this file CANNOT catch abandonment and must not be
   asked to — no to_thread/wait_for proximity heuristics, ever), cross-reference
   `tests/test_mt5_abandon_fence.py` by name.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/pytest tests/test_mt5_abandon_roster.py -x -q</automated>
  </verify>
  <done>Roster derives 7+ guarded methods from source; exempt set exact-equal to D-41's four; all self-tests (incl. unguarded-synthetic and aliased-synthetic) pass; no heuristic reaches beyond the lexical question.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_mt5_abandon_fence.py — guard #16, the runtime barrier + spy; shutdown-roster re-cut</name>
  <files>analytics-service/tests/test_mt5_abandon_fence.py, analytics-service/tests/test_mt5_shutdown_roster.py</files>
  <behavior>
    - Primary (D-37's sequence, verbatim): a fake session whose `login` blocks on a test-owned `threading.Event` (bounded `.wait(0.25)`); a harness coroutine takes `mt5_terminal_lease(key)` and runs the probe body via `asyncio.wait_for(asyncio.to_thread(...), timeout=0.05)` (a LOCAL tiny timeout — no module constant patched, so no patch-target trap); the `wait_for` fires → harness unwinds → lease releases → epoch bumps (assert `_mt5_epoch_for(key)` advanced); the test then sets the Event → the zombie proceeds → the zombie's recorded exception is `Mt5SessionAbandoned`, and the fake's ordered touch log shows NO session touch at an index after the release marker (order by INDEX into the log, never `at_time` — a wall-clock comparison is a sleep-ordered race wearing a different hat).
    - Embedded negative control, same harness parametrized `neuter=True`: monkeypatch `Mt5Client._assert_live` to a no-op → the post-release touch DOES land in the log — proving the primary assertion non-vacuous. Both directions in one file, permanently.
    - Loop-liveness ticker (the WEDGE-01 proof): a 10ms ticker coroutine accumulates ticks throughout; assert ticks > 0 — the accepted fix did not quietly become the rejected hold-the-lease-until-the-thread-confirms design.
    - Zombie thread accounting: the zombie signals its own completion Event; the test joins on THAT (bounded), and every gate is set in a finally — a mis-ordered test reds in <1s, never stalls the 300s THREAD_JOIN_TIMEOUT.
  </behavior>
  <action>
NEW file `tests/test_mt5_abandon_fence.py` (asyncio_mode=auto — no markers). Build the
spy fake by subclassing/adapting the existing `_FakeMt5` shape rather than inventing a
new double; record `(method, epoch_seen)` tuples into a plain list (GIL-atomic append —
read it only after the zombie's done-Event round-trip). Drive a REAL `Mt5Client` via
the `_connect=` seam so the object under test is the shipped class. Use
`reset_terminal_state_for_tests()` in an autouse fixture. Module docstring: the other
half of the two-guards table, cross-referencing the roster file; state guard #16's
question ("is the check ENFORCED when the lease has moved on?") and D-43's honest limit
(a call already on the wire cannot be un-sent — this test proves the post-release
REFUSAL, which is the common case).

Falsification beyond the embedded control (D-37: observe the red, do not predict it):
once during development, comment the epoch comparison in `_assert_live`, run the
primary test, OBSERVE the red, restore, re-run green. Record in SUMMARY.

Then re-cut `tests/test_mt5_shutdown_roster.py:393-417`
(`test_every_restart_call_site_holds_the_terminal_lease`'s docstring): it currently
claims to be "the ONLY thing standing between the permitted shutdown() and the exact
cross-tenant -10004". Re-cut per the D-35 template — name its own ceiling (its
enclosure proof is LEXICAL; it is green on the abandonment case by construction, which
is the ceiling of static analysis, not a bug), name the runtime guard file that now
answers the temporal half, and keep every assertion unchanged. ⛔ Docstring-only edit —
zero pins move in that file.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/pytest tests/test_mt5_abandon_fence.py tests/test_mt5_shutdown_roster.py -x -q && for i in 1 2 3; do .venv/bin/pytest tests/test_mt5_abandon_fence.py -q || exit 1; done</automated>
  </verify>
  <done>Primary + negative control + ticker green, three consecutive runs green (no timing sensitivity); the manually-neutered red was observed and noted; shutdown roster docstring re-cut with zero assertion changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future code change → fence coverage | a new session-touching method or a weakened check shipping unguarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.5-15 | Tampering | fence erosion over time | mitigate | ast roster with unguarded-synthetic self-test (Task 1) |
| T-153.5-16 | Repudiation | vacuous runtime guard | mitigate | embedded neuter control + observed manual red (Task 2) |
| T-153.5-17 | DoS | CI stalled by an unbounded Event | mitigate | bounded waits + finally-set gates + triple-run verify |
</threat_model>

<verification>
cd analytics-service && .venv/bin/pytest tests/test_mt5_abandon_roster.py tests/test_mt5_abandon_fence.py tests/test_mt5_shutdown_roster.py tests/test_mt5_client_contract.py tests/test_mt5_concurrency.py -q && make typecheck
</verification>

<success_criteria>
- Both guards exist, each answering only its own question, cross-referenced both ways.
- The runtime guard is deterministic (Event-scheduled), bounded (reds, never hangs), and provably non-vacuous (embedded control + observed red).
- The shutdown roster no longer overclaims; zero of its pins moved.
</success_criteria>

<output>
Create `.planning/phases/153.5-wizform-abandon-work-that-outlives-its-timeout/153.5-05-SUMMARY.md` when done.
</output>
