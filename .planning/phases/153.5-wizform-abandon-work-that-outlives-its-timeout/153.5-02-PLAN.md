---
phase: 153.5-wizform-abandon
plan: 02
type: execute
wave: 2
depends_on: [153.5-01]
files_modified:
  - analytics-service/services/mt5_client.py
  - analytics-service/services/mt5_concurrency.py
  - analytics-service/tests/test_mt5_client_contract.py
  - analytics-service/tests/test_mt5_validate.py
  - analytics-service/tests/test_ingestion_mt5.py
autonomous: true
requirements: [ABANDON-05, ABANDON-06]
must_haves:
  truths:
    - "A restart whose lease released mid-flight NEVER reaches stale.shutdown() — and on that fenced path the stale socket is still closed, the fresh WR-01 socket is disposed, and self._closed is restored to True (the three invariants no existing test covers together)"
    - "A restart that completes within its bound under a live lease is UNCHANGED — the heal path is not broken (the epoch cannot move while the lease is held)"
    - "A connect-stage zombie (#6a router / #6b worker) that constructs an Mt5Client after its caller's lease released disposes its own just-opened transport and raises — the rpyc session no longer leaks"
    - "A client constructed in preflight with NO lease held carries no occupancy token and is exempt from the construction fence — structurally, not via a call-site opt-in (D-36 AMENDED, RESEARCH Open Q-1 RESOLVED)"
    - "A construction under a genuinely held lease cannot raise Mt5SessionAbandoned — proven by test, so exchange.py's broad connect except (:536) can never mint a breaker-keying 503 from the fence"
  artifacts:
    - path: "analytics-service/services/mt5_client.py"
      provides: "restart entry check + pre-shutdown second check with three-invariant cleanup; __init__ occupancy participation (pre-connect refusal, post-connect teardown-then-raise)"
      contains: "_assert_live(\"restart\")"
  key_links:
    - from: "Mt5Client.restart/_reconnect_then_dispose_stale"
      to: "_mt5_epoch_for"
      via: "second comparison immediately before stale.shutdown() (mt5_client.py:892 at HEAD)"
      pattern: "_mt5_epoch_for"
    - from: "Mt5Client.__init__"
      to: "_MT5_LEASE_OCCUPANCY"
      via: "reads the frozen occupancy tuple asyncio.to_thread copied into the zombie thread"
      pattern: "_MT5_LEASE_OCCUPANCY"
---

<objective>
Close findings #5 and #6 at the sink. #5: `restart()` gets its TWO checks — the entry
check, and the load-bearing second check immediately before `stale.shutdown()`, with the
three interacting cleanup invariants on the fenced path. #6a/#6b: `Mt5Client.__init__`
participates via the lease-occupancy ContextVar plan 01 landed — a zombie construction
disposes its own socket and raises; a preflight construction is exempt because it carries
no token. Re-cut the four docblocks in these two modules whose safety argument this
change replaces.

Honest limit to state in code comments and SUMMARY (D-43): the fence cannot un-send a
call already on the wire — if the zombie had already DISPATCHED `stale.shutdown()` when
the lease released, the pipe still dies for the next holder. The fence closes the common
case (the zombie reaching shutdown after the release). `_assert_expected_login`'s
detection bracket therefore stays.

Purpose: this is the repair of D-35's safety argument — "every restart call site holds
the terminal lease" is false on the timeout path, and this plan replaces the belief with
a runtime check.
Output: fenced restart + fenced construction, sink- and path-level tests, re-cut
rationales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153.5-wizform-abandon-work-that-outlives-its-timeout/153.5-CONTEXT.md

Targeted reads (one pass each, extract everything then stop):
- `analytics-service/services/mt5_client.py` — `restart:824-909` (WR-01 ordering paragraph :824-846, the "ONE PERMITTED shutdown" paragraph :848-860, `_reconnect_then_dispose_stale:880`, `stale = self._mt5:881`, reconnect :883-885, `self._closed = False:886`, `stale.shutdown():892`, `_teardown_transport(stale,...):907`), `__init__:383-458` (D-24 ordering ValueError guard :431-440 — BEFORE connect; `self._mt5 = connect(...):457`), `_teardown_transport:705-752`.
- `analytics-service/services/mt5_concurrency.py` — `_mt5_bounded_restart:~104-126` (docstring re-cut target) and the ✅-CLOSED D-35 paragraph above `_MT5_TERMINAL_LOCKS` (re-cut target).
- `analytics-service/routers/exchange.py:483-560` — the connect stage (`client: Mt5Client | None = None:486`, the `wait_for(to_thread(lambda: Mt5Client(...)))` :513-524, the broad `except Exception as connect_err:` ~:536) — READ ONLY in this plan; its docblock re-cut is plan 04's.
- `analytics-service/services/ingestion/mt5.py:180-280` — `Mt5Adapter.validate`'s lease at :199, the connect `wait_for` at :207-209, `_MT5_PROBE_TIMEOUT_S:86`.
- Test analogs: `tests/test_mt5_client_contract.py` `_FakeMt5:99`/`_make:190` and the `stale`/`fresh` double pair at :1128-1129; `tests/test_mt5_validate.py` `exchange_router` fixture :89-130 and `_install_real_mt5_client`; `tests/test_ingestion_mt5.py` `_install_shared_terminal:603-631`, `_expected_terminal_key:556-563`.
- Deeper rationale on demand: `153.5-RESEARCH.md` §Q4 (restart ordering — includes a worked code shape), §Open Q-1 (resolution context); `153.5-PATTERNS.md` §Artifact 3 ("the live-path 503 hazard") and §Artifact 6 (re-cut discipline). Consult by section, do not load wholesale.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: restart()'s two checks + the three-invariant fenced cleanup</name>
  <files>analytics-service/services/mt5_client.py, analytics-service/tests/test_mt5_client_contract.py</files>
  <behavior>
    - Fenced restart (the case no existing test covers): a client bound at epoch E; the injected `_connect` seam's side-effect bumps the epoch DURING the WR-01 reconnect (deterministically simulating the lease releasing while the reconnect round-trip was in flight — no threads, no sleeps); then: (a) the stale fake's `shutdown` was NEVER called, (b) the stale transport's `_MetaTrader5__conn` close was called, (c) the FRESH connection returned by the injected connect was ALSO closed, (d) `client._closed is True` afterwards, (e) `Mt5SessionAbandoned` propagates out of `restart()`, (f) a WARNING was logged naming the skipped shared-IPC shutdown.
    - Entry-fenced restart: epoch bumped BEFORE `restart()` is called → refuses before any I/O — the injected `_connect` is never invoked, no fresh socket is ever opened.
    - Live heal unchanged: no bump → `stale.shutdown()` called exactly once, only the stale transport disposed, `_closed` ends False, the client remains usable (drive one read after).
  </behavior>
  <action>
In `Mt5Client.restart()`:

1. ENTRY check: `self._assert_live("restart")` placed in `restart()`'s body BEFORE the
   `self._timed("restart", ...)` call (outside the bracket — a refusal is not a
   round-trip and must not enter the D-32 `duration_ms` population; reuse the existing
   `restart` stage name in the log only). ⛔ Keep `_reconnect_then_dispose_stale` NESTED
   — `PERMITTED_SHUTDOWN_SITES` in tests/test_mt5_shutdown_roster.py keys on the
   outermost enclosing def (`restart`); hoisting the closure to module level reds it.
2. SECOND check, inside `_reconnect_then_dispose_stale`, positioned AFTER the WR-01
   reconnect + `self._closed = False` and IMMEDIATELY BEFORE `stale.shutdown()`
   (mt5_client.py:892 at HEAD): compare `self._epoch != _mt5_epoch_for(self.terminal_key)`
   inline (not via `_assert_live` — cleanup must run before the raise). On the fenced
   path, in order: log a WARNING stating the shared-IPC shutdown is being SKIPPED
   because it would -10004 the next holder, and that only our own sockets are being
   disposed (D-35's distinction); `self._teardown_transport(stale, who="Mt5Client.restart(abandoned)", stage=None)`;
   `self._teardown_transport(self._mt5, who="Mt5Client.restart(abandoned)", stage=None)`
   (the fresh WR-01 connection is dead weight on this path); `self._closed = True`
   (restore the latch cleared at :886 — otherwise a client its caller already gave up
   on is left latched-open and `close()` will no-op forever); then
   `raise Mt5SessionAbandoned("restart")`. These are RESEARCH §Q4's three invariants —
   miss any one and the fix introduces a new leak.
3. Preserve everything else byte-for-byte: WR-01 reconnect-first order, the existing
   swallow-and-log around `stale.shutdown()`, the unconditional `_teardown_transport(stale, ...)`
   on the un-fenced path.
4. ⛔ No new bare `.shutdown`/`.restart` attribute references anywhere in the edit —
   `TEARDOWN_BARE_REFERENCE_SITES` in the shutdown roster is asserted with `==` and a
   new one reds it. (The `stale.shutdown()` CALL stays inside outermost-def `restart`,
   so `PERMITTED_SHUTDOWN_SITES` needs no edit.)

Add the three behavior tests to `tests/test_mt5_client_contract.py` using the existing
`_FakeMt5` pair idiom (:1128-1129 builds a `stale` and a `fresh` double already).
Falsification: temporarily remove the second check, run the fenced-restart test, OBSERVE
`shutdown_calls == 1` red the assertion, restore, observe green. Note the observation in
the SUMMARY.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/pytest tests/test_mt5_client_contract.py -x -q && .venv/bin/pytest tests/test_mt5_shutdown_roster.py -x -q && make typecheck</automated>
  </verify>
  <done>All three restart behaviors pass; the shutdown roster stays green with zero pin edits; the neutered-check red was observed; mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: __init__ construction fence via the occupancy token</name>
  <files>analytics-service/services/mt5_client.py, analytics-service/tests/test_mt5_client_contract.py</files>
  <behavior>
    - Zombie construction, timeout DURING connect (the #6 shape): occupancy var set to a stale tuple (key, E) while the registry reads E+1; the injected `_connect` returns a fake conn; `Mt5Client(...)` raises `Mt5SessionAbandoned` AND the just-opened fake conn's close was called (no leak) AND a WARNING was logged.
    - Zombie construction, already stale BEFORE connect: occupancy stale at entry → raises BEFORE the injected `_connect` is ever invoked — no socket is opened at all.
    - Preflight exemption: occupancy var is None (no lease); epoch bumps between; construction succeeds — no token, no fence, no opt-in parameter anywhere.
    - Live-path proof (the PATTERNS "live-path 503 hazard"): construct INSIDE `async with mt5_terminal_lease(key)` (real lease, fake connect) — no raise; assert with a comment that this is the structural proof that `routers/exchange.py:536`'s broad connect-except can never see `Mt5SessionAbandoned` on a live path, because the lock is exclusive per key, so the epoch cannot move during a held lease.
  </behavior>
  <action>
In `Mt5Client.__init__`, after the existing D-24 ordering `ValueError` guard (:431-440)
and around the `self._mt5 = connect(...)` at :457:

1. Read `_MT5_LEASE_OCCUPANCY.get()`. If `None` → no lease held → structurally exempt
   (this IS the Open Q-1 resolution — write it in the comment: "a preflight build holds
   no lease, therefore carries no token, therefore is exempt — not a special case
   someone maintains"). If a tuple `(occ_key, occ_epoch)` and `occ_key` equals this
   client's terminal key (derive it from host/port the same way the `terminal_key`
   property does — compute once, do not retype the format):
   - PRE-CONNECT: if `_mt5_epoch_for(occ_key) != occ_epoch` → the spawning lease has
     already released → raise `Mt5SessionAbandoned("connect")` BEFORE calling connect —
     no socket is opened, so no teardown is needed.
   - POST-CONNECT: immediately after `self._mt5 = connect(...)`, re-read; if the epoch
     moved during the connect → log the WARNING, then
     `self._teardown_transport(self._mt5, who="Mt5Client.__init__(abandoned)", stage=None)`,
     set `self._closed = True`, and raise `Mt5SessionAbandoned("connect")`. ⛔ The
     teardown-before-raise is mandatory — PATTERNS §Artifact 3: a raise after the
     connect leaks the socket, which is Pitfall 6 in a new costume, the exact leak this
     fence exists to close.
2. Keys that do NOT match (a client for a different terminal than the leased one): treat
   as no-token — the fence makes no claim across terminals.
3. ⚠️ Method-fence interaction: `self._epoch` stays `None` at construction (lazy bind is
   plan 01's contract; the construction fence uses the TOKEN's epoch, a different
   variable — RESEARCH Pitfall 2 warns against conflating them; name them distinctly).
4. ⛔ Do not add any constructor parameter for this (no `fence_construction=` opt-in —
   that alternative was REJECTED with the Q-1 resolution).

Add the four behavior tests to `tests/test_mt5_client_contract.py`. Manipulate the
occupancy var in tests via `begin_mt5_lease_occupancy`/direct `ContextVar` use plus
`bump_mt5_terminal_epoch` — never by monkeypatching `_assert_live` internals.
Falsification: neuter the post-connect re-check, observe the zombie-construction test
red on "close was called", restore.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/pytest tests/test_mt5_client_contract.py -x -q && make typecheck</automated>
  </verify>
  <done>All four construction behaviors pass; zero call-site edits and zero new constructor parameters; the neutered re-check red was observed; mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Path-level #6a/#6b tests + the four rationale re-cuts</name>
  <files>analytics-service/tests/test_mt5_validate.py, analytics-service/tests/test_ingestion_mt5.py, analytics-service/services/mt5_client.py, analytics-service/services/mt5_concurrency.py</files>
  <behavior>
    - #6a (router): drive the validate route with a fake connect that BLOCKS on a test-owned `threading.Event` (bounded `.wait(0.25)`, `gate.set()` in the test's `finally`); monkeypatch `_MT5_VALIDATE_STAGE_TIMEOUT_S` to ~0.05 ON THE MODULE OBJECT the `exchange_router` fixture yields (the §Q6 patch-target table — patching anywhere else is a silent no-op); the route answers its existing 503 connect-timeout envelope; then release the gate, join the zombie via its own done-signal (bounded), and assert the conn the zombie's construction opened WAS closed (the fake records close) — the leak is gone.
    - #6b (worker adapter): same shape against `Mt5Adapter.validate` with `_MT5_PROBE_TIMEOUT_S` patched on `services.ingestion.mt5` (its reader), using `_install_shared_terminal` + `_expected_terminal_key`; the abandoned `_build_client` thread's conn is closed after the gate opens.
  </behavior>
  <action>
Add the two path tests. ⚠️ CI-hang trap: every Event wait bounded; every gate set in a
`finally`; join the zombie on a signal IT sets, never a sleep. Ordering oracle is an
index into the fake's recorded call log, never a wall clock (D-37 discipline applies
here too even though the runtime guard proper is plan 05's).

Then the re-cuts (the half of D-35's template planners skip — behaviour right + record
wrong is the more dangerous state). Re-cut, never delete; each re-cut names what changed
and why (Rule 9):
1. `mt5_client.py` restart docstring :848-860 — "safe because every call site holds the
   terminal lease… derived from source" becomes: safe because the epoch fence enforces
   it AT RUNTIME (entry + pre-shutdown checks); the shutdown roster derives the LEXICAL
   half only and is green on the abandonment case by construction.
2. `mt5_client.py` restart ORDERING/WR-01 paragraph :824-846 — the abandoned-thread
   reasoning stays correct; append what the fence adds and its D-43 limit (a dispatched
   shutdown cannot be un-sent).
3. `mt5_concurrency.py` `_mt5_bounded_restart` docstring — "A hung restart is ABANDONED
   at the bound… the thread is never joined" gains: and is now FENCED — its
   `stale.shutdown()` refuses once the lease has moved on (finding #5, WIZFORM-ABANDON).
4. `mt5_concurrency.py` ✅-CLOSED D-35 paragraph above `_MT5_TERMINAL_LOCKS` — same
   correction as (1).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/pytest tests/test_mt5_validate.py tests/test_ingestion_mt5.py -x -q && .venv/bin/pytest tests/test_mt5_client_contract.py tests/test_mt5_concurrency.py tests/test_mt5_shutdown_roster.py -q && make typecheck</automated>
  </verify>
  <done>Both path-level leak tests pass with deterministic barriers; all four docblocks state the post-153.5 safety argument; full MT5 sink suites green; mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zombie restart → next holder's IPC pipe | the one permitted `mt5.shutdown()` crossing a released lease |
| zombie construction → gateway FD budget | orphaned rpyc sockets nobody will close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.5-05 | DoS | `stale.shutdown()` under next holder (-10004) | mitigate | pre-shutdown second check + three-invariant cleanup (Task 1) |
| T-153.5-06 | DoS | rpyc socket leak on connect-stage timeout | mitigate | construction fence + mandatory teardown-before-raise (Task 2) |
| T-153.5-07 | DoS | breaker-keyed 503 minted from a live-path fence bug | mitigate | live-path-cannot-raise proof test (Task 2, behavior 4) |
| T-153.5-08 | Tampering | already-dispatched shutdown on the wire | accept | D-43: client-side fence cannot un-send; detection bracket (`_assert_expected_login`) stays; stated in comments + SUMMARY |
</threat_model>

<verification>
cd analytics-service && .venv/bin/pytest tests/test_mt5_client_contract.py tests/test_mt5_concurrency.py tests/test_mt5_validate.py tests/test_ingestion_mt5.py tests/test_mt5_shutdown_roster.py -q && make typecheck
</verification>

<success_criteria>
- Finding #5 closed at the sink: fenced restart skips the shared shutdown, closes both sockets, restores `_closed`, raises — and the live heal is byte-equivalent to HEAD.
- Finding #6 closed at the sink: zombie constructions self-dispose; preflight builds exempt with no opt-in surface.
- Live path provably cannot raise the new exception; D-43's limit stated, not hidden.
- All four invalidated rationales re-cut; shutdown roster untouched and green.
</success_criteria>

<output>
Create `.planning/phases/153.5-wizform-abandon-work-that-outlives-its-timeout/153.5-02-SUMMARY.md` when done.
</output>
