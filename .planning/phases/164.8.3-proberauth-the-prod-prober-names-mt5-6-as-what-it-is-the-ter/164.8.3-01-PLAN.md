---
phase: "164.8.3"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/prod-prober/fixtures/mt5/6.txt
  - scripts/prod-prober/arms/mt5.mjs
  - scripts/prod-prober/run.mjs
  - src/__tests__/prod-prober-wiring.test.ts
autonomous: true
requirements: ["#753"]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A `railway ssh` transcript carrying `last_error: [-6, …]` produces a defect row whose kind is `mt5-not-authorized` and whose subject is `-6` — not `mt5-terminal-error` (C1, D-01)."
    - "That row's remedy names the VNC console, the \"Save password\" step, the Expert-Advisors options a login re-clears, and the terminal's Journal — and routes the operator nowhere else (C2, D-04)."
    - "`mt5-terminal-error` still fires, on BOTH `init-false-other.txt` and `terminal-info-null.txt`, with two distinct subjects (C4, D-03)."
    - "`node scripts/prod-prober/run.mjs --self-test` reports a PASSED banner at the new scenario count and prints a header naming `mt5-not-authorized` on `6.txt` (C3)."
    - "The auto-issue step's issue SELECTION is provably independent of every defect kind, so a `-6` under the new name cannot be suppressed by, nor open a second issue beside, the live `prod-prober`-labelled issue."
    - "Criterion 8's MET-BEFORE-START claim is re-measured at HEAD and recorded, and no line of `.github/workflows/prod-prober.yml` is changed (C8)."
  artifacts:
    - scripts/prod-prober/fixtures/mt5/6.txt
    - scripts/prod-prober/arms/mt5.mjs
    - scripts/prod-prober/run.mjs
    - src/__tests__/prod-prober-wiring.test.ts
  key_links:
    - "`6.txt` → `classifyProbe` branch (6b) → `addDefect(\"mt5-not-authorized\", …)` → `REMEDIES[\"mt5-not-authorized\"]` → the printed defect row."
    - "`DEFECT_KINDS` ↔ `ARM_FIXTURE_TABLE` mt5 `kinds:` ↔ `red:` ↔ `KIND_ASSERTIONS` ↔ `EXPECTED_DEFECT_KINDS` — five INDEPENDENT spellings; the SET-equality gate at `run.mjs:1091-1102` refuses a partial landing."
    - "`SELF_TEST_SCENARIOS` (`run.mjs:149`) ↔ the literal pin (`prod-prober-wiring.test.ts:1822`) — a new `red:` row adds exactly one `scenario()` call at `run.mjs:1120-1121`."
    - "`WORKFLOW_TEXT` (`prod-prober-wiring.test.ts:68`) → the `Open or update the prod-prober issue` step slice → `const dedupLabel = \"prod-prober\";` → `listForRepo({ labels: dedupLabel })`."
  prohibitions:
    - "Never add a `kinds` array to `scripts/prod-prober/arms/mt5.mjs` — the registry is `DEFECT_KINDS` + the fixture table's per-entry `kinds:` + `KIND_ASSERTIONS`, and a second registry would drift."
    - "Never place the `-6` branch after branch (8) — branch (8) is the unguarded tail, so a branch below it is dead code that fails nothing at review time."
    - "Never derive `expect(SELF_TEST_SCENARIOS).toBe(N)` from the export — the literal is hand-typed on purpose (`prod-prober-wiring.test.ts:1809-1816`); a derived pin is a list that can never disagree."
    - "Never let a remedy or a defect detail quote an MT5 account number, a broker server name, or a password — remedies land in a PUBLIC Actions log and a PUBLIC auto-filed issue."
    - "Never edit or rewrite the historical comments on the auto-filed `prod-prober` issues — they are a correct record of what the old code said."
    - "Never touch `scripts/mt5-diag.sh` in this plan, and never add an MT5 call outside `initialize()`/`last_error()`/`terminal_info()`."
    - "Never add the artifact upload criterion 8 offered as an alternative — the other alternative shipped in `604d655f`, and shipping a fix for a defect that no longer exists is the vacuity class this milestone removes."
    - "Never edit `.github/workflows/prod-prober.yml` in this plan."
    - "Never cite `gh run view --log` as evidence — it truncates; use the run LOG ARCHIVE."
    - "Never treat a green hourly prober run as this phase's acceptance signal — the live outage is a founder action, out of this phase's gift."
    - "Never use a bare `grep -c <token> <file>` as a gate over a file whose own comments contain that token — filter comments first (`grep -v '^[[:space:]]*#'`)."
---

<objective>
Land the `mt5-not-authorized` defect kind end-to-end, in ONE atomic slice that every
registry gate can see, and prove the publication path cannot swallow it.

Purpose: today a live `-6` reads as `mt5-terminal-error` with the remedy *"Read the reported
code against the MT5 error table…"*. That sentence cost two real investigations (2026-09-09 and
2026-09-10). `-6` has exactly one cause and exactly one remedy, so the lookup instruction IS the
defect (C1, C2, D-01, D-02, D-04).

Output: `scripts/prod-prober/fixtures/mt5/6.txt`; a `(6b)` branch and a `REMEDIES` entry in
`scripts/prod-prober/arms/mt5.mjs`; five independent registrations plus two counter bumps across
`scripts/prod-prober/run.mjs` and `src/__tests__/prod-prober-wiring.test.ts`; and two durable
gates over the auto-issue step.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-CONTEXT.md
@.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-RESEARCH.md
@.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-PATTERNS.md
</context>

<measured_baseline>
Every number below was re-measured at HEAD `de1a3947` during planning. `run.mjs`'s anchors did
NOT move since the research read; `prod-prober.yml` and `prod-prober-wiring.test.ts` did, and
their anchors were re-checked individually.

| Fact | Value | How measured |
|---|---|---|
| `node scripts/prod-prober/run.mjs --self-test` | `SELF-TEST PASSED: 78/78 …`, exit 0 | run at HEAD |
| `run.mjs:149` | `export const SELF_TEST_SCENARIOS = 78;` | grep |
| `DEFECT_KINDS` length | 20 | parsed from `run.mjs:162-188` |
| Sorted position of `mt5-not-authorized` | index 12 — **after** `mt5-no-ipc`, **before** `mt5-probe-timeout` | `Array.sort()` over the parsed roster |
| mt5 `kinds:` / `red:` / `KIND_ASSERTIONS` | `run.mjs:918` / `:922-931` / `:1007-1010` | grep |
| `EXPECTED_DEFECT_KINDS` | `prod-prober-wiring.test.ts:761-781`, 20 names sorted | read |
| Scenario-count pin | `prod-prober-wiring.test.ts:1822` → `expect(SELF_TEST_SCENARIOS).toBe(78);` | read |
| Longest digit run in any current mt5 remedy | none ≥ 6 digits | `Object.entries(REMEDIES)` scan |

⛔ **RESEARCH.md's sort claim is WRONG and is corrected here.** RESEARCH says
`mt5-not-authorized` "lands between `mt5-ipc-timeout` and `mt5-no-ipc`". Measured: `"mt5-no-ipc"`
< `"mt5-not-authorized"` because `-` (0x2D) sorts before `t` (0x74). It lands **between
`mt5-no-ipc` and `mt5-probe-timeout`**. Use the measured order.
</measured_baseline>

<dedup_key_proof>
## ⛔ MANDATORY PRE-WRITE PROOF — the new kind cannot collide with the live auto-filed issue

Read at HEAD `de1a3947`, `.github/workflows/prod-prober.yml`, step
`- name: Open or update the prod-prober issue` (`:371`). **The dedup key expression, verbatim:**

```js
const dedupLabel = "prod-prober";                                   // :384
…
const { data: existing } = await github.rest.issues.listForRepo({   // :409
  owner: context.repo.owner,
  repo: context.repo.repo,
  state: "open",
  labels: dedupLabel,                                              // :413
  per_page: 1,
});
if (existing.length > 0) { await github.rest.issues.createComment({ … body: […detail…] }); return; }
…
await github.rest.issues.create({                                  // :435
  title: `prod-prober: ${defectCount} defect(s) on ${today}`,      // :438
  labels: [dedupLabel, "p1"],                                      // :440
});
```

**The key is the constant string `"prod-prober"`. It is not derived from the defect kind, the
subject, the remedy, the defect count, or the title.** Measured this session over the step's
own 3419-character slice:

- DEFECT_KINDS literals appearing anywhere in the slice: `[]` (zero, ranged over all 20).
- `const dedupLabel = "prod-prober";` occurrences: 1.
- `labels: dedupLabel,` occurrences: 1.

**Therefore, for the live `prod-prober`-labelled issue:**

1. **No suppression.** Selection reads only `state: "open"` + `labels: "prod-prober"`. It never
   inspects the table. The comment body is
   `["```", armsLine, "", table, "```"].join("\n")` where `table = lines.slice(tableAt)` from the
   first `❌` line **verbatim** — kind-agnostic slicing. A `mt5-not-authorized` row therefore
   reaches the operator in full, remedy included.
2. **No second parallel issue.** While a `prod-prober`-labelled issue is open, `existing.length
   > 0` and the step `return`s after commenting. It is structurally incapable of opening a
   second one. (This closes RESEARCH assumption **A3**, whose stated worst case was exactly that.)
3. **Accepted, recorded consequence — NOT a defect to fix here.** The open issue's *title and
   body* keep whatever the first filing said; only the new COMMENT carries the new kind. Editing
   history is prohibited (see `must_haves.prohibitions`). The operator reads the newest comment,
   which is where the remedy lives.

Task 2 converts this proof into a durable, calibrated gate so it cannot silently rot.
</dedup_key_proof>

<criterion_8_status>
## ✅ Criterion 8 — MET BEFORE THE PHASE BEGAN. Re-verified at HEAD. No code is owed.

Re-measured at HEAD `de1a3947`, with comments filtered (the file's own prose quotes the idiom,
so an unfiltered count reads 3 instead of 2):

| Check | Filtered count | Raw count |
|---|---|---|
| `\|\| status=$?` in `.github/workflows/prod-prober.yml` | **2** (both branches) | 3 |
| `cat "$RUNNER_LOG"` | **1** (unconditional, outside both branches) | 1 |

The regression pin exists at `src/__tests__/prod-prober-wiring.test.ts:349` (*"EVERY branch of
the probe step captures its status the same way — the `--arm` branch too"*) and `:396`
(`const cat = idx('cat "$RUNNER_LOG"');` — the ordering pin).

⛔ **Do not add the artifact upload.** ⛔ **Do not edit `prod-prober.yml` in this phase.**
The ROADMAP already carries the corrected criterion (commit `31c714cc`), so no ROADMAP edit is
owed either. **This phase ships SEVEN live criteria.**
</criterion_8_status>

<api_coverage_decision>
**No external API integration: this phase changes no API call — `MT5_PROBE_PY`'s call set
(`initialize()`, `last_error()`, `terminal_info()`) is byte-asserted unchanged at
`run.mjs:3894-3896` and fenced by the FORBIDDEN-token list at `run.mjs:3797-3806`; plan 03
narrows only the PROJECTION of a dict `terminal_info()` already returned.** No COVERAGE.md is
produced; the declaration stands in its place, as the checkpoint permits.
</api_coverage_decision>

<assumption_delta_decision>
**Signal:** singular → plural. `-10004` and `-10005` were the only code-specific mt5 kinds;
`-6` makes three. The checkpoint asks whether the catch-all `mt5-terminal-error` still names
the right noun.

**Decision: `no-change` to the kind name — with one REQUIRED text correction, not an
improvement.**

Rationale: `mt5-terminal-error` is a RESIDUAL bucket, and its noun is already correctly
non-specific — *"the bridge answered and the terminal is the problem, cause not enumerated."*
Pluralising the enumerated set does not change what the residue is. Promoting (renaming) it
would (a) contradict criterion 4, which requires the catch-all *survive* as the same bucket,
(b) change a string that reaches the PUBLIC issue history, and (c) force a rewrite of two
fixtures' expectations for no measured gain. The contrary in-repo precedent is real and named:
`cron-obs` keeps ONE kind for every non-2xx and carries the 401's specific meaning in the remedy
prose (`arms/cron-obs.mjs:139,340-348`), and `cron-drift` declines the split explicitly
(`run.mjs:880-887`). The split here is in pattern only because the two remedies are OPPOSITE
(`arms/mt5.mjs:36-50`), which is this repo's stated test.

**The required correction:** branch (8)'s detail currently reads *"which is neither -10004 nor
-10005"*. After the split that enumeration is incomplete, so it becomes a three-code
enumeration. That is a staleness fix, not a redesign.

**Accepted debt, and what would force a promote later:** if a FOURTH code earns its own kind,
the residual's detail sentence becomes an unwieldy enumeration; at that point the enumeration
should be derived from the branch set rather than hand-typed, and the noun revisited.
</assumption_delta_decision>

<schema_push_gate>
No schema-relevant files in this phase's scope (`git ls-files supabase/migrations` is untouched
by every path in `files_modified`). No schema-push task injected.
</schema_push_gate>

<artifacts_this_plan_produces>
| Symbol / path | Kind | Where |
|---|---|---|
| `mt5-not-authorized` | defect-kind id (D-01) | `run.mjs` `DEFECT_KINDS`; `ARM_FIXTURE_TABLE` mt5 `kinds:`; `red:`; `KIND_ASSERTIONS`; `EXPECTED_DEFECT_KINDS` |
| `REMEDIES["mt5-not-authorized"]` | remedy key + string | `scripts/prod-prober/arms/mt5.mjs` |
| branch `(6b)` — `if (code === -6)` | classifier branch | `scripts/prod-prober/arms/mt5.mjs`, between `(6)` and `(7)` |
| `scripts/prod-prober/fixtures/mt5/6.txt` | NEW red fixture (D-02) | new file |
| `SELF_TEST_SCENARIOS = 79` | exported count | `run.mjs:149` |
| `expect(SELF_TEST_SCENARIOS).toBe(79)` | literal pin | `prod-prober-wiring.test.ts:1822` |
| `AUTO-ISSUE DEDUP: …` (2 `it()` blocks) | durable gate + its CALIBRATION | `src/__tests__/prod-prober-wiring.test.ts` |
</artifacts_this_plan_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: the `mt5-not-authorized` kind, wired end-to-end in one atomic slice</name>

  <read_first>
    - `scripts/prod-prober/arms/mt5.mjs` — header `:36-71` (why `-10004`/`-10005` do not collapse; and `:61-64`, `connected`/`trade_allowed` are PRINTED AND NEVER JUDGED), `REMEDIES` `:133-144`, `classifyProbe` docblock `:150-159`, branches `(5)`/`(6)` `:244-268`, branch `(7)` `:270-281`, branch `(8)` `:283-292`.
    - `scripts/prod-prober/run.mjs` — `SELF_TEST_SCENARIOS` `:149`, `DEFECT_KINDS` `:150-188` (incl. the "ALL TWENTY KINDS" prose at `:156`), the fixture-table `kinds:` contract `:812-818`, the mt5 entry `:905-933`, `KIND_ASSERTIONS` `:995-1016`, the SET-equality + REMEDIES gates `:1091-1114`, the red loop `:1120-1167`.
    - `src/__tests__/prod-prober-wiring.test.ts` — `:757-787` (`EXPECTED_DEFECT_KINDS`, the "Twenty names, sorted." prose at `:759`), `:1809-1828` (the interpolated-title / literal-pin rule).
    - `scripts/prod-prober/fixtures/mt5/10005.txt` — the two-line fixture shape to copy.
    - This plan's `<measured_baseline>` — in particular the CORRECTED sort position.
  </read_first>

  <files>
    scripts/prod-prober/fixtures/mt5/6.txt,
    scripts/prod-prober/arms/mt5.mjs,
    scripts/prod-prober/run.mjs,
    src/__tests__/prod-prober-wiring.test.ts
  </files>

  <behavior>
    - A transcript whose PROBE line is `{"initialize": false, "last_error": [-6, "Terminal: Authorization failed"], "terminal_info": null}` classifies to kind `mt5-not-authorized`, subject `-6`, and raises exactly one defect.
    - `10004.txt` still classifies to `mt5-no-ipc`; `10005.txt` still to `mt5-ipc-timeout`.
    - `init-false-other.txt` (code `-2`) still classifies to `mt5-terminal-error`, subject `-2` — branch (8).
    - `terminal-info-null.txt` (`initialize: true`, `terminal_info: null`) still classifies to `mt5-terminal-error`, subject `terminal_info` — branch (7). The `-6` branch cannot intercept it: its code is `1`.
    - `ok.txt` still runs green with zero defects.
    - The self-test prints one NEW scenario header naming `mt5-not-authorized` on `6.txt`, and its tail completeness assertion agrees with the exported count.
  </behavior>

  <action>
    ⛔ **This is ONE commit. Every registry gate below refuses a partial landing, so a half-done
    slice is a knowingly-red intermediate state — do not split it.** Work in the order given.

    **(a) The fixture — D-02.** Create `scripts/prod-prober/fixtures/mt5/6.txt` as RAW TEXT,
    exactly two lines plus a trailing newline, matching the live 2026-09-09 reading. Line 1 is
    the CLI banner `Connecting to service mt5-gateway on environment production...` — it is
    load-bearing, it proves the scanner at `arms/mt5.mjs:191-193` scans rather than assuming line
    1. Line 2 is `PROBE ` followed by one line of JSON with `initialize` false, `last_error`
    `[-6, "Terminal: Authorization failed"]`, and `terminal_info` null. ⛔ Do not store it as
    JSON; the raw-text form is deliberate (`run.mjs:667-675`).

    **(b) The classifier branch — C1, D-01.** In `arms/mt5.mjs`, insert a new branch numbered
    `(6b)` BETWEEN the `-10005` branch's closing brace and the `probe.initialize === true` branch.
    ⛔ Never below branch (8): that branch is the unguarded tail, so anything after it is dead
    code and nothing at review time would say so. Guard on `code === -6` and return kind
    `mt5-not-authorized`, subject the string `-6` (matching how `-10004`/`-10005` carry their raw
    code for grepping), `info: null`, and a `detail` that STATES WHAT WAS MEASURED rather than
    naming a code the reader must look up: the rpyc bridge ANSWERED, so the terminal is up and
    the IPC transport is not implicated, but no account is authorized on it — and therefore
    `terminal_info()` came back null and neither boolean could be read. Carry a comment above it
    in the house `⛔/⚠️/⭐` voice making the same argument the `-10004`/`-10005` header makes at
    `:36-50`, applied once more.

    **(c) Branch (8)'s enumeration — the assumption-delta correction.** Branch (8)'s detail
    enumerates the IPC codes it is not. That enumeration is now incomplete; widen it to name all
    three enumerated codes. Nothing else about branch (8) changes (D-03: the catch-all survives).

    **(d) The remedy — C2, D-04.** Add `REMEDIES["mt5-not-authorized"]` in `arms/mt5.mjs`. House
    style (`arms/mt5.mjs:124-132`, and the extraction in PATTERNS.md §3): open with the STATE
    with the discriminator in caps; name the exact tool and action; say what does NOT work with
    ⛔ and why; name the confirming observation. The four REQUIRED elements, all four of which a
    machine gate in plan 02 will assert on the DEFECT ROW:
      1. the VNC console on the `mt5-gateway` service;
      2. logging the terminal back into the investor account with the "Save password" step;
      3. re-checking the Expert-Advisors options a login re-clears — a login is an account
         change, and `arms/mt5.mjs:8-13` records the 2026-08-13 incident where each diagnostic
         round re-disabled algo trading;
      4. the terminal's own **Journal** tab, read FIRST, because it is the one place that
         separates an invalid account from a lost connection and neither this prober nor
         `scripts/mt5-diag.sh` can tell them apart (D-04).
    Close by naming the confirming observation: `terminal_info()` must report BOTH of its two
    booleans true (`docs/runbooks/mt5-go-live.md` Step 2 states this criterion verbatim).
    ⛔ **Four absolute constraints on the string.** It must not send the reader to a lookup
    table — that instruction is the whole defect this phase removes, and plan 02 gates its
    absence on the row. It must not mention the modal login dialog, which belongs to `-10005`
    and would make the two remedies near-duplicates that pass the exact-string uniqueness gate
    and fail the operator. It must quote NO account number, NO broker server name and NO
    password: this row lands in a PUBLIC Actions log and a PUBLIC auto-filed issue. It must be
    ≥ 40 characters and a string no other mt5 remedy equals (`run.mjs:1107-1114`).
    ⭐ Also re-read `mt5-terminal-error`'s existing remedy after the split: leave it as it is.
    Its lookup instruction is CORRECT advice for a genuinely unenumerated code such as the `-2`
    that `init-false-other.txt` carries, and criterion 2's prohibition is scoped to the `-6`
    kind. Narrowing the residual's own text is not owed by any criterion.

    **(e) Five INDEPENDENT registrations — C3.** Spell the name out by hand in each; deriving
    any one from another produces a list that can never disagree with the thing it checks
    (`prod-prober-wiring.test.ts:757-760`).
      1. `run.mjs` `DEFECT_KINDS` — add under the `// mt5 (plan 04)` group.
      2. `run.mjs:918` — append to the mt5 entry's `kinds:` array.
      3. `run.mjs:922-931` — add `"6.txt": "mt5-not-authorized",` to `red:`, with a one-line
         comment beside it in the style of the two catch-all rows.
      4. `run.mjs:1007-1010` — add the `KIND_ASSERTIONS` entry, spelled independently as a
         literal `d.kind === "mt5-not-authorized"` comparison. It is MANDATORY: `run.mjs:1122-1128`
         turns a missing entry into a FAIL, and the wiring test's coverage extractor reads
         literal comparisons out of `selfTest()`'s source.
      5. `prod-prober-wiring.test.ts:761-781` `EXPECTED_DEFECT_KINDS` — insert keeping the list
         sorted. ⛔ **Measured position: between `mt5-no-ipc` and `mt5-probe-timeout`.** RESEARCH
         states this wrongly; use the measured order.

    **(f) The two counters, in this same commit.** Bump `run.mjs:149`
    `export const SELF_TEST_SCENARIOS` and the hand-typed literal at
    `prod-prober-wiring.test.ts:1822`. One new `red:` row adds exactly one `scenario()` call
    (`run.mjs:1120-1121`), so both go to 79. ⛔ **Do not trust that derivation over the run.**
    Run the self-test; its tail assertion PRINTS the true count it counted. If the run reports a
    different number, the run is right — set both constants to what it counted and say so in the
    commit message. ⛔ Never spell the pin as `toBe(SELF_TEST_SCENARIOS)`; the literal is D5's
    deliberate-edit pin.

    **(g) Two prose counts that rot on this edit.** `run.mjs:156` says *"ALL TWENTY KINDS WERE
    REGISTERED UP FRONT"* and `prod-prober-wiring.test.ts:759` says *"Twenty names, sorted."*
    The roster becomes 21 (measured: it is 20 today). Correct both numerals in the same commit,
    and leave the surrounding argument untouched.

    ⛔ Do not add a `kinds` array to the arm file. ⛔ Do not touch `scripts/mt5-diag.sh`,
    `.github/workflows/prod-prober.yml`, or `scripts/prod-prober/fixtures/mt5/ok.txt` (plan 03
    owns `ok.txt`).
  </action>

  <verify>
    <automated>ST=$(node scripts/prod-prober/run.mjs --self-test 2>&1); printf '%s\n' "$ST" | tail -4 >&2; printf '%s\n' "$ST" | grep -q 'SELF-TEST PASSED: 79/79' &amp;&amp; printf '%s\n' "$ST" | grep -q 'mt5-not-authorized fires on 6.txt' &amp;&amp; printf '%s\n' "$ST" | grep -q 'mt5-terminal-error fires on init-false-other.txt' &amp;&amp; printf '%s\n' "$ST" | grep -q 'mt5-terminal-error fires on terminal-info-null.txt' &amp;&amp; ! printf '%s\n' "$ST" | grep -q 'SELF_TEST_SCENARIOS is 78'</automated>
    <fails_when>non-zero exit; or the output does not contain the literal `SELF-TEST PASSED: 79/79`; or no scenario header reads `mt5-not-authorized fires on 6.txt` (the fixture is on disk but unregistered, so the suite passed without exercising it); or either `mt5-terminal-error fires on …` header is missing (the catch-all lost a fixture, breaking D-03/C4); or the line `the counted scenario set is complete: 79 header(s) printed, SELF_TEST_SCENARIOS is 78` appears, which names a missed constant bump.</fails_when>

    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
    <fails_when>non-zero exit, or the summary line does not read `Test Files  1 passed`. A failure naming `EXPECTED_DEFECT_KINDS` means the sorted insert is in the wrong position; one naming `SELF_TEST_SCENARIOS` means the literal pin at `:1822` was not bumped; one naming `uncovered` means the `KIND_ASSERTIONS` entry is missing or not spelled as a literal comparison.</fails_when>

    <automated>node --input-type=module -e 'const m = await import("./scripts/prod-prober/arms/mt5.mjs"); const r = m.REMEDIES["mt5-not-authorized"]; const bad = []; if (!r) bad.push("MISSING"); else { if (r.length < 40) bad.push("TOO_SHORT"); if (/\b[0-9]{6,}\b/.test(r)) bad.push("LONG_DIGIT_RUN"); if (Object.values(m.REMEDIES).filter((x) =&gt; x === r).length !== 1) bad.push("NOT_UNIQUE"); } console.log(bad.length ? "REMEDY-CHECK FAIL: " + bad.join(",") : "REMEDY-CHECK OK"); process.exit(bad.length ? 1 : 0);'</automated>
    <fails_when>non-zero exit, or the line printed is not exactly `REMEDY-CHECK OK`. `MISSING` = no remedy registered; `TOO_SHORT` = under the 40-char floor `run.mjs:1107-1109` enforces; `LONG_DIGIT_RUN` = a six-or-more-digit run reached a string that lands in a PUBLIC log (an account number); `NOT_UNIQUE` = another mt5 remedy is the same string.</fails_when>
  </verify>

  <acceptance_criteria>
    - `6.txt` exists, is two lines of raw text, and its PROBE line carries `last_error` code `-6` (D-02).
    - `classifyProbe` on that fixture returns kind `mt5-not-authorized`, subject `-6` (C1, D-01), and the branch sits between the `-10005` branch and the `probe.initialize === true` branch.
    - `REMEDIES["mt5-not-authorized"]` names the VNC console, the "Save password" step, the Expert-Advisors options, and the Journal (C2, D-04); it contains no lookup-table instruction, no account number, no broker server name, no password.
    - `DEFECT_KINDS`, the mt5 entry's `kinds:`, its `red:`, `KIND_ASSERTIONS` and `EXPECTED_DEFECT_KINDS` each carry the name, hand-typed, with `EXPECTED_DEFECT_KINDS` still sorted (C3).
    - Both `init-false-other.txt` and `terminal-info-null.txt` still report `mt5-terminal-error`, with subjects `-2` and `terminal_info` respectively (C4, D-03).
    - `SELF_TEST_SCENARIOS` and its literal pin both equal the number the self-test actually counted; the two "twenty" prose counts read twenty-one.
    - `scripts/mt5-diag.sh` and `.github/workflows/prod-prober.yml` are byte-unchanged (C5).
  </acceptance_criteria>

  <done>
    `node scripts/prod-prober/run.mjs --self-test` prints `SELF-TEST PASSED: 79/79` and exits 0
    with a scenario header naming `mt5-not-authorized` on `6.txt`; the wiring test file passes;
    the remedy check prints `REMEDY-CHECK OK`; and `git diff --name-only` lists exactly the four
    paths in `<files>` (C1, C2, C3, C4, D-01, D-02, D-03, D-04).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: make the dedup-key proof and criterion 8's MET claim into gates that cannot rot</name>

  <read_first>
    - This plan's `<dedup_key_proof>` and `<criterion_8_status>` blocks — both carry the measured numbers this task pins.
    - `.github/workflows/prod-prober.yml:371-441` — the `Open or update the prod-prober issue` step in full.
    - `.github/workflows/prod-prober.yml:274-282` — the `status=0` / `|| status=$?` / unconditional `cat` shape.
    - `src/__tests__/prod-prober-wiring.test.ts:61-70` (`WORKFLOW_PATH`, `WORKFLOW_TEXT`, `RUNNER_TEXT`), `:76-111` (the ANCHOR DISCIPLINE note and `anchorIndex`, which THROWS by name rather than letting `slice(-1)` degenerate into a vacuous pass), `:340-410` (the existing probe-step pins), `:804-810` (the canonical CALIBRATION shape to copy).
  </read_first>

  <files>src/__tests__/prod-prober-wiring.test.ts</files>

  <behavior>
    - Gate: the auto-issue step's slice of `WORKFLOW_TEXT` contains `const dedupLabel = "prod-prober";` exactly once, contains `labels: dedupLabel` in the `listForRepo` options, and contains ZERO members of the exported `DEFECT_KINDS` roster — so issue selection is provably kind-independent and widens by itself when a kind is added.
    - CALIBRATION: the same predicate applied to an in-memory mutant of that slice, in which the label is made kind-derived, REPORTS the kind it found. The mutant must be asserted to differ from the original.
    - Gate: with comments filtered, `prod-prober.yml` carries `|| status=$?` on exactly 2 lines and `cat "$RUNNER_LOG"` on exactly 1 — criterion 8's MET claim, pinned.
  </behavior>

  <action>
    Add a new `describe` block to `src/__tests__/prod-prober-wiring.test.ts` titled so that all
    THREE of its `it()`s carry the literal `AUTO-ISSUE DEDUP` in their names (the verify filters
    on it and asserts the count, so a name that does not carry the literal reads as a vacuous
    green).

    **(a) The kind-independence gate.** Slice `WORKFLOW_TEXT` from the anchor
    `- name: Open or update the prod-prober issue` using the existing `anchorIndex` helper — ⛔
    use `anchorIndex`, never a bare `indexOf`, because `indexOf` returns `-1` on a miss and
    `slice(-1)` reads from the END, which is the vacuity the file's `:76-111` note exists to
    prevent. Over that slice assert three things: exactly one occurrence of the `dedupLabel`
    constant declaration; that the `listForRepo` options pass the label variable rather than a
    computed expression; and — the load-bearing one — that
    `DEFECT_KINDS.filter((k) => slice.includes(k))` is `[]`. ⭐ Range over the EXPORTED roster,
    never a hand-typed list, so the assertion automatically covers `mt5-not-authorized` the
    moment plan 01 task 1 registers it. Write the assertion message so it names what a failure
    would mean: an issue-selection path that reads a defect kind could suppress a new kind behind
    an old one's issue, or open a second parallel P1.

    **(b) The CALIBRATION, in a separate `it()`.** Build an in-memory mutant of the same slice in
    which the label assignment is replaced by a kind-derived one — concretely, splice one real
    member of `DEFECT_KINDS` into the label expression. Assert the mutant actually differs from
    the original (`expect(mutant).not.toBe(slice)`), then assert the SAME filter predicate now
    returns exactly that one kind. Without this the gate in (a) is a predicate only ever applied
    to passing input, which this file's own rule at `:15-17` refuses as evidence.

    **(c) Criterion 8's MET claim, pinned.** In the same block, assert over `WORKFLOW_TEXT` with
    COMMENT LINES FILTERED OUT that `|| status=$?` appears on exactly 2 lines and
    `cat "$RUNNER_LOG"` on exactly 1. ⛔ The comment filter is load-bearing and was measured:
    unfiltered, the first count reads 3, because the file's own 20-line argument quotes the idiom
    it mandates — an unfiltered gate would be satisfied by prose. Name commit `604d655f` and
    narrowed dispatch `34706551355` in the block's comment as the dated evidence, and state
    plainly that criterion 8 is MET-BEFORE-START and that this test is a pin, not a new
    requirement.

    ⛔ Change nothing in `.github/workflows/prod-prober.yml`. This task adds tests only.
    ⛔ Do not add an artifact-upload step; `604d655f` already shipped the other alternative.
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts -t 'AUTO-ISSUE DEDUP' 2>&amp;1 | tee /dev/stderr | grep -qE 'Tests +3 passed'</automated>
    <fails_when>non-zero exit; or the summary does not contain `Tests  3 passed`. A summary reading `Tests  no tests` or `0 passed`, or the line `No test files found`, means the `-t` filter matched nothing — the gate did not run and the green is vacuous.</fails_when>

    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts &amp;&amp; node -e 'const code = require("fs").readFileSync(".github/workflows/prod-prober.yml","utf8").split("\n").filter((l) =&gt; !/^\s*#/.test(l)).join("\n"); const st = (code.match(/\|\| status=\$\?/g) || []).length; const cat = (code.match(/cat .{0,2}RUNNER_LOG/g) || []).length; if (st !== 2 || cat !== 1) { console.log("CRITERION-8 PIN FAIL: status-captures=" + st + " (expected 2), cat-runner-log=" + cat + " (expected 1)"); process.exit(1); } console.log("CRITERION-8 PIN OK: status-captures=2, cat-runner-log=1");' &amp;&amp; test -z "$(git status --porcelain -- .github/workflows/prod-prober.yml scripts/mt5-diag.sh)"</automated>
    <fails_when>non-zero exit. The vitest leg failing means a gate or its calibration is red. A printed `CRITERION-8 PIN FAIL: status-captures=N` other than 2 means a probe-step branch stopped capturing its own status on the same line and criterion 8's MET claim has stopped holding at HEAD — re-settle it from the run LOG ARCHIVE, never from `gh run view --log`. `cat-runner-log=N` other than 1 means the unconditional `cat` is gone or duplicated. The `test -z` failing means `git status --porcelain` printed a line for `.github/workflows/prod-prober.yml` or `scripts/mt5-diag.sh` — a file this phase must leave byte-unchanged was edited (C5, and the ⛔ against the artifact upload). ⚠️ The counts are taken over MATCHES with comment lines filtered first, never `grep -c`: the file's own 20-line argument quotes the idiom it mandates, so an unfiltered line count reads 3 and a line-based count could hide two matches on one line.</fails_when>
  </verify>

  <acceptance_criteria>
    - ⛔ **The dedup-key proof is discharged as a MACHINE GATE, not a note:** the auto-issue step's slice is asserted to contain zero members of `DEFECT_KINDS`, ranged over the export so it covers `mt5-not-authorized` automatically, and the assertion is CALIBRATED by a mutant that makes the label kind-derived and is caught.
    - The quoted key expression — `const dedupLabel = "prod-prober";` selecting via `listForRepo({ state: "open", labels: dedupLabel, per_page: 1 })` — is reproduced in the test block's comment, so the proof travels with the gate.
    - Criterion 8's two counts (2 and 1, comment-filtered) are pinned, with `604d655f` and run `34706551355` named as the evidence.
    - `.github/workflows/prod-prober.yml` is byte-unchanged; no artifact-upload step was added.
  </acceptance_criteria>

  <done>
    `npx vitest run src/__tests__/prod-prober-wiring.test.ts -t 'AUTO-ISSUE DEDUP'` reports
    `Tests  3 passed`; the full file passes; the two comment-filtered counts over
    `prod-prober.yml` read 2 and 1; and `git diff --name-only` for this task lists only
    `src/__tests__/prod-prober-wiring.test.ts` (C8, and the dedup proof that guards C1/C2's
    publication path).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container → prober | `railway ssh` stdout crosses into `classifyProbe` as untrusted text; only the first `PROBE `-prefixed line and one stderr line may leave the arm (`run.mjs:3689-3692`). |
| prober → PUBLIC Actions log | every defect row, detail and remedy is printed into a world-readable log. |
| prober → PUBLIC GitHub issue | the auto-issue step copies the defect table verbatim into an issue body/comment on a PUBLIC repo. |
| repo → PUBLIC `.planning/` | `.planning/` is TRACKED; this PLAN.md is world-readable on push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.3-01 | Information Disclosure | `REMEDIES["mt5-not-authorized"]` → public log + public issue | high | mitigate | Task 1's remedy carries no account number, broker server name or password; the Task 1 verify runs a `\b[0-9]{6,}\b` negative over the remedy string and fails on `LONG_DIGIT_RUN`. The remedy tells the operator to READ the Journal; it never quotes a Journal line. |
| T-164.8.3-02 | Information Disclosure | branch (6b) `detail` | high | mitigate | The detail is a fixed sentence about the measured STATE; it interpolates no `probe` field and echoes no raw ssh stdout. `run.mjs:3691-3693` already asserts the arm does not echo raw stdout. |
| T-164.8.3-03 | Elevation of Privilege | `MT5_PROBE_PY` | high | mitigate | Untouched by this plan. The byte-for-byte decode (`run.mjs:3882-3885`) and the no-`${` check (`run.mjs:3890-3892`) stay in force and are exercised by every self-test run in `<verify>`. |
| T-164.8.3-04 | Denial of Service | the probe re-breaking the terminal by authenticating | high | mitigate | The FORBIDDEN-token list (`run.mjs:3797-3806`) refuses `login`, `order_send`, … in the committed body. C5's fence: `scripts/mt5-diag.sh` is not edited and `MT5_PROBE_PY` gains no call. Prohibited explicitly in `must_haves.prohibitions`. |
| T-164.8.3-05 | Spoofing | a new kind silently hijacking or hiding behind the live auto-filed issue | high | mitigate | Task 2's `AUTO-ISSUE DEDUP` gate + its calibration prove selection reads only the constant label and zero defect kinds. The proof expression is quoted in `<dedup_key_proof>`. |
| T-164.8.3-06 | Tampering | npm/pip/cargo installs | n/a | accept | **No packages are installed by this phase.** RESEARCH.md's `## Package Legitimacy Audit` records NOT APPLICABLE with the measurement `grep -n '^import\|require(' scripts/prod-prober/arms/mt5.mjs` → no matches. No `[ASSUMED]`/`[SUS]`/`[SLOP]` candidates exist, so no legitimacy checkpoint is inserted. |
| T-164.8.3-07 | Repudiation | a defect row that cannot be traced back to the transcript that produced it | low | accept | The row carries `subject: "-6"`, the raw code, exactly as `-10004`/`-10005` do; the full run log is `cat`-ed into the public log. No further control is warranted at ASVS L1. |
</threat_model>

<verification>
- `node scripts/prod-prober/run.mjs --self-test` → `SELF-TEST PASSED: 79/79`, exit 0.
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts` → 1 file passed.
- `git diff --name-only` lists exactly the four paths in `files_modified` — no `mt5-diag.sh`, no `prod-prober.yml`, no `ok.txt`.
</verification>

<success_criteria>
- ROADMAP criteria 1, 2, 3, 4 have their implementation landed and machine-exercised; criterion 5's fence is observably intact (two untouched files); criterion 8 is pinned as MET-BEFORE-START with no code written for it.
- Locked decisions D-01, D-02, D-03 and D-04 are implemented exactly as specified.
- The dedup-key proof is a passing, calibrated test — not a paragraph.
</success_criteria>

<output>
Create `.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-01-SUMMARY.md` when done.
Record in it: the self-test's TRUE counted scenario number, the exact `REMEDIES["mt5-not-authorized"]` string as shipped, and the `Tests  N passed` line from the `-t 'AUTO-ISSUE DEDUP'` run.
</output>
