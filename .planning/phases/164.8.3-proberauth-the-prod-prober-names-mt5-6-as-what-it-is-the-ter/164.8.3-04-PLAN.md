---
phase: "164.8.3"
plan: "04"
type: execute
wave: 4
depends_on: ["164.8.3-03"]
files_modified:
  - src/__tests__/prod-prober-wiring.test.ts
  - TODOS.md
autonomous: true
requirements: ["#753"]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A DURABLE falsifier runs on every CI shard: with the `-6` branch excised from an in-memory copy of the arm, `6.txt` classifies as `mt5-terminal-error`, while the shipping module classifies it as `mt5-not-authorized` (C6)."
    - "That falsifier proves the excision was SURGICAL — the mutant still classifies `10005.txt` as `mt5-ipc-timeout`, so a green cannot come from having deleted half the file."
    - "The falsifier writes nothing into the working tree: the mutant lives in a `mkdtempSync` directory and is removed in a `finally`, so the `git checkout --` hazard never arises."
    - "TWO independent levers are observed RED through the real `--self-test`, each dropped ALONE, each restored from a byte backup and `cmp`-verified, with both transcripts recorded verbatim and dated beside the durable control (C6)."
    - "After the manual neuter the working tree is byte-identical to before it, proven by `git status --porcelain` and by `cmp` against the backup."
    - "The one adjacent observation this phase found and must NOT fix is booked with a NAMED destination phase."
  artifacts:
    - src/__tests__/prod-prober-wiring.test.ts
    - TODOS.md
  key_links:
    - "`classifyProbe` is exported and PURE (`arms/mt5.mjs:160`) and the arm has ZERO `import`/`require` statements (measured at HEAD) — which is the property that makes a tmp-dir mutant module importable at all."
    - "`anchorIndex` (`prod-prober-wiring.test.ts:98-111`) THROWS by name on a missing anchor; `indexOf` returns `-1` and `slice(-1)` reads from the END, which is the vacuity the file's `:76-86` note exists to prevent."
    - "`run.mjs:1152` prints `the defect kind is <expected> (got <actual>)` — lever 1's RED message. `run.mjs:1113` prints `no two mt5 remedies are the same string …` — lever 2's RED message."
  prohibitions:
    - "⛔ NEVER `git checkout --` in the neuter harness — it restores to HEAD and SILENTLY destroys uncommitted work. Byte backups only."
    - "⛔ NEVER leave a neuter applied. Restore, `cmp` against the backup, and re-run the self-test green BEFORE moving on — and re-take the backup after every further edit, because a backup goes stale the moment the file changes again."
    - "Never run the manual neuter concurrently with any other work touching `scripts/prod-prober/arms/mt5.mjs` — that is why this plan is a wave of its own."
    - "Never use `indexOf` for a slice anchor in this file — use `anchorIndex`, which throws by name."
    - "Never write the mutant into the working tree — `mkdtempSync` + a `finally` `rmSync` only."
    - "Never credit ONE observed RED to TWO controls — each lever is dropped alone, with the other left live."
    - "Never claim a RED that was not printed: the recorded transcript must be the runner's own output, pasted verbatim."
    - "Never leave a deferral's destination blank — every booked item names a phase."
    - "Never fix the adjacent `capture-manifest` observation in this phase; it is out of scope by RESEARCH's own verdict."
    - "Never edit `.github/workflows/prod-prober.yml`, `scripts/mt5-diag.sh`, `scripts/prod-prober/run.mjs` or `scripts/prod-prober/arms/mt5.mjs` in this plan's COMMITTED diff."
---

<objective>
Prove the `-6` gate can fail — twice, on two independent levers — and leave one of the two
proofs running forever.

Purpose: criterion 6 is this repo's non-negotiable. A control only ever applied to passing input
is not evidence, and a remedy without a gate that can go RED is the vacuity class this milestone
exists to remove. RESEARCH found no reusable neuter harness for a `.mjs` arm, so this plan uses
both in-repo idioms deliberately: a DURABLE in-memory mutant that re-runs on every shard, and ONE
recorded on-disk neuter that proves the self-test SCENARIO — not just the classifier — goes red.

Output: a calibrated falsifier block in `src/__tests__/prod-prober-wiring.test.ts`, two verbatim
dated RED transcripts recorded beside it, and one booked deferral with a named owner.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-CONTEXT.md
@.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-PATTERNS.md
@.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-01-SUMMARY.md
</context>

<measured_baseline>
| Fact | Value | How measured |
|---|---|---|
| `scripts/prod-prober/arms/mt5.mjs` import statements | **zero** | `grep -n '^import\|require('` at HEAD `de1a3947` — this is what makes a tmp-dir mutant module importable standalone |
| `classifyProbe` | exported, pure | `arms/mt5.mjs:160` |
| `mkdtempSync` precedent | 5+ files under `src/__tests__/` (`check-banned-packages.test.ts`, `sql-body-normalize.test.ts`, `gitleaks-allowlist.test.ts`, …) | `grep -rln` |
| `await import()` of a `.mjs` precedent | `src/__tests__/mutation-annotation-parser.test.ts:1331` | grep |
| The anchor helper that THROWS on a miss | `anchorIndex`, `prod-prober-wiring.test.ts:98-111` | read |
| The recorded-neuter comment precedent | `prod-prober-wiring.test.ts:1899-1912` — dated, per-lever, with the observed counts | read |
| The commit-message precedent | `98357965` — *"ANTI-VACUITY, OBSERVED not asserted — two independent levers, cp aside, one edit each, cp back, cmp-verified (RESTORE-CMP-OK both times)"* | `git show` |
| Scenario count entering this plan | 80 | plan 03's verify |

⛔ **Why this plan is a wave of its own, despite touching files nothing else in this phase
touches.** Task 2 temporarily MUTATES `scripts/prod-prober/arms/mt5.mjs` ON DISK. Plan 03 owns
that file. File-disjointness in the frontmatter is NOT isolation here: a concurrent editor and a
neuter harness sharing one working tree is how uncommitted work gets destroyed. This is a
non-file coupling and it is the reason for `wave: 4`.
</measured_baseline>

<artifacts_this_plan_produces>
| Symbol / path | Kind | Where |
|---|---|---|
| `it("FALSIFIER: …")` ×3 carrying the literal `164.8.3 FALSIFIER` | durable in-memory mutant gate + its controls | `src/__tests__/prod-prober-wiring.test.ts` |
| a dated `// MEASURED <date>` block carrying both RED transcripts and `RESTORE-CMP-OK` | recorded manual-neuter evidence | `src/__tests__/prod-prober-wiring.test.ts`, beside the falsifier |
| `[164.8.3-CAPTURE-MANIFEST-ERREXIT]` | booked deferral with a named destination phase | `TODOS.md` |
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the DURABLE falsifier — a tmp-dir mutant of the arm, with its surgical-excision control</name>

  <read_first>
    - `src/__tests__/prod-prober-wiring.test.ts:15-17` — *"EVERY PREDICATE HERE IS WRITTEN OVER ARBITRARY TEXT AND CALIBRATED ON A MUTATED COPY."*
    - `src/__tests__/prod-prober-wiring.test.ts:76-111` — the ANCHOR DISCIPLINE note and `anchorIndex`.
    - `src/__tests__/prod-prober-wiring.test.ts:804-810` — the canonical in-memory mutant shape (rename a literal in a slice, assert the mutant DIFFERS, assert exactly the expected consequence).
    - `src/__tests__/prod-prober-wiring.test.ts:1840-1897` — `[164.8.5-02] compareManifest totality`: four separate `it()`s on purpose, *"the neuter matrix darkens each alone with the other two left live. A single test covering all three would credit one RED to three controls."*
    - `src/__tests__/mutation-annotation-parser.test.ts:1331` — `await import()` of a `.mjs` from a test.
    - `scripts/prod-prober/arms/mt5.mjs` — the shipped `-6` branch, its exact opening line, and the branch immediately after it.
  </read_first>

  <files>src/__tests__/prod-prober-wiring.test.ts</files>

  <behavior>
    - Control (shipping): `classifyProbe` on `6.txt`'s transcript returns `kind === "mt5-not-authorized"`.
    - Falsifier: the same transcript through a mutant module with the `-6` branch excised returns `kind === "mt5-terminal-error"` — the fallback the phase exists to eliminate.
    - Surgical control: the SAME mutant still returns `mt5-ipc-timeout` for `10005.txt` and `mt5-no-ipc` for `10004.txt`, proving the excision removed one branch and not a region.
    - The mutant text is asserted to differ from the original before it is used.
    - Nothing is written into the working tree; the temp directory is removed in a `finally`.
  </behavior>

  <action>
    Add a `describe` block to `src/__tests__/prod-prober-wiring.test.ts` with THREE `it()`s, each
    carrying the literal `164.8.3 FALSIFIER` in its name (the verify filters and counts on it).
    Split them deliberately — one RED credited to three controls is the failure
    `prod-prober-wiring.test.ts:1840-1844` names by name.

    **Building the mutant.** Read `scripts/prod-prober/arms/mt5.mjs` into a string. Locate the
    `-6` branch's opening line with `anchorIndex` — ⛔ `anchorIndex`, never `indexOf`: on a miss
    `indexOf` returns `-1` and `slice(-1)` reads from the END of the file, producing a mutant that
    is a near-copy of the original and a test that passes for the wrong reason. Find the branch's
    closing brace by searching FORWARD from the anchor for the block terminator, and excise the
    span. Assert `expect(mutant).not.toBe(original)` before using it. Write the mutant to a file
    named with a `.mjs` extension inside `mkdtempSync(join(tmpdir(), …))`, `await import` it via
    `pathToFileURL(...).href`, and `rmSync(dir, { recursive: true, force: true })` in a `finally`.
    ⭐ The arm has ZERO imports (measured), so the mutant module stands alone — this is the
    property that makes the whole approach possible, and it is worth a comment so a future import
    added to the arm reveals itself here rather than as a puzzling failure.

    **`it()` #1 — the control.** The SHIPPING `classifyProbe`, driven on `6.txt`'s file contents,
    returns `mt5-not-authorized`. Build the probe result object the same way the arm's caller does
    (`{ status: 0, stdout: <file text>, stderr: "", timedOut: false, measureFail: null }`).

    **`it()` #2 — the falsifier.** The MUTANT's `classifyProbe`, driven on the same transcript,
    returns `mt5-terminal-error`. Write the assertion message so it states what the RED means:
    without the `-6` branch, a real live reading falls back into the catch-all whose remedy sends
    the operator to a lookup table — which is precisely the defect this phase removed, now
    demonstrated rather than asserted.

    **`it()` #3 — the surgical-excision control.** The MUTANT still classifies `10005.txt` as
    `mt5-ipc-timeout` and `10004.txt` as `mt5-no-ipc`. ⭐ Without this, `it()` #2 would pass just
    as well if the excision had deleted the whole classifier tail — a green from a broken mutant
    is not evidence about the `-6` branch.

    ⛔ Write nothing into `scripts/`. ⛔ Do not `git checkout` anything. There is no restore step
    in this task by construction, which is the point of choosing this idiom for the durable half.
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts -t '164.8.3 FALSIFIER' 2>&amp;1 | tee /dev/stderr | grep -qE 'Tests +3 passed'</automated>
    <fails_when>non-zero exit; or the summary does not contain `Tests  3 passed`. A summary reading `Tests  no tests`, `0 passed`, or the line `No test files found` means the `-t` filter matched nothing — the falsifier did not run and the green is vacuous. A thrown `anchor not found` names a moved or renamed `-6` branch.</fails_when>

    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts &amp;&amp; node scripts/prod-prober/run.mjs --self-test 2&gt;&amp;1 | grep -q 'SELF-TEST PASSED: 80/80' &amp;&amp; test -z "$(git status --porcelain -- scripts/)"</automated>
    <fails_when>non-zero exit. The vitest leg failing means the file is red. The self-test leg failing means the count moved, which this plan must not do. The `test -z` failing means `git status --porcelain` printed a line under `scripts/` — the falsifier wrote into the working tree instead of a temp directory, or a mutant was left behind.</fails_when>
  </verify>

  <acceptance_criteria>
    - Three `it()`s exist, each named with `164.8.3 FALSIFIER`, split one property each (C6).
    - The mutant is built with `anchorIndex`, asserted to differ from the original, written only under `mkdtempSync`, and removed in a `finally`.
    - The falsifier observes the FALLBACK to `mt5-terminal-error`, and the surgical control proves only the one branch was removed.
    - `git status --porcelain -- scripts/` is silent after the run.
  </acceptance_criteria>

  <done>
    `npx vitest run src/__tests__/prod-prober-wiring.test.ts -t '164.8.3 FALSIFIER'` reports
    `Tests  3 passed`; the full file passes; the self-test is still `80/80`; nothing under
    `scripts/` is dirty (C6).
  </done>
</task>

<task type="auto">
  <name>Task 2: two on-disk levers, each observed RED alone, restored from bytes and cmp-verified</name>

  <precondition>The working tree is clean for `scripts/prod-prober/` and `src/__tests__/prod-prober-wiring.test.ts` before the first `cp` backup is taken — `git status --porcelain -- scripts/prod-prober src/__tests__/prod-prober-wiring.test.ts` prints nothing. A backup taken over a dirty tree restores the dirt, and a `cmp` against it would pass while the real state is lost.</precondition>

  <read_first>
    - `src/__tests__/prod-prober-wiring.test.ts:1899-1912` — the recorded-neuter comment format to copy (dated, per-lever, with the observed counts).
    - `git show 98357965 --stat` and its commit message — the `cp aside / one edit each / cp back / cmp-verified (RESTORE-CMP-OK both times)` ritual, verbatim.
    - `scripts/prod-prober/run.mjs:1146-1156` — the red loop's per-fixture assertions, so you know which message lever 1 will print.
    - `scripts/prod-prober/run.mjs:1107-1114` — the REMEDIES length and uniqueness gates, so you know which message lever 2 will print.
    - `scripts/prod-prober/arms/mt5.mjs` — the `-6` branch and the `REMEDIES` table as shipped.
  </read_first>

  <files>src/__tests__/prod-prober-wiring.test.ts</files>

  <behavior>
    - Lever 1 (the `-6` branch deleted, REMEDIES left live): `--self-test` FAILS, printing a line of the form `the defect kind is mt5-not-authorized (got mt5-terminal-error)`.
    - Lever 2 (the branch restored; `REMEDIES["mt5-not-authorized"]` pointed at the catch-all's exact string): `--self-test` FAILS, printing `no two mt5 remedies are the same string`.
    - After each restore, `cmp` against the pristine backup is silent and `--self-test` is green at `80/80`.
    - The final committed diff touches only `src/__tests__/prod-prober-wiring.test.ts`.
  </behavior>

  <action>
    ⛔ **Read the prohibitions in this plan's frontmatter before touching a file. `git checkout --`
    is FORBIDDEN in this harness: it restores to HEAD and silently destroys uncommitted work.**

    Run the two levers SEQUENTIALLY, never together — the matrix darkens each alone with the
    other left live, so each observed RED is credited to exactly one control.

    **Setup.** `BK="$(mktemp -d)"`, then `cp scripts/prod-prober/arms/mt5.mjs "$BK/mt5.mjs.pristine"`.
    Confirm the precondition first: `git status --porcelain -- scripts/prod-prober
    src/__tests__/prod-prober-wiring.test.ts` must print nothing. ⛔ Do not use an absolute home
    path anywhere; `.planning/` is TRACKED and this repo is PUBLIC.

    **Lever 1 — the classifier branch.** Delete the `-6` branch from
    `scripts/prod-prober/arms/mt5.mjs`, leaving `REMEDIES` untouched. Run
    `node scripts/prod-prober/run.mjs --self-test`. ⭐ **OBSERVE the RED — do not assert it.**
    Capture the failing line and the final banner verbatim. Expect a failure naming the fixture
    and both kinds, from `run.mjs:1152`. Restore: `cp "$BK/mt5.mjs.pristine"
    scripts/prod-prober/arms/mt5.mjs`, then `cmp` the two files — a silent `cmp` is the
    `RESTORE-CMP-OK` evidence. Re-run the self-test and confirm `80/80` green BEFORE proceeding.

    **Lever 2 — the remedy.** Re-take the byte backup first: a backup goes stale the moment the
    file changes again, and this is the step where that bites. Then point
    `REMEDIES["mt5-not-authorized"]` at the EXACT string `REMEDIES["mt5-terminal-error"]` already
    holds, leaving the classifier branch live. Run the self-test. Expect a failure from
    `run.mjs:1111-1114`. Capture it verbatim. Restore, `cmp`, re-run, confirm `80/80` green.

    **Record it.** Add a dated comment block in `src/__tests__/prod-prober-wiring.test.ts`
    immediately beside the Task 1 falsifier, in the format of `:1899-1912`: the date, each lever
    named, the failing line each printed QUOTED VERBATIM, `RESTORE-CMP-OK` for each restore, and
    the green count after each. ⛔ Paste what the runner printed; do not paraphrase and do not
    write a transcript you did not observe. Put the same summary in the commit message, following
    `98357965`'s shape.

    **Close out.** `git status --porcelain -- scripts/` must be silent. If it is not, the restore
    did not complete — restore from `"$BK/mt5.mjs.pristine"` again and re-verify with `cmp`. ⛔
    Do not "fix" a dirty tree with `git checkout --`.
  </action>

  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2&gt;&amp;1 | tail -2 | tee /dev/stderr | grep -q 'SELF-TEST PASSED: 80/80' &amp;&amp; test -z "$(git status --porcelain -- scripts/)"</automated>
    <fails_when>non-zero exit; or the tail does not read `SELF-TEST PASSED: 80/80`, meaning a neuter was left applied; or `test -z` fails because `git status --porcelain -- scripts/` printed a line — a file under `scripts/` is still modified and the restore did not complete. ⛔ Do not clear this with `git checkout --`; restore from the byte backup.</fails_when>

    <automated>node -e 'const t=require("fs").readFileSync("src/__tests__/prod-prober-wiring.test.ts","utf8"); const need=["the defect kind is mt5-not-authorized (got mt5-terminal-error)","no two mt5 remedies are the same string","RESTORE-CMP-OK"]; const miss=need.filter(s=&gt;!t.includes(s)); const dated=/MEASURED 20[0-9]{2}-[0-9]{2}-[0-9]{2}/.test(t); if (miss.length || !dated) { console.log("NEUTER-RECORD FAIL:" + (miss.length ? " missing " + JSON.stringify(miss) : "") + (dated ? "" : " no MEASURED <date> stamp")); process.exit(1); } console.log("NEUTER-RECORD OK: both levers quoted, restores cmp-verified, dated");'</automated>
    <fails_when>non-zero exit, or the printed line starts with `NEUTER-RECORD FAIL:`. `missing [...]` names which evidence is absent: the lever-1 failure line, the lever-2 failure line, or a `RESTORE-CMP-OK` marker. `no MEASURED &lt;date&gt; stamp` means the record is undated, and an undated measurement in this repo is a claim rather than evidence. ⚠️ This gate can only prove the record EXISTS — the observation itself is the human-run half of criterion 6, which is why Task 1's durable falsifier carries the re-running proof.</fails_when>

    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
    <fails_when>non-zero exit, or the summary line does not read `Test Files  1 passed` — the recorded comment was added inside a code region rather than beside it, or a lever edit survived into the committed file.</fails_when>
  </verify>

  <acceptance_criteria>
    - TWO levers were run, each ALONE with the other left live, and each printed a RED from the real `--self-test` (C6).
    - Each restore came from a `cp` byte backup and was `cmp`-verified; the backup was re-taken between levers.
    - Both failing lines are recorded VERBATIM, dated, beside the Task 1 falsifier, in the `:1899-1912` format.
    - ⛔ `git checkout --` was not used at any point, and `git status --porcelain -- scripts/` is silent.
    - The committed diff for this task touches only `src/__tests__/prod-prober-wiring.test.ts`.
  </acceptance_criteria>

  <done>
    `node scripts/prod-prober/run.mjs --self-test` prints `SELF-TEST PASSED: 80/80`; nothing under
    `scripts/` is dirty; the record check prints `NEUTER-RECORD OK: …`; and the wiring test file
    passes (C6).
  </done>
</task>

<task type="auto">
  <name>Task 3: book the one adjacent observation this phase must NOT fix, with a named owner</name>

  <read_first>
    - `.github/workflows/prod-prober.yml:343-351` — the `Capture the cron manifest` step, which still carries `node … --capture-manifest --out …` followed by `status=$?` on the NEXT line, under the same `shell: /usr/bin/bash -e {0}`.
    - `src/__tests__/prod-prober-wiring.test.ts:349-363` — the branch scan that pins the OTHER steps, and `:357`'s filter on `$RUNNER_LOG`, which is WHY this step is invisible to it.
    - `TODOS.md` — the entry format: `- [ ] **[ID] title (booked YYYY-MM-DD)** — body`, under a phase heading; and the standalone `### [ID] title` form.
    - `.planning/ROADMAP.md` — Phase 164.8.4 GATERESIDUE's goal paragraph, including its explicit *"⚠️ NOT A PRECEDENT. A second outside item may be admitted only by the same explicit decision, recorded the same way."*
  </read_first>

  <files>TODOS.md</files>

  <behavior>
    - `TODOS.md` gains one entry with the id `[164.8.3-CAPTURE-MANIFEST-ERREXIT]`, carrying the measured shape, the reason it is harmless today, the reason it is booked rather than fixed, and a NAMED destination phase.
    - No file under `.github/` or `scripts/` is touched.
  </behavior>

  <action>
    RESEARCH found exactly one adjacent observation and its verdict was *"Dead code, not a defect
    — book it, do not fix it here."* Book it; do not fix it.

    **The observation, measured:** the `Capture the cron manifest` step in
    `.github/workflows/prod-prober.yml` still uses the pre-`604d655f` shape — a bare `node …`
    call followed by `status=$?` on the next line — under GitHub's default `shell: /usr/bin/bash
    -e {0}`. It is behaviourally harmless TODAY because errexit exits with the same status the
    next line would have captured and there is nothing between them. It is invisible to the
    wiring test's branch scan because that scan filters on `$RUNNER_LOG`
    (`prod-prober-wiring.test.ts:357`). It is a latent trap rather than a live defect: inserting
    any line between the call and the capture silently changes the step's behaviour, and it is
    the exact shape the wiring test reds on everywhere else.

    Write the entry under a new `## Phase 164.8.3 (PROBERAUTH) — residuals (logged <today>)`
    heading in `TODOS.md`, using the `- [ ] **[ID] title (booked YYYY-MM-DD)** — body` form. The
    body must carry: the anchor cited BY SYMBOL (the step's `- name:` key, not a line number —
    this file has a recorded `[164.7-CITATION-DRIFT-01]` class about re-numbering prose that then
    drifts); the measurement; why it is harmless today; why it is invisible to the existing pin;
    and the remedy shape (`|| status=$?` on the SAME line plus a `status=0` initialiser, exactly
    as `604d655f` did for the two `$RUNNER_LOG` branches) together with the note that the wiring
    test's branch scan would need widening past `$RUNNER_LOG` for a pin to cover it.

    **The destination — ⛔ never leave it blank.** Name **Phase 164.8.4 GATERESIDUE** as the
    destination, and in the SAME sentence record that 164.8.4's goal carries an explicit
    *"NOT A PRECEDENT"* fence requiring an explicit founder decision, recorded the same way, to
    admit an outside item — so this entry is a NOMINATION awaiting that decision, not a silent
    insertion. ⛔ Do NOT edit `.planning/ROADMAP.md` in this task: roadmap phase CRUD goes
    through `/gsd-phase --edit`, never a hand-edit. Surface the nomination in this plan's SUMMARY
    `<deferred>` block so the orchestrator can route it.

    ⛔ Change nothing in `.github/workflows/prod-prober.yml`. Fixing it here would be scope this
    phase's own fence excludes, and the whole point of the entry is that the class is worth a
    decision rather than a drive-by edit.
  </action>

  <verify>
    <automated>node -e 'const t=require("fs").readFileSync("TODOS.md","utf8"); const bad=[]; if (!t.includes("[164.8.3-CAPTURE-MANIFEST-ERREXIT]")) bad.push("NO_ENTRY"); const i=t.indexOf("[164.8.3-CAPTURE-MANIFEST-ERREXIT]"); const body=i&gt;=0?t.slice(i,i+4000):""; if (!/164\.8\.4/.test(body)) bad.push("NO_NAMED_DESTINATION_PHASE"); if (!/Capture the cron manifest/.test(body)) bad.push("NO_SYMBOL_ANCHOR"); if (!/NOT A PRECEDENT/.test(body)) bad.push("NO_ADMISSION_FENCE_NOTE"); if (bad.length) { console.log("DEFERRAL-BOOKING FAIL: " + bad.join(" ")); process.exit(1); } console.log("DEFERRAL-BOOKING OK: booked with a named destination and the admission fence recorded");'</automated>
    <fails_when>non-zero exit, or the printed line starts with `DEFERRAL-BOOKING FAIL:`. `NO_ENTRY` = the item was not booked at all; `NO_NAMED_DESTINATION_PHASE` = the entry names no owning phase, which this project forbids outright; `NO_SYMBOL_ANCHOR` = the step is cited by line number instead of by its `- name:` symbol, the `[164.7-CITATION-DRIFT-01]` class; `NO_ADMISSION_FENCE_NOTE` = the entry inserts into a fenced phase without recording that the fence requires an explicit decision.</fails_when>

    <automated>S="$(git status --porcelain -- .github scripts)"; test -z "$S" || { echo "SCOPE FAIL: $S"; exit 1; }; D="$(git diff --name-only HEAD -- TODOS.md)"; test "$D" = "TODOS.md" || { echo "BOOKING FAIL: git diff against HEAD for TODOS.md printed [$D]"; exit 1; }; echo "SCOPE OK: nothing under .github or scripts changed; TODOS.md changed"</automated>
    <fails_when>non-zero exit, or the output starts with `SCOPE FAIL:` or `BOOKING FAIL:`. `SCOPE FAIL:` names a modified file under `.github/` or `scripts/` — this task must touch neither, and fixing the capture-manifest step here is explicitly out of scope. `BOOKING FAIL: … printed []` means `TODOS.md` shows no change against HEAD, so nothing was actually booked. Each `git` status is captured into a variable before it is tested, so a failing `git` cannot be swallowed by a pipeline's last stage.</fails_when>
  </verify>

  <acceptance_criteria>
    - `TODOS.md` carries `[164.8.3-CAPTURE-MANIFEST-ERREXIT]` with the measurement, the harmlessness argument, the invisibility argument, and the remedy shape.
    - The entry names **Phase 164.8.4 GATERESIDUE** as the destination AND records that phase's `NOT A PRECEDENT` admission fence, so the routing is a nomination rather than a silent insertion.
    - The step is cited by its `- name:` symbol, not by a line number.
    - `.github/workflows/prod-prober.yml` is byte-unchanged, and so is everything under `scripts/`.
    - The plan SUMMARY carries a `<deferred>` block naming the same destination.
  </acceptance_criteria>

  <done>
    The booking check prints `DEFERRAL-BOOKING OK: …`; `git status --porcelain -- .github scripts`
    is silent; and `TODOS.md` is the only file this task changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → working tree | Task 2 mutates a tracked source file on disk and restores it. This is the only step in the phase that can DESTROY work. |
| test → filesystem | Task 1 writes a mutant module; it must land only under a `mkdtempSync` directory. |
| repo → PUBLIC `.planning/` and `TODOS.md` | both are tracked in a PUBLIC repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.3-14 | Denial of Service (loss of work) | the on-disk neuter harness in Task 2 | high | mitigate | `cp` byte backups only, re-taken between levers; `cmp` after each restore; `git status --porcelain -- scripts/` asserted silent in `<verify>`; `git checkout --` prohibited in the frontmatter AND in the action AND in both `<fails_when>` remedies. The plan is given its own wave so no concurrent editor shares the tree. |
| T-164.8.3-15 | Tampering | a mutant module escaping into the working tree | medium | mitigate | Task 1 writes only under `mkdtempSync(join(tmpdir(), …))` with a `finally` `rmSync`; the verify asserts `git status --porcelain -- scripts/` is silent. |
| T-164.8.3-16 | Repudiation | a recorded RED transcript that was never observed | medium | mitigate | The record gate requires the runner's OWN failure strings (`run.mjs:1152` and `run.mjs:1113`) plus a `MEASURED <date>` stamp and `RESTORE-CMP-OK`. ⚠️ Stated limit: this proves the record exists, not that it was observed — which is exactly why Task 1's durable falsifier carries the re-running half. |
| T-164.8.3-17 | Information Disclosure | an absolute home path or a credential pasted into a recorded transcript | medium | mitigate | The action mandates `mktemp -d` and forbids absolute home paths; the transcripts are self-test output, which is fixtures-only and touches no credential (`SELFTEST_ENV`). |
| T-164.8.3-06 | Tampering | npm/pip/cargo installs | n/a | accept | No packages are installed. RESEARCH.md's `## Package Legitimacy Audit` records NOT APPLICABLE; no `[ASSUMED]`/`[SUS]`/`[SLOP]` candidates exist, so no legitimacy checkpoint is inserted. |
</threat_model>

<verification>
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts -t '164.8.3 FALSIFIER'` → `Tests  3 passed`.
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts` → 1 file passed.
- `node scripts/prod-prober/run.mjs --self-test` → `SELF-TEST PASSED: 80/80`, exit 0.
- `git status --porcelain -- scripts/ .github/` → silent.
- `git diff --name-only` lists exactly `src/__tests__/prod-prober-wiring.test.ts` and `TODOS.md`.

**Phase gate, after this plan:** the full `npm test` suite green, plus both observed RED
transcripts present in the commit history.
</verification>

<success_criteria>
- ROADMAP criterion 6 is discharged on BOTH halves: a durable falsifier that re-runs on every CI shard, and two levers observed RED through the real self-test, each restored from bytes and `cmp`-verified.
- No work was destroyed and no `git checkout --` was run in the harness.
- The single adjacent observation this phase found is booked with a NAMED destination and its admission fence recorded.
</success_criteria>

<output>
Create `.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-04-SUMMARY.md` when done.
Record in it: both RED transcripts verbatim, both `RESTORE-CMP-OK` confirmations, the `Tests  3 passed` line, and a `<deferred>` block naming `[164.8.3-CAPTURE-MANIFEST-ERREXIT]` → **Phase 164.8.4 GATERESIDUE (nomination — admission fence applies)**.
</output>