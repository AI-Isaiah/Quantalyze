# Phase 164.8.3: PROBERAUTH - Context

**Gathered:** 2026-09-13
**Status:** Ready for planning
**Mode:** Smart discuss (autonomous, batch proposals — founder accepted all five, plus one routing decision)

<domain>
## Phase Boundary

The prod-prober's MT5 arm gives a dedicated defect kind to `-6`, so the operator is told what is
wrong and what to do instead of being sent to the MT5 error table.

⛔ **IN scope:** the prober's defect VOCABULARY — the `-6` kind, its remedy text, its fixture and
registration, the catch-all's continued meaning, the `terminal_info()` recording, the falsifier,
and the narrowed `--arm` diagnostic's output.

⛔ **NOT in scope, and named so nobody re-acquires it:**
- **Clearing the live outage.** The terminal has no authorized account; restoring it needs a VNC
  session and broker credentials and is a founder action. ⛔ **A green prober is NOT this phase's
  acceptance signal** — it is not in this phase's gift.
- **Making the gateway log itself back in.** That is Phase 164.6.2 MT5RELOGIN.
- **Making the probe authenticate.** Criterion 5's fence: `scripts/mt5-diag.sh` stays READ-ONLY.
  A login is an "account change", and MT5 re-clears `[Experts] Enabled` on every account change
  while `Account=1` is armed — a probe that authenticated would re-break the thing it measures
  (recorded 2026-08-13 incident, where each diagnostic round re-disabled algo trading).

⭐ **OWNERSHIP, settled 2026-09-13 (founder):** this phase is the NAMED OWNER of the `-6`
vocabulary. Phase 164.6.2 carried a duplicate naming criterion in its title, goal and criterion 3;
all three were struck and deferred here. The ROADMAP's standing instruction — *"fold or defer
explicitly with a named owner"* — is discharged. Two grounds: this phase depends on Phase 164.1,
which owns the prober's MT5 arm and its defect vocabulary; and this phase is closeable WITHOUT the
founder, while 164.6.2 is founder-gated on an observed restart self-heal.

</domain>

<decisions>
## Implementation Decisions

### Founder-accepted proposals (smart discuss, 2026-09-13)

| # | Area | Decision | Why |
|---|------|----------|-----|
| 1 | New defect kind name | `mt5-not-authorized` | Names the STATE, not the code. The kind string reaches the filed issue title. |
| 2 | Fixture name | `6.txt` | Matches the bare-code convention already set by `10004.txt` and `10005.txt`. |
| 3 | Catch-all survival | Keep BOTH `terminal-info-null.txt` and `init-false-other.txt` mapping to `mt5-terminal-error` | Criterion 4 requires the bucket stay non-empty and meaningful. `init-false-other.txt` holds `-2 Invalid params` — a genuine non-IPC, non-auth code. Two fixtures, not one, so the bucket's discrimination is provable. |
| 4 | Remedy text | Names the **Journal** explicitly | Criterion 2's own ⭐ note: the Journal is the ONE place that distinguishes "invalid account" from "no connection", and neither the prober nor `mt5-diag.sh` can currently tell them apart. The 2026-09-10 root cause was only visible there. |
| 5 | `terminal_info()` recording | **Two booleans exactly** — `connected` and `trade_allowed`. Nothing else. | Those are precisely the two fields `docs/runbooks/mt5-go-live.md` Step 2 names as the verification. Founder rejected capturing the fuller dict and rejected reading the Journal from the probe — the latter would be new file access against criterion 5's READ-ONLY fence. |

### Claude's discretion

- Exact remedy wording, beyond the requirement that it names the VNC action, the "Save password"
  step, the two Expert-Advisors options a login re-clears, and the Journal — and that it does NOT
  route the operator to the MT5 error table.
- How the falsifier (criterion 6) is structured, provided the neuter is OBSERVED RED and restored
  from pristine bytes. ⛔ Never `git checkout --` in the harness.

</decisions>

<code_context>
## Existing Code Insights

Scouted 2026-09-13 at HEAD.

- **`scripts/prod-prober/arms/mt5.mjs`** — the arm. Its header already argues at length why `-10004`
  and `-10005` must not collapse; the `-6` split is the same argument applied once more. Current
  remedy for `mt5-terminal-error` is the sentence this phase removes: *"Read the reported code
  against the MT5 error table and the gateway container log for the same minute; neither IPC
  remedy applies here."*
- **`terminal_info` is collapsed today** at `arms/mt5.mjs:113-117`: the embedded probe sets
  `out["terminal_info"]` to the string `"present"` or to `None`. `:230` then derives only
  `terminalInfoPresent`. This is criterion 7's target.
- **Fixtures** live in `scripts/prod-prober/fixtures/mt5/`: `10004.txt`, `10005.txt`,
  `init-false-other.txt` (`-2 Invalid params`), `no-probe-line.txt`, `ok.txt`,
  `terminal-info-null.txt`.
- **The narrowed `--arm` path** is `.github/workflows/prod-prober.yml:274-281`.

⚠️ **A `kinds` grep in `arms/mt5.mjs` returned NOTHING** — criterion 3 says the new kind must be
"registered in the arm's `kinds` list, exactly as `10004.txt` and `10005.txt` are". The planner
must locate the actual registration mechanism (likely the fixture-to-kind coverage assertion in
`src/__tests__/prod-prober-wiring.test.ts` or the registry) rather than assuming a `kinds` array
exists in the arm file. ⛔ Do not invent one to satisfy the criterion.

</code_context>

<specifics>
## Specific Ideas

### ⛔ RESEARCH TASK ONE — criterion 8's premise is probably STALE, settle it before planning a fix

Criterion 8 asserts the narrowed `--arm` path "neither prints [`$RUNNER_LOG`] nor uploads it",
measured 2026-09-10 on dispatch run `34497471175`.

**MEASURED AT HEAD 2026-09-13, and it does not agree:** `prod-prober.yml:281` runs
`cat "$RUNNER_LOG"` UNCONDITIONALLY after both the narrowed and full branches, and
`git log -L 281,281` dates that line to `42868a9b` (**2026-09-06**) — FOUR DAYS BEFORE the
criterion's own measurement. So the stated cause cannot be right as written.

⚠️ It could not be settled from `gh run view --log-failed`: that view TRUNCATES, which this repo
has a dated record of, and the steps came back labelled `UNKNOWN STEP`. What IS visible is that
the job's `Probe production` step concluded `failure` while `Self-test` concluded `success`, and
the only `NARROWED DIAGNOSTIC` lines in the visible portion belong to the SELF-TEST (arm
`pyapi06`), not to the live probe.

**The research step's FIRST job, founder decision 2026-09-13:** fetch run `34497471175`'s log
ARCHIVE — not `gh run view --log` — and determine what that run actually did. Then either:
(a) plan the real fix, if a genuine gap is found; or
(b) mark criterion 8 ALREADY MET, with the evidence, and record the correction in the ROADMAP.

⛔ Do NOT ship an artifact upload merely because criterion 8 offers it as an option. Shipping a
change whose justifying defect is unverified is the vacuity class this milestone exists to remove.

### Evidence to carry into planning (dated, do not re-derive)

- **`-6` is `RES_E_AUTH_FAILED`.** Measured live from this checkout 2026-09-09:
  `PROBE {"initialize": false, "last_error": [-6, "Terminal: Authorization failed"], "terminal_info": null}`
  The rpyc bridge answered (a dead bridge gives `-10004`), so the terminal is up and un-authorized.
- **The Journal is where the answer was**, 2026-09-10:
  `'<key A>': authorization on VantageMarkets-Live 14 failed (Invalid account)` — the terminal
  dropped a working session and re-attached to an account it could not authorize, then failed
  identically for three days. Resolved ~16:10Z by re-entering valid credentials.
- **STILL FIRING 2026-09-13** — run `34758502223` at `f10b0e2`: `arms: 4/4/0`, one defect,
  `kind: mt5-terminal-error`, `subject: -6`, with the target remedy sentence. ⭐ This is a SECOND
  loss of session, three days after the hand fix — which is 164.6.2's problem, not this phase's,
  and is the evidence that the two phases are separate live problems.
- ⭐ **The prober is behaving CORRECTLY in that reading.** It refused to implicate IPC and said so.
  Phase 164.1 criterion 3 is working. The defect is the remedy text, not the detection.

</specifics>

<deferred>
## Deferred Ideas

- **Capturing the fuller `terminal_info` dict** (build, company, path, `tradeapi_disabled`) —
  considered and REJECTED by the founder 2026-09-13 in favour of the two booleans exactly.
  `tradeapi_disabled` is already used by the Phase 161 WIZERR-01 flag→cause builder, so a future
  phase may want it; it is not this phase's business.
- **Reading the MT5 Journal from the probe** — considered and REJECTED 2026-09-13. It is the
  highest-diagnostic option and it is new file access from a probe whose criterion 5 fence exists
  to keep it minimal and read-only. The remedy NAMES the Journal so a human reads it; the probe
  does not.
- **Clearing the live outage** — founder action, out of scope by the phase's own fence.
- **The gateway's self-healing login** — Phase 164.6.2 MT5RELOGIN.

</deferred>
