---
phase: "164.8.3"
plan: "01"
subsystem: infra
tags: [prod-prober, mt5, defect-kinds, github-actions, anti-vacuity, vitest]

requires:
  - phase: "164.1"
    provides: "the prod-prober harness — DEFECT_KINDS, ARM_FIXTURE_TABLE, KIND_ASSERTIONS, the --self-test scenario counter, and the mt5 arm"
  - phase: "164.8.2"
    provides: "the ANCHOR DISCIPLINE helpers (`anchorIndex`, `sliceBetweenAnchors`) that make a narrowing slice fail loud instead of degenerating"
provides:
  - "`mt5-not-authorized` — a first-class defect kind for MT5 `-6`, replacing the residual `mt5-terminal-error` whose remedy said 'read the code against the MT5 error table'"
  - "`scripts/prod-prober/fixtures/mt5/6.txt` — the committed red fixture carrying the live 2026-09-09 transcript shape"
  - "`REMEDIES[\"mt5-not-authorized\"]` — a single-action remedy (VNC → Journal FIRST → investor login → re-arm Expert Advisors) that names no account number, broker server or password"
  - "`AUTO-ISSUE DEDUP` — three tests pinning that prod-prober issue SELECTION is kind-independent, plus criterion 8's MET-BEFORE-START counts"
affects: [164.8.3 plan 02, 164.8.3 plan 03, prod-prober, mt5-gateway operations]

actuals:
  tokens: 62000
  tasks: 2
  commits: 4  # MEASURED: git rev-list --count 56ecac37..HEAD — 2 task commits + SUMMARY + STATE/ROADMAP
plan_head_before: 56ecac37c1ceb683ea02832b08d05a6879d38ded

tech-stack:
  added: []
  patterns:
    - "Five INDEPENDENT hand-typed registrations for one defect kind (DEFECT_KINDS / fixture-table `kinds:` / `red:` / KIND_ASSERTIONS / EXPECTED_DEFECT_KINDS) — a SET-equality gate refuses a partial landing, so the kind lands atomically or not at all"
    - "A workflow-slice gate that ranges over an EXPORTED roster rather than a hand-typed list, so it widens by itself when the roster grows"

key-files:
  created:
    - scripts/prod-prober/fixtures/mt5/6.txt
  modified:
    - scripts/prod-prober/arms/mt5.mjs
    - scripts/prod-prober/run.mjs
    - src/__tests__/prod-prober-wiring.test.ts

key-decisions:
  - "`mt5-terminal-error` keeps its name and its lookup-table remedy (assumption-delta `no-change`): it is a RESIDUAL bucket whose noun is already correctly non-specific, and its lookup instruction is CORRECT advice for a genuinely unenumerated code such as the `-2` that `init-false-other.txt` carries. Only its enumeration sentence was widened."
  - "Branch (6b) sits between the `-10005` branch and branch (7), never below branch (8) — branch (8) is the unguarded tail, so anything under it is dead code nothing at review time would name."
  - "The `run.mjs` roster prose was NOT bumped to 'ALL TWENTY-ONE KINDS WERE REGISTERED UP FRONT' — that sentence is a historical claim and the twenty-first was not. The live count was corrected and a separate ⚠️ paragraph records when and how the twenty-first landed. (Deviation 1.)"
  - "The criterion-8 pin counts MATCHES with comment lines filtered, and ALSO asserts the unfiltered count is 3 — so the filter is a measured reading rather than an unexamined formality."

patterns-established:
  - "Tracer-slice RED: register the fixture and every counter FIRST, run the suite, and capture the named failure (`the defect kind is mt5-not-authorized (got mt5-terminal-error)`) before writing the classifier branch — RED observed without a knowingly-red commit."
  - "Workflow-file neuter proof: `cp` byte backup → mutate → observe RED → `cp` restore → `cmp` + `git status --porcelain` clean. Never `git checkout --`."

requirements-completed: ["#753"]

coverage:
  - id: D1
    description: "A `railway ssh` transcript carrying `last_error: [-6, …]` produces a defect row whose kind is `mt5-not-authorized` and whose subject is `-6` — not `mt5-terminal-error`"
    requirement: "#753"
    verification:
      - kind: integration
        ref: "node scripts/prod-prober/run.mjs --self-test  # scenario 24/79: 'mt5-not-authorized fires on 6.txt, and NOTHING else does'"
        status: pass
    human_judgment: false
  - id: D2
    description: "The row's remedy names the VNC console, the \"Save password\" step, the Expert-Advisors options a login re-clears, and the terminal's Journal — and routes the operator nowhere else"
    requirement: "#753"
    verification:
      - kind: other
        ref: "node --input-type=module -e '… REMEDIES[\"mt5-not-authorized\"] …'  # REMEDY-CHECK OK (length/digit-run/uniqueness)"
        status: pass
      - kind: other
        ref: "content probe: VNC=true, 'Save password'=true, 'Expert Advisors'=true, Journal=true, /error table/i=false, /modal/i=false"
        status: pass
    human_judgment: false
  - id: D3
    description: "`mt5-terminal-error` still fires, on BOTH `init-false-other.txt` and `terminal-info-null.txt`, with two distinct subjects (`-2` and `terminal_info`)"
    requirement: "#753"
    verification:
      - kind: integration
        ref: "node scripts/prod-prober/run.mjs --self-test  # scenarios 26/79 and 27/79"
        status: pass
    human_judgment: false
  - id: D4
    description: "The self-test reports a PASSED banner at the new scenario count and prints a header naming `mt5-not-authorized` on `6.txt`"
    requirement: "#753"
    verification:
      - kind: integration
        ref: "node scripts/prod-prober/run.mjs --self-test  # 'SELF-TEST PASSED: 79/79', exit 0"
        status: pass
      - kind: unit
        ref: "src/__tests__/prod-prober-wiring.test.ts#SELF_TEST_SCENARIOS is 79, and the runner PRINTS exactly 79 headers numbered 1..79"
        status: pass
    human_judgment: false
  - id: D5
    description: "The auto-issue step's issue SELECTION is provably independent of every defect kind, so a `-6` under the new name cannot be suppressed by, nor open a second issue beside, the live `prod-prober`-labelled issue"
    requirement: "#753"
    verification:
      - kind: unit
        ref: "src/__tests__/prod-prober-wiring.test.ts#AUTO-ISSUE DEDUP: the step selects on a CONSTANT label and mentions ZERO defect kinds"
        status: pass
      - kind: unit
        ref: "src/__tests__/prod-prober-wiring.test.ts#AUTO-ISSUE DEDUP: CALIBRATION — a kind-derived label is CAUGHT by the same predicate"
        status: pass
    human_judgment: false
  - id: D6
    description: "Criterion 8's MET-BEFORE-START claim is re-measured at HEAD and pinned, and no line of `.github/workflows/prod-prober.yml` is changed"
    requirement: "#753"
    verification:
      - kind: unit
        ref: "src/__tests__/prod-prober-wiring.test.ts#AUTO-ISSUE DEDUP: criterion 8 was MET BEFORE THIS PHASE — both status captures and the one `cat`, PINNED"
        status: pass
      - kind: other
        ref: "test -z \"$(git status --porcelain -- .github/workflows/prod-prober.yml scripts/mt5-diag.sh)\""
        status: pass
    human_judgment: false

duration: 14 min
completed: 2026-09-13
status: complete
---

# Phase 164.8.3 Plan 01: the `mt5-not-authorized` kind, wired end-to-end Summary

**MT5 `-6` now reports as `mt5-not-authorized` with a single-action remedy (VNC → Journal first → investor login → re-arm Expert Advisors) instead of as the residual `mt5-terminal-error` whose "read the code against the MT5 error table" instruction cost two real investigations; and the auto-issue publication path is now a calibrated test proving issue selection cannot read a defect kind.**

## Performance

- **Duration:** 14 min
- **Tasks:** 2 of 2
- **Files touched:** 4 (1 created, 3 modified)
- **Commits:** 4 — `cb34fa26` (feat, task 1), `4661d822` (test, task 2), the SUMMARY commit, and the STATE/ROADMAP commit this line was folded into. MEASURED with `git rev-list --count 56ecac37..HEAD`, not narrated.

## Accomplishments

1. **`mt5-not-authorized` landed as ONE atomic slice.** New fixture, new classifier branch `(6b)`, new remedy, five independent hand-typed registrations and both scenario counters, in a single commit — because the runner's SET-equality gate (`run.mjs`), its `KIND_ASSERTIONS` FAIL-on-missing loop, and the wiring test's coverage extractor each independently refuse a partial landing.
2. **The remedy stopped being a lookup instruction.** `-6` has exactly one cause and one remedy, so the old residual advice *"Read the reported code against the MT5 error table"* WAS the defect. The new string names the Journal tab first (the only place that separates a rejected account from a lost broker connection), then the investor login, then the Expert-Advisors options a login re-clears.
3. **The residual survived, narrowed only where it had gone stale.** `mt5-terminal-error` still fires on both its fixtures with two distinct subjects; only branch (8)'s enumeration sentence widened from *"neither -10004 nor -10005"* to the three enumerated codes.
4. **The dedup-key proof became a machine gate with a calibration.** `DEFECT_KINDS.filter((k) => slice.includes(k))` over the auto-issue step's 3419-char slice is `[]`, ranged over the EXPORTED roster so it covered `mt5-not-authorized` the moment task 1 registered it.
5. **Criterion 8 was re-measured at HEAD and pinned as MET-BEFORE-START.** No code was written for it and `prod-prober.yml` is byte-unchanged.

## The numbers the plan asked to be recorded

**The self-test's TRUE counted scenario number — MEASURED, not derived.** The run's own tail assertion printed it:

```
  ok — the counted scenario set is complete: 79 header(s) printed, SELF_TEST_SCENARIOS is 79

=== SELF-TEST PASSED: 79/79 scenarios, every arm's kinds fired on their own fixtures and nowhere else ===
```

`79` — the plan's figure and the run agree, so both constants were set to 79.

**The `Tests  N passed` line from the `-t 'AUTO-ISSUE DEDUP'` run:**

```
 Test Files  1 passed (1)
      Tests  3 passed | 78 skipped (81)
```

**`REMEDIES["mt5-not-authorized"]` exactly as shipped (985 chars):**

> The bridge ANSWERED but NO ACCOUNT IS AUTHORIZED on the terminal (-6). Open the gateway's VNC console on the mt5-gateway service and read the terminal's \*\*Journal\*\* tab FIRST: it is the one place that separates a rejected account from a lost broker connection, and neither this prober nor \`scripts/mt5-diag.sh\` can tell those two apart. Then log the terminal back into the INVESTOR (read-only) account with the "Save password" box ticked, so the login survives a restart. Then re-check \*Tools → Options → Expert Advisors\* — a login is an ACCOUNT CHANGE, and MT5 re-clears those options on every account change; that is what re-disabled algo trading on each diagnostic round during the 2026-08-13 investigation. ⛔ A redeploy does NOT fix this: the saved login lives on the persistent volume, so a terminal with no usable credential comes straight back with no usable credential. Confirmed fixed when terminal_info() reports BOTH connected true AND trade_allowed true — both, not either.

## Anti-vacuity evidence — every assertion shown able to FAIL

| Assertion | Neuter applied | Observed RED | Restore verified |
|---|---|---|---|
| `6.txt` → `mt5-not-authorized` | fixture + all five registrations landed, classifier branch (6b) and remedy withheld | `SELF-TEST FAIL: the defect kind is mt5-not-authorized (got mt5-terminal-error)`, exit 1, header printed as `24/79` | n/a — the branch was then written, not reverted |
| `AUTO-ISSUE DEDUP` kind-independence + its calibration | `const dedupLabel = "prod-prober";` → `"prod-prober-mt5-no-ipc"` in `prod-prober.yml` | `Tests  2 failed \| 1 passed \| 78 skipped` | `cmp` OK, `git status --porcelain` empty for the file |
| `AUTO-ISSUE DEDUP` criterion-8 pin | `--arm` branch reverted to the 2026-09-11 bare-call shape | `AssertionError: both probe-step branches must capture their own status ON THE SAME LINE (criterion 8): expected 1 to be 2`, `Tests  1 failed \| 2 passed` | `cmp` OK, `git status --porcelain` empty for the file |

⛔ Both workflow neuters were restored from a **byte backup taken with `cp`** and verified with `cmp`. `git checkout --` was never used — it restores to HEAD and silently destroys uncommitted work, which this repo has a dated record of.

The `\b[0-9]{6,}\b` scan over the mt5 remedies was itself calibrated with a **synthetic** repeated digit run (`999999` in a throwaway string, never a real account number): the predicate fires on the synthetic run and stays quiet on the dated `2026-08-13` the remedy legitimately carries. `REMEDY-CHECK OK`.

## Verification results

| Check | Result |
|---|---|
| `node scripts/prod-prober/run.mjs --self-test` | `SELF-TEST PASSED: 79/79`, **exit 0** |
| Task 1 verify leg 1 (compound: banner + 3 scenario headers + no stale-count line) | **exit 0** |
| `npx vitest run src/__tests__/prod-prober-wiring.test.ts` | `Test Files  1 passed (1)` · `Tests  81 passed (81)` |
| Task 1 verify leg 3 (remedy length / digit-run / uniqueness) | `REMEDY-CHECK OK`, exit 0 |
| Task 2 verify leg 1 (`-t 'AUTO-ISSUE DEDUP'` → `Tests +3 passed`) | **exit 0** |
| Task 2 verify leg 2 (full file + criterion-8 counts + byte-unchanged fence) | `CRITERION-8 PIN OK: status-captures=2, cat-runner-log=1`, **exit 0** |
| `git diff --name-only 56ecac37..HEAD` | exactly the four paths in `files_modified` — no `mt5-diag.sh`, no `prod-prober.yml`, no `ok.txt` |

Classifier behaviour across every mt5 fixture, measured directly on `classifyProbe`:

| Fixture | kind | subject |
|---|---|---|
| `6.txt` | `mt5-not-authorized` | `-6` |
| `10004.txt` | `mt5-no-ipc` | `-10004` |
| `10005.txt` | `mt5-ipc-timeout` | `-10005` |
| `init-false-other.txt` | `mt5-terminal-error` | `-2` |
| `terminal-info-null.txt` | `mt5-terminal-error` | `terminal_info` |
| `ok.txt` | `null` | `null` |

Measurement currency re-checked before execution: `git diff --name-only de1a3947..HEAD -- scripts/prod-prober .github/workflows/prod-prober.yml src/__tests__/prod-prober-wiring.test.ts scripts/mt5-diag.sh` returned EMPTY, so every anchor and line number in the plan still held byte-for-byte at HEAD `56ecac37`. The plan's own dedup measurements reproduced exactly: slice 3419 chars, zero kinds present, `dedupLabel` declared once, `labels: dedupLabel` once, `|| status=$?` filtered 2 / raw 3, `cat "$RUNNER_LOG"` 1.

## Deviations from Plan

### 1. [Rule 1 — Bug] The `run.mjs` roster prose numeral was corrected WITHOUT making a false historical claim

- **Found during:** Task 1, step (g).
- **Issue:** The plan instructs *"Correct both numerals"* — `run.mjs`'s *"ALL TWENTY KINDS WERE REGISTERED UP FRONT in plan 01"* and the test's *"Twenty names, sorted."* The test's sentence is a plain count of a list and bumping it to twenty-one is simply correct. The runner's sentence is not a count — it is a **historical claim about plan 01**, and twenty-one kinds were *not* registered up front. Bumping that numeral literally would have shipped a prose lie into the file whose entire argument is that prose must not drift from what was measured.
- **Fix:** The live-count half of the sentence now reads *"Every one of the twenty-one is now raised by a registered arm"*, and a separate `⚠️` paragraph records that the twenty-first, `mt5-not-authorized`, was NOT registered up front — it landed in phase 164.8.3 with its fixture, remedy, `KIND_ASSERTIONS` entry and both counters in the same commit, which is the rule rather than an exception to it. The surrounding argument is untouched, exactly as the plan required.
- **Files modified:** `scripts/prod-prober/run.mjs`
- **Verification:** `--self-test` 79/79; wiring test 81 passed.
- **Commit:** `cb34fa26`

### 2. [Rule 2 — Missing critical] The mt5 fixture entry's own kind-count comment was corrected

- **Found during:** Task 1, step (e2).
- **Issue:** The comment above the mt5 entry's `kinds:` array read *"⚠️ FOUR kinds, not five."* Appending `mt5-not-authorized` made that count stale on the very line it annotates — the same rot class the plan's step (g) exists to close, at an anchor the plan did not enumerate.
- **Fix:** Now reads *"⚠️ FIVE kinds, not six."* The argument it makes (why `mt5-probe-timeout` is deliberately absent from the table) is unchanged.
- **Files modified:** `scripts/prod-prober/run.mjs`
- **Verification:** covered by the same self-test run.
- **Commit:** `cb34fa26`

### 3. [Plan-directed] Task 1's RED and GREEN are ONE commit, not the standard TDD split

- **Found during:** Task 1.
- **Issue:** Task 1 carries `tdd="true"`, which normally means a `test(...)` RED commit followed by a `feat(...)` GREEN commit. The plan explicitly forbids that here: *"This is ONE commit. Every registry gate below refuses a partial landing, so a half-done slice is a knowingly-red intermediate state — do not split it."*
- **Fix:** RED was **observed and recorded** rather than committed — the fixture and all five registrations were landed first, the self-test was run, and it failed by name (`the defect kind is mt5-not-authorized (got mt5-terminal-error)`, exit 1) before the classifier branch existed. The failing output is quoted in the commit message so the RED is auditable from `git log` alone.
- **Impact:** No knowingly-red commit on the branch; the anti-vacuity property (the new assertion can fail) is preserved and evidenced.

**Total deviations:** 2 auto-fixed (1 prose-accuracy correction, 1 stale-count correction) + 1 plan-directed process note.
**Impact:** None on behaviour. Both auto-fixes are comment-only and were required to keep the file's own measured-prose discipline.

## Authentication Gates

None — this plan runs entirely on committed fixtures. No network call, no credential, no package install.

## Known Stubs

None. Every artifact the plan lists is on disk, registered, and exercised by a named scenario.

## Issues Encountered

None.

## Scope Fences Held

- `.github/workflows/prod-prober.yml` — byte-unchanged (asserted, and re-asserted after both neuter cycles). No artifact-upload step was added; `604d655f` already shipped the other alternative.
- `scripts/mt5-diag.sh` — byte-unchanged.
- `scripts/prod-prober/fixtures/mt5/ok.txt` — byte-unchanged (plan 03 owns it).
- `MT5_PROBE_PY` gained no call; its call set is still `initialize()` / `last_error()` / `terminal_info()`, byte-asserted by the self-test on every run above.
- No secret, account number, broker server name or absolute home path reached any tracked file. The only long digit run in the diff is `34706551355` — the GitHub Actions run id the plan **required** to be named as criterion 8's dated evidence, in a test comment, not in a remedy.

## Next Phase Readiness

Ready for `164.8.3-02`. Plan 02's machine gate over the DEFECT ROW has its four required elements present in the shipped remedy string (VNC console, "Save password", Expert Advisors, Journal), and `mt5-not-authorized` is in `DEFECT_KINDS`, so any gate that ranges over the exported roster will see it without further wiring.

## Self-Check: PASSED

- `scripts/prod-prober/fixtures/mt5/6.txt` — FOUND on disk (2 lines, 168 bytes).
- `scripts/prod-prober/arms/mt5.mjs`, `scripts/prod-prober/run.mjs`, `src/__tests__/prod-prober-wiring.test.ts` — FOUND, modified.
- Commit `cb34fa26` — FOUND in `git log`.
- Commit `4661d822` — FOUND in `git log`.
- All plan `<verification>` commands re-run after the final commit: self-test `79/79` exit 0; wiring test `81 passed`; `git diff --name-only` lists exactly the four `files_modified` paths.
</content>
</invoke>
