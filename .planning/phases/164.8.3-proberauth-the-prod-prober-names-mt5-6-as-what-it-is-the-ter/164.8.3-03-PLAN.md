---
phase: "164.8.3"
plan: "03"
type: execute
wave: 3
depends_on: ["164.8.3-02"]
files_modified:
  - scripts/prod-prober/arms/mt5.mjs
  - scripts/prod-prober/fixtures/mt5/ok.txt
  - scripts/prod-prober/run.mjs
autonomous: true
requirements: ["#753"]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`MT5_PROBE_PY` records `terminal_info()` as EXACTLY two booleans — `connected` and `trade_allowed` — nested under `terminal_info`, and nothing else (C7, D-05)."
    - "The `\"present\"` sentinel is gone, and the positive-marker property survives: a healthy reading is a JSON OBJECT, which a truncated or malformed line cannot produce."
    - "The mt5 OK info line an operator reads carries `connected=` and `trade_allowed=` and no longer carries `build=` (C7, D-05)."
    - "`connected` and `trade_allowed` are still PRINTED AND NEVER JUDGED — no defect is raised from either (`arms/mt5.mjs:61-64`)."
    - "Branch (7) — initialize ok, `terminal_info()` null — still fires `mt5-terminal-error` on `terminal-info-null.txt` (C4)."
    - "`scripts/mt5-diag.sh` is byte-unchanged, and the arm's header ARGUES the second deliberate divergence from it rather than leaving it silent (C5)."
  artifacts:
    - scripts/prod-prober/arms/mt5.mjs
    - scripts/prod-prober/fixtures/mt5/ok.txt
    - scripts/prod-prober/run.mjs
  key_links:
    - "`MT5_PROBE_PY` (`arms/mt5.mjs:108-122`) → base64 → `railway ssh` → `PROBE {…}` → `classifyProbe` `:229-241`. The self-test decodes the argv and asserts byte-equality with the committed constant (`run.mjs:3882-3885`), so a constant that is not a literal array of strings breaks the gate."
    - "`terminalInfoPresent` (`arms/mt5.mjs:230`) → the OK gate `:234` AND, by falling through, branch (7) `:272`. Both meanings must survive the tightening."
    - "`scripts/prod-prober/fixtures/mt5/ok.txt` → the green leg of the fixture table AND `allGreenSeams()` (`run.mjs:962`) for the four-arm scenarios."
  prohibitions:
    - "Never emit `bool(d.get(k))` in the probe body — `bool(None)` is `False`, which renders a MISSING key as a confident measurement; that is the exact 'an absence produced a positive reading' failure the arm's `:102-106` comment exists to prevent. Emit raw `d.get(k)`."
    - "Never flatten the booleans to top level and drop the `terminal_info` key — that deletes the presence marker branch (7) keys on, and forces every null fixture to change."
    - "Never JUDGE `connected` or `trade_allowed` — recording them is not licence to raise a defect from them; a `connected:false` is MT5GW-COPY-01's concern, not this arm's."
    - "Never add a call to `MT5_PROBE_PY` beyond `initialize()`, `last_error()` and `terminal_info()`, and never introduce a `${` marker or assemble the constant at run time."
    - "Never edit `scripts/mt5-diag.sh` — it is the phase's READ-ONLY fence, and its own `Reading the result:` note at `:55-66` interprets a field this plan stops collecting."
    - "Never change any fixture other than `ok.txt` — measured: the other five carry `\"terminal_info\": null` or no PROBE line at all."
    - "Never add a counted `scenario()` in this plan — the count must stay where plan 02 left it."
    - "Never edit `.github/workflows/prod-prober.yml`."
---

<objective>
Make the instrument record the two fields the runbook says are the verification, and stop it
recording three the founder excluded.

Purpose: `docs/runbooks/mt5-go-live.md` Step 2 states the criterion as *"`terminal_info()` must
report `connected: true` AND `trade_allowed: true` — both, not either"*, and the prober collapses
`terminal_info` itself into the string `"present"`. The gate is weaker than the sentence beside
it. D-05 settles the shape: two booleans exactly.

⚠️ **This is a NARROWING plus a restructure, not an addition — and it is NOT what fixes `-6`.**
The probe already emits both booleans (measured live in run `34706551355`:
`connected=true trade_allowed=true build=6182`); it also emits three fields D-05 excludes. On a
`-6` the terminal returns `terminal_info: null`, so neither boolean exists to record whatever the
shape — `-6` attribution is closed by plans 01 and 02, not by this one. Criterion 7 is worth
doing on its own merits; do not sell it as the `-6` fix.

Output: a narrowed `MT5_PROBE_PY`, a rewritten divergence argument in the arm header, a tightened
presence check, a two-field info line, an updated `ok.txt`, and a calibrated assertion on the
info line's field SET.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-CONTEXT.md
@.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-RESEARCH.md
@docs/runbooks/mt5-go-live.md
</context>

<measured_baseline>
⭐ **Measurement currency — re-verified 2026-09-13, after HEAD moved.** Every figure in this
plan was taken at HEAD `de1a3947`; HEAD is now `42b4e2a9`, four commits later. Re-measured:
`git diff --name-only de1a3947..42b4e2a9 -- scripts/prod-prober .github/workflows/prod-prober.yml
src/__tests__/prod-prober-wiring.test.ts scripts/mt5-diag.sh` returns **EMPTY** — all four
intervening commits are `docs(roadmap)`/planning-only, and `SELF_TEST_SCENARIOS = 78` still reads
at `run.mjs:149`. Every anchor, count and line number below therefore still holds byte-for-byte.
⛔ This is a DATED reading, not a licence to skip re-measuring: if HEAD has moved again by the
time you execute, re-run that diff before trusting a line number.

Blast radius re-measured at HEAD `de1a3947` by grepping `scripts/prod-prober/` and the wiring
test for every consumer of `terminal_info`, `"present"`, and the three flattened fields. It is
**exactly six call sites plus one fixture** — nothing else in the tree reads them:

| # | Anchor | Today | Required change |
|---|---|---|---|
| 1 | `arms/mt5.mjs:108-122` | `MT5_PROBE_PY` sets `"present"` / `None` and copies five keys | nest exactly `connected` + `trade_allowed` under `terminal_info` |
| 2 | `arms/mt5.mjs:102-106` | the ONE-DELIBERATE-DIFFERENCE comment, which names `"present"` | rewrite to describe the object marker; **keep the positive-marker argument intact** and ADD the second divergence |
| 3 | `arms/mt5.mjs:230` | `probe.terminal_info !== null && probe.terminal_info !== undefined` | tighten to an object test (⚠️ `typeof null === "object"`, so keep a null guard) |
| 4 | `arms/mt5.mjs:234` | OK gate `probe.initialize === true && terminalInfoPresent` | unchanged |
| 5 | `arms/mt5.mjs:236-241` | info line reads `probe.connected` / `probe.trade_allowed` / `probe.build` | read from the nested object; **drop `build=`** (D-05: nothing else) |
| 6 | `arms/mt5.mjs:270-281` | branch (7) detail *"terminal_info() returned null"* | still accurate — leave |
| F | `fixtures/mt5/ok.txt` | the only fixture carrying the healthy shape | rewrite its PROBE line |

Gates that must SURVIVE unchanged (they are why the constant must stay a literal array of
strings): `run.mjs:3882-3885` byte-for-byte decode of what was SENT; `run.mjs:3890-3892`
FORBIDDEN-token list + no-`${` check; `run.mjs:3894-3896` *"it DOES call all three read-only
methods"*.

Other fixtures, measured: `10004.txt`, `10005.txt`, `init-false-other.txt`,
`terminal-info-null.txt` all carry `"terminal_info": null`; `no-probe-line.txt` has no PROBE line
at all; `6.txt` (plan 01) carries `null`. **`ok.txt` is the only one that changes.**

`ok.txt` is also loaded by `allGreenSeams()` (`run.mjs:962`) for the four-arm green scenarios, so
a bad edit there reddens more than the mt5 leg.
</measured_baseline>

<artifacts_this_plan_produces>
| Symbol / path | Kind | Where |
|---|---|---|
| `MT5_PROBE_PY` (narrowed) | committed probe constant | `scripts/prod-prober/arms/mt5.mjs:108-122` |
| `out["terminal_info"] = {"connected": …, "trade_allowed": …}` | emitted JSON shape | the probe body |
| `terminalInfoPresent` (tightened to an object test) | derived boolean | `arms/mt5.mjs:230` |
| the two-field OK info line | operator-visible string | `arms/mt5.mjs:236-240` |
| `scripts/prod-prober/fixtures/mt5/ok.txt` (rewritten PROBE line) | green fixture | existing file |
| an assertion block carrying the literal `164.8.3 CRITERION 7` | added to an EXISTING scenario | `run.mjs`, the byte-for-byte scenario |
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: two booleans, nested — the probe body, the presence check, the info line, and `ok.txt`</name>

  <read_first>
    - `scripts/prod-prober/arms/mt5.mjs:1-71` — the whole header, and specifically `:8-13` (why a login re-clears `[Experts] Enabled`), `:61-64` (`connected` and `trade_allowed` are PRINTED AND NEVER JUDGED) and `:36-50`.
    - `scripts/prod-prober/arms/mt5.mjs:90-122` — the `MT5_PROBE_PY` docblock including the ONE DELIBERATE DIFFERENCE argument at `:102-106`, and the constant itself.
    - `scripts/prod-prober/arms/mt5.mjs:229-292` — `code`, `terminalInfoPresent`, the OK branch, and branches (7) and (8).
    - `scripts/prod-prober/run.mjs:3882-3900` — the three gates the constant must keep satisfying.
    - `scripts/prod-prober/fixtures/mt5/ok.txt` and `scripts/prod-prober/fixtures/mt5/terminal-info-null.txt` — the two shapes.
    - `docs/runbooks/mt5-go-live.md` Step 2 — the two-field verification criterion, verbatim, which is D-05's whole justification.
    - `scripts/mt5-diag.sh:25-70` — READ ONLY. Read `:55-66`'s `Reading the result:` note so the divergence you are about to create is argued rather than discovered later.
  </read_first>

  <files>scripts/prod-prober/arms/mt5.mjs, scripts/prod-prober/fixtures/mt5/ok.txt</files>

  <behavior>
    - `MT5_PROBE_PY` remains a literal array of single-quoted / double-quoted string elements joined with `\n`, containing no `${`, calling exactly `initialize()`, `last_error()` and `terminal_info()`.
    - On a healthy terminal the probe emits `"terminal_info": {"connected": <raw>, "trade_allowed": <raw>}` and no `tradeapi_disabled`, `build` or `path` key anywhere in the payload.
    - On `ti is None` it still emits `"terminal_info": null`, so branch (7) is unchanged.
    - `classifyProbe` on the rewritten `ok.txt` returns `kind: null` with an info line matching `initialize=true last_error=1 connected=true trade_allowed=true` and containing no `build=`.
    - `classifyProbe` on `terminal-info-null.txt` still returns `mt5-terminal-error`, subject `terminal_info`.
    - A PROBE line whose `terminal_info` is the bare string `"present"` is NOT treated as a healthy reading after the tightening.
  </behavior>

  <action>
    **(a) The probe body — D-05, C7.** Rewrite the `else` arm of `MT5_PROBE_PY` so it assigns
    `out["terminal_info"]` a dict built from `ti._asdict()` containing exactly the two keys D-05
    names, and DELETE the five-key copy loop along with the `"present"` sentinel. ⛔ Take the
    values RAW via `d.get(...)` — never wrapped in `bool(...)`. `bool(None)` is `False`, which
    would render a *missing* key as a confident `false`; raw gives `null`, which prints as `null`
    and is visibly not a boolean. That is the same "an absence produced a positive reading"
    failure the constant's own docblock argues against.
    ⛔ Keep it a literal array of strings joined with `\n`. Three self-test gates read this
    constant: the base64 payload must still decode BYTE-FOR-BYTE to it (`run.mjs:3882-3885`), it
    must still contain none of the FORBIDDEN tokens and no `${` (`run.mjs:3890-3892`), and it
    must still call all three read-only methods (`run.mjs:3894-3896`). Adding a template literal
    or a run-time join breaks the first and third.

    **(b) The divergence argument — `:102-106`.** That comment currently names the `"present"`
    sentinel. Rewrite it so the POSITIVE-MARKER argument survives verbatim in substance — a
    healthy reading must be something a truncated or malformed line cannot produce, and a JSON
    object is exactly that, where an absent key is not — and ADD the SECOND divergence this task
    creates: the arm now projects two fields where `scripts/mt5-diag.sh` copies five. Argue it
    the way the existing one is argued, naming D-05 and the runbook's two-field criterion as the
    reason, and stating plainly that `mt5-diag.sh` is deliberately left alone because its own
    `Reading the result:` note interprets `tradeapi_disabled` and criterion 5 fences that file.
    ⛔ Leave `scripts/mt5-diag.sh` byte-unchanged.

    **(c) The presence check — `:230`.** Tighten `terminalInfoPresent` from "not null and not
    undefined" to an OBJECT test. ⚠️ `typeof null === "object"` in JavaScript, so the null guard
    must stay. After the tightening a stray string can no longer read as present — which closes
    the exact hole the `"present"` sentinel was introduced to close, now that the sentinel is
    gone. Both consumers keep their meaning: the OK gate at `:234`, and branch (7) at `:272`,
    which is reached by falling through.

    **(d) The info line — `:236-241`, D-05.** Read the two booleans from the nested object rather
    than from top-level keys, and DROP the `build=` clause. D-05 says two booleans exactly, and
    `build` is one of the three fields the founder excluded. ⛔ Do not start judging either
    boolean: `arms/mt5.mjs:61-64` says they are printed and never judged, and a `connected:false`
    raising a defect here would make the arm fire on a state its own header calls harmless to
    this measurement. Criterion 7 asks for RECORDING, not judging.

    **(e) `ok.txt` — the one fixture that changes.** Rewrite its PROBE line to the new shape:
    `initialize` true, `last_error` `[1, "Success"]`, and `terminal_info` an object carrying the
    two booleans, both true. Keep line 1 (the CLI banner) exactly as it is — it is load-bearing
    proof that the scanner at `arms/mt5.mjs:191-193` scans rather than assuming line 1. Keep the
    trailing newline, and add no other content. ⛔ Touch no other fixture; measured, the other
    five need no change.
  </action>

  <verify>
    <automated>ST=$(node scripts/prod-prober/run.mjs --self-test 2>&amp;1); printf '%s\n' "$ST" | tail -4 &gt;&amp;2; printf '%s\n' "$ST" | grep -q 'SELF-TEST PASSED: 80/80' &amp;&amp; printf '%s\n' "$ST" | grep -q 'mt5-terminal-error fires on terminal-info-null.txt'</automated>
    <fails_when>non-zero exit; or the output lacks `SELF-TEST PASSED: 80/80` — in particular a failure naming `decodes to MT5_PROBE_PY BYTE-FOR-BYTE` means the constant stopped being a committed literal, and one naming `it DOES call all three read-only methods` means a call was dropped; or the `terminal-info-null.txt` header is missing, which means branch (7) stopped firing and C4 broke.</fails_when>

    <automated>node --input-type=module -e 'const m = await import("./scripts/prod-prober/arms/mt5.mjs"); const py = m.MT5_PROBE_PY; const bad = []; for (const t of ["tradeapi_disabled","\"build\"","\"path\"","present","bool(d.get","${"]) if (py.includes(t)) bad.push("PRESENT:" + t); for (const t of ["initialize()","last_error()","terminal_info()","connected","trade_allowed"]) if (!py.includes(t)) bad.push("ABSENT:" + t); console.log(bad.length ? "PROBE-BODY FAIL: " + bad.join(" ") : "PROBE-BODY OK: two booleans, three read-only calls, no sentinel"); process.exit(bad.length ? 1 : 0);'</automated>
    <fails_when>non-zero exit, or the printed line starts with `PROBE-BODY FAIL:`. `PRESENT:tradeapi_disabled` / `PRESENT:"build"` / `PRESENT:"path"` means a field D-05 excluded is still collected; `PRESENT:present` means the sentinel survived; `PRESENT:bool(d.get` means an absent key would be rendered as a confident `false`; `PRESENT:${` means an interpolation marker reached a constant that must never be assembled at run time; any `ABSENT:` entry names a required call or field that was dropped.</fails_when>

    <automated>node --input-type=module -e 'const fs = await import("node:fs"); const m = await import("./scripts/prod-prober/arms/mt5.mjs"); const txt = fs.readFileSync("scripts/prod-prober/fixtures/mt5/ok.txt","utf8"); const v = m.classifyProbe({ status: 0, stdout: txt, stderr: "", timedOut: false, measureFail: null }); const line = txt.split("\n").find((l) =&gt; l.startsWith("PROBE ")); const ti = JSON.parse(line.slice(6)).terminal_info; const keys = ti &amp;&amp; typeof ti === "object" ? Object.keys(ti).sort() : null; const bad = []; if (v.kind !== null) bad.push("NOT_GREEN:" + v.kind); if (JSON.stringify(keys) !== JSON.stringify(["connected","trade_allowed"])) bad.push("KEYS:" + JSON.stringify(keys)); if (!/connected=true/.test(v.info) || !/trade_allowed=true/.test(v.info)) bad.push("INFO_MISSING_BOOLEANS"); if (/build=/.test(v.info)) bad.push("INFO_STILL_HAS_BUILD"); const stray = m.classifyProbe({ status: 0, stdout: "banner\nPROBE {\"initialize\": true, \"last_error\": [1, \"Success\"], \"terminal_info\": \"present\"}", stderr: "", timedOut: false, measureFail: null }); if (stray.kind === null) bad.push("CALIBRATION_STRAY_STRING_READ_AS_PRESENT"); console.log(bad.length ? "OK-FIXTURE FAIL: " + bad.join(" ") : "OK-FIXTURE OK: " + v.info); process.exit(bad.length ? 1 : 0);'</automated>
    <fails_when>non-zero exit, or the printed line starts with `OK-FIXTURE FAIL:`. `NOT_GREEN:<kind>` means the rewritten fixture stopped classifying green; `KEYS:<…>` means `terminal_info` does not carry exactly `connected` and `trade_allowed` (D-05); `INFO_MISSING_BOOLEANS` means the operator-visible line stopped naming them; `INFO_STILL_HAS_BUILD` means an excluded field survived into the line; `CALIBRATION_STRAY_STRING_READ_AS_PRESENT` means the presence check was NOT tightened — a bare string still reads as a healthy terminal, which is the hole this task closes.</fails_when>

    <automated>test -z "$(git status --porcelain -- scripts/mt5-diag.sh .github/workflows/prod-prober.yml scripts/prod-prober/fixtures/mt5/10004.txt scripts/prod-prober/fixtures/mt5/10005.txt scripts/prod-prober/fixtures/mt5/init-false-other.txt scripts/prod-prober/fixtures/mt5/terminal-info-null.txt scripts/prod-prober/fixtures/mt5/no-probe-line.txt scripts/prod-prober/fixtures/mt5/6.txt)"</automated>
    <fails_when>non-zero exit — `git status --porcelain` printed a line, meaning at least one of the fenced files was modified. `scripts/mt5-diag.sh` is criterion 5's READ-ONLY fence; the other fixtures were measured to need no change, so an edit there is unexplained scope.</fails_when>
  </verify>

  <acceptance_criteria>
    - `MT5_PROBE_PY` records exactly `connected` and `trade_allowed`, nested under `terminal_info`, taken RAW (D-05, C7); `tradeapi_disabled`, `build`, `path` and the `"present"` sentinel are gone.
    - The constant is still a committed literal array of strings with no `${`, and all three read-only calls survive — the three self-test gates pass unchanged.
    - The divergence comment at `:102-106` keeps the positive-marker argument and ADDS the second divergence from `mt5-diag.sh`, argued and dated.
    - `terminalInfoPresent` is an object test with a null guard; a bare-string `terminal_info` no longer reads as healthy, proven by the in-line calibration.
    - The OK info line carries both booleans and no `build=`, and neither boolean is judged.
    - `ok.txt` is the only fixture changed; `scripts/mt5-diag.sh` and `prod-prober.yml` are byte-unchanged (C5).
  </acceptance_criteria>

  <done>
    `node scripts/prod-prober/run.mjs --self-test` prints `SELF-TEST PASSED: 80/80`; the probe-body
    check prints `PROBE-BODY OK: …`; the fixture check prints `OK-FIXTURE OK: initialize=true …
    connected=true trade_allowed=true` with no `build=`; and the fenced-files check is silent
    (C5, C7, D-05).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pin the OK info line's field SET in the self-test, with its calibration</name>

  <read_first>
    - `scripts/prod-prober/run.mjs:3788-3820` — the byte-for-byte scenario's opening, where `ok.txt` is already loaded and driven through `runProber` with `log: quiet`.
    - `scripts/prod-prober/run.mjs:1054-1068` — the green leg's `lines` capture idiom (`log: (s) => lines.push(s)`), which is what must replace `quiet` here.
    - `scripts/prod-prober/arms/mt5.mjs:337-350` — `run()`, which emits the info as `log(\`mt5: ${verdict.info}\`)` on the `kind === null` path.
    - `scripts/prod-prober/run.mjs:1027-1031` — `scenario()`; ⛔ do NOT call it in this task.
    - `src/__tests__/prod-prober-wiring.test.ts:15-17` — the calibration rule.
  </read_first>

  <files>scripts/prod-prober/run.mjs</files>

  <behavior>
    - The green `ok.txt` run's captured log lines contain exactly one line starting `mt5: `.
    - That line contains `connected=` and `trade_allowed=`.
    - That line does NOT contain `build=`.
    - CALIBRATION: an in-memory copy of that line with `build=…` appended is REJECTED by the same predicate, and the copy is asserted to differ from the original.
    - The counted scenario set is unchanged at 80.
  </behavior>

  <action>
    Extend the EXISTING byte-for-byte scenario (`run.mjs:3788`) — ⛔ do not add a new
    `scenario()`; plan 02 already moved the count and this task must leave it at 80.

    Change that scenario's green `runProber` call for `ok.txt` from `log: quiet` to a capturing
    logger, using the same `const lines = []; … log: (s) => lines.push(s)` idiom the green leg
    uses at `run.mjs:1054-1068`. Everything else about that call stays as it is — in particular
    `capture`, which the argv decode depends on.

    Then add an assertion group of FOUR SEPARATE `expect(...)` calls — one property each, so a
    failure names which property broke rather than crediting one RED to four controls. ⛔ **Every
    one of the four messages must carry the literal `164.8.3 CRITERION 7`**: `expect` prints
    `ok — <message>` per passing assertion, and the verify counts those printed lines, so a
    message missing the literal reads as an assertion that did not run. The four properties:
    exactly one captured line begins `mt5: `; it contains `connected=`; it contains
    `trade_allowed=`; and — the negative — it does NOT contain `build=`. ⭐ Assert on the
    CAPTURED LINE, not on
    `verdict.info` or on the arm's source: this is the string that reaches the operator's log and
    the auto-filed issue, and it is the only place the field SET is observable end-to-end.

    Add the CALIBRATION in the same group: build an in-memory copy of that captured line with a
    `build=` clause appended, assert the copy actually differs from the original, and assert the
    SAME negative predicate REJECTS the copy. Without it the negative is a predicate only ever
    applied to passing input, which this repo does not accept as evidence.

    ⚠️ The negative is written over a RUNTIME string, never over a source file — `arms/mt5.mjs`
    may legitimately mention `build` in a comment explaining why it was dropped, and a source
    grep would make that comment self-invalidating.
  </action>

  <verify>
    <automated>ST=$(node scripts/prod-prober/run.mjs --self-test 2>&amp;1); printf '%s\n' "$ST" | tail -3 &gt;&amp;2; printf '%s\n' "$ST" | grep -q 'SELF-TEST PASSED: 80/80' &amp;&amp; test "$(printf '%s\n' "$ST" | grep -c '164.8.3 CRITERION 7')" -ge 4</automated>
    <fails_when>non-zero exit; or the output lacks `SELF-TEST PASSED: 80/80` (a different count means a `scenario()` was added or removed, which this task forbids); or fewer than 4 printed `ok — ` lines carry `164.8.3 CRITERION 7`, meaning the four asserted properties (one `mt5: ` line, `connected=`, `trade_allowed=`, no `build=`) did not all run and the green is partly vacuous. The count is taken over the RUN OUTPUT, not over a source file, so nothing written in a comment can satisfy it.</fails_when>

    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
    <fails_when>non-zero exit, or the summary line does not read `Test Files  1 passed`. A failure naming `SELF_TEST_SCENARIOS` means this task changed the counted set when it must not have.</fails_when>
  </verify>

  <acceptance_criteria>
    - The OK info line's field set is asserted end-to-end on the CAPTURED log line: both booleans present, `build=` absent (C7, D-05).
    - The negative carries a calibration proving it fires, and the mutant is asserted to differ from the original.
    - The counted scenario set is still 80; no `scenario()` call was added.
    - The `capture` array and the argv byte-equality assertions in the same scenario still pass.
  </acceptance_criteria>

  <done>
    `node scripts/prod-prober/run.mjs --self-test` prints `SELF-TEST PASSED: 80/80` and at least
    one `ok — ` line carrying `164.8.3 CRITERION 7`; `npx vitest run
    src/__tests__/prod-prober-wiring.test.ts` passes (C7, D-05).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prober → container | `MT5_PROBE_PY` is base64'd into a single `railway ssh` word and executed inside the production gateway. This is the highest-privilege surface in the phase. |
| container → prober | the returned PROBE line is untrusted JSON parsed in a `try/catch`. |
| prober → PUBLIC Actions log / issue | the OK info line is printed world-readably. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.3-03 | Elevation of Privilege | `MT5_PROBE_PY` — run-time interpolation into the container command | high | mitigate | The constant stays a literal array of strings. Task 1's `PROBE-BODY` check fails on a `${` marker, and `run.mjs:3882-3885` asserts the SENT payload decodes byte-for-byte to the committed constant. Both run in `<verify>`. |
| T-164.8.3-04 | Denial of Service | a probe that authenticates re-clears `[Experts] Enabled` and re-breaks the terminal it measures | high | mitigate | No call is added; the FORBIDDEN-token list (`run.mjs:3797-3806`) refuses `login`, `order_send`, … and `run.mjs:3894-3896` asserts the three read-only calls are still all present. `scripts/mt5-diag.sh` is fenced byte-unchanged by a `git status --porcelain` gate. |
| T-164.8.3-11 | Input Validation (V5) | dereferencing a nested `terminal_info` object before the presence check | high | mitigate | Task 1 (c) tightens `terminalInfoPresent` to an object test WITH a null guard, and the OK branch reads the nested object only inside that guard. The calibration drives a bare-string `terminal_info` and asserts it is NOT read as healthy. |
| T-164.8.3-12 | Information Disclosure | the probe collecting `path` — a filesystem path from the production container — into a PUBLIC log | medium | mitigate | D-05 drops `path` entirely; Task 1's `PROBE-BODY` check fails on `PRESENT:"path"`. This is a genuine reduction in what the public log can carry. |
| T-164.8.3-13 | Tampering | an absent `_asdict()` key rendered as a confident `false` | medium | mitigate | Raw `d.get(k)` only; the `PROBE-BODY` check fails on `PRESENT:bool(d.get`. |
| T-164.8.3-06 | Tampering | npm/pip/cargo installs | n/a | accept | No packages are installed. RESEARCH.md's `## Package Legitimacy Audit` records NOT APPLICABLE; no `[ASSUMED]`/`[SUS]`/`[SLOP]` candidates exist, so no legitimacy checkpoint is inserted. |
</threat_model>

<verification>
- `node scripts/prod-prober/run.mjs --self-test` → `SELF-TEST PASSED: 80/80`, exit 0, with an `ok — ` line carrying `164.8.3 CRITERION 7`.
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts` → 1 file passed.
- `git diff --name-only` lists exactly `scripts/prod-prober/arms/mt5.mjs`, `scripts/prod-prober/fixtures/mt5/ok.txt`, `scripts/prod-prober/run.mjs`.
</verification>

<success_criteria>
- ROADMAP criterion 7 is implemented as D-05 specifies — two booleans exactly — and the instrument now records what `docs/runbooks/mt5-go-live.md` Step 2 calls the verification.
- Criterion 5's fence holds: `scripts/mt5-diag.sh` byte-unchanged, no new MT5 call, no interpolation.
- The narrowing is recorded honestly as NOT the `-6` fix.
</success_criteria>

<output>
Create `.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-03-SUMMARY.md` when done.
Record in it: `MT5_PROBE_PY` as shipped, the rewritten `ok.txt` PROBE line, and the captured `mt5: …` info line verbatim.
</output>
