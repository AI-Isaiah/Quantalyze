---
phase: 164.8.3-proberauth
verified: 2026-09-13T18:43:11Z
verified_initial: 2026-09-13T17:40:00Z  # first stamp, status human_needed; re-affirmed after the routing item was resolved
status: passed
score: 32/32 must-haves verified (8 ROADMAP success criteria + 24 PLAN truths)
covered_files:
  - ".planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-01-PLAN.md"
  - ".planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-01-SUMMARY.md"
  - ".planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-02-PLAN.md"
  - ".planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-02-SUMMARY.md"
  - ".planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-03-PLAN.md"
  - ".planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-03-SUMMARY.md"
  - ".planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-04-PLAN.md"
  - ".planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-04-SUMMARY.md"
  - ".planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-CONTEXT.md"
  - ".planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-REVIEW-FIX.md"
  - ".planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-REVIEW.md"
  - "TODOS.md"
  - "scripts/prod-prober/arms/mt5.mjs"
  - "scripts/prod-prober/fixtures/mt5/6.txt"
  - "scripts/prod-prober/fixtures/mt5/ok.txt"
  - "scripts/prod-prober/run.mjs"
  - "src/__tests__/prod-prober-wiring.test.ts"
covered_digest: "v1:sha256:bca445fb6c5a31ed95284bcb5e194006a6dddb3c1a8c912fb2a153f9c0a97d31"  # re-stamped by gsd-verifier via `query verification.fingerprint`, never hand-patched
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Decide the routing of `[164.8.3-CAPTURE-MANIFEST-ERREXIT]`, booked in TODOS.md as a NOMINATION to Phase 164.8.4 GATERESIDUE. Either admit it to 164.8.4 through `/gsd-phase --edit` (recording the decision the way that phase's `⚠️ NOT A PRECEDENT` fence requires), or name a different destination phase."
    expected: "The item appears in `.planning/ROADMAP.md` under a named phase, so the deferral lives in the queue rather than only in TODOS.md."
    resolved: "RESOLVED 2026-09-13 by founder decision: ADMITTED to Phase 164.8.4 GATERESIDUE as ADMISSION 2, entered through `/gsd-phase --edit 164.8.4`. The expectation is met by measurement — `[164.8.3-CAPTURE-MANIFEST-ERREXIT]` now appears in that entry's goal, its ADMISSION 2 merits block, its group (d), its success criterion 9 and its Requirements list, and the fence is re-armed as `STILL NOT A PRECEDENT` for a THIRD item. The TODOS entry's `awaits the same explicit decision` sentence was replaced with the decision. Verdict flips human_needed -> passed; the original why_human is kept below as the record of why it could not be taken by an agent."
    why_human: "Phase 164.8.4's ROADMAP entry carries an explicit fence — `⚠️ **NOT A PRECEDENT.** A second outside item may be admitted only by the same explicit decision, recorded the same way` — so admission is a founder decision by construction, not something a verifier or an executor may take. The executor correctly declined to hand-edit the ROADMAP (roadmap CRUD goes through `/gsd-phase --edit`), which leaves the routing open. ⭐ This is NOT a code defect and does not reduce the phase's delivery: plan 04's must-have was `booked with a NAMED destination phase`, and it is."
---

# Phase 164.8.3: PROBERAUTH Verification Report

**Phase Goal:** The prober's MT5 arm gives a dedicated defect kind to `-6`, so the operator is told what is wrong and what to do instead of being sent to the MT5 error table.
**Verified:** 2026-09-13T20:40:00Z
**Status:** passed — ⭐ opened as `human_needed` on one routing decision with zero code gaps; that decision was taken by the founder on 2026-09-13 (admit to Phase 164.8.4 as ADMISSION 2) and the item is now in the ROADMAP queue under a named phase. The 32/32 must-have result is unchanged by it.
**Re-verification:** No — initial verification, re-affirmed 2026-09-13T18:43:11Z after the one `human_needed` item was resolved (see "Routing Item — Resolved and Re-verified" below)
**HEAD:** `cf2c3e9c`, branch `phase-164.8.3-proberauth-r2`. ⚠️ At the ORIGINAL stamp the working tree was clean; at the re-stamp it carries three uncommitted planning edits (`TODOS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`) recording the founder's routing decision. **Every code and fixture artifact this phase delivered is byte-unchanged since the original stamp**, confirmed by `git status --porcelain` over `scripts/prod-prober/` and `src/__tests__/prod-prober-wiring.test.ts` (no entries).

⭐ **VERDICT IN ONE LINE: the phase goal IS achieved.** The `-6` state has its own kind, its own fixture, its own instructing remedy, five independent registrations, a durable falsifier, and a two-boolean `terminal_info`. Every claimed RED I spot-checked reproduced **verbatim**.

---

## Goal Achievement

### ROADMAP Success Criteria — the contract

| # | Criterion | Status | Evidence (measured by me at HEAD, not read from a SUMMARY) |
|---|-----------|--------|-------------|
| 1 | `-6` reports as its own kind, never as `mt5-terminal-error`; the detail states what was measured | ✓ VERIFIED | `scripts/prod-prober/arms/mt5.mjs` branch (6b) returns `kind: "mt5-not-authorized", subject: "-6"`. Self-test scenario 24/80: `ok — 6.txt ISOLATES exactly one defect (got 1: mt5-not-authorized)`. Detail reads *"the rpyc bridge ANSWERED, so the terminal is up and the IPC transport is not implicated — but NO ACCOUNT IS AUTHORIZED on it"* — a measurement, not a code to look up. |
| 2 | The remedy names the action (VNC, Save password, the two EA options) and must NOT route to the MT5 error table | ✓ VERIFIED | Scenario 75/80 asserts on the **runtime DEFECT ROW**, not the table: `(b) … VNC console=true, "Save password"=true, Expert Advisors=true, Journal=true`; `(c) … the -6 ROW's remedy carries NO lookup-table instruction`; `(d)` is a **calibration** that splices the catch-all's lookup sentence into an in-memory copy and proves `(c)` rejects it. Plus `(e2)`: the `-6` and `-10005` instructions are proven non-convergent on their distinguishing words. |
| 3 | Ships with a red fixture, registered in the arm's `kinds` list | ✓ VERIFIED | `scripts/prod-prober/fixtures/mt5/6.txt` exists and carries the real 2026-09-09 transcript. **Five independent spellings** all present: `DEFECT_KINDS` (`run.mjs:191`), the fixture table's `kinds:` (`:931`), `red:` (`:943` → `"6.txt": "mt5-not-authorized"`), `KIND_ASSERTIONS` (`:1029`), and the wiring test's `EXPECTED_DEFECT_KINDS` pin. Self-test prints the set-equality gate green: `declared [… mt5-not-authorized …] vs covered [… mt5-not-authorized …]`. |
| 4 | `mt5-terminal-error` survives and keeps meaning something; the absence assertions still discriminate | ✓ VERIFIED | `(f1)` BOTH catch-all fixtures still produce `mt5-terminal-error`; `(f2)` their **subjects differ** (`-2` from branch (8), `terminal_info` from branch (7)) — discrimination measured, not asserted. The two by-name absence lists are now roster-derived (`VERDICT_KINDS.filter(k => k !== …)`), so they widen automatically and cover the new kind by construction. |
| 5 | ⛔ Scope fence — `scripts/mt5-diag.sh` stays READ-ONLY | ✓ VERIFIED | `git diff e64b0811..HEAD -- scripts/mt5-diag.sh` → **byte-unchanged**. The probe body calls only `initialize()`, `last_error()`, `terminal_info()`; the self-test's forbidden-call gate names `login` explicitly and passes. The arm's header ARGUES the second deliberate divergence (the `(2) FIELD SET (D-05)` paragraph) rather than leaving it silent. |
| 6 | Falsifier observed RED; restore from pristine bytes; never `git checkout --` | ✓ VERIFIED — **and independently reproduced** | See "Anti-Vacuity Spot-Checks" below. I re-ran BOTH recorded levers myself and got the recorded strings **verbatim**, plus a third lever of my own. The durable half — three `164.8.3 FALSIFIER` tests building a tmp-dir mutant via `mkdtempSync` + `finally rmSync` — is real, and its `anchorIndex` discipline throws by name when the anchor moves (observed). |
| 7 | `terminal_info()` recorded as `connected` and `trade_allowed`, two separate booleans — not present/absent | ✓ VERIFIED | `MT5_PROBE_PY` emits `{"connected": d.get("connected"), "trade_allowed": d.get("trade_allowed")}` — **raw `d.get`, never `bool(d.get(...))`**, so a missing key cannot render as a confident `False`. The `"present"` sentinel is gone (only a historical comment mentions it). OK line reads `mt5: initialize=true last_error=1 connected=true trade_allowed=true` with **no `build=`**. Both booleans are printed and never judged — the OK branch returns `...none` with only `info` set. |
| 8 | ⛔ MET BEFORE THE PHASE BEGAN — no code owed | ✓ CONFIRMED INDEPENDENTLY AT HEAD | **I re-measured this rather than trusting the claim.** `.github/workflows/prod-prober.yml`: `cat "$RUNNER_LOG"` runs **unconditionally outside both branches** (comment-filtered count = 1), and **both** branches capture status on the same line via `|| status=$?` (comment-filtered count = 2, unfiltered 3 — the workflow's own prose quotes the idiom, which is why the filter is load-bearing). `git log -S'\|\| status=$?'` dates the fix to **`604d655f`, 2026-09-11, PR #774, Phase 164.8.5** — before this phase. The file is **byte-unchanged** by this phase. ⭐ **The green phase is NOT built on a false premise.** |

**Score: 8/8 ROADMAP criteria** (7 live verified + criterion 8 confirmed met-before-start).

### PLAN must_have truths

All 24 truths across the four plans verified. Condensed, with the non-obvious ones called out:

| Plan | Truth | Status | Note |
|------|-------|--------|------|
| 01 | 6/6 including the auto-issue dedup independence | ✓ VERIFIED | `kindsMentioned(autoIssueStep(WORKFLOW_TEXT))` is `[]` — selection reads a constant `dedupLabel = "prod-prober"`, so a new kind can neither be suppressed behind an old kind's issue nor open a second one. Carries its own calibration (a kind-derived label IS caught). |
| 02 | 6/6 | ✓ VERIFIED (one superseded spelling — see below) | The public-log control `(g)` scans all 6 mt5 remedies for a six-or-more-digit run, gated by `(g0)` which derives the kind set from `DEFECT_KINDS` so an empty/half-landed table cannot pass silently. `(g-calibration)` fires on a synthetic spliced run. |
| 03 | 6/6 | ✓ VERIFIED | Includes the executor's own Rule-2 deviation: `!Array.isArray(ti)` added, because `typeof [] === "object"` would have read a JSON array as a healthy terminal and printed `connected=undefined` as GREEN. That is the tightening's own failure class, caught and closed. |
| 04 | 6/6 | ✓ VERIFIED | Committed diff fence held: plan 04's commits (`9fb751d6`, `76cfb120`, `788d64e0`) touch only `prod-prober-wiring.test.ts` and `TODOS.md`. |

⚠️ **One truth is satisfied by a SUPERSEDING implementation, recorded so it is not read as drift.** Plan 02 truth 6 says *"The two hand-kept by-name ABSENCE lists both **name** `mt5-not-authorized`, and the second list's prose numeral moves with it."* At HEAD the lists are **no longer hand-kept**: review finding WR-03 replaced them with `VERDICT_KINDS.filter((k) => k !== …)` and the prose numeral with `${VERDICT_KINDS.length - 1}`. So the literal wording ("name") is no longer true, and the **intent is met more strongly** — the lists now widen by themselves when a kind is registered, and the numeral cannot drift. `VERDICT_KINDS` derives from `DEFECT_KINDS`, which contains `mt5-not-authorized`, so coverage of the new kind is by construction. Counted VERIFIED.

---

## Anti-Vacuity Spot-Checks — the load-bearing section

⛔ **A claimed RED I could not reproduce would be the most important finding in this report. I reproduced every one I tried, verbatim.** Method throughout: `cp` byte backup → one edit → run the real gate → read the runner's own output → `cp` restore → `cmp` → `git status --porcelain`. **`git checkout --` was never used.**

| # | Lever (mine, applied on disk at HEAD) | Gate run | Result | Matches recorded claim? |
|---|---|---|---|---|
| A | `-6` branch guard changed to `code === -99999` (branch can never fire) | `node scripts/prod-prober/run.mjs --self-test` | exit **1**. `SELF-TEST FAIL: the defect kind is mt5-not-authorized (got mt5-terminal-error)` + `FAIL: (a) the -6 fixture's ROW is mt5-not-authorized on subject -6 (got mt5-terminal-error / -6)` + `(c)` the lookup-table negative fired | ✅ **VERBATIM** match to lever 1's recorded transcript at `prod-prober-wiring.test.ts` |
| A′ | same mutation, durable falsifier | `npx vitest -t "164.8.3 FALSIFIER"` | exit **1**, all 3 tests RED with `Error: ANCHOR MISSING: "  if (code === -6 && probe.initialize !== true) {" …` | ✅ Proves the `anchorIndex` discipline fires by name instead of degenerating into a vacuous `slice(-1)` |
| B | `REMEDIES["mt5-not-authorized"]` pointed at the exact `mt5-terminal-error` string | `--self-test` | exit **1**. `SELF-TEST FAIL: no two mt5 remedies are the same string — two kinds with one remedy is two kinds pretending to be one` + `(b) … VNC console=false, "Save password"=false, Expert Advisors=false, Journal=false` | ✅ **VERBATIM** match to lever 2's recorded transcript |
| C | `build=6182` re-added to the OK info line (my own lever, criterion 7) | `--self-test` | exit **1**. `SELF-TEST FAIL: 164.8.3 CRITERION 7: and it carries NO build= …` | ✅ Criterion 7's assertion is a reading, not decoration |

**Restore proof:** after every lever, `cmp` against the pristine backup was silent, `git status --porcelain` showed only the pre-existing untracked `164.11-*` directory, and `--self-test` returned to `=== SELF-TEST PASSED: 80/80 scenarios … ===`.

---

## Behavioral Spot-Checks (run by me)

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full prober self-test | `node scripts/prod-prober/run.mjs --self-test` | `SELF-TEST PASSED: 80/80`, exit 0 | ✓ PASS |
| Wiring test suite | `npx vitest run src/__tests__/prod-prober-wiring.test.ts` | 86 passed | ✓ PASS |
| Full prod-prober blast radius (all 7 test files referencing `prod-prober`) | `npx vitest run gitleaks-allowlist supabase-migrate-test-first restore-test-from-baseline test-restore-workflow-wiring drift-check-scripts prod-prober-wiring lint-app-guc` | **900 passed / 1 skipped**, 7 files | ✓ PASS |
| CI-shaped plan-anchor gate | `node scripts/verify-plan-anchors.mjs --pending` | `OK: 3 plan file(s), no stale claims` | ✓ PASS |
| Planning hygiene leak gate (public repo, `.planning/` tracked) | `npx tsx scripts/check-planning-hygiene.ts` | `OK — 6369 tracked files scanned` | ✓ PASS |
| Issue #753 state | `gh issue view 753` | `CLOSED 2026-09-10T16:13:58Z` | ℹ️ INFO (see Requirements) |

⚠️ **Reported honestly rather than smoothed over:** running `node scripts/verify-plan-anchors.mjs` against this phase's four **executed** PLANs returns `FAIL: 111 stale claim(s)`. That is **expected and NOT a gate failure** — the script excludes plans with a sibling SUMMARY by design (`PENDING PLANS ONLY, in --pending mode. A plan with a sibling SUMMARY has been executed; its anchors describe a tree that has legitimately moved on`), and the CI job runs `--pending`, which is green. Recorded so nobody re-derives it as a defect.

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `scripts/prod-prober/fixtures/mt5/6.txt` | The real `-6` transcript | ✓ VERIFIED | 168 bytes, carries the measured 2026-09-09 live reading; wired into the fixture table's `red:` map |
| `scripts/prod-prober/arms/mt5.mjs` | Branch (6b), remedy, two-boolean probe | ✓ VERIFIED | +109 lines; branch (6b) sits **above** branch (7) and (8), so it is not dead code below the unguarded tail |
| `scripts/prod-prober/run.mjs` | 5 registrations + scenarios 24, 75 | ✓ VERIFIED | +345 lines; `SELF_TEST_SCENARIOS = 80` matched by the hand-typed pin at `prod-prober-wiring.test.ts:1893` and by the runner's own printed header count |
| `src/__tests__/prod-prober-wiring.test.ts` | Falsifier, dedup pin, criterion-8 pin, transcript scan | ✓ VERIFIED | +428 lines, 86 tests green |
| `scripts/prod-prober/fixtures/mt5/ok.txt` | Two booleans, no machine path | ✓ VERIFIED | `"terminal_info": {"connected": true, "trade_allowed": true}`; the `C:\Program Files\MetaTrader 5` path is gone |
| `TODOS.md` | One deferral, named destination | ✓ VERIFIED | `[164.8.3-CAPTURE-MANIFEST-ERREXIT]` → Phase 164.8.4, as a nomination (see Human Verification) |

### Fence verification (files this phase must NOT touch)

| File | Status |
|---|---|
| `.github/workflows/prod-prober.yml` | ✓ byte-unchanged vs `e64b0811` |
| `scripts/mt5-diag.sh` | ✓ byte-unchanged |
| `fixtures/mt5/10004.txt`, `10005.txt`, `init-false-other.txt`, `no-probe-line.txt`, `terminal-info-null.txt` | ✓ all byte-unchanged |

---

## Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `6.txt` | branch (6b) | `classifyProbe` on the fixture transcript | ✓ WIRED (scenario 24/80) |
| branch (6b) | `REMEDIES["mt5-not-authorized"]` | `addDefect(verdict.kind, …, REMEDIES[verdict.kind])` at `mt5.mjs:434` | ✓ WIRED |
| `REMEDIES` | the printed defect ROW | `rowFor("6.txt")` drives the real `runProber` path, not the table | ✓ WIRED — ⭐ this is the link that matters: criterion 2 is asserted on what an operator sees |
| `DEFECT_KINDS` ↔ fixture-table `kinds:` ↔ `red:` ↔ `KIND_ASSERTIONS` ↔ `EXPECTED_DEFECT_KINDS` | all five | set-equality gate | ✓ WIRED — a partial landing is refused |
| `SELF_TEST_SCENARIOS` (80) | hand-typed pin (80) | `prod-prober-wiring.test.ts:1893` | ✓ WIRED (not derived from the export — the literal is hand-typed on purpose) |
| `arms/mt5.mjs` | tmp-dir mutant | `execFileSync(process.execPath, …)` | ✓ WIRED — child Node, no bundler; the plan's `await import()` was impossible under vitest and the deviation is recorded |
| `WORKFLOW_TEXT` | auto-issue step | `const dedupLabel = "prod-prober"` | ✓ WIRED, kind-independent |

---

## Requirements Coverage

| Requirement | Source | Status | Evidence |
|---|---|---|---|
| `#753` | all four PLANs' `requirements:` frontmatter | ℹ️ **ACCOUNTED FOR — no REQUIREMENTS.md row exists, and none was invented** | Measured: `grep -c '753' .planning/REQUIREMENTS.md` → **0**; `grep -c '164.8.3'` → **0**. ⭐ This is **authorized by the ROADMAP itself**, which states the requirement as `TBD (no v1.20 requirement IDs) + GitHub issue #753`. `#753` is a GitHub issue id used as the requirement token because v1.20 defines no REQ-IDs for this phase. |

**Orphaned requirements:** none — `.planning/REQUIREMENTS.md` maps no additional IDs to Phase 164.8.3.

⚠️ **Stated plainly, per the honest-verification contract:** I have **not** marked a requirement complete that does not exist. `#753` (GitHub) is `CLOSED` as of `2026-09-10T16:13:58Z` — closed by the **founder's hand fix of the live terminal**, not by this phase. The phase's own fence says so: *"⛔ This phase does NOT clear the live outage … Do not treat a green prober as this phase's acceptance signal."* This phase's contribution to `#753` is the **vocabulary**, which is delivered and machine-proven; the outage itself is out of its gift and belongs to Phase 164.6.2 MT5RELOGIN.

---

## Anti-Patterns Found

| File | Pattern | Result |
|---|---|---|
| `arms/mt5.mjs`, `run.mjs`, `prod-prober-wiring.test.ts`, `6.txt`, `ok.txt` | `TBD` / `FIXME` / `XXX` (blocker tier) | **0 hits** |
| same | `TODO` / `HACK` / `PLACEHOLDER` (warning tier) | **0 hits** |
| all phase-modified files | hardcoded empty returns, stub shapes | none — every added path is exercised by a scenario or an `it()` |

**Debt-marker gate: CLEAN.**

### Known-and-recorded (per the dispatch — not re-reported as new)
- **WR-03** — the two absence lists were structurally unable to fail; fixed by making the coverage real (roster-derived). ✓ confirmed fixed at HEAD.
- **WR-04** — four `file:line` citations replaced with symbol citations. ✓ confirmed (commit `7a105db9`).
- **WR-05** — `.txt` transcripts were outside every scan; now inside `proberTranscripts` with an emptiness guard (`toBeGreaterThan(0)`), a `toContain("…/ok.txt")` reach assertion, and a four-shape calibration. ✓ confirmed.

---

## Informational Notes (no action required from this phase)

1. ℹ️ **`STATE.md`'s narrative quotes the falsifier anchor as `  if (code === -6) {`.** The live anchor is `  if (code === -6 && probe.initialize !== true) {` — the guard review finding WR-02 added *after* plan 04 wrote that narrative. The **test file's anchor is correct** and I proved it throws by name when it moves (lever A′). This is stale prose in STATE.md, not a code gap.
2. ℹ️ **The full `npm test` phase gate was not run by the executor** (a dispatch constraint, honestly disclosed in `STATE.md:8`). I closed the blast radius instead: **all 7 test files in the repo that reference `prod-prober` are green (900 passed)**, plus the orchestrator's 10-file / 435-test contract subset that scans all of `src/`. Residual risk: a full-suite run has still not happened on this branch; CI will be the first.
3. ℹ️ **Every deviation recorded in the four SUMMARYs is a STRENGTHENING**, and each is independently confirmed in the tree: the `Array.isArray` guard (03-D1), the four criterion-7 assertions moved outside the `&&` chain so one RED is never credited to four controls (03-D2), the `execFileSync` mutant driver (04-D1), and the branch-sized `< 1200` byte-delta bound on the excision (04-D2). No deviation widened scope; `git show --stat` confirms the file sets.

---

## Gaps Summary

**None.** No truth FAILED, no artifact is missing or a stub, no key link is unwired, and no blocker anti-pattern exists. Every fence held byte-for-byte. The one open item is a **routing decision reserved to the founder by Phase 164.8.4's own recorded fence**, not a defect in this phase's delivery — it is listed under `human_verification` above.

⭐ **The specific failure mode this verification was sent to find — a phase whose criteria are satisfied by text an author typed rather than by behavior a machine reads — is not present here.** Criterion 2 is asserted on the runtime defect ROW (not the `REMEDIES` table), with a calibration that splices the removed sentence back in and watches the predicate reject it; the public-log digit-run control is gated by a liveness check on the table it scans; the absence lists derive from the kind roster; and both recorded REDs reproduced verbatim under my own hands.

---

## Routing Item — Resolved and Re-verified (2026-09-13T18:43:11Z)

The single `human_needed` item is **closed**. ⛔ I verified the resolution against the tree rather than accepting the account of it; each claim below was measured.

| Claim to check | How I checked it | Result |
|---|---|---|
| The `TODOS.md` edit is confined to one sentence of `[164.8.3-CAPTURE-MANIFEST-ERREXIT]` | `git diff --stat TODOS.md`, then the full hunk | ✓ **1 insertion / 1 deletion**, a single line inside that entry. Nothing else in `TODOS.md` moved. |
| The edit does not disturb what plan 04's must-have rested on | Read the replaced line against the original | ✓ The must-have was *"booked with a NAMED destination phase."* **`Destination: Phase 164.8.4 GATERESIDUE` survives verbatim**; only `"this item awaits the same explicit decision before it is in scope"` was replaced, by a ✅ record of the decision plus a pointer to criterion 9's forbidden half-fix. The `⛔ ROADMAP was NOT hand-edited` clause is also intact. The fact is **strengthened, not altered**. |
| Phase 164.8.3's own ROADMAP section is byte-unchanged | Extracted the section by header anchor from `HEAD` and from the working tree, `cmp`'d them | ✓ **Byte-identical — 12,710 bytes both.** The section only shifted down 11 lines (starts at 1549 in HEAD, 1560 in the tree). |
| Every ROADMAP hunk sits inside the 164.8.4 block | `git diff -U0` hunk headers vs. the section boundaries | ✓ All **six** hunks fall in old lines 1470–1541; the 164.8.4 block runs 1468→1548. None reaches 164.8.3. |
| The `human_verification` expectation is literally met | Read the 164.8.4 entry | ✓ **Met.** The expectation was *"The item appears in `.planning/ROADMAP.md` under a named phase."* `[164.8.3-CAPTURE-MANIFEST-ERREXIT]` now appears in **five** places in that entry: the goal line (*"by founder decision 2026-09-13"*), an **ADMISSION 2** merits block, the group (d) bullet, new **success criterion 9**, and the Requirements list. |
| The fence was re-armed rather than removed | Read the text that replaced `⚠️ NOT A PRECEDENT` | ✓ `⚠️ **STILL NOT A PRECEDENT.** A THIRD outside item may be admitted only by the same explicit decision … if a fourth candidate appears, the right answer is a new phase, not a third widening.` |

⭐ **Two things worth recording beyond the checklist.**

1. **The admission is not a parking space.** Criterion 9 does not merely ask for the fix — it names the **forbidden half-fix in advance** (*"a widened count over an unwidened scan is the coverage overclaim this phase exists to end"*) and requires a **calibration that reverts the fixed step to its `-e`-unreachable shape and observes RED**. That is this repo's anti-vacuity standard applied to the deferral itself, and it is the shape my report said was missing when the item lived only in TODOS.md. Phase 164.8.4 remains `**Plans:** 0 plans` — an upcoming, unplanned phase, which is the preferred destination for a deferral.
2. ℹ️ **One file changed that was not in the account given to me: `.planning/STATE.md`.** I checked it rather than assume. It is **+1 line, 0 deletions** — a single Roadmap-Evolution entry recording the 164.8.4 edit. No handler clobber (no `progress:` reset, no key relocation, no blank-line injection), it touches nothing this report rests on, and it is not in `covered_files`, so it does not bear on the digest. Recorded because an unreported change is worth naming even when it is benign.

### Fingerprint

The stale-verification gate **fired correctly** on the `TODOS.md` byte change, and the digest was **recomputed, never hand-patched** — clearing a red gate by editing the number it checks is the exact shape this repo forbids.

| | Value |
|---|---|
| Original | `v1:sha256:89f5ae0e…04917` (status `human_needed`) |
| Re-stamped | `v1:sha256:bca445fb…a97d31` |
| Computed by | `gsd-tools query verification.fingerprint` — the tool's own computation, same 17-file list, so it is the value the gate will compute |
| Covered-file drift since the original stamp | **`TODOS.md` only.** `git status --porcelain` over `scripts/prod-prober/`, `src/__tests__/prod-prober-wiring.test.ts` and the phase directory returns no modified entries. |

### Re-affirmation

**The 32/32 result stands, unchanged and un-re-run** (per the dispatch, only the routing item and the fingerprint were in question). The phase's central gate was re-run once at re-stamp time as grounding: `node scripts/prod-prober/run.mjs --self-test` → `=== SELF-TEST PASSED: 80/80 scenarios … ===`. Status **`passed`**.

---

_Verified: 2026-09-13T17:40:00Z; re-affirmed 2026-09-13T18:43:11Z_
_Verifier: Claude (gsd-verifier)_
