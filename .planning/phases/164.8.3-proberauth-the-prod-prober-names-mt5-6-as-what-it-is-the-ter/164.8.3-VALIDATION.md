---
phase: "164.8.3"
slug: "proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: true
wave_0_complete: true
created: "2026-09-13"
filled: "2026-09-13"
---

# Phase 164.8.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

⛔ **The acceptance signal for this phase is the fixture-driven self-test plus the falsifier's
observed RED. It is NOT a green hourly `prod-prober` run.** The live `-6` is cleared by a founder
VNC action and is out of this phase's gift; a green prober would measure the human fix, not the
code. A live `--arm mt5` dispatch is a bonus observation and never a gate — measured both ways
within 24 hours (run `34706551355` read healthy on 2026-09-12; run `34758502223` read `-6` on
2026-09-13).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | the runner's own `--self-test` (fixtures only, no network) + vitest (`vitest.config.ts`) |
| **Config file** | `vitest.config.ts` — ⛔ read the coverage ratchet live, never from a number restated anywhere |
| **Quick run command** | `node scripts/prod-prober/run.mjs --self-test` |
| **Full suite command** | `npx vitest run src/__tests__/prod-prober-wiring.test.ts`, then `npm test` |
| **Estimated runtime** | self-test < 1 s; the wiring-test file a few seconds; `npm test` minutes |

⚠️ `npm test` must not share the box with another full vitest run.

---

## Sampling Rate

- **After every task commit:** `node scripts/prod-prober/run.mjs --self-test` (expect `PASSED: N/N`, exit 0)
- **After every plan wave:** `npx vitest run src/__tests__/prod-prober-wiring.test.ts`
- **Before `/gsd-verify-work`:** full `npm test` green, plus both observed RED transcripts present in the commit history
- **Max feedback latency:** ~1 second (the self-test); no task in this phase is more than one task away from an automated check

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 164.8.3-01-01 | 01 | 1 | #753 · C1 C2 C3 C4 · D-01 D-02 D-03 D-04 | T-164.8.3-01 / -02 / -03 / -04 | the remedy and the detail carry no account number, no broker server, no password, and echo no raw ssh stdout; `MT5_PROBE_PY` gains no call | fixture replay + contract | `node scripts/prod-prober/run.mjs --self-test` · `npx vitest run src/__tests__/prod-prober-wiring.test.ts` · the `REMEDY-CHECK` node scan | ✅ (fixture `6.txt` is created BY this task) | ⬜ pending |
| 164.8.3-01-02 | 01 | 1 | #753 · C8 | T-164.8.3-05 | the auto-issue step's issue SELECTION reads zero defect kinds, so a new kind can neither be suppressed behind an old issue nor open a second P1 | contract (text predicates + calibration) | `npx vitest run src/__tests__/prod-prober-wiring.test.ts -t 'AUTO-ISSUE DEDUP'` · the `CRITERION-8 PIN` node scan | ✅ | ⬜ pending |
| 164.8.3-02-01 | 02 | 2 | #753 · C2 C4 · D-03 D-04 | T-164.8.3-01 / -08 / -09 | no mt5 remedy carries a six-or-more-digit run (the account-number shape) — calibrated | fixture replay (row-level) | `node scripts/prod-prober/run.mjs --self-test` · the `PUBLIC-LOG SCAN` node scan | ✅ | ⬜ pending |
| 164.8.3-02-02 | 02 | 2 | #753 · C4 | T-164.8.3-10 | a by-name absence list cannot grow while its own printed sentence stays narrower | fixture replay + source scan | `node scripts/prod-prober/run.mjs --self-test` · the absence-list node scan | ✅ | ⬜ pending |
| 164.8.3-03-01 | 03 | 3 | #753 · C5 C7 · D-05 | T-164.8.3-03 / -04 / -11 / -12 / -13 | the committed probe constant stays a literal with no `${`; `path` stops reaching a PUBLIC log; a nested object is never dereferenced before the presence check; an absent key is never rendered `false` | fixture replay + unit | `node scripts/prod-prober/run.mjs --self-test` · the `PROBE-BODY` and `OK-FIXTURE` node scans · the fenced-files `git status` scan | ✅ | ⬜ pending |
| 164.8.3-03-02 | 03 | 3 | #753 · C7 · D-05 | T-164.8.3-12 | the operator-visible info line is asserted END-TO-END on the captured log line, so what reaches the public log is what is measured | fixture replay (captured log) | `node scripts/prod-prober/run.mjs --self-test` · `npx vitest run src/__tests__/prod-prober-wiring.test.ts` | ✅ | ⬜ pending |
| 164.8.3-04-01 | 04 | 4 | #753 · C6 | T-164.8.3-15 | the mutant module lands only under `mkdtempSync` and is removed in a `finally`; nothing under `scripts/` is written | mutant import + contract | `npx vitest run src/__tests__/prod-prober-wiring.test.ts -t '164.8.3 FALSIFIER'` | ✅ | ⬜ pending |
| 164.8.3-04-02 | 04 | 4 | #753 · C6 | T-164.8.3-14 / -16 / -17 | byte backups only, `cmp`-verified, `git checkout --` prohibited; transcripts contain no absolute home path and no credential | recorded manual neuter + record gate | `node scripts/prod-prober/run.mjs --self-test` + `git status --porcelain -- scripts/` · the `NEUTER-RECORD` node scan | ✅ | ⬜ pending |
| 164.8.3-04-03 | 04 | 4 | #753 (deferral routing) | — | nothing under `.github/` or `scripts/` is modified — the observation is booked, never fixed here | source scan | the `DEFERRAL-BOOKING` node scan · the `SCOPE OK` git scan | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Sampling continuity:** every one of the nine tasks carries at least one `<automated>` check with
a stated `<fails_when>`. There is no run of three consecutive tasks without one, and no
watch-mode flag is used anywhere.

---

## Wave 0 Requirements

**Existing infrastructure covers all phase requirements.** No framework install, no `conftest`
equivalent, no new harness.

The four gaps RESEARCH identified are all created INSIDE the plan set by the task that needs
them, never as a separate Wave 0:

- [x] `scripts/prod-prober/fixtures/mt5/6.txt` — created by plan 01 task 1, which is also its first consumer (covers C1, C3, C6)
- [x] the criterion-2 row scenario — created by plan 02 task 1 (covers C2)
- [x] the falsifier block — created by plan 04 task 1 (covers C6)
- [x] the OK info-line field-set assertion — created by plan 03 task 2 (covers C7)

⚠️ A fixture named in `ARM_FIXTURE_TABLE` and MISSING from disk is a SELF-TEST FAIL, never a skip
(`run.mjs:684`), so a half-created fixture cannot pass quietly.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The self-test SCENARIO (not just the classifier) goes RED when the `-6` branch is neutered, and again when the remedy is pointed at the catch-all's string | C6 | An on-disk neuter of a tracked source file cannot run inside CI without risking the working tree; the repo's own idiom for it is a recorded manual procedure (`prod-prober-wiring.test.ts:1899-1912`, commit `98357965`) | Plan 04 task 2, step by step: `cp` the arm aside into a `mktemp -d`; drop ONE lever; run `node scripts/prod-prober/run.mjs --self-test`; capture the failing line verbatim; `cp` back; `cmp`; re-run green; re-take the backup; repeat for lever 2. ⛔ Never `git checkout --`. |
| A live `--arm mt5` dispatch showing the new kind and remedy in production | — | Requires Railway credentials and a terminal that happens to be in the `-6` state; the reading is intermittent (measured healthy 2026-09-12, `-6` 2026-09-13) | **BONUS ONLY, never a gate.** If run, read the LOG ARCHIVE (`gh api …/actions/runs/{id}/logs`), ⛔ never `gh run view --log` — it truncates and has already produced a wrong reading in this repo. |

The manual half of C6 is paired with a DURABLE automated falsifier (plan 04 task 1) that re-runs
on every CI shard, so the criterion is not left resting on a one-day observation.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (created in-plan by their first consumer)
- [x] No watch-mode flags
- [x] Feedback latency < 2s for the quick command
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-09-13 (planning)
