---
phase: "164.8.3"
plan: "02"
type: execute
wave: 2
depends_on: ["164.8.3-01"]
files_modified:
  - scripts/prod-prober/run.mjs
  - src/__tests__/prod-prober-wiring.test.ts
autonomous: true
requirements: ["#753"]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A machine gate reads the `-6` DEFECT ROW an operator actually sees — not the `REMEDIES` table — and proves its remedy names the VNC console, the \"Save password\" step, the Expert-Advisors options and the Journal (C2, D-04)."
    - "The same gate proves, NEGATIVELY and with a calibration, that the `-6` row's remedy carries no lookup-table instruction — the sentence this phase exists to remove (C2)."
    - "The `-6` remedy and the `-10005` remedy are proven non-overlapping on their distinguishing words, so the split cannot degenerate into two near-identical instructions that pass the exact-string uniqueness gate."
    - "Both catch-all fixtures still produce `mt5-terminal-error` rows and their SUBJECTS differ, so the bucket's discrimination is measured rather than asserted (C4, D-03)."
    - "No mt5 remedy contains a six-or-more-digit run, calibrated by a mutant that splices one in — the account-number control for a string that lands in a PUBLIC log."
    - "The two hand-kept by-name ABSENCE lists both name `mt5-not-authorized`, and the second list's prose numeral moves with it (C4)."
  artifacts:
    - scripts/prod-prober/run.mjs
    - src/__tests__/prod-prober-wiring.test.ts
  key_links:
    - "`rowFor(fixture)` (`run.mjs:3828-3839`) → `runProber` → the shipping `addDefect` path → the defect ROW's `remedy`/`subject`/`kind` fields. ⭐ This drives the real path, not the table."
    - "`noDefectOfKind` (`run.mjs:552-568`) — the ONE permitted spelling for an absence; an inline `kind === \"<k>\"` in an absence would be a vacuity INSIDE the anti-vacuity harness and would also be miscredited as coverage by the wiring test's extractor."
    - "`SELF_TEST_SCENARIOS` (`run.mjs:149`) ↔ the literal pin (`prod-prober-wiring.test.ts:1822`) — this plan's one new `scenario()` moves both again."
  prohibitions:
    - "Never assert criterion 2's prohibition by grepping a SOURCE FILE — `arms/mt5.mjs` legitimately still contains the lookup-table sentence in the surviving catch-all remedy, so a file grep would be self-invalidating. Assert it on the runtime ROW string."
    - "Never spell an absence assertion as an inline `kind === \"<k>\"` — use `noDefectOfKind`, per `run.mjs:552-568`."
    - "Never write a negative assertion without a calibration proving it can fire; a predicate only ever applied to passing input is not evidence (`prod-prober-wiring.test.ts:15-17`)."
    - "Never narrow or rewrite `REMEDIES[\"mt5-terminal-error\"]` in this plan — criterion 2's prohibition is scoped to the `-6` kind, and the lookup instruction is correct advice for a genuinely unenumerated code."
    - "Never let a remedy or detail quote an MT5 account number, a broker server name, or a password."
    - "Never derive `expect(SELF_TEST_SCENARIOS).toBe(N)` from the export — hand-type the literal."
    - "Never edit `.github/workflows/prod-prober.yml` or `scripts/mt5-diag.sh`."
    - "Never use a bare `grep -c <token> <file>` as a gate over a file whose own comments contain that token."
---

<objective>
Turn criteria 2 and 4 from text into calibrated gates read off the DEFECT ROW an operator sees.

Purpose: plan 01 shipped the remedy. Nothing yet REFUSES a future edit that reintroduces the
lookup-table instruction, collapses the `-6` remedy back into a near-copy of `-10005`'s, or
empties the catch-all bucket. The `-10004`/`-10005` pair already has exactly this gate at
`run.mjs:3902-3917`; this plan applies the same shape once more, plus the negative half that
pair never needed.

Output: one new self-test scenario in `scripts/prod-prober/run.mjs`, the two by-name ABSENCE
lists extended, and the scenario-count pair moved again.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-CONTEXT.md
@.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-PATTERNS.md
@.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-01-SUMMARY.md
</context>

<measured_baseline>
| Fact | Value | Anchor |
|---|---|---|
| The analog scenario to mirror | *"the COMMITTED probe reaches the container byte-for-byte, and the two IPC remedies are different (D-16, D-17)"* | `run.mjs:3788-3920` |
| Its row-level helper | `const rowFor = async (fixture) => {…}` returning `rr.defects[0] \|\| null` | `run.mjs:3828-3839` |
| Its criterion-style assertions | remedies differ + each ≥ 40; they say OPPOSITE things; each row names its own code as subject | `run.mjs:3906-3917` |
| Absence list A | `["mt5-no-ipc", "mt5-ipc-timeout", "mt5-probe-timeout", "mt5-terminal-error"]`, message *"a transport failure is NOT reported as any statement about the terminal"* | `run.mjs:3695-3697` |
| Absence list B | `["mt5-ipc-timeout", "mt5-no-ipc", "mt5-ssh-transport"]`, message *"NONE of the **three** states that would send an operator to the gateway fired"* | `run.mjs:3777-3779` |
| Absence list C (auto-derived) | `DEFECT_KINDS.filter((k) => k !== expectedKind)` | `run.mjs:1162-1164` — **no edit; it widens by itself** |
| Digit-run scan over all five current mt5 remedies | zero matches for `\b\d{6,}\b` | measured at HEAD `de1a3947` |
| Count after plan 01 | 79 | plan 01's verify |
</measured_baseline>

<artifacts_this_plan_produces>
| Symbol | Kind | Where |
|---|---|---|
| self-test scenario titled with the literal `164.8.3 CRITERION 2` | new counted scenario | `scripts/prod-prober/run.mjs`, `selfTest()` |
| `SELF_TEST_SCENARIOS = 80` | exported count | `run.mjs:149` |
| `expect(SELF_TEST_SCENARIOS).toBe(80)` | literal pin | `src/__tests__/prod-prober-wiring.test.ts:1822` |
| `mt5-not-authorized` added to absence lists A and B | extended by-name absence assertions | `run.mjs:3695-3697`, `run.mjs:3777-3779` |
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the criterion-2 row scenario — positive words, the negative, and both calibrated</name>

  <read_first>
    - `scripts/prod-prober/run.mjs:3788-3920` in full — the scenario this one mirrors, including `rowFor` and the `⛔ SUCCESS CRITERION 3:` assertion voice.
    - `scripts/prod-prober/run.mjs:1027-1031` — `scenario()`; every call increments the counted set.
    - `scripts/prod-prober/run.mjs:4150-4159` — the tail completeness assertion that PRINTS the true count.
    - `scripts/prod-prober/arms/mt5.mjs:124-144` — `REMEDIES` as shipped by plan 01, including the exact `-6` string.
    - `src/__tests__/prod-prober-wiring.test.ts:15-17` — *"EVERY PREDICATE HERE IS WRITTEN OVER ARBITRARY TEXT AND CALIBRATED ON A MUTATED COPY."*
    - `.planning/phases/164.8.3-…/164.8.3-01-SUMMARY.md` — the shipped remedy string, so the asserted words are read off what landed rather than off this plan's paraphrase.
  </read_first>

  <files>scripts/prod-prober/run.mjs, src/__tests__/prod-prober-wiring.test.ts</files>

  <behavior>
    - `rowFor("6.txt")` returns a row with `kind === "mt5-not-authorized"` and `subject === "-6"`.
    - That row's `remedy` contains each of the four required elements as literal substrings: the VNC console, the "Save password" step, the Expert-Advisors option phrase, and the Journal.
    - That row's `remedy` does NOT contain the lookup-table instruction. A mutant of the same string with the instruction spliced back in IS rejected by the same predicate.
    - `rowFor("6.txt").remedy !== rowFor("init-false-other.txt").remedy`, both ≥ 40 chars.
    - The `-6` remedy does not carry the `-10005` remedy's distinguishing modal-dialog wording, and the `-10005` remedy does not carry the Journal — so the two instructions cannot converge.
    - `rowFor("terminal-info-null.txt")` and `rowFor("init-false-other.txt")` both have `kind === "mt5-terminal-error"` and their `subject`s DIFFER.
    - No value of `MT5.REMEDIES` matches `\b\d{6,}\b`; a mutant with a synthetic eight-digit run spliced in DOES match.
  </behavior>

  <action>
    Add ONE new counted `scenario()` to `selfTest()` in `scripts/prod-prober/run.mjs`, placed
    immediately after the existing byte-for-byte / two-IPC-remedies scenario so the two
    criterion-style scenarios read together. **The scenario title MUST contain the literal
    `164.8.3 CRITERION 2`** — the verify greps the printed header for it, and a title without it
    reads as a vacuous green.

    Build the rows with the SHIPPING path, exactly as the analog does: copy the local `rowFor`
    helper shape from `run.mjs:3828-3839` (a fresh `runProber` per fixture with
    `createSeams({ sshRunner: fixtureSsh({ stdout: fx.data }) })`, `armsFloor: 1`, `log: quiet`,
    returning `rr.defects[0] || null`). ⭐ Assert on the ROWS, never on `arm.REMEDIES` directly —
    a table assertion would pass even if `addDefect` stopped attaching the remedy.

    **(a) Identity — C1/D-01.** The `6.txt` row's `kind` and `subject`. Guard the row against
    `null` before dereferencing, the way the analog does with `row4 !== null && row5 !== null`.

    **(b) The four required elements — C2, D-04.** Assert the `-6` row's remedy `.includes()`
    each of the four literals, read off the string plan 01 actually shipped. Put all four in ONE
    assertion whose message names them individually, so a failure says which element is missing.

    **(c) ⛔ THE NEGATIVE — the sentence this phase removes (C2).** Assert the `-6` row's remedy
    does NOT contain the lookup-table instruction. ⛔ **Assert this on the RUNTIME ROW STRING,
    never by grepping `arms/mt5.mjs`:** the surviving `mt5-terminal-error` remedy legitimately
    still contains that sentence, and a source grep would either be permanently red or would
    have to be narrowed into something that measures nothing. A row-level predicate reads only
    the string the operator receives, so nothing written in a comment, a doc or this plan can
    affect it.

    **(d) The calibration for (c).** In the same scenario, splice the forbidden instruction into
    an IN-MEMORY copy of the `-6` remedy, assert the copy actually differs from the original,
    and assert the SAME predicate REJECTS the copy. Without this, (c) is a predicate only ever
    applied to passing input.

    **(e) Non-convergence with `-10005`.** Assert the `-6` remedy and the catch-all remedy are
    different strings and each ≥ 40 chars (mirroring `run.mjs:3906-3909`), and — the part the
    exact-string uniqueness gate cannot see — that the `-6` remedy does not carry the `-10005`
    remedy's distinguishing modal-dialog wording while the `-10005` remedy does not carry the
    Journal. Near-duplicates pass `run.mjs:1111-1114` and fail the operator.

    **(f) The catch-all's discrimination — C4, D-03.** Assert both catch-all fixtures still
    produce `mt5-terminal-error` AND that their two `subject` values differ. ⭐ The differing
    subjects are what makes "the bucket still means something" a MEASUREMENT rather than a
    restatement: one row comes from branch (7) and one from branch (8).

    **(g) The public-log account-number control.** Range over EVERY value of the mt5 arm's
    `REMEDIES` and assert none matches a six-or-more-digit run — measured clean at HEAD across
    all five current remedies, so the assertion starts true and stays meaningful. Calibrate it:
    append a synthetic eight-digit run to an in-memory copy of one remedy, assert the copy
    differs, and assert the same predicate now matches. ⛔ Do not write a real account number
    anywhere, including in the calibration — generate the synthetic run from digits that are not
    an account (e.g. a repeated digit).

    **(h) The counters.** One new `scenario()` call means the counted set grows by exactly one.
    Bump `run.mjs:149` and the hand-typed literal at `prod-prober-wiring.test.ts:1822` to 80 in
    the same commit. ⛔ If the run's tail assertion reports a different number, the run is right
    — set both to what it counted.
  </action>

  <verify>
    <automated>ST=$(node scripts/prod-prober/run.mjs --self-test 2>&amp;1); printf '%s\n' "$ST" | tail -4 &gt;&amp;2; printf '%s\n' "$ST" | grep -q 'SELF-TEST PASSED: 80/80' &amp;&amp; printf '%s\n' "$ST" | grep -q '164.8.3 CRITERION 2' &amp;&amp; ! printf '%s\n' "$ST" | grep -q 'SELF_TEST_SCENARIOS is 79'</automated>
    <fails_when>non-zero exit; or the output lacks the literal `SELF-TEST PASSED: 80/80`; or no printed scenario header contains `164.8.3 CRITERION 2`, which means the new scenario was never registered and the pass is vacuous; or the completeness line reads `SELF_TEST_SCENARIOS is 79`, naming a missed constant bump.</fails_when>

    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
    <fails_when>non-zero exit, or the summary line does not read `Test Files  1 passed`. A failure naming `SELF_TEST_SCENARIOS` means the literal pin at `:1822` was not moved to 80; one naming `uncovered` means an inline `kind === "<k>"` was written into an absence and the coverage extractor miscredited it.</fails_when>

    <automated>node --input-type=module -e 'const m = await import("./scripts/prod-prober/arms/mt5.mjs"); const n = Object.keys(m.REMEDIES).length; const bad = Object.entries(m.REMEDIES).filter(([,v]) =&gt; /\b[0-9]{6,}\b/.test(v)).map(([k]) =&gt; k); if (bad.length) { console.log("PUBLIC-LOG SCAN FAIL: " + bad.join(",")); process.exit(1); } if (n !== 6) { console.log("PUBLIC-LOG SCAN FAIL: scanned " + n + " mt5 remedies, expected 6"); process.exit(1); } console.log("PUBLIC-LOG SCAN OK: 0 long digit runs in 6 mt5 remedies");'</automated>
    <fails_when>non-zero exit, or the printed line starts with `PUBLIC-LOG SCAN FAIL:`. `FAIL: <kind>` means a remedy destined for a PUBLIC Actions log and a PUBLIC issue carries a six-or-more-digit run, which is the shape of an account number. `FAIL: scanned N mt5 remedies, expected 6` means the scan ranged over a table that is not the post-phase one — a green from a stale or partially-landed `REMEDIES` proves nothing.</fails_when>
  </verify>

  <acceptance_criteria>
    - One new counted scenario exists whose printed header carries `164.8.3 CRITERION 2`, and the self-test count moved 79 → 80 in both constants.
    - The `-6` row is asserted by KIND, by SUBJECT, by four positive remedy literals, and by one NEGATIVE — and the negative carries a calibration proving it fires (C2, D-04).
    - The negative is written over the runtime ROW string; no source-file grep for the forbidden sentence exists anywhere in this phase.
    - Both catch-all rows still read `mt5-terminal-error` with DIFFERENT subjects (C4, D-03).
    - The digit-run scan over every mt5 remedy is green and calibrated.
    - `REMEDIES["mt5-terminal-error"]` is byte-unchanged.
  </acceptance_criteria>

  <done>
    `node scripts/prod-prober/run.mjs --self-test` prints `SELF-TEST PASSED: 80/80` with a header
    containing `164.8.3 CRITERION 2`; the wiring test file passes; the public-log scan prints
    `PUBLIC-LOG SCAN OK: 0 long digit runs in 6 mt5 remedies` (C2, C4, D-03, D-04).
  </done>
</task>

<task type="auto">
  <name>Task 2: both by-name ABSENCE lists learn the new kind, and the prose numeral moves with one</name>

  <read_first>
    - `scripts/prod-prober/run.mjs:3660-3700` — the `mt5-ssh-transport` scenario and absence list A with its message.
    - `scripts/prod-prober/run.mjs:3760-3785` — the `mt5-probe-timeout` scenario and absence list B with its message.
    - `scripts/prod-prober/run.mjs:552-568` — `noDefectOfKind` and its docblock.
    - `scripts/prod-prober/run.mjs:1162-1164` — absence list C, auto-derived from `DEFECT_KINDS`; read it to confirm it needs NO edit.
  </read_first>

  <files>scripts/prod-prober/run.mjs</files>

  <behavior>
    - Absence list A names five kinds including `mt5-not-authorized`, and its message still reads as a claim about "any statement about the terminal".
    - Absence list B names four kinds including `mt5-not-authorized`, and its message's numeral reads "four", not "three".
    - The self-test still passes at 80/80 — neither list edit adds or removes a scenario.
  </behavior>

  <action>
    Two surgical edits, both inside existing `noDefectOfKind(...)` calls. Do not add a scenario,
    do not touch the counters.

    **List A (`run.mjs:3695-3697`).** Its claim is *"a transport failure is NOT reported as any
    statement about the terminal"*. `mt5-not-authorized` IS a statement about the terminal, so
    omitting it would silently SHRINK the claim to four-fifths of what the sentence says. Add the
    kind. Leave the message wording as it stands — it is a category claim, not a count.

    **List B (`run.mjs:3777-3779`).** Its claim is *"NONE of the three states that would send an
    operator to the gateway fired — the prober blamed itself, correctly"*. The new remedy DOES
    send the operator to the gateway (the VNC console), so the kind belongs in the list AND the
    prose numeral must move from three to four in the same edit. ⛔ A list that grows while its
    own sentence still says "three" is the exact class of defect this repo books as a control
    narrower than the sentence beside it.

    **List C (`run.mjs:1162-1164`).** Read it and change NOTHING: it is
    `DEFECT_KINDS.filter((k) => k !== expectedKind)`, so it widened by itself when plan 01
    registered the kind. Record that in the commit message rather than "fixing" it.

    ⛔ Keep using `noDefectOfKind` for both. An inline `kind === "<k>"` in an absence would be a
    vacuity inside the anti-vacuity harness, and the wiring test's source-text coverage extractor
    would miscredit it as by-name coverage.
  </action>

  <verify>
    <automated>ST=$(node scripts/prod-prober/run.mjs --self-test 2>&amp;1); printf '%s\n' "$ST" | tail -3 &gt;&amp;2; printf '%s\n' "$ST" | grep -q 'SELF-TEST PASSED: 80/80' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
    <fails_when>non-zero exit; or the self-test output lacks `SELF-TEST PASSED: 80/80` (a count change means a scenario was added or removed, which this task must not do); or the vitest summary does not read `Test Files  1 passed`.</fails_when>

    <automated>node -e 'const t=require("fs").readFileSync("scripts/prod-prober/run.mjs","utf8"); const code=t.split("\n").filter(l=&gt;!/^\s*(\/\/|\*|\/\*)/.test(l)).join("\n"); const lists=[...code.matchAll(/noDefectOfKind\(\s*r\.defects,\s*\[([^\]]*)\]/g)].map(m=&gt;m[1].match(/"[a-z0-9-]+"/g)||[]); const withNew=lists.filter(l=&gt;l.includes("\"mt5-not-authorized\"")); console.log("hand-kept mt5 absence lists:", lists.length, "| naming the new kind:", withNew.length); const three=/NONE of the three states that would send an operator to the gateway/.test(code); console.log("stale numeral present:", three); process.exit(withNew.length &gt;= 2 &amp;&amp; !three ? 0 : 1);'</automated>
    <fails_when>non-zero exit; or the printed `naming the new kind:` count is below 2, meaning one of the two hand-kept absence lists was left behind; or `stale numeral present: true`, meaning list B grew to four kinds while its own sentence still says "three". The comment-line filter is applied before counting so the file's own prose cannot satisfy the check.</fails_when>
  </verify>

  <acceptance_criteria>
    - Both hand-kept mt5 absence lists name `mt5-not-authorized` (C4).
    - List B's message reads "four", and the literal "NONE of the three states that would send an operator to the gateway" no longer appears in non-comment source.
    - List C is unchanged and the commit message records WHY (it is derived from `DEFECT_KINDS`).
    - The scenario count is unchanged at 80.
  </acceptance_criteria>

  <done>
    `node scripts/prod-prober/run.mjs --self-test` still prints `SELF-TEST PASSED: 80/80`; the
    wiring test file passes; the absence-list scan reports at least 2 lists naming the new kind
    and `stale numeral present: false` (C4).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prober → PUBLIC Actions log | every remedy this plan asserts on is printed world-readably. |
| prober → PUBLIC GitHub issue | the auto-issue step copies the same rows into a public issue. |
| repo → PUBLIC `.planning/` | this PLAN.md is world-readable on push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.3-01 | Information Disclosure | every `arms/mt5.mjs` `REMEDIES` value | high | mitigate | Task 1 (g) adds a calibrated scan asserting no mt5 remedy carries a six-or-more-digit run, and the verify prints the scanned count so a stale scan is visible. The calibration uses a synthetic repeated digit, never a real account number. |
| T-164.8.3-08 | Tampering | a future edit reintroducing the lookup-table instruction into the `-6` remedy | high | mitigate | Task 1 (c)+(d): a calibrated negative on the runtime ROW string. Deliberately NOT a source grep, because the surviving catch-all remedy legitimately contains that sentence and a file-scoped grep would be self-invalidating. |
| T-164.8.3-09 | Tampering | the two remedies converging into near-duplicates that pass the exact-string uniqueness gate | medium | mitigate | Task 1 (e) asserts non-overlap on each remedy's distinguishing wording, which `run.mjs:1111-1114` cannot see. |
| T-164.8.3-10 | Repudiation | an absence list that grew while its own printed sentence did not | medium | mitigate | Task 2's verify greps non-comment source for the stale numeral and exits 1 on a hit. |
| T-164.8.3-03 | Elevation of Privilege | `MT5_PROBE_PY` | high | mitigate | Untouched by this plan; the byte-for-byte and no-`${` gates run on every self-test invocation in `<verify>`. |
| T-164.8.3-06 | Tampering | npm/pip/cargo installs | n/a | accept | No packages are installed. RESEARCH.md's `## Package Legitimacy Audit` records NOT APPLICABLE; no `[ASSUMED]`/`[SUS]`/`[SLOP]` candidates exist, so no legitimacy checkpoint is inserted. |
</threat_model>

<verification>
- `node scripts/prod-prober/run.mjs --self-test` → `SELF-TEST PASSED: 80/80`, exit 0, with a header carrying `164.8.3 CRITERION 2`.
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts` → 1 file passed.
- `git diff --name-only` lists exactly `scripts/prod-prober/run.mjs` and `src/__tests__/prod-prober-wiring.test.ts`.
</verification>

<success_criteria>
- ROADMAP criteria 2 and 4 are enforced by calibrated machine gates read off the operator-visible row.
- Locked decisions D-03 and D-04 are both measured, not restated.
- No negative gate in this phase is written as a source-file grep for a literal that legitimately survives elsewhere in the same file.
</success_criteria>

<output>
Create `.planning/phases/164.8.3-proberauth-the-prod-prober-names-mt5-6-as-what-it-is-the-ter/164.8.3-02-SUMMARY.md` when done.
Record in it: the new scenario's exact printed header line, the two catch-all subjects as measured, and the `PUBLIC-LOG SCAN OK:` line verbatim.
</output>
