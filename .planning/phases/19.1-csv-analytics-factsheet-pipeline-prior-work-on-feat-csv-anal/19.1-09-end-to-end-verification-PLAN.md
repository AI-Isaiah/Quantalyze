---
phase: 19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal
plan: 09
type: execute
wave: 4
depends_on: [08]
files_modified: []
autonomous: false
requirements: []
threat_model_refs: []

must_haves:
  truths:
    - "A real CSV uploaded via the production wizard at https://quantalyze-rho.vercel.app/strategies/new produces a csv_daily_returns row in PRODUCTION"
    - "A compute_jobs row of kind='compute_analytics_from_csv' is enqueued, claimed by the Railway worker, and completes successfully"
    - "The resulting strategy_analytics row reaches computation_status='complete' with data_quality_flags.csv_source=True"
    - "The factsheet at /strategy/[id] renders CAGR / Sharpe / drawdown / sparkline derived from the uploaded data (NOT the analyticsMissingMessage stop-gap copy)"
  artifacts:
    - path: "PRODUCTION test strategy created via wizard"
      provides: "end-to-end pipeline proof"
      contains: "real csv_daily_returns + compute_jobs + strategy_analytics rows"
  key_links:
    - from: "production wizard CSV upload"
      to: "factsheet /strategy/[id] renders real metrics"
      via: "csv-finalize → persist → after() enqueue → Railway worker → compute_all_metrics → strategy_analytics complete"
      pattern: "wizard upload visible as metrics within ~5 minutes"
---

<objective>
Run a real end-to-end smoke test of the CSV → analytics → factsheet pipeline in PRODUCTION. This is the gate that proves the pipeline works before Plan 10 deletes the `analyticsMissingMessage` stop-gap.

Purpose: Per Rule 6 (root-cause obsession) and RESEARCH.md Pitfall 5: the stop-gap removal must be paired with the working pipeline, NOT executed on hope. Plan 10's deletion is gated on this plan PASSING.

Output: One production test strategy demonstrating the full pipeline. Verbatim DB row snapshots + factsheet screenshot captured in the execute-phase summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-CONTEXT.md
@.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-RESEARCH.md
@.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-08-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Prepare a real CSV file + log into production wizard</name>
  <read_first>
    - .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-08-SUMMARY.md (env flip outcome — confirm flag = true + redeployed)
  </read_first>
  
  <files></files>
<action>
    Plans 01-08 are live in production. This task asks the user to:
    1. Prepare a small valid CSV (daily_returns format, ~20-50 rows, dates ascending, daily_return values realistic).
    2. Open `https://quantalyze-rho.vercel.app/strategies/new` in a browser as a real authenticated user (NOT the wizard hydration bug path — use the link from /strategies/new per MEMORY project_wizard_hydration_bug.md).
    3. Be prepared to capture:
       - The wizard URL after submit (contains the new strategy id)
       - The factsheet URL `/strategy/<id>`
       - Screenshots of factsheet pre-completion ("Analytics are being computed. Check back soon.") and post-completion (CAGR / Sharpe / drawdown / sparkline rendered)
  </action>
  <verify>
    <automated>test -f .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-08-SUMMARY.md</automated>
  </verify>
  <prompt-text>
    Before starting:
    - Plan 08 SUMMARY confirms USE_COMPUTE_JOBS_QUEUE=true on Vercel + redeploy complete
    - Plan 07 SUMMARY confirms Railway worker live on main HEAD
    - CSV file is well-formed: valid daily_returns format, ≤5000 rows, no duplicate dates, dates YYYY-MM-DD, all daily_return values finite
    Precondition gate (B3 revision 2026-05-22): the `<automated>` block above mechanically asserts Plan 08 SUMMARY is on disk before this checkpoint can release.
  </prompt-text>
  <done>User authorization captured; subsequent tasks unblocked.</done>
  <resume-signal>Type "ready to upload CSV through prod wizard" to unblock Task 2.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Upload CSV through production wizard + observe immediate response</name>
  <files></files>
  <read_first></read_first>
  <action>
    The user uploads through the browser (the planner cannot drive Chrome from this environment without the claude-in-chrome MCP).

    1. User uploads the CSV. Wizard completes; user lands on the factsheet at `/strategy/<id>`.
    2. User notes the strategy_id (UUID in the URL). User reports it back so subsequent SQL probes can target the right row.

    Pre-worker state expectation (within ~5s of upload):
    - `csv_daily_returns` has rows for the new strategy_id
    - `strategy_analytics` row exists with `computation_status` likely 'pending' or 'computing'
    - `compute_jobs` has a `compute_analytics_from_csv` row in `queued` or `running` state
    - Factsheet shows the standard "Analytics are being computed. Check back soon." placeholder (the analyticsMissingMessage stop-gap may also still trigger for this specific case if the trust tier matches — capture both states observed)
  </action>
  <verify>
    <automated>echo "Manual upload — strategy_id captured for Task 3 SQL probes"</automated>
  </verify>
  <acceptance_criteria>
    - User reports the strategy_id of the uploaded test strategy
    - Wizard returned success (no 400/500 to the user)
  </acceptance_criteria>
  <done>Strategy created. Probes proceed in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Probe production DB to confirm full pipeline shipped</name>
  <files></files>
  <read_first>
    - Task 2 output (strategy_id)
  </read_first>
  <action>
    Use Supabase MCP `execute_sql` against `khslejtfbuezsmvmtsdn` (read-only — SELECT only).

    Probes (substitute <SID> with the strategy_id from Task 2):

    1. csv_daily_returns landed:
       ```sql
       SELECT COUNT(*) AS rows FROM csv_daily_returns WHERE strategy_id = '<SID>';
       ```
       Expect: `rows ≥ 2` (at least 2 to satisfy run_csv_strategy_analytics's "≥ 2 data points" gate)

    2. compute_jobs enqueue:
       ```sql
       SELECT id, status, claimed_at, completed_at, error
         FROM compute_jobs
        WHERE strategy_id = '<SID>'
          AND kind = 'compute_analytics_from_csv'
        ORDER BY created_at DESC LIMIT 1;
       ```
       Expect: one row, status = 'done' or 'running' (poll if running — re-run after 60s; should converge to 'done' within 5 min for a small CSV)

    3. strategy_analytics landed:
       ```sql
       SELECT computation_status, computation_error,
              data_quality_flags->>'csv_source' AS csv_source,
              data_quality_flags->>'benchmark_unavailable' AS benchmark_unavailable,
              cagr, sharpe_ratio, max_drawdown
         FROM strategy_analytics
        WHERE strategy_id = '<SID>';
       ```
       Expect: `computation_status = 'complete'`, `csv_source = 'true'`, `cagr/sharpe_ratio/max_drawdown` non-null

    4. RLS contract — anon SELECT denied on csv_daily_returns for this strategy_id:
       ```sql
       -- Run as service_role (read-only) but verify the policy shape:
       SELECT polname, polroles::regrole[], polqual
         FROM pg_policies WHERE tablename = 'csv_daily_returns';
       ```
       Expect: the owner-SELECT policy from Plan 01 present.

    Poll loop: if step 2 returns `running` or step 3 returns `computing`, wait 30s and re-poll. Max wait: 10 min (handler timeout = 10 min from Plan 02). If still not `complete` after 10 min: STOP — investigate via Railway logs + structlog correlation_id.
  </action>
  <verify>
    <automated>echo "Manual SQL probe outcomes captured in execute-phase summary; Task 4 verifies factsheet render"</automated>
  </verify>
  <acceptance_criteria>
    - csv_daily_returns has ≥ 2 rows for the new strategy
    - compute_jobs `compute_analytics_from_csv` row reached status='done'
    - strategy_analytics row at `computation_status='complete'` with `data_quality_flags.csv_source=true`
    - Real metric values (cagr, sharpe_ratio, max_drawdown) are non-null
    - Owner-SELECT RLS policy on csv_daily_returns confirmed in pg_policies
  </acceptance_criteria>
  <done>Full DB pipeline shipped end-to-end in production.</done>
</task>

<task type="auto">
  <name>Task 4: Verify factsheet renders real metrics (NOT the stop-gap copy)</name>
  <files></files>
  <read_first></read_first>
  <action>
    User reloads `https://quantalyze-rho.vercel.app/strategy/<SID>` in the browser.

    Expect:
    - Page renders CAGR / Sharpe / drawdown / sparkline (or equivalent factsheet panels for the strategy's data shape)
    - The analyticsMissingMessage stop-gap copy ("Platform-computed analytics are not generated for CSV-uploaded strategies in this release") does NOT appear — even though the helper still exists on disk, the `computation_status === "complete"` gate now succeeds so the panels render

    User captures a screenshot or annotated text capture of the rendered factsheet. Capture includes:
    - URL with strategy_id
    - Visible metric values (cagr, sharpe, drawdown, etc.)
    - Absence of the stop-gap copy text

    If the stop-gap copy still renders despite `computation_status='complete'`:
    - Pin the source: is the ternary in `page.tsx:186` triggering on a different condition?
    - This would indicate a separate trust-tier-driven path that the standard "complete" rendering doesn't override, in which case the stop-gap removal in Plan 10 becomes a 2-step change (remove the call site AND the helper). Note for Plan 10 execution.

    Capture all observations in the summary.
  </action>
  <verify>
    <automated>curl -s "https://quantalyze-rho.vercel.app/strategy/${TEST_STRATEGY_ID}" -H "Cookie: ${FACTSHEET_AUTH_COOKIE}" > /tmp/factsheet-19.1-verify.html && grep -c "Analytics are being computed\|Platform-computed analytics are not generated" /tmp/factsheet-19.1-verify.html | xargs -I{} test {} -eq 0 && grep -c "data-testid=\"factsheet-cagr-value\"\|class=\"[^\"]*cagr-card\|class=\"[^\"]*sharpe-card" /tmp/factsheet-19.1-verify.html | xargs -I{} test {} -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - Factsheet renders real metric values for the test strategy (not "—" or "0" placeholders)
    - Stop-gap copy ("Platform-computed analytics are not generated...") does NOT appear on the factsheet
    - Screenshot or text capture filed in summary
    - **W5 revision 2026-05-22 — automated curl+grep gate (in addition to manual visual):**
      - Operator captures the test strategy ID into `$TEST_STRATEGY_ID` and an auth cookie/public token into `$FACTSHEET_AUTH_COOKIE` during Task 3 setup (the cookie can be copied from the browser dev tools after the wizard login; if the strategy is published to a public discovery page, swap to a public URL pattern with no cookie).
      - `curl -s "https://quantalyze-rho.vercel.app/strategy/$TEST_STRATEGY_ID" -H "Cookie: $FACTSHEET_AUTH_COOKIE" > /tmp/factsheet-19.1-verify.html` succeeds
      - `grep -c "Analytics are being computed\|Platform-computed analytics are not generated" /tmp/factsheet-19.1-verify.html` = 0 (neither stop-gap nor "computing" placeholder visible)
      - `grep -c "data-testid=\"factsheet-cagr-value\"\|class=\"[^\"]*cagr-card\|class=\"[^\"]*sharpe-card" /tmp/factsheet-19.1-verify.html` ≥ 1 (at least one rendered metric panel detected via testid or class hook)
    - Plan 10 (stop-gap removal + branch cleanup) is now unblocked
  </acceptance_criteria>
  <done>End-to-end pipeline verified working in production. Stop-gap removal is now safe per Rule 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → production wizard → analytics-service → factsheet render | first real cross-environment test of the full pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-21 | DoS / availability | E2E times out (worker stuck or claim loop broken) | mitigate | Task 3 has a 10-min polling cap (matches handler timeout) before escalating. Plan 02 + Plan 07 both ran the watchdog headroom test — any failure here points to an env mismatch on Railway, not a logic bug. |
| T-19.1-22 | Tampering | Test strategy created in production accidentally goes public | mitigate | Default strategy status from finalize_csv_strategy is `pending_review` per RESEARCH.md Open Question 4. Anon visibility requires publication, so the test strategy is not exposed. Note in summary; user may want to delete the test strategy after Plan 10 closes. |
</threat_model>

<verification>
- 4 production DB probes return expected values
- Factsheet renders real metrics (no stop-gap copy visible)
- Pipeline E2E successful within 10 minutes
</verification>

<success_criteria>
- One end-to-end successful CSV upload → factsheet render in production
- All 4 DB probes pass
- Plan 10 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-09-SUMMARY.md` capturing:
- test strategy_id
- DB probe results (verbatim row contents)
- Factsheet screenshot/text capture
- Confirmation that Plan 10 is unblocked
</output>
