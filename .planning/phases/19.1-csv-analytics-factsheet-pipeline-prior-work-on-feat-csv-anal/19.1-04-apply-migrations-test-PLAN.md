---
phase: 19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified: []
autonomous: false
requirements: []
threat_model_refs: [T-19.1-01, T-19.1-02]

must_haves:
  truths:
    - "Migration 20260522120000 applied to qmnijlgmdhviwzwfyzlc; csv_daily_returns table + persist_csv_daily_returns RPC visible via list_tables / pg_proc"
    - "Migration 20260522120100 applied; compute_jobs_kind_target_coherence admits compute_analytics_from_csv"
    - "Self-verifying DO blocks raised no exception (apply_migration MCP returned success)"
    - "No regression: existing kinds (sync_trades, compute_analytics, process_key_long, etc.) still in coherence constraint"
  artifacts:
    - path: "live test Supabase project qmnijlgmdhviwzwfyzlc"
      provides: "csv_daily_returns + persist_csv_daily_returns + compute_analytics_from_csv kind"
      contains: "applied via apply_migration MCP"
  key_links:
    - from: "Supabase MCP apply_migration"
      to: "qmnijlgmdhviwzwfyzlc.public schema"
      via: "MCP project_id parameter"
      pattern: "project_id=qmnijlgmdhviwzwfyzlc"
---

<objective>
**[BLOCKING — autonomous: false]** Apply both Plan 01 migrations to the TEST Supabase project `qmnijlgmdhviwzwfyzlc` via the Supabase MCP `apply_migration` tool, then verify the schema landed via MCP `list_tables` / SQL inspection.

Purpose: Plan 05's live-DB tests cannot run against an empty schema, and Plan 06's prod apply cannot proceed without verified test-project state. This is the gate that flips the migrations from "source-on-disk" to "live-running on test".

Output: Two migrations applied to test project, MCP responses captured. No source-code changes. The phase branch (per /gsd-execute-phase + /ship workflow) advances by one task commit recording the apply outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-CONTEXT.md
@.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-RESEARCH.md
@.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-01-db-foundation-PLAN.md
@supabase/migrations/20260522120000_csv_daily_returns.sql
@supabase/migrations/20260522120100_compute_analytics_from_csv_kind.sql

<interfaces>
<!-- Supabase MCP tool: apply_migration -->
// mcp__supabase__apply_migration({ project_id, name, query })
// - project_id: "qmnijlgmdhviwzwfyzlc" (TEST per MEMORY reference_test_supabase_project.md)
// - name: should match the local filename minus ".sql" extension
// - query: full SQL body (read with Read tool from supabase/migrations/ files)
// Behavior: applies + records in supabase_migrations.schema_migrations.
// Will fail loudly if any RAISE EXCEPTION in self-verify DO block fires.

<!-- Fallback if MCP unavailable: -->
// supabase db push --linked --include-all (with SUPABASE_ACCESS_TOKEN env)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] User authorization to apply migrations to TEST Supabase</name>
  <read_first>
    - .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-CONTEXT.md (decisions block — Phase 4 Deploy, Task 3)
    - supabase/migrations/20260522120000_csv_daily_returns.sql (the SQL about to be applied — verify it exists and acceptance criteria from Plan 01 passed)
    - supabase/migrations/20260522120100_compute_analytics_from_csv_kind.sql (same)
  </read_first>
  
  <files></files>
<action>
    Plan 01 wrote two migration files. They are NOT applied to any live database yet. This task asks the user to authorize applying them to the TEST Supabase project `qmnijlgmdhviwzwfyzlc`.

    The migrations carry self-verifying DO blocks that will raise on any structural failure, so a successful apply is itself a strong signal that:
    - `csv_daily_returns` table created with 3 RLS policies
    - `persist_csv_daily_returns` RPC created as SECURITY DEFINER with GRANT TO authenticated
    - No redundant `csv_daily_returns_strategy_date_idx` index (PR #272 anti-regression)
    - `compute_jobs_kind_target_coherence` admits `compute_analytics_from_csv`
    - `process_key_long` branch still present (strict superset check)
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260522120000_csv_daily_returns.sql && test -f supabase/migrations/20260522120100_compute_analytics_from_csv_kind.sql && test -f .planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-01-SUMMARY.md</automated>
  </verify>
  <prompt-text>
    Before authorizing:
    1. Confirm you understand this mutates LIVE state on the TEST project `qmnijlgmdhviwzwfyzlc`.
    2. This is NOT the production project (production = `khslejtfbuezsmvmtsdn`). Production apply is Plan 06.
    3. The migrations are additive only (no DROP of existing tables/columns; only CHECK constraint swap which is a strict superset).
    4. Both files passed Plan 01 acceptance criteria (grep checks).
    Precondition gate (W1 revision 2026-05-22): the `<automated>` block above mechanically asserts both migration source files AND the Plan 01 SUMMARY are on disk before the checkpoint can release (W1: also gate on Plan 01 having actually executed, not just file presence).
  </prompt-text>
  <done>User authorization captured; subsequent tasks unblocked.</done>
  <resume-signal>Type "authorized to apply 19.1-01 migrations to qmnijlgmdhviwzwfyzlc" to unblock Task 2.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply both migrations via Supabase MCP apply_migration</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260522120000_csv_daily_returns.sql (read full body — pass as `query` arg to MCP)
    - supabase/migrations/20260522120100_compute_analytics_from_csv_kind.sql (same)
  </read_first>
  <action>
    Apply in order (timestamp ordering matters):

    1. Call Supabase MCP tool `apply_migration`:
       ```
       mcp__supabase__apply_migration({
         project_id: "qmnijlgmdhviwzwfyzlc",
         name: "20260522120000_csv_daily_returns",
         query: <full body of supabase/migrations/20260522120000_csv_daily_returns.sql>
       })
       ```
       Expected: success (the self-verifying DO block returns `NOTICE: ...all assertions passed.` and apply records in schema_migrations).
       On error: STOP. Capture the error message verbatim. If a RAISE EXCEPTION from the self-verify block fires, Plan 01 has a defect and must be fixed; do NOT retry until Plan 01 is corrected.

    2. Call Supabase MCP tool `apply_migration` for the second file:
       ```
       mcp__supabase__apply_migration({
         project_id: "qmnijlgmdhviwzwfyzlc",
         name: "20260522120100_compute_analytics_from_csv_kind",
         query: <full body of supabase/migrations/20260522120100_compute_analytics_from_csv_kind.sql>
       })
       ```
       Expected: success.

    3. Fallback if MCP `apply_migration` is unavailable: prompt the user to run `supabase db push --linked` locally with `SUPABASE_ACCESS_TOKEN` env set. The Supabase project linkage must point at `qmnijlgmdhviwzwfyzlc`. Verify with `supabase status` before pushing.

    No source changes — this task only mutates live state on the test Supabase project.
  </action>
  <verify>
    <automated>echo "Manual verify next task — MCP apply_migration success codes recorded in execute-phase summary"</automated>
  </verify>
  <acceptance_criteria>
    - Both MCP `apply_migration` calls returned without error
    - Apply order respected: 20260522120000 BEFORE 20260522120100
    - No RAISE EXCEPTION fired from either self-verifying DO block
    - The execute-phase summary captures the MCP response for each apply (string transcripts)
  </acceptance_criteria>
  <done>Both migrations applied to test Supabase. Self-verify blocks passed.</done>
</task>

<task type="auto">
  <name>Task 3: Verify applied schema via list_tables + SQL probes</name>
  <files></files>
  <read_first>
    - Supabase MCP tooling: `list_tables`, `execute_sql` (read-only)
  </read_first>
  <action>
    Verify the apply landed correctly using read-only MCP tools:

    1. Call `mcp__supabase__list_tables({project_id: "qmnijlgmdhviwzwfyzlc", schemas: ["public"]})`. Expect `csv_daily_returns` in the returned list.

    2. Call `mcp__supabase__execute_sql({project_id: "qmnijlgmdhviwzwfyzlc", query: "SELECT proname, prosecdef FROM pg_proc WHERE proname = 'persist_csv_daily_returns';"})`. Expect 1 row with `prosecdef = true` (SECURITY DEFINER).

    3. Probe the GRANT shape:
       ```sql
       SELECT grantee FROM information_schema.routine_privileges
        WHERE routine_name = 'persist_csv_daily_returns'
          AND privilege_type = 'EXECUTE'
        ORDER BY grantee;
       ```
       Expect at least `authenticated` and `service_role` in the result (T-19.1-02 confirmation).

    4. Probe the absence of the redundant index (PR #272 anti-regression):
       ```sql
       SELECT indexname FROM pg_indexes
        WHERE schemaname = 'public'
          AND tablename = 'csv_daily_returns';
       ```
       Expect only the PK index (`csv_daily_returns_pkey`); explicit `csv_daily_returns_strategy_date_idx` MUST NOT appear.

    5. Probe the new compute_jobs CHECK constraint:
       ```sql
       SELECT check_clause FROM information_schema.check_constraints
        WHERE constraint_name = 'compute_jobs_kind_target_coherence';
       ```
       Expect the returned clause to contain both `compute_analytics_from_csv` AND `process_key_long` (strict superset).

    6. Probe the registry:
       ```sql
       SELECT name FROM compute_job_kinds WHERE name = 'compute_analytics_from_csv';
       ```
       Expect 1 row.

    Capture all probe responses in the execute-phase summary verbatim. These six results form the **TEST-project schema parity baseline** that Plan 06 Task 4 (W9 cross-project schema parity) diffs against the prod outcomes; record them in a stable, copy-paste-friendly shape (one probe per heading, raw row payload underneath).
  </action>
  <verify>
    <automated>echo "Probe results captured in execute-phase summary; manual review by user before unblocking Plan 05"</automated>
  </verify>
  <acceptance_criteria>
    - `list_tables` returns `csv_daily_returns` in the public schema
    - `pg_proc` row for `persist_csv_daily_returns` shows `prosecdef = true`
    - `routine_privileges` shows EXECUTE granted to `authenticated` AND `service_role`
    - `pg_indexes` for `csv_daily_returns` shows ONLY the PK index, NOT `csv_daily_returns_strategy_date_idx` (PR #272 confirmation)
    - `check_constraints.check_clause` for `compute_jobs_kind_target_coherence` contains both `compute_analytics_from_csv` and `process_key_long`
    - `compute_job_kinds` table contains `compute_analytics_from_csv`
    - All six probe results recorded verbatim in SUMMARY so Plan 06 W9 schema-parity diff can consume them
  </acceptance_criteria>
  <done>Test Supabase project schema verified consistent with Plan 01 source. Plan 05 (live-DB tests) is now unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner machine → live test DB | first time the new RPC + table cross into live state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-01 | Information Disclosure | persist_csv_daily_returns probe-oracle on live test project | mitigate | Verified in Plan 01 source; confirmed live via Plan 05 live-DB tests (gated on this plan completing). This plan only applies — the threat is closed by the SQL body inside the migration. |
| T-19.1-02 | Elevation of Privilege | GRANT shape misconfiguration | mitigate | Task 3 SQL probe asserts `authenticated` + `service_role` grants present and `anon` is NOT a grantee. |
| T-19.1-13 | Tampering | Migration apply order swapped (kind constraint before table exists) | mitigate | Apply order explicit in Task 2; timestamp `20260522120000` (table+RPC) sorts before `20260522120100` (kind). Compute_jobs CHECK constraint does not reference csv_daily_returns table, so ordering is informational not load-bearing. |
</threat_model>

<verification>
- All three MCP probes (list_tables, pg_proc, routine_privileges) return expected results
- check_constraints probe confirms strict-superset behavior (process_key_long preserved)
- No RAISE EXCEPTION fired during apply
</verification>

<success_criteria>
- Both migrations live on `qmnijlgmdhviwzwfyzlc`
- Plan 05 (live-DB tests) can now connect and exercise the new RPC
- Schema state verifiable via SQL probes recorded in summary
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-csv-analytics-factsheet-pipeline-prior-work-on-feat-csv-anal/19.1-04-SUMMARY.md` capturing:
- MCP apply_migration responses (success codes + any NOTICE strings from self-verify DO blocks)
- SQL probe results from Task 3 (tabular, all six probes ticked + raw payloads preserved for W9 schema-parity diff)
- Confirmation that Plan 05 is unblocked
</output>
