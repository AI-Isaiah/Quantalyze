---
phase: 135-mt5src-source-lockstep
plan: 03
type: execute
wave: 2
depends_on: [135-01]
files_modified:
  - analytics-service/routers/exchange.py
  - analytics-service/tests/test_mt5_validate.py
autonomous: true
requirements: [MT5SRC-02]

must_haves:
  truths:
    - "POST /validate-key with exchange=mt5 is intercepted BEFORE the ccxt path; the ccxt branch stays byte-identical"
    - "MT5_ENABLED unset/false → clean 400 MT5_DISABLED_DETAIL, NO client constructed, NO live probe (go-dark, fail-closed)"
    - "Investor login → {valid:true, read_only:true}; master (trade-capable) login → 400 with the EXACT MT5_MASTER_PASSWORD_DETAIL and is never persisted"
    - "Bad creds → EXACT AUTH_FAILED_DETAIL (KEY_AUTH_FAILED byte-identity); wrong server → EXACT MT5_WRONG_SERVER_DETAIL; three distinguishable failure paths"
    - "order_send is never called anywhere in the branch; the probe is order_check only, time-bounded off the event loop"
  artifacts:
    - path: "analytics-service/routers/exchange.py"
      provides: "_validate_mt5_key + is_mt5 intercept + LOUD credential-slot comment at the encrypt chokepoint"
      contains: "_validate_mt5_key"
    - path: "analytics-service/tests/test_mt5_validate.py"
      provides: "branch unit suite cloning test_sfox_validate.py posture (offline, injected transport double)"
      min_lines: 100
  key_links:
    - from: "analytics-service/routers/exchange.py"
      to: "analytics-service/services/closed_sets.py"
      via: "mt5_enabled_server gate + the three MT5_*_DETAIL constants (plan 135-01 contract)"
      pattern: "mt5_enabled_server"
    - from: "analytics-service/routers/exchange.py"
      to: "analytics-service/services/mt5_validation.py"
      via: "is_trade_capable + classify_mt5_login_error + mt5_probe_request (the ONE [ASSUMED] seam)"
      pattern: "from services.mt5_validation import"
---

<objective>
Add the `is_mt5` read-only validate branch to the FastAPI router — cloning the `is_sfox` intercept
and `_validate_sfox_key` fail-CLOSED template — wiring the Phase-134 `Mt5Client` (login +
account_info + order_check probe, NEVER order_send), behind the `MT5_ENABLED` go-dark gate, with
the credential-slot mapping commented LOUDLY at the one encrypt chokepoint.

Purpose: MT5SRC-02 — the worker half. Correction baked in: the branch lives in
routers/exchange.py (NOT services/exchange.py, which is the ccxt-only engine mt5 never routes
through).
Output: router branch + offline unit suite pinning all three distinguishable failure paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-mt5src-source-lockstep/135-CONTEXT.md
@.planning/phases/135-mt5src-source-lockstep/135-RESEARCH.md
@analytics-service/routers/exchange.py
@analytics-service/tests/test_sfox_validate.py

<interfaces>
Consumed contracts (defined by plan 135-01 — import, do not redefine):

From services/closed_sets.py:
- MT5_DISABLED_DETAIL / MT5_MASTER_PASSWORD_DETAIL / MT5_WRONG_SERVER_DETAIL (exact strings — the
  TS classifyKeyValidationError matcher in plan 135-04 pins "master password" and "broker server"
  substrings; never reword here)
- mt5_enabled_server() -> bool

From services/mt5_validation.py:
- mt5_probe_request(symbol) / is_trade_capable(info, check) / classify_mt5_login_error(err) -> "auth" | "wrong_server" | "transient"

From services/exchange.py (existing shared cross-language strings):
- AUTH_FAILED_DETAIL (:26), RATE_LIMITED_DETAIL (:35), NETWORK_ERROR_DETAIL (:40)

From services/mt5_client.py (Phase 134): Mt5Client(host, port, *, _connect=None) sync facade —
login/account_info/order_check/close, Mt5ClientError(code, detail) secret-scrubbed.

Credential-slot contract (LOUD comment target): login → api_key, investor password → api_secret,
broker server → passphrase. No new columns. ValidateKeyRequest already carries all three
(schemas.py:9-13, exchange is a bare str so "mt5" needs no schema edit); EncryptKeyRequest
likewise (routers/exchange.py:21-25).

Env contract (Phase 139 sets these live; unset now = server misconfig 503 path):
MT5_GATEWAY_HOST, MT5_GATEWAY_PORT (int). Probe symbol default "EURUSD" (mirrors the 134 spike).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _validate_mt5_key + gated is_mt5 intercept + encrypt-chokepoint slot comment</name>
  <files>analytics-service/routers/exchange.py</files>
  <read_first>
    - analytics-service/routers/exchange.py:28-116 (_validate_sfox_key — the fail-CLOSED template: up-front guard, construction-time 503, error-arm mapping, finally-close, no-secret logging)
    - analytics-service/routers/exchange.py:119-196 (validate_key sfox intercept :139-149 incl. the F2 gate comment; ccxt branch :151-183 that must stay byte-identical; encrypt_key :186-196)
    - analytics-service/services/mt5_validation.py + services/closed_sets.py mt5 block (plan 135-01 output)
    - analytics-service/services/mt5_client.py:200-271 (login/account_info/order_check/close semantics)
  </read_first>
  <action>
    Add async _validate_mt5_key(api_key: str, api_secret: str, passphrase: str | None) below
    _validate_sfox_key, documented as its clone with the MT5 divergences (per D: resolved Q-B/Q-C;
    the docstring explains the three distinguishable failure paths and the slot mapping). Behavior,
    every arm fail-CLOSED (never returns valid:true on any failure):
    - Broker server REQUIRED: passphrase missing/blank -> HTTPException(400, MT5_WRONG_SERVER_DETAIL)
      before any client construction (the server-distinct path also covers the required-field case;
      distinguishable from bad-password by copy).
    - Login coercion: int(api_key.strip()); non-numeric/empty -> HTTPException(400,
      AUTH_FAILED_DETAIL) — a non-numeric MT5 login cannot authenticate (mirror the sfox IN-01
      up-front guard rationale). Empty/blank api_secret (investor password) -> same 400
      AUTH_FAILED_DETAIL.
    - Gateway config: read MT5_GATEWAY_HOST / MT5_GATEWAY_PORT; missing/malformed -> log a
      secret-free server-misconfig error and HTTPException(503, NETWORK_ERROR_DETAIL) — mirrors the
      sfox construction-time 503 posture (a misconfig is NEVER the user's key).
    - Probe: run the synchronous body (Mt5Client(host, port); login; account_info; order_check with
      mt5_probe_request) inside asyncio.wait_for(asyncio.to_thread(...), timeout=~MT5 request
      timeout + margin) — sync RPyC on the event loop reopens WEDGE-01; a wait_for TimeoutError ->
      400 NETWORK_ERROR_DETAIL at WARNING level (transient upstream, mirrors the sfox transient
      arms; the terminal-restart recovery machinery is Phase 137 — do not build it here).
    - Master rejection (the EoP gate): if is_trade_capable(info, check) -> HTTPException(400,
      MT5_MASTER_PASSWORD_DETAIL). A master login is never persisted because the TS caller only
      proceeds to /encrypt-key after {valid:true}.
    - Mt5ClientError -> classify_mt5_login_error: "auth" -> 400 AUTH_FAILED_DETAIL; "wrong_server"
      -> 400 MT5_WRONG_SERVER_DETAIL; "transient" -> 400 NETWORK_ERROR_DETAIL (WARNING log with
      the scrubbed code only — never the interpolated remote text, never login/pw/server values).
    - Success -> {"valid": True, "read_only": True} with the structural read-only comment (Mt5Client
      composes only read methods + the order_check probe; no order surface, no __getattr__
      passthrough — read-only by construction, PLUS the behavioral investor-vs-master probe sfox
      lacks).
    - client.close() in a finally covering every path after construction (idempotent).

    In validate_key: insert the mt5 intercept directly after the sfox intercept block (:139-149),
    cloning its shape and F2 gate comment: if req.exchange == "mt5": if not mt5_enabled_server():
    raise HTTPException(400, MT5_DISABLED_DETAIL) — BEFORE any client construction or live probe
    (go-dark, resolved Q-C; mirrors SFOX_ENABLED); else return await _validate_mt5_key(req.api_key,
    req.api_secret, req.passphrase). The ccxt branch below stays byte-identical.

    In encrypt_key (:186-196): add the LOUD credential-slot comment block at the
    encrypt_credentials call — THE one chokepoint (CONTEXT locked): for exchange='mt5' the three
    fields reuse the existing encrypted slots as login -> api_key, investor password -> api_secret,
    broker server -> passphrase; no new columns; the Next.js caller populates the slots in this
    order and the worker is_mt5 branch reads them back identically. No logic change to encrypt_key.

    Comment hygiene: write "order_send" in prose only, never with a call form — the grep gate
    scans for the call token "order_send(".
  </action>
  <acceptance_criteria>
    - `cd analytics-service && ! grep -rn "order_send(" routers/exchange.py` exits 0
    - `grep -c "mt5_enabled_server" analytics-service/routers/exchange.py` >= 1 and the gate check precedes _validate_mt5_key invocation in the intercept
    - `grep -n "investor password -> api_secret\|investor password → api_secret" analytics-service/routers/exchange.py` hits inside encrypt_key's comment block
    - ccxt branch diff-clean: git diff shows no changes between the mt5 intercept insertion point and the existing create_exchange/validate_key_permissions lines
  </acceptance_criteria>
  <verify>
    <automated>cd analytics-service && pytest tests/test_sfox_validate.py tests/test_exchange_router_c0202.py -x -q</automated>
  </verify>
  <done>Branch intercepts mt5 pre-ccxt behind the gate; three distinguishable failure paths emit the exact contract strings; slot mapping documented at the single chokepoint; existing sfox/ccxt router suites untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_mt5_validate.py — offline branch suite (clone test_sfox_validate.py posture)</name>
  <files>analytics-service/tests/test_mt5_validate.py</files>
  <read_first>
    - analytics-service/tests/test_sfox_validate.py (whole file — the regression-gate rationale docstring + fixture/mocking pattern to clone)
    - analytics-service/tests/test_mt5_client*.py (the 134 injected _connect transport-double pattern for driving Mt5Client offline)
  </read_first>
  <behavior>
    - Gate OFF (MT5_ENABLED unset): POST /validate-key exchange=mt5 -> 400 with MT5_DISABLED_DETAIL exactly; Mt5Client never constructed (assert the constructor/factory not called) — never a live probe when disabled
    - Gate ON + investor creds (double: login ok, trade_allowed False, order_check non-trade-capable) -> {"valid": True, "read_only": True}
    - Gate ON + master creds (double: trade_allowed True) -> 400, detail == MT5_MASTER_PASSWORD_DETAIL byte-exact; also the order_check-positive/trade_allowed-False combination rejects (defensive either-signal rule)
    - Bad creds (login raises Mt5ClientError classified auth) -> 400, detail == AUTH_FAILED_DETAIL byte-exact (the cross-language KEY_AUTH_FAILED pin)
    - Wrong server (Mt5ClientError classified wrong_server) -> 400, detail == MT5_WRONG_SERVER_DETAIL byte-exact — distinguishable from the bad-password detail
    - Missing/blank passphrase (server) -> 400 MT5_WRONG_SERVER_DETAIL without constructing a client
    - Non-numeric login / blank investor password -> 400 AUTH_FAILED_DETAIL without constructing a client
    - Transient (Mt5ClientError classified transient, and a wait_for timeout) -> 400 NETWORK_ERROR_DETAIL; response never contains "authentication failed"; never {"valid": true}
    - close() called on EVERY path after construction (success, master-reject, auth, wrong-server, transient)
    - ccxt path untouched: exchange="binance" still flows create_exchange -> validate_key_permissions (branch-placement pin, cloned from the sfox suite)
    - Missing MT5_GATEWAY_HOST/PORT -> 503 NETWORK_ERROR_DETAIL, log line secret-free
  </behavior>
  <action>
    Create tests/test_mt5_validate.py cloning test_sfox_validate.py's structure: a Rule-9 docstring
    explaining WHY each gate matters (master-persist EoP, byte-identity cross-language contract,
    fail-closed honesty, close-on-every-path, ccxt placement pin, go-dark gate), FastAPI
    TestClient/direct-call pattern matching the sfox suite, monkeypatched MT5_ENABLED +
    MT5_GATEWAY_HOST/PORT env, and the Mt5Client injected _connect transport double (or patch
    Mt5Client at the routers.exchange import site — conform to how the sfox suite patches
    make_sfox_client, Rule 11). Pin ALL detail strings as EXACT literals imported from
    closed_sets/services.exchange and compared to response detail — a reworded string must red the
    suite (the wizardErrors substring contract depends on it). Include one grep-gate test asserting
    "order_send(" does not appear in routers/exchange.py source (read the file, assert the call
    token absent) so the invariant survives future edits.
  </action>
  <acceptance_criteria>
    - `cd analytics-service && pytest tests/test_mt5_validate.py -x -q` exits 0 with >= 12 tests
    - Detail-string assertions import the constants (no retyped string literals drifting from the contract)
    - `cd analytics-service && pytest -q` — no new failures vs pre-plan baseline (pandera-missing local reds excluded)
  </acceptance_criteria>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_validate.py tests/test_sfox_validate.py -x -q</automated>
  </verify>
  <done>All three failure paths + gate + master-reject pinned offline against the 134 contract double; suite fails if any detail string, gate, or branch placement drifts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → POST /validate-key | untrusted login/investor-pw/server (three fields) |
| worker → RPyC gateway | live probe against the MT5 terminal bridge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-09 | Elevation of Privilege | master password accepted as read-only | mitigate | is_trade_capable either-signal rejection BEFORE any valid:true; TS only encrypts after valid — a master login is never persisted |
| T-135-10 | Spoofing | bad creds pass validation | mitigate | fail-CLOSED every arm; unknown/transient errors are 400 NETWORK, never valid |
| T-135-11 | Information Disclosure | creds/remote text in logs/Sentry | mitigate | Mt5ClientError construction-time scrubbing; WARNING logs carry code only; never log api_key/api_secret/passphrase values |
| T-135-12 | Denial of Service | hung RPyC pipe wedges the event loop | mitigate | asyncio.to_thread + asyncio.wait_for bound (WEDGE-01 lesson); timeout -> clean 400, healthz stays live |
| T-135-13 | Elevation of Privilege | live probe fires pre-go-live | mitigate | MT5_ENABLED gate rejects BEFORE client construction (fail-closed default OFF) |
| T-135-SC | Tampering | package installs | accept | N/A — no installs this phase |
</threat_model>

<verification>
- pytest tests/test_mt5_validate.py green; sfox + ccxt router suites unchanged-green
- ! grep -rn "order_send(" analytics-service/routers/exchange.py
- Full analytics-service pytest: no new failures
</verification>

<success_criteria>
- MT5SRC-02 worker half done: gated is_mt5 branch proves auth+read, rejects master with targeted copy, honest KEY_AUTH_FAILED on bad creds, wrong-server distinguishable, slot mapping documented at the encrypt chokepoint, order_send never called
</success_criteria>

<output>
Create `.planning/phases/135-mt5src-source-lockstep/135-03-SUMMARY.md` when done
</output>
