---
phase: 135-mt5src-source-lockstep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/ingestion/adapter.py
  - analytics-service/services/ingestion/__init__.py
  - analytics-service/services/ingestion/mt5.py
  - analytics-service/services/mt5_validation.py
  - analytics-service/services/closed_sets.py
  - analytics-service/models/schemas.py
  - analytics-service/routers/debug_key_flow.py
  - analytics-service/tests/test_ingestion_mt5.py
  - analytics-service/tests/test_ingestion_protocol.py
  - analytics-service/tests/test_boundary_literals_parity.py
autonomous: true
requirements: [MT5SRC-01, MT5SRC-02]

must_haves:
  truths:
    - "get_adapter('mt5') resolves a cached Mt5Adapter; Source Literal, SUPPORTED_SOURCES and _FACTORIES agree (lockstep)"
    - "Mt5Adapter.compute_metrics and fetch_raw RAISE NotImplementedError (fail-loud until Phase 136)"
    - "Mt5Adapter.validate accepts an investor login, REJECTS a trade-capable (master) login, and fails CLOSED on bad creds — never calls order_send"
    - "The three MT5 error detail strings + mt5_enabled_server() exist as the shared cross-plan contract"
  artifacts:
    - path: "analytics-service/services/ingestion/mt5.py"
      provides: "Mt5Adapter (validate implemented; compute_metrics/fetch_raw raise)"
      min_lines: 80
    - path: "analytics-service/services/mt5_validation.py"
      provides: "ONE shared seam: probe request builder + is_trade_capable + classify_mt5_login_error"
    - path: "analytics-service/tests/test_ingestion_mt5.py"
      provides: "lockstep + fail-loud + validate unit suite (clone of test_ingestion_sfox.py posture)"
  key_links:
    - from: "analytics-service/services/ingestion/__init__.py"
      to: "analytics-service/services/ingestion/mt5.py"
      via: "_FACTORIES['mt5'] -> _make_mt5_adapter lazy import"
      pattern: "\"mt5\": _make_mt5_adapter"
    - from: "analytics-service/services/ingestion/mt5.py"
      to: "analytics-service/services/mt5_validation.py"
      via: "validate() delegates master-detection + error classification to the shared seam"
      pattern: "from services.mt5_validation import"
---

<objective>
Register `'mt5'` as a first-class `Source` in lockstep across the three registry points (mirroring
`'sfox'` exactly), land the `Mt5Adapter` clone of `SfoxAdapter` (validate IMPLEMENTED per resolved
Q-A; compute_metrics/fetch_raw fail-loud RAISE until Phase 136), widen the two pydantic boundary
Literals, and define the shared MT5 error-detail/gate constants that plans 03 and 04 consume.

Purpose: MT5SRC-01 in full + the adapter/constants half of MT5SRC-02. Interface-first: this plan
defines the cross-plan contracts (detail strings, gate helper, classification seam).
Output: 5 edited Python files + 3 new modules/tests, all pytest parity suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-mt5src-source-lockstep/135-CONTEXT.md
@.planning/phases/135-mt5src-source-lockstep/135-RESEARCH.md
@analytics-service/services/ingestion/sfox.py
@analytics-service/services/mt5_client.py

<interfaces>
Cross-plan string contract (plans 03/04 pin these EXACT literals — do not reword):

From analytics-service/services/closed_sets.py (created by this plan):
- MT5_DISABLED_DETAIL = "MT5 integration is not yet available."
- MT5_MASTER_PASSWORD_DETAIL = "MT5 master password detected — this login can place trades. Reconnect using your read-only investor password."
- MT5_WRONG_SERVER_DETAIL = "Broker server not found — check the exact server name shown in your MT5 terminal login window."
- def mt5_enabled_server() -> bool   # (os.getenv("MT5_ENABLED") or "").strip().lower() == "true"

String-safety invariant (Q-B): these strings were collision-checked against every substring branch
in src/lib/wizardErrors.ts classifyKeyValidationError ("signature", "invalid secret",
"authentication failed", "invalid_credentials", "ip"+"allow", "rate", "429", "timeout",
"could not verify", "permission scope", "probe", "trading", "withdraw"). Any rewording must re-run
that collision check — the TS matcher is substring-based.

From analytics-service/services/mt5_validation.py (created by this plan, consumed by plan 03):
- def mt5_probe_request(symbol: str) -> dict          # order_check probe shape (PROBE ONLY)
- def is_trade_capable(account_info: dict, order_check_result: dict) -> bool   # [ASSUMED] rule, ONE seam
- def classify_mt5_login_error(err: Mt5ClientError) -> Literal["auth", "wrong_server", "transient"]

Phase-134 client surface (services/mt5_client.py — do NOT modify):
- Mt5Client(host, port, *, _connect=None) — sync facade; _connect is the offline test injection seam
- client.login(login: int, password: str, server: str) -> None   # falsy -> Mt5ClientError via _raise_last
- client.account_info() -> dict   # includes "trade_allowed"
- client.order_check(request: dict) -> dict   # PROBE ONLY; facade exposes NO order_send
- client.close() -> None          # idempotent; call in finally
- Mt5ClientError(code, detail)    # secret-scrubbed at construction
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registry lockstep + Mt5Adapter + shared MT5 constants/classification seam</name>
  <files>analytics-service/services/ingestion/adapter.py, analytics-service/services/ingestion/__init__.py, analytics-service/services/ingestion/mt5.py, analytics-service/services/mt5_validation.py, analytics-service/services/closed_sets.py</files>
  <read_first>
    - analytics-service/services/ingestion/sfox.py (whole file — the SfoxAdapter template to clone)
    - analytics-service/services/ingestion/adapter.py:32-50 (Source Literal + sfox lockstep comment)
    - analytics-service/services/ingestion/__init__.py:100-180 (SUPPORTED_SOURCES :107, factory fns ~:161, _FACTORIES :166, get_adapter/_instantiate)
    - analytics-service/services/closed_sets.py (sfox_enabled_server block + SFOX_DISABLED_DETAIL — the gate template)
    - analytics-service/services/mt5_client.py:131-271 (ctor, login, account_info, order_check, close)
    - analytics-service/scripts/mt5_spike.py:83-130 (_probe_request + _TRADE_ACTION_DEAL/_ORDER_TYPE_BUY/_ORDER_FILLING_IOC constants — the probe shape to mirror)
    - .planning/phases/135-mt5src-source-lockstep/135-RESEARCH.md Q1 + Q2 sections
  </read_first>
  <behavior>
    - Test: "mt5" in get_args(Source) AND "mt5" in SUPPORTED_SOURCES AND get_adapter("mt5") returns an Mt5Adapter, cached (second call is the same instance)
    - Test: Mt5Adapter().compute_metrics([]) and asyncio.run(Mt5Adapter().fetch_raw({})) raise NotImplementedError
    - Test: validate with investor creds (double: order_check rejects trade / trade_allowed False) -> ValidationResult(valid=True, read_only=True)
    - Test: validate with master creds (double: trade_allowed True or order_check retcode signals trade-capable) -> valid=False, error_code="MT5_MASTER_PASSWORD", human_message == MT5_MASTER_PASSWORD_DETAIL exactly
    - Test: validate with bad creds (login raises Mt5ClientError classified auth) -> valid=False, error_code="AUTH_FAILED", human_message == AUTH_FAILED_DETAIL exactly (byte-identity with services/exchange.py:26)
    - Test: wrong-server Mt5ClientError -> valid=False, error_code="MT5_WRONG_SERVER", human_message == MT5_WRONG_SERVER_DETAIL exactly
    - Test: client.close() called on EVERY path (success, master-reject, auth-fail, propagating transient)
    - Test: mt5_enabled_server() truth table — unset/""/"1"/"TRUE "/"on" variants per the sfox docstring convention (.strip().lower() == "true")
  </behavior>
  <action>
    In services/closed_sets.py: add the three MT5 detail constants and mt5_enabled_server(), using
    the EXACT strings from the interfaces block, cloning the sfox_enabled_server block verbatim
    (same .strip().lower() normalization, same fail-closed rationale comment, referencing Phase 135
    and the Q-C go-dark decision). Note in the comment that the wizardErrors.ts substring-collision
    invariant pins these strings (per D: Q-B resolved decision).

    In services/ingestion/adapter.py:45: append "mt5" to the Source Literal with a lockstep comment
    block mirroring the sfox note at :32-44 (all three registry points widen TOGETHER — the SFOX-01
    pin precedent; the factory lands in this same change so no split).

    In services/ingestion/__init__.py: append "mt5" to SUPPORTED_SOURCES (:107); add
    _make_mt5_adapter() near :161 (lazy "from .mt5 import Mt5Adapter" mirroring _make_sfox_adapter);
    register "mt5": _make_mt5_adapter in _FACTORIES (:166). get_adapter/_instantiate need zero edits
    (registry-driven dispatch).

    Create services/mt5_validation.py — the ONE seam holding every [ASSUMED] investor-vs-master rule
    so the Phase-134 live-spike refinement is a one-line follow-up (CONTEXT locked): expose
    mt5_probe_request(symbol) building the minimal market-order-shaped order_check request mirroring
    scripts/mt5_spike.py:124 _probe_request (cross-reference it in a comment; default symbol
    "EURUSD"); is_trade_capable(account_info, order_check_result) combining
    account_info["trade_allowed"] with the order_check retcode/comment DEFENSIVELY (Pitfall 4 —
    either positive trade-capable signal rejects; mark the retcode rule [ASSUMED] pending
    MT5SPIKE-01 leg 2); classify_mt5_login_error(err) mapping Mt5ClientError code/text to
    "auth" | "wrong_server" | "transient" via a single rule table (auth-ish text tokens like
    "authorization"/"account"/"invalid" -> auth; server/connection tokens like "server"/"connect"/
    "ipc"/"network"/"terminal" -> wrong_server; anything unrecognized -> transient, fail-closed and
    never blaming creds). Mark the token table [ASSUMED] pending the live spike. NEVER reference
    order_send — write comments as "order_send" only without a call form; the grep gate below scans
    for the call token "order_send(".

    Create services/ingestion/mt5.py — Mt5Adapter cloning SfoxAdapter verbatim in structure, with
    SOURCE = "mt5" and a module docstring adapted to MT5 (Phase 135 / MT5SRC-01; returns are
    deal-ledger equity-reconstructed in Phase 136, NEVER fill-derived — the BYB-02 corruption
    class). validate(req) IS implemented (resolved Q-A): parse login from
    req.context["api_key"] (int coercion after strip; non-numeric -> fail CLOSED with
    error_code="AUTH_FAILED" + AUTH_FAILED_DETAIL — a non-numeric login cannot authenticate),
    investor password from "api_secret", broker server from "passphrase" (missing/blank server ->
    error_code="MT5_WRONG_SERVER" + MT5_WRONG_SERVER_DETAIL — server is REQUIRED). Construct
    Mt5Client from MT5_GATEWAY_HOST / MT5_GATEWAY_PORT env (missing -> raise RuntimeError: a SERVER
    misconfig, propagated — never valid, never blames creds; mirrors sfox's construction-time
    posture). Run the synchronous probe body (login -> account_info -> order_check with
    mt5_probe_request) via asyncio.to_thread — Mt5Client is sync RPyC and blocking the event loop
    reopens the WEDGE-01 class. Master-capable per is_trade_capable -> valid=False,
    error_code="MT5_MASTER_PASSWORD", human_message=MT5_MASTER_PASSWORD_DETAIL (NEVER persisted —
    the caller only encrypts after a valid=True). Investor -> valid=True, read_only=True
    (read-only asserted STRUCTURALLY: Mt5Client exposes no order surface — mirror the sfox A1
    comment). Mt5ClientError -> classify_mt5_login_error: auth -> AUTH_FAILED result; wrong_server
    -> MT5_WRONG_SERVER result; transient -> PROPAGATE untouched (sfox posture: never auth-failed,
    never valid). client.close() in finally on every path. fetch_raw and compute_metrics RAISE
    NotImplementedError with MT5-worded fail-loud messages (adapt the sfox wording: MT5 returns come
    from the Phase-136 combine_mt5_deal_ledger daily-NAV reconstruction through the ONE backbone;
    a fill-based snapshot here is the BYB-02 class). compute_fingerprint/reconstruct_positions
    delegate to the shared impls exactly like sfox.py:142-157.
  </action>
  <acceptance_criteria>
    - Source Literal, SUPPORTED_SOURCES, and _FACTORIES each contain "mt5" (all three in ONE commit)
    - services/ingestion/mt5.py exists; Mt5Adapter.validate implemented; compute_metrics/fetch_raw raise
    - services/mt5_validation.py exists with mt5_probe_request, is_trade_capable, classify_mt5_login_error and [ASSUMED] markers
    - closed_sets.py defines the three EXACT detail strings + mt5_enabled_server
    - `cd analytics-service && ! grep -rn "order_send(" services/ingestion/mt5.py services/mt5_validation.py` exits 0 (call token absent)
    - `cd analytics-service && python -c "from services.ingestion import get_adapter; a=get_adapter('mt5'); assert type(a).__name__=='Mt5Adapter'"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd analytics-service && pytest tests/test_ingestion_sfox.py tests/test_ingestion_deribit.py::test_source_literal_and_registry_agree -x -q</automated>
  </verify>
  <done>Registry resolves mt5 in lockstep; adapter fail-loud posture matches sfox byte-for-byte in structure; shared constants/seam exported for plans 03/04; existing sfox/deribit suites still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pydantic boundary Literals + parity-test edits + test_ingestion_mt5.py clone</name>
  <files>analytics-service/models/schemas.py, analytics-service/routers/debug_key_flow.py, analytics-service/tests/test_ingestion_protocol.py, analytics-service/tests/test_boundary_literals_parity.py, analytics-service/tests/test_ingestion_mt5.py</files>
  <read_first>
    - analytics-service/models/schemas.py:195-215 (VerifyStrategyRequest.exchange Literal :206)
    - analytics-service/routers/debug_key_flow.py:55-75 (Broker Literal :61 and how sfox is handled downstream of it)
    - analytics-service/tests/test_boundary_literals_parity.py:55-135 (_KEY_SAVE_EXCHANGES :61, the sfox CONTAIN test block :106-131 to mirror)
    - analytics-service/tests/test_ingestion_protocol.py:198-218 (hardcoded Source set-equality :212)
    - analytics-service/tests/test_ingestion_sfox.py (whole file — the suite to clone, esp. :190-205 lockstep block)
    - analytics-service/tests for the Mt5Client offline double: grep "_connect" analytics-service/tests/test_mt5_client*.py — reuse the 134 injection fixture pattern
  </read_first>
  <behavior>
    - Test (existing, must stay green): test_source_literal_and_registry_agree — auto-green from Task 1
    - Test (edit): test_ingestion_protocol.py:212 expected set gains "mt5"
    - Test (edit): _KEY_SAVE_EXCHANGES gains "mt5"; set-equality vs VerifyStrategyRequest.exchange holds
    - Test (new): test_verify_request_exchange_literal_contains_mt5, test_broker_literal_contains_mt5, test_source_literal_includes_mt5 (mirror the sfox CONTAIN block with Phase-135 comments)
    - Test (new file): full test_ingestion_mt5.py — lockstep membership, get_adapter caching, unknown-source rejection unchanged, fail-loud raises, and the Task-1 validate behavior suite against an injected _connect double
  </behavior>
  <action>
    In models/schemas.py:206: widen the VerifyStrategyRequest.exchange Literal to include "mt5"
    (per D: MT5SRC-01 lockstep; keep the SecretStr fields untouched). In
    routers/debug_key_flow.py:61: append "mt5" to the Broker Literal, following exactly how "sfox"
    was admitted there (read the surrounding handling first; if sfox has a non-ccxt guard
    downstream, mirror it for mt5 — do not let the debug harness route mt5 into ccxt classes).

    In tests/test_boundary_literals_parity.py: add "mt5" to _KEY_SAVE_EXCHANGES (:61) and add the
    three mt5 CONTAIN assertions mirroring the sfox block (:106-131), citing Phase 135 (MT5SRC-01)
    in the assertion messages. Do NOT add the migration-parity class here — that lands with the
    migration file in plan 135-02 (the class byte-reads a file that does not exist yet).

    In tests/test_ingestion_protocol.py:212: add "mt5" to the hardcoded expected Source set.

    Create tests/test_ingestion_mt5.py cloning test_ingestion_sfox.py's structure: the lockstep
    block (mt5 in Source + SUPPORTED_SOURCES; get_adapter("mt5") resolves an Mt5Adapter and caches;
    get_adapter("kraken") still ValueErrors), the fail-loud block (compute_metrics/fetch_raw raise
    NotImplementedError with the MT5 wording), plus the Mt5Adapter.validate unit suite from Task 1's
    behavior list, driving the adapter against Mt5Client's injected _connect transport double
    (reuse the Phase-134 offline contract-suite fixture pattern — no mt5linux install, no network).
    Include the mt5_enabled_server truth-table test here (or alongside existing sfox gate tests if
    a closed_sets test module exists — grep "sfox_enabled_server" analytics-service/tests first and
    conform to wherever those live, Rule 11). Set MT5_GATEWAY_HOST/PORT via monkeypatch in validate
    tests; assert the missing-env path raises (server misconfig, not an auth failure).
  </action>
  <acceptance_criteria>
    - `cd analytics-service && pytest tests/test_ingestion_mt5.py -x -q` exits 0
    - `cd analytics-service && pytest tests/test_boundary_literals_parity.py tests/test_ingestion_protocol.py tests/test_ingestion_deribit.py tests/test_ingestion_sfox.py -x -q` exits 0
    - MT5_MASTER_PASSWORD_DETAIL and AUTH_FAILED_DETAIL asserted as EXACT literals in the validate tests (byte-identity pins, the cross-language contract)
    - No edit to funding/UI surfaces; no migration-parity class added in this plan
  </acceptance_criteria>
  <verify>
    <automated>cd analytics-service && pytest tests/test_ingestion_mt5.py tests/test_boundary_literals_parity.py tests/test_ingestion_protocol.py -x -q</automated>
  </verify>
  <done>All Python boundary Literals admit mt5 in lockstep; parity pins green; the mt5 suite pins the validate contract offline. Full analytics pytest green (pre-existing pandera-missing local reds excluded per reference_local_python_missing_pandera — install pandera==0.32.1 --break-system-packages if needed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user MT5 creds → worker adapter | untrusted login/investor-pw/server enter validate via KeySubmissionRequest.context |
| worker → RPyC gateway | Mt5Client IPC; error text may embed remote strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Elevation of Privilege | Mt5Adapter.validate | mitigate | is_trade_capable (order_check + trade_allowed, either-positive-rejects) in ONE seam; master login returns valid=False so it is never encrypted/persisted |
| T-135-02 | Information Disclosure | mt5_validation / mt5.py error paths | mitigate | reuse Mt5ClientError construction-time scrubbing; never log login/pw/server values; propagated transients carry scrubbed detail only |
| T-135-03 | Spoofing | validate bad-creds path | mitigate | fail-CLOSED: non-numeric login, empty server, auth errors all return valid=False; unknown errors propagate, never valid=True |
| T-135-04 | Tampering | Source registry | mitigate | 3-point lockstep + set-equality parity tests red on any single-point drift |
| T-135-SC | Tampering | package installs | accept | N/A — this phase installs nothing (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- cd analytics-service && pytest -q (full suite; only pre-existing pandera-missing local reds tolerated)
- ! grep -rn "order_send(" analytics-service/services/ingestion/mt5.py analytics-service/services/mt5_validation.py
- python -c import check: get_adapter("mt5") resolves and caches
</verification>

<success_criteria>
- MT5SRC-01 fully satisfied: 'mt5' in lockstep across Source Literal + SUPPORTED_SOURCES + _FACTORIES, parity tests green
- Mt5Adapter registered with sfox-faithful fail-loud posture; validate implemented against the 134 contract double
- Shared constants + classification seam exported (plans 03/04 consume without renegotiation)
</success_criteria>

<output>
Create `.planning/phases/135-mt5src-source-lockstep/135-01-SUMMARY.md` when done
</output>
