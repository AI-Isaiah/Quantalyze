---
phase: 135-mt5src-source-lockstep
plan: 04
type: execute
wave: 3
depends_on: [135-02, 135-03]
files_modified:
  - src/lib/wizardErrors.ts
  - src/app/api/keys/validate-and-encrypt/route.ts
  - src/app/api/keys/validate-and-encrypt/route.test.ts
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/api/strategies/composite/add-key/route.test.ts
autonomous: true
requirements: [MT5SRC-02, MT5SRC-03]

must_haves:
  truths:
    - "The worker's three MT5 failure details classify to three DISTINGUISHABLE codes: KEY_AUTH_FAILED (bad creds), KEY_MT5_MASTER_PASSWORD, KEY_MT5_WRONG_SERVER"
    - "validate-and-encrypt fails closed for mt5 when MT5_ENABLED is unset (mirrors the sfox server gate) and requires all three mt5 credentials"
    - "create-with-key and composite/add-key accept exchange=mt5 and still 400 an invalid exchange"
    - "Raw worker messages are never returned to the client — only the classified code (H-0305 preserved)"
  artifacts:
    - path: "src/lib/wizardErrors.ts"
      provides: "KEY_MT5_MASTER_PASSWORD + KEY_MT5_WRONG_SERVER codes, copy entries, matcher branches"
      contains: "KEY_MT5_MASTER_PASSWORD"
    - path: "src/app/api/keys/validate-and-encrypt/route.ts"
      provides: "isMt5EnabledServer gate + mt5 three-credential defense-in-depth"
      contains: "isMt5EnabledServer"
  key_links:
    - from: "src/lib/wizardErrors.ts"
      to: "analytics-service/services/closed_sets.py"
      via: "substring pins: 'master password' / 'broker server' match the EXACT worker MT5_*_DETAIL strings"
      pattern: "master password"
    - from: "src/app/api/keys/validate-and-encrypt/route.ts"
      to: "src/lib/closed-sets.ts"
      via: "isMt5EnabledServer import (plan 135-02 contract)"
      pattern: "isMt5EnabledServer"
---

<objective>
Close the TS half of the seam: two new distinguishable wizard error codes mapped from the worker's
MT5 detail strings (resolved Q-B — three distinguishable failure paths), the `MT5_ENABLED` server
gate + three-credential defense at the validate-and-encrypt route (resolved Q-C), and mt5
acceptance cases across all three key routes' tests.

Purpose: completes MT5SRC-03 (routes) + the error-surfacing tail of MT5SRC-02. NO UI wizard
rendering (Phase 138) — wizardErrors.ts is the classification/copy layer the key routes already
consume, and mt5 stays OUT of every offered/UI surface.
Output: 2 edited src files + 3 test files; tsc + full vitest green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-mt5src-source-lockstep/135-RESEARCH.md
@src/lib/wizardErrors.ts
@src/app/api/keys/validate-and-encrypt/route.ts

<interfaces>
Worker detail strings to pin (EXACT, from analytics-service/services/closed_sets.py, plans
135-01/135-03 — routes receive these as the worker 400 detail):
- MT5_MASTER_PASSWORD_DETAIL = "MT5 master password detected — this login can place trades. Reconnect using your read-only investor password."
- MT5_WRONG_SERVER_DETAIL = "Broker server not found — check the exact server name shown in your MT5 terminal login window."
- Bad creds arrive as the existing shared AUTH_FAILED_DETAIL ("authentication failed" substring) → existing KEY_AUTH_FAILED branch, zero edits.
- MT5_DISABLED_DETAIL = "MT5 integration is not yet available." (defense-in-depth only — the TS gate fires first).

Matcher contract (classifyKeyValidationError is substring-based, top-down, src/lib/wizardErrors.ts:844-899):
- lower.includes("master password") → { code: "KEY_MT5_MASTER_PASSWORD", status: 400 }
- lower.includes("broker server")   → { code: "KEY_MT5_WRONG_SERVER", status: 400 }
- Insert BOTH branches immediately AFTER the KEY_AUTH_FAILED branch (:868) and BEFORE the ip/allow
  branch — collision-checked: neither worker string contains "signature", "invalid secret",
  "authentication failed", "invalid_credentials", "ip"+"allow", "rate", "429", "timeout",
  "could not verify", "permission scope", "probe", "trading", or "withdraw".

From src/lib/closed-sets.ts (plan 135-02): isMt5EnabledServer() — strict process.env.MT5_ENABLED === "true".

Credential-slot contract (route → worker): login → api_key, investor password → api_secret,
broker server → passphrase. MT5 is the mirror-image of sfox: sfox RELAXES api_secret; mt5 REQUIRES
all three (api_key, api_secret, AND passphrase).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wizardErrors.ts — two distinguishable MT5 codes + copy + matcher branches</name>
  <files>src/lib/wizardErrors.ts</files>
  <read_first>
    - src/lib/wizardErrors.ts:15-60 (WizardErrorCode union incl. the KEY_AUTH_FAILED rationale comment style), :195-260 (WIZARD_ERROR_COPY entry shape for KEY_AUTH_FAILED/KEY_RATE_LIMIT), :844-899 (classifyKeyValidationError branch order + per-branch comments)
    - Existing wizardErrors tests: grep -rln "classifyKeyValidationError" src/__tests__ src/lib — read the found spec before extending
    - DESIGN.md (copy tone; wizard copy is user-facing even though the MT5 wizard card is Phase 138)
  </read_first>
  <behavior>
    - Test: classifyKeyValidationError(MT5_MASTER_PASSWORD_DETAIL-exact-string) === { code: "KEY_MT5_MASTER_PASSWORD", status: 400 }
    - Test: classifyKeyValidationError(MT5_WRONG_SERVER_DETAIL-exact-string) === { code: "KEY_MT5_WRONG_SERVER", status: 400 }
    - Test: classifyKeyValidationError("Authentication failed. Check your API key and secret.") still === KEY_AUTH_FAILED (three paths distinguishable, no regression)
    - Test: existing branch classifications unchanged (signature/rate/timeout/probe/trading fixtures still map to their codes — branch-insertion placement pin)
    - Test: WIZARD_ERROR_COPY has entries for both new codes (formatKeyError renders without placeholder leakage)
  </behavior>
  <action>
    Add "KEY_MT5_MASTER_PASSWORD" and "KEY_MT5_WRONG_SERVER" to the WizardErrorCode union with a
    rationale comment mirroring the KEY_AUTH_FAILED DOGFOOD style: per D (resolved Q-B), a master
    (trade-capable) login and a wrong/unknown broker server are DISTINCT user mistakes from bad
    credentials and need targeted, actionable copy — collapsing them into KEY_AUTH_FAILED would
    tell the user to fix the wrong thing.

    Add WIZARD_ERROR_COPY entries (honest, actionable, matching existing entry shape and DESIGN.md
    tone): master → explain the login works but can place trades, so it was refused and never
    stored; instruct connecting with the read-only INVESTOR password from the MT5 terminal. Wrong
    server → instruct copying the exact server name shown in the MT5 terminal login window. No
    invented claims (never assert "your password was wrong" on the master path — it wasn't).

    In classifyKeyValidationError: insert the two substring branches from the interfaces block
    immediately after the KEY_AUTH_FAILED branch and before the ip/allow branch, each with a
    comment pinning it to the worker constant name in services/closed_sets.py and noting the
    substring-collision invariant (any reword on either side must re-run the collision check
    against every branch keyword). The raw message is still never returned — only the code (H-0305
    unchanged).

    Extend the existing wizardErrors spec with the behavior tests above, importing/pinning the two
    worker strings as exact literals (byte-identity is the cross-language contract; a Python-side
    reword must red this suite).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - wizardErrors vitest green with the two exact-string classification tests + the KEY_AUTH_FAILED non-regression test
    - `grep -n "KEY_MT5_MASTER_PASSWORD\|KEY_MT5_WRONG_SERVER" src/lib/wizardErrors.ts` shows union entries + copy entries + matcher branches (3 sites each)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run test -- wizardErrors</automated>
  </verify>
  <done>Three distinguishable failure paths classify to three distinct codes with honest copy; existing classifications regression-pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validate-and-encrypt MT5 gate + credential defense + mt5 route test cases</name>
  <files>src/app/api/keys/validate-and-encrypt/route.ts, src/app/api/keys/validate-and-encrypt/route.test.ts, src/app/api/strategies/create-with-key/route.test.ts, src/app/api/strategies/composite/add-key/route.test.ts</files>
  <read_first>
    - node_modules/next/dist/docs/ route-handler guide (AGENTS.md: this Next.js differs from training data — read before touching route.ts)
    - src/app/api/keys/validate-and-encrypt/route.ts (whole file — the isSfoxEnabledServer gate :37-60, the sfox api_secret relaxation, how it forwards to the worker /validate-key + /encrypt-key)
    - src/app/api/keys/validate-and-encrypt/route.test.ts (existing sfox gate test cases — the template for mt5 cases)
    - src/app/api/strategies/create-with-key/route.ts:47-92 + route.test.ts; src/app/api/strategies/composite/add-key/route.ts:67-119 + route.test.ts (isSupportedExchange gates + sfox relaxation blocks)
  </read_first>
  <behavior>
    - validate-and-encrypt: exchange=mt5 with MT5_ENABLED unset → 400 fail-closed rejection mirroring the sfox-disabled shape; NO worker call made
    - validate-and-encrypt: exchange=mt5, MT5_ENABLED="true", any of api_key/api_secret/passphrase missing or blank → 400 BEFORE the worker call (three-credential defense — the mirror-image of the sfox api_secret relaxation)
    - validate-and-encrypt: exchange=mt5 gate-on happy path → forwards all three slots to the worker /validate-key then /encrypt-key (mocked worker; assert the forwarded body carries api_key/api_secret/passphrase)
    - create-with-key: exchange=mt5 clears isSupportedExchange (no 400 on the exchange gate); exchange="notanexchange" still 400
    - composite/add-key: same pair of cases
  </behavior>
  <action>
    In validate-and-encrypt/route.ts: mirror the sfox server gate for mt5 — import
    isMt5EnabledServer from @/lib/closed-sets and reject exchange==="mt5" when the gate is off,
    using the same response shape/status as the sfox-disabled arm (read :37-60 and clone its form;
    defense-in-depth with the worker's own MT5_DISABLED_DETAIL gate behind it). Add the mt5
    three-credential presence check: mt5 requires non-blank api_key, api_secret AND passphrase
    (server) — reject 400 with the route's existing missing-field response shape before any worker
    call, commented as the mirror-image of the sfox api_secret relaxation and as defense-in-depth
    (the worker is_mt5 branch is the authoritative enforcement). Do NOT restructure the route or
    touch the sfox arms; the route intentionally has no isSupportedExchange gate (research Q3) —
    do not add one.

    create-with-key and composite/add-key need ZERO route.ts edits (isSupportedExchange auto-widened
    in plan 135-02) — add the mt5 acceptance + invalid-still-rejected cases to their route.test.ts,
    cloning how the sfox cases were added. In all three test files pin: mt5 flows the
    api_secret-REQUIRED path (no sfox-style relaxation leaks onto mt5).
  </action>
  <acceptance_criteria>
    - `npm run test -- validate-and-encrypt create-with-key add-key` green with the new mt5 cases
    - Gate-off test asserts the worker fetch mock was NOT called (no live probe when dark)
    - `npx tsc --noEmit` exits 0
    - `npm run lint` clean on touched files
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run test -- validate-and-encrypt create-with-key add-key wizardErrors closed-sets</automated>
  </verify>
  <done>All 3 key routes accept mt5 end-to-end at the TS layer, gated dark server-side, three credentials enforced, invalid exchanges still rejected; full vitest suite green (coverage thresholds hold).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js key routes | untrusted exchange + three credential fields |
| Next.js → worker /validate-key, /encrypt-key | server-to-server forward of secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-14 | Information Disclosure | route error responses | mitigate | raw worker message never returned — classifyKeyValidationError code only (H-0305 pinned by tests) |
| T-135-15 | Tampering | invalid exchange smuggled | mitigate | isSupportedExchange gate + pydantic Literal + SQL CHECK (3-layer, each parity-pinned) |
| T-135-16 | Elevation of Privilege | mt5 probe fired pre-go-live | mitigate | isMt5EnabledServer TS gate + worker mt5_enabled_server gate (both fail-closed, dual-layer) |
| T-135-17 | Spoofing | incomplete mt5 credentials reach worker | mitigate | three-credential presence check at route; worker branch authoritative |
| T-135-SC | Tampering | package installs | accept | N/A — no installs this phase |
</threat_model>

<verification>
- npx tsc --noEmit clean; npm run test (full vitest, sharded ok) green with coverage thresholds
- Phase gate (all 4 plans): cd analytics-service && pytest -q green; full vitest green; SQL gate GREEN on TEST project; grep gates: ! grep -rn "order_send(" analytics-service/routers/exchange.py analytics-service/services/ingestion/mt5.py analytics-service/services/mt5_validation.py
</verification>

<success_criteria>
- MT5SRC-03 complete: all 3 routes accept mt5, invalid rejected, DB widened + verified (135-02)
- MT5SRC-02 error surfacing complete: bad-creds / master-password / wrong-server are three distinguishable, honestly-worded codes end-to-end
- Seam ships DARK: MT5_ENABLED gates worker + TS server paths; mt5 absent from every offered/UI surface
</success_criteria>

<output>
Create `.planning/phases/135-mt5src-source-lockstep/135-04-SUMMARY.md` when done
</output>
