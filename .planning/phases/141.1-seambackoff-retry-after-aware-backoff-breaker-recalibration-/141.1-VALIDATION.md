---
phase: 141.1
slug: seambackoff-retry-after-aware-backoff-breaker-recalibration
status: executed
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-31
ledger_closed: 2026-07-31
ledger_rows_observed: 20/20
---

# Phase 141.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **The full decision→mutation map lives in `141.1-RESEARCH.md` § Validation Architecture.**
> This file is the executable contract; that section is its derivation. Every row below
> traces to a `D-NN` row there.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (`vitest.config.ts` at repo root, coverage via `@vitest/coverage-v8`) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npx vitest run src/lib/<file>.test.ts` |
| **Full suite command** | `npm run test` |
| **Typecheck** | `npm run typecheck` — **load-bearing for D-08 and D-11**, not optional here |
| **Lint** | `npm run lint` |
| **SQL gates** | `psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_*.sql` (CI `sql-tests` job auto-discovers) |
| **Estimated runtime** | ~30 s per file; full TS suite several minutes |

**Flake note:** local vitest flakes → re-run with `--no-file-parallelism` before treating a
failure as real.

---

## Sampling Rate

- **After every task commit:** `npx vitest run src/lib/<affected file>.test.ts` (< 30 s)
- **After every plan wave:** `npm run typecheck && npm run lint && npx vitest run src/lib/`
- **Before `/gsd:verify-work`:** `npm run test` full suite green + typecheck + lint
- **Max feedback latency:** 30 seconds per task

---

## Per-Task Verification Map

> Filled at plan time by `gsd-planner` — one row per task, keyed to the `D-NN` decision it
> discharges. Task IDs follow `141.1-<plan>-<task>`.

| Task ID | Plan | Wave | Decision | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| 141.1-01-T1 | 01 | 1 | D-01 | T-141.1-01/02 | fail-fast never throws in classification window | unit | `npx vitest run src/lib/resilient-fetch.retry.test.ts` | ✅ | ✅ **100 passed** (run with T2); SC-C + 503-only mutations RED |
| 141.1-01-T2 | 01 | 1 | D-02 | — | threshold ratified, derived trip arithmetic pinned | pin | `npx vitest run src/lib/seam-constants.pin.test.ts` | ✅ | ✅ **100 passed** (run with T1); threshold 5→4 reds **3** assertions (literal, derived, double-drift) |
| 141.1-02-T1 | 02 | 1 | D-03, D-04, D-05, D-06 | T-141.1-04/05 | evidence true, verdicts byte-identical | source+pin | `npx vitest run src/lib/seam-retry-registry.test.ts` | ✅ | ✅ **78 passed** (2 files); 10 of 13 evidence strings independently re-traced to live source, no correction needed; `retries:` diff vs base = 0 |
| 141.1-02-T2 | 02 | 1 | D-03, D-04, D-05 | T-141.1-04 | SC-G / SC-G′ content pins | pin | `npx vitest run src/lib/seam-retry-registry.test.ts` | ✅ | ✅ **78 passed**; SC-G + 3× SC-G′ + D-03 mutations RED (the D-03 pin was measured **vacuous** as planned and re-anchored — see SC-G′) |
| 141.1-02-T3 | 02 | 1 | D-06 | T-141.1-04 | SC-H / SC-H′ both polarities | invariant | `npx vitest run src/lib/seam-citations.invariant.test.ts src/lib/seam-retry-registry.test.ts` | ✅ | ✅ **78 passed**; both guards RED on their own halves, each GREEN on the other's — complementarity measured |
| 141.1-03-T1 | 03 | 1 | D-14 (c) | T-141.1-07 | SC-P draft-filter behaviour pin (Python test — OQ-2 decided: tests outside the fence) | behaviour | `cd analytics-service && python -m pytest tests/test_process_key.py -q -k resync` | ✅ | ✅ **9 passed**, 102 deselected (baseline 7 + the 2 new); full service suite 4824 passed |
| 141.1-04-T1 | 04 | 2 | D-08 | T-141.1-08/09 | SC-E axis closed by construction | types+unit | `npm run typecheck && npx vitest run src/lib/resilient-fetch.retry.test.ts` | ✅ | ✅ typecheck **clean**; **200 passed** (3 files); full suite 10 420 passed. Blast radius was tsc-enumerated: 49 errors across 5 files |
| 141.1-04-T2 | 04 | 2 | D-07 | T-141.1-10 | prose true post-D-08; rows-at-1 === 5 pin | pin | `npx vitest run src/lib/seam-constants.pin.test.ts` | ✅ | ✅ **78 passed**; flipping `bridge` to 0 reds **4** assertions; deleting the `bridge` registry entry reds the OQ-3 pin while the `it.each` merely shed a case (78→77) — the measured input to SC-J |
| 141.1-05-T1 | 05 | 2 | D-11 | T-141.1-11 | SC-I immutability (freeze + @ts-expect-error) | types+unit | `npm run typecheck && npx vitest run src/lib/seam-retry-registry.test.ts` | ✅ | ✅ typecheck **clean**; **43 registry tests passed** (67 across 2 files); freeze mutations C (12 RED) and D (6 RED, map level green) |
| 141.1-05-T2 | 05 | 2 | D-11 | T-141.1-12 | SC-I exhaustiveness + never-switch | types+unit | `npm run typecheck && npx vitest run src/lib/seam-retry-registry.test.ts` | ✅ | ✅ typecheck **clean**; 43 passed; 5th-`FlowType` mutation reds 2 independent sites with no hand-typed list edited, and the verdict-forcing chain was measured to step 3 |
| 141.1-06-T1 | 06 | 3 | D-17, D-19 (G2) | **T-141.1-13 (V7 credential leak — SC-M′ negative)**, T-141.1-14 | logs carry budgetKey+status ONLY; decodeBreakerLock bounded | unit | `npx vitest run src/lib/resilient-fetch.retry.test.ts src/lib/resilient-fetch.test.ts` | ✅ | ✅ **133 passed** (2 files); TDD RED `fe72e16c` precedes GREEN `f308b460`; SC-M (2 arms), SC-M′ (2 polarities) and G2 all RED |
| 141.1-06-T2 | 06 | 3 | D-12, D-13 | T-141.1-15 | SC-B / SC-D headline mutations RED | unit | `npx vitest run src/lib/resilient-fetch.retry.test.ts` | ✅ | ✅ **133 passed**; SC-B reds 2 assertions (was 125 green), SC-D reds 1 (was 21/21 green) while SC4b stays green — the discriminator holds |
| 141.1-06-T3 | 06 | 3 | D-10, D-14 (d) | T-141.1-15 | SC-A disagreement / SC-O production config | unit | `npx vitest run src/lib/resilient-fetch.retry.test.ts` | ✅ | ✅ **133 passed**; SC-A RED at both halves (row delegation + `typecheck`), SC-O RED on the **count** after the in-place upgrade; full suite 10 442 passed |
| 141.1-07-T1 | 07 | 3 | D-09 | T-141.1-16 | SC-F census retry axis, no <ABSENT> | invariant | `npx vitest run src/lib/resilient-fetch.wiring.test.ts` | ✅ | ✅ **188 passed** (3 files); defect measured first (22/22 green **unextended**), then 2 RED extended; second polarity `?? 0`→`?? 1` also RED |
| 141.1-07-T2 | 07 | 3 | D-14 (a, b) | T-141.1-18 | SC-J / SC-K + class-γ closed 3/3 | pin | `npx vitest run src/lib/seam-constants.pin.test.ts src/lib/seam-budgets.invariant.test.ts` | ✅ | ✅ **188 passed**; SC-J RED (`3 to be 4`), SC-K RED on literal **and** sum; class-γ grep = 3 code sites, all 3 fenced (2 of them documented as **not** previously exposed) |
| 141.1-07-T3 | 07 | 3 | D-15 | T-141.1-17 | SC-L per-leg recharge, 56_000 / 277_500 literals | invariant | `npx vitest run src/lib/seam-budgets.invariant.test.ts` | ✅ | ✅ **188 passed**; RED-before-GREEN at 43 250 vs the hand-typed 56 000, anti-shortcut RED at 405 000 vs 277 500; no route ceiling breached at the corrected figures |
| 141.1-08-T1 | 08 | 1 | D-16 | T-141.1-19/20 | numerator rebuilt on indexed fields; denominator distinct-correlation_id dedup | unit+integration | `npx vitest run tests/integration/cron-flag-monitor.test.ts src/app/api/cron/flag-monitor/route.test.ts` | ✅ | ✅ **49 passed** (2 files); query is `level:error transaction:/process-key correlation_id:* environment:production`; `head: true` count replaced by Set dedup; thresholds untouched |
| 141.1-08-T2 | 08 | 1 | D-16 | T-141.1-19/20 | SC-N strengthened: negative + positive pins, anti-`path:` recurrence guard, dedup mutation | integration | `npx vitest run tests/integration/cron-flag-monitor.test.ts src/app/api/cron/flag-monitor/route.test.ts` | ✅ | ✅ **49 passed**; 4 mutations RED (4 / 2 / 4 / 14 assertions). ⚠️ Sentry not re-probed this session — OQ-1 figures are plan-time; the end-to-end RED runs against an index-simulating double |
| 141.1-09-T1 | 09 | 4 | D-18, D-03 | T-141.1-22 | runbook content checklist (manual-only table) | grep+human | `grep -ci "fail-open" docs/runbooks/seam-breaker.md` | ✅ | ✅ **2** (≥1). Whole checklist measured: `breaker:railway` 3, `resetUsedTokens` 1, `Retry-After` 7, D-03 cost 2, README index row 1, `compute-queue.md` back-link 1, coordinate citations **0** |
| 141.1-09-T2 | 09 | 4 | D-19, D-03 | T-141.1-23 | ledger verdicts G1–G4 + H1–H7 (manual-only table) | grep+human | `grep -c "H-0562" TODOS.md` | ✅ | ✅ **1** (≥1). `DISCHARGED\|RE-BOOKED` grew 0→7; all seven `H1–H7` bullets present; G1's discharge independently re-derived (`recoverable` = 0 hits in `resilient-fetch.ts`) |
| 141.1-09-T3 | 09 | 4 | D-20 | — | lockstep bump, one commit | CLI | `diff <(cat VERSION) <(node -p "require('./package.json').version")` | ✅ | ✅ no output — both `0.51.0.0`; CHANGELOG head `[0.51.0.0] - 2026-07-31`; all three files in ONE commit `f6dee566`, append-only at the head |

---

## Wave 0 Requirements

**None — existing infrastructure covers every decision in this phase.**

All test files already exist: `src/lib/resilient-fetch.retry.test.ts`, `resilient-fetch.test.ts`,
`resilient-fetch.wiring.test.ts`, `seam-constants.pin.test.ts`, `seam-budgets.invariant.test.ts`,
`seam-citations.invariant.test.ts`, `seam-retry-registry.test.ts`,
`tests/integration/cron-flag-monitor.test.ts`, `supabase/tests/test_resync_retry_single_job.sql`.

The harness is complete: `fakeRedisFor`, `seedBreakerOpen`, `shared.mode.staleReadOnce`
(**already built and currently unused — it is D-13's tool**), fake timers, `vi.spyOn`-based
fetch mocking.

One **new non-test** file: `docs/runbooks/seam-breaker.md` (D-18).

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| SQL fixture's transaction claim is corrected without altering any assertion | D-07 | Comment-only edit inside a `.sql` fixture; no runner asserts on comments | `git diff --stat supabase/tests/test_resync_retry_single_job.sql` shows comment lines only; re-read `:40` against the actual `BEGIN;` position |
| Runbook is accurate and reaches on-call | D-18 | Prose; correctness is a human judgement about mental models | Read `docs/runbooks/seam-breaker.md`: must state fail-open semantics, the Upstash key shapes, `seam.breaker.open`, up-to-2-attempts, the D-01 `Retry-After` fail-fast rule, and the D-03 doubled-compute cost. Must **not** be confusable with `compute-queue.md:160`'s different (Python per-API-key 429) breaker |
| `TODOS.md` obligations carry an explicit verdict | D-19 | Ledger bookkeeping | A `Phase 141 / 141.1 deferrals` heading exists; G1, G2, G3, G4 each carry `DISCHARGED` or `RE-BOOKED`; all of bucket H1–H7 is recorded. **G5 is already discharged (`2e36016d`) — no task** |
| VERSION / package.json / CHANGELOG move in lockstep | D-20 | Release step, verified at ship | `diff <(cat VERSION) <(node -p "require('./package.json').version")` prints nothing; CHANGELOG head matches; **all three in ONE commit** |

---

## Falsifiability Ledger

> **Coverage answers "is it verified?". This section answers "CAN the verification FAIL?"**
>
> This phase exists partly *because* two production-code deletions left the suite green.
> Treat every row here as owed evidence, not as a formality.

**The two headline mutations — these are measured facts about the current suite, not hypotheses:**

| SC | Mutation (exact edit to production source) | Must turn RED | Observed? | Evidence |
|----|-------------------------------------------|---------------|-----------|----------|
| SC-A | *(post-D-08 two-part form; the pre-D-08 measured fact: deleting BOTH `retriesOverride` lines left **558 tests green across 11 files**)* (a) in `process-key-client.ts`, substitute the registry lookup with row delegation — `SEAM_BUDGETS[budgetKeyFor(args.flow_type)].retries as 0 \| 1`; (b) outright DELETE the `retriesOverride` line | (a) the disagreement test REDs (force `SEAM_BUDGETS["process-key-sync"].retries = 1`, `flow_type: "teaser"`, persistent transient ⇒ assert **1** fetch); (b) `npm run typecheck` REDs — the field is required post-D-08; paste the tsc error | ✅ **OBSERVED** (plan 06, `8613bdf2`) | (a) RED: *"The anonymous teaser RETRIED… expected "vi.fn()" to be called 1 times, but got 2 times"*. (b) RED: `process-key-client.ts(464,59): error TS2345 … 'retriesOverride' is missing`. **Third half added:** the same mutation applied to the CORE (`const retries = SEAM_BUDGETS[budgetKey].retries`) also REDs SC-A + 3 others — so the belt covers the core, not just the call site. |
| SC-B | `resilient-fetch.ts:2041`: hoist `const deadline = AbortSignal.timeout(timeoutMs)` above the `for` loop | `expect(calls[0][1].signal).not.toBe(calls[1][1].signal)` in `resilient-fetch.retry.test.ts` | ✅ **OBSERVED** (plan 06, `b9f9d5c3`) | **Was 125 green; now 2 RED.** Signal: *"expected AbortSignal { aborted: false } not to be AbortSignal { aborted: false }"*. Fresh-budget (fake timers): *"Attempt 2 aborted the instant it was issued… expected 2 to be 1"*. ⚠️ Mechanics note: `AbortSignal.timeout` is **not** driven by vitest fake timers (measured) — the case substitutes a `setTimeout`-backed spy. |
| SC-C | `resilient-fetch.ts:2160`: delete the `&& !hasContractualWait(res)` conjunct | D-01 test: a 503 carrying `Retry-After` must yield **1** fetch, must return attempt 1's body (`dependency` present), and must record **1** breaker failure — not 2 | ✅ **OBSERVED** (plan 01, `10d5f2cb`; mutations re-run at `0061a5cf`) | **Was 24 green; now 1 RED.** *"A 503 that advertised a 15s wait was retried anyway. Retrying inside the backoff is near-certain to fail and buys nothing but a second breaker failure and billed lambda wall-clock.: expected "vi.fn()" to be called 1 times, but got 2 times"* at `resilient-fetch.retry.test.ts:464` — `1 failed \| 23 passed (24)`; reverted → 24 passed. ⚠️ **Scope of what was observed:** the fetch-count assertion is the one that reddened and the only one quoted. The body-intact and one-breaker-failure halves live in the SAME `it` (*"exactly ONE fetch, body intact, ONE breaker failure, and no backoff sleep"*) but vitest stops at the first failing assertion, so their RED was not separately observed. |
| SC-C′ | *(anti-regression, opposite direction)* leave D-01 in and remove the header from the fixture | a 503 **without** `Retry-After` must still yield **2** fetches | ✅ **OBSERVED — green polarity** (plan 01, `10d5f2cb`) | Pins that D-01 did not disable the whole 503 retry arm. The case ships inside the `[SEAM-06 / D-01]` describe block and **ran GREEN as required** (`24 passed` at `0061a5cf`; it is among the 23 that stay green under SC-C's mutation). ⚠️ **No RED was measured for this row, and none exists by construction** — its oracle asserts 2 fetches, so it can only redden if the fail-fast is widened past the header check, and that mutation was **not run** (recorded as skipped, not as caught). The adjacent 503-only mutation (delete the `res.status !== 503` early return inside `hasContractualWait`) reddens a **different** case: *"The fail-fast widened past 503. Only a 503 is contractually obliged to carry a wait it means, so only a 503 may be believed.: expected "vi.fn()" to be called 2 times, but got 1 times"* — `1 failed \| 23 passed (24)`. |
| SC-D | `resilient-fetch.ts:2019`/`:2025`: swap `sleep(...)` and the pre-attempt-2 `isBreakerOpen` re-check | D-13 test via `shared.mode.staleReadOnce` (first read CLOSED, post-sleep read OPEN ⇒ `CircuitOpenError`, **1** fetch) | ✅ **OBSERVED** (plan 06, `b9f9d5c3`) | **Was 21/21 green; now RED:** *"The retry fired against an OPEN circuit… expected null to be an instance of CircuitOpenError"*. SC4b stayed GREEN under the same swap, confirming SC-D is the only discriminator. ⚠️ Built on a fake-timer `setTimeout` store-write at +100 ms, **not** `staleReadOnce` — that hook is `get`-only and `isBreakerOpen` uses `mget`, so it could not have driven this. |
| SC-E | `resilient-fetch.ts`: re-add the `?? SEAM_BUDGETS[budgetKey].retries` fallback | `npm run typecheck` rejects the `@ts-expect-error` fixture calling `resilientFetch("process-key-enqueue", …)` with no `retriesOverride`; runtime half reds SC-A's test | ✅ **OBSERVED — compile half in BOTH directions; runtime half re-scoped** (plan 04 `bf13b47c`, corrected by plan 06 `8613bdf2`) | D-08 — the axis must close **by construction**. **Compile half (plan 04).** Deleting the `@ts-expect-error`: `resilient-fetch.test.ts(2946,65): error TS2345 … Property 'retriesOverride' is missing in type '{ method: string; body: string; }' but required in type '{ timeoutMsOverride?: number \| undefined; retriesOverride: 0 \| 1; }'`. Re-optionalising the field (`retriesOverride?: 0 \| 1`): `resilient-fetch.test.ts(2946,3): error TS2578: Unused '@ts-expect-error' directive.` — the fixture is not merely satisfied by the current type, it **fails if the type is relaxed**. **Runtime half — this row's history is TWO-STEP, and the mutation as written is a no-op only in the second step.** *Pre*-D-08 the validator was wrapped in `if (retriesOverride !== undefined)` and the assignment really was `retriesOverride ?? SEAM_BUDGETS[budgetKey].retries`, so the row fallback **was reachable** — 141.1-04's D-08 change closed a real hole. *Post*-D-08 the validator is unguarded, so `undefined` meets `typeof retriesOverride !== "number"` and throws `SeamConfigError` above the assignment; re-adding the `??` fallback is therefore unreachable dead code and cannot redden any runtime test. 141.1-04 measured that green (254 passed) and booked it as an open hole — **that follow-up measurement was the error, not the D-08 change**; 141.1-06 re-measured it green after its own work (263 passed) and established why. **The axis is closed at three independent points, each observed:** (i) the call-site type, above; (ii) the semantically LIVE form of the same defect — `const retries = SEAM_BUDGETS[budgetKey].retries` inside the core — reds SC-A plus 3 other cases (`4 failed \| 28 passed (32)`); (iii) an ABSENT-override case added to the validator's `it.each`, proven RED by dropping the validator's `typeof` conjunct: *"expected Error: [resilient-fetch] retry loop exite… to be an instance of SeamConfigError"* (`1 failed \| 32 passed (33)`). |
| SC-F | `analytics-client.ts:434`: delete `retriesOverride`, **or** add a 6th `resilientFetch` call site with none | `resilient-fetch.wiring.test.ts` census set-equality reds with `<ABSENT>` | ✅ **OBSERVED — with the defect measured FIRST** (plan 07, `c9a286d9`) | D-09 — **extend `EXPECTED_BINDINGS`, do not invent a parallel mechanism.** This is the instance-not-class finding. **Baseline (the defect, measured before the fix):** with the census unextended, deleting `retriesOverride` from the `analyticsRequest` chokepoint left the wiring file **22 passed (22)** — the retry axis was discovered by nothing. **Same deletion, census EXTENDED** (a `retry` field on the existing roster, carried through `encodeBinding` — zero new files): the pre-existing set-equality reds with nine `<ABSENT>` rows (*"i \| bridge \| /api/portfolio-bridge \| src/lib/analytics-client.ts::findReplacementCandidates \| `<ABSENT>`"* ×9) **and** the dedicated assertion — *"A seam call site passes NO `retriesOverride`. … a site that never states one is an UNAUDITED replay, not a safe default.: expected [ …(9) ] to deeply equal []"* — `2 failed \| 21 passed (23)`; reverted → 23 passed. **Second polarity:** drifting the process-key chokepoint from `?? 0` to `?? 1` reds the set-equality alone (`1 failed \| 22 passed (23)`), the no-`<ABSENT>` assertion correctly staying green. Run under vitest only against a tsc-invalid tree, proving the census works **below** the type system. |
| SC-G | `seam-retry-registry.ts`: re-introduce `"no persisted server-side write"` into any analytics YES evidence other than `optimize-weights` | banned-string assertion in `seam-retry-registry.test.ts` | ✅ **OBSERVED** (plan 02, `aecc82ff`) | D-04 — the exact false claim, banned by construction. Re-inserted into `bridge` → RED: *"bridge carries the claim "no persisted server-side write", which the 141.1 re-derivation DISPROVED for it (bridge and simulator append audit rows; portfolio-optimizer additionally UPDATEs portfolio_analytics.optimizer_suggestions)… expected 'findReplacementCandidates → the portf…' not to contain 'no persisted server-side write'"* — `1 failed \| 26 passed (27)`. Non-vacuity twin ships alongside: `optimize-weights` must **still** carry the string, so deleting the phrase everywhere cannot leave the ban green. |
| SC-G′ | `seam-retry-registry.ts`: delete the 3-indexed-status qualification from `resync.evidence`, or the `isUuid` / `cleanup_abandoned_wizard_drafts` / constraint-name anchors from `onboard.evidence` | content assertions in `seam-retry-registry.test.ts` | ✅ **OBSERVED — 3 arms run of the 5 named** (plan 02, `aecc82ff`) | D-05. **(1)** Delete the `done_pending_children` qualification → *"resync no longer names the three statuses the partial unique index compute_jobs_one_inflight_per_kind_strategy actually admits (pending, running, done_pending_children). Unqualified, "exactly one job" reads as an absolute — but failed_retry sits OUTSIDE the index predicate…"*, expected the resync evidence to match `pending[\s\S]*running[\s\S]*done_pending_children`. **(2)** Replace `isUuid` with a generic phrase → *"onboard's evidence has lost part of the provenance chain that makes its non-NULL wizard_session_id guarantee FALSIFIABLE rather than merely asserted…"*, expected to match `/isUuid/`. **(3)** Remove the `validate-and-encrypt` producer mention → *"onboard's evidence no longer names both emitters of flow_type=onboard…"*, expected to match `/validate-and-encrypt/`. Each `1 failed \| 26 passed (27)`. ⚠️ The row's other two named anchors — `cleanup_abandoned_wizard_drafts` and the constraint-name anchor — were **not** mutated; the row reads "or" so the criterion is met, but those two pins are recorded as **un-mutated**, not as caught. (The `/s` dotAll form the plan specified failed `tsc` with TS1501 and was replaced by `[\s\S]*`; the resync mutation was re-run afterwards and is RED in the shipped form.) |
| SC-H | plant `"foo.ts:12"` inside an `evidence:` string in `seam-retry-registry.ts` | the **new registry-local** evidence guard | ✅ **OBSERVED — both polarities, both map shapes** (plan 02, `902b0e3d`) | D-06. ⚠️ **Adding the file to `SEAM_CITATION_SURFACE` alone is vacuous** — `extractComments` excludes string literals by design (self-test at `seam-citations.invariant.test.ts:369-375`). Prove **both** polarities: planted rot caught, symbol anchor not caught. **Planted rot, YES map:** `see foo.ts:12` in `RETRY_SAFE_ANALYTICS.bridge` → RED: *"a coordinate citation in evidence prose rots silently — anchor the symbol, not the address. RETRY_SAFE_ANALYTICS.bridge cites `foo.ts:12`"* — `1 failed \| 30 passed (31)`. **Required second member (bare-string NO map):** the same plant in `RETRY_AUDIT_NO_ANALYTICS["portfolio-analytics"]` also RED — *"Note that the sibling comment guard in seam-citations.invariant.test.ts CANNOT see these: it extracts comments, and this rot lives in string literals."* Both value shapes proven, so the guard is not half-vacuous on the 7 NO-map strings. **Negative polarity observed GREEN:** `citationsIn("resolved by _resume_duplicate_job in process_key.py — no coordinate")` → `[]` — a symbol anchor is **not** caught. **Anti-vacuity:** commenting out the `match-eval` NO entry reds the hand-typed population fence — *"the evidence-string population is 12, not 13 (2 flow YES + 4 analytics YES + 2 flow NO + 5 analytics NO)… expected 12 to be 13"*. |
| SC-H′ | plant a bare `// see foo.ts:12` **comment** in `seam-retry-registry.ts` | the `SEAM_CITATION_SURFACE` roster `it.each` | ✅ **OBSERVED** (plan 02, `902b0e3d`) | D-06 roster half; costs the `.toBe(34)` → `35` pin bump at `:263`. Planted comment → RED on the roster: *"src/lib/seam-retry-registry.ts:65 cites `foo.ts:12`"* — `1 failed \| 46 passed (47)`. The predicted pin bump was paid: roster grown to 35 with the `.toBe(35)` literal. **Complementarity proven:** this same comment mutation left the registry-local guard of SC-H **GREEN** (31 passed) — the two guards cover disjoint halves and neither alone closes the class. |
| SC-I | `seam-retry-registry.ts`: `RETRY_SAFE_FLOW_TYPES.teaser = {retries:1, evidence:""}` in a `@ts-expect-error` fixture; and add a 5th member to the `FlowType` union | must not compile (`npm run typecheck`); `expect(Object.isFrozen(...)).toBe(true)` at runtime; `budgetKeyFor`'s `never`-defaulted switch reds **without** editing the hand-typed list | ✅ **OBSERVED — 8 mutations** (plan 05, `522bfde4` / `14c1748e`) | D-11 — exhaustiveness must be *real*, not two hand-typed lists agreeing. **Compile (freeze):** removing either directive reds — `seam-retry-registry.test.ts(465,31): error TS2540: Cannot assign to 'teaser' because it is a read-only property` and `(479,16): error TS2704: The operand of a 'delete' operator cannot be a read-only property`. **Runtime (freeze):** neutering `freezeVerdicts` to `return map;` reds all twelve immutability tests — *"RETRY_SAFE_FLOW_TYPES is not frozen — a consumer push can ADD a verdict row, and an added row is indistinguishable from an audited one at the gate.: expected false to be true"*. A **SHALLOW** freeze reds only the 6 ENTRY assertions and leaves the 4 map-level ones green (`6 failed \| 37 passed (43)`), proving the two levels are separately load-bearing — the REPOINT vector, not just ADD. **Exhaustiveness, no hand-typed list edited:** adding a 5th `FlowType` member reds at two independent sites — `process-key-client.ts(126,13): error TS2322: Type '"backfill"' is not assignable to type 'never'` (`budgetKeyFor`'s `_exhaustive: never` default arm) and `seam-retry-registry.test.ts(94,7): error TS2322: Type 'true' is not assignable to type 'never'`. **Chain step 3, measured beyond the criterion:** making the only two edits that clear those errors turns `typecheck` clean and reddens the RUNTIME key-set pin (*"expected [ Array(4) ] to deeply equal [ 'backfill', 'csv', 'onboard', …(2) ]"*), so a real **verdict** is forced, not merely a coverage entry. A 14th `SeamBudgetKey` and a typo'd key in the hand-typed list also red (TS2322 / TS2820). |
| SC-J | empty `RETRY_SAFE_ANALYTICS` | `expect(Object.keys(RETRY_SAFE_ANALYTICS).length).toBe(4)` | ✅ **OBSERVED — with the vacuity reproduced FIRST** (plan 07, `86db72a8`) | D-14a — today `it.each(Object.keys(...))` at `seam-constants.pin.test.ts:337` yields **zero cases and stays green**. **141.1-04's measured signature reproduced verbatim before the fence:** deleting the `bridge` entry gave `1 failed \| 76 passed (77)` — 78 → 77, the `it.each` **shed a case** rather than failing, and the single failure came from 04's own OQ-3 pin, not from the `it.each`. **With the fence added**, the same deletion reds it: *"RETRY_SAFE_ANALYTICS no longer holds exactly 4 audited retry verdicts (bridge, simulator, portfolio-optimizer, optimize-weights). A FIFTH is a wrapper granted a replay without an idempotency audit; a THIRD means an authorised retry was withdrawn while SC-4b still charges for it — and the case list below would shrink to match, silently.: expected 3 to be 4"* — `2 failed \| 79 passed (81)`. ⚠️ The **executed** mutation was the single-entry deletion (4 → 3), not this row's literal "empty `RETRY_SAFE_ANALYTICS`"; the shipped fence is hand-typed across all four maps (4/2/2/5) so both shapes redden, but only the 4→3 shape was measured. Class-γ closed **3/3 by grep enumeration**, not at the named instance alone. |
| SC-K | change `SEAM_RETRY_BACKOFF_MS` (250) or `SEAM_RETRY_JITTER_MAX_MS` (250) | literal pins in `seam-constants.pin.test.ts` | ✅ **OBSERVED — one of the two named constants mutated** (plan 07, `86db72a8`) | D-14b — neither has a literal pin today, unlike every sibling constant. `SEAM_RETRY_BACKOFF_MS` 250 → 300 reds **both** the literal pin and the sum pin: *"SEAM_RETRY_BACKOFF_MS moved. RAISING it lengthens every retried route's worst-case lambda hold, which SC-4b charges at the MAX of backoff + jitter — the finalize-wizard composite has 22 500 ms of headroom, so this is not free.: expected 300 to be 250"* and *"The worst-case wait between a failed seam attempt and its retry is no longer 500ms. SC-4b charges this MAX once per retried leg: expected 550 to be 500"* — `2 failed \| 80 passed (82)`; reverted → 82 passed. ⚠️ `SEAM_RETRY_JITTER_MAX_MS` now carries its own hand-typed 250 pin but was **not itself mutated** — that pin is recorded as un-mutated; only the sum pin was measured reddening on that side. |
| SC-L | `seam-budgets.invariant.test.ts`: delete the `(1 + retries)` factor from the `storeMs` term | hand-typed worst-case assertion: `keys/sync` in the `failing` state is exactly **56 000 ms** | ✅ **OBSERVED — RED-before-GREEN plus both wrong-fix directions** (plan 07, `5caf7195`) | D-15. ⚠️ `STORE_COMMANDS_PER_SEAM_CALL.failing` **stays 3** with a `(1 + retries)` multiplier — a flat 6 false-REDs `finalize-wizard` composite at 405 000 ms vs a 300 000 ms ceiling. **RED first:** the `56_000` oracle added against the UNCORRECTED (flat) store term — *"keys/sync's failing-state worst case is now 43250ms; the hand-computed figure is 56 000ms (30 500 request + 25 500 store). A LOWER number means the retried leg's second store round stopped being charged and SC-4b is certifying headroom the route does not have.: expected 43250 to be 56000"* — `1 failed \| 82 passed (83)`; applying the per-leg fix → 83 passed. This row's own mutation (deleting the `(1 + retries)` factor) reproduces that output identically. **Anti-shortcut, the other direction:** `STORE_COMMANDS_PER_SEAM_CALL.failing` 3 → 6 → *"finalize-wizard's COMPOSITE branch now costs 405000ms in the failing state; it must stay 277 500ms. Every leg on this branch is retries: 0, so D-15's correction cannot move it. 405 000 means the store's per-attempt count was raised instead of the retry factor being applied per leg… do NOT raise its maxDuration to absorb an arithmetic error.: expected 405000 to be 277500"*, plus the ceiling assertion at *"expected 405000 to be less than 300000"* — `3 failed \| 80 passed (83)`. Both figures match the plan's predictions exactly; `failing` correctly stays 3. |
| SC-M | `resilient-fetch.ts`: delete the new counting-5xx `console.error` | D-17a spy assertion (logs once, names budget key + status) | ✅ **OBSERVED** (plan 06, `f308b460`) | RED: *"The counting-5xx retry arm discarded attempt 1's diagnosis in silence… expected [] to have a length of 1 but got +0"*. **Arm 2** (delete the `CircuitOpenError`-side log) also RED: *"…the diagnosis is gone from every surface at once: expected undefined to be defined"*. |
| SC-M′ | *(negative, security-critical)* make the new log line interpolate `requestInit` | D-17b assertion: logged string contains **neither** the request body **nor** any header value (`INTERNAL_API_TOKEN`, `X-Service-Key`, `X-User-Access-Token`) | ✅ **OBSERVED** (plan 06, `f308b460`) | RED, and the received string carried **all three credential headers and the body verbatim**: `…retrying after backoff {"method":"POST","headers":{"Authorization":"Bearer SENTINEL_AUTHORIZATION_XYZ","X-Service-Key":"SENTINEL_SERVICE_KEY_XYZ","X-User-Access-Token":"SENTINEL_USER_ACCESS_TOKEN_XYZ"},"body":"{\"secret\":\"SENTINEL_BODY_XYZ\"}"}`. A **second** polarity (interpolate `path`) also RED, so the path half of the oracle is not vacuous either. |
| SC-N | `cron/flag-monitor/route.ts`: (a) re-add `path:/api/process-key` to the numerator; (b) remove the indexed scoping term; (c) replace the Set-dedup denominator with a raw row count | (a) the negative pin (`not.toContain("path:")`) AND the anti-`path:` recurrence guard both RED; (b) the hand-typed full-query literal pin RED; (c) the 3-rows/2-correlation-ids ⇒ 2 denominator test RED | ✅ **OBSERVED — all three arms, plus a 4th unplanned** (plan 08, `71b97484` / `297677b1`) | D-16. **OQ-1 RESOLVED: the numerator was structurally DEAD** (0 events in 90d with the `path:` term, 681 without — see Open Questions). Recurrence guard covers a class currently of size ONE (grep-verified; `phase19-error-rollup` retired in `ce32afbd`). Baseline `49 passed (49)`. **(a)** Re-adding `path:/api/process-key` → **4 RED**: the negative pin (*"expected 'level:error path:/api/process-key tra…' not to contain 'path:'"*), the recurrence guard (*"…Sentry query "…" filters on `path:`. On this repo's instrumentation, path is written to Sentry `extra` and NEVER to `tags` — extra is unindexed, so a `path:` filter matches nothing and the alert goes permanently silent with no signal.: expected true to be false"*), the positive pin, and the end-to-end alert test at *"expected +0 to be 10"* — **the production silence reproduced**. **(b)** Dropping `transaction:/process-key` → 2 RED, including *"expected 682 to be 10"* — the cron-recompute swamp, so the pin distinguishes "scoped correctly" from "matches something". **(c)** Replacing the Set dedup with `return data.length` → 4 RED: *"expected 3 to be 2"* (3 audit rows across 2 correlation ids — the retry inflation) and *"expected 4 to be 3"*. **4th, not required by the plan:** neutering the numerator (`errorCount: 0 * parseSentryCount(...)`) reds 14 assertions, so the numerator is proven load-bearing on the alert decision. ⚠️ Two scope facts the executor recorded and this ledger keeps: the Sentry probe was **not re-run** in that session (the OQ-1 table above is quoted from plan time, not re-measured), and `transaction:/process-key` is **derived** from the FastAPI prefix rather than observed in the index — the end-to-end RED runs against an index-simulating double, so the repaired numerator CAN fire but is **not proven** to have fired. |
| SC-O | `SEAM_BUDGETS.bridge.retries` → 0 (pre-D-08) / delete the registry entry (post-D-08) | new `bridge`-at-production-configuration case in `resilient-fetch.retry.test.ts`, written through `findReplacementCandidates` | ✅ **OBSERVED** (plan 06, `8613bdf2`) | RED: *"`bridge` stopped retrying at production configuration… expected "vi.fn()" to be called 2 times, but got 1 times"*. ⚠️ **The case ALREADY EXISTED** (the plan-04-era `SC2 — findReplacementCandidates` test) and already reddened — but it reddened by THROWING `Analytics service is not reachable`, naming neither the registry nor the retry. Upgraded in place with a settle-capture so the COUNT reddens first; **not** duplicated into a twin. |
| SC-P | `process_key.py:1438`: drop `.eq("status","draft")` | a genuinely new resync (with a COMPLETED prior verification) must still queue, not answer `WIZARD_DUPLICATE` | ✅ **OBSERVED — twice, incl. under adversarial collection order** (plan 03, `4a81f614`) | D-14c. **OQ-2 DECIDED (plan 03): test files are outside the D-03 zero-Python fence**, so the pin lives in `analytics-service/tests/test_process_key.py`, the Python home this row names — `git diff --stat` = 1 test file, 149 insertions, zero production Python. Mutation applied → RED, and the failure payload **is** the user-facing defect verbatim: *"a resync whose ONLY prior verification is status='completed' is a genuinely NEW sync, not a seam retry — it must queue, not be refused as a duplicate; got {'ok': True, 'code': 'WIZARD_DUPLICATE', 'idempotent': True, … 'queued': False, 'job_state': 'not_applicable'}"* — `2 failed, 7 passed, 102 deselected`; filter restored → `9 passed`. Re-observed with `tests/test_worker_isolation_e2e.py` collected FIRST, ruling out a vacuous pass from `test_process_key.py`'s `sys.modules.pop` re-import (`2 failed, 11 passed`). The positive control (byte-identical except the seeded `status`) correctly stays **GREEN** under the mutation — that asymmetry is what makes the pair a controlled experiment rather than two copies of one assertion. |

*Rules:*
- **Observed means run.** "The test covers it" is not evidence. Paste the failing assertion.
- **A mutation that is skipped** is recorded as skipped, **never as caught**.
- **Prefer the second member of a class.** SC-F exists precisely because 141 fixed the instance
  and not the class.

---

## Oracle Independence

> The failure this catches: assertions that read their expected value out of the module under test.
> This repo has a documented history of exactly this failure mode on money math — three bugs
> survived six review passes behind self-referential oracles.

- [x] No test imports a **constant** from the module it tests — expected values are **literals** in the test
      — *Evidence: every pin's EXPECTED side is hand-typed (the module import is the pin's **subject**, not
      its oracle). Plan 02 measured `Object.keys(RETRY_SAFE…)` on the EXPECTED side of the new pins = **0**.*
      **⚠️ One documented exception:** `expect(Math.ceil(BREAKER_FAILURE_THRESHOLD / 2)).toBe(3)`
      (`seam-constants.pin.test.ts:569`) takes its INPUT from the module under test and sits directly below
      `expect(THRESHOLD).toBe(5)`, so it adds no discrimination — 141.1-01 measured the threshold mutation
      reddening it together with two independent assertions. Retained for its failure message, **not**
      counted as independent coverage.
- [x] No assertion compares a value to itself via a re-export, fixture, or table under test
      — *Evidence: plan 04's OQ-3 agreement pin asserts the row against the REGISTRY (two different sources)
      and was proven to catch drift from the registry side, which the same-source `it.each` structurally cannot.*
- [x] Table/registry sizes are pinned to a **literal count**, not to `len(THE_TABLE)`
      *(SC-J is exactly this: `.toBe(4)` / `.toBe(2)` / `.toBe(2)` / `.toBe(5)`, hand-typed)*
      — *Also measured: `SEAM_EXCLUSIONS` `.toBe(3)`, the citation roster `.toBe(35)`, the 13-evidence-string
      fence (`expected 12 to be 13`), the census's `>= 14` binding floor. All hand-typed, all observed RED.*
- [x] SC-L's 56 000 ms is a **hand-typed literal**, not re-derived from the formula under test
      — *Evidence: the oracle was added BEFORE the arithmetic was corrected and reddened at 43 250; the
      arithmetic was extracted into `branchWorstCases` so the literal exercises the real path without
      recomputing it. Plan 07 independently re-derived 30 500 + 25 500 = 56 000 and 150 000 + 127 500 = 277 500.*
- [x] SC-A's disagreement fixture puts row and registry in **conflict** — a test that reads both
      from the same source proves nothing (this is exactly why 558 tests stayed green)
      — *Evidence: the fixture forces `SEAM_BUDGETS["process-key-sync"].retries = 1` against a registry that
      still says no, and **asserts the pre-state is 0** so a future row flip cannot make the conflict vacuous.*
- [x] Any fake/double is pinned against the real contract it stands in for
      — *Scope: the doubles this phase's own tests depend on, enumerated — (i) the breaker double's
      `FAKE_THRESHOLD equals BREAKER_FAILURE_THRESHOLD` drift guard, observed RED in plan 01; (ii)
      `statusSequenceFetch`'s header-less shape, pinned in-comment as the capability-guard anti-regression;
      (iii) plan 03's STATEFUL supabase double, chosen over the scripted one because only a double honouring
      `.eq()` can exercise the filter, plus a positive control proving the seed reaches the pre-check;
      (iv) plan 06's `AbortSignal.timeout` spy, adopted only after MEASURING that vitest fake timers do not
      drive the real primitive, then verified at 999 / 1000 ms; (v) plan 08's index-simulating Sentry double,
      built because the pre-existing URL-blind doubles stayed green against a permanently dead query.
      **Not an exhaustive audit of every double in the repository** — three of the five above were found
      un-pinned during this phase and pinned as part of it.*

**Deliberate self-reference:** SC-L. Its mutation edits `seam-budgets.invariant.test.ts` itself —
SC-4b's arithmetic is test-resident by design, so the "production source" being mutated IS the
test file. Independent cover: the `56_000` oracle is a hand-typed literal (never derived from the
formula under test), and plan 07 T3's anti-shortcut mutation (`failing` 3→6 ⇒ the hand-typed
`277_500` assertion REDs at 405_000) proves the arithmetic cannot be gamed from the constants
side. No other deliberate self-reference exists.

---

## Open Questions Owed at Plan Time

1. **✅ RESOLVED 2026-07-31 (revision 1) — the numerator IS structurally dead and has never
   fired.** (D-16 / SC-N) Read-only probe, org `metaworld-fund-ltd`, EU `de.sentry.io`,
   dataset `errors`, 90d:

   | Query (90d) | `count()` |
   |---|---|
   | `level:error path:/api/process-key correlation_id:* environment:production` (live query) | **0** |
   | `level:error correlation_id:* environment:production` | **681** |
   | `level:error environment:production` | **694** |
   | `path:/api/process-key` | **0** |
   | grouped by `path` | one bucket: `""` × 694 |
   | grouped by `transaction` | `/api/match/cron-recompute` 682, `""` 12 |

   Two independent causes: `src/instrumentation.ts` writes `path` into `extra` (unindexed),
   never `tags`; and the serving route is FastAPI `/process-key`, so the `/api` prefix is wrong
   regardless. "Retries suppress the alert" is moot AS STATED — the alert never fired. Plan 08
   is rewritten to the determined shape (no probe task, no branches).
2. **DECIDED at plan time (plan 03).** Test files are OUTSIDE the Python fence; the pin lives
   in `analytics-service/tests/test_process_key.py`. Original question: **Where does D-14c's
   behaviour pin live?** (SC-P) It tests a Python filter. If the D-03
   zero-Python fence is read to include *test* files, this case has no home. Recommended reading:
   the fence covers production Python, not tests. **Decide explicitly; do not let the case vanish.**

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or are listed under Manual-Only Verifications
      — all 21 task rows carry an automated command; 09-T1 / 09-T2 are additionally covered by the
      Manual-Only table, as their `grep+human` type requires.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — every task row has one.
- [x] Wave 0 covers all MISSING references *(N/A — no Wave 0 gaps)*
- [x] No watch-mode flags — every command is `vitest run` / `pytest` / `grep` / `diff`.
- [x] Feedback latency < 30s — per-task sampling was per-file throughout (2–3 files, 49–200 tests).
      **⚠️ Two named exceptions, both deliberate:** `npm run typecheck`, which the Test Infrastructure table
      itself flags as load-bearing for D-08 and D-11, and the voluntary full-suite sweeps (10 420 / 10 442 /
      10 440 / 10 729 tests) that plans 04, 06 and 07 ran because a required field on a widely-imported type
      and edits to the shared classification path have blast radius outside the file being edited.
- [x] **Every success criterion has a Falsifiability Ledger row** — SC-A … SC-P plus the four primes
      (SC-C′, SC-G′, SC-H′, SC-M′) = **20 rows**, one per criterion.
- [x] **Every ledger row is `Observed ✅` with pasted evidence, or explicitly marked skipped-with-reason**
      — 20/20 observed with the executor's own quoted assertion text and counts. **Three rows carry an
      explicit skipped/limited note rather than a bare tick**, per the "never as caught" rule: **SC-C′**
      (green-polarity oracle; the mutation that would redden it was not run), **SC-G′** (3 of the 5 named
      anchors mutated; `cleanup_abandoned_wizard_drafts` and the constraint-name anchor un-mutated), and
      **SC-K** (`SEAM_RETRY_BACKOFF_MS` mutated, `SEAM_RETRY_JITTER_MAX_MS`'s own literal pin un-mutated).
      **SC-E** is recorded as observed-and-re-scoped: its compile half reddens in both directions, while the
      row's literal runtime mutation is unreachable dead code post-D-08 and the axis is instead closed at
      three independently observed points.
- [x] **SC-A and SC-B specifically: the measured green counts (558, 125) must become RED and the
      failing assertion pasted.** These two are the phase's reason for existing.
      — Both done in plan 06: SC-A *"The anonymous teaser RETRIED… expected 1 times, but got 2 times"*;
      SC-B **was 125 green, now 2 RED**. SC-D's measured 21/21-green fact is likewise dead.
- [x] **Oracle Independence checklist complete** — all six ticked above, two with their scope stated
      (the non-discriminating derived threshold pin; the double audit being phase-scoped, not repo-wide).
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** **CLOSED 2026-07-31** — Falsifiability Ledger 20/20 observed, transcribed from the nine
plan SUMMARYs at `22332e34`. **Three residuals are recorded rather than resolved**, and none of them is a
ledger row left on faith: the un-run SC-C′ discriminating mutation, the two un-mutated pins named under
SC-G′ / SC-K, and SC-N's derived-not-observed `transaction:/process-key` scoping term (the repaired
numerator CAN fire; it is not proven to have fired). Ledger entries were transcribed from recorded
evidence only — no row was marked observed because a plan claimed it or because the suite is green.
