---
phase: 141.1
slug: seambackoff-retry-after-aware-backoff-breaker-recalibration
status: draft
nyquist_compliant: false
wave_0_complete: true
created: 2026-07-31
---

# Phase 141.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **The full decision→mutation map lives in `141.1-RESEARCH.md` § Validation Architecture.**
> This file is the executable contract; that section is its derivation. Every row below
> traces to a `D-NN` row there.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (`vitest.config.ts` at repo root, coverage via `@vitest/coverage-v8`) |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npx vitest run src/lib/<file>.test.ts` |
| **Full suite command** | `npm run test` |
| **Typecheck** | `npm run typecheck` — **load-bearing for D-08 and D-11**, not optional here |
| **Lint** | `npm run lint` |
| **SQL gates** | `psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_*.sql` (CI `sql-tests` job auto-discovers) |
| **Estimated runtime** | ~30 s per file; full TS suite several minutes |

**Flake note:** local vitest flakes → re-run with `--no-file-parallelism` before treating a
failure as real.

---

## Sampling Rate

- **After every task commit:** `npx vitest run src/lib/<affected file>.test.ts` (< 30 s)
- **After every plan wave:** `npm run typecheck && npm run lint && npx vitest run src/lib/`
- **Before `/gsd:verify-work`:** `npm run test` full suite green + typecheck + lint
- **Max feedback latency:** 30 seconds per task

---

## Per-Task Verification Map

> Filled at plan time by `gsd-planner` — one row per task, keyed to the `D-NN` decision it
> discharges. Task IDs follow `141.1-<plan>-<task>`.

| Task ID | Plan | Wave | Decision | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| *(planner fills)* | | | D-01…D-20 | T-141.1-01 (D-17 credential leak) | | unit / types / invariant | | ✅ | ⬜ pending |

---

## Wave 0 Requirements

**None — existing infrastructure covers every decision in this phase.**

All test files already exist: `src/lib/resilient-fetch.retry.test.ts`, `resilient-fetch.test.ts`,
`resilient-fetch.wiring.test.ts`, `seam-constants.pin.test.ts`, `seam-budgets.invariant.test.ts`,
`seam-citations.invariant.test.ts`, `seam-retry-registry.test.ts`,
`tests/integration/cron-flag-monitor.test.ts`, `supabase/tests/test_resync_retry_single_job.sql`.

The harness is complete: `fakeRedisFor`, `seedBreakerOpen`, `shared.mode.staleReadOnce`
(**already built and currently unused — it is D-13's tool**), fake timers, `vi.spyOn`-based
fetch mocking.

One **new non-test** file: `docs/runbooks/seam-breaker.md` (D-18).

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| SQL fixture's transaction claim is corrected without altering any assertion | D-07 | Comment-only edit inside a `.sql` fixture; no runner asserts on comments | `git diff --stat supabase/tests/test_resync_retry_single_job.sql` shows comment lines only; re-read `:40` against the actual `BEGIN;` position |
| Runbook is accurate and reaches on-call | D-18 | Prose; correctness is a human judgement about mental models | Read `docs/runbooks/seam-breaker.md`: must state fail-open semantics, the Upstash key shapes, `seam.breaker.open`, up-to-2-attempts, the D-01 `Retry-After` fail-fast rule, and the D-03 doubled-compute cost. Must **not** be confusable with `compute-queue.md:160`'s different (Python per-API-key 429) breaker |
| `TODOS.md` obligations carry an explicit verdict | D-19 | Ledger bookkeeping | A `Phase 141 / 141.1 deferrals` heading exists; G1, G2, G3, G4 each carry `DISCHARGED` or `RE-BOOKED`; all of bucket H1–H7 is recorded. **G5 is already discharged (`2e36016d`) — no task** |
| VERSION / package.json / CHANGELOG move in lockstep | D-20 | Release step, verified at ship | `diff <(cat VERSION) <(node -p "require('./package.json').version")` prints nothing; CHANGELOG head matches; **all three in ONE commit** |

---

## Falsifiability Ledger

> **Coverage answers "is it verified?". This section answers "CAN the verification FAIL?"**
>
> This phase exists partly *because* two production-code deletions left the suite green.
> Treat every row here as owed evidence, not as a formality.

**The two headline mutations — these are measured facts about the current suite, not hypotheses:**

| SC | Mutation (exact edit to production source) | Must turn RED | Observed? | Evidence |
|----|-------------------------------------------|---------------|-----------|----------|
| SC-A | `process-key-client.ts:453` + `analytics-client.ts:434`: delete BOTH `retriesOverride` lines | new disagreement test in `resilient-fetch.retry.test.ts` (force `SEAM_BUDGETS["process-key-sync"].retries = 1`, `flow_type: "teaser"`, persistent transient ⇒ assert **1** fetch) | ⬜ pending | **Today: 558 tests green across 11 files.** Paste the failing assertion after D-10. |
| SC-B | `resilient-fetch.ts:2041`: hoist `const deadline = AbortSignal.timeout(timeoutMs)` above the `for` loop | `expect(calls[0][1].signal).not.toBe(calls[1][1].signal)` in `resilient-fetch.retry.test.ts` | ⬜ pending | **Today: 125 tests green.** Paste the failing assertion after D-12. |
| SC-C | `resilient-fetch.ts:2160`: delete the `&& !hasContractualWait(res)` conjunct | D-01 test: a 503 carrying `Retry-After` must yield **1** fetch, must return attempt 1's body (`dependency` present), and must record **1** breaker failure — not 2 | ⬜ pending | |
| SC-C′ | *(anti-regression, opposite direction)* leave D-01 in and remove the header from the fixture | a 503 **without** `Retry-After` must still yield **2** fetches | ⬜ pending | Pins that D-01 did not disable the whole 503 retry arm |
| SC-D | `resilient-fetch.ts:2019`/`:2025`: swap `sleep(...)` and the pre-attempt-2 `isBreakerOpen` re-check | D-13 test via `shared.mode.staleReadOnce` (first read CLOSED, post-sleep read OPEN ⇒ `CircuitOpenError`, **1** fetch) | ⬜ pending | **Today: 21/21 green.** |
| SC-E | `resilient-fetch.ts`: re-add the `?? SEAM_BUDGETS[budgetKey].retries` fallback | `npm run typecheck` rejects the `@ts-expect-error` fixture calling `resilientFetch("process-key-enqueue", …)` with no `retriesOverride`; runtime half reds SC-A's test | ⬜ pending | D-08 — the axis must close **by construction** |
| SC-F | `analytics-client.ts:434`: delete `retriesOverride`, **or** add a 6th `resilientFetch` call site with none | `resilient-fetch.wiring.test.ts` census set-equality reds with `<ABSENT>` | ⬜ pending | D-09 — **extend `EXPECTED_BINDINGS`, do not invent a parallel mechanism.** This is the instance-not-class finding |
| SC-G | `seam-retry-registry.ts`: re-introduce `"no persisted server-side write"` into any analytics YES evidence other than `optimize-weights` | banned-string assertion in `seam-retry-registry.test.ts` | ⬜ pending | D-04 — the exact false claim, banned by construction |
| SC-G′ | `seam-retry-registry.ts`: delete the 3-indexed-status qualification from `resync.evidence`, or the `isUuid` / `cleanup_abandoned_wizard_drafts` / constraint-name anchors from `onboard.evidence` | content assertions in `seam-retry-registry.test.ts` | ⬜ pending | D-05 |
| SC-H | plant `"foo.ts:12"` inside an `evidence:` string in `seam-retry-registry.ts` | the **new registry-local** evidence guard | ⬜ pending | D-06. ⚠️ **Adding the file to `SEAM_CITATION_SURFACE` alone is vacuous** — `extractComments` excludes string literals by design (self-test at `seam-citations.invariant.test.ts:369-375`). Prove **both** polarities: planted rot caught, symbol anchor not caught |
| SC-H′ | plant a bare `// see foo.ts:12` **comment** in `seam-retry-registry.ts` | the `SEAM_CITATION_SURFACE` roster `it.each` | ⬜ pending | D-06 roster half; costs the `.toBe(34)` → `35` pin bump at `:263` |
| SC-I | `seam-retry-registry.ts`: `RETRY_SAFE_FLOW_TYPES.teaser = {retries:1, evidence:""}` in a `@ts-expect-error` fixture; and add a 5th member to the `FlowType` union | must not compile (`npm run typecheck`); `expect(Object.isFrozen(...)).toBe(true)` at runtime; `budgetKeyFor`'s `never`-defaulted switch reds **without** editing the hand-typed list | ⬜ pending | D-11 — exhaustiveness must be *real*, not two hand-typed lists agreeing |
| SC-J | empty `RETRY_SAFE_ANALYTICS` | `expect(Object.keys(RETRY_SAFE_ANALYTICS).length).toBe(4)` | ⬜ pending | D-14a — today `it.each(Object.keys(...))` at `seam-constants.pin.test.ts:337` yields **zero cases and stays green** |
| SC-K | change `SEAM_RETRY_BACKOFF_MS` (250) or `SEAM_RETRY_JITTER_MAX_MS` (250) | literal pins in `seam-constants.pin.test.ts` | ⬜ pending | D-14b — neither has a literal pin today, unlike every sibling constant |
| SC-L | `seam-budgets.invariant.test.ts`: delete the `(1 + retries)` factor from the `storeMs` term | hand-typed worst-case assertion: `keys/sync` in the `failing` state is exactly **56 000 ms** | ⬜ pending | D-15. ⚠️ `STORE_COMMANDS_PER_SEAM_CALL.failing` **stays 3** with a `(1 + retries)` multiplier — a flat 6 false-REDs `finalize-wizard` composite at 405 000 ms vs a 300 000 ms ceiling |
| SC-M | `resilient-fetch.ts`: delete the new counting-5xx `console.error` | D-17a spy assertion (logs once, names budget key + status) | ⬜ pending | |
| SC-M′ | *(negative, security-critical)* make the new log line interpolate `requestInit` | D-17b assertion: logged string contains **neither** the request body **nor** any header value (`INTERNAL_API_TOKEN`, `X-Service-Key`, `X-User-Access-Token`) | ⬜ pending | Highest-probability security defect in this phase — `resilient-fetch.ts:2099-2103` records that omitting `scrubSeamError`'s second argument once shipped a live end-user Supabase JWT to the logs |
| SC-N | `cron/flag-monitor/route.ts`: revert the `path:` filter | `expect(url).not.toContain("path:/api/process-key")` in `tests/integration/cron-flag-monitor.test.ts` | ⬜ pending | D-16. **Check first whether the numerator has ever fired** — see Open Question 1 |
| SC-O | `SEAM_BUDGETS.bridge.retries` → 0 (pre-D-08) / delete the registry entry (post-D-08) | new `bridge`-at-production-configuration case in `resilient-fetch.retry.test.ts`, written through `findReplacementCandidates` | ⬜ pending | D-14d |
| SC-P | `process_key.py:1438`: drop `.eq("status","draft")` | a genuinely new resync (with a COMPLETED prior verification) must still queue, not answer `WIZARD_DUPLICATE` | ⬜ pending | D-14c. ⚠️ **Home undecided** — Python test vs SQL gate. See Open Question 2. Do **not** silently drop this case |

*Rules:*
- **Observed means run.** "The test covers it" is not evidence. Paste the failing assertion.
- **A mutation that is skipped** is recorded as skipped, **never as caught**.
- **Prefer the second member of a class.** SC-F exists precisely because 141 fixed the instance
  and not the class.

---

## Oracle Independence

> The failure this catches: assertions that read their expected value out of the module under test.
> This repo has a documented history of exactly this failure mode on money math — three bugs
> survived six review passes behind self-referential oracles.

- [ ] No test imports a **constant** from the module it tests — expected values are **literals** in the test
- [ ] No assertion compares a value to itself via a re-export, fixture, or table under test
- [ ] Table/registry sizes are pinned to a **literal count**, not to `len(THE_TABLE)`
      *(SC-J is exactly this: `.toBe(4)` / `.toBe(2)` / `.toBe(2)` / `.toBe(5)`, hand-typed)*
- [ ] SC-L's 56 000 ms is a **hand-typed literal**, not re-derived from the formula under test
- [ ] SC-A's disagreement fixture puts row and registry in **conflict** — a test that reads both
      from the same source proves nothing (this is exactly why 558 tests stayed green)
- [ ] Any fake/double is pinned against the real contract it stands in for

**Deliberate self-reference:** none. If one is introduced, name it here and say what independently
covers it.

---

## Open Questions Owed at Plan Time

1. **Is the flag-monitor numerator structurally dead?** (D-16 / SC-N) No `src/app/api/process-key`
   route exists and nothing found sets a Sentry `path` tag. If the numerator has never fired,
   "retries suppress the alert" is moot and D-16 becomes a larger, more valuable task.
   ~10-minute check via `scripts/probe-sentry-events-api.sh`. **Resolve in Wave 4 planning,
   not during execution.**
2. **Where does D-14c's behaviour pin live?** (SC-P) It tests a Python filter. If the D-03
   zero-Python fence is read to include *test* files, this case has no home. Recommended reading:
   the fence covers production Python, not tests. **Decide explicitly; do not let the case vanish.**

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or are listed under Manual-Only Verifications
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references *(N/A — no Wave 0 gaps)*
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] **Every success criterion has a Falsifiability Ledger row**
- [ ] **Every ledger row is `Observed ✅` with pasted evidence, or explicitly marked skipped-with-reason**
- [ ] **SC-A and SC-B specifically: the measured green counts (558, 125) must become RED and the
      failing assertion pasted.** These two are the phase's reason for existing.
- [ ] **Oracle Independence checklist complete**
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
