---
phase: 141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resilient-fetch.ts
  - src/lib/resilient-fetch.retry.test.ts
  - src/lib/seam-constants.pin.test.ts
autonomous: true
requirements: [SEAM-06]
tags: [seam-retry, retry-after, breaker-threshold, resilient-fetch]

must_haves:
  truths:
    - "D-01: a retry-eligible 503 carrying a Retry-After header is surfaced immediately (1 fetch, body intact with `dependency`), records exactly 1 breaker failure, and does not sleep"
    - "D-01: a 503 WITHOUT Retry-After still retries (2 fetches) — the fail-fast did not disable the 503 retry arm"
    - "D-01: the jittered transport/timeout backoff class is byte-unchanged"
    - "D-02: BREAKER_FAILURE_THRESHOLD stays the literal 5, and the per-attempt-counting tradeoff (trip in ceil(5/2)=3 user requests vs 5 pre-141) is recorded in the docblock AND asserted in a test"
  artifacts:
    - path: "src/lib/resilient-fetch.ts"
      provides: "hasContractualWait helper (capability-guarded, 503-only) + the one-conjunct fail-fast at the counting-status arm + the ratified BREAKER_FAILURE_THRESHOLD docblock"
    - path: "src/lib/resilient-fetch.retry.test.ts"
      provides: "SC-C and SC-C' cases (fail-fast + anti-regression) with a real-Response Retry-After fixture"
    - path: "src/lib/seam-constants.pin.test.ts"
      provides: "extended threshold pin message + the derived Math.ceil(BREAKER_FAILURE_THRESHOLD / 2) === 3 assertion"
  key_links:
    - from: "src/lib/resilient-fetch.ts counting-status arm"
      to: "hasContractualWait"
      via: "the `!lastAttempt && !hasContractualWait(res)` conjunct"
      pattern: "hasContractualWait"
    - from: "src/lib/resilient-fetch.retry.test.ts"
      to: "shared.counters.limitCalls"
      via: "breaker-failure oracle for the 'records 1, not 2' half of SC-C"
      pattern: "limitCalls"
---

<objective>
Consume the design input 140.5 built and 141 never used: when a retry-eligible 503 carries the
upstream's contractual `Retry-After` (mandatory on every SERVICE-TRANSIENT 503, 15s supabase /
30s mt5-gateway), fail fast instead of retrying in ~375ms into a near-certain second failure.
Ratify `BREAKER_FAILURE_THRESHOLD = 5` as an intentional decision under per-attempt counting.

Purpose: discharges D-01 (bucket A1) and D-02 (bucket A2) from the 141 review campaign.
Output: the fail-fast conjunct + helper in `resilient-fetch.ts`, SC-C/SC-C' regression tests,
the ratified-tradeoff docblock, and the derived-arithmetic pin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-CONTEXT.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-RESEARCH.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-PATTERNS.md

⚠️ Line coordinates in RESEARCH/PATTERNS are valid against tree `03e28d20`. If the file has
moved, re-locate by SYMBOL (function/constant name), never by trusting a stale line number.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-01 — Retry-After fail-fast (SC-C, SC-C′)</name>
  <files>src/lib/resilient-fetch.ts, src/lib/resilient-fetch.retry.test.ts</files>
  <read_first>
    - src/lib/resilient-fetch.ts — the retry loop (~:1999-2184), the counting-status arm (~:2154-2163), and `readDependencyBody` (~:1766-1808) whose docblock is the capability-guard contract to copy
    - src/lib/resilient-fetch.retry.test.ts — `statusSequenceFetch` (~:209-219, returns bare `{ ok, status } as Response` with NO `headers`), `jsonResponse` (~:237-242), `shared.counters.limitCalls` (~:44-48), the existing 503-retry test (~:338-350)
    - 141.1-RESEARCH.md § "D-01 — the exact insertion point" and § "Code Examples / D-01" — the verified helper shape and conjunct
    - analytics-service/services/error_contract.py:88-152 — the contract being consumed (READ ONLY; zero Python changes)
  </read_first>
  <behavior>
    - Test SC-C: a counting 503 whose Response is a REAL `Response` with header `Retry-After: 15` and JSON body `{ dependency: "supabase", human_message: "..." }`, called with `retriesOverride: 1` → exactly 1 fetch; the resolved SeamResponse body still carries `dependency`; `shared.counters.limitCalls` === 1 (one breaker failure recorded, not two); no sleep occurred (use `vi.useFakeTimers` or assert no pending timer — the call resolves without advancing timers).
    - Test SC-C′ (anti-regression): a 503 WITHOUT `Retry-After` (the existing bare `statusSequenceFetch(503, 200)` double) → still 2 fetches. The existing test at ~:338-350 already pins this; keep it green and add an explicit sibling if it does not already assert fetch count 2.
    - Test (503-only narrowing): a counting non-503 status (pick one that `seamBreakerVerdict` counts, e.g. 502/504 — read `seamBreakerVerdict` to confirm) carrying a stray `Retry-After` header → still retries (2 fetches).
  </behavior>
  <action>
    Add a `hasContractualWait(res: Response): boolean` helper sited beside `readDependencyBody`:
    returns false when `res.status !== 503`; returns false when `typeof res.headers?.get !== "function"`
    (the repo's fixtures are bare `{ ok, status }` literals — a throw here lands inside the
    classification window and replaces the real upstream error with a bookkeeping one, per the
    `readDependencyBody` docblock); otherwise returns `res.headers.get("Retry-After") !== null`.
    Give it a docblock stating the capability-guard reason and the 503-only rule (mirrors the
    Python contract, which mandates the header only on 503). Comment hygiene: `resilient-fetch.ts` is on `SEAM_CITATION_SURFACE` — every new or
    rewritten comment uses SYMBOL ANCHORS, never `file.ext:NN`; do not copy the line
    coordinates from this plan's prose into code comments.

    In the counting-status arm, change the retry guard from `if (!lastAttempt)` to
    `if (!lastAttempt && !hasContractualWait(res))` — nothing else. Falling through reaches the
    existing `return instrumentBody(res, ...)`, which surfaces attempt 1's response with body
    intact. `recordOnce` stays ABOVE the guard (attempt 1's failure IS recorded once — the 503
    happened; "no second breaker failure" is satisfied by not looping). Add the Phase 141.1 / D-01
    comment from RESEARCH § Code Examples (honour the upstream's contract; the jittered backoff
    for the transport/timeout class is UNCHANGED; 429 never reaches this arm — `counts: false`).

    DO NOT touch the transport-catch arm (~:2076-2129) — no Response exists there to carry a
    header, and it is the dominant Railway-blip class whose backoff D-01 explicitly preserves.

    Tests go in `resilient-fetch.retry.test.ts` using the existing harness (`vi.spyOn`-based fetch
    mock; `afterEach` teardown already calls `unstubAllGlobals` + `restoreAllMocks` — never a leaked
    `vi.stubGlobal`, the repo's CI-only Node-22 failure). Build the SC-C fixture from `jsonResponse`
    (or a sibling) extended to attach the `Retry-After` header. Fetch-count expectations and the
    `limitCalls` count are hand-typed literals (oracle independence).
  </action>
  <verify>
    <automated>npx vitest run src/lib/resilient-fetch.retry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-C): delete the `&& !hasContractualWait(res)` conjunct in `resilient-fetch.ts`, run `npx vitest run src/lib/resilient-fetch.retry.test.ts` → the SC-C case is RED (2 fetches / limitCalls 2); revert → GREEN. Paste the failing assertion.
    - MUTATION (503-only): delete the `res.status !== 503` early-return inside `hasContractualWait` → the counting-non-503-with-stray-header case is RED (1 fetch instead of 2); revert → GREEN.
    - SC-C′: with D-01 in place, `statusSequenceFetch(503, 200)` still yields 2 fetches — the existing ~:338-350 test stays GREEN (this also proves the capability guard: that double has no `headers` and must degrade to `false`, not throw).
    - The full file passes: `npx vitest run src/lib/resilient-fetch.retry.test.ts` exit 0.
    - `grep -n "hasContractualWait" src/lib/resilient-fetch.ts` shows exactly 2 hits: the declaration and the one conjunct.
    - Zero changes under `analytics-service/` (`git diff --stat` shows no .py files).
  </acceptance_criteria>
  <done>A 503-with-Retry-After fails fast with body intact and 1 recorded breaker failure; a 503 without it still retries; both directions mutation-proven RED/GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: D-02 — ratify BREAKER_FAILURE_THRESHOLD under per-attempt counting</name>
  <files>src/lib/resilient-fetch.ts, src/lib/seam-constants.pin.test.ts</files>
  <read_first>
    - src/lib/resilient-fetch.ts — `BREAKER_FAILURE_THRESHOLD` docblock (~:143-149), the house-form analog `BREAKER_COOLDOWN_S` docblock (~:158-191, direction + measured table + explicit ACCEPTED), the per-attempt latch reset (`recorded = false`, ~:2034, documented ~:1982-1990)
    - src/lib/seam-constants.pin.test.ts — the existing `BREAKER_FAILURE_THRESHOLD is the literal 5` pin (~:392-399) and the derived-assertion precedents at ~:442 (A-25) and ~:456 (A-14)
    - 141.1-PATTERNS.md § 5 — the exact house form and the arithmetic to record
  </read_first>
  <action>
    Rewrite the `BREAKER_FAILURE_THRESHOLD` docblock in the `BREAKER_COOLDOWN_S` house form for a
    RATIFIED tradeoff: state (a) the per-attempt latch means a doubly-failing retried request
    records 2 failures; (b) with threshold 5 that is ceil(5/2) = 3 user requests to trip, vs 5
    pre-141; (c) the trip gates all 15 routes including the anonymous teaser (the global
    `BREAKER_KEY` appended by `breakerKeysFor`); (d) this is ACCEPTED, decided 2026-07-31 —
    a Railway blip reaching 5 counted failures is a real outage and faster containment is the
    right direction. NO numeric change to `BREAKER_FAILURE_THRESHOLD`, `BREAKER_WINDOW`, or
    `BREAKER_COOLDOWN_S`. Comment hygiene: `resilient-fetch.ts` is on `SEAM_CITATION_SURFACE` — every new or
    rewritten comment uses SYMBOL ANCHORS, never `file.ext:NN`; do not copy the line
    coordinates from this plan's prose into code comments.

    In `seam-constants.pin.test.ts`: extend the existing pin's message with the per-attempt half
    (raising the threshold now also delays containment for retried traffic), and add a new sibling
    `it` asserting the derived arithmetic `Math.ceil(BREAKER_FAILURE_THRESHOLD / 2) === 3` (the
    right side is the hand-typed literal 3) with a message naming the retry: changing the
    threshold without revisiting the per-attempt-counting reasoning must redden HERE. Follow the
    A-25 / A-14 derived-assertion precedent for the message register (name the production meaning,
    name the remedy, forbid widening).
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-constants.pin.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION: change `BREAKER_FAILURE_THRESHOLD` to 4 → BOTH the literal pin AND the new derived assertion are RED (`Math.ceil(4/2) = 2 ≠ 3`); revert → GREEN. Paste one failing assertion.
    - `grep -c "per-attempt" src/lib/resilient-fetch.ts` ≥ 1 within the threshold docblock region (source assertion: the docblock names per-attempt counting).
    - The docblock contains the string "ACCEPTED" (or an equivalent explicit ratification word) and the "3 user requests" arithmetic.
    - `git diff src/lib/resilient-fetch.ts` for this task shows ONLY comment lines changed (no code) — the constant values are untouched.
  </acceptance_criteria>
  <done>The threshold is a decided number: docblock states the per-attempt consequence and acceptance; test pins both the literal and the derived trip arithmetic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel → Railway seam | untrusted upstream Response (status, headers, body) crosses into the classification window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.1-01 | DoS | retry loop / `hasContractualWait` | mitigate | capability-guard the header read (`typeof res.headers?.get !== "function"` → false) so a bare-literal fixture or malformed upstream can never throw inside the classification window and be miscounted as Railway degradation |
| T-141.1-02 | DoS (retry amplification) | counting-status arm | mitigate | D-01 removes the worst amplification case: retrying into a service that just said "wait 15–30s". `recordOnce` before the guard keeps the breaker's view honest |
| T-141.1-03 | Tampering | `Retry-After` header value | accept | the value is never parsed or slept on — only presence is tested; a hostile value cannot influence timing or logs |
| T-141.1-SC | Tampering | package installs | accept | zero package installs in this phase — no supply-chain surface |
</threat_model>

<verification>
- `npx vitest run src/lib/resilient-fetch.retry.test.ts src/lib/seam-constants.pin.test.ts` — all green
- `npm run typecheck` — 0 errors
- Both named mutations observed RED then reverted GREEN (evidence pasted in SUMMARY)
- `git diff --stat` shows zero `analytics-service/` files
</verification>

<success_criteria>
- D-01 fail-fast live with SC-C, SC-C′, and the 503-only narrowing all mutation-proven
- D-02 docblock + pin + derived assertion landed; no constant value changed
- Transport/timeout arm byte-identical (the dominant blip class keeps its jittered backoff)
</success_criteria>

<output>
Create `.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-01-SUMMARY.md` when done. Include the pasted RED evidence for both mutations.
</output>
