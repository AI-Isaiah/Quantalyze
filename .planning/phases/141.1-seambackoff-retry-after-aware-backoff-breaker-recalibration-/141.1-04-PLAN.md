---
phase: 141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration
plan: 04
type: execute
wave: 2
depends_on: ["141.1-01"]
files_modified:
  - src/lib/resilient-fetch.ts
  - src/app/api/keys/[id]/permissions/route.ts
  - src/app/api/keys/validate-and-encrypt/route.ts
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/lib/resilient-fetch.retry.test.ts
  - src/lib/resilient-fetch.test.ts
  - src/lib/seam-constants.pin.test.ts
  - supabase/tests/test_resync_retry_single_job.sql
  - src/lib/seam-breaker-failopen.regression.test.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
  - src/app/api/keys/[id]/permissions/route.test.ts
autonomous: true
requirements: [SEAM-06]
tags: [seam-retry, retriesOverride, registry-bypass, type-safety, prose-truth]

must_haves:
  truths:
    - "D-08: `retriesOverride` is a REQUIRED `0 | 1` on ResilientFetchInit and the `?? SEAM_BUDGETS[budgetKey].retries` fallback is GONE — a new call site cannot compile without a verdict; the registry-bypass axis is closed by construction"
    - "D-08: the runtime 0|1 validator survives (JS callers / `as any` still throw SeamConfigError ABOVE the classification window) and SEAM_BUDGETS[*].retries survives as a pure accounting declaration with a rewritten docblock"
    - "D-07: no comment in resilient-fetch.ts claims every SEAM_BUDGETS row carries retries: 0; the process-key-enqueue row comment describes the post-D-08 world; the SQL fixture's transaction claim is corrected"
    - "D-07: a test contradicts the false prose by construction — the count of rows at retries === 1 is pinned to the hand-typed literal 5"
  artifacts:
    - path: "src/lib/resilient-fetch.ts"
      provides: "required `retriesOverride: 0 | 1`, dropped row fallback, kept runtime validator, rewritten row-field + override docblocks"
    - path: "src/app/api/keys/[id]/permissions/route.ts"
      provides: "explicit `retriesOverride: 0`"
    - path: "src/app/api/keys/validate-and-encrypt/route.ts"
      provides: "explicit `retriesOverride: 0`"
    - path: "src/app/api/strategies/finalize-wizard/route.ts"
      provides: "explicit `retriesOverride: 0`"
    - path: "src/lib/seam-constants.pin.test.ts"
      provides: "rows-at-1 === 5 derivability pin + post-D-08 framing of the registry↔rows consistency block"
  key_links:
    - from: "src/lib/resilient-fetch.ts"
      to: "ResilientFetchInit.retriesOverride"
      via: "`const retries = retriesOverride;` — no fallback"
      pattern: "const retries = retriesOverride"
---

<objective>
Close the transport-boundary registry bypass (C1): today `retriesOverride?: number` falls back to
the budget row, so a hand-picked budget key with a hand-written flow_type inherits `retries: 1`
with the registry never consulted — and it typechecks. Mechanism A (founder left the choice to
the planner; RESEARCH verified A is the smaller diff and gains tsc enforcement): make the field a
required `0 | 1` and drop the row fallback. Then correct the four now-false prose sites in
`resilient-fetch.ts` and the SQL fixture's false transaction claim — written against the
post-D-08 world so they do not become false a second time.

Purpose: discharges D-08 (bucket C1) and D-07 (buckets B9, B10). SC-E.
Output: the closed axis + tsc-enforced call sites + true prose + the derivability pin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-CONTEXT.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-RESEARCH.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-PATTERNS.md
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-08 — required `retriesOverride: 0 | 1`, fallback dropped, blast radius closed (SC-E)</name>
  <files>src/lib/resilient-fetch.ts, src/app/api/keys/[id]/permissions/route.ts, src/app/api/keys/validate-and-encrypt/route.ts, src/app/api/strategies/finalize-wizard/route.ts, src/lib/resilient-fetch.retry.test.ts, src/lib/resilient-fetch.test.ts, src/lib/seam-constants.pin.test.ts</files>
  <read_first>
    - src/lib/resilient-fetch.ts — `ResilientFetchInit` (~:1469-1495, the `timeoutMsOverride` analog directly above), the runtime validator (~:1904-1918) and `SeamConfigError`'s reason for existing (~:1497-1511: config faults must never be counted as Railway degradation), the fallback line (~:1921), the SEAM_BUDGETS `retries` field docblock (~:457)
    - 141.1-RESEARCH.md § "D-08 — the 5 call sites and the two mechanisms" — the exact diff, the 5 production sites, and the two ⚠️ preservation rules
    - src/lib/resilient-fetch.retry.test.ts — the row-default case (~:367-385) whose premise DIES with this change
    - src/lib/seam-constants.pin.test.ts — the `registry ↔ rows consistency` describe (~:332-374)
  </read_first>
  <action>
    In `resilient-fetch.ts`:
    - `ResilientFetchInit`: change `retriesOverride?: number` to required `retriesOverride: 0 | 1`.
    - Replace `const retries = retriesOverride ?? SEAM_BUDGETS[budgetKey].retries;` with
      `const retries = retriesOverride;`.
    - KEEP the runtime validator (integer 0|1 → `SeamConfigError`) — `0 | 1` is compile-time only;
      a JS caller or an `as any` still needs the throw. Drop only its now-dead
      `retriesOverride !== undefined` outer guard. The validation MUST stay above the
      classification window (moving it below would let five config typos open the global circuit —
      SeamConfigError's entire reason for existing).
    - DO NOT delete `SEAM_BUDGETS[*].retries` — SC-4b arithmetic and `EXPECTED_RETRIES` read it.
      Rewrite the field's docblock (~:457) in the same commit: the row is now a pure ACCOUNTING
      declaration read by the SC-4b headroom arithmetic; the required override (fed by the
      registry at the two client chokepoints) is the ONLY thing that turns retry on.
      Comment hygiene: `resilient-fetch.ts` is on `SEAM_CITATION_SURFACE` — every new or
      rewritten comment uses SYMBOL ANCHORS, never `file.ext:NN`; do not copy the line
      coordinates from this plan's prose into code comments.

    Production call sites: the two client chokepoints already pass the field (unchanged); the 3
    override-free route sites gain explicit `retriesOverride: 0` — `keys/[id]/permissions/route.ts`
    (~:231), `keys/validate-and-encrypt/route.ts` (~:221), `strategies/finalize-wizard/route.ts`
    (~:174).

    Test blast radius: run `npm run typecheck` FIRST — the error list IS the task list. Every
    `resilientFetch(` call in tests gains `retriesOverride: 0` (or `1` where the test exercises the
    retry). Known members beyond the retry/transport test files:
    `seam-breaker-failopen.regression.test.ts` makes 5 direct `resilientFetch("validate-key", ...)`
    calls (~:182, :208, :226, :252, :300), and two route tests pass an init through —
    `finalize-wizard/route.test.ts` (~:389) and `keys/[id]/permissions/route.test.ts` (~:131)
    (`actual.resilientFetch(budgetKey, path, init)`) — update their signatures/inits as tsc
    directs. The `validate-key` row-default case at retry.test.ts ~:367-385 is no longer expressible
    ("no override" cannot compile): rewrite it as an explicit `retriesOverride: 0` single-attempt
    pin with a comment that "production configuration" now means "the client passes the registry
    value" (the real production-config case lands in plan 06 through `findReplacementCandidates`).

    SC-E fixture: add a type-level fixture in `resilient-fetch.test.ts` — a `// @ts-expect-error`
    annotated `resilientFetch("process-key-enqueue", ...)` call with NO `retriesOverride`, with a
    comment naming C1 (this exact call shape used to inherit retries: 1 with the registry never
    consulted). If the field ever becomes optional again, the `@ts-expect-error` itself fails tsc.

    In `seam-constants.pin.test.ts`: reframe the `registry ↔ rows consistency` describe's comments
    for the post-D-08 world (row = accounting; registry = the gate) and add an assertion that the
    row value and registry value AGREE for all five flipped rows (hand-typed key list) — OQ-3's
    recommendation, so the accounting can never silently diverge from the gate.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/lib/resilient-fetch.retry.test.ts src/lib/resilient-fetch.test.ts src/lib/seam-constants.pin.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-E, type half): temporarily remove the `@ts-expect-error` comment from the fixture → `npm run typecheck` REDs on that line (the no-override call does not compile); restore → clean. Paste the tsc error.
    - MUTATION (SC-E, runtime half): re-add `?? SEAM_BUDGETS[budgetKey].retries` to the retries assignment → `npm run typecheck` still passes (the fallback is type-legal) BUT plan 06's SC-A disagreement test will red — record here that the runtime half is owed to plan 06, and verify locally that `grep -n "SEAM_BUDGETS\[budgetKey\].retries" src/lib/resilient-fetch.ts` returns 0 hits inside the retries assignment after revert.
    - `grep -n "retriesOverride: 0" src/app/api/keys/[id]/permissions/route.ts src/app/api/keys/validate-and-encrypt/route.ts src/app/api/strategies/finalize-wizard/route.ts` → 3 hits (one each).
    - `grep -c "retriesOverride?" src/lib/resilient-fetch.ts` → 0 (the field is required).
    - The runtime validator still throws: the existing SeamConfigError test for an invalid override value stays GREEN.
    - Full verify command exit 0 (typecheck + all three test files).
  </acceptance_criteria>
  <done>The axis is closed by construction: absence does not compile, the fallback is gone, the validator and the accounting row survive with true docblocks.</done>
</task>

<task type="auto">
  <name>Task 2: D-07 — correct the false prose + the derivability pin</name>
  <files>src/lib/resilient-fetch.ts, supabase/tests/test_resync_retry_single_job.sql, src/lib/seam-constants.pin.test.ts</files>
  <read_first>
    - 141.1-RESEARCH.md § "D-07 — the four false prose sites, verified" — current text, verdict, and the replacement framing for each of the 5 sites (including the exact suggested wording for the SQL `:40` correction)
    - src/lib/resilient-fetch.ts — the four sites: the `retriesOverride` docblock (~:1481-1487), the retries-assignment comment (~:1919-1920), the retries=0 loop comment (~:2001-2002), the `process-key-enqueue` row comment (~:553-558)
    - supabase/tests/test_resync_retry_single_job.sql — header (~:28-60): the claim at ~:40 vs the pre-clean DELETEs at ~:56 that precede `BEGIN;`
  </read_first>
  <action>
    Rewrite the four `resilient-fetch.ts` comments to describe the POST-D-08 truth (Task 1 landed
    first in this plan, so these cannot rot again immediately):
    - ~:1481-1487: the override is REQUIRED and is the only retry input; five rows carry
      `retries: 1` as accounting; both clients pass the registry verdict.
    - ~:1919-1920: the fallback sentence dies with the fallback — describe `const retries =
      retriesOverride;` (the caller's verdict, validated above the window).
    - ~:2001-2002: drop "every row today"; keep the true remainder (at retries=0 this runs exactly
      once).
    - ~:553-558: rewrite to name the belt's actual location — the row is what SC-4b's arithmetic
      reads (an honest accounting literal); retry activation is the required override fed by
      `RETRY_SAFE_FLOW_TYPES`/`RETRY_SAFE_ANALYTICS` at the two client chokepoints.

    Comment hygiene: `resilient-fetch.ts` is on `SEAM_CITATION_SURFACE` — every new or
    rewritten comment uses SYMBOL ANCHORS, never `file.ext:NN`; do not copy the line
    coordinates from this plan's prose into code comments.

    SQL fixture ~:40: replace the false "All fixture work runs inside BEGIN...ROLLBACK" with the
    RESEARCH-verified correction: ASSERTION work runs inside BEGIN...ROLLBACK; the defensive
    pre-clean deliberately runs OUTSIDE it and commits (recovering a prior aborted run); both
    scopes touch only this file's literal fixture ids. COMMENT-ONLY edit — do not touch any
    assertion (shared test DB; the fixture ids scope every count).

    Derivability pin in `seam-constants.pin.test.ts`: add one assertion that the count of
    `SEAM_BUDGETS` rows with `retries === 1` is exactly the hand-typed literal 5, with a message
    saying prose claiming "every row is 0" is contradicted HERE by a test, not only by a reader.
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-constants.pin.test.ts && git diff --stat supabase/tests/test_resync_retry_single_job.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cn "retries: 0" src/lib/resilient-fetch.ts` — inspect each hit: none is a COMMENT claiming every/all rows carry 0 (source assertion; row literals for must-stay-0 rows are fine).
    - `grep -c "Plan 141-04 wires the callers" src/lib/resilient-fetch.ts` → 0 (the stalest forward-looking sentence is gone).
    - MUTATION (derivability): flip `SEAM_BUDGETS["bridge"].retries` to 0 → the new rows-at-1 === 5 pin is RED (as is the pre-existing EXPECTED_RETRIES pin — both directions); revert → GREEN.
    - `git diff supabase/tests/test_resync_retry_single_job.sql` shows ONLY comment lines (lines starting with `--`) changed; the file's assertion count is unchanged.
    - The corrected SQL header explicitly distinguishes the two scopes (grep the new header for "OUTSIDE").
  </acceptance_criteria>
  <done>All five false prose sites state the truth against the post-D-08 tree; a test now contradicts the "all rows 0" claim by construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new call site → transport core | a future caller's retry verdict must be forced at compile AND runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.1-08 | Elevation (unaudited retry) | `resilientFetch` transport boundary | mitigate | required `0 \| 1` + dropped fallback: a call site cannot acquire retry without an explicit verdict; runtime validator survives for JS/`as any` callers |
| T-141.1-09 | DoS (self-inflicted) | override validation placement | mitigate | validator stays ABOVE the classification window — a config typo throws SeamConfigError and is never recorded as Railway degradation |
| T-141.1-10 | Tampering (drift) | SEAM_BUDGETS.retries accounting row | mitigate | registry↔row agreement pin (hand-typed 5 keys) + rewritten docblock; deleting the row would red SC-4b and EXPECTED_RETRIES |
| T-141.1-SC | Tampering | package installs | accept | zero package installs in this phase |
</threat_model>

<verification>
- `npm run typecheck` — 0 errors at the final tree
- `npx vitest run src/lib/` — seam surface green
- SC-E type-half mutation observed (tsc error pasted); derivability mutation observed RED/GREEN
- SQL diff comment-only; zero `.py` files changed
</verification>

<success_criteria>
- Registry-bypass axis closed by construction (compile + runtime); all 5 production sites explicit
- All false prose corrected against the post-D-08 tree; SQL fixture claim true
- Rows-at-1 === 5 derivability pin live
</success_criteria>

<output>
Create `.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-04-SUMMARY.md` when done. Include the pasted tsc error for SC-E and the derivability RED evidence.
</output>
