---
phase: 141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration
plan: 07
subsystem: infra
tags: [census, wiring, anti-vacuity, sc4b, accounting, instance-not-class, seam-retry]

# Dependency graph
requires:
  - phase: 141.1-04
    provides: "`retriesOverride` as a REQUIRED `0 | 1` at every production call site — which is what makes the census's no-`<ABSENT>` assertion hold at the final tree; and the measured 78→77 case-shedding RED that D-14a's fence had to convert into a real failure"
  - phase: 141.1-05
    provides: "the repaired family-(ii) extractor in resilient-fetch.wiring.test.ts (`case \"x\":` labels no longer read as phantom budget keys) — preserved untouched by this plan"
  - phase: 141-seam-retry
    provides: "the retry axis itself: `retriesOverride`, the SEAM-06 registry, the five rows at `retries: 1`, and SEAM_RETRY_BACKOFF_MS / SEAM_RETRY_JITTER_MAX_MS"
  - phase: 140-seam
    provides: "the EXPECTED_BINDINGS census mechanism, SC-4b's headroom arithmetic, SEAM_EXCLUSIONS and its hand-typed roster"
provides:
  - "`DiscoveredBinding.retry` — the retry axis censused inside the EXISTING roster, so absence AND drift both redden below the type system"
  - "`retryExprFor` / `chokepointRetry` — bounded, sentinel-returning extractors whose every failure mode is a RED"
  - "the no-`<ABSENT>` assertion: the belt that survives an `as any`, a JS caller, or D-08's field being relaxed"
  - "hand-typed length fences on all 3 enumerated `it.each(Object.keys(` code sites"
  - "literal pins for SEAM_RETRY_BACKOFF_MS (250), SEAM_RETRY_JITTER_MAX_MS (250) and their sum (500)"
  - "SC-4b's per-leg `(1 + retries)` store term, with hand-typed 56 000 / 277 500 oracles fencing both wrong-fix directions"
affects: [141.1-09, seam-budgets-invariant, seam-constants-pin, resilient-fetch-wiring]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "close a second classification axis by adding a FIELD to the existing census, never a parallel census file — the encoder carries it, so the existing set-equality reddens on drift with no new assertion"
    - "bound a source-scan window by the NEXT occurrence of the construct being scanned, when the captured property can only legally appear inside that construct"
    - "every extractor failure mode returns a loud sentinel that no roster entry may hold, so the guard fails in the safe direction by construction"
    - "fence a vacuous `it.each(Object.keys(map))` with a hand-typed COUNT beside it, and close the CLASS by grep enumeration rather than fixing the named instance"
    - "extract the arithmetic under test into a named function so hand-typed oracles exercise the real code path without becoming self-referential"

key-files:
  created: []
  modified:
    - src/lib/resilient-fetch.wiring.test.ts
    - src/lib/seam-constants.pin.test.ts
    - src/lib/seam-budgets.invariant.test.ts

key-decisions:
  - "The retry expression for families (i) and (ii) is read from each client's SINGLE core chokepoint and attributed to every binding in the family — the same property `discoverBudgetKeyForArms` already relies on for its shared `path`. `chokepointRetry` returns a sentinel unless there is exactly one core call, so a second chokepoint is a RED rather than a half-truth silently attributed to nine bindings."
  - "The scan window runs from a core call to the NEXT core call. That is exact, not approximate: `retriesOverride` is a member of `ResilientFetchInit`, so after D-08 made it required it cannot legally appear anywhere but inside one of these inits."
  - "The SEAM_EXCLUSIONS fence is documented as NOT closing a live hole. Its two `it.each` sites were already covered by a hand-typed set equality that is strictly stronger than any count. Stated in the comment so nobody over-credits the line — the pin adds exactness over a `>= 3` floor plus co-location."
  - "`STORE_COMMANDS_PER_SEAM_CALL.failing` stays 3 and is documented PER ATTEMPT. Raising it to 6 is arithmetically true per retried call but double-charges non-retried legs; the 277 500 pin exists specifically to fence that."
  - "`branchWorstCases` / `worstBranch` were extracted as a pure refactor BEFORE the arithmetic changed, verified behaviour-preserving at 81 green, so the new oracles exercise the same code path as the ceiling assertion without recomputing the formula themselves."
  - "The `validate-and-encrypt` column was KEPT in the header table rather than swapped for keys/sync. Its figures are unchanged by D-15 (all legs `retries: 0`) and were verified; replacing it would have deleted information the plan did not ask to remove."

patterns-established:
  - "When a guard's own docblock names the property it protects ('would be discovered by nothing'), the new assertion's message quotes that phrase — the two halves of the guard cite each other rather than restating."
  - "A prose correction lands in the SAME commit as the arithmetic that falsified it, including forward-looking hypotheticals whose numbers the change moved."

requirements-completed: [SEAM-06]

# Metrics
duration: 40min
completed: 2026-07-31
---

# Phase 141.1 Plan 07: Retry-axis census, class-γ closure, SC-4b recharge Summary

**Phase 141 added a second classification axis at the seam call sites and shipped it with no census, a vacuous `it.each` that could only shed cases, two unpinned constants and a store term that never learned about retries — all four are the same instance-not-class shape, and all four are closed here by extending mechanisms that already existed rather than building new ones.**

## Performance

- **Duration:** ~40 min
- **Started:** 2026-07-31T21:25Z
- **Completed:** 2026-07-31T22:05Z
- **Tasks:** 3
- **Files modified:** 3 (zero created)

## Accomplishments

- **D-09 closed by a FIELD, not a file.** `DiscoveredBinding.retry` flows through `encodeBinding`, so the pre-existing set-equality reddens on retry drift with no new assertion — exactly the "extend, don't invent" answer RESEARCH specified. Zero new files; the repo does not gain an 18th unrewired `stripComments` copy.
- **The defect was MEASURED before it was fixed.** With the census unextended, deleting `retriesOverride` from the analytics chokepoint left the wiring file **22/22 GREEN**. The retry axis really was "discovered by nothing" — the exact property the roster's own docblock says the file-level fence exists to prevent.
- **The class-γ closure is by enumeration, 3/3.** The grep returns exactly 3 code sites and all 3 are fenced. Fixing only the named instance would have repeated the precise miss this phase exists to repair.
- **141.1-04's measured RED was converted into a real failure.** Deleting the `bridge` registry entry used to be `1 failed / 76 passed of 77` — the `it.each` merely shed a case (78→77) and stayed green. It is now `2 failed / 79 passed of 81`, with the fence itself red.
- **D-15's both-directions trap is fenced with hand-typed literals.** Removing the `(1 + retries)` factor computes 43 250 (RED); raising `failing` to 6 computes 405 000 (RED, phantom breach). Both figures match the plan's predictions exactly.
- **No ceiling is breached at the corrected numbers** — this is an accounting correction, as D-15 states. Every route-ceiling assertion stayed green.
- **Zero production files touched.** All three modified files are test files; no Python, no package installs, no constant retuned, no assertion weakened or narrowed.

## Task Commits

1. **Task 1: D-09 — retry axis on the existing census (SC-F)** — `c9a286d9` (test)
2. **Task 2: D-14a class-γ fences (all 3) + D-14b literal pins (SC-J, SC-K)** — `86db72a8` (test)
3. **Task 3: D-15 — per-leg store recharge + hand-typed worst cases (SC-L)** — `5caf7195` (fix)

## Files Created/Modified

- `src/lib/resilient-fetch.wiring.test.ts` — `DiscoveredBinding.retry` + docblock; `encodeBinding` carries it; `normaliseRetryExpr`, `retryExprFor`, `chokepointRetry`; all three discovery families wired; `ANALYTICS_RETRY` / `PROCESS_KEY_RETRY` hand-typed chokepoint expressions; `retry` on every roster site; the no-`<ABSENT>` assertion.
- `src/lib/seam-constants.pin.test.ts` — the four-map anti-vacuity fence (4/2/2/5) immediately above the `it.each` it protects; three SC-K literal pins (250 / 250 / sum 500); two added imports.
- `src/lib/seam-budgets.invariant.test.ts` — `SEAM_EXCLUSIONS` exact-count fence; `branchWorstCases` / `worstBranch` extraction; per-leg `(1 + retries)` store term; two hand-typed oracles; corrected header formula, `:81` preamble + table, `STORE_COMMANDS_PER_SEAM_CALL` docblock, the `MAX_COMPOSITE_MEMBERS` hypothetical, and the ceiling assertion's store message.

## Verification

All commands run in the worktree at `5caf7195`.

**Plan verification block:**

```
$ npx vitest run src/lib/resilient-fetch.wiring.test.ts \
    src/lib/seam-constants.pin.test.ts src/lib/seam-budgets.invariant.test.ts
 Test Files  3 passed (3)
      Tests  188 passed (188)

$ npm run typecheck        # tsc --noEmit
(no output — 0 errors)

$ npm run lint
✖ 1 problem (0 errors, 1 warning)
   # the single warning is pre-existing, in EquityChart.tsx, untouched by this plan
[check-admin-route-manifest] OK — 20 admin routes, all declared in manifest.
[check-route-contract] OK — 56 page routes, all declared in the manifest.

$ npx vitest run src/lib/
 Test Files  174 passed (174)
      Tests  3386 passed | 9 skipped (3395)

$ npx vitest run                    # full suite
 Test Files  735 passed | 19 skipped (754)
      Tests  10440 passed | 287 skipped (10727)
```

### Mutation evidence

#### Task 1 — SC-F

**BASELINE RED (the defect, measured before the fix) — delete `retriesOverride` from `analyticsRequest` with the census UNEXTENDED:**

```
 Test Files  1 passed (1)
      Tests  22 passed (22)
```

The retry axis was discovered by nothing. Reverted.

**MUTATION SC-F (a) — the same deletion, census EXTENDED:**

```
 × every discovered binding is classified in the roster (a 14th binding FAILS)
 × no discovered site is missing its retry verdict (D-09)

AssertionError: The set of budget-key bindings on disk no longer matches the roster
typed in this file. … Since 141.1/D-09 each entry also carries its `retriesOverride`
SOURCE EXPRESSION, so a call site that drops its override, or drifts one from `?? 0`
to `?? 1`, differs on this roster's other side and lands here too.:
expected [ …(14) ] to deeply equal [ …(14) ]

+   "i | bridge | /api/portfolio-bridge | src/lib/analytics-client.ts::findReplacementCandidates | <ABSENT>",
+   "i | encrypt-key | /api/encrypt-key | src/lib/analytics-client.ts::encryptKey | <ABSENT>",
+   "i | match-eval | /api/match/eval?{} | src/lib/analytics-client.ts::evalMatch | <ABSENT>",
+   "i | match-recompute | /api/match/recompute | src/lib/analytics-client.ts::recomputeMatch | <ABSENT>",
+   "i | optimize-weights | /api/optimize-weights | src/lib/analytics-client.ts::optimizeScenarioWeights | <ABSENT>",
+   "i | portfolio-analytics | /api/portfolio-analytics | src/lib/analytics-client.ts::computePortfolioAnalytics | <ABSENT>",
+   "i | portfolio-optimizer | /api/portfolio-optimizer | src/lib/analytics-client.ts::runPortfolioOptimizer | <ABSENT>",
+   "i | simulator | /api/simulator | src/lib/analytics-client.ts::simulateAddCandidate | <ABSENT>",
+   "i | validate-key | /api/validate-key | src/lib/analytics-client.ts::validateKey | <ABSENT>",

AssertionError: A seam call site passes NO `retriesOverride`. … a site that never
states one is an UNAUDITED replay, not a safe default. …:
expected [ …(9) ] to deeply equal []

      Tests  2 failed | 21 passed (23)
```

Run with vitest only, whose esbuild ignores the tsc error — proving the census works **below** the type system. Reverted → 23 passed.

**MUTATION SC-F (b) — drift the process-key chokepoint from `?? 0` to `?? 1`:**

```
-   "ii | process-key-enqueue | /process-key | src/lib/process-key-client.ts::budgetKeyFor | RETRY_SAFE_FLOW_TYPES[args.flow_type]?.retries ?? 0",
-   "ii | process-key-sync | /process-key | src/lib/process-key-client.ts::budgetKeyFor | RETRY_SAFE_FLOW_TYPES[args.flow_type]?.retries ?? 0",
+   "ii | process-key-enqueue | /process-key | src/lib/process-key-client.ts::budgetKeyFor | RETRY_SAFE_FLOW_TYPES[args.flow_type]?.retries ?? 1",
+   "ii | process-key-sync | /process-key | src/lib/process-key-client.ts::budgetKeyFor | RETRY_SAFE_FLOW_TYPES[args.flow_type]?.retries ?? 1",

      Tests  1 failed | 22 passed (23)
```

The no-`<ABSENT>` assertion correctly stays GREEN here (the override is present, merely drifted); only the set-equality reds. Reverted → 23 passed.

**Vacuity floor:** discovery still yields 14 rows (retry is a field, not a row), so the `>= 14` literal is unchanged. Not lowered.

#### Task 2 — SC-J, class-γ 2/3, SC-K

**141.1-04's measured signature, reproduced verbatim — delete the `bridge` entry from `RETRY_SAFE_ANALYTICS`, fence NOT yet added:**

```
 Test Files  1 failed (1)
      Tests  1 failed | 76 passed (77)
```

Exactly the `1 failed / 76 passed of 77` that plan 04 recorded: 78 → 77, the `it.each` shed a case rather than failing, and the single failure came from 04's own OQ-3 pin. Reverted.

**MUTATION SC-J — the same deletion, fence ADDED:**

```
 × the four registry maps have their hand-typed sizes (D-14a anti-vacuity)
 × all five flipped rows AGREE with the registry verdict that gates them (OQ-3)

AssertionError: RETRY_SAFE_ANALYTICS no longer holds exactly 4 audited retry
verdicts (bridge, simulator, portfolio-optimizer, optimize-weights). A FIFTH is a
wrapper granted a replay without an idempotency audit; a THIRD means an authorised
retry was withdrawn while SC-4b still charges for it — and the case list below would
shrink to match, silently.: expected 3 to be 4 // Object.is equality

      Tests  2 failed | 79 passed (81)
```

The silent case-shed is now a real RED. Reverted → 82 passed.

**MUTATION class-γ member 2/3 — remove one `SEAM_EXCLUSIONS` row (`warmup-analytics.ts`):**

```
 × documents at least the three known non-core seam call sites
 × excludes exactly the three hand-typed paths (SET equality, not count)
 × SEAM_EXCLUSIONS holds exactly 3 rows — the fence for both it.each blocks (D-14a)
 × keeps both /health warmers in the exclusion table (membership, not equality)

AssertionError: SEAM_EXCLUSIONS no longer holds exactly 3 rows. An exclusion is a
decision that a Railway call site deliberately gets no budget and no breaker …:
expected 2 to be 3 // Object.is equality

      Tests  4 failed | 75 passed (79)
```

The new fence reds — **and so do three pre-existing guards**, which confirms the honest scoping recorded in the code comment and in Deviation 2 below. Reverted.

**MUTATION SC-K — `SEAM_RETRY_BACKOFF_MS` 250 → 300:**

```
 × SEAM_RETRY_BACKOFF_MS is the literal 250 (D-14b)
 × the worst-case retry interval is 500ms — the sum SC-4b charges (D-14b)

AssertionError: SEAM_RETRY_BACKOFF_MS moved. RAISING it lengthens every retried
route's worst-case lambda hold, which SC-4b charges at the MAX of backoff + jitter —
the finalize-wizard composite has 22 500 ms of headroom, so this is not free. …:
expected 300 to be 250 // Object.is equality

AssertionError: The worst-case wait between a failed seam attempt and its retry is
no longer 500ms. SC-4b charges this MAX once per retried leg …:
expected 550 to be 500 // Object.is equality

      Tests  2 failed | 80 passed (82)
```

Both the literal pin AND the sum pin red, as the criterion requires. Reverted → 82 passed.

#### Task 3 — SC-L and the anti-shortcut

**RED-BEFORE-GREEN — the 56 000 oracle added against the UNCORRECTED (flat) store term:**

```
AssertionError: keys/sync's failing-state worst case is now 43250ms; the
hand-computed figure is 56 000ms (30 500 request + 25 500 store). A LOWER number
means the retried leg's second store round stopped being charged and SC-4b is
certifying headroom the route does not have. …:
expected 43250 to be 56000 // Object.is equality

      Tests  1 failed | 82 passed (83)
```

43 250 is exactly the figure the plan predicted. The 277 500 oracle was GREEN at this point — correct, since every leg on that branch is `retries: 0`. Applying the per-leg fix → **83 passed**.

**MUTATION SC-L — delete the `(1 + retries)` factor from the new store term:** identical output, 43 250. Reverted → 83 passed.

**MUTATION anti-shortcut — `STORE_COMMANDS_PER_SEAM_CALL.failing` 3 → 6:**

```
 × src/app/api/strategies/finalize-wizard/route.ts — failing state: request budget + store round trips < maxDuration x 1000
 × D-15: the RETRIED worst case is 56 000ms — keys/sync, failing, hand-typed
 × D-15: finalize-wizard's composite is UNCHANGED at 277 500ms — the anti-shortcut pin

AssertionError: "src/app/api/strategies/finalize-wizard/route.ts" can spend 405000ms
in the failing state on its worst branch [composite] (request: 150000ms = …; store:
255000ms = 6 command(s)/attempt x 4250ms x 10 seam call(s), charged per leg at
(1+retries)) against a 300000ms function ceiling. …:
expected 405000 to be less than 300000

AssertionError: finalize-wizard's COMPOSITE branch now costs 405000ms in the failing
state; it must stay 277 500ms. Every leg on this branch is retries: 0, so D-15's
correction cannot move it. 405 000 means the store's per-attempt count was raised
instead of the retry factor being applied per leg, which double-charges legs that
never retry. This route has 22 500ms of headroom — do NOT raise its maxDuration to
absorb an arithmetic error.: expected 405000 to be 277500 // Object.is equality

      Tests  3 failed | 80 passed (83)
```

The false-RED trap is fenced: 405 000 ms, precisely as RESEARCH predicted. Reverted → 83 passed.

### Class-γ closure grep (acceptance criterion)

```
$ grep -rn "it.each(Object.keys(" src/ --include="*.ts"
src/lib/seam-budgets.invariant.test.ts:973:      // CLASS-γ CLOSURE. Both `it.each(Object.keys(SEAM_EXCLUSIONS))` blocks in
src/lib/seam-budgets.invariant.test.ts:1001:    it.each(Object.keys(SEAM_EXCLUSIONS))(
src/lib/seam-budgets.invariant.test.ts:1040:    it.each(Object.keys(SEAM_EXCLUSIONS))(
src/lib/seam-constants.pin.test.ts:435:    it.each(Object.keys(RETRY_SAFE_ANALYTICS))(

$ grep -rn "it.each(Object.keys(" src/ --include="*.ts" | grep -vc "//"
3
```

**3/3 code sites, all fenced:**

| # | Site | Map | Fence | Status |
|---|------|-----|-------|--------|
| 1 | `seam-constants.pin.test.ts:435` | `RETRY_SAFE_ANALYTICS` | `:385` four-map count fence (4/2/2/5) | **fenced** — and was genuinely exposed (78→77 measured) |
| 2 | `seam-budgets.invariant.test.ts:1001` | `SEAM_EXCLUSIONS` | `:972` exact-count fence (3) | **fenced** — was already covered by the sibling set equality |
| 3 | `seam-budgets.invariant.test.ts:1040` | `SEAM_EXCLUSIONS` | `:972`, same fence (same map) | **fenced** — as above |

The 4th raw hit is the comment anchor at `:973` — see Deviation 1.

### Other acceptance checks

```
$ grep -c "56_000\|56000" src/lib/seam-budgets.invariant.test.ts
1                          # the hand-typed literal in the expect ✓ (≥ 1)

$ grep -c "every row at .retries: 0." src/lib/seam-budgets.invariant.test.ts
0                          # the false preamble is gone ✓

$ git diff --name-status 68336a69 HEAD
M	src/lib/resilient-fetch.wiring.test.ts
M	src/lib/seam-budgets.invariant.test.ts
M	src/lib/seam-constants.pin.test.ts
                           # zero new files, exactly the plan's declared set ✓

$ git diff --diff-filter=D --name-only 68336a69 HEAD
(empty)                    # zero deletions ✓
```

**Header-table figures independently recomputed from `SEAM_BUDGETS` before being typed into the docblock** — all six verified: finalize-wizard composite 192 500 / 42 500 / 277 500; keys/sync 39 000 / 4 250 / 56 000; validate-and-encrypt unchanged at 132 750 / 12 750 / 158 250.

## Decisions Made

See `key-decisions` in the frontmatter. The two that a reviewer should check first:

- **`chokepointRetry` requires exactly one core call per client.** This is a real added property, not defensive padding: a second chokepoint would mean the nine analytics bindings no longer share one retry verdict, and without the check the census would silently attribute the first call's expression to all of them.
- **The store correction is per leg and `failing` stays 3.** Both wrong fixes were mutation-tested rather than argued: flat-6 gives 405 000 (fenced), and a route-level multiplier has no defined `retries` on a multi-leg branch.

## Deviations from Plan

### 1. [Acceptance-criterion intent] The class-γ grep returns 4 hits, not 3

- **Found during:** Task 2 closure verification.
- **Criterion:** *"`grep -rn "it.each(Object.keys(" src/ --include="*.ts"` output in the SUMMARY with each hit either fenced or justified."*
- **Actual:** 4 hits. The 4th is `seam-budgets.invariant.test.ts:973`, a line **inside my own fence's comment** that names the construct it protects: `// CLASS-γ CLOSURE. Both \`it.each(Object.keys(SEAM_EXCLUSIONS))\` blocks in …`.
- **Resolution — kept, not "fixed". No code change was made for this.** The comment must name the construct it fences; a fence whose comment cannot say what it fences is worse documentation for a smaller grep count. Excluding comment lines returns exactly **3**, and all 3 are fenced. Rewording the comment to dodge the pattern (e.g. "both it.each blocks over that map") would trade a precise symbol anchor for a number — the same trade plans 01 and 04 declined, and the third instance of this class in this phase.

### 2. [Scope, stated in code] Two of the three class-γ members were not actually exposed

- **Found during:** Task 2, reading `seam-budgets.invariant.test.ts` before writing the fence.
- **Issue:** The plan treats all three members as equivalent. They are not. `SEAM_EXCLUSIONS` already had a hand-typed **set equality** (`excludes exactly the three hand-typed paths`) plus a `>= 3` floor and a warmer-membership pin. A set equality is strictly stronger than any count — it reds on shrink, swap *and* growth — so those two `it.each` sites could not have shed cases silently. Confirmed by mutation: removing a row reds **4** assertions, of which 3 pre-date this plan.
- **Resolution:** The fence was still added, per the plan's letter and because it does buy two things — exactness where only a floor sat, and co-location so the guard beside the `it.each` does not depend on a sibling `it` surviving a future edit. What changed is the **claim**: the code comment says explicitly *"HONEST SCOPE, so nobody over-credits this line … these two blocks were NOT actually exposed"*. Writing the fence while implying it closed a live hole would have been the more damaging outcome.
- **Impact:** None on the deliverable. The class is closed 3/3 either way.

### 3. [Rule 2 — missing critical correctness] Prose falsified by the arithmetic, corrected in the same commit

- **Found during:** Task 3.
- **Issue:** Beyond the `:81` preamble the plan names, the D-15 change also falsified (a) the header's formula block, which showed the store term outside the leg sum; (b) the ceiling assertion's own failure message, which described the store as `N command(s) × ms × seam calls` with no retry factor; and (c) the `MAX_COMPOSITE_MEMBERS` clause's hypothetical, which said flipping a row to `retries: 1` puts the composite at 427 500 ms. Under the corrected arithmetic it is **560 000 ms** (and the old figure was itself slightly stale — the flat arithmetic gives 432 500, the 427 500 having omitted the backoff term).
- **Resolution:** All three corrected in Task 3's commit. Leaving prose that the same commit made false is the exact D-07 defect class this phase is repairing; correcting it elsewhere would mean shipping a knowingly-false comment. The 11-member figure (305 250 ms) was re-derived and is **unchanged** — that branch is all `retries: 0` — so it was left alone.

### 4. [Surgical] `validate-and-encrypt` kept in the header table rather than replaced

- **Found during:** Task 3.
- **Issue:** The plan says to *add* the retried worst case to the table. My first edit swapped the `validate-and-encrypt` column for `keys/sync`, which would have deleted information.
- **Resolution:** Recomputed `validate-and-encrypt` under the corrected arithmetic (132 750 / 12 750 / 158 250 — unchanged, all legs at `retries: 0`), restored the column and added `keys/sync` as a third. Caught before commit.

---

**Total deviations:** 4 (1 acceptance-criterion interpretation, 1 scope honesty, 1 Rule-2 prose correction, 1 self-caught edit).
**Zero auto-fixed bugs in production code — no production file was touched by this plan.**
**Impact on plan:** None. No scope creep, no file outside the declared set, no constant retuned, no assertion weakened or narrowed.

## Issues Encountered

**⚠️ The worktree was spawned STALE, at `43d119bf` — 67 commits behind the phase branch and missing BOTH of its dependencies.** `43d119bf` is a merge commit on `main`; the wave-1 and wave-2 merges (including `141.1-04`, this plan's declared `depends_on`) were absent. Building against that base would have meant censusing a tree where `retriesOverride` was still optional and three route call sites had no override at all — the no-`<ABSENT>` assertion would have been unsatisfiable, and the whole plan would have been written against pre-dependency source while going green.

Verified `43d119bf` was an ancestor of `feat/v1.16-141-jobs-rate-retry` and that the agent branch carried **zero** unique commits, then `git merge --ff-only 68336a69`. No reset, no clean, no stash. Baseline is `68336a69`, the wave-2 merge tip, as intended. This is the second consecutive wave to hit this; it is a spawn-time defect, not a per-plan one.

**141.1-05's repair to the family-(ii) extractor was preserved.** That plan fixed `discoverBudgetKeyForArms` so `case "x":` labels stop reading as phantom budget keys. Task 1 added a `retry` field to the same function's return and a `chokepointRetry` call; it did not touch `withoutFlowTests`, the return-position filter, or their docblock. Both directions of that extractor's invariant still hold.

**No checkpoints hit. No auth gates. No blockers. No stubs. No package installs.**

## Threat Flags

None. The three threats this plan's register assigns `mitigate` are all discharged as specified:

- **T-141.1-16** (unaudited retry site) — the `<ABSENT>` sentinel plus set-equality, both mutation-proven, and demonstrated to work below the type system (vitest-only run against a tsc-invalid tree).
- **T-141.1-17** (headroom under-charge) — the per-leg `(1 + retries)` recharge, with hand-typed 56 000 / 277 500 oracles fencing both wrong-fix directions, each observed RED.
- **T-141.1-18** (vacuous guard) — hand-typed length fences on all 3 enumerated members, grep evidence above.

No new network endpoint, auth path, file access pattern or schema change. No production file was modified.

## User Setup Required

None — no external service configuration, no environment variables, no package installs.

## Next Phase Readiness

- **Ready.** All three tasks complete, full suite green at 10 440 tests.
- **For 141.1-09 (or any later plan touching the census):** `EXPECTED_BINDINGS` sites now carry a third hand-typed field. A new seam call site needs `family`, `key`, `path`, `site` **and** `retry` — and `retry` must be the normalised SOURCE EXPRESSION, not the resolved number.
- **For anyone editing SC-4b:** the arithmetic lives in `branchWorstCases`, not inline in the `it.each`. The two hand-typed oracles (56 000 / 277 500) will red on any change to it, which is the intent — recompute by hand from `SEAM_BUDGETS` and retype the literal, never derive it.
- **⚠️ Orchestrator note:** the stale-worktree-base hazard recurred on wave 3. Worth fixing at the spawn site before wave 4.

## Self-Check: PASSED

- `src/lib/resilient-fetch.wiring.test.ts` — FOUND
- `src/lib/seam-constants.pin.test.ts` — FOUND
- `src/lib/seam-budgets.invariant.test.ts` — FOUND
- `.planning/phases/141.1-…/141.1-07-SUMMARY.md` — FOUND
- commit `c9a286d9` — FOUND
- commit `86db72a8` — FOUND
- commit `5caf7195` — FOUND

---
*Phase: 141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration*
*Completed: 2026-07-31*
