# Phase 141.1 — wave execution manifest

Reboot-durable. Lives here (not `/private/tmp`) because the scratchpad is cleared on host
restart — that is exactly how the first Wave-1 manifest was lost on 2026-07-31.

Base commit for all Wave-1 worktrees: `5953feca`
Target branch: `feat/v1.16-141-jobs-rate-retry`

## Wave 1 — MERGED ✅ (resumed 2026-07-31 after host restart)

Merged to `feat/v1.16-141-jobs-rate-retry` as `638389a1`, `a46f0977`, `9b847444`, `823b015e`.
Post-merge gate GREEN: `tsc --noEmit` clean; vitest 176 files / 3412 passed / 9 skipped;
pytest `test_process_key.py` 111 passed. Worktrees removed, branches deleted.

**⭐ Lesson — an interrupted executor's uncommitted diff may be a MUTATION, not progress.**
141.1-08's orphaned `route.ts` diff had re-added `path:/api/process-key` — that is the
plan's own acceptance mutation, caught mid-probe. Merging the leftovers on faith would
have re-shipped the dead-alert defect the plan exists to repair. Always judge a resumed
worktree's uncommitted diff against the plan before keeping it.


| Plan | Branch / worktree | Pre-restart state |
|---|---|---|
| 141.1-01 Retry-After-aware backoff | `worktree-agent-a6cbda75517eddbbe` | 2 commits (`10d5f2cb`, `6711b6d3`) + uncommitted `resilient-fetch.ts`, `seam-constants.pin.test.ts` |
| 141.1-02 Registry evidence repair | `worktree-agent-a461240f004d8b1db` | 1 commit (`e405b452`), clean tree |
| 141.1-03 Resync pre-check filter test | `worktree-agent-a9ff827a1ec4429c7` | 0 commits + uncommitted `test_process_key.py` |
| 141.1-08 Flag-monitor error-rate alert | `worktree-agent-a4bd0e3967f493895` | 1 commit (`71b97484`) + uncommitted 3 files |

None had written SUMMARY.md, so all four were incomplete. Resumed in place.

## Wave 2 — IN FLIGHT (dispatched 2026-07-31, fresh worktrees off `1734808b`)

| Plan | depends_on | files_modified overlap |
|---|---|---|
| 141.1-04 Retry wiring at the call sites | 141.1-01 | none with 05 — parallel safe |
| 141.1-05 Registry/process-key-client | 141.1-02 | none with 04 — parallel safe |

**⚠️ Hazard — `isolation: "worktree"` does NOT guarantee a fork off the branch tip.**
141.1-05's worktree was created at `43d119bf`, **56 commits behind** the phase branch, i.e.
without Wave 1 — even though Wave 1 was merged before dispatch. The executor caught it (HEAD
was a strict ancestor with no unique commits) and fast-forwarded to `1734808b` itself.
For a `depends_on` plan this is silent poison: the plan would build against the pre-dependency
source and still go green in isolation. **Verify every new worktree contains its dependency's
commits before trusting its work** — `git merge-base --is-ancestor <dep-sha> HEAD`. Checked for
both Wave-2 worktrees; both contained Wave 1 at report time.

## Waves remaining

- Wave 3: 141.1-06 (deps 01, 04), 141.1-07 (deps 04)
- Wave 4: 141.1-09 (deps 06, 07, 08)

(Confirm against plan frontmatter `wave:` before dispatch.)

## Do NOT touch

These `worktree-agent-*` worktrees are residue from phases 140.x and are unrelated to 141.1.
Cleanup must be scoped to the four branches listed above:

`a04abd043064dbdc7`, `a265b1463591f09da`, `a3b0cd28c5786cd31`, `a660f887f4eb12f0f`,
`aa075a9692ac636b1`, `af9720ea4fca5189a`
