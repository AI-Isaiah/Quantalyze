---
phase: 141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/tests/test_process_key.py
autonomous: true
requirements: [SEAM-06]
tags: [process-key, resync, draft-filter, behaviour-pin, pytest]

must_haves:
  truths:
    - "D-14 (D-14c / SC-P): a genuinely NEW resync for a strategy whose prior resync verification is COMPLETED still queues (does not answer WIZARD_DUPLICATE) — the status='draft' filter has a BEHAVIOUR pin, not just a shape pin"
    - "The pin reddens when the .eq('status','draft') filter is removed from the resync pre-check"
    - "Zero PRODUCTION Python changes — the edit is test-only (the D-03 fence covers production Python, not tests; this decision is recorded)"
  artifacts:
    - path: "analytics-service/tests/test_process_key.py"
      provides: "the status='draft' behaviour pin driven through the full app stack against the stateful supabase fake"
  key_links:
    - from: "analytics-service/tests/test_process_key.py"
      to: "analytics-service/routers/process_key.py resync pre-check"
      via: "full-stack request through main.app with the stateful fake honouring .eq filters"
      pattern: "status.*draft"
---

<objective>
Close the one untested user-facing case D-14 names: the resync pre-check filters
`status='draft'`, and nothing proves that a COMPLETED prior verification does NOT block a
genuinely new resync. Without this pin, dropping the filter would wrongly answer
`WIZARD_DUPLICATE` to every repeat resync — and stay green.

PLANNER DECISION (OQ-2, owed at plan time): the D-03 zero-Python fence covers PRODUCTION
Python. Test files are outside it — a test changes no service behaviour, and Phase 141 itself
shipped Python tests. The pin therefore lives in `analytics-service/tests/test_process_key.py`
(the Python home named by SC-P), NOT in an SQL gate. This decision is explicit so the case
cannot silently vanish.

Purpose: discharges D-14c (bucket D5), SC-P.
Output: one behaviour test, mutation-proven against `process_key.py:1438`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-CONTEXT.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SC-P — behaviour pin for the resync status='draft' filter</name>
  <files>analytics-service/tests/test_process_key.py</files>
  <read_first>
    - analytics-service/routers/process_key.py — the resync pre-check (~:1432-1440: `.eq("strategy_id", ...)`, `.eq("flow_type", "resync")`, `.eq("status", "draft")`, `.limit(1).maybe_single()`) and the draft-insert path below it (READ ONLY — no production edit)
    - analytics-service/tests/test_resync_draft_dedup.py — the stateful in-memory supabase double that honours `.eq()` filters and stores inserts (141-01's harness; designed for reuse via `from tests.X import ...`)
    - analytics-service/tests/test_process_key.py — the existing resync filter-shape pin (the one that replaced PYAPI-09d) so the new behaviour pin sits beside it and does not duplicate it
  </read_first>
  <behavior>
    - Seed the fake store with a strategy_verifications row: same strategy_id, flow_type='resync', status='completed'.
    - Drive a resync request through the FULL main.app stack (the harness's established pattern).
    - Assert: the response is the normal queue/kickoff reply — NOT a WIZARD_DUPLICATE — and a NEW draft SV row was inserted (store row count for that strategy grew by 1).
    - Companion positive control (only if not already pinned by the existing dedup tests): with a status='draft' row seeded, the same request DOES short-circuit as duplicate — proving the seeded-row plumbing works and the new test cannot pass vacuously.
  </behavior>
  <action>
    Add the test to `analytics-service/tests/test_process_key.py` beside the existing resync
    filter-shape pin, reusing the stateful fake from `test_resync_draft_dedup.py` (import it — do
    not build a second double). Name it so the intent reads from the title, e.g.
    `test_resync_completed_prior_verification_does_not_block_new_resync`. Expected values
    hand-typed (row counts as literals). The docstring names the mutation it exists to catch:
    dropping `.eq("status", "draft")` from the resync pre-check makes ANY historical resync
    verification block every future resync.

    Local run note: if `pytest` fails on import (`pandera` missing locally), install it with
    `pip install 'pandera==0.32.1' --break-system-packages` — it is a declared dependency, CI has
    it. Do NOT touch any production `.py` file; `mypy --strict` exposure is nil for a test-only
    change, but keep the test typed cleanly anyway.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_process_key.py -q -k "resync"</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-P): remove `.eq("status", "draft")` from the resync pre-check in `analytics-service/routers/process_key.py`, run the verify command → the new test is RED (the new resync wrongly short-circuits as WIZARD_DUPLICATE); revert → GREEN. Paste the failing assertion.
    - `git diff --stat` shows ONLY `analytics-service/tests/test_process_key.py` changed (zero production Python).
    - The test seeds `status='completed'` (grep the new test body for `completed`), asserting the non-draft case specifically.
    - Full resync-scoped pytest selection passes: verify command exit 0.
  </acceptance_criteria>
  <done>The draft-filter's behaviour is pinned; the SC-P mutation observed RED/GREEN; production Python untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fence | the D-03 fence (no production Python) must survive this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.1-06 | Tampering (scope) | analytics-service production code | mitigate | acceptance criterion: `git diff --stat` shows test file only — the fence is verified, not assumed |
| T-141.1-07 | DoS (regression) | resync dedup pre-check | mitigate | the behaviour pin makes a filter regression (over-broad dedup blocking real resyncs) RED instead of silent |
| T-141.1-SC | Tampering | package installs | accept | zero package installs (pandera is a declared existing dependency) |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_process_key.py -q` — green
- SC-P mutation observed RED then reverted GREEN (evidence pasted in SUMMARY)
- `git diff --stat` — exactly one file, test-only
</verification>

<success_criteria>
- SC-P observed; the case named by D-14c exists and cannot silently vanish
- OQ-2 decision (test files outside the Python fence) recorded in the SUMMARY
</success_criteria>

<output>
Create `.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-03-SUMMARY.md` when done. Include the pasted SC-P RED evidence and the OQ-2 decision statement.
</output>
