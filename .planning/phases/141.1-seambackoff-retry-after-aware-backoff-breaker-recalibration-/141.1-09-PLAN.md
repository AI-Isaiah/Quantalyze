---
phase: 141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration
plan: 09
type: execute
wave: 4
depends_on: ["141.1-06", "141.1-07", "141.1-08"]
files_modified:
  - docs/runbooks/seam-breaker.md
  - docs/runbooks/README.md
  - docs/runbooks/compute-queue.md
  - TODOS.md
  - VERSION
  - package.json
  - CHANGELOG.md
autonomous: true
requirements: [SEAM-05, SEAM-06]
tags: [runbook, todos, ledger, version, changelog, ship-time]

must_haves:
  truths:
    - "D-18: docs/runbooks/seam-breaker.md exists in the house form, covers fail-open semantics, the Upstash keys, the constants + measured recovery band, seam.breaker.open, up-to-2-attempts, the D-01 Retry-After fail-fast rule, the D-03 doubled-compute cost, and the resetUsedTokens prohibition; it is indexed in README.md and cross-linked BOTH directions with compute-queue.md's DIFFERENT breaker"
    - "D-19: TODOS.md has a Phase 141/141.1 deferrals section; G1, G2, G3, G4 each carry an explicit DISCHARGED or RE-BOOKED verdict (G4 is discharged BY the section's existence and is NAMED as such in the section lead); all of bucket H1–H7 is recorded (H3 as know-it-don't-fix-it); H-0562 has a row (H7); the D-16 denominator follow-up and the D-03 cancellation deferral are booked; G5 gets NO task (already discharged in 2e36016d)"
    - "D-20: VERSION, package.json version, and the CHANGELOG head entry move in lockstep in ONE commit, covering 141 AND 141.1 together (one PR)"
    - "D-03: the doubled-compute consequence is recorded in the runbook AND in TODOS.md (its registry half landed in plan 02)"
  artifacts:
    - path: "docs/runbooks/seam-breaker.md"
      provides: "the ops surface for the Vercel→Railway seam breaker"
    - path: "TODOS.md"
      provides: "Phase 141/141.1 deferrals section with explicit verdicts"
    - path: "CHANGELOG.md"
      provides: "the 141+141.1 entry at the new version head"
  key_links:
    - from: "docs/runbooks/README.md"
      to: "docs/runbooks/seam-breaker.md"
      via: "index table row"
      pattern: "seam-breaker"
    - from: "docs/runbooks/compute-queue.md"
      to: "docs/runbooks/seam-breaker.md"
      via: "disambiguating cross-link (two DIFFERENT breakers)"
      pattern: "seam-breaker"
---

<objective>
Give the seam breaker an ops surface (25 runbooks exist; this breaker appears in none, and the
only "circuit breaker" hit is a DIFFERENT Python 429 cooldown), discharge or re-book every
undischarged 141 obligation in the single ground-truth backlog, and perform the ship-time
version/CHANGELOG bump for 141+141.1 as one release.

Purpose: discharges D-18 (F3), D-19 (G1–G4 + bucket H), D-20 (G6), and D-03's runbook/TODOS
halves.
Output: the runbook + index, the TODOS ledger section, the lockstep version bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-CONTEXT.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-RESEARCH.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-08-SUMMARY.md

Read plan 08's SUMMARY for its THREE handoffs — (a) the corrected two-cause diagnosis to record
FORWARD, (b) the post-repair recovered-retry visibility residual, (c) the transaction-form
confirmation follow-up — and the OQ-1 verdict (the CHANGELOG entry describes the remedy actually
landed).
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-18 — the seam-breaker runbook + index + disambiguation</name>
  <files>docs/runbooks/seam-breaker.md, docs/runbooks/README.md, docs/runbooks/compute-queue.md</files>
  <read_first>
    - docs/runbooks/railway-worker.md — the house form template (imperative title; "Health check first" with copy-pasteable curl + answer decode; numbered recovery with dashboard-then-CLI; bolded gotcha sections; inline do-not warnings)
    - docs/runbooks/README.md — the index tables (Subsystems vs "Incident response (start here)")
    - docs/runbooks/compute-queue.md (~:160) — the OTHER breaker (Python per-API-key 429 cooldown) that needs the bidirectional disambiguation
    - src/lib/resilient-fetch.ts — the content sources: fail-open doctrine (~:282-287), breaker keys + the two that can never open (~:435-437), the measured recovery-band table in the BREAKER_COOLDOWN_S docblock (~:163-189 — quote it, it is already written), `emitBreakerTransition` / seam.breaker.open (~:1130), the TRAP-6 resetUsedTokens prohibition (~:185-189)
    - 141.1-RESEARCH.md § "D-18 — the runbook surface" — the complete content checklist
  </read_first>
  <action>
    Create `docs/runbooks/seam-breaker.md` in the railway-worker house form, covering ALL of:
    fail-open semantics (a breaker-store failure lets the call through; a too-tight store deadline
    silently DISABLES the breaker); the Upstash keys (`breaker:railway` global — appended to every
    call site — plus `breaker:mt5-gateway` / `breaker:supabase`; `breaker:kek` and
    `breaker:egress-proxy` can never open at HEAD); constants (threshold 5 / window 30s /
    cooldown 30s / tombstone 60s) with the measured recovery band [30s, 60s] table quoted from
    the docblock; the per-attempt counting consequence (trip in ~3 user requests — D-02);
    `seam.breaker.open` transitions; up-to-2-attempts on the five audited budgets (link the
    registry); the D-01 Retry-After fail-fast rule; the D-03 doubled-compute cost on the four
    heavy budgets; what NOT to do (`resetUsedTokens` forbidden — TRAP-6, full-keyspace Lua SCAN
    against the DB shared with 15 production limiters); and the RESEND_API_KEY-not-set-in-Vercel
    gap (an alert can be computed and never emailed). Use SYMBOL anchors, not line coordinates,
    for every code reference (the phase's own citation discipline).

    Index it in README.md under "Incident response (start here)" (it is a prod-outage surface,
    beside railway-worker and sentry-triage). Add ONE disambiguating cross-link line in
    compute-queue.md near its :160 breaker mention ("this is the Python per-API-key 429 cooldown;
    for the Vercel→Railway seam circuit breaker see seam-breaker.md") and the mirror sentence in
    the new runbook. Surgical: compute-queue.md gains exactly that link, nothing else.
  </action>
  <verify>
    <automated>test -f docs/runbooks/seam-breaker.md && grep -c "seam-breaker" docs/runbooks/README.md docs/runbooks/compute-queue.md && grep -ci "retry-after" docs/runbooks/seam-breaker.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "fail-open" docs/runbooks/seam-breaker.md` ≥ 1; `grep -c "breaker:railway" docs/runbooks/seam-breaker.md` ≥ 1; `grep -ci "resetUsedTokens" docs/runbooks/seam-breaker.md` ≥ 1; `grep -ci "Retry-After" docs/runbooks/seam-breaker.md` ≥ 1; `grep -ci "concurrent\|doubled" docs/runbooks/seam-breaker.md` ≥ 1 (D-03 cost present).
    - `grep -c "seam-breaker" docs/runbooks/README.md` ≥ 1 and `grep -c "seam-breaker" docs/runbooks/compute-queue.md` ≥ 1 (both directions).
    - `git diff --stat docs/runbooks/compute-queue.md` shows a 1–3 line change only.
    - `grep -Ec "\.(ts|py|sql)\s*:\s*[0-9]" docs/runbooks/seam-breaker.md` → 0 (no coordinate citations).
  </acceptance_criteria>
  <done>On-call has a correct mental model of THIS breaker, findable from the index, not confusable with the 429 cooldown.</done>
</task>

<task type="auto">
  <name>Task 2: D-19 — the Phase 141/141.1 deferrals ledger</name>
  <files>TODOS.md</files>
  <read_first>
    - TODOS.md — the house-form analog `### v1.16 Phase-140.5 (SEAMPROSE) — deferred items` (~:474-487: lead paragraph with the founder blast-radius bar, bold-lead bullets, ✅RESOLVED / ⚠️CORRECTED markers), plus the three obligation rows: ~:121 (G1 / TS-35 W-4), ~:365 (G2 / LO-02), ~:483 (G3 — now factually false)
    - .planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-06-SUMMARY.md — G2's discharge commit (decodeBreakerLock bounds)
    - .planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-08-SUMMARY.md — the D-16 denominator handoff paragraph
    - 141.1-RESEARCH.md § "D-19 — obligation status, verified line by line" (incl. the G1 nuance) and § Deferred Ideas (H1–H7 texts)
  </read_first>
  <action>
    Add `### v1.16 Phase-141 / 141.1 (SEAM / SEAMBACKOFF) — deferred items (added 2026-07-31)` in
    the established house form. Content, each with an explicit verdict marker:
    - G1 (TS-35 W-4, ~:121): mark DISCHARGED with the recorded re-derivation — the retry loop
      branches on `verdict.counts` / `isDeadlineError`, NOT on `recoverable`; the rider's premise
      ("141 converts recoverable into an automated retry input") is overstated, and THAT finding
      is the re-derivation. Update/annotate the :121 row accordingly (do not silently delete it).
    - G2 (LO-02/TS-39, ~:365): mark DISCHARGED, naming the plan-06 commit that bounded
      decodeBreakerLock's span.
    - G4: the deferrals section ITSELF is G4's discharge — name it explicitly in the section lead
      (e.g. "this section discharges G4").
    - G3 (~:483): correct the now-false parenthetical ("the path 141 adds (unreachable today)" —
      141 added it, it is reachable), AND update the string-literal-citation residual note: the
      registry's evidence strings are now guarded by the plan-02 registry-local guard (the
      residual narrows to the other 34 files' string literals).
    - Bucket H, one bullet each: H1 (rate-limiter double-consumption during incidents — worth a
      recorded decision), H2 (`continue` without `res.body?.cancel()` — undici buffers until the
      signal fires), H3 (admittedAtMs pre-loop capture — explicitly "know it; don't fix it",
      fail-open per A-25), H4 (keys/sync legacy status/code drift — nobody reads it today),
      H5 (pre-check `.limit(1)` without ORDER BY), H6 (10-param `_enqueue_compute_job_internal`
      STRICT divergence — pre-existing, not 141), H7 (add an H-0562 multi-worker-durability row so
      the registry's citation has a ledger target).
    - D-03: the server-side-cancellation deferral (its own phase, own soak) with the
      doubled-compute cost statement.
    - D-16 follow-ups (three rows, from plan 08's handoffs — the denominator fix itself LANDED in
      plan 08, do not re-book it): (i) the corrected two-cause diagnosis recorded FORWARD (the
      2026-05-27 region-URL fix `8904b204` addressed one of two independent causes; the numerator
      stayed structurally 0 until 141.1 because Sentry `path` is written to `extra`, never `tags`
      — do NOT edit the historical `[0.24.x]` CHANGELOG entries); (ii) the post-repair
      recovered-retry visibility residual (a transient failure recovering on attempt 2 emits no
      Sentry event — warning-level capture + numerator widening as follow-up); (iii) confirm the
      first real process-key event's `transaction` form against the derived
      `transaction:/process-key` term.
    G5 gets NO row and NO task — already discharged (`2e36016d`), verified at plan time.
  </action>
  <verify>
    <automated>grep -c "Phase-141" TODOS.md && grep -c "H-0562" TODOS.md</automated>
  </verify>
  <acceptance_criteria>
    - The new section exists with ALL of: G1/G2/G3/G4 verdicts (`grep -Ec "DISCHARGED|RE-BOOKED" TODOS.md` grew by ≥ 4), seven H bullets, the D-03 deferral, the three D-16 follow-up rows.
    - The section lead names G4 DISCHARGED explicitly (`grep -c "G4" TODOS.md` ≥ 1).
    - `grep -n "unreachable today" TODOS.md` → 0 (the false parenthetical is corrected).
    - H3's bullet contains "don't fix" (the founder's explicit stance survives verbatim in spirit).
    - `grep -c "H-0562" TODOS.md` ≥ 1.
    - Backlog discipline: items added ONLY to TODOS.md (no new tracker file created).
  </acceptance_criteria>
  <done>Every 141 obligation carries an explicit verdict in the single ground-truth backlog; bucket H is recorded, not fixed.</done>
</task>

<task type="auto">
  <name>Task 3: D-20 — ship-time version/CHANGELOG bump (ONE commit)</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <read_first>
    - VERSION (currently 0.50.1.0), package.json `"version"`, CHANGELOG.md head (`## [0.50.1.0] - 2026-07-31` from 140.3)
    - CONTRIBUTING.md:68-71 — the ship-time step this implements
    - 141.1-RESEARCH.md § "D-20 — the version ladder" (4-part major.minor.patch.build; 140 → 0.50.0.0 feature-grade, 140.3 → 0.50.1.0)
  </read_first>
  <action>
    Bump to `0.51.0.0` — 141 turns retry ON for five budgets, feature-grade on the 4-part ladder
    (matching 140's 0.50.0.0 precedent). Write VERSION and package.json's `"version"` to the
    identical string. Add the CHANGELOG head entry `## [0.51.0.0] - <today>` covering **141 and
    141.1 together** (one PR): the audited bounded seam retry (registry-gated, teaser provably
    never retried), the Retry-After fail-fast, the ratified breaker threshold, the SEAM-05
    evidence re-derivation, the closed registry-bypass axis, the flag-monitor repair — INCLUDING
    the corrected two-cause diagnosis recorded forward (the numerator had been structurally dead
    since Phase 19; the 2026-05-27 region-URL fix `8904b204` was one of two independent causes;
    `path` lives in Sentry `extra`, never `tags`) with NO edit to the historical `[0.24.x]`
    entries — and the new seam-breaker runbook. All three files in ONE
    commit — this is the plan's final commit and the phase's last code action.
  </action>
  <verify>
    <automated>diff <(cat VERSION) <(node -p "require('./package.json').version") && head -3 CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `diff <(cat VERSION) <(node -p "require('./package.json').version")` prints NOTHING.
    - `head -3 CHANGELOG.md` shows `## [0.51.0.0]` with today's date above the `[0.50.1.0]` entry.
    - `git show --stat HEAD` (after the commit) lists exactly VERSION, package.json, CHANGELOG.md.
    - CHANGELOG.md is APPEND-ONLY at the head: `git diff HEAD~1 -- CHANGELOG.md | grep -c '^-[^-]'` → 0. Historical `[0.24.x]` entries (notably `:3417` and `:3444`) MUST NOT be edited — a shipped changelog records what was believed at release time, and the partial region-URL diagnosis is itself the useful evidence. The corrected two-cause account belongs in the new `[0.51.0.0]` entry only.
    - The entry names both 141 and 141.1.
  </acceptance_criteria>
  <done>Release artifacts in lockstep in one commit; the 141+141.1 release is describable from the CHANGELOG alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → on-call operator | a wrong runbook drives wrong incident response against prod infrastructure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.1-22 | DoS (wrong ops action) | seam-breaker runbook | mitigate | the resetUsedTokens prohibition (TRAP-6) is stated IN the runbook; content checklist verified by grep criteria; bidirectional disambiguation from the 429-cooldown breaker |
| T-141.1-23 | Repudiation (lost obligations) | TODOS.md ledger | mitigate | explicit DISCHARGED/RE-BOOKED verdicts per obligation; single-backlog discipline enforced by acceptance criteria |
| T-141.1-SC | Tampering | package installs | accept | zero package installs (the package.json edit is the version string only — acceptance criterion pins the 3-file commit) |
</threat_model>

<verification>
- All grep-based acceptance criteria pass
- `npm run typecheck && npx vitest run src/lib/` still green at the final tree (docs/ledger/version changes cannot break code — verify anyway, fail loud)
- The D-20 commit contains exactly the three release files
</verification>

<success_criteria>
- Runbook live, indexed, disambiguated; D-03 cost recorded in runbook + TODOS
- Every 141 obligation has an explicit verdict; bucket H fully recorded; G5 untouched
- 0.51.0.0 in lockstep across all three artifacts in one commit
</success_criteria>

<output>
Create `.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-09-SUMMARY.md` when done.
</output>
