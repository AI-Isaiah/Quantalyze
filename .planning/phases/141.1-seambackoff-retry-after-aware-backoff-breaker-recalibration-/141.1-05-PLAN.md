---
phase: 141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration
plan: 05
type: execute
wave: 2
depends_on: ["141.1-02"]
files_modified:
  - src/lib/seam-retry-registry.ts
  - src/lib/seam-retry-registry.test.ts
  - src/lib/process-key-client.ts
autonomous: true
requirements: [SEAM-05]
tags: [seam-retry-registry, immutability, exhaustiveness, budgetKeyFor, type-safety]

must_haves:
  truths:
    - "D-11: the four registry maps are immutable at COMPILE time (`as const satisfies`) and RUNTIME (`Object.freeze`) — `RETRY_SAFE_FLOW_TYPES.teaser = ...` and `delete ...onboard` no longer typecheck, and `Object.isFrozen` is asserted true"
    - "D-11: FlowType exhaustiveness is REAL — adding a 5th union member breaks compilation without anyone editing a hand-typed list"
    - "D-11: `budgetKeyFor` has a never-defaulted exhaustive switch — a 5th flow can no longer silently land on the process-key-enqueue budget"
    - "D-11: the registry docblock's enforcement claim is TRUE (or deleted) — it no longer claims an enforcement that does not exist"
  artifacts:
    - path: "src/lib/seam-retry-registry.ts"
      provides: "frozen `as const satisfies` maps + a true docblock"
    - path: "src/lib/seam-retry-registry.test.ts"
      provides: "@ts-expect-error mutation fixtures, Object.isFrozen assertions, type-derived exhaustiveness check"
    - path: "src/lib/process-key-client.ts"
      provides: "never-defaulted budgetKeyFor switch + docblock naming the budget consequence"
  key_links:
    - from: "src/lib/seam-retry-registry.test.ts"
      to: "FlowType union in src/lib/process-key-client.ts"
      via: "type-derived exhaustiveness (Record<FlowType, true> or Exclude-to-never)"
      pattern: "FlowType"
---

<objective>
Make the registry's immutability and exhaustiveness claims TRUE. Today the maps are exported
mutable with a widening type annotation, the "exhaustiveness pin" compares two hand-typed lists
that agree by copy-paste, and `budgetKeyFor` is a ternary whose fall-through hands any future
5th flow the process-key-enqueue budget silently.

Purpose: discharges D-11 (bucket C4). SC-I.
Output: frozen maps, compile-time exhaustiveness, never-defaulted switch — all via the three
established house patterns (audit.ts `as const satisfies`; seam-discriminator.ts `Object.freeze`;
for-quants-lead / scenario-commit `never` checks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-CONTEXT.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-PATTERNS.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the four maps — compile-time and runtime (SC-I immutability half)</name>
  <files>src/lib/seam-retry-registry.ts, src/lib/seam-retry-registry.test.ts</files>
  <read_first>
    - src/lib/seam-retry-registry.ts — the four map declarations (annotation form `: Partial<Record<...>>`) and the docblock at ~:32-39 whose claim this task makes true; the leaf-purity constraint (2 `import type`-only lines — `Object.freeze` needs no import, so purity survives)
    - src/lib/audit.ts:530-560 — the `as const satisfies` house form WITH its rationale docblock (why `as const`, why NOT the annotation)
    - src/lib/seam-discriminator.ts:69-80 — the `Object.freeze` closed-vocabulary sibling
    - src/lib/seam-retry-registry.test.ts — the purity guard (~:145-175) that must stay green
  </read_first>
  <action>
    Convert all four maps (`RETRY_SAFE_FLOW_TYPES`, `RETRY_SAFE_ANALYTICS`,
    `RETRY_AUDIT_NO_FLOW_TYPES`, `RETRY_AUDIT_NO_ANALYTICS`) from the annotation form to
    `Object.freeze({...} as const satisfies Partial<Record<...original key/value types...>>)` —
    both mechanisms, because the docblock's claim is about RUNTIME mutation and `as const` alone
    is type-level. `RetrySafeEntry.retries` is already the literal type `1`, so `as const` is
    compatible. Copy audit.ts's docblock rationale style (one sentence each for what `satisfies`
    buys and what `as const` buys). Update the registry docblock at ~:32-39 so every enforcement
    it claims now exists (the exhaustiveness half is Task 2).

    In the test file: add `expect(Object.isFrozen(map)).toBe(true)` for all four maps, and two
    `@ts-expect-error` fixtures — `RETRY_SAFE_FLOW_TYPES.teaser = { retries: 1, evidence: "" }`
    and `delete RETRY_SAFE_FLOW_TYPES.onboard` — each with a comment naming C4 (both used to
    typecheck clean). Note: `as const` makes properties readonly, so both lines are now type
    errors; the `@ts-expect-error` enforces they STAY errors.

    Consumers: `process-key-client.ts` / `analytics-client.ts` only READ the maps (`?.retries`),
    so the readonly conversion must not require consumer changes — verify with typecheck.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/lib/seam-retry-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION: remove one `@ts-expect-error` comment → `npm run typecheck` REDs on that line (the mutation genuinely does not compile); restore → clean. Paste the tsc error.
    - `expect(Object.isFrozen(RETRY_SAFE_FLOW_TYPES)).toBe(true)` (and the other three) GREEN.
    - The leaf-purity guard stays GREEN (no new imports; `Object.freeze` is global).
    - `grep -c "as const satisfies" src/lib/seam-retry-registry.ts` → 4.
  </acceptance_criteria>
  <done>All four maps immutable at both levels; both former mutation vectors pinned as compile errors.</done>
</task>

<task type="auto">
  <name>Task 2: Real exhaustiveness + never-defaulted budgetKeyFor (SC-I exhaustiveness half)</name>
  <files>src/lib/seam-retry-registry.test.ts, src/lib/process-key-client.ts</files>
  <read_first>
    - src/lib/seam-retry-registry.test.ts — the current exhaustiveness pin (~:96-109) comparing YES∪NO against HAND-TYPED `EXPECTED_ALL_FLOW_KEYS` / `EXPECTED_ALL_ANALYTICS_KEYS` (both sides hand-typed to the same list — the defect)
    - src/lib/process-key-client.ts — `FlowType` union (~:56), `budgetKeyFor` ternary (~:79-83) and its docblock
    - src/app/api/for-quants-lead/route.ts:144-161 — the `Exclude`-to-never house form (fired twice on real PRs)
    - src/app/api/allocator/scenario/commit/route.ts:222-232 — the inline `_exhaustive: never` switch-default form
  </read_first>
  <action>
    In the test file: make one side of the flow exhaustiveness TYPE-DERIVED. Add a
    `const _flowCoverage: Record<FlowType, true> = { csv: true, onboard: true, resync: true, teaser: true }`
    (or the `Exclude`-to-never form from for-quants-lead — pick whichever reads cleaner beside the
    existing pins) so that adding a 5th member to the `FlowType` union breaks compilation WITHOUT
    anyone editing `EXPECTED_ALL_FLOW_KEYS`. Keep the hand-typed runtime pin too (it catches map
    edits; the type check catches union edits — different failure classes). Do the same for the
    analytics side against the wrapper/budget-key union that `RETRY_SAFE_ANALYTICS` keys on (read
    the actual key type in the registry to name it).

    In `process-key-client.ts`: convert `budgetKeyFor` from the ternary to an exhaustive `switch`
    on `flowType` with a `default` arm using the inline `const _exhaustive: never = flowType;
    throw new Error(...)` house form (fail loud per Rule 12, message naming schema drift). Extend
    the docblock with the budget consequence: without this, a 5th flow silently gets the 15s
    enqueue budget instead of the 60s inline one — the retry consequence died with D-08, the
    budget consequence is why the switch exists.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/lib/seam-retry-registry.test.ts src/lib/resilient-fetch.retry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (the SC-I headline): add a 5th member (e.g. `"backfill"`) to the `FlowType` union in `process-key-client.ts` → `npm run typecheck` REDs in AT LEAST two places WITHOUT editing any hand-typed list: the `Record<FlowType, true>` coverage const AND `budgetKeyFor`'s switch (non-exhaustive). Paste both tsc errors; revert → clean.
    - `budgetKeyFor` behaviour unchanged for the 4 real flows: the existing wiring tests (teaser/csv → process-key-sync budget; onboard/resync → process-key-enqueue) stay GREEN.
    - `grep -c "_exhaustive: never" src/lib/process-key-client.ts` → 1.
    - The registry docblock no longer claims a non-existent enforcement: re-read ~:32-39 against the now-true mechanisms (source assertion recorded in SUMMARY).
  </acceptance_criteria>
  <done>A 5th flow_type physically cannot ship without a verdict, a coverage entry, and a budget arm; nothing relies on two hand-typed lists agreeing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime consumers → registry maps | a mutated allowlist entry becomes a legitimate retry verdict from that moment on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.1-11 | Tampering | registry maps at runtime | mitigate | `Object.freeze` + `isFrozen` assertion — a consumer push cannot mint a retry verdict |
| T-141.1-12 | Elevation (unaudited flow) | `budgetKeyFor` fall-through / FlowType growth | mitigate | never-defaulted switch + type-derived coverage: a 5th flow is a compile error at two independent sites |
| T-141.1-SC | Tampering | package installs | accept | zero package installs in this phase |
</threat_model>

<verification>
- `npm run typecheck` — 0 errors; both SC-I mutations observed with pasted tsc output
- `npx vitest run src/lib/seam-retry-registry.test.ts src/lib/resilient-fetch.retry.test.ts` — green
</verification>

<success_criteria>
- SC-I fully observed (immutability + exhaustiveness + switch)
- Registry docblock claims only enforcements that exist
- Zero behaviour change for the 4 real flows
</success_criteria>

<output>
Create `.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-05-SUMMARY.md` when done. Include pasted tsc errors for both SC-I mutations.
</output>
