---
phase: 141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration
plan: 06
type: execute
wave: 3
depends_on: ["141.1-01", "141.1-04"]
files_modified:
  - src/lib/resilient-fetch.ts
  - src/lib/resilient-fetch.retry.test.ts
  - src/lib/resilient-fetch.test.ts
autonomous: true
requirements: [SEAM-06]
tags: [seam-retry, observability, logging, mutation-testing, deadline, breaker-ordering]

must_haves:
  truths:
    - "D-17: a counting-503 first attempt logs once (budget key + status + attempt index) and the pre-attempt-2 CircuitOpenError logs the discarded attempt-1 status — pre-threshold degradation is no longer silent"
    - "D-17 (security-critical NEGATIVE): no new log line contains the request body or any credential header value (INTERNAL_API_TOKEN, X-Service-Key, X-User-Access-Token) — asserted, not assumed"
    - "D-19/G2: decodeBreakerLock rejects nonsensical spans (<= 0 or > (BREAKER_COOLDOWN_S + BREAKER_LOCK_TOMBSTONE_S) * 1000) by returning null — a Retry-After of 1e17 can no longer be minted (LO-02/TS-39 DISCHARGED)"
    - "D-12: attempt 2 fires with a FRESH abort signal and a full fresh per-attempt budget, and its request is byte-identical to attempt 1 minus the signal — the 125-green deadline-hoist mutation now reddens (SC-B)"
    - "D-13: the sleep→re-check ordering is load-bearing under test — a breaker that opens DURING the backoff aborts attempt 2 (SC-D)"
    - "D-10: the belt's INDEPENDENCE is pinned — with row and registry in forced disagreement, teaser is still single-fetch; the 558-green wiring-deletion mutation now reddens (SC-A)"
    - "D-14 (D-14d): bridge at production configuration retries through the real findReplacementCandidates chokepoint (SC-O)"
  artifacts:
    - path: "src/lib/resilient-fetch.ts"
      provides: "counting-arm console.error, lastCountingStatus hoist + CircuitOpenError log, decodeBreakerLock span bounds"
    - path: "src/lib/resilient-fetch.retry.test.ts"
      provides: "SC-A, SC-B, SC-D, SC-M, SC-M', SC-O cases + the G2 regression test"
  key_links:
    - from: "src/lib/resilient-fetch.ts counting arm"
      to: "console.error"
      via: "budgetKey + res.status only — never path/body/headers"
      pattern: "console\\.error"
    - from: "src/lib/resilient-fetch.retry.test.ts SC-A case"
      to: "SEAM_BUDGETS['process-key-sync'].retries"
      via: "forced in-test row/registry disagreement, restored in afterEach"
      pattern: "process-key-sync"
---

<objective>
Harden the retry loop's test oracle and give its silent arms a voice. Two measured facts drive
this plan: deleting all of 141-04's wiring left 558 tests green (SC-A), and hoisting the
per-attempt deadline left 125 green while silently making the retry a no-op for the dominant
timeout class (SC-B). Every fix here is proven by the exact mutation it reddens under. Also
discharges the G2/LO-02 obligation routed to "141's breaker pass" — this is that pass.

Purpose: discharges D-17 (F2, F4), D-12 (D1, D7), D-13 (D2), D-10 (C3), D-14d (D6), and the
G2 half of D-19.
Output: logging on both silent arms, decodeBreakerLock bounds, and five mutation-proven tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-CONTEXT.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-RESEARCH.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-PATTERNS.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-VALIDATION.md

⚠️ This plan runs AFTER 141.1-04 (D-08): `retriesOverride` is required `0 | 1` and the row
fallback is gone. Write every test and comment against that world.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-17 logging on both silent arms + G2 decodeBreakerLock bounds (SC-M, SC-M′)</name>
  <files>src/lib/resilient-fetch.ts, src/lib/resilient-fetch.retry.test.ts, src/lib/resilient-fetch.test.ts</files>
  <read_first>
    - src/lib/resilient-fetch.ts — the transport-catch arm's log discipline (~:2076-2108, THE model: budget key logged, never path/body/headers; the ⚠️ TS-15 incident record at ~:2099-2103 — omitting scrubSeamError's second argument once shipped a live end-user Supabase JWT), the counting arm (~:2154-2163), the CircuitOpenError re-check throw (~:2025-2030), `decodeBreakerLock` (~:959), `BREAKER_COOLDOWN_S` + `BREAKER_LOCK_TOMBSTONE_S` constants
    - TODOS.md:365 — the G2/LO-02 obligation text (the exact bound it prescribes)
    - src/lib/resilient-fetch.test.ts (~:2176) — the existing negative "never log a request body" assertion shape to mirror
    - 141.1-RESEARCH.md § "D-17 — the two silent arms" — the lastCountingStatus hoist mechanics (do NOT hoist `res` — that lengthens the H2 body-leak lifetime)
  </read_first>
  <behavior>
    - SC-M: with a counting 503 on attempt 1 (retriesOverride: 1), `console.error` (spied) is called once on the retry-and-continue branch with a message containing the budget key, the numeric status, and the attempt index.
    - SC-M (arm 2): when attempt 1's failure is the threshold-th and the pre-attempt-2 re-check throws CircuitOpenError, a log line names the discarded attempt-1 status.
    - SC-M′ (negative, security-critical): every string passed to console.error across these cases contains NEITHER the request body NOR any header value — drive the call with a sentinel body (e.g. "SENTINEL_BODY_XYZ") and sentinel header values for Authorization / X-Service-Key / X-User-Access-Token, and assert no logged string contains any sentinel.
    - G2: a breaker lock payload whose span encodes to > (BREAKER_COOLDOWN_S + BREAKER_LOCK_TOMBSTONE_S) * 1000 (e.g. 1e17) decodes to null; span <= 0 decodes to null; a legitimate span still decodes.
  </behavior>
  <action>
    Counting arm: add a `console.error` on the `!lastAttempt && !hasContractualWait(res)` retry
    branch interpolating ONLY `budgetKey`, `res.status`, and the attempt index — follow the
    transport arm's docblock discipline verbatim (no path, no requestInit, no headers, no body;
    if an error object is ever rendered, `scrubSeamError(err, credentialHeaderValues(requestInit))`
    with BOTH arguments). Keep the message register distinct from the SeamConfigError "CONFIG
    fault" sentences.

    CircuitOpenError arm: `res` is out of scope at the re-check — hoist a
    `let lastCountingStatus: number | undefined` above the loop, set it in the counting arm, and
    include it in a `console.error` beside the pre-attempt-2 throw ("discarding attempt 1's
    <status> — breaker opened between attempts"). Do NOT hoist `res` itself.

    G2: in `decodeBreakerLock`, return null when the decoded span is <= 0 or exceeds
    `(BREAKER_COOLDOWN_S + BREAKER_LOCK_TOMBSTONE_S) * 1000` (the TODOS.md:365 prescription).
    Fail-open doctrine: null means "no lock", consistent with A-25. Add a docblock line citing
    LO-02/TS-39 by ID. Comment hygiene: `resilient-fetch.ts` is on `SEAM_CITATION_SURFACE` — every new or
    rewritten comment uses SYMBOL ANCHORS, never `file.ext:NN`; do not copy the line
    coordinates from this plan's prose into code comments.

    Tests in the retry harness (console.error via `vi.spyOn` — the harness already silences it in
    places; assert on the spy's calls). G2's regression test goes wherever the existing
    decodeBreakerLock/lock-decode tests live (check `resilient-fetch.test.ts` first; create the
    case beside its siblings).
  </action>
  <verify>
    <automated>npx vitest run src/lib/resilient-fetch.retry.test.ts src/lib/resilient-fetch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-M): delete the new counting-arm console.error → the spy assertion is RED; revert → GREEN. Paste the failing assertion.
    - MUTATION (SC-M arm 2): delete the CircuitOpenError-side log → RED; revert → GREEN.
    - MUTATION (SC-M′): temporarily interpolate `JSON.stringify(requestInit)` into the new log line → the sentinel-absence assertion is RED; revert → GREEN. This is the highest-probability security defect in the phase — observe it, do not assume it.
    - MUTATION (G2): remove the span upper bound from decodeBreakerLock → the 1e17 case is RED (decodes to a lock instead of null); revert → GREEN.
    - Existing breaker tests (SC4a/SC4b, cooldown, tombstone) all stay GREEN — the bounds change no legitimate decode.
  </acceptance_criteria>
  <done>Both silent arms log (credential-clean, mutation-proven both polarities); G2 discharged with a regression test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-12 deadline/identity + D-13 sleep→re-check ordering (SC-B, SC-D)</name>
  <files>src/lib/resilient-fetch.retry.test.ts</files>
  <read_first>
    - src/lib/resilient-fetch.ts — the loop: per-attempt deadline creation (~:2041, inside the loop — the thing SC-B pins), `sleep(...)` then `isBreakerOpen` re-check ordering (~:2019/:2025)
    - src/lib/resilient-fetch.retry.test.ts — the SC4b skeleton (~:463-496, "attempt 1 trips it, the re-check aborts attempt 2"), fake timers usage (~:395-399), `driveFailures` (~:273-288), `seedBreakerOpen` and the fake store (`shared.store`), `shared.mode.staleReadOnce` (~:36, ~:138-141 — built for D-13, currently unused)
    - 141.1-VALIDATION.md rows SC-B and SC-D — the exact mutations
  </read_first>
  <behavior>
    - SC-B (signal freshness): two-attempt run (503 then 200, retriesOverride: 1) → `calls[0][1].signal !== calls[1][1].signal`.
    - SC-B (fresh budget): with fake timers, attempt 1 times out at its full `timeoutMsOverride`; after the backoff, attempt 2 is still live just under a FULL second budget (advance to attempt-1-deadline + backoff + budget − ε → not aborted; the retry is not a no-op for the timeout class).
    - SC-B (attempt-2 identity, D7): attempt 2's URL strictly equals attempt 1's, and its init minus `signal` deep-equals attempt 1's init minus `signal` (same method, Authorization, X-Service-Key, tenant headers, body).
    - SC-D (ordering): breaker CLOSED at entry; attempt 1 returns a counting 503; the breaker OPENS DURING the 250ms backoff (fake timers, jitter pinned via Math.random → 0; flip the store to open at +100ms — via seedBreakerOpen/direct store write scheduled with setTimeout, or via `shared.mode.staleReadOnce` if the interleaving is cleaner) → the post-sleep re-check sees OPEN → CircuitOpenError, exactly 1 fetch.
  </behavior>
  <action>
    Add the cases to `resilient-fetch.retry.test.ts` using the existing harness only (no new
    doubles; `statusSequenceFetch` for SC-B, fake timers + Math.random spy for determinism).
    For the identity assertion, compare with an omit-signal helper local to the test (hand-rolled
    destructure, not a new dependency). Timing literals (250, budget values) are hand-typed in
    the tests, never imported from the module under test.

    For SC-D the acceptance criterion IS the discriminator: whatever interleaving you use, the
    swap mutation below must flip the outcome. If the first mechanics attempt does not
    discriminate (both orders pass), rework the interleaving until the swap is observably RED —
    do not keep a test that stays green under the mutation.
  </action>
  <verify>
    <automated>npx vitest run src/lib/resilient-fetch.retry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-B — the second headline, measured 125-green today): hoist `const deadline = AbortSignal.timeout(timeoutMs)` (and its signal wiring) above the `for` loop in resilient-fetch.ts → the signal-inequality case AND the fresh-budget fake-timer case are RED; revert → GREEN. Paste the failing assertions.
    - MUTATION (SC-D — measured 21/21-green today): swap the `sleep(...)` call and the pre-attempt-2 `isBreakerOpen` re-check → the SC-D case is RED (2 fetches / no CircuitOpenError); revert → GREEN. Paste the failing assertion.
    - MUTATION (identity): temporarily drop one header from attempt 2's request construction (e.g. mutate the loop to rebuild init without Authorization) → the identity case is RED; revert → GREEN.
    - `npx vitest run src/lib/resilient-fetch.retry.test.ts` exit 0 at the final tree.
  </acceptance_criteria>
  <done>The deadline-hoist and order-swap mutations — both measured green today — are observed RED; attempt-2 identity closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-10 disagreement test + D-14d bridge at production config (SC-A, SC-O)</name>
  <files>src/lib/resilient-fetch.retry.test.ts</files>
  <read_first>
    - src/lib/resilient-fetch.retry.test.ts — the client-level wiring tests from 141-04 (postProcessKey / analytics-client driven with fetch-only mock), `throwAlways` (~:254-258), `throwThenJsonOk` (~:237-251)
    - src/lib/process-key-client.ts + src/lib/analytics-client.ts — the two `retriesOverride:` registry-lookup lines (the belt whose independence SC-A pins)
    - src/lib/seam-retry-registry.ts — `RETRY_SAFE_ANALYTICS.bridge` (the SC-O entry)
    - 141.1-RESEARCH.md § "D-10 — the disagreement test" — mechanics incl. the SEAM_BUDGETS in-test mutation + restore, and the post-D-08 meaning note
  </read_first>
  <behavior>
    - SC-A: force `SEAM_BUDGETS["process-key-sync"].retries = 1` in-test (row and registry now in genuine CONFLICT — the row says retry, the registry says no; restore in afterEach), call `postProcessKey` with `flow_type: "teaser"` against a persistent transient (`throwAlways`) → exactly 1 fetch. The oracle reads the row and the registry from DIFFERENT sources by construction — that conflict is the entire point (558 tests stayed green because row and registry mirrored each other).
    - SC-O: drive `findReplacementCandidates` (the real analytics chokepoint, production configuration — the client passes the registry's bridge verdict) against a transient-then-OK fetch → exactly 2 fetches, resolves ok.
  </behavior>
  <action>
    Add both cases to `resilient-fetch.retry.test.ts`. SC-A's comment is written against the
    post-D-08 world: the row CANNOT turn retry on structurally any more, so this test is the belt
    that reddens if anyone re-adds the row fallback or swaps the client's registry lookup for a
    row read. `SEAM_BUDGETS` is an exported non-frozen literal — mutate the row after
    `await import("./resilient-fetch")`, restore explicitly in afterEach (vi.resetModules bounds
    leakage, restore anyway). SC-O goes through the REAL `findReplacementCandidates` export of
    analytics-client (mock fetch only, never the client), asserting the hand-typed literal 2.
  </action>
  <verify>
    <automated>npx vitest run src/lib/resilient-fetch.retry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-A — THE headline, measured 558-green today): in `process-key-client.ts`, replace `RETRY_SAFE_FLOW_TYPES[args.flow_type]?.retries ?? 0` with `(SEAM_BUDGETS[budgetKeyFor(args.flow_type)].retries as 0 | 1)` (re-introducing row delegation; add the import the mutation needs) → the disagreement test is RED (teaser retries, 2 fetches); revert fully → GREEN. Additionally record: outright DELETING the retriesOverride line no longer typechecks post-D-08 (paste the tsc error) — the construction-level closure SC-A used to lack.
    - MUTATION (SC-O): delete the `bridge` entry from `RETRY_SAFE_ANALYTICS` → the bridge-at-production-config case is RED (1 fetch); revert → GREEN. Paste the failing assertion.
    - afterEach restores `SEAM_BUDGETS["process-key-sync"].retries` to 0 — assert it in the test teardown (source assertion: the restore line exists).
    - `grep -rn "MUTANT" src/` → 0 at the final tree.
  </acceptance_criteria>
  <done>The 558-green measured fact is dead: row/registry disagreement reddens; bridge's production configuration is exercised through the real chokepoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seam request → logs | the seam carries raw exchange credentials + INTERNAL_API_TOKEN; new log lines cross this boundary |
| Upstash lock payload → Retry-After | a stored lock span becomes a caller-visible header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.1-13 | Information disclosure (V7 — THE live one) | new console.error lines | mitigate | interpolate budgetKey + status + attempt only; SC-M′ NEGATIVE sentinel assertion observed RED under the leak mutation; `scrubSeamError` with BOTH args if any error is rendered (the :2099-2103 JWT incident is the record) |
| T-141.1-14 | DoS / Tampering | decodeBreakerLock span | mitigate | bound span to (COOLDOWN + TOMBSTONE)×1000, reject <= 0 → null (fail-open per A-25); regression test at 1e17 |
| T-141.1-15 | DoS (retry no-op / amplification) | per-attempt deadline + re-check ordering | mitigate | SC-B and SC-D pin the two silent-severe mutations |
| T-141.1-SC | Tampering | package installs | accept | zero package installs in this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/resilient-fetch.retry.test.ts src/lib/resilient-fetch.test.ts` — green
- `npm run typecheck && npm run lint` — clean
- ALL six named mutations observed RED then reverted GREEN, evidence pasted (SC-A and SC-B especially — they are the phase's reason for existing)
</verification>

<success_criteria>
- SC-A, SC-B, SC-D, SC-M, SC-M′, SC-O all Observed with pasted evidence
- G2/LO-02 discharged in code with a regression test
- No new log line can carry a credential (negative asserted, not assumed)
</success_criteria>

<output>
Create `.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-06-SUMMARY.md` when done. Paste RED evidence for every mutation; flag the SC-A/SC-B rows in 141.1-VALIDATION.md as Observed.
</output>
