---
phase: 141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seam-retry-registry.ts
  - src/lib/seam-retry-registry.test.ts
  - src/lib/seam-citations.invariant.test.ts
autonomous: true
requirements: [SEAM-05]
tags: [seam-retry-registry, evidence, citations, audit-artifact, SEAM-05]

must_haves:
  truths:
    - "D-04: no analytics YES evidence except optimize-weights claims 'no persisted server-side write'; each of the other three states its actual traced writes AND why the retry is still safe; runPortfolioOptimizer and findReplacementCandidates descriptions match their OWN handlers"
    - "D-05: resync evidence no longer claims the SEQUENTIAL class is closed (it is open for the 15s-timeout sub-case); onboard evidence records WHY wizard_session_id is guaranteed non-NULL; both carry the 3-indexed-status qualification"
    - "D-06: no evidence string contains a bare file:line citation — indexes are cited by constraint NAME; a planted rot is caught by a registry-local guard and a symbol anchor is not"
    - "D-03: all four analytics YES entries record the doubled-compute cost (nothing awaits request.is_disconnected())"
    - "No verdict changes: YES/NO sets and all retries values are byte-identical"
  artifacts:
    - path: "src/lib/seam-retry-registry.ts"
      provides: "13 re-derived evidence strings, symbol-anchored, verdicts unchanged"
    - path: "src/lib/seam-retry-registry.test.ts"
      provides: "content pins (banned string, required anchors, D-03 cost), the registry-local citation guard with both polarities, and the 13-string anti-vacuity count"
    - path: "src/lib/seam-citations.invariant.test.ts"
      provides: "roster grown to 35 (registry appended) with pin/title/docblock bumped; citationsIn needle made reusable"
  key_links:
    - from: "src/lib/seam-retry-registry.test.ts"
      to: "src/lib/seam-citations.invariant.test.ts"
      via: "reused CITE needle (citationsIn) applied to evidence VALUES"
      pattern: "citationsIn"
---

<objective>
Repair the SEAM-05 deliverable's defective evidence: re-derive all 13 registry evidence strings
from their traced handlers (the tracing is already done in 141.1-RESEARCH.md — copy findings, do
not re-derive), convert rotted `file:line` citations to symbol anchors, and close the rot class
with two guards: the roster append (comments) plus a registry-local guard over the evidence
STRING LITERALS — the half that actually catches today's rot.

Purpose: discharges D-03 (registry half), D-04, D-05, D-06 (buckets B1–B8). The registry's whole
premise is "the evidence IS the entry"; wrong evidence is a defective deliverable.
Output: corrected registry + content pins (SC-G, SC-G′) + both citation guards (SC-H, SC-H′).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-CONTEXT.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-RESEARCH.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-PATTERNS.md

⚠️ HARD FENCES: no verdict changes; no `SEAM_BUDGETS` row un-flipped; do not re-litigate the
RULED OUT table in 141-REVIEW-CONSOLIDATED.md; zero Python changes (this plan READS Python,
WRITES TypeScript only).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive all 13 evidence strings from traced source (D-03, D-04, D-05, D-06 anchors)</name>
  <files>src/lib/seam-retry-registry.ts</files>
  <read_first>
    - src/lib/seam-retry-registry.ts — all four maps and all 13 current evidence strings (the file under repair)
    - 141.1-RESEARCH.md § "SEAM-05 evidence re-derivation — ANALYTICS (D-04)" and § "— FLOWS (D-05)" and § "D-06's citation corrections, verified" and the symbol-anchor conversion table (§ after "Both-polarity") — EVERY claim is pre-traced there with file+symbol; the executor's job is transcription into evidence prose, not re-derivation
    - analytics-service/routers/portfolio.py — `portfolio_optimizer` (~:1636, UPDATE ~:1837-1839, audit ~:1863) and `portfolio_bridge` (~:1898, audits ~:2047/:2123/:2167) — spot-check the traced claims against the live handler
    - analytics-service/routers/simulator.py — `portfolio_simulator` (~:233, audits ~:442/:490)
    - analytics-service/routers/process_key.py — resync pre-check (~:1432-1440), onboard gate (~:1033-1035)
    - src/app/api/keys/validate-and-encrypt/route.ts — the SECOND `onboard` producer (`resilientFetch("process-key-unified-dormant", ...)` with a hand-written flow_type; the same call site C1 cites) — trace what it actually sends
  </read_first>
  <action>
    Rewrite the evidence strings, verdicts and keys UNCHANGED:

    ANALYTICS YES (D-04):
    - `optimize-weights` / optimizeScenarioWeights: current "pure compute, no persisted write" claim is
      ACCURATE — keep it; it is the ONLY entry allowed to carry it.
    - `portfolio-optimizer` / runPortfolioOptimizer: state the TWO writes — the single-column
      whole-value UPDATE of `portfolio_analytics.optimizer_suggestions` keyed by row id from a
      deterministic scorer (idempotent by construction: same bytes, same row, no `+=`/append/counter;
      name the H-0573 residual: a new COMPLETE row between attempts retargets the newer id, still
      whole-value), and the append-only `optimizer_run` audit row (no counting consumer — established
      RULED OUT finding, cite it as such). Fix the B6 errors: it takes a third `weights` input,
      raises 404 on missing/unowned portfolio (does NOT return null); "returns null on degenerate
      input" belongs to optimize-weights.
    - `bridge` / findReplacementCandidates: three `bridge_run` audit appends (exactly one fires per
      request → a retry appends exactly one extra row; no counting consumer); NOT weighted-covariance
      — a composite of three deltas (Sharpe / avg-correlation / max-drawdown, weights 0.4/0.3/0.3);
      inputs are READ FROM SUPABASE by the router (portfolio_strategies weights + strategy_analytics
      series), not caller-supplied.
    - `simulator` / simulateAddCandidate: two `simulator_run` audit appends; the failure-path row
      cannot be doubled by a seam retry (it rides a retryable=False 500 — SERVICE-PERMANENT, never
      counted or retried); only the happy-path row can double, and only when attempt 1 timed out
      after the server committed.
    - ALL FOUR carry the D-03 cross-cutting cost sentence: a retry adds a second concurrent full
      compute while attempt 1 still burns CPU, because nothing on the FastAPI side awaits
      `request.is_disconnected()` — a known ACCEPTED consequence (server-side cancellation deferred).

    FLOWS (D-05):
    - `resync`: DELETE the "SEQUENTIAL-retry class is closed" sentence. State: the class is OPEN for
      the 15s-timeout sub-case — the worker's 30s tick advances SV#1 out of `draft` with ~40%
      probability inside the blip, the `status='draft'` pre-check misses, and a second draft row is
      inserted; still exactly one compute job (the partial unique index holds), not user-visible,
      self-heals on the next resync. Keep the existing concurrent-tab-race residual sentence.
    - `onboard`: replace the conditional-stated-as-fact with the guarantee's PROVENANCE: the
      `isUuid` 400 gate in create-with-key (before the RPC), the two stamping RPCs, and the 7-day
      draft purge `cleanup_abandoned_wizard_drafts` — so a future NULL/non-uuid producer visibly
      trips this recorded reasoning.
      ⚠️ TWO PRODUCERS (founder-surfaced, grep-verified): `onboard` is emitted by BOTH
      `strategies/finalize-wizard` (via `postProcessKey` — the path the wizard reasoning above
      was derived from) AND `keys/validate-and-encrypt` (via
      `resilientFetch("process-key-unified-dormant", ...)`). Trace the second producer's
      emission and make the evidence state WHICH PRODUCERS it covers. If the
      wizard_session_id guarantee holds on both paths, say so and name both. If it holds only
      for the wizard path, the evidence must state why the second producer is nonetheless
      retry-safe — e.g. it lands on the `process-key-unified-dormant` budget row pinned at
      retries 0 by `EXPECTED_RETRIES` (and gains an explicit `retriesOverride: 0` with D-08 in
      this same PR), so no retry can occur on that path. Record dormancy as a DATED,
      FALSIFIABLE fact anchored to the pins that redden if it changes — "dormant" rots
      exactly like a line number, which is this phase's whole thesis. Verdict unchanged:
      onboard stays YES; do NOT re-open the RULED OUT CR-02 row (this is about which producers
      the EVIDENCE covers, not whether the verdict is right).
    - BOTH: qualify "exactly one SV row and one job" as holding only while the job is in the 3
      indexed statuses (`pending`, `running`, `done_pending_children`) — `failed_retry` is outside
      the index predicate.

    CITATIONS (D-06): convert every coordinate citation to a symbol anchor per the RESEARCH
    conversion table — e.g. "the idempotent_by_session pre-check in process_key.py",
    "_resume_duplicate_job in process_key.py", "the teaser uuid4 mint in process_key.py". Cite the
    indexes by CONSTRAINT NAME (`compute_jobs_one_inflight_per_kind_strategy`,
    `strategy_verifications_strategy_wizard_session_uniq`), never by migration id — the cited
    migration id `20260418194206` is WRONG (it creates the allocator index). Zero `file.ext:NN`
    tokens may remain in any evidence string.
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-retry-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "no persisted server-side write" src/lib/seam-retry-registry.ts` → exactly 1 (the optimize-weights entry).
    - `grep -c "SEQUENTIAL-retry class is closed" src/lib/seam-retry-registry.ts` → 0.
    - `grep -c "20260418194206" src/lib/seam-retry-registry.ts` → 0; `grep -c "compute_jobs_one_inflight_per_kind_strategy" src/lib/seam-retry-registry.ts` ≥ 2.
    - `grep -Ec "\.(ts|py|sql|tsx)\s*:\s*[0-9]" src/lib/seam-retry-registry.ts` → 0 on evidence lines (no coordinate citations survive).
    - Verdict invariance: the existing `seam-retry-registry.test.ts` key-set pins (YES/NO sets, YES∩NO=∅) all stay GREEN, and `git diff src/lib/seam-retry-registry.ts` shows no `retries:` value changed.
  </acceptance_criteria>
  <done>All 13 evidence strings state their actual traced findings with symbol anchors; verdicts and keys byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Content pins — ban the false claim, require the anchors (SC-G, SC-G′, D-03 pin)</name>
  <files>src/lib/seam-retry-registry.test.ts</files>
  <read_first>
    - src/lib/seam-retry-registry.test.ts — the `match-recompute` PROCESS-LOCAL content pin (~:135-143, the exact analog shape) and the oracle-independence docblock (~:11-30)
    - 141.1-RESEARCH.md § Validation Architecture rows D-03, D-04, D-05
  </read_first>
  <behavior>
    - SC-G: for each analytics YES key except `optimize-weights` (hand-typed list: `["bridge", "portfolio-optimizer", "simulator"]`), `expect(RETRY_SAFE_ANALYTICS[key].evidence).not.toContain("no persisted server-side write")`.
    - SC-G′ resync: evidence does NOT match `/class is closed/i` AND matches `/pending.*running.*done_pending_children/s`.
    - SC-G′ onboard: evidence matches `/isUuid/`, `/cleanup_abandoned_wizard_drafts/`, and `/strategy_verifications_strategy_wizard_session_uniq/`.
    - SC-G′ onboard (producers): evidence matches BOTH `/finalize-wizard/` and `/validate-and-encrypt/` — the two onboard producers; evidence derived from one member of a class of two is the instance-not-class shape this phase repairs.
    - D-03: each of the 4 analytics YES entries matches `/concurrent|is_disconnected|doubled/i`.
  </behavior>
  <action>
    Add the assertions above in the shape of the existing `match-recompute` pin: hand-typed key
    lists on the EXPECTED side (never `Object.keys` of the map under test — the file's own
    oracle-independence docblock is the rule), one `it` per property with a fail-loud message
    naming what the drift means (a future engineer extending the allowlist by reasoning from a
    reverted-false claim). Comment each pin with the D-NN it discharges.
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-retry-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-G): re-insert the string "no persisted server-side write" into the `bridge` evidence → RED; revert → GREEN. Paste the failing assertion.
    - MUTATION (SC-G′): delete the `done_pending_children` qualification from resync's evidence → RED; delete `isUuid` from onboard's evidence → RED; revert both → GREEN.
    - MUTATION (SC-G′ producers): delete the `validate-and-encrypt` mention from onboard's evidence → RED; revert → GREEN.
    - MUTATION (D-03): delete the doubled-compute sentence from `simulator`'s evidence → RED; revert → GREEN.
    - All expected keys are hand-typed literals in the test (grep the new `it` blocks for `Object.keys(RETRY_SAFE` → 0 hits on the EXPECTED side).
  </acceptance_criteria>
  <done>Every re-derived claim is pinned so deleting it reddens; the banned false claim cannot silently return.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Both citation guards — roster append + registry-local evidence guard (SC-H, SC-H′)</name>
  <files>src/lib/seam-citations.invariant.test.ts, src/lib/seam-retry-registry.test.ts</files>
  <read_first>
    - src/lib/seam-citations.invariant.test.ts — `SEAM_CITATION_SURFACE` (exported, ~:104-142), the CITE regex + `citationsIn` (module-private, ~:179-206), the roster-length pin `.toBe(34)` (~:263) and its message, the both-polarity self-tests (~:349-392) including the `-1` string-literal negative (~:369-375) that PROVES the roster append alone is vacuous here
    - src/lib/seam-retry-registry.test.ts — where the new guard lands (beside the Task-2 pins)
    - 141.1-RESEARCH.md § "D-06 — the guard gap" — the full mechanism and costs
  </read_first>
  <behavior>
    - Registry-local guard: iterating all 13 evidence strings across ALL FOUR maps — ⚠️ TWO VALUE
      SHAPES: the two YES maps hold `RetrySafeEntry` objects (scan `entry.evidence`); the two NO
      maps are `Partial<Record<K, string>>` where the VALUE **is** the string (scan the value
      itself). A naive `.evidence` access on the NO maps yields `undefined` for 7 of 13 strings
      and `citationsIn(undefined)` would silently pass — the guard MUST handle both shapes
      explicitly, and the 13-count must count actual non-empty STRINGS scanned, not map entries.
      `citationsIn(text)` returns `[]` for each.
    - Anti-vacuity: the number of evidence strings scanned is exactly 13 (hand-typed: 2 flow YES + 2 flow NO + 4 analytics YES + 5 analytics NO) — emptying a map reddens.
    - +1 polarity: `citationsIn('planted foo.ts:12 rot')` returns a non-empty array (the needle works on evidence-shaped text).
    - −1 polarity: `citationsIn('resolved by _resume_duplicate_job in process_key.py — no coordinate')` returns `[]` (symbol anchors pass).
  </behavior>
  <action>
    (a) Make the needle reusable: export `citationsIn` (and the CITE regex if needed) from
    `seam-citations.invariant.test.ts`, following the established `SEAM_CITATION_SURFACE` export
    precedent. ⚠️ If importing the test file from `seam-retry-registry.test.ts` causes vitest to
    register that file's tests twice (verify by running BOTH files and checking test counts are
    unchanged), fall back to hoisting CITE + `citationsIn` into a small non-test module (e.g.
    `src/lib/seam-citations-needle.ts`) imported by both — never write a second regex (17 unrewired
    `stripComments` copies already exist; do not add an analog).

    (b) Roster append: add `src/lib/seam-retry-registry.ts` to `SEAM_CITATION_SURFACE`; bump the
    `.toBe(34)` pin to 35, the test title "exactly 34 files" to 35, and the docblock's "34 ratified
    seam files" (with one sentence on why the registry joined). The comment-lines floor (`>= 6500`)
    needs NO change. This half guards future COMMENTS in the registry; it finds zero offences today
    — that is expected, not sufficient, which is why (c) exists.

    (c) Registry-local evidence guard in `seam-retry-registry.test.ts`: implement the behaviors
    above. Message register: name the failure ("a coordinate citation in evidence prose rots
    silently — anchor the symbol, not the address") and the remedy per the sibling guard's
    conversion protocol. The 13-count is a hand-typed literal per map (4+5+2+2), never derived
    from the maps.
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-citations.invariant.test.ts src/lib/seam-retry-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-H, YES-map): plant `"see foo.ts:12"` inside the `bridge` evidence string → the registry-local guard is RED; revert → GREEN. Paste the failing assertion.
    - MUTATION (SC-H, NO-map — second member of the class, REQUIRED): plant `"see foo.ts:12"` inside the bare string value `RETRY_AUDIT_NO_ANALYTICS["portfolio-analytics"]` → the guard is RED; revert → GREEN. The YES-map mutation alone CANNOT detect a guard that only scans `.evidence` fields (half-vacuous on 7 of 13 strings — and that half is where today's load-bearing rot lives: the `resilient-fetch.ts:502` citation sits in this very NO-map string). BOTH mutations must be observed RED.
    - MUTATION (SC-H′): plant a `// see foo.ts:12` COMMENT in `seam-retry-registry.ts` → the surface `it.each` in `seam-citations.invariant.test.ts` is RED; revert → GREEN.
    - MUTATION (anti-vacuity): comment out one analytics NO entry → the 13-count assertion is RED; revert → GREEN.
    - Negative polarity observed GREEN: a symbol-anchored evidence string produces no offence.
    - `npx vitest run src/lib/seam-citations.invariant.test.ts` passes with the roster at 35 and its own +/− self-tests intact; total test count across both files matches expectation (no duplicate registration from the cross-import).
  </acceptance_criteria>
  <done>Comment rot AND evidence-string rot in the registry both redden; both polarities proven; roster pin at 35.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit artifact → future engineer | evidence strings are the reasoning surface a future allowlist extension trusts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.1-04 | Tampering (drift) | `seam-retry-registry.ts` evidence | mitigate | content pins (SC-G/G′) ban the false claim and require the load-bearing anchors; citation guards (SC-H/H′) stop coordinate rot in both comments and string literals |
| T-141.1-05 | Repudiation | audit verdicts | mitigate | verdict-invariance criteria: key-set pins stay green, `retries:` values byte-identical — evidence repair cannot smuggle a verdict change |
| T-141.1-SC | Tampering | package installs | accept | zero package installs in this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/seam-retry-registry.test.ts src/lib/seam-citations.invariant.test.ts` — green
- `npm run typecheck` — 0 errors
- All named mutations observed RED then reverted GREEN (evidence pasted in SUMMARY)
- `git diff --stat` — zero `.py` files; `seam-retry-registry.ts` diff touches only evidence/notes strings and comments
</verification>

<success_criteria>
- 13 evidence strings re-derived, symbol-anchored, verdicts unchanged
- SC-G, SC-G′, SC-H, SC-H′ all observed
- The rot class is closed by TWO guards (comments + string literals), extending existing mechanisms — no parallel regex
</success_criteria>

<output>
Create `.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-02-SUMMARY.md` when done. Include pasted RED evidence for SC-G, SC-G′, SC-H, SC-H′.
</output>
