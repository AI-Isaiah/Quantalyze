---
phase: 141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration
plan: 08
type: execute
wave: 4
depends_on: ["141.1-06"]
files_modified:
  - src/app/api/cron/flag-monitor/route.ts
  - tests/integration/cron-flag-monitor.test.ts
  - src/lib/resilient-fetch.ts
  - src/lib/resilient-fetch.retry.test.ts
autonomous: true
requirements: [SEAM-06]
tags: [flag-monitor, sentry, error-rate, observability, alert-bias]

must_haves:
  truths:
    - "D-16: OQ-1 is RESOLVED WITH EVIDENCE before any remedy is chosen — the probe determined whether the Sentry numerator (`path:/api/process-key`) has ever been able to match, and the finding is recorded"
    - "D-16: the dead `path:/api/process-key` filter (a route that does not exist) is gone from the numerator query, and a test pins its absence (SC-N)"
    - "D-16: the retry-suppression direction is closed or neutralised — a transient failure that recovers on attempt 2 is no longer invisible to the repo's only error-rate alert (numerator widening via a capture on the retried-recovered path, unless the probe finding dictates a numerator repair instead)"
    - "The distinct-correlation-id denominator correction is explicitly HANDED OFF for TODOS booking (plan 09), not silently dropped"
  artifacts:
    - path: "src/app/api/cron/flag-monitor/route.ts"
      provides: "corrected numerator query (and any threshold/tag changes the probe finding dictates)"
    - path: "tests/integration/cron-flag-monitor.test.ts"
      provides: "SC-N pin (query must not contain the dead path filter) + coverage of the chosen remedy"
  key_links:
    - from: "src/app/api/cron/flag-monitor/route.ts getNumerator"
      to: "Sentry events API query string"
      via: "URLSearchParams `query` value"
      pattern: "level:"
---

<objective>
Correct the flag-monitor bias D-16 names: the repo's ONLY error-rate alert filters on
`path:/api/process-key` (a route that does not exist under `src/app/api/`), and retries now
suppress the alert precisely during the degradation that triggers them. The remedy SHAPE depends
on OQ-1 — whether the numerator has ever fired — so the probe is the gating first task, per the
phase's plan-time obligation.

Purpose: discharges D-16 (F1, F5). SC-N.
Output: probe finding + the mechanically-correct fix + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-CONTEXT.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-RESEARCH.md

CONTEXT.md leaves the remedy shape to Claude's discretion — "trace the actual query before
deciding". RESEARCH § "D-16 — the real flag-monitor query" pre-traces the three candidate
remedies and recommends: F5 fix + numerator widening via a warning-level capture on the
retried-and-recovered path; threshold recalibration is REJECTED (calibrating against an
unmeasured retry success rate is guessing). The Sentry MCP is READ-ONLY (org metaworld-fund-ltd,
de.sentry.io); events LAG deploys.
</context>

<tasks>

<task type="auto">
  <name>Task 1: OQ-1 — probe whether the numerator has ever been able to fire</name>
  <files>(no source changes — evidence-gathering; findings recorded in the SUMMARY)</files>
  <read_first>
    - scripts/probe-sentry-events-api.sh — the probe the route's own docblock says verified the API shape pre-deploy
    - src/app/api/cron/flag-monitor/route.ts — `getNumerator` (~:88-98: `level:error path:/api/process-key correlation_id:* environment:production`), `getDenominator` (~:159-165), thresholds (~:49-52)
    - 141.1-RESEARCH.md § OQ-1 — what is known (no such route; nothing sets a Sentry `path` tag) and what is unclear (Sentry's `path:` field semantics)
  </read_first>
  <action>
    Run the probe (read-only) twice against the live Sentry events API: once with the CURRENT
    query verbatim, once with `path:/api/process-key` removed — over the widest statsPeriod the
    endpoint allows, ideally covering a window with known `/process-key` errors. Compare counts.
    Decide and record ONE of:
    (a) NUMERATOR ALIVE (path: matches events) → the filter is merely stale; proceed with the
    recommended remedy (Task 2 default).
    (b) NUMERATOR STRUCTURALLY DEAD (with-path count is 0 while without-path count is > 0, or the
    field is unindexed) → "retries suppress the alert" is moot because the alert never fired;
    Task 2 becomes a numerator REPAIR: build the query from fields that demonstrably match
    process-key errors (e.g. `level:error correlation_id:* environment:production` plus a
    discriminating tag that EXISTS — verify candidate tags against real events via the probe).
    If Sentry is unreachable, record that, land only the mechanical F5 fix in Task 2, and hand
    the rest to the TODOS booking. Do not guess: the SUMMARY carries the raw probe output.
  </action>
  <verify>
    <automated>bash scripts/probe-sentry-events-api.sh 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - Raw probe output for BOTH query variants pasted in the SUMMARY (counts, not paraphrase).
    - An explicit written verdict: ALIVE / STRUCTURALLY DEAD / UNREACHABLE, with the count comparison that justifies it.
    - The Task-2 remedy branch is named before Task 2 starts.
  </acceptance_criteria>
  <done>OQ-1 resolved with pasted evidence; the remedy shape is decided on data, not assumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-16 — land the remedy (SC-N)</name>
  <files>src/app/api/cron/flag-monitor/route.ts, tests/integration/cron-flag-monitor.test.ts, src/lib/resilient-fetch.ts, src/lib/resilient-fetch.retry.test.ts</files>
  <read_first>
    - src/app/api/cron/flag-monitor/route.ts — the full handler (the change is a query-string/filter edit inside the handler body; no Next.js API surface is touched — `dynamic`/`maxDuration` already exported)
    - tests/integration/cron-flag-monitor.test.ts — the existing suite's mocking shape for the Sentry fetch
    - src/lib/resilient-fetch.ts — the counting arm (post plan-06: it now logs) — the retried-and-recovered capture site if numerator widening is chosen
    - src/lib/sentry-capture.ts — the repo's capture wrapper (NEVER `@sentry/*` directly; the retry harness resets its throttle via `__resetCaptureThrottleForTests`)
  </read_first>
  <behavior>
    - SC-N: the outbound Sentry query URL does NOT contain `path:/api/process-key` — `expect(url).not.toContain("path:/api/process-key")` against the mocked fetch's captured URL.
    - Branch (a) ALIVE / default: on a seam call where attempt 1 counted and attempt 2 SUCCEEDED, a warning-level capture is emitted via sentry-capture (message naming budgetKey + recovered status; NO path/body/header values — same V7 discipline as plan 06), and the numerator query is widened to include it (e.g. `level:[error,warning]` scoped by a dedicated tag, or the tag alone — whichever the probe showed is expressible); the integration test asserts the widened query matches the capture's shape.
    - Branch (b) DEAD: the numerator query is rebuilt per Task 1's verified fields; the integration test pins the new query string as a hand-typed literal.
  </behavior>
  <action>
    Remove `path:/api/process-key` from `getNumerator`'s query and implement the Task-1 branch.
    For the capture (branch a): site it where the loop KNOWS recovery happened — after a
    successful response when a prior attempt recorded a counting failure (the `lastCountingStatus`
    hoist from plan 06 already carries this state); use `captureMessage` at warning level through
    sentry-capture. A recovered request is NOT an error — do not use level:error (category error
    the research names). Keep the alert thresholds untouched unless branch (b)'s finding forces a
    documented change (record the reasoning if so).

    HANDOFF (do not implement): the semantically-complete denominator fix (count distinct
    `metadata->>'correlation_id'` instead of raw audit rows — needs an RPC) is booked to TODOS.md
    by plan 09; state it explicitly in this plan's SUMMARY under "handoffs" so plan 09 copies it.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/cron-flag-monitor.test.ts src/lib/resilient-fetch.retry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-N): re-add `path:/api/process-key` to the query → the SC-N pin is RED; revert → GREEN. Paste the failing assertion.
    - If branch (a): MUTATION — delete the recovered-path capture → the new capture test is RED; revert → GREEN. NEGATIVE: the captured message contains no sentinel body/header value (same sentinel technique as plan 06's SC-M′).
    - `grep -c "path:/api/process-key" src/app/api/cron/flag-monitor/route.ts` → 0.
    - No `@sentry/` direct import added (`grep -n "@sentry" src/lib/resilient-fetch.ts src/app/api/cron/flag-monitor/route.ts` shows only pre-existing hits, if any).
    - The handoff paragraph (denominator RPC follow-up) exists in the SUMMARY.
  </acceptance_criteria>
  <done>The dead filter is gone and pinned; the suppression direction is closed per the probe's verdict; the denominator follow-up is explicitly handed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron route → external Sentry API | outbound authed query; response drives a founder-facing alert |
| retry loop → Sentry capture | a new telemetry line crosses the credential-carrying seam (V7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.1-19 | Information disclosure (V7) | recovered-path capture | mitigate | budgetKey + status only; sentinel-based negative assertion; via sentry-capture wrapper, never @sentry direct |
| T-141.1-20 | Repudiation (alert suppression) | flag-monitor errorRate | mitigate | probe-verified numerator + widened/repaired query; SC-N pins the dead filter's absence |
| T-141.1-21 | Configuration (V14) | SENTRY_* / RESEND_API_KEY env reads | accept | no new env vars, no value logging; the RESEND prod gap is a known item recorded in the D-18 runbook |
| T-141.1-SC | Tampering | package installs | accept | zero package installs in this phase |
</threat_model>

<verification>
- `npx vitest run tests/integration/cron-flag-monitor.test.ts src/lib/resilient-fetch.retry.test.ts` — green
- `npm run typecheck && npm run lint` — clean
- SC-N mutation observed RED/GREEN; probe evidence pasted
</verification>

<success_criteria>
- OQ-1 resolved with raw evidence; remedy matched to the finding
- SC-N observed; dead filter gone
- No credential can reach the new telemetry line (negative asserted)
</success_criteria>

<output>
Create `.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-08-SUMMARY.md` when done. Include the probe output, the branch verdict, mutation evidence, and the explicit denominator handoff for plan 09.
</output>
