---
phase: 138-mt5ui-addkey-badge-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/security/page.tsx
  - src/app/(marketing)/security/page.test.tsx
autonomous: true
requirements: [MT5UI-01]

must_haves:
  truths:
    - "With MT5_ENABLED === 'true' (server env), /security renders a #mt5-readonly SubAnchor titled MT5 nested inside the #readonly-key section"
    - "The guide explicitly steers: use your INVESTOR (read-only) password, never your master password — and honestly states a master password is refused at connect time with nothing stored"
    - "The guide tells the user to copy the broker server string exactly from the MT5 terminal login window (region / Demo-Live suffix caveat)"
    - "With MT5_ENABLED unset or any non-exact value, the block is ABSENT — the public page is byte-identical to today (test-pinned)"
    - "Sibling anchors (#deribit-readonly, the sfox gating) are untouched"
  artifacts:
    - path: "src/app/(marketing)/security/page.tsx"
      provides: "Server-flag-gated #mt5-readonly setup guide (SubAnchor)"
      contains: "mt5-readonly"
    - path: "src/app/(marketing)/security/page.test.tsx"
      provides: "Renders-iff-MT5_ENABLED describe blocks (on / off / non-exact) + content honesty pins"
  key_links:
    - from: "src/app/(marketing)/security/page.tsx"
      to: "src/lib/closed-sets.ts"
      via: "isMt5EnabledServer() — the SERVER flag, NOT the client MT5_UI_ENABLED"
      pattern: "isMt5EnabledServer"
    - from: "ConnectKeyStep mt5 setup-guide link (/security#mt5-readonly, plan 138-01)"
      to: "src/app/(marketing)/security/page.tsx"
      via: "stable SubAnchor id (the page's anchor-stability contract)"
      pattern: "id=\"mt5-readonly\""
---

<objective>
Add the flag-gated MT5 read-only (investor-password) setup guide — a `#mt5-readonly`
SubAnchor on /security nested under `#readonly-key`, rendered ONLY when the SERVER flag
`MT5_ENABLED` is on, mirroring the shipped `#sfox-readonly` block. It is the deep-link
target of the wizard's auto-derived `/security#mt5-readonly` href (plan 138-01) and the
MT5UI-01 "Guest-Mode trust signal surfaced up front" requirement.

Purpose: the #1 MT5 user error is submitting the master password; the guide steers to the
investor password BEFORE the wizard, in muted DESIGN.md-conformant tone.
Output: gated SubAnchor + clone of the sfox describe blocks in page.test.tsx.

⚠️ SCOPE PIN — the gate `isMt5EnabledServer()` already exists (135, closed-sets.ts:151).
This plan only CALLS it, exactly as the sfox block calls isSfoxEnabledServer() at
security/page.tsx:499. Do NOT gate on the client MT5_UI_ENABLED (RESEARCH Pitfall 3: a guide
pointing at a fail-closed connect). Do NOT add a new flag or gate code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-mt5ui-addkey-badge-e2e/138-CONTEXT.md
@.planning/phases/138-mt5ui-addkey-badge-e2e/138-UI-SPEC.md
@DESIGN.md

<interfaces>
From src/app/(marketing)/security/page.tsx:2 — the import to extend:
  import { isSfoxEnabledServer } from "@/lib/closed-sets";
(add isMt5EnabledServer to the same import; the page is a pure Server Component reading
server env at render — closed-sets.ts:151 `isMt5EnabledServer(): boolean`.)

From security/page.tsx:499-529 — the sFOX gated-SubAnchor idiom to clone:
  {isSfoxEnabledServer() && (
    <SubAnchor id="sfox-readonly" title="sFOX">
      <ol className="mt-2 list-decimal space-y-1 pl-5 text-body text-text-secondary"> … </ol>
    </SubAnchor>
  )}
nested inside <Section id="readonly-key"> (:412). Anchor-stability contract noted at
page.tsx:15 — ids are load-bearing deep-link targets, never rename.

From security/page.test.tsx:199-305 — the two describe idioms to clone: the flag-ON block
sets process.env.SFOX_ENABLED = "true" in beforeEach and deletes it in afterEach (this file
uses direct process.env, NOT vi.stubEnv — the page reads env per-render, no module reset
needed); the fail-closed block pins absence when unset and it.each(["1","on","","false"])
for non-exact values, plus the siblings-unaffected case (:302-305).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: #mt5-readonly gated SubAnchor + renders-iff-flag tests</name>
  <files>src/app/(marketing)/security/page.tsx, src/app/(marketing)/security/page.test.tsx</files>
  <read_first>security/page.tsx:400-535 (the readonly-key Section, the deribit + sfox SubAnchors, the gating comment at :488-498), security/page.test.tsx:180-305 (both sfox describe blocks — clone their structure and env hygiene), 138-UI-SPEC.md Delta 3 (placement decision: INLINE page section, NOT a modal), wizardErrors.ts:227-259 (the KEY_MT5_* cause/fix copy the guide must AGREE with, not contradict)</read_first>
  <behavior>
    - MT5_ENABLED="true" → document.getElementById("mt5-readonly") exists, its heading reads "MT5", and #readonly-key contains it
    - Content pins (flag ON): matches /investor/i AND /master password/i (the steer); matches a refusal-honesty phrase (e.g. /refuse/i or /store nothing/i or /stored nothing/i — assert the exact copy written); matches /server name/i or /broker server/i exact-string guidance incl. a Demo\/Live or region caveat
    - No literal IP address in the block (reuse the :248-249 IPv4/IPv6 negative regexes — the gateway/hosting story is Phase 139, founder ops; the guide must not assert infrastructure that does not exist yet)
    - MT5_ENABLED unset → getElementById("mt5-readonly") is null; it.each("1","on","","false","TRUE") → still null (strict === "true")
    - Siblings unaffected: #deribit-readonly still present; the sfox block still gated by ITS flag (assert absent with SFOX_ENABLED unset)
  </behavior>
  <action>
    Import isMt5EnabledServer alongside isSfoxEnabledServer at page.tsx:2. Directly after the
    sfox SubAnchor's closing brace (:529), inside the same #readonly-key wrapper div, add the
    gated block: isMt5EnabledServer() && a SubAnchor id "mt5-readonly" title "MT5" containing
    an ordered list (same classes as the sfox <ol> at :501 — the page's existing muted
    text-body text-text-secondary voice; NO amber/red, NO ⚠ glyph, per the DESIGN.md
    Semantic-color gate — this is steady-state guidance, not an event).
    Write 3-4 declarative, sentence-case steps (exact wording is executor discretion within
    the UI-SPEC Delta 3 contract + DESIGN.md §Voice; keep every claim TRUE per wizardErrors
    honesty):
    (1) the steer — connect with the account's INVESTOR (read-only) password, never the
    master password; a master password can place trades, so Quantalyze refuses it at connect
    time and stores nothing (this claim is honest for MT5: read-only is SERVER-ENFORCED by
    the investor login and the 135 validate-time rejection — unlike sfox, no "we cannot
    verify" hedge is needed);
    (2) where to find it — the investor password is separate from the master password; if
    missing, ask the broker to issue or reset it (agree with the KEY_MT5_MASTER_PASSWORD fix
    copy at wizardErrors.ts:235-239);
    (3) the broker server — copy the server name EXACTLY as shown in the MT5 terminal's login
    window; it is broker-specific and often carries a region or Demo/Live suffix (agree with
    KEY_MT5_WRONG_SERVER fix copy at :252-256);
    (4) then enter login, investor password, and broker server in the wizard.
    Add a gating comment mirroring :488-498: rendered ONLY when the server go-live flag
    MT5_ENABLED is on — pre-launch there is no MT5 wizard card to point at, and the page must
    stay byte-identical to its pre-mt5 baseline; gate on the SERVER flag (backend
    availability), never the client NEXT_PUBLIC flag.
    Do NOT mention IPs, the gateway, hosting, or whitelisting (Phase 139 founder ops — not
    yet real; the no-invented-data rule).
    Tests: clone the two sfox describe blocks (:199-305) for MT5_ENABLED/#mt5-readonly with
    the behavior-block cases, using the same direct process.env set/delete hygiene (set in
    beforeEach, delete in afterEach; delete after each it.each iteration like :292-299).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(marketing)/security/page.test.tsx" --no-file-parallelism</automated>
  </verify>
  <done>#mt5-readonly renders iff MT5_ENABLED === "true"; steer + honesty + server-string content pins green; absent for unset + 5 non-exact values; siblings untouched; all pre-existing page tests green.</done>
  <acceptance_criteria>
    Renders-iff-flag proven both directions (on + absent-off + non-exact it.each); content
    steer pins /investor/i + /master password/i; no literal IP regex match; `npx tsc --noEmit`
    clean; the anchor id is exactly "mt5-readonly" (the plan-01 wizard link target).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web → /security SSR | anonymous readers; the page reads server env at render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-05 | Info disclosure | guide rendered pre-launch advertises an unavailable capability (and would coach users into a fail-closed connect) | mitigate | gate on isMt5EnabledServer() (server env, per-request) — absent-when-off is test-pinned incl. non-exact values |
| T-138-06 | Repudiation/honesty | guide over-claims (e.g. infrastructure that only exists after 139, or a false verification claim) | mitigate | copy constrained to server-enforced facts (investor-login Guest Mode + 135 refusal); no IP/gateway/hosting mentions; negative IP-regex test |
| T-138-07 | Tampering | gating on the CLIENT flag would desynchronize guide vs backend availability | mitigate | server-flag gate only (RESEARCH Pitfall 3); the client flag is never imported into this page |
| T-138-SC | Tampering | package installs | accept | no install tasks in this plan |
</threat_model>

<verification>
- `npx vitest run "src/app/(marketing)/security/page.test.tsx" --no-file-parallelism` — all green (new + pre-existing).
- `npx tsc --noEmit` — clean.
- `npm run lint` — clean.
</verification>

<success_criteria>
The wizard's /security#mt5-readonly deep-link (plan 138-01) has a real, flag-gated target
whose copy steers to the investor password in muted tone, agrees with the 135 error copy,
and leaves the public page byte-identical while MT5_ENABLED is off.
</success_criteria>

<output>
Create `.planning/phases/138-mt5ui-addkey-badge-e2e/138-02-SUMMARY.md` when done.
</output>
