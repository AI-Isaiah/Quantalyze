---
phase: 138-mt5ui-addkey-badge-e2e
plan: 03
type: execute
wave: 2
depends_on: [138-01]
files_modified:
  - src/components/strategy/ApiKeyManager.tsx
  - src/components/strategy/ApiKeyManager.test.tsx
  - src/components/exchanges/AllocatorExchangeManager.tsx
  - src/components/exchanges/AllocatorExchangeManager.test.tsx
  - e2e/helpers/seed-test-project.ts
  - e2e/mt5-badge.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [MT5UI-02]

must_haves:
  truths:
    - "A seeded api_verified MT5 strategy's badge is visible to OWNER (factsheet), ALLOCATOR (browse detail), ADMIN (non-owner factsheet), and ANON (logged-out factsheet) — proven by the seeded e2e"
    - "The owner's /strategies/[id]/edit renders the real 'MT5' mono tag for an mt5 key — never the '?' unknown-exchange fallback"
    - "e2e/mt5-badge.spec.ts is REGISTERED in the blocking e2e-seeded explicit spec list in ci.yml (which feeds the frontend aggregator gate) — an unregistered spec never runs"
    - "With MT5_ENABLED unset, the key route 400s and the worker derive branch fails permanent BEFORE any live read — confirmed by running the EXISTING 135/136 fail-closed tests (no new gate code)"
    - "The api_verified badge components are byte-identical — zero visual change, no new variant"
  artifacts:
    - path: "e2e/helpers/seed-test-project.ts"
      provides: "seedMt5VerifiedStrategy + SeededMt5VerifiedStrategy + cleanupMt5VerifiedStrategy (clone of the sfox seed trio)"
      contains: "seedMt5VerifiedStrategy"
    - path: "e2e/mt5-badge.spec.ts"
      provides: "All-roles seeded badge + tag spec (owner/allocator/admin/anon + one axe pass)"
      min_lines: 120
    - path: ".github/workflows/ci.yml"
      provides: "mt5-badge.spec.ts in the e2e-seeded explicit list"
      contains: "e2e/mt5-badge.spec.ts"
    - path: "src/components/strategy/ApiKeyManager.tsx"
      provides: "mt5 entry in the exchangeIcon mono-tag map"
      contains: "mt5"
  key_links:
    - from: "e2e/mt5-badge.spec.ts"
      to: "e2e/helpers/seed-test-project.ts"
      via: "import seedMt5VerifiedStrategy/cleanupMt5VerifiedStrategy"
      pattern: "seedMt5VerifiedStrategy"
    - from: ".github/workflows/ci.yml e2e-seeded job"
      to: "e2e/mt5-badge.spec.ts"
      via: "explicit spec list (line ~1557)"
      pattern: "e2e/mt5-badge\\.spec\\.ts"
    - from: "strategy_verifications.trust_tier (seeded 'api_verified')"
      to: "VerifiedBadge / TrustTierLabel render"
      via: "getStrategyDetail/getPublicStrategyDetail projection + get_published_trust_signals SECDEF RPC (Phase 126)"
      pattern: "data-trust-tier=\"api_verified\""
---

<objective>
Prove the api_verified trust distinction SURFACES for MT5 across all roles: a seeded
MT5-backed strategy renders the shipped badge (VerifiedBadge / TrustTierLabel — ZERO visual
change) for owner / allocator / admin / anon via a new seeded Playwright spec cloned from
sfox-badge.spec.ts, REGISTERED in the blocking e2e-seeded CI list; add the missing 'MT5'
provenance tag entries (the sfox-precedent unconditional mono tag) so the owner edit surface
never shows the '?' fallback; and confirm the MT5_ENABLED go-dark gate stays fail-closed by
RUNNING the existing 135/136 tests.

Purpose: MT5UI-02. The factsheet trust_tier projection is provenance-blind and already
proven by the passing sfox-badge spec — this plan proves MT5 rides it and wires the CI gate
so the proof actually runs.

⚠️ SCOPE PIN — already exists, DO NOT duplicate or touch:
- Badge components + TRUST_TIER_TOKENS + trust-tier-tokens.test.ts: ZERO change (UI-SPEC Delta 4).
- Server-gate fail-closed tests ALREADY LANDED (research listed them as gaps; they exist):
  TS route truth table at src/app/api/keys/validate-and-encrypt/route.test.ts:486-531
  ("fails closed … MT5_ENABLED unset" + non-exact it.each), py truth table at
  analytics-service/tests/test_ingestion_mt5.py:402-424, worker derive go-dark at
  analytics-service/tests/test_mt5_derive_branch.py (:15 header, :348 zero-live-calls pin).
  This plan RUNS them as the phase gate; it authors no new gate tests unless one of those
  commands fails to collect the named cases (fail loud, then add the missing pin only).
- DB CHECKs admit mt5 (migration 20260723172032) — the seed relies on it; NO new migration.
- MT5 stays OUT of MultiKeyConnectStep (single-key forex venue, no composite use case — an
  mt5 composite member fails loud at the worker stitch unknown-venue gate per 136-02) and
  OUT of manager ApiKeyForm / UI_EXCHANGE_CODES (explicit non-goals, CONTEXT + UI-SPEC
  §MT5-Manager-Parity). The tag-map entries below are a PROVENANCE surface (renders the
  user's own already-connected key), not an offer surface — the sfox SFOX-09 precedent ships
  them unconditionally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-mt5ui-addkey-badge-e2e/138-CONTEXT.md
@.planning/phases/138-mt5ui-addkey-badge-e2e/138-RESEARCH.md
@.planning/phases/138-mt5ui-addkey-badge-e2e/138-UI-SPEC.md

<interfaces>
From src/components/strategy/ApiKeyManager.tsx:298-311 — the mono-tag map missing mt5
(an mt5 key currently falls through to the "?" fallback the sfox spec forbids):
  const exchangeIcon: Record<string, string> = { binance: "B", okx: "O", bybit: "By",
    deribit: "DRB", sfox: "SFOX" };

From src/components/exchanges/AllocatorExchangeManager.tsx:122-137 — the styled tag map,
sfox entry (the neutral-slate no-brand-colour precedent to reuse for mt5 — NO new hex per
UI-SPEC constraint 3):
  sfox: { label: "SFOX", bg: "#F1F5F9", fg: "#0F172A" }
Fallback at :741/:828: EXCHANGE_TAGS[key.exchange] ?? generic "?" tag.

From e2e/helpers/seed-test-project.ts:
  :1046-1082 — sfox boundary-violation detector + fail-loud precondition thrower (names the
    sfox migration; the mt5 twin must name supabase/migrations/20260723172032_mt5_exchange_boundary_checks.sql)
  :1084-1093 — SeededSfoxVerifiedStrategy { owner, admin, strategyId, apiKeyId, namePrefix }
  :1130-1253 — seedSfoxVerifiedStrategy: seedTestAllocator(role:"both") owner + isAdmin user;
    api_keys insert {exchange:"sfox", api_key_encrypted:"e2e-placeholder-ciphertext"};
    published strategies insert {source:"sfox", api_key_id, supported_exchanges:["sfox"],
    benchmark:"BTC", markets:["BTC"], deterministic 120d start_date}; strategy_analytics
    insert (computation_status:"complete", deterministic sin() returns_series); 
    strategy_verifications insert {wizard_session_id: uuid, status:"validated",
    trust_tier:"api_verified", flow_type:"onboard", source:"sfox"}
  :1263-1274 — cleanupSfoxVerifiedStrategy (name-prefix strategy delete + api_key delete)

From e2e/sfox-badge.spec.ts (219 lines — the spec to clone):
  :56-58 HAS_SEED_ENV gate; :63-75 inline loginViaForm; :82-86 apiVerifiedBadge OR-locator
  (getByText("Verified",{exact}) .or [data-trust-tier="api_verified"]); :88-108 describe +
  beforeAll seed / afterAll cleanup + test.skip(!HAS_SEED_ENV) per test; five legs:
  owner edit-page tag (:110-131, asserts real tag visible AND "?" count 0), owner factsheet
  badge + ONE buildAxe pass (:133-151), allocator /browse/crypto-sma/<id> (:153-171), admin
  non-owner factsheet (:173-194), anon factsheet (:196-214); PUBLIC-DEMO deliberately not
  swept (:216-218, no-invented-data).

From .github/workflows/ci.yml:1544-1565 — the EXPLICIT seeded spec list (npx playwright test
with one spec path per line; e2e/sfox-badge.spec.ts at :1557). The e2e-seeded job is
BLOCKING and feeds the `frontend` aggregator (branch protection).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MT5 provenance mono-tag entries (SFOX-09 precedent, unconditional)</name>
  <files>src/components/strategy/ApiKeyManager.tsx, src/components/strategy/ApiKeyManager.test.tsx, src/components/exchanges/AllocatorExchangeManager.tsx, src/components/exchanges/AllocatorExchangeManager.test.tsx</files>
  <read_first>ApiKeyManager.tsx:298-311 (exchangeIcon map + the SFOX-09 comment), ApiKeyManager.test.tsx:~289 (the DRB tag test idiom), AllocatorExchangeManager.tsx:120-137 (EXCHANGE_TAGS + sfox neutral-slate rationale), AllocatorExchangeManager.test.tsx:~168 (the sfox tag test idiom)</read_first>
  <behavior>
    - ApiKeyManager renders the "MT5" tag (not "?") for a key with exchange "mt5" (clone the existing DRB/sfox tag test case)
    - AllocatorExchangeManager renders label "MT5" (not the generic "?" fallback) for exchange "mt5" (clone the :168 sfox case)
  </behavior>
  <action>
    Add `mt5: "MT5"` to ApiKeyManager's exchangeIcon map (:298-311) and
    `mt5: { label: "MT5", bg: "#F1F5F9", fg: "#0F172A" }` to AllocatorExchangeManager's
    EXCHANGE_TAGS (:122-137) — REUSE the sfox neutral slate pairing verbatim (UI-SPEC
    constraint 3: no new hex this phase; MT5 has no bright brand colour, the same rationale
    as the SFOX-09 comment at :132-135 — copy that comment style, citing this phase).
    Both entries ship UNCONDITIONALLY (independent of MT5_UI_ENABLED and MT5_ENABLED),
    mirroring SFOX-09: a connected mt5 key must render the real mono tag before the 139
    flip, and the tag renders only for the user's OWN existing key (provenance, not offer).
    Label is "MT5" — 3 chars, DM-Sans-mono convention, no emoji (DESIGN.md AI-Slop Ban).
    Extend the two component test files with one mt5 case each, cloning their existing
    DRB/sfox tag-case idioms. Touch NOTHING else in these components (Rule 3 surgical).
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/ApiKeyManager.test.tsx src/components/exchanges/AllocatorExchangeManager.test.tsx --no-file-parallelism</automated>
  </verify>
  <done>Both tag maps carry mt5; both new cases green; all pre-existing cases green; no other component change.</done>
  <acceptance_criteria>
    "MT5" tag test green in BOTH files; zero new hex introduced (git diff shows only the two
    map entries + comments + tests); `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: seedMt5VerifiedStrategy seed-helper trio</name>
  <files>e2e/helpers/seed-test-project.ts</files>
  <read_first>e2e/helpers/seed-test-project.ts:1040-1274 (the ENTIRE sfox seed section: boundary detector, thrower, iface, seed, cleanup — the clone source), :76-165 (seedTestAllocator), supabase/migrations/20260723172032_mt5_exchange_boundary_checks.sql (confirm the three widened CHECKs the seed relies on)</read_first>
  <action>
    Clone the sfox seed trio → SeededMt5VerifiedStrategy / seedMt5VerifiedStrategy /
    cleanupMt5VerifiedStrategy with exchange/source "mt5", supported_exchanges ["mt5"],
    labels/prefixes "e2e-mt5-key-…" / "e2e-mt5-verified-…".
    Fail-loud precondition: the boundary-violation path must name
    supabase/migrations/20260723172032_mt5_exchange_boundary_checks.sql (the mt5 twin of
    :1046-1082). Prefer PARAMETERIZING one shared detector/thrower pair (venue + migration
    path args) over a copy-paste twin — but the sfox call sites' emitted message must stay
    byte-identical (the sfox spec's fail-loud contract; verify with the existing suite).
    Keep verbatim from the sfox seed: the getAdmin() prod-safety path (every mutation routes
    through assertNotProductionSupabaseUrl — TEST project only), the gitleaks-safe
    "e2e-placeholder-ciphertext" idiom (never a real credential), the deterministic 120d
    sin() series (no RNG), status "published", computation_status "complete", and the
    strategy_verifications row {wizard_session_id: crypto.randomUUID(), status:"validated",
    trust_tier:"api_verified", flow_type:"onboard", source:"mt5"}.
    The benchmark/markets/strategy_types fixture values may stay identical to the sfox seed
    (BTC/spot): this seeds a BADGE-rendering proof, not an economics oracle, and the sfox
    seed deliberately omits asset_class too — do NOT invent forex analytics (no-invented-data;
    the √252 economics are 136's, already gated there).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Trio exported; type-clean; sfox seed behavior (incl. its fail-loud message) unchanged; consumed by Task 3's spec.</done>
  <acceptance_criteria>
    seedMt5VerifiedStrategy/cleanupMt5VerifiedStrategy/SeededMt5VerifiedStrategy exported;
    fail-loud message names the 20260723172032 migration; grep shows every new mutation goes
    through the shared getAdmin() client (no second client construction).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: mt5-badge.spec.ts (all roles) + blocking CI registration + go-dark gate confirmation</name>
  <files>e2e/mt5-badge.spec.ts, .github/workflows/ci.yml</files>
  <read_first>e2e/sfox-badge.spec.ts (whole file — clone structure, comments-intent, and every leg), .github/workflows/ci.yml:1544-1566 (the explicit seeded list + the else-branch invocation), src/app/api/keys/validate-and-encrypt/route.test.ts:468-531 (the EXISTING TS gate tests to run, not re-author), analytics-service/tests/test_ingestion_mt5.py:402-424 + analytics-service/tests/test_mt5_derive_branch.py:1-30 (the EXISTING py gate tests to run)</read_first>
  <action>
    Create e2e/mt5-badge.spec.ts cloning sfox-badge.spec.ts one-for-one for the mt5 seed:
    HAS_SEED_ENV skip gate (TEST_SUPABASE_URL / TEST_SUPABASE_SERVICE_ROLE_KEY — clean
    self-skip pre-secrets, visible skip not silent pass), the inline loginViaForm idiom, the
    apiVerifiedBadge OR-locator (getByText("Verified",{exact}) .or
    '[data-trust-tier="api_verified"]' — target stable attributes, never restyle), beforeAll
    seedMt5VerifiedStrategy / afterAll cleanupMt5VerifiedStrategy. Five legs, mirroring
    :110-214: (1) OWNER /strategies/[id]/edit — assert the "MT5" mono tag visible (Task 1's
    entry) AND getByText("?", {exact}) count 0, with the URL pinned to the edit surface (the
    ME-02 false-green guard); (2) OWNER factsheet /strategy/[id] — badge visible + ONE
    buildAxe() pass with zero violations (the only sanctioned axe factory); (3) ALLOCATOR
    (same "both" user) /browse/crypto-sma/[id] — badge visible (direct-id route needs no
    category membership; VerifiedBadge there is gated on strategy.api_key_id, which the seed
    links); (4) ADMIN (separate is_admin session) non-owner factsheet — badge visible (the
    Phase-126 FACTSHEET-01 SECDEF-RPC anti-mask net); (5) ANON logged-out factsheet — badge
    visible (public provenance; do NOT log in). PUBLIC-DEMO deliberately NOT swept
    (no-invented-data — keep the sfox header note). Header comment: both surfaces are
    UNCONDITIONAL (independent of NEXT_PUBLIC_MT5_ENABLED — the badge/tag are provenance, the
    flag gates only the OFFER), so the spec runs against a default flag-OFF build; MT5UI-02's
    connect-flow error distinguishability is covered at the component level in plan 138-01
    (the honest scope note, mirroring the sfox spec's :33-36 scope note — a live connect
    fails closed while dark).
    Register the spec in ci.yml: add `e2e/mt5-badge.spec.ts \` to the explicit else-branch
    spec list (:1544-1565), directly after the e2e/sfox-badge.spec.ts line (:1557), preserving
    the backslash continuation format. This list is the ONLY way the spec runs in the
    BLOCKING e2e-seeded job feeding the `frontend` aggregator — an unregistered spec is a
    grep-gate ghost (the v1.10 lesson). Touch NOTHING else in ci.yml.
    Go-dark gate confirmation (RUN, don't re-author — the tests already exist): execute the
    three verify commands below; all named cases must collect AND pass. If local pytest reds
    at csv_validator.py:31 import, that is the known missing-pandera local env issue
    (pip install 'pandera==0.32.1' --break-system-packages), not a bug. Only if a named case
    fails to COLLECT does this task add the missing pin (fail loud in the SUMMARY either way).
  </action>
  <verify>
    <automated>npx playwright test e2e/mt5-badge.spec.ts --list && grep -F -c "e2e/mt5-badge.spec.ts" .github/workflows/ci.yml | grep -qx 1 && npx vitest run src/app/api/keys/validate-and-encrypt/route.test.ts --no-file-parallelism -t "mt5" && python3 -m pytest analytics-service/tests/test_ingestion_mt5.py -k "enabled_server" analytics-service/tests/test_mt5_derive_branch.py -q</automated>
  </verify>
  <done>Spec compiles and lists 5 tests; registered exactly once in the seeded CI list; existing TS + py fail-closed gate tests collected and green; seeded run passes locally if seed env is exported, self-skips cleanly otherwise (CI e2e-seeded is the real gate).</done>
  <acceptance_criteria>
    All-roles legs present (owner-edit tag, owner/allocator/admin/anon badge, one axe pass);
    ci.yml grep count is exactly 1; the go-dark commands run green (or the pandera local-env
    caveat is documented in the SUMMARY with the CI run as the authority); ZERO diff on
    VerifiedBadge.tsx / TrustTierLabel.tsx / TRUST_TIER_TOKENS / trust-tier-tokens.test.ts.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/non-owner web → factsheet trust signals | badge must be publicly visible WITHOUT over-exposing owner-only verification rows |
| e2e seed helper → Supabase | service-role writes must be impossible against prod |
| worker → MT5 gateway | a stored mt5 key must never fire a live read while the founder gate is off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-08 | Info disclosure | trust_tier read path for non-owners | mitigate | reads ride the published-gated get_published_trust_signals SECURITY DEFINER RPC (Phase 126); the admin + anon spec legs are the anti-mask regression net — they fail RED on an RLS-scoped embed regression |
| T-138-09 | Tampering | seed helper pointed at prod | mitigate | every mutation via getAdmin() whose assertNotProductionSupabaseUrl + service-role assertions fire before any write (kept verbatim from the sfox trio); placeholder ciphertext only |
| T-138-10 | Elevation | flag-off period: stored mt5 key fires live RPyC reads | mitigate | existing 135/136 gates (route 400 + derive permanent-fail before decrypt/login/read) CONFIRMED by running their tests as this plan's phase gate — no new gate code |
| T-138-11 | Repudiation | spec exists but never runs (silent non-gate) | mitigate | explicit ci.yml registration in the BLOCKING e2e-seeded list + grep-count verify; self-skip is visible (named skip reason), never a silent pass |
| T-138-SC | Tampering | package installs | accept | no install tasks in this plan |
</threat_model>

<verification>
- `npx vitest run src/components/strategy/ApiKeyManager.test.tsx src/components/exchanges/AllocatorExchangeManager.test.tsx --no-file-parallelism` — green.
- `npx playwright test e2e/mt5-badge.spec.ts --list` — 5 tests listed; full seeded run green in CI e2e-seeded.
- `grep -F -c "e2e/mt5-badge.spec.ts" .github/workflows/ci.yml` — exactly 1.
- Existing go-dark gate green: vitest validate-and-encrypt `-t "mt5"` cases + pytest `test_mt5_enabled_server_truth_table` + `test_mt5_derive_branch.py`.
- `git diff --stat src/components/ui/VerifiedBadge.tsx src/components/strategy/TrustTierLabel.tsx` — EMPTY.
- `npx tsc --noEmit` + `npm run lint` — clean.
</verification>

<success_criteria>
The MT5 api_verified badge is e2e-proven across owner/allocator/admin/anon on seeded
fixtures, the proof is wired into the blocking CI gate, the owner surface shows a real MT5
tag, and the dark-until-139 posture is confirmed fail-closed at both layers by the existing
tests — with zero badge-component or gate-code changes.
</success_criteria>

<output>
Create `.planning/phases/138-mt5ui-addkey-badge-e2e/138-03-SUMMARY.md` when done.
</output>
