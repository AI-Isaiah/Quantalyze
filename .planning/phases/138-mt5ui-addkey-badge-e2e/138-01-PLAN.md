---
phase: 138-mt5ui-addkey-badge-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/closed-sets.ts
  - src/lib/utils.ts
  - src/lib/closed-sets.mt5-flag.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx
autonomous: true
requirements: [MT5UI-01, MT5UI-02]

must_haves:
  truths:
    - "With NEXT_PUBLIC_MT5_ENABLED unset (default), the wizard renders exactly today's cards — no MT5 pixel exists (byte-identical, test-pinned)"
    - "With the flag === exact 'true', an MT5 card renders; selecting it shows exactly three labeled fields: MT5 login / Investor password / Broker server, with Broker server REQUIRED and gating submit"
    - "Submitting mt5 POSTs login into api_key, investor password into api_secret, broker server into passphrase — the 135 slot mapping"
    - "KEY_AUTH_FAILED, KEY_MT5_WRONG_SERVER, KEY_MT5_MASTER_PASSWORD each render their OWN distinguishable envelope (own data-error-code + title), never collapsing to a generic error"
    - "OKX's passphrase field renders byte-identically to today (the label-override refactor is neutral for existing venues)"
    - "UI_EXCHANGE_CODES / EXCHANGES never contain mt5, regardless of the flag — no silent manager-surface widening"
  artifacts:
    - path: "src/lib/closed-sets.ts"
      provides: "MT5_UI_ENABLED client flag const (strict === 'true', single static member access)"
      contains: "MT5_UI_ENABLED"
    - path: "src/lib/closed-sets.mt5-flag.test.ts"
      provides: "Strict-true truth table + no-UI_EXCHANGE_CODES-widening pin"
      min_lines: 40
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      provides: "Flag-gated MT5 ExchangeOption card + passphrase-label override + mt5 trust-atom swap"
      contains: "wizard-exchange-mt5"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx"
      provides: "Flag byte-identity, 3-field card, slot mapping, 3 distinguishable envelopes, OKX regression"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      to: "src/lib/closed-sets.ts"
      via: "import MT5_UI_ENABLED from @/lib/utils (re-export)"
      pattern: "MT5_UI_ENABLED"
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      to: "/api/strategies/create-with-key"
      via: "existing wizardFetch submit (payload keys unchanged)"
      pattern: "api/strategies/create-with-key"
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      to: "src/lib/wizardErrors.ts"
      via: "buildEnvelope(code) — codes pre-authored in Phase 135, ZERO new strings"
      pattern: "buildEnvelope"
---

<objective>
Expose MT5 as a flag-gated venue card in the add-key wizard (ConnectKeyStep) behind the new
client flag `MT5_UI_ENABLED` (env `NEXT_PUBLIC_MT5_ENABLED`, strict "true"), collecting the
three MT5 credentials (login / investor password / broker server) into the existing
{api_key, api_secret, passphrase} slots, with the mt5-honest Guest-Mode trust atom and the
muted investor-password steer — DARK by default, byte-identical when OFF.

Purpose: MT5UI-01 (flag-gated 3-credential add-key) + the MT5UI-02 distinguishable-error
surface at the component level. Ships dark; Phase 139 flips the flag.
Output: MT5_UI_ENABLED const + flag test; MT5 card + field overrides in ConnectKeyStep;
component tests pinning byte-identity, fields, slot mapping, and the three envelopes.

⚠️ SCOPE PIN — already exists in 135/136, DO NOT duplicate: the server gate
`isMt5EnabledServer()` (closed-sets.ts:151) + its route wiring (validate-and-encrypt/route.ts:77),
the worker gate `mt5_enabled_server()` (closed_sets.py:107), the KEY_MT5_* envelope copy
(wizardErrors.ts:227-260), the slot mapping (job_worker.py:916-917), and the DB CHECKs
(migration 20260723172032). 138 SURFACES these; it adds no gate code and no new copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/138-mt5ui-addkey-badge-e2e/138-CONTEXT.md
@.planning/phases/138-mt5ui-addkey-badge-e2e/138-UI-SPEC.md
@.planning/phases/138-mt5ui-addkey-badge-e2e/138-RESEARCH.md
@DESIGN.md
@AGENTS.md

<interfaces>
<!-- Extracted from the codebase this session. Use directly — no exploration needed. -->

From src/lib/closed-sets.ts:75 (the pattern to clone verbatim for MT5):
  export const SFOX_UI_ENABLED = process.env.NEXT_PUBLIC_SFOX_ENABLED === "true";

From src/lib/utils.ts:161-167 (the re-export block to extend):
  export { SUPPORTED_EXCHANGES, UI_EXCHANGE_CODES, SFOX_UI_ENABLED, FUNDING_EXCHANGES, type SupportedExchange } from "./closed-sets";

From ConnectKeyStep.tsx:29-47 (the metadata interface to extend minimally):
  interface ExchangeOption {
    id: ExchangeId; name: string; caption: string;
    requiresPassphrase: boolean;
    requiresSecret?: boolean;
    credentialLabels?: { key: string; secret: string };
    credentialPlaceholders?: { key: string; secret: string };
  }

From ConnectKeyStep.tsx:82-97 — the sFOX spread-append inside `const EXCHANGES: ExchangeOption[]`
(the exact idiom for the MT5 card): `...(SFOX_UI_ENABLED ? [{ id: "sfox" as const, … }] : [])`.

From ConnectKeyStep.tsx:186-206 — activeExchange lookup, requiresPassphrase/requiresSecret
derivation, trustAtoms swap (keyed on `!requiresSecret` for sfox at :193-197), and the
keyLabel/secretLabel/placeholder resolution idiom to mirror for the passphrase overrides.

From ConnectKeyStep.tsx:388-404 — the passphrase (third) field: label HARDCODED "OKX Passphrase"
(:391), placeholder hardcoded (:395), helper prose hardcoded (:399-402). This is the ONLY
render change needed beyond the card: read the label/placeholder/helper from ExchangeOption
overrides with today's OKX strings as defaults.

From ConnectKeyStep.tsx:220-233 — the submit payload: exchange, api_key, api_secret
(requiresSecret ? apiSecret : ""), passphrase (requiresPassphrase ? passphrase : null).
UNCHANGED — mt5 sets requiresPassphrase: true so the server slot flows automatically.
Submit-disable predicate at :435 already includes `(requiresPassphrase && !passphrase)`.

From ConnectKeyStep.tsx:324 — `data-testid={"wizard-exchange-" + ex.id}` → mt5 gets
`wizard-exchange-mt5` for free. From :335 — `href={"/security#" + exchange + "-readonly"}`
→ `/security#mt5-readonly` for free.

From wizardErrors.ts:214-259 — the three pre-authored envelope entries (titles):
  KEY_AUTH_FAILED → "The exchange rejected these credentials."
  KEY_MT5_MASTER_PASSWORD → "This MT5 login can place trades."
  KEY_MT5_WRONG_SERVER → "We could not find that broker server."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MT5_UI_ENABLED client flag const + strict-true flag test</name>
  <files>src/lib/closed-sets.ts, src/lib/utils.ts, src/lib/closed-sets.mt5-flag.test.ts</files>
  <read_first>src/lib/closed-sets.ts:58-98 (SFOX_UI_ENABLED + SMOOTHED_MTM docblocks — the two shipped analogs), src/lib/closed-sets.sfox-flag.test.ts (the whole 89-line test — clone its structure), src/lib/utils.ts:150-167 (re-export block)</read_first>
  <behavior>
    - NEXT_PUBLIC_MT5_ENABLED unset → MT5_UI_ENABLED === false
    - Exact string "true" → true
    - Each of "1", "TRUE", "True", "on", "yes", "" → false (strict-equality fail-closed, it.each)
    - UI_EXCHANGE_CODES does NOT contain "mt5" and EXCHANGES does NOT contain "MT5" even when NEXT_PUBLIC_MT5_ENABLED="true" (mt5 is wizard-card-only this phase — the manager <Select> must not silently widen; UI-SPEC §MT5-Manager-Parity, RESEARCH Pitfall 2)
  </behavior>
  <action>
    Per the CONTEXT locked flag decision (reconciled in RESEARCH Q1): the env var is
    NEXT_PUBLIC_MT5_ENABLED; the TS const is MT5_UI_ENABLED. In closed-sets.ts, add
    `export const MT5_UI_ENABLED = process.env.NEXT_PUBLIC_MT5_ENABLED === "true"` directly
    adjacent to SFOX_UI_ENABLED (:75), with a docblock stating: strict equality against the
    EXACT string "true" (fail-closed); MUST stay a single static member access so Next.js
    inlines it into the client bundle (dynamic indexing breaks inlining); DEFAULT OFF, dark
    until the founder flips it in Phase 139; DISTINCT from the server gate isMt5EnabledServer
    (:151) — reference the Phase-135 comment at :143-145 which anticipated this flag.
    Also update that :143-145 comment ("deliberately NOT defined here … Phase 138") since the
    flag now exists — point it at MT5_UI_ENABLED instead.
    Re-export MT5_UI_ENABLED from the utils.ts export block (:161-167) alongside
    SFOX_UI_ENABLED so ConnectKeyStep imports it from @/lib/utils like the sfox precedent
    (ConnectKeyStep.tsx:12).
    Do NOT touch UI_EXCHANGE_CODES, FUNDING_EXCHANGES, or CRYPTO_EXCHANGES — mt5 stays out of
    all three (Pitfall 2; the √252 divergence is 136's, frozen).
    Create src/lib/closed-sets.mt5-flag.test.ts cloning the closed-sets.sfox-flag.test.ts
    idiom exactly: vi.resetModules() + dynamic import per case (module-scope const),
    vi.unstubAllEnvs() in afterEach (the Node22-vs-Node25 stub-leak lesson), it.each for the
    non-exact values, plus the no-widening pin from the behavior block.
  </action>
  <verify>
    <automated>npx vitest run src/lib/closed-sets.mt5-flag.test.ts src/lib/closed-sets.sfox-flag.test.ts --no-file-parallelism</automated>
  </verify>
  <done>MT5_UI_ENABLED exported from closed-sets.ts and re-exported from utils.ts; new flag test green including the UI_EXCHANGE_CODES no-widening pin; existing sfox-flag test still green (no regression from the adjacent edit).</done>
  <acceptance_criteria>
    Flag truth table green (unset/exact-true/6 non-exact values); UI_EXCHANGE_CODES pinned
    mt5-free under flag ON; `npx tsc --noEmit` clean; sfox-flag test untouched and green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MT5 wizard card + 3-field labeled variant + mt5 trust atom + component tests</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx</files>
  <read_first>ConnectKeyStep.tsx (whole file, 461 lines — especially :29-47 ExchangeOption, :82-97 sfox spread, :100-123 trust atoms, :186-206 derivations, :388-404 passphrase field, :435 submit predicate), ConnectKeyStep.test.tsx:206-345 (the flag-OFF byte-identity case :206-216 and the flag-ON stubEnv + resetModules + dynamic-import describe :234-337 — clone both idioms), 138-UI-SPEC.md Delta 1 + Delta 2 + Copywriting Contract (the pinned copy)</read_first>
  <behavior>
    - Flag OFF (default env, static import): queryByTestId("wizard-exchange-mt5") is null; the four existing cards (+ no sfox) render — byte-identity (mirror :206-216)
    - Flag ON (stubEnv NEXT_PUBLIC_MT5_ENABLED="true" + resetModules + dynamic import): the mt5 card renders with name "MT5" and caption "Live investor (read-only) login. Forex & CFD."
    - Selecting mt5 renders exactly three credential fields labeled "MT5 login", "Investor password", "Broker server" (the third is the passphrase slot with the label override — NOT "OKX Passphrase")
    - The muted investor-password steer renders under the secret field, and the broker-server find-it helper renders under the third field (UI-SPEC Copywriting Contract strings)
    - Submit stays disabled until ALL THREE fields are filled (broker server REQUIRED — the :435 predicate covers it via requiresPassphrase: true)
    - Happy-path submit POSTs body {exchange:"mt5", api_key:<login>, api_secret:<investor pw>, passphrase:<server>} — the 135 slot mapping (assert the fetch body like :287-293)
    - The setup-guide link for mt5 has href "/security#mt5-readonly" (mirror :296-301)
    - With mt5 selected, the "What we reject" atom shows the MT5 Guest-Mode body (UI-SPEC Delta 2 copy) and the generic "rejected before we store it" scope claim is absent; with binance selected the generic atom is unchanged; the sfox swap (keyed on !requiresSecret) still works when the sfox flag is also on
    - Three distinguishable envelopes: stub fetch to return {code:"KEY_AUTH_FAILED"} / {code:"KEY_MT5_WRONG_SERVER"} / {code:"KEY_MT5_MASTER_PASSWORD"} (status 400/422) and assert findByTestId("error-envelope") has data-error-code equal to EACH code and renders EACH distinct title ("The exchange rejected these credentials." / "We could not find that broker server." / "This MT5 login can place trades.") — the :317-336 idiom, three cases
    - OKX regression: selecting okx still renders label "OKX Passphrase" with today's helper prose (the override refactor is byte-neutral for existing venues)
  </behavior>
  <action>
    Extend ExchangeOption (:29-47) with FOUR optional presentation-only fields, defaulting to
    today's behavior: passphraseLabel (default "OKX Passphrase"), passphrasePlaceholder
    (default "Paste the OKX passphrase"), passphraseHelper (default today's :399-402 OKX
    prose), secretHelper (default undefined → render nothing). Resolve them next to
    keyLabel/secretLabel (:201-206) and read them in the passphrase block (:388-404) and
    under the secret input (:358-386, render a mt-1 text-micro text-text-muted paragraph only
    when secretHelper is set — the same helper idiom as :399-402). Document in the interface
    comment that these mirror the credentialLabels precedent (:40-46): payload keys and
    storage columns are UNCHANGED, only rendered text differs.
    Append the MT5 card behind MT5_UI_ENABLED (import from @/lib/utils next to
    SFOX_UI_ENABLED at :12), cloning the sfox spread idiom (:82-97):
    id "mt5" as const, name "MT5", caption "Live investor (read-only) login. Forex & CFD.",
    requiresPassphrase true (renders the third field AND gates submit via :435 AND flows the
    server into payload.passphrase via :230), credentialLabels { key: "MT5 login", secret:
    "Investor password" }, credentialPlaceholders { key: "Your MT5 account number", secret:
    "Your read-only investor password" }, passphraseLabel "Broker server",
    passphrasePlaceholder "Exactly as shown in your MT5 terminal", passphraseHelper = the
    UI-SPEC broker-server helper sentence ("Open your MT5 terminal's login window and copy
    the server name exactly as it appears there — it is broker-specific and often carries a
    region or Demo/Live suffix."), secretHelper = the UI-SPEC investor-password steer ("Use
    your investor (read-only) password — not your master password. A master password can
    place trades, so we refuse it and store nothing.").
    Trust-atom swap: add a module-scope MT5_REJECT_ATOM_BODY const with the UI-SPEC Delta 2
    copy ("MT5 master passwords can place trades, so we reject them at connect time and store
    nothing — only a read-only investor login is accepted."). In the trustAtoms map (:193-197)
    key the mt5 swap on activeExchange?.id === "mt5" — NOT on !requiresSecret (mt5 requires a
    secret, so the sfox condition can never fire for it); check mt5 first, then the existing
    !requiresSecret sfox branch, leaving sfox and default venues byte-identical.
    DESIGN.md gates (UI-SPEC): the steer is text-text-muted — NEVER amber/red/⚠ on the
    resting form (Semantic-color gate; red only inside the Error Envelope after a real
    rejection); no new hex/spacing/type tier; no raw text-[Npx]; error copy ONLY via
    buildEnvelope — author ZERO new envelope strings (all three codes landed in 135).
    Tests: add the describe blocks from the behavior list to ConnectKeyStep.test.tsx, cloning
    the two shipped idioms exactly — the flag-OFF static-import case (:206-216) and the
    flag-ON stubEnv+resetModules+dynamic-import harness (:234-245) with vi.unstubAllEnvs()
    AND vi.restoreAllMocks() in afterEach (Node22 stub-leak lesson). The envelope cases spy
    on globalThis.fetch like :317-336. This is the realistic error-surface test path — a live
    connect fails closed while MT5_ENABLED is unset (dark), so the component test IS the
    distinguishability proof until 139's live E2E.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx" --no-file-parallelism</automated>
  </verify>
  <done>All behavior-block cases green, including byte-identity OFF, three labeled fields, submit gating on the broker server, slot-mapped payload, /security#mt5-readonly href, mt5 trust-atom swap, three distinct envelopes, and the OKX byte-neutrality regression. Pre-existing cases in the file untouched and green.</done>
  <acceptance_criteria>
    Flag OFF byte-identical test green; 3 distinguishable envelope tests green (each asserts
    its OWN data-error-code AND title); OKX passphrase regression green; `npx tsc --noEmit`
    clean; no new envelope strings added to wizardErrors.ts (git diff shows the file
    untouched); no ⚠/amber/red on the resting mt5 form.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/strategies/create-with-key | untrusted MT5 credentials cross here; UI adds NO trusted validation (135 route + worker own it) |
| build-time env → client bundle | NEXT_PUBLIC_MT5_ENABLED is inlined at build; a wrong read ships the card to everyone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01 | Elevation | client flag ON without server flag | mitigate | Two-gate design: card shown but connect fail-closed 400 (isMt5EnabledServer, landed 135, test-pinned at route.test.ts:486-531) — a SAFE half-state; this plan adds no gate code |
| T-138-02 | Info disclosure | master (trade-capable) password typed into the secret field | mitigate | type=password masked input + autoComplete off (existing field); worker refuses + KEY_MT5_MASTER_PASSWORD (135); muted up-front steer reduces incidence; nothing stored on rejection |
| T-138-03 | Elevation | silent UI_EXCHANGE_CODES widening exposes an unlabeled MT5 option on manager ApiKeyForm/StrategyForm | mitigate | mt5 lives ONLY in ConnectKeyStep's local EXCHANGES array; Task 1 test pins UI_EXCHANGE_CODES mt5-free under flag ON |
| T-138-04 | Tampering | flag read broken by dynamic env indexing → undefined in browser | mitigate | single static member access (docblock + the sfox precedent); flag test pins behavior |
| T-138-SC | Tampering | npm/pip/cargo installs | accept | NO install tasks exist this phase (RESEARCH §Standard Stack: zero new packages) — no legitimacy gate needed |
</threat_model>

<verification>
- `npx vitest run src/lib/closed-sets.mt5-flag.test.ts src/lib/closed-sets.sfox-flag.test.ts "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx" --no-file-parallelism` — all green.
- `npx tsc --noEmit` — clean.
- `git diff --stat src/lib/wizardErrors.ts src/components/ui/VerifiedBadge.tsx src/components/strategy/TrustTierLabel.tsx` — EMPTY (zero new copy, zero badge change).
- `npm run lint` — clean (no-raw-font-px etc.).
</verification>

<success_criteria>
MT5 card + 3-field variant exists behind MT5_UI_ENABLED, dark by default with test-pinned
byte-identity OFF; the three failure codes render distinguishable envelopes at the component
level; no gate code, envelope copy, badge component, or closed-set (other than the new flag
const) was touched.
</success_criteria>

<output>
Create `.planning/phases/138-mt5ui-addkey-badge-e2e/138-01-SUMMARY.md` when done.
</output>
