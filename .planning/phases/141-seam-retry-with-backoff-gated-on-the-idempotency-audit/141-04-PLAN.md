---
phase: 141-seam-retry-with-backoff-gated-on-the-idempotency-audit
plan: 04
type: execute
wave: 3
depends_on: [141-02, 141-03]
files_modified:
  - src/lib/process-key-client.ts
  - src/lib/analytics-client.ts
  - src/lib/resilient-fetch.ts
  - src/lib/seam-constants.pin.test.ts
  - src/lib/seam-budgets.invariant.test.ts
  - src/lib/resilient-fetch.retry.test.ts
autonomous: true
requirements: [SEAM-05, SEAM-06]

must_haves:
  truths:
    - "postProcessKey decides retry on args.flow_type via retriesOverride — teaser and csv pass an EXPLICIT 0, so even a future flip of the process-key-sync row can never retry them (the belt)"
    - "analyticsRequest decides retry at the one chokepoint all nine wrappers inherit; a tenth wrapper defaults to 0 by registry absence"
    - "Under a single injected transient failure: teaser → exactly ONE fetch; resync/onboard → exactly TWO fetches; bridge → TWO; validateKey → ONE"
    - "Exactly five SEAM_BUDGETS rows carry literal retries: 1; SEAM_RETRIES stays 0; keys-permissions and process-key-sync stay 0 and are pinned; SC-4b charges timeout×(1+retries) + retries×(backoff+jitterMax) and every route still clears its maxDuration"
  artifacts:
    - path: "src/lib/process-key-client.ts"
      provides: "flow_type-keyed retriesOverride (RETRY_SAFE_FLOW_TYPES[args.flow_type]?.retries ?? 0) in the resilientFetch init"
      contains: "retriesOverride"
    - path: "src/lib/analytics-client.ts"
      provides: "budgetKey-keyed retriesOverride (RETRY_SAFE_ANALYTICS[options.budgetKey]?.retries ?? 0) in analyticsRequest"
      contains: "retriesOverride"
    - path: "src/lib/resilient-fetch.ts"
      provides: "five rows flipped to literal retries: 1 (bridge, simulator, portfolio-optimizer, optimize-weights, process-key-enqueue)"
      contains: "retries: 1"
    - path: "src/lib/seam-constants.pin.test.ts"
      provides: "per-row retries pins updated (5 rows expect 1, rest 0) + registry↔rows consistency pin"
  key_links:
    - from: "src/lib/process-key-client.ts"
      to: "src/lib/seam-retry-registry.ts"
      via: "RETRY_SAFE_FLOW_TYPES lookup keyed on args.flow_type"
      pattern: "RETRY_SAFE_FLOW_TYPES\\[args\\.flow_type\\]"
    - from: "src/lib/analytics-client.ts"
      to: "src/lib/seam-retry-registry.ts"
      via: "RETRY_SAFE_ANALYTICS lookup keyed on options.budgetKey"
      pattern: "RETRY_SAFE_ANALYTICS\\[options\\.budgetKey\\]"
---

<objective>
Activate the retry: wire both seam clients to the registry (Class A, N=2 — missing either leaves a whole seam dead or retrying the wrong flow), flip the five allowlisted `SEAM_BUDGETS` rows to literal `retries: 1` with the two negative pins edited in the SAME commit (Classes B+C), and make SC-4b charge the real retry cost including max jitter. Then prove the wiring end-to-end at the client level (SC3's teaser one-fetch pin, resync/bridge two-fetch) and observe the SC-2 and SC-3 falsifiability-ledger mutations.

Purpose: SEAM-06 activation + the SEAM-05 consistency pin (registry ↔ rows cannot drift). Design A arithmetic (locked): rows are flipped so the SC-4b invariant reads a single honest source; the flow_type registry is the belt that keeps `process-key-sync` (teaser+csv) at 0 forever.

Output: live bounded retry for onboard/resync + the four compute-only analytics wrappers; everything else provably at 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-CONTEXT.md
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-PATTERNS.md
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-02-SUMMARY.md
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-03-SUMMARY.md
</context>

<interfaces>
Verified anchors:
- `src/lib/process-key-client.ts:64-68` `budgetKeyFor` (many-to-one: teaser+csv → process-key-sync, onboard+resync → process-key-enqueue); `:322` budgetKey resolution; `:400-433` the resilientFetch init to extend.
- `src/lib/analytics-client.ts:318` `analyticsRequest`; `:324-336` the tenantId centralization docblock (the instance-not-class precedent to cite); `:344` timeout resolution; `:407-419` the resilientFetch init to extend.
- `src/lib/resilient-fetch.ts` SEAM_BUDGETS rows to flip (retries: SEAM_RETRIES → literal 1): bridge `:451`, simulator `:458`, portfolio-optimizer `:465`, optimize-weights `:472`, process-key-enqueue `:507`. Must-stay-0 rows (named belt): validate-key, encrypt-key, match-eval, match-recompute, portfolio-analytics, process-key-sync `:511`, keys-permissions `:548`, process-key-unified-dormant. `SEAM_RETRIES` at `:252` stays 0.
- `src/lib/seam-constants.pin.test.ts:284` the per-row `"%s.retries is 0"` it.each pin; `:339` the `SEAM_RETRIES is 0` pin (seed stays 0 → this pin stays).
- `src/lib/seam-budgets.invariant.test.ts:631` the `timeoutMs × calls × (1 + retries)` worst-case model; header `:94-95` warns finalize-wizard composite breaches at keys-permissions retries=1.
- Plan 02 exported `SEAM_RETRY_BACKOFF_MS = 250`, `SEAM_RETRY_JITTER_MAX_MS = 250` and `retriesOverride` on `ResilientFetchInit`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wire both clients to the registry (Class A — both members, same commit)</name>
  <files>src/lib/process-key-client.ts, src/lib/analytics-client.ts, src/lib/resilient-fetch.retry.test.ts</files>
  <read_first>
    - src/lib/process-key-client.ts:53-68 (the many-to-one docblock to extend) and :400-433 (the init object)
    - src/lib/analytics-client.ts:318-345 and :407-419 (the chokepoint + init)
    - src/lib/seam-retry-registry.ts (the two maps being consulted)
    - src/lib/resilient-fetch.retry.test.ts (plan 02's harness — extend, do not fork a new harness)
  </read_first>
  <behavior>
    Client-level wiring tests (extend resilient-fetch.retry.test.ts; drive the REAL postProcessKey / real analytics wrappers with only fetch mocked — test the wiring, not a helper):
    - postProcessKey flow_type "teaser" + single transient failure → exactly ONE fetch, error surfaces (SC3 TS pin).
    - postProcessKey flow_type "csv" + single transient failure → exactly ONE fetch.
    - postProcessKey flow_type "resync" + single transient failure then 200 → exactly TWO fetches, resolves (SC2 mechanics at the wiring level).
    - postProcessKey flow_type "onboard" → TWO fetches likewise.
    - findReplacementCandidates (bridge) + single transient then 200 → exactly TWO fetches.
    - validateKey + single transient → exactly ONE fetch (registry absence inherited through the chokepoint).
  </behavior>
  <action>
    Write the six wiring tests FIRST; observe teaser/csv pass trivially (nothing retries yet) but resync/onboard/bridge FAIL (one fetch, not two) — that asymmetric RED is the wiring's proof-of-necessity. Then:

    1. `process-key-client.ts`: import `RETRY_SAFE_FLOW_TYPES` from `./seam-retry-registry` (value import of a leaf — mock-safe). In the `resilientFetch` init at :400-433 add `retriesOverride: RETRY_SAFE_FLOW_TYPES[args.flow_type]?.retries ?? 0`. The `?? 0` is EXPLICIT and load-bearing: it means absence never falls through to the budget row, so a future flip of process-key-sync's row cannot retry teaser/csv (the belt). Extend the :53-68 docblock: the retry decision keys on flow_type for the same many-to-one reason budgetKeyFor exists — keying on budgetKey would retry teaser (SC3).

    2. `analytics-client.ts`: import `RETRY_SAFE_ANALYTICS`. In `analyticsRequest`, beside the :344 timeout resolution, compute and pass `retriesOverride: RETRY_SAFE_ANALYTICS[options.budgetKey]?.retries ?? 0` in the :407-419 init. Add a comment mirroring the :324-336 tenantId docblock: the decision lives at the ONE chokepoint so all nine wrappers (and any tenth) inherit it — per-wrapper consultation is the instance-not-class defect.

    Note the design intent in both comments: the registry is the single decision source for BOTH seams; the SEAM_BUDGETS rows (task 2) exist so SC-4b's arithmetic reads an honest row — the consistency pin in task 2 ties them.
  </action>
  <verify>
    <automated>npx vitest run --no-file-parallelism src/lib/resilient-fetch.retry.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The resync/onboard/bridge two-fetch REDs observed BEFORE wiring (pasted into SUMMARY), green after.
    - `grep -n "RETRY_SAFE_FLOW_TYPES\[args.flow_type\]" src/lib/process-key-client.ts` → exactly 1 hit inside the resilientFetch init; `grep -n "RETRY_SAFE_ANALYTICS\[options.budgetKey\]" src/lib/analytics-client.ts` → exactly 1 hit.
    - Both lookups carry the explicit `?? 0` (grep confirms; absence-means-zero never delegates to the row from the client side).
    - All six wiring behaviors green; no wrapper-level (per-function) registry consultation exists (`grep -c "RETRY_SAFE_ANALYTICS" src/lib/analytics-client.ts` → 1 import + 1 use = 2 lines max).
  </acceptance_criteria>
  <done>Both seams consult the registry at their chokepoints; teaser/csv provably single-fetch under failure; allowlisted flows/wrappers provably two-fetch.</done>
</task>

<task type="auto">
  <name>Task 2: flip the five rows + edit the pins + charge the backoff in SC-4b (ONE commit)</name>
  <files>src/lib/resilient-fetch.ts, src/lib/seam-constants.pin.test.ts, src/lib/seam-budgets.invariant.test.ts</files>
  <read_first>
    - src/lib/resilient-fetch.ts:413-565 (SEAM_BUDGETS — the five rows and the must-stay-0 rows)
    - src/lib/seam-constants.pin.test.ts:283-296 and :339-350 (the two negative pins — EDIT deliberately, never delete; they are the intended tripwire)
    - src/lib/seam-budgets.invariant.test.ts:94-101 (the retries=1 composite-breach warning) and :620-640 (the worst-case model at :631)
  </read_first>
  <action>
    In ONE commit (the pins redden any partial state — that is their job):

    1. `resilient-fetch.ts`: flip exactly five rows from `retries: SEAM_RETRIES` to a literal `retries: 1` — bridge (:451), simulator (:458), "portfolio-optimizer" (:465), "optimize-weights" (:472), "process-key-enqueue" (:507). Every other row keeps `retries: SEAM_RETRIES`; the `:252` seed stays 0. Add a one-line comment on each flipped row naming its registry entry (audit cross-reference).

    2. `seam-constants.pin.test.ts`: rework the :284 per-row pin so the five flipped keys expect literal 1 and every other key still expects 0 (e.g. a hand-typed EXPECTED_RETRIES record — literals, not derived). The :339 SEAM_RETRIES pin stays as-is (verify no production code path resolves an allowlisted route's retries via SEAM_RETRIES — the rows now carry literals). ADD the registry↔rows consistency pin here (this file already imports SEAM_BUDGETS): (a) every key of RETRY_SAFE_ANALYTICS has SEAM_BUDGETS[key].retries === 1; (b) SEAM_BUDGETS["process-key-enqueue"].retries === 1 (the onboard+resync grain); (c) SEAM_BUDGETS["process-key-sync"].retries === 0 AND SEAM_BUDGETS["keys-permissions"].retries === 0 — the SC3 belt and the Pitfall-2 fence at the row grain, as hand-typed literals.

    3. `seam-budgets.invariant.test.ts`: extend the :631 model to charge the backoff — per budget entry, worst case = timeoutMs × calls × (1 + retries) + retries × calls × (SEAM_RETRY_BACKOFF_MS + SEAM_RETRY_JITTER_MAX_MS), importing the two constants from resilient-fetch (they are production values; the CEILINGS stay hand-typed). Charge the MAX jitter (PATTERNS "No Analog" note: jitter must remain stateable — bound it and charge the bound). Run the invariant: every route must still clear its maxDuration ceiling. Expected: keys/sync ≈ 15000×2 + store worst case + 500 ≈ 43k vs 300k; finalize-wizard composite still passes BECAUSE keys-permissions stays 0 — if the invariant reddens, a row was mis-flipped; fix the row, never the ceiling.
  </action>
  <verify>
    <automated>npx vitest run --no-file-parallelism src/lib/seam-constants.pin.test.ts src/lib/seam-budgets.invariant.test.ts src/lib/resilient-fetch.test.ts src/lib/resilient-fetch.retry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "retries: 1," src/lib/resilient-fetch.ts` → exactly 5; `grep -n "retries: SEAM_RETRIES" src/lib/resilient-fetch.ts` still matches every non-flipped row; line :252 unchanged.
    - Both pin edits and the row flips are in the SAME commit (git show confirms one commit touches all three files).
    - Consistency pin green: registry keys ⊆ rows-at-1; process-key-sync and keys-permissions pinned 0.
    - SC-4b green with the backoff term included; the composite branch still passes.
    - Full quick lane green; `npx tsc --noEmit` → 0.
  </acceptance_criteria>
  <done>Retry is live for exactly the five audited budgets; every unproven row is pinned at 0; SC-4b charges the true worst case including max jitter.</done>
</task>

<task type="auto">
  <name>Task 3: SC-2 and SC-3 falsifiability-ledger observations + phase gate</name>
  <files>src/lib/resilient-fetch.retry.test.ts</files>
  <read_first>
    - .planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-VALIDATION.md (ledger rows SC-2, SC-3)
    - src/lib/process-key-client.ts (the wiring under mutation)
    - src/lib/seam-retry-registry.ts
  </read_first>
  <action>
    Execute both remaining ledger mutations against PRODUCTION source, one at a time, restoring between:

    1. SC-2 mutation ("drop the flow_type gate so it keys on budgetKey"): in postProcessKey, change the lookup to key on the budgetKey (e.g. index RETRY_SAFE_FLOW_TYPES with the computed budgetKey — which has no flow-type keys, so every flow resolves to 0). The resync two-fetch wiring test MUST go RED (one fetch observed, two expected). Paste the failing assertion; flip the ledger row to Observed ✅.

    2. SC-3 mutation ("add teaser to the allowlist"): add `teaser: { retries: 1, evidence: "MUTANT" }` to RETRY_SAFE_FLOW_TYPES. TWO independent tests must go RED: the teaser one-fetch wiring test (task 1) AND the registry absence pin (plan 03) — record BOTH failures (the red asymmetry across two files is the proof the belt is independent of the pin). Paste; flip the ledger row.

    Restore after each; `grep -rn MUTANT src/` → 0. Then run the phase gate: full vitest suite with coverage thresholds, the real-Redis lane, and tsc/lint. Update 141-VALIDATION.md: all four ledger rows Observed ✅ (SC-1 from plan 03, SC-4 from plan 02 — verify they were recorded; if either is missing, STOP and observe it now), Per-Task Verification Map filled, `nyquist_compliant: true` only if all rows are Observed.
  </action>
  <verify>
    <automated>npx vitest run --no-file-parallelism && npx tsc --noEmit && grep -rn MUTANT src/ | wc -l | grep -q "^ *0$"</automated>
  </verify>
  <acceptance_criteria>
    - SC-2 and SC-3 mutations OBSERVED RED first-hand with pasted failing assertions in SUMMARY + 141-VALIDATION.md; SC-3 reddened in BOTH files (wiring test + registry pin).
    - All four ledger rows Observed ✅ in 141-VALIDATION.md; `nyquist_compliant: true` set.
    - Phase gate: full vitest suite green with coverage thresholds cleared; `npm run test:redis` lane green (SC4 cases under real Redis); lint 0 errors; tree clean of mutants.
  </acceptance_criteria>
  <done>Every success criterion's verification is proven ABLE to fail; the phase gate is green at the final tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel→Railway seam (both clients) | retries re-cross here; eligibility must be decided at the layer that knows flow_type |
| anonymous teaser → /process-key | the non-idempotent, lead-minting flow the retry must never touch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Tampering/abuse | teaser retry double-mints verification/public_token/lead | mitigate | flow_type-keyed lookup with explicit ?? 0 + process-key-sync row pinned 0 + SC-3 mutation observed RED in two independent files |
| T-141-03 | Denial of Service | retry amplifies an open-breaker outage | mitigate | plan 02's dual breaker gates are inherited unchanged; real-Redis lane re-proves SC4 at the final tree |
| T-141-09 | Denial of Service | fan-out route allowlisted → composite branch breaches the 300k lambda ceiling mid-outage | mitigate | keys-permissions row pinned 0 (hand-typed literal) + SC-4b charges timeout×(1+retries)+backoff+jitterMax and reddens on breach |
| T-141-07 | Tampering | credential double-write via validate-key/encrypt-key retry | mitigate | registry absence + rows stay 0 + per-row pin expects 0 |
| T-141-SC | Tampering | npm/pip/cargo installs | accept (n/a) | zero package installs this phase |
</threat_model>

<verification>
- Full suite: `npx vitest run --no-file-parallelism` green with coverage thresholds (82/80/74/72); `npm run test:redis` green; `npx tsc --noEmit` 0; `npm run lint` 0 errors.
- Wiring greps from task 1 + row greps from task 2 all hold at the final tree.
- 141-VALIDATION.md: 4/4 ledger rows Observed ✅, nyquist_compliant: true.
</verification>

<success_criteria>
- SC2 (mechanics half): allowlisted resync retries once and succeeds; proven at the wiring level against the real postProcessKey. (Server half: plan 01.)
- SC3: teaser provably never retried — registry absence pin + one-fetch wiring pin + mutation observed RED in both.
- SC4: re-proven at the final tree in the real-Redis lane.
- SEAM-05 consistency: registry ↔ rows pinned; audit and enforcement cannot drift.
</success_criteria>

<output>
Create `.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-04-SUMMARY.md` when done — include all mutation evidence verbatim and the final phase-gate numbers.
</output>
