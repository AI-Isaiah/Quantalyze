---
phase: 141-seam-retry-with-backoff-gated-on-the-idempotency-audit
plan: 03
type: execute
wave: 2
depends_on: [141-01]
files_modified:
  - src/lib/seam-retry-registry.ts
  - src/lib/seam-retry-registry.test.ts
autonomous: true
requirements: [SEAM-05]

must_haves:
  truths:
    - "One committed in-repo artifact maps every /process-key flow_type (4) and every analytics seam wrapper (9) to a retry-safe yes/no verdict with traced server-side evidence — and it IS the runtime allowlist, so audit and enforcement cannot drift"
    - "teaser and csv are ABSENT from the YES map; absence means no-retry by construction (Partial<Record> — no separate rule)"
    - "_get_recompute_lock is recorded as PROCESS-LOCAL in the match-recompute NO-verdict evidence (SEAM-05's explicit ask)"
    - "resync's YES entry exists only because plan 01's draft-SV dedup landed first (wave ordering) and its evidence says so"
  artifacts:
    - path: "src/lib/seam-retry-registry.ts"
      provides: "dependency-free leaf: RetrySafeEntry, RETRY_SAFE_FLOW_TYPES, RETRY_SAFE_ANALYTICS, RETRY_AUDIT_NO_FLOW_TYPES, RETRY_AUDIT_NO_ANALYTICS"
      contains: "RETRY_SAFE_FLOW_TYPES"
    - path: "src/lib/seam-retry-registry.test.ts"
      provides: "absence pins (teaser/csv), exhaustiveness at both grains, disjointness, literal counts, purity assertion"
      min_lines: 80
  key_links:
    - from: "src/lib/seam-retry-registry.ts"
      to: "src/lib/process-key-client.ts FlowType + src/lib/resilient-fetch.ts SeamBudgetKey"
      via: "import type ONLY (erased — leaf purity preserved)"
      pattern: "import type"
---

<objective>
Create the SC1 deliverable: the typed retry-safety registry that is simultaneously the committed SEAM-05 audit artifact AND the runtime allowlist. Absence ⇒ no-retry by construction (`Partial<Record>`); every verdict — YES and NO — carries its traced server-side evidence inline so the documented audit and the enforcement cannot drift.

Purpose: SEAM-05. Everything unproven defaults to no-retry because the map physically has no entry for it — the wrapper reads `registry[key]?.retries ?? 0` (plan 04). This plan is wave 2 because CONTEXT locks "resync is added to the allowlist ONLY AFTER the draft-SV dedup lands" — plan 01 landed it in wave 1.

Output: `src/lib/seam-retry-registry.ts` + its test, SC-1 falsifiability-ledger row Observed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-CONTEXT.md
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-RESEARCH.md
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-PATTERNS.md
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-01-SUMMARY.md
</context>

<interfaces>
Verified type sources (import type ONLY — a VALUE import of resilient-fetch.ts breaks leaf purity):
- `src/lib/process-key-client.ts:51` — `export type FlowType = "teaser" | "onboard" | "resync" | "csv";`
- `src/lib/resilient-fetch.ts:316-328` — `export type SeamBudgetKey = ... | "bridge" | "simulator" | "portfolio-optimizer" | "optimize-weights" | ... | "process-key-enqueue" | "process-key-sync" | "keys-permissions" | ...`

The nine analytics-wrapper budget keys the audit must exhaustively cover (Class E, PATTERNS): `validate-key`, `encrypt-key`, `bridge`, `simulator`, `portfolio-optimizer`, `optimize-weights`, `portfolio-analytics`, `match-recompute`, `match-eval`. (`keys-permissions` and the `process-key-*` budget rows are EXCLUDED BY GRAIN — they are route budgets, not seam-function verdicts; the process-key seam is audited at flow_type grain and `keys-permissions` is protected by its row staying 0, pinned in plan 04. A registry comment must say this so the exclusion reads as deliberate.)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: the registry leaf (audit + allowlist in one artifact)</name>
  <files>src/lib/seam-retry-registry.ts</files>
  <read_first>
    - src/lib/process-key-onboard-contract.ts:1-31 (the leaf-purity docblock to copy: zero imports, zero env reads, zero module-load side effects, ONE implementation, callable with no mocks — and the two forcing constraints: browser-bundle reachability from "use client" wizard components, survival under the ~16 wholesale seam-client mocks)
    - src/lib/seam-discriminator.ts:46-80 (the hand-duplicated-vocabulary + Object.freeze closed-vocab precedent)
    - .planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-RESEARCH.md (§Q2 — every verdict + its traced evidence; §Q3 — the registry shape)
  </read_first>
  <action>
    Create `src/lib/seam-retry-registry.ts` per the RESEARCH Q3 shape. Exports:

    1. `interface RetrySafeEntry { readonly retries: 1; readonly evidence: string }` — retries is the literal type 1 (exactly-one locked).

    2. `RETRY_SAFE_FLOW_TYPES: Partial<Record<FlowType, RetrySafeEntry>>` with exactly TWO entries:
       - `onboard`: evidence citing caller wizard_session_id → idempotent_by_session, the SELECT pre-check + _resume_duplicate_job (process_key.py:643-714, :1351), SV row deduped by strategy_verifications_strategy_wizard_session_uniq (migration 20260726000225), job deduped by compute_jobs_one_inflight_per_kind_strategy (migration 20260418194206).
       - `resync`: evidence citing the compute-job dedup on (strategy_id, kind) AND the phase-141 plan-01 draft-SV pre-check (strategy_id + flow_type='resync' + status='draft' → duplicate path), noting the sequential-retry class is closed and the concurrent-tab race remains a documented residual. State explicitly: allowlisted only after that dedup landed.

    3. `RETRY_AUDIT_NO_FLOW_TYPES: Partial<Record<FlowType, string>>` (evidence-only NO verdicts):
       - `teaser`: mints fresh uuid4 session + SV row + public_token + lead every call (process_key.py:936-938, :1033) — deliberately non-idempotent; a retry double-mints. SC3 pins the YES-map absence.
       - `csv`: validate is side-effect-free and finalize is 23505-fenced (migration 20260728120000), but csv shares flow_type AND the process-key-sync budget row with teaser — default no-retry, blast radius over marginal value (RESEARCH A5).

    4. `RETRY_SAFE_ANALYTICS: Partial<Record<SeamBudgetKey, RetrySafeEntry>>` with exactly FOUR entries — `bridge`, `simulator`, `"portfolio-optimizer"`, `"optimize-weights"` — each evidenced as pure compute over caller-supplied inputs with no persisted server-side write on the request path.

    5. `RETRY_AUDIT_NO_ANALYTICS: Partial<Record<SeamBudgetKey, string>>` with exactly FIVE entries:
       - `"validate-key"` and `"encrypt-key"`: credential writes / live exchange probes — non-idempotent by construction, REQUIREMENTS Out-of-Scope.
       - `"match-recompute"`: **_get_recompute_lock is PROCESS-LOCAL** (match.py — dict[str, asyncio.Lock], in-memory per worker; the SEAM-05 explicit resolution) — it bounds but does not prove idempotency across instances; no unique constraint on match_batches; H-0562 open → unproven → no-retry.
       - `"portfolio-analytics"`: ZERO callers (resilient-fetch.ts:502) — recorded as such per SC1; a dead path is never allowlisted.
       - `"match-eval"`: read-only admin sweep, low value → default no-retry (RESEARCH discretion).

    File header: the copied leaf-purity docblock, plus (a) the audit-and-allowlist-are-one-artifact rationale (SC1 anti-drift), (b) the grain note — process-key seam decided by flow_type because budgetKeyFor is many-to-one (teaser+csv → process-key-sync, onboard+resync → process-key-enqueue; keying on budgetKey would retry teaser — the SC3 landmine), analytics seam decided by its 1:1 budgetKey, (c) the keys-permissions / process-key-* row exclusion-by-grain note from <interfaces>.

    `import type { FlowType } from "./process-key-client";` and `import type { SeamBudgetKey } from "./resilient-fetch";` — NOTHING else. No values, no env, no side effects, no fenced runtime logic beyond the const maps.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "^import type" src/lib/seam-retry-registry.ts | grep -q "^2$" && grep -c "^import " src/lib/seam-retry-registry.ts | grep -q "^2$"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 2 import lines, both `import type` (the grep pair above proves it).
    - All 4 flow_types and all 9 analytics wrapper keys appear across the YES/NO maps (13 verdicts total: 2+2 flow, 4+5 analytics).
    - The string "process-local" (case-insensitive) appears in match-recompute's evidence — the SEAM-05 lock resolution is IN the committed artifact.
    - resync's evidence names the plan-01 dedup; teaser's evidence cites process_key.py:936-938.
    - `npx tsc --noEmit` → 0 errors.
  </acceptance_criteria>
  <done>The committed audit artifact exists, covers every required target with traced evidence, and is structurally incapable of allowlisting anything unproven (absence semantics).</done>
</task>

<task type="auto">
  <name>Task 2: registry test — absence pins, exhaustiveness, purity + SC-1 ledger observation</name>
  <files>src/lib/seam-retry-registry.test.ts</files>
  <read_first>
    - src/lib/seam-retry-registry.ts (just created)
    - src/lib/seam-discriminator.purity.test.ts (the EXPECTED_EXPORTS surface pin + source-read purity assertion to copy)
    - src/lib/seam-constants.pin.test.ts (hand-typed-literal pin style)
    - .planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-VALIDATION.md (Falsifiability Ledger SC-1 row + Oracle Independence rules)
  </read_first>
  <action>
    Create `src/lib/seam-retry-registry.test.ts`. Every expected value is a LITERAL typed into the test — never read from the registry and re-asserted against itself (Oracle Independence: "Registry size is pinned to a literal count"). Assertions:

    1. SC3 belt: `RETRY_SAFE_FLOW_TYPES.teaser` and `.csv` are strictly `undefined`.
    2. YES-map contents: sorted keys of RETRY_SAFE_FLOW_TYPES equal the literal `["onboard", "resync"]`; sorted keys of RETRY_SAFE_ANALYTICS equal the literal `["bridge", "optimize-weights", "portfolio-optimizer", "simulator"]`. Every entry: `retries === 1` and `evidence.length > 0`.
    3. Exhaustiveness (the SC1 "maps EVERY" clause, made red-able): sorted union of YES+NO flow keys equals the literal `["csv", "onboard", "resync", "teaser"]`; sorted union of YES+NO analytics keys equals the literal nine-wrapper list `["bridge", "encrypt-key", "match-eval", "match-recompute", "optimize-weights", "portfolio-analytics", "portfolio-optimizer", "simulator", "validate-key"]`. A future flow_type or wrapper added without an audit verdict reddens here.
    4. Disjointness: YES ∩ NO = ∅ at both grains (a key cannot be simultaneously safe and unsafe).
    5. The lock resolution: match-recompute's NO evidence matches /process-local/i (deleting the SEAM-05 resolution from the artifact reddens).
    6. Purity: copy seam-discriminator.purity.test.ts's mechanism — read the module source from disk and assert every import line starts with `import type` (a VALUE import that pulls resilient-fetch into the wizard bundle reddens).

    Then execute the ledger SC-1 mutation against production source: temporarily add `teaser: { retries: 1, evidence: "MUTANT" }` to RETRY_SAFE_FLOW_TYPES. Run the test file — assertions 1, 2, and 4 MUST go RED. Paste the failing output into the SUMMARY and flip 141-VALIDATION.md's SC-1 row to Observed ✅ with the evidence. Revert; `grep -rn MUTANT src/` → 0; re-run green.
  </action>
  <verify>
    <automated>npx vitest run --no-file-parallelism src/lib/seam-retry-registry.test.ts && grep -rn MUTANT src/ | wc -l | grep -q "^ *0$"</automated>
  </verify>
  <acceptance_criteria>
    - All six assertion groups green; expected key lists are hand-typed literals (grep the test for `Object.keys` used ONLY on the actual side of comparisons).
    - SC-1 mutation OBSERVED RED first-hand; failing assertion pasted into SUMMARY + 141-VALIDATION.md SC-1 row Observed ✅.
    - Tree restored; full quick lane green: `npx vitest run --no-file-parallelism src/lib/seam-retry-registry.test.ts`.
  </acceptance_criteria>
  <done>The audit artifact is pinned: any unaudited addition, any teaser/csv allowlisting, any purity break, or any deleted lock-resolution reddens CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry (audit) → runtime retry decision | if these drift, an unproven call gets retried; this plan makes them ONE artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Tampering/abuse | teaser allowlisted → duplicate verification/public_token/lead minting on retry | mitigate | Partial<Record> absence semantics + the strict-undefined pin + disjointness; mutation-tested (SC-1 ledger row) |
| T-141-07 | Tampering | credential double-write via validate-key/encrypt-key allowlisting | mitigate | both carry NO verdicts with evidence; exhaustiveness pin means removing them from NO without adding to YES also reddens |
| T-141-08 | Repudiation | audit verdict changed without traceable evidence | mitigate | evidence is a required non-empty field on every YES entry; NO verdicts are evidence strings by type |
| T-141-SC | Tampering | npm/pip/cargo installs | accept (n/a) | zero package installs this phase |
</threat_model>

<verification>
- `npx vitest run --no-file-parallelism src/lib/seam-retry-registry.test.ts` green; `npx tsc --noEmit` 0.
- Registry consulted by NOTHING yet (`grep -rn "seam-retry-registry" src/lib/analytics-client.ts src/lib/process-key-client.ts` → 0) — wiring is plan 04's, so this plan cannot change production behavior.
</verification>

<success_criteria>
- SC1 fully satisfied: one committed artifact, 13 evidenced verdicts, _get_recompute_lock resolved process-local in-artifact, unproven ⇒ no-retry by construction.
- SC-1 ledger row Observed with pasted evidence.
</success_criteria>

<output>
Create `.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-03-SUMMARY.md` when done.
</output>
