---
phase: 141-seam-retry-with-backoff-gated-on-the-idempotency-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/routers/process_key.py
  - analytics-service/tests/test_resync_draft_dedup.py
  - analytics-service/tests/test_teaser_non_idempotent.py
  - supabase/tests/test_resync_retry_single_job.sql
autonomous: true
requirements: [SEAM-06]

must_haves:
  truths:
    - "A second identical resync submission for the same strategy takes the duplicate path: exactly ONE draft strategy_verifications row exists and the reply carries code WIZARD_DUPLICATE"
    - "Two identical teaser submissions mint TWO strategy_verifications rows with DISTINCT server-minted wizard_session_ids — the non-idempotency is real, observed, not assumed"
    - "Two enqueue attempts for one (strategy, kind) yield exactly ONE non-terminal compute_jobs row, proven against the real partial-unique index"
  artifacts:
    - path: "analytics-service/routers/process_key.py"
      provides: "resync draft-SV dedup pre-check (SELECT scoped by strategy_id + flow_type='resync' + status='draft' → _resume_duplicate_job + _wizard_duplicate_reply)"
      contains: "flow_type"
    - path: "analytics-service/tests/test_resync_draft_dedup.py"
      provides: "full-app pytest: two resync submissions → 1 draft SV row, second reply WIZARD_DUPLICATE"
      min_lines: 60
    - path: "analytics-service/tests/test_teaser_non_idempotent.py"
      provides: "SC3 DB pin: two teaser submissions → TWO SV rows, distinct uuid4 session ids"
      min_lines: 40
    - path: "supabase/tests/test_resync_retry_single_job.sql"
      provides: "real-index SC2 proof: 1 non-terminal compute_jobs row after double enqueue; SV index passes distinct-session rows and 23505s same-session"
      min_lines: 60
  key_links:
    - from: "analytics-service/routers/process_key.py resync pre-check"
      to: "_resume_duplicate_job + _wizard_duplicate_reply"
      via: "duplicate-hit branch before the draft insert at :1399"
      pattern: "_resume_duplicate_job"
    - from: "supabase/tests/test_resync_retry_single_job.sql"
      to: "compute_jobs_one_inflight_per_kind_strategy"
      via: "enqueue_compute_job RPC called twice with p_kind 'process_key_long'"
      pattern: "process_key_long"
---

<objective>
Make resync's draft `strategy_verifications` write idempotent for the retry class (the LOCKED CONTEXT precondition for allowlisting resync), and land the DB-real proofs SC2 and SC3 stand on: the compute_jobs partial-unique index proof and the teaser two-rows non-idempotency pin.

Purpose: SC2 asserts a retried resync yields exactly ONE compute_job AND ZERO duplicate draft SV rows; SC3 needs the server's teaser non-idempotency proven real (not a mock tautology) so the TS no-retry rule is load-bearing. Per CONTEXT's locked decision, resync enters the allowlist ONLY AFTER this dedup lands — which is why this plan is Wave 1 and the registry (plan 03) depends on it.

Scope note (conflict resolved): RESEARCH's user_constraints carry a stale "no Python changes / TypeScript-only" line; CONTEXT's later locked decision ("This phase MUST make resync's draft SV write idempotent") supersedes it. Python is in scope for exactly this dedup, nothing else.

Output: deduped resync draft write + three test artifacts (pytest ×2, SQL gate ×1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-CONTEXT.md
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-RESEARCH.md
@.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-PATTERNS.md
</context>

<interfaces>
Verified anchors in `analytics-service/routers/process_key.py` (read at HEAD):
- `:643` `def _resume_duplicate_job(` — the shared re-enqueue helper both duplicate emitters call.
- `:936-938` teaser overwrite: `body.context["wizard_session_id"] = str(uuid.uuid4())` — DO NOT TOUCH (SC3 depends on it).
- `:1018` `wizard_session_id = body.context.get("wizard_session_id") or str(uuid.uuid4())` — resync's server-minted session id.
- `:1033` `idempotent_by_session = body.flow_type != "teaser" and bool(...)` — False for resync by construction; leave the expression alone.
- `:1351` `if idempotent_by_session:` — the onboard SELECT pre-check to MIRROR (scoped `.eq` reads, hit → `_resume_duplicate_job` at `:1384` + `_wizard_duplicate_reply`).
- `:1399-1415` the draft INSERT to guard (columns include `strategy_id`, `wizard_session_id`, `status: "draft"`, `flow_type`).
- `:1416-1427` the 23505 TOCTOU arm: for `not idempotent_by_session` it RAISES — correct and unchanged (resync's fresh uuid4 can never 23505 on the session index; the app-level SELECT is resync's dedup, not this arm).

`src/app/api/keys/sync/route.ts:563-599` already translates the `WIZARD_DUPLICATE` + `queued` 200 envelope — the resync duplicate reply needs ZERO TS route changes.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resync draft-SV dedup pre-check (RED first, then GREEN)</name>
  <files>analytics-service/routers/process_key.py, analytics-service/tests/test_resync_draft_dedup.py</files>
  <read_first>
    - analytics-service/routers/process_key.py:930-1035 (flow-type context shaping) and :1335-1470 (the onboard pre-check + draft insert + 23505 arm — the in-file analog to mirror)
    - analytics-service/tests/test_process_key_onboard_contract.py (the full-app harness analog: drives main.app so verify_service_key/_gate_process_key run; the ONLY patched symbol is routers.process_key.get_supabase; branches selected by DB state alone)
  </read_first>
  <behavior>
    - Test 1 (the RED): submit `flow_type="resync"` twice, identical body, same `strategy_id`, through the full app with a fake supabase whose store starts empty. After both calls: exactly ONE `strategy_verifications` row with `flow_type == "resync"` and `status == "draft"` for that strategy_id, and the SECOND reply carries `code == "WIZARD_DUPLICATE"` with `idempotent` handling per the shared builder. Before the fix this test observes TWO draft rows — run it, observe the RED, paste the failing assertion into the SUMMARY, THEN implement.
    - Test 2: the second submission still re-enqueues — `_resume_duplicate_job` is reached (assert the enqueue RPC was invoked on the duplicate path, mirroring the onboard duplicate contract's enqueue-aware behavior).
    - Test 3 (negative control, teaser untouched): two `flow_type="teaser"` submissions through the same harness → TWO draft rows. This proves the dedup branch is resync-scoped and did not widen into teaser (which would break SC3's contract).
    - Test 4 (scope): a resync for strategy B does NOT hit strategy A's draft — the pre-check is strategy-scoped (two different strategy_ids → two rows, one each).
  </behavior>
  <action>
    Add a resync-specific duplicate pre-check in `process_key` BEFORE the draft INSERT at :1399, structurally mirroring the `idempotent_by_session` block at :1351-1396: when `body.flow_type == "resync"`, SELECT `strategy_verifications` scoped `.eq("strategy_id", strategy_id).eq("flow_type", "resync").eq("status", "draft")` (the non-terminal/draft window mirrors the compute_jobs dedup's non-terminal restriction). On a hit: call `_resume_duplicate_job(supabase=..., body=..., strategy_id=..., existing=<hit row>, correlation_id=...)` and return `_wizard_duplicate_reply(...)` — the SAME two helpers the onboard path uses (one builder, so the contract cannot drift). On no hit: fall through to the existing draft INSERT unchanged.

    Constraints (all locked): do NOT touch the teaser branch at :936-938; do NOT modify the `idempotent_by_session` expression at :1033; do NOT change the 23505 arm at :1416-1427 (its raise-through for `not idempotent_by_session` stays — a resync 23505 cannot come from this dedup and must surface). NO new migration and NO new unique index — this is an application-level SELECT-then-guarded-INSERT (PATTERNS records the migration path as a scope decision deliberately NOT taken). Document in a code comment the bound this buys: the pre-check closes the SEQUENTIAL retry class (a seam retry re-reaching the server); a concurrent two-tab race (both SELECTs pass before either INSERT, distinct session ids so no 23505) can still mint two drafts — recorded residual, out of 141's scope.

    The pre-check runs strictly after the existing ownership gate (strategy_id has already passed the strategies id+user_id check per 140.1-02), so scoping by strategy_id carries the tenant scope — state this in the comment (PYAPI-01d discipline).

    Harness: build the fake supabase per test_process_key_onboard_contract.py — patch ONLY `routers.process_key.get_supabase`; never patch the reply builder, `_resume_duplicate_job`, or the handler; drive `main.app` via TestClient so auth middleware runs.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_resync_draft_dedup.py -x -q && mypy --strict routers/process_key.py</automated>
  </verify>
  <acceptance_criteria>
    - RED observed first-hand before the fix: Test 1 fails with a 2-vs-1 row-count assertion; the failing output is pasted into the plan SUMMARY (never "asserted only").
    - After the fix: all 4 behavior tests green; `grep -n "flow_type.*resync" analytics-service/routers/process_key.py` shows the new pre-check between the :1351 block and the draft insert.
    - Teaser negative control (Test 3) green — two teaser rows still minted.
    - `mypy --strict routers/process_key.py` exits 0 with zero new `# type: ignore` (MEMORY: mypy before ship, fix via cast() not ignore).
    - Full pytest suite: `python -m pytest -q` — no new failures vs the pre-plan baseline.
    - Zero changes to lines :936-938, :1033, :1416-1427 (verify with git diff — those anchors byte-unchanged).
  </acceptance_criteria>
  <done>A retried resync (sequential second identical submission) yields exactly ONE draft SV row and a WIZARD_DUPLICATE reply; teaser still mints per-call; mypy clean.</done>
</task>

<task type="auto">
  <name>Task 2: SQL gate — real-index SC2 proof</name>
  <files>supabase/tests/test_resync_retry_single_job.sql</files>
  <read_first>
    - supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql (READ IN FULL — the skeleton to copy: DO $$ blocks with RAISE EXCEPTION, literal fixture ids, counts scoped to the fixture strategy id, `job_kind TEXT := 'process_key_long'` at :92, SET LOCAL ROLE service_role, defensive pre-clean, BEGIN…ROLLBACK)
    - supabase/migrations/20260726000225_strategy_verifications_tenant_scope_uniq.sql (the SV unique index under test)
  </read_first>
  <action>
    Create `supabase/tests/test_resync_retry_single_job.sql` copying the analog's entire skeleton (plain PL/pgSQL, NOT pgTAP — it is not installed). Three assertions, each in its own DO block, all counts scoped to this file's OWN literal fixture ids (shared test DB — never a global count), all expected values typed as literals:

    (a) SC2 job half: call `enqueue_compute_job` twice for the same fixture strategy with `p_kind => 'process_key_long'` (the kind /process-key's long-fetch flows enqueue) → assert exactly 1 non-terminal `compute_jobs` row for that strategy. Comment: this is SC2's "exactly ONE server-side effect" — the JOB is the money-bearing effect of record; the RPC dedupes on (strategy_id, kind) via compute_jobs_one_inflight_per_kind_strategy.

    (b) SV index reality — distinct sessions pass: INSERT two `strategy_verifications` draft rows for the fixture strategy with two DIFFERENT literal wizard_session_ids → assert 2 rows for that strategy. Comment: this is WHY teaser/resync duplicates are possible at the DB layer (fresh uuid4 per call slips the index) and why plan 01's app-level pre-check is load-bearing, not redundant.

    (c) SV index reality — same session 23505s: attempt a third INSERT reusing one of the (strategy_id, wizard_session_id) pairs → assert SQLSTATE 23505 is raised (catch it in an EXCEPTION block; RAISE EXCEPTION if it does NOT fire). Comment: the onboard fence (strategy_verifications_strategy_wizard_session_uniq) is real.

    Wrap in BEGIN … ROLLBACK; `SET LOCAL ROLE service_role` to mirror the real caller; filename keeps the `test_*.sql` prefix so CI `sql-tests` (`psql -v ON_ERROR_STOP=1`) auto-discovers it.
  </action>
  <verify>
    <automated>bash -c 'grep -c "RAISE EXCEPTION" supabase/tests/test_resync_retry_single_job.sql | awk "{exit (\$1>=3)?0:1}" && grep -q "process_key_long" supabase/tests/test_resync_retry_single_job.sql && grep -q "ROLLBACK" supabase/tests/test_resync_retry_single_job.sql'</automated>
    <human-check>If TEST-DB credentials are available locally, run the file via psql against the test project (qmnijlgmdhviwzwfyzlc) — otherwise CI sql-tests executes it; do NOT run against PROD.</human-check>
  </verify>
  <acceptance_criteria>
    - File exists at supabase/tests/test_resync_retry_single_job.sql with all three DO blocks; every count query filters on the literal fixture strategy id (grep shows no unscoped `count(*) FROM compute_jobs` without a strategy filter).
    - Expected values are literals (1, 2) — nothing read back from the RPC and re-asserted against itself (oracle independence).
    - `SET LOCAL ROLE service_role` present; final statement is ROLLBACK; zero pgTAP functions used.
    - Assertion (a) comment explicitly states the job is SC2's effect-of-record and WHY (resync mints its own SV row by construction — RESEARCH Pitfall 3).
  </acceptance_criteria>
  <done>CI-discoverable SQL gate proves: double enqueue → 1 non-terminal job (real index), distinct-session SV rows pass (2), same-session 23505s — the full DB-truth substrate for SC2/SC3.</done>
</task>

<task type="auto">
  <name>Task 3: teaser non-idempotency DB pin (SC3 server half)</name>
  <files>analytics-service/tests/test_teaser_non_idempotent.py</files>
  <read_first>
    - analytics-service/tests/test_process_key.py (TestClient-driven /process-key call shapes for teaser) and the task-1 harness in test_resync_draft_dedup.py (reuse its fake-supabase fixture — import or duplicate the fixture, do not invent a third harness)
    - analytics-service/routers/process_key.py:930-940 (the teaser fresh-uuid mint under test)
  </read_first>
  <action>
    Create `analytics-service/tests/test_teaser_non_idempotent.py`: two byte-identical `flow_type="teaser"` submissions through the full app (same harness discipline as task 1 — only `routers.process_key.get_supabase` patched). Assert: (1) exactly TWO `strategy_verifications` rows exist after both calls; (2) their `wizard_session_id` values are DISTINCT (proving the :938 server-side fresh `uuid.uuid4()` mint is genuine — if a refactor ever made teaser reuse a caller-supplied session id, this assertion reddens); (3) both rows carry the teaser anchor strategy id (read the constant name from process_key.py — the :936 overwrite target — and pin its VALUE as a literal in the test, not an import, per oracle independence).

    Comment the WHY at the top: this pin makes the TS no-retry rule (plan 04's teaser one-fetch test) load-bearing rather than decorative — the server genuinely mints a new verification/public_token/lead per submission, so a seam retry would double-mint. Combined with plan-01-task-2's SQL proof that distinct-session rows pass the REAL index, this is the DB-real evidence chain CONTEXT requires ("real DB-observing test, not a mock tautology": the fake store faithfully accepts inserts; the index behavior that could have deduped is proven separately against real Postgres in the SQL gate — state this division of labor in the comment).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_teaser_non_idempotent.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts row count == 2 (literal) and session-id inequality; neither expected value is imported from routers.process_key.
    - Neuter probe observed: temporarily hardcode the :938 mint to a fixed string (MUTANT) → the distinct-session assertion goes RED (1 failed); restore, `grep -rn MUTANT analytics-service/` → 0. Paste the RED into SUMMARY.
    - Full analytics suite still green: `python -m pytest -q` — no new failures.
  </acceptance_criteria>
  <done>Two identical teaser calls provably mint two SV rows with distinct server-minted session ids; the pin reddens if teaser's non-idempotency is ever quietly removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel→Railway seam (/process-key) | a retried request re-crosses with the same body; the server must dedupe or the retry double-executes |
| anonymous teaser → /process-key | unauthenticated, lead/public_token-minting — the widest blast radius on this seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141-01 | Tampering/abuse | teaser retry mints duplicate verification + public_token + lead | mitigate | teaser branch byte-unchanged (deliberately non-idempotent, pinned by task 3); the RETRY side never fires for teaser (plans 03/04 own that half) |
| T-141-02 | Tampering | resync retry mints duplicate draft SV row | mitigate | task 1's strategy-scoped SELECT pre-check → _resume_duplicate_job; sequential-retry class closed; concurrent-tab residual documented |
| T-141-05 | Elevation of Privilege | dedup pre-check reads another tenant's SV row | mitigate | pre-check scoped by strategy_id which already passed the strategies id+user_id ownership gate (140.1-02, PYAPI-01d discipline) — stated in code comment |
| T-141-SC | Tampering | npm/pip/cargo installs | accept (n/a) | zero package installs this phase (REQUIREMENTS Out-of-Scope pins no retry/breaker deps) |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest -q` — full suite, no new failures; `mypy --strict routers/process_key.py` clean.
- `git diff analytics-service/routers/process_key.py` shows the pre-check only; anchors :936-938, :1033, :1416-1427 byte-unchanged.
- SQL gate discoverable: file matches `supabase/tests/test_*.sql`.
</verification>

<success_criteria>
- SC2 server half: retried resync → exactly ONE draft SV row + duplicate reply; ONE non-terminal compute_job proven against the real index (SQL gate).
- SC3 server half: two teaser calls → TWO SV rows, distinct session ids, RED observed on neuter.
- Zero migrations, zero TS changes, zero teaser-path changes.
</success_criteria>

<output>
Create `.planning/phases/141-seam-retry-with-backoff-gated-on-the-idempotency-audit/141-01-SUMMARY.md` when done — include the observed RED outputs (task 1 pre-fix, task 3 neuter) verbatim.
</output>
