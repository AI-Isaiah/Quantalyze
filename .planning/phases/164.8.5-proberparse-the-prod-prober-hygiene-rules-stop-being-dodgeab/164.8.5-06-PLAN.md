---
phase: 164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab
plan: 06
type: execute
wave: 6
depends_on:
  - 164.8.5-05
files_modified:
  - scripts/prod-prober/arms/cron-drift.mjs
  - scripts/prod-prober/run.mjs
autonomous: true
requirements:
  - 164.7-CR03-HYGIENE-BYPASS

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every control Plan 05 added — the executable Vault-read shape, the three resolver arms, the real-snapshot default, the token length threshold, the digit test, the bare-URL exemption, the header-region exclusion, the jwt exemption, and the TOKEN_MIN derivation/pin — has been disabled ALONE, from a byte backup, produced a RED naming its own fixture or scenario, and been restored to byte identity (D3)."
    - "The TOKEN_MIN pin BITES: making TOKEN_MIN diverge from HEADERS_LITERAL_MAX turns the wiring partition test RED (plan-check W3 / D-b)."
    - "The prober half of CONTEXT's 'What done looks like' is complete: CR-03's and SR-02's reproduction commands both return non-empty lists, both pasted."
  artifacts:
    - path: "scripts/prod-prober/arms/cron-drift.mjs"
      provides: "unchanged after the matrix — cmp-verified"
    - path: "scripts/prod-prober/run.mjs"
      provides: "unchanged after the matrix — cmp-verified"
  key_links:
    - from: "neuter.sh (scratchpad, Plan 01)"
      to: "node scripts/prod-prober/run.mjs --self-test / npx vitest run"
      via: "byte backup → single replacement (exit 2 if unchanged) → run → cp restore → cmp"
      pattern: "SELF-TEST"
---

<objective>
Prove, one control at a time, that `vault-absent`'s redesign, `long-token-anywhere`, and the
`TOKEN_MIN` pin can each FAIL — the evidentiary half of Plan 05, split per plan-check W6 — and
close the prober side of the phase with the CR-03 and SR-02 reproductions and the three-suite
vitest run.

Purpose: D3 for criterion 2 and the (h)/(i) family fix; the W3 pin proved to bite; CONTEXT's
"What done looks like" prober half.

Output: a twelve-row matrix with exit codes; both reproduction outputs; the Node-22 note; green
closing runs on a tree identical to Plan 05's end state.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-CONTEXT.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-01-SUMMARY.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-05-SUMMARY.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-REVIEW.md
@scripts/prod-prober/arms/cron-drift.mjs
@scripts/prod-prober/run.mjs
</context>

<decisions_honoured>
- D3 (LOCKED): one control per row, byte backup, `cmp` restore, never `git checkout --`.
- This plan ADDS no scenario, fixture or test; a row without a RED surface is a Plan 05 defect and is fixed there.
- Scope fence: no `supabase/**` file and no `scripts/prod-prober/cron-manifest.json` change.
</decisions_honoured>

<revision_notes>
Plan-check 2026-09-10: this plan is the former plan 04 Task 3, peeled out per W6 (D-a), plus one row proving the W3 (D-b) pin bites. The codeSpans re-proof row is kept (it now has a second RED surface via `hygiene GREEN` once `vault-absent` reads spans).
</revision_notes>

<tasks>

<task type="auto">
  <name>Task 1: Neuter the vault-absent controls, one per run — five rows</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs</files>
  <read_first>
    - 164.8.5-01-SUMMARY.md — the harness.
    - 164.8.5-05-SUMMARY.md — the N and the scenario titles as printed.
  </read_first>
  <action>
One control per run, byte backup, `cmp` restore:
| control | neuter (ONE edit) | expected RED |
| executable shape on spans | make `VAULT_READ_RE` never match | `hygiene GREEN` naming `match_engine_cron` (the flagship DO body) AND the TRI-STATE `DO $$ … FROM vault…` accept |
| resolver: missing file fires | return `{ reads: true }` for a missing file | TRI-STATE `fixture_unknown` reject |
| resolver: transitive edge | depth cap 0 | TRI-STATE `fixture_wrapper` accept |
| resolver: comment/literal in body | test the body's raw text instead of `scanSql(body).masked` | TRI-STATE `fixture_comment_only` / `fixture_literal_only` rejects (one control: the masked-vs-raw choice; both rows are its RED surface) |
| real-snapshot collision | point the default `FUNCTIONS_DIR` at a wrong directory name | the wiring `FUNCTIONS_DIR` pin AND (via the throw) the REAL snapshot scenario |

Every row RED; record exit codes and first RED lines.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed' &amp;&amp; test "$(git status --short -- supabase scripts/prod-prober/cron-manifest.json | wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <done>Five individual neuters, five REDs, five byte-identical restores. Failing direction: any green row blocks; a `cmp` mismatch blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Neuter the token-rule controls and the TOKEN_MIN pin — seven rows — then the phase-level prober checks</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs</files>
  <read_first>
    - 164.7-REVIEW.md CR-03 "Proven" and SR-02 — the two reproduction commands (SR-02 is the `k := 'sk_live_…'` variable shape; run it with a `FAKE-…` digit-bearing value of the SAME length).
    - 164.8.5-CONTEXT.md "What done looks like" — the phase's acceptance sentence; this task closes the prober half of it.
  </read_first>
  <action>
| control | neuter (ONE edit) | expected RED |
| token length | make the rule compare against `TOKEN_MIN + 967` (i.e. 999) at the comparison site only — `TOKEN_MIN`'s definition stays | `hygiene RED` naming the `long-token-anywhere` row |
| TOKEN_MIN pin (W3) | change the definition to `TOKEN_MIN = HEADERS_LITERAL_MAX + 8` | the wiring `TOKEN_MIN equals HEADERS_LITERAL_MAX` test — AND record that `hygiene BYPASS` `header_region_only` stays GREEN in the same run: that is exactly the silent-gap shape the pin exists to catch (a 32–39-char header token now caught by nobody, and no scenario notices — only the pin does) |
| token digit test | remove `/\d/` | the committed-manifest false-positive-budget scenario (RAISE prose fires) |
| bare-URL exemption | remove it | the false-positive-budget scenario (jobid 1's railway URL) or `fixture_bare_url_job` in `hygiene GREEN` |
| header-region exclusion | scan header regions too | `hygiene BYPASS` `header_region_only` row's `expect_not` |
| jwt exemption | remove it | `hygiene RED` isolation on the `jwt-shape` row |
| codeSpans (re-proved) | `DO_PREFIX` never matches | `hygiene GREEN` naming `match_engine_cron` |

Then the phase-level prober checks: `node scripts/prod-prober/run.mjs --self-test` green; `npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/sql-body-normalize.test.ts src/__tests__/gitleaks-allowlist.test.ts` green; `gitleaks detect --no-git -c .gitleaks.toml --source scripts/prod-prober` — a finding FAILS this task (change the string, never `.gitleaks.toml`), absence is a loud `SKIPPED` line naming CI; re-run CR-03's exact reproduction command and the SR-02 measured command and paste both violation lists into the SUMMARY. If Node 22 is available locally (`nvm ls 22` or `command -v node22`), run the self-test under it once and record the result; if not, record that CI (Node 22) is the first run.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/sql-body-normalize.test.ts src/__tests__/gitleaks-allowlist.test.ts 2>&amp;1 | grep -q 'Test Files  3 passed' &amp;&amp; node --input-type=module -e 'import {hygieneViolations as h} from "./scripts/prod-prober/arms/cron-drift.mjs"; const sr02=h("match_engine_cron","DO $$ DECLARE k text; BEGIN k := \x27FAKE-0123456789-0123456789-0123456789ab\x27; PERFORM net.http_post(url := \x27https://x.invalid/a\x27, headers := jsonb_build_object(\x27X-Service-Key\x27, k), body := \x27{}\x27::jsonb); END $$"); process.exit(sr02.length>0?0:1)' &amp;&amp; if command -v gitleaks >/dev/null 2>&amp;1; then gitleaks detect --no-git -c .gitleaks.toml --source scripts/prod-prober; else echo "SKIPPED: gitleaks is not installed locally — CI (gitleaks 8.30.1) is the FIRST run of this scan; record that in the SUMMARY" >&amp;2; fi &amp;&amp; test "$(git status --short -- supabase scripts/prod-prober/cron-manifest.json | wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <done>Seven more individual neuters (twelve in total), each RED, each restored byte-identical; the pin row's output shows `header_region_only` still green beside the RED pin; CR-03 and SR-02 reproductions both non-empty (pasted); the three vitest files pass; gitleaks clean or loudly SKIPPED; the manifest and `supabase/` untouched. Failing direction: any green row blocks; an empty SR-02 list exits 1; a gitleaks finding exits 1; a non-empty `git status` under `supabase/` is a fence breach and blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| neuter harness → working tree | a neuter that does not restore byte-identically corrupts the chain for Plan 07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.5-30 | Repudiation | a control credited as proved by a batch neuter | high | mitigate | one control per row |
| T-164.8.5-31 | Tampering | restore via `git checkout --` | high | mitigate | byte `cp` + `cmp`; exit 2 on a no-op |
| T-164.8.5-32 | Tampering | `TOKEN_MIN` / `HEADERS_LITERAL_MAX` divergence (W3) | medium | mitigate | pin row proves the wiring test reds on divergence while every scenario stays green |
| T-164.8.5-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Twelve-row matrix in the SUMMARY; the pin row records the green `header_region_only` beside the red pin.
- CR-03 and SR-02 reproductions pasted; Node-22 note recorded.
- Both files `cmp`-identical to pre-matrix backups; three vitest files green.
</verification>

<success_criteria>
Every Plan 05 control, and the W3 pin, is individually proved able to fail. The prober half of "What done looks like" is complete; Plan 07 finishes the linter half.
</success_criteria>

<output>
Create `.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-06-SUMMARY.md` when done, with the matrix, both reproduction outputs and the Node-22 note.
</output>
