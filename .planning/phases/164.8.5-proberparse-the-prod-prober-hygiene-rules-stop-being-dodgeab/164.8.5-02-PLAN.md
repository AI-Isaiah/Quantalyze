---
phase: 164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab
plan: 02
type: execute
wave: 2
depends_on:
  - 164.8.5-01
files_modified:
  - scripts/prod-prober/arms/cron-drift.mjs
  - scripts/prod-prober/run.mjs
  - scripts/prod-prober/fixtures/cron-drift/prod-username-changed.json
  - scripts/prod-prober/fixtures/cron-drift/prod-database-changed.json
  - scripts/prod-prober/fixtures/cron-drift/manifest-sha-mismatch.json
  - src/__tests__/prod-prober-wiring.test.ts
autonomous: true
requirements:
  - 164.7-REPAIR-REVERTED

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A PROD job whose username OR database differs from the manifest row is cron-drift naming the field and both values (CR-01) — one ARM_FIXTURE_TABLE red fixture each, individually neuter-proved."
    - "A manifest row that omits or mistypes active, schedule, username or database is manifest-invalid naming the jobname and the field (CR-04 totality, SR-09 one loop) — four direct compareManifest unit tests in the wiring test, each with a calibration."
    - "A manifest row whose command does not hash to its own command_sha256 is manifest-invalid naming the jobname and both 12-hex prefixes (WR-11) — AND the PROD credential scan still fires under that hand-edited state (the fourth HOIST state, added to Plan 01's scenario set)."
    - "The dead changed.includes(\"jobid\") headline term is gone (SR-05), the jobid rationale says an unschedule/re-schedule CYCLE (SR-08), and run.mjs's fixture-table comment carries no count that can rot (SR-07)."
    - "SELF_TEST_SCENARIOS and the wiring test's three literals agree with the printed tail (criterion 9)."
  artifacts:
    - path: "scripts/prod-prober/arms/cron-drift.mjs"
      provides: "username/database comparison; four-field totality loop; sha binding; corrected comments"
      contains: "command_sha256 it publishes"
    - path: "scripts/prod-prober/fixtures/cron-drift/prod-username-changed.json"
      provides: "red control for the username arm"
    - path: "scripts/prod-prober/fixtures/cron-drift/prod-database-changed.json"
      provides: "red control for the database arm"
    - path: "scripts/prod-prober/fixtures/cron-drift/manifest-sha-mismatch.json"
      provides: "red control for WR-11 and the HAND-EDITED hoist state"
    - path: "src/__tests__/prod-prober-wiring.test.ts"
      provides: "four CR-04 validator unit tests + calibrations; bumped scenario pin"
  key_links:
    - from: "scripts/prod-prober/run.mjs ARM_FIXTURE_TABLE cron-drift red map"
      to: "fixtures/cron-drift/prod-username-changed.json, prod-database-changed.json"
      via: "the red loop asserts exactly one cron-drift per fixture; KIND_ASSERTIONS already has a cron-drift entry so no new by-name assertion is needed"
      pattern: "prod-database-changed.json"
    - from: "compareManifest section (1) row validation"
      to: "sha256Hex(normalizeCommand(row.command)) === row.command_sha256"
      via: "WR-11 binding, BELOW section (0) so it cannot silence the credential scan"
      pattern: "sha256Hex\\(normalizeCommand\\(row\\.command\\)\\)"
---

<objective>
Re-apply the four reverted repairs — CR-01 (username/database uncompared), CR-04 (Boolean(undefined)
agrees with a deactivated job), WR-11 (command unbound from its sha), and the SR-05/07/08/09
comment and shape fixes — now that Plan 01's section (0) guarantees none of the new validation
returns can silence the credential scan. Each control ships its own red fixture, neuter-proved
individually (D3), which is the evidence the reverted repair lacked (SR-03: five of six controls
could not fail).

Purpose: `[164.7-REPAIR-REVERTED]` items (2) and (3) — "re-apply the four repairs; ship a red
fixture for EACH, neuter-proved INDIVIDUALLY".

Output: the repairs in `cron-drift.mjs`; three fixtures; two loop scenarios (via the red map) and
one WR-11 scenario plus the HAND-EDITED hoist state; four validator unit tests with calibrations.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-CONTEXT.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-01-SUMMARY.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-REVIEW.md
@scripts/prod-prober/arms/cron-drift.mjs
@scripts/prod-prober/run.mjs
@src/__tests__/prod-prober-wiring.test.ts
</context>

<decisions_honoured>
- D1 (LOCKED): every new validation return sits BELOW section (0). The HAND-EDITED hoist state proves it.
- D3 (LOCKED): one red control per control; individual neuters from byte backups (Task 3).
- D5 (LOCKED): four literals across two files, read from the printed tail.
- `[164.7-REPAIR-REVERTED]`: CR-03's comment strip is NOT re-applied (plans 03/04). jobid is compared NOWHERE — SR-08's field-coverage argument is the load-bearing reason and stays.
- Scope fence: no `supabase/**` file is modified.
</decisions_honoured>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 username/database comparison + CR-04 four-field totality (one loop, SR-09) + SR-05/SR-08 — with two ARM_FIXTURE_TABLE red fixtures</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs, scripts/prod-prober/fixtures/cron-drift/prod-username-changed.json, scripts/prod-prober/fixtures/cron-drift/prod-database-changed.json, src/__tests__/prod-prober-wiring.test.ts</files>
  <read_first>
    - 164.7-REVIEW.md CR-01 (the proven zero-defect repoint `postgres` → `supabase_admin`, `attacker_db`), CR-04 (Boolean(undefined) agrees with a DISABLED job), SR-05 (dead `changed.includes("jobid")` term), SR-08 (an ordinary `cron.schedule` UPSERT preserves the jobid; only an unschedule/re-schedule CYCLE allocates a new one), SR-09 (one loop for the field presence checks).
    - scripts/prod-prober/arms/cron-drift.mjs `compareManifest` section (1) row loop (jobname / command_sha256 / withheld pair) — the totality checks go INSIDE this loop, after the sha check, BELOW Plan 01's section (0).
    - scripts/prod-prober/arms/cron-drift.mjs the `changed` list in the per-job comparison and the headline builder — where `username`/`database` are added and where the dead jobid term is deleted.
    - scripts/prod-prober/run.mjs `ARM_FIXTURE_TABLE` cron-drift entry — the `red` map and its "SIX fixtures, ONE kind" comment (SR-07's target).
    - scripts/prod-prober/fixtures/cron-drift/prod-active-flipped.json — the fixture shape to byte-copy.
    - src/__tests__/prod-prober-wiring.test.ts any existing `CALIBRATION:` `it()` — the S4 shape (mutant differs from original, predicate flips).
  </read_first>
  <action>
**(a) Comparison (CR-01).** Beside the schedule/active/command tests add `username` and `database`: `String(m[f] ?? "").trim() !== String(p[f] ?? "").trim()` → `changed.push(f)`. Extend the headline the way `schedule` is spelled out: `(username postgres → supabase_admin)` and `(database … → …)`. Delete the `changed.includes("jobid")` term (SR-05) — it is dead by construction. Rewrite the jobid decision comment per SR-08: an ordinary reschedule PRESERVES the id; only an unschedule/re-schedule CYCLE allocates a new one; and every field a jobid could proxy for is already compared, which is the load-bearing reason it is not compared.

**(b) Totality (CR-04, one loop per SR-09).** Inside section (1)'s row loop, after the withheld-pair check: `if (typeof row.active !== "boolean") return invalid(<jobname, field active, Boolean(undefined) is false so an omitted flag silently AGREES with a deactivated PROD job — the quietest form of the outage this arm exists for>)`; then ONE loop `for (const field of ["schedule", "username", "database"])` requiring a non-empty trimmed string, `invalid()` naming jobname and field. The per-field rationale goes in a comment above the loop, not as three copies of the predicate.

**(c) Fixtures.** `prod-username-changed.json`: byte-copy of `prod-ok.json` with exactly ONE row's `username` changed `postgres` → `supabase_admin`. `prod-database-changed.json`: byte-copy with exactly one row's `database` changed `postgres` → `template1`. Register both in the cron-drift `red` map as `"cron-drift"`. Rewrite the map's comment count-free per SR-07: `… ONE kind, each with a DIFFERENT cause. cron-drift is not one kind per fixture: …` — no numeral that can rot on the next fixture.

**(d) Unit tests (SR-03 item 4, no scenario bump).** In the wiring test, import `compareManifest` and load `fixtures/cron-drift/manifest.json` + `prod-ok.json`; for each of `active`, `schedule`, `username`, `database`: deep-copy the manifest, delete the key from the `match_engine_cron` row, call `compareManifest(copy, prodRows, { liveMarker: <the fixture's database_marker> })`, assert exactly one `manifest-invalid` whose detail contains `match_engine_cron` and the field name. Add ONE calibration: the untouched deep copy with the same `liveMarker` yields ZERO `manifest-invalid` — and assert the mutated copy differs from the original (a delete that hit a missing key would pass vacuously).

**(e) D5 bump** — the two new red-map entries add two loop scenarios; read the tail and set the four literals.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | grep -q 'cron-drift fires on prod-username-changed.json' &amp;&amp; node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | grep -q 'cron-drift fires on prod-database-changed.json' &amp;&amp; node --input-type=module -e 'import {compareManifest} from "./scripts/prod-prober/arms/cron-drift.mjs"; import {readFileSync} from "node:fs"; const man=JSON.parse(readFileSync("scripts/prod-prober/fixtures/cron-drift/manifest.json","utf8")); const prod=JSON.parse(readFileSync("scripts/prod-prober/fixtures/cron-drift/prod-ok.json","utf8")); delete man.jobs.find(j=>j.jobname==="match_engine_cron").active; const r=compareManifest(man,prod,{liveMarker:man.database_marker}); process.exit(r.defects.filter(d=>d.kind==="manifest-invalid"&amp;&amp;String(d.detail).includes("active")).length===1?0:1)' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed'</automated>
  </verify>
  <done>Two new red fixtures each isolate exactly one cron-drift naming the changed field; a manifest row missing `active` is manifest-invalid naming the field; the dead jobid term is gone. Failing direction: deleting the `username` comparison line reds the `prod-username-changed.json` loop scenario; replacing the `typeof row.active !== "boolean"` guard with `false` makes the inline node check exit 1.</done>
</task>

<task type="auto">
  <name>Task 2: WR-11 sha binding + manifest-sha-mismatch fixture + the HAND-EDITED hoist state</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs, scripts/prod-prober/fixtures/cron-drift/manifest-sha-mismatch.json, src/__tests__/prod-prober-wiring.test.ts</files>
  <read_first>
    - 164.7-REVIEW.md WR-11 — the proof that a hand-cleaned `command` beside a dirty sha reports clean on the manifest side and prints a fictional diff; the prescribed clause (derived sha vs published sha, 12-hex prefixes, "Re-capture rather than hand-editing; to hide text, use the withhold procedure").
    - 164.7-REVIEW.md SR-01 — the measurement that the sha-binding return SILENCED the credential scan when it sat above the PROD loop. Plan 01's section (0) is why it now cannot; this task proves it.
    - scripts/prod-prober/run.mjs Plan 01's three `HOIST:` scenarios — the fourth state is added in the same style.
    - scripts/prod-prober/fixtures/cron-drift/manifest-inline-key.json — the dirty oracle whose `match_engine_cron` command carries `FAKE-inline-key`; `prod-inline-key.json` is its PROD twin.
  </read_first>
  <action>
**(a) Binding.** Inside section (1)'s row loop, when `typeof row.command === "string"`: compute `sha256Hex(normalizeCommand(row.command))`; if it differs from `row.command_sha256`, `return invalid(<jobname, derived prefix, published prefix, "The two fields describe different text, so the printed diff and the manifest-side hygiene scan are about a command that was never captured. Re-capture rather than hand-editing; to hide text, use the withhold procedure.">)`. Prefixes are `slice(0, 12)` — truncated digests of command TEXT, already public in the manifest (REVIEW "No new leak surface").

**(b) Fixture `manifest-sha-mismatch.json`:** byte-copy of `manifest-inline-key.json` with the `match_engine_cron` row's `command` hand-edited to a CLEAN Vault-backed body (copy `hygiene-green.json`'s `match_engine_cron` command) while its `command_sha256` is left as the DIRTY command's sha. This is WR-11's exact adversary: the committed text looks tidy, the sha still describes the inline key.

**(c) Scenarios.** (1) "WR-11: a manifest row whose command does not hash to its own sha is manifest-invalid naming the jobname" — `driftRun(prodInlineKey.data, driftFixturePath("manifest-sha-mismatch.json"), quiet)`; assert exactly one `manifest-invalid` whose detail contains `match_engine_cron` and `command_sha256 it publishes`, and that NO `manifest:` credential finding is emitted (the manifest-side loop never ran on fabricated text — assert via `noDefectOfKind` on nothing; instead assert `defects.filter(d => d.subject === "manifest:match_engine_cron").length === 0`). (2) "HOIST: the PROD credential scan fires with the oracle HAND-EDITED (sha mismatch)" — same run; assert `cron-secret-in-command` with subject `prod:match_engine_cron` is present. Two scenarios, because the RED for WR-11 and the RED for the hoist must be attributable to different neuters (D3).

**(d) D5 bump** from the printed tail, four literals.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; grep -q 'HAND-EDITED' scripts/prod-prober/run.mjs &amp;&amp; grep -q 'command_sha256 it publishes' scripts/prod-prober/arms/cron-drift.mjs &amp;&amp; node --input-type=module -e 'import {compareManifest,sha256Hex,normalizeCommand} from "./scripts/prod-prober/arms/cron-drift.mjs"; import {readFileSync} from "node:fs"; const man=JSON.parse(readFileSync("scripts/prod-prober/fixtures/cron-drift/manifest-sha-mismatch.json","utf8")); const prod=JSON.parse(readFileSync("scripts/prod-prober/fixtures/cron-drift/prod-inline-key.json","utf8")); const row=man.jobs.find(j=>j.jobname==="match_engine_cron"); if(sha256Hex(normalizeCommand(row.command))===row.command_sha256){console.error("fixture is not a mismatch");process.exit(2)} const r=compareManifest(man,prod,{liveMarker:man.database_marker}); const ok=r.defects.some(d=>d.kind==="manifest-invalid")&amp;&amp;r.defects.some(d=>d.kind==="cron-secret-in-command"&amp;&amp;d.subject==="prod:match_engine_cron"); process.exit(ok?0:1)' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed'</automated>
  </verify>
  <done>A hand-edited command beside its old sha is manifest-invalid, and the PROD credential is STILL reported in the same run — SR-01's exact regression is now impossible by test. Failing direction: replacing the sha inequality with `false` reds the WR-11 scenario; moving the binding above section (0) reds the HAND-EDITED hoist scenario.</done>
</task>

<task type="auto">
  <name>Task 3: Individual neuter matrix for the four repairs, from byte backups</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs</files>
  <read_first>
    - 164.8.5-01-SUMMARY.md — the `neuter.sh` harness location and matrix format from Plan 01; reuse the harness (it lives in the session scratchpad, not the repo).
    - 164.7-REVIEW.md SR-03 — the six-row matrix where five controls were vacuous; this task is that matrix done right.
  </read_first>
  <action>
Run the Plan 01 harness once per control, one control per run, each restored and `cmp`-verified before the next:
| control | neuter | expected RED |
| CR-01 username | delete the `username` comparison (restrict the field list to `["database"]`) | `cron-drift fires on prod-username-changed.json` |
| CR-01 database | restrict the field list to `["username"]` | `cron-drift fires on prod-database-changed.json` |
| CR-04 active | replace the `typeof row.active !== "boolean"` guard with `false` | the wiring test's `active` validator `it()` (run `npx vitest run src/__tests__/prod-prober-wiring.test.ts` for this row instead of the self-test; the harness's expected-substring check runs against that output) |
| CR-04 loop | make the field loop iterate an empty list | the wiring test's `schedule`/`username`/`database` validator `it()`s |
| WR-11 | replace the derived-vs-published inequality with `false` | `WR-11: a manifest row whose command does not hash` |
| hoist (regression guard) | move the WR-11 `invalid()` ABOVE section (0) by inserting a duplicate sha check before it | `HOIST: … HAND-EDITED` |

Record the matrix with exit codes in the SUMMARY. A green result on any row is a control that cannot fail: fix the fixture or scenario, re-run, and only then continue. Finish with both quick runs green and `git status --short` showing only this plan's six files.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed' &amp;&amp; test "$(git status --short -- supabase | wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <done>Six individual neuters, six REDs each naming its own control, six byte-identical restores; recorded in the SUMMARY. Failing direction: any row that stays green blocks the plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD `cron.job` role/database columns → arm | a repointed execution role is a privilege change the arm must see |
| committed oracle → arm | hand-edited text beside a machine sha (WR-11's adversary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.5-06 | Elevation of privilege | cron job repointed to another role/database unseen | high | mitigate | CR-01 comparison + two red fixtures |
| T-164.8.5-07 | Tampering | manifest row omits `active` and agrees with a disabled job | high | mitigate | CR-04 totality + validator unit tests with calibration |
| T-164.8.5-08 | Repudiation | manifest `command` text unbound from its sha; fictional diff | medium | mitigate | WR-11 binding, prefixes only, HAND-EDITED hoist scenario proves the credential scan survives |
| T-164.8.5-09 | Information disclosure | headline interpolates role/database names | low | accept | pg_cron role/database names are never credential-bearing (REVIEW "No new leak surface") |
| T-164.8.5-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed |
</threat_model>

<verification>
- Self-test green at the new N; wiring test green with four agreeing literals.
- Task 3 matrix in the SUMMARY: six controls, six individual REDs.
- `git status --short supabase` empty.
</verification>

<success_criteria>
`[164.7-REPAIR-REVERTED]` items (2), (3) and the three comment fixes are done with a red control each; T-164.7-07's "rules stay in force" clause now survives every oracle state including hand-edit. Nothing under `supabase/` moved.
</success_criteria>

<output>
Create `.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-02-SUMMARY.md` when done, with the neuter matrix and the N read from the tail.
</output>
