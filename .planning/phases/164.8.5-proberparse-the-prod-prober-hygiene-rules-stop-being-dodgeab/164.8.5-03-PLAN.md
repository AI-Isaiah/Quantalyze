---
phase: 164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab
plan: 03
type: execute
wave: 3
depends_on:
  - 164.8.5-02
files_modified:
  - scripts/prod-prober/arms/cron-drift.mjs
  - scripts/prod-prober/run.mjs
  - scripts/prod-prober/fixtures/cron-drift/hygiene-red.json
  - scripts/prod-prober/fixtures/cron-drift/hygiene-green.json
  - scripts/prod-prober/fixtures/cron-drift/hygiene-bypass.json
  - src/__tests__/prod-prober-wiring.test.ts
autonomous: true
requirements:
  - 164.7-CR03-HYGIENE-BYPASS
  - 164.7-REVIEW-INFO-FOUR

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "cron-drift.mjs imports scanSql from ../../sql-body-normalize.mjs and exports codeSpans(sql): the outer command plus every DO body, recursively, each span carrying { base, sql, masked, dollarRegions }; the real jobid-1 DO $body$ command and the GREEN match_engine_cron fixture both expose their Vault read on a code span (CONTEXT CORRECTION)."
    - "Every hygiene rule reads codeSpans(): the three header rules and long-literal-in-headers on each span's masked text (literal boundaries from masked, NAME anchored on sql and confirmed by masked[idx] === quote), the five text-shape rules (app-guc, pg-password, bearer-literal, service-role, jwt-shape) on each span's raw sql. matchingParen and singleQuotedLiterals are deleted."
    - "The three header rules length-test the DERIVED value: sum of literal contents that are direct operands of ||, or arguments of an allowlisted string builder (concat, format, chr counts 1, decode, convert_from, quote_literal, …), never descending into (SELECT … or a non-allowlisted function — the green 'Bearer ' || (SELECT …) shape sums to 7 and stays clean (criterion 3, D4: HEADER_LITERAL_MIN untouched)."
    - "long-literal-in-headers ALSO length-tests the DERIVED value of EVERY depth-0 argument in a header region at HEADERS_LITERAL_MAX, so a ||-split or builder-assembled value under ANY header name (not only the three anchored ones) fires — the plan-check's W4 shape is covered, not booked. The threshold VALUE is untouched (D4); only the measurement fed to it changed."
    - "$q$don't$q$ and $q$dont$q$ in a header list produce the SAME verdict set, both shipped as fixtures (criterion 4)."
    - "headerRegions(span) returns a LIST of { from, to, unparseable } regions; the no-paren headers := '{…}'::jsonb form (bypass (i)'s inline spelling) is preserved as a [from, semi) region; an unparseable region spans to the end of the span, is skipped by every header-rule walk AND by the token rule, and yields exactly one verdict: [header-unparseable], classified as measure-fail by compareManifest/run(); no new DEFECT_KIND is added."
    - "The 14 committed manifest commands produce ZERO violations from EVERY rule, asserted by a counted scenario that lands in THIS plan (the same wave as the derived-length rules it guards), with a floor of at least 10 rows judged; Plan 05's token rule is covered by the same scenario by construction."
    - "pg-password catches v_password := (masked, plpgsql assignment) and postgres://user:pw@host (raw URI) and PGPASSWORD: — each with a red row (IN-04)."
    - "hygiene-red's hand-typed row count is replaced by a relation derived from HYGIENE_RULE_IDS plus a judged-rows tally; hygiene-bypass.json rows (a)-(g), the non-anchored split (j) and the apostrophe pair each fire at least one NAMED rule."
  artifacts:
    - path: "scripts/prod-prober/arms/cron-drift.mjs"
      provides: "codeSpans, derived-length header rules on masked (anchored ids AND long-literal-in-headers), header-unparseable, IN-04 pg-password"
      exports: ["codeSpans", "hygieneViolations", "HYGIENE_RULE_IDS"]
      contains: "sql-body-normalize.mjs"
    - path: "scripts/prod-prober/fixtures/cron-drift/hygiene-bypass.json"
      provides: "shapes (a)-(g), (j) non-anchored split, the apostrophe pair, each with the rule ids expected to fire"
    - path: "scripts/prod-prober/fixtures/cron-drift/hygiene-red.json"
      provides: "one row per rule id including header-unparseable, plus the IN-04 pg-password rows, the derived-length rows under the anchored ids, and the non-anchored split row under long-literal-in-headers"
    - path: "scripts/prod-prober/run.mjs"
      provides: "bypass scenario, apostrophe-pair scenario, derived-length GREEN scenario, committed-manifest zero-FP scenario, count-free RED scenario, measure-fail classification"
  key_links:
    - from: "scripts/prod-prober/arms/cron-drift.mjs"
      to: "scripts/sql-body-normalize.mjs scanSql"
      via: "the FIRST ../ ESM import between .mjs files under scripts/ (PATTERNS §6) — justified in a header comment: the reverted repair shipped a naive duplicate one import away from the correct lexer"
      pattern: "from \"\\.\\./\\.\\./sql-body-normalize\\.mjs\""
    - from: "hygieneViolations"
      to: "codeSpans(command)"
      via: "every rule iterates the span list; none reads the raw command directly"
      pattern: "codeSpans\\("
    - from: "compareManifest section (0) / manifest loop"
      to: "defect kind by rule-id prefix"
      via: "[header-unparseable] → measure-fail; everything else → cron-secret-in-command"
      pattern: "header-unparseable"
    - from: "scripts/prod-prober/run.mjs false-positive budget scenario"
      to: "scripts/prod-prober/cron-manifest.json"
      via: "zero violations from every rule across every committed command, asserted the same wave the derived-length rules land"
      pattern: "cron-manifest.json"
---

<objective>
Give the hygiene rules a real lexer and stop them being dodgeable by spelling: import `scanSql`,
add the `codeSpans()` recursion so a `DO $tag$ … $tag$` body is CODE (4 of the 14 real PROD
commands are that shape — a masked-only rule reads the flagship job as `vault-exec: false`),
rewrite the header rules to length-test the DERIVED value on masked text — for the three anchored
names at `HEADER_LITERAL_MIN` AND for every header argument at `HEADERS_LITERAL_MAX` — make an
unparseable header region a loud rule of its own, and sweep IN-04's `pg-password` forms. This plan
covers bypass shapes (a)–(g) plus the non-anchored split (j); (h)/(i) and the `vault-absent`
redesign are Plan 05, which builds on the spans this plan proves. Plan 04 is this plan's neuter
matrix — split out (plan-check W6) so neither half runs out of budget.

Purpose: criteria 3 and 4; the first two of CONTEXT's three family fixes; the design-changing
finding of RESEARCH Q2/Q3; T-164.7-07 parts (b) and (c); plan-check W4 covered as a class.

Output: `codeSpans`, the rewritten rules, `header-unparseable`, IN-04 forms; `hygiene-bypass.json`;
red/green rows; the derived-length GREEN scenario; the committed-manifest FP-budget scenario; the
RED scenario's count relation fixed; the name-anchor calibration in the wiring test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-CONTEXT.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-RESEARCH.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-PATTERNS.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-02-SUMMARY.md
@scripts/sql-body-normalize.mjs
@scripts/prod-prober/arms/cron-drift.mjs
@scripts/prod-prober/run.mjs
@scripts/prod-prober/cron-manifest.json
@scripts/prod-prober/fixtures/cron-drift/hygiene-red.json
@scripts/prod-prober/fixtures/cron-drift/hygiene-green.json
</context>

<decisions_honoured>
- CONTEXT "do not write a lexer" + ⛔ CORRECTION: import `scanSql`; add `codeSpans` in the ARM (not the normalizer — VAC-04/VAC-08 compare function bodies and the recursion would change hashes; RESEARCH Architectural Map).
- D4 (LOCKED): `HEADER_LITERAL_MIN` stays 16 and `HEADERS_LITERAL_MAX` stays 32; no naive comment-strip. Feeding `long-literal-in-headers` a DERIVED length is a change to the MEASUREMENT, not to the threshold — D4 protects the values.
- D7 (LOCKED): the header NAME is a literal and is invisible in `masked`; the choice is: anchor the name on `sql` and confirm `masked[idx] === "'"`, then walk the VALUE on `masked`. Proven by the criterion-4 pair.
- Q2 decision (CONTEXT): `long-literal-in-headers` keeps its id and threshold; Plan 05's token rule is scoped to non-header regions.
- Q3 (CONTEXT): fixtures use low-entropy `FAKE-…` strings with digits; `.gitleaks.toml` is NOT edited.
- D5: four literals, read from the tail. D3: individual neuters — Plan 04.
- Design choice recorded (Claude's discretion): the derived-length sum feeds the EXISTING header rule ids rather than a new id — the defect is "this header carries a literal value" regardless of how the value was assembled; PATTERNS suggested its own id, RESEARCH Q3 folds it in; folding is chosen so one defect does not report under two names depending on spelling. The false-positive direction gets its own GREEN scenario instead.
</decisions_honoured>

<revision_notes>
Plan-check 2026-09-10 findings addressed in this file:
- W6 (D-a) APPLIED: this plan is the IMPLEMENTATION half of the former plan 03; its nine-row neuter matrix is now Plan 04. Two tasks, both inside the smart zone.
- W4 (D-c) COVERED, not booked: `long-literal-in-headers` now runs the derived-length sum over every depth-0 argument of every header region at `HEADERS_LITERAL_MAX`, so a `||`-split or builder-assembled value under a header name outside the three anchored ones fires. Red row + bypass row (j) prove it. The stated residual — a derived value of 16–31 characters under a NON-anchored name — is below `HEADERS_LITERAL_MAX`, which D4 locks; it is the pre-existing threshold policy (the three anchored names are the ones the achievable configuration sends), recorded in the docstring beside A2 as an accepted residual, NOT a bypass family.
- W5 FIXED: the committed-manifest zero-FP scenario lands in Task 1(h) of THIS plan, the same wave as the derived-length rules; Plan 05 does not add a second copy.
- W7 FIXED: `headerRegions` is respecified as a LIST; the no-paren `[from, semi)` branch is preserved; an unparseable region's `to` is the span end, it is skipped by header walks AND excluded from Plan 05's token-rule scan, and `[header-unparseable]` is the only verdict for it.
- B3 FIXED: Task 2's gitleaks verify is an `if/else` with no `--exit-code`/redirect swallow — a FINDING exits 1 and fails the `&&` chain; ABSENCE prints a loud `SKIPPED` line naming CI as the first run.
- The name-anchor wiring-test CALIBRATION (formerly written during the matrix) is added in Task 2(e) here, so Plan 04 only neuters.
</revision_notes>

<tasks>

<task type="tracer">
  <name>Task 1: Import scanSql, add codeSpans, rewrite the header rules on masked text with derived-length (anchored ids at HEADER_LITERAL_MIN, every header argument at HEADERS_LITERAL_MAX), and land the committed-manifest FP guard — the GREEN match_engine_cron DO-body, the 'Bearer ' || (SELECT …) shape and the 14 real commands are the first assertions</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs, scripts/prod-prober/fixtures/cron-drift/hygiene-red.json, scripts/prod-prober/fixtures/cron-drift/hygiene-green.json</files>
  <reversibility rating="reversible">All changes are inside one arm behind the unchanged `hygieneViolations(jobname, command)` contract; swapping the lexer back is a local edit.</reversibility>
  <read_first>
    - scripts/sql-body-normalize.mjs `scanSql` docblock and body — `masked` (same length, delimiters kept, contents blanked, newlines preserved), `stripped`, `dollarRegions` `{start, tag, contentStart, contentEnd, end}` with nested tags absorbed. Also `extractFunctionDefs` — THE index-preserving idiom to copy (one call, one destructuring; match/walk on `masked`; slice values from `sql`; depth loop for parens), and `countArgs` (depth-aware split over masked text — the exact helper shape for splitting a header region into depth-0 arguments).
    - scripts/sql-body-normalize.mjs `invokedDirectly` — the main-module guard; run `node -e 'import("./scripts/sql-body-normalize.mjs").then(()=>console.log("ok"))'` once and confirm NOTHING but `ok` prints before relying on the import.
    - 164.8.5-RESEARCH.md Q2 — the `codeSpans` prototype (`DO_PREFIX`, `MAX_DOLLAR_DEPTH = 4` throw, recursion on `dollarRegions` whose masked prefix ends in `DO [LANGUAGE x]`), the rule-by-rule table (which rules move to masked, which stay raw), and the measured `naive: false / withDo: true` table.
    - 164.8.5-RESEARCH.md Q3 — the derived-length walk: from just after the name's `,` to the next depth-0 `,` or `)` on masked; sum literal contents at depth 0 or inside an allowlisted BUILDERS callee; `chr(` counts 1; NEVER descend into `(SELECT` or a non-allowlisted function; measured sums (b 16 / c 24 / d 24 / e 38 / g 24 / split 28 / greenbearer 7 / greenvar 0); the accepted residual (`my_fn('…') || '…'` → 8, recorded in the docstring, NOT fixed by descending everywhere — that re-creates the SELECT false positive).
    - 164.8.5-RESEARCH.md Q4 — the token census of the committed manifest (longest literal tokens: 79 bare URL on `match_engine_cron`, 34 `retention_notification_dispatches:`, 32 `reconcile_dropped_enqueue_sweep:`) — these are the values the FP-budget scenario must stay green over.
    - scripts/prod-prober/arms/cron-drift.mjs `headerCarriesLiteral`, `headerRegions`, `matchingParen`, `singleQuotedLiterals` and the `long-literal-in-headers` loop — what is replaced. Note the LIVE `headerRegions` (a) returns an ARRAY of `[from, to]` pairs, (b) has TWO branches under `headers\s*:=` — a paren branch and a no-paren `headers := '{…}'::jsonb` branch whose region is `[from, semi)`, and (c) silently widens to `text.length` on an unmatched paren. (a) and (b) are PRESERVED; (c) becomes `header-unparseable`.
    - 164.8.5-PATTERNS.md §6 — the import specifier convention (relative, explicit `.mjs`, no barrel) and the two workflow `paths:` filters that now indirectly guard the prober; §7 — the five idiom rules.
    - scripts/prod-prober/run.mjs `scenario("hygiene RED: all ten rules fire …")` — the `red.data.length === 10` literal and the "all ten" title/prose; `scenario("hygiene GREEN: …")`; `loadFixture` and the S2 missing-file rule for the manifest load in (h).
  </read_first>
  <action>
**(a) Import + justification.** Add `import { scanSql } from "../../sql-body-normalize.mjs";` beside the `node:` imports with a comment block stating: this is the first upward `.mjs` import under `scripts/` (PATTERNS §6 census: 16 hits, all sibling/downward); it is deliberate — the reverted repair `84b21cb5` shipped a NAIVE comment-strip one import away from the quote-aware, dollar-aware lexer this module already unit-tests; a future edit to the normalizer can now break this arm's self-test and `src/__tests__/prod-prober-wiring.test.ts` is the tripwire; the three open `[VAC04-C*]` items against the normalizer concern `extractFunctionDefs`/`readQualifiedName`, not `scanSql`. Name the memory: "a control that cannot fail" — a second lexer would be one.

**(b) `codeSpans(sql, base = 0, depth = 0, out = [])`, EXPORTED**, per the RESEARCH prototype: throw `Error` past `MAX_DOLLAR_DEPTH` (the runner's throw→measure-fail mapping carries it); push `{ base, sql, masked, dollarRegions }`; recurse into each dollar region whose `masked.slice(0, r.start)` matches `DO_PREFIX = /\bDO\s+(?:LANGUAGE\s+[A-Za-z_]+\s+)?$/i`. Docstring: why a DO body is CODE for a cron command but a STRING for the normalizer (hash stability of VAC-04/VAC-08), and RESEARCH A1 (only the `DO LANGUAGE x $$` prefix form needs handling).

**(c) Helpers on masked text, replacing `matchingParen`/`singleQuotedLiterals` (delete both):** `literalsIn(span, from, to)` returns `{ start, end, content }` for every literal whose delimiters `masked` marks (single-quote runs, and `dollarRegions` inside `[from, to)`), `content` sliced from `span.sql` with `''` collapsed; `closingParen(masked, openIdx)` is a depth loop over `masked` returning `-1` when unbalanced. **`headerRegions(span)` returns an ARRAY of `{ from, to, unparseable }`** (W7): it runs both regexes on `masked`; for `jsonb_build_object\s*\(` and for the paren branch of `headers\s*:=`, `to` is `closingParen(...)`; when that is `-1` the region is `{ from, to: span.masked.length, unparseable: true }` — it NEVER silently widens as a judgeable region. The no-paren branch `headers := '{…}'::jsonb` is PRESERVED verbatim as `{ from, to: semi === -1 ? span.masked.length : semi, unparseable: false }` — that is bypass (i)'s inline spelling and Plan 05's token rule must still see it as a header region. Docstring rule for an unparseable region: every header-rule walk skips it, Plan 05's token rule EXCLUDES its literals from scanning, and `[header-unparseable]` is the ONLY verdict for it — an unjudgeable region is neither claimed clean nor claimed dirty; it is refused, loudly.

**(d) Header rules (x-service-key-literal / authorization-literal / apikey-literal).** `headerCarriesLiteral(span, headerName)`: find `'<Name>'\s*,` on `span.sql` (case-insensitive, escaped), require `span.masked[m.index] === "'"` so a name inside a comment or inside a bigger literal (bypass (i)'s JSON) is NOT an anchor; then `derivedLiteralLength(span, valueStart, stopAt)` walks masked from just after the comma to the next depth-0 `,` or `)` (or `stopAt`), summing per RESEARCH Q3 with `BUILDERS = /(?:concat|concat_ws|format|chr|decode|encode|convert_from|quote_literal|lower|upper|replace|reverse|translate|btrim|trim|substr|substring|left|right|repeat|lpad|rpad|regexp_replace|split_part|to_hex)\s*$/i` tested on the masked text before each `(`; `chr(` contributes 1; a `(` whose preceding masked text ends in a non-allowlisted callee, or whose contents begin with `SELECT`, is skipped as a whole. Fire when the sum `>= HEADER_LITERAL_MIN`. Record the residual in the docstring (non-allowlisted function + split).

**(e) `long-literal-in-headers` — DERIVED per argument (W4 coverage).** For each parseable header region, split `[from, to)` into depth-0 arguments on `masked` (the `countArgs` idiom — commas at paren depth 0, literal contents already blanked so a comma inside a literal cannot split); for EVERY argument compute `derivedLiteralLength` over that argument's range; fire once when any argument's sum `>= HEADERS_LITERAL_MAX`, then break. A single literal of 32+ characters sums to itself, so the existing red row keeps firing; `$q$…$q$` literals now count (criterion 4); a `||`-split or `concat`/`format`/`chr`/`decode` value under ANY header name — `'X-Auth'`, `'X-Trace'`, the whole-JSON no-paren form — now fires when its derived length reaches the threshold, which is exactly what an unsplit literal of that length already did. Docstring records, beside A2, the accepted residual this does not close and why: a derived value of 16–31 characters under a NON-anchored name sits below `HEADERS_LITERAL_MAX`, whose VALUE D4 locks; the three anchored names at `HEADER_LITERAL_MIN` are the ones the achievable configuration sends a credential under, and lowering the general threshold fires on `'application/json'`-class constants. That residual is the threshold policy, not a bypass family.

**(f) `header-unparseable`** — append to `HYGIENE_RULE_IDS`; when any header region is `unparseable`, `say("header-unparseable", <job name; a header argument list whose parentheses do not balance cannot be judged, and an unjudgeable command is not a clean one>)` and skip the header-rule walks for that region only.

**(g) Rule iteration.** `hygieneViolations` computes `spans = codeSpans(text)` once and runs: header rules + long-literal on every span (masked); `app-guc`, `pg-password`, `bearer-literal`, `service-role`, `jwt-shape` on every span's raw `sql` (RESEARCH Q2: `'app.` is inside a literal and would be blanked; the others are text-shape rules). `vault-absent` keeps its current `includes()` body in THIS plan (Plan 05 redesigns it). De-duplicate: each rule id says at most once per command.

**(h) Fixtures + scenarios.** `hygiene-red.json`: add a `header-unparseable` row (`headers := jsonb_build_object('X-Trace', 'a'` — no close; must fire ONLY that id). Add derived-length rows under the EXISTING anchored ids: `x-service-key-literal` with `'FAKE-key-' || '0123456789ab'` (split), `concat('FAKE-key-','0123456789ab')`, `format('%s%s','FAKE-key-','0123456789ab')`, `chr(70)||chr(65)||…` (16 chr calls), `convert_from(decode('RkFLRS1rZXktMDEyMzQ1Njc4OWFi','base64'),'utf8')`, `quote_literal('FAKE-key-0123456789ab')`, and a `$q$FAKE-key-0123456789ab$q$` dollar-quoted value (shape (a)); every row must fire ONLY `x-service-key-literal`. Add ONE derived row under `long-literal-in-headers` for W4: jobname `fixture_nonanchored_split_job`, `SELECT net.http_post(url := 'https://x.invalid/a', headers := jsonb_build_object('X-Auth', 'FAKE-key-0123456789' || 'abcdef0123456789'), body := '{}'::jsonb)` (19 + 16 = 35 ≥ 32; each piece alone is under 32; the name is not one of the three anchored ones) — must fire ONLY `long-literal-in-headers`. In the RED scenario replace `red.data.length === 10` with TWO legs: a `judged` counter incremented inside the loop must equal `red.data.length` (row axis, PATTERNS §3), and `red.data.length >= CRON_DRIFT_MOD.HYGIENE_RULE_IDS.length` (never a literal); reword the title and the "all ten" prose count-free. `hygiene-green.json`: add `fixture_do_language_job` (`DO LANGUAGE plpgsql $$ … FROM vault.decrypted_secrets … jsonb_build_object('X-Service-Key', k) … $$`) so A1's prefix form is calibrated green, and `fixture_short_split_headers_job` (`jsonb_build_object('Content-Type', 'application/' || 'json', 'X-Trace', 'FAKE-' || '01')`) so a short derived value under a non-anchored name is calibrated GREEN — the residual is stated by a fixture, not only by prose. New scenario "DERIVED LENGTH is calibrated: 'Bearer ' || (SELECT …) sums below HEADER_LITERAL_MIN while a split key sums above it" — call `hygieneViolations` directly on the green concat shape (expect none of the three header ids AND not `long-literal-in-headers`) and on the split row (expect `x-service-key-literal`); and assert `codeSpans` on the green `match_engine_cron` command yields 2 spans whose second `masked` contains `vault.decrypted_secrets` (the CORRECTION's measurement, now a permanent control). **New scenario (W5) "the COMMITTED cron manifest passes every hygiene rule — zero violations on all rows (the false-positive budget)":** load `scripts/prod-prober/cron-manifest.json` (S2: missing = FAIL), for every row with a string `command` call `hygieneViolations(row.jobname, row.command)`, assert the flagged list is empty and that at least 10 rows were judged (a manifest with fewer rows than that is not the oracle this scenario means); the comment names the one-character margin from RESEARCH Q4 (`reconcile_dropped_enqueue_sweep:` is exactly 32 characters; `retention_notification_dispatches:` is 34 and clean only for lack of a digit) so whoever renames a job knows what they are touching. If this scenario is RED at execution time, the rule is wrong — narrow the rule, never edit the manifest. D5 bump from the tail.
  </action>
  <verify>
    <automated>test -z "$(node -e 'import("./scripts/sql-body-normalize.mjs")' 2>&amp;1)" &amp;&amp; node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; grep -q 'false-positive budget' scripts/prod-prober/run.mjs &amp;&amp; grep -q 'from "../../sql-body-normalize.mjs"' scripts/prod-prober/arms/cron-drift.mjs &amp;&amp; grep -q '^export function codeSpans' scripts/prod-prober/arms/cron-drift.mjs &amp;&amp; node --input-type=module -e 'import {hygieneViolations,codeSpans} from "./scripts/prod-prober/arms/cron-drift.mjs"; import {readFileSync} from "node:fs"; const ids=(j,c)=>hygieneViolations(j,c).map(x=>x.slice(1,x.indexOf("]"))); const g=JSON.parse(readFileSync("scripts/prod-prober/fixtures/cron-drift/hygiene-green.json","utf8")); const mec=g.find(x=>x.jobname==="match_engine_cron").command; const spans=codeSpans(mec); const split="SELECT net.http_post(url := \x27https://x.invalid/a\x27, headers := jsonb_build_object(\x27X-Service-Key\x27, \x27FAKE-key-\x27 || \x270123456789ab\x27), body := \x27{}\x27::jsonb)"; const nonanch="SELECT net.http_post(url := \x27https://x.invalid/a\x27, headers := jsonb_build_object(\x27X-Auth\x27, \x27FAKE-key-0123456789\x27 || \x27abcdef0123456789\x27), body := \x27{}\x27::jsonb)"; const m=JSON.parse(readFileSync("scripts/prod-prober/cron-manifest.json","utf8")); const fp=m.jobs.filter(r=>typeof r.command==="string"&amp;&amp;hygieneViolations(r.jobname,r.command).length>0).map(r=>r.jobname); const ok=spans.length===2&amp;&amp;spans[1].masked.includes("vault.decrypted_secrets")&amp;&amp;ids("j",split).includes("x-service-key-literal")&amp;&amp;JSON.stringify(ids("j",nonanch))===JSON.stringify(["long-literal-in-headers"])&amp;&amp;fp.length===0&amp;&amp;g.every(x=>hygieneViolations(x.jobname,x.command).length===0); if(!ok)console.error({spans:spans.length,split:ids("j",split),nonanch:ids("j",nonanch),fp}); process.exit(ok?0:1)' &amp;&amp; test "$(grep -c 'function matchingParen\|function singleQuotedLiterals' scripts/prod-prober/arms/cron-drift.mjs)" = 0</automated>
  </verify>
  <done>The `||`-split key fires `x-service-key-literal` (criterion 3) with `HEADER_LITERAL_MIN` untouched; the non-anchored `X-Auth` split fires exactly `long-literal-in-headers` with `HEADERS_LITERAL_MAX` untouched (W4 covered); every green row including the DO-body flagship and the short-split calibration is clean; all 14 committed commands are clean under every rule and the scenario is counted; `codeSpans` exposes the Vault read on the second span; the two hand-rolled walkers are gone. Failing direction: replacing the derived sum with the first-literal length makes the inline check print `split` without the id and exit 1; restricting the per-argument sum to the first literal makes `nonanch` come back empty and exit 1; removing the `DO_PREFIX` recursion makes `spans.length === 1` and exits 1; a rule that fires on a real command prints its jobname under `fp` and exits 1.</done>
</task>
<!-- planner-discipline-allow: function matchingParen -->
<!-- planner-discipline-allow: function singleQuotedLiterals -->

<task type="auto">
  <name>Task 2: The apostrophe pair, IN-04 pg-password forms, hygiene-bypass.json (a)–(g) + (j), the name-anchor calibration, header-unparseable → measure-fail classification, and a gitleaks verify that can fail</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs, scripts/prod-prober/fixtures/cron-drift/hygiene-red.json, scripts/prod-prober/fixtures/cron-drift/hygiene-bypass.json, src/__tests__/prod-prober-wiring.test.ts</files>
  <read_first>
    - 164.7-REVIEW.md IN-04 — `/password\s*:?=/i` plus a libpq-URI arm `postgres(?:ql)?:\/\/[^\s:]+:[^\s@]+@`; RESEARCH Q2's `pg-password` row: the `:=` form is code (masked), the connstring/URI forms are text (raw); measured today `v_password := 'x'` MISSED, `postgres://u:pw@host/db` MISSED, `PGPASSWORD: x` DETECTED.
    - 164.8.5-RESEARCH.md Q3 "measure-fail vs violation for case 1" — why `header-unparseable` is a violation (refuses capture) that `compareManifest`/`run()` classify as `measure-fail` by prefix, and why NO new `DEFECT_KIND` is added (four pins would need editing: `DEFECT_KINDS`, `EXPECTED_DEFECT_KINDS`, `KIND_ASSERTIONS`, the coverage extractor).
    - scripts/prod-prober/arms/cron-drift.mjs section (0) and the manifest-side loop (Plan 01) — the two places that turn a violation list into defects; `captureManifest` in `run.mjs` refuses on ANY violation and needs no change.
    - 164.8.5-CONTEXT.md the nine-member table (a)–(g) and criterion 4's pair.
    - .gitleaks.toml allowlist block for `scripts/prod-prober/` — regex-scoped; fixture strings must stay low-entropy `FAKE-…` with digits. `gitleaks detect` exits 1 on any finding by default and 0 on a clean scan; the verify relies on exactly that.
  </read_first>
  <action>
**(a) IN-04.** `pg-password`: on each span's `masked` test `/password\s*:=/i` (the plpgsql assignment — the operator is code); on each span's raw `sql` keep the existing `/password\s*:?=/i` text arm (it already covers `PGPASSWORD=` / `password=` / `PGPASSWORD:`) and add the URI arm. Three red rows in `hygiene-red.json` under `pg-password`: `DO $$ DECLARE v_password text; BEGIN v_password := 'FAKEpw'; END $$` (jobname `fixture_plpgsql_pw_job`), `SELECT dblink_connect('postgres://u:FAKEpw@db.invalid/db')`, and `SELECT set_config('x', 'PGPASSWORD: FAKEpw', false)`. Each must fire ONLY `pg-password`.

**(b) Classification.** In section (0) and the manifest loop, split each job's violation list: those starting with `[header-unparseable]` become `{ kind: "measure-fail", subject: "prod:<job>" | "manifest:<job>", detail: <the sentence + "the command could not be judged, and an unjudged command is not a clean one"> }`; the rest stay `cron-secret-in-command` as today. The `prodHygiene`/`manifestHygiene` maps still record the FULL list so the withhold logic keeps working. Scenario "an UNPARSEABLE header region is a measure-fail on the PROD row, never a credential verdict": a prod fixture built in memory from `prod-ok.json` with the `match_engine_cron` command replaced by the unbalanced row's command, run through `driftRun` against `manifest.json`; assert exactly one `measure-fail` with subject `prod:match_engine_cron`, ZERO `cron-secret-in-command` (via `noDefectOfKind`), and that the run exits 1.

**(c) `hygiene-bypass.json`** — rows `{ shape, jobname, command, expect_any_of: [ruleIds] }` for (a) dollar-quoted value, (b) `chr()` chain, (c) `concat`, (d) `format`, (e) `convert_from(decode(…,'base64'),'utf8')`, (f) `U&'\0046\0041…'` unicode escapes, (g) `quote_literal(…) || …`, all as `X-Service-Key` header values inside `DO $$ … PERFORM net.http_post(… headers := jsonb_build_object('X-Service-Key', <shape>) …) END $$` with a leading `-- resolves from vault.decrypted_secrets` comment (CR-03's exact reproduction preamble); plus **(j) the non-anchored split** (W4): the same DO shape with `jsonb_build_object('X-Auth', 'FAKE-key-0123456789' || 'abcdef0123456789')` and `expect_any_of: ["long-literal-in-headers"]`; plus the criterion-4 pair `pair_apostrophe` / `pair_no_apostrophe` (`jsonb_build_object('X-Trace', $q$FAKE-don't-0123456789-0123456789$q$)` vs the same without the apostrophe) carrying `expect_same_verdict_as` pointing at each other. Values are `FAKE-…` with digits, ≥ 16 chars, never a real key. Scenario "hygiene BYPASS: every enumerated shape fires at least one NAMED rule, and the apostrophe pair agrees": for each row assert `ids.length >= 1` and `ids.some(id => row.expect_any_of.includes(id))`, never that the command is quoted; for the pair assert `JSON.stringify(idsA) === JSON.stringify(idsB)` AND `idsA.includes("long-literal-in-headers")` (a pair that agrees on EMPTY proves nothing). Bypass rows legitimately fire several rules, which is why they are a separate file from `hygiene-red` (one-rule isolation).

**(d) gitleaks — a check that can FAIL.** Run `gitleaks detect --no-git -c .gitleaks.toml --source scripts/prod-prober/fixtures/cron-drift` locally (8.30.1 = CI's pin). A finding exits 1 and MUST fail the task: change the fixture STRING, never `.gitleaks.toml` (Q3 decision, outside the fence). The verify's shell is an `if command -v gitleaks; then gitleaks detect …; else echo SKIPPED …; fi` — no `--exit-code`, no `>/dev/null` swallow, no `||` fallback — so the finding's exit 1 propagates through the `&&` chain. Absence of the binary is a LOUD `SKIPPED` line on stderr naming CI (8.30.1) as the first run; it is never a silent pass.

**(e) Wiring-test calibrations + D5.** (1) Bypass file: parse it, assert every row has non-empty `expect_any_of` whose ids all exist in `HYGIENE_RULE_IDS` — and that a mutated copy with one id misspelled fails that check. (2) **Name anchor confirmed on masked** (moved here from the matrix so Plan 04 only neuters): a command whose `'X-Service-Key',` appears ONLY inside a `--` comment, followed by a long `FAKE-…` literal in a non-header position, must NOT fire `x-service-key-literal`; calibration: the same command with the comment marker removed (so the anchor is code) MUST fire it — assert the mutated command differs from the original. D5 bump from the tail, four literals.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; grep -q 'hygiene BYPASS' scripts/prod-prober/run.mjs &amp;&amp; grep -q 'UNPARSEABLE header region' scripts/prod-prober/run.mjs &amp;&amp; node --input-type=module -e 'import {hygieneViolations as h} from "./scripts/prod-prober/arms/cron-drift.mjs"; const ids=(c)=>h("j",c).map(x=>x.slice(1,x.indexOf("]"))); const a=ids("SELECT net.http_post(headers := jsonb_build_object(\x27X-Trace\x27, $q$FAKE-don\x27t-0123456789-0123456789$q$))"); const b=ids("SELECT net.http_post(headers := jsonb_build_object(\x27X-Trace\x27, $q$FAKE-dont-0123456789-0123456789$q$))"); const pw=ids("DO $$ DECLARE v_password text; BEGIN v_password := \x27FAKEpw\x27; END $$"); const uri=ids("SELECT dblink_connect(\x27postgres://u:FAKEpw@db.invalid/db\x27)"); const anch=ids("SELECT 1 -- jsonb_build_object(\x27X-Service-Key\x27, x)\n, \x27FAKE-key-0123456789ab\x27"); const ok=JSON.stringify(a)===JSON.stringify(b)&amp;&amp;a.includes("long-literal-in-headers")&amp;&amp;pw.includes("pg-password")&amp;&amp;uri.includes("pg-password")&amp;&amp;!anch.includes("x-service-key-literal"); if(!ok)console.error({a,b,pw,uri,anch}); process.exit(ok?0:1)' &amp;&amp; if command -v gitleaks >/dev/null 2>&amp;1; then gitleaks detect --no-git -c .gitleaks.toml --source scripts/prod-prober/fixtures/cron-drift; else echo "SKIPPED: gitleaks is not installed locally — CI (gitleaks 8.30.1) is the FIRST run of this scan; record that in the SUMMARY" >&amp;2; fi &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed'</automated>
  </verify>
  <done>Criterion 4 holds (identical, non-empty verdicts for the pair); IN-04's three forms fire `pg-password`; every (a)–(g) and (j) bypass row fires a named rule; an unparseable header is a measure-fail, not a credential verdict; a comment-only header name is not an anchor; gitleaks is clean on the fixture dir. Failing direction: reverting `literalsIn` to single-quote-only splits the pair's verdicts and exits 1; removing the masked `:=` arm drops `pg-password` from `pw` and exits 1; removing the `masked[m.index] === "'"` anchor check puts `x-service-key-literal` into `anch` and exits 1; a credential-shaped fixture string makes `gitleaks detect` exit 1 and the chain stops there (a missing binary prints `SKIPPED` and is recorded, never silent).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD `cron.job` text → lexer | hostile SQL spelling (comments, dollar quotes, E''/U&'', builders, unbalanced parens) |
| arm → public Actions log | violation sentences must never echo literal contents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.5-10 | Information disclosure | inline key dodged by `\|\|`/builder splitting (T-164.7-07 b) | high | mitigate | derived-length sum with builder allowlist on the anchored ids; red rows per builder |
| T-164.8.5-11 | Information disclosure | inline key inside `$q$…$q$` / `E''` / `U&''` (T-164.7-07 c) | high | mitigate | `scanSql` literal boundaries; pair fixture; shape (a)/(f) rows |
| T-164.8.5-12 | Denial of service | pathological nesting | low | mitigate | `MAX_DOLLAR_DEPTH` throw → runner measure-fail |
| T-164.8.5-13 | Tampering | unbalanced header region silently widened to text end | medium | mitigate | `header-unparseable` rule + measure-fail classification; unparseable region excluded from every other verdict |
| T-164.8.5-14 | Information disclosure | fixture strings credential-shaped in a PUBLIC repo | medium | mitigate | `FAKE-…` low-entropy values; local gitleaks run whose finding FAILS the verify; `.gitleaks.toml` untouched |
| T-164.8.5-15 | Information disclosure | a new sentence quoting the offending text | high | mitigate | RED scenario's `includes(row.command)` check; BYPASS scenario asserts the same |
| T-164.8.5-28 | Information disclosure | `\|\|`-split value under a header name outside the three anchored ones (plan-check W4) | medium | mitigate | `long-literal-in-headers` derived per argument at `HEADERS_LITERAL_MAX`; red row + bypass (j); the 16–31 residual is recorded, not silent |
| T-164.8.5-29 | Denial of service (alert fatigue) | derived-length rules fire hourly on real PROD text | medium | mitigate | committed-manifest zero-FP scenario lands in this wave (W5); one-character margin recorded |
| T-164.8.5-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Self-test green at the new N; wiring vitest green.
- CR-03's exact reproduction command (REVIEW CR-03 "Proven") now returns a NON-EMPTY list — run it and paste the ids into the SUMMARY.
- The committed-manifest FP-budget scenario is counted and green.
- gitleaks result (clean, or `SKIPPED` with the CI note) pasted into the SUMMARY.
</verification>

<success_criteria>
Criteria 3 and 4 met with fixtures in both spellings; bypass shapes (a)–(g) and (j) each fire; IN-04 swept; `header-unparseable` loud; the 14 committed commands clean under every rule; `HEADER_LITERAL_MIN`/`HEADERS_LITERAL_MAX` unchanged; no `supabase/**` change. Plan 04 proves each control individually.
</success_criteria>

<output>
Create `.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-03-SUMMARY.md` when done, with the CR-03 reproduction output, the gitleaks result and the N.
</output>
