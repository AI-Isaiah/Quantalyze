---
phase: 164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab
plan: 07
type: execute
wave: 7
depends_on:
  - 164.8.5-06
files_modified:
  - scripts/lint-app-guc.mjs
  - scripts/prod-prober/arms/cron-drift.mjs
  - scripts/lint-app-guc-fixtures/spelling-doubled-quote.red.sql
  - scripts/lint-app-guc-fixtures/spelling-e-string.red.sql
  - scripts/lint-app-guc-fixtures/spelling-dollar-tag.red.sql
  - scripts/lint-app-guc-fixtures/spelling-unicode.red.sql
  - scripts/lint-app-guc-fixtures/spelling-block-comment.red.sql
  - scripts/lint-app-guc-fixtures/successor-not-sql.red.sql
  - scripts/lint-app-guc-fixtures/notes.txt
  - src/__tests__/lint-app-guc.test.ts
autonomous: true
requirements:
  - 164.7-APPGUC-SUCCESSOR-VACUOUS
  - APPGUC-DETECT-DOUBLEQUOTE-01
  - APPGUC-UTF16-01
  - 164.7-REVIEW-INFO-FOUR

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "DETECT_RE matches the plain form AND the five missed spellings (doubled '', E'', $tag$, U&'', and a block comment before the paren) — FIVE single-spelling red fixtures under unannotated-reader, each firing exactly one kind in the gate's OWN --self-test, so each spelling can be neutered alone and its own fixture goes RED (criterion 7 as WRITTEN); the corpus stays at EXACTLY 0 findings and the five LINEAGE_ALLOWLIST counts (1/5/4/1/1, sum 12) do not move (D4)."
    - "cron-drift.mjs carries the byte-identical DETECT_RE.source in its app-guc rule — the identity pin in lint-app-guc.test.ts stays green."
    - "successor-invalid rejects: a successor that is not a .sql file (case-insensitive), one containing a path separator (traversal is forbidden outright — a successor is a SIBLING), one that exists but is not a regular file, and for two real migration names one whose timestamp is not strictly later; plus the existing absent / still-reads arms. successor: notes.txt and successor: ../out/escaped.md each produce a successor-invalid (T-164.7-02 closure evidence). The type test and the separator test are TWO controls with two RED surfaces."
    - "--files mode prints a one-line banner that allowlist enforcement is OFF (IN-01); sqlFilesUnder follows symlinked directories and matches .SQL case-insensitively (IN-02); two lineage markers in the scan window are header-malformed (IN-03); a BOM or a NUL in the first KiB is a measureFail naming the file, never a clean count (APPGUC-UTF16-01) — BOM and NUL each with their own vitest so each can be neutered alone."
  artifacts:
    - path: "scripts/lint-app-guc.mjs"
      provides: "widened DETECT_RE; four-arm successor check; banner; symlink/case walk; multi-marker detection; encoding measureFail"
      contains: "successor-invalid"
    - path: "scripts/lint-app-guc-fixtures/spelling-doubled-quote.red.sql"
      provides: "the '' spelling alone, no header — fires ONLY unannotated-reader"
    - path: "scripts/lint-app-guc-fixtures/spelling-e-string.red.sql"
      provides: "the E'' spelling alone"
    - path: "scripts/lint-app-guc-fixtures/spelling-dollar-tag.red.sql"
      provides: "the $q$…$q$ spelling alone"
    - path: "scripts/lint-app-guc-fixtures/spelling-unicode.red.sql"
      provides: "the U&'' spelling alone"
    - path: "scripts/lint-app-guc-fixtures/spelling-block-comment.red.sql"
      provides: "the block-comment-before-paren spelling alone"
    - path: "scripts/lint-app-guc-fixtures/successor-not-sql.red.sql"
      provides: "complete header + FIXTURE_ALLOWLIST entry, successor: notes.txt — fires ONLY successor-invalid"
    - path: "src/__tests__/lint-app-guc.test.ts"
      provides: "traversal / directory / timestamp successor tests; IN-03 multi-marker test; BOM and NUL measureFail tests; DETECT_RE spelling calibrations (second pin)"
  key_links:
    - from: "scripts/lint-app-guc.mjs DETECT_RE"
      to: "scripts/prod-prober/arms/cron-drift.mjs app-guc rule"
      via: "byte-identical regex source, pinned by lint-app-guc.test.ts 'shares ONE definition'"
      pattern: "current_setting"
    - from: "FINDING_KINDS[unannotated-reader].redFixtures"
      to: "spelling-*.red.sql (five files)"
      via: "self-test requires every *.red.sql to be declared by a kind and to fire exactly one kind; five declarations, one per spelling"
      pattern: "spelling-dollar-tag.red.sql"
    - from: "FIXTURE_ALLOWLIST"
      to: "successor-not-sql.red.sql header (occurrences + successor)"
      via: "both fields agree so the ONLY defect left is the successor kind"
      pattern: "successor-not-sql.red.sql"
---

<objective>
Make `lint-app-guc`'s two vacuous controls real and sweep the linter-side Info findings: widen
`DETECT_RE` to the four unbooked spellings plus the booked doubled-quote form (moving the
byte-identical copy in `cron-drift.mjs` in the same commit) with ONE red fixture PER SPELLING in
the gate's own `--self-test`, give the successor check four arms that an arbitrary readable file
cannot satisfy, and close IN-01/02/03 and the UTF-16 blindness — each with a red fixture in the
gate's own `--self-test` or a vitest calibration, and with the corpus provably still at
0 findings / 12 annotated sites.

Purpose: criterion 7 as written ("each with a red fixture in the gate's own `--self-test`"),
criterion 8 (IN-01..03; IN-04 landed in Plan 03), T-164.7-02 closure,
`[164.7-APPGUC-SUCCESSOR-VACUOUS]`, `[APPGUC-DETECT-DOUBLEQUOTE-01]`, `[APPGUC-UTF16-01]`.

Output: the linter changes, six red fixtures (+ one `.txt` helper the successor fixture names),
the wiring of all into `FINDING_KINDS`/`FIXTURE_ALLOWLIST`, vitest coverage for the arms a
fixture cannot isolate, and the one-line `cron-drift.mjs` regex identity.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-CONTEXT.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-RESEARCH.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-PATTERNS.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-REVIEW.md
@scripts/lint-app-guc.mjs
@scripts/lint-app-guc-fixtures/successor-invalid.red.sql
@scripts/lint-app-guc-fixtures/lineage.green.sql
@src/__tests__/lint-app-guc.test.ts
</context>

<decisions_honoured>
- D4 (LOCKED): the corpus is 0 findings BY DESIGN. Do NOT widen the allowlist or relax `DETECT_RE` to clear a red corpus step; RESEARCH Q6 measured the proposed regex at 12→12 with zero per-file diffs, so ANY movement of the five counts after this plan is evidence the regex is wrong — fix the regex, never the counts.
- D6 (LOCKED): `lint-app-guc.test.ts` pins `DETECT_RE.source` as a SUBSTRING of `cron-drift.mjs`; the two files move in the same commit.
- Sequencing (RESEARCH Q7): this plan is independent of the prober plans except for that one regex line; it runs LAST, serially, so no two plans edit `cron-drift.mjs` in overlapping worktrees.
- D3: each new control individually neuter-proved (Task 3) — one spelling per fixture so one spelling per neuter row; the successor type test and separator test are separate rows; BOM and NUL are separate rows.
- Scope fence: no `supabase/**` change; the five annotated migrations are READ by the corpus scan only.
</decisions_honoured>

<revision_notes>
Plan-check 2026-09-10 findings addressed in this file:
- B2 FIXED: criterion 7 is delivered as written. The single five-spelling fixture is replaced by FIVE single-spelling red fixtures under `FINDING_KINDS[unannotated-reader].redFixtures` (the gate's fixture idiom already supports several fixtures per kind; this plan declares two for `successor-invalid`). Task 3 rows 1–5 are `--self-test` rows; the vitest spelling calibrations remain as the SECOND pin. The former "record this reduction in the SUMMARY" note is gone because there is no reduction left.
- B4 FIXED: the `[EU]&?` row (one edit disabling two spellings) is two rows — `[EU]` → `[E]` expects the U&'' fixture RED; `[EU]` → `[U]` expects the E'' fixture RED.
- W10 FIXED: the planner's deliberation in the matrix ("--self-test? No — …") is removed; B2 made it moot.
- Two further batch-shaped rows found on the D3 re-read the checker asked for, both split: successor arm (1) was ONE row deleting BOTH the `.sql` type test and the separator test (two controls, two RED surfaces: the `notes.txt` fixture and the traversal vitest); the UTF-16 row dropped BOTH the BOM test and the NUL test under one vitest. Task 2(g) now adds a separate NUL vitest so each has its own RED.
</revision_notes>

<tasks>

<task type="tracer">
  <name>Task 1: Widen DETECT_RE in both files with FIVE single-spelling red fixtures, and prove the corpus stays at 0 / 12 — one path end to end</name>
  <files>scripts/lint-app-guc.mjs, scripts/prod-prober/arms/cron-drift.mjs, scripts/lint-app-guc-fixtures/spelling-doubled-quote.red.sql, scripts/lint-app-guc-fixtures/spelling-e-string.red.sql, scripts/lint-app-guc-fixtures/spelling-dollar-tag.red.sql, scripts/lint-app-guc-fixtures/spelling-unicode.red.sql, scripts/lint-app-guc-fixtures/spelling-block-comment.red.sql, src/__tests__/lint-app-guc.test.ts</files>
  <reversibility rating="reversible">A regex literal in two files under an identity pin; reverting is two one-line edits.</reversibility>
  <read_first>
    - 164.7-REVIEW.md WR-07 — the eight-row spelling table and the proposed regex; the ⛔ note that a tightening MAY surface new sites and the answer is never to relax the regex.
    - 164.8.5-RESEARCH.md Q6 — `[MEASURED]`: the proposed regex detects all five missed spellings; corpus `current total 12 proposed total 12 diffs []` across 292 files; `countReads` rebuilds the regex with `gi` from `.source` so the alternation is safe; the red-fixture rule (every `*.red.sql` declared by a kind, fires exactly one kind) → add each fixture to `unannotated-reader`'s `redFixtures`, no new kind id, so `EXPECTED_KIND_IDS` is untouched.
    - scripts/lint-app-guc.mjs `FINDING_KINDS` — `redFixtures` is an ARRAY per kind (the `successor-invalid` kind will carry two entries after Task 2); five entries for `unannotated-reader` are the same idiom.
    - scripts/lint-app-guc.mjs the file header's `v_guc_needle` blind-spot note and `cron-drift.mjs`'s `app-guc` rule sentence — both are prose ADJACENT to the detector's shape; after widening, confirm neither becomes a site (run the corpus scan and the prober self-test).
    - 164.8.5-PATTERNS.md §4 — "every widening is a new way for the gate's OWN prose to become a site"; the header self-reference trap in `lineage.green.sql`'s comment; the identity pin at `lint-app-guc.test.ts` "shares ONE definition".
    - src/__tests__/lint-app-guc.test.ts the census `it()` — `EXPECTED_CENSUS`, the per-file `countReads` re-measure, and the sum-to-12 pin.
  </read_first>
  <action>
**(a) Regex.** Replace `DETECT_RE` in `scripts/lint-app-guc.mjs` with WR-07's proposed source: `current_setting\s*(?:\/\*[\s\S]*?\*\/\s*)?\(\s*(?:[EU]&?)?'{1,2}app\.|current_setting\s*\(\s*\$[A-Za-z_]*\$app\.` with flag `i`. Update the docstring's spelling table to list all six detected forms and to keep the concatenation blind spot as the one that remains. In `scripts/prod-prober/arms/cron-drift.mjs`'s `app-guc` rule, replace the regex with the byte-identical source (same escapes, same flag) — the identity test compares `DETECT_RE.source` as a substring, so copy the SOURCE string exactly. Do not touch any other line of `cron-drift.mjs`.

**(b) Five fixtures, ONE spelling each (B2)** — each with NO lineage header, a rationale comment in prose that does NOT spell the call (the header self-reference trap), exactly ONE statement, and the trailing `-- MUST fire exactly one finding kind: unannotated-reader.` line:
  - `spelling-doubled-quote.red.sql`: `current_setting(''app.x'')` inside an outer literal context (the booked `[APPGUC-DETECT-DOUBLEQUOTE-01]` form);
  - `spelling-e-string.red.sql`: `current_setting(E'app.x')`;
  - `spelling-dollar-tag.red.sql`: `current_setting($q$app.x$q$)`;
  - `spelling-unicode.red.sql`: `current_setting(U&'app.x')`;
  - `spelling-block-comment.red.sql`: `current_setting/*c*/('app.x')`.
  Declare all five in `FINDING_KINDS[unannotated-reader].redFixtures` with a comment that one fixture per spelling is what lets Task 3 neuter ONE alternation and watch ONE fixture stop firing — a combined fixture would still fire on the other four and prove nothing about the neutered one.

**(c) Prove the corpus did not move.** Run `node scripts/lint-app-guc.mjs --self-test` then `node scripts/lint-app-guc.mjs` and confirm `findings 0` and `annotated (lineage) 5`. Run `npx vitest run src/__tests__/lint-app-guc.test.ts` — the census arm re-measures each allowlist count with the NEW regex and pins the sum to 12; if it reds, the regex matched something new: open the named file, and if the hit is real prose (a header or comment spelling the call), the fix is in THAT prose or the regex, never in the count. Add to the vitest a `CALIBRATION` block (the SECOND pin, beside the self-test fixtures): `DETECT_RE` matches each of the six spellings and does NOT match `current_setting('quantalyze.x')` nor `current_setting(v_guc_needle)`; a mutant regex with `[EU]` narrowed to `[E]` must FAIL to match the `U&'` form and a mutant narrowed to `[U]` must FAIL to match the `E'` form (each mutant must differ from the original).
  </action>
  <verify>
    <automated>node scripts/lint-app-guc.mjs --self-test 2>&amp;1 | grep -q 'self-test OK' &amp;&amp; node scripts/lint-app-guc.mjs 2>&amp;1 | grep -q 'findings 0' &amp;&amp; node scripts/lint-app-guc.mjs 2>&amp;1 | grep -q 'annotated (lineage) 5' &amp;&amp; test "$(ls scripts/lint-app-guc-fixtures/spelling-*.red.sql | wc -l | tr -d ' ')" = 5 &amp;&amp; node --input-type=module -e 'import {DETECT_RE} from "./scripts/lint-app-guc.mjs"; const f=["current_setting(\x27\x27app.x\x27\x27)","current_setting(E\x27app.x\x27)","current_setting($q$app.x$q$)","current_setting(U&amp;\x27app.x\x27)","current_setting/*c*/(\x27app.x\x27)","current_setting(\x27app.x\x27)"]; process.exit(f.every(s=>DETECT_RE.test(s))&amp;&amp;!DETECT_RE.test("current_setting(\x27quantalyze.x\x27)")?0:1)' &amp;&amp; node -e 'const fs=require("fs");const src=fs.readFileSync("scripts/prod-prober/arms/cron-drift.mjs","utf8");import("./scripts/lint-app-guc.mjs").then(m=>process.exit(src.includes(m.DETECT_RE.source)?0:1))' &amp;&amp; node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; npx vitest run src/__tests__/lint-app-guc.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed'</automated>
  </verify>
  <done>All six spellings detected; five single-spelling fixtures exist and each fires exactly `unannotated-reader` in `--self-test`; corpus `findings 0`, `annotated 5`, census sum 12 unchanged; identity pin green; prober self-test green. Failing direction: dropping the second alternation makes the `$q$` form miss — the inline check exits 1 AND `--self-test` reds naming `spelling-dollar-tag.red.sql`; editing only one of the two files reds the identity pin.</done>
</task>

<task type="auto">
  <name>Task 2: Four-arm successor check (WR-06 / T-164.7-02) + IN-01 banner + IN-02 walk + IN-03 multi-marker + UTF-16 BOM and NUL measureFail (separate vitests)</name>
  <files>scripts/lint-app-guc.mjs, scripts/lint-app-guc-fixtures/successor-not-sql.red.sql, scripts/lint-app-guc-fixtures/notes.txt, src/__tests__/lint-app-guc.test.ts</files>
  <reversibility rating="reversible">Static-gate logic and fixtures inside one script; no contract outside `FINDING_KINDS` changes.</reversibility>
  <read_first>
    - 164.7-REVIEW.md WR-06 (`successor: notes.txt` and `successor: ../out/escaped.md` both pass today), IN-01, IN-02, IN-03; 164.7-SECURITY.md T-164.7-02 "what would have to be true" — closure evidence is both WR-06 strings producing `successor-invalid`.
    - 164.8.5-RESEARCH.md Q6 "What a correct successor check asserts" — the four arms in order, the sibling rule (no separator → simpler than `startsWith(corpusRoot)` and closes `../` and absolute paths in one test), the conditional timestamp arm so the fixture pair keeps working, the `FIXTURE_ALLOWLIST` entry requirement, and IN-03's vitest-only coverage (`selfTestFixture: false`).
    - scripts/lint-app-guc.mjs `scanFile` successor block, `sqlFilesUnder`, `parseLineageHeader`, `main` `--files` branch, the `readFileSync(absPath, "utf8")` read and its catch → `measureFail` shape (§2b: "could not measure must never share a code path with measured zero").
    - scripts/lint-app-guc-fixtures/successor-invalid.red.sql + its `FIXTURE_ALLOWLIST` entry — the recipe (header + allowlist agree; the only defect left is the successor).
    - src/__tests__/lint-app-guc.test.ts the temp-corpus `scanCorpus` tests and the `parseLineageHeader` bad-payload tests — the shapes to extend.
  </read_first>
  <action>
**(a) Successor arms, in order, each its own `successor-invalid` message, inside the existing `header.successor !== "none"` block:** (1a) `!/\.sql$/i.test(successor)` → "not a .sql file — a successor is a migration; any other file type trivially contains zero app-GUC reads and would satisfy this check while proving nothing"; (1b) `/[\\/]/.test(successor)` → "not a SIBLING — a successor sits beside this migration; a path with a separator (traversal, absolute) is refused outright". Two predicates, two messages, two lines: Task 3 neuters each alone. (2) resolve with `resolve(dirname(absPath), successor)`, require `existsSync` AND `statSync(...).isFile()` (a directory named `x.sql` must not pass — T-164.7-04's shape); (3) when BOTH basenames match `^\d{14}_`, require the successor's 14-digit prefix to be strictly greater — "a lineage cannot point backwards"; (4) the existing zero-`DETECT_RE`-reads arm. Arms (1a)/(1b) short-circuit (2)–(4).

**(b) Fixtures.** `successor-not-sql.red.sql`: a complete header (`retired 2026-09-10; occurrences: 1; successor: notes.txt; reason: …`), one executable read, ending `-- MUST fire exactly one finding kind: successor-invalid.`; `notes.txt` beside it (a plain-text file WITH a line that would be a read if it were SQL — so arm (1a) is proven to reject by TYPE, not by content); a `FIXTURE_ALLOWLIST` entry with `occurrences: 1, successor: "notes.txt"` and a reason saying the paperwork is complete so the only defect is the type. Add the fixture to `FINDING_KINDS[successor-invalid].redFixtures` (declare BOTH names: the existing `successor-invalid.red.sql` and the new one).

**(c) Vitest for the arms a fixture cannot isolate** (temp corpus via `scanPaths`/`scanCorpus` with a temp allowlist, as the existing tests do): traversal `../out/escaped.md` → `successor-invalid` whose message names SIBLING (arm 1b — note this string ALSO fails arm 1a on type; the traversal test must use a `.sql` name, `../out/escaped.sql`, so that ONLY the separator arm can reject it, otherwise the row cannot be attributed; keep `../out/escaped.md` as a second assertion for the T-164.7-02 closure evidence); an existing DIRECTORY named `later.sql` → `successor-invalid`; `20260907130000_x.sql` naming successor `20260907120000_y.sql` (earlier) → `successor-invalid`; the same pair the right way round → no finding. Each with a calibration that flips (the mutated corpus differs from the original).

**(d) IN-01.** In `main`'s `--files` branch print ONE line before scanning: `app-guc: --files mode — allowlist enforcement OFF; a lineage header alone exempts a file here.` Vitest: capture `console.log` around `main(["--files", <green fixture>])` and assert the banner; calibration: `main([])` (corpus mode) does NOT print it.

**(e) IN-02.** In `sqlFilesUnder`: `ent.name.toLowerCase().endsWith(".sql")` and `else if (ent.isDirectory() || (ent.isSymbolicLink() && statSync(p).isDirectory())) walk(p)`; keep the "`.sql` test FIRST" comment and T-164.7-04 rationale. Vitest on a temp tree: a `.SQL` file is scanned; a symlinked directory's file is scanned (skip the symlink assertion with a named reason only if the platform cannot create one — never silently).

**(f) IN-03.** `parseLineageHeader`: collect every marker line within `HEADER_SCAN_LINES`; if more than one, return `{ line: <first>, malformed: "<n> lineage markers in the scan window; a file may carry exactly one" }`. Vitest: two markers → `malformed` names the count; one marker → unchanged parse.

**(g) UTF-16 / NUL — two predicates, two tests.** In `scanFile`, after the read, inspect the first 1024 bytes (read as a Buffer first, then decode): predicate BOM — a leading `FE FF`, `FF FE` or `EF BB BF`; predicate NUL — any `0x00` byte. Either returns the existing `measureFail` shape with reason `cannot measure <file>: <BOM|NUL> in the first KiB — an undecodable migration is not a clean one`, naming WHICH predicate fired. Vitest on temp files: (1) a UTF-16LE-encoded file (BOM `FF FE`) containing a read → `measureFails.length === 1` naming the file and `BOM`, `findings` empty; (2) a UTF-8 file with a single `0x00` byte in the first KiB and NO BOM → `measureFails.length === 1` naming the file and `NUL`, `findings` empty; (3) calibration: the same content as clean UTF-8 → one `unannotated-reader` and no measureFail. Test (2) exists so Task 3 can drop the NUL predicate alone and see exactly one test red.
  </action>
  <verify>
    <automated>node scripts/lint-app-guc.mjs --self-test 2>&amp;1 | grep -q 'self-test OK' &amp;&amp; node scripts/lint-app-guc.mjs 2>&amp;1 | grep -q 'findings 0' &amp;&amp; grep -q 'allowlist enforcement OFF' scripts/lint-app-guc.mjs &amp;&amp; test -f scripts/lint-app-guc-fixtures/successor-not-sql.red.sql &amp;&amp; test -f scripts/lint-app-guc-fixtures/notes.txt &amp;&amp; node --input-type=module -e 'import {scanPaths,FIXTURE_ALLOWLIST,relPath} from "./scripts/lint-app-guc.mjs"; import {resolve} from "node:path"; const p=resolve("scripts/lint-app-guc-fixtures/successor-not-sql.red.sql"); const r=scanPaths([p],{allowlist:FIXTURE_ALLOWLIST.filter(e=>e.file===relPath(p))}); const kinds=[...new Set(r.findings.map(f=>f.kind))]; process.exit(kinds.length===1&amp;&amp;kinds[0]==="successor-invalid"&amp;&amp;r.measureFails.length===0?0:1)' &amp;&amp; npx vitest run src/__tests__/lint-app-guc.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed'</automated>
  </verify>
  <done>`successor: notes.txt` fires exactly `successor-invalid` through the self-test fixture; separator, directory and backwards-timestamp arms are vitest-proven with calibrations; the banner prints only in `--files` mode; `.SQL` and symlinked dirs are walked; two markers are malformed; a BOM file and a NUL file are each a measureFail naming the predicate. Corpus still `findings 0`. Failing direction: removing arm (1a) lets `notes.txt` through and the inline check exits 1 (`kinds` empty); removing arm (1b) reds the `../out/escaped.sql` vitest only; removing the BOM predicate reds the UTF-16 vitest only; removing the NUL predicate reds the NUL vitest only.</done>
</task>

<task type="auto">
  <name>Task 3: Individual neuter matrix for the linter controls, and the phase-level closing checks</name>
  <files>scripts/lint-app-guc.mjs</files>
  <read_first>
    - 164.8.5-01-SUMMARY.md — the harness; adapt the run command to `node scripts/lint-app-guc.mjs --self-test` or `npx vitest run src/__tests__/lint-app-guc.test.ts` per row.
    - .planning/ROADMAP.md Phase 164.8.5 — the nine success criteria; this task records where each is proven.
  </read_first>
  <action>
One control per run, byte backup, `cmp` restore. Every row disables exactly ONE predicate:
| control | neuter (ONE edit) | expected RED |
| DETECT_RE doubled-quote | `'{1,2}` → `'` | `--self-test` naming `spelling-doubled-quote.red.sql` (fires nothing) |
| DETECT_RE E-string | `[EU]` → `[U]` | `--self-test` naming `spelling-e-string.red.sql` |
| DETECT_RE unicode | `[EU]` → `[E]` | `--self-test` naming `spelling-unicode.red.sql` |
| DETECT_RE dollar | remove the second alternation | `--self-test` naming `spelling-dollar-tag.red.sql` |
| DETECT_RE comment | remove `(?:\/\*[\s\S]*?\*\/\s*)?` | `--self-test` naming `spelling-block-comment.red.sql` |
| identity pin | edit only `lint-app-guc.mjs`'s regex by one character | `shares ONE definition` vitest |
| successor arm (1a) type | delete the `.sql` type test | `--self-test` (`successor-not-sql.red.sql` fires nothing) |
| successor arm (1b) separator | delete the separator test | vitest `../out/escaped.sql` traversal test |
| successor arm (2) isFile | drop `isFile()` | vitest directory test |
| successor arm (3) timestamp | drop the comparison | vitest backwards-timestamp test |
| IN-01 banner | delete the log line | vitest banner test |
| IN-02 case | restore `endsWith(".sql")` | vitest `.SQL` test |
| IN-02 symlink | drop the symlink branch | vitest symlink test |
| IN-03 | return on first marker | vitest two-marker test |
| UTF-16 BOM | drop the BOM predicate | vitest UTF-16 (BOM) test |
| NUL | drop the NUL predicate | vitest NUL test |

For each `--self-test` row the harness ALSO records that the other four spelling fixtures still fire (they appear in the self-test's per-fixture output as firing) — that is the attribution that makes the row individual.

Closing checks for the phase (the nine criteria, each with the scenario/test title that proves it, pasted into the SUMMARY): 1 → Plan 01 HOIST scenarios; 2 → Plan 05 TRI-STATE + REAL snapshot; 3 → Plan 03 DERIVED LENGTH + split red rows; 4 → Plan 03 apostrophe pair; 5 → Plan 01/02 HOIST states; 6 → Plan 01 MALFORMED scenario; 7 → this plan's Task 1 (five self-test fixtures) and Task 2 (successor fixture + vitests); 8 → IN-01..03 here, IN-04 Plan 03; 9 → the final `--self-test` tail and the wiring test's four literals. Then: `node scripts/prod-prober/run.mjs --self-test`; `node scripts/lint-app-guc.mjs --self-test && node scripts/lint-app-guc.mjs`; `node scripts/lint-app-guc.mjs --self-test | head -1` pasted (do not restate the kind/fixture counts from memory); `npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/lint-app-guc.test.ts src/__tests__/sql-body-normalize.test.ts src/__tests__/gitleaks-allowlist.test.ts`; `node scripts/mutation-runner/run.mjs` exits 0 with tallies unchanged from CLAUDE.md's dated reading (this phase adds no SQL gate file — record the printed `arms:` line as evidence the mutation lane is untouched); `git status --short supabase` empty. ⚠️ Do NOT ship: Phase 164.7's gate stays blocked on `threats_open: 3` until 164.8.6 closes T-164.7-08; this phase ends when the nine criteria are met.
  </action>
  <verify>
    <automated>node scripts/lint-app-guc.mjs --self-test 2>&amp;1 | grep -q 'self-test OK' &amp;&amp; node scripts/lint-app-guc.mjs 2>&amp;1 | grep -q 'findings 0' &amp;&amp; node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/lint-app-guc.test.ts src/__tests__/sql-body-normalize.test.ts src/__tests__/gitleaks-allowlist.test.ts 2>&amp;1 | grep -q 'Test Files  4 passed' &amp;&amp; test "$(git status --short -- supabase | wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <done>Sixteen individual neuters recorded with their RED surface (self-test or vitest) and byte-identical restores; for the five spelling rows the other four fixtures are recorded as still firing; the criteria-to-proof table is in the SUMMARY; all four vitest files, both self-tests and the corpus scan are green; the mutation lane's printed tallies are unchanged; nothing under `supabase/` moved. Failing direction: any green neuter row blocks; a moved allowlist count is a regex defect, not a number to update.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file text → linter | a new reader can be spelled to dodge the regex; a header's `successor` is free text |
| filesystem → linter | symlinks, case, encodings, directories named `.sql` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.5-22 | Tampering | app-GUC read spelled `E''`/`$q$`/`U&''`/`''`/comment-before-paren dodges the gate | medium | mitigate | widened `DETECT_RE`, FIVE single-spelling self-test fixtures, vitest per-spelling calibrations as the second pin, identity pin |
| T-164.8.5-23 | Tampering | `successor-invalid` satisfied by any readable file (T-164.7-02) | medium | mitigate | four-arm successor check (type and separator as separate predicates); `notes.txt` fixture; traversal/dir/timestamp vitests |
| T-164.8.5-24 | Repudiation | UTF-16/NUL file scans clean | medium | mitigate | BOM predicate and NUL predicate → measureFail by name, each with its own vitest |
| T-164.8.5-25 | Elevation of privilege | `--files` mode silently disarms the allowlist | low | mitigate | banner (IN-01); CI never uses `--files` |
| T-164.8.5-26 | Tampering | second lineage marker contradicting the first | low | mitigate | multi-marker → header-malformed |
| T-164.8.5-27 | Denial of service (false positive) | widened regex fires on the gate's own prose | medium | mitigate | corpus 0 / census 12 asserted; prose rewritten rather than counts |
| T-164.8.5-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- `node scripts/lint-app-guc.mjs --self-test` OK; bare run `findings 0`, `annotated 5`; vitest census sum 12.
- Identity pin green; prober self-test green.
- Task 3 matrix (sixteen rows) and the nine-criteria proof table in the SUMMARY.
</verification>

<success_criteria>
Criteria 7 (as written — each spelling with its own `--self-test` red fixture) and 8 met; T-164.7-02 closure evidence produced (both WR-06 strings → `successor-invalid`); `[APPGUC-DETECT-DOUBLEQUOTE-01]`, `[APPGUC-UTF16-01]`, `[164.7-APPGUC-SUCCESSOR-VACUOUS]` and `[164.7-REVIEW-INFO-FOUR]` complete; the corpus count did not move. With Plans 01–06 this is the full nine-criteria set — the phase ends here, unshipped by design.
</success_criteria>

<output>
Create `.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-07-SUMMARY.md` when done, with the neuter matrix, the criteria-to-proof table, the pasted self-test first line and the mutation-runner `arms:` line.
</output>
