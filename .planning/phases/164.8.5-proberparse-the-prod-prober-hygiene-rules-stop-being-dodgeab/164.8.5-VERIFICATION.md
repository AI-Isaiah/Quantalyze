---
phase: 164.8.5-proberparse
verified: 2026-09-11T13:30:00Z
status: passed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
verification_method: drive-and-neuter
head_at_start: 04f13af839122dbe414c90c96528206f89bc4238
head_at_end: 93026cd457d64e486d36c442c1d50f899b14f2df
head_moved_during_verification: true
head_move_touched_files_under_test: false
notes:
  - "Every criterion was driven: command run, control neutered, RED observed, restored by `cp` from a byte backup and proven with `cmp`."
  - "No source file was edited on disk at the end of verification: all five files under test are byte-identical to their pre-verification backups AND clean against git."
warnings:
  - id: ROADMAP-CHECKBOX-STALE
    detail: "ROADMAP.md lists plans 05, 06 and 07 as `[ ]` unchecked, though all three have completed SUMMARYs and their deliverables are verified present and falsifiable in this report. Documentation inconsistency only."
  - id: REVIEW-ARTIFACTS-UNTRACKED
    detail: "164.8.5-REVIEW.md, -R1, -R2, -R3 and .planning/STOPPING-RULE-PROPOSAL.md are untracked at verification time. Orchestrator commits."
  - id: MANIFEST-SIDE-LOOP-DEAD-IN-PROD
    detail: "Measured, not inferred: with the REAL committed manifest (declares ws-collapse-v1, arm computes v2) compareManifest returns manifest-invalid before section (2), so the MANIFEST-side hygiene scan is unreachable on every production run today. This is the founder's known-open CR-01 artifact half and is NOT part of any of the nine criteria — criteria 1 and 5 are about the PROD/live side, which was measured firing in exactly this state."
carried_known_open_confirmed:
  - "cron-manifest.json declares ws-collapse-v1 vs the arm's ws-collapse-v2 → version refusal, not a hash break. Confirmed by measurement."
  - "retention_compute_jobs_orphaned_running holds 0 newlines in the committed manifest. Confirmed by measurement (needs a PROD re-capture — founder's call)."
---

# Phase 164.8.5: PROBERPARSE Verification Report

**Phase Goal:** the prod-prober's hygiene rules stop being dodgeable and its parser stops dropping rows silently.

**Verified:** 2026-09-11 · **Status:** passed (9/9) · **Re-verification:** No — initial verification.

## How this was verified

The phase's subject matter is controls, so it was held to the standard it imposes. **No SUMMARY claim
was accepted as evidence.** For each criterion: run the command, neuter the control, grep the changed
line to confirm the edit landed, observe the RED, restore by `cp` from a byte backup and prove with
`cmp`. `git checkout --` was never used.

**Environment:** `node v25.8.1` (`/opt/homebrew/bin/node`) — no login shell was used, so the v19.2.0
trap was avoided.

**One trap hit and caught, recorded because it nearly produced a false MET.** The first attempt at
Criterion 2's reject-direction neuter edited `span.text`, a field that does not exist on a span
(the field is `span.sql`). `span.text ?? span.masked` silently fell back to `masked`, the neuter was
a no-op, and the self-test printed `PASSED: 78/78` against effectively unmodified behaviour. It was
caught by inspecting the span's real shape before drawing a conclusion, and re-run as NEUTER-B2.
**A neuter that does not apply is indistinguishable from a control that cannot fail.**

## Baseline — independently reproduced

Every figure the founder measured was reproduced before any neuter:

| Measurement | Founder's baseline | Reproduced |
|---|---|---|
| `prod-prober/run.mjs --self-test` | 78/78, exit 0 | ✓ 78/78, exit 0 |
| `lint-app-guc --self-test` | OK | ✓ OK (4 kinds, 11 red + 2 green fixtures), exit 0 |
| `lint-app-guc` corpus | 292 files, 0 findings | ✓ 292 scanned, 5 annotated, **0 findings**, exit 0 |
| `LINEAGE_ALLOWLIST` | 1/5/4/1/1 | ✓ 1/5/4/1/1, 5 entries |
| `lint-sql-gates --self-test` | 7 rules, red+green | ✓ 7 rules, red+green each |
| committed manifest | judged 14, VIOLATIONS 0 | ✓ scenario 56 green — 14 rows, zero violations |
| vitest, the two suites | 136 passed | ✓ 2 files, **136 passed** |
| `npm run lint` | 0 errors, 2 warnings | ✓ 0 errors, 2 warnings (pre-existing) |
| `check-planning-hygiene` | OK | ✓ OK — 6327 tracked files, none carry a home path |
| `git diff main...HEAD` over `supabase/`, `.gitleaks.toml`, `cron-manifest.json` | EMPTY | ✓ empty |

⚠️ **HEAD moved during verification**, from `04f13af8` to `93026cd4` (`bff2937a` docs + `93026cd4`
`chore(release): v0.77.33.0`). The two new commits touch only `CHANGELOG.md`, `TODOS.md`, `VERSION`
and `package.json` — **no file under test changed**, so every measurement below still holds at the
new HEAD. `VERSION` and `package.json` are byte-equal `0.77.33.0` (4-digit), as
`critical-regressions.test.ts` requires.

## Criterion-by-criterion verdict

### Criterion 1 — PROD hygiene above every return — **MET**

The four oracle states named by the criterion each have their own scenario: **42** (ABSENT),
**43** (STALE / schema bumped), **44** (marker MISMATCHED), **46** (HAND-EDITED / sha mismatch).

**Neuter:** relocated section (0)'s PROD hygiene loop from the top of `compareManifest` to below the
oracle-validation block — the exact pre-hoist layout the 2026-09-10 revert exists to prevent.
Confirmed landed (`grep` showed the marker at line 2326; `git diff --stat` 30 insertions / 29
deletions).

**RED observed — all four, individually named:**

```
=== SELF-TEST 42/78: HOIST: ... oracle ABSENT ===
SELF-TEST FAIL: AND the PROD credential is STILL reported — an unreadable oracle is not an off switch for the live scan
=== SELF-TEST 43/78: ... oracle STALE (schema_version bumped) ===
SELF-TEST FAIL: AND the PROD credential is STILL reported — a stale oracle is not an off switch either
=== SELF-TEST 44/78: ... live marker MISMATCHED ===
SELF-TEST FAIL: AND the PROD credential is STILL reported — the rows were really read, whatever database they came from
=== SELF-TEST 46/78: ... oracle HAND-EDITED (sha mismatch) ===
SELF-TEST FAIL: AND the PROD credential is STILL reported — a HAND-EDITED oracle is not an off switch either, which is the exact regression that reverted the first repair
```

⭐ **Scenario 41 (an ABSENT manifest is `manifest-invalid`) stayed GREEN under the same neuter.** That
is the discriminating evidence: these scenarios test the loop's **position**, not merely its
existence. A test that reddened alongside 41 would have proven much less.

⭐ **Confirmed in the REAL production state, not only in fixtures.** Driving `compareManifest` with
the actual committed `cron-manifest.json` — which is refused today as `ws-collapse-v1` — against a
leaky PROD row still yields `cron-secret-in-command`. The property holds on the configuration that
is actually deployed.

Restored; `cmp` identical.

### Criterion 2 — one `vault-absent` rule, both directions — **MET**

Both directions were neutered **separately**, because satisfying one re-breaks the other.

**Direction A (must ACCEPT `SELECT public.match_engine_cron_tick();`)** — NEUTER-A disabled the
callable resolver (`if (false && resolveCallable(...))`). Scenarios **52 and 53** went RED; 53 is the
real-snapshot 164.5.1 collision, and it failed with the full `[vault-absent]` message naming the
job — i.e. the command Phase 164.5.1 depends on would have been rejected.

**Direction B (must REJECT a comment-only / literal-only mention)** — NEUTER-B2 tested `span.sql`
(raw) instead of `span.masked`. Scenario **52** went RED on precisely the reject half:

```
SELF-TEST FAIL: and every command that merely MENTIONS one is rejected — this is what `text.includes()`
could not do (the table named inside a single-quoted literal | the table named inside a DOLLAR-quoted literal)
```

Both halves are independently falsifiable. Restored; `cmp` identical.

### Criterion 3 — `||`-split service key — **MET**

**Neuter:** replaced the derived-length **summation** with `Math.max` — attacking the summing
property itself, *not* the threshold (D4 forbids lowering it).

**RED observed, naming the fixtures:**

```
=== SELF-TEST 39/78 === FAIL: every MEASURED bypass shape now fires at least one rule ((j) ||-split value under a header name outside the three anchored ones)
=== SELF-TEST 49/78 === FAIL: every red row fires the rule it names (x-service-key-literal (fixture_split_key_job), long-literal-in-headers (fixture_nonanchored_split_job))
=== SELF-TEST 51/78 === FAIL: while '<prefix>' || '<suffix>' under the SAME threshold DOES fire — the sum is over operands, not over the first literal ([])
```

**D4 honoured, verified against `main`:** `HEADER_LITERAL_MIN = 16` and `HEADERS_LITERAL_MAX = 32` are
unchanged from `main` (only `HEADERS_LITERAL_MAX` gained an `export`). Scenario 51 is a genuine
calibration — it pins that `'Bearer ' || (SELECT …)` sums *below* the threshold while a split key sums
*above* it, so the rule was widened without being loosened.

Restored; `cmp` identical.

### Criterion 4 — dollar-quoted regions skipped wholesale — **MET**

**Neuter:** made `literalAt` stop recognising `dollarRegions` as literals. Scenarios **39 and 49**
went RED naming `fixture_dollar_quoted_key_job` and both halves of the apostrophe pair
(`criterion 4: an apostrophe INSIDE a dollar-quoted literal is not a delimiter`,
`criterion 4: the same value spelled without the apostrophe`).

**Same-verdict property measured directly** (not inferred from a scenario title) — both fixtures
ship, and at HEAD:

```
apostrophe   : ["[long-literal-in-headers]"]
no-apostrophe: ["[long-literal-in-headers]"]
SAME VERDICT : true      non-empty: true
```

The verdicts are identical **and** non-vacuous — the pair could not agree by both being empty.

### Criterion 5 — live credential reported whatever the oracle's state — **MET**

Driven directly against `compareManifest` across **seven** oracle states, rather than trusting the
three the criterion names:

| Oracle state | `cron-secret-in-command` |
|---|---|
| absent (`null`) | FIRES |
| unparseable (garbage string) | FIRES |
| unparseable (array) | FIRES |
| unparseable (number) | FIRES |
| `schema_version` bumped to 99 | FIRES |
| no `jobs` key | FIRES |
| marker mismatched | FIRES |

All seven also emit `manifest-invalid` — the refusal and the credential finding are **additive**, which
is the property that matters. Falsifiability is established by Criterion 1's neuter, under which these
same paths go silent.

### Criterion 6 — malformed record is a measure-fail, never a `continue` — **MET**

**Neuter:** deleted `malformed.push({ index, fields: f.length })`, restoring the WR-05 silent drop.
Scenarios **59, 60 and 61** went RED.

⭐ The parser carries **two independent guards** — the field-width check and an out-of-band row count
— and scenario 59 asserts *both* fire ("TWO measure-fails, one per independent guard"). Under the
neuter the width guard died and the count guard still caught it, which is defence in depth rather
than a single point of failure.

`captureManifest` has its own refusal path (scenario 61: exit 3, nothing written), satisfying D6's
"second caller needs its own refusal".

Restored; `cmp` identical.

### Criterion 7 — `lint-app-guc` successor check + `DETECT_RE` — **MET**

⚠️ **The criterion predicted "four missed spellings". I measured FIVE**, each with its own committed
single-spelling red fixture. More than promised is not a failure, but the number in the criterion is
stale — reporting what was measured:

| Spelling | Neuter applied | Fixture that went RED | self-test exit |
|---|---|---|---|
| dollar-tag `$tag$app.` | `\$[A-Za-z_]*\$app\.` → never-match | `spelling-dollar-tag.red.sql` | 1 |
| doubled-quote `''app.` | `'{1,2}` → `'{1,1}` | `spelling-doubled-quote.red.sql` | 1 |
| E-string `E'app.` | `[EU]&?` → `[U]&?` | `spelling-e-string.red.sql` | 1 |
| unicode `U&'app.` | `[EU]&?` → `[E]&?` | `spelling-unicode.red.sql` | 1 |
| block comment `current_setting/*…*/(` | comment groups → never-match | `spelling-block-comment.red.sql` | 1 |

**Each neuter reddened exactly one fixture** — the spellings are individually isolated, satisfying D3.
The self-test's true exit code was captured separately (`> file; echo $?`) rather than through a pipe,
confirming these are real gate failures and not merely printed text.

**Successor check (threat T-164.7-02).** Seven arms, enumerated in-code. Arm **1a TYPE** neutered →
`successor-not-sql.red.sql` goes RED, exit 1. This is the arm that closes T-164.7-02: `notes.txt`
exists and holds zero app-GUC reads, which is exactly why the old one-arm check passed it.

⚠️ **Nuance on arm 2a (ABSENT):** neutering it does not produce a *named finding* — it produces an
uncaught `ENOENT` from the subsequent `statSync`, exit 1. The gate still **fails closed and loud**, so
the arm cannot be removed silently, but the failure is a crash rather than a diagnostic. Not a defect
against the criterion; recorded for accuracy.

⭐ **The in-code honesty note is itself corroborating evidence.** The arm table records that the
CONTENT arm was re-measured on 2026-09-11 and *had no surface at all* — neutering it left everything
green — and that the two missing surfaces were then built. A phase that documents its own vacuous
control is behaving the way this criterion asks.

**Corpus stayed at exactly 0.** 292 files scanned, 5 annotated, **0 findings**, both at HEAD and under
every neuter. `LINEAGE_ALLOWLIST` remains 1/5/4/1/1. D4's forbidden remedies were not taken.

**DETECT_RE identity across both files:** `cron-drift.mjs` contains the byte-identical 278-character
source. Neutering the cron-drift copy alone reds the vitest pin
(`× shares ONE definition of an app-GUC read with the cron-drift prober`), so neither file can move
alone — D6's coupling is live.

### Criterion 8 — the four Info findings — **MET**

| Item | Present | Test surface | Falsifiable |
|---|---|---|---|
| IN-01 `--files` allowlist enforcement | banner at `lint-app-guc.mjs:1216` | `describe(... IN-01 ...)` | declared |
| IN-02 symlinks + `.SQL` case | `.toLowerCase().endsWith(".sql")`; symlink `statSync` → measure-fail | `describe(... IN-02 ...)` | ✓ neutered `.toLowerCase()` → `× scans a '.SQL' file` RED |
| IN-03 `parseLineageHeader` first-marker-wins | markers collected over the whole window, `>1` is header-malformed | `describe(... IN-03 ...)` | declared |
| IN-04 `pg-password` plpgsql forms | four predicates, deliberately disjoint | self-test fixtures | ✓ all four neutered individually |

⭐ **IN-04 deserves special note.** The code claims "each one can be neutered alone and exactly one red
row goes clean." That claim was tested rather than believed, and it is **true**:

| Predicate neutered | Fixture row that went clean |
|---|---|
| `/password\s*:=/i.test(span.masked)` | `fixture_plpgsql_pw_job` |
| `/PGPASSWORD/i.test(span.sql)` | `fixture_pgpassword_colon_job` |
| `/password\s*=/i.test(span.sql)` | `fixture_dblink_job` |
| libpq URI regex | `fixture_libpq_uri_job` |

Four predicates, four distinct named rows, no subsumption — the `password\s*=` vs `password\s*:=`
separation the docstring defends is real.

### Criterion 9 — `SELF_TEST_SCENARIOS` bumped in the same edit — **MET**

`SELF_TEST_SCENARIOS = 78` (`run.mjs:149`). **Neuter to 77** → **both** independent guards bit:

- Runtime completeness: `SELF-TEST FAIL: the counted scenario set is complete: 78 header(s) printed, SELF_TEST_SCENARIOS is 77`, exit 1.
- Wiring test: `× SELF_TEST_SCENARIOS is 77, and the runner PRINTS exactly 77 headers numbered 1..77`, 1 failed / 70 passed.

⭐ **D5's hazard is structurally closed, not merely satisfied.** D5 warned of *four* hand-typed
literals across two files, two of them inside the `it()` title where a miss would leave the test name
lying while still passing. The title now **interpolates** `${SELF_TEST_SCENARIOS}`, so exactly one
intentional pin remains (`expect(SELF_TEST_SCENARIOS).toBe(78)`). The failure mode D5 describes can no
longer occur.

## Scope fence

**Held.** Filtering to commits whose subject is `<type>(164.8.5…)`, the non-planning paths touched are
exclusively: `scripts/lint-app-guc.mjs`, `scripts/lint-app-guc-fixtures/**`,
`scripts/prod-prober/arms/cron-drift.mjs`, `scripts/prod-prober/run.mjs`,
`scripts/prod-prober/fixtures/cron-drift/**`, and the two test files.

**No 164.8.5 commit touched `supabase/**`, `.github/workflows/**`, `.gitleaks.toml` or
`cron-manifest.json`.** (An earlier loose `--grep` appeared to show workflow changes; those belong to
`baa990fc` and Phase 164.8.2's merge `eeb8e731`, which merely mention 164.8.5 in their bodies.)

**Public-repo fixture safety (CONTEXT Q3) honoured.** 33 `FAKE-`-prefixed low-entropy tokens across the
new fixtures. The one JWT-shaped string in `hygiene-red.json` (pre-existing on `main`) decodes to
`{"alg":"HS256","typ":"FAKE"}` — self-identifying as fake. `.gitleaks.toml` unchanged.

## Anti-pattern scan

61 changed source files under `scripts/` and `src/`: **zero** `TBD` / `FIXME` / `XXX` (debt-marker gate
clean) and **zero** `TODO` / `HACK` / `PLACEHOLDER`.

## Review convergence

Four rounds: 4C/6W/5I → 2C/3W/3I → 1C/7W/5I → **0/0/0**. The terminal round is clean, and the fix
reports show findings closed rather than deferred.

## Final state

All five files under test are byte-identical to their pre-verification backups (`cmp`) **and** clean
against git. Full re-run at the end: prober **78/78 exit 0**, app-GUC self-test **exit 0**, corpus
**0 findings exit 0**, vitest **136 passed**.

## Goal verdict

> *"the prod-prober's hygiene rules stop being dodgeable and its parser stops dropping rows silently"*

**ACHIEVED.**

*Dodgeable* → the nine-member bypass family became a **24-row** fixture corpus; every enumerated shape
fires at least one named rule, and the three structural fixes (a real lexer via `codeSpans`, the
derived-length sum, and `long-token-anywhere` for the variable-indirection and whole-header-JSON
shapes that a normal developer writes) each go RED individually when neutered.

*Dropping rows silently* → a malformed record is now a `measure-fail` naming the count, guarded twice
over, with `captureManifest` refusing on its own path.

And the **precondition** that made the 2026-09-10 revert necessary is genuinely in force: the PROD
credential scan sits above every `return`, proven by relocation-neuter across all four oracle states,
and proven **on the real production configuration** where the manifest is currently refused.

⚠️ **One thing a later reader should not have to re-derive.** Because the committed manifest still
declares `ws-collapse-v1`, `compareManifest` refuses it before section (2), so the **manifest-side**
hygiene scan is unreachable on every production run today. The founder has booked this (CR-01's
artifact half needs a PROD re-capture). It is **not** part of any of the nine criteria — both
credential-scan criteria are about the live side, and the live side was measured firing in exactly
that state — but "the hygiene rules are in force" is true of the PROD side and **not yet** of the
manifest side, and that distinction is worth carrying forward rather than rounding off.

---

_Verified: 2026-09-11 · Verifier: Claude (gsd-verifier) · method: drive-and-neuter, byte-backup restore_
