---
phase: 164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/prod-prober/arms/cron-drift.mjs
  - scripts/prod-prober/run.mjs
  - scripts/prod-prober/fixtures/cron-drift/manifest-schema-bumped.json
  - scripts/prod-prober/fixtures/cron-drift/prod-malformed-record.json
  - src/__tests__/prod-prober-wiring.test.ts
autonomous: true
requirements:
  - 164.7-REPAIR-REVERTED
  - 164.7-WR04-HYGIENE-BELOW-ORACLE
  - 164.7-WR05-PARSER-DROPS-ROWS

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A PROD cron.job command carrying a credential is reported as cron-secret-in-command:prod:<job> when the oracle is ABSENT, when its schema_version is BUMPED, when a row fails validation, and when the live database marker does not match the manifest's — proven by one counted scenario per state (criteria 1 and 5, D1)."
    - "compareManifest's PROD-side hygiene loop is section (0), above every return, and run() never returns before compareManifest has been called with the parsed PROD rows — the two short-circuits WR-04 measured are both gone."
    - "opts.liveMarker is MANDATORY: an absent or empty marker is a measure-fail on subject 'database marker', and a marker that disagrees with manifest.database_marker is a measure-fail that still carries the PROD credential findings above it (CR-02 + SR-04)."
    - "parseCronJobRows returns { rows, malformed }; a record with fewer than seven fields is COUNTED, never dropped, and run() raises exactly one measure-fail on subject 'cron.job' naming the count (criterion 6). The empty-record skip stays — fixtureSql emits a trailing record separator by design."
    - "captureManifest refuses (exit 3, nothing written) when any record is malformed — its fourth refusal path."
    - "SELF_TEST_SCENARIOS and the wiring test's three literal copies agree with the printed k/N tail (criterion 9)."
  artifacts:
    - path: "scripts/prod-prober/arms/cron-drift.mjs"
      provides: "section (0) PROD hygiene at the top of compareManifest; mandatory liveMarker; { rows, malformed } parser"
      contains: "section (0)"
    - path: "scripts/prod-prober/run.mjs"
      provides: "reordered run() call path via the arm; malformed-record measure-fail scenario; four-state hoist scenarios; captureManifest fourth refusal"
      contains: "malformed"
    - path: "scripts/prod-prober/fixtures/cron-drift/manifest-schema-bumped.json"
      provides: "the STALE-oracle state for the hoist proof"
    - path: "scripts/prod-prober/fixtures/cron-drift/prod-malformed-record.json"
      provides: "a cron.job answer whose command carries a raw 0x1E, splitting one record into a 7-field head and a 1-field tail"
    - path: "src/__tests__/prod-prober-wiring.test.ts"
      provides: "bumped SELF_TEST_SCENARIOS pin (three literals) and direct parseCronJobRows unit tests"
  key_links:
    - from: "scripts/prod-prober/arms/cron-drift.mjs run()"
      to: "compareManifest(manifest, prodRows, { liveMarker, manifestReadError })"
      via: "run() reads the manifest WITHOUT returning on failure, passes null plus the read error, and compareManifest section (0) scans PROD rows before section (1) validates the oracle"
      pattern: "compareManifest\\(manifest, prodRows, \\{"
    - from: "scripts/prod-prober/run.mjs captureManifest"
      to: "parseCronJobRows(...).malformed"
      via: "a non-empty malformed list is refusal path four — exit 3, nothing written"
      pattern: "malformed"
    - from: "scripts/prod-prober/run.mjs SELF_TEST_SCENARIOS"
      to: "src/__tests__/prod-prober-wiring.test.ts it() title + toBe()"
      via: "D5 — four literals across two files move in ONE edit, value read from the printed tail"
      pattern: "SELF_TEST_SCENARIOS"
---

<objective>
Restructure the cron-drift arm so that NO oracle state can silence the live credential scan, and
so that a cron.job record the parser cannot read is a loud measurement failure rather than a
silent drop. This plan is EVIDENTIARY, not merely first in sequence: every fixture the later
plans add is asserted through `run()`, and until this plan lands a stale oracle can switch that
path off — which is exactly how the reverted repair (`84b21cb5`) became a net regression (SR-01).

Purpose: criterion 1 (hoist above every return, in BOTH `compareManifest` and `run()`), criterion 5
(secret reported with the oracle missing / unparseable / bumped), criterion 6 (malformed record →
measure-fail naming the count), CR-02 + SR-04 (live marker compared and mandatory), and the
`[164.7-WR04-HYGIENE-BELOW-ORACLE]` closure D1 names.

Output: `cron-drift.mjs` with section (0), a `{ rows, malformed }` parser and a mandatory marker;
`run.mjs` with the hoist-proof scenarios, the malformed-record scenario and `captureManifest`'s
fourth refusal; two new fixtures; the wiring test's pins bumped and two direct parser unit tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-CONTEXT.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-RESEARCH.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-PATTERNS.md
@scripts/prod-prober/arms/cron-drift.mjs
@scripts/prod-prober/run.mjs
@src/__tests__/prod-prober-wiring.test.ts
</context>

<decisions_honoured>
- D1 (LOCKED): PROD-side hygiene at the TOP of `compareManifest`, above every `return`. Section (0).
- D3 (LOCKED): every control ships its own red fixture, neuter-proved INDIVIDUALLY, restored from a byte backup — Task 3.
- D5 (LOCKED): `SELF_TEST_SCENARIOS` moves in the same edit as any added scenario, in FOUR literals across TWO files — the constant in `run.mjs`, and in `src/__tests__/prod-prober-wiring.test.ts` the `it()` title (twice) plus the `toBe()`.
- D6 (LOCKED): `parseCronJobRows` has TWO `continue`s and only the `f.length < 7` one is the defect; `captureManifest` is a second caller needing its own refusal path.
- Scope fence: nothing under `supabase/migrations/**` or `supabase/schema/functions/**` is modified. This plan writes nothing to production.
- CR-03's comment-strip is NOT re-applied here (`[164.7-CR03-HYGIENE-BYPASS]` is plans 03/04). `vault-absent` keeps its current `includes()` body in this plan.
</decisions_honoured>

<tasks>

<task type="tracer">
  <name>Task 1: Hoist PROD hygiene to section (0), make run() never return before it, and make the live marker mandatory and compared — one path end to end, proven through run()</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs, scripts/prod-prober/fixtures/cron-drift/manifest-schema-bumped.json, src/__tests__/prod-prober-wiring.test.ts</files>
  <reversibility rating="costly">`compareManifest`'s signature gains a required third argument and `run()`'s call order changes; `run()` is the only caller in the repo (SR-04 verified), but the self-test's `driftRun` helper and every cron-drift scenario read through it, so undoing it later is a coordinated edit across `run.mjs` scenarios.</reversibility>
  <read_first>
    - scripts/prod-prober/arms/cron-drift.mjs `compareManifest` (grep -n 'export function compareManifest') — sections (1) validation with its `invalid()` early returns, (2) the two hygiene loops, (3) adjacency. The PROD loop in (2) is what moves.
    - scripts/prod-prober/arms/cron-drift.mjs `async function run(` — the manifest read that `return`s on error BEFORE the cron.job query is issued (WR-04's second short-circuit), the marker read that logs and DISCARDS the marker (CR-02), and the final `compareManifest(manifest, prodRows)` call.
    - scripts/prod-prober/run.mjs `const driftRun =` and `fixtureSql` (the `data.marker` branch: `undefined` → `FIXTURE_DB_MARKER`, `null` → empty marker, any string → that string) — this is how the marker-mismatch state is driven with no new seam.
    - scripts/prod-prober/run.mjs `scenario("an ABSENT manifest is manifest-invalid, never 'no drift' (the SKIP-01 shape)")` — its assertions survive the hoist unchanged (it uses the CLEAN prod-ok rows), but its "with no oracle nothing was compared" wording becomes half-true and must be reworded to "with no oracle nothing was COMPARED — the PROD hygiene scan still ran, and these rows are clean".
    - scripts/prod-prober/run.mjs `scenario("cron-secret-in-command fires on an inline key whose sha MATCHES the manifest")` — the assertion shape to copy for the hoist proof (`prod-inline-key.json` carries the `FAKE-inline-key` command on `match_engine_cron`; `manifest-inline-key.json` is its dirty oracle).
    - scripts/prod-prober/run.mjs `catch` block inside `runProber` that maps an arm throw to a `measure-fail` (grep -n '"measure-fail"' around the arm loop) — nothing here changes, but the marker measure-fail must be pushed as a DEFECT by `compareManifest`, not thrown.
    - src/__tests__/prod-prober-wiring.test.ts `it("SELF_TEST_SCENARIOS is 52, and the runner PRINTS exactly 52 headers numbered 1..52"` — the three literal copies D5 names (two in the title, one in `toBe`).
    - 164.8.5-RESEARCH.md Q7 "Is criterion 1 (the hoist) really a precondition?" — the two short-circuits and the reorder.
  </read_first>
  <action>
**(a) `compareManifest(manifest, prodRows, opts)` gains section (0), placed BEFORE the `invalid` helper and every return.** Section (0) is the existing PROD hygiene loop moved verbatim (still `prodHygiene` Map + `cron-secret-in-command` with subject `prod:<jobname>`), preceded by a comment block headed `section (0)` that carries SR-01's measurement in prose: with a PROD command carrying an inline JWT, appending a tidy-comment to the manifest's copy of that command collapsed the result to `manifest-invalid` and the credential finding VANISHED for all 14 jobs; therefore this loop reads `prodRows` only, needs nothing from the oracle, and `⛔ Do not move this below any return`. Guard `prodRows` with `Array.isArray(prodRows) ? prodRows : []` so a null oracle AND a null row list still reach section (1). Section (1)'s `invalid()` and every existing validation stay exactly where they are, now BELOW section (0). Section (2) keeps only the MANIFEST-side loop. Section (3) onward is unchanged.

**(b) Mandatory, compared live marker (CR-02 + SR-04), placed immediately AFTER section (0) and BEFORE section (1).** Read `opts.liveMarker`. If it is not a non-empty trimmed string, push `{ kind: "measure-fail", subject: "database marker", detail: <provenance was never established; current_database() is postgres on every Supabase project and proves nothing; no statement about drift can be made> }` and `return { defects, lines }`. Then, ONLY after section (1)'s object/schema/normalization checks have passed (so a null oracle still reports `manifest-invalid` rather than a marker mismatch against `undefined`), compare `String(manifest.database_marker).trim()` to `opts.liveMarker.trim()`; on disagreement push a `measure-fail` on subject `database marker` whose detail names BOTH markers verbatim (they are human-chosen names, safe to print — CR-02's prescribed sentence) and `return`. Also accept `opts.manifestReadError`: when it is a non-empty string, section (1)'s first check (`!manifest || typeof manifest !== "object"`) must produce the `manifest-invalid` detail that names the read error and the path, so the sentence `run()` used to emit is preserved word for word inside `compareManifest`.

**(c) `run()` never returns before `compareManifest`.** New order: (1) marker read — the two existing `measure-fail` returns for an unreadable/empty marker STAY (no cron.job row was read, so there is nothing to scan; say so in the comment); (2) cron.job read — its `measure-fail` return stays for the same reason; (3) parse rows; (4) read the manifest into `manifest` with `readError` captured but NO return; (5) `const { defects, lines } = compareManifest(manifest, prodRows, { liveMarker: marker, manifestReadError: readError || null })`; forward as today. Delete the old `manifest-invalid` `addDefect` + `return` block from `run()` — its sentence now lives in `compareManifest` via `manifestReadError`. Update the file-header prose that already claims hygiene runs "EVEN WHEN ITS SHA MATCHES THE MANIFEST" to add "and even when there is no readable manifest at all — section (0)".

**(d) Fixture `manifest-schema-bumped.json`:** byte-copy of `fixtures/cron-drift/manifest.json` with `schema_version` set to `2`. Nothing else differs.

**(e) Three counted scenarios in `run.mjs`, added beside the ABSENT-manifest scenario, each driving `prod-inline-key.json` rows through `driftRun`-style `runProber` calls and each asserting `r.defects.some(d => d.kind === "cron-secret-in-command" && d.subject === "prod:match_engine_cron")` PLUS the state's own defect PLUS exit 1:**
  1. "HOIST: the PROD credential scan fires with the oracle ABSENT" — `manifestPath` pointing at a non-existent file; assert exactly one `manifest-invalid` whose detail names the path AND the credential finding.
  2. "HOIST: … with the oracle STALE (schema_version bumped)" — `manifest-schema-bumped.json`; assert one `manifest-invalid` mentioning `schema_version` AND the credential finding.
  3. "HOIST: … with the live marker MISMATCHED" — seams built with `fixtureSql({ cronJobRows: prod.data, marker: "quantalyze-fixture-OTHER" })` against `manifest-inline-key.json`; assert exactly one `measure-fail` on subject `database marker` whose detail contains BOTH marker strings, ZERO `cron-drift` defects (`noDefectOfKind`), AND the PROD credential finding — SR-03 item 3 (a)/(b)/(c) in one scenario.
  Plus a fourth: "MARKER MANDATORY: compareManifest called without a liveMarker is a measure-fail, never a pass" — call `CRON_DRIFT_MOD.compareManifest(man.data, prod.data, {})` directly with the CLEAN `prod-ok.json` + `manifest.json`; assert exactly one defect, kind `measure-fail`, subject `database marker`. Assert absence ONLY through `noDefectOfKind` (S3 — never an inline `kind === "…"` negation; that string in a comment credits coverage).
  Reword the ABSENT-manifest scenario's final expectation message as described in read_first; its assertions do not change.

**(f) D5 bump, in the same edit:** run `node scripts/prod-prober/run.mjs --self-test`, read the `k/N` tail, set `SELF_TEST_SCENARIOS` in `run.mjs` and ALL THREE literals in the wiring test's `it()` title and `toBe()` to that N. Do not pre-compute N; read it.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; grep -q 'section (0)' scripts/prod-prober/arms/cron-drift.mjs &amp;&amp; grep -q 'HOIST: the PROD credential scan fires with the oracle ABSENT' scripts/prod-prober/run.mjs &amp;&amp; grep -q 'live marker MISMATCHED' scripts/prod-prober/run.mjs &amp;&amp; grep -q 'MARKER MANDATORY' scripts/prod-prober/run.mjs &amp;&amp; test -f scripts/prod-prober/fixtures/cron-drift/manifest-schema-bumped.json &amp;&amp; node -e 'const m=require("./scripts/prod-prober/fixtures/cron-drift/manifest-schema-bumped.json");process.exit(m.schema_version===2?0:1)' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed'</automated>
  </verify>
  <done>Self-test exits 0 with the three HOIST scenarios and the MARKER MANDATORY scenario counted; the wiring test passes with its four literals agreeing; a PROD credential fires under absent / bumped / marker-mismatched oracles. Failing direction: reverting section (0) below `invalid()` turns the ABSENT and STALE scenarios red naming `prod:match_engine_cron`; deleting the marker comparison turns the MISMATCHED scenario red on `measure-fail`.</done>
</task>

<task type="auto">
  <name>Task 2: parseCronJobRows returns { rows, malformed }; run() raises a measure-fail naming the count; captureManifest refuses — with the E'\x1e' counter-example fixture</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs, scripts/prod-prober/fixtures/cron-drift/prod-malformed-record.json, src/__tests__/prod-prober-wiring.test.ts</files>
  <reversibility rating="costly">The parser's return type changes from an array to `{ rows, malformed }`, and it has TWO callers (`run()` in the arm and `captureManifest` in `run.mjs`); undoing it means re-touching both call sites and the scenarios that read them.</reversibility>
  <read_first>
    - scripts/prod-prober/arms/cron-drift.mjs `export function parseCronJobRows` — the TWO `continue`s. `rec.trim().length === 0` is CORRECT (trailing record separator, `fixtureSql`'s "psql prints the record separator AFTER every record" comment) and MUST STAY. `f.length < 7` is the defect.
    - scripts/prod-prober/arms/cron-drift.mjs docstring above `CRON_JOB_SEPARATORS` — the claim that 0x1E/0x1F "cannot occur in SQL source text"; this task ships its counter-example and the docstring must be corrected to say a command containing an `E'\x1e'` literal or a pasted control byte DOES produce one, which is why the parser counts rather than drops.
    - scripts/prod-prober/arms/cron-obs.mjs `judgeCronObs` ATTRIBUTION FENCE block — the house shape for "a pure judge that could not read a row it was handed": a `measure-fail` whose detail's second sentence says what the zero must NOT be read as.
    - scripts/prod-prober/run.mjs `captureManifest` — the three existing refusal paths (exit 3 usage / 3 unidentified database / 1 hygiene) and its docblock listing them; the malformed refusal is the FOURTH and the docblock gains a line for it.
    - scripts/prod-prober/run.mjs `scenario("two responses in ONE run's window is measure-fail, not a verdict (review WR-01)")` — the closest standalone measure-fail scenario; copy its assertion shape. ⛔ Do NOT register the malformed fixture in `ARM_FIXTURE_TABLE` (PATTERNS §1c: it would force `measure-fail` into the `kinds` array and the REMEDIES leg).
    - 164.8.5-RESEARCH.md "WR-05 reproduction" — the measured outputs for good+bad and for a raw RS byte inside a command.
  </read_first>
  <action>
**(a) Parser.** `parseCronJobRows(stdout)` returns `{ rows, malformed }` where `malformed` is an array of `{ index, fields }` (record index and field count — NEVER the record text, which may carry the credential the arm exists to find). Keep the empty-record skip. Replace the `f.length < 7` `continue` with a push onto `malformed` followed by `continue`. Update the JSDoc: "A row the parser could not read is NOT a row that is not there."

**(b) Arm.** In `run()`, after parsing: if `malformed.length > 0`, `addDefect("measure-fail", "cron-drift", "cron.job", <detail>)` where the detail names `${malformed.length} of ${malformed.length + rows.length} record(s)` and their field counts, and whose second sentence is the doctrine sentence above. Do NOT return — the readable rows still go through section (0) and the comparison, because a parse failure on one row must not silence the credential scan on the others; say so in the comment.

**(c) Capture.** In `captureManifest`, destructure `{ rows, malformed }`; when `malformed.length > 0`, log `REFUSED: <n> cron.job record(s) could not be parsed … Nothing was written.` and `return 3` BEFORE the hygiene loop. Add the fourth refusal line to the docblock (`3 — a cron.job record the parser could not read`).

**(d) Fixture `prod-malformed-record.json`:** byte-copy of `prod-ok.json`, then in the `match_engine_cron` row's `command` insert a literal U+001E character (JSON escape `\\u001e`) inside the `'{}'` body literal, so `fixtureSql` renders a record that the record separator splits into a 7-field head and a 1-field tail. Verify by hand that `parseCronJobRows` on that rendered stdout yields `rows.length === 3` and `malformed.length === 1` before writing the scenario.

**(e) Scenario "a MALFORMED cron.job record is a measure-fail naming the count, never a continue (WR-05)":** run `driftRun(prod.data, driftFixturePath("manifest.json"), quiet)` with the new fixture; assert exit 1, exactly ONE `measure-fail` whose subject is `cron.job` and whose detail contains `1 of 4 record(s)`, and that the detail does NOT contain the string `{}` (the record text is never quoted). Do not assert the drift side — the truncated head legitimately drifts. Bump D5's four literals again from the printed tail.

**(f) Two direct unit tests in `src/__tests__/prod-prober-wiring.test.ts`** (no scenario bump): import `parseCronJobRows` and `CRON_JOB_SEPARATORS` from the arm; (1) a stdout of one good 7-field record + one 5-field record → `rows.length === 1 && malformed.length === 1 && malformed[0].fields === 5`; (2) CALIBRATION per S4: the same stdout with the 5-field record padded to 7 fields → `malformed.length === 0` and `rows.length === 2` — the predicate must flip on the mutated copy, and the test must assert the mutated stdout differs from the original.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; grep -q 'MALFORMED cron.job record' scripts/prod-prober/run.mjs &amp;&amp; node --input-type=module -e 'import {parseCronJobRows,CRON_JOB_SEPARATORS as S} from "./scripts/prod-prober/arms/cron-drift.mjs"; const good=["1","a","* * * * *","t","db","u","SELECT 1"].join(S.fieldSep); const bad=["2","b","*","t","db"].join(S.fieldSep); const r=parseCronJobRows(good+S.recordSep+bad+S.recordSep); process.exit(r.rows.length===1&amp;&amp;r.malformed.length===1?0:1)' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed'</automated>
  </verify>
  <done>A 5-field record is counted in `malformed`, never dropped; the self-test's WR-05 scenario is green; `captureManifest` has a fourth refusal path; the docstring's "cannot occur" claim is corrected. Failing direction: restoring the bare `continue` makes the inline node check exit 1 (`malformed.length === 0`) and turns the WR-05 scenario red.</done>
</task>

<task type="auto">
  <name>Task 3: Neuter-prove every control this plan added, INDIVIDUALLY, from byte backups</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs</files>
  <read_first>
    - 164.8.5-CONTEXT.md D3 — a batch neuter proves nothing about the individual arms; the reverted repair disabled two comparisons together and reported one RED as proof of both.
    - 164.7-REVIEW.md SR-03 — the neuter matrix format (control / neuter / result) and the rule that each surviving control needs its own RED.
    - src/__tests__/prod-prober-wiring.test.ts header lines 12-17 — "a `.replace()` that matched nothing returns the original and the calibration passes vacuously"; the same applies to a neuter edit that does not apply.
  </read_first>
  <action>
Write a scratchpad harness `neuter.sh <file> <needle> <replacement> <expected-red-substring>` under the session scratchpad directory (never under the repo) that: (1) `cp`-backs up the file byte-for-byte; (2) applies the replacement with `node` string replace and EXITS 2 if the file did not change (a neuter that did not apply proves nothing); (3) runs `node scripts/prod-prober/run.mjs --self-test`, captures exit code and output; (4) restores with `cp` from the backup and `cmp`s to prove byte identity — ⛔ never `git checkout --`, which destroys uncommitted work; (5) exits 0 only if the self-test exited 1 AND its output contains the expected substring.

Run it once per control, ONE control per run, and record the matrix in the SUMMARY:
| control | neuter | expected RED names |
| section (0) placement | move the PROD loop back below `invalid()` (replace the section (0) loop with a no-op that leaves `prodHygiene` empty) | `HOIST: … ABSENT` and `HOIST: … STALE` naming `prod:match_engine_cron` |
| marker comparison | replace the marker inequality test with a constant `false` | `live marker MISMATCHED` |
| marker mandatory | replace the `typeof opts.liveMarker !== "string"` guard with a constant `false` | `MARKER MANDATORY` |
| malformed count | replace the `malformed.push` with a bare `continue` path (i.e. neutralise the push) | `MALFORMED cron.job record` |
| captureManifest refusal | replace the `malformed.length > 0` refusal condition with `false` | the capture-mode scenario for malformed rows, if one exists; otherwise add ONE counted scenario driving `captureManifest` with the malformed fixture and asserting exit 3 and no file written, and bump D5's four literals |

After the matrix: `cmp` both files against their backups, then run the full quick set: `node scripts/prod-prober/run.mjs --self-test` (green) and `npx vitest run src/__tests__/prod-prober-wiring.test.ts` (green). Confirm `git status --short` lists only this plan's five files.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed' &amp;&amp; test "$(git status --short -- scripts src | grep -vc -e 'cron-drift.mjs' -e 'prod-prober/run.mjs' -e 'manifest-schema-bumped.json' -e 'prod-malformed-record.json' -e 'prod-prober-wiring.test.ts')" = 0</automated>
  </verify>
  <done>Five individual neuters each produced a RED naming the expected scenario, each restored to byte identity; the matrix is in the SUMMARY with exit codes; the tree carries only this plan's files. Failing direction: a neuter that leaves the self-test green is a control that cannot fail and BLOCKS this plan — fix the scenario, do not record it as "acceptable".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD `cron.job` text → arm | untrusted SQL text (may carry credentials, control bytes, hostile literals) crosses into a Node text parser |
| committed oracle → arm | a hand-reviewable public file; a hand-edit is this arm's own adversary (SR-01) |
| arm → public Actions log | every defect detail is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.5-01 | Information disclosure | `compareManifest` ordering | high | mitigate | section (0) above every return; three HOIST scenarios; closes T-164.7-07 clause "rules stay in force" on the ordering axis |
| T-164.8.5-02 | Tampering | oracle hand-edit / stale schema | high | mitigate | credential scan independent of oracle validity; `manifest-invalid` still emitted below it |
| T-164.8.5-03 | Spoofing | wrong database compared under the manifest's marker | high | mitigate | mandatory `liveMarker`, compared to `manifest.database_marker`, mismatch = measure-fail (CR-02, SR-04) |
| T-164.8.5-04 | Repudiation | parser drops a PROD row silently | medium | mitigate | `{ rows, malformed }` + measure-fail naming the count; captureManifest refusal |
| T-164.8.5-05 | Information disclosure | measure-fail detail quoting a record | medium | mitigate | `malformed` carries index + field count only; scenario asserts the body literal is not echoed |
| T-164.8.5-SC | Tampering | npm/pip/cargo installs | low | accept | no package is installed by this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `node scripts/prod-prober/run.mjs --self-test` exits 0 and its tail names the new N; `npx vitest run src/__tests__/prod-prober-wiring.test.ts` passes.
- The three HOIST scenarios and the MARKER MANDATORY scenario each exist by title and each was individually neuter-proved (Task 3 matrix in SUMMARY).
- `grep -n 'section (0)' scripts/prod-prober/arms/cron-drift.mjs` resolves ABOVE `const invalid =`.
- No file under `supabase/` changed: `git status --short supabase` is empty.
</verification>

<success_criteria>
Criteria 1, 5 and 6 of ROADMAP Phase 164.8.5 are met by counted scenarios; `[164.7-WR04-HYGIENE-BELOW-ORACLE]` and `[164.7-WR05-PARSER-DROPS-ROWS]` have their fix in place with a red control each; criterion 9 holds at this plan's N. The four repairs (CR-01, CR-04, WR-11, SR-05/07/08/09) are NOT in this plan — they are Plan 02, and may now be re-applied because nothing above section (0) can silence detection.
</success_criteria>

<output>
Create `.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-01-SUMMARY.md` when done, carrying the neuter matrix with exit codes and the N read from the self-test tail.
</output>
