---
phase: 164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab
plan: 06
subsystem: testing
tags: [prod-prober, cron-drift, neuter-matrix, anti-vacuity, vault, secret-hygiene, cr-03]

requires:
  - phase: 164.8.5-04
    provides: "the adjudicated three-part matrix standard: one control per EDIT (a harness invariant), a DECLARED expected-failing SET matched exactly, pairwise-distinct message-granularity fingerprints"
  - phase: 164.8.5-05
    provides: "the twelve controls this plan neuters — vault-absent reachability, the resolver, long-token-anywhere and the TOKEN_MIN pin"
provides:
  - "a twelve-row neuter matrix over Plan 05's controls: 12 RED, 12 byte-identical restores, 12/12 exact declared-set matches, 0 message-fingerprint clashes"
  - "the W3 TOKEN_MIN pin proved to BITE, with `header_region_only`'s expect_not measured HOLDING beside the red pin at ROW granularity"
  - "the routed wave-4 residual closed: `fixture_unbalanced_headers_job`'s load-bearing value length is now ASSERTED, not merely noted — three branches, each neuter-proved"
  - "CR-03 and SR-02 reproductions re-run at HEAD, both non-empty, plus SR-02's second-order FALSE-POSITIVE direction measured clean"
  - "two harness defects found and fixed that would have made a matrix measure the wrong thing: `bash -lc` resolving Node 19.2.0, and two rows neutering a SHARED constant (a batch neuter)"
affects: [164.5.1, 164.8.6, 164.8.5-07]

actuals:
  tokens: 603   # chars/4 over the realized diff (2,415 added chars in 97bbba30). See "Estimate calibration".
  tasks: 2
  commits: 1

tech-stack:
  added: []
  patterns:
    - "A matrix row separates PREDICTED observers (`expect`, checked by `missing`) from a MEASURED whole-arm blast radius (`expectCascade`, checked by `others`) — provenance is recorded rather than blurred, and both are exact-matched"
    - "A neuter targets a CALL SITE, never a shared constant: blanking a constant with N call sites is a batch neuter wearing one edit's clothing"
    - "A fixture value whose LENGTH is load-bearing is asserted by DERIVING the length from its operand chain, never by hand-typing it"

key-files:
  created: []
  modified:
    - scripts/prod-prober/run.mjs

key-decisions:
  - "Rows 1 and 12 were NARROWED from the plan's letter: `VAULT_READ_RE` has three call sites and `DO_PREFIX` two, so blanking either constant disables several controls in one edit — D3's forbidden batch neuter, and one of row 1's victims is row 4's own control."
  - "Row 5's neuter points FUNCTIONS_DIR at a directory that EXISTS but lacks the tick function, not at an absent one: an absent directory throws, and scenario 46 calls compareManifest outside runProber's try/catch, so the self-test CRASHES at 46 and never reaches scenario 52 — the very observer the plan names."
  - "The matrix runs under `bash -c`, never `bash -lc`: the login shell on this box resolves /usr/local/bin/node v19.2.0, below the repo's own `engines: >=22`."
  - "The routed run.mjs guard was committed SEPARATELY and BEFORE the matrix was re-run from scratch, so no edit is folded into a matrix it would invalidate — wave 4's ordering for the same situation."
  - "The pin row's `header_region_only` obligation is measured at ROW granularity, because at SCENARIO granularity `hygiene BYPASS` reds for a different row and the question cannot be answered."

patterns-established:
  - "Probe the harness before trusting it: a no-op neuter must exit 2, an inert neuter must report GREEN, and a mis-declared expected set must report both missing and others."
  - "Prove a NEW guard the same way as an old one — every branch of its message neutered alone, restored from a byte backup."

requirements-completed: [164.7-CR03-HYGIENE-BYPASS]

coverage:
  - id: D1
    description: "Every control Plan 05 added — the executable Vault-read shape on spans, the three resolver arms, the real-snapshot default, the token length threshold, the digit test, the bare-URL exemption, the header-region exclusion, the jwt exemption and the codeSpans re-entry — has been disabled ALONE, from a byte backup, produced a RED naming its own fixture or scenario, and been restored to byte identity"
    requirement: "164.7-CR03-HYGIENE-BYPASS"
    verification:
      - kind: other
        ref: "scratchpad/w6/matrix.mjs — 12 rows: red 12/12, restores byte-identical 12/12, declared-set exact 12/12, fingerprint clashes 0"
        status: pass
    human_judgment: true
    rationale: "The matrix harness is a scratchpad script, not a committed CI gate. Its verdict is reproducible from the pasted rows and the committed tree, but nothing in CI re-runs it — the fourth wave running to record this limit. A human must accept the dated reading."
  - id: D2
    description: "The TOKEN_MIN pin BITES: making TOKEN_MIN diverge from HEADERS_LITERAL_MAX turns the wiring partition test RED, while `header_region_only`'s expect_not still HOLDS in the same run"
    verification:
      - kind: unit
        ref: "src/__tests__/prod-prober-wiring.test.ts#TOKEN_MIN equals HEADERS_LITERAL_MAX — the Q2 region partition has no gap… (matrix row 7)"
        status: pass
    human_judgment: false
  - id: D3
    description: "CR-03's and SR-02's reproduction commands both return NON-EMPTY violation lists at HEAD, and SR-02's second-order FALSE-POSITIVE direction returns []"
    requirement: "164.7-CR03-HYGIENE-BYPASS"
    verification:
      - kind: integration
        ref: "node --input-type=module -e '…hygieneViolations…' — CR-03 verbatim => 2, SR-02 (a) => 2, SR-02 (b) => 2, SR-02 second-order => []"
        status: pass
    human_judgment: false
  - id: D4
    description: "`fixture_unbalanced_headers_job`'s load-bearing header-value length is ASSERTED rather than documented — scenario 48 derives it (37) and compares against HEADERS_LITERAL_MAX (32)"
    verification:
      - kind: integration
        ref: "node scripts/prod-prober/run.mjs --self-test#48 — three branches each neuter-proved (G1 value shortened, G2 anchor renamed, G3 row deleted)"
        status: pass
    human_judgment: false
  - id: D5
    description: "`cron-drift.mjs` and `run.mjs` are byte-identical to their post-guard pre-matrix state; zero `supabase/` changes; gitleaks delta against base 9636ad77 is ZERO"
    verification:
      - kind: other
        ref: "cmp against both pre-matrix backups; git diff --stat main...HEAD -- supabase/ => 0 lines; gitleaks JSON set-diff => added 0, removed 0"
        status: pass
    human_judgment: false

duration: 25 min
completed: 2026-09-11
status: complete
---

# Phase 164.8.5 Plan 06: The permanent neuter matrix over Plan 05's controls — Summary

**All twelve of Plan 05's controls were disabled one at a time from byte backups and every one produced a RED naming its own fixture — 12 red, 12 byte-identical restores, 12/12 exact declared-set matches, 0 message-fingerprint clashes — and the matrix itself found two defects that would have made it measure the wrong thing: a login shell silently resolving Node 19.2.0, and two rows whose prescribed neuter blanked a SHARED constant, which is a batch neuter wearing one edit's clothing.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-09-10T22:13Z
- **Completed:** 2026-09-10T22:38Z
- **Tasks:** 2
- **Files modified:** 1 (`scripts/prod-prober/run.mjs`, +31 lines — the routed guard only)

## Accomplishments

- **Twelve controls, twelve individual neuters, twelve REDs.** Every row: `cp` byte backup → anchor asserted to occur EXACTLY once → ONE replacement (exit 2 if the file did not change) → the edit verified to have LANDED on disk → run → structural attribution → `cp` restore → `cmp` against BOTH the row backup and the pre-matrix backup. ⛔ No `git checkout --` anywhere.
- **The W3 pin bites, and its second obligation was measured properly.** Divergence reds the wiring pin; `header_region_only`'s `expect_not` still HOLDS in the same run.
- **The routed wave-4 residual is closed and proved.** The fixture length that keeps the unparseable-region skip falsifiable is now asserted, derived from the operand chain rather than hand-typed. Measured first that the gap was real: shortening the value passed **69/69 at exit 0**.
- **CR-03 returns 2, not `[]`.** Wave 4 recorded that `2` is what the fully-repaired rule set should return, and it now does — both weaknesses closed.
- **Two harness defects found before they could corrupt the verdict** (below). Both are the class this phase exists to eliminate: a measurement that looks green while measuring the wrong thing.

## Task Commits

This is a **pure verification plan**: Tasks 1 and 2 restore every file byte-identically and therefore produce no commit by design. The one commit is the residual wave 4 routed here, committed separately and *before* the matrix was re-run from scratch.

1. **Routed guard — the unbalanced row's load-bearing value length made falsifiable** — `97bbba30` (test)

**Plan metadata:** this SUMMARY (docs).

## Files Created/Modified

- `scripts/prod-prober/run.mjs` — scenario 48 gains one assertion (plus its derivation and rationale) that `fixture_unbalanced_headers_job`'s header value still DERIVES ≥ `HEADERS_LITERAL_MAX`. Adds no scenario, no fixture row and no rule; `SELF_TEST_SCENARIOS` stays **69** (D5) and `HEADERS_LITERAL_MAX` is read, never moved (D4).
- `scripts/prod-prober/arms/cron-drift.mjs` — **UNCHANGED**, `cmp`-verified.

## THE MATRIX — twelve rows, one control per EDIT

Harness: `scratchpad/w6/matrix.mjs`, written for this wave. Streams merged **at the shell** (`2>&1`) because `run.mjs` prints headers to stdout and `SELF-TEST FAIL:` to stderr; self-test REDs are credited to the last `=== SELF-TEST k/N: <title> ===` header seen, vitest REDs come from `--reporter=json`.

⭐ Every row's `expect` was written **in the harness source before the first run**, from reading the code.

| # | task | control | one edit | exit | obs | RED observed (quoted) | restore |
|---|---|---|---|---|---|---|---|
| 1 | 1 | the EXECUTABLE Vault-read shape on code **spans** | the span call site → `if (false)` | **1** | 23 | `prod-ok.json exits 0 (got 1; defects [{"kind":"cron-secret-in-command",…"[vault-absent] job match_engine_cron does not reach a Vault read that EXECUTES…` | BYTE-IDENTICAL |
| 2 | 1 | the resolver FIRES on a callable with no snapshot file (fail closed) | missing file → `return true` | **1** | 2 | `and every command that merely MENTIONS one is rejected — this is what `text.includes()` could not do (a callable with no file in the snapshot at all)` | BYTE-IDENTICAL |
| 3 | 1 | the TRANSITIVE edge | `CALLABLE_DEPTH_MAX = 0` | **1** | 1 | `every command that REACHES an executing Vault read is accepted (an UNQUALIFIED call, resolved TRANSITIVELY through a wrapper)` | BYTE-IDENTICAL |
| 4 | 1 | a callable's body is judged on **masked** text | `scanSql(d.body).masked` → `d.body` | **1** | 1 | `and every command that merely MENTIONS one is rejected … (a callable whose body mentions the table only in a comment \| a callable whose body mentions it only inside a literal)` | BYTE-IDENTICAL |
| 5 | 1 | `FUNCTIONS_DIR` points at the REAL committed snapshot | default → the fixture snapshot dir | **1** | 3 | `PRECONDITION: the real snapshot carries match_engine_cron_tick.sql at …/fixtures/cron-drift/functions/match_engine_cron_tick.sql — its absence is a FAIL, not a skip` | BYTE-IDENTICAL |
| 6 | 2 | the token LENGTH threshold **at the comparison site** | `< TOKEN_MIN` → `< TOKEN_MIN + 967` | **1** | 2 | `every MEASURED bypass shape now fires at least one rule ((h) variable indirection …, (i) whole-header JSON — the header NAME is inside a bigger literal, so no anchor ever matches)` | BYTE-IDENTICAL |
| 7 | 2 | **the W3 PIN** — `TOKEN_MIN` DERIVED from `HEADERS_LITERAL_MAX` | `= HEADERS_LITERAL_MAX + 8` | **1** | 4 | `vitest: TOKEN_MIN equals HEADERS_LITERAL_MAX — the Q2 region partition has no gap…` (+ the two rows below) | BYTE-IDENTICAL |
| 8 | 2 | the token DIGIT test | `if (!/\d/.test(token))` → `if (false)` | **1** | 3 | `and fires ONLY that rule — red-fixture ISOLATION … (pg-password->[long-token-anywhere,pg-password])` | BYTE-IDENTICAL |
| 9 | 2 | the BARE-URL exemption | the `BARE_URL_RE` continue deleted | **1** | 1 | `no achievable shape is flagged (fixture_bare_url_job: [long-token-anywhere] job fixture_bare_url_job carries a 32+ character whitespace-delimited token containing a digit …)` | BYTE-IDENTICAL |
| 10 | 2 | the HEADER-REGION exclusion (the Q2 partition) | the region `continue` → `if (false)` | **1** | 2 | `and no row fires a rule its expect_not forbids — the header-region token belongs to long-literal-in-headers ALONE (header_region_only->long-token-anywhere header_region_only_in_do->long-token-anywhere)` | BYTE-IDENTICAL |
| 11 | 2 | the JWT hand-off exemption | the `eyJ…` continue → `if (false)` | **1** | 1 | `and fires ONLY that rule — red-fixture ISOLATION … (jwt-shape->[long-token-anywhere,jwt-shape])` | BYTE-IDENTICAL |
| 12 | 2 | `codeSpans` re-enters a top-level `DO` body as CODE (re-proved) | the re-entry call site → `if (false)` | **1** | 26 | `prod-ok.json exits 0 (got 1; defects [{"kind":"cron-secret-in-command",…"[vault-absent] job match_engine_cron does not reach a Vault read that EXECUTES…` | BYTE-IDENTICAL |

Final harness verdict, on the tree that is `cmp`-identical to the committed one:

```
  rows: 12   red: 12   restores byte-identical: 12
  declared-set exact match (others=[] AND missing=[]): 12/12
  MESSAGE-granularity fingerprint clashes: 0 []
  OBSERVER-SET-granularity clashes (what others=[] can see): 2 [[3,4],[6,10]]
  ✅ MATRIX CLEAN
```

### The two observer-set clashes are exactly wave 4's Finding 1, re-measured

Two pairs of rows are pairwise **indistinguishable** at the granularity `others=[]` works at, and distinct at MESSAGE granularity — which is why the phase's standard needs both criteria and not either alone:

```
pair (3,4) SHARE observers: ["vault-absent TRI-STATE on the fixture snapshot: …"]
  row 3: every command that REACHES an executing Vault read is accepted (an UNQUALIFIED call, resolved TRANSITIVELY through a wrapper)
  row 4: and every command that merely MENTIONS one is rejected … (a callable whose body mentions the table only in a comment | … only inside a literal)

pair (6,10) SHARE observers: ["hygiene BYPASS: …", "hygiene RED: …"]
  row 6:  every MEASURED bypass shape now fires at least one rule ((h) …, (i) …) || every red row fires the rule it names (long-token-anywhere (fixture_var_key_job))
  row 10: and no row fires a rule its expect_not forbids … (header_region_only->long-token-anywhere header_region_only_in_do->long-token-anywhere) || and fires ONLY that rule — red-fixture ISOLATION … (long-literal-in-headers->[long-literal-in-headers,long-token-anywhere])
```

### The pin row's second obligation — measured at ROW granularity

```
ROW 7 exit=1 obs=4 missing=0 others=0 besideGreen=HELD (as required)
  BESIDE: hygiene BYPASS row `header_region_only` — its expect_not still holds (no RED names it) => HELD
  that scenario reds for a DIFFERENT row: every MEASURED bypass shape now fires at least one rule
                                          ((h) variable indirection — k := '<key>' then jsonb_build_object(name, k))
```

⚠️ **The plan predicted the pin would be the ONLY observer** — *"no scenario notices — only the pin does."* **MEASURED FALSE, in the safe direction.** Raising `TOKEN_MIN` to 40 lifts it above the (h) and RED-isolation fixture tokens (32–39 chars), so those rows stop firing and their scenarios red too. The divergence is caught by MORE than the pin. What the plan actually asks for still holds and is the load-bearing part: **no RED names `header_region_only`** — its `expect_not` is satisfied while the derivation is broken, i.e. the partition itself reports no gap. Only the pin sees that. Scenario granularity cannot answer this question at all, so the check is written at row granularity.

### The harness was probed before it was trusted, and again after the guard commit

| probe | mutation | result (both runs) |
|---|---|---|
| A — declared-set detection | row 11's `expect` pointed at the wrong scenario | `missing=1 others=1`, defect reported, exit 1 |
| B — no-op neuter | `repl` set equal to `find` | `ROW 11: INVALID — the replacement did not change the file (no-op neuter)`, **exit 2** |
| C — GREEN detection | an inert neuter (a word inside a comment) | `exit=0 … red: 0` → `❌ MATRIX DEFECTIVE` |

All three bite. All three restored byte-identically.

## The routed wave-4 residual — closed, and the gap measured FIRST

Wave 4 shipped `fixture_unbalanced_headers_job` with a 37-character derived value and a `note` explaining that the length is load-bearing, then recorded as accepted residual 1: *"nothing FAILS if a future editor shortens it — the control would silently become unfalsifiable again … Routed to Plan 06."*

**The gap was real, and measured before it was fixed:**

```
$ # value shortened from 37 to 10 derived characters, guard ABSENT
$ node scripts/prod-prober/run.mjs --self-test | tail -1
=== SELF-TEST PASSED: 69/69 scenarios, every arm's kinds fired on their own fixtures and nowhere else ===
exit=0   <-- the shortening was INVISIBLE
```

The guard now derives the length from the `||` operand chain following the header name, so the number is measured and never hand-typed:

```
  ok — and fixture_unbalanced_headers_job's header value still DERIVES at least HEADERS_LITERAL_MAX
       characters (37 vs 32) — shorten it and neutering the unparseable-region skip stops reddening
       anything, which is how that control silently became unfalsifiable once already
```

**All three of its branches neuter-proved individually**, each restored from a byte backup and re-greened:

| # | one edit to `hygiene-red.json` | RED observed | restore |
|---|---|---|---|
| G1 | the value shortened to 10 derived chars | `and fixture_unbalanced_headers_job's header value still DERIVES at least HEADERS_LITERAL_MAX characters (10 vs 32)…` | byte-identical |
| G2 | the header-name anchor renamed on that row only | `fixture_unbalanced_headers_job's command still anchors its header value on 'X-Trace' — the guard below cannot measure a value it cannot find` | byte-identical |
| G3 | the row deleted | `PRECONDITION: hygiene-red.json carries a header-unparseable row…` + `the fixture's rule ids are EXACTLY the arm's HYGIENE_RULE_IDS…` | byte-identical |

⛔ **Ordering.** The guard was committed in `97bbba30` and only THEN was the full twelve-row matrix re-run from scratch against re-taken backups — wave 4's ordering for the same situation, because a matrix that repairs what it is measuring is self-confirming.

## Verification Gate Output (verbatim, run at HEAD)

### `node scripts/prod-prober/run.mjs --self-test`

```
  ok — the counted scenario set is complete: 69 header(s) printed, SELF_TEST_SCENARIOS is 69

=== SELF-TEST PASSED: 69/69 scenarios, every arm's kinds fired on their own fixtures and nowhere else ===
exit=0
```

### The 14 committed manifest commands at ZERO hygiene violations

```
=== SELF-TEST 54/69: the COMMITTED cron manifest passes every hygiene rule — zero violations on all rows (the false-positive budget) ===
  ok — the committed manifest parses (ok)
  ok — at least ten committed commands were judged — a manifest with fewer rows than that is not the oracle this scenario means (got 14)
  ok — and NONE of them trips a hygiene rule (all 14 clean)
```

### `npx vitest run` — the three files

```
 Test Files  3 passed (3)
      Tests  101 passed | 1 skipped (102)
   Duration  1.32s
```

⚠️ **The one skip is named rather than hidden.** It is `gitleaks-allowlist.test.ts` → *"advertises skip reason when the gitleaks binary is unavailable"*, declared `it.skipIf(HAS_GITLEAKS)`. It is inactive **because gitleaks IS installed here**; its two `skipIf(!HAS_GITLEAKS)` siblings RAN. It is the complementary arm for a machine without the binary, not a disabled assertion, and it is not this plan's.

### `npm run lint`

```
  121:7   warning  Unused eslint-disable directive (no problems were reported from 'react-hooks/set-state-in-effect')
  1119:6  warning  React Hook useMemo has a missing dependency: 'period'. …  react-hooks/exhaustive-deps

✖ 2 problems (0 errors, 2 warnings)

[check-admin-route-manifest] OK — 20 admin routes, all declared in manifest.
[check-route-contract] OK — 58 page routes, all declared in the manifest.
[check-planning-hygiene] OK — 6303 tracked files scanned, none carry the local username or an absolute home path.
```

0 errors; the 2 warnings are the known pre-existing pair in files this plan never touched.

### gitleaks — the DELTA against base `9636ad77` is ZERO

Measured as a SET DIFF over the JSON reports, not by comparing two totals (equal counts could hide a swap):

```
BASE 9636ad77 : 2
    generic-api-key|scripts/prod-prober/arms/cron-drift.mjs|0123456789ab
    generic-api-key|scripts/prod-prober/run.mjs|0123456789ab
HEAD          : 2
    generic-api-key|scripts/prod-prober/arms/cron-drift.mjs|0123456789ab
    generic-api-key|scripts/prod-prober/run.mjs|0123456789ab
DELTA added   : 0 []
DELTA removed : 0 []
```

Local gitleaks is **8.30.1**, the CI version. `.gitleaks.toml` was NOT touched (D4/Q3). The two hits are the pre-existing pair wave 5 recorded; see "Deferred issues".

### `cmp` — both named artifacts identical to their post-guard pre-matrix state

```
cron-drift.mjs  cmp IDENTICAL
run.mjs         cmp IDENTICAL
$ git diff --stat -- scripts/ src/     # working tree == HEAD
(empty)
```

### Scope fence

```
$ git diff --stat main...HEAD -- supabase/            | wc -l   => 0
$ git status --short -- supabase scripts/prod-prober/cron-manifest.json | wc -l   => 0
$ git diff --name-only main...HEAD -- supabase/       | wc -l   => 0
```

Zero `supabase/` changes and the committed manifest untouched.

## The CR-03 and SR-02 reproductions — RUN at HEAD, output pasted

### CR-03's "Proven" command, VERBATIM

Exactly as `164.7-REVIEW.md:180-184` prints it, `sk_live_` token and all — **shell only**; the credential-shaped spelling is not written to any file in this public repo.

```
[
  '[x-service-key-literal] job match_engine_cron passes an X-Service-Key header as an inline quoted
   literal — this is the exact pre-2026-09-01 shape that kept a live analytics key inside
   cron.job.command for months. Resolve it from vault.decrypted_secrets at execution time instead.',
  '[vault-absent] job match_engine_cron does not reach a Vault read that EXECUTES: neither the command
   itself nor any public function it calls reads FROM vault.decrypted_secrets in its committed body.
   Naming the table in a comment or inside a string literal is not reading it. The command calls
   jsonb_build_object, which has no file in the committed function snapshot, so it could not be shown
   to read Vault either — an unknown callable is never assumed to be a reader. …'
]
```

**Before this phase: `[]` — zero of ten rules.** After wave 4: `1`. **Now: `2`** — which is exactly the number wave 4 recorded as *"what the fully-repaired rule set should return"*. Weakness (2), the split key, was closed by wave 3/4; weakness (1), the `--` comment satisfying `vault-absent`, is closed by wave 5's redesign and confirmed here.

### SR-02 (a) — the plan's own verify one-liner, VERBATIM

The `k := '<key>'` variable shape (bypass (h)) with a `FAKE-…` digit-bearing value:

```
["[long-token-anywhere]","[vault-absent]"]
violations: 2      exit=0   (an EMPTY list would exit 1)
```

### SR-02 (b) — the review's FULL measured shape

Variable indirection **plus** `RAISE NOTICE 'reads vault.decrypted_secrets'` — the literal-only mention. The review measured `[]` for this and wrote *"All ten rules clean on a command inlining a live key."* The `FAKE-` token is 40 characters, the same length as the review's `sk_live_AAAA…HHHH`, assembled from short operands (Q3):

```
token length: 40 — the review shape sk_live_AAAA…HHHH is 40
violations: 2
  [long-token-anywhere] job match_engine_cron carries a 32+ character whitespace-delimited token
                        containing a digit inside a string literal, outside any h…
  [vault-absent] job match_engine_cron does not reach a Vault read that EXECUTES: neither the command
                 itself nor any public function it calls reads…
and NO verdict quotes the token it judged (V8 holds)
exit=0
```

### SR-02's SECOND-ORDER HARM — the FALSE-POSITIVE direction, also closed

The review measured that a `--` inside a literal deleted the remainder of a one-line command, producing a **false** `[vault-absent]` that withheld the drift diff for the flagship job:

```
PERFORM set_config('x','a--b',true); SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'k'
violations: []
✅ NO false vault-absent — the '--' inside a literal no longer eats the real Vault read
```

## The Node-22 note

⛔ **CI (Node 22) is the FIRST run of this plan's checks under Node 22.** No Node 22 exists on this box:

```
local node          : v25.8.1  (/opt/homebrew/bin/node)     — satisfies engines >=22
.nvmrc              : 22
package.json engines: {"node":">=22"}
nvm                 : NOT INSTALLED
node22 on PATH      : NOT FOUND
other node on box   : /usr/local/bin/node v19.2.0            — BELOW engines >=22
```

Everything above was measured under **v25.8.1**.

## Decisions Made

1. **Rows 1 and 12 were NARROWED from the plan's letter, because the plan's neuters are batch neuters.** `VAULT_READ_RE` has THREE call sites (`:711` the resolver's body test, `:737` the span shape test, `:888` the token rule's `vaultSpan`) and `DO_PREFIX` has TWO (`:344` `codeSpans`' re-entry, `:431` `literalsIn`'s skip). Blanking either constant disables several controls in ONE edit — and one of `VAULT_READ_RE`'s victims is **row 4's own control**, so the matrix would have credited one RED to two arms, which is precisely the shape D3 forbids and which this phase's history shows 9+ times. Each row now neuters the call site the plan NAMES.
2. **Row 5's neuter points `FUNCTIONS_DIR` at a directory that EXISTS but lacks the tick function.** The plan says "a wrong directory name". Measured: an ABSENT directory makes `reachesVaultRead` throw, and scenario 46 calls `compareManifest` outside `runProber`'s try/catch, so the self-test **CRASHES at 46** and scenarios 47–69 never run — including scenario 52, the observer the plan names for this row. A row whose evidence is "the process died before reaching the observer" proves less, not more. The narrower neuter produces no throw, no truncation and no cascade, and exactly the two observers the plan predicts. (The absent-directory throw is a different control with its own scenario (53), neuter-proved by wave 5's N3.)
3. **`bash -c`, never `bash -lc`.** See Deviations 1.
4. **The pin row's `header_region_only` obligation is a ROW-granularity predicate**, because at scenario granularity the question is unanswerable.
5. **PREDICTED and MEASURED observers are declared separately.** `expect` holds what was written before the first run and is checked by `missing`; `expectCascade` holds the whole-arm blast radius of rows 1 and 12 and is checked by `others`. Both are exact-matched — a declared cascade entry that fails to fire is reported as `missing` too, so the list cannot go stale silently. The provenance is recorded rather than blurred, because a transcribed expectation is weaker evidence than a predicted one and the SUMMARY should say which is which.

## Deviations from Plan

### 1. [Rule 1 - Bug] The harness ran the whole first matrix under Node **19.2.0**, not the repo's Node

- **Found during:** Task 1, while diagnosing why the two `vitest` rows reported "reporter produced no readable JSON".
- **Issue:** the harness spawned runners with `bash -lc`. A **login** shell on this box sources a profile that puts `/usr/local/bin` ahead of Homebrew, resolving `node` to **v19.2.0** — below the repo's own `engines: ">=22"`. It surfaced only because `rolldown` needs `node:util`'s `styleText` (Node ≥ 20.12) and crashed with a *Startup Error*; the **self-test** did not crash, so all twelve self-test rows had silently been measured on the wrong runtime and read as perfectly normal REDs.
- **Fix:** `bash -c` throughout, inheriting the session PATH (v25.8.1). Verified by printing `node -v` under all three invocation forms.
- **Verification:** both `vitest` rows now report real assertion names; the full matrix was re-run from scratch.
- **Why it matters beyond this plan:** a green-looking measurement taken on the wrong runtime is the exact failure class this phase exists to eliminate — and the only thing that exposed it was a *second* runner in the same harness failing loudly.

### 2. [Rule 1 - Bug] Two of the plan's prescribed neuters are batch neuters

- **Found during:** Task 1 and Task 2, before trusting the first results.
- **Issue / Fix / Verification:** full account in Decisions 1. Narrowing row 1 also produced its own evidence: scenario 52, which red under the broad constant neuter, stays **GREEN** under the span-only one, because 52's command resolves purely through the callable path. **Two controls the shared constant blanked together are now separable** — which is what D3 asks for, demonstrated rather than asserted.
- **Files modified:** none (harness only).

### 3. [Rule 1 - Bug] Row 5's prescribed neuter truncates the run before its own observer

- Full account in Decisions 2. Recorded rather than worked around: **a whole-arm neuter that throws cannot observe anything past its crash point**, so a matrix row of that shape must be re-aimed, not re-interpreted.

### 4. [Scope — the routed residual, taken deliberately] The guard the plan's letter forbids

- **Found during:** Task 2, cross-reading wave 4's accepted residual 1 and broken-windows entry 1, both of which name **Plan 06** as the owner on the reasoning that "Plan 06 … already edits `run.mjs`" — which this plan does not, since its own `must_haves` freeze both files.
- **Decision:** taken. It is a one-line assertion inside an EXISTING scenario, so it adds no scenario, fixture or test (the plan's actual prohibition); it is the routed destination and no later plan owns it (Plan 07 is the linter half); and leaving it would be the fifth wave to record the same limit. Committed **separately and BEFORE** the matrix was re-run from scratch, per wave 4's ordering.
- **Verification:** the gap measured first (69/69 green with the value shortened); three branches each neuter-proved; `SELF_TEST_SCENARIOS` unchanged at 69.
- **Committed in:** `97bbba30`.

### 5. [Prediction error, recorded not hidden] Six of twelve declared sets were wrong on the first run

- The harness reported `missing`/`others` on rows 1, 5, 7, 8, 9 and 12. **None was a defect in the code under test**; all six were my predictions being wrong, and the harness catching them is criterion 2 doing its job.
  - Rows **8** and **7**: genuinely derivable and I did not derive them — removing the digit test also fires the token rule on the `pg-password` red row; raising `TOKEN_MIN` lifts it above the (h) and isolation fixture tokens. Both are now declared in `expect`.
  - Row **9**: the plan itself predicts an OR ("the FP-budget scenario **or** `fixture_bare_url_job` in hygiene GREEN"). Measurement picks the GREEN arm only. Declared as measured; the OR is recorded here rather than kept as a set membership that could never be exact.
  - Rows **1** and **12**: a genuine whole-arm cascade, now in `expectCascade` with its provenance labelled MEASURED-THEN-DECLARED.
  - Row **5**: fixed by re-aiming the neuter (Deviations 3), not by re-declaring.
- ⚠️ **Stated plainly so the evidence is not overclaimed:** the load-bearing prediction result is `missing = []` on rows 1 and 12 for their DIRECT observers on the very first run — those were predicted. Their 19- and 22-entry cascade lists were measured and then written down, which is weaker, and they are kept in a separate field for exactly that reason.

---

**Total deviations:** 3 auto-fixed bugs (all in the harness, all found before their results were trusted) + 1 deliberate scope decision + 1 recorded prediction error.
**Impact on plan:** no scope creep; the only production change is the residual wave 4 routed here. Three of the five entries exist because the matrix was probed instead of believed, which is this phase's entire subject.

## Known Stubs

None. This plan ships one assertion, and that assertion is neuter-proved in all three of its branches.

## Threat Flags

None. No network endpoint, auth path, file-access pattern or schema moved; the only change is one assertion in a self-test scenario over a committed fixture. `T-164.8.5-30` (a control credited by a batch neuter) was the live risk and is mitigated twice over — by the harness invariant and by catching the plan's own two batch neuters. `T-164.8.5-31` (restore via `git checkout --`) never occurred: every restore is `cp` + `cmp`. `T-164.8.5-32` (the `TOKEN_MIN` pin) is proved to bite by row 7.

## Issues Encountered

- **`gsd-tools windows append` REFUSES**, for the sixth wave running, and it did NOT write:
  ```
  Error: Ledger counts disagree with entries: frontmatter open/waived/fixed/total=36/0/11/47
  but entries yield 40/0/10/50.
  ```
  Byte-identical counts to wave 5's reading. Pre-existing, booked as `[WINDOWS-LEDGER-DRIFT]`, routed to 164.8.4. Not touched — hand-appending would deepen it. The entries this plan would have filed:
  1. **`deviation` · `scripts/prod-prober/arms/cron-drift.mjs`** — **nothing in CI enforces that the hygiene controls are non-vacuous.** Twelve rows ran here from the session scratchpad; the harness is still not in the repo. **Fifth wave running to record this**, and this wave is fresh proof it matters: the plan's own prescribed neuters contained two batch neuters and one that crashed before its observer, none of which any CI gate would have caught.
  2. **`deviation` · `scripts/prod-prober/run.mjs`** — the matrix's runtime is unpinned. A login shell on a developer box resolves Node 19.2.0, below `engines: ">=22"`, and the prober's self-test runs happily on it.

## Deferred Issues

- **Two PRE-EXISTING gitleaks hits under `scripts/prod-prober/`**, both on `'FAKE-key-' || '0123456789ab'` (`arms/cron-drift.mjs`, `run.mjs`). Proved pre-existing again here by a set diff against `9636ad77`'s copies: **added 0, removed 0**. NOT fixed — out of scope (only issues this plan's changes caused). ⛔ The fix is to split the operand further in those two places, never to widen `.gitleaks.toml` (D4/Q3). Worth closing before ship, since CI's gitleaks scans the PR's commit range.
- **`/usr/local/bin/node` is v19.2.0**, below the repo's declared `engines: ">=22"`, and is what a login shell selects on this box. Not this plan's to fix, but it is how deviation 1 happened and it will happen again to the next harness.
- **The matrix harness is still not in the repo and not in CI.** Recorded above as ledger entry 1.

## Estimate calibration

`estimate.tokens` was **50,000** at `confidence: low`. `actuals.tokens` is **603** on the template's stated scale — chars/4 over the realized diff (2,415 added characters in `97bbba30`). The gap is ~80×, and it is not an estimating error so much as a **scale mismatch that this plan makes unusually visible**: a pure-verification plan does almost no writing by design (both named artifacts must end `cmp`-identical), so a chars-of-diff scale measures nearly nothing, while the actual work — twelve neuter/restore cycles, three harness probes, four reproductions and two full matrix runs — is entirely reads and runs. Reported as measured, not rounded toward the estimate. ⚠️ **The calibration signal for the planner is that `estimateTokens` should not be applied to verification-only plans at all**, or it will keep predicting ~50k against a realized ~0.6k and the ratio will read as chronic over-estimation when it is a category error.

## User Setup Required

None — no external service configuration, no credential, no migration.

## Next Phase Readiness

- **The prober half of CONTEXT's "What done looks like" is complete.** Each new control is neuter-proved individually; the PROD credential scan's four broken-oracle states are wave 1's and still green; `vault-absent` accepts the 164.5.1 command against the real snapshot while rejecting the comment-only and literal-only spellings. **Plan 07 finishes the linter half.**
- **Plan 07 should adopt this plan's harness corrections, not just wave 4's standard**: `bash -c` for the runners, a neuter aimed at a CALL SITE rather than a shared constant, and a check that the neuter did not truncate the run before its own observer.
- **164.5.1 remains unblocked on the prober's side and is re-measured here** — scenario 52 is green at HEAD, and row 5 proves that green is a real resolution against the real snapshot rather than a rule that accepts everything. ⛔ 164.5.1 still owes the manifest re-capture after its repoint; this plan changed no rule at all and left `cron-manifest.json` byte-identical.
- **164.8.6 must still know that scenario 52 is coupled to it on purpose** — removing the Vault read from `match_engine_cron_tick`'s body, or editing that function without running `npm run schema:functions`, reds it. Row 5 is the neuter proof that the coupling is live.
- ⚠️ **`T-164.7-07` is now falsifiable-by-construction rather than asserted**, which is what the ship gate needs from this phase. `T-164.7-08` is untouched and remains 164.8.6's.

## Self-Check: PASSED

- `scripts/prod-prober/run.mjs` — FOUND, modified, `cmp`-identical to its post-guard pre-matrix backup
- `scripts/prod-prober/arms/cron-drift.mjs` — FOUND, **unmodified**, `cmp`-identical to its pre-matrix backup
- commit `97bbba30` — FOUND in `git log --oneline --all`
- `git status --short -- .planning/STATE.md .planning/ROADMAP.md` → **0** (the orchestrator owns both; neither was touched or committed by this plan)
- `git status --short -- supabase scripts/prod-prober/cron-manifest.json` → **0**
- the self-test re-runs GREEN at 69/69, exit 0, at final HEAD

The one untracked file in the tree, `.planning/STOPPING-RULE-PROPOSAL.md`, predates this plan and is not mine — left untouched.

---
*Phase: 164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab*
*Completed: 2026-09-11*
