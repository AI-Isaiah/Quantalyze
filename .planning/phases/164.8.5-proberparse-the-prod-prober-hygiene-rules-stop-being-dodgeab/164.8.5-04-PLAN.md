---
phase: 164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab
plan: 04
type: execute
wave: 4
depends_on:
  - 164.8.5-03
files_modified:
  - scripts/prod-prober/arms/cron-drift.mjs
  - scripts/prod-prober/run.mjs
  - scripts/prod-prober/fixtures/cron-drift/hygiene-red.json
  - scripts/prod-prober/fixtures/cron-drift/hygiene-green.json
  - scripts/prod-prober/fixtures/cron-drift/hygiene-bypass.json
  - scripts/prod-prober/fixtures/cron-drift/functions/
  - src/__tests__/prod-prober-wiring.test.ts
autonomous: true
requirements:
  - 164.7-CR03-HYGIENE-BYPASS

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "vault-absent (jobname match_engine_cron) fires unless the command REACHES an executing Vault read: the shape /\\b(?:FROM|JOIN)\\s+vault\\.decrypted_secrets\\b/i on some code span's masked text, OR a call (on masked text, so not inside a comment or literal) to a public/unqualified function whose committed dollar-body in the functions snapshot contains that shape, transitively (visited set, depth cap 5). Unknown name or missing file → the rule FIRES naming the callable; missing snapshot directory → throws (runner measure-fail). Criterion 2, D2."
    - "SELECT public.match_engine_cron_tick(); resolves against the REAL snapshot and is ACCEPTED, measured every self-test run — the 164.5.1 contract; a comment-only, literal-only and dollar-literal-only mention each REJECT."
    - "long-token-anywhere: a whitespace-delimited token inside any literal on any code span, OUTSIDE header regions, with length >= 32 AND at least one digit AND not a bare URL, and not already claimed by jwt-shape, fires — catching (h) variable indirection and (i) whole-header JSON; the 14 committed manifest commands produce ZERO violations from EVERY rule (false-positive budget measured to 0 by RESEARCH Q4, now asserted every run)."
    - "The Q2 scoping decision is PROVEN, not assumed: a 32+ digit-bearing token that lands only inside a header region fires long-literal-in-headers (token >= 32 implies literal >= 32), so excluding header regions from the token rule loses nothing."
    - "Every id in HYGIENE_RULE_IDS has a red row; run.mjs and captureManifest carry no hand-typed rule count; SELF_TEST_SCENARIOS and the wiring test agree (criterion 9)."
  artifacts:
    - path: "scripts/prod-prober/arms/cron-drift.mjs"
      provides: "vault-absent redesign with injectable functionsDir; long-token-anywhere"
      contains: "long-token-anywhere"
    - path: "scripts/prod-prober/fixtures/cron-drift/functions/"
      provides: "a fixture snapshot: fixture_vault_reader.sql, fixture_wrapper.sql (transitive), fixture_comment_only.sql, fixture_literal_only.sql"
    - path: "scripts/prod-prober/run.mjs"
      provides: "tri-state vault-absent scenario on the fixture snapshot; REAL-snapshot 164.5.1 collision scenario; committed-manifest zero-FP scenario; count-free prose"
  key_links:
    - from: "hygieneViolations(jobname, command, { functionsDir })"
      to: "supabase/schema/functions/<name>.sql via extractFunctionDefs"
      via: "read-only node:fs, injected directory defaulting to FUNCTIONS_DIR — NEVER through seams (greenSeamCalls: 2 is pinned twice)"
      pattern: "functionsDir"
    - from: "scripts/prod-prober/run.mjs collision scenario"
      to: "supabase/schema/functions/match_engine_cron_tick.sql"
      via: "the REAL 164.5.1 command string SELECT public.match_engine_cron_tick(); resolved against the real snapshot dir"
      pattern: "match_engine_cron_tick"
    - from: "long-token-anywhere"
      to: "scripts/prod-prober/cron-manifest.json"
      via: "zero violations across all committed commands, asserted as a scenario"
      pattern: "cron-manifest.json"
---

<objective>
Finish the rule redesign on top of Plan 03's spans: `vault-absent` becomes "does this command reach
a Vault read that EXECUTES" (accepting the 164.5.1 tick call, rejecting comment/literal mentions),
and a token-level `long-token-anywhere` rule catches the two bypasses a normal developer writes
((h) variable indirection, (i) whole-header JSON) with a false-positive budget of ZERO on the
14 committed PROD commands.

Purpose: criterion 2 (the hardest — it serves two phases pulling opposite ways, and getting it
wrong silently un-blocks or re-blocks 164.5.1), CONTEXT's third family fix, RESEARCH Q4/Q5,
`[164.7-VAULT-ABSENT-RULE]`'s rule half.

Output: the two rules, the fixture snapshot directory, red/green/bypass rows for (h)/(i) and the
header-region proof, the tri-state + real-snapshot + zero-FP scenarios.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-CONTEXT.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-RESEARCH.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-03-SUMMARY.md
@scripts/sql-body-normalize.mjs
@scripts/prod-prober/arms/cron-drift.mjs
@scripts/prod-prober/run.mjs
@scripts/prod-prober/cron-manifest.json
@supabase/schema/functions/match_engine_cron_tick.sql
</context>

<decisions_honoured>
- D2 (LOCKED): one rule, two directions; executable read shape on masked OR resolvable committed body; this phase lands first, 164.5.1 consumes.
- Scope fence: `supabase/schema/functions/**` is READ, never written; `scripts/prod-prober/cron-manifest.json` is NOT re-captured or edited (164.5.1 re-captures after the repoint).
- Q2 decision (CONTEXT): scope the token rule to non-header regions; VERIFY it against the census — Task 2 ships the proof fixture.
- RESEARCH Q4 deviation recorded: the token+digit+bare-URL narrowing is what makes the rule usable on the real corpus; the Vault-read exemption CONTEXT named is kept as an additional exemption for the `match_engine_cron` job's own URL literal but is not load-bearing. A2 (real credentials carry a digit) is an accepted residual in the docstring.
- Q3 decision: `FAKE-…` digit-bearing strings; `.gitleaks.toml` untouched.
- S6 (PATTERNS): the snapshot read is plain `node:fs` through an injected directory — never `seams`.
- D3/D5 as in prior plans.
</decisions_honoured>

<tasks>

<task type="auto">
  <name>Task 1: vault-absent = "reaches an executing Vault read" — executable shape on spans OR a resolvable committed callable, injectable snapshot dir, fixture snapshot, tri-state + real-snapshot scenarios</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs, scripts/prod-prober/fixtures/cron-drift/functions/, scripts/prod-prober/fixtures/cron-drift/hygiene-red.json, scripts/prod-prober/fixtures/cron-drift/hygiene-green.json, src/__tests__/prod-prober-wiring.test.ts</files>
  <reversibility rating="costly">The rule's accept set is a contract Phase 164.5.1 will schedule PROD against (`SELECT public.match_engine_cron_tick();`); changing it later means re-checking a live cron row and the committed manifest together (`[164.7-VAULT-ABSENT-RULE]`).</reversibility>
  <read_first>
    - 164.8.5-RESEARCH.md Q5 — the measured resolvable set (120 files, exactly one executable reader, zero transitive edges), the five-step resolution algorithm, "fail closed" direction, the fixture-snapshot rationale (a 164.8.6 edit to the tick must not flip a 164.8.5 scenario), and "what this phase must NOT do" (the manifest).
    - scripts/sql-body-normalize.mjs `extractFunctionDefs(sql)` → `{ name, nargs, bodyKind: "dollar" | "none", body, hash, text }[]`; `readQualifiedName`'s identifier charset limitation (`[VAC04-C1]`) — a name outside `[A-Za-z0-9_]` is dropped; this arm only ever looks up names it extracted with the same charset, so the limitation cannot silently un-resolve a real call. Say so in the docstring.
    - scripts/prod-prober/arms/cron-drift.mjs `readManifestFile` + the `manifestPath` injection (S6) — the injection idiom to copy for `functionsDir`.
    - scripts/prod-prober/run.mjs `ARM_FIXTURE_TABLE` cron-drift `greenSeamCalls: 2` and its two assertions — why the snapshot read must not touch `seams`.
    - supabase/schema/functions/match_engine_cron_tick.sql — the `FROM vault.decrypted_secrets` line inside the `$function$` body; `@generated by scripts/dump-sql-functions.ts`.
    - scripts/prod-prober/fixtures/cron-drift/hygiene-red.json `vault-absent` row (`SELECT net.http_post(url := 'https://x.invalid/a', body := '{}'::jsonb)`, jobname `match_engine_cron`) — must STILL fire under the redesign: `net.http_post` is schema-qualified outside `public`, so it is never resolved.
  </read_first>
  <action>
**(a) Signature.** `hygieneViolations(jobname, command, { functionsDir = FUNCTIONS_DIR } = {})` with `export const FUNCTIONS_DIR = resolve(HERE, "..", "..", "..", "supabase", "schema", "functions")`. Thread `opts` through `compareManifest` (section (0) and the manifest loop) from `run()`, and through `captureManifest`; the default is the real snapshot.

**(b) The rule (jobname `match_engine_cron` only, as today).** `reachesVaultRead(spans, functionsDir)`: (1) if any span's `masked` matches `VAULT_READ_RE = /\b(?:FROM|JOIN)\s+vault\.decrypted_secrets\b/i` → true. (2) Else collect calls on each span's `masked` with `/(?:\b([A-Za-z_][A-Za-z0-9_]*)\.)?\b([A-Za-z_][A-Za-z0-9_]*)\s*\(/g`; keep only unqualified or `public.`-qualified names; drop SQL keywords that precede `(` (`IF`, `VALUES`, `IN`, `EXISTS`, `COALESCE`, `NULLIF` and the like — a small denylist; an unknown keyword merely resolves to "no file" and fires, which is the loud direction). (3) `resolveCallable(name, functionsDir, visited, depth)`: if `!existsSync(functionsDir)` throw `Error("functions snapshot directory <dir> is absent — the callable resolver cannot measure")` (the runner maps a throw to `measure-fail`; a missing snapshot is not "no Vault read"); if `<dir>/<name>.sql` does not exist → return `{ reads: false, unresolved: name }`; else `extractFunctionDefs(readFileSync(...))`, for each def with `bodyKind === "dollar"` test `scanSql(body).masked` against `VAULT_READ_RE`; if none, recurse into calls found in each body's masked text (visited set, `depth > 5` → treat as not resolved). Any reader counts (overloads share one file). (4) Fire `vault-absent` when nothing reaches a read; the sentence names the unresolved callable NAME(s) if any (a function name, never a value) and states that the judgement is against the COMMITTED body (`supabase/schema/functions/`), not PROD's — VAC-04 owns that direction.

**(c) Fixture snapshot `fixtures/cron-drift/functions/`** — four files in the `@generated` shape: `fixture_vault_reader.sql` (dollar body with `FROM vault.decrypted_secrets`), `fixture_wrapper.sql` (dollar body `PERFORM public.fixture_vault_reader();` — the transitive edge), `fixture_comment_only.sql` (body mentions the table only in a `--` comment), `fixture_literal_only.sql` (body has `RAISE NOTICE 'reads vault.decrypted_secrets'`). Guard it like every fixture: a missing file is a FAIL (S2).

**(d) Scenarios.** (1) "vault-absent TRI-STATE on the fixture snapshot": with `{ functionsDir: <fixture dir> }` assert ACCEPT for `SELECT public.fixture_vault_reader();`, ACCEPT for `SELECT fixture_wrapper();` (transitive), REJECT for `SELECT public.fixture_comment_only();`, REJECT for `SELECT public.fixture_literal_only();`, REJECT for `SELECT public.fixture_unknown();` with the sentence containing `fixture_unknown`, REJECT for `SELECT 1 -- vault.decrypted_secrets`, REJECT for `SELECT 'FROM vault.decrypted_secrets'`, REJECT for `SELECT $q$FROM vault.decrypted_secrets$q$`, ACCEPT for `DO $$ BEGIN PERFORM 1 FROM vault.decrypted_secrets; END $$` (span recursion). Each reject must include `vault-absent` and no sentence may include the command. (2) "vault-absent against the REAL snapshot: the 164.5.1 command SELECT public.match_engine_cron_tick(); is ACCEPTED" — default `functionsDir`; assert zero `vault-absent`; this is the `[164.7-VAULT-ABSENT-RULE]` collision measured every run, and its comment must say a RED here means 164.8.6 removed the tick's Vault read or the snapshot was not regenerated. (3) "a MISSING functions snapshot is a measure-fail, never a clean vault-absent" — run `driftRun`-style with `functionsDir` pointed at a nonexistent path through `runProber`'s arm options (add a `functionsDir` pass-through on `run({ … })` exactly like `manifestPath`), assert exactly one `measure-fail` and ZERO `cron-secret-in-command` via `noDefectOfKind`. Keep the `hygiene-red` `vault-absent` row unchanged and confirm it still isolates.

**(e) Wiring pin.** Add to the wiring test a bidirectional check that `FUNCTIONS_DIR` resolves to an existing directory containing `match_engine_cron_tick.sql` — so a rename of the snapshot dir reds the pin before it silently fires `vault-absent` hourly. D5 bump from the tail.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; grep -q 'vault-absent TRI-STATE' scripts/prod-prober/run.mjs &amp;&amp; grep -q 'against the REAL snapshot' scripts/prod-prober/run.mjs &amp;&amp; test -f scripts/prod-prober/fixtures/cron-drift/functions/fixture_wrapper.sql &amp;&amp; node --input-type=module -e 'import {hygieneViolations as h} from "./scripts/prod-prober/arms/cron-drift.mjs"; const has=(c,o)=>h("match_engine_cron",c,o).some(x=>x.startsWith("[vault-absent]")); const fx={functionsDir:"scripts/prod-prober/fixtures/cron-drift/functions"}; const ok=!has("SELECT public.match_engine_cron_tick();")&amp;&amp;has("SELECT 1 -- vault.decrypted_secrets")&amp;&amp;has("SELECT \x27FROM vault.decrypted_secrets\x27")&amp;&amp;!has("SELECT fixture_wrapper();",fx)&amp;&amp;has("SELECT public.fixture_comment_only();",fx)&amp;&amp;has("SELECT public.fixture_unknown();",fx); process.exit(ok?0:1)' &amp;&amp; test "$(git status --short -- supabase scripts/prod-prober/cron-manifest.json | wc -l | tr -d ' ')" = 0 &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed'</automated>
  </verify>
  <done>The tick call is accepted against the real snapshot; comment-only, literal-only, dollar-literal-only and unknown-callable commands are rejected; the wrapper resolves transitively; a missing snapshot dir is a measure-fail; the committed manifest and the snapshot are unchanged. Failing direction: reverting to `includes()` accepts the comment-only command and the inline check exits 1; making the resolver return `reads: true` for a missing file accepts `fixture_unknown` and exits 1.</done>
</task>

<task type="auto">
  <name>Task 2: long-token-anywhere with a measured zero-FP budget — shapes (h) and (i), the header-region scoping proof, and the committed-manifest scenario</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs, scripts/prod-prober/fixtures/cron-drift/hygiene-red.json, scripts/prod-prober/fixtures/cron-drift/hygiene-bypass.json, scripts/prod-prober/fixtures/cron-drift/hygiene-green.json, src/__tests__/prod-prober-wiring.test.ts</files>
  <reversibility rating="reversible">A rule id inside the arm; removing it is one entry in `HYGIENE_RULE_IDS` and one red row.</reversibility>
  <read_first>
    - 164.8.5-RESEARCH.md Q4 — the FP measurement (a literal-level rule fires on 5 of 14 real jobs at every threshold to 64), the token census (every ≥ 24-char token in the real corpus; NONE carries a digit; the one URL is bare), the rule as narrowed (token ≥ 32 AND a digit AND not a bare URL `^https?:\/\/[A-Za-z0-9.-]+(?::\d+)?(?:\/[A-Za-z0-9._~\/-]*)?$`), the `jwt-shape` collision (the `eyJ…` row would fire two rules) and A2.
    - 164.8.5-CONTEXT.md Q2 decision — scope the rule to NON-header regions; the planner must VERIFY that a bypass landing only inside a header region is caught by `long-literal-in-headers`. The argument: a whitespace-delimited token of length ≥ 32 lives inside a literal of length ≥ 32, and `long-literal-in-headers` fires on ANY literal ≥ `HEADERS_LITERAL_MAX` (32) in a header region now that Plan 03 reads dollar/E/U& literals off `masked`. So the two rules partition the command by region with no gap. Ship the proof fixture; do not rely on the argument alone.
    - scripts/prod-prober/cron-manifest.json — the 14 committed commands (jobid 1 is the `DO $body$` Vault-reading form, 546 chars); every row must remain at zero violations from every rule.
    - scripts/prod-prober/run.mjs `captureManifest` docblock ("ANY of the ten hygiene rules", "those ten rules") — count-free rewording.
  </read_first>
  <action>
**(a) Rule.** Append `long-token-anywhere` to `HYGIENE_RULE_IDS`. On every span: for each literal from `literalsIn` whose range does NOT intersect any header region of that span, split `content` on `/\s+/`; a token fires when `length >= 32 && /\d/.test(token) && !BARE_URL_RE.test(token) && !/eyJ[A-Za-z0-9_-]{20,}/.test(token)` (the last: already claimed by `jwt-shape`, so the two rules never double-report). Additional exemption per CONTEXT: skip the literal when the same span's masked text carries `VAULT_READ_RE` AND the token is a bare URL — redundant with the URL test, kept explicit and documented as such. Docstring: the FP measurement, the census, A2 as accepted residual, and the region partition argument.

**(b) Fixtures.** `hygiene-red.json`: one row `long-token-anywhere`, jobname `fixture_var_key_job`, `DO $$ DECLARE k text; BEGIN k := 'FAKE-0123456789-0123456789-0123456789ab'; PERFORM net.http_post(url := 'https://x.invalid/a', headers := jsonb_build_object('X-Service-Key', k), body := '{}'::jsonb); END $$` — fires ONLY this id (the header value is a variable, so no header rule fires). `hygiene-bypass.json`: (h) the same shape, (i) `DO $$ DECLARE h jsonb; BEGIN h := '{"X-Service-Key":"FAKE-0123456789-0123456789-01"}'::jsonb; PERFORM net.http_post(url := 'https://x.invalid/a', headers := h, body := '{}'::jsonb); END $$`, and the scoping PROOF row `header_region_only`: `SELECT net.http_post(url := 'https://x.invalid/a', headers := jsonb_build_object('X-Trace', 'FAKE-0123456789-0123456789-0123456789'), body := '{}'::jsonb)` with `expect_any_of: ["long-literal-in-headers"]` AND a new optional field `expect_not: ["long-token-anywhere"]` asserted by the BYPASS scenario — this row is what makes the Q2 decision a measurement. `hygiene-green.json`: add `fixture_raise_prose_job` (`DO $$ BEGIN RAISE NOTICE 'retention_notification_dispatches: nothing to do for this window, orphaned_running_reaped:%', 0; END $$`) and `fixture_bare_url_job` (`SELECT net.http_post(url := 'https://quantalyze-analytics-production.up.railway.app/api/match/cron-recompute-0123456789')`) so the census's two near-misses are calibrated green.

**(c) Committed-manifest scenario** "the COMMITTED cron manifest passes every hygiene rule — zero violations on all rows (the false-positive budget)": load `scripts/prod-prober/cron-manifest.json` (S2: missing = FAIL), for every row with a string `command` call `hygieneViolations(row.jobname, row.command)` with the default snapshot dir, assert the flagged list is empty and that at least 10 rows were judged (a manifest with fewer rows than that is not the oracle this scenario means). If this scenario is RED at execution time, the rule is wrong — narrow the rule, never edit the manifest.

**(d) Count-free prose.** Reword `captureManifest`'s docblock and any remaining "ten rules" in `run.mjs` and `cron-drift.mjs` to "every rule in HYGIENE_RULE_IDS". D5 bump from the tail; wiring-test CALIBRATION: `BARE_URL_RE` (export it) rejects `https://x.invalid/a?token=1`, `https://u:p1@x.invalid/` and accepts the railway URL — and a mutated regex with the `?` allowed flips the first.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; grep -q 'false-positive budget' scripts/prod-prober/run.mjs &amp;&amp; node --input-type=module -e 'import {hygieneViolations as h,HYGIENE_RULE_IDS} from "./scripts/prod-prober/arms/cron-drift.mjs"; import {readFileSync} from "node:fs"; const ids=(j,c)=>h(j,c).map(x=>x.slice(1,x.indexOf("]"))); const m=JSON.parse(readFileSync("scripts/prod-prober/cron-manifest.json","utf8")); const fp=m.jobs.filter(r=>typeof r.command==="string"&amp;&amp;h(r.jobname,r.command).length>0).map(r=>r.jobname); const hI=ids("j","DO $$ DECLARE k text; BEGIN k := \x27FAKE-0123456789-0123456789-0123456789ab\x27; PERFORM net.http_post(headers := jsonb_build_object(\x27X-Service-Key\x27, k)); END $$"); const iI=ids("j","DO $$ DECLARE hh jsonb; BEGIN hh := \x27{\"X-Service-Key\":\"FAKE-0123456789-0123456789-01\"}\x27::jsonb; PERFORM net.http_post(headers := hh); END $$"); const hdr=ids("j","SELECT net.http_post(headers := jsonb_build_object(\x27X-Trace\x27, \x27FAKE-0123456789-0123456789-0123456789\x27))"); const ok=fp.length===0&amp;&amp;hI.includes("long-token-anywhere")&amp;&amp;iI.includes("long-token-anywhere")&amp;&amp;hdr.includes("long-literal-in-headers")&amp;&amp;!hdr.includes("long-token-anywhere")&amp;&amp;HYGIENE_RULE_IDS.includes("long-token-anywhere"); if(!ok)console.error({fp,hI,iI,hdr}); process.exit(ok?0:1)' &amp;&amp; test "$(git status --short -- supabase scripts/prod-prober/cron-manifest.json | wc -l | tr -d ' ')" = 0 &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed'</automated>
  </verify>
  <done>(h) and (i) fire `long-token-anywhere`; the header-region-only token fires `long-literal-in-headers` and NOT the token rule (Q2 decision measured); all 14 committed commands are clean under every rule; no hand-typed rule count remains. Failing direction: dropping the digit test makes the committed manifest's RAISE prose fire and the inline check prints the job names and exits 1; dropping the region exclusion makes `hdr` include the token rule and exits 1.</done>
</task>

<task type="auto">
  <name>Task 3: Individual neuter matrix for vault-absent and long-token-anywhere; phase-level checks</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs</files>
  <read_first>
    - 164.8.5-01-SUMMARY.md — the harness.
    - 164.8.5-CONTEXT.md "What done looks like" — the phase's acceptance sentence; this task closes the prober half of it.
  </read_first>
  <action>
One control per run, byte backup, `cmp` restore:
| control | neuter | expected RED |
| executable shape on spans | make `VAULT_READ_RE` never match | `hygiene GREEN` naming `match_engine_cron` (the flagship DO body) AND the TRI-STATE `DO $$ … FROM vault…` accept |
| resolver: missing file fires | return `{ reads: true }` for a missing file | TRI-STATE `fixture_unknown` reject |
| resolver: transitive edge | depth cap 0 | TRI-STATE `fixture_wrapper` accept |
| resolver: comment/literal in body | test the body's raw text instead of `scanSql(body).masked` | TRI-STATE `fixture_comment_only` / `fixture_literal_only` rejects |
| real-snapshot collision | point the default `FUNCTIONS_DIR` at a wrong directory name | the wiring `FUNCTIONS_DIR` pin AND (via the throw) the REAL snapshot scenario |
| token length | threshold 32 → 999 | `hygiene RED` naming the `long-token-anywhere` row |
| token digit test | remove `/\d/` | the committed-manifest false-positive-budget scenario (RAISE prose fires) |
| bare-URL exemption | remove it | the false-positive-budget scenario (jobid 1's railway URL) or `fixture_bare_url_job` in `hygiene GREEN` |
| header-region exclusion | scan header regions too | `hygiene BYPASS` `header_region_only` row's `expect_not` |
| jwt exemption | remove it | `hygiene RED` isolation on the `jwt-shape` row |
| codeSpans (re-proved) | `DO_PREFIX` never matches | `hygiene GREEN` naming `match_engine_cron` |

Then the phase-level prober checks: `node scripts/prod-prober/run.mjs --self-test` green; `npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/sql-body-normalize.test.ts src/__tests__/gitleaks-allowlist.test.ts` green; `gitleaks detect --no-git -c .gitleaks.toml --source scripts/prod-prober` clean if installed; re-run CR-03's exact reproduction command and the SR-02 measured command (`k := 'sk_live_…'` variable shape, with a `FAKE-…` digit-bearing value of the same length) and paste both violation lists into the SUMMARY. If Node 22 is available locally (`nvm ls 22` or `command -v node22`), run the self-test under it once and record the result; if not, record that CI (Node 22) is the first run.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/sql-body-normalize.test.ts src/__tests__/gitleaks-allowlist.test.ts 2>&amp;1 | grep -q 'Test Files  3 passed' &amp;&amp; test "$(git status --short -- supabase scripts/prod-prober/cron-manifest.json | wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <done>Eleven individual neuters, eleven REDs, restored byte-identical; CR-03's and SR-02's reproduction commands both return non-empty lists (pasted); the three vitest files pass; the manifest and `supabase/` are untouched. Failing direction: any green row blocks; a non-empty `git status` under `supabase/` is a fence breach and blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD `cron.job` text → resolver | a command may name a function to launder a Vault mention; the resolver reads only the committed snapshot |
| `supabase/schema/functions/` → arm | a hermetic, `@generated`, advisory-freshness snapshot; absence must not read as "clean" |
| arm → public log | violation sentences name function NAMES only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.5-16 | Information disclosure | `vault-absent` satisfied by a comment/literal (T-164.7-07 a) | high | mitigate | executable shape on masked spans; TRI-STATE rejects |
| T-164.8.5-17 | Spoofing | a call to an unknown/deleted function read as a Vault reader | high | mitigate | fail closed: unknown → fires naming the callable; missing dir → throw → measure-fail |
| T-164.8.5-18 | Tampering | 164.5.1's tick call re-blocked or silently un-blocked by a later snapshot edit | medium | mitigate | REAL-snapshot scenario measured every run; `FUNCTIONS_DIR` wiring pin |
| T-164.8.5-19 | Information disclosure | key via variable/JSON indirection (bypasses h, i) | high | mitigate | `long-token-anywhere`; red row; bypass rows |
| T-164.8.5-20 | Denial of service (alert fatigue) | rule fires hourly on RAISE prose/URLs | medium | mitigate | digit + bare-URL narrowing; committed-manifest zero-FP scenario |
| T-164.8.5-21 | Information disclosure | pure-alphabetic 32+ key evades the token rule (A2) | low | accept | recorded residual in the docstring; every credential shape this repo has recorded carries a digit |
| T-164.8.5-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Self-test green at the final prober N; three vitest files green; matrix of eleven REDs in the SUMMARY.
- CR-03 and SR-02 reproductions non-empty (pasted).
- `git status --short supabase scripts/prod-prober/cron-manifest.json` empty.
</verification>

<success_criteria>
Criterion 2 met in both directions with the 164.5.1 command measured against the real snapshot every run; all nine bypass shapes (a)–(i) fire a named rule; the FP budget on the committed manifest is zero by scenario; criterion 9 holds. The prober half of "What done looks like" is complete; Plan 05 finishes the linter half.
</success_criteria>

<output>
Create `.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-04-SUMMARY.md` when done, with the matrix, both reproduction outputs, the Node-22 note and the N.
</output>
