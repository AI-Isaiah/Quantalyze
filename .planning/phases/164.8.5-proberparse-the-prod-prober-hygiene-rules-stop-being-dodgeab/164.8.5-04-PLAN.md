---
phase: 164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab
plan: 04
type: execute
wave: 4
depends_on:
  - 164.8.5-03
files_modified:
  - scripts/prod-prober/arms/cron-drift.mjs
  - scripts/prod-prober/run.mjs
autonomous: true
requirements:
  - 164.7-CR03-HYGIENE-BYPASS
  - 164.7-REVIEW-INFO-FOUR

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every control Plan 03 added — codeSpans recursion, the anchored derived-length sum, the per-argument derived sum on long-literal-in-headers, the SELECT skip, dollar literals in header regions, the masked name anchor, header-unparseable, both new pg-password arms, and the measure-fail classification — has been disabled ALONE, from a byte backup, and produced a RED naming its own fixture or scenario, then restored to byte identity (D3)."
    - "No row disables more than one control; a row whose RED could be attributed to two controls is split before it is run."
    - "After the matrix, cron-drift.mjs and run.mjs are byte-identical to their pre-matrix state, both quick runs are green, and nothing under supabase/ moved."
  artifacts:
    - path: "scripts/prod-prober/arms/cron-drift.mjs"
      provides: "unchanged after the matrix — cmp-verified"
    - path: "scripts/prod-prober/run.mjs"
      provides: "unchanged after the matrix — cmp-verified"
  key_links:
    - from: "neuter.sh (scratchpad, Plan 01)"
      to: "node scripts/prod-prober/run.mjs --self-test / npx vitest run"
      via: "byte backup → single replacement (exit 2 if unchanged) → run → cp restore → cmp"
      pattern: "SELF-TEST"
---

<objective>
Prove, one control at a time, that every rule Plan 03 shipped can FAIL. This is the evidentiary
half of the lexer work, split from the implementation (plan-check W6) so the nine-plus-row matrix —
each row a full byte-backup / neuter / self-test / `cmp`-restore cycle — has its own budget. A
wave boundary here cannot corrupt the evidentiary chain because every row restores byte-identically
by construction.

Purpose: D3 for criteria 3 and 4, IN-04, `header-unparseable`, and the W4 coverage; CONTEXT's
"What done looks like" — "each new control has been neuter-proved individually".

Output: an eleven-row matrix with exit codes in the SUMMARY; the CR-03 reproduction output; both
quick runs green on a tree identical to Plan 03's end state.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-CONTEXT.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-01-SUMMARY.md
@.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-03-SUMMARY.md
@scripts/prod-prober/arms/cron-drift.mjs
@scripts/prod-prober/run.mjs
</context>

<decisions_honoured>
- D3 (LOCKED): one control per row, byte backup, `cmp` restore, never `git checkout --`.
- This plan ADDS no scenario, no fixture and no test: every control it neuters already has its RED surface from Plan 03 (including the name-anchor calibration, Plan 03 Task 2(e)). If a row has no RED surface, that is a Plan 03 defect — fix it THERE (re-open the plan, add the control, bump D5), never here mid-matrix.
- Scope fence: no `supabase/**` file is modified.
</decisions_honoured>

<revision_notes>
Plan-check 2026-09-10: this plan is the former plan 03 Task 3, peeled out per W6 (D-a). Rows added since the check: the per-argument `long-literal-in-headers` sum (W4 coverage). The name-anchor row no longer writes its own calibration (that moved to Plan 03 Task 2(e)); it only neuters.
</revision_notes>

<tasks>

<task type="auto">
  <name>Task 1: Neuter the lexer and header-rule controls, one per run — six rows</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs</files>
  <read_first>
    - 164.8.5-01-SUMMARY.md — the `neuter.sh` harness location and format (scratchpad, not repo).
    - 164.8.5-03-SUMMARY.md — the N at Plan 03's end and the scenario titles as printed.
    - 164.8.5-CONTEXT.md D3 and "What done looks like".
  </read_first>
  <action>
One control per run, byte backup, `cmp` restore, expected RED substring checked against the self-test output (or the wiring vitest output for the vitest row):
| control | neuter (ONE edit) | expected RED |
| codeSpans recursion | make `DO_PREFIX` never match (replace with `/$^/`) | the DERIVED LENGTH scenario's `codeSpans` span-count assertion (`spans.length === 2`) |
| anchored derived-length sum | make `derivedLiteralLength` return the FIRST literal's length only — for the anchored-id call site only | `hygiene RED` naming the `x-service-key-literal` split/concat/format/chr/decode/quote_literal rows |
| per-argument derived sum on `long-literal-in-headers` (W4) | make the `long-literal-in-headers` walk test single literals only (skip the per-argument sum) — the anchored call site stays live | `hygiene RED` naming `fixture_nonanchored_split_job` AND `hygiene BYPASS` row (j) |
| SELECT skip | descend into `(SELECT` | `hygiene GREEN` naming `fixture_bearer_concat_job` AND the DERIVED LENGTH scenario |
| dollar literals in header regions | drop `dollarRegions` from `literalsIn` | `hygiene BYPASS` pair (apostrophe row) and the shape-(a) row in `hygiene RED` |
| name anchor confirmed on masked | remove the `masked[m.index] === "'"` check | the wiring-test name-anchor CALIBRATION `it()` (run `npx vitest run src/__tests__/prod-prober-wiring.test.ts` for this row) |

Every row must be RED; a green row blocks. Record each row's exit code and the first RED line in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts 2>&amp;1 | grep -q 'Test Files  1 passed' &amp;&amp; test "$(git status --short -- supabase | wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <done>Six individual neuters, six REDs, six byte-identical restores, recorded; the tree is green afterwards. Failing direction: a green row is a control that cannot fail and blocks the plan; a `cmp` mismatch after restore blocks the plan.</done>
</task>

<task type="auto">
  <name>Task 2: Neuter header-unparseable, the IN-04 arms and the classification — five rows — then the CR-03 reproduction and the closing runs</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs, scripts/prod-prober/run.mjs</files>
  <read_first>
    - 164.7-REVIEW.md CR-03 "Proven" — the exact reproduction command whose ids get pasted into the SUMMARY.
    - scripts/prod-prober/arms/cron-drift.mjs section (0) / manifest loop classification split (Plan 03 Task 2(b)).
  </read_first>
  <action>
| control | neuter (ONE edit) | expected RED |
| header-unparseable | make `headerRegions` mark an unbalanced region `unparseable: false` with `to = span.masked.length` (the old silent widening) | `hygiene RED` (the unparseable row fires nothing) |
| pg-password `:=` arm | delete the masked `:=` test | `hygiene RED` naming the plpgsql row |
| pg-password URI arm | delete the URI regex | `hygiene RED` naming the URI row |
| measure-fail classification | classify everything as `cron-secret-in-command` | `UNPARSEABLE header region` scenario |
| unparseable region skipped by header walks | let the header-rule walks run over an unparseable region too | `hygiene RED` isolation on the unparseable row (a second id fires beside `header-unparseable`) |

Then: run CR-03's exact reproduction command (REVIEW CR-03 "Proven") through `hygieneViolations` and paste the NON-EMPTY id list into the SUMMARY. Closing runs: `node scripts/prod-prober/run.mjs --self-test` green; `npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/sql-body-normalize.test.ts` green (the normalizer's own suite must be untouched by the import); `cmp` both files against their pre-matrix backups; `git status --short supabase` empty.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test 2>&amp;1 | tail -1 | grep -q 'SELF-TEST PASSED' &amp;&amp; npx vitest run src/__tests__/prod-prober-wiring.test.ts src/__tests__/sql-body-normalize.test.ts 2>&amp;1 | grep -q 'Test Files  2 passed' &amp;&amp; node --input-type=module -e 'import {hygieneViolations as h} from "./scripts/prod-prober/arms/cron-drift.mjs"; const v=h("match_engine_cron","-- resolves from vault.decrypted_secrets\nDO $$ BEGIN PERFORM net.http_post(url := \x27https://x.invalid/a\x27, headers := jsonb_build_object(\x27X-Service-Key\x27, \x27FAKE-key-\x27 || \x270123456789ab\x27), body := \x27{}\x27::jsonb); END $$"); process.exit(v.length>0?0:1)' &amp;&amp; test "$(git status --short -- supabase | wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <done>Five more individual neuters (eleven in total across both tasks), each RED, each restored byte-identical; CR-03's reproduction returns a non-empty list (pasted); the normalizer suite still passes. Failing direction: any green row blocks; an EMPTY CR-03 reproduction list exits 1 and blocks — that is the arm's reason for existing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| neuter harness → working tree | a neuter that does not restore byte-identically corrupts the evidentiary chain for every later plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.5-30 | Repudiation | a control credited as proved by a batch neuter | high | mitigate | one control per row; a row with two attributable REDs is split before running |
| T-164.8.5-31 | Tampering | restore via `git checkout --` destroying uncommitted work | high | mitigate | byte `cp` backup + `cmp`; harness exits 2 on a no-op replacement |
| T-164.8.5-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Eleven-row matrix in the SUMMARY, every row RED with its exit code and first RED line.
- CR-03 reproduction ids pasted.
- Both files `cmp`-identical to their pre-matrix backups; both quick runs green.
</verification>

<success_criteria>
Every Plan 03 control is individually proved able to fail; the tree is byte-identical to Plan 03's end state; the normalizer suite is untouched.
</success_criteria>

<output>
Create `.planning/phases/164.8.5-proberparse-the-prod-prober-hygiene-rules-stop-being-dodgeab/164.8.5-04-SUMMARY.md` when done, with the matrix and the CR-03 reproduction output.
</output>
