---
phase: 164.5.1.3-syncadmit-admit-the-owner-only-status-to-the-trade-sync-cons
plan: 01
subsystem: api
tags: [trade-sync, cron, lifecycle-filter, supabase-migrate, pytest]

# Dependency graph
requires:
  - phase: 164.5.1.4-synccursor
    provides: per-strategy trade-sync resume marker machinery (_STRATEGY_SYNC_CURSOR_TABLE, _strategy_resume_point, _resume_floor_ms) that this phase's widening composes on top of without reopening SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01
provides:
  - ALLOWED_STRATEGY_STATUSES now admits the owner-only terminal status "private" (analytics-service/routers/cron.py), ending the five-key trade-drop measured on PROD 2026-09-19
  - A permanent, calibrated ordering gate (TestPrivateAdmissionOrderingGate) that fails CI if a future edit removes 164.5.1.4's marker machinery while private stays admitted
  - A composition proof (TestPrivateAdmissionComposesWithSyncCursor) that a failed private strategy's marker holds while its key's cursor still advances
  - TODOS.md ledger honestly re-scoped against a live gh re-check of 164.5.1.4's PROD-apply gate (still waiting on human reviewer approval as of 2026-09-20)
affects: [ship/merge decision for feat/164.5.1.3-syncadmit, any future cron.py lifecycle-filter change, SYNC-HELD-CURSOR-REFETCH-COST-01 closure]

# Actuals (#2632)
actuals:
  tokens: 4420
  tasks: 3
  commits: 4
plan_head_before: 91e3617570c3e20ef82879ab7bb05c06f0d2cb74

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Anti-vacuity calibration via cp/cmp byte-backup (never git checkout/restore/stash) for a hand-edited neuter of a Python module constant"
    - "monkeypatch.delattr for a self-calibrating gate that needs no on-disk edit (module symbol removed, auto-restored at teardown)"

key-files:
  created: []
  modified:
    - analytics-service/routers/cron.py
    - analytics-service/tests/test_cron_router.py
    - TODOS.md

key-decisions:
  - "Admit only \"private\" into ALLOWED_STRATEGY_STATUSES; rejected/archived stay excluded per CONTEXT.md Area 1 — one-line-of-substance widening, no new admits beyond the recorded verdict"
  - "Extended the guard comment to state explicitly that trade-sync's admit set is meant to diverge from the ledger fan-out migration's three-value set (which excludes draft for a ledger-specific reason that does not apply to trade-sync)"
  - "The ordering gate (Area 4) is a calibrated pytest class, not a comment or TODOS line — proven capable of failing via monkeypatch.delattr on one of the three marker symbols"
  - "TODOS.md SYNC-HELD-CURSOR-REFETCH-COST-01 is re-scoped, not closed — closure requires PR merge AND 164.5.1.4's PROD-apply success AND a post-deploy PROD read, none of which are true yet"
  - "The general unbounded-refetch-window defect (unrelated to the five measured keys) is intentionally DROPPED, not routed to a new phase, per the repo's data-integrity/user-facing deferral-gating rule — it cannot be fixed without touching should_advance_cursor or the key-cursor fallback path, both forbidden in this phase, and has no known live instance"

patterns-established:
  - "Ordering-invariant gates for cross-phase composition: assert 'if X is true, Y must hold' and calibrate via monkeypatch rather than a static comment"

requirements-completed:
  - FANOUT-COHORT-SYNC-CONSTANT-01
  - SYNC-HELD-CURSOR-REFETCH-COST-01
  - SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01

coverage:
  - id: D1
    description: "ALLOWED_STRATEGY_STATUSES admits \"private\" and nothing else new; proven end-to-end by driving cron_sync(), never by asserting membership in the constant"
    requirement: FANOUT-COHORT-SYNC-CONSTANT-01
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_cron_router.py::TestStrategyLifecycleFilter::test_only_live_statuses_receive_sync_trades_rpc"
        status: pass
    human_judgment: false
  - id: D2
    description: "A private-only key moves out of the held bucket into a full ok sync once private is admitted (full bucket vector asserted, not one counter)"
    requirement: SYNC-HELD-CURSOR-REFETCH-COST-01
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_cron_router.py::TestHeldStatusBucketForStalledKeys::test_private_only_key_no_longer_held_once_admitted"
        status: pass
    human_judgment: false
  - id: D3
    description: "On a mixed key, a failed private strategy's per-strategy marker holds while the key cursor still advances — the widening does not reopen per-key stranding"
    requirement: SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_cron_router.py::TestPrivateAdmissionComposesWithSyncCursor::test_mixed_key_private_failure_holds_marker_while_key_advances"
        status: pass
    human_judgment: false
  - id: D4
    description: "A permanent, mechanical ordering gate fails if private is admitted while 164.5.1.4's marker symbols are absent"
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_cron_router.py::TestPrivateAdmissionOrderingGate::test_synccursor_marker_present_while_private_admitted"
        status: pass
      - kind: unit
        ref: "analytics-service/tests/test_cron_router.py::TestPrivateAdmissionOrderingGate::test_ordering_gate_fails_if_marker_symbols_removed"
        status: pass
    human_judgment: false
  - id: D5
    description: "TODOS.md ledger honestly reflects the live PROD-apply gate state and this phase's own merge precondition"
    verification: []
    human_judgment: true
    rationale: "Whether the ledger prose accurately represents the live gh measurement, and whether the merge precondition wording is unambiguous, is a documentation-correctness judgment call, not something a unit test can classify."
  - id: D6
    description: "This phase's change is NOT yet safe to merge — 164.5.1.4's PROD-apply job has not reported success"
    verification: []
    human_judgment: true
    rationale: "The merge/ship decision is explicitly a human call gated on a live infrastructure state (GitHub's Production environment reviewer approval), not something this plan's execution can or should resolve."

duration: 20min
completed: 2026-09-20
status: complete
---

# Phase 164.5.1.3 Plan 01: SYNCADMIT Summary

**Admits the owner-only `private` status into `ALLOWED_STRATEGY_STATUSES` (analytics-service/routers/cron.py), proves the widening composes safely with 164.5.1.4's per-strategy SYNCCURSOR marker via a calibrated ordering gate, and re-scopes the TODOS ledger against a live GitHub Actions read confirming the phase is NOT yet safe to merge.**

## Performance

- **Duration:** ~20 min (first commit 03:15:49+02:00, last commit 03:34:53+02:00 on 2026-09-20)
- **Tasks:** 3/3 completed
- **Files modified:** 3 (`analytics-service/routers/cron.py`, `analytics-service/tests/test_cron_router.py`, `TODOS.md`)
- **Commits:** 4 (measured via `git rev-list --count 91e36175..HEAD`)

## Accomplishments

- `ALLOWED_STRATEGY_STATUSES` widened from `{"draft", "pending_review", "published"}` to also include `"private"` — the owner-only terminal status the contribution wizard finalizes to. Guard comment extended to record the Area 1/Area 2 reasoning and to state explicitly that this admit set is meant to diverge from the ledger fan-out migration's three-value set.
- Four new/extended behavioural tests, each calibrated (neutered, observed RED with recorded failure text, restored via `cp`/`cmp`, observed GREEN again) — no test asserts membership in the constant directly; every gate drives `cron_sync()` end to end and asserts on the consequence.
- A permanent ordering gate (`TestPrivateAdmissionOrderingGate`) that fails if `private` is ever admitted without 164.5.1.4's marker machinery present — proven capable of failing via `monkeypatch.delattr`, not just currently passing.
- `TODOS.md` re-scoped honestly: `SYNC-HELD-CURSOR-REFETCH-COST-01` is NOT closed (named trigger + owner recorded instead), and `FANOUT-COHORT-SYNC-CONSTANT-01` carries an addendum recording that the implementation actually landed.
- Live re-check performed (not inherited from a document): 164.5.1.4's PROD-apply gate is still `waiting` on human reviewer approval as of 2026-09-20 — **this phase's change is not yet safe to merge.**

## Task Commits

1. **Task 1 (RED): extend lifecycle-filter test with a private-status strategy** — `1d5266bc` (test)
2. **Task 1 (GREEN): admit private into ALLOWED_STRATEGY_STATUSES** — `15f02f32` (feat)
3. **Task 2: prove private-admission composes safely with SYNCCURSOR** — `f5895565` (test)
4. **Task 3: close the ledger honestly against a live PROD-apply re-check** — `c8a61577` (docs)

_Task 1 is `type="tracer" tdd="true"` and was split into a RED test commit and a GREEN implementation commit per the plan's explicit instruction, mirroring this repo's own convention for this class of change. Task 2 is `type="auto" tdd="true"` — its tests were already green on arrival (private was admitted in Task 1), so its RED evidence comes entirely from the neuter/restore/cmp calibration below, not from a natural pre-implementation failure; it is committed as a single test-only commit. Task 3 touches only `TODOS.md` and has no code to split._

## Files Created/Modified

- `analytics-service/routers/cron.py` — `ALLOWED_STRATEGY_STATUSES` widened to admit `"private"`; guard comment extended (13 lines added, 1 line changed).
- `analytics-service/tests/test_cron_router.py` — `TestStrategyLifecycleFilter::test_only_live_statuses_receive_sync_trades_rpc` extended with an `s-private` fixture entry; new `TestHeldStatusBucketForStalledKeys::test_private_only_key_no_longer_held_once_admitted`; new `TestPrivateAdmissionComposesWithSyncCursor` class (1 test); new `TestPrivateAdmissionOrderingGate` class (2 tests).
- `TODOS.md` — `SYNC-HELD-CURSOR-REFETCH-COST-01` addendum (re-scoped, not closed); `FANOUT-COHORT-SYNC-CONSTANT-01` addendum (implementation-landed note).

## Calibration Evidence (verbatim, per this plan's `<output>` contract)

### Task 1 — pre-edit RED (unmodified `cron.py`, `s-private` absent from `ALLOWED_STRATEGY_STATUSES`)

```
AssertionError: assert ['s-draft', 's-pub', 's-review'] == ['s-draft', 's-private', 's-pub', 's-review']

At index 1 diff: 's-pub' != 's-private'
Right contains one more item: 's-review'

Full diff:
  [
      's-draft',
-     's-private',
      's-pub',
      's-review',
  ]
```

### Task 1 — post-implementation neuter RED (hand-removed `"private"` from the widened set, `cp` byte-backup restored afterward, `cmp` confirmed byte-identical)

Identical failure shape to the pre-edit run above (same assertion, same diff), confirming the neuter targeted the correct line and the restore was clean.

### Task 2 — shared neuter RED, `test_private_only_key_no_longer_held_once_admitted`

```
AssertionError: {'synced': 0, 'partial': 0, 'failed': 0, 'timed_out': 0, ...}
assert 1 == 0
```
(`response["held"]` was `1` instead of the expected `0` — the private-only key fell back into the `held` bucket once `private` was removed from the admit set, exactly as it did before this phase.)

### Task 2 — shared neuter RED, `test_mixed_key_private_failure_holds_marker_while_key_advances`

```
AssertionError: [{'key_id': 'key-mixed', 'strategy_id': 's-pub', 'strategy_ids': ['s-pub'], 'exchange': 'binance', ...}]
assert 'ok' == 'partial'

- partial
+ ok
```
(With `private` removed from the admit set, `s-private` is filtered out of `strategy_ids` entirely before it ever reaches the RPC or the marker logic — matching the plan's predicted failure mechanism exactly.)

Both Task 2 tests were restored via `cp` from a fresh byte-backup, confirmed byte-identical via `cmp`, and re-confirmed GREEN afterward.

## Live PROD-Apply Re-Check (Task 3 Part A — exact `gh run` finding)

- **Run:** `35478916418` (workflow "Supabase Migrate", branch `main`)
- **headSha:** `60b8ed1b5ca0345df56bd8489b4e1a8bfda266df` — confirmed as the 164.5.1.4 merge commit that touched `analytics-service/routers/cron.py` via `git log --oneline main -- analytics-service/routers/cron.py`
- **Job `apply-test`:** `status: completed`, `conclusion: success` (applied cleanly to shared TEST)
- **Job `plan`** (the PROD-apply gate): `status: waiting`, `conclusion: ""` — **NOT yet applied to PROD**, awaiting the `Production` environment's human reviewer approval
- No `apply` job exists yet in this run because it is gated behind `plan`
- This reading is identical in substance to the planning-time reading recorded in CONTEXT.md, re-confirmed live on 2026-09-20 rather than trusted from a document

**Merge-safety verdict: this phase's change is NOT safe to merge yet.** It becomes safe once 164.5.1.4's PROD-apply job reports `success` — that has not happened as of this execution.

## Decisions Made

- Admitted only `"private"` — no other status. `rejected` and `archived` stay excluded per CONTEXT.md Area 1.
- The ordering gate is a calibrated pytest class (`TestPrivateAdmissionOrderingGate`), not a comment or a TODOS line, per CONTEXT.md Area 4's explicit requirement that the gate be mechanical and outlive this phase.
- `SYNC-HELD-CURSOR-REFETCH-COST-01` re-scoped rather than closed: closure requires PR merge AND 164.5.1.4's PROD-apply success AND a post-deploy PROD read confirming the held counter drops to 0 for the five known keys. Owner is the next post-deploy verification step (a `checkpoint:human-action` PROD read).
- The general unbounded-refetch-window defect (unrelated to the five measured `private` keys) is intentionally DROPPED rather than routed to a new phase — it cannot be fixed without touching `should_advance_cursor` or the key-cursor fallback path (both forbidden in this phase), has no currently known live instance, and is not itself data-integrity or user-facing (the hold is lossless by design).

## Deviations from Plan

None — plan executed exactly as written. All five `must_haves.truths` are satisfied: the private-status strategy reaches `sync_trades` on its key's next tick; a private-only key's full bucket vector moves out of `held`; the mixed-key composition test proves `SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01` does not reopen; the ordering gate fails if the marker machinery is removed while private stays admitted; and this phase's own merge/deploy status was checked live at execution time (Task 3 Part A), not trusted from a document.

## Known Stubs

None.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required. The unresolved item is not a setup task but a **merge gate**: 164.5.1.4's PROD-apply job must report `success` (GitHub's `Production` environment human reviewer approval) before this phase's PR can merge or deploy. That approval is the founder's action, not an agent's.

## Next Phase Readiness

- Code and tests for this phase are complete and committed on `feat/164.5.1.3-syncadmit`. Full `analytics-service` suite: 5932 passed, 89 skipped (baseline 5928/89, +4 new test methods). `mypy --strict --follow-imports=silent services/ routers/ models/`: clean (95 source files).
- **Blocker before ship:** 164.5.1.4's PROD-apply job (`gh run view 35478916418`, job `plan`) was `status: waiting` as of this execution. Re-check live again before merging — do not trust this document's reading past its own re-verification.
- Once PROD-apply succeeds and this PR merges and deploys, the next step is a `checkpoint:human-action` PROD read confirming the `held` counter for the five previously-measured `private` keys drops to 0 — that is what actually closes `SYNC-HELD-CURSOR-REFETCH-COST-01`.

---
*Phase: 164.5.1.3-syncadmit-admit-the-owner-only-status-to-the-trade-sync-cons*
*Completed: 2026-09-20*

## Self-Check: PASSED

- SUMMARY.md exists at its expected path — FOUND.
- All 4 task commits (`1d5266bc`, `15f02f32`, `f5895565`, `c8a61577`) found via `git log --oneline --all`.
