---
phase: 164.5.1.3-syncadmit-admit-the-owner-only-status-to-the-trade-sync-cons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/routers/cron.py
  - analytics-service/tests/test_cron_router.py
  - TODOS.md
autonomous: true
requirements:
  - FANOUT-COHORT-SYNC-CONSTANT-01
  - SYNC-HELD-CURSOR-REFETCH-COST-01
  - SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - A private-status strategy's trades reach the sync_trades RPC on its key's next tick (previously silently dropped by ALLOWED_STRATEGY_STATUSES).
    - A key backing only private strategies stops reporting "held" every tick once its sync succeeds — the full status-bucket vector (held/synced/partial/...) reflects the move, not just one counter.
    - Admitting private does not reopen SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01 — on a mixed-success key, the failed private strategy's per-strategy marker holds (surfaced in strategy_cursors_held) while the key cursor still advances, exactly as it does today for any other status.
    - A future edit that removes 164.5.1.4's SYNCCURSOR marker machinery while private stays admitted fails a permanent, already-in-the-suite test immediately, not silently in production.
    - This phase's own change is not merged or deployed while 164.5.1.4's Supabase Migrate PROD-apply job has not reported success — checked live at execution time, not trusted from a document.
  artifacts:
    - analytics-service/routers/cron.py (ALLOWED_STRATEGY_STATUSES admits "private"; guard comment extended with the Area 1/Area 2 exclusion reasoning)
    - analytics-service/tests/test_cron_router.py (TestStrategyLifecycleFilter's consequence-gate case extended; a new held-bucket-drop case; a new TestPrivateAdmissionComposesWithSyncCursor class; a new TestPrivateAdmissionOrderingGate class)
    - TODOS.md (SYNC-HELD-CURSOR-REFETCH-COST-01 re-scoped honestly with the live PROD-apply reading; FANOUT-COHORT-SYNC-CONSTANT-01 addendum recording that the widening landed in code)
  key_links:
    - ALLOWED_STRATEGY_STATUSES -> the strategy_ids list-comprehension in cron_sync's key-hydration loop -> the per-strategy sync_trades RPC fan-out -> per_strategy_stored -> should_advance_cursor (key cursor, untouched) and strategy_cursor_rows (per-strategy marker, untouched)
    - '"private" in ALLOWED_STRATEGY_STATUSES implies _STRATEGY_SYNC_CURSOR_TABLE / _strategy_resume_point / _resume_floor_ms are present in the same module (the mechanical ordering gate)'
    - This branch's merge/deploy is gated on 164.5.1.4's Supabase Migrate workflow run reaching a successful PROD apply job — a live GitHub Actions read, re-taken at Task 3, not this document's citation of it
---

<objective>
Implement the Area 2 verdict from `164.5.1.3-CONTEXT.md`: admit the owner-only `private` status into
`ALLOWED_STRATEGY_STATUSES` in `analytics-service/routers/cron.py`, prove the widening non-vacuously
by driving `cron_sync()` end to end (never by asserting membership in the constant), prove it composes
safely with Phase 164.5.1.4 SYNCCURSOR's per-strategy resume marker instead of reopening
`SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01`, add a mechanical gate that fails if `private` is ever
admitted without that marker machinery present, and close out the ledger honestly — including
re-checking, live, whether this change is actually safe to merge yet.

Purpose: five real, live, key-bearing production strategies (measured 5-of-5, PROD, 2026-09-19) have
had their trades silently dropped since `private` shipped in July 2026, because the trade-sync
lifecycle filter never learned that status. `private` is terminal, so the gap never self-closes.

Output: a one-line-of-substance widening in `cron.py`, four new/extended behavioural tests in
`test_cron_router.py` (each calibrated: neutered, observed RED, restored, observed GREEN again — no
new SQL, no migration), and an honest TODOS.md ledger stating exactly what shipped in code versus what
still needs a live PROD measurement before it can be marked closed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.5.1.3-syncadmit-admit-the-owner-only-status-to-the-trade-sync-cons/164.5.1.3-CONTEXT.md
@.planning/phases/164.5.1.3-syncadmit-admit-the-owner-only-status-to-the-trade-sync-cons/164.5.1.3-RESEARCH.md
@.planning/phases/164.5.1.3-syncadmit-admit-the-owner-only-status-to-the-trade-sync-cons/164.5.1.3-PATTERNS.md
@analytics-service/routers/cron.py
@analytics-service/tests/test_cron_router.py

⭐ **A finding from planning that supersedes RESEARCH.md's Pitfall 4 / Open Question 3 — read this
before Task 3.** RESEARCH.md and CONTEXT.md both describe "a failed `enqueue_compute_job` is not
re-driven by the next tick" as a live, accepted, third stranding path that this phase must NAME (in a
comment near `recompute_strategy_ids`) but not fix. Re-measured directly against `cron.py` at this
branch's HEAD: that path is **already fixed**, by Phase 164.5.1.4 itself. The comment block
immediately above `recompute_strategy_ids` now reads, in its own words, "A FAILED ENQUEUE *IS*
RE-DRIVEN BY THE NEXT SYNC TICK ON THIS PATH NOW, and this note used to say the opposite" — the
per-strategy marker write's `sid not in recompute_enqueue_errors` conjunct holds a strategy's marker
when its enqueue raises, so the next tick re-fetches and re-attempts it. `SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01`'s
own TODOS.md entry (closed-in-code by 164.5.1.4) documents this as `SYNCCURSOR-C4`. **Do not add a new
comment restating the stale "not fixed" framing — it would be false.** Task 2's composition test is
what actually needs to exist here: proof that this already-shipped protection extends correctly to a
newly-admitted `private` strategy, not a fresh fix.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Admit "private" into ALLOWED_STRATEGY_STATUSES — RED, then GREEN, end to end</name>
  <files>analytics-service/routers/cron.py, analytics-service/tests/test_cron_router.py</files>
  <precondition>164.5.1.4's SYNCCURSOR marker symbols (_STRATEGY_SYNC_CURSOR_TABLE, _strategy_resume_point, _resume_floor_ms) already exist in analytics-service/routers/cron.py at this branch's HEAD — confirm with a grep before editing; if any is missing, halt and escalate rather than widening the constant.</precondition>
  <behavior>
    - Test 1 (extend `TestStrategyLifecycleFilter::test_only_live_statuses_receive_sync_trades_rpc`): a strategy entry with `status: "private"` (id `s-private`) reaches the `sync_trades` RPC and appears in the response's `strategy_ids`, alongside the three already-admitted statuses (`s-draft`, `s-pub`, `s-review`) — the archived/suspended/deleted entries in the same fixture keep being dropped, unchanged.
    - Test 2 (unchanged regression): `TestStrategyLifecycleFilter::test_strategy_missing_status_field_is_dropped` stays green, untouched — a strategy with no `status` key at all must still fail closed.
  </behavior>
  <action>
    Per Area 2's verdict (CONTEXT.md, PROD measurement 2026-09-19: 5 of 5 live keyed `private`
    strategies sit on keys with no in-set sibling, so none of their trades are ever stored), widen
    the constant this phase exists to change.

    First, extend `TestStrategyLifecycleFilter::test_only_live_statuses_receive_sync_trades_rpc`'s
    `mixed_strategies` fixture with one additional entry, `{"id": "s-private", "status": "private"}`,
    and extend both existing assertions (the sorted `rpc_strategy_ids` list and the sorted
    `result_strategy_ids` list) to include `s-private` alongside `s-draft`, `s-pub`, `s-review`. Update
    the test's docstring one line to note it now also proves the Area 2 verdict. Do not touch
    `test_strategy_missing_status_field_is_dropped` — it is the fail-closed sibling and must stay
    exactly as it is.

    Run only that class (see verify) against the UNMODIFIED `cron.py`. It MUST fail — `s-private` will
    be absent from both lists. Record the exact assertion failure text (the list-mismatch diff) in your
    working notes; it is the calibration evidence success criterion 3 asks for, and it goes in the
    SUMMARY verbatim.

    Now make it pass. In `analytics-service/routers/cron.py`, widen `ALLOWED_STRATEGY_STATUSES` from
    `{"draft", "pending_review", "published"}` to also include `"private"`. Extend its existing
    three-line guard comment (the one explaining the archived/deleted overwrite hazard) — do not delete
    that reasoning, append to it — with: `private` is the owner-only TERMINAL status the contribution
    wizard finalizes to by design (`src/app/api/strategies/finalize-wizard/route.ts`), admitted per this
    phase's Area 2 verdict; `rejected` and `archived` stay excluded on purpose (Area 1) and mirroring
    the ledger fan-out migration's three-value set is explicitly not the goal here — the two constants
    are meant to diverge (trade-sync keeps `draft`, which the ledger fan-out excludes for a
    ledger-specific reason: a draft strategy has no factsheet to refresh, but its trades still need
    storing).

    Re-run the same test class. It MUST now pass.

    Anti-vacuity calibration, blocking (this is user-facing and data-integrity — success criterion 3):
    copy `analytics-service/routers/cron.py` to a byte backup OUTSIDE the repo tree (your session
    scratchpad or equivalent — never a `.bak` file left inside the repo). Hand-edit the just-widened
    line to temporarily remove `"private"` again (neuter — never `git checkout`/`restore`/`stash`).
    Re-run the same test class and confirm it goes RED again, with the same shape of failure as the
    pre-edit run; record that observed text too. Restore `cron.py` by copying the backup back over it
    (`cp`, not git), then run `cmp` between the restored file and the backup to confirm a byte-identical
    restore. Re-run the test class one final time and confirm GREEN. Delete the scratchpad backup once
    confirmed.

    Commit as two atomic commits mirroring this repo's own convention for this exact class of change
    (see TODOS.md's `SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01` entry, which cites its own RED/GREEN pair
    by sha): a RED commit containing only the test extension, then a GREEN commit containing only the
    `cron.py` constant/comment edit.
  </action>
  <verify>
    <automated>cd analytics-service && ./.venv/bin/python -m pytest tests/test_cron_router.py::TestStrategyLifecycleFilter -x -v</automated>
  </verify>
  <done>Both tests in TestStrategyLifecycleFilter pass; `s-private` is present in the widened test's expected `rpc_strategy_ids` and `result_strategy_ids`; the neuter/restore/cmp calibration was performed and its two observed failure texts are recorded for the SUMMARY; ALLOWED_STRATEGY_STATUSES == {"draft", "pending_review", "published", "private"} in the committed file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the widening composes safely with 164.5.1.4's marker, and add the ordering gate</name>
  <files>analytics-service/tests/test_cron_router.py</files>
  <behavior>
    - Held-bucket-drop: a key whose only strategy is now `private` (previously the class of key that
      landed in `held` every tick, per `TestHeldStatusBucketForStalledKeys`'s own docstring) reports
      the FULL status-bucket vector as a successful sync (`held == 0`, `synced == 1`, every other
      counter `0`) once its `sync_trades` call stores rows — mirroring the existing
      `test_stalled_key_reports_held_not_ok` fixture shape, private-status variant.
    - Composition: on a single key with two strategies — one `published` (succeeds) and one `private`
      (its `sync_trades` RPC raises) — the key's overall status is `partial`, the key cursor still
      advances (unchanged pre-phase behaviour, per `TestC0198CursorOnlyAdvancesWhenStored`), AND the
      failed `private` strategy's id appears in that key's `strategy_cursors_held` while the succeeding
      strategy's id does not — i.e. `SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01` does not reopen for a
      newly-admitted status.
    - Ordering gate: if `"private"` is a member of `cron_mod.ALLOWED_STRATEGY_STATUSES`, then
      `cron_mod._STRATEGY_SYNC_CURSOR_TABLE`, `cron_mod._strategy_resume_point`, and
      `cron_mod._resume_floor_ms` must all exist — calibrated by proving the assertion CAN fail, not
      just that it currently passes.
  </behavior>
  <action>
    Add a private-only-key case to `TestHeldStatusBucketForStalledKeys` (a new method in that class,
    e.g. `test_private_only_key_no_longer_held_once_admitted`): copy
    `test_stalled_key_reports_held_not_ok`'s fixture shape exactly, but give the key's single embedded
    strategy `status: "private"` instead of `"archived"`, and let `fetch_all_trades` and the default
    `sync_trades` RPC return succeed as normal (rows stored > 0). Assert the FULL bucket vector this
    class's own docstring insists on (Rule 9: asserting one counter dropping is not enough) —
    `held == 0`, `synced == 1`, `partial == failed == timed_out == revoked == transient == deferred == 0`,
    `total_keys == 1` — and that the key's own `results[0]["status"] == "ok"`.

    Add a new class `TestPrivateAdmissionComposesWithSyncCursor` with one test,
    `test_mixed_key_private_failure_holds_marker_while_key_advances`. Build ONE key via
    `_make_mock_supabase_for_cron_sync` (the same helper `TestStrategyLifecycleFilter` uses) with two
    embedded strategies: `{"id": "s-pub", "status": "published"}` and
    `{"id": "s-private", "status": "private"}`. After constructing the mock, REPLACE
    `mock_supabase.rpc.side_effect` (do not rely on the helper's single shared `rpc_data` scalar, which
    cannot differentiate per strategy) with a closure keyed on the RPC name and `p_strategy_id`, mirroring
    `TestC0198CursorOnlyAdvancesWhenStored`'s own `_rpc(name, args)` pattern: `sync_trades` succeeds
    (returns an integer > 0) for `s-pub` and raises for `s-private`; `enqueue_compute_job` always
    succeeds for both. Drive `cron_mod.cron_sync()` the same way `TestStrategyLifecycleFilter` does
    (patch `get_kek`, `get_supabase`, `decrypt_credentials`, `create_exchange`,
    `validate_key_permissions`, `fetch_all_trades` returning one trade, `fetch_usdt_balance`, and
    `parse_since_ms` returning `None`). Assert: `response["results"][0]["status"] == "partial"`; the
    key's `api_keys` UPDATE payload still carries `last_sync_at` (the cursor advanced — cite
    `SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01`'s own precedent that this is unchanged, correct,
    pre-phase behaviour); and `response["results"][0]["strategy_cursors_held"] == ["s-private"]` (never
    contains `s-pub`). This directly proves the widening does not reopen the per-key stranding defect on
    the newly-admitted status.

    Add a new class `TestPrivateAdmissionOrderingGate` (CONTEXT.md Area 4: "the plan must leave a gate
    that FAILS if ALLOWED_STRATEGY_STATUSES contains private while 164.5.1.4's resume marker is
    absent" — a gate that must outlive this phase). First test,
    `test_synccursor_marker_present_while_private_admitted`: assert that if
    `"private" in cron_mod.ALLOWED_STRATEGY_STATUSES` then `hasattr(cron_mod, "_STRATEGY_SYNC_CURSOR_TABLE")`,
    `hasattr(cron_mod, "_strategy_resume_point")`, and `hasattr(cron_mod, "_resume_floor_ms")` are all
    True. This currently passes trivially, which is why the second test exists. Second test,
    `test_ordering_gate_fails_if_marker_symbols_removed` (accepts the `monkeypatch` fixture): use
    `monkeypatch.delattr(cron_mod, "_resume_floor_ms")` to remove ONE of the three marker symbols at
    runtime (private stays admitted — do not touch `ALLOWED_STRATEGY_STATUSES` for this calibration),
    then wrap the same guarded assertion in `pytest.raises(AssertionError)` (or re-run it as a plain
    assertion and assert it raises) to prove the gate CAN fail. `monkeypatch` auto-restores the module
    at teardown — no file edit, no cp/cmp needed for this one, since nothing on disk changes.

    For the held-bucket-drop test and the composition test (both of which assume `private` is already
    admitted, so there is no natural "pre-edit" RED left to observe the way Task 1 had one): repeat
    Task 1's neuter/restore/cmp calibration ONE more time, covering both new tests together — `cp` a
    fresh backup of `cron.py`, hand-remove `"private"` from `ALLOWED_STRATEGY_STATUSES`, re-run both
    tests and confirm BOTH go RED (the held-bucket test will report `held == 1, synced == 0` instead of
    the reverse; the composition test's `strategy_cursors_held` assertion will fail because `s-private`
    is filtered out of `strategy_ids` entirely before it ever reaches the RPC or the marker logic — record
    both observed failure texts), restore via `cp` from the backup, `cmp` to confirm byte-identical, and
    confirm GREEN again.
  </action>
  <verify>
    <automated>cd analytics-service && ./.venv/bin/python -m pytest tests/test_cron_router.py::TestHeldStatusBucketForStalledKeys tests/test_cron_router.py::TestPrivateAdmissionComposesWithSyncCursor tests/test_cron_router.py::TestPrivateAdmissionOrderingGate -x -v</automated>
  </verify>
  <done>All three: the new held-bucket-drop case passes with the full bucket vector asserted; TestPrivateAdmissionComposesWithSyncCursor passes proving the failed private strategy's marker holds while the key cursor advances; TestPrivateAdmissionOrderingGate's two tests pass, with the second proving the gate can fail via monkeypatch; the shared neuter/restore/cmp calibration for the first two was performed and its observed failure texts recorded for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Close the ledger honestly, and gate merge on a live PROD-apply re-check</name>
  <files>TODOS.md</files>
  <action>
    This task does not touch source. It has three parts, run in order.

    Part A — the live ship-precondition re-check (do this FIRST, its result feeds Part B). Run
    `gh run list --workflow "Supabase Migrate" --branch main --limit 5 --json databaseId,headSha,status,conclusion,createdAt`
    and find the run whose `headSha` matches this repo's 164.5.1.4 merge commit (`60b8ed1b` at
    planning time — confirm the actual sha via `git log --oneline main -- analytics-service/routers/cron.py`
    if `main` has moved). Then run
    `gh run view <that databaseId> --json status,conclusion,jobs` and read the PROD-apply gate job
    (named `plan` at planning time, and whatever `apply` job it unlocks). At planning time this read
    was: `apply-test` succeeded against shared TEST, but the PROD-apply gate job was `status: waiting`,
    `conclusion: ""` — i.e. NOT yet applied to PROD. Re-run it now; do not trust that number. This is a
    read of GitHub Actions run metadata via the already-authenticated `gh` CLI — no credential entry, no
    database connection, nothing that requires a `checkpoint:human-action`.

    Part B — re-scope `SYNC-HELD-CURSOR-REFETCH-COST-01` in `TODOS.md` honestly, using Part A's fresh
    reading. Append a dated addendum (today's date, "Phase 164.5.1.3 SYNCADMIT plan 01") stating: the
    code fix (admitting `private`) has shipped in this phase's commits, which structurally ends the
    held state for the five measured keys ONCE deployed — but the entry cannot be marked CLOSED yet,
    because (a) 164.5.1.4's PROD-apply job had not reported `success` as of Part A's reading [cite the
    exact status/conclusion/run id you just observed], (b) this phase's own PR has not merged or
    deployed, and (c) no post-deploy tick has actually measured the held counter dropping for the five
    known keys. State the real trigger for closure: this phase's PR merges AND 164.5.1.4's PROD apply
    succeeds AND a subsequent PROD read confirms the held counter for the five known keys is 0 — and
    name the owner: the next post-deploy verification step for this phase (a `checkpoint:human-action`
    PROD read — the founder enters credentials, never an agent). Separately, and explicitly: the
    GENERAL unbounded-refetch-window defect (a hypothetical FUTURE key with zero eligible strategies for
    some OTHER reason) is out of this phase's scope and is not being routed to a new phase — it cannot
    be fixed without touching `should_advance_cursor` or the key-cursor fallback path, both of which
    this phase is explicitly forbidden from modifying; it has no currently known live instance now that
    the five measured keys are addressed; and it is not itself a data-integrity or user-facing defect
    (the hold is lossless by design), so per this repo's own deferral-gating rule (only
    data-integrity/user-facing items get a phase) it is intentionally DROPPED here rather than kept
    open or given a phase. Record that reasoning in the entry so a future reader does not "complete" a
    fix that was never promised.

    Part C — append a short addendum to the existing "`FANOUT-COHORT-SYNC-CONSTANT-01` is now FULLY
    CLOSED" note recording that Phase 164.5.1.3 shipped the widening in code (cite the actual commit
    shas from Task 1 once they exist), so the ledger shows the promised implementation actually landed
    and not just the verdict.

    Finally, run the full `analytics-service` suite and the CI mypy gate as the phase-level check (see
    verify) — both must be clean before this plan is considered done.
  </action>
  <verify>
    <automated>cd analytics-service && ./.venv/bin/python -m pytest -q && ./.venv/bin/python -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <done>The live gh re-check was performed and its result (not a document's citation of it) is recorded in TODOS.md; SYNC-HELD-CURSOR-REFETCH-COST-01 is re-scoped (not falsely closed) with a named trigger and owner; the general unbounded-growth case is explicitly dropped with its reasoning recorded; FANOUT-COHORT-SYNC-CONSTANT-01 carries an addendum noting implementation landed; the full analytics-service suite and mypy --strict are both clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST service-role RPC (`sync_trades`, `enqueue_compute_job`) | Backend-to-database boundary; both are `SECURITY DEFINER`, invoked only by the cron scheduler's own service-role credential, never by an end user directly. |
| `strategies.status` read | A DB column read inside the FastAPI cron handler, compared against a fixed Python set literal — not user-supplied on this request path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5.1.3-01 | Tampering | `ALLOWED_STRATEGY_STATUSES` (cron.py) | medium | mitigate | The extended guard comment documents, at the point of change, exactly which two lifecycle values must never be admitted (`rejected`, `archived`) and why (Area 1), mirroring the ledger-fan-out migration's own exclusion-boundary idiom so a future "tidy" cannot silently widen past this phase's decision. |
| T-164.5.1.3-02 | Tampering / data loss | `strategy_sync_cursors` marker machinery (`_STRATEGY_SYNC_CURSOR_TABLE`, `_strategy_resume_point`, `_resume_floor_ms`) | high | mitigate | Task 2's `TestPrivateAdmissionOrderingGate` is a permanent, calibrated test asserting the marker symbols exist whenever `private` is admitted — if a future edit reverts 164.5.1.4's marker machinery while leaving `private` admitted, this test fails on the next CI run rather than silently reopening `SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01` in production. |
| T-164.5.1.3-03 | Tampering / data loss | Deploy ordering vs. 164.5.1.4's PROD migration apply | critical | mitigate | Task 3's live `gh run` re-check, performed at execution time (not inherited from a document), plus the explicit ship-precondition recorded in TODOS.md: this phase's PR must not merge/deploy until 164.5.1.4's PROD-apply job reports `success` — both new Supabase paths fail open, so deploying ahead of the apply would silently re-open the exact per-strategy trade loss this ordering exists to prevent. |

No new SECURITY.md-style register is warranted beyond these three rows (per RESEARCH.md's own Security Domain analysis) — no new attack surface, no new secret, no new external caller, no auth/session-boundary change.
</threat_model>

<verification>
Run the full `analytics-service` suite (`cd analytics-service && ./.venv/bin/python -m pytest -q`) and confirm it is green with a count at or above the pre-phase baseline of 90 passed in `tests/test_cron_router.py` alone (5928 passed, 89 skipped repo-wide) plus this phase's new/extended cases (Task 1 adds 0 new tests but extends 1; Task 2 adds 4 new test methods across 2 new classes plus 1 new method on an existing class). Run `mypy --strict --follow-imports=silent services/ routers/ models/` and confirm it is clean. Confirm `ALLOWED_STRATEGY_STATUSES == {"draft", "pending_review", "published", "private"}` by direct read of the committed file. Confirm TODOS.md's `SYNC-HELD-CURSOR-REFETCH-COST-01` entry no longer claims a closure it cannot support, and carries the live gh-check evidence from Task 3. Run `npm run check:planning-hygiene` (repo root) after all edits, and read both the count line and the OK line — never `tail -1`.
</verification>

<success_criteria>
1. A recorded verdict (Area 2, already decided in CONTEXT.md) is implemented in code: `ALLOWED_STRATEGY_STATUSES` admits `"private"` and nothing else new.
2. The blast radius is traced in the extended guard comment and this plan's objective/context: new `sync_trades` RPC load (bounded, ≤5 additional calls/tick — Area 3), newly stored trades for five keys, `enqueue_compute_job` follow-ons, and the interaction with `should_advance_cursor` (proven unchanged) and the SYNCCURSOR marker (proven composing correctly).
3. Every new/extended test is calibrated: neutered, observed RED with recorded failure text, restored via `cp`/`cmp` (never git), observed GREEN again. Anti-vacuity holds throughout — no test asserts membership in the constant directly.
4. Nothing widens a ceiling, relaxes a floor, or adds an exemption — confirmed by the mypy/pytest full-suite run in Task 3 showing no floor/ceiling file touched outside this plan's own `files_modified`.
5. This `cron.py`-only change ships via the ordinary CI path (no SQL, no migration, no `apply-test`/PROD-apply pipeline of its own) — but its MERGE is explicitly gated, in TODOS.md, on 164.5.1.4's own PROD-apply pipeline succeeding first, re-checked live in Task 3.
</success_criteria>

<output>
Create `.planning/phases/164.5.1.3-syncadmit-admit-the-owner-only-status-to-the-trade-sync-cons/164.5.1.3-01-SUMMARY.md` when done. Include verbatim: both observed RED failure texts from Task 1's calibration, both observed RED failure texts from Task 2's shared calibration, the exact `gh run` finding from Task 3 Part A (run id, job name, status, conclusion), and an explicit line stating whether this phase's change is safe to merge yet (it is not, unless Task 3's live re-check found the PROD-apply job already `success`).
</output>
