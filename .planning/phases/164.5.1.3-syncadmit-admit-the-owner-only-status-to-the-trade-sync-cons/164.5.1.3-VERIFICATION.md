---
phase: 164.5.1.3-syncadmit-admit-the-owner-only-status-to-the-trade-sync-cons
verified: 2026-09-20T08:30:00Z
status: passed
score: 5/5 must-haves verified (PLAN frontmatter) + 5/5 ROADMAP success criteria
verified_at_sha: 900dd4ae93401543b68f3dd65207d4c63c807e2f
drift_subjects:
  - analytics-service/routers/cron.py
  - analytics-service/tests/test_cron_router.py
behavior_unverified: 0
overrides_applied: 0
---

# Phase 164.5.1.3: SYNCADMIT Verification Report

**Phase Goal:** Decide, on evidence, whether `ALLOWED_STRATEGY_STATUSES` must admit the owner-only
(`private`) status — and if so, ship that widening with its blast radius traced.
**Verified:** 2026-09-20, at HEAD `900dd4ae` (branch `feat/164.5.1.3-syncadmit`, forked from `main`
at `60b8ed1b`)
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (PLAN `must_haves.truths`)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | A `private`-status strategy's trades reach the `sync_trades` RPC on its key's next tick | ✓ VERIFIED | `TestStrategyLifecycleFilter::test_only_live_statuses_receive_sync_trades_rpc` re-run independently by this verifier: PASS. Drives `cron_sync()` end-to-end, asserts on the actual `mock_supabase.rpc.call_args_list` for `sync_trades`, not on constant membership. |
| 2 | A key backing only `private` strategies stops reporting `held` every tick — full status-bucket vector, not one counter | ✓ VERIFIED | `TestHeldStatusBucketForStalledKeys::test_private_only_key_no_longer_held_once_admitted` re-run independently: PASS. Asserts all 8 bucket counters (`held==0`, `synced==1`, six others `==0`) plus `results[0]["status"]=="ok"`. |
| 3 | Admitting `private` does not reopen `SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01` — failed strategy's marker holds while the key cursor still advances | ✓ VERIFIED | `TestPrivateAdmissionComposesWithSyncCursor::test_mixed_key_private_failure_holds_marker_while_key_advances` re-run independently: PASS. Per-strategy RPC closure makes `s-pub` succeed and `s-private` raise; asserts `results[0]["status"]=="partial"`, the `api_keys` UPDATE payload still carries `last_sync_at`, and `strategy_cursors_held == ["s-private"]` only. |
| 4 | A future edit that removes 164.5.1.4's SYNCCURSOR marker machinery while `private` stays admitted fails a permanent, already-in-the-suite test immediately | ✓ VERIFIED | `TestPrivateAdmissionOrderingGate`'s two tests re-run independently: both PASS. The second (`test_ordering_gate_fails_if_marker_symbols_removed`) uses `monkeypatch.delattr(cron_mod, "_resume_floor_ms")` and asserts the guarded invariant then raises `AssertionError` — proves the gate CAN fail, not just that it currently holds. |
| 5 | This phase's own change is not merged/deployed while 164.5.1.4's PROD-apply job has not reported success — checked live, not trusted from a document | ✓ VERIFIED | Branch `feat/164.5.1.3-syncadmit` is unmerged at HEAD (confirmed: no merge commit into `main`). Task 3's live `gh run view 35478916418` read (job `plan`: `waiting`) is recorded in `TODOS.md` at commit `c8a61577`. A later commit `900dd4ae` records a live re-check superseding it: run `35478916418` now reports `completed`/`success` overall with every job (`apply-test`, `plan`, `apply-test-verdict`, `apply`, `prod-credential-divergence-verdict`) `success` — both readings are kept, the gap between them is the point of the condition, and the branch still has not merged. This matches the task brief's independently-supplied PROD reading; not reported as outstanding. |

**Score:** 5/5 truths verified, 0 present-but-behavior-unverified.

### ROADMAP Success Criteria

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | A recorded verdict: widen or prove it must not be | ✓ VERIFIED | CONTEXT.md Area 2: "VERDICT: `private` MUST be admitted" with cited PROD evidence (5-of-5). Implemented in code. |
| 2 | If widened: blast radius traced before shipping (RPC load, newly stored trades, `enqueue_compute_job` follow-ons, interaction with `should_advance_cursor` and the SYNCCURSOR marker) | ✓ VERIFIED | CONTEXT.md Area 3 and the plan's `<context>` note trace all four; `should_advance_cursor` confirmed byte-identical to `main` at `60b8ed1b` (0 diff lines touching it — verified independently, see below); composition with the marker proven by truth 3 above. |
| 3 | Calibrated gate: neuter → RED → restore via `cmp` → GREEN, with recorded failure text; anti-vacuity blocks (data-integrity/user-facing) | ✓ VERIFIED | SUMMARY records four verbatim observed-RED texts (Task 1 pre-edit and post-edit-neuter, both matching; Task 2's two shared-neuter REDs). No test asserts membership in the constant directly — every gate drives `cron_sync()` and asserts on RPC calls, bucket counters, or marker state. |
| 4 | Nothing widens a ceiling, relaxes a floor, or adds an exemption | ✓ VERIFIED | `git diff 60b8ed1b..900dd4ae --stat` touches only `cron.py`, `test_cron_router.py`, `TODOS.md`, `ROADMAP.md`, `STATE.md`, and this phase's own planning artifacts — no `mutation-runner/run.mjs`, `lint-sql-gates.mjs`, `lint-app-guc.mjs`, `vac08-ledger-baseline.txt`, or `supabase/migrations/**` touched. |
| 5 | Apply path matches what changed: `cron.py`-only ships via ordinary CI; merge explicitly gated in TODOS.md on 164.5.1.4's own PROD-apply pipeline, re-checked live | ✓ VERIFIED | No SQL/migration in this phase's diff. `TODOS.md`'s `SYNC-HELD-CURSOR-REFETCH-COST-01` addendum states the merge gate explicitly and records both the execution-time and superseding live `gh run` readings. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `analytics-service/routers/cron.py` | `ALLOWED_STRATEGY_STATUSES` admits `"private"`; guard comment extended | ✓ VERIFIED | `ALLOWED_STRATEGY_STATUSES = {"draft", "pending_review", "published", "private"}` at one declaration site, one reader (the `strategy_ids` comprehension). Guard comment (lines ~190-208) states the Area 1/Area 2 reasoning and the ledger-fan-out divergence claim — independently checked against `supabase/migrations/20260917120000_ledger_fanout_admit_private.sql`, which admits `('published','pending_review','private')`; trade-sync's set differs by exactly `draft`, matching the comment's claim exactly. |
| `analytics-service/tests/test_cron_router.py` | `TestStrategyLifecycleFilter` extended; new held-bucket case; new `TestPrivateAdmissionComposesWithSyncCursor`; new `TestPrivateAdmissionOrderingGate` | ✓ VERIFIED | All four present at the expected class/method names; all 8 relevant tests (2 lifecycle + 2 held-bucket-relevant + 1 composition + 2 ordering-gate, minus the unrelated `test_stalled_key_per_key_line_logs_at_warning`) re-run by this verifier and PASS. |
| `TODOS.md` | `SYNC-HELD-CURSOR-REFETCH-COST-01` re-scoped honestly with live PROD-apply reading; `FANOUT-COHORT-SYNC-CONSTANT-01` addendum | ✓ VERIFIED | Both addenda present, accurate, and (for the held-cursor entry) updated a second time (`900dd4ae`) to record the superseding PROD-apply success without deleting the earlier reading — an honest ledger of the gap between the two reads. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `ALLOWED_STRATEGY_STATUSES` | `strategy_ids` comprehension in `cron_sync`'s key-hydration loop | direct membership test | ✓ WIRED | Sole reader, confirmed by grep (one declaration, one reader). |
| `strategy_ids` fan-out | `sync_trades` RPC per strategy → `per_strategy_stored` → `should_advance_cursor` (unchanged) and per-strategy marker (unchanged) | `_sync_single_key` | ✓ WIRED | Confirmed by the composition test driving the real function and asserting on both the key cursor (`api_keys` UPDATE payload) and `strategy_cursors_held`. |
| `"private" in ALLOWED_STRATEGY_STATUSES` | `_STRATEGY_SYNC_CURSOR_TABLE` / `_strategy_resume_point` / `_resume_floor_ms` presence | mechanical ordering gate | ✓ WIRED | `TestPrivateAdmissionOrderingGate`, both tests re-run, PASS; second test proves the gate is capable of failing (not merely passing today). |
| This branch's merge | 164.5.1.4's Supabase Migrate PROD-apply job | live `gh run` read | ✓ WIRED | Read performed twice (Task 3 Part A, then commit `900dd4ae`'s re-check); branch remains unmerged at HEAD; result matches the task brief's independently supplied PROD reading. |

### Behavioral Spot-Checks (independently re-run by this verifier, not inherited from SUMMARY)

| Behavior | Command | Result | Status |
|---|---|---|---|
| All 4 new/extended test classes pass | `pytest tests/test_cron_router.py::TestStrategyLifecycleFilter tests/test_cron_router.py::TestHeldStatusBucketForStalledKeys tests/test_cron_router.py::TestPrivateAdmissionComposesWithSyncCursor tests/test_cron_router.py::TestPrivateAdmissionOrderingGate -v` | 8 passed in 6.74s | ✓ PASS |
| Full `analytics-service` suite green at or above baseline | `pytest -q` (run once) | 5932 passed, 89 skipped, 921 warnings in 409.65s | ✓ PASS (matches SUMMARY's claimed 5932/89, +4 over the 5928/89 baseline) |
| `mypy --strict` clean | `mypy --strict --follow-imports=silent services/ routers/ models/` (run once) | Success: no issues found in 95 source files | ✓ PASS |
| `should_advance_cursor` byte-identical to `main` at fork point | `git diff 60b8ed1b -- analytics-service/routers/cron.py \| grep should_advance_cursor` | no output (0 diff lines touch it); expression at line 1037 unchanged: `(not trades) or synced_count > 0` | ✓ PASS |
| Ledger fan-out admit set matches the guard comment's claim | `grep -n "IN ('published', 'pending_review', 'private')" supabase/migrations/20260917120000_ledger_fanout_admit_private.sql` | present, line 535 | ✓ PASS |
| No debt markers introduced | `grep -naE "TBD\|FIXME\|XXX" cron.py test_cron_router.py TODOS.md` | 0 hits (all three files) | ✓ PASS |
| No floor/ceiling/gate-governor file touched | `git diff 60b8ed1b..900dd4ae --stat` | Only `cron.py`, `test_cron_router.py`, `TODOS.md`, `ROADMAP.md`, `STATE.md`, and this phase's own `.planning/phases/...` artifacts changed | ✓ PASS |
| `check:planning-hygiene` clean | `npm run check:planning-hygiene` | `OK — 6683 tracked files scanned, none carry the local username or an absolute home path` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| `FANOUT-COHORT-SYNC-CONSTANT-01` | 164.5.1.3-01 | Trade-sync constant admits the owner-only status | ✓ SATISFIED | `ALLOWED_STRATEGY_STATUSES` widened; `TODOS.md` addendum records implementation landed with commit shas. |
| `SYNC-HELD-CURSOR-REFETCH-COST-01` | 164.5.1.3-01 | Held-cursor re-fetch cost for the five `private` keys | ✓ SATISFIED (re-scoped, honestly not closed) | Structural fix shipped in code; `TODOS.md` correctly states closure requires merge + PROD-apply success + a post-deploy PROD read, none of which have happened yet, and names the owner. The general (non-five-key) unbounded-refetch case is explicitly dropped with reasoning, per this repo's own deferral-gating rule. |
| `SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01` | 164.5.1.3-01 | Admitting `private` must not reopen this defect | ✓ SATISFIED | Composition test proves the failed `private` strategy's marker holds while the key cursor advances, exactly as pre-phase behavior does for any other status. |

No orphaned requirements found for this phase in `TODOS.md`/ROADMAP beyond the three declared.

### Anti-Patterns Found

None blocking. No `TBD`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER` markers in any file this phase modified. No empty implementations, no hardcoded-empty stub props, no console.log-only handlers.

**ℹ️ Info (non-blocking):** `.planning/ROADMAP.md`'s Phase 164.5.1.3 plan-status line (written at commit `35dc1c76`) still reads "NOT yet merged; 164.5.1.4's PROD-apply gate was still `waiting`... which is this phase's own merge precondition." This predates commit `900dd4ae`'s superseding reading (PROD-apply now `success`) and was not updated afterward. This is outside this phase's own `must_haves` (Task 3's artifact scope was `TODOS.md` only, not `ROADMAP.md`), and `TODOS.md` — the actual ledger of record for this condition — is accurate and carries both readings correctly. Flagged for hygiene only; does not affect this phase's goal achievement.

### Human Verification Required

None. The one item that requires a human/founder action — a post-deploy PROD read confirming the `held` counter drops to 0 for the five known keys — is explicitly a downstream action gated on merge and deploy, both outside this phase's own scope (Task 3's own `<done>` criteria and CONTEXT.md Area 5 both frame it this way, and the task brief confirms the same reading). This phase's own deliverable — code, calibrated tests, and an honest ledger — is complete and independently verified above.

### Gaps Summary

None. All five PLAN `must_haves.truths`, all five ROADMAP success criteria, and all three requirement IDs are satisfied at HEAD `900dd4ae`. Every claim in `164.5.1.3-01-SUMMARY.md` that could be independently re-checked (test results, full-suite count, mypy cleanliness, the ledger-fan-out admit set, `should_advance_cursor`'s byte-identity, the live `gh run` findings) was re-verified directly against the codebase and/or re-run, not taken on the SUMMARY's word — all held up. The one place a SUMMARY-era reading later needed correction (the PROD-apply status) was itself corrected by a subsequent commit (`900dd4ae`) in the same branch, and that correction was verified against the task brief's independently supplied PROD reading.

---

_Verified: 2026-09-20T08:30:00Z_
_Verifier: Claude (gsd-verifier)_
