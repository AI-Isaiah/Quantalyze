# Phase 164.5.1.3: SYNCADMIT - Context

**Gathered:** 2026-09-19
**Status:** Ready for planning
**Mode:** Smart discuss, autonomous. ⚠️ **DEVIATION RECORDED (CLAUDE.md deviation policy).** The
smart-discuss workflow prescribes an `AskUserQuestion` per grey area. The founder's invocation
explicitly delegated these decisions ("answer every question yourself from the repo's own evidence
and RECORD the reasoning in CONTEXT.md; do NOT emit a `checkpoint:decision` a founder would
otherwise answer"). Every area below is therefore decided here, with its evidence. The ROADMAP
carries the same note.

<domain>
## Phase Boundary

Decide, on measured evidence, whether `ALLOWED_STRATEGY_STATUSES` in
`analytics-service/routers/cron.py` must admit the owner-only `private` status — and record that
verdict as explicitly as a code change. If it must be admitted, ship the widening with its blast
radius traced first.

**IN scope:** the admission verdict; the ordering decision versus Phase 164.5.1.4 SYNCCURSOR; the
accurate disposition of `SYNC-HELD-CURSOR-REFETCH-COST-01`.

**OUT of scope:** the position-poll predicate (refused as inert by 164.5.1.2 and byte-identical to
`main` — re-verified this phase); `enqueue_ledger_composite_refresh`; the per-key cursor redesign
itself (that is 164.5.1.4's work).

</domain>

<decisions>
## Implementation Decisions

### Area 1 — Scope of the admission

- **Admit `private` ONLY. Do not admit `rejected` or `archived`.** MEASURED at HEAD: the status
  universe is `{draft, pending_review, published, rejected, private, archived}` and the constant
  admits the first three. The phase title says "the owner-only status" (singular); `rejected` and
  `archived` are correctly excluded and must stay excluded — syncing a rejected or archived
  strategy would be a new behaviour nobody asked for.
- **`private` is a TERMINAL status, not a transitional one.** `src/app/api/strategies/finalize-wizard/route.ts`
  finalizes the contribution path to `private` by design. So an omitted `private` is not a
  short-lived window — those strategies stay dark permanently. This strengthens the case that the
  omission is a defect rather than a deliberate lifecycle gate.
- **The constant has exactly ONE declaration and ONE reader** (`ALLOWED_STRATEGY_STATUSES`, read in
  the `strategy_ids` list-comprehension inside the key-hydration loop of `cron_sync`). Cite BY
  SYMBOL — a roadmap citation of `:148`/`:658` had already drifted to `:150`/`:660`, and at HEAD the
  declaration and reader have moved again. Anything that pins a line number here will rot.
- **No migration and no schema change.** The constant is Python. This phase must not touch
  `supabase/migrations/**`.

### Area 2 — The verdict, and it is the decision this phase exists to take

- **VERDICT: `private` MUST be admitted — the omission is a real defect, not a safety property.**
  Evidence: 5 of 5 private strategies sit on keys with no in-set sibling (164.5.1.2's PROD read),
  so nothing syncs them; `private` is terminal, so the gap never closes on its own; and the
  downstream consequence is concrete (Area 3).
- ⭐ **THE WIDENING SHIPS IN THIS PHASE — after 164.5.1.4 SYNCCURSOR lands.** FOUNDER DECISION,
  2026-09-19: the queue is re-ordered so 164.5.1.4 runs FIRST, and this phase then ships the
  widening onto a cursor that no longer strands a failed strategy. So the exposure in Area 4 is
  removed UPSTREAM rather than mitigated here.
- **What the omission actually costs, MEASURED — and it is narrower than one prior framing and
  wider than another.** `164.5.1.1-RESEARCH.md` frames the gap as starving the ledger refresh.
  ⚠️ **The brief inherited a correction stating `run_derive_broker_dailies_job` contains "trades ZERO
  times". That literal claim is FALSE at HEAD** — re-measured over the real Python body
  (`analytics-service/services/job_worker.py::run_derive_broker_dailies_job`, 3327 lines), `trades`
  occurs 4 times and `sync_trades` once, **all of them inside comments or log strings, none a table
  read**. The SUBSTANCE of the correction therefore holds: the ledger job runs its own venue crawl
  and does not read the `trades` table. ⛔ Record the substance, not the discredited number.

### Area 3 — Blast radius (traced before any change ships)

- ⭐ **A consequence the brief did not name concretely, and it is the sharpest one.** `cron_sync`
  builds `recompute_strategy_ids` from `per_strategy_stored` entries with `stored > 0`, then
  enqueues one `derive_broker_dailies` job per such strategy via `enqueue_compute_job`. The five
  private keys store nothing, so today they generate **no recompute enqueue at all**. Admitting
  `private` turns on that entire chain: sync -> stored trades -> `derive_broker_dailies` ->
  `compute_analytics_from_csv`. That is new production load and new analytics output for five live
  keys, and it is the concrete mechanism behind the vague "enqueue_compute_job follow-ons" item.
- **This refines, rather than contradicts, the ledger correction.** The daily ledger refresh runs
  regardless of this gap; what the gap suppresses is the **per-sync enqueue**. Both statements are
  true and they are about different triggers. Do not collapse them.
- ⚠️ **A THIRD stranding path already lives in that same block, documented and accepted:** the code
  comment states that a failed `enqueue_compute_job` is NOT re-driven by the next tick, because
  `last_sync_at` has already advanced. Distinct from `SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01`
  (that one loses TRADES; this one loses a RECOMPUTE and has a named recovery — the daily cascade
  or a user-triggered recompute). It goes live on these keys at the same moment. Name it in the
  plan; do not silently inherit it.
- **RPC load:** one additional `sync_trades` call per newly admitted strategy per tick.
- **`held` bucket:** the five keys currently land in `held` every tick. Admitting `private` should
  move them out of `held`. The `held` counter and the per-key WARNING volume should DROP — if a
  post-deploy measurement shows otherwise, the widening did not do what it claims.

### Area 4 — Ordering versus Phase 164.5.1.4 SYNCCURSOR (success criterion 4)

- ⭐ **DECISION: 164.5.1.4 SYNCCURSOR LANDS FIRST, then this phase ships the widening.**
- ⛔ **A DEFECT IN THE FIRST FORM OF THIS DECISION, CORRECTED BY MEASUREMENT.** It was first
  recorded as "gated on 164.5.1.4" without checking that 164.5.1.4 was SCHEDULED. It was not:
  the founder's queue is 164.5.1.3 -> 164.5.1.1 -> 164.5.1 -> 164.5.3 -> 164.5.4, and 164.5.1.4
  appears nowhere in it — because it did not EXIST when that queue was set (it was booked during
  164.5.1.2's review round, afterwards). A gate behind an unscheduled phase is not a gate, it is
  an indefinite deferral wearing one. Re-measured 2026-09-19: `164.5.1.4  in_queue: NO,
  disk: empty, plans: 0`.
- **RESOLUTION (founder, 2026-09-19): re-order the queue rather than weaken the decision.**
  New order: **164.5.1.4 -> 164.5.1.3 -> 164.5.1.1 -> 164.5.3 -> 164.5.4**. The alternative
  considered and declined was having THIS phase build its own stranding mitigation, which
  164.5.1.4 would then replace — the ratchet-shortcut pattern the repo answers with ordering
  rather than compromise.
- ⚠️ **164.5.1 DROPS OUT OF THE QUEUE — measured, not assumed.** It reads `phase_complete: true`,
  `verification_status: passed`, 9 plans. An earlier note claiming it needed only an
  `update_roadmap` tail step is STALE. `--only 164.5.1` would exit "already complete".
- **Reasoning, and it turns on the asymmetry between the two failure modes.** Today's state is a
  stall that is **LOUD and LOSSLESS**: the cursor holds, nothing is lost, and since 164.5.1.2 the
  condition is *observable* — a terminal `held` status, its own summary counter derived from
  `SYNC_STATUS_SUMMARY_KEYS`, membership in `_NON_OK_STATUSES`, and a WARNING-level per-key line.
  Widening before 164.5.1.4 replaces that with a path that is **SILENT and LOSSY**: on a key backing
  N strategies where one RPC succeeds and another raises, `synced_count > 0` holds, the cursor
  advances, and the failed strategy's window is permanently stranded with nothing recording which
  window was lost. Trading a loud lossless stall for a silent lossy one — on precisely the five keys
  the whole 164.5.1.x line was opened to protect — is a bad trade at any schedule.
- **This is the repo's own standing rule applied, not a new preference:** when something already
  planned removes the risk that would justify a shortcut, the answer is ORDERING, not compromise.
  164.5.1.4 is already in the ROADMAP with `deps_satisfied` and exists precisely to remove this
  risk.
- ⛔ **This phase does not flip the constant until 164.5.1.4 has landed.** Planning must therefore
  confirm at HEAD that 164.5.1.4's per-strategy resume marker EXISTS before the widening task
  runs — a dated claim that it landed is not evidence.
- **The ordering stays MECHANICAL, not a note:** the plan must leave a gate that FAILS if
  `ALLOWED_STRATEGY_STATUSES` contains `private` while 164.5.1.4's resume marker is absent. That
  gate outlives this phase and protects the invariant against a future editor's memory, which is
  why it is still required even though both phases now ship in sequence.

### Area 5 — `SYNC-HELD-CURSOR-REFETCH-COST-01` (success criterion 5)

- **It CAN now be closed by this phase**, because the widening ships here once 164.5.1.4 has
  landed, and admitting `private` ends the stall structurally: the five keys stop holding their
  cursor every tick, so the re-fetch window stops growing. ⛔ Close it by making that claim TRUE
  and MEASURED (the `held` counter for those keys drops), never by asserting it.
- ⚠️ If the widening does NOT ship in this phase for any reason, the entry must be RE-SCOPED with a
  named owner instead. It must never end this phase implying a closure that did not happen.
- **Re-scope to the part this phase CAN honestly close: the unbounded growth.** The held cursor
  pins `since_ms`, so `fetch_all_trades` re-fetches a window that grows monotonically and without
  limit. Bounding that window is independent of the admission decision, carries none of the
  stranding exposure, and makes the stall cheap to hold for as long as the ordering requires.
  ⚠️ Whether to ship that bound in THIS phase or hand it to 164.5.1.4 is a planning call for
  `gsd-plan-phase` to make on cost evidence — but the TODOS entry must end this phase describing
  the real state with a real owner either way.
- ⛔ Nothing here widens a ceiling, relaxes a floor, or adds an exemption.

### Claude's Discretion

- Plan shape, task decomposition and the precise form of the ordering gate.
- Whether the re-fetch bound (Area 5) ships here or in 164.5.1.4.
- Where the calibrated gate lives, provided it satisfies the anti-vacuity rule below.

</decisions>

<code_context>
## Existing Code Insights

### Re-measured at HEAD (2026-09-19, main at the MARKERGATE merge) — every claim below was checked, not inherited

- `ALLOWED_STRATEGY_STATUSES` — ONE declaration, ONE reader, two further comment mentions.
  CONFIRMED.
- `should_advance_cursor = (not trades) or synced_count > 0` — present and shipped. CONFIRMED.
- The `held` terminal status, `SYNC_STATUS_SUMMARY_KEYS` and `_NON_OK_STATUSES` — all present.
  CONFIRMED.
- `TestC0198CursorOnlyAdvancesWhenStored` — exactly THREE members
  (`test_C0198_zero_stored_does_not_advance_last_sync_at`,
  `test_C0198_partial_success_does_advance_last_sync_at`,
  `test_D03_empty_strategy_ids_with_trades_does_not_advance_cursor`). CONFIRMED.
- `SYNC-HELD-CURSOR-REFETCH-COST-01` and `SYNC-CURSOR-PER-KEY-STRANDS-STRATEGY-01` — both present
  in `TODOS.md`. CONFIRMED.
- `supabase/schema/functions/enqueue_poll_positions_for_all_strategies.sql` — byte-identical to
  `main`. CONFIRMED, and it must stay that way.
- ⚠️ `run_derive_broker_dailies_job` is **Python** (`analytics-service/services/job_worker.py`), not
  SQL. A first measurement against `supabase/migrations/20260614120000_derive_broker_dailies_kind.sql`
  proved nothing — that migration only mentions the name in a comment. Measure the Python body.

### Established patterns this phase must follow

- Terminal sync statuses are registered in `SYNC_STATUS_SUMMARY_KEYS` and counters are DERIVED from
  it rather than hand-written — any new status follows that shape.
- Per-strategy failures are best-effort and surfaced in the result envelope
  (`strategy_errors`, `recompute_enqueue_errors`) so the summary alarm sees them.
- `enqueue_compute_job` is dedup-safe via the partial unique index
  `compute_jobs_one_inflight_per_kind_strategy`.

### Integration points

- `cron_sync` key hydration (the `strategy_ids` comprehension) — the single reader.
- `_sync_single_key` — the fan-out loop, the cursor gate and the `held` classification.
- The recompute tail — `recompute_strategy_ids` / `enqueue_compute_job`.

</code_context>

<specifics>
## Specific Ideas

- The ordering gate must be MECHANICAL. A comment or a TODOS line is not a gate; a future editor
  flipping the constant must make something go RED.
- Any post-deploy claim about the five keys must be bound to a measurement, not asserted. The
  `held` counter dropping is the observable that would confirm a future widening worked.
- ⛔ Production evidence is now available for the first time: 164.5.1.2 is LIVE (verified — Railway
  `/health` reports the MARKERGATE merge SHA). Whether the five keys are actually reporting `held`
  every tick is now measurable rather than predicted, and a PROD read is a
  `checkpoint:human-action` — the founder enters credentials, no agent ever does.

</specifics>

<deferred>
## Deferred Ideas

- The per-strategy resume marker / replay marker itself — Phase 164.5.1.4 SYNCCURSOR owns it.
- The recompute-enqueue stranding path (a failed `enqueue_compute_job` is never re-driven) — named
  here so it is not silently inherited; it belongs with 164.5.1.4's cursor work, not with the
  admission verdict.
- `enqueue_ledger_composite_refresh` and `FANOUT-COHORT-SIBLING-COMPOSITE-01` — out of scope by
  founder decision 2026-09-17.
- Admitting `rejected` or `archived` — explicitly refused in Area 1, not deferred.

</deferred>
