---
phase: 140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route
plan: 04
type: execute
wave: 3
depends_on: [140.1.2-01, 140.1.2-02, 140.1.2-03]
files_modified:
  - analytics-service/tests/test_process_key_200_discriminator.py
  - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-VERIFICATION.md
  - analytics-service/docs/STATUS_CONTRACT.md
  - TODOS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-VERIFICATION.md  # CONDITIONAL — edited only if it already exists; otherwise the disposition is handed to the verifier via the SUMMARY (Task 3 item 3)
  - .planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-CONTEXT.md
autonomous: true
requirements: [PYAPIFIX2-05, PYAPIFIX2-06]
must_haves:
  truths:
    - "Deleting a _SHAPES row turns a test RED naming the uncovered fingerprint — OBSERVED, restored from a /tmp cp copy (the 141→140-passed silent shrink is dead)"
    - "The M-15 AST fence still reddens on a 7th 200-capable return — the new oracle did not weaken the existing one (probe OBSERVED)"
    - "140.1.1-VERIFICATION.md reads status: gaps_found, with the blanket 'no already-correct test was removed' sentence corrected and the grep -c carve-out evidence restated as marker-exists-not-list-complete"
    - "STATUS_CONTRACT.md: the not-seam-reachable list no longer points at a live 424 arm; S-11's line refs are correct; the classes heading matches its five-row table — every replacement coordinate RE-DERIVED at current HEAD by behaviour, never computed as old+N"
    - "PYAPIFIX2-01 is ticked with an HONEST SPLIT disposition, not a bare [x]: REQUIREMENTS.md, ROADMAP SC1 and 140.1.2-VERIFICATION.md all record 'PYTHON HALF CLOSED (7/7 sites carry code+recoverable); RENDER HALF → OB-1/TS-35, owner 140.3' — because SC1's render half is deferred, so an unqualified tick would make ROADMAP SC1 literally false"
    - "Phase gate: unscoped pytest reconciles arithmetically, mypy 89 files clean, zero TS files in the phase diff, 0 new type: ignore, grep -rn MUTANT → 0"
  artifacts:
    - path: "analytics-service/tests/test_process_key_200_discriminator.py"
      provides: "the _SHAPES↔router-AST binding oracle (two artifacts: router source AST vs live HTTP bodies)"
    - path: "analytics-service/docs/STATUS_CONTRACT.md"
      provides: "corrected not-seam-reachable list, S-11 refs, five-classes heading"
  key_links:
    - from: "analytics-service/tests/test_process_key_200_discriminator.py"
      to: "analytics-service/routers/process_key.py"
      via: "AST-derived dict fingerprints confronted with real response bodies driven through _SHAPES reach fns"
      pattern: "_EXPECTED_200_FINGERPRINTS|_SHAPES"
---

<objective>
Close PYAPIFIX2-05 (the 200-discriminator corpus cannot silently shrink — one assertion binding
_SHAPES to the router's AST, never a count, never a same-file literal pair) and PYAPIFIX2-06
(artifact truth: four named items, each re-derived at source), then run the PHASE GATE.

Purpose: the corpus fence closes the one observed silent-shrink survivor; the artifact
corrections stop the phase's own evidence from lying to the next reviewer.
Output: the new oracle + two observed probes; four artifact corrections with before/after quotes;
the phase-wide gate receipts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-CONTEXT.md
@.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-RESEARCH.md  (§PYAPIFIX2-05 incl. OQ-2, §PYAPIFIX2-06 items 1–5, C-6, C-7, P-4, P-5, P-6)
@.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-PATTERNS.md  (§PYAPIFIX2-05, M-2, M-8, OB routing)

Wave 3: plans 01–03 have landed. exchange.py, internal.py, portfolio.py, error_contract.py line
numbers have ALL shifted — every STATUS_CONTRACT coordinate MUST be re-derived at current HEAD by
reading the line, NEVER by arithmetic (the old+18 diagnosis was REFUTED; most drift pre-dates the
phase). routers/process_key.py is untouched by this phase — the discriminator fence's subject is
stable.

The census item (REQUIREMENTS' fifth PYAPIFIX2-06 item) was SETTLED by the planner and landed in
plan 02 as a fenced code-comment exception — this plan RECORDS the settlement and does not touch
error_contract.py.
</context>

<interfaces>
Verified at 39688d69 in tests/test_process_key_200_discriminator.py:
- `_SHAPES` (≈:372-379): six rows `(id, reach_fn, expected_ok)` — consumed ONLY by the
  parametrised test; the M-15 fence never reads it (THE gap: deleting a row was observed to
  survive, 141→140 passed).
- `_ROUTER_SRC` (≈:443) = pathlib.Path(process_key_router.__file__);
  `_SHAPE_BEARING_FUNCTIONS` (≈:447) = ("process_key", "_wizard_duplicate_reply",
  "_run_validate_only"); `_EXPECTED_200_FINGERPRINTS` (≈:453-466) = frozenset of 8 (5 `dict:` +
  3 `call:` incl. "call:_envelope_error"); the AST walker + 200-capable filter (≈:547-611);
  companion tests: set-equality fence (≈:592), non-200 exclusion (≈:613).
- Sanctioned oracle direction (the file's own comment ≈:434-437): ACTUAL derived from the
  router's source via ast; EXPECTED a literal typed in the file — a pinned contract confronted
  with the code's real shape.

PYAPIFIX2-06 items (each verified this session at 39688d69 — re-verify at current HEAD):
1. 140.1.1-VERIFICATION.md — frontmatter `status: passed` (line 4); the false blanket sentence
   at ≈:239-241 ("No already-correct test was weakened or removed. … the only deletions in test
   files are the five assertions listed above"); the Direction-2 table ≈:231-237 (missing the
   deleted Python test FUNCTION `test_pyapi_10a_exactly_six_shapes_are_covered`, which IS named
   in the Self-Referential Oracle Scan ≈:251); frontmatter `deferred[0].evidence` = the
   `grep -c "BLOCKED-BY: TS-05" … → 1` claim.
2. STATUS_CONTRACT.md not-seam-reachable bullet (≈:344-345): cites `routers/exchange.py:453,491,553`
   as "/api/fetch-trades, no TS caller" — WRONG at ALL THREE (at 39688d69: :453 a closing paren,
   :491 a LIVE 424 arm on /api/validate-key, :553 a comment rule). Intended members: the
   deliberate error arms inside the /api/fetch-trades handler — re-derive by behaviour ("the
   deliberate error sites inside the fetch_trades handler that carry no S-number"), read each.
3. S-11 row (≈:258): cites `routers/internal.py:414`; the except arm and the
   ADAPTER_INIT_FAILED raise are lower — re-derive both at current HEAD by code text.
4. Heading `## 1. The four classes` (≈:27) heads a FIVE-row table (CALLER, CALLER-THROTTLED,
   CALLER'S EXCHANGE, SERVICE-TRANSIENT, SERVICE-PERMANENT) — one-word fix.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PYAPIFIX2-05 — bind _SHAPES to the router AST; two falsifiability probes OBSERVED</name>
  <files>analytics-service/tests/test_process_key_200_discriminator.py</files>
  <read_first>
    - analytics-service/tests/test_process_key_200_discriminator.py — FULL read of :366-660 (the _SHAPES table, reach fns, the AST walker, the fence, the exclusion test, and the :420-437 history/direction comments)
    - 140.1.2-RESEARCH.md §PYAPIFIX2-05 (designs a/b + OQ-2) and P-5/P-6
  </read_first>
  <behavior>
    - OQ-2 FIRST, empirically: for each _SHAPES row, drive reach(client) and print
      `"dict:" + ",".join(sorted(body.keys()))` beside the AST `dict:` fingerprint set. Record
      both sets verbatim in the SUMMARY. Decision rule: if wire keys == AST fingerprints for
      every 200-capable shape (shape 6 delegating to _envelope_error handled per design (a)),
      adopt design (a): add "_envelope_error" to _SHAPE_BEARING_FUNCTIONS, extend
      _EXPECTED_200_FINGERPRINTS with its dict fingerprint (a literal typed in the file), and
      assert SET EQUALITY between the AST dict: fingerprints and the wire-derived fingerprints.
      If FastAPI response_model post-processing perturbs ANY shape's key set, fall back to
      design (b): coverage (every AST dict: fingerprint ⊇-covered by some driven _SHAPES row),
      with the observed perturbation documented in the test docstring. NEVER assume — check
      before pinning.
    - The new test: side A = AST-derived dict: fingerprints (from _ROUTER_SRC); side B = wire
      fingerprints from driving every _SHAPES row. Assert every side-A fingerprint is covered by
      side B, failure message NAMING the uncovered fingerprint and saying why it matters
      ("a router-declared 200 shape has no reaching fixture — the corpus silently shrank").
      Two different artifacts ⇒ not self-referential. FORBIDDEN shapes (P-5): len(_SHAPES) == N;
      {s[0] for s in _SHAPES} == literal-id-set — the deleted test in a costume.
  </behavior>
  <action>
    Implement per the decision rule above. Then BOTH probes, each observed first-hand:
    (1) cp the test file AND routers/process_key.py to /tmp scratch copies. Delete one _SHAPES
    row (pick a mid-table row) → run the file → OBSERVE RED naming the uncovered fingerprint →
    restore the test file from the /tmp copy (NEVER git checkout/stash/clean — standing P-6
    rule) → re-run GREEN.
    (2) Add a 7th 200-capable `return {"mutant": True, "ok": True, "code": None}`-style dict to
    routers/process_key.py (marked MUTANT) → run → OBSERVE the M-15 set-equality fence STILL
    reddens (proving this task did not weaken it) → restore from the /tmp copy → re-run GREEN →
    `grep -rn MUTANT analytics-service/` → 0, `git status` clean.
    Commits: `test(140.1.2-04): bind _SHAPES to the router AST — corpus cannot silently shrink
    (PYAPIFIX2-05)` (single commit; the RED here is the probe, observed and documented, since the
    new oracle is born green against unmutated source — record both probe outputs verbatim).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_process_key_200_discriminator.py -q</automated>
  </verify>
  <acceptance_criteria>
    - OQ-2 empirical print (both sets, verbatim) in SUMMARY BEFORE the pin; chosen design stated with the observed reason.
    - Probe 1 RED output quoted (names the uncovered fingerprint); probe 2 output quoted (fence still bites); both restores verified (`git status` clean, `grep -rn MUTANT` → 0).
    - The new oracle's two sides come from two artifacts (router AST vs live HTTP) — no _SHAPES-vs-literal comparison anywhere: `grep -n "len(_SHAPES)" tests/test_process_key_200_discriminator.py` → 0.
    - File suite green post-restore.
  </acceptance_criteria>
  <done>ROADMAP criterion 5: a deleted _SHAPES row can no longer ship green — proven by observation, not assertion.</done>
</task>

<task type="auto">
  <name>Task 2: PYAPIFIX2-06 — the four artifact corrections, each re-derived at source; obligations routed</name>
  <files>.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-VERIFICATION.md, analytics-service/docs/STATUS_CONTRACT.md, TODOS.md, .planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-CONTEXT.md</files>
  <read_first>
    - 140.1.1-VERIFICATION.md — frontmatter, the Direction-2 table + blanket sentence, the deferred[0] evidence block (read the ACTUAL current text; quote before editing)
    - analytics-service/docs/STATUS_CONTRACT.md — the not-seam-reachable bullet, the S-11 row, the classes heading (quote each at CURRENT head)
    - analytics-service/routers/exchange.py — the fetch_trades handler END TO END (to re-derive its deliberate error arms by behaviour)
    - analytics-service/routers/internal.py — the ADAPTER_INIT_FAILED except arm (re-derive both line refs)
    - TODOS.md current structure (it has intentional uncommitted parent edits — APPEND only, never reflow)
  </read_first>
  <action>
    Item 1 (140.1.1-VERIFICATION.md — Edit tool; .planning is gitignored, NEVER git add):
    flip frontmatter to `status: gaps_found`; replace the blanket sentence with the two
    supportable facts (per C-7): a whole test FUNCTION
    (test_pyapi_10a_exactly_six_shapes_are_covered) was deleted and is absent from the
    Direction-2 table (it appears only in the oracle scan); it was self-referential as an oracle
    AND was the only _SHAPES fence — closed by 140.1.2 plan 04. Restate deferred[0].evidence:
    the grep -c proves a marker EXISTS, not that the enumerated list was complete — and it was
    not (three further arms, closed by 140.1.2 plan 03). Add nothing else; do not re-litigate
    other verdicts.

    Item 2 (STATUS_CONTRACT.md): replace the three wrong coordinates in the not-seam-reachable
    bullet with the re-derived deliberate error arms of the fetch_trades handler — derived by
    READING the handler at current HEAD (expected neighbourhood from the planning session:
    the bare-503 get_kek arm, the fetch-failure 500 arm, and siblings — but the executor's own
    read is authoritative). Print and quote each replacement line in the SUMMARY (the
    plan-checker rejects unquoted replacements per RESEARCH's verification discipline).
    Item 3: correct the S-11 row's internal.py refs (except arm + raise line, re-derived).
    Item 4: heading "The four classes" → "The five classes" (one word).
    ⚠️ NOTHING ELSE in STATUS_CONTRACT.md — OB-7's portfolio.py:2242/:2446 drift is
    PRE-EXISTING, explicitly out of the fence → TODOS.md instead. No number recomputed as
    old+N anywhere.

    Census settlement record: state in the SUMMARY that the fifth REQUIREMENTS item (the 6/7→6/10
    census) was settled by the planner as a fenced comment edit landed in plan 02 (it lives in
    services/error_contract.py, not STATUS_CONTRACT.md — the REQUIREMENTS location claim was
    wrong), so THIS plan touches no code.

    Routing (append-only edits):
    - TODOS.md: two entries — (a) M-2: tests/test_mt5_validate.py carries 8 self-referential
      detail assertions (list the 8 line refs from PATTERNS M-2; pre-existing, do-not-copy);
      (b) OB-7: STATUS_CONTRACT.md portfolio.py:2242/:2446 coordinate drift, pre-existing.
    - 140.2-CONTEXT.md: append one line under its deferred/notes area — OB-8: breaker
      amplification classification is correct; the harm is the global breaker:railway key 140.2
      replaces; record as a 140.2 acceptance note (source: 140.1.2 review evidence).
  </action>
  <verify>
    <automated>grep -q "gaps_found" ".planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-VERIFICATION.md" && grep -q "five classes" analytics-service/docs/STATUS_CONTRACT.md && ! grep -q "exchange.py:453,491,553" analytics-service/docs/STATUS_CONTRACT.md && ! grep -q "internal.py:414" analytics-service/docs/STATUS_CONTRACT.md && ! grep -q "four classes" analytics-service/docs/STATUS_CONTRACT.md && echo ARTIFACT-TRUTH-HELD</automated>
  </verify>
  <acceptance_criteria>
    - Every edited line quoted before/after in the SUMMARY (per-item table).
    - `grep -n "exchange.py:453,491,553" analytics-service/docs/STATUS_CONTRACT.md` → 0.
    - No edit outside the four named items + the two routing appends; `git diff --stat` for the
      STATUS_CONTRACT commit shows 1 file.
    - TODOS.md edits are pure appends (existing uncommitted parent edits byte-preserved — verify
      with `git diff TODOS.md` before and after: prior hunks unchanged).
    - The census settlement recorded; error_contract.py untouched by this plan.
  </acceptance_criteria>
  <done>ROADMAP criterion 6: the phase's artifacts state only what they can support; nothing re-widened.</done>
</task>

<task type="auto">
  <name>Task 3: PHASE GATE — full-suite arithmetic, mypy, TS no-change proof, ledger tick</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - Each prior plan's SUMMARY (test-count additions for the arithmetic)
    - .planning/REQUIREMENTS.md:94-107 (the PYAPIFIX2 block) and the traceability row
  </read_first>
  <action>
    Run and record ALL of, first-hand:
    1. `cd analytics-service && python3 -m pytest -q` — reconcile EXACTLY:
       expected passed = 4743 + Σ(new tests from plans 01–04); skips still 96; 0 failed. Any
       delta is traced (the module-level-skip lesson), never waved through.
    2. `python3 -m pytest --collect-only -q 2>&1 | tail -2` — collection errors 0.
    3. `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` —
       Success, 89 files (the phase added no new module under the gate paths).
    4. TS fence proof: `git diff --name-only 39688d69..HEAD | grep -c "^src/"` → 0 AND
       `npx tsc --noEmit` exit 0 AND `npm run lint` → 0 errors (no-change proof; full vitest not
       required since zero TS files changed — state this rationale).
    5. `git diff 39688d69..HEAD | grep -c "type: ignore"` → 0 new.
    6. `grep -rn MUTANT analytics-service/ src/` → 0; `git status --porcelain` shows only the
       expected TODOS.md modification + pre-existing untracked nautilus_factsheet.py +
       .planning changes (gitignored).
    Then close the ledgers HONESTLY. ⚠️ **B-1 — THE PREVIOUS INSTRUCTION WAS THE DEFECT
    PYAPIFIX2-06 EXISTS TO CORRECT, REPRODUCED INSIDE THIS PHASE.** It said: tick `- [x]` on
    PYAPIFIX2-01..06, carry a disposition note ONLY on -02's line, and set traceability
    "Complete". But **PYAPIFIX2-01 / ROADMAP SC1 are only HALF closed** — plan 03 delivers the
    Python half (7/7 sites emit `code` + `recoverable`) and explicitly hands the RENDER half to
    140.3 as OB-1 / ledger row TS-35. ROADMAP SC1 demands *"a test must prove that a
    Binance-maintenance-shaped failure during key-connect no longer renders as `UNKNOWN`/500 …
    with no retry affordance"* — **the RENDER assertion is not delivered by this phase.** A bare
    `[x]` there makes SC1 literally false, which is exactly the artifact overstatement this phase
    is correcting in 140.1.1. Do not repeat it one document later.

    **REQUIRED (all four, each quoted before/after in the SUMMARY):**
    1. `.planning/REQUIREMENTS.md` — tick PYAPIFIX2-01..06, and on **PYAPIFIX2-01's line** record
       verbatim: **`PYTHON HALF CLOSED (7/7 sites carry code+recoverable); RENDER HALF → OB-1,
       owner 140.3`** (cite the ledger row `TS-35` that carries it). Keep the existing disposition
       note on **-02's** line (fixed, or NOT-REPRODUCIBLE-with-evidence, per plan 01's observed
       gate). Set the traceability row to **`Complete (PYAPIFIX2-01 Python half only — render half
       owned by 140.3 / TS-35)`**, NOT a bare "Complete".
    2. `.planning/ROADMAP.md` — append the SAME annotation to the **Success Criteria bullet 1**
       of Phase 140.1.2 (the bullet beginning *"A venue-transient fault answers a typed,
       classifiable error at EVERY consumer"*). One clause, appended; do not rewrite the bullet
       and do not touch criteria 2–6.
    3. `.planning/phases/140.1.2-.../140.1.2-VERIFICATION.md` — **conditional, and fail-loud
       either way.** This file is the VERIFIER's artifact and may not exist yet when this plan
       runs. Therefore: **if it exists**, add the disposition sentence to it in place (Edit tool).
       **If it does not exist**, do NOT create a stub — a stub with a `status:` field would be
       read as a verdict this plan has no authority to give. Instead emit, at the TOP of this
       plan's SUMMARY, a block headed exactly:
       `VERIFIER-MUST-MIRROR (B-1): PYTHON HALF CLOSED (7/7 sites carry code+recoverable); RENDER HALF → OB-1, owner 140.3`
       so `/gsd:verify-work` copies it verbatim instead of re-deriving it. The same sentence is
       already recorded in `140.1.2-VALIDATION.md` §Artifact-Truth Gate as the durable backstop.
       State in the SUMMARY which of the two branches was taken.
    4. Tick the four plan checkboxes in ROADMAP's `Plans:` list for this phase.

    **W-1 — the same artifact-truth principle applies to PYAPIFIX2-06's own line.** REQUIREMENTS
    describes the census item as *"In `docs/STATUS_CONTRACT.md`: … the 6/7 dependency census"*.
    **That location clause is FALSE**: the census is a CODE COMMENT at
    `analytics-service/services/error_contract.py` ≈:158-163, not in `STATUS_CONTRACT.md`. When
    ticking PYAPIFIX2-06, correct the location clause on its line in the same edit and note the
    settlement: *"census lives in services/error_contract.py (code comment), not
    STATUS_CONTRACT.md; corrected as a fenced comment-only exception in plan 02."* Leaving the
    false location in a ticked requirement is the identical defect class.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest -q 2>&1 | tail -3 && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/ 2>&1 | tail -1 && cd .. && npx tsc --noEmit && git diff 39688d69..HEAD --name-only | grep -c "^src/" | grep -qx 0 && echo TS-FENCE-HELD</automated>
  </verify>
  <acceptance_criteria>
    - All six gate outputs quoted verbatim in the SUMMARY with the reconciliation arithmetic shown.
    - REQUIREMENTS.md checkboxes + traceability updated; nothing under .planning staged.
    - **B-1 artifact-truth gate (all four, or the phase is not closeable):**
      `grep -n "RENDER HALF" .planning/REQUIREMENTS.md` → the PYAPIFIX2-01 line; `grep -n "RENDER HALF" .planning/ROADMAP.md` → the SC1 bullet;
      and for the VERIFICATION mirror, EXACTLY ONE of: `grep -n "RENDER HALF" .planning/phases/140.1.2-*/140.1.2-VERIFICATION.md` → the mirrored disposition (file existed), OR `grep -n "VERIFIER-MUST-MIRROR" .planning/phases/140.1.2-*/140.1.2-04-SUMMARY.md` → the handoff block (file did not exist). Name which branch was taken;
      and the traceability row is NOT the bare string `Complete`, pinned by a RUNNABLE command
      like the other three sub-checks rather than by prose: `grep -n "| PYAPIFIX2-01..06 |"
      .planning/REQUIREMENTS.md` → exactly one row (today `| PYAPIFIX2-01..06 | Phase 140.1.2 |
      Pending |` at ≈:281), whose status cell must AFTER this task contain the qualifier —
      `grep -c "PYAPIFIX2-01..06 .*Python half only" .planning/REQUIREMENTS.md` → **1**, and
      `grep -c "PYAPIFIX2-01..06 | Phase 140.1.2 | Complete |" .planning/REQUIREMENTS.md` → **0**
      (the bare-`Complete` form must NOT appear). Quote the matched row verbatim in the SUMMARY.
    - **W-1:** PYAPIFIX2-06's line no longer claims the census lives in `docs/STATUS_CONTRACT.md`
      — `grep -n "STATUS_CONTRACT.md.*dependency census" .planning/REQUIREMENTS.md` prints nothing,
      and the corrected clause naming `services/error_contract.py` is quoted in the SUMMARY.
    - Zero production TypeScript files in the phase diff (the fence held).
  </acceptance_criteria>
  <done>Phase-wide gates green with reconciled arithmetic; the phase is verifiable by the checker without re-derivation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test suite → routers/process_key.py source | the AST fence reads production source; a weakened fence silently widens the wire surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1.2-10 | Repudiation | phase artifacts overstating evidence | mitigate | the PYAPIFIX2-06 corrections themselves + per-line before/after quotes |
| T-140.1.2-11 | Tampering | mutation probes left in the tree | mitigate | /tmp cp restore discipline + grep MUTANT → 0 + git status clean (both asserted) |
| T-140.1.2-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
Both probes observed · OQ-2 settled empirically before pinning · four artifact items quoted
before/after · gates run first-hand with arithmetic reconciliation · TS fence proven by diff.
</verification>

<success_criteria>
ROADMAP criteria 5 and 6 observably true; all six PYAPIFIX2 requirements ticked with dispositions;
the phase diff contains zero TypeScript production changes and zero scope beyond the surviving
findings.
</success_criteria>

<output>
Create `.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-04-SUMMARY.md`
with probe outputs, the OQ-2 print, the artifact before/after table, and all gate receipts.
Never `git add` anything under `.planning/`.
</output>
