---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 09
type: execute
wave: 5
depends_on: ["164.9.1-01", "164.9.1-07"]
files_modified:
  - supabase/migrations/<ts3>_bridge_outcomes_invariant_comments.sql
  - src/__tests__/match-decisions-xor-rls.test.ts
autonomous: true
coupling_justified: ["164.9.1-08: order-independent: 08 runs mocked-RPC unit tests only and touches no database, local stack, migration, env var or shared config, while this plan alone boots the one host stack and adds M3; the two are file-disjoint and wave 5 executes each plan in its own worktree (use_worktrees true in .planning/config.json), so they share no git index either; both orders give the same result"]
requirements:
  - "[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]"

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-13 / criterion 5: the written answer is recorded verbatim in this plan, in M3's header and in the test file's header: the CURRENT bridge_outcomes uniqueness invariant is two-part — (i) bridge_outcomes_allocator_match_decision_unique UNIQUE (allocator_id, match_decision_id), one outcome per decision (migration 081); (ii) bridge_outcomes_legacy_per_strategy_holding_when_md_null, a PARTIAL unique on (allocator_id, strategy_id, COALESCE(original_holding_ref, '')) WHERE match_decision_id IS NULL (migration 083), restoring 072's per-strategy guarantee only for rows whose decision was nulled out. Two strategy-sourced outcomes for the same (allocator, strategy) under two DIFFERENT decisions are ALLOWED by design; the old arm's 23505 expectation described migration 072's world."
    - "D-14: the arm is rewritten, not deleted: (a) two different decisions, same (allocator, strategy) -> both INSERTs succeed; (b) a second outcome for the SAME match_decision_id -> code 23505 AND the message names bridge_outcomes_allocator_match_decision_unique; (c) two outcomes with match_decision_id NULL and the same (allocator, strategy) -> code 23505 AND the message names bridge_outcomes_legacy_per_strategy_holding_when_md_null; every assertion that named the retired bridge_outcomes_unique_per_strategy_holding is re-pointed and every code-OR-message disjunction becomes a conjunction; each is calibrated neuter -> RED -> restore."
    - "D-15 / D-25: M3 (the third and last migration of the phase) rewrites the stale COMMENT ON TABLE public.bridge_outcomes and COMMENT ON COLUMN public.bridge_outcomes.original_holding_ref so both name the two current constraints and neither names the retired index; its self-verify is catalog-only (D-07)."
    - "The rewrite asserts DOCUMENTED design (081's constraint and 083's partial index, cited in the header), never 'whatever the database did', and the false 'strict superset' wording is repeated nowhere."
  artifacts:
    - path: "supabase/migrations/<ts3>_bridge_outcomes_invariant_comments.sql"
      provides: "M3: table + column comments, catalog-only verify"
    - path: src/__tests__/match-decisions-xor-rls.test.ts
      provides: "D-14 arms (a), (b), (c) with conjunction assertions"
  key_links:
    - from: "match-decisions-xor-rls arms"
      to: "bridge_outcomes_allocator_match_decision_unique / bridge_outcomes_legacy_per_strategy_holding_when_md_null"
      via: "PostgREST error .code and .message"
      pattern: "toContain\\(\"bridge_outcomes_"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> Wave coupling that is not a file overlap: this plan boots the SAME single local stack as plans 02, 03 and 07
> (one stack per host), and M3's timestamp must sort after M2's. So it runs after plan 07. Plan 08 shares this
> wave; it runs mocked unit tests only and never boots the stack.

<objective>
Answer the migration-081 intent question in writing and make the arm assert the current invariant
(ROADMAP criterion 5, `[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]` item b), and correct the two catalog
comments that still describe the retired index (D-13, D-14, D-15).

Output: M3, the rewritten live-DB arms, and calibration evidence for each tightened assertion.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-RESEARCH.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: M3 — the table and column comments name the two current constraints (D-13, D-15, D-25, D-07)</name>
  <files>supabase/migrations/&lt;ts3&gt;_bridge_outcomes_invariant_comments.sql</files>
  <read_first>
    - `supabase/migrations/20260426131719_bridge_outcomes_relax_for_voluntary.sql` (081) and `supabase/migrations/20260426131721_commit_scenario_batch_race_fix.sql` (083): the constraint and the partial index, by name.
    - `supabase/migrations/20260421154029_match_decisions_original_holding_ref.sql` (the stale column COMMENT) and `supabase/migrations/20260418060747_bridge_outcomes.sql` (the stale table COMMENT). Re-grep for any later `COMMENT ON TABLE public.bridge_outcomes` / `COMMENT ON COLUMN … original_holding_ref`: `grep -ln "COMMENT ON \(TABLE\|COLUMN\).*bridge_outcomes" supabase/migrations/*.sql`.
    - `supabase/migrations/20260823120000_revoke_api_keys_insert.sql` `$verify$` block (the `col_description` read and stale-text absence arm) — the verify SHAPE only; not its read-modify-append comment idiom.
    - `scripts/lint-migration-data-dependence.mjs` header.
  </read_first>
  <action>
    Create `supabase/migrations/<ts3>_bridge_outcomes_invariant_comments.sql`, `<ts3>` the real UTC time, strictly
    newer than M2 and every other file, name matching `^[0-9]+_[a-z0-9_]+\.sql$`. Header: the D-13 written answer
    verbatim; the two stale texts being replaced (quoted); why 081's "strict superset" claim is false (074's
    `match_decisions` key includes `decision`) — stated without repeating that wording as a claim.
    Statements: `SET LOCAL lock_timeout = '3s';` then plain-literal `COMMENT ON TABLE public.bridge_outcomes IS '…'`
    and `COMMENT ON COLUMN public.bridge_outcomes.original_holding_ref IS '…'`, each naming
    `bridge_outcomes_allocator_match_decision_unique` and `bridge_outcomes_legacy_per_strategy_holding_when_md_null`
    and never `bridge_outcomes_unique_per_strategy_holding`. The column comment says the column feeds the md-NULL
    partial index via the sync trigger. Self-verify DO block (catalog-only): read
    `obj_description('public.bridge_outcomes'::regclass, 'pg_class')` and `col_description` via `pg_attribute`
    for `original_holding_ref`; RAISE if either is NULL, lacks either current name, or contains the retired name;
    also RAISE if `to_regclass('public.bridge_outcomes_legacy_per_strategy_holding_when_md_null')` is NULL or no
    `pg_constraint` row named `bridge_outcomes_allocator_match_decision_unique` exists (catalog reads only). Single-literal
    RAISE formats; no BEGIN/COMMIT; no `cron.schedule`; no table-data read. No function body changes, so no
    `-- prod-body-ack:` and no snapshot regeneration are needed; confirm `npx tsx scripts/dump-sql-functions.ts --check` stays green.
  </action>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.9.1-09"; mkdir -p "$S"; M3="$(ls supabase/migrations/*_bridge_outcomes_invariant_comments.sql)"; M2="$(ls supabase/migrations/*_allocator_sync_restore_inflight_prefetch.sql)"; test "$(basename "$M3")" \> "$(basename "$M2")" && grep -v '^\s*--' "$M3" | grep -qF 'COMMENT ON TABLE public.bridge_outcomes' && grep -v '^\s*--' "$M3" | grep -qF 'COMMENT ON COLUMN public.bridge_outcomes.original_holding_ref' && ! grep -qiE 'cron\.schedule|strict superset' "$M3" && npx tsx scripts/dump-sql-functions.ts --check && node scripts/lint-migration-data-dependence.mjs > "$S/lint.log" 2>&1; RC=$?; cat "$S/lint.log"; test "$RC" = 0 && grep -qE '\b0 NEW\b' "$S/lint.log" && PB="$(sed -nE 's/^PHASE-BASE: ([0-9a-f]{40})$/\1/p' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md)" && test -n "$PB" && git diff --quiet "$PB" -- scripts/lint-migration-data-dependence-baseline.txt && echo M3-OK</automated>
    <fails_when>no "M3-OK": M3 missing or not newer than M2, a comment statement missing, cron.schedule or the "strict superset" wording present, the snapshot check red, or the data-dependence linter reports a NEW refusal / its baseline differs from the phase base (plan 01's `PHASE-BASE:` sha, compared with the working tree, so a committed change fails too).</fails_when>
  </verify>
  <acceptance_criteria>
    - One file matches `supabase/migrations/*_bridge_outcomes_invariant_comments.sql`, lexically after M2.
    - Both COMMENT statements name `bridge_outcomes_allocator_match_decision_unique` and `bridge_outcomes_legacy_per_strategy_holding_when_md_null`; the retired name `bridge_outcomes_unique_per_strategy_holding` appears only in the header's quotation of the stale text and in the DO block's absence arm.
    - `ls supabase/migrations/ | grep -cE '_(fanin_initial_status_10param|allocator_sync_restore_inflight_prefetch|bridge_outcomes_invariant_comments)\.sql$'` returns 3 and no other migration file was added by this phase (D-25, FRONTIER_EXEMPT_CEILING).
    - Linter `0 NEW`; baseline unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">Comment-only migration; a later COMMENT restores any text.</reversibility>
  <done>The catalogue's own record of the invariant is correct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the XOR arms to the current invariant with conjunction assertions, calibrated (D-13, D-14)</name>
  <files>src/__tests__/match-decisions-xor-rls.test.ts</files>
  <read_first>
    - `src/__tests__/match-decisions-xor-rls.test.ts` in full: header, fixture scaffold (`beforeAll`, stable synthetic UUIDs, `createdMatchDecisionIds` / `createdBridgeOutcomeIds`, `afterAll` cleanup order), the arm "bridge_outcomes widened UNIQUE: same (allocator+strategy+holding_ref) second INSERT fails with 23505" and the arm "bridge_outcomes widened UNIQUE: strategy-sourced rows preserve (allocator+strategy) 1-per-pair guarantee via COALESCE('')", and both `code === "23505" ||` disjunctions.
    - `164.9.1-RESEARCH.md` "Q7 — bridge_outcomes invariant" (measured constraint-firing table; the sync trigger forces NULL holding for md-NULL rows; the kind CHECKs require strategy_id when md is NULL).
    - `src/__tests__/bridge-outcomes-voluntary-schema.test.ts` (existing catalog assertions — do not duplicate).
  </read_first>
  <behavior>
    - (b) Re-pointed arm: second outcome for the SAME decision -> `error.code === "23505"` AND `error.message` contains `bridge_outcomes_allocator_match_decision_unique`.
    - (a) Rewritten arm: two strategy-sourced decisions for the same (allocator, strategy) -> BOTH outcome INSERTs succeed (error null, two rows returned).
    - (c) New arm: two outcomes with `match_decision_id: null`, `strategy_id` set, same allocator -> second INSERT `error.code === "23505"` AND message contains `bridge_outcomes_legacy_per_strategy_holding_when_md_null`; rows tracked for cleanup.
  </behavior>
  <action>
    Replace the header's description of the retired index with the D-13 written answer verbatim, citing 081 and 083
    as the authority (the TODOS entry forbids asserting observed behaviour; this asserts documented design). Rewrite
    the arms per <behavior>. Rename titles whose wording becomes false (the "preserve … 1-per-pair guarantee" arm now
    asserts both succeed); the `describe` title may keep or drop "widened UNIQUE" — plan 10 removes the ledger's K3 token
    either way. Replace every `code === "23505" || message.includes(…)` with two `expect`s (code `toBe("23505")`,
    message `toContain(<current constraint>)`). Track every created row for cleanup, assert only about the arm's own rows.

    Re-boot the lane (`bash scripts/local-stack/run.sh down` then `up`; the `baseline-replay:` line names M1, M2 and M3,
    proving M3's DO block passed) and run the file. Calibrate each tightened assertion on the working copy with a byte
    backup: swap the two constraint-name literals in (b) and (c) -> both RED; make (a) expect an error -> RED; restore
    with `cp`, `cmp`, GREEN. Record each (test title + RED/GREEN only) in the SUMMARY.
    Machine-checkable record: write into this plan's SUMMARY (create it if absent) exactly one line per calibration,
    `CALIBRATION-RED: xor-b-constraint-swap neuter=FAIL restored-cmp=0 restored=OK` `CALIBRATION-RED: xor-c-constraint-swap neuter=FAIL restored-cmp=0 restored=OK` `CALIBRATION-RED: xor-a-expect-error neuter=FAIL restored-cmp=0 restored=OK`,
    each written only AFTER its neuter was observed RED, the `cp` restore was `cmp`-verified (exit 0) and the arm was
    observed GREEN again; never pre-write one. A neuter that did not go RED means the arm is not armed: fix the arm. Commit hygiene as in plan 02.
  </action>
  <precondition>Plan 07 committed; Docker up; no other local stack booted on this host.</precondition>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.9.1-09"; mkdir -p "$S"; T=src/__tests__/match-decisions-xor-rls.test.ts; ! grep -qE '"23505" \|\|' "$T" && ! grep -v '^\s*\*\|^\s*//' "$T" | grep -qF 'bridge_outcomes_unique_per_strategy_holding' && grep -qF 'bridge_outcomes_legacy_per_strategy_holding_when_md_null' "$T" && grep -qF 'bridge_outcomes_allocator_match_decision_unique' "$T" && npx vitest run --config vitest.livedb.config.ts "$T" --reporter=verbose > "$S/xor.log" 2>&1; RC=$?; tail -25 "$S/xor.log"; test "$RC" = 0 && ! grep -qE '\b[1-9][0-9]* (failed|skipped)\b' "$S/xor.log" && grep -qE '✓.*md_null|✓.*match_decision_id NULL|✓.*NULL decision' "$S/xor.log" && test "$(grep -cxF 'CALIBRATION-RED: xor-b-constraint-swap neuter=FAIL restored-cmp=0 restored=OK' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-09-SUMMARY.md)" = 1 && test "$(grep -cxF 'CALIBRATION-RED: xor-c-constraint-swap neuter=FAIL restored-cmp=0 restored=OK' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-09-SUMMARY.md)" = 1 && test "$(grep -cxF 'CALIBRATION-RED: xor-a-expect-error neuter=FAIL restored-cmp=0 restored=OK' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-09-SUMMARY.md)" = 1 && echo XOR-OK</automated>
    <fails_when>no "XOR-OK": a code-OR-message disjunction remains, a non-comment line still names the retired index, either current constraint name is absent, an arm failed or was skipped (lane down), or the new md-NULL arm did not run. Also fails when the SUMMARY lacks exactly one `CALIBRATION-RED:` line for `xor-b-constraint-swap`, `xor-c-constraint-swap`, `xor-a-expect-error` (the neuter's RED, the `cmp`-verified restore and the GREEN re-run are recorded as a structured line, not only as prose).</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE '"23505" \|\|' src/__tests__/match-decisions-xor-rls.test.ts` returns 0.
    - The retired index name appears only in header/comment prose explaining the history.
    - The file header contains the D-13 answer, citing migrations 081 and 083.
    - All arms pass on the lane; the new md-NULL arm's title contains `md_null`, `match_decision_id NULL` or `NULL decision` (so the verify can see it ran).
    - The SUMMARY records three calibrations, each RED under its neuter and GREEN after `cp` restore with `cmp` exit 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only.</reversibility>
  <done>The arm asserts the invariant the schema actually documents, and each assertion is proven able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> shared TEST / PROD | M3 auto-applies on merge (FC-1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-24 | Denial of service | M3 DO block refusing on TEST (data-dependent) and blocking the PROD apply | high | mitigate | catalog-only reads; linter 0 NEW; lane replay FATALs on any DO failure so a local boot proves it |
| T-164.9.1-25 | Repudiation | a test that encodes observed behaviour as the contract | medium | mitigate | header cites 081/083 as authority; conjunction assertions; calibrations recorded |
| T-164.9.1-26 | Tampering | test rows leaking into shared data | low | mitigate | lane is private; rows tracked and deleted in afterAll dependency order |
</threat_model>

<verification>
- M3-OK and XOR-OK printed; three calibrations recorded.
</verification>

<success_criteria>
- ROADMAP criterion 5 met: answered in writing (plan, M3, test header) and asserted by execution.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `supabase/migrations/<ts3>_bridge_outcomes_invariant_comments.sql` (M3) | migration | plan 11 reviewers, FC-1 |
| rewritten XOR arms + D-13 header | live-DB regression gate | plan 10 ledger run |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-09-SUMMARY.md` when done.
</output>
