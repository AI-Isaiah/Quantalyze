# Phase 164.9.1: JOBRPCTRUTH - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md - this log preserves the alternatives considered.

**Date:** 2026-09-24
**Phase:** 164.9.1-jobrpctruth
**Mode:** `--auto` - founder asleep; every choice auto-selected from repo evidence (no AskUserQuestion).
**Areas discussed:** harm proof, overload strategy, collision signal, 081 invariant, restore re-arm, sequencing with 164.4.2

---

## Harm proof (criterion 1)

| Option | Description | Selected |
|--------|-------------|----------|
| Probe on local stack / pg-lane first, own wave | Loopback only; verdicts only | ✓ |
| Probe on shared TEST | Forbidden - shared, and CLI links to PROD | |
| Skip probe, fix from reading | ROADMAP forbids | |

**Choice:** [auto] local probe first. Production harm recorded as LATENT: zero production callers pass non-empty `p_parent_job_ids` (measured by grep). Fix-or-drop resolves to FIX.

## Overload strategy (criterion 3)

| Option | Description | Selected |
|--------|-------------|----------|
| Ten-arg computes + INSERTs status (agree) | Re-base on 20260826150000; seven-arg untouched | ✓ |
| DROP seven-arg | Removes gate arms, comment markers, drift-check pins | |
| Route wrapper to seven-arg | Seven-arg lacks allocator/api_key scopes and run_at | |

## Collision signal (criterion 4)

| Option | Description | Selected |
|--------|-------------|----------|
| Restore 067 prefetch in `request_allocator_holdings_sync` (re-base on 076) | Root cause: stale re-base in 070 | ✓ |
| Change internal enqueue contract | Breaks idempotent callers (C-19 path); forces DROP | |
| Retire the Queued shape | UI and route already consume it | |

## 081 invariant (criterion 5)

| Option | Description | Selected |
|--------|-------------|----------|
| Rewrite arm to per-decision key + NULL-md partial index; tighten `||` to `&&`; fix stale column COMMENT | | ✓ |
| Delete the assertion | ROADMAP forbids | |

## Restore re-arm

| Option | Description | Selected |
|--------|-------------|----------|
| Inside the restore transaction after refdata replay | No commit window; marker-guarded | ✓ |
| Separate workflow step calling the script as-is | Script refuses under the workflow's held mutex (guard 2) | |
| Edit refdata allowlist / migration | Forbidden closures | |

## Sequencing

| Option | Description | Selected |
|--------|-------------|----------|
| Execute and merge after 164.4.2 | Lane tail-replay needed to see new migrations | ✓ |
| Merge first | Ledger lines could not be removed; CI lane cannot prove criteria 2/6 | |

## Claude's Discretion

Migration file names/timestamps; one vs two compute-job migrations; shape of the harm-probe harness.

## Deferred Ideas

Retiring the seven-arg overload (dropped, fix-or-drop); closing the 067 prefetch race fully (not booked); `[164.9-BASELINE-PRIVILEGES-ABSENT]` (out of scope).
