---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 02
type: execute
wave: 2
depends_on: ["164.9.1-01"]
files_modified:
  - supabase/migrations/<ts1>_fanin_initial_status_10param.sql
  - supabase/schema/functions/_enqueue_compute_job_internal.sql
  - src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts
autonomous: true
requirements:
  - "[164.9-FANIN-STATUS-NEVER-SET]"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "D-04 / criterion 3: the TEN-arg public._enqueue_compute_job_internal(uuid, uuid, text, text, uuid[], text, jsonb, uuid, uuid, timestamptz) computes v_initial_status ('done_pending_children' when p_parent_job_ids has at least one element, else 'pending') and INSERTs it; the SEVEN-arg public._enqueue_compute_job_internal(uuid, uuid, text, text, uuid[], text, jsonb) is byte-unchanged. Both are named by symbol AND arity."
    - "D-05: M1 is re-based on the LATEST ten-arg body found by re-grepping ALL of supabase/migrations/** at execution; everything outside the status delta is byte-identical to that body."
    - "D-06: M1 re-issues the ten-arg ACL pair (REVOKE from PUBLIC, anon, authenticated; GRANT to service_role) and a COMMENT that still carries the phrase 'Phase 163 OPS-08' plus one sentence recording that the body now computes and INSERTs the initial status."
    - "D-07: M1's self-verify DO block reads only catalogs and passes scripts/lint-migration-data-dependence.mjs with 0 NEW and no new baseline line."
    - "D-08 / criterion 2: arm P12 in the g10b live-DB file passes on the lane with M1 replayed (its criterion-6 calibration, neuter -> RED -> restore, and the dedupe twins' re-point are plan 03, which runs next and before anything else boots the lane)."
    - "D-01 / D-03: the P12-harm arm from plan 01 passes post-fix, and its post-fix HARM-PROBE verdict line is recorded beside the pre-fix one."
    - "D-10: the idempotent return-the-existing-id contract of _enqueue_compute_job_internal is NOT changed (no raise, no return-type change, no DROP FUNCTION)."
    - "D-25: M1 carries a '-- prod-body-ack:' line whose hash is the live column of scripts/sql-body-normalize.mjs --diff-bodies against origin/main's snapshot, derived at execution; the function snapshot is regenerated in the same change."
    - "D-24: the SUMMARY records the expected single VAC-08 '_enqueue_compute_job_internal/10 DRIFT' row and its pre-change hash as derived at execution."
    - "D-26: no new supabase/tests/*.sql arm is added for the fan-in status."
    - "D-27: M1's header records that a seven-argument call cannot resolve (42725) and that enqueue_compute_portfolio_job, the seven-arg's only caller, is broken but latent and has no caller."
  artifacts:
    - path: "supabase/migrations/<ts1>_fanin_initial_status_10param.sql"
      provides: "M1: ten-arg status fix, ACL re-convergence, OPS-08 comment, catalog-only DO block, prod-body-ack"
    - path: supabase/schema/functions/_enqueue_compute_job_internal.sql
      provides: "regenerated @generated snapshot"
  key_links:
    - from: "public.enqueue_compute_job (wrapper, all three modes)"
      to: "TEN-arg public._enqueue_compute_job_internal INSERT"
      via: "positional 10-arg call"
      pattern: "v_initial_status"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> Read floors, ceilings and counts from plan 01's SUMMARY pin table or re-measure them; never from this file.

<objective>
The tracer of the fix: harm probe -> M1 -> P12 green, proven by EXECUTION on the local lane.

M1 is one forward-only migration that makes the TEN-arg `public._enqueue_compute_job_internal` compute
and INSERT the initial status (D-04..D-07, D-25, D-27). The regenerated snapshot rides in the same task.
The dedupe gate's twin re-point and the neuter calibration of P12 go stale the instant M1 exists
(research Q4, Pitfall 1); they are plan 03, which depends on this plan alone and runs in the very next
wave, before any other plan boots the lane. Nothing is pushed in between (research Pitfall 9).

Purpose: ROADMAP criteria 2, 3 and 6 for `[164.9-FANIN-STATUS-NEVER-SET]`.
Output: M1, the regenerated snapshot, a green P12 and P12-harm on the lane, and the post-fix verdict line.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-RESEARCH.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-PATTERNS.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: M1 — the TEN-arg overload INSERTs the fan-in status; P12 and P12-harm green on the lane (D-04..D-07, D-10, D-25, D-27)</name>
  <files>supabase/migrations/&lt;ts1&gt;_fanin_initial_status_10param.sql, supabase/schema/functions/_enqueue_compute_job_internal.sql, src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts</files>
  <read_first>
    - The LATEST ten-arg body. Re-grep first: `grep -ln "FUNCTION public._enqueue_compute_job_internal\|FUNCTION _enqueue_compute_job_internal" supabase/migrations/*.sql | sort | tail -5`, then open the newest file that CREATEs the ten-arg arity (expected `supabase/migrations/20260826150000_destrict_enqueue_internal_10param.sql`; if a newer one exists, re-base on THAT, per D-05).
    - `supabase/migrations/20260716090000_retire_compute_analytics_kind_rpc_guard.sql` — the SEVEN-arg's `v_initial_status` declaration, computation and INSERT (the logic to mirror).
    - `supabase/migrations/20260911120000_vault_tick_hardening.sql` — the `-- prod-body-ack:` block format.
    - `supabase/tests/test_enqueue_internal_destrict.sql` (`c_applied_marker`, `c_expected_arms`, the strict/serfail regexes) and `supabase/tests/test_compute_analytics_kind_retired.sql` (the retired-kind message literal).
    - `scripts/lint-migration-data-dependence.mjs` header (what it refuses).
    - `164.9.1-RESEARCH.md` "Q4 — Gate impact map", "Q5", "Code Examples: M1 body delta", Pitfalls 3, 4, 11; `164.9.1-PATTERNS.md` "M1".
    - arm P12 in `src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts` and its "THIS ARM IS RED ON PURPOSE" prose.
  </read_first>
  <action>
    Create M1 at `supabase/migrations/<ts1>_fanin_initial_status_10param.sql`, where `<ts1>` is the real
    UTC time at authoring, strictly newer than every file in `supabase/migrations/` (research Pitfall 4),
    and the name matches `^[0-9]+_[a-z0-9_]+\.sql$` (the 164.4.2 lane replay FATALs otherwise).

    Body (D-04, D-05): copy the latest ten-arg `CREATE OR REPLACE FUNCTION public._enqueue_compute_job_internal(`
    statement verbatim — schema-qualified name, the ten parameters with `DEFAULT NULL` on the last three,
    `SECURITY DEFINER`, `SET search_path = public, pg_catalog`, the 4-way XOR guard, the `p_kind` NULL
    guard, the Phase 106 retired-kind reject (message literal unchanged), the optimistic look-up (the
    strategy arm byte-identical including its 5-space `WHERE` indent), `ON CONFLICT DO NOTHING`, exactly
    FOUR plain `SELECT id INTO v_new_id` lost-race re-reads, and the `serialization_failure` raise with its
    operator-shaped message. The ONLY delta: declare `v_initial_status TEXT`; before the optimistic look-up,
    set it to `'done_pending_children'` when `p_parent_job_ids IS NOT NULL AND array_length(p_parent_job_ids, 1) > 0`
    and to `'pending'` otherwise (mirroring the seven-arg); append `status` to the INSERT column list and
    `v_initial_status` to its VALUES. Per D-10 do not change the return type, do not raise on collision,
    do not DROP FUNCTION. Do not touch the seven-arg.

    Re-issue the ten-arg ACL pair verbatim (D-06) with its default-grant event-trigger note, then
    `COMMENT ON FUNCTION public._enqueue_compute_job_internal(uuid, uuid, text, text, uuid[], text, jsonb, uuid, uuid, timestamptz)`
    carrying the prior comment text forward (including `Phase 163 OPS-08`) plus one sentence stating the
    body now computes the initial status and INSERTs it.

    Self-verify DO block (D-07): copy the SHAPE and arms (a)-(h) of the source migration's DO block, renaming
    the RAISE prefix to this migration's own tag, and add COPY-CHECK arms on the comment-stripped ten-arg
    body: the `v_initial_status := 'done_pending_children'` assignment, the parents-non-empty predicate, and
    `status` in the INSERT column list with `v_initial_status` in VALUES. Label them copy-checks (execution
    proof is P12). Read only `pg_proc`, `pg_get_functiondef`, `obj_description`, `has_function_privilege`,
    `to_regprocedure`, `to_regrole` and `regexp_matches` over the body. Every RAISE format string is one literal.
    `SET LOCAL lock_timeout = '3s';`, no explicit BEGIN/COMMIT, no `cron.schedule`.

    Header (D-27, Pitfall 3, D-11-style limits): the re-base grep and its result; that a seven-argument call
    cannot resolve (42725) so nothing executes the seven-arg, and `public.enqueue_compute_portfolio_job` — its
    only caller — raises on every call and has no caller (not fixed here); the stranded-fan-in-child limit
    (a child whose parents are already done, failed or absent never leaves `done_pending_children`), latent
    because no caller passes parents (D-02). ⛔ No comment in M1 may contain the dedupe twins' `find` text
    (the strategy-arm look-up lines), or `occurrences: 1` breaks in Task 2.

    Regenerate the snapshot with `npm run schema:functions`; never hand-edit it. Derive the VAC-04 ack:
    write origin/main's snapshot to a scratch file with
    `git show origin/main:supabase/schema/functions/_enqueue_compute_job_internal.sql > "$S/main-enqueue.sql"`,
    run `node scripts/sql-body-normalize.mjs --diff-bodies supabase/schema/functions/_enqueue_compute_job_internal.sql "$S/main-enqueue.sql"`,
    and put the TEN-arg row's live-column hash on a `-- prod-body-ack:` line in M1's header with the
    derivation command and the "EARNED only if VAC-04 reports that SAME hash for PROD" warning (D-25).
    If that hash differs from the research reading (A1), do not edit the ack to match anything; record the
    difference in the SUMMARY as a possible out-of-band PROD drift. Re-run `npm run schema:functions` after
    the header edit so the snapshot carries the final comment block (Pitfall 11).

    Update P12's "RED ON PURPOSE" prose to cite M1 as the fix; keep the assertion
    `expect(child.status).toBe("done_pending_children")` unchanged.

    Re-boot the lane (`bash scripts/local-stack/run.sh down` then `up`); the `baseline-replay:` line must name
    M1. Run P12 and P12-harm; record the post-fix `HARM-PROBE verdict:` line in the SUMMARY beside plan 01's.
    Run the SQL gates that read this function on the lane: extract the DSN with
    `L="$(sed -n 's/^DB_URL="\{0,1\}\([^"]*\)"\{0,1\}$/\1/p' scripts/local-stack/.stack-env | head -1)"`, refuse
    it unless `LOOPBACK_DSN="$L" node scripts/local-stack/capability-probe.mjs --refuse-nonlocal-dsn` passes, then
    `psql "$L" -X -v ON_ERROR_STOP=1 -q -f` each of `supabase/tests/test_enqueue_internal_destrict.sql`,
    `supabase/tests/test_compute_analytics_kind_retired.sql`, `supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql`.

    Record in the SUMMARY (D-24): the pre-change ten-arg hash (the live-column hash above) as the TEST hash
    the PR's single expected VAC-08 `_enqueue_compute_job_internal/10 DRIFT` row will show.

    Commit hygiene: never stage `.planning/milestone.lock`, `.planning/state.json`, `.planning/phases/164.5.3-*`;
    `NM="$(readlink node_modules)"; rm node_modules`, commit, `ln -s "$NM" node_modules`. No CI skip token.
  </action>
  <precondition>Plan 01's SUMMARY exists and records ANCESTRY-OK; Docker up; no other local stack booted on this host.</precondition>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.9.1-02"; mkdir -p "$S"; M1="$(ls supabase/migrations/*_fanin_initial_status_10param.sql)"; test "$(echo "$M1" | wc -l | tr -d ' ')" = 1 && grep -qF 'Phase 163 OPS-08' "$M1" && grep -qE '^-- prod-body-ack: [0-9a-f]{64}$' "$M1" && ! grep -qiE 'cron\.schedule' "$M1" && npx tsx scripts/dump-sql-functions.ts --check && node scripts/lint-migration-data-dependence.mjs --self-test && node scripts/lint-migration-data-dependence.mjs > "$S/lint.log" 2>&1; RC=$?; cat "$S/lint.log"; test "$RC" = 0 && grep -qE '\b0 NEW\b' "$S/lint.log" && PB="$(sed -nE 's/^PHASE-BASE: ([0-9a-f]{40})$/\1/p' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md)" && test -n "$PB" && git diff --quiet "$PB" -- scripts/lint-migration-data-dependence-baseline.txt && npx vitest run --config vitest.livedb.config.ts src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts -t "P12" --reporter=verbose > "$S/p12.log" 2>&1; RC=$?; cat "$S/p12.log"; test "$RC" = 0 && grep -qE 'HARM-PROBE verdict:.*child_status_at_enqueue=done_pending_children' "$S/p12.log" && grep -qE '✓.*P12: enqueue with parent_job_ids' "$S/p12.log" && ! grep -qE '\b[1-9][0-9]* failed\b' "$S/p12.log" && echo M1-TRACER-OK</automated>
    <fails_when>no "M1-TRACER-OK": M1 missing or duplicated, OPS-08 marker or prod-body-ack absent, a cron.schedule present, the snapshot stale, the data-dependence linter reports a NEW refusal or its baseline changed, P12 or P12-harm failed or did not run (no ✓ for P12, no post-fix verdict line), or any arm failed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one file matches `supabase/migrations/*_fanin_initial_status_10param.sql`, and its timestamp prefix is greater than every other file's prefix in `supabase/migrations/`.
    - In M1, `grep -c 'SELECT id INTO v_new_id'` over the non-comment lines returns 4, and `grep -c 'INTO STRICT'` over the non-comment lines returns 0.
    - M1 contains `REVOKE ALL ON FUNCTION public._enqueue_compute_job_internal(` and `TO service_role;` and the literal `Phase 163 OPS-08`.
    - M1 contains exactly one `CREATE OR REPLACE FUNCTION` statement (`grep -c 'CREATE OR REPLACE FUNCTION' "$M1"` over non-comment lines returns 1) and its parameter list includes `p_run_at` (the ten-arg); the seven-arg is not re-created.
    - `npx tsx scripts/dump-sql-functions.ts --check` exits 0.
    - `node scripts/lint-migration-data-dependence.mjs` prints `0 NEW`; `scripts/lint-migration-data-dependence-baseline.txt` is unchanged against the phase base (`git diff --quiet "$PB" -- <file>`, where `PB` is the `PHASE-BASE:` sha plan 01's SUMMARY records; this compares the base commit with the working tree, so a committed change and an uncommitted one both fail it).
    - The lane boot's `baseline-replay:` line names M1; the three SQL gates run with psql exit 0 on the lane.
    - The SUMMARY holds both HARM-PROBE verdict lines (pre-fix from plan 01, post-fix here), the derived prod-body-ack hash, and the expected VAC-08 DRIFT row statement (D-24).
  </acceptance_criteria>
  <reversibility rating="reversible">Forward-only CREATE OR REPLACE; reverting is re-running the prior body. Not applied anywhere shared until the founder merges (FC-1).</reversibility>
  <done>A parented child enqueued through the public wrapper lands done_pending_children on a real database, and the harm probe shows it is no longer claimable before its parent is done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> shared TEST / PROD | A merged file under supabase/migrations/** auto-applies to TEST, then PROD (FC-1). |
| service_role caller -> SECDEF ten-arg | Private implementation; only service_role may EXECUTE. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-05 | Elevation of privilege | M1 CREATE OR REPLACE vs the default-grant event trigger | high | mitigate | schema-qualified CREATE; ACL pair re-issued after it; DO block runs `_assert_no_public_execute` and anon/authenticated `has_function_privilege` arms |
| T-164.9.1-06 | Tampering | stale re-base dropping a guard | high | mitigate | re-grep all migrations at execution; body copied from the latest ten-arg; destrict + retired-kind SQL gates run on the lane; VAC-04 ack earned from a local diff |
| T-164.9.1-07 | Denial of service | data-dependent DO block refusing on TEST and blocking the PROD apply | high | mitigate | catalog-only reads; linter 0 NEW with unchanged baseline |
| T-164.9.1-08 | Tampering | stranded done_pending_children child blocking its (kind, partition) | low | accept | pre-existing seven-arg design, no caller passes parents (D-02); recorded in M1 header and SUMMARY |
| T-164.9.1-09 | Information disclosure | serialization_failure / guard RAISE messages | low | accept | messages stay operator-shaped as in the source body (WR-07 note); no user-visible surface calls the ten-arg |
</threat_model>

<verification>
- M1-TRACER-OK printed.
- Both HARM-PROBE verdict lines and the derived prod-body-ack hash recorded in the SUMMARY.
</verification>

<success_criteria>
- Criteria 2 and 3 for the fan-in item are proven by execution on the lane; criterion 6 (calibration) follows in plan 03.
- No floor moved; no allowlist or baseline line added.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `supabase/migrations/<ts1>_fanin_initial_status_10param.sql` (M1) | migration | plan 11 reviewers, FC-1 merge |
| `supabase/schema/functions/_enqueue_compute_job_internal.sql` | regenerated snapshot | sql-function-snapshot, VAC-04, VAC-08 |
| post-fix HARM-PROBE verdict + expected VAC-08 row (SUMMARY) | evidence | plan 14 FC-1 precondition and CHANGELOG |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-02-SUMMARY.md` when done.
</output>
