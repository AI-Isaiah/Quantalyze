---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 12
subsystem: ui
tags: [review-round-2, allocator-exchanges, sync-status, reconnect, queued-hold]
status: complete

requires:
  - phase: 164.9.1-11
    provides: "round 1 of the three-reviewer gate, 13 FIX dispositions, and the round-1 fixing commits"
provides:
  - "Round 2 of the three-reviewer gate (D-21): all three reviewers re-reviewed every round-1 fix; 4 findings, all dispositioned, 0 FC-1 blockers"
  - "exchange rows drop section-bound client state (helper line, queued timestamp) when a disconnect or reconnect moves them between lists"
  - "the reconnect stale-snapshot guard is an explicit client flag that handleReconnect sets and clears, so no row can be pinned active and Syncing until reload"
affects: [164.9.1-13, 164.9.1-14]

actuals:
  tokens: 7500
  tasks: 2
  commits: 1
plan_head_before: c06bad9854409ffe854d4550b36c992a241e5340

tech-stack:
  added: []
  patterns:
    - "client-only row state that describes a section is dropped when the row changes section, compared against the MERGED disconnected_at, not the server's"
    - "an in-flight guard is an explicit flag set by the handler and cleared in its finally, never inferred from a state shape another cause can also produce"

key-files:
  created:
    - .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-12-SUMMARY.md
  modified:
    - src/components/exchanges/AllocatorExchangeManager.tsx
    - src/components/exchanges/AllocatorExchangeManager.test.tsx

key-decisions:
  - "R2-3 replaced the inferred isReconnectInFlight with an explicit reconnect_in_flight flag cleared in handleReconnect's finally. The inference (syncing + null disconnected_at + server disconnected) also matched a row disconnected elsewhere during a sync or a queued hold, and it latched. Accepted cost: a server snapshot taken before the reconnect RPC committed that arrives after handleReconnect returns now moves the row to Disconnected until the next 5s poll corrects it."
  - "The section-change clear keeps overrides that were set together with their section (the 409 reason, 'Reconnect failed'), because those rows do not move on the next refresh."
  - "The migration-reviewer's MEDIUM (diamond-DAG deadlock of M1's FOR SHARE lock) is fixed by documentation in c06bad985 (M1 KNOWN LIMIT note plus 164.5.2 routing), not by a body change, on the reviewer's own recommendation."

duration: ~45min
completed: 2026-09-25
---

# Phase 164.9.1 Plan 12: Review round 2 Summary

**Round 2 re-reviewed all 13 round-1 fixes with all three reviewers. It found one latent SQL deadlock shape, now documented and routed to 164.5.2, and three user-facing state bugs in the allocator exchange card. Those are fixed with seven new component tests, each of which went RED when its guard was neutered. The gate chain was then re-run.**

## Task 1: orchestrator checkpoint

ROUND2-SIGNAL: reviews-delivered

Plan 11 recorded `ROUND1-FIX-COUNT: 13`. The orchestrator dispatched `migration-reviewer`, `rls-policy-auditor` and `silent-failure-hunter` in one message. Each covered the whole round-1 fix diff `80e07112b..411b56e9d`, which is the four fixing commits `74a125a64`, `f72d9dd4c`, `eb4c5407e` and `8a3053f1e` plus the SUMMARY commit, and the files they touched. All three reports are non-empty and sit at `${TMPDIR}/gsd-164.9.1-12/review-r2-{migration-reviewer,rls-policy-auditor,silent-failure-hunter}.md`, outside the repo.

## Round 2

**Re-reviewed:** `74a125a64` (M1: ten-arg parent refusals under FOR SHARE), `f72d9dd4c` (M2 arm (e), M3 comments, the no-oracle live arm), `eb4c5407e` (the 409 path, the info trace, the queued hold), `8a3053f1e` (the emitter refusal and self-test). Every reviewer covered every commit.

| # | Reviewer | Severity | Finding | Disposition | Commit |
|---|---|---|---|---|---|
| R2-M | migration-reviewer | MEDIUM, latent (conf 7) | M1's `ORDER BY pj.id FOR SHARE` parent lock can deadlock (40P01) with `mark_compute_job_done`'s fan-in UPDATE on a diamond-shaped DAG. The header implied id ordering prevents that. | **FIXED BY DOCUMENTATION** (orchestrator, before this executor). M1's KNOWN LIMIT block now records the deadlock shape and says 40P01 must be retried. The limit is routed to Phase 164.5.2 BRIDGELOCK beside the failed_final residual. There is no body change, as the reviewer recommended: the lock is needed and no caller passes parents (D-02). | `c06bad985` |
| R2-1 | silent-failure-hunter | MEDIUM, user-facing | A stale "Sync request failed — click Sync now to retry" followed the row into Disconnected, where it has no Sync now button. This happened on a same-tab soft disconnect and on a disconnect made elsewhere. | **FIXED.** `handleDeleteKey`'s soft branch clears `helper_override` and `queued_next_attempt_at` and sets `sync_status: "idle"`. `normalizeInitialKey` drops both carried fields whenever the merge moves the row between lists. | `57f999b88` |
| R2-2 | silent-failure-hunter | MEDIUM, user-facing | The 409 sentence "Reconnect it before syncing holdings" stayed on a row after a reconnect made elsewhere brought it back to the active list. | **FIXED** by the same section-change clear. Overrides set together with their section stay while the row does not move, and a test pins that. | `57f999b88` |
| R2-3 | silent-failure-hunter | MEDIUM, user-facing | The queued hold widened the inferred `isReconnectInFlight` latch. A remote disconnect during a hold pinned the row active and "Syncing…" until reload. A same-tab disconnect during a hold showed Reconnect disabled for minutes. | **FIXED.** `isQueuedHold` returns false when the server row is disconnected. The inferred guard is replaced by an explicit `reconnect_in_flight` flag. `handleReconnect` sets it in its optimistic update and clears it in a `finally`. | `57f999b88` |
| — | rls-policy-auditor | — | 0 findings (0 CRITICAL, 0 HIGH, 0 MEDIUM, 0 LOW; leak scopes []). The M1 22023 refusals are reachable only by service_role. The new live arm pins the no-oracle order. | nothing to fix | — |

Below the reporting bar, recorded and not fixed:
- rls-policy-auditor: the ten-arg's parent refusals would become an existence oracle if a future authenticated wrapper forwarded caller-supplied parents. Today none does (D-02), and the header records that.
- silent-failure-hunter: the normalize self-test still runs only on `workflow_dispatch`. This was already flagged in plan 11.

**The M1 test was rewritten, not just kept.** The old test seeded a row that was `syncing` with a null `disconnected_at` and called that "a reconnect in progress". That is the exact inference R2-3 removes, so the old test encoded the defect. It now clicks Reconnect with the RPC held open, and a sibling test proves the guard ends when `handleReconnect` returns.

**Neuter → RED → restore.** Each guard was neutered in place, the file was run, and it was restored by `cp` from a byte backup with `cmp` OK:

| Neuter | Went RED |
|---|---|
| N1 drop `helper_override: null` from the soft disconnect | R2-1 same-tab |
| N2 `changedSection` forced false | R2-1 remote, R2-2 remote reconnect |
| N3 drop `isQueuedHold`'s disconnected guard | R2-3 remote disconnect during hold |
| N4 restore the inferred latch | R2-3 remote disconnect, M1 after-reconnect |
| N5 drop `sync_status: "idle"` from the soft disconnect | R2-3 same-tab disconnect during hold |
| N6 `finally` no longer clears the flag | M1 after-reconnect |
| N7 optimistic update does not set the flag | M1 stale snapshot during reconnect |
| N8 `changedSection` forced true (over-clearing) | R2-2 override kept in section, and the existing queued-hold test |

**FC-1 blockers:** none. Every round-2 finding is fixed, or fixed by documentation with a named phase (164.5.2). D-21's two-round limit is reached, so there is no round 3 and the next step is the verifier.

### Gates re-run by this executor after the last fix

The plan's verify ran in three stages, as in plans 10 and 11, so each long step had its own 600 s watchdog. The stages use the verify's own clauses and predicates. The lane was torn down unconditionally after stage 1 (`down=0`). The verdict clause was then run as written against the captured statuses and this SUMMARY.

```
status: lane+ledger=0 static=0 mutation=2
REVIEW-GATES-OK
```

| Stage | Command | Verdict |
|---|---|---|
| lane + ledger | `run.sh down`, `run.sh up`, then `npm run test:live-db:ledger`, then `down` | `lane+ledger=0`. `baseline-replay: 3 migration(s) newer than the dump` names all three. `executed: 401 test(s) (394 passed, 7 failed)`, `OK — the failing set is EXACTLY the ledger (7/7).` |
| static | `lint-migration-data-dependence` | `corpus 277 migration(s), 350 anonymous block(s); 83 refusal(s), 83 allowlisted, 0 NEW.` |
| static | `dump-sql-functions.ts --check` | `SQL function snapshot is current (121 functions).` |
| static | migrations added vs PHASE-BASE (D-25) | exactly 3: `20260924230827_fanin_initial_status_10param.sql`, `20260924233749_allocator_sync_restore_inflight_prefetch.sql`, `20260925071300_bridge_outcomes_invariant_comments.sql` |
| static | normalize `--self-test` | `test-only-normalize-analytics-url: self-test OK (36 checks)` |
| static | restore `--self-test` | `restore-test-from-baseline: self-test OK (37/37 arms — …` |
| static | route and restore vitest | `Test Files  2 passed (2)` · `Tests  56 passed (56)` · `static=0` |
| mutation | `run.mjs --file …/test_enqueue_compute_job_dedupe_non_terminal.sql` | exit 2 · `arms: 2/2/0` · `biting: 2` · `lane-invocations: 2` · `No defects in the narrowed scope.` · defect-class hits: 0 |
| extra | vitest, touched file | `AllocatorExchangeManager.test.tsx`: `Tests  77 passed (77)` |
| extra | vitest, wider allocator and exchanges suites (`vitest run allocator exchange`) | `Test Files  25 passed \| 1 skipped (26)` · `Tests  407 passed \| 23 skipped (430)`. The skipped file holds live-DB arms, which ran in the ledger stage above. |
| extra | `tsc --noEmit -p .` | exit 0 |
| extra | `eslint` on both touched files | exit 0 |
| extra | `npm run lint` | exit 0 · `[check-planning-hygiene] OK — 7064 tracked files scanned` |

A first attempt at the wider suite passed 26 explicit paths, and vitest ran only 2 files, because paths containing `[id]` are read as filter patterns. It was re-run with the name filters `allocator exchange`, which collected all 26 files. That re-run is the result recorded above.

**D-07 and D-25 still hold.** No migration, function body or SQL file changed in this plan, so there are 0 NEW data-dependence refusals, the snapshots are current, the prod-body-ack lines need no re-derivation, and exactly three migrations exist. No Python was touched, so mypy was not run.

## Deviations from Plan

1. **[Rule 1, directed by the review]** `AllocatorExchangeManager.tsx` is not in this plan's `files_modified`. Round 2's only fixable findings are in that file, and the orchestrator named it. Task 2 allows fixes to "any file listed", so this is a scope extension. It is recorded here.
2. **[Rule 1]** The existing M1 component test was rewritten. It asserted the latch that R2-3 removes, so keeping it would have kept the defect.
3. **[environment]** The verify's stages ran with the scratch directory given by absolute path and `TMPDIR` unset, because tsx's IPC socket path under the long scratchpad `TMPDIR` exceeds the socket limit (the same deviation as plan 11). The verify's clauses and predicates are unchanged.

## Self-Check: PASSED

FOUND: this SUMMARY, `AllocatorExchangeManager.tsx`, `AllocatorExchangeManager.test.tsx`. FOUND commits: `57f999b88` (the round-2 fix) and `c06bad985` (R2-M, by documentation). The verdict clause printed `REVIEW-GATES-OK` in this executor's own run, after the last fix.
