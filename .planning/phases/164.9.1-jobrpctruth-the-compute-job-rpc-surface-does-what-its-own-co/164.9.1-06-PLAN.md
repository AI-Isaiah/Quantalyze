---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 06
type: execute
wave: 4
depends_on: ["164.9.1-05"]
files_modified:
  - src/__tests__/restore-test-from-baseline.test.ts
autonomous: true
requirements:
  - "[164.9-TEST-ANALYTICS-URL-REARM]"

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "D-16a: a static pin fails if the emitted-fragment concatenation moves out of the slot between the reference-data gate (TXN_REFDATA_GATE) and the ledger DDL (TXN_LEDGER_DDL) in build_transaction; its move-calibration proves it flips."
    - "D-16a: a static pin fails if any `if [ \"$mode\"` condition appears between the emitter call and the terminator block, so the fragment stays byte-identical in both modes; its calibration proves it flips."
    - "D-18: every EXPECTED_ARMS= literal in the arm-ratchet tests equals the value plan 05 set in the script (read from the script, never a document), except the single bump-calibration literal, which equals that value plus one; and their bump / comment-out / double calibrations still work."
    - "D-28 / D-18: E2 ('every .sql the workflow PUBLISHES is a file the scan actually reads') and src/__tests__/test-restore-workflow-wiring.test.ts run green, and the wiring test is unchanged against the phase base plan 01 recorded."
  artifacts:
    - path: src/__tests__/restore-test-from-baseline.test.ts
      provides: "order pin (gate -> fragment -> ledger DDL) and byte-identity pin, each calibrated; EXPECTED_ARMS literals moved"
  key_links:
    - from: "src/__tests__/restore-test-from-baseline.test.ts order pin"
      to: "scripts/restore-test-from-baseline.sh build_transaction emitter call"
      via: "liveIndexOf over the live script text"
      pattern: "emit-restore-sql"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> Split out of plan 05 (checker iteration 1: per-plan budget). Static vitest only: no database, no local stack,
> so it may share a wave with plan 07.

<objective>
Pin the two structural invariants plan 05 relies on — the fragment's position inside the restore transaction and
the byte-identical-modes rule — so a later edit to `build_transaction` cannot silently break the REARM fix
(`[164.9-TEST-ANALYTICS-URL-REARM]`, D-16a, D-18, D-28).

Output: two calibrated static pins, moved EXPECTED_ARMS literals, and green E2 + wiring tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-PATTERNS.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static pins for the fragment's position and for byte-identical modes, each calibrated; E2 and the wiring test stay green (D-16a, D-18, D-28)</name>
  <files>src/__tests__/restore-test-from-baseline.test.ts</files>
  <read_first>
    - `src/__tests__/restore-test-from-baseline.test.ts`: `describe` "L-01" (the `STEPS` array, `liveIndexOf`, move-calibrations), the "arm ratchet" `describe` and every `EXPECTED_ARMS=` literal, "the MODE picks the terminator", E2 ("every .sql the workflow PUBLISHES is a file the scan actually reads"), the `--run` region softening allowlist.
    - `src/__tests__/test-restore-workflow-wiring.test.ts` (verify-only).
    - `scripts/restore-test-from-baseline.sh` `build_transaction` as plan 05 committed it.
  </read_first>
  <action>
    Per D-16a, add the fragment concatenation as a step between "the gate" and "the ledger DDL" in L-01's `STEPS` (or
    as its own pin in the same style), with a move-calibration that relocates the concatenation above the gate on an
    in-memory copy and asserts the order check flips. Add a byte-identity pin: in the live `build_transaction` text
    between the emitter call and the terminator, no line matches `if [ "$mode"`; calibrate by wrapping the call in such
    a condition on an in-memory copy and asserting the pin detects it. Per D-18, move every `EXPECTED_ARMS=<old>`
    literal in the arm-ratchet tests to the new value (read it from the script; never copy from a document), move the
    bump calibration's target literal to the new value plus one (on origin/main it is the `moved` copy's `EXPECTED_ARMS=33`
    beside `=32`), and keep their bump / comment-out / double calibrations working. Per D-28 do not edit E2 or the wiring test; run them.

    Commit hygiene: `NM="$(readlink node_modules)"; rm node_modules`, commit, `ln -s "$NM" node_modules`; never stage
    `.planning/milestone.lock`, `.planning/state.json` or `.planning/phases/164.5.3-*`. No CI skip token.
  </action>
  <precondition>Plan 05 committed (the emitter call exists in build_transaction and EXPECTED_ARMS was raised); plan 01's SUMMARY carries a `PHASE-BASE:` line.</precondition>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.9.1-06"; mkdir -p "$S"; rm -f "$S/pins.log"; T=src/__tests__/restore-test-from-baseline.test.ts; PB="$(sed -nE 's/^PHASE-BASE: ([0-9a-f]{40})$/\1/p' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md)"; E="$(sed -nE 's/^EXPECTED_ARMS=([0-9]+)$/\1/p' scripts/restore-test-from-baseline.sh)"; test -n "$PB" && test -n "$E" && git diff --quiet "$PB" -- src/__tests__/test-restore-workflow-wiring.test.ts && grep -qF 'emit-restore-sql' "$T" && L="$(grep -oE 'EXPECTED_ARMS=[0-9]+' "$T")" && test "$(printf '%s\n' "$L" | grep -cxF "EXPECTED_ARMS=$((E+1))")" = 1 && test "$(printf '%s\n' "$L" | grep -cxF "EXPECTED_ARMS=$E")" -ge 1 && test "$(printf '%s\n' "$L" | grep -cvxE "EXPECTED_ARMS=($E|$((E+1)))")" = 0 && npx vitest run "$T" src/__tests__/test-restore-workflow-wiring.test.ts --reporter=verbose > "$S/pins.log" 2>&1; RC=$?; tail -20 "$S/pins.log"; test "$RC" = 0 && test -s "$S/pins.log" && ! grep -qE '\b0 (passed|tests)\b' "$S/pins.log" && ! grep -qE '\b[1-9][0-9]* (failed|skipped)\b' "$S/pins.log" && grep -qE '✓.*E2' "$S/pins.log" && echo PINS-OK</automated>
    <fails_when>no "PINS-OK": the phase base or EXPECTED_ARMS cannot be read, the wiring test differs from the phase base (committed or not), the test file does not reference the emitter call, the test's `EXPECTED_ARMS=` literals are not exactly {E, and ONE bump-calibration literal E+1} (a stale old-value literal, a missing or doubled E+1 calibration literal, or no E literal at all), a pin or calibration failed or was skipped, zero tests ran, or E2 did not run green. RC is the status of the whole chain ending in vitest, so an earlier failure cannot be masked. The literal check uses `grep -c` counts only, never an exit status of `grep -v`: in this harness `grep` is a shell function routed to ugrep, and MEASURED 2026-09-24 `printf 'a\nb\n' | grep -vq a` exits 1 there although a non-matching line exists, so the previous `! ... | grep -vqx` form could never fail. Calibrated against origin/main (E=32: fifteen `=32` literals and one `=33` bump literal): passes as-is, fails with one literal left at 31, fails with the bump literal at 34, identically in zsh and bash.</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/restore-test-from-baseline.test.ts` references `emit-restore-sql` in an order pin and a byte-identity pin, each with a calibration that asserts the pin flips on an in-memory mutation.
    - Every `EXPECTED_ARMS=` literal in the test equals the script's new value E, except exactly one bump-calibration literal equal to E+1 (the `moved` copy that proves the ratchet flips).
    - Both test files pass; `git diff --quiet "$PB" -- src/__tests__/test-restore-workflow-wiring.test.ts` exits 0 (`PB` = plan 01's `PHASE-BASE:` sha; base commit versus working tree).
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only.</reversibility>
  <done>The fragment's position and the byte-identical-modes invariant are pinned and proven able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future edits -> restore transaction | a later change to build_transaction could move or mode-gate the normalisation without any runtime arm noticing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-41 | Tampering | normalisation moved before the refdata gate or after the ledger DDL | medium | mitigate | order pin with move-calibration over the live script text |
| T-164.9.1-42 | Tampering | preflight/restore divergence re-introduced by a mode condition | high | mitigate | byte-identity pin with calibration; preflight still ends ROLLBACK (plan 05 arm) |
</threat_model>

<verification>
- PINS-OK printed.
</verification>

<success_criteria>
- Both structural invariants of the REARM fix are pinned and calibrated; E2 and the wiring test green and the wiring test untouched.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| order + byte-identity pins | static regression gate | frontend vitest CI, plan 10 sweep, plan 12 gate re-run |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-06-SUMMARY.md` when done.
</output>
