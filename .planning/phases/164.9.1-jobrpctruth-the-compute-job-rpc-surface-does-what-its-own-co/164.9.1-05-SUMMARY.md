---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 05
subsystem: ops-scripts
tags: [rearm, shared-test, analytics-service-url, restore, self-test, calibration]
status: complete

requires:
  - phase: 164.9.1-01
    provides: "PHASE-BASE and the re-measured 164.4.2 pin table (EXPECTED_ARMS=32, four restore: tables=2 pins)"
  - phase: 164.9.1-04
    provides: "scripts/test-only-normalize-analytics-url.sh --emit-restore-sql and --print-sink"
provides:
  - "a restore of shared TEST that re-normalises analytics_service_url inside its own transaction, after the reference-data gate and before the ledger DDL, byte-identical in both modes"
  - "self-test arms 33 (restore commits the sink) and 34 (preflight rolls the same fragment back)"
affects: [164.9.1-06, 164.9.1-10, 164.9.1-14]

actuals:
  tokens: 7800
  tasks: 1
  commits: 1
plan_head_before: 40aa2643aff3e1b3ae11198a5daa35552639c4a6

tech-stack:
  added: []
  patterns:
    - "one emitter, captured by command substitution with its exit code, fails the assembly on refusal or empty output"
    - "a self-test arm reads its expected value from the owning script (--print-sink), never a literal"

key-files:
  created: []
  modified:
    - scripts/restore-test-from-baseline.sh
    - scripts/restore-test-from-baseline-fixtures/baseline-fixture.sql
    - scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql
    - scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt

key-decisions:
  - "The emitter is reached through a new REARM_EMITTER seam (default: the normalize script beside this one). The self-test runs scratch copies of the restore from its mktemp dir, where a SCRIPT_DIR-relative path does not exist, so run_leg passes the real path. The seam is documented in the ENV SEAMS block."
  - "Arm 34 seeds the stand-in row inline with psql rather than through a fixtures/arm-<name>.sql overlay. The overlay directory is itself a pinned census ('exactly seven arm overlays' in src/__tests__/restore-test-from-baseline.test.ts), and plan 06 does not own moving it."
  - "Arm 33 reads the committed row BEFORE its structural checks (note line, fragment in restore.sql, summary line), so the calibration's RED names the row."
  - "The system_settings seed sits at the foot of fixture_c.sql and its allowlist line sits after fx_keep's: the fixture ledger keeps three rows, and the extractor (which emits in allowlist order) still replays an fx_keep statement first, which arm 23 leg (b) names."

duration: ~20min
completed: 2026-09-25
---

# Phase 164.9.1 Plan 05: The Restore Normalises the Analytics Destination In-Transaction Summary

**`build_transaction` in `scripts/restore-test-from-baseline.sh` now runs `test-only-normalize-analytics-url.sh --emit-restore-sql` with the restore's own marker predicates. It captures the exit code and appends the fragment after the reference-data gate and before the ledger DDL, in both modes and byte for byte. A restore commits the loopback sink, and a preflight rolls the same write back. Two new self-test arms prove both on throwaway clusters: 34/34 arms, calibrated RED.**

CALIBRATION-RED: restore-fragment-concat neuter=FAIL restored-cmp=0 restored=OK

## What changed (commit `8559c9e2d`)

- **`build_transaction`.** The new block sits directly after the `TXN_REFDATA_GATE` heredoc and its `note "refdata: …"` line, and before `TXN_LEDGER_DDL`. It runs `rearm_sql=$(NORMALIZE_EXPECT_MARKER_RE="$RESTORE_EXPECT_MARKER_RE" NORMALIZE_REFUSE_MARKER_RE="$RESTORE_REFUSE_MARKER_RE" bash "$REARM_EMITTER" --emit-restore-sql) || rearm_rc=$?`. A non-zero exit calls `fail`, and so does an empty fragment. The fragment is appended with `printf '%s\n'`. A separate `note "rearm: …"` line prints after it, and the `restore: tables=…` summary format is unchanged. There is no mode test around the block. Measured placement: gate terminator, then the emitter call, then the ledger DDL, then the terminator block. There are 0 `if [ "$mode"` lines between the emitter call and the terminator, and `COMMIT;`/`ROLLBACK;` each still appear once.
- **D-28.** No `normalize.sql` is written. `refuse_credential_in_published_sql`'s `staged=(…)` list is unchanged. `.github/workflows/test-restore-from-baseline.yml` and the REAL `scripts/restore-test-refdata-allowlist.txt` are unchanged against PHASE-BASE (`git diff --quiet` exit 0, inside the verify below).
- **`REARM_EMITTER` seam.** The default is `$SCRIPT_DIR/test-only-normalize-analytics-url.sh`. It is documented in the ENV SEAMS block, and `run_leg` passes it.
- **Fixture.** `baseline-fixture.sql` gains `public.system_settings` in the dump's own style. Its allow-list CHECK admits a Railway-shaped host OR `^…http://127\.0\.0\.1:[0-9]+$`, a port pattern that never carries the sink literal. The fixture's count prose now reads 3 CREATE TABLE lines. `fixture_c.sql` gets an unqualified `INSERT INTO system_settings … ON CONFLICT (key) DO NOTHING` at its foot, seeding the synthetic stand-in host. The FIXTURE `refdata-allowlist.txt` gets one TAB line (`public.system_settings`, 1 statement), and its header prose now says two entries.
- **Arms.** Arm 33 is GREEN in `--mode restore`. It checks the premise (no `public.system_settings` before the run), reads the expected sink through `--print-sink` with the exit status and a non-empty value both checked, and requires the committed row to equal it. It then requires the rearm note, exactly one emitter header in `restore.sql`, and the exact summary line. Arm 34 is GREEN in `--mode preflight`. It seeds the stand-in row inline and then requires `post-census == pre-census`, the rearm note, exactly one emitter header in the preflight's `restore.sql`, and the row still reading as the stand-in, not the sink. Neither arm prints a value, only a length.

## Pin moves (all measured, none deleted)

| pin | before (plan 01 table / measured) | after (measured) |
|---|---|---|
| `EXPECTED_ARMS=` | 32 | **34** (+2, one per new arm; dated `# MEASURED 2026-09-25 — … prints 34/34`) |
| `grep -aqxF 'restore: tables=` exact-line pins | `grep -c "grep -aqxF 'restore: tables=2"` = **4** (`tables=2 policies=1 functions=2 ledger_rows=3 survivors=3/3` ×3, `survivors=4/4` ×1) | the same 4 now read `tables=3 …`, plus arm 33's own = **5** `tables=3` pins |
| arm 11 shape-mismatch needles | `expected 3` / `public holds 2 table(s)` | `expected 4` / `public holds 3 table(s)` (measured from the RED output) |
| arm 23 leg (a) needle | `reference table(s) public.fx_keep are EMPTY after the replay` | `reference table(s) public.fx_keep, public.system_settings are EMPTY after the replay` (measured; the gate collects all empty tables before raising) |
| closing `self-test OK` sentence | … | now also names the in-transaction normalisation |
| fixture ledger rows (`ledger_rows=`) | 3 | 3 (unchanged; the seed went into an existing fixture migration) |

The `tables=2` literals left in arms 25 and 28 to 30 are in synthetic census files those arms write themselves. They do not pin the fixture shape and were not moved.

## Calibration (observed)

1. The script was byte-backed-up to the scratchpad. The one line `printf '%s\n' "$rearm_sql" >> "$out"` was deleted, and `grep` confirmed it was absent.
2. The self-test then exited 1 with `SELF-TEST FAIL: 32/34 arms behaved as declared.`. Arm 33 FAILED naming the row: `MEASURE_FAIL: after a COMMITTED restore the analytics_service_url row in public.system_settings is NOT the loopback sink (it reads as a value of length 40). The PROD-shaped destination the replay restored was committed.`. Arm 34 FAILED with `the preflight's restore.sql does not carry the emitter's fragment exactly once`.
3. The file was restored with `cp`, and `cmp` exited 0.
4. The self-test was re-run: exit 0, `self-test OK (34/34 arms …`.

The first neuter attempt, before arm 33 was reordered, also went RED, but on its restore.sql check rather than on the row. I reordered the arm so the database reading comes first, then re-ran the whole neuter, RED, `cp`/`cmp`, GREEN cycle on the final bytes. The line above records that final cycle.

## Verification

Plan verify, run verbatim after the SUMMARY was written (tail, closing sentence abbreviated here with `…`):

```
  ok   32 GREEN the reference-data value-pinning leg is QUIET when the row holds its pinned value (164.9-07) (exit 0)
  ok   33 GREEN restore — the analytics destination is re-normalised INSIDE the transaction and the COMMITTED row is the sink (164.9.1-05) (exit 0)
  ok   34 GREEN preflight — the same fragment runs and ROLLS BACK: the seeded stand-in survives and the census still matches (164.9.1-05) (exit 0)
restore-test-from-baseline: self-test OK (34/34 arms — … the analytics destination is re-normalised INSIDE the transaction so a restore commits the loopback sink and a preflight rolls the same fragment back, harness calibrated)
REARM-RESTORE-OK
```

Static pins, measured and NOT this plan's to move: `npx vitest run src/__tests__/restore-test-from-baseline.test.ts src/__tests__/test-restore-workflow-wiring.test.ts` gives 158 passed and 2 failed. The two failures are `EXPECTED_ARMS=32 is a live line, exactly once, …` and `plan 01's interim closing line is GONE …`. Both are `EXPECTED_ARMS=32` literals, which plan 06 moves to the value set here (D-18). The wiring test and E2 are green.

## Deviations from Plan

1. **[Rule 3 - blocking] `REARM_EMITTER` seam added.** The plan names `bash "$SCRIPT_DIR/test-only-normalize-analytics-url.sh"`. Self-test arms 11, 14, 23 and 27 run scratch copies of the script from the harness's mktemp dir. There `SCRIPT_DIR` is that tmp dir, so a hard `$SCRIPT_DIR` path would have failed every one of them for a reason unrelated to what the arm tests. The default is still that path, and the seam is documented in ENV SEAMS. The first draft of that entry named the two marker seams verbatim, which made the IN-05/WR-04 per-seam deletion calibration in the static test go blind for `RESTORE_EXPECT_MARKER_RE`. I reworded it ("the TEST and PROD seams above").
2. **[Rule 3 - blocking] Arm 34 seeds inline, not via an overlay file.** A `fixtures/arm-analytics-url-standin.sql` overlay turned the static test "exactly seven arm overlays" RED. That census is outside plan 06's listed moves, so the overlay was removed before commit and the seed is written in the arm.
3. **[Rule 1 - pin re-measure] Arm 11 and arm 23 leg (a) needles moved.** The plan named the `restore: tables=` pins. The new fixture table also moved arm 11's two shape counts and the text arm 23(a) greps for. Both were re-measured from the RED output, and each still names what it named before.
4. **Worktree branch-namespace assertion not applied.** This checkout is the `feat/164.9.1-jobrpctruth` worktree pinned by the orchestrator (same as plans 01 to 04). The protected-branch check was run and passed.
5. **Plan head.** The plan started at `dbe9f867d`. Plan 07 committed `dd1fb6d25` and `40aa2643a` in parallel before this plan's first commit, so the ledger base is `40aa2643a` and `commits:` counts from there.

## Known Stubs

None.

## Threat Flags

None. T-164.9.1-19 to 23 are mitigated as the register says: the write happens in-transaction and is proven by arm 33, the preflight rollback by arm 34, the pre-transaction PROD-marker refusal is unchanged (arm 3 green) and the fragment re-reads the marker with the restore's own predicates, no new published file exists, and an emitter failure is `fail`ed. No softening idiom was added.

## Self-Check: PASSED

- The four modified files exist, and `8559c9e2d` is on `feat/164.9.1-jobrpctruth`.
- The plan's automated verify printed `REARM-RESTORE-OK` (tail above).
