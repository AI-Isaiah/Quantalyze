---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 10
type: execute
wave: 6
depends_on: ["164.9.1-02", "164.9.1-03", "164.9.1-05", "164.9.1-06", "164.9.1-07", "164.9.1-08", "164.9.1-09"]
files_modified:
  - scripts/live-db-execution-ledger.txt
  - scripts/live-db-execution-ledger.mjs
  - docs/runbooks/test-analytics-url.md
  - supabase/tests/test_analytics_service_settings_and_vault_tick.sql
autonomous: true
requirements:
  - "[164.9-FANIN-STATUS-NEVER-SET]"
  - "[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]"
  - "[164.9-TEST-ANALYTICS-URL-REARM]"

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-20 / orchestrator constraint 6: exactly the three K3 ledger lines this phase closes (g10b P12, match-decisions-xor-rls, request-allocator-holdings-sync-queued) are removed, '# ENTRY_COUNT' and ENTRY_CEILING are each lowered by exactly 3 from the value READ on the rebased tree, and the three matching K3_ARMS tokens are removed — all in ONE commit (precedent: 164.4.2 commit 31d9c1fdf)."
    - "D-08 / criterion 6: npm run test:live-db:ledger is GREEN on the lane with M1, M2 and M3 replayed: the failing set equals the shrunk ledger exactly, and the three closed arms plus the P12-harm, Queued-refusal and XOR md-NULL arms pass."
    - "D-18: the runbook and arm D1's header say the restore now re-normalises in-transaction, and that a hand run of the runbook is still the remedy when a migration re-seeds the row."
    - "D-25: exactly three migrations were added by this phase (FRONTIER_EXEMPT_CEILING is met, not exceeded)."
    - "D-24: VAC-08 (scripts/test-ledger-drift-check.sh) is NOT edited by this phase."
    - "Nothing lowers FILES_FLOOR or ARMS_FLOOR, raises WAIVED_CEILING, widens an allowlist or adds a waiver; the data-dependence linter baseline gains no line."
  artifacts:
    - path: scripts/live-db-execution-ledger.txt
      provides: "three K3 lines removed; ENTRY_COUNT lowered by 3"
    - path: scripts/live-db-execution-ledger.mjs
      provides: "ENTRY_CEILING lowered by 3 with a dated SHRUNK note; three K3_ARMS tokens removed"
  key_links:
    - from: "scripts/live-db-execution-ledger.mjs ENTRY_CEILING"
      to: "src/__tests__/live-db-execution-ledger.contract.test.ts (ENTRY_CEILING must EQUAL entries)"
      via: "static ratchet"
      pattern: "ENTRY_CEILING"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> The 164.4.2 tree read 10 / 10 on 2026-09-24, which would make this plan's target 7 / 7 — that is an
> ILLUSTRATION. The executor reads the live values and subtracts 3.
> Every "phase base" in this plan's commands is `PB`, the sha on the `PHASE-BASE:` line of plan 01's SUMMARY,
> parsed by the command itself (the diffs must compare against the tree this phase started from, not against
> unrelated later main commits). `git diff "$PB" -- <path>` compares that commit with the WORKING TREE, so a
> committed change and an uncommitted one both show.

<objective>
Close the phase's regression gate where the defects were found — the live-DB lane — by shrinking its
execution ledger by exactly the three entries this phase fixed, and bring the two prose records that
described the restore hazard up to date. Then run the whole-phase gate sweep on a clean tree.

Why one commit for the ledger: the ledger gate reds on a STALE entry (a listed arm that now passes) and on
a stale-high ceiling, so removing lines and lowering the ceiling separately would leave a red commit
(research Pitfall 9). Plans 02, 07 and 09 (and plan 01's harm arm) were committed locally and are lane-red against the old ledger by
design; nothing has been pushed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-RESEARCH.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shrink the live-DB ledger by the three closed K3 entries in one commit; the lane gate is GREEN (D-08, D-20)</name>
  <files>scripts/live-db-execution-ledger.txt, scripts/live-db-execution-ledger.mjs</files>
  <read_first>
    - `scripts/live-db-execution-ledger.txt` in full (header incl. `# ENTRY_COUNT =`, the "VERDICTS, NEVER SPECIMENS" rule, the `K3-migration-or-invariant-decision  N entries` block, the three TAB lines for g10b `P12:`, match-decisions-xor-rls `widened UNIQUE:`, request-allocator-holdings-sync-queued `Queued path`).
    - `scripts/live-db-execution-ledger.mjs`: `ENTRY_CEILING` and its `// SHRUNK …` history, `K3_ARMS` (tokens `P12:`, `widened UNIQUE:`, `Queued path`, and the evidence-keyed wizard entry that STAYS), and the header sentence counting the K3 keys.
    - `git show 31d9c1fdf` (the precedent diff), `src/__tests__/live-db-execution-ledger.contract.test.ts` (verify-only).
  </read_first>
  <action>
    Read `# ENTRY_COUNT` and `ENTRY_CEILING` on the tree (call them N). In ONE commit: delete exactly the three TAB lines
    named above (re-locate them by content, never by line number); set `# ENTRY_COUNT = N-3` and
    `export const ENTRY_CEILING = N-3` with a dated `// SHRUNK 2026-MM-DD (Phase 164.9.1 plan 10): N -> N-3, …` note naming
    the three fixes (M1, M2, D-14 rewrite); remove the three token-keyed `K3_ARMS` entries (`P12:`, `widened UNIQUE:`,
    `Queued path`) and keep every other entry; update the `.txt` K3 count block and the `.mjs` header sentence that counts
    the K3 keys. Prose added follows "VERDICTS, NEVER SPECIMENS": no ids, no values. Do not touch
    `src/__tests__/live-db-execution-ledger.contract.test.ts`.

    Re-boot the lane from clean (`bash scripts/local-stack/run.sh down` then `up`) and run `npm run test:live-db:ledger`.
    GREEN means the failing set equals the shrunk ledger. If any OTHER arm fails, that is new drift — stop and surface;
    never add a ledger line to make it green (growth is a founder decision). Run `node scripts/live-db-execution-ledger.mjs --self-test`
    and the contract test. Commit hygiene: node_modules symlink removed around commit; the three forbidden `.planning` paths
    never staged. No CI skip token.
  </action>
  <precondition>Plans 02 through 09 committed; plan 01's SUMMARY carries a `PHASE-BASE:` line; Docker up; no other local stack booted on this host.</precondition>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.9.1-10"; mkdir -p "$S"; L=scripts/live-db-execution-ledger.txt; C="$(sed -nE 's/^# ENTRY_COUNT = ([0-9]+)$/\1/p' "$L")"; E="$(sed -nE 's/^export const ENTRY_CEILING = ([0-9]+);$/\1/p' scripts/live-db-execution-ledger.mjs)"; N="$(awk '!/^#/ && NF' "$L" | wc -l | tr -d ' ')"; test -n "$C" && test "$C" = "$N" && test "$E" = "$N" && ! grep -qE 'P12: enqueue with parent_job_ids|bridge_outcomes widened UNIQUE: strategy-sourced|Queued path > returns \{already_inflight' "$L" && ! grep -qE 'token: "(P12:|widened UNIQUE:|Queued path)"' scripts/live-db-execution-ledger.mjs && node scripts/live-db-execution-ledger.mjs --self-test && npx vitest run src/__tests__/live-db-execution-ledger.contract.test.ts --reporter=dot > "$S/contract.log" 2>&1 && ! grep -qE '\b0 (passed|tests)\b' "$S/contract.log" && npm run test:live-db:ledger > "$S/ledger.log" 2>&1; RC=$?; tail -30 "$S/ledger.log"; test "$RC" = 0 && echo LEDGER-OK</automated>
    <fails_when>no "LEDGER-OK": ENTRY_COUNT, ENTRY_CEILING and the line count disagree, a closed line or K3 token remains, the ledger self-test or contract test failed or ran zero tests, or the lane ledger gate is not green (a STALE entry, a new failure, or the lane is down).</fails_when>
  </verify>
  <acceptance_criteria>
    - `# ENTRY_COUNT`, `ENTRY_CEILING` and the non-comment line count are equal, and each is exactly 3 lower than the value recorded in plan 01's pin table (re-read if the base moved; the SUMMARY states both numbers).
    - None of the three closed arm identities appears in the ledger; none of the three tokens appears in `K3_ARMS`; the wizard evidence entry remains.
    - `npm run test:live-db:ledger` exits 0 on the lane with M1, M2, M3 replayed.
    - The ledger diff is in a single commit (`git log --format=%H "$PB"..HEAD -- scripts/live-db-execution-ledger.txt scripts/live-db-execution-ledger.mjs` shows exactly one commit, and it touches both files).
  </acceptance_criteria>
  <reversibility rating="reversible">Shrink-only ledger edit; re-adding a line is a founder decision, but reverting the commit is mechanical.</reversibility>
  <done>The lane's regression gate holds the three fixes: they now pass and the ledger no longer excuses them.</done>
</task>

<task type="auto">
  <name>Task 2: Closing prose for the REARM, then the whole-phase gate sweep on a clean tree (D-18, D-24, D-25, D-07)</name>
  <files>docs/runbooks/test-analytics-url.md, supabase/tests/test_analytics_service_settings_and_vault_tick.sql</files>
  <read_first>
    - `docs/runbooks/test-analytics-url.md` §7 (the "future restore re-arms the hazard" paragraph).
    - `supabase/tests/test_analytics_service_settings_and_vault_tick.sql` arm D1's header ("So a future restore RE-ARMS it"); its `RED-UNDER` / `RED-UNDER-M` lines (NOT edited).
    - `CLAUDE.md` "SQL gate integrity jobs" (the linters and the mutation runner's floors read by symbol).
  </read_first>
  <action>
    Rewrite the runbook §7 paragraph and D1's header comment so they say: a restore now re-normalises the destination
    row inside its own transaction (plan 05), preflight writes nothing, and a hand run of this runbook remains the remedy
    when a MIGRATION re-seeds the row or the restore refuses. Prose only — do not change any SQL statement, `RED-UNDER`
    annotation or arm logic in the D1 file. Commit.

    Then run the sweep and paste each command's final verdict line into the SUMMARY:
    `node scripts/lint-sql-gates.mjs --self-test && node scripts/lint-sql-gates.mjs`;
    `node scripts/lint-app-guc.mjs --self-test && node scripts/lint-app-guc.mjs`;
    `node scripts/lint-migration-data-dependence.mjs --self-test && node scripts/lint-migration-data-dependence.mjs` (0 NEW);
    `npx tsx scripts/dump-sql-functions.ts --check`;
    the whole `supabase/tests/test_*.sql` corpus on the lane DSN (extracted from `scripts/local-stack/.stack-env` and refused
    unless `scripts/local-stack/capability-probe.mjs --refuse-nonlocal-dsn` accepts it), each file with `psql -X -v ON_ERROR_STOP=1 -q -f`, skipping and printing only files whose text carries a `-- LANE-ONLY:` header line (the `sql-tests` job's own exclusion predicate), so every file is either run or named;
    `node scripts/mutation-runner/run.mjs --file` for `supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql` and for the D1 file
    (each exit 2 with a clean narrowed verdict; `PGBIN` set);
    `bash scripts/test-only-normalize-analytics-url.sh --self-test`; `bash scripts/restore-test-from-baseline.sh --self-test`;
    `npm run typecheck`; `npm run lint`; `npx vitest run` (static suite);
    the floor symbols `grep -nE '^export const (FILES_FLOOR|ARMS_FLOOR|WAIVED_CEILING)' scripts/mutation-runner/run.mjs` equal to plan 01's reading;
    `git diff --quiet "$PB" -- scripts/test-ledger-drift-check.sh` exits 0 (D-24: VAC-08 not edited);
    the count of migrations this phase added equals 3 (D-25).
    Any red is fixed at its root cause before this plan completes; nothing is waived. Leave the lane as it is at the end of the action: the verify below boots its OWN fresh lane (`run.sh down` then `up`, the same shape as `sql-tests` and as plans 11/12) so the corpus never runs on the lane Task 1 already wrote to, reads `DB_URL` from the `.stack-env` that fresh `up` writes, and tears that lane down unconditionally right after the corpus stage, outside the pass path.
  </action>
  <verify>
    <automated>export PGBIN="${PGBIN:-/opt/homebrew/opt/postgresql@16/bin}"; S="${TMPDIR:-/tmp}/gsd-164.9.1-10"; mkdir -p "$S"; rm -f "$S/ldd.log" "$S/mutd1.log" "$S/mutdd.log" "$S/tc.log" "$S/vitest.log" "$S/normst.log" "$S/restst.log" "$S/lint.log" "$S/corpus-one.log"; PB="$(sed -nE 's/^PHASE-BASE: ([0-9a-f]{40})$/\1/p' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md)"; test -n "$PB" && ! grep -qF 'future restore re-arms the hazard' docs/runbooks/test-analytics-url.md && ! grep -qF 'So a future restore RE-ARMS it' supabase/tests/test_analytics_service_settings_and_vault_tick.sql && node scripts/lint-sql-gates.mjs --self-test && node scripts/lint-sql-gates.mjs && node scripts/lint-app-guc.mjs --self-test && node scripts/lint-app-guc.mjs && node scripts/lint-migration-data-dependence.mjs > "$S/ldd.log" 2>&1 && tail -3 "$S/ldd.log" && grep -qE '\b0 NEW\b' "$S/ldd.log" && npx tsx scripts/dump-sql-functions.ts --check && ADDED="$(git diff --name-only --diff-filter=A "$PB" -- supabase/migrations)" && test "$(printf '%s\n' "$ADDED" | awk NF | wc -l | tr -d ' ')" = 3 && git diff --quiet "$PB" -- scripts/test-ledger-drift-check.sh scripts/lint-migration-data-dependence-baseline.txt scripts/restore-test-refdata-allowlist.txt && FL_NOW="$(grep -E '^export const (FILES_FLOOR|ARMS_FLOOR|WAIVED_CEILING) = ' scripts/mutation-runner/run.mjs)" && git show "$PB":scripts/mutation-runner/run.mjs > "$S/run-base.mjs" && FL_BASE="$(grep -E '^export const (FILES_FLOOR|ARMS_FLOOR|WAIVED_CEILING) = ' "$S/run-base.mjs")" && test "$(printf '%s\n' "$FL_NOW" | awk NF | wc -l | tr -d ' ')" = 3 && test "$FL_NOW" = "$FL_BASE" && printf '%s\n' "$FL_NOW" | grep -qxF 'export const WAIVED_CEILING = 0;'; A=$?; bash scripts/test-only-normalize-analytics-url.sh --self-test > "$S/normst.log" 2>&1; NST=$?; tail -2 "$S/normst.log"; bash scripts/restore-test-from-baseline.sh --self-test > "$S/restst.log" 2>&1; RST=$?; tail -2 "$S/restst.log"; npm run lint > "$S/lint.log" 2>&1; LNT=$?; tail -5 "$S/lint.log"; rm -f "$S/up.log"; bash scripts/local-stack/run.sh down >/dev/null 2>&1; bash scripts/local-stack/run.sh up > "$S/up.log" 2>&1; UP=$?; tail -3 "$S/up.log"; DSN="$(sed -n 's/^DB_URL="\{0,1\}\([^"]*\)"\{0,1\}$/\1/p' scripts/local-stack/.stack-env 2>/dev/null | head -1)"; CORPUS=1; TOT=0; RAN=0; LO=0; BAD=0; if test "$UP" = 0 && test -n "$DSN" && LOOPBACK_DSN="$DSN" node scripts/local-stack/capability-probe.mjs --refuse-nonlocal-dsn > /dev/null; then for f in supabase/tests/test_*.sql; do TOT=$((TOT+1)); if grep -a -m1 -q '^-- LANE-ONLY:' "$f"; then LO=$((LO+1)); echo "corpus LANE-ONLY (not run here): $f"; continue; fi; if "$PGBIN/psql" -X -v ON_ERROR_STOP=1 -q -d "$DSN" -f "$f" > "$S/corpus-one.log" 2>&1; then RAN=$((RAN+1)); else BAD=$((BAD+1)); echo "corpus FAIL: $f"; tail -5 "$S/corpus-one.log"; fi; done; test "$TOT" -gt 0 && test "$RAN" -gt 0 && test "$BAD" = 0 && test $((RAN+LO)) = "$TOT" && CORPUS=0; fi; echo "corpus: up=$UP total=$TOT ran=$RAN lane-only=$LO failed=$BAD"; bash scripts/local-stack/run.sh down >/dev/null 2>&1; node scripts/mutation-runner/run.mjs --file supabase/tests/test_analytics_service_settings_and_vault_tick.sql > "$S/mutd1.log" 2>&1; M1RC=$?; tail -5 "$S/mutd1.log"; node scripts/mutation-runner/run.mjs --file supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql > "$S/mutdd.log" 2>&1; M2RC=$?; tail -5 "$S/mutdd.log"; npm run typecheck > "$S/tc.log" 2>&1; B=$?; npx vitest run --reporter=dot > "$S/vitest.log" 2>&1; C=$?; tail -8 "$S/vitest.log"; echo "status: static=$A normSelfTest=$NST restoreSelfTest=$RST lint=$LNT corpus=$CORPUS mutD1=$M1RC mutDedupe=$M2RC typecheck=$B vitest=$C"; test "$A" = 0 && test "$NST" = 0 && test "$RST" = 0 && grep -qF 'self-test OK' "$S/restst.log" && test "$LNT" = 0 && test "$CORPUS" = 0 && test "$M1RC" = 2 && test "$M2RC" = 2 && grep -qF 'No defects in the narrowed scope.' "$S/mutd1.log" && grep -qF 'No defects in the narrowed scope.' "$S/mutdd.log" && ! grep -qE 'no-red|wrong-first-failure|occurrence-mismatch|bad-file-ref|waiver' "$S/mutd1.log" "$S/mutdd.log" && test "$B" = 0 && test "$C" = 0 && echo SWEEP-OK</automated>
    <fails_when>no "SWEEP-OK". Each stage's status is captured on its own and every log is deleted first, so a failed static check can never be masked by a mutation run's expected exit 2 and no stale log can satisfy a grep: static != 0 means the phase base cannot be read, the stale "re-arms" prose remains, a linter or self-test is red, the data-dependence linter reports a NEW refusal, the snapshots are stale, the phase added other than exactly 3 migrations, or VAC-08 / the data-dependence baseline / the real refdata allowlist differ from the phase base, or the three `FILES_FLOOR` / `ARMS_FLOOR` / `WAIVED_CEILING` lines differ from their text at the phase base (the commit plan 01's pin table was read at, so "equal to plan 01's reading" is checked against the base commit rather than a copied number) or `WAIVED_CEILING` is not 0; normSelfTest or restoreSelfTest != 0 (or the restore self-test printed no `self-test OK` line) means a restore-path self-test is red; lint != 0 means `npm run lint` (eslint plus the manifest, route-contract and planning-hygiene checks) is red; corpus != 0 means the fresh lane the verify boots (`run.sh down` then `up`, torn down again unconditionally right after the corpus stage whatever its verdict) failed to come up, the lane handoff has no DB_URL or it is not loopback (refused, never run elsewhere), the corpus glob was empty, no file ran, a file failed under `ON_ERROR_STOP`, or ran plus LANE-ONLY exclusions do not account for every file (the same `-- LANE-ONLY:` predicate the `sql-tests` job applies); mutD1 or mutDedupe != 2 or a missing clean verdict / a named defect class means a narrowed mutation run is not clean; typecheck or vitest != 0 means the type-check or the static vitest suite failed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Neither stale sentence remains; the D1 file's non-comment lines are unchanged (`git diff "$PB" -- supabase/tests/test_analytics_service_settings_and_vault_tick.sql`, base commit versus working tree, touches comment lines only).
    - The SUMMARY holds one verdict line per sweep command listed in the action, including the per-file corpus results on the lane and both self-test counts.
    - Exactly 3 migration files added versus the phase base `PB`; `git diff --quiet "$PB" -- scripts/test-ledger-drift-check.sh` exits 0.
    - Floors equal plan 01's readings; `WAIVED_CEILING` is 0.
    - `npx tsx scripts/check-planning-hygiene.ts` passes after the SUMMARY is written.
  </acceptance_criteria>
  <reversibility rating="reversible">Prose edits and read-only checks.</reversibility>
  <done>The branch is green on every local gate the phase touches, with the evidence written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger / SUMMARY prose -> public repo | verdicts only, never specimens |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-27 | Repudiation | a ledger that silently excuses a regression | medium | mitigate | shrink-only; ceiling equals entries (contract test); STALE detection; no line added to go green |
| T-164.9.1-28 | Information disclosure | ledger prose, SUMMARY sweep output | medium | mitigate | "VERDICTS, NEVER SPECIMENS"; check-planning-hygiene after writing |
| T-164.9.1-29 | Tampering | a waiver or floor change hiding a red gate | medium | mitigate | floors compared with plan 01 readings; WAIVED_CEILING 0; baselines/allowlists diff-checked against the phase base (plan 01's `PHASE-BASE:` sha) |
</threat_model>

<verification>
- LEDGER-OK and SWEEP-OK printed.
</verification>

<success_criteria>
- Criterion 6 closed in the lane; the whole local gate set is green.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| shrunk live-DB execution ledger (-3, ceiling -3, tokens -3) | shrink-only gate | frontend-live-db-lane CI |
| updated runbook §7 and D1 header prose | operator record | founder (FC-3), future readers |
| sweep verdict table (SUMMARY) | evidence | plan 11 reviewers, gsd-verifier |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-10-SUMMARY.md` when done.
</output>
