---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 03
type: execute
wave: 3
depends_on: ["164.9.1-02"]
files_modified:
  - supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql
  - supabase/migrations/<ts1>_fanin_initial_status_10param.sql
autonomous: true
requirements:
  - "[164.9-FANIN-STATUS-NEVER-SET]"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-08 / criterion 6: arm P12 in the g10b live-DB file goes RED (child status pending) when M1's status column is neutered, and GREEN again after a byte-exact restore of M1 — observed by execution on the lane, recorded in the SUMMARY."
    - "Research Pitfall 1: in ONE commit, M1's path becomes the LAST element of the dedupe gate's RED-UNDER-SETUP apply list and twins B1/B2 are re-pointed at M1 with find/replace/occurrences byte-identical; the narrowed mutation run reports RED (identity ok) for both."
    - "D-26: no new supabase/tests/*.sql arm is added; no twin is added or removed."
    - "FILES_FLOOR, ARMS_FLOOR and WAIVED_CEILING in scripts/mutation-runner/run.mjs do not move; the file is unchanged against the phase base plan 01 recorded."
    - "M1 is byte-identical to its plan-02 commit when this plan ends (the neuter is temporary; restore is by cp from a byte backup, never git checkout)."
  artifacts:
    - path: supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql
      provides: "RED-UNDER-SETUP apply list ending at M1; twins B1/B2 re-pointed at M1"
  key_links:
    - from: "test_enqueue_compute_job_dedupe_non_terminal.sql twins B1/B2"
      to: "M1's strategy-arm optimistic look-up text (byte-identical, 5-space WHERE indent)"
      via: "mutation-runner edit twin find string, occurrences 1"
      pattern: "RED-UNDER-M"
    - from: "arm P12 (g10b live-DB file)"
      to: "M1's INSERT column list (status / v_initial_status)"
      via: "lane replay of the neutered then restored M1"
      pattern: "v_initial_status"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> Split out of plan 02 (checker iteration 1: per-plan budget). It runs in the wave directly after plan 02 and
> before any other plan boots the local stack, because the twins go stale the instant M1 exists (research
> Pitfall 1). Wave coupling that is not a file overlap: Task 2 boots the SAME single local stack plans 02, 07
> and 09 boot (one stack per host). Plan 05 shares this wave; it uses throwaway pg-lane clusters only.

<objective>
Prove P12 can fail (ROADMAP criterion 6 for `[164.9-FANIN-STATUS-NEVER-SET]`, D-08) and keep the dedupe gate's
mutation twins biting the body that is now live (research Pitfall 1, D-26).

Purpose: a green P12 that could not go red would be worse than no arm; and twins pointing at the superseded
20260826150000 body would silently mutate a body nothing executes.
Output: a re-pointed twin pair with a clean narrowed mutation run, and a recorded neuter -> RED -> restore -> GREEN
calibration of P12.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-RESEARCH.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-PATTERNS.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point the dedupe gate's twins at M1 in one commit; the narrowed mutation run bites (research Pitfall 1, D-26)</name>
  <files>supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql</files>
  <read_first>
    - `supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql`: the `RED-UNDER-SETUP` line, twins B1 and B2 (`RED-UNDER-M` objects whose `file` names 20260826150000) and the prose `RED-UNDER:` lines.
    - `scripts/mutation-runner/run.mjs` — the note above `ARMS_FLOOR` that begins "Plan 04 RE-POINTED all 27 pre-existing edit-kind twins" (a precedent from an earlier phase, not a plan of this one), and `--file` narrowed mode. Read `FILES_FLOOR`, `ARMS_FLOOR`, `WAIVED_CEILING` BY SYMBOL.
    - `164.9.1-RESEARCH.md` Pitfall 1 and the Q4 dedupe-gate row; `164.9.1-PATTERNS.md` "test_enqueue_compute_job_dedupe_non_terminal.sql".
    - M1 as plan 02 committed it (to confirm the twins' `find` text occurs exactly once, in the body and in no comment).
  </read_first>
  <action>
    In ONE commit (research Pitfall 1): append M1's path as the LAST element of the `RED-UNDER-SETUP` apply list;
    change the `file` of B1's first edit and of B2 to M1's path, keeping `find`, `replace` and `occurrences`
    byte-identical (B1's second edit on `20260416125430` stays); update the prose `RED-UNDER:` lines to name M1.
    Per D-26 do not add or remove any twin and add no SQL-corpus arm; `FILES_FLOOR`, `ARMS_FLOOR` and
    `WAIVED_CEILING` do not move.

    Run the narrowed mutation run for this file with the pg-lane binaries
    (`PGBIN=/opt/homebrew/opt/postgresql@16/bin` unless already exported) and confirm B1 and B2 each report
    `RED (identity ok)`. If either reports `no-red` or `occurrence-mismatch`, the cause is M1 (its comment carrying
    the `find` text, or a changed indent): fix M1's COMMENT in a separate commit and re-run
    `npm run schema:functions`; never loosen the twin.

    Commit hygiene: `NM="$(readlink node_modules)"; rm node_modules`, commit, `ln -s "$NM" node_modules`; never stage
    `.planning/milestone.lock`, `.planning/state.json` or `.planning/phases/164.5.3-*`. No CI skip token.
  </action>
  <precondition>Plan 02 committed (M1 exists); plan 01's SUMMARY carries a `PHASE-BASE:` line; pg-lane binaries resolvable (`bash scripts/pg-lane/run.sh --print-pgbin` prints a directory).</precondition>
  <verify>
    <automated>export PGBIN="${PGBIN:-/opt/homebrew/opt/postgresql@16/bin}"; S="${TMPDIR:-/tmp}/gsd-164.9.1-03"; mkdir -p "$S"; rm -f "$S/mut.log"; G=supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql; M1B="$(basename "$(ls supabase/migrations/*_fanin_initial_status_10param.sql)")"; PB="$(sed -nE 's/^PHASE-BASE: ([0-9a-f]{40})$/\1/p' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md)"; test -n "$M1B" && test -n "$PB" && grep -E '^-- RED-UNDER-SETUP:' "$G" | grep -qE "${M1B}\"\]\}$" && test "$(grep -E '^[[:space:]]*-- RED-UNDER-M:' "$G" | grep -c "$M1B")" -ge 2 && ! grep -E '^[[:space:]]*-- RED-UNDER-M:' "$G" | grep -q '20260826150000_destrict_enqueue_internal_10param' && git diff --quiet "$PB" -- scripts/mutation-runner/run.mjs && grep -nE '^export const (FILES_FLOOR|ARMS_FLOOR|WAIVED_CEILING)' scripts/mutation-runner/run.mjs; A=$?; node scripts/mutation-runner/run.mjs --file "$G" > "$S/mut.log" 2>&1; RC=$?; cat "$S/mut.log"; test "$A" = 0 && test "$RC" = 2 && test -s "$S/mut.log" && grep -qF 'No defects in the narrowed scope.' "$S/mut.log" && test "$(grep -cF 'RED (identity ok)' "$S/mut.log")" -ge 2 && ! grep -qE 'no-red|wrong-first-failure|occurrence-mismatch|bad-file-ref|waiver' "$S/mut.log" && echo TWINS-OK</automated>
    <fails_when>(The twin lines sit indented two spaces inside their DO blocks on origin/main, lines 174 and 228, so every twin filter allows leading whitespace; the unindented `RED-UNDER-SETUP` line at line 90 keeps its column-0 anchor. Measured against origin/main before the re-point: the whitespace-tolerant filter yields 2 twin lines naming 20260826150000, so the negative check fails on an un-rewritten file.) No "TWINS-OK": the static-shape chain failed (status A non-zero: the apply list does not end at M1, a twin still names 20260826150000, fewer than two twins name M1, run.mjs differs from the phase base, or the floor symbols cannot be read), OR the narrowed run did not exit 2, left an empty log, lacks the clean verdict, shows fewer than two `RED (identity ok)` lines, or names a defect class. The two statuses are captured separately, so a failed shape check can never be masked by the runner's own exit code.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `RED-UNDER-SETUP` apply list's last element is M1's path.
    - Both B1's first edit and B2 name M1's path; their `find`/`replace`/`occurrences` fields are byte-identical to before (`git diff "$PB" -- supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql`, base commit versus working tree, shows only the `file` value change on those lines plus the apply-list and prose `RED-UNDER:` edits).
    - The narrowed mutation run prints `RED (identity ok)` for B1 and B2 and `No defects in the narrowed scope.`, exit 2.
    - `git diff --quiet "$PB" -- scripts/mutation-runner/run.mjs` exits 0, where `PB` is the `PHASE-BASE:` sha from plan 01's SUMMARY (base commit versus working tree: catches committed and uncommitted edits alike).
  </acceptance_criteria>
  <reversibility rating="reversible">Test-annotation re-point only.</reversibility>
  <done>The gate that tests the dedupe look-up follows M1 and bites it.</done>
</task>

<task type="auto">
  <name>Task 2: Calibrate P12 by neuter -> RED -> restore -> GREEN on the lane (D-08, criterion 6)</name>
  <files>supabase/migrations/&lt;ts1&gt;_fanin_initial_status_10param.sql (neuter is temporary and restored byte-for-byte; no commit carries the neuter)</files>
  <read_first>
    - M1 in full: the INSERT column list and VALUES, and the DO block's COPY-CHECK arms for `status` / `v_initial_status`.
    - Arm P12 in `src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts`.
    - `CLAUDE.md` memory note on destructive edits: `git checkout --` restores to HEAD and silently destroys uncommitted work; a byte backup must be re-taken after every edit.
  </read_first>
  <action>
    Take a byte backup of M1 to the scratch dir (`cp "$M1" "$S/m1.bak"`). Neuter it by removing `status` from the
    INSERT column list and `v_initial_status` from VALUES AND disabling the M1 DO block's corresponding copy-check arms
    (otherwise the lane replay FATALs and proves nothing about P12). Re-boot the lane
    (`bash scripts/local-stack/run.sh down` then `up`), run P12 and observe it RED with the child status `pending`.
    Restore with `cp "$S/m1.bak" "$M1"`, verify `cmp "$S/m1.bak" "$M1"` exits 0 and that M1's INSERT carries
    `v_initial_status` again, re-boot, and observe P12 GREEN. Never `git checkout --` to restore; never commit the neuter.
    Record the RED and GREEN observations (test name + status only, no ids) in the SUMMARY with the `cmp` exit.
    Machine-checkable record: write into this plan's SUMMARY (create it if absent) exactly one line per calibration,
    `CALIBRATION-RED: p12-m1-status neuter=FAIL restored-cmp=0 restored=OK`,
    each written only AFTER its neuter was observed RED, the `cp` restore was `cmp`-verified (exit 0) and the arm was
    observed GREEN again; never pre-write one. A neuter that did not go RED means the arm is not armed: fix the arm.
    Finish with `bash scripts/local-stack/run.sh down` only if no later step of this plan needs the lane.
  </action>
  <precondition>Task 1 committed; Docker up; no other local stack booted on this host.</precondition>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.9.1-03"; mkdir -p "$S"; rm -f "$S/p12-final.log"; M1="$(ls supabase/migrations/*_fanin_initial_status_10param.sql)"; git diff --quiet HEAD -- "$M1" && grep -v '^\s*--' "$M1" | grep -qF 'v_initial_status'; A=$?; npx vitest run --config vitest.livedb.config.ts src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts -t "P12" --reporter=verbose > "$S/p12-final.log" 2>&1; RC=$?; tail -15 "$S/p12-final.log"; test "$A" = 0 && test "$RC" = 0 && grep -qE '✓.*P12: enqueue with parent_job_ids' "$S/p12-final.log" && ! grep -qE '\b[1-9][0-9]* (failed|skipped)\b' "$S/p12-final.log" && test "$(grep -cxF 'CALIBRATION-RED: p12-m1-status neuter=FAIL restored-cmp=0 restored=OK' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-03-SUMMARY.md)" = 1 && echo P12-CALIBRATED</automated>
    <fails_when>no "P12-CALIBRATED": M1 differs from its committed bytes (the neuter was not restored, or was committed), M1's INSERT lacks v_initial_status, or P12 is not green on the lane after the restore (failed, skipped because the lane is down, or did not run). The RED half of the calibration is evidence recorded in the SUMMARY; this verify proves the restore left the tree and the lane as they were. Also fails when the SUMMARY lacks exactly one `CALIBRATION-RED:` line for `p12-m1-status` (the neuter's RED, the `cmp`-verified restore and the GREEN re-run are recorded as a structured line, not only as prose).</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records: P12 RED under the neuter (child status `pending`), P12 GREEN after restore, and `cmp` of the backup and the restored M1 exited 0.
    - `git diff --quiet HEAD -- <M1>` exits 0 at the end of the plan and no commit on the branch contains the neutered INSERT.
  </acceptance_criteria>
  <reversibility rating="reversible">Temporary working-copy neuter restored from a byte backup.</reversibility>
  <done>P12 is proven able to fail, and the tree is exactly as plan 02 left it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> database | the only permitted databases are the loopback local stack and throwaway pg-lane clusters |
| working copy -> commit | a neutered migration must never be committed (it would auto-apply on merge) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-39 | Tampering | neutered M1 committed and later merged | high | mitigate | restore by `cp` from a byte backup, `cmp` exit 0; verify requires `git diff --quiet HEAD -- <M1>` and v_initial_status present |
| T-164.9.1-40 | Repudiation | twins silently mutating a superseded body | medium | mitigate | apply list and twins re-pointed in one commit; narrowed run must show two `RED (identity ok)` and no defect class |
</threat_model>

<verification>
- TWINS-OK and P12-CALIBRATED printed; the RED half of the calibration recorded in the SUMMARY.
</verification>

<success_criteria>
- Criterion 6 for the fan-in item is proven by execution; the dedupe twins bite M1.
- No floor moved; no allowlist or baseline line added.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| re-pointed B1/B2 twins | mutation-gate annotation | sql-mutation CI job, plan 10 sweep, plan 12 gate re-run |
| P12 calibration record (SUMMARY) | criterion-6 evidence | gsd-verifier, CHANGELOG (plan 14) |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-03-SUMMARY.md` when done.
</output>
