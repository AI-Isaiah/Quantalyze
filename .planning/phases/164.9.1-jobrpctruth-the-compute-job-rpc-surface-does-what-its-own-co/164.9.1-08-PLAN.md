---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 08
type: execute
wave: 5
depends_on: ["164.9.1-07"]
files_modified:
  - src/app/api/allocator/holdings/sync/route.ts
  - src/app/api/allocator/holdings/sync/route.test.ts
autonomous: true
coupling_justified: ["164.9.1-09: order-independent: this plan runs mocked-RPC unit tests only and touches no database, local stack, migration, env var or shared config, while 09 alone boots the one host stack and adds M3; the two are file-disjoint and wave 5 executes each plan in its own worktree (use_worktrees true in .planning/config.json), so they share no git index either; both orders give the same result"]
requirements:
  - "[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "D-23: the route maps exactly the RPC error with code P0001 AND message api_key_disconnected (raised by M2, plan 07) to HTTP 409 with NO_STORE_HEADERS and a fixed user-safe sentence; any other P0001 still falls through to the generic 500; a route unit test fails if the mapping is removed or widened to all of P0001. Flagged as an autonomous decision the founder may overrule at FC-1."
    - "D-09 / D-12: the route's header docblock and the comment above the RPC call describe M2's in-flight prefetch and the {already_inflight, next_attempt_at} / {ok, job_id} shapes, and no longer describe a 23505 / unique_violation handler."
    - "The route keeps passing the RPC's JSONB success body through verbatim and adds no log line carrying the key id."
  artifacts:
    - path: src/app/api/allocator/holdings/sync/route.ts
      provides: "P0001 + api_key_disconnected -> 409 branch; docblock describing the prefetch"
    - path: src/app/api/allocator/holdings/sync/route.test.ts
      provides: "409 test and the P0001-other-message -> 500 test, calibrated"
  key_links:
    - from: "route.ts error branch"
      to: "M2 RAISE 'api_key_disconnected' USING ERRCODE 'P0001'"
      via: "error.code === 'P0001' && error.message === 'api_key_disconnected'"
      pattern: "api_key_disconnected"
    - from: "AllocatorExchangeManager / sync route"
      to: "request_allocator_holdings_sync already_inflight shape"
      via: "route passes JSONB through verbatim"
      pattern: "already_inflight"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> Split out of plan 07 (checker iteration 1: per-plan budget). Unit tests with a mocked RPC only: no database,
> no local stack, so it may share a wave with plan 09 (file-disjoint).

<objective>
Give a user whose exchange key is disconnected a clear 409 instead of a generic 500 when they press sync
(D-23, `[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]` item a), and stop the route's comments describing a handler that
M2 removed.

Output: one route branch, two tests that can fail, and a corrected docblock.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-PATTERNS.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-07-SUMMARY.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The route maps exactly P0001 api_key_disconnected to 409 and its docblock stops describing the dead handler (D-23)</name>
  <files>src/app/api/allocator/holdings/sync/route.ts, src/app/api/allocator/holdings/sync/route.test.ts</files>
  <read_first>
    - AGENTS.md, then the route-handler guide under `node_modules/next/dist/docs/` (find it with `ls node_modules/next/dist/docs` and read the route handlers page) before editing route code.
    - `src/app/api/allocator/holdings/sync/route.ts` in full (header docblock, the `42501` branch, the generic 500, `NO_STORE_HEADERS`, the verbatim JSONB pass-through).
    - `src/app/api/allocator/holdings/sync/route.test.ts` in full (the `vi.hoisted` `mockRpc` scaffold, test "5. RPC raises SQLSTATE 42501 → 403", the audit-on-success tests).
    - M2 as plan 07 committed it (the exact RAISE message and ERRCODE the route keys on).
    - `164.9.1-PATTERNS.md` sections for these two files.
  </read_first>
  <behavior>
    - RPC error `{ code: "P0001", message: "api_key_disconnected" }` -> HTTP 409, `NO_STORE_HEADERS`, a user-safe message, audit not called.
    - RPC error `{ code: "P0001", message: "something_else" }` -> HTTP 500 (the mapping is not widened to all of P0001).
    - Existing 42501 -> 403 and success-path tests unchanged and passing.
  </behavior>
  <action>
    Per D-23, write the two new tests first (RED against the current route: the 409 case returns 500). Then add, before
    the generic 500 branch and after the `42501` branch, a branch matching `error.code === "P0001" && error.message === "api_key_disconnected"`
    that returns `NextResponse.json({ error: <one user-safe sentence telling the user the key is disconnected and must be reconnected before syncing> }, { status: 409, headers: NO_STORE_HEADERS })`.
    Do not log the key id at a new site; do not rebuild the JSONB success body. Update the header docblock and the
    comment above the RPC call so they describe M2's prefetch (not a 23505 handler) and list the 409 branch (D-09, D-12, D-23).
    Run the file's tests; then calibrate: byte-backup `route.ts` to the scratch dir, comment out the new branch on the
    working copy, observe the 409 test RED, restore with `cp` from the backup, `cmp` it, observe GREEN. Record the RED
    and the `cmp` exit in the SUMMARY. Never `git checkout --` to restore.
    Machine-checkable record: write into this plan's SUMMARY (create it if absent) exactly one line per calibration,
    `CALIBRATION-RED: route-409-branch neuter=FAIL restored-cmp=0 restored=OK`,
    each written only AFTER its neuter was observed RED, the `cp` restore was `cmp`-verified (exit 0) and the arm was
    observed GREEN again; never pre-write one. A neuter that did not go RED means the arm is not armed: fix the arm.

    Commit hygiene: `NM="$(readlink node_modules)"; rm node_modules`, commit, `ln -s "$NM" node_modules`; never stage
    `.planning/milestone.lock`, `.planning/state.json` or `.planning/phases/164.5.3-*`. No CI skip token.
  </action>
  <precondition>Plan 07 committed (M2 exists and raises `api_key_disconnected` with ERRCODE `P0001`).</precondition>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.9.1-08"; mkdir -p "$S"; rm -f "$S/route.log" "$S/tsc.log"; RT=src/app/api/allocator/holdings/sync/route.ts; grep -v '^\s*//' "$RT" | grep -qF '"api_key_disconnected"' && grep -v '^\s*//' "$RT" | grep -qE 'status: 409' && ! grep -qE '23505|unique_violation' "$RT" && npx vitest run src/app/api/allocator/holdings/sync/route.test.ts --reporter=verbose > "$S/route.log" 2>&1; A=$?; cat "$S/route.log"; npx tsc --noEmit -p tsconfig.json > "$S/tsc.log" 2>&1; B=$?; tail -5 "$S/tsc.log"; npx eslint "$RT" src/app/api/allocator/holdings/sync/route.test.ts; C=$?; test "$A" = 0 && test -s "$S/route.log" && ! grep -qE '\b0 (passed|tests)\b' "$S/route.log" && ! grep -qE '\b[1-9][0-9]* (failed|skipped)\b' "$S/route.log" && grep -qE '✓.*409' "$S/route.log" && test "$B" = 0 && test "$C" = 0 && test "$(grep -cxF 'CALIBRATION-RED: route-409-branch neuter=FAIL restored-cmp=0 restored=OK' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-08-SUMMARY.md)" = 1 && echo ROUTE-409-OK</automated>
    <fails_when>no "ROUTE-409-OK": the branch is missing or not a 409, the docblock still names the 23505/unique_violation handler, the branch exists only inside a comment (the calibration's commented-out copy was left behind), a route test failed, was skipped or zero tests ran, no passing test title mentions 409, the type-check failed, or eslint reported an error. Each stage's status is captured separately (A, B, C). Also fails when the SUMMARY lacks exactly one `CALIBRATION-RED:` line for `route-409-branch` (the neuter's RED, the `cmp`-verified restore and the GREEN re-run are recorded as a structured line, not only as prose).</fails_when>
  </verify>
  <acceptance_criteria>
    - `route.ts` contains `error.code === "P0001"` and `"api_key_disconnected"` in one condition, and `status: 409` with `NO_STORE_HEADERS`.
    - `route.test.ts` has a 409 test whose title contains `409` and a "P0001 with another message -> 500" test; both pass; the SUMMARY records the RED observed when the branch was commented out and `cmp` exit 0 after the restore.
    - `grep -cE '23505|unique_violation' src/app/api/allocator/holdings/sync/route.ts` returns 0.
    - `npx tsc --noEmit` and eslint on both files exit 0.
  </acceptance_criteria>
  <reversibility rating="reversible">A route branch and two tests. D-23 may be overruled by the founder at FC-1 (plan 14).</reversibility>
  <done>A user whose key is disconnected gets a clear 409, not a generic 500.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (authenticated user JWT) -> route -> SECDEF RPC | untrusted `api_key_id` crosses here; ownership is checked inside the RPC (plan 07) |
| RPC error -> user-visible JSON | error text must be user-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-13 | Information disclosure | route error body | medium | mitigate | 409 message is a fixed user-safe sentence; the mapping keys on code AND message; no new log line carrying key id |
| T-164.9.1-43 | Tampering | mapping widened to every P0001 (other raises masked as "disconnected") | medium | mitigate | the P0001-other-message -> 500 test; calibration recorded |
</threat_model>

<verification>
- ROUTE-409-OK printed; calibration recorded.
</verification>

<success_criteria>
- D-23's user-facing half delivered with a failing-capable unit test; the route's comments describe the code that runs.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| 409 route branch + tests | user-facing behaviour | FC-1 review (D-23 overrule point, plan 14), plan 11 silent-failure-hunter |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-08-SUMMARY.md` when done.
</output>
