---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 12
type: execute
wave: 8
depends_on: ["164.9.1-11"]
files_modified:
  - supabase/migrations/<ts1>_fanin_initial_status_10param.sql
  - supabase/migrations/<ts2>_allocator_sync_restore_inflight_prefetch.sql
  - supabase/migrations/<ts3>_bridge_outcomes_invariant_comments.sql
  - supabase/schema/functions/_enqueue_compute_job_internal.sql
  - supabase/schema/functions/request_allocator_holdings_sync.sql
  - src/app/api/allocator/holdings/sync/route.ts
  - scripts/test-only-normalize-analytics-url.sh
  - scripts/restore-test-from-baseline.sh
autonomous: false
requirements:
  - "[164.9-FANIN-STATUS-NEVER-SET]"
  - "[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]"
  - "[164.9-TEST-ANALYTICS-URL-REARM]"

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-21: a second round re-reviews the round-1 fixes — whenever plan 11 recorded ROUND1-FIX-COUNT > 0, ALL THREE reviewers (migration-reviewer, rls-policy-auditor, silent-failure-hunter) are re-dispatched by the ORCHESTRATOR, in ONE message, each on EVERY round-1 fixing diff plus the touched files, because a fix prompted by one reviewer can regress another reviewer's surface; at most two rounds, then the verifier. Only when ROUND1-FIX-COUNT is 0 does no re-dispatch happen, and that is recorded. Task 2's verify enforces this pairing mechanically."
    - "After round 2, anything still open is recorded as a blocker for FC-1 with its reason; nothing is deferred without a named phase."
    - "The post-fix-round gate re-run DECIDES the verdict: REVIEW-GATES-OK prints only when the lane (all three migrations replayed) + ledger gate, the data-dependence linter (0 NEW), the snapshot check, exactly three added migrations, both self-tests, the route and restore static tests and the dedupe narrowed mutation run are all green, each status captured separately, with the lane torn down unconditionally."
    - "D-07 / D-25 still hold after round 2: catalog-only DO blocks, 0 NEW, exactly three migrations, prod-body-ack lines re-derived if a function body changed."
  artifacts:
    - path: ".planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-12-SUMMARY.md"
      provides: "'## Round 2' section: re-reviewed diffs, findings, dispositions, any FC-1 blocker, and the final gate verdicts"
  key_links:
    - from: "three-reviewer gate (plans 11 and 12)"
      to: "FC-1 precondition in plan 14"
      via: "SUMMARY findings tables with all findings dispositioned and REVIEW-GATES-OK"
      pattern: "Round 2"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> Split out of the former single review plan (checker iteration 1: reviewer dispatch is an explicit orchestrator
> checkpoint, and the final gate verdict must be decided by the gate chain's own status).

<objective>
Round 2 of the three-reviewer gate (D-21): re-review what round 1 changed, because a fix round is where regressions
enter, then prove every local gate green once more on a freshly booted lane before the phase moves to FC-2.

Output: a `## Round 2` record and a REVIEW-GATES-OK verdict that can only print on a green gate chain.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-11-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: ORCHESTRATOR CHECKPOINT — re-dispatch all three reviewers on every round-1 fix diff, in one message (D-21)</name>
  <files>(none — orchestrator act; reports land outside the repo)</files>
  <read_first>
    - Plan 11 SUMMARY `## Round 1` table and its `ROUND1-FIX-COUNT:` line (the fixing commits).
    - The fixing commits' diffs (`git show <commit>` for each).
    - `164.9.1-CONTEXT.md` D-21 and any D-xx a round-1 fix touched (briefs quote them verbatim).
  </read_first>
  <action>
    The executor STOPS here and returns this checkpoint, listing in its return plan 11's `ROUND1-FIX-COUNT:` value and
    every round-1 fixing commit sha. ACTOR: the orchestrator (not the founder, not the executor). Per D-21, if the count
    is above 0 the orchestrator re-dispatches ALL THREE reviewers (`migration-reviewer`, `rls-policy-auditor`,
    `silent-failure-hunter`), in parallel in ONE message, each on EVERY fixing diff plus the touched files (not only the
    diffs its own findings caused: a migration fix can reopen an ACL, a 409 fix can swallow an error), with the
    instruction to report findings without editing; it writes each report verbatim to
    `${TMPDIR:-/tmp}/gsd-164.9.1-12/review-r2-<reviewer>.md` and resumes with `reviews-delivered`. Only if the count is 0
    does the orchestrator dispatch nothing and resume with `no-round-2: <reason>`.
  </action>
  <instructions>
    Claude (executor) already did: round 1 fixes are committed and every local gate was re-run green (plan 11 SUMMARY).
    The ONE thing the executor cannot do itself: spawn the reviewer agents for the re-review. Orchestrator: re-dispatch
    all three reviewers on all fixing diffs in one message, save their reports to the named files, then resume.
  </instructions>
  <verification>
    On resume the executor checks one non-empty `review-r2-<reviewer>.md` exists for each of the three reviewers when
    plan 11's `ROUND1-FIX-COUNT` is above 0, or that it is 0 and the resume signal was `no-round-2`. Task 2's verify
    enforces the same pairing.
  </verification>
  <resume-signal>Type `reviews-delivered`, `no-round-2: <reason>`, or `cannot-dispatch: <reason>` (the executor then records it and STOPS; it never self-reviews).</resume-signal>
  <verify>
    <automated>D="${TMPDIR:-/tmp}/gsd-164.9.1-12"; mkdir -p "$D"; N="$(ls "$D"/review-r2-*.md 2>/dev/null | wc -l | tr -d ' ')"; echo "round-2 reports present: $N (zero is valid only with the no-round-2 resume signal, recorded in the SUMMARY by Task 2)"</automated>
    <fails_when>never decides alone: the count is recorded; Task 2's verify cross-checks plan 11's `ROUND1-FIX-COUNT` against the three report files and the SUMMARY's `ROUND2-SIGNAL:` line, and the executor refuses to proceed on `cannot-dispatch`.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the orchestrator's resume signal verbatim on one line `ROUND2-SIGNAL: <signal>` and, when reports exist, that all three reviewers re-reviewed every fixing commit.
  </acceptance_criteria>
  <done>The round-1 fixes have been re-reviewed by the reviewers whose findings caused them, or it is recorded why no re-review was needed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix round-2 findings, stop after round 2, and let the re-run gate chain decide REVIEW-GATES-OK (D-21, D-07, D-25)</name>
  <files>(fixes only, if round 2 finds anything — any file listed in this plan's files_modified)</files>
  <read_first>
    - The round-2 report files `${TMPDIR:-/tmp}/gsd-164.9.1-12/review-r2-*.md` (if any).
    - Plan 11's `## Round 1` table and plan 02's prod-body-ack recipe.
  </read_first>
  <action>
    Per D-21, fix round-2 findings at their root cause (same rules as plan 11: snapshots regenerated and prod-body-ack
    re-derived if a function body changes; twins' `find` text still unique). Stop after round 2 regardless: anything still
    open is recorded as a blocker for FC-1 with its reason. Write a `## Round 2` section into this plan's SUMMARY
    (re-reviewed commits, findings, dispositions, FC-1 blockers or "none", and exactly one line `ROUND2-SIGNAL: <the
    resume signal verbatim>`, i.e. `ROUND2-SIGNAL: reviews-delivered` or `ROUND2-SIGNAL: no-round-2: <reason>`), then run this
    task's verify and paste its status line and verdict into that section. Commit hygiene as in plan 11. Public-repo rule
    applies to the SUMMARY.
  </action>
  <precondition>Task 1 resumed with `reviews-delivered` or `no-round-2`; Docker up; no other local stack booted on this host; plan 01's SUMMARY carries a `PHASE-BASE:` line.</precondition>
  <verify>
    <automated>export PGBIN="${PGBIN:-/opt/homebrew/opt/postgresql@16/bin}"; S="${TMPDIR:-/tmp}/gsd-164.9.1-12"; mkdir -p "$S"; rm -f "$S/up.log" "$S/ledger.log" "$S/ldd.log" "$S/mut.log" "$S/n.log" "$S/r.log" "$S/v.log"; SUM=.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-12-SUMMARY.md; PB="$(sed -nE 's/^PHASE-BASE: ([0-9a-f]{40})$/\1/p' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md)"; bash scripts/local-stack/run.sh down >/dev/null 2>&1; bash scripts/local-stack/run.sh up > "$S/up.log" 2>&1 && grep -E 'baseline-replay:' "$S/up.log" | grep -q 'fanin_initial_status_10param' && grep -E 'baseline-replay:' "$S/up.log" | grep -q 'allocator_sync_restore_inflight_prefetch' && grep -E 'baseline-replay:' "$S/up.log" | grep -q 'bridge_outcomes_invariant_comments' && npm run test:live-db:ledger > "$S/ledger.log" 2>&1; G1=$?; bash scripts/local-stack/run.sh down >/dev/null 2>&1; test -n "$PB" && node scripts/lint-migration-data-dependence.mjs > "$S/ldd.log" 2>&1 && grep -qE '\b0 NEW\b' "$S/ldd.log" && npx tsx scripts/dump-sql-functions.ts --check && ADDED="$(git diff --name-only --diff-filter=A "$PB" -- supabase/migrations)" && test "$(printf '%s\n' "$ADDED" | awk NF | wc -l | tr -d ' ')" = 3 && bash scripts/test-only-normalize-analytics-url.sh --self-test > "$S/n.log" 2>&1 && bash scripts/restore-test-from-baseline.sh --self-test > "$S/r.log" 2>&1 && npx vitest run src/app/api/allocator/holdings/sync/route.test.ts src/__tests__/restore-test-from-baseline.test.ts --reporter=dot > "$S/v.log" 2>&1; G2=$?; node scripts/mutation-runner/run.mjs --file supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql > "$S/mut.log" 2>&1; M=$?; echo "status: lane+ledger=$G1 static=$G2 mutation=$M"; test "$G1" = 0 && test "$G2" = 0 && test "$M" = 2 && grep -qF 'No defects in the narrowed scope.' "$S/mut.log" && ! grep -qE 'no-red|wrong-first-failure|occurrence-mismatch|bad-file-ref|waiver' "$S/mut.log" && grep -qE '^## Round 2' "$SUM" && FC="$(sed -nE 's/^ROUND1-FIX-COUNT: ([0-9]+)$/\1/p' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-11-SUMMARY.md)" && test -n "$FC" && test "$(grep -cE '^ROUND2-SIGNAL: ' "$SUM")" = 1 && if [ "$FC" -gt 0 ]; then grep -qxF 'ROUND2-SIGNAL: reviews-delivered' "$SUM" && for R in migration-reviewer rls-policy-auditor silent-failure-hunter; do test -s "$S/review-r2-$R.md" || exit 1; done; else grep -qE '^ROUND2-SIGNAL: no-round-2: .+' "$SUM"; fi && echo REVIEW-GATES-OK</automated>
    <fails_when>no "REVIEW-GATES-OK". The lane is torn down unconditionally before any verdict and each status is captured on its own, so the marker prints only if EVERY gate passed: lane+ledger != 0 means the lane did not replay all three migrations or the ledger gate is red; static != 0 means the phase base is unreadable, a NEW data-dependence refusal, stale snapshots, other than exactly three added migrations, either self-test red, or the route/restore static tests red; mutation != 2, a missing clean verdict or a named defect class means the dedupe twins no longer bite cleanly; or the SUMMARY has no `## Round 2` section; OR plan 11's `ROUND1-FIX-COUNT:` line is unreadable, the SUMMARY has zero or several `ROUND2-SIGNAL:` lines, round 1 had FIX dispositions (count above 0) but the signal is not `reviews-delivered` or any of the three `review-r2-<reviewer>.md` reports (migration-reviewer, rls-policy-auditor, silent-failure-hunter) is missing or empty, or the count is 0 and the signal is not a reasoned `no-round-2`.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY carries a `## Round 2` section: re-reviewed diffs (or the `no-round-2` reason), findings, dispositions, and any blocker for FC-1 with its reason.
    - REVIEW-GATES-OK printed by this executor's own run after the last fix (not taken from a reviewer's report), with the status line pasted into the SUMMARY.
    - Exactly three migrations remain added by the phase.
  </acceptance_criteria>
  <reversibility rating="reversible">Review and targeted fixes on an unpushed branch.</reversibility>
  <done>D-21 satisfied: reviewed, fixed, re-reviewed, gates re-run and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branch -> shared TEST / PROD | merge auto-applies with no human reviewer gate; this review is the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-31 | Elevation of privilege | a fix round re-opening EXECUTE or dropping a guard | high | mitigate | all three reviewers, rls-policy-auditor included, re-review EVERY round-1 fix diff whenever round 1 fixed anything (verify-enforced against plan 11's ROUND1-FIX-COUNT); DO-block ACL arms re-run via the lane boot |
| T-164.9.1-45 | Repudiation | a red gate chain still printing the pass marker | high | mitigate | per-stage status capture; lane down outside the pass path; marker gated on every status |
</threat_model>

<verification>
- REVIEW-GATES-OK printed; `## Round 2` recorded.
</verification>

<success_criteria>
- The three-reviewer gate is complete (two rounds at most) and recorded before any founder merge, and every local gate is green after the last fix.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| round-2 record + final gate verdicts (SUMMARY) | review evidence | plan 14 FC-1 precondition, gsd-verifier |
| any fix commits | code | FC-1 |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-12-SUMMARY.md` when done.
</output>
