---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 10
subsystem: testing
tags: [live-db-lane, execution-ledger, shrink-only, runbook, gate-sweep]
status: complete

requires:
  - phase: 164.9.1-01
    provides: "PHASE-BASE and the pin table (ledger 10 / 10, floors 50 / 449 / 0)"
  - phase: 164.9.1-02
    provides: "M1, the fan-in initial status (closes g10b P12)"
  - phase: 164.9.1-05
    provides: "the in-transaction restore normalisation the new prose describes"
  - phase: 164.9.1-07
    provides: "M2, the in-flight look-up and the disconnected-key refusal (closes the Queued arm)"
  - phase: 164.9.1-09
    provides: "M3 and the D-14 rewrite of the XOR arms (closes the widened-UNIQUE entry)"
provides:
  - "live-DB execution ledger shrunk 10 -> 7, ENTRY_CEILING 10 -> 7, three K3_ARMS tokens removed, in one commit"
  - "runbook section 7 and arm D1's header describe the in-transaction re-normalisation and when a hand run is still the remedy"
  - "the whole-phase gate sweep verdicts below"
affects: [164.9.1-11, 164.9.1-12, 164.9.1-14]

actuals:
  tokens: 3000
  tasks: 2
  commits: 2
plan_head_before: c12842fadc2cdd4dcdd7055789daaad567ffda2e

tech-stack:
  added: []
  patterns:
    - "measure STALE on the unchanged ledger first, then delete exactly the lines the gate named"

key-files:
  created: []
  modified:
    - scripts/live-db-execution-ledger.txt
    - scripts/live-db-execution-ledger.mjs
    - docs/runbooks/test-analytics-url.md
    - supabase/tests/test_analytics_service_settings_and_vault_tick.sql

key-decisions:
  - "The shrink was taken from a measurement, not from the carry-forward notes. The gate ran on a freshly booted lane against the UNCHANGED ledger first, and reported NEW 0 and STALE 3. The three STALE entries are exactly the three K3 lines."
  - "The K3 bullets in the ledger header are kept as dated lineage and marked CLOSED. They are not deleted."

duration: ~45min
completed: 2026-09-25
---

# Phase 164.9.1 Plan 10: Ledger shrink, REARM prose and the whole-phase sweep Summary

**The live-DB execution ledger lost the three K3 entries this phase fixed. `ENTRY_COUNT` and `ENTRY_CEILING` both went from 10 to 7, and the three arm-token keys left `K3_ARMS`, all in one commit (`0e559db17`). On a clean lane, with M1, M2 and M3 applied by the lane's own replay loop, the gate reads `OK — the failing set is EXACTLY the ledger (7/7)`. Two local gates were not clean at the end. `verify-plan-anchors` is red by construction until plans 11–13 write their SUMMARYs. The full static vitest run had 6 failures that come from load, and each of those files passes when run alone.**

## Carry-forward items (orchestrator), each settled by measurement

1. **Lane replay of M1, M2 and M3 (plan 09 deviation 1).** The lane went `down` then `up` from this tree. The boot printed these lines verbatim:
   ```
   baseline-currency: carried=274 replay=3 marker-sha=match defects=0
   baseline-replay: 3 migration(s) newer than the dump: 20260924230827_fanin_initial_status_10param.sql 20260924233749_allocator_sync_restore_inflight_prefetch.sql 20260925071300_bridge_outcomes_invariant_comments.sql
   [local-stack] replayed 20260924230827_fanin_initial_status_10param.sql (ledger row written)
   [local-stack] replayed 20260924233749_allocator_sync_restore_inflight_prefetch.sql (ledger row written)
   [local-stack] replayed 20260925071300_bridge_outcomes_invariant_comments.sql (ledger row written)
   ```
   Each migration's DO block printed its NOTICE during replay. The lane's `supabase_migrations.schema_migrations` then returned all three versions. The DSN was checked to be loopback first and was never printed. M3 is now applied by the replay loop, not only by the psql control run.
2. **The `Queued path` line (plan 07).** The gate ran against the unchanged ledger and named it STALE.
3. **The `widened UNIQUE:` line (plan 09).** The gate named it STALE as well. `grep -c 'widened UNIQUE:' src/__tests__/match-decisions-xor-rls.test.ts` returns 0, so the ledgered title no longer exists in the module.

## Task 1: the ledger shrink (D-08, D-20)

**Pre-shrink measurement** (fresh lane, unchanged ledger, `npm run test:live-db:ledger`, exit 1):
```
[live-db-ledger] executed: 399 test(s) (392 passed, 7 failed), 81 skipped, 480 collected, across 49 file(s)
[live-db-ledger] ledger: 10 entr(y/ies) (see scripts/live-db-execution-ledger.txt)
[live-db-ledger] STALE: 3 ledger entr(y/ies) the run no longer reproduces.
```
The three STALE entries were g10b `P12:`, match-decisions-xor-rls `widened UNIQUE:` and request-allocator-holdings-sync-queued `Queued path`. There was no NEW section. The 7 remaining failures are the ledger's other 7 lines: one sanitize_user K1 arm and the six wizard K3 arms.

**The change.** Read on the tree: `# ENTRY_COUNT = 10`, `ENTRY_CEILING = 10`, 10 non-comment lines. This matches plan 01's pin table. The commit made these changes:
- It deleted the three TAB lines. They were found by content, not by line number.
- It set `# ENTRY_COUNT = 7` and `export const ENTRY_CEILING = 7`, with a dated `// SHRUNK 2026-09-25 (Phase 164.9.1 plan 10): 10 -> 7` note naming M1, M2 and the D-14 rewrite.
- It removed the three token-keyed `K3_ARMS` entries. The evidence-keyed wizard entry stays.
- It updated the `.txt` K3 count line to `3 entries (9 since 2026-09-23, 6 since 2026-09-25)`, added a dated SHRUNK block beside the 2026-09-23 RECONCILED block, and marked the K3 bullets CLOSED, keeping them as lineage.
- It updated the `.mjs` header sentence that counts the K3 keys.

The prose follows "VERDICTS, NEVER SPECIMENS": it has no ids and no values. The contract test was not touched.

**Task 1 verify**, run byte-identical under `bash`:
```
live-db-execution-ledger self-test OK: 20 cases, red and green both observed.
      Tests  6 passed (6)          <- live-db-execution-ledger.contract.test.ts
[live-db-ledger] executed: 399 test(s) (392 passed, 7 failed), 81 skipped, 480 collected, across 49 file(s)
[live-db-ledger] ledger: 7 entr(y/ies) (see scripts/live-db-execution-ledger.txt)
[live-db-ledger] OK — the failing set is EXACTLY the ledger (7/7). Every corpus file executed; no arm outside the ledger failed; no ledger entry has gone stale.
LEDGER-OK
```
The closed arms pass, and so do the P12-harm, Queued-refusal and XOR (c) md-NULL arms. None of them is among the 7 failures.

**Single commit:** `git log --format=%h "$PB"..HEAD -- scripts/live-db-execution-ledger.txt scripts/live-db-execution-ledger.mjs` returns only `0e559db17`, and that commit touches both files.

## Task 2: the REARM prose (D-18)

- `docs/runbooks/test-analytics-url.md` §7 now says:
  - a restore re-normalises the row inside its own transaction;
  - the fragment is appended after the reference-data gate and before the ledger DDL;
  - a preflight runs the same fragment and rolls it back, so it writes nothing;
  - an emitter refusal assembles no transaction;
  - a hand run is still the remedy when a migration re-seeds the row or the restore refuses.
- Arm D1's header in `supabase/tests/test_analytics_service_settings_and_vault_tick.sql` says the same in comment lines only. `git diff "$PB" -- <D1 file>` shows no changed non-comment line. No SQL, `RED-UNDER` annotation or arm logic changed.
- Neither stale sentence remains.

## Whole-phase gate sweep (verdict lines, verbatim)

Deviation: to stay within the 600 s watchdog on long commands, the plan's single verify command was run as six stages. Each stage runs the verify's own clauses with the same predicates, and each has its own 600 s watchdog.

| Command | Verdict |
|---|---|
| phase base | `PB=f8a3096e3f984f96e7ff0a442e44d2c46f78f9bd` |
| stale prose absent | `prose-ok` |
| `node scripts/lint-sql-gates.mjs --self-test` | `lint-sql-gates self-test OK: 7 rules, red+green each.` |
| `node scripts/lint-sql-gates.mjs` | `lint-sql-gates: scanned 77 file(s); 0 finding(s), 0 measure-fail(s), 0 allowlist error(s).` |
| `node scripts/lint-app-guc.mjs --self-test` | `lint-app-guc self-test OK: 4 finding kinds, red+green each (11 red, 2 green fixtures; 2 kind(s) vitest-covered by declaration).` |
| `node scripts/lint-app-guc.mjs` | `app-guc: findings 0` |
| `node scripts/lint-migration-data-dependence.mjs --self-test` | `lint-migration-data-dependence self-test OK (5 refusals, red+green each.)` |
| `node scripts/lint-migration-data-dependence.mjs` | `lint-migration-data-dependence: corpus 277 migration(s), 350 anonymous block(s); 83 refusal(s), 83 allowlisted, 0 NEW.` |
| `npx tsx scripts/dump-sql-functions.ts --check` | `SQL function snapshot is current (121 functions).` and `DRIFT-05 gate (a): baseline 121 name(s) vs migration-replay 121 name(s) — agree, with 0 ratcheted disagreement(s) carried.` |
| migrations added vs `PB` (D-25) | 3: `20260924230827_fanin_initial_status_10param.sql`, `20260924233749_allocator_sync_restore_inflight_prefetch.sql`, `20260925071300_bridge_outcomes_invariant_comments.sql` |
| VAC-08 script, data-dependence baseline, refdata allowlist vs `PB` (D-24) | `git diff --quiet` exit 0 (`vac08/baseline/allowlist unchanged`) |
| floors vs `PB` | `FILES_FLOOR = 50`, `ARMS_FLOOR = 449`, `WAIVED_CEILING = 0`, byte-equal to the base commit (`static=0`) |
| `bash scripts/test-only-normalize-analytics-url.sh --self-test` | exit 0, `test-only-normalize-analytics-url: self-test OK (36 checks)` |
| `bash scripts/restore-test-from-baseline.sh --self-test` | exit 0, `restore-test-from-baseline: self-test OK (34/34 arms — …)` |
| `npm run lint` | exit 0. It printed `[check-admin-route-manifest] OK — 20 admin routes`, `[check-route-contract] OK — 58 page routes` and `[check-planning-hygiene] OK — 7062 tracked files scanned` |
| `npm run typecheck` | exit 0 |
| SQL corpus on a fresh lane (`down`, `up` exit 0, loopback DSN accepted, `psql -X -v ON_ERROR_STOP=1 -q -f` per file, then `down`) | `corpus: up=0 total=77 ran=76 lane-only=1 failed=0 corpus=0`. The one LANE-ONLY file, named and not run: `supabase/tests/test_prod_prober_cadence.sql`. The other 76 each exited 0. |
| `node scripts/mutation-runner/run.mjs --file …/test_analytics_service_settings_and_vault_tick.sql` | exit 2. It printed `arms: 14/14/0`, `biting: 14`, `lane-invocations: 14` and `No defects in the narrowed scope.` |
| `node scripts/mutation-runner/run.mjs --file …/test_enqueue_compute_job_dedupe_non_terminal.sql` | exit 2. It printed `arms: 2/2/0`, `biting: 2`, `lane-invocations: 2` and `No defects in the narrowed scope.` |
| defect-class grep over both mutation logs | no match |
| `npx vitest run` (static suite) | **exit 1**: `Test Files 4 failed \| 874 passed \| 19 skipped (897)`, `Tests 8 failed \| 15924 passed \| 283 skipped (16215)`. See below. |

### The two reds, and why neither is waived or "fixed" here

1. **`src/__tests__/verify-plan-anchors.test.ts` failed 2 of 58, deterministically.** `node scripts/verify-plan-anchors.mjs --pending` reports 5 `context-ref-missing` misses. Each one is a later plan's `@`-reference to a sibling SUMMARY that does not exist yet: 11 → 10, 12 → 11, 14 → 11, 12 and 13. The miss for this plan's own SUMMARY clears now that this file exists. The other 4 clear as plans 11, 12 and 13 write their SUMMARYs. This is the phase's plan set in progress, not a code defect. Editing the verifier or those plans to make the gate green now would be a gate-weakening change, so none was made. The gate should be re-read after plan 13.
2. **Six failures in three files, all load-induced.** Five are `Test timed out in 5000ms` in `ci-anti-skip-gate.contract.test.ts` and `lint-sql-gates.test.ts`. The sixth is a focus-transition timing assertion in `ScenarioCommitDrawer.test.tsx`. The host load average was about 5 during the 255 s full run. Re-run alone, each file passes:
   - `ci-anti-skip-gate.contract.test.ts`: `Tests 33 passed (33)`
   - `lint-sql-gates.test.ts`: `Tests 74 passed (74)`
   - `ScenarioCommitDrawer.test.tsx`: `Tests 49 passed (49)`

   None of these files is touched by this phase.

   **Second full run**, after this SUMMARY existed: `Test Files 3 failed | 875 passed | 19 skipped (897)`, `Tests 4 failed | 15928 passed | 283 skipped (16215)`. The 5-minute load average was 14.
   - Two failures were the same `verify-plan-anchors` arms. `--pending` now reports 4 misses, down from 5, because this SUMMARY resolves one.
   - The other two were `Test timed out in 5000ms`, in `ci-anti-skip-gate.contract.test.ts` and `test-restore-workflow-wiring.test.ts`. That set of timed-out tests differs from the first run's. Re-run alone, they pass: `Tests 33 passed (33)` and `Tests 117 passed (117)`.

Result: the plan's `SWEEP-OK` sentinel was **not** printed, because of item 1. Every other stage of the sweep is green.

## Deviations from Plan

1. **[Constraint over plan text] The sweep ran in six watchdogged stages** instead of one command, because of the 600 s watchdog rule. The predicates are the verify's own.
2. **[Constraint] Scratch logs went to the session scratchpad** with the `164.9.1-10-` prefix. Task 1's verify still wrote to its own `${TMPDIR}/gsd-164.9.1-10` as written.
3. **The ledger `.txt` K3 bullets were kept as dated lineage** and not deleted. The plan asks only for the count block to be updated, and the file's own convention keeps superseded text as lineage.
4. **Not fixed (out of scope):** the `.txt` header still says "The 18 below were seeded" in its add-rule paragraph, which predates this phase. It is left as it was.

## Threat model check

- **T-164.9.1-27 (mitigated):** the ledger only shrank. The ceiling equals the entry count (contract test 6/6). STALE was detected before the edit, and nothing was added.
- **T-164.9.1-28 (mitigated):** the ledger prose and this SUMMARY carry verdicts only. `check-planning-hygiene` was OK after each `git add`.
- **T-164.9.1-29 (mitigated):** the floors are byte-equal to `PB`, `WAIVED_CEILING` is 0, and the VAC-08 script, data-dependence baseline and refdata allowlist are unchanged against `PB`.

No new threat surface. The only database touched was the loopback local stack.

## Lane state at hand-off

The lane is **down**. The sweep's corpus stage tore it down, as the plan's verify does.

## Commits

- `0e559db17` fix(164.9.1-10): shrink the live-DB execution ledger by the three K3 arms this phase closed, ceiling 10 to 7
- `2a8bcbe10` docs(164.9.1-10): the runbook and arm D1 say a restore now re-normalises the analytics destination in-transaction

## Self-Check: PASSED

- FOUND: all four modified files. FOUND: `0e559db17` and `2a8bcbe10` on `feat/164.9.1-jobrpctruth`.
- `LEDGER-OK` was printed by Task 1's verify. `SWEEP-OK` was not printed, for the reasons recorded above.
