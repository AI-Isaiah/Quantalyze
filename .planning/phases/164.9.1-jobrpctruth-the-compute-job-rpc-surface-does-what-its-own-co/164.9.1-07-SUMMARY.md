---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 07
subsystem: database
tags: [allocator-sync, secdef, migration, live-db-lane, calibration]
status: complete

requires:
  - phase: 164.9.1-01
    provides: "PHASE-BASE and the pin table"
  - phase: 164.9.1-03
    provides: "M1 replayed and restored byte-exact on the lane; M2 must sort after it"
provides:
  - "M2: request_allocator_holdings_sync re-based on 076 with 075's refusal and 067's in-flight look-up restored"
  - "regenerated supabase/schema/functions/request_allocator_holdings_sync.sql"
  - "the Queued live-DB arms with no vacuous early return, plus the D-23 refusal arm"
affects: [164.9.1-08, 164.9.1-10, 164.9.1-11, 164.9.1-14]

actuals:
  tokens: 9400
  tasks: 1
  commits: 1
plan_head_before: dbe9f867d323f79e8d3c6135b9318c5c92b12daf

tech-stack:
  added: []
  patterns:
    - "order arms read regexp_instr positions on the comment-stripped body, so a statement moved past another statement fails even when every statement is still present"
    - "the D-23 arm pins a live job before disconnecting the key, so the same arm catches both a lost refusal and a refusal moved after the look-up"

key-files:
  created:
    - supabase/migrations/20260924233749_allocator_sync_restore_inflight_prefetch.sql
  modified:
    - supabase/schema/functions/request_allocator_holdings_sync.sql
    - src/__tests__/request-allocator-holdings-sync-queued.test.ts

key-decisions:
  - "M2 re-bases on 076 (20260422122720). The re-base grep at execution found five CREATEs, and 076 is the newest. 20260515210300 and 20260812083206 name the function in comments only."
  - "The D-23 arm also pins an in-flight job. Without it, a refusal moved after the look-up would still pass. With it, that order answers already_inflight and the arm goes RED."
  - "createAuthedClient throws on a failed sign-in (pattern (a)), not signInAsTestUser. The arms keep their own email and password scheme, and the lane accepts password sign-in, which was measured."

duration: ~15min
completed: 2026-09-25
---

# Phase 164.9.1 Plan 07: M2 — the holdings sync RPC tells "already queued" from "enqueued", and refuses a disconnected key Summary

**Migration `20260924233749_allocator_sync_restore_inflight_prefetch` re-bases `public.request_allocator_holdings_sync(uuid)` on 076. It puts back two behaviours that earlier re-bases lost. The first is 067's in-flight look-up: the RPC now returns `{already_inflight: true, next_attempt_at}` when a poll job for the key is live. The second is 075's refusal of a soft-disconnected key: `api_key_disconnected`, SQLSTATE `P0001`, checked after ownership and before the look-up. On the local lane, the Queued arms were RED before M2, are GREEN with it, and each new arm was observed RED under its own neuter.**

CALIBRATION-RED: m2-prefetch-return neuter=FAIL restored-cmp=0 restored=OK
CALIBRATION-RED: m2-disconnected-raise neuter=FAIL restored-cmp=0 restored=OK

## What changed

- **M2 body (D-09, D-12, D-23).** The body is 076's, verbatim, with three edits:
  1. DECLARE gains `v_disconnected`. The ownership SELECT reads `disconnected_at` into it, and 075's refusal follows the ownership RAISE.
  2. 076's `BEGIN … EXCEPTION WHEN unique_violation` block around the poll enqueue is replaced by 067's look-up, its early RETURN and a plain `v_job_id := enqueue_compute_job(...)`.
  3. Everything after that is unchanged: the reconstruct gate, its benign handler, the UPDATE and the `{ok, job_id}` RETURN.

  The CREATE, the REVOKE/GRANT pair and the COMMENT are schema-qualified with the full signature. The COMMENT names the three outcomes (queued, enqueued, refused).
- **Header.** It records:
  - the lineage 067 → 070 (look-up lost) → 075 (refusal added) → 076 (refusal lost) → M2;
  - the re-base grep and its result;
  - the D-11 race in 067's words;
  - the prod-body-ack block.

  It also notes that `20260812083206`'s header claims this RPC skips disconnected keys. That claim was false from 076 until M2.
- **DO block (D-07).** It reads catalogs only, with M1's shape: `to_regprocedure`, a comment-stripped `pg_get_functiondef`, a NULL-body guard, and single-literal RAISE formats. The arms are:
  - (a) SECDEF and the exact `search_path=public, pg_catalog` value in `proconfig`;
  - (b) the ownership SELECT reads `disconnected_at`, and the refusal is present and after the ownership RAISE;
  - (c) the look-up is present, the refusal comes before it, and the order is look-up, then early RETURN, then poll enqueue;
  - (d) exactly one `EXCEPTION[[:space:]]+WHEN[[:space:]]+unique_violation`, located after the reconstruct enqueue;
  - (e) 076's reconstruct-gate tokens are present, and there is no `v_snapshot_count`;
  - (f) the ACL: role existence, `_assert_no_public_execute`, no EXECUTE for anon, EXECUTE for authenticated.
- **Snapshot.** Regenerated with `npm run schema:functions` after the final header edit. Its source-migration line names M2. `dump-sql-functions.ts --check` reports it current (121 functions).
- **Test file.**
  - The header records the lineage and the fix.
  - `createAuthedClient` now throws on a failed sign-in, so both `if (!authed) return` lines are gone (Pitfall 8).
  - New arm: `refuses a soft-disconnected key with P0001 api_key_disconnected, before the in-flight look-up`. It asserts only about its own seeded key and cleans up in `finally`.

## Evidence

**Pre-M2 lane** (M1 only, plan 03's restored lane), `Tests  1 failed | 2 passed (3)`:
```
× … returns {already_inflight, next_attempt_at} when a live job exists
  → expected undefined to be true // Object.is equality
```
The lane accepts password sign-in, so this was a real call returning `{ok, job_id}` for a live job. No early return masked it.

**Lane with M2** (`bash scripts/local-stack/run.sh down`, then `up`, exit 0):
```
baseline-currency: carried=274 replay=2 marker-sha=match defects=0
baseline-replay: 2 migration(s) newer than the dump: 20260924230827_fanin_initial_status_10param.sql 20260924233749_allocator_sync_restore_inflight_prefetch.sql
NOTICE:  allocator-sync-prefetch: request_allocator_holdings_sync refuses a disconnected key after the ownership check, returns already_inflight from an in-flight look-up placed before the poll enqueue, keeps exactly one (reconstruct) unique_violation handler and the per-api_key reconstruct gate; SECDEF, the exact search_path pin and the ACL (PUBLIC, anon, authenticated) intact.
[local-stack] replayed 20260924233749_allocator_sync_restore_inflight_prefetch.sql (ledger row written)
```
`npx vitest run --config vitest.livedb.config.ts src/__tests__/request-allocator-holdings-sync-queued.test.ts --reporter=verbose` exited 0:
```
✓ … returns {already_inflight, next_attempt_at} when a live job exists
✓ … returns {ok, job_id} when no live job exists (happy path)
✓ … refuses a soft-disconnected key with P0001 api_key_disconnected, before the in-flight look-up
✓ … advertises skip reason when live DB is unavailable
     Tests  4 passed (4)
```

### Live-arm calibration (criterion 6)

Method: M2 was byte-backed-up with `cp` to scratch, and `cmp` confirmed the backup. Each neuter edited the tracked file in the working tree only. The lane was then rebooted (`down`, `up`), and the whole file was run.

| Calibration | Neuter | Neutered-lane result | Restore |
|---|---|---|---|
| `m2-prefetch-return` | `IF v_next_attempt IS NOT NULL` → `IF false AND …`, and DO-block arms `v_pos_ret = 0` and the order arm disabled with `false AND` so the replay does not FATAL | `× returns {already_inflight, next_attempt_at} …` `→ expected undefined to be true`; `Tests 1 failed \| 3 passed (4)` | `cp` backup → `cmp` exit **0**, 0 NEUTER markers, reboot, `Tests 4 passed (4)` |
| `m2-disconnected-raise` | `IF v_disconnected IS NOT NULL` → `IF false AND …`, and DO-block arms `v_pos_disc = 0` and the after-ownership arm disabled | `× refuses a soft-disconnected key …` `→ expected null not to be null`; `Tests 1 failed \| 3 passed (4)` | `cp` backup → `cmp` exit **0**, 0 NEUTER markers, reboot, `Tests 4 passed (4)` |

In the second neuter, the RPC call succeeded where it should have been refused. With the pinned job, it answered "queued" for a disconnected key, which is the user-facing harm D-23 names. The refusal arm stayed GREEN under the first neuter, and the Queued arm stayed GREEN under the second. So each arm is tied to its own statement.

### DO-block arm calibration

Each neuter was a scratch copy of M2, applied with `psql -X -1 -v ON_ERROR_STOP=1` on the loopback lane (the DSN host was checked to be loopback first, and was never printed). The failed transaction rolled back. The tracked M2 was `cmp`-verified unchanged afterwards (`M2-UNCHANGED`), and the unmodified control was applied last, so the lane holds M2's body.

| Neuter | Observed |
|---|---|
| control (unmodified) | rc=0, NOTICE printed |
| refusal removed | rc=3 `the body has no api_key_disconnected refusal …` |
| refusal kept only as a `--` comment | rc=3, same arm (the comment strip is load-bearing) |
| refusal moved after the look-up | rc=3 `the api_key_disconnected refusal runs after the in-flight look-up …` |
| `done_pending_children` dropped from the look-up | rc=3 `the body has no in-flight look-up …` |
| early `RETURN` → `PERFORM` | rc=3 `the in-flight look-up is not followed by an early RETURN …` |
| dead handler put back around the poll enqueue | rc=3 `the body carries 2 unique_violation handler(s), expected exactly 1 …` |
| `GRANT … TO authenticated` → `REVOKE … FROM authenticated` | rc=3 `authenticated lost EXECUTE …` |
| `TO authenticated, anon` | rc=3 `anon holds EXECUTE …` |

### Gates

- `node scripts/lint-migration-data-dependence.mjs --self-test`: `self-test OK (5 refusals, red+green each.)`.
- `node scripts/lint-migration-data-dependence.mjs`: `corpus 276 migration(s), 349 anonymous block(s); 83 refusal(s), 83 allowlisted, 0 NEW.` No baseline line was added.
- `npx tsx scripts/dump-sql-functions.ts --check`: `SQL function snapshot is current (121 functions).`
- `npx tsc --noEmit` reports no error. `eslint` on the test file exits 0.
- Acceptance counts over M2's non-comment lines:
  - `EXCEPTION WHEN unique_violation`: 1;
  - `poll_allocator_positions`: 7;
  - `already_inflight`: present;
  - the exact `REVOKE ALL … FROM PUBLIC, anon;` line: 1.
- The name matches `^[0-9]+_[a-z0-9_]+\.sql$`. It sorts after M1 and is the newest migration.
- Static suites that scan `supabase/migrations/**`, plus the two unit tests naming this RPC (`route.test.ts`, `AllocatorExchangeManager.test.tsx`): 26 files, `4 failed | 1356 passed | 34 skipped`. All 4 failures are in `src/__tests__/restore-test-from-baseline.test.ts`, which pins `scripts/restore-test-from-baseline.sh`. Plan 05 had uncommitted edits in progress in that file and its fixtures at the time (`git status`). Nothing in this plan touches either, so the failures are not attributable here. Plan 05 owns them.

## D-25: VAC-04 prod-body-ack

```
git show origin/main:supabase/schema/functions/request_allocator_holdings_sync.sql > <scratch>/main-rahs.sql
node scripts/sql-body-normalize.mjs --diff-bodies supabase/schema/functions/request_allocator_holdings_sync.sql <scratch>/main-rahs.sql
```
Before the snapshot regeneration, the result was `MATCH … 1a5a1959…ff137 1a5a1959…ff137`. After it:
```
DRIFT	request_allocator_holdings_sync	1	47a7d156a401227d2f2762d261280489b6abc40a880c4bcd21cc5128ab7b2b35	1a5a19591878d67b863614ca68839258255b235825b725f5223abcee368ff137	14
```
The ack is `-- prod-body-ack: 1a5a19591878d67b863614ca68839258255b235825b725f5223abcee368ff137`. That is the live column, and it equals the research reading A1, so no difference was found. It is earned only if VAC-04 on the PR reports the same PROD hash. The new snapshot hash is `47a7d156…7b2b35`. M2 is the second of at most three migrations.

## Deviations from Plan

1. **[Rule 2 - correctness] The D-23 arm pins a live job before disconnecting the key.** The plan's arm would pass against a refusal placed after the look-up whenever no job is live. The pinned job makes that order fail. The calibration confirmed it: with the RAISE neutered, the call answered "queued". The "no new poll row" assertion is therefore "the only poll row for this key is the pinned one".
2. **[Rule 2 - correctness] Extra DO-block arms beyond the plan's list.**
   - The early RETURN must follow the look-up and precede the enqueue. Without that, a look-up whose result is discarded would pass.
   - The one kept handler must sit after the reconstruct enqueue.
   - `v_snapshot_count` must be absent (076's own arm).

   All were calibrated.
3. **Scratch location.** Backups, logs and scratch copies went to the session scratchpad with the `164.9.1-07-` prefix, as the orchestrator instructed, not to `${TMPDIR}/gsd-164.9.1-07`. The plan's verify still writes its own logs where it says.
4. **The neuter edited the tracked file in the working tree, as the plan specifies, and never reached a commit.** `git log -S 'NEUTER-164.9.1-07'` finds nothing, and `cmp` was 0 after each restore.

## Known consequences for later plans

- **Plan 08** maps `P0001` `api_key_disconnected` to HTTP 409 in `src/app/api/allocator/holdings/sync/route.ts`. Until then, the route surfaces the refusal through its existing non-42501 error branch.
- **Plan 10**: the K3 ledger line for this file (token `Queued path`) now names a file whose arms all pass. The ledger gate would call it STALE until plan 10 removes it. Do not push before plan 10.
- **VAC-08 / FC-1**: VAC-08 pairs only `_enqueue_compute_job_internal` (D-24), so M2 adds no expected red there. VAC-04 will report `request_allocator_holdings_sync` DRIFT, acked above.
- D-23 is an autonomous decision that the founder may overrule at FC-1.

## Threat model check

- **T-164.9.1-10:** schema-qualified CREATE. The ACL pair is re-issued, and arm (f) (`_assert_no_public_execute`, anon, authenticated) passed and was calibrated.
- **T-164.9.1-11:** the auth gate and ownership check are verbatim from 076 and run before the refusal and the look-up. Arm (b) pins that order. The arms run as the real owner.
- **T-164.9.1-12:** the refusal is restored before any enqueue. The D-23 arm proves that no poll row is added.
- **T-164.9.1-14:** the D-11 race is accepted and recorded in M2's header.

There is no new threat surface. The only database touched was the loopback local stack. No value, DSN or host was printed.

## Commits

- `dd1fb6d25` fix(164.9.1-07): the holdings sync RPC reports an in-flight job and refuses a disconnected key again

## Plan verify

The `<verify>` command was run byte-identical after this SUMMARY carried the two `CALIBRATION-RED:` lines. The lane had M1 and M2 replayed. It printed:
- `SQL function snapshot is current (121 functions).`
- `lint-migration-data-dependence: corpus 276 migration(s), 349 anonymous block(s); 83 refusal(s), 83 allowlisted, 0 NEW.`
- `Tests  4 passed (4)`
- `M2-OK`

It ran under `bash`. The interactive shell here is zsh, whose `test` has no `\>` string operator, so the first attempt failed at that clause (`condition expected: >`) before running anything. The command text is unchanged.

## Self-Check: PASSED

- FOUND: `supabase/migrations/20260924233749_allocator_sync_restore_inflight_prefetch.sql`
- FOUND: `supabase/schema/functions/request_allocator_holdings_sync.sql`
- FOUND: `src/__tests__/request-allocator-holdings-sync-queued.test.ts`
- FOUND: `dd1fb6d25` on `feat/164.9.1-jobrpctruth`
- `git log --all -S 'NEUTER-164.9.1-07'` returns 0 commits.
- `M2-OK` was printed by the plan's own verify.
