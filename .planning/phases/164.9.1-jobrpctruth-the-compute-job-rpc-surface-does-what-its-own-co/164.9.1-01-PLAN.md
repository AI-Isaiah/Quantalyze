---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts
autonomous: true
requirements:
  - "[164.9-FANIN-STATUS-NEVER-SET]"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-19: this branch has 164.4.2 in its ancestry before any other task of the phase runs; if it cannot be established, execution STOPS and nothing proceeds on main's non-replaying lane."
    - "D-19 / D-20: plan 01's SUMMARY carries one line 'PHASE-BASE: <40-hex sha>' naming the base commit the ancestry step brought in; every later 'file unchanged' check compares that commit with the working tree (git diff --quiet <PHASE-BASE> -- <file>), never the working tree alone."
    - "D-20: every 164.4.2-touched pin (live-DB ledger ENTRY_COUNT and ENTRY_CEILING, lane postgres image, sql-tests vs test-db-drift job split, 61616158 counts, FILES_FLOOR / ARMS_FLOOR / WAIVED_CEILING by symbol, restore summary-line pin count, EXPECTED_ARMS) is READ on the rebased tree and written into this plan's SUMMARY; no later plan copies a number from the planning documents."
    - "D-01: before ANY fix lands, a live-DB arm enqueues a parented child through the PUBLIC wrapper on the LOCAL lane only and records a verdict table of statuses and booleans (no ids): (i) the child's landed status, (ii) whether the claim RPC hands it out while the parent is not done, and (iii) what the parent's mark-done does to it. (iii) is recorded by EXECUTION on the pre-fix lane whatever (ii) showed: mark-done on the parent always runs after (ii); only the SECOND claim is conditional."
    - "D-02: the SUMMARY states 'mechanism demonstrated / production harm latent' only when the probe recorded claimed_while_parent_running=true (D-02's mechanism: a parented child claimable before its parent finishes); with false it states the mechanism was NOT demonstrated on the lane and surfaces that; it never states 'harm observed in production'."
    - "D-03: whatever the probe shows, the fan-in defect proceeds to FIX in plan 02; the probe's result never drops the item."
    - "D-26: the probe calls claim_compute_jobs_with_priority with p_kind_include set, because a two-arg overload makes a two-named-arg call ambiguous; the probe is an arm in the g10b live-DB file, not a new supabase/tests file."
    - "D-22: the only database touched is the loopback local stack booted by scripts/local-stack/run.sh; no supabase CLI command, no remote psql, no credential."
  artifacts:
    - path: src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts
      provides: "the harm-probe arm (title prefix 'P12-harm') that prints one HARM-PROBE verdict line and asserts the POST-fix fan-in contract"
  key_links:
    - from: "harm-probe arm"
      to: "public.enqueue_compute_job -> public._enqueue_compute_job_internal (TEN-arg) -> claim_compute_jobs_with_priority -> mark_compute_job_done"
      via: "admin.rpc calls on the lane's service_role client"
      pattern: "P12-harm"
  prohibitions:
    - "No git checkout, git restore or git stash, ever (they silently destroy uncommitted work)."
    - "No write to shared TEST or PROD; no supabase CLI invocation outside run.sh's derived workdir."
    - "No CI skip trailer token in any commit message."
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> Every number in this plan that came from the planning documents (for example "10 / 10" for the
> live-DB ledger on the 164.4.2 tree) is a reading taken on 2026-09-24. It is NOT a constant.

<objective>
Establish the tree this whole phase runs on, then prove the harm before anything is fixed.

1. Bring 164.4.2 into this branch's ancestry (D-19) and re-measure every pin 164.4.2 touched (D-20).
   Without the 164.4.2 D-F tail replay, the local lane loads only the baseline dump and would never
   see this phase's migrations, so no later plan can prove anything by execution.
2. ROADMAP success criterion 1 ("the harm is proven or the phase shrinks"): add ONE live-DB arm to
   the g10b file that drives the real mechanism end to end, and run it on the PRE-FIX lane so the
   verdict table is recorded before M1 exists (D-01, D-02, D-03, D-26).

Purpose: every fix plan depends on this one. Harm first, then fixes.
Output: a rebased branch, a re-measured pin table in the SUMMARY, and a committed harm-probe arm
whose pre-fix RED output carries the verdict line.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-RESEARCH.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-PATTERNS.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Put 164.4.2 in this branch's ancestry and re-measure every pin it touched (D-19, D-20)</name>
  <files>(git history only; no tracked file is edited by this task)</files>
  <read_first>
    - `CLAUDE.md` (repo) sections "Which database am I on?", the 2026-09-23 D-F note, and "SQL gate integrity jobs".
    - `164.9.1-CONTEXT.md` section F (D-19, D-20) and the FC-2 entry under Deferred.
    - `164.9.1-RESEARCH.md` "Q6 — Live-DB lane" and "State of the Art".
    - `scripts/local-stack/run.sh` header (the `SCHEMA SOURCE:` line) as it reads AFTER the ancestry step.
  </read_first>
  <action>
    Per D-19, choose the base: if `origin/main` already contains 164.4.2, the base is `origin/main`; otherwise
    the base is `origin/feat/164.4.2-subsetsplit`. ⛔ Do NOT decide "contains" with
    `git merge-base --is-ancestor origin/feat/164.4.2-subsetsplit origin/main`: this repo SQUASH-merges PRs, so a
    merged branch tip is never an ancestor of main (measured 2026-09-24 at planning revision: 164.4.2 is on
    `origin/main` as squash commit #842 and that test prints false). Decide by CONTENT after `git fetch origin`:
    `origin/main` contains 164.4.2 when `git cat-file -e origin/main:supabase/schema/baseline-carried-migrations.txt`
    exits 0 AND `git show origin/main:scripts/local-stack/run.sh` (captured into a variable first, so git's own exit
    status is checked before any pipe) contains `SCHEMA SOURCE: baseline + the TAIL`. Record which, and write
    the base's commit (`git rev-parse <base>`, full 40-hex) into the SUMMARY on a line of its own, exactly
    `PHASE-BASE: <sha>` with nothing else on the line. Later plans parse that line to prove a file is unchanged
    against the tree this phase started from (a working-tree-only `git diff --quiet` misses a committed change).

    Establish ancestry. When running directly on the phase branch, use `git rebase <base>`. When
    running inside a per-plan worktree whose branch merges back into the phase branch, use
    `git merge --no-edit <base>` instead (a squash merge of the eventual PR makes the two equivalent).
    164.4.2 adds no migration, so no conflict is expected. If ANY conflict appears, run
    `git rebase --abort` or `git merge --abort`, STOP, and return a blocker naming the conflicting
    paths. Do not resolve conflicts by guessing. Do not proceed on main's non-replaying lane (D-19).
    Never use `git checkout`, `git restore` or `git stash`.

    Then re-measure per D-20 and write each reading into the SUMMARY as a table of symbol, reading and
    the command that produced it:
    the live-DB ledger `# ENTRY_COUNT =` header value and the non-comment line count of
    `scripts/live-db-execution-ledger.txt`; `export const ENTRY_CEILING` in
    `scripts/live-db-execution-ledger.mjs`; the three K3 lines (g10b `P12:`, match-decisions-xor-rls
    `widened UNIQUE:`, request-allocator-holdings-sync-queued `Queued path`) present or absent;
    the `K3_ARMS` tokens; `FILES_FLOOR`, `ARMS_FLOOR`, `WAIVED_CEILING` read BY SYMBOL from
    `scripts/mutation-runner/run.mjs`; the postgres image tag pinned by `scripts/local-stack/run.sh`;
    whether `.github/workflows/ci.yml` has both a `sql-tests:` and a `test-db-drift:` job key;
    `grep -c 61616158` for `ci.yml` and `supabase-migrate.yml`; `grep -c 'restore: tables=2'` and
    the `EXPECTED_ARMS=` value in `scripts/restore-test-from-baseline.sh`; the newest file in
    `supabase/migrations/`; `VERSION`; `FRONTIER_EXEMPT_CEILING` in `scripts/test-ledger-drift-check.sh`.
    If a reading differs from the planning documents, the READING wins; name the difference in the SUMMARY.
  </action>
  <verify>
    <automated>PB="$(sed -nE 's/^PHASE-BASE: ([0-9a-f]{40})$/\1/p' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md)" && test -n "$PB" && test "$(grep -cE '^PHASE-BASE: ' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md)" = 1 && git merge-base --is-ancestor "$PB" HEAD && BRS="$(git show "$PB":scripts/local-stack/run.sh)" && printf '%s\n' "$BRS" | grep -qF 'SCHEMA SOURCE: baseline + the TAIL' && grep -qF 'SCHEMA SOURCE: baseline + the TAIL' scripts/local-stack/run.sh && test -f supabase/schema/baseline-carried-migrations.txt && grep -qE '^  test-db-drift:' .github/workflows/ci.yml && grep -nE '^export const (FILES_FLOOR|ARMS_FLOOR|WAIVED_CEILING)' scripts/mutation-runner/run.mjs && grep -nE '^export const ENTRY_CEILING' scripts/live-db-execution-ledger.mjs && echo ANCESTRY-OK</automated>
    <fails_when>non-zero exit or no "ANCESTRY-OK" line: the SUMMARY lacks exactly one `PHASE-BASE:` line, that base is not an ancestor of HEAD or does not itself carry 164.4.2's replaying lane header, the lane header still says baseline-only, the carried-migrations marker is absent, the test-db-drift job does not exist, or a floor/ceiling symbol cannot be read.</fails_when>
  </verify>
  <acceptance_criteria>
    - `git merge-base --is-ancestor "$PB" HEAD` exits 0, where `PB` is the `PHASE-BASE:` sha, and the base's own `scripts/local-stack/run.sh` carries the 164.4.2 header (164.4.2 is in the ancestry by CONTENT, which survives a squash merge; branch-tip ancestry does not).
    - `scripts/local-stack/run.sh` contains the literal `SCHEMA SOURCE: baseline + the TAIL`.
    - The SUMMARY carries a pin table with at least these rows, each with its command: ENTRY_COUNT, ENTRY_CEILING, ledger line count, FILES_FLOOR, ARMS_FLOOR, WAIVED_CEILING, lane image tag, `61616158` count per workflow, `restore: tables=2` count, EXPECTED_ARMS, newest migration, VERSION, FRONTIER_EXEMPT_CEILING.
    - The SUMMARY contains exactly one line matching `^PHASE-BASE: [0-9a-f]{40}$`, and that sha is an ancestor of HEAD.
    - `git status --short` shows no conflict markers and no staged change to `.planning/milestone.lock`, `.planning/state.json` or any `.planning/phases/164.5.3-*` path.
  </acceptance_criteria>
  <reversibility rating="reversible">A rebase or merge is recoverable from the reflog; no tracked file content is authored.</reversibility>
  <done>The branch carries 164.4.2, and the SUMMARY holds the measured pin table every later plan reads instead of the planning documents.</done>
</task>

<task type="tracer">
  <name>Task 2: The harm probe, end to end on the PRE-FIX lane — wrapper, claim, mark-done (D-01, D-02, D-03, D-26)</name>
  <files>src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts</files>
  <read_first>
    - `src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts` in full, in particular the helpers `mintClaimToken`, `seedStrategy`, `insertComputeJob`, `fetchJob`, arm P7's own-rows filter, arm P12 (the wrapper call with `p_parent_job_ids`), arm P15 (running parent with a token, then `mark_compute_job_done`).
    - `src/lib/test-helpers/live-db.ts` (`HAS_LIVE_DB`, `createLiveAdminClient`, `createTestUser`, `cleanupLiveDbRow`).
    - `supabase/migrations/20260603120000_compute_jobs_rpc_clear_error_and_gin_fanin.sql` (claim candidacy, fan-in advance in `mark_compute_job_done`) and `supabase/migrations/20260719073701_claim_kind_filter.sql` (`claim_compute_jobs_with_priority` five-arg signature).
    - `164.9.1-RESEARCH.md` "Q1 — Harm mechanism" (probe design and expected pre/post verdicts).
    - `vitest.livedb.config.ts` (how the lane handoff reaches the run).
  </read_first>
  <action>
    Add ONE arm to the existing `describe("audit-2026-05-07 G10.B / mig 109 — fan-in chain", …)` block,
    wrapped in `it.skipIf(!HAS_LIVE_DB)`, titled with the prefix `P12-harm:` (for example
    `P12-harm: a parented child is not claimable before its parent is done and is released by the parent's mark-done`).
    The title must NOT contain the substring `P12:` (that is the ledger's K3 token for arm P12).

    Seed per D-01 with the file's own helpers: a test user and strategy; a parent via
    `insertComputeJob` with kind `sync_trades`, status `running`, a minted claim token (P15's shape);
    then the child through the PUBLIC wrapper exactly as P12 calls it
    (`admin.rpc('enqueue_compute_job', { p_strategy_id, p_kind: 'reconcile_strategy', p_idempotency_key: null, p_parent_job_ids: [parentId] })`).
    A different kind from the parent keeps the optimistic look-up from collapsing child onto parent.
    Why a RUNNING parent rather than D-01's "still-pending" one (research Q1): D-01's operative condition is
    "while the parent is not done", and a running parent is not done; but `mark_compute_job_done` is fenced on
    a `running` row and its claim token, so a pending parent could only reach (iii) by being claimed through the
    very claim RPC under test (and could itself be handed out by the probe's claim). Seeding it running with a
    minted token keeps (ii) about the child alone and makes (iii) executable.

    Observe, collecting into one verdict object BEFORE any assertion:
    (i) `child_status_at_enqueue` from `fetchJob`;
    (ii) call `claim_compute_jobs_with_priority` with all five named args
    (`p_batch_size`, `p_worker_id` set to a probe-specific worker name, `p_unified_backbone_active: null`,
    `p_kind_include: ['reconcile_strategy']`, `p_kind_exclude: null`) per D-26, and record
    `claimed_while_parent_running` as whether the child's id is in the returned set, filtered to the
    probe's OWN ids (P7's pattern). `p_batch_size` is the explicit literal `1000`, the RPC's own cap
    (`claim_kind_filter.sql` raises above 1000), so the probe's child cannot be crowded out of the batch by
    other eligible rows: immediately before the claim the arm counts `compute_jobs` rows with
    `status = 'pending'` and `kind = 'reconcile_strategy'` through the service-role client and asserts the
    count is below 1000 (a count at or above the cap fails the arm loudly rather than letting a crowded-out
    child read as `false`). Nothing else can move rows between that count and the claim, because
    `vitest.livedb.config.ts` sets `fileParallelism: false` (live-DB files run serially, and arms within
    a file run sequentially) on a lane private to this host. Any FOREIGN rows the claim hands out (ids
    in the returned set that are not the probe's) are counted, never printed, and recorded in the verdict
    as `foreign_rows_claimed=<n>`; they are left `running` on the private lane (not reset, since the probe
    must not write rows it did not seed), and a non-zero count is written into the SUMMARY as a cleanup item
    naming the lane re-boot (`run.sh down` then `up`) that clears it;
    (iii) ALWAYS, whatever (ii) showed: call `mark_compute_job_done` for the parent with its token and record
    `parent_mark_done_ok` (the RPC returned without error) and `child_status_after_parent_done` from `fetchJob`
    (pre-fix the expected reading is the child's post-claim status, e.g. `running`, which records that the
    fan-in advance was a no-op for it; post-fix it is `pending`). Then, ONLY if the child was NOT claimed in (ii),
    re-run the same claim and record `claimed_after_parent_done`; if it WAS claimed in (ii), record
    `claimed_after_parent_done=not-reached` (a second claim of an already-running row measures nothing).
    Print exactly one line with `console.log`, beginning `HARM-PROBE verdict:` and carrying only
    those keys with status strings, booleans and the one `foreign_rows_claimed` count. Never print an id, a DSN or a user email.

    Then assert the POST-fix contract (these assertions are the regression gate and are expected to
    FAIL on this pre-fix lane): child lands `done_pending_children`; not claimed while the parent runs;
    `pending` after the parent's mark-done; claimed on the re-run. Clean up in `finally` with
    `cleanupLiveDbRow` for the arm's own user and strategy, as P12 does.

    Boot the lane on the rebased tree (`bash scripts/local-stack/run.sh down` then
    `bash scripts/local-stack/run.sh up`; one stack at a time, disk is tight), run the arm, and copy
    the `HARM-PROBE verdict:` line into the SUMMARY, then write ONE line beginning `VERDICT-WORDING: ` whose
    wording is chosen by the recorded keys, never by expectation. D-02 defines the MECHANISM as a parented child
    claimable before its parent finishes, so the D-02 phrase is earned only by (ii):
    - child landed `pending` AND `claimed_while_parent_running=true`: `VERDICT-WORDING: mechanism demonstrated /
      production harm latent` (D-02's sentence, verbatim).
    - child landed `pending` AND `claimed_while_parent_running=false`: `VERDICT-WORDING: mechanism NOT demonstrated
      on the lane: the child landed pending but was not claimed while its parent ran / production harm latent`,
      followed in the SUMMARY by a surfaced note stating that the status defect (pending instead of
      `done_pending_children`) is still recorded by (i) and is what plan 02 fixes, while the early-claim harm was
      not reproduced; the orchestrator surfaces this to the founder at the phase gate. Per D-03 plan 02 proceeds.
    - child did NOT land `pending`: `VERDICT-WORDING: mechanism NOT demonstrated on the lane: the child did not land
      pending / production harm latent`, plus a stop-and-surface note: the premise of plans 02, 03 and 10 would
      need a REPLAN, not a patch.
    Write exactly one `VERDICT-WORDING:` line and do not quote the other alternatives anywhere in the SUMMARY.
    Per D-03 the fix proceeds in every case except the last, which stops for a REPLAN; the SUMMARY records
    D-02's evidence that no production caller passes a non-empty `p_parent_job_ids` by re-running
    `grep -rn parent_job_ids analytics-service --include='*.py'` excluding `.venv`, and reporting the count.

    Commit the arm. This local commit is lane-RED by design until plan 02 lands M1; the branch is
    NOT pushed before plan 10's ledger commit (research Pitfall 9). Commit hygiene: never stage
    `.planning/milestone.lock`, `.planning/state.json` or `.planning/phases/164.5.3-*`; save
    `NM="$(readlink node_modules)"`, remove the `node_modules` symlink, commit, then re-create it with
    `ln -s "$NM" node_modules`.
  </action>
  <precondition>Docker daemon up and at least ~8 GB of disk free; no other worktree has a local stack booted (research "Environment Availability": one stack at a time).</precondition>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.9.1-01"; mkdir -p "$S"; rm -f "$S/harm.log"; npx vitest run --config vitest.livedb.config.ts src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts -t "P12-harm" > "$S/harm.log" 2>&1; RC=$?; cat "$S/harm.log"; test "$RC" -ne 0 && test -s "$S/harm.log" && grep -qE 'HARM-PROBE verdict:.*child_status_at_enqueue=pending' "$S/harm.log" && grep -qE 'HARM-PROBE verdict:.*parent_mark_done_ok=true' "$S/harm.log" && grep -qE 'HARM-PROBE verdict:.*child_status_after_parent_done=[a-z_]+' "$S/harm.log" && grep -qE 'HARM-PROBE verdict:.*claimed_after_parent_done=(true|false|not-reached)' "$S/harm.log" && grep -qE 'HARM-PROBE verdict:.*foreign_rows_claimed=[0-9]+' "$S/harm.log" && CW="$(sed -nE 's/^.*HARM-PROBE verdict:.*claimed_while_parent_running=(true|false).*$/\1/p' "$S/harm.log" | head -1)" && test -n "$CW" && SM=.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md && test "$(grep -cE '^VERDICT-WORDING: ' "$SM")" = 1 && grep -qE "HARM-PROBE verdict:.*claimed_while_parent_running=$CW" "$SM" && if [ "$CW" = true ]; then grep -qxF 'VERDICT-WORDING: mechanism demonstrated / production harm latent' "$SM"; else grep -qxF 'VERDICT-WORDING: mechanism NOT demonstrated on the lane: the child landed pending but was not claimed while its parent ran / production harm latent' "$SM"; fi && echo HARM-RECORDED</automated>
    <fails_when>no "HARM-RECORDED" line: the arm passed on the pre-fix lane (so the post-fix contract is not what fails, or M1 is already present), the arm was skipped (HAS_LIVE_DB false: the lane is not up), the log is empty, or the verdict line is missing, shows a child status other than pending (premise disproved: stop and surface for a REPLAN), or lacks a recorded (iii): the parent's mark-done did not run successfully or the child's status after it was not recorded; OR the verdict carries no `claimed_while_parent_running` boolean, the SUMMARY's copied verdict line disagrees with the log on it, the SUMMARY has zero or several `VERDICT-WORDING:` lines, or its wording does not match the case the log recorded (D-02's "mechanism demonstrated" phrase with `claimed_while_parent_running=false`, or the not-demonstrated wording with `=true`).</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'P12-harm' src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts` returns at least 1, and `grep -c "P12-harm:" …` equals the count of the new arm title only (the title never contains `P12:`).
    - The arm's claim call names `p_kind_include` (`grep -n "p_kind_include" src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts` returns a line inside the new arm).
    - The arm prints a single `HARM-PROBE verdict:` line and no `console.log` in it prints an id variable.
    - In the arm's source, the `mark_compute_job_done` call for the parent is NOT inside the branch conditioned on the child's (ii) claim result; only the second claim call is.
    - The SUMMARY contains the verdict line from the pre-fix run and exactly one `VERDICT-WORDING:` line whose text matches the recorded case: D-02's `mechanism demonstrated / production harm latent` ONLY when the verdict shows `claimed_while_parent_running=true`; the not-demonstrated wording (with its surfaced note) when it shows `false`; plus the Python caller count for `parent_job_ids`.
    - The SUMMARY contains no id, DSN, email, host or home path (`npx tsx scripts/check-planning-hygiene.ts` passes after the SUMMARY is written).
  </acceptance_criteria>
  <reversibility rating="reversible">An additive test arm; no schema or data outside the private loopback lane is touched.</reversibility>
  <done>The pre-fix verdict table exists in the SUMMARY, recorded by execution on the local lane, before any migration of this phase exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> database | The only permitted database is the loopback local stack; this checkout's Supabase CLI is linked to PRODUCTION. |
| test output -> public logs/SUMMARY | The repo is public; anything printed or written can be read by anyone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-01 | Tampering | ancestry step (rebase/merge) | medium | mitigate | abort on any conflict and STOP; never checkout/restore/stash; ancestry proved by `merge-base --is-ancestor` |
| T-164.9.1-02 | Tampering | harm probe target database | high | mitigate | lane only via `scripts/local-stack/run.sh` (derived workdir, loopback DSN, env handoff refuses non-loopback); no supabase CLI, no remote psql, no credential |
| T-164.9.1-03 | Information disclosure | HARM-PROBE verdict line and SUMMARY | medium | mitigate | verdict keys carry statuses and booleans only; `check-planning-hygiene.ts` run after the SUMMARY is written |
| T-164.9.1-04 | Denial of service | the probe's claim call on a shared lane | low | accept | the lane is private to this host; the claim touches only eligible `reconcile_strategy` rows there, and assertions read only the probe's own ids |
</threat_model>

<verification>
- Ancestry proved; pin table in the SUMMARY with commands.
- Pre-fix harm verdict recorded with D-02 wording.
</verification>

<success_criteria>
- `ANCESTRY-OK` and `HARM-RECORDED` both printed by their verify commands.
- No fix of any kind exists on the branch yet.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| rebased/merged branch carrying 164.4.2 | git history | every later plan |
| pin table + `PHASE-BASE:` line (SUMMARY) | measurement | plans 02, 03, 05, 06, 07, 09, 10, 11, 12, 13 |
| `P12-harm:` arm in `src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts` | live-DB regression arm | plan 02 (turns it green), plan 10 (ledger run) |
| pre-fix `HARM-PROBE verdict:` line (SUMMARY) | criterion-1 evidence | verifier, CHANGELOG (plan 14) |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md` when done.
</output>
