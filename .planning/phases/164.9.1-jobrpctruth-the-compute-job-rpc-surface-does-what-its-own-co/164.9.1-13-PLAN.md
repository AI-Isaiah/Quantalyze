---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 13
type: execute
wave: 9
depends_on: ["164.9.1-12"]
files_modified:
  - scripts/live-db-execution-ledger.txt
  - scripts/live-db-execution-ledger.mjs
  - supabase/migrations/<ts1>_fanin_initial_status_10param.sql
  - supabase/migrations/<ts2>_allocator_sync_restore_inflight_prefetch.sql
  - supabase/migrations/<ts3>_bridge_outcomes_invariant_comments.sql
  - supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql
autonomous: false
requirements:
  - "[164.9-FANIN-STATUS-NEVER-SET]"
  - "[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]"
  - "[164.9-TEST-ANALYTICS-URL-REARM]"

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "FC-2 / D-19: the founder confirms the merge ORDER — 164.4.2 merges to main before this PR (locked). On an overrule, the executor records it in BOTH CONTEXT.md and the ROADMAP (deviation policy) and then STOPS: the plan set assumed the locked order (the ledger removals, plan 10), so an overrule is a REPLAN via /gsd-plan-phase, never a task improvised inside this plan."
    - "D-19: '164.4.2 is on main' is decided by CONTENT (origin/main carries supabase/schema/baseline-carried-migrations.txt and the replaying lane header), never by branch-tip ancestry, because this repo squash-merges and a squashed branch tip is never an ancestor of main."
    - "The re-sync verdict RESYNC-OK is the ONLY state that lets plan 14 author the release commit; on 164.4.2-pending this plan HALTS without completing (no SUMMARY marked complete) and is re-run when 164.4.2 lands."
    - "D-20: after 164.4.2 is on main, this branch is brought onto origin/main and every 164.4.2-touched pin is RE-MEASURED again (whichever phase merges second re-measures); the ledger count/ceiling, prod-body-ack hashes, migration timestamps (strictly newer than main's newest) and the frontier count are re-checked on that tree."
    - "D-25: after the re-sync the phase still adds exactly three migrations, each timestamped after every migration on main."
  artifacts:
    - path: ".planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-13-SUMMARY.md"
      provides: "FC-2 decision, base used, re-measured pin table on main"
  key_links:
    - from: "FC-2"
      to: "plan 14 FC-1 precondition"
      via: "recorded merge order and re-sync verdict"
      pattern: "FC-2"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**

<objective>
FOUNDER CHECKPOINT FC-2 (merge order vs 164.4.2), then re-sync onto the tree the PR will actually merge into.

D-19 locks "164.4.2 first": without its D-F tail replay the CI live-DB lane cannot see this phase's migrations,
so criteria 2 and 6 would be unprovable in CI and the three ledger removals would turn `frontend-live-db-lane` RED.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: FOUNDER CHECKPOINT FC-2 — confirm 164.4.2 merges to main before this PR (D-19)</name>
  <files>(none)</files>
  <read_first>
    - `164.9.1-CONTEXT.md` D-19, D-20 and the FC-2 entry.
    - `git log --oneline -1 origin/main` and this task's verify (the content test for 164.4.2 on `origin/main`, after `git fetch origin`). ⛔ Not `git merge-base --is-ancestor <164.4.2 branch> origin/main`: squash merges make it false even after the merge.
  </read_first>
  <action>
    Present to the founder: whether 164.4.2 is already on `origin/main` (measured now), the locked order "164.4.2 first",
    and the consequence of the other order (the three ledger lines must stay and a follow-up reconciliation is owed).
    ⛔ The agent does not merge anything. Resume signals: `164.4.2-merged` (it is on main), `164.4.2-pending` (not yet;
    this phase waits — plan 14 cannot proceed), or `overrule: <reason>` (the executor records the overrule and its reason in
    BOTH `164.9.1-CONTEXT.md` and the ROADMAP phase entry, per the deviation policy, and then STOPS with a blocker
    "D-19 overruled: REPLAN required" — the orchestrator re-runs `/gsd-plan-phase 164.9.1` so that restoring the three
    ledger lines and the other-order consequences are PLANNED, re-reviewed and checked; no task is improvised here).
  </action>
  <instructions>
    Claude already did: every autonomous plan (01–10) and both review rounds (11–12) are complete, the three reviewers ran, the local lane and every local
    gate are green, and the ancestry of 164.4.2 on `origin/main` has just been measured by the verify below.
    The ONE thing requiring the founder: decide and state the merge order — 164.4.2 merged to main first (locked by D-19),
    or an explicit overrule with a reason.
  </instructions>
  <verification>
    Claude re-measures, after `git fetch origin`, whether `origin/main` carries 164.4.2 by content (this task's verify) and
    records it beside the founder's answer; Task 2 then re-syncs only on `164.4.2-merged`.
  </verification>
  <resume-signal>Type `164.4.2-merged`, `164.4.2-pending`, or `overrule: <reason>`.</resume-signal>
  <verify>
    <automated>git fetch origin >/dev/null 2>&1; git rev-parse --verify -q origin/main >/dev/null || { echo "origin/main unresolvable"; exit 1; }; if MRS="$(git show origin/main:scripts/local-stack/run.sh)" && git cat-file -e origin/main:supabase/schema/baseline-carried-migrations.txt && printf '%s\n' "$MRS" | grep -qF 'SCHEMA SOURCE: baseline + the TAIL'; then echo FC2-164.4.2-ON-MAIN; else echo FC2-164.4.2-NOT-ON-MAIN; fi</automated>
    <fails_when>the command itself errors (no network / unknown ref); the printed verdict is recorded, not judged — the founder's resume signal decides.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the measured ancestry verdict, the founder's resume signal verbatim, and (on overrule) the paths edited in CONTEXT.md and ROADMAP.md.
  </acceptance_criteria>
  <done>The merge order is a recorded founder decision, not an assumption.</done>
</task>

<task type="auto">
  <name>Task 2: Re-sync onto origin/main carrying 164.4.2 and re-measure every pin (D-20, D-25)</name>
  <files>scripts/live-db-execution-ledger.txt, scripts/live-db-execution-ledger.mjs, supabase/migrations/&lt;ts1&gt;…, supabase/migrations/&lt;ts2&gt;… (only if a re-measure forces a change)</files>
  <read_first>
    - Plan 01's pin table and plan 10's SUMMARY (the ledger numbers this branch set).
    - `git log --oneline "$PB"..origin/main -- supabase/migrations scripts/live-db-execution-ledger.txt scripts/mutation-runner/run.mjs scripts/restore-test-from-baseline.sh .github/workflows` (what moved on main since this phase started).
  </read_first>
  <action>
    Only after the resume signal `164.4.2-merged`: `git fetch origin`, then bring `origin/main` into the branch (rebase when on
    the phase branch, `git merge --no-edit` inside a worktree; abort and STOP on any conflict; never checkout/restore/stash).
    Re-measure the plan-01 pin table on this tree and diff it against plan 01's; for each moved pin, re-verify the dependent
    artifact: if main's ledger moved, `# ENTRY_COUNT`/`ENTRY_CEILING` must still equal the line count (this branch's -3 applied
    to main's value); if a newer migration landed on main, rename M1/M2/M3 to timestamps strictly newer than main's newest
    (keeping M1 < M2 < M3), update every reference to the old names (dedupe gate apply list and twins, snapshot source lines via
    `npm run schema:functions`); if main changed either function's snapshot, re-derive both `-- prod-body-ack:` hashes against the
    new `origin/main` copy. Re-boot the lane and re-run `npm run test:live-db:ledger`, the dedupe narrowed mutation run, the
    data-dependence linter and `dump-sql-functions --check`. Record everything in the SUMMARY; commit only what changed.
    If the signal was `164.4.2-pending`, this task records "waiting on 164.4.2" in the SUMMARY draft and HALTS: it returns a
    blocker, the plan is NOT marked complete, and plan 14 cannot start (its precondition requires this plan's RESYNC-OK).
    The orchestrator re-runs this plan once 164.4.2 is on main. If the signal was `overrule: …`, Task 1 already stopped.
  </action>
  <precondition>FC-2 resume signal received and it is `164.4.2-merged`, AND this task's content test finds 164.4.2 on `origin/main`; otherwise HALT (pending) or stop for replan (overrule) as the action says.</precondition>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.9.1-13"; mkdir -p "$S"; git fetch origin >/dev/null 2>&1; if MRS="$(git show origin/main:scripts/local-stack/run.sh)" && git cat-file -e origin/main:supabase/schema/baseline-carried-migrations.txt && printf '%s\n' "$MRS" | grep -qF 'SCHEMA SOURCE: baseline + the TAIL'; then git merge-base --is-ancestor origin/main HEAD && ADDED="$(git diff --name-only --diff-filter=A origin/main HEAD -- supabase/migrations)" && test "$(printf '%s\n' "$ADDED" | awk NF | wc -l | tr -d ' ')" = 3 && MT="$(git ls-tree --name-only origin/main supabase/migrations/)" && NEWEST_MAIN="$(printf '%s\n' "$MT" | grep -E '/[0-9]+_[a-z0-9_]+\.sql$' | sort | tail -1 | xargs basename)" && test -n "$NEWEST_MAIN" && for f in $ADDED; do test "$(basename "$f")" \> "$NEWEST_MAIN" || exit 1; done && npx tsx scripts/dump-sql-functions.ts --check && echo RESYNC-OK; else echo RESYNC-WAITING-ON-164.4.2; exit 1; fi</automated>
    <fails_when>no "RESYNC-OK" (exit non-zero): 164.4.2 is not yet on origin/main by content (prints RESYNC-WAITING-ON-164.4.2 and FAILS, so the plan cannot complete and plan 14 cannot start), origin/main is not an ancestor of HEAD, the phase adds other than exactly three migrations versus main, git ls-tree failed or main's newest migration cannot be read, a phase migration does not sort after main's newest, or the snapshots are stale.</fails_when>
  </verify>
  <acceptance_criteria>
    - On `164.4.2-merged`: `origin/main` is an ancestor of HEAD; exactly 3 added migrations versus `origin/main`, each newer than main's newest; the lane ledger gate green; the SUMMARY carries the re-measured pin table and a diff against plan 01's, and the verify's `RESYNC-OK` output copied onto a line of its own (plan 14's precondition greps for exactly that line).
    - On `164.4.2-pending`: the plan HALTS un-completed with the waiting verdict recorded; plan 14 does not start.
    - On `overrule: …`: CONTEXT.md and the ROADMAP record the overrule and its reason, and execution stopped with "REPLAN required".
  </acceptance_criteria>
  <reversibility rating="costly">A merge in the wrong order forces the ledger lines to stay and a follow-up reconciliation (D-19); the re-sync itself is recoverable from the reflog.</reversibility>
  <done>The branch sits on the exact tree it will merge into, with every pin re-measured there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branch -> main | merge order determines whether CI can see the migrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-33 | Tampering | a migration timestamp older than PROD's tip (backdated-migration policy) | medium | mitigate | rename to timestamps strictly newer than main's newest on re-sync; migration-policy.yml checks on the PR |
| T-164.9.1-34 | Denial of service | ledger removals merged before the replay exists (lane RED on main) | medium | mitigate | FC-2 founder confirmation; RESYNC verify requires 164.4.2 on main |
</threat_model>

<verification>
- FC-2 recorded; RESYNC-OK printed (a waiting verdict halts the plan un-completed).
</verification>

<success_criteria>
- The branch is re-measured on the tree it merges into (RESYNC-OK). Waiting on 164.4.2 or an overrule leaves this plan un-completed; neither lets plan 14 run.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| FC-2 decision + re-measured pin table on main (SUMMARY) | founder decision + measurement | plan 14 (CHANGELOG base, FC-1 precondition) |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-13-SUMMARY.md` when done.
</output>
