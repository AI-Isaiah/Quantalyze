---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 14
type: execute
wave: 10
depends_on: ["164.9.1-13"]
files_modified:
  - CHANGELOG.md
  - VERSION
  - package.json
  - TODOS.md
autonomous: false
requirements:
  - "[164.9-FANIN-STATUS-NEVER-SET]"
  - "[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]"
  - "[164.9-TEST-ANALYTICS-URL-REARM]"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-19 / D-20: the release commit is authored ONLY on a tree where 164.4.2 is on origin/main and plan 13 printed RESYNC-OK (recorded in its SUMMARY and re-measured live by Task 1's precondition); otherwise execution stops at a checkpoint and nothing is committed."
    - "CLAUDE.md CHANGELOG discipline: one unified CHANGELOG entry newest-first, written BEFORE the push in the same `chore(release): vX.Y.Z.B — JOBRPCTRUTH` commit as the VERSION bump; every commit from `git log <base>..HEAD` maps to at least one bullet (checklist cross-check recorded); VERSION and package.json are byte-equal 4-digit strings; npm version is never run."
    - "The three TODOS.md entries are closed in TODOS.md with the evidence (plan SUMMARY references), and the REARM entry notes that the live observation is FC-3, founder-owned and not a completion gate."
    - "FC-1 / D-21 / D-24: the founder merges the PR only when: the three-reviewer gate is complete (plans 11 and 12 SUMMARYs, REVIEW-GATES-OK), gsd-verifier has passed, and every CI check bound to the head SHA is green EXCEPT the single pre-declared VAC-08 row `_enqueue_compute_job_internal/10 DRIFT` whose TEST hash is the pre-change hash recorded in plan 02's SUMMARY; any other red blocks. After merge, the push-run test-db-drift is green, SHA-bound."
    - "D-23 is surfaced at FC-1 as an autonomous decision the founder may overrule (the restored disconnected-key refusal and the route's 409)."
    - "FC-3 / D-18: a live dispatch of test-restore-from-baseline.yml (preflight, then restore) to observe the in-transaction normalisation on shared TEST is a founder act, blocked on the founder-owned baseline re-dump, and NOT a completion gate of this phase; no agent dispatches it."
    - "No agent writes to shared TEST or PROD; no CI skip trailer token appears in any commit message or PR body."
  artifacts:
    - path: CHANGELOG.md
      provides: "unified entry for this phase"
    - path: VERSION
      provides: "4-digit version, byte-equal to package.json"
  key_links:
    - from: "chore(release) commit"
      to: "-pr filtered branch"
      via: "VERSION, package.json, CHANGELOG.md are non-planning paths and survive the filter"
      pattern: "chore\\(release\\)"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> ⭐ **FOUNDER CHECKPOINTS carried by this phase** (listed together here, as the orchestrator requires):
> **FC-1** merge of the PR (Task 2, blocking). **FC-2** merge ORDER vs 164.4.2 (plan 13 Task 1, blocking, locked
> "164.4.2 first"). **FC-3** any live dispatch of `test-restore-from-baseline.yml` (Task 3, recorded, NOT a
> completion gate). **D-23** is flagged at FC-1 as an autonomous decision the founder may overrule.

<objective>
Write the release record, then hand the two founder acts that remain to the founder with exact, SHA-bound
preconditions. Execution stops at FC-1 (run to the gate); the orchestrator runs gsd-verifier and /gsd-ship in
the meantime, and resumes for the post-merge SHA-bound check.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-02-SUMMARY.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-11-SUMMARY.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-12-SUMMARY.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-13-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CHANGELOG + VERSION + package.json in one chore(release) commit, commit-checklist cross-checked; TODOS entries closed</name>
  <files>CHANGELOG.md, VERSION, package.json, TODOS.md</files>
  <read_first>
    - `CLAUDE.md` "CHANGELOG discipline" (the six-step mechanism, section vocabulary, the gstack rule deliberately NOT adopted) and "CI gate integrity" trap 1 (skip tokens).
    - `CHANGELOG.md` top three entries (house voice), `VERSION`, `package.json` `"version"`.
    - `src/__tests__/critical-regressions.test.ts` (VERSION/package.json byte-equality assertion).
    - `TODOS.md` entries `[164.9-FANIN-STATUS-NEVER-SET]`, `[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]`, `[164.9-TEST-ANALYTICS-URL-REARM]` (their forbidden closures and owner lines).
    - All phase SUMMARYs 01–13.
  </read_first>
  <action>
    Base = `origin/main` (after plan 13). Step 1: `git log origin/main..HEAD --oneline`, count the commits, keep the list as a
    checklist. Step 2: `git diff origin/main...HEAD`, read it. Step 3: group by theme — Fixed (fan-in status, M1; Queued shape
    reachable, M2; disconnected-key refusal restored + 409, D-23), Changed (restore normalises in-transaction; ledger shrunk
    by 3), Tests (harm probe, P12 calibrated, XOR rewrite, restore/emitter self-tests), Root cause (the stale re-bases 070/076),
    Notes (D-02 "mechanism demonstrated / production harm latent"; known limits: D-11 race, stranded fan-in child, seven-arg
    42725 / enqueue_compute_portfolio_job latent; the pre-declared VAC-08 DRIFT row; FC-3 pending). Step 4: write ONE entry
    `## [X.Y.Z.B] - YYYY-MM-DD — JOBRPCTRUTH: …` newest-first, replacing any earlier entry for this branch. Step 5: cross-check
    every commit in the checklist maps to a bullet; record the mapping table (commit subject -> section) in the SUMMARY; an
    unmapped commit is a defect to fix before committing. Never quote a credential-shaped literal, host, key id or DSN.

    Version: read `VERSION` on the tree. Decide on MEANING and record the reason: this phase changes user-visible RPC
    behaviour (the Queued shape is now returned; a disconnected key now gets 409) and fixes a production write path, which
    this repo has bumped as a MINOR component (for example 0.86 -> 0.87 for a phase); bump accordingly unless main's history
    for comparable fix phases says otherwise, and cite the precedent you followed. Write the same 4-digit string to `VERSION`
    and `package.json` `"version"` by hand (never `npm version`; no lockfile change).

    TODOS.md: close the three entries (tick them, add a dated closure line naming the fix and the plan SUMMARYs as evidence).
    For the REARM entry, state that the restore now normalises in-transaction and that the live observation on shared TEST is
    FC-3, founder-owned, blocked on the baseline re-dump. Do not touch `[164.9-BASELINE-PRIVILEGES-ABSENT]`.

    Commit as `chore(release): vX.Y.Z.B — JOBRPCTRUTH` (plus a body; no CI skip token anywhere, not even denied), with the
    node_modules symlink removed around the commit and the three forbidden `.planning` paths unstaged.
  </action>
  <precondition>Plan 13's SUMMARY contains the line `RESYNC-OK` (its Task 2 verify passed on `164.4.2-merged`), AND, re-measured now after `git fetch origin`, `origin/main` carries 164.4.2 by content (`git cat-file -e origin/main:supabase/schema/baseline-carried-migrations.txt` exits 0 and `git show origin/main:scripts/local-stack/run.sh`, captured to a variable first, contains `SCHEMA SOURCE: baseline + the TAIL`) AND `git merge-base --is-ancestor origin/main HEAD` exits 0. If ANY of these is false, do NOT write VERSION, CHANGELOG or TODOS: STOP and return a `checkpoint:human-action` (gate blocking-human) stating which fact failed and that plan 13 must be re-run to RESYNC-OK first.</precondition>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.9.1-14"; mkdir -p "$S"; rm -f "$S/cr.log"; git fetch origin >/dev/null 2>&1; MRS="$(git show origin/main:scripts/local-stack/run.sh)" && git cat-file -e origin/main:supabase/schema/baseline-carried-migrations.txt && printf '%s\n' "$MRS" | grep -qF 'SCHEMA SOURCE: baseline + the TAIL' && git merge-base --is-ancestor origin/main HEAD && grep -qx 'RESYNC-OK' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-13-SUMMARY.md && V="$(cat VERSION)" && printf '%s' "$V" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$' && grep -qF "\"version\": \"$V\"" package.json && head -40 CHANGELOG.md | grep -qF "## [$V]" && MV="$(git show origin/main:VERSION)" && test -n "$MV" && test "$MV" != "$V" && HS="$(git log -1 --format=%s)" && printf '%s\n' "$HS" | grep -qE "^chore\(release\): v$V" && HST="$(git show --stat --format= HEAD)" && printf '%s\n' "$HST" | grep -qE 'CHANGELOG.md' && printf '%s\n' "$HST" | grep -qE '\bVERSION\b' && LOGM="$(git log origin/main..HEAD --format=%B)" && test -n "$LOGM" && ! printf '%s\n' "$LOGM" | grep -qiE '\[(skip ci|ci skip|no ci|skip actions|actions skip)\]|skip-checks:true' && npx vitest run src/__tests__/critical-regressions.test.ts --reporter=dot > "$S/cr.log" 2>&1 && ! grep -qE '\b0 (passed|tests)\b' "$S/cr.log" && echo RELEASE-OK</automated>
    <fails_when>no "RELEASE-OK": 164.4.2 is not on origin/main by content, origin/main is not an ancestor of HEAD, plan 13's SUMMARY has no `RESYNC-OK` line, VERSION is not 4-digit or differs from package.json, the CHANGELOG's newest entry is not this version, VERSION did not move from main, the head commit is not the chore(release) commit carrying CHANGELOG and VERSION, a git command whose output is grepped failed (every such output is captured into a variable first, so git's own status is checked and an empty log cannot pass a negated grep), a skip token appears in any branch commit message, or the critical-regressions test failed.</fails_when>
  </verify>
  <acceptance_criteria>
    - VERSION and package.json carry the same new 4-digit string; the CHANGELOG's first entry is `## [<that string>]`.
    - The SUMMARY holds the commit-checklist mapping with zero unmapped commits and the version-bump rationale with its precedent.
    - The three TODOS entries are ticked with dated closure lines; the REARM entry names FC-3 as pending and non-gating.
    - No branch commit message contains a skip token.
  </acceptance_criteria>
  <reversibility rating="reversible">Release bookkeeping on an unpushed branch.</reversibility>
  <done>The release record is written before the push and survives the -pr filter.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: FOUNDER CHECKPOINT FC-1 — merge the PR (auto-applies M1, M2, M3 to shared TEST, then PROD) (D-21, D-23, D-24)</name>
  <files>(none — founder act)</files>
  <read_first>
    - `164.9.1-11-SUMMARY.md` and `164.9.1-12-SUMMARY.md` (three-reviewer findings, both rounds, all dispositioned; REVIEW-GATES-OK), `164.9.1-02-SUMMARY.md` (pre-change ten-arg hash = the expected VAC-08 TEST hash), `164.9.1-13-SUMMARY.md` (FC-2 and re-sync verdict).
    - `CLAUDE.md` "CI gate integrity" (count checks bound to the head SHA) and the currency notes on `apply-test` / `test-db-drift`.
  </read_first>
  <action>
    Execution STOPS here. The orchestrator runs gsd-verifier and /gsd-ship (which opens the PR) while this checkpoint is open.
    Present to the founder, then wait:
    PRECONDITIONS (all must hold): FC-2 recorded as `164.4.2-merged` and RESYNC-OK; plans 11 and 12 findings all dispositioned with no
    open blocker and REVIEW-GATES-OK recorded; gsd-verifier status passed; on the PR head SHA, count the checks with
    `gh api "repos/AI-Isaiah/Quantalyze/actions/runs?head_sha=<sha>" -q '.workflow_runs | length'` (never trust "no red") and every
    check is green EXCEPT exactly one: `test-db-drift`'s VAC-08 step reporting a single `_enqueue_compute_job_internal/10 DRIFT` row
    whose TEST hash equals plan 02's recorded pre-change hash (D-24, red by construction because TEST still runs the old body until
    `apply-test`). Any other red, or a DRIFT row with a different hash, BLOCKS. VAC-04 must report the PROD hashes the two
    `-- prod-body-ack:` lines carry (earned acks).
    D-23 NOTICE: the restored `api_key_disconnected` refusal and the route's 409 were decided autonomously; the founder may overrule
    before merging (then M2 drops the refusal and the route branch in a follow-up commit, re-reviewed).
    ⛔ No agent merges. After the founder merges, resume with `merged <merge-sha>`; the executor then confirms, SHA-bound to the merge
    commit, that `supabase-migrate.yml`'s `apply-test` and `apply` concluded success and the push-run `test-db-drift` is green, and
    records the run ids. Alternative resume: `hold: <reason>` — recorded; the phase stays open at FC-1.
  </action>
  <instructions>
    Claude already did: all code, three migrations, three-reviewer gate (plan 11), re-sync onto main (plan 13), the release
    commit (Task 1); the orchestrator ran gsd-verifier and /gsd-ship. The ONE thing requiring the founder: after checking the
    preconditions above on the PR's head SHA, merge the PR (or hold, or overrule D-23 first). Merging applies M1, M2, M3 to shared
    TEST and then PROD automatically; no agent can or may do it.
  </instructions>
  <verification>
    Claude lists the workflow runs bound to the merge SHA (`gh api "repos/AI-Isaiah/Quantalyze/actions/runs?head_sha=<merge-sha>"`),
    confirms `apply-test` and `apply` concluded success and the push-run `test-db-drift` is green (VAC-08's DRIFT row cleared),
    and records the run ids. A red there is surfaced immediately; the recorded interim remedy is to revert the merge, never to
    edit `supabase-migrate.yml`.
  </verification>
  <resume-signal>Type `merged <merge-sha>`, `hold: <reason>`, or `overrule D-23` (then Claude drops the refusal and the 409 in a re-reviewed follow-up before FC-1 is asked again).</resume-signal>
  <verify>
    <automated>echo "FC-1 is a founder act; post-merge check: gh api repos/AI-Isaiah/Quantalyze/actions/runs?head_sha=<merge-sha> -q '.workflow_runs[] | [.name, .conclusion] | @tsv'"</automated>
    <fails_when>after resume, any workflow run bound to the merge SHA other than none-yet-finished concludes non-success, or test-db-drift is not green on the push run.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records: the PR head SHA, the check count bound to it, the single pre-declared VAC-08 row and its hash match, the founder's resume signal verbatim, D-23 kept or overruled, and (after merge) the merge SHA with the `apply-test`, `apply` and push-run `test-db-drift` run ids and conclusions.
  </acceptance_criteria>
  <done>The migrations are on TEST and PROD by the founder's hand, and the post-merge drift check is green and SHA-bound.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: FOUNDER CHECKPOINT FC-3 — live restore dispatch to observe the REARM on shared TEST (recorded; NOT a completion gate) (D-18)</name>
  <files>(none — founder act)</files>
  <read_first>
    - `164.9-11-SUMMARY.md` (the stale-baseline refusal that currently blocks a restore) and `docs/runbooks/test-analytics-url.md`.
  </read_first>
  <action>
    Present to the founder: a dispatch of `test-restore-from-baseline.yml` (preflight, then restore) would show the in-transaction
    normalisation on shared TEST, a database other people's CI uses; it is blocked on the founder-owned baseline re-dump against PROD.
    ⛔ No agent dispatches it and it does NOT gate this phase's completion. Resume signals: `deferred` (recorded, phase completes) or
    `dispatched <preflight-run-id> <restore-run-id>` (the executor then reads the runs' `note` normalisation line and arm D1's next
    reading, shapes only, and records them).
  </action>
  <instructions>
    Claude already did: the restore now normalises the destination row inside its own transaction, proven by the restore and
    emitter self-tests on disposable clusters (plans 04, 05). The ONE thing requiring the founder, when the baseline re-dump
    exists: dispatch `test-restore-from-baseline.yml` from main, preflight first, then restore.
  </instructions>
  <verification>
    If dispatched, Claude reads the two runs' normalisation `note` lines and the next arm D1 verdict (shapes only) and records
    them with the run ids. Nothing here gates the phase.
  </verification>
  <resume-signal>Type `deferred` or `dispatched <preflight-run-id> <restore-run-id>`.</resume-signal>
  <verify>
    <automated>echo "FC-3 recorded as founder-owned and non-gating"</automated>
    <fails_when>never fails the phase by design; a dispatched run that concludes failure is recorded and routed, not treated as this phase's regression gate.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the founder's FC-3 resume signal and, if dispatched, the run ids and the shape-only verdict lines.
  </acceptance_criteria>
  <done>FC-3 is recorded without gating the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR merge -> shared TEST -> PROD | auto-apply, no human reviewer gate beyond FC-1 |
| CHANGELOG / commit messages -> public repo and CI triggers | public text; skip tokens suppress CI even when denied |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-35 | Tampering | merging on an unverified or partially red board | high | mitigate | FC-1 preconditions: checks counted and SHA-bound; only the single pre-declared VAC-08 row with the recorded hash tolerated; post-merge push-run test-db-drift green |
| T-164.9.1-36 | Repudiation | CI silently suppressed by a skip token | high | mitigate | verify greps every branch commit message for the token family |
| T-164.9.1-37 | Information disclosure | CHANGELOG quoting a credential-shaped literal or host | medium | mitigate | shapes and verdicts only; gitleaks on the push-to-main squash commit |
| T-164.9.1-38 | Tampering | agent dispatching a destructive restore on shared TEST | high | mitigate | FC-3 is a founder act; no agent dispatches; non-gating |
</threat_model>

<verification>
- RELEASE-OK printed; FC-1 and FC-3 resume signals recorded.
</verification>

<success_criteria>
- Release record written before push; founder acts carried with SHA-bound preconditions.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| CHANGELOG entry + VERSION/package.json bump | release record | -pr branch, readers of main |
| TODOS.md closures | backlog ledger | milestone audit |
| FC-1 / FC-3 records (SUMMARY) | founder decisions + SHA-bound run ids | gsd-verifier, milestone close |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-14-SUMMARY.md` when done.
</output>
