---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 07
type: execute
wave: 4
depends_on: ["164.9.1-01", "164.9.1-03"]
files_modified:
  - supabase/migrations/<ts2>_allocator_sync_restore_inflight_prefetch.sql
  - supabase/schema/functions/request_allocator_holdings_sync.sql
  - src/__tests__/request-allocator-holdings-sync-queued.test.ts
autonomous: true
requirements:
  - "[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]"

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "D-09 / criterion 4: public.request_allocator_holdings_sync(uuid) returns {already_inflight: true, next_attempt_at} when an in-flight poll_allocator_positions job exists for the key, and {ok: true, job_id} when none exists — proven by execution in the live-DB Queued arms, which go RED when the prefetch is neutered."
    - "D-09: M2 is re-based on the LATEST body (076, 20260422122720, after re-grepping all migrations at execution); 067's pre-enqueue in-flight look-up replaces the dead poll-enqueue unique_violation handler; 076's per-api_key reconstruct gate is verbatim."
    - "D-10: _enqueue_compute_job_internal's contract is not changed to signal collisions; the user-facing RPC owns the shape."
    - "D-11: M2's header records the accepted race — two concurrent calls that both pass the prefetch collapse onto one row and the loser receives {ok, job_id} naming the winner's job — and no test is taught to accept the old return."
    - "D-12: exactly one EXCEPTION WHEN unique_violation remains in the body (the benign reconstruct-enqueue handler), kept verbatim."
    - "D-23: a soft-disconnected key is refused with SQLSTATE P0001 and message api_key_disconnected, after the ownership check and before the prefetch, proven by a live-DB arm whose title contains 'api_key_disconnected' and which goes RED when the RAISE is neutered. (The route's 409 mapping of exactly that error is plan 08.) Flagged as an autonomous decision the founder may overrule at FC-1."
    - "D-07: M2's self-verify DO block is catalog-only; lint-migration-data-dependence prints 0 NEW with no baseline line added."
    - "D-25: M2 carries a '-- prod-body-ack:' line derived locally at execution; the snapshot is regenerated in the same change; M2 is the second of at most three migrations."
    - "Orchestrator constraint 6 / research Pitfall 8: the Queued arms' early return on a failed password sign-in becomes a thrown failure (or, if the lane does not support password grant, the SUMMARY records why and the arm is not claimed as a gate)."
  artifacts:
    - path: "supabase/migrations/<ts2>_allocator_sync_restore_inflight_prefetch.sql"
      provides: "M2: 075 refusal + 067 prefetch on 076's body; ACL; catalog-only DO block; prod-body-ack"
    - path: supabase/schema/functions/request_allocator_holdings_sync.sql
      provides: "regenerated snapshot whose source-migration line names M2"
    - path: src/__tests__/request-allocator-holdings-sync-queued.test.ts
      provides: "Queued arms (no vacuous early return) + the D-23 refusal arm"
  key_links:
    - from: "AllocatorExchangeManager / sync route"
      to: "request_allocator_holdings_sync already_inflight shape"
      via: "route passes JSONB through verbatim"
      pattern: "already_inflight"
    - from: "D-23 live-DB arm"
      to: "M2 RAISE 'api_key_disconnected' USING ERRCODE 'P0001'"
      via: "owner-authenticated RPC call on the lane"
      pattern: "api_key_disconnected"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> Wave coupling that is not a file overlap: this plan boots the SAME single local stack plans 02 and 03 boot
> (one stack per host, disk-bound), and M2's timestamp must sort after M1's. So it runs after plan 03.
> The route's 409 branch was split into plan 08 (checker iteration 1: per-plan budget).

<objective>
Make an in-flight collision distinguishable from a fresh enqueue at the caller whose contract promises
the shape (ROADMAP criterion 4, `[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]` item a), and close the second
stale-re-base loss on the same function (D-23: 075's disconnected-key refusal); plan 08 maps that refusal
to a 409 at the route.

Purpose: the Queued pill is user-facing; a disconnected key being re-syncable is user-facing.
Output: M2, its snapshot, and the Queued live-DB arms (including the D-23 refusal arm) green and calibrated.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-RESEARCH.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-PATTERNS.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: M2 — restore 075's refusal and 067's prefetch on 076's body; Queued arms green and calibrated (D-07, D-09..D-12, D-23, D-25)</name>
  <files>supabase/migrations/&lt;ts2&gt;_allocator_sync_restore_inflight_prefetch.sql, supabase/schema/functions/request_allocator_holdings_sync.sql, src/__tests__/request-allocator-holdings-sync-queued.test.ts</files>
  <read_first>
    - Re-grep first: `grep -ln "FUNCTION request_allocator_holdings_sync\|FUNCTION public.request_allocator_holdings_sync" supabase/migrations/*.sql | sort`. The newest is the re-base source (expected `supabase/migrations/20260422122720_reconstruct_per_api_key_gate.sql`, 076).
    - `supabase/migrations/20260420103104_allocator_sync_queued_prefetch.sql` (067: the prefetch and its race-window comment; ⛔ NOT its DO blocks — the pattern arm forbids the handler D-12 keeps and the STEP 3 functional probe is a data-dependent DO block).
    - `supabase/migrations/20260422101911_api_keys_disconnected_at.sql` STEP 5 (075: `v_disconnected`, the `api_key_disconnected` RAISE with ERRCODE `P0001`).
    - `supabase/migrations/20260420213754_allocator_equity_snapshots.sql` (070, where the prefetch was lost).
    - `supabase/migrations/<ts1>_fanin_initial_status_10param.sql` (M1) DO block — the catalog-only SHAPE to copy.
    - `src/__tests__/request-allocator-holdings-sync-queued.test.ts` in full (`seedApiKey`, `pinInflightJob`, `createAuthedClient`, both arms, the header).
    - `src/lib/test-helpers/live-db.ts` `signInAsTestUser` (throws instead of skipping).
    - `164.9.1-RESEARCH.md` Contradiction 2, Q5 "M2 arms", "Code Examples: M2 prefetch"; `164.9.1-PATTERNS.md` "M2" including its anti-pattern list.
  </read_first>
  <behavior>
    - Arm "returns {already_inflight, next_attempt_at} when a live job exists": with a pinned in-flight poll job for the key, the RPC as the owner returns `already_inflight === true` and a non-null `next_attempt_at`, and the key's `sync_status` is not flipped to `syncing`.
    - Arm "returns {ok, job_id} when no live job exists (happy path)": unchanged contract, `ok === true`, a `job_id`.
    - New arm (D-23), with `api_key_disconnected` in its title (the verify greps a passing line for that token): after an admin UPDATE setting the seeded key's `disconnected_at`, the owner's RPC call returns an error with code `P0001` and message `api_key_disconnected`, the response carries no `already_inflight`, and no new `poll_allocator_positions` row exists for that key.
    - A failed password sign-in THROWS (never `return`s) in every arm.
  </behavior>
  <action>
    Create M2 at `supabase/migrations/<ts2>_allocator_sync_restore_inflight_prefetch.sql`, `<ts2>` the real UTC
    time at authoring and strictly newer than M1 and every other file, name matching `^[0-9]+_[a-z0-9_]+\.sql$`.

    Body (D-09, D-23, D-12): a schema-qualified `CREATE OR REPLACE FUNCTION public.request_allocator_holdings_sync(p_api_key_id uuid)`
    that copies 076's signature, `SECURITY DEFINER`, `SET search_path = public, pg_catalog` (verify the exact value
    076 sets and keep it), DECLARE, auth gate and ownership check verbatim, with these changes only:
    (1) the ownership SELECT also reads `disconnected_at` into a new `v_disconnected TIMESTAMPTZ`, and right after the
    ownership RAISE, `IF v_disconnected IS NOT NULL THEN RAISE EXCEPTION 'api_key_disconnected' USING ERRCODE = 'P0001'`
    (075's exact SQLSTATE and message, D-23);
    (2) 076's `BEGIN … EXCEPTION WHEN unique_violation` block around the poll enqueue is REPLACED by 067's prefetch —
    SELECT the newest `next_attempt_at` of a `poll_allocator_positions` job for this key in status `pending`,
    `running` or `done_pending_children`; if found, RETURN `jsonb_build_object('already_inflight', true, 'next_attempt_at', v_next_attempt)`;
    otherwise assign `v_job_id` from the `enqueue_compute_job` poll call exactly as 067 does (D-09);
    (3) 076's reconstruct gate, its benign `EXCEPTION WHEN unique_violation THEN NULL` handler, the `sync_status = 'syncing'`
    UPDATE and the `{ok, job_id}` RETURN stay verbatim (D-12).
    ACL (schema-qualified, full signature): `REVOKE ALL ON FUNCTION public.request_allocator_holdings_sync(uuid) FROM PUBLIC, anon;`
    `GRANT EXECUTE ON FUNCTION public.request_allocator_holdings_sync(uuid) TO authenticated;`. Do not revoke service_role.
    `COMMENT ON FUNCTION public.request_allocator_holdings_sync(uuid)` states the three return shapes and the refusal.

    Header: the lineage 067 -> 070 (prefetch lost) -> 075 (refusal added) -> 076 (refusal lost) -> M2, the re-base grep
    and its result, the D-11 accepted race in 067's words, and the `-- prod-body-ack:` block derived exactly as plan 02
    did (`git show origin/main:supabase/schema/functions/request_allocator_holdings_sync.sql > "$S/main-rahs.sql"`, then
    `node scripts/sql-body-normalize.mjs --diff-bodies supabase/schema/functions/request_allocator_holdings_sync.sql "$S/main-rahs.sql"`,
    the live-column hash; a difference from the research reading is recorded, never "fixed" by editing the ack).

    Self-verify DO block (D-07), SHAPE copied from M1's: `to_regprocedure('public.request_allocator_holdings_sync(uuid)')`,
    comment-stripped `pg_get_functiondef`, NULL-body guard; arms — the prefetch SELECT's `poll_allocator_positions` read
    appears BEFORE the `enqueue_compute_job(` poll call (compare `strpos` positions); EXACTLY ONE
    `EXCEPTION WHEN unique_violation`, counted whitespace-tolerantly as
    `(SELECT count(*) FROM regexp_matches(v_body, 'EXCEPTION[[:space:]]+WHEN[[:space:]]+unique_violation', 'g'))`
    and required to equal 1 (never assert zero; `[[:space:]]+` because the handler may be written across a line break,
    and POSIX classes are what PostgreSQL's ARE engine documents; keep the handler on ONE source line anyway so the
    line-oriented acceptance grep below agrees with the DO block); `api_key_disconnected` and
    `v_disconnected` present and before the prefetch; `v_prior_reconstruct`, `reconstruct_allocator_history` and
    `api_key_id = p_api_key_id` present; `already_inflight` present; SECDEF; exact search_path in `proconfig`;
    `PERFORM public._assert_no_public_execute('public.request_allocator_holdings_sync(uuid)')`; anon has no EXECUTE and
    authenticated has EXECUTE (`has_function_privilege` with `to_regrole` existence checks). Single-literal RAISE formats,
    `SET LOCAL lock_timeout = '3s';`, no BEGIN/COMMIT, no `cron.schedule`, never `FROM public.<table>` in the DO block.

    `npm run schema:functions` after the header is final.

    Test file: rewrite the header to record the lineage and the fix; make `createAuthedClient` THROW on a sign-in error
    (or switch to `signInAsTestUser`), removing both `if (!authed) return;` lines (Pitfall 8); add the D-23 arm per
    <behavior>, asserting only about its OWN seeded key and cleaning up in `finally`.

    Re-boot the lane (`bash scripts/local-stack/run.sh down` then `up`; the `baseline-replay:` line names M1 and M2) and
    run the file. Calibrate (criterion 6): byte-backup M2 to `"$S/m2.bak"`, neuter the prefetch's early RETURN (and the
    DO-block arm that checks it, so the replay does not FATAL), re-boot, observe the `already_inflight` arm RED; restore
    with `cp`, `cmp` it, re-boot, observe GREEN. Same for the refusal: neuter the `api_key_disconnected` RAISE (and its
    arm), observe the D-23 arm RED, restore.
    Machine-checkable record: write into this plan's SUMMARY (create it if absent) exactly one line per calibration,
    `CALIBRATION-RED: m2-prefetch-return neuter=FAIL restored-cmp=0 restored=OK` `CALIBRATION-RED: m2-disconnected-raise neuter=FAIL restored-cmp=0 restored=OK`,
    each written only AFTER its neuter was observed RED, the `cp` restore was `cmp`-verified (exit 0) and the arm was
    observed GREEN again; never pre-write one. A neuter that did not go RED means the arm is not armed: fix the arm. Never `git checkout --`. If the lane rejects password sign-in, the arms now
    FAIL loudly; record that in the SUMMARY as a blocker rather than restoring the early return.

    Commit hygiene as in plan 02 (node_modules symlink removed around commit; three `.planning` paths never staged).
  </action>
  <precondition>Plans 02 and 03 committed (M1 exists, its timestamp known, P12 calibrated and M1 restored byte-exact); plan 01's SUMMARY carries a `PHASE-BASE:` line; Docker up; no other local stack booted on this host.</precondition>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.9.1-07"; mkdir -p "$S"; M2="$(ls supabase/migrations/*_allocator_sync_restore_inflight_prefetch.sql)"; M1="$(ls supabase/migrations/*_fanin_initial_status_10param.sql)"; test "$(basename "$M2")" \> "$(basename "$M1")" && grep -qE '^-- prod-body-ack: [0-9a-f]{64}$' "$M2" && grep -qF "'api_key_disconnected'" "$M2" && grep -qF "USING ERRCODE = 'P0001'" "$M2" && ! grep -qiE 'cron\.schedule' "$M2" && ! grep -qE '^\s*(BEGIN|COMMIT);' "$M2" && ! grep -qF 'if (!authed) return' src/__tests__/request-allocator-holdings-sync-queued.test.ts && npx tsx scripts/dump-sql-functions.ts --check && node scripts/lint-migration-data-dependence.mjs > "$S/lint.log" 2>&1; RC=$?; cat "$S/lint.log"; test "$RC" = 0 && grep -qE '\b0 NEW\b' "$S/lint.log" && PB="$(sed -nE 's/^PHASE-BASE: ([0-9a-f]{40})$/\1/p' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md)" && test -n "$PB" && git diff --quiet "$PB" -- scripts/lint-migration-data-dependence-baseline.txt && npx vitest run --config vitest.livedb.config.ts src/__tests__/request-allocator-holdings-sync-queued.test.ts --reporter=verbose > "$S/queued.log" 2>&1; RC=$?; cat "$S/queued.log"; test "$RC" = 0 && grep -qE '✓.*already_inflight, next_attempt_at' "$S/queued.log" && grep -qE '✓.*api_key_disconnected' "$S/queued.log" && ! grep -qE '\b[1-9][0-9]* (failed|skipped)\b' "$S/queued.log" && test "$(grep -cxF 'CALIBRATION-RED: m2-prefetch-return neuter=FAIL restored-cmp=0 restored=OK' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-07-SUMMARY.md)" = 1 && test "$(grep -cxF 'CALIBRATION-RED: m2-disconnected-raise neuter=FAIL restored-cmp=0 restored=OK' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-07-SUMMARY.md)" = 1 && echo M2-OK</automated>
    <fails_when>no "M2-OK": M2 missing, not newer than M1, missing the ack or the P0001 refusal, containing cron.schedule or explicit BEGIN/COMMIT, the vacuous early return still present, snapshot stale, a NEW data-dependence refusal or a baseline change, or any Queued arm failed, was skipped (lane down) or did not run. Also fails when the SUMMARY lacks exactly one `CALIBRATION-RED:` line for `m2-prefetch-return`, `m2-disconnected-raise` (the neuter's RED, the `cmp`-verified restore and the GREEN re-run are recorded as a structured line, not only as prose).</fails_when>
  </verify>
  <acceptance_criteria>
    - One file matches `supabase/migrations/*_allocator_sync_restore_inflight_prefetch.sql`, named after M1 lexically.
    - Over M2's non-comment lines: `grep -c 'EXCEPTION WHEN unique_violation'` returns 1; `grep -c 'poll_allocator_positions'` returns at least 2 (prefetch and enqueue); `already_inflight` present; `REVOKE ALL ON FUNCTION public.request_allocator_holdings_sync(uuid) FROM PUBLIC, anon;` present.
    - `supabase/schema/functions/request_allocator_holdings_sync.sql` names M2 on its source-migration line.
    - `grep -c 'if (!authed) return' src/__tests__/request-allocator-holdings-sync-queued.test.ts` returns 0.
    - All arms in the Queued file pass on the lane with M2 replayed; the SUMMARY records the two neuter -> RED -> restore -> GREEN calibrations with `cmp` exit 0 after each restore.
    - `0 NEW` from the data-dependence linter; `git diff --quiet "$PB" -- scripts/lint-migration-data-dependence-baseline.txt` exits 0 (`PB` = plan 01's `PHASE-BASE:` sha).
    - The D-23 arm's title contains `api_key_disconnected`; `EXCEPTION WHEN unique_violation` is written on one source line in M2.
  </acceptance_criteria>
  <reversibility rating="reversible">Forward-only CREATE OR REPLACE; the prior body is re-runnable. D-23 may be overruled by the founder at FC-1 review.</reversibility>
  <done>A caller can tell an in-flight collision from a fresh enqueue, a disconnected key is refused, and both are proven by execution and calibrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (authenticated user JWT) -> route -> SECDEF RPC | untrusted `api_key_id` crosses here; ownership is checked inside the RPC |
| RPC error -> user-visible JSON | error text must be user-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-10 | Elevation of privilege | M2 SECDEF + default-grant event trigger | high | mitigate | schema-qualified CREATE; REVOKE FROM PUBLIC, anon + GRANT authenticated re-issued; DO block `_assert_no_public_execute` + anon/authenticated arms |
| T-164.9.1-11 | Information disclosure / spoofing | ownership check in request_allocator_holdings_sync | high | mitigate | auth gate and ownership check kept verbatim from 076 and placed BEFORE the refusal and the prefetch, so a non-owner learns neither disconnection nor in-flight state; D-23 arm and existing arms run as the real owner |
| T-164.9.1-12 | Tampering | re-syncing a soft-disconnected key | medium | mitigate | 075 refusal restored (P0001) before any enqueue; D-23 live arm proves no poll row is created |
| T-164.9.1-14 | Tampering | prefetch/enqueue race (D-11) | low | accept | two concurrent calls collapse onto one row via the partial unique index; loser gets {ok, job_id}; accepted by 067, recorded in M2 header |
</threat_model>

<verification>
- M2-OK printed; the two calibrations (prefetch, refusal) recorded.
</verification>

<success_criteria>
- ROADMAP criterion 4 met by execution; D-23's refusal delivered with a failing-capable live-DB arm.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| `supabase/migrations/<ts2>_allocator_sync_restore_inflight_prefetch.sql` (M2) | migration | plan 11 reviewers, FC-1 |
| `supabase/schema/functions/request_allocator_holdings_sync.sql` | regenerated snapshot | sql-function-snapshot, VAC-04 |
| Queued live-DB arms (incl. D-23 arm, no vacuous return) | live-DB regression gate | plan 10 ledger run |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-07-SUMMARY.md` when done.
</output>
