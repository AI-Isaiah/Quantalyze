---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 13
subsystem: release-sequencing
tags: [fc-2, merge-order, resync, pin-remeasure]
status: complete
requires:
  - phase: 164.9.1-12
    provides: "review rounds complete, REVIEW-GATES-OK"
provides:
  - "FC-2 merge order recorded: 164.4.2 is on origin/main by content, so the locked order D-19 already holds"
  - "the branch merged with origin/main 01dcf1cc7 (v0.91.0.0), with every plan-01 pin re-measured on the merged tree"
  - "RESYNC-OK, the line plan 14's precondition greps for"
affects: [164.9.1-14]

actuals:
  tokens: 4000
  tasks: 2
  commits: 3
plan_head_before: 5e43d4d97dd95e7296e821ed2f9b0575675ddda2

tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-13-SUMMARY.md
  modified: []

key-decisions:
  - "Merge, not rebase. The phase SUMMARYs cite this branch's commits by SHA, and a rebase would orphan every one of them."
  - "FC-2 was treated as settled by fact. 164.4.2 shipped to main as v0.87.0.0 before this phase started, so the order D-19 locks has already happened, and the overrule branch is not physically available. No founder resume signal was given; see Task 1."

duration: ~20min
completed: 2026-09-25
---

# Phase 164.9.1 Plan 13: FC-2 merge order and the re-sync onto origin/main Summary

**164.4.2 was already on `origin/main` by content, so the locked merge order holds as a fact. The branch now carries `origin/main` at `01dcf1cc7` (v0.91.0.0) through merge commit `49c984a41`. Main moved no path under `supabase/`, `scripts/` or `.github/` since the phase base, so every pin re-measured to its plan-01 value or to the value this branch set itself. All three phase migrations still sort after main's newest, and the whole gate chain is green on the merged tree.**

RESYNC-OK

## Task 1: FC-2, the merge order (D-19)

The content test, run after `git fetch origin`, printed:

```
FC2-164.4.2-ON-MAIN
```

`origin/main` was `01dcf1cc7`. It carries `supabase/schema/baseline-carried-migrations.txt`, and its `scripts/local-stack/run.sh` carries `SCHEMA SOURCE: baseline + the TAIL`. 164.4.2 shipped as v0.87.0.0. Plan 01 already found it on the phase base `f8a3096e3`.

**Founder resume signal: none was given.** The orchestrator relayed the measured state and directed the re-sync. The checkpoint asks the founder to choose between "164.4.2 first" and an overrule. 164.4.2 is already merged, so the only order that can still happen is the locked one. The overrule path, where this PR merges before 164.4.2, is no longer possible. The decision this gate protects is therefore settled by a measured fact, not by an assumption. **This is a deviation from the plan's letter:** the task's acceptance criterion asks for the founder's signal verbatim. If the founder wants the signal on record, `164.4.2-merged` is the only answer consistent with the measurement. Nothing in Task 2 depends on the signal beyond that fact. No edit was made to CONTEXT.md or the ROADMAP, because there was no overrule.

## Task 2: re-sync and re-measure (D-20, D-25)

**What main moved since the phase base.** `git log f8a3096e3..origin/main` lists two commits:
- `bb31564f9`, Phase 166 QSTATS-TRUTH v0.91.0.0 (#858)
- `01dcf1cc7`, the verification paperwork close (#856)

`git diff --name-only f8a3096e3 origin/main -- supabase scripts .github` is **empty**. The only path both sides touched is `.planning/ROADMAP.md`, and `git merge-tree` resolved it cleanly. The merge `git merge --no-ff origin/main` → `49c984a41` had no conflict and deleted no files. `git merge-base --is-ancestor origin/main HEAD` exits 0.

### Re-measured pin table (merged tree, diffed against plan 01)

| Symbol | Plan 01 (phase base) | Now | Why it moved |
|---|---|---|---|
| ledger `# ENTRY_COUNT =` | 10 | **7** | This branch (plan 10 removed the three K3 lines). `origin/main` still reads 10, so the branch's −3 applies to main's value. |
| ledger non-comment lines | 10 | **7** | Same. It equals `ENTRY_COUNT`. |
| `ENTRY_CEILING` | 10 | **7** | Same. It equals the line count. |
| K3 lines (`P12:` / `widened UNIQUE:` / `Queued path`) | present | **0** | This branch (plan 10). |
| `FILES_FLOOR` | 50 | 50 | unchanged |
| `ARMS_FLOOR` | 449 | 449 | unchanged |
| `WAIVED_CEILING` | 0 | 0 | unchanged |
| `LANE_PG_VERSION` | `17.6.1.113` | `17.6.1.113` | unchanged |
| `ci.yml` job keys `sql-tests:` / `test-db-drift:` | both present | both present | unchanged |
| `61616158` in `ci.yml` / `supabase-migrate.yml` | 29 / 7 | 29 / 7 | unchanged |
| `restore: tables=2` in the restore script | 4 | **0** | This branch (plan 05, `8559c9e2d`, rewrote the restore's summary line). `origin/main` still reads 4. |
| `EXPECTED_ARMS=` | 32 | **37** | This branch: 32 → 34 in plan 05 (`8559c9e2d`), then 34 → 37 in plan 11 (`8a3053f1e`, arms 35–37). `origin/main` still reads 32. |
| `FRONTIER_EXEMPT_CEILING` | 3 | 3 | unchanged |
| newest migration on main | `20260924120000_ledger_fanout_failure_count.sql` | same | Main added no migration. |
| `VERSION` | `0.90.1.0` | **`0.91.0.0`** | Main (Phase 166). Plan 14's bump must start from 0.91.0.0. |

Each "Now" value was read by the same command as plan 01's table. The attribution column comes from reading the same symbol at `f8a3096e3`, `origin/main` and `HEAD`.

**Dependent artifacts:**
- **Migration timestamps.** Exactly three migrations are added against `origin/main`, and each sorts after `20260924120000_ledger_fanout_failure_count.sql`. No rename was needed.
  - `20260924230827_fanin_initial_status_10param.sql`
  - `20260924233749_allocator_sync_restore_inflight_prefetch.sql`
  - `20260925071300_bridge_outcomes_invariant_comments.sql`
- **prod-body-ack hashes.** Main changed nothing under `supabase/`, so neither function's snapshot moved and no re-derivation was needed. `dump-sql-functions --check` reports current.
- **Ledger.** Main's ledger did not move, so 7 = 7 = 7 still holds.

### Gate chain on the merged tree

Plan 12's staged chain was re-run with the same clauses. The scratch directory was given by absolute path and `TMPDIR` was unset (plan 11's tsx-socket deviation). Each stage ran under a 600 s `timeout`.

```
down=0
lane+ledger=0
static=0
mutation=2
tsc=0
RESYNC-OK
```

| Stage | Output (verbatim) |
|---|---|
| lane boot | `baseline-replay: 3 migration(s) newer than the dump: 20260924230827_fanin_initial_status_10param.sql 20260924233749_allocator_sync_restore_inflight_prefetch.sql 20260925071300_bridge_outcomes_invariant_comments.sql` · `[local-stack] up complete` |
| `npm run test:live-db:ledger` | `[live-db-ledger] executed: 401 test(s) (394 passed, 7 failed), 81 skipped, 482 collected, across 49 file(s)` · `[live-db-ledger] OK — the failing set is EXACTLY the ledger (7/7). Every corpus file executed; no arm outside the ledger failed; no ledger entry has gone stale.` |
| data-dependence linter | `lint-migration-data-dependence: corpus 277 migration(s), 350 anonymous block(s); 83 refusal(s), 83 allowlisted, 0 NEW.` |
| `dump-sql-functions.ts --check` | `SQL function snapshot is current (121 functions).` |
| migrations added vs PHASE-BASE | exactly 3 (as listed above) |
| normalize `--self-test` | `test-only-normalize-analytics-url: self-test OK (36 checks)` |
| restore `--self-test` | `restore-test-from-baseline: self-test OK (37/37 arms — TEN refusals fire before any write …` |
| route + restore vitest | `Test Files  2 passed (2)` · `Tests  56 passed (56)` |
| narrowed mutation (`test_enqueue_compute_job_dedupe_non_terminal.sql`) | exit 2 · `arms: 2/2/0` · `biting: 2` · `lane-invocations: 2` · `No defects in the narrowed scope.` · defect-class hits: 0 |
| `tsc --noEmit -p .` | exit 0 |
| plan 13 verify | `RESYNC-OK` |

The lane was torn down after stage 1 (`down=0`).

## Deviations from Plan

1. **[FC-2, recorded]** No founder resume signal was obtained. The merge order was settled as a measured fact (see Task 1). The orchestrator directed the re-sync on that basis.
2. **[environment]** Plan 12's verify stages ran with the scratch directory given by absolute path and `TMPDIR` unset, the same deviation as plans 11 and 12. The clauses and predicates are unchanged.
3. **[scope, directed]** The merge used `git merge --no-ff` with an explicit message. The plan names `--no-edit`; the explicit message was used to carry the attribution trailers. The merge had no conflicts.

`actuals.commits: 3` is `git rev-list --count 5e43d4d97..HEAD`, measured before this SUMMARY's commit. It counts the merge commit plus the two main commits it brings in. The first-parent count is 1.

## Self-Check: PASSED

- FOUND: merge commit `49c984a41` on `feat/164.9.1-jobrpctruth`, with `origin/main` `01dcf1cc7` as its second parent.
- FOUND: this SUMMARY.
- The `RESYNC-OK` line above is on a line of its own.
