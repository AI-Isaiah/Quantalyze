---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 03
subsystem: testing
tags: [compute-jobs, fan-in, mutation-runner, live-db-lane, calibration]
status: complete

requires:
  - phase: 164.9.1-02
    provides: "M1 (20260924230827_fanin_initial_status_10param), the newest ten-arg _enqueue_compute_job_internal body"
provides:
  - "the dedupe gate's RED-UNDER-SETUP apply list ends at M1, and twins B1/B2 edit M1's body"
  - "P12 calibrated by neuter, RED, restore and GREEN on the local lane (criterion 6, D-08)"
affects: [164.9.1-10, 164.9.1-12, 164.9.1-14]

actuals:
  tokens: 2200
  tasks: 2
  commits: 1
plan_head_before: f7f96c162dddd0fafa311d4d7584966f7e8efbfb

tech-stack:
  added: []
  patterns:
    - "re-point a mutation twin by changing only its file value, so find, replace and occurrences stay byte-identical"
    - "neuter a migration's INSERT together with the DO-block arm that guards it, so the lane replay succeeds and the arm under test is the one that goes RED"

key-files:
  created: []
  modified:
    - supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql

key-decisions:
  - "20260826150000 stays in the apply list, and M1 is appended after it. The plan says append. M1 re-issues only the ten-arg body, so the earlier file still supplies whatever else it defines."
  - "The neuter also disabled M1's DO-block arm (k), the only arm that reads the INSERT. Arms (i), (j) and (l) read the assignments and the seven-arg body, which the neuter did not touch, so they stayed live."

duration: ~15min
completed: 2026-09-25
---

# Phase 164.9.1 Plan 03: Dedupe twins follow M1, and P12 is proven able to fail Summary

**The dedupe gate's mutation twins now edit the body that is live, M1's ten-arg `_enqueue_compute_job_internal`, and both still bite. Arm P12 was observed RED (child status `pending`) with M1's INSERT status neutered, and GREEN after a `cmp`-verified byte restore.**

## Task 1: re-point the twins (research Pitfall 1, D-26)

One commit, `44bc7d93d`, changes only `supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql`:
- `RED-UNDER-SETUP` apply list: `supabase/migrations/20260924230827_fanin_initial_status_10param.sql` is appended as the LAST element.
- Twin B1's first edit and twin B2 now name M1 as their `file`. Their `find`, `replace` and `occurrences` are byte-identical to before. B1's second edit, on `20260416125430`, is unchanged.
- The header note and both prose `RED-UNDER:` lines name `20260924230827`.
- No twin was added or removed, and no SQL-corpus arm was added (D-26).

Before the edit, the twins' `find` text was measured in M1: it occurs exactly once, at the strategy-arm optimistic look-up in the body, and in no comment.

**Plan verify, run verbatim** (exit status A=0, runner RC=2):

```
978:export const FILES_FLOOR = 50;
2304:export const ARMS_FLOOR = 449;
2418:export const WAIVED_CEILING = 0;
mutation-runner: scope supabase/tests
scope: DIAGNOSTIC supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql
  baseline  supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql — exit 0 (1.5s)
  arm B1                       exit   3  RED (identity ok)  (1.4s)
  arm B2                       exit   3  RED (identity ok)  (1.5s)
  restore   supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql — exit 0 (1.4s)

coverage: files 50/77
arms: 2/2/0   (executed/annotated/waived)
biting: 2   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 2   (arm lanes actually spawned — tallied inside runLane, independent of the 2 the verdict loop counted; plus 1 baseline / 1 restore leg(s))
  file test_enqueue_compute_job_dedupe_non_terminal.sql: sections 2 / judged 2 / annotated 2 / waived 0 / biting 2
per-arm lane time: mean 1.5s over 2 arm run(s)

⚠️ NARROWED DIAGNOSTIC RUN (--file/--arm): coverage floors NOT enforced.
   This mode never exits 0, so it can never be mistaken for a passing gate.

No defects in the narrowed scope.
A=0 RC=2
TWINS-OK
```

The `unreachable:` and `lane-*` lines of the run are omitted above. They list the corpus-wide `[REDUNDER-NONIDIOM]` files and are not specific to this file.

- `FILES_FLOOR`, `ARMS_FLOOR` and `WAIVED_CEILING` did not move. `git diff --quiet <PHASE-BASE> -- scripts/mutation-runner/run.mjs` exits 0.
- `node scripts/lint-sql-gates.mjs` reported `scanned 77 file(s); 0 finding(s), 0 measure-fail(s), 0 allowlist error(s).`
- M1 applies cleanly on the vanilla pg-lane cluster at the end of the apply list, because the baseline leg exited 0.

## Task 2: P12 calibration (D-08, criterion 6)

1. **Backup.** `cp` M1 to a scratch byte backup, then `cmp` it.
2. **Neuter** (working copy only, never committed):
   - `status` removed from the INSERT column list;
   - `v_initial_status` removed from VALUES;
   - DO-block arm (k), the INSERT copy-check, disabled with `IF false THEN`. Without that, the replay would FATAL on arm (k) and say nothing about P12.
3. **Neutered lane.** `bash scripts/local-stack/run.sh down`, then `up`, exit 0. It printed `baseline-replay: 1 migration(s) newer than the dump: 20260924230827_fanin_initial_status_10param.sql`, and M1 replayed with its ledger row written.
4. **P12 under the neuter.** `npx vitest run --config vitest.livedb.config.ts src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts -t "P12" --reporter=verbose` exited 1:
   ```
   × ... fan-in chain > P12: enqueue with parent_job_ids inserts row as done_pending_children
   AssertionError: expected 'pending' to be 'done_pending_children' // Object.is equality
   × ... fan-in chain > P12-harm: a parented child is not claimable before its parent is done and is released by the parent's mark-done
         Tests  2 failed | 18 skipped (20)
   ```
   The harm probe in the same run printed the pre-fix verdict: `child_status_at_enqueue=pending claimed_while_parent_running=true`.
5. **Restore.** `cp` the backup over M1, then:
   - `cmp` exit **0**;
   - `git diff --quiet HEAD -- <M1>` exit 0;
   - `COALESCE(p_run_at, now()), v_initial_status` present once in M1.
6. **Restored lane.** `down`, then `up`, exit 0. M1's `fanin-status-10param` NOTICE printed.
7. **P12 after the restore.** The same command exited 0:
   ```
   ✓ ... fan-in chain > P12: enqueue with parent_job_ids inserts row as done_pending_children
   ✓ ... fan-in chain > P12-harm: a parented child is not claimable before its parent is done and is released by the parent's mark-done
         Tests  2 passed | 18 skipped (20)
   ```

CALIBRATION-RED: p12-m1-status neuter=FAIL restored-cmp=0 restored=OK

The neutered bytes are in no commit. `git log --all -S 'IF false THEN  -- NEUTER'` returns nothing. The local stack is left UP with the restored M1 replayed, for the plans that reuse it.

## Deviations from Plan

1. **Scratch directory.** Backups and logs went to the session scratchpad, not `${TMPDIR}/gsd-164.9.1-03`. The exception is the Task 1 verify, which was run verbatim and used its own path. This has no effect on the result.
2. **Task 2 authored no commit, by design.** The neuter is temporary and the restore is byte-exact, so the tree is unchanged. The plan's `commits:` count is therefore 1.
3. **[Rule 1 - defect in the plan's verify] Task 2's verify cannot pass as written.** It runs vitest with `-t "P12"` and then requires `! grep -qE '\b[1-9][0-9]* (failed|skipped)\b'`. A `-t` filter always reports the file's other arms as skipped, so the verbatim run on the green, restored lane (`A=0 RC=0`, `Tests  2 passed | 18 skipped (20)`) printed no `P12-CALIBRATED`. The skip clause exists to catch a lane-down run, where every arm skips. The same verify was therefore re-run with the `-t "P12"` filter removed, everything else byte-identical, over the whole g10b file. Output: `Tests  20 passed (20)`, `✓ … P12: enqueue with parent_job_ids inserts row as done_pending_children`, `A=0 RC=0`, `P12-CALIBRATED`. This is strictly stronger, because no arm can be skipped. Plans 10 and 12 should use the unfiltered form if they re-run this verify.

No other deviations. No stubs. No new threat surface: the only database touched was the loopback local stack and throwaway pg-lane clusters.

## Commits

- `44bc7d93d` test(164.9.1-03): re-point the dedupe gate's twins at the fan-in status migration

## Self-Check: PASSED

- `44bc7d93d` exists on `feat/164.9.1-jobrpctruth`.
- `TWINS-OK` was printed by Task 1's verify.
- `P12-CALIBRATED` was printed by Task 2's verify after this file was written, in its unfiltered form (Deviation 3). The `-t "P12"` form printed nothing, because of the verify defect recorded in Deviation 3.
- `check-planning-hygiene` reported OK.
