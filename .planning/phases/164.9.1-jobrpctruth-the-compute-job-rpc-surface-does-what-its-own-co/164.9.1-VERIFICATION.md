---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
verified: 2026-09-25T20:28:17Z
status: human_needed
score: 8/8 must-haves verified on the local lane (0 present, behavior-unverified); 4 post-merge observations outstanding
verified_at_sha: 616b95aaf8b4255ec0667d8ed59cd24be022338a
drift_subjects:
  - supabase/migrations/20260924230827_fanin_initial_status_10param.sql
  - supabase/migrations/20260924233749_allocator_sync_restore_inflight_prefetch.sql
  - supabase/migrations/20260925071300_bridge_outcomes_invariant_comments.sql
  - supabase/schema/functions/_enqueue_compute_job_internal.sql
  - supabase/schema/functions/request_allocator_holdings_sync.sql
  - src/app/api/allocator/holdings/sync/route.ts
  - src/components/exchanges/AllocatorExchangeManager.tsx
  - scripts/restore-test-from-baseline.sh
  - scripts/test-only-normalize-analytics-url.sh
  - .github/workflows/test-restore-from-baseline.yml
  - scripts/live-db-execution-ledger.txt
  - scripts/live-db-execution-ledger.mjs
  - src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts
  - src/__tests__/request-allocator-holdings-sync-queued.test.ts
  - src/__tests__/match-decisions-xor-rls.test.ts
  - supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "FC-1 precondition, before the merge. Push the branch at its release head (plan 14), then count the PR's checks and bind every one to the head SHA."
    expected: "Every check is green at that SHA except exactly one pre-declared VAC-08 row in test-db-drift, `_enqueue_compute_job_internal/10 DRIFT`, whose TEST hash is ef92e4160b7ef27348c5a38ee362242b3ee7c8f879f745d606cb9968e7dceed7 (D-24). VAC-04 must report PROD hashes equal to the two prod-body-ack lines (ef92e416... for the ten-arg enqueue, 1a5a1959... for request_allocator_holdings_sync). Any other red blocks the merge."
    why_human: "The remote branch head is f6191aa6f, not 616b95aaf, so no CI run exists for the verified SHA. VAC-04 and VAC-08 read PROD and shared TEST, which no agent may query."
    owner: "orchestrator (plan 14), founder merges (FC-1)"
  - test: "Post-merge: watch supabase-migrate.yml apply-test and then apply for the merge commit, and the push-run test-db-drift."
    expected: "apply-test succeeds on shared TEST, with all three DO-block NOTICEs and no refusal. PROD apply succeeds after it (no reviewer gate). The push-run test-db-drift is green, SHA-bound, with no DRIFT row."
    why_human: "Observable only after merge. A merge of supabase/migrations/** auto-applies to TEST and then to PROD, so this is the founder's FC-1 act."
    owner: "founder (FC-1); orchestrator reads the runs"
  - test: "FC-3: dispatch test-restore-from-baseline.yml against shared TEST, preflight first, then restore."
    expected: "The Normalize script self-test step passes. The restore prints the `rearm:` note, and after the COMMIT the analytics_service_url row on TEST is the loopback sink. A preflight leaves the row unchanged."
    why_human: "It writes to a database other people's CI uses. D-18 says it is a founder checkpoint and not a completion gate. On disposable clusters, restore self-test arms 33 and 34 already prove the same behaviour."
    owner: "founder (FC-3)"
  - test: "After deploy, in the logged-in browser: soft-disconnect an allocator exchange key, then press Sync now on a key that another tab has disconnected."
    expected: "The row moves to Disconnected, shows 'This API key is disconnected. Reconnect it before syncing holdings.' next to its Reconnect button, and does not loop on 'click Sync now to retry'."
    why_human: "Copy and placement are a visual check. The 409 mapping and the state transitions are pinned by route.test.ts and AllocatorExchangeManager.test.tsx, which ran green here."
    owner: "orchestrator (visual UAT in the logged-in Chrome, per the founder's 2026-09-24 rule)"
---

# Phase 164.9.1: JOBRPCTRUTH Verification Report

**Phase goal:** The compute-job RPC surface does what its own comments and migrations say it does. A fan-in job actually reaches the fan-in state, and an in-flight collision can be told apart from a fresh enqueue.
**Verified:** 2026-09-25T20:28:17Z at `616b95aaf` (branch `feat/164.9.1-jobrpctruth`; `origin/main` `01dcf1cc7` is an ancestor)
**Status:** human_needed. Every must-have is proven by execution on the loopback lane. Four observations can only be made after the merge or on shared databases, and each one is listed with an owner.
**Re-verification:** No, this is the initial verification.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence (re-run by this verifier unless marked) |
|---|---|---|---|
| 1 | SC1: the harm was proven before the fix | ✓ VERIFIED | Plan 01 committed arm `P12-harm` (`163cb233c`) before M1 (`ee325bfee`). Its pre-fix verdict line was `child_status_at_enqueue=pending claimed_while_parent_running=true … child_status_after_parent_done=running`. I reproduced that exact verdict by neutering M1's INSERT on the lane (below). Plan 01 records the D-02 wording "mechanism demonstrated / production harm latent", which is honest because no production caller passes parents. |
| 2 | SC2: a job enqueued through the public wrapper with `parent_job_ids` lands in `done_pending_children`, proven by execution | ✓ VERIFIED | `P12` passed on the lane with M1 replayed by the lane's own loop. The harm probe prints `child_status_at_enqueue=done_pending_children claimed_while_parent_running=false parent_mark_done_ok=true child_status_after_parent_done=pending claimed_after_parent_done=true`. |
| 3 | SC3: the two overloads agree, or one stops existing, named by symbol and arity | ✓ VERIFIED (with the D-04 amendment) | M1 does `CREATE OR REPLACE public._enqueue_compute_job_internal(uuid, uuid, text, text, uuid[], text, jsonb, uuid, uuid, timestamptz)`, which is the arity the wrapper reaches. The seven-arg `(uuid, uuid, text, text, uuid[], text, jsonb)` is byte-unchanged. The snapshot shows `v_initial_status` computed and INSERTed. The two bodies agree for empty parents and for open parents. They differ on purpose for NULL, missing or failed_final parents, and when all parents are done. That difference is recorded in CONTEXT D-04 (amended), in the ROADMAP amendment line and in M1's header. The seven-arg cannot be called at all (42725, D-27, recorded in M1's header). |
| 4 | SC4: an in-flight collision can be told apart from a fresh enqueue at the caller | ✓ VERIFIED | The snapshot of `public.request_allocator_holdings_sync(uuid)` has the in-flight look-up, which returns `{already_inflight, next_attempt_at}` before the poll enqueue, and no dead handler around it. The live arms `returns {already_inflight, …}` and `returns {ok, job_id}` both passed. I neutered the look-up on the lane: `× returns {already_inflight …}` with `expected undefined to be true`, then 5/5 after the restore. |
| 5 | SC5: the 081 invariant question is answered in writing, and the arm asserts the current invariant | ✓ VERIFIED | D-13's answer is in CONTEXT, in M3's header and in the test header. M3 rewrites the table and column comments so they name `bridge_outcomes_allocator_match_decision_unique` and `bridge_outcomes_legacy_per_strategy_holding_when_md_null`, and its DO block printed its NOTICE on replay. Arms (a), (b) and (c) passed on the lane. `"23505" \|\|` disjunctions: 0. Plan 09 recorded three calibrations (not re-run by me). |
| 6 | SC6: there is a regression gate in the live-DB lane, calibrated neuter → RED → restore | ✓ VERIFIED | I ran this myself. I applied a scratch ten-arg body with `v_initial_status` replaced by `'pending'` in VALUES to the lane: `× P12 … expected 'pending' to be 'done_pending_children'`, `× P12-harm`, `Tests 2 failed \| 1 passed \| 18 skipped`. I re-applied the committed snapshot body: `Tests 21 passed (21)`, and the lane body contains `now()), v_initial_status` (`t`). The ledger no longer carries the P12 line (ENTRY 7/7), so a regression would fail `frontend-live-db-lane` as NEW. |
| 7 | REARM: a restore of shared TEST leaves `analytics_service_url` normalised, inside the restore transaction, single-sourced | ✓ VERIFIED (disposable clusters); live TEST observation is FC-3 | `build_transaction` calls `bash "$REARM_EMITTER" --emit-restore-sql`, checks its rc and the fragment's shape, then appends it. `REARM_EMITTER` is honoured only under the self-test seam. The restore self-test printed `37/37`, including arm 33 (restore commits the sink), arm 34 (preflight rolls back), arms 35 and 36 (emitter refusals commit nothing) and arm 37 (seam off). The normalize self-test printed `36 checks`. The workflow's restore job runs the normalize self-test before the restore. |
| 8 | D-23: the `api_key_disconnected` refusal (P0001) is restored, and the route maps it to 409 | ✓ VERIFIED | The snapshot does the ownership check, then `RAISE 'api_key_disconnected' USING ERRCODE 'P0001'`, then the look-up. Live arms passed: the refusal comes before the look-up, and a non-owner gets 42501 (no oracle). `route.ts` maps only `code === "P0001" && message === "api_key_disconnected"` to 409. The route and component suites passed, including the 409 arms. Plan 08 recorded two calibrations (not re-run by me). |

**Score:** 8/8 truths verified by execution on the loopback lane (0 present-but-behavior-unverified). The post-merge observations are listed under Human Verification. They are not counted as passed.

### Required Artifacts

| Artifact | Status | Details |
|---|---|---|
| `supabase/migrations/20260924230827_fanin_initial_status_10param.sql` (M1) | ✓ VERIFIED | It re-bases on 20260826150000 and re-issues REVOKE (PUBLIC, anon, authenticated) and GRANT (service_role). The COMMENT keeps `Phase 163 OPS-08`. It carries `prod-body-ack: ef92e416…`. The DO block is catalog-only. It replayed with its NOTICE. |
| `supabase/migrations/20260924233749_allocator_sync_restore_inflight_prefetch.sql` (M2) | ✓ VERIFIED | It re-bases on 076 and restores 067's look-up and 075's refusal. ACL: REVOKE from PUBLIC and anon, GRANT to authenticated. It carries `prod-body-ack: 1a5a1959…`. It replayed with its NOTICE. |
| `supabase/migrations/20260925071300_bridge_outcomes_invariant_comments.sql` (M3) | ✓ VERIFIED | Comments only, with a catalog-only DO block. It replayed with its NOTICE. |
| Function snapshots (2) | ✓ VERIFIED | `dump-sql-functions.ts --check` reports them current. |
| `route.ts` + `AllocatorExchangeManager.tsx` | ✓ VERIFIED | Wired. The 409 reaches the UI, which moves the row to Disconnected (component arms passed). |
| Restore and normalize scripts, workflow step | ✓ VERIFIED | See truth 7. |
| Ledger `.txt`/`.mjs` | ✓ VERIFIED | `# ENTRY_COUNT = 7`, `ENTRY_CEILING = 7`, 7 non-comment lines, 0 lines with the closed tokens. |

Exactly 3 migrations are added against `origin/main` (D-25). No existing migration is modified. None contains `cron.schedule`.

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `public.enqueue_compute_job` | ten-arg `_enqueue_compute_job_internal` | the wrapper's routing (unchanged), now reaching M1's body | WIRED: P12 passes through the public wrapper |
| `request_allocator_holdings_sync` | `route.ts` | RPC error `P0001`/`api_key_disconnected` → 409 | WIRED |
| `route.ts` 409 | `AllocatorExchangeManager` | `handleSync` / `handleReconnect` / `handleAddKey` read the 409 | WIRED (component arms) |
| `restore-test-from-baseline.sh build_transaction` | `test-only-normalize-analytics-url.sh --emit-restore-sql` | rc-checked, shape-checked concatenation before the ledger DDL | WIRED (arms 33 to 37) |
| `test-restore-from-baseline.yml` restore job | normalize `--self-test` | a new step before the restore | WIRED (wiring test green) |

### Behavioral Spot-Checks and Gate Re-runs (this verifier, verbatim tails)

**Lane + ledger** (`run.sh down`, `run.sh up`, `npm run test:live-db:ledger`, each under a 600 s timeout):
```
baseline-currency: carried=274 replay=3 marker-sha=match defects=0
baseline-replay: 3 migration(s) newer than the dump: 20260924230827_fanin_initial_status_10param.sql 20260924233749_allocator_sync_restore_inflight_prefetch.sql 20260925071300_bridge_outcomes_invariant_comments.sql
[local-stack] replayed 20260924230827_fanin_initial_status_10param.sql (ledger row written)
[local-stack] replayed 20260924233749_allocator_sync_restore_inflight_prefetch.sql (ledger row written)
[local-stack] replayed 20260925071300_bridge_outcomes_invariant_comments.sql (ledger row written)
[local-stack] up complete
UP_RC=0
[live-db-ledger] executed: 401 test(s) (394 passed, 7 failed), 81 skipped, 482 collected, across 49 file(s)
[live-db-ledger] ledger: 7 entr(y/ies) (see scripts/live-db-execution-ledger.txt)
[live-db-ledger] OK — the failing set is EXACTLY the ledger (7/7). Every corpus file executed; no arm outside the ledger failed; no ledger entry has gone stale.
LEDGER_RC=0
```

**The phase's three live-DB files on the lane:** `Test Files  3 passed (3)` · `Tests  35 passed (35)`. This includes P12, P12-harm, P12-dead-end, the three Queued arms, the non-owner no-oracle arm, and bridge_outcomes uniqueness (a), (b) and (c).

**Independent calibrations on the lane** (scratch bodies applied with `psql -1` after the loopback-DSN refusal check; the committed files were not touched):
```
P12 neuter (VALUES v_initial_status -> 'pending'):  Tests  2 failed | 1 passed | 18 skipped (21)   AssertionError: expected 'pending' to be 'done_pending_children'
P12 restored (snapshot body):                        Tests  21 passed (21)
M2 neuter (IF false AND v_next_attempt IS NOT NULL): Tests  1 failed | 4 passed (5)   × returns {already_inflight, next_attempt_at} when a live job exists
M2 restored (snapshot body):                         Tests  5 passed (5)
```

**Static gates:**
```
lint-migration-data-dependence self-test OK (5 refusals, red+green each.)                       rc=0
lint-migration-data-dependence: corpus 277 migration(s), 350 anonymous block(s); 83 refusal(s), 83 allowlisted, 0 NEW.   rc=0
SQL function snapshot is current (121 functions).                                               rc=0
test-only-normalize-analytics-url: self-test OK (36 checks)                                     rc=0
restore-test-from-baseline: self-test OK (37/37 arms — TEN refusals fire before any write …    rc=0
vitest (route.test, AllocatorExchangeManager.test, restore-test-from-baseline, test-restore-workflow-wiring, live-db-execution-ledger.contract): Test Files  5 passed (5) · Tests  256 passed (256)
vitest (name filters: allocator exchange restore-test-from-baseline test-restore-workflow-wiring live-db-execution-ledger): Test Files  28 passed | 1 skipped (29) · Tests  575 passed | 23 skipped (598)
tsc --noEmit -p .                                                                               rc=0
```
Unchanged against `origin/main` (`git diff --quiet` exit 0): the data-dependence baseline, the VAC-08 script, the refdata allowlist, and `scripts/mutation-runner/run.mjs`. `FILES_FLOOR = 50`, `ARMS_FLOOR = 449`, `WAIVED_CEILING = 0`. The lane was torn down afterwards (`down=0`).

**Not re-run by this verifier:** the full static vitest suite, the SQL corpus over all 77 files, and the narrowed mutation run. Plans 10 to 13 record those as green or load-only. I did not repeat them because the phase-owned subsets above cover every touched file.

### Probe Execution

No `scripts/*/tests/probe-*.sh` is declared by this phase. The restore and normalize self-tests are the phase's runnable checks, and both were run above.

### Requirements Coverage

| Requirement | Status | Evidence |
|---|---|---|
| `[164.9-FANIN-STATUS-NEVER-SET]` | ✓ SATISFIED (lane); PROD apply post-merge | Truths 1, 2, 3 and 6 |
| `[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]` | ✓ SATISFIED (lane); PROD apply post-merge | Truths 4, 5 and 8. The Queued arm was fixed in the RPC, not by teaching the test. The XOR arm was rewritten, not deleted. |
| `[164.9-TEST-ANALYTICS-URL-REARM]` | ✓ SATISFIED on disposable clusters; live TEST is FC-3 | Truth 7 |

No orphaned requirements. The `TODOS.md` entries are closed by plan 14 (release), which has not run yet.

### Anti-Patterns Found

No `TBD`, `FIXME` or `XXX` was added by the phase diff against `origin/main`, and no `TODO`, `HACK` or `PLACEHOLDER` either. No stubs.

ℹ️ Recorded limits, not gaps (each is written down and routed):
- a fan-in child whose parent is still open at enqueue and later ends `failed_final` stays stranded;
- the diamond-DAG 40P01 on M1's `FOR SHARE` lock;
- the pre-existing cascade 23505 on a second `match_decisions` delete.

All three are latent (D-02) or fail loudly. They are recorded in M1's header and routed to the ROADMAP `### Phase 164.5.2` "Routed here 2026-09-25" paragraph.
ℹ️ The normalize self-test runs only in the dispatch-only restore job, not on PRs. This was accepted in the ROADMAP amendment of 2026-09-25.
ℹ️ The D-11 prefetch race is accepted by design and recorded in M2's header.

### Human Verification Required

These are post-merge or shared-database observations. None is marked passed. Owners are listed in the frontmatter.

1. **FC-1 precondition.** The PR checks must be SHA-bound green at the release head, except the single pre-declared VAC-08 `_enqueue_compute_job_internal/10 DRIFT` row. VAC-04's PROD hashes must equal the two acks. The remote branch is at `f6191aa6f`, not the verified SHA, so no CI run exists yet.
2. **Post-merge apply.** `apply-test` and then the PROD `apply` must succeed, and the push-run `test-db-drift` must be green and SHA-bound. This is the founder's FC-1 act.
3. **FC-3.** A live restore dispatch against shared TEST. This is the founder's checkpoint and not a completion gate.
4. **Visual check** of the disconnected-key sentence and the row placement in the logged-in browser after deploy. The orchestrator owns it.

### Gaps Summary

No gaps. Every roadmap success criterion, the REARM requirement and D-23 are proven by execution on the loopback lane. I reproduced the two load-bearing calibrations (P12 and the Queued look-up) myself, neuter → RED → restore → GREEN. What is left is observable only after the merge or on shared TEST/PROD, and each item is listed above as human_needed with an owner. None was marked passed.

---

_Verified: 2026-09-25T20:28:17Z_
_Verifier: Claude (gsd-verifier)_
