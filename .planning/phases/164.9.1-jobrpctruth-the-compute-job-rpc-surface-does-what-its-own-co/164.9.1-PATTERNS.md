# Phase 164.9.1: JOBRPCTRUTH - Pattern Map

**Mapped:** 2026-09-24
**Files analyzed:** 22 new or modified files
**Analogs found:** 22 / 22. Every file has a real in-repo analog. Four of the analogs also carry an ANTI-pattern that must not be copied (flagged inline).

> 164.4.2 pins (ledger counts, lane image, job names, mutex-key counts) are re-measured by whichever of 164.4.2 / 164.9.1 merges second.
>
> ⚠️ **Line numbers below are read at this worktree's HEAD, which is NOT yet rebased onto 164.4.2** (`git merge-base --is-ancestor origin/feat/164.4.2-subsetsplit HEAD` is false). 164.4.2 changes three of the analog files: `scripts/restore-test-from-baseline.sh` and `scripts/live-db-execution-ledger.{txt,mjs}` (119 insertions, 32 deletions across the three). Every other analog file cited here is byte-identical between HEAD and `origin/feat/164.4.2-subsetsplit` (measured with `git diff --stat`). **Plans must cite by SYMBOL** (the `plan-anchor-verify` CI gate re-resolves `file:line` anchors). The line numbers are there to help a reader find things, and they are not anchors.

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `supabase/migrations/<ts1>_fanin_initial_status_10param.sql` (M1) | migration (SECDEF fn body) | CRUD (enqueue INSERT) | `supabase/migrations/20260826150000_destrict_enqueue_internal_10param.sql` | exact (it is the re-base source) |
| `supabase/migrations/<ts2>_allocator_sync_restore_inflight_prefetch.sql` (M2) | migration (SECDEF user-facing RPC) | request-response | `supabase/migrations/20260420103104_allocator_sync_queued_prefetch.sql` (067 prefetch) + `20260422122720_reconstruct_per_api_key_gate.sql` (076 body) + `20260422101911_api_keys_disconnected_at.sql` STEP 5 (075 refusal). The DO-block shape comes from M1's analog | exact (three re-base sources) |
| `supabase/migrations/<ts3>_bridge_outcomes_invariant_comments.sql` (M3) | migration (catalog comment only) | transform (catalog) | `supabase/migrations/20260823120000_revoke_api_keys_insert.sql` (`$comment$` and `$verify$` DO blocks) | role-match |
| `supabase/schema/functions/_enqueue_compute_job_internal.sql` | generated snapshot | file-I/O | itself (`@generated`) | exact (regenerate, never hand-edit) |
| `supabase/schema/functions/request_allocator_holdings_sync.sql` | generated snapshot | file-I/O | itself (`@generated`) | exact |
| `supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql` | SQL gate (mutation twins) | batch | itself: `RED-UNDER-SETUP` + `RED-UNDER-M` lines. Precedent in `scripts/mutation-runner/run.mjs` above `ARMS_FLOOR` (the "RE-POINTED all 27" note) | exact |
| `src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts` (P12 prose + harm-probe arm) | test (live-DB) | CRUD + RPC | same file: arms P12, P15, P7 | exact |
| `src/__tests__/request-allocator-holdings-sync-queued.test.ts` | test (live-DB) | request-response | same file + `src/lib/test-helpers/live-db.ts` `signInAsTestUser` (throws instead of skipping) | exact |
| `src/__tests__/match-decisions-xor-rls.test.ts` | test (live-DB) | CRUD (constraint firing) | same file: arms "two different holdings" / "same holding_ref" | exact |
| `src/app/api/allocator/holdings/sync/route.ts` | route handler | request-response | same file's `error.code === "42501"` branch | exact |
| `src/app/api/allocator/holdings/sync/route.test.ts` | test (unit, mocked) | request-response | same file's test "5. RPC raises SQLSTATE 42501 → 403" | exact |
| `scripts/live-db-execution-ledger.txt` | config (shrink-only ledger) | batch | commit `31d9c1fdf` on `origin/feat/164.4.2-subsetsplit` (it removed 8 lines and moved the ceiling from 18 to 10) | exact |
| `scripts/live-db-execution-ledger.mjs` | utility (gate) | batch | same file: `ENTRY_CEILING` and `K3_ARMS`, as edited by `31d9c1fdf` | exact |
| `src/__tests__/live-db-execution-ledger.contract.test.ts` | test (static ratchet) | batch | itself. Expect NO edit: it re-derives `ENTRY_COUNT` and `ENTRY_CEILING` equality | exact (verify-only) |
| `scripts/test-only-normalize-analytics-url.sh` (shared emitter + self-test cases) | utility (ops script) | request-response (single guarded txn) | same file: `build_sql()` and `self_test()` | exact |
| `scripts/restore-test-from-baseline.sh` (fragment concatenation + new arms) | utility (destructive ops script) | batch (one atomic txn) | same file: the refdata replay block in `build_transaction` (extractor → file → `cat >> "$out"`), `arm_wrongstate_red` / `arm_wrongstate_green` | exact |
| `scripts/restore-test-from-baseline-fixtures/baseline-fixture.sql` | test fixture (schema) | file-I/O | same file: `public.fx_keep` + `fx_keep_kind_check` | exact |
| `scripts/restore-test-from-baseline-fixtures/migrations/<new or fixture_c>.sql` | test fixture (seed) | file-I/O | `scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql` | exact |
| `scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt` | test fixture (allowlist) | config | same file (single TAB line for `public.fx_keep`) | exact |
| `src/__tests__/restore-test-from-baseline.test.ts` | test (static pins) | transform (source text) | same file: `describe("… L-01: the reference-data replay's position")`, `"… the arm ratchet"`, `"… the MODE picks the terminator"` | exact |
| `docs/runbooks/test-analytics-url.md` §7, `supabase/tests/test_analytics_service_settings_and_vault_tick.sql` D1 header | docs / SQL gate prose | n/a | the prose itself (the "RE-ARMS" sentences) | exact (prose-only) |
| `src/__tests__/test-restore-workflow-wiring.test.ts` | test (static pins) | transform | itself | verify-only (research: no workflow change, so no edit expected) |

---

## Pattern Assignments

### M1 — `supabase/migrations/<ts1>_fanin_initial_status_10param.sql` (migration, CRUD)

**Analog:** `supabase/migrations/20260826150000_destrict_enqueue_internal_10param.sql`. Take the body VERBATIM and change only the status column. Re-run the re-base grep at execution time (D-05).

**House-style header (analog lines 177-182, 200)** — no BEGIN/COMMIT, `SET LOCAL`, single-literal RAISE:
```sql
-- Transaction style: NO explicit BEGIN/COMMIT — Supabase wraps each migration
-- in an implicit transaction. SET LOCAL lock_timeout applies to that wrap. ...
-- Every RAISE format string below is a SINGLE literal (Phase 85 invariant #21 —
-- no '||' concatenation inside a RAISE format slot).
SET LOCAL lock_timeout = '3s';
```

**Signature, schema-qualified (analog lines 242-268).** Keep the "⚠️ SCHEMA-QUALIFIED DELIBERATELY" note:
```sql
CREATE OR REPLACE FUNCTION public._enqueue_compute_job_internal(
  p_strategy_id     UUID,
  p_portfolio_id    UUID,
  p_kind            TEXT,
  p_idempotency_key TEXT,
  p_parent_job_ids  UUID[],
  p_exchange        TEXT,
  p_metadata        JSONB,
  p_allocator_id    UUID DEFAULT NULL,
  p_api_key_id      UUID DEFAULT NULL,
  p_run_at          TIMESTAMPTZ DEFAULT NULL
)
RETURNS UUID
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public, pg_catalog
AS $$
```

**The ONLY body delta.** The status block comes from the seven-arg in `20260716090000_retire_compute_analytics_kind_rpc_guard.sql` (declared at :66, computed at :94-96, INSERTed at :131). The INSERT to change is analog lines 335-348:
```sql
-- analog (current) INSERT — status omitted, column DEFAULT 'pending' wins:
  INSERT INTO compute_jobs (
    strategy_id, portfolio_id, allocator_id, api_key_id,
    kind, parent_job_ids, idempotency_key, exchange, metadata,
    next_attempt_at
  )
  VALUES (
    p_strategy_id, p_portfolio_id, p_allocator_id, p_api_key_id,
    p_kind, COALESCE(p_parent_job_ids, '{}'::uuid[]), p_idempotency_key,
    p_exchange, p_metadata,
    COALESCE(p_run_at, now())
  )
  ON CONFLICT DO NOTHING
  RETURNING id INTO v_new_id;
```
Add `v_initial_status TEXT;` to DECLARE. Add the `IF p_parent_job_ids IS NOT NULL AND array_length(...) ...` block (RESEARCH "Code Examples: M1 body delta"). Append `status` / `v_initial_status` to the column and VALUES lists.

**Everything that MUST stay byte-identical.** Analog lines 274-333 cover the XOR guard, the `p_kind` NULL guard, the retired-kind reject and the optimistic look-up. Lines 350-434 cover the four plain `SELECT id INTO v_new_id` re-reads and the `serialization_failure` RAISE.
- ⛔ The strategy-arm optimistic look-up (analog lines 302-307) must stay byte-identical, including the 5-space `WHERE` indent. It is the `find` string of mutation twins B1/B2 (see the dedupe-gate section below).
- ⛔ No comment anywhere in M1 may contain that `find` text, or `occurrences: 1` breaks.
- ⛔ Do not add a fifth `SELECT id INTO v_new_id`. The destrict gate's Part 2 counts exactly 4 (`c_expected_arms`), and so does M1's own arm (c2).

**ACL re-convergence (analog lines 460-468).** Copy it verbatim with its ⛔ note:
```sql
REVOKE ALL ON FUNCTION public._enqueue_compute_job_internal(
  uuid, uuid, text, text, uuid[], text, jsonb,
  uuid, uuid, timestamptz
) FROM PUBLIC, anon, authenticated;

GRANT EXECUTE ON FUNCTION public._enqueue_compute_job_internal(
  uuid, uuid, text, text, uuid[], text, jsonb,
  uuid, uuid, timestamptz
) TO service_role;
```

**COMMENT with the revert-discriminator phrase (analog lines 509-521).** Carry `Phase 163 OPS-08` forward and append one sentence about the status (D-06):
```sql
COMMENT ON FUNCTION public._enqueue_compute_job_internal(
  uuid, uuid, text, text, uuid[], text, jsonb,
  uuid, uuid, timestamptz
) IS
  'Private shared implementation of the idempotent enqueue pattern. ... '
  '... (Phase 163 OPS-08, parity with the '
  '7-param overload''s mig 109 P3 fix).';   -- + one sentence: computes and INSERTs the initial status
```

**Catalog-only self-verify DO block (analog lines 589-872).** Copy the whole DO block and its arms (a)-(h) as they stand, and add the new status copy-check arms (RESEARCH Q5). The pieces to reuse:
```sql
  v_oid10        oid := to_regprocedure(
    'public._enqueue_compute_job_internal(uuid, uuid, text, text, uuid[], text, jsonb, uuid, uuid, timestamptz)'
  );
  ...
  v_body10 := regexp_replace(regexp_replace(v_fn10, '/\*.*?\*/', '', 'gs'), '--.*', '', 'gn');
  IF v_body7 IS NULL OR v_body10 IS NULL THEN
    RAISE EXCEPTION '... Refusing to report compliance on an unread body.';
  END IF;
  ...
  SELECT count(*) INTO v_n FROM regexp_matches(v_body10, c_plain_re, 'g');   -- catalog-derived, linter-clean
  ...
  PERFORM public._assert_no_public_execute(c_sig10);
  ...
  v_comment10 := coalesce(obj_description(v_oid10, 'pg_proc'), '');
  IF strpos(v_comment10, c_applied_marker) = 0 THEN RAISE EXCEPTION '...'; END IF;
```
- Label the new arms as COPY-CHECKS. The analog's "⚠️ WHAT THESE ARMS ACTUALLY PROVE" block (lines 551-565) is the wording to reuse.
- Rename the RAISE prefix from `destrict-enqueue-10param:` to this migration's own tag.
- ⚠️ Keep arm (h)'s marker constant `'Phase 163 OPS-08'`. It is copied FROM `supabase/tests/test_enqueue_internal_destrict.sql` `c_applied_marker`.

**Header content M1 must add (D-27, Pitfall 3):**
- the seven-arg overload cannot resolve (42725);
- `enqueue_compute_portfolio_job` is its only caller and is broken but latent;
- stranded-DPC-child limit.
Also add `-- prod-body-ack:` lines, formatted as in `supabase/migrations/20260911120000_vault_tick_hardening.sql` lines 56-102 (next block).

**VAC-04 ack block (copy the shape from `20260911120000_vault_tick_hardening.sql` lines 56-102):**
```sql
-- VAC-04 ACKNOWLEDGEMENT — the PROD body this CREATE OR REPLACE overwrites
--   node scripts/sql-body-normalize.mjs --diff-bodies \
--     supabase/schema/functions/<fn>.sql <origin/main's copy>
-- ⭐ THE ACKED HASH IS THE `live` COLUMN OF --diff-bodies, NOT `--hash` OF THE
-- SNAPSHOT FILE.
-- prod-body-ack: <live-hash, re-derived at execution>
-- ⚠️ ... EARNED only if VAC-04 on the PR reports that SAME hash for PROD.
```

---

### M2 — `supabase/migrations/<ts2>_allocator_sync_restore_inflight_prefetch.sql` (migration, request-response)

**Re-base source (LATEST body):** `supabase/migrations/20260422122720_reconstruct_per_api_key_gate.sql` lines 47-122 (076).

**Keep verbatim from 076** — DECLARE, auth gate, ownership check (lines 53-71), then the reconstruct gate and its benign handler, the UPDATE and the RETURN (lines 96-120, D-12):
```sql
  SELECT EXISTS (
    SELECT 1 FROM compute_jobs
    WHERE api_key_id = p_api_key_id
      AND kind = 'reconstruct_allocator_history'
      AND status IN ('done','pending','running','done_pending_children')
  ) INTO v_prior_reconstruct;

  IF NOT v_prior_reconstruct THEN
    BEGIN
      PERFORM enqueue_compute_job(
        p_strategy_id     := NULL,
        p_kind            := 'reconstruct_allocator_history',
        p_idempotency_key := 'reconstruct-alloc-' || p_api_key_id::text || '-initial',
        p_api_key_id      := p_api_key_id
      );
    EXCEPTION WHEN unique_violation THEN
      NULL; -- racing first-connect call landed first; benign
    END;
  END IF;

  UPDATE api_keys SET sync_status = 'syncing' WHERE id = p_api_key_id;
  RETURN jsonb_build_object('ok', true, 'job_id', v_job_id);
```

**Restore from 075 (`20260422101911_api_keys_disconnected_at.sql` STEP 5, lines 297-322) — D-23.** Put it after the ownership check and BEFORE the prefetch:
```sql
  v_disconnected   TIMESTAMPTZ;
  ...
  SELECT user_id, disconnected_at INTO v_owner, v_disconnected
    FROM api_keys
    WHERE id = p_api_key_id;
  IF v_owner IS NULL OR v_owner <> v_uid THEN
    RAISE EXCEPTION 'api_key_not_found_or_not_owned'
      USING ERRCODE = '42501';
  END IF;

  -- Migration 075: reject sync on soft-disconnected keys.
  IF v_disconnected IS NOT NULL THEN
    RAISE EXCEPTION 'api_key_disconnected'
      USING ERRCODE = 'P0001';
  END IF;
```

**Restore from 067 (`20260420103104_allocator_sync_queued_prefetch.sql` lines 68-98).** It REPLACES 076's lines 73-94 (the dead `BEGIN … EXCEPTION WHEN unique_violation` around the poll enqueue):
```sql
  SELECT next_attempt_at INTO v_next_attempt
    FROM compute_jobs
    WHERE api_key_id = p_api_key_id
      AND kind = 'poll_allocator_positions'
      AND status IN ('pending', 'running', 'done_pending_children')
    ORDER BY next_attempt_at DESC
    LIMIT 1;

  IF v_next_attempt IS NOT NULL THEN
    RETURN jsonb_build_object(
      'already_inflight', true,
      'next_attempt_at', v_next_attempt
    );
  END IF;

  v_job_id := enqueue_compute_job(
    p_strategy_id := NULL,
    p_kind        := 'poll_allocator_positions',
    p_api_key_id  := p_api_key_id
  );
```
Also carry 067's race-window comment (lines 90-93) into M2's header as the D-11 accepted limit.

**ACL (076 lines 127-128).** Schema-qualify it and give the full signature:
```sql
REVOKE ALL ON FUNCTION public.request_allocator_holdings_sync(uuid) FROM PUBLIC, anon;
GRANT EXECUTE ON FUNCTION public.request_allocator_holdings_sync(uuid) TO authenticated;
```
Do not revoke service_role.

**Self-verify DO block: use M1's analog SHAPE, NOT 076's or 067's.** Arms are listed in RESEARCH Q5 "M2 arms". Use `to_regprocedure('public.request_allocator_holdings_sync(uuid)')` and a comment-stripped `pg_get_functiondef`. Read `search_path=public, pg_catalog` from `proconfig`. Use `_assert_no_public_execute` and the `has_function_privilege` anon/authenticated checks. Assert **exactly one** `EXCEPTION WHEN unique_violation` (count with `regexp_matches`). Check that the prefetch position comes before the `enqueue_compute_job(` poll call. Add the `api_key_disconnected` refusal arm.

**⛔ Anti-patterns in the re-base sources — do NOT copy:**
- 076 lines 40-42 and 164: `BEGIN;` / `SET lock_timeout` (not LOCAL) / `COMMIT;`. New files use neither (house style since 20260826150000).
- 076 lines 47 and 124: an unqualified `CREATE OR REPLACE FUNCTION request_allocator_holdings_sync` and `COMMENT ON FUNCTION request_allocator_holdings_sync IS` with no signature.
- 076 lines 137-158: `prosrc LIKE` on the RAW body. Comments can satisfy it, the T-163-16 class.
- 067 lines 135-137: `v_body LIKE '%EXCEPTION WHEN unique_violation%'` → RAISE. M2 KEEPS one handler (D-12), so this arm would abort M2's own apply.
- **067 lines 143-210 (the STEP 3 "functional probe" DO block).** It INSERTs into `auth.users`, `api_keys` and `compute_jobs`, calls the RPC and RAISEs on the result. That is a data-writing, data-reading DO block, forbidden by D-07 / `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]` and refused by `scripts/lint-migration-data-dependence.mjs`. Execution proof belongs in the live-DB Queued arm.

---

### M3 — `supabase/migrations/<ts3>_bridge_outcomes_invariant_comments.sql` (migration, catalog)

**Analog:** `supabase/migrations/20260823120000_revoke_api_keys_insert.sql` — the `$verify$` block (lines 240-310) and its `col_description` read (lines 283-306).

**Stale texts being replaced** (quote them in the header, and never repeat the "strict superset" wording):
- `20260421154029_match_decisions_original_holding_ref.sql` lines 93-98: `'... Enables the widened bridge_outcomes_unique_per_strategy_holding index. ...'`
- `20260418060747_bridge_outcomes.sql` lines 97-102: `'One row per (allocator_id, strategy_id) enforced by unique index.'`

**Write shape.** Use a plain literal statement, as the ancestors do. Do NOT use the analog's read-modify-append `EXECUTE format(...)`. That idiom exists to preserve other migrations' markers inside one comment, and neither comment here carries a marker:
```sql
COMMENT ON TABLE public.bridge_outcomes IS '...';   -- names both current constraints (D-13)
COMMENT ON COLUMN public.bridge_outcomes.original_holding_ref IS '...';
```

**Verify block pattern (analog lines 283-306):**
```sql
  SELECT col_description('public.api_keys'::regclass, attnum) INTO v_comment
    FROM pg_attribute
   WHERE attrelid = 'public.api_keys'::regclass AND attname = 'exchange';

  IF v_comment NOT LIKE '%20260823120000_revoke_api_keys_insert%' THEN
    RAISE EXCEPTION 'Migration 20260823120000 POST-VERIFY FAILED: ... Rolling back.'
      USING ERRCODE = 'data_exception';
  END IF;
  IF v_comment LIKE '%deliberately still uses this column%' THEN   -- stale-text ABSENCE arm
    RAISE EXCEPTION '...';
  END IF;
```
For M3:
- read `col_description('public.bridge_outcomes'::regclass, attnum)` for `original_holding_ref`, and `obj_description('public.bridge_outcomes'::regclass, 'pg_class')`;
- presence arms: both `bridge_outcomes_allocator_match_decision_unique` and `bridge_outcomes_legacy_per_strategy_holding_when_md_null`;
- absence arm: `bridge_outcomes_unique_per_strategy_holding`;
- guard against a NULL comment first (the analog does this at lines 191-195).
All of these are catalog reads (`pg_attribute`, `col_description`, `obj_description`), so they are linter-clean. Use `SET LOCAL lock_timeout = '3s';` and no BEGIN/COMMIT.

---

### Snapshots — `supabase/schema/functions/{_enqueue_compute_job_internal,request_allocator_holdings_sync}.sql`

- **Pattern:** header `-- @generated by scripts/dump-sql-functions.ts — DO NOT EDIT BY HAND.`
- **Command:** `npm run schema:functions` (`package.json` script `schema:functions` = `tsx scripts/dump-sql-functions.ts`).
- **Verify:** `npx tsx scripts/dump-sql-functions.ts --check`.
- The snapshot captures the comment block immediately above each `CREATE` (Pitfall 11). Keep M1/M2's pre-CREATE comment short and free of the twin `find` string.
- The `request_allocator_holdings_sync.sql` snapshot's `-- source migration:` line will move from `20260422122720_…` to M2.

---

### `supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql` (SQL gate, twins)

**Current pins** (both reference `20260826150000`):
- `-- RED-UNDER-SETUP: {"apply":[ … ,"supabase/migrations/20260826150000_destrict_enqueue_internal_10param.sql"]}` (line 90). The last element is the superseded migration.
- `-- RED-UNDER-M: {"arm":"B1","apply":[{"kind":"edit","file":"supabase/migrations/20260826150000_destrict_enqueue_internal_10param.sql","find":"    SELECT id INTO v_existing_id\n      FROM compute_jobs\n     WHERE strategy_id = p_strategy_id\n       AND kind = p_kind\n       AND status IN ('pending', 'running', 'done_pending_children')", … ,"occurrences":1}, {…20260416125430…}]}` (line 174).
- The B2 twin at line 228 has the same `file` and `find`.
- The prose `RED-UNDER:` comments at lines 159-164 and 216-224 name `20260826150000`.

**Edit pattern — one commit** (precedent: `scripts/mutation-runner/run.mjs`, the note above `ARMS_FLOOR` that begins "Plan 04 RE-POINTED all 27 pre-existing edit-kind twins … at the superseding migration"):
1. Append M1's path to the `RED-UNDER-SETUP` apply list.
2. Change `"file"` in the B1 first edit and in B2 to M1's path. Keep `find`, `replace` and `occurrences` byte-identical. B1's second edit (on `20260416125430`) stays as it is.
3. Update the prose `RED-UNDER:` lines to name M1.
4. Re-observe both twins `RED (identity ok)` with `node scripts/mutation-runner/run.mjs` (or 164.4.2's subset for this file).

`FILES_FLOOR` (49), `ARMS_FLOOR` (426) and `WAIVED_CEILING` (0) do not move. Read them by symbol with `grep -nE '^export const (FILES_FLOOR|ARMS_FLOOR|WAIVED_CEILING)' scripts/mutation-runner/run.mjs`.

---

### `src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts` (live-DB test)

**Imports (lines 30-40):**
```typescript
import { describe, it, expect } from "vitest";
import { type SupabaseClient } from "@supabase/supabase-js";
import {
  HAS_LIVE_DB,
  createLiveAdminClient,
  createTestUser,
  cleanupLiveDbRow,
  advertiseLiveDbSkipReason,
} from "@/lib/test-helpers/live-db";
```

**Helpers to reuse, not re-declare (lines 78-133):** `mintClaimToken()`, `seedStrategy(admin, userId, marker)`, `insertComputeJob(admin, fields)` and `fetchJob(admin, jobId)`.

**Running-parent seed with a token (P15, lines 749-759)** — the harm probe's parent:
```typescript
const tokenP1 = mintClaimToken();
const p1 = await insertComputeJob(admin, {
  strategy_id: strategyA,
  kind: "sync_trades",
  status: "running",
  attempts: 1,
  max_attempts: 3,
  claimed_at: new Date().toISOString(),
  claimed_by: "test-worker-p1",
  claim_token: tokenP1,
});
```

**Wrapper enqueue with parents (P12, lines 692-704)** — the probe's child:
```typescript
const { data: newId, error } = await admin.rpc(
  "enqueue_compute_job",
  {
    p_strategy_id: strategyId,
    p_kind: "reconcile_strategy",
    p_idempotency_key: null,
    p_parent_job_ids: [parentId],
  } as never,
);
expect(error).toBeNull();
const child = await fetchJob(admin, newId as unknown as string);
```

**Claim call and filtering to OWN rows (P7, lines 272-297).** For the probe, switch to `claim_compute_jobs_with_priority` and ALWAYS pass `p_kind_include` (Pitfall 6: PROD has a 2-arg overload, so a 2-named-arg call hits the same 42725 shape):
```typescript
admin.rpc("claim_compute_jobs", { p_batch_size: 5, p_worker_id: "skip-locked-A" } as never)
...
const ourClaimedA = claimedA.filter((id) => jobIds.includes(id));
```

**Mark-done with token (P15, lines 783-796):**
```typescript
await admin.rpc("mark_compute_job_done", { p_job_id: p1, p_claim_token: tokenP1 } as never);
let cRow = await fetchJob(admin, c);
expect(cRow.status).toBe("done_pending_children");
```

**Cleanup (P12, lines 725-730):** `finally { await cleanupLiveDbRow(admin, { userIds: [userId], strategyIds: [strategyId] }); }`.

**P12 prose:** lines 708-723 ("⛔ THIS ARM IS RED ON PURPOSE AND IS LEFT RED…") become false once M1 lands. Rewrite them to cite M1, and keep the assertion `expect(child.status).toBe("done_pending_children")` at line 724 unchanged. The harm-probe verdict must use D-02's exact wording: "mechanism demonstrated / production harm latent".

---

### `src/__tests__/request-allocator-holdings-sync-queued.test.ts` (live-DB test)

**Existing structure to keep:**
- `seedApiKey` (lines 42-62) and `pinInflightJob(admin, apiKeyId, secondsFromNow)` (lines 64-88);
- the two arms at lines 116-253;
- the `already_inflight` / `next_attempt_at` / `sync_status` assertions (lines 146-172, 217-232).

**⛔ Vacuous early return to REMOVE (Pitfall 8)** — lines 139-140 and 210-211:
```typescript
const authed = await createAuthedClient(email, password);
if (!authed) return; // password-grant disabled — graceful skip
```
Two replacement patterns:
- (a) Make `createAuthedClient` (lines 90-114) throw instead of returning `null`.
- (b) Use the repo helper, which already throws. `src/lib/test-helpers/live-db.ts` `signInAsTestUser` (lines 102-125):
```typescript
const client = createClient(LIVE_DB_URL!, anonKey);
const { error } = await client.auth.signInWithPassword({ email, password: LIVE_DB_TEST_PASSWORD });
if (error) throw error;
```
Precedent for an inline throw: `src/__tests__/portfolio-strategies-alias-rls.test.ts` line 81, `throw new Error(\`Sign-in A failed: ${aSignIn.error?.message}\`);`.

The header (lines 1-28) must record that 067's prefetch was lost at 070, carried through 076, and restored by M2. Add a D-23 arm (a disconnected key gets `P0001` `api_key_disconnected` and no `already_inflight`) if the plan puts it here. It uses the same seed plus an admin UPDATE setting `disconnected_at`.

---

### `src/__tests__/match-decisions-xor-rls.test.ts` (live-DB test)

**Fixture and cleanup scaffold to keep (lines 40-104):** stable synthetic strategy UUIDs, `beforeAll` seeding `strategies` + `strategy_analytics`, and `createdMatchDecisionIds` / `createdBridgeOutcomeIds` cleaned up in dependency order in `afterAll`.

**Insert shapes to reuse (lines 254-322):** `match_decisions` insert with `kind: "bridge_recommended"`, `decided_by`, and exactly one `original_*`; then `bridge_outcomes` insert with `match_decision_id`, `kind: "allocated"`, `percent_allocated`, `allocated_at`, followed by `.select("id, original_holding_ref").single()` and a push onto `createdBridgeOutcomeIds`.

**The decorative disjunction to REPLACE (lines 381-387 and 459-465):**
```typescript
expect(boErr2).not.toBeNull();
expect(
  boErr2?.code === "23505" ||
    boErr2?.message?.includes(
      "bridge_outcomes_unique_per_strategy_holding",
    ),
).toBe(true);
```
Replace it with a conjunction against the constraint that actually fires (RESEARCH Q7 table):
```typescript
expect(boErr2?.code).toBe("23505");
expect(boErr2?.message).toContain("bridge_outcomes_allocator_match_decision_unique");
```
- The arm at lines 327-390 (same `md1`) is re-pointed to `…_allocator_match_decision_unique`.
- The arm at lines 392-468 (two different decisions `mdS1` / `mdS2`) becomes D-14 (a): both succeed.
- A new shape (c) arm covers `match_decision_id: null` with `strategy_id` set, twice. It expects `23505` and a message containing `bridge_outcomes_legacy_per_strategy_holding_when_md_null`, and it tracks those rows in `createdBridgeOutcomeIds`.

Header lines 1-18 get D-13's verbatim written answer and cite 081 + 083. The `describe` title "widened UNIQUE" is a K3 token (`widened UNIQUE:` in `K3_ARMS`). Once the arms pass, that token is removed from the ledger script. Renaming the describe block is allowed but not required.

---

### `src/app/api/allocator/holdings/sync/route.ts` (route, request-response)

**Analog:** the same file's error branch (lines 67-87):
```typescript
if (error) {
  if (error.code === "42501") {
    return NextResponse.json(
      { error: "API key not found or not owned by you" },
      { status: 403, headers: NO_STORE_HEADERS },
    );
  }
  console.error(
    `[allocator/holdings/sync] RPC failed for user ${user.id} key ${api_key_id}:`,
    error,
  );
  return NextResponse.json(
    { error: "Could not start sync. Try again in a moment." },
    { status: 500, headers: NO_STORE_HEADERS },
  );
}
```
- **D-23 addition:** a sibling branch BEFORE the generic 500 that matches `error.code === "P0001" && error.message === "api_key_disconnected"` (key on BOTH, because `P0001` is the generic `RAISE EXCEPTION` class). It returns `{ status: 409, headers: NO_STORE_HEADERS }` with a user-safe sentence.
- Keep `NO_STORE_HEADERS` on every response.
- Keep the header docblock (lines 9-38) truthful. Its line 20 claim "(on 23505 unique_violation) returns the already-inflight shape" and the comment at lines 57-60 describe the dead handler, so they should name the M2 prefetch.
- The route passes JSONB through verbatim (line 106). Do not rebuild the object.
- AGENTS.md: read the relevant `node_modules/next/dist/docs/` guide before editing route code. No Next.js API surface changes here: `withAuth`, `NextResponse.json` and the zod `safeParse` are already in use.

### `src/app/api/allocator/holdings/sync/route.test.ts` (unit test)

**Analog:** test "5. RPC raises SQLSTATE 42501 → 403" (lines 149-171). Copy its shape for the 409 arm:
```typescript
mockRpc.mockResolvedValueOnce({
  data: null,
  error: { code: "42501", message: "api_key_not_found_or_not_owned" },
});
const consoleSpy = vi.spyOn(console, "error").mockImplementation(() => {});
const { POST } = await import("./route");
const res = await POST(makeReq({ api_key_id: TEST_API_KEY_ID }));
expect(res.status).toBe(403);
const body = await res.json();
expect(body.error).toContain("not found or not owned");
consoleSpy.mockRestore();
```
The mocking scaffold (lines 37-81) is `vi.hoisted` `mockRpc`, the `@/lib/supabase/server` mock, `server-only` / `@/lib/audit` / `@/lib/csrf` mocks and `makeReq`. Reuse it unchanged.

Two things make the new arm able to fail:
- `code: "P0001", message: "api_key_disconnected"` must give 409. Removing the branch then yields 500 and the test goes RED.
- Add a guard case: `code: "P0001"` with a DIFFERENT message still gives 500, so the mapping cannot be widened to all of `P0001`.
- Also assert that `mockLogAuditEvent` was not called (the audit fires on success only, lines 199-218).

---

### `scripts/live-db-execution-ledger.txt` + `scripts/live-db-execution-ledger.mjs` (shrink-only ledger)

**Analog:** commit `31d9c1fdf` on `origin/feat/164.4.2-subsetsplit`, "reconcile the live-DB execution ledger - 8 entries deleted … ceiling 18 to 10". Its `.mjs` diff is the template:
```javascript
// SHRUNK 2026-09-23 (Phase 164.4.2 plan 08 checkpoint RED, CI run 35914559318
// attempt 2): 18 -> 10, in the commit that deleted the eight entries the lane's
// default-ACL reset and non-public objects stopped reproducing.
export const ENTRY_CEILING = 10;
```
**In the same commit:**
- Delete the three TAB lines: the `compute-jobs-audit-2026-05-07-g10b.test.ts … P12:` line, the `match-decisions-xor-rls.test.ts … widened UNIQUE:` line, and the `request-allocator-holdings-sync-queued.test.ts … Queued path` line (HEAD lines 144, 146 and 147; re-locate them on the rebased tree).
- Lower `# ENTRY_COUNT = N` and `export const ENTRY_CEILING = N` by 3, with a dated `// SHRUNK …` note. Read N from the rebased tree: 10 → 7 on 164.4.2, 18 → 15 on HEAD. Never copy the number from this file.
- Remove the three token-keyed `K3_ARMS` entries (`token: "P12:"`, `"widened UNIQUE:"`, `"Queued path"`). Keep the 164.4.2 evidence-keyed wizard entry.
- Update the prose that counts them: the `K3-migration-or-invariant-decision  N entries` block in the `.txt` header, and "(three keyed by an arm token, and since 2026-09-23 one module keyed by its measured refusal)" in the `.mjs` header.

The public-repo rule stated in the ledger header ("VERDICTS, NEVER SPECIMENS") applies to any prose added.

**Contract test** `src/__tests__/live-db-execution-ledger.contract.test.ts` needs no edit. It pins `ENTRY_COUNT` = line count, and `ENTRY_CEILING` `toBe(entries)` (stale-high is RED). Pitfall 9 is about intermediate commits: remove all three lines together with the last fix, or remove one line and lower the ceiling by 1 per fix commit.

---

### `scripts/test-only-normalize-analytics-url.sh` (shared emitter)

**Analog:** the same file's `build_sql()` (lines 112-200) and its guard order: marker → mutex → no-op → write → `ROW_COUNT = 1`.

**Marker guard to reuse verbatim in the emitted fragment (lines 136-153):**
```sql
  SELECT shobj_description(d.oid, 'pg_database')
    INTO v_marker
    FROM pg_database d
   WHERE d.datname = current_database();

  IF v_marker IS NULL OR btrim(v_marker) = '' THEN
    RAISE EXCEPTION 'REFUSED: the database identity marker is ABSENT. ...';
  END IF;
  IF v_marker ~* c_refuse THEN
    RAISE EXCEPTION 'REFUSED: the database identity marker names PRODUCTION. ...';
  END IF;
  IF v_marker !~* c_expect THEN
    RAISE EXCEPTION 'REFUSED: the database identity marker does not name TEST. ...';
  END IF;
```
**Write and rowcount guard (lines 186-193):**
```sql
  UPDATE public.system_settings
     SET value = c_sink
   WHERE key = 'analytics_service_url';
  GET DIAGNOSTICS v_n = ROW_COUNT;
  IF v_n <> 1 THEN
    RAISE EXCEPTION 'REFUSED: the update touched % row(s), expected exactly 1. ...', v_n;
  END IF;
```
**Shape-only verdict (lines 174-175):** `RAISE NOTICE 'VERDICT: ... length % character(s) whose scheme is %, ...', length(v_val), split_part(v_val, ':', 1);`. Never the value.

**What the emitter mode must differ on (RESEARCH Q8):**
- No `BEGIN;`/`COMMIT;` wrapper (lines 114-116, 198).
- No `pg_try_advisory_xact_lock` (guard 2, lines 155-158). The restore has already run `refuse_without_mutex`, and the fragment runs inside the holder's lock window.
- No "already holds the sink" RAISE (guard 3, lines 169-171). In restore context that is a NOTICE, or it cannot happen after a replay.
- No psql `:'sink'` variables (lines 117-123). The restore's `run_transaction` passes no `-v`. Render literals after a strict charset check, with `'` doubled. The restore script's `sql_lit()` (`printf "%s" "$1" | sed "s/'/''/g"`) is the house idiom.
- Add a post-write re-read that asserts value = sink.

The dollar-quote tag stays unique (`$tonau$`-style) so it cannot collide with the restore's `$restore$`.

**Existing CLI contract to keep unchanged:**
- `--run` stays a dry-run default and `--commit` writes (lines 80-94).
- `usage()` prints `sed -n '2,62p'` of its own header (line 83). Any header edit shifts that range, so re-cut it.
- The existing 19-check `self_test()` must stay valid.

**Self-test additions — copy the `st_case` / `st_row_is` harness (lines 336-360):**
```bash
st_case() {  # name want_rc needle env-prefixed command…
  ...
  if ! grep -aq "$needle" "$out"; then
    results+=("  FAIL ${name}: exit ${rc} as expected, but the output does not carry '${needle}' — it stopped for some OTHER reason, so this refusal is UNMEASURED"); ...
```
To exercise the emitted fragment on the throwaway cluster, pipe the emitter output into `psql "$dsn" -X -q -v ON_ERROR_STOP=1` inside a `BEGIN … COMMIT`. Cover:
- TEST marker → row = sink;
- PROD marker (the existing `'quantalyze TEST-shaped fixture that names PROD'` calibration, line 378) → RAISE, and `st_row_is … "$seed"`;
- unmarked → RAISE;
- no row → RAISE;
- the output carries neither the value nor a DSN. Extend the `output-carries-no-dsn` scan at lines 470-482, which already uses `nullglob` with an empty-corpus FAIL.

---

### `scripts/restore-test-from-baseline.sh` (fragment concatenation + arms)

**Emit-to-file-then-cat analog:** the refdata block inside `build_transaction` (HEAD lines 1390-1396, 1434):
```bash
  local refdata_rc=0 refdata_n
  node "$REFDATA_EXTRACTOR" --allowlist "$REFDATA_ALLOWLIST" --migrations "$MIGRATIONS_DIR" \
    > "$RESTORE_OUT_DIR/refdata.sql" 2> "$RESTORE_OUT_DIR/refdata.err" || refdata_rc=$?
  if [ "$refdata_rc" -ne 0 ]; then
    cat "$RESTORE_OUT_DIR/refdata.err" >&2
    fail "reference-data extraction refused (exit ${refdata_rc}); nothing was replayed and no transaction was assembled."
  fi
  ...
  cat "$RESTORE_OUT_DIR/refdata.sql" >> "$out"
```
Sibling-script path convention: `REFDATA_EXTRACTOR="${REFDATA_EXTRACTOR:-scripts/extract-reference-inserts.mjs}"` (line 327) and `SCRIPT_DIR` (line 272).

**Placement (D-16a).** Directly after the `TXN_REFDATA_GATE` heredoc terminator and its `note "refdata: …"` line (HEAD ~1607-1609), and BEFORE `cat >> "$out" <<'TXN_LEDGER_DDL'` (HEAD 1614). `search_path` is `pg_catalog` at that point, so every name in the fragment must be schema-qualified.

**⛔ Byte-identical modes, the invariant to preserve (HEAD lines 1705-1711):**
```bash
  # ⛔ THE ONLY DIFFERENCE BETWEEN THE TWO MODES. Everything above is byte-identical,
  # which is what makes a green preflight evidence about the restore that follows.
  if [ "$mode" = "restore" ]; then
    echo "COMMIT;" >> "$out"
  else
    echo "ROLLBACK;" >> "$out"
  fi
```
Do NOT put an `if [ "$mode" = … ]` around the fragment. `src/__tests__/restore-test-from-baseline.test.ts` "the MODE picks the terminator" counts `COMMIT;` and `ROLLBACK;` as exactly 1 each in the run region.

**Marker predicates.** The fragment's EXPECT and REFUSE regexes should be the restore's own `RESTORE_EXPECT_MARKER_RE` / `RESTORE_REFUSE_MARKER_RE` (lines 313-314, default values identical to normalize's). Do not use a second set of defaults. `refuse_wrong_database` (line 896) is the pre-transaction twin.

**⚠️ Published-file coupling — a finding the research did not state.** RESEARCH Q8 says to write `$RESTORE_OUT_DIR/normalize.sql` and "extend that scan's file list". Doing so trips `src/__tests__/restore-test-from-baseline.test.ts` "E2 — every .sql the workflow PUBLISHES is a file the scan actually reads" (lines 1849-1879). E2 asserts `new Set(workflow staging loop) == new Set(staged=(…))`. Adding `normalize.sql` to `refuse_credential_in_published_sql`'s `local -a staged=(census.sql survivors.sql restore.sql refdata.sql)` (HEAD line 1841) without also adding it to `.github/workflows/test-restore-from-baseline.yml`'s staging loop turns E2 RED, and a workflow edit contradicts the research's "no workflow change". Two consistent options:
- (a) Write the fragment under `$RESTORE_OUT_DIR` but leave it OUT of both lists. It is concatenated into `restore.sql`, which the scan already reads, and E2 only compares the two lists.
- (b) Add it to BOTH lists and accept the workflow change, plus a re-run of `src/__tests__/test-restore-workflow-wiring.test.ts`.
The planner picks one. Option (a) keeps the research's no-workflow-change claim true.

**New self-test arms — analog pair `arm_wrongstate_red` / `arm_wrongstate_green` (HEAD lines 3746-3785):**
```bash
  arm_wrongstate_green() {
    setup_lane || return 1
    local out="$SELFTEST_TMPD/a32.out" rc=0
    arm_env restore a32 > "$out" 2>&1 || rc=$?
    cat "$out"
    [ "$rc" -eq 0 ] || { echo "MEASURE_FAIL: ..."; return 1; }
    ...
    n=$(lane_q "SELECT status FROM public.fx_keep WHERE id = 1;")
    [ "$n" = "verified" ] || { echo "MEASURE_FAIL: the premise is broken — ..."; return 1; }
    return 0
  }
```
A `--mode preflight` arm follows `arm_g_preflight` (HEAD lines 2738-2772): run `arm_env preflight`, then `lane_q` to assert the row is still the seeded stand-in. That proves the rollback. Register the new arms with `run_arm "<n> GREEN|RED …" 0 arm_…` (HEAD lines 3787-3819).

**Ratchets that move in the SAME commit:**
- `EXPECTED_ARMS=32` gets its dated `# MEASURED …` line (HEAD 2207-2208) and the `self-test OK (${pass}/${EXPECTED_ARMS} arms — …)` closing sentence (HEAD 3874).
- The `restore: tables=2 …` exact-line pins: measured **4** occurrences of `restore: tables=2` at HEAD and on the 164.4.2 tree (**14** of `tables=2` counting prose). RESEARCH said 5, so re-measure with `grep -c`.
- Arm 26 (`refuse_backticks_in_txn_heredocs`) refuses backticks in any unquoted heredoc, so write no markup inside the new heredocs.
- The `--run` region softening allowlist `{ "|| true": 4, "set +e": 7 }` (test line 996). Use `|| rc=$?`, never `|| true`, `continue-on-error`, `exit 0` or `::warning`.

---

### Restore self-test fixtures

**`baseline-fixture.sql`** — analog `public.fx_keep` (lines 75-88, 121-148). Add `public.system_settings` with the allow-list CHECK. Take the shape from normalize's self-test fixture (normalize script lines 324-325):
```sql
CREATE TABLE public.system_settings (key TEXT PRIMARY KEY, value TEXT NOT NULL);
ALTER TABLE public.system_settings ADD CONSTRAINT system_settings_analytics_service_url_allowed CHECK (key <> 'analytics_service_url' OR value ~ '^(https://[a-z0-9][a-z0-9.-]*\.up\.railway\.app|http://127\.0\.0\.1:9)$');
```
Write it in the dump's own style (`CREATE TABLE IF NOT EXISTS "public"."…"`, `OWNER TO "postgres"`). Update the fixture's own count prose at lines 31-32 ("2 CREATE TABLE lines…") and the 164.9-07 note at line 80 ("arms 9/12/18's exact `tables=2` pin is unaffected").

**Seed.** Analog `migrations/20260103000000_fixture_c.sql`: an unqualified INSERT that resolves only under the replay's `search_path` bracket, with header prose explaining why. Use the synthetic stand-in host (normalize self-test line 327: `https://selftest-stand-in.up.railway.app`) and never a real host.

**`refdata-allowlist.txt`.** Add one TAB line in the existing format: `<migration file>\t<schema.table>\t<statement count>\t# <why>`. ⛔ This is the FIXTURE allowlist. The real `scripts/restore-test-refdata-allowlist.txt` is not edited (a TODOS forbidden closure).

---

### `src/__tests__/restore-test-from-baseline.test.ts` (static pins)

**Order-pin analog: "L-01" (lines 542-610).** The `STEPS` array plus `liveIndexOf`, with calibrations that move a block on an in-memory copy and assert the order flips:
```typescript
const STEPS = [
  ["the search_path bracket", "SET LOCAL search_path = public, pg_catalog;"],
  ["the replay concatenation", 'cat "$RESTORE_OUT_DIR/refdata.sql" >> "$out"'],
  ["the path restore", "TXN_REFDATA_TAIL"],
  ["the gate", "TXN_REFDATA_GATE"],
  ["the ledger DDL", "TXN_LEDGER_DDL"],
] as const;
```
Add a new pin (or a sixth STEP between gate and ledger DDL) for the fragment concatenation, with its own move-calibration.

**Arm-ratchet analog (lines 450-540).** Literal `EXPECTED_ARMS=32` pins with bump / comment-out / double calibrations. Every `"EXPECTED_ARMS=32"` literal in the test moves with the constant.

**Byte-identity pin (new).** Assert that no `if [ "$mode"` line sits between the fragment and the terminator, and calibrate by wrapping the fragment in one on an in-memory copy. The analog is "the MODE picks the terminator" (lines 804-837).

---

## Shared Patterns

### Catalog-only self-verify (all three migrations)
**Source:** `supabase/migrations/20260826150000_destrict_enqueue_internal_10param.sql` DO block (lines 589-872).
**Apply to:** M1, M2, M3.
- Read only `pg_proc`, `pg_constraint`, `pg_attribute`, `pg_get_functiondef`, `obj_description` / `col_description`, `has_function_privilege`, `to_regprocedure` and `to_regrole`.
- Strip comments first.
- Guard against NULL bodies.
- Every RAISE format string is a single literal.
- Verify with `node scripts/lint-migration-data-dependence.mjs --self-test && node scripts/lint-migration-data-dependence.mjs`. The run must print 0 NEW, and no line may be added to `scripts/lint-migration-data-dependence-baseline.txt`.

### ACL re-convergence on every CREATE OR REPLACE
**Source:** 20260826150000 lines 438-468 (the ⛔ note about the mig-118 default-grant event trigger).
**Apply to:** M1 (service_role only) and M2 (authenticated, no anon, no PUBLIC). Converge first, then assert with `PERFORM public._assert_no_public_execute('<sig>')`, then run the named-role checks.

### Re-base discipline
**Source:** 20260826150000 header lines 120-144 ("RE-BASE DISCIPLINE … RE-MEASURED AT HEAD **WITH THIS FILE PRESENT**").
**Apply to:** M1 and M2.
- Each migration header records the grep and its result at execution time.
- M2's header records the 067 → 070 → 075 → 076 lineage (D-09 plus D-23).
- File names must match `^[0-9]+_[a-z0-9_]+\.sql$`, with a timestamp strictly newer than every file in `supabase/migrations/`. The newest at HEAD is `20260922120000`; re-check after the rebase. Note that `supabase/migrations/down/` is a directory and not a migration.

### Live-DB arm scaffold
**Source:** `@/lib/test-helpers/live-db` (`HAS_LIVE_DB`, `createLiveAdminClient`, `createTestUser`, `cleanupLiveDbRow`, `advertiseLiveDbSkipReason`, `signInAsTestUser`).
**Apply to:** the g10b, Queued and XOR test edits.
- Wrap each arm in `it.skipIf(!HAS_LIVE_DB)`, with cleanup in `finally`.
- Assert only about the arm's OWN rows (filter claim results by seeded ids, as P7 does).
- Calibrate every tightened assertion by neuter → observe RED → restore.
- Single-arm run: `npx vitest run --config vitest.livedb.config.ts <file> -t "<arm>"` against the local stack from `bash scripts/local-stack/run.sh up`.

### Never print a value, a DSN or a host
**Source:** `scripts/test-only-normalize-analytics-url.sh` header "⛔ NO VALUE IS EVER PRINTED" (lines 40-43) and its stream filter (lines 232-237). Also `scripts/restore-test-from-baseline.sh` `refuse_credential_in_published_sql`.
**Apply to:** the emitter, the new restore arms, harm-probe SUMMARY output, and any ledger prose. Print shapes, counts and verdicts only.

---

## No Analog Found

None. The weakest match is **M3**. `20260823120000_revoke_api_keys_insert.sql` is a REVOKE migration that also rewrites a column comment, so only its `$verify$` catalog-read shape transfers. Its read-modify-append comment idiom does NOT transfer.

## Conflicts surfaced for the planner (Rule 7)

1. **`normalize.sql` as a published file vs E2.** See the restore-script section: pick option (a) or (b). RESEARCH Q8's "extend that scan's file list" plus "no workflow change" cannot both hold.
2. **Summary-line pin count.** RESEARCH says 5 occurrences of `restore: tables=2`. Measured 4, at HEAD and on `origin/feat/164.4.2-subsetsplit`. Re-measure on the rebased tree.
3. **067's DO blocks are not reusable for M2.** Its pattern-check arm forbids the handler that D-12 keeps, and its functional probe is a data-dependent DO block. M2 takes its body from 067/075/076 and its verify shape from 20260826150000.

## Metadata

**Analog search scope:**
- `supabase/migrations/`, `supabase/tests/`, `supabase/schema/functions/`
- `src/__tests__/`, `src/app/api/`, `src/lib/test-helpers/`
- `scripts/` (restore, normalize, ledger, mutation-runner, fixtures)
- `origin/feat/164.4.2-subsetsplit` (for the ledger-shrink precedent and pin re-measurement)

**Files read:** 24. Every analog cited was checked as tracked source (`git ls-files`). No mirror paths are cited.
**Pattern extraction date:** 2026-09-24
