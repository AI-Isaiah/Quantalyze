---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 05
type: execute
wave: 3
depends_on: ["164.9.1-01", "164.9.1-04"]
files_modified:
  - scripts/restore-test-from-baseline.sh
  - scripts/restore-test-from-baseline-fixtures/baseline-fixture.sql
  - scripts/restore-test-from-baseline-fixtures/migrations/<fixture seeding the system_settings row>.sql
  - scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt
autonomous: true
requirements:
  - "[164.9-TEST-ANALYTICS-URL-REARM]"

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "D-16: a --mode restore run of scripts/restore-test-from-baseline.sh leaves system_settings.analytics_service_url equal to the loopback sink when it COMMITs; the PROD-shaped value replayed by the reference-data step is never committed."
    - "D-16a: the normalisation fragment is concatenated into restore.sql after the reference-data gate (TXN_REFDATA_GATE) and before the ledger DDL (TXN_LEDGER_DDL), byte-identical in both modes; the only mode difference in the whole transaction remains the final COMMIT; / ROLLBACK;."
    - "D-16: a --mode preflight run writes nothing (its ROLLBACK leaves the seeded stand-in value), and the census byte-compare still passes."
    - "D-17: the restore obtains the fragment from the emitter in scripts/test-only-normalize-analytics-url.sh and passes its own RESTORE_EXPECT_MARKER_RE / RESTORE_REFUSE_MARKER_RE; the restore script and its fixtures contain no copy of the sink literal, and the new self-test arms obtain the EXPECTED sink from the emitter's --print-sink mode (plan 04), never from a literal."
    - "D-18: proof is the restore --self-test on disposable clusters (this plan) plus the static pins in src/__tests__/restore-test-from-baseline.test.ts and an unchanged, green src/__tests__/test-restore-workflow-wiring.test.ts (plan 06)."
    - "D-28: the fragment is not a separately published .sql artifact; the published-file scan list and the workflow staging loop stay equal (test E2 green) and .github/workflows/test-restore-from-baseline.yml is not edited."
    - "Forbidden closures honoured: scripts/restore-test-refdata-allowlist.txt (the REAL allowlist) is not edited, and no migration carries the normalisation."
  artifacts:
    - path: scripts/restore-test-from-baseline.sh
      provides: "emitter call with rc capture, fragment concatenation, new GREEN arms, re-measured summary pins and EXPECTED_ARMS"
  key_links:
    - from: "build_transaction"
      to: "scripts/test-only-normalize-analytics-url.sh --emit-restore-sql"
      via: "command substitution with rc captured, appended to $out between TXN_REFDATA_GATE and TXN_LEDGER_DDL"
      pattern: "emit-restore-sql"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> `scripts/restore-test-from-baseline.sh` is one of the three files 164.4.2 changed. Every summary-line pin
> (`restore: tables=…`), `EXPECTED_ARMS`, and the softening allowlist counts are read on the rebased tree.
> Runs in parallel with plan 03 (file-disjoint; this plan uses pg-lane clusters only, never the local Supabase stack).
> Its static pins were split into plan 06 (checker iteration 1: per-plan budget).

<objective>
Wire the plan-04 emitter into the restore's own atomic transaction so a restore of shared TEST leaves
`analytics_service_url` normalised inside the held mutex and never commits the PROD-shaped value
(`[164.9-TEST-ANALYTICS-URL-REARM]`, D-16, D-16a, D-17, D-18, D-28).

Surfaced conflict kept visible (Rule 7): the ROADMAP and the TODOS entry say "a post-restore step in
`test-restore-from-baseline.yml`". D-16 keeps that intent and moves it INTO the transaction because the
commit window between a restore and a later step is itself the hazard, and a separate step running the
script as-is would always refuse (the workflow's holder session owns the mutex the script tries to take).

Output: a restore that normalises in-transaction, proven by its self-test and a calibration. The static pins
over `build_transaction` are plan 06.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-RESEARCH.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-PATTERNS.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The restore concatenates the emitted fragment after the reference-data gate; restore mode commits the sink, preflight writes nothing (D-16, D-16a, D-17, D-28)</name>
  <files>scripts/restore-test-from-baseline.sh, scripts/restore-test-from-baseline-fixtures/baseline-fixture.sql, scripts/restore-test-from-baseline-fixtures/migrations/&lt;fixture&gt;.sql, scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt</files>
  <read_first>
    - `scripts/restore-test-from-baseline.sh` on the rebased tree: `SCRIPT_DIR`, `REFDATA_EXTRACTOR` path convention, `build_transaction` from `TXN_HEAD` through the refdata extractor block (`refdata_rc`, `fail`), `TXN_REFDATA_TAIL`, `TXN_REFDATA_GATE`, `TXN_LEDGER_DDL`, the mode terminator block ("THE ONLY DIFFERENCE BETWEEN THE TWO MODES"), `refuse_without_mutex`, `refuse_credential_in_published_sql` (its `staged=(…)` list), `refuse_backticks_in_txn_heredocs`, `EXPECTED_ARMS` with its dated `# MEASURED` line, every `restore: tables=` pin, `arm_g_preflight`, `arm_wrongstate_red` / `arm_wrongstate_green`, `run_arm` registrations, and the closing `self-test OK (${pass}/${EXPECTED_ARMS} arms — …)` sentence.
    - `scripts/restore-test-from-baseline-fixtures/baseline-fixture.sql` (the `public.fx_keep` shape and its own count prose), `scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql`, `scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt`.
    - `scripts/test-only-normalize-analytics-url.sh` `--emit-restore-sql` (plan 04) and its env overrides for the marker regexes.
    - `.github/workflows/test-restore-from-baseline.yml` staging loop (read only — it is NOT edited).
    - `164.9.1-RESEARCH.md` Contradiction 1 and "Q8 — REARM"; `164.9.1-PATTERNS.md` "scripts/restore-test-from-baseline.sh" and "Restore self-test fixtures".
  </read_first>
  <action>
    In `build_transaction`, directly after the `TXN_REFDATA_GATE` heredoc (and its `note "refdata: …"` line) and
    before the `TXN_LEDGER_DDL` heredoc, obtain the fragment by command substitution from
    `bash "$SCRIPT_DIR/test-only-normalize-analytics-url.sh" --emit-restore-sql`, passing the restore's own
    `RESTORE_EXPECT_MARKER_RE` and `RESTORE_REFUSE_MARKER_RE` through the emitter's override variables (D-17), with
    the exit code captured by `|| frag_rc=$?` and a `fail` naming the refusal when non-zero (the refdata extractor's
    idiom; never `|| true`, `set +e`, `continue-on-error`, `exit 0` or `::warning`, because the `--run` region's
    softening allowlist is pinned). Append it to `$out` with `printf '%s\n'`. ⛔ No `if [ "$mode" … ]` around it:
    both modes emit it byte-identically and preflight's `ROLLBACK;` is what makes preflight write nothing (D-16a).
    Emit a separate `note` line with the normalisation verdict; do not change the `restore: tables=…` summary-line
    format (the workflow and the self-test parse it). Per D-28, write no `normalize.sql` into `$RESTORE_OUT_DIR`
    and do not touch `refuse_credential_in_published_sql`'s `staged=(…)` list or the workflow file.
    No backticks inside any new heredoc (arm 26).

    Fixture ripple (research Q8): add `public.system_settings` to `baseline-fixture.sql` in the dump's own style with
    the analytics-URL allow-list CHECK admitting the synthetic stand-in host used by the normalize self-test and the
    loopback SHAPE, written as a pattern (for example a `~` regex on `^http://127\.0\.0\.1:[0-9]+$`), never the sink
    literal itself (D-17) and never a real host; seed the row with an INSERT in a fixture migration (prefer adding it to an
    existing fixture migration so the fixture ledger row count does not move; if a new fixture migration is needed,
    re-measure `ledger_rows=` too); add one TAB line for it to the FIXTURE `refdata-allowlist.txt` (⛔ never the real
    `scripts/restore-test-refdata-allowlist.txt`). Update the fixture's own count prose.

    New self-test arms, registered with `run_arm`: a `--mode restore` GREEN arm asserting via `lane_q` that the
    fixture row equals the sink after commit, where the expected sink is read at arm time from
    `bash "$SCRIPT_DIR/test-only-normalize-analytics-url.sh" --print-sink` with its exit status checked (a failed or
    empty read FAILS the arm; it is never compared against an empty string) — per D-17 the arm types no sink literal and that the output carries the normalisation `note`; a `--mode preflight`
    GREEN arm asserting the row still equals the seeded stand-in (rollback) with the census byte-compare still passing.
    Keep the existing PROD-marker arm refusing before any transaction. Raise `EXPECTED_ARMS` by exactly the number of
    arms added, with a dated `# MEASURED` line, and update the closing `self-test OK` sentence to mention the in-transaction
    normalisation. Re-measure every `restore: tables=…` exact-line pin with `grep -c` and update each to the new
    fixture shape (the pins move together; never delete one).

    Calibrate: byte-backup the script, delete the concatenation, run the self-test, observe the restore-mode arm FAIL
    naming the row; restore with `cp`, `cmp`, observe OK. Record in the SUMMARY.
    Machine-checkable record: write into this plan's SUMMARY (create it if absent) exactly one line per calibration,
    `CALIBRATION-RED: restore-fragment-concat neuter=FAIL restored-cmp=0 restored=OK`,
    each written only AFTER its neuter was observed RED, the `cp` restore was `cmp`-verified (exit 0) and the arm was
    observed GREEN again; never pre-write one. A neuter that did not go RED means the arm is not armed: fix the arm.
    Commit hygiene: node_modules symlink removed around commit; the three forbidden `.planning` paths never staged.
  </action>
  <precondition>Plan 04 committed (the emitter exists); pg-lane binaries resolvable.</precondition>
  <verify>
    <automated>export PGBIN="${PGBIN:-/opt/homebrew/opt/postgresql@16/bin}"; S="${TMPDIR:-/tmp}/gsd-164.9.1-05"; mkdir -p "$S"; rm -f "$S/rst.log"; R=scripts/restore-test-from-baseline.sh; F=scripts/restore-test-from-baseline-fixtures; PB="$(sed -nE 's/^PHASE-BASE: ([0-9a-f]{40})$/\1/p' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md)"; test -n "$PB" && grep -qF -- '--emit-restore-sql' "$R" && grep -qF -- '--print-sink' "$R" && ! grep -v '^\s*#' "$R" | grep -qF '127.0.0.1:9' && ! grep -rv '^\s*--' "$F" | grep -qF '127.0.0.1:9' && git diff --quiet "$PB" -- scripts/restore-test-refdata-allowlist.txt .github/workflows/test-restore-from-baseline.yml && bash "$R" --self-test > "$S/rst.log" 2>&1; RC=$?; tail -15 "$S/rst.log"; test "$RC" = 0 && E="$(sed -nE 's/^EXPECTED_ARMS=([0-9]+)$/\1/p' "$R")" && grep -qE "self-test OK \(${E}/${E} arms" "$S/rst.log" && test "$(grep -cxF 'CALIBRATION-RED: restore-fragment-concat neuter=FAIL restored-cmp=0 restored=OK' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-05-SUMMARY.md)" = 1 && echo REARM-RESTORE-OK</automated>
    <fails_when>no "REARM-RESTORE-OK": the phase base cannot be read from plan 01's SUMMARY, the restore does not call the emitter or does not read the expected sink through --print-sink, the restore script or a fixture carries its own copy of the sink literal, the REAL allowlist or the workflow file differs from the phase base (committed or not), the self-test failed, or it did not report every arm passing against the raised EXPECTED_ARMS. (RC is the status of the whole chain ending in the self-test, so an earlier failure cannot be masked.) Also fails when the SUMMARY lacks exactly one `CALIBRATION-RED:` line for `restore-fragment-concat` (the neuter's RED, the `cmp`-verified restore and the GREEN re-run are recorded as a structured line, not only as prose).</fails_when>
  </verify>
  <acceptance_criteria>
    - In `build_transaction`, the emitter call sits after `TXN_REFDATA_GATE` and before `TXN_LEDGER_DDL`, and no `if [ "$mode"` line appears between the emitter call and the terminator block.
    - `grep -c 'COMMIT;'` and `grep -c 'ROLLBACK;'` over the terminator region are unchanged (1 each).
    - `restore-test-from-baseline.sh --self-test` prints `self-test OK (E/E arms` where E is the new `EXPECTED_ARMS`, and E is greater than the value recorded in plan 01's pin table.
    - `git diff --quiet "$PB" -- scripts/restore-test-refdata-allowlist.txt .github/workflows/test-restore-from-baseline.yml` exits 0 (`PB` = plan 01's `PHASE-BASE:` sha; base commit versus working tree).
    - The restore-mode arm reads its expected value via `--print-sink`; no non-comment line of the restore script or of `scripts/restore-test-from-baseline-fixtures/` contains the sink literal.
    - The SUMMARY records the old and new `restore: tables=` pin text and count, the EXPECTED_ARMS move, and the calibration (arm FAIL with concatenation removed, OK after `cp` restore with `cmp` exit 0).
  </acceptance_criteria>
  <reversibility rating="reversible">Script and fixture change; no schema, no shared database touched.</reversibility>
  <done>A restore normalises the destination row inside its own transaction; preflight proves the same SQL and writes nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder dispatch -> restore on shared TEST | destructive, inside the held mutex; not run by any agent (FC-3) |
| restore.sql -> published backup artifact | credential-scanned; public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-19 | Information disclosure | PROD-shaped analytics_service_url committed on TEST (Vault key posted to a real host by a tick) | high | mitigate | normalisation inside the restore transaction after the replay and gate; restore-mode arm proves the committed row is the sink |
| T-164.9.1-20 | Tampering | preflight writing to shared TEST | high | mitigate | fragment byte-identical in both modes; preflight ends ROLLBACK; preflight arm proves the stand-in survives; byte-identity static pin |
| T-164.9.1-21 | Tampering | restore running against PROD | high | mitigate | existing pre-transaction marker refusal kept; fragment re-reads the marker with the restore's own regexes |
| T-164.9.1-22 | Information disclosure | new published artifact escaping the credential scan | medium | mitigate | D-28: no separate normalize.sql; content lives in restore.sql, which the scan reads; E2 unchanged green |
| T-164.9.1-23 | Denial of service | emitter failure silently skipped | medium | mitigate | rc captured and `fail`ed; no softening idiom added (pinned allowlist) |
</threat_model>

<verification>
- REARM-RESTORE-OK printed; calibration recorded.
</verification>

<success_criteria>
- The restore can no longer commit a PROD-shaped destination on TEST; proven on disposable clusters.
- FC-3 (live dispatch) remains a founder act and is not a completion gate (plan 14).
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| in-transaction normalisation in `scripts/restore-test-from-baseline.sh` | ops-script behaviour | FC-3 dispatch (founder), plan 10 docs |
| fixture `system_settings` table/seed/allowlist line | self-test fixture | restore `--self-test` |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-05-SUMMARY.md` when done.
</output>
