---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 01
subsystem: testing
tags: [compute-jobs, fan-in, live-db-lane, harm-probe, postgres]
status: complete

requires:
  - phase: 164.4.2
    provides: "the local-stack lane's D-F tail replay (baseline + migrations the dump does not carry)"
provides:
  - "PHASE-BASE line and the re-measured 164.4.2 pin table every later plan reads"
  - "P12-harm live-DB arm in the g10b file: the fan-in harm proven by execution on the pre-fix lane"
affects: [164.9.1-02, 164.9.1-03, 164.9.1-10, 164.9.1-14]

actuals:
  tokens: 1860
  tasks: 2
  commits: 1
plan_head_before: d0057747d5cbb967f70714d4407f60ffe8a6643f

tech-stack:
  added: []
  patterns:
    - "verdict line printed BEFORE assertions, so a pre-fix RED still records what the lane did"
    - "crowd-out fence: count the claimable rows of the kind below the claim RPC's 1000 cap before each claim"

key-files:
  created: []
  modified:
    - src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts

key-decisions:
  - "Base is origin/main (164.4.2 is on it by CONTENT). The ancestry was already present when this plan started, so Task 1 made no commit."
  - "Crowd-out fence counts pending AND failed_retry rows, not only pending, because the claim RPC's candidacy includes failed_retry."

duration: ~35min
completed: 2026-09-25
---

# Phase 164.9.1 Plan 01: Ancestry, pin re-measure and the pre-fix harm probe Summary

**On the pre-fix local lane, a child enqueued through the public wrapper with a running parent lands `pending`. A worker claims it while its parent is still running, and the parent's mark-done leaves it `running`, so the fan-in never engages. This was demonstrated by execution. The production harm is latent, because no production caller passes a parent.**

PHASE-BASE: f8a3096e3f984f96e7ff0a442e44d2c46f78f9bd

## Task 1: ancestry (D-19) and pin re-measure (D-20)

**Base decision (by content, never by branch-tip ancestry):** `origin/main` (at `f8a3096e`, #857) contains 164.4.2:
- `git cat-file -e origin/main:supabase/schema/baseline-carried-migrations.txt` exits 0.
- `git show origin/main:scripts/local-stack/run.sh`, captured into a variable first, carries `SCHEMA SOURCE: baseline + the TAIL`.

The base is therefore `origin/main`.

**Ancestry:** `git merge-base --is-ancestor origin/main HEAD` was already true when this plan started. The phase branch had merged `origin/main` at merge commit `2dbc315d1`, the parent of the starting HEAD `d0057747d`. No rebase or merge was needed, so no conflict could arise, and Task 1 authored no tracked change.

The lane boot on this tree confirms the D-F replay is live. It printed `baseline-currency: carried=274 replay=0 marker-sha=match defects=0` and `baseline-replay: 0 migration(s) newer than the dump (none)`.

### Pin table (READ on this tree, 2026-09-25)

| Symbol | Reading | Command |
|---|---|---|
| live-DB ledger `# ENTRY_COUNT =` | 10 | `grep -nE '^# ENTRY_COUNT' scripts/live-db-execution-ledger.txt` |
| ledger non-comment line count | 10 | `grep -cvE '^\s*(#\|$)' scripts/live-db-execution-ledger.txt` |
| `ENTRY_CEILING` | 10 | `grep -nE '^export const ENTRY_CEILING' scripts/live-db-execution-ledger.mjs` |
| K3 line g10b `P12:` | present | `grep -nE 'P12:\|widened UNIQUE:\|Queued path' scripts/live-db-execution-ledger.txt` |
| K3 line match-decisions-xor-rls `widened UNIQUE:` | present | same |
| K3 line request-allocator-holdings-sync-queued `Queued path` | present | same |
| `K3_ARMS` entries | tokens `P12:`, `widened UNIQUE:`, `Queued path`; plus one EVIDENCE-keyed entry for `wizard-rpcs-live-db.test.ts` (`permission denied for function create_wizard_strategy`) | read `const K3_ARMS` in `scripts/live-db-execution-ledger.mjs` |
| `FILES_FLOOR` | 50 | `grep -nE '^export const (FILES_FLOOR\|ARMS_FLOOR\|WAIVED_CEILING)' scripts/mutation-runner/run.mjs` |
| `ARMS_FLOOR` | 449 | same |
| `WAIVED_CEILING` | 0 | same |
| lane postgres image | `17.6.1.113` (`LANE_PG_VERSION`); boot printed `postgres image pinned: …/supabase/postgres:17.6.1.113` | `grep -nE 'LANE_PG_VERSION=' scripts/local-stack/run.sh` |
| `ci.yml` job keys `sql-tests:` and `test-db-drift:` | both present | `grep -nE '^  (sql-tests\|test-db-drift):' .github/workflows/ci.yml` |
| `61616158` in `ci.yml` | 29 | `grep -c 61616158 .github/workflows/ci.yml` |
| `61616158` in `supabase-migrate.yml` | 7 | `grep -c 61616158 .github/workflows/supabase-migrate.yml` |
| `restore: tables=2` in the restore script | 4 | `grep -c 'restore: tables=2' scripts/restore-test-from-baseline.sh` |
| `EXPECTED_ARMS=` | 32 | `grep -nE 'EXPECTED_ARMS=' scripts/restore-test-from-baseline.sh` |
| newest migration | `20260924120000_ledger_fanout_failure_count.sql` | `ls supabase/migrations \| grep -E '^[0-9]' \| sort \| tail -1` |
| `VERSION` | `0.90.1.0` | `cat VERSION` |
| `FRONTIER_EXEMPT_CEILING` | 3 | `grep -nE 'FRONTIER_EXEMPT_CEILING=' scripts/test-ledger-drift-check.sh` |

**Where the readings differ from the planning documents (the READING wins):**
- `FILES_FLOOR` reads **50**, and research Q4 says 49.
- `ARMS_FLOOR` reads **449**, and research Q4 says 426. Both floors moved up with the phases merged since research.
- The ledger reads **10 / 10**. CONTEXT D-20 says 18 on `origin/main` and 10 on the 164.4.2 branch. `main` now carries 164.4.2, so 10 is `main`'s number. After plan 10 removes the three K3 lines, the count is 7, but plan 10 must re-read it.
- `61616158` reads 29 in `ci.yml` and 7 in `supabase-migrate.yml`. That matches CLAUDE.md's 2026-09-23 re-measure.
- `supabase/migrations/down` is a directory, not a migration. Any "newest migration" sort must filter to `^[0-9]`.

## Task 2: the harm probe (D-01, D-02, D-03, D-26)

The new arm is titled `P12-harm: a parented child is not claimable before its parent is done and is released by the parent's mark-done`, in the `fan-in chain` describe block of `src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts`. The title does not contain the ledger's K3 token `P12:`.

- The parent is seeded `running` with a minted claim token (P15's shape). The child is enqueued through the public `enqueue_compute_job` with `p_parent_job_ids`, kind `reconcile_strategy`, exactly as P12 calls it.
- The claim calls `claim_compute_jobs_with_priority` with all five named args, including `p_kind_include` (D-26), and `p_batch_size: 1000`.
- The parent's `mark_compute_job_done` runs unconditionally. Only the second claim is conditional on the result of (ii).

**Lane:** the local stack booted from this tree with `bash scripts/local-stack/run.sh down` then `up` (`up complete`, exit 0, loopback only). No phase migration exists yet, so this is the pre-fix lane.

**Pre-fix run** (`npx vitest run --config vitest.livedb.config.ts src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts -t "P12-harm"`, exit 1):

```
HARM-PROBE verdict: child_status_at_enqueue=pending claimed_while_parent_running=true parent_mark_done_ok=true child_status_after_parent_done=running claimed_after_parent_done=not-reached foreign_rows_claimed=0
AssertionError: expected 'pending' to be 'done_pending_children' // Object.is equality
```

VERDICT-WORDING: mechanism demonstrated / production harm latent

What each key records:
- (i) The child lands `pending`, which is the status defect.
- (ii) A worker is handed the child while its parent is still running, which is the early-claim harm.
- (iii) The parent's mark-done succeeds, but the fan-in advance is a no-op for the child, which stays `running` under the probe's worker.

The second claim was correctly `not-reached`. `foreign_rows_claimed=0`, so there is no lane cleanup item.

**D-02 evidence, re-measured:** `grep -rn parent_job_ids analytics-service --include='*.py' --exclude-dir=.venv | wc -l` returns **0**. No Python caller passes a parent, so the production harm is latent.

**D-03:** the fan-in defect proceeds to FIX in plan 02.

**Whole-file run on the same lane:** `Tests 2 failed | 18 passed (20)`. The failing arms are exactly `P12:`, which is ledgered as K3, and the new `P12-harm:`.

### Guard calibration (neuter, observe RED, restore via cp/cmp)

| Guard | Neuter | Observed | Restored |
|---|---|---|---|
| `expect(claimedWhileParentRunning).toBe(false)` (the harm assertion, masked pre-fix by the status assertion before it) | removed the status assertion line | `AssertionError: expected true to be false`, at the `claimedWhileParentRunning` line | `cmp` OK |
| crowd-out fence `toBeLessThan(claimCap)` | cap changed to `1` | `AssertionError: expected 1 to be less than 1` | `cmp` OK |

The status assertion `toBe("done_pending_children")` is RED on the unmodified pre-fix run, shown above.

## Deviations from Plan

1. **[Rule 3 - no-op] Task 1 authored no commit.** The phase branch already had `origin/main` merged in before this plan started, at merge `2dbc315d1`. The ancestry step was verified, not performed. The plan's success criterion is ancestry, and it holds.
2. **[Rule 2 - correctness] The crowd-out fence counts `status IN ('pending','failed_retry')`, not `pending` alone.** The claim RPC's candidacy predicate includes `failed_retry`, so counting only `pending` could under-count the rows competing for the batch. This is strictly stronger than the plan's wording.
3. **Worktree commit guard.** The executor protocol's worktree allow-list expects an `agent-*` branch. This checkout is the orchestrator-pinned phase worktree on `feat/164.9.1-jobrpctruth`. The commit went there as instructed. The protected-branch check passed, because the branch is not the default branch.

## Known consequences for later plans

- The `P12-harm:` failure does not match a K3 token (`P12:`). Until plan 02 lands M1 and turns it green, the live-DB execution ledger gate would report it as an unledgered failure. Per research Pitfall 9, the branch is NOT pushed before plan 10's ledger commit.
- No stubs. No new threat surface: the arm reads and writes only the loopback lane, and prints statuses, booleans and one count.

## Commits

- `163cb233c` test(164.9.1-01): harm probe for the fan-in status defect, lane-RED by design

## Self-Check: PASSED

- `163cb233c` exists; the modified test file exists; `ANCESTRY-OK` and `HARM-RECORDED` both printed by the plan's own verify commands.
