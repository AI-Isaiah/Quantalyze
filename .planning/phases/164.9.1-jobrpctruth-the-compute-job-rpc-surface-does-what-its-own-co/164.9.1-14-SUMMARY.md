---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 14
subsystem: release
tags: [release, changelog, version, todos, fc-1, fc-3]
status: checkpoint-fc1
requires:
  - phase: 164.9.1-13
    provides: "RESYNC-OK, the branch carries origin/main with 164.4.2 on it by content"
provides:
  - "one chore(release) v0.93.0.0 commit carrying CHANGELOG, VERSION, package.json, the three TODOS closures and this SUMMARY"
  - "a commit checklist with 40 of 40 non-merge branch commits mapped to the entry"
affects: [164.9.1-VERIFICATION, gsd-ship]

actuals:
  tokens: 6000
  tasks: 1
  commits: 1
plan_head_before: a892e8560897d6e252aef3fe1d0c84ac2bdc03dd

tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-14-SUMMARY.md
  modified:
    - CHANGELOG.md
    - VERSION
    - package.json
    - TODOS.md

key-decisions:
  - "v0.93.0.0, a MINOR bump. Callers see new behaviour: the holdings sync RPC returns the Queued shape again, and a disconnected key gets 409 instead of 500. Precedent: KEYCARDSYNC 0.88.0.0 and GATEHYGIENE 0.90.0.0, both fix phases on the same key-card and job surfaces that took a minor bump. 0.92.0.0 is taken by PR #859, which lands first."
  - "No merge of origin/main was needed. After git fetch, origin/main was still 01dcf1cc7, which is already an ancestor of HEAD through plan 13's merge commit 49c984a41."
  - "This SUMMARY rides in the release commit instead of a later docs commit. The plan's verify requires HEAD to be the chore(release) commit, and a SUMMARY commit on top would fail it."

duration: ~25min
completed: 2026-09-25
---

# Phase 164.9.1 Plan 14: Release record for JOBRPCTRUTH Summary

**One `chore(release): v0.93.0.0 — JOBRPCTRUTH` commit carries a unified CHANGELOG entry. The entry maps all 40 non-merge branch commits, and none is left unmapped. VERSION and package.json are byte-equal `0.93.0.0` with no trailing newline. The commit also closes the three TODOS entries this phase owned, each with evidence. Execution stops at FC-1, where the orchestrator runs the three migration reviewers, pushes, opens the PR and reads CI bound to the head SHA.**

## Task 1: the release commit

### Precondition, re-measured live after `git fetch origin`

- `RESYNC-OK` is present in `164.9.1-13-SUMMARY.md` as a line of its own.
- `git cat-file -e origin/main:supabase/schema/baseline-carried-migrations.txt` exits 0.
- `origin/main`'s `scripts/local-stack/run.sh` contains `SCHEMA SOURCE: baseline + the TAIL` (captured into a variable first; 1 hit).
- `git merge-base --is-ancestor origin/main HEAD` exits 0. `origin/main` is `01dcf1cc7` with VERSION `0.91.0.0`. `git log HEAD..origin/main` is empty, so there was nothing to merge.

### Commit checklist (step 1): `git log origin/main..HEAD`

42 commits, of which 40 are non-merge. The two merges are `2dbc315d1` (the earlier main merge) and `49c984a41` (plan 13's re-sync, FC-2). Both are covered by the entry's FC-2 re-sync and planning-record bullets. The mapping was checked mechanically: every non-merge short hash was grepped in the new entry, with the result `mapped=40 unmapped=0`.

| # | Commit | Subject (abridged) | Entry section |
|---|---|---|---|
| 1 | `d8702293c` | capture phase context | Notes, planning record |
| 2 | `d5d2d9c60` | research, validation, decisions | Notes, planning record |
| 3 | `342188b10` | pattern map and D-28 | Notes, planning record |
| 4 | `f6191aa6f` | create phase plan | Notes, planning record |
| 5 | `d0057747d` | D-23 answer, FC-3 blocker | Notes, planning record |
| 6 | `163cb233c` | harm probe, lane-RED by design | Tests; Fixed (M1 before/after) |
| 7 | `e1c4d2e90` | plan 01 PHASE-BASE and pins | Notes, planning record |
| 8 | `ee325bfee` | M1, the ten-arg INSERTs the fan-in status | Fixed |
| 9 | `f7f96c162` | plan 02 SUMMARY | Notes, planning record |
| 10 | `44bc7d93d` | dedupe twins re-pointed at M1 | Tests |
| 11 | `381798e02` | plan 03 SUMMARY | Notes, planning record |
| 12 | `48f84c232` | normalize script emits the restore fragment | Changed |
| 13 | `d9730d004` | fragment refusal self-test | Tests |
| 14 | `dbe9f867d` | plan 04 SUMMARY | Notes, planning record |
| 15 | `dd1fb6d25` | M2, the prefetch and the disconnected refusal | Fixed |
| 16 | `40aa2643a` | plan 07 SUMMARY | Notes, planning record |
| 17 | `8559c9e2d` | restore normalises in-transaction | Changed; Tests |
| 18 | `ec2336967` | plan 05 SUMMARY | Notes, planning record |
| 19 | `250762420` | route 409 | Fixed |
| 20 | `810659108` | plan 08 SUMMARY | Notes, planning record |
| 21 | `bb0fd0b7f` | fragment position and byte-identity pins | Tests |
| 22 | `0725da4e2` | M3, bridge_outcomes comments | Fixed |
| 23 | `0fec29911` | plan 06 SUMMARY | Notes, planning record |
| 24 | `97d987661` | XOR asserts the current invariant | Tests |
| 25 | `c12842fad` | plan 09 SUMMARY | Notes, planning record |
| 26 | `0e559db17` | ledger shrink 10 to 7 | Changed |
| 27 | `2a8bcbe10` | runbook and arm D1 prose | Changed |
| 28 | `80e07112b` | plan 10 SUMMARY | Notes, planning record |
| 29 | `74a125a64` | ten-arg refuses an unreleasable parent list | Fixed |
| 30 | `f72d9dd4c` | M2 arm, M3 comments, no-oracle pin | Fixed (M2 and M3 bullets) |
| 31 | `eb4c5407e` | 409 reaches the user, trace, queued hold | Fixed (route and card bullets) |
| 32 | `8a3053f1e` | emit failure is a refusal, emitter self-tested | Changed |
| 33 | `411b56e9d` | review round 1 SUMMARY | Notes, planning record |
| 34 | `bce6c5171` | D-04 amendment, residuals routed to 164.5.2 | Fixed (D-04 amendment) |
| 35 | `c06bad985` | M1 records the diamond deadlock | Notes, known limits |
| 36 | `57f999b88` | round-2 exchange-card fixes | Fixed; Tests |
| 37 | `5e43d4d97` | review round 2 SUMMARY | Notes, planning record |
| 38 | `616b95aaf` | plan 13, FC-2 and RESYNC-OK | Notes, planning record |
| 39 | `c07e8a409` | phase verification | Notes, planning record |
| 40 | `a892e8560` | security threat verification | Notes, planning record |

Every theme from step 3 is present: Fixed, Changed, Tests, Root cause and Notes. The Notes section carries the known limits the brief listed: the three residuals routed to 164.5.2 (a stranded fan-in child, the diamond 40P01 deadlock, and the `match_decisions` 23505), the D-11 race, the 42725 seven-arg call, that the normalize self-test runs only in the dispatch restore job, the pre-declared VAC-08 row, and FC-3 as the founder's step after merge. The entry names shapes only. It carries no host, DSN, key id, credential-shaped literal or hash value. The pre-change hash is described, not quoted.

### VERSION

The version went from `0.91.0.0` to `0.93.0.0`. Both files were written by hand, and `npm version` was not run. `xxd VERSION` shows 8 bytes and no newline. `package.json` has `"version": "0.93.0.0"` (1 hit). `node scripts/check-version-bump.mjs` printed `base=0.91.0.0 head=0.93.0.0 package=0.93.0.0 changed=60` and `✅ No defects`.

### TODOS.md

Three entries were ticked `[x]`, each with a dated `✅ CLOSED 2026-09-25` line that names the fix and cites the plan SUMMARYs as evidence:
- `[164.9-FANIN-STATUS-NEVER-SET]`: closed by M1 and the review-round lock and refusals. The closure line says the fix reaches PROD at merge, and that the latent residuals are routed to 164.5.2.
- `[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]`: (a) is closed by M2 and (b) by the D-13 intent decision with M3 and the XOR rewrite. The closure line also records the ledger going from 10 to 7.
- `[164.9-TEST-ANALYTICS-URL-REARM]`: closed by the in-transaction step. The closure line states that the live observation is **FC-3, founder-owned, blocked on the baseline re-dump, and not a completion gate**.

`[164.9-BASELINE-PRIVILEGES-ABSENT]` was not touched.

### Gates run before the commit

- `npx vitest run src/__tests__/critical-regressions.test.ts src/__tests__/lint-migration-data-dependence.test.ts`: `Test Files  2 passed (2)`, `Tests  183 passed (183)`. The second file reads TODOS.md ids.
- `npx vitest run src/__tests__/phase-150-capital-ownership-invariant.test.ts src/__tests__/contracts/ci-docs-path-filter.contract.test.ts`: `Tests  47 passed (47)`. Both read CHANGELOG or VERSION.
- Skip-token sweep of the entry: 0 hits.

The plan's RELEASE-OK verify, the planning hygiene check and the commit-message sweep ran around the commit. Their output is in the executor's report to the orchestrator, because this file is inside the commit it describes.

## Task 2: FC-1 (not reached by this executor)

This task is a founder checkpoint. Execution stops at `READY-FOR-3-REVIEWERS`. Next, the orchestrator dispatches `migration-reviewer`, `rls-policy-auditor` and `silent-failure-hunter` on the three migrations, then pushes, opens the PR and reads CI bound to the head SHA. It merges only when the FC-1 preconditions in the plan hold. The single tolerated red is VAC-08's `_enqueue_compute_job_internal/10 DRIFT` row with plan 02's pre-change TEST hash. **D-23 is flagged for the founder:** the restored disconnected-key refusal and the 409 are an autonomous decision the founder may overrule before merging. The post-merge check, bound to the merge SHA (`apply-test`, `apply`, and the push-run `test-db-drift`), is still to be recorded.

## Task 3: FC-3 (founder-owned, non-gating)

This task is not reached here. A dispatch of `test-restore-from-baseline.yml` is a founder act that waits on the baseline re-dump. No agent dispatches it.

## Deviations from Plan

1. **[Rule 3 - Blocking] The SUMMARY is inside the release commit.** The plan's verify needs HEAD to be the `chore(release)` commit, and a separate docs commit on top would fail it. Because of that, the SUMMARY cannot quote its own commit hash. The hash is in the executor's report.
2. **Version chosen by the orchestrator's fact, not only by the plan's `X.Y.Z.B` rule.** The plan's rule alone would give 0.92.0.0. PR #859 holds that number and lands first, so the version is 0.93.0.0. A VERSION and CHANGELOG conflict with #859 is expected when this branch next merges main, and the orchestrator resolves it.

## Known Stubs

None.

## Self-Check: PASSED

- `CHANGELOG.md`, `VERSION`, `package.json`, `TODOS.md` modified, and this file created. All five are staged by explicit path in the release commit.
- Every hash cited in the checklist table is on the branch: `git log origin/main..HEAD` lists all 40, as `mapped=40` confirms.
