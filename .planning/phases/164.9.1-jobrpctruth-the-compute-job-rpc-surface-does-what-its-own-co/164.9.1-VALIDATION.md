---
phase: "164.9.1"
slug: "jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-24"
---

# Phase 164.9.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Seeded from
> `164.9.1-RESEARCH.md` § Validation Architecture; the planner fills the per-task map.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (static `vitest.config.ts` + live-DB `vitest.livedb.config.ts`); plpgsql DO-block SQL corpus under `supabase/tests`; bash self-tests |
| **Config file** | `vitest.config.ts`, `vitest.livedb.config.ts` |
| **Quick run command** | `npx vitest run --config vitest.livedb.config.ts <file> -t "<arm>"` (local stack up) / `npx vitest run <static test>` |
| **Full suite command** | `bash scripts/local-stack/run.sh up && npm run test:live-db:ledger` plus `npx vitest run` plus both restore/normalize `--self-test`s |
| **Estimated runtime** | ~600 seconds (lane boot dominates) |

---

## Sampling Rate

- **After every task commit:** the single-file live-DB arm or static vitest for the touched surface, plus `npx tsx scripts/dump-sql-functions.ts --check` when a migration changed
- **After every plan wave:** full `npm run test:live-db:ledger`, both self-tests, `node scripts/lint-migration-data-dependence.mjs`, mutation runner for touched gates
- **Before `/gsd-verify-work`:** everything green on a clean tree; CI SHA-bound, with only the single pre-declared VAC-08 DRIFT row (CONTEXT D-24)
- **Max feedback latency:** 600 seconds

---

## Per-Task Verification Map

| Criterion | Behavior | Automated Command | Failing direction |
|-----------|----------|-------------------|-------------------|
| C1 harm | pre-fix verdict table (child `pending`, claimable before parent done) | `npx vitest run --config vitest.livedb.config.ts src/__tests__/compute-jobs-audit-2026-05-07-g10b.test.ts` | post-fix assertions RED on the pre-fix lane |
| C2 fan-in | wrapper + parents → `done_pending_children` | same file, arm P12 | neuter M1 (drop status from INSERT) → P12 RED |
| C3 overloads agree | ten-arg computes status; seven-arg byte-unchanged | M1 DO block on lane boot; `supabase/tests/test_enqueue_internal_destrict.sql` | body without status arm aborts the apply |
| C4 collision | in-flight → `{already_inflight,next_attempt_at}`; none → `{ok,job_id}` | `npx vitest run --config vitest.livedb.config.ts src/__tests__/request-allocator-holdings-sync-queued.test.ts` | remove prefetch → RED |
| C5 invariant | per-decision + NULL-md partial index named in errors | `npx vitest run --config vitest.livedb.config.ts src/__tests__/match-decisions-xor-rls.test.ts` | swap constraint name literal → RED |
| C6 ledger | ledger shrinks by 3; gate GREEN on exactly the remaining set | `npm run test:live-db:ledger`; `npx vitest run src/__tests__/live-db-execution-ledger.contract.test.ts` | closed line left in → STALE RED |
| REARM | restore → sink; preflight → unchanged; prod marker → refused | `bash scripts/restore-test-from-baseline.sh --self-test`; `bash scripts/test-only-normalize-analytics-url.sh --self-test` | drop the fragment → restore-mode arm RED |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Rebase onto a tree containing 164.4.2 (CONTEXT D-19) and re-measure ledger count/ceiling, floors, lane image, mutex-key counts
- [ ] Restore self-test fixture gains a `system_settings` table (research Q8 ripple)
- No framework installs.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| REARM on shared TEST | `[164.9-TEST-ANALYTICS-URL-REARM]` | writes a shared database; founder-owned (FC-3); blocked on baseline re-dump | founder dispatches `test-restore-from-baseline.yml` preflight then restore; arm D1 reads the live row |
| PROD/TEST apply of M1–M3 | all | merge auto-applies (FC-1) | founder merges after 3 reviewers; confirm push-run `test-db-drift` green SHA-bound |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 600s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
