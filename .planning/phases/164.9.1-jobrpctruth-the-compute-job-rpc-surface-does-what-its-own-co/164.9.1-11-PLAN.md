---
phase: 164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co
plan: 11
type: execute
wave: 7
depends_on: ["164.9.1-10"]
files_modified:
  - supabase/migrations/<ts1>_fanin_initial_status_10param.sql
  - supabase/migrations/<ts2>_allocator_sync_restore_inflight_prefetch.sql
  - supabase/migrations/<ts3>_bridge_outcomes_invariant_comments.sql
  - supabase/schema/functions/_enqueue_compute_job_internal.sql
  - supabase/schema/functions/request_allocator_holdings_sync.sql
  - src/app/api/allocator/holdings/sync/route.ts
  - scripts/test-only-normalize-analytics-url.sh
  - scripts/restore-test-from-baseline.sh
autonomous: false
requirements:
  - "[164.9-FANIN-STATUS-NEVER-SET]"
  - "[164.9-LIVEDB-RESIDUE-RPC-AND-INTENT]"
  - "[164.9-TEST-ANALYTICS-URL-REARM]"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-21: migration-reviewer, rls-policy-auditor and silent-failure-hunter have each reviewed M1, M2 and M3 (silent-failure-hunter also the route branch, the emitter and the restore concatenation) BEFORE any merge. They are dispatched by the ORCHESTRATOR, in ONE message, at this plan's checkpoint; the executor never self-reviews in their place."
    - "D-21: every round-1 finding is dispositioned FIX (root cause, with commit) or NOT-A-DEFECT (with evidence); only data-integrity or user-facing findings may block; nothing is deferred without a named phase."
    - "The orchestrator-level gates are RE-RUN by the executor itself after the round-1 fixes (lane ledger gate with all three migrations replayed, data-dependence linter, snapshot check, dedupe narrowed mutation run, both self-tests, route and restore static tests); a fix round is where regressions enter."
    - "D-07 / D-25 still hold after fixes: catalog-only DO blocks, 0 NEW, exactly three migrations, prod-body-ack lines re-derived if a function body changed in a fix."
  artifacts:
    - path: ".planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-11-SUMMARY.md"
      provides: "'## Round 1' findings table (reviewer, finding class, file by symbol, disposition, fixing commit) and re-run gate verdicts"
  key_links:
    - from: "orchestrator reviewer dispatch (Task 1 checkpoint)"
      to: "round-1 fixes (Task 2) and plan 12's round-2 re-review"
      via: "three per-reviewer report files outside the repo, named in the resume signal"
      pattern: "review-r1-"
---

> **164.4.2 pins are re-measured by whichever of 164.4.2 / 164.9.1 merges second; numbers here are READ at execution, never copied.**
> Checker iteration 1 moved the reviewer dispatch to an explicit ORCHESTRATOR checkpoint (an executor may lack the
> ability to spawn agents) and split round 2 into plan 12. This plan is `autonomous: false` for that reason only;
> the checkpoint is an orchestrator act, not a founder act.

<objective>
Run round 1 of the repo's mandatory three-reviewer gate on every migration this phase authors, before anything can
be merged (D-21; CLAUDE.md and the standing rule "3 reviewers BEFORE asking to apply any migration"). A merge of
`supabase/migrations/**` auto-applies to shared TEST and then PROD with no human reviewer gate, so this is the
last line of defence before FC-1.

Output: three reviewer reports, a dispositioned round-1 findings table, fixes committed, and re-run gate verdicts.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-CONTEXT.md
@.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-10-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: ORCHESTRATOR CHECKPOINT — dispatch the three reviewers in parallel, in one message (D-21)</name>
  <files>(none — orchestrator act; reports land outside the repo)</files>
  <read_first>
    - The three migrations and the two regenerated snapshots (paths by glob: `supabase/migrations/*_fanin_initial_status_10param.sql`, `*_allocator_sync_restore_inflight_prefetch.sql`, `*_bridge_outcomes_invariant_comments.sql`; `supabase/schema/functions/_enqueue_compute_job_internal.sql`, `request_allocator_holdings_sync.sql`).
    - `src/app/api/allocator/holdings/sync/route.ts`, `scripts/test-only-normalize-analytics-url.sh` (`--emit-restore-sql`, `--print-sink`), the emitter call in `scripts/restore-test-from-baseline.sh` `build_transaction`.
    - `164.9.1-CONTEXT.md` D-04..D-15, D-16a, D-17, D-23 (briefs must quote them verbatim, never paraphrase from memory).
  </read_first>
  <action>
    The executor STOPS here and returns this checkpoint. ACTOR: the orchestrator session that spawned the executor
    (not the founder, and not the executor). Per D-21 the orchestrator, in ONE message, dispatches three agents in
    parallel: `migration-reviewer` (M1, M2, M3: re-base correctness against the latest prior bodies, forward-only,
    lock_timeout, catalog-only DO blocks, prod-body-ack derivation, D-27/D-11 header records), `rls-policy-auditor`
    (SECDEF + `search_path` pin, ACL re-convergence against the default-grant event trigger, ownership check order in
    `request_allocator_holdings_sync`, no anon/PUBLIC EXECUTE), and `silent-failure-hunter` (the three migrations, the
    route's 409 branch, the emitter and the restore concatenation: swallowed errors, rc capture, fall-through branches,
    error-message leakage to users). Each brief carries the file list, the relevant D-xx decisions QUOTED from
    CONTEXT.md, and the instruction to report findings without editing any file. The orchestrator writes each report
    verbatim to `${TMPDIR:-/tmp}/gsd-164.9.1-11/review-r1-<reviewer>.md` (one file per reviewer, per-plan names so a
    parallel agent's scratch cannot collide), then resumes the executor.
  </action>
  <instructions>
    Claude (executor) already did: plans 01–10 are complete and the local lane, the ledger gate and the whole local gate
    sweep are green (plan 10 SUMMARY). The ONE thing the executor cannot do itself: spawn the three reviewer agents.
    Orchestrator: dispatch migration-reviewer, rls-policy-auditor and silent-failure-hunter in one message with the
    briefs above, save the three reports to the named files, then resume.
  </instructions>
  <verification>
    On resume the executor checks the three report files exist and are non-empty.
  </verification>
  <resume-signal>Type `reviews-delivered` once the three report files exist, or `cannot-dispatch: <reason>` (the executor then records it and STOPS; it never self-reviews in the reviewers' place).</resume-signal>
  <verify>
    <automated>D="${TMPDIR:-/tmp}/gsd-164.9.1-11"; for R in migration-reviewer rls-policy-auditor silent-failure-hunter; do test -s "$D/review-r1-$R.md" || { echo "MISSING $R"; exit 1; }; done; echo REVIEWS-R1-DELIVERED</automated>
    <fails_when>no "REVIEWS-R1-DELIVERED": at least one of the three report files is missing or empty (a reviewer did not run or its report was not saved).</fails_when>
  </verify>
  <acceptance_criteria>
    - Three non-empty report files exist, one per reviewer; the orchestrator's resume signal is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>All three reviewers have reviewed every migration once, dispatched by the orchestrator.</done>
</task>

<task type="auto">
  <name>Task 2: Disposition and fix every round-1 finding at its root cause, then re-run every local gate yourself (D-21, D-07, D-25)</name>
  <files>supabase/migrations/&lt;ts1&gt;_fanin_initial_status_10param.sql, supabase/migrations/&lt;ts2&gt;_allocator_sync_restore_inflight_prefetch.sql, supabase/migrations/&lt;ts3&gt;_bridge_outcomes_invariant_comments.sql, supabase/schema/functions/_enqueue_compute_job_internal.sql, supabase/schema/functions/request_allocator_holdings_sync.sql (plus any file a finding's fix touches)</files>
  <read_first>
    - The three report files `${TMPDIR:-/tmp}/gsd-164.9.1-11/review-r1-*.md`.
    - Plan 02's prod-body-ack recipe (Task 1 action) and plan 03 Task 1 (the twins' `find` text must stay unique).
  </read_first>
  <action>
    Per D-21, classify every finding: FIX (root cause, in this plan) or NOT-A-DEFECT (with the evidence). Only
    data-integrity or user-facing findings may BLOCK; everything else is fixed or dropped with a reason — never deferred
    without a named phase. Apply fixes. If a fix changes a function body: regenerate snapshots (`npm run schema:functions`),
    re-derive the `-- prod-body-ack:` hash per plan 02's recipe, and re-check the dedupe twins' `find` text is still unique.
    If a fix changes the 409 mapping or the restore, re-run their tests. Keep D-07 (catalog-only DO blocks) and D-25
    (exactly three migrations) true.

    Write a `## Round 1` section into this plan's SUMMARY: a findings table (reviewer, class, file by symbol, disposition,
    fixing commit) whose disposition cell holds exactly `FIX` or `NOT-A-DEFECT`, then one line `ROUND1-FIX-COUNT: <n>`
    where n is the number of table rows dispositioned `FIX` (plan 12 reads this line to decide whether round 2 is owed),
    and the gate verdicts from this task's verify. The public-repo rule applies (no ids, hosts, DSNs, home
    paths). Commit hygiene: node_modules symlink removed around each commit; `.planning/milestone.lock`,
    `.planning/state.json` and `.planning/phases/164.5.3-*` never staged; no CI skip token.
  </action>
  <precondition>Task 1 resumed with `reviews-delivered`; Docker up; no other local stack booted on this host; plan 01's SUMMARY carries a `PHASE-BASE:` line.</precondition>
  <verify>
    <automated>export PGBIN="${PGBIN:-/opt/homebrew/opt/postgresql@16/bin}"; S="${TMPDIR:-/tmp}/gsd-164.9.1-11"; mkdir -p "$S"; rm -f "$S/up.log" "$S/ledger.log" "$S/ldd.log" "$S/mut.log" "$S/n.log" "$S/r.log" "$S/v.log"; SUM=.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-11-SUMMARY.md; PB="$(sed -nE 's/^PHASE-BASE: ([0-9a-f]{40})$/\1/p' .planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-01-SUMMARY.md)"; bash scripts/local-stack/run.sh down >/dev/null 2>&1; bash scripts/local-stack/run.sh up > "$S/up.log" 2>&1 && grep -E 'baseline-replay:' "$S/up.log" | grep -q 'fanin_initial_status_10param' && grep -E 'baseline-replay:' "$S/up.log" | grep -q 'allocator_sync_restore_inflight_prefetch' && grep -E 'baseline-replay:' "$S/up.log" | grep -q 'bridge_outcomes_invariant_comments' && npm run test:live-db:ledger > "$S/ledger.log" 2>&1; G1=$?; bash scripts/local-stack/run.sh down >/dev/null 2>&1; test -n "$PB" && node scripts/lint-migration-data-dependence.mjs > "$S/ldd.log" 2>&1 && grep -qE '\b0 NEW\b' "$S/ldd.log" && npx tsx scripts/dump-sql-functions.ts --check && ADDED="$(git diff --name-only --diff-filter=A "$PB" -- supabase/migrations)" && test "$(printf '%s\n' "$ADDED" | awk NF | wc -l | tr -d ' ')" = 3 && bash scripts/test-only-normalize-analytics-url.sh --self-test > "$S/n.log" 2>&1 && bash scripts/restore-test-from-baseline.sh --self-test > "$S/r.log" 2>&1 && npx vitest run src/app/api/allocator/holdings/sync/route.test.ts src/__tests__/restore-test-from-baseline.test.ts --reporter=dot > "$S/v.log" 2>&1; G2=$?; node scripts/mutation-runner/run.mjs --file supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql > "$S/mut.log" 2>&1; M=$?; echo "status: lane+ledger=$G1 static=$G2 mutation=$M"; test "$G1" = 0 && test "$G2" = 0 && test "$M" = 2 && grep -qF 'No defects in the narrowed scope.' "$S/mut.log" && ! grep -qE 'no-red|wrong-first-failure|occurrence-mismatch|bad-file-ref|waiver' "$S/mut.log" && grep -qE '^## Round 1' "$SUM" && for R in migration-reviewer rls-policy-auditor silent-failure-hunter; do grep -qF "$R" "$SUM" || exit 1; done && FC="$(sed -nE 's/^ROUND1-FIX-COUNT: ([0-9]+)$/\1/p' "$SUM")" && test "$(printf '%s\n' "$FC" | awk NF | wc -l | tr -d ' ')" = 1 && test "$FC" = "$(awk '/^## Round 1/{f=1;next} /^## /{f=0} f' "$SUM" | grep -cE '^\|.*\|[[:space:]]*FIX[[:space:]]*\|')" && echo ROUND1-GATES-OK</automated>
    <fails_when>no "ROUND1-GATES-OK". Statuses are captured separately and the lane is torn down unconditionally before any verdict: lane+ledger != 0 means the lane did not replay all three migrations or the ledger gate is red; static != 0 means the phase base is unreadable, a NEW data-dependence refusal, stale snapshots, other than exactly three added migrations, either self-test red, or the route/restore static tests red; mutation != 2, a missing clean verdict or a named defect class means the dedupe twins no longer bite cleanly; or the SUMMARY lacks a `## Round 1` section naming all three reviewers, or its single `ROUND1-FIX-COUNT:` line is missing, doubled, or disagrees with the number of `FIX` rows in the Round 1 table.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY's `## Round 1` section names all three reviewers with their findings, each dispositioned FIX (with commit) or NOT-A-DEFECT (with evidence).
    - No finding is left undispositioned; no waiver, floor change or allowlist widening was used to clear one.
    - All gates in the verify are green after the last round-1 fix, re-run by this executor (not taken from a reviewer's report).
  </acceptance_criteria>
  <reversibility rating="reversible">Review and targeted fixes on an unpushed branch.</reversibility>
  <done>Every migration has been through all three reviewers once, every finding has a disposition, and the gates are green again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branch -> shared TEST / PROD | merge auto-applies with no human reviewer gate; this review is the control |
| orchestrator -> reviewer agents | briefs carry the premises the reviewers judge against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9.1-30 | Tampering | an unreviewed migration reaching PROD | high | mitigate | three reviewers dispatched by the orchestrator at an explicit checkpoint; report files verified to exist; gates re-run by the executor; FC-1 precondition cites plans 11 and 12 |
| T-164.9.1-32 | Repudiation | a brief with an unsourced premise becoming a shipped decision | medium | mitigate | briefs quote D-xx from CONTEXT verbatim |
| T-164.9.1-44 | Repudiation | an executor self-reviewing in place of the three reviewers | medium | mitigate | `cannot-dispatch` resume signal stops the plan; report files must exist before Task 2 runs |
</threat_model>

<verification>
- REVIEWS-R1-DELIVERED and ROUND1-GATES-OK printed.
</verification>

<success_criteria>
- Round 1 of the three-reviewer gate is complete, dispositioned and fixed, with gates re-run, before any founder merge.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Kind | Consumed by |
|----------|------|-------------|
| round-1 findings table + gate verdicts (SUMMARY) | review evidence | plan 12 (round 2), plan 14 FC-1 precondition, gsd-verifier |
| any fix commits | code | plan 12 re-review, FC-1 |

<output>
Create `.planning/phases/164.9.1-jobrpctruth-the-compute-job-rpc-surface-does-what-its-own-co/164.9.1-11-SUMMARY.md` when done.
</output>
