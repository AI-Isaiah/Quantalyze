---
phase: 167.2.1
fixer: A (build core, owner factsheet, copy)
fixed_at: 2026-09-26
review_paths:
  - 167.2.1-REVIEW.md
  - 167.2.1-REVIEW-SFH.md
iteration: 1
base_sha: 0f6741450
findings_in_scope: 12
fixed: 10
superseded: 2
skipped: 0
status: all_fixed
---

# Phase 167.2.1: round-1 fix report, fixer A

**Scope.** CR-01, WR-03, WR-04, IN-02, IN-03, IN-04 from `167.2.1-REVIEW.md`, and H-1, M-1, M-2,
M-3, M-5, L-1 from `167.2.1-REVIEW-SFH.md`. Files owned: `src/lib/factsheet/fetch-and-build-payload.ts`,
`build-payload.ts`, `composite-read-path.ts`, `src/app/factsheet/[id]/v2/page.tsx`,
`src/lib/status-surface-copy.ts` and their tests, plus `src/__tests__/factsheet-buildable-live-db.test.ts`.
Citations are by symbol.

**Method.** Each behaviour fix has a test that fails on the code before it. The fix was neutered
from a byte backup (or from `git show HEAD:<file>` for the previous commit), the test was run and
observed RED, then the backup was copied back and checked with `cmp`. `git checkout`, `restore` and
`stash` were not used.

## Fixed

| Finding | Commit | What changed | The test that bit (observed RED on the neutered code) |
|---|---|---|---|
| CR-01 + IN-02 | `ca87ab09b` | The four unbuildable notes state the reason from the **stored results**, not "the last computation". They use the active voice and interpolate `SUPPORT_EMAIL`. CONTEXT D-02 carries a dated revision. | `status-surface-copy.test.ts`: the 4 KCS12-*UNBUILDABLE* pins and `CR-01 COHERENCE`. `page.owner-compute-state.test.tsx`: `CR01-NEVER-STARTED` (KCS09-NEVER line + note) and `CR01-FAILED` (KCS09-FAIL-PERMANENT + composite note). 9 RED. |
| L-1 | `46e17081c` | `unbuildableNoteKindOf` lists every reason and ends in a `never` check. | `L-1 EXHAUSTIVE` (runtime: an unlisted reason throws). The compile half fired for real when M-3 added `malformed_series`: `status-surface-copy.ts: error TS2322: Type '"malformed_series"' is not assignable to type 'never'`. |
| WR-04 | `3e1723cb7` | `hasBuildableSeries` is now a type guard to a branded `BuildableSeries`. `buildFactsheetPayload` is overloaded non-null for one. Its body past the two gates moved into `buildFromBuildableSeries(): FactsheetPayload`. The build past the resolve moved into `buildFromResolved(): Promise<FactsheetPayload>`. The probe docstring names what actually fails. | Three neuters. (A) `return null` in `buildFromResolved` gave `tsc` `error TS2322: Type 'null' is not assignable to type 'FactsheetPayload'` and RED on the source scan. (B) a third `return null` gate in `buildFactsheetPayload` was RED on the scan. (C) `buildFromBuildableSeries` widened to `\| null`: **`tsc` stays silent** (overload compatibility is loose), and the scan is RED. That is why the scan exists beside the types. |
| WR-03 (+ IN-04, + M-5 owner half) | `b8ec66c93` | A module-private `resolveAndBuild` returns `{payload, reason:null} \| {payload:null, reason}`. `fetchAndBuildPayload` is a thin wrapper (the phase-148 single-declaration pin still holds). New export `fetchAndBuildPayloadWithReason` serves the owner lane only. `readOwnerBuildability` and its second resolve are gone, replaced by the pure `ownerBuildabilityOf(id, reason)`. | `page.owner-compute-state.test.tsx`: `S7-UNBUILDABLE-SHORT` (one admin `strategies` read), `S7-UNBUILDABLE-COMPOSITE` (one `csv_daily_returns` read, where the old code made two), `FULL-RENDER-NO-NOTE` and `S7-OWNER-BUILD-NOT-VISIBLE`. 4 RED on the old page. Lib: `WR-03 WITH-REASON` and the `resolveAndBuild` source scan. |
| M-1 | `71d869a48` | `resolveFactsheetInputs` takes `caller: "build" \| "probe"`. Every resolve log line reads `[factsheet] resolve(<caller>) — …` and carries `caller`. | `SFH M-1 LOG LABEL`. 1 RED. |
| M-2 | `20df828a7` | G0 splits into `error` and no row. A failed read now logs at error level and is **captured once in the stage** with tags `{stage:"factsheet-resolve", caller, reason:"read_error", code}`, using `"none"` when the error has no code. This covers the public and token builders, which captured nothing before. The owner lane stops capturing it a second time. `not_visible` stays uncaptured in the stage. | Lib `SFH M-2 READ-ERROR CAPTURE`. Page `S7-OWNER-BUILD-READ-ERROR` (exactly one event, code `57014`). 2 RED. |
| H-1 | `73b902ac5` | Every `composite_unbuildable` answer is captured once, at `level:"warning"`, with tags `{stage:"factsheet-resolve-composite", caller, gate}` and `gate ∈ headline / empty_series / short_series`. That includes a `csv_daily_returns` read outage folded into an empty series. The false "(→ Sentry)" claim in `readCompositeFactsheet`'s comments is corrected. | Lib `SFH H-1 COMPOSITE CAPTURE` (with a csv outage case), page `S7-UNBUILDABLE-COMPOSITE`. 2 RED. |
| M-3 | `0b6fcb6e3` | New reason `malformed_series`. The stored columns can hold at least `MIN_FACTSHEET_SERIES_POINTS` dated returns (`storedEntryCount`: array distinct dates, dict leaves, `returns_series` points minus 1) while normalization leaves fewer. In that case the stage answers `malformed_series`, maps it to the `cannot_build` note and captures it with its counts. A series that really is short keeps `too_few_points`. CONTEXT D-07 carries a dated revision. | Parity fixtures `one finite point plus one NaN` (was `too_few_points`), `three numeric-string values` and `non-positive wealth points`. Also `WR-03 WITH-REASON`, `SFH M-3 MALFORMED CAPTURE`, the kind-map table, and page `S7-MALFORMED`. 7 RED. |
| IN-03 | `6f1a4e51c` | One sentence in the `probeFactsheetBuildable` docstring: "buildable" means "no null exit", not "renders", so a builder throw at the recipient is outside the note's domain. Doc only. | n/a (doc) |
| (live-DB) | `301432989` | `factsheet-buildable-live-db.test.ts` `WITH-REASON`: on real rows, the reason-carrying build agrees with the probe. | Ran on a local lane stack: 7/7 passed. |

## Superseded, not separately fixed

- **IN-04** (two sequential owner reads). WR-03 removed the second read, so nothing is left to
  parallelise. `readOwnerPendingStatus` runs only on the null-payload branch. Running it alongside
  the build would add an RPC to every full owner render.
- **M-5, owner half.** WR-03 removed the owner lane's `buildable: true`-on-null race arm. WR-04 now
  holds the invariant it was meant to detect, by type and source scan. **The /strategies half**
  (a `console.warn` on the `not_computed` race arm in `StrategiesPage` `shareNotes`) is in
  `src/app/(dashboard)/strategies/page.tsx`, which fixer B owns. See below.

## For fixer B (runs next): what my changes require of `src/app/(dashboard)/strategies/**`

1. **`page.key-pill.test.tsx` is RED on 4 tests at my HEAD, and the cause is my change.** Three pin
   the old copy (`COMPUTED-UNBUILDABLE`, `COMPOSITE-UNBUILDABLE`, `PUBLIC-UNBUILDABLE`). The new
   literals are in `src/lib/status-surface-copy.test.ts` (`UNBUILDABLE_SHORT`,
   `UNBUILDABLE_COMPOSITE`, `PUBLIC_UNBUILDABLE_SHORT`). The fourth, `PROBE-READ-ERROR`, now sees
   **two** captures: the resolve stage's (M-2) and the page's.
2. **Drop the page's own `read_error` capture.** The stage captures it once, with the code, as
   `{stage:"factsheet-resolve", caller:"probe", reason:"read_error", code}`, and the fixture has no
   code, so it gets `"none"`. Keep the `console.error` if you want it. Keep capturing a probe
   **throw**, which the stage cannot see. IN-01 (`not_visible` → warn, no capture) is yours as filed.
3. **H-1 needs nothing on the page.** Every `composite_unbuildable` and `malformed_series` answer is
   captured in the stage. Adding a capture on the page would double it.
4. **New reason `malformed_series`.** The page's fall-through
   `unbuildableNoteKindOf(probe.reason)` already maps it to `cannot_build`. No code change is
   needed. Do not add a `default` arm anywhere that would hide the next reason (L-1).
5. **The exported API is unchanged for the list:** `probeFactsheetBuildable(id, visibility)` and
   `FactsheetBuildability` keep their shapes. `fetchAndBuildPayloadWithReason` is for the owner lane
   only.
6. **IN-03's cited location is your file.** Add the same sentence to the `shareNotes` comment
   (`if (probe.buildable) return [s.id, null]`): "buildable" means "no null exit", not "renders",
   so a builder throw at the recipient is outside the note's domain.
7. **M-5, /strategies half.** `console.warn` (no capture) on the `probe.reason === "not_computed"`
   race arm, so the race rate can be measured.
8. **H-2 and WR-02 are copy in MY file, `src/lib/status-surface-copy.ts`.** A public-url
   "could not check" line and softer public unbuildable lines have to be added there, with pins in
   `status-surface-copy.test.ts`. I have finished, so the file is free. CONTEXT D-02 now carries a
   revision note. Add yours beside it.

## Gates (verbatim, at `301432989`)

Ran in this worktree (the phase checkout), not an isolated fixer worktree. `node_modules` is a
symlink to the main checkout. It was removed before each commit and relinked after.

```
$ npx eslint <9 touched files>            → eslint exit=0
$ npx tsc --noEmit                         → tsc exit=0
$ npx vitest run src/lib/factsheet/fetch-and-build-payload.test.ts src/lib/factsheet/build-payload.test.ts "src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx" src/lib/status-surface-copy.test.ts
 Test Files  4 passed (4)
      Tests  149 passed (149)
$ npx vitest run --config vitest.livedb.config.ts src/__tests__/factsheet-buildable-live-db.test.ts   (own local lane stack)
 ✓ src/__tests__/factsheet-buildable-live-db.test.ts (7 tests) 567ms
 Test Files  1 passed (1)
      Tests  7 passed (7)
```

Wider regression (`src/lib/factsheet`, `src/app/factsheet`, `src/app/factsheet-share`,
`src/app/(dashboard)`, phase-147/148 guards, `no-store-coverage`, `status-surface-copy`,
`sentry-capture`): 262 files passed and 1 failed, 3807 tests passed and 4 failed. All 4 are in
`strategies/page.key-pill.test.tsx`, and item 1 above covers them.

## Recorded, not fixed

- `composite-read-path.ts` `readMtmSeries` and `readSmoothedSeries` make the same false
  "Log at ERROR (→ Sentry)" claim H-1 corrected in `readCompositeFactsheet`. Those degrades
  (charts stay cash) are not in any assigned finding, and I did not touch them. I booked no TODOS
  entry. The orchestrator decides.
- **Local-stack contention, measured.** My first two `run.sh up` attempts overlapped another
  agent's stack churn. They failed with "supabase start is already running" and then a realtime
  `schema_migrations_pkey` constraint error, and the lane's EXIT trap ran `sb stop --no-backup` on
  project `quantalyze`. That trap may have stopped a stack another agent was booting at the same
  moment. I cannot confirm it did or did not. I then waited for three consecutive quiet minutes
  before booting, ran the spec, and ran `run.sh down` on my own stack.

---

_Fixed: 2026-09-26_
_Fixer: Claude (gsd-code-fixer, round-1 fixer A)_
_Iteration: 1_
