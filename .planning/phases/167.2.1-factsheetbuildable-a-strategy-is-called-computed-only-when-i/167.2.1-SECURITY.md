---
phase: "167.2.1"
slug: "factsheetbuildable-a-strategy-is-called-computed-only-when-i"
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
block_on: high
created: "2026-09-26"
audited_at_sha: 199eb302e3c11dd244c43ddeea9df25a8b9ce16b
diff_base_sha: 01dcf1cc7ad0488d2c8687c4f69e6891a1be3b0e
---

# Phase 167.2.1 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

**Scope.** The audit covers the `<threat_model>` blocks of all 4 plans (21 threats,
T-167.2.1-01 to -21). It also covers five surfaces added after planning by the review-fix
rounds (`167.2.1-REVIEW-FIX-*.md`, `167.2.1-REVIEW-FIX-R2-*.md`), registered here as
T-167.2.1-22 to -26. Every verdict is bound to `audited_at_sha`. The phase diff is
`diff_base_sha...audited_at_sha`: 25 files under `src/`, and 0 files under `supabase/`.
There is no migration.

**Config.** ASVS L1, `block_on: high`. The auditor traced the high-severity rows to their
boundary (L2 depth), because the share page, the public factsheet cache and the
service-role route are public-facing or tenant-gating.

**Register drift, recorded rather than hidden.** T-18, T-19 and T-20 are recorded by their
CURRENT mechanism, because review rounds changed them after planning. For T-18, the public
cache key now carries `computedAt` and a read error is thrown rather than cached. For T-19,
the owner lane builds with `fetchAndBuildPayloadWithReason` instead of running the probe.
For T-20, the copy key is now `probeUnreadableShareNote` (KCS12-PROBE-UNREADABLE). Several
plan-time symbols named in those rows no longer exist.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| server page → service-role admin client (probe) | the injected visibility predicate is the only row gate | strategy rows, analytics presence |
| browser → `GET /api/keys/[id]/memberships` | an authenticated but untrusted caller names a key id in the path | key id in; membership name/status counts out |
| memberships route → service-role admin client | RLS does not apply; the explicit owner equality is the only tenant gate | `api_keys`, `strategy_keys` rows |
| `/strategies` RSC → service-role probe | per-row admin reads, gated by the owner list select and the owner predicate | fixed note keys only |
| owner v2 RSC → owner build | runs only on `lane === "owner"`, after the RLS-scoped ownership match | lane-local render props |
| per-request facts → id-keyed public cache (`buildFactsheetPayloadCached`) | viewer-independent key; nothing viewer-dependent may cross | public payload only |
| anonymous recipient → `/factsheet-share/[token]` | sessionless; the constant-time token match is the only authorisation | token (capability) |
| server errors → Sentry | captures from build callers and the probe aggregate | tags (codes, counts, internal strategy id on build callers only) |
| live-DB spec → local lane | fixtures are written with a key minted by the private local stack | test rows only |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation / Evidence | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-167.2.1-01 | Information disclosure | `probeFactsheetBuildable` | high | mitigate | The `visibility: StrategyVisibility` parameter is required and has no default. In `fetch-and-build-payload.test.ts`, the fake records `orFilters`, and REPRO-SINGLE-ONE-POINT, REPRO-COMPOSITE-PRE86, CONTROL-BUILDABLE and PARITY each assert that the filter names the session owner. | closed |
| T-167.2.1-02 | Information disclosure | probe routed via the cached wrapper | high | mitigate | The probe lives in `fetch-and-build-payload.ts`, whose closure `phase-148-owner-lane-cache-isolation.test.ts` walks and finds no cache API in. The probe's doc comment forbids `buildFactsheetPayloadCached`. The guard case "no file other than the factsheet v2 page mentions buildFactsheetPayloadCached" passes. | closed |
| T-167.2.1-03 | Tampering | probe/builder drift | medium | mitigate | `resolveAndBuild` and `probeFactsheetBuildable` both call the one resolve stage, `resolveFactsheetInputs`. `buildFromResolved` is declared non-null. Covered by the PARITY table, NO-NULL-AFTER-RESOLVE, WR-03 WITH-REASON and the source-scan cases. | closed |
| T-167.2.1-04 | Tampering | live spec writing to a shared DB | high | mitigate | The control sits at the lane boundary, not in the spec: `assertLocalApiUrl` (live-DB vitest global setup) refuses any non-loopback API URL. The spec runs only through `liveDbLaneCorpus()`, and `src/test-setup.ts` scrubs inherited Supabase env from the main config. `afterAll` deletes only the spec's own `strategyIds` and `ownerId`. | closed |
| T-167.2.1-05 | Denial of service | probe per-row admin select | low | accept | AR-01 | closed |
| T-167.2.1-06 | Information disclosure | memberships route, foreign key id | high | mitigate | `GET` runs `api_keys` `.eq("id", keyId).eq("user_id", user.id).maybeSingle()` before any `strategy_keys` read. An absent key and a foreign key get the same 404. Only `{name, status}` leave the route, and a foreign-owned embed is nulled. FOREIGN-KEY-404 and ABSENT-KEY-404 assert byte-identical bodies. | closed |
| T-167.2.1-07 | Tampering / Repudiation | silent RLS filter read as "no memberships" | high | mitigate | The route reads through `createAdminClient`, and a failed read returns 500 with a capture. `readKeyCompositeMemberships` (`ApiKeyManager.tsx`) maps any non-ok status, malformed body, throw or timeout to `"unreadable"`, which shows `DELETE_MEMBERSHIP_UNCHECKED_COPY`. | closed |
| T-167.2.1-08 | Spoofing | unauthenticated caller | medium | mitigate | `GET = withAuth(...)`. With no user it returns 401 with `NO_STORE_HEADERS`, and the client maps that to "unreadable". | closed |
| T-167.2.1-09 | Tampering | path id injection | medium | mitigate | `isUuid(keyId)` rejects a malformed id with 400 before `checkLimit` and before `createAdminClient` (BAD-ID-400). | closed |
| T-167.2.1-10 | Denial of service | route abuse | low | mitigate | `checkLimit(userActionLimiter, key-memberships:<user>)` feeds `rateLimitDenyJson` (RATE-LIMITED). On the client, `BASELINE_READ_BOUND_MS` and an `AbortController` bound the read. | closed |
| T-167.2.1-11 | Information disclosure | route Sentry captures | low | mitigate | Tags are `{route, stage, code}` only, and the coherence capture has counts in `extra`. No key id or user id is sent (OWNERSHIP-ERROR-500, MEMBERS-ERROR-500, COHERENCE-TRIPWIRE-LOUD). | closed |
| T-167.2.1-12 | Tampering | TOCTOU membership add before Delete | low | accept | AR-02 | closed |
| T-167.2.1-13 | Information disclosure | cached tenant data | medium | mitigate | Every route response carries `NO_STORE_HEADERS`, including withAuth's 401 and the limiter's deny. The route is listed in `no-store-coverage.test.ts`. | closed |
| T-167.2.1-14 | Information disclosure | probe as another tenant's buildability oracle | medium | mitigate | `StrategiesPage` probes only ids from the `.eq("user_id", user.id)` list select, with the predicate `withPublishedOrOwner(q, user.id)`. The only output is fixed copy (`probeUnreadableShareNote`, `recipientShareNoteFor`). | closed |
| T-167.2.1-15 | Repudiation / Tampering | probe error read as "has a factsheet" | medium | mitigate | A throw, a timeout, `read_error` and `not_visible` each render `probeUnreadableShareNote(mode)` (revised D-05) and never the "has a factsheet" note. They are counted by `captureProbeOutcomes` and `captureProbeThrows`. | closed |
| T-167.2.1-16 | Information disclosure | `/strategies` Sentry captures | low | mitigate | `captureProbeOutcomes` sends one event per load, with count tags and gate/source counts. `captureProbeThrows` sends at most `MAX_PROBE_THROW_EVENTS` grouped events. No id is sent. | closed |
| T-167.2.1-17 | Denial of service | one admin select per computed row | low | accept | AR-01. The select is now also bounded by `PROBE_CONCURRENCY`. | closed |
| T-167.2.1-18 | Information disclosure | public cached lane staleness | low | accept (mechanism drifted; see T-22) | The key is now `["factsheet-v2-payload-v6", id, computedAt]`, so a recompute gets a new entry, and a `read_error` throws `FactsheetReadError` instead of caching a null. The residual (AR-03) is the composite `csv_daily_returns`-outage variant, which is still cached as null. | closed |
| T-167.2.1-19 | Information disclosure | owner result leaking into the id-keyed cache | high | mitigate (mechanism drifted) | `FactsheetV2Page` calls `fetchAndBuildPayloadWithReason(id, withPublishedOrOwner(.., ownerUid))` only when `lane === "owner"`. `ownerBuildability` is lane-local, and `buildFactsheetPayloadCached` takes no visibility parameter. The phase-148 pins hold: exactly one `unstable_cache(`, a literal `withPublishedOnly` in the callback, and no visibility parameter on the wrapper. | closed |
| T-167.2.1-20 | Repudiation | owner-lane error shown as "has a factsheet" | medium | mitigate (copy key drifted) | `ownerBuildability?.unreadable` renders `probeUnreadableShareNote("mint-token")` (KCS12-PROBE-UNREADABLE). The capture happens once, in the resolve stage, under `caller === "build"`. | closed |
| T-167.2.1-21 | Information disclosure | TODOS.md on a public repo | low | mitigate | The phase's TODOS.md additions contain 0 UUID-shaped strings, 0 home paths and 0 emails. `check-planning-hygiene` reports OK. | closed |
| T-167.2.1-22 | Information disclosure | public factsheet `unstable_cache` key | high | mitigate | The `buildFactsheetPayloadCached(id, computedAt)` key parts are the route param id and the row's `strategy_analytics.computed_at`. No cookies, headers, searchParams or viewer id enter the key. The callback throws `FactsheetReadError` on `read_error`, and the page catches only that class and renders the placeholder uncached. Tests: KEY SHAPE ("nothing viewer-dependent"), READ-ERROR-NOT-CACHED, CACHED-NULL-THEN-COMPUTED, SAME-RUN-STILL-CACHED, and the phase-148 read_error case. | closed |
| T-167.2.1-23 | Information disclosure | owner lane vs public lane (`/factsheet/[id]/v2`) | high | mitigate | `lane = "owner"` is set only after an RLS-scoped `withPublishedOrOwner(.., user.id)` match on an unpublished row. The owner-only facts `ownershipMark`, `hasActiveShare`, `ownerSource`/`ownerApiKeyId`, `ownerStatus` and `ownerBuildability` are passed only as render props, never into the payload or the cache callback. An anonymous viewer gets `notFound()` before the owner lane. A published row stays on the public cached lane. | closed |
| T-167.2.1-24 | Information disclosure / Spoofing | share-token lane (`/factsheet-share/[token]`) | high | mitigate | The order is `checkLimit(publicIpLimiter)`, then `TOKEN_RE`, then `findShareMatch` (active rows only, `revoked_at` null, constant-time `verifyShareToken`). An unknown, revoked or read-failed token goes to `GONE_PATH`. The page builds with `fetchAndBuildPayload(.., withTokenBearer)`, uncached, under `dynamic = "force-dynamic"`, and passes no owner-only props. `page.cache-isolation.test.tsx` cases 1–4 cover: zero cache touches, an anonymous request for the same id still 404s, the public key is unchanged, and an anti-vacuity check. | closed |
| T-167.2.1-25 | Information disclosure | Sentry: `strategy_id` tag, probe aggregate, `scrubSentryEvent` | low | mitigate | The `strategy_id` tag appears only in the `caller === "build"` branches of `singleKeyUnbuildable`, `compositeUnbuildable` and the read-error branch of `resolveFactsheetInputs`. The probe caller returns `NotBuildableDetail` and captures nothing, and the `/strategies` aggregate carries counts only. `scrubSentryEvent` (`beforeSend`/`beforeSendTransaction`) scrubs share-path tokens from url, transaction, breadcrumbs, spans, trace data, `extra` and `tags`. The internal-UUID tag is intentional per CONTEXT "D-13 — Sentry `strategy_id` goes in `tags`" (renumbered from a duplicate D-12), and no user or key id is sent. | closed |
| T-167.2.1-26 | Denial of service / Repudiation | probe limiter and deadline | low | mitigate | `concurrencyLimiter(PROBE_CONCURRENCY)` is shared per page load. `FACTSHEET_PROBE_DEADLINE_MS` races the resolve, aborts the admin read and rejects with `FactsheetProbeTimeoutError`. That renders `probeUnreadableShareNote(mode)` and is counted as `timeout`, never as buildable. Each row has its own try/catch (SFH-R2 N-3 PROBE DEADLINE). Residual: see AR-04. | closed |

*Status: open · closed · open — below high threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-01 | T-167.2.1-05, T-167.2.1-17 | Each computed row on `/strategies` costs one admin select. It is the builder's own select, with no compute and no basis reads, and `PROBE_CONCURRENCY` bounds it. CONTEXT D-08 covers this, with its round-1 and round-2 revisions. The cost is booked as `[167.2.1-STRATEGIES-PROBE-COST]` in TODOS.md. | founder (CONTEXT D-08) | 2026-09-26 |
| AR-02 | T-167.2.1-12 | A membership can be added between the read and the Delete. The founder's decision is "warn, never block" (CONTEXT D-10). The DB guard still refuses to delete a PUBLISHED composite's member. Closing the window needs a migration. | founder (CONTEXT D-10) | 2026-09-26 |
| AR-03 | T-167.2.1-18 | Public cache staleness is now narrowed: `computedAt` is a key part, and a `read_error` is never cached. The residual is a composite `csv_daily_returns` outage, which is still cached as null until TTL. It is accepted under CONTEXT D-07 and owned by Phase 169 plan 04. | founder (CONTEXT D-07 / D-11) | 2026-09-26 |
| AR-04 | T-167.2.1-26 | The probe deadline cannot abort a composite's `csv_daily_returns` read. That read may finish in the background after the probe has already answered UNREADABLE. The user-facing answer is still correct and bounded. Only the server-side work outlives the deadline. | orchestrator (autonomous mode, non-blocking low) | 2026-09-26 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit 2026-09-26

| Metric | Count |
|--------|-------|
| Threats found | 26 |
| Closed | 26 |
| Open | 0 |

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-09-26 | 26 | 26 | 0 | gsd-security-auditor (verdict SECURED) via /gsd-secure-phase |

**Verification run (read-only).** 48 vitest files and 929 tests passed: the memberships
route, `fetch-and-build-payload`, the v2 page public-cache-key and owner-compute-state tests,
the share page cache-isolation and page tests, phase-148 isolation, no-store coverage and the
`/strategies` tests. The live-DB spec was not run in this audit, because it belongs to its own
lane. No implementation file was modified.

**Notes.** CONTEXT.md uses the label "D-12" for two decisions, the VERSION/CHANGELOG rule and
the Sentry `strategy_id`-as-tag rule. This file cites the Sentry one by its heading. The
double label is not a security gap. 0 unregistered threat flags: only plan 02's SUMMARY has a
`## Threat Flags` section, and it reads "None".

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-09-26
