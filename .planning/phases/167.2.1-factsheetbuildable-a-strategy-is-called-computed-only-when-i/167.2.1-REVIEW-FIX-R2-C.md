---
phase: 167.2.1
fixer: C (round 2 — share-page comment, TODOS bookings, CONTEXT decisions)
fixed_at: 2026-09-26
review_path: .planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-REVIEW-R2.md
iteration: 2
base_sha: 0779ce56b
findings_in_scope: 2
fixed: 2
skipped: 0
status: all_fixed
---

# Phase 167.2.1: Code Review Fix Report, fixer C (round 2)

**Source reviews:** `167.2.1-REVIEW-R2.md` (IN-03, IN-04) and `167.2.1-REVIEW-SFH-R2.md`
(N-2, N-3 and N-6, as context for the bookings and decisions).
**Where verification ran:** in the shared phase worktree that fixers A and B also use. This
fixer changed no behaviour: one source comment, `TODOS.md` and `CONTEXT.md`. The gate was
`scripts/check-planning-hygiene.ts`, and it was OK before each commit.

## Commits

| Commit | What |
|---|---|
| `a053c83e5` | IN-03: share page comment, and `DEF-148-A` closed |
| `9e12e006d` | Three `TODOS.md` bookings (IN-04 and two orchestrator items) |
| `f662adab2` | Round-2 orchestrator decisions (a) to (e) in `CONTEXT.md` |

## Fixed Issues

### IN-03: stale "id-ONLY" prose, and `DEF-148-A` still read as open

**Files modified:** `src/app/factsheet-share/[token]/page.tsx` (comment only), `TODOS.md`
**Commit:** `a053c83e5`
**Applied fix:**
- The SL-1 header now says what the code does. The id route's key carries no viewer: its
  `keyParts` are the id and `computed_at` (`buildFactsheetPayloadCached`). Before WR-02 the key
  was the id alone.
- `DEF-148-A` is closed with a dated paragraph naming Phase 167.2.1, `bea5fd373` and
  `page.public-cache-key.test.tsx`. The closure says it does not cover the R2 WR-02 cached-null
  residual. The "load-bearing corollary" is kept, because it still holds.
**Verification:** re-read (Tier 1). Comment-only, so no behaviour to test.

### IN-04: the client Sentry no-op, booked and not fixed

**Files modified:** `TODOS.md`
**Commit:** `9e12e006d`
**Applied fix:** booked as `[167.2.1-CLIENT-SENTRY-NOOP]`, with the count command, the two
candidate fixes (a scrubbed client init, or a tags-only rate-limited server endpoint), an
interim rule, a trigger, and the founder as owner.
- **The count differs from the reviewer's.** It is 12 non-test files with a real `"use client"`
  directive that call Sentry. The broad grep matches 18. Six of those only mention the directive
  in prose (a server route, a contracts registry, and four `src/lib` modules). The reviewer
  reported 16. The entry gives the command, says 12 is a floor, and says to regenerate the count.
- **No client init exists.** Checked on 2026-09-26: no `sentry.client.config.*`, no
  `instrumentation-client.ts`, and the only `Sentry.init` in `src/` is in `src/instrumentation.ts`.

## Also done (orchestrator brief, not review findings)

- **`[167.2.1-LOCAL-STACK-NO-CROSS-WORKTREE-LOCK]`**
  - `run.sh` reads `PROJECT_ID` from the committed `config.toml`, so container names are the
    same in every checkout. It has a `teardown()` trap but no lock.
  - Interim convention: take an `mkdir` lock directory before `up`, and remove it with `rmdir`
    after `down`. The entry gives no path.
  - The collision itself is cited as the orchestrator reported it. This fixer did not reproduce it.
- **`[167.2.1-STRATEGIES-PROBE-COST]`**
  - The cost ceiling is written in symbols: N heavy reads per load, and at worst
    ceil(N / `PROBE_CONCURRENCY`) times the per-probe deadline.
  - The upgrade path is to read a persisted buildable flag written by the analytics writer, keeping
    D-04 parity.
  - ⚠️ At the time of writing, the per-probe deadline (SFH-R2 N-3) was **not yet in**
    `src/app/(dashboard)/strategies/page.tsx`. The entry cites it by finding, not by value. If
    round 2 does not land it, remove that clause from the entry.
- **CONTEXT.md**
  - D-11 revision (a): the residual is dropped. Fixer C re-read `sync_strategy_analytics_status`
    branch (a) in migration `20260906120000` and confirmed that its `ON CONFLICT` arm sets
    `computed_at = now()`.
  - D-07 revision (b): the composite outage variant and SFH-R2 N-6's composite half go to
    Phase 169 plan 04.
  - D-05 revision (c), (d) and (e), and a D-08 pointer to it.
  - Deferred Ideas updated.
  - The CONTEXT entries are written as decisions. Whether fixers A and B implement them is for
    their reports and the re-review to show.

## Not done

Nothing in scope was skipped. No database command was run, nothing was pushed, and only this
fixer's paths were staged, with `git commit -- <paths>`.

---

_Fixed: 2026-09-26_
_Fixer: Claude (gsd-code-fixer), round 2, fixer C_
_Iteration: 2_
