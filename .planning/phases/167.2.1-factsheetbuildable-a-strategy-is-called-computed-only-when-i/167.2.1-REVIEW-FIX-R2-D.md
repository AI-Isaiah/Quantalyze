---
phase: 167.2.1-factsheetbuildable
fixer: D
round: 2
fixed_at: 2026-09-26
base_sha: 57d7d5e6b
iteration: 2
findings_in_scope: 3
fixed: 3
skipped: 0
status: all_fixed
---

# Phase 167.2.1: Code Review Fix Report, round 2, fixer D

This fixer finished the items fixers A and B left open. Their reports are
`167.2.1-REVIEW-FIX-R2-A.md` (it skipped IN-02, flagged the live-DB break, and recorded the
N-5 copy limit) and `167.2.1-REVIEW-FIX-R2-B.md`.

**Where verification ran:** in the pinned phase checkout, the one A, B and C used. No isolated
worktree was used. `node_modules` was a symlink to the primary checkout's while the gates ran,
and it was removed before each commit. The live-DB spec ran on a private local stack.

## Commits

| Commit | Item |
|---|---|
| `c77f4c3b0` | IN-02: the build-caller captures carry the tag `strategy_id` |
| `de34ff19f` | Live-DB spec `PROBE-COMPOSITE` expects `gate: "headline"` |
| `fdfce3c3b` | N-5: the SHORT note speaks only of the results the factsheet is built from |

## 1. REVIEW-R2 IN-02: build-caller captures name the strategy

**Files:** `fetch-and-build-payload.ts`, `fetch-and-build-payload.test.ts`,
`page.owner-compute-state.test.tsx`

- Three captures in the resolve stage now tag `strategy_id: id`. They are G0's read error in
  `resolveFactsheetInputs`, `compositeUnbuildable` and `singleKeyUnbuildable`. Each fires only
  when `caller === "build"`. The two helpers now take `id` as their first argument.
- A probe still captures nothing in the stage. `StrategiesPage`'s one outcomes event per page
  load stays id-free. `WR01-OUTCOMES-ONE-EVENT` already checks that no row id appears in the
  error or its context, and that there is exactly one event.
- Two comments that the change made false were corrected in the same commit:
  - the `compositeUnbuildable` docstring said "with tags only";
  - the G0 comment said "a code is not identifying".
- **The brief missed two exact `toEqual`s, in the lib test.** They sit in
  `SFH M-2 READ-ERROR CAPTURE` and in the `SFH H-1 COMPOSITE CAPTURE` loop, and both were
  updated. `SFH M-3 MALFORMED CAPTURE` now asserts `ctx.tags.strategy_id`. The brief's two page
  cases were updated: the `factsheet-resolve-composite` "headline" case and the `57014`
  read-error case.
- **Neuters.** The tag was dropped from one site at a time, each time restored from a `cp`
  backup and checked with `cmp`. Every run went RED:
  - malformed: 1 test failed;
  - composite: 2 failed (the lib test and `S7-UNBUILDABLE-COMPOSITE`);
  - read error: 2 failed (the lib test and `S7-OWNER-BUILD-READ-ERROR`).
- **This departs from repo precedent, and the departure is deliberate.**
  `finalize-wizard` and `csv-finalize` put `strategy_id` in `extra`. This change follows the
  brief and puts it in `tags`, where it is indexed and searchable. The build lane includes the
  owner lane, so the tag also lands on UNPUBLISHED rows. IN-02 asked to "decide explicitly for
  a published row", so this answers a broader question than it asked. `scrubSentryEvent`
  scrubs share-path capabilities from tags, and an internal UUID is not one. If the founder
  wants published rows only, or `extra` only, the change is one line per site.

## 2. Live-DB `PROBE-COMPOSITE`

**File:** `src/__tests__/factsheet-buildable-live-db.test.ts`

- The expected object now includes `gate: "headline"`. It is still an exact `toEqual`.
- **The spec was run:**
  - The shared `mkdir` lock was taken, and `docker ps` showed 0 supabase containers.
  - `scripts/local-stack/run.sh up` exited 0, and the lane replayed 0 migrations.
  - `vitest --config vitest.livedb.config.ts` on the spec passed 7 of 7.
  - The same run with the old expected object failed 1 of 7, `PROBE-COMPOSITE` only.
  - `down` exited 0 and left 0 supabase containers, and the lock was removed.
- `WITH-REASON` was not affected: `fetchAndBuildPayloadWithReason` (through `resolveAndBuild`)
  returns the reason alone and carries no detail.

## 3. SFH-R2 N-5 copy

**Files:** `status-surface-copy.ts`, `status-surface-copy.test.ts`, `page.key-pill.test.tsx`,
`page.owner-compute-state.test.tsx`, `167.2.1-CONTEXT.md`

- **The old wording made a claim about all stored data.** It read "Its stored results hold
  fewer than 2 days of returns". A row with one valid `daily_returns` entry beside a long
  `returns_series` holds many days, so the sentence was false about it. The wording was
  therefore changed.
- The three SHORT lines now read: "The stored results we build its factsheet from hold fewer
  than 2 days of returns, and a factsheet needs at least 2."
  - It keeps "stored results", which the `CR-01 COHERENCE` test requires.
  - It keeps the `MIN_FACTSHEET_SERIES_POINTS` threshold that `DRIFT` pins.
  - It is in the active voice, per DESIGN.md § Voice / microcopy.
  - The COMPOSITE lines and the opening clauses did not change.
- **New test `N-5 SCOPE`.** It requires the scoped phrase and refuses the old all-data claim.
  All 7 hard-coded pins were updated.
- **Neuter.** The old wording was put back in the source only, and 9 tests went RED. The
  source was restored from a `cp` backup and checked with `cmp`.
- `CONTEXT.md` records this as a dated revision under D-02. The copy was a locked decision
  there.

## Gates

- `npx tsc --noEmit` exited 0.
- `vitest run` covered `src/lib/factsheet`, `src/app/(dashboard)/strategies`,
  `src/app/factsheet`, `src/app/factsheet-share`, `src/lib/status-surface-copy.test.ts` and
  `src/__tests__/phase-148*`: 108 files and 1652 tests, all passed.
- `check-planning-hygiene` returned OK after every `git add`.
- The skip-token sweep returned 0 on every commit message.
- No `git checkout`, `git restore` or `git stash` was used.
- No command touched a remote database, and nothing was pushed.

---

_Fixer: Claude (gsd-code-fixer), round 2, fixer D_
