---
phase: 167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i
plan: 03
type: execute
wave: 2
depends_on: ["167.2.1-01"]
files_modified:
  - src/lib/status-surface-copy.ts
  - src/lib/status-surface-copy.test.ts
  - src/app/(dashboard)/strategies/page.tsx
  - src/app/(dashboard)/strategies/page.key-pill.test.tsx
autonomous: true
requirements: [SC3, SC2]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC3: on /strategies, an unpublished row whose analytics read computed but whose factsheet cannot build (a single-key row with one dated point), with a finished chain job, shows KCS12-UNBUILDABLE-SHORT beside its share control, matching the share page's 'not available' card for that row. The case COMPUTED-UNBUILDABLE was observed RED against today's page (no note rendered) before the fix, and is GREEN after it."
    - "D-02: the four new KCS-12 rows (UNBUILDABLE-SHORT, UNBUILDABLE-COMPOSITE, PUBLIC-UNBUILDABLE-SHORT, PUBLIC-UNBUILDABLE-COMPOSITE) render byte-for-byte as recorded in 167.2.1-CONTEXT.md D-02, pinned by hand-typed oracles; the '2' in the SHORT lines equals MIN_FACTSHEET_SERIES_POINTS."
    - "D-02 selection: an unbuildable public-url row takes its PUBLIC-UNBUILDABLE line with no RPC; an unbuildable mint-token row takes UNBUILDABLE on arm not_available, keeps KCS12-MINT-A on arm in_progress, and keeps KCS12-UNREADABLE on arm unreadable."
    - "D-05: a probe that throws, or answers read_error or not_visible, renders recipientShareNote(mode, 'unreadable') and is logged and captured with tags only (stage factsheet-probe), never 'no note'; a not_computed answer takes the existing uncomputed path."
    - "D-08: only rows the embed calls computed are probed; uncomputed rows keep today's path exactly and are never probed; a computed AND buildable row shows no note and makes no RPC; the probe runs with the owner predicate withPublishedOrOwner(q, session user id)."
    - "Neuter to RED to restore is recorded in the SUMMARY for: the list back on the status-only filter (COMPUTED-UNBUILDABLE RED), a probe throw folded into 'buildable' (PROBE-THROWS RED), the selection ignoring the unbuildable kind (COMPUTED-UNBUILDABLE RED on the MINT-B text)."
  artifacts:
    - path: src/lib/status-surface-copy.ts
      provides: "UnbuildableNoteKind, unbuildableNoteKindOf, recipientShareNoteFor and the four D-02 strings"
    - path: src/app/(dashboard)/strategies/page.tsx
      provides: "the share-note derivation that probes computed rows with probeFactsheetBuildable"
    - path: src/app/(dashboard)/strategies/page.key-pill.test.tsx
      provides: "COMPUTED-UNBUILDABLE (RED-first) and the probe-arm cases"
  key_links:
    - from: src/app/(dashboard)/strategies/page.tsx
      to: src/lib/factsheet/fetch-and-build-payload.ts
      via: "import { probeFactsheetBuildable }, called with (q) => withPublishedOrOwner(q, user.id)"
      pattern: "probeFactsheetBuildable\\("
    - from: src/app/(dashboard)/strategies/page.tsx
      to: src/lib/status-surface-copy.ts
      via: "recipientShareNoteFor(mode, arm, unbuildableNoteKindOf(reason))"
      pattern: "recipientShareNoteFor\\("
---

<objective>
Make the `/strategies` share note tell the truth about a row that is computed but cannot build
(SC3, 167.2 review WR-02). The list decides with plan 01's probe, which is the builder's own code.
It then says what the recipient sees, in the precise D-02 copy.

Purpose: today such a row shows no note, which implies a working factsheet, while its share-link
recipient lands on "not available". This is the only surface that lies (RESEARCH signal inventory).
Output: the four D-02 copy rows, the probe-based derivation on the list, and a list test observed
RED against today's code.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-CONTEXT.md
@.planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-RESEARCH.md
@src/app/(dashboard)/strategies/page.tsx
@src/lib/status-surface-copy.ts
@DESIGN.md
Read once plan 01 has landed (not @-referenced here): `167.2.1-01-SUMMARY.md` in this phase
directory, and the plan-01 exports `probeFactsheetBuildable`, `NotBuildableReason` and
`MIN_FACTSHEET_SERIES_POINTS`.

<!-- verify-symbol: src/app/(dashboard)/strategies/page.tsx readRecipientArm -->
<!-- verify-symbol: src/app/(dashboard)/strategies/page.tsx computationStatusOf -->
<!-- verify-symbol: src/app/(dashboard)/strategies/page.tsx shareNotes -->
<!-- verify-symbol: src/lib/status-surface-copy.ts recipientShareNote -->
<!-- verify-symbol: src/lib/status-surface-copy.ts MINT_TOKEN_NOTES -->
<!-- verify-symbol: src/lib/status-surface-copy.ts PUBLIC_URL_NOTE -->
<!-- verify-symbol: src/lib/compute-state.ts RecipientArm -->
<!-- verify-symbol: src/lib/share-affordance.ts shareAffordanceMode -->
<!-- verify-symbol: src/lib/visibility.ts withPublishedOrOwner -->
<!-- verify-symbol: src/app/(dashboard)/strategies/page.key-pill.test.tsx noteOf -->
</context>

<hard_constraints>
Carried into every task of this plan:
- **Worktree setup:** symlink the main checkout's `node_modules`, finding the path at run time with
  `git worktree list`. TEST a command. Stage by explicit path only. Remove the symlink before every
  commit. ⛔ Never write the resolved path into a tracked file.
- **Verify logs:** `export FB_SP=<your session scratchpad directory>` first. Never use /tmp. ⛔ Never
  write the expanded path into a tracked file.
- **Precondition:** plan 01 is merged into this branch. `grep -c 'export async function probeFactsheetBuildable' src/lib/factsheet/fetch-and-build-payload.ts`
  returns 1. If it does not, halt.
- **Copy is locked.** Use exactly the four D-02 strings from 167.2.1-CONTEXT.md, and change no
  existing KCS-12 string. DESIGN.md governs the voice. This plan adds copy to an existing band and no
  new visual element.
- ⛔ Do not re-implement any builder gate in the page (RESEARCH anti-pattern). Do not add a narrower
  `daily_returns` select to the page (the phase-147 guard). Never route the probe through a cache
  (D-11).
- **No database command against any remote.** Unit tests only. No migration.
- **Public repo:** synthetic ids and names only.
- ⛔ Never `git checkout -- <path>`, `git restore`, `git stash` or `npm version`. Take byte backups
  for neuter checks. No VERSION or CHANGELOG edit (D-12). No CI skip token in any commit message.
- Every moved pin keeps a lineage comment naming the 167.2.1 decision that moved it. Cite by SYMBOL.
</hard_constraints>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): a computed single-key row that cannot build shows KCS12-UNBUILDABLE-SHORT on /strategies, from the admin read through the probe to the rendered note (RED first)</name>
  <files>src/lib/status-surface-copy.ts, src/lib/status-surface-copy.test.ts, src/app/(dashboard)/strategies/page.tsx, src/app/(dashboard)/strategies/page.key-pill.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/page.tsx: the list select with the `strategy_analytics ( computation_status )` embed, `computationStatusOf`, `readRecipientArm`, and the `shareNotes` derivation with its "Known limit" comment
    - src/app/(dashboard)/strategies/page.key-pill.test.tsx: the module mocks, the `state` object and `beforeEach` (the session user id), `row`, `renderPage`, `noteOf`, and the whole `KCS-12` describe including COMPUTED-NO-NOTE
    - src/lib/status-surface-copy.ts: `MINT_TOKEN_NOTES`, `PUBLIC_URL_NOTE`, `recipientShareNote`, `SHARE_CARD_COPY`; and src/lib/status-surface-copy.test.ts (the KCS12 oracles)
    - src/lib/factsheet/fetch-and-build-payload.ts as left by plan 01 (`probeFactsheetBuildable`, `NotBuildableReason`) and src/lib/factsheet/fetch-and-build-payload.test.ts (its admin fake, to copy)
    - 167.2.1-CONTEXT.md D-02, D-05, D-08; RESEARCH § Pattern 2 and Pitfall 3
  </read_first>
  <action>
    Re-read the four files at HEAD. Run `node scripts/verify-plan-anchors.mjs` on this plan file and halt on non-zero.
    RED FIRST (RESEARCH Pitfall 3). In page.key-pill.test.tsx:
    - Add a module-level `vi.mock("@/lib/supabase/admin", …)` whose `createAdminClient` returns a chainable fake. It records every `from(table)` and the `.or(filter)` string. It answers the strategies `.maybeSingle()` from a per-id `state.adminRows` map. With no entry it answers a computed row with a 30-point series, so the other describes' computed rows stay "buildable" and render as they do today.
    - `withPublishedOrOwner` fails closed on a non-UUID. Change the `beforeEach` session id to a UUID-shaped synthetic value, with a lineage comment saying why. Nothing else in the file names the old id; confirm that with grep.
    - Add COMPUTED-UNBUILDABLE inside the KCS-12 describe: an unpublished row whose embed reads 'complete', whose admin row is computed with ONE point, and whose RPC answers a finished chain job. Its note must equal the hand-typed UNBUILDABLE_SHORT oracle from CONTEXT D-02.
    Run it against TODAY's page before touching production code. It must fail on the ASSERTION: no note renders, because the page imports nothing from the admin module yet. It must not fail on an import. Record that RED in the SUMMARY as the SC3 evidence.
    Copy (D-02), in status-surface-copy.ts:
    - add `export type UnbuildableNoteKind = "too_short" | "cannot_build"`;
    - add `export function unbuildableNoteKindOf(reason: NotBuildableReason): UnbuildableNoteKind | null`, with a TYPE-ONLY import of `NotBuildableReason` from the plan-01 module: too_few_points → too_short, composite_unbuildable → cannot_build, anything else → null;
    - add module-private `MINT_UNBUILDABLE_NOTES` and `PUBLIC_UNBUILDABLE_NOTES`, keyed by kind, holding exactly the four D-02 strings;
    - add `export function recipientShareNoteFor(mode, arm, kind: UnbuildableNoteKind | null): string`. It returns `recipientShareNote(mode, arm)` when kind is null. Otherwise it returns the PUBLIC line for `public-url`, the MINT line for `mint-token` with arm `not_available`, and `recipientShareNote(mode, arm)` for any other arm.
    Extend the doc comment on `recipientShareNote`'s family with the four new ids.
    In status-surface-copy.test.ts, add hand-typed oracles for the four strings and the selection table. Add a drift pin: import `MIN_FACTSHEET_SERIES_POINTS` and assert both SHORT strings contain `fewer than ${MIN_FACTSHEET_SERIES_POINTS} days` and `at least ${MIN_FACTSHEET_SERIES_POINTS}`.
    Page (per D-08 and D-05): replace the status-only filter in the `shareNotes` derivation. Map EVERY listed row to `[id, note | null]`, then drop the nulls.
    - An uncomputed row, by `computationStatusOf`, takes today's code path unchanged. Extract it into one local function so both branches call the same arm read.
    - A computed row awaits `probeFactsheetBuildable(s.id, (q) => withPublishedOrOwner(q, user.id))` inside try/catch. buildable gives null. A throw, `read_error` or `not_visible` gives `recipientShareNote(mode, "unreadable")`, plus a console.error and `captureToSentry` with tags `{ route: "strategies/page", stage: "factsheet-probe" }` only. `not_computed` takes the uncomputed path.
    - Otherwise compute the kind with `unbuildableNoteKindOf`. A `public-url` row renders `recipientShareNoteFor(mode, "not_available", kind)` with no RPC (167.2 IN-04). A `mint-token` row calls `readRecipientArm` with the same stitch inputs as today, then renders `recipientShareNoteFor(mode, arm, kind)`.
    Keep `Promise.all`. Replace the "Known limit" sentences with a lineage comment: WR-02 is closed by 167.2.1 D-04 and D-08, and the list and the share page now decide from the builder's own code.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/page.key-pill.test.tsx" src/lib/status-surface-copy.test.ts --reporter=verbose > ${FB_SP:?FB_SP_unset}/fb-03-t1.log 2>&1; RC=$?; tail -60 ${FB_SP:?FB_SP_unset}/fb-03-t1.log; test "$RC" = 0 && grep -q 'COMPUTED-UNBUILDABLE' ${FB_SP:?FB_SP_unset}/fb-03-t1.log && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${FB_SP:?FB_SP_unset}/fb-03-t1.log && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records COMPUTED-UNBUILDABLE failing on its assertion against the pre-change page, then passing.
    - `grep -c 'probeFactsheetBuildable(' "src/app/(dashboard)/strategies/page.tsx"` is 1, and `grep -c 'withPublishedOrOwner' "src/app/(dashboard)/strategies/page.tsx"` is at least 1.
    - `grep -c 'Known limit' "src/app/(dashboard)/strategies/page.tsx"` is 0.
    - Every existing KCS-12 oracle string in both test files is unchanged. Write the diff first: `git diff "$(git merge-base HEAD origin/main)" -- src/lib/status-surface-copy.test.ts "src/app/(dashboard)/strategies/page.key-pill.test.tsx" > "${FB_SP:?FB_SP_unset}/fb-03-oracle.diff"` must exit 0. Then, for each NAME in MINT_A, MINT_B, UNREADABLE and PUBLIC, `grep -c "^-  const NAME =" "${FB_SP:?FB_SP_unset}/fb-03-oracle.diff"` prints 0.
  </acceptance_criteria>
  <done>The one-point computed row reads KCS12-UNBUILDABLE-SHORT on the list, proven RED against today's code, and the copy family carries the four D-02 rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: every probe outcome and share mode on the list, with the probe never reaching uncomputed rows</name>
  <files>src/app/(dashboard)/strategies/page.key-pill.test.tsx, src/app/(dashboard)/strategies/page.tsx</files>
  <read_first>
    - both files as left by Task 1
    - src/app/factsheet-share/[token]/page.tsx: the null-payload arm choice (`recipientArm(readShareComputeState(...))` → `SHARE_CARD_COPY`), to confirm each list case below agrees with what that page renders for the same row
  </read_first>
  <behavior>
    - COMPUTED-NO-NOTE is amended to mean "computed AND buildable". It shows no note and no RPC, with a lineage comment naming 167.2.1 D-08. Its fixture uses the default buildable admin row.
    - COMPOSITE-UNBUILDABLE: computed composite, `metrics_json_by_basis` null, finished job. The note is UNBUILDABLE_COMPOSITE.
    - PUBLIC-UNBUILDABLE: a published computed row with one point. The note is PUBLIC_UNBUILDABLE_SHORT, and `state.rpcCalls` is empty.
    - UNBUILDABLE-IN-PROGRESS: unbuildable, with a running chain job made recent through `isoMinutesAgo`. The note is MINT_A.
    - UNBUILDABLE-RPC-ERROR: unbuildable, and the RPC errors. The note is UNREADABLE.
    - PROBE-THROWS: the admin factory throws. The note is UNREADABLE, console.error was called, and `captureToSentry` was called once with tags `{ route: "strategies/page", stage: "factsheet-probe" }` and no id.
    - PROBE-READ-ERROR: the admin strategies select answers an error. Same as PROBE-THROWS.
    - PROBE-NOT-COMPUTED: the embed says complete, but the admin row says failed, with a failed chain job. The row takes the uncomputed path and renders MINT_B.
    - NOT-PROBED-UNCOMPUTED: two uncomputed rows and one computed row. The admin fake saw exactly one strategies read, for the computed id.
    - OWNER-PREDICATE: the admin fake's recorded `.or` filter contains `user_id.eq.<the session id>`.
  </behavior>
  <action>
    Write the behavior cases and run them. Fix only the page when a case fails, never an oracle.
    Confirm by reading the share page that each case's note matches the card that page would show for the same row: unbuildable plus not_available gives the "not available" card; in_progress gives the "being prepared" card. Record the check in the SUMMARY.
    Neuter checks, each against a fresh byte backup that is re-taken after every edit:
    - (1) restore the status-only filter, so computed rows are never probed: COMPUTED-UNBUILDABLE and COMPOSITE-UNBUILDABLE go RED;
    - (2) map a probe throw to "no note": PROBE-THROWS goes RED;
    - (3) call `recipientShareNote(mode, arm)` instead of `recipientShareNoteFor`: COMPUTED-UNBUILDABLE goes RED, showing the MINT-B text.
    Restore each, confirm GREEN, and record all three in the SUMMARY.
    Then run the wider regression set in the verify command.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies" src/lib/status-surface-copy.test.ts "src/app/factsheet-share/[token]" src/lib/factsheet/fetch-and-build-payload.test.ts src/__tests__/phase-148-owner-lane-cache-isolation.test.ts src/__tests__/phase-147-series-resolution-guards.test.ts --reporter=verbose > ${FB_SP:?FB_SP_unset}/fb-03-t2.log 2>&1; RC=$?; tail -60 ${FB_SP:?FB_SP_unset}/fb-03-t2.log; test "$RC" = 0 && grep -q 'PROBE-THROWS' ${FB_SP:?FB_SP_unset}/fb-03-t2.log && grep -q 'NOT-PROBED-UNCOMPUTED' ${FB_SP:?FB_SP_unset}/fb-03-t2.log && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${FB_SP:?FB_SP_unset}/fb-03-t2.log && npm run typecheck && npx eslint "src/app/(dashboard)/strategies/page.tsx" "src/app/(dashboard)/strategies/page.key-pill.test.tsx" src/lib/status-surface-copy.ts src/lib/status-surface-copy.test.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All ten behavior cases pass by name. The SUMMARY records the three neuter runs, each RED and then restored GREEN.
    - The SUMMARY records the share-page agreement check for each unbuildable arm.
  </acceptance_criteria>
  <done>The list's note agrees with the share page for every probe outcome and share mode; errors never read as "has a factsheet"; uncomputed rows behave exactly as before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /strategies RSC → service-role probe | per-row admin reads, gated only by the injected owner predicate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.2.1-14 | Information disclosure | probe used as a buildability oracle for another tenant | medium | mitigate | Only ids from the list select, which is filtered with `.eq("user_id", user.id)`, are probed, and the predicate is `withPublishedOrOwner(q, user.id)` (OWNER-PREDICATE case). The output is one of the fixed notes. |
| T-167.2.1-15 | Repudiation / Tampering | probe error read as "has a factsheet" | medium | mitigate | D-05: a throw, `read_error` or `not_visible` renders UNREADABLE and is captured (PROBE-THROWS, PROBE-READ-ERROR, and neuter 2). |
| T-167.2.1-16 | Information disclosure | Sentry capture | low | mitigate | Tags only; the test asserts no id is in the capture. |
| T-167.2.1-17 | Denial of service | one admin select per computed row | low | accept | The builder's own select, with no compute and no basis reads. D-08 records the bound-concurrency fallback. |
| T-167.2.1-18 | Information disclosure | public cached lane staleness | low | accept | D-11: a published row's note can lead its cached public page by up to the 3600 s TTL (Phase 148 TODO). |
</threat_model>

<verification>
- COMPUTED-UNBUILDABLE was observed RED against today's page and is GREEN now (SC3).
- The ten Task 2 cases pass, and the three neuter runs are recorded.
- `node scripts/verify-plan-anchors.mjs .planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-03-PLAN.md` exits 0 at the start.
</verification>

<success_criteria>
The `/strategies` note for a computed-but-unbuildable row names what the recipient sees and why,
in the D-02 copy. No row whose factsheet cannot build reads as "has a factsheet".
</success_criteria>

<output>
Create `.planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-03-SUMMARY.md` when done.
</output>
