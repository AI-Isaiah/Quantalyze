# Phase 167.2.1: FACTSHEETBUILDABLE — a strategy is called computed only when its factsheet can actually build - Context

**Gathered:** 2026-09-25
**Status:** Ready for planning
**Decided at sha:** 07c30f0fc (the research commit; `167.2.1-RESEARCH.md` read every anchor at 01dcf1cc7)
**Mode:** Autonomous. The orchestrator took the three open questions of `167.2.1-RESEARCH.md`
(OQ1-OQ3) as D-01 to D-03 on 2026-09-25. The planner recorded the rest (D-04 to D-12) from the
research evidence. No founder prompt was issued. Every decision below is reversible, and none is
founder-owned.

<domain>
## Phase Boundary

**One claim: a surface that says "this strategy has a factsheet" agrees with what the share-link
recipient actually sees.**

1. **`/strategies` (S5 share note, 167.2 review WR-02).** A row whose analytics status is computed,
   but whose factsheet cannot build, shows no note today. Its recipient lands on the "not available"
   card. After this phase the list decides buildability with the builder's own code and shows a note
   that says what the recipient sees.
2. **The owner factsheet's pending panel (S7).** The same row shows the same note there, so the two
   owner surfaces cannot disagree about it.
3. **The key-card Delete warning (167.2 review R2-L4(b), ROADMAP criterion 5).** The composite
   membership read moves server-side, so an RLS policy that filters the read to an empty list,
   without an error, cannot hide the warning.

**Out of scope:** the share page itself (it is the ground truth and does not change); the public
cached lane's staleness (Phase 148 TODO, D-11); the discovery detail page's second assembly of the
builder (D-03, booked to Phase 169); scalar KPIs on discovery or my-strategies versus the factsheet
(Phase 169 SC4); the wizard's in-page preview (RESEARCH A5); any migration (D-01, D-04); any change
to what the analytics worker writes.

</domain>

<decisions>
## Implementation Decisions

- **D-01 (2026-09-25, orchestrator, OQ1): criterion 5 is closed by a server route, not a migration.**
  A new `GET /api/keys/[id]/memberships`, wrapped in `withAuth`, proves ownership with an explicit
  `api_keys.user_id = <session user>` equality on the service-role client. It then reads
  `strategy_keys` joined to `strategies ( name, status )` on that same client, and answers
  `{ memberships: [{ name, status }] }`. `ApiKeyManager`'s `readKeyCompositeMemberships` fetches this
  route instead of reading `strategy_keys` in the browser. It treats any non-2xx answer, a body that
  is not JSON, or a missing or non-array `memberships` as `"unreadable"`, which renders
  KCS-DELETE-UNCHECKED.
  **Why:** both designs make the read immune to an RLS regression. The route needs no migration.
  So it avoids the three-reviewer gate, the SQL-census churn, and the auto-apply to PROD.
  The SECURITY DEFINER RPC `get_api_key_composite_memberships` specified in
  RESEARCH § Migration fallback stays the **recorded fallback** if the founder later wants the
  guard at the database layer.
  **ROADMAP wording:** criterion 5's "(a migration)" is superseded. It was written on the
  reviewer's assumption. A dated note records this in the ROADMAP `### Phase 167.2.1` entry, as
  the deviation policy requires (CONTEXT **and** ROADMAP).
  — **Reversibility:** reversible. One new route and one client function, no schema.

- **D-02 (2026-09-25, orchestrator, OQ2): a computed-but-unbuildable row gets its own precise note.
  It never gets KCS12-MINT-B verbatim.** MINT-B's tail, "The numbers appear there once a computation
  succeeds", is false for this row, because its computation DID succeed. The new rows join the
  curated KCS-12 family in `src/lib/status-surface-copy.ts`. Each is pinned by a hand-typed oracle in
  `src/lib/status-surface-copy.test.ts`, as every KCS-12 row is. The voice is DESIGN.md
  § Voice / microcopy: declarative, active, and the limitation stated with its threshold attached.

  | ID | Share mode | Condition | Copy |
  |---|---|---|---|
  | `KCS12-UNBUILDABLE-SHORT` | `mint-token` | a single-key row that is computed but has fewer than 2 distinct dated returns, AND the recipient arm is `not_available` | `Right now, a private link to this strategy shows that its factsheet is not available. Its last computation succeeded with fewer than 2 days of returns, and a factsheet needs at least 2.` |
  | `KCS12-UNBUILDABLE-COMPOSITE` | `mint-token` | a composite row that is computed but whose factsheet cannot build, AND the recipient arm is `not_available` | `Right now, a private link to this strategy shows that its factsheet is not available. Its last computation succeeded, but its results cannot be built into a factsheet. Contact support@quantalyze.com to have this composite checked.` |
  | `KCS12-PUBLIC-UNBUILDABLE-SHORT` | `public-url` | a published single-key row, computed, fewer than 2 distinct dated returns | `Right now, this strategy's factsheet link shows that the factsheet is not available. Its last computation succeeded with fewer than 2 days of returns, and a factsheet needs at least 2.` |
  | `KCS12-PUBLIC-UNBUILDABLE-COMPOSITE` | `public-url` | a published composite row, computed, factsheet cannot build | `Right now, this strategy's factsheet link shows that the factsheet is not available. Its last computation succeeded, but its results cannot be built into a factsheet. Contact support@quantalyze.com to have this composite checked.` |

  **Selection rule:** for an unbuildable row, a `public-url` row takes its PUBLIC-UNBUILDABLE line
  and needs no RPC, as 167.2 IN-04 already rules for published rows. A `mint-token` row reads its
  recipient arm with the existing `readRecipientArm`. Arm `not_available` takes the UNBUILDABLE line,
  because that is exactly when the share page shows its "not available" card. Arm `in_progress`
  keeps KCS12-MINT-A, because the recipient really does see "being prepared" while a recompute runs.
  Arm `unreadable` keeps KCS12-UNREADABLE.
  **The "2"** is `MIN_FACTSHEET_SERIES_POINTS`. A test pins the copy to the constant, so the sentence
  cannot drift from the gate. "yet" is dropped on purpose: waiting does not change this row.
  **The composite line names support** because the probe cannot tell a missing headline from a
  failed series read (D-07). "Support" is the remedy that is true in both cases, following the
  KCS-SHAPE-UNKNOWN precedent.
  **Revision 2026-09-26 (167.2.1-REVIEW CR-01, IN-02; round-1 fixer A):** the four sentences in
  the table above are superseded, and the table is kept as lineage. "Its last computation
  succeeded" was false beside the owner page's job-derived state line in the `never_started`
  arm (done jobs are purged after 30 days) and the `failed` / `stalled` arms. The reason is now
  stated from the stored results the probe read, in the active voice, with the address from
  `SUPPORT_EMAIL`. SHORT: `… Its stored results hold fewer than 2 days of returns, and a
  factsheet needs at least 2.` COMPOSITE: `… We cannot build a factsheet from its stored
  results. Contact support@quantalyze.com to have them checked.` The opening clause of each row
  is unchanged. `src/lib/status-surface-copy.test.ts` pins the new text.
  The COMPOSITE line also serves a single-key row whose stored series is malformed (see the D-07
  revision), which is why it now asks to have "them" (the stored results) checked rather than
  "this composite".
  **Revision 2026-09-26 (167.2.1-REVIEW-SFH-R2 N-5; round-2 fixer D):** the SHORT sentence
  quoted above is superseded, and kept as lineage. SFH-R2 N-5 made `too_few_points` count only
  the column the builder reads (`countedSeriesColumn`), so a row with one valid `daily_returns`
  entry beside a long `returns_series` is now SHORT. "Its stored results hold fewer than 2 days"
  was false about that row, whose other column holds many. SHORT is now: `… The stored results
  we build its factsheet from hold fewer than 2 days of returns, and a factsheet needs at least
  2.` It keeps "stored results" (the CR-01 coherence rule), the threshold and the active voice,
  and binds the count to what the factsheet is built from. The opening clause of each row and
  the COMPOSITE line are unchanged. `src/lib/status-surface-copy.test.ts` pins the new text
  (`N-5 SCOPE`).
  — **Reversibility:** reversible. Copy only.

- **D-03 (2026-09-25, orchestrator, OQ3): the discovery detail page's second assembly of the
  builder is out of scope.** `src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx` calls
  `resolveDailyReturnSeries`, `readCompositeFactsheet`, `readSingleKeyBasisOpts` and
  `buildFactsheetPayload` itself, instead of calling `fetchAndBuildPayload`. That is a drift risk,
  not a false "has a factsheet" claim: it serves published rows only and renders the honest KCS-10
  fallback. It is recorded as a `TODOS.md` drift-risk entry routed to **Phase 169 PAGETRUTH**, which
  owns factsheet KPI sourcing. Plan 04 writes the entry.
  **Revision 2026-09-25 (plan-checker W4):** so the routing is visible where Phase 169 is planned,
  a dated routed note naming this duplicate assembly was added under `### Phase 169` in the
  ROADMAP at planning time. Plan 04's `TODOS.md` entry cross-references it.
  — **Reversibility:** reversible. A backlog entry.

- **D-04 (research Pattern 1): buildability is decided at read time, on the server, by the builder's
  own code. Nothing is stored.** `fetchAndBuildPayload` is split into a module-private resolve stage
  (gates G0 to G3) and the build. Both it and a new exported `probeFactsheetBuildable(id, visibility)`
  call that stage. The G4 point-count predicate becomes the exported `hasBuildableSeries`, and
  `buildFactsheetPayload` itself calls it.
  **Revision 2026-09-25 (plan-checker W3):** the resolve stage holds G0 to G4. G4 is a
  `hasBuildableSeries` call INSIDE the stage, so every null exit of `fetchAndBuildPayload` is a
  failed resolve, and the probe is the resolve stage and nothing else. `buildFactsheetPayload` keeps
  its own `hasBuildableSeries` gate for its other caller (the discovery detail page, D-03). The
  invariant `probe.buildable === (fetchAndBuildPayload(...) !== null)` holds for a builder that does
  not throw. A throw after the resolve stage (the basis reads, the build) is not a null exit and is
  outside the invariant's domain; the page's own error handling owns it.
  A stored flag written by the Python job is rejected. It would re-implement the TypeScript gates
  in a second language, it would need a migration, and it could not be backfilled honestly.
  This satisfies ROADMAP criterion 2 ("or decided server-side for the list") and makes criterion 4
  vacuous: there is no migration.
  — **Reversibility:** reversible. A refactor inside one module, and the builder's output does not change.

- **D-05: a probe that errors is never read as "buildable".** A thrown probe, or a `read_error` or
  `not_visible` result, renders `recipientShareNote(mode, "unreadable")`. It is logged, and captured
  with tags only (`stage: "factsheet-probe"`). A `not_computed` result (the list's RLS embed and the
  admin read disagree, a race) takes the existing uncomputed path.
  **Revision 2026-09-26 (167.2.1-REVIEW-SFH H-2, M-2, M-5, 167.2.1-REVIEW IN-01, WR-01; round-1
  fixer B):** the mapping above is superseded, and kept as lineage. A failed check no longer
  renders `recipientShareNote(mode, "unreadable")`, because for a published row that is
  KCS12-PUBLIC ("… not available yet …"), a recipient-view claim a failed check cannot back. A
  throw, a `read_error` and a `not_visible` now render `probeUnreadableShareNote(mode)`:
  KCS12-PROBE-UNREADABLE or KCS12-PUBLIC-PROBE-UNREADABLE, "We could not check what … shows right
  now. Reload this page to check again." Capture: a `read_error` is captured once by the resolve
  stage with its code (not again by the page); a `not_visible` on `/strategies` is a delete race
  (ids from the owner's own list, owner predicate), so it is a `console.warn` and never a capture;
  a throw is logged per row and captured once per page load with a `probe_failures` count tag; the
  `not_computed` race is a `console.warn`. A computed-but-unbuildable row whose JOB read fails
  takes KCS12-UNBUILDABLE-UNREADABLE-SHORT or -COMPOSITE (the placeholder and the stored-results
  reason, both known), never KCS12-UNREADABLE, whose "once a computation succeeds" is false for it.
  **Revision 2026-09-26 (orchestrator, round 2; recorded by round-2 fixer C; 167.2.1-REVIEW-R2
  WR-01, IN-04, 167.2.1-REVIEW-SFH-R2 N-1, N-2):** the capture rule in the revision above is
  superseded, and kept as lineage. These are decisions. Whether the code matches them is for the
  round-2 fixers' reports and the re-review to show.
  (c) **Probe Sentry aggregation.** The resolve stage captures per row only when the caller is
  `"build"` (the owner lane and the public cache fill). A `"probe"` caller gets the facts back in
  its answer instead of a capture. `/strategies` sends ONE aggregated event per load, covering
  every failed or unbuildable kind (`read_error`, `composite_unbuildable`, `malformed_series`,
  throws), not only throws. Reason: saturation reaches the stage as a returned `{ error }`, not a
  throw, so the round-1 aggregate missed the case it was built for, and durable data states sent
  k events on every navigation.
  (e) **Throws are grouped, with a ceiling of 3.** Probe throws are bucketed by cause, and at most
  3 grouped throw events go out per load, each with its own count. Reason: keeping only the first
  throw let a transient hide a code regression (SFH-R2 N-2), and an uncapped per-cause fan-out
  would bring back the flood.
  (d) **Client Sentry is not fixed in this phase.** A `captureToSentry` call in browser code is a
  no-op because there is no client `Sentry.init`. Adding one is a security decision
  (`scrubSentryEvent` in `src/instrumentation.ts`), so it is booked as
  `[167.2.1-CLIENT-SENTRY-NOOP]` in `TODOS.md`, with the tags-only server endpoint as the
  alternative.
  — **Reversibility:** reversible.

- **D-06 (planner, 2026-09-25): the owner factsheet's S7 note uses the same derivation as the list.**
  The owner lane's pending panel renders `recipientShareNote("mint-token", …)`. For a computed but
  unbuildable row, that is MINT-B with the false tail. If the list moved to the D-02 line and the
  owner lane did not, the phase would create a new disagreement between two owner surfaces. On its
  null-payload path the owner lane therefore runs the same probe and the same selection rule.
  **Revision 2026-09-25 (plan-checker W1):** "the same selection rule" includes D-05's error
  mapping. On the owner lane a thrown probe, or a `read_error` or `not_visible` answer, renders
  `recipientShareNote("mint-token", "unreadable")`, exactly as `/strategies` does, and a
  `not_computed` answer keeps today's arm-derived note.
  **Revision 2026-09-26 (167.2.1-REVIEW-SFH H-2; round-1 fixer B):** "exactly as `/strategies`
  does" still holds, so the owner lane moved with the list: an unreadable owner build renders
  `probeUnreadableShareNote("mint-token")`, not KCS12-UNREADABLE (see the D-05 revision).
  This is plan 04. The orchestrator's three-plan split did not include it, because the research
  assumed MINT-B would be reused verbatim, and D-02 overrides that assumption.
  — **Reversibility:** reversible.

- **D-07: every composite unbuildable case maps to one reason, `composite_unbuildable`.**
  `readCompositeFactsheet` folds a `csv_daily_returns` read error into an empty series and a missing
  headline into `null`, so the probe cannot tell an outage from missing data. Splitting them would
  mean editing `composite-read-path.ts`, which Phase 169 plan 04 edits. The single-key cases (G3
  empty and G4 below the minimum) map to `too_few_points`. They involve no read, so they are data
  facts.
  **Revision 2026-09-26 (167.2.1-REVIEW-SFH M-3; round-1 fixer A):** the single-key cases were not
  all data facts. Normalization drops a non-finite or string value, a non-string date and a
  non-positive wealth point, so a row whose stored columns hold two or more dated entries could
  still reach G3 or G4 and read as "fewer than 2 days of returns". The stage now answers a sixth
  reason, `malformed_series`, when the stored columns hold at least `MIN_FACTSHEET_SERIES_POINTS`
  dated returns and normalization left fewer. It maps to the `cannot_build` note and is captured.
  A series that really is short keeps `too_few_points`. Every `composite_unbuildable` answer is
  now also captured at warning (SFH H-1), which does not change the reason code.
  **Revision 2026-09-26 (orchestrator, round 2; recorded by round-2 fixer C; 167.2.1-REVIEW-R2
  WR-02, 167.2.1-REVIEW-SFH-R2 N-6):** (b) the composite `csv_daily_returns`-outage variant of the
  cached-null problem is ACCEPTED under this decision. The outage folds into
  `composite_unbuildable`, so the stage cannot tell it from a composite with no rows, and a `null`
  built from it is cached like any other answer. The round-2 read-error fix does not cover it and
  must not claim to. SFH-R2 N-6's composite half (an outage and a truly empty composite are the
  same event) is accepted the same way. Both are owned by Phase 169 plan 04, which already edits
  `composite-read-path.ts` and can return the read error from `readCompositeFactsheet`.
  — **Reversibility:** reversible.

- **D-08: the list probes only its computed rows, with `Promise.all`, uncached** (RESEARCH A3 and
  Pitfall 7). Uncomputed rows are never probed. The probe is never routed through
  `buildFactsheetPayloadCached`.
  **Revision 2026-09-26 (167.2.1-REVIEW WR-01; round-1 fixer B):** bounded. The probes run
  through a FIFO slot limiter, `PROBE_CONCURRENCY = 4` in `src/app/(dashboard)/strategies/page.tsx`,
  and a probe throw is captured once per page load. The review did not wait for a measured large
  list: the list is unpaginated, each probe is the builder's heaviest read, and pool exhaustion
  turned into a failed check on every row. The cost is still one heavy read per computed row per
  load, written at the probe; a cheaper answer (a writer-side verdict, or a column-light resolve)
  is routed to the orchestrator, not taken here.
  **Revision 2026-09-26 (orchestrator, round 2; recorded by round-2 fixer C):** "a probe throw is
  captured once per page load" is superseded by the D-05 round-2 revision, (c) and (e): one
  aggregated event per load for every failure kind, and at most 3 grouped throw events. The cost
  routed to the orchestrator above is booked as `[167.2.1-STRATEGIES-PROBE-COST]` in `TODOS.md`.
  — **Reversibility:** reversible. Bound the concurrency if a list is measured to be large.

- **D-09: merge order with in-flight phases.** Phase 169 plans 04 and 05 edit `build-payload.ts`,
  `fetch-and-build-payload.ts`, `types.ts` and `composite-read-path.ts`. Phase 167.1.2 plan 04 edits
  `ApiKeyManager.tsx` and its test. **Whichever merges second rebases.** Each plan's first task
  re-reads its files at HEAD and re-resolves every symbol before editing. Hunks stay minimal, and no
  plan reformats untouched code.
  **Post-rebase rule (revision 2026-09-25, plan-checker W3):** whichever of Phase 169 and Phase
  167.2.1 merges SECOND re-runs plan 01's probe-to-builder parity table and its SC1 live
  reproduction on the local lane after the rebase, and records both runs. A rebase that adds a null
  exit to `fetchAndBuildPayload` outside the shared resolve stage breaks the invariant stated in the
  probe's doc comment; the fix is to move that exit into the resolve stage, never to special-case
  the probe.
  — **Reversibility:** n/a (process).

- **D-10: the Delete TOCTOU residual is accepted, not fixed.** A membership added between the read
  and the Delete still cascades without a warning. `api_keys_published_composite_delete_guard` still
  refuses the delete for a PUBLISHED composite. Closing the window needs a delete RPC that compares
  an acknowledged membership count, which is a migration. The founder's 2026-09-24 decision is
  "warn, never block".
  — **Reversibility:** reversible.

- **D-11: known limit, the public cached lane lags the probe** (RESEARCH Pitfall 4). A published
  row's public URL is served through `unstable_cache` with an id-only key and a 3600 s TTL. The
  probe answers for a fresh build now. For up to an hour the public page can therefore still serve an
  older payload or a cached `null`. That cache is the Phase 148 TODO and is not touched here.
  **Revision 2026-09-26 (167.2.1-REVIEW WR-02; round-1 fixer B):** superseded, and kept as
  lineage. The lag made this phase's own present-tense notes false: no note while the link served
  a cached placeholder, or "not available" while it served a cached payload. The root fix is the
  one DEF-148-A recorded: `computed_at` is now a real `keyParts` member
  (`["factsheet-v2-payload-v6", id, computedAt]`), so every successful analytics run gets a fresh
  entry. Softening the copy was rejected because it cannot fix the "no note" half, and
  `revalidateTag` on compute is unavailable (the writer is the Python worker). Residual, recorded:
  within ONE run (same `computed_at`) a status change that does not stamp `computed_at` (a
  recompute entering `computing`, or a failed refresh) still drains on the TTL, and a `null`
  cached from a transient admin read error is kept for the TTL of that run.
  **Revision 2026-09-26 (orchestrator, round 2; recorded by round-2 fixer C; 167.2.1-REVIEW-R2
  WR-02):** (a) the first residual above, "a recompute entering `computing`", is DROPPED as
  non-existent. `sync_strategy_analytics_status` (latest definition, migration `20260906120000`)
  stamps `computed_at = now()` in branch (a), the `v_nonterminal_count > 0` arm, in its
  `ON CONFLICT DO UPDATE` on every bridge call, including the transition into `computing`. Branch
  (c) stamps it again at completion. So the key moves when a job starts and when it finishes. The
  protected branch (b-prime) writes neither status nor `computed_at`. Fixer C re-read branch (a)'s
  `ON CONFLICT` arm at the branch head and confirmed the stamp. The second residual, a cached
  `null` from a transient read error, stands and is the round-2 WR-02 fix. Its composite variant is
  accepted under D-07 (see the D-07 revision).

- **D-12: no VERSION or CHANGELOG edit in any plan.** Both go in the ship commit, under the CLAUDE.md
  CHANGELOG mechanism. The bump is patch-level in meaning: a user-facing truth fix.

### Claude's Discretion
- Symbol names: `hasBuildableSeries`, `MIN_FACTSHEET_SERIES_POINTS`, `resolveFactsheetInputs`,
  `probeFactsheetBuildable`, `NotBuildableReason`, `FactsheetBuildability`,
  `unbuildableNoteKindOf`, `recipientShareNoteFor`, and the route path. They are fixed in the plans.
- A non-UUID key id on the route answers 400 before any query. The shape leaks nothing, so
  there is no existence oracle.

</decisions>

<canonical_refs>
## Canonical References

- `.planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-RESEARCH.md`: gates G0-G4, the signal inventory, the patterns and the pitfalls
- `.planning/ROADMAP.md` `### Phase 167.2.1`: criteria 1-5
- `.planning/phases/167.2-keycardsync-the-key-card-never-shows-one-key-s-sync-result-a/167.2-UI-SPEC.md`: the KCS-12 family and the round-2 founder-decision rows for KCS-DELETE-*
- `DESIGN.md` § Voice / microcopy
- `CLAUDE.md`: which database, the CHANGELOG discipline, and never a CI skip token

</canonical_refs>

<deferred>
## Deferred Ideas

- The discovery detail page's duplicate builder assembly: booked to Phase 169 (D-03).
- The SECURITY DEFINER membership RPC: the recorded fallback for D-01, built only if the founder asks.
- The Delete TOCTOU window (D-10).
- The public cached lane's staleness (D-11, Phase 148). Revision 2026-09-26: no longer deferred.
  Closed by the D-11 revision (`computed_at` in `keyParts`), and `DEF-148-A` is closed in `TODOS.md`.
- The composite csv-outage variant of the cached `null`, and SFH-R2 N-6's composite half: accepted
  under D-07 and owned by Phase 169 plan 04 (revision 2026-09-26).
- Client-side Sentry: booked as `[167.2.1-CLIENT-SENTRY-NOOP]` in `TODOS.md` (D-05 revision
  2026-09-26).
- The `/strategies` probe cost, and its upgrade to a persisted buildable flag: booked as
  `[167.2.1-STRATEGIES-PROBE-COST]` in `TODOS.md` (2026-09-26).

</deferred>

### D-12 — Sentry `strategy_id` goes in `tags`, not `extra` (orchestrator, 2026-09-26)

The build-caller captures carry `strategy_id` as a Sentry **tag**. The repo's older precedent
(`finalize-wizard`, `csv-finalize`) puts it in `extra`. Tags are chosen here on purpose: IN-02's
point is that support can find a strategy's events, and only tags are searchable and filterable;
`extra` is visible only inside one event. The tag also lands on unpublished rows through the owner
lane, which is accepted: the id is an internal UUID in an admin-only Sentry project, and
`scrubSentryEvent` scrubs share-path tokens, not ids. Reversible by moving the key per site.
