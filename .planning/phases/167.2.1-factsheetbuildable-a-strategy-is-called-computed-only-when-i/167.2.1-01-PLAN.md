---
phase: 167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/factsheet-buildable-live-db.test.ts
  - src/lib/factsheet/fetch-and-build-payload.test.ts
  - src/lib/factsheet/fetch-and-build-payload.ts
  - src/lib/factsheet/build-payload.ts
  - src/lib/factsheet/build-payload.test.ts
autonomous: true
requirements: [SC1, SC2, SC4]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC1: on the local lane, a seeded single-key strategy_analytics row with computation_status complete and ONE dated point, and a seeded composite (data_quality_flags.composite true, metrics_json_by_basis NULL, two csv_daily_returns rows) each read 'complete' through the OWNER's RLS-scoped embed while fetchAndBuildPayload(id, owner predicate) returns null. Observed green against the UNCHANGED builder before any source edit (the reproduction), with a buildable control row proving the harness can build."
    - "SC2 / D-04: probeFactsheetBuildable(id, visibility) and fetchAndBuildPayload share ONE resolve stage (G0-G3) and ONE point-count predicate, hasBuildableSeries, which buildFactsheetPayload itself calls. For every fixture in the parity table, probe.buildable === (fetchAndBuildPayload(...) !== null), and each fixture's absolute expectation (null or not, reason) holds."
    - "D-07: the probe's reasons are exactly read_error, not_visible, not_computed, composite_unbuildable, too_few_points. Every composite failure (G2 null, G3 empty, G4 short) is composite_unbuildable; single-key G3 and G4 are too_few_points."
    - "fetchAndBuildPayload's output is unchanged for every input: same payload bytes (build-payload snapshots unchanged), same null cases, every existing log line kept with its message text."
    - "SC4: no file under supabase/ changes (no migration, D-04)."
    - "Neuter to RED to restore is recorded in the SUMMARY for: the probe skipping hasBuildableSeries (parity RED), MIN_FACTSHEET_SERIES_POINTS raised to 3 (truth table RED), the resolve stage dropping G1 (absolute expectation RED)."
  artifacts:
    - path: src/lib/factsheet/build-payload.ts
      provides: "exported MIN_FACTSHEET_SERIES_POINTS and hasBuildableSeries, called by buildFactsheetPayload at its point-count gate"
    - path: src/lib/factsheet/fetch-and-build-payload.ts
      provides: "module-private resolveFactsheetInputs; exported probeFactsheetBuildable, NotBuildableReason, FactsheetBuildability; fetchAndBuildPayload re-expressed on the resolve stage"
    - path: src/lib/factsheet/fetch-and-build-payload.test.ts
      provides: "REPRO arms and the probe-to-builder parity table on a mocked admin client"
    - path: src/__tests__/factsheet-buildable-live-db.test.ts
      provides: "the SC1 reproduction on the local Supabase lane, gated on HAS_LIVE_DB"
  key_links:
    - from: src/lib/factsheet/build-payload.ts
      to: "buildFactsheetPayload's point-count gate"
      via: "hasBuildableSeries(dailyReturns)"
      pattern: "hasBuildableSeries\\("
    - from: src/lib/factsheet/fetch-and-build-payload.ts
      to: src/lib/factsheet/build-payload.ts
      via: "import { hasBuildableSeries } used by probeFactsheetBuildable"
      pattern: "hasBuildableSeries"
    - from: src/lib/factsheet/fetch-and-build-payload.ts
      to: "fetchAndBuildPayload and probeFactsheetBuildable"
      via: "both call resolveFactsheetInputs"
      pattern: "resolveFactsheetInputs\\("
---

<objective>
Reproduce the WR-02 mismatch on the local lane first (SC1), then make "can this factsheet build"
answerable by the builder's own code, without building (SC2, D-04).

Purpose: today only `fetchAndBuildPayload` knows whether a computed row can build, and it answers
by building. The list (plan 03) and the owner lane (plan 04) need the same answer cheaply, and from
the same code, so they cannot drift from the share page.
Output: the live reproduction spec, a parity-tested `probeFactsheetBuildable`, and an exported
`hasBuildableSeries` that `buildFactsheetPayload` itself uses.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-CONTEXT.md
@.planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-RESEARCH.md
@src/lib/factsheet/fetch-and-build-payload.ts
@src/lib/factsheet/build-payload.ts
@src/lib/factsheet/composite-read-path.ts
@src/lib/test-helpers/live-db.ts

<!-- verify-symbol: src/lib/factsheet/fetch-and-build-payload.ts fetchAndBuildPayload -->
<!-- verify-symbol: src/lib/factsheet/fetch-and-build-payload.ts StrategyVisibility -->
<!-- verify-symbol: src/lib/factsheet/build-payload.ts normalizeDailyReturns -->
<!-- verify-symbol: src/lib/factsheet/build-payload.ts buildFactsheetPayload -->
<!-- verify-symbol: src/lib/factsheet/composite-read-path.ts readCompositeFactsheet -->
<!-- verify-symbol: src/lib/factsheet/basis-metrics.ts hasBasisHeadline -->
<!-- verify-symbol: src/lib/visibility.ts withPublishedOrOwner -->
<!-- verify-symbol: src/lib/test-helpers/live-db.ts signInAsTestUser -->
<!-- verify-symbol: src/lib/test-helpers/live-db.ts createLiveAdminClient -->
<!-- verify-symbol: src/lib/test-helpers/live-db.ts HAS_LIVE_DB -->
<!-- verify-symbol: src/lib/test-helpers/live-db.ts cleanupLiveDbRow -->
<!-- verify-symbol: scripts/live-db-lane-corpus.mjs liveDbLaneCorpus -->
</context>

<hard_constraints>
Carried into every task of this plan:
- **Worktree setup:** the worktree has no `node_modules` (RESEARCH Pitfall 6). Symlink the main
  checkout's `node_modules` into the worktree root. Find the main checkout's path at run time with
  `git worktree list`. Then TEST a command (`npx vitest --version`) rather than `ls`-ing the
  directory. Stage by explicit path only. Remove the symlink before every commit: `git status --short`
  must not list `node_modules`. ⛔ Never write the resolved path into a tracked file.
- **Verify logs:** before running any `<automated>` command, `export FB_SP=<your session scratchpad directory>`.
  Never use /tmp. The commands expand `${FB_SP:?FB_SP_unset}` and fail loudly when it is unset.
  ⛔ Never write the expanded path into a tracked file: `npm run check:planning-hygiene` rejects home
  paths in a public repo.
- **Databases:** the ONLY database this plan touches is the local lane that
  `bash scripts/local-stack/run.sh up` boots. ⛔ No `supabase` or `psql` command against any remote,
  because the CLI in the main checkout is linked to PRODUCTION. No migration, and nothing under
  `supabase/` changes (D-04).
- **Public repo:** synthetic ids, names and dates only.
- ⛔ Never `git checkout -- <path>`, `git restore`, `git stash` or `npm version`. Take byte backups
  for neuter checks and re-take them after every edit. No VERSION or CHANGELOG edit (D-12). No CI skip
  token in any commit message, not even to deny using one.
- No floor lowered, no allowlist widened, no ledger line added to `scripts/live-db-execution-ledger`
  data. Cite by SYMBOL, never by line number, in comments.
- **D-09 (merge order):** Phase 169 plans 04 and 05 edit `build-payload.ts`, `fetch-and-build-payload.ts`,
  `types.ts` and `composite-read-path.ts`. Re-read each file at HEAD before editing, keep hunks
  minimal, and never reformat untouched code. Whichever phase merges second rebases.
- **Keep the builder's structure:** `fetchAndBuildPayload` keeps its REQUIRED `visibility` parameter.
  It stays in this module with no `next/cache` import (the phase-148 guard pins this file as the ONE
  declaring module). Its select keeps `daily_returns` WITH `returns_series` (the phase-147 guard).
</hard_constraints>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): reproduce the mismatch end to end on the local lane, from a seeded row through the owner's RLS read to a null builder (SC1)</name>
  <files>src/__tests__/factsheet-buildable-live-db.test.ts, src/lib/factsheet/fetch-and-build-payload.test.ts</files>
  <read_first>
    - src/lib/factsheet/fetch-and-build-payload.ts (the whole `fetchAndBuildPayload`: the admin select under `visibility`, the `isComputedAnalytics` gate, `resolveDailyReturnSeries`, the composite branch via `readCompositeFactsheet`, the empty-series gate)
    - src/lib/factsheet/build-payload.ts (`normalizeDailyReturns`, and the point-count gate inside `buildFactsheetPayload`)
    - src/lib/factsheet/composite-read-path.ts (`readCompositeFactsheet`: the csv_daily_returns read and the `hasBasisHeadline` refusal; and a valid `cash_settlement` headline fixture in src/lib/factsheet/composite-read-path.test.ts)
    - src/lib/test-helpers/live-db.ts (`HAS_LIVE_DB`, `createLiveAdminClient`, `signInAsTestUser`, `cleanupLiveDbRow`, `advertiseLiveDbSkipReason`)
    - src/__tests__/strategy-verifications-rls.test.ts (the minimal legal service-role `strategies` insert and the dependency-order cleanup to copy)
    - scripts/live-db-lane-corpus.mjs (`liveDbLaneCorpus`: a `*.test.ts` under src that references `HAS_LIVE_DB` joins the lane) and vitest.livedb.config.ts
    - scripts/live-db-fixture-drift-census.mjs header (what counts as fixture drift in a live spec)
    - 167.2.1-RESEARCH.md § Reproduction and § Why a row can be computed and still unbuildable
  </read_first>
  <action>
    Before any source edit, re-read the five files of this plan at HEAD (D-09). Run `node scripts/verify-plan-anchors.mjs` on this plan file and halt on non-zero.
    Reproduce first (SC1). Do not edit any production file in this task.
    (a) Create src/__tests__/factsheet-buildable-live-db.test.ts. Gate it with `describe.skipIf(!HAS_LIVE_DB)` and call `advertiseLiveDbSkipReason` as sibling live specs do. Mock `server-only` to an empty module, because the real `createAdminClient` imports it; the lane supplies the service-role env. Seed with the service-role client: an owner through `signInAsTestUser`, and three strategies owned by that user with synthetic names.
    - (1) single-key: a `strategy_analytics` row with `computation_status` 'complete', `daily_returns` holding ONE `{date, value}` point, and `returns_series` null.
    - (2) pre-Phase-86 composite: `data_quality_flags` `{"composite": true}`, `metrics_json_by_basis` null, and two `csv_daily_returns` rows.
    - (3) CONTROL: a 'complete' single-key row with at least 30 distinct dated points, so the harness is proven able to build.
    Arms:
    - REPRO-SINGLE-ONE-POINT and REPRO-COMPOSITE-PRE86: the OWNER's signed-in client reads `strategies` with the embed `strategy_analytics ( computation_status )` and sees 'complete', and `fetchAndBuildPayload(id, (q) => withPublishedOrOwner(q, ownerId))` returns null.
    - CONTROL-BUILDABLE: the same call returns a non-null payload whose `id` is the control strategy's.
    Assert only about your own rows. In `afterAll`, delete your rows in dependency order and then the user.
    (b) Create src/lib/factsheet/fetch-and-build-payload.test.ts with the same three REPRO/CONTROL cases at unit level. Mock `@/lib/supabase/admin` so `createAdminClient` returns a chainable fake:
    - `from("strategies").select(...).eq(...)` records calls, accepts `.or(filter)` and records the filter string, and answers `.maybeSingle()` from a per-test fixture;
    - `from("csv_daily_returns")...order(...).limit(...)` answers a per-test fixture;
    - any `strategy_analytics_series` read answers no rows.
    Also mock `server-only`. ⛔ This unit file must NOT name the live-DB gate symbol, or the lane corpus walker would pull it into the live lane. Pass the owner predicate with a UUID-shaped synthetic user id; `withPublishedOrOwner` fails closed on a non-UUID. In each case, assert that the recorded `.or` filter names that id.
    These cases are the REPRODUCTION. They must be GREEN against today's unchanged builder. Record that run in the SUMMARY as the SC1 evidence: the owner-visible status reads 'complete' and the builder returns null.
    Then run the lane: `bash scripts/local-stack/run.sh up`, the spec, `bash scripts/local-stack/run.sh down`. Also run the fixture-drift census and the two lane contract tests. A new spec must add no drift and no ledger line.
    If either population cannot be seeded under the lane's constraints, STOP and report it. ROADMAP criterion 1 then says the phase shrinks, and that is a finding, not a workaround.
  </action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/fetch-and-build-payload.test.ts --reporter=verbose > ${FB_SP:?FB_SP_unset}/fb-01-t1-unit.log 2>&1; RC=$?; tail -40 ${FB_SP:?FB_SP_unset}/fb-01-t1-unit.log; test "$RC" = 0 && grep -q 'REPRO-SINGLE-ONE-POINT' ${FB_SP:?FB_SP_unset}/fb-01-t1-unit.log && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${FB_SP:?FB_SP_unset}/fb-01-t1-unit.log && bash scripts/local-stack/run.sh up > ${FB_SP:?FB_SP_unset}/fb-01-t1-up.log 2>&1 && npm run test:live-db -- src/__tests__/factsheet-buildable-live-db.test.ts --reporter=verbose > ${FB_SP:?FB_SP_unset}/fb-01-t1-live.log 2>&1; LRC=$?; bash scripts/local-stack/run.sh down > /dev/null 2>&1; tail -40 ${FB_SP:?FB_SP_unset}/fb-01-t1-live.log; test "$LRC" = 0 && grep -q 'REPRO-COMPOSITE-PRE86' ${FB_SP:?FB_SP_unset}/fb-01-t1-live.log && ! grep -qE '[1-9][0-9]* skipped' ${FB_SP:?FB_SP_unset}/fb-01-t1-live.log && node scripts/live-db-fixture-drift-census.mjs && npx vitest run src/__tests__/live-db-fixture-drift.test.ts src/__tests__/live-db-execution-ledger.contract.test.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both spec files exist; the live spec references `HAS_LIVE_DB` and `node scripts/live-db-lane-corpus.mjs` lists it.
    - The live run shows REPRO-SINGLE-ONE-POINT, REPRO-COMPOSITE-PRE86 and CONTROL-BUILDABLE passing, none skipped, against an unchanged `src/lib/factsheet/` (`git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/factsheet/` lists only the new test file).
    - The fixture-drift census exits 0 and the two lane contract tests pass.
  </acceptance_criteria>
  <done>The mismatch is reproduced on the local lane and at unit level against today's code, with a control that builds; the SUMMARY carries both runs as SC1 evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: split resolve from build, export hasBuildableSeries and probeFactsheetBuildable, and pin probe-to-builder parity (SC2, D-04, D-07)</name>
  <files>src/lib/factsheet/build-payload.ts, src/lib/factsheet/build-payload.test.ts, src/lib/factsheet/fetch-and-build-payload.ts, src/lib/factsheet/fetch-and-build-payload.test.ts, src/__tests__/factsheet-buildable-live-db.test.ts</files>
  <read_first>
    - the two test files as left by Task 1
    - src/__tests__/phase-148-owner-lane-cache-isolation.test.ts (this file is the ONE declaring module, no cache reach in the closure) and src/__tests__/phase-147-series-resolution-guards.test.ts (the `daily_returns` without `returns_series` scan)
    - src/lib/factsheet/build-payload.test.ts and its snapshot file (the byte-neutral payload pins must not move)
    - 167.2.1-CONTEXT.md D-04, D-07
  </read_first>
  <behavior>
    - hasBuildableSeries([]) false; one point false; two rows with the SAME date false; one finite point plus one NaN value false; one point plus one row with a non-string date false; two distinct finite dated points true; an unsorted 30-point series true.
    - buildFactsheetPayload returns null exactly when hasBuildableSeries is false, over the same fixtures (it CALLS the predicate).
    - Parity table in fetch-and-build-payload.test.ts. Each row has an absolute expectation plus the invariant probe.buildable === (payload !== null):
      admin read error (payload null, reason read_error); no row (not_visible); status 'failed' with a good series (not_computed); status 'computing' (not_computed); composite with metrics_json_by_basis null (composite_unbuildable); composite with a valid headline and an empty csv read (composite_unbuildable); composite with a valid headline and one csv row (composite_unbuildable); composite with a valid headline and 30 csv rows (buildable); single-key with daily_returns null and returns_series null (too_few_points); one point (too_few_points); one point duplicated (too_few_points); NaN values (too_few_points); two good points (buildable); a normal 30-point series in each of the three daily_returns shapes resolveDailyReturnSeries accepts (buildable); returns_series-only equity curve (buildable).
    - The probe reads no strategy_analytics_series row (assert the admin fake saw no such table) and never calls buildFactsheetPayload (spy on the module export).
  </behavior>
  <action>
    Tests first. Add the behavior cases above to the two unit test files and run them. The new cases fail because the exports do not exist yet: that is import RED, and not the meaningful RED (RESEARCH Pitfall 3). The meaningful RED is the neuter step below.
    build-payload.ts (D-04): export `MIN_FACTSHEET_SERIES_POINTS` = 2, and export `hasBuildableSeries(rows: DailyReturn[]): boolean`. It returns true when the existing private `normalizeDailyReturns` yields at least that many rows. Do NOT add a second normalize.
    In `buildFactsheetPayload`, keep the empty-input early return and keep computing the dedup for the rest of the function. Replace the literal `< 2` gate with `!hasBuildableSeries(dailyReturns)`. Keep the same console.warn message and payload fields. The warn still reports `sortedDedupCount` from the local dedup. The second normalize this costs is O(n log n) on at most a few thousand rows; accept it and say so in a one-line comment naming D-04.
    fetch-and-build-payload.ts (D-04, D-07): extract a module-private `resolveFactsheetInputs(supabase, id, visibility)`. It takes the admin client, the id and the REQUIRED visibility predicate, and returns either `{ ok: true, strategy, analytics, dqf, isComposite, dailyRaw, dailyReturns, compositeBuildOpts }` or `{ ok: false, reason }`. It holds these steps VERBATIM, with every existing comment and log line:
    - the strategies select, with `read_error` on an error and `not_visible` on no row;
    - the `isComputedAnalytics` gate (`not_computed`);
    - `resolveDailyReturnSeries`;
    - the both-populated warn (IMPORTANT-3);
    - the composite branch via `readCompositeFactsheet` (`composite_unbuildable` on null);
    - the empty-series gate, which answers `composite_unbuildable` for a composite and `too_few_points` otherwise.
    Moving the empty-series gate ahead of `readSingleKeyBasisOpts` is intended: it only skips basis reads whose result was discarded anyway.
    Re-express `fetchAndBuildPayload` as: resolve, then return null when the result is not ok, then the unchanged single-key `readSingleKeyBasisOpts` / `singleKeyDataQuality` assembly, then the unchanged `computed_at` handling and the `buildFactsheetPayload` call. Its signature, its null cases and its payload do not change.
    Export `type NotBuildableReason` (exactly the five reasons named in must_haves), `type FactsheetBuildability`, and `async function probeFactsheetBuildable(id: string, visibility: StrategyVisibility): Promise<FactsheetBuildability>`. It does the same `createAdminClient()` and resolve. On a failed resolve it returns `{ buildable: false, reason }`. Otherwise it answers with `hasBuildableSeries(dailyReturns)`, giving `too_few_points`, or `composite_unbuildable` for a composite. It makes no basis reads and does no compute. It does not catch a throw, because the caller maps a throw to "unreadable" (D-05).
    Add a doc comment on the probe. It names D-04. It says the probe and the builder share the resolve stage and the predicate, so they agree by construction, and that the parity table pins it. It also says the probe must never be routed through the cached wrapper (D-11).
    Extend the live spec with PROBE-SINGLE (`{ buildable: false, reason: "too_few_points" }`), PROBE-COMPOSITE (`composite_unbuildable`) and PROBE-CONTROL (`{ buildable: true }`), then re-run the lane.
    Neuter checks, each against a fresh byte backup that is re-taken after every edit:
    - (1) the probe returns buildable without calling `hasBuildableSeries`: the parity rows for one point, duplicated and NaN go RED;
    - (2) `MIN_FACTSHEET_SERIES_POINTS` set to 3: the truth table and two-good-points rows go RED;
    - (3) the resolve stage without the `isComputedAnalytics` gate: the not_computed rows go RED on their absolute expectation.
    Restore each, confirm GREEN, and record all three in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/fetch-and-build-payload.test.ts src/lib/factsheet/build-payload.test.ts src/lib/factsheet/composite-read-path.test.ts src/lib/factsheet/audit-c20.test.ts src/__tests__/phase-148-owner-lane-cache-isolation.test.ts src/__tests__/phase-147-series-resolution-guards.test.ts "src/app/factsheet-share/[token]" "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" "src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx" "src/app/factsheet/[id]/v2/page.smoothed-wiring.test.tsx" "src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx" --reporter=verbose > ${FB_SP:?FB_SP_unset}/fb-01-t2.log 2>&1; RC=$?; tail -60 ${FB_SP:?FB_SP_unset}/fb-01-t2.log; test "$RC" = 0 && grep -q 'too_few_points' ${FB_SP:?FB_SP_unset}/fb-01-t2.log && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${FB_SP:?FB_SP_unset}/fb-01-t2.log && bash scripts/local-stack/run.sh up > ${FB_SP:?FB_SP_unset}/fb-01-t2-up.log 2>&1 && npm run test:live-db -- src/__tests__/factsheet-buildable-live-db.test.ts --reporter=verbose > ${FB_SP:?FB_SP_unset}/fb-01-t2-live.log 2>&1; LRC=$?; bash scripts/local-stack/run.sh down > /dev/null 2>&1; tail -30 ${FB_SP:?FB_SP_unset}/fb-01-t2-live.log; test "$LRC" = 0 && grep -q 'PROBE-CONTROL' ${FB_SP:?FB_SP_unset}/fb-01-t2-live.log && ! grep -qE '[1-9][0-9]* skipped' ${FB_SP:?FB_SP_unset}/fb-01-t2-live.log && npm run typecheck && npx eslint src/lib/factsheet/fetch-and-build-payload.ts src/lib/factsheet/build-payload.ts src/lib/factsheet/fetch-and-build-payload.test.ts src/__tests__/factsheet-buildable-live-db.test.ts && test -z "$(git diff --name-only "$(git merge-base HEAD origin/main)" -- supabase/)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function hasBuildableSeries' src/lib/factsheet/build-payload.ts` is 1, and `grep -c 'hasBuildableSeries(dailyReturns)' src/lib/factsheet/build-payload.ts` is at least 1.
    - `grep -c 'export async function probeFactsheetBuildable' src/lib/factsheet/fetch-and-build-payload.ts` is 1, and `grep -c 'resolveFactsheetInputs(' src/lib/factsheet/fetch-and-build-payload.ts` is at least 3 (declaration plus two callers).
    - The build-payload snapshot file is unchanged (`git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/factsheet/__snapshots__` is empty).
    - The SUMMARY records the three neuter runs, each RED and then restored GREEN.
  </acceptance_criteria>
  <done>The probe and the builder decide from one resolve stage and one predicate; parity holds on every fixture; the builder's output is unchanged; the lane shows the probe answering too_few_points, composite_unbuildable and buildable on real rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server page → service-role admin client | The probe reads on the service role, where the injected visibility predicate is the ONLY row gate |
| live-DB spec → local lane | Test fixtures are written with a service-role key minted by the local stack |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.2.1-01 | Information disclosure | probeFactsheetBuildable | high | mitigate | `visibility` is a REQUIRED parameter with no default, as for the builder (compile error when omitted). The unit fake records the `.or` filter, and each case asserts it names the session user. |
| T-167.2.1-02 | Information disclosure | probe routed through the id-keyed cached wrapper | high | mitigate | The probe lives in the no-cache module that the phase-148 closure guard walks. The doc comment forbids routing it through `buildFactsheetPayloadCached`. |
| T-167.2.1-03 | Tampering | probe and builder drift apart | medium | mitigate | One resolve stage and one predicate, both called by the builder. The parity table runs on every fixture, with neuter checks recorded. |
| T-167.2.1-04 | Tampering | live spec writing to a shared database | high | mitigate | The lane boots a private local stack (`scripts/local-stack/run.sh up`). There is no remote command, and the spec deletes only its own rows. |
| T-167.2.1-05 | Denial of service | the probe's per-row admin select | low | accept | It is the same select the builder already runs, with no compute and no basis reads. The list size is small (RESEARCH A3); D-08 records the bound-concurrency fallback. |
</threat_model>

<verification>
- The Task 1 reproduction runs are green on unchanged production code (SC1).
- The Task 2 parity table and neuter checks pass (SC2).
- `git diff --name-only "$(git merge-base HEAD origin/main)" -- supabase/` is empty (SC4 vacuous, D-04).
- `node scripts/verify-plan-anchors.mjs .planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-01-PLAN.md` exits 0 at the start.
</verification>

<success_criteria>
A reader can call `probeFactsheetBuildable(id, visibility)` and get the same buildable answer the
builder would give, with a typed reason, without building. The mismatch it answers is reproduced on
real rows on the local lane.
</success_criteria>

<output>
Create `.planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-01-SUMMARY.md` when done.
</output>
