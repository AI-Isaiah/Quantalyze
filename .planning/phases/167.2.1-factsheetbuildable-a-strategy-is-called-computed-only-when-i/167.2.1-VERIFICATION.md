---
phase: 167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i
verified: 2026-09-26T11:05:00Z
status: human_needed
score: 11/12 must-haves verified (1 uncertain, routed to a SHA-bound CI check)
verified_at_sha: 199eb302e3c11dd244c43ddeea9df25a8b9ce16b
drift_subjects:
  - src/lib/factsheet/fetch-and-build-payload.ts
  - src/lib/factsheet/build-payload.ts
  - src/lib/factsheet/composite-read-path.ts
  - src/lib/status-surface-copy.ts
  - src/app/(dashboard)/strategies/page.tsx
  - src/app/api/keys/[id]/memberships/route.ts
  - src/components/strategy/ApiKeyManager.tsx
  - src/app/factsheet/[id]/v2/page.tsx
  - src/app/factsheet-share/[token]/page.tsx
  - scripts/live-db-lane-corpus.mjs
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "SC1 on the local-stack lane: confirm the `frontend-live-db-lane` job is green on the PR head, bound by head SHA (count the runs for that SHA, do not read the board)."
    expected: "`src/__tests__/factsheet-buildable-live-db.test.ts` runs 7 of 7 green (REPRO-SINGLE-ONE-POINT, REPRO-COMPOSITE-PRE86, CONTROL-BUILDABLE, PROBE-SINGLE, PROBE-COMPOSITE, PROBE-CONTROL, WITH-REASON)."
    why_human: "The verifier's own `run.sh up` did not come up (two attempts, both exit 1, torn down to 0 containers, lock released). The lane-level evidence on record is executor and fixer narration only. The unit REPRO cases the verifier ran use a mocked admin client, so they prove the builder's gates, not the RLS-scoped owner read against real rows. Owner: orchestrator, at ship."
  - test: "Visual check at 320px width and at 200% zoom, in the logged-in browser after deploy: the /strategies share note beside the share control for a computed-but-unbuildable row (SHORT and COMPOSITE lines, and the 'could not check' line), the owner factsheet's S7 pending panel with the same note and the KCS09-FINISHED-UNREADABLE state line, and the key card's Delete confirm with the composite warning and KCS-DELETE-UNCHECKED."
    expected: "Every line wraps inside its container, nothing is clipped or overlaps the control beside it, and the copy reads exactly as the D-02 revisions state."
    why_human: "Layout and reflow are not observable by grep or jsdom. The D-02 lines are long. Owner: orchestrator, in the logged-in Chrome after deploy (founder rule 2026-09-24). Never marked passed here."
  - test: "Live PROD read after deploy: pick a computed strategy whose factsheet cannot build (or confirm none exists), open /strategies and the owner factsheet, then open its private share link in a signed-out window."
    expected: "The list note, the owner S7 note and the share page agree: the share page shows its 'not available' card, and both owner surfaces name the stored-results reason. A buildable computed row shows no note and its link renders the factsheet."
    why_human: "Needs real PROD rows and a real share token. No DB command against any remote was run by the verifier. Owner: orchestrator, after deploy. Counts and verdicts only in any record; no ids, names or tokens."
---

# Phase 167.2.1: FACTSHEETBUILDABLE Verification Report

**Phase Goal:** The owner's `/strategies` list and every "has a factsheet" signal agree with what a share-link recipient actually sees. A computed strategy whose factsheet cannot build is no longer shown as having a factsheet with no share note. Criterion 5 adds that the key-card Delete warning cannot be silenced by an RLS-filtered membership read.
**Verified:** 2026-09-26
**Code verified at:** `199eb302e3c11dd244c43ddeea9df25a8b9ce16b` (branch `feat/167.2.1`)
**Status:** human_needed
**Re-verification:** No. This is the initial verification.

## Verifier's own runs (at the verified sha)

| Check | Result |
|---|---|
| `tsc --noEmit` | exit 0 |
| vitest over `src/lib/factsheet`, `status-surface-copy.test.ts`, `(dashboard)/strategies`, `app/factsheet`, `app/factsheet-share`, `app/api/keys`, `ApiKeyManager*`, `phase-148-owner-lane-cache-isolation`, `no-store-coverage`, `limiter-ordering`, `live-db-execution-ledger.contract` | 120 files, 2089 tests passed, 0 failed, rc 0 |
| Neuter: `/strategies` treats every probed row as buildable (`if (probe.buildable) return null` made unconditional) | RED. COMPUTED-UNBUILDABLE, COMPOSITE-UNBUILDABLE, PUBLIC-UNBUILDABLE, UNBUILDABLE-IN-PROGRESS, UNBUILDABLE-RPC-ERROR, PROBE-READ-ERROR, PROBE-NOT-VISIBLE, PUBLISHED-PROBE-FAILS and others failed. Restored by byte backup, `cmp` identical, 0 neuter markers left. |
| Next `unstable_cache` source, bundled 16.2.11 | Read directly. On a miss the callback is awaited before `cacheNewResult`, so a throw propagates and nothing is stored. On a stale entry the background revalidation's `.catch` returns the cached value and never calls `cacheNewResult`. This matches the claim the WR-02 fix rests on. |
| Local-stack lane (`scripts/local-stack/run.sh up`, under the shared `mkdir` lock) | Did not boot. Two attempts, both exit 1 inside the Supabase CLI start. `teardown` ran, 0 supabase containers remained, and the lock was released. The live-DB spec was therefore NOT re-run by the verifier. See human item 1. |

The orchestrator's wider run (157 files, 2615 tests) is consistent with the subset above. It was not repeated in full.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | SC1: the mismatch is reproduced first on the local lane: a computed row whose payload does not build | ? UNCERTAIN | `factsheet-buildable-live-db.test.ts` exists, with REPRO-SINGLE-ONE-POINT, REPRO-COMPOSITE-PRE86 and CONTROL-BUILDABLE. It is in the lane corpus (the `CORPUS_FLOOR` raise in `scripts/live-db-lane-corpus.mjs`). The unit REPRO arms in `fetch-and-build-payload.test.ts` pass in the verifier's run. The lane run itself is recorded only in the 01 SUMMARY and the R2-D fixer report, and the verifier's lane boot failed. This is routed to the SHA-bound `frontend-live-db-lane` job (human item 1). |
| 2 | SC2: buildability is decided server-side for the list, by the builder's own code | ✓ VERIFIED | `resolveFactsheetInputs` holds G0 to G4. G4 is `hasBuildableSeries`, the same predicate `buildFactsheetPayload` calls. `probeFactsheetBuildable` runs that stage and nothing else. `buildFromResolved` is typed non-null over a `BuildableSeries`, so a null exit past the stage does not compile. The PARITY table, NO-NULL-AFTER-RESOLVE and WR-03 WITH-REASON pass. |
| 3 | SC3: the list, the share note and the share page agree for that row, proven by a test observed RED against today's code | ✓ VERIFIED | `StrategiesPage` probes each computed row and picks the note through `recipientShareNoteFor` and `unbuildableNoteKindOf`. The share page, unchanged, renders its `not_available` card for a null `fetchAndBuildPayload` unless a job is in progress, and the D-02 selection rule maps exactly that arm. COMPUTED-UNBUILDABLE is recorded RED against the pre-change page (03 SUMMARY), and the verifier re-observed it RED under a neuter. |
| 4 | SC4: any migration passes the three reviewers before merge | ✓ VERIFIED (vacuous) | The branch diff against the merge base touches no `supabase/` path. D-01 replaced the migration with a route, and a dated ROADMAP note records that. |
| 5 | SC5: the key-card Delete warning cannot be silenced by an RLS-filtered membership read | ✓ VERIFIED | `GET /api/keys/[id]/memberships` checks ownership with an explicit `user_id` equality on the service role, then reads `strategy_keys` on the same client. A non-array answer is a 500, never `[]`. `readKeyCompositeMemberships` fetches the route. The browser no longer reads `strategy_keys`, and any non-2xx, non-JSON, non-array or timed-out answer renders KCS-DELETE-UNCHECKED. Route tests RLS-REGRESSION and MEMBERS-NOT-ARRAY-500 pass, as do client tests R2-WR02-VIA-ROUTE, R2-WR02-UNREADABLE and UNCHECKED-TIMEOUT. |
| 6 | D-02: an unbuildable row gets its own precise note, never MINT-B, with the threshold tied to the gate | ✓ VERIFIED | `MINT_UNBUILDABLE_NOTES`, `PUBLIC_UNBUILDABLE_NOTES` and `MINT_UNBUILDABLE_UNREADABLE_NOTES` carry the round-2 (N-5) SHORT text and the CR-01 COMPOSITE text. The DRIFT test pins "2" to `MIN_FACTSHEET_SERIES_POINTS`. The support address comes from `SUPPORT_EMAIL`. |
| 7 | D-04: `probe.buildable === (fetchAndBuildPayload(...) !== null)` for a builder that does not throw | ✓ VERIFIED | This holds structurally (see truth 2), and the parity table samples it. The domain limit (a builder throw) is stated at the probe and in the IN-03 comment on the page. |
| 8 | D-05 (as revised): a failed check is never read as buildable or as "not available"; captures follow the round-2 rule | ✓ VERIFIED | A throw, a `read_error`, a `not_visible` or a timeout (`FactsheetProbeTimeoutError`, `FACTSHEET_PROBE_DEADLINE_MS`) renders `probeUnreadableShareNote`. The stage captures only for `caller === "build"`. `/strategies` sends one aggregated event per load (`captureProbeOutcomes`) and at most `MAX_PROBE_THROW_EVENTS` grouped throw events (`captureProbeThrows`). `not_computed` and `not_visible` are warnings. |
| 9 | D-06: the owner factsheet's S7 note uses the same derivation as the list | ✓ VERIFIED | The owner lane uses `fetchAndBuildPayloadWithReason`, one resolve that serves both payload and reason. `ownerBuildabilityOf` applies the list's mapping, and `ownerStateLine` / `ownerRemedy` take `buildUnreadable` (KCS09-FINISHED-UNREADABLE). S7-UNBUILDABLE-SHORT and S7-UNBUILDABLE-COMPOSITE pass. |
| 10 | D-08 (as revised): only computed rows are probed, uncached, bounded | ✓ VERIFIED | Uncomputed rows return through `uncomputedNote` before any probe. Probes run through `concurrencyLimiter(PROBE_CONCURRENCY)` and never through `buildFactsheetPayloadCached`. NOT-PROBED-UNCOMPUTED and OWNER-PREDICATE pass. |
| 11 | D-11 (as revised): the public cache is keyed by the analytics run, and a transient read error is never cached | ✓ VERIFIED | The keyParts are `["factsheet-v2-payload-v6", id, computedAt]`. A `read_error` throws `FactsheetReadError` inside the callback, and the page renders the placeholder uncached. The public-cache-key tests pass (CACHED-NULL-THEN-COMPUTED, CACHED-PAYLOAD-THEN-UNBUILDABLE, READ-ERROR-NOT-CACHED, KEY SHAPE). The test's `next/cache` double is backed by the verifier's own reading of the bundled Next source (table above). |
| 12 | D-03 / D-12: the discovery double assembly is booked to Phase 169 in TODOS and the ROADMAP; no plan edits VERSION or CHANGELOG | ✓ VERIFIED | `[167.2.1-DISCOVERY-DETAIL-DOUBLE-ASSEMBLY]` is in `TODOS.md`, with a dated routed note under `### Phase 169`. The branch diff touches neither VERSION, CHANGELOG.md nor package.json. |

**Score:** 11/12 truths verified (0 present-but-behavior-unverified, 1 uncertain).

### Required Artifacts

| Artifact | Expected | Status |
|---|---|---|
| `src/lib/factsheet/fetch-and-build-payload.ts` | shared resolve stage, `probeFactsheetBuildable`, `fetchAndBuildPayloadWithReason` | ✓ VERIFIED, wired (strategies page, v2 page) |
| `src/lib/factsheet/build-payload.ts` | exported `hasBuildableSeries`, `MIN_FACTSHEET_SERIES_POINTS` | ✓ VERIFIED, wired (resolve stage, copy test) |
| `src/lib/status-surface-copy.ts` | `unbuildableNoteKindOf`, `recipientShareNoteFor`, `probeUnreadableShareNote`, KCS09-FINISHED-UNREADABLE | ✓ VERIFIED, wired (both owner surfaces) |
| `src/app/(dashboard)/strategies/page.tsx` | probe per computed row, bounded, aggregated capture | ✓ VERIFIED |
| `src/app/factsheet/[id]/v2/page.tsx` | owner S7 derivation, run-keyed public cache | ✓ VERIFIED |
| `src/app/api/keys/[id]/memberships/route.ts` | owner-checked service-role membership read | ✓ VERIFIED, wired (`readKeyCompositeMemberships`) |
| `src/components/strategy/ApiKeyManager.tsx` | fetch the route, fail closed to KCS-DELETE-UNCHECKED | ✓ VERIFIED |
| `src/__tests__/factsheet-buildable-live-db.test.ts` | SC1 reproduction on real rows | ✓ EXISTS, in the lane corpus. Its run is item 1. |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `StrategiesPage` | `probeFactsheetBuildable` | `runProbe(() => probeFactsheetBuildable(s.id, withPublishedOrOwner(·, user.id)))` | WIRED |
| owner lane in `FactsheetV2Page` | `fetchAndBuildPayloadWithReason` | owner predicate, uncached | WIRED |
| `buildFactsheetPayloadCached` | `fetchAndBuildPayloadWithReason` | `read_error` throws, other reasons return null | WIRED |
| `readKeyCompositeMemberships` | `GET /api/keys/[id]/memberships` | `fetch` with no-store, bound, abort | WIRED |
| share page | `fetchAndBuildPayload` | unchanged, identity predicate after the token match | WIRED (ground truth) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| The list note bites on an unbuildable row | neuter plus `vitest run page.key-pill.test.tsx` | RED on the SC3 cases, GREEN after restore | ✓ PASS |
| A throw inside `unstable_cache` is not stored | read the bundled Next source | callback awaited before `cacheNewResult`; the stale path returns the cached value on error | ✓ PASS |
| Live lane reproduction | `run.sh up` | the stack did not boot | ? SKIP, see item 1 |

### Probe Execution

No `scripts/*/tests/probe-*.sh` is declared by this phase. The runtime "probe" here is `probeFactsheetBuildable`, a function exercised by the tests above. Step 7c: not applicable.

### Requirements Coverage

The ROADMAP entry lists `Requirements: TBD`. Coverage is measured against success criteria 1 to 5 (truths 1 to 5). No REQUIREMENTS.md IDs are mapped to this phase, so none are orphaned.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|---|---|---|---|
| (all added lines under `src/` and `scripts/`) | TBD / FIXME / XXX / TODO / HACK | none found | none |
| `.planning/ROADMAP.md` `### Phase 169`, and `169-04-PLAN.md` on the Phase 169 branch | The composite `csv_daily_returns`-outage residual (a cached `null` on a transient csv read error, CONTEXT D-07 round-2 revision) is said to be "owned by Phase 169 plan 04". Neither the Phase 169 ROADMAP section nor that plan names it. That plan's `composite-read-path.ts` scope is `readSingleKeyBasisOpts`, not `readCompositeFactsheet` returning its read error. | ⚠️ WARNING | The routing is recorded on the sending side only. The deviation policy asks for CONTEXT and ROADMAP, and D-03 set the precedent of a dated routed note under `### Phase 169`. Fix before or at ship: add a dated routed note under `### Phase 169` naming the residual and `readCompositeFactsheet`. |
| `167.2.1-CONTEXT.md` | two decisions carry the id `D-12` (no VERSION/CHANGELOG, and Sentry `strategy_id` as a tag) | ℹ️ Info | An ambiguous citation. Renumber the Sentry one (for example D-13) when the ledger is next edited. |
| `.planning/ROADMAP.md` `### Phase 167.2.1` | the four `167.2.1-0N-PLAN.md` boxes are unchecked while all four SUMMARYs exist | ℹ️ Info | Paperwork for the ship step. |

### Routed residuals (checked for phase and date)

| Residual | Where routed | Status |
|---|---|---|
| Composite csv-outage cached `null`, and SFH-R2 N-6's composite half | Phase 169 plan 04, CONTEXT D-07 revision 2026-09-26 | ⚠️ sending side only (see Anti-Patterns) |
| `[167.2.1-CLIENT-SENTRY-NOOP]` | `TODOS.md`, booked 2026-09-26 | ✓ present |
| `[167.2.1-LOCAL-STACK-NO-CROSS-WORKTREE-LOCK]` | `TODOS.md`, booked 2026-09-26 | ✓ present |
| `[167.2.1-STRATEGIES-PROBE-COST]` | `TODOS.md`, booked 2026-09-26 | ✓ present |
| Delete TOCTOU window (D-10) | accepted, founder "warn, never block" | ✓ recorded |

### Notes for ship

- `origin/main` is 3 commits ahead of the merge base. None of them touch a file this phase changed, so a rebase is mechanical.
- D-09: if Phase 169 merges first, this phase re-runs the parity table and the SC1 lane reproduction after its rebase, and records both.
- `verified_at_sha` is the code head verified above. It is not the commit that carries this report.

### Human Verification Required

1. **SC1 lane run, SHA-bound.** Confirm `frontend-live-db-lane` is green on the PR head, 7 of 7 for the FACTSHEETBUILDABLE spec. Why: the verifier's own lane boot failed, so the lane evidence is narration only. Owner: orchestrator, at ship.
2. **Visual at 320px and 200% zoom.** Check the /strategies unbuildable and "could not check" notes, the owner S7 panel with its state line, and the Delete confirm's composite warning and KCS-DELETE-UNCHECKED. Why: layout and reflow cannot be seen in jsdom. Owner: orchestrator, in the logged-in Chrome after deploy.
3. **Live PROD agreement read.** For a computed-but-unbuildable row, the list note, the owner S7 note and the signed-out share page should agree. A buildable row should show no note. Why: this needs real rows and a real token. Owner: orchestrator, after deploy. Record verdicts and counts only.

### Gaps Summary

No must-have FAILED. The code achieves the goal. The list and the owner factsheet decide buildability from the builder's own resolve stage and name the reason, and they agree with the unchanged share page by construction and by test. The Delete warning reads memberships server-side and fails closed. One truth, the SC1 lane reproduction, is UNCERTAIN only because the verifier could not boot the lane. It is routed to the CI job that fails rather than skips. One routing WARNING stands: the composite csv-outage residual is not yet visible where Phase 169 is planned.

---

_Verified: 2026-09-26_
_Verifier: Claude (gsd-verifier)_
