---
phase: 167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i
plan: 04
type: execute
wave: 3
depends_on: ["167.2.1-01", "167.2.1-03"]
files_modified:
  - src/app/factsheet/[id]/v2/page.tsx
  - src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx
  - TODOS.md
autonomous: true
requirements: [SC3]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "SC3 / D-06: on the owner factsheet's pending panel (S7), an unpublished row whose analytics are computed but whose factsheet cannot build, with a finished chain job, shows the same KCS12-UNBUILDABLE line the /strategies list shows for it. The case S7-UNBUILDABLE-SHORT was observed RED against today's page (it rendered KCS12-MINT-B) before the fix."
    - "D-06: the owner lane probes ONLY on its null-payload path, with the same owner predicate as its builder call, and selects the note with recipientShareNoteFor('mint-token', recipientArm(ownerStatus.state), unbuildableNoteKindOf(reason)); a buildable, not_computed, not_visible or read_error probe answer leaves today's note unchanged; a thrown probe leaves today's note and is logged and captured with tags only (stage factsheet-probe)."
    - "The public lane and the full owner render run no probe; the cached wrapper is untouched (D-11)."
    - "D-03: TODOS.md carries one drift-risk entry for the discovery detail page's second assembly of the builder, routed to Phase 169 PAGETRUTH."
    - "Neuter to RED to restore is recorded in the SUMMARY for: the owner lane passing a null kind (S7-UNBUILDABLE-SHORT RED), the probe called on the public lane (PUBLIC-PENDING RED on its no-owner-read assertion)."
  artifacts:
    - path: src/app/factsheet/[id]/v2/page.tsx
      provides: "the S7 note on the owner null-payload path, derived like the list's"
    - path: src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx
      provides: "S7-UNBUILDABLE-SHORT (RED first), S7-UNBUILDABLE-COMPOSITE, S7-PROBE-THROWS"
    - path: TODOS.md
      provides: "the D-03 drift-risk entry routed to Phase 169"
  key_links:
    - from: src/app/factsheet/[id]/v2/page.tsx
      to: src/lib/factsheet/fetch-and-build-payload.ts
      via: "probeFactsheetBuildable on the owner lane's null-payload path"
      pattern: "probeFactsheetBuildable\\("
    - from: src/app/factsheet/[id]/v2/page.tsx
      to: src/lib/status-surface-copy.ts
      via: "recipientShareNoteFor for the OwnerUnpublishedPanel shareNote"
      pattern: "recipientShareNoteFor\\("
---

<objective>
Keep the two owner surfaces in agreement (D-06). Once plan 03 gives the list the precise D-02 line,
the owner factsheet's pending panel must show the same line for the same row. Without this plan it
keeps KCS12-MINT-B, whose tail is false for a row whose computation succeeded.
Also book the D-03 drift risk in TODOS.md.

Purpose: SC3 requires the list, the share note and the share page to agree. The share note appears
on two owner surfaces: S5, the list, and S7, the owner factsheet.
Output: the S7 note derived through the same probe and selection rule, and the TODOS entry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-CONTEXT.md
@src/app/factsheet/[id]/v2/page.tsx
@src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx
Read once plans 01 and 03 have landed (not @-referenced): `167.2.1-01-SUMMARY.md` and
`167.2.1-03-SUMMARY.md`, `probeFactsheetBuildable` in src/lib/factsheet/fetch-and-build-payload.ts,
and `recipientShareNoteFor` / `unbuildableNoteKindOf` in src/lib/status-surface-copy.ts.

<!-- verify-symbol: src/app/factsheet/[id]/v2/page.tsx readOwnerPendingStatus -->
<!-- verify-symbol: src/app/factsheet/[id]/v2/page.tsx OwnerUnpublishedPanel -->
<!-- verify-symbol: src/app/factsheet/[id]/v2/page.tsx recipientShareNote -->
<!-- verify-symbol: src/app/factsheet/[id]/v2/page.tsx buildFactsheetPayloadCached -->
<!-- verify-symbol: src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx givenOwnerPendingDraft -->
<!-- verify-symbol: src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx mockAdmin -->
<!-- verify-symbol: src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx buildFactsheetPayload -->
<!-- verify-symbol: src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx readCompositeFactsheet -->
</context>

<hard_constraints>
Carried into every task of this plan:
- **Worktree setup:** symlink the main checkout's `node_modules`, finding the path at run time with
  `git worktree list`. TEST a command. Stage by explicit path only. Remove the symlink before every
  commit. ⛔ Never write the resolved path into a tracked file.
- **Verify logs:** `export FB_SP=<your session scratchpad directory>` first. Never use /tmp. ⛔ Never
  write the expanded path into a tracked file.
- **Precondition:** plans 01 and 03 are merged into this branch. `grep -c 'export function recipientShareNoteFor' src/lib/status-surface-copy.ts`
  returns 1. If it does not, halt.
- ⛔ The owner lane's builder call, the cached wrapper `buildFactsheetPayloadCached`, the public
  lane and the full owner render do not change. The probe runs ONLY where `lane === "owner"` and
  the payload is null, and its result is a lane-local value handed only to the panel's `shareNote`.
  It never enters the payload or the cache (the same rule the file states for `ownerStatus`).
- **No database command against any remote.** Unit tests only. No migration.
- **Public repo:** synthetic ids and names only. The TODOS entry names no strategy and no identifier.
- ⛔ Never `git checkout -- <path>`, `git restore`, `git stash` or `npm version`. Take byte backups
  for neuter checks. No VERSION or CHANGELOG edit (D-12). No CI skip token in any commit message.
- Re-read `v2/page.tsx` at HEAD before editing (D-09: other phases may have moved it). Keep hunks
  minimal. Cite by SYMBOL.
</hard_constraints>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the owner factsheet's pending panel shows the same unbuildable line as the list, from the owner lane's probe to the rendered S7 note (RED first)</name>
  <files>src/app/factsheet/[id]/v2/page.tsx, src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/page.tsx: the owner-arm `fetchAndBuildPayload(id, (q) => withPublishedOrOwner(q, ownerUid!))` call, the `!payload` branch, `readOwnerPendingStatus`, and the `OwnerUnpublishedPanel` `shareNote` prop built with `recipientShareNote("mint-token", recipientArm(ownerStatus.state))`
    - src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx: `mockAdmin`, `STATE.adminRow`, `givenOwnerPendingDraft`, `givenJobs`, `panelOf`, the `KCS-12 (S7)` describe, and `PUBLIC-PENDING` (its no-owner-read assertions)
    - src/lib/status-surface-copy.ts `recipientShareNoteFor`, `unbuildableNoteKindOf` (plan 03) and src/lib/factsheet/fetch-and-build-payload.ts `probeFactsheetBuildable` (plan 01)
    - 167.2.1-CONTEXT.md D-02, D-06
  </read_first>
  <action>
    Run `node scripts/verify-plan-anchors.mjs` on this plan file and halt on non-zero.
    RED FIRST. In the test file, add `S7-UNBUILDABLE-SHORT` to the KCS-12 (S7) describe:
    - `givenOwnerPendingDraft()`;
    - set `STATE.adminRow` to a computed row: an embedded `strategy_analytics` with `computation_status` 'complete', ONE `daily_returns` point, `returns_series` null, and no composite flag;
    - `givenJobs` with a finished (done) chain job.
    Expect the panel's last child to equal the hand-typed UNBUILDABLE_SHORT oracle from CONTEXT D-02. Extend `mockAdmin` only as far as the builder's and the probe's reads need: a `csv_daily_returns` chain answering no rows, and no `strategy_analytics_series` rows. Keep every existing case's behaviour; with `adminRow` null the probe answers not_visible and today's note stands.
    Run it against TODAY's page. It must fail on the ASSERTION with the MINT-B text. Record that in the SUMMARY.
    Implement in v2/page.tsx, on the owner lane's `!payload` path only. After `readOwnerPendingStatus`, compute a lane-local `unbuildableKind`:
    - when `lane === "owner"`, await `probeFactsheetBuildable(id, (q) => withPublishedOrOwner(q, ownerUid!))` inside try/catch;
    - a `buildable: false` answer gives `unbuildableNoteKindOf(reason)`; anything else gives null;
    - a throw gives null, plus a console.error and `captureToSentry` with tags `{ route: "factsheet/v2/page", stage: "factsheet-probe" }` only.
    Build the panel's `shareNote` with `recipientShareNoteFor("mint-token", recipientArm(ownerStatus.state), unbuildableKind)` when `ownerStatus` is set, and `undefined` otherwise, as today.
    Add a comment naming D-06: the list and this panel derive the note the same way, and the probe runs only on this null path, after the builder has already said null. Also add S7-UNBUILDABLE-COMPOSITE (a computed composite with `metrics_json_by_basis` null and a finished job, expecting UNBUILDABLE_COMPOSITE) and S7-PROBE-THROWS (the admin factory throws on its SECOND call, expecting today's MINT-B note, console.error, and one capture with `stage: "factsheet-probe"`).
    If an EXISTING case's admin fixture turns out to be computed-but-unbuildable, its expected text now changes. It pinned the WR-02 defect, so MOVE it with a lineage comment naming D-06. Never weaken it.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx" "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" "src/app/factsheet/[id]/v2/page.stale-analytics.test.tsx" "src/app/factsheet/[id]/v2/page.smoothed-wiring.test.tsx" "src/app/factsheet/[id]/v2/FactsheetView.recipient-share.test.tsx" "src/app/factsheet-share/[token]" src/__tests__/phase-148-owner-lane-cache-isolation.test.ts --reporter=verbose > ${FB_SP:?FB_SP_unset}/fb-04-t1.log 2>&1; RC=$?; tail -60 ${FB_SP:?FB_SP_unset}/fb-04-t1.log; test "$RC" = 0 && grep -q 'S7-UNBUILDABLE-SHORT' ${FB_SP:?FB_SP_unset}/fb-04-t1.log && grep -q 'PUBLIC-PENDING' ${FB_SP:?FB_SP_unset}/fb-04-t1.log && ! grep -qE '(^|[^0-9])0 (passed|tests)' ${FB_SP:?FB_SP_unset}/fb-04-t1.log && npm run typecheck && npx eslint "src/app/factsheet/[id]/v2/page.tsx" "src/app/factsheet/[id]/v2/page.owner-compute-state.test.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records S7-UNBUILDABLE-SHORT failing on its assertion (MINT-B text) against the pre-change page, then passing.
    - `grep -c 'probeFactsheetBuildable(' "src/app/factsheet/[id]/v2/page.tsx"` is 1, and `grep -c 'recipientShareNoteFor(' "src/app/factsheet/[id]/v2/page.tsx"` is 1.
    - Neuter checks, each against a fresh byte backup: (1) pass `null` as the kind, and S7-UNBUILDABLE-SHORT goes RED; (2) run the probe regardless of lane, and PUBLIC-PENDING goes RED on its no-owner-read assertion (if it does not, add that assertion first and say so). Both restored GREEN and recorded in the SUMMARY.
  </acceptance_criteria>
  <done>For the same computed-but-unbuildable row, the list and the owner factsheet show the same D-02 line, and a probe error never changes a note into a "has a factsheet" claim.</done>
</task>

<task type="auto">
  <name>Task 2: book the discovery detail page's second builder assembly as a drift risk routed to Phase 169 (D-03)</name>
  <files>TODOS.md</files>
  <read_first>
    - TODOS.md: the `## 🟡 FIX MID-TERM` section's entry shape (a `- [ ] **\`[ID]\` title**` bullet with measured facts, the shape of the fix, and severity/routing lines)
    - src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx: the calls to `resolveDailyReturnSeries`, `readCompositeFactsheet`, `readSingleKeyBasisOpts` and `buildFactsheetPayload` (cite by symbol)
    - .planning/ROADMAP.md `### Phase 169` (its goal and SC4, which owns factsheet KPI sourcing)
    - 167.2.1-CONTEXT.md D-03
  </read_first>
  <action>
    Add one entry at the end of the `## 🟡 FIX MID-TERM` list, with the id `[167.2.1-DISCOVERY-DETAIL-DOUBLE-ASSEMBLY]`, dated 2026-09-25. State:
    - what the page does: it calls the builder's steps itself (the four symbols above) instead of `fetchAndBuildPayload`, measured at HEAD by grep, with the grep line quoted as a command and not as output;
    - why it is a drift risk and not a false claim today: it serves published rows only and falls back to KCS-10;
    - that Phase 167.2.1 split the builder into a resolve stage plus `hasBuildableSeries` (D-04), so the natural fix is to call `fetchAndBuildPayload`, or the resolve stage, from that page;
    - Severity: a drift risk on a user-facing number surface, not broken today;
    - Routing: Phase 169 PAGETRUTH, which owns factsheet KPI sourcing.
    Name no strategy, no id and no local path. Do not edit the ROADMAP: the orchestrator owns that.
  </action>
  <verify>
    <automated>grep -c '167.2.1-DISCOVERY-DETAIL-DOUBLE-ASSEMBLY' TODOS.md | grep -qx 1 && grep -A12 '167.2.1-DISCOVERY-DETAIL-DOUBLE-ASSEMBLY' TODOS.md | grep -q 'Phase 169' && npm run check:planning-hygiene && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one entry with that id, inside the FIX MID-TERM section, routed to Phase 169.
    - `npm run check:planning-hygiene` passes.
  </acceptance_criteria>
  <done>The discovery detail duplication has one owner and one entry, and nothing in this phase changes that page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner v2 RSC → service-role probe | runs only on the owner lane, after the signature gate proved ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167.2.1-19 | Information disclosure | probe result leaking into the id-keyed cache | high | mitigate | The probe runs only on `lane === "owner"` with a null payload, and its result is lane-local, like `ownerStatus`. The cached wrapper is untouched, and the phase-148 closure guard is in the verify set. Neuter (2) proves the public lane reads nothing. |
| T-167.2.1-20 | Repudiation | probe error changing a note into a "has a factsheet" claim | medium | mitigate | A throw leaves today's note and is captured (S7-PROBE-THROWS). This path is reached only when the payload is already null. |
| T-167.2.1-21 | Information disclosure | TODOS.md on a public repo | low | mitigate | The entry names no strategy, id or local path, and the hygiene check runs. |
</threat_model>

<verification>
- S7-UNBUILDABLE-SHORT was observed RED against today's page and is GREEN now. The neuter runs are recorded.
- The TODOS entry exists once and is routed to Phase 169.
- `node scripts/verify-plan-anchors.mjs .planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-04-PLAN.md` exits 0 at the start.
</verification>

<success_criteria>
The owner never reads two different notes for the same computed-but-unbuildable strategy, and the
remaining duplicate builder assembly has an owner.
</success_criteria>

<output>
Create `.planning/phases/167.2.1-factsheetbuildable-a-strategy-is-called-computed-only-when-i/167.2.1-04-SUMMARY.md` when done.
</output>
