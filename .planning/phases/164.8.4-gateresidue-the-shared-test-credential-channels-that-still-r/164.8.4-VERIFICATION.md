---
phase: 164.8.4-gateresidue-the-shared-test-credential-channels-that-still-r
verified: 2026-09-19T12:58:00Z
status: passed
score: 4/4 live must-haves verified
verified_at_sha: a71d7245f1ba46a264faf6920240ffbc5aace10e
drift_subjects:
  - scripts/redact-psql-stderr.sed
  - scripts/derive-restore-channels.sh
  - scripts/test-ledger-drift-check.sh
  - .github/workflows/ci.yml
  - .github/workflows/test-restore-from-baseline.yml
  - .github/workflows/supabase-migrate.yml
  - .github/workflows/mutex-probe.yml
  - src/test-setup.ts
  - src/test-setup.live-db-guard.test.ts
  - src/__tests__/test-restore-workflow-wiring.test.ts
  - src/__tests__/drift-check-scripts.test.ts
  - src/__tests__/critical-regressions.test.ts
  - src/__tests__/supabase-migrate-test-first.test.ts
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts
overrides_applied: 0
---

# Phase 164.8.4: GATERESIDUE Verification Report

**Phase Goal (the four LIVE Success Criteria, per the 2026-09-17 founder scope prune):**
1. Every psql site in `test-ledger-drift-check.sh`, `test-restore-from-baseline.yml` and their
   shared redaction is swept in ONE pass, with a test that FAILS on a new unredirected site.
2. The `gstack-evidence` credential inheritance is closed, skip gates untouched, proven by a run
   whose skip counts match a plain `npx vitest run` at the same commit.
3. The channel allowlist is DERIVED, and a calibration proves a newly written channel reaches the
   artifact without anyone editing a list.
4. Nothing widens `WAIVED_CEILING`, relaxes a floor, or adds an exemption to make a gate pass.

**Verified:** 2026-09-19, at HEAD `a71d7245` (branch `feat/164.8.4-gateresidue`, branched from
`main` at `8371be23`).
**Status:** passed
**Re-verification:** No — initial verification.

⚠️ **Note on the task brief's stated HEAD.** The verification brief named HEAD as `0f19541e`.
At the start of this run, `git rev-parse HEAD` returned `a71d7245` — one commit later. That
commit (`chore: drop the REDACTCONFIRM phase — the deferral stays a TODO (founder call)`) reverses
part of what the brief's `<known_open_and_deliberate>` section describes (Phase 164.8.7
REDACTCONFIRM was inserted and then removed the same day). This report verifies against the actual
HEAD, `a71d7245`, and calls out the discrepancy explicitly below rather than verifying a stale
snapshot silently.

## Goal Achievement

### Observable Truths (the four live criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Psql sweep — one pass, test FAILS on a new unredirected site | ✓ VERIFIED | Three independent calibration tests, each run directly and confirmed RED-on-mutation / GREEN-on-real-file: `test-restore-workflow-wiring.test.ts` — `"a NEW capture-then-echo site with nothing redacting it FAILS this gate — SC-1's own requirement"` (ci.yml); `drift-check-scripts.test.ts` — `"UP: a NEW softening site is reported as UNLISTED"` (test-ledger-drift-check.sh); `test-restore-workflow-wiring.test.ts`'s `ALLOWED_SITES` bijection (`SITE_ALLOWLISTED_TOKEN` UNLISTED-SITE branch, test-restore-from-baseline.yml). All three ran green in this session. Repo-wide grep confirms 0 inline `sed -E -e`/`sed -i -E` redaction copies remain outside `scripts/redact-psql-stderr.sed` across all 4 workflows (26 references, all `${GITHUB_WORKSPACE}`-rooted). |
| 2 | Credential-inheritance closure, skip gates untouched, skip-count parity vs plain `npx vitest run` | ✓ VERIFIED | `src/test-setup.ts` carries `assertLiveDbWasIntended()`, a module-scope fail-loud guard gated on `VITEST_LIVE_DB_INTENDED=1`; `git diff main..HEAD -- src/lib/test-helpers/live-db.ts` is empty (skip gates byte-unchanged). Ran the FULL suite once, unshared box, myself: `854 passed | 19 skipped (875)` test files, `14886 passed | 6 failed | 280 skipped (15172)` tests — **19 skipped files / 280 skipped tests exactly matches** the SUMMARY's own baseline-vs-parity reading (19/280 in both the guard-disabled and guard-enabled runs). The 6 failures are two pre-existing, unrelated 5000ms-timeout flakes (see Anti-Patterns / Behavioral Spot-Checks below) — re-run individually with headroom, both pass (580/580 and 74/74). `src/test-setup.live-db-guard.test.ts` (4 legs, both polarities + no-credential-value guarantee) passes 4/4. |
| 3 | Channel allowlist DERIVED, calibration proves a new channel reaches the artifact | ✓ VERIFIED | `scripts/derive-restore-channels.sh` scans both producers' declared write targets (never a runtime glob); `--self-check` passes when run directly. Three-armed calibration in `test-restore-workflow-wiring.test.ts` (`"[164.8.4-05] the staged channel set is DERIVED from the producers, not hand-typed"`) — arm 1 (agreement with an independently-written regex, non-vacuous), arm 2 (a new declared channel is picked up AND an undeclared on-disk file is NOT), arm 3 (no literal channel name survives in the staging step) — ran directly: 3/3 pass. |
| 4 | Nothing widens a ceiling, relaxes a floor, or adds an exemption | ✓ VERIFIED | Diffed the full branch against `main` for `WAIVED_CEILING`/`EXPECTED_ARMS`/`FILES_FLOOR`/`.skip(`/new `toContain`-for-`toEqual` softenings — none found. `EXPECTED_ARMS` in `test-ledger-drift-check.sh` unchanged at 11. `gate-family-meta.test.ts`'s diff REMOVES a stale allowlist entry (narrower, the correct direction). The one flagged change — `test-restore-workflow-wiring.test.ts`'s twin-1 assertion moving from `toEqual(keepers.slice().sort())` to `toEqual([...keepers, residual].sort())` — was independently re-derived in this session (not merely trusted from REVIEW.md): the portable `sed … > "$f.redacted" && mv` form's shell redirect creates an empty `.redacted` residue file BEFORE the command runs, on a forced failure. It remains an EXACT-SET equality (not `toContain`/`arrayContaining`), and the residue is unpublishable four independent ways, each confirmed by reading the live code: `withhold_channels()` globs only `*.err/*.log/*.out` (`.redacted` matches none); `derive-restore-channels.sh`'s `WRITE_TARGET_RE` ends `\.(err|log|out)`, so no producer can ever declare a `.redacted` target; the staging step's channel-copy loop runs only `if [ "${REDACT_OUTCOME}" = "success" ]`, and a forced-fail scenario reports `failure`; an empty/unrecognised `REDACT_OUTCOME` is a `MEASURE_FAIL` that `exit 1`s rather than silently denying. Not a relaxation. |

**Score:** 4/4 live truths verified. 0 present-but-behaviour-unverified. 0 human-verification items.

### Deviation recorded, and where it is (and is not) written down

**Criterion 2's literal wording ("closed at the wrapper") vs. the shipped mechanism (a repo-side
guard).** `164.8.4-CONTEXT.md` line ~105 carries an explicit, reasoned decision: *"the repo-owned
remedy is a REPO-SIDE guard, not a wrapper edit"* — because `gstack-evidence` has no repo-local
copy and lives under a global tool path `/gstack-upgrade` overwrites, so a fix there would not
survive. This is a legitimate, recorded, reasoned deviation that achieves the underlying goal (the
credential-inheritance leak is closed, durably, and skip gates stay untouched) — arguably more
robust than a literal wrapper edit would have been.

⚠️ **However, `ROADMAP.md`'s SC-2 text itself was never edited to reflect this** — it still reads
*"closed at the wrapper"* verbatim (`.planning/ROADMAP.md`, Success Criteria list 2, item 2). Per
this repo's own standing rule (CLAUDE.md, "Deviation policy": *"record the reason and the override
... in the phase's CONTEXT.md **and** the ROADMAP, never just one"*), only CONTEXT.md carries the
reasoning; ROADMAP.md does not. This is a **documentation-only gap**, not a functional one — the
goal is achieved and the reasoning exists in the phase's own artifacts — but it means a future
reader of ROADMAP.md alone would not learn why the wrapper was never touched. Recommend a small
follow-up ROADMAP annotation (not a new phase) the next time this phase's ROADMAP entry is edited.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `scripts/redact-psql-stderr.sed` | Union of all six psql-stderr redaction shapes | ✓ VERIFIED | 6 expressions present, read from disk; all 26 workflow references are `-f`, `${GITHUB_WORKSPACE}`-rooted |
| `scripts/derive-restore-channels.sh` | Derive channel set from producer source text, default-DENY | ✓ VERIFIED | `--self-check` passes; extension-restricted to `.err/.log/.out`; wired into `sql-gate-lint` in `ci.yml` (confirmed: job exists at `ci.yml:1062`, step present, `sql-gate-lint` is in `frontend` aggregator's `needs:` (`:2563`) and result loop (`:2624`)) |
| `scripts/test-ledger-drift-check.sh` | `missing`-direction stderr captured, counted, withheld | ✓ VERIFIED | `run_ledger_query missing ... > "$missing_file" 2>"$missing_err"`; bounded `set +e`/`set -e` with rc read; message reports line-count only |
| `src/test-setup.ts` | Fail-loud module-scope credential-inheritance guard | ✓ VERIFIED | `assertLiveDbWasIntended(HAS_LIVE_DB, process.env)` as first module-scope statement after imports; `live-db.ts` byte-unchanged |
| `src/test-setup.live-db-guard.test.ts` | Both polarities + no-value-leak falsifiable | ✓ VERIFIED | 4/4 pass, run directly |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| Every workflow psql call site | `scripts/redact-psql-stderr.sed` | `sed -E -f "${GITHUB_WORKSPACE}/scripts/redact-psql-stderr.sed"` | ✓ WIRED (26 references across 4 workflow files, 0 stragglers) |
| `test-restore-from-baseline.yml` staging step | `scripts/derive-restore-channels.sh` | invoked once at step start, loop consumes `${derived_channels}` | ✓ WIRED |
| `src/test-setup.ts` | `src/lib/test-helpers/live-db.ts`'s `HAS_LIVE_DB` | imported, never recomputed | ✓ WIRED |
| `ci.yml`'s `sql-gate-lint` job | `derive-restore-channels.sh --self-check` | new step, no `continue-on-error`, no softening token | ✓ WIRED |

### Behavioral Spot-Checks (run directly in this session, not trusted from SUMMARY/REVIEW)

| Behavior | Command | Result | Status |
|---|---|---|---|
| `src/test-setup.live-db-guard.test.ts` all 4 legs | `npx vitest run src/test-setup.live-db-guard.test.ts` | 4 passed | ✓ PASS |
| SC-3 three-armed derivation calibration | `npx vitest run .../test-restore-workflow-wiring.test.ts -t "the staged channel set is DERIVED"` | 3 passed | ✓ PASS |
| SC-1 new-site calibration (ci.yml) | `npx vitest run .../test-restore-workflow-wiring.test.ts -t "SC-1"` | 1 passed | ✓ PASS |
| SC-1 new-site calibration (ledger-drift) | `npx vitest run .../drift-check-scripts.test.ts -t "a NEW softening site is reported as UNLISTED"` | 1 passed | ✓ PASS |
| `derive-restore-channels.sh --self-check` | `bash scripts/derive-restore-channels.sh --self-check` | OK, both arms | ✓ PASS |
| Full `test-restore-workflow-wiring.test.ts` | `npx vitest run .../test-restore-workflow-wiring.test.ts` | 117/117 passed | ✓ PASS |
| Five phase-owned suites together | `npx vitest run` (5 named files) | 898/898 passed | ✓ PASS — matches SUMMARY's cited combined total exactly |
| Full workspace suite, once, unshared box | `npx vitest run` | 854 passed/19 skipped files (875); 14886 passed/6 failed/280 skipped tests (15172) | ⚠️ 6 failures, both pre-existing/unrelated — see below |
| `check:planning-hygiene` | `npm run check:planning-hygiene` | OK — 6642 tracked files, 0 leaks | ✓ PASS |

**On the 6 full-suite failures (2 files: `drift-check-scripts.test.ts`, `lint-sql-gates.test.ts`):**
both are `Test timed out in 5000ms` failures, and **neither file is in this phase's `files_modified`
set**. Re-run `drift-check-scripts.test.ts` alone: 580/580 pass (matches the SUMMARY's own note
that this exact class of full-suite timeout is "load starvation, clean in isolation"). Re-run the
`lint-sql-gates.test.ts` "REPAIR PROOF" test alone with `--testTimeout=30000`: passes in 6.06s of
actual test time — well inside a normal timeout, confirming a tight 5000ms default margin under
this session's CPU contention (from this verification's own concurrent per-file test runs), not a
logic defect. Both are pre-existing test-timeout-margin issues, orthogonal to the redaction/channel/
credential-guard changes this phase makes, and neither touches phase-owned files.

### Anti-Patterns Found

None. `grep -aE "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` across all phase-owned scripts and
`src/test-setup.ts`/`src/test-setup.live-db-guard.test.ts`: 0 hits.

### Known Open, Deliberate, Not Gaps (confirmed current at HEAD `a71d7245`, not the brief's stale snapshot)

- `[164.8.4-REDACT-NO-POSITIVE-SCAN]` — confirmed still open in `TODOS.md` (~line 8971), and its
  disposition changed since the verification brief was written: **Phase 164.8.7 REDACTCONFIRM was
  inserted and then REMOVED the same day** (commit `a71d7245`, after `0f19541e`). Current
  disposition: *"DISPOSITION (founder, 2026-09-19): NO PHASE. This stays a TODO"* — an explicit,
  recorded founder override of the standing "every data-integrity deferral names a phase" rule,
  with the sizing rationale (no end-user path, no paying clients, reconnaissance-only on shared
  TEST) written inline. Outside this phase's four live criteria; not a gap.
- `[164.8.4-LIVEDB-GUARD-LANE-GAP]` — confirmed booked in `TODOS.md` (~line 9016): two vitest lanes
  (`vitest.redis.config.ts`, `scripts/vitest.config.ts`) load no `setupFiles` and so do not run the
  live-DB guard. Outside this phase's four live criteria; not a gap.
- The rejected `|| true` tolerance at three best-effort redaction sites — confirmed in REVIEW.md as
  deliberately dropped (would have evaded `softeningOffenders`, and each site `exit 1`s
  unconditionally on the next line regardless). Not present in the shipped diff.

### Requirements Coverage

| Requirement | Source Plan(s) | Status | Evidence |
|---|---|---|---|
| SC-1 (psql sweep) | 01, 02, 03 | ✓ SATISFIED | See Truth #1 |
| SC-2 (credential inheritance) | 04 | ✓ SATISFIED | See Truth #2 |
| SC-3 (channel allowlist derived) | 05 | ✓ SATISFIED | See Truth #3 |
| SC-4 (no widened ceiling/floor/exemption) | all | ✓ SATISFIED | See Truth #4 |
| `164.8.2-LEDGER-STDERR-PUBLIC-LOG` | 03 | ✓ SATISFIED | `missing`-direction stderr now captured/counted/withheld |
| `164.8.2-REDACT-HOSTNAME-01` | 01, 02 | ✓ SATISFIED | 6-expression union closes the DNS-failure-hostname gap |
| `164.8.2-EVIDENCE-DOTENV-LEAK` | 04 | ✓ SATISFIED | Fail-loud guard, skip gates untouched |
| `164.8.2-CHANNEL-ALLOWLIST-STALE` | 05 | ✓ SATISFIED | Derivation replaces hand-typed list |

### Human Verification Required

None. All four live criteria are backed by tests this session ran directly (not merely read as
"present"), and the full workspace suite was run once end-to-end with the skip-count comparison
independently reproduced.

## Gaps Summary

No gaps against the four live Success Criteria. One documentation-only discrepancy is noted above
(ROADMAP.md's SC-2 wording not updated to reflect the CONTEXT.md-recorded "repo-side guard, not
wrapper" decision) — it does not affect goal achievement and is not blocking.

---

*Verified: 2026-09-19*
*Verifier: Claude (gsd-verifier)*
