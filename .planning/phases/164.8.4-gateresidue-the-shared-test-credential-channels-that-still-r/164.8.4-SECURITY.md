---
phase: "164.8.4"
slug: "gateresidue-the-shared-test-credential-channels-that-still-r"
status: secured
# threats_open = count of OPEN threats at or above workflow.security_block_on (high). The blocking gate.
threats_open: 0
asvs_level: 1
created: "2026-09-19"
audited_at_sha: "9684ff67"
---

# Phase 164.8.4 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

**Verdict: SECURED — 24/24 threats closed, `threats_open: 0`.**

⭐ The register was authored at PLAN time (all five plans carry a `<threat_model>` block), so the
audit VERIFIED declared mitigations rather than scanning for new threats. The auditor read the five
plan sources directly rather than the dispatch summary — and corrected it: the true severity split
is **17 high / 6 medium / 1 low**, not the 16 high the brief stated. Recorded because a register
summary that undercounts is exactly the kind of dated claim this repo re-measures rather than
inherits.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| shared-TEST Postgres → GitHub Actions log | psql/CLI error text crosses here on a connect or auth failure | the TEST pooler host, its resolved address, and the DB role |
| GitHub Actions log → the public internet | this repository is PUBLIC; an Actions log is world-readable with no further control | the same, plus anything echoed from a captured output file |
| restore output directory → the uploaded artifact | the staging step decides what becomes a 90-day world-readable artifact | diagnostic channels and the reversal recipe |
| developer `.env.local` → a child `vitest` process | a wrapper's runtime auto-loads dotenv into the child | live-DB credentials that flip `HAS_LIVE_DB` true |

---

## Threat Register

24 threats, `T-164.8.4-01` … `-24`. 19 `mitigate` (all verified closed), 5 `accept` (premise
verified, see Accepted Risks). Full per-threat evidence is in the Audit Trail below; the dominant
class is Information Disclosure (11 of 24), which is this phase's entire subject.

| Severity | Count | Status |
|----------|-------|--------|
| high | 17 | closed |
| medium | 6 | closed |
| low | 1 | closed |

*Status: open · closed · open — below high threshold (non-blocking)*
*Only open threats at or above `workflow.security_block_on` (high) count toward `threats_open`.*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-01 | T-05, T-10, T-14, T-19, T-24 | Tampering via npm/pip/cargo install. Each plan reasoned that it installs no package, so the Package Legitimacy Gate is skipped by its own trigger condition. ⭐ **The premise was VERIFIED, not taken:** `git diff --name-only 8371be23..HEAD` touches no `package.json` dependency block, no lockfile, no `requirements.txt`, no `pyproject.toml`, no `Cargo.*`. Had one crept in, the acceptance would be void and the threat open. | founder (standing disposition) | 2026-09-19 |

---

## Audit Trail

## Security Audit 2026-09-19

| Metric | Count |
|--------|-------|
| Threats found | 24 |
| Closed | 24 |
| Open | 0 |

**Method.** Declared-mitigation verification at ASVS L1, performed at L2/L3 depth: the auditor
grepped each cited implementation file for the actual mechanism, then RAN the relevant suites and
scripts rather than trusting the reported pass counts in SUMMARY.md or REVIEW.md. Zero
`## Threat Flags` sections exist in any of the five SUMMARYs.

**Independent runs (not taken from any report):** `test-restore-workflow-wiring.test.ts` 117/117 ·
`drift-check-scripts.test.ts` 580/580 · `test-setup.live-db-guard.test.ts` 4/4 ·
`supabase-migrate-test-first` + `critical-regressions` + `gate-family-meta` +
`contracts/ci-anti-skip-gate.contract` 248/248 · `derive-restore-channels.sh --self-check` passes ·
`shellcheck -S warning` clean on both scripts · `actionlint` clean on all four workflows.

**Load-bearing verifications worth naming:**
- **The redaction is genuinely centralised.** 26 references counted (ci 10, restore 10, migrate 4,
  mutex-probe 2); every executable `-f` call site is workspace-rooted, and the only
  non-`GITHUB_WORKSPACE` hits are prose comments naming the file, not invocations. This closes
  `T-02`, whose specific risk was a relative `-f` from the `python` job, which sets a non-root
  `working-directory`.
- **No specimen was published while proving absence.** The auditor extracted the repo's real
  project ref and TEST pooler hostname LOCALLY, never printed them, and grepped every phase
  document and implementation file for both: **zero hits**. Fixtures use `db.example`,
  `example.invalid`, RFC 5737 addresses and `fake_role` throughout.
- **The guard runs before anything else.** `assertLiveDbWasIntended` is the first executable
  statement after the import block in `src/test-setup.ts` — ahead of the `AsyncLocalStorage`
  install and every other module-scope statement. Its thrown message is a static string naming
  three KEY NAMES with no env-value interpolation, and a positive-control test proves it names the
  keys while never naming the synthetic values.
- **The per-expression falsifier bites.** It parses the shipped `.sed` file at runtime, removes
  each expression in turn and asserts the output changes — so all six expressions are proven
  load-bearing rather than assumed.

**Residual items — verified NOT to reopen any registered threat:**
- `[164.8.4-REDACT-NO-POSITIVE-SCAN]` (booked in `TODOS.md`; founder-dispositioned to stay a TODO).
  `sed` exits 0 whether or not a shape matched, so there is no positive post-substitution
  confirmation. ⭐ The auditor was explicitly told the founder's sizing does NOT bind its verdict,
  and judged independently: this is a forward-looking gap against **unknown future** leak shapes.
  It does not weaken any of the six currently-registered shapes, each confirmed present and
  confirmed load-bearing by the falsifier above. **Does not leave T-06 or any other registered
  high threat open.**
- `[164.8.4-LIVEDB-GUARD-LANE-GAP]` (booked in `TODOS.md`). The two lanes that do not load the
  guard were checked for live exposure: `vitest.redis.config.ts` includes only
  `tests/redis/**/*.test.ts` and `scripts/vitest.config.ts` only `scripts/**/*.test.ts`; across all
  46 files carrying `it.skipIf(!HAS_LIVE_DB)`, **zero** fall under either path, and
  `scripts/vitest.config.ts` is not wired into any CI job. The gap is structurally inert against
  `T-15` today — which is why it is a booked TODO and not an open threat.

**Unregistered flags:** none. The diff is confined to four workflow files, two shell scripts, one
guard file and test files, with no new external calls, tokens or secret-handling patterns beyond
the redaction and guard mechanisms themselves.
