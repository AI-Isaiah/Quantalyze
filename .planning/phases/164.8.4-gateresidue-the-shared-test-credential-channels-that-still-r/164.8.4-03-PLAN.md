---
phase: 164.8.4-gateresidue-the-shared-test-credential-channels-that-still-r
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/test-ledger-drift-check.sh
  - src/__tests__/drift-check-scripts.test.ts
autonomous: true
requirements:
  - SC-1
  - SC-4
  - 164.8.2-LEDGER-STDERR-PUBLIC-LOG
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "When the `missing`-direction ledger query fails, its stderr does not reach the public Actions log — the gate reports a line COUNT and withholds the content, exactly as its own failure sentence already claimed."
    - "The failure message never renders a blank where the line count belongs, even when the capture file is unreadable."
    - "A test drives the failing `missing` direction through the real gate and proves the injected stderr marker does not appear in the gate's output."
  artifacts:
    - scripts/test-ledger-drift-check.sh
    - src/__tests__/drift-check-scripts.test.ts
  key_links:
    - "`run_ledger_query missing` -> a per-run `.err` capture file -> a line count in the `fail` message; the content goes nowhere."
    - "`writeStubLedger`'s `missing` branch -> a new non-zero-exit path -> the gate's failure text, asserted to carry the count and not the marker."
---

<objective>
Close the one bare stderr channel in `scripts/test-ledger-drift-check.sh`: the `missing`-direction
`run_ledger_query` call site redirects stdout and leaves stderr entirely unredirected, so a psql
connect, auth or DNS failure streams the shared-TEST pooler host and the DB user straight into a
world-readable Actions log — while the very `|| fail` message attached to that call already tells
the operator the output was withheld.

Purpose: this is the phase's declared defect class in its purest form — a control narrower than the
sentence beside it. The correct idiom already exists in the same file, in the sibling arm.

Output: the `missing` call site using the capture-count-WITHHOLD idiom verbatim, and a driven test
proving the content does not escape.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.4-gateresidue-the-shared-test-credential-channels-that-still-r/164.8.4-CONTEXT.md
@.planning/phases/164.8.4-gateresidue-the-shared-test-credential-channels-that-still-r/164.8.4-PATTERNS.md
@CLAUDE.md
</context>

<measured_baseline>
Measured at HEAD `dc8087db` on 2026-09-19. Re-measure; do not trust.

- **ONE channel, not five sites.** The file holds several url-bearing psql lines, but the stderr
  redirect lives at the CALL SITE, not the psql site. Four of the five callers are already safe.
  Counting psql invocations would have planned five fixes for one defect.
- **The defective site:** `run_ledger_query missing "$names_csv" > "$missing_file" || fail …`. Stdout
  is redirected; stderr has no redirect at all. Locate it by symbol:
  `grep -a -n 'run_ledger_query missing' scripts/test-ledger-drift-check.sh`.
- **The idiom to reuse is in the same file, in the `ledger_rows` arm** — `set +e` around the call
  with stderr captured to a per-run `.err` file, the return code captured, `set -e` restored, then
  the braced-before-pipe line count, then the `fail` carrying the count. Locate with
  `grep -a -n 'ledger_rows_err' scripts/test-ledger-drift-check.sh`.
- ✅ **INDEPENDENTLY CONFIRMED, do NOT "fix" these:** `run_body_fetch` and the `extra` and `shape`
  arms already handle stderr correctly. The `extra` arm's advisory warning already says the stderr is
  withheld and already means it.
- **The test file already has the injection machinery.** `writeStubLedger` takes a `ledgerRowsRc` and
  an `extraRc` and emits a connect-failure-shaped line to stderr on the corresponding branch, using
  the repo's own synthetic host convention. There is no equivalent for the `missing` branch; adding
  one is this plan's test work.
</measured_baseline>

<assumption_delta_decision>
The phase-level decision is `promote`, for the redaction DEFINITION and the channel SET (recorded in
full in Plan 01). It does not bear on this plan's subject: the capture-count-WITHHOLD idiom is
already a single in-file convention, and this plan makes one more call site use it rather than
introducing a general representation. No `add-alongside` risk arises here — ⛔ specifically, do NOT
extract the idiom into a shared helper function as part of this plan. The sibling arm's version is
the reference; copying it keeps the two arms readable side by side, which is how the discrepancy was
found in the first place.
</assumption_delta_decision>

<artifacts_this_plan_produces>
| Symbol | Kind | Where |
|---|---|---|
| `missing_err` | shell local — the per-run stderr capture path | `scripts/test-ledger-drift-check.sh` |
| `missing_rc` | shell local — the captured return code | same |
| `missing_err_lines` | shell local — the never-blank line count | same |
| `missingRc` | test fixture option on `writeStubLedger` and `scaffoldLedgerCase` | `src/__tests__/drift-check-scripts.test.ts` |
| a new `it(...)` in the existing VAC-08 describe block | test | same |
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: capture, count and withhold the `missing`-direction stderr</name>
  <files>scripts/test-ledger-drift-check.sh, src/__tests__/drift-check-scripts.test.ts</files>
  <read_first>
    - `scripts/test-ledger-drift-check.sh` — the `missing` call site and everything down to the `missing_count` read that follows it (locate with `grep -a -n 'run_ledger_query missing'`, then `sed -n` a narrow range), and the `ledger_rows` arm in full (locate with `grep -a -n 'ledger_rows_err'`). The comment above the count idiom explains why the braces matter; read it, it is the detail that must not be dropped.
    - `.planning/phases/164.8.4-...-r/164.8.4-PATTERNS.md`, the `scripts/test-ledger-drift-check.sh` section — the idiom quoted verbatim and the defective site beside it.
    - `src/__tests__/drift-check-scripts.test.ts` — `writeStubLedger` and `scaffoldLedgerCase` (locate with `grep -a -n 'function writeStubLedger'`), and one existing `ledger_rows`-direction failure test to copy its assertion shape, including its redaction assertion. ⛔ This file is over five thousand lines — read narrow ranges, never the whole.
  </read_first>
  <behavior>
    - The `missing` query exits non-zero having written stderr: the gate exits 1, its message names the exit status and a NON-BLANK line count, carries the word marking the content as withheld, and the injected stderr marker does NOT appear anywhere in the gate's combined output.
    - The `missing` query exits non-zero and the capture file is unreadable: the message still renders a count placeholder, never a blank where a number belongs.
    - The `missing` query succeeds: behaviour is unchanged from today — the existing VAC-08 tests that drive the clean path must stay green with no edit.
  </behavior>
  <action>
**Write the test first, and observe it RED against the unfixed script.** Extend `writeStubLedger`
with a `missingRc` parameter mirroring the existing `ledgerRowsRc` and `extraRc` parameters: when
non-zero, the `missing` branch emits a connect-failure-shaped line to stderr and exits with that
code. ⭐ Reuse the repo's existing synthetic host convention already present in that helper's other
branches — do NOT invent a different fake host, and NEVER use a real one. Thread `missingRc` through
`scaffoldLedgerCase` the same way its siblings are threaded.

Add one test in the existing VAC-08 describe block, shaped like the `ledger_rows`-direction failure
test beside it: scaffold a case with a non-zero `missingRc`, run the real gate, and assert the gate
exits 1; that its output names the failing query's exit status; that it carries a non-blank line
count; that it carries the word marking the content withheld; and — the load-bearing assertion —
that the injected stderr marker does NOT appear anywhere in the output. Run it now and observe it
FAIL for the right reason: the marker is present, because stderr is bare. Record that observed
failure text.

**Then fix the call site**, reusing the sibling arm's idiom rather than inventing one. The shape:
declare the capture path and the locals alongside the existing `missing_file` local; disable errexit
around the call; run `run_ledger_query missing` with stdout to the existing file AND stderr to the
new capture file; capture the return code; restore errexit; compute the line count with the
braced-before-pipe fallback so an unreadable capture yields a placeholder rather than a blank; then
branch on the return code into the existing `fail`, with its message extended to carry the exit
status and the line count.

⭐ THE BRACES ARE THE DETAIL THAT MUST NOT BE DROPPED. The count must be computed with the grouping
that puts the fallback BEFORE the pipe; without it the downstream filter succeeds on nothing and the
fallback never fires, which is the exact hole the sibling arm's own comment records. Copy the
construction, do not paraphrase it.

Keep the existing `fail` message's meaning and voice — it already says the output is withheld because
it can carry connection detail. Extend it so the sentence is now TRUE of the mechanism, rather than
replacing it with a differently-worded one.

⛔ Do NOT touch `run_body_fetch`, the `extra` arm or the `shape` arm. All three were independently
confirmed correct; editing them would widen this change past the one channel it owns.

⛔ Do NOT change `EXPECTED_ARMS` in this file or add a self-test arm to it. This plan adds a vitest
test, not a self-test arm, so the ratchet does not move. If you find yourself wanting to change that
constant, stop — something has gone wrong.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/drift-check-scripts.test.ts --reporter=dot</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <verify>
    <automated>grep -a -n 'run_ledger_query missing' scripts/test-ledger-drift-check.sh | grep -q '2>' &amp;&amp; grep -a -c 'wc -l <' scripts/test-ledger-drift-check.sh</automated>
    <fails_when>non-zero exit — the `missing` call site still has no stderr redirect, or the count idiom is absent from the file</fails_when>
  </verify>
  <done>
The `missing` call site captures stderr to a per-run file, reports its line count in the failure
message and never echoes its content. The new test passes, its pre-fix RED was observed and its text
recorded, and every pre-existing test in the file is still green with no edit.
  </done>
  <reversibility rating="reversible">One call site in one script plus a test fixture parameter.</reversibility>
</task>

<task type="auto">
  <name>Task 2: prove the withholding is falsifiable, on disk</name>
  <files>scripts/test-ledger-drift-check.sh</files>
  <read_first>
    - `.planning/phases/164.8.4-...-r/164.8.4-CONTEXT.md`, `<specifics>` — the neuter-and-restore rules.
    - `CLAUDE.md`, the destructive-edit safety section.
  </read_first>
  <action>
Anti-vacuity BLOCKS here, so the new assertion must be shown capable of failing against the file on
disk, not only against a fixture.

Perform one neuter-and-restore cycle: `cp` a byte backup of `scripts/test-ledger-drift-check.sh` to
the session scratch directory and verify it with `cmp`; remove the stderr redirect from the `missing`
call site so the channel is bare again; run the scoped vitest command; capture the VERBATIM failure
text, which must be the new test reporting that the injected marker reached the gate's output;
restore from the backup and verify byte-identity with `cmp`.

Then perform a SECOND, narrower neuter that proves the braces are load-bearing rather than
decorative: change the count computation so the fallback sits outside the grouping, point the capture
path at something unreadable, and observe that the message renders a blank where the count belongs.
Restore the same way. This is the one detail the pattern map flags as easy to lose in a rewrite, so
it gets its own observation.

⛔ NEVER `git checkout --`, `git restore` or `git stash` in this harness — they restore to HEAD and
silently destroy uncommitted work elsewhere in the tree. Re-take the backup after every edit; a
backup goes stale the moment you edit again.

Record both observations in the SUMMARY: what was neutered, the verbatim output, and the `cmp`
result. ⛔ A verdict and the gate's own message only — never a specimen, and never the injected
marker's surroundings if they could be mistaken for a real value.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/drift-check-scripts.test.ts --reporter=dot</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line — after the restore this would mean a backup was not byte-exact</fails_when>
  </verify>
  <done>
Two observed REDs with verbatim text recorded, both restores proved byte-exact with `cmp`, and the
scoped suite green again. Neither observation quotes a specimen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared-TEST Postgres -> the VAC-08 gate's stderr -> the public Actions log | the `missing`-direction call site is the crossing with no control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.4-11 | Information Disclosure | the `missing`-direction `run_ledger_query` call site | high | mitigate | Capture stderr to a per-run file, report only a line count, never echo the content — the sibling arm's idiom, reused verbatim |
| T-164.8.4-12 | Repudiation | a failure message rendering a blank where the count belongs | low | mitigate | The braced-before-pipe fallback, with its own observed neuter proving it fires |
| T-164.8.4-13 | Information Disclosure | the new test fixture | high | mitigate | Reuse the file's existing synthetic host convention; no real host, role or DSN enters a tracked file |
| T-164.8.4-14 | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan |
</threat_model>

<verification>
- `npx vitest run src/__tests__/drift-check-scripts.test.ts` green, including every pre-existing test.
- The `missing` call site has a stderr redirect and the count idiom retains its grouping.
- `run_body_fetch` and the `extra`/`shape` arms are byte-unchanged: confirm with `git diff`.
- `EXPECTED_ARMS` in this script is byte-unchanged — no ratchet moved (SC-4).
- No real host, IP, DB user or connection string in any changed file.

⭐ **This plan adds NO migration.** Nothing under `supabase/migrations/` is touched, so merging it
does not trigger the TEST apply or the Production human-reviewer gate.
</verification>

<success_criteria>
- SC-1 for this channel: the one bare stderr channel in the sweep is closed, and the closure is
  proven by a driven test rather than by reading.
- SC-4: no ceiling widened, no floor relaxed, no exemption added; `EXPECTED_ARMS` unchanged.
</success_criteria>

<output>
Create `.planning/phases/164.8.4-gateresidue-the-shared-test-credential-channels-that-still-r/164.8.4-03-SUMMARY.md` when done.
</output>
