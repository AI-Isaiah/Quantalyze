---
phase: 164.8.4-gateresidue-the-shared-test-credential-channels-that-still-r
plan: 02
type: execute
wave: 2
depends_on:
  - 164.8.4-01
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/test-restore-from-baseline.yml
  - src/__tests__/test-restore-workflow-wiring.test.ts
autonomous: true
requirements:
  - SC-1
  - SC-4
  - 164.8.2-REDACT-HOSTNAME-01
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No hand-copied redaction expression survives anywhere in either workflow — the definition exists once and every site references it."
    - "A psql run whose combined output is captured to a file and then echoed to the log passes through the shared redaction first; the two `sql-tests` sites that echoed raw connect and auth stderr no longer do."
    - "Adding a NEW unredacted capture-then-echo site, or a NEW inline redaction expression, makes a test go RED — the gate fails on a new site, it does not assert today's count."
    - "The anti-skip NOTICE-marker greps still read what they always read: no marker text the job depends on is altered by the redaction, and a future marker that WOULD be altered reds a gate instead of silently breaking the skip checks."
  artifacts:
    - .github/workflows/ci.yml
    - .github/workflows/test-restore-from-baseline.yml
    - src/__tests__/test-restore-workflow-wiring.test.ts
  key_links:
    - "sql-tests capture -> shared redaction -> `cat` of the captured file -> the anti-skip greps that read the same file."
    - "The gates' needles -> parsed from the shipped `scripts/redact-psql-stderr.sed`, so a seventh expression extends them with no test edit."
---

<objective>
Finish the one-pass sweep: convert the remaining seven inline redaction blocks onto the shared
definition, close the third leak shape (two `sql-tests` sites that capture psql's combined output and
`cat` it unredacted), and ship the gates that make a NEW unredirected or unredacted site fail.

Purpose: SC-1's whole point is that this is ONE sweep with a gate, not a fix to the sites someone
happened to name. Converting seven of eight and leaving one, or fixing the two echo sites without a
gate, reproduces the half-class that 164.8.2 produced three times.

Output: zero surviving inline redaction expressions, two closed echo sites, and three calibrated
gates in the existing wiring test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.4-gateresidue-the-shared-test-credential-channels-that-still-r/164.8.4-CONTEXT.md
@.planning/phases/164.8.4-gateresidue-the-shared-test-credential-channels-that-still-r/164.8.4-PATTERNS.md
@.planning/phases/164.8.4-gateresidue-the-shared-test-credential-channels-that-still-r/164.8.4-01-SUMMARY.md
@CLAUDE.md
</context>

<measured_baseline>
Measured at HEAD `dc8087db` on 2026-09-19 by this plan's author. Re-measure; do not trust.

- **Remaining after Plan 01: seven blocks** — two in `ci.yml` (the `sql-tests` and `e2e-seeded`
  mutex-acquire retry blocks) and five in `test-restore-from-baseline.yml` (mutex-acquire retry,
  marker check, activity gate, dry-run push, and the `id: redact` step's loop).
- **The `id: redact` loop is a 5-of-6 block MISSING the DNS-hostname expression**, and the activity
  gate is a 4-of-6 block missing `host=`/`user=`. ⛔ Neither is "the correct form" — converting both
  onto the union is what closes the gap in both directions at once.
- **The third leak shape: two sites in `ci.yml`'s `sql-tests` job.** The NOTICE-channel probe and the
  per-file test loop each do `> "$out" 2>&1` and then `cat "$out"` with no redaction between.
- ⭐ **RESEARCH Open Question 1 is ANSWERED BY MEASUREMENT, and the answer is 0 collisions.** Every
  anti-skip marker, partial-skip pattern and arms sentinel the job greps for was extracted from
  `supabase/tests/*.sql` and tested against all six redaction expressions: six markers, thirty-five
  sentinel and partial patterns, **zero** carry any of the six shapes. So redacting `"$out"` in place
  before the `cat` cannot alter any text the anti-skip mechanism reads. Regenerate with the
  extraction in Task 2's action; the standing risk is a FUTURE marker containing one of the shapes,
  which Task 3's collision gate closes.
- **Every reader of `"$out"` in `sql-tests`** is one of: the `ANTISKIP-NOTICE-CHANNEL-OK` probe
  check, a fixed-string marker grep, a `NOTICE: +SKIP:` scan, the partial-skip pattern scan, and the
  arms sentinel check. All five read the file AFTER the `cat`, so an in-place redaction placed
  immediately after the psql call covers every one of them.
</measured_baseline>

<assumption_delta_decision>
Carried from Plan 01 and completed HERE. The noun promoted to primary is the psql-stderr redaction
DEFINITION; the decision is `promote`, and `add-alongside` is the named anti-pattern. This plan is
the half that makes the promotion real: it DEMOTES the remaining inline copies to nothing. A plan
set that shipped the shared `.sed` and left seven inline blocks in place would be exactly the
`add-alongside` outcome the decision rejects.
</assumption_delta_decision>

<artifacts_this_plan_produces>
| Symbol | Kind | Where |
|---|---|---|
| `"no hand-copied redaction expression survives outside the shared definition"` | describe block | `src/__tests__/test-restore-workflow-wiring.test.ts` |
| `"every captured psql output is redacted before it is echoed"` | describe block | same |
| `"no anti-skip marker is altered by the shared redaction"` | describe block | same |
| `stripComments()` | test helper | same |
| `captureEchoSites()` | test helper — pairs a combined-output capture with the echo of that same file | same |
| `antiSkipMarkers()` | test helper — extracts marker, partial and sentinel text from `supabase/tests/*.sql` | same |
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: convert the remaining seven blocks onto the shared definition</name>
  <files>.github/workflows/ci.yml, .github/workflows/test-restore-from-baseline.yml</files>
  <read_first>
    - `.planning/phases/164.8.4-gateresidue-the-shared-test-credential-channels-that-still-r/164.8.4-01-SUMMARY.md` — the converted call site's exact shape, which every site here must match.
    - `scripts/redact-psql-stderr.sed` — the shipped definition.
    - `.planning/phases/164.8.4-...-r/164.8.4-PATTERNS.md`, the two workflow sections — the `id: redact` loop's fail-closed discipline and the note that it is MISSING the DNS expression.
    - The seven sites themselves. Locate by symbol, never by line number: `grep -a -n 'sed -E -e' .github/workflows/ci.yml .github/workflows/test-restore-from-baseline.yml` and `grep -a -n 'sed -i -E' .github/workflows/test-restore-from-baseline.yml`. Read each with a narrow `sed -n` range around its hit. ⛔ Both files are thousands of lines — never read either whole.
  </read_first>
  <action>
Convert each of the seven remaining blocks to reference the shared definition, exactly as Plan 01's
site does: replace the chained `-e` expression list with a single `-f` operand pointing at the
workspace-rooted path, and change NOTHING else on the command — the input operand, any `-i` flag,
the output redirection, the `if`/`else` structure around it and the trailing `|| true` all stay
byte-identical to what is there now.

Two of the seven need the `-i` in-place form preserved: the `id: redact` step's loop and any other
site already editing a file in place. Keep `-i` and add `-f`; do not convert an in-place site to the
stream form or a stream site to in-place, because the surrounding fail-closed logic depends on which
one it is. The `id: redact` loop's `if sed … ; then redacted=… else withhold_channels; …` shape is
LOAD-BEARING — a failed substitution must still take the explicit fail-closed path rather than the
trap's catch-all.

Every reference must be workspace-rooted. One converted job declares a `working-directory` that is
not the repo root, so the rule is uniform rather than per-site; a per-site exception is how a future
edit copies the wrong one.

⛔ **COMMENT-TEXT DISCIPLINE — this is a hard stop, not advice.** Task 2 ships a gate that scans
comment-stripped workflow text for the definition's own regex source strings. When you update the
explanatory comments above each converted block — and you should, because several of them currently
describe the expression list that is no longer there — describe the shapes in PROSE and point at
`scripts/redact-psql-stderr.sed`. ⛔ Do NOT paste any expression's regex source text into a comment,
and do NOT leave a comment that quotes the old `-e` chain. The gate strips comments, so such a
comment would not actually break it — but writing one is precisely the habit ("a control narrower
than the sentence beside it") that this phase exists to end, and the next reader will copy it.

Preserve the reasoning already recorded in those comments. Several name a real incident — the
userinfo-only pattern that matched nothing while raw text went into a public log. That history stays;
only the sentence describing WHERE the expressions live changes.
  </action>
  <verify>
    <automated>for f in .github/workflows/ci.yml .github/workflows/test-restore-from-baseline.yml; do n=$(grep -a -v '^[[:space:]]*#' "$f" | grep -acE -- "-e 's#" || true); echo "$f surviving-inline-substitutions=$n"; [ "$n" -eq 0 ] || exit 1; done; r=$(grep -ac 'redact-psql-stderr.sed' .github/workflows/ci.yml .github/workflows/test-restore-from-baseline.yml | awk -F: '{s+=$2} END {print s}'); echo "references=$r"; [ "$r" -ge 8 ]</automated>
    <fails_when>non-zero exit — either a `surviving-inline-substitutions` count above zero (a hand-copied expression is still in a non-comment line), or fewer than eight references to the shared definition across the two files</fails_when>
  </verify>
  <verify>
    <automated>npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts --reporter=dot</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line — the existing wiring tests pin the `id: redact` step's fail-closed structure and would red if the conversion changed it</fails_when>
  </verify>
  <done>
All seven remaining blocks reference `scripts/redact-psql-stderr.sed` workspace-rooted, no
hand-copied substitution survives in any non-comment line of either workflow, the in-place sites kept
their `-i` and their surrounding fail-closed structure, and no comment restates a regex.
  </done>
  <reversibility rating="reversible">Workflow edits only; git history holds the pre-conversion blocks verbatim.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: close the third leak shape — redact before the echo in `sql-tests`</name>
  <files>.github/workflows/ci.yml, src/__tests__/test-restore-workflow-wiring.test.ts</files>
  <read_first>
    - `.github/workflows/ci.yml`, the `sql-tests` job's two capture-then-echo sites. Locate by symbol: `grep -a -n 'cat "$out"' .github/workflows/ci.yml` gives two hits; read a narrow `sed -n` range around each, extending far enough forward to see every later reader of the same file.
    - `.planning/phases/164.8.4-...-r/164.8.4-RESEARCH.md`, Open Questions 1 — the two candidate fixes and why "never cat this" would break the anti-skip consumer.
    - `src/__tests__/test-restore-workflow-wiring.test.ts` — the `calibrate` helper and the `stripComments` helper if Task 1 of Plan 01 added one; otherwise add it here.
  </read_first>
  <behavior>
    - A workflow text where every combined-output capture is followed by a redaction of that same file before that file is echoed: the predicate is TRUE.
    - A workflow text with the redaction line deleted from between one capture and its echo: the predicate is FALSE, naming the capture's variable.
    - A workflow text with a NEW capture-then-echo pair added and no redaction between: the predicate is FALSE — this is SC-1's "fails on a NEW site", and it is the arm that must be observed flipping.
    - A workflow text with no capture-then-echo pair at all: the predicate is FALSE, not vacuously TRUE.
  </behavior>
  <action>
**Fix.** At each of the two `sql-tests` sites, insert a redaction of the captured file immediately
after the psql call and before the `cat` of that file, using the in-place form of the shared
definition with the workspace-rooted path. In-place is the correct choice here, and the reason is
measured rather than assumed: five later readers all read the SAME captured file after the `cat`, so
redacting the file once covers every one of them, whereas redacting only the stream into `cat` would
leave the file itself unredacted for those readers.

Do not alter the `|| probe_status=$?` / `|| status=$?` capture of the psql exit status, and do not
place the redaction between the psql call and that capture — the status capture must remain the very
next thing after the call. Insert the redaction after the status has been captured.

If the redaction itself fails, the operator must not lose the diagnosis silently: follow the
capture-count-WITHHOLD reasoning the repo already uses — on a failed substitution, emit a workflow
error naming the file and the fact that its content is WITHHELD, and do not `cat` it. Report a line
COUNT, never the content. Use the braced-before-pipe count idiom from
`scripts/test-ledger-drift-check.sh`'s `ledger_rows` arm so a blank can never render where a number
belongs.

**Measure the non-overlap before you commit to in-place, and record the measurement.** Extract every
text the anti-skip mechanism greps for out of `supabase/tests/*.sql` — the skip markers, the
partial-skip patterns and the arms sentinel — and run each through
`sed -E -f scripts/redact-psql-stderr.sed`. Assert every one comes back byte-identical. This plan's
author measured zero collisions across six markers and thirty-five sentinel/partial patterns; your
job is to re-measure at HEAD and record the numbers you observe, not to restate these.

**Gate.** Add a describe block asserting the capture-then-echo ordering over comment-stripped `CI`
text. Build the subject set mechanically: find each line capturing a command's combined output into a
file held in a shell variable; find the next echo of that same variable; require a reference to the
shared definition naming that same variable somewhere between them. Assert the subject set is
non-empty first, with a message saying a gate green over an empty corpus is not a gate.

Prove it with `calibrate` three times: delete the redaction line between one pair; append a NEW
capture-then-echo pair with no redaction between; and remove every pair so the corpus is empty. Each
must flip the predicate from true to false. The middle one is SC-1's own requirement — the gate fails
on a NEW site — so its failure message must say that a new unredacted echo site was found and name
the variable, rather than reporting a generic mismatch.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts -t "redacted before it is echoed" --reporter=dot</automated>
    <fails_when>non-zero exit, or "No test found" / "0 passed" in the summary line</fails_when>
  </verify>
  <verify>
    <automated>grep -ahoE "RAISE NOTICE 'SKIP: [^'%]{0,60}" supabase/tests/*.sql | sed "s/^.*RAISE NOTICE '//" | sort -u > /tmp/164841-markers.txt; grep -ahoE 'SKIP \([^)]*\)|SKIP Part [0-9]+|PASS WITH [0-9]+ SKIPS?|ALL [0-9]+ ARMS EXECUTED' supabase/tests/*.sql | sort -u >> /tmp/164841-markers.txt; echo "subjects=$(wc -l < /tmp/164841-markers.txt)"; [ "$(wc -l < /tmp/164841-markers.txt)" -gt 0 ] &amp;&amp; sed -E -f scripts/redact-psql-stderr.sed /tmp/164841-markers.txt | cmp -s - /tmp/164841-markers.txt</automated>
    <fails_when>non-zero exit — either `subjects=0` (an empty corpus, which would make the check vacuous) or `cmp` reporting a differing byte, meaning the redaction alters text the anti-skip greps depend on</fails_when>
  </verify>
  <done>
Both `sql-tests` sites redact the captured file before echoing it, the psql status capture is still
the statement immediately after the call, a failed redaction reports a withheld line COUNT and
suppresses the echo, the non-overlap is re-measured at HEAD with the observed numbers recorded, and
the ordering gate is green with all three calibrations observed flipping it.
  </done>
  <reversibility rating="reversible">Two inserted lines in one job plus a test block.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the survivor gate and the marker-collision gate, both observed RED on disk</name>
  <files>src/__tests__/test-restore-workflow-wiring.test.ts</files>
  <read_first>
    - `src/__tests__/test-restore-workflow-wiring.test.ts` — the `redactionExpressions` helper Plan 01 added, and the `calibrate` helper.
    - `scripts/redact-psql-stderr.sed` — the shipped definition the gates derive their needles from.
    - `.planning/phases/164.8.4-...-r/164.8.4-CONTEXT.md`, `<specifics>` — the neuter-and-restore rules.
  </read_first>
  <behavior>
    - Given the shipped definition, the survivor gate derives one needle per expression and finds NONE of them in comment-stripped `CI` or `WF`: TRUE.
    - Re-insert one inline expression into a mutated copy of either workflow: FALSE, naming which expression reappeared.
    - The collision gate: every anti-skip marker passes through the definition unchanged: TRUE. A synthetic marker carrying one of the masked shapes: FALSE.
  </behavior>
  <action>
**The survivor gate.** Assert that no hand-copied redaction expression survives outside the shared
definition. ⭐ DERIVE the needles: parse `scripts/redact-psql-stderr.sed` with the
`redactionExpressions` helper (comment lines and blanks dropped), take each expression's match
pattern, and assert that pattern does not appear in comment-stripped `CI` or `WF`. Deriving rather
than restating is the point — a seventh expression added to the definition extends this gate with no
test edit, which is the promoted-definition decision applied to the gate itself.

⛔ Do NOT assert how many expressions there are, how many blocks were converted, or how many
references exist. A count-pinning arm is the overclaim SC-1 forbids by name; the structural invariant
is the whole assertion.

Assert the derived needle set is non-empty before asserting the property, so a definition file that
went missing or was emptied cannot read as a clean pass.

Calibrate twice with the existing helper: re-insert one derived expression into a mutated `WF` and
into a mutated `CI`; each must flip the predicate to false. The failure message must name the
expression that reappeared and point at `scripts/redact-psql-stderr.sed` as where it belongs.

**The marker-collision gate.** Assert that the redaction cannot alter any text the `sql-tests`
anti-skip mechanism reads. Read `supabase/tests/*.sql`, extract the skip markers, the partial-skip
patterns and the arms sentinel with the same expressions the workflow itself uses, and for each
assert that running the shipped definition over it returns it byte-identical. Assert the extracted
set is non-empty first.

This gate is the durable half of RESEARCH Open Question 1. Today the collision count is zero, so
redacting in place is safe; the gate is what keeps it safe when someone later writes a marker
containing one of the masked shapes. Its failure message must say that a marker would be rewritten by
the redaction and that the anti-skip check reading it would then match nothing — a silent-green
outcome — and name the offending marker. Calibrate by feeding it a synthetic marker carrying one of
the masked shapes and observing the flip. ⛔ The synthetic marker is a fabricated placeholder, never a
real host, role or connection string.

**Observe both RED on disk.** `calibrate` proves a predicate flips on a mutated string; anti-vacuity
blocks here, so also prove each shipped gate reds against a mutated FILE. For each: `cp` a byte
backup to the session scratch directory, verify it with `cmp`, make one neutering edit, run the
scoped vitest command, capture the VERBATIM failure text, restore from the backup, and verify
byte-identity with `cmp`. ⛔ NEVER `git checkout --`, `git restore` or `git stash` in this harness —
they restore to HEAD and silently destroy uncommitted work elsewhere in the tree. Re-take the backup
after every edit. Record each observation in the SUMMARY as a verdict plus the gate's own message —
never a specimen.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts --reporter=dot</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line — after the restore step this would mean a backup was not byte-exact</fails_when>
  </verify>
  <done>
Both gates pass at HEAD, both derive their subjects rather than restating them, neither asserts a
count, both assert their corpus is non-empty, all four calibrations are observed flipping, and each
gate has an observed on-disk RED with its verbatim failure text and a `cmp`-proved byte-exact
restore recorded in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared-TEST Postgres -> GitHub Actions log | psql connect, auth and DNS error text crosses here |
| the captured output file -> `cat` -> the public Actions log | the third leak shape: combined output echoed with no redaction between |
| `supabase/tests/*.sql` marker text -> the anti-skip greps | the redaction must not alter what these read, or a skip check silently matches nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.4-06 | Information Disclosure | the two `sql-tests` capture-then-echo sites | high | mitigate | Redact the captured file in place after the status capture and before the echo; on a failed substitution report a withheld line COUNT and suppress the echo |
| T-164.8.4-07 | Information Disclosure | a future call site added without redaction | high | mitigate | The ordering gate fails on a NEW capture-then-echo pair, calibrated by adding exactly that pair to a mutant |
| T-164.8.4-08 | Information Disclosure | a future inline redaction expression re-copied into a workflow | medium | mitigate | The survivor gate derives its needles from the shipped definition and reds on any reappearance |
| T-164.8.4-09 | Denial of Service (of the skip checks) | a future anti-skip marker containing a masked shape | medium | mitigate | The collision gate asserts every marker survives the redaction byte-identical and names the offender; measured zero collisions at HEAD |
| T-164.8.4-10 | Tampering | npm/pip/cargo installs | high | accept | No package is installed by this plan; the legitimacy gate is skipped by its own trigger condition |
</threat_model>

<verification>
- No non-comment line in either workflow carries a hand-copied substitution.
- At least eight references to the shared definition, all workspace-rooted.
- `npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts` green.
- The anti-skip marker corpus passes through the definition byte-identical, with the corpus size
  reported and non-zero.
- Nothing widens a ceiling, relaxes a floor, or adds an exemption (SC-4): no numeric threshold is
  introduced or moved. Every new assertion is an existence, ordering or byte-identity predicate.
- No real host, IP, DB user, project ref or connection string in any changed file.
</verification>

<success_criteria>
- SC-1 COMPLETE for the redaction half: every psql channel in the sweep is fixed in one pass, and
  three gates fail on a NEW unredirected or unredacted site rather than asserting today's count.
- SC-4: no ceiling widened, no floor relaxed, no exemption added.
- RESEARCH Open Question 1 is answered by measurement AND closed by a gate — both halves, neither
  quietly dropped.
</success_criteria>

<output>
Create `.planning/phases/164.8.4-gateresidue-the-shared-test-credential-channels-that-still-r/164.8.4-02-SUMMARY.md` when done.
</output>