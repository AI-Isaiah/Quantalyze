# Phase 164.5.1: CRONREPOINT — the live `match_engine_cron` row is repointed at the mechanism the repo actually describes - Context

**Gathered:** 2026-09-10
**Status:** Ready for planning
**Mode:** Smart discuss — TRANSCRIPTION, not proposal

⛔ **This phase's ROADMAP entry carries a `DECIDED 2026-09-09` block stating "nothing in this phase
is left to choose."** Success criterion 1 requires D1, D2 and D3 to be transcribed here VERBATIM
with their measurements, *"not re-derived and not re-opened."* No grey areas were generated and no
grey-area questions were put to the founder, because generating them would itself violate criterion
1. The one live gate is D4, and it is a human gate on purpose.

<domain>
## Phase Boundary

Close the LIVE-ROW half of `CRON-DRIFT-01`: PROD's `cron.job` jobid 1 runs a hand-repaired
2026-09-01 command that exists in no migration, so a rebuild from migrations does not reproduce it.
This phase repoints that live row at `public.match_engine_cron_tick()` — a callable that **already
exists on both projects** — via a RUNBOOK registration, re-captures the prober's cron manifest with
a purpose-built script, and repoints the tree's "Phase 164.5 item 7" references at this phase.

**In scope:** the runbook registration section; a manifest re-capture script under
`scripts/prod-prober/`; a pre-flight that ABORTS on a live-vs-manifest mismatch; the re-captured
`cron-manifest.json`; the reference repoint across five source files plus `TODOS.md`.

**Out of scope, by decision not by omission:** ⛔ **no forward migration is written by this phase,
neither to schedule nor to unschedule** (D1 + D2). Building the callable is out of scope — Phase
164.7 already shipped it.

⚠️ **PROD is currently CORRECT** (verified 2026-09-01, `net._http_response` id 3485 returned 200).
**This closes a REPRODUCIBILITY gap, not an outage** — there is no time pressure, which is exactly
why settling the convention first was affordable.

</domain>

<decisions>
## Implementation Decisions

### TRANSCRIBED VERBATIM from ROADMAP `DECIDED 2026-09-09` (criterion 1)

⛔ The four entries below are reproduced word-for-word from `.planning/ROADMAP.md`. A planner
RECORDS these; it does not re-derive them. Do not paraphrase them into a plan — quote them.

- **D1 — Registration lives in the RUNBOOK, never in a migration.** Basis: the 164.7 rule shipped in `20260907120000` (*"A migration that schedules is a scope violation"*), plus two in-tree precedents already following it (`ledger-refresh-go-live.md:412`, `flipretry-derived-equity-go-live.md:162`). The losing convention — Phase 164.5's old criterion 7 — is flagged for cleanup, not left to disagree silently.

- **D2 — What a rebuild-from-migrations produces: NO `match_engine_cron` job at all, plus one loud row.** MEASURED, not assumed: `20260408215026` skips scheduling whenever the two `app.*` GUCs are empty, and they are ALWAYS empty here because setting them returns 42501 on this platform. So a rebuilt database has no hourly job and one `cron_runs` row reading `error = 'GUC unset at migration 015'`, and the runbook registration is what creates the job. ⭐ This is the runbook convention working as designed — the migration fails CLOSED and leaves a trace — not a gap to be patched. ⛔ Therefore **no forward migration is written by this phase**, neither to schedule nor to unschedule. The window to name in the runbook is: *from a rebuild until the registration step is run, the hourly recompute does not fire, and `cron_runs` says why.*

- **D3 — The manifest re-capture is a SCRIPT, not hand work.** `scripts/prod-prober/` has no capture tool today. Criterion 3 already demands a pre-flight that ABORTS on a live-vs-manifest mismatch, which is code either way; the capture is the same read plus a write, and the drift arm compares by `command_sha256` and refuses a row without a valid one — so a hand-edited manifest is a defect waiting to happen. Write the small script, use it for the re-capture, commit both.

- **D4 — The PROD apply stays a HUMAN gate, and this block does not weaken it.** Criterion 5 is unchanged: three reviewers (`migration-reviewer`, `rls-policy-auditor`, `silent-failure-hunter`) before any production statement, and the founder runs or approves the live `cron.schedule`. Everything above removes *guesswork*, not *oversight*.

### Re-verified at HEAD 2026-09-10 (the ROADMAP says regenerate rather than trust)

Each ROADMAP claim below was re-measured on this branch before planning, per its own instruction
that these are *"dated readings bound to a run or a session, not constants."*

- ⛔ **CORRECTED 2026-09-10 — D3's PREMISE IS FALSE, and this file asserted the false version.**
  This line previously read *"✅ No capture tool exists … D3's premise HOLDS."* That was wrong, and
  the way it was wrong is this phase's own subject: the check grepped **filenames** for
  `captur|snap`, but the capture is a **MODE, not a file**. A measurement whose shape does not match
  its claim answers a different question and reads as confirmation.
  **Measured at HEAD:** `scripts/prod-prober/run.mjs:12` documents
  `node scripts/prod-prober/run.mjs --capture-manifest --out <p>`; `captureManifest` is implemented
  at `run.mjs:2448+`; `.github/workflows/prod-prober.yml:73,304` wires `capture-manifest` as a
  dispatch mode; and `docs/runbooks/ledger-refresh-go-live.md:442` ("2e — re-capture the cron
  manifest, IMMEDIATELY after registering") already prescribes its use as part of a registration.
  ⭐ **D3's CONCLUSION still holds and is already enforced** — capture by script, sha regenerated,
  never hand-edited. What changes is the WORK: the plan must **NOT write a second capture tool.**
- ✅ **Two runbook `cron.schedule` precedents, none for `match_engine_cron`.**
  `ledger-refresh-go-live.md:412` and `flipretry-derived-equity-go-live.md:162`. D1's basis HOLDS.
  (`compute-queue.md:410` mentions the jobname re-registration idiom in prose, not a registration.)
- ✅ **The callable is in the repo:** `supabase/schema/functions/match_engine_cron_tick.sql`, 72
  lines. This phase points the live row at something already there.

### ⛔ THE BLOCKER THE `DECIDED` BLOCK OMITS — criterion 2 is IMPOSSIBLE until a rule is widened

⛔ **The re-capture that criterion 2 requires REFUSES to run against the repaired configuration.**
Found by the researcher, then reproduced by EXECUTION here — not by reading:

```
hygieneViolations("match_engine_cron", "SELECT public.match_engine_cron_tick();")  =>  [vault-absent]
hygieneViolations("match_engine_cron", "…vault.decrypted_secrets…")               =>  (clean)
```

`scripts/prod-prober/arms/cron-drift.mjs:329-334` hardcodes
`name === "match_engine_cron" && !text.includes("vault.decrypted_secrets")` — it requires that
literal in the **command TEXT**. The repaired command moves the Vault read INSIDE
`match_engine_cron_tick()`, so the text no longer carries it. `captureManifest` then takes its
refusal branch (`run.mjs:2497-2512`): *"REFUSED: … cron.job row(s) fail secret hygiene, so this
configuration cannot become the oracle. Nothing was written."* — **exit 1, no file.**

So the rule fires **against a correct repair**, and criterion 2 cannot be met until it is widened.

⚠️ **This is NOT out of scope and NOT a surprise** — it is booked as `[164.7-VAULT-ABSENT-RULE]`
(`TODOS.md:1733-1749`) and that id is already in this phase's **Requirements** line. The gap is in
the `DECIDED` block, which never mentions it while claiming nothing is left to choose.

**The TODOS entry states the shape, and it is binding — three things move together, in one commit:**

> (a) the rule — accept EITHER the literal or a call to a function whose committed body contains
> it, and ship a RED fixture for the new arm, **because a widened rule with no fixture is a rule
> nobody has watched fail**; (b) `scripts/prod-prober/cron-manifest.json`, re-captured from PROD
> after the repair; and (c) the live row itself.

⛔ **The trap it names, verbatim:** *"Do not discover this at repair time and 'fix' it by keeping a
decorative `DO` wrapper around the call purely to satisfy a grep — that is a gate shaped by its own
false positive."*

⚠️ **Ordering consequence:** (a) is the FIRST work of this phase, not a follow-up. (b) depends on
the founder's D4 apply having happened, so the phase's own commits cannot contain a truthful
re-captured manifest until after the human gate — the plan must sequence around that, not pretend
it can re-capture early.

### ⛔ NEW FINDING — criterion 6 has a trap, and the trap is armed by a gate

Criterion 6 enumerates **five** source files. A grep of the tree returns **six**. The sixth is
`supabase/schema/baseline.sql:6218`.

**The criterion is RIGHT and the extra hit must NOT be edited** — but nothing in the ROADMAP says
so, and a naive grep-and-replace execution walks straight into it:

1. `baseline.sql` is a **DUMP, not a source.** Its line 6218 is a byte-copy of what migration
   `20260907120000:380` wrote — measured: the sentence *"repointing the live job row at it is Phase
   164.5 item (7)"* appears in BOTH files, identically.
2. Its **sha256 is pinned** at `27826b7660026a5b6a7fbee9223265ab98f8fbf485b87237498881034c558ec7`
   in `supabase/schema/BASELINE.md:54`, and that pin is read by seven scripts plus `ci.yml`
   (`baseline-content-drift-check.mjs`, `check-baseline-staleness.mjs`,
   `extract-reference-inserts.mjs`, `lint-sql-gates.mjs`, `preflight-drop-allocator-fn.sh`,
   `prod-body-drift-check.sh`, `restore-test-from-baseline.sh`).
3. `scripts/baseline-content-drift-check.mjs:8` states the gate outright: it **"fires on a BYTE
   CHANGE to `baseline.sql`"** and demands a corresponding change to the MIGRATION CHAIN.

So editing that comment would change the dump's bytes, break the pinned sha, and trip a gate
demanding exactly the migration edit that **D1 and D2 forbid this phase from writing**.

⭐ **Decision: `baseline.sql` is EXCLUDED from criterion 6, and the phase must SAY WHY rather than
leave the exclusion to be rediscovered.** The reference there self-corrects at the next baseline
regeneration, and only then. Record it; do not edit the dump.

### Criterion 4 is already satisfied by measurement — the deliverable is the sentence

Criterion 4 reads *"A rebuild-from-migrations no longer produces the unrunnable GUC-reading job,
and the phase states plainly what it DOES produce."* Its first clause is **already true and was
never false**: D2 measured that `20260408215026` takes its skip branch and writes NO job at all.
The ROADMAP itself struck the opposite claim as FALSIFIED. So criterion 4 requires **no code
change** — the deliverable is D2's statement, in the runbook, with its window named.

### Claude's Discretion

Only the mechanical shape is discretionary: the capture script's filename and flags, the runbook
section's placement, and how the pre-flight's mismatch fixture is constructed. Everything with a
consequence is answered in D1-D4 above.

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `scripts/prod-prober/run.mjs` (123 772 bytes) — carries the cron-drift arm that compares live
  rows to the manifest by `command_sha256` and **refuses a row without a valid one**. The capture
  script must regenerate that sha, never hand-write it.
- `scripts/prod-prober/cron-manifest.json` (12 307 bytes, captured 2026-09-06) — the oracle.
  PROD matches it TODAY, so the drift arm reads ZERO drift right now; repointing the row makes the
  arm RED unless the manifest is re-captured **in this same phase**.
- `supabase/schema/functions/match_engine_cron_tick.sql` — the callable being pointed at. Present
  on shared TEST and, via merged `20260907120000`, on PROD.
- `docs/runbooks/ledger-refresh-go-live.md:412` and
  `docs/runbooks/flipretry-derived-equity-go-live.md:162` — the two in-tree registration patterns
  to follow verbatim in shape.

### Established Patterns
- **Registration in the runbook, never in a migration** (164.7, shipped and tested).
- **A migration that cannot do its job fails CLOSED and leaves a trace** — `20260408215026` writes
  a `cron_runs` row `status='error', error='GUC unset at migration 015'` rather than proceeding.
- **Manifests are compared by content hash**, so they are generated, never edited.

### Integration Points
- `docs/runbooks/match-engine.md:27` — currently says the live row *"is not yet pointed at that
  function"* and names Phase 164.5 item (7). This is both a criterion-6 target AND where the new
  registration section belongs.
- `scripts/lint-app-guc.mjs:222` — a criterion-6 target. ⚠️ It carries a **count-pinned
  `LINEAGE_ALLOWLIST`**; criterion 6 warns that a comment edit must not move those counts.
- `.github/workflows/prod-prober.yml` — runs the drift arm that criterion 2 scores.

</code_context>

<specifics>
## Specific Ideas

- The runbook section must name D2's window in plain words: *from a rebuild until the registration
  step is run, the hourly recompute does not fire, and `cron_runs` says why.*
- Criterion 3's proof is a RUN against a deliberately mismatched manifest, not an inspection.
  ⛔ *"Never infer from an absent measurement."*
- ⚠️ `[A1]` is HELD, not proven: no `SECURITY DEFINER` wrapper has been EXECUTED against the real
  Vault — the ACL was read, the function was not called. **The phase must not upgrade it by
  retelling**, and ⛔ the pg-lane cannot answer this class at all, since its vault is a plaintext
  stand-in. Do not "confirm" A1 from a lane run.

</specifics>

<deferred>
## Deferred Ideas

- **The `baseline.sql:6218` reference** — left to self-correct at the next baseline regeneration.
  Editing the dump is forbidden here (see the criterion-6 trap above).
- **Proving `[A1]` by execution** — needs a Vault read by a `SECURITY DEFINER` wrapper that does
  not fire a real `net.http_post` at the production analytics service. Out of scope; it does not
  gate this repoint.

</deferred>
