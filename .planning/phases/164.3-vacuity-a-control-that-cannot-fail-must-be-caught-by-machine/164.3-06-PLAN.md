---
phase: 164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine
plan: 06
type: execute
subsystem: sql-gate-linter
tags: [vacuity-linter, lint-fixtures, ci-gate]
wave: 2
depends_on: ["164.3-02"] # no code dependency — sequential ci.yml ownership: plan 02 edits ci.yml in wave 1; one owner per wave
files_modified:
  - scripts/lint-sql-gates.mjs
  - scripts/lint-sql-gates-fixtures/
  - src/__tests__/lint-sql-gates.test.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [VAC-03]
provides:
  - "Static linter over supabase/tests/*.sql rejecting the statically-detectable vacuity shapes, each rule with a proven-firing red fixture"
affects: [164.3-10, supabase-tests-authoring]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Mechanisms 1, 2 and 4 are lint failures on gate files; mechanism 3 has the narrow rule only; mechanism 5 has NO rule — the runner's first-failure discipline is its detector (D-16)"
    - "Every shipped rule fires on a committed red fixture and passes a committed green fixture — a rule that cannot fire does not merge (D-16)"
    - "The linter reads files via node fs (never shell grep — the repo has a measured NUL-blind file) and runs identically locally and as a CI job"
  artifacts:
    - path: "scripts/lint-sql-gates.mjs"
      provides: "rule engine + per-rule named errors, aggregate then exit 1"
    - path: "src/__tests__/lint-sql-gates.test.ts"
      provides: "asserts each rule fires on red and passes green, and pins the shipped-rule set exactly"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/lint-sql-gates.mjs"
      via: "sql-gate-lint job runs the exact local invocation; frontend aggregator gains a strict row"
      pattern: "lint-sql-gates"
---

<objective>
Ship the static linter for the measured vacuity shapes (VAC-03), honestly bounded by D-16: mechanisms 1, 2 and 4 are statically detectable; 3 only narrowly; 5 is NOT — do NOT ship a rule for mechanism 5 to make the count look complete, because a lint rule that cannot fire is precisely this phase's named defect, committed by the phase itself. Every rule that does ship carries a red fixture proving it fires.

Purpose: a sixth mechanism on NEW gate files becomes a lint failure instead of a red-team finding.
Output: linter script + fixture corpus + vitest proof + a small hermetic CI job wired into the frontend aggregator.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-CONTEXT.md

Read before implementing: `164.3-RESEARCH.md` §Q4 IN FULL — the per-mechanism detectability table with exact rule shapes, the repaired idioms quoted from the gate file (the comment-strip regexp_replace, the four-bit tgtype test, the subtract-never-add diagnostic note), and the allowlist guidance. Also `164.3-PATTERNS.md` §2 (node script conventions; the psql-preflight one-named-error-per-rule output shape to copy) and §3 Analog B (the hermetic job scaffold from sql-function-snapshot.yml).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rules + red/green fixture pairs, honestly scoped</name>
  <files>scripts/lint-sql-gates.mjs, scripts/lint-sql-gates-fixtures/, src/__tests__/lint-sql-gates.test.ts</files>
  <behavior>
    - Rule R1 (mechanism 1): flags DML or `SELECT ... INTO` used by a later assertion appearing INSIDE an `EXCEPTION WHEN` handler block (the implicit-subtransaction trap — the probe reads its own rollback). Lexical BEGIN/EXCEPTION/END nesting tracking is sufficient for this corpus.
    - Rule R2 (mechanism 2): flags any `pg_get_functiondef` result reaching a regex or LIKE match without passing through the comment-strip idiom (the in-repo `regexp_replace(...)` shape quoted in RESEARCH §Q4).
    - Rule R4 (mechanism 4): a trigger-type bitmask test must carry every canonical bit for the events named in the adjacent failure message — a mask test missing one of the four bits for a claimed BEFORE INSERT OR UPDATE FOR EACH ROW is a failure (the repaired four-term shape is in the gate file).
    - Rule R3-narrow (mechanism 3): flags additive arithmetic on a value read from the table under test when it appears inside a diagnostic/assertion expression; the rule's doc comment states plainly that general overflow-in-diagnostic detection is undecidable and the mutation runner is the primary net.
    - NO rule for mechanism 5 (D-16). The vitest suite pins the shipped-rule ID set EXACTLY to these four — adding or removing a rule reds the pin until the fixture proof exists.
    - Each rule: one red fixture the rule MUST flag, one green fixture it MUST pass, committed under scripts/lint-sql-gates-fixtures/. The engine reads via node:fs, emits one named error per finding (the preflight's per-pattern shape), aggregates, exits 1 if any.
  </behavior>
  <action>
    Write the vitest file first (each rule: red fixture fires with the expected rule ID, green fixture passes; plus the exact-set rule-registry pin), watch it fail, then implement the engine and fixtures. Then run the linter over all 71 real gate files: findings in the 70 not-yet-annotated files go into a per-file allowlist (file + rule + one-line reason) so the rule bites on NEW code from day one without demanding the 70-file cleanup this phase explicitly defers to 164.4 — the allowlist is exact-set-asserted in the vitest file so entries cannot silently accumulate. Findings in `test_strategy_shares_rls.sql` itself would be real regressions of already-repaired idioms — investigate rather than allowlist. Document each rule's honest scope in its header (especially R3-narrow's limits and mechanism 5's absence, citing D-16).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lint-sql-gates.test.ts && node scripts/lint-sql-gates.mjs</automated>
  </verify>
  <done>Four rules ship, each with proven-firing red fixture; rule set pinned exactly; full-corpus run green (with any allowlist entries counted and reasoned); no mechanism-5 rule exists.</done>
</task>

<task type="auto">
  <name>Task 2: CI job + aggregator row</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Add a `sql-gate-lint` job to ci.yml copying the hermetic scaffold PATTERNS §3 Analog B describes (SHA-pinned checkout with persist-credentials false, SHA-pinned setup-node at 22, node_modules cache restore keyed on the lockfile hash, npm ci retry loop, `permissions: contents: read`, concurrency group) with a header comment stating: no database, no Docker, no secrets — this gate cannot flake. Its run step is the EXACT local invocation from the script header (mode identity — no wrapper). Wire it into the `frontend` aggregator: add to `needs:` AND the explicit result loop as a strict row with NO skip tolerance (it consumes no secret, so a skipped state is always a real fault — same reasoning as the existing seam-redis strict row), with a WHY comment matching the density of the existing rows. This job touches no shared DB, so the aggregator's ban on new needs-edges among TEST-mutex jobs does not apply — say so in the comment.
    Prove the wiring can fail before finalizing: point the job's invocation at a scratch copy of a red fixture (or temporarily add an un-allowlisted finding in the working tree), confirm the linter exits 1 as the job step would, restore. Record the observed RED in the commit message body.
  </action>
  <verify>
    <automated>node scripts/lint-sql-gates.mjs && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <done>Job exists with the verbatim invocation; aggregator gained the strict row + WHY comment; the RED path was observed and recorded; YAML parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate files → linter | linter reads tracked SQL only; no execution, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-15 | Repudiation | allowlist absorbing new violations silently | medium | mitigate | exact-set assertion in vitest; entries carry reasons; drift reds |
| T-164.3-16 | Tampering | a shipped rule that cannot fire (fake coverage) | high | mitigate | red-fixture-or-no-merge discipline enforced by the vitest pin (D-16) |
| T-164.3-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed by this plan |
</threat_model>

<verification>
- Vitest proves every rule fires and the rule set is exactly the shipped four.
- Full-corpus linter run green; ci.yml parses; aggregator row present.
</verification>

<success_criteria>
- VAC-03 delivered with D-16's honesty intact: statically-detectable shapes lint-fail on new gate files, every rule demonstrably able to fire, mechanism 5 explicitly delegated to the runner's first-failure discipline.
</success_criteria>

<output>
Create `.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-06-SUMMARY.md` when done. Record the shipped rule IDs, allowlist size + reasons, and the observed RED evidence. Plan 10 consumes the R2 red fixture for the mechanism-2 demo.
</output>
