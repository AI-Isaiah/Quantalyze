---
phase: 164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine
plan: 04
type: execute
subsystem: e2e-local-stack
tags: [supabase-cli, replay-spike, disposable-stack]
wave: 1
depends_on: []
files_modified:
  - scripts/local-stack/run.sh
  - scripts/local-stack/REPLAY-SPIKE.md
autonomous: true
requirements: [VAC-07]
provides:
  - "Measured answer: does the 262-migration chain (re-measured at HEAD 2026-08-29; RESEARCH's 263 was a day older) replay under supabase db reset (RESEARCH A4)"
  - "Local-stack lane script: up / down / --self-test with trapped supabase stop --no-backup"
affects: [164.3-07]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The replay question is answered by MEASUREMENT and recorded — pass, or fail with the failing migration named and the db-dump fallback implemented (D-15)"
    - "One command brings the Supabase CLI local stack up with the schema loaded and hands Playwright its env; one command (and every failure/interrupt path) tears it down leaving no containers"
    - "The stack never points at shared TEST or PROD; the app under test is never uvicorn"
  artifacts:
    - path: "scripts/local-stack/run.sh"
      provides: "up/down/--self-test lifecycle with EXIT/INT/TERM trap running supabase stop --no-backup"
    - path: "scripts/local-stack/REPLAY-SPIKE.md"
      provides: "Measured spike outcome + the mode decision plan 07 builds on"
  key_links:
    - from: "scripts/local-stack/run.sh"
      to: "supabase/config.toml"
      via: "supabase start reads the repo config (project_id quantalyze, api port 54421)"
      pattern: "supabase (start|stop|db reset)"
---

<objective>
De-risk VAC-07 before anything is built on it: SPIKE whether the 262-migration chain (re-measured at HEAD 2026-08-29; RESEARCH's 263 was a day older) (starting `20260405061911_initial_schema.sql`) replays cleanly under `supabase db reset` on the CLI local stack — the phase's single biggest unknown (D-15, RESEARCH A4: HIGH risk) — then build the second disposable lane: the Supabase CLI local-stack lifecycle script that plan 07's concurrent-finalize spec runs on. Two lanes exist by design (D-15): the initdb lane (plan 01) serves SQL gates; THIS lane serves the app, because `csv-finalize` and `withAuth` speak PostgREST + GoTrue via supabase-js and a bare cluster structurally cannot serve them.

Purpose: an unproven replay must not sit in the middle of a wave — measure it first; the fallback (read-only `supabase db dump` baseline) is decided on evidence, not guessed.
Output: the spike record + the lane script. No spec yet (that is plan 07), no production code changes (D-08 bound).

⛔ Constraints: never `uvicorn main:app`; never point the stack, the app, or any seed at shared TEST or PROD for stack operation (the only permitted TEST touch is a READ-ONLY `supabase db dump` if the fallback fires); no credentials in artifacts — the local stack's generated dev keys are not secrets, but TEST DSNs are and must never appear in a committed file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-CONTEXT.md
@supabase/config.toml

Read before implementing: `164.3-RESEARCH.md` §Q7 (why the app cannot talk to a bare initdb cluster; the local-stack resolution; A4/A5 assumptions this plan converts to measurements) and Pitfall 9 (replay wedging) + Pitfall 5 (orphan cleanup, `supabase stop --no-backup` trapped).
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPIKE — prove or disprove the 262-migration replay, decide the baseline mode</name>
  <precondition>Docker daemon is running (`docker info` succeeds) and the Supabase CLI 2.x is on PATH (`supabase --version`)</precondition>
  <files>scripts/local-stack/REPLAY-SPIKE.md</files>
  <action>
    From the repo root (the CLI reads `supabase/config.toml`): run `supabase start`, then `supabase db reset` and let it replay the full migration chain. Record in `scripts/local-stack/REPLAY-SPIKE.md`: CLI version, Docker version, wall-clock replay time, and the outcome.
    - PASS: record it; the lane's mode is `db reset` replay. Probe that the stack actually serves the app's protocols: PostgREST answers on the configured api port and GoTrue's health endpoint responds (converts RESEARCH A5 into a measurement). Record both probe results.
    - FAIL: record the exact failing migration filename and its error (redact nothing — these are public repo files). Do NOT edit any migration (⛔ migrations are immutable history here). Implement the D-15 fallback instead: generate a schema baseline via READ-ONLY `supabase db dump` against TEST (the TEST DSN comes from the local environment the e2e helpers already use; it must not be committed or echoed), store the dump under `scripts/local-stack/` as a generated-at-run-time artifact — decide and record whether it is committed (schema DDL is already public via migrations; commit ONLY if it contains no DSN/host/ref strings — grep it before adding) or regenerated per run. The lane's mode is then baseline-load.
    Either way REPLAY-SPIKE.md ends with a one-line MODE decision that Task 2 and plan 07 consume. Leave the stack running for Task 2 or stop it cleanly — never leave it half-up.
  </action>
  <verify>
    <automated>test -s scripts/local-stack/REPLAY-SPIKE.md && grep -aE "MODE: (replay|baseline)" scripts/local-stack/REPLAY-SPIKE.md</automated>
  </verify>
  <done>The replay question has a measured answer with timing; the MODE line exists; A4 and A5 are measurements, not assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: The local-stack lane script with trapped teardown</name>
  <files>scripts/local-stack/run.sh</files>
  <action>
    Write `scripts/local-stack/run.sh` (bash, `set -euo pipefail`, WHY-first header naming D-15's two-lane design and the exact invocations — ci wiring in plan 07 pastes them verbatim):
    - `up`: `supabase start`, then load schema per the MODE decision from REPLAY-SPIKE.md (`db reset`, or baseline-load), then write the env handoff a Playwright run consumes — the stack's API URL and the generated anon + service-role keys read from `supabase status` — to a gitignored env file (e.g. `scripts/local-stack/.stack-env`; add the gitignore entry if the path is not already covered) so no key ever lands in a committed file, even a dev key, by construction.
    - `down`: `supabase stop --no-backup`.
    - Trap discipline (same standard the initdb lane meets, D-04's lesson applied to Docker): register the teardown trap on EXIT with INT/TERM routed through it BEFORE `supabase start` runs, so a failed or interrupted `up` still stops the stack. Preserve exit status.
    - `--self-test`: up → probe PostgREST (`/rest/v1/` with the anon key answers) and GoTrue (`/auth/v1/health` answers) → down → assert no Supabase containers for this project remain (`docker ps` filtered on the project id from supabase/config.toml, count 0).
    Anti-vacuity proof: temporarily neuter the trap registration in a scratch COPY, interrupt an `up`, observe the self-test's no-containers assertion go RED against the orphaned stack, clean up manually, discard the copy. Record the observed RED in the commit message body.
  </action>
  <verify>
    <automated>bash scripts/local-stack/run.sh --self-test</automated>
  </verify>
  <done>up/down/self-test all work; interrupt mid-up leaves no containers; the neutered-copy RED was observed and recorded; env handoff file is gitignored and never committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lane → Docker daemon | the lane starts/stops vendor containers locally and in CI |
| fallback path → TEST Postgres | read-only `supabase db dump` ONLY if the replay fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-09 | Information Disclosure | TEST DSN reaching a committed file or log via the fallback | high | mitigate | DSN only from local env, never echoed; dump grepped for DSN/host/ref strings before any commit decision; env handoff file gitignored |
| T-164.3-10 | Denial of Service | orphaned Docker stacks exhausting disk | medium | mitigate | trap registered before start; self-test asserts zero containers remain; `--no-backup` removes volumes |
| T-164.3-11 | Tampering | stack accidentally pointed at shared TEST | high | mitigate | lane hardcodes the local stack URL shape; the WR-05 prod-pattern refusal in the seed helper stays the second net (plan 07) |
| T-164.3-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed by this plan |
</threat_model>

<verification>
- `bash scripts/local-stack/run.sh --self-test` exits 0 and `docker ps` shows no project containers afterward.
- REPLAY-SPIKE.md carries the MODE line, timings, and probe results.
- `git status` shows no env/dump file staged unless the dump was explicitly cleared by the grep check.
</verification>

<success_criteria>
- VAC-07's biggest unknown is a measured fact with a recorded decision; the app-facing disposable lane exists with teardown proven able to fail and observed failing when neutered (D-15, D-04 standard).
</success_criteria>

<output>
Create `.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-04-SUMMARY.md` when done. Record the MODE decision, replay/probe timings, and anything plan 07 must know about the stack's env handoff.
</output>
