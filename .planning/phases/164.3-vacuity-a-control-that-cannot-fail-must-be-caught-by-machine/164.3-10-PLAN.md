---
phase: 164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine
plan: 10
type: execute
subsystem: mutation-runner
tags: [mechanism-demos, vac06, durable-evidence]
wave: 5
depends_on: ["164.3-06", "164.3-08"]
files_modified:
  - .planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-VAC06-DEMOS.md
  - src/__tests__/vac06-mechanism-arms.test.ts
autonomous: true
requirements: [VAC-06]
provides:
  - "Each of the five historical vacuity mechanisms re-introduced and demonstrated caught by the new machinery, with durable re-proof on every push"
affects: [164.3-verification]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Each of the five mechanisms from the ROADMAP table is re-introduced against the phase-164 corpus and observed CAUGHT — mechanisms 1, 3, 4, 5 by the runner's first-failure discipline, mechanism 2 by the linter rule plus the comment-strip idiom"
    - "The demonstrations are durable: the demo arms remain annotated (re-proven by the sql-mutation job every push) and a vitest pin reds if any demo arm's annotation disappears"
  artifacts:
    - path: ".planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-VAC06-DEMOS.md"
      provides: "mechanism -> detector -> arm/fixture -> command -> observed first-failure evidence, one section per mechanism"
    - path: "src/__tests__/vac06-mechanism-arms.test.ts"
      provides: "pins that the five demo arms stay annotated in the gate file"
  key_links:
    - from: "src/__tests__/vac06-mechanism-arms.test.ts"
      to: "supabase/tests/test_strategy_shares_rls.sql"
      via: "fs read asserting each demo arm ID retains its structured annotation"
      pattern: "RED-UNDER-M"
---

<objective>
Demonstrate criterion 6 (VAC-06): re-introduce each of the five historical vacuity mechanisms against the phase-164 corpus and show the new machinery catches it. A vacuity detector that has never caught a vacuity is the joke that writes itself — this plan is the phase eating its own cooking, and it makes the demonstrations DURABLE: the demo arms are part of the annotated corpus the sql-mutation job re-proves on every push, and a vitest pin reds if any demo arm's annotation is ever removed.

All five mechanisms live in `supabase/tests/test_strategy_shares_rls.sql` (the TENANT, TRIGGER, SANITIZE, STEP 2 and N1 arm families named in the ROADMAP's mechanism table), so this is reachable with the existing annotations — no new gate coverage is written (that is 164.4).

Output: the demo record + the durable pin. Runs entirely on the plan-01 lane via the plan-08 runner and the plan-06 linter fixtures.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-CONTEXT.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-08-SUMMARY.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-06-SUMMARY.md

Read before implementing: the ROADMAP's Phase 164.3 five-mechanism table (the section titled with the phase name — each row names the mechanism and where it hid), `164.3-RESEARCH.md` §Q4's per-mechanism table (which detector owns which mechanism), and plan 08's SUMMARY (authoritative arm list + runner invocation). NOTE: earlier waves edited ci.yml and the gate file — trust the current tree and plan 08's SUMMARY over any line number recorded in research.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the five demonstrations and capture first-failure evidence</name>
  <precondition>node scripts/mutation-runner/run.mjs exits 0 on the full corpus (plan 08 complete) and the linter fixtures pass (plan 06 complete)</precondition>
  <files>.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-VAC06-DEMOS.md</files>
  <action>
    For each mechanism, select the concrete annotated arm (or fixture) that embodies it, execute the demonstration, and capture the command plus the observed catching output:
    1. Mechanism 1 (post-rejection probe inside a handler — the arm reads its own rollback): a TENANT or TRIGGER family arm per the ROADMAP table; run the runner single-arm mode on it and record the first-failure line proving the genuine cross-tenant/trigger defect is reported by the INTENDED arm. Additionally show the linter side: rule R1's red fixture firing (it encodes this shape statically for NEW files).
    2. Mechanism 2 (functiondef regex satisfied by an in-body comment): the linter's R2 red fixture firing, plus the comment-strip idiom demonstrated by the normalizer's D-05 fixture test (a body whose only matching token lives in a comment normalizes clean).
    3. Mechanism 3 (diagnostic overflowing in exactly the diagnosed state): the N1-family arm; runner single-arm demo with the first-failure line.
    4. Mechanism 4 (partial bitmask — narrowed trigger satisfies remaining terms): the LAYERED trigger-timing arm; runner demo showing the mutation (trigger narrowed back) caught with correct identity; note the linter's R4 rule as the static net for new files.
    5. Mechanism 5 (arm made structurally unreachable; the reachable one reports the opposite): NOT statically detectable (D-16) — the detector is first-failure identity. Demonstrate it directly: run a mutation declared for arm A that actually reddens arm B first (the SANITIZE-family masking shape, or a scratch-copy variant constructed for the demo) and record the runner exiting 1 with the wrong-first-failure defect naming the mismatch — the runner refusing red-anywhere IS the catch.
    Write `164.3-VAC06-DEMOS.md`: one section per mechanism — mechanism, where it hid historically (from the ROADMAP table), detector, arm/fixture used, exact command, observed evidence lines. Commands must be re-runnable as written.
  </action>
  <verify>
    <automated>[ "$(grep -ac '^## Mechanism' .planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-VAC06-DEMOS.md)" -eq 5 ]</automated>
    Must report 5, and every section must contain a command and an evidence excerpt (spot-check by re-running mechanism 5's command).
  </verify>
  <done>Five sections, each with a re-runnable command and captured catching output; mechanism 5's demonstration shows the wrong-identity exit-1, not a red-anywhere pass.</done>
</task>

<task type="auto">
  <name>Task 2: Make the demonstrations durable</name>
  <files>src/__tests__/vac06-mechanism-arms.test.ts</files>
  <action>
    Write `src/__tests__/vac06-mechanism-arms.test.ts`: reads `supabase/tests/test_strategy_shares_rls.sql` via node:fs and asserts each of the five demo arms' IDs (as chosen in Task 1) still carries a line-start-anchored structured annotation — so removing or renaming a demo arm's annotation reds vitest, and the sql-mutation CI job independently re-proves the arms bite on every push. The test header names VAC-06 and points at the DEMOS doc.
    Prove it can fail: temporarily point one expected arm ID at a nonexistent ID, observe RED, restore, observe GREEN. Record the RED in the commit message body.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/vac06-mechanism-arms.test.ts</automated>
  </verify>
  <done>Pin green against the real file; the nonexistent-ID RED observed and recorded; every-push durability holds via the pin + the sql-mutation job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo runs → throwaway clusters | all demonstrations execute on the plan-01 lane; nothing shared is touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-24 | Repudiation | demos recorded once then silently rotting | medium | mitigate | vitest pin + the demo arms living in the every-push mutation corpus |
| T-164.3-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed by this plan |
</threat_model>

<verification>
- DEMOS doc has five sections with re-runnable commands; mechanism 5's command re-run reproduces the wrong-identity exit-1.
- Vitest pin green; its RED observed under the nonexistent-ID neuter.
- `git status --porcelain` clean of scratch artifacts after all demo runs.
</verification>

<success_criteria>
- VAC-06 delivered: all five historical mechanisms re-introduced and caught by machine, with evidence a verifier can re-execute and durability a machine enforces.
</success_criteria>

<output>
Create `.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-10-SUMMARY.md` when done. Record the mechanism→arm mapping table and the two observed REDs. This SUMMARY plus the DEMOS doc are the primary evidence for criterion 6 at phase verification.
</output>
