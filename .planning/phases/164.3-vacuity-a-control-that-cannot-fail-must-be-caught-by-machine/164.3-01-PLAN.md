---
phase: 164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine
plan: 01
type: execute
subsystem: ci-tooling
tags: [pg-lane, disposable-postgres, mutation-substrate]
wave: 1
depends_on: []
files_modified:
  - scripts/pg-lane/run.sh
  - scripts/pg-lane/fixtures/01-fixture-core.sql
  - scripts/pg-lane/fixtures/02-fixture-sanitize-tables.sql
  - scripts/pg-lane/README.md
autonomous: true
requirements: [VAC-02]
provides:
  - "Lane CLI contract: bash scripts/pg-lane/run.sh --workdir <dir> --apply <sql...> [--post-apply <sql>] --gate <gate.sql>"
  - "Tracer proof: one real arm (SHAPE 1c) machine-mutated to RED with first-failure identity, restored to GREEN"
affects: [164.3-05, 164.3-08, phase-164.4]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One command boots a throwaway PostgreSQL cluster, applies SQL files, runs a gate file, and removes everything it created — on success, failure, and interrupt (D-04)"
    - "The lane runs on macOS (homebrew keg) and would run on ubuntu (PATH / pg_config / /usr/lib/postgresql), with a free port per run (D-07)"
    - "The lane refuses to touch a cluster it did not create (collision guard preserved, D-03)"
    - "A deliberate mutation of migration copy 20260827120000 (generation BIGINT -> INTEGER) makes the real gate file go RED with SHAPE 1c as the FIRST failure, and pristine copies go GREEN (D-02 tracer)"
  artifacts:
    - path: "scripts/pg-lane/run.sh"
      provides: "Promoted disposable-PG lane with trap cleanup, PGBIN chain, free-port allocation, --self-test"
    - path: "scripts/pg-lane/README.md"
      provides: "Invocation contract (pasted verbatim into ci.yml later) + fixture STAND-IN disclaimer"
    - path: "scripts/pg-lane/fixtures/01-fixture-core.sql"
      provides: "Stand-in fixture promoted from the pg-harness"
  key_links:
    - from: "scripts/pg-lane/run.sh"
      to: "supabase/tests/test_strategy_shares_rls.sql"
      via: "the tracer run applies copied migrations + fixtures then executes the gate file copy"
      pattern: "ON_ERROR_STOP"
---

<objective>
Promote the mid-phase pg-harness (`.planning/phases/164-share-copy-link-always-works-and-never-discloses/pg-harness/run.sh`, 56 lines) into the real disposable-PostgreSQL lane (VAC-02, D-03), fixing its three measured defects (macOS-only PGBIN, fixed port, and NO cleanup of any kind — D-04, D-07), and prove the phase's core mechanic end-to-end with a tracer: ONE real annotated arm (SHAPE 1c) machine-mutated to RED with first-failure identity, then GREEN on pristine copies (D-02).

Purpose: this lane is the substrate for the mutation runner (plan 05), the corpus run + CI job (plan 08), and Phase 164.4's backfill oracle. Its invocation contract is therefore COSTLY to change later — get it right here.
Output: `scripts/pg-lane/run.sh` + fixtures + README with the exact invocation the CI job will paste verbatim.

⛔ Constraints binding every task: never touch the shared TEST database or PROD; never run `uvicorn main:app`; no credentials or project refs in any artifact (repo is public).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-CONTEXT.md
@.planning/phases/164-share-copy-link-always-works-and-never-discloses/pg-harness/run.sh

Read before implementing: `164.3-RESEARCH.md` §Q2 (PGBIN resolution chain, free-port allocation, the exact cleanup-trap shape that survives `set -euo pipefail`) and `164.3-PATTERNS.md` §1 (what to copy verbatim from run.sh, what to fix, and the repo's shell-hygiene conventions from `scripts/repro-key-flow.sh`).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Promote the lane and drive ONE real mutation end-to-end (SHAPE 1c)</name>
  <precondition>PostgreSQL 16 server binaries exist locally — `/opt/homebrew/opt/postgresql@16/bin/pg_ctl` is executable, or `pg_ctl`/`pg_config` is on PATH</precondition>
  <reversibility rating="costly">The lane's CLI contract becomes the invocation surface for plan 05's runner, plan 08's CI job, and Phase 164.4's backfill oracle (D-07's own rating) — changing flags or semantics later means changing all three.</reversibility>
  <files>scripts/pg-lane/run.sh, scripts/pg-lane/fixtures/01-fixture-core.sql, scripts/pg-lane/fixtures/02-fixture-sanitize-tables.sql, scripts/pg-lane/README.md</files>
  <action>
    Copy the pg-harness script and its two fixture files into `scripts/pg-lane/` (per D-03: promote, do not rewrite). Preserve verbatim: the pg_isready collision guard that REFUSES an occupied port (D-03 says it must survive), the `psqlq()` wrapper with `-v ON_ERROR_STOP=1`, the `-k ''` TCP-only setting with its socket-path comment, and the header's fixture STAND-IN disclaimer (also restate it in README.md — the fixtures do NOT prove behaviour against the real schema's own RLS).

    Fix the three measured defects (all named in D-04/D-07; implement the exact shapes given in 164.3-RESEARCH.md §Q2):
    1. PGBIN resolution chain replacing the hardcoded homebrew path: PATH lookup of pg_ctl, then `pg_config --bindir`, then the `/usr/lib/postgresql/*/bin` and `/opt/homebrew/opt/postgresql@*/bin` globs, then fail loud. `PGBIN` env var still overrides.
    2. Free port per run: bind port 0 via a one-line node helper, keep the pg_isready refuse-guard afterward (TOCTOU protection). `PORT` env var still overrides.
    3. Cleanup that cannot be skipped: a `cleanup()` function registered on EXIT BEFORE `initdb` runs, with INT routed to exit 130 and TERM to exit 143 so both flow through the EXIT trap; a `CREATED` flag set only after this run makes its data dir; `pg_ctl stop -m immediate -w` then `rm -rf` of only what this run created; original exit status preserved. Also replace the startup sleep race with `pg_ctl -w` plus a bounded pg_isready loop.

    Generalize the CLI (this is the contract — document it in README.md and the script header, because ci.yml later pastes the invocation VERBATIM for mode identity):
    `bash scripts/pg-lane/run.sh --workdir <scratch-dir> --apply <file.sql> [<file.sql> ...] [--post-apply <file.sql>] --gate <gate.sql>`
    Semantics: boot throwaway cluster under `<scratch-dir>`, apply the `--apply` files in order, then execute the optional `--post-apply` file (this hook exists for the runner's live-DB GRANT-shape mutations — D-14's shape 2), then run the `--gate` file streaming psql output; exit 0 iff the gate passed; the cluster is stopped and removed by the trap in every outcome. With no arguments, run the legacy fixture sequence currently coded in the harness (read its final lines for the file order and keep that as the default demo).

    Tracer proof (the phase's thinnest end-to-end slice, D-02): script a proof mode (`--tracer-proof`) that (a) copies into a scratch dir: the lane fixtures, migrations `supabase/migrations/20260827120000_strategy_shares_generation_model.sql` and `20260827130000_sanitize_user_revoke_strategy_shares.sql`, and the gate file `supabase/tests/test_strategy_shares_rls.sql` — NEVER mutating the checkout; (b) in the migration COPY applies the SHAPE 1c mutation (replace the first occurrence of `generation BIGINT` with `generation INTEGER`); (c) runs the lane over the mutated copies and asserts the run exits non-zero AND the first line matching `TEST FAILED (` names `SHAPE 1c` — first-failure identity, not red-anywhere (D-14); (d) re-runs over pristine copies and asserts exit 0. If the gate file needs schema objects the stand-in fixtures do not provide, extend the fixture copies minimally and record what was added in README.md — do not weaken the gate file.
  </action>
  <verify>
    <automated>bash scripts/pg-lane/run.sh --tracer-proof</automated>
    Passing means: mutated run RED with `TEST FAILED (SHAPE 1c` as first failure; pristine run GREEN; no postgres listener and no scratch data dir left behind afterward.
  </verify>
  <done>The promoted lane exists with the documented CLI contract; the SHAPE 1c tracer proves mutate→RED(identity)→pristine→GREEN end-to-end on the real corpus file; the checkout is byte-identical after the run (`git status --porcelain` clean).</done>
</task>

<task type="auto">
  <name>Task 2: Lane self-test — prove the cleanup and the guard can FAIL</name>
  <files>scripts/pg-lane/run.sh, scripts/pg-lane/README.md</files>
  <action>
    Add `--self-test` to the lane (same script, no wrapper — local and CI invocations must be byte-identical in mode). It must exercise, in order:
    1. Occupied-port refusal: start a throwaway listener on a port, invoke the lane with `PORT` forced to it, assert exit code 2 and the refusal message (collision guard bites).
    2. Kill-mid-run orphan check (discharges D-04's "including on failure and on interrupt" and converts RESEARCH assumption A2 into a measurement): launch a lane run in the background, SIGTERM it after the cluster starts, then assert no postgres process is listening on the run's port and the run's data dir is gone. Repeat with SIGINT.
    3. Failure-path cleanup: run a gate file that deliberately fails, assert non-zero exit AND full cleanup.
    4. Success-path cleanup: normal run, assert exit 0 AND full cleanup.

    Anti-vacuity gate on the self-test itself (this phase's own standard): before finalizing, temporarily neuter the trap registration in a scratch COPY of the script, run the copy's self-test, observe check 2 go RED (orphan detected), then discard the copy. Record the observed RED in the task's commit message body. Do not leave any neutered artifact behind.
  </action>
  <verify>
    <automated>bash scripts/pg-lane/run.sh --self-test</automated>
  </verify>
  <done>All four self-test checks pass; the neutered-copy run was observed RED for the orphan check (evidence in commit message); README.md documents both entry points (`--self-test`, `--tracer-proof`) and the exact CLI contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → local psql execution | the lane executes SQL from tracked repo files on a private throwaway cluster |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-01 | Tampering | lane touching a foreign cluster | high | mitigate | collision guard preserved verbatim (refuse, never attach); free-port allocation reduces collisions |
| T-164.3-02 | Denial of Service | orphaned clusters exhausting disk | high | mitigate | EXIT/INT/TERM trap registered before initdb; self-test kill-check proves it (D-04) |
| T-164.3-03 | Information Disclosure | lane output in public CI logs | low | mitigate | lane connects only to 127.0.0.1 throwaway clusters; no DSNs or secrets exist in this plan |
| T-164.3-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed by this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `bash scripts/pg-lane/run.sh --tracer-proof` and `--self-test` both exit 0 locally.
- `git status --porcelain` shows no changes outside the four plan files (mutations happened only in scratch copies).
- After both runs: `pg_isready` finds no listener on any port the runs used; no scratch dirs remain.
</verification>

<success_criteria>
- VAC-02's script half is real: one script, trap-based cleanup proven able to fail and observed failing when neutered, portable PGBIN/port resolution (D-07's CI half lands in plan 08's job using this exact invocation).
- The tracer slice proves the phase's architecture: mutate-copy → RED with first-failure identity → pristine → GREEN, on the real corpus (D-02, D-14).
</success_criteria>

<output>
Create `.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-01-SUMMARY.md` when done. Record: the final CLI contract, the tracer-proof transcript's first-failure line, measured lane boot+apply+gate runtime (plan 08 budgets 30 arms against it), and any fixture extensions made for the gate file.
</output>
