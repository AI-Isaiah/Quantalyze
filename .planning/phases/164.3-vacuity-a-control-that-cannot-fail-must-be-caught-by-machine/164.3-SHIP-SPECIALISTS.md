---
phase: 164.3
stage: ship
round: specialists
status: issues_found
specialists: [testing, silent-failure-hunter, security]
counts: { critical: 6, high: 1, medium: 3, low: 2, informational: 10 }
---

# Ship-stage specialist findings — phase 164.3

Three specialists dispatched at ship time over `git merge-base origin/main HEAD..HEAD`,
excluding the generated `supabase/schema/baseline.sql`. IDs are `SP-`.

## CRITICAL

### SP-C01 — `src/__tests__/mutation-annotation-parser.test.ts:722` — the rule-3c "ORACLE" is decorative
The block claims to quantify over five injection spellings, but `injected` NEVER REACHES the
system under test: `laneOutput` is built from `ARM` alone, so all five arms are byte-identical.
**PROVEN by neuter:** replacing every `SPELLINGS` value with `NEUTERED-GARBAGE-N` leaves all 63
tests green. Same "enumerates instances while claiming the class" defect the file's own R3-C01
comment says it replaced, one describe block later. The sibling `it` at :714 is titled
"...every spelling names the same arm at runtime" but asserts only `Object.keys(SPELLINGS).length > 0`.
FIX: feed each spelling through the real path (parse → applyFileStep → stampIdentities →
unstampedIdentities/firstFailureArm) so the assertion depends on `injected`. If the nonce really is
spelling-independent by construction, DELETE the table, keep one honest arm, and state the property.
Make :714 assert what it names, or rename it.

### SP-C02 — `.github/workflows/ci.yml` — `mutation-runner/run.mjs --self-test` is invoked by NOTHING
Verified by repo-wide grep. It is the only one of the three new self-tests left unwired:
ci.yml runs `lint-sql-gates.mjs --self-test` and `verify-plan-anchors.mjs --self-test`.
The self-test is the SOLE proof the runner's six defect kinds (`no-red`, `wrong-first-failure`,
`occurrence-mismatch`, `identity-rewrite`, `synthesised-identity`, `floor`) can fire. The
full-corpus run CI does execute is green-by-construction at 30/30, so a runner whose defect
reporting was disabled would still print 30/30, clear both floors and exit 0. Vitest covers
`neuterArm`, the floors and the identity helpers — never `runCorpus`'s defect reporting.
Measured: `--self-test` passes 8/8 in seconds on the PostgreSQL the job already provisions.
FIX: add `run: node scripts/mutation-runner/run.mjs --self-test` to the `sql-mutation` job before
the corpus run, and pin the bare `run:` line from vitest the way `lint-sql-gates.test.ts` does.

### SP-C03 — `src/__tests__/mutation-runner-floors.test.ts:86` — mathematical tautology
`expect(naive).toBeGreaterThanOrEqual(file.prose)` cannot fail for any input: `naive` counts every
`RED-UNDER` substring, `file.prose` counts line-anchored marker lines, each such line contains at
least one substring and the lines are disjoint. Title says "STRICTLY larger" but asserts `>=`, and
neither quantity comes from the implementation (the loop never calls `scanCorpus`/`parseAnnotations`),
so it cannot detect the failure its own comment describes.
FIX: compare `parseFile(GATE).prose.length` against the naive count with `toBeGreaterThan` — an
unanchored parser then reds it. Keep `expect(totalAnchored).toBe(30)`.

### SP-C04 — `src/__tests__/local-stack-teardown-assertion.test.ts:178` — asserts a local string literal
`const prose = "the stack comes up and the baseline is loaded"; expect(prose).toContain("up");
expect(prose).not.toContain("run.sh up");` — true regardless of `run.sh`, `usage()`, or anything in
the repo. Cannot fail even if the whole lane is deleted. Its stated purpose is "non-vacuity for the
arm above, DRIVEN rather than argued".
FIX: drive it against the real `usage()` text — delete the `run.sh up` line from it, assert the bare
needle still passes on the mutilated text while the anchored needle now fails.

### SP-C05 — VAC-04's absence "measurement" is the SAME extractor as the fetcher
`.github/workflows/migration-drift-check.yml:192,197` wire both `BODY_FETCH_CMD` (`--extract-fn`) and
`BODY_NAME_INDEX_CMD` (`--function-names`) to `extractFunctionDefs()` over the SAME dump.
That function silently `continue`s past any definition it cannot parse
(`scripts/sql-body-normalize.mjs:313,326`), so a dropped def is absent from the index AND the fetch →
`scripts/prod-body-drift-check.sh:288` prints "measured absent — Treated as a NEW function (pass)".
The same extractor produces the left-hand `NAMES` list, so a dropped def in the MIGRATION exits 0
even earlier at `:171-174`.
**MEASURED end-to-end with the CI-shaped commands**, for a migration containing
`CREATE OR REPLACE FUNCTION public.sanitize_user$v2(p uuid)` — a `$` in the identifier is enough
(`readQualifiedName` stops at it, `masked[j] !== "("`, def dropped):
```
Migrations changed by this PR: 1
::notice::…this PR's migrations define no functions — nothing to compare.
GATE EXIT=0
```
`prod-body-drift-check.sh:59-69` claims the index makes absence "a MEASUREMENT". It does not — the
two sides are one code path. The only test exercising that branch
(`src/__tests__/drift-check-scripts.test.ts:271`) uses a STUB index from `basename live/*.sql`, so it
proves the helper, not the wiring.
FIX: make the index INDEPENDENT of `extractFunctionDefs` — query PROD
(`SELECT proname FROM pg_proc JOIN pg_namespace …`) as `test-ledger-drift-check.sh` already does; or
minimally cross-check in the workflow (`grep -acE '^\s*CREATE (OR REPLACE )?FUNCTION' "$DUMP"` vs
`--function-names | wc -l`) and MEASURE_FAIL when the extractor sees fewer. Also make
`NAME_COUNT -eq 0` at `:171` a MEASURE_FAIL when the changed migrations DO contain a
`CREATE … FUNCTION` by an independent count.

### SP-C06 — `scripts/prod-body-drift-check.sh:134` — `git diff … > changed.txt || true`
Converts "could not list the changed files" into "no migration files changed" → `exit 0`. With the
merge base resolved but the diff failing (bad object, shallow object DB, pathspec error),
`changed.txt` is empty, `CHANGED_COUNT=0`, and `:147-155` takes the "HONEST EXIT 0" branch — whose own
comment asserts "'could not measure' never lands here". `set -e` would have caught this; the
`|| true` defeats it.
FIX: delete `|| true`, or capture `rc` and `fail "could not enumerate this PR's migration changes"`.

## HIGH

### SP-H01 — `scripts/pg-lane/run.sh:501-503,539` — a skipped self-test arm still prints 5/5 and exits 0
A missing prerequisite (`node`) skips arm 2; the message says "Not a pass" but nothing acts on it, so
the arm is silently dropped from the count. The skipped arm is precisely the one proving the collision
guard is as wide as its message (IN-07). Same shape as the SKIP-01 defect this branch exists to delete.
FIX: track `skipped`; when non-zero print `SELF-TEST INCOMPLETE (4/5 run, 1 skipped: node absent)` and
`exit 1`.

## MEDIUM

### SP-M01 — `scripts/test-ledger-drift-check.sh:201-202` — "could not count" == "counted zero"
`missing_count="$(grep -ac … || true)"; missing_count="${missing_count:-0}"`. Any grep exit >= 2
(unreadable file, I/O error) yields an empty string, which `${:-0}` turns into `0`, and `:212` then
prints "all N repo migrations found by name."
FIX: `set +e; out=$(grep -ac '[^[:space:]]' "$missing_file"); rc=$?; set -e;
[ "$rc" -le 1 ] || fail "MEASURE_FAIL: could not count the missing-migration rows"`.

### SP-M02 — `scripts/test-ledger-drift-check.sh:166` — INT/TERM trap does not exit
`trap "rm -rf '$tmp'" EXIT INT TERM` — bash resumes after the handler, so Ctrl-C deletes `$tmp` and
execution continues against files that no longer exist. `pg-lane/run.sh:137-138` and
`local-stack/run.sh:143-144` both get this right.
FIX: split — `trap "rm -rf '$tmp'" EXIT; trap 'exit 130' INT; trap 'exit 143' TERM`.

### SP-M03 — `supabase/schema/BASELINE.md:48-52` — the secret-scan CLAIM exceeds the COMMAND
Prose certifies "no DSN, no `\connect`, no `ALTER DATABASE`, no JWT, no project ref", but the
documented repeatable command (`BASELINE.md:88`, mirrored `REPLAY-SPIKE.md:157`) matches only
`postgres(ql)://`, `@*.supabase.(co|com)` and `[a-z]{20}.supabase` — 3 of the 5 claimed classes. A
future regeneration carrying a `\connect` or a JWT passes and lands in a PUBLIC repo. Today's file is
clean (full set run independently), so this is forward-looking, not live exposure. Same
claim-never-compared-to-the-thing class the phase exists to remove.
FIX: extend both greps with `|\\connect|ALTER DATABASE|eyJ[A-Za-z0-9_-]{10,}`.

## LOW

### SP-L01 — `scripts/verify-plan-anchors.mjs:190-194` — walk swallows `readdirSync` errors
`catch { return; }` yields a silently incomplete index; a shorthand anchor that should report
`path-ambiguous` can instead uniquely match the wrong file and pass.
FIX: surface it as a `measureFails` entry rather than pruning quietly.

### SP-L02 — `.github/workflows/ci.yml` meta-command preflight (~:929-950) — `2>/dev/null` on grep
An unreadable or unstattable file reads as "clean"; the step's MEASURE_FAIL covers only a zero-length
glob. Same "silently declines to read a file" shape the surrounding comment names for NUL blindness.
FIX: drop `2>/dev/null`, treat grep exit >= 2 as `bad=1`.

## INFORMATIONAL

### SP-I01 — `src/__tests__/audit-coverage.test.ts:158` — mid-line-opened block comments not stripped
`stripBlockComments` enters a block only when a line's first non-whitespace token is `/*`. A block
opened MID-line and closed later leaves its continuation lines as code; reproduced — a JSDoc-ish line
mentioning `supabase.from('trades').insert()` is reported as a mutation site. That is the fabricated-site
class the fix's own header names as measured, now feeding a COUNTED exact-set allowlist.
FIX: enter on the first unmatched `/*` anywhere on the line (after same-line pairs are removed); add a
red fixture for the mid-line-open shape.

### SP-I02 — `src/__tests__/lint-sql-gates.test.ts:152` — arm does not assert what it is titled
"every red fixture CITES THE MECHANISM it reproduces" asserts only `toContain("RED FIXTURE")`. A
fixture whose mechanism attribution was deleted, or copied from another rule, passes.
FIX: assert the mechanism number derived from `RULES`, not hard-coded.

### SP-I03 — `src/__tests__/lint-sql-gates.test.ts:268` — only one of three new jobs has a wiring pin
`sql-gate-lint` gets a machine-checked blocking-wiring pin; `sql-mutation` and `plan-anchor-verify`
get none, so either could be dropped from `needs:` or from the result loop and stay green.
`sql-mutation` is the phase's headline detector.
FIX: generalise the pin over all three job names, asserting both the `needs:` entry and the
`"<job>=${{ needs.<job>.result }}"` line, and that no per-job tolerance arm exists for any of them.

### SP-I04 — `src/__tests__/mutation-runner-floors.test.ts:69` — two quantities under one name
`rederive()` classifies a file annotated on `prose > 0`; `scanCorpus` counts prose OR structured
markers (deliberately — IN-01). They agree today only because the single annotated file carries both.
When 164.4 backfills a structured-only file, `FILES_FLOOR` and the runner's coverage numerator bound
different quantities — the ARMS_FLOOR IN-05 defect one file over. Fails loudly, but on the ratchet
rather than the corpus, sending the reader to the wrong place.
FIX: make `rederive()`'s predicate `prose > 0 || twins > 0`; keep the prose pin for the line-start
property.

### SP-I05 — `supabase/schema/BASELINE.md:96-100` — overstates DRIFT-04's risk
Describes `create_allocator_connected_strategy` as SECURITY DEFINER + `GRANT ALL … TO authenticated`
writing encrypted credentials, and OMITS that the body enforces `auth.uid() IS NOT NULL`,
`auth.uid() = p_user_id`, and portfolio ownership. It is NOT exploitable. The real issue is governance:
defined by no migration, no snapshot, called by nothing in `src/`, invisible to every gate.
FIX: correct the wording so DRIFT-04 is not triaged as a live vulnerability. Disposition stays DROP.

### SP-I06 — `scripts/test-ledger-drift-check.sh:108` — asymmetric injection guard (shared TEST DB)
Migration filenames get an explicit charset allowlist before interpolation (`:176`, fails loud), but
`default_body_fetch` interpolates `p.proname = '$1'` from `BODY_CHECK_FUNCTIONS` with no equivalent
guard. Not exploitable today (hardcoded default, workflow does not override) but one env-var away from
a database other people's CI depends on.
FIX: apply the same allowlist.

### SP-I07 — `scripts/prod-body-drift-check.sh:264` — unsanitized function name in a filesystem path
`live="$TMP/${fname}.live.sql"`; `fname` comes from the normalizer, which reads a possibly-quoted,
possibly-schema-qualified name. `public."../../x"` yields a traversal write. Requires a
maintainer-authored migration, so hardening rather than a hole.
FIX: constrain `fname` to `[A-Za-z0-9_]` before using it as a path component.

## VERIFIED CLEAN (do not re-investigate)

- **`supabase/schema/baseline.sql` is clean.** Independently checked: 0 DSN/`\connect`/`ALTER DATABASE`,
  0 JWT/project-ref/`*.supabase.co`, 0 data statements, 0 `CREATE ROLE`/`PASSWORD`, 0 cron/`net.http_*`/
  Railway/Vercel/Fly hostnames/Bearer tokens, GRANT targets `public` only, sha256 matches BASELINE.md.
  Comment provenance: 338 of 342 `COMMENT ON` trace to public files; the 4 novel ones are benign.
  RLS: 61 tables, 57 granted to `anon`, **61/61 RLS-enabled, 0 gaps**, 5 deny-all.
- No `pull_request_target` anywhere; all `github.event.*` uses are concurrency numbers or `BASE_SHA`
  via `env:` — no untrusted input reaches a shell command.
- `ci.yml` holds zero production Supabase credentials.
- `test-ledger-drift-check.sh` issues read-only `SELECT`s against TEST; its `CREATE OR REPLACE FUNCTION`
  strings are self-test fixture TEXT behind stub commands and never reach TEST.
- `prod-body-drift-check.sh` honours its redaction contract on failure paths too — fetcher stderr
  withheld at `:217` and `:270`; every error branch emits only names, arg counts, sha256, hunk counts.
- `.stack-env`: `mktemp` 0600, `chmod 600` after `mv`, removed on teardown, gitignored (verified with
  `git check-ignore`). Keys are the CLI's well-known local defaults.
- `pg-lane` cluster is loopback-only: `listen_addresses=127.0.0.1` with `-k ''` disabling the unix
  socket. `--auth=trust` is acceptable for a throwaway fixture-only cluster.
- Aggregator wiring sound: 12 `needs:` entries all present in the result loop; the two tolerance arms
  (`e2e-seeded`, `sql-tests`) unchanged; `sql-gate-lint`/`sql-mutation` take the strict default arm;
  `plan-anchor-verify`'s `skipped` tolerance correctly conditioned on `is_pr != "true"`; no new
  `continue-on-error`.
- No `child_process` in the new `.mjs`; `mutation-runner/run.mjs:716` uses `spawnSync` with array args,
  no `shell: true`.

### `.github/workflows/migration-drift-check.yml:31-35` — FOUNDER DECISION NEEDED, not a code fix
The `paths:` filter now includes `scripts/prod-body-drift-check.sh` and `scripts/sql-body-normalize.mjs`,
so a PR touching only those runs with `SUPABASE_DB_PASSWORD` + `SUPABASE_ACCESS_TOKEN` in env, executing
script content from the PR head. Fork PRs are correctly excluded by the same-repo `if:` at `:55`, so the
blast radius is push-access collaborators. Confirm the `Production` environment carries required
reviewers; otherwise this is arbitrary code execution with the prod DB password for anyone with push.
