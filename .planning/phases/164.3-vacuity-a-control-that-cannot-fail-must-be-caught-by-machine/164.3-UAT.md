---
status: complete
phase: 164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine
source: 164.3-VERIFICATION.md (5 gaps + 3 behavior_unverified_items + 1 human_verification)
started: 2026-09-05T21:00:00Z
updated: 2026-09-05T21:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. G1 — the VAC-07 local-stack lane can load a schema
expected: `bash scripts/local-stack/run.sh up` boots against the committed baseline instead of exiting FATAL
result: blocked
blocked_by: prior-phase
reason: |
  RE-MEASURED 2026-09-05 at HEAD. UNCHANGED and still open:
  `scripts/local-stack/run.sh:57` still reads `BASELINE_FILE="${LANE_DIR}/baseline.sql"`
  (= `scripts/local-stack/baseline.sql`), and `.gitignore:138` still excludes that path, while the
  committed baseline lives at `supabase/schema/baseline.sql`. Two mutually inconsistent answers to
  "where does the baseline live" remain in the repo and the code honours neither.

  CORRECTLY ROUTED, which is what changed since verification: this is now Phase 164.5 item (1),
  named verbatim in its goal ("repoint `scripts/local-stack/run.sh:50` at
  `supabase/schema/baseline.sql` and drop the `.gitignore:138` exclusion"). Not 164.3's to fix.

### 2. G2 — the VAC-07 deferral is routed to a named owner
expected: A dated TODOS deferral item, Phase 159's two items re-pointed, and a ROADMAP phase explicitly carrying VAC-07
result: pass
reported: "All three 'missing' bullets satisfied. Measured 2026-09-05."
notes: |
  (a) `TODOS.md:1631` carries `[VAC-07-DEFER]` — dated 2026-08-29, founder decision, owner named
      as Phase 164.5, with the measured reason (69 of 262 migrations fail to replay from empty).
  (b) `159-VERIFICATION.md` carries `⛔ RE-POINTED 2026-08-29 (Phase 164.3 verification gap G2):
      this item is now blocked on PHASE 164.5, not 164.3`, and replaces the falsified
      "cheap once 164.3 lands" recipe with the measured one.
  (c) ROADMAP Phase 164.5 item (6) is VAC-07, and its `**Requirements**` line lists VAC-07 with the
      `[VAC-07-DEFER]` scoring prohibition attached.

### 3. G3 — VAC-03's requirement sentence matches the shipped rule count
expected: ROADMAP criterion 3 and REQUIREMENTS VAC-03 stop claiming "the five measured shapes"
result: pass
reported: "Both amended, each carrying a dated '(Corrected 2026-08-29, gap G3)' note. Measured 2026-09-05."
notes: |
  `ROADMAP.md:510` and `REQUIREMENTS.md:159` now read "the four statically-decidable measured
  shapes (mechanisms 1, 2, 4 and a narrow 3)" with mechanism 5's delegation stated and pinned by
  `DELEGATED_MECHANISMS`. Both also carry a later 2026-09-04 update (Phase 164.4 WR-03) recording
  that the linter now ships SEVEN rules closing a sixth mechanism, with per-rule real-corpus
  floors — and explicitly stating that this does not disturb the mechanism-5 delegation.

  ⚠️ Residual, NOT blocking (prose only, per the stopping rule): `ROADMAP.md:865` still describes
  H-0001 as "one of the five measured vacuity shapes (criterion 3)" while criterion 3 now says
  four. `:502` and `:623` also say "all five measured mechanisms/vacuity shapes", but those
  sentences count MECHANISMS FOUND, not lint rules, and are correct as written. Only `:865` mixes
  the two. Booked, not fixed here.

### 4. G4 — the H-0001 ledger entry stops asking for work plan 03 completed
expected: The entry records the detector fix + un-skipped test + re-run census, leaving only the six allowlisted routes open
result: pass
reported: "Rewritten. Measured 2026-09-05."
notes: |
  `TODOS.md:2759-2778` now carries a dated section recording that the old entry "said 'H-0001 stays
  deferred' and asked for a fix that had already landed, which would have sent [a reader to redo
  it]". `:2796` is the corrected remaining item — bring the six `H_0001_UNCOVERED_ALLOWLIST` routes
  under the audit law — which is the genuinely-open per-site compliance judgment.

### 5. G5 — plan 01's SUMMARY over-claimed VAC-02
expected: Recorded so the pattern is not repeated; no other action
result: pass
reported: "Closed as record-only per the gap's own `missing:` field ('No action required beyond this record — the verifier, not a SUMMARY, marks requirements'). No requirement checkbox was ever ticked off the false claim, so nothing downstream was misled."

### 6. BU-1 / VAC-02 — the disposable-PostgreSQL lane has identical semantics in a CI job on ubuntu
expected: The PostgreSQL-binaries probe resolves on the ubuntu runner and the runner reports `arms: 30/30/0`, exit 0
result: pass
reported: "DISCHARGED, SHA-bound. workflow_dispatch run 33620169220, head sha 89cbef8bec875055330875ad6d937a7c07690912, 2026-09-02: `sql-mutation: success`, `sql-gate-lint: success`."
notes: |
  RESEARCH assumption A1 (ubuntu ships server binaries under /usr/lib/postgresql/<major>/bin) is
  now executed, not assumed. WINDOWS.md 28 closes.
  ⚠️ The enclosing workflow run's overall conclusion is `failure` — other jobs in it failed. The
  two jobs this item names both succeeded. Recorded because a reader checking the run-level badge
  would draw the wrong conclusion in either direction.
  ⚠️ The `30/30/0` figure is that run's DATED reading. The corpus has since grown to
  `arms: 363/363/0` at `files 44/71` (run 33961609382 @ `1aa8bb70`), so read the floors off
  `scripts/mutation-runner/run.mjs`, never off this line.

### 7. BU-2 / VAC-04 — the repo-vs-PROD body diff reads PROD
expected: `supabase db dump --linked --schema public` succeeds, the CREATE FUNCTION guard passes, and the gate prints MATCH lines with sha256s (or a real DRIFT finding)
result: issue
reported: "HALF discharged. The credential path is proven; the comparator has still never compared anything against PROD."
severity: minor
notes: |
  Measured in run 33643046189, job 100290700544, 2026-09-02 (branch
  `phase-164.3.1-sound-primitives-pr`):
  ✅ `Assert secrets are configured (D-17 — a skip is not a pass)` → success, "Drift-check
     credentials present."
  ✅ `supabase link` succeeded against project ref `khslejtfbuezsmvmtsdn`; "Dumping schemas from
     remote database..." → "Dumped schema to /home/runner/work/_temp/prod-schema.sql." So the
     PROD-credentialed dump path — the never-executed half WINDOWS 25 recorded — HAS now run.
  ✅ The CREATE FUNCTION guard passed (no "the PROD schema dump contains no function definitions").
  ⛔ The step then ended: "VAC-04 repo-vs-PROD function-body drift gate: this PR changes no
     migration files — nothing to compare against PROD."

  So the comparator has only ever taken its no-op branch against real PROD. Its biting behaviour
  is proven ONLY against an injected fetcher (locally, by the verifier). Closing this needs a PR
  that actually touches `supabase/migrations/**` — which Phase 164.5 will produce twice (DRIFT-04
  and CRON-DRIFT-01-REPAIR). Recorded as an issue rather than a pass so the no-op branch is not
  mistaken for a green comparison.

### 8. BU-3 / VAC-08 — the repo-vs-TEST ledger and body check runs against TEST
expected: Zero missing migrations joined on `schema_migrations.name`, and the body assertion passes
result: pass
reported: "DISCHARGED against the real credential. Run 33979505776 on main @ e01cc2e6, job 101344200489, step 7, 2026-09-05T17:14:07Z."
notes: |
  Step output, read from the job log rather than the step conclusion:
  "VAC-08 repo-vs-TEST ledger and body drift gate: 262 migration(s) checked against the ledger,
   4 body comparison(s) over 3 named function(s)."
  "VAC-08 ... ledger and body checks clean."

  The denominators are NON-EMPTY and 262 equals `ls supabase/migrations/*.sql | wc -l` exactly.
  VAC-08 exits 1 on an absent DSN and never skips, so a `success` here means it connected and ran.
  WINDOWS.md 26 closes.

  ⛔ RETRACTION, same day. I first recorded a gap claiming this "clean" CONTRADICTED the live
  VAC08-LEDGER-32 item (32 repo migrations with no TEST ledger row). That was MY error, and the
  method that produced it is worth recording: I grepped the job log for the token `VAC-08`, which
  matches only the two prefixed summary lines. The decisive lines are indented and unprefixed, so
  my own filter hid them. Read in full, the step says:
      Repo migrations: 262
        ledger presence: 32 absent, all 32 baselined (see scripts/vac08-ledger-baseline.txt); 0 NEW drift.
        advisory: 2 ledger row(s) have no repo file (squashes / CLI-era rows). Not a failure.
  So "clean" means 0 NEW drift and 0 STALE baseline entries under a DATED RATCHET — not 0 absent.
  VAC08-LEDGER-32 and this run are the SAME measurement (230 of 262 matched, 32 absent), and the
  baseline plus the ratchet shipped together in `7bcddd94` deliberately. Nothing was applied to
  TEST; that premise is confirmed, not refuted — the only non-dry-run `db push` in
  `.github/workflows/` is `supabase-migrate.yml:244`, which targets PROD.
  ⭐ The lesson generalises: grepping a CI log by a token tests the token, not the gate.

## Summary

total: 8
passed: 5
issues: 1
pending: 0
skipped: 0
blocked: 1

(Test 5 is counted under passed as a record-only closure.)

## Gaps

- truth: "VAC-08's ledger ratchet reads its own NEW/STALE counts with the exact pattern the same file documents as wrong (SP-M01)"
  status: failed
  reason: >
    Found 2026-09-05 by a read-only investigation opened because I had MISREAD this gate (see the
    retraction below). scripts/test-ledger-drift-check.sh:304-318 carries an explicit SP-M01 comment
    block explaining that `missing_count="$(grep -ac … || true)"; missing_count="${missing_count:-0}"`
    "turns 'could not count' into 'counted zero'" — grep exits 0 with a count, 1 with no match, and
    >=2 on an ERROR, and on >=2 the substitution is EMPTY, `${:-0}` makes it 0, and the gate reports
    the result it wanted. That site was FIXED: it captures grep_rc and calls
    `fail "MEASURE_FAIL: ... An uncountable result is not a count of zero."`
    Sixty lines later, :372-373 uses the UNFIXED pattern twice, for the two counts that actually
    drive the exit decision:
        new_count="$(grep -ac '[^[:space:]]' "$new_file" || true)";   new_count="${new_count:-0}"
        stale_count="$(grep -ac '[^[:space:]]' "$stale_file" || true)"; stale_count="${stale_count:-0}"
    `bad` is set ONLY by stale_count>0, new_count>0, body drift, or zero comparisons (:376, :386,
    :550-553) — missing_count never gates. So these two are the whole ledger half of the gate.
    Also :363/:365 swallow a grep ERROR (rc>=2) with `|| true` without distinguishing it from the
    legitimate rc 1 (nothing selected), and :336 disables the absurdity floor on a failed count
    via `2>/dev/null || echo ""`.
  severity: major
  test: 8
  root_cause: "The SP-M01 fix was applied at the site that raised it and not to the two sites with the same shape in the same function."
  artifacts:
    - path: "scripts/test-ledger-drift-check.sh"
      issue: ":372-373 — new_count/stale_count use the `|| true` + `${:-0}` shape that :304-318 documents as a false-clean; these two counts are the only ones that gate the exit"
    - path: "scripts/test-ledger-drift-check.sh"
      issue: ":363/:365 — `grep -aFxv … || true` does not distinguish rc 1 (nothing selected, legitimate) from rc >=2 (error), so a broken filter reads as 0 NEW drift"
    - path: "scripts/test-ledger-drift-check.sh"
      issue: ":336 — `2>/dev/null || echo \"\"` silently disables the ledger-row absurdity floor when its count query fails"
  missing:
    - "Apply the SP-M01 idiom already present at :304-318 to new_count and stale_count: capture the rc, MEASURE_FAIL on >=2, and only let rc 1 yield an empty count."
    - "Distinguish rc 1 from rc >=2 at :363/:365."
    - "Prove it the way this phase requires: a test that makes the grep fail (unreadable temp file) and asserts MEASURE_FAIL rather than a clean report — a fix with no failing test is the defect class 164.3 exists to remove."
    - "Owner: Phase 164.6 GATE-HYGIENE (gate integrity, not user-facing copy)."
  debug_session: ""

- truth: "ROADMAP:865 still calls H-0001 'one of the five measured vacuity shapes (criterion 3)' after criterion 3 was narrowed to four"
  status: failed
  reason: >
    Prose residue from gap G3's correction, which amended ROADMAP:510 and REQUIREMENTS:159 but not
    this back-reference. Non-blocking under the stopping rule (prose/stale-integer, no user-facing
    or data-integrity effect). Distinct from ROADMAP:502 and :623, which say "five measured
    mechanisms" correctly — those count mechanisms FOUND, not lint rules.
  severity: cosmetic
  test: 3
  root_cause: "A scope amendment that touched two files left a third back-reference behind."
  artifacts:
    - path: ".planning/ROADMAP.md"
      issue: "line 865 — cites criterion 3's pre-narrowing count"
  missing:
    - "Route to Phase 164.6 GATE-HYGIENE with the other prose/citation-truth items."
  debug_session: ""
