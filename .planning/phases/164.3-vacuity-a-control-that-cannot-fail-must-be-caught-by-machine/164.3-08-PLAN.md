---
phase: 164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine
plan: 08
type: execute
subsystem: mutation-runner
tags: [red-under-m, corpus-annotation, sql-mutation-job, aggregator]
wave: 3
depends_on: ["164.3-01", "164.3-05"]
files_modified:
  - supabase/tests/test_strategy_shares_rls.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-runner-floors.test.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [VAC-01]
provides:
  - "Every arm-anchored prose RED-UNDER in the corpus has a machine twin; parity green"
  - "Full-corpus runner run green with ARMS_FLOOR pinned at the measured biting count"
  - "sql-mutation CI job in the frontend aggregator — the on-every-push detector exists"
affects: [ci-frontend-aggregator, phase-164.4]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every arm-anchored prose RED-UNDER annotation in test_strategy_shares_rls.sql has an adjacent RED-UNDER-M twin (apply or waiver); the parity gate passes (D-14)"
    - "The full-corpus runner run is GREEN: every non-waived arm's mutation reddens its arm as the FIRST failure and pristine runs are green (D-02, D-14)"
    - "A sql-mutation CI job runs the exact local invocation on every push and the frontend aggregator fails on anything but success (D-07, D-09)"
    - "ARMS_FLOOR is pinned at the measured biting count — measured, never invented"
  artifacts:
    - path: "supabase/tests/test_strategy_shares_rls.sql"
      provides: "the annotated corpus: prose + machine twins, waivers counted"
      contains: "RED-UNDER-M"
    - path: ".github/workflows/ci.yml"
      provides: "sql-mutation job + strict aggregator row"
      contains: "sql-mutation"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/mutation-runner/run.mjs"
      via: "job step pastes the runner invocation verbatim (mode identity)"
      pattern: "mutation-runner/run.mjs"
    - from: "supabase/tests/test_strategy_shares_rls.sql"
      to: "scripts/mutation-runner/GRAMMAR.md"
      via: "every RED-UNDER-M line conforms to the plan-05 grammar"
      pattern: "RED-UNDER-M"
---

<objective>
Turn the runner loose on the real corpus: translate every arm-anchored prose `RED-UNDER` annotation in `supabase/tests/test_strategy_shares_rls.sql` into its structured `RED-UNDER-M` twin (D-14), drive the full-corpus run to green (every non-waived arm bites with first-failure identity), pin `ARMS_FLOOR` at the measured value, and wire the `sql-mutation` CI job into the frontend aggregator — the on-every-push machine detector the phase exists to create.

⚠️ Count discipline (planner-measured 2026-08-29): the file carries **30** arm-anchored prose markers by line-start-anchored count — NOT the 33 that CONTEXT/RESEARCH recorded from a naive substring grep (the file header mentions the marker in prose three times; one header mention even matches a plain marker-with-colon grep at line 48). Task 1 re-derives the number with the plan-05 parser and works from THAT list. Do not chase phantom arms; do not trust either recorded number over the parser's output.

Purpose: completes VAC-01. After this plan, a gate arm whose declared mutation stops biting reds CI on every push.
Output: annotated corpus + green full run + pinned floors + CI job.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-CONTEXT.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-05-SUMMARY.md
@scripts/mutation-runner/GRAMMAR.md

Read before implementing: `164.3-RESEARCH.md` §Q3 (the three prose shapes with the verbatim corpus examples and their recommended structured forms — including the NONCE arm whose prerequisite is neutering a different arm, and the LAYERED trigger mutation that must also remove a migration-arm term or the file never runs), plan 05's SUMMARY (final grammar keys, runner invocation, per-arm timing), and plan 01's SUMMARY (lane file sequence). `164.3-PATTERNS.md` §3 (the sql-mutation job scaffold, aggregator strict-row shape, preflight reuse) and §5 (annotation excerpts).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate every prose annotation into its structured twin</name>
  <files>supabase/tests/test_strategy_shares_rls.sql</files>
  <action>
    Enumerate the arm-anchored prose markers with the plan-05 parser (line-start anchor — this is the authoritative list and count). For each, add the adjacent `RED-UNDER-M` line per GRAMMAR.md:
    - Migration-edit shapes: byte-exact find/replace with `nth`, resolved by READING the named migration and locating the prose's target (e.g. "the STEP 1 CREATE TABLE" becomes an exact find string); insertions get exact `insert-after` anchors.
    - Live-DB GRANT shapes: `sql` steps (the lane's post-apply hook), with `neuter` entries where the prose records that another arm fires first (the NONCE arm documenting SHAPE 3b's exact-set pin is the worked example — encode its neuter chain exactly as the prose describes).
    - LAYERED shapes: multi-step `apply` arrays spanning migration and gate copies in one mutation (the trigger-timing arm whose migration-arm term must be removed in the same mutation, or the file never runs).
    - Recorded waivers (the deleted-nonce note on SHAPE 1, and NONCE 5c per the header): explicit `waiver` twins carrying the recorded reason — counted, never silently skipped.
    Add nothing else to the file: no arm changes, no message changes, no reformatting (surgical-change rule — the diff must be RED-UNDER-M lines only). Parity gate must pass: every prose marker has exactly one twin.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only 2>/dev/null || node scripts/mutation-runner/run.mjs --self-test</automated>
    (Use the runner's parse/parity entry point per plan 05's final contract; parity over the real file must report zero missing twins. If no parse-only mode exists, add one — it is a trivial flag on the existing parser.)
  </verify>
  <done>Parser reports N prose markers / N twins / W waivers for the file (record N and W — expected near 30 total); `git diff` shows only added RED-UNDER-M comment lines.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the full-corpus run green and pin ARMS_FLOOR</name>
  <files>supabase/tests/test_strategy_shares_rls.sql, scripts/mutation-runner/run.mjs, src/__tests__/mutation-runner-floors.test.ts</files>
  <action>
    Run the full corpus through the runner. Iterate on defects the aggregate report names:
    - Wrong-first-failure: usually a missing `neuter` prerequisite the prose documents — encode it; if the prose does NOT document one and the arm genuinely cannot be made the first failure, convert to a counted waiver with the measured reason recorded in the twin. NEVER delete or weaken a prose annotation, an arm, or a failure message to make the run pass — the corpus is the specification, the annotations adapt to it.
    - Non-biting mutation: fix the twin's find/replace against the real migration bytes.
    Then pin `ARMS_FLOOR` in run.mjs at the measured biting-arm count from this green run (biting = executed non-waived arms; the floor comment states the measured date and that it fails on regression only), and update the floor contract test to re-derive and match. Record total wall-clock: if the full run exceeds roughly 10 minutes, the template-database optimization RESEARCH §Q3 describes is permitted PROVIDED per-arm semantics (fresh state per arm, first-failure identity, pristine-green) are unchanged — record whichever you do.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs && npx vitest run src/__tests__/mutation-runner-floors.test.ts && git status --porcelain | wc -l</automated>
    Full run exit 0 with coverage `files 1/71` and the arm counts printed; floors test green; checkout clean of scratch artifacts.
  </verify>
  <done>Every non-waived arm bites with correct identity; waivers counted and printed; ARMS_FLOOR pinned at the measured value; runtime recorded.</done>
</task>

<task type="auto">
  <name>Task 3: The sql-mutation CI job + strict aggregator row</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Add the `sql-mutation` job per PATTERNS §3: hermetic scaffold (SHA-pinned checkout persist-credentials false, setup-node 22, node_modules cache + npm ci retry, `permissions: contents: read`, concurrency), an early step verifying the runner-image PostgreSQL 16 binaries resolve (list the /usr/lib/postgresql glob — converts RESEARCH assumption A1 into a measurement; the lane's PGBIN chain handles either layout), the psql meta-command preflight from the sql-tests job applied to every file the runner will execute (reuse the existing per-pattern named-error shape with `grep -a`; the security note in RESEARCH requires the new lane to run it too), then the runner invocation PASTED VERBATIM from the script header (mode identity — wrapping a run changes its result, measured). NO mutex and NO secrets: the job touches only its own throwaway clusters; add the WHY comment stating the aggregator's ban on new needs-edges among TEST-mutex jobs does not apply to it.
    Aggregator: add `sql-mutation` to `needs:` AND the result loop as a strict row with NO skip tolerance (no secret consumed → a skipped state is always a real fault), WHY comment at the density of the existing rows.
    Job-level anti-vacuity: the run's coverage line is part of the job log — grep-assert in a final step that the printed coverage matches the floor constants (a runner that silently stopped printing coverage would otherwise pass unnoticed; this is D-01's print made load-bearing).
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && node scripts/mutation-runner/run.mjs --self-test</automated>
    The live proof is the job's first run on this branch — a red first run names a real portability defect; fix forward, never soften an exit code.
  </verify>
  <done>Job + aggregator row exist with verbatim invocation and preflight; coverage-line assertion present; first CI conclusion recorded in the SUMMARY (or noted pending).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked gate/migration files → CI psql execution | the job executes repo SQL against a private throwaway cluster on the runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-19 | Tampering | gate files smuggling psql meta-commands into the job | medium | mitigate | the existing meta-command preflight runs before any gate executes, same as sql-tests |
| T-164.3-20 | Denial of Service | 30 cluster boots exhausting job time | low | mitigate | timing measured in Task 2; template-DB optimization sanctioned if needed; job timeout set |
| T-164.3-21 | Repudiation | waiver creep hiding non-biting arms | medium | mitigate | waivers counted + printed every run; ARMS_FLOOR reds if biting count regresses |
| T-164.3-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed by this plan |
</threat_model>

<verification>
- Full-corpus run green locally with coverage `files 1/71` and pinned floors; floors contract test green.
- `git diff` on the gate file contains only RED-UNDER-M additions.
- ci.yml parses; the job's run step is byte-identical to the script header's documented invocation (diff them).
</verification>

<success_criteria>
- VAC-01 complete: on every push, a machine mutates every annotated arm, requires the named arm to be the FIRST failure, restores green, prints coverage, and exits 1 on a non-biting annotation or a floor regression (D-01, D-02, D-09, D-14). 164.4 inherits a working oracle.
</success_criteria>

<output>
Create `.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-08-SUMMARY.md` when done. Record: the authoritative arm count (prose/twins/waivers), the pinned ARMS_FLOOR, full-run wall-clock, per-arm defects fixed during iteration, and the CI job's first conclusion.
</output>
