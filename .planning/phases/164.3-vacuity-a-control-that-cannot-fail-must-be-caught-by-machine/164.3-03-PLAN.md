---
phase: 164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine
plan: 03
type: execute
subsystem: vitest-gates
tags: [audit-coverage, findMutations, blind-gate]
wave: 1
depends_on: []
files_modified:
  - src/__tests__/audit-coverage.test.ts
autonomous: true
requirements: [H-0001]
provides:
  - "audit-coverage gate sees single-line from(...).insert(...) mutation sites"
affects: [audit-coverage-gate]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "findMutations detects the single-line from(...).insert(...) idiom — a seventh uncovered site can no longer appear without the gate going red"
    - "The formerly-skipped intended-behavior test runs (no it.skip) and FAILS if the single-line detection regresses"
    - "The known uncovered sites are a counted, visible allowlist — not silent blindness"
  artifacts:
    - path: "src/__tests__/audit-coverage.test.ts"
      provides: "fixed findMutations + un-skipped intended-behavior test + re-measured census allowlist"
      contains: "H-0001"
  key_links:
    - from: "src/__tests__/audit-coverage.test.ts"
      to: "src/app/api"
      via: "findMutations scans route sources for mutation call sites"
      pattern: "findMutations"
---

<objective>
Close absorbed backlog item H-0001: the audit-coverage gate is blind to the single-line `from(...).insert(...)` idiom at six known call sites, and a seventh can appear without anything going red — a gate that cannot fail, which is this phase's exact subject. Fix `findMutations`' detection, un-skip the intended-behavior test, re-run the census, and convert the silent blindness into a counted, visible allowlist that reds on any NEW uncovered site.

Purpose: the phase goal is that a non-failing control is caught by machine on every push; this is the one already-identified blind gate in the vitest layer.
Output: one test file, fixed and un-skipped. NO production code changes — adding audit-law coverage to the six sites is separate product work; this plan makes the debt visible and ratcheted, not silently absent.

⛔ Constraints: file-scoped vitest runs cannot clear `src/__tests__/contracts/` (those scan all of src/ and full-suite noise clears them) — verify with the file-scoped run here and rely on CI's sharded full suite for the rest. Do NOT edit TODOS.md (the H-0001 entry is closed at phase verification against this plan's SUMMARY evidence).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-CONTEXT.md

Read before implementing: TODOS.md section "Phase 163 / SEC-03 — H-0001 census RE-MEASURED" (the re-measured six-site census, its method, and the lesson: re-measure records, never re-number them) and the full `it.skip` block plus `findMutations` implementation in `src/__tests__/audit-coverage.test.ts`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix findMutations single-line detection, un-skip, re-census, allowlist visibly</name>
  <files>src/__tests__/audit-coverage.test.ts</files>
  <action>
    1. Fix `findMutations` so the single-line `from(...).insert(...)` idiom (and its `update`/`upsert`/`delete` siblings if the same regex class misses them — check while you are in there, per the fix-whole-class discipline) is detected. The current detector's miss mechanism is documented in the file's own H-0001 comment.
    2. RE-MEASURE the census rather than trusting the recorded list (the TODOS lesson: the previous record under-reported by two AND named a deleted site). Run the fixed detector, diff detected-but-uncovered sites against the file's recorded list (the record names `keys/sync`, two `finalize-wizard` sites, and three others — verify each at HEAD).
    3. Newly-visible uncovered sites go into an EXPLICIT allowlist inside the test — each entry carrying file path, the reason it is uncovered, and a reference to H-0001 — asserted with an exact-set comparison so that (a) a NEW single-line mutation site not in the allowlist fails the gate, and (b) a site that gets fixed or deleted also fails until removed from the allowlist (drift in either direction reds, matching the floor-plus-contract pattern used elsewhere in CI). This converts silent blindness into counted debt without pulling production audit work into a CI-machinery phase.
    4. Un-skip the `it.skip("H-0001 (intended behavior)")` test and make it assert the intended behavior directly: a canonical single-line mutation sample IS detected by `findMutations`. This is the self-test that reds if the regex ever regresses.
    5. Update the H-0001 comment block: the re-measured list, the method, the date, and the standing warning not to trust the numbers past the next refactor.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/audit-coverage.test.ts</automated>
  </verify>
  <done>The file has no `it.skip` for H-0001; the census is re-measured at HEAD; the allowlist is exact-set-asserted; the intended-behavior test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the gate bites in both directions</name>
  <files>src/__tests__/audit-coverage.test.ts</files>
  <action>
    Founder rule, applied to the fix itself (neuter → observe RED → restore):
    1. New-site direction: create a temporary scratch file under the scanned tree containing a synthetic single-line mutation call NOT in the allowlist, run the file-scoped test, observe RED, delete the scratch file, observe GREEN. Do this in a scratch copy or clean up in the same task — the committed tree must not carry the synthetic site.
    2. Regression direction: temporarily re-break the detection regex (in the working tree only), observe the un-skipped intended-behavior test go RED, restore, observe GREEN. Use byte-level care: verify the restore by re-running the test, not by eyeballing (stale-backup lesson).
    Record both observed REDs (test names + failure lines) in the commit message body. Nothing neutered may survive in the commit — `git diff` before committing must show only the intended fix.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/audit-coverage.test.ts</automated>
    Plus `git status --porcelain` shows only src/__tests__/audit-coverage.test.ts modified.
  </verify>
  <done>Both directional REDs observed and recorded; final state GREEN; no synthetic site or broken regex in the commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test scanner → src/ sources | findMutations reads repo sources; no external input crosses in |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-08 | Repudiation | allowlist hiding new uncovered sites | medium | mitigate | exact-set assertion reds on drift in either direction; entries carry reasons |
| T-164.3-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed by this plan |
</threat_model>

<verification>
- File-scoped vitest green; both neuter runs observed RED (evidence in commit message).
- The allowlist count equals the re-measured census (state the number in the SUMMARY — do not trust the recorded 6 without re-measuring).
</verification>

<success_criteria>
- H-0001 discharged: single-line detection works, the intended-behavior test is live and can fail, known debt is counted and visible, and a seventh site cannot appear silently.
</success_criteria>

<output>
Create `.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-03-SUMMARY.md` when done. Record the re-measured census (count + sites), both observed REDs, and note that adding audit coverage to the allowlisted sites remains open product work under H-0001's TODOS entry.
</output>
