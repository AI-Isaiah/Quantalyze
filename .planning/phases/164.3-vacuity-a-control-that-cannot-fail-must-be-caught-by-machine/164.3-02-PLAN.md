---
phase: 164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine
plan: 02
type: execute
subsystem: ci-drift-gates
tags: [drift, prod-body-diff, ledger-join, normalizer]
wave: 1
depends_on: []
files_modified:
  - scripts/sql-body-normalize.mjs
  - scripts/prod-body-drift-check.sh
  - scripts/test-ledger-drift-check.sh
  - src/__tests__/sql-body-normalize.test.ts
  - src/__tests__/drift-check-scripts.test.ts
  - .github/workflows/migration-drift-check.yml
  - .github/workflows/ci.yml
autonomous: true
requirements: [VAC-04, VAC-08, SKIP-01, DRIFT-01, OPS-08-F9]
provides:
  - "Shared comment-strip + whitespace normalizer (one implementation, two callers — D-05)"
  - "VAC-04 step inside migration-drift-check.yml (PR-triggered, unskippable, redacted)"
  - "VAC-08 name-join ledger drift step + body assertion inside sql-tests"
affects: [supabase-migrations-workflow, sql-tests-job]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A PR that whole-body CREATE OR REPLACEs a function whose PROD body differs from the committed snapshot goes RED with hashes and hunk counts only — never body text (VAC-04, D-12, D-13)"
    - "When the credential is absent the VAC-04 step exits 1 with an explicit error — it NEVER skips and never exits 0 (D-11 reasoning, preserved through D-12)"
    - "The repo-vs-TEST ledger check joins schema_migrations on name, tolerates double-apply, and a repo migration missing from the ledger goes RED (VAC-08, VERIFIED CORRECTION 1)"
    - "Every body comparison strips comments before matching on BOTH sides (D-05)"
    - "The pre-existing configured=false skip in migration-drift-check.yml no longer reads as PASS for same-repo runs (D-17)"
  artifacts:
    - path: "scripts/sql-body-normalize.mjs"
      provides: "strip -- line comments, /* */ block comments, collapse whitespace — the single shared implementation"
    - path: "scripts/prod-body-drift-check.sh"
      provides: "VAC-04 gate logic with injectable body fetcher, testable without PROD"
    - path: "scripts/test-ledger-drift-check.sh"
      provides: "VAC-08 name-join query + body assertion with --self-test"
  key_links:
    - from: ".github/workflows/migration-drift-check.yml"
      to: "scripts/prod-body-drift-check.sh"
      via: "new step invokes the script verbatim (mode identity)"
      pattern: "prod-body-drift-check"
    - from: ".github/workflows/ci.yml"
      to: "scripts/test-ledger-drift-check.sh"
      via: "sql-tests step after the mutex invokes the script"
      pattern: "test-ledger-drift-check"
    - from: "scripts/prod-body-drift-check.sh"
      to: "scripts/sql-body-normalize.mjs"
      via: "normalizes both sides before diffing"
      pattern: "sql-body-normalize"
---

<objective>
Close the drift half of the phase in one plan: the shared comment-strip normalizer (D-05), the VAC-04 repo-vs-PROD body gate riding the EXISTING `migration-drift-check.yml` job and credential (D-12, D-13, D-17), and the VAC-08 repo-vs-TEST ledger check that joins on `name` never `version` (VERIFIED CORRECTION 1), with its body-level pairing. Absorbs SKIP-01 and DRIFT-01 (both land in VAC-08) and OPS-08-F9 (verify-and-record ONLY — the floors are already raised).

Purpose: DRIFT-02 nearly shipped a GDPR regression and was caught by a human; SKIP-01's shape (green-having-checked-nothing) sits latent in the very workflow VAC-04 joins. Both become machine checks.
Output: two workflow edits + three scripts + two vitest files.

⛔ Constraints: read-only against TEST and PROD, no migrations applied; the repo is PUBLIC — no DSN, host, username, project ref, or PROD body text in any log or artifact (redaction precedent: the sql-tests mutex step); do NOT edit TODOS.md in this plan (closure of SKIP-01/DRIFT-01/OPS-08-F9 entries is recorded at phase verification, backed by this plan's SUMMARY evidence).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-CONTEXT.md
@.github/workflows/migration-drift-check.yml

Read before implementing: `164.3-RESEARCH.md` §Q5 (VAC-04 job design, redaction, fork-PR carve-out), §Q8 (VAC-08 query design, floor verification), and `164.3-PATTERNS.md` §4 and §7 (secrets discipline, what NOT to copy from the existing skip step, the sql-tests DSN/fork-guard idiom).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared SQL-body normalizer (one implementation, two callers)</name>
  <files>scripts/sql-body-normalize.mjs, src/__tests__/sql-body-normalize.test.ts</files>
  <behavior>
    - Strips `--` line comments to end-of-line (the idiom already proven in the gate file at the line reading `regexp_replace(pg_get_functiondef(p.oid), '--[^\n]*', '', 'g')` — port it to JS).
    - Strips `/* ... */` block comments non-greedily, including multiline.
    - Collapses every whitespace run to a single space and trims — so formatting-only differences vanish.
    - D-05's measured case as a fixture: a function body whose ONLY `INTO STRICT` occurrence sits inside a `--` comment normalizes to output in which that token does not appear (PROD's 7-param `_enqueue_compute_job_internal` is exactly this shape).
    - Known limitation documented in the header, not hidden: `--` inside string literals is also stripped — acceptable because the SAME normalization is applied to both sides of every diff.
  </behavior>
  <action>
    Write `scripts/sql-body-normalize.mjs` (Node 22-compatible, `node:fs` reads, no dependencies): reads a file argument or stdin, writes the normalized text to stdout, exit 0. Also export the normalize function for direct import by the vitest test. Follow the repo's script conventions from `scripts/dump-sql-functions.ts` (WHY-first header comment stating the defect class — D-05, mechanism 2 — the scope boundary, and the exact invocation). Write the vitest tests FIRST from the behavior list, watch them fail, then implement.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/sql-body-normalize.test.ts</automated>
  </verify>
  <done>All behavior cases pass, including the D-05 comment-only-token fixture; the module is imported (not reimplemented) by both drift scripts in Tasks 2 and 3.</done>
</task>

<task type="auto">
  <name>Task 2: VAC-04 — PROD body gate as a step in migration-drift-check.yml, and D-17's skip made honest</name>
  <files>scripts/prod-body-drift-check.sh, .github/workflows/migration-drift-check.yml, src/__tests__/drift-check-scripts.test.ts</files>
  <action>
    Implement `scripts/prod-body-drift-check.sh` (bash, `set -euo pipefail`) so the gate's logic is testable without PROD. Contract:
    - FIRST and unconditionally: assert the required credentials env vars are non-empty; if any is missing, print an explicit `::error::` naming the gate and why a skip is forbidden, and `exit 1` (D-11's reasoning survives D-12: never skip, never exit 0 while unconfigured).
    - Inputs: the merge-base list of migration files this PR adds/changes (reuse the diff idiom already in the workflow's dry-run step); the committed snapshot dir `supabase/schema/functions/` (118 bodies — VERIFIED CORRECTION 4: this IS the canonical left-hand side per D-12); a PROD body fetcher command injectable via env `BODY_FETCH_CMD` (tests stub it; the workflow sets the real one).
    - For each `CREATE OR REPLACE FUNCTION public.<name>` in the PR's changed migrations: fetch PROD's current definition through the fetcher; normalize BOTH the snapshot body and the PROD body via `scripts/sql-body-normalize.mjs` (D-05); if the function is absent in PROD, pass with a note (new function); if PROD is present but the snapshot lacks the file, exit 1 (stale snapshot — the snapshot workflow should have caught it, fail loud); if normalized bodies are equal, pass; if they differ, exit 1 UNLESS the PR's migration carries an acknowledgment pragma comment `-- prod-body-ack: <sha256>` whose value equals the sha256 of the NORMALIZED PROD body — the hash binds the ack to the exact PROD content, forcing the author to have looked (DRIFT-02b's claim-vs-thing requirement).
    - Output redaction (public repo, hard rule): print ONLY function names, sha256 hashes of both normalized sides, and hunk counts. NEVER print PROD body text, a DSN, host, username, or project ref.
    Real fetcher in the workflow: after the existing `supabase link`, use the CLI's linked connection to dump/read function definitions (a schema-only `supabase db dump --linked` written to a workspace file that is never echoed, from which the script extracts the target function's body, is the proven-connectivity path — the job already links with these exact secrets). A directly-constructed psql DSN is acceptable only if the dump path fails, and must never be echoed.

    Workflow edits (see 164.3-PATTERNS.md §4 for the secrets/env discipline to copy and the skip semantics NOT to copy):
    1. Add the VAC-04 step AFTER the existing dry-run step. It must NOT carry the `configured == 'true'` condition (D-17a) — its own first act is the credential check above. Secrets via `env:` blocks only.
    2. D-17b — convert the existing configured=false skip into a hard failure, but ONLY after resolving fork behavior: GitHub withholds secrets and environment secrets from fork-PR `pull_request` runs, so an unconditional hard fail would redden every fork PR for a reason no contributor can fix. Mirror the same-repo carve-out precedent already used by the sql-tests job's `if:` expression (push OR same-repo PR): gate the whole `check` job with a job-level same-repo condition, so fork PRs show a visibly SKIPPED job (an honest state — distinct from green-having-checked-nothing), and same-repo runs with missing secrets now `exit 1` instead of emitting the `::notice::` skip. Preserve the existing dry-run step's own behavior otherwise; add a comment contrasting the old skip with why it is now a failure (SKIP-01's shape, D-17). Record in the SUMMARY the measured fact you verified fork behavior against (the sql-tests `if:` precedent plus GitHub's documented fork-secret withholding).
    3. Since this workflow is `pull_request`-triggered with a `supabase/migrations/**` paths filter, D-13 is satisfied by construction — state in a comment WHY it must never become main-push (Railway skips analytics deploys when main CI is red). Keep the workflow file itself in the paths filter (already present) so edits to the gate re-run it.

    Prove the gate can FAIL (founder rule, machine-checked): in `src/__tests__/drift-check-scripts.test.ts`, drive `scripts/prod-body-drift-check.sh` with a stub `BODY_FETCH_CMD` through four cases — missing credential → exit 1; body mismatch without ack → exit 1; mismatch with correct-hash ack → exit 0 with warning; exact match after normalization (bodies differing only by comments) → exit 0.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/drift-check-scripts.test.ts</automated>
    Plus: `actionlint .github/workflows/migration-drift-check.yml` if actionlint is available locally; otherwise verify YAML parses via `node -e "require('js-yaml')..."` or `python3 -c "import yaml,sys; yaml.safe_load(open(sys.argv[1]))" .github/workflows/migration-drift-check.yml`.
  </verify>
  <done>The step is wired without the configured-inherit condition; the four red/green stub cases pass; the old skip is a hard failure for same-repo runs and a visible job-skip for forks; no PROD-derived text can reach the log (grep the script for echo/cat of the dump file — only hashes and counts are printed). The first real-PROD execution happens on the next migrations PR; note this residual in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: VAC-08 — ledger drift check joining on name, body assertion, and OPS-08-F9 verify-and-record</name>
  <files>scripts/test-ledger-drift-check.sh, .github/workflows/ci.yml, src/__tests__/drift-check-scripts.test.ts</files>
  <action>
    Implement `scripts/test-ledger-drift-check.sh` with two halves and a `--self-test`:
    1. Ledger presence (SKIP-01 + DRIFT-01's landing spot): build the repo-side list as `basename <file> .sql` for every file in `supabase/migrations/`; run the single-round-trip EXISTS query from 164.3-RESEARCH.md §Q8 against `$TEST_SUPABASE_DB_URL` — repo names with no `schema_migrations` row whose `name` matches. Zero rows = green; any row = exit 1 listing the missing names. The join key is `name` because the ledger re-stamps the `version` column at apply time (VERIFIED CORRECTION 1 — a version-join reported 12/12 recent migrations missing when all 12 were present; that check would itself be a vacuous control). `EXISTS` makes a twice-applied migration match once — double-apply tolerated by construction. Reverse direction (ledger rows with no repo file) is printed as advisory only, never a failure (squashes and CLI-era rows make it noisy).
    2. Body-level pairing (presence in the ledger is not evidence the deployed body matches): for `_enqueue_compute_job_internal` (every overload), `sanitize_user`, and `mark_compute_job_done` — the DRIFT-named trio — fetch TEST's live `pg_get_functiondef`, normalize via `scripts/sql-body-normalize.mjs`, compare against the normalized committed snapshot in `supabase/schema/functions/`; mismatch → exit 1 with function name + both hashes only.
    3. `--self-test` with injectable inputs (stub ledger list and stub body pairs; no DB needed): prove BOTH red modes fire — a fabricated repo filename absent from the stub ledger → exit 1; a stub body mismatch → exit 1; and the green path → exit 0. Add these cases to `src/__tests__/drift-check-scripts.test.ts`.

    CI wiring: add a step inside the `sql-tests` job in `.github/workflows/ci.yml`, AFTER the mutex-acquisition step, using the job's existing `TEST_SUPABASE_DB_URL` env pattern including its `if [ -z ... ]` fork-skip guard (this step inherits sql-tests' already-documented tolerated skip surface for fork PRs — it does not invent a new one; say so in a comment). Copy the job's never-echo-the-DSN discipline. Plain SELECTs — the session-mode DSN rewrite is not needed; note that in the comment.

    OPS-08-F9 (verify-and-record ONLY — VERIFIED CORRECTION 3): assert at HEAD that ci.yml still contains the lines `SENTINEL_FLOOR=8` and `ARMS_FLOOR=166` exactly (the raise the TODOS entry prescribes is ALREADY DONE); record the verification in the SUMMARY. Do NOT change the floors.
  </action>
  <verify>
    <automated>bash scripts/test-ledger-drift-check.sh --self-test && npx vitest run src/__tests__/drift-check-scripts.test.ts && grep -a "SENTINEL_FLOOR=8" .github/workflows/ci.yml && grep -a "ARMS_FLOOR=166" .github/workflows/ci.yml</automated>
  </verify>
  <done>Self-test proves both red modes and green; the step is wired inside sql-tests after the mutex with the existing DSN/fork-guard idiom; floors verified 8/166 and recorded; the live-TEST execution happens on the next CI run of this branch (expected green per VERIFIED CORRECTION 1's object-level probes — if it reds, that is a real finding, not a bug in this plan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI job → PROD Postgres | VAC-04 reads live function bodies with the job's existing Production-environment secrets |
| CI job → TEST Postgres | VAC-08 runs read-only SELECTs with the existing sql-tests secret |
| CI logs → public internet | repo is public; anything printed is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-04 | Information Disclosure | VAC-04 step output | critical | mitigate | hashes/hunk-counts/names only; PROD dump written to workspace file, never echoed; no DSN/ref in any log (mutex-step redaction precedent) |
| T-164.3-05 | Elevation of Privilege | reusing the drift-check credential | medium | accept | D-12: adding a step to a job that already holds the secret widens nothing; `[VAC-04-ROLE]` least-privilege swap stays booked in TODOS.md — do not add it here |
| T-164.3-06 | Repudiation | ack pragma waving drift through | medium | mitigate | ack hash binds to the sha256 of the normalized PROD body — a stale or blind ack fails |
| T-164.3-07 | Tampering | fork PR triggering secret-bearing steps | medium | mitigate | job-level same-repo condition (sql-tests precedent); GitHub withholds fork secrets regardless |
| T-164.3-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed by this plan |
</threat_model>

<verification>
- Both scripts' stub-driven red cases observed RED then restored GREEN (vitest output).
- Workflow YAML parses; the new VAC-04 step has no `configured` condition (inspect the step block).
- No secret material, project ref, or body text can be printed by either script (review every echo/printf).
</verification>

<success_criteria>
- VAC-04: unskippable, PR-triggered, snapshot-LHS PROD body gate with proof it can fail (VAC-04, D-12, D-13, D-17, D-05).
- VAC-08: name-join ledger check + body assertion with proof both red modes fire (SKIP-01, DRIFT-01 absorbed).
- OPS-08-F9 verified at HEAD and recorded, no raise planned.
</success_criteria>

<output>
Create `.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-02-SUMMARY.md` when done. Record: the fetcher mechanism chosen for VAC-04 (dump-path vs psql fallback and why), the fork-behavior facts verified for D-17, the floors verification, and the two residuals (first real-PROD execution pends the next migrations PR; first real-TEST execution pends the next CI run).
</output>
