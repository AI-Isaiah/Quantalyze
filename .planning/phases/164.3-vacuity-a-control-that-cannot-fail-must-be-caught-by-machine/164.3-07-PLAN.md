---
phase: 164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine
plan: 07
type: execute
subsystem: e2e-local-stack
tags: [csv-finalize, cas-race, playwright, phase-159-closure]
wave: 2
depends_on: ["164.3-04"]
files_modified:
  - e2e/csv-finalize-race.spec.ts
  - e2e/helpers/seed-test-project.ts
  - .github/workflows/e2e-local-stack.yml
autonomous: true
requirements: [VAC-07]
provides:
  - "Phase 159's two blocked items closed as ONE spec on the local-stack lane"
  - "Proof the disposable lane serves a REAL app consumer, not just the gate corpus (D-08)"
affects: [phase-159-verification, phase-161-item-3-future]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Two concurrent csv-finalize POSTs carrying one never-classified wizard_session_id yield exactly one 2xx applied receipt and one honest raced refusal, and category_id holds the winner's value (VAC-07, D-08)"
    - "The spec runs against the Supabase CLI local stack — never shared TEST, never PROD (D-15)"
    - "Zero production code changes (D-08 bound)"
    - "The spec was observed RED under a neutered expectation before being accepted (founder rule)"
  artifacts:
    - path: "e2e/csv-finalize-race.spec.ts"
      provides: "the concurrent-CAS race spec with a header explaining the concurrency IS the subject"
    - path: "e2e/helpers/seed-test-project.ts"
      provides: "extended (not forked) with a validated-but-never-finalized wizard session seeder"
    - path: ".github/workflows/e2e-local-stack.yml"
      provides: "standalone workflow running the lane + the spec with the byte-identical local invocation"
  key_links:
    - from: "e2e/csv-finalize-race.spec.ts"
      to: "src/app/api/strategies/csv-finalize/route.ts"
      via: "two page.request.post calls in Promise.all against the CAS update guarded by is(category_id, null)"
      pattern: "csv-finalize"
    - from: ".github/workflows/e2e-local-stack.yml"
      to: "scripts/local-stack/run.sh"
      via: "workflow brings the stack up via the plan-04 lane before Playwright runs"
      pattern: "local-stack/run.sh"
---

<objective>
Close Phase 159's two blocked items (the concurrent-CAS race and the 409 remedy) as ONE Playwright spec on the plan-04 local-stack lane (VAC-07, D-08): two concurrent `csv-finalize` POSTs on one never-classified `wizard_session_id`; assert exactly one 2xx applied receipt, one honest raced refusal, and that `category_id` holds the winner. A browser structurally cannot produce this (re-entering the wizard mints a fresh session — observed live), and TEST must not be raced — which is exactly why the lane exists.

Purpose: proves the lane against a REAL consumer instead of only the gate corpus — a lane that can run SQL files is not automatically one that can drive concurrent HTTP POSTs against an app talking to it (D-08's stated reason for doing this HERE).
Output: one spec, one helper extension, one standalone workflow. Bounded: NO production code changes.

⛔ Constraints: never `uvicorn main:app`; env points at the local stack only; the seed helper's production-URL refusal guard must remain effective (localhost passes it); no TEST/PROD writes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-CONTEXT.md
@.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-04-SUMMARY.md

Read before implementing: `164.3-RESEARCH.md` §Q7 IN FULL (why Playwright not vitest; the verified CAS shape — `update(...).eq("id").eq("user_id").is("category_id", null).select("id")` with zero-rows-no-error meaning raced and the 409 refusal path; the `page.request` + `Promise.all` pattern; the seeding seam), `164.3-PATTERNS.md` §6 (the authenticated `page.request` precedent, the REMOVED-Promise.all seeding precedent and why it does not apply here, the seed-helper conventions to extend not fork), and the two `blocked:` entries in `.planning/phases/159-rank-public-ranking-integrity/159-VERIFICATION.md` — they carry the closing recipe verbatim and this spec implements it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed seam + the race spec</name>
  <precondition>scripts/local-stack/run.sh up succeeds and its MODE decision is recorded in scripts/local-stack/REPLAY-SPIKE.md (plan 04 complete)</precondition>
  <files>e2e/helpers/seed-test-project.ts, e2e/csv-finalize-race.spec.ts</files>
  <action>
    Extend `e2e/helpers/seed-test-project.ts` (do NOT fork it) with an exported seeder that creates a strategy row in a VALIDATED-but-never-finalized wizard state: `wizard_session_id` set, `category_id` NULL, owned by a seeded user — following the existing exported-function shape (module load stays side-effect-free; env asserted at call time; the production-URL refusal guard untouched and passing for localhost). Read the existing wizard-draft seeder before writing — reuse its row shape and extend only what the finalize route requires to reach the CAS.
    Write `e2e/csv-finalize-race.spec.ts`: sign in via the e2e layer's established auth flow; fire the two finalize POSTs for the SAME `wizard_session_id` with `await Promise.all([page.request.post(...), page.request.post(...)])` so both are in flight simultaneously — the race is adjudicated in Postgres, which is the point. Assert: exactly one response is 2xx with the applied receipt, exactly one is the honest raced refusal (409 with the raced shape — read the route's refusal payload before pinning fields; assert content-by-field, not just status), and a service-role read of the strategy row shows `category_id` equal to the winner's submitted value. Spec header comment: state that the concurrency IS the subject — a previous Promise.all elsewhere in e2e was deliberately serialized because GoTrue admin SEEDING races; that fix must not be "applied" here (PATTERNS §6 records the distinction).
    Anti-vacuity proof before acceptance: seed a session that is ALREADY finalized and run the spec expecting the standard assertions — observe RED (no 2xx winner exists), restore the correct never-classified seed, observe GREEN. Record the observed RED in the commit message body.
  </action>
  <verify>
    <automated>bash scripts/local-stack/run.sh up && npx playwright test e2e/csv-finalize-race.spec.ts; RC=$?; bash scripts/local-stack/run.sh down; exit $RC</automated>
  </verify>
  <done>Spec green on the local stack; the RED-under-neutered-seed run observed and recorded; exactly one 2xx + one honest refusal + winner's category_id asserted content-by-field; zero production files changed.</done>
</task>

<task type="auto">
  <name>Task 2: Standalone CI workflow for the local-stack spec</name>
  <files>.github/workflows/e2e-local-stack.yml</files>
  <action>
    Create `.github/workflows/e2e-local-stack.yml` as its own workflow (NOT a ci.yml job — keeps this plan file-disjoint from the wave's ci.yml owner, and the existing standalone workflows are the precedent): SHA-pinned checkout (persist-credentials false), SHA-pinned setup-node 22, node_modules cache + npm ci retry, the SHA-pinned supabase setup-cli action at the version already pinned in migration-drift-check.yml, Docker is preinstalled on the runner. Steps run the BYTE-IDENTICAL local sequence from Task 1's verify line (lane up → playwright test → lane down) — no wrapper, no mode drift (the measured wrapped-vs-direct lesson). Trigger: pull_request + push on main, `permissions: contents: read`, concurrency group per ref. No secrets — the stack generates its own keys; a skipped or red run is therefore always a real signal; state that in the header comment. It must NOT touch the TEST mutex or any shared resource — state that too.
    Note in the workflow header that Phase 161's item 3 is a known future consumer of this lane (recorded, not implemented — it is founder-gated).
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/e2e-local-stack.yml'))"</automated>
    The live proof is the workflow's first run on this branch's PR — record its conclusion in the SUMMARY.
  </verify>
  <done>Workflow exists, hermetic scaffold + verbatim invocation, no secrets, no shared resources; first-run result recorded (or noted pending if the PR is not yet open).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec → local stack HTTP | authenticated requests against a throwaway PostgREST/GoTrue; keys are generated per-run |
| workflow → Docker | vendor containers on the runner, torn down by the lane trap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3-17 | Tampering | spec accidentally pointed at shared TEST | high | mitigate | env comes only from the lane's handoff file; seed helper's production-URL refusal stays active |
| T-164.3-18 | Spoofing | seeded users/sessions leaking realistic identifiers | low | mitigate | synthetic UUIDs only; stack is destroyed after the run |
| T-164.3-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed by this plan |
</threat_model>

<verification>
- Spec green on the local stack locally; RED-under-neutered-seed observed.
- `git diff --stat` shows only the three plan files — no production code (D-08 bound is machine-checkable here).
- Workflow YAML parses; invocation matches the local sequence byte-for-byte.
</verification>

<success_criteria>
- VAC-07 delivered: Phase 159's two blocked items close on evidence from this spec (record the mapping for 159-VERIFICATION.md in the SUMMARY — the closure edit itself belongs to phase verification, not this plan).
- D-08's real purpose served: the lane demonstrably drives a real app consumer.
</success_criteria>

<output>
Create `.planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-07-SUMMARY.md` when done. Record: the observed one-2xx/one-409 transcript, the winner-holds assertion evidence, the RED proof, and the exact evidence lines 159-VERIFICATION.md's two blocked entries need for closure.
</output>
