---
phase: 164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/mt5_validation.py
  - analytics-service/tests/test_mt5_validate.py
  - analytics-service/tests/test_job_worker.py
  - analytics-service/tests/test_mt5_client_contract.py
  - analytics-service/tests/test_mt5_derive_branch.py
  - analytics-service/tests/test_mt5_validate_parity.py
  - analytics-service/tests/fixtures/mt5_login_rejection_observations.json
  - analytics-service/services/mt5_client.py
  - analytics-service/services/mt5_probe.py
  - analytics-service/services/mt5_concurrency.py
  - analytics-service/services/job_worker.py
  - analytics-service/services/ingestion/mt5.py
  - analytics-service/tests/test_ingestion_mt5.py
autonomous: true
requirements: ["164.5.4-D2"]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A login rejection whose text the classifier does not RECOGNISE returns `transient`, never `auth` and never `wrong_server` — so no unrecognised broker message can produce a user-attributed permanent stamp (D-02 refusal rule)."
    - "A message matching BOTH phrase tables returns `wrong_server` — the fail-CLOSED precedence is pinned by an executing test, not by a comment (D-02)."
    - "The `Mt5ClientError` string WRAPPER (`MT5 client error (code=N): ...`) matches no phrase in either table, so the wrapper alone can never decide a class."
    - "The four production-copy token-freedom gates assert the REAL property — that the copy classifies `transient` through `classify_mt5_login_error` itself — so they cannot go vacuous when the table changes shape."
    - "The IPC transport code-gate still runs BEFORE any text matching and keeps its own arm."
  artifacts:
    - analytics-service/services/mt5_validation.py
    - analytics-service/tests/test_mt5_validate.py
    - analytics-service/tests/fixtures/mt5_login_rejection_observations.json
  key_links:
    - "`classify_mt5_login_error` is the ONE seam; its three production callers (`routers/exchange.py` validate_key MT5 arm, `services/ingestion/mt5.py` Mt5ClientError handler, `services/job_worker.py` MT5 derive branch) are UNCHANGED by this plan."
    - "`tests/test_job_worker.py`, `tests/test_mt5_client_contract.py`, `tests/test_mt5_derive_branch.py`, `tests/test_mt5_validate_parity.py` import the tables BY NAME — the rename is what makes that census mechanical instead of grep-shaped."
---

<objective>
Close defect (2) of the ROADMAP entry (`164.5.4-D2`): `classify_mt5_login_error` stamps a
PERMANENT, CONFIDENT, WRONG verdict on a working credential because its `[ASSUMED]` token tables
OVER-match.

Purpose: a wrong user-blame stamp sends the founder to change a password that was fine while the
real cause goes uninvestigated. This plan makes the classifier REFUSE to guess rather than default
to blame — per D-02, keeping the three-member `Literal` and adding a refusal rule.

Output: anchored-phrase tables replacing the bare single-word tokens, an executable fail-CLOSED
precedence pin, the four production-copy gates re-pointed at the real property, and the live-spike
hand-off landed as an empty-but-live synthetic fixture table.

⚠️ `requirements: ["164.5.4-D2"]` is a PHASE-SCOPED reference to the ROADMAP entry's own numbered
defect (2). It is NOT a `.planning/REQUIREMENTS.md` ID — this phase has none (`**Requirements**:
TBD`, confirmed by grep at HEAD).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-CONTEXT.md
@.planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-RESEARCH.md
@.planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-PATTERNS.md
@analytics-service/services/mt5_validation.py
</context>

<planner_findings>
## MEASURED AT HEAD BY THE PLANNER — read before writing code

**1. `str(err)` is a WRAPPER, and the phrases must survive it.**
`Mt5ClientError.__init__` builds `f"MT5 client error (code={code}): {scrub_freeform_string(detail)}"`.
`classify_mt5_login_error` lowercases THAT whole string. So every phrase must match inside the
wrapper, and the wrapper's own words must match nothing. Measured: today's wrapper is neutral
against both tables — preserve that, and pin it.

**2. The observed-string corpus in this repo (the ONLY honest phrase source — Pitfall 5: no
authoritative MT5 error-text table exists).** Measured by grep over `analytics-service/` at HEAD:

| construction | today | REQUIRED after this plan |
|---|---|---|
| `Mt5ClientError(-10004, "No IPC connection")` | transient (code-gate) | transient — UNCHANGED |
| `Mt5ClientError(-10005, "IPC timeout")` | transient (code-gate) | transient — UNCHANGED |
| `Mt5ClientError(0, "trade server not found")` | wrong_server | wrong_server |
| `Mt5ClientError(134, "invalid account or password")` | auth | auth |
| `Mt5ClientError(0, "timeout waiting for response")` | transient | transient |
| `Mt5ClientError(0, "MT5 returned a non-namedtuple/degenerate shape")` | transient | transient |
| `Mt5ClientError(10001, "unattended login timed out")` | **auth** ← OVER-MATCH | **transient** |
| `Mt5ClientError(5, "terminal pipe broke")` | **wrong_server** ← OVER-MATCH | **transient** |
| `Mt5ClientError(0, "Invalid account")` (the ROADMAP's own case) | **auth** ← OVER-MATCH | **transient** |

**3. ⛔ THE CENSUS TRAP FOR THIS PLAN, AND IT IS A SILENT-VACUITY REGRESSION.**
FOUR test files import `_WRONG_SERVER_TOKENS` / `_AUTH_TOKENS` BY NAME and iterate them, asserting
that production operator copy contains NO member:

- `analytics-service/tests/test_job_worker.py` (the `Mt5GatewayMisconfigured` copy sweep)
- `analytics-service/tests/test_mt5_client_contract.py` (the fenced stage names)
- `analytics-service/tests/test_mt5_derive_branch.py` (the `compute_jobs.error_message` sweep)
- `analytics-service/tests/test_mt5_validate_parity.py`

Those gates work today ONLY because the members are short common words. Replace them with long
anchored phrases and every one of them becomes **trivially green while measuring nothing** — the
exact "a test that cannot fail" class this milestone exists to remove, introduced BY this phase's
own fix. ⛔ Do not ship the table change without task 2's re-pointing in the SAME commit.

**4. Why the RENAME is load-bearing.** Renaming `_WRONG_SERVER_TOKENS` → `_WRONG_SERVER_PHRASES`
and `_AUTH_TOKENS` → `_AUTH_PHRASES` turns the four-file census from a GREP (which this repo has a
dated record of failing) into an ImportError — loud, immediate, mechanical. That is the whole
reason to rename; the name being more accurate is a bonus.

**5. Prose that becomes FALSE the moment the tables change, and WHICH TASK OWNS EACH FILE.**
A stale comment asserting the opposite of the code is the drift class this repo keeps booking. ⛔ The
rename's ImportError does NOT catch any of these: a docstring cannot fail. Every file below is
instructed inside the owning task's own `<action>` — an executor reading only `<action>` blocks, as
the workflow tells it to, reaches all of them.

| file | prose hits NAMING an old tuple | owner |
|---|---|---|
| `tests/test_mt5_derive_branch.py` | 2 (of 4 hits; 2 are imports/iteration) | task 1 — it already opens the file for the ImportError |
| `tests/test_mt5_validate_parity.py` | 3 (of 6 hits; 3 are executable) | task 1 — same |
| `tests/test_mt5_validate.py` | 4 (all 4 hits) | **task 2** — does NOT import the tables, so NOTHING here fails loudly |
| `tests/test_ingestion_mt5.py` | 1 | **task 3** — likewise no import, and no earlier task opens it |
| `services/mt5_probe.py` | 3 | task 3 |
| `services/job_worker.py` | 2 | task 3 |
| `services/mt5_client.py` | 1 | task 3 |

⚠️ `tests/test_job_worker.py` and `tests/test_mt5_client_contract.py` carry EXECUTABLE references
only — zero prose — so the ImportError alone closes them.

⛔ **THE OLD-NAME GREP IS NECESSARY, NOT SUFFICIENT, AND HERE IS THE PROOF.**
`services/ingestion/mt5.py` scores ZERO on it, yet carries a comment that reasons from the seam's
membership in words — naming the bare terminal/server words directly without ever naming a tuple.
`services/mt5_concurrency.py` scores zero too and carries prose about when the seam produces a
PERMANENT user-attributed stamp. Both are task 3's, and both are why task 3's census greps
`classify_mt5_login_error` as well as the tuple names. A file returning no old-name hits is NOT
thereby clean.

**6. OUT OF SCOPE, surfaced not smuggled.** `services/mt5_relogin.py` single-sources
`-6` as "no account is authorized on this terminal" — an INFRASTRUCTURE fault with a shipped heal,
NOT a credential rejection — and `classify_mt5_login_error` has no code-gate for it. Whether a
`-6` becomes `auth` today depends entirely on unmeasured broker text passing through a table that
contains an authorization-shaped token. ⛔ Do NOT add a `-6` code-gate in this plan: it is in no
source artifact, and inventing a code table from memory is Pitfall 5 exactly. The anchored-phrase
rewrite must make a terminal-authorization message degrade to `transient` by the LOCKED mechanism.
Report the residual in the SUMMARY.
</planner_findings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the over-matching token tables with anchored phrases, and re-point the four copy gates onto the classifier itself</name>
  <files>analytics-service/services/mt5_validation.py, analytics-service/tests/test_job_worker.py, analytics-service/tests/test_mt5_client_contract.py, analytics-service/tests/test_mt5_derive_branch.py, analytics-service/tests/test_mt5_validate_parity.py</files>
  <read_first>
    - `analytics-service/services/mt5_validation.py` — the whole module: `_IPC_TRANSPORT_CODES`, both token tuples, and `classify_mt5_login_error`'s body.
    - `analytics-service/services/mt5_client.py`, symbol `Mt5ClientError.__init__` — the message wrapper the classifier lowercases.
    - The four gate bodies named in `<planner_findings>` item 3. Read each gate's own docstring: every one of them STATES that the property it wants is "if it is ever fed to `classify_mt5_login_error`, a working key gets blamed". That sentence is the re-pointing target.
  </read_first>
  <behavior>
    - `classify_mt5_login_error(Mt5ClientError(0, "Invalid account"))` returns `"transient"` (today: `"auth"`).
    - `classify_mt5_login_error(Mt5ClientError(10001, "unattended login timed out"))` returns `"transient"` (today: `"auth"`).
    - `classify_mt5_login_error(Mt5ClientError(5, "terminal pipe broke"))` returns `"transient"` (today: `"wrong_server"`).
    - `classify_mt5_login_error(Mt5ClientError(134, "invalid account or password"))` returns `"auth"` — unchanged.
    - `classify_mt5_login_error(Mt5ClientError(0, "trade server not found"))` returns `"wrong_server"` — unchanged.
    - `classify_mt5_login_error(Mt5ClientError(-10004, "No IPC connection"))` and `(-10005, "IPC timeout")` return `"transient"` via the code-gate — unchanged, and the code-gate still runs first.
    - Every message each of the four gates sweeps classifies `"transient"`.
  </behavior>
  <action>
Rename `_WRONG_SERVER_TOKENS` to `_WRONG_SERVER_PHRASES` and `_AUTH_TOKENS` to `_AUTH_PHRASES`, and
REPLACE their contents with anchored phrases rather than extending them (D-02: adding members makes
over-matching worse). Keep both as `tuple[str, ...]`, keep the `[ASSUMED] pending the live spike`
marker convention each already carries, and keep `classify_mt5_login_error`'s ordering exactly as it
is: the `_IPC_TRANSPORT_CODES` code-gate first, then wrong-server, then auth, then the `transient`
default.

Choose the phrase set so the nine-row verdict table in `<planner_findings>` item 2 holds exactly.
Draw every phrase from strings already present in this repo's own corpus — Pitfall 5 records that
no authoritative MT5 error-text table exists, so a phrase invented from memory is a fabrication.
A workable starting set, which you must VALIDATE against the table rather than adopt on faith:
wrong-server phrases anchored on the broker-server-lookup failure the corpus already shows
(`trade server not found`, and the narrower `server not found` only if a corpus string needs it);
auth phrases anchored on a credential rejection naming BOTH halves of the credential pair as the
corpus string does, plus explicit password-rejection wordings. The single-word members that carry
the defect — the bare account word and the bare truth-value-negation word in the auth tuple, and
the bare bridge/terminal words in the wrong-server tuple — must not survive as standalone members
in any form.

Update `classify_mt5_login_error`'s docstring to state the REFUSAL rule in its own words: an
unrecognised or ambiguous message degrades to `transient`, which the caller propagates untouched,
and is NEVER a user-attributed permanent stamp. Record in the docstring that the phrases are
anchored precisely so a bridge/terminal fault and a broker-server fault cannot be confused, citing
the 2026-08-12 wedged-gateway incident the module already records.

Then, in the SAME commit, re-point the FOUR gates. Each one currently iterates the live tuples and
asserts substring-absence from production copy. Replace that assertion in each with the property the
gate's own docstring already names: feed each swept message into `classify_mt5_login_error` inside an
`Mt5ClientError` and assert the result is `"transient"`. Keep each gate's own message corpus, its own
non-vacuity floor (the length check, the "the tables must be non-empty" guard rewritten to assert the
PHRASE tuples are non-empty), and its own failure message — only the assertion changes. This is
strictly stronger than substring-absence: it is the real property, and it cannot go vacuous when the
table changes shape again.

⛔ Do NOT keep a substring-absence assertion beside the new one "for belt and braces" — a
trivially-green assertion sitting next to a real one is what makes a suite unreadable about its own
power.

⚠️ IN THE SAME COMMIT, sweep the PROSE of the four gate files you are already opening. The rename
hands you an ImportError at each gate's `from services.mt5_validation import ...` line, which is
loud and mechanical — but these files ALSO reason about the old membership in docstrings and
comments, where NOTHING fails and the stale claim survives silently forever. Run
`grep -aEn '_(AUTH|WRONG_SERVER)_TOKENS' analytics-service/tests/test_job_worker.py analytics-service/tests/test_mt5_client_contract.py analytics-service/tests/test_mt5_derive_branch.py analytics-service/tests/test_mt5_validate_parity.py`
and correct every NON-executable hit. Measured at HEAD, those hits are: in
`tests/test_mt5_derive_branch.py`, the docstrings of
`test_classify_no_ipc_connection_is_transient_not_wrong_server` (claims the bare bridge word is
matched) and of `test_the_abandoned_derive_error_message_carries_no_classifier_token` (claims the
tables are substring-matched bare words); in `tests/test_mt5_validate_parity.py`, the MODULE
docstring and the docstrings of
`test_run_probe_short_circuits_order_check_on_an_undetermined_terminal` and
`test_order_check_short_circuit_on_both_paths` (each reasons from the bare terminal word being a
member). Rewrite each to name the anchored phrase that replaced the bare word and the refusal rule
that now governs an unrecognised message. ⛔ Correct the PROSE, never an assertion — the assertions
here are this task's own re-pointing, and a docstring is not evidence.
  </action>
  <verify>
    <automated>cd analytics-service && ./.venv/bin/python -m pytest tests/test_mt5_validate.py tests/test_job_worker.py tests/test_mt5_client_contract.py tests/test_mt5_derive_branch.py tests/test_mt5_validate_parity.py tests/test_ingestion_mt5.py -q</automated>
    <fails_when>The run exits non-zero, or reports `0 collected` / `no tests ran` (a collection failure from the rename reads as a clean exit otherwise), or any test id in the output is reported as `F`/`E`.</fails_when>
    <automated>cd analytics-service && ./.venv/bin/python -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <fails_when>Output does not end with `Success: no issues found`, or names `mt5_validation.py` on any `error:` line.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every row of the nine-row verdict table in `<planner_findings>` item 2 holds, proven by an executing assertion (task 2 adds the ones not already present).
    - `grep -aEn '_(AUTH|WRONG_SERVER)_TOKENS' analytics-service/services/mt5_validation.py analytics-service/tests/test_job_worker.py analytics-service/tests/test_mt5_client_contract.py analytics-service/tests/test_mt5_derive_branch.py analytics-service/tests/test_mt5_validate_parity.py` returns no hits — the rename is complete across every file THIS task owns, EXECUTABLE references and prose alike. ⚠️ Scoped deliberately: `tests/test_mt5_validate.py` (task 2) and the production comments plus `tests/test_ingestion_mt5.py` (task 3) still carry the old names at this point, so a repo-wide grep HERE would be unsatisfiable by construction. The repo-wide sweep is task 3's first acceptance criterion, and task 3 runs last.
    - All four re-pointed gates call `classify_mt5_login_error` and assert `"transient"`; none of them retains a substring-absence loop over the phrase tuples.
    - ⛔ ANTI-VACUITY, and it BLOCKS (this phase is USER-FACING): for ONE of the four re-pointed gates, neuter it by substituting a message that DOES classify as `auth`, observe the gate go RED, then restore the file byte-identically and prove it with `cmp` against a `cp` backup taken before the neuter. ⛔ Never `git checkout --` / `git restore` / `git stash`. Record the neuter, the observed RED and the `cmp` result in the SUMMARY.
    - The `_IPC_TRANSPORT_CODES` gate still precedes all text matching and still has its own arm.
  </acceptance_criteria>
  <done>Both phrase tables are anchored, the three over-matching corpus strings now classify `transient`, the two correct ones are unchanged, and the four copy gates assert the classifier's verdict rather than substring absence — with one of them observed RED under a neuter and restored byte-identically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the refusal rule, the fail-CLOSED precedence and the wrapper; land the live-spike hand-off as an empty-but-live synthetic fixture</name>
  <files>analytics-service/tests/test_mt5_validate.py, analytics-service/tests/fixtures/mt5_login_rejection_observations.json</files>
  <read_first>
    - `analytics-service/tests/test_mt5_derive_branch.py`, symbol `test_classify_no_ipc_connection_is_transient_not_wrong_server` — the SHAPE to model both new tests on: construct, classify, assert, with a docstring recording the regression being guarded and a trailing `# pre-fix returned ...` note.
    - `analytics-service/tests/test_mt5_validate.py` — the existing IPC-code parametrize, so the new tests sit beside it in the file's own idiom.
    - `164.5.4-CONTEXT.md` section *The live spike hand-off* — the transport contract this fixture must implement.
  </read_first>
  <behavior>
    - Refusal rule: a message recognised by NEITHER table classifies `transient`. Cover the three corpus strings that regress in `<planner_findings>` item 2 AND a deliberately unrecognisable string.
    - Fail-CLOSED precedence: a message containing a member of BOTH tables classifies `wrong_server`, not `auth`.
    - Wrapper neutrality: `classify_mt5_login_error(Mt5ClientError(0, ""))` — an EMPTY detail, so the lowercased subject is the wrapper and nothing else — classifies `transient`.
    - Live-spike fixture: the file parses; every entry (zero today) carries `code`, `text`, `expected` and a synthetic account label; each entry classifies to its own `expected`.
  </behavior>
  <action>
Add three tests to `analytics-service/tests/test_mt5_validate.py`, each in the file's established
construct-classify-assert shape with a docstring naming the regression it guards:

1. The REFUSAL rule (D-02): parametrize over the strings that must degrade — the ROADMAP's own
   case, the login-timeout case, the broken-bridge case, and one string that resembles nothing —
   and assert `transient` for each. Carry the `# pre-fix returned ...` annotation on the three that
   changed, matching the precedent test's own convention, so the next reader can see this was a
   measured regression and not a preference.
2. The FAIL-CLOSED precedence pin (D-02 requires this to be EXECUTABLE, not a comment): build one
   message that contains a member of BOTH phrase tables and assert `wrong_server` wins. Name in the
   docstring that a server/bridge signal beating an auth signal is the whole reason the ordering
   exists, and that a comment cannot hold it.
3. The WRAPPER neutrality pin: assert that an `Mt5ClientError` with an EMPTY detail classifies
   `transient`. This proves the `MT5 client error (code=N):` envelope the classifier lowercases
   carries no phrase of its own — if a future phrase is ever chosen that collides with the
   envelope, EVERY error classifies the same way and this pin is the only thing that would catch it.

⚠️ IN THE SAME COMMIT AS THOSE THREE TESTS, sweep this file's OWN stale prose — it is this task's
file and no other task edits it. `tests/test_mt5_validate.py` does NOT import the tables, so task 1's
rename raises NO ImportError here: its four references are pure docstring/comment text and every one
of them survives silently, asserting the opposite of the shipped code. Run
`grep -aEn '_(AUTH|WRONG_SERVER)_TOKENS' analytics-service/tests/test_mt5_validate.py`, read every
hit, and correct each. Measured at HEAD they are: the docstring of
`test_ipc_transport_codes_are_transient_never_wrong_server` (claims the swept bridge word is matched,
which is the stated REASON the code-gate must run first); the docstring AND the inline comment inside
`test_a_conclusive_terminal_verdict_is_not_pre_empted_by_an_erroring_probe` (both reason from the
bare terminal word being a member); and the docstring of
`test_terminal_transport_failure_at_materialization_still_refuses` (lists three bare words as carried
members). Rewrite each to name the anchored phrase that replaced the bare word and the refusal rule,
preserving the hazard each one is warning about — the hazard is NARROWER after the rewrite, not gone.

⛔ Correct the PROSE, never the assertions. If the anchored-phrase rewrite changes what one of these
PRE-EXISTING tests actually observes, that is a finding for the SUMMARY and a matter for task 1's
`<verify>` (which already runs this whole file) — never a reason to bend a docstring, and never a
reason to relax an assertion.

Then create `analytics-service/tests/fixtures/mt5_login_rejection_observations.json` — the live-spike
hand-off CONTEXT.md specifies. It is a JSON array, EMPTY on landing. Its header block (a leading
object with a `_protocol` key, since JSON carries no comments) states the capture contract verbatim
from CONTEXT.md: the founder enters credentials and no agent ever does; only `(code, text)` pairs
from login rejections are captured; NEVER an account identifier, NEVER a broker server name, NEVER a
password; accounts are SYNTHETIC labels substituted before anything is written; partial sets LAND,
each pair retiring one `[ASSUMED]` marker independently; nothing in this phase blocks on it.

Consume it from `test_mt5_validate.py` with a `pytest.mark.parametrize` over the entries, asserting
each pair classifies to its declared `expected`. With zero entries today that parametrize collects
zero cases — so add an UNCONDITIONAL meta-test beside it that asserts the file exists, parses, and
carries the `_protocol` header with each required clause present. Without that meta-test the fixture
can be deleted and the parametrize would still "pass" by collecting nothing.

⛔ The repo is PUBLIC and `.planning/` is TRACKED, and this fixture is tracked source: NEVER write a
real account number, a real broker server name, a password, or a captured specimen into it. Verdicts
and synthetic labels only.
  </action>
  <verify>
    <automated>cd analytics-service && ./.venv/bin/python -m pytest tests/test_mt5_validate.py -q</automated>
    <fails_when>The run exits non-zero, or reports `0 collected`, or the refusal / precedence / wrapper / fixture-meta tests are absent from the collected ids (check with `--collect-only -q`).</fails_when>
    <automated>npm run check:planning-hygiene</automated>
    <fails_when>The scan's OK line is absent from the output, or the finding count line reports a non-zero count. ⛔ The count line and the OK line are DIFFERENT lines — read both, never `tail -1`. This scans TRACKED files only, so ALSO hand-scan the new fixture for an account number, a broker server name or a home path before committing.</fails_when>
  </verify>
  <acceptance_criteria>
    - Three new tests exist and pass: refusal, fail-CLOSED precedence, wrapper neutrality.
    - The precedence test genuinely feeds a message matching BOTH tables — prove it by asserting, inside the test, that the message contains a member of each tuple, so the pin cannot go vacuous if a phrase is later reworded.
    - The fixture file exists, is an empty observation array with a `_protocol` header, and is bound by an unconditional meta-test.
    - `grep -aEn '_(AUTH|WRONG_SERVER)_TOKENS' analytics-service/tests/test_mt5_validate.py` returns no hits, and each corrected site still WARNS about its own (now narrower) hazard rather than having been deleted. List the four corrected sites in the SUMMARY by their enclosing test symbol.
    - ⛔ ANTI-VACUITY (BLOCKS): neuter `classify_mt5_login_error`'s `transient` default to return `auth`, observe the refusal test go RED, restore byte-identically and prove with `cmp`. Then neuter the ORDER by moving the auth check above the wrong-server check, observe the precedence test go RED, restore and `cmp`. ⛔ Never `git checkout --`. Record both neuters, both observed REDs and both `cmp` results in the SUMMARY.
    - No credential, account number, broker server name, home path or local username appears anywhere in the new fixture.
  </acceptance_criteria>
  <done>The refusal rule, the fail-CLOSED ordering and the wrapper's neutrality are each held by a test observed to go RED when its subject is neutered; the live-spike hand-off exists as a tracked, empty, protocol-bearing synthetic fixture that a founder can extend one pair at a time.</done>
</task>

<task type="auto">
  <name>Task 3: Correct every comment the table rewrite makes false</name>
  <files>analytics-service/services/mt5_client.py, analytics-service/services/mt5_probe.py, analytics-service/services/mt5_concurrency.py, analytics-service/services/job_worker.py, analytics-service/services/ingestion/mt5.py, analytics-service/tests/test_ingestion_mt5.py</files>
  <read_first>
    - Re-run the census at HEAD before editing, because it drifts: `grep -rn '_AUTH_PHRASES\|_WRONG_SERVER_PHRASES\|_AUTH_TOKENS\|_WRONG_SERVER_TOKENS\|classify_mt5_login_error' analytics-service/services analytics-service/routers analytics-service/tests`. Read every hit; the ones that MATTER are prose claiming a specific membership.
    - `analytics-service/tests/test_mt5_derive_branch.py` and `analytics-service/tests/test_mt5_validate_parity.py` — task 1 already rewrote their assertions; their surrounding prose may still claim the old membership.
  </read_first>
  <action>
Every comment that names a SPECIFIC member of the old tables, or reasons from one, is now false.
Correct each to describe the anchored-phrase design and the refusal rule. The claims to hunt are of
the form "X's text matches the wrong-server table" or "the tables carry the bridge word" or "the
words an author reaches for are members" — each was TRUE of bare tokens and is FALSE of anchored
phrases.

⚠️ SCOPE: this task owns the PRODUCTION source comments — `services/mt5_client.py`,
`services/mt5_probe.py`, `services/mt5_concurrency.py`, `services/job_worker.py`,
`services/ingestion/mt5.py` — PLUS one test file, named below. Stale prose in the OTHER test files
belongs to whichever earlier task already edits that file: task 1 for the four gate files, task 2 for
`tests/test_mt5_validate.py`. Correct it there rather than reopening those files here, and confirm in
this task's census that nothing was left behind.

⭐ THE ONE TEST FILE THIS TASK OWNS, and it is already in this task's `<files>`:
`analytics-service/tests/test_ingestion_mt5.py`. NO earlier task edits it — it does not import the
tables (so the rename gives no ImportError) and it is not one of the four gate files — so if this
task does not correct it, nothing does. It carries exactly ONE stale site, measured at HEAD: the
comment inside `test_an_abandoned_session_refusal_propagates_out_of_validate_unchanged` explaining
that a re-raise would let the classifier blame the user's broker server for our own abandoned
thread. ⛔ That hazard is STILL REAL after the rewrite — re-scope the comment to name the anchored
phrase table and the refusal rule, do not delete it. Confirm with
`grep -aEn '_(AUTH|WRONG_SERVER)_TOKENS' analytics-service/tests/test_ingestion_mt5.py` returning no
hits.

⛔ Correct the COMMENTS, never the code: the classifier's behaviour is now pinned by task 2's tests,
so a reader who "fixes the code to match a stale comment" would reintroduce the defect. This is the
same posture `[164.6.2-KILLSWITCH-COMMENT-DRIFT]` records for the kill-switch comments.

⚠️ Two of these sites carry a WARNING that is still true in SUBSTANCE and must not be deleted: the
`services/job_worker.py` abandoned-session arm and the `services/mt5_probe.py` note both warn a
future author away from letting classifier-matchable text into a field that gets re-classified
downstream. That hazard survives the rewrite — the matched strings are narrower, not gone. Rewrite
those two to say the hazard is NARROWER and name the new mechanism, rather than deleting them.

⚠️ `analytics-service/tests/fixtures/validate_key_venue_transient_contract.json` was flagged UNKNOWN
by A-04, not confirmed safe. READ it. It is expected to encode the `-10004` CODE-gated contract,
which this plan does not touch — if that is what you find, record the verdict in the SUMMARY and
change nothing. If it encodes a TEXT-token contract instead, it is in this plan's scope and must be
updated with the rest.
  </action>
  <verify>
    <automated>cd analytics-service && ./.venv/bin/python -m pytest tests/ -q -x -k "mt5 or ingestion_mt5 or job_worker"</automated>
    <fails_when>The run exits non-zero, or reports `0 collected` (a `-k` expression that matches nothing is a silent pass otherwise — confirm the collected count is greater than 100).</fails_when>
  </verify>
  <acceptance_criteria>
    - No comment in `analytics-service/` claims a membership the phrase tables no longer have. ⭐ This is now satisfiable BY CONSTRUCTION and it is the phase's final sweep: task 1's `<action>` owns the four gate files' prose, task 2's owns `tests/test_mt5_validate.py`, and this task's owns the production sources plus `tests/test_ingestion_mt5.py` — every file carrying a measured site is instructed inside some task's own `<action>`. Prove it mechanically: `grep -aEn '_(AUTH|WRONG_SERVER)_TOKENS' -r analytics-service/services analytics-service/routers analytics-service/tests` returns no hits. Then re-run the task's census grep on the NEW names and read every hit for prose that reasons from a membership the anchored tables no longer have. List the corrected sites in the SUMMARY.
    - The two hazard warnings named above still exist and now describe the narrower hazard.
    - The venue-transient contract fixture has a RECORDED verdict — touched with a reason, or untouched with a reason. ⛔ "Probably unaffected" is not a verdict.
  </acceptance_criteria>
  <done>Every comment naming the old membership is corrected, the two surviving hazard warnings are re-scoped rather than deleted, and the contract fixture's disposition is recorded rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 broker terminal → `classify_mt5_login_error` | Untrusted, broker-supplied free text (`last_error()`) crosses here and DECIDES a user-facing verdict. |
| `classify_mt5_login_error` → the founder's screen / `compute_jobs.error_message` | A classification becomes a permanent, user-attributed accusation or a silent retry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5.4-01 | Spoofing | Broker-supplied `last_error()` text steering the classifier | high | mitigate | Anchored phrases + refusal rule: an unrecognised or ambiguous message can only reach `transient`, so hostile or merely unexpected text cannot manufacture a permanent user-blaming verdict. Pinned by task 2's refusal test. |
| T-164.5.4-02 | Repudiation | A permanent user-attributed `failed` stamp on OUR fault | high | mitigate | The refusal rule is the mitigation; the fail-CLOSED precedence pin keeps a bridge signal winning over an auth signal so infrastructure faults are never attributed to the user. |
| T-164.5.4-03 | Information Disclosure | Broker free text reaching logs/Sentry unscrubbed | medium | mitigate | UNCHANGED by this plan — `Mt5ClientError.__init__` already routes the detail through `scrub_freeform_string` at construction. Task 1 must not move or bypass that call; the wrapper-neutrality pin exercises the wrapper without relying on its content. |
| T-164.5.4-04 | Information Disclosure | The live-spike fixture publishing a real account or broker server into a PUBLIC repo | high | mitigate | The fixture lands EMPTY with a protocol header forbidding specimens; synthetic labels only; `npm run check:planning-hygiene` plus a hand-scan of the new untracked-until-committed file in task 2's verify. |
| T-164.5.4-05 | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE — this plan installs no packages (RESEARCH.md: "No new external packages"). No Package Legitimacy Audit is required and none is claimed. |
</threat_model>

<verification>
- The nine-row verdict table in `<planner_findings>` item 2 holds, every row by an executing assertion.
- The four re-pointed gates assert the classifier's verdict; one has been observed RED under a neuter and restored byte-identically (`cmp`).
- The refusal rule and the fail-CLOSED ordering have each been observed RED under a neuter of their own subject.
- `cd analytics-service && ./.venv/bin/python -m mypy --strict --follow-imports=silent services/ routers/ models/` ends `Success: no issues found`.
- `npm run check:planning-hygiene` reports zero findings and prints its OK line.
</verification>

<success_criteria>
- `classify_mt5_login_error` returns `transient` for every message it does not recognise, and no unrecognised broker text can produce `auth` or `wrong_server`.
- A message matching both tables returns `wrong_server`, held by a test that is proven able to fail.
- The four production-copy gates measure the real property and cannot be silently vacuated by a future table reshape.
- The live-spike hand-off exists, is safe to extend one measured pair at a time, and blocks nothing.
</success_criteria>

<artifacts_this_phase_produces>
Symbols this plan creates or renames:
- `analytics-service/services/mt5_validation.py`: `_WRONG_SERVER_PHRASES` (renamed from `_WRONG_SERVER_TOKENS`, contents replaced), `_AUTH_PHRASES` (renamed from `_AUTH_TOKENS`, contents replaced).
- `analytics-service/tests/fixtures/mt5_login_rejection_observations.json` (NEW) — the live-spike hand-off table, empty on landing, with a `_protocol` header.
- New tests in `analytics-service/tests/test_mt5_validate.py`: the refusal-rule parametrize, the fail-CLOSED precedence pin, the wrapper-neutrality pin, the live-spike fixture parametrize and its unconditional meta-test.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-01-SUMMARY.md` when done.
</output>
