---
phase: 164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.test.ts
  - src/app/api/keys/[id]/rotate-secret/route.ts
autonomous: true
requirements: ["164.5.4-D3"]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `KEY_UNDECRYPTABLE` wire code arriving at the rotate-secret route resolves to a minted `WizardErrorCode` whose copy says RECONNECT THIS KEY, not to the `UNKNOWN` terminal (D-03 / A-05)."
    - "That envelope is `recoverable: false` and therefore renders NO Retry control — offering a retry for a ciphertext that will never decrypt IS the defect (D-03)."
    - "The minted member is rostered for the rotate-secret dialog, so the dialog surface can actually render the honest card rather than refusing it."
    - "`KEY_UNDECRYPTABLE` holds a verdict row and NO exemption entry — a code cannot honestly sit in both, and a shipped gate asserts exactly that."
    - "Every comment that recorded this as an OPEN residual now records it as CLOSED, at all three sites that carry it."
  artifacts:
    - src/lib/wizardErrors.ts
    - src/lib/wizardErrors.test.ts
    - src/app/api/keys/[id]/rotate-secret/route.ts
  key_links:
    - "`classifyKeyValidationError` reads `VENUE_WIRE_CODE_TO_VERDICT.get(seamCode)` BEFORE its substring cascade, and that cascade has NO decrypt branch — the ROW is the routing mechanism, not an alternative to the mint (A-05)."
    - "`rotate-secret/route.ts` already carries `seamCode` forward (164.5.3 review / WR-01), which is what makes the row reachable at all."
    - "`buildEnvelope`'s `recoverable = copy.actions.some((a) => RECOVERABLE_ACTIONS.has(a))` is what suppresses Retry — the copy's `actions` list is the whole mechanism, no per-route branching."
---

<objective>
Close the ROTATE-SECRET half of defect (3) (`164.5.4-D3`): `KEY_UNDECRYPTABLE` is discarded at the
Python/TypeScript seam, so the founder is told to retry something that can never work.

Purpose: Python already KNOWS the remedy — it raises the code 500, `retryable=False`, detail *"This
stored key could not be decrypted. It must be reconnected."* TypeScript throws that away and renders
"we could not classify this failure" with a Retry button. This plan makes TypeScript carry the remedy
Python already established rather than re-deriving it.

Output: a minted `WizardErrorCode` with no-Retry copy, the `VENUE_WIRE_CODE_TO_VERDICT` row that
routes to it, the exemption entry removed, the dialog roster updated, both `EXPECTED_TABLE_SIZE`
pins moved with their reasoning re-run, and all three residual comments corrected.

⚠️ `requirements: ["164.5.4-D3"]` is a PHASE-SCOPED reference to the ROADMAP entry's own numbered
defect (3). It is NOT a `.planning/REQUIREMENTS.md` ID — this phase has none.

⚠️ This plan closes ONE of the two routes. The PERMISSIONS route is plan 03 and is a different
language, a different file and a different mechanism. ⛔ A plan that touches only `wizardErrors.ts`
leaves half the defect OPEN while reading as complete (A-02).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-CONTEXT.md
@.planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-RESEARCH.md
@.planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-PATTERNS.md
</context>

<planner_findings>
## MEASURED AT HEAD BY THE PLANNER — read before writing code

**1. ⛔ CONTEXT.md's `NOT a VENUE_WIRE_CODE_TO_VERDICT row` bullet is SUPERSEDED by A-05, and the
amendment is right.** Re-measured: `classifyKeyValidationError` resolves `VENUE_WIRE_CODE_TO_VERDICT.get(seamCode)`
BEFORE the substring cascade, and the cascade has no decrypt/reconnect branch. A minted member with
no row is unreachable and routes to `UNKNOWN`. **The row and the mint are not alternatives — the row
is the routing mechanism.** What the ROADMAP's option (a) actually rejected was a row pointing at an
EXISTING dishonest member; a row pointing at the NEWLY MINTED honest member is how option (c) is
delivered.

**2. The disjointness gate EXISTS — A-05's UNKNOWN is RESOLVED.** `src/lib/seam-venue-vocabulary.invariant.test.ts`
carries `it("no code is BOTH mapped and exempt — the two dispositions are exclusive", ...)`. So
adding the verdict row WITHOUT removing the `VENUE_WIRE_CODES_WITHOUT_VERDICT` entry goes RED. Good
news: that half of the census is mechanically enforced. Do not rely on grep for it — run the test.

**3. PATTERNS' `EXPECTED_EMITTED_CODES` claim is CORRECT, and here is why — but VERIFY it, do not
inherit it.** That roster is DERIVED from Python emitter sites and compared member-for-member against
the hand-typed list. `KEY_UNDECRYPTABLE` is already emitted by `decrypt_credentials` failures, so it
is already a member. Minting a TYPESCRIPT `WizardErrorCode` adds no Python emitter, so the derived
population does not move. Prove it by RUNNING `seam-venue-vocabulary.invariant.test.ts`, not by
reading this paragraph.

**4. The `EXPECTED_TABLE_SIZE` pins are NOT a mechanical bump.** Both live in `src/lib/wizardErrors.test.ts`,
both at `93`, guarded by a meta-gate that regex-extracts every `const EXPECTED_TABLE_SIZE = (\d+);`
and asserts exactly TWO exist and agree. But each pin FRONTS A DIFFERENT SCAN and each historical
bump documents the scan's reasoning re-run over the new entry:
  - site 1 fronts the DESTRUCTIVE-ACTION scan (is the new entry inside the scanned population?)
  - site 2 fronts the BANNED-CLAIMS honesty scan (does the new copy carry a forbidden claim?)
⛔ Moving the integers without writing both re-run arguments is the shortcut the comments exist to
prevent. ⛔ `grep` is SILENTLY BLIND to that file (deliberate NUL byte) — always `grep -a`.

**5. THREE comment sites record this as an OPEN residual and all three become FALSE.** All are named
by the ROADMAP or by A-05:
  a. `src/lib/wizardErrors.ts` — the `KEY_UNDECRYPTABLE` entry in `VENUE_WIRE_CODES_WITHOUT_VERDICT`,
     whose stated reason is *"a row in this table cannot close it because that route never calls this
     function"*. The entry is REMOVED, not reworded.
  b. `src/lib/wizardErrors.ts` — the `⚠️ NOT rostered: KEY_UNDECRYPTABLE` note inside
     `DASHBOARD_DIALOG_ROUTE_CODES` for `keys/[id]/rotate-secret`. It gives TWO reasons; the first
     (no verdict row) is closed by this plan. Re-derive the second before rewriting.
  c. `src/app/api/keys/[id]/rotate-secret/route.ts` — the `⚠️ RESIDUAL, not closed by this fix`
     block at the seam-failure throw site.

**6. Rostering the minted member in `DASHBOARD_DIALOG_ROUTE_CODES` is REQUIRED, and there is
precedent for a non-literal member.** The ARRIVAL law in `src/lib/dialog-envelope.invariant.test.ts`
derives codes from the ROUTE FILE's own literals and is ONE-DIRECTIONAL (route → roster), so a roster
member that is not a route literal is admitted — `KEY_RATE_LIMIT`, `SEAM_INTERNAL_FAULT`,
`KEY_AUTH_FAILED`, `KEY_MT5_MASTER_PASSWORD` and `KEY_MT5_WRONG_SERVER` are all already in that
roster and all arrive via the classifier. ⛔ A verdict row the dialog roster then refuses would close
nothing, so the roster edit is load-bearing, not cosmetic.

**7. `actions` is the entire no-Retry mechanism.** `src/lib/envelope.ts`'s
`RECOVERABLE_ACTIONS` holds exactly `clear_and_retry` and `try_another_key`; `buildEnvelope` derives
`recoverable` by `.some()` over the copy's `actions`. Omitting BOTH yields `recoverable: false`
automatically — no per-route code. Precedents in the same file: `DRAFT_STATE_INVALID`,
`COMPOSITE_UNSUPPORTED_UNIFIED`, `ALLOCATION_NOT_ALLOCATABLE`, and the CLOSEST one,
`MT5_VALIDATE_INVARIANT_VIOLATION`, minted by the immediately prior phase (164.5.3) for a
rotate-secret hardening path — cite ITS docblock when writing the new member's own.
</planner_findings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mint the WizardErrorCode, route it with the verdict row, and retire the exemption</name>
  <files>src/lib/wizardErrors.ts</files>
  <read_first>
    - `src/lib/wizardErrors.ts`, symbol `WizardErrorCode` (the union declaration) — where members are declared and how each carries its own docblock.
    - `src/lib/wizardErrors.ts`, symbol `VENUE_WIRE_CODE_TO_VERDICT`, the `MT5_VALIDATE_INVARIANT_VIOLATION` row AND its preceding docblock — the closest precedent, minted by 164.5.3, and the source of the "NOT `KEY_PROBE_FAILED` / NOT `SEAM_MISCONFIGURED`" reasoning this member must restate for itself.
    - `src/lib/wizardErrors.ts`, symbol `WIZARD_ERROR_COPY`, entries `DRAFT_STATE_INVALID` and `KEY_MISSING_READ_SCOPE` — the no-Retry copy shape and the general entry shape.
    - `src/lib/wizardErrors.ts`, symbol `VENUE_WIRE_CODES_WITHOUT_VERDICT`, the `KEY_UNDECRYPTABLE` entry — read its stated reason in full before deleting it, because the deletion commit message owes an account of WHY the premise no longer holds.
    - `src/lib/wizardErrors.ts`, symbol `DASHBOARD_DIALOG_ROUTE_CODES`, the `keys/[id]/rotate-secret` Set and its trailing note.
    - `src/lib/envelope.ts`, symbols `RECOVERABLE_ACTIONS`, `DESTRUCTIVE_ACTIONS` and `buildEnvelope`'s `recoverable` derivation.
    - ⛔ Use `grep -a -n` for every search in this file family.
  </read_first>
  <behavior>
    - `classifyKeyValidationError(err)` where `err` carries `seamCode === "KEY_UNDECRYPTABLE"` returns the minted code, NOT `UNKNOWN`.
    - `buildEnvelope(<minted code>, ...)` yields `recoverable: false`.
    - The minted code's copy names the remedy: reconnect this key.
    - `VENUE_WIRE_CODES_WITHOUT_VERDICT.has("KEY_UNDECRYPTABLE")` is `false`.
    - `DASHBOARD_DIALOG_ROUTE_CODES.get("keys/[id]/rotate-secret")?.has(<minted code>)` is `true`.
  </behavior>
  <action>
Mint ONE new member of the `WizardErrorCode` union. The identifier is Claude's Discretion per
CONTEXT.md, subject to the copy decision — pick one that reads as a permanent, non-operator-fixable
key fault rather than a probe failure, and place it beside its nearest `KEY_*` neighbours in the
union with its own docblock.

That docblock must state, in the file's own idiom and citing the `MT5_VALIDATE_INVARIANT_VIOLATION`
precedent's docblock as the model, why this member exists rather than reusing one that already does:
⛔ NOT `KEY_PROBE_FAILED` — it is recoverable and renders the same useless Retry; ⛔ NOT
`SEAM_MISCONFIGURED` — an operator cannot fix a corrupted per-row ciphertext, which is what that
member promises. Record that the remedy comes FROM Python (`routers/internal.py` raises this wire
code with `retryable=False` and the reconnect sentence) and that TypeScript carries it rather than
re-deriving it.

Add its `WIZARD_ERROR_COPY` entry. `actions` must be drawn ONLY from members that are in NEITHER
`RECOVERABLE_ACTIONS` NOR `DESTRUCTIVE_ACTIONS` — re-read both sets and verify each action you pick
against them rather than trusting this sentence. Copy says: reconnect this key. Write `fix` as the
concrete steps a founder takes to reconnect, and say plainly in `cause` that the stored ciphertext
can no longer be decrypted and that trying again cannot change that.

Add the `VENUE_WIRE_CODE_TO_VERDICT` row mapping wire `"KEY_UNDECRYPTABLE"` to the minted member with
the honest status, beside the `MT5_VALIDATE_INVARIANT_VIOLATION` row. A-05 is explicit that this row
is the routing mechanism and that CONTEXT.md's literal bullet forbidding it was conflating a row
pointing at an existing DISHONEST member with a row pointing at the new HONEST one. Record that
distinction in a comment on the row so the next reader does not re-litigate it from CONTEXT.md's
superseded text.

REMOVE the `KEY_UNDECRYPTABLE` entry from `VENUE_WIRE_CODES_WITHOUT_VERDICT` in the SAME commit — a
code cannot honestly hold a verdict row and an exemption reason, and a shipped gate asserts exactly
that.

ADD the minted member to `DASHBOARD_DIALOG_ROUTE_CODES`'s `keys/[id]/rotate-secret` Set, with a
comment recording that it arrives via `seamCode` through the classifier rather than as a route
literal — the same way the five classifier-arriving members already in that Set do.

REWRITE the trailing note in that Set that currently records this code as not rostered. It gives two
reasons; the verdict-row one is now closed. ⛔ Re-derive the SECOND reason against the route file at
HEAD before you write the replacement — do not assume it is closed and do not assume it is open. If
it still holds, say what it still means now that the member IS rostered; if it does not, say why.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.test.ts src/lib/seam-venue-vocabulary.invariant.test.ts src/lib/dialog-envelope.invariant.test.ts src/lib/wizardErrors.invariant.test.ts src/lib/probe-vocabulary.invariant.test.ts src/lib/wizardErrors.roster-render.test.tsx</automated>
    <fails_when>The run exits non-zero, or reports `No test files found`, or any of the six files reports zero tests. ⚠️ The `EXPECTED_TABLE_SIZE` guards are EXPECTED to be RED here — task 2 moves them. Record their exact failure text (`expected 94 to be 93`) and carry it into task 2; every OTHER failure in this run is this task's to fix.</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>`tsc --noEmit` exits non-zero or prints any line containing `error TS`.</fails_when>
  </verify>
  <acceptance_criteria>
    - The minted member exists in the `WizardErrorCode` union, in `WIZARD_ERROR_COPY`, and as the target of a `VENUE_WIRE_CODE_TO_VERDICT` row keyed on wire `"KEY_UNDECRYPTABLE"`.
    - Its `actions` intersect neither `RECOVERABLE_ACTIONS` nor `DESTRUCTIVE_ACTIONS`, verified by reading both sets — state both memberships explicitly in the SUMMARY.
    - `VENUE_WIRE_CODES_WITHOUT_VERDICT` no longer carries `KEY_UNDECRYPTABLE`, and the disjointness test in `seam-venue-vocabulary.invariant.test.ts` is GREEN.
    - The minted member is in the rotate-secret dialog roster, and the stale not-rostered note has been rewritten against a re-derivation, not against this plan's prose.
    - `seam-venue-vocabulary.invariant.test.ts`'s derived-population assertions are GREEN with NO edit to `EXPECTED_EMITTED_CODES` — confirming finding 3 by measurement. If that roster DID need to move, stop and record why in the SUMMARY; it would mean a Python emitter changed, which this plan does not do.
  </acceptance_criteria>
  <done>A `KEY_UNDECRYPTABLE` wire code arriving through `seamCode` resolves to a minted, honest, non-recoverable `WizardErrorCode` that the rotate-secret dialog is rostered to render, the exemption is retired, and the disjointness gate is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move both EXPECTED_TABLE_SIZE pins with their reasoning re-run, and pin the no-Retry behaviour</name>
  <files>src/lib/wizardErrors.test.ts</files>
  <read_first>
    - `src/lib/wizardErrors.test.ts` — BOTH `EXPECTED_TABLE_SIZE` declarations and, critically, the long comment block preceding EACH. Locate them with `grep -a -n "const EXPECTED_TABLE_SIZE" src/lib/wizardErrors.test.ts`; ⛔ do not trust any line number written down anywhere, including here.
    - The most recent precedent bump in both blocks — `92 → 93 (164.5.3-02)` for `KEY_VENUE_ALREADY_CONNECTED`. Each states which scan it fronts and re-runs that scan's reasoning over the new entry. Model your own bump paragraphs on those two, one per site, because the two scans ask DIFFERENT questions.
    - The meta-gate `describe("[153.1-04 / WIZFORM-02] the two EXPECTED_TABLE_SIZE pins cannot silently diverge", ...)` — it asserts exactly TWO declarations exist and that they agree.
    - `src/lib/envelope.ts`, symbol `buildEnvelope` — the shape the new behavioural test drives.
  </read_first>
  <behavior>
    - `Object.keys(WIZARD_ERROR_COPY).length` equals the new pin value at BOTH sites, and the meta-gate still finds exactly two agreeing declarations.
    - `buildEnvelope(<minted code>, ...)` returns `recoverable: false`.
    - `classifyKeyValidationError` on an error carrying `seamCode === "KEY_UNDECRYPTABLE"` returns the minted code with the honest status — and specifically NOT `UNKNOWN` and NOT `KEY_PROBE_FAILED`.
  </behavior>
  <action>
Move both `EXPECTED_TABLE_SIZE` declarations to the value the failing run from task 1 REPORTED — read
the number out of the guard's own failure message, exactly as the `76 at 154.1` precedent in this
file records having done. ⛔ Do not compute it, do not count the table, and do not derive it from
`Object.keys(...)` — the comment at the first site explains at length that reading the subject to
build the expectation is how a guard stops being able to fail.

Before each integer moves, WRITE that site's re-run argument in the established style, and they are
different arguments:

- At the DESTRUCTIVE-ACTION site: state the new entry's `actions` list, state which members
  `DESTRUCTIVE_ACTIONS` holds, and conclude whether the entry falls inside or outside the scanned
  population — and therefore whether the destructive class below it changes.
- At the BANNED-CLAIMS site: walk the new entry's title, cause and every fix line against the four
  forbidden fragments that scan enumerates, and state that it carries none of them. Pay particular
  attention to any claim about what was or was not saved: the new copy describes a decrypt failure,
  so it must not imply anything about whether a write landed.

⚠️ The count must ALSO move in any test TITLE or prose that restates it. Sweep with `grep -a -n` for
the OLD integer across the file and read every hit — some are unrelated numbers, so read, do not
replace blindly.

Then add the behavioural tests this phase actually exists for:

1. A `buildEnvelope` test asserting `recoverable: false` for the minted code, with the assertion
   message naming WHY (a ciphertext that will never decrypt cannot be fixed by retrying).
2. A `classifyKeyValidationError` test that builds an `Error` carrying a `seamCode` own-property of
   `"KEY_UNDECRYPTABLE"` — the exact shape `rotate-secret/route.ts` sets — and asserts the minted
   code and status come back. Assert explicitly that the result is neither the `UNKNOWN` terminal nor
   `KEY_PROBE_FAILED`, because those are the two wrong answers this phase removes and an assertion
   naming them is what makes the test legible to the next reader.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.test.ts src/lib/seam-venue-vocabulary.invariant.test.ts src/lib/dialog-envelope.invariant.test.ts src/lib/wizardErrors.invariant.test.ts src/lib/envelope.test.ts</automated>
    <fails_when>The run exits non-zero, or reports `No test files found`, or prints a line containing `EXPECTED_TABLE_SIZE`, or prints `expected 94 to be 93` or its inverse.</fails_when>
    <automated>npm run test</automated>
    <fails_when>The full vitest run exits non-zero, or its summary line reports any non-zero `failed` count. ⚠️ Run this alone — the full suite must not share the box with another heavy run.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both pins hold the same new integer, the meta-gate finds exactly two agreeing declarations, and each site carries a NEW bump paragraph answering ITS OWN scan's question.
    - Every prose restatement of the old integer in that file has been read and, where it restated the pin, moved.
    - ⛔ ANTI-VACUITY, and it BLOCKS (this phase is USER-FACING): add `try_another_key` to the minted entry's `actions`, observe the `recoverable: false` test go RED, then restore `src/lib/wizardErrors.ts` byte-identically and prove it with `cmp` against a `cp` backup taken before the neuter. ⛔ Never `git checkout --` / `git restore` / `git stash`. Record the neuter, the observed RED and the `cmp` result in the SUMMARY.
    - ⛔ ANTI-VACUITY (BLOCKS): delete the `VENUE_WIRE_CODE_TO_VERDICT` row, observe the `classifyKeyValidationError` test go RED naming `UNKNOWN`, restore byte-identically and `cmp`. This is the assertion that proves A-05's mechanism claim rather than inheriting it.
    - The full `npm run test` suite is green.
  </acceptance_criteria>
  <done>Both hand-typed pins moved with a written re-run of each scan's own reasoning, and two tests — each observed RED under a neuter of its own subject — hold the no-Retry envelope and the row-based routing.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the route's RESIDUAL comment and prove the founder-visible outcome end to end</name>
  <files>src/app/api/keys/[id]/rotate-secret/route.ts</files>
  <read_first>
    - `src/app/api/keys/[id]/rotate-secret/route.ts`, the seam-failure throw site — the block beginning `⚠️ RESIDUAL, not closed by this fix`, and the `WR-01` comment immediately above it that explains why `seamCode` is carried forward.
    - The route's own docblock section on which classifier outcomes it expects, so the correction lands in the file's established voice.
  </read_first>
  <action>
Rewrite the `⚠️ RESIDUAL, not closed by this fix` block. It currently records that `KEY_UNDECRYPTABLE`
has no verdict row, names the two candidate closures (a new `WizardErrorCode` member, or a dedicated
pre-classifier arm), and flags the gap for follow-up. That is now HISTORY.

Replace it with a record of what actually shipped and, more importantly, WHY the chosen shape was
chosen — because the next reader will otherwise find CONTEXT.md's superseded bullet forbidding the
verdict row and reopen a settled question. State: the mint ALONE does not close this route, because
`classifyKeyValidationError` resolves `VENUE_WIRE_CODE_TO_VERDICT` before its substring cascade and
that cascade has no decrypt branch; the row is the ROUTING MECHANISM; and what was rejected was a row
pointing at an existing dishonest member, never a row pointing at the newly minted honest one. Name
the minted member so a grep from this file finds it.

⛔ Do NOT add a pre-classifier arm here. It was the rejected option (it duplicates the fix once per
route), and adding it now on top of the row would give this route two answers for one fault.

Then prove the founder-visible outcome rather than asserting it. Drive the route's own test file at
HEAD (locate it beside the route) with a seam response carrying `KEY_UNDECRYPTABLE` and confirm the
envelope the browser receives carries the minted code, `recoverable: false`, and copy naming the
reconnect remedy. If the route's test file has no seam-failure harness that admits a `code`-bearing
body, EXTEND the existing harness rather than writing a parallel one.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/keys/ src/lib/wizardErrors.test.ts</automated>
    <fails_when>The run exits non-zero, or reports `No test files found`, or the rotate-secret route's test file reports zero tests.</fails_when>
    <automated>npm run lint</automated>
    <fails_when>Exits non-zero, or the planning-hygiene step within it reports a non-zero finding count. ⚠️ Read the hygiene step's count line AND its OK line — they are different lines; never `tail -1`.</fails_when>
  </verify>
  <acceptance_criteria>
    - The residual block records the shipped mechanism and the A-05 distinction, and no longer describes an open gap.
    - An executing test drives the rotate-secret route with a `KEY_UNDECRYPTABLE` seam body and asserts the minted code, `recoverable: false`, and reconnect-naming copy in the response the browser receives.
    - ⛔ ANTI-VACUITY (BLOCKS): remove the `seamCode` assignment at the throw site, observe that new route test go RED (it should fall to the `UNKNOWN` terminal — the pre-164.5.3 behaviour), restore byte-identically and prove with `cmp`. ⛔ Never `git checkout --`. This proves the route-level wiring, not just the table.
    - All three residual comment sites from `<planner_findings>` item 5 are now correct — (a) and (b) by task 1, (c) by this task. List all three in the SUMMARY with their new text summarised.
  </acceptance_criteria>
  <done>The route records the shipped mechanism rather than an open gap, and an end-to-end test observed RED under a neuter proves a founder hitting a decrypt failure on this route now reads the reconnect remedy with no Retry control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analytics-service (Python) → Next.js route | The wire `code` crosses here; a misclassification becomes the founder's instruction. |
| Next.js route → browser dialog | The envelope's `recoverable` flag decides whether a Retry control renders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5.4-06 | Denial of Service (of the founder's own time and trust) | The Retry control rendered against a permanently-undecryptable ciphertext | high | mitigate | Minted copy whose `actions` omit both `RECOVERABLE_ACTIONS` members, deriving `recoverable: false` via `buildEnvelope`; pinned by task 2's test and observed RED under a `try_another_key` neuter. |
| T-164.5.4-07 | Repudiation | A `UNKNOWN` envelope claiming we could not classify a failure the seam DID classify | high | mitigate | The `VENUE_WIRE_CODE_TO_VERDICT` row, pinned by task 2's classifier test and observed RED when the row is deleted. |
| T-164.5.4-08 | Information Disclosure | The new user-facing copy leaking internal state (a key id, a ciphertext, a KEK detail) | medium | mitigate | `WIZARD_ERROR_COPY` entries are static prose with no interpolation; the banned-claims honesty scan at `EXPECTED_TABLE_SIZE` site 2 runs over the new entry and its reasoning is re-run in writing by task 2. |
| T-164.5.4-09 | Tampering | A stale exemption entry contradicting the shipped row | medium | mitigate | The shipped disjointness gate in `seam-venue-vocabulary.invariant.test.ts` refuses a code that is both mapped and exempt; task 1 removes the entry in the same commit as the row. |
| T-164.5.4-10 | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE — this plan installs no packages (RESEARCH.md: "No new external packages"). No Package Legitimacy Audit is required and none is claimed. |
</threat_model>

<verification>
- `npx vitest run src/lib/ src/app/api/keys/` is green, including the disjointness, ARRIVAL, coverage-law and roster-render gates.
- Three neuters were each observed RED and each restored byte-identically with `cmp`: the recoverable action, the verdict row, and the route's `seamCode` assignment.
- `npm run test`, `npm run typecheck` and `npm run lint` all pass.
- Both `EXPECTED_TABLE_SIZE` pins agree at the new value and each carries a freshly written re-run argument for its own scan.
</verification>

<success_criteria>
- A founder whose stored key can no longer be decrypted, hitting the rotate-secret route, reads a card that says reconnect this key and is offered no Retry.
- `KEY_UNDECRYPTABLE` holds a verdict row and no exemption, with the shipped disjointness gate green.
- No comment anywhere records this as an open residual.
</success_criteria>

<artifacts_this_phase_produces>
Symbols this plan creates:
- `src/lib/wizardErrors.ts`: ONE new `WizardErrorCode` union member (identifier at Claude's Discretion per CONTEXT.md), its `WIZARD_ERROR_COPY` entry, and a `VENUE_WIRE_CODE_TO_VERDICT` row keyed on wire `"KEY_UNDECRYPTABLE"`.
- `src/lib/wizardErrors.ts`: the minted member added to `DASHBOARD_DIALOG_ROUTE_CODES`'s `keys/[id]/rotate-secret` Set.
- Removed: the `KEY_UNDECRYPTABLE` entry in `VENUE_WIRE_CODES_WITHOUT_VERDICT`.
- New tests in `src/lib/wizardErrors.test.ts`: the `recoverable: false` envelope pin and the `seamCode` routing pin.
- New test coverage in the rotate-secret route's own test file: the end-to-end decrypt-failure envelope.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-02-SUMMARY.md` when done.
</output>
