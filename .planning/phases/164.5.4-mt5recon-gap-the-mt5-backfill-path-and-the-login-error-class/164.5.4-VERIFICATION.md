---
phase: 164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class
verified: 2026-09-20T23:55:00Z
status: passed
score: 15/15 must-haves verified
verified_at_sha: a87d3317fa23abda793afbf0a17a1687da17b7dc
drift_subjects:
  - "analytics-service/services/equity_reconstruction.py"
  - "analytics-service/services/mt5_validation.py"
  - "analytics-service/services/mt5_read.py"
  - "analytics-service/services/broker_dailies.py"
  - "analytics-service/services/allocator_positions.py"
  - "analytics-service/services/mt5_concurrency.py"
  - "analytics-service/services/mt5_client.py"
  - "analytics-service/services/mt5_probe.py"
  - "analytics-service/services/job_worker.py"
  - "analytics-service/services/ingestion/mt5.py"
  - "analytics-service/scripts/mt5_spike.py"
  - "analytics-service/tests/test_equity_reconstruction.py"
  - "analytics-service/tests/test_mt5_validate.py"
  - "analytics-service/tests/test_mt5_validate_parity.py"
  - "analytics-service/tests/test_job_worker.py"
  - "analytics-service/tests/test_mt5_client_contract.py"
  - "analytics-service/tests/test_mt5_derive_branch.py"
  - "analytics-service/tests/test_mt5_read.py"
  - "analytics-service/tests/test_mt5_deal_reconstruction.py"
  - "analytics-service/tests/test_mt5_concurrency.py"
  - "analytics-service/tests/test_ingestion_mt5.py"
  - "analytics-service/tests/test_allocator_positions_non_ccxt.py"
  - "analytics-service/tests/fixtures/mt5_login_rejection_observations.json"
  - "src/lib/wizardErrors.ts"
  - "src/lib/wizardErrors.test.ts"
  - "src/lib/seam-venue-vocabulary.invariant.test.ts"
  - "src/lib/dialog-envelope.invariant.test.ts"
  - "src/lib/probe-vocabulary.invariant.test.ts"
  - "src/app/api/keys/[id]/rotate-secret/route.ts"
  - "src/app/api/keys/[id]/rotate-secret/route.test.ts"
  - "src/components/connect/KeyPermissionBadge.tsx"
  - "src/components/connect/KeyPermissionBadge.test.tsx"
  - "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
  - "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-01-PLAN.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-01-SUMMARY.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-02-PLAN.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-02-SUMMARY.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-03-PLAN.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-03-SUMMARY.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-04-PLAN.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-04-SUMMARY.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-05-PLAN.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-05-SUMMARY.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-06-PLAN.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-06-SUMMARY.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-CONTEXT.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-REVIEW.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/164.5.4-REVIEW-R2.md"
  - ".planning/phases/164.5.4-mt5recon-gap-the-mt5-backfill-path-and-the-login-error-class/deferred-items.md"
behavior_unverified: 0
overrides_applied: 0
human_verification: []
---

# Phase 164.5.4: MT5RECON-GAP Verification Report

**Phase Goal:** Three MEASURED defects on the MT5 path, all of one class — MT5 is routed down a
path that does not know about it, and the failure degrades to an endless silent retry (or a
confident wrong-blame verdict) instead of a verdict anyone can read: (1) the full-backfill job
has no MT5 branch; (2) `classify_mt5_login_error` over-matched and stamped a permanent,
user-blamed verdict on a working credential; (3) `KEY_UNDECRYPTABLE` was discarded at the
Python/TypeScript seam on both the rotate-secret and the permissions route.

**Verified:** 2026-09-20
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

Derived from ROADMAP `### Phase 164.5.4` (no explicit numbered Success Criteria list — this is
an INSERTED defect-fix phase; `Requirements: TBD`), CONTEXT.md's decisions/amendments, and the
`must_haves` blocks across all six PLAN frontmatters (Option C fallback, per verifier procedure
Step 2c).

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | An explicit `venue == "mt5"` branch exists in `run_reconstruct_allocator_history_job`, making the generic ccxt crawl (and its `AttributeError` on `fetch_my_trades`) unreachable by construction, not merely guarded. | ✓ VERIFIED | `services/equity_reconstruction.py:2698` `if venue == "mt5":` / `2708` `_mt5_outcome = await _mt5_fetch_window(...)`. `test_mt5_backfill_never_reaches_the_ccxt_crawl` (06-SUMMARY) replaces `_fetch_and_price_window` with a landmine and the job still reaches `DONE`. Single named test re-run: 4/4 passed (see Behavioral Spot-Checks). |
| 2 | The MT5 branch takes the `mt5_enabled_server()` kill-switch itself (A-03) — nothing about adding a venue branch inherits it. | ✓ VERIFIED | `services/equity_reconstruction.py:52` imports `mt5_enabled_server`; `:2412` `if not mt5_enabled_server(): ...`. `test_mt5_backfill_kill_switch_refuses_before_any_terminal_read` re-run: passed; anti-vacuity neuter (permanent→transient) observed RED per 06-SUMMARY. |
| 3 | An MT5 fetch failure routes through `classify_mt5_login_error`, and only a genuine `auth` signal is permanent — `wrong_server` and every unrecognised signal are transient (A-04 resolved, opposite of the researcher's recommendation, reasoning recorded in the arm's own comment). | ✓ VERIFIED | `test_mt5_backfill_wrong_server_is_transient_not_permanent` re-run: passed. `test_no_mt5_backfill_message_is_classifier_matchable` (read at `tests/test_equity_reconstruction.py:5904-5934`) asserts `_MT5_BACKFILL_MESSAGES` non-empty, then runs the **shipped** classifier over all 11 members and requires every one to be `"transient"` — not a grep. Re-run: passed. |
| 4 | Persisted `value_usd` values are absolute dollar levels (never a return series) — the returns→levels gap (A-01) is closed by a `reconstruct_mt5_nav_levels` sibling on the shared `nav_twr.reconstruct_nav` core, not by inverting returns. | ✓ VERIFIED | `services/broker_dailies.py:740` `def reconstruct_mt5_nav_levels(...)`, calls `_fold_mt5_deals` (`:548`) — the same fold `combine_mt5_deal_ledger` (`:613`) uses. `equity_reconstruction.py:2371` calls `reconstruct_mt5_nav_levels`. Magnitude-floor test + terminal-anchor oracle test in 05-SUMMARY. |
| 5 | The MT5 deal-ledger read (login, PRE bracket, `history_deals_get`, POST re-assertion) exists in exactly ONE place, shared by the derive job and the backfill job, with no cycle. | ✓ VERIFIED | `services/mt5_read.py::read_mt5_deal_ledger` exists; `python -c "import services.mt5_read, services.job_worker, services.equity_reconstruction"` re-run by verifier: `import-ok`, no cycle. `equity_reconstruction.py:2369` imports it; `job_worker.py` derive branch calls it (04-SUMMARY). |
| 6 | The persist, idempotency, sibling-check and audit half of the backfill job is SHARED with the ccxt path, not duplicated — the diff to `run_reconstruct_allocator_history_job` is only the `if venue == "mt5": ... else:` wrapper. | ✓ VERIFIED | 06-SUMMARY documents the exact diff shape and states the deribit arm's duplicated `aclose_exchange` was deliberately not copied (MT5 branch returns from inside the shared `try`, so the shared `finally` runs). Consistent with the code read at `:2698-2730`. |
| 7 | A negative reconstructed NAV is refused rather than published (review-round-1 SF-H1 fix), and the discarded NAV-quality meta is now logged instead of dropped on the floor. | ✓ VERIFIED | `services/equity_reconstruction.py:2605` `if len(nav) and float(nav.min()) < 0.0: return _fail("negative_nav", "permanent")`. `test_mt5_backfill_negative_reconstructed_nav_is_refused` re-run: passed. Arithmetic independently re-derived in REVIEW-R2.md and matches. |
| 8 | `classify_mt5_login_error`'s `_AUTH_PHRASES` / `_WRONG_SERVER_PHRASES` are anchored, multi-word phrases — the bare single-word tokens (`account`, `invalid`, `login`) that caused double-matching no longer exist in either table. | ✓ VERIFIED | `services/mt5_validation.py:111-133` — table contents read directly; every member is a multi-word anchored phrase. Repo-wide `grep -a` for `_(AUTH\|WRONG_SERVER)_TOKENS` in `services/routers/tests`: zero hits outside stale `__pycache__`. |
| 9 | A login rejection the classifier does not recognise degrades to `transient`, never to a user-attributed permanent stamp (the refusal rule). | ✓ VERIFIED | `mt5_validation.py:377-380` — default arm `return "transient"` after both phrase checks fail. `test_an_unrecognised_login_rejection_refuses_rather_than_blaming` re-run: passed (part of the 5/5 re-run below). |
| 10 | The fail-CLOSED precedence (a message matching BOTH tables resolves `wrong_server`, not `auth`) is pinned by an EXECUTING test, not a comment, and the IPC transport code-gate still runs before any text matching. | ✓ VERIFIED | `mt5_validation.py:372-378` — code-gate first, then `_WRONG_SERVER_PHRASES` before `_AUTH_PHRASES`. `test_a_message_matching_both_tables_is_wrong_server_not_auth` (`tests/test_mt5_validate.py:483`) asserts the fixture message matches BOTH tables before asserting the verdict. Re-run: passed. |
| 11 | The live-spike hand-off is designed (not executed by any agent): a protocol-bearing, zero-observation fixture that no code path blocks on. | ✓ VERIFIED | `tests/fixtures/mt5_login_rejection_observations.json` read directly: one `_protocol` header element, explicit `founder_enters_credentials_no_agent_ever_does` clause, zero observation rows. No account number, server name or password present. |
| 12 | A `KEY_UNDECRYPTABLE` wire code arriving at the rotate-secret route resolves to a minted `WizardErrorCode` (`KEY_MUST_BE_RECONNECTED`) via a `VENUE_WIRE_CODE_TO_VERDICT` row, rendering NO Retry control — not the `UNKNOWN` terminal. | ✓ VERIFIED | `src/lib/wizardErrors.ts:4633` `["KEY_UNDECRYPTABLE", { code: "KEY_MUST_BE_RECONNECTED", status: 500 }]`; `:2449-2465` the copy entry with `actions` excluding both `RECOVERABLE_ACTIONS` members. Re-run of `wizardErrors.test.ts` + `rotate-secret/route.test.ts`: 373 tests passed (combined run, see Behavioral Spot-Checks). |
| 13 | `KEY_UNDECRYPTABLE` holds a verdict row and NO exemption entry — a code cannot honestly sit in both, and a shipped gate asserts exactly that. | ✓ VERIFIED | `VENUE_WIRE_CODES_WITHOUT_VERDICT` (`wizardErrors.ts:4650+`) read directly: no `KEY_UNDECRYPTABLE` entry (only comments recording its removal). `seam-venue-vocabulary.invariant.test.ts:901` `"no code is BOTH mapped and exempt"` re-run: passed. |
| 14 | When the permissions route answers `KEY_UNDECRYPTABLE`, the key card's re-check control is unusable; every OTHER refusal leaves it usable once loading completes (narrow gate, not a blanket disable). | ✓ VERIFIED | `src/components/connect/KeyPermissionBadge.tsx:312` `disabled={loading \|\| errorCode === "KEY_UNDECRYPTABLE"}`. Three neuters in 03-SUMMARY: gate-removed → red on `KEY_UNDECRYPTABLE` only; gate-widened → red on 5 other codes (proving narrowness); dead-field-bound → class-complete. All three `cmp`-verified restored. Re-run of `KeyPermissionBadge.test.tsx` + `probe-vocabulary.invariant.test.ts`: passed (in the combined 373). |
| 15 | The `log_prefix` misattribution introduced by round-1's own fix (SF-M1) is corrected to the real dispatcher job kind, and the gate now pins the VALUE passed, not merely the keyword's existence (review-round-2 fix). | ✓ VERIFIED | `services/allocator_positions.py:691,730` both read `log_prefix="poll_allocator_positions"` — confirmed the real dispatcher kind via `job_worker.py:9517` `elif kind == "poll_allocator_positions":`. `refresh_allocator_positions` (the invented string) has zero hits anywhere except a test comment documenting the fixed defect. `test_mt5_read_timeout_restarts_and_raises_transient` re-run: passed, and its own assertion (`assert restart_prefixes == ["poll_allocator_positions"]`) captures the actual value passed — not just keyword acceptance. |

**Score:** 15/15 truths verified (0 present, behavior-unverified)

### Known Limitation Explicitly Routed, Not a Gap

**SF-H2 (review round 1):** removing defect (2)'s over-matching is correct and shipped, but its
side effect is that genuine MT5 credential rejections now land on the PRE-EXISTING `transient`
arm (`424 NETWORK_UNAVAILABLE`, `recoverable=True`), whose copy asserts the network is
unavailable and offers a Retry that cannot succeed for a real bad credential. This is a
TypeScript-side cross-language gap of the same shape as defect (3), and it was deliberately NOT
fixed inside this phase's fix round ("a fix round is where regressions enter"). It is ROUTED to
Phase 167 CREDTRUST, which already owned an equivalent defect on the factsheet-staleness surface
verbatim and carries an unmet hard dependency on Phase 164.7. Confirmed at HEAD: Phase 167's
ROADMAP entry (`.planning/ROADMAP.md`, `### Phase 167: CREDTRUST`) explicitly cites this phase's
SF-H2 evidence by name ("MEASURED 2026-09-20 by Phase 164.5.4's review (SF-H2)"). This routing is
judged legitimate — Phase 167 did not exist as a plausible pre-existing sink by accident, it
already carried the identical symptom and remedy shape, and shipping the fix from inside a
fix round (rather than as a planned phase with its own review) is exactly the risk this repo's
own process rules exist to prevent. Not counted as a gap; noted so it is not silently absorbed
either.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `analytics-service/services/equity_reconstruction.py` | `venue == "mt5"` branch, kill-switch, classifier routing, negative_nav refusal | ✓ VERIFIED | Read directly; all symbols present and wired (see truths 1-7). |
| `analytics-service/services/mt5_validation.py` | Anchored-phrase tables + refusal rule | ✓ VERIFIED | Read directly (see truths 8-11). |
| `analytics-service/services/mt5_read.py` | Shared leaf-module deal-ledger read | ✓ VERIFIED | 138 lines, imports cleanly with both callers, no cycle. |
| `analytics-service/services/broker_dailies.py` | `_fold_mt5_deals` shared fold + `reconstruct_mt5_nav_levels` sibling | ✓ VERIFIED | Both symbols present; `combine_mt5_deal_ledger` calls the same fold. |
| `analytics-service/services/allocator_positions.py` | R2 fix — correct `log_prefix` | ✓ VERIFIED | Both call sites corrected. |
| `src/lib/wizardErrors.ts` | `KEY_MUST_BE_RECONNECTED` mint + routing row + exemption removal | ✓ VERIFIED | All three present; `EXPECTED_TABLE_SIZE` moved 93→94 at both sites. |
| `src/app/api/keys/[id]/rotate-secret/route.ts` | `seamCode` threaded to the classifier | ✓ VERIFIED (inherited from 164.5.3, unchanged by this phase, confirmed still wired) | |
| `src/components/connect/KeyPermissionBadge.tsx` | `errorCode` state + narrow `disabled` gate | ✓ VERIFIED | Both present and correctly scoped. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `equity_reconstruction.py::_mt5_fetch_window` | `services/mt5_read.py::read_mt5_deal_ledger` | direct import + `asyncio.to_thread` call | ✓ WIRED | Confirmed at `:2369`, `:2433`. |
| `equity_reconstruction.py::_mt5_fetch_window` | `services/broker_dailies.py::reconstruct_mt5_nav_levels` | direct call | ✓ WIRED | Confirmed at `:2371`. |
| `equity_reconstruction.py::_mt5_fetch_window` (Mt5ClientError arm) | `services/mt5_validation.py::classify_mt5_login_error` | direct call | ✓ WIRED | Confirmed via `test_mt5_backfill_wrong_server_is_transient_not_permanent` passing. |
| `src/app/api/keys/[id]/rotate-secret/route.ts` | `classifyKeyValidationError` → `VENUE_WIRE_CODE_TO_VERDICT` | `seamCode` forwarding | ✓ WIRED | Confirmed via re-run of `route.test.ts`'s D-03 case. |
| `src/components/connect/KeyPermissionBadge.tsx` | `src/app/api/keys/[id]/permissions/route.ts` | `err.code` retained in state | ✓ WIRED | Server route unchanged (confirmed by plan-03's own scope statement and `git diff` showing no `src/app/api/` file touched by plan 03). |

### Behavioral Spot-Checks

All commands run by the verifier directly (single named tests / small scoped runs, never the
full suite — the phase's own gate board already ran the full suites on a quiet box per the task
brief and is not re-run here).

| Behavior | Command | Result | Status |
|---|---|---|---|
| Defect-1 core dispositions | `pytest tests/test_equity_reconstruction.py -k "negative_reconstructed_nav_is_refused or wrong_server_is_transient_not_permanent or no_mt5_backfill_message_is_classifier_matchable or kill_switch_refuses_before_any_terminal_read"` | 4 passed | ✓ PASS |
| R2 log_prefix value pin | `pytest tests/test_allocator_positions_non_ccxt.py -k test_mt5_read_timeout_restarts_and_raises_transient` | 1 passed | ✓ PASS |
| Classifier refusal + precedence | `pytest tests/test_mt5_validate.py -k "unrecognised_login_rejection or fail_closed or precedence"` | 5 passed | ✓ PASS |
| mt5_read.py leaf-module, no cycle | `python -c "import services.mt5_read, services.job_worker, services.equity_reconstruction"` | `import-ok` | ✓ PASS |
| mypy --strict on touched modules | `mypy --strict --follow-imports=silent services/equity_reconstruction.py services/mt5_validation.py services/mt5_read.py services/broker_dailies.py services/allocator_positions.py` | `Success: no issues found in 5 source files` | ✓ PASS |
| TS seam + component gates | `npx vitest run src/lib/wizardErrors.test.ts src/components/connect/KeyPermissionBadge.test.tsx src/lib/probe-vocabulary.invariant.test.ts src/lib/seam-venue-vocabulary.invariant.test.ts "src/app/api/keys/[id]/rotate-secret/route.test.ts"` | 373 passed (5 files) | ✓ PASS |
| Debt-marker scan on all touched files | `grep -naE "TBD\|FIXME\|XXX"` over every file this phase modified | 0 hits | ✓ PASS |

### Requirements Coverage

REQUIREMENTS.md (the v1.20 backlog burndown ledger) carries **no** `164.5.4-D1`/`D2`/`D3`
entries — this is expected and not an omission. `.planning/ROADMAP.md`'s Phase 164.5.4 entry
itself states `**Requirements**: TBD`, consistent with how other INSERTED defect-fix phases in
this milestone (e.g. 164.6, 164.6.1) are tracked: by TODOS/ROADMAP evidence rather than a
REQUIREMENTS.md row. `grep -n "164.5.4" .planning/REQUIREMENTS.md` returns zero hits in either
direction, so there is no orphaned requirement REQUIREMENTS.md expected this phase to close that
no plan claimed.

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| 164.5.4-D1 | 04, 05, 06 | The MT5 full-backfill job gets a venue branch, a shared deal-ledger read, a NAV-levels sibling, dispositions and a row contract | ✓ SATISFIED | Truths 1-7 |
| 164.5.4-D2 | 01 | The login classifier refuses to guess rather than defaulting to blame | ✓ SATISFIED | Truths 8-11 |
| 164.5.4-D3 | 02, 03 | `KEY_UNDECRYPTABLE` closed on both the rotate-secret and permissions routes | ✓ SATISFIED | Truths 12-14 |

### Anti-Patterns Found

None blocking. Debt-marker scan (`TBD`/`FIXME`/`XXX`) across every file this phase's plans
modified returned zero hits. `TODO`/`HACK`/`PLACEHOLDER`/"not available" scan returned only
pre-existing, unrelated matches (a template placeholder constant in `wizardErrors.ts`, prose
copy strings, and an unrelated `TODOS.md` cross-reference comment in `allocator_positions.py`) —
none introduced by this phase, none flowing to a stub code path.

### Review Findings — both rounds, dispositions verified against code

| id | severity | disposition | verified |
|---|---|---|---|
| SF-H1 (R1) | BLOCKER, data-integrity | FIXED + GATED | ✓ `negative_nav` refusal + quality-facts log present and passing |
| SF-H2 (R1) | BLOCKER, user-facing | ROUTED to Phase 167 CREDTRUST | ✓ Phase 167 entry carries this phase's evidence verbatim, judged legitimate (see Known Limitation above) |
| SF-M1 (R1) | medium | FIXED | ✓ `log_prefix` parameter added to `_mt5_bounded_restart`, all 6 call sites name their job |
| SF-M2 (R1) | medium | DROPPED (fix-or-drop, no established user-visible harm) | reasonable — shared idempotency gate change out of proportion to measured impact |
| SF-L1 (R1) | low | DROPPED (latent, not live) | reasonable |
| WR-01 (R1) | warning | FIXED | ✓ `import math` moved to top-level stdlib block |
| WR-01 (R2) | warning, found in R1's own fix | FIXED + GATED | ✓ `log_prefix` value corrected to `poll_allocator_positions`; gate now pins VALUE not existence |
| IN-01 (R1) | info | no action needed | ✓ correctly not a defect — fixture is deliberately empty by design |

No round-3 review was run; the repo's own two-round cap policy applies and the round-2 finding
was the only one, and it was fixed and gated.

### Human Verification Required

None. All must-haves are either symbol-presence-plus-wiring facts confirmed by direct code
reading, or behavior-dependent facts confirmed by a passing single named test the verifier itself
re-ran (not merely quoted from a SUMMARY).

### Gaps Summary

None. All 15 derived truths verified. The one known limitation (SF-H2's routing to Phase 167) is
a judged-legitimate routing decision with a named owning phase carrying this phase's own evidence
at HEAD, not a silently absorbed gap. Two review findings (SF-M2, SF-L1) were dropped under the
repo's own data-integrity/user-facing threshold rule and are recorded as such, not as unresolved
defects.

---

_Verified: 2026-09-20_
_Verifier: Claude (gsd-verifier)_
