---
phase: 24-benchmark-comparison
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.tsx
  - src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
autonomous: true
requirements: [BENCH-01]

must_haves:
  truths:
    - "The composer fetches the BTC daily-returns series from GET /api/benchmark/btc and passes a cumulative-wealth-form BTC curve to EquityChart.benchmark (the SVG widget), toggleable"
    - "A 'vs BTC over {N} overlapping days' metrics section renders Tracking Error / Information Ratio / Alpha / Beta over the aligned intersection window, numbers in Geist Mono"
    - "When overlap is below the 30-day floor OR the benchmark doesn't cover the window OR the fetch fails, a neutral 'Benchmark comparison unavailable' EmptyStateCard renders (two distinct bodies; no role=alert) instead of the metrics"
    - "Any null/non-finite metric renders an em-dash '—', never a fabricated 0/0.00%/N/A"
    - "The methodology line stamps the aligned {N} and notes '252-day annualized active returns'"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.tsx"
      provides: "Benchmark metrics section + two honest empty states (extracted, testable)"
      min_lines: 40
    - path: "src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.test.tsx"
      provides: "Both empty-state bodies + em-dash + no-role=alert + N-in-heading tests"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "BTC fetch + overlay wiring + section mount"
      contains: "ScenarioBenchmarkSection"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "/api/benchmark/btc"
      via: "fetch in useEffect"
      pattern: "/api/benchmark/btc"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "EquityChart.benchmark"
      via: "cumulative-wealth BTC series prop"
      pattern: "benchmark="
    - from: "src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.tsx"
      to: "scenario-benchmark.ts + sample-floor.ts + EmptyStateCard"
      via: "computeScenarioBenchmark + evaluateSampleFloor + EmptyStateCard"
      pattern: "evaluateSampleFloor|EmptyStateCard|computeScenarioBenchmark"
---

<objective>
Wire the benchmark dimension into the live scenario projection: fetch the BTC daily-returns series, overlay a cumulative-wealth BTC line on the composer's `EquityChart` (the SVG widget — NOT `EquityCurve.tsx`), and render an extracted `ScenarioBenchmarkSection` that shows the four active-return metrics over the inner-join window or an honest "Benchmark comparison unavailable" empty state. This consumes Plan 01's `computeScenarioBenchmark` + `portfolio_daily_returns` and Plan 02's GET route.

Purpose: This is the user-visible half of BENCH-01. The section is extracted into its own component so the honesty invariants (two distinct empty-state bodies, em-dash on null, no role=alert, intersection {N} in the heading) are unit-testable without mounting the 1900-line composer.
Output: new `ScenarioBenchmarkSection` (+ test) + composer edits (fetch, overlay, mount).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-benchmark-comparison/24-RESEARCH.md
@.planning/phases/24-benchmark-comparison/24-CONTEXT.md
@.planning/phases/24-benchmark-comparison/24-UI-SPEC.md
@.planning/phases/24-benchmark-comparison/24-01-SUMMARY.md
@.planning/phases/24-benchmark-comparison/24-02-SUMMARY.md

<critical_wiring_correction>
RESEARCH Pitfall 3 (load-bearing): the composer's projection chart is the SVG
`EquityChart` widget (`../widgets/performance/EquityChart.tsx`), already imported at
ScenarioComposer.tsx:85 and mounted at :1437. It has a `benchmark?: DailyPoint[]` prop
(EquityChart.tsx:192) that runs `anchorFromFirstPositive` (divide-by-first, :627) — so it
expects a CUMULATIVE-WEALTH series (like the strategy line, ~1.0 base), NOT raw daily
returns. Do NOT touch `EquityCurve.tsx` / its `benchmarkSeries` prop — that is a different
(lightweight-charts) component used elsewhere. Wiring `benchmarkSeries` onto `EquityCurve`
is the headline trap: BOTH `EquityChart.benchmark` and `EquityCurve.benchmarkSeries` accept
`DailyPoint[]`, so a wrong wire TYPE-CHECKS and passes every test — the only guard is the
static grep acceptance criteria in Task 3.

Two derived shapes from ONE source: the GET route returns BTC DAILY RETURNS. The METRICS
(Plan 01 `computeScenarioBenchmark`) take raw daily returns. The CHART overlay takes a
cumulative-wealth curve — derive it from the same BTC daily returns via the
`computeStrategyCurve`-style cumulative product (scenario.ts:444 `computeStrategyCurve`
already does exactly this: `c *= 1 + value` → wealth points). Feed `computeStrategyCurve(btcDaily)`
to `EquityChart.benchmark`; feed raw `btcDaily` to `computeScenarioBenchmark`.
</critical_wiring_correction>

<interfaces>
From src/app/(dashboard)/allocations/lib/scenario-benchmark.ts (Plan 01):
```typescript
export type ScenarioBenchmark = { n: number; trackingError: number|null; informationRatio: number|null; alpha: number|null; beta: number|null; correlation: number|null };
export function computeScenarioBenchmark(portfolioDaily: DailyPoint[], btcDaily: DailyPoint[]): ScenarioBenchmark;
export function innerJoinByDate(port: DailyPoint[], bench: DailyPoint[]): { dates: string[]; p: number[]; b: number[] };
```

From src/lib/scenario.ts (Plan 01 additive OPTIONAL field; already in scope via scenarioMetrics):
```typescript
ComputedMetrics.portfolio_daily_returns?: Array<{ date: string; value: number }>; // OPTIONAL, full daily, unrounded; [] on degenerate. computeScenario always sets it; read with `?? []`.
```

From src/lib/sample-floor.ts:
```typescript
export function evaluateSampleFloor(n: number|null|undefined, floor?: number): SampleFloorVerdict; // pass 30
// verdict.reason ∈ "ok" | "below-floor" | "no-usable-n"; verdict.ok boolean; verdict.n
```

From src/components/ui/EmptyStateCard.tsx:
```typescript
export function EmptyStateCard({ heading, body }: { heading: string; body: string }): JSX.Element; // neutral, NO role=alert
```

From src/lib/scenario-history.ts:
```typescript
export function methodologyLine(n: number): string; // "Historical realized · {n} overlapping days · not a forecast."
```

From src/lib/utils.ts (emit "—" for null/non-finite):
```typescript
export function formatPercent(value: number|null|undefined, decimals?, opts?: { signed?: boolean }): string; // "—" when null
export function formatNumber(value: number|null|undefined, decimals?): string;                              // "—" when null
```

From src/lib/scenario.ts (cumulative-wealth converter for the chart overlay):
```typescript
export function computeStrategyCurve(dailyReturns: DailyPoint[]): DailyPoint[]; // c *= 1+value → wealth points (1.0 base)
```

Composer mount facts: ScenarioComposer.tsx:85 imports `{ EquityChart, toWealth }`; the
`scenarioMetrics` memo (:866) now carries `portfolio_daily_returns` (OPTIONAL — read with `?? []`); the chart mounts at
:1437; the existing coverage caveat (`methodologyLine(scenarioMetrics.n)`) is at :1359-1367.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — ScenarioBenchmarkSection test stub (RED)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-compare.test.ts (sibling vitest+jsdom render style: render → query → assert text)
    - .planning/phases/24-benchmark-comparison/24-UI-SPEC.md §Copywriting (the EXACT two empty-state body strings + heading + metric labels) + §Color (no role=alert / no red)
    - .planning/phases/24-benchmark-comparison/24-VALIDATION.md (the component-test rows)
    - src/components/ui/EmptyStateCard.tsx (the rendered DOM it produces — heading in `.font-semibold`, body in `text-[11px]`, NO role=alert)
  </read_first>
  <action>
    Create `src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.test.tsx` importing the not-yet-existing `ScenarioBenchmarkSection` (RED). The component's props (define them in the test to drive the contract): `{ portfolioDaily: {date,value}[]; btcDaily: {date,value}[]; benchmarkAvailable: boolean }` (benchmarkAvailable=false models a failed/empty fetch). Write failing tests:
    - "renders metrics when n>=30": fully-overlapping 40-day pair → heading text matches `vs BTC over 40 overlapping days`; four labels present ("Tracking Error", "Information Ratio", "Alpha", "Beta"); the methodology line text contains "overlapping days" and "252-day annualized active returns".
    - "below floor body": pair overlapping only 12 days → EmptyStateCard heading "Benchmark comparison unavailable" with the BELOW-FLOOR body naming the count ("share 12 overlapping days" / "fewer than the 30 needed" per UI-SPEC verbatim). Assert the no-overlap body string is NOT present.
    - "no overlap / not covered body": portfolio dates disjoint from btc dates (or benchmarkAvailable=false) → same heading, the NO-OVERLAP/NOT-COVERED body ("doesn't cover this scenario's date window") — and assert the below-floor body is NOT present (the two must not be conflated — #509).
    - "em-dash on null metric": construct a pair where a metric is null (e.g. constant benchmark → beta null) but n>=30 → that metric cell renders "—", NEVER "0.00" / "0.00%" / "N/A".
    - "no alert / honest absence": in BOTH empty-state branches, `container.querySelector('[role="alert"]')` is null and there is no `text-negative`/red class on the empty card.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.test.tsx" 2>&1 | grep -q "fail\|Cannot find" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - File exists and FAILS (component missing).
    - The two empty-state bodies are asserted to be DISTINCT (each test asserts its own body present AND the other absent).
    - The em-dash test would FAIL if the implementation emitted a fabricated 0 (it asserts "—" and asserts the absence of "0.00").
    - Both empty-state assertions check `role="alert"` is absent.
  </acceptance_criteria>
  <done>ScenarioBenchmarkSection.test.tsx present; asserts heading-N, four labels, two distinct empty bodies, em-dash, no role=alert; fails (component absent).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build ScenarioBenchmarkSection component (GREEN)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.tsx</files>
  <read_first>
    - .planning/phases/24-benchmark-comparison/24-UI-SPEC.md §Typography + §Component Inventory (metric-row styling: `text-xs` label `text-text-muted`, right-aligned `.font-metric` value `text-text-secondary`, hairline `border-b border-border/50`; numbers Geist Mono; 2 weights only)
    - src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx (the row-styling primitive to mirror — label/value/border tokens)
    - src/components/ui/EmptyStateCard.tsx (verbatim shell)
  </read_first>
  <behavior>
    - Props `{ portfolioDaily, btcDaily, benchmarkAvailable }`. Compute `joined = innerJoinByDate(portfolioDaily, btcDaily)`; `n = joined.p.length`; `verdict = evaluateSampleFloor(n, 30)`.
    - Empty-state routing (order matters): if `!benchmarkAvailable` OR `n === 0` → render `EmptyStateCard` with the NO-OVERLAP/NOT-COVERED body. Else if `verdict.reason !== "ok"` (below-floor or no-usable-n) → `EmptyStateCard` with the BELOW-FLOOR body naming `{n}`. Else render the metrics.
    - Metrics path: `const m = computeScenarioBenchmark(portfolioDaily, btcDaily)`. Heading "vs BTC over {m.n} overlapping days". Four rows: Tracking Error = `formatPercent(m.trackingError)`, Information Ratio = `formatNumber(m.informationRatio)`, Alpha = `formatPercent(m.alpha)`, Beta = `formatNumber(m.beta)` (pick percent-vs-number per metric meaning; TE/Alpha are returns → percent, IR/Beta are ratios → number). Every value goes through `formatPercent`/`formatNumber` so null → "—". Numbers carry `.font-metric` (Geist Mono).
    - Methodology stamp under the rows: `methodologyLine(m.n)` + literal " Metrics are 252-day annualized active returns." in an 11px `text-text-muted` line.
    - NO role=alert anywhere; no red/amber; the empty card is the neutral EmptyStateCard.
  </behavior>
  <action>
    Create `src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.tsx` (client component — it does pure presentational work over props; no `"use server"`). Implement per the behavior block, importing `computeScenarioBenchmark` + `innerJoinByDate` from `../lib/scenario-benchmark`, `evaluateSampleFloor` from `@/lib/sample-floor`, `EmptyStateCard` from `@/components/ui/EmptyStateCard`, `methodologyLine` from `@/lib/scenario-history`, and `formatPercent`/`formatNumber` from `@/lib/utils`. Use the EXACT empty-state body strings from UI-SPEC §Copywriting (do not paraphrase — heading must match body, #509). Mirror `ScenarioCompareTable` row tokens. Use exactly 2 font weights (400/600).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - All Task-1 tests pass.
    - The two empty-state bodies are the verbatim UI-SPEC strings (no-overlap vs below-floor), routed by the order above — a no-overlap input never shows the below-floor body and vice versa.
    - Every metric value flows through `formatPercent`/`formatNumber` (grep both present); no raw `.toFixed`/`+ "%"` on a metric value in the JSX.
    - No `role="alert"` token in the file: `grep -c 'role="alert"' src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.tsx` returns 0.
    - Numbers use `.font-metric`: `grep -c 'font-metric' ...` >= 1.
  </acceptance_criteria>
  <done>ScenarioBenchmarkSection renders metrics or the correct honest empty state; em-dash on null; methodology stamp; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire fetch + overlay + section mount into ScenarioComposer</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:58 (hook imports), :85 (EquityChart/toWealth import), :572 (an existing useEffect to mirror), :866-931 (scenarioMetrics memo + scenarioWealthSeries memo), :1431-1442 (the EquityChart mount), :1359-1367 (existing coverage caveat where the new section mounts nearby)
    - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx:190-246 (Props incl. `benchmark?: DailyPoint[]`), :620-635 (anchorFromFirstPositive expects cumulative-wealth)
    - node_modules/react/... only if unsure of a hook signature (AGENTS.md: this is React 19 / Next 16)
  </read_first>
  <action>
    NOTE: CONTEXT.md's reference to `EquityCurve.tsx benchmarkSeries` as the overlay target is SUPERSEDED by RESEARCH Pitfall 3 — the composer renders the `EquityChart` SVG widget (verified at ScenarioComposer.tsx:85/:1437). Wire `EquityChart.benchmark`; do NOT touch `EquityCurve.tsx`. Both props accept `DailyPoint[]`, so a wrong wire type-checks and passes every test — the static grep acceptance criteria below are the only guard.

    Edit `src/app/(dashboard)/allocations/components/ScenarioComposer.tsx`:
    1. Add state + effect to fetch the BTC series: `const [btcDaily, setBtcDaily] = useState<{date:string;value:number}[]>([])` and `const [btcAvailable, setBtcAvailable] = useState(false)`. In a `useEffect(() => { let cancelled=false; fetch("/api/benchmark/btc").then(r => r.ok ? r.json() : []).then(d => { if(!cancelled){ setBtcDaily(Array.isArray(d)?d:[]); setBtcAvailable(Array.isArray(d)&&d.length>0);} }).catch(() => { if(!cancelled){ setBtcDaily([]); setBtcAvailable(false);} }); return () => { cancelled=true; }; }, [])`. A failed/empty fetch leaves `btcAvailable=false` → the section renders the honest empty state (RESEARCH Pitfall 5: transport failure degrades to the empty state, never a red alert).
    2. Add a `showBenchmark` toggle (default ON per UI-SPEC) — `const [showBenchmark, setShowBenchmark] = useState(true)`. Render the toggle as the verbatim `EquityCurve`-style `<label><input type="checkbox">` with the copy "BTC Benchmark" and a `#94A3B8` line swatch, near the chart.
    3. Derive the chart overlay series (cumulative wealth): `const btcWealth = useMemo(() => (showBenchmark && btcAvailable) ? computeStrategyCurve(btcDaily) : undefined, [showBenchmark, btcAvailable, btcDaily])` (import `computeStrategyCurve` from `@/lib/scenario`). Pass it to the existing `<EquityChart ... benchmark={btcWealth} />` mount (:1437) — add the `benchmark` prop ONLY to the EquityChart element; do NOT touch `EquityCurve.tsx` or introduce any new `EquityCurve` reference.
    4. Mount `<ScenarioBenchmarkSection portfolioDaily={scenarioMetrics.portfolio_daily_returns ?? []} btcDaily={btcDaily} benchmarkAvailable={btcAvailable} />` below the projection chart block (reuse the `mt-6` rhythm). The `?? []` default is required because `portfolio_daily_returns` is OPTIONAL (Plan 01). Import the section from `./ScenarioBenchmarkSection`.
  </action>
  <verify>
    <automated>npx vitest run "ScenarioBenchmarkSection" && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The composer fetches `/api/benchmark/btc` in a useEffect: `grep -c "/api/benchmark/btc" src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` >= 1.
    - EquityChart wiring (static grep guards — the wrong wire onto EquityCurve type-checks and passes all tests, so these are load-bearing; RESEARCH Pitfall 3):
      - `grep -c 'EquityCurve' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` does NOT increase vs the pre-edit count (no new EquityCurve reference introduced). Capture the pre-edit count first.
      - `grep -E 'benchmark=\{' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` matches ≥1 (the EquityChart benchmark prop is wired).
      - `grep -c 'computeStrategyCurve' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` ≥1 (the overlay is fed a cumulative-WEALTH curve, not raw daily returns).
    - `ScenarioBenchmarkSection` is mounted with `portfolioDaily={scenarioMetrics.portfolio_daily_returns ?? []}` and `btcDaily`/`benchmarkAvailable` props.
    - A failed/empty fetch leaves `benchmarkAvailable=false` (honest empty state, no red alert) — verifiable by reading the effect's error/empty branches.
    - `npm run typecheck` + `npm run lint` pass.
  </acceptance_criteria>
  <done>Composer fetches BTC, overlays a cumulative BTC line on EquityChart (toggleable, default on), mounts the benchmark section; static grep guards confirm EquityChart.benchmark wired and no new EquityCurve ref; typecheck/lint clean; component tests green.</done>
</task>

</tasks>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.test.tsx"` green.
- `npm test` full suite green; `npm run typecheck` + `npm run lint` pass; coverage gate not regressed (new component + lib carry tests — CLAUDE.md blocking gate).
- Honesty invariants visibly proven: intersection {N} in the heading (not the union window), two distinct empty-state bodies (heading matches body), em-dash on null, no role=alert, overlay is cumulative-wealth (not raw returns) on `EquityChart` (not `EquityCurve`, pinned by the static grep guards in Task 3).
- Manual /qa (post-deploy, per VALIDATION Manual-Only): toggle BTC benchmark on the Scenario tab; confirm overlay + TE/IR/alpha/beta + honest empty state when overlap < 30d.
</verification>

<success_criteria>
- BENCH-01 complete and user-visible: benchmark overlay aligned to the intersection window, four 252-annualized active-return metrics over that window, and an honest "unavailable" empty state for missing/short/failed benchmark coverage.
</success_criteria>

<output>
Create `.planning/phases/24-benchmark-comparison/24-03-SUMMARY.md` when done.
</output>
</content>
