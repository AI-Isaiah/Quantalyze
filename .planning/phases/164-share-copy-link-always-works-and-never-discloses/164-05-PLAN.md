---
phase: 164-share-copy-link-always-works-and-never-discloses
plan: 05
type: execute
wave: 2
depends_on: ["164-01"]
files_modified:
  - src/lib/scrub-share-path.ts
  - src/lib/scrub-share-path.test.ts
  - src/instrumentation.ts
  - src/instrumentation.test.ts
  - next.config.ts
  - src/app/layout.tsx
  - src/app/factsheet/[id]/v2/factsheet-analytics.ts
  - src/app/factsheet-share/[token]/page.cache-isolation.test.tsx
autonomous: true
requirements: [SHARE-02, SHARE-01]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "After a token-lane render of an unpublished strategy, an anonymous request for /factsheet/<id> STILL 404s — proven by the ORDERED adversarial test, demonstrated RED with the bypass neutered (SHARE-02)"
    - "A raw token in a request path never reaches Sentry event URL/transaction/extra fields — scrubbed on PATH, not query param"
    - "Plausible does not receive /factsheet-share/<token> pageview paths (the genuinely new leak channel under D-01)"
    - "Cross-origin AND same-origin referrers from the token route carry no token (per-route no-referrer)"
    - "Recipient-mode pageviews fire no product analytics events"
  artifacts:
    - path: "src/lib/scrub-share-path.ts"
      provides: "Pure path scrubber: any share-route segment -> the [token] placeholder"
      exports: ["scrubSharePath"]
    - path: "src/app/factsheet-share/[token]/page.cache-isolation.test.tsx"
      provides: "The ORDERED adversarial acceptance for SHARE-02"
  key_links:
    - from: "src/instrumentation.ts"
      to: "src/lib/scrub-share-path.ts"
      via: "beforeSend + beforeSendTransaction + onRequestError all route URL-shaped fields through the one scrubber"
      pattern: "scrubSharePath"
---

<objective>
Close every token-leak channel the A-D1 path-segment re-derivation found (164-CONTEXT.md Blocker 3
and 164-RESEARCH.md's channel table), and land the phase's headline acceptance: the ORDERED
adversarial cache-isolation test (SHARE-02).

Why this is not the query-param analysis: under D-01 the token is a PATH segment. The global
Referrer-Policy strips query strings cross-origin but NEVER strips the path; Plausible records
pathnames site-wide, so a path token reaches a third-party analytics host on every recipient view —
a channel a query token never had. Sentry is server-only here and its request-error hook forwards
the raw path; there is NO existing scrub anywhere in src/ — this is net-new with no analog.

Purpose: SHARE-02's behavioral acceptance + the disclosure half of SHARE-01 ("never discloses").
Output: scrubber module wired into Sentry init and the request-error hook, per-route no-referrer,
Plausible exclusion, recipient analytics suppression, and the ordered adversarial test with its
RED demonstration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164-share-copy-link-always-works-and-never-discloses/164-CONTEXT.md
@.planning/phases/164-share-copy-link-always-works-and-never-discloses/164-RESEARCH.md
@src/instrumentation.ts
@src/app/factsheet/[id]/v2/page.owner-lane.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sentry path scrub (net-new) + the secret's boot-time visibility (D-02)</name>
  <files>src/lib/scrub-share-path.ts, src/lib/scrub-share-path.test.ts, src/instrumentation.ts, src/instrumentation.test.ts</files>
  <action>
    1. `src/lib/scrub-share-path.ts`: one exported pure function replacing every occurrence of a
       share-route path segment (the path prefix followed by any run of non-slash, non-query,
       non-hash characters) with the literal "/factsheet-share/[token]" placeholder, global,
       preserving any query/hash suffix. Deliberately NOT scoped to the 43-char token format —
       malformed-token requests error too and must scrub identically (scrubbing the gone segment
       to the placeholder is harmless and keeps the rule uniform). Small pure module so tests can
       pin vectors and the future scenario-share cleanup can reuse it.
    2. `src/lib/scrub-share-path.test.ts`: input/output vectors — bare token path; token path with
       query suffix; with hash suffix; full absolute URL; multiple occurrences in one string;
       strings without the prefix pass through untouched; the gone path.
    3. `src/instrumentation.ts` (Sentry.init at :30, onRequestError at :38-61):
       - Add beforeSend AND beforeSendTransaction to the init options, both applying the scrubber
         across: event request url, event transaction name, every breadcrumb message and
         breadcrumb data url, and span/trace description fields carrying URL-shaped strings.
         Sentry is server-only in this repo (no client init exists — verified); add a guard
         comment that introducing a client-side Sentry init later RE-OPENS the browser
         breadcrumb/replay channel and must adopt this scrubber.
       - In onRequestError, scrub the request path with the same helper BEFORE building the extra
         block (extra.path is the raw request path today — for a token-page error that IS the raw
         token). tags.routePath is already parameterized and stays.
       - Boot-time visibility for the token secret (the second half of D-02's fail-loud, the
         module-load throw being the first): in register(), when running in production with the
         secret missing or shorter than 32 chars, emit a console.error naming the var and the
         remedy AND capture a Sentry message — so the deploy log shows the misconfig before
         anyone clicks. ⛔ Do NOT add the var to the warn-only soft-skip key list — that list is
         documented as never-crash-a-deploy, which is the opposite of this decision's intent.
    4. Extend `src/instrumentation.test.ts`: beforeSend scrubs a synthetic event carrying a token
       url in request.url + a breadcrumb (assert the placeholder replaces every occurrence — and
       assert on the OUTPUT event, not on config presence); onRequestError called with a
       token-path request produces extra.path with the placeholder and no 43-char base64url run;
       the boot check logs in a stubbed production env with the secret absent and stays silent
       with it present. Neuter one scrub assertion (bypass the scrubber call), observe RED,
       restore.
    5. Honesty boundary, recorded in the SUMMARY and handed to UAT: a config-level test proves the
       WIRING, not the effect. The 164-CONTEXT.md mandate — trigger a REAL error on a token URL in
       a deployed environment and read the captured event's URL fields — cannot run pre-merge;
       it is the phase's UAT item, not a skipped step.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scrub-share-path.test.ts src/instrumentation.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    Scrubber pinned by vectors; every Sentry-bound URL field routes through it; boot check loud in
    prod-without-secret and silent otherwise; the real-event verification recorded as an explicit
    UAT item.
  </done>
</task>

<task type="auto">
  <name>Task 2: Referrer, Plausible, and recipient analytics — the browser-side channels</name>
  <files>next.config.ts, src/app/layout.tsx, src/app/factsheet/[id]/v2/factsheet-analytics.ts</files>
  <action>
    1. `next.config.ts`: in headers(), add a route-scoped block for the share route path pattern
       (source "/factsheet-share/:path*") setting Referrer-Policy to no-referrer. The global
       strict-origin-when-cross-origin at :79 stays for everything else. Rationale comment: a path
       token survives the global policy on same-origin navigation and (unlike a query token) is
       the very thing cross-origin policies were never going to help with — no-referrer closes
       both and removes all dependence on spec nuance.
    2. `src/app/layout.tsx` (:92-96): the Plausible script records PATHNAMES site-wide — under
       D-01 that sends a raw capability token to a third-party host on every recipient view, the
       one genuinely NEW channel. Mitigate via Plausible's exclusions script extension: change the
       script src to the tagged-events + exclusions composite build and add a data-exclude
       attribute covering "/factsheet-share/*". ⚠️ The exclusions mechanics are [ASSUMED] in
       RESEARCH (network was down that session): VERIFY against the current Plausible script
       extensions docs before editing (extension composition + wildcard semantics). If the docs
       contradict the mechanism, do NOT ship a guess: fall back to evaluating conditional script
       omission and surface a checkpoint with the finding. Record the doc verification (source +
       date) in the SUMMARY.
    3. `src/app/factsheet/[id]/v2/factsheet-analytics.ts`: recipient-mode suppression — in the
       event tracker, early-return when the browser pathname starts with the share route prefix
       (structural, path-derived — no prop threading to drift, mirroring how the route itself
       implies share mode). Note in a comment: the CSP's connect-src happens to omit the PostHog
       host at HEAD, but a CSP accident is not a mitigation (Pitfall 6) — this suppression is.
       Colocated test: tracker no-ops under a share-route pathname (stub window.location),
       fires normally elsewhere; neuter → RED → restore.
    4. Update the layout's rendered-output test (or add one colocated with existing layout tests):
       the script tag carries the exclusions build src and the data-exclude value covering the
       share route; assert on rendered markup, not on prose.
  </action>
  <verify>
    <automated>set -o pipefail; npx vitest run src/app/layout src/app/factsheet/[id]/v2/factsheet-analytics --silent 2>&1 | tail -15 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    Per-route no-referrer present beside the untouched global policy; Plausible exclusion shipped
    against verified docs (or checkpointed if unverifiable); recipient pageviews fire no product
    analytics; build green.
  </done>
</task>

<task type="auto">
  <name>Task 3: The ORDERED adversarial cache test — SHARE-02's acceptance, RED first</name>
  <files>src/app/factsheet-share/[token]/page.cache-isolation.test.tsx</files>
  <action>
    The phase's headline acceptance (ROADMAP success criterion 3), built on the T-148-04 template
    (page.owner-lane.test.tsx item 5 — read it first; reuse its mocking approach: mock the Next
    cache module so the wrapper's invocations are observable, mock supabase clients with a
    mutable STATE fixture).

    New colocated file `page.cache-isolation.test.tsx` importing BOTH page modules (the token
    route and the id route):
    1. ORDER IS THE TEST: first, render the TOKEN page for an UNPUBLISHED strategy with a valid
       derived token (fixture secret) and assert the render succeeds AND the mocked cache wrapper
       was invoked ZERO times — the token lane must produce zero cache reads and zero cache
       writes at the id key (SL-1; a key suffix is not a key, so the only acceptable count is
       zero, not "a different key").
    2. Then, same test, same spies uncleared: render the ID route as an anonymous session for the
       SAME id and assert it rejects with notFound — the published probe misses because no
       token-lane artifact populated anything an anonymous reader could hit. Assert the cache
       mock is STILL at zero invocations (the anon miss 404s before the cache, and nothing the
       token render did changed that).
    3. A second case: token render for the unpublished id, then a PUBLISHED sibling id through the
       public lane invokes the cache exactly once with keyParts containing only that published id
       — pinning that the token lane didn't shift the public lane's key shape.
    4. Anti-vacuity, the phase's hardest rule (demonstrate and RECORD in the SUMMARY, then
       restore by re-editing — never a file-level checkout, which destroys uncommitted work):
       neuter the bypass by temporarily wiring the token page's payload fetch through a
       Next-cache-wrapped call keyed on the id (the exact poisoning D-01's structural argument
       prevents) and observe THIS test go RED on the zero-invocation assertion — proving the test
       detects a real poisoning, not an import accident. Also observe the phase-148 token-lane
       structural row (from plan 164-01) go RED on the same neutered tree — two independent
       detectors, one defect. Restore, re-run green.
    5. Confirm the deferred-idea fences hold on the final tree: the OG image route and the
       tearsheet/PDF routes have empty diffs this phase (D-04, SL-1d — a token-aware OG route
       would put a CDN-cached, un-revocable image of a private strategy behind a week-long
       stale-while-revalidate). Verify with git status/diff over those paths, not by memory.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet-share/[token]/page.cache-isolation.test.tsx" src/__tests__/phase-148-owner-lane-cache-isolation.test.ts</automated>
  </verify>
  <done>
    Ordered adversarial test green on the real tree, RED (both detectors) on the neutered tree,
    restoration verified; OG/tearsheet/PDF diffs empty.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipient browser → third-party analytics hosts | pathname (and thus a path token) crosses here unless excluded |
| server errors → Sentry | request paths cross into a third-party event store |
| token route responses → shared caches | must never be cacheable or key-shared with the id lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164-01 | Information Disclosure | id-keyed cache poisoning via the token lane | critical | mitigate | the ORDERED adversarial test + phase-148 structural rows, both demonstrated RED under neutering |
| T-164-02 | Information Disclosure | token → Plausible/PostHog/Sentry/referrers | high | mitigate | exclusions build + data-exclude; recipient tracker no-op; path scrubber on every Sentry URL field; per-route no-referrer |
| T-164-17 | Information Disclosure | server/CDN/platform access logs; browser history; link unfurlers | low | accept | operator-side or client-side channels; revocability (generation bump) is the designed mitigation; unfurls are deliberately dull (static metadata, plan 164-01) |
| T-164-09 | DoS/honesty | silent prod secret misconfig | medium | mitigate | register() boot check, loud, never the warn-only list |

No packages installed by this plan.
</threat_model>

<verification>
- All three task verifies green; both neutering demos recorded with their RED output and restoration.
- Full-suite arbiter at phase gate: `npm run test` (serialized — full vitest must not share the box), `npm run lint`, `npx tsc --noEmit`.
- <human-check>Post-deploy UAT (cannot run pre-merge): (1) trigger a real error on a token URL in the deployed env and read the Sentry event — every URL field shows the [token] placeholder, none shows a 43-char segment; (2) open a token link with the network panel filtered to the analytics host — no pageview request carries the token path.</human-check>
</verification>

<success_criteria>
- SHARE-02 accepted: ordered adversarial isolation proven, RED-demonstrated, restored.
- Every leak channel from the re-derived table has a shipped mitigation or an explicit, recorded acceptance.
- The two [ASSUMED] third-party behaviors verified against current docs at execution time, or checkpointed.
</success_criteria>

<output>
Create `.planning/phases/164-share-copy-link-always-works-and-never-discloses/164-05-SUMMARY.md` when done.
</output>
