---
phase: 164-share-copy-link-always-works-and-never-discloses
plan: 04
type: execute
wave: 2
depends_on: ["164-01"]
files_modified:
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/page.tsx
  - src/components/strategy/ShareableLink.tsx
  - src/app/(dashboard)/strategies/page.tsx
  - src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx
autonomous: true
requirements: [SHARE-01, SHARE-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Copy Link on a PUBLISHED strategy still yields /factsheet/<id>?share=1, byte-identical to today — no token (D-09)"
    - "Copy Link on an UNPUBLISHED strategy the owner views mints-or-reuses and copies the token url; a mint or copy failure surfaces an error state, NEVER the success flash (SHARE-04)"
    - "The revoke control lives on the factsheet with the inline-confirm shape; StrategyActions is byte-unchanged (D-03)"
    - "ONE shared predicate decides the share affordance at all three sites — FactsheetView, strategies page, discovery detail (SHARE-04)"
    - "OwnerUnpublishedNotice no longer claims 'anyone else sees a 404' when a live share link exists (SHARE-04)"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "Status-aware ShareLinkButton, factsheet revoke control with inline confirm, share-state-aware notice copy"
    - path: "src/components/strategy/ShareableLink.tsx"
      provides: "The ONE shared affordance component/predicate all three sites consume"
  key_links:
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "/api/strategies/[id]/share"
      via: "mint-or-reuse POST on Copy Link click for unpublished strategies (contract in plan 164-03's objective)"
      pattern: "strategies/.*/share"
    - from: "src/app/factsheet/[id]/v2/page.tsx"
      to: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      via: "owner lane threads publication status + hasActiveShare as owner-lane props (never on the payload)"
      pattern: "hasActiveShare"
---

<objective>
SHARE-04: the share affordance becomes honest as a CLASS. The defect, stated precisely
(164-CONTEXT.md verified corrections): ShareLinkButton takes only strategyId, does not know
publication status, and builds the query-param share url unconditionally — so an unpublished
strategy's owner copies a link that 404s for its recipient. End state is TWO mechanisms (D-09):
published keeps today's url untouched; unpublished mints-or-reuses the token url. Revoke lives on
the factsheet (D-03). One predicate across all three affordance sites.

**Mint/revoke response contract this plan builds against (defined in plan 164-03's objective,
repeat verbatim):** POST /api/strategies/{id}/share → 200 `{ url }`; POST
/api/strategies/{id}/share/revoke → 200 `{ revoked: true }`, 404 = convergence (treat as
success). Tests mock fetch — no runtime dependency on plan 164-03.

Purpose: SHARE-04 in full, and SHARE-01's owner half (the control that hands out the working URL).
Output: status-aware Copy Link, factsheet revoke control, corrected notice copy, one shared
affordance predicate across three sites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164-share-copy-link-always-works-and-never-discloses/164-CONTEXT.md
@src/app/(dashboard)/allocations/components/SavedScenariosList.tsx
@src/components/strategy/ShareableLink.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread share state through the owner lane and branch ShareLinkButton (D-09)</name>
  <files>src/app/factsheet/[id]/v2/page.tsx, src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <action>
    1. `v2/page.tsx` owner lane (the branch around :563-573 — touch ONLY the owner arm's
       surroundings; the cached wrapper, its callback, and generateMetadata are pinned by the
       phase-148 gate and must stay byte-identical): after the owner probe succeeds, read the
       strategy's share row on the REQUEST-scoped client (owner RLS permits): select generation +
       revoked_at from strategy_shares by strategy_id, maybeSingle, via the cast-through-unknown
       pattern until types regen. Derive hasActiveShare = row exists AND revoked_at is null. A
       read error degrades to hasActiveShare false with a server-side log (logged-degrade
       discipline) — never a crash on the owner's own factsheet.
       ⛔ Do NOT import the token module into page.tsx: deriving the url server-side would put the
       module-load secret assert on the whole id route's import graph. The url is obtained by the
       CLIENT calling mint (idempotent — same url back every time, that is D-02's reuse payoff).
       Thread publication status (already on the strategy row) + hasActiveShare to FactsheetView
       as owner-lane props (NEVER fields on FactsheetPayload — payload lane-state would be cached
       and served to anonymous readers for the full TTL; the existing OwnerLaneProps doc block
       states this rule).
    2. `FactsheetView.tsx` ShareLinkButton (:1483-1515): give it the publication status it
       currently lacks and branch (the actual work named by 164-CONTEXT.md):
       - published → today's behavior BYTE-IDENTICAL: rebuild origin + pathname + the share query
         param, copy, success/failure states unchanged. Add a test pinning the produced url shape
         so a regression to the token branch cannot ship silently.
       - unpublished (owner is viewing, so the button only renders on owner/scenario-free mounts)
         → POST the mint endpoint, then write the returned url to the clipboard IN THE SAME
         handler (mint happens over the network BEFORE the clipboard write — the async wrinkle);
         reuse ShareableLink's honest copy-failure discipline: any mint failure OR clipboard
         failure surfaces the failure state and NEVER the success flash (SHARE-04's core
         sentence: no "Link copied!" for a link that cannot work). Button label reflects state per
         the SavedScenariosList machine: no active share → mint-and-copy affordance; active →
         copy-again affordance (same url).
    3. `FactsheetView.tsx` revoke control (D-03 — on the factsheet, near the share control, owner
       lane only; `StrategyActions` is NOT touched, its private fall-through stays): render only
       when hasActiveShare. Click → the INLINE confirm shape from SavedScenariosList :599-619
       (confirm sentence + danger Revoke + ghost "Keep link" buttons; never window.confirm; copy
       may mirror "Revoke this share link? Anyone with the link will lose access."). Confirm →
       POST the revoke endpoint; 200 AND 404 both converge to the revoked UI state (copy the
       404-as-convergence comment argument from SavedScenariosList :333-341); local state flips
       immediately, the next mint click starts a fresh link (advanced generation).
    4. `OwnerUnpublishedNotice` (defined in FactsheetView.tsx :693-709, rendered from
       FactsheetView and from the page's pending fallback at :605 — one component, one edit):
       accept an optional hasActiveShare prop, thread it from both render sites, and branch the
       second sentence: with NO active share, keep the existing meaning (only you can see this;
       anyone else gets a 404 until published — now also mention you can create a private share
       link); with an ACTIVE share, the 404 claim is FALSE and must not render — state instead
       that a private share link is live and anyone holding it can view this factsheet until
       revoked. Both variants must remain TRUE sentences at all times (the honesty class this
       milestone exists for).
    5. Colocated tests (FactsheetView test files): published branch url pin; unpublished branch —
       mint success → clipboard write with the returned url + success state; mint failure → error
       state and NO success flash (neuter once: force the failure arm to show success, observe
       RED, restore); revoke inline-confirm renders and 404 converges; notice copy — with
       hasActiveShare true the 404 sentence is absent and the live-link sentence present, with
       false the inverse (string assertions on rendered output).
  </action>
  <verify>
    <automated>set -o pipefail; npx vitest run "src/app/factsheet/[id]/v2" --silent 2>&1 | tail -20 && npx vitest run src/__tests__/phase-148-owner-lane-cache-isolation.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    Owner sees status-correct Copy Link, a working revoke with inline confirm, and a notice that
    never asserts a false 404 claim; every phase-148 pin still green (the page edit stayed clear
    of the pinned regions).
  </done>
</task>

<task type="auto">
  <name>Task 2: One predicate, three sites — converge the affordance class (SHARE-04)</name>
  <files>src/components/strategy/ShareableLink.tsx, src/app/(dashboard)/strategies/page.tsx, src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx</files>
  <action>
    The class fix (feedback: close the whole class across the surface, not point-fixes). Direction
    per FEATURES.md (still governing after A-D1): always-visible, always-enabled, mint-or-reuse —
    never hidden, never disabled.

    1. Extend `ShareableLink.tsx` to be the ONE shared affordance: accept the strategy's
       publication status (required prop) and branch on the SAME predicate as ShareLinkButton —
       published → copy the public factsheet url exactly as today; unpublished → mint-or-reuse
       via the mint endpoint then copy the token url, with the honest failure discipline the
       component already models (its copy-failure branch is the house precedent — extend it to
       cover mint failure identically). Extract the predicate itself (published → public id url,
       else → token mint) into one small exported helper in the same file so the three sites
       cannot drift; ShareLinkButton (Task 1) uses the same decision shape — reference this
       helper from a comment there if direct reuse is impractical across client components.
    2. `strategies/page.tsx` (~:174-175): the row currently renders the share control ONLY for
       published rows — the hidden-affordance arm of the dishonesty class. Drop the status gate
       and always render ShareableLink with the row's status: every row on this owner-only page
       is the owner's own, so unpublished rows now yield a working private link instead of no
       affordance. ⛔ `StrategyActions` in the same row is untouched (D-03).
    3. Discovery detail (~:196): already renders ShareableLink under the page's published-only
       gate — pass the now-required status prop (always the published branch here). No behavior
       change; the site now consumes the same component contract, which is the point (one
       predicate, three sites).
    4. Colocated tests: ShareableLink published branch unchanged (url pin); unpublished branch
       mints then copies and never flashes success on failure; strategies page renders the
       affordance for EVERY status (assert an unpublished fixture row gets the control); a
       repo-level drift pin — the strategies page and discovery detail both render ShareableLink
       (not a bespoke copy control), asserted by import/usage in the two page sources.
  </action>
  <verify>
    <automated>set -o pipefail; npx vitest run src/components/strategy src/app/\(dashboard\)/strategies --silent 2>&1 | tail -20 && npx tsc --noEmit</automated>
  </verify>
  <done>
    All three affordance sites route through one component/predicate; unpublished rows on the
    strategies page carry a WORKING share control; discovery detail behavior unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Class-honesty regression sweep + human-check handoff</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <action>
    Close the loop on the SHARE-04 sentence-level claims:
    1. Re-read every rendered sentence this phase touched (notice variants, revoke confirm, share
       button states, the gone page copy from plan 164-01) against the rule that each must be a
       TRUE statement in every reachable state; fix any conditional that can render a false claim
       (e.g. the live-link sentence while revoked_at is set).
    2. Confirm the recipient suppression composes with this plan's additions: with recipientShare
       true (plan 164-01's prop), neither the new revoke control nor either Copy Link branch
       renders — a token-link recipient must never see a Copy-Link control (SHARE-04; the
       recipient rebuild-and-reshare hazard the verified corrections name). Add the assertion to
       the FactsheetView recipient-mode test if plan 164-01's version does not already cover the
       revoke control.
    3. Run the full FactsheetView + page test set and the phase-148 gate one final time from this
       plan's tree.
  </action>
  <verify>
    <automated>set -o pipefail; npx vitest run "src/app/factsheet/[id]/v2" --silent 2>&1 | tail -20</automated>
    <human-check>End-of-phase UAT, owner side: on an UNPUBLISHED strategy's factsheet — Copy Link produces a link that works in a private window; the notice describes the live link truthfully; Revoke shows the inline confirm (no browser dialog), and after revoking, the copied link lands on the dead-link 410 page. On a PUBLISHED strategy — Copy Link still copies the plain factsheet url with the share param, exactly as before this phase.</human-check>
  </verify>
  <done>
    Every user-facing sentence in the share surface is true in every reachable state; recipient
    mode suppresses all owner share controls; full local test set green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner browser → mint/revoke endpoints | the only writers; UI must never fabricate success |
| owner UI state → recipient rendering | owner-lane props must never reach the cached payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164-01 | Information Disclosure | owner-lane props leaking into the cached payload | critical | mitigate | share state threaded as OwnerLaneProps only; phase-148 gate re-run in-plan |
| T-164-15 | Spoofing (of success) | "Link copied!" for a dead or unminted link | medium | mitigate | mint/copy failure arms surface errors; neutered-RED-restored test on the failure arm |
| T-164-16 | Information Disclosure | recipient re-sharing another tenant's capability via a visible Copy Link | medium | mitigate | recipientShare suppresses every share control; asserted in tests |

No packages installed by this plan.
</threat_model>

<verification>
- FactsheetView/page test set green; phase-148 gate green from this tree.
- Neutered-RED-restored demo on the mint-failure honesty pin recorded in the SUMMARY.
- Human-check items handed to end-of-phase UAT.
</verification>

<success_criteria>
- SHARE-04 complete: honest affordance class at three sites, corrected notice, no false success,
  recipient sees no share controls.
- D-03 honored: revoke on the factsheet; StrategyActions diff is empty.
- D-09 honored: the published lane's url is byte-identical to before the phase.
</success_criteria>

<output>
Create `.planning/phases/164-share-copy-link-always-works-and-never-discloses/164-04-SUMMARY.md` when done.
</output>
