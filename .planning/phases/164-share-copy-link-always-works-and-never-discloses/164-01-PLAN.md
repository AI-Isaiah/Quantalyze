---
phase: 164-share-copy-link-always-works-and-never-discloses
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/factsheet/fetch-and-build-payload.ts
  - src/app/factsheet/[id]/v2/page.tsx
  - src/__tests__/phase-148-owner-lane-cache-isolation.test.ts
  - src/lib/strategy-share-token.ts
  - src/lib/strategy-share-token.test.ts
  - src/app/factsheet-share/[token]/page.tsx
  - src/app/factsheet-share/[token]/page.test.tsx
  - src/app/factsheet-share/gone/route.ts
  - src/app/factsheet-share/gone/route.test.ts
  - src/proxy.ts
  - src/lib/routing/route-contract-manifest.ts
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/test-setup.ts
  - .env.example
  - .github/workflows/ci.yml
autonomous: true
requirements: [SHARE-01, SHARE-02]
user_setup:
  - service: vercel
    why: "SHARE_TOKEN_SECRET signs every share token. It is a NEW prod-only env var: a missing value must fail LOUD (build/boot), never at the founder's first Copy Link click."
    env_vars:
      - name: SHARE_TOKEN_SECRET
        source: "Generate >=32 chars locally (e.g. openssl rand -base64 48). Add in Vercel -> Settings -> Environment Variables for ALL environments, and to .env.local for dev. Rotating it revokes EVERY outstanding share link (global kill-switch, by design)."

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An anonymous recipient opening /factsheet-share/<valid token> sees the unpublished strategy's HTML factsheet (D-01, D-04)"
    - "A recipient on the token route sees NO Copy-Link control and NO owner chrome — share mode is structural, not query-sniffed"
    - "An unknown/malformed/revoked token ends at a genuine HTTP 410 with Cache-Control no-store and a content-free body (D-08)"
    - "The token lane performs ZERO reads and ZERO writes at the id-keyed factsheet cache entry — structurally, because its module cannot reach the wrapper (SL-1)"
    - "A missing or short SHARE_TOKEN_SECRET fails LOUD with a named remedy at module load, never silently at first share (D-02)"
  artifacts:
    - path: "src/lib/factsheet/fetch-and-build-payload.ts"
      provides: "The ONE payload builder both lanes import — verbatim move of the page-private function (D-06)"
      exports: ["fetchAndBuildPayload", "StrategyVisibility"]
    - path: "src/lib/strategy-share-token.ts"
      provides: "HMAC + generation token derivation/verification, module-load secret assert (D-02)"
      exports: ["deriveShareToken", "verifyShareToken"]
    - path: "src/app/factsheet-share/[token]/page.tsx"
      provides: "Recipient RSC route: force-dynamic, nodejs, limiter-first, bounded constant-time scan (D-01, D-07)"
    - path: "src/app/factsheet-share/gone/route.ts"
      provides: "The 410 emitter: content-free, no-store, noindex (D-08)"
  key_links:
    - from: "src/app/factsheet-share/[token]/page.tsx"
      to: "src/lib/factsheet/fetch-and-build-payload.ts"
      via: "direct call with a local identity visibility predicate — the token match IS the authorization"
      pattern: "fetchAndBuildPayload\\("
    - from: "src/app/factsheet/[id]/v2/page.tsx"
      to: "src/lib/factsheet/fetch-and-build-payload.ts"
      via: "import from the canonical home; cached callback text unchanged"
      pattern: "@/lib/factsheet/fetch-and-build-payload"
    - from: "src/app/factsheet-share/[token]/page.tsx"
      to: "src/app/factsheet-share/gone/route.ts"
      via: "redirect() on every token-lane miss class"
      pattern: "factsheet-share/gone"
    - from: "src/proxy.ts"
      to: "src/lib/routing/route-contract-manifest.ts"
      via: "PUBLIC_ROUTES entry + manifest entries must land in the same commit or npm run lint reddens"
      pattern: "factsheet-share"
---

<objective>
TRACER — wire the whole recipient token lane end-to-end: extract the payload builder to its
canonical lib home (D-06), create the HMAC+generation token module (D-02), and ship the
`/factsheet-share/[token]` recipient route (D-01) with its 410 sibling (D-08), proxy wiring, and
recipient-mode rendering — proving the phase's architecture on one production-quality path before
any expansion plan runs.

**Decision map used across this phase** (source: 164-CONTEXT.md — all CLOSED, do not re-open):
- D-01 = A-D1: separate route `/factsheet-share/<token>`, never a query param on the id route
- D-02 = token model: HMAC + stored generation counter; never a token at rest; `SHARE_TOKEN_SECRET` fails loud at module load
- D-03 = A-D2: revoke lives on the factsheet; `StrategyActions` unchanged; no publish flow grows
- D-04 = A-D3: HTML factsheet ONLY; tearsheet/PDF routes stay 404 for recipients
- D-05 = Phase-29 guard scope amendment (never rename the migration to dodge it)
- D-06 = extract `fetchAndBuildPayload` to `src/lib/factsheet/`; re-point the phase-148 guard to pin the MODULE; `buildFactsheetPayloadCached` stays pinned to the page
- D-07 = token lookup: bounded constant-time scan, rate-limited FIRST, UNIQUE(strategy_id), explicit revisit threshold recorded
- D-08 = 410 via `redirect()` to a sibling route handler; content-free; extra hop flagged in UAT
- D-09 = TWO share mechanisms is the END STATE: published → `/factsheet/<id>?share=1` UNCHANGED; unpublished → token route. Never delete the `?share=1` path.

Purpose: SHARE-01 (a URL the recipient can view) and SHARE-02's structural half (the token lane
physically cannot touch the id-keyed cache).
Output: the seam module, the token module, the recipient route + 410 route, proxy/manifest wiring,
recipient-mode FactsheetView props, and colocated tests — all green except the intentionally-RED
demonstrations, which are restored.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164-share-copy-link-always-works-and-never-discloses/164-CONTEXT.md
@.planning/phases/164-share-copy-link-always-works-and-never-discloses/164-RESEARCH.md
@src/app/scenario-share/[token]/page.tsx
@src/lib/demo-pdf-token.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open the seam — move fetchAndBuildPayload to src/lib/factsheet/ and re-point the phase-148 guard (D-06)</name>
  <files>src/lib/factsheet/fetch-and-build-payload.ts, src/app/factsheet/[id]/v2/page.tsx, src/__tests__/phase-148-owner-lane-cache-isolation.test.ts</files>
  <action>
    Per D-06 (founder FINAL ruling, 164-CONTEXT.md Blocker 2): a one-function VERBATIM move, not a
    refactor. Count gate tokens PRE-EDIT (record current counts of both builder identifiers in
    src/ production files before touching anything).

    1. Create `src/lib/factsheet/fetch-and-build-payload.ts`. Move, byte-for-byte where possible:
       the `StrategyVisibility` type (currently `v2/page.tsx:47`, with its "the injected predicate
       is the ONLY gate" doc block) and the full `async function fetchAndBuildPayload(id: string,
       visibility: StrategyVisibility): Promise<FactsheetPayload | null>` declared at
       `v2/page.tsx:83-312`. Export both. Carry over exactly the imports its body needs
       (createAdminClient, isComputedAnalytics, resolveDailyReturnSeries, deriveIngestSource,
       readCompositeFactsheet, readSingleKeyBasisOpts, singleKeyDataQuality,
       buildFactsheetPayload, displayStrategyName, FactsheetPayload type). The new module must
       import NOTHING from `v2/page.tsx` and must not import Next's data-cache wrapper — the
       token lane's SL-1 argument is that this module has no cache reach at all.
    2. In `v2/page.tsx`: delete the moved type + function; add the import from
       `@/lib/factsheet/fetch-and-build-payload`. Everything else in the page is byte-unchanged —
       in particular the cached callback at :331 must still read exactly
       `fetchAndBuildPayload(id, withPublishedOnly)` (the phase-148 literal pin parses that text),
       the wrapper `buildFactsheetPayloadCached` (:314) stays in the page with its unchanged
       declaration head, and the owner arm at :572 keeps its lambda call.
    3. Re-point the phase-148 guard (`src/__tests__/phase-148-owner-lane-cache-isolation.test.ts`).
       The OWN-02 repo-walk (its `offenders()` at ~:358-373) currently bans BOTH builder names
       outside the page. Per D-06 the guard bans TWO tokens and only ONE moves:
       - `buildFactsheetPayloadCached`: rule UNCHANGED — no production file other than
         `src/app/factsheet/[id]/v2/page.tsx` may mention it.
       - `fetchAndBuildPayload`: new MODULE pin — (a) exactly ONE production file contains a
         `function fetchAndBuildPayload` declaration and it is
         `src/lib/factsheet/fetch-and-build-payload.ts`; (b) every OTHER production file whose
         comment-stripped source mentions the identifier must also contain the literal import
         specifier `@/lib/factsheet/fetch-and-build-payload` (canonical-home imports only; a copy
         or re-definition elsewhere is an offender). This is stronger than a two-caller
         allow-list: any new consumer is legal only via the module, and a duplicate builder is
         structurally impossible.
       - Keep every other pin intact (exactly-one data-cache call site in the page; callback
         literal; wrapper-head negative pins; generateMetadata pin; force-dynamic pin) and keep
         the non-vacuity fences (walk >100 files; the types.ts comment-stripping fence). Update
         the file's header prose to record this reviewed amendment with today's date and D-06.
    4. Anti-vacuity (record both demos + restoration in the SUMMARY): (a) create a temporary
       production file under src/ that mentions `fetchAndBuildPayload` WITHOUT the canonical
       import specifier → run the guard → observe RED → delete the file; (b) create a temporary
       production file mentioning the wrapper name → RED → delete. Restore by deleting the temp
       files (never `git checkout --`, which destroys uncommitted work).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-148-owner-lane-cache-isolation.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    All phase-148 pins green on the moved tree; both RED demos observed and restored; `v2/page.tsx`
    diff is import + deletion only (builder body byte-identical in its new home); no production
    file defines a second builder.
  </done>
</task>

<task type="auto">
  <name>Task 2: The token module — HMAC + generation, loud at module load (D-02)</name>
  <files>src/lib/strategy-share-token.ts, src/lib/strategy-share-token.test.ts, src/test-setup.ts, .env.example, .github/workflows/ci.yml</files>
  <action>
    New module per D-02 — SEPARATE from the scenario token module (never import or extend
    `src/lib/scenario-share-token.ts`: its test pins its digest, and one namespace serving two
    resources invites cross-resource replay). Mechanism precedent: `src/lib/demo-pdf-token.ts`
    (createHmac sha256 + timingSafeEqual), but deviate on validation timing: demo-pdf validates
    lazily, which is exactly the silent-prod-misconfig class this phase removes. Full module
    contract sketch: 164-RESEARCH.md §Q3 — follow it, with one correction below.

    1. `src/lib/strategy-share-token.ts`:
       - Read the secret as a LITERAL `process.env.SHARE_TOKEN_SECRET` expression (not indexed
         through a const) so the env-manifest contract gate discovers the read. Validate at
         MODULE SCOPE: missing or <32 chars throws with a named remedy (set in Vercel env for all
         environments + `.env.local`; note that rotation revokes every outstanding link). This
         module-scope throw is the D-02 hard stop: every share surface fails loudly and
         identically instead of the founder discovering it at first click.
       - `deriveShareToken(strategyId, generation)`: HMAC-SHA256 over the serialization
         `strategyId + "." + generation`, base64url digest (43 chars). The dot separator is the
         pinned serialization (D-02's formula with an explicit separator).
       - `verifyShareToken(presented, strategyId, generation)`: format-guard with the 43-char
         base64url regex FIRST, then constant-time compare via timingSafeEqual (lengths equalized
         by the regex — the demo-pdf validate-before-Buffer discipline).
       - Header comment: this is a STATEFUL keyed MAC (the stored generation counter is what makes
         revocation possible) — state this explicitly so a reviewer does not read it as
         contradicting the scenario module's documented rejection of a STATELESS MAC.
       - Record the D-07 revisit threshold in the header: the bounded verification scan (Task 3)
         is revisited when active share rows exceed 1,000; the O(1) alternative (a self-locating
         token) extends the token format and needs founder sign-off.
    2. `src/lib/strategy-share-token.test.ts` (colocated, mirroring the scenario token test's
       shape): with the fixture secret, pin (a) a LITERAL precomputed digest vector for a fixed
       UUID + generation 1 (compute it once while writing the test; a silent algorithm/encoding
       change must go RED); (b) determinism (same inputs → same token); (c) generation 1 vs 2 →
       different tokens (the revocation mechanism itself); (d) different secret → different token;
       (e) cross-namespace pin: the new module's output for a given input differs from the
       scenario module's hash of the same input (anti-replay across resources); (f) format guard
       rejects wrong-length and non-base64url input without reaching the compare; (g) the
       module-load throw: import the module fresh (vi.resetModules + dynamic import) with the
       secret deleted → expect the named error. Demonstrate (g) is non-vacuous by the
       neuter→RED→restore protocol on one assertion.
    3. `src/test-setup.ts`: set a >=32-char fixture value for the secret with a nullish-coalescing
       assignment BEFORE the `INHERITED_ENV` snapshot (~line 76) so the env-restore fence treats
       it as baseline and every test file can import the module without throwing.
    4. `.env.example`: add SHARE_TOKEN_SECRET with a one-line comment (>=32 chars; rotating kills
       all outstanding share links). This plus the literal read keeps BOTH directions of
       `src/__tests__/contracts/env-manifest.test.ts` green. If the Edit tool is denied on
       .env.example (env-file guard), STOP and surface a checkpoint — do not skip and do not park
       the key in the manifest test's exemption lists.
    5. `.github/workflows/ci.yml`: both `npm run build` steps (the frontend build with placeholder
       env, ~line 718, and the e2e rebuild, ~line 3047) get a `SHARE_TOKEN_SECRET` entry with an
       obviously-fake >=32-char placeholder value, so a build-time module evaluation of the share
       routes cannot redden CI. Do not touch any other step.
    6. Measure (do not assume) where the fail-loud fires: run `npm run build` locally WITHOUT the
       var — if the build fails naming SHARE_TOKEN_SECRET, that is the deploy-time loud arm;
       if the build passes, start the dev server without the var, request `/factsheet-share/x`,
       and confirm a 500 whose server log names the var and the remedy. Record which arm fired in
       the SUMMARY (Vercel deploy inherits whichever arm the build exhibits).
  </action>
  <verify>
    <automated>npx vitest run src/lib/strategy-share-token.test.ts src/__tests__/contracts/env-manifest.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    Token module green with a literal digest vector, generation divergence, cross-namespace pin,
    and a demonstrated module-load throw; env manifest green in both directions; both CI build
    steps carry the placeholder; the fail-loud arm is measured and recorded.
  </done>
</task>

<task type="tracer">
  <name>Task 3: End-to-end recipient lane — /factsheet-share/[token] renders an unpublished factsheet; every miss is a 410 (D-01, D-04, D-07, D-08)</name>
  <files>src/app/factsheet-share/[token]/page.tsx, src/app/factsheet-share/[token]/page.test.tsx, src/app/factsheet-share/gone/route.ts, src/app/factsheet-share/gone/route.test.ts, src/proxy.ts, src/lib/routing/route-contract-manifest.ts, src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <action>
    ONE path, wired through every layer, production quality. Spine: `/scenario-share/[token]/page.tsx`
    (read it first — copy its SECURITY BOUNDARY header prose style, force-dynamic + nodejs pins
    with the shared-caches-are-URL-keyed comment, limiter-FIRST ordering, service-role transport,
    and error-redaction discipline). Route/flow sketch: 164-RESEARCH.md §Code Examples (recipient
    resolution) — follow it, adjusted to the D-06 final ruling (call the moved builder directly;
    no separate row fetch, no shared column constant needed).

    1. `src/app/factsheet-share/[token]/page.tsx` (RSC, force-dynamic, runtime nodejs):
       a. Rate limit FIRST (D-07: before any DB or crypto work): getClientIp over headers() →
          checkLimit(publicIpLimiter, key prefix `factsheet-share:` + ip). On denial render the
          neutral EmptyStateCard "Please try again shortly" — NOT a 404/410, so the limiter leaks
          no token-existence signal (scenario-share precedent :114-130).
       b. Format-guard the raw token param (43-char base64url regex). Malformed → the same miss
          path as unknown (uniform within the lane).
       c. Bounded constant-time scan (D-07): createAdminClient() →
          select strategy_id + generation from strategy_shares where revoked_at is null (the table
          lands in plan 164-02; type it via the repo's cast-through-unknown pattern with the
          "delete when types regen lands" note — the scenario mint route models it). For each
          candidate row call verifyShareToken (timingSafeEqual inside). Early exit on match is
          acceptable — comparisons are against DIFFERENT candidates, not a byte-prefix oracle on
          one secret; say so in a comment. UNIQUE(strategy_id) bounds the scan at one active row
          per strategy; the module header records the 1,000-active-rows revisit threshold (D-07).
       d. Every miss class — malformed, no match, DB error on the share read (log redacted detail
          server-side, never echo) → `redirect("/factsheet-share/gone")` (D-08). NEVER notFound()
          on this lane: 410 is the token lane's honest status; the bare-id lane keeps its uniform
          404 (existence-oracle asymmetry, 164-CONTEXT.md).
       e. On match: call the moved `fetchAndBuildPayload(match.strategy_id, predicate)` where the
          predicate is a LOCAL named identity const (type StrategyVisibility from the lib module)
          with a loud comment: authorization already happened via the constant-time HMAC match —
          the token IS the authz, so the query carries deliberately NO status predicate. Do NOT
          route anything through the page's cached wrapper, do NOT generalize it (SL-1b), and do
          NOT import anything from `v2/page.tsx`.
       f. A null build result with a VALID token is "still computing", not a dead link: render an
          honest pending EmptyStateCard (content-free of metrics; no owner chrome) — never the 410
          (Pitfall 7).
       g. On payload: render FactsheetView with the payload plus the new recipient props (step 3).
          Response must not be cacheable: the force-dynamic + no-store discipline of the spine.
       h. Static `export const metadata`: constant title ("Factsheet — Quantalyze"), generic
          description, robots noindex, NO openGraph/twitter/images and NO reference to the OG
          image route — SL-1d: the token lane ships no OG image, and the scenario-share page's
          missing-metadata gap is a bug, not a pattern (164-RESEARCH.md §Q5). D-04: this page is
          the ONLY tokenized surface — tearsheet and PDF routes stay untouched and 404 for
          recipients.
    2. `src/app/factsheet-share/gone/route.ts`: GET returns status 410 with Content-Type
       text/html, Cache-Control no-store, X-Robots-Tag noindex, and a content-free body whose
       substance is exactly: "This link is no longer active. The person who shared it turned it
       off. Ask them for a new link." NO strategy name, NO metrics, NO id, NO owner identity
       (164-CONTEXT.md). App Router pages cannot emit 410 (verified against the bundled Next docs)
       — this sibling handler is the D-08 ruling. The recipient sees one extra hop in the URL bar;
       flag that in the SUMMARY as a UAT note so it is not read as a bug.
    3. `src/app/factsheet/[id]/v2/FactsheetView.tsx` — structural recipient mode (minimum for the
       tracer; plan 164-04 builds the owner side):
       - Extend the viewerNotice union (~:123) with a "shared_privately" kind rendering a note in
         the OwnerUnpublishedNotice visual shape whose copy states the factsheet is shared
         privately by its owner and is not published or reviewed by Quantalyze.
       - Add a `recipientShare?: boolean` prop threaded like the existing owner-lane props (NEVER
         onto FactsheetPayload — lane state in the payload would be cached and served to everyone,
         the T-150-27 rule). When true: suppress ShareLinkButton at :1741 and apply the existing
         shareMode suppressions (:1742 Compare etc.) — i.e. treat share mode as
         useShareMode() OR recipientShare. Do NOT relocate or modify useShareMode() itself: the
         `?share=1` mechanism on the id route stays byte-identical (D-09).
       - The token page passes viewerNotice "shared_privately" + recipientShare true.
    4. Proxy + route contract (must land in this same commit or `npm run lint` reddens):
       `src/proxy.ts:17` add "/factsheet-share" to PUBLIC_ROUTES (the existing "/factsheet" entry
       does NOT prefix-cover it — matching is exact-or-`route + "/"`); add an
       isFactsheetShareRoute const beside isScenarioShareRoute (~:118) and include it in the
       isAuthBounceExempt union (~:128-137) — omitting the exemption is a live bug (an authed
       owner opening their own link would be 307'd to the dashboard). Add TWO entries to
       `src/lib/routing/route-contract-manifest.ts` mirroring the scenario-share entry at
       :376-381: `/factsheet-share/:token` (class public, in-the-wild link — never move) and
       `/factsheet-share/gone` (class public, the 410 emitter). config.matcher needs no change.
    5. Colocated tests:
       - `page.test.tsx` (mock next/navigation, next/headers, the supabase admin client, the
         limiter — the scenario-share page test is the model): valid derived token (fixture
         secret) → FactsheetView rendered with recipientShare + shared_privately; unknown token →
         redirect to /factsheet-share/gone; malformed token → same redirect WITHOUT any DB call
         (assert the mock client was never invoked); revoked rows excluded from the scan (the
         is-null filter is asserted); rate-limited → neutral card, no DB call, no redirect; valid
         token + null payload → pending card, NOT the gone redirect; metadata pin: the module
         exports static metadata with robots noindex, exports NO generateMetadata, and its source
         never references the OG image route path.
       - `route.test.ts` for gone: status 410, no-store, noindex, body carries the exact copy and
         none of: a UUID shape, a strategy name placeholder, digits beyond the copy.
       - FactsheetView recipient-mode test (colocated with existing FactsheetView tests): with
         recipientShare, ShareLinkButton does not render; without it, behavior is unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/app/factsheet-share/[token]/page.test.tsx src/app/factsheet-share/gone/route.test.ts && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <done>
    The single happy path works end-to-end at the test level: derived token → scan → builder →
    FactsheetView(recipient), and every miss class lands on a genuine 410 + no-store. Proxy +
    manifest wiring green under lint. Committed as one slice.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous internet → /factsheet-share/[token] | untrusted token input; capability check happens here |
| token route → service-role Supabase | admin transport; the HMAC match is the only gate before it |
| build/deploy → SHARE_TOKEN_SECRET | prod-only config; absence must fail loud, not silent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164-01 | Information Disclosure | id-keyed factsheet cache | critical | mitigate | token lane in a module with no cache reach; D-06 module pin; ordered adversarial test (plan 164-05) |
| T-164-03 | Information Disclosure | bare-id lane existence oracle | high | mitigate | 410 confined to the token lane; id lane keeps uniform 404 (byte-unchanged) |
| T-164-04 | Elevation of Privilege | token enumeration | medium | mitigate | 256-bit MAC space + publicIpLimiter 10/60s BEFORE any DB/crypto + neutral limiter denial |
| T-164-06 | Spoofing | cross-resource replay (scenario↔strategy tokens) | medium | mitigate | separate module + cross-namespace test pin; different derivation |
| T-164-09 | DoS/honesty | silent prod misconfig of the secret | medium | mitigate | module-load throw with named remedy; boot check lands in plan 164-05 |
| T-164-10 | Information Disclosure | OG/CDN image of a private strategy | high | mitigate | token page ships NO OG reference; the OG route stays untouched (SL-1d; deferred idea, do not touch) |

No packages are installed by this phase (RESEARCH Package Legitimacy Audit: none) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `npx vitest run` on all new/touched test files green; both RED demonstrations recorded then restored.
- `npm run lint` green (route-contract Rules 1+2 across proxy + manifest; planning hygiene).
- `npx tsc --noEmit` green.
- <human-check>End-of-phase UAT: open a minted link in a private window — factsheet renders with the shared-privately notice and no Copy-Link control; open a garbage token — browser lands on /factsheet-share/gone with the dead-link copy. NOTE for the reviewer: the URL-bar hop to /factsheet-share/gone is the D-08 design, not a bug.</human-check>
</verification>

<success_criteria>
- SHARE-01 recipient half: a valid token renders the unpublished factsheet for an anonymous session (test-level now, UAT at phase end).
- SHARE-02 structural half: the token lane's module has no path to the cached wrapper, and the D-06 module pin makes a second builder impossible.
- All 164-CONTEXT.md rulings D-01, D-02, D-04, D-06, D-07, D-08 implemented exactly; D-09's `?share=1` path untouched.
</success_criteria>

<output>
Create `.planning/phases/164-share-copy-link-always-works-and-never-discloses/164-01-SUMMARY.md` when done.
</output>
