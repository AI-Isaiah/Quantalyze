---
phase: 164-share-copy-link-always-works-and-never-discloses
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/phase-29-frozen-spine-guards.test.ts
  - supabase/migrations/[ts]_strategy_shares_generation_model.sql
  - supabase/tests/test_strategy_shares_rls.sql
  - src/__tests__/audit-coverage.test.ts
  - supabase/schema/functions/create_strategy_share.sql
  - supabase/schema/functions/revoke_strategy_share.sql
autonomous: false
requirements: [SHARE-01, SHARE-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "strategy_shares stores (strategy_id, generation, revoked_at) and NEVER a token, raw or hashed (D-02)"
    - "Revoke is one atomic statement: revoked_at stamped AND generation incremented together; double-revoke converges (SHARE-03)"
    - "anon has NO path to strategy_shares — no table grant, no SECDEF reader (recipient reads ride service_role in Node)"
    - "The phase-29 guard is narrowed to the scenario spine and STILL bites on a scenario-spine migration filename (D-05)"
    - "The SQL gate has NO pre-apply tolerance arm — it is RED until the migration is hand-applied to TEST (SKIP-01)"
  artifacts:
    - path: "supabase/migrations/[ts]_strategy_shares_generation_model.sql"
      provides: "Table + RLS + two SECURITY INVOKER RPCs (mint-or-reuse, atomic revoke)"
      contains: "strategy_shares"
    - path: "supabase/tests/test_strategy_shares_rls.sql"
      provides: "RLS/grant/state-machine gate, RED until TEST hand-apply"
    - path: "src/__tests__/phase-29-frozen-spine-guards.test.ts"
      provides: "Narrowed FORBIDDEN_MIGRATION_RE scoped to the scenario locked set"
  key_links:
    - from: "src/__tests__/audit-coverage.test.ts"
      to: "supabase/migrations/[ts]_strategy_shares_generation_model.sql"
      via: "both new RPC names listed in MUTATING_RPC_NAMES so routes calling them fall under the audit law"
      pattern: "create_strategy_share"
---

<objective>
The storage layer for D-02's generation-counter token model: the `strategy_shares` table with
owner-coherent RLS, a mint-or-reuse RPC and an atomic-revoke RPC, the SKIP-01-clean SQL gate, and
the founder-ruled amendment of the phase-29 frozen-spine guard (D-05) that otherwise reddens on any
migration filename containing the share substring.

Purpose: SHARE-03's revocation semantics live here (generation increment kills every
previously-copied link at once); SHARE-01's reuse lives here (the counter makes the token
re-derivable, so Copy Link returns the SAME url until revoked).
Output: migration + SQL tests + narrowed guard + audit-law listings + regenerated function
snapshot, gated behind the three-reviewer blocking checkpoint before any apply.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164-share-copy-link-always-works-and-never-discloses/164-CONTEXT.md
@.planning/phases/164-share-copy-link-always-works-and-never-discloses/164-RESEARCH.md
@supabase/migrations/20260622120000_scenario_shares_and_read_rpc.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow the phase-29 guard (D-05), then author the migration it would have blocked</name>
  <files>src/__tests__/phase-29-frozen-spine-guards.test.ts, supabase/migrations/[ts]_strategy_shares_generation_model.sql, src/__tests__/audit-coverage.test.ts, supabase/schema/functions/create_strategy_share.sql, supabase/schema/functions/revoke_strategy_share.sql</files>
  <action>
    Order matters: the guard narrowing and the migration must land in the SAME commit, or the
    feature branch's own CI reddens between them. Count gate tokens PRE-EDIT.

    1. GUARD (D-05, founder ruling — ⛔ do NOT rename the migration to dodge the substring):
       `src/__tests__/phase-29-frozen-spine-guards.test.ts:141` currently uses a two-alternative
       regex over changed migration FILENAMES. Narrow it to match only the scenario spine — the
       locked set the guard's own comment names (`scenario_shares`, `get_shared_scenario`,
       `create_scenario_share`) all contain the substring "scenario", so `/scenario/i` covers the
       entire locked set while letting `strategy_shares` pass. Update the adjacent comment: record
       that the second alternative was redundant for the locked set and false-positived on
       strategy_shares, dated today, citing D-05; note the Phase 164.1 cross-reference (164.1
       retires frozen-spine gates — this narrowing is the 164 slice, recorded in ROADMAP so the
       guard is not edited twice with two rationales).
       Anti-vacuity: create an untracked probe file under supabase/migrations/ whose FILENAME
       contains "scenario" → run the guard → observe RED (it still bites on the spine) → delete
       the probe. Record the demo in the SUMMARY.
    2. MIGRATION `supabase/migrations/[ts]_strategy_shares_generation_model.sql` — timestamp must
       sort after the latest migration at execution time (currently 20260826150000). Transpose the
       scenario-shares migration's structure (header prose documenting apply flow, STEP layout,
       CR-01 owner-coherence commentary) with the D-02 deviations 164-RESEARCH.md §Q4 specifies:
       - Table `strategy_shares`: id uuid PK default gen_random_uuid(); strategy_id uuid NOT NULL
         UNIQUE references strategies on delete cascade; created_by uuid NOT NULL references
         profiles on delete cascade; generation integer NOT NULL default 1 check >= 1; created_at
         timestamptz default now(); revoked_at timestamptz. ENABLE ROW LEVEL SECURITY. A FULL
         unique on strategy_id (NOT the scenario table's partial index) — under the generation
         model the row IS the counter, there is nothing per-mint to store, and reactivation
         mutates in place. NO token column of any kind (D-02: never a token at rest — a DB leak
         yields only an id, an int and timestamps). COMMENT ON TABLE documenting the state
         machine: no row → never shared; revoked_at null → live; revoked_at set → revoked with
         generation already advanced; re-share clears revoked_at and the token re-derives from the
         advanced counter so old links stay dead.
       - RLS: one policy for authenticated, FOR ALL, USING created_by = auth.uid(), WITH CHECK
         created_by = auth.uid() AND an EXISTS probe that the strategy row belongs to auth.uid()
         (the CR-01 owner-coherence clause — created_by alone would let any authed user mint for
         another tenant's strategy id). REVOKE ALL on the table from PUBLIC and anon: the
         recipient lane reads via service_role in Node only; there is NO SECDEF reader RPC in this
         design (SQL cannot compute the HMAC — pgcrypto digest is not installed — so the scenario
         reader pattern does not carry over), and therefore NO anon EXECUTE question arises. If
         any SECDEF function is ever added here it takes the REVOKE/GRANT/self-verify treatment.
       - RPC `create_strategy_share(p_strategy_id uuid) RETURNS integer` — SECURITY INVOKER (RLS
         enforces ownership through WITH CHECK), SET search_path, body: single INSERT of
         (strategy_id, created_by = auth.uid()) ON CONFLICT (strategy_id) DO UPDATE SET revoked_at
         = NULL, RETURNING generation. Atomic reactivate-or-insert; never touches created_by or
         created_at on reactivation (this is why the RPC beats a client upsert — RESEARCH
         assumption A4). Grants: REVOKE ALL from PUBLIC, anon; GRANT EXECUTE to authenticated.
       - RPC `revoke_strategy_share(p_strategy_id uuid) RETURNS integer` — SECURITY INVOKER, SET
         search_path, body: single UPDATE setting revoked_at = now() AND generation = generation
         + 1 WHERE strategy_id matches AND revoked_at IS NULL, returning the affected row count.
         The increment must be in-statement (client libraries cannot express an atomic
         column-arithmetic update — this RPC exists precisely for that). Zero rows = already
         revoked or never shared: the ROUTE maps that to 404-as-convergence; the RPC just reports
         the count. Soft-revoke only — never DELETE. Same grants as the mint RPC.
       - House rules: CREATE OR REPLACE (never DROP+CREATE — default-ACL re-grants EXECUTE on
         re-create; it bit a prior migration); these are NEW function names so there is no earlier
         definition to re-base against — state that in the header (the re-base rule satisfied
         vacuously); ON DELETE CASCADE on both FKs means sanitize_user needs no new arm.
    3. AUDIT LAW: add BOTH RPC names to MUTATING_RPC_NAMES in
       `src/__tests__/audit-coverage.test.ts` (~:203-233) with a one-line rationale comment each —
       with a name unlisted the gate never sees the call (the SEC-03 lesson recorded in that
       list). The routes that call them land in plan 164-03 and carry logAuditEvent.
    4. SNAPSHOT: run the schema-functions regeneration script (hermetic text replay — no DB) so
       the two new function snapshot files exist; CI runs the check mode.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-29-frozen-spine-guards.test.ts src/__tests__/audit-coverage.test.ts && npx tsx scripts/dump-sql-functions.ts --check</automated>
  </verify>
  <done>
    Guard narrowed with the RED-on-scenario-probe demo recorded; migration authored with table +
    RLS + two INVOKER RPCs and no token column; both RPC names under the audit law; function
    snapshot regenerated and check-clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: The SQL gate — RED until TEST hand-apply, no tolerance arm (SKIP-01)</name>
  <files>supabase/tests/test_strategy_shares_rls.sql</files>
  <action>
    Write `supabase/tests/test_strategy_shares_rls.sql` in the house sql-tests style, asserting
    content-by-field:
    - anon is blocked at BOTH layers (no table grant, RLS) — a select/insert as anon fails;
    - cross-tenant WITH CHECK rejection: an authenticated user cannot insert a share row for a
      strategy owned by a different uid (the CR-01 clause is load-bearing, not decorative);
    - revoke immediacy: after revoke_strategy_share, the row has revoked_at set AND generation
      advanced by exactly 1, and a second revoke call affects 0 rows (convergence);
    - reactivation: create_strategy_share on a revoked row clears revoked_at without touching
      created_by/created_at and returns the ADVANCED generation (old links stay dead);
    - generation monotonicity: generation never decreases across the mint→revoke→re-mint cycle;
    - no token-shaped column exists on the table (information_schema probe: column set is exactly
      the six DDL columns).
    ⛔ SKIP-01 (164-CONTEXT.md): NO pre-apply tolerance arm. Nothing applies migrations to TEST
    automatically and sql-tests has no apply step, so a "table absent → pass" arm would be
    PERMANENTLY silent in CI. This file is EXPECTED RED until the migration is hand-applied to
    TEST at the checkpoint below — write a header comment saying exactly that, mirroring the
    163-06 shape. The gate avoids going dark precisely by refusing a pre-apply arm: its green is
    only reachable through the hand-apply.
  </action>
  <verify>
    <automated>MISSING until TEST hand-apply — the sql-tests CI lane is the runner and this file is expected RED before the checkpoint below completes; after hand-apply, the sql-tests job (or a local psql run against TEST) must show this file green</automated>
  </verify>
  <done>
    Gate file exists with all six assertion groups and no tolerance arm; header documents
    RED-until-hand-apply.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint — three reviewers, then TEST hand-apply approval</name>
  <what-built>
    The strategy_shares migration (table + RLS + two INVOKER RPCs), the narrowed phase-29 guard,
    and the SKIP-01-clean SQL gate. Merging supabase/migrations to main AUTO-applies to PROD, so
    this stops for a human in every mode.
  </what-built>
  <how-to-verify>
    1. Three reviewers BEFORE any apply (hard repo rule): run migration-reviewer,
       rls-policy-auditor, and silent-failure-hunter over the new migration; fix findings; only
       then proceed.
    2. Approve hand-applying the migration to the TEST project (the sql-tests CI prerequisite).
       After apply, confirm supabase/tests/test_strategy_shares_rls.sql goes green against TEST.
    3. PROD apply happens automatically at merge via the Supabase Migrate workflow — nothing to do
       now beyond approving the TEST apply.
  </how-to-verify>
  <resume-signal>Type "approved" after the three reviews pass and the TEST hand-apply is authorized; describe findings otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated tenant → strategy_shares writes | RLS WITH CHECK is the cross-tenant wall |
| anon → strategy_shares | must be a dead end at grant AND policy layers |
| merge-to-main → PROD schema | auto-apply; the three-reviewer checkpoint is the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164-07 | Information Disclosure | secret-at-rest in strategy_shares | medium | mitigate | no token column of any kind; DB leak yields (uuid, int, timestamps) only (D-02) |
| T-164-08 | Tampering/EoP | cross-tenant mint via forged strategy_id | high | mitigate | RLS WITH CHECK owner-coherence EXISTS clause + SQL test asserting the rejection |
| T-164-05 | Elevation of Privilege | replay after revoke | high | mitigate | atomic generation increment in revoke_strategy_share; SQL test pins monotonicity |
| T-164-11 | Repudiation | share mint/revoke without audit trail | low | mitigate | both RPC names under MUTATING_RPC_NAMES; routes (plan 164-03) carry logAuditEvent |
| T-164-12 | Tampering | default-ACL EXECUTE re-grant to anon on function re-create | medium | mitigate | CREATE OR REPLACE only + explicit REVOKE/GRANT block in the migration |

No packages installed by this plan.
</threat_model>

<verification>
- phase-29 guard green on the real tree, RED on the scenario probe, restored.
- audit-coverage suite green with the two listings.
- Function snapshot check-clean.
- SQL gate authored; green only after the checkpoint's TEST hand-apply (expected order).
</verification>

<success_criteria>
- SHARE-03's storage semantics complete: one atomic revoke statement kills all prior links; reuse
  is re-derivable from the stored counter.
- D-05 executed exactly: guard narrowed, migration filename NOT renamed to dodge, guard proven to
  still bite.
- Three-reviewer + hand-apply checkpoint honored before any apply.
</success_criteria>

<output>
Create `.planning/phases/164-share-copy-link-always-works-and-never-discloses/164-02-SUMMARY.md` when done.
</output>
