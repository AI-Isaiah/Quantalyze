---
phase: 164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire
plan: 04
type: execute
subsystem: sql-gates
tags: [red-under, pg_cron, floors-ratchet, file-move-3, stuck-computing-reaper]
wave: 4
depends_on: ["164.4.1-03"]
files_modified:
  - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-annotation-parser.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
autonomous: true
requirements: [SC-2]
provides:
  - "test_strategy_analytics_stuck_computing_reaper.sql (29 sections, NOTICE-skip shape) annotated and machine-proven — the third FILE move"
  - "Floors and pins ratcheted to the printed values; lane-blocked set pinned at 1"
affects: [164.4.1-05, 164.4.1-06]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 29 sections carry twins the runner drives RED naming that section FIRST, then restores GREEN (SC-2)"
    - "The three pg_cron-conditional skips (:282, :326, :483) print NOTHING on the lane — Part 1b and Parts 2-3 RUN and are falsified, which is the whole reason this NOTICE-shape file was deferred (PATTERNS C1)"
    - "cron.job.command on the lane is the LAST writer's body (20260803130000) — the one TEST holds"
    - "FILES_FLOOR / ARMS_FLOOR equal the printed lines; one higher FIRES; WAIVED_CEILING 0 (D-03)"
    - "lane-blocked pinned as the exact 1-name set (D-04)"
  artifacts:
    - path: "supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql"
      provides: "header with measured skip-sizing paragraph + oracle caveat; one RED-UNDER-SETUP; 29 twins"
    - path: "scripts/mutation-runner/run.mjs"
      provides: "dated derivation blocks; constants at measured values"
    - path: "src/__tests__/mutation-annotation-parser.test.ts"
      provides: "annotatedFiles +1, LANE_BLOCKED_1, arithmetic line, toHaveLength(1)"
    - path: "src/__tests__/mutation-runner-floors.test.ts"
      provides: "totalAnchored re-pinned, eleventh lineage paragraph, GREEN_LOG at the new numbers"
  key_links:
    - from: "supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql Part 2/3 body arms"
      to: "supabase/migrations/20260803130000_reaper_limit_bound_materialized_cte.sql cron.schedule(...)"
      via: "last writer of reap_strategy_analytics_stuck_computing; its MATERIALIZED-CTE body is what the gate reads"
      pattern: "reap_strategy_analytics_stuck_computing"
---

<objective>
Third FILE move: `test_strategy_analytics_stuck_computing_reaper.sql` — 29 sections, the NOTICE-skip spelling. This is the file where a silently-ineffective preload would be INVISIBLE: it baselines green with or without pg_cron (`supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:282-326` and line 483 withhold Part 1b and Parts 2-3 behind RAISE NOTICE SKIP lines), so its twins would come back no-red naming no cause. The apply list must be SIZED so the baseline prints zero skip lines, and the header must say so with the measurement (PATTERNS C1, § 1b).

CONTEXT decisions cited as D-01..D-07. Implements D-01, D-03, D-04.

Job reap_strategy_analytics_stuck_computing is scheduled at `supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql:501` (guarded by the RAISE at 438-442) and RE-scheduled at `supabase/migrations/20260803130000_reaper_limit_bound_materialized_cte.sql:107` (guard 96-100; self-verify 175-184 asserts exactly ONE job row and TWO MATERIALIZED CTEs). The Phase 142.1 delta 20260803120000 is guarded by Part 2 arm D and the trigger-sentinel part. Last writer = 20260803130000.

⚠️ Expected red in this interval: exactly one `lane-blocked-stale` defect naming `test_reconcile_dropped_enqueue_sweep.sql`. `TRIPWIRE-RED:` line in every commit. No PR and no green-expecting `workflow_dispatch` before plan 05 (pushing the branch itself triggers nothing — plan 01 objective).

Purpose: SC-2 for the largest NOTICE-shape file.
Output: annotated gate, measured floors, pins at the new sets.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-CONTEXT.md
@.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-PATTERNS.md
Also read the plan-03 SUMMARY (164.4.1-03-SUMMARY.md in this phase directory — it exists once plan 03 has run) for the retention apply list and the pin sites plan 03 left.
@scripts/mutation-runner/GRAMMAR.md
@supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
@supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql
@supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql
@supabase/migrations/20260803130000_reaper_limit_bound_materialized_cte.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply-list discovery — baseline GREEN with the three skips SILENT</name>
  <files>supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql</files>
  <read_first>
    - `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:1-60` — what it guards; the ANTI-GREEN-SKIP CONTRACT at 45-59
    - `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:224-257` — Part 1b: the IF EXISTS pg_cron guard, the 1/JOB-02 registration arm at 233, the SKIP LOCKED body arm at 257
    - `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:282` — the Part 1b + Parts 2-3 skip
    - `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:325-333` — Part 2 skip and job-missing arm
    - `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:483-488` — Part 3 skip and body read
    - `supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql:438-442` — the guard; the schedule call is at 501
    - `supabase/migrations/20260803130000_reaper_limit_bound_materialized_cte.sql:96-107` — guard and re-schedule; the self-verify is at 175-184
    - Plan 03's retention apply list (read the RED-UNDER-SETUP line of test_retention_orphaned_running.sql) for the compute_jobs + pg_cron chain; `scripts/pg-lane/fixtures/27-fixture-strategy-analytics-computation-error.sql` and the strategy_analytics stand-ins other annotated gates use (grep `strategy_analytics` across `supabase/tests/*RED-UNDER-SETUP*` lines)
  </read_first>
  <action>
    Build the `RED-UNDER-SETUP` list by iteration on real lanes under a scratch `--workdir` outside the repo. `supabase/migrations/20260513094906_enable_pg_cron.sql` before `20260802120000`; then `20260802120000`, `20260803120000`, `20260803130000` in order (last writer last), plus the real migrations the reaper body and the gate's seeds touch (`strategy_analytics` columns `computing_started_at`, `computation_status`, the bridge `sync_strategy_analytics_status`). Iterate until exit 0 AND `grep -a -c -i -E 'SKIP|skipping' out` = 0 — for THIS file that zero is the load-bearing measurement: a non-zero means Part 1b/2/3 are still withheld and every twin behind them is un-falsifiable.

    Header per PATTERNS § 1b: boilerplate; the "sized by the skips" paragraph quoting the three skip lines by line number and the MEASURED zero; the oracle-scope caveat (last writer `20260803130000`); the `RED-UNDER-SETUP` line. No spelling conversion, no assertion edits.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && S=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/0339e09c-f6d0-4f3c-abb2-8254bf7a3a12/scratchpad/p04-baseline && rm -rf "$S" && mkdir -p "$S" && f=test_strategy_analytics_stuck_computing_reaper && list=$(grep -a -o 'RED-UNDER-SETUP: .*' "supabase/tests/$f.sql" | sed 's/^RED-UNDER-SETUP: //' | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8"));console.log(j.apply.join(" "))') && bash scripts/pg-lane/run.sh --workdir "$S/$f" --apply $list --gate "supabase/tests/$f.sql" > "$S/$f.out" 2>&1 && test "$(grep -a -c -i -E 'SKIP|skipping' "$S/$f.out")" = "0" && echo "$list" | tr ' ' '\n' | grep -a -c -E '20260513094906|20260802120000|20260803130000' | grep -q '^3$'</automated>
    <fails_when>`run.sh` exits non-zero (a migration's guard or self-verify — `20260802120000:438-442`, `20260803130000:96-100` / `:175-184` — aborted the apply, or the gate raised `TEST FAILED`); the `SKIP|skipping` count is not `0` (one of the three skips at `:282`, `:326`, `:483` still fires — Part 1b / 2 / 3 withheld, the load-bearing failure for this file); or the list carries fewer than exactly three of `20260513094906` / `20260802120000` / `20260803130000` (`grep -q '^3$'` exits 1); an empty `$list` from a missing or malformed `RED-UNDER-SETUP` fails at `run.sh --apply`</fails_when>
  </verify>
  <done>Baseline exit 0 with ZERO skip/skipping lines; the list carries the enabling migration and both scheduling migrations; header quotes the three skip sites and the measured zero.</done>
</task>

<task type="auto">
  <name>Task 2: Twin all 29 sections and drive each RED naming itself first</name>
  <files>supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql</files>
  <read_first>
    - `scripts/mutation-runner/GRAMMAR.md` Shapes 1-3, rules 3a/4; PATTERNS § 1c (LAYERED — `20260803130000` self-verifies job count and MATERIALIZED count at :175-184, so a body twin that drops a MATERIALIZED keyword aborts the apply unless its self-verify is edited in the same twin; MEASURE step 1 alone first), PATTERNS § 2 (the exception wrap, only if needed)
    - Section list: `gateSectionCount` on the file prints 29; identities via `grep -a -oE "TEST FAILED \([^)]*\)" | sort -u` (note sub-lettered identities such as `1/JOB-02` share one section — the arm-unit rule)
  </read_first>
  <action>
    Annotate every section. Body-oracle arms (Parts 2-3, `cron.job.command`) target the LAST writer `20260803130000`; registration arms target its `cron.schedule` jobname/command with a layered self-verify edit where measured necessary; the Part 1a column-shape and bridge arms target `20260802120000` / `20260803120000` DDL and function bodies. `occurrences` exact.

    Proof loop: `node scripts/mutation-runner/run.mjs --file supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql > <scratch>/reaper.log 2>&1` — exit 1 expected; every arm `RED (identity ok)`; EXACTLY ONE defect row, kind `lane-blocked-stale`, naming only `test_reconcile_dropped_enqueue_sweep.sql`. Never edit during a run.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && S=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/0339e09c-f6d0-4f3c-abb2-8254bf7a3a12/scratchpad/p04-arms && rm -rf "$S" && mkdir -p "$S" && n=test_strategy_analytics_stuck_computing_reaper && node scripts/mutation-runner/run.mjs --file "supabase/tests/$n.sql" > "$S/$n.log" 2>&1; red=$(grep -a -c 'RED (identity ok)' "$S/$n.log"); rows=$(awk '/^  KIND +ARM/{f=1;next} f && /^  [a-z]/{n++} END{print n+0}' "$S/$n.log"); stale=$(grep -a -c -E '^  lane-blocked-stale ' "$S/$n.log"); echo "red=$red rows=$rows stale=$stale"; test "$red" -ge 29 && test "$rows" = "1" && test "$stale" = "1" && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -E '^lane-blocked: 1 file\(s\) .*test_reconcile_dropped_enqueue_sweep.sql' && npx vitest run src/__tests__/mutation-annotation-parser.test.ts -t "every SECTION an annotated file raises for also carries a twin" 2>&1 | grep -a -E 'passed'</automated>
    <fails_when>`red` < 29 (an arm is `no-red` — e.g. a body twin aimed at `20260802120000` that `20260803130000` overwrites — `wrong-first-failure`, or `occurrence-mismatch`); `rows` ≠ `1` or `stale` ≠ `1` (a second defect kind, or a MATERIALIZED-CTE twin aborting the apply as a `baseline` row); the `--parse-only` `lane-blocked:` line does not read exactly `1 file(s)` naming the reconcile file (the reaper file's annotation did not parse); or the vitest output lacks `passed`</fails_when>
  </verify>
  <done>≥29 `RED (identity ok)` verdicts; the single expected `lane-blocked-stale` row is the only defect; `--parse-only` names exactly the reconcile file as lane-blocked; set-inclusion control passes.</done>
</task>

<task type="auto">
  <name>Task 3: Ratchet floors and re-pin the measured sets</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/mutation-annotation-parser.test.ts, src/__tests__/mutation-runner-floors.test.ts</files>
  <read_first>
    - PATTERNS § 3 and § 6; plan 03's SUMMARY for the identifiers to `grep -an` (`export const FILES_FLOOR`, `export const ARMS_FLOOR`, `LANE_BLOCKED_2`, `toHaveLength(2)`, `filesAnnotated).toBe(`, `totalAnchored).toBe(`, `RE-MEASURED 2026-09-0`, `const GREEN_LOG = [`, `arm lane(s) spawned`, `per-file row(s) but reported`, `toContain("reports `) — never line numbers
  </read_first>
  <action>
    Full run first (`node scripts/mutation-runner/run.mjs > <scratch>/full.log 2>&1`, exit 1 with the single stale row). Read N and B off `coverage:` / `biting:` (projection 43 / plan-03 B + 29 — write what is printed).

    `run.mjs`: `FILES_FLOOR = N`, `ARMS_FLOOR = B`; dated derivation blocks in the house shape with SEPARATION both ways through `runCorpus({scopeDir:"supabase/tests",filesFloor,armsFloor,log:()=>{}})` (N/B → only `lane-blocked-stale`; N+1/B+1 → two additional `floor` defects quoted). `WAIVED_CEILING` untouched, one dated CURRENCY line (D-03).

    `parser.test.ts`: `filesAnnotated` N; array gains `test_strategy_analytics_stuck_computing_reaper.sql`; `LANE_BLOCKED_2` → `LANE_BLOCKED_1` (`test_reconcile_dropped_enqueue_sweep.sql`) with a dated reason line and the prior list as lineage; `toHaveLength(2)` → `toHaveLength(1)` beside `toEqual(LANE_BLOCKED_1)`; dated arithmetic line (`N + 0 + 27 + 0 + 1 = 71`). Pending AIM untouched. PARTITION test green.

    `floors.test.ts`: `totalAnchored` → new total; APPEND the `RE-MEASURED … (plan 164.4.1-04 …)` paragraph; `GREEN_LOG` header lines, new per-file row in sorted position, sum comment, the three derived literals.

    Commit with the `TRIPWIRE-RED:` line naming the ONE remaining file.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && npx vitest run src/__tests__/mutation-annotation-parser.test.ts src/__tests__/mutation-runner-floors.test.ts 2>&1 | grep -a -E 'Test Files .* passed' && node -e 'import("./scripts/mutation-runner/run.mjs").then(m=>{console.log(m.FILES_FLOOR,m.ARMS_FLOOR,m.WAIVED_CEILING);if(m.FILES_FLOOR<43||m.WAIVED_CEILING!==0)process.exit(1)})' && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -E "^coverage: files $(node -e 'import("./scripts/mutation-runner/run.mjs").then(m=>console.log(m.FILES_FLOOR))')/71" && grep -a -c 'LANE_BLOCKED_1' src/__tests__/mutation-annotation-parser.test.ts | grep -v '^0$'</automated>
    <fails_when>the vitest summary lacks `Test Files … passed`; the `node -e` read exits 1 (`FILES_FLOOR` < 43 or `WAIVED_CEILING` ≠ 0); the `--parse-only` `coverage:` numerator differs from the exported `FILES_FLOOR` (no matching line); or the `LANE_BLOCKED_1` count is `0`</fails_when>
  </verify>
  <done>Both vitest files green; FILES_FLOOR ≥ 43 equals the printed numerator; ARMS_FLOOR equals printed biting with separation both ways; WAIVED_CEILING 0; `LANE_BLOCKED_1` pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NOTICE-skip gate → lane | a still-firing skip makes a green baseline that proves nothing |
| lane oracle → TEST oracle | last-writer body must match TEST's |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.1-13 | Repudiation | a withheld Part reads as a passing Part | high | mitigate | Task 1's measured ZERO skip lines is a hard verify gate; the header quotes the three skip sites |
| T-164.4.1-14 | Tampering | a MATERIALIZED-CTE body twin aborts the apply at the migration's own self-verify and is "fixed" by a waiver | medium | mitigate | LAYERED twin per PATTERNS § 1c with measured step-1-alone; D-03 forbids the waiver |
| T-164.4.1-15 | Repudiation | floors from projection | high | mitigate | printed lines only; SEPARATION both ways |
| T-164.4.1-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- `--parse-only`: `coverage: files N/71`, `lane-blocked: 1 file(s) … test_reconcile_dropped_enqueue_sweep.sql`, `pending: 0`.
- Full run: exit 1, exactly one `lane-blocked-stale` row naming that one file; tallies agree.
- No `"waiver"` in the gate; nothing under `supabase/migrations/` in the diff.
</verification>

<success_criteria>
- SC-2 for the reaper file: 29 sections proven, zero skips measured; floors measured and separated; D-03/D-04 held.
</success_criteria>

<output>
Create `.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-04-SUMMARY.md` when done, quoting the run's own summary lines, both SEPARATION results, the apply list, the measured zero-skip baseline, and every LAYERED twin's step-1-alone outcome.
</output>
