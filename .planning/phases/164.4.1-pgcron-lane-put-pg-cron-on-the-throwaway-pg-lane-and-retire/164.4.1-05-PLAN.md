---
phase: 164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire
plan: 05
type: execute
subsystem: sql-gates
tags: [red-under, pg_cron, floors-ratchet, file-move-4, reconcile-sweep, tripwire-cleared]
wave: 5
depends_on: ["164.4.1-04"]
files_modified:
  - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-annotation-parser.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
autonomous: true
requirements: [SC-2, SC-3]
provides:
  - "test_reconcile_dropped_enqueue_sweep.sql (39 sections) annotated and machine-proven — the LAST file move; lane-blocked reaches 0"
  - "The lane-blocked-stale tripwire CLEARED on the real corpus: full run exits 0 with the probe still AVAILABLE"
  - "Phase end-state floors: FILES_FLOOR / ARMS_FLOOR at the printed values (projection 44 / 365 — measured, never assumed)"
affects: [164.4.1-06]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 39 sections carry twins the runner drives RED naming that section FIRST, then restores GREEN (SC-2)"
    - "The baseline is GREEN: the `1/JOB-04` RAISE-on-absent guard (:268-269) no longer fires; zero skip lines; cron.job.command is the last writer's (20260819150000)"
    - "`lane-blocked: 0 file(s)` on the run's own output AND `lane-probe: pg_cron AVAILABLE` AND exit 0 — the tripwire that fired in plan 01 is CLEARED by annotation, not by touching the classifier or the probe (SC-3)"
    - "FILES_FLOOR / ARMS_FLOOR equal the printed lines; one higher FIRES; WAIVED_CEILING 0 (D-03)"
    - "lane-blocked pinned as the MEASURED empty set beside an AIM proving the class is still computed (the selftest fixture pair still classifies lane-blocked) — never `toEqual([])` alone (D-04)"
  artifacts:
    - path: "supabase/tests/test_reconcile_dropped_enqueue_sweep.sql"
      provides: "header + RED-UNDER-SETUP + 39 twins"
    - path: "scripts/mutation-runner/run.mjs"
      provides: "phase end-state derivation blocks; constants at measured values"
    - path: "src/__tests__/mutation-annotation-parser.test.ts"
      provides: "44-name annotatedFiles set; lane-blocked measured-empty with AIM; arithmetic line; toHaveLength(0)"
    - path: "src/__tests__/mutation-runner-floors.test.ts"
      provides: "totalAnchored re-pinned, twelfth lineage paragraph, GREEN_LOG at the end-state numbers"
  key_links:
    - from: "scripts/mutation-runner/run.mjs lane probe + classification"
      to: "an EMPTY corpus.laneBlockedFiles"
      via: "annotating the fourth file empties the derived class; the defect at the probe leg is gated on length > 0"
      pattern: "lane-blocked-stale"
---

<objective>
The LAST file move and the plan that clears the tripwire: `test_reconcile_dropped_enqueue_sweep.sql` — 39 sections, the largest file in the phase, RAISE-on-absent spelling (`supabase/tests/test_reconcile_dropped_enqueue_sweep.sql:268-269`), Parts 2-4 reading cron.job.command as the oracle.

CONTEXT decisions cited as D-01..D-07. Implements D-01 (the deferral is fully retired — nothing is lane-blocked after this plan), D-03, D-04 (the lane-blocked set pin goes to a MEASURED empty set with an AIM), and delivers SC-3's "cleared" half on the real corpus.

Job reconcile_dropped_enqueue_sweep: scheduled at `supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql:711` (guard 592-596), re-scheduled by 20260819130500 and again at `supabase/migrations/20260819150000_reconcile_sweep_readmit_attempt_ceiling.sql:366` (guard 321-325; self-verify at 460 asserts exactly ONE job row). Last writer = 20260819150000. The gate asserts the 35-minute cadence at line 292 and a NULL-command arm at 286.

**What "cleared" means and what it does NOT:** after Task 2, `corpus.laneBlockedFiles` is empty because the class is derived over UNANNOTATED files only (`scripts/mutation-runner/parse.mjs:1086-1099`). The probe leg still runs (targets non-empty) and still says AVAILABLE; the defect is gated on a non-empty laneBlockedFiles and does not fire. Nothing in the classifier, the probe gate or SELF-TEST 17/17 is touched — the tripwire stays live for any future unannotated pg_cron gate. ⚠️ Until plan 06, the runner still prints the sentence "lane-probe: pg_cron AVAILABLE — lane-blocked class is STALE" even though the class is empty; the SENTENCE is now false-reading and plan 06 corrects it at the source. Do not change the message here.

⚠️ This is the commit that ENDS the TRIPWIRE-RED interval: the full run exits 0. The commit message says so. After this plan the branch may be pushed (plan 06 does it).

Purpose: SC-2 complete; SC-3 cleared half.
Output: annotated gate, end-state floors, pins at the measured sets.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-CONTEXT.md
@.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-PATTERNS.md
Also read the plan-04 SUMMARY (164.4.1-04-SUMMARY.md in this phase directory — it exists once plan 04 has run) for the reaper apply list and the pin sites plan 04 left.
@scripts/mutation-runner/GRAMMAR.md
@supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
@supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql
@supabase/migrations/20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql
@supabase/migrations/20260819150000_reconcile_sweep_readmit_attempt_ceiling.sql
@scripts/mutation-runner/fixtures/selftest/lane-blocked/lane-blocked-gate.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply-list discovery — baseline GREEN, zero skips, oracle = last writer</name>
  <files>supabase/tests/test_reconcile_dropped_enqueue_sweep.sql</files>
  <read_first>
    - `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql:1-70` — what it guards; the ANTI-GREEN-SKIP CONTRACT at 50-66
    - `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql:250-292` — Part 1: presence RAISE 268-269, registration 273-275, NULL command 283-286, cadence 292
    - `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql:331` — the comment-preserving anchor note; the Part 2-4 headers follow
    - `supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql:592-596` — the guard; the schedule call is at 711
    - `supabase/migrations/20260819150000_reconcile_sweep_readmit_attempt_ceiling.sql:321-325` — the guard; re-schedule at 366; self-verify at 460
    - Plan 04's reaper apply list and plan 03's retention list for the chain shape (the sweep body touches compute_jobs, strategy_analytics, csv dailies — read the apply abort text and add the REAL migration)
  </read_first>
  <action>
    Build the `RED-UNDER-SETUP` list by iteration on real lanes under a scratch `--workdir` outside the repo: `supabase/migrations/20260513094906_enable_pg_cron.sql` before `20260816140000`; then `20260816140000`, `20260819130500`, `20260819150000` in order; plus every real migration the sweep body and the gate's seeds need. Iterate until exit 0 and `grep -a -c -i -E 'SKIP|skipping' out` = 0. Watch for a baseline that aborts INSIDE a migration's self-verify — that text names the missing object.

    Header per PATTERNS § 1a/1b: boilerplate; ⚠️ notes with measured reasons; the oracle-scope caveat (last writer `20260819150000`); the `RED-UNDER-SETUP` line.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && S=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/0339e09c-f6d0-4f3c-abb2-8254bf7a3a12/scratchpad/p05-baseline && rm -rf "$S" && mkdir -p "$S" && f=test_reconcile_dropped_enqueue_sweep && list=$(grep -a -o 'RED-UNDER-SETUP: .*' "supabase/tests/$f.sql" | sed 's/^RED-UNDER-SETUP: //' | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8"));console.log(j.apply.join(" "))') && bash scripts/pg-lane/run.sh --workdir "$S/$f" --apply $list --gate "supabase/tests/$f.sql" > "$S/$f.out" 2>&1 && test "$(grep -a -c -i -E 'SKIP|skipping' "$S/$f.out")" = "0" && echo "$list" | tr ' ' '\n' | grep -a -c -E '20260513094906|20260816140000|20260819150000' | grep -q '^3$'</automated>
  </verify>
  <done>Baseline exit 0 with zero skip lines; the list carries the enabling migration and the first and last scheduling migrations in order; header records reasons and the oracle caveat.</done>
</task>

<task type="auto">
  <name>Task 2: Twin all 39 sections; the full corpus goes GREEN with the probe AVAILABLE</name>
  <files>supabase/tests/test_reconcile_dropped_enqueue_sweep.sql</files>
  <read_first>
    - `scripts/mutation-runner/GRAMMAR.md` Shapes 1-3, rules 3a/4; PATTERNS § 1c (LAYERED — `20260819150000:460` asserts exactly ONE job row, so a jobname twin must also edit that self-verify or the apply aborts; MEASURE step 1 alone), § 2 (exception wrap if needed)
    - Section list: `gateSectionCount` prints 39; identities via `grep -a -oE "TEST FAILED \([^)]*\)" | sort -u`
  </read_first>
  <action>
    Annotate every section. Body-oracle arms (Parts 2-4) target the last writer `20260819150000`; Part 1 registration/NULL-command/cadence arms target its `cron.schedule` call (jobname, command, `35 * * * *`) with layered self-verify edits where measured necessary. `occurrences` exact.

    Proof loop, narrowed: `node scripts/mutation-runner/run.mjs --file supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` — as soon as this file is annotated the lane-blocked class is EMPTY, so a clean narrowed run exits **2** (narrowed, no defects) and prints NO defect table; every arm `RED (identity ok)`. Then the FULL corpus: `node scripts/mutation-runner/run.mjs > <scratch>/full.log 2>&1` must exit **0** with `✅ No defects`, `lane-blocked: 0 file(s)`, and `lane-probe: pg_cron AVAILABLE …` — the cleared half of SC-3, on the real corpus. Save `full.log` for Task 3 and quote its `lane-probe:` and `lane-blocked:` lines in the SUMMARY. Never edit during a run.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && S=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/0339e09c-f6d0-4f3c-abb2-8254bf7a3a12/scratchpad/p05-arms && rm -rf "$S" && mkdir -p "$S" && n=test_reconcile_dropped_enqueue_sweep && node scripts/mutation-runner/run.mjs --file "supabase/tests/$n.sql" > "$S/$n.log" 2>&1; rc=$?; red=$(grep -a -c 'RED (identity ok)' "$S/$n.log"); rows=$(awk '/^  KIND +ARM/{f=1;next} f && /^  [a-z]/{n++} END{print n+0}' "$S/$n.log"); echo "rc=$rc red=$red rows=$rows"; test "$rc" = "2" && test "$red" -ge 39 && test "$rows" = "0" && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -E '^lane-blocked: 0 file\(s\) ' && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -E '^coverage: files 44/71' && npx vitest run src/__tests__/mutation-annotation-parser.test.ts -t "every SECTION an annotated file raises for also carries a twin" 2>&1 | grep -a -E 'passed'</automated>
  </verify>
  <done>Narrowed run exits 2 with ≥39 `RED (identity ok)` and no defect table; `--parse-only` prints `lane-blocked: 0 file(s)` and `coverage: files 44/71`; the full corpus run exits 0 with the probe AVAILABLE (quoted in the SUMMARY); set-inclusion control passes.</done>
</task>

<task type="auto">
  <name>Task 3: Phase end-state floors and the lane-blocked pin as a MEASURED empty set with an AIM</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/mutation-annotation-parser.test.ts, src/__tests__/mutation-runner-floors.test.ts</files>
  <read_first>
    - PATTERNS § 3 and § 6; plan 04's SUMMARY for identifiers to `grep -an` (`export const FILES_FLOOR`, `export const ARMS_FLOOR`, `LANE_BLOCKED_1`, `toHaveLength(1)`, `filesAnnotated).toBe(`, `totalAnchored).toBe(`, `RE-MEASURED 2026-09-0`, `const GREEN_LOG = [`, `arm lane(s) spawned`, `per-file row(s) but reported`, `toContain("reports `)
    - `scripts/mutation-runner/fixtures/selftest/lane-blocked/lane-blocked-gate.sql` and its comment-only sibling — the fixture pair whose `scanCorpus` classification is `lane-blocked` for exactly `lane-blocked-gate.sql` (SELF-TEST 17/17's own AIM asserts this from the fixture bytes)
    - The `pending` AIM plan 02 wrote (stripped copy in a temp dir classifies `pending`) — the lane-blocked AIM mirrors it
  </read_first>
  <action>
    Read N and B off Task 2's `full.log` (`coverage: files N/71`, `biting: B`, `arms: B/B/0`, `lane-invocations:`, `per-arm lane time:`). Projection 44 / plan-04 B + 39; write the printed values.

    `run.mjs`: `FILES_FLOOR = N`, `ARMS_FLOOR = B`; dated derivation blocks marked as the PHASE 164.4.1 END STATE, SEPARATION both ways via `runCorpus({scopeDir:"supabase/tests",filesFloor,armsFloor,log:()=>{}})` — N/B → `defects=0 SILENT`, exit 0; N+1/B+1 → exactly two `floor` defects quoted. Include the cleared-tripwire lines (`lane-blocked: 0 …`, `lane-probe: pg_cron AVAILABLE …`) in the COMMAND section. `WAIVED_CEILING` untouched; dated CURRENCY line: 0 through every arms move of 164.4 AND 164.4.1 (D-03).

    `parser.test.ts`: `filesAnnotated` N; the array gains `test_reconcile_dropped_enqueue_sweep.sql` (44 names, sorted); the lane-blocked `it` becomes "scanCorpus names ZERO lane-blocked files — MEASURED empty, beside proof the class is still computed": `expect(scanCorpus(<selftest lane-blocked fixture dir>).laneBlockedFiles).toEqual(["lane-blocked-gate.sql"])` as the AIM FIRST, then `expect(corpus.laneBlockedFiles).toEqual([])` on the live corpus; retire `LANE_BLOCKED_1` with the full four-name lineage kept in a dated comment (retired by annotation in plans 02–05, never by a classifier change — D-01); keep the negative controls (`test_retention_crons_safe.sql` still `unreachable`, `test_wizard_composite_fence.sql` annotated and not lane-blocked); `toHaveLength(1)` → `toHaveLength(0)` beside the AIM; dated arithmetic line (`44 + 0 + 27 + 0 + 0 = 71`, from `--parse-only`). The pending AIM stays. PARTITION test unchanged and green. Never `toEqual([])` without its AIM in the same `it` (D-04; the `:1445`-shape vacuity).

    `floors.test.ts`: `totalAnchored` → new total; APPEND the `RE-MEASURED … (plan 164.4.1-05, END STATE …)` paragraph; `GREEN_LOG` header lines, new per-file row, sum comment (44 terms), the three derived literals. Leave the synthetic `lane-blocked: 2 … d.sql e.sql` / `lane-probe: pg_cron absent` fixture lines as they are (plan 06 revisits the wording, not the counts).

    Commit message: `TRIPWIRE-CLEARED: lane-blocked: 0, full corpus exit 0 with lane-probe AVAILABLE — the 164.4.1-01..04 TRIPWIRE-RED interval ends here.`
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && npx vitest run src/__tests__/mutation-annotation-parser.test.ts src/__tests__/mutation-runner-floors.test.ts 2>&1 | grep -a -E 'Test Files .* passed' && node -e 'import("./scripts/mutation-runner/run.mjs").then(m=>{console.log(m.FILES_FLOOR,m.ARMS_FLOOR,m.WAIVED_CEILING);if(m.FILES_FLOOR!==44||m.ARMS_FLOOR<=262||m.WAIVED_CEILING!==0)process.exit(1)})' && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -E '^coverage: files 44/71' && test "$(grep -a -c 'toEqual(LANE_BLOCKED_1)' src/__tests__/mutation-annotation-parser.test.ts)" = "0" && grep -a -c 'lane-blocked-gate.sql' src/__tests__/mutation-annotation-parser.test.ts | grep -v '^0$' && node scripts/mutation-runner/run.mjs --self-test 2>&1 | grep -a 'SELF-TEST PASSED'</automated>
  </verify>
  <done>Both vitest files green; FILES_FLOOR is exactly 44 and ARMS_FLOOR equals the printed biting, separated both ways; WAIVED_CEILING 0; the lane-blocked pin is the measured empty set with the fixture-pair AIM in the same test; SELF-TEST 17/17 still passes (the tripwire is live).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| empty derived class → attestation | `lane-blocked: 0` must be a measured zero, not a class that stopped being computed |
| tripwire → future corpus | SELF-TEST 17/17 must keep firing on the synthetic corpus after the live class is empty |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.1-16 | Repudiation | `laneBlockedFiles).toEqual([])` passing on a dead branch | high | mitigate | the fixture-pair AIM in the same `it`; PARTITION test; SELF-TEST 17/17 in verify |
| T-164.4.1-17 | Tampering | clearing the tripwire by editing classifier/probe instead of annotating | high | mitigate | `parse.mjs`, `pg-cron-probe.sql`, and the probe/defect code in `run.mjs` are NOT in `files_modified`; `git diff --name-only` gate in `<verification>` |
| T-164.4.1-18 | Repudiation | floors from projection | high | mitigate | printed lines; SEPARATION both ways |
| T-164.4.1-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- `git diff --name-only HEAD~3..HEAD` contains NEITHER `scripts/mutation-runner/parse.mjs` NOR `scripts/mutation-runner/fixtures/lane-probe/pg-cron-probe.sql`; `git diff HEAD~3..HEAD -- scripts/mutation-runner/run.mjs | grep -a -E '^[-+].*(lane-blocked-stale|pg_available_extensions)'` prints nothing (the tripwire code is untouched).
- Full run: exit 0; `coverage: files 44/71`; `lane-blocked: 0 file(s)`; `lane-probe: pg_cron AVAILABLE`; `arms: B/B/0`; tallies agree; `  pending: 0`.
- `node scripts/mutation-runner/run.mjs --self-test` → 17/17 PASSED.
- No `"waiver"` in the gate; nothing under `supabase/migrations/` in the diff.
</verification>

<success_criteria>
- SC-2 complete: 5 files, 103 sections, all proven; end-state floors measured (44 / B).
- SC-3 cleared half on the real corpus: `lane-blocked: 0` with the probe AVAILABLE and exit 0; the fire half (plan 01 log) + the permanent arm remain.
- D-01: nothing deferred; D-03: 0 waivers; D-04: empty sets asserted with AIMs.
</success_criteria>

<output>
Create `.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-05-SUMMARY.md` when done, quoting the full run's `coverage:`, `lane-blocked:`, `lane-probe:`, `arms:`, `biting:`, `lane-invocations:`, `per-arm lane time:` lines, both SEPARATION results, the apply list, and every LAYERED twin's step-1-alone outcome.
</output>
