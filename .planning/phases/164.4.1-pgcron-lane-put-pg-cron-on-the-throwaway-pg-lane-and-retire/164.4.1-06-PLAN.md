---
phase: 164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire
plan: 06
type: execute
subsystem: mutation-runner
tags: [closure, laneblocked-blind, ci-measurement, timeout-decision, docs-currency]
wave: 6
depends_on: ["164.4.1-05"]
files_modified:
  - scripts/mutation-runner/run.mjs
  - scripts/mutation-runner/parse.mjs
  - src/__tests__/mutation-annotation-parser.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - TODOS.md
  - .github/workflows/ci.yml
  - CLAUDE.md
  - scripts/pg-lane/README.md
  - .planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-TRIPWIRE-FIRED.log
autonomous: true
requirements: [SC-1, SC-3, SC-4]
provides:
  - "[REDUNDER-LANEBLOCKED-BLIND] closed on purpose: the classifier's text-only scope is documented, calibrated by a hand-built test, and its unimplementable 'read the apply list' fix is retired with the reason"
  - "[REDUNDER-PGCRON] retired at the source: every runner sentence that says the lane cannot host pg_cron is corrected, dated, with the old lines kept as lineage"
  - "The ubuntu half of SC-1 MEASURED: apt-cache policy, package version, .so/.control present, lane-probe AVAILABLE, coverage/arms lines and the SHA-bound sql-mutation wall clock, from a workflow_dispatch run on this branch"
  - "The ci.yml TIMEOUT DECISION block updated with the measured legs and seconds; timeout-minutes raised to 20 ONLY if the measured run reached ~10 min; STOP + split booked if it reached 20"
affects: [164.4.1-verification, 164.4.1-ship]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The classifier is still text-only and that is now a DOCUMENTED, calibrated limit rather than a booked-but-unfixable defect: a hand-built test shows a gate with no pg_cron probe in its own text classifies `pending` no matter what its apply list would need, and says why that is the right answer (D-07, SC-4)"
    - "No runner print reads false at lane-blocked: 0 — the `lane-blocked:` reason and the AVAILABLE probe sentence are honest for the empty case AND still name a non-empty class as STALE (SC-3)"
    - "An ubuntu sql-mutation run on THIS branch's HEAD sha printed apt-cache policy, the package version, `lane-probe: pg_cron AVAILABLE`, `lane-blocked: 0 file(s)`, `coverage: files 44/71` and passed (SC-1)"
    - "ci.yml's DECISION block carries the measured wall clock and legs of that run; `timeout-minutes` is 15 unless the measurement reached ~600 s, and never above 20"
    - "Every prose currency site that restated 39/71, 4 lane-blocked or 1 pending carries a dated correction; old lines stay as lineage"
  artifacts:
    - path: "scripts/mutation-runner/parse.mjs"
      provides: "dated doc-comment corrections on gateNeedsPgCron and classifyGateIdiom: the lane hosts pg_cron since 164.4.1; the classifier reads gate text only, by design, and why the apply-list widening was retired"
    - path: "scripts/mutation-runner/run.mjs"
      provides: "logCorpusClassification honest for the empty case; dated currency lines on every restated-count comment"
    - path: "src/__tests__/mutation-annotation-parser.test.ts"
      provides: "the D-07 closure calibration test"
    - path: "TODOS.md"
      provides: "[REDUNDER-PGCRON] and [REDUNDER-LANEBLOCKED-BLIND] closed with dated reasoning; [REDUNDER-PENDING-UNBOOKED] currency updated"
    - path: ".github/workflows/ci.yml"
      provides: "measured DECISION paragraph; timeout unchanged or raised per the measured rule"
    - path: ".planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-TRIPWIRE-FIRED.log"
      provides: "the appended CLEARED half: run id, sha, the ubuntu lane-probe and lane-blocked lines"
  key_links:
    - from: "gh workflow run ci.yml --ref <branch>"
      to: "the sql-mutation job's provisioning step and runner log"
      via: "gh api …/actions/runs/<id>/jobs and …/jobs/<id>/logs (never `gh run view --log`, which truncates)"
      pattern: "postgresql-16-cron"
---

<objective>
Close the phase deliberately: D-07's `[REDUNDER-LANEBLOCKED-BLIND]`, the runner's now-false sentences, the ubuntu measurement RESEARCH could not take, and the ci.yml timeout decision — from a MEASURED run, per the rule already written at the job header.

CONTEXT decisions cited as D-01..D-07. This plan implements D-07 and D-06's ubuntu half (per-lane cost on the CI host), and finishes SC-1, SC-3 and SC-4.

**D-07, honestly costed against what the code can see.** TODOS proposed widening `gateNeedsPgCron` to read a gate's `RED-UNDER-SETUP` apply list. That fix is UNIMPLEMENTABLE for the class it targets: classification runs over UNANNOTATED files only (`classifyGateIdiom` is "which of four idiom classes an UNANNOTATED gate file falls in"), and an unannotated file has no `RED-UNDER-SETUP` line to read — the copy-parity file had none when plan 164.4-09 hit the block. A widened branch would be dead code behind a passing test, which is the exact "quietly become unreachable" shape D-07 forbids. The deliberate closure is therefore: (1) document at the function that the classifier reads GATE TEXT ONLY, by design, and that an apply-list-only dependency cannot be seen before annotation; (2) pin that limit with a hand-built calibration text so a future reader finds the boundary asserted rather than implied; (3) record that the SUBSTRATE closed the practical exposure — with pg_cron hosted, both classes resolve the same way (annotatable) — and that a future lane-blocking cause that lives only in a migration will surface as a `baseline` defect at annotation time, which is loud. RESEARCH's option (1) is retired with this reasoning, not silently.

**Message honesty (PATTERNS § 6).** `logCorpusClassification` prints "which the pg-lane cannot host" unconditionally and "lane-blocked class is STALE" whenever the probe is AVAILABLE — both false at `lane-blocked: 0`. Fix at the source; keep the `lane-blocked: N file(s) ` and `lane-probe: pg_cron (absent|AVAILABLE)` prefixes byte-identical (ci.yml greps them; the count-recheck arms in the floors vitest parse them).

**The CI measurement is a workflow_dispatch on this branch, not a PR.** `ci.yml` triggers on push to `main`, `pull_request`, and `workflow_dispatch`; a pushed feature branch runs nothing by itself. Dispatch runs the whole workflow including `plan-anchor-verify` over pending plans — this plan is pending while it runs, which is why every anchor in this plan is a symbol or a quoted string, never a line number into a file plans 01–05 edited.

**Timeout rule (orchestrator, from `.github/workflows/ci.yml` DECISION block):** raise 15 → 20 ONLY IF the measured ubuntu run reaches ~10 minutes; 20 is a CEILING; if a measured run reaches 20, STOP and book the subset-split plan — never a further raise. RESEARCH's ~10.3 min is a PROJECTION and does not trigger anything.

Purpose: SC-4; the ubuntu half of SC-1; SC-3's cleared half SHA-bound on ubuntu; docs currency.
Output: closed TODOS, honest prints, measured DECISION block, appended evidence log.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-CONTEXT.md
@.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-RESEARCH.md
@.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-PATTERNS.md
Also read the plan-01 and plan-05 SUMMARYs (164.4.1-01-SUMMARY.md and 164.4.1-05-SUMMARY.md in this phase directory — they exist once those plans have run) for the macOS per-lane numbers and the end-state floors.
@scripts/mutation-runner/parse.mjs
@scripts/mutation-runner/run.mjs
@scripts/mutation-runner/fixtures/lane-probe/pg-cron-probe.sql
@TODOS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close [REDUNDER-LANEBLOCKED-BLIND] and [REDUNDER-PGCRON] at the source — docs, prints, pins, TODOS</name>
  <files>scripts/mutation-runner/parse.mjs, scripts/mutation-runner/run.mjs, src/__tests__/mutation-annotation-parser.test.ts, src/__tests__/mutation-runner-floors.test.ts, TODOS.md</files>
  <read_first>
    - `scripts/mutation-runner/parse.mjs` — the doc-comment above `export function gateNeedsPgCron` (it says the founder decided 2026-09-03 NOT to install pg_cron on the lane) and the class table above `export function classifyGateIdiom` (its `"lane-blocked"` row says "which the pg-lane cannot host … SCOPE AMENDMENT #2")
    - `scripts/mutation-runner/run.mjs` — `export function logCorpusClassification` (the `lane-blocked:` template literal and the three `lane-probe:` branches); the currency comments that restate counts: grep `-an` for `40/71`, `39/71`, `4 lane-blocked`, `lane-blocked = 71`, `pending: 1`, `[REDUNDER-PGCRON]`, `[REDUNDER-LANEBLOCKED-BLIND]`
    - `.github/workflows/ci.yml` — the greps `^lane-blocked: [0-9]+ file\(s\) ` and `^lane-probe: pg_cron (absent|AVAILABLE)` and the claimed-vs-named cross-check (count of `*.sql` tokens on the line)
    - `src/__tests__/mutation-runner-floors.test.ts` — `const GREEN_LOG = [` and its `lane-blocked: 2 file(s) …` / `lane-probe: pg_cron absent …` lines, plus every RED arm that mutates them (grep `lane-blocked` in that file)
    - `src/__tests__/mutation-annotation-parser.test.ts` — the `describe("classifyGateIdiom — the exclusion decision, over HAND-BUILT texts"` block and the plan-02 flip at the former BLIND tripwire
    - `TODOS.md` — `[REDUNDER-PGCRON]`, `[REDUNDER-LANEBLOCKED-BLIND]`, `[REDUNDER-PENDING-UNBOOKED]`, and the line under `### CI / test-infra ratchet` saying `lane-blocked:` cites `[REDUNDER-PGCRON]`
  </read_first>
  <action>
    **parse.mjs (D-07 documented, not lapsed).** Above `gateNeedsPgCron`: append a dated `⭐ 2026-09-0X (Phase 164.4.1)` paragraph — the lane HOSTS pg_cron now (preload on the `pg_ctl -o` string, extension from `supabase/migrations/20260513094906_enable_pg_cron.sql` in each gate's apply list); the function's question is unchanged and still the tripwire's input (SELF-TEST 17/17); and the BOUNDARY: it reads the gate's own text ONLY. An unannotated file carries no `RED-UNDER-SETUP`, so a pg_cron dependency that lives only in a migration cannot be classified before annotation — that is a property of the input, not a bug to fix, and the TODOS proposal to "read the apply list" was retired for that reason; such a file surfaces as a `baseline` defect the moment someone annotates it, which is loud. Keep the 2026-09-03 paragraph as lineage. In the `classifyGateIdiom` class table, add a dated line under `"lane-blocked"`: the class is structurally empty on a lane that hosts pg_cron and stays derived so the tripwire can fire for a future unannotated probe.

    **run.mjs prints (SC-3 honesty).** In `logCorpusClassification`: keep the `lane-blocked: ${n} file(s) ` prefix; make the reason honest in BOTH states — non-empty: `probe pg_extension for pg_cron and are NOT yet annotated — <names> (the lane hosts pg_cron since Phase 164.4.1, so a non-empty class here is STALE — see the lane-probe line)`; empty: `probe pg_extension for pg_cron and are not yet annotated — (none; the lane hosts pg_cron since Phase 164.4.1, TODOS [REDUNDER-PGCRON] retired 2026-09-0X)`. The AVAILABLE branch: non-empty class → the existing STALE sentence; empty → `lane-probe: pg_cron AVAILABLE — lane-blocked class is empty, as a hosting lane requires`. The `absent` branch: non-empty → existing; empty → `lane-probe: pg_cron absent — lane-blocked class is empty; the lane LOST pg_cron (every annotated pg_cron gate will now fail at baseline)`. Add a dated comment: the count is still printed unconditionally because ci.yml MEASURE_FAILs on the line's absence. Then walk every restated-count comment found in read_first and append ONE dated CURRENCY line each (44 annotated / 0 pending / 27 unreachable / 0 inert / 0 lane-blocked = 71; floors at the plan-05 values; `[REDUNDER-PGCRON]` retired) — never rewrite an old line.

    **floors.test.ts GREEN_LOG.** Update the synthetic `lane-blocked: 2 file(s) …` line to the new non-empty wording (names `d.sql e.sql` preserved so the claimed-vs-named arms keep biting) and keep `lane-probe: pg_cron absent — lane-blocked class is current`; then run the count-recheck arms — any arm that greps the OLD reason text must be moved to the new text, and every RED arm must still go RED.

    **parser.test.ts (the D-07 calibration).** Add to the hand-built `classifyGateIdiom` block: `it("D-07 boundary: a gate whose OWN text never probes pg_cron classifies pending even though its apply list would need a pg_cron-RAISEing migration — the classifier reads gate text only, by design")` — build a text with a `TEST FAILED (…)` raise and a `-- run AFTER 20260826140000` comment but no `pg_extension` probe; assert `gateNeedsPgCron` false and `classifyGateIdiom` = `"pending"`; comment WHY that is the correct answer (no apply list exists before annotation) and that the practical exposure closed with the substrate (plans 164.4.1-02..05). Cross-reference the plan-02 flip so the two assertions read as one closure.

    **TODOS.md.** Mark `[REDUNDER-PGCRON]` `[x]` with a dated closing paragraph: the lane hosts pg_cron (how), the five files and 103 sections proven, end-state floors from plan 05's SUMMARY, `lane-blocked: 0`, the tripwire observed firing (plan 01 log) and cleared (plan 05 + this plan's CI run). Mark `[REDUNDER-LANEBLOCKED-BLIND]` `[x]` with the D-07 reasoning above verbatim in shape (why the proposed fix is dead code, what closed the exposure, where the boundary is pinned). Update `[REDUNDER-PENDING-UNBOOKED]` with a dated currency line (`pending: 0`, pinned as a measured empty set with an AIM). Fix the ratchet-section sentence that says `lane-blocked:` cites `[REDUNDER-PGCRON]` to say it cites a RETIRED id.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && npx vitest run src/__tests__/mutation-annotation-parser.test.ts src/__tests__/mutation-runner-floors.test.ts 2>&1 | grep -a -E 'Test Files .* passed' && node scripts/mutation-runner/run.mjs --self-test 2>&1 | grep -a 'SELF-TEST PASSED' && node scripts/mutation-runner/run.mjs --parse-only > /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/0339e09c-f6d0-4f3c-abb2-8254bf7a3a12/scratchpad/p06-parse.log 2>&1 && grep -a -E '^lane-blocked: 0 file\(s\) ' /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/0339e09c-f6d0-4f3c-abb2-8254bf7a3a12/scratchpad/p06-parse.log && test "$(grep -a -c 'which the pg-lane cannot host' scripts/mutation-runner/run.mjs | awk '{print $1}')" -le "$(grep -a -c 'which the pg-lane cannot host' <(git show HEAD~1:scripts/mutation-runner/run.mjs))" && grep -a -c 'D-07 boundary' src/__tests__/mutation-annotation-parser.test.ts | grep -v '^0$' && grep -a -c '\[x\] \*\*`\[REDUNDER-PGCRON\]`' TODOS.md | grep -v '^0$' && grep -a -c '\[x\] \*\*`\[REDUNDER-LANEBLOCKED-BLIND\]`' TODOS.md | grep -v '^0$' && grep -a -c 'lane-blocked class is empty' scripts/mutation-runner/run.mjs | grep -v '^0$'</automated>
  </verify>
  <done>Both vitest files and the runner self-test pass; `--parse-only` prints `lane-blocked: 0 file(s)` with the honest empty-case reason; the D-07 calibration test exists; both TODOS items are `[x]` with dated reasoning; no new false-reading sentence was introduced.</done>
</task>

<task type="auto">
  <name>Task 2: MEASURE the ubuntu half — dispatch CI on this branch, read the SHA-bound sql-mutation job, update the DECISION block by the measured rule</name>
  <files>.github/workflows/ci.yml, .planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-TRIPWIRE-FIRED.log</files>
  <read_first>
    - `.github/workflows/ci.yml` — the `sql-mutation` TIMEOUT/DECISION comment block (grep `-an` for `# TIMEOUT (threat T-164.3-20)` through `NOT a further timeout raise.`), `timeout-minutes:` on the job, and the plan-01 provisioning step
    - `.github/workflows/ci.yml` top — `on:` has `push: branches: [main]`, `pull_request`, `workflow_dispatch` with two boolean inputs defaulting false
    - Memory rules that bind here: CI green is meaningless unless SHA-bound (re-read `headSha` every poll); `gh run view --log` TRUNCATES — use `gh api repos/{owner}/{repo}/actions/jobs/<job_id>/logs`; compare the last log timestamp to the job's `completed_at`
  </read_first>
  <action>
    Confirm the tree is clean and the full corpus exits 0 locally (plan 05's state). `git push -u origin "$(git branch --show-current)"`. Dispatch: `gh workflow run ci.yml --ref "$(git branch --show-current)"`; poll `gh run list --workflow ci.yml --branch "$(git branch --show-current)" --limit 1 --json databaseId,headSha,status,conclusion` until status is `completed`, asserting on EVERY poll that `headSha` equals `git rev-parse HEAD` (a settled green board belonging to an ancestor is a MEASURE_FAIL). Fetch the jobs: `gh api "repos/{owner}/{repo}/actions/runs/<id>/jobs" --paginate` → the `sql-mutation` job's `id`, `started_at`, `completed_at`, `conclusion`; wall clock = completed − started in seconds. Fetch its log via `gh api "repos/{owner}/{repo}/actions/jobs/<job_id>/logs" > <scratch>/sql-mutation.log` and confirm the last timestamp in the log is within seconds of `completed_at` (not truncated). Read with `grep -a`: the provisioning step's `apt-cache policy` block (which source won — PGDG or noble universe — answers RESEARCH Open Question 1), the `dpkg` `Version:` line (A2), both `ls -l` lines; then the runner lines `lane-probe: pg_cron AVAILABLE`, `lane-blocked: 0 file(s)`, `coverage: files 44/71`, `arms:`, `biting:`, `lane-invocations:`, `per-arm lane time:` (A3 — compare to plan 01's macOS numbers). Also read `plan-anchor-verify`'s conclusion — it ran over THIS plan.

    **If the provisioning step failed** (package absent, .so missing, major ≠ 16): SC-1's ubuntu half is NOT met. Do not work around it in this task. Record the exact log lines in the SUMMARY and return the failure to the orchestrator; RESEARCH names PGDG apt as the fallback and the shim as the distant third, and choosing between them is a substrate decision with supply-chain implications, not an executor's call.

    **If `sql-mutation` timed out or failed for any other reason:** read the defect table from the log; a defect kind other than none is a regression to fix in the file that owns it, then re-push and re-dispatch.

    **Update the DECISION block (measured rule, written by the orchestrator against this exact block):** append a dated paragraph under `NOT a further timeout raise.` in the block's own shape: `MEASURED 2026-09-0X — Phase 164.4.1 end state, <arms> arms / 44 files = <legs> legs -> <seconds>s = <minutes> min [run <id>, sha <sha>]`, the derived s/leg with the 130 s fixed term, the macOS-vs-ubuntu per-arm comparison, and the RESOLUTION: if seconds < ~600 → `timeout-minutes` STAYS 15 and say so; if 600 ≤ seconds < 1200 → raise to 20 in the same commit, rewriting the DECISION sentence to cite this measurement, and restate that 20 is the CEILING; if seconds ≥ 1200 → do NOT raise, book `[REDUNDER-SUBSET-SPLIT]` in TODOS (runner subset mode + scheduled full run, PRINTED never silent — the 164.4-CONTEXT locked decision) and return `## PHASE SPLIT RECOMMENDED`-style findings to the orchestrator in the SUMMARY. Replace the block's `⚠️ This is the LAST projection Phase 164.4 needs` request with this measurement as it asks. If ci.yml changed, commit, push, re-dispatch and read the new SHA-bound run the same way before finishing.

    Append the CLEARED half to `164.4.1-TRIPWIRE-FIRED.log`: a dated section with run id, sha, host = ubuntu-latest, the verbatim `lane-probe:` and `lane-blocked:` lines, the job conclusion and wall clock — beside plan 01's FIRED half.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))" && grep -a -c -E 'MEASURED 2026-09-0[0-9] — Phase 164.4.1 end state' .github/workflows/ci.yml | grep -v '^0$' && grep -a -E '^\s*timeout-minutes: (15|20)$' .github/workflows/ci.yml | head -1 && grep -a -c 'CLEARED' .planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-TRIPWIRE-FIRED.log | grep -v '^0$' && B=$(git branch --show-current) && R=$(gh run list --workflow ci.yml --branch "$B" --limit 1 --json databaseId,headSha,conclusion --jq '.[0]') && echo "$R" && test "$(echo "$R" | jq -r .headSha)" = "$(git rev-parse HEAD)" && test "$(echo "$R" | jq -r .conclusion)" = "success"</automated>
  </verify>
  <done>A SHA-bound workflow_dispatch run on this branch's HEAD concluded `success`; the sql-mutation log shows the provisioning prints, `lane-probe: pg_cron AVAILABLE`, `lane-blocked: 0 file(s)`, `coverage: files 44/71`; the DECISION block carries the measured legs/seconds and the applied rule; `timeout-minutes` is 15 or 20 by that rule and never higher; the evidence log holds both halves.</done>
</task>

<task type="auto">
  <name>Task 3: Docs currency — CLAUDE.md, the lane README, and the phase's own ledger lines</name>
  <files>CLAUDE.md, scripts/pg-lane/README.md</files>
  <read_first>
    - `CLAUDE.md` section `## SQL gate integrity jobs (v0.77.0.0, Phase 164.3)` — the `sql-mutation` bullet and the chain of `⚠️ CURRENCY 2026-09-04` paragraphs ending at `pending: 1` / `⛔ pending: is NOT empty`
    - `scripts/pg-lane/README.md` — the `## pg_cron on the lane (Phase 164.4.1)` section plan 01 wrote and `## Measured runtime`
    - Plan 05's and this plan's SUMMARY numbers (floors, arms, wall clock, per-arm lane time on both hosts)
  </read_first>
  <action>
    `CLAUDE.md`: append ONE dated `⚠️ CURRENCY` paragraph after the existing chain (never edit an earlier one): Phase 164.4.1 put pg_cron on the lane (preload on `pg_ctl -o`, extension from migration `20260513094906` in each apply list, `cron.max_running_jobs=0`), the five files and 103 sections annotated, the measured end state (`coverage: files 44/71`, `arms:`, `biting:`, `FILES_FLOOR`/`ARMS_FLOOR` from plan 05, `WAIVED_CEILING` still 0), `lane-blocked: 0`, `pending: 0` — and that BOTH are pinned as measured empty sets with AIMs, so the earlier `⛔ pending: is NOT empty` sentence is superseded as of this date; the tripwire fired (plan 01 log) and cleared (plan 05 locally, this plan's run <id> on ubuntu); the ubuntu wall clock and the timeout decision taken; `[REDUNDER-PGCRON]` and `[REDUNDER-LANEBLOCKED-BLIND]` closed. Update the `sql-mutation` bullet's parenthetical that says the lane has no pg_cron. Keep "Read the run's own `coverage:` and `arms:` lines rather than any number restated in prose."

    `scripts/pg-lane/README.md`: add the ubuntu measurement to the pg_cron section (apt source, package version, per-arm lane time on ubuntu vs macOS) and a dated line to `## Measured runtime`.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && grep -a -c 'Phase 164.4.1' CLAUDE.md | grep -v '^0$' && grep -a -c 'lane-blocked: 0' CLAUDE.md | grep -v '^0$' && grep -a -c 'ubuntu' scripts/pg-lane/README.md | grep -v '^0$' && git diff --name-only HEAD~3..HEAD | grep -a -c '^supabase/migrations/' | grep -q '^0$'</automated>
  </verify>
  <done>CLAUDE.md carries the dated 164.4.1 currency paragraph superseding the `pending: 1` state; README records the ubuntu measurement; nothing under `supabase/migrations/` changed in the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local branch → GitHub Actions | first time the phase's work runs on ubuntu; the measurement is read back over `gh api` |
| runner prints → CI assertions | wording changes must keep the grep prefixes byte-identical |
| TODOS closure → future readers | a closed item must say what closed it and where the boundary is pinned |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.1-19 | Repudiation | a green board belonging to an ancestor sha | high | mitigate | `headSha == git rev-parse HEAD` asserted on every poll and in `<verify>` |
| T-164.4.1-20 | Repudiation | `gh run view --log` truncation reading a clean failure as a hang | medium | mitigate | `gh api …/jobs/<id>/logs`; last-timestamp vs `completed_at` check |
| T-164.4.1-21 | Tampering | print wording change breaks ci.yml MEASURE_FAIL greps | high | mitigate | prefixes kept byte-identical; floors vitest count-recheck arms re-run; dispatch run is the end-to-end proof |
| T-164.4.1-22 | Denial of Service | `timeout-minutes` raised on a projection | medium | mitigate | the measured rule in Task 2; a raise requires the measured seconds in the same comment |
| T-164.4.1-23 | Information Disclosure | pushing the feature branch exposes `.planning/` on the public repo | low | accept | already the case for every phase (memory: repo is public, planning is tracked); no secret in any file this phase writes |
| T-164.4.1-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- `node scripts/mutation-runner/run.mjs --parse-only` prints `lane-blocked: 0 file(s) …` with the honest reason and `  pending: 0 idiom file(s) without RED-UNDER — `.
- `node scripts/mutation-runner/run.mjs --self-test` → 17/17 PASSED (the tripwire arm still fires on the synthetic corpus).
- Full run on this box: exit 0; `lane-probe: pg_cron AVAILABLE — lane-blocked class is empty, as a hosting lane requires`.
- A SHA-bound ubuntu run: `sql-mutation`, `sql-gate-lint`, `plan-anchor-verify` all `success`.
- `git diff --name-only <phase base>..HEAD | grep -a '^supabase/migrations/'` prints nothing for the WHOLE phase.
</verification>

<success_criteria>
- SC-1 measured on ubuntu (or the failure returned, not worked around).
- SC-3: fired (plan 01 log) + cleared (plan 05 local, this plan's ubuntu run) + permanent arm (17/17) — and no false-reading print remains.
- SC-4: `[REDUNDER-LANEBLOCKED-BLIND]` closed with the boundary documented at the function and pinned by a hand-built test.
- The timeout decision is made from a measurement, by the written rule, and 20 remains the ceiling.
</success_criteria>

<output>
Create `.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-06-SUMMARY.md` when done, quoting: run id + sha; `apt-cache policy` source; package version; both hosts' `per-arm lane time:`; the ubuntu wall clock, legs and s/leg; the timeout decision taken and why; the D-07 closure reasoning in one paragraph. Then the branch is ready for `/gsd-ship` (one PR for the whole phase; run `/gsd-pr-branch` and its deletion guard first per CLAUDE.md).
</output>
