---
phase: 164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire
plan: 05
subsystem: sql-gates
tags: [red-under, pg_cron, floors-ratchet, file-move-4, reconcile-sweep, tripwire-cleared]

requires:
  - phase: 164.4.1
    provides: "plan 01's pg_cron-preloading pg-lane and the `lane-blocked-stale` tripwire; plan 04's floors 43/324, the empty `pending:` pin and the LANE_BLOCKED_1 one-name set"
provides:
  - "test_reconcile_dropped_enqueue_sweep.sql annotated: 39 sections, ALL 39 machine-proven to redden naming themselves FIRST and then restore GREEN, on the FIRST proof run"
  - "A 17-entry apply list one-out ablated on real lanes, measured to print ZERO gate-owned skip lines under the `SKIP Part` form, with the ORACLE narrowed by measurement to the LAST writer 20260819150000"
  - "scripts/pg-lane/fixtures/30-fixture-strategies-status-default.sql — the NOT NULL / DEFAULT 'draft' / CHECK shape 01-fixture-core.sql omits, without which every seed in Parts 2/3/4 drops out of the deployed batch CTE on three-valued logic"
  - "FILES_FLOOR 44 and ARMS_FLOOR 363, equal to the run's own printed lines, SEPARATION driven both ways through the real verdict loop"
  - "SC-3's CLEARED half on the real corpus: the full run EXITS 0 with `lane-blocked: 0 file(s)` and `lane-probe: pg_cron AVAILABLE`, cleared by ANNOTATION with parse.mjs, the probe fixture and the probe/defect code untouched"
  - "The lane-blocked pin as a MEASURED EMPTY SET beside a fixture-pair AIM, with the full four-name lineage kept dated"
affects: [164.4.1-06]

actuals:
  tokens: 139000
  tasks: 3
  commits: 5

tech-stack:
  added: []
  patterns:
    - "A gate-owned skip count must be scoped by the gate's OWN skip TOKEN, not only by its psql prefix: this file's Part 1 OK notice contains the words `SKIP LOCKED` as one of the anchors it asserts, so the prefix-scoped-but-token-only form returns 1 on a fully green run and can never reach 0"
    - "When a migration self-verifies nearly every anchor its gate re-asserts, the layering is not per-twin bad luck but a property of the PAIR: 13 of 39 twins here are layered against one STEP 2, and one needs three steps because that STEP 2 pins the same bound twice (once as a literal, once as a shape)"
    - "A behavioural arm is falsified by a mutation that leaves EVERY text anchor green: `'complete' || 'X'`, `LIMIT 25 + 10`, `'reconcile-sweep-x'`, `) + 1 < 3`, an inverted grace operator. That asymmetry is what makes the text half and the behavioural half non-redundant rather than duplicative"
    - "A raise whose value is the SUM of already-pinned per-arm counts cannot diverge unless one of them diverges first. Its honest falsifier is the scenario its own comment names — here, a seventeenth un-asserted seed — not a production mutation and not a waiver"
    - "An `IS NULL` oracle-precondition guard duplicated across Parts is dominated by the ungated Part that owns the production claim. MEASURED, the production route does not merely lose the race: the migration's own STEP 2 aborts the apply before the gate runs at all"

key-files:
  created:
    - scripts/pg-lane/fixtures/30-fixture-strategies-status-default.sql
    - .planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-05-SUMMARY.md
  modified:
    - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
    - scripts/mutation-runner/run.mjs
    - src/__tests__/mutation-annotation-parser.test.ts
    - src/__tests__/mutation-runner-floors.test.ts
    - CLAUDE.md

key-decisions:
  - "ARMS_FLOOR is 363, NOT the plan's projected 365. 324 + 39 = 363; the plan's number was arithmetic over a reading and the floor is what the run printed."
  - "No reclassification and no waiver were needed. Unlike plans 03 and 04, all 39 sections bit on the FIRST proof run. WAIVED_CEILING is UNEDITED at 0 and the corpus carries zero waivers across all fifteen arms moves of the phase family."
  - "FIVE arms are falsified by GATE-FILE edits rather than production ones, each with its domination measurement recorded at its site. That is the S-2 seed-integrity precedent plan 04 established, applied to three oracle preconditions, one seed-integrity control and one sum-of-pinned-counts invariant. A gate-file falsifier is NOT a waiver: the arm still raises, still names itself first, and is still counted in `biting`."
  - "A NEW stand-in fixture was created rather than seeding `status` in the gate. 20260405061911 is the INITIAL SCHEMA and cannot coexist with 01-fixture-core.sql on one lane; editing the gate's seeds would have been a behaviour change to the object under test. Follows the 27-/29-fixture precedent."
  - "The `lane-probe:` sentence still reads 'lane-blocked class is STALE' while the class is empty. Left untouched ON PURPOSE — plan 06 corrects it at the source. Changing what the tripwire SAYS in the same commit that clears it is the move this phase exists to refuse."

patterns-established:
  - "Clear a tripwire by removing its CAUSE, never by adjusting what it measures. `git diff 82eda0dc..HEAD` touches neither parse.mjs nor the probe fixture, and the only `lane-blocked-stale` / `pg_available_extensions` lines it adds to run.mjs are COMMENTS (measured: 0 non-comment changed lines matching either token)."
  - "Prove the prose you write. The `2/JOB-04` twin's prose claimed a one-step domination route; running it showed the apply aborts one check EARLIER than claimed, and the prose was corrected to the transcript rather than the transcript to the prose."

# SC-2 / SC-3 are this PHASE's success criteria (164.4.1-CONTEXT.md), not
# .planning/REQUIREMENTS.md IDs — `requirements.mark-complete SC-2 SC-3` returns
# `not_found` for both, as it did for plan 04. Left empty rather than invented.
requirements-completed: []

coverage:
  - id: D1
    description: "The apply list is GREEN on the lane with zero gate-owned skip lines and all four Parts running"
    requirement: SC-2
    verification:
      - kind: integration
        ref: "bash scripts/pg-lane/run.sh --workdir <scratch outside the repo> --apply <the file's own RED-UNDER-SETUP> --gate supabase/tests/test_reconcile_dropped_enqueue_sweep.sql => `TAG=v3 rc=0`; gate-owned `SKIP Part` notice count 0; `TEST FAILED` count 0; Part 1 / 2 / 3 / 4 OK all printed"
        status: pass
      - kind: integration
        ref: "one-out ablation over all 17 entries on real lanes: 14 REQUIRED with their first error quoted, 3 REMOVABLE (20260816140000, 20260819130500 and 07-fixture-supabase-default-privileges — the first two kept as the real scheduling chain, the third dropped)"
        status: pass
      - kind: other
        ref: "the list carries 20260513094906 at position 6 (before every cron-touching migration), 20260816140000 at 15 and 20260819150000 LAST at 17"
        status: pass
  - id: D2
    description: "Every section the file raises for carries a twin, and all of them are driven RED naming that section FIRST and then restored GREEN"
    requirement: SC-2
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_reconcile_dropped_enqueue_sweep.sql => rc=2, `sections 39 / judged 39 / annotated 39 / waived 0 / biting 39`, 39 `RED (identity ok)`, baseline and restore exit 0, `No defects in the narrowed scope.`"
        status: pass
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs (FULL, at HEAD b6b830cf) => exit 0, `arms: 363/363/0`, `biting: 363`, `lane-invocations: 363`, tallies agree, `✅ No defects.`"
        status: pass
      - kind: other
        ref: "No section was reclassified and none was waived — a first for this phase's file moves"
        status: pass
    human_judgment: true
  - id: D3
    description: "FILES_FLOOR and ARMS_FLOOR equal the run's own printed lines, and a floor one higher FIRES"
    requirement: SC-2
    verification:
      - kind: integration
        ref: "runCorpus({scopeDir:'supabase/tests',filesFloor:44,armsFloor:363,log:()=>{}}) => defects=0 SILENT (486.4 s); runCorpus({filesFloor:45,armsFloor:364}) => defects=2 kinds=[\"floor\",\"floor\"] naming `FILES_FLOOR regression: 44 annotated file(s) < floor 45` and `ARMS_FLOOR regression: 363 biting arm(s) < floor 364` (490.4 s)"
        status: pass
      - kind: unit
        ref: "node -e module read => `44 363 0`; --parse-only prints `coverage: files 44/71`, equal to the exported FILES_FLOOR"
        status: pass
    human_judgment: false
  - id: D4
    description: "SC-3's cleared half on the real corpus: the tripwire stops firing BY ANNOTATION, and the mechanism is provably untouched"
    requirement: SC-3
    verification:
      - kind: integration
        ref: "full run at HEAD => exit 0 with `lane-blocked: 0 file(s) …`, `lane-probe: pg_cron AVAILABLE — lane-blocked class is STALE`, `  pending: 0 idiom file(s) without RED-UNDER — `"
        status: pass
      - kind: other
        ref: "`git diff --name-only 82eda0dc..HEAD` contains NEITHER scripts/mutation-runner/parse.mjs NOR scripts/mutation-runner/fixtures/lane-probe/pg-cron-probe.sql (grep -c => 0); `git diff … -- run.mjs | grep '^[-+].*(lane-blocked-stale|pg_available_extensions)'` excluding comment lines => 0 lines"
        status: pass
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --self-test => `SELF-TEST PASSED`, including `ok — exactly one defect on the AVAILABLE arm (got 1: [\"lane-blocked-stale\"])` and its absent-marker CONTROL — the tripwire is still live on the synthetic corpus"
        status: pass
    human_judgment: false
  - id: D5
    description: "lane-blocked is pinned as a MEASURED empty set with an AIM, never `toEqual([])` alone"
    requirement: SC-2
    verification:
      - kind: unit
        ref: "the `it` \"scanCorpus names ZERO lane-blocked files — MEASURED empty, beside proof the class is still computed\" asserts the selftest fixture PAIR FIRST — `probe.laneBlockedFiles).toEqual(['lane-blocked-gate.sql'])` and `probe.pendingFiles).toEqual(['lane-blocked-comment-only-gate.sql'])` — and only then `corpus.laneBlockedFiles).toEqual([])`; `grep -c 'toEqual(LANE_BLOCKED_1)'` => 0, `grep -c 'lane-blocked-gate.sql'` => 3"
        status: pass
      - kind: unit
        ref: "non-vacuity loop: all four retired names must be `annotated` and NOT `pending`, so an empty class bought by reclassification rather than annotation fails; `toHaveLength(0)` sits beside `annotatedFiles).toHaveLength(44)` and the dated arithmetic 44 + 0 + 27 + 0 + 0 = 71"
        status: pass
    human_judgment: false
  - id: D6
    description: "WAIVED_CEILING is unedited at 0 and the corpus carries no waiver"
    requirement: SC-3
    verification:
      - kind: unit
        ref: "module read reports WAIVED_CEILING 0; the full run reports `arms: 363/363/0` and NO `floor` defect; `grep -a -c '\"waiver\"'` on the gate => 0"
        status: pass
    human_judgment: false
  - id: D7
    description: "No migration was modified and no lane was ever pointed at a Supabase database"
    requirement: SC-5
    verification:
      - kind: other
        ref: "`git diff --name-only 82eda0dc..HEAD -- supabase/migrations/ | wc -l` => 0; every lane ran via scripts/pg-lane/run.sh with --workdir under the session scratchpad, outside the repo. The one migration edit made for the domination probe was reverted from a byte backup and the restore confirmed by shasum (74bec8c5…), never `git checkout --`"
        status: pass

duration: 175 min
completed: 2026-09-05
status: complete
---

# Phase 164.4.1 Plan 05: Reconcile-Sweep Gate + Tripwire Cleared

The last lane-blocked gate is annotated and machine-proven — 39 sections, all 39
biting, on the first proof run — and the full corpus now **exits 0** with the
pg_cron probe still AVAILABLE.

## What was built

`supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` now carries a
`RED-UNDER-SETUP` apply list, a header stating what the lane can and cannot
falsify, and 39 prose/twin pairs. `FILES_FLOOR` and `ARMS_FLOOR` ratchet to `44`
and `363`. The `lane-blocked-stale` tripwire that fired on every run since plan
01 is cleared — by emptying the class, not by touching the mechanism.

**⚠️ Read the run's own lines, not this prose.** Measured 2026-09-05 at HEAD
`b6b830cf`, `node scripts/mutation-runner/run.mjs` exiting **0**:

```
coverage: files 44/71
lane-blocked: 0 file(s) probe pg_extension for pg_cron, which the pg-lane cannot host —  (deferred 2026-09-03, TODOS [REDUNDER-PGCRON])
lane-probe: pg_cron AVAILABLE — lane-blocked class is STALE
  pending: 0 idiom file(s) without RED-UNDER — 
arms: 363/363/0   (executed/annotated/waived)
biting: 363   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 363   (arm lanes actually spawned — tallied inside runLane, independent of the 363 the verdict loop counted; plus 44 baseline / 44 restore leg(s))
  file test_reconcile_dropped_enqueue_sweep.sql: sections 39 / judged 39 / annotated 39 / waived 0 / biting 39
per-arm lane time: mean 1.1s over 363 arm run(s)
✅ No defects. Every annotated arm bit its own arm first.
```

## SC-3: what "cleared" means here, and what it does not

`corpus.laneBlockedFiles` is empty because the class is derived over
**unannotated** files only. The probe leg still runs and still says AVAILABLE;
the `lane-blocked-stale` defect is gated on a non-empty class and does not fire.

Proven untouched, not asserted untouched:

```
git diff --name-only 82eda0dc..HEAD
  scripts/mutation-runner/run.mjs
  scripts/pg-lane/fixtures/30-fixture-strategies-status-default.sql
  src/__tests__/mutation-annotation-parser.test.ts
  src/__tests__/mutation-runner-floors.test.ts
  supabase/tests/test_reconcile_dropped_enqueue_sweep.sql

parse.mjs / pg-cron-probe.sql in that list  ->  0
non-comment changed run.mjs lines matching (lane-blocked-stale|pg_available_extensions)  ->  0
migrations in the diff  ->  0
```

and the tripwire still fires on the synthetic corpus:

```
node scripts/mutation-runner/run.mjs --self-test
  ok — exactly one defect on the AVAILABLE arm (got 1: ["lane-blocked-stale"])
  ok — absent: the SAME corpus with an absent-marker probe exits 0 with no lane-blocked-stale defect (got exit 0, defects [])
=== SELF-TEST PASSED: … the stale-deferral probe and the 164.3.1-11 regression corpus all fire ===
```

⚠️ The runner still PRINTS `lane-probe: pg_cron AVAILABLE — lane-blocked class is
STALE` while the class is empty. That sentence is now false-reading. It was left
alone deliberately: plan 06 corrects it at the source, and changing what the
tripwire says in the same commit that clears it is the move this phase exists to
refuse.

## The apply list, measured entry by entry

One-out ablation on real lanes, every entry dropped in turn:

| # | entry | verdict / first error |
|---|---|---|
| 1 | `01-fixture-core.sql` | REQUIRED — `42P01: relation "public.strategies" does not exist` |
| 2 | `30-fixture-strategies-status-default.sql` | REQUIRED — `TEST FAILED (2/arm A/JOB-04/SC#1): … got 0 compute_jobs rows from one tick, expected exactly 1` |
| 3 | `02-fixture-sanitize-tables.sql` | REQUIRED — `42P01: relation "portfolios" does not exist` |
| 4 | `03-fixture-compute-jobs.sql` | REQUIRED — `42703: column "id" referenced in foreign key constraint does not exist` |
| 5 | `12-fixture-profiles-is-admin.sql` | REQUIRED — `42703: column p.is_admin does not exist` |
| 6 | `20260513094906_enable_pg_cron.sql` | REQUIRED — `0A000: JOB-04: pg_cron extension is NOT installed` |
| 7 | `20260411144407_compute_jobs_queue.sql` | REQUIRED — `42P01: relation "compute_jobs" does not exist` |
| 8 | `04-fixture-compute-jobs-targets.sql` | REQUIRED — `42703: column "allocator_id" does not exist` |
| 9 | `29-fixture-compute-jobs-priority.sql` | REQUIRED — `42703: column "priority" of relation "compute_jobs" does not exist` |
| 10 | `20260515114555_compute_jobs_claim_token_fencing.sql` | REQUIRED — `42703: column "claim_token" of relation "compute_jobs" does not exist` |
| 11 | `20260522111839_csv_daily_returns.sql` | REQUIRED — `42P01: relation "public.csv_daily_returns" does not exist` |
| 12 | `20260522111858_compute_analytics_from_csv_kind.sql` | REQUIRED — `23503: … violates foreign key constraint "compute_jobs_kind_fkey"` |
| 13 | `20260614120000_derive_broker_dailies_kind.sql` | REQUIRED — `23514: … violates check constraint "compute_jobs_kind_target_coherence"` |
| 14 | `20260710120000_strategy_keys.sql` | REQUIRED — `42P01: relation "public.strategy_keys" does not exist` |
| 15 | `20260816140000_reconcile_dropped_enqueue_sweep.sql` | REMOVABLE — kept: the job's ORIGIN, and its own STEP 2 self-verify runs |
| 16 | `20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql` | REMOVABLE — kept: the B4 exemption's origin, same reason |
| 17 | `20260819150000_reconcile_sweep_readmit_attempt_ceiling.sql` | REQUIRED — `TEST FAILED (1/JOB-04/D-02/R3): the deployed body names public.compute_jobs 2 times, expected 3` |

⭐ **#15 and #16 being REMOVABLE is the load-bearing measurement of this plan.**
`cron.schedule` upserts by NAME, so only the LAST writer's bytes reach
`cron.job.command`. That is why every body-oracle twin targets `20260819150000`
and why a twin against either earlier migration would come back `no-red`.

## The zero-skip baseline, and the predicate that could not be used

```
TAG=v3 rc=0
Part 1 OK: reconcile_dropped_enqueue_sweep registered exactly once at 35 * * * * …
Part 2 OK: the FOUR healable arms (A, A2, C4, C5b) each got one pending compute_analytics_from_csv job …
Part 3 OK: a second sequential tick left the healed job untouched -- same row, still exactly one.
Part 4 OK: the per-tick bound holds -- my 25 oldest seeded orphans healed on tick 1 …
```

```
grep -a -cE '^psql:[^ ]*test_reconcile_dropped_enqueue_sweep\.sql:[0-9]+: NOTICE:.*SKIP Part'  ->  0
grep -a -c 'TEST FAILED'                                                                       ->  0
```

**DEVIATION (documented, not silently adapted).** The plan's Task-1 `<automated>`
block asserts `grep -a -c -i -E 'SKIP|skipping' out` equals `0`. Two independent
reasons that is unsatisfiable here, both measured:

1. Postgres emits its own `… does not exist, skipping` NOTICEs from
   `DROP … IF EXISTS` in applied migrations — **16** such lines on this list.
   (Plan 04 already recorded this half.)
2. **New to this file:** even the plan-03/04 *prefix-scoped* form
   `'^psql:[^ ]*<gate>\.sql:[0-9]+: NOTICE:.*SKIP'` returns **1** on a fully
   green run, because Part 1's own OK notice contains the words `SKIP LOCKED` —
   one of the anchors Part 1 asserts. The measurement that is actually
   load-bearing needs the gate's own skip TOKEN, `SKIP Part`, which no anchor
   prose contains. Recorded in the file's header.

## LAYERED twins — 13 of 39, and why they cluster

`20260819150000`'s STEP 2 self-verify re-asserts nearly every text anchor Part 1
makes, so a single-step body mutation aborts the APPLY instead of reddening the
arm. Step-1-alone outcome for every layered twin: **baseline abort** at the named
STEP 2 line.

| arm | STEP 2 line(s) re-based | steps |
|---|---|---|
| `1/JOB-04` | `:579` `%interval ''1 hour''%` | 2 |
| `1/JOB-04/D-02/R3` | `:552` `v_jobs <> 3` | 2 |
| `1/JOB-04/B4/R3` | `:508` ceiling SHAPE + `:518` marker count | **3** |
| `1/JOB-04/R3` | `:498` ceiling LITERAL + `:508` ceiling SHAPE | **3** |
| `1/JOB-04/B4` | `:531` `%IS TRUE%` | 2 |
| `1/JOB-04/DX-05` | `:561` `%public.strategy_keys%` | 2 |
| `1/JOB-04/D-11` | `:567` `%reconcile-sweep%` | 2 |
| `1/JOB-04/D-08` | `:594` word-bounded `LIMIT 25` | 2 |
| `1/JOB-04/SC#2` | `:573` `%ON CONFLICT DO NOTHING%` | 2 |
| `1/JOB-04/DX-06` | `:582` `%archived%` | 2 |
| `1/JOB-04/D-19` | `:613` `v_mat <> 1` | 2 |

Two Part-1 twins are **single-step by measurement, not by assumption**:
`1/JOB-04/D-04` (`'computing'`) and `1/JOB-04/D-03` (`'complete'`), because STEP 2
pins only `complete_with_warnings` out of the four terminal values (`:558`).
Every Part 2/3/4 twin is single-step by measurement: none of them changes a token
any self-verify in the apply list reads.

## The five GATE-FILE falsifiers, and the domination measurement behind them

| arm | why no production mutation reaches it | falsifier |
|---|---|---|
| `2/JOB-04`, `3/JOB-04`, `4/JOB-04` | Part 1 makes the SAME claim, runs FIRST and UNGATED | that Part's own oracle lookup is pointed at a jobname that does not exist |
| `3/tick 1/JOB-04` | the raise calls itself a SEED-INTEGRITY CONTROL; any production change that stops the heal REDs Part 2 arm A first | the part's seed is stamped inside the grace window instead of a century back |
| `2/whole-block/JOB-04` | its value is the SUM of sixteen already-pinned per-arm counts and cannot diverge unless one of them diverges first | a SEVENTEENTH healable seed with no per-arm assertion — the literal scenario the raise's own comment names |

The domination claim was **run**, not reasoned, and the run corrected the prose:

```
# step 1 — rename the jobname in 20260819150000's cron.schedule + re-base its v_count check
psql:supabase/migrations/20260819150000_…sql:643: ERROR:  P0001: R3/JOB-04 verification
  failed: reconcile_dropped_enqueue_sweep carries a NULL command after re-registration.
```

i.e. the production route does not even reach the gate. With the WHOLE of STEP 2
short-circuited so the apply survives:

```
psql:supabase/tests/test_reconcile_dropped_enqueue_sweep.sql:644: ERROR:  P0001:
  TEST FAILED (1/JOB-04): pg_cron IS installed but the reconcile_dropped_enqueue_sweep
  job is NOT registered.
```

Part 1's registration arm, not `2/JOB-04`. The migration was restored from a byte
backup and the restore confirmed by `shasum` (`74bec8c5…`), never
`git checkout --`.

⛔ A gate-file falsifier is **not** a waiver and must not be read as one: the arm
still RAISES, still names itself FIRST, and is still counted in `biting`.
`WAIVED_CEILING` is unedited at 0.

## SEPARATION, both directions

```
filesFloor=44 armsFloor=363  defects=0  kinds=[]                    (486.4 s)
filesFloor=45 armsFloor=364  defects=2  kinds=["floor","floor"]     (490.4 s)
    floor: FILES_FLOOR regression: 44 annotated file(s) < floor 45
    floor: ARMS_FLOOR regression: 363 biting arm(s) < floor 364
```

⭐ Unlike every block since plan 01, the first direction is now **defect-free**:
there is no tripwire row left to subtract.

## Deviations from Plan

### 1. [Rule 3 — blocking] The zero-skip verify predicate is unsatisfiable TWICE over

Covered above. The prefix-scoped form plan 04 used is *also* insufficient here;
the `SKIP Part` token form is what reaches 0, and the file's header says so with
both measurements.

### 2. [Rule 3 — blocking] A new stand-in fixture: `30-fixture-strategies-status-default.sql`

`01-fixture-core.sql:27` declares `status TEXT`, bare-nullable. Production
declares it `NOT NULL DEFAULT 'draft' CHECK (…)` (`20260405061911:63`). The
deployed body's FIRST conjunct is `s.status <> 'archived'`, and `NULL <>
'archived'` is **NULL**, so on the unpatched lane every seed in Parts 2/3/4 fell
out of the batch CTE:

```
psql:…: NOTICE:  00000: JOB-04 reconcile_dropped_enqueue_sweep: healed 0 dropped-enqueue strategies this tick.
psql:…:940: ERROR:  P0001: TEST FAILED (2/arm A/JOB-04/SC#1): … got 0 compute_jobs rows from one tick, expected exactly 1.
```

The real migration is the INITIAL SCHEMA and cannot coexist with
`01-fixture-core.sql` on one lane, so the shape had to come from a stand-in —
the same reasoning as `29-fixture-compute-jobs-priority.sql`. No twin targets it
(GRAMMAR rule 4).

### 3. [Rule 1 — my own prose was wrong] The `2/JOB-04` domination route is longer than claimed

Covered above. Corrected to the transcript in commit `b3e13011`, and the file
re-verified (`arms: 39/39/0`) after the edit.

### 4. [Rule 2 — missing critical correctness] `CLAUDE.md` was updated

Not in the plan's `files_modified`. The block plan 02 wrote says "Every `node
scripts/mutation-runner/run.mjs` in this interval EXITS 1", which is false at
HEAD. A dated claim read as a current fact is the exact hazard that file's own
currency convention names. The prior paragraph is kept verbatim as the record of
plans 01-04; the new one quotes the run's own lines and flips the standing
expectation.

### 5. [Scope] ARMS_FLOOR is 363, not the planned 365

`324 + 39 = 363`. The plan's 365 was a projection; the floor is what the run
printed. No section was dropped, reclassified or waived.

## Known Stubs

None. No hardcoded empty value, placeholder or unwired data source was
introduced.

## Threat Flags

None. No network endpoint, auth path, file-access pattern or trust-boundary
schema change was introduced. The one new file is an additive `ALTER TABLE` on a
throwaway-lane stand-in that never touches a Supabase database.

## Verification run at HEAD

| gate | result |
|---|---|
| `npx vitest run …parser …floors …drift-check-scripts` | `Test Files 3 passed (3)`, `Tests 527 passed (527)` |
| `npx tsc --noEmit` | `TSC_EXIT=0` |
| `node scripts/mutation-runner/run.mjs --self-test` | `SELF-TEST PASSED` (exit 0) |
| `node scripts/lint-sql-gates.mjs` | `scanned 71 file(s); 0 finding(s), 0 measure-fail(s)` |
| `node scripts/verify-plan-anchors.mjs --pending` | `claims: 37 checked` / `OK: 4 plan file(s), no stale claims` |
| `node scripts/mutation-runner/run.mjs` (full) | **exit 0**, `✅ No defects` |

## What plan 06 inherits

`FILES_FLOOR = 44`, `ARMS_FLOOR = 363`, `WAIVED_CEILING = 0`, `pending:` empty,
`lane-blocked:` empty, corpus exit 0. The open items it owns: the false-reading
`lane-probe: … class is STALE` sentence, the `lane-blocked:` line's own prose
(which still narrates "which the pg-lane cannot host" and cites a retired TODO),
`[REDUNDER-LANEBLOCKED-BLIND]`, and the ubuntu wall-clock measurement.

## Self-Check: PASSED

All 7 claimed files exist on disk. All 5 claimed commits resolve as children of
the expected base `82eda0dc`:

```
25893735 docs(164.4.1-05): CLAUDE.md — record that the expected-RED interval ended
b6b830cf feat(164.4.1-05): ratchet floors to the phase END STATE 44/363 and pin lane-blocked as a MEASURED empty set
b3e13011 docs(164.4.1-05): correct the 2/JOB-04 domination prose to what the lane actually printed, and re-resolve the header's skip anchors
e5146f64 feat(164.4.1-05): twin all 39 sections of the reconcile-sweep gate; 39/39 biting on the first proof run
ce8867d3 feat(164.4.1-05): apply list + header for the reconcile-sweep gate; baseline GREEN with zero gate-owned skips
```

⚠️ `state.advance-plan` REFUSES on this STATE.md (`ambiguous_position_phase`, a
pre-existing and deliberately-unresolved condition recorded in § Known Issues),
so `## Current Position` was hand-maintained, as the section's own note says it
must be. `state.update-progress`, `state.record-metric`,
`state.add-decision` (x3), `state.record-session` and
`roadmap.update-plan-progress` all ran and reported `updated`/`recorded`/`added`.
`requirements.mark-complete SC-2 SC-3` returns `not_found` for both — they are
the phase's success-criteria IDs, not `.planning/REQUIREMENTS.md` rows.
