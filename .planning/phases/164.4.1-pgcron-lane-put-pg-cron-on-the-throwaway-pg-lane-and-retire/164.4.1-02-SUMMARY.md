---
phase: 164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire
plan: 02
subsystem: sql-gates
tags: [red-under, pg_cron, floors-ratchet, pending-pin, file-move-1, mutation-runner, postgres]

requires:
  - phase: 164.4.1
    provides: "plan 01's pg-lane, which preloads pg_cron on its single `pg_ctl -o` start, and the `lane-blocked-stale` tripwire observed firing on the real corpus"
  - phase: 164.4
    provides: "the 39-file / 262-arm annotated corpus, the RED-UNDER grammar, the floors and the parser/floors pins this plan ratchets"
provides:
  - "The corpus's LAST `pending:` file annotated and machine-proven — `pending:` is measured EMPTY for the first time, which is the retirement of [REDUNDER-PGCRON] the founder chose over widening it"
  - "The smallest lane-blocked file annotated, proving BOTH shapes of pg_cron blocking (apply-list-only and own-text-only) on the new substrate"
  - "FILES_FLOOR 41 and ARMS_FLOOR 272, equal to the run's own printed lines, with SEPARATION driven both ways through the real verdict loop"
  - "The D-04 `pending:` SET pin moved deliberately to the measured empty set BESIDE an AIM proving the class is still computed — and the AIM proven able to fail"
  - "The D-07 BLIND tripwire flipped on purpose with its reason written beside it, and the still-open half named"
affects: [164.4.1-03, 164.4.1-04, 164.4.1-05, 164.4.1-06]

actuals:
  tokens: 19683
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "A LAYERED twin whose layering is the FINDING, not an obstacle: when a property is enforced twice over, a single-step mutation is a `no-red` rather than a falsifier, and the twin records that both halves had to fall"
    - "A falsifier chosen by NARROWING rather than DELETING, because deletion makes an EARLIER assertion die first as a raw driver error — first-failure identity constrains which production change a twin may express"
    - "An empty-set pin is only admissible beside an AIM: a synthetic member of the class, built in a temp scope dir, that MUST be classified — proving the class is computed before the live corpus is asserted to be empty of it"
    - "A gate-scoped skip assertion: PostgreSQL's own `does not exist, skipping` DDL notices are not the gate's skips, so the count that matters carries the gate file's `psql:` prefix"

key-files:
  created:
    - .planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-02-SUMMARY.md
  modified:
    - supabase/tests/test_compute_jobs_error_kind_copy_parity.sql
    - supabase/tests/test_derive_allocator_keys_fanout.sql
    - scripts/mutation-runner/run.mjs
    - scripts/mutation-runner/parse.mjs
    - src/__tests__/mutation-annotation-parser.test.ts
    - src/__tests__/mutation-runner-floors.test.ts
    - CLAUDE.md

key-decisions:
  - "D-04 honoured as its own deliberate task: the one-name `pending:` pin became the MEASURED empty set, and the empty set is asserted only after an AIM that classifies a stripped copy of a real gate as `pending`. The AIM was PROVEN able to fail twice by neuter → observe RED → restore, once at the fixture guard and once at the assertion that matters."
  - "D-03 honoured: WAIVED_CEILING is UNEDITED at 0. One arm came close — the derive gate's assertion 3, whose falsifier makes an INSERT raise where no handler existed — and it was closed by the root-cause fix (the exception idiom assertion 4 in the SAME FILE already used), not by an exception. Nine arms moves, zero waivers."
  - "Assertion 2's twin is LAYERED because a single-step mutation was MEASURED to be a `no-red`, not because layering was preferred: api_key in-flight dedup is enforced twice over, and removing either half alone leaves the assertion GREEN."
  - "Assertion 5's twin NARROWS the coherence arm instead of deleting it, because deleting it was MEASURED to kill assertion 1's fan-out first with an unhandled 23514 that names no arm."
  - "Assertion 6 uses a `sql` step, forced not preferred: the migration's own STEP 6 (g) pins the schedule EXACTLY, so a migration edit aborts the apply. The --post-apply hook is also where the drift really happens — an operator re-schedule, after the migration has self-verified."
  - "The plan's Task 1 verify was MEASURED unsatisfiable and was corrected rather than weakened: its whole-transcript `SKIP|skipping` grep returns 15 on the already-GREEN reference file. Scoped to the gate's own `psql:` prefix, both files measure 0."
  - "CLAUDE.md was edited outside the plan's files_modified list, deliberately: its standing directive that `pending:` must not be made to look empty is now false in its second clause and would misdirect plans 03-05."

patterns-established:
  - "When a gate assertion's genuine falsifier kills an EARLIER assertion first, the twin is wrong, not the gate: find the production change that lands the failure where first-failure identity requires it, and say in the prose which one was rejected and what it measured."
  - "A `toHaveLength(N)` beside a `toEqual(SET)`, never instead of one — a length is satisfied by any N names, which is exactly how a silently-substituted file passes."

requirements-completed: [SC-2, SC-3, SC-4]

coverage:
  - id: D1
    description: "Every section of test_compute_jobs_error_kind_copy_parity.sql (3) and test_derive_allocator_keys_fanout.sql (7) carries a RED-UNDER twin that the runner drives RED naming that section FIRST, then restores GREEN"
    requirement: SC-2
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_derive_allocator_keys_fanout.sql => arms: 7/7/0, biting: 7, all `RED (identity ok)`, baseline and restore exit 0"
        status: pass
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_compute_jobs_error_kind_copy_parity.sql => arms: 3/3/0, biting: 3, all `RED (identity ok)`, baseline and restore exit 0"
        status: pass
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs (FULL, at 31fae2ea) => 272 `RED (identity ok)`, arms: 272/272/0, biting: 272, tallies agree, exit 1 on the single lane-blocked-stale row"
        status: pass
    human_judgment: false
  - id: D2
    description: "Both files' baselines are GREEN on the lane with ZERO skip lines of their own — the pg_cron-conditional section 6 of the derive gate actually RUNS and is falsified rather than withheld"
    requirement: SC-2
    verification:
      - kind: integration
        ref: "bash scripts/pg-lane/run.sh --workdir <scratch> --apply <own RED-UNDER-SETUP list> --gate <gate> => exit 0 for both; own-prefix skip count 0 for both; the derive lane prints `Scheduled derive-allocator-key-dailies at 05:30 UTC` and the gate reads it back"
        status: pass
      - kind: integration
        ref: "calibration on the already-annotated reference file test_metrics_by_basis_write.sql => exit 0, whole-transcript skip count 15, own-prefix count 0 (the plan's whole-transcript form is unsatisfiable for every gate)"
        status: pass
    human_judgment: false
  - id: D3
    description: "FILES_FLOOR and ARMS_FLOOR equal the run's own coverage: and biting: lines, and a floor one higher FIRES"
    requirement: SC-2
    verification:
      - kind: integration
        ref: "runCorpus({filesFloor:41,armsFloor:272}) => defects=1 kinds=[\"lane-blocked-stale\"] (367.9 s); runCorpus({filesFloor:42,armsFloor:273}) => defects=3 kinds=[\"lane-blocked-stale\",\"floor\",\"floor\"] naming `FILES_FLOOR regression: 41 annotated file(s) < floor 42` and `ARMS_FLOOR regression: 272 biting arm(s) < floor 273` (368.7 s)"
        status: pass
      - kind: unit
        ref: "node -e module read => `41 272 0`; --parse-only coverage numerator equals the exported FILES_FLOOR"
        status: pass
    human_judgment: false
  - id: D4
    description: "`pending:` is pinned as the EXACT measured set (empty) beside an arm proving the class is still computed — never toEqual([]) alone"
    requirement: SC-4
    verification:
      - kind: unit
        ref: "src/__tests__/mutation-annotation-parser.test.ts `pending AIM (D-04)` — a stripped copy of a real annotated gate in a mkdtemp scope dir MUST classify `pending`, then the live corpus is asserted empty"
        status: pass
      - kind: unit
        ref: "NEUTER PROOF: classifyGateIdiom's `pending` branch changed to return \"unreachable\" => the AIM goes RED at `expected [] to include 'test_stripped_donor_probe.sql'`; restored, shasum-verified"
        status: pass
      - kind: unit
        ref: "NEUTER PROOF: the AIM's stripper neutered to `.filter(() => true)` => RED at the fixture's own non-vacuity guard `expect(stripped).not.toContain(\"RED-UNDER\")`; restored, shasum-verified"
        status: pass
    human_judgment: false
  - id: D5
    description: "`lane-blocked:` names exactly the three files still unannotated, pinned as a set; WAIVED_CEILING is still 0 and no twin carries a waiver"
    requirement: SC-3
    verification:
      - kind: unit
        ref: "LANE_BLOCKED_3 toEqual pin + toHaveLength(3) beside it; `grep -a -c '\"waiver\"'` = 0 in both new gate files; module read reports WAIVED_CEILING 0 and the run reports `arms: 272/272/0`"
        status: pass
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --self-test => SELF-TEST PASSED, including the stale-deferral probe arms"
        status: pass
    human_judgment: false
  - id: D6
    description: "The D-07 BLIND tripwire is flipped on purpose with the reason beside it, and its still-open half is named"
    requirement: SC-4
    verification:
      - kind: unit
        ref: "parser test: `annotatedFiles` CONTAINS and `pendingFiles` does NOT contain test_compute_jobs_error_kind_copy_parity.sql, with a dated comment naming the substrate exit that fired and deferring the classifier half to plan 06 (TODOS [REDUNDER-LANEBLOCKED-BLIND])"
        status: pass
    human_judgment: false
  - id: D7
    description: "No migration was modified and no lane was ever pointed at a Supabase database"
    requirement: SC-5
    verification:
      - kind: other
        ref: "git diff --name-only HEAD~3..HEAD | grep '^supabase/migrations/' => no output; git diff HEAD~3..HEAD | grep -aE '--db-url|--project-ref|supabase\\.co|db reset' => no output"
        status: pass
    human_judgment: false

duration: 68 min
completed: 2026-09-05
status: complete
---

# Phase 164.4.1 Plan 02: File move 1 — the pg_cron-deferred file and the smallest lane-blocked file Summary

**The corpus's last `pending:` file is annotated and `pending:` is measured EMPTY for the first time — 10 sections twinned across two gates that were blocked in the two DIFFERENT ways pg_cron blocks a gate, floors ratcheted to the printed `41` / `272` with separation driven both ways, and the D-04 pin moved to the empty set only behind an AIM that was proven able to fail.**

## Performance

- **Duration:** ~68 min
- **Tasks:** 3 of 3
- **Files modified:** 7 · **Files created:** 1 (this SUMMARY)
- **Commits:** 3 (`82d488b4`, `f1311fbf`, `31fae2ea`)

## What the run PRINTED

Full corpus at `31fae2ea`, 2026-09-05, 362 s wall clock:

```
coverage: files 41/71
unreachable: 27 file(s) raise outside the runner's identity idiom — … (TODOS [REDUNDER-NONIDIOM])
lane-blocked: 3 file(s) probe pg_extension for pg_cron, which the pg-lane cannot host — test_reconcile_dropped_enqueue_sweep.sql test_retention_orphaned_running.sql test_strategy_analytics_stuck_computing_reaper.sql (deferred 2026-09-03, TODOS [REDUNDER-PGCRON])
lane-probe: pg_cron AVAILABLE — lane-blocked class is STALE
  pending: 0 idiom file(s) without RED-UNDER — 
arms: 272/272/0   (executed/annotated/waived)
biting: 272   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 272   (arm lanes actually spawned — tallied inside runLane, independent of the 272 the verdict loop counted; plus 41 baseline / 41 restore leg(s))
per-arm lane time: mean 1.0s over 272 arm run(s)
```

Exit 1, with the defect table carrying **exactly one row**:

```
  KIND                  ARM                       FILE
  --------------------  ------------------------  ----
  lane-blocked-stale    -                         -
      MEASURE_FAIL — the lane can host pg_cron but 3 idiom file(s) are still classified lane-blocked: … the deferral has expired
```

That row is plan 01's tripwire, not a regression: `git status --porcelain` was empty
after the run, all 272 arms scored `RED (identity ok)`, and no floor defect fired.

An earlier measuring run at `f1311fbf` (before the constants moved) reported the same
numbers at 371 s. The values written into the constants are read off these lines, never
from the plan's 41/272 projection — which happened to be right, and would have been
written the same way if it had not been.

## SEPARATION, both directions, through the real verdict loop

```
filesFloor=41 armsFloor=272  exit=1  defects=1  kinds=["lane-blocked-stale"]                    (367.9 s)
filesFloor=42 armsFloor=273  exit=1  defects=3  kinds=["lane-blocked-stale","floor","floor"]    (368.7 s)
    FILES_FLOOR regression: 41 annotated file(s) < floor 42
    ARMS_FLOOR regression: 272 biting arm(s) < floor 273
```

So 41/272 is exactly the separation point. The `lane-blocked-stale` row is present in
**both** directions, which is what proves it is not what makes the second run fire.

## The two apply lists

**`test_compute_jobs_error_kind_copy_parity.sql`** (20 entries) — it was the corpus's
last `pending:` file and was blocked only through this list, never through its own text,
which names pg_cron nowhere:

```
scripts/pg-lane/fixtures/01-fixture-core.sql
scripts/pg-lane/fixtures/02-fixture-sanitize-tables.sql
scripts/pg-lane/fixtures/03-fixture-compute-jobs.sql
scripts/pg-lane/fixtures/27-fixture-strategy-analytics-computation-error.sql
supabase/migrations/20260513094906_enable_pg_cron.sql
supabase/migrations/20260411144407_compute_jobs_queue.sql
scripts/pg-lane/fixtures/04-fixture-compute-jobs-targets.sql
supabase/migrations/20260510175507_process_key_long_compute_job_kinds_repair.sql
supabase/migrations/20260515114555_compute_jobs_claim_token_fencing.sql
supabase/migrations/20260516104201_compute_jobs_audit_2026_05_07_residual.sql
supabase/migrations/20260522111858_compute_analytics_from_csv_kind.sql
supabase/migrations/20260529180000_fix_mark_compute_job_failed_error_kind_column.sql
supabase/migrations/20260614120000_derive_broker_dailies_kind.sql
supabase/migrations/20260708120000_sync_status_failed_final_bounce.sql
supabase/migrations/20260710120000_strategy_keys.sql
supabase/migrations/20260710130000_stitch_composite_kind.sql
supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql
supabase/migrations/20260825150000_sync_status_protect_marked_refresh.sql
supabase/migrations/20260826120000_computation_error_curated_copy.sql
supabase/migrations/20260826140000_compute_jobs_error_kind_orphaned.sql
```

Two entries were found by ITERATION on real lanes, not by reading:

- `20260516104201` — without it the apply aborts at `20260826140000:377` with
  `42P13 cannot change return type of existing function` on `get_user_compute_jobs`.
- `20260529180000` — `20260826140000`'s self-verify arm (c) calls
  `mark_compute_job_failed` and asserts it still REFUSES `'orphaned'`.

`20260513094906_enable_pg_cron.sql` sits ahead of `20260817120000` **and**
`20260826140000` because both RAISE `0A000 / feature_not_supported` on an absent
extension, `20260817120000` first. With it in place neither fires and the baseline
exits 0, all three Parts running.

**`test_derive_allocator_keys_fanout.sql`** (16 entries):

```
scripts/pg-lane/fixtures/01-fixture-core.sql
scripts/pg-lane/fixtures/02-fixture-sanitize-tables.sql
scripts/pg-lane/fixtures/03-fixture-compute-jobs.sql
scripts/pg-lane/fixtures/07-fixture-supabase-default-privileges.sql
scripts/pg-lane/fixtures/11-fixture-api-keys-created-at.sql
scripts/pg-lane/fixtures/15-fixture-auth-role.sql
scripts/pg-lane/fixtures/20-fixture-app-role-helper.sql
scripts/pg-lane/fixtures/21-fixture-api-keys-credential-columns.sql
scripts/pg-lane/fixtures/24-fixture-enqueue-compute-job-chain.sql
supabase/migrations/20260513094906_enable_pg_cron.sql
supabase/migrations/20260411144407_compute_jobs_queue.sql
scripts/pg-lane/fixtures/04-fixture-compute-jobs-targets.sql
supabase/migrations/20260418194206_scoring_weight_overrides.sql
supabase/migrations/20260420073003_allocator_holdings.sql
supabase/migrations/20260614120000_derive_broker_dailies_kind.sql
supabase/migrations/20260717233529_allocator_equity_derived_surface.sql
```

`20260614120000` is in the list because without it assertion 1's fan-out dies on
`compute_jobs_kind_fkey` — the `derive_broker_dailies` REGISTRY ROW is what is missing,
not anything in the gate. Both of this file's skips (`:56` fan-out fn absent, `:169`
pg_cron absent) are silent: the lane prints
`Scheduled derive-allocator-key-dailies at 05:30 UTC` and assertion 6 reads that row back.

### The skip measurement, corrected

The plan's Task 1 verify greps the WHOLE lane transcript for `SKIP|skipping` and requires
0. That is **unsatisfiable for every gate in this corpus**: the already-annotated, GREEN
reference file `test_metrics_by_basis_write.sql` measures **15** under it, all of them
PostgreSQL's own `does not exist, skipping` DDL notices from the apply phase, which no
gate controls. Scoped to skip lines carrying the gate file's own
`psql:supabase/tests/<gate>.sql` prefix — which is what the plan's `<fails_when>`
describes semantically — both files measure **0** (whole-transcript counts: 10 and 28).

## The LAYERED twins, with their measured step-1-alone outcomes

**`3/F-3` (copy-parity)** — shorten the `'orphaned'` sentence so it no longer tells the
user to retry. Step 1 ALONE was RUN on a lane and measured to ABORT the apply:

```
psql:supabase/migrations/20260826120000_computation_error_curated_copy.sql:1391: ERROR:  P0001:
  HONEST-01/F-3 verification failed: computation_error_copy('orphaned') does not carry the
  affirmative instruction to retry. …
STEP1-ONLY LANE EXIT=3
```

Step 2 re-points that one guard's needle (`'%retry the sync%'` → `'%went away%'`) and
nothing else. The layering is not a way around the migration's check — it is the
measurement that this gate is the second line of defence for a claim the migration also
makes, and the one that survives a re-based guard.

**`2` (derive)** — break the in-flight dedup for api_key jobs. MEASURED: dedup is
enforced TWICE, by `_enqueue_compute_job_internal`'s optimistic look-up
(`20260420073003:392-398`) and by the partial unique index
`compute_jobs_one_inflight_per_kind_api_key` (`:288-291`). Removing **either alone leaves
the assertion GREEN** — with the look-up narrowed the INSERT still loses to the index and
the function re-reads the winner; with the index narrowed the look-up short-circuits
before any INSERT. So a single-step mutation there is a `no-red`, not a falsifier. The
index step is scoped `kind <> 'derive_broker_dailies'` rather than dropping `'pending'`
from the predicate, because that migration's own Category-D probe (`:1050-1057`) still
requires a raw duplicate `poll_allocator_positions` INSERT to trip it — a blanket
narrowing aborts the apply with `Migration 066 failed: raw duplicate INSERT should have
hit …`. Step 2 needs `nth: 1`: those three lines occur twice in the function (the
optimistic look-up and the lost-the-race re-read) and only the first is the short-circuit.

## The falsifier that was REJECTED, and why

Assertion 5 of the derive gate asserts the api_key coherence arm SURVIVED the CHECK
re-base. The obvious falsifier — delete that arm — was measured and rejected: assertion
1's fan-out runs FIRST and its own INSERT then dies on the same constraint, propagating
out of `PERFORM enqueue_derive_broker_dailies_for_allocator_keys()` as an unhandled 23514
that aborts psql before assertion 5 is reached. The twin NARROWS the arm instead
(`AND (idempotency_key IS NOT NULL)`): every enqueue_compute_job path supplies a key so
assertions 1-4 stay green, while the assertion's own bare INSERT — and every other direct
writer, including the approval-gated phase35 backfill path — is refused. The migration's
STEP 6 arm (e) matches `%derive_broker_dailies%api_key_id IS NOT NULL%`, a substring test
an extra trailing conjunct satisfies unchanged.

## The one arm that came close to a waiver, and the root-cause fix

Assertion 3 of the derive gate asserts a coherence arm ADMITS a shape. Its falsifier makes
the INSERT raise — and the assertion had no handler, so a raw 23514 aborted psql naming no
arm, which the runner scores NO-IDENTITY rather than RED. Rather than spend the phase's
first waiver, the INSERT was wrapped in the nested `BEGIN … EXCEPTION WHEN check_violation`
idiom **assertion 4 of the same file already used** — the mirror-image reason (4 needs the
handler because the rejection is the PASS; 3 needs it because the rejection is the
FAILURE), with `GET STACKED DIAGNOSTICS CONSTRAINT_NAME` so a rejection by the wrong
constraint says so. No gate behaviour changed: a failure still fails, it now names itself.
`WAIVED_CEILING` stays 0 — nine arms moves, zero waivers.

## The D-04 pin move, and the AIM that guards it

The one-name pin `expect(corpus.pendingFiles).toEqual(["test_compute_jobs_error_kind_copy_parity.sql"])`
existed so an attestation of completeness could not ship ahead of this phase. It is now
the measured empty set — but never standing alone. The new `it` is titled
`pending AIM (D-04)` and asserts the empty set only AFTER an AIM: a copy of a real
annotated gate with every line-start `RED-UNDER` stripped is written into a `mkdtemp`
scope dir and MUST come back classified `pending`. That refuses the exact vacuity this
file already measured once at `expect(corpus.inertFiles).toEqual([])` (`:1445` / `:1504`),
where the empty-set assertion passed identically whether the branch worked or was dead
code.

The AIM was **proven able to fail**, twice, by neuter → observe RED → restore (byte
backups, each restore confirmed by `shasum`; never `git checkout --`):

| Neuter | Observed |
|---|---|
| the AIM's stripper → `.filter(() => true)` | RED at the fixture's own guard: `expect(stripped).not.toContain("RED-UNDER")` |
| `classifyGateIdiom`'s `pending` branch → `"unreachable"` | RED at the assertion that matters: `expected [] to include 'test_stripped_donor_probe.sql'` |

## The D-07 tripwire, flipped on purpose

The old assertion's own comment named its two exits: *"the day the classifier learns to
read apply lists, or the day the lane can host pg_cron"*. The SECOND happened (plan 01).
It now asserts the file is `annotated` and NOT `pending`, and states in the same place
that the FIRST exit is still open — `gateNeedsPgCron` still reads only a gate's executable
text and is still blind to its `RED-UNDER-SETUP` — and that it is closed deliberately in
plan 06 (TODOS `[REDUNDER-LANEBLOCKED-BLIND]`).

## Deviations from Plan

### 1. [Rule 1 — Bug in the plan's verify] Task 1's SKIP assertion was unsatisfiable

- **Found during:** Task 1
- **Issue:** the verify runs `grep -a -c -i -E 'SKIP|skipping'` over the WHOLE lane
  transcript and requires `0`. Measured on the already-annotated, GREEN reference file
  `test_metrics_by_basis_write.sql`: **15**. Those are PostgreSQL's own
  `does not exist, skipping` DDL notices from the apply phase.
- **Fix:** scoped the assertion to skip lines carrying the gate file's own
  `psql:supabase/tests/<gate>.sql` prefix — the thing the plan's `<fails_when>` actually
  describes. Not weakened: both files measure 0 there, and the calibration run is recorded
  in both gate headers.
- **Files modified:** the two gate headers (documentation of the measurement)
- **Commit:** `82d488b4`

### 2. [Rule 3 — Blocking] Worktree isolation vs. the plan's literal `cd`

- **Found during:** Task 1
- **Issue:** every `<automated>` verify begins `cd /Users/helios-mammut/claude-projects/quantalyze`,
  which is the MAIN checkout. This executor runs in an isolated worktree.
- **Fix:** every verify was run from the worktree root instead, otherwise byte-identical.
  `node_modules` was symlinked in first and `vitest 4.1.10` confirmed to resolve before any
  test was run, per the standing rule about `npx` silently downloading a different vitest.
- **Commit:** n/a (execution environment, no file change)

### 3. [Rule 2 — Missing critical correction] CLAUDE.md carried a directive this plan falsified

- **Found during:** Task 3
- **Issue:** CLAUDE.md states *"⛔ `pending:` is NOT empty and must not be made to look
  empty — the parser test pins it as a one-name SET"*. This plan legitimately made it
  empty, by founder decision D-04. Left alone, that line instructs plans 03-05 to undo
  the move.
- **Fix:** appended a dated CURRENCY block that supersedes the clause, records the
  measured 41/272 reading, says explicitly *"Do NOT restore the one-name pin"*, and warns
  that every run in this interval exits 1 on the tripwire. No dated measurement was
  rewritten (S2).
- **Files modified:** `CLAUDE.md`
- **Commit:** `31fae2ea`
- **Note:** CLAUDE.md is NOT in the plan's `files_modified` list. Flagged rather than
  silently done.

### 4. [No rule — recorded for plan 03-06] `vitest -t` treats its pattern as a REGEX

- The AIM's `it` title contains literal parentheses, so `-t "pending AIM (D-04)"` matches
  NOTHING and reports `107 skipped` — which reads exactly like a passing filtered run.
  The escaped form `-t "pending AIM \(D-04\)"` is required. The plan's Task 3 verify greps
  the FILE for the literal marker, which is unaffected.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema change at a trust
boundary. No `supabase/migrations/**` file was modified
(`git diff --name-only HEAD~3..HEAD | grep '^supabase/migrations/'` → no output), and no
lane was ever pointed at a Supabase target
(`git diff HEAD~3..HEAD | grep -aE '--db-url|--project-ref|supabase\.co|db reset'` → no
output).

## Known Stubs

None.

## What plans 03-06 inherit

- `FILES_FLOOR = 41`, `ARMS_FLOOR = 272`, `WAIVED_CEILING = 0`, all in
  `scripts/mutation-runner/run.mjs`, each with a dated derivation block above it.
- `pending:` is EMPTY and pinned as the empty set behind the `pending AIM (D-04)` arm.
  Plan 05's lane-blocked AIM should be guarded the same way (its own marker, greppable).
- `LANE_BLOCKED_3` — `test_reconcile_dropped_enqueue_sweep.sql` (39 sections),
  `test_retention_orphaned_running.sql` (25), `test_strategy_analytics_stuck_computing_reaper.sql`
  (29). Those are plans 03, 04 and 05.
- The `lane-blocked-stale` tripwire fires on EVERY run until plan 05 empties the class.
  A run showing any OTHER defect kind is a regression.
- `logCorpusClassification`'s narration (`"which the pg-lane cannot host"`, and
  `lane-probe: pg_cron AVAILABLE — lane-blocked class is STALE`) is still TRUE at this
  commit because the class is non-empty. Both sentences become false the moment plan 05
  lands, and correcting them at the source is plan 06's (PATTERNS § 6).
- `TODOS.md`'s `[REDUNDER-PGCRON]` and `[REDUNDER-LANEBLOCKED-BLIND]` entries are
  untouched by this plan, by scope.

## Self-Check: PASSED

All 8 claimed files exist on disk; all 3 claimed commits resolve in `git log --all`
(`82d488b4`, `f1311fbf`, `31fae2ea`).
