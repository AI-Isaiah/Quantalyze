---
phase: 164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire
plan: 03
type: execute
subsystem: sql-gates
tags: [red-under, pg_cron, floors-ratchet, file-move-2, retention-orphaned-running]
wave: 3
depends_on: ["164.4.1-02"]
files_modified:
  - supabase/tests/test_retention_orphaned_running.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-annotation-parser.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
autonomous: true
requirements: [SC-2]
provides:
  - "test_retention_orphaned_running.sql (25 sections, RAISE-on-absent shape) annotated and machine-proven — the phase's second FILE move"
  - "Floors and pins ratcheted to the values this plan's run PRINTS; lane-blocked set pinned at 2"
affects: [164.4.1-04, 164.4.1-05, 164.4.1-06]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 25 sections of test_retention_orphaned_running.sql carry twins the runner drives RED naming that section FIRST, then restores GREEN (SC-2)"
    - "The file's baseline is GREEN on the lane — its `1/JOB-05` RAISE-on-absent guard at :212-213 no longer fires — with ZERO skip lines (Part 2 at :474 RUNS)"
    - "cron.job.command on the lane equals what TEST holds for retention_compute_jobs_orphaned_running: the LAST scheduling migration (20260826140000) is in the apply list"
    - "FILES_FLOOR / ARMS_FLOOR equal the run's printed lines; a floor one higher FIRES; WAIVED_CEILING still 0 (D-03)"
    - "lane-blocked is pinned as the exact 2-name set; pending stays the measured empty set with its AIM (D-04)"
  artifacts:
    - path: "supabase/tests/test_retention_orphaned_running.sql"
      provides: "header with measured apply-list reasons + oracle-scope caveat; one RED-UNDER-SETUP; 25 twins"
    - path: "scripts/mutation-runner/run.mjs"
      provides: "dated derivation blocks; FILES_FLOOR / ARMS_FLOOR at the measured values"
    - path: "src/__tests__/mutation-annotation-parser.test.ts"
      provides: "annotatedFiles +1, LANE_BLOCKED_2, arithmetic line, toHaveLength(2)"
    - path: "src/__tests__/mutation-runner-floors.test.ts"
      provides: "totalAnchored re-pinned, tenth lineage paragraph, GREEN_LOG at the new numbers"
  key_links:
    - from: "supabase/tests/test_retention_orphaned_running.sql Parts 2-3 (cron.job.command oracle)"
      to: "supabase/migrations/20260826140000_compute_jobs_error_kind_orphaned.sql cron.schedule(...)"
      via: "the last writer of the job row; its body is what the gate's comment-stripped assertions read"
      pattern: "retention_compute_jobs_orphaned_running"
---

<objective>
Second FILE move: `test_retention_orphaned_running.sql` — 25 sections, the RAISE-on-absent spelling (`supabase/tests/test_retention_orphaned_running.sql:212-213`, PATTERNS C1's "the one to follow"), three Parts that read the deployed body out of cron.job.command.

CONTEXT decisions cited as D-01..D-07. This plan implements D-01 (the deferred file is annotated), D-03 (0 waivers) and D-04 (pins re-measured as exact sets).

**The oracle subtlety this file forces into the open (RESEARCH § Q3 caveat):** on TEST, cron.job.command holds a body deployed out of band, so Parts 2-3 detect DRIFT. On the lane it holds whatever the apply list just scheduled. Four migrations (re)schedule retention_compute_jobs_orphaned_running — 20260719120000, 20260720120000, 20260817120000 (its schedule call is at `supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql:602`) and 20260826140000 (the 'orphaned' fix; its schedule call is at `supabase/migrations/20260826140000_compute_jobs_error_kind_orphaned.sql:220`) — and cron.schedule upserts by name, so the LAST one applied is the body the gate reads. The apply list must therefore end its cron chain at 20260826140000, or the lane's oracle is a body TEST no longer has. The header must say this, and say that the twins prove FALSIFIABILITY of the assertions, not deployment drift.

⚠️ Expected red in this interval: every runner invocation exits 1 with EXACTLY ONE defect, `lane-blocked-stale`, naming the two files plans 04–05 take. Any other kind is a regression. `TRIPWIRE-RED:` line in every commit. No PR and no green-expecting `workflow_dispatch` before plan 05 (pushing the branch itself triggers nothing — plan 01 objective).

Purpose: SC-2 for the second-largest RAISE-shape file.
Output: annotated gate, measured floors, pins at the new sets.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-CONTEXT.md
@.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-PATTERNS.md
Also read the plan-02 SUMMARY (164.4.1-02-SUMMARY.md in this phase directory — it exists once plan 02 has run) for the copy-parity apply list and the exact pin sites plan 02 left.
@scripts/mutation-runner/GRAMMAR.md
@supabase/tests/test_retention_orphaned_running.sql
@supabase/tests/test_api_keys_exchange_not_user_writable.sql
@supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql
@supabase/migrations/20260826140000_compute_jobs_error_kind_orphaned.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply-list discovery — baseline GREEN, zero skips, oracle body = TEST's</name>
  <files>supabase/tests/test_retention_orphaned_running.sql</files>
  <read_first>
    - `supabase/tests/test_retention_orphaned_running.sql:1-30` — what it guards
    - `supabase/tests/test_retention_orphaned_running.sql:65-80` — the ANTI-GREEN-SKIP CONTRACT
    - `supabase/tests/test_retention_orphaned_running.sql:212-230` — the 1/JOB-05 presence / registration / NULL-command arms
    - `supabase/tests/test_retention_orphaned_running.sql:264` — the comment-stripper canary over cron.job.command; body arms at 378-379 and 420; the Part-2 skip near 474
    - `supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql:558-565` — RAISE on absent pg_cron with ERRCODE feature_not_supported
    - `supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql:572-602` — unschedule/schedule; its self-verify blocks follow 602
    - `supabase/migrations/20260826140000_compute_jobs_error_kind_orphaned.sql:198-220` — same guard shape; re-schedules the SAME job with the 'orphaned' body; the widened-CHECK self-verify is at 491-503
    - The copy-parity apply list plan 02 produced (read the RED-UNDER-SETUP line of test_compute_jobs_error_kind_copy_parity.sql) — it already reaches 20260817120000 and 20260826140000 with pg_cron enabled; seed from it
    - PATTERNS § 1b header paragraph (`supabase/tests/test_api_keys_exchange_not_user_writable.sql:131-158`) — the "sized by the skips, MEASURED" shape
  </read_first>
  <action>
    Build the `RED-UNDER-SETUP` list by iteration on real lanes under a scratch `--workdir` OUTSIDE the repo: seed from plan 02's copy-parity list, keep `supabase/migrations/20260513094906_enable_pg_cron.sql` before the first cron-touching migration, include EVERY migration that (re)schedules `retention_compute_jobs_orphaned_running` in chronological order so the row's final body is `20260826140000`'s, and whatever tables/functions the terminalizer body and the gate's seeds need (read the apply abort text; add the real migration, never a stand-in that SHADOWS the object under test — `sql-gate-lint` R5/R6/R7 will fire on a stand-in). Iterate until exit 0 and `grep -a -c -i -E 'SKIP|skipping' out` = 0.

    Header: boilerplate paragraph; one ⚠️ note per non-obvious entry with the MEASURED reason; an explicit ORACLE-SCOPE paragraph (lane oracle = last writer in the list = `20260826140000`; twins prove falsifiability, not drift; on TEST the same arms detect drift); the `RED-UNDER-SETUP` line. Do not convert any spelling; do not edit the gate's assertions.
  </action>
  <verify>
    <automated>cd /Users/<user>/claude-projects/quantalyze && S=/private/tmp/claude-501/-Users-<user>-claude-projects-quantalyze/0339e09c-f6d0-4f3c-abb2-8254bf7a3a12/scratchpad/p03-baseline && rm -rf "$S" && mkdir -p "$S" && f=test_retention_orphaned_running && list=$(grep -a -o 'RED-UNDER-SETUP: .*' "supabase/tests/$f.sql" | sed 's/^RED-UNDER-SETUP: //' | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8"));console.log(j.apply.join(" "))') && bash scripts/pg-lane/run.sh --workdir "$S/$f" --apply $list --gate "supabase/tests/$f.sql" > "$S/$f.out" 2>&1 && test "$(grep -a -c -i -E 'SKIP|skipping' "$S/$f.out")" = "0" && echo "$list" | tr ' ' '\n' | grep -a -n -E '20260513094906|20260817120000|20260826140000' && echo "$list" | tr ' ' '\n' | grep -a -n '20260826140000' | cut -d: -f1 | xargs -I{} test {} -gt "$(echo "$list" | tr ' ' '\n' | grep -a -n '20260817120000' | cut -d: -f1)"</automated>
    <fails_when>`run.sh` exits non-zero (the `1/JOB-05` RAISE-on-absent at `:212-213` fired, a migration's self-verify aborted the apply, or the gate raised `TEST FAILED`) — the `&&` chain stops there with the lane output in `$S/<file>.out`; the `SKIP|skipping` count is not `0` (Part 2 at `:474` is still withheld); the `grep -a -n` over the list finds fewer than three of `20260513094906` / `20260817120000` / `20260826140000` (grep exits 1 when any is absent); or the final `test {} -gt` exits 1 because `20260826140000` is not ordered AFTER `20260817120000` (the lane's `cron.job.command` would then be the older body, not TEST's) — an empty `$list` (missing or malformed `RED-UNDER-SETUP`) fails at the `run.sh --apply` step</fails_when>
  </verify>
  <done>Baseline exit 0 with zero skip lines; the list carries the enabling migration and both terminalizer migrations with `20260826140000` after `20260817120000`; header records the measured reasons and the oracle-scope caveat.</done>
</task>

<task type="auto">
  <name>Task 2: Twin all 25 sections and drive each RED naming itself first</name>
  <files>supabase/tests/test_retention_orphaned_running.sql</files>
  <read_first>
    - `scripts/mutation-runner/GRAMMAR.md` Shapes 1-3 and rules 3a/4; PATTERNS § 1c (LAYERED when the target migration self-verifies — both terminalizer migrations self-verify by NAME and by body tokens, so expect layered twins and MEASURE that step 1 alone aborts before adding step 2)
    - PATTERNS § 2 — the exception wrap, if any new twin turns a gate-internal statement into a raw driver error
    - The runner's section list for this file: `node -e 'import("./scripts/mutation-runner/run.mjs").then(m=>console.log(m.gateSectionCount(require("fs").readFileSync("supabase/tests/test_retention_orphaned_running.sql","utf8"))))'` prints 25; identities via `grep -a -oE "TEST FAILED \([^)]*\)" | sort -u`
  </read_first>
  <action>
    Annotate every section (prose `RED-UNDER` + `RED-UNDER-M` twin directly above the assertion). For body-token arms (Parts 2-3 read `cron.job.command`) the falsifier is an edit to the LAST scheduling migration's body (`20260826140000`) — an edit to `20260817120000`'s body is overwritten by the later schedule and comes back `no-red`; say so in the prose of the first such arm. For the registration arms (`1/JOB-05` job missing / NULL command), mutate the jobname or the command in `20260826140000`'s `cron.schedule` call, layering a matching edit to its self-verify where step 1 alone aborts the apply. `occurrences` exact on every edit.

    Proof loop per arm batch: `node scripts/mutation-runner/run.mjs --file supabase/tests/test_retention_orphaned_running.sql > <scratch>/ret.log 2>&1` (exit 1 expected from the stale tripwire) — every arm `RED (identity ok)`, EXACTLY ONE defect row, kind `lane-blocked-stale`. Never edit during a run.
  </action>
  <verify>
    <automated>cd /Users/<user>/claude-projects/quantalyze && S=/private/tmp/claude-501/-Users-<user>-claude-projects-quantalyze/0339e09c-f6d0-4f3c-abb2-8254bf7a3a12/scratchpad/p03-arms && rm -rf "$S" && mkdir -p "$S" && n=test_retention_orphaned_running && node scripts/mutation-runner/run.mjs --file "supabase/tests/$n.sql" > "$S/$n.log" 2>&1; red=$(grep -a -c 'RED (identity ok)' "$S/$n.log"); rows=$(awk '/^  KIND +ARM/{f=1;next} f && /^  [a-z]/{n++} END{print n+0}' "$S/$n.log"); stale=$(grep -a -c -E '^  lane-blocked-stale ' "$S/$n.log"); echo "red=$red rows=$rows stale=$stale"; test "$red" -ge 25 && test "$rows" = "1" && test "$stale" = "1" && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -E '^lane-blocked: 2 file\(s\) .*test_reconcile_dropped_enqueue_sweep.sql test_strategy_analytics_stuck_computing_reaper.sql' && npx vitest run src/__tests__/mutation-annotation-parser.test.ts -t "every SECTION an annotated file raises for also carries a twin" 2>&1 | grep -a -E 'passed'</automated>
    <fails_when>`red` < 25 (an arm printed something other than `RED (identity ok)` — `no-red` from an edit to the overwritten `20260817120000` body, `wrong-first-failure`, or `occurrence-mismatch`); `rows` ≠ `1` or `stale` ≠ `1` (a second defect row exists, or the single row is not `lane-blocked-stale`); the `--parse-only` `lane-blocked:` line does not read exactly `2 file(s)` naming reconcile and reaper (the retention file is still classified lane-blocked — its annotation did not parse); or the vitest output lacks `passed` (a raised section has no twin)</fails_when>
  </verify>
  <done>≥25 `RED (identity ok)` verdicts for the file with the single expected `lane-blocked-stale` row as the only defect; `--parse-only` names exactly reconcile + reaper as lane-blocked; the set-inclusion control passes.</done>
</task>

<task type="auto">
  <name>Task 3: Ratchet floors and re-pin the measured sets</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/mutation-annotation-parser.test.ts, src/__tests__/mutation-runner-floors.test.ts</files>
  <read_first>
    - PATTERNS § 3 and § 6; plan 02's SUMMARY for the exact edit sites it left (the blocks above `export const FILES_FLOOR` and `export const ARMS_FLOOR`, `LANE_BLOCKED_3`, the `pending` AIM, the lineage paragraph list, the `GREEN_LOG` rows) — locate them by these identifiers with `grep -an`, never by line number: plan 02 moved every line
    - In `src/__tests__/mutation-annotation-parser.test.ts`: `filesAnnotated).toBe(`, `LANE_BLOCKED_3`, `toHaveLength(3)`, the sum test `it("the FIVE classes sum`; in `src/__tests__/mutation-runner-floors.test.ts`: `totalAnchored).toBe(`, `RE-MEASURED 2026-09-0`, `const GREEN_LOG = [`, `arm lane(s) spawned`, `per-file row(s) but reported`, `toContain("reports `
  </read_first>
  <action>
    Full run first: `node scripts/mutation-runner/run.mjs > <scratch>/full.log 2>&1` (exit 1, one `lane-blocked-stale` row). Read `coverage: files N/71`, `biting: B`, `arms: B/B/0`, `lane-invocations:`, `per-arm lane time:` — these are the constants (projection 42 / plan-02 B + 25; write the printed values, not this).

    `scripts/mutation-runner/run.mjs`: `FILES_FLOOR = N`, `ARMS_FLOOR = B`; a new dated derivation block above each in the house shape (VALUE / DATE+sha / COMMAND with printed lines / SAMPLE SIZE / COVERAGE naming this file / SEPARATION both ways via `runCorpus({scopeDir:"supabase/tests",filesFloor,armsFloor,log:()=>{}})` — N/B → only `lane-blocked-stale`; N+1/B+1 → two additional `floor` defects quoted verbatim / RECORD). Prior blocks stay. `WAIVED_CEILING` untouched at 0; one dated CURRENCY line (D-03).

    `src/__tests__/mutation-annotation-parser.test.ts`: `filesAnnotated` → N and the sorted array gains `test_retention_orphaned_running.sql`; `LANE_BLOCKED_3` → `LANE_BLOCKED_2` (reconcile, reaper) with a dated line saying retention LEFT by annotation, prior list kept as lineage in a comment; the sum test's `toHaveLength(3)` → `toHaveLength(2)` beside `toEqual(LANE_BLOCKED_2)`, append the dated arithmetic line (`N + 0 + 27 + 0 + 2 = 71`); the `pending` AIM + `toEqual([])` from plan 02 stay as they are (D-04 — do not touch a pin you are not re-measuring); PARTITION test unchanged and green.

    `src/__tests__/mutation-runner-floors.test.ts`: `totalAnchored` → new total; APPEND a dated `RE-MEASURED … (plan 164.4.1-03 …)` paragraph after plan 02's; `GREEN_LOG` header lines, one new per-file row in sorted position from the run's own row, the sum comment, and the three derived literals (`B arm lane(s) spawned`, `printed N+1 per-file row(s) but reported N`, `reports B`).

    Commit with the `TRIPWIRE-RED:` line naming the TWO remaining files.
  </action>
  <verify>
    <automated>cd /Users/<user>/claude-projects/quantalyze && npx vitest run src/__tests__/mutation-annotation-parser.test.ts src/__tests__/mutation-runner-floors.test.ts 2>&1 | grep -a -E 'Test Files .* passed' && node -e 'import("./scripts/mutation-runner/run.mjs").then(m=>{console.log(m.FILES_FLOOR,m.ARMS_FLOOR,m.WAIVED_CEILING);if(m.FILES_FLOOR<42||m.WAIVED_CEILING!==0)process.exit(1)})' && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -E "^coverage: files $(node -e 'import("./scripts/mutation-runner/run.mjs").then(m=>console.log(m.FILES_FLOOR))')/71" && grep -a -c 'LANE_BLOCKED_2' src/__tests__/mutation-annotation-parser.test.ts | grep -v '^0$'</automated>
    <fails_when>the vitest summary lacks `Test Files … passed` (a pin — `filesAnnotated`, the sorted array, `totalAnchored`, `GREEN_LOG`, the sum test or PARTITION — disagrees with the run); the `node -e` read exits 1 (`FILES_FLOOR` < 42 or `WAIVED_CEILING` ≠ 0); the `--parse-only` `coverage:` numerator differs from the exported `FILES_FLOOR` (no matching line — the constant was not written from the print); or the `LANE_BLOCKED_2` count is `0` (the set was not re-pinned)</fails_when>
  </verify>
  <done>Both vitest files green; FILES_FLOOR ≥ 42 equals the printed coverage numerator; ARMS_FLOOR equals printed biting with separation quoted both ways; WAIVED_CEILING 0; `LANE_BLOCKED_2` pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lane oracle → TEST oracle | `cron.job.command` on the lane is authored by the apply list; on TEST by deployment |
| gate twins → real migrations | copies mutated on a throwaway cluster |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.1-10 | Repudiation | oracle arms read as drift-detection on the lane | medium | mitigate | the header's ORACLE-SCOPE paragraph states they prove falsifiability only; last-writer migration named |
| T-164.4.1-11 | Tampering | a body twin edits an OVERWRITTEN migration (20260817120000) and comes back no-red, tempting a waiver | medium | mitigate | Task 2 directs body edits to the last writer; D-03 forbids the waiver — a no-red is a wrong target, fix the target |
| T-164.4.1-12 | Repudiation | floors pinned from projection | high | mitigate | Task 3 reads the printed lines; SEPARATION both ways |
| T-164.4.1-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- `--parse-only`: `coverage: files N/71`, `lane-blocked: 2 file(s) … test_reconcile_dropped_enqueue_sweep.sql test_strategy_analytics_stuck_computing_reaper.sql`, `pending: 0`.
- Full run: exit 1, exactly one `lane-blocked-stale` row; `arms: B/B/0`, tallies agree.
- No `"waiver"` in the gate file; no file under `supabase/migrations/` in the diff.
</verification>

<success_criteria>
- SC-2 for `test_retention_orphaned_running.sql`: 25 sections proven; floors measured and separated; D-03/D-04 held.
</success_criteria>

<output>
Create `.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-03-SUMMARY.md` when done, quoting the run's own summary lines, both SEPARATION results, the apply list, and every LAYERED twin's measured step-1-alone outcome.
</output>
