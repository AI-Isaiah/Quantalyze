---
phase: 164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire
plan: 04
subsystem: sql-gates
tags: [red-under, pg_cron, floors-ratchet, file-move-3, stuck-computing-reaper, reclassification]

requires:
  - phase: 164.4.1
    provides: "plan 01's pg_cron-preloading pg-lane and the `lane-blocked-stale` tripwire; plan 03's floors 42/296, the empty `pending:` pin and the LANE_BLOCKED_2 set as corrected by fcbc0159 (296/296/0, zero waivers)"
provides:
  - "test_strategy_analytics_stuck_computing_reaper.sql annotated: 28 sections, ALL 28 machine-proven to redden naming themselves FIRST and then restore GREEN"
  - "A 25-entry apply list sized by the file's THREE `RAISE NOTICE 'SKIP'` sites, measured to print ZERO gate-owned skip lines, with every entry's necessity read off a real lane by one-out ablation"
  - "FILES_FLOOR 43 and ARMS_FLOOR 324, equal to the run's own printed lines, with SEPARATION driven both ways through the real verdict loop"
  - "lane-blocked pinned as the exact 1-name set; the D-04 empty `pending:` pin left untouched"
  - "A SECOND reclassification precedent: Part 3's unfalsifiable registration guard loses its `TEST FAILED (` identity rather than taking a waiver — WAIVED_CEILING still 0"
  - "scripts/pg-lane/fixtures/29-fixture-compute-jobs-priority.sql — a stand-in for compute_jobs.priority, whose real migration cannot coexist with the claim-token migration on one lane"
affects: [164.4.1-05, 164.4.1-06]

actuals:
  tokens: 151945
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "An apply list SIZED BY THE SKIP NOTICES, with the zero measured scoped to the gate's own psql prefix: an unscoped `grep -i SKIP` can never reach 0 because Postgres emits `… does not exist, skipping` from every DROP IF EXISTS in an applied migration"
    - "A twin that must break TWO INDEPENDENT DEFENCES of one invariant in the same mutation — the deployed cron body (or the bridge) AND the table trigger — because either alone is silently repaired by the other; six of this file's twins have that shape and it is the executable form of what the gate's own header calls defence-in-depth"
    - "For an S-2 SEED-INTEGRITY CONTROL the honest falsifier is a GATE-file edit, not a production one: such an arm makes no claim about production, so no production mutation can falsify it, and the arm's own message names a broken fixture"
    - "A `pg_get_functiondef` text anchor is satisfied by a COMMENT inside the function body — a revert that removes the code and keeps the explanatory comment is invisible to every `v_fn ~*` anchor in the corpus"
    - "The runner's `arm` field is matched against the LITERAL inside `TEST FAILED (…)`; section folding applies to the denominator it counts, never to the identity it reads. A twin spelled with the folded section name reports WRONG-ARM"

key-files:
  created:
    - scripts/pg-lane/fixtures/29-fixture-compute-jobs-priority.sql
    - .planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-04-SUMMARY.md
  modified:
    - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
    - scripts/mutation-runner/run.mjs
    - src/__tests__/mutation-annotation-parser.test.ts
    - src/__tests__/mutation-runner-floors.test.ts
    - .planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/deferred-items.md

key-decisions:
  - "D-03 honoured for the FOURTH time in this phase family, and for the SECOND time by reclassification: Part 3's `TEST FAILED (3)` guard has no first-failure mutation, so it stopped claiming to be an assertion instead of taking a waiver. WAIVED_CEILING is UNEDITED at 0 and the corpus carries zero waivers."
  - "The plan's `<verify>` predicate `grep -a -c -i -E 'SKIP|skipping' out == 0` is UNSATISFIABLE by any correct apply list and was replaced by the SCOPED form. Postgres itself emits 12 `does not exist, skipping` NOTICEs from DROP IF EXISTS in five of the applied migrations. The load-bearing measurement — zero skips OWNED BY THE GATE — is what plan 03 also used."
  - "A NEW stand-in fixture was created rather than applying migration 20260428120836. That migration adds `compute_jobs.priority` AND creates the first two-argument claim_compute_jobs_with_priority; no migration ever drops that overload, so the bare `COMMENT ON FUNCTION` at 20260515114555:348 aborts the apply with 42725. 20260515114555 is not optional — it is the sole source of `compute_jobs.claim_token`, which Parts 4 and 5 seed."
  - "The gate's FOUR column-shape raises are NOT proven on the lane and the header says so: 03-fixture-compute-jobs.sql already declares `computing_started_at`, so 20260802120000's STEP 1 applies as a no-op. The fixture was NOT edited — GRAMMAR rule 4 refuses twins on stand-ins, and test_sync_status_marked_refresh_protected.sql reads that column off the same fixture without applying the migration."
  - "The measured B is 324, not the 296 + 29 = 325 the plan projected, because the reclassification removes a section. Floors are set to what the run PRINTS."

patterns-established:
  - "Measure the DOMINANCE question before writing the twin, and quote what the lane printed. Both escape routes for section `3` were driven end-to-end and both produced raw driver errors naming no arm (22004 at Part 2's EXECUTE; XX000 at Part 3's second tick). The claims happen to match plan 03's, which is not the same as being evidence."
  - "A twin's own JSON is part of the file the runner counts occurrences over. A single-line gate-self-edit needle appears TWICE the moment it is written down; a MULTI-LINE needle does not, because JSON escapes the newline. Rule 2 caught this on the first parse."

requirements-completed: []

coverage:
  - id: D1
    description: "The apply list is GREEN on the lane with zero gate-owned skip lines and all seven Parts running"
    requirement: SC-2
    verification:
      - kind: integration
        ref: "bash scripts/pg-lane/run.sh --workdir <scratch outside the repo> --apply <the file's own RED-UNDER-SETUP> --gate supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql => `TAG=v4b rc=0 secs=1`; gate-owned SKIP-notice count 0; Part 1a / 1b / 2 / 3 / 4 / 5 / 6 OK all printed"
        status: pass
      - kind: integration
        ref: "one-out ablation over all 25 entries: 16 REQUIRED with their first error quoted, 9 REMOVABLE and kept on purpose as the bridge/mark-RPC/cron chain"
        status: pass
      - kind: other
        ref: "the list carries 20260513094906 at position 5 (before every cron-touching migration), 20260802120000 at 23 and 20260803130000 last at 25"
        status: pass
  - id: D2
    description: "Every section the file raises for carries a twin, and all of them are driven RED naming that section FIRST and then restored GREEN"
    requirement: SC-2
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql => `sections 28 / judged 28 / annotated 28 / waived 0 / biting 28`, 28 `RED (identity ok)`, baseline and restore exit 0, exactly one defect and it is `lane-blocked-stale`"
        status: pass
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs (FULL) => 324 `RED (identity ok)`, `arms: 324/324/0`, `biting: 324`, `lane-invocations: 324`, tallies agree, the 43 per-file biting rows SUM to 324"
        status: pass
      - kind: other
        ref: "Section `3` was RECLASSIFIED after two measured escape routes failed on real lanes. Not waived, not deleted; the guard is kept as a named INVARIANT."
        status: pass
    human_judgment: true
  - id: D3
    description: "FILES_FLOOR and ARMS_FLOOR equal the run's own printed lines, and a floor one higher FIRES"
    requirement: SC-2
    verification:
      - kind: integration
        ref: "runCorpus({filesFloor:43,armsFloor:324}) => defects=1 kinds=[\"lane-blocked-stale\"] (442.3 s); runCorpus({filesFloor:44,armsFloor:325}) => defects=3 kinds=[\"lane-blocked-stale\",\"floor\",\"floor\"] naming `FILES_FLOOR regression: 43 annotated file(s) < floor 44` and `ARMS_FLOOR regression: 324 biting arm(s) < floor 325` (441.3 s)"
        status: pass
      - kind: unit
        ref: "node -e module read => `43 324 0`; --parse-only prints `coverage: files 43/71`, equal to the exported FILES_FLOOR"
        status: pass
    human_judgment: false
  - id: D4
    description: "lane-blocked is pinned as the exact 1-name set; the D-04 empty `pending:` pin is untouched"
    requirement: SC-2
    verification:
      - kind: unit
        ref: "LANE_BLOCKED_1 toEqual pin + toHaveLength(1) beside it; the prior 2-name list kept as lineage in a comment; `pending AIM (D-04)` and its toEqual([]) not edited"
        status: pass
      - kind: integration
        ref: "--parse-only prints `lane-blocked: 1 file(s) … test_reconcile_dropped_enqueue_sweep.sql` and `  pending: 0 idiom file(s) without RED-UNDER — `"
        status: pass
    human_judgment: false
  - id: D5
    description: "WAIVED_CEILING is unedited at 0 and the corpus carries no waiver"
    requirement: SC-3
    verification:
      - kind: unit
        ref: "module read reports WAIVED_CEILING 0; the full run reports `arms: 324/324/0` and NO `floor` defect; `grep -a -c '\"waiver\"'` on the gate => 0"
        status: pass
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --self-test => `SELF-TEST PASSED` (both floor modes, the waiver ceiling, the wrong-identity mode, MEASURE_FAIL, the absurdity floor, the stand-in refusal, the stale-deferral probe and the 164.3.1-11 regression corpus all fire)"
        status: pass
    human_judgment: false
  - id: D6
    description: "No migration was modified and no lane was ever pointed at a Supabase database"
    requirement: SC-5
    verification:
      - kind: other
        ref: "git diff --stat fcbc0159..HEAD => no path under supabase/migrations/; every lane ran via scripts/pg-lane/run.sh with --workdir under the session scratchpad, outside the repo"
        status: pass

duration: 118 min
completed: 2026-09-05
status: complete
---

# Phase 164.4.1 Plan 04: Stuck-Computing Reaper Gate Summary

The largest `RAISE NOTICE 'SKIP'`-shaped idiom gate is annotated and machine-proven —
28 sections, all 28 biting — on an apply list sized so the baseline withholds nothing.

## What was built

`supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql` now carries a
`RED-UNDER-SETUP` apply list, a header stating what the lane can and cannot falsify, and
28 prose/twin pairs. `FILES_FLOOR` and `ARMS_FLOOR` ratchet to `43` and `324`.

**⚠️ Read the run's own lines, not this prose.** Measured 2026-09-05:

```
coverage: files 43/71
lane-blocked: 1 file(s) probe pg_extension for pg_cron, which the pg-lane cannot host — test_reconcile_dropped_enqueue_sweep.sql (deferred 2026-09-03, TODOS [REDUNDER-PGCRON])
lane-probe: pg_cron AVAILABLE — lane-blocked class is STALE
  pending: 0 idiom file(s) without RED-UNDER — 
arms: 324/324/0   (executed/annotated/waived)
biting: 324   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 324   (arm lanes actually spawned — tallied inside runLane, independent of the 324 the verdict loop counted; plus 43 baseline / 43 restore leg(s))
  file test_strategy_analytics_stuck_computing_reaper.sql: sections 28 / judged 28 / annotated 28 / waived 0 / biting 28
per-arm lane time: mean 1.1s over 324 arm run(s)
```

## The zero-skip baseline, and why the plan's predicate could not be used

The file baselines GREEN with or without pg_cron: `:282`, `:326` and `:483` withhold
Part 1b and the whole of Parts 2 and 3 behind `RAISE NOTICE 'SKIP …'`. A section behind a
still-firing skip is un-falsifiable, so the list is SIZED by those three sites.

Measured with the 25-entry list:

```
TAG=v4b rc=0 secs=1
```

and all seven Parts print:

```
Part 1a OK: computing_started_at is timestamptz, nullable, no default; …
Part 1b OK: reap_strategy_analytics_stuck_computing registered at */15 * * * * …
Part 2 OK: stranded row reaped on all four columns; …
Part 3 OK: LIMIT bound holds -- my 25 oldest seeds reaped on tick 1 …
Part 4 OK: transition into computing stamps; …
Part 5 OK: branch (b) terminal failure clears computing_started_at.
Part 6 OK: the stamp trigger never advances the clock …
```

**DEVIATION (documented, not silently adapted).** The plan's Task-1 `<automated>` block
asserts `grep -a -c -i -E 'SKIP|skipping' out` equals `0`. **No correct apply list can
satisfy that.** Postgres emits its own `… does not exist, skipping` NOTICEs from the
`DROP … IF EXISTS` statements inside applied migrations — twelve of them on this list:

```
psql:scripts/pg-lane/fixtures/01-fixture-core.sql:1: NOTICE:  00000: schema "auth" does not exist, skipping
psql:supabase/migrations/20260411144407_compute_jobs_queue.sql:233: NOTICE:  00000: policy "compute_jobs_deny_all" for relation "compute_jobs" does not exist, skipping
psql:supabase/migrations/20260802120000_…:220: NOTICE:  42701: column "computing_started_at" of relation "strategy_analytics" already exists, skipping
…
```

The measurement actually taken is the one the plan's own `<fails_when>` describes — the
three gate-owned skips at `:282`, `:326`, `:483` — scoped to the gate's psql prefix:

```
grep -a -c -iE '^psql:[^ ]*test_strategy_analytics_stuck_computing_reaper\.sql:[0-9]+: NOTICE:.*SKIP'  ->  0
```

This is the same scoped form plan 03's SUMMARY used ("gate-own SKIP-notice count 0").

## The apply list, measured entry by entry

One-out ablation on real lanes, every entry dropped in turn:

| # | entry | verdict / first error |
|---|---|---|
| 1 | `01-fixture-core.sql` | REQUIRED — `42P01: relation "profiles" does not exist` |
| 2 | `02-fixture-sanitize-tables.sql` | REQUIRED — `42P01: relation "portfolios" does not exist` |
| 3 | `03-fixture-compute-jobs.sql` | REQUIRED — `42P01: relation "public.strategy_analytics" does not exist` |
| 4 | `27-fixture-strategy-analytics-computation-error.sql` | REQUIRED — `42703: column "computation_error" … does not exist` |
| 5 | `20260513094906_enable_pg_cron.sql` | REQUIRED — `0A000: JOB-02: pg_cron extension is NOT installed` |
| 6 | `20260411144407_compute_jobs_queue.sql` | REQUIRED — `42P01: relation "compute_jobs" does not exist` |
| 7 | `04-fixture-compute-jobs-targets.sql` | REQUIRED — `42703: column "allocator_id" does not exist` |
| 8 | `29-fixture-compute-jobs-priority.sql` | REQUIRED — `42703: column "priority" of relation "compute_jobs" does not exist` |
| 9 | `20260412094454_sync_strategy_analytics_status.sql` | REMOVABLE — kept: it is the bridge the 20260802120000 re-base re-bases |
| 10 | `20260505115047_mark_compute_job_atomic_status_bridge.sql` | REMOVABLE — kept: origin of the in-RPC bridge call Parts 4/5 drive |
| 11 | `20260510175507_process_key_long_compute_job_kinds_repair.sql` | REMOVABLE — kept: keeps the kinds registry in the shape #13 verifies |
| 12 | `20260515114555_compute_jobs_claim_token_fencing.sql` | REQUIRED — `P0001: … mark_compute_job_done missing NOT EXISTS set-based fan-in` |
| 13 | `20260516104201_compute_jobs_audit_2026_05_07_residual.sql` | REMOVABLE — kept: it is what verifies #12's body rather than assuming it |
| 14 | `20260522111858_compute_analytics_from_csv_kind.sql` | REQUIRED — `23503: … violates foreign key constraint "compute_jobs_kind_fkey"` |
| 15 | `20260528183100_mark_compute_job_strict_claim_token.sql` | REMOVABLE — kept: LAST writer of the mark RPCs Parts 4/5 call |
| 16 | `20260529180000_fix_mark_compute_job_failed_error_kind_column.sql` | REQUIRED — `42703: column "last_error_kind" … does not exist` |
| 17 | `20260614120000_derive_broker_dailies_kind.sql` | REQUIRED — `23503: … "compute_jobs_kind_fkey"` (Part 4's second kind) |
| 18 | `20260707120000_sync_status_preserve_warnings.sql` | REMOVABLE — kept: the computation_warned semantics Part 2 arm A/SI-02 reads |
| 19 | `20260708120000_sync_status_failed_final_bounce.sql` | REQUIRED — `P0001: … strategy_analytics.computation_warned column missing` |
| 20 | `20260710120000_strategy_keys.sql` | REMOVABLE — kept: base for #21 |
| 21 | `20260710130000_stitch_composite_kind.sql` | REMOVABLE — kept: kind registry chain |
| 22 | `20260710150000_sync_status_supersede_failed_per_kind.sql` | REMOVABLE — kept: it is F-3/PUB-02, exactly what `1/re-base` names |
| 23 | `20260802120000_…_stuck_computing_reaper.sql` | REQUIRED — `TEST FAILED (1/JOB-01): branch (a) does not maintain computing_started_at with a conditional CASE` |
| 24 | `20260803120000_…_null_stamp_clock_start.sql` | REQUIRED — `TEST FAILED (6a/G1/D-01/SC-17-01): a direct write ADVANCED computing_started_at …` |
| 25 | `20260803130000_reaper_limit_bound_materialized_cte.sql` | REMOVABLE — kept: it is the LAST writer of `cron.job.command`, the oracle |

## LAYERED twins — every step-alone outcome was RUN

| arm | why layered | measured |
|---|---|---|
| `1/re-base` | `20260802120000` STEP 7 re-checks the same anchor | the two-conjunct deletion + STEP-7 re-base came back **`no-red`**; a fourth step was needed (see Deviations) |
| `1/JOB-02/SI-02` | `20260803130000` STEP 2 greps `%computation_warned%` | RED (identity ok) with the STEP-2 check re-based to `IF FALSE THEN` |
| `1/JOB-03` | STEP 2 greps `%interval ''16 hours''%` | RED (identity ok), same shape |
| `2/arm A/JOB-01` | the stamp TRIGGER's arm (d) repairs the cron body's edit | RED (identity ok) only with both migrations edited |
| `4a/seed` | THREE-deep: bridge Arm 3, trigger arm (a), then trigger arm (b) | RED (identity ok) at three steps; each of the first two alone leaves a non-NULL stamp |
| `4b/SC-2b/JOB-01` | the arm's own header says two mechanisms defend the value | RED (identity ok) at two steps — the executable form of that sentence |
| `4c/JOB-01`, `5/JOB-01` | trigger arm (d) clears the key on every exit | RED (identity ok) at two steps each |
| `6a/G1/D-01/SC-17-01`, `6b/G1/D-11`, `6c/G1/JOB-01` | single-step by MEASUREMENT, not assumption | each reddens at one step, and nowhere earlier |

## SEPARATION, both directions

```
filesFloor=43 armsFloor=324  defects=1  kinds=["lane-blocked-stale"]  (442.3 s)
filesFloor=44 armsFloor=325  defects=3  kinds=["lane-blocked-stale","floor","floor"]  (441.3 s)
    floor: FILES_FLOOR regression: 43 annotated file(s) < floor 44
    floor: ARMS_FLOOR regression: 324 biting arm(s) < floor 325
```

The tripwire row is present in BOTH directions, which is what proves it is not what makes
the second run fire.

## Deviations from Plan

### 1. [Rule 3 — blocking] The plan's zero-skip verify predicate is unsatisfiable; the scoped form was used

Covered above. The intent (`the three skips at :282, :326, :483 must not fire`) is met and
measured; the literal `grep -i 'SKIP|skipping'` count can never be 0.

### 2. [Rule 3 — blocking] A new stand-in fixture: `29-fixture-compute-jobs-priority.sql`

The gate's Parts 2/4/5 INSERT `compute_jobs.priority`, which no fixture carried. The real
migration cannot be applied:

```
psql:supabase/migrations/20260515114555_compute_jobs_claim_token_fencing.sql:349: ERROR:  42725: function name "claim_compute_jobs_with_priority" is not unique
```

`20260428120836` creates the first **two**-argument `claim_compute_jobs_with_priority`;
every later claim migration adds a **three**-argument overload with `CREATE OR REPLACE`;
and no migration in the repo ever drops the two-argument form (`grep -an
'claim_compute_jobs_with_priority(INTEGER, TEXT)' supabase/migrations/*.sql` → 0 hits). With
both present the bare `COMMENT ON FUNCTION` at `20260515114555:348` aborts. That migration
is not optional — it is the sole source of `compute_jobs.claim_token` (`:96`). Follows the
`27-fixture-…` precedent; no assertion in this gate reads `priority`.

### 3. [Rule 1 — bug in my own twin] `arm: "4"` reported `WRONG-ARM(4c)`

The GRAMMAR says the `arm` value "must be the section". Measured, that is only true when a
raise is literally named for the section. Here `sectionOfIdentity` folds `4a/4b/4c` into `4`
and **no raise is named `4`**, so the twin is spelled `4c`. Section folding applies to the
denominator the runner counts, never to the identity it reads. Recorded in the twin's prose.

### 4. [Rule 1 — bug in my own twin] `1/re-base` came back `no-red`; the cause is a corpus-wide blind spot

`pg_get_functiondef` returns the function **source, comments included**, and
`20260802120000:353` carries `-- PER-KIND (d.kind = f.kind): …` inside the body. With both
real conjuncts deleted the anchor still matched. Reproduced by hand: `rc=0`, all seven
Parts OK. The twin gained a fourth step that rewrites the comment; the residual weakness is
booked as **D-164.4.1-04-1** in `deferred-items.md` rather than fixed, because the fix edits
assertions (out of this plan's scope) in migrations this phase must not touch.

### 5. [Rule 2 — anti-vacuity] Rule 2 caught a self-referential needle on the first parse

```
occurrence-mismatch   6/seed A/D-18   MEASURE_FAIL: "VALUES (v_strat_a, 'computing', FALSE, now() - interval '3 hours');" occurs 2x …, annotation claims 1x
```

A single-line gate-self-edit needle appears twice the moment the twin is written down. Both
were re-cut as multi-line needles, whose JSON-escaped `\n` does not match the file's own
newline. The gate file was restored from a byte backup (`shasum` verified,
`3bbd87b6cb8fa47b9ed5c4b5e775fcaf31e1a0b8`) before re-generating, never `git checkout --`.

### 6. [Founder-precedent] Section `3` RECLASSIFIED — 29 sections became 28

The plan's `<done>` asks for **≥29** `RED (identity ok)`. Delivered: **28 of 28**, because
one section stopped being a section. Part 3's `TEST FAILED (3)` registration guard is the
third copy of one guard, each dominated by the one before it. Both escape routes were driven
on real lanes:

```
psql:<scratch>/…_reaper.sql:685: ERROR:  22004: query string argument of EXECUTE is null
CONTEXT:  PL/pgSQL function inline_code_block line 103 at EXECUTE
```
```
psql:<scratch>/…_reaper.sql:806: ERROR:  XX000: could not find valid entry for job 'reap_strategy_analytics_stuck_computing'
```

Neither carries a `TEST FAILED (…)`. Following `fcbc0159`, the identity spelling was removed
and the guard kept as a named `INVARIANT (Part 3 precondition)`. **Nothing was waived,
deleted or loosened**; `WAIVED_CEILING` is unedited at 0 and the corpus carries zero waivers.

## Known Stubs

None. No hardcoded empty value, placeholder or unwired data source was introduced.

## Threat Flags

None. No network endpoint, auth path, file-access pattern or trust-boundary schema change
was introduced; the one new file is an additive `ALTER TABLE … ADD COLUMN IF NOT EXISTS` on
a throwaway-lane stand-in that never touches a Supabase database.

## Verification run at HEAD

| gate | result |
|---|---|
| `npx vitest run …parser …floors …drift-check-scripts` | `Test Files 3 passed (3)`, `Tests 525 passed (525)` |
| `npx tsc --noEmit` | `TSC_EXIT=0` |
| `node scripts/mutation-runner/run.mjs --self-test` | `SELF-TEST PASSED` (exit 0) |
| `node scripts/lint-sql-gates.mjs` | `scanned 71 file(s); 0 finding(s), 0 measure-fail(s)` |
| `node scripts/verify-plan-anchors.mjs --pending` | `claims: 58 checked` / `OK: 5 plan file(s), no stale claims` |
| `node scripts/mutation-runner/run.mjs` (full) | exit 1, **one** defect, `lane-blocked-stale` — the expected tripwire |

## What plan 05 inherits

`FILES_FLOOR = 43`, `ARMS_FLOOR = 324`, `WAIVED_CEILING = 0`, `pending:` empty,
`lane-blocked:` = exactly `test_reconcile_dropped_enqueue_sweep.sql` — the last one. Only
when that file is annotated does `lane-blocked-stale` stop firing and the corpus exit 0.

## Self-Check: PASSED

All 7 claimed files exist on disk; all 3 claimed commits resolve
(`95197d28`, `ca4ad558`, `232c608b`), each a child of the expected base `fcbc0159`.
