---
phase: 164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire
plan: 02
type: execute
subsystem: sql-gates
tags: [red-under, pg_cron, floors-ratchet, pending-pin, file-move-1]
wave: 2
depends_on: ["164.4.1-01"]
files_modified:
  - supabase/tests/test_compute_jobs_error_kind_copy_parity.sql
  - supabase/tests/test_derive_allocator_keys_fanout.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-annotation-parser.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
autonomous: true
requirements: [SC-2, SC-3, SC-4]
provides:
  - "The pg_cron-deferred `pending:` file and the smallest lane-blocked file annotated and machine-proven — the phase's first FILE move"
  - "FILES_FLOOR / ARMS_FLOOR ratcheted to the values this plan's run PRINTS, with SEPARATION driven both ways"
  - "The one-name `pending:` SET pin re-measured to its new exact value — as its own task, never loosened"
affects: [164.4.1-03, 164.4.1-04, 164.4.1-05, 164.4.1-06]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every section of test_compute_jobs_error_kind_copy_parity.sql (3) and test_derive_allocator_keys_fanout.sql (7) carries a RED-UNDER twin that the runner drives RED naming that section FIRST, then restores GREEN (SC-2)"
    - "Both files' baselines are GREEN on the lane with ZERO `SKIP` / `skipping` NOTICE lines — the pg_cron-conditional section 6 of the derive gate actually RUNS and is falsified, not withheld (SC-2, PATTERNS C1)"
    - "FILES_FLOOR and ARMS_FLOOR equal the run's own `coverage:` and `biting:` lines, and a floor one higher FIRES (SC-2)"
    - "`pending:` is pinned as the EXACT measured set (expected empty) beside an arm proving the class is still computed — never `toEqual([])` alone (D-04, SC-4)"
    - "`lane-blocked:` names exactly the three files still unannotated, pinned as a set (D-04)"
    - "WAIVED_CEILING is still 0 and no twin carries a waiver (D-03)"
  artifacts:
    - path: "supabase/tests/test_compute_jobs_error_kind_copy_parity.sql"
      provides: "RED-UNDER-SETUP apply list including supabase/migrations/20260513094906_enable_pg_cron.sql ahead of 20260817120000 and 20260826140000; 3 twins"
    - path: "supabase/tests/test_derive_allocator_keys_fanout.sql"
      provides: "RED-UNDER-SETUP apply list including the enabling migration ahead of 20260717233529; 7 twins incl. the formerly pg_cron-skipped section 6"
    - path: "scripts/mutation-runner/run.mjs"
      provides: "new dated derivation blocks above FILES_FLOOR and ARMS_FLOOR (VALUE / DATE / COMMAND / SAMPLE SIZE / COVERAGE / SEPARATION / RECORD); constants raised to the MEASURED values"
    - path: "src/__tests__/mutation-annotation-parser.test.ts"
      provides: "annotatedFiles 41-name set, LANE_BLOCKED set of 3, pending pinned to the measured exact set with a non-vacuity arm, the BLIND tripwire flipped deliberately"
    - path: "src/__tests__/mutation-runner-floors.test.ts"
      provides: "totalAnchored re-pinned, a ninth RE-MEASURED lineage paragraph, GREEN_LOG rows/counts at the new numbers"
  key_links:
    - from: "supabase/tests/test_derive_allocator_keys_fanout.sql section 6"
      to: "supabase/migrations/20260717233529_allocator_equity_derived_surface.sql cron.schedule('derive-allocator-key-dailies', …)"
      via: "the lane now has pg_cron, so the IF EXISTS branch at :159 RUNS and reads cron.job"
      pattern: "derive-allocator-key-dailies"
    - from: "supabase/tests/test_compute_jobs_error_kind_copy_parity.sql"
      to: "supabase/migrations/20260826140000_compute_jobs_error_kind_orphaned.sql"
      via: "the apply list reaches the ONLY migration widening compute_jobs_error_kind_check to admit 'orphaned', which no longer RAISEs 0A000 at :206-209"
      pattern: "compute_jobs_error_kind_check"
---

<objective>
The first FILE move of Phase 164.4.1: annotate the two small files — the pg_cron-deferred `pending:` singleton `test_compute_jobs_error_kind_copy_parity.sql` (3 sections) and `test_derive_allocator_keys_fanout.sql` (7 sections) — prove them on the lane plan 01 built, and ratchet every pin to what the run PRINTS.

CONTEXT decisions are cited as D-01..D-07 (its items 1–7). This plan implements D-01 (retire, not grow: the deferred file is annotated, not re-deferred), D-03 (WAIVED_CEILING stays 0), D-04 (the `pending:` pin changes as its OWN task, measured, never loosened), and flips the D-07 tripwire at `src/__tests__/mutation-annotation-parser.test.ts:2094` on purpose — plan 06 completes D-07's closure.

**Why these two first:** they are the smallest (10 sections between them) and they exercise BOTH ways the lane used to block — the copy-parity file is blocked only through its APPLY LIST (`20260826140000:206`-209 RAISEs `0A000` without pg_cron), the derive file only through its own text (`supabase/tests/test_derive_allocator_keys_fanout.sql:159-169`, a `RAISE NOTICE` skip). Proving both shapes on the new lane before the three large files is the cheapest possible check that plan 01's substrate is complete.

**Floors are ratcheted to MEASURED values.** The runner's own section derivation says 3 + 7 = 10 new sections, so the projection is `coverage: files 41/71` and `biting: 272` — but a section can need a layered twin or turn out to be two identities in one section, so the constants in this plan are written from the run's `coverage:` / `biting:` lines, not from this sentence.

⚠️ **Expected red in this interval (from plan 01):** every `node scripts/mutation-runner/run.mjs` run — full or `--file` — exits 1 with EXACTLY ONE defect, `lane-blocked-stale`, naming the three files plans 03–05 take. That defect is the tripwire, not a regression; a run showing ANY OTHER kind is a regression. Commit messages carry the `TRIPWIRE-RED:` line from plan 01. Pushing the branch is harmless (`ci.yml` runs nothing on a feature-branch push); the rule is **no PR and no green-expecting `workflow_dispatch` before plan 05** — plan 01 Step G's expected-red dispatch is the only CI run in this interval, and it has already been read.

Purpose: SC-2 for two files; the D-04 pin move; the D-07 tripwire flipped deliberately.
Output: two annotated gate files, floors at the measured values, pins re-measured.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-CONTEXT.md
@.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-PATTERNS.md
Also read the plan-01 SUMMARY (164.4.1-01-SUMMARY.md in this phase directory — it exists once plan 01 has run) for the measured per-lane cost and the lane's new preload behaviour.
@scripts/mutation-runner/GRAMMAR.md
@supabase/tests/test_metrics_by_basis_write.sql
@supabase/tests/test_compute_jobs_error_kind_copy_parity.sql
@supabase/tests/test_derive_allocator_keys_fanout.sql
@supabase/migrations/20260513094906_enable_pg_cron.sql
@supabase/migrations/20260826140000_compute_jobs_error_kind_orphaned.sql
@supabase/migrations/20260717233529_allocator_equity_derived_surface.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply-list discovery — both baselines GREEN on the lane with ZERO skip lines</name>
  <files>supabase/tests/test_compute_jobs_error_kind_copy_parity.sql, supabase/tests/test_derive_allocator_keys_fanout.sql</files>
  <read_first>
    - PATTERNS § 1a (the canonical SIMPLE file header + one-line `RED-UNDER-SETUP` at `supabase/tests/test_metrics_by_basis_write.sql:45`) and § 1b's most transferable paragraph (`supabase/tests/test_api_keys_exchange_not_user_writable.sql:136`-142: "the apply list is sized by the SKIP notices … MEASURED … the baseline prints ZERO SKIP lines")
    - `supabase/tests/test_metrics_by_basis_write.sql:45` — its apply list ALREADY carries `supabase/migrations/20260717233529_allocator_equity_derived_surface.sql` behind the compute_jobs chain; it is the seed for the derive gate's list
    - `supabase/tests/test_derive_allocator_keys_fanout.sql:56` — the fan-out-fn-absent SKIP
    - `supabase/tests/test_derive_allocator_keys_fanout.sql:159-169` — the pg_cron branch: section 6 inside the IF EXISTS pg_cron guard, NOTICE skip at 169
    - `supabase/migrations/20260717233529_allocator_equity_derived_surface.sql:276-287` — the schedule is GUARDED by an IF EXISTS pg_cron check and NOTICE-skips otherwise; on the new lane it schedules `derive-allocator-key-dailies`
    - `supabase/tests/test_compute_jobs_error_kind_copy_parity.sql:1-45` — header: run AFTER 20260826120000 and 20260826140000; Parts 1-3 and their identities 1/A-3, 2/A-3, 3/F-3 (raised at lines 61, 124, 173)
    - `supabase/migrations/20260826140000_compute_jobs_error_kind_orphaned.sql:198-220` — the pg_cron RAISE at 206-209, then unschedule/schedule of retention_compute_jobs_orphaned_running; its header (lines 16-35) names mark_compute_job_failed (20260529180000), 20260817120000 and 20260826120000
    - The apply lists of test_set_compute_job_progress.sql and test_resync_retry_single_job.sql (read their RED-UNDER-SETUP lines) for the compute_jobs chain shape; the fixture 27-fixture-strategy-analytics-computation-error.sql exists as a stand-in for the strategy_analytics.computation_error column
  </read_first>
  <action>
    For EACH file, build the `RED-UNDER-SETUP` apply list by iteration on real lanes, exactly as the 164.4 batches did: start from the nearest neighbour's list, add `supabase/migrations/20260513094906_enable_pg_cron.sql` immediately AFTER the core/auth fixtures and BEFORE the first migration that calls `cron.schedule` or probes `pg_extension` (for derive: before `20260717233529`; for copy-parity: before `20260817120000` and `20260826140000`), then run `bash scripts/pg-lane/run.sh --workdir <scratch OUTSIDE the repo> --apply <list…> --gate <the gate>` and read the output. Iterate until (a) exit 0 and (b) `grep -a -c -i -E 'SKIP|skipping' <out>` is 0 — a section behind a still-firing skip is un-falsifiable (PATTERNS C1). For the derive gate the two skips that must NOT fire are `:56` (fan-out fn absent) and `:169` (pg_cron absent). For copy-parity there is no skip; its blocker was the `0A000` RAISE, which must now not fire — if it does, the enabling migration is missing or ordered after `20260817120000`.

    ⚠️ Apply order is the order given (README: "the order given is the order applied"); fixture `04-fixture-compute-jobs-targets.sql` follows `20260411144407`. A migration in the list that self-verifies (both `20260817120000` and `20260826140000` read back their `cron.job` row by NAME) is part of the apply cost — read its RAISE text if the apply aborts.

    Write the header block for each file per PATTERNS § 1a/§ 1b: the boilerplate MACHINE-EXECUTABLE TWINS paragraph, then one ⚠️ note per non-obvious apply-list entry with its MEASURED reason (why `20260513094906` is there; for derive, that the apply list is sized so `:56` and `:169` print nothing; for copy-parity, that `20260826140000:206`-209 is the former blocker and no longer fires), then the single `RED-UNDER-SETUP` line. Record the oracle-scope caveat honestly (RESEARCH § Q3): on the lane, `cron.job.command` holds whatever the apply list just scheduled, so the derive gate's section 6 proves falsifiability, not deployment drift. Do NOT convert any `RAISE NOTICE` skip to a `RAISE EXCEPTION` — a behaviour change to a gate is out of scope (CONTEXT domain boundary, PATTERNS C1).
  </action>
  <verify>
    <automated>cd /Users/<user>/claude-projects/quantalyze && S=/private/tmp/claude-501/-Users-<user>-claude-projects-quantalyze/0339e09c-f6d0-4f3c-abb2-8254bf7a3a12/scratchpad/p02-baseline && rm -rf "$S" && mkdir -p "$S" && for f in test_compute_jobs_error_kind_copy_parity test_derive_allocator_keys_fanout; do list=$(grep -a -o 'RED-UNDER-SETUP: .*' "supabase/tests/$f.sql" | sed 's/^RED-UNDER-SETUP: //' | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8"));console.log(j.apply.join(" "))'); bash scripts/pg-lane/run.sh --workdir "$S/$f" --apply $list --gate "supabase/tests/$f.sql" > "$S/$f.out" 2>&1 || { echo "BASELINE RED: $f"; tail -20 "$S/$f.out"; exit 1; }; test "$(grep -a -c -i -E 'SKIP|skipping' "$S/$f.out")" = "0" || { echo "SKIP LINES PRESENT: $f"; grep -a -i -E 'SKIP|skipping' "$S/$f.out"; exit 1; }; echo "ok $f"; done && grep -a -c '20260513094906_enable_pg_cron.sql' supabase/tests/test_compute_jobs_error_kind_copy_parity.sql supabase/tests/test_derive_allocator_keys_fanout.sql | grep -v ':0$'</automated>
    <fails_when>the loop prints `BASELINE RED: <file>` and exits 1 (the lane run through the file's own apply list exited non-zero — the `0A000` RAISE at `20260826140000:206` or a missing object aborted the apply, or the gate itself raised `TEST FAILED`); it prints `SKIP LINES PRESENT: <file>` and exits 1 (the `SKIP|skipping` count is non-zero — `:56` or `:169` still fires, so a section is withheld); the `RED-UNDER-SETUP` line is absent or not valid JSON (the `node -e` parse throws and `$list` is empty, so `run.sh` fails on a missing `--apply`); or the final per-file count of the enabling-migration path prints `:0` for either file (the `grep -v ':0$'` filter drops that line and the chain ends with a non-zero status)</fails_when>
  </verify>
  <done>Both gates baseline GREEN on the lane through their own `RED-UNDER-SETUP` lists with zero skip/skipping lines; both lists carry the enabling migration ahead of the first cron-touching migration; headers state the measured reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Twin every section — 3 + 7 — and drive each RED naming its own section first</name>
  <files>supabase/tests/test_compute_jobs_error_kind_copy_parity.sql, supabase/tests/test_derive_allocator_keys_fanout.sql</files>
  <read_first>
    - `scripts/mutation-runner/GRAMMAR.md` — Shapes 1-3, the `RED-UNDER-SETUP` schema, rule 3a (no `TEST FAILED (` injection), rule 4 (never target a stand-in fixture — mutate the real migration)
    - PATTERNS § 1a prose+twin pair (`supabase/tests/test_metrics_by_basis_write.sql:97-109`) and the arm-unit rule (`"arm"` = the section, sub-letter stripped)
    - PATTERNS § 1c (LAYERED, when a migration self-verifies its own effect: `supabase/tests/test_resync_retry_single_job.sql:215-247`) — expect it for copy-parity section 3/F-3, whose target 20260826140000 self-verifies the CHECK at lines 491-503
    - PATTERNS § 2 (the exception idiom that turns a raw SQLSTATE into an arm name) — needed only if a new twin makes a gate hit a driver-level error inside its own DO block
    - `scripts/mutation-runner/parse.mjs:1043`-1049 — irrelevant to annotation itself, but note: once this file carries annotations it leaves the classifier's input entirely
  </read_first>
  <action>
    Annotate every section with a prose `-- RED-UNDER:` explaining the production change that must redden it and WHY nothing upstream would notice, immediately followed by its `-- RED-UNDER-M:` twin, placed directly above the assertion it falsifies. Sections (from the runner's own derivation, `gateSectionCount`): copy-parity `1/A-3`, `2/A-3`, `3/F-3`; derive `1`..`7`. Choose falsifiers that make the FIRST `TEST FAILED (…)` name that section: for derive section 6, edit `supabase/migrations/20260717233529_allocator_equity_derived_surface.sql` so `cron.schedule` registers a different jobname (the gate then finds no `derive-allocator-key-dailies` row → `TEST FAILED (6)`); for copy-parity, mutate the widened CHECK / the `computation_error_copy` CASE in the REAL migrations (`20260826140000`, `20260826120000`), layering steps where the migration's self-verify would otherwise abort the apply (PATTERNS § 1c — MEASURE that step 1 alone aborts before adding step 2, and say so in the prose). `occurrences` is REQUIRED on every edit and must be exact.

    Per-file proof loop: `node scripts/mutation-runner/run.mjs --file supabase/tests/<file>.sql > <scratch>/<file>.log 2>&1` — expect exit 1 because the stale tripwire fires (plan 01), and read the log: every arm of the file prints `RED (identity ok)`; the defect table has EXACTLY ONE row and its kind is `lane-blocked-stale`. A `no-red`, `wrong-first-failure`, `occurrence-mismatch` or `baseline` row is yours to fix. Never edit a file while a run is in flight (the dirty-checkout assertion diffs `git status --porcelain` around the whole run).
  </action>
  <verify>
    <automated>cd /Users/<user>/claude-projects/quantalyze && S=/private/tmp/claude-501/-Users-<user>-claude-projects-quantalyze/0339e09c-f6d0-4f3c-abb2-8254bf7a3a12/scratchpad/p02-arms && rm -rf "$S" && mkdir -p "$S" && for f in test_compute_jobs_error_kind_copy_parity:3 test_derive_allocator_keys_fanout:7; do n=${f%%:*}; want=${f##*:}; node scripts/mutation-runner/run.mjs --file "supabase/tests/$n.sql" > "$S/$n.log" 2>&1; red=$(grep -a -c 'RED (identity ok)' "$S/$n.log"); rows=$(awk '/^  KIND +ARM/{f=1;next} f && /^  [a-z]/{n++} END{print n+0}' "$S/$n.log"); stale=$(grep -a -c -E '^  lane-blocked-stale ' "$S/$n.log"); echo "$n red=$red rows=$rows stale=$stale"; test "$red" -ge "$want" && test "$rows" = "1" && test "$stale" = "1" || { tail -30 "$S/$n.log"; exit 1; }; done && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -E '^coverage: files 41/71|^  pending: 0 idiom|^lane-blocked: 3 file' | wc -l | grep -q '^3$' && npx vitest run src/__tests__/mutation-annotation-parser.test.ts -t "every SECTION an annotated file raises for also carries a twin" 2>&1 | grep -a -E 'passed'</automated>
    <fails_when>for either file the `red` count is below its section count (3 / 7 — an arm came back `no-red`, `wrong-first-failure` or `occurrence-mismatch`), the defect-table row count is not exactly `1`, or the `lane-blocked-stale` row count is not exactly `1` (a second defect kind appeared, or the single row is not the tripwire) — the `test … || { tail -30; exit 1; }` branch prints the log tail and exits 1; the `--parse-only` line count is not `3` (coverage is not `41/71`, `pending` is not `0`, or `lane-blocked` is not `3 file(s)` — the annotation did not move the classification); or the vitest output has no `passed` (the set-inclusion control failed: a raised section has no twin)</fails_when>
  </verify>
  <done>Each file's arms all print `RED (identity ok)` with the single expected `lane-blocked-stale` row as the only defect; `--parse-only` shows `coverage: files 41/71`, `pending: 0`, `lane-blocked: 3`; the durable half-annotation control (set inclusion of sections) passes for both files.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pin the MEASURED sets and ratchet the floors — the `pending:` pin as its own deliberate move (D-04)</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/mutation-annotation-parser.test.ts, src/__tests__/mutation-runner-floors.test.ts</files>
  <read_first>
    - PATTERNS § 3 (the FULL ratchet surface) and § 6 (every pin that moves)
    - `scripts/mutation-runner/run.mjs:518` (`FILES_FLOOR = 39`) with its block above; `scripts/mutation-runner/run.mjs:1006` (`ARMS_FLOOR = 262`) with the plan-11 block at :940-1005 — the SEPARATION paragraph shape at :990-996; `scripts/mutation-runner/run.mjs:1047` (`WAIVED_CEILING = 0`, LOCKED)
    - `src/__tests__/mutation-annotation-parser.test.ts:1900-1942` — filesTotal 71, filesAnnotated 39, the exact sorted 39-name annotatedFiles array
    - `src/__tests__/mutation-annotation-parser.test.ts:2063-2068` — the LANE_BLOCKED_4 constant
    - `src/__tests__/mutation-annotation-parser.test.ts:2070-2104` — the set test: toEqual(LANE_BLOCKED_4), the BLIND tripwire toContain at 2094, the one-name pending pin at 2103
    - `src/__tests__/mutation-annotation-parser.test.ts:2106-2124` — the sum test; toHaveLength(4) at 2124
    - `src/__tests__/mutation-annotation-parser.test.ts:2127-2200` — the independent-derivation PARTITION test (derived; must pass unchanged)
    - `src/__tests__/mutation-annotation-parser.test.ts:1440-1450` — the hand-built classifyGateIdiom block whose header describes the inertFiles toEqual([]) vacuity (the assertion itself sits near 1504) — the EXACT prior vacuity D-04 forbids repeating
    - `src/__tests__/mutation-runner-floors.test.ts:265-311` — the eight dated lineage paragraphs (APPEND a ninth)
    - `src/__tests__/mutation-runner-floors.test.ts:318` (`expect(totalAnchored).toBe(262)`)
    - `src/__tests__/mutation-runner-floors.test.ts:1268-1342` — GREEN_LOG: coverage 39/71, arms 262/262/0, biting 262, lane-invocations 262 plus 39 baseline / 39 restore, the 39 per-file rows and the 39-term sum comment at 1295-1302
    - `src/__tests__/mutation-runner-floors.test.ts:1352` (`262 arm lane(s) spawned`) and the derived literals at 1505 (printed 40 per-file row(s) but reported 39) and 1515 (reports 262)
  </read_first>
  <action>
    **Measure first.** Run the FULL corpus: `node scripts/mutation-runner/run.mjs > <scratch>/full.log 2>&1` (exit 1 expected — the single `lane-blocked-stale` row). Read `coverage: files N/71`, `arms: E/A/W`, `biting: B`, `lane-invocations:`, `per-arm lane time:` off the log. N and B are THE values; this plan does not know them until here (projection 41 / 272 — do not write the projection into any constant).

    **Ratchet (PATTERNS § 3, sites 1-2, 4-5):** set `FILES_FLOOR = N` and `ARMS_FLOOR = B` in `scripts/mutation-runner/run.mjs`; above EACH constant add a new dated derivation block in the exact house shape (VALUE / DATE with HEAD sha / COMMAND with the printed lines / SAMPLE SIZE / COVERAGE naming the two files added / SEPARATION / RECORD = this plan's SUMMARY path). Prior blocks STAY as lineage. SEPARATION is driven through the real verdict loop BOTH ways: `node -e 'import("./scripts/mutation-runner/run.mjs").then(m=>{const r=m.runCorpus({scopeDir:"supabase/tests",filesFloor:N,armsFloor:B,log:()=>{}});console.log(r.exitCode, JSON.stringify(r.defects.map(d=>d.kind)))})'` must report ONLY `lane-blocked-stale`; the same with `filesFloor:N+1, armsFloor:B+1` must ADD two `floor` defects naming `N annotated file(s) < floor N+1` and `B biting arm(s) < floor B+1`. Quote both results in the block. `WAIVED_CEILING` stays 0 and its `arms:` W field must read 0 (D-03); add one dated CURRENCY line under its block, edit nothing else there.

    **Parser pins (`src/__tests__/mutation-annotation-parser.test.ts`) — D-04, deliberately:**
    - `:1901` → `filesAnnotated` N; the sorted array at `:1902`-1942 gains the two names in sorted position.
    - `:2063`-2068: rename to `LANE_BLOCKED_3` with the three remaining names (reconcile, retention, reaper) and a dated comment that the derive file LEFT the set because it is annotated, not because the classifier changed; keep the old four-name list in a comment as lineage.
    - `:2094`: the BLIND tripwire INVERTS — assert `annotatedFiles` CONTAINS `test_compute_jobs_error_kind_copy_parity.sql` and `pendingFiles` does NOT, with a dated comment: "flipped by 164.4.1-02 because the LANE now hosts pg_cron (the substrate half of the tripwire's two exits); the classifier half is closed deliberately in 164.4.1-06 — TODOS [REDUNDER-LANEBLOCKED-BLIND]".
    - `:2103`: replace the one-name pin with `expect(corpus.pendingFiles).toEqual([])` **AND, in the same `it`, a non-vacuity AIM directly above it**: build a temp scope dir (mkdtemp) holding a copy of ONE currently-annotated gate with every line-start `RED-UNDER` line stripped (so it is idiom-shaped and unannotated), run `scanCorpus` on it, and assert that copy IS in `pendingFiles` — proving the `pending` class is still computed before asserting the live corpus has none in it. The `it` title MUST contain the literal marker `pending AIM (D-04)` — the verify below greps for that marker so an executor cannot satisfy this task with the bare `toEqual([])` and no AIM (the exact vacuity the checker flagged; plan 05 guards its lane-blocked AIM the same way by grepping `lane-blocked-gate.sql`). Comment: this is the measured-empty-set shape D-04 requires and the `inertFiles).toEqual([])` vacuity at `:1445` is exactly what the AIM refuses to repeat.
    - `:2118`-2124: append a dated arithmetic line (`N + 0 + 27 + 0 + 3 = 71`, from `--parse-only`), keep the lineage lines, change `toHaveLength(4)` to `toHaveLength(3)` with an EXACT-set assertion beside it (`toEqual(LANE_BLOCKED_3)`).
    - The PARTITION test at `:2127`-2200 needs no edit and must pass — it is the arm proving the classes did not silently stop being computed.

    **Floors pins (`src/__tests__/mutation-runner-floors.test.ts`):** `:318` → the new anchored total (read it off the failing assertion's message, then confirm it equals the sum of the two new files' prose counts plus 262); append a ninth `RE-MEASURED 2026-09-0X (plan 164.4.1-02 …)` paragraph after `:311` — never edit an old one; `GREEN_LOG` (`:1268`-1342): `coverage: files N/71`, `arms: B/B/0`, `biting: B`, `lane-invocations: B … plus N baseline / N restore leg(s)`, TWO new per-file rows in sorted position with `sections`/`judged`/`annotated`/`biting` from the run's own per-file rows, the sum comment extended; the literals at `:1352`, `:1505`, `:1515` follow (`B arm lane(s) spawned`, `printed N+1 per-file row(s) but reported N`, `reports B`). Leave the synthetic `lane-blocked: 2 … d.sql e.sql` / `lane-probe: pg_cron absent` fixture lines (`:1283`-1284) alone: they are synthetic by design and coherent as a pair (absent + non-empty = current).

    Commit with the `TRIPWIRE-RED:` line naming the THREE remaining files.
  </action>
  <verify>
    <automated>cd /Users/<user>/claude-projects/quantalyze && npx vitest run src/__tests__/mutation-annotation-parser.test.ts src/__tests__/mutation-runner-floors.test.ts 2>&1 | grep -a -E 'Tests .* passed|Test Files .* passed' && node -e 'import("./scripts/mutation-runner/run.mjs").then(m=>{const out=m.FILES_FLOOR+" "+m.ARMS_FLOOR+" "+m.WAIVED_CEILING;console.log(out);if(m.FILES_FLOOR<41||m.ARMS_FLOOR<=262||m.WAIVED_CEILING!==0)process.exit(1)})' && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -E "^coverage: files $(node -e 'import("./scripts/mutation-runner/run.mjs").then(m=>console.log(m.FILES_FLOOR))')/71" && test "$(grep -a -c 'toEqual(\["test_compute_jobs_error_kind_copy_parity.sql"\])' src/__tests__/mutation-annotation-parser.test.ts)" = "0" && grep -a -c 'LANE_BLOCKED_3' src/__tests__/mutation-annotation-parser.test.ts | grep -v '^0$' && grep -a -c 'pending AIM (D-04)' src/__tests__/mutation-annotation-parser.test.ts | grep -q '^1$' && awk '/pending AIM \(D-04\)/,/^  \}\);/' src/__tests__/mutation-annotation-parser.test.ts | grep -a -c 'pendingFiles).toContain(' | grep -v '^0$'</automated>
    <fails_when>either vitest file reports `failed` (a pin disagrees with the run — floors, GREEN_LOG, the 41-name array, the PARTITION test); the `node -e` module read exits 1 (`FILES_FLOOR` < 41, `ARMS_FLOOR` ≤ 262 or `WAIVED_CEILING` ≠ 0 — a floor was not ratcheted or the ceiling moved); the `--parse-only` `coverage:` numerator differs from the exported `FILES_FLOOR` (grep finds no matching line — the constant was written from the projection, not the print); the one-name `toEqual(["test_compute_jobs_error_kind_copy_parity.sql"])` pin still exists (count ≠ 0); `LANE_BLOCKED_3` count is `0`; the `pending AIM (D-04)` marker count is not exactly `1` (the AIM `it` is missing or duplicated); or the block from that marker to its closing `});` contains no `pendingFiles).toContain(` (the `it` carries the empty-set assertion without the stripped-copy AIM — the D-04 vacuity)</fails_when>
  </verify>
  <done>Both vitest files pass; FILES_FLOOR ≥ 41 and ARMS_FLOOR > 262 read from the module and equal the run's printed lines; WAIVED_CEILING is 0; `--parse-only` coverage numerator equals FILES_FLOOR; the one-name pending pin is gone, replaced by the measured empty set with its AIM; `LANE_BLOCKED_3` is the pinned set; SEPARATION quoted both ways in the run.mjs blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate file → real migration | twins edit COPIES of real migrations on a throwaway lane |
| runner floors → CI gate | the constants this plan raises are what `sql-mutation` enforces on every push |
| parser pins → attestation of completeness | the `pending:` set pin is what stops an empty line shipping as a claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.1-06 | Repudiation | `pending:` pinned as `toEqual([])` without proof the class is computed | high | mitigate | Task 3's AIM: a stripped copy in a temp scope dir MUST classify `pending` in the same `it` (D-04) |
| T-164.4.1-07 | Tampering | a twin that injects `TEST FAILED (` or targets a stand-in fixture | medium | mitigate | parser refusals (`parse.mjs` rules 3a/4) fire at parse time; `--parse-only` in verify |
| T-164.4.1-08 | Repudiation | floors pinned from the projection (41/272) instead of the run | high | mitigate | Task 3 reads N and B off the log; SEPARATION both ways quoted; the floors vitest fails on drift either way |
| T-164.4.1-09 | Information Disclosure | a `sql` step that reads lane state into a comment | low | accept | lanes hold fixture data only; no secret exists on a lane |
| T-164.4.1-SC | Tampering | npm/pip/cargo installs | low | accept | none introduced — `run.mjs`/`parse.mjs` import node builtins only |
</threat_model>

<verification>
- `node scripts/mutation-runner/run.mjs --parse-only` exit 0: `coverage: files N/71`, `lane-blocked: 3 file(s) … test_reconcile_dropped_enqueue_sweep.sql test_retention_orphaned_running.sql test_strategy_analytics_stuck_computing_reaper.sql`, `  pending: 0 idiom file(s) without RED-UNDER — `.
- Full `node scripts/mutation-runner/run.mjs` exit 1 with EXACTLY ONE defect row, kind `lane-blocked-stale`, naming those three; `arms: B/B/0`; tallies agree; `biting: B`.
- `npx vitest run src/__tests__/mutation-annotation-parser.test.ts src/__tests__/mutation-runner-floors.test.ts` green.
- `git diff --name-only HEAD~3..HEAD | grep -a '^supabase/migrations/'` prints nothing.
- No `"waiver"` key in either new gate file (`grep -a -c '"waiver"' <file>` = 0) — D-03.
</verification>

<success_criteria>
- SC-2 for two files: 10 sections twinned, each RED naming itself first, GREEN restored; floors equal the printed values with separation proven.
- SC-4 progress: the BLIND tripwire flipped on purpose with the reason written beside it.
- D-04 honoured: the `pending:` set is re-measured, asserted exactly, and the class is proven still computed.
- D-03 honoured: 0 waivers.
</success_criteria>

<output>
Create `.planning/phases/164.4.1-pgcron-lane-put-pg-cron-on-the-throwaway-pg-lane-and-retire/164.4.1-02-SUMMARY.md` when done. It MUST quote the run's own `coverage:`, `arms:`, `biting:`, `lane-invocations:` and `per-arm lane time:` lines, both SEPARATION results, the two apply lists, and any LAYERED twin with its measured step-1-alone outcome.
</output>
