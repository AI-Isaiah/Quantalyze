---
phase: 164.4.1-pgcron-lane
verified: 2026-09-05T14:52:21Z
verified_at_head: 581d54cb
status: human_needed
score: 4/5 must-haves verified
behavior_unverified: 1
overrides_applied: 0
behavior_unverified_items:
  - truth: "SC-1: the lane hosts pg_cron on BOTH macOS and ubuntu CI, and the two measure the SAME corpus"
    test: "Dispatch (or open the PR so `pull_request` runs) CI at head sha 581d54cb and read the `sql-mutation` job log: the `Provision pg_cron` step's `PGBIN the lane will boot:` and `PostgreSQL major reported by` lines, then the runner's `coverage:`, `arms:`, `biting:`, `lane-invocations:`, `lane-blocked:`, `lane-probe:` lines."
    expected: "Step resolves `/usr/lib/postgresql/16/bin`, major 16, installs `postgresql-16-cron`; runner prints `coverage: files 44/71`, `arms: 361/361/0`, `biting: 361`, `lane-invocations: 361`, `lane-blocked: 0 file(s)`, `lane-probe: pg_cron AVAILABLE`, exit 0 — identical to the macOS reading at the same sha recorded below."
    why_human: "The only ubuntu evidence is workflow_dispatch run 33961609382 at 1aa8bb70, which is 24 commits BEHIND HEAD. Since that run the tree changed in `scripts/pg-lane/run.sh`, `scripts/mutation-runner/run.mjs`, `.github/workflows/ci.yml` (the pg_cron provisioning step's major selector was REWRITTEN from an `ls /usr/lib/postgresql/*/bin` glob to `run.sh --print-pgbin` — the step at HEAD has never executed on ubuntu), one gate file and one lane fixture, and the corpus went 363 -> 361 arms. `gh run list --branch phase-164.4.1-pgcron-lane` shows NO run at or after HEAD. A verifier on a macOS box cannot produce this measurement; it must be a SHA-bound CI run."
human_verification:
  - test: "SHA-bound ubuntu run of HEAD (581d54cb) — see behavior_unverified_items[0]."
    expected: "`sql-mutation` success with `arms: 361/361/0`, `lane-blocked: 0`, `lane-probe: pg_cron AVAILABLE`; and the `frontend` aggregator GREEN at that sha (run 33961609382's aggregator was RED on frontend-lint, frontend-test shard 2 and secret-scan — all three are fixed or pre-existing at HEAD per the local checks below, but no green board exists for HEAD)."
    why_human: "Needs a hosted ubuntu runner; not reproducible on the authoring box."
---

# Phase 164.4.1: PGCRON-LANE Verification Report

**Phase Goal:** The pg-lane can host pg_cron, so the `[REDUNDER-PGCRON]` deferral is RETIRED rather than grown — five gate files and ~100 sections become provable, and the runner's `lane-blocked:` class goes to 0.
**Verified:** 2026-09-05T14:52:21Z at HEAD `581d54cb` (worktree reset to that sha; `npx vitest --version` = 4.1.10)
**Status:** human_needed
**Re-verification:** No — initial verification
**Verdict:** PARTIAL — PASS on SC-2, SC-3, SC-4, SC-5 and every independent check; SC-1's ubuntu half is proven only on a superseded tree.

Adversarial stance held throughout: nothing below is taken from a SUMMARY. Every row says MEASURED (command + quoted output) or READ (inspected code/diff).

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP success criterion) | Status | Evidence |
| --- | --- | --- | --- |
| 1 | SC-1: pg_cron loads on BOTH macOS and ubuntu CI, and the two measure the SAME corpus | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED (ubuntu half proven on a SUPERSEDED tree) | **macOS, MEASURED at HEAD:** `node scripts/mutation-runner/run.mjs` -> `coverage: files 44/71` / `lane-blocked: 0 file(s)` / `lane-probe: pg_cron AVAILABLE — lane-blocked class is empty, as a hosting lane requires` / `pending: 0` / `arms: 361/361/0` / `biting: 361` / `lane-invocations: 361 … plus 44 baseline / 44 restore leg(s)` / `per-arm lane time: mean 1.1s over 361 arm run(s)` / `✅ No defects` / `exit=0`; wall clock 14:31:35Z -> 14:39:43Z = 488 s for 449 legs. `bash scripts/pg-lane/run.sh --self-test` -> `SELF-TEST PASSED (6/6)` incl. `6/6: the preload took effect — CREATE EXTENSION pg_cron succeeds, cron.schedule writes a cron.job row`. Server `postgres (PostgreSQL) 16.13 (Homebrew)`, `pg_cron.dylib` present in `pg_config --pkglibdir`. **ubuntu, MEASURED from the log of run 33961609382 (head 1aa8bb70, `gh api …/jobs/101294336893/logs`):** `PostgreSQL major under /usr/lib/postgresql: 16`, `Candidate: 1.6.2-1` (noble/universe), `Version: 1.6.2-1`, `/usr/lib/postgresql/16/lib/pg_cron.so`, `SELF-TEST 17/17 … ok`, `coverage: files 44/71`, `arms: 363/363/0`, `biting: 363`, `lane-invocations: 363`, `lane-blocked: 0 file(s)`, `lane-probe: pg_cron AVAILABLE`, `per-arm lane time: mean 1.1s over 363 arm run(s)`, `✅ No defects`; job 10:47:54Z -> 10:57:21Z = 567 s. **Why not VERIFIED:** `git merge-base --is-ancestor 1aa8bb70 HEAD` = YES but `git log 1aa8bb70..HEAD` = 24 commits touching `scripts/pg-lane/run.sh`, `scripts/mutation-runner/run.mjs`, `.github/workflows/ci.yml`, `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql`, `scripts/pg-lane/fixtures/30-…sql`; the run's provisioning step used the OLD selector (`major=$(ls -d /usr/lib/postgresql/*/bin …)`), whereas HEAD's uses `run.sh --print-pgbin` and has never run on ubuntu. `gh run list --branch phase-164.4.1-pgcron-lane` = exactly two runs (33961609382 @1aa8bb70, 33938272686 @f04ce51b); none at HEAD. The substrate claim (apt package loads, arms bite on ubuntu, same per-lane cost) IS met; the "same corpus" claim for the 361-arm tree is measured on one OS only. |
| 2 | SC-2: the five formerly-deferred gate files are annotated and every arm is machine-proven to bite; floors ratchet to MEASURED values | ✓ VERIFIED | **MEASURED, driving real arms:** `--file supabase/tests/test_compute_jobs_error_kind_copy_parity.sql` -> `arm 1/A-3 … RED (identity ok)`, `arm 2/A-3 … RED (identity ok)`, `arm 3/F-3 … RED (identity ok)`, `arms: 3/3/0`, `biting: 3`, `No defects in the narrowed scope` (exit 2 = narrowed mode's non-zero by design). `--file supabase/tests/test_derive_allocator_keys_fanout.sql` -> arms 1..7 all `RED (identity ok)`, `arms: 7/7/0`, `biting: 7`. Full run per-file lines: `test_compute_jobs_error_kind_copy_parity.sql: sections 3 / judged 3 / … biting 3`, `test_derive_allocator_keys_fanout.sql: … 7 / 7 / … 7`, `test_reconcile_dropped_enqueue_sweep.sql: … 37 / 37 / … 37`, `test_retention_orphaned_running.sql: … 24 / 24 / … 24`, `test_strategy_analytics_stuck_computing_reaper.sql: … 28 / 28 / … 28` = 99 sections; 262 + 99 = 361. Zero non-RED arm lines in the full log (`grep '^  arm ' | grep -v 'RED (identity ok)'` empty). Each file's RED-UNDER-SETUP names `supabase/migrations/20260513094906_enable_pg_cron.sql` (grep count >= 1 in all five). |
| 3 | SC-3: `lane-blocked:` is 0 AND the `lane-blocked-stale` tripwire is EXERCISED and still fires | ✓ VERIFIED | **Zero, MEASURED** (full run + `--parse-only`): `lane-blocked: 0 file(s)`, `pending: 0`. **Fires, MEASURED:** local `--self-test` -> `SELF-TEST 17/17 … ok — AIM (fixture bytes): the scenario corpus classifies exactly lane-blocked-gate.sql as lane-blocked`, `ok — AVAILABLE: exit code is 1`, `ok — the defect table carries a "lane-blocked-stale" defect NAMING the still-classified file`, `ok — exactly one defect on the AVAILABLE arm`, `ok — absent: the SAME corpus with an absent-marker probe exits 0 with no lane-blocked-stale defect` — both directions; same 17/17 in the ubuntu log. **Fired on the REAL corpus, READ** from `164.4.1-TRIPWIRE-FIRED.log`: at pristine `3b6c338a` and at `b59d2b1d` (`arms: 262/262/0`, `lane-probe: pg_cron AVAILABLE — lane-blocked class is STALE`, exactly one `lane-blocked-stale` defect naming the four files), and SHA-bound on ubuntu in run 33938272686 @ `f04ce51b` (`conclusion: failure`, sole defect kind `lane-blocked-stale`). **Cleared BY ANNOTATION, not by weakening — MEASURED/READ:** `git diff 3b6c338a..HEAD -- scripts/mutation-runner/parse.mjs` filtered to non-comment additions = EMPTY (62 added lines, all inside `/** */`); `gateNeedsPgCron` body byte-identical; `git diff --stat 3b6c338a..HEAD -- scripts/mutation-runner/fixtures` = nothing (probe fixture `lane-probe/pg-cron-probe.sql` untouched, still asks `pg_available_extensions`); in `run.mjs` the defect condition `if (laneProbe.available === true && corpus.laneBlockedFiles.length > 0) addDefect("lane-blocked-stale", …)` is intact and the probe leg runs on every real run (`if (targets.length > 0 || corpus.laneBlockedFiles.length > 0)`). Non-comment lines REMOVED from run.mjs by the phase are only the two floor constants and three print strings. |
| 4 | SC-4: `[REDUNDER-LANEBLOCKED-BLIND]` closed deliberately — text-only classifier documented and calibrated | ✓ VERIFIED | **READ:** `parse.mjs` above `gateNeedsPgCron` carries the dated D-07 paragraph ("reads THE GATE'S OWN TEXT ONLY … widening RETIRED, with reasons 1-3"); `TODOS.md:1399` entry is `[x]` with "CLOSED 2026-09-05 DELIBERATELY … classifier is STILL text-only; what changed is that the limit is now documented, calibrated and reasoned". **Calibrated, MEASURED:** `src/__tests__/mutation-annotation-parser.test.ts` has the `D-07 boundary` arm; neutered `gateNeedsPgCron` with a leading `if (text.toLowerCase().includes(PG_CRON_EXTENSION)) return true;` -> `npx vitest run src/__tests__/mutation-annotation-parser.test.ts` = `Tests 4 failed \| 104 passed`, the failures naming `D-07 boundary`, the COMMENT-only probe, WR-01 and `scanCorpus names ZERO lane-blocked files`; restored via `git checkout --` (no uncommitted work existed), `shasum -c` = `OK`, `git status --porcelain` empty. |
| 5 | SC-5: the lane stays a throwaway cluster under a scratch `--workdir`; NEVER a Supabase database | ✓ VERIFIED | **READ:** `run.sh` `initdb -D "$PGD/data" -U postgres --auth=trust` on a loopback port; `grep -n -E "DB_URL\|PGHOST\|PGPORT\|supabase\.co\|pooler" scripts/pg-lane/run.sh` = no hits; the only `supabase` references are repo-relative migration/test file paths. **MEASURED:** lane self-test arms 3/6 (kill mid-run leaves NO orphan), 4/6 (failure-path cleanup), 5/6 (success-path cleanup) all passed. |

**Score:** 4/5 truths verified (1 present, behavior-unverified: SC-1's ubuntu half at HEAD)

### Independent checks requested by the orchestrator

| Check | Status | Evidence |
| --- | --- | --- |
| `FILES_FLOOR = 44`, `ARMS_FLOOR = 361`, `WAIVED_CEILING = 0` | ✓ MEASURED | `grep -n -E "^export const (FILES_FLOOR\|ARMS_FLOOR\|WAIVED_CEILING)" scripts/mutation-runner/run.mjs` -> `789:export const FILES_FLOOR = 44;` `1578:export const ARMS_FLOOR = 361;` `1692:export const WAIVED_CEILING = 0;` |
| A floor one higher FIRES (real corpus, not fixture) | ✓ MEASURED | Throwaway local commit set 45/362; full run -> `❌ 2 defect(s):` `floor … FILES_FLOOR regression: 44 annotated file(s) < floor 45` and `floor … ARMS_FLOOR regression: 361 biting arm(s) < floor 362`, `exit=1`; then `git reset --hard 581d54cb`, constants back to 44/361/0, tree clean. (Self-test arms 2/17 and 5/17 additionally prove both floor modes and the ceiling on the fixture corpus.) |
| ZERO waivers corpus-wide | ✓ MEASURED | `cat supabase/tests/*.sql \| grep -c '"waiver"'` = `0`; `--parse-only` shows `0 waiver(s)` on every file and `arms: 0/361/0`. Four case-insensitive `waiver` hits are all `--` prose explaining why WAIVED_CEILING is 0. |
| Full corpus exits 0 with `arms: 361/361/0`, `biting: 361`, `pending: 0`, `lane-blocked: 0` | ✓ MEASURED | Quoted in truth 1 (macOS at HEAD). Tallies agree (`lane-invocations: 361`). |
| No `supabase/migrations/**` file modified | ✓ MEASURED | `git diff --name-only 3b6c338a..HEAD -- supabase/migrations \| wc -l` = `0` (56 commits, 40 files changed, none under migrations). |
| No tracked file contains a local absolute home path or the author's username | ✓ MEASURED | `git grep` over every tracked file for a macOS home-directory prefix and for the author's login = no hits. `src/__tests__/check-planning-hygiene.test.ts` passes at HEAD (it was RED with 32 violations in run 33961609382; fixed by `ea766c0e`). Pre-existing, NOT this phase: the founder's e-mail appears in four archived `.planning/milestones/v0.15…`/`v1.0…` docs untouched by this phase — info only. |
| Known open items booked, not dropped | ✓ MEASURED | `TODOS.md:1489` `- [ ] **[REDUNDER-GATESELF-UNBOUNDED] …** (19 of 363 measured, "BOOKED, NOT FIXED — deliberately", founder decision requested). `deferred-items.md:82` `## D-164.4.1-06-1 — plan 06 Task 2's CLEARED assertion passes at its own base commit`. Also present: `TODOS.md:2797` secret-scan full-history red on `workflow_dispatch` (pre-existing, explains that run's `secret-scan` failure — every flagged file a pre-existing test fixture). |
| Changed tests + lint green at HEAD | ✓ MEASURED | `npx vitest run` on `mutation-annotation-parser`, `mutation-runner-floors`, `gate-family-meta`, `drift-check-scripts`, `check-planning-hygiene` -> `Test Files 5 passed (5)`, `Tests 592 passed (592)` (run strictly AFTER all lane runs finished). `npx eslint "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"` exit 0 (that file's diff is comment + `_computationError` rename only). `gate-family-meta` registers `FILES_FLOOR=44` and `ARMS_FLOOR=361` with measurement tokens. |

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `scripts/pg-lane/run.sh` | pg_cron preloaded on the single `pg_ctl -o` start; fail-loud when absent | ✓ VERIFIED | `-o "-p $PORT -c listen_addresses=127.0.0.1 -k '' -c shared_preload_libraries=pg_cron -c cron.database_name=postgres -c cron.max_running_jobs=0"`; pre-initdb refusal on missing `.so/.dylib` or `.control`; `--print-pgbin` resolves `/opt/homebrew/opt/postgresql@16/bin` here; self-test 6/6 |
| `scripts/pg-lane/install-pg-cron-macos.sh` | repeatable macOS build for the @16 keg | ✓ VERIFIED (artifact present, 367 lines; its OUTPUT is what the lane loaded: `pg_cron.dylib` dated Sep 4 22:07) | Not re-run here (would rebuild the extension on the authoring box) |
| `.github/workflows/ci.yml` provisioning step | installs `postgresql-<major>-cron` keyed off the lane's own binaries | ⚠️ PRESENT, ubuntu behavior at HEAD unverified | Step exists with `--print-pgbin` selector + MEASURE_FAIL branches; the version that RAN (run 33961609382) used the previous `ls`-glob selector |
| `scripts/mutation-runner/run.mjs` | floors 44/361/0; honest `lane-blocked:`/`lane-probe:` prints; tripwire intact | ✓ VERIFIED | see truths 1, 3 and the floor check |
| `scripts/mutation-runner/parse.mjs` | classifier UNCHANGED, boundary documented | ✓ VERIFIED | diff is 100% comment |
| Five gate files under `supabase/tests/` | annotated, every arm bites | ✓ VERIFIED | 3 + 7 + 37 + 24 + 28 = 99 biting sections |
| `164.4.1-TRIPWIRE-FIRED.log` | the tripwire OBSERVED firing on the real corpus | ✓ VERIFIED | firing recorded at 3b6c338a, b59d2b1d and ubuntu f04ce51b; the file itself says the ubuntu CLEARED half is owed (D-164.4.1-06-1) |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| Gate files' `RED-UNDER-SETUP` | `supabase/migrations/20260513094906_enable_pg_cron.sql` | apply list | ✓ WIRED | present in all five; CREATE EXTENSION comes from the real migration, never a fixture (CONTEXT execution shape) |
| `run.mjs` probe leg | `fixtures/lane-probe/pg-cron-probe.sql` -> `run.sh` | `laneRunner({gateAbs: LANE_PROBE_GATE, leg: "probe"})` | ✓ WIRED | printed `lane-probe: pg_cron AVAILABLE` on every real run here and on ubuntu |
| `run.mjs` `lane-blocked-stale` | `parse.mjs` `scanCorpus().laneBlockedFiles` | `corpus.laneBlockedFiles.length > 0` | ✓ WIRED | class still DERIVED (parser test AIM proves fixture pair still classifies `lane-blocked-gate.sql`) |
| `ci.yml` MEASURE_FAIL greps | runner prints | `^lane-blocked: [0-9]+ file\(s\) `, `^lane-probe: pg_cron (absent\|AVAILABLE)`, `^arms: `, `^biting: ` | ✓ WIRED | HEAD's runner output matches all four regexes (checked against the local full log) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| copy_parity arms bite with pg_cron on the lane | `node scripts/mutation-runner/run.mjs --file supabase/tests/test_compute_jobs_error_kind_copy_parity.sql` | 3/3 `RED (identity ok)`, baseline/restore exit 0 | ✓ PASS |
| fanout arms bite | `… --file supabase/tests/test_derive_allocator_keys_fanout.sql` | 7/7 `RED (identity ok)` | ✓ PASS |
| Whole corpus | `node scripts/mutation-runner/run.mjs` | `arms: 361/361/0`, exit 0, 488 s | ✓ PASS |
| Floors fire | same, with 45/362 in a throwaway commit | two `floor` defects, exit 1 | ✓ PASS |
| Runner self-test | `node scripts/mutation-runner/run.mjs --self-test` | `SELF-TEST PASSED` 17/17 | ✓ PASS |
| Lane self-test | `bash scripts/pg-lane/run.sh --self-test` | `SELF-TEST PASSED (6/6)` | ✓ PASS |
| D-07 pin can fail | neuter `gateNeedsPgCron` -> vitest | 4 failed incl. `D-07 boundary`; restored, shasum OK | ✓ PASS |
| ubuntu run of HEAD | — | none exists | ? SKIP -> human |

### Requirements Coverage

No `REQUIREMENTS.md` IDs exist for this inserted phase (ROADMAP: "the five success criteria stand in for requirement IDs"). Coverage = the truths table above: SC-2, SC-3, SC-4, SC-5 SATISFIED; SC-1 NEEDS HUMAN (ubuntu at HEAD).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| (13 phase-modified code files) | — | `TBD\|FIXME\|XXX` | none found | — |
| `.planning/WINDOWS.md` | entry 32 | "5 of 39 sections use GATE-FILE falsifiers" for `test_reconcile_dropped_enqueue_sweep.sql` — now stale after CR-01 (2/JOB-04 got a production twin), CR-02 (3/JOB-04, 4/JOB-04 reclassified; file is 37 sections) and WR-A | ℹ️ Info | ledger currency only; dated record, not a gate input |
| `.github/workflows/ci.yml` | ~938 | DECISION block quotes `arms: 363/363/0` for run 33961609382 and immediately marks it SUPERSEDED by 361 | ℹ️ Info | honest lineage; the 361 measurement on ubuntu is what does not exist yet |

### Human Verification Required

#### 1. SHA-bound ubuntu measurement of HEAD

**Test:** Push/dispatch CI at `581d54cb` (or open the PR). In `sql-mutation`, read the `Provision pg_cron for the lane's PostgreSQL (Phase 164.4.1)` step (`PGBIN the lane will boot:`, `PostgreSQL major reported by …: 16`, `Version: 1.6.2-1`, both `ls -l` lines) and the runner summary.
**Expected:** `coverage: files 44/71`, `arms: 361/361/0`, `biting: 361`, `lane-invocations: 361`, `lane-blocked: 0 file(s)`, `lane-probe: pg_cron AVAILABLE`, `✅ No defects`, exit 0 — matching the macOS reading in this report; `frontend` aggregator green.
**Why human:** Only two CI runs exist on the branch and both predate HEAD by 24+ commits; HEAD's provisioning step has never executed on ubuntu; a macOS verifier cannot substitute. This is exactly the measurement `D-164.4.1-06-1` and the TRIPWIRE log's "CLEARED half … STILL OWED" section say is owed; nothing in the tree has been made to look as though it exists.

### Gaps Summary

No code gap was found. Every machine-checkable half of the goal holds at HEAD on the authoring box, and every claim the orchestrator asked to have falsified survived being run rather than read: the classifier and probe are byte-unchanged (the tripwire was cleared by annotating the fifth file, and it still fires in both self-test directions and fired three times on the real corpus during the phase), the floors are pinned at the measured 44/361/0 and each fires one higher, there are zero waivers, no migration was touched, and no home path leaked.

What remains is a MEASUREMENT, not a fix: SC-1 states that macOS and ubuntu measure the SAME corpus, and for the tree that will ship (361 arms, rewritten provisioning step) only the macOS half has been measured. Run 33961609382 proves the substrate works on ubuntu (apt `postgresql-16-cron` 1.6.2-1 loads, 363 arms bite at mean 1.1 s/lane) but it is a run of a superseded tree whose CI aggregator was also red for three since-fixed reasons. Until a SHA-bound run of HEAD exists, this phase is complete on one OS and presumed — not shown — on the other. Route: `human_needed`.

---

_Verified: 2026-09-05T14:52:21Z_
_Verifier: Claude (gsd-verifier)_
