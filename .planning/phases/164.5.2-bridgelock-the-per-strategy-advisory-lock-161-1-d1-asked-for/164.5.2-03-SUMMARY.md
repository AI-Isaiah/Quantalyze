---
phase: 164.5.2
plan: 03
subsystem: testing
tags: [mutation-runner, census, floors, sql-gates, todos]

requires:
  - phase: 164.5.2-01
    provides: "the LANE-ONLY gate test_mark_rpc_bridge_advisory_lock.sql (arms L1-L4) and the L1/L2 RED-then-GREEN evidence"
  - phase: 164.5.2-02
    provides: "the function snapshots, VAC-04 acks and CI dblink probe"
provides:
  - "FILES_FLOOR 51 and ARMS_FLOOR 453, read off a full lane run, separated in both directions"
  - "every corpus census pin at the measured corpus, which now includes the new gate"
  - "TODOS.md: DEC-4 names Phase 164.5.2; 161.1-D1 closed on evidence, with its remainder routed to 164.5.2.1"
affects: [164.5.2.1, sql-mutation CI job]

actuals:
  tokens: 12844
  tasks: 3
  commits: 2
plan_head_before: 547bbc5fe9fe5954be1f03295aad8f104a758c17

tech-stack:
  added: []
  patterns:
    - "census pins are read off a run or a failing test's actual value; calibration pins move to measured + their existing offset"

key-files:
  created: []
  modified:
    - scripts/mutation-runner/run.mjs
    - src/__tests__/mutation-runner-floors.test.ts
    - src/__tests__/mutation-annotation-parser.test.ts
    - src/__tests__/lint-sql-gates.test.ts
    - src/__tests__/gate-family-meta.test.ts
    - TODOS.md

key-decisions:
  - "Tasks 1 and 2 were committed together. The floors vitest stays red until the census pins move, and KNOWN_THRESHOLD_SITES must equal run.mjs in the same commit."
  - "SUBSET_LOG's `scope: SUBSET 2/N` denominator and the `lying` needle moved 50 -> 51 because they are the annotated-file count. The prose `2 of 49` in SUBSET_LOG's two trailing lines was already stale before this phase (49 against 50). The count-recheck step does not read it, so it was left as it is."
  - "The `per-arm lane time: mean 2.0s` figure in GREEN_LOG keeps its synthetic mean. Only its arm count moved, because only the count is census."

requirements-completed: [161.1-D1, DEC-4]

coverage:
  - id: D1
    description: "FILES_FLOOR and ARMS_FLOOR sit at the measured corpus, and each drift direction has been observed to fail"
    requirement: "161.1-D1"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs (final run: exit 0, No defects, arms 453/453/0, lane-invocations 453); too-high leg at ARMS_FLOOR=454: exit 1, single floor defect"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-runner-floors.test.ts at the pre-phase 50/449 (RATCHET STALE) and at 51/453 (99 passed)"
        status: pass
    human_judgment: false
  - id: D2
    description: "every census pin across the four test files agrees with the measured run; calibrations keep their offsets"
    verification:
      - kind: unit
        ref: "npx vitest run on the six files named by Task 2's verify: 6 files, 1198 tests passed; npm run lint rc 0; npm run typecheck rc 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "DEC-4 names Phase 164.5.2 and 161.1-D1 is closed on the plan 01 RED-then-GREEN evidence"
    requirement: "DEC-4"
    verification:
      - kind: other
        ref: "Task 3's automated verify chain (grep -A6 DEC-4 >= 2; awk 161.1-D1 range CLOSED count == 1; runbook no diff vs origin/main; check-planning-hygiene OK)"
        status: pass
    human_judgment: false
  - id: D4
    description: "the new gate's four arms bite on the CI sql-mutation job (ubuntu, dblink)"
    verification: []
    human_judgment: true
    rationale: "CI host facts cannot be measured locally. The PR's SHA-bound sql-mutation log is the evidence."

duration: 85min
completed: 2026-09-26
status: complete
---

# Phase 164.5.2 Plan 03: floors, census and the 161.1-D1 closeout Summary

**FILES_FLOOR moved 50 -> 51 and ARMS_FLOOR moved 449 -> 453, both read off full lane runs, and the runs showed each drift direction failing. Every corpus census in four test files now matches the measured corpus, with the three deliberate-mismatch calibrations still at their offsets. In `TODOS.md`, DEC-4 names Phase 164.5.2 and `161.1-D1` is closed on plan 01's RED-then-GREEN evidence.**

## Performance

- **Duration:** about 85 min, most of it four full mutation-runner lane runs of about 10-12 min each
- **Started:** 2026-09-26T14:19:50Z
- **Completed:** 2026-09-26T15:47:40Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- The floors are at the measured corpus, and a run with each floor out of place fails:
  - Too high (ARMS_FLOOR 454): the runner exits 1.
  - Too low (the pre-phase 50/449): the floors vitest goes RED with `RATCHET STALE`.
  - At the measured values: the run exits 0 with `✅ No defects`.
- Every census pin was moved to a measured value, in one commit.
- The backlog records who took DEC-4. It closes `161.1-D1` and names what stays open (Phase 164.5.2.1).

## Task Commits

1. **Tasks 1 + 2: floors and census pins** - `8f6adc12d` (test)
2. **Task 3: DEC-4 / 161.1-D1 closeout** - `983e78f1c` (docs)

**Plan metadata:** the commit that carries this SUMMARY.

## D-16 (pasted before the first edit)

```
$ grep -lE 'RED-UNDER-M.*(20260603120000|20260529180000)' supabase/tests/*.sql
(no output, rc=1)
$ grep -lE 'RED-UNDER-M.*20260926120000' supabase/tests/*.sql
supabase/tests/test_mark_rpc_bridge_advisory_lock.sql
```

No existing twin targets the two re-based migrations, so the new migration shadows none. `verify-plan-anchors` on this plan printed `OK: 1 plan file(s), no stale claims.` Preconditions: `--print-pgbin` rc 0, and `require.resolve('vitest/config')` resolved.

## Floor separation: the four legs

**Leg 1, stale-low runner leg (floors at pre-phase 50/449), full run, no file edited during it.** Before and after the run, `git status --short` was identical. It was run on the committed `run.mjs`, with only the two constants set back to 50/449 from a byte backup; after the run the file was restored, `cmp` exited 0, and `git diff` was empty:

```
scope: FULL 51/51 annotated files
coverage: files 51/78
lane-probe: pg_cron AVAILABLE — lane-blocked class is empty, as a hosting lane requires
  pending: 0 idiom file(s) without RED-UNDER —
arms: 453/453/0   (executed/annotated/waived)
biting: 453   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 453   (arm lanes actually spawned — tallied inside runLane, independent of the 453 the verdict loop counted; plus 51 baseline / 51 restore leg(s))
  file test_mark_rpc_bridge_advisory_lock.sql: sections 4 / judged 4 / annotated 4 / waived 0 / biting 4
per-arm lane time: mean 1.1s over 453 arm run(s)
✅ No defects. Every annotated arm bit its own arm first.
rc=0
```

With the floors below the corpus, the runner exits 0: it cannot see a floor that is too low, by construction. Leg (b) shows the floors vitest catching that direction instead.

An earlier attempt at this leg was invalidated, so the run above is a re-run. See Issues Encountered.

**Leg (a): floors set to measured 51/453, floors vitest run.** The `RATCHET STALE` and `ARMS_FLOOR is` tests passed. Only the census lines Task 2 moves were red.

**Leg (b), stale-low vitest leg.** The pre-phase values were restored from a byte backup and the floors vitest was run:

```
AssertionError: RATCHET STALE: 51 of 78 gate files are now annotated but FILES_FLOOR is still 50. Raise FILES_FLOOR in scripts/mutation-runner/run.mjs to 51.: expected 51 to be 50
AssertionError: The corpus declares 453 twin(s) of which 0 are waivers, so a green run bites 453. ARMS_FLOOR is 449. Update the floor in scripts/mutation-runner/run.mjs from a MEASURED run, never from this number.: expected 449 to be 453
      Tests  2 failed | 37 passed | 60 skipped (99)
```

The measured values were then restored from the byte backup, and `cmp` exited 0.

**Leg (c), too-high runner leg (ARMS_FLOOR = 454), full run.** It exited 1 with exactly one defect, the floor:

```
arms: 453/453/0   (executed/annotated/waived)
biting: 453   (…)
lane-invocations: 453   (… plus 51 baseline / 51 restore leg(s))
❌ 1 defect(s):
  floor                 -                         <repo>/supabase/tests
      ARMS_FLOOR regression: 453 biting arm(s) < floor 454
rc=1
```

`<repo>` stands for the worktree path the runner prints, redacted here. The value was restored from the byte backup, and `cmp` exited 0.

**Leg (d), final run at 51/453, the RE-DERIVED comments included, no file edited during it.** `git status --short` was identical before and after the run.

```
scope: FULL 51/51 annotated files
  arm L1                       exit   3  RED (identity ok)  (2.1s)
  arm L2                       exit   3  RED (identity ok)  (2.2s)
  arm L3                       exit   3  RED (identity ok)  (2.2s)
  arm L4                       exit   3  RED (identity ok)  (2.2s)
coverage: files 51/78
unreachable: 27 file(s) raise outside the runner's identity idiom — …
lane-blocked: 0 file(s) probe pg_extension for pg_cron and are not yet annotated — (none; …)
lane-probe: pg_cron AVAILABLE — lane-blocked class is empty, as a hosting lane requires
  pending: 0 idiom file(s) without RED-UNDER —
arms: 453/453/0   (executed/annotated/waived)
biting: 453   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 453   (arm lanes actually spawned — tallied inside runLane, independent of the 453 the verdict loop counted; plus 51 baseline / 51 restore leg(s))
  file test_mark_rpc_bridge_advisory_lock.sql: sections 4 / judged 4 / annotated 4 / waived 0 / biting 4
per-arm lane time: mean 1.7s over 453 arm run(s)
✅ No defects. Every annotated arm bit its own arm first.
rc=0
```

After the run: `FILES_FLOOR = 51`, `ARMS_FLOOR = 453`, `WAIVED_CEILING = 0`. The `arms:` executed figure (453) equals `lane-invocations:` (453).

## Pin classification (Task 2)

Pins were found by grepping `test_cron_runs_rls`, `449`, `50/77`, `50/50`, `77 file`, `toBe(77)` and `toBe(50)` across `src/__tests__`, `scripts` and `.github`. The `ci.yml` hits (`449 legs`) are dated prose about an older ubuntu measurement (361 + 44 + 44), not pins. `drift-check-scripts.test.ts` and `ci.yml` mention `test_cron_runs_rls` only in sentinel prose. The new gate declares no sentinel (`grep -c 'ALL [0-9]* ARMS EXECUTED'` on it prints `0`), so neither file moved.

**(i) CENSUS, moved to the measured value**

| File | Pin | Before -> after | Source |
|---|---|---|---|
| floors | `expect(totalAnchored).toBe(` | 449 -> 453 | failing test `expected 453 to be 449` |
| floors | `scope: FULL N/N annotated files` (GREEN_LOG, the push/PR `toContain`, the fallback replacement, `twice` ×3, `diag`) | 50/50 -> 51/51 (7 sites) | run `scope:` |
| floors | `coverage: files` (GREEN_LOG, SUBSET_LOG) | 50/77 -> 51/78 | run `coverage:` |
| floors | GREEN_LOG `arms:` / `biting:` / `lane-invocations:` lines incl. legs | 449 / 50 legs -> 453 / 51 legs | run lines, copied |
| floors | new per-file row `test_mark_rpc_bridge_advisory_lock.sql … biting 4` | added between staleness and metrics | run per-file line |
| floors | `per-arm lane time: … over N arm run(s)` | 449 -> 453 | run |
| floors | `N arm lane(s) spawned` | 449 -> 453 | run |
| floors | `printed N per-file row(s) but reported M annotated file(s)` | 51/50 -> 52/51 | failing test's actual output |
| floors | `reports N` | 449 -> 453 | run `biting:` |
| floors | `/^lane-invocations: N /m` needles (severed, extra, garbled) and `executed=N … biting=N` | 449 -> 453 | run |
| floors | `/^arms: N\/N\/0 /m` needles (waived, zero), the waived replacement's annotated/executed halves, and the zero replacement's annotated half | 449 -> 453 | run |
| floors | `/^biting: N /m` needle, the `executed (N)` half | 449 -> 453 | run |
| floors | `biting arms N >= N` | 449 -> 453 | run + ARMS_FLOOR |
| floors | SUBSET_LOG `scope: SUBSET 2/N` and `lying` `2/N ` -> `3/N ` | 50 -> 51 | annotated count |
| floors | `printed 2 per-file row(s) but reported N annotated file(s)` | 50 -> 51 | follows the fallback scope line |
| parser | `armsSeen` | 449 -> 453 | failing test `expected 453 to be 449` |
| parser | `stepsSeen` | 464 -> 476 | re-run after armsSeen moved: `expected 476 to be 464` |
| parser | `needles.length` | 464 -> 476 | failing test `expected 476 to be 464` |
| parser | `corpus.filesTotal` (two sites) | 77 -> 78 | failing test `expected 78 to be 77` |
| parser | `corpus.filesAnnotated`, `annotatedFiles` `toHaveLength` | 50 -> 51 | run `coverage: files 51/78` |
| parser | sorted `annotatedFiles` list | + `test_mark_rpc_bridge_advisory_lock.sql` | scanCorpus |
| lint-sql-gates | `/scanned N file/` | 77 -> 78 | `node scripts/lint-sql-gates.mjs`: `scanned 78 file(s); 0 finding(s)` |
| gate-family-meta | `KNOWN_THRESHOLD_SITES` `FILES_FLOOR=` / `ARMS_FLOOR=` | 50 / 449 -> 51 / 453 | run.mjs |

**(ii) CALIBRATION, moved to measured + existing offset (offset preserved)**

| Pin | Before (census / calibration, offset) | After (census / calibration, offset) |
|---|---|---|
| `extra` replacement `lane-invocations: N` and `lane-invocations=N` | 449 / 450, +1 | 453 / 454, +1 |
| `spliced` replacement `biting: N` and `biting (N) exceeds executed` | 449 / 450, +1 | 453 / 454, +1 |
| `rows sum to N biting arm(s)` paired with `reports M` | 449 / 448, -1 | 453 / 452, -1 |

**(iii) FIXED-VALUE, not moved.** `grep -nF` was run on each literal before the first edit and again after Task 2. The line counts are equal, and the line numbers shifted only because comments were added above them:

| Literal | Before | After |
|---|---|---|
| `, "lane-invocations: 0 ")` | 1 line (2204) | 1 line (2225) |
| `, "lane-invocations: abc ")` | 1 line (2237) | 1 line (2262) |
| `${WAIVED_CEILING + 1}` | 2 lines (2265, 2269) | 2 lines (2290, 2294) |
| `biting 45$/m, "$1biting 44")` | 1 line (2161) | 1 line (2180) |

The new gate's row ends `biting 4`, so the `biting 45$` regex still matches only the `test_strategy_shares_rls.sql` row. The zero replacement's executed field stays at its literal `0`.

A grep of the other three test files for `DELIBERATE`, `MISMATCH` and `gap of one` found only prose in `mutation-annotation-parser.test.ts`, at lines 2139, 2271 and 2805 (the lane-blocked closure narrative). None of those lines is a pin. `lint-sql-gates.test.ts` and `gate-family-meta.test.ts` had no hits.

GREEN_LOG's 51 per-file rows were diffed against the final run's 51 per-file lines. They are byte-identical.

## Task 2 verification

- `npx vitest run` on floors, parser, lint-sql-gates, gate-family-meta, drift-check-scripts and ci-anti-skip-gate.contract: `Test Files 6 passed (6)`, `Tests 1198 passed (1198)`.
- `npm run lint`: rc 0, and it ends with `[check-planning-hygiene] OK`.
- `npm run typecheck`: rc 0.
- `git show --name-only` on `8f6adc12d` and `983e78f1c` lists neither `.github/workflows/ci.yml` nor `src/__tests__/drift-check-scripts.test.ts`.

## Task 3 (TODOS.md)

- Before the edit, `awk '/^161\.1-D1\./,/^161\.1-D2\./' TODOS.md | grep -cE 'CLOSED 2026-[0-9]{2}-[0-9]{2} by Phase 164\.5\.2'` printed `0`. After the edit it prints `1`.
- The closure depends on the plan 01 SUMMARY carrying both RED observations, and it does. L1: `TEST FAILED (L1): a second mark_compute_job_done on the same strategy is waiting, but NOT on an advisory lock`, with the done lock removed. L2: `TEST FAILED (L2): … mark_compute_job_failed …`, with the failed lock removed. Each went GREEN after a `cmp`-verified restore.
- Both DEC-4 entries carry a dated `TAKEN 2026-09-26 by Phase 164.5.2 BRIDGELOCK` clause within six lines of the `DEC-4` token.
- The closure line names what stays open: the bridge's non-mark callers, and a lock inside the bridge itself, both routed to Phase 164.5.2.1.
- No `164.6.7` entry was touched (the diff has 0 `164.6.7` lines), and `docs/runbooks/ledger-refresh-go-live.md` has no diff against `origin/main`.
- Task 3's full automated verify chain exited 0, and planning hygiene was OK.

## Pre-merge obligations carried by the ship step (not executed here)

- **D-18, three reviewers before merge:** migration-reviewer, rls-policy-auditor and silent-failure-hunter review the migration and the gate, and every finding is fixed before merge. There is NO human PROD gate: merging `supabase/migrations/**` auto-applies the migration to TEST and then to PROD.
- **D-23, checks expected to be RED on the PR (none is allowlisted):**
  - `baseline-content-drift` in `sql-gate-lint`: already red on `main`, and this PR adds the `mark_compute_job_done` and `mark_compute_job_failed` rows.
  - VAC-04: green only if PROD's bodies equal the two earned acks. A red here is a real PROD drift to investigate.
  - VAC-08 in `test-db-drift`: red by construction until apply-on-merge.
  - Every other check must be green, and every check must be bound to the head SHA.
- **Manual CI reading:** the PR's `sql-mutation` log must show the dblink probe step passing and the four new arms biting.
- **D-22:** this plan set touches neither `sync_strategy_analytics_status` nor the 164.6.7 residues. D-12's four 164.9.1 routings are owned by the founder.
- CHANGELOG and VERSION were not written here. The ship step writes them.

## Decisions Made

See `key-decisions` in the frontmatter.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing pins] Census sites the plan did not list**
- **Found during:** Task 2.
- **Issue:** The plan's list left out several census sites: `scope: FULL 50/50` (seven sites), SUBSET_LOG's `2/50` denominator and its `lying` needle, `printed 2 … reported 50`, and `annotatedFiles` `toHaveLength(50)`.
- **Fix:** All of them moved to measured values, as listed in the classification table.
- **Commit:** `8f6adc12d`.

**Total deviations:** 1 auto-fixed (missing pins). **Impact:** none on the committed result. The final run and the full vitest set are green.

## Issues Encountered

- **The first attempt at the stale-low runner leg was invalidated, and I caused it.** I edited `TODOS.md` (Task 3's content) while that run was in progress. The runner compares the working tree before and after, and it exited 1 with its only defect `dirty-checkout: + M TODOS.md`. Its tallies (453/453/0, lane-invocations 453) were the same as the clean run's, and it raised no floor defect. I re-ran the leg after all three task commits, with nothing edited during the run, and that re-run is the Leg 1 paste above (rc 0, no defects). This is the run the `run.mjs` ARMS_FLOOR comment's "each with no file edited during it" refers to.
- The runner's `floor` defect line prints the absolute worktree path. It is redacted in the paste above.
- The SUBSET_LOG prose `2 of 49` has been stale since before this phase. It was left as it is: the count-recheck step does not read it.

## Next Phase Readiness

The phase's three plans are complete. The next step is the phase verifier. After it comes the ship step, with the three reviewers and the expected-RED list above. Phase 164.5.2.1 BRIDGERESIDUE carries what stays open.

## Self-Check: PASSED

- FOUND: all six modified files; `FILES_FLOOR = 51`, `ARMS_FLOOR = 453`, `WAIVED_CEILING = 0` in run.mjs.
- FOUND: commits `8f6adc12d` and `983e78f1c` (`git rev-list --count 547bbc5fe..HEAD` = 2 before the SUMMARY commits). `actuals.commits` counts the two task commits; the SUMMARY is `8f532285c` plus this revision.
