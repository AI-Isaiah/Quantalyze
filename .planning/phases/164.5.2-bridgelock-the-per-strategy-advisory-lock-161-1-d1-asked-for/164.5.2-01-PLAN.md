---
phase: 164.5.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql
  - supabase/tests/test_mark_rpc_bridge_advisory_lock.sql
  - scripts/pg-lane/fixtures/36-fixture-dblink.sql
  - src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts
autonomous: true
requirements:
  - 161.1-D1
  - DEC-4

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01: ONE migration changes BOTH `mark_compute_job_done` and `mark_compute_job_failed`; at the end of this plan the migration carries the lock in both, never in one only"
    - "D-02: the lock is `pg_advisory_xact_lock` in the TWO-KEY form with namespace `hashtext('mark_compute_job_bridge')`, disjoint from the single-key strategy hash that `sync_trades` and `positions_atomic_rebuild` take (objsubid 2 vs 1), and the migration's own DO block recomputes `hashtext('mark_compute_job_bridge') <> hashtext('admin_role_mutate')` at apply time; a grep of every migration shows no other two-key namespace equal to it"
    - "D-03 / D-24: the lock is the FIRST statement inside each RPC's existing `IF v_strategy_id IS NOT NULL THEN` guard, immediately before `PERFORM sync_strategy_analytics_status(v_strategy_id)`; a strategy-less job takes no lock and the placement is pinned by a comment-stripped, statement-shaped self-verify anchor (the strategy-less behaviour is a named INVARIANT, not a counted arm)"
    - "D-04: the migration header records the full lock order on both paths (job row, then fan-in children for done, then the advisory lock, then the strategy_analytics row) and states that the lock, taken last, joins no new deadlock cycle and does not worsen the 164.9.1 FOR SHARE diamond"
    - "D-05: each RPC body is re-based on its LATEST definition found by re-grep at execution; the per-RPC diff of the `CREATE OR REPLACE ... $$;` block against that latest body shows exactly ONE added line (the lock) and nothing removed, pasted in the SUMMARY"
    - "D-11: the migration timestamp sorts after every file in `supabase/migrations` and after `20260925120000`"
    - "D-13 / D-21: every schema proof in this plan is a pg-lane apply on a throwaway loopback cluster; no supabase or psql command touches any remote database"
    - "D-14 (CORRECTED): the gate proves serialization by WHICH lock backend B waits on — an ungranted `advisory` row with the masked-oid classid and objsubid 2 — never by whether B waits; its first statement is `CREATE EXTENSION IF NOT EXISTS dblink`; seeds are COMMITTED; the file is LANE-ONLY"
    - "D-15: every arm raises `TEST FAILED (X):` directly in a main-session DO body, carries a layered RED-UNDER-M twin (production edit + the matching self-verify anchor disabled in the same apply), and each twin reddens exactly its own arm first"
    - "161.1-D1 criterion 2: the gate is OBSERVED RED with the done lock line removed (`TEST FAILED (L1)`) and with the failed lock line removed (`TEST FAILED (L2)`), and GREEN after each byte-backup restore, both pasted"
    - "D-20: the lifted plan at `7910f614` is used as a source only; its migration name, directory references and founder-gate wording are not carried"
  artifacts:
    - path: "supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql"
      provides: "both mark RPCs re-based on their latest bodies plus the two-key per-strategy lock inside the existing strategy guard, and a catalog-only DO $verify$ block"
    - path: "supabase/tests/test_mark_rpc_bridge_advisory_lock.sql"
      provides: "LANE-ONLY two-backend dblink gate, arms L1-L4 with layered RED-UNDER-M twins"
    - path: "scripts/pg-lane/fixtures/36-fixture-dblink.sql"
      provides: "the lane-only fixture that creates the dblink contrib extension, named by the LANE-ONLY marker"
    - path: "src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts"
      provides: "the LANE_ONLY_SITES register entry for the new gate"
  key_links:
    - "the lock line appears TWICE with identical text (done first, failed second) -> every twin that edits one lock line uses `nth` and a MEASURED `occurrences`"
    - "the carried-forward done anchors open with `IF v_done_body !~*` / `IF v_done_body ~*` (the re-base source's STEP 3 has three `IF v_done …` lines plus an `OR v_done !~*`), so an `IF v_done_body !~` find would prefix-match them -> each lock anchor is a DEDICATED boolean (`v_done_lock_anchored`, `v_failed_lock_anchored`) tested by `IF NOT v_<rpc>_lock_anchored THEN`; that text appears exactly once per RPC, so each disable step's `find` matches only its lock anchor with `occurrences` 1"
    - "`LANE_ONLY_SITES` is compared with `toEqual` against a list SORTED by filename -> the new entry goes BEFORE `test_prod_prober_cadence.sql` in the array"
    - "`deriveLaneOnlySites` throws on a missing or empty `fixture` key -> the marker names the real dblink fixture, never an omitted key"
    - "a gate-level regex check of the lock line would redden before the dblink observable runs -> SETUP arms check only prerequisites no twin touches"
  prohibitions:
    - "NEVER add a data-reading assertion to the migration's DO block ([164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]); catalog reads and random-uuid behavioural probes only"
    - "NEVER restore a mutated file with `git checkout --`, `git restore` or `git stash`; restore from a byte backup taken immediately before the mutation and prove it with `cmp`"
    - "NEVER wrap the gate in BEGIN/ROLLBACK (dblink backends cannot see uncommitted seeds)"
    - "NEVER run `supabase db push`, `supabase db reset`, or psql against any non-loopback host"
---

<objective>
Close `161.1-D1` (DEC-4: the lock in its own phase, with a real concurrency test): add a per-strategy,
transaction-scoped, two-key advisory lock to `mark_compute_job_done` and `mark_compute_job_failed`,
and prove with two genuinely separate backends that a second terminal mark on the same strategy
waits on THAT lock.

Purpose: the bridge `sync_strategy_analytics_status` reads `compute_jobs` twice at READ COMMITTED;
two concurrent terminal marks on one strategy interleave there today. The lock serializes
mark-versus-mark per strategy (D-24 placement: last lock the transaction takes).

Output: one migration (both RPCs), one LANE-ONLY dblink gate with four counted arms, one lane
fixture, one register entry. Plan 02 regenerates snapshots and earns the VAC-04 acks; plan 03 moves
the census and closes the bookkeeping.

**Scope notes (recorded so no executor re-opens them):**
- CONTEXT D-06..D-10 (the two 164.6.7 bridge residues and the lock-inside-the-bridge question) MOVED
  to Phase 164.5.2.1 BRIDGERESIDUE (D-22). This plan does NOT edit `sync_strategy_analytics_status`.
- CONTEXT D-12 (the four 164.9.1 routings) is FOUNDER-OWNED and outside this plan set.
- There is no human PROD gate: merging `supabase/migrations/**` auto-applies to TEST then PROD
  (D-18). No task in any plan applies anything to a remote database.

**Sizing (revision round 1, kept as one plan):** the uncalibrated 95000 estimate is carried by Task 1,
the tracer, whose six steps are one indivisible slice (a migration with no gate, or a gate with no
register entry, is not a runnable path). Splitting would move Tasks 2-3 into a new sequential plan on
the same two files, adding a wave without shrinking the heaviest task; Task 3 is read-and-run only.

`<assumption_delta_decision>` noun: the per-strategy advisory lock in the two terminal mark RPCs —
unchanged. decision: `no-change`. Rationale: the detector fired on prose from the ROADMAP section's
routed D-12 items (a second `match_decisions` delete, etc.), which are out of this plan set, not on
any change to the identity model this phase builds.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-CONTEXT.md
@.planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-RESEARCH.md
@.planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-PATTERNS.md
@scripts/mutation-runner/GRAMMAR.md
@scripts/pg-lane/README.md
@supabase/migrations/20260603120000_compute_jobs_rpc_clear_error_and_gin_fanin.sql
@supabase/migrations/20260529180000_fix_mark_compute_job_failed_error_kind_column.sql
@supabase/migrations/20260924230827_fanin_initial_status_10param.sql
@supabase/migrations/20260530120000_admin_role_mutate.sql
@supabase/tests/test_prod_prober_cadence.sql
@supabase/tests/test_sync_status_curated_sentence_survives.sql
@scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql

**The lane command used by every verify below (LANE CMD).** It reads the gate's own
`-- RED-UNDER-SETUP:` line so the apply list has one source of truth, boots a throwaway
loopback cluster, and captures output to a file that is tailed:

`GATE=supabase/tests/test_mark_rpc_bridge_advisory_lock.sql; APPLY=$(node -e 'const s=require("fs").readFileSync(process.argv[1],"utf8");const l=s.split("\n").find(x=>x.startsWith("-- RED-UNDER-SETUP:"));process.stdout.write(JSON.parse(l.slice(19)).apply.join(" "))' "$GATE"); WD=$(mktemp -d); bash scripts/pg-lane/run.sh --workdir "$WD" --apply $APPLY --gate "$GATE" > "$WD.log" 2>&1; RC=$?; tail -40 "$WD.log"; echo "lane rc=$RC"; exit $RC`

**Terminology.** "The lock line" = the statement `PERFORM pg_advisory_xact_lock(hashtext('mark_compute_job_bridge'), hashtext(v_strategy_id::text));`
(RESEARCH Q2, measured on the lane). "Masked namespace oid" = `(hashtext('mark_compute_job_bridge')::bigint & 4294967295)::oid`
(RESEARCH Pitfall 1: `pg_locks.classid`/`objid` are oids, never compare the signed int4).
</context>

## Artifacts this phase produces

This plan produces:
- `supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql` (new; name re-checked per D-11 at execution — if the timestamp is taken, the next free `20260926…` value is used and every reference in plans 01-03 follows it)
- `supabase/tests/test_mark_rpc_bridge_advisory_lock.sql` (new, LANE-ONLY, RED-UNDER annotated, arms L1-L4)
- `scripts/pg-lane/fixtures/36-fixture-dblink.sql` (new; number is the next free fixture number at execution)
- `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` (modified: one `LANE_ONLY_SITES` entry)
- In `164.5.2-01-SUMMARY.md`: the per-RPC body diffs, the two-key namespace grep, and the pasted RED (L1, L2) then GREEN lane outputs

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — the lock in mark_compute_job_done + arm L1 through dblink, observed RED then GREEN on the lane</name>
  <precondition>`bash scripts/pg-lane/run.sh --print-pgbin` exits 0 and the printed bindir's `pg_config --sharedir` contains `extension/dblink.control`; `node -e "require.resolve('vitest/config')"` exits 0 from the worktree root (if not, the orchestrator links the main checkout's `node_modules` into the worktree; `/node_modules` is gitignored and is never staged).</precondition>
  <files>supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql, scripts/pg-lane/fixtures/36-fixture-dblink.sql, supabase/tests/test_mark_rpc_bridge_advisory_lock.sql, src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts</files>
  <reversibility rating="reversible">Every edit lands on a feature branch and a throwaway lane; the one-way door is the merge (auto-apply to TEST then PROD, D-18), which is the ship step, not this task.</reversibility>
  <read_first>
    - The phase CONTEXT (D-01..D-05, D-11, D-13..D-15, D-20, D-21, D-24), RESEARCH Q1-Q4 and Patterns 1-2, PATTERNS sections 1-2 and Shared Patterns.
    - `supabase/migrations/20260603120000_compute_jobs_rpc_clear_error_and_gin_fanin.sql` STEP 2 (the `mark_compute_job_done` body, REVOKE, COMMENT) and STEP 3 (its self-verify anchors for that function).
    - `supabase/migrations/20260924230827_fanin_initial_status_10param.sql` header (VAC-04 ack block shape, transaction-style paragraph, `SET LOCAL lock_timeout`) and its DO block (comment strip, NULL-body refusal, search_path value pin, ACL re-convergence via `_assert_no_public_execute` and `has_function_privilege`).
    - `supabase/migrations/20260530120000_admin_role_mutate.sql` (the only two-key call site).
    - `supabase/tests/test_prod_prober_cadence.sql` header (prose, then `-- LANE-ONLY:` line, then `-- RED-UNDER-SETUP:` line) and `supabase/tests/test_sync_status_curated_sentence_survives.sql` (its `RED-UNDER-SETUP` apply list, seed shape, the layered two-step twin idiom `IF FALSE AND …`).
    - `scripts/mutation-runner/GRAMMAR.md` rules 1-4, the layering rule, and the sequential multi-step `apply` semantics.
    - `scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql` header (fixture header style and the fixtures/ contract).
    - `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` `LANE_ONLY_SITES`, `deriveLaneOnlySites`, and the FORWARD / REVERSE checks.
  </read_first>
  <action>
    Step 0. Run `node scripts/verify-plan-anchors.mjs .planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-01-PLAN.md` and halt on non-zero. Re-check D-11: `ls supabase/migrations | tail -3` and `git ls-tree -r --name-only` of every open sibling branch you can see (`git branch -a`) for any `20260926*` migration and any `36-fixture-*`; if either name is taken, take the next free value and use it everywhere. Re-grep the latest definition per D-05: `grep -lE 'CREATE (OR REPLACE )?FUNCTION (public\.)?mark_compute_job_done\(' supabase/migrations/*.sql | sort | tail -1` (expected `20260603120000_…`) and the same for `mark_compute_job_failed` (expected `20260529180000_…`), plus `grep -lE 'ALTER FUNCTION (public\.)?mark_compute_job_(done|failed)' supabase/migrations/*.sql` (expected no output). If a newer definition exists, re-base on it and record that in the SUMMARY. Paste all outputs into the SUMMARY.

    Step 1 — the migration (per D-01, D-02, D-03, D-04, D-05, D-24). Create `supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql` in the NEWER house style of `20260924230827` (no explicit BEGIN/COMMIT, `SET LOCAL lock_timeout = '3s';`, single-literal RAISE formats; if the re-base source relies on a file-level `SET LOCAL search_path = public, pg_catalog;` carry it). Do NOT copy the explicit `BEGIN;`/`COMMIT;` wrapper of `20260603120000` (PATTERNS Rule-7 conflict: newer style wins). Header prose, all OUTSIDE any `$$` body: (a) what the lock closes (161.1-D1, DEC-4) and what it does not; (b) D-02: the namespace string, the two-key form, the objsubid 2 vs 1 disjointness, and the call sites sharing the single-key strategy hash (`sync_trades`, `positions_atomic_rebuild`); (c) D-04/D-24 lock order on both paths and why placing the lock last adds no cycle, including that the 164.9.1 FOR SHARE diamond (D-12 item 3) is unchanged; (d) the bridge callers that stay unserialized (Python DEFERRED direct call, claim RPCs, `reset_stalled_compute_jobs`, orphan terminalizer, enqueue inserts, cross-strategy fan-in release, marker retraction), routed to Phase 164.5.2.1; (e) a VAC-04 acknowledgement section heading whose two ack lines plan 02 writes; (f) the transaction-style paragraph. ⛔ The header must NOT contain the lock line's exact text, nor any exact anchor text a twin will `find` — describe them in words so every twin `occurrences` stays at its measured value. Then re-issue `mark_compute_job_done(UUID, UUID)` by copying its latest `CREATE OR REPLACE FUNCTION … $$;` block byte-for-byte and inserting the lock line as the first statement inside `IF v_strategy_id IS NOT NULL THEN`, indented to match the following PERFORM. Keep the one-line banner directly above CREATE short and neutral (the snapshot dumper copies it). Re-issue its REVOKE verbatim (`… FROM PUBLIC, anon, authenticated`). Do NOT reissue `COMMENT ON FUNCTION` (CREATE OR REPLACE keeps it; the diff stays the lock line only).

    Step 2 — the migration's `DO $verify$` for done (catalog-only). Resolve `to_regprocedure('public.mark_compute_job_done(uuid,uuid)')`; refuse a NULL oid; take `pg_get_functiondef`, strip block then line comments exactly as `20260924230827` does, refuse a NULL stripped body; store it in a variable named `v_done_body`. Anchor: assign a dedicated BOOLEAN `v_done_lock_anchored := v_done_body ~ '<the statement-shaped regex of RESEARCH Pattern 1>'` (guard `IF v_strategy_id IS NOT NULL THEN`, then the lock with namespace literal and `hashtext(v_strategy_id::text)`, then `PERFORM sync_strategy_analytics_status(v_strategy_id)`), then test it as ONE `IF NOT v_done_lock_anchored THEN RAISE EXCEPTION '<slug>: …'`. The find text for this anchor's disable step is `IF NOT v_done_lock_anchored THEN`; it must occur exactly once in the file (the carried-forward anchors below open with `IF v_done_body !~*` / `IF v_done_body ~*`, which an `IF v_done_body !~` find would prefix-match — hence the dedicated boolean, never a regex-prefixed find). Carry forward `20260603120000` STEP 3's four `mark_compute_job_done` anchors (the `@>` fan-in, no `FOR v_child_id IN`, the NULL-token gate with `invalid_parameter_value`, the bridge present) rewritten against `v_done_body`. Add: `hashtext('mark_compute_job_bridge') = hashtext('admin_role_mutate')` raises; `proconfig` contains the exact `search_path=public, pg_catalog` VALUE; SECURITY DEFINER (`prosecdef`); ACL re-convergence (`to_regrole` guard for anon/authenticated/service_role, `_assert_no_public_execute`, anon and authenticated lack EXECUTE). Every RAISE message is prefixed with the migration slug.

    Step 3 — the fixture. Create `scripts/pg-lane/fixtures/36-fixture-dblink.sql`: a header in fixture 34's style stating it is lane-only, NEVER applied to TEST or PROD, why the gate needs a second and third backend, and that dblink is PostgreSQL contrib (no package install); body is `CREATE EXTENSION IF NOT EXISTS dblink;`.

    Step 4 — the gate `supabase/tests/test_mark_rpc_bridge_advisory_lock.sql` (per D-13, D-14, D-15). Header prose: plain PL/pgSQL, no psql meta-commands, no BEGIN/ROLLBACK (committed seeds, fresh cluster per lane leg), the D-14 CORRECTED observable (discriminate on WHICH lock B waits on; with the lock removed B still waits, on a `transactionid` ShareLock — RESEARCH Q3 measurement), the named INVARIANT (a strategy-less job takes no lock; no production mutation is observable because `pg_advisory_xact_lock` is STRICT; it is pinned by the migration's placement anchor, not counted here), and the arm table. Then exactly one `-- LANE-ONLY: {json}` line with keys `object` = `dblink`, `fixture` = the new fixture path, `job` = `sql-mutation`, `reason` = why it cannot run on the local Supabase stack (dblink needs trust-auth superuser loopback and committed seeds; the extension is absent from the schema of record). Then exactly one `-- RED-UNDER-SETUP:` line whose `apply` is the curated gate's list (read it from that file, do not retype from memory) with the new fixture added after the core fixtures and the new migration LAST. Do not add `20260603120000` to the list (PATTERNS: it has never been applied on the lane and redefines both claim RPCs; the new migration re-bases the full bodies). First executable statement: `CREATE EXTENSION IF NOT EXISTS dblink;`. Seed DO block: neutral labels only (no real names), committed rows `auth.users` -> `profiles` -> `api_keys` -> `strategies` -> `running` `compute_jobs` rows with claim tokens, one strategy per arm, two jobs per same-strategy arm on DIFFERENT kinds (the one-inflight partial unique indexes forbid two in-flight rows of one kind per partition — measure on the lane), kinds chosen from those the apply list registers; record ids and tokens in a session TEMP table so later DO blocks re-select them (plpgsql variables do not cross DO blocks). Arm L1 (plus `L1-SETUP` identities) in its own DO block per RESEARCH Pattern 2: connect dblink sessions `a` and `b` to the lane via `format('host=127.0.0.1 port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user)`; `a` opens a transaction and runs `mark_compute_job_done` on job 1 of the L1 strategy through `dblink()` (never `dblink_exec` for a SELECT — 2F003); read `b`'s pid; `dblink_send_query('b', …)` the done mark of job 2; poll `pg_locks` for b's pid (bounded loop of `pg_sleep(0.05)`, exit on the first ungranted row); `L1-SETUP` raises if b never waits on anything; `L1` raises unless b's ungranted row has `locktype = 'advisory'` (L1 checks ONLY the locktype — not classid, objsubid or objid — so the L3 and L4 twins leave it green); then `a` commits, consume `dblink_get_result('b')`, disconnect both. SETUP identities check only prerequisites no twin touches (functions resolve, dblink loaded, the seeded rows are visible from backend b). ⛔ No SETUP arm inspects `pg_get_functiondef` for the lock — a static check would redden before the concurrency observable runs and make criterion 2 vacuous. Every raise is `RAISE EXCEPTION 'TEST FAILED (<id>): <why it matters>'` directly in the main session's DO body (rule 3c). ⛔ Use `--` comments only in the gate, never block comments: the contract test's `stripSqlComments` corpus check fails on any genuine block comment in `supabase/tests`. Add the L1 twin beside the arm: `{"arm":"L1","apply":[…]}` with two `edit` steps on the new migration — step 1 deletes the lock line in `mark_compute_job_done` (`nth` 1, `occurrences` MEASURED — 1 at this task, 2 after Task 2, re-measure then), step 2 rewrites `IF NOT v_done_lock_anchored THEN` to `IF FALSE AND NOT v_done_lock_anchored THEN` (`occurrences` 1, measured with `--parse-only`; a value other than 1 means the anchor text leaked into the header or another anchor — fix the source, never raise `occurrences` or add `nth`).

    Step 5 — register the LANE-ONLY site. In `ci-anti-skip-gate.contract.test.ts`, add a `LANE_ONLY_SITES` entry for `test_mark_rpc_bridge_advisory_lock.sql` with `object: "dblink"`, the fixture path, and a `why` (dblink backends need committed seeds and trust-auth superuser loopback, which the local Supabase stack does not give; the extension is not in the schema of record). Place it FIRST in the array: `deriveLaneOnlySites` sorts by filename and the test compares with `toEqual`.

    Step 6 — observe GREEN, then RED, then GREEN (criterion 2 for done). Run LANE CMD: GREEN. Take a byte backup of the migration into a per-agent-named scratch file (`mktemp`), delete the done lock line AND disable its anchor (the same two edits as the L1 twin), run LANE CMD: the FIRST `TEST FAILED (` line must name `L1`, and the lane must NOT fail at apply time. Restore from the backup, `cmp` backup vs file (exit 0), run LANE CMD: GREEN. Paste all three tails into the SUMMARY. Commit (migration, fixture, gate, contract test). Never stage `.planning/milestone.lock` or `.planning/state.json`.
  </action>
  <verify>
    <automated>GATE=supabase/tests/test_mark_rpc_bridge_advisory_lock.sql; APPLY=$(node -e 'const s=require("fs").readFileSync(process.argv[1],"utf8");const l=s.split("\n").find(x=>x.startsWith("-- RED-UNDER-SETUP:"));process.stdout.write(JSON.parse(l.slice(19)).apply.join(" "))' "$GATE"); WD=$(mktemp -d); bash scripts/pg-lane/run.sh --workdir "$WD" --apply $APPLY --gate "$GATE" > "$WD.log" 2>&1; RC=$?; tail -40 "$WD.log"; echo "lane rc=$RC"; exit $RC</automated>
    <fails_when>`lane rc=` is non-zero, or the log tail contains `TEST FAILED (` or a migration-slug RAISE from the DO $verify$ block, or `could not open extension control file` naming dblink</fails_when>
  </verify>
  <acceptance_criteria>
    - Shorthand in these criteria: `<migration>` = `supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql`, `<gate>` = `supabase/tests/test_mark_rpc_bridge_advisory_lock.sql`.
    - `grep -c "PERFORM pg_advisory_xact_lock(hashtext('mark_compute_job_bridge'), hashtext(v_strategy_id::text));" supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql` prints `1` at this task (the header does not repeat it).
    - `grep -cE '^(BEGIN|COMMIT);' supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql` prints `0`.
    - `grep -c "SET LOCAL lock_timeout = '3s';" <migration>` prints `1`.
    - `diff <(sed -n '/^CREATE OR REPLACE FUNCTION mark_compute_job_done(/,/^\$\$;/p' supabase/migrations/20260603120000_compute_jobs_rpc_clear_error_and_gin_fanin.sql) <(sed -n '/^CREATE OR REPLACE FUNCTION mark_compute_job_done(/,/^\$\$;/p' supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql)` prints exactly one `>` line (the lock line) and no `<` line.
    - `grep -c '^-- LANE-ONLY:' supabase/tests/test_mark_rpc_bridge_advisory_lock.sql` prints `1`; `grep -c '^-- RED-UNDER-SETUP:' <gate>` prints `1`; `grep -cE '^(BEGIN|ROLLBACK);' <gate>` prints `0`.
    - The first non-comment statement of the gate is `CREATE EXTENSION IF NOT EXISTS dblink;`.
    - `grep -c 'TEST FAILED (' <gate>` is at least 2 and `grep -c 'ARM .* FAILED' <gate>` prints `0`.
    - `grep -c 'IF NOT v_done_lock_anchored THEN' <migration>` prints `1`, and `grep -c 'IF v_done_body !~' <migration>` prints `3` — the three carried-forward lines that open `IF v_done !~*` in the re-base source's STEP 3 (measured at planning: `grep -c 'IF v_done !~' supabase/migrations/20260603120000_compute_jobs_rpc_clear_error_and_gin_fanin.sql` prints `3`); the lock anchor does not use that opening.
    - SUMMARY carries three pasted lane tails: GREEN, RED whose first `TEST FAILED (` names `L1`, GREEN after restore, plus the `cmp` exit 0.
    - `npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` exits 0.
  </acceptance_criteria>
  <done>The done-RPC lock is proven end-to-end on the lane by two real backends: RED names L1 with the lock removed, GREEN with it restored, and the LANE-ONLY register accepts the new gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand — the lock in mark_compute_job_failed, arms L2/L3/L4 with layered twins, L2 observed RED then GREEN</name>
  <files>supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql, supabase/tests/test_mark_rpc_bridge_advisory_lock.sql</files>
  <reversibility rating="reversible">Every edit lands on a feature branch and a throwaway lane; the one-way door is the merge (auto-apply to TEST then PROD, D-18), which is the ship step, not this task.</reversibility>
  <read_first>
    - `supabase/migrations/20260529180000_fix_mark_compute_job_failed_error_kind_column.sql` (the full `mark_compute_job_failed` block, its REVOKE, and its three random-uuid behavioural probes).
    - The migration and gate as committed by Task 1.
    - RESEARCH Pattern 2 arm table (L2, L3, L4) and `scripts/mutation-runner/GRAMMAR.md` (`nth`, sequential multi-step apply).
  </read_first>
  <behavior>
    - L2: A holds an uncommitted `mark_compute_job_failed` on strategy S; B's `mark_compute_job_failed` on S has an ungranted `advisory` row. Reddens only when the failed lock line is removed (done/done in L1 is unaffected).
    - L3: in a done/done pair, B's ungranted advisory row has `classid` = masked namespace oid AND `objsubid = 2`. Reddens when both locks are rewritten to the single-key strategy hash (L1/L2 still see an advisory wait).
    - L4: while A holds an uncommitted done mark on S, a done mark on a DIFFERENT strategy S2 completes (`dblink_is_busy` returns 0 within the bounded poll and B has no ungranted row). Reddens when both lock keys are replaced by a constant (L3 still passes: namespace intact).
  </behavior>
  <action>
    Per D-01 and D-05, append to the migration (after the done block and its REVOKE, so done's lock line is occurrence 1 and failed's is occurrence 2) the latest `mark_compute_job_failed(UUID, TEXT, TEXT, UUID)` `CREATE OR REPLACE … $$;` block byte-for-byte, with the lock line inserted as the first statement inside its `IF v_strategy_id IS NOT NULL THEN` (D-03, D-24); re-issue its REVOKE verbatim; no COMMENT reissue. Extend the DO block: body variable `v_failed_body` (comment-stripped, NULL refused), the statement-shaped lock anchor as a dedicated BOOLEAN `v_failed_lock_anchored := v_failed_body ~ '…'` tested by ONE `IF NOT v_failed_lock_anchored THEN RAISE` (that text occurs exactly once in the file, the same mechanism as the done anchor), the carried-forward random-uuid probes from `20260529180000` (NULL token and `'bogus_kind'` raise `invalid_parameter_value`; an unknown job raises `no_data_found`; they seed nothing), `search_path` VALUE pin, SECURITY DEFINER, ACL re-convergence for `mark_compute_job_failed(uuid,text,text,uuid)`.

    In the gate, add committed seeds for L2, L3, L4 (one strategy per arm, two for L4) and three arm DO blocks in file order L1, L2, L3, L4, each disconnecting its dblink sessions before ending. L2 mirrors L1 with `mark_compute_job_failed` on both sides (a `'transient'` or `'permanent'` kind argument and a fixed error text; measure which keeps the lane deterministic). L3 checks only `classid` and `objsubid` of b's ungranted advisory row (NOT `objid`). L4: `a` holds an uncommitted done mark on S; send b's done mark on S2; bounded poll of `dblink_is_busy('b')`; `L4` raises if b is still busy with an ungranted row at the bound; consume the result; then commit `a`. Add `-SETUP` identities as in L1 for "B never waited on anything" (L2, L3). Twins (layered, D-15):
    - L2: delete the lock line with `nth` 2, then `IF NOT v_failed_lock_anchored THEN` becomes `IF FALSE AND NOT v_failed_lock_anchored THEN` (`occurrences` 1).
    - L3: replace the lock line with the single-key form `PERFORM pg_advisory_xact_lock(hashtext(v_strategy_id::text));` at `nth` 1 and again at the remaining occurrence (sequential semantics: step 2 sees step 1's output — measure), then disable BOTH lock anchors by the two exact finds `IF NOT v_done_lock_anchored THEN` and `IF NOT v_failed_lock_anchored THEN` (`occurrences` 1 each).
    - L4: replace `hashtext(v_strategy_id::text))` inside each lock line with a constant second key (for example `0)`), both occurrences, then disable BOTH lock anchors by the same two exact finds (`occurrences` 1 each).
    - Re-measure the L1 twin's `occurrences` (now 2 for the lock line) and update it.
    Measure every `occurrences` with `node scripts/mutation-runner/run.mjs --parse-only` (or the diagnostic `--file`), never by reasoning. Then run LANE CMD (GREEN) and the diagnostic `node scripts/mutation-runner/run.mjs --file supabase/tests/test_mark_rpc_bridge_advisory_lock.sql` (it never exits 0 by design; read its per-arm lines): each of L1, L2, L3, L4 must report RED with identity ok and first failure = its own arm. If an arm's first failure is another arm, reorder or tighten the arm's assertion — never add a `neuter` to hide a wrong-first-failure without recording why in the gate header.

    Observe criterion 2 for failed: byte backup (per-agent `mktemp`), delete the failed lock line AND disable its anchor, LANE CMD -> first `TEST FAILED (` names `L2` and the apply succeeds; restore, `cmp` exit 0, LANE CMD GREEN. Paste into the SUMMARY. Commit.
  </action>
  <verify>
    <automated>GATE=supabase/tests/test_mark_rpc_bridge_advisory_lock.sql; APPLY=$(node -e 'const s=require("fs").readFileSync(process.argv[1],"utf8");const l=s.split("\n").find(x=>x.startsWith("-- RED-UNDER-SETUP:"));process.stdout.write(JSON.parse(l.slice(19)).apply.join(" "))' "$GATE"); WD=$(mktemp -d); bash scripts/pg-lane/run.sh --workdir "$WD" --apply $APPLY --gate "$GATE" > "$WD.log" 2>&1; RC=$?; tail -40 "$WD.log"; echo "lane rc=$RC"; exit $RC</automated>
    <fails_when>`lane rc=` is non-zero, or the tail contains `TEST FAILED (` or a migration-slug RAISE, or the run hangs past the harness timeout (an L4 poll without a bound)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "PERFORM pg_advisory_xact_lock(hashtext('mark_compute_job_bridge'), hashtext(v_strategy_id::text));" <migration>` prints `2`.
    - The `diff <(sed -n '/^CREATE OR REPLACE FUNCTION mark_compute_job_failed(/,/^\$\$;/p' supabase/migrations/20260529180000_fix_mark_compute_job_failed_error_kind_column.sql) <(sed -n '/^CREATE OR REPLACE FUNCTION mark_compute_job_failed(/,/^\$\$;/p' <migration>)` output has exactly one `>` line and no `<` line; the done diff from Task 1 is unchanged.
    - `grep -c 'RED-UNDER-M:' <gate>` prints `4` and the arms named are exactly L1, L2, L3, L4.
    - `grep -c 'IF NOT v_done_lock_anchored THEN' <migration>` prints `1` and `grep -c 'IF NOT v_failed_lock_anchored THEN' <migration>` prints `1`; `node scripts/mutation-runner/run.mjs --parse-only` reports no `occurrence-mismatch` for the gate.
    - The diagnostic `--file` output reports each of L1-L4 RED with its own identity first (pasted in SUMMARY).
    - SUMMARY carries GREEN / RED naming `L2` / GREEN-after-restore tails and `cmp` exit 0.
  </acceptance_criteria>
  <done>Both RPCs carry the lock in one migration; four counted arms each redden first under their own production mutation, and removing either lock line is observed RED on the lane.</done>
</task>

<task type="auto">
  <name>Task 3: Static proofs — lint clean, namespace grep, re-apply, contract register green</name>
  <files>supabase/tests/test_mark_rpc_bridge_advisory_lock.sql, supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql</files>
  <read_first>
    - `scripts/lint-sql-gates.mjs` (its seven rules; `--self-test`) and `scripts/lint-app-guc.mjs`.
    - The migration and gate as committed by Task 2.
  </read_first>
  <action>
    Run and paste into the SUMMARY: (1) `node scripts/lint-sql-gates.mjs --self-test` then `node scripts/lint-sql-gates.mjs` (0 findings; note the new `scanned N file` count for plan 03, READ off the run); (2) `node scripts/lint-app-guc.mjs --self-test` then `node scripts/lint-app-guc.mjs` (0 findings; `current_setting('port')` is not an `app.` GUC); (3) the D-02 grep of every two-key call site, `grep -rnE "pg_advisory_xact_lock\(\s*hashtext\('[a-z_]+'\)\s*," supabase/migrations`, showing only `admin_role_mutate` and `mark_compute_job_bridge`; (4) a re-apply: LANE CMD with the migration listed twice at the end of the apply list (proves CREATE OR REPLACE plus the DO block are re-runnable) — run it as an ad-hoc command, do not commit a doubled list; (5) the full contract test `npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts`. If any finding requires an edit, fix the ROOT CAUSE in the gate or migration (never widen an allowlist, add a waiver, or relax a detector) and re-run Task 2's lane and `--file` checks. Commit only if files changed.
  </action>
  <verify>
    <automated>node scripts/lint-sql-gates.mjs --self-test && node scripts/lint-sql-gates.mjs && node scripts/lint-app-guc.mjs --self-test && node scripts/lint-app-guc.mjs && npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts</automated>
    <fails_when>any command exits non-zero; lint output names `test_mark_rpc_bridge_advisory_lock.sql` in a finding; vitest reports a failed test in the LANE-ONLY register describe block</fails_when>
  </verify>
  <acceptance_criteria>
    - `node scripts/lint-sql-gates.mjs` exits 0 and its output contains `scanned` with a count one higher than before this plan (value read off the run and recorded).
    - The two-key grep output lists exactly two namespaces: `admin_role_mutate` and `mark_compute_job_bridge`.
    - The doubled-apply lane run exits 0.
    - The contract test file passes in full.
  </acceptance_criteria>
  <done>The gate and migration pass every static lint, the namespace is proven unique, the migration re-applies, and the LANE-ONLY register holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker (service_role) -> SECURITY DEFINER mark RPCs | the only callers; anon/authenticated must never reach them |
| migration merge -> TEST -> PROD | auto-apply on merge, no human stop (D-18) |
| gate -> throwaway pg-lane | dblink backends on a loopback trust-auth cluster that never leaves the runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5.2-01 | Tampering | full-body re-base of both RPCs | high | mitigate | re-grep latest definition (Step 0); per-RPC diff shows one added line; carried-forward anchors for both RPCs in the DO block |
| T-164.5.2-02 | Elevation of Privilege | CREATE OR REPLACE ACL drift | high | mitigate | REVOKE re-issued verbatim; DO block asserts anon/authenticated lack EXECUTE and `_assert_no_public_execute` passes; SECURITY DEFINER and the search_path VALUE asserted |
| T-164.5.2-03 | Denial of Service | a new deadlock path (40P01) on every job transition | high | mitigate | lock placed LAST inside the existing guard (D-24); lock order recorded in the header; no lock held while waiting on a row lock |
| T-164.5.2-04 | Denial of Service | namespace collision / cross-subsystem blocking | medium | mitigate | two-key form (objsubid 2), apply-time inequality vs `admin_role_mutate`, grep of every two-key site pasted |
| T-164.5.2-05 | Repudiation | half-applied lock discipline reads as protection | high | mitigate | one migration, both RPCs, one anchor each, L1 and L2 each redden on their own RPC |
| T-164.5.2-06 | Tampering | a vacuous concurrency gate (passes with the lock removed) | high | mitigate | observable is the ungranted ADVISORY row, not "B waits" (D-14 CORRECTED); RED observed for each lock line; no static lock precheck in SETUP |
| T-164.5.2-07 | Denial of Service | data-reading self-verify refuses on TEST and blocks PROD | high | mitigate | catalog-only DO block plus random-uuid probes; no row-content reads |
| T-164.5.2-08 | Information Disclosure | dblink in a committed file | low | accept | dblink is created only on the throwaway lane via a lane-only fixture; never in a migration; LANE-ONLY keeps it off sql-tests |
| T-164.5.2-SC | Tampering | package installs | low | accept | no npm/pip/cargo package is installed; dblink is PostgreSQL contrib created by `CREATE EXTENSION` on the throwaway lane only |
</threat_model>

<verification>
- LANE CMD exits 0 on the final gate; the diagnostic `--file` run shows L1-L4 each RED with its own identity first.
- RED-then-GREEN pasted for L1 (done lock removed) and L2 (failed lock removed), each with a byte-backup restore and `cmp` exit 0.
- Per-RPC diffs pasted, one added line each.
- `node scripts/lint-sql-gates.mjs`, `node scripts/lint-app-guc.mjs` and the contract test green.
- `env -u TMPDIR npx tsx scripts/check-planning-hygiene.ts` exits 0 after the SUMMARY is written (no home paths or usernames in the SUMMARY).
</verification>

<success_criteria>
Both mark RPCs take the two-key per-strategy lock inside their existing strategy guard, in one
migration re-based on the latest bodies; a LANE-ONLY two-backend gate proves the lock by which lock
B waits on, and is observed RED with each lock line removed and GREEN with it restored.
</success_criteria>

<output>
Create `.planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-01-SUMMARY.md` when done
</output>
