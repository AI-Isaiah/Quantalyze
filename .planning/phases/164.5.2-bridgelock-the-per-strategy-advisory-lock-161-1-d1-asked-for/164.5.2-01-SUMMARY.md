---
phase: 164.5.2
plan: 01
subsystem: database
tags: [postgres, plpgsql, advisory-lock, compute-jobs, dblink, pg-lane, mutation-runner]

requires:
  - phase: 164.9.1
    provides: "the 10-param enqueue FOR SHARE parent lock whose diamond this lock must not worsen (D-12 item 3)"
provides:
  - "migration 20260926120000: both terminal mark RPCs take pg_advisory_xact_lock(hashtext('mark_compute_job_bridge'), hashtext(strategy id)) first inside their strategy guard, before the bridge"
  - "LANE-ONLY two-backend dblink gate test_mark_rpc_bridge_advisory_lock.sql, arms L1-L4, each with a layered RED-UNDER-M twin that bites"
  - "lane fixture 36-fixture-dblink.sql"
  - "LANE_ONLY_SITES register entry for the new gate"
affects: [164.5.2-02, 164.5.2-03, 164.5.2.1]

actuals:
  tokens: 14690
  tasks: 3
  commits: 3
plan_head_before: e9362d900fe4e52b4e520801b53722fed5e3419b

tech-stack:
  added: []
  patterns:
    - "concurrency gate observes WHICH lock a second backend waits on (pg_locks, masked-oid classid, objsubid), never WHETHER it waits"
    - "lock anchor in a migration DO block is a dedicated boolean tested by one IF NOT line, so a twin can stand it down with a unique find"

key-files:
  created:
    - supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql
    - supabase/tests/test_mark_rpc_bridge_advisory_lock.sql
    - scripts/pg-lane/fixtures/36-fixture-dblink.sql
  modified:
    - src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts

key-decisions:
  - "Kept the migration's file-level SET LOCAL search_path from the failed-RPC re-base source, beside SET LOCAL lock_timeout; both are WARNING no-ops on the autocommit lane, as for every other migration on it."
  - "No service_role EXECUTE assertion in the DO block: the lane apply list has no default-privilege fixture, so that arm would be red on the lane for a reason unrelated to the migration."
  - "L2 marks with error_kind 'transient' (failed_retry); deterministic on the lane."
  - "L4 consumes b's result before committing a, since b must already have finished for the arm to pass."

patterns-established:
  - "Committed seeds plus a session TEMP table carry ids across DO blocks for dblink-driven gates."

requirements-completed: [161.1-D1, DEC-4]

coverage:
  - id: D1
    description: "mark_compute_job_done takes the per-strategy two-integer advisory lock; a second same-strategy done mark waits on it (L1)"
    requirement: "161.1-D1"
    verification:
      - kind: integration
        ref: "bash scripts/pg-lane/run.sh --apply <RED-UNDER-SETUP list> --gate supabase/tests/test_mark_rpc_bridge_advisory_lock.sql (GREEN; RED naming L1 with the done lock removed; GREEN after restore)"
        status: pass
    human_judgment: false
  - id: D2
    description: "mark_compute_job_failed takes the same lock; a second same-strategy failed mark waits on it (L2)"
    requirement: "161.1-D1"
    verification:
      - kind: integration
        ref: "same lane command (GREEN; RED naming L2 with the failed lock removed; GREEN after restore)"
        status: pass
    human_judgment: false
  - id: D3
    description: "the lock is two-integer in its own namespace (L3) and does not block a different strategy (L4); every twin bites its own arm first"
    requirement: "DEC-4"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_mark_rpc_bridge_advisory_lock.sql (L1-L4 each RED identity ok)"
        status: pass
    human_judgment: false
  - id: D4
    description: "each re-based body equals its latest definition plus exactly the lock line; migration re-applies; lint clean; namespace unique; LANE-ONLY register holds"
    verification:
      - kind: other
        ref: "per-RPC diff, doubled-apply lane run, lint-sql-gates, lint-app-guc, two-key grep, npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts"
        status: pass
    human_judgment: false
  - id: D5
    description: "the CI sql-mutation runner loads dblink and the arms bite there"
    verification: []
    human_judgment: true
    rationale: "CI host facts (ubuntu postgresql-16 contrib) cannot be measured locally; plan 02 adds the probe and the PR's SHA-bound sql-mutation log is the evidence."

duration: 15min
completed: 2026-09-26
status: complete
---

# Phase 164.5.2 Plan 01: the per-strategy bridge lock Summary

**Both terminal mark RPCs now take `pg_advisory_xact_lock(hashtext('mark_compute_job_bridge'), hashtext(strategy id))` first inside their existing strategy guard, and a LANE-ONLY dblink gate proves a second same-strategy mark waits on exactly that lock: RED with either lock line removed, GREEN with it restored.**

## Performance

- **Duration:** 15 min (timed from the first write; orientation and reading before it are not counted)
- **Started:** 2026-09-26T13:40:01Z
- **Completed:** 2026-09-26T13:55:27Z
- **Tasks:** 3 (Task 3 changed no file, so it has no commit)
- **Files:** 3 created, 1 modified

## Accomplishments

- One migration (`20260926120000`) re-bases `mark_compute_job_done` (source `20260603120000`) and `mark_compute_job_failed` (source `20260529180000`) byte-for-byte. It adds one lock statement to each and re-issues each REVOKE verbatim. COMMENT ON FUNCTION is not re-issued.
- The catalog-only `DO $verify$` block contains:
  - a statement-shaped lock anchor per RPC over the comment-stripped body, each held in a dedicated boolean;
  - an apply-time check that the namespace hash differs from `admin_role_mutate`'s;
  - the four carried-forward `20260603120000` anchors and the three `20260529180000` random-uuid probes;
  - checks for SECURITY DEFINER, the exact `search_path` value, and the ACL (PUBLIC, anon, authenticated).
- The LANE-ONLY gate has four counted arms, L1-L4, one layered twin each. The diagnostic runner reports every arm RED, each naming its own arm first.

## Task Commits

1. **Task 1: tracer, done lock + L1** - `3984b8658` (feat)
2. **Task 2 RED: arms L2-L4 + twins** - `f4340cab3` (test)
3. **Task 2 GREEN: failed lock + DO block** - `8f0e85829` (feat)
4. **Task 3: static proofs** - no commit (read-and-run only, nothing changed)

## Step 0 evidence (pasted)

```
verify-plan-anchors: scanned 1 plan file(s); claims: 13 checked; OK: 1 plan file(s), no stale claims. rc=0
ls supabase/migrations | tail (excluding the down/ dir):
  20260924233749_allocator_sync_restore_inflight_prefetch.sql
  20260925071300_bridge_outcomes_invariant_comments.sql
open-PR file scan (47 file rows, no PR at the 100-file cap): 0 hits for supabase/migrations/2026092[6-9]* and 36-fixture-*
latest mark_compute_job_done:   supabase/migrations/20260603120000_compute_jobs_rpc_clear_error_and_gin_fanin.sql
latest mark_compute_job_failed: supabase/migrations/20260529180000_fix_mark_compute_job_failed_error_kind_column.sql
ALTER FUNCTION mark_compute_job_(done|failed): no output (grep rc=1)
```
No newer definition exists, so both re-base sources are the ones the plan expected.

## Per-RPC body diffs (D-05)

```
$ diff <(sed -n '/^CREATE OR REPLACE FUNCTION mark_compute_job_done(/,/^\$\$;/p' .../20260603120000_...sql) <(same range of 20260926120000)
94a95
>     PERFORM pg_advisory_xact_lock(hashtext('mark_compute_job_bridge'), hashtext(v_strategy_id::text));

$ diff <(sed -n '/^CREATE OR REPLACE FUNCTION mark_compute_job_failed(/,/^\$\$;/p' .../20260529180000_...sql) <(same range of 20260926120000)
91a92
>     PERFORM pg_advisory_xact_lock(hashtext('mark_compute_job_bridge'), hashtext(v_strategy_id::text));
```
Each diff adds one line and removes none.

## Fail-then-pass evidence (161.1-D1 criterion 2)

Every run below is the plan's LANE CMD on a throwaway loopback pg-lane. For each neuter, the migration was byte-backed-up with `cp` to a per-agent scratch file. The lock line was then deleted and its anchor rewritten to `IF FALSE AND NOT v_<rpc>_lock_anchored THEN`. After the RED run, the file was restored with `cp`, and the restore was checked two ways: `cmp` exit 0, and a grep for the lock line.

### Done lock (L1), Task 1

GREEN before:
```
CREATE EXTENSION
CREATE TABLE
DO
DO
lane rc=0
```
RED, done lock removed and its anchor stood down. The apply succeeded (the migration's NOTICE printed), and the first failure names L1:
```
psql:supabase/tests/test_mark_rpc_bridge_advisory_lock.sql:205: ERROR:  P0001: TEST FAILED (L1): a second mark_compute_job_done on the same strategy is waiting, but NOT on an advisory lock. Two concurrent terminal done marks on one strategy are not serialized before the bridge, so both bridge runs read compute_jobs at READ COMMITTED while the other is changing it (161.1-D1).
CONTEXT:  PL/pgSQL function inline_code_block line 49 at RAISE
LOCATION:  exec_stmt_raise, pl_exec.c:3911
lane rc=3
```
Restore: `cmp exit=0`. The lock line is present once (Task 1 has one RPC), the anchor line once, and `IF FALSE AND` zero times. GREEN after:
```
CREATE EXTENSION
CREATE TABLE
DO
DO
lane rc=0
```

### Failed lock (L2), Task 2

GREEN before (all four arm DO blocks ran):
```
CREATE EXTENSION
CREATE TABLE
DO
DO
DO
DO
DO
lane rc=0
```
RED, failed lock (occurrence 2) removed and its anchor stood down. The apply succeeded (the both-RPC NOTICE printed once), there is exactly one `TEST FAILED (` line, and it names L2:
```
psql:supabase/tests/test_mark_rpc_bridge_advisory_lock.sql:304: ERROR:  P0001: TEST FAILED (L2): a second mark_compute_job_failed on the same strategy is waiting, but NOT on an advisory lock. Two concurrent terminal failure marks on one strategy are not serialized before the bridge, so both bridge runs read compute_jobs at READ COMMITTED while the other is changing it, and the failure path is the one that decides whether the user sees failed or computing (161.1-D1).
CONTEXT:  PL/pgSQL function inline_code_block line 49 at RAISE
LOCATION:  exec_stmt_raise, pl_exec.c:3911
lane rc=3
```
Restore: `cmp exit=0`. The lock line is present twice, the failed anchor once, and `IF FALSE AND` zero times. GREEN after: `DO` x5, `lane rc=0`.

The TDD RED for Task 2 came first, at commit `f4340cab3`, before the failed lock existed. Its first failure was the same `TEST FAILED (L2)` line, raised by the arm's own assertion and not by a SETUP check, with `lane rc=3`. The GSD `check tdd-red-evidence` verb parses TAP output, which the SQL lane does not emit, so the RED evidence is the lane line above rather than that verb's verdict.

### Twins, diagnostic runner (`--file`, never exits 0 by design)

```
  baseline  supabase/tests/test_mark_rpc_bridge_advisory_lock.sql — exit 0 (1.7s)
  arm L1                       exit   3  RED (identity ok)  (1.4s)
  arm L2                       exit   3  RED (identity ok)  (1.5s)
  arm L3                       exit   3  RED (identity ok)  (1.4s)
  arm L4                       exit   3  RED (identity ok)  (1.6s)
  restore   supabase/tests/test_mark_rpc_bridge_advisory_lock.sql — exit 0 (1.5s)
arms: 4/4/0   (executed/annotated/waived)
biting: 4
lane-invocations: 4   (... plus 1 baseline / 1 restore leg(s))
  file test_mark_rpc_bridge_advisory_lock.sql: sections 4 / judged 4 / annotated 4 / waived 0 / biting 4
No defects in the narrowed scope.
```
`--parse-only` reports `✅ No static defects`, with 4 prose / 4 twins for the gate and no occurrence mismatch.

## Task 3 static proofs

- `node scripts/lint-sql-gates.mjs --self-test`: `lint-sql-gates self-test OK: 7 rules, red+green each.`
- `node scripts/lint-sql-gates.mjs`, rc 0: `lint-sql-gates: scanned 78 file(s); 0 finding(s), 0 measure-fail(s), 0 allowlist error(s).` Before this plan the count was 77, measured as 77 `test_*.sql` files at `e9362d900`. **The new scanned count for plan 03 is 78.**
- `node scripts/lint-app-guc.mjs --self-test`, rc 0: OK, 4 finding kinds, red+green each.
- `node scripts/lint-app-guc.mjs`, rc 0: 304 files scanned, 5 annotated (lineage), `findings 0`.
- Two-key namespace grep (D-02). Exactly two namespaces:
  ```
  1 pg_advisory_xact_lock(hashtext('admin_role_mutate')
  2 pg_advisory_xact_lock(hashtext('mark_compute_job_bridge')
  ```
  The two `mark_compute_job_bridge` sites are the done and failed lock lines of `20260926120000`. The `admin_role_mutate` site is `20260530120000`.
- Doubled apply: LANE CMD with the migration listed twice at the end ran as an ad-hoc command; the committed list was not changed. Result: `lane rc=0`, and the both-RPC NOTICE printed twice, so the CREATE OR REPLACEs and the DO block re-run cleanly.
- `npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts`: 33 passed (33).

## Files Created/Modified

- `supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql`: both RPCs with the lock, the header (D-02 key space, D-04 lock order and no-new-cycle argument, unserialized bridge callers routed to 164.5.2.1, an empty VAC-04 section for plan 02), and the catalog-only `DO $verify$`.
- `supabase/tests/test_mark_rpc_bridge_advisory_lock.sql`: the LANE-ONLY two-backend gate, arms L1-L4 with layered twins.
- `scripts/pg-lane/fixtures/36-fixture-dblink.sql`: `CREATE EXTENSION IF NOT EXISTS dblink;` on the lane only.
- `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts`: `LANE_ONLY_SITES` entry, placed first in sorted order.

## Decisions Made

See `key-decisions` in the frontmatter. The layout follows the plan: done's lock line is occurrence 1 and failed's is occurrence 2. Each lock anchor is a dedicated boolean whose `IF NOT` line occurs exactly once. `grep -c 'IF v_done_body !~'` is 3.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] LANE CMD word-splitting under zsh**
- **Found during:** Task 1, first lane run.
- **Issue:** The harness shell is zsh, which does not word-split the unquoted `$APPLY`. run.sh therefore received the whole apply list as one path and failed with `apply file not found`.
- **Fix:** The identical LANE CMD was run under `bash` through a scratch wrapper. Nothing in the repo changed.
- **Verification:** All later lane runs applied every listed file.

**Total deviations:** 1 auto-fixed (blocking, environment only). **Impact:** none on the committed files.

## Issues Encountered

- The first full contract-test run had one timeout, `refuses a handoff whose DB_URL is not loopback`, at 5000 ms, while machine load was 26-43. That test is not in the LANE-ONLY describe block. The file then passed 33/33 twice.
- **Left red for plan 03 by design: the census pins.** This plan adds one annotated gate file with 4 arms and 12 find-bearing steps, and plan 03 owns moving those pins. Measured failures:
  - `lint-sql-gates.test.ts`: `scanned 77 file`, actual 78.
  - `mutation-annotation-parser.test.ts`: `filesTotal` 77, actual 78; `armsSeen` 449, actual 453; `stepsSeen` 464, actual 476; plus the annotated-file by-name list.
  - `mutation-runner-floors.test.ts`: `RATCHET STALE: 51 of 78 … FILES_FLOOR is still 50`; ARMS_FLOOR 449 against 453 declared twins.

  All of these fail against the old numbers, which is expected. None was moved here, because the pins move by grep from one full runner run in plan 03.

## Known Stubs

- The VAC-04 ack section in the migration header has no `-- prod-body-ack:` lines yet. Plan 02 earns them from `--diff-bodies`, and until then VAC-04 is expected red on the PR.

## Threat Flags

None beyond the plan's register. dblink is created only by the lane fixture and the lane-only gate; no migration creates it.

## Next Phase Readiness

Ready for 164.5.2-02 (function snapshots, the two VAC-04 acks, the CI dblink probe). Plan 03 then moves the census pins listed above.

## Self-Check: PASSED

- FOUND: the three created files and the modified contract test.
- FOUND: commits `3984b8658`, `f4340cab3`, `8f0e85829` (`git rev-list --count e9362d900..HEAD` = 3 before this SUMMARY commit).
