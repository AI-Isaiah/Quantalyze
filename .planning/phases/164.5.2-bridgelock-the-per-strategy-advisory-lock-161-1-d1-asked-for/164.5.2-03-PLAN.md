---
phase: 164.5.2
plan: 03
type: execute
wave: 3
depends_on:
  - 164.5.2-02
files_modified:
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - src/__tests__/lint-sql-gates.test.ts
  - src/__tests__/gate-family-meta.test.ts
  - TODOS.md
autonomous: true
requirements:
  - 161.1-D1
  - DEC-4

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-16: a grep of every `RED-UNDER-M` twin in `supabase/tests` for the two replaced migrations (`20260603120000`, `20260529180000`) returns no file, pasted; no existing twin is shadowed by the new migration"
    - "D-17: `FILES_FLOOR` and `ARMS_FLOOR` in `scripts/mutation-runner/run.mjs` move to values READ OFF one full `node scripts/mutation-runner/run.mjs` run, never computed as N+1; the floors are separated in BOTH directions (measured+1 -> runner exit 1; stale-low -> runner exit 0 AND the floors vitest RED with `RATCHET STALE`; measured -> exit 0 with `✅ No defects` and the `arms:` and `lane-invocations:` tallies agreeing); `WAIVED_CEILING` stays 0; no waiver, allowlist widening or lowered floor"
    - "D-17: every other census pin moves in ONE commit to the measured value: floors-test GREEN_LOG lines, the new per-file GREEN_LOG row and `totalAnchored`; annotation-parser `armsSeen`, `stepsSeen`, needle count, `filesTotal` (both sites), `filesAnnotated` and the sorted by-name list; lint-sql-gates `scanned N file`; gate-family-meta `KNOWN_THRESHOLD_SITES`. The `ci.yml` `sql-tests` sentinel roster and `drift-check-scripts.test.ts` do NOT move, because the new gate declares no `ALL N ARMS EXECUTED` sentinel"
    - "D-19: both `DEC-4` entries in `TODOS.md` name Phase 164.5.2 as the phase that took the decision; `161.1-D1` is marked closed ONLY with a citation of the plan 01 RED-then-GREEN observation; the 164.6.7 TODOS entries and `docs/runbooks/ledger-refresh-go-live.md` are not touched"
    - "D-18: the three reviewers (migration-reviewer, rls-policy-auditor, silent-failure-hunter) run on the migration and the gate BEFORE merge, as part of the ship step; there is NO human PROD gate — merging `supabase/migrations/**` auto-applies to TEST, then PROD"
    - "D-22: this plan set touches neither `sync_strategy_analytics_status` nor the 164.6.7 residues (moved to Phase 164.5.2.1), and D-12's four 164.9.1 routings are founder-owned and unplanned here"
    - "D-23: the expected-RED PR checks are named before the PR opens: `baseline-content-drift` (already red on `main`; this phase adds the two mark RPC rows), VAC-04 (red only if PROD differs from the earned acks) and VAC-08 (red by construction until apply-on-merge); none is allowlisted"
    - "`npm run lint`, `npm run typecheck` and the census vitest set exit 0"
  artifacts:
    - path: "scripts/mutation-runner/run.mjs"
      provides: "FILES_FLOOR / ARMS_FLOOR at the measured values with a dated RE-DERIVED block quoting the run"
    - path: "TODOS.md"
      provides: "DEC-4 names Phase 164.5.2; 161.1-D1 closed with its evidence"
  key_links:
    - "a new annotated gate -> FILES_FLOOR, ARMS_FLOOR, and five census test files move together, from ONE run's output"
    - "the runner only catches a floor set too HIGH -> the stale-low direction is proven by `src/__tests__/mutation-runner-floors.test.ts` going RED"
  prohibitions:
    - "NEVER compute a census value by arithmetic; read it off a run or a failing test's actual value"
    - "NEVER raise WAIVED_CEILING, add a waiver, or widen any allowlist"
    - "NEVER stage `.planning/milestone.lock` or `.planning/state.json`"
---

<objective>
Move the mutation-runner corpus census for the new gate and close the DEC-4 / `161.1-D1`
bookkeeping, then state the pre-merge obligations the ship step carries.

Purpose: a new annotated gate file and four biting arms move several independent pins; missing one
leaves CI red for a reason unrelated to the lock, and a floor left stale-low lets a future regression
through. `161.1-D1` closes only on the evidence plan 01 recorded.

Output: floors and five census files at measured values; `TODOS.md` DEC-4 and `161.1-D1` updated.

**Scope notes:** CONTEXT D-06..D-10 moved to Phase 164.5.2.1 BRIDGERESIDUE (D-22); D-12's four
164.9.1 routings are FOUNDER-OWNED and not planned here — the phase cannot be closed as complete
while they stay routed to it (CONTEXT D-12 default).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-CONTEXT.md
@.planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-PATTERNS.md
@scripts/mutation-runner/run.mjs
@src/__tests__/mutation-runner-floors.test.ts
@src/__tests__/mutation-annotation-parser.test.ts
</context>

## Artifacts this phase produces

This plan produces:
- `scripts/mutation-runner/run.mjs` (modified: `FILES_FLOOR`, `ARMS_FLOOR` and their dated RE-DERIVED comment blocks)
- `src/__tests__/mutation-runner-floors.test.ts`, `src/__tests__/mutation-annotation-parser.test.ts`, `src/__tests__/lint-sql-gates.test.ts`, `src/__tests__/gate-family-meta.test.ts` (modified census pins)
- `TODOS.md` (modified: the two DEC-4 entries and `161.1-D1`)
- In `164.5.2-03-SUMMARY.md`: the D-16 grep, the three floor-separation runs, the final full-run tail, and the expected-RED check list for the PR body

<tasks>

<task type="auto">
  <name>Task 1: One full mutation run, floors moved and separated in both directions</name>
  <precondition>Plans 01 and 02 are committed; `bash scripts/pg-lane/run.sh --print-pgbin` exits 0; `node -e "require.resolve('vitest/config')"` exits 0 from the worktree root.</precondition>
  <files>scripts/mutation-runner/run.mjs</files>
  <read_first>
    - `scripts/mutation-runner/run.mjs`: the symbols `FILES_FLOOR`, `ARMS_FLOOR`, `WAIVED_CEILING` and the dated RE-DERIVED comment block above each floor (read by symbol: `grep -nE '^export const (FILES_FLOOR|ARMS_FLOOR|WAIVED_CEILING)' scripts/mutation-runner/run.mjs`).
    - `git show 762c03c81` (precedent: a new gate joining the corpus, every pin moved in one commit, the separation evidence in its message).
    - `src/__tests__/mutation-runner-floors.test.ts` (the `RATCHET STALE` check).
  </read_first>
  <action>
    Run `node scripts/verify-plan-anchors.mjs .planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-03-PLAN.md` and halt on non-zero. D-16 check, pasted: `grep -lE 'RED-UNDER-M.*(20260603120000|20260529180000)' supabase/tests/*.sql` (expected: no output) and `grep -lE 'RED-UNDER-M.*20260926120000' supabase/tests/*.sql` (expected: only the new gate). If the first grep returns a file, that twin is shadowed: stop and re-point it at the new migration per D-16 before continuing (a shadowed twin is a runner defect, not a floor adjustment).

    Per D-17, with the floors still at their pre-phase values, run the full corpus once, capturing output to a file: `node scripts/mutation-runner/run.mjs > <mktemp log> 2>&1; echo rc=$?`. Record the lines `scope:`, `coverage: files a/b`, `arms: x/y/z`, `biting:`, `lane-invocations:`, `lane-blocked:`, `lane-probe:`, `unreachable:`, the new gate's per-file line (`file test_mark_rpc_bridge_advisory_lock.sql: sections … / biting …`) and the verdict. This run is the stale-low RUNNER leg: expected exit 0 with the new gate biting. Then:
    (a) set `FILES_FLOOR` and `ARMS_FLOOR` to the MEASURED values (annotated files and biting arms, read off the run) and run `npx vitest run src/__tests__/mutation-runner-floors.test.ts` — it may still be red only on the census lines Task 2 moves; the `RATCHET STALE` line must be gone;
    (b) stale-low vitest leg: temporarily restore the pre-phase floor values, run the floors vitest and observe `RATCHET STALE` naming the measured values, restore the measured values (byte backup + `cmp`, never `git checkout --`);
    (c) too-high runner leg: temporarily set ARMS_FLOOR to measured+1, run the full corpus, observe exit 1 with the floor defect named, restore (byte backup + `cmp`);
    (d) final full run at the measured values: exit 0, `✅ No defects`, `arms:` second figure equal to `lane-invocations:`, `WAIVED_CEILING` still `0`.
    Write the dated RE-DERIVED comment block above each floor in the precedent's shape (date, Phase 164.5.2, the quoted run lines, the new gate's per-file line, "WAIVED_CEILING stays 0"). Paste all four legs' outputs into the SUMMARY. Commit together with Task 2 if Task 2's pins are needed for green vitest; otherwise commit now.
  </action>
  <verify>
    <automated>L=$(mktemp); node scripts/mutation-runner/run.mjs > "$L" 2>&1; RC=$?; grep -E '^(coverage|arms|biting|lane-invocations|lane-blocked|lane-probe|unreachable):|No defects|test_mark_rpc_bridge_advisory_lock' "$L"; echo "runner rc=$RC"; grep -nE '^export const (FILES_FLOOR|ARMS_FLOOR|WAIVED_CEILING)' scripts/mutation-runner/run.mjs; exit $RC</automated>
    <fails_when>`runner rc=` is non-zero; `No defects` is absent; the new gate's per-file line is absent or reports `biting` below the number of its counted arms; `WAIVED_CEILING` is not `0`</fails_when>
  </verify>
  <acceptance_criteria>
    - D-16 grep output pasted: no file for the two replaced migrations; only the new gate for the new migration.
    - The final run exits 0 with `✅ No defects`, and its `arms:` figure equals its `lane-invocations:` figure.
    - The new gate's per-file line reports its counted arms all biting and `waived 0`.
    - `FILES_FLOOR` and `ARMS_FLOOR` equal the final run's annotated-file and biting-arm counts; `WAIVED_CEILING = 0`.
    - SUMMARY carries the too-high leg (exit 1, floor defect named) and the stale-low vitest leg (`RATCHET STALE`).
  </acceptance_criteria>
  <done>The floors are pinned at a measured corpus that includes the new gate, and each direction of drift has been observed to fail.</done>
</task>

<task type="auto">
  <name>Task 2: Move the census pins in the four test files, then lint and typecheck</name>
  <files>src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, src/__tests__/lint-sql-gates.test.ts, src/__tests__/gate-family-meta.test.ts</files>
  <read_first>
    - The four test files at the pins PATTERNS section 5 names (search by text, never by line): `expect(totalAnchored).toBe(`, the GREEN_LOG `coverage: files`, `arms:`, `biting:`, `lane-invocations:`, `per-arm lane time`, `arm lane(s) spawned`, `reports ` lines and the `/^lane-invocations: … /m`, `/^arms: …/m`, `/^biting: … /m` replace regexes, and the per-file GREEN_LOG rows; `expect(armsSeen).toBe(`, `expect(stepsSeen).toBe(`, `expect(needles.length).toBe(`, `expect(corpus.filesTotal).toBe(` (two sites), `expect(corpus.filesAnnotated).toBe(`, the sorted `annotatedFiles` list; `/scanned N file/`; `KNOWN_THRESHOLD_SITES` rows for `FILES_FLOOR=` and `ARMS_FLOOR=`.
    - `git show 762c03c81 -- src/__tests__` (the dated-comment style for each moved pin).
    - Task 1's pasted run output.
  </read_first>
  <action>
    Find every pin by grepping the most recently added gate basename (`test_cron_runs_rls`) and the current floor values across `src/__tests__ scripts .github`, then move each to the value from Task 1's final run (per D-17), each with a dated one-line comment in the precedent's style naming Phase 164.5.2 and the new gate. Add the new per-file GREEN_LOG row for `test_mark_rpc_bridge_advisory_lock.sql` in sorted position, and add `"test_mark_rpc_bridge_advisory_lock.sql"` to the sorted `annotatedFiles` list with a dated comment. For `armsSeen`, `stepsSeen` and the needle count, RUN `npx vitest run src/__tests__/mutation-annotation-parser.test.ts` and read the actual values from its failure output (a `sql` step adds 0 to `stepsSeen`; layered twins add one per `find`/`anchor` step) — never reason them. Move `/scanned N file/` to the count `node scripts/lint-sql-gates.mjs` printed in plan 01. Update `KNOWN_THRESHOLD_SITES` strings to the new floor values. Confirm no sentinel roster move is needed: `grep -c 'ALL [0-9]* ARMS EXECUTED' supabase/tests/test_mark_rpc_bridge_advisory_lock.sql` prints `0`, so `ci.yml` and `drift-check-scripts.test.ts` stay untouched. Then run the census vitest set, `npm run lint`, `npm run typecheck`. Commit (with Task 1's `run.mjs` if not yet committed).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/lint-sql-gates.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/drift-check-scripts.test.ts src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts && npm run lint && npm run typecheck</automated>
    <fails_when>vitest reports any failed test (notably `RATCHET STALE`, a `toBe` mismatch on a census count, or a missing name in `annotatedFiles`); `npm run lint` or `npm run typecheck` exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'test_mark_rpc_bridge_advisory_lock.sql' src/__tests__/mutation-annotation-parser.test.ts` is at least 1 and `grep -c 'test_mark_rpc_bridge_advisory_lock.sql' src/__tests__/mutation-runner-floors.test.ts` is at least 1.
    - `src/__tests__/gate-family-meta.test.ts` carries `FILES_FLOOR=<measured>` and `ARMS_FLOOR=<measured>` matching `run.mjs`.
    - `git show --name-only --format= <plan-03 task-1 sha> <plan-03 task-2 sha>` does not list `.github/workflows/ci.yml` or `src/__tests__/drift-check-scripts.test.ts`.
    - The six vitest files, `npm run lint` and `npm run typecheck` exit 0.
  </acceptance_criteria>
  <done>Every corpus census agrees with the measured run; lint and typecheck are clean.</done>
</task>

<task type="auto">
  <name>Task 3: DEC-4 and 161.1-D1 closeout in TODOS.md</name>
  <files>TODOS.md</files>
  <reversibility rating="reversible">A ledger text edit; a closure recorded in error is re-opened by a later edit.</reversibility>
  <read_first>
    - `TODOS.md`: both lines found by `grep -n 'DEC-4' TODOS.md` (the phase-creation summary bullet and the 161.1 founder-decision block entry) and the `161.1-D1.` entry.
    - `164.5.2-01-SUMMARY.md` (the pasted RED-then-GREEN for L1 and L2).
  </read_first>
  <action>
    Per D-19: append to each DEC-4 entry a dated clause that the decision was taken by Phase 164.5.2 BRIDGELOCK (migration `20260926120000_mark_compute_job_bridge_advisory_lock.sql`, gate `supabase/tests/test_mark_rpc_bridge_advisory_lock.sql`). Mark `161.1-D1` CLOSED with a dated line citing: both RPCs locked in one migration; the LANE-ONLY two-backend gate observed RED with each lock line removed (L1, L2) and GREEN restored, per the plan 01 SUMMARY; and what stays open and where (the bridge's non-mark callers, routed to Phase 164.5.2.1). Only close it if the plan 01 SUMMARY actually carries both RED observations; otherwise leave it open and fail the task loudly. Do not edit any `164.6.7` entry or `docs/runbooks/ledger-refresh-go-live.md`. Run `env -u TMPDIR npx tsx scripts/check-planning-hygiene.ts`. Commit.
  </action>
  <verify>
    <automated>test "$(grep -A6 'DEC-4' TODOS.md | grep -c '164\.5\.2')" -ge 2 && awk '/^161\.1-D1\./,/^161\.1-D2\./' TODOS.md | grep -q 'CLOSED' && git diff --quiet origin/main...HEAD -- docs/runbooks/ledger-refresh-go-live.md && env -u TMPDIR npx tsx scripts/check-planning-hygiene.ts</automated>
    <fails_when>fewer than two lines within the DEC-4 entries name 164.5.2 (the `test -ge 2` exits 1); the `161.1-D1.` entry (up to `161.1-D2.`) carries no `CLOSED`; the runbook shows a diff against `origin/main`; planning hygiene exits non-zero naming a home path or username</fails_when>
  </verify>
  <acceptance_criteria>
    - Both `DEC-4` entries mention `164.5.2`.
    - The `161.1-D1.` entry carries a dated `CLOSED` line citing the L1 and L2 RED-then-GREEN observation.
    - `git show --stat --format= <task-3 commit sha>` lists only `TODOS.md` (plus the SUMMARY if committed with it).
  </acceptance_criteria>
  <done>The backlog records who took DEC-4 and closes `161.1-D1` on evidence, with the remaining unserialized callers pointed at Phase 164.5.2.1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutation-runner floors -> CI `sql-mutation` verdict | a floor set wrong lets a regression through or fails CI for noise |
| TODOS.md -> future planners | a closed entry is read as a fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5.2-11 | Tampering | floor moved by arithmetic, not measurement | medium | mitigate | values read off one full run; both drift directions observed failing; WAIVED_CEILING 0 asserted |
| T-164.5.2-12 | Repudiation | `161.1-D1` closed without evidence | medium | mitigate | closure conditional on the plan 01 SUMMARY's RED-then-GREEN pastes; the task fails loudly otherwise |
| T-164.5.2-13 | Information Disclosure | public repo: SUMMARY/TODOS carry paths or usernames | low | mitigate | `check-planning-hygiene` after every write |
| T-164.5.2-SC | Tampering | package installs | low | accept | this plan installs no npm/pip/cargo package |
</threat_model>

<verification>
Plan-level:
- Final full `node scripts/mutation-runner/run.mjs`: exit 0, `✅ No defects`, tallies agree, floors = measured, `WAIVED_CEILING = 0`.
- Census vitest set, `npm run lint`, `npm run typecheck` exit 0.
- `TODOS.md` DEC-4 names 164.5.2; `161.1-D1` closed on evidence.

Phase-level obligations carried by the ship step (not executor tasks, stated here so verification does not misread the PR):
- **Three reviewers BEFORE merge (D-18):** migration-reviewer, rls-policy-auditor and silent-failure-hunter review the migration and the gate; every finding is fixed before merge. There is NO human PROD gate: the `Production` environment has no required reviewer, so merging `supabase/migrations/**` auto-applies the migration to TEST (`apply-test`) and then to PROD. The merge IS the apply; no review may be scheduled after it.
- **Expected-RED PR checks (D-23), none allowlisted:** `baseline-content-drift` in `sql-gate-lint` (already red on `main`; this PR adds the `mark_compute_job_done` and `mark_compute_job_failed` rows; the precedent remedy is a baseline re-dump after the PROD apply); VAC-04 (green only if PROD's bodies equal the two earned `live` hashes — a red here is a real PROD drift to investigate, not noise); VAC-08 in `test-db-drift` (red by construction until apply-on-merge). Every other check must be green and bound to the head SHA (count the runs for the head SHA; no check may be absent).
- **Manual CI reading (VALIDATION manual-only row):** the PR's `sql-mutation` log shows the new dblink probe step passing and the new gate's four arms biting; if the probe fails, the fix is a contrib install in that step, committed on the PR before merge.
- The ship step writes the CHANGELOG entry and the VERSION bump in one commit per CLAUDE.md; never the CI skip token in any commit message or PR body.
</verification>

<success_criteria>
The corpus census agrees with a measured run that includes the new gate, both floor directions are
proven to fail, DEC-4 and `161.1-D1` are closed on evidence, and the PR carries its expected-RED
list and the pre-merge review obligation with no reliance on a PROD reviewer gate.
</success_criteria>

<output>
Create `.planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-03-SUMMARY.md` when done
</output>
