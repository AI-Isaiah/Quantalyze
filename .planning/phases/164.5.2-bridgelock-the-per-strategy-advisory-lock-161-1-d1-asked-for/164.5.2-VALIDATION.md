---
phase: "164.5.2"
slug: "bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-26"
---

# Phase 164.5.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Seeded from
> `164.5.2-RESEARCH.md` `## Validation Architecture`, narrowed to the lock (the residue rows moved
> to Phase 164.5.2.1 with CONTEXT D-22).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | plain PL/pgSQL gates (`RAISE EXCEPTION 'TEST FAILED (X): …'`) on the pg-lane, the mutation runner, vitest for census pins |
| **Config file** | `RED-UNDER-SETUP` inside each gate; `scripts/mutation-runner/run.mjs` |
| **Quick run command** | `bash scripts/pg-lane/run.sh --workdir <scratch> --apply <setup list> --gate supabase/tests/<gate>.sql` |
| **Full suite command** | `node scripts/mutation-runner/run.mjs`, then the census vitest set (`mutation-runner-floors`, `mutation-annotation-parser`, `lint-sql-gates`, `gate-family-meta`, `drift-check-scripts`, `contracts/ci-anti-skip-gate.contract`), `node scripts/lint-sql-gates.mjs` |
| **Estimated runtime** | lane gate ~seconds; full mutation corpus ~10 min |

---

## Sampling Rate

- **After every task commit:** a lane run of the touched gate plus the migration's own apply.
- **After every plan wave:** full `node scripts/mutation-runner/run.mjs` plus the census vitest set.
- **Before `/gsd-verify-work`:** full corpus exit 0 with `✅ No defects` and the two tallies agreeing; neuter → RED → restore pasted for the lock-line removal in EACH RPC.
- **Max feedback latency:** one lane run.

---

## Per-Task Verification Map

Filled by the planner (task IDs are assigned in the PLAN files). Requirement → check map:

| Requirement | Behavior | Test Type | Automated Command | Failing direction |
|-------------|----------|-----------|-------------------|-------------------|
| 161.1-D1 | same-strategy `done` marks serialize on the advisory lock | two-backend (dblink) | lane `--gate` of the new lock gate, arm L1 | B waits on no advisory lock in the namespace |
| 161.1-D1 | same-strategy `failed` marks serialize | two-backend | arm L2 | as above |
| 161.1-D1 / D-02 | two-key namespace, not the single-key strategy hash | two-backend | arm L3 | `objsubid = 1` or wrong `classid` |
| 161.1-D1 | a different strategy does not block | two-backend | arm L4 | the second-strategy mark waits in the namespace |
| D-01 / D-05 | both bodies = latest definition plus exactly the lock line | migration self-verify + diff | lane `--apply` of the lock migration; per-RPC `diff` pasted into the SUMMARY | the apply raises; the diff shows extra lines |
| D-15 / D-17 | twins bite; floors and census re-derived; no shadowed twin | mutation runner + vitest | `node scripts/mutation-runner/run.mjs` | exit 1 (`no-red`, `wrong-first-failure`, `floor`) |
| regression | existing bridge / mark-RPC gates stay green | lane + `sql-tests` (CI) | the existing gates on this surface | any `TEST FAILED` |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] The LANE-ONLY two-backend lock gate (dblink arms L1-L4), created by the plan that writes it.
- [ ] `dblink` on the CI `sql-mutation` runner — unverified (research A1); the plan confirms or installs it.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The PR's `sql-mutation` job loads `dblink` and the lock gate's arms bite in CI | 161.1-D1 | CI host facts cannot be measured locally | Read the PR head-SHA-bound `sql-mutation` log for the gate's arms |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency within one lane run
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
