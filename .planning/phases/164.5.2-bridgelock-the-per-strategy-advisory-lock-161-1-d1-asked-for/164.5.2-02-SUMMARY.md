---
phase: 164.5.2
plan: 02
subsystem: database
tags: [postgres, schema-snapshots, vac-04, vac-08, ci, dblink, pg-lane]

requires:
  - phase: 164.5.2-01
    provides: "migration 20260926120000 (both mark RPCs take the per-strategy bridge lock) with an empty VAC-04 section, and the LANE-ONLY dblink gate"
provides:
  - "regenerated snapshots mark_compute_job_done.sql and mark_compute_job_failed.sql, both sourced from 20260926120000"
  - "two earned `-- prod-body-ack:` lines in the migration header, one per RPC"
  - "a named dblink presence probe step in the sql-mutation CI job"
affects: [164.5.2-03]

actuals:
  tokens: 2534
  tasks: 2
  commits: 2
plan_head_before: 20093686d637b905eb2408131b9cd7a98386949a

tech-stack:
  added: []
  patterns:
    - "one VAC-04 pragma per changed function, each read off the `live` column of --diff-bodies against origin/main's snapshot"
    - "a CI probe step reads the lane's own --print-pgbin answer and pg_config, never a second path guess"

key-files:
  created: []
  modified:
    - supabase/schema/functions/mark_compute_job_done.sql
    - supabase/schema/functions/mark_compute_job_failed.sql
    - supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql
    - .github/workflows/ci.yml

key-decisions:
  - "Replaced plan 01's placeholder parenthetical in the VAC-04 section rather than appending under it, so the section does not contradict itself."
  - "The dblink probe collects both `ls` results before failing, so the one `::error::` line naming dblink, the gate and the remedy is always printed. A bare `ls` under `set -e` would exit with no error line."
  - "The probe checks `dblink.so`, the Linux suffix, matching the pg_cron step. The step comment records that a macOS PostgreSQL 16 names the module `dblink.dylib`, so the body is runner-only."

patterns-established:
  - "A CI probe step is calibrated locally by extracting its `run:` body and running it against a fake bindir, once without the artifacts (RED) and once with them (GREEN)."

requirements-completed: [161.1-D1]

coverage:
  - id: D1
    description: "both mark-RPC snapshots regenerated by the repo dumper, naming 20260926120000; no other snapshot moved"
    requirement: "161.1-D1"
    verification:
      - kind: other
        ref: "npm run schema:functions (only the two mark-RPC snapshots modified) && npm run schema:functions:check (exit 0, 121 functions current)"
        status: pass
    human_judgment: false
  - id: D2
    description: "two earned `-- prod-body-ack:` lines, each equal to the `live` column of --diff-bodies against origin/main for its function"
    requirement: "161.1-D1"
    verification:
      - kind: other
        ref: "node scripts/sql-body-normalize.mjs --diff-bodies <snapshot> <origin/main snapshot> per function; the Task 1 <verify> counter (prod-body-ack lines: 2)"
        status: pass
    human_judgment: false
  - id: D3
    description: "the plan 01 lane gate stays GREEN after the header edit and no twin occurrence moved"
    verification:
      - kind: integration
        ref: "plan 01 LANE CMD (lane rc=0); node scripts/mutation-runner/run.mjs --parse-only (No static defects)"
        status: pass
    human_judgment: false
  - id: D4
    description: "the sql-mutation job carries a named dblink probe after the pg_cron step, as a pure insertion"
    verification:
      - kind: other
        ref: "actionlint .github/workflows/ci.yml (clean); npx vitest run test-restore-workflow-wiring, ci-anti-skip-gate.contract, drift-check-scripts (1042/1042); fake-bindir RED/GREEN legs of the extracted step body"
        status: pass
    human_judgment: false
  - id: D5
    description: "VAC-04 on the PR reports the same PROD hash as each ack, and the ubuntu runner has dblink (assumption A1)"
    verification: []
    human_judgment: true
    rationale: "Both are facts about PROD and about the CI host, and neither can be read from here: no remote DB reads are allowed and there is no ubuntu host. The PR's head-SHA-bound VAC-04 and sql-mutation logs are the evidence."

duration: 9min
completed: 2026-09-26
status: complete
---

# Phase 164.5.2 Plan 02: snapshots, VAC-04 acks and the dblink probe Summary

**Both mark-RPC snapshots were regenerated from `20260926120000`. The migration now carries two `prod-body-ack` lines, each read off `--diff-bodies`' `live` column against origin/main. The `sql-mutation` job gained a probe that fails with a line naming dblink, instead of an anonymous runner `baseline` defect.**

## Performance

- **Duration:** about 9 min, from the first write to the last task commit. Orientation is not counted.
- **Started:** 2026-09-26T14:03:35Z
- **Completed:** 2026-09-26T14:11:49Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- `npm run schema:functions` moved exactly two files: `mark_compute_job_done.sql` and `mark_compute_job_failed.sql`. Each now names `20260926120000_mark_compute_job_bridge_advisory_lock.sql` as its source. Each gains the one lock line, plus the migration's short neutral per-function banner. `schema:functions:check` exits 0.
- The migration header's VAC-04 section has the `20260924230827` shape: the derivation commands with `<scratch>`, one labelled ack per RPC, the earned-only-if-VAC-04-agrees paragraph, and the VAC-08-red-by-construction paragraph.
- The new step is `Probe - dblink contrib is present for the lane's PostgreSQL (Phase 164.5.2)`, placed directly after `Provision pg_cron for the lane's PostgreSQL (Phase 164.4.1)` in `sql-mutation`. The pg_cron step is untouched.

## Task Commits

1. **Task 1: regenerate both snapshots, earn the two VAC-04 acks.** `ca6d30fde` (chore)
2. **Task 2: dblink probe in sql-mutation.** `d32cf3d33` (ci)

## Evidence (pasted)

`verify-plan-anchors` on this plan: `claims: 6 checked`, `OK: 1 plan file(s), no stale claims.` (rc 0).

`--diff-bodies` rows. The second argument is `git show origin/main:supabase/schema/functions/<fn>.sql > <scratch>`, and origin/main = `ea4167a3f82a03306f29dcd2a10cbdc38768117b`. Columns: status, name, nargs, snapshot, live, differing lines.
```
DRIFT	mark_compute_job_done	2	2315eb47d0f99344679d04cdab6c1b0df6762864d23021907e81dfc687f183e7	d440576b7cbde00954e091d1e60136e79acdd43cf2c9471c3754cf9b27d3a6fb	1
DRIFT	mark_compute_job_failed	4	e40c6150ce7b53ecbf1aff6adaefafc2c2bb007de9961c29c487e7bd95adcaf1	ff74bc589774f1e7730b36879489f7d3c38b97bc3c6ba216ce0181f894716200	1
```
Column order was confirmed from `formatDiffRow` (`status, name, nargs, snapshotHash, candidateHash, hunks`), so `live` is field 5. As a cross-check, `--diff-bodies <origin/main snapshot> <origin/main snapshot>` prints `MATCH` with that same hash for each function. That confirms each ack is the normalized origin/main body.

The acks in the migration:
```
-- prod-body-ack: d440576b7cbde00954e091d1e60136e79acdd43cf2c9471c3754cf9b27d3a6fb
-- prod-body-ack: ff74bc589774f1e7730b36879489f7d3c38b97bc3c6ba216ce0181f894716200
```

`npm run schema:functions:check` (exit 0):
```
SQL function snapshot is current (121 functions).
DRIFT-05 gate (a): baseline 121 name(s) vs migration-replay 121 name(s) — agree, with 0 ratcheted disagreement(s) carried. ...
```

Twin safety. Each of the four `find` strings in plan 01's twins was counted with `grep -cF` before and after the header edit, and the counts did not change:
- the full lock statement: 2
- `IF NOT v_done_lock_anchored THEN`: 1
- `IF NOT v_failed_lock_anchored THEN`: 1
- `hashtext(v_strategy_id::text))`: 2

`--parse-only`: `✅ No static defects. Every prose marker has a twin and every needle still matches.`

Plan 01 LANE CMD after the edit: the migration's NOTICE printed, then `CREATE EXTENSION`, `CREATE TABLE`, `DO` x5, `lane rc=0`.

Task 1 `<verify>`: `prod-body-ack lines: 2`, then all three greps passed.

Task 2:
- `actionlint .github/workflows/ci.yml`: rc 0.
- Step-name `grep -c`: 1.
- Placement: the `sql-mutation` job starts at line 2023 and the next job at 3126. The pg_cron step is at 2271 and the new step at 2376.
- `git show d32cf3d33 -- .github/workflows/ci.yml`: 0 removed lines.
- `npx vitest run` over the three named files: `Test Files 3 passed (3)`, `Tests 1042 passed (1042)`.

Local calibration of the step body: the `run:` block was extracted from ci.yml and run under bash.
- A fake bindir with `pg_ctl`, `postgres` and a stub `pg_config`, and no dblink artifacts: both `ls` lines failed and the `::error::dblink is NOT present …` line printed with major 16, the gate path and the remedy. rc 1.
- The same bindir with both artifacts touched: rc 0.
- A fake bindir with no `postgres` binary: the lane's own refusal was reprinted under the `MEASURE_FAIL` line. rc 1.

## Files Created/Modified

- `supabase/schema/functions/mark_compute_job_done.sql`: generated. Source line is 20260926120000, and the body adds the lock line.
- `supabase/schema/functions/mark_compute_job_failed.sql`: generated, with the same changes.
- `supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql`: the VAC-04 section is filled in, and header comments are the only change.
- `.github/workflows/ci.yml`: one new step in `sql-mutation` (45 added lines, 0 removed).

## Decisions Made

See `key-decisions` in the frontmatter.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] LANE CMD under zsh**
- **Found during:** Task 1.
- **Issue:** Same as in plan 01: zsh does not word-split the unquoted `$APPLY`.
- **Fix:** The identical LANE CMD was run under `bash` from a scratch wrapper. No repo file changed.

**2. [Rule 2 - Missing critical] The probe's `::error::` line is always printed**
- **Found during:** Task 2.
- **Issue:** A bare `ls` under `set -e` would exit 1 without the `::error::` line the plan requires.
- **Fix:** Both `ls` results are collected first, and then the step prints the named error and runs `exit 1`.
- **Committed in:** `d32cf3d33`.

**Total deviations:** 2 (1 environment-only, 1 in-task correctness). **Impact:** none on scope.

## Issues Encountered

- **The probe's GREEN leg cannot run on this box.** The authoring box has PostgreSQL 16.13 (Homebrew) on macOS. There the module is `dblink.dylib`, not `dblink.so`, so the real-box leg printed the dblink error. This is a platform suffix difference, not a missing contrib: the lane gate loads dblink here and passes. The step comment records it. The GREEN leg was shown with a fake bindir instead.
- **I ran all 21 vitest files that read ci.yml. Six tests failed:**
  - Four are the census pins plan 03 owns and were known red before this plan: `lint-sql-gates.test.ts` `scanned 77 file` (actual 78), and in `mutation-runner-floors.test.ts` the parser count 453 vs 449, `RATCHET STALE: 51 of 78 … FILES_FLOOR is still 50`, and `ARMS_FLOOR is 449` vs 453 twins.
  - Two were 5 s timeouts at load average 10: the `lint-sql-gates` "each HALF tolerates" loop and the contract test's "refuses a handoff whose DB_URL is not loopback". Plan 01 saw the second one too. Each passed when rerun alone: 1/1 and 33/33.
- **Expected red on the PR by construction, and not allowlisted:**
  - VAC-04 stays red until the PR's PROD hashes are read and match the acks.
  - VAC-08 shows one DRIFT row per function until apply-on-merge.
  - `baseline-content-drift` gains two rows, per D-23.

## Known Stubs

None.

## User Setup Required

None.

## Next Phase Readiness

Ready for 164.5.2-03, which moves the census pins listed above. The first PR run settles two things:
- The `sql-mutation` log shows whether the dblink probe passes on ubuntu (A1).
- VAC-04 shows whether PROD's hashes match the two acks. On a mismatch, fold the difference into the migration and re-derive the acks. Never edit an ack to match the gate log.

## Self-Check: PASSED

- FOUND: all four modified files. `schema:functions:check` exits 0, and the migration has 2 ack lines.
- FOUND: commits `ca6d30fde` and `d32cf3d33`. `git rev-list --count 20093686d..HEAD` = 2 before this SUMMARY commit.
- Neither commit lists `.planning/milestone.lock` or `.planning/state.json`, and `.planning/STATE.md` was not staged.
