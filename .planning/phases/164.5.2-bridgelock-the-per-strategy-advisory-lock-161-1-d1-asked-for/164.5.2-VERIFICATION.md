---
phase: 164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for
verified: 2026-09-26T18:05:00Z
status: human_needed
score: 4/5 roadmap success criteria verified (criterion 5 awaits two reviewer records and the merge-time apply)
verified_at_sha: 37d992c1f639be86e7146fa832be67c7cde52b6f
drift_subjects:
  - supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql
  - supabase/tests/test_mark_rpc_bridge_advisory_lock.sql
  - scripts/pg-lane/fixtures/36-fixture-dblink.sql
  - src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - src/__tests__/lint-sql-gates.test.ts
  - src/__tests__/gate-family-meta.test.ts
  - supabase/schema/functions/mark_compute_job_done.sql
  - supabase/schema/functions/mark_compute_job_failed.sql
  - .github/workflows/ci.yml
behavior_unverified: 0
overrides_applied: 0
deferred:
  - truth: "IN-01: the deployed bridge's read-ORDER comment says neither mark RPC takes a per-strategy lock, which is now false for the two mark RPCs"
    addressed_in: "Phase 164.5.2.1"
    evidence: "164.5.2.1 BRIDGERESIDUE re-bases sync_strategy_analytics_status on its LATEST definition in ONE migration (ROADMAP goal), so the sentence is rewritten there"
human_verification:
  - test: "PRE-MERGE (owner: orchestrator / ship step). Confirm the migration-reviewer and rls-policy-auditor verdicts on 20260926120000 and the gate are RECORDED (a file or the PR body), beside 164.5.2-REVIEW-SFH.md"
    expected: "Both verdicts exist in writing, with no CRITICAL/HIGH open"
    why_human: "At 37d992c1f only the silent-failure-hunter review has an artifact. The other two are reported by the orchestrator but have none. Merging supabase/migrations/** auto-applies to TEST and then PROD, so the merge is the apply and every review must be recorded before it (CONTEXT D-18)"
  - test: "PRE-MERGE, PR CI bound to the head SHA (owner: ship step). Read the migration-drift-check VAC-04 step on the PR"
    expected: "VAC-04 reports the PROD hash d440576b... for mark_compute_job_done/2 and ff74bc58... for mark_compute_job_failed/4, equal to the two prod-body-ack lines. On a mismatch, fold the PROD difference into the migration and re-derive the acks; never edit an ack to match the log"
    why_human: "The acks were derived against origin/main's snapshot standing in for PROD. Reading PROD is forbidden here (no remote DB command)"
  - test: "PRE-MERGE, PR CI bound to the head SHA (owner: ship step). Read the sql-mutation job log"
    expected: "The step 'Probe - dblink contrib is present for the lane's PostgreSQL (Phase 164.5.2)' passes. The four arms L1-L4 of test_mark_rpc_bridge_advisory_lock.sql print RED (identity ok). The run ends with arms 453/453/0, biting 453, lane-invocations 453, 'No defects' and exit 0"
    why_human: "RESEARCH assumption A1 (dblink on the ubuntu runner) has never been executed. The full-corpus tally was produced locally by the executor and is not re-run here, and only the CI host can show it"
  - test: "PRE-MERGE, PR CI bound to the head SHA (owner: ship step). Count the checks for the head SHA and read each red"
    expected: "The only reds are the three named by D-23: baseline-content-drift (already red on main; this PR adds the two mark-RPC rows), VAC-08 in test-db-drift (red until apply-on-merge), and VAC-04 only if the item above disagrees. None is allowlisted"
    why_human: "An expected-red set is only honest when it is read off the SHA-bound board"
  - test: "POST-MERGE (owner: founder / land step). Read the supabase-migrate apply-test and apply logs for 20260926120000, then do the precedent baseline re-dump"
    expected: "Both applies print the 'mark-compute-job-bridge-lock: mark_compute_job_done and mark_compute_job_failed both take ...' NOTICE and raise nothing from the DO block. The baseline re-dump then clears the two new baseline-content-drift rows"
    why_human: "The apply happens only on merge, and no agent runs it"
  - test: "PHASE CLOSURE (owner: founder). Decide the four 164.9.1 routings (CONTEXT D-12) that the ROADMAP's '### Phase 164.5.2' section still lists as 'Routed here 2026-09-25': the stranded fan-in child after a failed_final parent, the 23505 from a second match_decisions delete, the FOR SHARE diamond 40P01, and the failed_retry+pending claim wedge"
    expected: "Each item is re-routed with /gsd-phase --edit, or a second planning pass is opened in this phase"
    why_human: "D-12 is FOUNDER-OWNED. They are outside the goal and criteria 1-5, so they are not a gap of this verdict. But CONTEXT D-12 says the phase cannot be closed as complete while they sit there unplanned, and item (4) is the loudest: when it fires, no job of any kind is claimed"
  - test: "BEFORE SHIP (owner: orchestrator). Confirm review WR-02 (a pre-existing concurrent fan-in 'lost release' in mark_compute_job_done that strands a two-parent child) has its routing entry"
    expected: "A TODOS.md entry, plus a ROADMAP routing line naming the owning phase"
    why_human: "The orchestrator reports it as being routed to another phase. At 37d992c1f neither TODOS.md nor the ROADMAP names it. It is a data-integrity item, so it must name a phase before ship. It is recorded as ROUTED, not as a gap of this phase: the body is carried byte-for-byte from 20260603120000, so this phase neither introduced nor was asked to close it"
---

# Phase 164.5.2: BRIDGELOCK Verification Report

**Phase goal:** Close `161.1-D1`. `mark_compute_job_done` and `mark_compute_job_failed` both fan into the bridge without serializing per strategy, so two concurrent terminal marks on one strategy can interleave. Add a per-strategy advisory lock to BOTH RPCs, with a real concurrency test observed RED without the lock and GREEN with it.
**Verified:** 2026-09-26, at HEAD `37d992c1f639be86e7146fa832be67c7cde52b6f` on `feat/164.5.2-bridgelock`
**Status:** human_needed
**Re-verification:** No. This is the initial verification.

## Goal Achievement

The goal is achieved in the code. Every criterion that can be proven before merge was re-measured here rather than read off a SUMMARY. What remains is PR-CI readings bound to the head SHA, two reviewer records, the merge-time apply, and a founder routing decision. None of them can be marked passed from this checkout.

### Observable Truths (ROADMAP success criteria, then CONTEXT decisions)

| # | Truth | Status | Evidence (re-measured here) |
|---|-------|--------|-----------------------------|
| SC1 | Both RPCs take a per-strategy advisory lock, and each body is re-based on its LATEST definition | ✓ VERIFIED | A re-grep of every `CREATE [OR REPLACE] FUNCTION` (quoted and schema-qualified forms included) names `20260603120000` as the newest `mark_compute_job_done` before this phase and `20260529180000` as the newest `mark_compute_job_failed`. `ALTER FUNCTION` has 0 hits. `diff` of each `CREATE OR REPLACE … $$;` block against the migration gives exactly one `>` line, the lock statement, and no `<` line (done `94a95`, failed `91a92`) |
| SC2 | Concurrency test RED with the lock removed, GREEN with it restored, on two genuinely separate sessions | ✓ VERIFIED | The gate drives two `dblink` backends (`lg_a`, `lg_b`), not a single client. Verifier run of `node scripts/mutation-runner/run.mjs --file supabase/tests/test_mark_rpc_bridge_advisory_lock.sql`: baseline exit 0. Arm L1 (the done lock deleted) and arm L2 (the failed lock deleted) each go RED with identity ok. Restore exit 0. The observable is WHICH lock b waits on (`locktype = 'advisory'`, plus classid/objsubid in L3), never WHETHER b waits, per the D-14 correction |
| SC3 | `RED-UNDER` annotations proven to bite; `FILES_FLOOR` and `ARMS_FLOOR` both move | ✓ VERIFIED | The same run: `arms: 4/4/0`, `biting: 4`, `lane-invocations: 4`. Read by symbol: `FILES_FLOOR = 51` (was 50), `ARMS_FLOOR = 453` (was 449), `WAIVED_CEILING = 0` (unchanged). `--parse-only`: no static defects. The census vitest (6 files, 1198 tests) is green, and it includes the `RATCHET STALE` stale-low check. ⚠️ The full-corpus `biting: 453` tally is pasted from the executor's run and was NOT re-run here. CI's `sql-mutation` owns it (human item) |
| SC4 | Arm failures print `TEST FAILED (X):`; a non-mutable arm is labelled INVARIANT, not counted | ✓ VERIFIED | Every raise in the gate is `TEST FAILED (L1..L4[-SETUP]):`. `grep 'ARM .* FAILED'` finds 0. The strategy-less no-lock behaviour is named as an INVARIANT in the gate header and is not an arm. The runner reports identity ok for all four |
| SC5 | Three reviewers before any apply. Since D-18 the merge IS the apply | ? UNCERTAIN | `164.5.2-REVIEW-SFH.md` (0 C/H/M, 4 LOW) and `164.5.2-REVIEW.md` (0 CRITICAL/HIGH) exist. The migration-reviewer and rls-policy-auditor verdicts are reported by the orchestrator, but at HEAD they have no artifact. The apply happens only on merge. Routed to human verification, never marked passed |
| D-01 | ONE migration changes BOTH RPCs | ✓ VERIFIED | Both `CREATE OR REPLACE` blocks and one `DO $verify$` are in `20260926120000`, and the lock statement occurs exactly twice |
| D-02 | Two-integer namespaced key, disjoint from the single-key strategy hash, with the namespace shown unique | ✓ VERIFIED | The two-integer grep over all migrations finds only `admin_role_mutate` (`20260530120000`) and `mark_compute_job_bridge` (×2). The DO block's arm (0) recomputes the inequality at apply time. L3 pins `classid = (hashtext(ns)::bigint & 4294967295)::oid` and `objsubid = 2` |
| D-03/D-24 | The lock is the first statement inside the existing non-NULL strategy guard, directly before the bridge PERFORM | ✓ VERIFIED | Read in both bodies. `c_lock_re` in the DO block pins guard → lock → bridge as consecutive statements over the comment-stripped body |
| D-04 | Lock order recorded; no new deadlock cycle | ✓ VERIFIED | The ordering is enforced by the code: the lock comes textually after the job-row UPDATE/`FOR UPDATE` and after the fan-in UPDATE, and it is the last statement before the bridge. The header records both paths and the FOR SHARE diamond argument. L1-L4 run both paths to completion with no 40P01. A missing deadlock class has no runnable positive test, so this rests on the code-enforced order plus the reviewer's trace |
| D-11 | Timestamp sorts after every migration | ✓ VERIFIED | `20260926120000` is last in `supabase/migrations` (the previous newest is `20260925071300`) |
| D-14 | LANE-ONLY gate, `CREATE EXTENSION IF NOT EXISTS dblink` first, committed seeds, no BEGIN/ROLLBACK | ✓ VERIFIED | Read in the file. The `-- LANE-ONLY:` marker is registered in `LANE_ONLY_SITES`, and the contract test is green |
| D-16 | No existing twin is shadowed | ✓ VERIFIED | Covered by the full census vitest and the `--parse-only` clean run. No `RED-UNDER-M` targets `20260603120000` or `20260529180000` (SUMMARY 03 grep, consistent with parse-only) |
| D-17 | Census pins moved by measurement; WAIVED_CEILING stays 0 | ✓ VERIFIED | The census vitest is green at 453/51/78. `lint-sql-gates` scans 78 files with 0 findings |
| D-19 | DEC-4 names 164.5.2; `161.1-D1` closed on the RED-then-GREEN evidence; 164.6.7 entries untouched | ✓ VERIFIED | The `TODOS.md` diff adds two `TAKEN 2026-09-26 by Phase 164.5.2` clauses and one `CLOSED` bullet under `161.1-D1` that names what stays open (routed to 164.5.2.1). No `164.6.7` line is touched |
| D-22 | `sync_strategy_analytics_status` not edited | ✓ VERIFIED | The migration defines only the two mark RPCs |
| D-23 | Snapshots regenerated; two earned acks; dblink CI probe | ✓ VERIFIED | `tsx scripts/dump-sql-functions.ts --check`: `SQL function snapshot is current (121 functions)`. The verifier re-derived `--diff-bodies` against the pre-phase snapshots, and the `live` column gives `d440576b…` / `ff74bc58…`, equal to the two `prod-body-ack` lines. The probe step is in the `sql-mutation` job (awk over job keys), with no `if:` and no `continue-on-error`, and it has a named `::error::` on each failure path |

**Score:** 4/5 roadmap criteria verified. 0 behaviour-unverified. SC5 is UNCERTAIN and routed to human verification. All 13 CONTEXT decisions checked are verified.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|--------------|----------|
| 1 | IN-01: the bridge's read-ORDER comment understates the protection now in place | Phase 164.5.2.1 | That phase re-bases `sync_strategy_analytics_status` on its latest definition in one migration |

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql` | ✓ VERIFIED | 514 lines. Two full-body re-bases plus the lock, REVOKEs re-issued, catalog-only `DO $verify$` (lock anchors, carried-forward anchors, random-uuid probes, SECDEF, the search_path VALUE, ACL), two acks |
| `supabase/tests/test_mark_rpc_bridge_advisory_lock.sql` | ✓ VERIFIED | 448 lines. LANE-ONLY, four counted arms with layered twins, bounded polls |
| `scripts/pg-lane/fixtures/36-fixture-dblink.sql` | ✓ VERIFIED | Lane-only `CREATE EXTENSION IF NOT EXISTS dblink`, listed after the core fixtures in the gate's `RED-UNDER-SETUP` apply list |
| `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` | ✓ VERIFIED | `LANE_ONLY_SITES` entry, and the contract test is green |
| `supabase/schema/functions/mark_compute_job_{done,failed}.sql` | ✓ VERIFIED | Generated, source is `20260926120000`, and each adds only the lock line plus the banner |
| `.github/workflows/ci.yml` dblink probe | ✓ VERIFIED (static) | Wired into `sql-mutation` after the pg_cron step. Its first ubuntu execution is a human item |
| `scripts/mutation-runner/run.mjs` floors + 4 census test files | ✓ VERIFIED | Read by symbol; census vitest green |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| Worker `_mark_done` / `_mark_failed` | the two RPCs | PostgREST `rpc`, one transaction per call (reviewer trace) | WIRED |
| each RPC | `pg_advisory_xact_lock(ns, strategy)` → `sync_strategy_analytics_status` | inside `IF v_strategy_id IS NOT NULL` | WIRED (DO-block anchor + L1/L2) |
| gate | migration | `RED-UNDER-SETUP` apply list, migration LAST | WIRED (baseline exit 0 on the lane) |
| gate | `sql-mutation` CI job | corpus scan + LANE-ONLY exclusion from `sql-tests` | WIRED (contract test green); CI execution is a human item |
| migration | VAC-04 | two `-- prod-body-ack:` lines, one per drifting function | WIRED locally; PROD agreement is a human item |

### Data-Flow Trace (Level 4)

Not applicable. The phase renders no dynamic data: it is a lock inside two SQL RPCs, and a gate over it.

### Behavioral Spot-Checks (run by the verifier)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Each arm bites its own identity; baseline and restore are green | `node scripts/mutation-runner/run.mjs --file supabase/tests/test_mark_rpc_bridge_advisory_lock.sql` (local pg-lane, under the shared lane lock) | see tail below; rc 2, as a diagnostic run is designed to return | ✓ PASS |
| Lint clean over the grown corpus | `node scripts/lint-sql-gates.mjs --self-test && node scripts/lint-sql-gates.mjs` | `self-test OK: 7 rules`; `scanned 78 file(s); 0 finding(s), 0 measure-fail(s), 0 allowlist error(s).` rc 0 | ✓ PASS |
| Floors by symbol | `grep -nE '^export const (FILES_FLOOR\|ARMS_FLOOR\|WAIVED_CEILING)' scripts/mutation-runner/run.mjs` | `FILES_FLOOR = 51`, `ARMS_FLOOR = 453`, `WAIVED_CEILING = 0` | ✓ PASS |
| Static twin grammar | `node scripts/mutation-runner/run.mjs --parse-only` | `✅ No static defects. Every prose marker has a twin and every needle still matches.` | ✓ PASS |
| Census pins | `npx vitest run` over the floors, annotation-parser, lint-sql-gates, gate-family-meta, drift-check-scripts and ci-anti-skip-gate.contract tests, `--test-timeout=30000` (node_modules linked from the main checkout for the run, then unlinked, and the worktree's own vite-cache dir put back) | `Test Files 6 passed (6)`, `Tests 1198 passed (1198)`, rc 0 | ✓ PASS |
| Snapshots current | `tsx scripts/dump-sql-functions.ts --check` | `SQL function snapshot is current (121 functions).` rc 0 | ✓ PASS |
| Acks earned | `node scripts/sql-body-normalize.mjs --diff-bodies <snapshot> <pre-phase snapshot at e9362d900>` per function | `DRIFT mark_compute_job_done 2 … d440576b… 1`, `DRIFT mark_compute_job_failed 4 … ff74bc58… 1`; the migration carries 2 matching ack lines | ✓ PASS |

Mutation-runner tail (verifier run):

```
scope: DIAGNOSTIC supabase/tests/test_mark_rpc_bridge_advisory_lock.sql
  baseline  supabase/tests/test_mark_rpc_bridge_advisory_lock.sql — exit 0 (2.8s)
  arm L1                       exit   3  RED (identity ok)  (2.6s)
  arm L2                       exit   3  RED (identity ok)  (2.9s)
  arm L3                       exit   3  RED (identity ok)  (2.7s)
  arm L4                       exit   3  RED (identity ok)  (2.8s)
  restore   supabase/tests/test_mark_rpc_bridge_advisory_lock.sql — exit 0 (2.7s)
coverage: files 51/78
lane-probe: pg_cron AVAILABLE — lane-blocked class is empty, as a hosting lane requires
arms: 4/4/0   (executed/annotated/waived)
biting: 4   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 4   (arm lanes actually spawned — tallied inside runLane, independent of the 4 the verdict loop counted; plus 1 baseline / 1 restore leg(s))
  file test_mark_rpc_bridge_advisory_lock.sql: sections 4 / judged 4 / annotated 4 / waived 0 / biting 4
⚠️ NARROWED DIAGNOSTIC RUN (--file/--arm): coverage floors NOT enforced.
   This mode never exits 0, so it can never be mistaken for a passing gate.
No defects in the narrowed scope.
```

The L1 and L2 twins are exactly "delete that RPC's lock statement and stand down its anchor". So their RED, followed by the restore leg's GREEN, is an independent re-observation of SC2 and does not rely on the executor's pasted lane tails.

### Probe Execution

SKIPPED. The phase declares no `probe-*.sh`, and none exists under `scripts/*/tests/`.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| `161.1-D1` | 01, 02, 03 | Per-strategy lock in both mark RPCs with a real concurrency test | ✓ SATISFIED (pre-merge) | SC1-SC4 above. Closed in `TODOS.md` on the RED-then-GREEN evidence |
| DEC-4 | 01, 03 | The lock in its own phase, with a real concurrency test | ✓ SATISFIED | Both DEC-4 lines name Phase 164.5.2 |

The requirements for this repo live in `TODOS.md`. `.planning/REQUIREMENTS.md` maps no ID to Phase 164.5.2, so there are no orphaned requirements.

### Anti-Patterns Found

A scan of the ADDED lines in the 12 changed non-ledger files for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` found 0 hits. `TODOS.md` is excluded, because it is the ledger.

### Known Limits (review round 1: MEDIUM/LOW only, no fix round, per the founder rule)

| ID | Severity | Limit |
|----|----------|-------|
| WR-01 | MEDIUM | The gate never exercises the FAILED RPC's lock key or namespace (L3/L4 drive done/done only), and no arm pairs done with failed on one strategy. A failed-only rewrite to the single-key form, another namespace, or a constant key would leave every arm green. It is caught instead by the migration's shared `c_lock_re` anchor on both bodies, so it is a mutation-coverage gap, not a shipped-behaviour gap. Optional L5 on a later touch of the gate |
| WR-02 | MEDIUM, pre-existing | A concurrent fan-in "lost release" in `mark_compute_job_done` can strand a two-parent child in `done_pending_children`. This phase neither introduced nor closes it (the body is carried byte-for-byte, and the lock is taken after the fan-in by design, per D-24). **ROUTED to another phase per the orchestrator, and not a gap of this phase.** At `37d992c1f` no TODOS/ROADMAP entry exists yet (human item) |
| IN-01 | LOW | The bridge's comment understates the new protection. Deferred to 164.5.2.1 |
| IN-02 | LOW | No `ALL 4 ARMS EXECUTED` completion sentinel. The arm count is guarded by the `ARMS_FLOOR` and parser pins only |
| SFH LOW-1 | LOW | The same as WR-01 from the silent-failure side: no failed/failed twin of L3's classid/objsubid predicate |
| SFH LOW-2 | LOW | The gate runs on a narrower schema than PROD (the apply list omits `20260603120000` and later compute_jobs migrations). The body is the full re-base, and VAC-04 plus snapshot-drift bind the PROD body |
| SFH LOW-3 | LOW | The namespace-distinctness check does not cover the CI mutex's two-integer key (classid 0). A collision would need `hashtext(ns) = 0`, so it is negligible |
| SFH LOW-4 | LOW | A stalled same-strategy holder surfaces as a generic `statement_timeout` (57014), not a named lock error. This is not a new wait class: the wait previously sat on the `strategy_analytics` row |

### Human Verification Required

The frontmatter `human_verification` block is the canonical list, with owner and reason for each item. In summary:

1. **Pre-merge, paper:** record the migration-reviewer and rls-policy-auditor verdicts (only the SFH review has an artifact at HEAD).
2. **Pre-merge, PR CI bound to the head SHA:** VAC-04 agrees with both acks.
3. **Pre-merge, PR CI bound to the head SHA:** in `sql-mutation`, the dblink probe passes on ubuntu (A1), L1-L4 bite, and the full run ends at 453/453/0 with exit 0.
4. **Pre-merge, PR CI bound to the head SHA:** the only reds are the D-23 expected set, read off a check count bound to the head SHA.
5. **Post-merge:** the TEST and PROD apply logs show the migration NOTICE and no DO-block raise, then the baseline re-dump.
6. **Phase closure, founder:** re-route or plan the four D-12 items still listed under `### Phase 164.5.2`.
7. **Before ship:** WR-02's routing entry exists and names a phase.

### Gaps Summary

There are no gaps. No truth FAILED, no artifact is missing or a stub, and no key link is broken. The lock is in both RPCs in one migration, re-based byte-for-byte on the latest bodies plus one line each. The two-backend gate was re-observed biting under both lock-removal twins and going green on restore. The census and floors move with the corpus. The status is `human_needed` rather than `passed`, because these can only be settled on the PR, at merge, or by the founder:

- the reviewer records;
- VAC-04 against PROD;
- dblink and the arms on the ubuntu runner;
- the auto-apply;
- the D-12 routing decision.

---

_Verified: 2026-09-26_
_Verifier: Claude (gsd-verifier)_
