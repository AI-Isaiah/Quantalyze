# Phase 164.5.2 — the three pre-merge migration reviews

Merging `supabase/migrations/**` auto-applies to TEST, then PROD, with no human gate, so these reviews are the last stop. All three reviewed `supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql` at `a8d36e7be`, read-only, with no database command.

## migration-reviewer — APPROVE (0 CRITICAL, 0 HIGH, 1 MEDIUM, 0 LOW)

- **Timestamp:** unique, and newer than origin/main's latest migration and every open branch's, including PR B's `20260925120000`.
- **Re-base:** the latest prior definitions are `mark_compute_job_done` in `20260603120000` and `mark_compute_job_failed` in `20260529180000`. Each body differs from its predecessor by exactly the added `pg_advisory_xact_lock(hashtext('mark_compute_job_bridge'), hashtext(v_strategy_id::text))`, inside `IF v_strategy_id IS NOT NULL`, directly before `sync_strategy_analytics_status`. There is no later definition and no `ALTER FUNCTION`.
- **SECURITY DEFINER hardening and ACL:** SECURITY DEFINER and `SET search_path = public, pg_catalog` are kept. The REVOKEs are word-for-word the prior ones, and no GRANT drifts.
- **Lock key:** every other strategy-keyed lock uses the single-key form. The only other two-key namespace, `admin_role_mutate`, is checked at apply time.
- **Lock scope and deadlock:** the lock is transaction-scoped. It adds no deadlock cycle, because the bridge writes only `strategy_analytics`, whose BEFORE-UPDATE triggers touch no other table.
- **Re-apply and TEST data:** re-applying is safe. The DO block reads catalogs only, so it cannot refuse on TEST's empty data.
- **MEDIUM (merge order):** PR B (`20260925120000`) must merge before this PR. Otherwise the backdated-migration policy blocks PR B. Do not allowlist it.

## rls-policy-auditor — PASS (0 CRITICAL, 0 HIGH, 0 MEDIUM, 0 LOW; leak scopes: none)

- **Re-base and ownership:** byte-faithful re-base. The owner is unchanged.
- **Grants:** `REVOKE ALL ... FROM PUBLIC, anon, authenticated` is re-issued and no grant is widened. `service_role` EXECUTE survives through CREATE OR REPLACE.
- **No probe oracle:** anon and authenticated cannot call either RPC. The lock key comes from the job row, and only after the claim-token fence has passed.
- **Two-layer gate:** unchanged. The ACL is service_role only, and the claim-token fence is mandatory.
- **Below the threshold:** a 32-bit `hashtext` collision would only serialize two unrelated strategies' marks. `pg_locks` visibility is unchanged from the existing single-key strategy locks.

## silent-failure-hunter — 0 CRITICAL, 0 HIGH, 0 MEDIUM, 4 LOW

See `164.5.2-REVIEW-SFH.md`.
