---
phase: 164.5.2
plan: 02
type: execute
wave: 2
depends_on:
  - 164.5.2-01
files_modified:
  - supabase/schema/functions/mark_compute_job_done.sql
  - supabase/schema/functions/mark_compute_job_failed.sql
  - supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - 161.1-D1

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-23: the committed snapshots `supabase/schema/functions/mark_compute_job_done.sql` and `mark_compute_job_failed.sql` are regenerated by the repo's own dumper and name the new migration as their source; `npm run schema:functions:check` exits 0, so `snapshot-drift` is GREEN on the PR"
    - "D-23: the migration header carries exactly one `-- prod-body-ack: <sha256>` line per changed function, each EARNED from the `live` column of `node scripts/sql-body-normalize.mjs --diff-bodies` against `origin/main`'s snapshot, never guessed or pasted from a gate log"
    - "D-23 / RESEARCH A1: the `sql-mutation` CI job carries a loud probe that the lane's PostgreSQL has dblink (`dblink.so` in pkglibdir and `dblink.control` in sharedir, from the same `pg_config` the lane boots), so an absent contrib fails naming dblink rather than surfacing as an anonymous runner `baseline` defect"
    - "D-21: the schema proof remains the pg-lane apply; this plan adds no `supabase db push` and runs nothing against a remote"
    - "The plan 01 gate stays GREEN on the lane after the header edit (no twin `occurrences` moved)"
  artifacts:
    - path: "supabase/schema/functions/mark_compute_job_done.sql"
      provides: "generated snapshot of the new done body"
    - path: "supabase/schema/functions/mark_compute_job_failed.sql"
      provides: "generated snapshot of the new failed body"
    - path: ".github/workflows/ci.yml"
      provides: "a dblink presence probe step in the `sql-mutation` job"
  key_links:
    - "the dumper copies the banner directly above each CREATE into the snapshot -> the banner stays short and neutral"
    - "VAC-04 greps the changed migration for `-- prod-body-ack: <live hash>` once per drifting function -> two lines, one per RPC"
    - "the ack lines live in the migration header -> they must not contain any twin `find` text, or plan 01's measured `occurrences` break"
  prohibitions:
    - "NEVER hand-edit a file under `supabase/schema/functions/`"
    - "NEVER install a contrib package pre-emptively in CI; the probe's first PR run is the measurement (mirrors the pg_cron step's own stance)"
    - "NEVER write the CI skip token in a commit message"
    - "NEVER stage `.planning/milestone.lock` or `.planning/state.json` (both carry unrelated local modifications in this checkout); stage each commit's files by explicit path, never `git add -A` or `git add .`"
---

<objective>
Ship the mechanics that make the plan 01 migration reviewable and CI-honest: regenerated function
snapshots, the two earned VAC-04 acknowledgements, and a loud dblink probe on the CI runner that
executes the LANE-ONLY gate.

Purpose: without regenerated snapshots `snapshot-drift` goes red for a reason that is not a defect;
without earned acks VAC-04 goes red; without the probe an absent dblink on ubuntu fails as an
unnamed runner defect (RESEARCH assumption A1 is unverified).

Output: two regenerated snapshots, two `-- prod-body-ack:` lines in the migration header, one new
probe step in `.github/workflows/ci.yml`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-CONTEXT.md
@.planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-PATTERNS.md
@supabase/migrations/20260924230827_fanin_initial_status_10param.sql
@scripts/sql-body-normalize.mjs
@scripts/dump-sql-functions.ts
@scripts/prod-body-drift-check.sh
</context>

## Artifacts this phase produces

This plan produces:
- `supabase/schema/functions/mark_compute_job_done.sql` and `supabase/schema/functions/mark_compute_job_failed.sql` (regenerated)
- `supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql` (modified: the VAC-04 ack section gains two `-- prod-body-ack:` lines)
- `.github/workflows/ci.yml` (modified: a new dblink probe step in the `sql-mutation` job)
- In `164.5.2-02-SUMMARY.md`: the `--diff-bodies` output rows the two hashes were read from, and the `--check` output

<tasks>

<task type="auto">
  <name>Task 1: Regenerate both snapshots and earn the two VAC-04 acks</name>
  <precondition>Plan 01 is committed on this branch; `node -e "require.resolve('vitest/config')"` exits 0 from the worktree root; `git rev-parse origin/main` resolves.</precondition>
  <files>supabase/schema/functions/mark_compute_job_done.sql, supabase/schema/functions/mark_compute_job_failed.sql, supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql</files>
  <reversibility rating="reversible">Generated snapshots and header comment lines on a feature branch; nothing is applied anywhere until the merge, which is the ship step.</reversibility>
  <read_first>
    - `supabase/migrations/20260924230827_fanin_initial_status_10param.sql` header, the VAC-04 ACKNOWLEDGEMENT section (the ack is the `live` column of `--diff-bodies`, not `--hash` of the snapshot file; `<scratch>` in prose, never a real path).
    - `scripts/sql-body-normalize.mjs` usage block (`--diff-bodies <snapshot.sql> <live.sql>`), and `scripts/prod-body-drift-check.sh` where it greps changed files for `-- prod-body-ack:`.
    - `scripts/dump-sql-functions.ts` (what it copies into a snapshot).
    - The current snapshots of both functions.
  </read_first>
  <action>
    Run `node scripts/verify-plan-anchors.mjs .planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-02-PLAN.md` and halt on non-zero. Run `npm run schema:functions`; `git status --short supabase/schema/functions/` must show exactly the two mark-RPC snapshots modified (if any other snapshot moves, stop and investigate — it means the dumper sees a change this phase did not intend). Each snapshot's `-- source migration:` line must name the new migration. For each of the two functions, write `origin/main`'s snapshot to a per-agent `mktemp` file with `git show origin/main:supabase/schema/functions/<fn>.sql`, run `node scripts/sql-body-normalize.mjs --diff-bodies supabase/schema/functions/<fn>.sql <that file>`, and read the `live` column of the row for that function. Per D-23, add to the migration header's VAC-04 section, in the `20260924230827` shape (the derivation commands with `<scratch>` standing for the temp path, a statement that each ack is earned only if VAC-04 on the PR reports the same PROD hash, and that VAC-08 is red on the PR by construction until apply-on-merge), exactly two lines `-- prod-body-ack: <sha256>`, one per RPC, each labelled in the prose above it with its function name. ⛔ No ack prose may contain the lock line's exact text or any anchor text a plan 01 twin finds. Re-run `npm run schema:functions:check`, then plan 01's LANE CMD (the gate must stay GREEN) and `node scripts/mutation-runner/run.mjs --parse-only` (no `occurrence-mismatch`). Paste the two `--diff-bodies` rows and the check output into the SUMMARY. Commit the two snapshots and the migration by explicit path; never stage `.planning/milestone.lock` or `.planning/state.json`.
  </action>
  <verify>
    <automated>npm run schema:functions:check && node -e 'const n=(require("fs").readFileSync(process.argv[1],"utf8").match(/^-- prod-body-ack: [0-9a-f]{64}$/gm)||[]).length;console.log("prod-body-ack lines: "+n);process.exit(n===2?0:1)' supabase/migrations/20260926120000_mark_compute_job_bridge_advisory_lock.sql && grep -q '20260926120000_mark_compute_job_bridge_advisory_lock.sql' supabase/schema/functions/mark_compute_job_done.sql && grep -q '20260926120000_mark_compute_job_bridge_advisory_lock.sql' supabase/schema/functions/mark_compute_job_failed.sql</automated>
    <fails_when>`schema:functions:check` exits non-zero reporting drift for either mark RPC; the node counter prints `prod-body-ack lines:` with a value other than 2 and exits 1; either snapshot's source line does not name the new migration (the `grep -q` exits 1)</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run schema:functions:check` exits 0.
    - Exactly two lines in the migration match `^-- prod-body-ack: [0-9a-f]{64}$`, and each hash equals the `live` value in the pasted `--diff-bodies` row for its function.
    - `git show --name-only --format= <task-1 commit sha> -- supabase/schema/functions/` lists only `mark_compute_job_done.sql` and `mark_compute_job_failed.sql`.
    - `git show --name-only --format= <task-1 commit sha> <task-2 commit sha>` lists neither `.planning/milestone.lock` nor `.planning/state.json`.
    - Plan 01's LANE CMD exits 0 and `node scripts/mutation-runner/run.mjs --parse-only` reports no `occurrence-mismatch`.
  </acceptance_criteria>
  <done>Snapshots match the migration chain and both PROD-body acks are earned from the tool, not asserted.</done>
</task>

<task type="auto">
  <name>Task 2: A loud dblink probe in the sql-mutation job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml`: the `sql-mutation:` job (find it with `grep -n '^  sql-mutation:' .github/workflows/ci.yml`), its step `Provision pg_cron for the lane's PostgreSQL (Phase 164.4.1)` and the step after it; cite by step name, never by line.
    - `src/__tests__/test-restore-workflow-wiring.test.ts` (it locates the pg_cron step by name via `ciStepNameContaining` and runs a calibration harness over it — a reason NOT to edit that step's script).
    - `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` and `src/__tests__/drift-check-scripts.test.ts` (what else reads `ci.yml`).
  </read_first>
  <action>
    Add a NEW step to the `sql-mutation` job immediately after the pg_cron provisioning step, named `Probe - dblink contrib is present for the lane's PostgreSQL (Phase 164.5.2)`. Do not edit the pg_cron step itself (its script is exercised by the wiring test's harness). The step runs under `set -euo pipefail`, resolves the bindir with `bash scripts/pg-lane/run.sh --print-pgbin` exactly as the pg_cron step does (stderr to a `RUNNER_TEMP` file, reprinted on an empty answer with a `::error::MEASURE_FAIL:` line), prints it, and then `ls -l` both `dblink.so` under `pg_config --pkglibdir` and `extension/dblink.control` under `pg_config --sharedir`, from that same `pg_config`. On a missing artifact, emit one `::error::` line naming dblink, the Phase 164.5.2 LANE-ONLY gate `supabase/tests/test_mark_rpc_bridge_advisory_lock.sql`, and the remedy (install the PostgreSQL contrib package for the lane's major), then exit 1. The step comment states: RESEARCH assumption A1 (ubuntu's server package carries contrib) is unverified, this step's first PR run is the measurement, and no package is installed pre-emptively. Validate with `actionlint .github/workflows/ci.yml` and run the three ci.yml-reading vitest files. Commit `.github/workflows/ci.yml` by explicit path; never stage `.planning/milestone.lock` or `.planning/state.json`.
  </action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml && grep -c "Probe - dblink contrib is present for the lane's PostgreSQL (Phase 164.5.2)" .github/workflows/ci.yml && npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts src/__tests__/drift-check-scripts.test.ts</automated>
    <fails_when>actionlint prints any finding in the new step; the step-name grep prints `0`; any of the three vitest files reports a failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - The new step's name appears exactly once in `ci.yml`, inside the `sql-mutation` job, after the pg_cron step.
    - The step contains `dblink.so` and `extension/dblink.control`, `set -euo pipefail`, and a `::error::` line naming dblink.
    - `git show <task-2 commit sha> -- .github/workflows/ci.yml` shows added lines only: no line beginning with a single `-` (the `---` file header excepted).
    - actionlint and the three vitest files pass.
  </acceptance_criteria>
  <done>An absent dblink on the CI runner fails the job at a named probe; the PR's first `sql-mutation` log settles assumption A1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR -> VAC-04 (repo-vs-PROD body diff) | an ack line turns a PROD-body drift into an accepted overwrite |
| CI runner -> apt / contrib | the probe reads only; no install happens in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.5.2-09 | Tampering | a guessed or stale `prod-body-ack` masks a PROD body the repo does not know | high | mitigate | hash read off `--diff-bodies` `live` column vs `origin/main` snapshot; pasted in SUMMARY; VAC-04 on the PR must report the same hash |
| T-164.5.2-10 | Repudiation | hand-edited snapshot hides a real body change | medium | mitigate | dumper only; `--check` exit 0; only two snapshots move |
| T-164.5.2-SC | Tampering | package installs | low | accept | no npm/pip/cargo install; no apt install in this plan; a later contrib install (only if the probe fails) is a distro PostgreSQL package from the runner's configured apt sources |
</threat_model>

<verification>
- `npm run schema:functions:check` exit 0; two earned ack lines; the plan 01 gate still GREEN; `--parse-only` clean.
- `actionlint` clean; the ci.yml-reading vitest files green.
- `env -u TMPDIR npx tsx scripts/check-planning-hygiene.ts` exits 0 after the SUMMARY is written.
</verification>

<success_criteria>
The migration ships with current snapshots and earned PROD-body acknowledgements, and CI names a
missing dblink explicitly.
</success_criteria>

<output>
Create `.planning/phases/164.5.2-bridgelock-the-per-strategy-advisory-lock-161-1-d1-asked-for/164.5.2-02-SUMMARY.md` when done
</output>
