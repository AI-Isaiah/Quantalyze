---
phase: "164.6.7"
slug: "compositeclaimsnapshot-the-composite-run-reads-the-live-job"
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-25"
---

# Phase 164.6.7 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from
> `164.6.7-RESEARCH.md` `## Validation Architecture`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest with pytest-asyncio (`asyncio_mode = auto`) |
| **Config file** | `analytics-service/pytest.ini` |
| **Quick run command** | `cd analytics-service && "$VPY" -m pytest tests/test_ledger_refresh_composite_nondestructive.py tests/test_ledger_refresh_gates.py tests/test_stitch_composite_job.py -q -p no:cacheprovider` |
| **Full suite command** | `cd analytics-service && "$VPY" -m pytest tests -q -p no:cacheprovider`, plus `ruff check` on touched files and CI's strict mypy command (the `ci.yml` step "Type gate - mypy strict over the running-service surface") |
| **Estimated runtime** | quick ~10 s; full suite several minutes |

`VPY` is the main checkout's venv interpreter, derived without a home path:
`VPY="$(git rev-parse --path-format=absolute --git-common-dir)/../analytics-service/.venv/bin/python"`.
pytest runs ONLY from `analytics-service/`.

---

## Sampling Rate

- **After every task commit:** the quick run command.
- **After every plan wave:** the full suite, ruff and strict mypy.
- **Before `/gsd-verify-work`:** full suite green, and the probe's pre-fix (plan 01) and post-fix (plan 02) verdict lines both in SUMMARYs.
- **Max feedback latency:** quick command under 60 s.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 164.6.7-01-01 | 01 | 1 | SC-1 | T-164.6.7-01, -03, -04 | probe refuses non-loopback before import; no ids in output | local-lane probe | plan 01 Task 1 verify (`TRACER-OK`) | ❌ W1 creates | ⬜ pending |
| 164.6.7-01-02 | 01 | 1 | SC-1 | T-164.6.7-02, -03 | own rows only; statuses/booleans only | local-lane probe, `--expect pre-fix` | plan 01 Task 2 verify (`PRE-FIX-RECORDED`) | ❌ W1 creates | ⬜ pending |
| 164.6.7-02-01 | 02 | 2 | SC-3 | T-164.6.7-05, -08 | a post-claim retraction takes the loud path; neuter is RED | unit (tdd) | quick run command (`FIX-OK`) | ✅ file exists, new class | ⬜ pending |
| 164.6.7-02-02 | 02 | 2 | SC-3 | T-164.6.7-05 | failing re-read fails LOUD; control stays error-only | unit + full suite + ruff + mypy | plan 02 Task 2 verify | ✅ | ⬜ pending |
| 164.6.7-02-03 | 02 | 2 | SC-2 | T-164.6.7-01 | both layers agree for every arm | local-lane probe, `--expect post-fix` | plan 02 Task 3 verify (`SC2-PROVEN`) | ✅ after W1 | ⬜ pending |
| 164.6.7-03-01 | 03 | 3 | SC-4 | T-164.6.7-09 | item 6 stays BLOCKING | doc grep + plan-anchor-verify | plan 03 Task 1 verify (`RUNBOOK-OK`) | ✅ | ⬜ pending |
| 164.6.7-03-02 | 03 | 3 | SC-4 | T-164.6.7-10 | no identifiers in public prose | doc grep + hygiene | plan 03 Task 2 verify (`TODOS-OK`) | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `_FakeSupabase` `live_job_metadata` seam, keyed on the job id and default ABSENT (built in plan 02 Task 1, test-first, before the fix).
- [ ] `analytics-service/scripts/probe_composite_claimtime.py` (plan 01, wave 1; it is the phase's tracer).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Pre-fix and post-fix harm probe on the local stack | SC-1, SC-2 | D-01: a recorded local run, not a CI gate; no CI job runs Python on the local stack | `bash scripts/local-stack/run.sh up`, then from `analytics-service/` run `"$VPY" scripts/probe_composite_claimtime.py --expect pre-fix --handler-log <per-plan scratch path outside the repo>` (plan 01) or `--expect post-fix` with the same flag (plan 02); paste only the `HARM-PROBE` verdict lines into the SUMMARY — the handler log carries ids and is never pasted |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
