---
phase: 164.6.7-compositeclaimsnapshot-the-composite-run-reads-the-live-job
plan: 02
type: execute
wave: 2
depends_on: ["164.6.7-01"]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_stitch_composite_job.py
  - analytics-service/tests/test_ledger_refresh_composite_nondestructive.py
autonomous: true
requirements:
  - "[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]"
  - "164.6.7-SC-2"
  - "164.6.7-SC-3"

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-05: inside run_stitch_composite_job's _stamp_failed closure, when the claim-time snapshot would GRANT protection, the LIVE compute_jobs row is re-read through _refresh_marker_still_on_row with the handler-local supabase client and LEDGER_REFRESH_COMPOSITE_SOURCE; a marker gone from the live row takes the LOUD path (computation_status failed, computation_warned False)."
    - "D-05: the live read only NARROWS: an unmarked job and a non-terminal-success row never trigger it, and a failing re-read (exception, no row, no id) takes the LOUD path."
    - "SC-3: a regression test drives a job dict that STILL carries the marker against a live row that does not, asserts the loud stamp, and was observed RED with the re-read neutered to True and GREEN after a byte-identical restore; the marker-present control stayed GREEN under the neuter."
    - "Gate 11 (exactly one _job_source literal comparison in the closure, equal to the SQL arm's), the composite region floor and sentinel, the M-3 source scan, and tests 1-8 of the nondestructive file (the eight existing `test_1_` .. `test_8_` methods, measured 2026-09-25) stay GREEN."
    - "D-05: _FakeSupabase gains live_job_metadata defaulting to ABSENT, keyed on the job id, so every existing construction behaves exactly as before and a fix that re-reads the wrong id fails."
    - "SC-2 / D-02: the plan 01 probe re-run on the post-fix tree with --expect post-fix exits 0 with layers_agree=true on every arm, and the SUMMARY compares its end-state readings with plan 01's, recording the harm size without gating on it."
    - "ruff finding count on services/job_worker.py does not rise against BASE02 (5 pre-existing findings at planning time, measured 2026-09-25: F401 x4 and F841 x1, left untouched; ruff is a local hygiene gate, not a CI step), the two test files stay at zero findings, CI's strict mypy command is green, and the full analytics pytest suite is green from analytics-service/ with the main checkout's venv and with TEST_SUPABASE_DB_URL, SUPABASE_TEST_DB_URL, SUPABASE_TEST_URL and SUPABASE_TEST_SERVICE_KEY unset, so no test can reach shared TEST."
    - "SC-2 is tied to the real bridge: every inflight=none arm of the post-fix probe prints bridge_agrees=true (plan 01's fidelity check)."
  artifacts:
    - path: analytics-service/services/job_worker.py
      provides: "the live re-read at the composite honour site"
      contains: "_refresh_marker_still_on_row("
    - path: analytics-service/tests/test_ledger_refresh_composite_nondestructive.py
      provides: "the post-claim retraction regression class"
    - path: analytics-service/tests/test_stitch_composite_job.py
      provides: "the _FakeSupabase live_job_metadata seam"
  key_links:
    - from: "run_stitch_composite_job._stamp_failed"
      to: "_refresh_marker_still_on_row"
      via: "an awaited call guarded by the snapshot comparison, before the error-only branch"
      pattern: "_refresh_marker_still_on_row\\(\\s*supabase"
    - from: "_FakeQuery.execute (compute_jobs select)"
      to: "_FakeSupabase.live_job_metadata"
      via: "served only when the query's id equality matches the seeded job id"
      pattern: "live_job_metadata"
  prohibitions:
    - "No git checkout --, git restore or git stash; neuter/restore uses a byte backup and cmp."
    - "No migration, no SQL change (D-04)."
    - "Never start uvicorn or a worker; pytest only from analytics-service/."
---

<objective>
Make the composite run decide from the live job row, and prove it three ways (D-05; ROADMAP
success criteria 2 and 3).

1. The fix: in the `_stamp_failed` closure of `run_stitch_composite_job`, keep the snapshot
   comparison as the first filter and, only when it would grant protection, re-read the live row
   with `_refresh_marker_still_on_row`, exactly as the single-key honour sites do.
2. The regression: a new test class in `tests/test_ledger_refresh_composite_nondestructive.py`,
   observed RED with the re-read neutered.
3. The execution proof: the plan 01 probe, re-run with `--expect post-fix`, shows both layers
   agreeing for a post-claim retraction.

Purpose: the two layers must reach the same verdict about the same job.
Output: the fix, the tests and fake seam, and the post-fix verdict and harm-size lines in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
<!-- Plan 01's SUMMARY (164.6.7-01-SUMMARY.md, same directory) is read in Task 3; it does not exist at planning time, so it is not an @-reference. -->
@.planning/phases/164.6.7-compositeclaimsnapshot-the-composite-run-reads-the-live-job/164.6.7-CONTEXT.md
@.planning/phases/164.6.7-compositeclaimsnapshot-the-composite-run-reads-the-live-job/164.6.7-RESEARCH.md
@CLAUDE.md
</context>

<!-- verify-symbol: analytics-service/services/job_worker.py _refresh_marker_still_on_row -->
<!-- verify-symbol: analytics-service/services/job_worker.py LEDGER_REFRESH_COMPOSITE_SOURCE -->
<!-- verify-symbol: analytics-service/services/job_worker.py STRATEGY_ANALYTICS_TERMINAL_SUCCESS_STATUSES -->
<!-- verify-symbol: analytics-service/services/job_worker.py _upsert_error_only -->
<!-- verify-symbol: analytics-service/tests/test_ledger_refresh_gates.py composite_guard_region -->
<!-- verify-symbol: analytics-service/tests/test_ledger_refresh_gates.py test_composite_sql_marker_equals_composite_python_marker -->
<!-- verify-symbol: analytics-service/tests/test_ledger_refresh_gates.py _COMPOSITE_GUARD_REGION_SENTINEL -->
<!-- verify-symbol: analytics-service/tests/test_stitch_composite_job.py _FakeSupabase -->
<!-- verify-symbol: analytics-service/tests/test_stitch_composite_job.py _FakeQuery -->
<!-- verify-symbol: analytics-service/tests/test_ledger_refresh_composite_nondestructive.py TestGuardSuppressesTheUnpublish -->
<!-- verify-symbol: analytics-service/tests/test_ledger_refresh_reuse_collision.py _refresh_marker_still_on_row -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Retracted-after-claim goes LOUD, test first, fix, neuter to RED (D-05, SC-3)</name>
  <files>analytics-service/tests/test_stitch_composite_job.py, analytics-service/tests/test_ledger_refresh_composite_nondestructive.py, analytics-service/services/job_worker.py</files>
  <read_first>
    - `analytics-service/services/job_worker.py`: `_refresh_marker_still_on_row` and its block comment; the single-key honour site inside `run_derive_broker_dailies_job` that calls it with `LEDGER_REFRESH_SINGLE_KEY_SOURCE` (the shape to mirror); the `_stamp_failed` closure in `run_stitch_composite_job` in full, including the comment that explains the M-3 word ban.
    - `analytics-service/tests/test_stitch_composite_job.py`: `_FakeQuery` (its `eq` list and `execute` select fall-through) and `_FakeSupabase.__init__`; the M-3 source scan (the test that asserts the banned words are absent from `run_stitch_composite_job`'s source).
    - `analytics-service/tests/test_ledger_refresh_composite_nondestructive.py` in full (module docstring, `_run`, `_UNSET`, `TestGuardSuppressesTheUnpublish`, `TestGuardIsNotABlanketSuppression`).
    - `analytics-service/tests/test_ledger_refresh_reuse_collision.py` module docstring (the "drive a job that STILL carries the marker against a live row that does not" discipline).
    - `analytics-service/tests/test_ledger_refresh_gates.py`: `composite_guard_region`, `test_composite_sql_marker_equals_composite_python_marker` (Gate 11), `_COMPOSITE_GUARD_REGION_MIN_CHARS`, `_COMPOSITE_GUARD_REGION_SENTINEL`, `test_composite_guard_uses_the_single_sourced_success_set`.
  </read_first>
  <behavior>
    - A job dict whose `metadata.source` is the composite marker, run against a live row whose metadata carries `refresh_marker_retracted` and no `source`, on a `complete_with_warnings` analytics row: the last `strategy_analytics` upsert has `computation_status == "failed"` and `computation_warned is False`.
    - The same drive with the fix neutered (the re-read replaced by `True`) writes an error-only payload with no publish-state key, so the test is RED.
  </behavior>
  <action>
    Before the first edit: `S="${TMPDIR:-/tmp}/gsd-164.6.7-02"; mkdir -p "$S"; git rev-parse HEAD > "$S/BASE02"`.
    Every later step and verify reads `BASE02` from that file (verify commands run in separate
    shells, so a shell variable does not survive between them).

    Seam first (D-05). In `_FakeSupabase.__init__` add keyword-only `live_job_metadata` (default
    ABSENT, expressed with a private sentinel or `None` meaning "no row", so no existing
    construction changes behaviour) and `live_job_id: str | None = None`. In `_FakeQuery.execute`'s
    select fall-through, when `self.table == "compute_jobs"` and the fake has `live_job_metadata`
    set and `("id", fake.live_job_id)` is in the query's recorded equalities, return
    `SimpleNamespace(data={"metadata": <that value>})`; otherwise keep today's `data=None`. Keying
    on the id is deliberate: a fix that re-reads the wrong id must fail. Comment the seam in this
    repo's voice (why it exists, what default keeps byte-identical).

    In `test_ledger_refresh_composite_nondestructive.py`, give `_run` two optional keyword
    parameters: `job_id` (default a module constant) and `live_metadata` (default: the snapshot
    `metadata` when it is a dict, else ABSENT), passed through to `_FakeSupabase` and put on the job
    dict as `"id"`. Default live equal to snapshot keeps tests 1-8 meaning exactly what they meant:
    the live row still carries whatever the snapshot carried. Update the module docstring's
    falsifiability paragraph to name the new neuter.

    Write the new class `TestPostClaimRetractionTakesTheLoudPath` with
    `test_marker_retracted_after_claim_takes_the_loud_path`: snapshot `{"source": _COMPOSITE_MARKER}`,
    live `{"refresh_marker_retracted": _COMPOSITE_MARKER, "correlation_id": "c"}`, existing status
    `complete_with_warnings`; assert the last analytics upsert carries `computation_status == "failed"`
    and `computation_warned is False`. Docstring: why the snapshot must still carry the marker (a
    test that retracts before the run is green against the bug). Run it on the UNFIXED code and
    record the RED output in the SUMMARY.

    Then the fix, per D-05, in `_stamp_failed`: bind the existing condition to a local (for example
    `_honour_marker`), keeping `_job_source == "ledger-refresh-composite"` as the ONLY literal
    comparison in the closure (Gate 11 counts them) and keeping the
    `STRATEGY_ANALYTICS_TERMINAL_SUCCESS_STATUSES` reference. When that local is true, await
    `_refresh_marker_still_on_row(supabase, job.get("id"), LEDGER_REFRESH_COMPOSITE_SOURCE)` on the
    handler-local `supabase`; if it answers False, log a warning naming the job and strategy (the
    derive site's wording adapted, saying the marker was retracted since the claim and the LOUD
    path is taken) and set the local False. Then `if _honour_marker:` guards the unchanged
    error-only branch. Add a short comment that the snapshot is the claim-time copy and the live
    row is the only place a post-claim retraction is visible, that it only narrows, and that it
    fails toward loud. ⛔ New comments and the log string must not contain either word the M-3
    source scan bans; use "live" and "publish state".

    Neuter-to-RED, with a byte backup and never `git checkout --`/`git restore`. Use a
    per-plan scratch dir, `S="${TMPDIR:-/tmp}/gsd-164.6.7-02"` (parallel agents share one
    scratchpad, so the name is plan-specific):
    `cp analytics-service/services/job_worker.py "$S/job_worker.py.bak"`, replace the
    `await _refresh_marker_still_on_row(...)` expression at the composite site with `True`, run the
    new test (must FAIL) and `TestGuardSuppressesTheUnpublish` (must PASS), then
    `cp "$S/job_worker.py.bak" analytics-service/services/job_worker.py` and `cmp` it against the
    backup. Record both runs' pass/fail lines in the SUMMARY. Re-take the backup after every edit
    to the file before neutering again.

    The SUMMARY's neuter table is written as one machine-checkable line per test, in exactly this
    shape (Task 3's verify greps these rows):
    `NEUTER-ROW: test=<test or class name> unfixed=<RED|GREEN> fixed=<RED|GREEN> neutered=<RED|GREEN> restored=<RED|GREEN> cmp=<exit code>`.
    `unfixed` is the reading on `job_worker.py` as of `BASE02`; for a test written after the fix,
    measure it by `read -r B02 < "$S/BASE02"` then `git show "$B02:analytics-service/services/job_worker.py"` into the
    working file, run, then restore from the byte backup and `cmp` (never `git checkout --`).
    Task 1 contributes two rows: `test=test_marker_retracted_after_claim_takes_the_loud_path`
    (expected RED, GREEN, RED, GREEN) and `test=TestGuardSuppressesTheUnpublish` (expected GREEN,
    GREEN, GREEN, GREEN).
  </action>
  <verify>
    <automated>VPY="$(git rev-parse --path-format=absolute --git-common-dir)/../analytics-service/.venv/bin/python"; cd analytics-service && env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY "$VPY" -m pytest tests/test_ledger_refresh_composite_nondestructive.py tests/test_ledger_refresh_gates.py tests/test_stitch_composite_job.py -q -p no:cacheprovider && grep -q 'class TestPostClaimRetractionTakesTheLoudPath' tests/test_ledger_refresh_composite_nondestructive.py && test "$(awk '/_refresh_marker_still_on_row\(/{n++} END{print n+0}' services/job_worker.py)" -ge 5 && test "$(awk '/LEDGER_REFRESH_COMPOSITE_SOURCE/{n++} END{print n+0}' services/job_worker.py)" -ge 3 && echo FIX-OK</automated>
    <fails_when>no FIX-OK: any test in the three files fails (including Gate 11, the region floor and sentinel, M-3, or tests 1-8), the new class is absent, or the composite site added no helper call or no LEDGER_REFRESH_COMPOSITE_SOURCE reference.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records: the new test RED on unfixed code; GREEN after the fix; RED under the neuter with `TestGuardSuppressesTheUnpublish` GREEN in the same run; GREEN after restore, with `cmp` exit 0 against the backup.
    - Inside the closure, the count of `_job_source == "` comparisons is still 1 (Gate 11 passing is the check).
    - Lines containing `_refresh_marker_still_on_row(` in `analytics-service/services/job_worker.py` number at least 5 (4 at planning time, measured 2026-09-25: the definition plus three single-key call sites) and lines containing `LEDGER_REFRESH_COMPOSITE_SOURCE` at least 3 (2 at planning time). Re-measure the pre-fix counts at execution; the READING wins over these numbers.
    - `$S/BASE02` is written before the first edit; `read -r B02 < "$S/BASE02"; git diff --stat "$B02"..HEAD` after the task's commit lists only the three files in `<files>`.
  </acceptance_criteria>
  <reversibility rating="reversible">About ten lines of Python and test code; no schema.</reversibility>
  <done>A post-claim retraction takes the loud path, a test proves it, and that test goes RED when the live read is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The control and fail-safe arms, then ruff, strict mypy and the full suite (D-05)</name>
  <files>analytics-service/tests/test_ledger_refresh_composite_nondestructive.py, analytics-service/tests/test_stitch_composite_job.py</files>
  <read_first>
    - The files as Task 1 left them.
    - `.github/workflows/ci.yml` step `Type gate - mypy strict over the running-service surface` (the exact mypy command).
    - `analytics-service/tests/test_ledger_refresh_reuse_collision.py`, the test that pins the helper's except arm (how an exception is induced).
  </read_first>
  <behavior>
    - Control: snapshot marked, live row STILL marked, status `complete_with_warnings`: no analytics upsert carries `computation_status` or `computation_warned` (error-only), and `computation_error` is written.
    - Fail-safe: snapshot marked, the live read raises: the loud stamp fires (`failed`, warned False).
    - No live row (seam absent) with a marked snapshot: loud.
  </behavior>
  <action>
    Add to `TestPostClaimRetractionTakesTheLoudPath`:
    `test_marker_still_on_the_live_row_stays_error_only` (the control; it guards the driver, so a
    broken seam cannot make the loud test pass for the wrong reason),
    `test_live_reread_that_raises_takes_the_loud_path` (add a `live_job_read_raises: bool = False`
    keyword to `_FakeSupabase` that makes the `compute_jobs` select raise in `execute`; default
    False keeps every other construction unchanged), and
    `test_marker_with_no_live_row_takes_the_loud_path`. Assert against `_PUBLISH_STATE_KEYS` and
    `_analytics_upserts` as the existing classes do.

    Neuter check for the control: under the Task 1 neuter the control must stay GREEN and the
    raises arm must go RED; record both in the SUMMARY (same byte-backup and `cmp` discipline).
    Add three `NEUTER-ROW:` lines in Task 1's shape: `test=test_marker_still_on_the_live_row_stays_error_only`
    (expected GREEN, GREEN, GREEN, GREEN), `test=test_live_reread_that_raises_takes_the_loud_path`
    (expected RED, GREEN, RED, GREEN) and `test=test_marker_with_no_live_row_takes_the_loud_path`
    (expected RED, GREEN, RED, GREEN). A row that reads otherwise is a finding to explain in the
    SUMMARY, never a row to edit into shape.

    Then run, and paste each tool's final line into the SUMMARY:
    ruff, as a NO-RISE gate rather than a clean gate. `services/job_worker.py` carries five
    pre-existing findings unrelated to this phase (measured 2026-09-25: F401 on three
    `services.mt5_concurrency` imports and on the `services.metrics.compute_all_metrics` import, and
    F841 on an unused `exc` binding). Leave them untouched: fixing them is outside this phase and
    `ruff` is not a CI step (it is this plan's local hygiene gate). Count the current findings with
    `ruff check --output-format=concise services/job_worker.py` and the BASE02 findings by writing
    `git show "$B02:analytics-service/services/job_worker.py"` (after `read -r B02 < "$S/BASE02"`) to a scratch file
    (checking its exit status, so a failed `git show` cannot read as zero findings) and feeding
    that file on stdin to `ruff check --output-format=concise --stdin-filename services/job_worker.py -` (the stdin form
    resolves the same config from `analytics-service/`); the current count must not exceed the
    BASE02 count. The two test files must stay at zero findings. Paste both counts.
    CI's strict mypy command copied from the `ci.yml` step named above, run from `analytics-service/`
    with the main checkout's venv;
    the full suite `python -m pytest tests -q -p no:cacheprovider` from `analytics-service/`, under
    `env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY`
    so no integration test can resolve a shared TEST credential from the developer shell (the
    tests that need them skip). Report the passed/skipped/failed counts verbatim. A skipped test is
    named, not hidden. Never run pytest from the repo root (cassette misses become live broker
    calls).
  </action>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.6.7-02"; VPY="$(git rev-parse --path-format=absolute --git-common-dir)/../analytics-service/.venv/bin/python"; VBIN="$(dirname "$VPY")"; test -s "$S/BASE02" && read -r B02 < "$S/BASE02" && cd analytics-service && git show "$B02:analytics-service/services/job_worker.py" > "$S/job_worker.base.py" && NOW="$(ruff check --output-format=concise services/job_worker.py | grep -cE '^services/job_worker\.py:[0-9]+:[0-9]+: ' || true)" && BASE="$(ruff check --output-format=concise --stdin-filename services/job_worker.py - < "$S/job_worker.base.py" | grep -cE '^services/job_worker\.py:[0-9]+:[0-9]+: ' || true)" && echo "ruff job_worker findings: now=$NOW base=$BASE" && test "$BASE" -ge 1 && test "$NOW" -le "$BASE" && ruff check tests/test_ledger_refresh_composite_nondestructive.py tests/test_stitch_composite_job.py && "$VBIN/mypy" --strict --follow-imports=silent --config-file=pyproject.toml services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py && env -u TEST_SUPABASE_DB_URL -u SUPABASE_TEST_DB_URL -u SUPABASE_TEST_URL -u SUPABASE_TEST_SERVICE_KEY "$VPY" -m pytest tests -q -p no:cacheprovider</automated>
    <fails_when>the BASE02 file is missing; the ruff finding count on job_worker.py rose above its BASE02 count (or the BASE02 count reads 0, which means the stdin measurement broke, since 5 pre-existing findings were measured at planning); either test file has a ruff finding; mypy strict reports an error; or any test in the full suite fails.</fails_when>
  </verify>
  <acceptance_criteria>
    - The number of `def test_` lines in `analytics-service/tests/test_ledger_refresh_composite_nondestructive.py` rose by four across Tasks 1-2 (8 at planning time, measured 2026-09-25; re-measure the starting count at execution and compare against that reading).
    - The SUMMARY carries five `NEUTER-ROW:` lines (the four new tests plus `TestGuardSuppressesTheUnpublish`) in the Task 1 shape, the ruff now/base counts, and the mypy and pytest final lines.
    - The mypy command in the verify equals the `run:` line of the named `ci.yml` step (re-read it at execution; if it changed, use the new one and say so).
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only additions.</reversibility>
  <done>The fix is bounded on both sides by tests that each fail when it is wrong, and every analytics gate the repo runs is green.</done>
</task>

<task type="auto">
  <name>Task 3: Criterion 2 by execution, the post-fix probe and the harm size (SC-2, D-02)</name>
  <files>(no tracked source file; writes this plan's SUMMARY)</files>
  <read_first>
    - `164.6.7-01-SUMMARY.md` (the pre-fix verdict lines and how the next-bridge call was driven).
    - `analytics-service/scripts/probe_composite_claimtime.py` (`--expect post-fix`).
  </read_first>
  <action>
    Boot the lane on the fixed tree (`bash scripts/local-stack/run.sh down` then `up`; one stack at
    a time) and run the probe with `--expect post-fix --handler-log "$S/post-handler.log"`. Paste
    the four `HARM-PROBE verdict:` lines and the summary line into the SUMMARY (never the handler
    log). Each `inflight=none` arm must print `bridge_agrees=true`: that is what ties criterion 2
    to the real bridge rather than to the probe's own copy of its predicate.

    Then write one `HARM-SIZE:` line per retracted arm comparing pre-fix (plan 01 SUMMARY) with
    post-fix end states: `HARM-SIZE: arm=<name> pre=<status_after_fail>/<warned_after_fail>-><status_after_next_bridge> post=<same shape> differs=<true|false>`.
    Then ONE line `HARM-VERDICT: ` chosen by the recorded values: when any retracted arm has
    `differs=true`, `HARM-VERDICT: end-state harm shown on the lane for <comma-separated arm names>; scoped to the lane, production cohort unmeasured`;
    when none does, `HARM-VERDICT: no end-state difference on the lane; the fix ships for the decision disagreement (D-02)`.
    Write exactly one `HARM-VERDICT:` line and do not quote the alternative wording.
    Per D-02 the fix is not reverted in either case; the orchestrator surfaces the verdict.
    `git add` the SUMMARY by explicit path, then run the hygiene scan with `tsx` resolved through
    the git common dir (`TSX="$(git rev-parse --path-format=absolute --git-common-dir)/../node_modules/.bin/tsx"; "$TSX" scripts/check-planning-hygiene.ts`)
    and require its `[check-planning-hygiene] OK` line.
  </action>
  <verify>
    <automated>S="${TMPDIR:-/tmp}/gsd-164.6.7-02"; mkdir -p "$S"; VPY="$(git rev-parse --path-format=absolute --git-common-dir)/../analytics-service/.venv/bin/python"; SM=.planning/phases/164.6.7-compositeclaimsnapshot-the-composite-run-reads-the-live-job/164.6.7-02-SUMMARY.md; TSX="$(git rev-parse --path-format=absolute --git-common-dir)/../node_modules/.bin/tsx"; (cd analytics-service && "$VPY" scripts/probe_composite_claimtime.py --expect post-fix --handler-log "$S/post-handler.log") > "$S/post.log" 2>&1; RC=$?; cat "$S/post.log"; test "$RC" -eq 0 && grep -qE '^HARM-PROBE summary: expect=post-fix arms=4 result=ok$' "$S/post.log" && test "$(awk '/^HARM-PROBE verdict: .*layers_agree=true/{n++} END{print n+0}' "$S/post.log")" -eq 4 && test "$(awk '/^HARM-PROBE verdict: arm=retracted.*python_branch=loud sql_is_protected=false/{n++} END{print n+0}' "$S/post.log")" -eq 3 && test "$(awk '/^HARM-PROBE verdict: .*inflight=none .*bridge_agrees=true /{n++} END{print n+0}' "$S/post.log")" -eq 3 && ! grep -E '^HARM-PROBE' "$S/post.log" | grep -qE '[0-9a-f]{8}-[0-9a-f]{4}-[0-9a-f]{4}-' && test "$(awk '/^HARM-SIZE: arm=retracted/{n++} END{print n+0}' "$SM")" -eq 3 && test "$(awk '/^HARM-VERDICT: /{n++} END{print n+0}' "$SM")" -eq 1 && test "$(grep -cE '^NEUTER-ROW: test=test_marker_retracted_after_claim_takes_the_loud_path unfixed=RED fixed=GREEN neutered=RED restored=GREEN cmp=0$' "$SM")" -eq 1 && test "$(grep -cE '^NEUTER-ROW: test=test_marker_still_on_the_live_row_stays_error_only unfixed=GREEN fixed=GREEN neutered=GREEN restored=GREEN cmp=0$' "$SM")" -eq 1 && test "$(grep -cE '^NEUTER-ROW: test=test_live_reread_that_raises_takes_the_loud_path unfixed=RED fixed=GREEN neutered=RED restored=GREEN cmp=0$' "$SM")" -eq 1 && test "$(grep -cE '^NEUTER-ROW: test=test_marker_with_no_live_row_takes_the_loud_path unfixed=RED fixed=GREEN neutered=RED restored=GREEN cmp=0$' "$SM")" -eq 1 && test "$(grep -cE '^NEUTER-ROW: test=TestGuardSuppressesTheUnpublish unfixed=GREEN fixed=GREEN neutered=GREEN restored=GREEN cmp=0$' "$SM")" -eq 1 && ! grep -qE '[0-9a-f]{8}-[0-9a-f]{4}-[0-9a-f]{4}-' "$SM" && "$TSX" scripts/check-planning-hygiene.ts | grep -qF '[check-planning-hygiene] OK' && echo SC2-PROVEN</automated>
    <fails_when>no SC2-PROVEN: the post-fix probe fails its expectation (a layer still disagrees, or the control no longer protects), fewer than four arms agree, a retracted arm is not loud, an inflight=none arm's bridge_agrees is not true, a uuid reached a HARM-PROBE line or the SUMMARY, the SUMMARY lacks the three HARM-SIZE lines, exactly one HARM-VERDICT line, or any of the five NEUTER-ROW lines with its expected readings (a restore whose cmp was not 0 fails here too), or the hygiene scan did not print OK.</fails_when>
  </verify>
  <acceptance_criteria>
    - Post-fix probe exits 0 with `result=ok`, all four arms `layers_agree=true`, three retracted arms loud and unprotected, control still error-only and protected.
    - The SUMMARY holds the verdict lines, three `HARM-SIZE:` lines and one `HARM-VERDICT:` line whose wording matches the recorded `differs` values.
    - `check-planning-hygiene` prints OK.
  </acceptance_criteria>
  <reversibility rating="reversible">A read-and-record step on the private lane.</reversibility>
  <done>Both layers are shown, by execution, to reach the same verdict for a marker retracted after the claim, and the harm size is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker -> compute_jobs (service role) | The re-read selects one row by the id the claim returned, never a caller-supplied value. |
| probe -> database | Loopback lane only (plan 01's guard). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.7-05 | Tampering / Repudiation | `_stamp_failed` publish-state decision | high | mitigate | live re-read through `_refresh_marker_still_on_row`; every error answers "not marked", so a failure is never suppressed; regression test RED under neuter |
| T-164.6.7-06 | Denial of service | the extra read on the failure path | low | accept | runs only when protection would be granted (a marked refresh on a terminal-success row), one indexed select by primary key |
| T-164.6.7-07 | Tampering | residual window between re-read and `mark_compute_job_failed` | low | accept | milliseconds, identical to the single-key arm; recorded in `TODOS.md` by plan 03 per D-03 |
| T-164.6.7-08 | Tampering | neuter/restore of `job_worker.py` | medium | mitigate | byte backup plus `cmp`; never `git checkout --`/`git restore`; backup re-taken after each edit |
</threat_model>

<verification>
- Task 1 and Task 2 verify commands green; SC2-PROVEN printed.
- Neuter table in the SUMMARY shows RED under neuter and a `cmp`-verified restore.
</verification>

<success_criteria>
- Criterion 2 proven by execution (post-fix probe).
- Criterion 3: a regression observed RED when the fix is neutered.
- ruff, CI's strict mypy and the full analytics suite green.
</success_criteria>

<output>
Create `.planning/phases/164.6.7-compositeclaimsnapshot-the-composite-run-reads-the-live-job/164.6.7-02-SUMMARY.md` when done.
</output>
