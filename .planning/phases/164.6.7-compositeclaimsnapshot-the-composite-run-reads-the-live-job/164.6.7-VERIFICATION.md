---
phase: 164.6.7-compositeclaimsnapshot-the-composite-run-reads-the-live-job
verified: 2026-09-26T09:00:00Z
status: human_needed
score: 4/4 roadmap success criteria verified (2 advisory warnings, 4 human/live items)
verified_at_sha: acdf7ed41e97f871e10eba192017f21b77a8e033
drift_subjects:
  - analytics-service/services/job_worker.py
  - analytics-service/scripts/probe_composite_claimtime.py
  - analytics-service/tests/test_ledger_refresh_composite_nondestructive.py
  - analytics-service/tests/test_stitch_composite_job.py
  - analytics-service/tests/test_ledger_refresh_reuse_collision.py
  - analytics-service/tests/test_ledger_refresh_nondestructive.py
  - analytics-service/tests/test_probe_composite_claimtime.py
  - docs/runbooks/ledger-refresh-go-live.md
behavior_unverified: 0
overrides_applied: 0
deferred:
  - truth: "A retraction committing between the Python live re-read and mark_compute_job_failed yields the same verdict in both layers"
    addressed_in: "Phase 164.5.2"
    evidence: "TODOS.md [164.6.7-COMPOSITE-REREAD-RESIDUE], dated ROADMAP routing line 2026-09-25 under Phase 164.5.2; runbook item 7 blocks scheduling on it"
  - truth: "The D-09 transient retry keeps a plain `complete` row published across the retry"
    addressed_in: "Phase 164.5.2"
    evidence: "TODOS.md [164.6.7-RETRY-PLAIN-COMPLETE], dated ROADMAP routing line 2026-09-26 under Phase 164.5.2"
human_verification:
  - test: "Re-run analytics-service/scripts/probe_composite_claimtime.py --expect post-fix (with --pre-fix-output from the plan 01 SUMMARY lines and a --handler-log outside any git work tree) on the loopback lane booted by scripts/local-stack/run.sh up, at the HEAD of this branch, holding the lane lock"
    expected: "Four verdict lines with layers_agree=true, `HARM-PROBE reread-failures: count=0`, `summary: expect=post-fix arms=4 result=ok`, and HARM-SIZE / HARM-VERDICT lines byte-identical to the plan 02 SUMMARY"
    why_human: "The last lane run was at 57d9fc8d3. The composite decision code is unchanged since then (measured by diff: only log and exception text moved), but the probe itself changed in 04ecd7df5 (foreign-claim release through defer_compute_job) and has not run on a lane since. The verifier did not boot the lane: it is shared across worktrees and a prior overlapping boot disturbed another stack. Owner: orchestrator, before ship."
  - test: "At the moment the composite fan-out is scheduled, read run_stitch_composite_job on the worker's DEPLOYED commit and confirm _stamp_failed awaits _refresh_marker_still_on_row before the marker is honoured, with the READ_ERROR arm raising RefreshMarkerRereadUnavailable (runbook precondition item 4)"
    expected: "The re-read and the READ_ERROR raise are present on the deployed commit"
    why_human: "Live deploy state (Railway worker). Owner: founder/operator at scheduling time. Scheduling is also still blocked by runbook items 6 and 7."
  - test: "Measure whether live composite strategy_analytics rows carry computation_warned = TRUE (research assumption A1)"
    expected: "A dated count, to size the H2/H3 harm beyond the lane"
    why_human: "Needs a PROD read; no remote database command runs in this phase. Owner: founder, before scheduling the composite."
  - test: "Re-count the Sentry event bound (runbook item 2, 'Alert volume') on the deployed commit"
    expected: "At most 3 events per failed re-read at a stamp site or the chain edge, 2 per failed entry read, at most 9 per job"
    why_human: "Depends on the deployed commit and on live Sentry config. Owner: operator at deploy time."
---

# Phase 164.6.7: COMPOSITECLAIMSNAPSHOT Verification Report

**Phase Goal:** A composite (`stitch_composite`) run decides whether it is a background ledger
refresh from the job row as it stands when the decision is made, not from the claim-time metadata
snapshot, so the Python composite guard and the SQL `is_protected` check agree about the same job.
**Verified:** 2026-09-26, at `acdf7ed41`
**Status:** human_needed
**Re-verification:** No, initial verification.

## Goal Achievement

### Observable Truths (ROADMAP success criteria, then CONTEXT decisions)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | The harm is shown on the local lane first (claim, retract, observe the decision) | VERIFIED | Probe `probe_composite_claimtime.py` drives the real `run_stitch_composite_job`, `claim_compute_jobs_with_priority` and `mark_compute_job_failed` (read by symbol). Recorded pre-fix table at `813f16a60`: three retracted arms `python_branch=error_only sql_is_protected=false layers_agree=false`, control agrees. Re-executed at HEAD by this verifier as a unit-level reproduction: with the composite re-read neutered to `PRESENT`, `test_marker_retracted_after_claim_takes_the_loud_path` fails `None == 'failed'` (the stamp takes the error-only branch on a retracted marker). |
| SC-2 | Composite guard and `is_protected` reach the same verdict for a post-claim retraction, proven by execution | VERIFIED | Recorded post-fix lane runs (plan 02 at `01043b7f5`; round 1 at `2b7ec95ba` and `57d9fc8d3`): all four arms `layers_agree=true`, three retracted arms `python_branch=loud`, `bridge_agrees=true` on each `inflight=none` arm. `git diff 57d9fc8d3 HEAD` over the `run_stitch_composite_job` region changes only a pre-read ERROR log line and exception text, so the decision path the runs exercised is the one at HEAD. SQL half: the latest `sync_strategy_analytics_status` definition (`20260906120000_computation_error_provenance.sql`) computes `AS is_protected` from the live `compute_jobs.metadata ->> 'source'`. See human item 1 for the probe's own post-round-2 re-run. |
| SC-3 | A regression test observed RED when the fix is neutered | VERIFIED | Verifier's own neuter at HEAD: replaced the awaited `_refresh_marker_still_on_row` call inside `_stamp_failed` with `MarkerLiveState.PRESENT` (cp byte backup). `test_ledger_refresh_composite_nondestructive.py` (22 tests collected) went `7 failed, 15 passed`. The SC-3 proof is the two `TestPostClaimRetractionTakesTheLoudPath` cases (retraction, no-row) going RED; the other five (`TestEveryNotConfirmedStateNamesItsOwnCause`, and `TestTransientReReadFailureRetries`, whose raising seam this neuter bypasses) are collateral of the neuter shape. The transient test's own neuter (composite `READ_ERROR` arm disabled) is recorded in 164.6.7-REVIEW-FIX.md, Topic A. Restored by `cp`, `cmp` = 0, `git status` clean. |
| SC-4 | The runbook precondition is removed or restated to match | VERIFIED (warning) | `docs/runbooks/ledger-refresh-go-live.md`, section `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]`: item 2 MET citing `_stamp_failed` / `_refresh_marker_still_on_row`, item 4 MET with the deploy-time check kept, item 5 shipped, items 6 and 7 BLOCKING. Matches the tree. Warning W-1 below: one round-2 conditional was never reconciled. |
| D-01 | Probe is a recorded local run, not collected by pytest, refuses non-loopback before service import | VERIFIED | `_assert_loopback_dsn` runs on `DB_URL` before `from services.job_worker import run_stitch_composite_job` and again after; 39 fake-driven guard tests (collected at HEAD) in `test_probe_composite_claimtime.py` pass. |
| D-04 | No migration | VERIFIED | Phase diff `01dcf1cc7..HEAD` outside `.planning/` touches no file under `supabase/`. |
| D-05 | Narrow-only live read, handler-local client, marker literal spelled once (Gate 11), M-3 scan clean, fake seam keyed on job id | VERIFIED | `_honour_marker` gates the read; `TestTheReReadOnlyNarrows` asserts zero `compute_jobs` reads for unmarked snapshots. `test_ledger_refresh_gates.py` (Gate 11, composite region) and `test_stitch_composite_job.py` (M-3 scan) pass. |
| D-09 | A transient re-read failure writes nothing and fails TRANSIENT at the composite and single-key stamp sites; definitive non-present answers go loud | VERIFIED | `READ_ERROR` arms raise `RefreshMarkerRereadUnavailable` in `_stamp_failed` and `_stamp_strategy_analytics_failed`; `classify_exception` maps it to `transient`. Pinned by `TestTransientReReadFailureRetries` (neutered RED above) and `TestReReadFailsSafe` (passes). |
| D-10 | Entry publish-state read and chain edge fail TRANSIENT on read error; cause kept in `last_error` | VERIFIED | `_read_entry_publish_state` via `db_read_with_retry`, raise before crawl; chain-edge raise before `_enqueue_csv_analytics`; raise text carries the scrubbed cause. Pinned by `TestEntryPublishStateReadFailsTransient`, `TestChainEdgeReadErrorFailsTransient`, all passing at HEAD. |
| D-03/D-07 | Residue recorded; `161.1-D13` corrected | VERIFIED | `TODOS.md` carries `[164.6.7-COMPOSITE-REREAD-RESIDUE]` and `[164.6.7-RETRY-PLAIN-COMPLETE]`, both dated and routed to Phase 164.5.2 with dated ROADMAP lines; `161.1-D13` has a dated CLOSED line with lineage. |

**Score:** 4/4 roadmap success criteria verified; 0 present-but-behavior-unverified.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Residual window between the live re-read and `mark_compute_job_failed` | Phase 164.5.2 | `[164.6.7-COMPOSITE-REREAD-RESIDUE]`, ROADMAP routing line dated 2026-09-25; runbook item 7 blocks scheduling |
| 2 | Plain-`complete` row not protected across the transient retry | Phase 164.5.2 | `[164.6.7-RETRY-PLAIN-COMPLETE]`, ROADMAP routing line dated 2026-09-26; latent (dated reading, 0 plain `complete` rows in the ledger cohort) |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `analytics-service/services/job_worker.py` | Live re-read in `_stamp_failed`, `MarkerLiveState`, `RefreshMarkerRereadUnavailable` | VERIFIED | Substantive, wired into `dispatch` via `classify_exception`; mypy clean |
| `analytics-service/scripts/probe_composite_claimtime.py` | Harm probe, `--expect pre-fix/post-fix`, loopback guard | VERIFIED | Imports the real handler; mypy clean; not collected by pytest |
| `analytics-service/tests/test_ledger_refresh_composite_nondestructive.py` | Retraction regression and fail-safe arms | VERIFIED | 22 tests; 7 RED under the re-read neuter |
| `analytics-service/tests/test_probe_composite_claimtime.py` | Probe guard tests | VERIFIED | 39 tests collected, passing |
| `docs/runbooks/ledger-refresh-go-live.md` | Precondition restated | VERIFIED (W-1) | See warning |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `_stamp_failed` | `_refresh_marker_still_on_row` | awaited only when `_honour_marker` holds | WIRED |
| `_refresh_marker_still_on_row` | `compute_jobs` row by id | `db_read_with_retry`, `.eq("id", job_id).maybe_single()` | WIRED |
| `RefreshMarkerRereadUnavailable` | queue retry | `classify_exception` returns `transient` | WIRED |
| Python loud path | SQL bridge | `mark_compute_job_failed` PERFORMs `sync_strategy_analytics_status`, whose `is_protected` reads live metadata | WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Phase test files | pytest (env-scrubbed, from `analytics-service/`) on the composite, stitch, reuse-collision, nondestructive, probe and gates files | `228 passed` | PASS |
| Neuter bites | re-read replaced with `PRESENT`, same composite file | `7 failed, 15 passed`; restored, `cmp` 0 | PASS |
| Types | mypy on `services/job_worker.py` and the probe | `Success: no issues found in 2 source files` | PASS |
| Plan anchors | `scripts/verify-plan-anchors.mjs` on the three PLANs | `OK … no stale claims` x3 (14, 14, 7 claims) | PASS |

### Probe Execution

| Probe | Result | Status |
|-------|--------|--------|
| `analytics-service/scripts/probe_composite_claimtime.py` | Not re-run by the verifier (shared local lane). Recorded runs pre-fix and post-fix are cited above; the decision path is unchanged since the last run. | SKIP, human item 1 |

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
|-------------|-------------|--------|----------|
| `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]` | 01, 02, 03 | SATISFIED in the tree | SC-1..SC-4 above; scheduling remains blocked by runbook items 6 and 7 |
| ROADMAP `**Requirements**: TBD` | — | n/a | No REQUIREMENTS.md IDs mapped to this phase |

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| 8 non-planning phase files | `TBD` / `FIXME` / `XXX` | none found | — |
| `job_worker.py`, `_log_marker_not_confirmed` | final `else` treats `PRESENT` as NO_ID/NO_ROW (round-2 SFH-R2-06, LOW) | Info | All callers guard; unaddressed, listed in REVIEW-FIX-R2-A "Not addressed" |
| `job_worker.py`, `_read_existing_failed_row` in `_stamp_failed` | bare `db_execute`, no 504 retry (SFH-R2-02 sibling, LOW) | Info | Non-destructive (propagates, retried as `unknown`); inconsistent with the read after it |

### Warnings

- **W-1 (SC-4 currency).** Runbook item 2's 2026-09-26 bullet, item 4's last correction, and
  CONTEXT D-10 bullet 3 still say the retry protects warned rows only "unless the DEPLOYED commit
  carries a fix that persists the attempt-1 publish state", and D-10 says the orchestrator
  reconciles this after fixer A reports. Fixer A took the QUALIFY option and landed no such fix;
  the gap is routed as `[164.6.7-RETRY-PLAIN-COMPLETE]`, and that id appears in neither the runbook
  nor CONTEXT (0 hits each). Safe in direction (the operator is told to confirm, not assume), but
  it points at a fix that does not exist. Reconcile before ship: state the limit as unconditional
  at this commit and cite the routed id.
- **W-2 (probe currency).** The probe changed after its last lane run (see human item 1).

### Human Verification Required

1. **Probe re-run at HEAD on the local lane.** See frontmatter item 1.
   ✅ **Run 2026-09-26 by the orchestrator's fixer at `39dc6a899`** (the round-2 finishing
   fixes, SFH-R2-06 and the stamp's 504-retried read, are in that tree). Lane lock held, 0
   supabase containers before `scripts/local-stack/run.sh up` (exit 0, `replay complete: 0
   migration(s) applied on top of the dump`), `run.sh down` exit 0 and 0 containers after.
   `services.job_worker` was confirmed to import from this worktree. `--handler-log` was
   outside every git work tree. Counts only:
   - `--expect post-fix --pre-fix-output <plan 01 SUMMARY's five recorded lines>`: exit 0.
     4 verdict lines, 4 with `layers_agree=true`; the three retracted arms `python_branch=loud`,
     the control `error_only`. `HARM-PROBE reread-failures: count=0`. `HARM-PROBE summary:
     expect=post-fix arms=4 result=ok`. The 3 `HARM-SIZE:` lines and the `HARM-VERDICT:` line
     are byte-identical (`cmp`) to the plan 02 SUMMARY. The full stdout is byte-identical to
     the round-1 record in 164.6.7-REVIEW-FIX.md once ` foreign_rows_claimed=0` is stripped
     from its 4 verdict lines; 04ecd7df5 removed that field (a foreign claim now stops the
     probe with exit 2), and it is the only difference.
   - `--expect post-fix` alone: exit 0, `result=ok`, first 7 lines byte-identical to the run
     above, then `HARM-PROBE harm-size: not computed (no --pre-fix-output)`.
   - `--expect pre-fix` (calibration, on a post-fix tree it must not pass): exit 3,
     `result=PREMISE-DISPROVED`, `reread-failures: count=0`.
   - no `--expect`: exit 0, `result=unasserted`; its 4 verdict lines byte-identical to the
     first run's.
   Items 2-4 remain post-deploy, so status stays `human_needed`.
2. **Deploy-time check of the re-read on the worker's deployed commit.** Runbook item 4.
3. **PROD `computation_warned` cohort for composite rows.** Research A1.
4. **Sentry event bound on the deployed commit.** Runbook item 2.

### Gaps Summary

No blocking gap. The composite stamp reads the live job row before honouring its marker, a
post-claim retraction now takes the loud path the SQL bridge also takes, and the regression test
bites under a neuter this verifier ran itself. Two data-integrity residues are routed with dates to
Phase 164.5.2. Status is `human_needed` because the probe's post-round-2 lane run and three live
checks cannot be done here, plus one documentation reconciliation (W-1).

---

_Verified: 2026-09-26_
_Verifier: Claude (gsd-verifier)_
