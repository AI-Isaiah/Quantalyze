---
phase: 164.6.7-compositeclaimsnapshot-the-composite-run-reads-the-live-job
plan: 03
subsystem: docs (go-live runbook, backlog)
tags: [runbook, stitch_composite, ledger-refresh, todos, residual-window]
status: complete
requires:
  - "164.6.7-02 (the fix, TestPostClaimRetractionTakesTheLoudPath, the HARM-VERDICT and HARM-SIZE lines)"
provides:
  - "runbook precondition [164.6-COMPOSITE-CLAIMTIME-SNAPSHOT] restated: items 2, 4, 5 MET, item 6 still BLOCKING"
  - "TODOS.md 161.1-D13 CLOSED with lineage kept"
  - "TODOS.md [164.6.7-COMPOSITE-REREAD-RESIDUE] with owner, trigger and fix shape"
affects:
  - "the ship commit (CHANGELOG, VERSION, package.json per D-08)"
tech-stack:
  added: []
  patterns:
    - "restate in place, keep the superseded sentence as dated lineage"
key-files:
  created: []
  modified:
    - docs/runbooks/ledger-refresh-go-live.md
    - TODOS.md
decisions:
  - "The residue entry names the single-key derive honour site by its enclosing function, run_derive_broker_dailies_job, and says its reachability depends on the single-key schedule, which this phase did not measure."
  - "The section heading keeps its bracketed id, so every existing reference still resolves, and the section still blocks scheduling through item 6."
metrics:
  duration: "about 5 minutes"
  completed: 2026-09-25
actuals:
  tokens: 2144
  tasks: 2
  commits: 2
plan_head_before: 9d10d3848bc4479c5f1a4328bdbac6d3aab8cc52
requirements-completed:
  - "[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]"
  - "164.6.7-SC-4"
---

# Phase 164.6.7 Plan 03: Runbook precondition and backlog currency Summary

The runbook's composite scheduling precondition now matches the tree. Items 2, 4 and 5 read MET,
cited by symbol. Item 6 still blocks scheduling. `TODOS.md` closes `161.1-D13` and books the
residual re-read window with an owner.

## Runbook (Task 1, D-07, SC-4)

In `docs/runbooks/ledger-refresh-go-live.md`, the section "Before the COMPOSITE fan-out is ever
scheduled", id `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]`:

- **Item 2** is MET by Phase 164.6.7. The `_stamp_failed` closure of `run_stitch_composite_job`
  re-reads the live row through `_refresh_marker_still_on_row` before it honours the marker. A
  retraction, a missing row or a failed re-read takes the loud path.
- **Item 3** is now "why it was blocking", in the past tense. It carries plan 02's verdict
  verbatim on one quoted line, dated and scoped to the lane:
  `HARM-VERDICT: end-state harm shown on the lane for retracted-warned, retracted-warned-inflight; scoped to the lane, production cohort unmeasured`.
  It gives one clause per `HARM-SIZE` arm, taken from plan 02's pre and post end states. It
  claims no production cohort. It points at `[164.6.7-COMPOSITE-REREAD-RESIDUE]` for the
  residual window.
- **Item 4** is MET in the tree. It cites `TestPostClaimRetractionTakesTheLoudPath` and names the
  probe as a recorded local run, not a CI gate. ⚠️ It keeps the deploy-time operator check: the
  worker's DEPLOYED commit must carry the re-read. Merging the fix does not satisfy it.
- **Item 5**: the owner phase shipped. The precondition is now blocked only by item 6.
- **Item 6** is unchanged and still ⛔ BLOCKING.
- Items 2, 4 and 5 keep their superseded sentences as dated lineage.
- The "What is NOT watched" bullet and the deribit-composite bullet in "What this runbook
  deliberately does not cover" now name item 6 as what still blocks.

Tracer gate: the Task 1 `<verify>` was run again after the commit and printed `RUNBOOK-OK`. At
that point `git diff --stat BASE03..HEAD` listed only the runbook.

## TODOS.md (Task 2, D-03)

- **`161.1-D13`** gains a dated CLOSED line and keeps its original text. The TypeScript
  retraction closed in Phase 164.6 plan 02. The "honouring side is covered" sentence was wrong
  for the composite until Phase 164.6.7.
- **`[164.6.7-COMPOSITE-REREAD-RESIDUE]`** is added at the top of `## 🟡 FIX MID-TERM`. It
  records the milliseconds between the live re-read and `mark_compute_job_failed`, the H2
  `computation_warned` residue, and that both honour arms share it. The fix shape is branch (b)
  of `sync_strategy_analytics_status` clearing `computation_warned`, or moving the decision
  inside the bridge's transaction. Either one is a shared-bridge migration that auto-applies to
  PROD. **Owner:** whoever next changes `sync_strategy_analytics_status`. **Trigger:** any change
  to that function. The composite arm cannot reach it today, since the composite is
  unscheduled. The single-key arm's reachability is stated as unmeasured.

## Gate output, verbatim

Task 1 verify (run before the commit and again after it as the tracer gate):

```
scanned: 1 plan file(s) named on the command line
claims: 7 checked
OK: 1 plan file(s), no stale claims.
RUNBOOK-OK
```

Task 2 verify, plus the hygiene scan:

```
TODOS-OK
[check-planning-hygiene] OK — 7078 tracked files scanned, none carry the local username or an absolute home path (rule 1 needle: derived at runtime from the home-directory basename).
```

Plan verification, `git diff --stat BASE03..HEAD` after the last task commit:

```
 TODOS.md                                | 37 ++++++++++++++
 docs/runbooks/ledger-refresh-go-live.md | 85 +++++++++++++++++++++++----------
 2 files changed, 98 insertions(+), 24 deletions(-)
```

`CHANGELOG.md`, `VERSION` and `package.json` have no changes against the merge base (D-08). The
skip-token sweep counted 0 in both commit messages.

## Commits

| Task | Commit | Files |
|------|--------|-------|
| 1 Restate the composite scheduling precondition (tracer) | `8df5ed256` | `docs/runbooks/ledger-refresh-go-live.md` |
| 2 Close 161.1-D13, book the residual window | `699c49ed2` | `TODOS.md` |

## Deviations from Plan

**1. [Rule 1 - Bug] The Task 2 verify missed on the first run.** The verify uses
`grep -A12` on the entry id. The bridge name first appeared on the entry's 14th line, so the
check did not find it. Fix: the "What remains" bullet now names `sync_strategy_analytics_status`
as the bridge that `mark_compute_job_failed` PERFORMs. That is accurate content, not padding.
The re-run printed `TODOS-OK`.

**2. [Rule 2 - Accuracy] A reachability qualifier was added to the residue entry.** The plan says
the window is not reachable today while the composite fan-out is unscheduled. That holds for
the composite arm only. The entry also names the single-key derive arm, so a qualifier was
added: that arm is reachable whenever the single-key fan-out is scheduled, and this phase did
not measure whether it is.

**3. [Note] BASE03 was kept on disk.** It was written to the session scratchpad file
`gsd-164.6.7-03-BASE03` (value `9d10d3848`), not held in a shell variable, as the orchestrator
directed.

## Known Stubs

None.

## Threat Flags

None. The only new content is prose with symbol citations and no identifiers. The hygiene scan
printed OK.

## Self-Check: PASSED

- Commits `8df5ed256` and `699c49ed2`: FOUND on `feat/164.6.7`.
- `docs/runbooks/ledger-refresh-go-live.md`, `TODOS.md` and this SUMMARY are present.
- `git rev-list --count 9d10d3848..HEAD` = 2 before this SUMMARY's docs commit.
