---
phase: 164.6.7-compositeclaimsnapshot-the-composite-run-reads-the-live-job
plan: 03
type: execute
wave: 3
depends_on: ["164.6.7-02"]
files_modified:
  - docs/runbooks/ledger-refresh-go-live.md
  - TODOS.md
autonomous: true
requirements:
  - "[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]"
  - "164.6.7-SC-4"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-07 / SC-4: the runbook section 'Before the COMPOSITE fan-out is ever scheduled' states items 2, 4 and 5 as MET, citing the fix and its regression test BY SYMBOL (run_stitch_composite_job, _stamp_failed, _refresh_marker_still_on_row, TestPostClaimRetractionTakesTheLoudPath), never by line number or commit sha."
    - "D-07: item 6 (the composite's own runs must be watched before it is scheduled) is still marked BLOCKING, so the section as a whole still blocks scheduling, and every cross-reference to the section (the 'What is NOT watched' bullet and the 'What this runbook deliberately does not cover' note) says what now remains blocking instead of the met item."
    - "Item 4's deploy-time condition stays: an operator scheduling the composite still confirms the worker's deployed commit carries the live re-read; merging the fix alone does not satisfy it."
    - "D-03: TODOS.md carries a new entry for the post-fix residual window between the live re-read and mark_compute_job_failed, naming both honour arms that share it, the fix shape, and its owner: whoever next changes the shared SQL bridge sync_strategy_analytics_status."
    - "TODOS.md 161.1-D13 no longer says the composite honouring side is covered; it records that the TypeScript retraction closed in Phase 164.6 plan 02 and the Python honour side in Phase 164.6.7, with the original text kept as lineage."
    - "D-08: no CHANGELOG.md, VERSION or package.json change in this plan."
  artifacts:
    - path: docs/runbooks/ledger-refresh-go-live.md
      provides: "the restated composite scheduling precondition"
    - path: TODOS.md
      provides: "161.1-D13 correction and the residue entry"
  key_links:
    - from: "docs/runbooks/ledger-refresh-go-live.md precondition item 4"
      to: "analytics-service/tests/test_ledger_refresh_composite_nondestructive.py TestPostClaimRetractionTakesTheLoudPath"
      via: "cited by class name"
      pattern: "TestPostClaimRetractionTakesTheLoudPath"
  prohibitions:
    - "No line-number citations in the new prose (cite by symbol)."
    - "No identifiers (ids, hosts, emails) in tracked prose; the repo is public."
    - "No git checkout --, git restore or git stash."
---

<objective>
Bring the go-live runbook and the backlog in line with the fix (ROADMAP success criterion 4,
D-07, D-03).

Purpose: the runbook precondition existed because the composite honour site read the claim-time
snapshot. With that closed, the precondition is restated to what is still true: items 2, 4 and 5
are met, item 6 still blocks scheduling, and the residual window is recorded with an owner.
Output: an edited runbook section and two `TODOS.md` edits. CHANGELOG and VERSION belong to the
ship commit (D-08).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
<!-- Plan 02's SUMMARY (164.6.7-02-SUMMARY.md, same directory) supplies the final test names and HARM-VERDICT; it does not exist at planning time, so it is not an @-reference. -->
@.planning/phases/164.6.7-compositeclaimsnapshot-the-composite-run-reads-the-live-job/164.6.7-CONTEXT.md
@docs/runbooks/ledger-refresh-go-live.md
@CLAUDE.md
</context>

<!-- verify-symbol: docs/runbooks/ledger-refresh-go-live.md [164.6-COMPOSITE-CLAIMTIME-SNAPSHOT] -->
<!-- verify-symbol: docs/runbooks/ledger-refresh-go-live.md enqueue_ledger_composite_refresh() -->
<!-- verify-symbol: TODOS.md 161.1-D13. -->
<!-- verify-symbol: analytics-service/services/job_worker.py _refresh_marker_still_on_row -->

<tasks>

<task type="tracer">
  <name>Task 1: Restate the composite scheduling precondition end to end (D-07, SC-4)</name>
  <files>docs/runbooks/ledger-refresh-go-live.md</files>
  <read_first>
    - `docs/runbooks/ledger-refresh-go-live.md`: the whole section headed "Before the COMPOSITE fan-out is ever scheduled — BLOCKING precondition [164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]" (items 1-6); the "What is NOT watched" bullet under "How the fan-out behaves around a failure", which points at item 6; the "What this runbook deliberately does not cover" section's deribit-composite bullet, which points at the section.
    - `164.6.7-02-SUMMARY.md` (same directory): the final test class and test names, the neuter table, and the `HARM-VERDICT:` line.
    - `analytics-service/services/job_worker.py` `_stamp_failed` inside `run_stitch_composite_job`, to confirm the symbol names being cited exist at HEAD.
  </read_first>
  <action>
    Per D-07, edit ONLY that section and the two cross-references, in the runbook's existing voice
    (numbered items, ⛔/⚠️ markers, measured statements).

    Keep the heading's bracketed id `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]` so every existing
    reference still resolves; the section still blocks scheduling because item 6 is still open.
    - Item 2: restate as MET by Phase 164.6.7. Say what is now true by symbol: the `_stamp_failed`
      closure of `run_stitch_composite_job` re-reads the live `compute_jobs` row through
      `_refresh_marker_still_on_row` before it honours the `ledger-refresh-composite` marker, and a
      failed re-read takes the loud path. Keep the original sentence as dated lineage beneath the
      restatement (this repo corrects in place and keeps the superseded text).
    - Item 3 (why): keep the mechanism, move it to past tense where it described the defect, and
      add one sentence on the residual window that remains (milliseconds between the re-read and
      `mark_compute_job_failed`, shared with the single-key arm), pointing at the new `TODOS.md`
      entry from Task 2 by its id.
    - Item 4: restate as MET in the tree, citing the regression by class name
      (`TestPostClaimRetractionTakesTheLoudPath` in
      `analytics-service/tests/test_ledger_refresh_composite_nondestructive.py`) and the probe
      (`analytics-service/scripts/probe_composite_claimtime.py`) as a recorded local run, not a CI
      gate (D-01). ⚠️ Keep the deploy-time condition as an operator check: the worker's deployed
      commit must carry the re-read at the moment the composite is scheduled.
    - Item 5: restate: Phase 164.6.7 shipped the Python change; the precondition is now blocked
      only by item 6.
    - Item 6: unchanged in substance, still ⛔ BLOCKING.
    - The "What is NOT watched" bullet and the deribit-composite bullet in "What this runbook
      deliberately does not cover": make each say that scheduling is blocked by item 6 of the
      section, so neither implies the claim-time snapshot is still open.
    No line numbers, no commit shas, no identifiers.
  </action>
  <verify>
    <automated>R=docs/runbooks/ledger-refresh-go-live.md; SEC="$(awk '/^## .*COMPOSITE fan-out is ever scheduled/{f=1} f&&/^## What this runbook deliberately does not cover/{exit} f' "$R")"; test -n "$SEC" && printf '%s\n' "$SEC" | grep -qF '[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]' && printf '%s\n' "$SEC" | grep -qF 'TestPostClaimRetractionTakesTheLoudPath' && printf '%s\n' "$SEC" | grep -qF '_refresh_marker_still_on_row' && printf '%s\n' "$SEC" | grep -q 'MET' && printf '%s\n' "$SEC" | grep -E '^6\. ' | grep -q 'BLOCKING' && ! printf '%s\n' "$SEC" | grep -qE '(job_worker|\.py|\.ts|\.md):[0-9]+' && node scripts/verify-plan-anchors.mjs .planning/phases/164.6.7-compositeclaimsnapshot-the-composite-run-reads-the-live-job/164.6.7-03-PLAN.md && echo RUNBOOK-OK</automated>
    <fails_when>no RUNBOOK-OK: the section heading lost its id, the test class or helper is not cited, nothing is stated MET, item 6 no longer says BLOCKING, or a file:line citation was added.</fails_when>
  </verify>
  <acceptance_criteria>
    - Item 6 still reads BLOCKING; items 2, 4 and 5 read MET with symbol citations.
    - Record `BASE03="$(git rev-parse HEAD)"` before the first edit; `git diff --stat "$BASE03"..HEAD` after this task's commit lists only `docs/runbooks/ledger-refresh-go-live.md`.
    - The two cross-references name item 6 as what remains.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <done>A reader who opens the runbook to schedule the composite learns that the snapshot defect is closed, how to confirm it on the deployed commit, and that item 6 still blocks.</done>
</task>

<task type="auto">
  <name>Task 2: TODOS.md, correct 161.1-D13 and record the residual window (D-03)</name>
  <files>TODOS.md</files>
  <read_first>
    - `TODOS.md` entry `161.1-D13` (in "Phase 161.1 — recorded deferrals") and the neighbouring `161.1-D12` for the house style of dated corrections.
    - `TODOS.md` section headed "FIX MID-TERM" (where the new entry goes) and one existing entry there for the id/owner/trigger shape.
    - `164.6.7-RESEARCH.md` "Migration verdict" (the residual and its two fix shapes).
  </read_first>
  <action>
    Correct `161.1-D13` in place, keeping the original text as lineage: add a dated line
    (execution date) marking it CLOSED, stating that the TypeScript retraction at the two
    `stitch_composite` enqueue sites closed in Phase 164.6 plan 02, and that the "honouring side is
    covered" sentence was wrong for the composite until Phase 164.6.7, whose
    `run_stitch_composite_job` now re-reads the live row through `_refresh_marker_still_on_row`.

    Add a new entry per D-03 in the "FIX MID-TERM" section, id `[164.6.7-COMPOSITE-REREAD-RESIDUE]`.
    Content: after the fix, a retraction that lands between the Python live re-read and
    `mark_compute_job_failed` still yields an error-only Python write followed by a loud SQL status
    (the H2 `computation_warned` residue), over a window of milliseconds; the single-key derive
    honour site carries the identical residual. Fix shape: branch (b) of
    `sync_strategy_analytics_status` clearing `computation_warned`, or moving the decision into the
    bridge's transaction; either is a migration to a bridge every job kind shares, which
    auto-applies to PROD on merge. **Owner:** whoever next changes `sync_strategy_analytics_status`.
    **Trigger:** any change to that function. Not data-integrity-reachable today while the
    composite fan-out is unscheduled; say so with the date. Cite by symbol, no line numbers.

    Run `check-planning-hygiene` (`node_modules/.bin/tsx scripts/check-planning-hygiene.ts`) and
    require OK. Do not touch `CHANGELOG.md`, `VERSION` or `package.json` (D-08).
  </action>
  <verify>
    <automated>grep -qF '[164.6.7-COMPOSITE-REREAD-RESIDUE]' TODOS.md && awk '/^161\.1-D13\./{f=1} f&&/^161\.1-D14\./{exit} f' TODOS.md | grep -q 'CLOSED' && awk '/^161\.1-D13\./{f=1} f&&/^161\.1-D14\./{exit} f' TODOS.md | grep -qF '164.6.7' && grep -A12 -F '[164.6.7-COMPOSITE-REREAD-RESIDUE]' TODOS.md | grep -qF 'sync_strategy_analytics_status' && git diff --quiet "$(git merge-base HEAD origin/main)" -- CHANGELOG.md VERSION package.json && echo TODOS-OK</automated>
    <fails_when>no TODOS-OK: the residue entry is missing or does not name the bridge, 161.1-D13 is not marked CLOSED with the phase, or a ledger file the ship commit owns was changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `161.1-D13` keeps its original text and gains a dated CLOSED line naming Phase 164.6 plan 02 and Phase 164.6.7.
    - The residue entry names owner, trigger, fix shape and both honour arms.
    - `check-planning-hygiene` prints OK; `CHANGELOG.md`, `VERSION`, `package.json` unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <done>The backlog tells the truth about 161.1-D13 and carries the residual window with an owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked docs -> public readers | The repo is public; runbook and TODOS prose is world-readable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.7-09 | Tampering (operational) | runbook precondition | high | mitigate | item 6 stays BLOCKING and item 4 keeps the deployed-commit check, so the restatement cannot be read as permission to schedule |
| T-164.6.7-10 | Information disclosure | runbook and TODOS prose | low | mitigate | symbol citations only, no identifiers; `check-planning-hygiene` must print OK |
</threat_model>

<verification>
- RUNBOOK-OK and TODOS-OK printed.
- `git diff --stat "$BASE03"..HEAD` after the plan's last commit lists exactly `docs/runbooks/ledger-refresh-go-live.md` and `TODOS.md`.
</verification>

<success_criteria>
- Criterion 4: the runbook precondition is restated to match the tree, with item 6 still blocking.
- The residual window has an owner in `TODOS.md` (D-03).
</success_criteria>

<output>
Create `.planning/phases/164.6.7-compositeclaimsnapshot-the-composite-run-reads-the-live-job/164.6.7-03-SUMMARY.md` when done.
</output>
