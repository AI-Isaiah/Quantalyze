---
phase: 164.6.7
round: 7
kind: narrow-confirmation (founder rule D9)
reviewer: silent-failure-hunter
scope: git diff 4a1741a39..HEAD, excluding .planning/
verdict: R6-01 CLOSED; no new CRITICAL or HIGH
---

# 164.6.7 review, round 7 (narrow confirmation)

## Q1. Is R6-01 closed? YES

- `_heal_delete_basis_series` (in `run_derive_broker_dailies_job`) now catches
  `Exception`, logs ONE ERROR line (scrubbed failure text, job id, a
  "programming error" label for `_READ_PROGRAMMING_ERRORS` members, and the
  stamp's `cause`), makes ONE tagged capture through `_capture_read_failure`,
  and returns. The `if _programming: raise` branch is gone.
  `asyncio.CancelledError` is a `BaseException` and still propagates.
- The heal has exactly one call site, `await _heal_delete_basis_series(cause=_cause)`,
  after the loud stamp write inside `_stamp_io` has returned. `_cause` is the
  scrubbed cause the stamp payload itself carries. The keyword-only argument is
  required, so a call without it would fail loudly at the call, and there is none.
- With no raise, the stamp closure returns `permanent`, so the job never reaches
  `failed_retry` and `sync_strategy_analytics_status` branch (a) cannot write
  `computing` over the landed FAILED stamp.
- Pinned by `test_a_programming_error_in_a_post_stamp_call_keeps_the_landed_stamp`:
  asserts `error_kind == "permanent"`, one analytics write carrying the cause,
  one heal line labelled "programming error" carrying the job id and the cause,
  an exact `log.error.call_count`, and `capture_exception.call_count == 1`.
- Measured: `tests/test_stamp_io_exhaustive.py`, `tests/test_ledger_refresh_gates.py`,
  `tests/test_ledger_refresh_publish_guard.py` -> 181 passed.

## Q2. Did this round's fixes introduce a NEW CRITICAL or HIGH? NO

- Heal `cause` argument: required keyword-only, one call site, value already
  scrubbed. No new failure path.
- `_flags_object_or_dropped`: object -> copy, `None` -> `{}`, anything else ->
  one ERROR (job id, type, bounded scrubbed `repr`) plus one tagged
  `capture_message`, then `{}`. It raises nothing on a bad value, so the composite
  failed stamp still lands once, and the composite success path no longer raises
  at persist on every re-stitch (the prior `failed_final` with no curated stamp).
  The persist then overwrites the bad value, so the drop cannot repeat after a
  landed write. The success-path read now returns the raw value instead of
  `dict(... or {})`; falsy non-objects (`[]`, `false`, `""`) that were silently
  coerced before are now reported. That is louder, not quieter.
- Derived `_IO_CALL_NAMES`: a fixpoint over module-level functions in
  `services/` seeded by the prior base names; a strict superset of the old list,
  guarded by a size assertion and named-helper membership assertions. Its blind
  spots (methods, functions outside `services/`, I/O through a passed callable)
  are stated in the test and the runbook. It can only widen what the scan flags.
- Sentinels: both now pin `await _heal_delete_basis_series(cause=_cause)`, the
  real awaited call form, so prose cannot satisfy them. Tests green.
- Runbook bound: recomputed per attempt from the table rows. Single-key or
  object-flags composite: 3 + 3 + 4 = 10 (13 with a provenance refusal).
  Non-object-flags composite: 5 per attempt (drop 2, breach 1, `_stamp_io`
  ERROR 1, capture 1), 15 (18). The round-5 figures are marked superseded in
  place. Arithmetic checks out against the rows.

## Lower (recorded, not fixed, by founder rule)

- LOW: the runbook's round-6 derivation assumes the 3-attempt retry budget
  without re-citing it by symbol; the figure is inherited from rounds 4-5.
