---
phase: 150-own-03-the-wizard-asks-whose-capital-this-is
plan: 03
type: execute
wave: 2
depends_on: ["150-01", "150-02"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
autonomous: true
requirements: [OWN-03]
user_setup: []

must_haves:
  truths:
    - "At allocator key-add (entryContext=contribution) the capital question renders FIRST in the categorization step, defaulted to team_review; manager sessions see no question and an identical step otherwise (D-01, D-07)"
    - "A never-touched submit is behaviour-compatible with today: status stays private, portfolio untouched, mark lands as team_review for allocators and NULL for paths that never reach the question (SC 2)"
    - "The categorization step shows only codename, description, category (+ the question); types/subtypes/markets/exchanges/leverage/AUM/max-capacity live behind a collapsed 'More details (optional)' disclosure — render-only cull, zero payload change (SC 1b, D-05/D-06)"
    - "The asset-class select is hoisted OUT of the disclosure when it is editable (!assetClassLocked) — the √252-on-crypto default never hides (Pitfall 4, research Open Q5 resolved: hoist)"
    - "The wizard writes NO position, asks NO amount, offers NO allocate-now shortcut (D-02, D-10); the mark persists via a separate post-finalize UPDATE, never a 14th RPC arg"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx"
      provides: "question-first + culled step with native details disclosure"
      contains: "CapitalOwnershipRadioGroup"
    - path: "src/app/api/strategies/finalize-wizard/route.ts"
      provides: "closed-set validation + post-RPC owner-scoped mark UPDATE"
      contains: "capital_ownership"
  key_links:
    - from: "WizardClient.tsx entryContext"
      to: "MetadataStep showCapitalQuestion prop"
      via: "entryContext === \"contribution\""
      pattern: "showCapitalQuestion"
    - from: "finalize-wizard/route.ts"
      to: "strategies.capital_ownership"
      via: "supabase.from(\"strategies\").update({ capital_ownership }) AFTER the 13-arg RPC returns"
      pattern: "capital_ownership"
---

<objective>
Make the wizard ask whose capital this is — the question the product never asked — and cull
the categorization step to essentials, exactly as the founder specified. Persist the answer
as the strategy-level mark via a separate owner-scoped UPDATE after the finalize RPC, keeping
the 13-arg SECURITY DEFINER signature byte-untouched and minting ZERO new wizard error codes.

Purpose: SC 1 + SC 1b. This is the mark's primary write path; a lost mark degrades to NULL,
which is non-allocatable — the safe state by design.
Output: modified MetadataStep/WizardClient/SubmitStep, finalize-wizard route with the mark
persistence, extended tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-UI-SPEC.md
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-PATTERNS.md
@DESIGN.md
</context>

<interfaces>
Consumed from Plan 02 (already merged):
```ts
import { CapitalOwnership, TEAM_REVIEW } from "@/lib/capital-ownership";
import { CapitalOwnershipRadioGroup } from "@/components/strategy/CapitalOwnershipRadioGroup";
```
Minted here:
- `MetadataStepProps` gains `showCapitalQuestion?: boolean` (default false → question absent, today's step).
- `MetadataDraft` gains `capitalOwnership?: CapitalOwnership` (present ONLY when the question rendered; defaulted to TEAM_REVIEW on mount when rendered).
- finalize-wizard request body gains optional `capital_ownership?: "own_capital" | "team_review"` — absent field = no write (strategy stays NULL).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MetadataStep — question first, cull to disclosure, asset-class hoist</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx (FULL — MetadataDraft :28-45, MetadataStepProps :46-56, assetClassLocked derivation :100-110, handleSubmit/onComplete :199-211, the cull region :282-366, submit gate :377, InlineChipGroup :386-419)
    - src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.test.tsx (EXISTS — extend, do not create; research assumption A4 was wrong)
    - src/components/ui/CollapsibleSection.tsx:135-147 (ONLY the CSS caret idiom — group class + rotate-90 triangle; do NOT use the component itself: localStorage persistence + open-all listener + uppercase-mono voice are wrong for a transient form control)
    - src/components/strategy/StrategyTable.tsx:1014-1018 (bare details/summary in-repo precedent)
    - 150-UI-SPEC.md Surface 1 (step order, disclosure anatomy, copy) + Copywriting Contract
  </read_first>
  <behavior>
    - showCapitalQuestion=true → the fieldset renders as the FIRST interactive element below the heading, option (b) preselected; showCapitalQuestion absent/false → zero question DOM, step otherwise identical
    - Disclosure: `<details>` collapsed by default; summary text `More details (optional)`; open reveals the seven culled controls in current order
    - assetClassLocked=false (CSV/unknown-exchange path) → the asset-class select renders OUTSIDE the disclosure (visible); assetClassLocked=true → it sits INSIDE the disclosure (inert, disabled, auto-detected)
    - Cull is render-only: an untouched submit's onComplete payload is deep-equal to today's (all culled fields still present with their defaults); with the question rendered and untouched, payload additionally carries capitalOwnership === "team_review"
    - Submit gate unchanged: disabled logic still exactly `!description.trim() || !categoryId` (wizard validation UX is Phase 153's — do not widen)
  </behavior>
  <action>
    1. Step copy (UI-SPEC Copywriting Contract, binding): heading → `Describe this strategy`; intro → `Codename, description, and category are all we need. Everything else is optional.`
    2. Add `showCapitalQuestion?: boolean` to MetadataStepProps. When true, mount `<CapitalOwnershipRadioGroup label="Whose capital is in this key?" ...>` as step item 2 (after heading, before codename), state initialized to TEAM_REVIEW (D-01: (b) is the DEFAULT, never null). Add `capitalOwnership?: CapitalOwnership` to MetadataDraft and include it in onComplete ONLY when the question rendered (manager path leaves it undefined → NULL mark → safe).
    3. Move the cull region (:282-366 — Strategy Types, Subtypes, Markets + micro notes, Supported exchanges, Leverage/AUM/Max-capacity grid) verbatim, in current order, into a native `<details className="group">` + `<summary>` block: summary = CSS caret (CollapsibleSection triangle idiom, rotates via `group-open:rotate-90`, `var(--color-text-muted)`) + label `More details (optional)` in `text-caption font-medium text-text-secondary`, `list-none`, `min-h-[44px]`; contents keep `space-y-6`, 16px summary→first control. No storageKey, no persistence.
    4. Asset-class select (Pitfall 4, decided: HOIST): render it inside the disclosure ONLY when `assetClassLocked`; when `!assetClassLocked` render it outside (between category and the disclosure) with its existing label/options untouched — a two-branch placement conditional, nothing else changes. Comment the reason: editable asset class defaults to traditional → √252 on a crypto CSV book inflates Sharpe; it must not hide (150-RESEARCH § Pitfall 4).
    5. D-08 consumer sweep is discharged by construction and must be ASSERTED: MetadataDraft keys (minus the one new optional), the onComplete payload keys, and the submit gate are unchanged — encode as the deep-equal payload test in the behavior block. Fields are collapsed, never deleted; downstream factsheet panels/browse pills/mandate chips keep their absent-hides-panel behavior with zero server change (150-RESEARCH § Phase Requirements OWN-03 1b).
    6. Extend MetadataStep.test.tsx per the behavior block.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts question-first ordering (DOM order assertion), team_review default, question absent when prop false
    - Test asserts untouched-submit payload deep-equality with the pre-change shape (render-only cull proof)
    - `grep -c "CollapsibleSection" src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx` == 0 (bare details, not the component)
    - `grep -c "storageKey\|useCrossTabStorage" src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx` == 0
    - Both assetClassLocked branches asserted (inside vs outside the disclosure)
  </acceptance_criteria>
  <done>Question-first culled step ships with the hoist; payload byte-compatible; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Thread the answer — WizardClient → MetadataStep → SubmitStep → finalize body</name>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx:74, 147, 157, 932-1000 (entryContext declaration, default "manager", isContribution derivation, the SubmitStep thread — the exact prop-thread pattern to extend)
    - src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx (find where the finalize-wizard request body is assembled — entry_context already travels there; the mark rides the same body)
    - 150-PATTERNS.md §10 ("trusted context selector, not a client-trusted privilege flag" — copy that reasoning into the code comment)
  </read_first>
  <action>
    In WizardClient: pass `showCapitalQuestion={entryContext === "contribution"}` into the MetadataStep mount (one prop on the existing thread — D-07 is a RENDER condition, not authz; comment says so, citing the finalize-wizard entry_context reasoning at route.ts:452-453). Carry `capitalOwnership` from the metadata draft through wizard state into SubmitStep exactly the way entry_context travels, and include it in the finalize-wizard request body as `capital_ownership` ONLY when defined (undefined must serialize to an ABSENT field, not null). No other wizard behavior changes; no allocate-now affordance anywhere (D-10).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/strategies/new/wizard" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "showCapitalQuestion" src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx` shows the contribution-derived prop
    - `grep -c "allocate" src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx` unchanged from before this plan (no allocate-now shortcut)
    - Existing wizard test files under the wizard tree stay green with zero assertion edits
  </acceptance_criteria>
  <done>Mark travels client→server on the existing entry_context thread; manager path sends nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: finalize-wizard — validate + persist the mark AFTER the RPC (no signature change, no new wizard code)</name>
  <files>src/app/api/strategies/finalize-wizard/route.ts, src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/finalize-wizard/route.ts:440-500 (the entry_context closed-set validation arm :445-469 — the shape to copy for capital_ownership; the payload fields :476-499 that must stay untouched; the withAuth wrapper :609)
    - src/app/api/strategies/finalize-wizard/route.ts:1182-1212 (the RPC call site — where "after the RPC returns" is)
    - src/app/api/strategies/finalize-wizard/route.test.ts (extend; find the existing validatePayload 400-arm test shape)
    - 150-RESEARCH.md § Alternatives Considered row 1 (why the separate UPDATE beats a 14th RPC arg) and § Pitfall 7 (why NO new wizard error code)
  </read_first>
  <behavior>
    - Body with `capital_ownership: "own_capital"` → after the RPC succeeds, the strategy row carries the mark (assert the update chain was called with `{ capital_ownership: "own_capital" }`, `.eq("id", <finalized id>)`, `.eq("user_id", <user>)`)
    - Body with `capital_ownership: "team_review"` → same, with team_review
    - Body WITHOUT the field → NO strategies update call for capital_ownership (strategy stays NULL)
    - Body with `capital_ownership: "garbage"` → 400 BEFORE the RPC, same envelope shape as the entry_context garbage arm (copy that arm's response shape exactly — no new code string is minted)
    - Mark-update failure (supabase error) → finalize response is UNCHANGED (success), error is console.error-logged with bound message + strategy id — a lost mark degrades to NULL = non-allocatable = safe; it must NOT become a wizard failure arm (Pitfall 7: the v0.53.3.1 roster invariant means any new code renders the UNKNOWN card)
  </behavior>
  <action>
    1. In validatePayload (or the equivalent validation section), accept optional `capital_ownership`; when present and not in `["own_capital","team_review"]`, return 400 mirroring the entry_context arm at :445-469 byte-for-byte in shape. Comment: closed set mirrored by the DB CHECK `strategies_capital_ownership_check`.
    2. After the finalize RPC returns successfully (post :1182-1212), when the field is present: `await supabase.from("strategies").update({ capital_ownership }).eq("id", finalizedStrategyId).eq("user_id", user.id).select("id")` — the explicit user_id predicate is load-bearing (strategies_update RLS has no WITH CHECK). On error or zero rows: console.error with bound error message + ids, then CONTINUE to the normal success response. Comment the degradation contract: NULL = non-allocatable, safe; deliberate non-atomicity, decided against a 13→14-arg DROP/CREATE of the SECURITY DEFINER RPC (150-RESEARCH § Alternatives Considered).
    3. Extend route.test.ts per the behavior block (five cases).
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/strategies/finalize-wizard/route.test.ts" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "finalize_wizard_strategy" supabase/schema/functions/finalize_wizard_strategy.sql` unchanged; NO new migration touches the RPC; `grep -rn "p_capital_ownership" supabase/ src/` == 0 (signature untouched)
    - The 400 arm reuses the existing envelope shape — `git diff` shows no new member added to KNOWN_CREATE_WITH_KEY_CODES / KNOWN_ADD_KEY_CODES and no new string in wizardErrors
    - The mark UPDATE appears AFTER the rpc call in source order and is guarded by field presence
    - All five behavior cases green
  </acceptance_criteria>
  <done>Mark persists post-finalize with safe degradation; zero wizard error surface change; RPC signature untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → finalize-wizard route | client-supplied `capital_ownership` value crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-09 | Tampering | Garbage/oversized `capital_ownership` in the finalize body | mitigate | closed-set validation with hard 400 (entry_context arm shape) + DB CHECK as second tier |
| T-150-10 | Elevation of Privilege | Mark UPDATE hitting another user's strategy | mitigate | `.eq("id", finalizedId).eq("user_id", user.id)` inline + RLS strategies_update; zero-rows logged |
| T-150-11 | Repudiation / integrity | Mark silently lost when the post-RPC UPDATE fails | accept | deliberate: degrades to NULL = non-allocatable (safe state); logged with ids; alternative (14-arg SECURITY DEFINER DROP/CREATE on the wizard critical path) judged higher-risk — recorded per research |
| T-150-12 | Information Disclosure | A manager path accidentally rendering/writing allocator UI | accept | D-07 is a render condition, not authz — a manager marking their own strategy commits no violation (research Pattern 4); no privilege attaches to the flag |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed this phase |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.test.tsx" "src/app/api/strategies/finalize-wizard/route.test.ts" --no-file-parallelism` green
- Collision gates green: `npx vitest run src/__tests__/phase-84-asset-class-flow.test.ts src/lib/visibility.test.ts --no-file-parallelism`
- `npx tsc --noEmit` + `npm run lint` clean
</verification>

<success_criteria>
- D-01/D-02/D-05/D-06/D-07/D-10 all observable in the running wizard; SC 1 + 1b satisfied.
- A never-touched allocator submit and every manager submit are behaviour-compatible with today (portfolio untouched, status private).
- Zero new wizard error codes; 13-arg RPC untouched.
</success_criteria>

<output>
Create `.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-03-SUMMARY.md` when done
</output>
