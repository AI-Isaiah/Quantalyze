---
phase: 150-own-03-the-wizard-asks-whose-capital-this-is
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260806120000_strategies_capital_ownership.sql
  - supabase/tests/test_capital_ownership_column.sql
  - supabase/tests/test_capital_ownership_allocation_guard.sql
autonomous: false
requirements: [OWN-03]
user_setup: []

must_haves:
  truths:
    - "strategies.capital_ownership exists as a nullable TEXT column constrained to ('own_capital','team_review') with NO default and NO backfill — legacy rows stay NULL"
    - "An INSERT (including the INSERT arm of an ON CONFLICT upsert) into portfolio_strategies RAISEs when the strategy is marked team_review (UNCONDITIONALLY — SC 2b literally) OR when the strategy is SELF-OWNED (strategy.user_id = inserting portfolio's owner) and not marked own_capital — D-03-A amended predicate"
    - "A THIRD-PARTY insert (strategy owner ≠ portfolio owner, mark NULL) still SUCCEEDS — the shipped AddToPortfolio/MigrationWizard/seed paths are untouched (B-1 regression)"
    - "An alias UPDATE on a legacy portfolio_strategies row whose strategy is unmarked still succeeds (the trigger is INSERT-scoped, never OR UPDATE)"
    - "A single RPC flips a mark own_capital→team_review and removes the caller's positions in ONE transaction"
    - "The migration is applied to the TEST project before any dependent code merges, and both pgTAP files run green against TEST"
  artifacts:
    - path: "supabase/migrations/20260806120000_strategies_capital_ownership.sql"
      provides: "column + CHECK + D-03-A trigger + flip RPC + grants + self-verification"
      contains: "BEFORE INSERT"
    - path: "supabase/tests/test_capital_ownership_column.sql"
      provides: "pgTAP column-shape assertions"
    - path: "supabase/tests/test_capital_ownership_allocation_guard.sql"
      provides: "pgTAP trigger positive/negative/null/third-party + legacy-alias regression + PK duplicate + flip RPC atomicity"
  key_links:
    - from: "portfolio_strategies BEFORE INSERT trigger"
      to: "strategies.capital_ownership + strategies.user_id + portfolios.user_id"
      via: "guard_allocation_requires_own_capital() lookups (mark + owner-equality conjunct)"
      pattern: "capital_ownership"
    - from: "flip_capital_ownership_to_team_review"
      to: "portfolio_strategies DELETE + strategies UPDATE"
      via: "single plpgsql transaction"
      pattern: "flip_capital_ownership_to_team_review"
---

<objective>
Ship the persistent ownership mark at the DATABASE tier: the `capital_ownership` column on
`strategies` (D-04: strategy-level), the D-03 hard invariant as a `BEFORE INSERT` trigger on
`portfolio_strategies` (the ONLY tier that holds against the two browser-side inserts at
`AddToPortfolio.tsx:54` and `MigrationWizard.tsx:72` and against direct PostgREST calls),
and the transactional mark-flip RPC that closes the "stranded position" hole. Prove all of it
with pgTAP (the only DB gate that actually runs in CI), then apply to TEST before merge.

⚠️ The trigger predicate is the AMENDED D-03-A form (CONTEXT amendment 2026-08-06, orchestrator
decision at plan-check): RAISE when mark = 'team_review' (unconditional) OR (strategy owner =
inserting portfolio's owner AND mark IS DISTINCT FROM 'own_capital'). A blanket
`IS DISTINCT FROM 'own_capital'` predicate would DELETE three shipped third-party allocation
paths — discovery AddToPortfolio.tsx:54 (other-owner published strategies), manager
MigrationWizard.tsx:72 (allocation with amount), and scripts/seed-full-app-demo.ts:1697,1929 —
all of which insert positions for strategies owned by OTHER users, unmarkable by the allocator
(owner-authz). Self-owned NULL (never-asked) stays non-allocatable (SC 3).

Purpose: every other plan in this phase builds on this column and this invariant. Route-level
checks in later plans are for clean errors only; THIS is the enforcement.
Output: one migration, two pgTAP files, migration applied to TEST (qmnijlgmdhviwzwfyzlc).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-CONTEXT.md
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-RESEARCH.md
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-PATTERNS.md
</context>

<interfaces>
Names this plan MINTS — downstream plans import/call these exactly:

- Column: `public.strategies.capital_ownership TEXT NULL` — values `'own_capital' | 'team_review'`, constraint name `strategies_capital_ownership_check`.
- Trigger function: `public.guard_allocation_requires_own_capital()` — trigger name `trg_portfolio_strategies_own_capital_only`, `BEFORE INSERT ON public.portfolio_strategies FOR EACH ROW`. Predicate (D-03-A): RAISE when `v_mark = 'team_review'` OR (`v_strategy_owner = v_portfolio_owner` AND `v_mark IS DISTINCT FROM 'own_capital'`); third-party inserts pass.
- RPC: `public.flip_capital_ownership_to_team_review(p_strategy_id uuid) RETURNS TABLE (removed_positions integer, updated_strategies integer)` — `SECURITY INVOKER`, `GRANT EXECUTE TO authenticated`, `REVOKE` from `PUBLIC, anon`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration (column + CHECK + D-03-A trigger + flip RPC) with pgTAP proof</name>
  <files>supabase/migrations/20260806120000_strategies_capital_ownership.sql, supabase/tests/test_capital_ownership_column.sql, supabase/tests/test_capital_ownership_allocation_guard.sql</files>
  <read_first>
    - supabase/migrations/20260716130000_strategies_status_private.sql (header rationale, pre-flight DO, DROP-then-ADD constraint, self-verifying DO — the composition analog)
    - supabase/migrations/20260409202758_portfolio_strategies_alias.sql (ADD COLUMN IF NOT EXISTS + COMMENT ON COLUMN idiom)
    - supabase/migrations/20260716131000_guard_strategies_publish_transition.sql:44-59 (trigger fn idiom: plpgsql, SET search_path, RAISE with ERRCODE)
    - supabase/migrations/20260716130500_finalize_terminal_status_param.sql (grants after CREATE FUNCTION: REVOKE ALL FROM PUBLIC, anon; GRANT EXECUTE TO authenticated)
    - supabase/migrations/20260405061911_initial_schema.sql:138-143 (portfolio_strategies composite PK — D-13's structural guarantee)
    - supabase/migrations/20260405061912_rls_policies.sql:28-32, 67-69 (strategies_read / strategies_update / portfolio_strategies_owner — why NO RLS change is needed)
    - src/components/portfolio/AddToPortfolio.tsx:54 + src/components/portfolio/MigrationWizard.tsx:72 + scripts/seed-full-app-demo.ts:1697,1929 (the three THIRD-PARTY insert paths the D-03-A owner-equality conjunct exists to preserve — read to confirm strategy owner ≠ portfolio owner in each)
    - supabase/tests/ — pick one recent test_*.sql for the pgTAP harness shape (plan/ok/finish, fixture creation)
    - 150-RESEARCH.md § Schema Findings 1-3, § Pitfall 1, § Pitfall 2, § Code Examples (the trigger body sketch — AMEND its predicate per D-03-A; the sketch predates the plan-check decision)
  </read_first>
  <behavior>
    pgTAP cases (write these FIRST; they are the oracle):
    - test_capital_ownership_column.sql: column exists on strategies; is nullable; has NO default; CHECK constraint `strategies_capital_ownership_check` rejects a value outside ('own_capital','team_review'); accepts both members and NULL.
    - test_capital_ownership_allocation_guard.sql:
      1. INSERT position for a SELF-OWNED own_capital-marked strategy SUCCEEDS.
      2. INSERT position for a team_review-marked strategy RAISEs (check_violation) — assert for BOTH the self-owned case AND the third-party case (the team_review arm is UNCONDITIONAL: SC 2b literally).
      3. INSERT position for a SELF-OWNED NULL-marked strategy RAISEs (NULL and team_review are BOTH non-allocatable for self-owned rows — two logic states).
      4. Upsert-edit path: `INSERT ... ON CONFLICT (portfolio_id, strategy_id) DO UPDATE` on a row whose self-owned strategy was later un-marked ALSO RAISEs — Postgres fires BEFORE INSERT triggers on the ON CONFLICT insert attempt; this is deliberate (an amount edit also requires the mark) and must be pinned so nobody "fixes" it.
      5. Legacy-alias regression (Pitfall 2): create a position while the strategy is own_capital, then flip the strategy mark to NULL via direct UPDATE on strategies (simulating a legacy row), then `UPDATE portfolio_strategies SET alias='x'` on it — MUST SUCCEED (proves the trigger has no UPDATE arm).
      6. Duplicate-add (D-13/SC4): a second plain INSERT of the same (portfolio_id, strategy_id) violates the composite PRIMARY KEY.
      7. Flip RPC: with a live position, `SELECT * FROM flip_capital_ownership_to_team_review(<id>)` leaves mark='team_review' AND zero positions — one call; calling it for a strategy the current role does not own affects zero rows and removes nothing.
      8. THIRD-PARTY regression (B-1 — the INSERT-side twin of case 5): create user A's portfolio and user B's strategy with mark NULL (the AddToPortfolio/MigrationWizard/seed shape), then INSERT a position into A's portfolio for B's strategy — MUST SUCCEED. Also assert the same succeeds when B's strategy is marked own_capital. Label the case "third-party" in its description string so the file-level grep gate finds it.
  </behavior>
  <action>
    Create migration `supabase/migrations/20260806120000_strategies_capital_ownership.sql` (BEGIN/COMMIT wrapped) with FIVE parts, composing the two analog migrations:

    1. HEADER comment block stating, explicitly: (a) D-04 — the mark is STRATEGY-level; (b) the column is NULLABLE with NO DEFAULT and NO backfill — a `DEFAULT 'team_review' NOT NULL` would stamp Black Swan / Alpha Centauri / Arctic Fox as team-review, a fabricated claim (no-invented-data); (c) THREE display states (`NULL` / own_capital / team_review) collapse to TWO logic states in the allocatable predicate — NULL and team_review are both non-allocatable for SELF-OWNED strategies; do not "simplify"; (d) D-03-A predicate rationale (CONTEXT amendment 2026-08-06): the trigger scopes the not-own_capital arm to SELF-OWNED strategies because three shipped paths insert positions for OTHER-owner strategies (AddToPortfolio.tsx:54, MigrationWizard.tsx:72, seed-full-app-demo.ts:1697,1929) which the allocator cannot mark (owner-authz); the team_review arm stays UNCONDITIONAL per SC 2b; (e) NO RLS change is needed and why (strategies_read = `status='published' OR user_id=auth.uid()`, strategies_update = `user_id=auth.uid()`; a column add changes neither — mirror the strategies_status_private.sql:20-27 header); (f) CONSCIOUS ACCEPTANCE: `capital_ownership` is publicly readable on published rows via the strategies_read splat — the value states "this allocator has capital in this", judged non-sensitive; UI-SPEC invariant 3 is a RENDER invariant (research Open Question 4, resolved: accept).
    2. `ALTER TABLE public.strategies ADD COLUMN IF NOT EXISTS capital_ownership TEXT;` + `COMMENT ON COLUMN` documenting NULL semantics ("never asked — non-allocatable for self-owned, renders no tag; remedy is the Mark dialog retro path"). Then pre-flight DO block (RAISE listing offending values — vacuously clean on a fresh column, keeps the idiom), then DROP-then-ADD `CONSTRAINT strategies_capital_ownership_check CHECK (capital_ownership IN ('own_capital','team_review'))`.
    3. Trigger: function `guard_allocation_requires_own_capital()` — plpgsql, `SET search_path = public, pg_catalog`. Body: `SELECT s.capital_ownership, s.user_id INTO v_mark, v_strategy_owner FROM strategies s WHERE s.id = NEW.strategy_id;` then `SELECT p.user_id INTO v_portfolio_owner FROM portfolios p WHERE p.id = NEW.portfolio_id;` then `IF v_mark = 'team_review' OR (v_strategy_owner = v_portfolio_owner AND v_mark IS DISTINCT FROM 'own_capital') THEN RAISE EXCEPTION ... USING ERRCODE = 'check_violation'` — with an in-function comment naming the three third-party paths the owner-equality conjunct preserves. Then `CREATE TRIGGER trg_portfolio_strategies_own_capital_only BEFORE INSERT ON public.portfolio_strategies FOR EACH ROW EXECUTE FUNCTION guard_allocation_requires_own_capital();` — ⛔ INSERT ONLY, comment in the file phrased EXACTLY as: "an UPDATE arm breaks the shipped alias UPDATE on every legacy row whose strategy is unmarked (150-RESEARCH § Pitfall 2)" — W-4: the comment must say "an UPDATE arm", NEVER the literal token OR UPDATE, so the anchored trigger-statement guard in this task's verify cannot be greened (or defeated) by editing documentation. Grep all prior migrations for `guard_allocation_requires_own_capital` first (re-base rule) — it is greenfield, but run the grep and say so in the header.
    4. RPC `flip_capital_ownership_to_team_review(p_strategy_id uuid) RETURNS TABLE (removed_positions integer, updated_strategies integer)` — `LANGUAGE plpgsql SECURITY INVOKER SET search_path = public, pg_catalog`. Body, one transaction by construction: DELETE FROM portfolio_strategies WHERE strategy_id = p_strategy_id AND portfolio_id IN (SELECT id FROM portfolios WHERE user_id = auth.uid()); then UPDATE strategies SET capital_ownership='team_review' WHERE id = p_strategy_id AND user_id = auth.uid(); return both row counts (GET DIAGNOSTICS). SECURITY INVOKER means RLS also applies; the explicit auth.uid() predicates are load-bearing regardless (strategies_update has no WITH CHECK). After CREATE: `REVOKE ALL ON FUNCTION ... FROM PUBLIC, anon; GRANT EXECUTE ON FUNCTION ... TO authenticated;`.
    5. Self-verifying DO block: assert via `pg_get_constraintdef(oid)` that the CHECK contains BOTH values; assert via information_schema.triggers that `trg_portfolio_strategies_own_capital_only` fires on exactly the INSERT event (a row for INSERT exists, NO row for UPDATE/DELETE); assert via `pg_get_functiondef` that the trigger function body contains both the `team_review` literal and a `portfolios` lookup (the two D-03-A arms); RAISE EXCEPTION otherwise.

    Then write the two pgTAP files per the behavior block. ⚠️ Shared-TEST discipline (STATE Phase-142.1 item 5 class): every fixture row is freshly created inside the test's own transaction with its own UUIDs — the third-party cases need TWO distinct user_id values on their fixture rows (the trigger reads table columns, not auth.uid(), so plain fixture rows suffice) — NO table-wide UPDATE/DELETE, every mutation predicate carries the fixture's own ids. pgTAP runs in a rolled-back transaction; keep it that way.
  </action>
  <verify>
    <automated>test -f supabase/tests/test_capital_ownership_column.sql && test -f supabase/tests/test_capital_ownership_allocation_guard.sql && grep -q "BEFORE INSERT ON public.portfolio_strategies" supabase/migrations/20260806120000_strategies_capital_ownership.sql && ! grep "BEFORE INSERT" supabase/migrations/20260806120000_strategies_capital_ownership.sql | grep -q "OR UPDATE" && grep -qi "third-party" supabase/tests/test_capital_ownership_allocation_guard.sql</automated>
  </verify>
  <acceptance_criteria>
    - W-4 anchored trigger-scope guard: `grep -c "BEFORE INSERT ON public.portfolio_strategies" supabase/migrations/20260806120000_strategies_capital_ownership.sql` ≥ 1 AND `! grep "BEFORE INSERT" supabase/migrations/20260806120000_strategies_capital_ownership.sql | grep -q "OR UPDATE"` exits 0 — the no-OR-UPDATE assertion anchors to the CREATE TRIGGER statement's own BEFORE INSERT line, not the whole file, and the rationale comment says "an UPDATE arm" (never the literal token), so the guard is immune to comment edits in both directions
    - The trigger function body contains BOTH D-03-A arms: the unconditional `= 'team_review'` comparison AND the owner-equality conjunct with a `portfolios` lookup (`grep -c "FROM portfolios" supabase/migrations/20260806120000_strategies_capital_ownership.sql` ≥ 2 — trigger + RPC)
    - Migration contains `ADD COLUMN IF NOT EXISTS capital_ownership TEXT` with NO `DEFAULT` and NO `NOT NULL` on that column, and NO UPDATE-backfill statement touching existing strategies rows
    - Migration contains `strategies_capital_ownership_check`, both literals `'own_capital'` and `'team_review'`, `SECURITY INVOKER`, `REVOKE ALL`, `GRANT EXECUTE`, and a self-verifying DO block reading `pg_get_constraintdef`
    - Both pgTAP files exist and together contain the EIGHT behavior cases (grep for `ON CONFLICT`, alias UPDATE case, PK-violation case, `flip_capital_ownership_to_team_review`, and the "third-party" case labels)
    - No pgTAP statement mutates rows it did not create in-test (manual read of every UPDATE/DELETE predicate)
  </acceptance_criteria>
  <done>Migration + two pgTAP files exist, encode all eight behaviors (incl. the third-party regression twin), and the migration self-verifies its own constraint, trigger scoping, and both predicate arms.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply the migration to TEST via MCP (orchestrator-only), then run pgTAP against TEST</name>
  <files>supabase/migrations/20260806120000_strategies_capital_ownership.sql</files>
  <what-built>The Phase-150 schema migration (column + CHECK + D-03-A trigger + flip RPC) exists in supabase/migrations/ but is not applied anywhere. MCP tools are stripped from subagents (upstream #13898), so this apply MUST happen in the orchestrator session or by the developer.</what-built>
  <action>
    Orchestrator/developer: run Supabase MCP `apply_migration` against the TEST project `qmnijlgmdhviwzwfyzlc` with the FULL contents of `supabase/migrations/20260806120000_strategies_capital_ownership.sql` and name `strategies_capital_ownership`. ⛔ NEVER against PROD `khslejtfbuezsmvmtsdn` — merging to main auto-applies to PROD later. Note MEMORY `feedback_supabase_apply_migration_drift`: MCP stamps `now()` — the local filename timestamp (20260806120000) and the stamped TEST timestamp will drift; expected, record it. Fallback if MCP is unavailable: `supabase db push` scoped to TEST with explicit credentials. Do NOT skip — the phase cannot pass verification unapplied (build/type checks pass without the live column, a false-positive verification state). AFTER the apply, run the automated pgTAP verify below — it is the falsifiable settlement of this plan (a wrong trigger predicate, a missing arm, or a broken third-party path turns it RED).
  </action>
  <verify>
    <automated>psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_capital_ownership_column.sql && psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_capital_ownership_allocation_guard.sql</automated>
  </verify>
  <how-to-verify>
    1. Run the automated pgTAP command above (the same shape as the CI loop at .github/workflows/ci.yml:1024-1030) — ALL cases green, including the third-party regression case.
    2. Confirm on TEST: `select column_name, is_nullable, column_default from information_schema.columns where table_name='strategies' and column_name='capital_ownership';` → one row, `YES`, null default.
    3. Confirm on TEST: `select event_manipulation from information_schema.triggers where trigger_name='trg_portfolio_strategies_own_capital_only';` → exactly `INSERT` (no UPDATE/DELETE rows).
  </how-to-verify>
  <acceptance_criteria>
    - The pgTAP psql run exits 0 against TEST with all cases green (pasted at the checkpoint)
    - Both information_schema query outputs pasted at the checkpoint match the expected values above
    - apply_migration targeted qmnijlgmdhviwzwfyzlc (TEST), never khslejtfbuezsmvmtsdn
  </acceptance_criteria>
  <done>Migration live on TEST; pgTAP green against TEST (incl. third-party regression); column nullable with no default; trigger INSERT-scoped — confirmed by pasted outputs.</done>
  <resume-signal>Reply "applied" with the pgTAP tail + two query outputs pasted, or describe the failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PostgREST | authenticated users can INSERT/UPDATE portfolio_strategies and UPDATE strategies directly under RLS, bypassing all routes (AddToPortfolio.tsx:54, MigrationWizard.tsx:72) |
| anon → strategies_read | anon can SELECT every column of published strategies, including the new one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-01 | Elevation of Privilege | portfolio_strategies INSERT (any path, incl. direct PostgREST) creating a position from a team_review strategy or a self-owned NULL strategy | mitigate | BEFORE INSERT trigger `guard_allocation_requires_own_capital` (D-03-A predicate) — RAISEs check_violation; pinned by pgTAP cases 2-4 |
| T-150-02 | Tampering / integrity | Mark flip own→team leaving a stranded position (two sequential writes, second fails) | mitigate | `flip_capital_ownership_to_team_review` — DELETE + UPDATE in one plpgsql transaction; pgTAP case 7 |
| T-150-03 | Tampering | Duplicate position minted for the same (portfolio, strategy) | mitigate | composite PRIMARY KEY (already shipped); pgTAP case 6 pins it as the D-13 mechanism |
| T-150-04 | Information Disclosure | `capital_ownership` readable by anon on published rows via strategies_read splat | accept | value is "this allocator has capital in this" — judged non-sensitive; acceptance written into the migration header so it is a decision, not a discovery (research Open Q4) |
| T-150-05 | Elevation of Privilege | flip RPC invoked with another user's strategy_id | mitigate | SECURITY INVOKER + RLS + explicit `auth.uid()` predicates in both statements; pgTAP case 7 non-owner arm proves zero rows affected |
| T-150-06 | Denial of Service (data) | UPDATE-scoped trigger breaking the shipped alias UPDATE on legacy unmarked rows | mitigate | trigger is INSERT-scoped only; pgTAP case 5 is the regression pin; migration self-check asserts no UPDATE event |
| T-150-37 | Denial of Service (data) | Over-broad trigger predicate deleting the three shipped THIRD-PARTY allocation paths (discovery AddToPortfolio, manager MigrationWizard, demo seed) whose strategies the allocator cannot mark | mitigate | D-03-A owner-equality conjunct scopes the not-own_capital arm to self-owned strategies; team_review arm stays unconditional; pgTAP case 8 is the INSERT-side regression twin; migration self-check asserts both arms present |
| T-150-38 | Denial of Service (UX) | Discovery AddToPortfolio on the viewer's OWN unmarked/team_review published strategy: the D-03-A trigger RAISEs check_violation (23514) and the shipped component renders a generic opaque failure | accept | accepted-with-consequence (W-6 decision): the DB tier MUST fire here — that is the invariant working; the UX consequence is mitigated in Plan 06 (AddToPortfolio maps 23514 → "This strategy isn't marked as your own capital — mark it in My Strategies first", test-asserted) |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- Migration file passes its own self-verifying DO blocks when applied (Task 2 confirms on TEST).
- Both pgTAP files are discovered by the CI glob `supabase/tests/test_*.sql` and run green against TEST post-apply (Task 2's psql command — the CI loop shape at .github/workflows/ci.yml:1024-1030).
- `grep -rn "capital_ownership" supabase/migrations/` matches exactly the one new file.
</verification>

<success_criteria>
- Column, CHECK, INSERT-scoped D-03-A trigger (unconditional team_review arm + owner-equality conjunct), and transactional flip RPC exist in one committed migration with the six header decisions written down.
- Eight pgTAP behaviors encoded across the two test files (incl. the third-party regression twin), fixture-scoped for the shared TEST DB.
- Migration applied to TEST (qmnijlgmdhviwzwfyzlc), pgTAP green against TEST, confirmed with the two information_schema queries.
</success_criteria>

<output>
Create `.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-01-SUMMARY.md` when done
</output>
