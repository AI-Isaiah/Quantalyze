---
phase: 150-own-03-the-wizard-asks-whose-capital-this-is
plan: 08
type: execute
wave: 4
depends_on: ["150-01", "150-02", "150-03", "150-04", "150-05", "150-06", "150-07"]
files_modified:
  - src/__tests__/phase-150-capital-ownership-invariant.test.ts
  - .planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-VALIDATION.md
autonomous: true
requirements: [OWN-03, OWN-05]
user_setup: []

must_haves:
  truths:
    - "D-03 is asserted structurally, phase-gate style: the trigger's existence and INSERT-only scoping, the single-source predicate, the write census, the atomic flip, and the current_weight non-goal are all mutation-proven pins (SC 2b, SC 3)"
    - "Every pin went RED under a semantic production mutation, observed first-hand, failure pasted, reverted by re-editing (Rule 9 ledger)"
    - "All collision gates (147/148/149, visibility B10, no-store, format-percent, phase-84) are green on the full phase diff"
    - "150-VALIDATION.md is complete: per-task map statuses, falsifiability ledger Observed column, oracle-independence checklist, nyquist_compliant: true"
  artifacts:
    - path: "src/__tests__/phase-150-capital-ownership-invariant.test.ts"
      provides: "the D-03 structural gate with mutation ledger"
      min_lines: 300
    - path: ".planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-VALIDATION.md"
      provides: "completed validation contract"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "phase-150 gate"
      to: "supabase/migrations/20260806120000_strategies_capital_ownership.sql"
      via: "source pins on BEFORE INSERT + absence of OR UPDATE"
      pattern: "BEFORE INSERT"
---

<objective>
Author the Phase-150 structural gate — the "assert it like the visibility gates" half of the
D-03 hard invariant — with a first-hand mutation ledger, then close the phase's validation
contract and run the full regression surface.

Purpose: SC 2b/SC 3's structural assertion + the phase's falsifiability evidence. Without
this plan, D-03 is a trigger plus good intentions; with it, every future edit that weakens
the invariant reddens CI with a pin that names the offender.
Output: the gate test file + completed 150-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-VALIDATION.md
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: phase-150-capital-ownership-invariant.test.ts — pins + mutation ledger</name>
  <files>src/__tests__/phase-150-capital-ownership-invariant.test.ts</files>
  <read_first>
    - src/__tests__/phase-149-my-strategies-parity.test.ts (FULL — copy the ARCHITECTURE: header docblock with numbered pins, Rule-9 mutation ledger format, stripComments/productionSources/bodyBraceIndex/functionBody helpers, occurrence COUNTS where a second occurrence would mask a mutation, anti-vacuity pins, missing-pinned-source-is-a-FAILURE)
    - src/lib/visibility.test.ts:87-146 (the SANCTIONED allowlist + rot-guard idiom for repo walks)
    - src/__tests__/phase-148-owner-lane-cache-isolation.test.ts:357-405 (the no-second-caller walk shape)
    - 150-PATTERNS.md §9 (the draft pin list this task refines) and §7 (the portfolio_strategies write census — the allowlist's ground truth)
    - All Phase-150 SUMMARYs (01-07) for exact file paths/identifiers as landed (do not pin against plan-time guesses if execution renamed something)
  </read_first>
  <behavior>
    Pins (each an it() with a number, refined from PATTERNS §9):
    - P1 single-source predicate: the literal 'own_capital' appears in production src/** (comment-stripped, non-test) ONLY in src/lib/capital-ownership.ts — queries, adapter, routes all import the constant. Occurrence-count based, with an anti-vacuity check that the walk found >100 files.
    - P2 write census: production files matching from("portfolio_strategies") with .insert(/.upsert( are EXACTLY the SANCTIONED set {src/app/api/portfolio-strategies/allocation/route.ts, src/components/portfolio/AddToPortfolio.tsx, src/components/portfolio/MigrationWizard.tsx} — allowlist with a B10-style rot-guard (an allowlisted file that stops matching fails loud); comment records that the two legacy client sites are gated by the DB trigger, not by review vigilance.
    - P3 money non-goal: the allocation route's source contains allocated_amount and does NOT contain current_weight (comment-stripped).
    - P4 migration pins: supabase/migrations/20260806120000_strategies_capital_ownership.sql exists; contains BEFORE INSERT and trg_portfolio_strategies_own_capital_only; its trigger-creation statement contains NO 'OR UPDATE'; contains both CHECK literals; contains BOTH D-03-A predicate arms (the unconditional 'team_review' comparison AND the owner-equality conjunct's portfolios lookup — B-1 amendment: dropping the conjunct would delete the shipped third-party allocation paths). A missing file is a FAILURE, not a skip.
    - P5 affordance gating (D-12-A union rule): toStrategyRows' function body derives capitalOwnership/weight via the imported isAllocatable/OWN_CAPITAL vocabulary (functionBody helper — no ad-hoc literal), and HoldingsTable's strategy-rows section renders the Allocate/Edit actions ONLY behind a capitalOwnership own-capital check (window/occurrence pin on the action cell) — unmarked union rows render read-only, they are never filtered out of the row set.
    - P6 atomic flip: the ownership route's source contains flip_capital_ownership_to_team_review and does NOT contain .delete( (comment-stripped) — the RPC is the only removal path.
    - P7 cache isolation extension: the v2 factsheet page's unstable_cache callback region contains no capital_ownership token (reuses the 148 extraction helpers' approach).
    - P8 tag gate (UI-SPEC invariant 3 — rev-2 blocker): every `<OwnershipTag` occurrence in src/components/strategy/StrategyTable.tsx sits inside a comment-stripped guard window containing `visibility === "owner-all-statuses"` (occurrence-counted: N mounts ⇒ N guarded windows), and the `"published-only"` default literal is still present — public rows arrive mark-populated via the shapeRankingRows `...strat` splat, so this gate is the ONLY render barrier; the render half (public mount of a published+own_capital row shows no tag) lives in StrategyTable.visibility.test.tsx (Plan 06).
    Mutation ledger (header, Rule 9 — run each, paste the failure verbatim, revert by re-editing, never git checkout --):
    - M1: capital-ownership.ts isAllocatable → `return mark !== null` ⇒ P1-consuming behavior tests + adapter tests redden (records which).
    - M2 (second-member rule): add a `.upsert(` on portfolio_strategies to a file NOT in the author's mind (e.g. RemoveStrategyButton.tsx) ⇒ P2 reddens naming the file.
    - M3: migration copy edited in a scratch buffer is NOT acceptable — instead mutate the real file's trigger statement to `BEFORE INSERT OR UPDATE` ⇒ P4 reddens; revert by re-editing the line.
    - M4: allocation route — add `current_weight: 0.5` to the upsert payload ⇒ P3 reddens.
    - M5: ownership route — replace the rpc call with a two-statement update+delete ⇒ P6 reddens.
    - M6: StrategyTable.tsx — delete the `visibility === "owner-all-statuses" &&` guard from the OwnershipTag mount (render the tag unconditionally) ⇒ P8 reddens AND the StrategyTable.visibility.test.tsx public-mount negative case reddens (record both).
    Record what stayed GREEN under each mutation too (the asymmetry is the file's justification).
  </behavior>
  <action>
    Author the gate copying the 149 file's architecture wholesale: header docblock (WHY structural, WHAT each pin covers, comment-hygiene paragraph), helpers (stripComments, productionSources walk skipping __tests__/node_modules/.d.ts/.test.*, functionBody), self-pinned literal constants with rationale comments, anti-vacuity assertions (walk found files; stripper strips; extractor found a body), occurrence counts over presence checks where masking is possible. The SQL/RLS half of D-03 is NOT re-asserted here — pgTAP owns it (MEMORY reference_db_test_ci_wiring: live-DB vitest never runs in CI); this file pins SOURCE facts only and says so in the header. Run the six mutations for real; paste each failing assertion into the ledger; revert by re-editing.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-150-capital-ownership-invariant.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - All pins green on the un-mutated tree; ledger records 6 mutations with pasted verbatim failures and the observed-green notes
    - `grep -rn MUTANT src/` → 0 after reverts; `git status` clean of unintended edits
    - Anti-vacuity pins present (file-count floor, stripper self-test)
    - The gate names pgTAP as the DB-tier owner in its header (no false claim of covering RLS/trigger runtime behavior)
  </acceptance_criteria>
  <done>The D-03 gate exists, is mutation-proven first-hand, and cannot pass vacuously.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression surface + close 150-VALIDATION.md</name>
  <files>.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-VALIDATION.md</files>
  <read_first>
    - .planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-VALIDATION.md (the per-task map and falsifiability ledger the planner filled — complete the Status/Observed columns from THIS session's runs, never from memory)
    - All 150-0X-SUMMARY.md files (what actually ran green per plan)
  </read_first>
  <action>
    1. Run the phase gate battery and record results: `npx vitest run src/__tests__/phase-147-series-resolution-guards.test.ts src/__tests__/phase-148-owner-lane-cache-isolation.test.ts src/__tests__/phase-149-my-strategies-parity.test.ts src/__tests__/phase-150-capital-ownership-invariant.test.ts src/lib/visibility.test.ts src/__tests__/no-store-coverage.test.ts src/__tests__/format-percent-contract.test.ts src/__tests__/phase-84-asset-class-flow.test.ts --no-file-parallelism`, then FULL `npm test`, `npm run test:coverage` (review round 2 W-3: coverage is a BLOCKING CI gate — thresholds 82/80/74/72 in vitest.config.ts; the frontend-coverage job enforces them on the merged full-suite numbers), `npx tsc --noEmit`, `npm run lint`.
    2. Update 150-VALIDATION.md: set every per-task-map Status from the actual runs (✅/❌ with reruns until green); transcribe the falsifiability ledger Observed column from Task 1's mutation runs plus the plan-level mutations already observed in earlier plans' SUMMARYs (mark any un-run mutation explicitly "skipped — <reason>", NEVER "caught"); complete the Oracle Independence checklist (copy strings and caps are literals in tests — verify by grep, note any deliberate exception); set frontmatter `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`.
    3. Note the two founder-visible recorded decisions in the ledger's evidence trail so /gsd:verify-work sees them as decisions, not gaps (both are dated CONTEXT amendments, 2026-08-06): (a) D-12-B — the Weight column is RENDER-DERIVED (allocation / Σ allocation over allocated own-capital rows, denominator named 'share of allocated capital'); `current_weight` remains unwritten (the Phase-151 pin stays); (b) D-12-A — positions-but-unmarked rows KEEP rendering (union rule, no vanished money) but expose no Allocate/Edit affordance until marked via the retro path; the Plan-05 read-only PROD census enumerates the affected rows.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && npm run test:coverage 2>&1 | tail -5 && npx tsc --noEmit && grep -c "nyquist_compliant: true" .planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Full vitest suite green (no skipped-because-red), coverage thresholds green (82/80/74/72 — review round 2 W-3), tsc 0 errors, lint 0 errors
    - 150-VALIDATION.md has zero ⬜ pending rows in the falsifiability ledger — every row is Observed-with-evidence or skipped-with-reason (the 142.1 item-14 lesson: pending ≠ skipped ≠ observed)
    - Frontmatter flags set
  </acceptance_criteria>
  <done>Phase evidence closed; ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | this plan adds a test file and documentation; it verifies the boundaries earlier plans defended |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-35 | Tampering (future) | A later edit re-opening D-03 (new write site, widened trigger, second predicate, current_weight write, non-atomic flip) | mitigate | the eight pins + rot-guarded allowlist — CI reddens naming the offender |
| T-150-36 | Repudiation (evidence) | Vacuous gate — pins that cannot fail (self-referential oracles, empty walks) | mitigate | anti-vacuity pins + first-hand mutation ledger with pasted failures; VALIDATION.md rules forbid "asserted — not observed" |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed this phase |
</threat_model>

<verification>
- Gate file green; full suite + coverage thresholds green; VALIDATION.md complete with no pending ledger rows.
</verification>

<success_criteria>
- SC 2b/SC 3 structurally asserted with mutation evidence; all phase collision gates green on the full diff; validation contract closed.
</success_criteria>

<output>
Create `.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-08-SUMMARY.md` when done
</output>
