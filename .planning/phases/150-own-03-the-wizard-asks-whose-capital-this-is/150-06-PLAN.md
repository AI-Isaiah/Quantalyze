---
phase: 150-own-03-the-wizard-asks-whose-capital-this-is
plan: 06
type: execute
wave: 3
depends_on: ["150-02", "150-04"]
files_modified:
  - src/components/strategy/MarkOwnershipDialog.tsx
  - src/components/strategy/MarkOwnershipDialog.test.tsx
  - src/components/strategy/RenameStrategyDialog.tsx
  - src/components/strategy/RenameStrategyDialog.test.tsx
  - src/components/strategy/StrategyTable.tsx
  - src/app/(dashboard)/my-strategies/MyStrategiesSection.tsx
  - src/app/factsheet/[id]/v2/page.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
autonomous: true
requirements: [OWN-03, OWN-05]
user_setup: []

must_haves:
  truths:
    - "A /my-strategies row shows the ownership tag (or nothing when unmarked) and the row actions per the UI-SPEC state table; Black Swan / Alpha Centauri / Arctic Fox become markable with zero re-onboarding (D-09, D-11)"
    - "Rename… renders on private/draft own rows ONLY — absent (not disabled) on published rows (D-17)"
    - "The Mark dialog's own→team flip with a live allocation shows the inline confirm and performs ONE confirmed write via the route's 409/confirm arc — never a silent removal"
    - "The owner factsheet masthead shows the read-only tag and (private/draft only) Rename…, riding the phase-148 owner lane — nothing enters the cached payload; anon still 404s after an owner views a draft"
    - "phase-149 pin 7 stays green: the action cluster never separates the published guard from SimulateImpactButton"
  artifacts:
    - path: "src/components/strategy/MarkOwnershipDialog.tsx"
      provides: "retro mark set/change dialog with flip-confirm arm"
      contains: "CapitalOwnershipRadioGroup"
    - path: "src/components/strategy/RenameStrategyDialog.tsx"
      provides: "rename dialog with inline validation"
    - path: "src/components/strategy/StrategyTable.tsx"
      provides: "tag + owner row-action cluster (prop-gated, public mounts unchanged)"
  key_links:
    - from: "MarkOwnershipDialog"
      to: "/api/strategies/{id}/ownership"
      via: "fetch PATCH; 409 live_allocation → inline confirm → re-submit with confirm_remove_allocation"
      pattern: "confirm_remove_allocation"
    - from: "factsheet page.tsx owner lane"
      to: "FactsheetView"
      via: "owner-only props threaded like viewerNotice — never through the cached payload"
      pattern: "viewerNotice"
---

<objective>
Mount the retro path and rename on their two surfaces: the /my-strategies row (tag + action
cluster + both dialogs) and the owner factsheet masthead (read-only tag + rename). All writes
go through Plan 04's routes; the phase-148 cache isolation and phase-149 parity pins stay
green throughout.

Purpose: SC 1c (rename surfaces), SC 2b's retro path (D-09/D-11), D-16/D-17.
Output: two dialogs + tests, StrategyTable/MyStrategiesSection integration, factsheet owner-lane thread.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-UI-SPEC.md
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-PATTERNS.md
</context>

<interfaces>
Consumed (already merged): `OwnershipTag`, `CapitalOwnershipRadioGroup`, `CapitalOwnership`/`TEAM_REVIEW` (Plan 02); route contracts from Plan 04's interfaces block (PATCH ownership with 409 `{ error: "live_allocation", allocated_amount }`; PATCH name with 400 arms).

Minted here:
```ts
// MarkOwnershipDialog.tsx
export function MarkOwnershipDialog(props: {
  open: boolean; onClose: () => void;
  strategyId: string; strategyName: string;
  currentMark: CapitalOwnership | null;
}): JSX.Element;

// RenameStrategyDialog.tsx
export function RenameStrategyDialog(props: {
  open: boolean; onClose: () => void;
  strategyId: string; currentName: string;
}): JSX.Element;
```
StrategyTable gains OPTIONAL props (absent on public mounts — pin-2's negative list must keep holding):
`onMarkOwnership?: (s) => void; onRename?: (s) => void;` — callbacks minted in MyStrategiesSection (RSC→client boundary rule in its header comment).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MarkOwnershipDialog + RenameStrategyDialog</name>
  <files>src/components/strategy/MarkOwnershipDialog.tsx, src/components/strategy/MarkOwnershipDialog.test.tsx, src/components/strategy/RenameStrategyDialog.tsx, src/components/strategy/RenameStrategyDialog.test.tsx</files>
  <read_first>
    - src/components/portfolio/RemoveStrategyButton.tsx (FULL, 83 lines — the complete dialog-host analog: useState open + status machine + router.refresh(); with THREE deliberate deviations per 150-PATTERNS §4: (1) fetch() the route, never a client-direct supabase write; (2) validation errors never disable the CTA — inline error + focus instead; in-flight label is fine; (3) errors render via buildEnvelope()→ErrorEnvelope, never an invented string)
    - src/components/ui/Modal.tsx (FULL — props are exactly { open, onClose, title, children }; NO footer slot, footers compose in children)
    - src/components/ui/Field.tsx + src/components/ui/Input.tsx (Field + bare control — do NOT double-wrap, two labels)
    - src/lib/envelope.ts (buildEnvelope/ErrorEnvelope exports)
    - 150-UI-SPEC.md Surface 2 "Dialogs" + Copywriting Contract + States table (ALL copy below is binding, byte-exact)
  </read_first>
  <behavior>
    Mark dialog:
    - Renders strategy name as first body line; CapitalOwnershipRadioGroup with label `Whose capital is this?`; initial selection = currentMark ?? team_review (an accidental save must not mint allocatability)
    - Save mark → PATCH /api/strategies/{id}/ownership { mark }; 200 → onClose + router.refresh()
    - 409 live_allocation → dialog body swaps to the inline confirm (NO nested modal): heading `Remove allocation?`, body `{name} has a ${amount} allocation. A team-review strategy cannot hold an allocation — changing the mark removes it.` (amount from the 409 payload, USD-formatted); primary `Change mark and remove allocation` = Button variant="danger"; secondary `Keep own capital` dismisses the confirm, mark and position unchanged
    - Confirm primary → re-submit with confirm_remove_allocation: true → 200 → close + refresh
    - Non-409 failure → ErrorEnvelope rendered in the dialog body, dialog stays open, selection preserved
    Rename dialog:
    - Field label `Name`, prefilled currentName; helper `Only you see this name. Public surfaces keep showing the codename.`
    - Empty-after-trim submit → inline `Enter a name.` + field focused, CTA stays clickable; >80 chars → inline `Keep it under 80 characters.`; no fetch happened for either
    - Valid submit → PATCH /api/strategies/{id}/name { name } → 200 → close + refresh; server failure → ErrorEnvelope, input preserved
  </behavior>
  <action>
    Build both dialogs on the RemoveStrategyButton shape (Modal + status machine + router.refresh) with the three deviations. CTAs: `Save mark` / `Save name` = Button primary; `Cancel` = secondary; in-flight labels per the existing Button pending idiom. The confirm arm is a useState body-swap inside the same Modal (native <dialog> does not nest — PATTERNS §6). Use the UI-SPEC's exact strings — the Copywriting Contract is byte-binding; tests assert copy as literals typed into the test. Amount in the confirm body renders via the exported USD formatter if Plan 07 has landed it — otherwise a local `toLocaleString en-US currency` matching HoldingsTable's formatUsd shape with a TODO(150-07) to swap to the export; do not block on it.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/MarkOwnershipDialog.test.tsx src/components/strategy/RenameStrategyDialog.test.tsx --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All behavior cases green; the 409→confirm→re-submit arc asserted with fetch-mock call sequence (second call carries confirm_remove_allocation: true)
    - `grep -c "supabase" src/components/strategy/MarkOwnershipDialog.tsx src/components/strategy/RenameStrategyDialog.tsx` == 0 (route writes only)
    - Validation cases assert NO fetch occurred and the CTA is not disabled (no disabled= on the primary for validation state)
    - Both dialogs import CapitalOwnershipRadioGroup / Field from the shared primitives (one question component — UI-SPEC invariant 5)
  </acceptance_criteria>
  <done>Both dialogs work against the Plan-04 contracts with binding copy and envelope errors.</done>
</task>

<task type="auto">
  <name>Task 2: StrategyTable row integration + MyStrategiesSection hosts (pin-7 discipline)</name>
  <files>src/components/strategy/StrategyTable.tsx, src/app/(dashboard)/my-strategies/MyStrategiesSection.tsx</files>
  <read_first>
    - src/__tests__/phase-149-my-strategies-parity.test.ts:403-455 (pins 1, 2, 7 — pin 7's 300-char window: `s.status === "published"` must stay within 300 comment-stripped chars BEFORE `<SimulateImpactButton`; RUN THIS TEST FIRST, then after every edit to the action cell)
    - src/components/strategy/StrategyTable.tsx:885-925 (name cell: link → verified check → status Badge — the tag mounts after the Badge), :1044-1063 (action cell), :1155-1161 (the louder placeholder CTA — do NOT copy for these actions)
    - src/components/portfolio/RemoveStrategyButton.tsx:51-57 (ghost text-action classes; swap hover:text-negative → hover:text-text-primary; add focus-visible:ring-2 focus-visible:ring-accent/20)
    - src/app/(dashboard)/my-strategies/MyStrategiesSection.tsx (FULL, 90 lines — the client boundary; header comment :2-17 explains why callbacks are minted here)
    - 150-UI-SPEC.md Surface 2 (row-action state table — binding)
  </read_first>
  <action>
    1. StrategyTable: mount `<OwnershipTag mark={s.capital_ownership} />` in the name-cell flex row AFTER the status Badge (:917-919). Add optional `onMarkOwnership` / `onRename` props; when present, render the ghost action cluster per the UI-SPEC state table — `Mark ownership…` (unmarked) / `Change mark…` (marked) always for real own rows; `Rename…` ONLY when `s.status === "private" || s.status === "draft"`; placeholder rows (149 Delta 5): nothing. ⛔ Pin-7 discipline: render the cluster as its OWN JSX block BEFORE the `{s.status === "published" && (` guard inside the action <td> (or as a sibling element) — the guard token and `<SimulateImpactButton` must remain adjacent within the 300-char window. Props stay optional and are NOT passed by any public mount — discovery/browse pages must keep zero mention (pin 2's negative list; do not edit those pages).
    2. MyStrategiesSection: mint the two callbacks + dialog open-state here (the RSC→client boundary comment is the rule); render MarkOwnershipDialog / RenameStrategyDialog with the selected row's id/name/mark; pass the callbacks into StrategyTable.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-149-my-strategies-parity.test.ts "src/app/(dashboard)/my-strategies" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - phase-149 parity test green — all 12 pins, especially 7 (window intact), 2 (public pages unchanged), 10/11 (still exactly one owner mount / one export)
    - `git diff --name-only` contains NO discovery/ or browse/ page files
    - Action cluster renders per state table (spot-assert via the my-strategies page test or a targeted RTL case in the section's test if one exists)
  </acceptance_criteria>
  <done>Row surface complete; retro path reachable; all 149 pins green.</done>
</task>

<task type="auto">
  <name>Task 3: Factsheet owner lane — read-only tag + Rename…</name>
  <files>src/app/factsheet/[id]/v2/page.tsx, src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/page.tsx:422-499 (the two-probe lane resolution — published first, then owner-inclusive with the SESSION id; :623 the viewerNotice thread — the exact pattern for the new props), and the unstable_cache block the 148 pins protect
    - src/__tests__/phase-148-owner-lane-cache-isolation.test.ts:301-349, 375-382 (the pins that MUST stay green: cached callback contains fetchAndBuildPayload(id, withPublishedOnly) literally, never withPublishedOrOwner; no visibility token in the cached wrapper head; no second payload caller)
    - src/app/factsheet/[id]/v2/FactsheetView.tsx:662-688 (masthead: H1 {payload.strategyName}; the TrustTierLabel row where the tag goes; the H1 baseline row for Rename…)
    - 150-RESEARCH.md § Pitfall 6 (the mark must NOT enter the cached payload) and § Pattern 5 (lane === "owner" IS the D-17 predicate for free — reachable only when unpublished AND owned)
  </read_first>
  <action>
    1. page.tsx: in the owner-lane branch ONLY, read `capital_ownership` (and status, already known) from the owner-probe's strategy row — widen THAT probe's select if needed; ⛔ nothing new inside the unstable_cache callback or the cached wrapper's head. Thread two owner-only props into FactsheetView alongside viewerNotice: `ownershipMark={lane === "owner" ? mark : undefined}` and `renameTarget={lane === "owner" ? { id, name } : undefined}` (lane==="owner" is only reachable for unpublished+owned — the D-17 gate for free; comment cites Pattern 5). Note: payload.strategyName is already sourced from strategies.name (build-payload.ts:429/450/472/490 — confirmed), so the rename shows in the masthead with no payload change.
    2. FactsheetView: render `<OwnershipTag mark={ownershipMark} />` in the TrustTierLabel row (:668-675); render the `Rename…` ghost text action (`text-caption text-text-muted hover:text-text-primary`) on the H1 baseline row when renameTarget is present, mounting RenameStrategyDialog (client boundary: if FactsheetView is a server component here, mount a small client wrapper for the action+dialog — follow how existing interactive masthead elements are mounted).
    3. Run the phase-148 cache test before and after; also extend page.owner-lane.test.tsx with: owner+draft → tag/rename present; public lane → absent.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-148-owner-lane-cache-isolation.test.ts "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - phase-148 cache-isolation pins ALL green (cached callback byte-unchanged w.r.t. visibility; `grep -c "capital_ownership" <the cached callback region>` == 0 — assert by keeping the 148 test green plus a source check that the unstable_cache block's diff is empty)
    - Owner-lane test asserts tag + Rename… on owner+draft and their ABSENCE on the public lane
    - `git diff` on page.tsx shows changes only in the owner-lane branch and prop thread
  </acceptance_criteria>
  <done>Factsheet shows the mark read-only and rename on the owner lane; cache adversarial acceptance intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner lane → public cache | owner-read data must never enter the anon-readable cached factsheet entry |
| browser dialogs → Plan-04 routes | all writes cross the already-defended route boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-27 | Information Disclosure | Mark or owner name entering the cached factsheet payload (anon-readable) | mitigate | owner-only props ride the viewerNotice thread; zero edits inside the unstable_cache callback; phase-148 pins are the regression gate |
| T-150-28 | Tampering | Dialog bypassing route defences with a client-direct supabase write | mitigate | fetch()-only dialogs; source grep pin (no supabase import); Plan 08 census |
| T-150-29 | Spoofing (UI) | Rename affordance appearing on published rows (D-17 render half) | mitigate | render gate `private|draft` on both surfaces; server-side gate already enforced in Plan 04 (defence in depth) |
| T-150-30 | Repudiation / integrity | Flip silently removing a position from the dialog | mitigate | confirm arm is the ONLY path to confirm_remove_allocation: true; copy names the consequence and the amount |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed this phase |
</threat_model>

<verification>
- phase-148 + phase-149 gates green; both dialog tests green; tsc + lint clean.
- Manual spot: /my-strategies renders tags + actions for the founder's three legacy strategies (unmarked → Mark ownership… present) — covered at phase /qa.
</verification>

<success_criteria>
- D-09/D-11/D-16/D-17 all observable; SC 1c's my-strategies + factsheet surfaces render the renamed name.
- Zero public-surface pixels changed; 148 adversarial cache acceptance stands.
</success_criteria>

<output>
Create `.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-06-SUMMARY.md` when done
</output>
