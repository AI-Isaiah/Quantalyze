---
phase: 150-own-03-the-wizard-asks-whose-capital-this-is
plan: 07
type: execute
wave: 3
depends_on: ["150-02", "150-05"]
files_modified:
  - src/app/(dashboard)/allocations/components/HoldingsTable.tsx
  - src/app/(dashboard)/allocations/components/HoldingsTable.strategy-rows.test.tsx
  - src/app/(dashboard)/allocations/components/AllocateDialog.tsx
  - src/app/(dashboard)/allocations/components/AllocateDialog.test.tsx
  - src/app/(dashboard)/allocations/HoldingsTabPanel.tsx
  - src/app/(dashboard)/allocations/page.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.tsx
  - src/app/(dashboard)/allocations/lib/strategies-row-adapter.ts
  - src/app/(dashboard)/allocations/lib/strategies-row-adapter.test.ts
  - src/__tests__/format-percent-contract.test.ts
autonomous: true
requirements: [OWN-03]
user_setup: []

must_haves:
  truths:
    - "The Holdings STRATEGIES panel renders the D-12-A UNION row set: unallocated own-capital rows show '— not allocated' + [Allocate…]; allocated own-capital rows show the amount · derived weight + [Edit allocation…]; positions-but-unmarked rows KEEP rendering (as today) with NO Allocate/Edit affordance and no tag — never both buttons, never a second row, never vanished money (D-12, D-13, D-12-A)"
    - "The Weight cell is RENDER-DERIVED per D-12-B: allocation / Σ allocation across allocated own-capital rows, formatPercent(w, 2, {signed:false}) — the approved mock's `$120,000 · 24.00%` renders with ZERO DB write; rows with no allocation (or outside the derived set) render `—`; the column header carries the honest denominator tooltip 'share of allocated capital'"
    - "The dead-end empty state is gone: three arms fire in priority order (union list ≻ 'No strategies marked as own capital.' ≻ 'No strategies yet.'), with the arm-2/arm-3 discriminator = getMyStrategies count > 0 (D-15, review round 1 W-6)"
    - "The strategy-rows Weight cell renders UNSIGNED (24.00%, never +24.00%) and the contract test pins it — the shipped signed cell was the non-conforming one (research delta 4)"
    - "The Allocate dialog validates inline (amount > 0, $1B cap = MAGNITUDE_CAPS.MAX_TICKET_SIZE_USD — review round 3 W-3), never disables the CTA for validation, and Edit offers the two-step Remove confirm"
    - "With no portfolio (props.portfolio null — MyAllocationDashboardPayload.portfolio is Portfolio | null, queries.ts:1903), the Allocate… action opens the SAME amount dialog as every other marked row — the Plan-05 route derives and lazily provisions the real portfolio server-side (rev-4, D-03-B), so the client sends NO portfolio id and no remedy modal or dead end exists"
    - "The B-3 back-compat slack in toStrategyRows (union input type + runtime discrimination + HAND-OFF comment) is REMOVED — HoldingsTabPanel passes { strategies: marked, positions } explicitly"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/AllocateDialog.tsx"
      provides: "allocate/edit/remove dialog against the Plan-05 route"
      exports: ["AllocateDialog"]
    - path: "src/app/(dashboard)/allocations/components/HoldingsTable.tsx"
      provides: "union strategy rows + derived unsigned weight + empty-state arms"
      contains: "signed: false"
  key_links:
    - from: "AllocateDialog"
      to: "/api/portfolio-strategies/allocation"
      via: "fetch POST (allocate/edit) + DELETE (remove)"
      pattern: "portfolio-strategies/allocation"
    - from: "HoldingsTabPanel"
      to: "getOwnCapitalStrategies + toStrategyRows"
      via: "server fetch → adapter merge (strategies + positions, slack removed)"
      pattern: "getOwnCapitalStrategies"
    - from: "HoldingsTable / AllocateDialog"
      to: "src/lib/dollar-validation.ts"
      via: "formatUsd import (Plan 02 — the ONE money formatter)"
      pattern: "formatUsd"
---

<objective>
Render the Holdings money surface: the D-12-A union strategy rows with the ownership tag,
the D-12-B render-derived weight (the approved mock's `$120,000 · 24.00%` with zero DB
write), the Allocate/Edit dialog (the phase's primary CTA), the honest three-arm empty
state, and the unsigned-weight bug fix with its extended contract pin. Also closes the B-3
hand-off: HoldingsTabPanel passes real positions + marked strategies and the adapter's
back-compat slack is deleted.

Purpose: SC 2's user-visible half + D-12/D-13/D-15. After this plan the founder can allocate
Black Swan from Holdings (once marked via Plan 06's retro path) and see its share of
allocated capital immediately.
Output: HoldingsTable + AllocateDialog + panel wiring + adapter slack removal + contract-test extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-CONTEXT.md
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-UI-SPEC.md
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-PATTERNS.md
@DESIGN.md
</context>

<interfaces>
Consumed (already merged): `StrategyRow` union shape (capitalOwnership / allocation / derived weight / age — Plan 05 interfaces block; `row.id` IS the strategy id, no separate strategyId field per review round 1 W-3), `getOwnCapitalStrategies` (Plan 05), `OwnershipTag` (Plan 02), `formatUsd` from `@/lib/dollar-validation` (Plan 02 — review round 1 W-7: import, never re-export or re-implement), allocation route contract (Plan 05).

Minted here:
```ts
// AllocateDialog.tsx
export function AllocateDialog(props: {
  open: boolean; onClose: () => void;
  mode: "allocate" | "edit";
  strategyId: string; strategyName: string;
  // NO portfolioId (rev-4): the route derives the caller's real portfolio from the
  // session and lazily provisions it (Plan 05, D-03-B) — a client-supplied container
  // id would be untrusted input the route must not honor anyway (T-150-40).
  currentAmount: number | null;          // prefill for edit
}): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HoldingsTable — union rows, derived unsigned weight, tag, empty-state arms</name>
  <files>src/app/(dashboard)/allocations/components/HoldingsTable.tsx, src/app/(dashboard)/allocations/components/HoldingsTable.strategy-rows.test.tsx, src/__tests__/format-percent-contract.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/HoldingsTable.tsx:72-101 (formatUsd is now an IMPORT from @/lib/dollar-validation — Plan 02 moved it; formatDays), :203-320 (StrategyRowsTable — comparator :212-214, the SIGNED weight cell :293-295, current dead-end empty state :256-259), :730-745 (the sibling unsigned weight cell :739 — the conforming one)
    - src/__tests__/format-percent-contract.test.ts:107-146 (the existing pin at :144 "HoldingsTable Weight cell is unsigned" and the rationale header — EXTEND this file with a strategy-rows pin, same shape)
    - src/components/strategy/StrategyTable.tsx:1067-1085 (three-arm empty-state priority-ordering comment — why arm order is load-bearing)
    - src/app/(dashboard)/my-strategies/MyStrategiesEmptyState.tsx (149 empty-state anatomy: rounded-lg border border-border bg-surface px-6 py-8, heading+body+link, no icon)
    - 150-UI-SPEC.md Surface 4 (row anatomy table + empty-state copy — byte-binding EXCEPT where the D-12-A/D-12-B amendments in 150-CONTEXT.md supersede: union population + derived weight)
  </read_first>
  <behavior>
    - Weight cell (strategy rows): `row.weight == null ? "—" : formatPercent(row.weight, 2, { signed: false })` — extend format-percent-contract.test.ts to pin BOTH HoldingsTable weight cells unsigned (the strategy-rows one was the non-conforming sibling; contract header rationale applies). row.weight is the adapter's DERIVED share (D-12-B) — an allocated own-capital row with allocation 120_000 in a 500_000 allocated book asserts `24.00%` rendered
    - Weight column header carries the honest denominator: `title="share of allocated capital"` tooltip on the th (D-12-B decision — the denominator is the allocated own-capital set, not book equity; name it)
    - Own-capital unallocated row: Allocation cell renders `—` in font-metric + `not allocated` in text-caption text-text-muted; Weight `—`; action `Allocate…` Button secondary size=sm; OwnershipTag after the name in the Strategy cell
    - Own-capital allocated row: `formatUsd(allocation)` in font-metric tabular-nums; derived weight per above; action `Edit allocation…`; NEVER both buttons on one row
    - Positions-but-unmarked row (capitalOwnership null — D-12-A union): renders EXACTLY as today (name/manager/metrics/allocation amount/age; existing affordances such as remove untouched), NO OwnershipTag, NO Allocate…/Edit allocation… button, Weight `—` — allocated money never leaves the surface, but the new affordances are own-capital-only
    - Empty arms in priority order: (1) ≥1 UNION row → the LIST renders; (2) zero rows but the user HAS strategies → heading `No strategies marked as own capital.` body `Set the mark from My Strategies, or when you connect a new key.` + accent underline link `Go to My Strategies →` (href /my-strategies); (3) zero strategies → heading `No strategies yet.` body `Connect an exchange API key or upload a CSV to see your strategies here.`
    - Arm 2 never fires when arm 1's condition holds (priority assertion)
  </behavior>
  <action>
    1. formatUsd: IMPORT from `@/lib/dollar-validation` (Plan 02 landed the move — review round 1 W-7). ⛔ Do not re-declare or re-export it here; a second money formatter is forbidden (UI-SPEC line 278).
    2. Change the strategy-rows weight cell at :293-295 to the unsigned form — this is a DELIBERATE visual change to a shipped column, decided by research delta 4 (the sibling at :739 is unsigned and contract-pinned; the signed one was the bug). Extend format-percent-contract.test.ts with the strategy-rows pin so the pair can't drift again. Add the `title="share of allocated capital"` header tooltip (D-12-B).
    3. Render OwnershipTag and the row arms per the behavior block: the Allocate…/Edit allocation… secondary sm buttons render ONLY when `row.capitalOwnership === "own_capital"` (derive the button from `allocation == null ? Allocate : Edit`); unmarked rows keep today's rendering untouched. Buttons dispatch `onAllocate(row)` / `onEditAllocation(row)` callback props (dialog host is the panel — Task 3).
    4. Replace the dead-end empty state with the three arms (149 anatomy classes; copy byte-exact; priority-order comment copied in spirit from StrategyTable :1067-1085). Arm-1 input = the union row set; the arm-2/arm-3 discriminator arrives as a prop from the panel (Task 3, review round 1 W-6: getMyStrategies count > 0).
    5. Extend HoldingsTable.strategy-rows.test.tsx per the behavior block (needs input: the widened union StrategyRow from Plan 05 — fixtures include an unmarked positioned row).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/HoldingsTable.strategy-rows.test.tsx" src/__tests__/format-percent-contract.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Contract test pins BOTH weight cells unsigned; strategy-rows cell asserts `24.00%` (derived, no leading `+`) for the 120k/500k fixture
    - Row-state assertions: own-capital unallocated (—/not allocated/Allocate…), own-capital allocated ($ formatted · derived %/Edit allocation…), tag presence, never-both-buttons, AND the unmarked-positioned row (present, amount shown, no tag, no new buttons, weight —)
    - Empty arms asserted in priority order with byte-exact copy
    - `grep -c "function formatUsd\|const formatUsd" "src/app/(dashboard)/allocations/components/HoldingsTable.tsx"` == 0 and `grep -c "dollar-validation" "src/app/(dashboard)/allocations/components/HoldingsTable.tsx"` ≥ 1 (import, not local copy — review round 1 W-7)
    - `grep -c "share of allocated capital" "src/app/(dashboard)/allocations/components/HoldingsTable.tsx"` ≥ 1
  </acceptance_criteria>
  <done>Union rows + derived unsigned weight + arms ship with the extended contract pin and the honest denominator label.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AllocateDialog — amount, validation, remove confirm, honest weight line</name>
  <files>src/app/(dashboard)/allocations/components/AllocateDialog.tsx, src/app/(dashboard)/allocations/components/AllocateDialog.test.tsx</files>
  <read_first>
    - src/components/portfolio/RemoveStrategyButton.tsx (dialog shape; deviations per 150-PATTERNS §4: fetch not supabase, no disabled-for-validation, ErrorEnvelope not invented strings)
    - src/components/portfolio/MigrationWizard.tsx:63-76 (Number()+isFinite()+>0 parse — copy the parse, not the error string, not the client upsert, not the missing cap)
    - src/components/ui/Modal.tsx + Field.tsx (Field + bare input; no footer slot)
    - src/lib/dollar-validation.ts (formatUsd for amount rendering — review round 1 W-7)
    - src/lib/closed-sets.ts:529-546 (MAGNITUDE_CAPS — canonical home, review round 3 W-2; the client cap mirror imports MAGNITUDE_CAPS.MAX_TICKET_SIZE_USD = 1_000_000_000 from @/lib/closed-sets — review round 3 W-3)
    - 150-UI-SPEC.md Surface 4 "Allocate / Edit dialog" table + Copywriting Contract (byte-binding copy incl. both validation strings and the remove-confirm copy — EXCEPT the weight-preview fallback line, superseded by D-12-B: see behavior)
    - 150-PATTERNS.md § No Analog Found row 2 (weight preview: NO book-equity scalar exists — still true; the D-12-B derived weight is share-of-allocated, rendered in the ROW after the confirmed write, never fabricated in the dialog)
  </read_first>
  <behavior>
    - Title `Allocate — {name}` / `Edit allocation — {name}`; field label `Allocation (USD)`; edit mode prefills currentAmount
    - Submit with non-finite or ≤0 → inline `Enter an amount above $0.` + red border + field focused; NO fetch; CTA not disabled
    - Submit with > $1,000,000,000 → inline `That's above the $1B sanity cap — check the amount.`; NO fetch. Boundary: exactly 1_000_000_000 is VALID and fetches (the copy fires only ABOVE the cap); both boundary literals typed into the test as numbers, never the constant (oracle independence — review round 3 W-3)
    - Valid submit → POST /api/portfolio-strategies/allocation { strategy_id, allocated_amount } (NO portfolio_id — rev-4: the route derives + lazily provisions the caller's real portfolio server-side) → 200 → onClose + router.refresh(); NO optimistic row update (the row updates from the confirmed write — UI-SPEC States; the derived weight appears in the refreshed row)
    - Helper line renders EXACTLY `Weight shows each strategy's share of your allocated capital.` — this SUPERSEDES the UI-SPEC fallback line ("Weight appears once your book equity is known.") per the D-12-B amendment: weight now renders immediately as share-of-allocated, so the book-equity conditional would be false copy. Never a fabricated `≈ {w}%` preview inside the dialog — no book-equity scalar exists (PATTERNS § No Analog Found row 2 stands)
    - Edit mode only: `Remove allocation…` text action in text-negative below the footer → body swaps to inline confirm `Remove this allocation? {name} leaves your allocation. The own-capital mark stays.` with `Remove` (Button variant="danger") + `Keep` (secondary); Remove → DELETE the route (body { strategy_id } — rev-4) → close + refresh
    - Server failure → ErrorEnvelope in body, dialog open, input preserved
  </behavior>
  <action>
    Build AllocateDialog per the interfaces block on the RemoveStrategyButton shape with the three deviations. Client-side validation mirrors the server (finite, >0, ≤ MAGNITUDE_CAPS.MAX_TICKET_SIZE_USD imported from @/lib/closed-sets — review round 3 W-3: the $1e9 ticket cap matching Plan 05's route bound; deliberate deviation from the checker's MAX_DOLLAR_VALUE_USD suggestion, which is the 1e12 aum cap and would make the approved "$1B sanity cap" copy wrong by 1000×) but the server remains authoritative; validation renders inline at the field (this form is explicitly IN SCOPE for Phase 153's inline-validation criterion — ROADMAP note — so no terminal envelope for field problems; envelopes are for WRITE failures only). Confirm arm = useState body swap, no nested modal. Amounts render via formatUsd imported from `@/lib/dollar-validation` (review round 1 W-7). Primary CTA labels `Allocate` / `Save allocation` per mode. Comment the helper-line supersession with the D-12-B citation so a copy-audit does not "fix" it back.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/AllocateDialog.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All behavior cases green; validation cases assert zero fetch calls and non-disabled CTA
    - `grep -c "supabase" "src/app/(dashboard)/allocations/components/AllocateDialog.tsx"` == 0
    - `grep -c "current_weight" "src/app/(dashboard)/allocations/components/AllocateDialog.tsx"` == 0
    - `grep -c "book equity is known" "src/app/(dashboard)/allocations/components/AllocateDialog.tsx"` == 0 (the superseded fallback line is not shipped)
    - Copy strings asserted as literals typed into the test (byte-exact vs UI-SPEC, with the D-12-B helper-line supersession noted in the test comment)
    - Cap boundary asserted with the LITERAL 1_000_000_000 typed into the test (the number, never the constant — oracle independence; review round 3 W-3)
  </acceptance_criteria>
  <done>The phase's primary CTA works end-to-end against the Plan-05 route with honest copy matching the derived-weight model.</done>
</task>

<task type="auto">
  <name>Task 3: HoldingsTabPanel wiring + B-3 slack removal</name>
  <files>src/app/(dashboard)/allocations/HoldingsTabPanel.tsx, src/app/(dashboard)/allocations/page.tsx, src/app/(dashboard)/allocations/AllocationsTabs.tsx, src/app/(dashboard)/allocations/lib/strategies-row-adapter.ts, src/app/(dashboard)/allocations/lib/strategies-row-adapter.test.ts, src/app/(dashboard)/allocations/components/HoldingsTable.strategy-rows.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/HoldingsTabPanel.tsx (FULL — :93-96 is the current toStrategyRows({ strategies }) feed; find where props.strategies originates and whether the panel is server- or client-side)
    - src/app/(dashboard)/allocations/lib/strategies-row-adapter.ts (Plan 05's union signature + the `// HAND-OFF(150-07)` slack markers this task deletes)
    - src/lib/queries.ts (getOwnCapitalStrategies — Plan 05; getMyStrategies :294-311 — the review round 1 W-6 discriminator source; :1903 MyAllocationDashboardPayload.portfolio stays Portfolio | null — rev-4: the dialog host no longer consumes it, the Plan-05 route provisions server-side)
    - src/app/(dashboard)/allocations/page.tsx + src/app/(dashboard)/allocations/AllocationsTabs.tsx (the server entry + tab host the two new fetches thread through — review round 2 W-2: both are in this task's files)
  </read_first>
  <action>
    1. Wire the widened data path: fetch the caller's own-capital strategies via getOwnCapitalStrategies (server-side, alongside wherever the existing portfolio_strategies rows are fetched — follow the panel's existing data flow upstream to the page/server component — src/app/(dashboard)/allocations/page.tsx → AllocationsTabs.tsx — and add the second fetch there, threading it down) and feed the adapter EXPLICITLY: `toStrategyRows({ strategies: markedStrategies, positions: payloadStrategies })`.
    2. B-3 hand-off completion (stated in Plan 05 Task 3): DELETE the adapter's back-compat slack — remove the `OwnCapitalStrategy[] | MyAllocationDashboardPayload["strategies"]` union type, the runtime `strategy_id`-shape discrimination, and every `// HAND-OFF(150-07)` comment; narrow `strategies` to `OwnCapitalStrategy[]` (keep the `positions = []` default or make it required — narrow fully, no slack remains). Re-run the adapter tests: the legacy-call back-compat case from Plan 05 is DELETED with the slack (it pinned the interim state, which no longer exists).
    3. Host the AllocateDialog open-state + the onAllocate/onEditAllocation callbacks at the panel's client boundary (same minting rule as MyStrategiesSection). NO portfolio id is sourced or threaded (rev-4 — the round-3 `portfolioId: string | null` widening is dead): the dialog needs only row identity (`row.id` is the strategy id), the display name, and `currentAmount`; the Plan-05 route derives and lazily provisions the caller's real portfolio server-side (D-03-B).
    4. Review round 1 W-6 (no execution-time decision): the empty-state arm-2/arm-3 discriminator is fetched server-side alongside getOwnCapitalStrategies and threaded to HoldingsTable as a boolean prop, in EXACTLY this null-safe form (review round 2 W-2 — mind precedence: `?? 0 > 0` parses as `?? (0 > 0)`, so the outer parentheses are load-bearing): `const hasAnyStrategies = ((await getMyStrategies(userId))?.length ?? 0) > 0;` — arm 2 renders when the union row set is empty AND hasAnyStrategies; arm 3 when the union row set is empty AND NOT hasAnyStrategies. If the panel's props already carry an equivalent any-strategies signal sourced from getMyStrategies, reuse it; the SOURCE is getMyStrategies either way.
    5. Portfolio-null renders keep the money path whole (rev-4, orchestrator decision 2026-08-06 — REPLACES the round-3 remedy modal, which is DELETED unbuilt): `Allocate…` opens the amount dialog for EVERY marked row regardless of `props.portfolio` — no remedy state, no dead end, no portfolio dependency at the client boundary (the Plan-05 route lazily provisions the real portfolio on first allocate, D-03-B). RTL case in HoldingsTable.strategy-rows.test.tsx: render the dialog host with portfolio null → click `Allocate…` → the AMOUNT dialog renders functional (title `Allocate — {name}` asserted as a literal typed into the test), and the string `No portfolio yet.` appears NOWHERE in the rendered tree.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/allocations" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Panel compiles and all allocations-tree tests green
    - `grep -rc "getOwnCapitalStrategies" "src/app/(dashboard)/allocations/"` ≥ 1 wired into the panel's data flow, and `grep -rn "getMyStrategies" "src/app/(dashboard)/allocations/"` (or the page/server component feeding it) shows the review round 1 W-6 discriminator source
    - `grep -c "HAND-OFF(150-07)" "src/app/(dashboard)/allocations/lib/strategies-row-adapter.ts"` == 0 (slack removed)
    - No client-side supabase read added (server fetch + props, matching the panel's existing pattern)
    - Portfolio-null case green in HoldingsTable.strategy-rows.test.tsx: portfolio null → `Allocate…` opens the AMOUNT dialog (title literal asserted); `grep -rc "No portfolio yet" "src/app/(dashboard)/allocations/"` returns 0 files (rev-4: the round-3 remedy state does not exist)
  </acceptance_criteria>
  <done>Union rows reach the panel from real data; dialogs mounted; portfolio-null renders keep a fully functional Allocate… (route-side provisioning — no remedy modal, no client portfolio id); empty arms driven by named signals; B-3 slack gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser dialog → allocation route | the money write crosses Plan-05's defended boundary; this plan adds no new write path and sends NO container id (rev-4 — the portfolio is derived server-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-31 | Tampering | UI offering Allocate… on an already-allocated row (duplicate-add UX half) | mitigate | mode is derived from allocation null-ness — one row, one button; test pins never-both; DB PK is the hard floor |
| T-150-32 | Tampering (money display) | Fabricated weight/equity numbers | mitigate | weight is render-derived allocation/Σallocation over the allocated own-capital set (D-12-B) with the denominator named in the header tooltip; no book-equity scalar invented; no current_weight reference (source grep); dialog never previews a number |
| T-150-33 | Spoofing (UI) | Allocate/Edit affordance rendering for non-own-capital rows | mitigate | affordance gated on row.capitalOwnership === "own_capital" (adapter-derived from marked-set membership, Plan 05); unmarked union rows render read-only; Plan 08 pins the predicate-in-scope invariant |
| T-150-34 | Denial of Service | Client-side cap mismatch letting absurd amounts hit the server | mitigate | shared MAGNITUDE_CAPS on both sides; server authoritative |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed this phase |
</threat_model>

<verification>
- HoldingsTable strategy-rows + AllocateDialog + format-percent-contract green; allocations tree green; tsc + lint clean.
- SUMMARY restates the two D-12 amendments as decisions (not gaps): (a) Weight is the D-12-B render-derived share of allocated capital — the approved mock's `$120,000 · 24.00%` renders with zero DB write; `current_weight` stays unwritten (Phase-151 pin intact); (b) positions-but-unmarked rows render read-only (no Allocate/Edit) until marked via the retro path (D-12-A union rule; the Plan-05 PROD census names the affected rows).
</verification>

<success_criteria>
- SC 2 user-visible: mark → Holdings row → Allocate… → position → amount · derived weight, with edit/remove and honest empties.
- D-13's UI half (never both buttons) + D-12-A's no-vanished-money rule + D-15's three arms + the unsigned-weight fix all pinned by tests.
- B-3 hand-off closed: explicit { strategies, positions } call, zero slack markers left.
</success_criteria>

<output>
Create `.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-07-SUMMARY.md` when done
</output>
