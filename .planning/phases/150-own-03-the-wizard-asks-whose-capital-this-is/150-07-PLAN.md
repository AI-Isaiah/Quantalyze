---
phase: 150-own-03-the-wizard-asks-whose-capital-this-is
plan: 07
type: execute
wave: 3
depends_on: ["150-02", "150-05"]
files_modified:
  - src/app/(dashboard)/allocations/components/HoldingsTable.tsx
  - src/app/(dashboard)/allocations/components/HoldingsTable.strategy-rows.test.tsx
  - src/app/(dashboard)/allocations/components/AllocateDialog.tsx
  - src/app/(dashboard)/allocations/components/AllocateDialog.test.tsx
  - src/app/(dashboard)/allocations/HoldingsTabPanel.tsx
  - src/__tests__/format-percent-contract.test.ts
autonomous: true
requirements: [OWN-03]
user_setup: []

must_haves:
  truths:
    - "The Holdings STRATEGIES panel lists own-capital rows: unallocated rows show '— not allocated' + [Allocate…]; allocated rows show the amount + [Edit allocation…] — never both, never a second row for the same strategy (D-12, D-13)"
    - "The dead-end empty state is gone: three arms fire in priority order (list ≻ 'No strategies marked as own capital.' ≻ 'No strategies yet.') (D-15)"
    - "The strategy-rows Weight cell renders UNSIGNED (24.00%, never +24.00%) and the contract test pins it — the shipped signed cell was the non-conforming one (research delta 4)"
    - "The Allocate dialog validates inline (amount > 0, $1B cap), never disables the CTA for validation, renders the honest weight-fallback line, and Edit offers the two-step Remove confirm"
    - "Uncomputed values render em-dash, never 0 — including Weight on allocated rows until Phase 151 (recorded delta: the approved mock's 24.00% appears only when current_weight gains a writer)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/AllocateDialog.tsx"
      provides: "allocate/edit/remove dialog against the Plan-05 route"
      exports: ["AllocateDialog"]
    - path: "src/app/(dashboard)/allocations/components/HoldingsTable.tsx"
      provides: "widened strategy rows + exported formatUsd + unsigned weight + empty-state arms"
      contains: "export function formatUsd"
  key_links:
    - from: "AllocateDialog"
      to: "/api/portfolio-strategies/allocation"
      via: "fetch POST (allocate/edit) + DELETE (remove)"
      pattern: "portfolio-strategies/allocation"
    - from: "HoldingsTabPanel"
      to: "getOwnCapitalStrategies + toStrategyRows"
      via: "server fetch → adapter merge"
      pattern: "getOwnCapitalStrategies"
---

<objective>
Render the Holdings money surface: widened strategy rows with the ownership tag, the
Allocate/Edit dialog (the phase's primary CTA), the honest three-arm empty state, and the
unsigned-weight bug fix with its extended contract pin.

Purpose: SC 2's user-visible half + D-12/D-13/D-15. After this plan the founder can allocate
Black Swan from Holdings (once marked via Plan 06's retro path).
Output: HoldingsTable + AllocateDialog + panel wiring + contract-test extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-UI-SPEC.md
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-PATTERNS.md
</context>

<interfaces>
Consumed (already merged): `StrategyRow` widened shape + `getOwnCapitalStrategies` (Plan 05 interfaces block), `OwnershipTag` (Plan 02), allocation route contract (Plan 05).

Minted here:
```ts
// HoldingsTable.tsx — formatUsd becomes exported (was module-private at :72-80); body unchanged
export function formatUsd(n: number | null): string;

// AllocateDialog.tsx
export function AllocateDialog(props: {
  open: boolean; onClose: () => void;
  mode: "allocate" | "edit";
  portfolioId: string; strategyId: string; strategyName: string;
  currentAmount: number | null;          // prefill for edit
}): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HoldingsTable — rows, unsigned weight, tag, empty-state arms</name>
  <files>src/app/(dashboard)/allocations/components/HoldingsTable.tsx, src/app/(dashboard)/allocations/components/HoldingsTable.strategy-rows.test.tsx, src/__tests__/format-percent-contract.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/HoldingsTable.tsx:72-101 (formatUsd + formatDays), :203-320 (StrategyRowsTable — comparator :212-214, the SIGNED weight cell :293-295, current dead-end empty state :256-259), :730-745 (the sibling unsigned weight cell :739 — the conforming one)
    - src/__tests__/format-percent-contract.test.ts:107-146 (the existing pin at :144 "HoldingsTable Weight cell is unsigned" and the rationale header — EXTEND this file with a strategy-rows pin, same shape)
    - src/components/strategy/StrategyTable.tsx:1067-1085 (three-arm empty-state priority-ordering comment — why arm order is load-bearing)
    - src/app/(dashboard)/my-strategies/MyStrategiesEmptyState.tsx (149 empty-state anatomy: rounded-lg border border-border bg-surface px-6 py-8, heading+body+link, no icon)
    - 150-UI-SPEC.md Surface 4 (row anatomy table + empty-state copy — byte-binding)
  </read_first>
  <behavior>
    - Weight cell (strategy rows): `row.weight == null ? "—" : formatPercent(row.weight, 2, { signed: false })` — extend format-percent-contract.test.ts to pin BOTH HoldingsTable weight cells unsigned (the strategy-rows one was the non-conforming sibling; contract header rationale applies)
    - Unallocated row: Allocation cell renders `—` in font-metric + `not allocated` in text-caption text-text-muted; Weight `—`; action `Allocate…` Button secondary size=sm; OwnershipTag after the name in the Strategy cell
    - Allocated row: `formatUsd(amount)` in font-metric tabular-nums; action `Edit allocation…`; NEVER both buttons on one row; weight renders `—` while current_weight is null (all rows this phase)
    - Empty arms in priority order: (1) ≥1 marked row → the LIST renders; (2) zero marked but user has strategies → heading `No strategies marked as own capital.` body `Set the mark from My Strategies, or when you connect a new key.` + accent underline link `Go to My Strategies →` (href /my-strategies); (3) zero strategies → heading `No strategies yet.` body `Connect an exchange API key or upload a CSV to see your strategies here.`
    - Arm 2 never fires when arm 1's condition holds (priority assertion)
  </behavior>
  <action>
    1. Export formatUsd (body byte-unchanged) — the dialog and the confirm copy consume it; a second money formatter is forbidden (UI-SPEC line 278).
    2. Change the strategy-rows weight cell at :293-295 to the unsigned form — this is a DELIBERATE visual change to a shipped column, decided by research delta 4 (the sibling at :739 is unsigned and contract-pinned; the signed one was the bug). Extend format-percent-contract.test.ts with the strategy-rows pin so the pair can't drift again.
    3. Render OwnershipTag, the two Allocation-cell arms, and the per-row secondary sm buttons per the UI-SPEC row-anatomy table; buttons dispatch an `onAllocate(row)` / `onEditAllocation(row)` callback prop (dialog host is the panel — Task 3).
    4. Replace the dead-end empty state with the three arms (149 anatomy classes; copy byte-exact; priority-order comment copied in spirit from StrategyTable :1067-1085).
    5. Extend HoldingsTable.strategy-rows.test.tsx per the behavior block (needs input: the existing test's fixture shape ← widened StrategyRow from Plan 05).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/HoldingsTable.strategy-rows.test.tsx" src/__tests__/format-percent-contract.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Contract test pins BOTH weight cells unsigned; strategy-rows cell asserts `24.00%` with no leading `+` for a positive weight
    - Row-state assertions: unallocated (—/not allocated/Allocate…), allocated ($ formatted/Edit allocation…), tag presence, never-both-buttons
    - Empty arms asserted in priority order with byte-exact copy
    - `grep -c "export function formatUsd\|export const formatUsd" "src/app/(dashboard)/allocations/components/HoldingsTable.tsx"` == 1
  </acceptance_criteria>
  <done>Rows + arms + unsigned weight ship with the extended contract pin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AllocateDialog — amount, validation, remove confirm, honest weight line</name>
  <files>src/app/(dashboard)/allocations/components/AllocateDialog.tsx, src/app/(dashboard)/allocations/components/AllocateDialog.test.tsx</files>
  <read_first>
    - src/components/portfolio/RemoveStrategyButton.tsx (dialog shape; deviations per 150-PATTERNS §4: fetch not supabase, no disabled-for-validation, ErrorEnvelope not invented strings)
    - src/components/portfolio/MigrationWizard.tsx:63-76 (Number()+isFinite()+>0 parse — copy the parse, not the error string, not the client upsert, not the missing cap)
    - src/components/ui/Modal.tsx + Field.tsx (Field + bare input; no footer slot)
    - src/lib/dollar-validation.ts (MAGNITUDE_CAPS for the client-side cap mirror)
    - 150-UI-SPEC.md Surface 4 "Allocate / Edit dialog" table + Copywriting Contract (byte-binding copy incl. both validation strings and the remove-confirm copy)
    - 150-PATTERNS.md § No Analog Found row 2 (the weight preview: NO book-equity scalar exists — the fallback arm is the only truthful arm this phase; do not invent a divisor)
  </read_first>
  <behavior>
    - Title `Allocate — {name}` / `Edit allocation — {name}`; field label `Allocation (USD)`; edit mode prefills currentAmount
    - Submit with non-finite or ≤0 → inline `Enter an amount above $0.` + red border + field focused; NO fetch; CTA not disabled
    - Submit with > $1,000,000,000 → inline `That's above the $1B sanity cap — check the amount.`; NO fetch
    - Valid submit → POST /api/portfolio-strategies/allocation { portfolio_id, strategy_id, allocated_amount } → 200 → onClose + router.refresh(); NO optimistic row update (the row updates from the confirmed write — UI-SPEC States)
    - Weight preview line renders EXACTLY `Weight appears once your book equity is known.` (the fallback arm; no equity scalar exists this phase — never a fabricated `≈ {w}%`)
    - Edit mode only: `Remove allocation…` text action in text-negative below the footer → body swaps to inline confirm `Remove this allocation? {name} leaves your allocation. The own-capital mark stays.` with `Remove` (Button variant="danger") + `Keep` (secondary); Remove → DELETE the route → close + refresh
    - Server failure → ErrorEnvelope in body, dialog open, input preserved
  </behavior>
  <action>
    Build AllocateDialog per the interfaces block on the RemoveStrategyButton shape with the three deviations. Client-side validation mirrors the server (finite, >0, ≤ MAGNITUDE_CAPS.MAX_DOLLAR_VALUE_USD) but the server remains authoritative; validation renders inline at the field (this form is explicitly IN SCOPE for Phase 153's inline-validation criterion — ROADMAP note — so no terminal envelope for field problems; envelopes are for WRITE failures only). Confirm arm = useState body swap, no nested modal. Amounts render via the exported formatUsd. Primary CTA labels `Allocate` / `Save allocation` per mode.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/AllocateDialog.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All behavior cases green; validation cases assert zero fetch calls and non-disabled CTA
    - `grep -c "supabase" "src/app/(dashboard)/allocations/components/AllocateDialog.tsx"` == 0
    - `grep -c "current_weight" "src/app/(dashboard)/allocations/components/AllocateDialog.tsx"` == 0
    - Copy strings asserted as literals typed into the test (byte-exact vs UI-SPEC)
  </acceptance_criteria>
  <done>The phase's primary CTA works end-to-end against the Plan-05 route with honest copy.</done>
</task>

<task type="auto">
  <name>Task 3: HoldingsTabPanel wiring</name>
  <files>src/app/(dashboard)/allocations/HoldingsTabPanel.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/HoldingsTabPanel.tsx (FULL — :93-96 is the current toStrategyRows({ strategies }) feed; find where props.strategies originates and whether the panel is server- or client-side)
    - src/app/(dashboard)/allocations/lib/strategies-row-adapter.ts (Plan 05's widened input signature)
  </read_first>
  <action>
    Wire the widened data path: fetch the caller's own-capital strategies via getOwnCapitalStrategies (server-side, alongside wherever the existing portfolio_strategies rows are fetched — follow the panel's existing data flow upstream to the page/server component and add the second fetch there, threading it down) and feed the adapter `{ strategies, positions }`. Host the AllocateDialog open-state + the onAllocate/onEditAllocation callbacks at the panel's client boundary (same minting rule as MyStrategiesSection), passing portfolioId + row identity into the dialog. The "has strategies at all" boolean for empty-arm 2 vs 3 comes from the user's strategy count (the marked query result is arm-1's input; reuse an existing any-strategies signal if one is already in the panel's props rather than adding a third query — read first, decide there, document which).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/allocations" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Panel compiles and all allocations-tree tests green
    - `grep -c "getOwnCapitalStrategies" src/app/(dashboard)/allocations/` -r ≥ 1 wired into the panel's data flow
    - No client-side supabase read added (server fetch + props, matching the panel's existing pattern)
  </acceptance_criteria>
  <done>Marked-but-unallocated strategies reach the panel; dialogs mounted; empty arms driven by real signals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser dialog → allocation route | the money write crosses Plan-05's defended boundary; this plan adds no new write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-31 | Tampering | UI offering Allocate… on an already-allocated row (duplicate-add UX half) | mitigate | mode is derived from allocatedAmount null-ness — one row, one button; test pins never-both; DB PK is the hard floor |
| T-150-32 | Tampering (money display) | Fabricated weight/equity numbers in the preview | mitigate | fallback arm only — no divisor exists; source grep pins no current_weight reference; copy states the condition |
| T-150-33 | Spoofing (UI) | Allocate affordance rendering for non-own-capital rows | mitigate | row set is own-capital-only by adapter construction (Plan 05); Plan 08 pins the predicate-in-scope invariant |
| T-150-34 | Denial of Service | Client-side cap mismatch letting absurd amounts hit the server | mitigate | shared MAGNITUDE_CAPS on both sides; server authoritative |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed this phase |
</threat_model>

<verification>
- HoldingsTable strategy-rows + AllocateDialog + format-percent-contract green; allocations tree green; tsc + lint clean.
- Recorded delta restated in SUMMARY: allocated rows show `$amount · —` (not `· 24.00%`) until Phase 151 writes weights — founder-visible, decided at plan time per research Open Question 1.
</verification>

<success_criteria>
- SC 2 user-visible: mark → Holdings row → Allocate… → position, with edit/remove and honest empties.
- D-13's UI half (never both buttons) + D-15's three arms + the unsigned-weight fix all pinned by tests.
</success_criteria>

<output>
Create `.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-07-SUMMARY.md` when done
</output>
