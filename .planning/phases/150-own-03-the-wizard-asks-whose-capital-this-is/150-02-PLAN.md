---
phase: 150-own-03-the-wizard-asks-whose-capital-this-is
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/capital-ownership.ts
  - src/lib/capital-ownership.test.ts
  - src/lib/database.types.ts
  - src/lib/types.ts
  - src/lib/dollar-validation.ts
  - src/lib/dollar-validation.test.ts
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/(dashboard)/allocations/components/HoldingsTable.tsx
  - src/components/strategy/OwnershipTag.tsx
  - src/components/strategy/OwnershipTag.test.tsx
  - src/components/strategy/CapitalOwnershipRadioGroup.tsx
  - src/components/strategy/CapitalOwnershipRadioGroup.test.tsx
autonomous: true
requirements: [OWN-03]
user_setup: []

must_haves:
  truths:
    - "The allocatable predicate is spelled in exactly ONE place (isAllocatable) and only own_capital passes it — NULL and team_review are both non-allocatable"
    - "OwnershipTag renders the accent tag for own_capital, the muted tag for team_review, and NOTHING for null/undefined — never a Draft-badge fallback"
    - "CapitalOwnershipRadioGroup defaults to team_review, has real radio semantics, and single-sources its copy for both future mounts"
    - "The dollar validator AND the USD formatter exist once, shared — finalize-wizard imports the validator instead of its route-local copy; HoldingsTable imports formatUsd instead of its module-private copy (W-7: Plans 06 and 07 both consume it)"
  artifacts:
    - path: "src/lib/capital-ownership.ts"
      provides: "CapitalOwnership type, OWN_CAPITAL/TEAM_REVIEW constants, isAllocatable()"
      exports: ["CapitalOwnership", "isAllocatable", "OWN_CAPITAL", "TEAM_REVIEW"]
    - path: "src/components/strategy/OwnershipTag.tsx"
      provides: "Badge-family ownership tag, null-safe"
      exports: ["OwnershipTag"]
    - path: "src/components/strategy/CapitalOwnershipRadioGroup.tsx"
      provides: "the ONE question component (wizard + Mark dialog mounts)"
      exports: ["CapitalOwnershipRadioGroup"]
    - path: "src/lib/dollar-validation.ts"
      provides: "isValidDollar lifted from finalize-wizard (imports MAGNITUDE_CAPS from @/lib/closed-sets — canonical home, NOT moved, NOT re-exported); formatUsd lifted from HoldingsTable"
      exports: ["isValidDollar", "formatUsd"]
  key_links:
    - from: "src/lib/types.ts Strategy"
      to: "strategies.capital_ownership"
      via: "capital_ownership field on the row types"
      pattern: "capital_ownership"
    - from: "src/app/api/strategies/finalize-wizard/route.ts"
      to: "src/lib/dollar-validation.ts"
      via: "import (route-local copy deleted)"
      pattern: "from \"@/lib/dollar-validation\""
    - from: "src/app/(dashboard)/allocations/components/HoldingsTable.tsx"
      to: "src/lib/dollar-validation.ts"
      via: "formatUsd import (module-private copy deleted)"
      pattern: "formatUsd"
---

<objective>
Mint the shared contracts every downstream plan builds against (interface-first): the
single-source allocatable predicate, the widened row types, the two shared UI primitives
(OwnershipTag, CapitalOwnershipRadioGroup), the lifted dollar validator, and the lifted
USD formatter. No surface mounts anything yet — this plan creates the vocabulary.

Purpose: Wave-2/3 plans (wizard, routes, Holdings, my-strategies, factsheet) all import
from here; without this plan each would re-derive types, copy, and money formatting, which
is exactly the drift the phase gate later forbids. formatUsd lives here (not Plan 07) per
checker W-7 so Plan 06's confirm copy and Plan 07's cells import the SAME formatter with
no local-copy fallback.
Output: four new modules + tests, two type files widened, one import swap in finalize-wizard,
one import swap in HoldingsTable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-UI-SPEC.md
@.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-PATTERNS.md
@DESIGN.md
</context>

<interfaces>
Exact exports this plan mints (downstream plans consume verbatim):

```ts
// src/lib/capital-ownership.ts
export const OWN_CAPITAL = "own_capital" as const;
export const TEAM_REVIEW = "team_review" as const;
export type CapitalOwnership = typeof OWN_CAPITAL | typeof TEAM_REVIEW;
export function isAllocatable(mark: CapitalOwnership | null | undefined): boolean;

// src/components/strategy/OwnershipTag.tsx
export function OwnershipTag({ mark }: { mark: CapitalOwnership | null | undefined }): JSX.Element | null;

// src/components/strategy/CapitalOwnershipRadioGroup.tsx
export function CapitalOwnershipRadioGroup(props: {
  label: string;                       // "Whose capital is in this key?" (wizard) | "Whose capital is this?" (dialog)
  value: CapitalOwnership;             // never null — defaulted by the caller to TEAM_REVIEW
  onChange: (v: CapitalOwnership) => void;
}): JSX.Element;

// src/lib/dollar-validation.ts
// Imports MAGNITUDE_CAPS from "@/lib/closed-sets" — closed-sets.ts:529 is the canonical
// home (already imported by 5 files; review round 3 W-2). NO re-export: downstream
// consumers (Plan 05 route, Plan 07 dialog) import MAGNITUDE_CAPS from @/lib/closed-sets directly.
export function isValidDollar(v: unknown): v is number;          // exact closure moved from finalize-wizard/route.ts:405-409, not rewritten (caps at MAGNITUDE_CAPS.MAX_DOLLAR_VALUE_USD, accepts 0)
export function formatUsd(n: number | null): string;             // exact body moved from HoldingsTable.tsx:72-80, not rewritten
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Predicate module + type widening + dollar-validator and formatUsd lifts</name>
  <files>src/lib/capital-ownership.ts, src/lib/capital-ownership.test.ts, src/lib/database.types.ts, src/lib/types.ts, src/lib/dollar-validation.ts, src/lib/dollar-validation.test.ts, src/app/api/strategies/finalize-wizard/route.ts, src/app/(dashboard)/allocations/components/HoldingsTable.tsx</files>
  <read_first>
    - 150-RESEARCH.md § Code Examples "The allocatable predicate — spell it ONCE" (the module's comment block is spec, copy its reasoning: 3 display states / 2 logic states)
    - src/app/api/strategies/finalize-wizard/route.ts:397-433 (the route-local isValidDollar closure at :405-409 — it closes over MAGNITUDE_CAPS.MAX_DOLLAR_VALUE_USD, ALREADY imported from @/lib/closed-sets — and its aum/max_capacity call sites; move the closure only, do not rewrite; MAGNITUDE_CAPS does NOT move — review round 3 W-2)
    - src/lib/closed-sets.ts:525-546 (MAGNITUDE_CAPS — the canonical caps object with 5 existing importers; dollar-validation.ts IMPORTS it, never re-declares or re-exports it)
    - src/app/(dashboard)/allocations/components/HoldingsTable.tsx:72-80 (the module-private formatUsd — move byte-identical, swap to import; touch NOTHING else in this file: the row/cell work is Plan 07's)
    - src/lib/database.types.ts (find the strategies table Row/Insert/Update blocks; match the generated style exactly — e.g. how `codename: string | null` is declared)
    - src/lib/types.ts:154 (the Strategy interface; also grep the file for RankedStrategyRow)
  </read_first>
  <behavior>
    - isAllocatable("own_capital") === true; isAllocatable("team_review") === false; isAllocatable(null) === false; isAllocatable(undefined) === false
    - dollar-validation test: pin the exact cap value as a LITERAL in the test (1_000_000_000_000 typed in — oracle independence; do not import the constant to assert itself); accepted/rejected boundary cases identical to the pre-move behavior
    - formatUsd behavior byte-identical to the pre-move HoldingsTable output: pin with LITERAL expectations typed into the test (e.g. formatUsd(120000) === "$120,000" — confirm the exact string against the moved body's convention, incl. the null arm)
    - finalize-wizard route behavior is byte-identical for aum/max_capacity validation after the import swap (its existing route.test.ts stays green untouched); HoldingsTable renders identically after its import swap (existing HoldingsTable.strategy-rows.test.tsx stays green untouched)
  </behavior>
  <action>
    1. Create `src/lib/capital-ownership.ts` exactly per the interfaces block, carrying the research comment: three DISPLAY states, two LOGIC states; NULL renders no tag, team_review renders the muted tag; both are non-allocatable; "do not simplify" warning citing 150-RESEARCH.md § Schema Findings 1.
    2. Widen `src/lib/database.types.ts`: strategies Row gains `capital_ownership: string | null`, Insert/Update gain `capital_ownership?: string | null` — hand-edit in the generated style (the file is not regenerated; precedent: the finalize RPC's untyped-cast note in 150-PATTERNS §8).
    3. Widen `src/lib/types.ts` Strategy (and RankedStrategyRow if it is a distinct type that flows to StrategyTable rows — grep the file) with `capital_ownership?: "own_capital" | "team_review" | null`.
    4. Create `src/lib/dollar-validation.ts` by MOVING (cut, not copy) ONLY the `isValidDollar` closure out of finalize-wizard/route.ts:405-409. `MAGNITUDE_CAPS` is NOT route-local — it lives canonically in `@/lib/closed-sets` (closed-sets.ts:529, 5 existing importers; review round 3 W-2): the new module IMPORTS it from there and never re-declares or re-exports it. Update the route to import isValidDollar; delete the route-local closure (keep or drop the route's closed-sets import per remaining usage). Zero behavior change — this exists so Plan 05's allocation route does not mint a second dollar validator (150-RESEARCH § Don't Hand-Roll).
    5. In the SAME module, MOVE (cut, not copy) `formatUsd` from HoldingsTable.tsx:72-80 — body byte-unchanged — and export it; update HoldingsTable to import it from `@/lib/dollar-validation`. This is the ONLY HoldingsTable edit in this plan (W-7: the formatter is a wave-1 shared contract because Plan 06's Mark-dialog confirm copy and Plan 07's cells/dialog both render amounts through it; a second money formatter is forbidden — UI-SPEC line 278).
    6. Tests per behavior block.
  </action>
  <verify>
    <automated>npx vitest run src/lib/capital-ownership.test.ts src/lib/dollar-validation.test.ts "src/app/api/strategies/finalize-wizard/route.test.ts" "src/app/(dashboard)/allocations/components/HoldingsTable.strategy-rows.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "isValidDollar" src/app/api/strategies/finalize-wizard/route.ts` shows only the import + call sites, no local function declaration
    - `grep -c "MAGNITUDE_CAPS" src/lib/dollar-validation.ts` counts only the @/lib/closed-sets import + closure usage — no re-declaration, no re-export (review round 3 W-2)
    - `grep -c "function formatUsd\|const formatUsd" "src/app/(dashboard)/allocations/components/HoldingsTable.tsx"` == 0 and the file imports formatUsd from `@/lib/dollar-validation`
    - `grep -c "capital_ownership" src/lib/database.types.ts` ≥ 3 (Row/Insert/Update)
    - All four isAllocatable truth-table cases asserted with literal expected values; formatUsd pinned with literal strings incl. the null arm
    - finalize-wizard route.test.ts and HoldingsTable.strategy-rows.test.tsx pass with ZERO edits to their assertions
  </acceptance_criteria>
  <done>Predicate + types + shared validator + shared formatter exist; finalize-wizard and HoldingsTable compile against the lifted module; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OwnershipTag component</name>
  <files>src/components/strategy/OwnershipTag.tsx, src/components/strategy/OwnershipTag.test.tsx</files>
  <read_first>
    - src/components/ui/Badge.tsx:52-67 (the class string to reuse verbatim; note the `statusMap[label] ?? statusMap.draft` fallback at :55 — the reason this is a SEPARATE component)
    - 150-UI-SPEC.md "The third chip family" table (exact classes per mark; binding)
    - src/components/strategy/StrategyTable.tsx:915-919 (the "absence of a marker" precedent)
  </read_first>
  <behavior>
    - mark="own_capital" → renders text `Own capital` with classes `inline-flex items-center rounded-md px-2 py-0.5 text-caption font-medium bg-accent/10 text-accent`
    - mark="team_review" → renders text `Team review` with `bg-badge-other/10 text-text-muted` (same anatomy classes)
    - mark=null and mark=undefined → component returns null (renders nothing; assert container is empty)
    - Negative pin: the component does NOT import or reference Badge's statusMap (source assertion in the test)
  </behavior>
  <action>
    Create `OwnershipTag` per the interfaces block: a pure presentational span reusing the Badge anatomy class string VERBATIM (`inline-flex items-center rounded-md px-2 py-0.5 text-caption font-medium`) plus the per-mark ink from the UI-SPEC family table. Labels `Own capital` / `Team review` as module constants. Null/undefined → return null (unmarked legacy rows render no tag — absence is honest, D-11 retro path is the remedy). Do NOT add keys to Badge's statusMap and do not wrap Badge — its `?? statusMap.draft` fallback would render unknown values as a Draft badge.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/OwnershipTag.test.tsx --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - RTL assertions for all four mark values per the behavior block, class assertions on exact token strings
    - `grep -c "statusMap" src/components/strategy/OwnershipTag.tsx` == 0
  </acceptance_criteria>
  <done>Tag component exists with the exact family tokens; null-safe; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CapitalOwnershipRadioGroup — the ONE question component</name>
  <files>src/components/strategy/CapitalOwnershipRadioGroup.tsx, src/components/strategy/CapitalOwnershipRadioGroup.test.tsx</files>
  <read_first>
    - src/components/auth/SignupForm.tsx:174-207 (SEMANTICS analog: role="radio" buttons with aria-checked, each a tab stop — copy this structure)
    - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx:630-655 (CLASS-STRING analog: fieldset + legend `text-caption font-medium text-text-primary`, cards `rounded-md border px-4 py-3 text-left transition-colors`, active `border-accent bg-accent/5`, inactive `border-border bg-white hover:border-accent/50`)
    - 150-UI-SPEC.md Surface 1 "The capital question — anatomy" + Copywriting Contract (binding copy)
    - 150-PATTERNS.md §1 (the reconciliation: SignupForm structure, ConnectKeyStep/UI-SPEC tokens; NO roving tabindex — each option is a tab stop, the repo baseline)
  </read_first>
  <behavior>
    - Renders a fieldset with legend = the `label` prop and an inner role="radiogroup"; two options with role="radio" and correct aria-checked
    - Option labels are EXACTLY `My own capital` and `A trading team's key I'm verifying`; helper line EXACTLY `Own-capital strategies can be allocated from the Holdings tab. A team's key can never join your allocation.`
    - Clicking an option calls onChange with the corresponding CapitalOwnership value; value prop drives aria-checked
    - Copy single-sourcing pin: option labels + helper are module constants; the test asserts them as literals typed into the test (never imported from the component — oracle independence)
  </behavior>
  <action>
    Create `CapitalOwnershipRadioGroup` per the interfaces block. Structure from SignupForm (stacked full-width option-card buttons, `type="button"`, `role="radio"`, `aria-checked`, each a natural tab stop — do NOT invent roving-tabindex); wrapper is `<fieldset>` + `<legend className="text-caption font-medium text-text-primary">{label}</legend>` + inner `role="radiogroup"` div. Card tokens from ConnectKeyStep/UI-SPEC: `rounded-md border px-4 py-3 text-left text-body transition-colors`, selected `border-accent bg-accent/5 text-text-primary font-medium`, unselected `border-border text-text-secondary hover:border-accent/50`; 8px gaps (label→cards, between cards, cards→helper); helper `text-caption text-text-secondary`. Option (b) team_review is the caller-side default — the component itself is controlled and never null. `data-testid` per option (`capital-ownership-own_capital` / `capital-ownership-team_review`). This component is UI-SPEC invariant 5: it will be mounted by BOTH MetadataStep (Plan 03) and MarkOwnershipDialog (Plan 06) — only `label` differs; option copy and helper are module constants.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/CapitalOwnershipRadioGroup.test.tsx --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - RTL test asserts role structure (fieldset/legend/radiogroup/2×radio), aria-checked tracking, onChange values, and the three copy strings as literals
    - `grep -c "tabIndex" src/components/strategy/CapitalOwnershipRadioGroup.tsx` == 0 (no roving-tabindex invention)
    - Component file contains no fetch/supabase import (pure form control)
  </acceptance_criteria>
  <done>The single question component exists with binding copy and repo-baseline radio semantics; tests green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | this plan is types + pure presentational/form components + two code moves; no request handling, no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-07 | Tampering | Predicate drift — a second ad-hoc `=== "own_capital"` comparison later diverging from isAllocatable | mitigate | single-source module now; Plan 08's gate pins the literal to this file only |
| T-150-08 | Spoofing (UI) | Unknown mark value rendering as a trusted-looking badge (Badge statusMap draft-fallback) | mitigate | separate OwnershipTag component with a closed switch; null branch returns null; negative pin on statusMap |
| T-150-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/capital-ownership.test.ts src/lib/dollar-validation.test.ts src/components/strategy/OwnershipTag.test.tsx src/components/strategy/CapitalOwnershipRadioGroup.test.tsx "src/app/api/strategies/finalize-wizard/route.test.ts" "src/app/(dashboard)/allocations/components/HoldingsTable.strategy-rows.test.tsx" --no-file-parallelism` green
- `npx tsc --noEmit` clean; `npm run lint` clean on touched files
</verification>

<success_criteria>
- All four modules exist with the exact exported signatures in the interfaces block (incl. formatUsd).
- finalize-wizard uses the lifted validator and HoldingsTable uses the lifted formatter with zero behavior change.
- Types widened so Wave-2 plans compile against `capital_ownership` without touching these files again.
</success_criteria>

<output>
Create `.planning/phases/150-own-03-the-wizard-asks-whose-capital-this-is/150-02-SUMMARY.md` when done
</output>
