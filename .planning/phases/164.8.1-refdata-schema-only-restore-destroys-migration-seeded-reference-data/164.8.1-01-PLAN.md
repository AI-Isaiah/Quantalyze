---
phase: 164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lint-sql-gates.mjs
  - scripts/restore-test-refdata-allowlist.txt
  - scripts/extract-reference-inserts.mjs
  - scripts/extract-reference-inserts-fixtures/
  - src/__tests__/refdata-allowlist.test.ts
  - .github/workflows/ci.yml
  - .github/workflows/test-restore-from-baseline.yml
autonomous: true
requirements:
  - 164.2-TEST-APPLY-PROVENANCE
  - T-164.8-09

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The allowlist is keyed (migration basename, schema.table, pinned top-level statement count) — never per table — and every line carries the consumer that reads the row (L-02)."
    - "The extractor REFUSES by default: a listed (file, table) whose only INSERT sits inside a dollar-quoted body, an INSERT … SELECT, a pinned count that disagrees with the measured count, a zero-span line, or an emitted span carrying a dollar tag each exit 1 naming file:line, table and reason. Emitted SQL is the ORIGINAL bytes sliced by offset — nothing is rewritten."
    - "`--audit` reddens when any migration gains a top-level literal INSERT into an allowlisted table that has no allowlist line, so the allowlist cannot rot silently; it is wired into `sql-gate-lint` and the restore workflow, self-test first."
    - "`system_settings.analytics_service_url` is on the allowlist and replayed faithfully (L-03); the allowlist is not the place that hazard gets resolved."
    - "The contract test proves the parser classifies (AIM over a synthetic corpus first) AND that every allowlist count agrees with the live corpus in both directions."
  artifacts:
    - "scripts/restore-test-refdata-allowlist.txt — dated masthead + 22 entries over 8 tables in 19 files (⛔ DERIVE this count from the file with `cut -f1 | sort -u | wc -l`, never restate it — 20260510173005 carries TWO entries, which is how the first draft said 20), every entry anchored to the migration line and its consumer"
    - "scripts/extract-reference-inserts.mjs — emit / --audit / --self-test, zero dependencies, reuses maskSql + statements"
    - "scripts/extract-reference-inserts-fixtures/ — one red + one green fixture per refusal kind"
    - "src/__tests__/refdata-allowlist.test.ts — AIM-first, count agreement both directions"
  key_links:
    - "maskSql and statements exported from the lexer in scripts/lint-sql-gates.mjs with no behaviour change — the lint self-test must still print exactly 7 rules"
    - "the `-- refdata-expect: <schema.table>=<n>` trailer the extractor emits is what Plan 02's bash side parses to build the gate's table list and the census keys — its grammar is a contract between the two plans"
    - "the `--audit` step sits in `sql-gate-lint` AFTER the app-GUC steps at .github/workflows/ci.yml:977-979 and in the restore workflow beside the staleness gate at .github/workflows/test-restore-from-baseline.yml:462-469, self-test step BEFORE corpus step in both"
---

<objective>
Build the two things the replay depends on and that need no database: the reference-data
allowlist and the extractor that turns it into replayable SQL, refusing anything it cannot
prove safe. Plan 02 consumes the extractor's output inside the restore transaction.

Purpose: CONTEXT's "the hard part is the ALLOWLIST, not the replay". RESEARCH applied the
criterion (C1 top-level, C2 literals only, C3 target in `public`, plus the SINGLE NAMED `auth.users` exception (RESEARCH A5 — FK head of the teaser trio: that row SURVIVES the restore, which drops only `public`, so replaying it is a no-op via ON CONFLICT; the `public.profiles` and `public.strategies` rows below it do NOT survive, and keeping the head makes the trio FK-self-contained), C4 a named consumer) statement by
statement and found reference-ness is a property of a STATEMENT — `strategies` is a fixture
in 19 files and a required sentinel in one — so the allowlist is keyed (file, table, count)
per the orchestrator's decision L-02. A regex over table names would replay encrypted
`api_keys` fixtures into shared TEST; a per-table list would either do that or lose the
sentinel. The extractor makes the human judgment machine-re-checkable and the `--audit`
mode makes the list unable to rot when the next `*_kind.sql` migration lands.

Output: the allowlist, the extractor with fixtures, the contract test, two CI wirings.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-CONTEXT.md
@.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-RESEARCH.md
@.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-PATTERNS.md
@scripts/lint-sql-gates.mjs
@scripts/check-baseline-staleness.mjs
@scripts/vac08-ledger-baseline.txt
@src/__tests__/vac08-ledger-dispositions.test.ts
</context>

<decisions_honoured>
The orchestrator's four decisions, cited below by ID:
- L-01 (LOCKED, founder): replay lives in `scripts/restore-test-from-baseline.sh`, in the SAME transaction as the schema load, followed by a fail-loud gate. Not in `baseline.sql`, not a curated `reference-data.sql`. This plan builds the INPUT to that replay; it writes no SQL file that a restore would load wholesale.
- L-02: allowlist keyed (migration file, table, pinned statement count).
- L-03: `system_settings.analytics_service_url` replayed faithfully; the TEST-points-at-PROD-compute hazard is booked in Plan 03, never resolved here.
- L-04: machine-run anti-vacuity uses a scratch copy, never a byte backup, never `git checkout --`.
</decisions_honoured>

<tasks>

<task type="auto">
  <name>Task 1: Export the lexer, then write the allowlist — 22 entries, each anchored to its statement and its consumer</name>
  <files>scripts/lint-sql-gates.mjs, scripts/restore-test-refdata-allowlist.txt</files>
  <read_first>
    - scripts/lint-sql-gates.mjs:318-345 — `maskSql`: blanks comments and string interiors, preserves offsets, dollar-quote interior scanned as code; returns `{error,line}` on an unterminated quote.
    - scripts/lint-sql-gates.mjs:533-545 — `statements`: splits the MASKED text on `;`, returns `{start,end,text}` spans.
    - scripts/lint-sql-gates.mjs:1364-1365 — the entrypoint guard; exporting two functions does not change what runs.
    - scripts/vac08-ledger-baseline.txt:1-40 — the masthead shape to copy: dated, states the reading, carries the rules as ⛔/⚠️ lines. Its ledger-presence-is-not-effect-presence paragraph is at :22-27 and this allowlist's header must AGREE with it word for word on that sentence (CONTEXT).
    - 164.8.1-RESEARCH.md §1 — the criterion table (C1-C4) and the applied result table. ⚠️ Its prose summary says "6 tables across 7 files"; its own table lists 22 statements over 8 tables (compute_job_kinds, discovery_categories, feature_flags, system_flags, system_settings, auth.users, profiles, strategies) in 19 files. The TABLE is the content — every row below was re-read at HEAD during planning; the summary line is stale and is not to be copied.
  </read_first>
  <action>
**Export, do not move.** Prefix the export keyword to the maskSql function declaration (scripts/lint-sql-gates.mjs:334) and to the statements function declaration (scripts/lint-sql-gates.mjs:533). Nothing else in that file changes. Run the lint script's own self-test flag and confirm it still prints the 7-rules OK line — the same command CLAUDE.md names as the only source of that count.

**Write `scripts/restore-test-refdata-allowlist.txt`.** Format: `#` comments; entry lines `<basename>.sql<TAB><schema.table><TAB><expected-top-level-statements><TAB># <consumer>`. The masthead (copy the vac08 shape) states, dated 2026-09-09: what an entry means (reference-ness is a property of a STATEMENT — per L-02 an entry names a file, a table and a pinned count, never a bare table); the four-part criterion C1-C4 in one line each; the direction rule — THIS FILE GROWS when a new reference statement is authored and SHRINKS only when the consumer that reads the row is gone, and a pinned count moves only when an applied migration is edited, which is the event that should fail loud; the vac08 sentence on ledger presence versus effect presence, verbatim; the OUT-of-scope rule from CONTEXT — a failure is NAMED, never hand-seeded, because TEST is shared; the known gap from RESEARCH Pitfall 8 — `--audit` sees only tables this file names, a brand-new reference TABLE is still a human call; and the L-03 line — `analytics_service_url` is replayed as the migration wrote it, and the hazard that TEST's tick then targets PROD compute is booked in TODOS `[164.8.1-TEST-ANALYTICS-URL-PROD] (⚠️ this id is CREATED by Plan 03 Task 2 in the same wave; the files are disjoint so there is no race, but do not treat a missing TODOS entry as an error while Plan 03 is still running)`, not edited out here.

The 22 entries, in filename order (counts are top-level statements in that file targeting that table; RESEARCH §1 anchors, all re-resolved at HEAD):
`20260405061911_initial_schema.sql` `public.discovery_categories` 1 (:146-151; consumer `scripts/seed-demo-data.ts` slug lookup + `strategies.category_id` FK);
`20260407164606_perfect_match.sql` `public.system_flags` 1 (:55-56);
`20260411144407_compute_jobs_queue.sql` `public.compute_job_kinds` 1 (:90-94; consumer `compute_jobs.kind` FK at :110);
`20260412094452_poll_positions_kind.sql` 1 (:42-44); `20260416081039_funding_fees.sql` 1 (:176-177); `20260416125429_reconciliation.sql` 1 (:97-98); `20260416125430_contact_request_metadata.sql` 1 (:110-111); `20260418194206_scoring_weight_overrides.sql` 1 (:141-142); `20260420073003_allocator_holdings.sql` 1 (:279-280); `20260420213754_allocator_equity_snapshots.sql` **2** (:144-147); `20260510173005_process_key_long_idempotency_drain.sql` 1 (:105-106); `20260510175507_process_key_long_compute_job_kinds_repair.sql` 1 (:33-34); `20260522111858_compute_analytics_from_csv_kind.sql` 1 (:23-24); `20260614120000_derive_broker_dailies_kind.sql` 1 (:32-33); `20260710130000_stitch_composite_kind.sql` 1 (:44-45); `20260717233529_allocator_equity_derived_surface.sql` 1 (:134-135) — all `public.compute_job_kinds`;
`20260510173005_process_key_long_idempotency_drain.sql` `public.feature_flags` 1 (:279-281);
20260515095804_teaser_anchor_strategy.sql — three lines: auth.users 1 (:48-53), public.profiles 1 (:60-73), public.strategies 1 (:78-90) — consumers src/lib/phase-19-constants.ts:21 and analytics-service/routers/process_key.py:1121; the auth.users line carries RESEARCH A5 in its comment (the row survives the restore anyway; its ON CONFLICT clause makes the replay a no-op, kept for lane-correctness);
20260907120000_analytics_service_settings_and_vault_tick.sql — public.system_settings 1 (:294-296) — with the L-03 note;
`20260907130000_ledger_refresh_switch_to_system_flags.sql` `public.system_flags` 1 (:188-190).

Before finishing, re-derive the compute_job_kinds invariant RESEARCH verified: the 15 statements yield 16 distinct names and `compute_jobs_kind_check` in supabase/schema/baseline.sql:1334 lists exactly those 16. Record the two lists' equality in the masthead as a dated reading, not a constant.
  </action>
  <verify>
    <automated>node scripts/lint-sql-gates.mjs --self-test | grep -qF '7 rules, red+green each' &amp;&amp; grep -q '^export function maskSql' scripts/lint-sql-gates.mjs &amp;&amp; grep -q '^export function statements' scripts/lint-sql-gates.mjs &amp;&amp; test "$(grep -v '^#' scripts/restore-test-refdata-allowlist.txt | grep -c .)" = 22 &amp;&amp; grep -q 'analytics_service_url' scripts/restore-test-refdata-allowlist.txt &amp;&amp; grep -q 'LEDGER PRESENCE IS NOT EFFECT PRESENCE' scripts/restore-test-refdata-allowlist.txt</automated>
  </verify>
  <done>Both functions exported, lint self-test unchanged at 7 rules; the allowlist has exactly 22 non-comment lines, a dated masthead carrying the criterion, the direction rule, the vac08 sentence verbatim, the audit gap, and the L-03 note.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The extractor — emit, --audit, --self-test — refusing by default</name>
  <files>scripts/extract-reference-inserts.mjs, scripts/extract-reference-inserts-fixtures/</files>
  <read_first>
    - scripts/check-baseline-staleness.mjs:1-40 — the header shape: the defect class, the public-repo printing rule, "could not measure" never shares a code path with "measured zero".
    - scripts/lint-sql-gates.mjs:104-112 and :1353-1365 — `--self-test` dispatch and the `FIXTURE_DIR` convention (red + green fixture per rule).
    - scripts/sql-body-normalize.mjs:32-44 — the "KNOWN LIMITATIONS, STATED RATHER THAN HIDDEN" heading PATTERNS asks a new extractor to copy.
    - 164.8.1-RESEARCH.md §2 — the corpus shapes (one- to six-line VALUES literals, uuid casts, a file that manages its own BEGIN/COMMIT) and Pitfall 7.
  </read_first>
  <behavior>
    - emit: for the fixtures' green corpus, prints each allowlisted statement's ORIGINAL bytes (offset slice, byte-identical) in allowlist=filename order, each followed by `-- refdata: <basename>:<line> <schema.table>`, then one `-- refdata-expect: <schema.table>=<n>` trailer per distinct table; exit 0.
    - refuse C1: allowlist names a (file, table) whose only INSERT is inside `DO $$ … $$` → exit 1, message carries basename, table, `inside a dollar-quoted body`.
    - refuse C2: the span contains a SELECT/FROM token, or a tuple identifier outside `NULL/TRUE/FALSE/now()/gen_random_uuid()/::cast` → exit 1 with `not a literal VALUES`.
    - refuse count: pinned 2, measured 1 (and pinned 1, measured 2) → exit 1 printing both numbers.
    - refuse zero: an allowlisted (file, table) with no qualifying span → exit 1.
    - refuse tag: an emitted span that would carry a `$…$` tag → exit 1 (keeps the transaction's replay section body-free).
    - refuse malformed: bad allowlist line, unknown basename, table outside `public.` or `auth.users`, unterminated quote from maskSql → exit 1 naming the line.
    - Pitfall 7: a file with top-level `BEGIN;`/`COMMIT;` around its INSERT emits ONLY the INSERT span.
    - --audit: a corpus dir holding a migration with an unlisted top-level literal INSERT into an allowlisted table → exit 1 naming the file and table; the green corpus → exit 0 printing the census (files scanned, spans classified, per-table statement totals) so the allowlist is re-derivable.
    - --self-test: every refusal kind above fires on its red fixture and stays silent on its green twin; prints `extract-reference-inserts self-test OK: N kinds, red+green each.`
  </behavior>
  <action>
Write `scripts/extract-reference-inserts.mjs` as a zero-dependency Node script (Node 22-safe APIs only — CI runs 22, local is 25). Import `maskSql` and `statements` from `./lint-sql-gates.mjs`. Flags: `--allowlist <path>` (default `scripts/restore-test-refdata-allowlist.txt`), `--migrations <dir>` (default `supabase/migrations`), `--audit`, `--self-test`. Emit mode writes SQL to stdout and diagnostics to stderr; every refusal exits 1 through ONE `refuse(file, line, table, reason)` path that prints names, line numbers, counts and reasons only — never statement text (the repo is public and the restore workflow's log is public; T-164.8-05's redaction grep will scan whatever Plan 02 captures).

Classification runs on the MASKED text (`maskSql` → `statements`); a span qualifies when its masked text matches `^\s*INSERT\s+INTO\s+(public\.)?<table>\b` (C1 falls out — a `DO` block is one span beginning with `DO`). Slice the emitted statement from the ORIGINAL source by `start..end+1` so the bytes are untouched. Implement every refusal in the behavior list. Before trusting the C1 refusal, verify RESEARCH A4 on the real corpus: run emit against the real allowlist and confirm 0 spans are reported for `public.strategies` in any file other than the teaser file — if a DO-body INSERT surfaces as its own span, add an explicit dollar-depth counter over the ORIGINAL text before classification (A4's stated fallback) and add a red fixture for it.

`--audit` scans EVERY `.sql` in the migrations dir for qualifying spans into any table the allowlist names and fails on a (file, table) pair with no allowlist line. Its header states the known gap (a new reference TABLE is a human call) under the "KNOWN LIMITATIONS, STATED RATHER THAN HIDDEN" heading.

Fixtures live in `scripts/extract-reference-inserts-fixtures/` as `<kind>.red/` and `<kind>.green/` corpus dirs each with its own tiny allowlist, so a self-test leg is an end-to-end run, not a unit call. Write the fixtures and the self-test FIRST, observe every red leg go red and the green legs stay green, then run emit + `--audit` on the real corpus: both must exit 0 and the emit output must carry 8 `-- refdata-expect:` trailers with `public.compute_job_kinds=15`.
  </action>
  <verify>
    <automated>node scripts/extract-reference-inserts.mjs --self-test &amp;&amp; node scripts/extract-reference-inserts.mjs --audit &amp;&amp; node scripts/extract-reference-inserts.mjs | grep -c '^-- refdata-expect: ' | grep -qx 8 &amp;&amp; node scripts/extract-reference-inserts.mjs | grep -qx -- '-- refdata-expect: public.compute_job_kinds=15' &amp;&amp; ! node scripts/extract-reference-inserts.mjs | grep -qE '^(BEGIN|COMMIT);'</automated>
  </verify>
  <done>Self-test green with every refusal kind observed red on its fixture; emit on the real corpus exits 0 with 8 trailers and no transaction-control statements; `--audit` on the real corpus exits 0 and prints its census.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Contract test (AIM-first, counts agree both ways) and the two CI wirings</name>
  <files>src/__tests__/refdata-allowlist.test.ts, .github/workflows/ci.yml, .github/workflows/test-restore-from-baseline.yml</files>
  <read_first>
    - src/__tests__/vac08-ledger-dispositions.test.ts:198-240 — the AIM block shape over a synthetic fixture whose answer is NOT the empty set.
    - scripts/lint-app-guc.mjs:188-190 — the count-agreement device: every allowlist count is re-measured against the real file and fails in BOTH directions.
    - .github/workflows/ci.yml:913-979 — the `sql-gate-lint` job's step order (self-test, then corpus) and the sibling-not-rule prose for lint-app-guc.
    - .github/workflows/test-restore-from-baseline.yml:460-469 — the staleness gate's self-test-then-corpus pair.
    - src/__tests__/test-restore-workflow-wiring.test.ts:425-495 — pins over the restore workflow's step names and ORDER; a new step must land between the pinned neighbours without moving them.
    - src/__tests__/lint-sql-gates.test.ts:936-960 — the aggregator-posture pin naming `sql-gate-lint`; adding steps does not move it, but run it.
  </read_first>
  <behavior>
    - AIM first: a synthetic allowlist text with 3 entries (one malformed) parses to 2 entries and 1 named malformed line — asserted BEFORE the live file is read.
    - Live: parsing `scripts/restore-test-refdata-allowlist.txt` yields exactly 22 entries; for each entry, re-measuring the count with the extractor's own classifier over the real migration file equals the pinned count — and a synthetic +1 / -1 on a copy of the entry flips the assertion (calibration).
    - Every entry names a basename that exists under `supabase/migrations/` and a schema in {public, auth}.
    - The file carries the vac08 sentence on ledger presence and the direction rule.
    - Both workflow files carry the self-test step BEFORE the audit step, verbatim `node scripts/extract-reference-inserts.mjs --self-test` then `node scripts/extract-reference-inserts.mjs --audit`.
  </behavior>
  <action>
Write `src/__tests__/refdata-allowlist.test.ts` in the vac08 AIM-first shape, importing the parser and classifier from `scripts/extract-reference-inserts.mjs` (export them). Pin `ENTRY_COUNT = 22` with the both-directions message (GREW: "a reference statement was added — cite its consumer in the entry"; SHRANK: "a consumer went away — say which"). Re-measure each count against the real migration bytes.

Wire the audit into CI. In `.github/workflows/ci.yml` add two steps to `sql-gate-lint` after the app-GUC pair (:977-979): `Reference-data extractor self-test - every refusal fires on its red fixture` running `node scripts/extract-reference-inserts.mjs --self-test`, then `Reference-data allowlist audit - no unlisted top-level reference INSERT in supabase/migrations` running `node scripts/extract-reference-inserts.mjs --audit`. Prose above them, in the job's existing voice: a FOURTH sibling, not a rule of lint-sql-gates — different corpus (`supabase/migrations`), different question (does the restore's allowlist still cover every reference statement), hermetic, no database; a red audit step is cleared by ADDING the allowlist line with its consumer, never by relaxing the classifier. In `.github/workflows/test-restore-from-baseline.yml` add the same two steps directly after the staleness gate pair (:462-469) and before the server-binaries probe, so an unlisted reference INSERT is a NAMED red before anything connects to TEST. Run `npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/lint-sql-gates.test.ts src/__tests__/refdata-allowlist.test.ts` and fix any step-order pin that the insertion moved by re-measuring, never by deleting the pin.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/refdata-allowlist.test.ts src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/lint-sql-gates.test.ts &amp;&amp; grep -c 'node scripts/extract-reference-inserts.mjs --audit' .github/workflows/ci.yml .github/workflows/test-restore-from-baseline.yml | grep -qE ':1$' &amp;&amp; grep -c 'node scripts/extract-reference-inserts.mjs --self-test' .github/workflows/ci.yml .github/workflows/test-restore-from-baseline.yml | grep -qE ':1$'</automated>
  </verify>
  <done>Contract test green with the AIM running first and calibrations flipping; both workflows carry self-test-then-audit exactly once each; existing wiring pins green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| allowlist → replay SQL | a line in a reviewed text file decides what DML reaches shared TEST |
| migration corpus → extractor | untrusted-shaped SQL text (comments, dollar bodies, embedded `;`) is classified |
| extractor → public Actions log | anything the extractor prints is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.1-01 | Tampering | allowlist widened to a fixture table (e.g. `api_keys`) | critical | mitigate | (file, table, count) keying per L-02; C1/C2 refusals are mechanical; every entry carries a consumer the reviewer checks; contract test re-measures counts |
| T-164.8.1-02 | Information disclosure | statement text printed by the extractor | high | mitigate | one `refuse()` path prints names/lines/counts only; Plan 02's arm-level `$tag$`/DSN grep scans the captured output |
| T-164.8.1-03 | Tampering | a DO-body INSERT surfaces as a top-level span (RESEARCH A4) | high | mitigate | verified on the real corpus in Task 2 (0 `strategies` spans outside the teaser file); dollar-depth counter + red fixture as the stated fallback |
| T-164.8.1-04 | Repudiation | a new `*_kind.sql` merges with no allowlist line | medium | mitigate | `--audit` in `sql-gate-lint` and in the restore workflow, self-test first |
| T-164.8.1-05 | Tampering | replaying a file wholesale carries its own COMMIT into the restore transaction | high | mitigate | INSERT-spans-only rule; Pitfall 7 red fixture |
| T-164.8.1-SC | Tampering | npm/pip/cargo installs | low | accept | no packages installed by this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `node scripts/extract-reference-inserts.mjs --self-test` and `--audit` exit 0 locally; emit prints 8 trailers.
- `node scripts/lint-sql-gates.mjs --self-test` still prints 7 rules.
- `npx vitest run src/__tests__/refdata-allowlist.test.ts` green with the AIM first.
- `node scripts/verify-plan-anchors.mjs .planning/phases/164.8.1-*/164.8.1-01-PLAN.md` exits 0 (executor runs this as its first act).
</verification>

<success_criteria>
1. The allowlist has 22 (file, table, count) entries, each with a consumer, and its masthead states the criterion, the direction, the audit gap and the L-03 note.
2. Every refusal kind of the extractor was observed RED on its fixture; the real corpus passes emit and audit.
3. The audit is wired self-test-first in both workflows.
4. What would make this RED: an entry without a consumer; a per-table entry; the extractor rewriting a statement; a refusal printing statement text; `--audit` present in one workflow only.
</success_criteria>

<output>
Create `.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-01-SUMMARY.md` when done, recording the A4 reading on the real corpus and each red-fixture observation.
</output>
