# Phase 164.8.1: REFDATA - Context

**Gathered:** 2026-09-09
**Status:** Ready for planning
**Mode:** Orchestrator-written from MEASUREMENT (autonomous run, --only 164.8.1)

<domain>
## Phase Boundary

Make the TEST restore reproduce the reference rows that migrations INSERT, and fail
loud when it does not.

IN scope: the replay mechanism, the allowlist that decides which tables are reference
tables, and a gate that reddens when an allowlisted table comes back empty.

OUT of scope: hand-seeding shared TEST to go green (Plan 06 says such a failure is
NAMED, not hand-seeded, and TEST is shared with other people's CI); the extension-guard
defect at restore-test-from-baseline.sh:1090 and the never-executed CLI post-verify
(both routed to Phase 164.9); per-run TEST isolation (Phase 164.9).
</domain>

<decisions>
## Implementation Decisions

### LOCKED by the founder, 2026-09-09 - do not re-litigate

**The replay lives in `scripts/restore-test-from-baseline.sh`, after the schema load,
inside the SAME transaction, followed by a non-empty gate.**

Rejected, with the reason recorded so a planner does not "improve" it back:
- NOT appended to `supabase/schema/baseline.sql`. That file's sha256 IS the confirm
  token for the destructive dispatch (`BASELINE.md:54`, workflow step "Refuse restore
  without the confirm token"). Appending data would churn the token on every
  reference-data edit and stop the dump being a byte-identical `pg_dump`.
- NOT a separately curated `reference-data.sql`. It would duplicate values that already
  live in the migrations and can silently drift from them.

### At the planner's discretion
How the allowlist is expressed and where it lives; how statements are extracted; whether
the gate asserts non-empty only or also row counts.
</decisions>

<code_context>
## Existing Code Insights - ALL MEASURED, do not re-derive

**The failure, from CI run 34280468314 (head 7798f865), two independent jobs, one cause:**
- `python`: `insert or update on table "compute_jobs" violates foreign key constraint
  "compute_jobs_kind_fkey"` / `Key (kind)=(sync_trades) is not present in table
  "compute_job_kinds"`.
- `e2e-seeded`: `[seed] discovery_categories row for slug='crypto-sma' not found. The
  initial schema migration (20260405061911_initial_schema.sql) seeds this row - if it's
  missing the migration didn't run.`
  WARNING: that seeder's inference is now FALSE. The migration DID run and IS in the
  ledger. Any fix should consider correcting that misleading message.

**Why nothing puts the rows back:**
- `supabase/schema/baseline.sql` carries 562 GRANT/REVOKE and **0** `COPY`/`INSERT INTO`
  (`grep -cE '^(COPY|INSERT INTO)'` = 0). `BASELINE.md:3` and `:55` both say "schema only,
  zero data statements".
- The restore SEEDS the ledger from the repo file set, so all 266 migrations read as
  applied and `supabase db push` will never replay them - it skips applied versions.
- Therefore: **ledger presence is not effect presence**, and for DML-bearing migrations it
  is false by construction. This sentence is now in the ratchet header; keep the two in
  agreement.

**Why a replay is tractable and carries no disclosure risk (repo is PUBLIC):**
- The reference INSERTs are already idempotent. `compute_job_kinds`: 14 migrations touch
  it, all `ON CONFLICT (name) DO NOTHING` (15 occurrences).
- Every value is a LITERAL in the repo, e.g.
  `INSERT INTO compute_job_kinds (name) VALUES ('stitch_composite') ON CONFLICT (name) DO NOTHING;`
  and `discovery_categories` is a plain multi-row VALUES in `20260405061911_initial_schema.sql`.
  No PROD data leaves PROD.

**⛔ CORRECTED 2026-09-09 by RESEARCH — the orchestrator's framing below was WRONG.**
"Reference table" is the wrong unit: **reference-ness is a property of a STATEMENT, not a
table.** `strategies` is a DO-block fixture in 20 files AND a required application singleton
in one (`20260515095804_teaser_anchor_strategy.sql:78-90`, consumed by
`src/lib/phase-19-constants.ts:21` and `analytics-service/routers/process_key.py:1121`). A
per-TABLE allowlist would either replay 20 files of fixtures (including encrypted `api_keys`
blobs) or drop that sentinel. The allowlist must be keyed
**(migration file, table, pinned statement count)**. See RESEARCH.md for the applied
criterion and the 6-tables-across-7-files result.

**The hard part is the ALLOWLIST, not the replay.**
39 distinct tables receive an `INSERT INTO` somewhere in `supabase/migrations/`. Most are
data backfills inside DO blocks, not reference seeding - `strategies` (21 files, 0
ON CONFLICT) and `compute_jobs` (14 files, 0 ON CONFLICT) are backfills/fixtures, not
reference data. Deciding which are reference tables is PER-TABLE JUDGMENT and must not be
done with a regex. Two are already established by the two red jobs:
`compute_job_kinds`, `discovery_categories`.

**The FK that makes this bite:** `supabase/migrations/20260411144407_compute_jobs_queue.sql:110`
- `kind TEXT NOT NULL REFERENCES compute_job_kinds(name)`.

**Anti-vacuity precedent to follow:** the restore script already carries a counted
self-test (21 arms) and runs on a throwaway PostgreSQL lane via `scripts/pg-lane/run.sh`.
A new gate must be added to that harness and proven able to fail: neuter, observe RED,
⛔ **AMENDED 2026-09-09 by PATTERNS — use a SCRATCH COPY, not a byte backup, for
machine-run arms.** `scripts/mutation-runner/run.mjs:51-54` invariant 1 is that the checkout
is NEVER mutated, which eliminates the stale-byte-backup class. Model on
`restore-test-from-baseline.sh:1328-1346` (arms 11/14). A `cp` byte backup remains correct
for an out-of-band HUMAN neuter record; it is not the idiom for an arm. NEVER
`git checkout --` in either case.
</code_context>

<specifics>
## Specific Ideas

- The gate must fail LOUD and name the empty table. A restore that silently produces an
  empty reference table is the class this whole phase exists to remove.
- `T-164.8-09` in `164.8-04-PLAN.md:208` is REOPENED: it accepted irreversible data loss
  on the compensating control "e2e re-seeds", and that control is measured false. This
  phase is what makes the acceptance honest again.
- ⚠️ **FOUNDER CALL 2026-09-09 — `system_settings.analytics_service_url`.** That seed
  inserts the PROD Railway host (`20260907120000_...:295`) and feeds
  `match_engine_cron_tick()`, a pg_net tick. DECISION: **replay it faithfully.** It is
  PRE-EXISTING, not introduced — the restore drops only `public` (`:861`), so TEST's
  `cron.job` schedule is untouched — and the migration's own guard requires an allowlisted
  https host. Omitting it would leave TEST missing a setting its ledger claims applied.
  ⛔ But TEST pointing at PROD compute is a real hazard that must NOT be silently normalised
  by a restore: book it as its own finding and route it to a phase. Do NOT resolve it by
  editing the allowlist.
- The restore currently exits 1 at `:1090` before reaching `:1097`, so any new step's
  placement relative to that guard matters - do not assume the tail of the script runs.
</specifics>

<deferred>
## Deferred Ideas

- The extension guard (`:1090`) and the never-executed CLI post-verify -> Phase 164.9,
  already routed in ROADMAP.md.
- TODOS entry `[164.8-TEST-DATA-RESEEDED]` is owned by Phase 164.8 Plan 06 and does NOT
  exist in TODOS.md yet (measured 2026-09-09: 0 hits).
</deferred>
