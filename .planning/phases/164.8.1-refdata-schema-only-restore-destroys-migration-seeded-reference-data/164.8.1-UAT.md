---
status: complete
phase: 164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data
source: [164.8.1-01-SUMMARY.md, 164.8.1-02-SUMMARY.md, 164.8.1-03-SUMMARY.md, 164.8.1-04-SUMMARY.md]
started: 2026-09-09T12:00:06Z
updated: 2026-09-09T12:00:06Z
---

## Current Test

[testing complete]

⛔ **READ THIS BEFORE READING THE RESULTS.** Every checkpoint below is marked
`source: recorded` — a named CI run, a workflow log, or a self-test transcript.
**No human observed any of them during this session, and none is claimed to
have.** This phase ships no UI and no user-facing flow: its deliverables are a
destructive restore script, an extractor, and their gates. The one judgement it
genuinely needed from a person — whether to authorise the destructive restore on
the preflight's readings — was taken by the founder in Plan 04 and is recorded
there, not re-asked here.

A checkpoint whose evidence is a run id is stronger than a human saying "looks
right", but it is a DIFFERENT claim, so it is labelled differently.

## Tests

### 1. Cold Start Smoke Test
expected: From an empty database, the restore assembles and runs its transaction, the migration ledger is seeded, and the reference rows the migrations INSERT are present afterwards — not merely a ledger claiming they applied.
result: pass
source: recorded — self-test arm 9 (GREEN restore, full shape round-trips) and arm 22 (reference data replayed, gated and censused) on a throwaway PostgreSQL lane; `--self-test` 26/26 exit 0 at HEAD.
note: injected by the workflow's cold-start rule (the SUMMARYs touch `migrations/*`). The lane is a genuine cold start: initdb, restore, assert.

### 2. Shared TEST carries its migration-seeded reference data again
expected: After the restore, the tables whose rows several migrations exist to insert are populated, where before they were empty while the ledger held all 266 migrations.
result: pass
source: recorded — restore run `34330741339`, conclusion success, every step green and none skipped. Pre-census read `compute_job_kinds=0`, `discovery_categories=0`, `feature_flags=0`, `system_flags=0`, `system_settings=0`; post-census 16 / 5 / 1 / 2 / 1. `profiles` 7 -> 1 is CORRECT and not a loss: DROP SCHEMA removes every row and the replay restores only the allowlisted sentinel.

### 3. The claim is falsifiable, and was falsified
expected: Re-running the SAME commit's previously-failing CI jobs after the restore turns them green — so the restore is what changed, not the code.
result: pass
source: recorded — `python` (27 failures, 26 of them `compute_jobs_kind_fkey`) and `e2e-seeded` (`crypto-sma` missing) both went to success on re-run of the same commit. This is the phase's central evidence: nothing was edited between the red and the green.

### 4. A preflight leaves the database byte-identical
expected: `--mode preflight` runs the whole transaction and rolls it back, and says so on evidence rather than assertion.
result: pass
source: recorded — preflight run `34329459044` ran the transaction and rolled back with no RLS error, answering RESEARCH A1 on hosted TEST (the connecting role DOES bypass RLS — a measurement the self-test lane structurally cannot make, since its `postgres` is initdb's superuser). Machine-armed by arm 8, and by arm 25 for the rollback view CR-01 introduced.

### 5. An incomplete replay fails loud instead of committing
expected: If the replay is deleted, loses a whole statement's rows, loses a single row, or runs without its search_path bracket, the restore ABORTS and rolls back — it never commits a ledger swearing migrations applied over missing rows.
result: pass
source: recorded — arm 23, four legs, each on its own scratch copy: (a) replay deleted -> EMPTY named; (b) search_path bracket deleted -> unqualified targets stop resolving; (c) one row deleted -> the kind-admission invariant names it; (d) a whole statement's rows deleted -> the SHORT floor names it. Every leg also asserts the transaction ROLLED BACK, not merely refused.
note: leg (d) did not exist until the phase review. Before it, reverting the SHORT comparison to `t.n = 0` turned nothing red anywhere.

### 6. The operator is told the truth when a row is missing
expected: The seeder's failure message, read after a restore, leads an operator to the right action instead of the wrong one.
result: pass
source: recorded — message rewritten twice: once for the false "migration didn't run" inference, once for the self-contradiction the code review found (WR-07). It now states that a COMMITTED restore cannot leave the count at 0, because the in-transaction gate aborts on EMPTY — so a missing row means the last restore never ran or never committed.

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

<!-- none -->
