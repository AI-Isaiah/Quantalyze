---
phase: 164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-demo-data.ts
  - TODOS.md
  - .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/deferred-items.md
autonomous: true
requirements:
  - T-164.8-09
  - 164.2-TEST-APPLY-PROVENANCE

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The e2e seeder no longer tells a reader that a missing `crypto-sma` row means the migration did not run — the sentence CI 34280468314 printed is gone and its replacement names the restore's `refdata:` census and the shared-TEST no-hand-seed rule."
    - "The TEST-points-at-PROD-compute hazard (`system_settings.analytics_service_url` seeds the PROD Railway host) is booked as its own dated TODOS finding with a named destination phase — per L-03 it is NOT resolved by editing the allowlist."
  artifacts:
    - "scripts/seed-demo-data.ts — corrected error message at the `discovery_categories` lookup"
    - "TODOS.md — entry `[164.8.1-TEST-ANALYTICS-URL-PROD]` under FIX MID-TERM, routed to Phase 164.9"
    - "deferred-items.md — this phase's deferral record naming the destination"
  key_links:
    - "the seeder message must not name a table's migration as the thing that failed to run — after a baseline restore the ledger says it ran; the sentence that stays true is: the restore replays it and censuses it"
    - "the TODOS entry's destination is a PHASE (founder rule 2026-09-08: every deferral names a phase); Phase 164.9 is the ROADMAP's declared home for hardening deferrals that depend on the restore"
---

<objective>
Two file-disjoint corrections that do not touch the restore script, so they run in wave 1
beside Plan 01: fix the seeder's now-false inference, and book the hazard the founder chose
to replay faithfully rather than normalise away.

Purpose: CONTEXT records that the `e2e-seeded` failure line — "if it's missing the
migration didn't run" — is FALSE after a schema-only restore (the migration ran, is in the
ledger, and only its DML is absent), and that it actively misleads the next reader. And the
founder's L-03 call replays `analytics_service_url` as written, which leaves TEST's
`match_engine_cron_tick()` targeting PROD compute exactly as before the restore; that is a
real hazard the restore must not silently normalise, so it gets its own finding and a phase.

Output: one corrected string, one TODOS entry, one deferral record.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-CONTEXT.md
@scripts/seed-demo-data.ts
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the seeder's inference at the discovery_categories lookup</name>
  <files>scripts/seed-demo-data.ts</files>
  <read_first>
    - scripts/seed-demo-data.ts:901-921 — the lookup, the throw, and the four-line template string; only the message changes.
    - 164.8.1-CONTEXT.md, code_context section — "WARNING: that seeder's inference is now FALSE".
  </read_first>
  <action>
Replace the middle three template lines of the thrown message (the ones read_first anchors at 916-918) so it reads, in substance: the row is seeded by `20260405061911_initial_schema.sql` AND replayed by the TEST restore (`scripts/restore-test-from-baseline.sh`, Phase 164.8.1); if it is missing on TEST, read the restore's `refdata:public.discovery_categories` census row from the last `test-restore-from-baseline` run — a `0` there is a restore defect to NAME, not a reason to hand-seed shared TEST (Phase 164.8 Plan 06 rule); a ledger row for the migration does NOT prove the row exists. Keep the `Error: ${catErr?.message ?? "no rows"}` tail. Do not change the query, the throw, or anything else in the file (surgical change). The sentence being removed is the one that blames the migration for not having run; do not paraphrase it back in.
  </action>
  <verify>
    <automated>! grep -q "the migration didn't run" scripts/seed-demo-data.ts &amp;&amp; grep -q 'refdata:public.discovery_categories' scripts/seed-demo-data.ts &amp;&amp; grep -q 'restore-test-from-baseline.sh' scripts/seed-demo-data.ts &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>The false inference is gone, the replacement names the census row and the no-hand-seed rule, and the file still type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Book the PROD-compute hazard as its own finding, routed to a phase</name>
  <files>TODOS.md, .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/deferred-items.md</files>
  <read_first>
    - TODOS.md:1435-1441 — the `[164.5-TEST-VAC08-DROP-UNAPPLIABLE]` entry: the checkbox-bold-id-title shape, the "booked <date>, founder decision" parenthetical, the ⚠️ contrast paragraph.
    - TODOS.md:1182 — the `## 🟡 FIX MID-TERM` section this entry joins.
    - .planning/ROADMAP.md:1484-1500 — Phase 164.9's charter: "the hardening deferrals' home", items that depend on 164.8's restore live there even when topically about something else.
    - supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql:294-296 — the seed; supabase/schema/baseline.sql:10880 — the CHECK admitting only Railway hosts or the loopback port-9 sink.
  </read_first>
  <action>
Add to `TODOS.md` under FIX MID-TERM, directly after `[164.5-TEST-VAC08-DROP-UNAPPLIABLE]`'s entry, `[164.8.1-TEST-ANALYTICS-URL-PROD]`: TEST's `system_settings.analytics_service_url` is the PROD Railway host, so `match_engine_cron_tick()` (a pg_net tick) on TEST targets PROD compute. Booked 2026-09-09, founder decision (CONTEXT `<specifics>`): the restore REPLAYS the row faithfully per L-03 because it is pre-existing — the same migration seeded the same value on TEST when it applied, the restore drops only `public` so `cron.job` is untouched, and omitting it would leave TEST missing a setting its ledger claims applied. ⚠️ Contrast paragraph: this is NOT an allowlist question and MUST NOT be closed by editing `scripts/restore-test-refdata-allowlist.txt` — the fix is a TEST-side value (the CHECK admits `http://127.0.0.1:9` as a deliberate sink, or a TEST-scoped Railway service) applied by a founder-run statement or a migration guarded by the database identity marker, decided in its destination phase. Destination: **Phase 164.9 TESTISOLATION** — the ROADMAP's declared home for hardening deferrals that depend on the restore; the orchestrator confirms the routing with the founder via `/gsd-phase --edit 164.9`. Memory rule: no DSNs, no credentials, no PROD data in the entry — the host literal is already public in the migration, cite the file:line rather than repeating it.

Create this phase's `deferred-items.md` in the 164.8 shape (dated heading, item, "⛔ Not fixed here" line with the destination phase named). Record two items: the hazard above, and RESEARCH A1/Pitfall 2 (the lane cannot prove hosted BYPASSRLS behaviour under `FORCE ROW LEVEL SECURITY`; Plan 04's preflight is what measures it — not a deferral to another phase but a measurement owed to Plan 04, named here so it is not lost).
  </action>
  <verify>
    <automated>grep -q '\[164.8.1-TEST-ANALYTICS-URL-PROD\]' TODOS.md &amp;&amp; grep -A12 '\[164.8.1-TEST-ANALYTICS-URL-PROD\]' TODOS.md | grep -q 'Phase 164.9' &amp;&amp; ! grep -A12 '\[164.8.1-TEST-ANALYTICS-URL-PROD\]' TODOS.md | grep -qE 'postgres(ql)?://' &amp;&amp; test -s .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/deferred-items.md &amp;&amp; grep -q 'Phase 164.9' .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/deferred-items.md</automated>
  </verify>
  <done>The finding exists with a dated founder decision, a contrast paragraph refusing the allowlist route, and Phase 164.9 as its destination; the deferral record names the same phase and the owed preflight measurement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TEST cron tick → PROD analytics service | a TEST-side pg_net call crosses into PROD compute |
| TODOS/plan text → public repo | every sentence written here is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.1-10 | Elevation | TEST's `match_engine_cron_tick()` calls PROD compute | high | transfer | booked as `[164.8.1-TEST-ANALYTICS-URL-PROD]` with Phase 164.9 as owner; pre-existing, not introduced; NOT masked by the restore (L-03) |
| T-164.8.1-11 | Repudiation | a reader hand-seeds shared TEST because the seeder told them the migration did not run | medium | mitigate | the message now names the census row and the no-hand-seed rule |
| T-164.8.1-12 | Information disclosure | the TODOS entry repeats a host or a DSN | low | mitigate | cite file:line; verify negative-greps the DSN shape |
| T-164.8.1-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- `grep -c "migration didn't run" scripts/seed-demo-data.ts` → 0.
- `npx tsc --noEmit -p tsconfig.json` clean.
- The TODOS entry names Phase 164.9 and no DSN.
</verification>

<success_criteria>
1. The seeder's false inference is gone and replaced by a sentence that stays true after a restore.
2. The hazard is a dated finding with a phase, not an allowlist edit.
3. What would make this RED: the old sentence paraphrased back in; a TODOS entry with no destination phase; an allowlist change in this plan.
</success_criteria>

<output>
Create `.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-03-SUMMARY.md` when done.
</output>
