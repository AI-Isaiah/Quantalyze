---
phase: 164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data
plan: 04
type: execute
wave: 3
depends_on:
  - 164.8.1-01
  - 164.8.1-02
  - 164.8.1-03
files_modified:
  - .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-04-RESTORE.log
autonomous: false
requirements:
  - 164.2-TEST-APPLY-PROVENANCE
  - T-164.8-09

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The FIRST real run of the replay is `--mode preflight` on `main`, and it is read before any `--mode restore` is dispatched — RESEARCH A1: hosted RLS/BYPASSRLS on `compute_job_kinds` is the one thing the lane cannot prove, and only a rolled-back full transaction against TEST measures it."
    - "The preflight's pre-census shows every `refdata:` row at its current value (today `0` for `public.compute_job_kinds`, `public.discovery_categories`) and the post-census is byte-identical (rolled back); the transaction reached COMMIT-or-ROLLBACK without a `relation … does not exist` or an RLS error."
    - "After the founder's restore dispatch, the post-census shows `refdata:public.compute_job_kinds=16`, `refdata:public.discovery_categories=5`, and every other allowlisted table non-zero; the next `main` CI run's `python` and `e2e-seeded` jobs are green, SHA-bound to a commit at or after the restore."
    - "Every reading is recorded with its run id and head sha, names and counts only, in the log."
  artifacts:
    - "164.8.1-04-RESTORE.log — preflight run id + census rows, activity-gate line, restore run id + census rows, the SHA-bound CI readings"
  key_links:
    - "the workflow refuses any ref but `main` (.github/workflows/test-restore-from-baseline.yml:180-188), so Plans 01-03 must be merged first"
    - "the confirm token is still `baseline.sql`'s sha256 (BASELINE.md:54) — unchanged by this phase because the replay lives in the script, not the dump (L-01)"
    - "the activity gate lives INSIDE the workflow under the held mutex (164.8-04-PLAN Task 2 amendment); nothing is probed from a laptop"
---

<objective>
Run the replay for real, preflight first, and let the founder decide the restore on the
preflight's measured readings — then close the loop that opened this phase: a green
`python` and `e2e-seeded` on `main`.

Purpose: the lane proves the mechanism on a fixture; it cannot prove that hosted TEST's
`postgres` role bypasses `FORCE ROW LEVEL SECURITY` on `compute_job_kinds` under
`row_security = off` (RESEARCH Pitfall 2, A1, MEDIUM confidence). `--mode preflight` runs the
whole transaction and rolls it back, so an RLS error or a search_path miss is a NAMED red
with nothing written. Only then is a restore worth dispatching — and TEST is shared, so it is
dispatched under the workflow's own mutex and activity gate, once.

Output: a restored TEST carrying its reference rows, an evidence log, and the reopened
`T-164.8-09` closed on a control that now exists.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-CONTEXT.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-PLAN.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-RESTORE.log
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Merge Plans 01-03, dispatch PREFLIGHT, read the census</name>
  <precondition>Plans 01, 02 and 03 are merged to `main` (the workflow is dispatchable only from the default branch — .github/workflows/test-restore-from-baseline.yml:180-188). The restore script's self-test step printed 24/24 on that merge's CI run.</precondition>
  <action>Merge the PR carrying plans 164.8.1-01, 164.8.1-02 and 164.8.1-03 to main via /gsd-ship (reviewers before the pr-branch filter, per CLAUDE.md). Everything after the merge the executor does itself: a preflight commits nothing.</action>
  <instructions>
    The ONE thing requiring you: merge that PR, then say "merged".
    On resume the executor will: run `gh workflow run test-restore-from-baseline.yml --ref main -f mode=preflight`; read the job log via `gh api repos/:owner/:repo/actions/jobs/&lt;job-id&gt;/logs` (never `gh run view --log`, which truncates); and write `164.8.1-04-RESTORE.log` (names and counts only) with the `which_database:` line, the extractor self-test and audit step results, the self-test tally (24/24), the `refdata:` note line from `refuse_bad_refdata_allowlist`, every `refdata:` row of the pre-census, the transaction outcome, every `refdata:` row of the post-census, the `post-census == pre-census` verdict, `mode=preflight`, run id and head sha.
    If the preflight is RED the ROLLBACK already happened. Read the `ERROR:` line the script extracts: `relation "…" does not exist` means the search_path bracket is wrong (Plan 02 Task 1 — a script fix, re-merge, re-preflight); `new row violates row-level security policy` or `permission denied` on `compute_job_kinds` means RESEARCH A1 is FALSE on hosted TEST — record it, and the fix is `SET LOCAL row_security = off` is NOT enough under FORCE; the remedy is decided by the founder (options: replay as a role that bypasses RLS, or a `SET LOCAL` of a session role) and goes back through Plan 02's self-test, never a hot edit; `restore aborted: reference table … is EMPTY` means the extractor emitted nothing for that table on the runner — compare the runner's emit output with the local one. Task 2 is never reached on a red preflight.
  </instructions>

⛔ **W6 — a FOURTH triage class the first draft omitted: `auth` schema permissions.** The replay writes to `auth.users` for the FIRST time in this script's history, so `permission denied for table users` / `insufficient_privilege` on the `auth` schema is a plausible hosted-only failure the lane cannot reproduce. It rolls back like the others, but without this line the reader has no route. Same routing as the rest: back through Plan 02's self-test, NEVER a hot edit to the script between preflight and restore.

  <verification>test -s .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-04-RESTORE.log and it contains `mode=preflight`, `post-census == pre-census`, and at least one `refdata:public.compute_job_kinds=` line; `gh run list --workflow test-restore-from-baseline.yml -L1` shows `success` on `main`.</verification>
  <resume-signal>Type "merged"; the executor runs the preflight and returns with the readings.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Founder decides the restore on the preflight's readings</name>
  <decision>Dispatch `mode=restore` against shared TEST now, on the preflight's measured `refdata:` readings — or wait.</decision>
  <precondition>Task 1's preflight was GREEN on `main` and its `refdata:` rows are in the log.</precondition>
  <context>The preflight proved the full transaction — dump, survivors, reference replay, gate, ledger seed, shape assertion — runs to completion on hosted TEST and rolls back byte-for-byte. A restore re-runs the same transaction and COMMITs. TEST is shared; the workflow holds the advisory mutex and runs its activity gate before writing (164.8-04-PLAN Task 2's amendment), and refuses without the confirm token. Data loss on TEST is irreversible: everything e2e seeded since the 164.8 restore is dropped with `public`; the reference rows this phase replays are the only rows put back. That is the same trade the founder made on 2026-09-08, now with the compensating control present.</context>
  <options>
    <option id="restore">
      <name>Dispatch mode=restore now with the confirm token from BASELINE.md</name>
      <pros>`python` and `e2e-seeded` go green on the next `main` run; T-164.8-09's compensating control exists and was measured</pros>
      <cons>irreversible drop of TEST's `public` data seeded since the 2026-09-08 restore</cons>
    </option>
    <option id="wait">
      <name>Do not dispatch; leave main red on those two jobs until a quieter window</name>
      <pros>none technical</pros>
      <cons>every branch stays red on the go-live badge gate</cons>
    </option>
  </options>
  <recommendation>restore — the preflight measured the whole transaction, the activity gate is inside the mutex, and the phase exists to make this exact act safe.</recommendation>
  <resume-signal>Type "restore" or "wait".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Dispatch the restore, measure, and close the loop on `main`</name>
  <files>.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-04-RESTORE.log</files>
  <precondition>The founder typed "restore" in Task 2.</precondition>
  <action>
Dispatch `gh workflow run test-restore-from-baseline.yml --ref main -f mode=restore -f confirm=&lt;sha256 from supabase/schema/BASELINE.md&gt;`. Read the job log via the jobs API. Append to the log: the run id and head sha, the `activity-gate:` line verbatim, every `refdata:` row of the post-census, the verbatim `restore:` summary line (it carries no refdata field — that is by design, Plan 02), and the transaction outcome. Expected post-census: `refdata:public.compute_job_kinds=16`, `refdata:public.discovery_categories=5`, `refdata:public.feature_flags=1`, `refdata:public.system_flags=2`, `refdata:public.system_settings=1`, `refdata:public.profiles`, `refdata:public.strategies` and `refdata:auth.users` ≥ 1. A row reading `0` after a committed restore is impossible by construction (the gate would have aborted) — if seen, treat it as a MEASURE_FAIL of the census, not of the replay, and stop.

⛔ The extension guard at scripts/restore-test-from-baseline.sh:1090 may still exit 1 AFTER the commit (Phase 164.9's item). That exit is not a failed restore: the census rows above are the evidence, and the log must say which line the run stopped on.

Then close the loop: wait for the next `main` push CI run at a sha at or after the restore (SHA-BOUND — a settled board belonging to an ancestor proves nothing; MEMORY "CI green must be SHA-bound") and record `python` and `e2e-seeded` as `success` with the run id. Record that `T-164.8-09` is re-accepted on a control that now exists (this phase's replay + gate, arm 23) in the SUMMARY; do not edit 164.8-04-PLAN.md.
  </action>
  <verify>
    <automated>grep -q 'mode=restore' .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-04-RESTORE.log &amp;&amp; grep -q 'refdata:public.compute_job_kinds=16' .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-04-RESTORE.log &amp;&amp; grep -q 'refdata:public.discovery_categories=5' .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-04-RESTORE.log &amp;&amp; grep -qE 'e2e-seeded: success .*run [0-9]+' .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-04-RESTORE.log &amp;&amp; grep -qE 'python: success .*run [0-9]+' .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-04-RESTORE.log &amp;&amp; ! grep -qE 'postgres(ql)?://' .planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-04-RESTORE.log</automated>
  </verify>
  <done>The log holds both run ids, the census rows at their expected values, and SHA-bound green readings for the two jobs that opened this phase; no DSN in the log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder decision → destructive dispatch | the only human authorisation of a write to shared TEST |
| shared TEST → other people's CI | a torn schema or a half-replayed registry is visible to any run not holding the mutex |
| Actions log → public | census rows and summary lines are world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.1-13 | Elevation | restore dispatched before a preflight of the NEW transaction | critical | mitigate | Task 1 is preflight-only; Task 2's decision needs Task 1's `refdata:` rows in the log; Task 3's precondition is the typed decision |
| T-164.8.1-14 | Denial of service | replay fails on hosted TEST only (RLS/BYPASSRLS, A1) | medium | mitigate | preflight rolls back; the instructions name each error class and its route back through Plan 02's self-test — no hot edit |
| T-164.8.1-15 | Tampering | partial registry visible to foreign CI | high | mitigate | one atomic transaction under the mutex; the gate aborts on any empty table and the kinds/CHECK invariant aborts on a partial registry |
| T-164.8-09 | Repudiation | data loss on TEST irreversible | high | accept | re-accepted here on the measured control: the restore's own replay + gate (arm 23 proves it can fail) and the post-census rows recorded with run id; stated in Task 2's context |
| T-164.8-14 | Repudiation | readings restated from memory | medium | mitigate | every number carries run id + sha; the SUMMARY quotes the log |
| T-164.8.1-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- Preflight green, then restore green, both on `main`, both run ids in the log.
- Post-census `refdata:` rows at the expected values.
- `python` and `e2e-seeded` green on a `main` run SHA-bound at or after the restore.
</verification>

<success_criteria>
1. The first real run of the replay was a preflight, and the founder decided on its readings.
2. TEST carries its reference rows and the two red jobs are green, SHA-bound.
3. What would make this RED: a restore run id with no preceding preflight run id for this phase's script; a census row at `0` after a commit; a green reading quoted from a run at an ancestor sha; a DSN in the log.
</success_criteria>

<output>
Create `.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-04-SUMMARY.md` when done, quoting the log's run ids, the `refdata:` rows and the SHA-bound CI readings verbatim.
</output>
