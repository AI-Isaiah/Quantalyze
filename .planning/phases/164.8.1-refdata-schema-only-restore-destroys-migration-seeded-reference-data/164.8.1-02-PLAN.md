---
phase: 164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data
plan: 02
type: execute
wave: 2
depends_on:
  - 164.8.1-01
files_modified:
  - scripts/restore-test-from-baseline.sh
  - scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt
  - scripts/restore-test-from-baseline-fixtures/migrations/20260101000000_fixture_a.sql
  - scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql
  - src/__tests__/restore-test-from-baseline.test.ts
autonomous: true
requirements:
  - 164.2-TEST-APPLY-PROVENANCE
  - T-164.8-09

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Inside the restore transaction, after the dump and survivors and before the ledger seed, every allowlisted reference statement is replayed under `SET LOCAL search_path = public, pg_catalog`, and `pg_catalog` is restored before the ledger DDL (L-01; RESEARCH Pitfall 1)."
    - "A `DO` gate in the SAME transaction names every allowlisted table that is empty after the replay and aborts with `restore aborted:` so the transaction rolls back; it also refuses when `compute_jobs_kind_check` admits a kind the registry lacks (a partial replay)."
    - "The census carries one `refdata:<schema.table>=<n|absent>` row per allowlisted table, so the reading survives the `:1090` exit and a preflight's byte-for-byte compare still holds."
    - "The self-test proves the gate CAN FAIL: arm 23 runs a scratch copy whose replay line is deleted, observes exit 1, the gate's sentence naming the table, and `e2e_leftover` still present (rollback, not just refusal) — per L-04, never the checkout, never a byte backup."
    - "`EXPECTED_ARMS` moves 21 → 24 in the same commit as the arms, with the MEASURED line re-dated, and every pin that the change reddens is re-measured, not deleted."
  artifacts:
    - "scripts/restore-test-from-baseline.sh — seams REFDATA_ALLOWLIST/REFDATA_EXTRACTOR, refuse_bad_refdata_allowlist(), replay block, gate block, census rows, arms 22/23/24, EXPECTED_ARMS=24"
    - "scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt — one line: 20260103000000_fixture_c.sql public.fx_keep 1"
    - "scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql — carries the fixture reference INSERT"
    - "src/__tests__/restore-test-from-baseline.test.ts — ratchet pins at 24, MEASURED date, softening-token set re-measured, prose updated"
  key_links:
    - "the `-- refdata-expect:` trailer grammar from Plan 01 is what build_transaction() parses to build the gate's VALUES list and what write_census_sql() reads for its rows — one parse, charset-refused before interpolation"
    - "the fifth ARM_* seam (`ARM_REFDATA_ALLOWLIST`) — without it every existing arm inherits the real allowlist naming tables the fixture dump never creates and all 21 go red on a table the fixture was never supposed to have (PATTERNS §2)"
    - "arms 9/12/18 grep the `restore:` summary line with `grep -aqxF` at scripts/restore-test-from-baseline.sh:1731, :1801, :1907 — the summary line must NOT grow a field; counts go in the census"
---

<objective>
Put the replay and its gate where the founder locked them (L-01): inside `build_transaction()`
after the schema load and before the ledger seed, then prove on the throwaway lane that the
replay commits, that the gate bites and rolls back, and that the script refuses a bad
allowlist before any write.

Purpose: CONTEXT's failure — a ledger that swears 266 migrations applied while
`compute_job_kinds` is empty and `discovery_categories` has no `crypto-sma` — is removed by
construction: a restore that leaves an allowlisted table empty cannot commit. The two
placement traps RESEARCH measured decide the shape: `search_path` is `pg_catalog` at the
replay site (scripts/restore-test-from-baseline.sh:872-875), so unqualified statements need
`SET LOCAL search_path`; and the tail after the extension guard at :1090 does not run on TEST
today, so visibility is census rows, not a post-restore print.

Output: the script edits, the fixture substrate, three new arms with their observed-RED
records, and the vitest pins re-measured.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-CONTEXT.md
@.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-RESEARCH.md
@.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-PATTERNS.md
@scripts/restore-test-from-baseline.sh
@src/__tests__/restore-test-from-baseline.test.ts
@scripts/restore-test-from-baseline-fixtures/old-test.sql
@scripts/restore-test-from-baseline-fixtures/baseline-fixture.sql
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end on the lane — one fixture reference row replayed, gated, censused, committed</name>
  <files>scripts/restore-test-from-baseline.sh, scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt, scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql, scripts/restore-test-from-baseline-fixtures/migrations/20260101000000_fixture_a.sql</files>
  <precondition>Plan 01 is merged or checked out on this branch: `node scripts/extract-reference-inserts.mjs --self-test` exits 0 and `scripts/restore-test-refdata-allowlist.txt` has 22 entries.</precondition>
  <read_first>
    - scripts/restore-test-from-baseline.sh:212-216 — `fail`/`note`, the ONLY two vocabularies; no `::warning`.
    - scripts/restore-test-from-baseline.sh:218-228 — the env seams with repo defaults; add two here.
    - scripts/restore-test-from-baseline.sh:270-300 — `write_census_sql`: quoted heredoc, `SET search_path = pg_catalog` first, the `to_regclass` + `query_to_xml` idiom for a relation that may be absent.
    - scripts/restore-test-from-baseline.sh:405-407 — `sql_lit`; :467-484 — `refuse_bad_migration_corpus`, the pre-write refusal shape (module-level array, charset refusal, EMPTY-is-an-error, closing `note`).
    - scripts/restore-test-from-baseline.sh:726-749 — `TXN_WHITELIST`: the loop-over-VALUES abort shape with `\$restore\$` in an UNQUOTED heredoc — the gate copies it with the predicate inverted.
    - scripts/restore-test-from-baseline.sh:860-892 — DROP, dump, `TXN_MID` (search_path pg_catalog), survivors at :875, ledger DDL at :880; the replay lands between :875 and :877.
    - scripts/restore-test-from-baseline.sh:1014-1017 — the filesystem-only refusal block the allowlist refusal joins.
    - scripts/restore-test-from-baseline.sh:1027 — `write_census_sql` is called AFTER that block, so the census can read the parsed table list.
    - scripts/restore-test-from-baseline.sh:1409-1425 — the four ARM_* seams and `run_leg`; :1437-1447 — the arm contract.
    - .planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/deferred-items.md:99 — the unescaped-backtick incident in an unquoted heredoc; escape `$` and backticks or split into a quoted heredoc plus an interpolated fragment.
  </read_first>
  <action>
**Seams.** Beside `BASELINE_FILE` (:218) add `REFDATA_ALLOWLIST` defaulting to `scripts/restore-test-refdata-allowlist.txt` and `REFDATA_EXTRACTOR` defaulting to `scripts/extract-reference-inserts.mjs`.

**Pre-write refusal.** `refuse_bad_refdata_allowlist()` after `refuse_bad_migration_corpus` in the :1014-1017 block: the file must exist and be non-empty of entries (EMPTY is an error — say what emptiness would silently produce: a restore that commits with every reference table empty, which is the Phase 164.8 defect); run the extractor with `--allowlist "$REFDATA_ALLOWLIST" --migrations "$MIGRATIONS_DIR"` capturing stdout to `$RESTORE_OUT_DIR/refdata.sql` is NOT possible here because the out dir does not exist yet — so this refusal runs the extractor in `--audit`-free emit mode to a mktemp file it owns, keeps only the exit code and the parsed `-- refdata-expect:` trailers, and stores the table list in a module-level `REFDATA_TABLES=()` after a charset refusal (`*[!a-z0-9_.]*` — these names reach SQL). Non-zero from the extractor is `fail "reference-data extraction refused (exit N); nothing was replayed and no transaction was assembled."` with the extractor's stderr echoed first. Use the arm idiom `rc=0; node … || rc=$?` — NOT `set +e`/`|| true` — so the softening-token exact-set pin (Task 3's read_first names it) does not move; if it moves anyway, re-measure the pin and extend its justification prose, never widen it silently. Close with `note "refdata: N table(s) from M allowlist line(s) under $REFDATA_ALLOWLIST"`.

**Census.** In `write_census_sql`, after the `ledger_bad_version` row, emit one row per `REFDATA_TABLES` entry in the `to_regclass` + `query_to_xml` form so a table absent pre-restore reads `absent`: key `refdata:<schema.table>`. The heredoc is quoted today; generate these rows with a `printf` loop appended after the heredoc rather than unquoting it (deferred-items :99).

**Replay.** In `build_transaction()` between the survivors line (:875) and the ledger comment (:877): re-run the extractor into `$RESTORE_OUT_DIR/refdata.sql` (same rc idiom, same fail sentence), then append a quoted-heredoc head that sets `SET LOCAL search_path = public, pg_catalog;` with a comment naming Pitfall 1, then `cat "$RESTORE_OUT_DIR/refdata.sql" >> "$out"` on its OWN line (arm 23's anchor), then a quoted-heredoc tail restoring `SET LOCAL search_path = pg_catalog;`. `note` the count of replayed statements the way :979 notes the assembled transaction.

**Gate.** Directly after the tail and BEFORE the ledger DDL, an unquoted heredoc `TXN_REFDATA_GATE` in the `TXN_WHITELIST` shape: `DO \$restore\$` … `FOR r IN SELECT * FROM (VALUES <one ('schema.table', (SELECT count(*) FROM schema.table)) per REFDATA_TABLES, built by a printf loop through sql_lit>) AS t(tbl, n) WHERE t.n = 0 ORDER BY 1 LOOP RAISE EXCEPTION 'restore aborted: reference table % is EMPTY after the replay — the ledger would say its seed migration applied while its rows are gone; fix the allowlist line or the extractor, never hand-seed shared TEST (Phase 164.8.1)', r.tbl; END LOOP;` then the invariant: when `to_regclass('public.compute_job_kinds')` is not null and a constraint named `compute_jobs_kind_check` exists, collect the quoted kind literals from `pg_get_constraintdef` with `regexp_matches(…, '''([a-z_]+)''', 'g')` in LATERAL form and `RAISE EXCEPTION 'restore aborted: compute_jobs_kind_check admits kind(s) % that compute_job_kinds does not carry — a partial replay'` when any is missing; on the fixture lane neither object exists so this leg is a no-op there.

⛔ **W1 — THAT IS NOT ACCEPTABLE AS WRITTEN, and closing it is part of this task.** An invariant leg no arm exercises is decorative: if the LATERAL `regexp_matches` form returns nothing, `v_missing IS NULL` always and the partial-replay check passes VACUOUSLY FOREVER. That is the 'a test that cannot fail is worse than none' class this repo treats as most severe regardless of label. REQUIRED: make the table and constraint names SEAMS (defaults `public.compute_job_kinds` / `compute_jobs_kind_check`), add a `fx_keep_kind_check`-style analog to `baseline-fixture.sql`, and give arm 23 a THIRD LEG that deletes one kinds row on the lane and asserts the invariant's OWN sentence. Observe that leg RED before believing it — say so in the comment. Plain `count(*)`, no `to_regclass` guard on the loop (PATTERNS 1b: inside the transaction the dump just created these tables; a guard here would be the softening this phase removes).

**Fixture substrate.** `scripts/restore-test-from-baseline-fixtures/refdata-allowlist.txt`: one entry `20260103000000_fixture_c.sql` / `public.fx_keep` / 1. Add to `20260103000000_fixture_c.sql` an `INSERT INTO fx_keep (id, label) VALUES (1, 'ref-a'), (2, 'ref-b') ON CONFLICT (id) DO NOTHING;` — `fx_keep` is chosen over `fx_other` because it exists in `old-test.sql` (pre-census reads `=0`, the real TEST shape) and carries RLS + a SELECT-only policy in the dump, the closest the lane gets to Pitfall 2. Update `20260101000000_fixture_a.sql`'s header (its "NEVER executed by anything" sentence is now false for an allowlisted statement — say the restore replays allowlisted reference INSERTs and nothing else). Add the fifth seam `local ARM_REFDATA_ALLOWLIST="$FIXTURES/refdata-allowlist.txt"` beside :1412 and `REFDATA_ALLOWLIST="$ARM_REFDATA_ALLOWLIST"` in `run_leg`.

**Then run the whole self-test** — it must still print 21/21 (the ratchet is untouched in this task) and arm 9's output must show `refdata:public.fx_keep=0` in the pre-census and `=2` in the post-census. That is the tracer's end-to-end proof; arm 22 formalises it in Task 2.
  </action>
  <verify>
    <automated>bash scripts/restore-test-from-baseline.sh --self-test 2>&amp;1 | tee /dev/stderr | grep -q 'self-test OK (21/21' &amp;&amp; grep -q 'SET LOCAL search_path = public, pg_catalog' scripts/restore-test-from-baseline.sh &amp;&amp; grep -q 'reference table % is EMPTY after the replay' scripts/restore-test-from-baseline.sh &amp;&amp; ! grep -aE '^  echo "restore: tables=.*refdata' scripts/restore-test-from-baseline.sh</automated>
  </verify>
  <done>Self-test still 21/21 with the replay live on every restore arm; the summary line is unchanged; the gate and search_path bracket are in the transaction; the census carries the refdata row.</done>
</task>

<task type="auto">
  <name>Task 2: Arms 22, 23, 24 — GREEN commits, RED gate bites on a scratch copy, RED allowlist refused before any write — EXPECTED_ARMS=24</name>
  <files>scripts/restore-test-from-baseline.sh</files>
  <read_first>
    - scripts/restore-test-from-baseline.sh:1141-1194 — the inventory table, the LEG list, the ratchet comment and its MEASURED line.
    - scripts/restore-test-from-baseline.sh:1328-1346 — the scratch-copy idiom: awk with an anchor assertion, `grep -aq` proving the mutation landed, both failures MEASURE_FAIL.
    - scripts/restore-test-from-baseline.sh:1712-1752 — `arm_g_restore`, the GREEN shape; :1810-1826 — `arm_derived_census_orphan`, the RED shape with the `e2e_leftover` rollback witness; :1984-2024 — `arm_bad_migration_corpus`, the multi-leg refusal shape with per-leg reason greps.
    - scripts/restore-test-from-baseline.sh:1771-1777 — MUTATE THE EXPECTED SIDE: mutating a migration raises both the replay and the gate; arm 23 therefore weakens the REPLAY line while the allowlist-driven gate keeps its subject.
    - scripts/restore-test-from-baseline.sh:2026-2047 — registration; :2060-2068 — the two count checks and the closing narrative.
  </read_first>
  <action>
**Arm 22 GREEN** `arm_g_refdata_replay`: `setup_lane`, `arm_env restore a22`, assert exit 0; `lane_q "SELECT count(*) FROM public.fx_keep"` = 2; `grep -aq 'refdata:public.fx_keep=0'` in the pre-census section and `=2` in the post-census section of the captured output (read the two census files under `$SELFTEST_TMPD/out-a22` rather than the interleaved log); `e2e_leftover` count 0 (committed).

**Arm 23 RED — the gate bites** `arm_refdata_gate_bites`: build `$SELFTEST_TMPD/refdata-no-replay.sh` from `$0` with the :1328-1346 awk idiom, DELETING the line `cat "$RESTORE_OUT_DIR/refdata.sql" >> "$out"` (anchor-asserted; then `! grep -aq` proving the copy lacks it); `run_leg "$copy" restore a23`; assert exit 1; `grep -aq 'reference table public.fx_keep is EMPTY after the replay'` — on the `ERROR:` line the :997 extraction prints, not a NOTICE; `lane_q` `e2e_leftover` count = 1 (rolled back, not merely refused). Per L-04 the checkout is never touched. This is the phase's falsifier: the allowlist still names `fx_keep`, the gate still counts it, only the replay is gone — expected side intact, observed side empty.

**Arm 24 RED — refused before any write** `arm_bad_refdata_allowlist`, two legs each with its own reason grep: (a) a scratch allowlist whose only line pins count 2 for `fixture_c`/`fx_keep` (measured 1) → exit 1, output carries `reference-data extraction refused` and both numbers, and `refdata.sql` does not exist under the out dir; (b) an EMPTY allowlist file → exit 1 with the empty-is-an-error sentence. Both via `local ARM_REFDATA_ALLOWLIST=…` and `arm_env preflight`; assert the pre-census never ran (no `pre-census` banner in the output) so the refusal is proven pre-write.

**Ratchet.** Add rows 22-24 to the inventory table and a LEG note for arm 24; extend the closing narrative at :2068 with "reference data is replayed and gated inside the transaction, the gate bites on a scratch copy, a bad allowlist is refused before any write"; register the three `run_arm` lines in numeric order; set `EXPECTED_ARMS=24` and rewrite the MEASURED line as `MEASURED 2026-09-09 — --self-test prints 24/24 …` (SC-9 needs the token and the date beside the constant); add to the ratchet comment that arms 22-24's falsifiers are recorded in this phase's 02-SUMMARY.

**Observe RED before trusting each arm**, on scratch copies only: for arm 22, a copy with the fixture INSERT's `ON CONFLICT` clause intact but the allowlist pointing at `fx_other` must fail arm 22's `fx_keep`-count assertion; for arm 23, a copy of the harness whose awk anchor string is misspelt must MEASURE_FAIL with ANCHOR-NOT-FOUND, and a copy where the gate's `WHERE t.n = 0` reads `t.n < 0` must make arm 23 red (exit 0 where 1 was expected); for arm 24, a copy where the refusal's `fail` is replaced by `note` must make leg (a) red. Record each observation with its scratch path in the SUMMARY. Run the full self-test: 24/24, exit 0, and confirm the redaction check passed on every new arm (no `$tag$`, no DSN in the captured output — the gate message prints table names only).
  </action>
  <verify>
    <automated>bash scripts/restore-test-from-baseline.sh --self-test 2>&amp;1 | tee /dev/stderr | grep -q 'self-test OK (24/24' &amp;&amp; test "$(grep -c '^EXPECTED_ARMS=24$' scripts/restore-test-from-baseline.sh)" = 1 &amp;&amp; grep -q 'MEASURED 2026-09-09' scripts/restore-test-from-baseline.sh &amp;&amp; grep -c 'run_arm "2[234] ' scripts/restore-test-from-baseline.sh | grep -qx 3</automated>
  </verify>
  <done>24/24 arms green; arm 23 was observed to go red when the gate's predicate was neutered on a copy and green again on the real script; the ratchet, inventory, MEASURED line and narrative moved in one commit.</done>
</task>

<task type="auto">
  <name>Task 3: Re-measure every vitest pin the change touches</name>
  <files>src/__tests__/restore-test-from-baseline.test.ts</files>
  <read_first>
    - src/__tests__/restore-test-from-baseline.test.ts:172-228 — the ratchet pins: `liveCount(region, "EXPECTED_ARMS=21")` exactly once, the SC-9 adjacency `MEASURED 2026-09-08`, and the two CALIBRATION replace calls whose needle is the constant's own line — every literal moves to 24 and the date to 2026-09-09. Count them with grep, do not restate PATTERNS' "six".
    - src/__tests__/restore-test-from-baseline.test.ts:540-577 — the softening-token exact set and its justification prose; re-measure `|| true` and `set +e` over the `--run` region AFTER Tasks 1-2 and, if either moved, update the number AND the per-site prose (a new `set +e` must be a checked rc capture, never a swallow).
    - src/__tests__/restore-test-from-baseline.test.ts:638-703 — the fixture-corpus pins: three migrations by name (unchanged — the INSERT went into an existing file), seven overlays equal to what `setup_lane` loads (unchanged — arms 22-24 use no overlay; if an overlay was added, add it to BOTH the list and a `setup_lane` call).
    - src/__tests__/restore-test-from-baseline.test.ts:145-168 — region slicers; the new functions are new names, `build_transaction`/`run_restore` were not renamed.
  </read_first>
  <action>
Update every literal of the OLD ratchet value (the constant's name followed by 21 — count them with grep first) in the ratchet describe to the new value 24, and the SC-9 adjacency to `MEASURED 2026-09-09`, keeping the CALIBRATION replace strings in step (a calibration whose needle no longer matches passes vacuously — assert `mutated !== SRC` still holds, it already does). Re-measure the softening set and record the reading in the comment. Add ONE new pin in the `build_transaction` region: the live line order `SET LOCAL search_path = public, pg_catalog` < `cat "$RESTORE_OUT_DIR/refdata.sql"` < `SET LOCAL search_path = pg_catalog` < `TXN_REFDATA_GATE` < `TXN_LEDGER_DDL`, with a calibration that swaps the gate below the ledger DDL on an in-memory copy and asserts the pin flips — this is the ordering L-01 locked and the one an "improvement" would most plausibly break. Run the file plus `src/__tests__/test-restore-workflow-wiring.test.ts` and `src/__tests__/drift-check-scripts.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/restore-test-from-baseline.test.ts src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/drift-check-scripts.test.ts &amp;&amp; test "$(grep -c 'EXPECTED_ARMS=21' src/__tests__/restore-test-from-baseline.test.ts)" = 0 &amp;&amp; grep -q 'MEASURED 2026-09-09' src/__tests__/restore-test-from-baseline.test.ts</automated>
  </verify>
  <done>No `EXPECTED_ARMS=21` literal survives; the date pin, softening set and fixture pins are re-measured and green; the new ordering pin flips on its calibration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| allowlist table names → SQL text | names are interpolated into the gate's VALUES list and the census |
| replay SQL → shared TEST `public` | DML committed inside the restore transaction |
| self-test output → public Actions log | every arm's capture is grepped for DSNs and dollar tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.1-06 | Tampering | table name interpolated into SQL | high | mitigate | charset refusal `*[!a-z0-9_.]*` in `refuse_bad_refdata_allowlist()` before any interpolation, then `sql_lit` — the :467-484 + :405-407 pair (belt and braces, T-164.8-04) |
| T-164.8.1-07 | Tampering | partial replay commits | critical | mitigate | non-empty gate per table INSIDE the transaction; the kinds/CHECK invariant; Plan 01's per-file pins and refuse-on-zero; arm 23 proves rollback |
| T-164.8.1-08 | Denial of service | replay fails on hosted TEST for a reason the lane cannot see (RLS/BYPASSRLS, RESEARCH A1) | medium | mitigate | the transaction rolls back; Plan 04 dispatches `--mode preflight` FIRST and reads the census; recorded beside the self-test's stated limits at :1180-1184 |
| T-164.8.1-09 | Information disclosure | gate/refusal messages carry statement text into the public log | high | mitigate | messages print table names, counts and file:line only; the per-arm redaction grep at :1466 is the check with arm 14 as its subject |
| T-164.8-09 | Repudiation | data loss on TEST irreversible; the "e2e re-seeds" control was measured false | high | mitigate | the compensating control is now the restore's OWN replay + gate, proven able to fail by arm 23; re-accepted only after Plan 04's real preflight and restore census show every `refdata:` row non-zero |
| T-164.8.1-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- `bash scripts/restore-test-from-baseline.sh --self-test` → 24/24, exit 0, on the throwaway lane.
- `npx vitest run src/__tests__/restore-test-from-baseline.test.ts` green after the re-measure.
- `node scripts/verify-plan-anchors.mjs` over this plan exits 0 (executor's first act).
- A `git status --porcelain` that is clean of scratch artifacts after the self-test (L-04: the checkout is never mutated).
</verification>

<success_criteria>
1. The replay and the gate are inside the transaction in the locked order and the summary line is byte-identical to before.
2. Arm 23 goes red when the gate is neutered on a copy and the real script passes 24/24.
3. Every vitest pin moved by re-measurement, none by deletion.
4. What would make this RED: a `to_regclass` guard inside the gate loop; a `refdata=` field on the summary line; a byte backup or `git checkout --` anywhere in an arm; a lowered or unchanged `EXPECTED_ARMS`; an arm that asserts exit code only.
</success_criteria>

<output>
Create `.planning/phases/164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data/164.8.1-02-SUMMARY.md` when done, recording each arm's observed-RED scratch path and the softening-set reading.
</output>


⛔ **W2 — RE-MEASURE the closing narrative's refusal count, do not just append a clause.** `scripts/restore-test-from-baseline.sh:2068` currently reads `seven refusals fire before any write`. Arm 24 adds two more refusal legs that fire before any write, so SEVEN becomes stale in the same commit. Count the refusals in the file, update the number, and if a vitest pin quotes that sentence move the pin BY MEASUREMENT. A narrative that miscounts its own guards is the same defect class as a stale floor.
