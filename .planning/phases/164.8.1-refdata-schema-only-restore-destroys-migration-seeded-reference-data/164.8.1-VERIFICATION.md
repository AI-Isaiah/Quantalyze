---
phase: 164.8.1-refdata-schema-only-restore-destroys-migration-seeded-reference-data
verified: 2026-09-09T13:45:00Z
status: passed
score: 13/13 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 11/13
  previous_verified: 2026-09-09T13:20:00Z
  previous_head: 24b0c9276cbf8397bf3b1d8d8727c75ad7788475
  head_at_this_run: bedd9d1e947f34bd411eb1982008a54dfdc46e93
  gaps_closed:
    - "GAP 1 (BLOCKER — the dead ratchet). CLOSED, and closed by NEUTERING rather than by reading the diff. All ELEVEN `EXPECTED_ARMS` literals in src/__tests__/restore-test-from-baseline.test.ts read 26; the script's constant is 26; `npx vitest run` over the four phase files → 71 passed | 0 failed. The calibration legs are LIVE again: the replace target `\\nEXPECTED_ARMS=26\\n` occurs exactly ONCE in the script, so all three `expect(mutated).not.toBe(SRC)` guards apply. Simulated the drift that broke it (script moved to 27, test still pins 26): 7 of the 8 legs flip RED, including all three calibration guards. The anti-vacuity leg can fail again."
    - "GAP 1's stale-count tail. CLOSED: `Arms 22-26` (:1660), `prints 26/26` (:1666), `NINE refusals` (:2950, and 9 `refuse_*` function definitions measured), `19 kinds` at scripts/extract-reference-inserts.mjs:797 and src/__tests__/extract-reference-inserts-selftest-floor.test.ts:46. Zero residual `16 kinds` / `24/24` / `25/25` strings under scripts/ and src/."
    - "GAP 2 (WR-02's inverted derivation). CLOSED and the direction re-derived from the allowlist, not from the comment: `awk -F'\\t' '$2==\"public.compute_job_kinds\"{s+=$3}'` → **15** statements; the hosted post-census recorded `refdata:public.compute_job_kinds=16` rows. The comment at :1204 now reads `pinned at 15 statements and yields 16 distinct kind names`. It also names the allowlist and `--audit` as the authority and records, in the file, that the paragraph has been wrong twice."
    - "GAP 3 (backticks in unquoted heredocs). CLOSED. Verified INDEPENDENTLY, with my own scanner over ALL heredocs (not just `TXN_*`, and handling `<<-`): zero unescaped backticks inside any unquoted heredoc in the script. Re-ran the N7 probe at HEAD — all FOUR unquoted heredocs (TXN_WHITELIST, TXN_CLOSURE, TXN_REFDATA_GATE, TXN_ASSERT) assemble with ZERO `command not found` / `syntax error` on stderr, against six such errors last run. Refusal 9 `refuse_backticks_in_txn_heredocs` exists (:615), is wired pre-write (:1451, before RESTORE_OUT_DIR is even created), and is armed by arm 26's three legs including leg (c) which scans THIS script."
    - "OFF-GOAL note (no CHANGELOG entry / no VERSION bump). CLOSED. VERSION and package.json are both the byte-equal 4-digit `0.77.30.0`. The `[0.77.30.0]` entry REPLACES `[0.77.29.0]` and explicitly records that the old entry's `EXPECTED_ARMS 21 -> 24 with all eleven vitest literals re-measured` was FALSE. Commit cross-check run mechanically: 9 of the 11 commits in `origin/main..HEAD` are cited BY SHA in the entry; the 2 uncited are the two release commits themselves (`bedd9d1e`, which carries the entry, and `27d519e2`, whose entry this one supersedes). Every substantive commit maps to at least one bullet."
  regressions: []
gaps: []
deferred: []
human_verification:
  - test: "Decide whether to sweep, or to annotate as dated snapshots, three phase-record artifacts that still carry the SUPERSEDED arm count. `164.8.1-02-SUMMARY.md:18` — 'Self-test arms 22, 23 …, 24 … and 25 …, EXPECTED_ARMS 21 -> 25'. `.planning/ROADMAP.md` Phase 164.8.1 plan-02 bullet — 'arms 22-24 …, EXPECTED_ARMS 21 → 25'. `164.8.1-REVIEW.md` Resolution gate list — 'bash scripts/restore-test-from-baseline.sh --self-test → 25/25, exit 0'."
    expected: "Either each reads 26 (and the REVIEW gate line reads 26/26), or each carries an explicit 'dated snapshot, superseded by arm 26' marker. The ROADMAP bullet is the one I would sweep unconditionally — it is a LIVE index of what the phase delivers, not a dated record."
    why_human: "This is the project-convention call the previous cycle already faced once (WARNING 2, closed by amending 02-SUMMARY to 25). Arm 26 landed after Plan 02, so 'stale' and 'correct as of its date' are both defensible readings of a plan SUMMARY and of a review Resolution. Only the founder's convention settles it. ⚠️ It is the SAME class the phase names as its own lesson — 164.8.1-02-SUMMARY.md:33: 'A count in prose beside a guard is a count that will go stale.'"
  - test: "Decide whether refusal 9's detector regex should be widened. MEASURED on scratch fixtures (never the checkout): `refuse_backticks_in_txn_heredocs` matches only `/<<TXN_[A-Z_]+$/`, so FOUR spellings carry a live backtick straight past it — `<<-TXN_A` (tab-stripping form), `<<TXN_A ` (one trailing space), `<<TXN_Gate2` (any lowercase or digit in the delimiter), and any unquoted heredoc whose delimiter is not `TXN_`-prefixed. Each was fed a fixture whose heredoc body contains an unescaped backtick; the guard printed ACCEPTED for all four and refused only the exact `<<TXN_A` spelling."
    expected: "Either the regex is widened (e.g. `<<-?[[:space:]]*[A-Za-z_][A-Za-z0-9_]*[[:space:]]*$`, refusing every UNQUOTED heredoc rather than the `TXN_[A-Z_]+` spelling alone) with an arm-26 leg per new spelling, or the narrowness is written down beside the guard so the next author knows `<<-TXN_X` is unscanned."
    why_human: "NOT a live defect — every unquoted heredoc in the script today is `<<TXN_[A-Z_]+` with no trailing space, and my independent scan finds zero unescaped backticks in any of them. This is a judgment call about how wide a durability control should be, and about arm cost: four more spellings is four more legs on an arm that already runs on every self-test."
  - test: "Re-state, or delete, the `164.8.1-REVIEW.md` Resolution gate line 'vitest over the five affected files → 788 passed'. It was the previous run's gap-2 `missing` item and it was NOT actioned — `git log -- 164.8.1-REVIEW.md` shows the file untouched since `24b0c927`. Its numbers are not reproducible: the four phase test files at HEAD are 71 tests, and the whole suite enumerates at 14507."
    expected: "Either the line carries a reproducible reading, or it is replaced by a pointer to the CHANGELOG's stronger claim ('the FULL suite, 853 files, 14507 tests'), whose test count I corroborated by enumeration (`npx vitest list` → 14507)."
    why_human: "Whether a dated review Resolution is amended after the fact or left as a historical record is the same convention call as item 1. The SUBSTANCE is closed — the evidence was restated, in the CHANGELOG, at greater strength — so this is about the phase record, not about the code."
  - test: "Decide whether the SC-9 leg should also pin the COUNT, not just the DATE. `src/__tests__/restore-test-from-baseline.test.ts:186` asserts only that `MEASURED 2026-09-09` sits beside the constant. Simulated: rewriting `# MEASURED 2026-09-09 — --self-test prints 26/26` back to `prints 24/24` leaves EVERY leg of the ratchet GREEN."
    expected: "Either a leg that parses the N/M out of the MEASURED line and asserts N === M === EXPECTED_ARMS, or an explicit note that the count in that comment is decorative."
    why_human: "Advisory. This was a 'Consider' item in the previous report, not a gap, and it is a ratchet-scope judgment call of exactly the kind the founder settled once already for the SHORT branch (WR-01)."
---

# Phase 164.8.1: REFDATA Verification Report

**Phase Goal:** Make the TEST restore reproduce the reference rows that migrations INSERT, and fail loud when it does not.
**Verified:** 2026-09-09T13:45:00Z at HEAD `bedd9d1e`
**Status:** passed — ⛔ flipped from `human_needed` by the ORCHESTRATOR after this report was written; see "Orchestrator resolution" at the end of this file. The verifier's own judgement was `human_needed` and stands as recorded.
**Re-verification:** Yes — third pass. This file has been **overwritten**. Nothing from the 13:20 report (HEAD `24b0c927`) is carried forward, including the verdicts that passed: every truth below was re-measured at `bedd9d1e`.

## How this run differs

Two commits landed since the last report (`3811b221`, `bedd9d1e`) and they touched the script, the vitest ratchet, the extractor and the CHANGELOG. **All three gaps I filed are closed, and the off-goal CHANGELOG/VERSION note with them.** I did not accept any of the five closure claims on its diff:

- the ratchet was checked by **simulating the drift that broke it** and observing 7 of 8 legs flip RED;
- the WR-02 direction was **re-derived from the allowlist and the hosted census**, not read from the corrected comment;
- the heredoc seam was checked with **my own scanner, written independently of the shipped guard**, plus a re-run of the N7 execution probe over all four unquoted heredocs;
- the guard was **attacked on scratch fixtures** to find what it does not see;
- the CHANGELOG cross-check was run **mechanically over `git log origin/main..HEAD`**, SHA by SHA.

What remains is not code. Four items, all prose-currency or control-scope, all needing a convention call.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence (all re-measured at `bedd9d1e`) |
|---|-------|--------|------------------------------------------|
| 1 | The restore reproduces the reference rows migrations INSERT | ✓ VERIFIED | Replay inside the restore transaction under `SET LOCAL search_path = public, pg_catalog`; arm 22 GREEN on the throwaway lane (26/26, exit 0). Hosted restore **34330741339** re-corroborated this run via `gh run view`: `conclusion=success`, `headSha=a622df27`. Post-census `refdata:public.compute_job_kinds=16 (was 0)`, `refdata:public.discovery_categories=5 (was 0)` |
| 2 | It fails loud when it does not — EMPTY, SHORT and row-level partial, each **by name** | ✓ VERIFIED | Behavior-dependent (abort + rollback), so presence is not enough: arms 23(a)-(d) RUN the real transaction on the lane, assert the named RAISE text AND that `e2e_leftover` survived. All GREEN in the 26/26 run at HEAD. (Legs (c) and (d) were driven RED by neutering in the previous pass; the shipped legs are unchanged since) |
| 3 | Allowlist keyed (migration file, table, pinned count), never per table; every line carries its consumer | ✓ VERIFIED | `--audit` over the real corpus: **22 entr(ies), 19 file(s), 8 table(s), 23 statement(s)**, exit 0 — derived, not restated |
| 4 | The extractor refuses by default; emitted SQL is the original bytes; every refusal kind has a red AND a green fixture | ✓ VERIFIED | `--self-test` → `19 kinds, red+green each.` exit 0 |
| 5 | `--audit` reddens on an unlisted top-level reference INSERT; wired self-test-first in both workflows | ✓ VERIFIED | `ci.yml:1097,1126,1153` and `test-restore-from-baseline.yml:486,509,537` — self-test step before audit step in both, each with a `MEASURE_FAIL` banner assertion and a `SELF_TEST_KINDS_FLOOR` read by symbol. `test-restore-workflow-wiring.test.ts` passes |
| 6 | `system_settings.analytics_service_url` replayed faithfully (L-03); hazard booked, not resolved by an allowlist edit | ✓ VERIFIED | Booked at `TODOS.md:1464`, routed to ROADMAP Phase 164.9 |
| 7 | The contract test proves classification and count agreement in both directions | ✓ VERIFIED | `refdata-allowlist.test.ts` + `extract-reference-inserts-selftest-floor.test.ts` both pass |
| 8 | **`EXPECTED_ARMS` moves in the same commit as the arms, and every pin the change reddens is re-measured, not deleted** | ✓ **VERIFIED — was FAILED** | Script `EXPECTED_ARMS=26` (:1667); **eleven** literals in the vitest file, all 26; zero `=24`/`=25` residue in either file. `npx vitest run` over the four phase files → **71 passed, 0 failed**. `3811b221` touched the script AND the test file in the SAME commit. Anti-vacuity re-armed — see spot-check **N8** |
| 9 | The seeder's false "the migration didn't run" inference is gone | ✓ VERIFIED | `seed-demo-data.ts:914-928` read in full: names the `refdata:` census, the schema-only dump, the ledger reseed, the in-transaction gate, and the no-hand-seed rule |
| 10 | Every deferral is routed — TODOS *and* a named phase | ✓ VERIFIED | `TODOS.md:1464`, `:1499`, `:7076` (`[164.8-PUSH-RACE-VAC08]`, new on this branch), all routed to Phase 164.9 |
| 11 | Preflight first, byte-identical rollback, then the restore, SHA-bound green CI | ✓ VERIFIED | Preflight **34329459044** and restore **34330741339** both `success`, both `headSha=a622df27`, re-read from GitHub this run. `164.8.1-04-RESTORE.log:144,152` records `python` and `e2e-seeded` failure → success |
| 12 | L-01 honoured — replay lives in the script, the confirm token is untouched | ✓ VERIFIED | `git log a622df27~1..HEAD -- supabase/schema/baseline.sql BASELINE.md` → **empty** |
| 13 | Every code-review finding's claimed fix holds at HEAD | ✓ **VERIFIED — was PARTIAL** | All 11 code fixes hold; WR-02's numbers are now correct in the direction the allowlist and the hosted census both give. ⚠️ The Resolution's own *gate list* is stale in two lines (`25/25`, `788 passed`) and its WR-02 row says "No per-table derivation restated" while the shipped comment restates one — correctly, but it restates one. Prose, not code → human item 1/3, not a gap |
| 14 | The transaction the restore assembles is built without the shell evaluating its own SQL comments | ✓ **VERIFIED — was PARTIAL** | Independently scanned and independently EXECUTED — see spot-checks **N9** and **N10** |

**Score:** 14/14 truths verified (0 present-behavior-unverified). Counted as **13/13 must-haves** against the previous run's denominator; truth 14 was the previous run's gap 3 and is folded in.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/restore-test-refdata-allowlist.txt` | 22/19/8/23, consumer per line | ✓ VERIFIED | Derived by `--audit`, unchanged from the previous pass |
| `scripts/extract-reference-inserts.mjs` | emit / `--audit` / `--self-test`, `SELF_TEST_KINDS_FLOOR` | ✓ VERIFIED | Self-test 19 kinds; docblock at :797 corrected to `19 kinds` |
| `scripts/extract-reference-inserts-fixtures/` | one red + one green per refusal kind | ✓ VERIFIED | 19 kinds, red+green each |
| `scripts/restore-test-from-baseline.sh` | replay, gate, census, arms 22-26, `EXPECTED_ARMS=26`, refusal 9 | ✓ VERIFIED | 26/26 exit 0. `Arms 22-26` (:1660), `prints 26/26` (:1666), `NINE refusals` (:2950) — all three corrected, and **9** `refuse_*` definitions measured. WR-02 comment (:1199-1221) now correct AND self-documenting about having been wrong twice |
| `scripts/restore-test-from-baseline-fixtures/20260103000000_fixture_c.sql` | 2 statements / 3 rows | ✓ VERIFIED | Unchanged; arm 23 legs (c)/(d) still on different gates |
| `src/__tests__/restore-test-from-baseline.test.ts` | ratchet pinned at the shipped constant, calibration live | ✓ **VERIFIED — was STUB-BY-STALENESS** | 11 literals at 26; 19 `not.toBe(SRC)` calibration guards in the file; the arm-ratchet replace target occurs exactly once in the script so all three of its guards apply |
| `src/__tests__/extract-reference-inserts-selftest-floor.test.ts` | floor ratchet both directions | ✓ VERIFIED | Passing; docblock at :46 corrected to `19 kinds` |
| `src/__tests__/refdata-allowlist.test.ts` / `test-restore-workflow-wiring.test.ts` | contract + wiring | ✓ VERIFIED | Passing |
| `scripts/seed-demo-data.ts` | corrected inference | ✓ VERIFIED | Read in full |
| `164.8.1-04-RESTORE.log` | run ids, censuses, SHA-bound readings | ✓ VERIFIED | Run ids re-corroborated against GitHub this run |
| `CHANGELOG.md` / `VERSION` / `package.json` | one unified entry, 4-digit byte-equal bump | ✓ VERIFIED | `0.77.30.0` in both files, byte-equal. Entry cross-checked SHA by SHA |
| `164.8.1-02-SUMMARY.md` | matches the shipped gate | ⚠️ **STALE AGAIN** | Says `EXPECTED_ARMS 21 -> 25`; ships 26. Human item 1 |
| `164.8.1-REVIEW.md` Resolution | reproducible gate list | ⚠️ **STALE** | `25/25` and `788 passed`; untouched since `24b0c927`. Human items 1 and 3 |
| `.planning/ROADMAP.md` Phase 164.8.1 plan-02 bullet | current arm count | ⚠️ **STALE** | `arms 22-24 … EXPECTED_ARMS 21 → 25`. Human item 1 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `lint-sql-gates.mjs` | `extract-reference-inserts.mjs` | `import { maskSql, statements }` | ✓ WIRED | `--self-test` → `7 rules, red+green each.` — no behaviour change |
| `extract-reference-inserts.mjs` | `restore-test-from-baseline.sh` | `-- refdata-expect: <t>=<n>` trailer | ✓ WIRED | 8 trailers; `public.compute_job_kinds=15` reaches the gate's third VALUES column as a bare numeral |
| `--audit` | `sql-gate-lint` job | workflow step | ✓ WIRED | `ci.yml:1097-1190`, self-test before audit |
| `--audit` | restore workflow | workflow step | ✓ WIRED | `test-restore-from-baseline.yml:486-578` |
| Gate | census | `refdata:<schema.table>=<n\|absent>` | ✓ WIRED | Hosted post-census carries the real values |
| `refuse_backticks_in_txn_heredocs` | the `--run` path | call at `:1451` | ✓ WIRED | Sits after `refuse_bad_refdata_allowlist` and BEFORE `RESTORE_OUT_DIR` is created — filesystem-only and pre-write, like refusals 1-8 |
| `refuse_backticks_in_txn_heredocs` | arm 26 | `run_arm` at `:2915` | ✓ WIRED | Three legs; leg (c) points the guard at `$0` — the file the restore actually assembles from |
| Script `EXPECTED_ARMS` | vitest ratchet | source-text literal | ✓ **WIRED — was BROKEN** | 26 ↔ 26, and the link is falsifiable (N8) |

### Data-Flow Trace (Level 4)

| Artifact | Data variable | Source | Produces real data | Status |
|----------|---------------|--------|--------------------|--------|
| `restore-test-from-baseline.sh` | `REFDATA_TABLES` / `REFDATA_EXPECT` | extractor trailers over the live allowlist | Yes — 8 tables, 23 statements | ✓ FLOWING |
| Gate VALUES list | `t.n` | `SELECT count(*) FROM <table>` inside the transaction | Yes — hosted run produced 16 and 5 | ✓ FLOWING |
| Gate VALUES list | `t.expected` | `${REFDATA_EXPECT[$i]}`, charset-refused before interpolation | Yes — `compute_job_kinds=15`, and 16 ≥ 15 is exactly the floor the comment now describes | ✓ FLOWING |
| Census | `refdata:` rows | `query_to_xml` count per table | Yes — real hosted pre (0/0) and post (16/5) readings | ✓ FLOWING |
| Preflight equality test | `census_rollback_view` output | census with `[0-9]+` refdata values → `present` | Structural half flows; COUNT half deliberately dropped | ⚠️ NARROWED BY DESIGN — armed by arm 25; residual unchanged from the previous report and still recorded, not re-litigated |

### Behavioral Spot-Checks

All local. **No Supabase database was contacted** — the only database used was the throwaway PostgreSQL lane the self-test spins up. Every mutation was made on scratch copies under the scratchpad tmpdir; `git status --porcelain` was **empty before and after** (`STATUS_LINES=0`, HEAD still `bedd9d1e`), and `git checkout --` was never used.

| # | Behavior | Command | Result | Status |
|---|----------|---------|--------|--------|
| P1 | Restore self-test at HEAD | `bash scripts/restore-test-from-baseline.sh --self-test` | `self-test OK (26/26 arms — NINE refusals …)`, exit 0. Arm 26 present: `ok 26 RED a backtick inside an unquoted <<TXN_* heredoc is refused, and THIS script is clean` | ✓ PASS |
| P2 | Extractor self-test | `node scripts/extract-reference-inserts.mjs --self-test` | `19 kinds, red+green each.` exit 0 | ✓ PASS |
| P3 | Corpus audit | `--audit --migrations supabase/migrations --allowlist …` | `22 entr(ies), 19 file(s), 8 table(s), 23 statement(s)` exit 0 | ✓ PASS |
| P4 | Lexer unchanged | `node scripts/lint-sql-gates.mjs --self-test` | `7 rules, red+green each.` | ✓ PASS |
| P5 | **Phase vitest — the previous BLOCKER** | `npx vitest run` over the 4 phase files | **4 files, 71 passed, 0 failed** (was 2 failed \| 69 passed) | ✓ **PASS** |
| P6 | Wider vitest over the other branch-touched files | `npx vitest run supabase-migrate-test-first + drift-check-scripts + critical-regressions + mutation-runner-floors` | **4 files, 767 passed, 0 failed** | ✓ PASS |
| P7 | Suite size behind the CHANGELOG's "14507 tests" | `npx vitest list \| grep -c " > "` | **14507** — matches the entry exactly | ✓ PASS (enumerated, not executed) |
| P8 | Hosted run corroboration | `gh run view 34330741339 / 34329459044 --json` | both `success`, `headSha=a622df27` | ✓ PASS |
| **N8** | **The arm ratchet's calibration is REAL again** | Re-implemented all 8 legs of the arm-ratchet test as pure functions of the script text, ran them against HEAD and against a NEUTERED copy (`EXPECTED_ARMS=26` → `27`, i.e. the exact drift that broke it last time) | HEAD: 8/8 PASS. Neutered: **7 of 8 FAIL**, including all three `expect(mutated).not.toBe(SRC)` guards (`CALIB-commented-differs`, `CALIB-doubled-differs`, `CALIB-interim-differs`) and the `exactly-once` leg. Measured directly: `\nEXPECTED_ARMS=26\n` occurs **exactly once** in the script, so every calibration replace applies | ✓ PASS |
| **N9** | **No unescaped backtick survives in ANY unquoted heredoc** — independent scanner | Own node scanner over the whole script: finds every heredoc open (`<<` and `<<-`, quoted and unquoted, ALL delimiters not just `TXN_*`), strips `\\\`` escapes, reports live backticks | 13 heredocs found — 4 unquoted (`TXN_WHITELIST`, `TXN_CLOSURE`, `TXN_REFDATA_GATE`, `TXN_ASSERT`), 9 quoted. **NONE**. No unterminated heredoc at EOF | ✓ PASS |
| **N10** | **The heredocs no longer execute their own prose** — the N7 probe, re-run | Each of the four unquoted heredocs extracted verbatim from HEAD, given stub variables, executed in isolation; stderr counted for `command not found` / `syntax error` | `1236-1302: 0` · `968-991: 0` · `1007-1100: 0` · `1327-1398: 0`. Previous run: six such errors from `TXN_REFDATA_GATE` alone | ✓ PASS |
| **N11** | **Refusal 9 fires on the shape that occurred** | Guard body extracted to a scratch harness; fixture `<<TXN_A` heredoc containing `` `RAISE` `` | `FAIL: restore aborted: a backtick appears inside an unquoted <<TXN_* heredoc …` — and it prints the offending line | ✓ PASS |
| **N12** | **Refusal 9's blind spots** (adversarial — "can the guard be trivially defeated?") | Same harness, four alternative spellings of the same violation | `<<-TXN_A` → **ACCEPTED** · `<<TXN_A ` (trailing space) → **ACCEPTED** · `<<TXN_Gate2` → **ACCEPTED** · `<<SQLX` → **ACCEPTED**. Only the exact `/<<TXN_[A-Z_]+$/` spelling is refused | ⚠️ **RESIDUAL** — no live violation, but the control is narrower than its name. Human item 2 |
| **N13** | SC-9's date-only pin | Simulated `prints 26/26` → `prints 24/24` beside the constant | Every ratchet leg stays GREEN. The previous run's "Consider strengthening" was not actioned | ⚠️ ADVISORY — human item 4 |
| **N14** | CHANGELOG commit cross-check (this repo's stated rule) | `for c in $(git log origin/main..HEAD --format=%h); do grep -q $c <0.77.30.0 section>; done` | **9 CITED / 2 MISSING**. The two missing are `bedd9d1e` (the commit that carries the entry) and `27d519e2` (the superseded 0.77.29.0 release commit, named in the entry's own preamble). Every substantive commit maps to at least one bullet | ✓ PASS |

### Probe Execution

This phase declares no `scripts/*/tests/probe-*.sh`. Its probes are the two self-tests, the audit and the lexer self-test — P1-P4, all executed in this process, exit codes read directly. No SUMMARY-reported PASS count was substituted for a run.

### Review Resolution audit — re-checked at HEAD, not carried forward

| # | Claim | Verdict at `bedd9d1e` | How I checked |
|---|-------|-----------------------|---------------|
| CR-01 | `census_rollback_view` normalises counts, keeps `absent`; arm 25 falsifies both directions | ✓ HOLDS (residual unchanged, recorded in the 13:20 report and not re-litigated) | Arm 25 GREEN in P1 |
| WR-01 | Arm 23 leg (d) is the SHORT branch's first falsifier | ✓ HOLDS | Arm 23 GREEN in P1; legs unchanged since the pass that drove them RED |
| **WR-02** | The floor's soundness argument | ✓ **NOW HOLDS** | Re-derived independently: allowlist sums to **15** statements for `public.compute_job_kinds`; hosted post-census recorded **16** rows. Comment now states 15 → 16, the right way round. ⚠️ The REVIEW's own row still claims "No per-table derivation restated" — the comment does restate one |
| WR-03 | `sql_lit` at the three RAISE sites | ✓ HOLDS | Unchanged |
| WR-04 | Header re-states the falsified line as the SCAN | ✓ HOLDS | `c1-dollar-body` still red+green in P2 |
| WR-05 | `ON CONFLICT` action refused unless `DO NOTHING` | ✓ HOLDS | Kind present in the 19; fixtures intact |
| WR-06 | Quoted head accepted, CTE-prefixed refused by name, floor 16 → 19 | ✓ HOLDS | `SELF_TEST_KINDS_FLOOR = 19` read by symbol; 19 kinds in P2 |
| WR-07 | Seeder contradiction removed | ✓ HOLDS | `seed-demo-data.ts:914-928` |
| WR-08 | Four mutex takers | ✓ HOLDS | Unchanged |
| WR-09 | Collect first, raise once | ✓ HOLDS | Arm 23 (a)/(d) GREEN |
| WR-10 | `process.exitCode` | ✓ HOLDS | Unchanged |
| — | Resolution gate line "self-test → 25/25" | ✗ **STALE** | HEAD prints 26/26 |
| — | Resolution gate line "five affected files → 788 passed" | ✗ **STILL NOT RE-STATED** | REVIEW.md untouched since `24b0c927`. The substance moved to the CHANGELOG instead (full suite, 14507 tests — corroborated at P7) |

### Requirements Coverage

| Requirement | Source plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| `164.2-TEST-APPLY-PROVENANCE` (reference-data half) | 01-04 | Migration-apply provenance on shared TEST | ✓ SATISFIED (reference-data half, by design) | Lives in `TODOS.md:1378`, not in `.planning/REQUIREMENTS.md` — consistent with the ROADMAP's own "the reference-data half of". Discharged by replay + gate + census, all executed here |
| `T-164.8-09` (reopened) | 01-04 | Irreversible data loss on TEST, accepted on a compensating control measured false | ✓ SATISFIED | A threat-table ID (`164.8-04-PLAN.md:208`), not a REQUIREMENTS.md ID. Re-dispositioned to **mitigate** at `164.8.1-02-PLAN.md:184`; the compensating control is now the restore's own replay + gate, armed by arm 23's four legs |
| TODOS `[164.8-TEST-DATA-RESEEDED]` | — (ROADMAP Requirements line only) | Owned by Phase 164.8 Plan 06 | ℹ️ NOT PRESENT (expected) | 0 hits in `TODOS.md`, exactly as the ROADMAP line and `164.8.1-CONTEXT.md` both predict. **Not a gap of this phase** |

`.planning/REQUIREMENTS.md` carries v1.20 `RANK-*`-style IDs only; none map to Phase 164.8.1. **No ORPHANED requirements.**

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD` / `FIXME` / `XXX` across all 26 non-planning files in `origin/main..HEAD` | — | **Zero hits.** No unreferenced debt markers |
| `164.8.1-02-SUMMARY.md` | 18 | Superseded arm count in prose (`21 -> 25`) | ⚠️ WARNING | Human item 1 |
| `.planning/ROADMAP.md` | Phase 164.8.1 plan-02 bullet | Superseded arm count (`arms 22-24`, `21 → 25`) | ⚠️ WARNING | Human item 1 — this one is a live index, not a dated record |
| `164.8.1-REVIEW.md` | Resolution gate list | `25/25`; `788 passed` unreproducible; WR-02 row's "no derivation restated" | ⚠️ WARNING | Human items 1 and 3 |
| `scripts/restore-test-from-baseline.sh` | 615-618 | Guard regex narrower than the class it names (4 bypass spellings, measured) | ⚠️ WARNING | Human item 2. **No live violation** |
| `src/__tests__/restore-test-from-baseline.test.ts` | 186 | SC-9 pins the DATE beside the constant but not the COUNT | ℹ️ INFO | Human item 4 |
| `scripts/restore-test-from-baseline.sh` | 1215-1216 | The corrected WR-02 paragraph says "Do NOT restate a per-table derivation here" three lines after restating one, and carries a stray mid-sentence line break | ℹ️ INFO | Cosmetic. The numbers are right and the authority is named |

### Locked Decisions (CONTEXT.md L-01..L-04)

| Decision | Status | Evidence |
|----------|--------|----------|
| **L-01** — replay in the script, not the dump; confirm token unchanged | ✓ HONOURED | `git log a622df27~1..HEAD -- supabase/schema/baseline.sql BASELINE.md` empty |
| **L-02** — allowlist keyed (file, table, count) | ✓ HONOURED | 14 `compute_job_kinds` lines across 14 distinct files, each with its own pinned count and consumer |
| **L-03** — `analytics_service_url` replayed faithfully, hazard booked elsewhere | ✓ HONOURED | `TODOS.md:1464`, routed to Phase 164.9 |
| **L-04** — scratch copy for machine-run arms, never the checkout, never a byte backup | ✓ HONOURED | Followed for N8-N13; `git status --porcelain` empty before and after, HEAD unchanged |

### Gaps Summary

**No gaps. The phase goal is achieved and all three of the previous run's gaps are closed by measurement.**

The replay sits inside the restore transaction under the correct `search_path` bracket; the gate aborts and rolls back on EMPTY, on SHORT and on a registry that lost individual rows, each by name and each armed on a throwaway lane; the hosted restore put all eight allowlisted tables back and took `python` and `e2e-seeded` green on the same commit. The allowlist is (file, table, count)-keyed with a consumer per line and `--audit` re-derives it every run.

The three defects I filed last run are gone, and none of them was accepted on its diff:

1. **The dead ratchet is alive.** Eleven literals at 26, 71 tests green, and — the part that actually mattered — the calibration mutations apply again, proven by simulating the exact drift that killed them: 7 of 8 legs flip RED on a script moved to 27. The anti-vacuity leg can fail, which is the only property that makes the pin worth having.
2. **WR-02's derivation is right**, and right in the direction the allowlist (15 statements) and the hosted census (16 rows) independently give. The comment now names `--audit` as the authority and records in the file that it has been wrong twice.
3. **The heredoc seam is closed at the source, not just guarded.** My own scanner — written without reference to the shipped `awk` — finds zero unescaped backticks in any of the four unquoted heredocs, and all four now assemble with zero shell errors where `TXN_REFDATA_GATE` alone previously produced six. Refusal 9 is wired pre-write and armed by an arm whose deciding leg scans the real script.

The off-goal CHANGELOG note is also closed, and closed properly: `0.77.30.0` in both `VERSION` and `package.json` byte-equal, one unified entry replacing `0.77.29.0`, an explicit retraction of the old entry's false "all eleven literals re-measured", and a mechanical SHA-by-SHA cross-check that every substantive commit on the branch reaches a bullet.

**What is left is prose currency and one control-scope call, and I am not softening either.** Three phase-record artifacts still carry `25` where the tree says `26` — including the ROADMAP's own plan bullet, which is a live index rather than a dated snapshot — and the REVIEW Resolution's `788 passed` gate line, which I asked for last run, was not re-stated (the evidence moved to the CHANGELOG instead, at greater strength). Separately, refusal 9 refuses exactly `/<<TXN_[A-Z_]+$/` and I got a live backtick past it four different ways on scratch fixtures; nothing in the script triggers any of them today, but a control named for a class should either cover the class or say where it stops.

That is why this closes **human_needed** rather than **passed**: the code is done, and four convention calls are not mine to make.

---

_Verified: 2026-09-09T13:45:00Z at HEAD `bedd9d1e`_
_Verifier: Claude (gsd-verifier) — third pass, overwriting the 13:20 report. Nothing carried forward._

---

## Orchestrator resolution — 2026-09-09, AFTER this report was written

⛔ **THIS SECTION WAS NOT WRITTEN BY THE VERIFIER, and the frontmatter `status:` was
changed from `human_needed` to `passed` by the orchestrator, not by it.** The
verifier's judgement stands exactly as recorded above. What changed is that the
four convention calls it explicitly declined to make ("four items I will not
decide for you") have since been MADE and implemented, in commits that landed
after the `bedd9d1e` it verified. The status is flipped so the ship gate reads the
current tree rather than a superseded one — it is NOT a claim that the verifier
passed the phase.

Decided under the founder's standing instruction to take decisions autonomously
where no client is affected. Each is a commit, not an assertion:

| Verifier item | Call taken | Commit |
|---|---|---|
| 1. Sweep vs. annotate three stale arm counts | **Swept.** `164.8.1-02-SUMMARY.md`, the ROADMAP plan-02 bullet (a live index — the verifier said it would sweep that one unconditionally) and the REVIEW gate line now read 26. | `6b0cdbad` |
| 2. Widen refusal 9's detector | **Widened.** It now matches every unquoted heredoc opener and skips quoted ones. Arm 26 gained legs (d) and (e): one fixture per evasion the verifier demonstrated, plus a quoted delimiter that must be ACCEPTED. Narrowing it back to the old regex drives leg (d) RED. | `6b0cdbad` |
| 3. Re-state or delete the REVIEW's "788 passed" | **Corrected in place, not deleted.** The line now records that the file set was hand-picked, that it omitted the file pinning `EXPECTED_ARMS`, and that two of that file's tests were red while the line was being written. | `6b0cdbad` |
| 4. Should SC-9 pin the COUNT, not just the DATE | **Pinned.** A new leg derives the expected count FROM `EXPECTED_ARMS` and asserts every `prints N/N` sentence agrees, with a calibration proving the check is not vacuous. The verifier's own N13 simulation now fails: "a comment claims prints 24/24 while EXPECTED_ARMS is 26". | `6b0cdbad` |

Also closed, taken from this report's anti-pattern table rather than its human
list: the corrected WR-02 paragraph in `restore-test-from-baseline.sh` said "Do
NOT restate a per-table derivation here" three lines after restating one. The
instruction now matches what the paragraph does — one worked example, re-derivable
in two commands, and no second table's derivation.

Re-run by the orchestrator after those commits, not taken from any agent's claim:
`--self-test` **26/26 exit 0** · extractor **19 kinds, red+green each** · `--audit`
unchanged at **22/19/8/23** · full vitest suite **853 files / 14507 tests**.

_Resolution recorded by the orchestrator, 2026-09-09._
