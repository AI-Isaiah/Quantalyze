# Phase 153.6: PARITY — the fixes that only landed on one path - Context

**Gathered:** 2026-08-11
**Status:** Ready for planning

<domain>
## Phase Boundary

Every fix the 153 span made on one path exists on its twin, no broad `except` re-absorbs a refusal
the fence was built to surface, and the venue label stops being a client-writable authority for
skipping a security probe.

⛔ **THE SHAPE OF THIS PHASE IS "the fix landed once, not twice."** Three of the four root causes are
the same failure: a correct fix applied to one path while its duplicate went untouched, with no
guard asserting the two agree. That is the instance-not-class mistake — found *inside the span whose
own charter said "fix it at the SINK, not three times."* No cluster closes as N point patches. Each
must answer: **what makes the two paths unable to diverge again?**

Source: `/code-review xhigh` over the whole 153→153.5 span (2026-08-11, 40 agents, 29 verified
findings → 13 distinct defects). Nine come here; two were fixed unplanned in the 153.5 PR (#673);
two are deliberately out.

</domain>

<decisions>
## Implementation Decisions

### D — the venue lock is bypassable (SECURITY, live on PROD)

- **D-01: The server-held venue becomes the probe gate's authority. The client-writable `exchange`
  column stops being load-bearing for the probe decision.** `/api/keys/validate-and-encrypt` already
  knows the venue at mint time — it just validated against it. Bind the venue to the minted
  ciphertext server-side and have `finalize-wizard`'s scope-broadening probe gate read *that*.
  DELETE-then-re-INSERT with a forged `exchange` then becomes irrelevant rather than merely harder.
- **D-02: Do NOT add a new REVOKE on `api_keys`.** Migration `20260810120000` line 181 warns in its
  own text that an over-broad REVOKE "would silently break the connect-a-key" flow. Revoking DELETE
  or INSERT is explicitly rejected for this phase. No new grant/revoke on `api_keys`.
- **D-03: The column may remain user-writable.** Once the probe no longer trusts it, a mislabelled
  row is a cosmetic lie, not a control bypass. Do not spend this phase making the column immutable.
- **D-04: The migration header of `20260810120000` currently asserts the opposite of the truth**
  ("the mislabelled row can never be corrected back"), and that false premise is what its
  "self-defeating forgery" residual argument rests on. The header must be corrected as part of this
  work — a security migration whose stated threat model is wrong is worse than one that says nothing.
- **D-05: `supabase/tests/test_api_keys_exchange_not_user_writable.sql` proves DELETE survives but
  never tests the round trip.** It must gain the DELETE + re-INSERT-under-a-different-exchange case,
  and that case must assert the *probe* outcome, not merely the row state.
- ⚠️ **Calibrate honestly in all prose:** this is a SELF-targeted control bypass, not a tenant leak.
  An owner mislabels their own key to dodge a probe on their own key. What is lost is the assurance
  that a key read-only at Connect was not broadened to trade. Serious for a product selling verified
  performance; not a 3am page. Do not write it up as a data breach.

### A — adapter parity (3 findings, ONE cause)

- **D-06: Extract the shared logic into one module both paths call, THEN add a parity guard over
  whatever genuinely resists extraction.** Not one or the other. Extraction removes today's
  duplicate; the guard catches the next duplicate someone adds. This span already shipped a fix "at
  the SINK" and still produced three one-path-only findings, so the structural fix alone has a
  demonstrated failure mode here.
- **D-07: Do NOT make `services/ingestion/mt5.py` import from `routers/exchange.py`.** That inverts
  the dependency direction (worker → HTTP layer) and is a circular-import hazard of exactly the kind
  that forced 153.5 to put the epoch registry in `mt5_client.py` rather than `mt5_concurrency.py`.
  The shared module is a third module both depend on.
- **D-08: The three missing twins are named and each must land:**
  - **A1** `_probe` — the terminal short-circuit that stops `order_check` running once the verdict is
    already `undetermined`. Without it, `_WRONG_SERVER_TOKENS` (which carry "terminal") turn an
    operator-side refusal into a 400 accusing the user their broker server is wrong. **This is the
    exact documented incident the router-side fix was written to prevent.**
  - **A2** `_read_terminal` — the broad `except Exception` around netref materialization. ⚠️
    `mt5linux` f-string-interpolates the password into remotely-eval'd source, so exception TEXT is a
    credential-disclosure surface (T-134-01 / T-153.3-23). **Only the exception CLASS is safe to
    surface.**
  - **A3** the D-31 arm — raises a bare `RuntimeError` documented PERMANENT, which escapes
    `Mt5Adapter.validate` into `job_worker.classify_exception`'s `("unknown", str(exc))`
    fall-through, so **the worker RETRIES a fault that can never clear**, re-running the whole
    serialized probe against the ONE shared terminal each time, queueing ahead of every other user's
    validate, and finally showing the user raw internal copy naming investor/master passwords.

### B — absorption: broad `except` re-swallowing the fence

- **D-09: D-42 made absorption structurally impossible for the classify arms; pre-existing catch-alls
  upstream reintroduced it. Close them at the same standard.** The fence type must reach its
  dedicated arm before any broad handler sees it.
  - **B1** `routers/exchange.py` › `_read_terminal`'s `except Exception` swallows
    `Mt5SessionAbandoned` → an operator triaging in Railway reads a gateway materialization fault
    **that never happened**, and the probe continues on a "terminal unreadable" premise that is false.
  - **B2** connect stage: the construction fence's `Mt5SessionAbandoned("connect")` is caught by the
    broad `except Exception as connect_err` **before** the dedicated D-40 arm, returning a 503 that
    **counts toward the mt5-gateway breaker** — our own abandoned thread driving the breaker toward
    opening against a healthy gateway.
  - **B3** `mt5_client.py` › `restart()` check 2 raises from INSIDE `_timed`, emitting an `mt5.stage`
    event whose `duration_ms` measures a round-trip never made — the exact event check 1 was
    deliberately placed outside `_timed` to avoid (RESEARCH §Q-4). The two checks must agree on the
    telemetry contract.

### C — the budget correction, and the oracle that cannot see it

- **D-10: Fix BOTH halves. The number alone is half the job.** `connectAbortDeadlineMsFor` was sized
  against the branch table's **closed**-breaker column (~158 500 ms) when the governing figure is the
  **failing** column (175 500 ms serialized / 85 500 ms default) — the failing state being exactly
  what a stalling seam is in when a client deadline fires. CR-01's "nothing was saved" lie is
  reachable again ~10.5 s before the route finishes writing the key.
- **D-11: `src/lib/seam-budgets.invariant.test.ts` pins the same wrong column and is therefore
  structurally unable to red on this.** A guard that cannot fail when the behaviour it names changes
  is the defect class this span shipped repeatedly. **The corrected oracle must be falsified by
  mutation — restore the wrong column and observe the red — not merely asserted.**

### E — retry affordance

- **D-12: A probe parse miss must not be permanently classified.** It was moved off
  `KEY_NETWORK_TIMEOUT` onto `KEY_SCOPE_CHECK_UNAVAILABLE`, removing the Retry control for a
  condition that is **not** always permanent — a 2xx body the schema cannot read is also what a
  rolling analytics deploy produces.
- **D-13: Touching a wizard error code ripples into 153.1's pinned code tables.**
  `EXPECTED_TABLE_SIZE` and friends must be **re-cut, never deleted** — deleting a pin to make a
  change pass is the failure this milestone has guarded against throughout.

### Claude's Discretion

- Plan sequencing, wave grouping, and how many PLAN files the five clusters become.
- Whether the A-cluster shared module lives under `services/` or a new sibling — planner's call from
  PATTERNS.md, subject to D-07.
- The exact shape of the server-held venue record (column on an existing table vs a new one), subject
  to D-01 and D-02.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The phase charter and its source
- `.planning/ROADMAP.md` §"Phase 153.6" (line ~435) — the nine findings, the four root causes, and
  the explicit OUT list.

### Cluster D — the venue lock
- `supabase/migrations/20260810120000_lock_api_keys_exchange_column.sql` — the 153.2 CR-01 remedy.
  Line 104 is the table-level `REVOKE UPDATE`; lines 25-27 record that `GRANT ALL` left UPDATE in
  place; **line 181 warns an over-broad REVOKE would break connect-a-key** (the basis of D-02); the
  header's "can never be corrected back" claim is the falsehood D-04 must fix.
- `supabase/tests/test_api_keys_exchange_not_user_writable.sql` — proves DELETE survives, never tests
  the round trip (D-05).
- `src/components/strategy/ApiKeyManager.tsx:254` (INSERT), `:352` (DELETE) — the client write sites.
- `src/components/strategy/StrategyForm.tsx:140` — the second client INSERT site.
- `src/app/api/strategies/finalize-wizard/route.ts` — the ASVS V4 scope-broadening probe and its
  venue gate; also the file 153.5 changed for the composite member `safeParse`.

### Cluster A/B — the MT5 paths
- `analytics-service/routers/exchange.py` — the path that RECEIVED 153.3's fixes; source of the twins.
- `analytics-service/services/ingestion/mt5.py` — the path that did NOT.
- `analytics-service/services/mt5_client.py` — `restart()`, `_assert_live`, `_timed` (B3).
- `analytics-service/services/job_worker.py` — `classify_exception`'s `("unknown", str(exc))`
  fall-through that makes A3 retry forever.
- `.planning/phases/153.5-wizform-abandon-work-that-outlives-its-timeout/153.5-CONTEXT.md` — D-36
  (two mechanisms), D-40 (the dedicated connect arm B2 bypasses), D-42 (absorption), D-43 (accepted
  residue).
- `.planning/phases/153.5-.../153.5-RESEARCH.md` §Q-4 — why check 1 sits outside `_timed` (B3).

### Cluster C — the budget
- `src/lib/wizard/validate-budget.ts` — `connectAbortDeadlineMsFor` (the wrong-column figure).
- `src/lib/seam-budgets.invariant.test.ts:118` — the branch table; the oracle that pins the wrong
  column (D-11).
- `src/lib/resilient-fetch.ts` — `SEAM_BUDGETS["validate-key-serialized"]`, `BREAKER_LOCK_TOMBSTONE_S`.
- `.planning/phases/153.4-.../153.4-VERIFICATION.md` — the arithmetic table that asserts
  `165 000 > 158 500 ✓`, i.e. the claim this cluster falsifies.

### Cluster E — the error code tables
- `src/lib/wizard/` error-code roster + `wizardErrors.invariant.test.ts` —
  `EXPECTED_FINALIZE_REJECTION_SITES = 32` and `EXPECTED_TABLE_SIZE` (D-13).

### Standing project rules that bind this phase
- `AGENTS.md` — "This is NOT the Next.js you know"; read `node_modules/next/dist/docs/` before
  writing App Router code.
- `CLAUDE.md` — coverage gate (lines 82 / statements 80 / functions 74 / branches 72), DESIGN.md
  precedence for any visual change.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **`delete_allocator_api_key` RPC** (`src/__tests__/delete-allocator-api-key-rpc.test.ts`) — an
  existing SECURITY DEFINER precedent for routing a key write server-side. Not needed under D-01/D-02,
  but it is the pattern to copy if a future phase revisits the write path.
- **`compositeMemberRowsSchema` + `safeParse`** (`finalize-wizard/route.ts`, landed in #673) — the
  in-file idiom for "validate an upstream shape rather than cast it". The venue-authority read added
  under D-01 should match it rather than invent a second style.
- **`scrubSeamError`** — the established wrapper that makes a logged error provably
  credential-safe; `seam-log-coverage.test.ts` enforces it and already caught one draft during the
  #673 fixer work. A2's remedy must satisfy it.

### Established Patterns
- **Money/security oracles pin ECONOMICS or INVARIANTS, never the implementation's own formula.**
  Three money bugs survived six review passes via self-referential oracles. D-11 is this rule applied
  to a timing budget.
- **Falsify by mutation, then restore.** 153.5 verified all five load-bearing mechanisms this way and
  it is the standard this phase's guards are held to.
- **Pinned code tables are re-cut, never deleted** (D-13).
- **`supabase/migrations/**` merged to main AUTO-APPLIES to PROD.** Any migration in this phase lands
  on production the moment the PR merges — there is no separate deploy step to catch it.

### Integration Points
- The mint endpoint (`/api/keys/validate-and-encrypt`) → the new server-held venue record →
  `finalize-wizard`'s probe gate. This is the spine of cluster D and crosses the Next layer and the DB.
- `routers/exchange.py` ↔ new shared module ↔ `services/ingestion/mt5.py` — the spine of cluster A,
  entirely within `analytics-service/`.

</code_context>

<specifics>
## Specific Ideas

- The user chose the **root-cause** option for cluster D over three cheaper write-path patches,
  explicitly preferring to remove the trust rather than harden the write. Planning should honour that
  framing: the deliverable is "the probe no longer believes the client", not "the client can no
  longer lie".
- The user chose **extract + guard** for cluster A over either alone, on the reasoning that this span
  already proved a structural fix by itself is not sufficient here.

</specifics>

<deferred>
## Deferred Ideas

Carried from the charter's explicit OUT list — do not let these leak into this phase:

- **MT5 as a composite member** — 153.4's CR-03 fix made an MT5 composite panel reachable for the
  first time, and `run_stitch_composite_job` has no `mt5` arm, so it `_stamp_failed`s the whole job
  as permanent (`venue 'mt5' is not a supported exchange`). That is a **product decision** (teach the
  stitch worker MT5, or block MT5 in the composite wizard), not a bug fix. Natural neighbour: Phase 155.
- **The epoch never re-binds** — `mt5_client.py`'s `_assert_live` binds on first touch only, so one
  `Mt5Client` is usable under exactly one lease for its life. No production path does this today (all
  five lease blocks ast-verified) and 153.5 pinned the constraint with a named future fix (rebind on
  lease entry). Latent, documented, not scheduled.
- **`153.5-VALIDATION.md` was never reconciled** — all 74 falsifiability rows still read `⬜ pending`,
  and SC-05-2 still states the WEDGE-01 `ticks > 0` oracle that plan 05 MEASURED as unable to see its
  own mutation. The shipped guard uses the corrected release-window discriminator; the ledger
  describes the rejected one. Same defect class as cluster C (a guard that cannot fail), but it is a
  document rather than a test. Fix it opportunistically if cluster C's work makes it cheap; do not
  scope it as a requirement.
- **`ABANDON-05/06/07` were never written into `.planning/REQUIREMENTS.md`** where every other ID is
  defined and status-tracked (W-153.5-1). Traceability debt.
- **`main_worker.py` is outside the mypy `--strict` floor** — 9 `no-untyped-def` errors sit there
  today. CI gates `services/ routers/ models/` only. The ratchet's next step, not this phase's.
- **`ScenarioComposer.tsx:2021` passes a relative URL to `fetch`** in a `useEffect`, which jsdom
  cannot resolve — surfaces as unhandled stderr noise in every test run that mounts it, and would be
  a real failure under any server-side execution. Pre-existing, unrelated to the 153 span.
- **`AllocationsTabs.scenario-state-preservation.test.tsx` fails under concurrent load** and passes in
  isolation (1389 ms). Either genuinely slow or leaking state; worth a look, not this phase.

### Reviewed Todos (not folded)
None — `gsd-sdk query todo.match-phase 153.6` returned 0 matches.

</deferred>

---

*Phase: 153.6-parity-the-fixes-that-only-landed-on-one-path*
*Context gathered: 2026-08-11*
