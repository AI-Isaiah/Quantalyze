---
phase: 153.6-parity-the-fixes-that-only-landed-on-one-path
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
  - src/lib/closed-sets.sfox-flag.test.ts
autonomous: true
requirements: [PARITY-04]
must_haves:
  truths:
    - "A row with exchange='mt5' and attested_venue='binance' is PROBED — the forged label no longer skips the ASVS V4 scope-broadening probe"
    - "A row with attested_venue NULL (legacy-unbackfilled or client-INSERT-scrubbed) is PROBED — fail-toward, with NO fallback to exchange anywhere on the gate path"
    - "A row with attested_venue='mt5' skips the probe (the capability record still governs; only the AUTHORITY changed)"
    - "Both arms — single-key and composite per-member — read the attestation; the composite embed is validated with safeParse, never cast"
  artifacts:
    - path: "src/app/api/strategies/finalize-wizard/route.ts"
      provides: "probe gate reads attested_venue on both arms; asset_class stamp deliberately stays on exchange (OQ-2 flagged in-code)"
      contains: "attested_venue"
    - path: "src/app/api/strategies/finalize-wizard/route.test.ts"
      provides: "class sweeps re-pointed at attestation + the forged-label case + the NULL case"
      contains: "attested_venue"
  key_links:
    - from: "src/app/api/strategies/finalize-wizard/route.ts single-key arm"
      to: "api_keys.attested_venue"
      via: "admin select('exchange, attested_venue') — one column added, zero extra round trips"
      pattern: "attested_venue"
    - from: "compositeMemberRowsSchema"
      to: "api_keys ( exchange, attested_venue ) embed"
      via: "zod safeParse (the #673 idiom)"
      pattern: "attested_venue.*nullable|nullable.*attested_venue"
---

<objective>
Cluster D, route half: `finalize-wizard`'s scope-broadening probe gate stops believing the
client-writable `exchange` column and reads the SERVER-attested venue — on both arms. The user's
framing binds: the deliverable is "the probe no longer believes the client", not "the client can no
longer lie". NULL attests nothing ⇒ probed (`venueSupportsScopeProbe(null) === true` is the
fail-toward direction that makes the whole design safe).

⛔ Never fall back to `exchange` when `attested_venue` is null — that is "the hole that never
closes" and would make the phase a no-op for every existing row (RESEARCH Q4.4).

Planning-time verification: route.ts:1923's third `from("api_keys")` is a `last_sync_at`
denormalization UPDATE — NOT on the probe path (assumption A4 resolved). The gate genuinely has one
authority, behind an `api_key_id` presence check on each arm.

Output: the gate re-pointed, class sweeps re-pointed, the forged-label oracle, and the OQ-3 class
guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-CONTEXT.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-RESEARCH.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-PATTERNS.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-VALIDATION.md

Key sections: RESEARCH §Q2 (the two call sites + other exchange readers), §Q5 (the gate is sole
authority + the existing class sweeps to re-point), "Code Examples" (the gate after D-01);
PATTERNS §D2 (read analogs + schema analog + sweep analogs with anti-vacuity floors).

⚠️ AGENTS.md: this is NOT the Next.js you know — the edits are inside an existing
`export const POST = withAuth(...)` route handler; verify any route-handler behaviour against
node_modules/next/dist/docs/ before writing, not training data.
</context>

<interfaces>
From src/lib/closed-sets.ts (existing — do not modify):
```ts
export function venueSupportsScopeProbe(venue: string | null): boolean;
  // venueCapabilities(venue)?.scopeProbeSupported ?? true  — null ⇒ true ⇒ PROBED (fail-toward)
  // Object.hasOwn lookup — prototype keys cannot forge a row; do not regress
```
From route.ts (existing shapes to extend):
```ts
// :159-164  compositeMemberRowsSchema — gains: api_keys: z.object({ exchange: z.string().nullable(),
//           attested_venue: z.string().nullable() }).nullish()
// :1162-1177 single-key admin read — select("exchange") → select("exchange, attested_venue")
// :726-932  runScopeBroadeningProbe(…, venue) — the venue argument becomes the ATTESTED venue
```
Test harness: route.test.ts STATE drives `adminApiKeysExchange` (:3735) and
`strategyKeysList[].api_keys.exchange` (:3763) — both gain attested_venue fields.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 fixtures + the forged-label oracle, observed RED</name>
  <files>src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/finalize-wizard/route.test.ts:3712-3771 (the two CLASS SWEEPS + their anti-vacuity floors — the shape to extend, derived from the capability registry, never from a venue name)
    - src/app/api/strategies/finalize-wizard/route.ts:726-770 (the gate + the :767-769 skip), :1161-1177, :1319-1345
    - 153.6-VALIDATION.md row D-probe + "attested_venue fixtures in route.test.ts's STATE harness" (Wave 0)
  </read_first>
  <behavior>
    - NEW: exchange='mt5' + attested_venue='binance' ⇒ the probe RUNS (the whole cluster-D deliverable in oracle form)
    - NEW: exchange='mt5' + attested_venue=null ⇒ the probe RUNS (legacy/scrubbed ⇒ fail-toward)
    - NEW: attested_venue='mt5' ⇒ the probe is SKIPPED (capability record still governs)
    - Both CLASS SWEEPS drive attested_venue (single-key and composite), keeping their anti-vacuity floors (nonProbing non-empty AND smaller than the full set)
    - Composite: a member whose embed is absent/null still parses and is PROBED with a null venue (the :150-158 rationale — a real runtime state, not drift)
  </behavior>
  <action>
    Extend the STATE harness with attestation fields (`adminApiKeysAttestedVenue` beside
    `adminApiKeysExchange`; `attested_venue` in `strategyKeysList[].api_keys`). Re-point both class
    sweeps to drive attestation. Add the three new behaviour cases above on BOTH arms where
    applicable. Run and OBSERVE RED (the gate still reads exchange today) — the forged-label case
    failing is the demonstration that the oracle can see the live defect; paste the failing
    assertion into the SUMMARY. Do not weaken floors to get there.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts -t "attested" 2>&1 | grep -qi "fail"</automated>
  </verify>
  <acceptance_criteria>
    - Fixtures added without breaking existing cases (only the sweeps + new cases exercise attestation)
    - The forged-label case and the NULL case observed RED against the current gate
    - Anti-vacuity floors intact (hand-typed, non-empty, strictly smaller than the full venue set)
  </acceptance_criteria>
  <done>The cluster-D oracle exists and demonstrably sees today's bypass.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point the gate; flag OQ-2; add the OQ-3 class guard; falsify</name>
  <files>src/app/api/strategies/finalize-wizard/route.ts, src/app/api/strategies/finalize-wizard/route.test.ts, src/lib/closed-sets.sfox-flag.test.ts</files>
  <read_first>
    - src/app/api/strategies/finalize-wizard/route.ts:1161-1240 (single-key read + :1200-1206 asset_class stamp + :1230-1231 gate call), :1319-1345 + :1511-1518 (composite embed, safeParse, per-member gate), :159-164 (schema)
    - src/lib/closed-sets.ts:146, :196-200, :436-484 (the capability row, the helper, UI_EXCHANGE_CODES vs WIZARD_ONLY_EXCHANGE_CODES)
    - 153.6-RESEARCH.md "Code Examples" (the exact post-D-01 gate shape) and §OQ-3
  </read_first>
  <action>
    Single-key arm: widen the admin read to `select("exchange, attested_venue")` (zero extra round
    trips). Derive `attestedVenue` (string or null) and pass THAT to `runScopeBroadeningProbe` —
    never `apiKeyExchange`, no fallback. ⚠️ The existing read's error arm is non-blocking BY DESIGN
    for the asset_class stamp; the probe gate must NOT inherit that leniency — on a failed read the
    attestation is simply null ⇒ PROBED (fail-toward; state this in a comment). The asset_class
    stamp (:1200-1206) KEEPS reading `exchange` — add a one-line comment flagging it as the known
    forgeable money-math input deliberately left for the PARITY-04 follow-on (OQ-2: mechanism now
    exists, change is one identifier; do NOT widen this phase). `resolvedSource` (:1566-1588) is
    untouched (a mislabel there is self-defeating — sync fails).
    Composite arm: embed becomes `api_keys ( exchange, attested_venue )`; add the nullable field to
    `compositeMemberRowsSchema` (safeParse idiom, never a cast); the per-member gate call passes the
    member's attested_venue. Absent/null embed still parses ⇒ probed with null.
    Class guard (OQ-3, cheap, closes the future hole): in `src/lib/closed-sets.sfox-flag.test.ts`
    (the existing closed-sets test file) assert no venue in UI_EXCHANGE_CODES has
    `scopeProbeSupported: false` — i.e. any venue reachable from a client-INSERT surface must be
    probeable (those surfaces produce NULL attestations that get probed; a probe-exempt venue there
    would be permanently unprobeable-and-unattested). Anti-vacuity: assert UI_EXCHANGE_CODES is
    non-empty. Falsified by adding mt5 to UI_EXCHANGE_CODES.
    FALSIFY D-probe (ledger): revert the gate's read to `exchange` (both arms) → the forged-label
    and NULL cases red; restore; paste the failing assertions into the VALIDATION ledger.
    State in the SUMMARY: this is the TS half of D-05 (the probe OUTCOME); the SQL half (input
    unforgeability) is plan 03 — neither alone discharges D-05.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts src/lib/closed-sets.sfox-flag.test.ts 2>&1 | tail -5 | grep -qi "passed"</automated>
  </verify>
  <acceptance_criteria>
    - All Task-1 cases green; both sweeps green driving attestation; full route.test.ts green
    - grep of route.ts probe-gate path shows zero `exchange` reads feeding runScopeBroadeningProbe; the ONLY exchange readers left are the asset_class stamp (flagged) and resolvedSource
    - OQ-3 guard present with anti-vacuity; observed red when mt5 is temporarily added to UI_EXCHANGE_CODES (then restored)
    - D-probe mutation observed red and recorded in the ledger
    - `npm test` full suite green; seam-log-coverage still passes (any new logged error goes through scrubSeamError)
  </acceptance_criteria>
  <done>The probe believes only the server; sweeps, forged-label oracle, and class guard all green; falsification recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-writable api_keys.exchange → probe gate | severed by this plan: the gate reads only the server-attested value |
| Supabase admin read → gate decision | admin client, RLS-exempt; a failed read must fail TOWARD probing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.6-D1 | Elevation of privilege (self-scoped) | probe gate authority | mitigate | gate reads attested_venue only; NULL ⇒ probed; no fallback (the read half of plan 03's write half) |
| T-153.6-D7 | Tampering | composite embed shape from PostgREST | mitigate | zod safeParse on the widened schema (V5 — the #673 idiom, never a cast) |
| T-153.6-D8 | Elevation (future) | a probe-exempt venue joining a client-INSERT surface | mitigate | OQ-3 class guard: UI_EXCHANGE_CODES ∩ {scopeProbeSupported:false} = ∅ |
| T-153.6-D9 | Tampering (accepted residual) | asset_class stamp reads forgeable exchange (√365 vs √252) | accept | OUT of D-01's charter (OQ-2); flagged in-code as the PARITY-04 follow-on; a mislabel is self-targeted money-math distortion of the owner's own strategy |
| T-153.6-SC | Tampering | package installs | accept | phase installs nothing |
</threat_model>

<verification>
- `npm test` green with coverage thresholds; the forged-label case is the cluster's headline oracle
- VALIDATION ledger row D-probe reconciled with pasted evidence
</verification>

<success_criteria>
- The forged-label row is probed; the NULL row is probed; the attested-mt5 row is skipped
- Both arms read attestation via one widened read each (zero new round trips, no seam calls — D-01c honored)
- OQ-2 flagged, not implemented; OQ-3 guard landed
</success_criteria>

<output>
Create `.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-04-SUMMARY.md` when done.
Record: the observed-red outputs and the exact gate-path diff.
</output>
