---
phase: 153.6-parity-the-fixes-that-only-landed-on-one-path
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/wizard/validate-budget.ts
  - src/lib/wizard/validate-budget.test.ts
  - src/lib/seam-budgets.invariant.test.ts
  - src/lib/seam-constants.pin.test.ts
  - src/app/(dashboard)/strategies/new/wizard/ValidateWaitCard.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
autonomous: true
requirements: [PARITY-03]
must_haves:
  truths:
    - "The browser is the LAST party to give up on both connect routes, in EVERY breaker state — the client deadline exceeds the route's failing-state worst case on the serialized arm (190 500 > 175 500) AND the default arm (100 500 > 85 500)"
    - "CR-01's 'nothing was saved' lie is unreachable again: a client abort can only mean the server stopped answering, never that the route is still writing the key"
    - "158_500 appears nowhere as an oracle (repo grep 1 → 0)"
    - "The new oracle reds when the deadline is sized on the closed column — including structurally (MAX-over-states replaced by closed-only must also red)"
  artifacts:
    - path: "src/lib/wizard/validate-budget.ts"
      provides: "BREAKER_STORE_WORST_CASE_FAILING_MS = 25_500 (hand-typed client-safe twin) added to connectAbortDeadlineMsFor's arm-agnostic body"
      contains: "BREAKER_STORE_WORST_CASE_FAILING_MS"
    - path: "src/lib/seam-budgets.invariant.test.ts"
      provides: "The state-quantified economic oracle ('browser is the last party') + its anti-vacuity floor (hand-typed 2 aborting routes)"
      contains: "last party"
    - path: "src/lib/seam-constants.pin.test.ts"
      provides: "Both-sides-hand-typed agreement pin for the new constant"
      contains: "BREAKER_STORE_WORST_CASE_FAILING_MS"
  key_links:
    - from: "src/lib/seam-budgets.invariant.test.ts"
      to: "src/lib/wizard/validate-budget.ts"
      via: "imports connectAbortDeadlineMsFor and quantifies it over BREAKER_STATES via worstBranch"
      pattern: "connectAbortDeadlineMsFor"
    - from: "src/lib/wizard/validate-budget.ts"
      to: "seam core"
      via: "NO import edge (client-safe module; enforced by the source-scan test)"
      pattern: "T-153.4-10"
---

<objective>
Close cluster C: `connectAbortDeadlineMsFor` was sized against the branch table's CLOSED-breaker
column (~158 500 ms) when the governing figure is the FAILING column — and the shortfall is a
per-route quantity (`failing_store − grace = 25 500 − 15 000 = 10 500 ms`), so BOTH venue arms are
short by the identical amount (D-10a). Fix the number on both arms (190 500 serialized / 100 500
default) AND replace the blind oracle with one that pins the ECONOMICS — quantified over every
breaker state, so copying the wrong column can never again pass (D-11/D-11a).

⛔ D-11 CORRECTED: the blind oracle is `src/lib/wizard/validate-budget.test.ts:226`
(`toBeGreaterThan(158_500)`) — NOT `seam-budgets.invariant.test.ts`, which holds 158 500 only in a
prose comment table. Editing the wrong file leaves the real guard green (Pitfall P1).

Purpose: a stalling seam is in the FAILING state when a client deadline fires; the deadline must
cover that state or the abort screen lies while the route is still writing the key (CR-01).
Output: corrected deadline on both arms, the state-quantified oracle, the agreement pin, and three
prose re-cuts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-CONTEXT.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-RESEARCH.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-PATTERNS.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-VALIDATION.md

Key sections: RESEARCH §Q13 (branch table + which column the deadline targets), §Q14 (the exact
arithmetic, the corrected figure, the oracle shape, the three-mutation falsification plan),
PATTERNS §C1 (ENCRYPT_KEY_BUDGET_MS docblock analog), §C2 (the D-15 literal-oracle analog +
anti-vacuity fence), §C3 (both-sides-hand-typed pin doctrine).
</context>

<interfaces>
The arithmetic (hand-verified in RESEARCH §Q14 against source constants — restate in comments,
never re-derive in a new file):

| create-with-key / composite/add-key | serialized | default |
|---|---|---|
| request legs (validate + encrypt) | 150 000 | 60 000 |
| store, failing (2 legs × 3 × 4 250) | 25 500 | 25 500 |
| governing worst case (failing) | 175 500 | 85 500 |
| today's deadline | 165 000 ❌ | 75 000 ❌ |
| corrected deadline (+ 25 500 failing store term) | **190 500** | **100 500** |

New export in `validate-budget.ts`:
```ts
export const BREAKER_STORE_WORST_CASE_FAILING_MS = 25_500; // hand-typed twin; NEVER imported from seam core
// connectAbortDeadlineMsFor = validateBudgetMsFor(exchange) + ENCRYPT_KEY_BUDGET_MS
//                           + BREAKER_STORE_WORST_CASE_FAILING_MS + WAIT_ABORT_GRACE_MS
```
Existing machinery to reuse in the oracle (never re-derive): `BREAKER_STATES` (`:408`),
`worstBranch`/`branchWorstCases` (`:680-731`) in `seam-budgets.invariant.test.ts`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The state-quantified economic oracle — written FIRST, observed RED against today's deadline</name>
  <files>src/lib/seam-budgets.invariant.test.ts, src/lib/wizard/validate-budget.test.ts</files>
  <read_first>
    - src/lib/seam-budgets.invariant.test.ts:36-65 (SC-4b model + the "table compared to itself is green forever" header), :114-135 (branch table + existing literal oracles), :408, :680-731 (BREAKER_STATES, worstBranch), :836-895 (anti-vacuity fence + D-15 literal-oracle analog)
    - src/lib/wizard/validate-budget.test.ts:203-240 (the property oracle above the blind one; the T-153.4-10 no-seam-import scan)
    - 153.6-VALIDATION.md rows C-serialized / C-default / C-oracle + Oracle Independence table
  </read_first>
  <behavior>
    - New it("… the browser is the last party to give up …") in seam-budgets.invariant.test.ts: for EACH of the two browser-aborted connect routes (create-with-key, composite/add-key) and EACH venue branch (serialized-venue, default-venue), clientDeadline = connectAbortDeadlineMsFor(<a venue on that branch>) exceeds MAX over BREAKER_STATES of worstCaseMs(route, branch, state)
    - Anti-vacuity floor: the set of aborting routes is non-empty and equals a hand-typed 2 (the :229-233 idiom)
    - RED first: against today's 165 000/75 000 both arms fail (165 000 < 175 500; 75 000 < 85 500)
  </behavior>
  <action>
    Write the oracle in `seam-budgets.invariant.test.ts` (the only file owning worstCaseMs and the
    branch grouping — PATTERNS minor-gap note says this is a new USE of existing machinery, not a
    new mechanism). Import `connectAbortDeadlineMsFor` from the client module. State the economics
    in the comment: "the browser must be the LAST party to give up, for the route it aborts, in
    EVERY breaker state — quantifying over all states is what makes the wrong-column mistake
    structurally impossible" (copy the D-15 analog's three comment properties: hand-computed
    derivation written out; 'change this only because inputs changed'; which mutation reds it).
    Run it and OBSERVE THE RED on both arms against the current deadline — paste both failing
    assertions into the SUMMARY (this is the defect being demonstrated, not a test failure to fix
    by weakening).
    Then re-cut the blind oracle at `validate-budget.test.ts:221-227`: replace
    `toBeGreaterThan(158_500)` with assertions against the hand-typed FAILING-column figures for
    BOTH arms (serialized > 175_500, default > 85_500), with a derivation comment naming the failing
    column and why the failing state governs (a stalling seam is failing when the deadline fires),
    and a cross-reference naming the invariant-file oracle as the primary state-quantified guard.
    ⛔ Do not delete the it() — re-cut it (a deleted guard is the phase's own defect class).
    ⛔ Do not touch the prose branch table at seam-budgets.invariant.test.ts:114-121 except to
    re-derive/extend it if the executor updates the deadline row — it is a canonical reference.
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-budgets.invariant.test.ts -t "last party" 2>&1 | grep -q "fail" && npx vitest run src/lib/wizard/validate-budget.test.ts 2>&1 | grep -qv "158_500"</automated>
  </verify>
  <acceptance_criteria>
    - The new oracle exists, quantifies over BREAKER_STATES via worstBranch, and REDS against today's deadline on BOTH arms (failing output captured)
    - Anti-vacuity floor pins the aborting-route count at a hand-typed 2
    - `grep -rn "158_500\|158500" src/` returns 0 code hits (prose "158 500" in the branch table may remain)
    - The re-cut validate-budget.test.ts oracle pins 175_500/85_500 with the derivation written out
  </acceptance_criteria>
  <done>Oracle in place and demonstrably able to see the live defect (observed red); blind 158_500 pin gone.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the deadline on BOTH arms + prose re-cuts</name>
  <files>src/lib/wizard/validate-budget.ts, src/app/(dashboard)/strategies/new/wizard/ValidateWaitCard.tsx, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx</files>
  <read_first>
    - src/lib/wizard/validate-budget.ts:140-188 (ENCRYPT_KEY_BUDGET_MS docblock analog + connectAbortDeadlineMsFor + its now-wrong :160 docblock)
    - src/app/(dashboard)/strategies/new/wizard/ValidateWaitCard.tsx:171, steps/ConnectKeyStep.tsx:559, steps/MultiKeyConnectStep.tsx:1054 (the three prose restatements of "validate + encrypt + grace")
  </read_first>
  <action>
    Add `BREAKER_STORE_WORST_CASE_FAILING_MS = 25_500` to `validate-budget.ts` with a docblock
    copying the ENCRYPT_KEY_BUDGET_MS shape exactly: what it is a hand-typed twin of (2 seam legs ×
    STORE_COMMANDS_PER_SEAM_CALL.failing(3) × STORE_COMMAND_WORST_CASE_MS(4 250)), the ⛔ this
    module may never import the seam table ban, and the "deliberately NOT a copy figure" note. Add
    it to `connectAbortDeadlineMsFor`'s ARM-AGNOSTIC body (D-10a — one edit fixes both arms:
    serialized 165 000 → 190 500, default 75 000 → 100 500; both stay far under the 300 000 ms
    maxDuration ceiling). Correct the function's docblock: the governing figure is the FAILING
    column (175 500 / 85 500), and cite why (the failing state is the state a stalling seam is in
    when the deadline fires). Re-cut the three prose restatements ("validate + encrypt + grace" →
    "validate + encrypt + failing-state store worst case + grace") at the three listed component
    sites — comments only, no visual/DESIGN change. Task 1's oracle must now be GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizard/validate-budget.test.ts src/lib/seam-budgets.invariant.test.ts 2>&1 | tail -5 | grep -qi "passed"</automated>
  </verify>
  <acceptance_criteria>
    - connectAbortDeadlineMsFor("mt5") === 190_500 and a default venue === 100_500 (asserted by the re-cut oracle)
    - The economic oracle from Task 1 is green; the no-seam-import source scan (T-153.4-10) still passes
    - All three prose sites restate the corrected formula; no UI/visual change
  </acceptance_criteria>
  <done>Both arms cover the failing-state worst case; every oracle green; prose consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Agreement pin + the three falsifying mutations, observed and recorded</name>
  <files>src/lib/seam-constants.pin.test.ts</files>
  <read_first>
    - src/lib/seam-constants.pin.test.ts:1120-1175 (the both-sides-hand-typed pin trio; the ENCRYPT_KEY_BUDGET_MS half explains the CR-01 mechanism this phase widens)
    - 153.6-RESEARCH.md §Q14 falsification table (the three mutations; the second is the one that distinguishes this oracle from the one it replaces)
    - 153.6-VALIDATION.md Falsifiability Ledger rows C-serialized / C-default / C-oracle
  </read_first>
  <action>
    Add the fourth agreement pin in `seam-constants.pin.test.ts` beside the existing trio: (a)
    `BREAKER_STORE_WORST_CASE_FAILING_MS` equals the server-side derivation (2 × 3 × 4 250) hand-
    typed against the seam-table constants, AND (b) equals a bare literal 25_500 typed in the pin
    file — both halves, per the file's own doctrine ("one assertion without the other would be half
    a guard"). Extend the ENCRYPT_KEY_BUDGET_MS block's CR-01 sentence rather than starting a new
    one.
    FALSIFY (run each, observe red, restore, paste the failing assertion into the VALIDATION
    ledger):
    C-serialized/C-default: revert connectAbortDeadlineMsFor to validate+encrypt+grace → the
    economic oracle must red on BOTH branches (this is the ledger's ⛔ second-member-of-the-class
    row — a fix that only corrected MT5 must red HERE).
    Structural: change the oracle's MAX-over-states to read "closed" only → it must ALSO red; if it
    stays green the oracle is still column-selectable — fix the oracle, not the number.
    Store-model sensitivity: set STORE_COMMANDS_PER_SEAM_CALL.failing to 1 → the oracle (or the
    existing anti-vacuity fence) must red.
    C-oracle: re-introduce `toBeGreaterThan(158_500)` at the old site and note it stays GREEN under
    both figures — that vacuity is the defect being removed; record the observation, then remove it
    again.
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-constants.pin.test.ts src/lib/seam-budgets.invariant.test.ts src/lib/wizard/validate-budget.test.ts 2>&1 | tail -5 | grep -qi "passed"</automated>
  </verify>
  <acceptance_criteria>
    - Agreement pin present with BOTH halves hand-typed
    - All four mutations executed; reds observed for the three that must red; the C-oracle vacuity observation recorded; everything restored
    - VALIDATION.md ledger rows C-serialized, C-default, C-oracle carry pasted failing assertions / observations
    - `npm test` full suite green (Node-22 check via PATH=/opt/homebrew/opt/node@22/bin if any CI-only doubt)
  </acceptance_criteria>
  <done>Pin landed; falsification measured, not predicted; ledger reconciled for cluster C.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser deadline → server route lifetime | the client's abort claim ("nothing was saved") must never fire while the route is still writing the key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.6-C1 | Denial of service / integrity (self) | connectAbortDeadlineMsFor vs failing-state route worst case | mitigate | deadline covers failing column on BOTH arms; a premature abort invited a resubmit → duplicate keys + double Railway validate/encrypt charges (the CR-01 mechanism) |
| T-153.6-C2 | (meta) guard that cannot fail | the deadline oracle | mitigate | state-quantified economics oracle + closed-only structural mutation must red (D-11a) |
| T-153.6-SC | Tampering | package installs | accept | phase installs nothing |
</threat_model>

<verification>
- `npm test` green; coverage thresholds hold (lines 82 / statements 80 / functions 74 / branches 72)
- grep `158_500|158500` in src/ → 0 code hits
- VALIDATION ledger rows C-* reconciled with pasted evidence
</verification>

<success_criteria>
- Both arms' deadlines exceed their failing-state worst cases with exactly the grace as margin
- The oracle quantifies over every breaker state and was observed to red under the wrong-column mutation AND the closed-only structural mutation
- No new import edge from the client-safe module to the seam core
</success_criteria>

<output>
Create `.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-02-SUMMARY.md` when done.
Record: the observed-red outputs (pre-fix oracle red, all mutations), and the final figures 190 500 / 100 500.
</output>
