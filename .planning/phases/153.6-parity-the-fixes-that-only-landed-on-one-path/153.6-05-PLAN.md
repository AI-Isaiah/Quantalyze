---
phase: 153.6-parity-the-fixes-that-only-landed-on-one-path
plan: 05
type: execute
wave: 2
depends_on: [153.6-01]
files_modified:
  - analytics-service/routers/exchange.py
  - analytics-service/services/mt5_client.py
  - analytics-service/tests/test_mt5_validate.py
  - analytics-service/tests/test_mt5_client_contract.py
  - analytics-service/tests/test_mt5_probe_parity_roster.py
autonomous: true
requirements: [PARITY-02, PARITY-01]
must_haves:
  truths:
    - "A stage-1 (connect) Mt5SessionAbandoned answers 424 NETWORK_UNAVAILABLE transient — never the 503 that counts toward breaker:mt5-gateway (B2: our own abandoned thread can no longer drive the breaker open against a healthy gateway)"
    - "No mt5.stage event ever carries error_class='Mt5SessionAbandoned' — a fence refusal is not a round-trip and never enters the D-32 recovery-latency population (B3, at the sink, for EVERY stage)"
    - "A REAL stage failure still emits ok=False (the suppression is fence-only, proven by a positive control)"
    - "No hand-written second copy of the probe body exists across the three files — and one collapsed scan cannot be masked by two healthy ones (per-file floors)"
  artifacts:
    - path: "analytics-service/routers/exchange.py"
      provides: "stage-1 connect try gains the dedicated abandon arm (byte-mirror of the stage-2 D-40 disposition)"
      contains: "Mt5SessionAbandoned"
    - path: "analytics-service/services/mt5_client.py"
      provides: "_timed re-raises Mt5SessionAbandoned WITHOUT emitting, ahead of the BaseException emit arm"
      contains: "Mt5SessionAbandoned"
    - path: "analytics-service/tests/test_mt5_probe_parity_roster.py"
      provides: "ast roster over three files: routers/exchange.py, services/ingestion/mt5.py, services/mt5_probe.py — per-file floors, self-tests, read-by-path"
      min_lines: 200
  key_links:
    - from: "routers/exchange.py stage-1 try"
      to: "424 NETWORK_UNAVAILABLE disposition"
      via: "new arm BELOW asyncio.TimeoutError, ABOVE except Exception as connect_err"
      pattern: "except Mt5SessionAbandoned"
    - from: "mt5_client._timed"
      to: "emit_mt5_stage_event"
      via: "fence-type re-raise BEFORE the emit arm"
      pattern: "except Mt5SessionAbandoned"
    - from: "test_mt5_probe_parity_roster.py"
      to: "the three scanned files"
      via: "ast.parse over files read BY PATH (never inspect.getsource — importing routers.exchange pollutes sys.modules)"
      pattern: "ast\\.parse"
---

<objective>
Close cluster B's remaining two absorption sites and land the D-06 second half — the structural
parity guard.

B2: the construction fence's `Mt5SessionAbandoned("connect")` is caught by stage 1's broad
`except Exception as connect_err` and answered as a 503 that COUNTS toward `breaker:mt5-gateway`
(`resilient-fetch.ts:519-525` names this exact arm as one of the three counting sites). ⚠️ D-15:
this is NOT arm-ordering — the dedicated D-40 arm sits on a DIFFERENT try block (stage 2); the fix
is a new arm on the stage-1 try. B3: `restart()` check 2 raises from INSIDE `_timed`, emitting a
stage event for a deliberately REFUSED restart, contaminating the D-32 recovery-latency population
Phase 155 reads. ⚠️ D-16: the remedy goes in `_timed` (the sink — the contract becomes structural
for every stage), NEVER by restructuring `restart()` (three interacting cleanup invariants, P6).

The roster answers the phase's question for cluster A: what makes the two paths unable to diverge
AGAIN? Extraction (plan 01) removed today's duplicates; the roster reds on the next hand-written
copy someone adds.

Output: two arm additions with tests + falsifications, and the three-file ast parity roster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-CONTEXT.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-RESEARCH.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-PATTERNS.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-VALIDATION.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-01-SUMMARY.md

Key sections: RESEARCH §Q10 (exact except structures + placements), §Q11 (the breaker-counting
proof), §Q12 (the telemetry contract, verbatim, + the rejected alternative), §Q9 (guard shape);
PATTERNS §B2 (the stage-2 disposition to byte-mirror), §B3 (the _timed analog + the :735-741
contract sentence to copy), §A2 (the five roster elements to copy exactly), No-Analog #3 (why this
roster needs per-file floors + SCANNED_RELPATHS).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: B2 — the stage-1 connect abandon arm (424, breaker-inert)</name>
  <files>analytics-service/routers/exchange.py, analytics-service/tests/test_mt5_validate.py</files>
  <read_first>
    - analytics-service/routers/exchange.py:513-578 (the stage-1 try: :526 TimeoutError arm, :537 broad arm, :554 inversion branch, :571-578 the counting 503), :729-764 (the stage-2 D-40 arm to byte-mirror)
    - src/lib/resilient-fetch.ts:519-545 (the counting set naming this arm; 424 documented as never counting)
    - 153.6-VALIDATION.md row B2
  </read_first>
  <behavior>
    - A stage-1 Mt5SessionAbandoned (raised by Mt5Client.__init__'s construction fence) answers 424 NETWORK_UNAVAILABLE, recoverable=True, trace.outcome="transient", with a WARNING log
    - It NEVER answers 503 and never a body naming dependency="mt5-gateway" with status 503 (the breaker-counting shape)
    - asyncio.TimeoutError on the same try still takes its own arm (a timeout is a different fact)
  </behavior>
  <action>
    Add `except Mt5SessionAbandoned:` to the STAGE-1 connect try, placed BELOW
    `except asyncio.TimeoutError:` (:526) and ABOVE `except Exception as connect_err:` (:537),
    byte-mirroring the stage-2 D-40 disposition (:729-764): WARNING log naming the decision only
    (no host/port/terminal key/credential — S2), `trace.outcome = "transient"`,
    `VenueTransientHTTPException(status_code=424, code="NETWORK_UNAVAILABLE",
    detail=NETWORK_ERROR_DETAIL, recoverable=True)`. Reuse the existing code — never mint one
    (the :554-568 discipline). Comment carries the S1 beats + why 424: 503 with
    dependency="mt5-gateway" is one of exactly three breaker-counting sites, and this fault is our
    own abandoned thread, not gateway health.
    Test (`-k connect_stage_abandon` in test_mt5_validate.py): drive the router with a client
    constructor raising Mt5SessionAbandoned("connect"); assert 424 + the transient envelope, and
    assert NOT 503.
    FALSIFY B2 (ledger): remove the new arm → the test reds observing a 503; restore; paste.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_validate.py -k connect_stage_abandon -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Arm placement exactly between :526 and :537 equivalents; stage-2 arm untouched
    - Test asserts 424-and-not-503 (both halves)
    - B2 mutation observed red and recorded in the ledger
  </acceptance_criteria>
  <done>A stage-1 abandon is transient and breaker-inert, proven and falsified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: B3 — _timed suppresses the stage event for fence refusals (sink fix)</name>
  <files>analytics-service/services/mt5_client.py, analytics-service/tests/test_mt5_client_contract.py</files>
  <read_first>
    - analytics-service/services/mt5_client.py:768-806 (_timed + its docstring), :735-741 (the ⛔ NOT-a-stage-event contract sentence inside _assert_live — copy it into the new arm), :1230-1237 (why restart's check 1 sits OUTSIDE _timed)
    - analytics-service/tests/test_mt5_client_contract.py:1983, :2154, :2185 (existing abandon-escape assertions — the new test lives beside them)
    - 153.6-RESEARCH.md §Q12 (contract verbatim + safety argument: four raise sites, all our own fences — grep-verified)
    - 153.6-VALIDATION.md rows B3 and B3-inverse
  </read_first>
  <behavior>
    - No mt5.stage event carries error_class="Mt5SessionAbandoned" (any stage — the contract is structural, not positional)
    - A genuine Mt5ClientError from a _timed call still emits ok=False (positive control — the guard cannot pass by emitting nothing)
    - The exception is re-raised UNCHANGED (same object/type/traceback — the docstring's existing ⛔)
  </behavior>
  <action>
    Add `except Mt5SessionAbandoned: raise` to `_timed` AHEAD of the `except BaseException` emit
    arm. The comment copies the :735-741 contract sentence ("⛔ NOT a mt5.stage event. A refusal is
    not a round-trip; emitting one would pollute the duration_ms population Phase 155 depends on…
    If a COUNT is ever wanted it needs a distinct event name") and adds: check 1 of restart()
    already sits OUTSIDE this bracket for exactly this reason; this arm makes the contract
    structural for every stage. Safety note: Mt5SessionAbandoned is raised at exactly four sites,
    all our own fences (mt5_client.py :681/:711/:766/:1295) — nothing from transport can hide
    behind it. ⛔ Do NOT touch restart() (:1151-1318) — P6: the three interacting cleanup
    invariants at :1276-1295 are where a fix creates two leaks.
    Test (`-k stage_event` in test_mt5_client_contract.py): drive a _timed verb whose call raises
    Mt5SessionAbandoned → assert the exception escapes AND zero mt5.stage events were emitted for
    it; positive control in the same test: a Mt5ClientError-raising call still emits ok=False.
    FALSIFY (ledger): B3 — remove the new arm → the no-abandon-event assertion reds; B3-inverse —
    make a REAL failure stop emitting (temporarily widen the suppression) → the positive control
    reds, proving the fix suppresses only the abandon. Restore both; paste.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_client_contract.py -k stage_event -x -q</automated>
  </verify>
  <acceptance_criteria>
    - _timed diff is the single arm + comment; restart() untouched (git diff confirms)
    - Both test halves present (suppression + positive control)
    - B3 AND B3-inverse mutations observed red and recorded
  </acceptance_criteria>
  <done>Fence refusals are telemetry-invisible at the sink; real failures still counted; both directions falsified.</done>
</task>

<task type="auto">
  <name>Task 3: The three-file ast parity roster (per-file floors, self-tests, read-by-path)</name>
  <files>analytics-service/tests/test_mt5_probe_parity_roster.py</files>
  <read_first>
    - analytics-service/tests/test_mt5_abandon_roster.py (FULL, 703 lines — the five structural elements: the two-guards header table :16-33, ast-not-regex + read-by-path :34-46, hand-typed floors with the forbidden len(derived)-1 derivation :120-134, "the scan actually read the file" :296-331, the eight test_selftest_* cases :530-703)
    - analytics-service/tests/test_mt5_shutdown_roster.py:113 (the _TEARDOWN_NAMES derived-set technique for two hand-typed halves)
    - 153.6-PATTERNS.md No-Analog #3 (why per-file floors + SCANNED_RELPATHS) and §A2
    - 153.6-VALIDATION.md rows A-guard and A-guard-vacuity
  </read_first>
  <action>
    Create `tests/test_mt5_probe_parity_roster.py`. The roster's question: which functions still
    contain a hand-written copy of the extracted probe body? Scan a hand-typed `SCANNED_RELPATHS`
    tuple — `routers/exchange.py`, `services/ingestion/mt5.py`, `services/mt5_probe.py` — read BY
    PATH and `ast.parse`d (⛔ NEVER `inspect.getsource`: importing routers.exchange loads the real
    slowapi limiter, pollutes sys.modules and breaks test_exchange_router_c0202's stub-reload
    fixture — the analog's :44-46 warning applies verbatim). Predicate: a function (module-level OR
    the outermost def of a nested closure) whose body calls probe-body verbs (`order_check` /
    `terminal_info` reached via a client parameter/attribute) must either BE in
    services/mt5_probe.py or DELEGATE to it (call into mt5_probe.*) — a hand-rolled fourth copy
    reds. Copy all five analog elements: (1) header states what this roster CANNOT do, with the
    two-guards table cross-referencing test_mt5_validate_parity.py (behavioural) both ways; (2)
    ast, never regex; (3) hand-typed PER-FILE floors with a `# MEASURED 2026-08-…` date and the ⛔
    NEVER-len(derived)-1 comment — one collapsed file must not be masked by two healthy ones; (4) a
    "the scan actually read each file" first test asserting each file's floor independently; (5)
    test_selftest_* cases proving the predicates have teeth in BOTH directions on synthetic source
    (guarded + unguarded through ONE call), plus the prose-is-ignored case and the
    nested-closure-attribution case (the probe callers ARE nested closures).
    FALSIFY (ledger): A-guard — paste a hand-written second copy of the probe body into
    ingestion/mt5.py → the roster reds; A-guard-vacuity — make ONE scanned file empty/unreadable →
    the PER-FILE floor reds (⚠️ if only a TOTAL floor reds, the guard is masking a collapsed scan —
    fix the roster). Restore both; paste evidence.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_probe_parity_roster.py -q && pytest --cov=services --cov=routers --cov=main_worker --cov-fail-under=80 -q && mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <acceptance_criteria>
    - SCANNED_RELPATHS + per-file floors hand-typed with measurement dates; no floor derived from the scan
    - Self-tests cover both verdict directions, prose-ignorance, and nested-closure attribution
    - A-guard and A-guard-vacuity mutations observed red (per-file floor specifically) and recorded
    - Full pytest + coverage + mypy --strict green from analytics-service/
  </acceptance_criteria>
  <done>A future hand-written probe copy reds; a collapsed scan cannot hide; both falsified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| our own fence refusals → breaker accounting | a self-inflicted 503 must not count as gateway health evidence |
| fence refusals → mt5.stage telemetry | refusals must not contaminate the population Phase 155's decisions read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.6-B2 | Denial of service (self) | breaker:mt5-gateway | mitigate | stage-1 abandon → 424 (never counts); the counting set shrinks by the false member only |
| T-153.6-B3 | Tampering (telemetry integrity) | D-32 duration_ms population | mitigate | _timed sink suppression, fence-type only, positive control proves real failures still emit |
| T-153.6-A9 | (meta) parity guard vacuity | the roster | mitigate | per-file floors + self-tests + both falsifying mutations measured |
| T-153.6-SC | Tampering | package installs | accept | phase installs nothing |
</threat_model>

<verification>
- `cd analytics-service && pytest` full green; mypy --strict clean; coverage ≥ 80
- VALIDATION ledger rows B2, B3, B3-inverse, A-guard, A-guard-vacuity reconciled with pasted evidence
</verification>

<success_criteria>
- The fence type reaches a dedicated arm on every try block that can see it; no broad handler absorbs it anywhere upstream of the classify arms
- The telemetry contract is structural at the sink for every stage
- The parity roster reds on a hand-written second copy and cannot go vacuously green per-file
</success_criteria>

<output>
Create `.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-05-SUMMARY.md` when done.
Record: the measured floors, all five mutation observations, and confirmation restart() is untouched.
</output>
