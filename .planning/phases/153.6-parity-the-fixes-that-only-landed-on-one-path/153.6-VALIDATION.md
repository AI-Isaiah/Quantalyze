---
phase: 153.6
slug: parity-the-fixes-that-only-landed-on-one-path
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-11
---

# Phase 153.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

⛔ **THIS FILE MUST BE RECONCILED AT EXECUTION, NOT LEFT AT `⬜ pending`.** Phase 153.5 shipped its
VALIDATION.md with all 74 rows still `⬜ pending` **and** with row SC-05-2 describing a WEDGE-01
oracle that plan 05 had already MEASURED as unable to see its own mutation — the ledger documented
the *rejected* design while the code shipped the corrected one (W-153.5-2). A falsifiability ledger
that is never reconciled is itself a guard that cannot fail, which is the exact defect class this
phase exists to close. Reconcile every row, or record it as **skipped** — never silently as caught.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (TS)** | vitest + `@vitest/coverage-v8` |
| **Config file** | `vitest.config.ts` (thresholds: lines 82 / statements 80 / functions 74 / branches 72) |
| **Quick run (TS)** | `npx vitest run src/lib/wizard/validate-budget.test.ts src/lib/seam-budgets.invariant.test.ts` |
| **Full suite (TS)** | `npm test` |
| **Framework (Py)** | pytest + pytest-cov, `--cov-fail-under=80` |
| **Quick run (Py)** | `cd analytics-service && pytest tests/test_mt5_validate_parity.py tests/test_mt5_abandon_roster.py -x` |
| **Full suite (Py)** | `cd analytics-service && pytest --cov=services --cov=routers --cov=main_worker --cov-fail-under=80` |
| **Type gate (Py)** | `cd analytics-service && mypy --strict --follow-imports=silent services/ routers/ models/` |
| **SQL** | CI-only — `supabase/tests/test_*.sql`, auto-discovered. No local `psql`. |
| **Estimated runtime** | quick ~5 s · pytest full ~130 s · TS full ~6 min sharded in CI |

⚠️ **pytest MUST run from `analytics-service/`.** A repo-root run misses the VCR
`cassette_library_dir` and fires LIVE broker calls.

⚠️ **Node 22, not local Node 25**, when reproducing a CI-only vitest failure:
`PATH=/opt/homebrew/opt/node@22/bin:$PATH`.

---

## Sampling Rate

- **After every task commit:** the cluster's quick-run command from the map below.
- **After every plan wave:** `npm test` **and** `cd analytics-service && pytest`. A fixture change
  touching both suites requires BOTH — a green half is not a signal.
- **Before `/gsd:verify-work`:** both full suites green, `mypy --strict` clean, and a CI run for the
  SQL tests (they cannot run locally).
- **Max feedback latency:** ~130 s (pytest full) for the Python clusters; ~5 s for the TS oracles.

---

## Per-Task Verification Map

Re-keyed at planning (2026-08-11). ✅ `PARITY-01..05` are now DEFINED in
`.planning/REQUIREMENTS.md` (section + traceability rows written during the 153.6 planning pass —
the W-153.5-1 debt is discharged, not repeated).

**Requirement → plan/task map:**

| Req | Owner plan · task |
|-----|-------------------|
| PARITY-01 A1/A2/A3 | 153.6-01 · T1–T3 (extraction + rewiring + classify arm) |
| PARITY-01 guard (ast roster) | 153.6-05 · T3 |
| PARITY-02 B1 | 153.6-01 · T2 (same edit as A2, D-14) |
| PARITY-02 B2/B3 | 153.6-05 · T1/T2 |
| PARITY-03 | 153.6-02 · T1–T3 |
| PARITY-04 (DB: column/trigger/backfill/RPCs) | 153.6-03 · T1/T2 |
| PARITY-04 (route: probe-gate authority) | 153.6-04 · T1/T2 |
| PARITY-05 | 153.6-06 · T1/T2 |

Ledger-row ownership: A1/A2/A3 → 153.6-01 · T3; A-guard/A-guard-vacuity → 153.6-05 · T3;
B1 → 153.6-01 · T2; B2/B3/B3-inverse → 153.6-05 · T1/T2; C-serialized/C-default/C-oracle →
153.6-02 · T1/T3; D-probe → 153.6-04 · T2; D-trigger → 153.6-03 · T2; D-backfill → 153.6-03 · T1;
E/E-pin → 153.6-06 · T2.

| Req | Cluster | Behaviour | Test Type | Automated Command | File Exists | Status |
|-----|---------|-----------|-----------|-------------------|-------------|--------|
| PARITY-01 A1 | A | Neither path calls `order_check` once the verdict is `undetermined` | unit | `pytest tests/test_mt5_validate_parity.py -k short_circuit -x` | ✅ 153.6-01 | ✅ green — ledger A1 observed RED on BOTH halves (one shared body) |
| PARITY-01 A2 | A | Both paths return `None` on materialization failure, logging the CLASS only | unit | `… -k materialize -x` | ✅ 153.6-01 | ✅ green — ledger A2 observed, 3 RED |
| PARITY-01 A3 | A | The operator fault classifies `("permanent", <curated>)` and names no credential | unit | `… -k mt5_gateway_misconfigured -x` | ✅ 153.6-01 | ✅ green — ledger A3 observed, 2 RED. ⚠️ the sibling negative-vocabulary case cannot see this mutation (default message IS the curated constant); the `kind` assertion is the one with teeth |
| PARITY-01 guard | A | No hand-written second copy of the probe body exists | unit (ast) | `pytest tests/test_mt5_probe_parity_roster.py -x` | ✅ 153.6-05 | ✅ green — 12 cases (4 roster + 1 predicate fence + 7 self-tests); both A-guard mutations observed RED |
| PARITY-02 B1 | B | `Mt5SessionAbandoned` escapes `read_terminal` unabsorbed | unit | `pytest tests/test_mt5_validate.py -k abandoned_read_terminal -x` | ✅ 153.6-01 | ✅ green — ledger B1 observed. ⛔ keys on the operator LOG, not status: absorbed and unabsorbed BOTH answer 424, so a status assertion here cannot fail. Do not "simplify" it |
| PARITY-02 B2 | B | A stage-1 abandon answers 424, never a 503 naming `mt5-gateway` | unit | `… -k connect_stage_abandon -x` | ✅ 153.6-05 | ✅ green — W0 red observed (503/`dependency=mt5-gateway`), now 424 transient |
| PARITY-02 B3 | B | No `mt5.stage` event carries `error_class="Mt5SessionAbandoned"`; a real failure still emits | unit | `pytest tests/test_mt5_client_contract.py -k stage_event -x` | ✅ 153.6-05 | ✅ green — both directions falsified (B3 + B3-inverse) |
| PARITY-03 a | C | The client deadline exceeds the route worst case in EVERY breaker state, **both arms** | unit | `npx vitest run src/lib/seam-budgets.invariant.test.ts -t "browser is the last party"` | ✅ 153.6-02 | ✅ green — 85 tests in-file. 4 ledger mutations observed RED, incl. C-default (the instance-fix that only corrects MT5) and C-structural (which the OLD oracle survives) |
| PARITY-03 b | C | `158_500` appears nowhere as an oracle | unit | `npx vitest run src/lib/wizard/validate-budget.test.ts` | 🟡 exists, re-cut | ✅ green — orchestrator-verified 2026-08-11: `grep -rn "158_500\|158500" src/ --include="*.ts" --include="*.tsx"` returns **0**. ⚠️ ledger C-oracle measured the old pin GREEN under BOTH the corrected and the defective deadline — it was vacuous, not merely redundant |
| PARITY-04 a | D | A row with `exchange='mt5'`, `attested_venue='binance'` is PROBED | unit | `npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts -t "attested"` | ❌ W0 | ✅ 153.6-04 — 7 rows, both arms; W0 red observed (9 failed incl. the 2 re-pointed sweeps), now green |
| PARITY-04 b | D | DELETE + re-INSERT with a forged `attested_venue` stores NULL | SQL | CI only | 🟡 exists, +assertion | 🟡 mutation OBSERVED on a local PG16 fixture (plan 03); the CI/TEST apply is still outstanding — assertion 5 SKIPs loudly until then |
| PARITY-04 c | D | The RPC door cannot forge SELECTIVELY: `exchange` and `attested_venue` move together, and the coupling is a CHECK | SQL | CI only | ✅ 153.6-07 (5d/5e) | ✅ **9/9 assertions green** on a local PG16 fixture driving the shipped test file; both new rows falsified (see ledger CR-01-5d / CR-01-5e). ⚠️ same gate as 5a–5c — SKIPs loudly until the TEST DB catches up |
| PARITY-05 a | E | The parse miss renders a Retry; the permanent probe status does not | unit | `npx vitest run src/lib/wizardErrors.test.ts src/app/api/strategies/finalize-wizard/route.test.ts` | ✅ 153.6-06 | ✅ green — `KEY_SCOPE_CHECK_UNREADABLE` minted and wired at all four sites in one commit; RED observed first (5 failed). Both directions and the roster omission falsified (rows E / E-inverse / E-roster) |
| PARITY-05 b | E | The two `EXPECTED_TABLE_SIZE` pins agree at the re-cut value | unit | `npx vitest run src/lib/wizardErrors.test.ts -t "cannot silently diverge"` | ✅ exists | ✅ green at 75/75; `.toBe(2)` count and the regex shape untouched. 32 / 3 / 29 grep-proven UNMOVED |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `analytics-service/services/mt5_probe.py` — the extraction target must exist before its tests.
- [x] `analytics-service/tests/test_mt5_probe_parity_roster.py` — the ast guard. Copy the floors +
      self-test idiom from `tests/test_mt5_abandon_roster.py`, **with a PER-FILE floor**: this roster
      spans three files, and both existing rosters pin a single file, so one collapsed scan would
      otherwise be masked by two healthy ones (PATTERNS no-analog #3). *(153.6-05 — shipped with
      per-file walk floors 6/7/3 and NO total, plus `PROBE_VERB_UNITS_IN_HOME_FLOOR = 2`, the fence
      on the PREDICATE that catches the collapse a total would mask.)*
- [ ] New behavioural cases in `analytics-service/tests/test_mt5_validate_parity.py`.
- [ ] The economic oracle + anti-vacuity floor in `src/lib/seam-budgets.invariant.test.ts`.
- [x] `attested_venue` fixtures in `finalize-wizard/route.test.ts`'s `STATE` harness. *(153.6-04 —
      `adminApiKeysAttestedVenue` + the embed field; both CLASS SWEEPS re-pointed at the
      attestation with the client label pinned to a registry-derived non-probing code.)*
- [ ] Assertion 5 (the DELETE + re-INSERT round trip) in
      `supabase/tests/test_api_keys_exchange_not_user_writable.sql`.
- [ ] `PARITY-01..05` rows written into `.planning/REQUIREMENTS.md`.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The migration applies cleanly to PROD and the backfill's self-verify does not abort | PARITY-04 | `supabase/migrations/**` AUTO-APPLIES on merge; there is no staging replica of PROD's 29 rows | Before merge: re-run the census (`SELECT exchange, count(*) FROM api_keys GROUP BY exchange`) and confirm the **probe-exempt** population still reads **2 mt5, both created 2026-08-04**. ⚠️ UPDATED 2026-08-12 (MIG-01): the total is **no longer an abort condition** — it is reported as a delta — so a changed total does *not* require re-cutting. Only mt5 drift does. |
| ⛔ The migration is applied to PROD **BEFORE** the Vercel deployment goes live | PARITY-04 | The auto-apply and the Vercel build fire on the same merge with no ordering between them, and the ordering cannot be enforced from inside the migration | Apply via Supabase MCP `apply_migration` (or `supabase db push`), confirm the `Supabase Migrate` run is green, and only then merge/promote. If the deploy wins the race: every MT5 wizard submit answers a permanent `KEY_SCOPE_CHECK_UNAVAILABLE` and **no composite finalizes at all** (`COMPOSITE_MEMBERSHIP_UNKNOWN` 503) for the duration. Recorded in the migration header and CONTRIBUTING.md §2 (WR-03 / MIG-03). |
| The fence's behaviour against a REAL gateway | (carried) | Inherited from 153.5 D-43 — every proof is against the offline double | Deferred to Phase 155. Not this phase's gate. |

---

## Falsifiability Ledger

> **Coverage answers "is it verified?". This answers "CAN the verification FAIL?"**
> Complete the Observed column at execution. Paste the failing assertion — "the test covers it" is
> not evidence.

| SC | Mutation (exact edit to production source) | Must turn RED | Observed? | Evidence |
|----|-------------------------------------------|---------------|-----------|----------|
| A1 | shared probe: delete the terminal short-circuit so `order_check` runs on an `undetermined` verdict | `test_mt5_validate_parity.py -k short_circuit` | ✅ observed 2026-08-11 (plan 01) | 2 RED, and the ROUTER half reds too — both callers share one body: `assert "order_check" not in router_client.calls` → `AssertionError: ['login', 'account_info', 'terminal_info', 'order_check', 'account_info', 'release']`; adapter half → `AssertionError: ['login', 'account_info', 'terminal_info', 'order_check', 'account_info']` |
| A2 | shared probe: widen the narrow arm so `Mt5SessionAbandoned` is caught by the broad `except Exception` | `-k materialize` **and** `-k abandoned_read_terminal` | ✅ observed 2026-08-11 (plan 01) | 3 RED. `-k materialize`: `assert 0 == 2 where 0 = ''.count('error_class=OSError')` (+ `AttributeError: 'OSError' object has no attribute 'code'` — the widened arm reads `.code` off a raw transport error). `-k abandoned_read_terminal`: `AttributeError: 'Mt5SessionAbandoned' object has no attribute 'code'` |
| A3 | `classify_exception`: return `("unknown", str(exc))` for the operator-fault type | `-k mt5_gateway_misconfigured` | ✅ observed 2026-08-11 (plan 01) | 2 RED (unit + parity): `AssertionError: assert 'unknown' == 'permanent'`. ⚠️ the sibling `..._carries_no_classify_vocabulary` case stays GREEN under this mutation (the default message IS the curated constant, so `str(exc)` matches) — the `kind` assertion is the one with teeth here |
| A-guard | paste a hand-written second copy of the probe body into `ingestion/mt5.py` | `test_mt5_probe_parity_roster.py` | ✅ observed 2026-08-11 (plan 05) | RED, and it names both halves of the pasted copy: `AssertionError: a HAND-WRITTEN copy of the MT5 probe body exists outside services/mt5_probe.py:` / `services/ingestion/mt5.py::_probe_again (line 476) calls ['account_info', 'login', 'order_check']` / `services/ingestion/mt5.py::_read_terminal_again (line 468) calls ['terminal_info']`. Restored → 12 passed |
| A-guard-vacuity | make ONE of the three scanned files unreadable/empty | the **per-file floor**, not the total | ✅ observed 2026-08-11 (plan 05), BOTH variants | **(a) empty `services/mt5_probe.py`** → the PER-FILE floor reds and NAMES the file: `AssertionError: the walk of services/mt5_probe.py found only 0 outermost functions (hand-typed floor 3 …)` `assert 0 >= 3`. **(b) the distinguishing variant** — collapse the PREDICATE without touching the walk (rewrite `client.<verb>(` as `getattr(client, "<verb>")(` in the home module): every per-file walk floor **PASSES** (units still 7/8/4, so a TOTAL floor would have passed too) while `PROBE_VERB_UNITS_IN_HOME_FLOOR` reds — `only 0 unit(s) in services/mt5_probe.py were derived as calling a probe verb (hand-typed floor 2 …)` `assert 0 >= 2`. ⭐ (b) is the masking the row warns about, caught by a per-file fence on the PREDICATE that no unit count could provide. There is deliberately NO total floor in the file |
| B1 | `routers/exchange.py` `_read_terminal`: remove `except Mt5SessionAbandoned: raise` | `-k abandoned_read_terminal` | ✅ observed 2026-08-11 (plan 01) | Body now lives in `services/mt5_probe.read_terminal` (D-14 — one edit closes A2+B1). Arm removed → RED on the LOG oracle, and the captured log IS the finding: `WARNING quantalyze.analytics:mt5_probe.py:177 validate_key: terminal_info failed to materialize (error_class=Mt5SessionAbandoned) — capability undetermined` — a materialization fault that never happened. ⚠️ status is NOT an oracle here: absorbed and unabsorbed both answer 424 |
| B2 | remove the stage-1 abandon disposition so the broad `except Exception as connect_err` wins | `-k connect_stage_abandon` (expects 424, not 503) | ✅ observed 2026-08-11 (plan 05), TWICE | Pre-fix (the RED half of the TDD cycle) and again as an explicit post-fix mutation, identical both times: `AssertionError: assert 503 == 424` … `HTTPException(status_code=503, detail={'code': 'MT5_GATEWAY_UNREACHABLE', 'dependency': 'mt5-gateway', 'retryable': True, …})`, with `WARNING exchange.py:575 validate_key: MT5 gateway connect failed (server/bridge)` in the captured log. ⭐ **The status IS the oracle here**, unlike B1: `dependency="mt5-gateway"` + 503 is the breaker-counting body shape, so the two dispositions are genuinely distinguishable. The case drives the SHIPPED construction fence (a `_connect` seam that bumps the generation mid-connect), not an injected exception |
| B3 | `mt5_client.py` `_timed`: remove `except Mt5SessionAbandoned: raise` ahead of the emit | `test_mt5_client_contract.py -k stage_event` | ✅ observed 2026-08-11 (plan 05) | RED, and the emitted event is the finding verbatim: `AssertionError: a fence REFUSAL was recorded as an mt5.stage round-trip ([{'stage': 'restart', 'duration_ms': 0, 'ok': False, 'terminal_key': 'mt5-gw.internal:18812', 'error_class': 'Mt5SessionAbandoned', 'event': 'mt5.stage'}])` — `duration_ms: 0` is precisely the zero-duration non-call the D-32 population must not contain |
| B3-inverse | make a REAL stage failure stop emitting | same test | ✅ observed 2026-08-11 (plan 05) | Suppression widened `except Mt5SessionAbandoned` → `except Exception`: the positive control reds — `AssertionError: a REAL restart failure stopped emitting its stage event ([])` `assert 0 == 1` — **and it drags a pre-existing sibling guard down with it**: `test_no_credential_literal_reaches_any_stage_event` → `AssertionError: no failed-stage event was captured — the sweep never reaches the emission path that carries exception information`. Two independent witnesses that the fix is fence-type-only, not telemetry-wide |
| C-serialized | `validate-budget.ts`: restore the closed-column figure (deadline → 165 000) | `seam-budgets.invariant.test.ts -t "browser is the last party"` | ✅ RED (2026-08-11) | `AssertionError: The browser gives up on "src/app/api/strategies/create-with-key/route.ts" after 165000ms for a mt5 key, but that route can honestly spend 175500ms on its serialized-venue arm in the failing state … expected 165000 to be greater than 175500` |
| C-default | restore the **ccxt/default** arm's shortfall (75 000 against 85 500) | the same invariant | ✅ RED (2026-08-11) | Mutation applied as the INSTANCE fix — store term added only when `venueIsSerialized(exchange)`, so the serialized assertion PASSES: `AssertionError: The browser gives up on "…/create-with-key/route.ts" after 75000ms for a binance key, but that route can honestly spend 85500ms on its default-venue arm in the failing state … expected 75000 to be greater than 85500`. ⛔ second member of the class — an MT5-only fix reds HERE |
| C-oracle | re-introduce `toBeGreaterThan(158_500)` at `validate-budget.test.ts:226` | the economic oracle | ✅ observed GREEN under BOTH figures (2026-08-11) | `Tests 1 passed \| 34 skipped` at the corrected deadline (190 500) **and** at the defective one (165 000). ⚠️ That is the vacuity being removed, not a caught mutation: the old pin cannot fail under the defect it was written to prevent. Removed again after the observation. |
| C-structural | narrow the oracle's `BREAKER_STATES` quantification to `["closed"]` only | the economic oracle | ✅ RED (2026-08-11) | `AssertionError: This oracle no longer walks all three breaker states … expected [ 'closed' ] to deeply equal [ 'closed', 'open', 'failing' ]`. ⭐ THE distinguishing mutation — the oracle this replaces survives it (see C-oracle) |
| C-store-model | `STORE_COMMANDS_PER_SEAM_CALL.failing` 3 → 1 | the economic oracle + the anti-vacuity fence | ✅ RED (2026-08-11) | 5 reds: `expected 'closed' to be 'failing'` (the new oracle's governing-state pin), `expected 1 to be greater than 1` (the fence), plus the three pre-existing literal oracles (56 000 → 39 000, 277 500 → 192 500, 248 250 → 222 750) |
| D-probe | `finalize-wizard`: read `exchange` instead of `attested_venue` for the probe gate | `route.test.ts -t "attested"` | ✅ observed (153.6-04) | Both arms reverted → **7 failed / 126 skipped**. Forged-label + NULL rows: `AssertionError: The scope-broadening probe did NOT run… expected null not to be null`. Attested-skip rows red the OTHER way: `…expected { budgetKey: 'keys-permissions', …(2) } to be null`. Restored → green. |
| D-OQ3 | add `mt5` to `UI_EXCHANGE_CODES` (a probe-exempt venue on a client-INSERT surface) | `closed-sets.sfox-flag.test.ts -t "OQ-3"` | ✅ observed (153.6-04) | `AssertionError: Venue(s) ["mt5"] are offered on a client-facing picker AND opted out of the submit-time scope probe… expected [ 'mt5' ] to deeply equal []`. Restored → green. |
| D-trigger | drop the `BEFORE INSERT` NULLing so a client-supplied `attested_venue` persists | `test_api_keys_exchange_not_user_writable.sql` assertion 5 | ✅ observed 2026-08-11 (plan 03, local PG16 fixture) | Trigger dropped → `SET LOCAL ROLE authenticated; INSERT … attested_venue='mt5'` stored **`'mt5'`**. Assertion 5c's `IF v_attested IS NOT NULL THEN RAISE EXCEPTION` fires on exactly that row state. SQL extracted by regex from the shipped migration, not retyped |
| D-backfill | change a pinned count (29 → 28) in the self-verify block | the migration must ABORT | ✅ observed 2026-08-11 (plan 03, local PG16 fixture) | `ERROR Migration 20260811210000 ABORT: PROD census drift (found total=29, mt5=2; pinned total=28, mt5=2). Re-measure … Rolling back.` (SQLSTATE 22000). ⭐ this run FOUND A BUG: the pin was duplicated into the abort message, so it printed `pinned 29/2` — fixed to named CONSTANTs in `24548cbc` |
| D-backfill-vacuity | correct census (29/2) unmodified; a 30th row w/ signature intact; signature-free TEST-shaped DB | NOTICE / ABORT / NOTICE respectively | ✅ observed 2026-08-11 (plan 03) | Each branch reached separately — an unconditionally-aborting block would have "passed" both rows above. Signature-free half matters most: a pin that aborted every non-PROD apply would leave assertion 5 skipping forever |
| E | reclassify the parse miss as permanent so Retry disappears | `wizardErrors.test.ts` + `route.test.ts` | ✅ observed (153.6-06) | Route arm reverted to `KEY_SCOPE_CHECK_UNAVAILABLE` → **5 failed / 303 passed** in `route.test.ts`. `AssertionError: The parse miss must render its OWN copy. \`Something went wrong.\` means the code reached \`UNKNOWN\`…: expected 'We could not check this key\\'s permis…' to be 'We could not read the permission chec…'` + `expected 'KEY_SCOPE_CHECK_UNAVAILABLE' not to be 'KEY_SCOPE_CHECK_UNAVAILABLE'` + 3 `toEqual` envelope diffs. ⚠️ `wizardErrors.test.ts` stays GREEN under this mutation and that is honest, not a gap: the copy table has no view of the route. Its half is the INVERSE mutation, run separately — see E-inverse. Restored → green. |
| E-inverse | give `KEY_SCOPE_CHECK_UNAVAILABLE` a `clear_and_retry` action (the shortcut a careless fix takes instead of minting a code) | `wizardErrors.test.ts` **and** `route.test.ts` | ✅ observed (153.6-06) | 2 RED, one in EACH file: `AssertionError: ⛔ T-153.6-E2. The parse miss got its Retry back by MINTING a code, not by widening this one's actions. If this is now true, the affordance leaked onto the arm where a retry is guaranteed to fail.: expected true to be false`. ⭐ This is the row that closes E's other direction — E alone would let the permanent arm's Retry suppression be traded away silently. Restored → green. |
| E-roster | delete `KEY_SCOPE_CHECK_UNREADABLE` from `SubmitStep.tsx`'s `KNOWN_FINALIZE_CODES` (T-153.6-E3, the invisible ship) | `wizardErrors.invariant.test.ts` | ✅ observed (153.6-06) | `AssertionError: finalize-wizard emits codes that KNOWN_FINALIZE_CODES does not admit… The step rejects an unrecognised code and renders UNKNOWN — silently, with nothing else reddening.: expected [ 'KEY_SCOPE_CHECK_UNREADABLE' ] to deeply equal []`. ⚠️ Under this mutation a "does a Retry render?" oracle would have stayed GREEN — UNKNOWN's copy is recoverable — which is why `route.test.ts` pins the rendered TITLE. Restored → green. |
| CR-01-check | drop `api_keys_attested_venue_matches_exchange`, then repeat the SAME privileged divergent INSERT | the INSERT must stop being refused | ✅ observed 2026-08-12 (plan 07, local PG16) | With the CHECK: `ERROR: new row for relation "api_keys" violates check constraint "api_keys_attested_venue_matches_exchange"` / `DETAIL: Failing row contains (…, binance, divergent, …, mt5)`. `ALTER TABLE … DROP CONSTRAINT` → the identical write returns `INSERT 0 1` and reads back `exchange=binance, attested_venue=mt5, moved_together=f` — **the selectively forged attestation**: a probe skip with no forged routing label to pay for it. ⚠️ run as the PRIVILEGED role deliberately; as `authenticated` the scrub trigger NULLs the column first and the CHECK is never reached, so a "refused" there would be the trigger's doing |
| CR-01-verify-g | rename the constraint in the ADD so the post-verify's lookup misses it | the migration must ABORT | ✅ observed 2026-08-12 (plan 07, local PG16) | `ERROR: Migration 20260811210000 failed: the api_keys_attested_venue_matches_exchange CHECK is absent, so a writer could persist an attested_venue that disagrees with exchange — a forgeable attestation with no forged routing label to pay for it. Rolling back.` Restored → COMMIT |
| CR-01-5d | re-own `create_wizard_strategy` to a role OUTSIDE the scrub trigger's allowlist (WR-02's scenario, reached at test time) | SQL test assertion 5d | ✅ observed 2026-08-12 (plan 07, local PG16) | `ERROR: CR-01/153.6 REGRESSION (5d): a wizard RPC minted a key with attested_venue NULL — the scrub trigger is firing for the RPC's owner too. Every wizard key would be probed, and for MT5 that is a permanent KEY_SCOPE_CHECK_UNAVAILABLE with no remedy.` ⚠️ **the blunt mutation does NOT reach 5d**: making the trigger scrub for *everyone* is intercepted first by the pre-existing 5b anti-vacuity control (`TEST FAILED (5b): a privileged INSERT did not persist attested_venue (got <NULL>)`), so 5d had to be isolated by re-owning ONLY the function. An earlier attempt also red on 5d's own "could not be called" arm (`42501 permission denied for schema auth`) — that arm exists precisely to stop a broken call being read as a passing security claim |
| CR-01-5e | drop the coupling CHECK, leave both RPC bodies untouched | SQL test assertion 5e — and 5d must STAY GREEN | ✅ observed 2026-08-12 (plan 07, local PG16) | `ERROR: CR-01/153.6 REGRESSION (5e): a privileged writer persisted exchange=binance with attested_venue=mt5. The api_keys_attested_venue_matches_exchange CHECK is missing, so an attestation can be forged WITHOUT forging the routing label — the coupling 5d relies on is back to being an accident.` ⭐ **5d stayed GREEN in the same run** — today's two writers still agree — which is exactly why 5e is a separate row: 5d pins the writers' behaviour, 5e pins that the behaviour is ENFORCED rather than incidental |
| WR-01-cutoff | remove `AND created_at < current_setting('quantalyze.attest_backfill_cutoff')` from the backfill, then apply → client INSERT → RE-APPLY | the scrubbed row must be retro-attested | ✅ observed 2026-08-12 (plan 07, local PG16) | Bounded: re-run reports `UPDATE 0` and the row stays NULL. Unbounded: `UPDATE 1`, and the row comes back `attested_venue=deribit` — `ERROR: WR-01 REGRESSION: a re-run RETRO-ATTESTED a row the scrub trigger had deliberately left NULL`. ⭐ the post-verify printed **OK** in the same unbounded run, which is how this would have shipped unnoticed. ⚠️ first attempt used an `mt5` label and passed VACUOUSLY — the census pin aborted the re-run before the backfill ran; re-done with a non-exempt label so the cutoff is the only thing under test |
| WR-02-owner | `ALTER FUNCTION create_wizard_strategy … OWNER TO wizard_owner` before the apply (CREATE OR REPLACE preserves ownership) | the migration must ABORT | ✅ observed 2026-08-12 (plan 07, local PG16) | `ERROR: Migration 20260811210000 failed: create_wizard_strategy is owned by wizard_owner, which the api_keys_scrub_attested_venue trigger does not admit (allowed: postgres, service_role, supabase_admin). Every key the wizard mints would be scrubbed to NULL and every MT5 finalize would answer a permanent KEY_SCOPE_CHECK_UNAVAILABLE. Rolling back.` ⭐ MIG-02 judged the local fixture "structurally incapable" of catching this because everything is `postgres` there — re-owning the function makes it reachable |
| WR-02-allowlist | delete `'service_role'` from the scrub trigger's `current_user IN (...)` list, leaving the post-verify's array copy behind | the migration must ABORT | ✅ observed 2026-08-12 (plan 07, local PG16) | `ERROR: Migration 20260811210000 failed: the scrub trigger body does not name the role service_role, so the owner allowlist this post-verify checked is not the one actually enforced. Re-cut both together. Rolling back.` ⭐ without this loop the owner check would assert against a fiction — the two-copies-that-can-disagree shape this phase exists to close |
| MIG-01-mt5 | add ONE unmeasured `mt5` row before the apply | the pre-flight must ABORT | ✅ observed 2026-08-12 (plan 07, local PG16) | `ERROR: Migration 20260811210000 ABORT: probe-exempt census drift (found mt5=3, of which 2 carry the 2026-08-04 signature; pinned mt5=2, all of them signed). An unmeasured probe-exempt row would be backfilled into a probe SKIP on trust this census never established.` |
| MIG-01-total | add ONE `deribit` row before the apply (the total moves, the probe-exempt population does not) | the pre-flight must **NOT** abort — this is the regression being removed | ✅ observed 2026-08-12 (plan 07, local PG16) | Now: `NOTICE: … the probe-exempt population matches the pin (2 mt5 rows, all created 2026-08-04). Total api_keys observed 30 against a pinned 29 (delta 1) — reported, not enforced.` → `COMMIT`. Under the old strict-equality pin this aborted the PROD auto-apply of a security fix. Two-sided discriminator re-checked in the same session: mt5 rows deleted → lenient non-PROD NOTICE (`census signature absent … found total=27, mt5=0`), so a PROD apply still cannot reach the lenient branch |
| E-pin | change ONE of the two `EXPECTED_TABLE_SIZE` sites to 75 and leave the other at 74 | `-t "cannot silently diverge"` | ✅ observed (153.6-06) | Run as 75/74 after the re-cut (same divergence, same direction of proof): `AssertionError: The two EXPECTED_TABLE_SIZE pins disagree: 75 vs 74. Moving one and not the other is a silent half-fix — one scan keeps its shrink detection and the other one loses it… : expected 2 to be 1`. Restored → green. proves the two pins cannot drift apart |

*Rules:*
- **Observed means run.** Paste the failing assertion.
- **A skipped mutation is recorded as skipped, never as caught.**
- **Prefer the second member of a class** — the `C-default` and `E-pin` rows exist precisely because
  this phase's subject is instance-fixes masquerading as class-fixes.

---

## Oracle Independence

> The failure this catches: an assertion that reads its expected value out of the module under test,
> so it stays green no matter what that module does.

| Oracle | Reads its expectation from | Independent? |
|--------|---------------------------|--------------|
| C — the corrected budget invariant | the **branch table's governing failing-column figure**, written as a literal with its derivation in prose | ✅ required — must NOT call `connectAbortDeadlineMsFor` to compute what it then asserts about `connectAbortDeadlineMsFor` |
| A-guard | the ast of the source files, with hand-typed floors carrying a measurement date | ✅ — ⛔ **NEVER compute a floor as `len(derived) - 1`**, the idiom `test_mt5_abandon_roster.py` writes down explicitly |
| D | a row state constructed in the test, asserted against the PROBE's observable decision | ✅ |
| E | the two pinned table sizes, cross-checked against each other and against a meta-test that counts its own declarations | ✅ |

⛔ **The standing rule (MEMORY: money-math oracles):** pin ECONOMICS or INVARIANTS, never the
implementation's own formula. Three money bugs survived six review passes via self-referential
oracles. Cluster C is that rule applied to a timing budget, and cluster C's *existing* oracle is
what happens when it is ignored.

---

*Phase: 153.6-parity-the-fixes-that-only-landed-on-one-path*
*Created: 2026-08-11*
