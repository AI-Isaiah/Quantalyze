---
phase: 153.6-parity-the-fixes-that-only-landed-on-one-path
verified: 2026-08-13T01:35:00Z
status: passed_with_concerns
score: 24/25 must-haves verified (1 not observable from the codebase — PROD apply outcome)
overrides_applied: 1
retroactive: true
verified_against: "main @ 24a6392a (phase shipped at 54a0d26d / v0.58.0.0 / PR #675); read from branch docs/phase-156-connect-refactor, which is main plus docs-only commits"
overrides:
  - must_have: "supabase/migrations/20260810120000_lock_api_keys_exchange_column.sql — corrected header (D-04): the 'can never be corrected back' falsehood removed; residual argument rewritten"
    reason: "Deliberately reversed in-phase by plan 153.6-09 (migration re-audit M2-05). 20260810120000 is APPLIED; editing an applied migration's header makes a later reader unable to tell an amended file from the one the database actually saw. The threat-model correction was relocated into the SUPERSEDING migration 20260811210000 (lines 54-72), where it paraphrases the falsehood rather than re-greps it. Verified: `git diff origin/main -- 20260810120000` is empty, and 20260811210000 carries the full correction plus the reason for the relocation. Superior practice to the planned artifact; intent achieved."
    accepted_by: "gsd-verifier (retroactive; documented decision in 153.6-09-SUMMARY.md + MIGRATION-REVIEW-3)"
    accepted_at: "2026-08-13T01:35:00Z"
warnings:
  - id: W-153.6-1
    concern: "Cluster A closed the parity of the A1 defect but NOT the accusation surface that made A1 harmful. `classify_mt5_login_error` (services/mt5_validation.py:271) code-gates only `-10004` ahead of `_WRONG_SERVER_TOKENS`; `-10005 'IPC timeout'` still matches the token `ipc` and classifies `wrong_server`. On 2026-08-12 — the day after this phase shipped — a wedged MT5 gateway terminal returned -10005 and the wizard told a user 'We could not find that broker server.' for a byte-for-byte CORRECT server string. That is A1's exact user-visible failure mode (an operator/infra fault rendered as a user-blaming broker-server accusation) reached through a different door. A fix exists on the unmerged branch `fix/mt5-ipc-timeout-misclassified` (adds `_IPC_TRANSPORT_CODES = (-10004, -10005)`)."
    severity: warning
    kind: class-not-fully-closed
    blocking: false
    scope_note: "services/mt5_validation.py is NOT in this phase's `Owns` list, the -10005 case was not among the nine code-review findings 153.6 was chartered against, and the token/code tables carry explicit `[ASSUMED] pending the live spike` markers owned by Phase 155. Out of declared scope — but recorded here because the phase's own bar is class closure, and the class recurred in production within 24h."
  - id: W-153.6-2
    concern: "`Mt5Client._timed`'s docstring (services/mt5_client.py:768-769) still opens 'emitting its `stage` + `duration_ms` event on BOTH outcomes (153.3 / D-32)'. Since B3 that headline is false for `Mt5SessionAbandoned`, which the body deliberately does not emit for. The carve-out is documented at length inside the arm but never in the docstring, so a reader of the contract alone concludes the D-32 population includes every failure. This is 153.6-REVIEW.md IN-01, which was not closed."
    severity: warning
    kind: documentation
    blocking: false
  - id: W-153.6-3
    concern: "The falsehood corrected by the override above is still greppable in the applied file. `20260810120000_lock_api_keys_exchange_column.sql:86-88` still reads '...with UPDATE revoked the mislabelled row can never be corrected back', and nothing inside that file points at 20260811210000. A reader who opens the older migration alone reads a threat model this phase measured as false. The correction is discoverable only from the newer file."
    severity: warning
    kind: documentation
    blocking: false
  - id: W-153.6-4
    concern: "PARITY-04 ships remedy (b) — the `api_keys_attested_venue_matches_exchange` CHECK — with remedy (a) (the connect-flow refactor) deliberately deferred. This is an ACCEPTED RESIDUAL, not a failure, and is now owned by Phase 156. Recorded as a warning only so the precondition travels with the verdict: the CHECK makes the forgery self-defeating rather than impossible, and that price is payable ONLY while every probe-exempt venue is unsyncable (mt5 is the sole member of `scopeProbeSupported: false`). Adding a SYNCABLE venue to that set makes the forgery free and requires remedy (a) FIRST."
    severity: warning
    kind: accepted-residual
    blocking: false
deferred:
  - truth: "`_WRONG_SERVER_TOKENS` / `_AUTH_TOKENS` / the IPC code-gate set are complete and correct against a real MT5 terminal"
    addressed_in: "Phase 155 (MT5-VERIFY)"
    evidence: "services/mt5_validation.py:66-83 marks both token tables `[ASSUMED] token table pending the live spike`, and the sibling `_TRADE_RETCODE_TRADE_DISABLED` note names 'Owner: **Phase 155 (MT5-VERIFY)**, to be resolved against a live master AND investor login.' Phase 155's ROADMAP goal is the live-hardware gate: 'proven true against the terminal's own figures on a trading day.' ⚠️ The -10005 instance (W-153.6-1) has a ready fix on `fix/mt5-ipc-timeout-misclassified` and should not wait for the phase."
  - truth: "PARITY-04 remedy (a): the venue `attested_venue` records is one the SERVER validated, not one the caller passed to an `authenticated`-invokable RPC"
    addressed_in: "Phase 156 (CONNECT-REFACTOR)"
    evidence: "ROADMAP `### Phase 156: CONNECT-REFACTOR — the venue the server validated is the venue the server writes`; REQUIREMENTS.md CONNECT-01..05 explicitly 'Closes the PARITY-04 `threat_flag: deferred-control` shipped live on PROD in PR #675 (CR-01 remedy (a))'."
  - truth: "The `strategies.asset_class` annualization stamp reads the attestation rather than the forgeable `exchange` label"
    addressed_in: "flagged in-code as OQ-2, unscheduled"
    evidence: "finalize-wizard/route.ts:1274-1283 documents the deliberate scope boundary and states the swap is now a one-identifier change. Residual is self-targeted (√365 vs √252 on the forger's OWN strategy), not a security control."
human_verification:
  - test: "Confirm the 2026-08-11 PROD census pin held when 20260811210000 auto-applied to `khslejtfbuezsmvmtsdn` on merge of PR #675 — i.e. the apply log shows 'pre-flight OK: the probe-exempt population matches the pin (2 mt5 rows...)' and NOT the 'ABORT: probe-exempt census drift' exception."
    expected: "Migration applied; every pre-2026-08-11 PROD api_keys row carries attested_venue = its exchange, and the 2 mt5 rows are probe-exempt by ATTESTATION."
    why_human: "PROD apply outcome is not observable from the repository. TEST (`qmnijlgmdhviwzwfyzlc`) is confirmed applied — CI observed assertions 5a-5e green on main — but the PROD branch of the census block is the one that can abort, and only the PROD apply log or a PROD query shows which branch ran."
---

# Phase 153.6: PARITY — the fixes that only landed on one path — Verification Report

**Phase Goal:** Every fix the 153 span made on one path exists on its twin, and no broad `except` re-absorbs a refusal the fence was built to surface.
**Verified:** 2026-08-13T01:35:00Z (retroactive — the phase shipped 2026-08-12 at `54a0d26d` without a VERIFICATION.md)
**Status:** passed_with_concerns (4 warnings, none blocking)
**Re-verification:** No — initial verification, performed retroactively

---

## Verification method

Nothing in the nine SUMMARY files was accepted as evidence. Every load-bearing claim below was traced to source on `main`, and **eleven of them were falsified by mutating the shipped source and observing the red**, then restoring. The measured mutations are tabulated under *Falsification*. `git status --porcelain` confirms zero source files modified at the end of this run (the only working-tree diffs are the concurrent Phase 156 planning session's `.planning/` edits, which are not mine).

Where a claim could not be falsified by mutation — the SQL round trip, which needs a live Postgres — the evidence is the **CI log of the shipped commit's own run**, not the SUMMARY's assertion that it passes.

---

## Goal Achievement

### The phase's own bar: was the CLASS closed, or were N instances patched?

The charter is explicit: *"do not close these as N point patches; the question each cluster must answer is **what makes the two paths unable to diverge again**."* Each cluster is scored against that bar first.

| Cluster | The mechanism that makes divergence impossible | Verdict |
|---|---|---|
| **A** (A1/A2/A3) | **Extraction, not copy.** `analytics-service/services/mt5_probe.py` (271 lines) holds ONE `read_terminal` / `run_probe` / `assert_expected_login` body. `routers/exchange.py:54,659` and `services/ingestion/mt5.py:70-73,243` each retain only a thin forwarder that calls `run_probe`. `grep "terminal_info()"` across both callers returns **zero** — the verb exists only in the shared module. Backed by `test_mt5_probe_parity_roster.py` (610 lines, ast over three files, PER-FILE floors 6/7/3 with no total, plus `PROBE_VERB_UNITS_IN_HOME_FLOOR = 2` and seven self-tests). **CLASS CLOSED** — with the caveat in W-153.6-1. |
| **B1** | Same edit as A2, in the shared module (`mt5_probe.py:159-181`): the `except Mt5SessionAbandoned: raise` arm sits **above** both sibling arms. There is one body, so there is one ordering. **CLASS CLOSED.** |
| **B2** | Single-site by nature (the stage-1 connect `try` had no arm at all). Fixed at `exchange.py:543-591`, byte-mirroring the stage-2 D-40 disposition. Not a parity defect. **INSTANCE CLOSED, correctly.** |
| **B3** | **Fixed at the sink, not the call site.** The suppression lives in `Mt5Client._timed` (`mt5_client.py:793-818`), so it holds for **every** stage and for whatever fence someone adds inside a bracket next — explicitly rather than restructuring `restart()`. **CLASS CLOSED.** |
| **C** | **Arm-agnostic body.** `BREAKER_STORE_WORST_CASE_FAILING_MS = 25_500` is added inside `connectAbortDeadlineMsFor`'s single return expression, so both venue arms move together (the shortfall was the identical 10 500 ms on each). AND the oracle now quantifies over `BREAKER_STATES` and asserts `governing.state === "failing"` — so selecting a column can never again be silent. **CLASS CLOSED, both halves.** |
| **D** | **Authority moved, not locked down.** The gate stops reading a client-writable column at all rather than trying to make that column unwritable (D-02: no new GRANT/REVOKE). Both gate arms — single-key and composite per-member — read `attested_venue` with **no `?? exchange` fallback anywhere on the gate path**. **CLASS CLOSED for the shipped scope**; remedy (a) is an accepted, documented residual (W-153.6-4) now owned by Phase 156. |
| **E** | Single-site by nature. The two conditions no longer share a code; the pin inventory moved exactly as specified. **INSTANCE CLOSED, correctly.** |

⭐ **The charter's sharpest test — "a cluster that fixed both paths but left nothing asserting they agree has not met the bar" — is passed.** `test_mt5_validate_parity.py` drives each of A1/A2/A3 through **both** paths in one test (`test_order_check_short_circuit_on_both_paths`, `test_terminal_materialize_failure_yields_no_signal_on_both_paths`, `test_mt5_gateway_misconfigured_operator_fault_on_both_paths`), and each of those was **measured red** under the corresponding mutation.

---

### Observable Truths

Merged from the phase goal, PARITY-01..05 in REQUIREMENTS.md, and all six PLAN frontmatters. ROADMAP declares no separate `success_criteria` block for 153.6, so the contract is goal + requirements + plan must-haves. No PLAN reduced scope.

#### Plan 01 — Cluster A + B1 (the shared probe body)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | An `order_check`-refusing terminal classifies as an OPERATOR fault on BOTH paths — never `MT5_WRONG_SERVER`, never a 400 blaming the user's broker server (A1) | ✓ VERIFIED | `mt5_probe.py:239-262` — `if classify_trade_capability(info, {}, terminal) == "undetermined":` short-circuits before `client.order_check(...)`, and the predicate is the SEAM ITSELF, not a fourth copy of the shape test. **Falsified M-A1**: neutering it reds `test_order_check_short_circuit_on_both_paths` AND the router-only `test_a_conclusive_terminal_verdict_is_not_pre_empted_by_an_erroring_probe`. |
| 2 | A netref materialization failure yields `None` on BOTH paths, logging the exception CLASS only — never the message (A2) | ✓ VERIFIED | `mt5_probe.py:183-200` — the broad arm logs `type(terminal_exc).__name__`. **Falsified M-A2**: swapping to `str(terminal_exc)` reds two tests, and the failure output shows the mutated log line leaking `rpyc: eval failed for login(123456, investor-pw, Broker-Demo)` — the oracle catches the actual credential disclosure, not merely a format change. |
| 3 | The worker stamps the operator fault `("permanent", <curated>)`; it is never retried, and the copy names no credential and no `_WRONG_SERVER`/`_AUTH` token (A3) | ✓ VERIFIED | `mt5_probe.py:96-121` defines `Mt5GatewayMisconfigured` with `MT5_GATEWAY_MISCONFIGURED_DETAIL` as its **default** message (so no site can raise it carrying raw remote text by omission); `ingestion/mt5.py:343` raises it where the bare `RuntimeError` was; `job_worker.py:668-669` maps it to `("permanent", MT5_GATEWAY_MISCONFIGURED_DETAIL)`. **Falsified M-A3**: reverting to `raise RuntimeError(...)` reds `test_mt5_gateway_misconfigured_operator_fault_on_both_paths`. |
| 4 | `Mt5SessionAbandoned` escapes the shared terminal read unabsorbed, on both paths (B1) | ✓ VERIFIED | `mt5_probe.py:159-181` — bare `raise`, above both sibling arms, with nothing else in the arm. **Falsified M-B1**: deleting it reds two tests, and the failure output shows the fence arriving as `error_class=Mt5SessionAbandoned` inside a "terminal_info failed to materialize" log — precisely the false operator-facing premise B1 names. |

#### Plan 02 — Cluster C (the budget and its oracle)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 5 | The browser is the LAST party to give up on both connect routes in EVERY breaker state — 190 500 > 175 500 serialized AND 100 500 > 85 500 default | ✓ VERIFIED | `validate-budget.ts:246-256` — `validateBudgetMsFor + ENCRYPT_KEY_BUDGET_MS + BREAKER_STORE_WORST_CASE_FAILING_MS + WAIT_ABORT_GRACE_MS`, one arm-agnostic body. `seam-budgets.invariant.test.ts:1070-1189` asserts it per route × per arm × per state. 85/85 green. |
| 6 | CR-01's "nothing was saved" lie is unreachable again | ✓ VERIFIED | The abort now lands after the route's failing-state worst case by exactly the grace on each arm; derivation written out at `validate-budget.ts:153-192`. |
| 7 | `158_500` appears nowhere as an oracle (repo grep 1 → 0) | ✓ VERIFIED | `grep -rn "158_500\|158500" src/ analytics-service/ supabase/` → only three hits, all inside bybit VCR cassettes (`"postOnlyMaxLimitOrderSize": "8158500"`). Zero in first-party source. |
| 8 | The oracle reds when the deadline is sized on the closed column — **including structurally** | ✓ VERIFIED | **Falsified M-C1 and M-C2** (below). The economic assertion reds on the value mutation; the `.toEqual(["closed","open","failing"])` floor reds on the structural narrowing. Plus a `governing.state === "failing"` assertion and two hand-typed anti-vacuity floors (`BROWSER_ABORTED_CONNECT_ROUTES.length === 2`, `CONNECT_VENUE_ARMS.length === 2`) and a `throw` (not an assert) on a missing branch label, because `undefined` compared with `toBeGreaterThan` passes vacuously. **The oracle can fail. This is the span's repeated defect class, and it is closed here.** |

#### Plan 03 — Cluster D, the database half

| # | Truth | Status | Evidence |
|---|---|---|---|
| 9 | A client INSERT cannot persist a non-NULL `attested_venue` — the BEFORE INSERT trigger scrubs it | ✓ VERIFIED | `20260811210000:515-552` — `scrub_client_supplied_attested_venue()`, `SECURITY INVOKER` **stated explicitly** with the DEFINER-makes-it-a-no-op trap documented (the `prevent_profile_role_change` precedent). Scrubs rather than raises, so the two live client INSERT paths stay open. **CI-observed on main:** `Assertion 5c OK: DELETE + re-INSERT with a forged attested_venue SUCCEEDS and stores NULL`. |
| 10 | Both wizard RPCs stamp `attested_venue = p_exchange` and RETAIN their advisory-lock idempotency fences | ✓ VERIFIED | `20260811210000:387` and `:492-495` widen the INSERT column lists; `pg_advisory_xact_lock` survives at `:348` and `:457`, and post-verify `:818-822` asserts both bodies still contain `pg_advisory_xact_lock` AND their respective `wizdraft:` / `wizcomposite:` key prefixes — so a future re-base that drops the fence fails the migration's own self-verify. |
| 11 | All pre-phase PROD rows carry `attested_venue` = their census-time exchange; the 2 mt5 rows are probe-exempt via ATTESTATION | ? UNCERTAIN | The mechanism is present and correct: dated-cutoff backfill at `:697-701` bounded by a transaction-local GUC that post-verify (a) **re-reads rather than re-types** (a second literal could drift earlier and pass vacuously). The PROD *outcome* is not observable from the repository — see `human_verification` above. TEST is confirmed applied. |
| 12 | The migration ABORTS loudly on PROD-census drift yet applies cleanly on TEST | ✓ VERIFIED | `:644-673` — the discriminator is the census SIGNATURE (`v_sig >= 1`, mt5 rows on a pinned creation date), tested FIRST, so a PROD apply cannot fall through to the lenient branch. The **total** is reported as a delta and never enforced (MIG-01: a live user-mutable count would have aborted a security fix's PROD auto-apply over one key connected between census and merge). `c_pin_dates` is a `DATE[]`, so the pin is re-cuttable (round-3 audit fix). |
| 13 | Assertions 2/3 still gate on the `20260810120000` comment substring (no silent SKIP), and assertion 4 (DELETE survives) still passes | ✓ VERIFIED | `:755-757` re-stamps the `exchange` comment preserving the marker, and **section 7 was moved to precede the post-verify block** — measured on a local PG16 fixture 2026-08-12 that in the old order a marker-dropping re-stamp COMMITTED while post-verify (d) reported success. **CI-observed on main:** `Assertion 5a OK: the 20260810120000 gate marker survived the re-stamp, so assertions 2 and 3 really ran.` |

#### Plan 04 — Cluster D, the route half

| # | Truth | Status | Evidence |
|---|---|---|---|
| 14 | `exchange='mt5'` + `attested_venue='binance'` is PROBED — the forged label no longer buys a skip | ✓ VERIFIED | `route.ts:1221-1257`, `:1333`. **Falsified M-D1.** |
| 15 | `attested_venue` NULL is PROBED — fail-toward, with NO fallback to `exchange` anywhere on the gate path | ✓ VERIFIED | `route.ts:1252-1257` (`⛔ NEVER `?? apiKeyExchange``) and `:1627-1637` (per-member, same rule). **Falsified M-D1**: restoring the fallback on BOTH arms reds exactly three tests, one single-key and two composite. |
| 16 | `attested_venue='mt5'` still skips — only the AUTHORITY moved, not the capability | ✓ VERIFIED | `route.test.ts:4170` and `:4249` — the counterweight, asserted on both arms. |
| 17 | Both arms read the attestation; the composite embed is validated with `safeParse`, never cast | ✓ VERIFIED | `route.ts:177` (`attested_venue: z.string().nullable().optional()` in `compositeMemberRowsSchema`), `:1434` widens the embed select, `:1622-1638` walks members. `route.test.ts:4226` proves a member with NO attestation field at all still parses and is probed. |

#### Plan 05 — Cluster B remainder + the parity roster

| # | Truth | Status | Evidence |
|---|---|---|---|
| 18 | A stage-1 connect `Mt5SessionAbandoned` answers 424 `NETWORK_UNAVAILABLE` — never the 503 that counts toward `breaker:mt5-gateway` | ✓ VERIFIED | `exchange.py:543-591`, positioned below `asyncio.TimeoutError` and above `except Exception as connect_err`. **Falsified M-B2**: deleting the arm reds `test_a_connect_stage_abandon_is_transient_and_never_the_counting_503`, and the failure output shows the fence falling through to the broad arm's `MT5 gateway connect failed (server/bridge)` — the breaker-counting 503. |
| 19 | No `mt5.stage` event ever carries `error_class='Mt5SessionAbandoned'` — at the sink, for EVERY stage | ✓ VERIFIED | `mt5_client.py:793-818` — bare `raise`, ahead of the `except BaseException` emit arm. Fence-type ONLY, with the safety argument written out (the type is raised at exactly four sites, all our own fences). **Falsified M-B3.** |
| 20 | A REAL stage failure still emits `ok=False` (the suppression is fence-only) | ✓ VERIFIED | The positive control is in the SAME test that M-B3 reds — `test_a_fence_refusal_emits_no_stage_event_while_a_real_failure_still_does`. Widening the arm to `Exception` would delete the FAILURE half of the D-32 population and this test reds on it. |
| 21 | No hand-written second copy of the probe body exists across the three files, and one collapsed scan cannot be masked by two healthy ones | ✓ VERIFIED | `test_mt5_probe_parity_roster.py` — `UNIT_FLOORS` per file (no total), `PROBE_VERB_UNITS_IN_HOME_FLOOR = 2`, `DELEGATION_FLOORS`, files read BY PATH via `ast.parse` (never `inspect.getsource`, which would pollute `sys.modules`), plus seven self-tests including "a copy and a delegation are told apart". **Falsified M-R1**: appending a nine-line hand-written duplicate probe body to `ingestion/mt5.py` reds `test_no_hand_written_second_copy_of_the_probe_body_exists`. |

#### Plan 06 — Cluster E (retry affordance)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 22 | A probe parse miss renders honest transient copy WITH a Retry control | ✓ VERIFIED | `route.ts:935-944` emits `KEY_SCOPE_CHECK_UNREADABLE`; `wizardErrors.ts:1083-1105` gives it `actions: ["clear_and_retry", "request_call"]`, so `buildEnvelope` derives `recoverable: true`. **Falsified M-E1**: removing `clear_and_retry` reds `the parse-miss code derives recoverable — the Retry control renders`. |
| 23 | The genuinely permanent probe-status arm keeps `KEY_SCOPE_CHECK_UNAVAILABLE` with NO Retry — and it was never reverted to `KEY_NETWORK_TIMEOUT` | ✓ VERIFIED | `route.ts:850` untouched; `wizardErrors.ts:1063-1081` holds only `["request_call"]`. The two entries are deliberately adjacent so the pair reads together. `route.ts:928-929` states the `⛔ never back to KEY_NETWORK_TIMEOUT` rule in-code. |
| 24 | The pin inventory moved exactly: `EXPECTED_TABLE_SIZE` re-cut (never deleted), `EXPECTED_FINALIZE_REJECTION_SITES` UNMOVED at 32 | ✓ VERIFIED | `EXPECTED_FINALIZE_REJECTION_SITES = 32` at `wizardErrors.invariant.test.ts:534`; `expectedSites: 29` at `:270`. Both **unmoved** — the D-18 proof this was a fix, not an addition. `EXPECTED_TABLE_SIZE` now reads **76** at both sites, not 75: `git log -S` shows 153.6 (`54a0d26d`) set it to 75 and **Phase 154 (`24a6392a`) moved it to 76**. Legitimate downstream re-cut, not a 153.6 defect. The `.toBe(2)` declaration-count meta-test at `:3114` is intact. |
| 25 | The new code renders its OWN copy, not UNKNOWN's | ✓ VERIFIED | `SubmitStep.tsx:345` adds the member to `KNOWN_FINALIZE_CODES` in the same commit the route emits it; the copy entry has its own `title` ("We could not read the permission check's answer.") distinct from UNKNOWN's, and the test asserts on the rendered TITLE — because UNKNOWN's copy is itself recoverable and would fake the Retry for the wrong reason. |

**Score: 24/25 verified, 1 uncertain (not observable from the codebase).**

---

## Falsification (measured, not predicted)

Eleven mutations. Each was applied to shipped source, the targeted suite run, the source restored, and `git status --porcelain` confirmed clean.

| # | Mutation | Site | Result |
|---|---|---|---|
| M-A1 | Terminal short-circuit disabled (`if False:`) | `mt5_probe.py:239` | **RED 3** — `..._short_circuits_order_check_on_an_undetermined_terminal`, `..._on_both_paths`, `test_a_conclusive_terminal_verdict_is_not_pre_empted_by_an_erroring_probe` |
| M-A2 | Broad arm logs `str(exc)` instead of the class | `mt5_probe.py:198` | **RED 2** — and the failure output leaked `investor-pw` into the log, proving the oracle sees the disclosure, not the format |
| M-A3 | `Mt5GatewayMisconfigured` → bare `RuntimeError` | `ingestion/mt5.py:343` | **RED 1** — `test_mt5_gateway_misconfigured_operator_fault_on_both_paths` |
| M-B1 | `except Mt5SessionAbandoned: raise` deleted from `read_terminal` | `mt5_probe.py:159` | **RED 2** — output shows the fence absorbed into the false "failed to materialize" premise |
| M-B2 | Connect-stage abandon arm deleted | `exchange.py:543` | **RED 1** — `test_a_connect_stage_abandon_is_transient_and_never_the_counting_503`; fence falls to the breaker-counting 503 |
| M-B3 | `_timed` fence suppression deleted | `mt5_client.py:793` | **RED 1** — `test_a_fence_refusal_emits_no_stage_event_while_a_real_failure_still_does` |
| M-R1 | Nine-line hand-written second copy of the probe body appended | `ingestion/mt5.py` | **RED 1** — roster: "a HAND-WRITTEN copy of the MT5 probe body exists outside services/mt5_probe.py" |
| M-C1 | `BREAKER_STORE_WORST_CASE_FAILING_MS` 25_500 → 8_500 (the closed column) | `validate-budget.ts:192` | **RED 1** — "expected 173500 to be greater than 175500". **The oracle CAN fail.** |
| M-C2 | Structural: quantification narrowed to `["closed"]` | `seam-budgets.invariant.test.ts:1103` | **RED 1** — the states-covered floor: "expected [ 'closed' ] to deeply equal [ 'closed', 'open', 'failing' ]" |
| M-D1 | `?? exchange` fallback restored on BOTH gate arms | `finalize-wizard/route.ts:1254`, `:1635` | **RED 3** — one single-key, two composite |
| M-E1 | `clear_and_retry` removed from the new code's actions | `wizardErrors.ts:1104` | **RED 1** — the Retry control assertion |

---

## Behavioural Spot-Checks

| Check | Command | Result | Status |
|---|---|---|---|
| Python parity + roster suites | `cd analytics-service && python3 -m pytest tests/test_mt5_probe_parity_roster.py tests/test_mt5_validate_parity.py -q` | 21 passed | ✓ PASS |
| Full MT5 python surface | `... -q` over `test_mt5_validate` / `_validate_parity` / `_client_contract` / `test_job_worker` / `_probe_parity_roster` | 298 passed, 1 skipped | ✓ PASS |
| mypy --strict on the touched Python | `python3 -m mypy --strict services/mt5_probe.py services/ingestion/mt5.py routers/exchange.py services/job_worker.py` | `Success: no issues found in 4 source files` | ✓ PASS |
| Seam budget invariant | `npx vitest run src/lib/seam-budgets.invariant.test.ts` | 85 passed | ✓ PASS |
| Cluster D + E frontend | `npx vitest run finalize-wizard/route.test.ts wizardErrors.test.ts wizardErrors.invariant.test.ts seam-constants.pin.test.ts closed-sets.sfox-flag.test.ts` | 449 passed | ✓ PASS |
| SQL round trip (assertion 5) | CI `sql-tests` job, run 31609751159 on `main` | `5a OK` / `5b OK` / `5c OK` / `5d OK` / `5e OK` — all five NOTICEs present, none SKIPPED | ✓ PASS |

⭐ **The SQL result closes the ROADMAP's open condition on PARITY-04.** The requirements matrix records PARITY-04 as *"Goes fully ✅ when CI observes assertion 5 green."* CI has now observed it: assertions 5a–5e ARM (they are not the `SKIP (5)` branch) and all five pass on `main`. The shipped scope of PARITY-04 — remedy (b) — is confirmed live and guarded. Only remedy (a), the deferred control, remains open, and it is Phase 156's.

---

## Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `routers/exchange.py` | `services/mt5_probe.py` | `from services.mt5_probe import run_probe` (`:54`), delegated at `:659` | ✓ WIRED — local body deleted, only a thin forwarder remains |
| `services/ingestion/mt5.py` | `services/mt5_probe.py` | `from services.mt5_probe import (...)` (`:70-73`), delegated at `:243` | ✓ WIRED — same |
| `services/job_worker.py` | `Mt5GatewayMisconfigured` | `isinstance` arm at `:668` → `("permanent", MT5_GATEWAY_MISCONFIGURED_DETAIL)` | ✓ WIRED |
| `mt5_probe.py` | `routers/*` | **NO import edge** (D-07 direction invariant) | ✓ VERIFIED — the leaf invariant is stated in the module docstring and holds; `mt5_probe` imports only `services.mt5_client` and `services.mt5_validation` |
| `seam-budgets.invariant.test.ts` | `wizard/validate-budget.ts` | imports `connectAbortDeadlineMsFor`, quantifies over `BREAKER_STATES` via `branchWorstCases`/`worstBranch` | ✓ WIRED |
| `validate-budget.ts` | seam core | **NO import edge** (client-safe module) | ✓ VERIFIED |
| Both wizard RPCs | `api_keys.attested_venue` | INSERT column widened inside both `SECURITY DEFINER` bodies | ✓ WIRED |
| BEFORE INSERT trigger | `api_keys` | `SECURITY INVOKER` scrub function, `DROP TRIGGER IF EXISTS` + `CREATE TRIGGER` | ✓ WIRED — CI-proven live |
| `finalize-wizard/route.ts` (both arms) | `api_keys.attested_venue` | `select("exchange, attested_venue")` / embed `api_keys ( exchange, attested_venue )` | ✓ WIRED — one column added, zero extra round trips |
| `SubmitStep.tsx` `KNOWN_FINALIZE_CODES` | the route's emitted vocabulary | same-commit membership | ✓ WIRED |
| `COMMENT ON COLUMN api_keys.exchange` | the SQL test's arming gate | the preserved `20260810120000` substring | ✓ WIRED — CI 5a proves it survived the re-stamp |

---

## The critical-scoring question: does the shipped prose overclaim remedy (b)?

**No. This was checked specifically, and the shipped prose is more honest than the REVIEW's concern required.**

The 153.6 REVIEW's CR-01 established that the CHECK "is a property of the current venue set, not a control" — it holds only while every probe-exempt venue is unsyncable. The shipped artifacts state exactly that, in the two places a future engineer will actually read:

- **Migration header** `20260811210000:254-279`: *"⛔ IT DOES NOT MAKE THE VENUE SERVER-VALIDATED... What this CHECK buys is that the forgery is SELF-DEFEATING, not that it is impossible... That price is only payable because EVERY probe-exempt venue is currently UNSYNCABLE... ⛔ ADDING A **SYNCABLE** VENUE TO THAT SET MAKES THE BYPASS FREE... remedy (a) must land FIRST. This CHECK does not protect against that case, and no constraint on these two columns can."*
- **The permanent `COMMENT ON COLUMN attested_venue`** `:713-754` repeats the precondition verbatim, so it survives in the database even if the migration file is never read again, and it opens with *"⛔ READ THE GUARANTEE PRECISELY, it is narrower than 'server-validated': the two SECURITY DEFINER wizard RPCs do NOT validate this value."*
- The SQL test's own assertion 5d NOTICE says *"the RPC door accepts a forged venue (it validates nothing)"* — the residual is asserted, not hidden.

The claim in the shipped record matches the measured property. No overclaim finding. Recorded as W-153.6-4 only so the precondition travels with the verdict.

---

## Anti-Patterns Found

| Scope | Pattern | Result |
|---|---|---|
| All 14 phase-touched source/test/SQL files | `TBD` / `FIXME` / `XXX` debt markers | **0** — the debt-marker gate is clean |
| Same | `TODO` / `HACK` / `PLACEHOLDER` | Only `SIZE_MB_PLACEHOLDER` (a legitimate template-token identifier, pre-existing) and one reference to pre-existing phase-17 hoist markers. **No new debt.** |
| `mt5_probe.py`, `exchange.py`, `ingestion/mt5.py` | Broad `except Exception` | Present **by design** at exactly one site (`mt5_probe.py:183`), carrying `# noqa: BLE001` and a docstring paragraph justifying it, with the fence arm sitting above it. This is A2's fix, not an anti-pattern. |
| `validate-budget.ts` | Hand-typed twin constants without an agreement pin | None — `seam-constants.pin.test.ts:31,1196,1222` pins `BREAKER_STORE_WORST_CASE_FAILING_MS` on both sides |

---

## Concerns (detail)

### W-153.6-1 — Cluster A's class survived the phase, at a site the phase did not own

This is the most substantive finding and deserves plain language.

Cluster A1's stated harm is: *"`_WRONG_SERVER_TOKENS` (which carry 'terminal') turn an operator-side refusal into a 400 accusing the user their BROKER SERVER is wrong. This is the exact documented incident the router-side fix was written to prevent."*

153.6 closed the **probe-ordering** door into that accusation, on both paths, provably. It did not close the **classifier** door. `services/mt5_validation.py:271` code-gates exactly one MT5 error code ahead of the token table:

```python
if err.code == -10004:
    return "transient"
text = str(err).lower()
if any(tok in text for tok in _WRONG_SERVER_TOKENS):   # "server", "connect", "ipc", "network", "terminal", "not found"
    return "wrong_server"
```

`-10005 "IPC timeout"` — the timeout sibling of -10004 — contains `ipc`, so it falls to `wrong_server`. On 2026-08-12, the day after this phase shipped, a wedged MT5 gateway terminal returned -10005 and the wizard told a user *"We could not find that broker server."* for a correct server string. Same user-visible defect, different door.

**Calibration.** `services/mt5_validation.py` is not in the phase's `Owns` list; the -10005 case was not one of the nine chartered findings; the token tables carry `[ASSUMED] pending the live spike` with Phase 155 named as owner. On a strict scope reading, 153.6 is not accountable for it, and I do not score it as a gap. But the phase's own bar is class closure, and the honest statement is: **the class "an operator/infra fault rendered as a user-blaming broker-server accusation" is not closed; one of its two doors is.** The fix on `fix/mt5-ipc-timeout-misclassified` is small, additive and already written — it should not wait for Phase 155's live spike.

### W-153.6-2 — `_timed`'s docstring now describes a contract its body no longer keeps

`mt5_client.py:768-769` still opens *"emitting its `stage` + `duration_ms` event on BOTH outcomes (153.3 / D-32)"*. Since B3 that is false for `Mt5SessionAbandoned`. The carve-out is documented at length **inside** the arm (`:793-818`) but never in the docstring, so a reader of the contract concludes the D-32 population Phase 155 reads includes every failure. This is REVIEW IN-01, not closed. Documentation-only; per the standing stopping rule (block only on user-facing or data-integrity), it belongs in `TODOS.md`, not in a gate.

### W-153.6-3 — the corrected falsehood is still greppable in the applied migration

The override above accepts, correctly, that `20260810120000` must not be edited. The consequence is that `20260810120000:86-88` still asserts *"with UPDATE revoked the mislabelled row can never be corrected back"*, and nothing in that file points forward to `20260811210000` where the correction lives. Someone auditing the older migration alone reads a threat model this phase measured as false. A one-line forward pointer would need an edit to an applied file, so there may be no clean remedy — recorded so the trade-off is visible rather than accidental.

---

## Gaps Summary

**None blocking.** All five clusters are present in the codebase, substantive, wired, and — for eleven of the load-bearing mechanisms — proven falsifiable by mutation. The three cluster-A twins live in one shared body rather than two copies, guarded by an ast roster that reds on a hand-written duplicate. Cluster C's oracle, which was structurally unable to fail before this phase, was measured failing under both the value mutation and the structural narrowing. Cluster D's shipped half is confirmed live by CI's own SQL log, closing the ROADMAP's stated condition for advancing PARITY-04's shipped scope.

The one unverifiable item is the PROD apply outcome of the census-pinned backfill, which no amount of code reading can settle; it is routed to human verification.

The one finding worth a founder's attention is **W-153.6-1**: cluster A's user-visible defect class recurred in production within 24 hours of the phase shipping, through a door the phase did not own. That does not make 153.6's work wrong — it makes the class boundary the phase drew narrower than the harm it was chartered against.

---

_Verified: 2026-08-13T01:35:00Z (retroactive)_
_Verifier: Claude (gsd-verifier) — goal-backward, FORCE stance, 11 measured mutations, working tree restored clean_
