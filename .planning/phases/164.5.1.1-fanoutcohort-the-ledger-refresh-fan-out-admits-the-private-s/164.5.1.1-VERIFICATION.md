---
phase: "164.5.1.1"
slug: "fanoutcohort-the-ledger-refresh-fan-out-admits-the-private-s"
verified: 2026-09-20T07:07:18Z
status: gaps_found
score: 12/15 must-haves verified
verified_at_sha: 98c46a16fcce902540f664c6838994e22ab61e0c
drift_subjects:
  - supabase/migrations/20260917120000_ledger_fanout_admit_private.sql
  - supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql
  - supabase/tests/test_ledger_refresh_fanout.sql
  - supabase/tests/test_ledger_refresh_composite_arm.sql
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-runner-floors.test.ts
gaps:
  - truth: "T-164.5.1-09-07 was exercised using the mechanism plan 08 of the previous phase already recorded, sequenced after the natural tick, and the outcome is recorded as either a live exercise or a code-read (CTX-12, plan 04 must_haves)"
    status: failed
    reason: "164.5.1.1-PROD-SESSION.md § B says so itself, in its own words: 'This tick did not exercise that branch — both jobs SUCCEEDED — so the requirement is now reachable and still outstanding. Do not record it as met.' Neither of plan 04's two acceptable outcomes (a live exercise with its SQLSTATE, or a code-read of the per-candidate handler) is present in the session record. Plan 04's own automated verify command for this task was executed by this verifier and exits 1, with t0907=0."
    artifacts:
      - path: ".planning/phases/164.5.1.1-fanoutcohort-the-ledger-refresh-fan-out-admits-the-private-s/164.5.1.1-PROD-SESSION.md"
        issue: "§ B ends without a code-read confirmation of the per-candidate WARNING handler and without a live-exercise SQLSTATE; the requirement is explicitly left open rather than closed."
    missing:
      - "A code-read confirmation (or a genuine live exercise) of the per-candidate enqueue-failure handler, recorded in the session file using the plan's own required label."
  - truth: "The database marker query's output is recorded before any other statement of plan 04 task 3's session (acceptance criterion, plan 04)"
    status: failed
    reason: "Plan 04 task 3's own automated verify (`grep -ci which_database`) was executed by this verifier directly against 164.5.1.1-PROD-SESSION.md and returns 0 — the literal marker-query column header/output is not pasted for the task-3 (natural-tick) session. § A carries a marker line for the earlier apply session ('Marker first, every time: ⛔ PRODUCTION — real customer data.'), but § B (task 3's own session) has no marker read of its own."
    artifacts:
      - path: ".planning/phases/164.5.1.1-fanoutcohort-the-ledger-refresh-fan-out-admits-the-private-s/164.5.1.1-PROD-SESSION.md"
        issue: "§ B omits a fresh database-marker query output, which the plan requires as the first statement of that session."
    missing:
      - "The `shobj_description` marker query's pasted output at the start of § B, or an explicit note that it was taken and why it is not repeated."
  - truth: "FANOUT-COHORT-PRIVATE-01 is closed against the production reading, and the block it placed on Phase 164.5.1's verification is recorded as lifted (plan 04 task 3 acceptance criterion)"
    status: failed
    reason: "TODOS.md's FANOUT-COHORT-PRIVATE-01 entry, read at HEAD, still states verbatim: 'THE ENTRY IS NOT CLOSED, and the BLOCKS clause above is NOT lifted.' `git log`/`git show` confirm the only commit in this phase that touched TODOS.md is b24853fa (PR #807, plans 01-03's sibling-cohort bookings); no later commit amended this entry. The downstream block was in fact lifted by an unrelated mechanism — arm R, merged as PR #810 as part of Phase 164.5.1's own closure, not as part of this phase — but that supersession is not reflected anywhere in this phase's own artifacts."
    artifacts:
      - path: "TODOS.md"
        issue: "FANOUT-COHORT-PRIVATE-01 entry still reads as open/half-closed; no closure text referencing the natural tick's oracle movement exists."
    missing:
      - "A TODOS.md edit closing FANOUT-COHORT-PRIVATE-01 against the tick 11231 reading (or an explicit note that closure instead happened via arm R / PR #810 outside this phase, so a future reader is not misled)."
  - truth: "Plan 03's handed-over question — whether an owner-only strategy shares an api_key_id with a sibling strategy whose status IS in ALLOWED_STRATEGY_STATUSES — is answered while a PROD read is open (plan 04 task 3 acceptance criterion)"
    status: failed
    reason: "164.5.1.1-03-SUMMARY.md explicitly hands this one question to plan 04's PROD session, and Phase 164.5.1.2's own ROADMAP text independently confirms the same hand-off ('handed to Phase 164.5.1.1 plan 04's PROD session, where a read is already happening'). Grepping 164.5.1.1-PROD-SESSION.md for 'api_key' or 'sibling' returns zero hits — the question was never answered inside this phase."
    artifacts:
      - path: ".planning/phases/164.5.1.1-fanoutcohort-the-ledger-refresh-fan-out-admits-the-private-s/164.5.1.1-PROD-SESSION.md"
        issue: "No record of the api_key-sharing query or its answer."
    missing:
      - "The answer to plan 03's handed-over question, recorded as output."
  - truth: "164.5.1.1-04-SUMMARY.md exists, recording the marker output, reviewer verdicts, both workflow run ids, the body-hash comparison, the natural tick's readings, the T-164.5.1-09-07 outcome, and the answer to plan 03's handed-over question (plan 04 <output> contract)"
    status: failed
    reason: "The phase directory has no 164.5.1.1-04-SUMMARY.md. `git log --all -- '*164.5.1.1-04-SUMMARY.md*'` returns no commits — the file was never created."
    missing:
      - "164.5.1.1-04-SUMMARY.md, per plan 04's own <output> block."
deferred:
  - truth: "T-164.5.1-09-07 (the fan-out's all-candidates-failed branch) is exercised or otherwise closed"
    addressed_in: "Not a clean roadmap-level defer — see narrative below. TODOS.md books it as [FANOUT-FAILBRANCH-UNEXERCISED-01], owner 'Phase 164.5.1.2 FANOUTSIBLINGS', but the actual mitigation (arm R, PR #810) shipped as part of Phase 164.5.1's own closure, and Phase 164.5.1.2's ROADMAP text does not name this item. Reported as a gap above rather than silently deferred, per this repo's own disclosure standard."
    evidence: "TODOS.md § FANOUT-FAILBRANCH-UNEXERCISED-01 / 164.5.1-SECURITY.md T-164.5.1-09-07 row: 'disclosed SUBSTITUTION, not compliance ... Merged 9c6e53ce (PR #810) ... does NOT block: medium is below workflow.security_block_on (high).'"
overrides:
  - must_have: "T-164.5.1-09-07 was exercised using the mechanism plan 08 of the previous phase already recorded, sequenced after the natural tick, and the outcome is recorded as either a live exercise or a code-read (CTX-12, plan 04 must_haves)"
    reason: "Superseded by a stronger, permanent mitigation: mutation-gate arm R in supabase/tests/test_ledger_refresh_fanout.sql, merged as PR #810 during Phase 164.5.1's own closure. Disclosed there as a non-blocking SUBSTITUTION (medium, below workflow.security_block_on=high), never as compliance with plan 04's literal mechanism. A permanent gate re-proves the branch on every run where a one-off live exercise proves it once; the branch itself stayed unreached because the natural tick's two jobs both SUCCEEDED."
    accepted_by: founder
    accepted_at: "2026-09-20"
    recorded_in: ".planning/phases/164.5.1.1-fanoutcohort-the-ledger-refresh-fan-out-admits-the-private-s/164.5.1.1-04-SUMMARY.md"
# ⚠️ NO override is claimed for the § B database-marker gap. It is DISCLOSED, not closed:
# § B performed no writes (it observed a self-scheduled tick, then read three tables) and the
# marker rule is scoped to statements that write, with § A — the session that DID write — carrying
# its marker. So the miss is against plan 04's literal acceptance criterion with zero safety
# consequence. The founder accepted arm R above and has NOT been asked about this one, so it is
# surfaced for a decision rather than graded away. See PROD-SESSION.md § C-3.
---

# Phase 164.5.1.1: FANOUTCOHORT — Verification Report

**Phase Goal:** The ledger-refresh fan-out enqueues work for the strategies that actually exist in
production (the `private` lifecycle status is admitted), proven on PROD by the staleness oracle
moving rather than by a green cron row.

**Verified:** 2026-09-20T07:07:18Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Summary, stated plainly

**The core defect this phase exists to fix IS fixed, and the fix IS proven on production.** The
migration widening `enqueue_ledger_refresh_for_strategies()`'s lifecycle conjunct to
`{published, pending_review, private}` shipped, applied to PROD via the correct chain (three
reviewers → `apply-test` on shared TEST → the `Production` environment's human-reviewer gate), and
the **natural** `25 * * * *` tick (runid 11231, 2026-09-17 17:25Z) moved the staleness oracle from
6 stale strategies (min 23 / avg 52.50 / max 141 days) to 4 (min 23 / avg 56.75 / max 141), with two
`derive_broker_dailies` jobs actually created and completed. All four projections the phase staked
out in advance (venue cap, 4/5 mt5 strategies becoming candidates, the disconnected key staying
excluded for the stated cause, the deribit composite staying excluded by design) were checked
against PROD row data, not assumed, and all four held. Plans 01–03 (the migration, the gate arms,
and the sibling-cohort bookkeeping) are each fully delivered and independently corroborated by
`CHANGELOG.md`, `git log`, and direct inspection of the shipped SQL and gate files.

**What is not done is plan 04 task 3's own closing paperwork — a real, checkable gap, not a
rigor-manufacturing one.** Plan 04 (`autonomous: false`, a blocking-human checkpoint) declared, in
its own frontmatter, that `T-164.5.1-09-07` would be exercised (live or code-read) inside this
phase's PROD session, that the ledger entry `FANOUT-COHORT-PRIVATE-01` would be closed, and that
plan 03's handed-over `api_key`-sharing question would be answered — all "while a read is open."
None of the three happened, and this verifier confirmed each independently: by grep against
`164.5.1.1-PROD-SESSION.md`, by running plan 04's own automated verify command byte-for-byte (it
exits 1), and by `git log`/`git show` on `TODOS.md` (no closing commit exists). No
`164.5.1.1-04-SUMMARY.md` was ever written.

The underlying production risk (`T-164.5.1-09-07`, a Denial-of-Service threat in Phase 164.5.1's
own threat model) was in fact later mitigated — by a permanent mutation-gate ("arm R"), merged in
PR #810 as part of **Phase 164.5.1's own closure**, and explicitly disclosed there as a
"substitution, not compliance," below the `high` blocking threshold. That is a defensible outcome
on its own terms. But it did not happen through this phase's own plan 04, it is not reflected in
this phase's own `PROD-SESSION.md` or `TODOS.md` entry, and `164.5.1.1-PROD-SESSION.md` itself says,
verbatim, "Do not record it as met." Reporting this phase as fully `passed` would do exactly what
that sentence warns against.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The deployed body of `enqueue_ledger_refresh_for_strategies()` admits `private`: conjunct is exactly `{published, pending_review, private}` (CTX-01) | ✓ VERIFIED | `grep -n "status IN" supabase/migrations/20260917120000_ledger_fanout_admit_private.sql` and the canonical snapshot both show `AND s.status IN ('published', 'pending_review', 'private')`; confirmed live on PROD in `PROD-SESSION.md` § A (`exact conjunct ... true`, live body md5 `039c394ef...`). |
| 2 | The widening is a forward migration, one inline site, no applied migration edited, no shared constant (CTX-02) | ✓ VERIFIED | `git log` shows `20260917120000_ledger_fanout_admit_private.sql` is a new file; `20260911130000` unchanged in this phase's diff (confirmed by `git show b24853fa --stat`). |
| 3 | A stale, eligible `private` strategy IS ENQUEUED by the fan-out, proven by a gate arm rather than asserted (CTX-05) | ✓ VERIFIED | Arm P in `supabase/tests/test_ledger_refresh_fanout.sql` (RED-UNDER-M reverting the widened literal), neuter-verified per `164.5.1.1-01-SUMMARY.md`; independently confirmed enqueued in production (`PROD-SESSION.md` § B, two `derive_broker_dailies` rows). |
| 4 | The canonical snapshot under `supabase/schema/functions/` carries the new body; VAC-04 ack earned, not pasted | ✓ VERIFIED | `enqueue_ledger_refresh_for_strategies.sql` matches the migration's conjunct; VAC-04 read PROD post-apply and reported `0 mismatch ... acknowledged drift` (`PROD-SESSION.md` § A). |
| 5 | A cohort-agreement gate arm proves the CLASS: no status admitted by `strategies_status_check` is surfaced stale by the view and refused by the fan-out, except the two named exclusions (CTX-06) | ✓ VERIFIED | Arm Q in `test_ledger_refresh_fanout.sql`, deriving both sets from `pg_get_constraintdef`/comment-stripped `pg_get_functiondef`; neuter-verified against a genuinely new sixth status (`quarantined`) per `164.5.1.1-02-SUMMARY.md`. |
| 6 | `ARMS_FLOOR` equals the corpus's own freshly-printed count in both directions; no existing arm regressed (CTX-07, CTX-08) | ✓ VERIFIED | `PROD-SESSION.md` § R: `arms: 394/394/0 ... ✅ No defects`, run by the orchestrator four times. (Current `run.mjs` shows `ARMS_FLOOR = 413`, reflecting later phases 164.5.1.2–.4 raising it further — expected drift, not a regression of this phase's own delivery.) |
| 7 | Every production site of the two-value lifecycle literal is enumerated; exactly one widened here (CTX-03) | ✓ VERIFIED | `164.5.1.1-03-SUMMARY.md` table: three canonical bodies carry the literal, only `enqueue_ledger_refresh_for_strategies` widened; corroborated by `TODOS.md` § FANOUT-COHORT-SIBLING-COMPOSITE-01 / -POLL-01. |
| 8 | `ALLOWED_STRATEGY_STATUSES` is measured, not changed, with every reader named (CTX-04) | ✓ VERIFIED | `164.5.1.1-03-SUMMARY.md` § (b); `cron.py:148` unmodified in this phase's diff. |
| 9 | The composite gate's precondition is annotated, not widened, and states why it stays narrow | ✓ VERIFIED | `supabase/tests/test_ledger_refresh_composite_arm.sql:429-437` carries the dated 2026-09-17 annotation; the literal itself is unchanged. |
| 10 | Sibling-cohort findings are booked with an owner, a trigger and a destination phase | ✓ VERIFIED | `TODOS.md` § FANOUT-COHORT-SIBLING-COMPOSITE-01, -POLL-01, -SYNC-CONSTANT-01 each carry owner/trigger/destination (Phase 164.5.1.2). |
| 11 | The 141-day deribit composite is never reported as fixed by this phase, anywhere (CTX-10, backstop) | ✓ VERIFIED | This verifier executed plan 04's own automated negative-claim scan (`composite_claims_unnegated`) against all six produced artifacts named in the plan: `scanned_files=6`, `composite_claims_unnegated=0`, exit 0. |
| 12 | Three named reviewers ran against the shipped diff and every finding was dispositioned before any apply was requested | ✓ VERIFIED | `PROD-SESSION.md` § R: verdicts table (0/0/1-MEDIUM/0/2-must-fix-then-clear), founder `proceed` 2026-09-17; corroborated by the PR #807 commit history (`21104d57`, `0b9a4c7b`). |
| 13 | The migration reached PROD only via merge → `apply-test` → the PROD `apply` behind the human-reviewer gate, with run ids recorded; natural tick (not manual) used as the oracle; success = census moving, not job status (CTX-09) | ✓ VERIFIED | `PROD-SESSION.md` §§ A–B: run ids `35247369125` / `35243584543`, `apply-test` success, natural tick runid 11231; census 6→4, avg 52.50→56.75. Independently corroborated by `CHANGELOG.md` `[0.77.46.0]`/`[0.77.46.1]` and `git log` (`b24853fa`, `29f852f7`). |
| 14 | `T-164.5.1-09-07` exercised inside this phase's PROD session, outcome honestly labelled (live exercise or code-read), and `FANOUT-COHORT-PRIVATE-01` closed against that reading, with plan 03's handed-over question answered (CTX-12) | ✗ FAILED | See Gaps. `PROD-SESSION.md` § B: "still outstanding... Do not record it as met." `TODOS.md` entry unclosed. No answer to the api_key question. No `164.5.1.1-04-SUMMARY.md`. |
| 15 | The disconnected key and the deribit composite are both reported as still excluded, with reasons stated (CTX-10, CTX-11) | ✓ VERIFIED | `PROD-SESSION.md` § B items 3–4, both marked ✅ with the causal conjunct read at the row level. |

**Score:** 12/15 truths verified (0 present-but-behavior-unverified; 3 failed).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/20260917120000_ledger_fanout_admit_private.sql` | Forward migration, one edit | ✓ VERIFIED | Exists, 67153 bytes, conjunct widened, re-issued REVOKE and catalogue-only verify block present. |
| `supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql` | Regenerated snapshot | ✓ VERIFIED | Conjunct matches migration; regenerated 2026-09-18. |
| `supabase/tests/test_ledger_refresh_fanout.sql` | Arms P, Q, re-pointed apply list | ✓ VERIFIED | Both arms present with RED-UNDER-M twins targeting the new migration / the domain migration. |
| `scripts/mutation-runner/run.mjs` | `ARMS_FLOOR` raised | ✓ VERIFIED | Raised at the time (392→393→394 across plans 01/02); now 413 from later phases — expected drift. |
| `supabase/tests/test_ledger_refresh_composite_arm.sql` | Dated annotation, literal unmoved | ✓ VERIFIED | Annotation block present at `:429-437`. |
| `TODOS.md` | `FANOUT-COHORT-PRIVATE-01` closed; sibling findings booked | ⚠️ PARTIAL | Sibling findings (composite/poll/sync-constant) fully booked with owner/trigger/destination. `FANOUT-COHORT-PRIVATE-01` itself explicitly **not** closed — verbatim "NOT CLOSED... NOT lifted" at HEAD. |
| `.planning/.../164.5.1.1-PROD-SESSION.md` | Full plan-04 session record | ⚠️ PARTIAL | §§ R, A, B present and substantive for the apply and the tick. Missing: task-3 marker output, `T-164.5.1-09-07` outcome label, plan-03's handed question. |
| `.planning/.../164.5.1.1-04-SUMMARY.md` | Plan 04 summary | ✗ MISSING | Never created (`git log --all` confirms). |
| `CHANGELOG.md` | Unified entry, version bump | ✓ VERIFIED | `[0.77.46.0]`, `[0.77.46.1]` entries present, cross-checked against commits `b24853fa`/`29f852f7`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| New migration | Gate's RED-UNDER-SETUP apply list | Every edit-kind twin names `20260917120000` last | ✓ WIRED | Confirmed by grep of the apply-list JSON in `test_ledger_refresh_fanout.sql:293`. |
| Widened literal | Foreign-candidate precondition (fanout gate) | Both move together | ✓ WIRED | Same literal present in both the migration and the gate's arm G1/G2 context. |
| Migration body | `npm run schema:functions` → VAC-04 | Snapshot regeneration | ✓ WIRED | Snapshot content matches; VAC-04 read live PROD post-apply and reported acknowledged drift only. |
| Three reviewers | The apply request | Reviewers gate the request, not the merge | ✓ WIRED | `PROD-SESSION.md` § R precedes § A chronologically and by commit history. |
| `apply-test` green | PROD `apply`'s `needs` | Two independent gates | ✓ WIRED | `PROD-SESSION.md` § A run-id table shows `apply-test` → `plan` (Production, after founder approval) → `apply`, all `success`. |
| Natural tick | Staleness oracle → ledger closure → Phase 164.5.1's verification | Chain of four links | ⚠️ PARTIAL | First two links hold (tick → oracle movement, confirmed). The closure link (→ `FANOUT-COHORT-PRIVATE-01`) is broken inside this phase; Phase 164.5.1's own verification did close (10/10, `[0.77.48.0]`), but via a different, unrelated mechanism (arm R / PR #810), not via this phase's closure step. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Plan 04 task 3's own automated verify (marker/oracle/T-09-07/UUID-truncation) | `bash -c '...grep -ci which_database...'` (byte-for-byte from `164.5.1.1-04-PLAN.md`) run against `164.5.1.1-PROD-SESSION.md` | `marker=0 oracle=2 t0907=0 full_uuids=0`, exit 1 | ✗ FAIL |
| Plan 04 task 3's composite-non-repair scan | `bash -c '...composite_claims_unnegated...'` (byte-for-byte from `164.5.1.1-04-PLAN.md`) against the six produced artifacts | `scanned_files=6 composite_claims_unnegated=0`, exit 0 | ✓ PASS |
| Debt-marker scan (TBD/FIXME/XXX) on every file this phase modified | `grep -n -E "TBD|FIXME|XXX"` on the migration, snapshot, both gate files, `run.mjs` | 0 hits | ✓ PASS |
| Migration/snapshot conjunct match | `grep -n "status IN"` on both files | Identical `('published', 'pending_review', 'private')` | ✓ PASS |
| ROADMAP checkbox state vs SUMMARY existence | `ls` phase dir + `sed` the ROADMAP phase block | Plans 02/03/04 shown `- [ ]` although 02/03 have SUMMARYs and 04's work is otherwise evidenced | ⚠️ Hygiene finding, see below |

### Probe Execution

N/A — this phase has no `scripts/*/tests/probe-*.sh` deliverable; its verification instrument is the
SQL mutation-gate corpus (covered under Behavioral Spot-Checks and the reproduced `PROD-SESSION.md`
readings) and a production read no local probe can replay.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| `FANOUT-COHORT-PRIVATE-01` | 01, 02, 03, 04 | The ledger-refresh fan-out enqueues nothing because every PROD strategy is `private` | ⚠️ SUBSTANTIALLY SATISFIED, LEDGER NOT CLOSED | The defect is fixed and proven on PROD (truths 1–13, 15). The TODOS.md ledger entry itself, however, is explicitly not closed — see Gaps. Not a `REQUIREMENTS.md` id; the ROADMAP entry says so explicitly ("Requirements: TBD"). |
| `T-164.5.1-09-07` | 04 | The fan-out's all-candidates-failed branch, unblocked by this phase, to be exercised and closed | ✗ BLOCKED (within this phase) | See Gaps. Mitigated outside this phase's scope via arm R / PR #810 (Phase 164.5.1's own closure), disclosed there as a non-blocking substitution — but not delivered by 164.5.1.1 itself. |

No orphaned requirements: `.planning/REQUIREMENTS.md` carries no ids mapped to Phase 164.5.1.1.

### Anti-Patterns Found

None. No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`placeholder` markers in any file this phase modified.
No stub implementations, no hardcoded-empty data flowing to production behavior.

### Hygiene Finding (non-blocking, reported per instructions)

⚠️ **ROADMAP checkboxes for plans 02, 03, 04 are stale.** `.planning/ROADMAP.md`'s Phase 164.5.1.1
section shows only `164.5.1.1-01-PLAN.md` checked (`- [x]`); plans 02 and 03 show `- [ ]` despite
both having `-SUMMARY.md` files on disk, and plan 04 shows `- [ ]` despite its work being evidenced
in `PROD-SESSION.md` and `CHANGELOG.md`. Per this verifier's instructions, the ROADMAP is not edited
here — flagged for a human to correct.

## Human Verification Required

None. Every finding above was resolved by direct file inspection, `git log`/`git show`, or executing
the phase's own declared automated verify commands byte-for-byte. No visual, real-time, or
external-service judgment is needed to confirm the gaps.

## Gaps Summary

The phase's central engineering claim — "the fan-out enqueues work for strategies that actually
exist in production" — is true, proven on live PROD data, and independently corroborated across
three separate artifacts (`PROD-SESSION.md`, `CHANGELOG.md`, `git log`). Plans 01, 02 and 03 are each
fully and verifiably delivered.

The gap is narrow and concentrated entirely in **plan 04 task 3's closing paperwork**: the
`T-164.5.1-09-07` exercise/code-read, the `FANOUT-COHORT-PRIVATE-01` ledger closure, plan 03's
handed-over question, and the plan-04 SUMMARY were all left undone, and this verifier confirmed each
independently rather than taking `PROD-SESSION.md`'s own honest self-report at face value. The
underlying production risk this task was meant to close (`T-164.5.1-09-07`, DoS) was subsequently
mitigated — by a stronger, permanent mutation gate ("arm R") — but that happened through a different
mechanism, in a different phase's (164.5.1's) own closure, and was never folded back into this
phase's own records. `164.5.1.1-PROD-SESSION.md` itself says not to record the requirement as met;
this report honors that instruction rather than overriding it.

**This looks like a closeable, low-effort gap, not a rework.** A human/agent session could close it
by: (1) writing `164.5.1.1-04-SUMMARY.md` documenting what actually happened and explicitly
cross-referencing arm R / PR #810 as the mechanism that ultimately closed `T-164.5.1-09-07`; (2)
editing the `FANOUT-COHORT-PRIVATE-01` TODOS.md entry to record that closure and cite arm R rather
than pretending plan 04's original literal mechanism ran; (3) answering plan 03's handed-over
`api_key`-sharing question from a fresh, brief production read (or explicitly deferring it with an
owner/trigger, matching this repo's own deferral discipline).

**If the founder judges arm R's mitigation an acceptable substitute for plan 04's literal
requirement** (a defensible position — a permanent gate is arguably stronger than a one-off
exercise, and `164.5.1-SECURITY.md` already accepted this trade at `medium`, below the blocking
threshold), the following override would close this gap without further code changes:

```yaml
overrides:
  - must_have: "T-164.5.1-09-07 was exercised using the mechanism plan 08 of the previous phase already recorded, sequenced after the natural tick, and the outcome is recorded as either a live exercise or a code-read (CTX-12, plan 04 must_haves)"
    reason: "Superseded by a stronger, permanent mitigation: mutation-gate arm R in supabase/tests/test_ledger_refresh_fanout.sql, merged PR #810 as part of Phase 164.5.1's own closure, disclosed there as a non-blocking substitution (medium, below workflow.security_block_on=high) rather than compliance with the original literal mechanism."
    accepted_by: "<founder>"
    accepted_at: "<ISO timestamp>"
```

The remaining two gaps (the TODOS.md closure and plan 03's handed-over question) are not covered by
that override and should simply be finished — they cost a short session, not a replan.

---

_Verified: 2026-09-20T07:07:18Z_
_Verifier: Claude (gsd-verifier)_
